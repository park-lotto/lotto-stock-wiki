# 레퍼런스 영상 출처·정보 표시 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 렌즈 유사영상 패널과 제작소 재료 리스트의 영상 카드에 채널·구독자·지표·인기댓글·원문캡션을 표시한다(유튜브 자동, 나머지 수동 버튼).

**Architecture:** 기존 `youtube_client.py`(키 로테이션·`_stats`)를 확장해 채널정보·인기댓글·캡션을 추가하고, url을 키로 하는 `source_enrichment` 캐시 테이블(7일 TTL)에 저장한다. `POST /api/enrich`가 캐시 우선 조회 후 유튜브는 자동·비유튜브는 force일 때만 채운다. 프론트는 기존 카드 메타 표시부를 확장하고, IntersectionObserver로 유튜브 카드를 lazy 자동조회한다.

**Tech Stack:** Python(FastAPI, sqlite3), YouTube Data API v3, 순수 JS(프론트, 빌드 없음), pytest.

## Global Constraints

- 작업 위치: `.tracks/레퍼런스정보` 트랙 폴더. main 폴더 코드 직접수정 금지. 모든 git은 `git -C .tracks/레퍼런스정보`.
- 유튜브 조회는 `config.YOUTUBE_API_KEYS` 로테이션만 사용(신규 키 추가 금지).
- 비유튜브(틱톡·인스타·샤오홍슈·도우인)는 **관리자(customer_id===0)만** `force=true`로 조회(Apify 비용 통제). 이번 계획에선 Apify 실조회는 스텁 — 관리자 게이트·버튼만 완성.
- 카드 렌더는 어떤 조회 실패에도 막히지 않는다.
- 실 API 호출은 테스트에서 mock. 실검증은 서버 배포 후 실카드 1장.
- 캐시 키 = `url`. `mix_basket`에는 컬럼 추가 안 함.

---

### Task 1: 유튜브 URL → video_id 파서

**Files:**
- Modify: `shopping_shorts/youtube_client.py`
- Test: `shopping_shorts/tests/test_youtube_enrich.py` (create)

**Interfaces:**
- Produces: `video_id_from_url(url: str) -> str | None`

- [ ] **Step 1: Write the failing test**

```python
# shopping_shorts/tests/test_youtube_enrich.py
from shopping_shorts.youtube_client import video_id_from_url

def test_video_id_from_url_forms():
    assert video_id_from_url("https://www.youtube.com/watch?v=abc123DEF45") == "abc123DEF45"
    assert video_id_from_url("https://youtu.be/abc123DEF45") == "abc123DEF45"
    assert video_id_from_url("https://www.youtube.com/shorts/abc123DEF45") == "abc123DEF45"
    assert video_id_from_url("https://www.youtube.com/watch?v=abc123DEF45&t=10s") == "abc123DEF45"
    assert video_id_from_url("https://www.tiktok.com/@x/video/123") is None
    assert video_id_from_url("") is None
```

- [ ] **Step 2: Run test to verify it fails**

Run: `py -m pytest shopping_shorts/tests/test_youtube_enrich.py::test_video_id_from_url_forms -v`
Expected: FAIL — `ImportError: cannot import name 'video_id_from_url'`

- [ ] **Step 3: Write minimal implementation**

`youtube_client.py` 상단(import 아래)에 추가:

```python
import re as _re
_YT_ID = _re.compile(r"(?:v=|youtu\.be/|/shorts/)([A-Za-z0-9_-]{11})")

def video_id_from_url(url):
    """유튜브 watch/youtu.be/shorts URL → 11자 video_id. 유튜브 아니면 None."""
    if not url:
        return None
    m = _YT_ID.search(url)
    return m.group(1) if m else None
```

- [ ] **Step 4: Run test to verify it passes**

Run: `py -m pytest shopping_shorts/tests/test_youtube_enrich.py::test_video_id_from_url_forms -v`
Expected: PASS

- [ ] **Step 5: Commit**

```bash
git -C .tracks/레퍼런스정보 add shopping_shorts/youtube_client.py shopping_shorts/tests/test_youtube_enrich.py
git -C .tracks/레퍼런스정보 commit -m "feat(레퍼런스정보): youtube video_id_from_url 파서"
```

---

### Task 2: enrich_youtube — 채널·지표·인기댓글·캡션 조회

**Files:**
- Modify: `shopping_shorts/youtube_client.py`
- Test: `shopping_shorts/tests/test_youtube_enrich.py`

**Interfaces:**
- Consumes: `video_id_from_url` (Task 1), 기존 `YOUTUBE_API_KEYS`
- Produces: `enrich_youtube(url: str) -> dict | None`
  반환 dict 키: `platform`("youtube"), `channel_name`, `channel_url`, `subscribers`(int),
  `views`(int), `likes`(int), `comment_count`(int), `upload_date`(str, ISO), `caption`(str),
  `top_comments`(list[{author,text,likes}]). 조회 불가 시 `None`, 쿼터소진 시 예외 대신 `{"status":"quota"}`.

- [ ] **Step 1: Write the failing test**

```python
import shopping_shorts.youtube_client as yc

class _Resp:
    def __init__(self, data): self._d = data; self.status_code = 200
    def json(self): return self._d
    def raise_for_status(self): pass

def test_enrich_youtube_combines(monkeypatch):
    monkeypatch.setattr(yc, "YOUTUBE_API_KEYS", ["k1"])
    def fake_get(url, params=None, timeout=None):
        if "videos" in url:
            return _Resp({"items": [{"snippet": {"title": "T", "description": "설명전문 #해시",
                "channelId": "CID", "publishedAt": "2026-07-01T00:00:00Z"},
                "statistics": {"viewCount": "1000", "likeCount": "50", "commentCount": "7"}}]})
        if "channels" in url:
            return _Resp({"items": [{"snippet": {"title": "채널명", "customUrl": "@chan"},
                "statistics": {"subscriberCount": "12345"}}]})
        if "commentThreads" in url:
            return _Resp({"items": [{"snippet": {"topLevelComment": {"snippet":
                {"authorDisplayName": "A", "textDisplay": "댓글1", "likeCount": 3}}}}]})
        raise AssertionError(url)
    monkeypatch.setattr(yc.requests, "get", fake_get)
    r = yc.enrich_youtube("https://youtu.be/abc123DEF45")
    assert r["channel_name"] == "채널명"
    assert r["subscribers"] == 12345
    assert r["channel_url"] == "https://www.youtube.com/@chan"
    assert r["views"] == 1000 and r["likes"] == 50 and r["comment_count"] == 7
    assert r["caption"] == "설명전문 #해시"
    assert r["upload_date"].startswith("2026-07-01")
    assert r["top_comments"][0] == {"author": "A", "text": "댓글1", "likes": 3}

def test_enrich_youtube_non_youtube_url():
    assert yc.enrich_youtube("https://www.tiktok.com/@x/video/1") is None
```

- [ ] **Step 2: Run test to verify it fails**

Run: `py -m pytest shopping_shorts/tests/test_youtube_enrich.py -k enrich_youtube -v`
Expected: FAIL — `AttributeError: module ... has no attribute 'enrich_youtube'`

- [ ] **Step 3: Write minimal implementation**

`youtube_client.py`에 추가(기존 `_VIDEOS_URL` 옆에 URL 상수, 그리고 함수). 기존 파일이
`import requests` 하는지 확인하고 없으면 추가:

```python
_CHANNELS_URL = "https://www.googleapis.com/youtube/v3/channels"
_COMMENTS_URL = "https://www.googleapis.com/youtube/v3/commentThreads"

def _first_ok(url, params):
    """YOUTUBE_API_KEYS를 순서대로 시도. 쿼터/403이면 다음 키. 전부 실패면 None."""
    for tok in YOUTUBE_API_KEYS:
        try:
            r = requests.get(url, params={**params, "key": tok}, timeout=30)
            if r.status_code == 403:
                continue
            r.raise_for_status()
            return r.json()
        except Exception:
            continue
    return None

def enrich_youtube(url):
    vid = video_id_from_url(url)
    if not vid:
        return None
    vd = _first_ok(_VIDEOS_URL, {"part": "snippet,statistics", "id": vid})
    if not vd or not vd.get("items"):
        return {"status": "quota"} if vd is None else None
    it = vd["items"][0]; sn = it.get("snippet", {}); stt = it.get("statistics", {})
    channel_id = sn.get("channelId", "")
    cd = _first_ok(_CHANNELS_URL, {"part": "snippet,statistics", "id": channel_id}) if channel_id else None
    csn = (cd["items"][0]["snippet"] if cd and cd.get("items") else {})
    cst = (cd["items"][0]["statistics"] if cd and cd.get("items") else {})
    custom = csn.get("customUrl", "")
    channel_url = ("https://www.youtube.com/" + custom) if custom else (
        "https://www.youtube.com/channel/" + channel_id if channel_id else "")
    cm = _first_ok(_COMMENTS_URL, {"part": "snippet", "videoId": vid,
                                   "order": "relevance", "maxResults": 5})
    top = []
    for t in (cm.get("items", []) if cm else []):
        c = t["snippet"]["topLevelComment"]["snippet"]
        top.append({"author": c.get("authorDisplayName", ""),
                    "text": c.get("textDisplay", ""), "likes": int(c.get("likeCount") or 0)})
    return {
        "platform": "youtube",
        "channel_name": csn.get("title", ""),
        "channel_url": channel_url,
        "subscribers": int(cst.get("subscriberCount") or 0),
        "views": int(stt.get("viewCount") or 0),
        "likes": int(stt.get("likeCount") or 0),
        "comment_count": int(stt.get("commentCount") or 0),
        "upload_date": sn.get("publishedAt", ""),
        "caption": sn.get("description", ""),
        "top_comments": top,
    }
```

- [ ] **Step 4: Run test to verify it passes**

Run: `py -m pytest shopping_shorts/tests/test_youtube_enrich.py -k enrich_youtube -v`
Expected: PASS (2 tests)

- [ ] **Step 5: Commit**

```bash
git -C .tracks/레퍼런스정보 add shopping_shorts/youtube_client.py shopping_shorts/tests/test_youtube_enrich.py
git -C .tracks/레퍼런스정보 commit -m "feat(레퍼런스정보): enrich_youtube 채널·지표·인기댓글·캡션 조회"
```

---

### Task 3: source_enrichment 캐시 테이블 + store 메서드

**Files:**
- Modify: `shopping_shorts/store.py`
- Test: `shopping_shorts/tests/test_enrich_store.py` (create)

**Interfaces:**
- Produces:
  - `Store.upsert_enrichment(url, platform, data: dict, status: str, now: str) -> None`
  - `Store.get_enrichment(url, max_age_days=7, now=None) -> dict | None` (만료/없으면 None; dict엔 위 필드 + `status`)

- [ ] **Step 1: Write the failing test**

```python
# shopping_shorts/tests/test_enrich_store.py
from shopping_shorts.store import Store

def _store(tmp_path):
    return Store(str(tmp_path / "t.db"))

def test_enrichment_roundtrip_and_ttl(tmp_path):
    s = _store(tmp_path)
    data = {"channel_name": "채널", "subscribers": 100, "views": 9,
            "top_comments": [{"author": "A", "text": "c", "likes": 1}], "caption": "cap"}
    s.upsert_enrichment("http://u", "youtube", data, "ok", "2026-07-22T00:00:00")
    got = s.get_enrichment("http://u", max_age_days=7, now="2026-07-24T00:00:00")
    assert got["channel_name"] == "채널" and got["subscribers"] == 100
    assert got["top_comments"][0]["text"] == "c" and got["status"] == "ok"
    # 8일 지나면 만료 → None
    assert s.get_enrichment("http://u", max_age_days=7, now="2026-07-31T00:00:00") is None
    assert s.get_enrichment("http://none") is None
```

- [ ] **Step 2: Run test to verify it fails**

Run: `py -m pytest shopping_shorts/tests/test_enrich_store.py -v`
Expected: FAIL — `AttributeError: 'Store' object has no attribute 'upsert_enrichment'`

- [ ] **Step 3: Write minimal implementation**

store.py의 `_init`(CREATE TABLE 블록들 있는 곳, mix_basket 근처)에 테이블 추가:

```python
            c.execute("""
                CREATE TABLE IF NOT EXISTS source_enrichment (
                    url TEXT PRIMARY KEY,
                    platform TEXT,
                    channel_name TEXT, channel_url TEXT, subscribers INTEGER,
                    views INTEGER, likes INTEGER, comment_count INTEGER,
                    upload_date TEXT, top_comments_json TEXT, caption TEXT,
                    status TEXT, fetched_at TEXT
                )
            """)
```

Store 클래스에 메서드 추가(파일 상단에 `import json`, `from datetime import datetime, timedelta`가 이미 있으면 재사용):

```python
    def upsert_enrichment(self, url, platform, data, status, now):
        d = data or {}
        with self._conn() as c:
            c.execute("""
                INSERT INTO source_enrichment
                  (url, platform, channel_name, channel_url, subscribers, views, likes,
                   comment_count, upload_date, top_comments_json, caption, status, fetched_at)
                VALUES (?,?,?,?,?,?,?,?,?,?,?,?,?)
                ON CONFLICT(url) DO UPDATE SET
                  platform=excluded.platform, channel_name=excluded.channel_name,
                  channel_url=excluded.channel_url, subscribers=excluded.subscribers,
                  views=excluded.views, likes=excluded.likes, comment_count=excluded.comment_count,
                  upload_date=excluded.upload_date, top_comments_json=excluded.top_comments_json,
                  caption=excluded.caption, status=excluded.status, fetched_at=excluded.fetched_at
            """, (url, platform, d.get("channel_name",""), d.get("channel_url",""),
                  d.get("subscribers"), d.get("views"), d.get("likes"), d.get("comment_count"),
                  d.get("upload_date",""), json.dumps(d.get("top_comments") or [], ensure_ascii=False),
                  d.get("caption",""), status, now))

    def get_enrichment(self, url, max_age_days=7, now=None):
        with self._conn() as c:
            row = c.execute("SELECT platform, channel_name, channel_url, subscribers, views, likes, "
                            "comment_count, upload_date, top_comments_json, caption, status, fetched_at "
                            "FROM source_enrichment WHERE url=?", (url,)).fetchone()
        if not row:
            return None
        fetched_at = row[11]
        if max_age_days is not None and fetched_at:
            now_dt = datetime.fromisoformat(now) if now else datetime.utcnow()
            try:
                if now_dt - datetime.fromisoformat(fetched_at) > timedelta(days=max_age_days):
                    return None
            except ValueError:
                pass
        return {"platform": row[0], "channel_name": row[1], "channel_url": row[2],
                "subscribers": row[3], "views": row[4], "likes": row[5],
                "comment_count": row[6], "upload_date": row[7],
                "top_comments": json.loads(row[8] or "[]"), "caption": row[9],
                "status": row[10]}
```

> 주의: `self._conn()`은 store.py의 기존 커넥션 헬퍼명에 맞춰라(파일에서 확인). 다르면 그 이름 사용.

- [ ] **Step 4: Run test to verify it passes**

Run: `py -m pytest shopping_shorts/tests/test_enrich_store.py -v`
Expected: PASS

- [ ] **Step 5: Commit**

```bash
git -C .tracks/레퍼런스정보 add shopping_shorts/store.py shopping_shorts/tests/test_enrich_store.py
git -C .tracks/레퍼런스정보 commit -m "feat(레퍼런스정보): source_enrichment 캐시 테이블+store 메서드"
```

---

### Task 4: POST /api/enrich 엔드포인트

**Files:**
- Modify: `shopping_shorts/app.py`
- Test: `shopping_shorts/tests/test_api_enrich.py` (create)

**Interfaces:**
- Consumes: `enrich_youtube`(Task 2), `store.get_enrichment/upsert_enrichment`(Task 3)
- Produces: `POST /api/enrich` body `{url, platform, force?}` → JSON.
  - 캐시 히트(7일 이내) → 그대로 반환(재조회 안 함).
  - 유튜브 미스 → `enrich_youtube` 조회·캐시·반환(`status:"ok"` 또는 `"quota"`/`"no_data"`).
  - 비유튜브 & `force!=true` → `{"status":"needs_manual"}`(캐시 안 함).
  - 비유튜브 & `force=true` & **비관리자** → `_require_admin` 403.

- [ ] **Step 1: Write the failing test**

```python
# shopping_shorts/tests/test_api_enrich.py
from fastapi.testclient import TestClient
import shopping_shorts.app as app_mod

def _client(monkeypatch, tmp_path):
    monkeypatch.setattr(app_mod, "store", app_mod.Store(str(tmp_path / "t.db")))
    return TestClient(app_mod.app)

def test_enrich_youtube_auto_then_cache(monkeypatch, tmp_path):
    calls = {"n": 0}
    def fake_enrich(url):
        calls["n"] += 1
        return {"platform": "youtube", "channel_name": "채널", "subscribers": 5,
                "views": 9, "likes": 1, "comment_count": 2, "upload_date": "2026-07-01",
                "caption": "cap", "top_comments": [{"author": "A", "text": "c", "likes": 0}],
                "channel_url": "https://youtube.com/@c"}
    monkeypatch.setattr(app_mod, "enrich_youtube", fake_enrich)
    c = _client(monkeypatch, tmp_path)
    r1 = c.post("/api/enrich", json={"url": "https://youtu.be/abc123DEF45", "platform": "youtube"})
    assert r1.json()["channel_name"] == "채널" and r1.json()["status"] == "ok"
    r2 = c.post("/api/enrich", json={"url": "https://youtu.be/abc123DEF45", "platform": "youtube"})
    assert r2.json()["channel_name"] == "채널"
    assert calls["n"] == 1  # 두 번째는 캐시 히트

def test_enrich_non_youtube_needs_manual(monkeypatch, tmp_path):
    c = _client(monkeypatch, tmp_path)
    r = c.post("/api/enrich", json={"url": "https://www.tiktok.com/@x/video/1", "platform": "tiktok"})
    assert r.json()["status"] == "needs_manual"

def test_enrich_non_youtube_force_requires_admin(monkeypatch, tmp_path):
    # 비관리자(customer_id!=0)가 force로 비유튜브 조회 → 403
    c = _client(monkeypatch, tmp_path)
    # 인증 OFF면 전원 admin(customer_id=0)이 되어 이 테스트가 무의미해지므로,
    # test_paywall_admin.py의 '비관리자 세션' 픽스처/미들웨어 세팅을 그대로 빌려 customer_id!=0을 만든다.
    r = c.post("/api/enrich", json={"url": "https://www.tiktok.com/@x/video/1",
                                    "platform": "tiktok", "force": True})
    assert r.status_code == 403
```

> 인증 미들웨어가 막으면 기존 테스트(`test_app.py`)가 쓰는 인증 우회 픽스처/헤더 패턴을 그대로 적용하라. 관리자/비관리자 구분 픽스처는 `test_paywall_admin.py`를 참고(customer_id=0=사장님).

- [ ] **Step 2: Run test to verify it fails**

Run: `py -m pytest shopping_shorts/tests/test_api_enrich.py -v`
Expected: FAIL — 404 (엔드포인트 없음)

- [ ] **Step 3: Write minimal implementation**

app.py 상단 import에 추가: `from shopping_shorts.youtube_client import enrich_youtube`.
기존 `/api/mix/basket` 엔드포인트들 근처에 추가(now는 기존 코드의 시각 헬퍼가 있으면 재사용):

```python
@app.post("/api/enrich")
async def api_enrich(req: Request):
    body = await req.json()
    url = (body.get("url") or "").strip()
    platform = (body.get("platform") or "").lower()
    force = bool(body.get("force"))
    if not url:
        return {"status": "no_data"}
    cached = store.get_enrichment(url)
    if cached:
        return {"ok": True, **cached}
    now = datetime.utcnow().isoformat()
    if platform == "youtube" or "youtu" in url:
        data = enrich_youtube(url)
        if not data:
            store.upsert_enrichment(url, platform, {}, "no_data", now)
            return {"status": "no_data"}
        if data.get("status") == "quota":
            return {"status": "quota"}
        store.upsert_enrichment(url, "youtube", data, "ok", now)
        return {"ok": True, "status": "ok", **data}
    if not force:
        return {"status": "needs_manual"}
    denied = _require_admin(req)   # 비유튜브 실조회는 관리자만
    if denied:
        return denied
    # 비유튜브 force(관리자): Apify 경로 — 현 단계는 미구현 스텁(후속 계획)
    return {"status": "no_data"}
```

> `datetime`이 app.py에 import 안 돼 있으면 `from datetime import datetime` 추가.
> `_require_admin`은 `request.state.customer_id != 0`이면 403 JSONResponse 반환(app.py:4023). 위에서 핸들러 인자가 `req: Request`이므로 `_require_admin(req)`로 넘긴다.

- [ ] **Step 4: Run test to verify it passes**

Run: `py -m pytest shopping_shorts/tests/test_api_enrich.py -v`
Expected: PASS (2 tests)

- [ ] **Step 5: Commit**

```bash
git -C .tracks/레퍼런스정보 add shopping_shorts/app.py shopping_shorts/tests/test_api_enrich.py
git -C .tracks/레퍼런스정보 commit -m "feat(레퍼런스정보): POST /api/enrich 유튜브자동+캐시+비유튜브 needs_manual"
```

---

### Task 5: 렌즈 카드 정보 표시 + 유튜브 lazy 자동조회 (index.html)

**Files:**
- Modify: `shopping_shorts/static/index.html` (렌즈 카드 빌더 ~1123–1145, `renderLens` 부근)

**Interfaces:**
- Consumes: `POST /api/enrich`(Task 4)
- Produces: 카드 하단 `<div class="enrich" data-url data-platform>` 슬롯 + `enrichCard(el)` JS + IntersectionObserver

- [ ] **Step 1: 카드 빌더에 enrich 슬롯 추가**

`renderLens`의 카드 템플릿에서 `🔗 원본 열기` 줄 아래에 삽입:

```html
        <div class="enrich" data-url="${esc(i.url||'')}" data-platform="${esc(plabel||'')}"
             style="font-size:11px;color:var(--sub);margin-top:6px"></div>
```

- [ ] **Step 2: enrichCard 함수 추가**

index.html의 `<script>` 안(renderLens 근처)에 추가:

```javascript
async function enrichCard(el){
  if(el.dataset.done) return;
  const url=el.dataset.url, pf=(el.dataset.platform||'').toLowerCase();
  const isYt = pf.includes('유튜브') || url.includes('youtu');
  if(!isYt){
    // 비유튜브 실조회는 Apify 유료 → 관리자(customer_id===0)만 버튼 노출. 비관리자는 빈 슬롯.
    if(window.IS_ADMIN){
      el.innerHTML=`<button onclick="forceEnrich(this.parentNode)" style="font-size:11px;background:none;border:1px solid var(--sub);color:var(--sub);border-radius:8px;padding:2px 8px;cursor:pointer">ℹ️ 정보 채우기</button>`;
    }
    return;
  }
  el.dataset.done="1"; el.textContent="…";
  try{
    const d=await (await fetch('/api/enrich',{method:'POST',headers:{'Content-Type':'application/json'},
      body:JSON.stringify({url, platform:'youtube'})})).json();
    el.innerHTML=renderEnrich(d);
  }catch(e){ el.textContent=""; el.dataset.done=""; }
}
async function forceEnrich(el){
  el.innerHTML="…";
  const d=await (await fetch('/api/enrich',{method:'POST',headers:{'Content-Type':'application/json'},
    body:JSON.stringify({url:el.dataset.url, platform:(el.dataset.platform||'').toLowerCase(), force:true})})).json();
  el.innerHTML=renderEnrich(d);
}
function renderEnrich(d){
  if(!d || (d.status && d.status!=='ok')){
    const msg={quota:'정보 없음(쿼터)', no_data:'정보 없음', needs_manual:''}[d&&d.status]||'정보 없음';
    return msg?`<span style="color:var(--sub)">${msg}</span>`:'';
  }
  const ch = d.channel_name?`👤 <a href="${d.channel_url||'#'}" target="_blank" style="color:var(--accent)">${esc(d.channel_name)}</a>${d.subscribers?(' · 구독 '+_fmtCount(d.subscribers)):''}`:'';
  const stat = [d.views!=null?('👁 '+_fmtCount(d.views)):'', d.likes!=null?('❤ '+_fmtCount(d.likes)):'', d.comment_count!=null?('💬 '+_fmtCount(d.comment_count)):'', d.upload_date?('📅 '+esc((d.upload_date||'').slice(0,10))):''].filter(Boolean).join('  ·  ');
  const cmts = (d.top_comments||[]).slice(0,5).map(c=>`<div style="margin:2px 0">💬 <b>${esc(c.author)}</b> ${esc((c.text||'').slice(0,120))}</div>`).join('');
  const cmtBlock = cmts?`<details style="margin-top:4px"><summary style="cursor:pointer">인기 댓글 ${d.top_comments.length}</summary>${cmts}</details>`:'';
  const capBlock = d.caption?`<details style="margin-top:4px"><summary style="cursor:pointer">원문 캡션</summary><div style="white-space:pre-wrap;margin-top:2px">${esc(d.caption)}</div></details>`:'';
  return `${ch?('<div>'+ch+'</div>'):''}${stat?('<div style="margin-top:2px">'+stat+'</div>'):''}${cmtBlock}${capBlock}`;
}
```

- [ ] **Step 2b: 관리자 플래그 세팅(비유튜브 버튼 게이트용)**

index.html에 이미 있는 `/api/me` admin 감지(line ~1721) 옆/안에 `window.IS_ADMIN` 세팅을 보장한다. 없으면 추가:

```javascript
window.IS_ADMIN=false;
fetch('/api/me').then(r=>r.ok?r.json():null).then(d=>{ if(d&&d.customer_id===0) window.IS_ADMIN=true; }).catch(()=>{});
```

> 기존 line 1722 fetch가 이미 `d.customer_id===0`을 검사하므로, 그 then 블록 안에서 `window.IS_ADMIN=true`만 추가해도 된다(중복 fetch 피함).

- [ ] **Step 3: renderLens 뒤에 IntersectionObserver 연결**

`box.innerHTML=...; box._lensCurSC=shortcode;` 다음 줄에 추가:

```javascript
  const io=new IntersectionObserver((ents)=>{ents.forEach(e=>{ if(e.isIntersecting){ enrichCard(e.target); io.unobserve(e.target);} });},{rootMargin:'100px'});
  box.querySelectorAll('.enrich').forEach(el=>io.observe(el));
```

- [ ] **Step 4: 수동 브라우저 검증(실 API 없이 UI 골격)**

Run(로컬 서버 기동 후): 렌즈 유사영상을 열어 유튜브 카드에 채널/지표/댓글 슬롯이 나타나는지 확인. 실 데이터는 서버 배포 후 확인(로컬은 쿼터 키 없을 수 있음). `esc`·`_fmtCount`가 index.html에 이미 정의돼 있는지 확인(있음).
Expected: 유튜브 카드에 "…" 후 채널·지표 또는 "정보 없음"이 뜬다. 비유튜브 카드엔 "ℹ️ 정보 채우기" 버튼.

- [ ] **Step 5: Commit**

```bash
git -C .tracks/레퍼런스정보 add shopping_shorts/static/index.html
git -C .tracks/레퍼런스정보 commit -m "feat(레퍼런스정보): 렌즈 카드 채널·지표·인기댓글·캡션 표시+유튜브 lazy 자동조회"
```

---

### Task 6: 제작소 재료 리스트 정보 표시 (produce.html)

**Files:**
- Modify: `shopping_shorts/static/produce.html` (재료 리스트 = mix_basket 렌더부)

**Interfaces:**
- Consumes: `POST /api/enrich`(Task 4), Task 5의 `enrichCard/renderEnrich`(produce.html에 동일 함수 복제 — 별도 파일이라 공유 불가)

- [ ] **Step 1: 재료 카드 렌더부 찾기**

Run: `grep -nE "넘어온 영상|재료|basket|영상 [0-9]|원본 ↗" shopping_shorts/static/produce.html | head`
→ mix_basket 항목을 그리는 함수(예: `renderMaterials`/basket 목록 map)를 특정.

- [ ] **Step 2: 각 재료 카드에 enrich 슬롯 추가**

재료 항목 템플릿(제목·원본↗ 링크 있는 곳)에 삽입(url·platform은 basket 아이템 필드 사용, 필드명은 `/api/mix/basket` 응답 확인 — `url`, 플랫폼은 url로 추정):

```html
        <div class="enrich" data-url="${it.url||''}" data-platform="${(it.url||'').includes('youtu')?'유튜브':''}"
             style="font-size:11px;color:var(--sub);margin-top:4px"></div>
```

- [ ] **Step 3: enrichCard/forceEnrich/renderEnrich 함수 복제**

Task 5 Step 2의 세 함수를 produce.html `<script>`에 그대로 복제. **`window.IS_ADMIN` 세팅도 함께 복제**(Task 5 Step 2b — produce.html은 별도 페이지라 index.html 플래그를 공유 못 함). `esc`/`_fmtCount`가 produce.html에 없으면 최소 정의 추가:

```javascript
function esc(s){return (s==null?'':(''+s)).replace(/[&<>"]/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;'}[m]));}
function _fmtCount(n){n=+n||0;return n>=10000?(n/10000).toFixed(1)+'만':(n>=1000?(n/1000).toFixed(1)+'천':''+n);}
```

- [ ] **Step 4: 재료 렌더 직후 IntersectionObserver 연결**

재료 리스트를 그린 컨테이너에 대해 Task 5 Step 3과 동일한 IO 코드를 붙인다(컨테이너 변수명만 교체).

- [ ] **Step 5: 수동 브라우저 검증**

Run: 제작소에서 유튜브 재료가 넘어온 상태로 재료 리스트를 열어 채널·지표 슬롯이 뜨는지 확인.
Expected: 유튜브 재료에 채널/지표, 비유튜브에 "정보 채우기" 버튼.

- [ ] **Step 6: Commit**

```bash
git -C .tracks/레퍼런스정보 add shopping_shorts/static/produce.html
git -C .tracks/레퍼런스정보 commit -m "feat(레퍼런스정보): 제작소 재료 리스트 출처·지표 표시"
```

---

## 최종 게이트

- [ ] `py -m pytest shopping_shorts/tests/test_youtube_enrich.py shopping_shorts/tests/test_enrich_store.py shopping_shorts/tests/test_api_enrich.py -v` 전체 PASS
- [ ] `py tools/track.py finish 레퍼런스정보` (문법→import→pytest 게이트, 기준선 대비 실패 증가 없어야 병합)
- [ ] 서버 배포(3분) 후 라이브에서 유튜브 카드 1장 실 데이터 표시 확인

## 후속(YAGNI, 이번 계획 밖)

- Task 4의 Apify 스텁 → 틱톡·인스타·샤오홍슈·도우인 실제 `enrich_apify` 구현(별도 계획).
- 인기댓글 감성/요약, 지표 시계열.
