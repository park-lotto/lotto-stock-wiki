# 핸드오프 — 브랜딩 트랙

## 2026-07-23 (집PC) 세션 요약

숏템박스 브랜딩: 로고 v2 + PWA 설치 + 런처 로그인. **v2까지 전부 라이브 반영 완료.**

### ✅ 완료 (라이브)
- 로고 v2: 다크 조리개 디스크+민트 링+스택 3단(TOP 골드)+상승 화살촉
  - 원본·아이콘 산출물: `C:\Users\CH\AppData\Local\ShotemBox\` (shotembox_logo_v2.png / shotembox_v2.ico / 프리뷰)
  - 생성 스크립트는 세션 스크래치패드(임시)였음 — 재생성 필요하면 PIL로 다시 (2048 슈퍼샘플→1024)
- PWA: `static/manifest.webmanifest` + icon-192/512·maskable·apple-touch·favicon + 전 페이지 태그 주입
  (공개페이지=app.py 인라인 head, 앱페이지=sidebar.js 동적 주입) + `_AUTH_ALLOW`에 매니페스트·아이콘 공개
  - ⚠️ 정적마운트는 루트(`app.mount("/")`) — 아이콘 URL은 `/icon-512.png` 식 루트 경로 (처음에 /static/으로 틀렸었음)
  - ⚠️ sidebar.js 주입은 **setAttribute 금지** — test_sidebar_works의 미니DOM에 없어 13건 깨짐 → 프로퍼티 할당으로
- 페이지 내 로고 통일: `_LOGO_SVG`(대문·로그인·요금) + sidebar.js(🛍️→인라인 SVG, id는 ss- 접두어)
- 런처 로그인 v2: 카드+글로우 엠블럼+STUDIO 표기+부팅 게이지(4단계)→로그인 페이드인
- 바탕화면 바로가기: `C:\Users\CH\Desktop\숏템박스.lnk` (Edge --app, 아이콘 v2)

### ⏭ 미완 — 런처 v3 (사장님 요구, 유튜브 촬영용 "있어보이게")
1. **부팅을 엔진 도는 느낌으로**: 터미널식 모듈 로드 로그(코어 부팅→AI 엔진 초기화→트렌드 동기화→대본 모듈→렌더 웜업→보안 채널→준비 완료) + %카운터 + 엠블럼 둘레 링 스피너 + 흐르는 그라데이션 바
2. **아이디·비번 폼을 메인으로** (구글은 "또는" 아래 보조 버튼): `/api/login`이 이미 관리자(env)+고객(DB `verify_customer`) 둘 다 검증하고 성공 시 `/`(레퍼런스 홈) 303 — 백엔드 수정 불필요. ⚠️진짜 구글 비번 검증은 불가(피싱) — 자체 계정 폼으로 가는 걸로 사장님께 설명 완료
3. **랜딩 → 작은 팝업 로그인**: 랜딩의 `a[href="/login"]` 클릭 → `window.open('/login','stb_login','width=430,height=780,...')`, 팝업 차단 시 일반 이동 폴백
4. **팝업 핸드오프**: sidebar.js IIFE 초입에 — `window.name==='stb_login' && window.opener` 이면 opener를 현재 URL로 보내고 `window.close()` (폼 303이든 구글 콜백이든 `/` 로드 시점에 잡힘). + 앱 작은 창이면 `resizeTo(1500,950)` 시도(막히면 무시). ⚠️미니DOM 안전: `window.innerWidth<700` 단락평가를 matchMedia보다 앞에
5. **바로가기 작은 창**: `숏템박스.lnk` Arguments를 `--app=.../login --window-size=430,780` 으로 (WScript.Shell로 갱신)
6. ✅(이미 커밋됨, 이 세션) `/login` GET에 로그인 세션이면 `/`로 303 — 앱이 /login을 열어도 재로그인 강요 안 함

**작업 재개 시 주의**: v3 템플릿 전체교체 Edit이 old_string 불일치로 한 번 실패함 —
`_LOGIN_TMPL`을 먼저 Read로 현재 상태 그대로 뜬 뒤 교체할 것. 검증은
`py -m uvicorn shopping_shorts.app:app --port 8871` + Playwright 스크린샷으로(이번 세션 방식).
관련 테스트: test_sidebar_works(13)·test_brand_logo_shottembox·test_paywall_gate/google·test_app — 전부 통과 상태 유지할 것.

### 관련 파일
- `shopping_shorts/app.py` — `_LOGIN_TMPL`(런처)·`_LANDING_TMPL`·`_LOGO_SVG`·`_AUTH_ALLOW`·`/manifest.webmanifest` 라우트·`_login_page`
- `shopping_shorts/static/sidebar.js` — PWA 주입 + 브랜드 SVG (+v3에서 팝업 핸드오프 추가 예정)
- `shopping_shorts/static/manifest.webmanifest`, icon-*.png, favicon.ico, apple-touch-icon.png
