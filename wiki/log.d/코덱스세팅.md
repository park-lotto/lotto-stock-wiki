
## 2026-09-09 Codex CLI 도입
- 설치·로그인·샌드박스 승인 완료(`codex-cli 0.153.4`, ChatGPT 구독, 모델 `gpt-5.6-sol`/필요시 `gpt-6-astra`).
- `AGENTS.md` 추가 — Codex가 CLAUDE.md를 통째로 읽고 **트랙을 스스로 만들게** 함. 실제로 시켜 순서대로 답하는 것 확인.
- `코덱스.bat` + `track_open.py --agent codex` — 여는 절차는 한 곳으로 공용화(0순위-B).
- cmd 켜면 자동 실행(AutoRun, `cmd /c`는 제외하는 안전장치 검증). 바탕화면 바로가기도 생성.
- 함정: `codex exec`는 항상 read-only / 브라우저는 크롬 확장을 사람이 직접 깔아야 함 / `.agents/`는 gitignore라 PC마다 SKILL.md 머리말 수정 필요.
- 새 PC 설치 순서는 `handoff/코덱스세팅.md` 4단계 그대로.
