
## 2026-09-09 — 박세현님 job 57722281732e (work=dbb99123b5ca)
제보: 자막제거 후 6단계 장면꾸미기 배경에 원본 자막(“하루 자고 간다고 하길래”·[광고])이 그대로.

**서버는 정상이었다.** `_beatframe_file`은 청소본에서 뜨고 있었고(`5_c1_s8@3.34_clean.jpg`,
실물 확인 → 자막 없음), 화면에 나간 건 **청소 전 23:38에 뽑혀 브라우저에 남아 있던 `_src` 그림**.
주소(`/beatframe/{job}/{i}?cut={c}`)가 청소 전후로 똑같아 `<img>`가 다시 받을 이유가 없었다.

수정(main `25b04e3d5`): `_frame_cache_key`(청소 출처·mtime 해시)를 `beats_preview`가 `fkey`로
내려주고 `_beatFrameUrl`이 `?v=`로 붙인다 → 청소본이 생기거나 바뀌면 주소가 바뀐다.

라이브 실측(배포·재시작 후): fkey=`336992057fef`, 19칸, `5?cut=1&v=…` → 60,613B =
`_clean.jpg`(자막 없음). `_src`(68,374B) 아님.

곁: 옛 테스트 `test_서명_청소본이_없으면_종전대로_판정한다`는 09-02에 바뀐 계약(옛 청소본이라도
쓴다)과 어긋나 있었다 → 갱신.
