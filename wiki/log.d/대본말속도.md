# 대본말속도

## 2026-09-18
- ★**원인 확정: 「말 밀도」 창이 모델 실제 출력과 안 맞는다.**
  실패 사례의 tries가 전부 말 밀도다(257→210→218→202 재단, 4회 전부 실패).
  **살아남은 안조차 게이트를 못 넘고 fail-open으로 나간다** — 옆 스타일은 같은 조건에서 빈손.
  저장된 안 764개 첫 시도 글자수 **중앙 203자**, **창 상한 185 초과가 68%**.
  실패 사유 누적: 말 밀도 1,521회 / 고조 심화 291 / 수치 근거 152.
- 배제된 원인(전부 실측): 키 소진·429 **로그 0건** / 게이트 정식 반려 **하루 5건**뿐 /
  스타일 순서 무관(19 vs 16) / grounded 차이 아님(관리자도 59%) / 재료 재현 시 2/2 나옴.
- 규모 정정: 앞서 보고한 "76%"는 생성 호출 단위라 다른 걸 셌다. **사용자가 2안을 요청한
  경우만 보면 449회 성공 / 56회 실패 = 실패율 11%**. 그래도 11명 중 1명꼴.
- 박복래 첫 생성(23:24)은 `script_drafts` 기준 **1행(1안)**, 23:29 재시도가 2행.
  `produce_works.state_json`은 마지막 시도로 덮여 첫 실패가 안 보인다 — 내가 "2개 정상"이라
  결론낸 건 두 번째 결과를 본 것이었다. **사장님 말씀이 맞았다.**
- 내가 틀린 것 5번: 폴백 범인설·천장 이중적용설·말속도 9.69설(실측 7.42)·
  "말 밀도는 fail-open이라 안 죽는다"설·"시스템 문제 아님"설.
- 곁가지 수리 2건(대본 개수와 무관, 표시 초만 정확해짐 — 영상 결과물 불변):
  ① narr_secs가 분자만 raw → 계획 초 30% 과대(791→614초, 실제 608). 커밋 70f50d684.
  ② 화면 JS가 배속 미적용 → **라이브 실측 190자가 33초 표시(실제 25.6초)** → 26초. 커밋 14148a3ec.
- 조사 팁: `script_drafts`를 봐라(최종본은 덮인다) / 대본 생성은 **본체**, mix는 **워커** /
  서버 grep에 한글 쓰면 `Invalid collation character` → `LC_ALL=C grep -F`.
- ★**2층 원인: 사유가 버려진다.** `app.py:3630` `_gen_reasons`는 **0안일 때만** 화면에 나가고,
  1안이면 그대로 버려진 채 200 OK를 준다. 서버는 어느 스타일이 왜 빠졌는지 알면서 말하지 않는다
  — 이것이 08-17 이후 8번 재발하며 매번 다른 답이 나온 구조적 이유다.
- 치명 검사 가설도 기각: 1안 39건 중 치명흔적 8% vs 2안 220건 중 10%(차이 없음).
  `사실 근거`는 FATAL 목록엔 있으나 active엔 없어(스위치 off) 59회 실패해도 안 죽인다.
- ⏭ 창을 실제 출력(중앙 203)에 맞출지 프롬프트를 조일지 결정(영상 길이 영향) +
  1안일 때도 사유를 남기게 하기. finish 안 함.
