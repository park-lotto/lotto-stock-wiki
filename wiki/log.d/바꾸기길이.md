
## 2026-08-22
- [바꾸기] 길이 폭주 차단: 상한 `max(per*1.8, per+40)`이 **거의 언제나 +40이 이겨** 게이트가
  사실상 무력(10자 칸이 5배까지 허용). 1.35배 + 12자로 조이고 두 군데 중복식을
  `_beat_len_cap()` 하나로 통합. 프롬프트에 상한을 **숫자로** 명시(재시도가 성공하도록).
- "생성 실패(키 소진 또는 응답 오류)"가 거짓말이던 것 수정 — 로그에 429가 없는데도 그 문구가
  떠서 사장님이 키 회복을 헛되이 기다렸다. `_call_json`이 모든 실패를 `{}`로 뭉개던 것을
  사유별로 올려 6가지로 갈라 말한다. 회귀 0(인자 안 주면 종전 동작).
- 검증: 기준선 2 failed/5037 passed = 수정 후 동일. 라이브 DB는 reference.db임을 확인.
