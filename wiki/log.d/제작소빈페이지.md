# 제작소빈페이지

## 2026-08-24 (회사PC)
- 사이드바 '숏템 제작소'가 직전/계정 최신작업을 열던 것을 **항상 빈 작업**(`?new=1`)으로 변경.
  원인은 버그가 아니라 2026-07-26 크로스기기 싱크(`_bootAccountLatestOrLocal`)의 의도된 동작이었다.
- ★`href`에 `?new=1`을 박으면 active 판정(`it.href === path`, 쿼리 없는 pathname)과 유료게이트
  `data-ss-href`가 동시에 깨진다 → `go` 필드를 따로 두고 클릭 목적지만 바꿨다. 사보타주로 실측 확인.
- 회귀 테스트 4개 신설. `pytest -k "sidebar or nav or produce"` 352 passed / 0 failed. 라이브 병합 `5804e9a3b`.

## 2026-08-24 (2차)
- 1단계 빈 영상풀에 재료 가지러 갈 버튼 2개(📊 랭킹 `/` · ⭐ 즐겨찾기 `/collection`). 글만 있던 자리.
- 버튼은 `#poolEmpty` **안**에 — renderPool이 그 하나만 토글한다. 밖으로 새면 재료 담은 뒤에도 남는다(실측).
- ★마크업 테스트를 문자열 `in`으로 쓰면 무용지물이다: 첫 버전은 버튼을 밖으로 빼도 통과했다.
  HTMLParser 부분트리로 교체. 공태그 미제외 → 버튼 57개, 음수 센티널만으론 안 멈춤 → done 플래그.
- playwright 실측(1600/390px, 클릭, 토글) + 387 passed / 0 failed. 라이브 MD5 일치.
