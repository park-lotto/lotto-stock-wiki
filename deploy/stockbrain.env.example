# /etc/stockbrain.env  (서버에만 둠 — git에 올리지 말 것)
# 대시보드 로그인 자격증명. 비어있으면 인증 OFF(로컬)라서 배포 시 반드시 설정.
DASH_USER=admin
DASH_PASS=여기에_원하는_비밀번호
# 세션 서명 비밀키 — 서버에서 랜덤 생성: openssl rand -hex 32
DASH_SECRET=CHANGE_ME_openssl_rand_hex_32
