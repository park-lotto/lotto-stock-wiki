# 자막길이표시 로그

## 2026-08-17 (회사 PC)
- 제작소 2단계 칸 헤더 길이 표기를 `5.1초`(합계 하나) → **`담은재료/자막` 짝**으로 바꿈.
  `scene_lab.html:1027` renderBand. 라이브 병합 완료(b64ee3c77 → main 5e79ddca4).
- ★핵심 발견: **`sec/need`로 짝을 만들면 의미가 없다.** `planClips`(scene_play.js:241)가
  부족분을 반드시 0으로 만들기 때문 — 토글 OFF면 마지막 컷에 몰고(`clips[last].dur += short`),
  ON이면 전 컷 비례배분(`c.dur *= ttsDur/filled`). 즉 **합계는 언제나 자막 길이와 같아**
  늘 `5.0/5.0`만 뜬다. 사장님이 기대한 `4.9/5`는 원래 안 나오는 값이었다.
  → 왼쪽을 **늘리기 전 진짜 재료**(`effLen` 합)로 두어야 정보가 생긴다.
- 4상태: `2.5/5.0 · 2.5초 모자람`(빨강) / `2.5/5.0 · 늘려 채움`(노랑) /
  `5.0/5.0`(초록) / `0.0/5.0 · 장면 없음`(빨강).
- ★함정: 컷 0장이면 `planClips`가 `clips.length` 검사로 보정을 **통째로 건너뛴다** →
  토글이 켜져 있어도 실제론 무음 화면. `STRETCH[i] && clips.length > 0`로 갈라야
  "늘려 채움"이 거짓말이 안 된다.
- 검증: node --check green / 4케이스 실측(부족·딱맞음·남음·0장) /
  test_merge_split_playback 5 passed / **기준선 대조(git stash)** — `test_edit_plan_build`
  1건 실패는 변경 전에도 동일(회귀 0).
- ⏭ 브라우저 실화면 육안 확인(사장님). 아래 편집 뷰(`scene_lab.html:1197~`)는 이미
  `멘트 N초 · N초 부족` 표기가 따로 있어 **두 곳이 같은 판단을 두 번 적고 있다** —
  0순위-B 대상. 나중에 한 함수로 뽑을 후보.
