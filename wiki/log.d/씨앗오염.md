# 씨앗오염

## 2026-08-18
- 제작소 2단계가 옛 작업의 씨앗(`s2.seed`)을 물고 와 카메라 5편 담은 작업에서 다이소 청소 대본이 나오던 버그 수정·배포(`2d25652d7`). `_s2Hydrate`가 drafts만 지우고 seed를 남긴 것 + `s2RenderSeeds`의 `!S2.seed` 가드가 새 AI PICK을 무시한 것, 두 곳이 짝으로 안 움직인 게 원인(0순위-B).
- 회귀 테스트 2종 신설(`test_s2_seed.js` 12/12, `test_s2_restore.js` 7/7). 수정 전 코드로는 4건 FAIL하는 것까지 확인 — 반사실 대조.
- ⏭ 미수정: 구글 Gemini 5xx(일시장애)를 `failed_empty`로 래치해 영상이 영영 재시도 안 되는 문제(`prewarm.py:143`에 `KeyPoolExhausted`만 롤백 대상). 랭킹 0건은 이 트랙과 무관 — 수집 미실행(크론 없음, 수동).
