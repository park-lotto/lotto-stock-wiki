# 자막제거장면선택 — 핸드오프

## 2026-09-26 (PC: CH) — 장면 골라 지우기 구현·LAB·화면 QA 완료, **라이브 미배포(승인 대기)**

### 사장님 요청
"고급 요금이 초당 과금인데 몇 장면만 지우면 되는 경우가 있다 — 넘겨보며 지울 장면만 골라 지우게."
+ "장면꾸미기·렌더·캡컷에서 **다음 장면에 영향 주는 것**까지 다시 계산해보고 진행."

### 구조 (핵심 결정)
- 고른 컷 구간만 조립본(mix_raw)에서 프레임 번호로 잘라 이어 **업체 1콜** → 돌려받은 걸 **같은 프레임 자리에 되붙임**
  (`mix_pipeline._clean_partial`). 결과 길이·프레임 수 = 조립본 그대로 → 청소본 정본·렌더·캡컷·꾸미기 프레임이
  기존 코드 그대로 돈다. **다음 장면이 밀릴 구조가 없다.**
- 선택 = 컷 키 `beat|video_id|원본초`(허용오차 0.35초) → `mix_jobs.clean_cuts_json`(NULL=전체, 옛 job 불변).
  판정은 `clean_selection_of / cut_selected / clean_pick_cuts` 한 곳.
- 서명에 선택 해시(`s…`) — 전체 파일과 안 섞임(`_clean_sig_for`, tiers_ready도 같은 함수).
- 정본(clean_base.json)에 `partial / sel / skip_beats`, 컷마다 `cleaned`.
  - 안 고른 비트 = `skip` → **원본 재료 그대로**, 장면을 바꾸거나 음성이 길어져도 **증분 청소 안 함(과금 0)**.
  - 고른 비트의 장면을 바꾸면 증분 1콜(종전과 같음).
  - 고른 비트가 안 지운 컷 조각을 빌려 쓰지 않는다(`piece_map(cleaned_only)`).
  - `render_inputs_for`가 원본 재료가 남으면 원본 소스도 같이 넘긴다(렌더·캡컷·꾸미기 프레임 공통).
- 곁에 발견해 고친 기존 버그: **증분 청소가 등급을 안 넘겨** 고급 job의 바뀐 장면이 기본으로 지워지고 있었다
  (`_clean_joined(tier=)`). ⚠️라이브에 가면 고급 고객의 증분 청소 비용이 초당 2→4로 바뀐다(원래 맞는 값).

### 실측 (LAB = 실제 job bte60cb14b7d 사본, 업체는 가짜 '아래 1/4 검은 띠')
| 항목 | 결과 |
|---|---|
| 업체로 보낸 길이 | 전체 24.5초 → 고른 3컷 **6.33초** |
| 프레임 수 | 조립본 734 = 청소본 734 |
| 프레임별 띠(지움) ↔ 고른 구간 | 734프레임 중 어긋남 **0** |
| 안 고른 프레임 ↔ 조립본 | 최대 평균차 0.46/255(재인코딩 잡음) |
| 캡컷 | 11조각=11컷, 경계 어긋남 0, 고른 것만 띠 |
| 꾸미기 프레임(_beatframe_file) ↔ 완성본 | 11컷 전부 일치(전체·골라서 둘 다) |
| 안 고른 비트 장면교체+음성1.5배 | 렌더 업체 호출 **0** |
| 고른 비트 장면교체 | 증분 1콜(cb2_0) |
| 화면(헤드리스 크롬, QA 서버) | 11카드·고르기·크레딧 안내·보낸 cuts·완료 후 비교문구 11장면 전부 맞음, JS 오류 0 |

LAB이 잡은 것: ① 조립본 시각 빈틈(344번 앞 0.070초)을 `-r 30`이 복제 프레임으로 메워 **둘째 구간부터 1프레임 밀림** →
프레임 번호(setpts=N/FR)로만 이동하게 고침. ② 검사기 자체 함정 2개(정지그림에 -ss 0 → 0장, rawvideo 기본 cfr → 복제).

### 테스트
- 신규 `tests/test_clean_partial_pick.py` 9건(실제 ffmpeg 되붙이기 포함) — **사보타주 6종 전부 빨강 확인**
  (옛 되붙이기·skip 무시·안 지운 조각 빌림·원본 소스 누락·등급 누락·서명 누락).
- 기존 clean/subclean/vmake 관련 343건 green(incremental·save 테스트는 새 표식에 맞게 기대값 갱신).

### 도구
- `scripts/clean_base_lab.py --pick 1,2,7 [--edit swap --edit voice]` — 컷별 기대↔띠, 캡컷, 꾸미기 프레임까지
- `scripts/partial_clean_frames_check.py --work <job폴더>` — 프레임 단위 밀림 검사
- `scripts/serve_clean_pick_qa.py`(8769, 가짜 업체) + `scripts/qa_clean_pick_ui.py` — 화면 QA

### 진짜 업체 시험 (2026-09-26 사장님 "내꺼로 테스트", 사장님 키·기본 등급, 로컬 LAB 사본)
- 보낸 것: `partial_in.mp4` **6.33초 1콜**(고른 3컷, 전체 24.5초) → status ready
- 프레임: 조립본 734 = 청소본 734 · 렌더 done(렌더 중 업체 호출 0) · 캡컷 11조각 = 11컷
- 눈 확인(`data/mix_jobs/bte60cb14b7d/realqa/`): 고른 컷은 자막·워터마크 깨끗, 안 고른 컷은 원본 그대로,
  고른 구간 경계 앞뒤 프레임 튐·밀림 없음.
- 고른 컷2 첫머리의 **큰 제목 글자는 검게 번진다** — 서버의 09-22 **전체 청소본도 똑같이 번진다**(나란히 대조) →
  잘라 보내서 생긴 게 아니라 기본 등급의 큰 글자 한계(고급 대상).
- 로컬에서 SDK 돌리려면 `pip install oss2 alibabacloud-oss-v2` 필요(첫 시도는 import에서 멈춤 — 업로드 전이라 과금 없음).
- 도구: `scripts/partial_clean_real_test.py --keyfile … --pick … --out … [--skip-clean]`

### 진짜 업체 고급 시험 (2026-09-26 16:19, 사장님 키, 같은 3컷 6.33초)
- 업체: 업로드→consume OK(SKM0005)→처리 완료(112초). **결과 다운로드에서 내 PC 연결 끊김(WinError 10053)** →
  task_id `t_mt1a3i5n7b3affecb7-15c0-43d9-8735-bf97a162376f`로 `poll_task_status`(재과금 없음) 재조회해 받음(190프레임).
- 되붙이기 734=734 · 렌더 done(업체 0) · 캡컷 11=11. `realqa_pro/pro_title.png`: 큰 제목 **깨끗이 복원**(기본은 검게 번짐),
  자막도 지워짐. 단 흐린 워터마크(beetem)는 고급에서 남음(기본은 지웠음).
- 크레딧 실측: 업체에 잔액 API가 없고 크롬 확장이 끊겨 **못 읽음** — 대시보드 사용내역 15:3x(기본)·16:2x(고급) 두 건 확인 필요.
- ★발견한 기존 위험: `vmake_client._download`에 재시도가 없다. consume 뒤 다운로드가 한 번 끊기면 job이 failed →
  고객이 다시 누르면 **두 번 과금**. task_id로 재조회하면 공짜로 살릴 수 있다(이번에 실증).

### 2026-09-27 — 다른 세션(완성본컷불일치)이 병합·배포한 뒤 확인
- 09-26 사장님 승인으로 `9960807f9`에서 라이브(완성본컷불일치와 병합). UI 게이트 규칙으로 썸네일 lazy 제거(`2c85ee1d7`).
- ★내 결함 사고: 이정민님 a90253dd235b — 조립본 머리 기록 r_frame_rate=240/1(실제 30)을 믿어 setpts=N/240로 705프레임을
  2.97초에 몰아 최종 렌더 7연속 실패. 그쪽이 `74ade9867`로 수정(실제 프레임÷길이, 결과 길이 검사) + 파일 시각 재기록 복구(업체 0).
  내 LAB·테스트는 머리 기록이 정상인 영상이라 못 잡았다 — **프레임 수만 대조하고 길이는 안 쟀다.**
- 라이브 골라 지우기 job 5건 전수(09-27): 청소본 전부 실제 30.00fps, 길이 = 컷 지도 끝 +0.3~0.6초(옛 칸 올림 누적 — 그쪽 calibrate 대상),
  고른 초 14.6/45.0 · 6.6/19.4 · 2.2/37.1 · 7.6/23.3 · 49.5/51.0. 렌더 done 4 · 대기 1(9f75783f375d).
- 같은 꼴 검색: `r_frame_rate`를 쓰는 곳은 scene_cut.py(원본 소스용) 하나 남음 — 이어 붙인 파일이 아니라 미확인·미수정.

### 2026-09-27 새벽 — 결과 받기 끊김 재과금 방지 (사장님 "고쳐야지") · 미배포
- `vmake_client.remove_subtitles(resume_key=)`: 제출 직후(on_async_submitted) 작업 번호를 결과 폴더 `.vmake_pending.json`에 적는다.
  같은 이름표로 다시 오면 poll_task_status로 **업로드·consume 없이** 받는다(길이 ±0.5초 검사, 24시간). 다운로드 4회 재시도(.part→rename),
  주소 만료면 작업 번호로 새 주소. 끝내 실패 → "결과 받기가 중단되었습니다 — 다시 누르면 추가 비용 없이…"(→ interrupted).
- 이름표: 완성본 `final:<파일명>` · 골라 지우기 `part:<파일명>` · 증분 `inc:<조각·원본구간 해시>`(파일 바이트는 재조립 때마다 달라 못 씀, 실측).
- 진짜 업체 시험(1.42초 기본, `scripts/vmake_resume_real_test.py`): 1차 강제 끊김 → failed/interrupted + 장부 · 2차 다시 누름 →
  업로드·consume 0(stderr 로그 대조, 전체 consume 1줄), ready, 장부 정리.
- 테스트 `test_vmake_resume.py` 7건, 사보타주 5종 전부 빨강. 관련 563 green, 실패 2건(test_s2_review_fallback_ui)은 main에서도 실패(무관).
- 화면: interrupted 문구에 "이미 처리된 결과가 있으면 추가 비용 없이 이어받아요" 추가.

### 2026-09-27 — 사장님 "이런 구멍이 또 없나" → 돈 나간 뒤 결과 잃는 길 전수 점검, 4곳 수정(오푸스 실행·페이블 검증) · 미배포
| # | 구멍 | 근거 | 수정 |
|---|---|---|---|
| ① | 진행 조회 끊김(poll_aborted)에 장부 삭제 → 재클릭 재과금 | SDK `_poll_aborted_payload`도 `error`로 옴 | poll_aborted면 장부 유지 + "다시 누르면 추가 비용 없이"(interrupted) |
| ② | 받은 뒤 되붙이기·쪼개기 실패 → 받아둔 파일 안 쓰고 재전송 | `partial_clean.mp4` 고정 이름·`join{tag}` 공용 폴더 | `partial_clean_{서명}.mp4`·`join{tag}_{이름표해시}/` — 있으면 업체 0 |
| ③ | 큐 대기 10분 뒤 재클릭 → 같은 청소 큐 2건 | 30일: 10분 넘게 대기 38건, 겹침 1건(08-30 f6800428f1ea) | `queue_has_pending("clean","job_id")`면 안 넣음 |
| ④ | 옛 소스별 경로(`_clean_one`) 이름표 없음 | 코드 | `resume_key="src:{vid}:{파일명}"` |
- 못 막는 틈 1곳: SDK 순서가 과금(consume)→제출→작업번호. 그 HTTP 한 번 사이에 워커가 죽으면 번호가 없어 이어받기 불가(업체 SDK 구조).
- 테스트 `test_clean_no_double_charge.py` 6건 + 기존 380 green, 사보타주 4종 빨강(오푸스 실행 → 페이블 재실행 56 green·diff 대조).
- 30일 고객 실패 기록엔 연결 끊김 0건 — 터진 적은 없고 터질 자리였다.

### 2026-09-27 새벽 — ★실측 사고: 이어받기가 **동기 완료** 때 안 됨 → 수정·재실측 OK (오푸스 실행·페이블 검증)
- 사장님 키 과금 탐침(03:40, `scripts/vmake_charge_probe.py`): A 정상 1콜 / B 끊김→재클릭 **consume 2회(재과금)** / C 정상 1콜.
  consume.json 응답은 `context` 토큰뿐 — 잔액·차감 칸 없음(실제 차감은 대시보드에서만).
- 원인(`scripts/vmake_resume_trace.py` 03:54): 장부 `put`이 0회. SDK(api.py)는 제출 응답 status==9(비동기)일 때만
  on_async_submitted를 부르고, 짧은 영상은 sync_timeout 안에 끝나 콜백 없이 결과를 바로 준다. 02:34·16:19는 업체가 느려 비동기라 우연히 통과.
  로컬 가짜는 콜백을 직접 불러 HIT → 로컬 통과가 증거가 아니었다.
- 수정(`01b25ac17`): 결과 dict에 task_id가 오면 **다운로드 전에** `_pending_put`(동기·비동기 공통). 테스트 1건 추가·사보타주 빨강.
- 재실측(03:59, 업체 1콜): B1 끊김 → 장부 있음 / B2 재클릭 → **consume 0·HIT·이어받기 OK·ready**.
- 오늘 사장님 키로 나간 consume 합계: 02:34 1 · 03:40 4 · 03:54 2 · 03:59 1 = **8회**(전부 기본 1.4~2.6초, 계산상 4~6크레딧씩).
- 남은 것: 대시보드 실제 차감 대조(크롬 확장 미연결) · SDK 과금→작업번호 사이 틈(경보로 완화 예정) · 워커 사망 시 10분 대기(하트비트 가드·재시작 정리 예정)

### 2026-09-27 04:25 — ★라이브 배포 완료 (사장님 "남은 두 건도 진행하고 배포해 끝까지")
- 남은 2건: ⑤ SDK 틈 — 과금 직후 `consumed` 표식(`_pending_put(consumed=True)`), 번호 없이 다시 오면 `ops_alert("vmake_orphan_charge")` 경보 후 새로 맡김
  ⑥ 워커 죽음 — `app._clean_interrupted`(하트비트→큐 기록→10분 규칙 순) 한 곳을 상태 API·재클릭 가드가 공용, `_CLEAN_DEAD_MSG` 이어받기 안내,
  `clean_failure_kind`에 "중단됐"/"워커가 중단" 추가(청소기 `_DEAD_MSG`가 unknown으로 떨어지던 것).
- 오푸스 실행: 테스트 101+385 green(무관 19건 기준선), 사보타주 3종 빨강, 커밋 029a66da8, finish 게이트 통과 → main f782e9a3a, 서버 04:24:55 pull·04:25 웹/워커12 재시작.
- 페이블 대조: merge-base MERGED, 서버 코드 grep OK, 서비스 active, 워커 Traceback 0, 웹 "error"는 error_help.js 304뿐,
  배포 코드로 라이브 DB 판정 호출(bte60cb14b7d·9f75783f375d interrupted=False, failure_kind 매핑 OK). 배포 후 고객 청소 0건(새벽).
- ⚠️ 서버 repo untracked: `'`(이름이 따옴표인 파일)·`DEPLOY_NOW`·`backups/` — pull엔 무해(untracked). 내 것 아님, 손 안 댐.
- 못 한 것: 라이브에서 끊김 재현(장애 주입 필요) — 새 코드 경로는 로컬 실측(03:59 consume 0)으로만 확인. 대시보드 실제 차감 대조(크롬 확장 미연결).

### ⏭ 다음
- [ ] 크롬 확장 연결되면 대시보드 사용내역으로 오늘 consume 8회 실제 차감 대조
- [ ] 배포 후 첫 고객 청소 워커 로그 확인(`[vmake]` 이어받기/경보 줄 유무) — 고객 동작 변화: 끊김 시 재과금 없음, 실패 문구 1줄
- [ ] **사장님 승인 후** `finish` → 배포 (고객 화면 바뀜: 자막제거 단계에 '전체/골라서' 버튼. 기본은 전체라 안 누르면 종전과 같다)
- [x] 진짜 업체 1회 — 위 표. (배포 후엔 라이브 화면에서 한 번 더 눌러볼 것)
- [ ] 선택을 넓혀 다시 지울 때 이미 지운 컷도 다시 보낸다(지금은 새 선택 = 새 파일). 필요하면 '추가분만' 증분으로
- [ ] 안 고른 비트에 새로 생긴 비트(청소 뒤 추가)는 skip 처리(안 지움) — 고객 안내 필요하면 문구 추가
