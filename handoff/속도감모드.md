# 핸드오프 — 속도감모드 트랙

## 2026-07-22 (집PC / Opus 4.8)

### 한 일 (라이브 배포 완료)
쇼츠 핵심 "무음 컷 + 문장 끊김 없이 타이트하게 이음"을 자동화 → **라이브**(병합 `579521185`, 3분 배포).
- 캡컷 수작업(문장 끝↔다음 시작 겹침) 대체. 말소리는 안 겹치고 문장 사이 숨·여운(무음)만 컷.
- 구현: `audio_post.post_process(pace_mode=True)` — 앞무음제거(start_periods=1)+내부쉼+끝여백 0.08s+클릭방지 afade. ffmpeg 한 패스.
- 핵심: mp3 짧아지면 `tts_dur` 자동 적응 → `video_assemble.py`·자막싱크·타임라인 **무변경**.
- 배선: voice 스냅샷 `pace_mode`(기본 ON) → `_voice_snapshot`(app.py)/`_voice_params`(mix_pipeline)/`synthesize_line`. produce "⚡ 속도감 모드" 토글.
- 검증: 실 ffmpeg 1.40s→0.85s(0.551s 컷). 신규 테스트 23 pass, 게이트 통과(기준선 8건 그대로).
- 커밋: ef3747a(배선)/bfd6cec(스냅샷ON)/09f60e1(UI)/d1a17fa(ffmpeg검증).
- 스펙·계획: docs/superpowers/specs·plans/2026-07-22-속도감모드-*.md

### ⏭ 다음 할 것 (내일)
1. **귀 튜닝** — 실제로 한 편 만들어 들어보고: 너무 빡빡하면 끝여백↑(현 0.08s), 늘어지면 무음 임계 강하게.
   상수 = `shopping_shorts/audio_post.py`의 `_PACE_TAIL_PAD`(0.08) / `_PACE_FADE`(0.012),
   silenceremove 임계 `-38dB`/`stop_duration=0.3`/`start_silence=0.05`.
2. **끝단 fade-out 여부** — 현재 afade는 fade-in만. 콘텐츠→끝여백 경계 클릭이 실제로 들리면 추가 검토
   (fade-out은 길이를 알아야 해 areverse 트릭 필요 — 청감상 문제없으면 안 건드림).
3. 트리거: **"속도감모드 이어서"**

### 참고
- 트랙 폴더 `.tracks/속도감모드` 유지됨(finish는 폴더 안 지움). 바로 다음 작업 얹기 가능.
- 관련 트랙/기능: 보이스 프리셋, 영상믹스 파이프라인(mix_pipeline).
