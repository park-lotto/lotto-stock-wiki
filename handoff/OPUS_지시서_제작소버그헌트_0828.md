# OPUS 지시서 — 제작소 단계별 딥 버그헌트 (2026-08-28 새벽)

> 작성 2026-08-28 02~03시 · 작성자 **Fable**(사장님 지시: "제작소 단계별로 오늘 일어났던 문제들
> 너무많았어. 다시 문제 안생기게 깊게 버그찾고 문제점 다 오푸스한테 넘겨서 고칠수있게해") · **대상: Opus**
>
> 배경: 08-27 하루에만 수리 트랙 10개+가 병합됐다(자막제거1콜·자막제거2단계·자막제거짝맞춤·
> 자막제거썸네일·키프로브·자막재발방지·회원키합류·관리자전용키·렌즈게이트·키교체·예열상한·
> 썸네일후보16·꾸미기·핀터레스트탭·수집스킵방지·관측판·추출실패오표기). 이 지시서는
> **그 수리들이 남긴 구멍 + 새로 심은 버그**를 4개 영역 병렬 딥리뷰(코드) + 라이브 서버 실측(DB·journalctl)으로
> 찾은 결과다. 리뷰 범위: `git diff 23cf77f586 HEAD -- shopping_shorts` (69파일, +5,897).

## 0. 이 문서를 어떻게 쓰나

- **P0·P1은 Fable이 핵심 근거를 직접 재확인했다**(P0 grep 0건, P1-7 bool 비교, 라이브 로그·DB 실측).
  P2는 리뷰 에이전트 발견을 옮긴 것이라 **줄번호·전제를 열어보고 확인 후 고쳐라**(0순위 규칙).
- 고치는 작업은 **트랙 폴더에서** (`py tools/track.py start <트랙명>` → `.tracks/<트랙명>`에서 편집 →
  `finish`). main 폴더 코드 수정 금지. finish 후 병합 판정은 `git branch -r --contains <커밋>`으로
  (exit 0 ≠ 병합 — 메모리 `reference_finish_exit0_미병합`).
- ⚠️ **아래 두 곳은 기존 테스트가 "틀린 동작"을 계약으로 고정하고 있다** — 코드를 고치면 테스트도
  같이 고쳐야 한다: `test_capcut_clean_clips.py:75`(P1-3), `test_admin_concurrent_claim.py:52-62`(P1-9).
- 서버 확인 SSH(회사PC): `ssh -i C:/Users/CH/crawling_bot_client/LightsailDefaultKey-ap-northeast-2.pem ubuntu@43.200.48.69`
  DB에 sqlite3 CLI 없음 → `python3 - <<EOF` + sqlite3 모듈. DB 시각 = **UTC**, journalctl = **KST**.

## 1. 라이브 실측 (2026-08-28 00~02시 KST, 서버 43.200.48.69)

08-27 00:00 이후 mix_jobs: done 28 / ready_for_review 23 / **failed 7**
(가로형 차단 5 · plan_empty 1 · ElevenLabs 402 1). job_queue: done 498 / failed 11(배포재시작 10 · 워커중단 1).

| # | 실측 | 판정 |
|---|---|---|
| 1 | **VMake `[60002] not enough credits`** 저녁 12회 + 새벽 3회(00:09·00:16·00:24 KST). 전부 **cid 57 작업**과 시각 일치 | 사장님 계정 아님 — **cid 57의 옛 BYOK 키** 크레딧 소진. 새 키를 등록했는데 옛 키가 이기던 버그(01:38 `0f8ad4f36`으로 수정·01:51 워커 반영). 01:18부터 consume OK. **키교체 수정의 실전 검증은 아직 안 됐다** — cid 57 다음 청소가 새 키로 나가는지 확인해라 |
| 2 | **VMake `[10021] sign not equals`** 5회+(09:48~11:47 KST). Access=`8851fd2e...` = **cid 134의 등록 키** | cid 134가 AK/SK 짝이 틀린 키를 등록. `customer_keys.status`는 계속 **'ok'** — 등록 시 실검증 없음(§4) |
| 3 | **ElevenLabs 402** — 이틀간 3명(158×4·70×4·99×1), 같은 voice `aiUUgjHa4mp...` | BYOK 무료키 소진 추정. 고객이 20분에 4번씩 재시도(영문 원문만 보고 영문 모름). §3 P1-5·§4와 직결 |
| 4 | plan_empty(`생성기=legacy`) 1건 — 08-27 14:08 KST | **수정 배포(14:55) 전 발생분**. 재발 시 §3 P1-6의 3개 구멍 순으로 짚어라 |
| 5 | 틱톡 yt-dlp `Unexpected response` 다수 — 소스 스킵 | 코드 아님. **서버 yt-dlp 업그레이드** + `scripts/prewarm_unlatch.py --apply`로 래치 회수(§7) |
| 6 | 가로형 차단 5건(cid 109×1·62×4) | 기능 정상 작동. cid 62가 20분에 4번 — 담기 시점 경고 부재(§5 C-4) 탓. 과금 피해 없음(BYOK 무과금 + 환불 배선 정상 확인) |
| 7 | 배포 상태 | 정상 — 워커 01:51·웹 02:14 재시작, 92a9a13a0 반영. 서버 워킹트리 staged 없음 |

## 2. P0 — 회원 Gemini 키 31개가 **워커에 안 닿는다** (합류가 웹 프로세스에서만 돈다)

- **파일**: `shopping_shorts/worker.py`(main에 합류 호출 **0건** — Fable grep 재확인) vs
  `app.py:124-135`(`@app.on_event("startup") _join_member_keys` → `_resync_pools`), 키 등록/삭제 시에도 app 안에서만(3725·3738).
- **문제**: mix(EDL)·prewarm(추출)·preview·render 등 **Gemini를 실제로 태우는 작업은 전부 별도
  systemd 워커 프로세스**(worker@1~3)에서 돈다. 워커는 사장님 키 19개로만 돌고, 회원 키 31개는
  웹 프로세스(1단계 autoload·댓글·태깅)에서만 쓰인다. 기동 로그 `[keypool] 사장님 19 + 회원 31 = 50개`는
  **app 프로세스 출력**이라 착시. 08-27 "회원키합류" 트랙의 목표가 주경로에서 미달성이고,
  키 소진('EDL 비어있음'·추출 실패) 재발의 배후 유력.
- **방향**: `worker.main()` 시작부 + 주기적(폴링 루프 N분마다) `_resync_pools(Store(DB_PATH))`.
  ⚠️ 프로세스마다 재동기 시점이 다르면 **인덱스 낙인이 어긋난다**(소진 상태파일이 idx 기반) —
  회원 키는 지금처럼 사장님 키 **뒤에 append**만 유지하고, 회원 키 삭제 시 낙인 밀림은 §5 B-4 참조.

## 3. P1 — 실돈·실기능이 걸린 것 (11건)

### 청소(자막제거) 파이프라인 — mix_pipeline.py

**P1-1. `final_clean_{sig}.mp4` 캐시 검증이 "크기>1024"뿐 — 부분 다운로드가 영구 오염 캐시가 된다**
`mix_pipeline.py:2308-2321` + `vmake_client.py`(urlretrieve가 **최종 경로에 직접** 다운로드).
다운로드 중 워커 재시작(배포 3분 크론) → 잘린 파일 잔존 → 다음 재시도가 `size>1024` 통과 →
**깨진 파일을 "완성본 재사용(과금 0)"으로 영영 재사용**. → 임시파일에 받고 성공 시 `os.replace`.
(소스별 경로 `_clean_one`도 같은 패턴.)

**P1-2. `_plan_signature`가 완성본 그림을 다 못 덮는다 — 편집이 조용히 무시된다**
서명(2277-2295)엔 재료+target_seconds뿐. `fixed_lens`(손 컷)·`stretch_fill`·`head_trim/tail_trim`·
`cutaway`는 mix_raw 그림을 바꾸는데 서명에 없다 → 2단계 청소(5P) 후 실험실에서 컷을 고치고
렌더하면 sig가 같아 **옛 청소본 위에 새 자막만 구워진다**. 오류 0건, 고객은 "고쳤는데 안 바뀜"만 본다.
→ 서명에 포함(재과금 증가 트레이드오프는 §6-1과 함께 결정).

**P1-3. 캡컷 조각 캐시(`capcut_clean_cc*.mp4`)에 편성 서명이 없다**
`mix_pipeline.py:2103-2135` — clean_preview.mp4는 재청소 때 같은 이름으로 덮이는데 조각 파일명엔
sig가 없어, 편성 수정 후 캡컷 재내보내기가 **옛 편성 조각**을 내보낸다.
`test_capcut_clean_clips.py:75`가 이 동작을 계약으로 고정 — 테스트도 고쳐라.
→ 파일명에 sig(또는 clean_final mtime:size) 포함.

**P1-4. 차감~환불 사이에 워커가 죽으면 5P 증발**
`_charge_clean` 선차감 후 VMake 2~10분 — 그 사이 배포 재시작이면 except의 `_refund_clean`이 못 돈다.
`api_mix_status`의 가드는 화면 표시만 하고 환불 없음. → job에 `clean_charge_pending` 표식,
성공/환불 시 제거, 워커 기동·reap_stale에서 표식 잔존분 환불(기존 `render_charge_day` 표식 패턴).

**P1-5. run_render 실패 시 유료 고객 포인트 미환불 — 오늘 402 실측과 직결**
`mix_pipeline.py:2728-2736` — except가 **체험만** `_refund_render_charge`. `run_mix_job` except는
`_refund_mix_points`+`_refund_render_charge` 둘 다 부르는데(973-975) 최종렌더 유료 경로만 빠졌다.
TTS 402(3회 무의미 재시도 후 raise)가 여기로 오면 유료 고객은 실패+미환불.
→ 최소 영구성 실패(402·401)는 환불. 402는 재시도 없이 즉시 실패 + 한국어 안내(§4).

**P1-6. plan_empty 잔존 구멍 3개** (`edit_plan.py`)
08-27 수정은 "일시장애→다음 키"만 넓혔다. `[생성기=legacy]` = 확정대본 경로(현 주경로):
① `json.loads(resp.text)`(2301) 파싱 실패 → 어느 분기에도 안 걸려 **즉시 None**(재시도 0) —
   MAX_TOKENS로 잘린 응답이 정확히 이 모양.
② legacy는 `max_tries=4` + key_offset 없음(3969) — 워커 3개가 keys[0]부터 동시에 때려 분당 429 →
   `continue`(대기 없음) → 몇 초에 소진. scene_first(8+offset)와 비대칭.
③ 그라운딩 전량 탈락(1995-2000)이면 raw 정상인데 beats 0 — 재시도·사유코드 없음.
   ("키 N개를 다 돌았는데 결과 없음" 로그 유무로 ①②/③을 가른다.)
→ ①을 빈 응답과 같은 `continue`로, ②에 offset+8트라이, ③ 사유 세분화(`plan_grounding_empty`)+1회 재생성.

**P1-7. 보이스 라이브러리 검색/담기가 키 있어도 항상 차단 (기능 통째 사망 — Fable 재확인)**
`app.py:5082-5084, 5100-5103` — `keys, _src = keyroute.keys_for(...)` 인데 `keys_for` 2번째 반환은
**bool**(keyroute.py:180-190). `True != "customer"`는 항상 참 → 무조건 need_key.
(베이스 이전 08-24 커밋이지만 확실한 사망.) → `if not keys or not _src:`. 회귀 테스트 추가.

### 서버부

**P1-8. prewarm "자동 재시도 대상 — 고장 아님"은 거짓말 — 재큐 장치가 없다 + SIGTERM 누수**
`store.py:6060`(문구) — prewarm을 다시 큐에 넣는 코드가 **어디에도 없다**(enqueue는 담기 토글뿐).
durfill(1시간 재적재)·overseas(자체 크론)는 문구가 맞고 prewarm만 구멍. 게다가 배포가 prewarm을
15분 상한 후 죽이면(auto_deploy.sh) 이미 ①래치 attempts+1 ②크레딧 차감 ③일일 cap 소모 상태에서
SIGTERM은 finally 환불(prewarm.py:254)을 안 태운다 → **크레딧 미환불 + 3회 겹치면 영구 래치**.
오늘 "배포 재시작으로 중단됨" 10건. → reap_stale에서 prewarm은 `state='queued'` 재큐(또는
롤백+환불), 문구는 사실대로 분리.

**P1-9. `mix_queue_ahead`·`waiting()`의 prio 방향이 claim_next와 정반대**
`store.py:5990-5996` `q.prio > me.prio`를 '내 앞'으로 세는데 claim_next(5865)는 **오름차순**
(prio 낮을수록 먼저, TASK_PRIO: mix=0 먼저). `test_admin_concurrent_claim.py:52-62`가 반대 계약을
고정. 지금은 mix 전부 prio 0이라 잠복 — 누군가 "급한 작업 prio=10"을 쓰는 순간 워커는 뒤로 미루고
화면은 "내 앞 0개". → 부등호 반전 + 테스트·주석·waiting() 정렬을 claim_next 기준으로 통일(0순위-B).

### 프론트 — produce.html

**P1-10. 워터마크 복원이 가로 위치(x)를 잃는다 — 저장할수록 서버 데이터가 오염**
`produce.html:4292`(applyWatermark) — `wmPos` select를 복원하지 않고 y만 살림 → `updateDeco`(4312)가
초기 프리셋(bc, x=50)으로 x 재계산 → 다음 자동저장이 x=50을 서버에 덮음(복원할수록 가운데로 수렴).
→ 저장된 `wm.x`와 일치하는 WM_POS 프리셋을 먼저 복원.

**P1-11. 자막제거 BEFORE/AFTER 첫 화면이 '완성본에 안 쓰인 소스'로 열릴 수 있다**
`produce.html:9218·9246` — `CLEAN_VIEW.si` 기본 0인데 `clean_source_indices`에 0이 없으면(편집안이
소스 [1,3]만 쓴 경우) 첫 화면이 "이 영상은 완성본에 안 쓰였습니다" + ▶ 버튼이 첫 소스를 건너뜀.
→ `renderCleanReady` 첫머리에 목록 밖이면 `CLEAN_VIEW.si = L[0]` 한 줄.

## 4. 횡단 이슈 — BYOK 서드파티 키 사고군 (오늘 실측 3종의 공통 뿌리)

vmake 21명·elevenlabs 20명·gemini 23명이 자기 키를 등록해 뒀다(라이브 실측). 오늘 터진
60002(크레딧 소진)·10021(서명 불일치)·402(크레딧 소진)의 공통 결함:

1. **영어 원문이 고객에게 그대로 노출** — "You don't have enough credits... vmake.ai/pricing",
   "402 Client Error: Payment Required for url:...". 고객은 영문을 모른 채 20분에 4번씩 재시도했다.
   → 오류 → 한국어 안내 매핑을 한 곳에(`_SVC_KO`(app.py:9241) 있는 자리 확장):
   "등록하신 {서비스} 키의 크레딧이 소진되었습니다 — 충전 후 다시 시도해 주세요" /
   "키가 올바르지 않습니다 — 🔑탭에서 다시 등록해 주세요".
2. **`customer_keys.status`가 실패해도 'ok' 유지** — cid 134의 틀린 키가 5번 폭발해도 ok.
   → 60002·10021·402·401을 맞으면 status를 `no_credit`/`bad`로 갱신하고 🔑탭에 표시.
3. **등록 시 실검증 없음** — 10021(AK/SK 짝 틀림)은 등록 순간 1콜로 걸러진다.
   → 등록 시 가벼운 검증 콜(잔액조회류. VMake는 config fetch가 서명 검증을 겸함).
4. **run_render 중 청소 실패 시 `clean_status=ready` 선기록 잔존** — `mix_pipeline.py:2658-2664`가
   청소 **시작 전에** ready를 기록. 실패해도 ready+clean_error None으로 남아 ①2단계 UI 완료 표시
   ②no_credit 맞춤 안내 미작동 ③썸네일·캡컷 게이트 오작동. (오늘 라이브에서 ConsumeDenied 난
   작업들이 정확히 clean_status=ready·clean_error=None이었다 — Fable DB 실측.)
   → ready 기록을 assemble 성공 후로, except에서 청소 기인이면 clean_status=failed+사유.

## 5. P2 목록 (영역별 — 고치기 전 줄번호·전제 재확인)

### A. 청소 파이프라인
- **A-1** TTS·훅 선확정 실패를 삼키고 청소 진행(`mix_pipeline.py:2484-2493`) — 그 경우 렌더에서 sig가
  바뀌어 "편당 2콜" 부활. `test_clean_once_per_video.py:73`이 이 동작을 고정. → TTS 실패 시 청소 중단.
- **A-2** `final_clip_pairs`가 TTS 없는 비트를 시간 계상(2168-2192) — 렌더는 스킵(`_render_mix:1187`) →
  이후 소스 전부의 전/후 비교·5단계 프레임 어긋남. "세 번 틀린" 그 축의 다섯 번째.
- **A-3** 가로형 판정이 rotation 메타데이터 미인식(`_probe_wh_dur:664-676`) — rotate=90 세로 영상을
  가로로 오차단 가능. → ffprobe side_data_list 반영.
- **A-4** 완성본 청소 경로에선 `clean_regions`(지워진 자리)가 영영 안 채워짐 — 5단계 표시·꾸미기 힌트
  조용한 소실. 의도된 포기인지 확인 후 `detect_erased_region(mix_raw, cleaned)` best-effort.
- **A-5** `_final_clean_fn`에 간헐 10101 재시도 없음(소스별은 3회) — 1회 실패=작업 실패(환불은 됨).
- **A-6** 썸네일 자가치유(`app.py:5873-5886`)가 final 경로에서 무력 — clean_video_path 유실 시
  **자막 있는 preview로 폴백**(07-20 사고 재발 자리). final_clean_{sig} 살아 있으면 그걸로 재조립.
- **A-7** `_beat_materials`(mix_pipeline:2013) = `video_assemble._beat_material`(:427) 복제(0순위-B 잔존).
- **A-8** 캡컷: 조각 없는 비트는 원본 소스가 그대로 나감(2148-2155) — 원본 자막 유출. 테스트가 고정.

### B. 키풀
- **B-1** `_exhausted_map` 옛 list 폴백이 읽기만으론 영영 안 만료(comment_gen.py:80-81 — 읽을 때마다
  TTL 재계산). 마이그레이션 창은 지나 실피해 없음, 주석과 실동작 불일치.
- **B-2** `comment_gen.generate`만 retry_after 미배선(:366) — 13곳 중 유일 누락.
- **B-3** `get_pooled_keys`(store.py:6293)가 status='off' 회원 키를 안 거름 — 관리자가 끈 키가 로테이션
  잔존. cid 57 사고와 같은 모양의 재발 자리.
- **B-4** 회원 키 삭제 시 인덱스 낙인 밀림 — exhausted·revived_once가 idx 기반인데 ORDER BY id 목록이
  당겨짐. 삭제 시 회원 구간 낙인 초기화.
- **B-5** revived_once·상태파일 '오늘' 경계가 UTC(=KST 09시) — prewarm은 KST로 고쳤는데 이쪽은 그대로
  (같은 판단 두 기준, 0순위-B). Gemini RPD 리셋(태평양 자정)과도 안 맞음.
- **B-6** 렌즈 월 카운터·전역 상한이 BYOK 회원 검색까지 셈(app.py:6655·6858) — 실잔량 조회 실패 날
  공용 경로 조기 차단 + 회원들이 전역 200을 채워 전원 차단 가능. 의도인지 확인 후 분리.
- **B-7** 풀 축소 직후 `SHORTS_GEMINI_KEYS[idx]` IndexError 창(comment_gen.py:312, try 밖).
- **B-8** `_TRANSIENT_MARKS`의 "500"·"CANCELLED" 부분문자열 오탐 가능(script_extract.py:355) — `\b500\b`로.
- **B-9** daily-429에 서버가 짧은 retry(예: 32s)를 주면 낙인이 금방 풀려 재소진 왕복 — daily면 하한 30분.
- **B-10** `as_cid` 파싱 실패가 0으로 떨어져 **관리자 개인 키**로 비용 귀속(관리자전용키 낙수).
  `_charge_clean`의 "cid 0은 개인키 조회 건너뜀" 주석(mix_pipeline.py:1506)도 낡음.
- **B-11** `_POOL_IMPORTERS` 부패 감지 테스트가 하위 디렉터리·괄호 여러 줄 import를 못 잡음(현 위반자 0).

### C. 서버부·수집
- **C-1** 핀터레스트 비공개 탭이 서버에서 안 막힘 — `/api/reference?platform=pinterest`는 로그인 누구나.
  수집만 admin. → api_reference에서 pinterest면 `_is_admin` 검사.
- **C-2** 핀터레스트 누적 무상한(`app.py:10404-10413` `fresh+prev` 통저장) — 상한 500 등 절사.
- **C-3** 핀터레스트 수집이 웹 요청 스레드에서 동기 Playwright 최대 8회 — 렌더 중이면 크롬+ffmpeg 경합.
  워커 task화 또는 heavy_job_active면 409.
- **C-4** 가로형 경고 배지가 brief 없으면 안 뜸(`produce.html:6563` 조기 return이 lscape보다 앞) —
  담는 순간(사장님 요구 시점) 경고 부재 → 진행하다 차단당함. cid 62가 4번 부딪힌 원인.
- **C-5** deferred(일시장애)가 하루 예열 몫(cap)을 안 돌려줌 — 크레딧은 환불되는데 cap은 환불 함수
  자체가 없음. Gemini 장애 긴 날 "밤 담기 전멸" 재현 가능.
- **C-6** `waiting()` 인원 집계가 LIMIT 40 행에서만 — 41건+ 밀린 순간(취지의 순간) 과소집계.
- **C-7** admin usage reset이 체험 "trial" 버킷을 못 되돌림(날짜 버킷만) — 여전히 SSH행.
- **C-8** `_stale_results`가 라운드만 바뀌어도 저장 썸네일 전부 "이전 영상 ⚠️" 오표시(`app.py:5912-5921`,
  video_sig에 r{round}:n{N} 포함 탓) — 재추출용 서명과 stale 비교용 서명 분리.
- **C-9** `daily_instagram_collect._wait_until_idle` — queued 좀비엔 가드 없음(워커 서비스 통째 죽으면
  4시간 대기 후 그날 수집 0건).
- **C-10** `_CREDIT_BYOK_DEFAULTS` 죽은 상수 + `_limit_tier`가 BYOK render/script를 free/pro로 흘림
  (주석 의도와 다를 수 있는 자리 — 의도 확인).

### D. 프론트
- **D-1** [다른 장면 더 뽑기]가 고른 배경을 조용히 교체 + `grid_00.jpg` 덮어써 복구 불가(produce.html:11040~).
- **D-2** 핀터레스트 탭에 seedPanel(계정 등록)이 그대로 보임(index.html:917) — 여기서 등록하면
  메모리 `새플랫폼탭 조용한0건` 함정으로 이어짐. instagram과 같이 숨겨라.
- **D-3** filmroll 기본 높이 139가 세 곳(filmroll.js:33·cur() 폴백·scene_lab.html:47) — 0순위-B 냄새.
- **D-4** admin 모달이 setPlan/grantTrial 후 재렌더 안 됨(코스메틱) / 🔄 버튼 로딩 중 무반응 피드백.

## 6. 사장님 판단 필요 — 코드로 결정하지 마라

1. **완성본 1편 청소의 재과금 성질**: 옛 소스별 캐시는 편집해도 재과금 0이었는데(옛 주석
   mix_pipeline.py:1688이 정확히 그 이유를 적음), final 전환으로 "2단계 청소 후 편집→렌더 = 추가 5P
   자동 차감(안내 없음)"이 됐다. P1-2(서명 보강)를 하면 재과금이 **더 늘어난다** — 서명을 촘촘히 할수록
   낡은 결과물은 없어지고 재과금은 는다. 재청소 전 화면 고지/확인을 붙일지 사장님 결정.
2. **BYOK 회원의 렌즈가 전역 상한(일 200)을 채우는 것** — 서버 보호 목적이면 의도, 아니면 분리(B-6).
3. **ElevenLabs 402 시 사장님 키 폴백** — 비용 유출이라 기본은 하지 말 것. 안내만(§4).

## 7. 운영 조치 (코드 아님 — Opus가 서버에서 직접, 단 라이브 트리에 git add 금지)

- [ ] **서버 yt-dlp 업그레이드**(틱톡 전멸 해소) → `scripts/prewarm_unlatch.py --apply`로 래치 회수.
- [ ] cid 57 다음 청소가 **새 키**로 나가는지 journalctl로 확인(키교체 수정 실전 검증).
- [ ] `_FINAL_CLEAN` 서버 env(`SHORTS_CLEAN_FINAL=1`) 실재 확인 — 코드 기본은 꺼짐인데 handoff는
  라이브라고 기록(로컬에선 검증 불가였음).
- [ ] 사장님 보고용: cid 134에게 VMake 키 재등록 안내 필요(현재 키는 AK/SK 짝 틀림).
- [ ] main 폴더 워킹트리에 `?? '`(작은따옴표 이름 파일) 등 잡동사니 untracked — 내용 확인 후 정리.

## 8. 이번 리뷰가 "이상 없음"으로 확인한 것 (다시 파지 마라)

60002 시 5P 환불 짝(차감→실패→환불→raise) / NotEnoughPoints 경로 / 가로형 차단의 3P 환불 /
정상 흐름의 1콜 서명 멱등(TTS skip_existing·훅 hook_orig_start) / hook_orig_start 옛 job 마이그레이션 /
exhausted dict 왕복·클램프 / `_push_pool_to_importers` 자체(원자적 재할당·명단 6개 실사용 일치) /
키교체 `reversed(mine)` 배선(SINGLE_KEY 실사용처 2곳 모두 keys_for 경유, 우회 SELECT 없음) /
렌즈 게이트(체험 차단 우선·회원 자기 키 잔량 판정·캐시 분리) / prewarm KST 경계·daily_remaining
읽기 전용 / deferred_api 래치 롤백 배선(prewarm·autoload 양쪽) / dead worker 전파(_TASK_JOB_FIELDS
단일화·완료값 미덮음·배경작업 분리 — "배포 재시작" 문구는 고객 화면에 안 감) / admin 신설 라우트
인증·usage_reset 원자성 / claim_next 동시성 / 수집스킵방지 대기 루프(4h 상한·좀비 가드) /
핀터레스트 `_reject_cdn_proxy`·검은 카드 수리 / produce.html 문법·TDZ 전 블록 통과 / sticky 수정
안전 / 12장 그리드 배선 / 배지·워터마크 크기·투명도 저장 짝 / 폰트 4종 실파일·배선 / admin XSS 이스케이프.

## 9. 우선순위 권고 (Fable 의견 — 뒤집어도 된다)

```
1) P0 (워커 회원키)          — 키 소진 재발의 배후. 작고 효과 큼
2) P1-5 + §4 (402 환불·안내)  — 실돈 + 고객 신용. 오늘 실측 9건
3) P1-1·P1-4 (캐시 오염·5P 증발) — 유료 기능 신뢰
4) P1-7 (보이스 라이브러리)   — 한 줄 수정으로 기능 부활
5) P1-2·P1-3 (서명·캡컷)      — §6-1 사장님 결정과 함께
6) P1-6 (plan_empty)·P1-8 (prewarm) — 재발 잦은 증상의 잔여 구멍
7) P1-9~11, P2               — 순서 자유
```
