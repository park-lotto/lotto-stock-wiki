# 핸드오프 — 완성본컷불일치 트랙

> 소유 트랙: 완성본컷불일치 — 이 파일은 이 트랙 세션만 수정한다.

## 2026-09-26 (CH PC) — 청소본 정본이 편집 화면과 다른 장면을 내던 뿌리 수리

### 제보
강규봉님(work eb8d6b1fae74 / job 7bbb1329aff0): "미리보기랑 완성본 만들기가 다르게 나온다".
사장님: "청소본 자꾸 매번 자막제거 후 이상하게 꼬인다 — 근본적으로 다 점검해서 끝까지 수정".

### 원인 (서버 실측)
청소본 정본 경로(`clean_base.remap_plan`)가 두 가지를 틀렸다.
1. **손으로 정한 컷(manual_cuts)을 몰랐다** — `mix_pipeline._beat_materials`가 scene_override만 봐서
   청소 구간·서명·정본 재배치·증분 청소가 전부 옛 컷 기준. 강규봉님 10칸 중 1·5·7·8번이 다른 장면
   (1번: 고객 한 컷 s2 2.533+5.17초 → 완성본은 옛 두 컷, 뒤 컷은 s2 8.967 = 딴 가방).
2. **청소본 조각으로 바꿔 끼운 뒤 컷 리듬을 다시 계산** — max_shot·라운드로빈·낱말 스냅이 청소본 컷
   경계로 쪼개진 조각 위에서 새로 돌아 같은 장면 반복·딴 장면 삽입(예: bca033ae6278 칸마다 3회 반복).
3. 곁: `piece_map`은 일부만 겹쳐도 "덮였다"고 봄 / 증분 조각이 둘 이상이면 첫 조각만 씀 /
   정본 컷 지도가 완성본 길이(dur)만 기록해 느리게·정지로 늘린 컷이면 안 지운 원본까지 덮었다고 봄.

### 고친 것
- **렌더 컷 재생**: `remap_plan(..., src_durs=)` — 칸마다 원본으로 그렸을 컷 계획(`plan_beat_clips_for`)을
  그대로 청소본 좌표 수동 컷으로 옮긴다(`clean_replay`). 못 덮은 칸만 uncovered + `_clean_need`(그 구간만 증분 청소).
- `video_assemble.manual_cut_spans` 한 곳이 "손 컷이 화면" 판정 → `_beat_materials`가 사용.
- 정본 컷에 `sdur`(원본 읽은 길이) 기록, 증분 조각에 `src_vid/src_start` 기록 → 재생이 그 조각도 씀.
- 컷 끝 자투리(≤0.3초·≤15%)는 재청소 대신 살짝 느리게 읽어 메움(과금 절감, 66칸 해당).
- 원본 길이를 못 재는 칸·옛 증분 조각(위치 기록 없음)은 종전 판정으로(재과금·렌더 실패 방지).
- 재청소 안내 API(`/api/produce/mix/clean_base_preview`)도 같은 판정.

### 검증
- 전수 점검 도구 `tools/clean_base_audit.py` (서버, `--patch DIR`로 배포 전 대조 가능):
  7일 청소본 job 349개 — **라이브 293개·780칸 불일치 → 수정본 3개·3칸**(옛 정본 sdur 없음 0.2초 오차 2 · 옛 증분조각 1).
- 비용 영향: 재청소 대상 13칸·70초 → 254칸·409초(실제 미청소분 191.5초). 전부 "안 지운 구간을 옛 장면으로 때우던" 칸.
- 강규봉님 job 수정 모듈로 미리보기 렌더(/tmp) → 2번 칸 프레임이 원본 s0 6.03초·청소본과 일치.
- pytest: 청소본 53개 + 관련 990개 통과(대본 문장 테스트 1건 기존 실패, 무관).

### ⏭ 다음
- [ ] 배포 후 강규봉님 job 라이브에서 완성본 만들기 → 프레임 대조(이 세션에서 진행)
- [ ] 1·3·6번 칸은 최종 렌더 때 증분 청소(과금) — 고객 화면 재청소 안내 문구 확인
- [ ] 편집 화면 미리보기(pvproxy)가 장면 적용 직후 낡은 파일을 내주는지 — 16:03:21 proxy vs 16:03:29 apply(별건)

## 2026-09-26 저녁 — 재청소는 안 지워진 부분만 · 장면 골라 지우기와 호환

- 사장님: "안 지워진 부분만 해야 되는 거 아니야?" — 맞다. 첫 배포는 못 옮긴 **컷 전체**를 증분 청소로 넘겼다(설계 실수).
  → `clean_base.uncleaned_gaps` 로 빈 부분만 `_clean_need`에 담는다. 새 조각(src 위치 기록)이 청소본 조각과 이어 붙는다.
- `자막제거장면선택` 트랙(장면 골라 지우기, 승인 대기·미병합)과 맞물림:
  - 재생(`_regions`)이 `cleaned: False` 컷을 지운 조각으로 안 쓴다 / `partial`+`skip_beats` 칸은 원본 그대로·청소 0.
  - 시험 병합 브랜치 `scratch/merge-자막제거`(origin에 push): 충돌 3곳(clean_base piece_map·coverage docstring·coverage 분기)
    양쪽 살림 + render_inputs_for의 "원본 소스 동봉" 두 벌을 `_left` 한 곳으로. 청소본 55 + 골라 지우기 9 = 64 green.
  - ★그 트랙이 finish할 때 이 브랜치의 해결을 그대로 가져가면 된다.

## 2026-09-26 밤 — ★정정: 기준은 "렌더 계산"이 아니라 "편집 화면"이다

- 첫 보고("10칸 모두 같은 장면")는 **렌더 계산(plan_beat_clips_for) 기준** 대조였다. 편집 화면 기준으론 2번 칸이 여전히 달랐다.
  화면(`scene_play.js syncCuts→frozenClips`)은 재료 목록에 없는 조각의 손 컷을 **버리고**, 목록 조각을 채우고, 목록 순서로
  정렬하고, 음성보다 길면 뒤에서 줄이며, 시작은 **조각 시작**이다. 서버는 저장된 manual_cuts를 그대로 썼다(두 벌 판단).
  실측: pvproxy(편집 화면) 9.6초 = 원본 s0 10.16초(재료 film_s0_9.03) / 렌더 = s0 6.03초(옛 손 컷 dwrozf).
- 고친 것: `video_assemble.synced_manual_cuts` = syncCuts 서버판. 렌더 수동 분기·manual_cut_spans가 사용.
  청소본 재생 칸(clean_replay)은 제외. 반올림도 JS Math.round와 같게.
- 오늘 첫 배포의 "재료에 없는 영상의 손 컷도 쓴다"는 **정반대 방향이라 되돌림**.
- 영향: 7일 514 job·3934칸 중 64칸 변경(대부분 마지막 컷이 음성 길이로 줄어듦 = 화면과 같아짐).
- 강규봉님 7bbb: 화면 기준 미청소 칸 1·2·3·6·7(최종 렌더 때 빈 부분만 청소).

## 2026-09-26 심야 — 근본해결: 완성본 컷 = 편집 화면 컷(screen_clips)

- 7일 전수(라이브 scene_play.js를 node로 돌린 화면 컷 vs 서버 렌더 컷): **502칸/97job 불일치**(구절 맞춤 기본값 켬/끔 ~390칸 등).
  규칙 맞추기로는 끝이 안 남 → 서버가 **화면 코드 그대로** 돌려 그 컷을 쓴다.
- `shopping_shorts/screen_clips.py` + `screen_clips_runner.js`: warm(job)=화면 데이터(api_mix_scene_lab_data)로 node 실행 → 칸 내용 키 캐시.
  `plan_beat_clips_for` 맨 앞 lookup(청소본 재생 칸 제외) · warm 호출은 render_inputs_for·_src_durs_for(완성본 컷 지도도 같게).
  실패·데이터 없음 → 종전 계산(조용히, stderr 로그 `[screen_clips]`). 끄기: env `SCREEN_CLIPS=0`.
- 렌더가 훅 시작점을 모션 피크로 옮기던 것(_apply_hook_inpoint)은 화면 컷 있는 칸에서 안 함(화면이 이긴다).
- 검증: 패치본 7일 전수 502→2칸(데이터 이상 2건) · 3 job 렌더 중 lookup 적중 100%(8/8·11/11·10/10, 렌더 중 칸 변형 0)
  · 94f5b1834a0a 렌더 프레임이 화면 컷 원본 프레임과 일치(차이 0.7~7.9, 기존 시간축 +0.1~0.35s 밀림은 종전부터).
- 곁: 이정민님 a90253dd235b 골라 지우기 청소본 240fps 사고 — `_probe_fps_frames` 실제 프레임÷길이, 되붙이기 길이 검사,
  `_probe_duration` N/A→0. 파일은 시각만 재기록해 복구(업체 0), 최종 렌더 done.
- 도구: 서버 /tmp/cbpatch/editor_audit.py(화면 vs 렌더 전수) — tools/로 옮길 것.
