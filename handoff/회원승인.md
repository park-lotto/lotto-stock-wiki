# 핸드오프 — 회원승인 (화이트리스트/대기실 + 회원관리 고도화)

- 갱신: 2026-07-22 (집 PC) — **회원관리 A+B+C 6태스크 완료, finish 진행**
- 트랙: `회원승인` / 브랜치 `track/회원승인`

## 🆕 회원관리 고도화 (2026-07-22, A+B+C) — SDD 6태스크 완료
설계: `docs/superpowers/specs/2026-07-22-회원관리-이름전화결제기간-design.md`
계획: `docs/superpowers/plans/2026-07-22-회원관리-이름전화결제기간.md`

사장님 결정: 앱 가입폼 확장(외부 구글폼X)·결제 admin 수동입력·여러건 누적·승인시 함께입력·자동체험 제거·시작일=approved_at 재활용.
| Task | 내용 | 커밋 |
|---|---|---|
| 1 | customers.name·phone + payments 테이블 | 8ca3a20 |
| 2 | create_customer가 name·phone 저장 | acca014d1 |
| 3 | add_payment/list_payments 결제이력 스토어 | c002c71c6 |
| 4 | approve_customer=결제+기간, 재승인=연장(자동체험 제거) | 415cd5408 |
| 5 | approve 결제본문+/api/admin/payment·payments+signup name·phone+list_customers | c3497a3ec |
| 6 | admin 이름·전화·시작·마감·결제 표시+승인/결제추가 폼+결제이력 펼침 | 35870306a |

- 각 태스크 spec+quality 서브에이전트 리뷰 통과. 최종 whole-feature 리뷰 **READY TO MERGE**.
- 페이월+회원관리 테스트 **56 passed**(회귀0). admin.html Playwright 렌더 그라운딩 완료(대기명단 이름·전화 / 승인폼 / 고객목록 시작·마감·결제 / 결제이력 펼침 alert 동작).
- 남은 Minor(비차단): create_customer approved=True 자동체험 분기가 이제 unreachable(프로덕션은 signup·google 둘 다 approved=False) — 나중에 정리. admin esc()는 <> 만 이스케이프(admin전용이라 무해).
- **후속(D/E, 별도 스펙)**: 무료체험 이벤트=레퍼런스 채널페이지만·24시간. ranking_only 등급 재활용해 "가입 후 24h ranking_only 허용"으로 얹기 쉬움. 일수·페이지범위 사장님 확정 후.

---

## 이전: 화이트리스트/대기실 (2026-07-21, Task 1~8 라이브)
- shopping_shorts "사장님 승인한 사람만 사용" 대기실 방식. Task7(grab 구멍)·Task8(admin 대기명단)·브랜드명 통일·logout CSRF 보강까지 라이브 배포 완료. 페이월 **43 passed**.

## 완료 (커밋 SHA)
| Task | 내용 | 커밋 |
|---|---|---|
| 1 | approved_at 컬럼+백필 | 777248f74 |
| 2 | create_customer(approved=)+get_customer approved_at | 4da5c72f4 |
| 3 | access_level에 pending(전면차단) | d6f7fe944 |
| 4 | 구글·아이디가입=대기계정 | 15180d9df |
| 5 | approve_customer(멱등)+POST /api/admin/approve | 31371b4ac |
| 6 | 미들웨어 pending 게이트+대기실 화면 | dd08ce5a0 |
| **7** | **/api/grab이 pending도 차단(!=full) + 대기실 브랜드명 숏템박스 통일** | **7c099deee** |
| **8** | **관리자 대기명단 섹션+[승인] 버튼, list_customers approved_at 노출** | **384822c3f** |
| docs | 계획·설계 stale 숏템탑스→숏템박스 정리 | (직후 커밋) |

## 이번 세션 결정 (사장님)
- **브랜드명 = 숏템박스로 통일**. `_PENDING_HTML`을 `_fill_brand()` 통과시켜 하드코딩 "숏템탑스" 제거(라이브 커밋 9f63a3a21과 일치). 문서도 정리.
- **범위 = Task7+8 구현 후 finish**.

## 그라운딩 (실측)
- 페이월 테스트: `py -m pytest shopping_shorts/tests/test_paywall_*.py -q` = **43 passed**(기존 40 + 신규 3).
- admin.html 실제 렌더 확인(Playwright, 목킹 API): "🕓 승인 대기중(2명)" 섹션에 [✅ 승인] 버튼만 노출(set_plan 숨김) → 승인 클릭 → 대기명단 1명으로 줄고 그 계정이 "고객(4명)"에 체험중으로 이동·체험만료일 세팅. 정상.

## Task6 리뷰 처리 결과
- 🔴 Critical(grab pending 우회) → **Task7으로 해소**(`!= "full"`). 완료.
- 🟡 Important(브랜드명 불일치) → **숏템박스로 통일** 완료.
- 🟢 Minor: `GET /logout` CSRF 무방비 — **미처리(비차단)**. 저영향(멱등·세션삭제뿐)이나 원하면 POST 전용+토큰으로 강화 가능.

## 남은 Minor (전부 비차단 — 다음 세션/원할 때)
- `GET /logout` CSRF(`<img src="/logout">` 강제 로그아웃) — POST화 고려.
- store.py `get_or_create_by_google` docstring "가입시 체험 자동시작" stale → 승인시 시작.
- `/api/admin/approve`: 없는 customer_id면 200 `{customer:None}`(404 아님). 사소.

## ⏭ 다음
- finish 게이트 통과 시 main 병합 → 3분 뒤 라이브(shoppingshorts.duckdns.org).
- 라이브 후 실계정 그라운딩: 신규 가입 → 대기실 갇힘 → 사장님 /admin에서 [승인] → 열림 눈으로 재확인.
