# 장면그림 — 2026-09-09

## 변경
- 지시서.md의 확정 방식 ② 구현. verify_beat_screens는 최종 primary의 기존 seg_thumbs JPEG 1장을 모델에 보낸다. 캐시가 없으면 기존 work/<video_id>/*.mp4에서 추출하며 다운로드하지 않는다. 이미지 확보 실패는 종전 텍스트 판정으로 폴백한다.
- 썸네일 시점 판단을 frame_extract.extract_seg_thumb로 공용화하고 app._seg_strip_thumb도 같은 함수를 사용한다.
- 문자열 vault는 텍스트 비교용으로 유지, 이미지는 frame_script._call_with_key_rotation + types.Part.from_bytes 사용. 응답 오류·예외는 stderr에 기록하고 원본을 유지한다.
- 생성기 중간 검증을 제거하고 store._ensure_screen_time의 마지막 _fill 뒤에서 검증한다. 수동 대본 경로도 동일 출구를 지난다. 화면은 교체하지 않고 표시만 한다.
- 설정 이름은 지시서의 screen_verify_enabled로 통일, 기본 OFF. 기존 코드의 verify_screens_enabled는 더 이상 사용하지 않는다. 설정값을 실제로 켜지 않았다.
- work/screen_verify.jsonl 및 stderr에 job·비트·대사·primary·텍스트/이미지 판정·불일치·소요초·논리 호출 수 기록. calls는 재시도를 포함한 실제 네트워크 요청 수가 아니다. 성공 결과는 beat.screen_verification에 입력 지문과 보관하여 동일 입력 저장 시 재호출을 막는다.

## 확인
- 기존 계약 테스트 8개 유지. 이미지 전달·판정 불일치·OFF·실패·캐시·저장 출구 추가 테스트 14개.
- 화면 길이·수동 대본·silent-except 예산을 포함한 34개 통과. edit_plan·대본 편집·비트 삭제 관련 추가 44개 통과. 합계 78개. 앱 import 성공, diff 공백 검사 통과.
- 테스트는 모델 응답을 주입한 계약 검증이다. 실제 모델 품질·라이브 job·ffmpeg 추출·비용/시간은 확인하지 못했다. 이 트랙의 사용 가능한 모델 키 없음, 작업 시작 당시 data 내 mp4/JPEG 없음, ffmpeg 명령 없음.
- pytest 기본 임시 폴더 접근이 거부되어 이 트랙의 shopping_shorts/data 아래 새 basetemp로 실행했다.

## 다음
- git -C . add -A 실행이 .git/worktrees/장면그림/index.lock Permission denied로 거부됨. 스테이징/커밋 미완료. 권한이 허용되는 환경에서 현재 변경을 검토하고 커밋해야 한다. 권한 우회나 다른 폴더 수정은 하지 않았다.
- 실제 job 파일과 키가 있는 환경에서 트랙 변경본으로 실행해 이미지 요청과 실제 추가 호출·소요시간 확인. screen_verify.jsonl의 disagreed=true만 사람이 블라인드 검수한다. 정확도 향상 주장은 하지 않았다.
- 대표 이미지 1장으로만 검증하므로 전체 동작이나 alternates 전체를 검증하는 것은 아니다.
- 사용자 지시대로 finish/배포는 하지 않는다. 이 기록과 로그도 현재 트랙에만 작성했다.
