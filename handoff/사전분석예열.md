# 사전분석예열 — 핸드오프

## 2026-07-30 (PC: 사무실)

### 배경 — 왜 만들었나
사장님 제보: "렌즈에서 제작소로 영상 3개 보냈는데 제미니가 미리 장면작업 하고 있나?"
→ **서버 실측: 아무것도 안 돌고 있었다.**

| 확인 | 결과 |
|---|---|
| 오늘 담긴 영상 (`mix_basket`) | 3건 — 02:04:49 / 02:04:50 / 02:05:28 (인스타 `DUPHoKAEzOy`·`DTPjBK_D8vZ`·`DbUFVwmy40I`) |
| `produce_autoload` 오늘 | **0건** |
| `mix_jobs` 오늘 | **0건** (최신은 07-29 16:17) |

원인: 어제 설계 `docs/superpowers/specs/2026-07-29-추출속도-3종묶음-design.md`의
**②(extract_auto)만 라이브**(97706e5f9)고 **①(사전분석)은 미구현**이었다
(`worker.TASKS`에 `extract` 없음, `enqueue("extract")` 0건).
게다가 설계 ①의 트리거는 **즐겨찾기**(`api_wiki_save`)였는데 사장님 동작은 **담기**(`mix_basket`)라
애초에 대상도 아니었다 — 담길 때 도는 건 `_enrich_grab`(조회수·댓글 메타)뿐.

### ✅ 이번에 라이브 (c55b3afe1 → main 병합, 게이트 기준선13 그대로=신규0)
- **`shopping_shorts/prewarm.py` 신설** — `run_prewarm()`:
  다운로드 → `extract_auto` → `save_script` → `analyze_structure` → `save_extract_structure`.
  즉 **대본추출 + 제미니 구조분석 둘 다** 미리 채운다(제작소 1단계 `build_aipick`가 읽는 것).
- **`worker.TASKS["prewarm"]`** — 서버 BackgroundTasks가 아니라 독립워커.
  이유: 배포 `systemctl restart`가 진행 중 예열을 SIGKILL하는 구조를 피한다(07-29 실사고 계보).
- **담기 두 경로에서 enqueue** — `/api/mix/basket/toggle`, `/api/grab`. 헬퍼 `_enqueue_prewarm`.
- **`store.queue_has_pending(task, key, value)`** 추가 — 담기 연타/재담기 중복 적재 차단.
- 즐겨찾기(`api_wiki_save`)는 **손대지 않았다** — 이미 그 자리에서 동기 추출+캐시를 한다.

### 안전장치 (07-26 무한루프·크레딧 소모 사고 계보 그대로)
| # | 장치 | 위치 |
|---|---|---|
| ① | 래치를 `produce_autoload`로 **autoload와 공유** — 실패한 영상에 제미니 두 번 안 쓴다 | `prewarm._PREWARM_MAX_ATTEMPTS=1` |
| ② | 추출 **전에** 선래치 | `autoload_mark_attempt` |
| ③ | `full_text` 비면 **저장 금지** (빈 대본 영구캐시 방지) | `failed_empty` |
| ④ | 크레딧·글로벌캡 autoload와 동일 + 실패 시 환불 | `_gate()` |
| ⑤ | **일일 예열 상한 40** (담기는 즐겨찾기보다 남발) | `_PREWARM_DAILY_CAP` |
| ⑥ | 큐 중복 차단 | `queue_has_pending` |

`_gate()`는 app에서 **늦은 import**(순환 방지). import 실패 시 `skipped_nogate`로
예열을 아예 안 한다 — 게이트 없이 제미니를 무료로 태우지 않는다.

### 테스트
신규 12건 (`test_prewarm.py` 8 + `test_app_prewarm_enqueue.py` 4).
워커 배선 존재 / 캐시히트 스킵 / 래치 / 빈대본 미저장+환불 / 일일상한 /
구조분석 503 무해 / 큐중복 / 담기→적재·URL없음·캐시존재 스킵.
관련 기존 70건(basket·autoload·worker·grab) 통과.

### ⏭ 미완 — 다음 세션
1. **★라이브 실측 (제일 중요, 아직 안 함)**: 3분 뒤 새 영상 하나 담기 →
   서버에서 `job_queue` prewarm 소비 확인 → `script_extracts`에 그 shortcode
   `full_text`+`structure_json` 채워졌나 → **제작소 1단계 진입 시 로딩 사라졌나 체감 확인**.
   ```
   ssh ... "cd /home/ubuntu/lotto-stock-wiki && python3 -c \"
   import sqlite3;c=sqlite3.connect('shopping_shorts/data/reference.db')
   print(list(c.execute(\\\"select task,state,created_at from job_queue where task='prewarm' order by id desc limit 5\\\")))\""
   ```
2. **오늘 담긴 3건은 예열 안 됨** (코드 배포 전에 담김). 다시 담거나 제작소에서 그때 추출.
3. **큐 우선순위** — 지금 FIFO라 예열이 믹스 job 앞에 설 수 있다(1GB, 1개씩).
   설계 §6 위험 그대로 남아있음. 렌더 대기가 길어지면 `claim_next`에 task 우선순위 도입.
4. **일일상한 40이 적정한가** — 실사용 담기 빈도 보고 조정. 카운터는
   `settings.prewarm_daily` = `"YYYY-MM-DD|n"`.
5. ③(B1 실측·승격) 미완 — `frame_extract_enabled` 플래그는 여전히 off 유지(타 세션 소유).
