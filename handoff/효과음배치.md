> **소유 트랙**: 효과음배치 — 이 파일은 이 트랙 세션만 수정한다. 다른 트랙은 읽기만.
> 트리거: "효과음 이어서" / "sfx 이어서"

# 🔊 효과음 배치·미리듣기 (2026-08-21, 회사PC)

## 한 줄
효과음을 **장면처럼** 3단계 장면편집에서 고르고·빼고·타점 옮기고 **미리 들어본다**. 전부 라이브.

## 왜 시작했나
사장님: "이븐쇼핑같은 채널들 보면 효과음이 다 들어가는데 우리는 어떻게 구현하지?"
→ 조사해보니 **시스템은 2026-07-22에 이미 완성·라이브**였는데(`장면라이브러리` 트랙,
   `match_sfx` 역할매칭·amix·업로드 UI) **sfx 자산이 0개**라 아무 영상에도 안 붙고 있었다.
   구현 문제가 아니라 등록 문제였다.
이어서 사장님: "믹스 3단계에서 장면 넘어갈 때 자동배치 되게. 빼거나 다른거 넣거나. 장면처럼"

## ✅ 오늘 한 것 (전부 main 병합·배포·브라우저 검증)

### 1. 전환 타점 신설 — `transition`
```
first      = 칸 시작
last       = 칸의 마지막 자막(종전 기본)
transition = 칸이 끝나는 순간 = 다음 칸이 시작하는 지점   ← 신설, 훅의 기본값
```
- 렌더는 `offset = b["dur"]` 한 줄(`video_assemble._burn_captions`)
- 이름은 `scene_match.SFX_POSITIONS`가 통제 — **한쪽만 늘리면 조용히 `last`로 떨어진다**

### 2. API 확장 (`POST /api/produce/mix/{job}/sfx`)
빼기만 되던 것을 교체·타점변경까지. `asset_id`는 소유권 확인(`get_scene_asset`),
`position`은 통제어휘만. 
```
{beat_idx}                        → 빼기
{beat_idx, asset_id}              → 교체·삽입
{beat_idx, position:"transition"} → 타점만 이동
```

### 3. ★재매칭이 수동 선택을 안 덮는다
`match_type="manual"` 표시. 재매칭은 **대본이 바뀔 때마다** 도는데(mix_pipeline) 무조건
덮으면 사장님이 바꾼 효과음이 조용히 되돌아간다 — 오늘 오전 장면편집 무음 사고와 같은 종류.

### 4. UI — 3단계 장면편집 칸 도구줄
```
✏ 대본수정 | 🔊 띠용 ▾ | ▶ | 다음 칸 넘어갈 때 ▾ | 비우기 | ✨ 채우기
```
- 효과음 자산이 0개면 **줄 자체를 안 낸다**(빈 드롭다운은 고장으로 보인다)
- `▶` = 그 소리만 즉시 미리듣기
- 칸·전체 재생 시 **타점에 맞춰 실제로 울린다**(`scene_play.armSfx`)
- 정지·일시정지에 예약 해제(안 하면 멈춘 뒤 울린다)

## ⏭ 남은 것

1. **★타이밍 미검증** — 자동화 브라우저 탭이 계속 `document.hidden`이라 setTimeout이
   클램프돼 0.7~0.8초 늦게 측정됐다(칸 2.04초 → 2.8초에 울림). **활성 탭 정확도는
   확인 못 했다.** 사장님이 눌러보고 늦게 들리면 → setTimeout 대신 **실제 재생 진행
   (seq/오디오 currentTime) 기준**으로 바꿔야 한다.
2. **실제 효과음 파일 없음** — 지금 등록된 건 검증용으로 ffmpeg로 만든 단순 톤 2개
   (id 13 `띠용(테스트)`/훅, id 14 `뿅(테스트)`/본문). 사장님 파일로 교체 대상.
3. **`sfx_volume` 기본 60은 추정치** — 실제 파일 넣고 렌더해서 귀로 튜닝(2026-07-22부터 미결).
4. **효과음이 비트보다 길면 다음 비트로 흘러넘침**(v1 알려진 한계, 스펙 §4.3).
5. **4단계 검수판은 아직 빼기만** — 교체 UI는 3단계에만 넣었다.

## 등록 방법 (사장님용)
사이드바 → 🎬 장면 라이브러리 → 업로드 → 종류 `효과음(mp3/wav/m4a/aac)` + 역할 지정.
역할 통제어휘: 훅·반전·CTA·비법공개·반응·전환·본문

## 파일
- `shopping_shorts/scene_match.py` — `SFX_POSITIONS`·`_sfx_position`·`match_sfx`(manual 보호)
- `shopping_shorts/video_assemble.py` — 타점→초 계산(`_burn_captions`)
- `shopping_shorts/app.py` — `api_produce_mix_sfx`
- `shopping_shorts/static/scene_lab.html` — `sfxControl`·`setSfx`·`setSfxPos`
- `shopping_shorts/static/scene_play.js` — `armSfx`·`previewSfx`·`clearSfxTimers`
- 테스트 `shopping_shorts/tests/test_sfx_match.py` (16 green)

---
## 2026-09-22 — 이븐쇼핑 효과음 구간·자막 규칙 실측 (12편)
사장님: "어떤 구간에 어떤 효과음 규칙인지 정확하게 분석해서 벤치마킹 / 효과음 폴더 확인하고 모자라는 건 더 받아".
- ★기준점은 컷이 아니라 **자막 줄 교체**: 휙·둥·띠링 93~94%(우연 16%). 배경음악 없음(무음 70~84%).
- 구간: 제목훅=오프너 1발(12/12) / 떡밥 시작=휙→컷→틱(틱 12/12) / 반전="충격적인" 자막에 둥(6/10) / 마무리 1.5초 안 1발(11/12).
- 문구가 소리를 정함: 휙=기본 넘김, 둥=반전·강조, 띠링=결과·감탄·제품 첫등장, 뽁=시연 동작.
- 파일: PC 179개+mixkit 762개 대조, 똑같은 파일 없음. 확보=틱·휙·뽁 / 비슷=딸깍·띠링 / **부족=둥·오프너**.
- 산출물: 바탕화면 `이븐쇼핑_효과음_벤치마크/`(보고서·실측샘플·길이맞춤 후보). 도구 `tools/sfx_bench/`.
⏭ 사장님이 둥·오프너·띠링(1타) 파일을 받아오면 best2.py로 확정 → 서버 팩 교체 + `scene_match._CYCLE_SLOTS`를 "자막 교체 + 문구 규칙"으로 바꿀지 결정(라이브 변경이라 승인 필요).

### 2026-09-22 (2) — 효과음팩 20종
사장님: "후보 만들어서 프리셋으로, 1번팩 a b c d / 2번팩 a' b c' d 식으로 조합 많이 — 사용자들이 달라보이게".
- 바탕화면 `효과음 종합`(358개)에서 오프너=`두둥.mp3`(0.89)·띠링=`챙.mp3`(0.81) 확보.
- 칸별 후보: 오프너3(두둥+음높이변형)·뽁5·둥4(합성2+저음2)·띠링4·휙4·틱3·딸깍5(조립2 포함).
- 팩 20개: 두 팩 사이 다른 칸 최소4·중앙5(7칸 중). 최종 140파일 재채점 최저 0.63(둥). 음량=이븐쇼핑 실측비.
- 산출물 `바탕화면/이븐쇼핑_효과음_벤치마크/3_효과음팩/`(팩01~20·미리듣기·팩구성표.md/json).
⏭ 사장님 청취 판정 → 서버 sfx 자산 등록 + 회원별 팩 배정(라이브 변경=승인 후). 둥 진짜 파일 확보 시 교체. `효과음 종합` 라이선스 확인.
