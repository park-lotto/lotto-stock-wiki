# 유튜브 인제스트 + 포스트소스 겸용화 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 블로그 전용 인제스트를 범용 `post_ingest`로 겸용화해 blog·youtube를 한 모듈로 처리한다(소스 추가=config 한 줄).

**Architecture:** blog_ingest/blog_questionnaire를 source config 파라미터화된 `post_ingest`/`post_questionnaire`로 편입. 소스 차이(폴더·헤더라벨·레지스트리·source_type)는 `post_sources` config로. 추출·내용라우팅·텔레 2층 fan-out·sector_classify는 그대로 재사용.

**Tech Stack:** Python 3.14, google-genai, pytest. 재사용: telegram_questionnaire(fan-out), sector_classify, telegram_stance, db, vector_db.

## Global Constraints

- 설계 출처: `docs/superpowers/specs/2026-06-21-유튜브-인제스트-겸용화-design.md`.
- 소스 차이는 config: `{source_type, dir, header_label, registry}`. 코드는 단일.
- 라우터 출력=텔레 호환 타입명(stock_tips/sector/market/insight). 종목슬롯 sector_hint. 섹터=sectors.json.
- source_type∈{blog,youtube}, source_name=출처/채널(라벨 가변), stance_key="{source_name}|{대상}", trust=소스레지스트리(기본 C).
- 기존 blog 동작 동일성 유지(편입 후 blog 라이브 재검증). 텔레/리포트 fan-out 후방호환.
- blog_ingest/blog_questionnaire는 post_*로 **대체**(삭제). atom_pipeline STEP3.6=`post_ingest --source blog`, STEP3.7=`--source youtube`.
- Gemini 호출은 테스트 안 함(프롬프트 구조·결정론 로직만). 테스트는 로그/DB 격리.

---

### Task 1: post_sources config + youtube_registry

**Files:**
- Create: `pipeline/atoms/post_sources.py`
- Create: `pipeline/atoms/youtube_registry.json`
- Test: `pipeline/atoms/test_post_sources.py`

**Interfaces:**
- Produces: `source_config(name: str) -> dict` ({source_type, dir, header_label, registry}). 미등록 → KeyError.

- [ ] **Step 1: youtube_registry.json 작성**

```json
{
  "UP_CYCLE_STOCK": "B"
}
```

- [ ] **Step 2: Write the failing test**

```python
# pipeline/atoms/test_post_sources.py
import pytest
from pipeline.atoms.post_sources import source_config


def test_blog_config():
    c = source_config("blog")
    assert c["source_type"] == "blog"
    assert c["dir"] == "raw/blog"
    assert c["header_label"] == "출처"
    assert c["registry"] == "blog_registry.json"


def test_youtube_config():
    c = source_config("youtube")
    assert c["source_type"] == "youtube"
    assert c["dir"] == "raw/yt"
    assert c["header_label"] == "채널"
    assert c["registry"] == "youtube_registry.json"


def test_unknown_source_raises():
    with pytest.raises(KeyError):
        source_config("듣보소스")
```

- [ ] **Step 3: Run test to verify it fails**

Run: `python -m pytest pipeline/atoms/test_post_sources.py -v`
Expected: FAIL — 모듈 없음.

- [ ] **Step 4: post_sources.py 구현**

```python
# pipeline/atoms/post_sources.py
"""포스트형 소스(블로그·유튜브·뉴스) config. 소스 추가 = 여기 한 줄."""

POST_SOURCES = {
    "blog": {
        "source_type": "blog", "dir": "raw/blog",
        "header_label": "출처", "registry": "blog_registry.json",
    },
    "youtube": {
        "source_type": "youtube", "dir": "raw/yt",
        "header_label": "채널", "registry": "youtube_registry.json",
    },
}


def source_config(name: str) -> dict:
    return POST_SOURCES[name]
```

- [ ] **Step 5: Run tests to verify they pass**

Run: `python -m pytest pipeline/atoms/test_post_sources.py -v`
Expected: PASS (3 tests).

- [ ] **Step 6: Commit**

```bash
git add pipeline/atoms/post_sources.py pipeline/atoms/youtube_registry.json pipeline/atoms/test_post_sources.py
git commit -m "feat(post): 소스 config + youtube 레지스트리"
```

---

### Task 2: post_questionnaire (blog_questionnaire 편입·일반화)

**Files:**
- Create: `pipeline/atoms/post_questionnaire.py`
- Test: `pipeline/atoms/test_post_questionnaire.py`

**Interfaces:**
- Consumes: `sector_classify.sectors_list`, `atomizer._load_gemini_key`.
- Produces: `POST_PROMPT: str`, `post_trust(registry_file: str, name: str) -> str`(미등록 "C"), `extract_post(md_path: Path) -> dict`.

- [ ] **Step 1: Write the failing test**

```python
# pipeline/atoms/test_post_questionnaire.py
from pipeline.atoms.post_questionnaire import POST_PROMPT, post_trust


def test_post_trust_blog_registry():
    assert post_trust("blog_registry.json", "pokara61") == "B"


def test_post_trust_youtube_registry():
    assert post_trust("youtube_registry.json", "UP_CYCLE_STOCK") == "B"


def test_post_trust_unregistered_default_c():
    assert post_trust("blog_registry.json", "듣보") == "C"


def test_post_prompt_structure():
    for kw in ("target_kind", "stock_tips", "sector", "market", "insight", "quote"):
        assert kw in POST_PROMPT


def test_post_prompt_injects_sector_taxonomy():
    from pipeline.atoms.sector_classify import sectors_list
    assert sectors_list()[0] in POST_PROMPT


def test_extract_post_importable():
    from pipeline.atoms.post_questionnaire import extract_post
    assert callable(extract_post)
```

- [ ] **Step 2: Run test to verify it fails**

Run: `python -m pytest pipeline/atoms/test_post_questionnaire.py -v`
Expected: FAIL — 모듈 없음.

- [ ] **Step 3: post_questionnaire.py 구현**

```python
# pipeline/atoms/post_questionnaire.py
"""포스트형 소스(블로그·유튜브 등) 내용라우팅 질문지. blog_questionnaire 일반화."""
import json
from pathlib import Path

from google import genai
from google.genai import types

from .atomizer import _load_gemini_key
from .sector_classify import sectors_list

_MODEL = "gemini-3.1-flash-lite"
_DIR = Path(__file__).parent
_REGISTRY_CACHE: dict[str, dict] = {}


def post_trust(registry_file: str, name: str) -> str:
    if registry_file not in _REGISTRY_CACHE:
        path = _DIR / registry_file
        try:
            _REGISTRY_CACHE[registry_file] = json.loads(path.read_text(encoding="utf-8"))
        except (OSError, json.JSONDecodeError):
            _REGISTRY_CACHE[registry_file] = {}
    return _REGISTRY_CACHE[registry_file].get((name or "").strip(), "C")


POST_PROMPT = """너는 투자 컨텐츠(블로그·유튜브 등) 요약본을 '정해진 칸'에 옮겨 적는 사람이다.
판단하지 말고 칸을 채워라. 잡담·인사는 버려라.
철칙: 없으면 null(지어내지 마라) / 종목명·수치·날짜 원문 그대로 /
각 종목에 quote(근거 원문 문장)와 sector를 붙여라. sector는 아래 목록에서 정확히 하나(모르면 "기타"): """ + " / ".join(sectors_list()) + """

## STEP 0 — target_kind 판별 (본문 내용 기준)
- "stock_tips" : 특정 종목 집중 분석
- "sector"     : 단일 섹터/테마 전반
- "market"     : 시황/매크로/시장전략
- "insight"    : 에세이·전략·시장철학

## STEP 1 — target_kind에 맞는 칸만 채워라
[stock_tips] stocks:[{name, signal(bull/bear/neutral), reason, ts(null 허용), quote, sector}], news_items:[{fact, ts, quote}], quote
[sector] sector_name, sector_view(긍정/중립/부정), points(배열), stocks_mentioned:[{name, comment, ts, quote, sector}], events:[{fact, ts, quote}], quote
[market] market_direction, macro_events:[{fact, ts, quote}], sectors_mentioned:[{sector, stance, comment}], quote
[insight] leading_sectors(배열), stance:[{target, view(긍정/중립/부정), ts, quote}], methods:[{rule, quote}], stocks_mentioned:[{name, comment, ts, quote, sector}], quote

## 출력
target_kind + 해당 타입 칸만 채운 JSON 1개. 다른 텍스트 금지."""


def extract_post(md_path: Path) -> dict:
    """포스트 .md를 Gemini로 읽어 채워진 질문지(target_kind+슬롯) 반환. 실패 시 {}."""
    text = md_path.read_text(encoding="utf-8")
    client = genai.Client(api_key=_load_gemini_key())
    try:
        resp = client.models.generate_content(
            model=_MODEL, contents=[text, POST_PROMPT],
            config=types.GenerateContentConfig(response_mime_type="application/json"),
        )
        return json.loads(resp.text or "{}")
    except Exception as e:
        print(f"  [WARN] 포스트 추출 실패: {e}")
        return {}
```

- [ ] **Step 4: Run tests to verify they pass**

Run: `python -m pytest pipeline/atoms/test_post_questionnaire.py -v`
Expected: PASS (6 tests).

- [ ] **Step 5: Commit**

```bash
git add pipeline/atoms/post_questionnaire.py pipeline/atoms/test_post_questionnaire.py
git commit -m "feat(post): 범용 질문지(blog_questionnaire 일반화) + 소스별 trust"
```

---

### Task 3: post_ingest (blog_ingest 편입) + 파이프라인 + blog_* 제거

**Files:**
- Create: `pipeline/atoms/post_ingest.py`
- Modify: `scripts/atom_pipeline.py`
- Delete: `pipeline/atoms/blog_ingest.py`, `pipeline/atoms/blog_questionnaire.py`, `pipeline/atoms/test_blog_ingest_helpers.py`, `pipeline/atoms/test_blog_questionnaire.py`
- Test: `pipeline/atoms/test_post_ingest_helpers.py`

**Interfaces:**
- Consumes: `post_sources.source_config`, `post_questionnaire.extract_post/post_trust`, `questionnaire_to_atoms_tg`, `telegram_stance.deactivate_prior_stance`.
- Produces: `_parse_post_header(md_path, header_label) -> {source_name, date, link, title}`, `get_done_post_files(source_type)`, `get_pending_post(cfg, date_filter)`, `ingest_post(md_path, cfg) -> int`, `main()`.

- [ ] **Step 1: Write the failing test**

```python
# pipeline/atoms/test_post_ingest_helpers.py
import pytest
import pipeline.atoms.db as dbmod
from pipeline.atoms.post_ingest import _parse_post_header, get_done_post_files


@pytest.fixture(autouse=True)
def _temp_db(tmp_path, monkeypatch):
    monkeypatch.setattr(dbmod, "DB_PATH", tmp_path / "t.db")
    yield


def test_parse_header_blog_label(tmp_path):
    p = tmp_path / "2026-06-21_0900_바이오.md"
    p.write_text(
        "# 바이오\n- **출처**: pokara61 블로그\n- **날짜**: 2026-06-21\n"
        "- **링크**: [원문](https://blog.naver.com/pokara61/1)\n## 본문\n내용",
        encoding="utf-8")
    h = _parse_post_header(p, "출처")
    assert h["source_name"] == "pokara61"
    assert h["date"] == "2026-06-21"
    assert "blog.naver.com/pokara61/1" in h["link"]


def test_parse_header_youtube_label(tmp_path):
    p = tmp_path / "2026-06-05_0905_주식.md"
    p.write_text(
        "# 주식\n- **채널**: UP_CYCLE_STOCK\n- **날짜**: 2026-06-05\n"
        "- **링크**: [유튜브](https://www.youtube.com/watch?v=abc)\n## 핵심 요약\n내용",
        encoding="utf-8")
    h = _parse_post_header(p, "채널")
    assert h["source_name"] == "UP_CYCLE_STOCK"
    assert h["date"] == "2026-06-05"
    assert "youtube.com/watch?v=abc" in h["link"]


def test_get_done_post_files_by_source_type(tmp_path):
    dbmod.init_db(); dbmod.migrate_db()
    import uuid
    from datetime import datetime
    conn = dbmod.get_conn()
    conn.execute(
        "INSERT OR REPLACE INTO atoms (id,date,source_type,source_name,source_trust,"
        "raw_file,layer,sector,asset,asset_level,signal,event_type,magnitude,"
        "content_type,strength_score,content,relations,created_at) "
        "VALUES (?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?)",
        (str(uuid.uuid4()), "2026-06-05", "youtube", "UP_CYCLE_STOCK", 3,
         "raw/yt/2026-06-05_0905_주식.md", "L5", "반도체", "반도체", "sector",
         "neutral", "report", "minor", "fact", 2, "x", "[]", datetime.now().isoformat()),
    )
    conn.commit(); conn.close()
    done = get_done_post_files("youtube")
    assert "2026-06-05_0905_주식.md" in done
```

- [ ] **Step 2: Run test to verify it fails**

Run: `python -m pytest pipeline/atoms/test_post_ingest_helpers.py -v`
Expected: FAIL — 모듈 없음.

- [ ] **Step 3: post_ingest.py 구현**

```python
# pipeline/atoms/post_ingest.py
"""포스트형 소스(블로그·유튜브 등) 인제스트 — source config 파라미터화.

사용법:
    python -m pipeline.atoms.post_ingest --source blog --all [--date] [--limit] [--dry-run]
    python -m pipeline.atoms.post_ingest --source youtube --all
    python -m pipeline.atoms.post_ingest --source blog raw/blog/2026-06-21_xxx.md
"""
import re
import sys
import json
import argparse
from pathlib import Path

if sys.stdout.encoding and sys.stdout.encoding.lower() not in ("utf-8", "utf8"):
    try:
        sys.stdout.reconfigure(encoding="utf-8", errors="replace")
    except Exception:
        pass

from .db import init_db, migrate_db, insert_atom, get_conn
from .vector_db import embed_and_store
from .post_sources import source_config
from .post_questionnaire import extract_post, post_trust
from .telegram_questionnaire import questionnaire_to_atoms_tg
from .telegram_stance import deactivate_prior_stance

_ROOT = Path(__file__).parent.parent.parent
_Q_ROOT = _ROOT / "raw" / "post_q"
_FNAME = re.compile(r"^(\d{4}-\d{2}-\d{2})_\d+_(.+)$")


def _parse_post_header(md_path: Path, header_label: str) -> dict:
    text = md_path.read_text(encoding="utf-8", errors="replace")
    title = md_path.stem
    name = re.search(rf"\*\*{re.escape(header_label)}\*\*[:\s]+(.+)", text)
    date = re.search(r"\*\*날짜\*\*[:\s]+(\d{4}-\d{2}-\d{2})", text)
    link = re.search(r"\*\*링크\*\*.*?(https?://[^\)\s]+)", text)
    nm = name.group(1).strip() if name else title
    nm = re.sub(r"\s*(블로그|유튜브)$", "", nm).strip()
    d = date.group(1) if date else ""
    if not d:
        m = re.search(r"(\d{4}-\d{2}-\d{2})", md_path.name)
        d = m.group(1) if m else ""
    return {"source_name": nm, "date": d, "link": link.group(1) if link else "", "title": title}


def get_done_post_files(source_type: str) -> set[str]:
    conn = get_conn()
    rows = conn.execute(
        "SELECT DISTINCT raw_file FROM atoms WHERE source_type=?", (source_type,)
    ).fetchall()
    conn.close()
    return {Path(r[0]).name for r in rows if r[0]}


def get_pending_post(cfg: dict, date_filter: str = None) -> list[Path]:
    src_dir = _ROOT / cfg["dir"]
    if not src_dir.exists():
        return []
    done = get_done_post_files(cfg["source_type"])
    files = []
    for f in sorted(src_dir.glob("*.md")):
        if "_analysis" in f.name:
            continue
        m = _FNAME.match(f.stem)
        if date_filter and (not m or m.group(1) != date_filter):
            continue
        if f.name in done:
            continue
        files.append(f)
    return sorted(files, reverse=True)


def _save_artifact(q: dict, source_type: str, date: str, title: str) -> None:
    out = _Q_ROOT / source_type / (date or "unknown")
    out.mkdir(parents=True, exist_ok=True)
    safe = re.sub(r"[^\w가-힣]+", "_", title)[:60]
    (out / f"{safe}.json").write_text(
        json.dumps(q, ensure_ascii=False, indent=2), encoding="utf-8")


def ingest_post(md_path: Path, cfg: dict) -> int:
    h = _parse_post_header(md_path, cfg["header_label"])
    q = extract_post(md_path)
    if not q or not q.get("target_kind"):
        print(f"  [WARN] 빈/미라우팅 질문지: {md_path.name}")
        return 0
    _save_artifact(q, cfg["source_type"], h["date"], h["title"])
    meta = {
        "date": h["date"], "channel": h["source_name"], "type": q["target_kind"],
        "source_type": cfg["source_type"], "trust": post_trust(cfg["registry"], h["source_name"]),
        "raw_file": str(md_path),
    }
    atoms = questionnaire_to_atoms_tg(q, meta)
    for a in atoms:
        if a.get("stance_key"):
            deactivate_prior_stance(a["stance_key"], keep_id=a["id"])
        insert_atom(a)
        try:
            embed_and_store(a)
        except Exception as e:
            print(f"  [WARN] embed 실패: {e}")
    return len(atoms)


def main():
    ap = argparse.ArgumentParser()
    ap.add_argument("--source", required=True, help="blog | youtube")
    ap.add_argument("file", nargs="?")
    ap.add_argument("--all", action="store_true")
    ap.add_argument("--date", default=None)
    ap.add_argument("--limit", type=int, default=30)
    ap.add_argument("--dry-run", action="store_true")
    args = ap.parse_args()
    cfg = source_config(args.source)
    init_db(); migrate_db()

    if args.file:
        targets = [Path(args.file)]
    elif args.all:
        targets = get_pending_post(cfg, args.date)[: args.limit]
        print(f"미처리 {args.source}: {len(targets)}개\n")
    else:
        print("file 또는 --all 필요")
        return

    if args.dry_run:
        for f in targets:
            print(f"  {f.name} → {_parse_post_header(f, cfg['header_label'])['source_name']}")
        return

    total = 0
    for i, f in enumerate(targets, 1):
        print(f"[{i}/{len(targets)}] {f.name}")
        n = ingest_post(f, cfg)
        total += n
        print(f"  → {n}개 원자")
    print(f"\n완료: {len(targets)}개, {total}개 원자")


if __name__ == "__main__":
    main()
```

- [ ] **Step 4: blog_* 제거 + atom_pipeline 교체**

blog 모듈·테스트 삭제:
```bash
git rm pipeline/atoms/blog_ingest.py pipeline/atoms/blog_questionnaire.py pipeline/atoms/test_blog_ingest_helpers.py pipeline/atoms/test_blog_questionnaire.py
```
(blog_registry.json은 유지 — post_ingest가 config로 참조.)

`scripts/atom_pipeline.py`의 STEP3.6(blog) 블록을 교체하고 STEP3.7(youtube) 추가:
```python
    # 3.6단계: 블로그 인제스트 (겸용 post_ingest)
    run([PYTHON, "-m", "pipeline.atoms.post_ingest", "--source", "blog",
         "--all", "--limit", "30"],
        "STEP3.6 blog")

    # 3.7단계: 유튜브 인제스트 (겸용 post_ingest)
    run([PYTHON, "-m", "pipeline.atoms.post_ingest", "--source", "youtube",
         "--all", "--limit", "30"],
        "STEP3.7 youtube")
```
dry-run 블록의 기존 `blog_ingest` 줄을 교체 + youtube 추가:
```python
        subprocess.run([PYTHON, "-m", "pipeline.atoms.post_ingest", "--source", "blog", "--all", "--dry-run", "--limit", "5"], cwd=str(ROOT))
        subprocess.run([PYTHON, "-m", "pipeline.atoms.post_ingest", "--source", "youtube", "--all", "--dry-run", "--limit", "5"], cwd=str(ROOT))
```

- [ ] **Step 5: Run tests to verify they pass**

Run: `python -m pytest pipeline/atoms/test_post_ingest_helpers.py pipeline/atoms/ -q`
Expected: 신규 post 테스트 PASS, blog_* 테스트 삭제로 사라짐, 전체 회귀 PASS.

- [ ] **Step 6: Commit**

```bash
git add pipeline/atoms/post_ingest.py pipeline/atoms/test_post_ingest_helpers.py scripts/atom_pipeline.py
git commit -m "feat(post): 겸용 오케스트레이터 + blog 편입(blog_* 제거) + STEP3.6/3.7"
```

---

### Task 4: 라이브 검증 (blog 회귀 + youtube)

**Files:** (코드 변경 없음 — 검증)

- [ ] **Step 1: 전체 테스트 회귀**

Run: `python -m pytest pipeline/atoms/ -q`
Expected: 전체 PASS (post 신규 + 기존 텔레/리포트/sector).

- [ ] **Step 2: dry-run 스모크 (blog + youtube)**

Run: `python -m pipeline.atoms.post_ingest --source blog --all --dry-run --limit 3`
Run: `python -m pipeline.atoms.post_ingest --source youtube --all --dry-run --limit 3`
Expected: 각 미처리 목록 + source_name(블로거/채널) 출력, 에러 없음.

- [ ] **Step 3: blog 회귀 — 1포스트 재인제스트**

기존 blog 원자 삭제 후 재인제스트(동작 동일성):
```bash
python -c "from pipeline.atoms.db import get_conn; c=get_conn(); c.execute(\"DELETE FROM atoms WHERE source_type='blog'\"); c.commit(); c.close()"
python -m pipeline.atoms.post_ingest --source blog "raw/blog/2026-06-21_0900_바이오 주식과 코스닥 승강제에 대해서.md"
```
Expected: 원자 생성, source_type=blog, sector=바이오(겸용화 후에도 동일).

- [ ] **Step 4: youtube 라이브 — 1영상 인제스트**

raw/yt에 영상이 있어야 함. 1개 인제스트:
```bash
python -m pipeline.atoms.post_ingest --source youtube "raw/yt/2026-06-05_0905_[주식] 곧 1만시대 코스닥 3000시대 열릴겁니다 오늘 금감원 '비상대.md"
```
Expected: 원자 생성, source_type=youtube, source_name=UP_CYCLE_STOCK.

- [ ] **Step 5: DB 표면 확인**

```bash
python -c "import sys; sys.stdout.reconfigure(encoding='utf-8'); from pipeline.atoms.db import get_conn; c=get_conn(); [print(r) for r in c.execute(\"SELECT source_type, source_name, asset_level, sector FROM atoms WHERE source_type IN ('blog','youtube') ORDER BY source_type\").fetchall()]; c.close()"
```
Expected: blog(pokara61)·youtube(UP_CYCLE_STOCK) 원자 공존, 섹터 분류됨.

- [ ] **Step 6: Commit**

```bash
git add -A
git commit -m "test(post): 겸용화 라이브 검증 (blog 회귀 + youtube)"
```

---

## Self-Review

**1. Spec coverage:**
- §3 겸용 아키텍처(config·post_ingest·재사용) → Task 1·2·3 ✅
- §4 post_sources config → Task 1 ✅
- §5 post_questionnaire(POST_PROMPT 중립·post_trust·extract_post) → Task 2 ✅
- §6 post_ingest(헤더라벨 가변·source config·STEP3.6/3.7) → Task 3 ✅
- §7 blog 편입(blog_* 제거·pipeline 교체·재검증) → Task 3·4 ✅
- §2 소스별 레지스트리(기본 C) → Task 1(youtube_registry)·Task 2(post_trust) ✅

**갭:** youtube "주요 발언" verbatim quote 강화는 스펙 §9 범위 밖. POST_PROMPT가 핵심요약 quote 사용 — 정합.

**2. Placeholder scan:** TBD/TODO 없음. Task 4 Step 4는 실제 파일명 예시 제공(존재 확인 안내).

**3. Type consistency:** `source_config(name)->dict`, `post_trust(registry_file, name)->str`, `extract_post(md_path)->dict`, `_parse_post_header(md_path, header_label)->{source_name,date,link,title}`, `ingest_post(md_path, cfg)->int` — Task 정의·사용 일치. meta["type"]=target_kind, source_type=cfg["source_type"], channel=source_name 일치. fan-out source_type 전파(이전 작업 완료)와 정합.
