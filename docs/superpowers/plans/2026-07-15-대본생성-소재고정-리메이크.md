# 대본생성 소재고정 리메이크 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 도서관 대본생성의 애매한 모드 A/B를 `remake`(원본 소재 고정 리메이크) / `transplant`(내 제품 이식) 2모드로 재편하고, 리메이크 모드가 원본 대본에서 소재를 자동 감지해 고정한다.

**Architecture:** 백엔드 `script_generate.py`에 소재 감지 헬퍼(`detect_subject`)를 추가하고 `generate_variations`의 프롬프트 분기를 소재 고정 방향으로 교체. `app.py`에 소재 감지 API를 추가하고 생성 API body를 확장. `library.html` 생성 모달을 2모드 + 소재 입력칸(자동 프리필)로 교체.

**Tech Stack:** Python(FastAPI), Google GenAI(Gemini, key_vault 캐스케이드 키풀), 바닐라 JS/HTML, pytest + FastAPI TestClient.

---

## 파일 구조

| 파일 | 변경 | 책임 |
|------|------|------|
| `shopping_shorts/script_generate.py` | 수정 | `detect_subject` 신규, `generate_variations` mode/subject 재편 |
| `shopping_shorts/app.py` | 수정 | `GET /api/wiki/subject` 신규, `POST /api/wiki/generate` body 확장 |
| `shopping_shorts/static/library.html` | 수정 | 2모드 UI·소재칸 프리필·toggle·runGen body |
| `shopping_shorts/tests/test_script_generate.py` | 수정 | detect_subject·mode 분기 테스트 |
| `shopping_shorts/tests/test_app.py` | 수정 | subject API·generate body 테스트 |

**테스트 실행 환경:** repo 루트에서 `python -m pytest shopping_shorts/tests/<file>::<test> -v`. 서버 기동(수동 검증용)은 repo 루트에서 `python -m uvicorn shopping_shorts.app:app --port <p>` (절대임포트).

---

## Task 1: 소재 감지 헬퍼 `detect_subject`

**Files:**
- Modify: `shopping_shorts/script_generate.py` (파일 끝, `refine_draft_partial` 뒤에 추가)
- Test: `shopping_shorts/tests/test_script_generate.py`

- [ ] **Step 1: Write the failing tests**

`shopping_shorts/tests/test_script_generate.py` 끝에 추가:

```python
def test_detect_subject_returns_one_line(monkeypatch):
    _wire(monkeypatch, json.dumps({"subject": "무선 가습기 물때 청소"}))
    out = script_generate.detect_subject("가습기 물때 어떻게 닦지... 이거 하나면 끝")
    assert out == "무선 가습기 물때 청소"


def test_detect_subject_no_keys(monkeypatch):
    monkeypatch.setattr(comment_gen, "SHORTS_GEMINI_KEYS", [])
    assert script_generate.detect_subject("아무 대본") == ""


def test_detect_subject_empty_text(monkeypatch):
    monkeypatch.setattr(comment_gen, "SHORTS_GEMINI_KEYS", ["k"])
    assert script_generate.detect_subject("   ") == ""


def test_detect_subject_bad_json(monkeypatch):
    _wire(monkeypatch, "not json")
    assert script_generate.detect_subject("대본") == ""


def test_detect_subject_strips_whitespace(monkeypatch):
    _wire(monkeypatch, json.dumps({"subject": "  물때 청소 \n"}))
    assert script_generate.detect_subject("대본") == "물때 청소"
```

- [ ] **Step 2: Run tests to verify they fail**

Run: `python -m pytest shopping_shorts/tests/test_script_generate.py -k detect_subject -v`
Expected: FAIL — `AttributeError: module 'shopping_shorts.script_generate' has no attribute 'detect_subject'`

- [ ] **Step 3: Write minimal implementation**

`shopping_shorts/script_generate.py` 끝(`refine_draft_partial` 함수 뒤)에 추가:

```python
_SUBJECT_SCHEMA = {
    "type": "object",
    "properties": {"subject": {"type": "string"}},
    "required": ["subject"],
}

_SUBJECT_PROMPT = """다음 한국 쇼핑 숏폼 대본이 다루는 '소재'(무엇에 관한 영상인지 —
제품/장면/주제)를 한 줄 명사구로만 요약해라. 말투·훅 방식·기법이 아니라 '무엇'인지만.
예: "무선 가습기 물때 청소".

[대본]
{full_text}

다음 JSON으로만 출력: {{"subject": "소재 한 줄"}}"""


def detect_subject(full_text, max_key_tries=3):
    """원본 대본 원문에서 '소재 한 줄'을 Gemini로 요약. 실패/무키/빈입력이면 ""."""
    if not comment_gen.SHORTS_GEMINI_KEYS or not (full_text or "").strip():
        return ""
    prompt = _SUBJECT_PROMPT.format(full_text=full_text[:3000])
    for _ in range(max_key_tries):
        key, ki = comment_gen._current_key_and_idx()
        if key is None:
            return ""
        try:
            resp = comment_gen._client_for_key(key).models.generate_content(
                model=_MODEL, contents=prompt,
                config=types.GenerateContentConfig(
                    response_mime_type="application/json", response_schema=_SUBJECT_SCHEMA),
            )
            return (json.loads(resp.text).get("subject", "") or "").strip()
        except Exception as e:  # noqa: BLE001 — 감지 실패는 치명적 아님(빈 문자열)
            if (comment_gen.key_vault.is_daily_exhausted_error(e)
                    or comment_gen.key_vault.is_account_disabled_error(e)):
                comment_gen._mark_key_exhausted(ki)
                continue
            return ""
    return ""
```

- [ ] **Step 4: Run tests to verify they pass**

Run: `python -m pytest shopping_shorts/tests/test_script_generate.py -k detect_subject -v`
Expected: PASS (5 passed)

- [ ] **Step 5: Commit**

```bash
git add shopping_shorts/script_generate.py shopping_shorts/tests/test_script_generate.py
git commit -m "feat(shopping_shorts): 대본 소재 자동감지 detect_subject 헬퍼"
```

---

## Task 2: `generate_variations` 모드 재편 (remake/transplant + subject)

**Files:**
- Modify: `shopping_shorts/script_generate.py:215-227` (`generate_variations` 시그니처·분기)
- Test: `shopping_shorts/tests/test_script_generate.py`

- [ ] **Step 1: Write the failing tests**

`shopping_shorts/tests/test_script_generate.py` 끝에 추가:

```python
def _capture_prompt(mp, holder):
    """generate_content에 들어간 prompt를 holder['p']에 담고 정상 JSON 반환하도록 배선."""
    mp.setattr(comment_gen, "SHORTS_GEMINI_KEYS", ["k"])
    mp.setattr(comment_gen, "_current_key_and_idx", lambda: ("k", 0))
    def gen(model, contents, config):
        holder["p"] = contents
        return type("R", (), {"text": json.dumps({"drafts": [{"hook": "h", "script": "s", "applied": "a"}]})})()
    models = type("M", (), {"generate_content": staticmethod(gen)})()
    mp.setattr(comment_gen, "_client_for_key", lambda key: type("C", (), {"models": models})())


def test_remake_mode_prompt_locks_subject(monkeypatch):
    h = {}
    _capture_prompt(monkeypatch, h)
    script_generate.generate_variations(_STRUCT, "원본 대본", {}, {}, mode="remake",
                                        subject="무선 가습기 물때 청소")
    assert "소재(고정): 무선 가습기 물때 청소" in h["p"]
    assert "중복 회피" in h["p"]
    assert "신선하게 변주" not in h["p"]  # 옛 A 문구 부재


def test_transplant_mode_prompt_uses_my_topic(monkeypatch):
    h = {}
    _capture_prompt(monkeypatch, h)
    script_generate.generate_variations(_STRUCT, "원본 대본", {}, {}, mode="transplant",
                                        my_topic="여름 결로 방지 커튼")
    assert "내 주제/제품: 여름 결로 방지 커튼" in h["p"]


def test_legacy_mode_A_maps_to_remake(monkeypatch):
    h = {}
    _capture_prompt(monkeypatch, h)
    script_generate.generate_variations(_STRUCT, "원본", {}, {}, mode="A", subject="물때 청소")
    assert "소재(고정): 물때 청소" in h["p"]


def test_legacy_mode_B_maps_to_transplant(monkeypatch):
    h = {}
    _capture_prompt(monkeypatch, h)
    script_generate.generate_variations(_STRUCT, "원본", {}, {}, mode="B", my_topic="커튼")
    assert "내 주제/제품: 커튼" in h["p"]


def test_remake_without_subject_still_locks_original(monkeypatch):
    h = {}
    _capture_prompt(monkeypatch, h)
    script_generate.generate_variations(_STRUCT, "원본 대본 원문", {}, {}, mode="remake", subject="")
    assert "중복 회피" in h["p"]
    assert "소재(고정):" not in h["p"]  # 빈 소재면 명시줄 생략, full_text로만 잠금
```

- [ ] **Step 2: Run tests to verify they fail**

Run: `python -m pytest shopping_shorts/tests/test_script_generate.py -k "remake or transplant or legacy_mode" -v`
Expected: FAIL — remake 프롬프트에 소재 고정 문구가 없어 assert 실패(현재는 "신선하게 변주"가 들어감)

- [ ] **Step 3: Write implementation**

`shopping_shorts/script_generate.py`의 `generate_variations` 시그니처(215-216행)와 분기(221-224행)를 교체.

시그니처를 다음으로:

```python
def generate_variations(structure, full_text, elem_modes, category_lookup, mode="remake",
                        my_topic="", subject="", n=3, max_key_tries=3):
```

`n = max(...)` 다음의 mode 분기(현재 221-224행)를 다음으로 교체:

```python
    mode = {"A": "remake", "B": "transplant"}.get(mode, mode)  # 구버전 하위호환
    if mode == "transplant" and (my_topic or "").strip():
        topic_line = (f"주제: 구조만 빌리고 아래 '내 주제/제품'에 맞춰 새로 써라."
                      f"\n내 주제/제품: {my_topic.strip()}")
    else:
        subj = (subject or "").strip()
        subj_line = f"\n소재(고정): {subj}" if subj else ""
        topic_line = (
            "주제: 아래 원본의 '소재'를 그대로 유지한 리메이크다. 원본의 제품·사실·장면·정보는 "
            "바꾸지 말고, 표현(훅 문장·어휘·문장 순서·말투)만 새로 써서 원문을 그대로 베끼지 않게 "
            "(중복 회피) 리라이트하라. 없던 내용이나 다른 제품을 지어내지 마라." + subj_line)
```

(`prompt = _GEN_PROMPT.format(...)` 이하는 그대로 둔다.)

- [ ] **Step 4: Run tests to verify they pass**

Run: `python -m pytest shopping_shorts/tests/test_script_generate.py -v`
Expected: PASS (기존 테스트 + 신규 전부 통과. 기존 `test_generate_ok` 등은 `mode="A"`를 넘기는데 이제 remake로 매핑돼 정상 동작)

- [ ] **Step 5: Commit**

```bash
git add shopping_shorts/script_generate.py shopping_shorts/tests/test_script_generate.py
git commit -m "feat(shopping_shorts): 대본생성 모드 remake/transplant 재편 — 소재 고정 리메이크"
```

---

## Task 3: API — 소재 감지 엔드포인트 + 생성 body 확장

**Files:**
- Modify: `shopping_shorts/app.py:644-683` (`api_wiki_element_options` 뒤에 `api_wiki_subject` 추가, `api_wiki_generate` body 확장)
- Test: `shopping_shorts/tests/test_app.py`

- [ ] **Step 1: Write the failing tests**

`shopping_shorts/tests/test_app.py` 끝에 추가(파일 상단 `import` 블록에 `from shopping_shorts import app as app_module`, `from shopping_shorts import script_generate`가 이미 있으면 재사용; 없으면 테스트 함수 내부에서 `from shopping_shorts import script_generate` import):

```python
def test_wiki_subject_returns_detected(monkeypatch):
    from shopping_shorts import app as app_module
    from shopping_shorts import script_generate
    monkeypatch.setattr(app_module, "_AUTH_ON", False)
    monkeypatch.setattr(app_module.Store, "get_wiki_item",
                        lambda self, sc, customer_id=0: {"full_text": "가습기 물때"})
    monkeypatch.setattr(script_generate, "detect_subject", lambda ft: "무선 가습기 물때 청소")
    client = TestClient(app_module.app)
    r = client.get("/api/wiki/subject?shortcode=ABC")
    assert r.status_code == 200
    assert r.json()["subject"] == "무선 가습기 물때 청소"


def test_wiki_subject_404_when_no_item(monkeypatch):
    from shopping_shorts import app as app_module
    monkeypatch.setattr(app_module, "_AUTH_ON", False)
    monkeypatch.setattr(app_module.Store, "get_wiki_item", lambda self, sc, customer_id=0: None)
    client = TestClient(app_module.app)
    r = client.get("/api/wiki/subject?shortcode=NOPE")
    assert r.status_code == 404


def test_wiki_generate_passes_subject(monkeypatch):
    from shopping_shorts import app as app_module
    from shopping_shorts import script_generate
    monkeypatch.setattr(app_module, "_AUTH_ON", False)
    monkeypatch.setattr(app_module.Store, "get_wiki_item",
                        lambda self, sc, customer_id=0: {"structure": {}, "full_text": "원본", "category": ""})
    monkeypatch.setattr(app_module.Store, "get_element_options", lambda self, cat: {})
    monkeypatch.setattr(app_module.Store, "save_draft", lambda self, *a, **k: None)
    captured = {}
    def fake_gen(structure, full_text, elem_modes, lookup, mode="remake", my_topic="", subject="", n=3):
        captured.update(mode=mode, subject=subject, my_topic=my_topic)
        return [{"hook": "h", "script": "s", "applied": "a"}]
    monkeypatch.setattr(script_generate, "generate_variations", fake_gen)
    client = TestClient(app_module.app)
    r = client.post("/api/wiki/generate?shortcode=ABC",
                    json={"mode": "remake", "subject": "물때 청소", "n": 1, "elem_modes": {}})
    assert r.status_code == 200
    assert captured["mode"] == "remake" and captured["subject"] == "물때 청소"
```

- [ ] **Step 2: Run tests to verify they fail**

Run: `python -m pytest shopping_shorts/tests/test_app.py -k "wiki_subject or generate_passes_subject" -v`
Expected: FAIL — `/api/wiki/subject` 404(라우트 없음), generate 테스트는 `subject` 미전달로 KeyError

- [ ] **Step 3: Write implementation**

`shopping_shorts/app.py`에서 `api_wiki_element_options`(644행) 함수 정의 뒤에 신규 엔드포인트 추가:

```python
@app.get("/api/wiki/subject")
def api_wiki_subject(request: Request, shortcode: str):
    """리메이크 모달 프리필용 — 위키 항목 원문에서 소재 한 줄 자동 감지."""
    store = Store(DB_PATH)
    it = store.get_wiki_item(shortcode, customer_id=_cid(request))
    if not it:
        return JSONResponse(status_code=404, content={"ok": False, "error": "위키에 없는 항목"})
    subject = script_generate.detect_subject(it.get("full_text") or "")
    return {"ok": True, "subject": subject}
```

그리고 `api_wiki_generate`의 body 파싱(663-664행)에 `subject` 추가하고 `generate_variations` 호출(673-675행)에 전달:

663-664행:
```python
    mode = body.get("mode", "remake")
    my_topic = body.get("my_topic", "")
    subject = body.get("subject", "")
```

673-675행:
```python
    drafts = script_generate.generate_variations(
        it.get("structure") or {}, it.get("full_text") or "", elem_modes, category_lookup,
        mode=mode, my_topic=my_topic, subject=subject, n=n)
```

- [ ] **Step 4: Run tests to verify they pass**

Run: `python -m pytest shopping_shorts/tests/test_app.py -k "wiki_subject or generate_passes_subject" -v`
Expected: PASS (3 passed)

- [ ] **Step 5: Commit**

```bash
git add shopping_shorts/app.py shopping_shorts/tests/test_app.py
git commit -m "feat(shopping_shorts): 소재감지 API + 생성 body subject 전달"
```

---

## Task 4: UI — 2모드 모달 + 소재칸 자동 프리필 (`library.html`)

**Files:**
- Modify: `shopping_shorts/static/library.html:66-100`(모달 CSS 근처, 신규 CSS), `:126-131`(주제 섹션 HTML), `:242-265`(openGen), `:271`(toggleTopic), `:281-288`(runGen)

- [ ] **Step 1: CSS 추가**

`shopping_shorts/static/library.html`의 `<style>` 안 생성 모달 CSS 근처(예: `.draft` 정의들 앞, 100행 부근)에 추가:

```css
  .subject-row{margin:6px 0 10px 20px;font-size:13px}
  .subject-row .subj-label{color:var(--sub);margin-right:6px}
  .subject-row #genSubject{width:70%;background:#0d131c;border:1px solid var(--line);
    border-radius:6px;color:var(--txt);padding:6px 10px;font-size:13px}
  .subject-row .subj-hint{color:var(--sub);font-size:11.5px;margin-top:5px}
```

- [ ] **Step 2: 주제 섹션 HTML 교체**

126-131행(`<div class="gen-sec">주제</div>` ~ `<input id="genTopic" ...>`)을 다음으로 교체:

```html
        <div class="gen-sec">주제 / 소재</div>
        <div class="modes">
          <label><input type="radio" name="genmode" value="remake" checked onchange="toggleTopic()"> ♻️ 원본 소재 그대로 · 표현만 새로 (중복 회피)</label>
          <div class="subject-row" id="subjectRow">
            <span class="subj-label">감지된 소재:</span><input id="genSubject" placeholder="소재 감지 중…">
            <div class="subj-hint">원본 영상 재사용·2영상 믹싱용. 제품·사실·장면은 그대로, 훅·문장·순서만 새로.</div>
          </div>
          <label><input type="radio" name="genmode" value="transplant" onchange="toggleTopic()"> 🔀 내 제품으로 이식 (구조만 빌림)</label>
          <input id="genTopic" placeholder="예: 무선 가습기 / 여름 결로 방지 커튼" style="display:none">
        </div>
```

- [ ] **Step 3: openGen 프리필 추가**

`openGen`(242행) 함수의 마지막 `});`(264행, 요소토글 forEach 닫힘) 다음, 함수 닫는 `}`(265행) 앞에 추가:

```javascript
  // 리메이크가 기본 → 원본 소재 자동감지 프리필
  const subjEl = document.getElementById('genSubject');
  subjEl.value=''; subjEl.placeholder='소재 감지 중…';
  document.querySelector('input[name=genmode][value=remake]').checked = true;
  toggleTopic();
  try{
    const s = await (await fetch('/api/wiki/subject?shortcode='+encodeURIComponent(sc))).json();
    if(s.ok) subjEl.value = s.subject || '';
  }catch(e){}
  subjEl.placeholder='예: 무선 가습기 물때 청소';
```

- [ ] **Step 4: toggleTopic 교체**

271행 `function toggleTopic(){ ... }` 한 줄을 다음으로 교체:

```javascript
function toggleTopic(){
  const mode=document.querySelector('input[name=genmode]:checked').value;
  document.getElementById('genTopic').style.display = mode==='transplant' ? 'block' : 'none';
  document.getElementById('subjectRow').style.display = mode==='remake' ? 'block' : 'none';
}
```

- [ ] **Step 5: runGen mode/subject 반영**

281-288행 교체:

```javascript
  const mode=document.querySelector('input[name=genmode]:checked').value;
  const topic=document.getElementById('genTopic').value.trim();
  const subject=document.getElementById('genSubject').value.trim();
  if(mode==='transplant' && !topic){ res.innerHTML='<div class="gen-err">내 주제/제품을 입력하세요.</div>'; return; }
  btn.disabled=true; btn.textContent='⏳ 생성 중…'; res.innerHTML='<div class="gen-load">✨ S급 구조를 빌려 초안 만드는 중… (10~20초)</div>';
  try{
    const d=await (await fetch('/api/wiki/generate?shortcode='+encodeURIComponent(window._genSC), {
      method: 'POST', headers: {'Content-Type':'application/json'},
      body: JSON.stringify({mode, my_topic: topic, subject, n: 3, elem_modes: elemModes}),
    })).json();
```

- [ ] **Step 6: JS 구문 검사**

Run: `node --check shopping_shorts/static/library.html 2>&1 || python -c "import re,sys; s=open('shopping_shorts/static/library.html',encoding='utf-8').read(); m=re.search(r'<script>(.*)</script>', s, re.S); open('/tmp/lib.js','w',encoding='utf-8').write(m.group(1)); print('extracted')"`
Then: `node --check /tmp/lib.js`
Expected: 구문 에러 없음(출력 없음 = OK). node 없으면 브라우저 콘솔로 대체(Step에서 grounding).

- [ ] **Step 7: Commit**

```bash
git add shopping_shorts/static/library.html
git commit -m "feat(shopping_shorts): 대본생성 모달 2모드 재편 + 소재 자동감지 프리필"
```

---

## Task 5: 실렌더 grounding + 라이브 검증

**Files:** 없음(검증 전용)

- [ ] **Step 1: 로컬 서버 기동 + 브라우저 grounding**

repo 루트에서:
```bash
python -m uvicorn shopping_shorts.app:app --port 8099
```
Playwright MCP로 `http://localhost:8099/library` 열고(인증 off 또는 로그인):
- 도서관 카드에서 "🎬 이 구조로 생성" 클릭 → 모달 열림
- **소재칸이 "소재 감지 중…" → 감지값으로 프리필**되는지 확인(무키 환경이면 빈 칸 폴백 정상)
- 라디오를 "🔀 내 제품으로 이식"으로 토글 → 소재칸 숨고 제품 입력칸 표시. 다시 리메이크로 → 반대.
- 유지/변형 요소 토글 7개 여전히 정상 동작.
- 브라우저 콘솔 JS 에러 0 확인.

- [ ] **Step 2: 전체 테스트 스위트**

Run: `python -m pytest shopping_shorts/tests/test_script_generate.py shopping_shorts/tests/test_app.py -v`
Expected: 신규 실패 0(기존 무관 실패는 그대로 무시).

- [ ] **Step 3: 배포 + 라이브 실생성 검증**

`git push origin main` 후 3분 크론 자동배포 대기(또는 서버 `git pull --ff-only origin main && sudo systemctl restart stockbrain`).
라이브 `stockbrain1.duckdns.org/library`에서 실제 S급 항목 하나로:
- 리메이크 생성 → 초안의 **소재가 원본과 동일**(제품·사실 유지)하고 **표현만 다른지** 육안 확인.
- 원문 문장을 그대로 복붙하지 않았는지(중복 회피) 확인.

- [ ] **Step 4: log.md 기록 + NEXT_SESSION 갱신**

`wiki/log.md`에 1~3줄 기록, `NEXT_SESSION.md`에서 이 트랙 완료 반영. 커밋:
```bash
git add wiki/log.md NEXT_SESSION.md
git commit -m "docs: 대본생성 소재고정 리메이크 완료 기록"
git push origin main
```

---

## Self-Review 체크

- **스펙 커버리지:** UI 2모드 재편(Task4) / 소재 자동감지(Task1,3) / 소재 한 줄 수정가능(Task4 입력칸) / remake 프롬프트 소재고정(Task2) / transplant 유지(Task2) / 하위호환 A·B 매핑(Task2) / 2영상 믹싱=기존 핸드오프(신규작업 없음, 검증 Task5) — 전부 태스크 존재. ✅
- **플레이스홀더:** 없음(모든 코드 블록 완전). ✅
- **타입 일관성:** `mode` 값 `remake`/`transplant` 전 태스크 일치, `subject` 파라미터 시그니처(Task2)·API(Task3)·JS body(Task4) 일치, `detect_subject(full_text)` 시그니처 Task1 정의·Task3 호출 일치. ✅
