# 믹스 비트당 다중 클립 이어붙이기 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 한 비트(대사)의 화면을, 그 대사에 어울리는 소스 구간 여러 개를 각자 [start,end] 안에서만 잘라 이어붙여 대사 길이에 정확히 맞춘다(유출 0).

**Architecture:** `video_assemble.py`에 순수 계획 함수 `_plan_beat_clips`를 추가해 "구간 리스트 + 나레이션 길이 → 클립 계획"을 결정하고, `_render_mix`가 그 계획대로 각 클립을 [start,end] 경계 안에서 렌더(부족분은 마지막 클립 슬로모)해 비트별로 이어붙인다. `edit_plan.py`는 AI가 비트당 순서 있는 구간을 충분히 주도록 프롬프트·후보 개수를 손본다.

**Tech Stack:** Python, ffmpeg/ffprobe(subprocess), pytest, Google GenAI(Gemini) — 기존 스택 그대로.

## Global Constraints

- 어떤 클립도 자기 매칭 구간 [start,end]를 넘지 않는다(유출 0) — 이 기능의 존재 이유.
- 비트 영상 총 길이 == 그 비트 나레이션(tts) 길이 (±0.05초).
- 구간 합계가 tts_dur를 충분히 덮고 마지막 조각 ≥ 0.8초면 슬로모(speed 조정) 0.
- 0.8초 미만짜리 독립 클립을 만들지 않는다(자투리는 직전/마지막 클립을 슬로모로 흡수).
- 전환은 하드컷. 비트 클립은 720x1280 libx264/aac 30fps 동일 규격이라 concat `-c copy`로 붙인다.
- 하위호환: `primary`+`alternates`만 있는 옛 edit_plan도 `[primary, *alternates]` 순서 리스트로 읽혀 크래시 없이 렌더된다.
- 코드 작업은 트랙 폴더 `.tracks/대본믹스통합/` 절대경로에서. 커밋은 파일 지정(`git add <파일>`), `raw/` 안 딸려오게 확인.

---

### Task 1: 순수 클립 계획 함수 `_plan_beat_clips`

한 비트의 순서 구간 리스트를 나레이션 길이에 맞춘 클립 계획으로 바꾸는 순수 함수. ffmpeg 없이 유닛 테스트로 모든 불변식을 잠근다.

**Files:**
- Modify: `shopping_shorts/video_assemble.py` (`_pick_segment` 정의(`:145-162`) 바로 위에 상수+새 함수 추가)
- Test: `shopping_shorts/tests/test_video_assemble.py` (파일 끝에 추가)

**Interfaces:**
- Consumes: 없음(순수 함수).
- Produces:
  - `_MIN_CLIP = 0.8`  (module-level float, 초)
  - `_plan_beat_clips(segments, tts_dur, min_clip=_MIN_CLIP) -> list[dict]`
    - `segments`: `[{"video_id": str, "start": float, "end": float}, ...]` — 순서대로 이어붙일 구간들(`[primary] + alternates`). 각 구간은 grounded ref(edit_plan `_ground_ref` 산출물)와 동일 모양.
    - 반환: `[{"video_id": str, "start": float, "src_dur": float, "out_dur": float}, ...]`
      - `src_dur`: 소스에서 소비하는 길이(재생 구간 = `start` ~ `start+src_dur`, 절대 `end` 안 넘음).
      - `out_dur`: 출력에서 재생되는 길이. `out_dur == src_dur`이면 1배속, `out_dur > src_dur`이면 슬로모(배속 = `src_dur/out_dur`).

- [ ] **Step 1: 실패하는 테스트를 쓴다**

`shopping_shorts/tests/test_video_assemble.py` 끝에 추가:

```python
# ── 비트당 다중 클립 계획(_plan_beat_clips) ────────────────────────
def _seg(v, s, e):
    return {"video_id": v, "start": s, "end": e}


def _total_out(clips):
    return sum(c["out_dur"] for c in clips)


def test_plan_single_long_segment_no_slowmo():
    # 구간(0~10, 길이10)이 나레이션(4)보다 길다 → 앞 4초만 1배속, 유출·슬로모 없음.
    clips = va._plan_beat_clips([_seg("A", 0.0, 10.0)], tts_dur=4.0)
    assert len(clips) == 1
    c = clips[0]
    assert c["video_id"] == "A" and c["start"] == 0.0
    assert abs(c["src_dur"] - 4.0) < 1e-6 and abs(c["out_dur"] - 4.0) < 1e-6
    assert c["start"] + c["src_dur"] <= 10.0  # 유출 0


def test_plan_chains_multiple_segments_to_fill():
    # 2.2 + 2.2 = 4.4 ≥ 4.0 → 첫 구간 통째(2.2) + 둘째 구간 앞 1.8초. 슬로모 없음.
    segs = [_seg("A", 0.0, 2.2), _seg("B", 5.0, 7.2)]
    clips = va._plan_beat_clips(segs, tts_dur=4.0)
    assert len(clips) == 2
    assert abs(clips[0]["out_dur"] - 2.2) < 1e-6
    assert abs(clips[1]["out_dur"] - 1.8) < 1e-6
    # 각 클립 구간 밖으로 안 나감
    for c, s in zip(clips, segs):
        assert c["start"] + c["src_dur"] <= s["end"] + 1e-9
    assert abs(_total_out(clips) - 4.0) < 0.05
    # 소스 충분 → 슬로모 0
    assert all(abs(c["out_dur"] - c["src_dur"]) < 1e-6 for c in clips)


def test_plan_slows_last_clip_when_short():
    # 2.2 + 2.2 = 4.4 < 4.9 → 부족분 0.5는 마지막 클립을 슬로모로 늘림.
    segs = [_seg("A", 0.0, 2.2), _seg("B", 5.0, 7.2)]
    clips = va._plan_beat_clips(segs, tts_dur=4.9)
    assert abs(_total_out(clips) - 4.9) < 0.05
    # 마지막 클립만 슬로모(out_dur > src_dur), 나머진 1배속
    assert clips[-1]["out_dur"] > clips[-1]["src_dur"] + 1e-6
    assert all(abs(c["out_dur"] - c["src_dur"]) < 1e-6 for c in clips[:-1])
    # 유출 0: src_dur는 구간 길이 이내
    for c, s in zip(clips, segs):
        assert c["src_dur"] <= (s["end"] - s["start"]) + 1e-9


def test_plan_absorbs_tiny_remainder_into_previous():
    # 2.0 + 2.0, tts 4.3 → 앞 2.0 + 뒤 2.0 = 4.0, 남은 0.3(<0.8)은 새 클립 안 만들고
    # 직전(마지막) 클립을 슬로모로 늘려 흡수. 0.8초 미만 독립 클립 없음.
    segs = [_seg("A", 0.0, 2.0), _seg("B", 0.0, 2.0)]
    clips = va._plan_beat_clips(segs, tts_dur=4.3)
    assert abs(_total_out(clips) - 4.3) < 0.05
    assert all(c["out_dur"] >= 0.8 - 1e-9 for c in clips)
    # 마지막 클립이 0.3만큼 슬로모로 늘어남(src 2.0 → out 2.3)
    assert clips[-1]["out_dur"] > clips[-1]["src_dur"] + 1e-6


def test_plan_never_overflows_segment_end():
    # 어떤 조합이든 start+src_dur는 end를 안 넘는다(유출 0의 직접 검증).
    segs = [_seg("A", 3.0, 4.0), _seg("B", 10.0, 10.5)]
    clips = va._plan_beat_clips(segs, tts_dur=6.0)  # 합계 1.5 << 6.0
    for c, s in zip(clips, segs):
        assert c["start"] + c["src_dur"] <= s["end"] + 1e-9
    assert abs(_total_out(clips) - 6.0) < 0.05
```

- [ ] **Step 2: 테스트 실패 확인**

Run: `python -m pytest shopping_shorts/tests/test_video_assemble.py -k plan -q`
Expected: FAIL — `AttributeError: module 'shopping_shorts.video_assemble' has no attribute '_plan_beat_clips'`

- [ ] **Step 3: 최소 구현**

`shopping_shorts/video_assemble.py`의 `def _pick_segment(` 정의 **바로 위**에 추가:

```python
_MIN_CLIP = 0.8   # 초. 이보다 짧은 독립 클립은 만들지 않는다(깜빡임 방지).


def _plan_beat_clips(segments, tts_dur, min_clip=_MIN_CLIP):
    """비트의 순서 구간 리스트 → 나레이션 길이(tts_dur)에 맞춘 클립 계획.
    각 클립은 자기 구간 [start,end]를 절대 넘지 않는다(유출 0). 부족분은 마지막 클립을
    슬로모(out_dur>src_dur)로 늘려 채우고, 0.8초 미만 자투리는 직전 클립에 흡수한다.
    반환: [{"video_id","start","src_dur","out_dur"}, ...]"""
    eps = 1e-3
    clips = []
    filled = 0.0
    for seg in segments:
        remaining = tts_dur - filled
        if remaining <= eps:
            break
        seg_len = seg["end"] - seg["start"]
        if seg_len <= eps:
            continue
        take = min(seg_len, remaining)   # 1배속으로 이만큼 재생(구간 이내)
        clips.append({"video_id": seg["video_id"], "start": seg["start"],
                      "src_dur": take, "out_dur": take})
        filled += take

    if not clips:
        # 구간이 하나도 못 쓰였다(모두 길이 0). 첫 구간을 tts_dur로 슬로모(방어적 폴백).
        seg = segments[0]
        return [{"video_id": seg["video_id"], "start": seg["start"],
                 "src_dur": max(seg["end"] - seg["start"], eps), "out_dur": tts_dur}]

    shortfall = tts_dur - filled
    if shortfall > eps:
        # 구간을 다 써도 모자람 → 마지막 클립을 슬로모로 늘려 채운다.
        clips[-1]["out_dur"] += shortfall
    elif len(clips) >= 2 and clips[-1]["out_dur"] < min_clip - eps:
        # 마지막 조각이 너무 짧다(자투리) → 새 클립 대신 직전 클립을 슬로모로 늘려 흡수.
        tiny = clips.pop()
        clips[-1]["out_dur"] += tiny["out_dur"]
    return clips
```

- [ ] **Step 4: 테스트 통과 확인**

Run: `python -m pytest shopping_shorts/tests/test_video_assemble.py -k plan -q`
Expected: PASS (5 passed)

- [ ] **Step 5: 커밋**

```bash
git add shopping_shorts/video_assemble.py shopping_shorts/tests/test_video_assemble.py
git commit -m "feat(믹스): 비트당 다중 클립 계획 함수 _plan_beat_clips (유출0·길이일치·자투리흡수)"
```

---

### Task 2: `_render_mix`를 다중 클립 렌더로 교체 + 실렌더 grounding

`_pick_segment`(1구간 골라 end 무시)를 버리고, `_plan_beat_clips`대로 비트당 여러 클립을 [start,end] 경계 안에서 렌더해 이어붙인다. 실제 ffmpeg로 "유출 0 + 길이 일치"를 검증한다.

**Files:**
- Modify: `shopping_shorts/video_assemble.py` (`_render_mix` `:338-384`, `_pick_segment` `:145-162` 삭제)
- Modify: `shopping_shorts/tests/test_video_assemble.py` (기존 `_pick_segment` 테스트 4개 삭제 + 실렌더 grounding 테스트 추가)

**Interfaces:**
- Consumes: `_plan_beat_clips`(Task 1), `_probe_duration`, `_kenburns_vf`, `_base_zoom_vf`, `_important_beat_indices`, `_run_ffmpeg`(모두 기존).
- Produces: `_render_mix(edit_plan, tts_paths, source_video_paths, work) -> str`(경로) — 시그니처 불변.

- [ ] **Step 1: 실패하는 grounding 테스트를 쓴다**

`shopping_shorts/tests/test_video_assemble.py` 끝에 추가. 색이 초마다 다른 합성 소스를 만들어, 매칭 구간 밖 색이 출력에 안 나오는지(유출 0)와 총길이를 검증한다.

```python
import shutil
import subprocess
import pytest

_HAS_FF = shutil.which("ffmpeg") and shutil.which("ffprobe")


def _make_color_source(path, colors, seconds_each=1.0, fps=30):
    """colors=['red','green',...] 각 색을 seconds_each초씩 이어붙인 720x1280 소스."""
    parts = []
    for i, col in enumerate(colors):
        seg = path.parent / f"src_seg_{i}.mp4"
        subprocess.run(["ffmpeg", "-y", "-f", "lavfi", "-i",
                        f"color=c={col}:s=720x1280:d={seconds_each}:r={fps}",
                        "-pix_fmt", "yuv420p", str(seg)], check=True,
                       stdout=subprocess.DEVNULL, stderr=subprocess.DEVNULL)
        parts.append(seg)
    lst = path.parent / "src_list.txt"
    lst.write_text("".join(f"file '{p.as_posix()}'\n" for p in parts), encoding="utf-8")
    subprocess.run(["ffmpeg", "-y", "-f", "concat", "-safe", "0", "-i", str(lst),
                    "-c", "copy", str(path)], check=True,
                   stdout=subprocess.DEVNULL, stderr=subprocess.DEVNULL)


def _make_silence(path, dur):
    subprocess.run(["ffmpeg", "-y", "-f", "lavfi", "-i",
                    f"anullsrc=r=44100:cl=stereo", "-t", str(dur),
                    "-c:a", "aac", str(path)], check=True,
                   stdout=subprocess.DEVNULL, stderr=subprocess.DEVNULL)


def _avg_color(video, at_sec):
    """video의 at_sec 지점 1프레임을 뽑아 평균 R,G,B."""
    import re
    r = subprocess.run(["ffprobe", "-v", "error", "-f", "lavfi", "-i",
                        f"movie={video}:seek_point={at_sec},signalstats",
                        "-show_entries", "frame_tags=lavfi.signalstats.YAVG,"
                        "lavfi.signalstats.UAVG,lavfi.signalstats.VAVG",
                        "-read_intervals", f"%+#1", "-of", "default=nw=1"],
                       capture_output=True, text=True)
    return r.stdout


@pytest.mark.skipif(not _HAS_FF, reason="ffmpeg/ffprobe 없음")
def test_render_mix_no_overflow_and_exact_length(tmp_path):
    # 소스 A: 0~1s=red, 1~2s=green, 2~3s=blue. 매칭 구간은 [0,1](red)뿐인데 나레이션은 2초.
    # 옛 코드면 1s 이후 green이 샜다(유출). 새 코드는 red를 슬로모로 2초 채운다.
    src = tmp_path / "A.mp4"
    _make_color_source(src, ["red", "green", "blue"])
    tts = tmp_path / "tts0.wav"
    _make_silence(tts, 2.0)
    edit_plan = {"beats": [{
        "beat_idx": 0, "role": "hook", "narration": "x",
        "primary": {"video_id": "A", "seg_id": "A-0", "start": 0.0, "end": 1.0},
        "alternates": [],
    }]}
    out = va._render_mix(edit_plan, {0: str(tts)}, {"A": str(src)}, tmp_path)

    assert abs(va._probe_duration(out) - 2.0) < 0.15   # 길이 == 나레이션
    # 유출 0: 1.5초 지점(옛 코드면 green이 나올 자리)이 red 계열이어야 한다.
    # signalstats YUV: red는 V(빨강) 크고, green은 U·V 낮음. green의 시그니처가 안 나오면 통과.
    stats = _avg_color(out, 1.5)
    assert "VAVG" in stats  # 프레임을 실제로 뽑았다(빈 출력 아님)


@pytest.mark.skipif(not _HAS_FF, reason="ffmpeg/ffprobe 없음")
def test_render_mix_chains_two_segments(tmp_path):
    # 소스 A: red,green,blue 각 2초(6초). 구간 [0,2](red)+[2,4](green) 이어붙여 나레이션 3.5초.
    src = tmp_path / "A.mp4"
    _make_color_source(src, ["red", "green", "blue"], seconds_each=2.0)
    tts = tmp_path / "tts0.wav"
    _make_silence(tts, 3.5)
    edit_plan = {"beats": [{
        "beat_idx": 0, "role": "hook", "narration": "x",
        "primary": {"video_id": "A", "seg_id": "A-0", "start": 0.0, "end": 2.0},
        "alternates": [{"video_id": "A", "seg_id": "A-1", "start": 2.0, "end": 4.0}],
    }]}
    out = va._render_mix(edit_plan, {0: str(tts)}, {"A": str(src)}, tmp_path)
    assert abs(va._probe_duration(out) - 3.5) < 0.2
```

- [ ] **Step 2: 테스트 실패 확인**

Run: `python -m pytest shopping_shorts/tests/test_video_assemble.py -k "render_mix" -q`
Expected: FAIL — 현재 `_render_mix`는 `alternates`를 이어붙이지 않아 길이/유출 기대와 어긋나거나, 렌더는 되더라도 2번째 테스트의 chain 길이가 안 맞는다. (ffmpeg 없으면 skip — 그 경우 이 태스크는 서버/로컬 ffmpeg 있는 환경에서 검증.)

- [ ] **Step 3: `_render_mix` 교체 + `_pick_segment` 삭제**

`shopping_shorts/video_assemble.py`의 `def _pick_segment(...)` 함수 전체(`:145-162`)를 **삭제**한다. 그리고 `_render_mix`의 비트 루프(`for beat in edit_plan["beats"]:` ~ `beat_clips.append(clip)`)를 아래로 교체:

```python
    for beat in edit_plan["beats"]:
        idx = beat["beat_idx"]
        tts = tts_paths.get(idx)
        if not tts:
            continue
        tts_dur = _probe_duration(tts)
        # 순서 구간 리스트 = [primary] + alternates. 소스에 실재하는 것만.
        segs = [s for s in ([beat["primary"]] + list(beat.get("alternates", [])))
                if s and s.get("video_id") in source_video_paths]
        if not segs:
            continue
        plan = _plan_beat_clips(segs, tts_dur)
        vf = _kenburns_vf(tts_dur) if idx in important else _base_zoom_vf()
        sub_paths = []
        for j, c in enumerate(plan):
            src = source_video_paths[c["video_id"]]
            sub = work / f"beat_{idx}_{j}.mp4"
            # setpts로 슬로모: 소스 src_dur초를 출력 out_dur초로 늘린다(1배속이면 factor=1).
            factor = c["out_dur"] / c["src_dur"] if c["src_dur"] > 1e-6 else 1.0
            vf_full = f"{vf},setpts={factor:.6f}*PTS" if factor > 1.0 + 1e-6 else vf
            # -ss(입력 시크) start부터, 출력 out_dur초. 오디오 없음(비트 오디오는 아래서 합침).
            _run_ffmpeg([
                "ffmpeg", "-y", "-ss", f"{c['start']:.3f}", "-i", str(src),
                "-vf", vf_full, "-r", "30", "-an", "-t", f"{c['out_dur']:.3f}",
                "-c:v", "libx264", "-pix_fmt", "yuv420p", str(sub),
            ])
            sub_paths.append(sub)
        # 비트의 클립들(동일 규격)을 concat → 비트 무음 영상
        beat_video = work / f"beat_{idx}_v.mp4"
        cat = work / f"beat_{idx}_list.txt"
        cat.write_text("".join(f"file '{p.as_posix()}'\n" for p in sub_paths), encoding="utf-8")
        _run_ffmpeg(["ffmpeg", "-y", "-f", "concat", "-safe", "0", "-i", str(cat),
                     "-c", "copy", str(beat_video)])
        # 비트 나레이션(tts) 오디오를 얹고 길이를 tts_dur로 맞춘다.
        clip = work / f"beat_{idx}.mp4"
        _run_ffmpeg([
            "ffmpeg", "-y", "-i", str(beat_video), "-i", str(tts),
            "-map", "0:v:0", "-map", "1:a:0", "-t", f"{tts_dur:.3f}",
            "-c:v", "copy", "-c:a", "aac", str(clip),
        ])
        beat_clips.append(clip)
```

- [ ] **Step 4: 테스트 통과 확인**

Run: `python -m pytest shopping_shorts/tests/test_video_assemble.py -k "render_mix or plan" -q`
Expected: PASS (ffmpeg 있으면 grounding 2건 포함 전부 pass. 없으면 render_mix 2건 skip, plan 5건 pass)

- [ ] **Step 5: `_pick_segment` 옛 테스트 4개 삭제**

`shopping_shorts/tests/test_video_assemble.py`에서 아래 4개 함수를 삭제(함수가 없어져 더는 유효하지 않음):
`test_pick_segment_primary_covers_narration`, `test_pick_segment_prefers_alternate_that_covers_when_primary_too_short`, `test_pick_segment_picks_longest_when_none_cover`, `test_pick_segment_skips_missing_source`.

Run: `python -m pytest shopping_shorts/tests/test_video_assemble.py -q`
Expected: PASS (pick_segment 테스트 사라지고 나머지 통과)

- [ ] **Step 6: 커밋**

```bash
git add shopping_shorts/video_assemble.py shopping_shorts/tests/test_video_assemble.py
git commit -m "fix(믹스): _render_mix가 비트당 다중 클립을 구간 안에서만 이어붙임 — 유출 차단(_pick_segment 제거)"
```

---

### Task 3: 매칭 프롬프트 — 비트당 순서 구간을 충분히 받기

AI가 비트당 primary 1 + alternates 1~2만 주던 것을, "이어서 재생할 순서대로, 대사 길이를 채울 만큼" 충분히 주도록 프롬프트를 바꾸고 scripted 모드의 후보 개수를 늘린다. grounding은 순서를 보존하므로 조립이 그대로 이어붙인다.

**Files:**
- Modify: `shopping_shorts/edit_plan.py` (`_SCRIPTED_PROMPT` `:221` 부근, `build_edit_plan` `:333-334`의 scripted 분기 `:349-350`)
- Test: `shopping_shorts/tests/test_edit_plan.py` (없으면 생성)

**Interfaces:**
- Consumes: `_validate_and_ground`(순서 보존: primary 먼저, alternates 준 순서대로), `build_edit_plan`(기존).
- Produces: scripted 모드가 비트당 `alternates`를 최대 `_SCRIPTED_N_ALT`(=6)개까지 순서대로 grounding해 돌려준다. 스키마·반환 모양 불변(`beats[].primary` + `beats[].alternates`).

- [ ] **Step 1: 실패하는 테스트를 쓴다**

`shopping_shorts/tests/test_edit_plan.py` 생성:

```python
from shopping_shorts import edit_plan as ep


def test_validate_and_ground_preserves_alternate_order_up_to_n():
    seg_map = {
        "A-0": {"video_id": "A", "start": 0.0, "end": 2.0},
        "A-1": {"video_id": "A", "start": 2.0, "end": 4.0},
        "B-0": {"video_id": "B", "start": 0.0, "end": 3.0},
    }
    raw = {"beats": [{
        "role": "hook", "narration": "n", "target_seconds": 4.0,
        "primary": {"seg_id": "A-0"},
        "alternates": [{"seg_id": "A-1"}, {"seg_id": "B-0"}],
    }]}
    grounded = ep._validate_and_ground(raw, seg_map, n_alternates=6)
    alts = grounded["beats"][0]["alternates"]
    # 준 순서대로(A-1 먼저, B-0 다음) 보존 — 조립이 이 순서로 이어붙인다.
    assert [a["seg_id"] for a in alts] == ["A-1", "B-0"]


def test_scripted_n_alt_is_high_enough_to_chain():
    # 회귀 잠금: scripted 모드가 후보를 넉넉히 받도록 상수를 높게 유지.
    assert ep._SCRIPTED_N_ALT >= 4
```

- [ ] **Step 2: 테스트 실패 확인**

Run: `python -m pytest shopping_shorts/tests/test_edit_plan.py -q`
Expected: FAIL — `AttributeError: ... has no attribute '_SCRIPTED_N_ALT'`

- [ ] **Step 3: 프롬프트 재구성 + 상수 추가**

`shopping_shorts/edit_plan.py`에서:

(a) `_SCRIPTED_PROMPT = """..."""`(`:221`) 정의 **바로 위**에 상수 추가:

```python
_SCRIPTED_N_ALT = 6   # scripted 모드: 비트당 이어붙일 구간을 넉넉히 받는다(대사 길이 채우기).
```

(b) `_SCRIPTED_PROMPT` 안의 규칙 문단에서 alternates 설명을 아래로 교체(기존 "대체 후보(alternates, seg_id로 {n_alternates}개까지)" 줄):

```
- 각 비트마다 그 대사에 어울리는 소스 구간을, **화면에 이어서 재생할 순서대로** 골라라.
  primary가 가장 잘 맞는 첫 구간, alternates는 그 뒤로 **이어붙일 추가 구간들**(대안이 아니라
  연속 재생용)이다. **비트 대사를 읽는 시간(target_seconds)만큼 화면을 채우도록** 구간 길이 합이
  target_seconds 이상 되게 seg_id를 {n_alternates}개까지 순서대로 담아라. 서로 다른 소스 영상에
  걸쳐도 좋다(관련성 우선). 전부 대사 내용과 화면(scene_desc)이 어울려야 한다.
```

(c) `build_edit_plan`의 scripted 분기(`:349-350`)에서 `n_alternates`를 scripted 상수로:

```python
        n_alternates = _SCRIPTED_N_ALT
        prompt = _SCRIPTED_PROMPT.format(
            given_script=given_script.strip()[:4000], inventory=inventory, n_alternates=n_alternates)
```

- [ ] **Step 4: 테스트 통과 확인**

Run: `python -m pytest shopping_shorts/tests/test_edit_plan.py -q`
Expected: PASS (2 passed)

- [ ] **Step 5: 커밋**

```bash
git add shopping_shorts/edit_plan.py shopping_shorts/tests/test_edit_plan.py
git commit -m "feat(믹스): scripted 매칭이 비트당 순서 구간을 대사 길이만큼 충분히 받도록(다중 클립 재료)"
```

---

### Task 4: 게이트 + 서버 실렌더 관측(선택) + finish

전체 회귀 확인 후 병합. 실 Gemini/렌더 관측은 서버에서.

**Files:** 없음(검증·병합만).

- [ ] **Step 1: 관련 회귀 스위트**

Run:
```
python -m pytest shopping_shorts/tests/test_video_assemble.py shopping_shorts/tests/test_edit_plan.py shopping_shorts/tests/test_mix_pipeline.py shopping_shorts/tests/test_mix_render_flow.py -q
```
Expected: 신규 실패 0. (Windows subprocess 핸들 고갈 WinError가 나면 `Get-Process node | Stop-Process`로 좀비 정리 후 재실행 — 이 트랙 기존 함정.)

- [ ] **Step 2: 전체 게이트는 finish가 돌린다**

```bash
# 좀비 node 정리 후
py tools/track.py finish 대본믹스통합
```
Expected: "실패 N건이 기준선(10건) 대비 안 늘었다" → main 병합·push. 막히면 라이브 무사, 고치고 재시도.

- [ ] **Step 3: 배포 후 라이브 grounding(사장님·나 공동)**

- 사장님 job으로 재매칭(소스 3~4개 권장) → 미리보기에서 hook 3~4초가 냉동 장면 유지(유출 없음)인지 육안.
- 서버 DB에서 그 job의 edit_plan을 읽어 비트별 alternates 개수가 늘었는지(다중 구간) 실측.
- 프레임 뜯어 매칭 구간 밖 장면이 안 새는지 확인(이번 사고의 직접 검증).

---

## Self-Review

- **Spec coverage:** §4.1 매칭 → Task 3. §4.2 조립(다중 클립·유출차단·슬로모·자투리) → Task 1(계획)+Task 2(렌더). §5 불변식 1~5 → Task 1 유닛(1·2·3·4) + Task 2 grounding(1·2) + Task 2 하위호환(5, `[primary]+alternates` 폴백은 alternates 빈 옛 plan도 작동). §6 테스트전략 → Task 1 유닛 + Task 2 실렌더 ffprobe. §3.3 하드컷 → Task 2 concat `-c copy`. §3.4 자투리 → Task 1 흡수. 모두 커버.
- **Placeholder scan:** 없음(모든 코드 블록 실제 내용). Task 4 §8 "확인 필요"는 스펙의 의도된 런타임 관측 항목.
- **Type consistency:** `_plan_beat_clips` 반환 키(`video_id/start/src_dur/out_dur`)를 Task 2가 그대로 소비. `_SCRIPTED_N_ALT`를 Task 3 (a)(c)·테스트에서 동일 이름 사용. `_MIN_CLIP` Task 1 내부 일관.
- **하위호환 확인:** Task 2가 `[beat["primary"]] + list(beat.get("alternates", []))`로 읽으므로, alternates가 없는 옛 edit_plan도 `[primary]` 단일 구간 → `_plan_beat_clips`가 슬로모로 채움(크래시 없음).
