# 미리보기재생

## 2026-09-02 — 전체재생 '훅 뒤 꼬다리' = 도착 판정이 성급했다

### 제보
사장님: "훅에서는 뒤에 꼬다리가 안 나오는데, 전체영상 재생하면 훅 마지막에 꼬다리가 나온다."
정정 제보: "3번 장면 얘기가 아니다. 오렌지박스보다 **1초 정도 앞** 화면이 나온다."

(첫 조사에서 나는 planClips의 fill(부족분 채우기)을 원인으로 지목했다 — **틀렸다**.
사장님이 위치를 정확히 짚어주셔서 갈렸다. 배지 `3.9/4.9초 1.0초 모자람`에 끌려
"모자라서 뒤에 붙인 것"으로 단정한 것이 오진의 원인이다.)

### 진짜 원인 (`scene_play.js` step())
```js
if (Math.abs(v.currentTime - c.start) < 0.05 && v.readyState >= 2) show();
```
HTML 명세상 `v.currentTime = X`는 **요청 즉시** X를 돌려주고 `seeking`만 true가 된다.
실제 프레임은 나중에 온다. 즉 이 조건은 "도착했다"가 아니라 **"도착하라고 시켰다"**를 본다.

**전체 재생에서만 난 이유** — `runAllFrom`이 다음 칸 첫 컷을 `seat()`으로 미리 앉힌다.
`seat()`은 currentTime만 꽂고 **기다리지 않는다**(주석엔 "시크 완료까지 기다린다"고
적혀 있었지만 코드는 안 기다렸다 — 이 거짓 주석이 오해를 키웠다).
앞 칸과 **같은 소스**를 쓰면 readyState는 이미 높고 seeking만 true라 위 조건이 통과해
**가림막(썸네일) 없이 바로 화면에 올린다** → 아직 도착 못 한 앞부분이 1초쯤 노출.
칸별 재생(`playBeat`)은 미리 앉히기가 없어 늘 else(썸네일로 가리고 대기)로 갔다 → 멀쩡.

### 고친 것
- 판정에 `&& !v.seeking` 추가 — 도착을 실제로 본다
- else에서 **진행 중인 시크에 currentTime을 재대입하지 않는다**. 같은 값을 다시 꽂으면
  브라우저가 seeked를 안 쏘아 진행 중이던 완료 신호까지 놓치고 타이머로만 깨어난다
  (컷 시작이 cutWaitMs만큼 늦어진다 — 미리 앉힌 컷이 정확히 이 경우다)
- `seat()` 주석을 사실에 맞춤: 기다리지 않는다고 명시
- **대기 타이머로 깨어나도 도착 전이면 가림막(썸네일)을 걷지 않는다** — 첫 수정은
  이 경로를 안 막아 반쪽이었다(진짜 꼬다리 경로가 여기다). 걷는 일은 seeked/안전핀이 한다.
- 회귀 테스트 `test_preview_seek_arrival.py` 3건

### 실제 고객 데이터로 확정 (job 8b5aed8af66b, 2026-09-02 12:16)
```
칸0 훅   : s0 11.75~12.63 (0.88) → s4 16.30~18.39 (2.09) → s4 10.06~11.43 (1.37)   TTS 5.184초
칸1 문제 : s4 0.75~2.65 (1.90)  → s3 25.23~28.15 (2.92)                            ← 같은 s4!
```
- 훅 **마지막 컷이 s4@10.06** — 사장님이 짚은 오렌지박스(10초)와 일치하고,
  "그 1초 앞(9초대)이 보인다" = **10.06에 도착하기 전 화면이 노출**된 것.
- 전체 재생만 나는 이유도 데이터로 설명된다: 훅이 도는 동안 `runAllFrom`이
  **다음 칸 첫 컷(같은 s4, 0.75)을 미리 앉힌다**. 같은 파일에 시크가 겹쳐
  훅 마지막 컷의 시크가 느려지고, 대기 타이머(cutWaitMs=1.37×0.6≈822ms)가
  **도착 전에** 화면을 켰다. 칸별 재생은 미리 앉히기가 없어 경쟁이 없다.

### ⚠️ 검증 한계 (정직하게)
- 근거는 **코드 독해 + HTML 명세**다. 실제 `<video>`로 "seeking=true인데 옛 조건이
  참이 된다"를 **실증하지 못했다** — 자동화 탭에서 video가 readyState 0에서 안 올라갔다
  (python http.server가 Range/206을 안 줘 blob으로 우회해도 동일).
- 즉 **사장님이 실제로 전체재생을 눌러 꼬다리가 사라졌는지 확인해야 확정**이다.
- 기존 실패 1건(`test_merge_split_playback.py` ANSI 색코드)은 손대지 않은 main에서도
  동일하게 실패한다 — 내 변경과 무관한 기준선.

### ⏭ 다음
- [ ] 배포 후 사장님이 전체재생 → 훅 뒤 꼬다리가 없어졌는지 확인
- [ ] 여전하면: 같은 소스를 쓰는 칸 넘김을 실제로 계측(seeking·readyState를 콘솔에 찍어 재현)

---

## 2026-09-02 — 고객 제보(왕혜원): 전체재생이 "훅만 영상, 나머지는 이미지 나열"

### 제보 원문(요약 아님, 고객 표현 그대로)
> 영상대본 단계에서 제가 아무것도 안 하고, 프로그램이 알아서 세팅한 영상 '전체재생'
> 해보면, 훅부분만 동영상이고, 이후 화면들은 그냥 이미지들 나열하고, 엉망입니다...
> 첫 작업인데, 다운받아보면 동영상으로 나올런지.... 그렇다 하더라도, 작업중 단계에서
> 이런식이면, 영상변경 작업도 안 되고,,, 왜 이럴까요?
> 라이브방송 다시보기 해봤는데, 이렇지 않던데요

### 아직 **모르는 것** (추측 금지 — 아래를 받아야 갈린다)
- [ ] job id (왕혜원 계정 / 제작 시각)
- [ ] 제보 시각이 커밋 `23c127315`(09-02 21:30, 가림막 판정 변경) **배포 전인지 후인지**
- [ ] "이미지"가 ① 썸네일 가림막이 안 걷힌 것인지 ② 애초에 정지컷 소재인지
      → 화면 우클릭/개발자도구 없이 가르는 법: 소리·컷번호(pinfo)는 흐르는데 그림만
        멈춰 있으면 ①, 컷마다 그림이 바뀌는데 안 움직이면 ②일 수 있다

### 후보 원인 (검증 전 — 어느 것도 확정 아님)
1. **가림막(썸네일) 고착** — 09-02 수정이 `show()`의 즉시-걷기 조건에 `!v.seeking`을
   추가했다(`scene_play.js` step()). else 경로에 안전핀 타이머(120~600ms)가 있어
   *코드상으로는* 반드시 걷히지만, 실제 브라우저에서 걷히는지는 **실증 안 됨**
   (직전 세션도 자동화 탭에서 video readyState가 안 올라가 실증 실패로 기록돼 있다).
   ★"훅 뒤 꼬다리" 수정의 부작용일 가능성이 가장 먼저 확인할 대상.
2. **소재 다운로드가 훅 칸만 됨** — 나머지 칸이 소재 없이 썸네일로 배치됐을 수 있다.
3. **소재 자체가 정지 이미지/시크 느린 코덱(AV1 등)** — 09-01 사고와 같은 계열.

### ⏭ 다음
- [ ] 왕혜원 job id 확보 → 서버에서 그 job의 칸별 배치(소스·start/dur)와 소재 파일 유무 실측
- [ ] 사장님 계정에서 같은 화면 전체재생 재현 시도(콘솔에 seeking·readyState 기록)
- [ ] 1번이면 롤백 후보: `23c127315` (되돌리면 '훅 뒤 꼬다리'가 재발한다 — 맞바꿈 주의)
