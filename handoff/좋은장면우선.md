# 좋은장면우선 — 핸드오프

_최종: 2026-09-06 새벽 · 상태: **main 병합 대기** · 선행 트랙: `B1프레임태깅`(1·2·3단계, 라이브 켜짐)_

## 무엇을 · 왜

사장님(2026-09-06, 라이브 결과를 보시고):
> "정말 다양하고 좋은 장면이 다양하게 있는데 **국내 영상 위주로만** 되어 있다. 대본 씨앗을 그걸로
> 선택해서 그런가? 가장 좋은 건 **외국 영상으로 그 퀄리티를 찾는 것**인데."
> → "**씨앗은 그대로, 모든 장면매칭은 좋은장면 우선**"

### 실측 — 씨앗 영상이 장면까지 독점하고 있었다
라이브 작업 `dbddcce899bb`(레트로 카메라, 소스 **7편·97구간**, 03:03 생성):

| 소스 | 구간 | 전사(말) | 번역된 구간 | 대본이 쓴 칸 |
|---|---|---|---|---|
| `J-t6rhxilm4` (국내 유튜브, **씨앗**) | 10 | 249자 | – | **4칸** |
| `lens_instagram_11v1rgw` | 13 | 1자(🎵) | 0 | 1칸 |
| **`lens_instagram_ly1wtm`** (외국) | **20** | **1,207자** | **17** | **0칸** |
| `lens_instagram_1snuqcc` (외국) | 16 | 506자 | 16 | **0칸** |
| `lens_instagram_1ksi1cx` (외국) | 14 | 256자 | 9 | **0칸** |
| `lens_tiktok_hahhv5` | 16 | 0자 | 0 | 0칸 |
| `lens_youtube_30b8q6` | 8 | 24자 | 4 | 0칸 |

말·구간이 가장 풍부하고 **번역까지 끝난** 외국 소스 3편(50구간·1,969자)이 한 칸도 안 쓰였다.

### 원인 — 주제 고정 문구가 **그림 선택까지** 막고 있었다
`script_generate._mix_source_block` 끝(소스 2편 이상일 때):
```
★★주제는 반드시 [대본 1]의 제품·소재다. [대본 2] 이하는 **말투·전개·표현을 참고만** 하고,
   거기 나오는 제품·기능·사례를 주제로 삼거나 섞지 마라.
```
이 문구는 **2026-08-17 사고**(치아바타를 뽑았는데 도마 얘기)를 막으려고 넣은 것이고 그 목적은 정당하다.
그런데 "**참고만**"이 *주제*와 *그림*을 뭉뚱그려, 같은 제품을 여러 각도로 찍은 소스의 **장면까지** 막았다.

## 고친 것 — 주제는 고정, **장면은 예외**

`shopping_shorts/script_generate.py` `_mix_source_block` (소스 2편 이상일 때만 붙는다):
```
★★단 **장면(그림)은 예외**다 — 같은 제품이라면 [대본 2] 이하의 장면도 얼마든지 써라.
src_seg는 **그 줄의 내용을 가장 잘 보여주는 장면**을 소스 구분 없이 골라라
(다른 제품의 장면을 쓰라는 뜻이 아니다 — 같은 제품을 다른 각도에서 찍은 것을 말한다).
씨앗 영상에 더 나은 그림이 없으면 다른 영상에서 가져오는 게 맞다.
```

## 검증 (0순위-A1 — 실제로 돌렸다)

- 테스트 3건 신설(주제 고정 유지 / 장면 예외 문구 존재 / 소스 1편이면 둘 다 안 붙음).
  **사보타주로 확인**: 예외 문구를 지우면 빨개진다 ✅
- 관련 스위트 **120건 green**(script_grounded·script_generate·inherit_plan·edit_plan·b1_frame_fixes)
- **실제 대본 재생성 2조합**(모델 호출):

| 조합 | 제품 | 결과 |
|---|---|---|
| 손목보호대 2편(14+44구간) | **같은 제품** | **s0 3 + s1 2** — 고루 씀(고치기 전 s0만 5개), 게이트 통과, 중복 0 |
| 비비크림+히노키(5+44) | **다른 제품** | s0만 4개 — **안 섞음이 정상**(지시문 "같은 제품이라면") |

## ⏭ 남은 것

1. **라이브 재실측** — 같은 카메라 소재(소스 7편)로 다시 뽑아 외국 소스가 실제로 쓰이는지.
   비교 기준: 고치기 전 `씨앗 4칸 / 외국 3편 0칸`
2. 3단계 자동 b-roll(대본이 안 정한 칸)도 소스 구분 없이 고르는지 확인 — `edit_plan._next_cut`은
   "앞 비트와 **같은 소스**의 다음 컷"을 1순위로 본다. 이게 씨앗 쏠림을 다시 만들 수 있다
3. 화면 컷 31개 중 대본이 정한 건 6개뿐 — 나머지 25개(자동)의 소스 분포도 재 볼 것

## ⚠️ 함정 (이 트랙에서 밟은 것)

- **heredoc·py - <<EOF 로 파일에 `\n`을 쓰면 실제 개행으로 풀려 문자열이 두 동강 난다**(오늘 2번).
  `chr(92)+"n"`으로 우회하거나 Write 툴을 써라. 메모리 `heredoc_개행이스케이프_붕괴`
- 트랙 폴더 `B1프레임태깅`이 세션 도중 사라졌다(다른 세션이 정리). **커밋은 전부 main에 있어 유실 없음**
  — `git merge-base --is-ancestor`로 5건 모두 확인했다. 트랙 폴더는 언제든 다시 만들면 된다.

---

## 2차 (2026-09-06 낮) — 🔴 **라이브 장애 2건**(둘 다 내 배포 실수) + 채우기 진단

### 🔴 장애 ① AI 키 소진 — 1단계를 **전체 고객**에게 켠 것

| 시각 | 429 오류 |
|---|---|
| 12:58 | 1건 |
| **13:04** | 21건 |
| 13:06~13:12 | **200~370건/분** |
| 3시간 합계 | **2,054건** |

새벽에 내가 `frame_extract_enabled=1`(전체)로 켰다. 서버 30편 정확도(0.894→0.971)만 보고
**호출량·비용을 재보지 않은 것**이 판단 착오다.

★**왜 소진되나**(사장님 질문): 방식이 근본적으로 다르다.

| | 옛 방식(classic) | 새 방식(B1) |
|---|---|---|
| AI 호출 | 영상 1편당 **1번**(통째 업로드) | **구간마다** |

실측: 구간 **114개**짜리 긴 영상 하나가 들어오자 수십 번씩 호출 → 키 전부 소진 →
다른 고객 분석도 줄줄이 실패("묘사 빈 구간 84% → 옛 추출로 폴백").

★**1단계엔 `admin` 옵션이 아예 없다** — `script_extract._frame_flag_on()`이 `== "1"`만 본다.
2·3단계(`_setting_gate`)와 달리 "사장님만"이 불가능해 켜려면 전체밖에 방법이 없었다.

**조치**: `frame_extract_enabled=0`으로 되돌림. 2·3단계는 `admin` 그대로(고객 영향 0).
**⏭ 남은 것**: 1단계에도 `_setting_gate`를 쓰게 해 `admin` 지원 추가.

### 🔴 장애 ② 컷 목록이 통째로 안 뜸 — `const` 재대입 (약 40분)

사장님·고객 전원이 3단계에서 빈 화면. **미리보기 영상은 재생돼서** "데이터는 있는데 목록만 빈" 모양.

**진단 경로**(서버부터 갈라서 봤다 — 전부 정상이었다):
구간 97개 · 편성 9칸 · 자막 9개 · 소스 7개 · 썸네일 생성 43KB · `beatframe` 정상 →
**서버 무죄** 확정 → Playwright로 브라우저 콘솔을 직접 봤다:

```
TypeError: Assignment to constant variable.
  at planClips (scene_play.js:430) → renderBand → render → boot
```

**원인**: `91ffcff03`에서 `const bounds = [0];`를 선언하고 아래에서
`bounds = pickSplitBounds(...)`로 재대입. planClips가 죽으니 그걸 부르는 render 사슬이
통째로 멈춰 컷이 하나도 안 그려졌다.

**고침**: `const` → `let` (한 글자). 커밋 `0597a236c` → main `437e17250`.

★**배경은 내 배포 실수다**: 서버는 **새벽 2~6시에만** 자동 배포한다
(`auto_deploy.log`: "배포 대기(시간창 2~6시 밖)"). 내가 낮 **12:51에 수동 pull+재시작**해서
다른 세션의 컷 작업이 **절반만**(`91ffcff03`) 들어간 상태로 라이브가 됐다.
후속 수정 4건(`74a37b6c4` 등)은 아직 서버에 없었다.

### 재발 방지 — `tests/test_scene_play_runs.py` 신설
`scene_play.js`를 node로 **실제 실행**해 planClips를 구절맞춤 경로로 호출한다.

왜 기존 검사가 못 잡았나:
- `node --check`는 **문법만** 본다 — const 재대입은 문법상 합법, **실행할 때** 터진다
- `test_cut_count_wired`는 "본문에 pickSplitBounds라는 **글자**가 있나"만 본다

★사보타주 확인: const로 되돌리면 `RUN_FAIL:Assignment to constant variable`로 빨개진다.
⚠️**이 테스트를 쓰면서 두 번 가짜였다**(사보타주가 통과하는 것으로만 드러났다):
1. `planClips`에 **객체 배열**을 넘겼다 — 실제로는 **seg_id 문자열 배열**. 조용히 `[]`가 되어 버그 지점 미도달
2. `phraseSyncOn`을 `globalThis`로 덮어써도 **파일 안 함수가 이긴다** → 덮지 말고 `PHRASE_SYNC`를 비워 두면 기본이 '켬'

### ✅ 1차 수정(씨앗 고정·장면 예외) 라이브 실측 — **효과 확인**
work `dbddcce899bb`(레트로 카메라, 소스 7편·97구간) 재생성:

| | 고치기 전 | **고친 후** |
|---|---|---|
| 씨앗(국내 `J-t6rhxilm4`) | 4칸 | 3칸 |
| **외국 영상** | **0칸** | **3칸**(`1snuqcc` 2 + `ly1wtm` 1) |
| 사용 영상 수 | 2개 | **3개** |
| 3단계 | inherit | inherit (6칸 중 5칸 상속) |

**대본↔장면 눈검사 6/6 정확**:
- "**크랭크 돌리며** 써봤거든요" → **측면 크랭크를 돌려 작동**
- "**데스크테리어**로도 딱" → **데스크테리어 활용 설명**
- "영화 같은 사진이 나오거든요" → 찍은 결과물(풍경)이 지나감

### 채우기(✨) 진단 — 사장님 "잘 골라내지 못해 불만이 많다"
★**채우기는 오늘 고친 것과 다른 경로다** — 달라지는 게 없다.

| | 1차에서 고친 곳 | 채우기 |
|---|---|---|
| 코드 | `script_generate._mix_source_block` (2단계 대본 생성) | **`edit_plan.fill_beat_scenes`** (`/api/mix/scene_lab/{job}/fill`) |
| 언제 | 대본 처음 뽑을 때 | **버튼 누를 때마다** |

**실측(같은 job, 3칸에 실제 호출) — 6장 중 5장 정확(83%)**:

| 대사 | 고른 화면 | |
|---|---|---|
| "몰래 쓰던 건데요" | 뷰파인더 덮개 올려 촬영 준비 / 카메라 조작 | ⭕ |
| "입소문이 쫙 퍼지면서" | 촬영된 고양이 사진 / 감성 풍경 연속 | ⭕ |
| **"크랭크 돌리며 써봤거든요"** | **측면 크랭크를 돌려 작동**(fit=5) | ⭕⭕⭕ |

세 칸 모두 **여러 영상에서** 골랐다(`1snuqcc`·`J-t6rhxilm4`·`ly1wtm`).

→ **이 소재에서는 잘 고른다.** 불만은 다른 조건에서 나올 가능성이 크다:
1. **키가 소진됐을 때** — AI가 못 돌아 규칙 경로로 폴백(`aiPicks`가 null → "결만 맞고 내용은 딴소리")
2. **장면 묘사가 부실한 소재** — 카메라는 묘사가 풍부한 편이다
3. **`taken`이 많을 때** — 이미 담긴 게 많으면 남는 후보가 나쁜 것뿐

### ⏭ 남은 것 (우선순위)
1. **채우기 불만의 진짜 조건 찾기** — 불만이 나온 **실제 job**으로 돌려봐야 한다.
   실측 없이 고치면 엉뚱한 데를 만진다(이번에도 "다른 경로"라는 것부터 확인해야 했다)
2. **1단계에 `admin` 옵션 추가** — `_frame_flag_on`을 `_setting_gate`로 갈아 사장님만 켤 수 있게.
   그래야 키 소진 없이 새 분석을 실사용해볼 수 있다
3. 매칭 개선 계속 — 지금 6/6이지만 **한 편으로 판단하긴 이르다**. 몇 편 더 재 볼 것
4. ⚠️**집 PC 디스크**: 233GB 중 여유 12GB(pytest 임시폴더 16GB 정리해 겨우 확보).
   `C:\Users` 145GB · 바탕화면 70GB

### ⚠️ 다음 세션이 반드시 지킬 것
- **낮에 서버로 수동 배포하지 마라.** 새벽 2~6시 시간창이 있는 이유가 이번 장애다.
  급하면 커밋 제목에 `[긴급]` 또는 `touch /home/ubuntu/DEPLOY_NOW`
- **서버 IP는 `3.35.251.172`** (옛 `43.200.48.69`는 죽은 주소 — 타임아웃이라 "SSH 막힘"으로 오판한다).
  키: `C:/Users/CH/crawling_bot_client/LightsailDefaultKey-ap-northeast-2.pem`
- **`py - <<EOF`로 파일에 `\n`을 쓰면 실제 개행으로 풀려 문자열이 두 동강 난다**(이 세션에서 3번 밟음).
  `chr(92)+"n"`으로 우회하거나 Write 툴을 써라
