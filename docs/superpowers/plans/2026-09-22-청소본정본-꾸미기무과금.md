# 청소본 정본(clean base) — 자막제거 job당 1회 구현 계획

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 4단계에서 한 번 지운 완성본을 job의 정본으로 삼아, 5단계 꾸미기 뒤 최종렌더·미리보기·프레임·캡컷·ZIP이 전부 그 청소본에서 나오게 하고(VMake 재호출 0), 새 영상으로 교체된 장면만 증분 청소한다.

**Architecture:** 청소 시점의 컷 지도(`final_clip_pairs` 결과)를 `work/<job>/clean_base.json`에 저장한다. 렌더 계열은 편성표를 **파생 사본**으로 바꿔(`remap_plan`) 각 비트의 재료를 청소본 좌표 `("clean", t0, t1)`로 치환하고 `source_video_paths={"clean": 청소본}`로 기존 `assemble`을 `clean_fn=None`으로 돌린다. 재료가 바뀐 비트만 원본에서 그 구간을 잘라 1콜로 지워 `extras`에 추가한다. 관리자 스위치 `clean_base_enabled`(기본 끔) 뒤에 둔다.

**Tech Stack:** Python 3.12 · FastAPI · ffmpeg/ffprobe · pytest · 기존 `mix_pipeline`/`video_assemble`/`capcut_draft`

**Spec:** `docs/superpowers/specs/2026-09-22-청소본정본-꾸미기무과금-design.md`

## Global Constraints

- 작업 폴더는 트랙 `.tracks/소스구간청소`. main 폴더 코드는 건드리지 않는다.
- 판정은 한 곳(0순위-B): "청소본이 무엇인가"=`clean_base.load_base`, "재료가 청소본 어디인가"=`clean_base.remap_plan`. 새 계산을 다른 파일에 적지 않는다.
- 파생 편성표(`plan2`)는 **절대 DB에 저장하지 않는다**(`store.update_mix_job(edit_plan=plan2)` 금지). 원본 `edit_plan`은 그대로.
- 스위치 기본값 끔. 켜기 전 고객 화면은 한 글자도 바뀌지 않는다.
- 상수: `STRETCH_MAX = 0.10`(느리게 한도) · `EXTEND_MIN = 0.30`(이 이상 모자라면 증분) · `CLEAN_VID = "clean"`.
- 실제 VMake 호출은 테스트·LAB에서 **절대** 하지 않는다(`_vmake_clean`을 monkeypatch).
- 테스트 실행: 트랙 폴더에서 `py -m pytest shopping_shorts/tests/<파일> -q`.

---

### Task 1: `clean_base.py` — 저장·읽기·덮임 판정·재배치 (순수 함수)

**Files:**
- Create: `shopping_shorts/clean_base.py`
- Test: `shopping_shorts/tests/test_clean_base_remap.py`

**Interfaces:**
- Consumes: `mix_pipeline._beat_materials(b)` (재료 규칙), `mix_pipeline.final_clip_pairs` 결과 모양 `{video_id, beat_idx, src, fin, dur}`
- Produces:
  - `save_base(work, *, sig, path, plan, cuts) -> dict`
  - `load_base(work) -> dict | None`
  - `beat_material_key(beat) -> tuple[tuple[str, float, float], ...]`
  - `coverage(plan, base) -> dict[int, str]` 값은 `"covered" | "changed" | "new"`
  - `remap_plan(plan, base, *, tts_durs=None) -> tuple[dict, list[int], list[dict]]` = (plan2, uncovered_beat_idx, extend_requests)
  - `source_paths(base) -> dict[str, str]`
  - `time_in_clean(base, beat_idx, pos=0.5) -> float | None`

- [ ] **Step 1: 실패하는 테스트 작성**

```python
# shopping_shorts/tests/test_clean_base_remap.py
# -*- coding: utf-8 -*-
"""청소본 정본(clean base) — 재료가 그대로면 청소본 좌표로, 바뀌면 uncovered (2026-09-22)."""
import copy
import json
import pytest

from shopping_shorts import clean_base as cb


def _plan():
    return {"beats": [
        {"beat_idx": 0, "target_seconds": 2.0, "narration": "첫 줄",
         "primary": {"video_id": "s0", "seg_id": "s0-0", "start": 10.0, "end": 14.0}, "alternates": []},
        {"beat_idx": 1, "target_seconds": 3.0, "narration": "둘째 줄",
         "primary": {"video_id": "s1", "seg_id": "s1-0", "start": 5.0, "end": 9.0},
         "alternates": [{"video_id": "s2", "seg_id": "s2-0", "start": 1.0, "end": 3.0}]},
    ]}


def _cuts():
    # 청소 시점 컷 지도: beat0 = clean 0.0~2.0 (s0 10.0부터), beat1 = clean 2.0~4.0(s1) + 4.0~5.0(s2)
    return [
        {"video_id": "s0", "beat_idx": 0, "src": 10.0, "fin": 0.0, "dur": 2.0},
        {"video_id": "s1", "beat_idx": 1, "src": 5.0, "fin": 2.0, "dur": 2.0},
        {"video_id": "s2", "beat_idx": 1, "src": 1.0, "fin": 4.0, "dur": 1.0},
    ]


@pytest.fixture
def base(tmp_path):
    (tmp_path / "final_clean_abc.mp4").write_bytes(b"x" * 2048)
    return cb.save_base(tmp_path, sig="abc", path=str(tmp_path / "final_clean_abc.mp4"),
                        plan=_plan(), cuts=_cuts())


def test_save_then_load_roundtrip(tmp_path, base):
    got = cb.load_base(tmp_path)
    assert got["sig"] == "abc" and got["cuts"] == _cuts() and got["extras"] == {}
    assert json.loads((tmp_path / cb.BASE_FILE).read_text(encoding="utf-8"))["sig"] == "abc"


def test_load_none_when_file_missing(tmp_path):
    assert cb.load_base(tmp_path) is None


def test_load_none_when_clean_mp4_missing(tmp_path, base):
    (tmp_path / "final_clean_abc.mp4").unlink()
    assert cb.load_base(tmp_path) is None


def test_coverage_same_materials_is_covered(base):
    assert cb.coverage(_plan(), base) == {0: "covered", 1: "covered"}


def test_coverage_caption_lines_and_zoom_do_not_matter(base):
    p = _plan()
    p["beats"][0]["caption_lines"] = ["첫", "줄"]
    p["beats"][1]["scene_zoom"] = 1.2
    p["beats"][1]["target_seconds"] = 3.4
    assert cb.coverage(p, base) == {0: "covered", 1: "covered"}


def test_coverage_changed_material_and_new_beat(base):
    p = _plan()
    p["beats"][0]["scene_override"] = [{"video_id": "s3", "seg_id": "s3-0", "start": 0.0, "end": 2.0}]
    p["beats"].append({"beat_idx": 2, "target_seconds": 1.0,
                       "primary": {"video_id": "s0", "seg_id": "s0-1", "start": 20.0, "end": 22.0}, "alternates": []})
    assert cb.coverage(p, base) == {0: "changed", 1: "covered", 2: "new"}


def test_remap_replaces_materials_with_clean_coords(base):
    p = _plan()
    p["beats"][0]["caption_lines"] = ["첫", "줄"]          # 자막 편집은 살아남는다
    plan2, uncovered, extend = cb.remap_plan(p, base)
    assert uncovered == [] and extend == []
    b0, b1 = plan2["beats"]
    assert b0["scene_override"] == [{"video_id": "clean", "seg_id": "clean-0", "start": 0.0, "end": 2.0}]
    assert b1["scene_override"] == [{"video_id": "clean", "seg_id": "clean-1", "start": 2.0, "end": 4.0},
                                    {"video_id": "clean", "seg_id": "clean-2", "start": 4.0, "end": 5.0}]
    assert b0["caption_lines"] == ["첫", "줄"]
    assert plan2["clean_base"] is True
    assert p["beats"][0].get("scene_override") is None       # 원본은 안 건드린다


def test_remap_keeps_original_materials_for_uncovered(base):
    p = _plan()
    p["beats"][0]["scene_override"] = [{"video_id": "s3", "seg_id": "s3-0", "start": 0.0, "end": 2.0}]
    plan2, uncovered, _ = cb.remap_plan(p, base)
    assert uncovered == [0]
    assert plan2["beats"][0]["scene_override"][0]["video_id"] == "s3"


def test_remap_extend_request_when_much_longer(base):
    p = _plan()
    p["beats"][0]["target_seconds"] = 2.0 * 1.5     # 청소본에 2.0초뿐 → 50% 모자람 > EXTEND_MIN
    plan2, uncovered, extend = cb.remap_plan(p, base, tts_durs={0: 3.0})
    assert uncovered == []
    assert extend == [{"beat_idx": 0, "video_id": "s0", "start": 12.0, "end": 13.2, "need": 1.0}]


def test_remap_no_extend_when_slightly_longer(base):
    p = _plan()
    plan2, _, extend = cb.remap_plan(p, base, tts_durs={0: 2.4})     # 20% → 느리게+정지로 채운다
    assert extend == []


def test_extras_cover_changed_beat(tmp_path, base):
    p = _plan()
    p["beats"][0]["scene_override"] = [{"video_id": "s3", "seg_id": "s3-0", "start": 0.0, "end": 2.0}]
    (tmp_path / "cb0_0.mp4").write_bytes(b"x" * 2048)
    cb.add_extra(tmp_path, base, vid="cb0_0", path=str(tmp_path / "cb0_0.mp4"),
                 beat_idx=0, material_key=cb.beat_material_key(p["beats"][0]), seconds=2.0)
    base2 = cb.load_base(tmp_path)
    assert cb.coverage(p, base2) == {0: "covered", 1: "covered"}
    plan2, uncovered, _ = cb.remap_plan(p, base2)
    assert uncovered == []
    assert plan2["beats"][0]["scene_override"] == [{"video_id": "cb0_0", "seg_id": "cb0_0", "start": 0.0, "end": 2.0}]
    assert cb.source_paths(base2) == {"clean": base2["path"], "cb0_0": str(tmp_path / "cb0_0.mp4")}


def test_time_in_clean(base):
    assert cb.time_in_clean(base, 1, pos=0.0) == 2.0
    assert cb.time_in_clean(base, 1, pos=0.5) == pytest.approx(3.5)
    assert cb.time_in_clean(base, 7) is None
```

- [ ] **Step 2: 실패 확인**

Run: `py -m pytest shopping_shorts/tests/test_clean_base_remap.py -q`
Expected: FAIL — `ModuleNotFoundError: No module named 'shopping_shorts.clean_base'`

- [ ] **Step 3: 구현**

```python
# shopping_shorts/clean_base.py
# -*- coding: utf-8 -*-
"""청소본 정본(clean base) — 자막제거는 job당 한 번 (2026-09-22 사장님 대전제).

4단계에서 지운 완성본(final_clean_{sig}.mp4)을 **정본**으로 두고, 그 시점의 컷 지도를
work/<job>/clean_base.json 에 남긴다. 최종렌더·미리보기·프레임·캡컷·ZIP은 편성표의
재료를 청소본 좌표("clean", t0, t1)로 바꾼 **파생 사본**(remap_plan)으로 조립한다 →
VMake를 다시 안 탄다. 재료(장면)가 바뀐 비트만 uncovered로 돌려주고, 그 비트는
mix_pipeline.incremental_clean 이 원본에서 잘라 1콜로 지워 extras 에 넣는다.

★판정은 여기 두 함수뿐이다(0순위-B): load_base(무엇이 청소본인가) · remap_plan(재료가 어디인가).
★파생 사본은 DB에 저장하지 않는다. 원본 edit_plan은 손대지 않는다.
"""
import copy
import json
from pathlib import Path

BASE_FILE = "clean_base.json"
CLEAN_VID = "clean"
STRETCH_MAX = 0.10      # 이만큼까진 느리게 재생으로 채운다(그 다음은 정지)
EXTEND_MIN = 0.30       # 부족분이 컷의 이 비율을 넘으면 원본에서 더 잘라 증분 청소
EXTEND_PAD = 0.2        # 증분 조각 뒤 여유(초)


def beat_material_key(beat):
    """비트의 재료를 (video_id, start, end) 튜플열로. mix_pipeline._beat_materials 규칙 그대로."""
    from shopping_shorts.mix_pipeline import _beat_materials
    out = []
    for m in _beat_materials(beat or {}):
        try:
            out.append((str(m.get("video_id")), round(float(m.get("start")), 3), round(float(m.get("end")), 3)))
        except (TypeError, ValueError):
            out.append((str(m.get("video_id")), None, None))
    return tuple(out)


def save_base(work, *, sig, path, plan, cuts):
    """청소 직후 호출. 컷 지도(final_clip_pairs 결과)와 그때의 재료 키를 비트별로 남긴다."""
    keys = {int(b["beat_idx"]): list(map(list, beat_material_key(b)))
            for b in (plan or {}).get("beats") or [] if b.get("beat_idx") is not None}
    base = {"sig": sig, "path": str(path), "cuts": [dict(c) for c in cuts or []],
            "beat_keys": {str(k): v for k, v in keys.items()}, "extras": {}}
    Path(work).mkdir(parents=True, exist_ok=True)
    (Path(work) / BASE_FILE).write_text(json.dumps(base, ensure_ascii=False), encoding="utf-8")
    return base


def load_base(work):
    """정본이 있고 파일이 살아 있으면 dict, 아니면 None(호출부는 종전 경로로)."""
    p = Path(work) / BASE_FILE
    if not p.exists():
        return None
    try:
        base = json.loads(p.read_text(encoding="utf-8"))
        f = Path(base["path"])
        if not (f.exists() and f.stat().st_size > 1024):
            return None
        base.setdefault("extras", {})
        base.setdefault("beat_keys", {})
        return base
    except Exception:      # noqa: BLE001 — 깨진 정본은 없는 것으로(종전 경로 폴백)
        return None


def add_extra(work, base, *, vid, path, beat_idx, material_key, seconds):
    """증분 청소 조각을 정본에 붙인다. 같은 재료 키로 다시 오면 covered."""
    base["extras"][vid] = {"path": str(path), "beat_idx": int(beat_idx),
                           "key": list(map(list, material_key)), "seconds": float(seconds)}
    (Path(work) / BASE_FILE).write_text(json.dumps(base, ensure_ascii=False), encoding="utf-8")
    return base


def _key_list(k):
    return [list(x) for x in k]


def _extra_for(base, beat_idx, key):
    for vid, ex in (base.get("extras") or {}).items():
        if int(ex.get("beat_idx", -1)) == int(beat_idx) and ex.get("key") == _key_list(key):
            if Path(ex.get("path", "")).exists():
                return vid, ex
    return None, None


def _cuts_of(base, beat_idx):
    return [c for c in base.get("cuts") or [] if int(c.get("beat_idx", -1)) == int(beat_idx)]


def coverage(plan, base):
    """{beat_idx: "covered" | "changed" | "new"} — 재료(장면)만 본다. 자막·확대·길이는 무관."""
    out = {}
    for b in (plan or {}).get("beats") or []:
        bi = int(b["beat_idx"])
        key = beat_material_key(b)
        saved = base.get("beat_keys", {}).get(str(bi))
        if saved is None:
            out[bi] = "new" if not _extra_for(base, bi, key)[0] else "covered"
        elif saved == _key_list(key) and _cuts_of(base, bi):
            out[bi] = "covered"
        elif _extra_for(base, bi, key)[0]:
            out[bi] = "covered"
        else:
            out[bi] = "changed"
    return out


def remap_plan(plan, base, *, tts_durs=None):
    """편성표 → 청소본을 소스로 쓰는 **파생 사본**.

    covered 비트: scene_override = 청소본 컷 구간들("clean", fin, fin+dur) 또는 extras 조각.
    uncovered 비트: 재료를 그대로 둔다(호출부가 증분 청소 뒤 다시 remap 한다).
    extend: covered인데 새 길이가 청소본 컷 합보다 EXTEND_MIN 넘게 길면, 원본에서 더 잘라
            지울 구간을 요청한다({beat_idx, video_id, start, end, need}). 그 이하는
            plan_beat_clips_for 의 느리게(_MAX_SLOWMO)+정지가 채운다.
    """
    plan2 = copy.deepcopy(plan or {})
    plan2["clean_base"] = True
    cov = coverage(plan, base)
    uncovered, extend = [], []
    for b in plan2.get("beats") or []:
        bi = int(b["beat_idx"])
        if cov.get(bi) != "covered":
            uncovered.append(bi)
            continue
        key = beat_material_key(b)
        cuts = _cuts_of(base, bi)
        if base.get("beat_keys", {}).get(str(bi)) == _key_list(key) and cuts:
            b["scene_override"] = [
                {"video_id": CLEAN_VID, "seg_id": "%s-%d" % (CLEAN_VID, base["cuts"].index(c)),
                 "start": float(c["fin"]), "end": float(c["fin"]) + float(c["dur"])} for c in cuts]
            have = sum(float(c["dur"]) for c in cuts)
            need = float((tts_durs or {}).get(bi) or b.get("target_seconds") or 0.0)
            if have > 0 and need > have * (1.0 + EXTEND_MIN):
                last = cuts[-1]
                s = float(last["src"]) + float(last["dur"])
                extend.append({"beat_idx": bi, "video_id": last["video_id"], "start": round(s, 3),
                               "end": round(s + (need - have) + EXTEND_PAD, 3), "need": round(need - have, 3)})
        else:
            vid, ex = _extra_for(base, bi, key)
            b["scene_override"] = [{"video_id": vid, "seg_id": vid, "start": 0.0, "end": float(ex["seconds"])}]
    return plan2, uncovered, extend


def source_paths(base):
    """조립에 넘길 소스 맵: 청소본 + 증분 조각들."""
    out = {CLEAN_VID: base["path"]}
    for vid, ex in (base.get("extras") or {}).items():
        out[vid] = ex["path"]
    return out


def time_in_clean(base, beat_idx, pos=0.5):
    """그 비트가 청소본 어디에 있나(초). pos=0.0 첫 컷 시작 … 1.0 마지막 컷 끝. 없으면 None."""
    cuts = _cuts_of(base, beat_idx)
    if not cuts:
        return None
    t0 = float(cuts[0]["fin"])
    t1 = float(cuts[-1]["fin"]) + float(cuts[-1]["dur"])
    return t0 + (t1 - t0) * max(0.0, min(1.0, float(pos)))
```

- [ ] **Step 4: 통과 확인**

Run: `py -m pytest shopping_shorts/tests/test_clean_base_remap.py -q`
Expected: 12 passed

- [ ] **Step 5: 커밋**

```bash
git add shopping_shorts/clean_base.py shopping_shorts/tests/test_clean_base_remap.py
git commit -m "clean_base: 청소본 정본 저장·덮임 판정·재배치(순수 함수)"
```

---

### Task 2: 4단계 청소 직후 정본 저장 + 훅 시작점 가드

**Files:**
- Modify: `shopping_shorts/mix_pipeline.py` — `_final_clean_fn`(약 3273행) 안 `_clean` 성공 뒤, `run_clean_sources`
- Modify: `shopping_shorts/video_assemble.py` — `_apply_hook_inpoint`(1957행) 첫 줄
- Test: `shopping_shorts/tests/test_clean_base_save.py`

**Interfaces:**
- Consumes: `clean_base.save_base`, `final_clip_pairs(plan, tts_paths, src_durs)`, `_src_durs_for(job, work)`
- Produces: 청소 성공 시 `work/<job>/clean_base.json` 존재. `edit_plan["clean_base"] is True`면 훅 시작점 자동 이동을 건너뜀.

- [ ] **Step 1: 실패하는 테스트**

```python
# shopping_shorts/tests/test_clean_base_save.py
# -*- coding: utf-8 -*-
"""4단계 청소가 끝나면 정본(clean_base.json)이 남고, 파생 사본에선 훅 시작점을 안 옮긴다."""
import json
from pathlib import Path

import pytest

from shopping_shorts import mix_pipeline as mp
from shopping_shorts import video_assemble as va
from shopping_shorts import clean_base as cb


class _Store:
    def __init__(self): self.updates = []
    def update_mix_job(self, job_id, **kw): self.updates.append(kw)


def test_final_clean_fn_saves_base(tmp_path, monkeypatch):
    plan = {"beats": [{"beat_idx": 0, "target_seconds": 2.0, "tts_path": None,
                       "primary": {"video_id": "s0", "seg_id": "s0-0", "start": 1.0, "end": 3.0}, "alternates": []}]}
    job = {"edit_plan": plan, "subtitle_removal": 1, "customer_id": 0}
    fake_cuts = [{"video_id": "s0", "beat_idx": 0, "src": 1.0, "fin": 0.0, "dur": 2.0}]
    monkeypatch.setattr(mp, "_charge_clean", lambda *a, **k: 0)
    monkeypatch.setattr(mp, "_vmake_clean", lambda src, keys, out, tier=None: (Path(out).write_bytes(b"x" * 4096), out)[1])
    monkeypatch.setattr(mp, "final_clip_pairs", lambda plan, tts, durs: fake_cuts)
    monkeypatch.setattr(mp, "_src_durs_for", lambda job, work: {"s0": 30.0})
    fn = mp._final_clean_fn(_Store(), job, "job1", tmp_path, ["k"], 0)
    mix_raw = tmp_path / "mix_raw.mp4"; mix_raw.write_bytes(b"y" * 4096)
    out = fn(str(mix_raw))
    base = cb.load_base(tmp_path)
    assert base is not None and base["path"] == out and base["cuts"] == fake_cuts
    assert base["beat_keys"] == {"0": [["s0", 1.0, 3.0]]}


def test_hook_inpoint_skipped_for_clean_base_plan(tmp_path, monkeypatch):
    called = []
    monkeypatch.setattr(va, "_sc", type("X", (), {"peak_time_in_window": staticmethod(lambda *a: called.append(1) or 0.0)})(), raising=False)
    plan = {"clean_base": True, "beats": [{"beat_idx": 0, "primary": {"video_id": "clean", "start": 0.0, "end": 2.0}}]}
    va._apply_hook_inpoint(plan, {"clean": str(tmp_path / "c.mp4")}, tmp_path)
    assert called == []
    assert plan["beats"][0]["primary"]["start"] == 0.0
```

- [ ] **Step 2: 실패 확인**

Run: `py -m pytest shopping_shorts/tests/test_clean_base_save.py -q`
Expected: 첫 테스트 FAIL(`load_base` None), 둘째는 `_apply_hook_inpoint` 내부에 따라 FAIL 또는 ERROR

- [ ] **Step 3: 구현** — `_final_clean_fn`의 `_clean` 안, `res = _vmake_clean(...)` 성공 뒤(`_save_clean_plan_snapshot` 호출 바로 다음)에 추가:

```python
        _save_clean_plan_snapshot(work, sig, job.get("edit_plan"))
        # ★청소본 정본(2026-09-22): 이 파일과 그 시점 컷 지도를 job의 정본으로 남긴다.
        #   이후 렌더·프레임·캡컷은 clean_base.remap_plan 으로 이 파일을 소스 삼아 조립한다.
        try:
            from shopping_shorts import clean_base as _cb
            _plan = job.get("edit_plan") or {}
            _tts = {b["beat_idx"]: b["tts_path"] for b in _plan.get("beats") or [] if b.get("tts_path")}
            _cb.save_base(work, sig=sig, path=str(res),
                          plan=_plan, cuts=final_clip_pairs(_plan, _tts, _src_durs_for(job, work)))
        except Exception as _e:      # noqa: BLE001 — 정본 저장 실패는 종전 경로로 남을 뿐, 청소는 성공
            print("[clean-base] 정본 저장 실패(무해, 종전 경로): %s" % _e, file=sys.stderr)
        return res
```

같은 블록을 재사용 분기(`완성본 재사용(편성 그대로, 과금 0)`)에도 넣는다 — 옛 job이 재사용될 때도 정본이 생기게:

```python
        if out.exists() and out.stat().st_size > 1024:
            print(f"[clean] 완성본 재사용(편성 그대로, 과금 0): {out.name}", file=sys.stderr)
            _save_clean_plan_snapshot(work, sig, job.get("edit_plan"))
            try:
                from shopping_shorts import clean_base as _cb
                _old = _cb.load_base(work)
                # 정본이 없거나 **다른 등급/서명의 파일**이면 이 파일로 정본을 다시 쓴다(등급 변경 = 새 정본)
                if _old is None or _old.get("sig") != sig:
                    _plan = job.get("edit_plan") or {}
                    _tts = {b["beat_idx"]: b["tts_path"] for b in _plan.get("beats") or [] if b.get("tts_path")}
                    _cb.save_base(work, sig=sig, path=str(out),
                                  plan=_plan, cuts=final_clip_pairs(_plan, _tts, _src_durs_for(job, work)))
            except Exception as _e:      # noqa: BLE001
                print("[clean-base] 정본 저장 실패(무해): %s" % _e, file=sys.stderr)
            return str(out)
```

`video_assemble._apply_hook_inpoint` 맨 앞:

```python
def _apply_hook_inpoint(edit_plan, source_video_paths, work):
    # ★청소본 정본 파생 사본(clean_base=True)은 재료가 이미 청소본 컷 좌표다 — 피크를 다시
    #   찾아 옮기면 컷 밖(딴 장면)으로 나간다. 청소 시점에 이미 반영된 시작점을 그대로 쓴다(2026-09-22).
    if (edit_plan or {}).get("clean_base"):
        return
```

- [ ] **Step 4: 통과 확인**

Run: `py -m pytest shopping_shorts/tests/test_clean_base_save.py shopping_shorts/tests/test_clean_base_remap.py -q`
Expected: 14 passed

- [ ] **Step 5: 커밋**

```bash
git add shopping_shorts/mix_pipeline.py shopping_shorts/video_assemble.py shopping_shorts/tests/test_clean_base_save.py
git commit -m "clean_base: 4단계 청소 직후 정본 저장, 파생 사본에선 훅 시작점 이동 생략"
```

---

### Task 3: 관리자 스위치 `clean_base_enabled` (판정 한 곳)

**Files:**
- Modify: `shopping_shorts/mix_pipeline.py` — 새 함수 `clean_base_on(store, customer_id)` (`_clean_strategy` 바로 위)
- Modify: `shopping_shorts/app.py` — 관리자 설정 화면의 스위치 목록에 키 추가(기존 `story_writer_enabled`가 등록된 자리와 같은 곳, `grep -n "story_writer_enabled" shopping_shorts/app.py shopping_shorts/static/admin*.html`로 찾는다)
- Test: `shopping_shorts/tests/test_clean_base_gate.py`

**Interfaces:**
- Produces: `mix_pipeline.clean_base_on(store, customer_id) -> bool`. 설정값 규칙은 `app._setting_gate`와 동일(""/"0" 끔 · "admin" 관리자만 · "11,42" 목록 · "1" 전체).

- [ ] **Step 1: 실패하는 테스트**

```python
# shopping_shorts/tests/test_clean_base_gate.py
# -*- coding: utf-8 -*-
from shopping_shorts import mix_pipeline as mp


class _S:
    def __init__(self, v): self.v = v
    def get_setting(self, k, d=None): return self.v if k == "clean_base_enabled" else d


def test_default_off():
    assert mp.clean_base_on(_S(""), 42) is False
    assert mp.clean_base_on(_S("0"), 0) is False


def test_all_on():
    assert mp.clean_base_on(_S("1"), 42) is True


def test_admin_only(monkeypatch):
    from shopping_shorts import app as _app
    monkeypatch.setattr(_app, "_is_admin", lambda cid: cid == 0)
    assert mp.clean_base_on(_S("admin"), 0) is True
    assert mp.clean_base_on(_S("admin"), 42) is False


def test_customer_list(monkeypatch):
    from shopping_shorts import app as _app
    monkeypatch.setattr(_app, "_is_admin", lambda cid: False)
    monkeypatch.setattr(_app, "_as_cid", lambda cid: int(cid))
    assert mp.clean_base_on(_S("11,42"), 42) is True
    assert mp.clean_base_on(_S("11,42"), 7) is False
```

- [ ] **Step 2: 실패 확인**

Run: `py -m pytest shopping_shorts/tests/test_clean_base_gate.py -q`
Expected: FAIL — `AttributeError: module 'shopping_shorts.mix_pipeline' has no attribute 'clean_base_on'`

- [ ] **Step 3: 구현** (`_clean_strategy` 바로 위)

```python
def clean_base_on(store, customer_id=0):
    """청소본 정본 경로 스위치(2026-09-22). 설정 키 clean_base_enabled — 판정 규칙은
    app._setting_gate 하나를 그대로 쓴다(0순위-B: 같은 규칙을 두 번 적지 않는다).
    기본 끔 → 종전 경로(완성본 서명 재사용/재청소). 워커에서도 부르므로 app을 늦게 import 한다."""
    try:
        from shopping_shorts.app import _setting_gate
        return bool(_setting_gate(store, "clean_base_enabled", customer_id))
    except Exception:      # noqa: BLE001 — 판정 실패는 '끔'
        return False
```

관리자 화면 등록: `story_writer_enabled`가 나열된 설정 키 목록(설정 화면 렌더 또는 허용 키 목록)에 `"clean_base_enabled"`를 같은 형식으로 한 줄 추가하고, 라벨은 `"자막제거 정본(재청소 없음)"`.

- [ ] **Step 4: 통과 확인**

Run: `py -m pytest shopping_shorts/tests/test_clean_base_gate.py -q`
Expected: 4 passed

- [ ] **Step 5: 커밋**

```bash
git add shopping_shorts/mix_pipeline.py shopping_shorts/app.py shopping_shorts/tests/test_clean_base_gate.py
git commit -m "clean_base_enabled 스위치 — 기본 끔, 판정은 _setting_gate 하나"
```

---

### Task 4: 증분 청소 `incremental_clean` (바뀐 장면·큰 늘림만, 1콜)

**Files:**
- Modify: `shopping_shorts/mix_pipeline.py` — 새 함수 `incremental_clean` (`_final_clean_fn` 아래)
- Test: `shopping_shorts/tests/test_clean_base_incremental.py`

**Interfaces:**
- Consumes: `clean_base.remap_plan` 결과의 `uncovered`(비트 번호) · `extend`(요청 dict), `_resolve_sources`, `_clean_joined(items, keys, work, tag)`(여러 조각 → 1콜 → `{vid: path}`), `_charge_clean(store, cid, n)`, `_refund_clean`, `clean_tier_of(job)`, `clean_base.add_extra`
- Produces: `incremental_clean(store, job, job_id, work, keys, customer_id, base, plan, uncovered, extend) -> dict` (갱신된 base). 조각 vid 규칙: 바뀐 비트 `cb{beat_idx}_{k}`, 늘림 `cbx{beat_idx}`.

- [ ] **Step 1: 실패하는 테스트**

```python
# shopping_shorts/tests/test_clean_base_incremental.py
# -*- coding: utf-8 -*-
"""바뀐 장면·큰 늘림만 원본에서 잘라 **1콜**로 지우고 정본 extras에 붙인다."""
from pathlib import Path
import pytest

from shopping_shorts import mix_pipeline as mp
from shopping_shorts import clean_base as cb


class _Store:
    def update_mix_job(self, *a, **k): pass


def _job_and_base(tmp_path):
    plan = {"beats": [
        {"beat_idx": 0, "target_seconds": 2.0, "primary": {"video_id": "s0", "seg_id": "s0-0", "start": 1.0, "end": 3.0}, "alternates": []},
        {"beat_idx": 1, "target_seconds": 2.0, "primary": {"video_id": "s1", "seg_id": "s1-0", "start": 5.0, "end": 7.0}, "alternates": []}]}
    job = {"edit_plan": plan, "urls": ["u0", "u1"], "customer_id": 0, "clean_tier": "basic"}
    for i in ("s0", "s1"):
        (tmp_path / i).mkdir(); (tmp_path / i / "v.mp4").write_bytes(b"v" * 4096)
    (tmp_path / "final_clean_abc.mp4").write_bytes(b"c" * 4096)
    base = cb.save_base(tmp_path, sig="abc", path=str(tmp_path / "final_clean_abc.mp4"), plan=plan,
                        cuts=[{"video_id": "s0", "beat_idx": 0, "src": 1.0, "fin": 0.0, "dur": 2.0},
                              {"video_id": "s1", "beat_idx": 1, "src": 5.0, "fin": 2.0, "dur": 2.0}])
    return job, base


def test_changed_beat_cleaned_in_one_call_and_added_to_extras(tmp_path, monkeypatch):
    job, base = _job_and_base(tmp_path)
    plan = job["edit_plan"]
    plan["beats"][0]["scene_override"] = [{"video_id": "s1", "seg_id": "s1-9", "start": 10.0, "end": 12.5}]
    calls, charged = [], []
    def _cut(src, ss, dur, dst):
        Path(dst).write_bytes(b"p" * 2048); return dst
    monkeypatch.setattr(mp, "_cut_piece", _cut)
    def _joined(items, keys, work, tag=""):
        calls.append([v for v, _ in items])
        return {v: str(Path(work) / f"{v}_clean.mp4") for v, _ in items}, {}
    monkeypatch.setattr(mp, "_clean_joined", _joined)
    monkeypatch.setattr(mp, "_charge_clean", lambda s, c, n: charged.append(n) or 0)
    for v in ("cb0_0",):
        (tmp_path / f"{v}_clean.mp4").write_bytes(b"q" * 2048)
    base2 = mp.incremental_clean(_Store(), job, "j", tmp_path, ["k"], 0, base, plan, uncovered=[0], extend=[])
    assert calls == [["cb0_0"]] and charged == [1]
    assert base2["extras"]["cb0_0"]["seconds"] == pytest.approx(2.5)
    assert cb.coverage(plan, cb.load_base(tmp_path)) == {0: "covered", 1: "covered"}


def test_extend_request_cleans_tail_piece(tmp_path, monkeypatch):
    job, base = _job_and_base(tmp_path)
    plan = job["edit_plan"]
    monkeypatch.setattr(mp, "_cut_piece", lambda src, ss, dur, dst: (Path(dst).write_bytes(b"p" * 2048), dst)[1])
    monkeypatch.setattr(mp, "_clean_joined", lambda items, keys, work, tag="": ({v: str(Path(work) / f"{v}_clean.mp4") for v, _ in items}, {}))
    (tmp_path / "cbx1_clean.mp4").write_bytes(b"q" * 2048)
    monkeypatch.setattr(mp, "_charge_clean", lambda s, c, n: 0)
    ext = [{"beat_idx": 1, "video_id": "s1", "start": 7.0, "end": 8.2, "need": 1.0}]
    base2 = mp.incremental_clean(_Store(), job, "j", tmp_path, ["k"], 0, base, plan, uncovered=[], extend=ext)
    assert base2["extras"]["cbx1"]["seconds"] == pytest.approx(1.2)
    assert base2["extras"]["cbx1"]["beat_idx"] == 1 and base2["extras"]["cbx1"]["key"] == [["s1", 5.0, 7.0]]


def test_failure_refunds_and_raises(tmp_path, monkeypatch):
    job, base = _job_and_base(tmp_path)
    plan = job["edit_plan"]
    plan["beats"][0]["scene_override"] = [{"video_id": "s1", "seg_id": "s1-9", "start": 10.0, "end": 12.5}]
    monkeypatch.setattr(mp, "_cut_piece", lambda src, ss, dur, dst: (Path(dst).write_bytes(b"p" * 2048), dst)[1])
    def _boom(*a, **k): raise RuntimeError("vendor down")
    monkeypatch.setattr(mp, "_clean_joined", _boom)
    refunded = []
    monkeypatch.setattr(mp, "_charge_clean", lambda s, c, n: 7)
    monkeypatch.setattr(mp, "_refund_clean", lambda s, c, amt: refunded.append(amt))
    with pytest.raises(RuntimeError):
        mp.incremental_clean(_Store(), job, "j", tmp_path, ["k"], 0, base, plan, uncovered=[0], extend=[])
    assert refunded == [7]
```

- [ ] **Step 2: 실패 확인**

Run: `py -m pytest shopping_shorts/tests/test_clean_base_incremental.py -q`
Expected: FAIL — `no attribute 'incremental_clean'` / `'_cut_piece'`

- [ ] **Step 3: 구현** (`_final_clean_fn` 아래)

```python
def _cut_piece(src, ss, dur, dst):
    """원본 소스에서 [ss, ss+dur) 한 조각을 규격 그대로 잘라낸다(증분 청소 입력)."""
    subprocess.run(["ffmpeg", "-y", "-v", "error", "-ss", f"{ss:.3f}", "-i", str(src),
                    "-t", f"{dur:.3f}", "-c:v", "libx264", "-preset", "veryfast", "-crf", "18",
                    "-pix_fmt", "yuv420p", "-c:a", "aac", "-ar", "48000", "-ac", "2", str(dst)], check=True)
    return str(dst)


def incremental_clean(store, job, job_id, work, keys, customer_id, base, plan, uncovered, extend):
    """정본에 없는 재료(바뀐 장면·큰 늘림)만 원본에서 잘라 **1콜**로 지우고 extras에 붙인다.

    ★돈이 나가는 함수다 — 콜 1회 선차감(_charge_clean), 실패 시 전액 환불 후 예외.
    ★조각 vid: 바뀐 비트 cb{beat}_{k} · 늘림 cbx{beat}. 재료 키를 함께 저장해 같은 재료로
      다시 오면 clean_base.coverage 가 covered 로 본다(재과금 0)."""
    from shopping_shorts import clean_base as _cb
    work = Path(work)
    srcs = _resolve_sources(job, work)
    beats = {int(b["beat_idx"]): b for b in (plan or {}).get("beats") or []}
    items, meta = [], {}
    for bi in uncovered:
        b = beats.get(int(bi))
        if not b:
            continue
        key = _cb.beat_material_key(b)
        for k, m in enumerate(_beat_materials(b)):
            src = srcs.get(m.get("video_id"))
            if not src:
                continue
            s, e = float(m["start"]), float(m["end"])
            vid = "cb%d_%d" % (int(bi), k)
            dst = _cut_piece(src, s, e - s, work / f"{vid}.mp4")
            items.append((vid, dst)); meta[vid] = (int(bi), key, e - s)
    for ex in extend or []:
        src = srcs.get(ex["video_id"])
        if not src:
            continue
        b = beats.get(int(ex["beat_idx"]))
        vid = "cbx%d" % int(ex["beat_idx"])
        dst = _cut_piece(src, float(ex["start"]), float(ex["end"]) - float(ex["start"]), work / f"{vid}.mp4")
        items.append((vid, dst)); meta[vid] = (int(ex["beat_idx"]), _cb.beat_material_key(b), float(ex["end"]) - float(ex["start"]))
    if not items:
        return base
    charged = _charge_clean(store, customer_id, 1)
    try:
        print("[clean-base] 증분 청소 %d조각 1콜: %s" % (len(items), [v for v, _ in items]), file=sys.stderr)
        paths, _regions = _clean_joined(items, keys, str(work), tag="cb")
    except Exception:
        if charged:
            _refund_clean(store, customer_id, charged)
        raise
    for vid, p in paths.items():
        bi, key, sec = meta[vid]
        base = _cb.add_extra(work, base, vid=vid, path=p, beat_idx=bi, material_key=key, seconds=sec)
    return base
```

`_clean_joined`의 반환값이 `(paths, regions)` 두 개인지 함수 본문(약 2634행)에서 확인하고, 하나만 돌려주면 위 호출을 그에 맞춘다(테스트도 같이).

- [ ] **Step 4: 통과 확인**

Run: `py -m pytest shopping_shorts/tests/test_clean_base_incremental.py -q`
Expected: 3 passed

- [ ] **Step 5: 커밋**

```bash
git add shopping_shorts/mix_pipeline.py shopping_shorts/tests/test_clean_base_incremental.py
git commit -m "clean_base: 바뀐 장면·큰 늘림만 원본에서 잘라 1콜 증분 청소"
```

---

### Task 5: `run_render`·`run_preview`가 정본 경로를 탄다 (VMake 0회)

**Files:**
- Modify: `shopping_shorts/mix_pipeline.py` — `run_render`(3834행 부근, `if job.get("subtitle_removal"):` 블록), `run_preview`(3699행 부근, `assemble(` 직전)
- Test: `shopping_shorts/tests/test_clean_base_render.py`

**Interfaces:**
- Consumes: `clean_base.load_base/remap_plan/source_paths`, `clean_base_on`, `incremental_clean`
- Produces: 새 헬퍼 `render_inputs_for(store, job, job_id, work, keys, customer_id, *, allow_clean=True) -> (plan_used, source_video_paths, base_or_None)` — 렌더·미리보기·(Task 6에서) 캡컷·ZIP·프레임이 **같은 함수**로 입력을 받는다.

- [ ] **Step 1: 실패하는 테스트**

```python
# shopping_shorts/tests/test_clean_base_render.py
# -*- coding: utf-8 -*-
"""정본이 있으면 렌더는 청소본을 소스로 조립하고 VMake를 안 부른다(재과금 0)."""
from pathlib import Path
import pytest

from shopping_shorts import mix_pipeline as mp
from shopping_shorts import clean_base as cb


class _Store:
    def __init__(self, job): self.job = job; self.updates = []
    def get_mix_job(self, jid): return self.job
    def update_mix_job(self, jid, **kw): self.updates.append(kw); self.job.update(kw)
    def get_setting(self, k, d=None): return "1" if k == "clean_base_enabled" else d


def _setup(tmp_path):
    plan = {"beats": [{"beat_idx": 0, "target_seconds": 2.0, "narration": "a", "tts_path": None,
                       "primary": {"video_id": "s0", "seg_id": "s0-0", "start": 1.0, "end": 3.0}, "alternates": []}]}
    job = {"job_id": "j", "edit_plan": plan, "urls": ["u"], "subtitle_removal": 1, "customer_id": 0, "clean_status": "ready"}
    (tmp_path / "s0").mkdir(); (tmp_path / "s0" / "v.mp4").write_bytes(b"v" * 4096)
    (tmp_path / "final_clean_abc.mp4").write_bytes(b"c" * 4096)
    cb.save_base(tmp_path, sig="abc", path=str(tmp_path / "final_clean_abc.mp4"), plan=plan,
                 cuts=[{"video_id": "s0", "beat_idx": 0, "src": 1.0, "fin": 0.0, "dur": 2.0}])
    return job


def test_render_inputs_use_clean_base(tmp_path, monkeypatch):
    job = _setup(tmp_path)
    job["edit_plan"]["beats"][0]["caption_lines"] = ["a", "b"]      # 꾸미기 편집
    monkeypatch.setattr(mp, "_vmake_clean", lambda *a, **k: pytest.fail("VMake가 불렸다"))
    plan_used, paths, base = mp.render_inputs_for(_Store(job), job, "j", tmp_path, ["k"], 0)
    assert base is not None and paths == {"clean": str(tmp_path / "final_clean_abc.mp4")}
    assert plan_used["beats"][0]["scene_override"][0]["video_id"] == "clean"
    assert plan_used["clean_base"] is True
    assert job["edit_plan"].get("clean_base") is None              # DB 원본은 그대로


def test_render_inputs_fall_back_when_switch_off(tmp_path, monkeypatch):
    job = _setup(tmp_path)
    class _Off(_Store):
        def get_setting(self, k, d=None): return ""
    plan_used, paths, base = mp.render_inputs_for(_Off(job), job, "j", tmp_path, ["k"], 0)
    assert base is None and "s0" in paths and plan_used is job["edit_plan"]


def test_render_inputs_run_incremental_for_changed_beat(tmp_path, monkeypatch):
    job = _setup(tmp_path)
    job["edit_plan"]["beats"][0]["scene_override"] = [{"video_id": "s0", "seg_id": "s0-1", "start": 20.0, "end": 22.0}]
    seen = {}
    def _inc(store, job, job_id, work, keys, cid, base, plan, uncovered, extend):
        seen["uncovered"] = uncovered
        (Path(work) / "cb0_0.mp4").write_bytes(b"q" * 2048)
        return cb.add_extra(work, base, vid="cb0_0", path=str(Path(work) / "cb0_0.mp4"), beat_idx=0,
                            material_key=cb.beat_material_key(plan["beats"][0]), seconds=2.0)
    monkeypatch.setattr(mp, "incremental_clean", _inc)
    plan_used, paths, base = mp.render_inputs_for(_Store(job), job, "j", tmp_path, ["k"], 0)
    assert seen["uncovered"] == [0]
    assert plan_used["beats"][0]["scene_override"][0]["video_id"] == "cb0_0" and "cb0_0" in paths


def test_run_render_passes_clean_fn_none_with_base(tmp_path, monkeypatch):
    job = _setup(tmp_path)
    store = _Store(job)
    monkeypatch.setattr(mp, "Store", lambda db: store)
    monkeypatch.setattr(mp, "_synthesize_beats", lambda *a, **k: None)
    monkeypatch.setattr(mp, "pron_corrections", type("P", (), {"load": staticmethod(lambda s: {})})())
    monkeypatch.setattr(mp, "_vmake_keys", lambda s, c: ["k"])
    monkeypatch.setattr(mp, "_vmake_clean", lambda *a, **k: pytest.fail("VMake가 불렸다"))
    got = {}
    def _assemble(plan, tts, paths, out, clean_fn=None, **kw):
        got["clean_fn"] = clean_fn; got["paths"] = paths; got["plan"] = plan
        Path(out).write_bytes(b"f" * 4096); return out
    monkeypatch.setattr(mp, "assemble", _assemble)
    for name in ("resolve_deco_media", "_template_layer", "_resolve_cutaway_paths", "_resolve_sfx_paths"):
        if hasattr(mp, name):
            monkeypatch.setattr(mp, name, lambda *a, **k: {} if name != "_template_layer" else None)
    mp.run_render("j", "db", tmp_path.parent) if (tmp_path / "j").exists() else None
    # run_render는 work_root/<job_id>를 쓴다 → tmp_path 를 j 폴더로 맞춘다
    import shutil; shutil.copytree(tmp_path, tmp_path.parent / "j", dirs_exist_ok=True)
    mp.run_render("j", "db", tmp_path.parent)
    assert got["clean_fn"] is None and got["paths"]["clean"].endswith("final_clean_abc.mp4")
    assert store.job.get("status") == "done"
```

- [ ] **Step 2: 실패 확인**

Run: `py -m pytest shopping_shorts/tests/test_clean_base_render.py -q`
Expected: FAIL — `no attribute 'render_inputs_for'`

- [ ] **Step 3: 구현** — `run_render` 위에 헬퍼를 두고, `run_render`/`run_preview`의 소스·청소 결정 블록을 이 헬퍼로 바꾼다.

```python
def render_inputs_for(store, job, job_id, work, keys, customer_id=0, *, allow_clean=True):
    """렌더 계열(최종·미리보기·캡컷·ZIP·프레임)의 **입력을 정하는 유일한 자리**(2026-09-22).

    반환 (plan_used, source_video_paths, base):
      정본 경로(스위치 켬 + clean_base.json 있음):
        plan_used = 청소본 좌표로 재배치한 파생 사본, source_video_paths = {"clean": 청소본, cb…: 증분 조각}
        base = 정본 dict. VMake는 증분(바뀐 장면·큰 늘림)일 때만, allow_clean=True 일 때만 탄다.
      아니면: (job["edit_plan"], _resolve_sources(job, work), None) — 종전 그대로.
    ★plan_used 는 DB에 저장하지 않는다."""
    from shopping_shorts import clean_base as _cb
    plan = job.get("edit_plan") or {}
    work = Path(work)
    if not (job.get("subtitle_removal") and clean_base_on(store, customer_id)):
        return plan, _resolve_sources(job, work), None
    base = _cb.load_base(work)
    if base is None:
        return plan, _resolve_sources(job, work), None
    tts_durs = {}
    for b in plan.get("beats") or []:
        try:
            tts_durs[int(b["beat_idx"])] = float(b.get("target_seconds") or 0)
        except (TypeError, ValueError):
            pass
    plan2, uncovered, extend = _cb.remap_plan(plan, base, tts_durs=tts_durs)
    if (uncovered or extend) and allow_clean:
        base = incremental_clean(store, job, job_id, work, keys, customer_id, base, plan, uncovered, extend)
        plan2, uncovered, extend = _cb.remap_plan(plan, base, tts_durs=tts_durs)
    if uncovered:
        # 증분을 못 했거나(allow_clean=False) 실패 — 원본 재료가 남는 비트가 있다. 원본 소스도 같이 넘긴다.
        print("[clean-base] 원본 재료 잔존 비트 %s (자막 남을 수 있음)" % uncovered, file=sys.stderr)
        paths = dict(_resolve_sources(job, work)); paths.update(_cb.source_paths(base))
        return plan2, paths, base
    print("[clean-base] 정본 조립(VMake 0회): %s" % Path(base["path"]).name, file=sys.stderr)
    return plan2, _cb.source_paths(base), base
```

`run_render`에서:
- `source_video_paths = _resolve_sources(job, work)` 줄을 삭제하고, `if job.get("subtitle_removal"):` 블록 **앞**에 넣는다:

```python
        keys = _vmake_keys(store, job.get("customer_id") or 0) if job.get("subtitle_removal") else []
        plan_used, source_video_paths, _base = render_inputs_for(
            store, job, job_id, work, keys, job.get("customer_id") or 0)
```
- 기존 `if job.get("subtitle_removal"):` 블록을 `if job.get("subtitle_removal") and _base is None:`로 바꾼다(정본이 없을 때만 종전 경로).
- 그 아래 `assemble(plan, ...)` 호출의 첫 인자를 `plan_used`로 바꾼다. **단** `_burn_captions`가 쓰는 자막·대사는 plan2에도 그대로 복사돼 있으므로 다른 인자는 그대로.
- `store.update_mix_job(job_id, edit_plan=plan)`(TTS 보장 뒤 저장)은 `plan`(원본) 그대로 둔다. plan2를 저장하는 줄이 생기면 안 된다.

`run_preview`에서 `source_video_paths = _resolve_sources(job, work)`를:

```python
        plan_used, source_video_paths, _base = render_inputs_for(
            store, job, job_id, work, [], job.get("customer_id") or 0, allow_clean=False)
```
로 바꾸고 `assemble(plan_used, ...)`. (미리보기는 돈을 안 쓴다 — 바뀐 비트는 원본 재료로 보인다.)

- [ ] **Step 4: 통과 확인**

Run: `py -m pytest shopping_shorts/tests/test_clean_base_render.py shopping_shorts/tests/test_clean_once_per_video.py shopping_shorts/tests/test_final_clean.py shopping_shorts/tests/test_clean_step2_final.py -q`
Expected: 신규 4 passed, 기존 3파일 회귀 없음(기존 실패 수가 늘지 않음 — 트랙 시작 시점 `py -m pytest shopping_shorts/tests/test_clean_*.py -q`의 실패 수를 먼저 적어 둔다)

- [ ] **Step 5: 커밋**

```bash
git add shopping_shorts/mix_pipeline.py shopping_shorts/tests/test_clean_base_render.py
git commit -m "render_inputs_for: 렌더·미리보기가 정본(청소본)을 소스로 조립, VMake 0회"
```

---

### Task 6: 프레임·썸네일·전후비교·캡컷·ZIP도 같은 입력을 쓴다 (app.py)

**Files:**
- Modify: `shopping_shorts/app.py` — `_final_cuts`(21241행 부근), `_clean_frame_src`(21258행 부근), `api_mix_capcut`(9232행 부근), `api_mix_export`(9182행 부근), `api_produce_mix_clean_clips`(7390행 부근)
- Modify: `shopping_shorts/mix_pipeline.py` — `clean_final_path_for_plan`, `clean_compare_clips`
- Test: `shopping_shorts/tests/test_clean_base_app_paths.py`

**Interfaces:**
- Consumes: `mix_pipeline.render_inputs_for(..., allow_clean=False)`, `clean_base.load_base`, `clean_base.time_in_clean`
- Produces: 정본이 있으면 `clean_final_path_for_plan(job, work)`가 정본 경로를 돌려준다(서명 무관). `_final_cuts`·캡컷·ZIP은 `plan_used`+`source_video_paths`로 컷을 편다.

- [ ] **Step 1: 실패하는 테스트**

```python
# shopping_shorts/tests/test_clean_base_app_paths.py
# -*- coding: utf-8 -*-
"""정본이 있으면 화면·내보내기도 그 청소본을 본다 — 서명이 달라져도 stale 아님."""
from pathlib import Path
import pytest

from shopping_shorts import mix_pipeline as mp
from shopping_shorts import clean_base as cb


def _job_with_base(tmp_path):
    plan = {"beats": [{"beat_idx": 0, "target_seconds": 2.0, "narration": "a",
                       "primary": {"video_id": "s0", "seg_id": "s0-0", "start": 1.0, "end": 3.0}, "alternates": []}]}
    job = {"edit_plan": plan, "urls": ["u"], "subtitle_removal": 1, "customer_id": 0, "clean_status": "ready"}
    (tmp_path / "final_clean_old.mp4").write_bytes(b"c" * 4096)
    cb.save_base(tmp_path, sig="old", path=str(tmp_path / "final_clean_old.mp4"), plan=plan,
                 cuts=[{"video_id": "s0", "beat_idx": 0, "src": 1.0, "fin": 0.0, "dur": 2.0}])
    plan["beats"][0]["caption_lines"] = ["a", "b"]     # 서명이 바뀐다
    return job


def test_clean_final_path_for_plan_returns_base_when_on(tmp_path, monkeypatch):
    job = _job_with_base(tmp_path)
    monkeypatch.setattr(mp, "clean_base_on", lambda store, cid: True)
    assert str(mp.clean_final_path_for_plan(job, tmp_path)).endswith("final_clean_old.mp4")
    assert mp.clean_final_matches_plan(job, tmp_path) is True


def test_clean_final_path_for_plan_unchanged_when_off(tmp_path, monkeypatch):
    job = _job_with_base(tmp_path)
    monkeypatch.setattr(mp, "clean_base_on", lambda store, cid: False)
    assert mp.clean_final_path_for_plan(job, tmp_path) is None       # 종전: 서명이 다르면 None


def test_compare_clips_not_stale_with_base(tmp_path, monkeypatch):
    job = _job_with_base(tmp_path)
    monkeypatch.setattr(mp, "clean_base_on", lambda store, cid: True)
    monkeypatch.setattr(mp, "_src_durs_for", lambda job, work: {"s0": 30.0})
    out = mp.clean_compare_clips(job, tmp_path)
    assert out["stale"] is False and out["clean_path"].endswith("final_clean_old.mp4")
```

- [ ] **Step 2: 실패 확인**

Run: `py -m pytest shopping_shorts/tests/test_clean_base_app_paths.py -q`
Expected: 첫·셋째 FAIL

- [ ] **Step 3: 구현**

`mix_pipeline.clean_final_path_for_plan` 맨 앞(try 안, `clean_sources` 검사 뒤)에:

```python
        # ★정본(2026-09-22): 스위치가 켜져 있고 clean_base.json이 있으면 그 파일이 곧 정본이다.
        #   편성 서명이 바뀌어도(줄·확대·컷) 청소본은 유효하다 — 렌더가 그 위에서 조립하므로.
        from shopping_shorts import clean_base as _cb
        try:
            _store = Store(config.DB_PATH)
        except Exception:      # noqa: BLE001
            _store = None
        if _store is not None and clean_base_on(_store, (job or {}).get("customer_id") or 0):
            _b = _cb.load_base(work)
            if _b is not None:
                return Path(_b["path"])
```

`clean_compare_clips`는 `clean_final_path_for_plan`을 이미 부르므로 그대로 `stale=False`가 된다(테스트로 확인). **좌우 컷 좌표**는 정본 시점 편성이어야 맞다 — `plan = job["edit_plan"]` 대신 정본이면 `plan = json.loads(Path(work / f"final_clean_{base['sig']}.plan.json").read_text())`(스냅샷)로 편다:

```python
        fresh = clean_final_path_for_plan(job, work)
        if fresh is not None:
            _b = _cb.load_base(work)
            if _b is not None and Path(_b["path"]) == Path(str(fresh)):
                sp = _clean_plan_snapshot_path(work, _b["sig"])
                plan = json.loads(sp.read_text(encoding="utf-8")) if sp.exists() else (job.get("edit_plan") or {})
            else:
                plan = job.get("edit_plan") or {}
            out["clean_path"], out["plan_used"] = str(fresh), "current"
```

`app.py`:
- `_final_cuts(job, work)`: 정본이면 `render_inputs_for(Store(DB_PATH), job, job_id?, work, [], cid, allow_clean=False)`의 `plan_used`·`source_video_paths`로 `final_clip_pairs`를 편다. `_final_cuts`는 `job_id`를 안 받으므로 `work.name`을 job_id로 쓴다:

```python
def _final_cuts(job, work):
    try:
        plan, srcs, _b = mix_pipeline.render_inputs_for(
            Store(DB_PATH), job, Path(work).name, work, [], job.get("customer_id") or 0, allow_clean=False)
    except Exception:      # noqa: BLE001
        plan, srcs = (job or {}).get("edit_plan") or {}, {}
    tts = {b["beat_idx"]: b["tts_path"] for b in (plan.get("beats") or []) if b.get("tts_path")}
    try:
        durs = {v: (frame_extract._probe_duration(pth) or 0.0) for v, pth in (srcs or {}).items()}
    except Exception:      # noqa: BLE001
        durs = {}
    try:
        return mix_pipeline.final_clip_pairs(plan, tts, durs) or []
    except Exception:      # noqa: BLE001
        return []
```
- `_clean_frame_src`: 정본이면 `clean_fresh=True`, `clean_final=정본 경로`, `final_ratio`는 `clean_base.time_in_clean(base, beat_idx, 0.5) / 청소본 길이`. 기존 분기 `_fresh_path = mix_pipeline.clean_final_path_for_plan(job, work)`가 이미 정본을 돌려주므로 `fresh=True`가 된다 — **좌표만** 정본 컷 지도로 바꾼다: `final_ratio` 계산 직전에

```python
    _base = clean_base.load_base(work) if mix_pipeline.clean_base_on(Store(DB_PATH), job.get("customer_id") or 0) else None
    if _base is not None:
        _t = clean_base.time_in_clean(_base, beat_idx, 0.5)
        _dur = frame_extract._probe_duration(cvp) or 0.0
        if _t is not None and _dur > 0:
            return {}, cvp, max(0.0, min(1.0, _t / _dur)), "_cb_%s" % _base["sig"], True
```
- `api_mix_capcut`·`api_mix_export`: `plan = job["edit_plan"]` / `source_video_paths = _resolve_sources(job, work)` 두 줄을

```python
    plan, source_video_paths, _base = mix_pipeline.render_inputs_for(
        Store(DB_PATH), job, job_id, work, [], job.get("customer_id") or 0, allow_clean=False)
```
로 바꾼다. 캡컷의 `if job.get("subtitle_removal") and not (job.get("clean_sources") or {}):` 분기는 `_cf = clean_final_path_for_plan(...)`가 정본을 돌려주고 `timeline = _beat_timeline(plan, tts_paths)`가 plan2 기준이라 그대로 동작한다(검증은 Task 8 LAB).
- `api_produce_mix_clean_clips`: `clean_compare_clips`를 쓰므로 변경 없음(확인만).

- [ ] **Step 4: 통과 확인**

Run: `py -m pytest shopping_shorts/tests/test_clean_base_app_paths.py shopping_shorts/tests/test_clean_compare_snapshot.py shopping_shorts/tests/test_clean_frame_prefers_sig_file.py shopping_shorts/tests/test_clean_fresh_by_signature.py shopping_shorts/tests/test_capcut_clean_clips.py -q`
Expected: 신규 3 passed, 기존 회귀 없음(스위치 기본 끔이므로 기존 테스트는 종전 경로를 탄다)

- [ ] **Step 5: 커밋**

```bash
git add shopping_shorts/app.py shopping_shorts/mix_pipeline.py shopping_shorts/tests/test_clean_base_app_paths.py
git commit -m "정본 경로: 프레임·전후비교·캡컷·ZIP이 render_inputs_for 로 같은 청소본을 본다"
```

---

### Task 7: 화면 — 최종렌더 전 안내 (재청소 없음 / 새 장면 N초만)

**Files:**
- Modify: `shopping_shorts/app.py` — 새 라우트 `GET /api/produce/mix/clean_base_preview/{job_id}` (`api_produce_mix_clean_clips` 아래)
- Modify: `shopping_shorts/static/produce.html` — 최종렌더 `fetch('/api/mix/render'` (15959행 부근) 직전
- Test: `shopping_shorts/tests/test_clean_base_preview_api.py`

**Interfaces:**
- Produces: `{"ok": true, "enabled": bool, "uncovered": [{"beat_idx", "seconds"}], "extend": [{"beat_idx","need"}], "est_credits": int|null}`

- [ ] **Step 1: 실패하는 테스트**

```python
# shopping_shorts/tests/test_clean_base_preview_api.py
# -*- coding: utf-8 -*-
from pathlib import Path
from shopping_shorts import app as A
from shopping_shorts import clean_base as cb


def test_preview_lists_uncovered_seconds(tmp_path, monkeypatch):
    plan = {"beats": [
        {"beat_idx": 0, "target_seconds": 2.0, "primary": {"video_id": "s0", "seg_id": "s0-0", "start": 1.0, "end": 3.0}, "alternates": []},
        {"beat_idx": 1, "target_seconds": 2.0, "primary": {"video_id": "s1", "seg_id": "s1-0", "start": 0.0, "end": 2.5}, "alternates": []}]}
    (tmp_path / "final_clean_x.mp4").write_bytes(b"c" * 4096)
    cb.save_base(tmp_path, sig="x", path=str(tmp_path / "final_clean_x.mp4"), plan=plan,
                 cuts=[{"video_id": "s0", "beat_idx": 0, "src": 1.0, "fin": 0.0, "dur": 2.0}])
    job = {"edit_plan": plan, "subtitle_removal": 1, "customer_id": 0, "clean_tier": "basic"}
    monkeypatch.setattr(A, "_MIX_WORK_DIR", tmp_path.parent)
    (tmp_path.parent / "jobx").mkdir(exist_ok=True)
    import shutil; shutil.copytree(tmp_path, tmp_path.parent / "jobx", dirs_exist_ok=True)
    monkeypatch.setattr(A, "Store", lambda db: type("S", (), {"get_mix_job": lambda self, j: job,
                                                             "get_setting": lambda self, k, d=None: "1"})())
    out = A.api_produce_mix_clean_base_preview("jobx")
    assert out["enabled"] is True
    assert out["uncovered"] == [{"beat_idx": 1, "seconds": 2.5}]
    assert out["est_credits"] == 6      # 기본 등급 1초 2크레딧 × 올림(2.5→3)
```

- [ ] **Step 2: 실패 확인**

Run: `py -m pytest shopping_shorts/tests/test_clean_base_preview_api.py -q`
Expected: FAIL — `no attribute 'api_produce_mix_clean_base_preview'`

- [ ] **Step 3: 구현**

```python
@app.get("/api/produce/mix/clean_base_preview/{job_id}")
def api_produce_mix_clean_base_preview(job_id: str):
    """최종렌더 전에 화면이 묻는다: 이번 렌더가 자막제거를 다시 타나? (2026-09-22)
    정본이 있으면 바뀐 장면·큰 늘림만 초수로 알려준다. 판정은 clean_base.remap_plan 하나."""
    from shopping_shorts import clean_base as _cb
    safe = os.path.basename(job_id)
    if not safe or safe != job_id:
        return {"ok": False, "enabled": False}
    store = Store(DB_PATH)
    job = store.get_mix_job(job_id)
    if not job or not job.get("edit_plan") or not job.get("subtitle_removal"):
        return {"ok": True, "enabled": False, "uncovered": [], "extend": [], "est_credits": None}
    cid = job.get("customer_id") or 0
    work = _MIX_WORK_DIR / job_id
    if not mix_pipeline.clean_base_on(store, cid):
        return {"ok": True, "enabled": False, "uncovered": [], "extend": [], "est_credits": None}
    base = _cb.load_base(work)
    if base is None:
        return {"ok": True, "enabled": True, "base": False, "uncovered": [], "extend": [], "est_credits": None}
    plan = job["edit_plan"]
    beats = {int(b["beat_idx"]): b for b in plan.get("beats") or []}
    _plan2, uncovered, extend = _cb.remap_plan(plan, base, tts_durs={
        int(b["beat_idx"]): float(b.get("target_seconds") or 0) for b in plan.get("beats") or []})
    unc = []
    for bi in uncovered:
        secs = sum(float(m["end"]) - float(m["start"]) for m in mix_pipeline._beat_materials(beats[bi]))
        unc.append({"beat_idx": bi, "seconds": round(secs, 2)})
    ext = [{"beat_idx": e["beat_idx"], "need": e["need"]} for e in extend]
    total = sum(u["seconds"] for u in unc) + sum(float(e["end"]) - float(e["start"]) for e in extend)
    est = mix_pipeline.clean_credit_estimate(total, tier=mix_pipeline.clean_tier_of(job)) if total > 0 else 0
    return {"ok": True, "enabled": True, "base": True, "uncovered": unc, "extend": ext, "est_credits": est}
```

`produce.html` 최종렌더 `fetch('/api/mix/render', ...)` 직전에:

```javascript
  // ★정본(2026-09-22): 스위치가 켜진 계정만. 바뀐 장면이 있을 때만 초수·크레딧을 알리고 확인받는다.
  try{
    const cbp=await (await fetch('/api/produce/mix/clean_base_preview/'+encodeURIComponent(MIX_JOB))).json();
    if(cbp && cbp.enabled && cbp.base){
      const n=(cbp.uncovered||[]).length + (cbp.extend||[]).length;
      if(n>0){
        const secs=(cbp.uncovered||[]).reduce((a,u)=>a+u.seconds,0)+(cbp.extend||[]).reduce((a,e)=>a+e.need,0);
        const msg='바뀐 장면 '+n+'개('+secs.toFixed(1)+'초)만 자막제거를 다시 합니다'+(cbp.est_credits!=null?(' — 약 '+cbp.est_credits+'크레딧'):'')+'.\n나머지 장면은 지운 결과를 그대로 씁니다(추가 과금 없음).\n\n계속할까요?';
        if(!confirm(msg)) return;
      }
    }
  }catch(_){}
```

- [ ] **Step 4: 통과 확인**

Run: `py -m pytest shopping_shorts/tests/test_clean_base_preview_api.py -q` → 1 passed.
브라우저: 로컬 서버(`py -m shopping_shorts.app` 또는 기존 실행법)에서 관리자 job의 5단계를 열고 최종렌더 버튼을 눌러 확인창이 뜨는지/안 뜨는지(정본·바뀐 장면 유무별) **눈으로** 본다. 콘솔 오류 0.

- [ ] **Step 5: 커밋**

```bash
git add shopping_shorts/app.py shopping_shorts/static/produce.html shopping_shorts/tests/test_clean_base_preview_api.py
git commit -m "최종렌더 전 안내: 정본이면 바뀐 장면 초수·크레딧만, 나머지 재과금 없음"
```

---

### Task 8: 실제 job 사본 LAB — 재과금 0·그림 일치·캡컷 경계 (스크립트로 저장)

**Files:**
- Create: `shopping_shorts/scripts/clean_base_lab.py`
- Test(수동 실행 결과를 핸드오프에 기록): `handoff/소스구간청소.md`

**Interfaces:**
- Consumes: 서버에서 가져온 job 폴더 사본(`shopping_shorts/data/mix_jobs/<job>/`, 관리자 본인 job만) + `reference.db`의 그 행(`sqlite3 .backup`으로 뜬 사본)
- Produces: 콘솔 리포트 — VMake 호출 수 / 컷별 프레임 phash 거리 / 캡컷 클립 경계 대조 / OCR 자막 검출 수

- [ ] **Step 1: 스크립트 작성**

```python
# shopping_shorts/scripts/clean_base_lab.py
# -*- coding: utf-8 -*-
"""청소본 정본 LAB — 실제 job 사본으로 "자막제거 1회 · 그림 일치 · 캡컷 경계"를 잰다 (2026-09-22).

VMake는 절대 안 부른다: _vmake_clean 을 'drawbox로 아래 1/4을 검게 칠한 사본'으로 바꾼다.
그러면 결과 영상에서 "지워진 곳"이 눈과 phash로 보인다.

실행(트랙 폴더):
  py shopping_shorts/scripts/clean_base_lab.py --job <job_id> --db shopping_shorts/data/lab.db \
     --edit lines --edit zoom --edit swap
  --edit lines : 첫 비트 자막을 3줄로   --edit zoom : 둘째 비트 확대 1.2   --edit swap : 셋째 비트를 다른 소스로
"""
import argparse, json, os, shutil, subprocess, sys
from pathlib import Path

ROOT = Path(__file__).resolve().parents[2]
sys.path.insert(0, str(ROOT))
os.environ.setdefault("SHORTS_CLEAN_FINAL", "1")

from shopping_shorts import mix_pipeline as mp                      # noqa: E402
from shopping_shorts import clean_base as cb                        # noqa: E402
from shopping_shorts.store import Store                             # noqa: E402

CALLS = []


def _fake_vmake(src, keys, out, tier=None):
    CALLS.append((Path(src).name, tier))
    subprocess.run(["ffmpeg", "-y", "-v", "error", "-i", str(src),
                    "-vf", "drawbox=x=0:y=ih*3/4:w=iw:h=ih/4:color=black@1:t=fill",
                    "-c:v", "libx264", "-preset", "veryfast", "-crf", "18", "-c:a", "copy", str(out)], check=True)
    return str(out)


def _phash(path, t):
    """t초 프레임의 8x8 평균 해시(외부 라이브러리 없이)."""
    raw = subprocess.run(["ffmpeg", "-v", "error", "-ss", f"{t:.3f}", "-i", str(path), "-frames:v", "1",
                          "-vf", "scale=8:8,format=gray", "-f", "rawvideo", "-"], capture_output=True, check=True).stdout
    px = list(raw[:64]); avg = sum(px) / max(1, len(px))
    return sum(1 << i for i, v in enumerate(px) if v > avg)


def _ham(a, b):
    return bin(a ^ b).count("1")


def main():
    ap = argparse.ArgumentParser()
    ap.add_argument("--job", required=True); ap.add_argument("--db", required=True)
    ap.add_argument("--edit", action="append", default=[])
    a = ap.parse_args()
    store = Store(a.db)
    store.set_setting("clean_base_enabled", "1")
    work_root = ROOT / "shopping_shorts" / "data" / "mix_jobs"
    work = work_root / a.job
    assert work.exists(), f"job 폴더 없음: {work}"
    for f in list(work.glob("final_clean_*")) + [work / cb.BASE_FILE]:
        if f.exists(): f.unlink()
    mp._vmake_clean = _fake_vmake
    mp._vmake_keys = lambda s, c=0: ["lab-key"]
    mp._charge_clean = lambda s, c, n: 0

    print("== 1) 4단계 청소"); mp.run_clean_sources(a.job, a.db, str(work_root))
    base = cb.load_base(work); assert base, "정본이 안 생겼다"
    print("   정본:", Path(base["path"]).name, "컷", len(base["cuts"]), "VMake 콜", len(CALLS))

    job = store.get_mix_job(a.job); plan = job["edit_plan"]; beats = plan["beats"]
    if "lines" in a.edit:
        beats[0]["caption_lines"] = [x for x in (beats[0].get("narration") or "가 나 다").split(" ")][:3] or ["가", "나", "다"]
    if "zoom" in a.edit and len(beats) > 1:
        beats[1]["scene_zoom"] = 1.2
    if "swap" in a.edit and len(beats) > 2:
        srcs = mp._resolve_sources(job, work); other = [v for v in srcs if v != (beats[2].get("primary") or {}).get("video_id")]
        if other:
            beats[2]["scene_override"] = [{"video_id": other[0], "seg_id": other[0] + "-lab", "start": 0.0, "end": 2.0}]
    store.update_mix_job(a.job, edit_plan=plan)

    n0 = len(CALLS)
    print("== 2) 최종렌더"); mp.run_render(a.job, a.db, str(work_root))
    job = store.get_mix_job(a.job)
    print("   VMake 콜(렌더 중):", len(CALLS) - n0, "→ 기대: swap 없으면 0, swap이면 1(증분)")
    final = Path(job["video_path"])

    print("== 3) 그림 일치(컷별 phash, 청소본 vs 완성본)")
    plan2, paths, base = mp.render_inputs_for(store, job, a.job, work, [], 0, allow_clean=False)
    tts = {b["beat_idx"]: b["tts_path"] for b in plan2["beats"] if b.get("tts_path")}
    durs = {v: mp._probe_duration(p) for v, p in paths.items()}
    bad = 0
    for c in mp.final_clip_pairs(plan2, tts, durs):
        if c["video_id"] != "clean":
            continue
        d = _ham(_phash(paths["clean"], c["src"] + c["dur"] / 2), _phash(final, c["fin"] + c["dur"] / 2))
        flag = "" if d <= 12 else "  ★불일치"
        if d > 12: bad += 1
        print(f"   beat{c['beat_idx']} clean@{c['src']:.2f} vs final@{c['fin']:.2f} 거리 {d}{flag}")
    print("   불일치 컷:", bad, "(확대 비트는 거리가 커도 정상 — 눈으로 확인)")

    print("== 4) 캡컷 경계")
    from shopping_shorts import app as A
    r = A.api_mix_capcut(a.job, base=str(work / "capcut_lab"))
    body = r.body if hasattr(r, "body") else r
    print("   응답 종류:", type(r).__name__, "| draft 클립 수는 work/capcut_lab 아래 draft_content.json 에서 센다")
    dc = list((work / "capcut").rglob("draft_content.json"))
    if dc:
        d = json.loads(dc[-1].read_text(encoding="utf-8"))
        segs = [s for t in d.get("tracks", []) if t.get("type") == "video" for s in t.get("segments", [])]
        print("   캡컷 비디오 세그먼트:", len(segs), "| 렌더 컷:", len(mp.final_clip_pairs(plan2, tts, durs)))
        for s in segs[:8]:
            tr = s.get("target_timerange", {}); print(f"     {tr.get('start',0)/1e6:.3f}~{(tr.get('start',0)+tr.get('duration',0))/1e6:.3f}")
    print("== 끝. VMake 총 콜:", len(CALLS), CALLS)


if __name__ == "__main__":
    main()
```

- [ ] **Step 2: 재료 준비(서버 → 로컬, 관리자 본인 job만)**

```bash
# 서버에서 관리자(cid 0) job 하나를 고른다(자막제거 켜진 것, 최근 것). 아래 <job>은 그 id.
ssh -i C:/Users/TheRose/crawling_bot_client/LightsailDefaultKey-ap-northeast-2.pem ubuntu@3.35.251.172 \
  "cd /home/ubuntu/lotto-stock-wiki && sqlite3 shopping_shorts/data/reference.db \".backup /tmp/lab.db\" && tar czf /tmp/lab_job.tgz -C shopping_shorts/data/mix_jobs <job>"
scp -i C:/Users/TheRose/crawling_bot_client/LightsailDefaultKey-ap-northeast-2.pem ubuntu@3.35.251.172:/tmp/lab.db shopping_shorts/data/lab.db
scp -i C:/Users/TheRose/crawling_bot_client/LightsailDefaultKey-ap-northeast-2.pem ubuntu@3.35.251.172:/tmp/lab_job.tgz .
tar xzf lab_job.tgz -C shopping_shorts/data/mix_jobs && rm lab_job.tgz
```
(job 폴더는 수백 MB일 수 있다 — 하나만 가져온다. `reference_서버AB테스트가_디스크를_채웠다` 참고.)

- [ ] **Step 3: 실행 — 세 편집을 각각·함께**

```bash
py shopping_shorts/scripts/clean_base_lab.py --job <job> --db shopping_shorts/data/lab.db --edit lines
py shopping_shorts/scripts/clean_base_lab.py --job <job> --db shopping_shorts/data/lab.db --edit zoom
py shopping_shorts/scripts/clean_base_lab.py --job <job> --db shopping_shorts/data/lab.db --edit lines --edit zoom --edit swap
```
Expected: 렌더 중 VMake 콜 `lines`=0, `zoom`=0, `swap` 포함=1(증분 조각 `cb2_0`). 불일치 컷 0(확대 비트 제외). 캡컷 세그먼트 수 = 렌더 컷 수, 경계 시각 ±0.034초(1프레임).

- [ ] **Step 4: 눈으로 확인**

완성본 `final.mp4`와 캡컷 초안 폴더를 열어: 아래 1/4 검은 띠가 **모든 컷**에 있는지(= 청소본에서 나왔다), 확대 비트가 확대돼 있는지, 줄 나눈 비트의 자막이 3줄로 갈리는지, swap 비트만 원본 화면(검은 띠 없음 → 증분 조각은 가짜 청소라 띠가 있어야 함: `cb2_0_clean` 결과에도 drawbox가 적용됐는지)를 본다. 캡컷 앱에서 초안을 열어 재생.

- [ ] **Step 5: 결과를 핸드오프에 적고 커밋**

`handoff/소스구간청소.md`에 표(편집 종류 / VMake 콜 / 불일치 컷 / 캡컷 세그먼트=렌더 컷 / 눈 확인)로 기록.

```bash
git add shopping_shorts/scripts/clean_base_lab.py handoff/소스구간청소.md
git commit -m "clean_base LAB: 실제 job 사본으로 재과금 0·그림 일치·캡컷 경계 실측 도구"
```

---

### Task 9: 기존 계약 테스트의 의미 갱신 + 전체 게이트

**Files:**
- Modify: `shopping_shorts/tests/test_clean_sig_caption_lines.py`, `shopping_shorts/tests/test_clean_once_per_video.py`(정본 경로 케이스 추가)

- [ ] **Step 1: 정본 경로 계약 테스트 추가** — `test_clean_once_per_video.py` 끝에:

```python
def test_clean_base_path_never_recleans_on_caption_lines(tmp_path, monkeypatch):
    """★정본(2026-09-22): 줄을 나눠도 렌더는 VMake를 안 탄다. 이 테스트가 뒤집히면 재과금이 돌아온다."""
    from shopping_shorts import mix_pipeline as mp
    from shopping_shorts import clean_base as cb
    plan = {"beats": [{"beat_idx": 0, "target_seconds": 2.0, "narration": "가 나 다",
                       "primary": {"video_id": "s0", "seg_id": "s0-0", "start": 1.0, "end": 3.0}, "alternates": []}]}
    job = {"edit_plan": plan, "urls": ["u"], "subtitle_removal": 1, "customer_id": 0, "clean_status": "ready"}
    (tmp_path / "s0").mkdir(); (tmp_path / "s0" / "v.mp4").write_bytes(b"v" * 4096)
    (tmp_path / "final_clean_a.mp4").write_bytes(b"c" * 4096)
    cb.save_base(tmp_path, sig="a", path=str(tmp_path / "final_clean_a.mp4"), plan=plan,
                 cuts=[{"video_id": "s0", "beat_idx": 0, "src": 1.0, "fin": 0.0, "dur": 2.0}])
    plan["beats"][0]["caption_lines"] = ["가", "나", "다"]
    monkeypatch.setattr(mp, "clean_base_on", lambda s, c: True)
    monkeypatch.setattr(mp, "_vmake_clean", lambda *a, **k: pytest.fail("재청소가 났다"))
    plan_used, paths, base = mp.render_inputs_for(object(), job, "j", tmp_path, ["k"], 0)
    assert base is not None and list(paths) == ["clean"]
```

- [ ] **Step 2: 전체 clean 테스트 + 트랙 게이트**

Run: `py -m pytest shopping_shorts/tests/test_clean_*.py shopping_shorts/tests/test_capcut_*.py shopping_shorts/tests/test_final_clean.py -q`
Expected: 실패 수가 트랙 시작 시점 기준선보다 늘지 않음. 신규 테스트 전부 passed.

- [ ] **Step 3: 커밋**

```bash
git add shopping_shorts/tests/test_clean_once_per_video.py
git commit -m "계약: 정본 경로에선 줄 나누기에 재청소 없음"
```

---

### Task 10: 핸드오프·로그, 병합(라이브는 스위치 끔), 관리자 실사용 확인

**Files:**
- Modify: `handoff/소스구간청소.md`, `wiki/log.d/소스구간청소.md`

- [ ] **Step 1: 핸드오프** — 무엇을·왜·실측 표·켜는 법(`관리자 설정 → clean_base_enabled = admin`)·되돌리는 법(`""`)·다음 할 일(관리자 job 1건 라이브 4→5→최종렌더→캡컷 직접 확인 → 사장님 승인 후 `1`).

- [ ] **Step 2: 로그 한 줄** — `wiki/log.d/소스구간청소.md`에 날짜·요지.

- [ ] **Step 3: 커밋 후 병합**

```bash
git add -A && git commit -m "docs: 소스구간청소 핸드오프·로그"
py tools/track.py finish 소스구간청소
```
스위치 기본 끔이므로 병합·배포돼도 고객 화면은 그대로다.

- [ ] **Step 4: 배포 뒤 관리자 실사용(0순위-A1)** — 서버 설정 `clean_base_enabled=admin`으로 켜고(사장님 승인 후), 관리자 job 1건: 4단계 청소 → 5단계에서 줄 나누기·확대 → 최종렌더 → 워커 로그에 `[clean-base] 정본 조립(VMake 0회)` 1줄, `청소 시작` 없음 → 캡컷 내보내기 열어 재생. 결과를 핸드오프에 적는다. 고객 전체(`1`)는 사장님이 따로 결정.

---

## Self-review (작성 후 점검)

- 스펙 §3.1 정본 저장 → Task 2 · §3.2 remap → Task 1/5 · §3.3 증분 → Task 4 · §3.4 늘림(느리게→정지→증분) → Task 1 `EXTEND_MIN`+`plan_beat_clips_for`의 기존 `_MAX_SLOWMO=1.15`/정지, Task 4 · §3.5 무효 조건(등급 변경) → `_clean_sig`가 등급 접미로 다른 파일을 만들고 `_final_clean_fn` 재사용 분기가 정본을 새로 쓴다(Task 2 두 번째 블록에서 `load_base is None`일 때만 저장하므로 **등급 변경 시 정본을 갱신하지 않는 구멍**이 있다 → Task 2 Step 3 재사용 분기의 조건을 `if _cb.load_base(work) is None or _cb.load_base(work)["sig"] != sig:`로 고친다) · §4 흐름 → Task 5/6 · §5 스위치 → Task 3 · §7 오류(정본 깨짐 → 종전 경로) → `load_base` None 반환 + `render_inputs_for` 폴백 · §8 검증 8항목 → Task 8(1~6), Task 9(7), Task 10(8).
- 자리표시자 없음. 함수명 일치: `save_base/load_base/add_extra/coverage/remap_plan/source_paths/time_in_clean/beat_material_key`(Task 1) · `clean_base_on`(3) · `_cut_piece/incremental_clean`(4) · `render_inputs_for`(5, 6, 7, 8, 9) · `api_produce_mix_clean_base_preview`(7).
- `_clean_joined` 반환 형태는 Task 4에서 본문 확인 후 맞춘다(명시).
