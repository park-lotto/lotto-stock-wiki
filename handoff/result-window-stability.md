# 결과창 유지 진단 — 2026-09-10

## 실측 원인과 구분

- Browser Use 런타임 `browser.documentation()`의 Tab Cleanup: 에이전트 생성 Chrome 탭은 턴 종료 시 정리 대상이다. `markDeliverable`은 턴 단위 표시이며 이후 턴에도 유지하려면 다시 표시해야 한다. 한 번 표시했다고 영구 탭이 되는 것은 아니다.
- 일반 사용자 탭을 `browser.user.openTabs()`의 실제 반환 객체로 claim하면, 표시하지 않은 탭은 세션 제어에서 해제되고 열린 상태로 남는다. 결과 전달 기본은 일반 Chrome 창으로 열고, 필요한 경우 그 사용자 탭을 claim하여 QA한다.
- 8899 서버 PID 19048은 18:25 기동 후 18:36에도 HTTP 200이었다. 8801/8802/8803 서버는 13:41/13:48/14:06 기동 후 18:36에도 살아 있었다. 과거 `yt-stable-launcher` 기록의 'PTY 서버 턴 종료'를 모든 프로세스에 일반화하면 안 된다. 이번 과거 탭 소실 순간의 로그는 없으므로 개별 사건의 확정 원인은 아니다.

## 구현

- `tools/open_static_result.ps1`: HTML 파일 확인 → 기존 서버 응답 SHA256이 지정 파일과 동일한지 확인 → 없으면 루프백 전용 숨김 분리 서버 기동 → 준비 확인 → 일반 Chrome `--new-window` 실행.
- 포트가 다른 내용을 제공하면 실패하고 기존 프로세스를 종료하지 않는다. 페이지/API/유료 생성은 수정하거나 호출하지 않는다.
- `tools/open_loop_comparison.cmd`: 사용자 더블클릭 진입점. 기존 AI Shorts의 비교 HTML을 포트 8899로 연다. 저장소 루트 코드는 변경하지 않았다.
- 새 서버가 필요한 경우 런처 프로세스와 분리된다. 이미 응답 중인 8899는 재사용했으며 강제 재기동하지 않았다. Windows 재부팅 후에는 실행기를 다시 실행해야 한다.

## 검증

- `py -m pytest tests/test_static_result_launcher.py -q`: 2 passed (6.67초). 한글/공백 경로, 부모 PowerShell 종료 후 실제 HTTP 응답, 재사용, 다른 내용 포트 차단, 기존 서버 보존, 잘못된 확장자 거부를 검증했다. 테스트가 만든 PID만 종료했다.
- 실제 compare.html을 실행기에 전달: `reused=true`, `browser_opened=true`. 일반 사용자 Chrome 탭 1926681829 생성. 두 영상 readyState=4, duration=4, error=null, 스크린샷에서 실제 프레임과 컨트롤 확인. 해당 순간 두 영상은 일시정지 상태였다.
- 앞서 부모가 claim한 사용자 탭 1926681765에서는 두 영상 paused=false로 실재생 및 스크린샷 확인.
- 파일 직접 열기 역시 일반 Chrome으로 실행되어 사용자 탭 1926681827의 제목/URL을 확인했으나, Browser Use 보안정책은 file:// claim을 차단했다. 우회하지 않았으며 그 파일창 재생은 검증하지 않았다.

## 남은 확인

- 부모 에이전트가 이 서브에이전트 완료 뒤 사용자 탭 1926681829의 존속을 새 목록으로 확인한다. 자동 생성 탭으로 다시 바꾸거나 mark하지 않는다.
- 실제 사용자 다음 턴 종료까지의 장기 생존은 아직 확인하지 않았다. 탭 영구 보장, Chrome 종료/재부팅 후 자동 재개는 주장하지 않는다.
- 트랙 커밋 후 부모의 후속 턴 QA가 남아 있으므로 아직 finish/배포하지 않는다. 원본 영상과 compare.html은 변경하지 않았다.
