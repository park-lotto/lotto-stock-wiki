# 러너 뼈대 (트랙 4) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: superpowers:executing-plans / subagent-driven-development. Steps use checkbox (`- [ ]`) syntax.

**Goal:** 기존 엔진(랭킹·대본생성·믹스)을 호출만 해서 S1→S2→S3를 자동 관통시키는 백엔드 러너(`auto_run.py`)를 만든다. 상태·원가·판정·재개를 `auto_jobs`에 영속하고, 사람 개입이 필요하면(`unsure`) 텔레그램으로 부른다.

**Architecture:** 러너는 **의존성 주입(DI)** 구조 — 단계 어댑터·심사기·알림기를 인자로 받아, 실제 엔진 없이 단위테스트한다. 기본 어댑터가 `service.collect`/`generate_variations`/`run_mix_job`+`run_render`를 감싼다. 기본 심사기는 트랙5까지 "무조건 pass"(자리만 확보). 화면 없음 — produce.html 무접촉.

**Tech Stack:** Python 3 / SQLite(store.py) / FastAPI(선택적 트리거 라우트) / requests(텔레 Bot API) / pytest + monkeypatch.

## Global Constraints

- **얇은 세로 슬라이스(스펙 §8 트랙4)**: S1~S3만. S4~S8·게이트(트랙5)·프론트는 범위 밖.
- **기존 엔진 무수정**: `run_mix_job`·`run_render`·`service.collect`·`generate_variations`는 **호출만**. 시그니처(조사 확정): `run_mix_job(job_id, db_path, work_root)` 동기 → status `ready_for_review`; `run_render(job_id, db_path, work_root)` 동기 → status `done`, `mix_jobs.video_path`. `service.collect(platform="instagram", limit_channels=None)` → 채점 item 리스트. `script_generate.generate_variations(structure, full_text, elem_modes, category_lookup, mode, my_topic, subject, n)` → `[{hook,script,applied}]`.
- **원가 미터 필수(스펙 §9.3)**: 러너는 처음부터 `cost_krw`를 누적. 상한 **1,000원**(스펙 결정4) 도달 시 즉시 `unsure`(재시도 금지).
- **동시 1 job(스펙 §9.1)**: MIX_JOB 전역상태 위험 → 러너 v1은 한 번에 한 auto_job만 돈다고 가정(강제는 트리거 계층 최소 가드).
- **텔레 알림은 fire-and-forget**: 키(`SHORTS_TELEGRAM_TOKEN`/`SHORTS_TELEGRAM_CHAT_ID`) 없으면 조용히 no-op(무키 폴백 = tts.py 관례). 알림 실패가 러너를 죽이면 안 된다.
- **멀티테넌시**: `auto_jobs.customer_id INTEGER NOT NULL DEFAULT 0`.
- **경로/커밋**: 편집은 `.tracks/러너뼈대/...` 절대경로. `git -C .tracks/러너뼈대`. 커밋 전 `status --porcelain`로 내 파일만 확인.

---

### Task 1: `auto_jobs` 테이블 + store CRUD

**Files:**
- Modify: `shopping_shorts/store.py` — `_init_schema()`에 `auto_jobs` CREATE 추가(pick_events 블록 뒤), `Store`에 CRUD 메서드
- Test: `tests/test_auto_jobs_store.py`

**Interfaces — Produces:**
- `Store.create_auto_job(job_id, *, mode="C", customer_id=LEGACY_CUSTOMER_ID) -> str` — 초기 status `running`, current_stage None, cost_krw 0. job_id 반환.
- `Store.get_auto_job(job_id) -> dict|None` — 키: `id, customer_id, mode, current_stage, status, stage_results(파싱 dict), cost_krw, mix_job_id, unsure_reason, created_at, updated_at`.
- `Store.update_auto_job(job_id, **fields)` — 화이트리스트 부분업데이트(안 넘긴 필드 보존, updated_at 자동). 허용 스칼라: `current_stage, status, cost_krw, mix_job_id, unsure_reason`. JSON 필드: `stage_results`.
- `Store.list_auto_jobs(*, customer_id=None, limit=50) -> list[dict]` — 최근(created_at DESC), state 무겁지 않게 stage_results 포함해도 됨(작음).

- [ ] **Step 1: 실패 테스트 작성** — `tests/test_auto_jobs_store.py`:

```python
"""러너 상태 저장소 — auto_jobs(스펙 §7, 트랙4). 상태·원가·재개 스냅샷을 영속."""
import pytest
from shopping_shorts.store import LEGACY_CUSTOMER_ID, Store


@pytest.fixture
def store(tmp_path):
    return Store(tmp_path / "t.db")


def test_create_defaults(store):
    jid = store.create_auto_job("J1")
    got = store.get_auto_job("J1")
    assert jid == "J1"
    assert got["status"] == "running"
    assert got["current_stage"] is None
    assert got["cost_krw"] == 0
    assert got["mode"] == "C"
    assert got["stage_results"] == {}


def test_partial_update_preserves(store):
    store.create_auto_job("J1")
    store.update_auto_job("J1", current_stage="S1", cost_krw=120.0)
    store.update_auto_job("J1", status="waiting_human")   # cost_krw 재전송 안 함
    got = store.get_auto_job("J1")
    assert got["status"] == "waiting_human"
    assert got["cost_krw"] == 120.0          # 보존
    assert got["current_stage"] == "S1"      # 보존


def test_stage_results_roundtrip(store):
    store.create_auto_job("J1")
    store.update_auto_job("J1", stage_results={"S1": {"output_ref": "abc", "score": 0.8}})
    assert store.get_auto_job("J1")["stage_results"]["S1"]["output_ref"] == "abc"


def test_mix_job_bridge_and_unsure_reason(store):
    store.create_auto_job("J1")
    store.update_auto_job("J1", mix_job_id="mix-9", unsure_reason="cost_cap")
    got = store.get_auto_job("J1")
    assert got["mix_job_id"] == "mix-9" and got["unsure_reason"] == "cost_cap"


def test_unknown_field_ignored_not_crash(store):
    store.create_auto_job("J1")
    store.update_auto_job("J1", bogus="x", status="done")   # 화이트리스트 밖은 무시
    assert store.get_auto_job("J1")["status"] == "done"


def test_list_recent_first_and_isolation(store):
    store.create_auto_job("J1", customer_id=LEGACY_CUSTOMER_ID)
    store.create_auto_job("J2", customer_id=7)
    store.create_auto_job("J3", customer_id=LEGACY_CUSTOMER_ID)
    rows = store.list_auto_jobs(customer_id=LEGACY_CUSTOMER_ID)
    assert [r["id"] for r in rows] == ["J3", "J1"]


def test_get_missing_none(store):
    assert store.get_auto_job("없음") is None


def test_migration_reopen(tmp_path):
    p = tmp_path / "old.db"
    Store(p)
    s2 = Store(p)
    s2.create_auto_job("J1")
    assert s2.get_auto_job("J1")["status"] == "running"
```

- [ ] **Step 2: 실패 확인** — `cd .tracks/러너뼈대 && python -m pytest shopping_shorts/tests/test_auto_jobs_store.py -q` → FAIL(create_auto_job 없음)

- [ ] **Step 3: 테이블 추가** — `store.py` `_init_schema()`의 pick_events CREATE/INDEX 뒤:

```python
            # 러너(2026-07-18, 트랙4) — 오케스트레이터 상태·원가·재개 스냅샷.
            c.execute("""
                CREATE TABLE IF NOT EXISTS auto_jobs (
                    id TEXT PRIMARY KEY,
                    customer_id INTEGER NOT NULL DEFAULT 0,
                    mode TEXT NOT NULL DEFAULT 'C',
                    current_stage TEXT,
                    status TEXT NOT NULL,
                    stage_results_json TEXT,
                    cost_krw REAL NOT NULL DEFAULT 0,
                    mix_job_id TEXT,
                    unsure_reason TEXT,
                    created_at TEXT NOT NULL,
                    updated_at TEXT NOT NULL
                )
            """)
            c.execute("CREATE INDEX IF NOT EXISTS idx_auto_jobs "
                      "ON auto_jobs(customer_id, created_at DESC)")
```

- [ ] **Step 4: CRUD 메서드 추가** — `Store`에(`create_mix_job` 근처):

```python
    _AUTO_SCALAR = ("current_stage", "status", "cost_krw", "mix_job_id", "unsure_reason")

    def create_auto_job(self, job_id, *, mode="C", customer_id=LEGACY_CUSTOMER_ID):
        now = datetime.now(timezone.utc).isoformat()
        with self._conn() as c:
            c.execute(
                "INSERT INTO auto_jobs(id, customer_id, mode, current_stage, status, "
                "stage_results_json, cost_krw, mix_job_id, unsure_reason, created_at, updated_at) "
                "VALUES(?,?,?,?,?,?,?,?,?,?,?)",
                (job_id, customer_id, mode, None, "running",
                 json.dumps({}), 0, None, None, now, now),
            )
        return job_id

    def get_auto_job(self, job_id):
        with self._conn() as c:
            r = c.execute(
                "SELECT id, customer_id, mode, current_stage, status, stage_results_json, "
                "cost_krw, mix_job_id, unsure_reason, created_at, updated_at "
                "FROM auto_jobs WHERE id=?", (job_id,)).fetchone()
        if not r:
            return None
        return {"id": r[0], "customer_id": r[1], "mode": r[2], "current_stage": r[3],
                "status": r[4], "stage_results": json.loads(r[5]) if r[5] else {},
                "cost_krw": r[6], "mix_job_id": r[7], "unsure_reason": r[8],
                "created_at": r[9], "updated_at": r[10]}

    def update_auto_job(self, job_id, **fields):
        sets, args = ["updated_at=?"], [datetime.now(timezone.utc).isoformat()]
        for k in self._AUTO_SCALAR:
            if k in fields:
                sets.append(f"{k}=?"); args.append(fields[k])
        if "stage_results" in fields:
            sets.append("stage_results_json=?")
            args.append(json.dumps(fields["stage_results"], ensure_ascii=False))
        args.append(job_id)
        with self._conn() as c:
            c.execute(f"UPDATE auto_jobs SET {', '.join(sets)} WHERE id=?", args)

    def list_auto_jobs(self, *, customer_id=None, limit=50):
        q = ("SELECT id, customer_id, mode, current_stage, status, stage_results_json, "
             "cost_krw, mix_job_id, unsure_reason, created_at, updated_at FROM auto_jobs")
        conds, args = [], []
        if customer_id is not None:
            conds.append("customer_id=?"); args.append(customer_id)
        if conds:
            q += " WHERE " + " AND ".join(conds)
        q += " ORDER BY created_at DESC, rowid DESC LIMIT ?"; args.append(limit)
        with self._conn() as c:
            rows = c.execute(q, args).fetchall()
        return [{"id": r[0], "customer_id": r[1], "mode": r[2], "current_stage": r[3],
                 "status": r[4], "stage_results": json.loads(r[5]) if r[5] else {},
                 "cost_krw": r[6], "mix_job_id": r[7], "unsure_reason": r[8],
                 "created_at": r[9], "updated_at": r[10]} for r in rows]
```

- [ ] **Step 5: 통과 확인** — `python -m pytest shopping_shorts/tests/test_auto_jobs_store.py -q` → PASS(8)
- [ ] **Step 6: Commit** — `git -C .tracks/러너뼈대 add shopping_shorts/store.py shopping_shorts/tests/test_auto_jobs_store.py && git -C .tracks/러너뼈대 commit -m "feat(러너): auto_jobs 테이블 + CRUD (트랙4 Task1)"`

---

### Task 2: 텔레그램 알림기 `notify.py`

**Files:**
- Create: `shopping_shorts/notify.py`
- Test: `tests/test_notify.py`

**Interfaces — Produces:**
- `notify.send_telegram(text, *, token=None, chat_id=None, _requests=None) -> bool` — token/chat_id 없으면 env(`SHORTS_TELEGRAM_TOKEN`/`SHORTS_TELEGRAM_CHAT_ID`)에서 읽음. 둘 중 하나라도 없으면 **no-op → False**(예외 없음). 있으면 Bot API `sendMessage` POST, 성공 True. 네트워크 예외는 삼켜 False(러너 보호). `_requests`는 테스트 주입구(기본 실 requests).

- [ ] **Step 1: 실패 테스트** — `tests/test_notify.py`:

```python
"""텔레 알림 — 러너의 unsure/실패 호출용(스펙 트랙4). 무키 폴백·예외삼킴."""
import shopping_shorts.notify as notify


class _FakeReq:
    def __init__(self, exc=None):
        self.calls = []
        self._exc = exc
    def post(self, url, json=None, timeout=None):
        self.calls.append({"url": url, "json": json})
        if self._exc:
            raise self._exc
        class R:  # noqa
            status_code = 200
        return R()


def test_no_keys_is_noop(monkeypatch):
    monkeypatch.delenv("SHORTS_TELEGRAM_TOKEN", raising=False)
    monkeypatch.delenv("SHORTS_TELEGRAM_CHAT_ID", raising=False)
    fr = _FakeReq()
    assert notify.send_telegram("hi", _requests=fr) is False
    assert fr.calls == []                       # 호출 자체 안 함


def test_sends_when_keys_present():
    fr = _FakeReq()
    ok = notify.send_telegram("작업 확인 필요", token="TOK", chat_id="42", _requests=fr)
    assert ok is True
    assert len(fr.calls) == 1
    assert "TOK" in fr.calls[0]["url"] and fr.calls[0]["url"].endswith("/sendMessage")
    assert fr.calls[0]["json"]["chat_id"] == "42"
    assert fr.calls[0]["json"]["text"] == "작업 확인 필요"


def test_network_error_swallowed():
    fr = _FakeReq(exc=RuntimeError("down"))
    assert notify.send_telegram("x", token="T", chat_id="1", _requests=fr) is False


def test_reads_env(monkeypatch):
    monkeypatch.setenv("SHORTS_TELEGRAM_TOKEN", "ENVTOK")
    monkeypatch.setenv("SHORTS_TELEGRAM_CHAT_ID", "99")
    fr = _FakeReq()
    assert notify.send_telegram("y", _requests=fr) is True
    assert "ENVTOK" in fr.calls[0]["url"] and fr.calls[0]["json"]["chat_id"] == "99"
```

- [ ] **Step 2: 실패 확인** — `python -m pytest shopping_shorts/tests/test_notify.py -q` → FAIL(모듈 없음)

- [ ] **Step 3: 구현** — `shopping_shorts/notify.py`:

```python
"""텔레그램 알림 — 러너(auto_run)가 unsure/실패 시 사장님을 부른다(스펙 트랙4).

★부가 채널이라 러너를 절대 죽이지 않는다: 키 없으면 no-op, 네트워크 예외는 삼킨다(tts.py 무키 폴백 관례).
봇 토큰/챗ID는 env(SHORTS_TELEGRAM_TOKEN/SHORTS_TELEGRAM_CHAT_ID). 프로젝트에 기존 텔레 인프라가
없어 여기서 새로 배선한다.
"""
import os


def send_telegram(text, *, token=None, chat_id=None, _requests=None):
    token = token or os.environ.get("SHORTS_TELEGRAM_TOKEN")
    chat_id = chat_id or os.environ.get("SHORTS_TELEGRAM_CHAT_ID")
    if not token or not chat_id:
        return False                     # 무키 폴백 — 조용히 넘어간다
    if _requests is None:
        import requests as _requests      # 실행시에만 임포트(테스트는 주입)
    try:
        r = _requests.post(
            f"https://api.telegram.org/bot{token}/sendMessage",
            json={"chat_id": chat_id, "text": text},
            timeout=10,
        )
        return getattr(r, "status_code", 0) == 200
    except Exception:
        return False                     # 알림 실패가 러너를 죽이면 안 된다
```

- [ ] **Step 4: 통과 확인** — `python -m pytest shopping_shorts/tests/test_notify.py -q` → PASS(4)
- [ ] **Step 5: Commit** — `git -C .tracks/러너뼈대 add shopping_shorts/notify.py shopping_shorts/tests/test_notify.py && git -C .tracks/러너뼈대 commit -m "feat(러너): 텔레그램 알림기 notify.py (트랙4 Task2)"`

---

### Task 3: 러너 코어 `auto_run.py` (DI 루프)

**Files:**
- Create: `shopping_shorts/auto_run.py`
- Test: `tests/test_auto_run_core.py`

**Interfaces — Produces:**
- `StageResult(output_ref="", metrics=dict, candidates=list)` dataclass. `metrics`에 `cost_krw`(없으면 0으로 취급).
- `Verdict(decision, reasons=list)` — decision ∈ `{"pass","unsure","fail"}`.
- `default_judge(stage_name, result, ctx) -> Verdict` — 항상 `pass`(트랙5가 교체).
- `STAGE_ORDER = ["S1", "S2", "S3"]`
- `run_auto_job(job_id, store, *, stages, judge=default_judge, notifier=None, cost_cap_krw=1000, max_retries=2) -> dict` — 최종 auto_job dict 반환.
  - `stages`: `{"S1": fn, "S2": fn, "S3": fn}`, 각 fn `(ctx) -> StageResult`. ctx = `{"job_id", "store", "results": {stage: StageResult}, "job": <auto_job dict>}`.
  - 재개: `job["current_stage"]`가 이미 있으면 그 **다음** 단계부터(끝난 단계 스킵). None이면 S1부터.
  - 루프: 각 단계 `running`+current_stage 기록 → run(재시도 최대 max_retries) → cost 누적·persist → **cost_cap 초과 시 즉시 unsure**(재시도 금지) → judge → pass=다음/ unsure=멈춤+알림/ fail=재시도. 재시도 소진 시 unsure로 승격.
  - unsure/실패 시 `status="waiting_human"`, `unsure_reason` 기록, `notifier`(있으면) 호출. 전 단계 통과 시 `status="done"`.

- [ ] **Step 1: 실패 테스트** — `tests/test_auto_run_core.py`:

```python
"""러너 코어 — DI 루프의 판정·원가·재개·알림(스펙 §4·§5, 트랙4).

실 엔진 없이 가짜 단계 함수로 오케스트레이션만 검증한다.
"""
import pytest
from shopping_shorts.store import Store
from shopping_shorts import auto_run
from shopping_shorts.auto_run import StageResult, Verdict, run_auto_job


@pytest.fixture
def store(tmp_path):
    return Store(tmp_path / "t.db")


def _stage(ref, cost=0.0):
    def fn(ctx):
        return StageResult(output_ref=ref, metrics={"cost_krw": cost})
    return fn


def _stages(**over):
    base = {"S1": _stage("reel1"), "S2": _stage("script1"), "S3": _stage("video1")}
    base.update(over)
    return base


def test_all_pass_reaches_done(store):
    store.create_auto_job("J1")
    out = run_auto_job("J1", store, stages=_stages())
    assert out["status"] == "done"
    assert out["current_stage"] == "S3"
    r = store.get_auto_job("J1")["stage_results"]
    assert r["S1"]["output_ref"] == "reel1" and r["S3"]["output_ref"] == "video1"


def test_unsure_stops_and_notifies(store):
    store.create_auto_job("J1")
    sent = []
    def judge(name, result, ctx):
        return Verdict("unsure", ["애매"]) if name == "S2" else Verdict("pass")
    out = run_auto_job("J1", store, stages=_stages(), judge=judge,
                       notifier=lambda text: sent.append(text))
    assert out["status"] == "waiting_human"
    assert out["current_stage"] == "S2"           # 멈춘 자리
    assert len(sent) == 1 and "S2" in sent[0]
    # S3는 실행되지 않았다
    assert "S3" not in store.get_auto_job("J1")["stage_results"]


def test_fail_retries_then_escalates_to_unsure(store):
    store.create_auto_job("J1")
    calls = {"n": 0}
    def s2(ctx):
        calls["n"] += 1
        return StageResult(output_ref=f"try{calls['n']}")
    def judge(name, result, ctx):
        return Verdict("fail", ["별로"]) if name == "S2" else Verdict("pass")
    out = run_auto_job("J1", store, stages=_stages(S2=s2), judge=judge, max_retries=2)
    assert calls["n"] == 3                          # 최초 + 재시도2
    assert out["status"] == "waiting_human"          # 소진 후 unsure 승격
    assert out["current_stage"] == "S2"


def test_cost_cap_forces_unsure_no_retry(store):
    store.create_auto_job("J1")
    out = run_auto_job("J1", store, stages=_stages(S1=_stage("r", cost=1500.0)),
                       cost_cap_krw=1000)
    assert out["status"] == "waiting_human"
    assert out["unsure_reason"] == "cost_cap"
    assert out["current_stage"] == "S1"
    assert out["cost_krw"] == 1500.0


def test_cost_accumulates_across_stages(store):
    store.create_auto_job("J1")
    out = run_auto_job("J1", store, stages=_stages(
        S1=_stage("a", 300), S2=_stage("b", 300), S3=_stage("c", 300)))
    assert out["status"] == "done"
    assert out["cost_krw"] == 900.0


def test_resume_skips_completed_stages(store):
    store.create_auto_job("J1")
    # S1 이미 통과한 상태로 만든다(재개 시뮬)
    store.update_auto_job("J1", current_stage="S1",
                          stage_results={"S1": {"output_ref": "reel1", "metrics": {}}})
    ran = []
    def track(name, ref):
        def fn(ctx):
            ran.append(name); return StageResult(output_ref=ref)
        return fn
    run_auto_job("J1", store, stages={"S1": track("S1", "x"),
                                       "S2": track("S2", "s"), "S3": track("S3", "v")})
    assert ran == ["S2", "S3"]                       # S1 재실행 안 함


def test_default_judge_is_pass():
    assert auto_run.default_judge("S1", StageResult(), {}).decision == "pass"


def test_notifier_none_does_not_crash(store):
    store.create_auto_job("J1")
    def judge(name, result, ctx):
        return Verdict("unsure") if name == "S1" else Verdict("pass")
    out = run_auto_job("J1", store, stages=_stages(), judge=judge, notifier=None)
    assert out["status"] == "waiting_human"          # 알림기 없어도 정상 멈춤
```

- [ ] **Step 2: 실패 확인** — `python -m pytest shopping_shorts/tests/test_auto_run_core.py -q` → FAIL(모듈 없음)

- [ ] **Step 3: 구현** — `shopping_shorts/auto_run.py`:

```python
"""원클릭 오케스트레이터 러너 — S1→S2→S3를 자동 관통(스펙 §3·§4·§5, 트랙4).

DI 구조: 단계 어댑터·심사기·알림기를 주입받아 실 엔진 없이 단위테스트한다.
기본 심사기는 무조건 pass(트랙5가 기계 게이트로 교체). 화면 없음 — produce.html 무접촉.
"""
from dataclasses import dataclass, field

STAGE_ORDER = ["S1", "S2", "S3"]


@dataclass
class StageResult:
    output_ref: str = ""
    metrics: dict = field(default_factory=dict)   # 'cost_krw' 포함(없으면 0)
    candidates: list = field(default_factory=list)


@dataclass
class Verdict:
    decision: str                                  # 'pass' | 'unsure' | 'fail'
    reasons: list = field(default_factory=list)


def default_judge(stage_name, result, ctx):
    """트랙5(기계 게이트)가 교체할 자리. 지금은 무조건 통과."""
    return Verdict("pass")


def _to_dict(result):
    return {"output_ref": result.output_ref, "metrics": result.metrics,
            "candidates": result.candidates}


def _halt(store, job_id, stage_name, reason, notifier, results, cost):
    store.update_auto_job(job_id, status="waiting_human", current_stage=stage_name,
                          unsure_reason=reason, stage_results=results, cost_krw=cost)
    if notifier:
        try:
            notifier(f"[러너] {stage_name} 확인 필요: {reason}")
        except Exception:
            pass                                   # 알림 실패가 러너를 죽이면 안 된다
    return store.get_auto_job(job_id)


def run_auto_job(job_id, store, *, stages, judge=default_judge, notifier=None,
                 cost_cap_krw=1000, max_retries=2):
    job = store.get_auto_job(job_id)
    results = dict(job.get("stage_results") or {})
    cost = job.get("cost_krw") or 0

    # 재개: current_stage가 이미 있으면 그 다음 단계부터
    done_stage = job.get("current_stage")
    start = STAGE_ORDER.index(done_stage) + 1 if done_stage in STAGE_ORDER else 0

    for stage_name in STAGE_ORDER[start:]:
        ctx = {"job_id": job_id, "store": store, "results": results,
               "job": store.get_auto_job(job_id)}
        store.update_auto_job(job_id, status="running", current_stage=stage_name)

        verdict = None
        for _attempt in range(max_retries + 1):
            result = stages[stage_name](ctx)
            cost += (result.metrics or {}).get("cost_krw", 0) or 0
            results[stage_name] = _to_dict(result)
            store.update_auto_job(job_id, cost_krw=cost, stage_results=results)

            if cost > cost_cap_krw:
                return _halt(store, job_id, stage_name, "cost_cap", notifier, results, cost)

            verdict = judge(stage_name, result, ctx)
            if verdict.decision == "pass":
                break
            if verdict.decision == "unsure":
                return _halt(store, job_id, stage_name,
                             "; ".join(verdict.reasons) or "unsure", notifier, results, cost)
            # fail → 재시도 루프 계속
        else:
            # 재시도 소진 → unsure로 승격
            return _halt(store, job_id, stage_name,
                         "재시도 소진: " + ("; ".join(verdict.reasons) if verdict else ""),
                         notifier, results, cost)

    store.update_auto_job(job_id, status="done", current_stage=STAGE_ORDER[-1],
                          stage_results=results, cost_krw=cost)
    return store.get_auto_job(job_id)
```

- [ ] **Step 4: 통과 확인** — `python -m pytest shopping_shorts/tests/test_auto_run_core.py -q` → PASS(8)
- [ ] **Step 5: Commit** — `git -C .tracks/러너뼈대 add shopping_shorts/auto_run.py shopping_shorts/tests/test_auto_run_core.py && git -C .tracks/러너뼈대 commit -m "feat(러너): auto_run 코어 DI 루프(판정·원가·재개·알림) (트랙4 Task3)"`

---

### Task 4: 기본 단계 어댑터 (실 엔진 래핑)

**Files:**
- Modify: `shopping_shorts/auto_run.py` — `default_stages()` 추가
- Test: `tests/test_auto_run_adapters.py`

**Interfaces — Produces:**
- `auto_run.default_stages(db_path, work_root, *, platform="instagram", target_seconds=20) -> dict` — `{"S1": s1_rank, "S2": s2_script, "S3": s3_mix}`. 각 어댑터가 실 엔진을 호출한다:
  - `s1_rank(ctx)`: `service.collect(platform)` → `max(items, key=score)`. `StageResult(output_ref=pick["shortcode"], metrics={"score":…, "cost_krw":APIFY_COST_KRW}, candidates=상위5)`. 결과의 pick을 `ctx["results"]` 통해 다음 단계가 읽도록 output에 넣는다(pick dict를 metrics["pick"]에 저장).
  - `s2_script(ctx)`: S1 pick의 구조/원문으로 `script_generate.generate_variations(...)` → 첫 초안. `StageResult(output_ref=draft["script"], metrics={"hook":…, "cost_krw":GEMINI_COST_KRW}, candidates=drafts)`.
  - `s3_mix(ctx)`: `store.create_mix_job(mix_id, urls, target_seconds, "free", given_script=script)` → `mix_pipeline.run_mix_job(mix_id, db_path, work_root)` → `run_render(...)`. `StageResult(output_ref=video_path, metrics={"mix_job_id":mix_id, "cost_krw":TTS_COST_KRW})`. `store.update_auto_job(job_id, mix_job_id=mix_id)`.
- 비용 상수(추정, 캘리브레이션 대상): `APIFY_COST_KRW≈1010`(회당 $0.755×약1340, 스펙 결정4의 1000원 상한과 비교되도록 실측 튜닝 필요 — 값은 상수로 두고 주석에 근거), `GEMINI_COST_KRW`, `TTS_COST_KRW`.

> ⚠️ **정직 표기(스펙 §9)**: S1 pick → S2 입력(원본 structure/full_text) 데이터 배선은 소스 구조분석(source_analysis)을 거쳐야 완전해진다. 이 태스크에선 어댑터가 **올바른 엔진을 올바른 인자로 부르는지**까지 monkeypatch로 잠그고, S1 pick에서 structure/full_text를 어떻게 뽑을지의 완전한 실배선은 라이브 grounding(Task5)에서 관측·보정한다. 비용 상수도 실측 캘리브레이션 대상(스펙 §6 주석: 근거 없는 상수 금지 → 주석에 추정 근거 명시).

- [ ] **Step 1: 실패 테스트** — `tests/test_auto_run_adapters.py`:

```python
"""기본 단계 어댑터 — 실 엔진을 올바른 인자로 부르는지(트랙4 Task4). 엔진은 monkeypatch."""
import pytest
from shopping_shorts import auto_run
from shopping_shorts.store import Store


@pytest.fixture
def store(tmp_path):
    return Store(tmp_path / "t.db")


def test_s1_picks_highest_score(monkeypatch, store, tmp_path):
    items = [{"shortcode": "a", "score": 0.3, "video_url": "u_a", "caption": "", "structure": {}, "full_text": "ta"},
             {"shortcode": "b", "score": 0.9, "video_url": "u_b", "caption": "", "structure": {}, "full_text": "tb"},
             {"shortcode": "c", "score": 0.5, "video_url": "u_c", "caption": "", "structure": {}, "full_text": "tc"}]
    monkeypatch.setattr(auto_run.service, "collect", lambda *a, **k: items)
    stages = auto_run.default_stages(tmp_path / "t.db", tmp_path / "work")
    ctx = {"job_id": "J1", "store": store, "results": {}, "job": {}}
    res = stages["S1"](ctx)
    assert res.output_ref == "b"                    # 최고 점수
    assert res.metrics["pick"]["shortcode"] == "b"
    assert res.metrics["cost_krw"] > 0
    assert len(res.candidates) <= 5


def test_s2_calls_generator_and_takes_first(monkeypatch, store, tmp_path):
    seen = {}
    def fake_gen(structure, full_text, *a, **k):
        seen["full_text"] = full_text
        return [{"hook": "훅1", "script": "대본1"}, {"hook": "훅2", "script": "대본2"}]
    monkeypatch.setattr(auto_run.script_generate, "generate_variations", fake_gen)
    stages = auto_run.default_stages(tmp_path / "t.db", tmp_path / "work")
    ctx = {"job_id": "J1", "store": store,
           "results": {"S1": {"metrics": {"pick": {"full_text": "원본대본", "structure": {}, "video_url": "u"}}}},
           "job": {}}
    res = stages["S2"](ctx)
    assert res.output_ref == "대본1"
    assert seen["full_text"] == "원본대본"
    assert len(res.candidates) == 2


def test_s3_creates_job_runs_mix_and_render(monkeypatch, store, tmp_path):
    calls = []
    monkeypatch.setattr(auto_run.mix_pipeline, "run_mix_job",
                        lambda jid, db, wr: calls.append(("mix", jid)))
    def fake_render(jid, db, wr):
        calls.append(("render", jid))
        store.update_mix_job(jid, status="done", video_path="/out/final.mp4")
    monkeypatch.setattr(auto_run.mix_pipeline, "run_render", fake_render)
    store.create_auto_job("J1")
    stages = auto_run.default_stages(store.db_path, tmp_path / "work")
    ctx = {"job_id": "J1", "store": store,
           "results": {"S1": {"metrics": {"pick": {"video_url": "u1"}}},
                       "S2": {"output_ref": "확정대본"}},
           "job": {}}
    res = stages["S3"](ctx)
    assert ("mix", res.metrics["mix_job_id"]) in calls
    assert ("render", res.metrics["mix_job_id"]) in calls
    assert res.output_ref == "/out/final.mp4"       # video_path
    assert store.get_auto_job("J1")["mix_job_id"] == res.metrics["mix_job_id"]
    # 실제 mix_jobs 행이 생겼다
    assert store.get_mix_job(res.metrics["mix_job_id"]) is not None
```

- [ ] **Step 2: 실 시그니처 확인(구현 전 게이트)** — 실제 함수명·인자를 grep으로 재확인(계획의 행번호는 이동했을 수 있음):
  `cd .tracks/러너뼈대 && grep -n "def collect\|def generate_variations\|def run_mix_job\|def run_render\|def create_mix_job\|def get_mix_job" shopping_shorts/service.py shopping_shorts/script_generate.py shopping_shorts/mix_pipeline.py shopping_shorts/store.py`
  → 인자 순서가 조사(run_mix_job(job_id, db_path, work_root) 등)와 다르면 어댑터를 실측에 맞춰라.

- [ ] **Step 3: 실패 확인** — `python -m pytest shopping_shorts/tests/test_auto_run_adapters.py -q` → FAIL(default_stages 없음)

- [ ] **Step 4: 구현** — `auto_run.py` 상단에 엔진 임포트 + 하단에 `default_stages`:

```python
import uuid
from shopping_shorts import service, script_generate, mix_pipeline

# 비용 상수(원, 추정 — 실측 캘리브레이션 대상). 근거:
#  APIFY: 인스타 수집 회당 약 $0.755(스펙 결정4) × 환율 ≈ 1010원
#  GEMINI/TTS: 초안 N개 + eleven_v3 나레이션. 실측 전 보수적 추정.
APIFY_COST_KRW = 1010
GEMINI_COST_KRW = 50
TTS_COST_KRW = 200


def default_stages(db_path, work_root, *, platform="instagram", target_seconds=20):
    def s1_rank(ctx):
        items = service.collect(platform)
        if not items:
            return StageResult(output_ref="", metrics={"cost_krw": APIFY_COST_KRW, "empty": True})
        pick = max(items, key=lambda i: i.get("score") or 0)
        top5 = sorted(items, key=lambda i: i.get("score") or 0, reverse=True)[:5]
        return StageResult(output_ref=pick.get("shortcode", ""),
                           metrics={"pick": pick, "score": pick.get("score"),
                                    "cost_krw": APIFY_COST_KRW},
                           candidates=[{"shortcode": i.get("shortcode"),
                                        "score": i.get("score")} for i in top5])

    def s2_script(ctx):
        pick = (ctx["results"].get("S1", {}).get("metrics", {}) or {}).get("pick", {}) or {}
        drafts = script_generate.generate_variations(
            pick.get("structure") or {}, pick.get("full_text") or "",
            {}, {}, mode="remake", my_topic="", subject="", n=3)
        if not drafts:
            return StageResult(output_ref="", metrics={"cost_krw": GEMINI_COST_KRW, "empty": True})
        first = drafts[0]
        return StageResult(output_ref=first.get("script", ""),
                           metrics={"hook": first.get("hook"), "cost_krw": GEMINI_COST_KRW},
                           candidates=drafts)

    def s3_mix(ctx):
        script = ctx["results"].get("S2", {}).get("output_ref", "") or ""
        pick = (ctx["results"].get("S1", {}).get("metrics", {}) or {}).get("pick", {}) or {}
        urls = [pick.get("video_url")] if pick.get("video_url") else []
        store = ctx["store"]
        mix_id = uuid.uuid4().hex[:12]
        store.create_mix_job(mix_id, urls, target_seconds, "free", given_script=script)
        mix_pipeline.run_mix_job(mix_id, str(db_path), str(work_root))
        mix_pipeline.run_render(mix_id, str(db_path), str(work_root))
        job = store.get_mix_job(mix_id) or {}
        store.update_auto_job(ctx["job_id"], mix_job_id=mix_id)
        return StageResult(output_ref=job.get("video_path") or "",
                           metrics={"mix_job_id": mix_id, "status": job.get("status"),
                                    "cost_krw": TTS_COST_KRW})

    return {"S1": s1_rank, "S2": s2_script, "S3": s3_mix}
```

> ⚠️ 어댑터 상단에 `db_path`가 store와 같은 DB를 가리키게 하라 — 테스트는 `store.db_path`를 넘긴다. uuid는 결정론 테스트에서 문제 없음(mix_job_id는 반환값으로 확인).

- [ ] **Step 5: 통과 확인** — `python -m pytest shopping_shorts/tests/test_auto_run_adapters.py -q` → PASS(3). ⚠️ `auto_run.service`/`.script_generate`/`.mix_pipeline`가 monkeypatch 대상이 되려면 모듈 최상위 `from shopping_shorts import service, ...` 필요(위 임포트가 그것).
- [ ] **Step 6: Commit** — `git -C .tracks/러너뼈대 add shopping_shorts/auto_run.py shopping_shorts/tests/test_auto_run_adapters.py && git -C .tracks/러너뼈대 commit -m "feat(러너): 기본 단계 어댑터 S1/S2/S3 실 엔진 래핑 (트랙4 Task4)"`

---

### Task 5: 트리거 라우트 + grounding + finish

**Files:**
- Modify: `shopping_shorts/app.py` — `POST /api/auto/start`, `GET /api/auto/{job_id}`
- Test: `tests/test_auto_api.py`

**Interfaces — Produces:**
- `POST /api/auto/start` → 새 auto_job 생성, `run_auto_job`을 `BackgroundTasks`로 예약(실 엔진 사용), `{"ok":True,"job_id":…}`.
- `GET /api/auto/{job_id}` → auto_job 상태 dict(없으면 404).

- [ ] **Step 1: 실패 테스트** — `tests/test_auto_api.py`:

```python
"""러너 트리거 라우트 — 생성·상태조회(트랙4 Task5). 러너 자체는 monkeypatch."""
import pytest
from fastapi.testclient import TestClient


@pytest.fixture
def client(tmp_path, monkeypatch):
    from shopping_shorts import app as app_mod
    monkeypatch.setattr(app_mod, "DB_PATH", tmp_path / "t.db")
    # 실 엔진이 안 돌게 러너를 가짜로
    monkeypatch.setattr(app_mod, "run_auto_job",
                        lambda job_id, store, **k: store.update_auto_job(job_id, status="done"),
                        raising=False)
    return TestClient(app_mod.app)


def test_start_creates_job(client):
    r = client.post("/api/auto/start", json={})
    assert r.status_code == 200 and r.json()["ok"] is True
    assert r.json()["job_id"]


def test_status_returns_job(client):
    jid = client.post("/api/auto/start", json={}).json()["job_id"]
    r = client.get(f"/api/auto/{jid}")
    assert r.status_code == 200
    assert r.json()["id"] == jid


def test_status_missing_404(client):
    assert client.get("/api/auto/없는id").status_code == 404
```

- [ ] **Step 2: 실패 확인** → FAIL(404)

- [ ] **Step 3: 구현** — `app.py`에 라우트 추가(`api_pick_log` 근처). 상단 임포트에 `from shopping_shorts.auto_run import run_auto_job, default_stages` 필요(없으면 추가). `_MIX_WORK_DIR` 재사용:

```python
@app.post("/api/auto/start")
def api_auto_start(request: Request, body: dict, background: BackgroundTasks):
    import uuid as _uuid
    store = Store(DB_PATH)
    job_id = _uuid.uuid4().hex[:12]
    store.create_auto_job(job_id, customer_id=_cid(request))
    stages = default_stages(DB_PATH, _MIX_WORK_DIR)
    background.add_task(run_auto_job, job_id, store, stages=stages)
    return {"ok": True, "job_id": job_id}


@app.get("/api/auto/{job_id}")
def api_auto_status(job_id: str):
    job = Store(DB_PATH).get_auto_job(job_id)
    if not job:
        return JSONResponse(status_code=404, content={"ok": False, "error": "없음"})
    return job
```

> ⚠️ `BackgroundTasks`가 app.py에 임포트돼 있는지 확인(`from fastapi import BackgroundTasks`). 없으면 기존 import에 추가. 테스트는 `run_auto_job`을 monkeypatch하므로 실 엔진 안 돎.

- [ ] **Step 4: 통과 확인** → PASS(3)
- [ ] **Step 5: 전체 신규 회귀** — `python -m pytest shopping_shorts/tests/test_auto_jobs_store.py shopping_shorts/tests/test_notify.py shopping_shorts/tests/test_auto_run_core.py shopping_shorts/tests/test_auto_run_adapters.py shopping_shorts/tests/test_auto_api.py -q` → 전부 PASS
- [ ] **Step 6: import 게이트** — `python -c "import shopping_shorts.app; print('OK')"`
- [ ] **Step 7: Commit** — `git -C .tracks/러너뼈대 add shopping_shorts/app.py shopping_shorts/tests/test_auto_api.py && git -C .tracks/러너뼈대 commit -m "feat(러너): /api/auto/start·status 트리거 라우트 (트랙4 Task5)"`
- [ ] **Step 8: finish** — `py tools/track.py finish 러너뼈대`. 게이트(문법→import→pytest) "실패 증가 없음"으로 판정. 통과 시 main 병합.

> **라이브 grounding 한계(정직 표기)**: 러너를 실제로 끝까지 돌리면 Apify($)·Gemini·ElevenLabs 실호출이라 이 트랙에선 미실행. 오케스트레이션(상태·원가·재개·판정·알림)은 단위테스트로 관측했고, 실 엔진 관통(S1 pick→S2 입력 데이터 배선·비용 상수 캘리브레이션)은 사장님이 키 갖춰 `/api/auto/start`를 눌러 첫 실행할 때 관측·보정한다. 트랙5(기계 게이트)가 default_judge를 실제 판정으로 교체.

## Self-Review

**1. Spec coverage (스펙 §8 트랙4 = auto_run.py + 어댑터 + auto_jobs + 텔레알림 + 재개, S1~S3):**
- auto_jobs → Task1 ✅ / 어댑터 → Task4 ✅ / auto_run 코어 → Task3 ✅ / 텔레알림 → Task2 ✅ / 재개 → Task3(test_resume) ✅ / 원가미터(§9.3) → Task3(cost_cap·accumulate) ✅ / 동시1job(§9.1) → Global Constraints(가정) ✅ / 트리거 → Task5 ✅
- 3판정(§5) → Task3(pass/unsure/fail재시도/cost_cap) ✅ / DI 계약(§4 StageResult) → Task3 ✅

**2. Placeholder scan:** 비용 상수는 "추정·캘리브레이션 대상"으로 주석에 근거 명시(근거 없는 상수 금지 원칙 준수). Task4·5의 "실측에 맞춰라" 게이트는 의도된 검증 단계. TBD/미완 코드 없음.

**3. Type consistency:** `StageResult`/`Verdict` 필드가 Task3 정의 = Task4 어댑터 반환 = 테스트에서 동일. `run_auto_job` 시그니처가 Task3 정의 = Task5 라우트 호출 = 테스트에서 동일. `default_stages(db_path, work_root, ...)` Task4 정의 = Task5 호출 동일. store 메서드(create/get/update/list_auto_job) Task1 정의 = Task3·4·5 사용 동일.

**주의(구현자):** Task4 Step2·Task5의 실 시그니처 확인 게이트를 반드시 먼저 돌려라 — 조사 시점 이후 코드가 이동/변경됐을 수 있다. monkeypatch가 먹으려면 `auto_run.py`가 `from shopping_shorts import service, script_generate, mix_pipeline`로 **모듈**을 잡아야 한다(함수 직접 import 금지).
