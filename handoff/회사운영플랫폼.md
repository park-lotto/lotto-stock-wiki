# 회사운영플랫폼 — 다음 모델 시작점

기준일: 2026-09-11. 이 문서는 이번 작업의 상태 원장이다. 오래된 회사운영규칙 핸드오프의 '트랙 생성 불가'를 현재 상태로 믿지 않는다.

## 대표님의 최신 지시

아스트라는 설계·합격 기준·중요 검수에 집중하고, 실행 모델이 구현한다. 실행 모델이 검수 요청을 올리고 수정·재검수를 반복한다. 대표에게 구현 선택이나 테스트를 떠넘기지 않고 검수된 최종 결과를 제공한다. 새 비용·외부 공개·고객 영향·추가 권한은 예외로 필요한 결정만 요청한다.

## 읽는 순서

1. 저장소 루트 CLAUDE.md와 AGENTS.md, 루트 wiki/rules/회사운영규칙.md 및 적용되는 wiki/rules 상세 규칙.
2. 이 문서. 실제 디스크와 git 상태를 재확인한다.
3. docs/superpowers/specs/2026-09-11-회사운영플랫폼-인수설계.md
4. docs/superpowers/plans/2026-09-11-회사운영플랫폼.md
5. docs/superpowers/specs/2026-09-11-회사운영플랫폼-검수절차.md

위 3~5는 이 트랙 기준 상대경로. 운영규칙은 루트 파일이 원본이다. 설계 문서는 회사 공통 규칙을 대체하지 않는다.

## 작업 공간 / 실행 확인

- 루트: C:/Users/CH/Desktop/로또의 주식
- 코드 작업: C:/Users/CH/Desktop/로또의 주식/.tracks/회사운영플랫폼
- 브랜치: track/회사운영플랫폼 (이전 확인). 새 작업 전에 git -C로 다시 확인.
- 트랙 생성 및 일반 파일 쓰기 확인 완료. 다른 작업 트랙이나 루트 코드 파일을 수정하지 않는다.
- 현재 세션은 .git/.agents/.codex 읽기 전용, approval never. Git 쓰기·권한 우회·타 프로세스를 통한 대행 금지. 정책이 다른 다음 세션은 자기 실제 정책을 확인한다.
- Python: C:/Users/CH/AppData/Local/Python/bin/python.exe. py 별칭은 이 환경에서 실패했음.
- FastAPI/uvicorn/httpx import 성공. Node v24.16.0 확인. 루트 package.json puppeteer 의존성 및 node_modules 존재 확인. Chromium 실행은 미검증.
- PowerShell에서 한글 문서는 Get-Content -Encoding UTF8로 읽는다. Python 도구에는 PYTHONIOENCODING=utf-8을 사용한다. 깨진 출력만 보고 파일 손상으로 단정하지 않는다.

## 현재 상태 — 구현 완료로 읽지 말 것

- 운영규칙과 계획 문서를 작성했다. 실행 가능한 통합 앱·백엔드·DB·테스트·실제 AI 연동은 아직 완료하지 않았다.
- UI 에이전트 /root/company_ui에게 company_ops/static/의 HTML/CSS/JS를 맡겼다. 이 문서 작성 시 구현 진행 중. 최종 파일/상태는 아래 후속 확인란으로 갱신한다.
- 앞선 '화면과 저장 기능 구현 중' 안내는 작업 착수 의미다. 백엔드 구현 파일·테스트 통과·브라우저 구동 증거는 아직 없다.
- Git 커밋·푸시·배포 안 함. localhost 서버 가동도 확인하지 않았다.
- 페이블의 답변은 루트 handoff/페이블_회사운영플랫폼_권한논의.md에 있다. 별도 Claude 세션과 직접 통신 연결은 확인되지 않았다. 그 문서에 적힌 과거 Git 성공을 현재 세션 권한으로 해석하지 않는다.

## 다음 실행 순서

1. git -C 상태와 company_ops/ 목록을 읽고 UI가 저장됐는지 확인. 기존 변경 보존.
2. 계획 T1부터 수행. UI 초안이 있으면 API 계약과 대조해 재사용하며 새로 디자인하지 않는다.
3. 자동 테스트만으로 완료하지 말고 실제 로컬 서버에서 브라우저 저장·단계·재시작·모바일을 검증한다.
4. 검수절차의 G1/G2/G3 요청서 작성 및 실제 상위 검수 요청. 한 번에 계획 전체를 다시 승인받지 않는다.
5. 승인받은 판본만 최종 결과로 전달. AI 자동 실행 미연결 사실과 미배포 사실은 숨기지 않는다.

## 다음 모델 시작 지시문

> 이 핸드오프 및 연결 설계·계획·검수절차를 읽고 기존 회사운영플랫폼 트랙에서 M1을 구현하라. 통상 구현 판단은 스스로 처리하고 대표에게 작업을 되묻지 마라. 테스트 실패부터 시작해 실제 브라우저와 DB 보존까지 검증하라. 중대한 설계 변경과 최종 판본은 상위 검수 요청서를 제출하라. 아스트라 호출 기능이 없으면 검수 대기로 기록하고 아스트라 승인이나 자율운영을 가장하지 마라. 현재 승인은 M1 로컬 구현이며 M2 이후 고객 서비스 연결·유료 실행·배포 승인이 아니다.

## 후속 확인란

- 인계 전 직접 확인: company_ops/static/index.html(6361 bytes), style.css(11450 bytes), app.js(10319 bytes) 존재. 주 에이전트도 node --check app.js를 실행해 rc=0 확인. 브라우저/API 검증은 미완료다. 새 세션에서 파일 변경 여부 재확인.
- 인계 문서 4종 디스크 존재 확인, git status에서 company_ops/와 문서들이 untracked인 것을 확인. git diff --check는 추적된 변경 검사일 뿐 새 파일의 실행/완전성 검증은 아니다.
- UI 알려진 문제: refresh() 진행 중 새 refresh 호출을 버려서 저장 직후 재조회가 주기 조회와 겹치면 최신 데이터 반영이 늦어질 수 있다. in-flight 뒤 trailing refresh를 보장하거나 요청 번호 기반 갱신으로 고치고 시험한다.
- 최신 대표 지시: AGENT HQ 참고 이미지의 조직 탐색은 유용하나 2D 게임 외형 거절. 훨씬 화려하고 생동감 있는 시네마틱 운영실. 인수설계 §3-A가 최신 디자인 기준이며 현재 UI 초안은 그 지시 이전 버전이다.
- UI 테스트 식별자: #company-makers/#company-hnl/#company-stock, [data-testid="team-new"], [data-testid="project-row"], [data-testid="empty-projects"], #new-project, #create-dialog, #project-title, #project-team, #project-owner, #project-description, #create-submit, #detail-dialog, #project-action, #action-note, #action-evidence, #action-reviewer, #action-submit, #global-error, #create-error, #action-error.

## 2026-09-11 M1 구현 결과

- company_ops 독립 FastAPI+SQLite 백엔드와 시네마틱 static UI 구현. 실행법은 company_ops/README.md.
- API 24 tests 최종 통과. 규격 검수 1회, 품질 검수 1회 반려 후 각각 보완·재검수 승인.
- 아스트라가 actual screenshots를 직접 검토하고 시각 디자인을 한 차례 자체 반려·강화한 뒤 G3 승인.
- 최종 browser.cjs: 실제 프로젝트 생성→설계→구현→검증→완료, 검증 차단, 새로고침 보존, 회사 격리, 모바일 overflow 0 통과.
- 상세 검수 판본·hash·증거: handoff/reviews/회사운영플랫폼-G2-G3-001.md.
- 로컬 테스트 서버는 127.0.0.1:8917, 시험 DB company_ops/.artifacts/e2e.sqlite3로 실행했다. 기본 운영 DB는 건드리지 않음.
- AI/숏템메이커/회계는 미연결. M2 read-only 실제 사건 연결 전에는 자동 운영 중이라고 표현하지 않는다.
- 현재 변경은 untracked/missing commit 상태이며 배포 안 됨. Git 쓰기가 허용되지 않은 세션 정책을 우회하지 않는다.
