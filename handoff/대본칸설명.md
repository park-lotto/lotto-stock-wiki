# 핸드오프 — 대본칸설명

- 갱신: 2026-08-22 (PC: 사무실)
- 트랙: `대본칸설명` / 브랜치 `track/대본칸설명`

---

## ★오늘의 결론 — 어제 진단이 틀렸다

사장님 제보(08-21): *"템플릿을 만들고 어거지로 내용을 끼워맞추는 느낌 / 씨앗영상과
대본스타일 차이로 어미가 틀어지고 / 문장과 문장의 연계가 없고 / 문장 내에서도 말이 안 된다.
많이 좋았는데 템플릿을 만들고 나서 안 좋아졌다."*

### 범인은 문장틀이 아니라 **조립**이었다 (실측으로 확정)

오늘 뽑힌 대본 18개를 전부 읽어 시각으로 갈랐다:

| 시각(KST) | 경로 | 결과 |
|---|---|---|
| 11:02~11:22 | **조립** | 깨짐. 틀 문장이 **글자 그대로** 박힘 |
| 13:11~14:47 | 생성기 | 멀쩡. 같은 뼈대인데 소재에 맞게 다시 씀 |

**같은 스타일(가족갈등 반전형)인데 오전은 깨지고 오후는 멀쩡하다** → 스타일·문장틀 탓이 아니다.

증거: 오전 대본의 `"저 친구 만났다가 진짜 충격 받았어요"`는 52번 스파인 틀의 1번 문장과
**완전히 동일**하다(캠핑 소재인데 그대로 박힘). 오후는 `"저 친구네 캠핑장 따라갔다가"`로 다시 썼다.

→ **조립은 어제(08-21 20:46) 이미 껐다**(`assemble_off=1`). 사장님이 보신 나쁜 대본은
   끄기 전에 뽑으신 것이다.

### 내가 오늘 헛짚은 것 (기록해 둔다)

1. "문장틀이 문제다" → **틀림.** 12개를 전부 비웠다가 전량 복구했다.
2. "빈칸이 서술구라 깨진다" → **틀림.** 오용형이 반례(빈칸 36개·조사와 물리는 비율 58%로
   가장 높은데 잘 나온다). 오후 인스타 대본도 반례.
3. **문장틀을 비우니 카드가 통째로 비었다** — `produce.html:10096`의 카드 첫 문장이
   `templates`를 읽는다(`s2Ex`). DB를 비우면 화면이 빈다. 사장님이 스크린샷으로 잡아주셨다.
   → ★DB에서 값을 지우기 전에 **그 값을 화면이 읽는지** 먼저 grep할 것.

---

## 오늘 고친 것 (커밋 35c718332)

### 칸 설명이 `zip()`으로 조용히 끊기던 것

`style_block`이 `dict(zip(roles, beat_chain))`으로 짝지었다. **zip은 짧은 쪽에서 조용히
끊긴다** — 오류도 경고도 없다.

라이브 실측(서버 43개 스파인):
- 어긋난 스파인 **10개**
- id=52 가족갈등 반전형: 칸 10 · 설명 5 → 뒤 5칸이 설명 없이 나감
- **6개는 설명이 0개** → 8칸 전부 role 이름만 나감(다이소 내부인형·사회증거형·정체의문형·무지후회형·금지경고형·나열추천형)

실제 피해(08-21 단정 명령형 스콘): cta 칸에 설명이 없어
`"다들 이 방법으로 편하게 만드시길 바라요"`로 끝나고 **CTA가 통째로 증발**했다.

**★CTA가 중간 칸으로 밀리던 것도 같은 뿌리다.** beat_chain의 마지막 원소가 CTA 문구인데
칸이 더 많으면 그게 중간 칸 몫이 된다. 실측(08-21 13:11): 5번 칸 `reveal`이
`"댓글에 '불꽃' 남겨주시면 좌표 드릴게요"`를 말하고 cta 칸에서 또 말했다.
→ CTA 문구는 **마지막 칸에 앵커**하고 나머지를 앞에서부터 채운다.

**0순위-B**: 같은 `zip()`이 `bank_assemble`·`script_generate` 두 군데 있었다 →
`bank_assemble.beat_descs()` 한 곳으로 공용화.

검증: 라이브 43개 스파인에 고친 코드를 그대로 태워 **설명 빈 칸 0개 · CTA 중간칸 0개**
확인(합성 테스트가 아니라 실데이터). 테스트 `test_beat_desc_pairing.py` 3건.

---

## ⏭ 다음에 할 일

### 1. 사장님 육안 확인 ← 여기부터
고친 뒤 대본을 뽑아 **뒤쪽 칸**(escalation·regret·benefit·witness)이 실해졌는지,
CTA가 마지막에서 한 번만 나오는지 본다. 특히 **단정 명령형**(스콘에서 CTA가 증발했던 스타일).

### 2. `beat_chain`을 실제로 채우는 것은 아직 안 했다
지금은 **역할어 기본 안내**(`_ROLE_FALLBACK` 40종)로 메꾼 것이다. 근본은 스파인마다
그 칸이 무슨 말을 해야 하는지를 `beat_chain`에 적는 것.
재료는 있다 — `pattern_source` **514건**에 히트작 실제 대사가 역할별로 분해돼 있고
`ending`에 실제 어미 목록(`~하더라고요`·`~거 있죠`·`~거든요`)까지 있다.

### 3. 어미(말투) 기준은 여전히 아무도 안 잡는다
`script_gate.py:172-175`가 틀 대조에서 **어미를 일부러 떼고** 본다("틀이 정하는 건 뼈대").
그래서 씨앗영상 말투와 대본 말투가 어긋나도 아무도 안 잡는다.
→ 사장님 제보의 "어미가 틀어진다"가 이것일 수 있다. **2번(pattern_source ending)과 같이 풀면 된다.**

---

## 🔴 지금 라이브 상태

```
settings.assemble_off = "1"    ← 조립 꺼짐(어제부터). 전부 생성기.
spine 52~63 templates          ← 전량 복구 완료(411문장). 비운 적 있었으나 되돌렸다.
```
- 문장틀 백업: `/home/ubuntu/spine_templates_backup_20260822_rollback.json` (31KB, 12개)
- 조립을 되돌리려면 DB에서 `assemble_off=0` (배포 불필요)

---

## 관련 파일
- 짝짓기: `shopping_shorts/bank_assemble.py` `beat_descs()` · `_ROLE_FALLBACK`
- 호출부 2곳: `bank_assemble.style_block` / `script_generate.py:526`
- 테스트: `shopping_shorts/tests/test_beat_desc_pairing.py`
- 카드가 문장틀을 읽는 곳: `shopping_shorts/static/produce.html` `s2Ex`(9862) · `10096`
- 조립 스위치: `shopping_shorts/app.py` `_assembled_drafts`(11910~)

---

# 2026-08-22 (이어서) — 25초 규격 + 배속 2.2

## 사장님 제보
"전체길이가 평균 25초가 제일 많이 하는 시간인데 우리껀 렌더해보면 30초 후반대야 모두."
"말이 빈다고 느끼는 게 제일 안 좋아."
"메종이 하는 게 제일 밀도있는 대본에 시간도 딱 맞아" / "채이홈·홈테리어픽·메종 3명"

## ★층을 갈라 재니 범인은 대본이 아니었다

렌더 영상 10편:
```
① 대본  221자 → 추정 26.9초   목표 30초보다 오히려 짧다   ← 죄 없음
② TTS   실제 음성 34.2초       +7.3초                     ← ★전부 여기
③ 렌더  최종 34.8초            음성 대비 +0.6초           ← 죄 없음
```
**대본 글자수를 줄이는 처방은 틀렸다.** 우리 TTS 6.46자/초는 히트작 하위10%(6.72)보다도
느려서, 같은 25초에 메종보다 49자 적게 말한다 = 사장님이 말한 "말이 빈다".

## 기준 채널 실측 (username 정확히)

| 채널 | username | 편 | 길이 | 글자 | 속도 |
|---|---|---|---|---|---|
| **메종 홈디노** | `maison_homedino` | 5 | **23.8초** | 193자 | **8.45자/초** |
| 채이홈 | `chae2home` | 7 | 29.5초 | 227자 | 7.98자/초 |
| 홈테리어픽 | `homterior_pick` | 0 | — | — | 전사+길이 둘 다 있는 편 0 |

히트작 전체 110편: 중앙 24.2초 · 7.63자/초 · 184자.
조인: `script_extracts` ⋈ `reel_durations` ⋈ `reel_history`.

## 고친 것 셋 (커밋 5b29fdb70)

1. 보이스 배속 `mix_pipeline._DEFAULT_VOICE.speed` 1.6 → **2.2**
2. 목표 길이 `target_seconds` 기본 30 → **25** (app.py 6곳 · produce.html 2곳)
3. 길이 추정 `edit_plan._speech_speed()` 1.44 → **1.54**

**3번을 빼면 안 된다** — 게이트 상한이 205자인데 25초엔 219자가 들어가 대본이 14자
짧게 잘린다 → 말이 비는 쪽으로 되돌아간다. 셋은 짝이다(0순위-B).

### ★내가 틀린 것: speed와 자/초는 비례하지 않는다
추정 상수에 배속 2.2를 그대로 넣었더니 5.7×2.2 = **12.54자/초**(실측의 1.4배).
실측에서 역산해야 한다: 8.75 ÷ 5.7 = **1.54**.
실측 곡선: 1.6→6.51 · 1.8→7.26 · 2.0→7.66 · 2.2→8.75.

## 검증 (렌더와 같은 경로로 실제 합성)
같은 대본 225자: speed 1.6 → **32.4초** / speed 2.2 → **24.0초**(목표 -1.0초).
글자수는 한 자도 안 줄였다. 샘플: 바탕화면 `배속샘플/확정_speed2.2_전문_24초.mp3`
전체 5,051 passed · 늘어난 실패 0(2건은 기준선과 동일한 beat_polish 흔들림).

## ⏭ 다음
1. **자막 싱크 검증** ← 여기부터. 음성이 31% 빨라졌으니 자막 타이밍을 실제 렌더로 확인.
   (`reference_자막드리프트_함정` 참조 — 부호 반대 오차가 상쇄되는 함정이 있다)
2. 사장님 육안·청취 확인 후 배속 미세조정(2.0~2.2)
3. 어제 기록된 밀도 297~327자/30초는 **환산 과정에서 부풀려진 값**이다(재측정 229자/30초).
   지금은 배속으로 길이를 맞췄으므로 밀도는 그대로 두었다 — 건드리면 말이 빈다.

## ⚠️ finish 함정 (오늘 두 번 밟았다)
- 트랙 브랜치가 origin에 push 안 된 상태로 finish하면 **"병합 완료" 메시지가 뜨는데
  실제로는 안 된다**(origin의 옛 커밋을 병합하므로). 반드시 `git branch -r --contains`로 대조.
- 재시도로 남은 finish 프로세스가 전역 락을 서로 붙들어 조용히 멈춘다.
  `Get-CimInstance Win32_Process | Where CommandLine -like '*track.py*'`로 먼저 확인.

---

# 2026-08-22 (3) — 길이(무음컷) + 유튜브 말투

## A. 길이: 배속이 아니라 **무음**이었다

★내가 두 번 오판했고 두 번 다 사장님이 잡았다.

**오판①** "배속 2.2로 메종 밀도를 맞춘다" → 청취 반려("말도 안 되게 빠르다").
숫자(메종 8.45자/초)는 맞지만 **배속으로 따라갈 값이 아니었다.** 같은 대본(메종 전사
191자)을 우리 TTS 1.6으로 읽히니 28.0초(사람 22.6초) — 발화속도가 24% 느리다.

**오판②** "무음컷은 1.2초가 최대" → 사장님 지적("이게 최대로 할수있는거라고?").
`stop_duration=0.12`에서 멈추고 단정했는데 그 값이 **20구간·2.38초를 그대로 남기고**
있었다. `silenceremove`는 무음을 삭제하는 게 아니라 **그 값까지 줄인다**.

**진짜 차이**: 사람(메종 원본)은 무음이 **0.00초**(임계 -30/-38/-45 전부 0구간),
우리는 2.54초/20구간. 메종이 24초에 되는 건 빨리 말해서가 아니라 **안 쉬어서**다.

무음컷 강도별 실측(같은 대본 191자·speed 1.6):
```
0.12 → 27.74초(잔여 2.38초/20구간)  ← 여기서 멈추면 "안 된다"로 오판
0.05 → 26.14초(0.11초/1구간)  ★채택 — 메종원본 26.31초와 일치
0.02 → 24.70초  ·  0.00 → 22.94초(사람 22.67초와 동급이나 숨이 없다)
```

고친 값: 임계 `-38dB→-30dB` · 상한 `0.12→0.05` · 배속 `2.2→1.6` ·
화면 SPEEDS 상한 `1.5→1.6` · 길이추정 상수 `→1.30`(=7.41자/초, 무음컷 포함).
결과: 대본 217자 **34.8초 → 29.3초**. 대본·배속 무변경.

⚠️ **화면 상한 함정**: 기본 배속 1.6인데 화면 최대가 1.5였다 = 실제 쓰이는 값을
고를 수 없고, 버튼을 한 번 누르면 1.5로 내려가 되돌릴 방법이 없었다.

## B. 유튜브 말투: 판정만 있고 지시가 없었다

사장님: "유튜브는 그냥 말투가 ~했음 이런 거 있잖아. 초반 후킹 말투로 끝까지. 존댓말 없음."

화면이 이미 `말끝(반말체)`·`CTA 금지` 경고를 띄우고 있었는데 아무도 안 고쳤다:
```
script_gate        검사 있음
프롬프트            ★반말 얘기가 아예 없음(유튜브 스파인 3개 voice 사전이 빔)
_STORY_RULES_CORE  ★"댓글에 'OO' 남겨주시면…" 쓰라고 시킴 — 시켜놓고 벌주기
```
→ `bank_assemble.genre_block` 신설. hook_3s면 반말체, no_cta면 CTA금지를 프롬프트에.
실측 A/B: 은폐형 존댓말 3종→**0종** · CTA 2종→**0종**. 훅 말투가 끝까지 유지된다.

## ⏭ 다음
1. **사장님 육안·청취 확인** ← 유튜브 스타일로 뽑아 ⚠️ 2개가 사라졌는지
2. 무음컷 0.05가 과하면 0.08로(값 하나)
3. 25초는 아직 못 맞춘다(29.3초). 더 줄이려면 배속↑ 또는 대본↓ — 둘 다 사장님이
   꺼린 방향이라 **30초를 규격으로 두는 것**도 선택지다.
