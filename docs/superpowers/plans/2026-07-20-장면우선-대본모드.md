# 장면 우선 대본 모드 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax.

**Goal:** 제작소에 같은 주제 영상 여러 개를 넣으면, scene_cut 장면 팔레트를 보며 기존 스토리 헌장으로 장면-매칭되는 탄탄한 대본 후보 3개를 만들고 최고안을 추천해 그대로 렌더한다.

**Architecture:** 신규 `build_scene_first_plan`(edit_plan.py)이 `_STORY_RULES_CORE`(script_generate) + scene_cut 팔레트 + 레퍼 구조를 결합해 후보 3개를 생성하고, 각 후보를 **기존 EDL 포맷**(build_edit_plan 반환형)으로 grounding한다. mix_pipeline이 scene_first 모드에서 이걸 호출해 추천 후보를 edit_plan으로 세팅 → 기존 render·respine·스왑UI 그대로. UI는 후보 선택만 추가.

**Tech Stack:** Python 3.14, pytest, google-genai(Gemini, key_vault), FastAPI + produce.html JS.

## Global Constraints

- **작업 폴더**: 모든 편집은 `.tracks/장면스파인/...` 절대경로. main 폴더 코드 금지(흡수 방지). git은 `git -C .tracks/장면스파인`. **git stash 금지**(공유스택 사고).
- **출력 EDL 포맷 호환**: 후보 plan은 `build_edit_plan` 반환형과 같아야 한다 — `{"structure":str, "beats":[{"beat_idx":int,"role":str,"narration":str,"target_seconds":float,"primary":{"video_id","seg_id","start","end","scene_desc"},"alternates":[동일dict...],"effect":"cut","fit":int,"forced":bool}], "detected_type":str, "affiliate_target":str, "plagiarism_flags":list}`. 하류 렌더·conform·produce.html이 이 형태를 읽는다.
- **환각 방지**: seg_id/start/end는 코드가 `_ground_ref`로 되붙인다. Gemini는 seg_id로만 지목.
- **다중컷**: 비트의 `seg_ids`는 2~4개. `seg_ids[0]`→primary, 나머지→alternates(연속재생).
- **핑퐁 루프 금지**: 후보 생성은 1 Gemini 콜. 검증 루프 없음(헌장이 품질 담당).
- **fail-open**: Gemini 실패(call None/비dict) 시 빈 candidates 반환(호출부가 기존 build_edit_plan로 폴백).
- **표절 회피**: 레퍼 원문 그대로 베끼지 않음 — 기존 `_plagiarism_flags` 재사용.
- **로컬 TTS 무음 주의**: 길이·렌더 검증은 서버(reference_local_tts_silent_mock_trap). 순수로직은 로컬 pytest.
- **소스 원칙(운영)**: 같은 주제 3~4개+, 이종 혼합 금물. 코드 제약 아님, 문서화만.

---

### Task 1: `_scene_first_candidates` — 헌장+팔레트 결합 생성 (Gemini)

`_STORY_RULES_CORE`+`_STORY_DECLARE`(script_generate에서 import) + 장면 팔레트 + 레퍼 구조를 프롬프트로 묶어 후보 3개를 생성한다. `call` 주입으로 테스트에서 Gemini mock.

**Files:**
- Modify: `.tracks/장면스파인/shopping_shorts/edit_plan.py` (import + `_SCENE_FIRST_SCHEMA` + `_scene_first_candidates`)
- Create: `.tracks/장면스파인/shopping_shorts/tests/test_scene_first_gen.py`

**Interfaces:**
- Consumes: `_build_inventory(source_scripts) -> (seg_map, inventory_text)` (기존 edit_plan.py), `_vault_call(prompt, schema)` (기존).
- Produces: `_scene_first_candidates(inventory_text, reference_text, target_seconds, n=3, call=_vault_call) -> list[dict]` — 각 dict = `{"hook","story_person","story_event","story_resolution","cta_line","cta_keyword","beats":[{"role","narration","seg_ids":[str],"fit":int,"forced":bool}]}`. call 실패(None/비dict) 시 `[]`.

- [ ] **Step 1: 실패 테스트**

`tests/test_scene_first_gen.py`:
```python
from shopping_shorts import edit_plan


def _fake_call(prompt, schema, **kw):
    # 프롬프트에 헌장·팔레트가 실렸는지 + 후보 형태 반환
    assert "스토리라인" in prompt and "[s0-0]" in prompt and "레퍼" in prompt
    return {"candidates": [{
        "hook": "양파 이렇게 두지 마세요",
        "story_person": "살림고수", "story_event": "썩는 양파 발견",
        "story_resolution": "통풍보관 터득", "cta_line": "댓글에 '보관법'", "cta_keyword": "보관법",
        "beats": [{"role": "훅", "narration": "곰팡이 핀 양파 보셨죠?", "seg_ids": ["s0-1", "s0-2"], "fit": 5, "forced": False}]}]}


def test_scene_first_candidates_shape():
    cands = edit_plan._scene_first_candidates("[s0-0] 화면:양파\n[s0-1] 화면:곰팡이\n[s0-2] 화면:단면",
                                              reference_text="원본대본 텍스트", target_seconds=20,
                                              n=3, call=_fake_call)
    assert len(cands) == 1
    assert cands[0]["cta_keyword"] == "보관법"
    assert cands[0]["beats"][0]["seg_ids"] == ["s0-1", "s0-2"]


def test_scene_first_candidates_fail_open():
    assert edit_plan._scene_first_candidates("[s0-0] 화면:양파", "ref", 20, call=lambda *a, **k: None) == []
```

- [ ] **Step 2: 실패 확인**

Run: `cd .tracks/장면스파인 && python -m pytest shopping_shorts/tests/test_scene_first_gen.py -v`
Expected: FAIL — `_scene_first_candidates` 미정의.

- [ ] **Step 3: 구현**

edit_plan.py 상단 import에 추가(순환 import 주의 — 함수 안에서 지연 import):
```python
# (파일 상단엔 넣지 않는다 — script_generate가 edit_plan을 import할 수 있어 순환 위험)
```
edit_plan.py에 추가:
```python
_SCENE_FIRST_SCHEMA = {
    "type": "object",
    "properties": {"candidates": {"type": "array", "minItems": 1, "items": {
        "type": "object",
        "properties": {
            "hook": {"type": "string"},
            "story_person": {"type": "string"}, "story_event": {"type": "string"},
            "story_resolution": {"type": "string"}, "cta_line": {"type": "string"},
            "cta_keyword": {"type": "string"},
            "beats": {"type": "array", "minItems": 4, "items": {
                "type": "object",
                "properties": {
                    "role": {"type": "string"}, "narration": {"type": "string"},
                    "seg_ids": {"type": "array", "minItems": 1, "items": {"type": "string"}},
                    "fit": {"type": "integer"}, "forced": {"type": "boolean"}},
                "required": ["role", "narration", "seg_ids", "fit"]}}},
        "required": ["hook", "beats"]}}},
    "required": ["candidates"],
}


def _scene_first_candidates(inventory_text, reference_text, target_seconds, n=3, call=_vault_call):
    """스토리 헌장 + 장면 팔레트 + 레퍼 구조 → 후보 n개. 각 비트는 seg_ids(2~4 다중컷)로
    장면을 지목한다. 실패 시 []. 헌장이 품질을 담당하므로 별도 검증루프 없음(1콜)."""
    from shopping_shorts import script_generate  # 지연 import(순환 방지)
    char_target = int(target_seconds * _SYLLABLES_PER_SEC)
    prompt = (
        "너는 한국 쇼핑 숏폼(살림·요리) 대본 작가다. 아래 '스토리 헌장'을 반드시 지켜 "
        f"탄탄한 대본 후보 {n}개를 만들어라. 단, 우리가 가진 장면으로만 말할 수 있게 쓰고, "
        "각 비트(문장)에 그 말과 어울리는 장면 seg_id를 2~4개 시간순으로 붙여라.\n\n"
        "[레퍼런스 — 훅·전개·설득구조만 계승(표절 금지), 다국어면 한국어로]\n"
        f"{(reference_text or '')[:1500]}\n\n"
        "[우리 장면 팔레트 — 이 seg_id 화면만 쓸 수 있다]\n"
        f"{inventory_text}\n\n"
        + script_generate._STORY_RULES_CORE + "\n" + script_generate._STORY_DECLARE + "\n"
        "- ★위 헌장(인과사슬·훅 한방·CTA 미끼·비법 킥 감추기)을 반드시 지켜라 — 장면에 맞추느라 "
        "스토리가 밋밋해지면 실패다. 스토리가 왕, 장면은 그 스토리를 보여줄 그림이다.\n"
        f"- 전체 나레이션 글자수 합은 약 {char_target}자 내외. 각 비트: role·narration(구어체)·"
        "seg_ids(2~4)·fit(1~5)·forced(그 장면이 이 말과 안 맞는데 억지로 붙였으면 true).\n"
        "- 화면에 없는 걸 말하지 마라. 같은 seg_id를 여러 비트에서 재사용 금지.\n"
        "출력은 스키마 JSON만.")
    raw = call(prompt, _SCENE_FIRST_SCHEMA)
    if not raw or not isinstance(raw, dict):
        return []
    return raw.get("candidates", []) or []
```

- [ ] **Step 4: 통과 확인**

Run: `cd .tracks/장면스파인 && python -m pytest shopping_shorts/tests/test_scene_first_gen.py -v`
Expected: 2 PASS.

- [ ] **Step 5: 커밋**

```bash
git -C .tracks/장면스파인 add shopping_shorts/edit_plan.py shopping_shorts/tests/test_scene_first_gen.py
git -C .tracks/장면스파인 commit -m "feat(edit_plan): 장면우선 후보생성 _scene_first_candidates(헌장+팔레트)"
```

---

### Task 2: `_ground_candidate` — 후보를 기존 EDL 포맷으로 grounding (다중컷)

후보의 비트(narration + seg_ids[])를 `build_edit_plan` 반환형 plan으로 변환한다. `seg_ids[0]`→primary, 나머지→alternates(전부 `_ground_ref`로 실 타임코드), target_seconds는 narration 글자수로 재계산.

**Files:**
- Modify: `.tracks/장면스파인/shopping_shorts/edit_plan.py` (`_ground_candidate`)
- Modify: `.tracks/장면스파인/shopping_shorts/tests/test_scene_first_gen.py`

**Interfaces:**
- Consumes: Task 1 후보 dict, `_ground_ref(ref, seg_map)` (기존, scene_desc 포함 반환), `_SYLLABLES_PER_SEC`.
- Produces: `_ground_candidate(cand, seg_map, structure="free") -> dict|None` — EDL-shape plan(위 Global Constraints 형태) + `story`·`affiliate_target` 없음(호출부 Task4가 story 부착). primary grounding 실패한 비트는 드롭. beats 0개면 None.

- [ ] **Step 1: 실패 테스트 추가**

`tests/test_scene_first_gen.py`에 추가:
```python
def _seg_map():
    def s(sid, st, en, sc):
        return {"video_id": sid.split("-")[0], "seg_id": sid, "start": st, "end": en,
                "text": "", "scene_desc": sc}
    return {x["seg_id"]: x for x in [
        s("s0-1", 1.0, 2.0, "곰팡이"), s("s0-2", 2.0, 3.5, "단면"), s("s0-3", 4.0, 6.0, "통풍")]}


def test_ground_candidate_multicut_to_edl():
    cand = {"beats": [
        {"role": "훅", "narration": "곰팡이 핀 양파 보셨죠?", "seg_ids": ["s0-1", "s0-2"], "fit": 5, "forced": False},
        {"role": "해결", "narration": "통풍이 핵심이에요 진짜", "seg_ids": ["s0-3"], "fit": 4, "forced": True}]}
    plan = edit_plan._ground_candidate(cand, _seg_map())
    b0 = plan["beats"][0]
    assert b0["primary"]["seg_id"] == "s0-1"                      # 첫 seg=primary
    assert [a["seg_id"] for a in b0["alternates"]] == ["s0-2"]     # 나머지=alternates(다중컷)
    assert b0["primary"]["scene_desc"] == "곰팡이"                 # scene_desc 실림
    assert plan["beats"][1]["forced"] is True and plan["beats"][1]["fit"] == 4
    assert b0["target_seconds"] > 0                                # 글자수 기준 재계산됨


def test_ground_candidate_drops_invalid_primary():
    cand = {"beats": [{"role": "x", "narration": "n", "seg_ids": ["없는id"], "fit": 3, "forced": False}]}
    assert edit_plan._ground_candidate(cand, _seg_map()) is None   # 유효 비트 0개 → None
```

- [ ] **Step 2: 실패 확인**

Run: `cd .tracks/장면스파인 && python -m pytest shopping_shorts/tests/test_scene_first_gen.py -k ground_candidate -v`
Expected: FAIL — `_ground_candidate` 미정의.

- [ ] **Step 3: 구현**

edit_plan.py에 추가:
```python
def _ground_candidate(cand, seg_map, structure="free"):
    """후보 비트(narration + seg_ids 다중컷)를 build_edit_plan 반환형 EDL로 grounding.
    seg_ids[0]=primary, 나머지=alternates(연속재생). start/end/scene_desc는 코드가 되붙인다.
    primary 무효 비트는 드롭. 유효 비트 0개면 None."""
    beats_out = []
    for beat in cand.get("beats", []):
        segs = beat.get("seg_ids") or []
        primary = _ground_ref({"seg_id": segs[0]}, seg_map) if segs else None
        if primary is None:
            continue
        alts, seen = [], {primary["seg_id"]}
        for sid in segs[1:]:
            g = _ground_ref({"seg_id": sid}, seg_map)
            if g and g["seg_id"] not in seen:
                alts.append(g)
                seen.add(g["seg_id"])
        narration = beat.get("narration", "")
        beats_out.append({
            "beat_idx": len(beats_out), "role": beat.get("role", ""),
            "narration": narration,
            "target_seconds": round(max(1.5, len(narration.strip()) / _SYLLABLES_PER_SEC), 1),
            "primary": primary, "alternates": alts, "effect": "cut",
            "fit": int(beat.get("fit") or 0), "forced": bool(beat.get("forced", False)),
        })
    if not beats_out:
        return None
    return {"structure": structure, "beats": beats_out}
```

- [ ] **Step 4: 통과 확인**

Run: `cd .tracks/장면스파인 && python -m pytest shopping_shorts/tests/test_scene_first_gen.py -k ground_candidate -v`
Expected: 2 PASS.

- [ ] **Step 5: 커밋**

```bash
git -C .tracks/장면스파인 add shopping_shorts/edit_plan.py shopping_shorts/tests/test_scene_first_gen.py
git -C .tracks/장면스파인 commit -m "feat(edit_plan): _ground_candidate — 다중컷 후보를 EDL포맷으로 grounding"
```

---

### Task 3: `_score_candidate` — 채점 + 추천 선정

후보 plan을 점수화해 최고안을 고른다. `score = 0.5*avg(fit)/5 + 0.3*(1-forced비율) + 0.2*장면다양성`.

**Files:**
- Modify: `.tracks/장면스파인/shopping_shorts/edit_plan.py` (`_score_candidate`)
- Modify: `.tracks/장면스파인/shopping_shorts/tests/test_scene_first_gen.py`

**Interfaces:**
- Produces: `_score_candidate(plan) -> float` — 0~1. 빈 beats면 0.0.

- [ ] **Step 1: 실패 테스트 추가**

```python
def test_score_prefers_high_fit_low_forced():
    good = {"beats": [{"fit": 5, "forced": False, "primary": {"seg_id": "a"}, "alternates": [{"seg_id": "b"}]},
                      {"fit": 5, "forced": False, "primary": {"seg_id": "c"}, "alternates": []}]}
    bad = {"beats": [{"fit": 2, "forced": True, "primary": {"seg_id": "a"}, "alternates": []},
                     {"fit": 2, "forced": True, "primary": {"seg_id": "a"}, "alternates": []}]}
    assert edit_plan._score_candidate(good) > edit_plan._score_candidate(bad)
    assert edit_plan._score_candidate({"beats": []}) == 0.0
```

- [ ] **Step 2: 실패 확인**

Run: `cd .tracks/장면스파인 && python -m pytest shopping_shorts/tests/test_scene_first_gen.py -k score -v`
Expected: FAIL.

- [ ] **Step 3: 구현**

```python
def _score_candidate(plan):
    """후보 추천 점수(0~1): 매칭 fit·억지없음·장면다양성. 빈 beats면 0.0."""
    beats = plan.get("beats") or []
    if not beats:
        return 0.0
    avg_fit = sum(int(b.get("fit") or 0) for b in beats) / len(beats) / 5.0
    forced_ratio = sum(1 for b in beats if b.get("forced")) / len(beats)
    seg_ids = []
    for b in beats:
        seg_ids.append((b.get("primary") or {}).get("seg_id"))
        seg_ids += [(a or {}).get("seg_id") for a in (b.get("alternates") or [])]
    seg_ids = [s for s in seg_ids if s]
    diversity = (len(set(seg_ids)) / len(seg_ids)) if seg_ids else 0.0
    return round(0.5 * avg_fit + 0.3 * (1 - forced_ratio) + 0.2 * diversity, 3)
```

- [ ] **Step 4: 통과 확인**

Run: `cd .tracks/장면스파인 && python -m pytest shopping_shorts/tests/test_scene_first_gen.py -k score -v`
Expected: PASS.

- [ ] **Step 5: 커밋**

```bash
git -C .tracks/장면스파인 add shopping_shorts/edit_plan.py shopping_shorts/tests/test_scene_first_gen.py
git -C .tracks/장면스파인 commit -m "feat(edit_plan): _score_candidate — fit·억지·다양성 추천점수"
```

---

### Task 4: `build_scene_first_plan` — 오케스트레이션

Task 1~3을 묶어 후보 3개를 생성·grounding·채점하고 최고안에 recommended 표시. 반환은 mix_pipeline이 쓸 형태.

**Files:**
- Modify: `.tracks/장면스파인/shopping_shorts/edit_plan.py` (`build_scene_first_plan`)
- Modify: `.tracks/장면스파인/shopping_shorts/tests/test_scene_first_gen.py`

**Interfaces:**
- Consumes: `_build_inventory`, `_scene_first_candidates`, `_ground_candidate`, `_score_candidate`, `detect_video_type`(기존), `_plagiarism_flags`(기존).
- Produces: `build_scene_first_plan(source_scripts, reference_text, target_seconds, n_candidates=3, video_type=None, call=None) -> dict` — `{"candidates":[{"plan":EDL, "story":{...}, "score":float, "recommended":bool}], "detected_type":str}`. 후보 0개면 `{"candidates":[], "detected_type":...}`.

- [ ] **Step 1: 실패 테스트 추가**

```python
def test_build_scene_first_plan_recommends_best(monkeypatch):
    src = [{"video_id": "s0", "full_text": "원본", "segments": [
        {"seg_id": "s0-1", "start": 1.0, "end": 2.0, "text": "", "scene_desc": "곰팡이"},
        {"seg_id": "s0-2", "start": 2.0, "end": 3.0, "text": "", "scene_desc": "단면"},
        {"seg_id": "s0-3", "start": 3.0, "end": 5.0, "text": "", "scene_desc": "통풍"}]}]

    def fake_call(prompt, schema, **kw):
        return {"candidates": [
            {"hook": "A", "cta_keyword": "k", "beats": [
                {"role": "훅", "narration": "곰팡이 보셨죠", "seg_ids": ["s0-1", "s0-2"], "fit": 5, "forced": False},
                {"role": "cta", "narration": "댓글 남겨요", "seg_ids": ["s0-3"], "fit": 5, "forced": False}]},
            {"hook": "B", "cta_keyword": "k", "beats": [
                {"role": "훅", "narration": "음", "seg_ids": ["s0-1"], "fit": 2, "forced": True},
                {"role": "cta", "narration": "음", "seg_ids": ["s0-1"], "fit": 2, "forced": True}]}]}

    out = edit_plan.build_scene_first_plan(src, "원본대본", 20, n_candidates=2, call=fake_call)
    assert len(out["candidates"]) == 2
    rec = [c for c in out["candidates"] if c["recommended"]]
    assert len(rec) == 1 and rec[0]["story"]["hook"] == "A"        # 고fit 후보 추천
    assert rec[0]["plan"]["beats"][0]["primary"]["seg_id"] == "s0-1"
```

- [ ] **Step 2: 실패 확인**

Run: `cd .tracks/장면스파인 && python -m pytest shopping_shorts/tests/test_scene_first_gen.py -k build_scene_first -v`
Expected: FAIL.

- [ ] **Step 3: 구현**

```python
def build_scene_first_plan(source_scripts, reference_text, target_seconds,
                           n_candidates=3, video_type=None, call=None):
    """장면 우선 대본 모드: 팔레트+헌장으로 후보 n개 생성 → 각 EDL grounding·채점 →
    최고 score에 recommended=True. 각 candidate.plan은 build_edit_plan 반환형(하류 렌더 호환).
    후보 0개면 candidates=[](호출부가 기존 build_edit_plan로 폴백)."""
    seg_map, inventory = _build_inventory(source_scripts)
    detected = video_type or (detect_video_type(source_scripts) if source_scripts else _DEFAULT_TYPE)
    if not seg_map:
        return {"candidates": [], "detected_type": detected}
    _call = call or _vault_call
    raws = _scene_first_candidates(inventory, reference_text, target_seconds, n=n_candidates, call=_call)
    src_texts = [s.get("full_text", "") for s in source_scripts]
    cands = []
    for r in raws:
        plan = _ground_candidate(r, seg_map)
        if plan is None:
            continue
        plan["detected_type"] = detected
        plan["affiliate_target"] = r.get("story_event", "") or ""
        plan["plagiarism_flags"] = _plagiarism_flags(plan["beats"], src_texts)
        story = {k: r.get(k, "") for k in
                 ("hook", "story_person", "story_event", "story_resolution", "cta_line", "cta_keyword")}
        cands.append({"plan": plan, "story": story, "score": _score_candidate(plan), "recommended": False})
    if cands:
        best = max(range(len(cands)), key=lambda i: cands[i]["score"])
        cands[best]["recommended"] = True
    return {"candidates": cands, "detected_type": detected}
```

- [ ] **Step 4: 통과 확인 + 회귀**

Run: `cd .tracks/장면스파인 && python -m pytest shopping_shorts/tests/test_scene_first_gen.py -v`
Expected: 전부 PASS.

- [ ] **Step 5: 커밋**

```bash
git -C .tracks/장면스파인 add shopping_shorts/edit_plan.py shopping_shorts/tests/test_scene_first_gen.py
git -C .tracks/장면스파인 commit -m "feat(edit_plan): build_scene_first_plan — 후보3개 생성·grounding·추천"
```

---

### Task 5: scene_desc 라벨 정확도 보강

스파이크서 s0-8이 "참외"로 오탐. `script_extract._PROMPT`의 scene_desc 지시를 강화해 주 대상을 정확히 적게 한다.

**Files:**
- Modify: `.tracks/장면스파인/shopping_shorts/script_extract.py:67` (scene_desc 지시 줄)
- Modify: `.tracks/장면스파인/shopping_shorts/tests/test_script_extract.py`

**Interfaces:** 없음(프롬프트 문자열).

- [ ] **Step 1: 프롬프트 계약 테스트 추가**

`tests/test_script_extract.py`에 추가:
```python
def test_prompt_scene_desc_accuracy_guard():
    from shopping_shorts import script_extract
    p = script_extract._PROMPT
    assert "주 대상" in p and "정확" in p
```

- [ ] **Step 2: 실패 확인**

Run: `cd .tracks/장면스파인 && python -m pytest shopping_shorts/tests/test_script_extract.py -k scene_desc_accuracy -v`
Expected: FAIL.

- [ ] **Step 3: 프롬프트 강화**

script_extract.py 67줄 `- scene_desc: 그 구간 화면에 무엇이 보이는지 짧게(제품/행동/구도)`를 아래로 교체:
```
- scene_desc: 그 구간 화면에 무엇이 보이는지 짧게(제품/행동/구도). 화면 속 **주 대상을
  정확히** 적어라 — 헷갈리는 물체를 다른 것으로 단정하지 마라(예: 양파를 참외로, 무를 감자로
  오인 금지). 확실치 않으면 색·형태로만 묘사하고 엉뚱한 이름을 붙이지 마라.
```

- [ ] **Step 4: 통과 확인**

Run: `cd .tracks/장면스파인 && python -m pytest shopping_shorts/tests/test_script_extract.py -v`
Expected: PASS(회귀 없음).

- [ ] **Step 5: 커밋**

```bash
git -C .tracks/장면스파인 add shopping_shorts/script_extract.py shopping_shorts/tests/test_script_extract.py
git -C .tracks/장면스파인 commit -m "feat(script_extract): scene_desc 주대상 정확도 가드(참외 오탐 방지)"
```

---

### Task 6: mix_pipeline 배선 + 후보 저장/선택

`run_mix_job`/`_plan_and_tts`가 scene_first 모드에서 `build_scene_first_plan`을 호출, 후보를 저장하고 추천 후보를 edit_plan으로 세팅. 후보 선택 store 메서드 추가.

**Files:**
- Modify: `.tracks/장면스파인/shopping_shorts/mix_pipeline.py` (`_plan_and_tts`에 scene_first 분기)
- Modify: `.tracks/장면스파인/shopping_shorts/store.py` (mix_jobs에 candidates_json 컬럼 + getter/setter, `select_mix_candidate`)
- Modify: `.tracks/장면스파인/shopping_shorts/app.py` (`/api/mix/start`가 scene_first 받기 + `/api/mix/candidate` 선택 엔드포인트)
- Create: `.tracks/장면스파인/shopping_shorts/tests/test_scene_first_wiring.py`

**Interfaces:**
- Consumes: `build_scene_first_plan`(Task 4), 기존 `_plan_and_tts` 흐름.
- Produces: `Store.set_mix_candidates(job_id, candidates)` / `Store.get_mix_candidates(job_id) -> list`; `_plan_and_tts(..., scene_first=False, reference_text="")` 시그니처 확장(기본값이라 기존 호출 무변경).

- [ ] **Step 1: store 마이그레이션 + 순수 테스트**

`store.py`의 mix_jobs 스키마에 `candidates_json TEXT` 추가(기존 `_ensure_column`/마이그레이션 패턴 따를 것 — 파일에서 mix_jobs 컬럼 추가하는 기존 코드 확인 후 동일 방식). getter/setter:
```python
    def set_mix_candidates(self, job_id, candidates):
        self._conn().execute("UPDATE mix_jobs SET candidates_json=? WHERE job_id=?",
                             (json.dumps(candidates, ensure_ascii=False), job_id))
        self._conn().commit()

    def get_mix_candidates(self, job_id):
        row = self._conn().execute("SELECT candidates_json FROM mix_jobs WHERE job_id=?",
                                   (job_id,)).fetchone()
        return json.loads(row[0]) if row and row[0] else []
```
`tests/test_scene_first_wiring.py`:
```python
from shopping_shorts.store import Store


def test_mix_candidates_roundtrip(tmp_path):
    st = Store(str(tmp_path / "t.db"))
    st.create_mix_job("j", ["u"], 20, "free")
    st.set_mix_candidates("j", [{"score": 0.9, "recommended": True}])
    got = st.get_mix_candidates("j")
    assert got[0]["recommended"] is True
```

- [ ] **Step 2: 실패 확인 → 구현 → 통과**

Run: `cd .tracks/장면스파인 && python -m pytest shopping_shorts/tests/test_scene_first_wiring.py -v`
먼저 FAIL(메서드/컬럼 없음) 확인 후 구현, 다시 PASS. (store 컬럼 추가 방식은 기존 mix_jobs 컬럼 추가 코드와 동일하게.)

- [ ] **Step 3: `_plan_and_tts` scene_first 분기**

mix_pipeline.py `_plan_and_tts` 시그니처에 `scene_first=False, reference_text=""` 추가. line 264 `plan = build_edit_plan(...)` 앞에 분기:
```python
    if scene_first:
        from shopping_shorts.edit_plan import build_scene_first_plan
        sf = build_scene_first_plan(source_scripts, reference_text, target_seconds, video_type=video_type)
        if sf["candidates"]:
            store.set_mix_candidates(job_id, sf["candidates"])
            rec = next((c for c in sf["candidates"] if c["recommended"]), sf["candidates"][0])
            plan = rec["plan"]
        else:
            plan = build_edit_plan(source_scripts, target_seconds, structure=structure,
                                   video_type=video_type, given_script=given_script)
    else:
        plan = build_edit_plan(source_scripts, target_seconds, structure=structure,
                               video_type=video_type, given_script=given_script)
```
`run_mix_job`의 `_plan_and_tts(...)` 호출에 `scene_first=job.get("scene_first", False), reference_text=job.get("given_script") or ""` 추가(레퍼 대본을 구조계승 텍스트로 재활용).

- [ ] **Step 4: app.py — start가 scene_first 수신 + candidate 선택 엔드포인트**

`/api/mix/start`(app.py:1422) body에서 `scene_first = bool(body.get("scene_first", False))`를 읽어 `create_mix_job(..., scene_first=scene_first)`로 전달(create_mix_job에 컬럼/파라미터 없으면 store에 추가). 신규 엔드포인트:
```python
@app.post("/api/mix/candidate")
def api_mix_candidate(body: dict):
    """후보 선택 — 고른 후보의 plan을 edit_plan으로 세팅(미리보기/렌더가 이걸 쓴다)."""
    job_id = (body.get("job_id") or "").strip()
    idx = int(body.get("index") or 0)
    store = Store(DB_PATH)
    cands = store.get_mix_candidates(job_id)
    if not cands or not (0 <= idx < len(cands)):
        return JSONResponse(status_code=404, content={"ok": False, "error": "후보 없음"})
    store.update_mix_job(job_id, edit_plan=cands[idx]["plan"])
    return {"ok": True}
```

- [ ] **Step 5: 배선 테스트(build_scene_first_plan mock으로 _plan_and_tts 분기 검증) + 통과**

`tests/test_scene_first_wiring.py`에 추가 — `monkeypatch`로 `build_scene_first_plan`이 후보 반환 시 `set_mix_candidates` 호출되고 추천 plan이 edit_plan으로 세팅되는지(TTS는 무거우니 `_plan_and_tts` 내부 build 부분만 좁게 검증하거나, run_mix_job 대신 분기 로직을 별도 함수로 뽑았다면 그것을 테스트). 실키 안 쓰게 mock.
Run: `cd .tracks/장면스파인 && python -m pytest shopping_shorts/tests/test_scene_first_wiring.py -v`
Expected: PASS.

- [ ] **Step 6: 커밋**

```bash
git -C .tracks/장면스파인 add shopping_shorts/mix_pipeline.py shopping_shorts/store.py shopping_shorts/app.py shopping_shorts/tests/test_scene_first_wiring.py
git -C .tracks/장면스파인 commit -m "feat(mix): 장면우선 모드 배선 — 후보 저장·추천 자동선택·선택 엔드포인트"
```

---

### Task 7: produce.html — 장면 우선 토글 + 후보 선택 UI

"우리 시스템으로 믹스" 경로에 장면우선 토글(기본 ON)과, 매칭 리뷰 위 후보 3개 카드(추천 배지)를 추가. 백엔드는 Task 6로 추천 후보가 이미 세팅돼 UI 없이도 동작 — 이 태스크는 후보 전환을 얹는다.

**Files:**
- Modify: `.tracks/장면스파인/shopping_shorts/static/produce.html` (믹스 시작 payload에 scene_first, 리뷰 상단 후보 카드 렌더 + `/api/mix/candidate` 호출)
- Create: `.tracks/장면스파인/shopping_shorts/tests/test_scene_first_ui_js.py`

**Interfaces:**
- Consumes: `/api/mix/start`(scene_first), `/api/mix/candidate`, status 응답의 candidates(status에 candidates 요약 노출 필요 시 app.py mix/status에 `candidates` 요약 추가).

- [ ] **Step 1: JS 구조 테스트(문자열 존재/문법)**

`tests/test_scene_first_ui_js.py`: produce.html에 `scene_first` payload 키와 후보 렌더 함수(`renderCandidates`)·`/api/mix/candidate` 호출 문자열이 있는지, `node --check`로 인라인 스크립트 문법 검증(기존 test_mix_swap_js 패턴 따를 것).
```python
import re, subprocess, pathlib
def test_produce_has_scene_first_and_candidates():
    html = pathlib.Path("shopping_shorts/static/produce.html").read_text(encoding="utf-8")
    assert "scene_first" in html and "/api/mix/candidate" in html and "renderCandidates" in html
```

- [ ] **Step 2: 실패 확인**

Run: `cd .tracks/장면스파인 && python -m pytest shopping_shorts/tests/test_scene_first_ui_js.py -v`
Expected: FAIL.

- [ ] **Step 3: 구현**

produce.html "우리 시스템으로 믹스" 시작 시 payload에 `scene_first:true` 추가. `mix/status`가 `ready_for_review`가 되면 `store.get_mix_candidates`를 노출하는 status 필드(app.py mix/status 응답에 `candidates`:[{score,recommended,story.hook,story.story_person}] 요약 추가)를 읽어 리뷰 상단에 `renderCandidates()`로 카드 N개 렌더(추천 배지). 카드 클릭 → `fetch('/api/mix/candidate',{POST, job_id, index})` → 성공 시 매칭 리뷰 다시 로드(기존 결과 렌더 함수 호출). **JS 문법 안전**(제작소 안 열림 사고 방지) — 인라인 스크립트 추가 후 육안/`node --check`.

- [ ] **Step 4: 통과 확인**

Run: `cd .tracks/장면스파인 && python -m pytest shopping_shorts/tests/test_scene_first_ui_js.py shopping_shorts/tests/test_mix_swap_js.py -v`
Expected: PASS.

- [ ] **Step 5: 커밋**

```bash
git -C .tracks/장면스파인 add shopping_shorts/static/produce.html shopping_shorts/app.py shopping_shorts/tests/test_scene_first_ui_js.py
git -C .tracks/장면스파인 commit -m "feat(produce): 장면우선 토글 + 후보 선택 UI"
```

---

### Task 8: 실 렌더 검증 + finish

**Files:** 없음(검증).

- [ ] **Step 1: finish 게이트**

```bash
cd .tracks/장면스파인 && git -C . status --porcelain
py tools/track.py finish 장면스파인
```
Expected: 문법→import→pytest 게이트 통과(신규 실패 0, baseline 8건은 기존). 통과 시 main 병합 → 3분 뒤 라이브.

- [ ] **Step 2: 서버 실 파이프라인 관측**

서버서 같은주제 소스 3~4개(양파 계열)로 scene_first 모드 믹스 생성 → (a)후보 3개·추천 배지 (b)각 대사↔장면 매칭(fit) (c)억지 비트에 빨간불+스왑 (d)다중컷 속도감. 스파이크와 동일 방식으로 EDL·프레임 육안 검증.

- [ ] **Step 3: 결과를 handoff/장면스파인.md에 기록 + 커밋**

---

## Self-Review

**1. Spec coverage:**
- 팔레트+헌장 결합 생성 → Task 1 ✅ / 다중컷 EDL grounding → Task 2 ✅ / 추천 채점 → Task 3 ✅ / 오케스트레이션 → Task 4 ✅ / 라벨 정확도 → Task 5 ✅ / mix 배선+후보저장·선택 → Task 6 ✅ / UI → Task 7 ✅ / 검증 → Task 8 ✅
- 기존 EDL 포맷 호환(render 재사용) → Task 2가 build_edit_plan 반환형 생성 ✅
- 억지→스왑UI → forced/fit 필드 Task 2 + produce.html 기존 게이트(페이즈1) ✅
- fail-open → Task 1(빈리스트)·Task 6(build_edit_plan 폴백) ✅

**2. Placeholder scan:** Task 6 store 컬럼 추가·Task 7 UI는 "기존 패턴 따를 것"으로 지시 — 실제 코드는 구현자가 파일의 기존 mix_jobs 컬럼 추가/후보 렌더 방식을 보고 맞춘다(순수 신규 로직은 코드 제공). 이는 파일 국소 관례 준수가 필요한 부분이라 의도적. Task 1~5는 완전 코드 제공.

**3. Type consistency:** `_scene_first_candidates`(→후보 dict, beats에 seg_ids) → `_ground_candidate`(seg_ids[0]=primary, 나머지=alternates, EDL dict) → `_score_candidate`(EDL dict→float) → `build_scene_first_plan`({candidates:[{plan,story,score,recommended}]}) → mix_pipeline(rec["plan"]→edit_plan). fit·forced 필드가 Task 2 EDL→produce.html 게이트까지 일관. `_ground_ref` scene_desc 포함(페이즈1 Task4에서 추가됨) 전제.

**주의:** Task 1~4는 edit_plan.py의 같은 영역에 순차 추가 — 1→2→3→4 순서. Task 6은 store 스키마 변경이라 기존 mix_jobs 컬럼 추가 코드를 먼저 읽고 동일 방식 적용. Task 7 UI는 JS 문법 안전 필수.
