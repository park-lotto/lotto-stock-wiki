# 소스비교원문재생 — 작업 로그

## 2026-08-17
- 제작소 1단계 `담은 영상` 썸네일에 ▶ 배지 → 그 칸에서 인라인 재생(레퍼런스 랭킹 `playInline`과 같은 방식).
  카드 클릭=pickFootage는 그대로 두고 배지만 stopPropagation으로 갈랐다.
- 재생 3단: play_url→/api/video · 틱톡→/api/play(mp4 재요청 403) · 그 외→/api/media 후 **직접**재생
  (프록시 무조건 씌우면 유튜브가 `_ALLOWED_VIDEO_HOSTS` 없어 400). 실패 시 embed/안내+▶복구.
- ★썸네일 없는 항목(URL 직접 추가)은 `<img>`가 없어 재생이 조용히 죽던 버그를 브라우저 실측으로 잡음.
