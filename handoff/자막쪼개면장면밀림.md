# 자막 줄을 쪼개면 장면 배치가 밀린다 (박세현님 제보 2026-09-21)

## 제보
박세현(cid 340) 14:33 "렌더 후 믹스에서 수정했는데 장면이 반영 안 됨 / 대본 자막 손본 게 풀리는 것 같다".
job `fe21f8a5dc71` (work `31cdc16e6948`, 0920 캐릭터행주). 고객 IP 211.234.202.65.

## 실측 (2026-09-21, 서버 로그·렌더 스냅샷·완성본 프레임)
- 저장은 전부 정상(200, 409 0건). 렌더 #2(14:22) 스냅샷 `final_clean_7b11cead4080bde5.plan.json`에
  고객의 장면 교체·자막 줄이 다 들어 있다. **저장·덮어쓰기 사고가 아니다**(09-15·09-19 건과 다른 뿌리).
- 14:21 고객이 **자막 단계(produce.html 💾 이 장면 줄 저장)**에서 긴 줄을 읽기 좋게 쪼갰다
  (칸1 3→4줄, 칸2 3→4줄, 칸3 2→3줄). 믹스로 안 돌아가고 14:22 바로 렌더.
- 구절맞춤 규칙 `idx = (k * 조각수) // 구절수`가 **어느 줄을 쪼갰는지 안 보고 앞에서부터 고르게** 나눈다.
  칸1(조각 3·구절 4 → [0,0,1,2]): 마지막 줄을 쪼갰는데 **2번째 구절 「기름때 낀 프라이팬을」이
  고객이 고른 s5 닦는 장면 → s2 행주 장면 반복**으로 바뀌고, 닦는 장면은 0.58초로 밀렸다.
  칸3: s0 장면이 두 번 나오고 s8(2.2초로 맞춰둔 것)이 1.24초로 잘렸다.
- 완성본 프레임으로 확인: 렌더 #2의 3.4초·4.8초가 같은 s2 행주 장면, 5.75초에만 s5 닦는 장면.
- 같은 식이 두 벌: `video_assemble.py:941`(서버) · `static/scene_play.js:702`(화면).
- 렌더 뒤 14:26 고객이 믹스 타임라인에서 칸2·칸3 줄을 다시 합쳤다 → 완성본 무효화(정상), 재렌더 안 함.
  ★칸1은 아직 4줄이라 지금 재렌더해도 행주 장면 반복이 남는다.

## 고칠 방향(미착수 — 사장님 확인 대기)
줄을 쪼개면 **원래 그 줄을 덮던 장면이 쪼갠 줄들을 이어서 덮는다**(09-11 사장님 "장면은 2개인데 줄만 4개면
편하잖아"의 본뜻). 고르게 나누기는 이 뜻의 틀린 구현이다. 서버·화면 두 곳을 짝으로 고친다.
검증 재료: 이 job의 R1→R2 스냅샷 — 고친 뒤 칸1 2번째 구절=s5, 칸3 s0 반복 없음·s8 2.2초가 나와야 한다.

## 따로 본 것(미조사)
- 13:32:52~13:33:07 같은 고객이 `POST /api/produce/mix/settings`를 15초에 약 90번 쐈다(전부 200). 원인 미확인.

---

## 2026-09-21 오후 — 수정 완료(트랙 브랜치), **라이브 미배포 — 사장님 승인 대기**

브랜치 `track/자막쪼개면장면밀림` (커밋 2694d13be · 74dff7430 · 2e1198664 · cc6c8d061 + 이 기록). `finish` 안 함(0순위-A1c).

### 뿌리
"어느 말에 어느 장면을 붙였는지"를 어디에도 기억하지 않고, 렌더·화면이 매번 **개수만으로** 다시 나눴다.
→ 짝을 **대사 글자 위치**로 얼려 둔다(`beat["clip_anchor"] = {key: 대사지문, n: 조각수, offs: [조각별 시작 글자위치]}`).

### 규칙 — 판단처는 `video_assemble.phrase_owners` 한 곳
- R1 구절 수 == 조각 수 → 순서대로 1:1 (그리고 `ensure_clip_anchor`가 그 짝으로 **다시 얼린다** = 잘못 얼린 옛 상태에서 빠져나오는 길)
- R2 수가 다르고 얼린 짝이 유효 → 조각은 자기가 덮던 글자 위치를 계속 덮는다(줄을 나누면 새 줄은 같은 조각 몫)
- R3 얼린 짝 없음 → 종전 식 `(k*조각수)//구절수` (옛 job 배정 그대로)
- 얼린 짝은 **스스로 유효성 증명**(대사 지문·조각 수가 다르면 무효) — caption_lines 고치는 곳이 20곳 가까이라 곳곳에서 지우게 하지 않았다
- 같은 조각이 바로 다음 줄로 이어질 때 재료가 모자라면 **되감지 않고 끝 프레임에서 버틴다**(서버·화면 둘 다). ★옛 job에도 적용되는 유일한 동작 변경

### 배선
| 곳 | 무엇 |
|---|---|
| `app.py _caplines_locked` | 줄 바꾸기 **전** 얼림 + 바꾼 **뒤** 수가 같아졌으면 1:1로 다시 얼림(자막 단계·장면꾸미기·믹스 타임라인 공용 입구) |
| `app.py _freeze_clip_anchors` | 믹스 저장(apply)·되돌리기·판본 복원 뒤 얼림. apply 응답에 `captions`(owner 포함) |
| `app.py _lab_captions` | 구절마다 `owner`·`owner_n`을 실어 보냄 — 화면은 계산 안 하고 받은 값을 쓴다 |
| `static/scene_play.js planClips` | `caps[k].owner` 사용. 화면에서 조각 수가 달라지면 종전 식 + 받은 짝 삭제(수가 되돌아와도 옛 짝을 되살리지 않는다) |
| `static/scene_lab.html adoptOwners` | 저장 응답의 새 짝으로 화면을 서버와 다시 맞춤 |
| `app.py _save_render_inputs` | 비교에서 `clip_anchor` 제외 — 옛 job을 열어 저장만 해도 완성본이 무효가 되지 않게 |
| `mix_pipeline._plan_signature` | 짝이 종전 식과 **다를 때만** 서명에 추가 — 옛 job 서명 불변(유료 재청소 없음), 짝을 바로잡으면 옛 청소본 재사용 안 함 |
| `app.py narration 수정` + `video_assemble.carry_caption_lines` | 대사 글자를 고쳐도 **사람이 나눈 줄**을 새 문장으로 옮기고 짝도 옮겨 적는다(종전: 통째로 지움 = 고객 "자막 손본 게 풀린다"의 또 한 갈래, 로그 14:16:15 narration/2) |

### 검증 (전부 이 세션 실측)
- 새 테스트 22건(`test_phrase_owner_anchor*.py` 3파일 + `test_narration_edit_keeps_human_lines.py`) 통과.
  **고치기 전 규칙/파일로 되돌려 돌리면 서버 4건·화면 4건 실패** = 검사가 실제로 잡는다.
- 관련 회귀 867 passed. 남은 실패 2건(`test_scene_style*`)은 main 폴더에서도 똑같이 실패(내 변경 무관).
- 전수 점검 `tools/phrase_owner_audit.py`(서버 전 job 덤프 930 job·1195쌍): 장면·대사는 그대로인데 줄 수만 바뀐 칸 369 중
  **안 건드린 줄의 장면이 바뀐 칸 — 옛 규칙 59(job 49·고객 12명) → 새 규칙 10**. 남은 10은 전부 줄 수 == 장면 수가 된 경우(R1, 가려졌던 장면이 나오는 쪽).
- 로컬 E2E(박세현님 실제 소스·음성, job `pse_e2e`로 복제, 사고 직전 상태): 실제 믹스 화면에서 저장 → caplines로 마지막 줄 나눔
  → 화면 컷 `s2,s5,s2,s2`·칸3 `s0,s8,s8` → **실제 렌더 → 프레임 확인**: 「기름때 낀 프라이팬을」에 닦는 장면, 「싱크대 물기까지/한 번에 정리돼요」에 s8이 이어서.

### ⏭ 남은 일
- [ ] **사장님 승인 후** `py tools/track.py finish 자막쪼개면장면밀림` → 라이브에서 직접 눌러 확인(자막 단계 줄 나눔 → 믹스 화면 컷 → 렌더)
- [ ] 박세현님 job `fe21f8a5dc71` 칸1은 **이미 틀린 짝(4줄·3장면 [0,0,1,2])**이라 배포만으로는 안 고쳐진다.
      고객이 칸1 마지막 두 줄을 합쳐 3줄로 만들면 1:1로 돌아오고(R1), 다시 나눠도 안 밀린다. 고객 데이터를 직접 고칠지는 사장님 결정.
- [ ] 미조사: 13:32:52~13:33:07 같은 고객의 `POST /api/produce/mix/settings` 15초 약 90회.
- 로컬 검증 재료: `.tracks/자막쪼개면장면밀림/shopping_shorts/data/mix_jobs/pse_e2e`(gitignore) — 다 쓰면 지워도 된다.
