# 매칭판정축 로그

## 2026-08-18 (회사 PC)
- 사장님 질문 "장면매칭이 왜 이렇게 힘드냐 / 1단계 태깅·영상분석이 문제냐"에서 출발.
- **조사 결론: 1단계 태깅은 문제가 아니다.** 라이브 세그 1,953건 실측 채움률 —
  scene_desc·shot_role **100%**, change 92.7%, action 80.6%. label·use_point는
  08-16부터 0%→100%로 정상화됨. 재료는 충분하다.
- ★진짜 원인 = **교정 장치가 통째로 꺼져 있었다.** 판정축이 `beat_action_mismatch`
  하나뿐인데 동사사전 30개(요리 전용)에 매달려 있어, 스토리형 대사엔 동사가 없다:
  ```
  대사행위 None 148/168건(88%) → 어긋남 미검출 → _verify_fits가 fit 못 깎음
    → fit 5로 남음 → _repick_weak_beats(fit<=3)에 안 걸림 → 아무도 안 고침
  ```
  실측: 훅·CTA 58건 중 **27건이 어긋났는데 fit>=4라 교정 대상에서 빠졌다.**
  fit=5 자기신고 37건 중 **26건(70%)이 실제로는 결이 어긋나** 있었다.
  훅에 붙은 결 분포는 사용중 21 / 완성·after 4 = **13%**.
- ★08-17 `_ROLE_WANT_SHOTS` 수정이 안 먹은 이유: **고친 곳은 안 돌고, 도는 곳은 안 고쳤다.**
  `fill_beat_scenes`는 `app.py:3865`(장면실험실 [채우기] 버튼)에서만 호출되고,
  `_repick_weak_beats`는 fit<=3에서만 도는데 fit이 5로 남아 있었다.
- 처방: 두 번째 판정축 `backbone.beat_role_mismatch`(역할↔결) 신설 → `_verify_fits`에 배선.
  판단표는 `edit_plan._ROLE_WANT_SHOTS` 한 곳만 참조(0순위-B). **표를 복사하면 실패하는
  테스트**를 넣어 갈라지는 걸 막았다.
- 효과 실측 2단:
  ① 라이브 잡 30개 재판정 — 재픽 대상 **38건 → 94건**, 오탐 0건
  ② ★**재픽만 떼어 실제 Gemini로 돌림**(3단계 전체 불필요) — 잡 4개·호출 4회:
     화면 바뀐 비트 7건 중 **틀린 결→맞는 결 3건, 악화 0건**.
     `hook 사용중→완성` 2건, `결과 before→사용중` 1건.
- ★**3단계 전체를 돌려야만 아는 건 "영상이 좋아 보이나" 하나뿐이다.** `_repick_weak_beats`가
  `call`을 인자로 받으므로 라이브 비트+세그를 넣고 재픽만 실행할 수 있다(영상·TTS·렌더 없음).
  내가 처음에 "Gemini 호출이 필요해 안 쟀다"고 한 건 틀렸다 — 호출 4번이면 됐다.
- 부수 효과(짚어둠): fit은 후보채점 `avg_fit`(매칭점수의 50%)에도 들어간다. 잡 28/30에서
  avg_fit이 내려간다(최대 1.00→0.64). 모든 후보가 같은 규칙으로 내려가 순위는 대체로
  보존되지만 뒤바뀔 수 있다 — 후보채점 테스트 207건 green으로 확인.
- 내가 깬 것 1건: `test_scene_first_backbone_base`가 **"마지막 Gemini 호출"** 프롬프트를
  검사하고 있어, 재픽이 마지막이 되며 깨졌다. 기준선(git stash) 대조로 내 탓 확인 →
  **생성 프롬프트를 직접 집도록** 구조 수정(예전 restyle 때도 같은 이유로 깨진 이력 있음).
- 검증: 신규 14건 green / 전체 **4,259 passed**, 실패는 기준선과 동일한 1건(회귀 0).
  커밋 `5a3665a92` → main `fb71192de`, 서버 재시작 **01:24:03 확인**.
- **(같은 날 2차) 사장님이 3단계까지 돌려 버그를 하나 더 잡았다.** 배포(01:24) **뒤**에 돈
  잡 `a4e619328313`(01:41)에서 훅에 '사용중'이 붙었는데 **fit=5 · evidence 없음 · repicked 없음**.
  - 축은 멀쩡했다 — 그 잡의 비트로 직접 돌리면 `beat_role_mismatch(Hook,사용중)=True`.
  - ★원인은 **경로**였다. 그 잡은 `generator="legacy"`(scene_first 미사용)라 `build_edit_plan`을
    탔는데, 이 함수는 `_repick_weak_beats`는 부르면서 **`_verify_fits`를 한 번도 안 불렀다**
    → fit이 자기신고 5로 남아 재픽이 대상 0개로 헛돌았다.
  - **어제 08-17 `_ROLE_WANT_SHOTS`와 똑같은 실수를 내가 한 층 위에서 반복했다** —
    "고친 곳은 안 돌고 도는 곳은 안 고쳤다". `_verify_fits`가 scene_first에만 있는 걸 보고
    그게 주경로라고만 여겨 **다른 경로를 세지 않았다.**
  - 처방: `build_edit_plan`에도 `_verify_fits` 배선(**재픽 앞**에 둬야 재픽이 깎인 비트를 본다).
    회귀 방지 `test_옛경로도_fit을_검증한다` — 호출 존재 + 재픽보다 앞인지까지 검사.
  - 서버 실측 경로 분포(최근 60잡): scene_first 39 / **legacy 4** / 옛형식 17.
  - 검증: 신규 15건 green / 전체 **4,263 passed**, 기준선과 동일한 1건만 실패(회귀 0).
    커밋 `24d2e958b` → main `dc2b62bd4`, 서버 재시작 **04:39:03 확인**.
- **(3차) `legacy`는 버그가 아니라 정상이었다.** `mix_pipeline.py:1024` — 확정 대본이 있으면
  scene_first를 끄고 `build_edit_plan`으로 간다(08-17 사장님 "어이없게 대본을 또 쓰냐").
  08-16 저녁부터 전부 legacy인 건 그 수정 때문. **내 "scene_first가 안 잡힌다" 가설은 틀렸다.**
- ★대신 진짜를 찾았다: **화면 배치 지시가 `_scene_first_candidates` 안에만 인라인**으로 있어
  확정 대본 경로(`_SCRIPTED_PROMPT`)는 한 번도 못 받았다. 그 경로의 화면 선택 지시는
  "나레이션과 화면이 어울리게 골라라 / 얼굴 클로즈업 배제 / 여러 영상 섞어라"가 전부였다.
  대사가 감정만 말하면 '어울리는 화면'이 사실상 아무거나다 → 훅에 세탁통 클로즈업.
  → **공용 상수 `_SCENE_PLACEMENT_RULES`**로 뽑아 양쪽이 같은 문장을 쓰게 함(인라인 사본 삭제).
  `SCENE_GUIDE=off` 환경변수로 끌 수 있다(A/B용, 기본 켜짐). UI 토글은 안 만들었다 —
  끄면 나빠지는 쪽이라 항상 켜져 있어야 한다. 커밋 `df1df9256`.
- ★**"대본 문제냐 3단계 매칭 문제냐"** → 3단계다. `_SCRIPTED_PROMPT`는 대본을 안 바꾸고
  화면만 고른다. 즉 이 수정으로 **대본은 한 글자도 안 바뀌고 붙는 화면만 달라진다.**
- ★**두 경로 전수 감사**(서브에이전트) — 같은 병이 세 번 반복된 게 우연이 아니었다.
  B(확정대본) 경로에 빠진 것, 심각도순:
  | 1위 | `_fill_beat_screen_time` | 화면 길이 ≥ 대사 길이 보장이 **코드에 없다**. 주석 실측: 말 31.9초 vs 화면 13.8초. *"프롬프트는 개수만 요구해서 1.3초 컷 하나로도 통과했다 — 대본을 아무리 고쳐도 이건 안 고쳐진다"* |
  | 2위 | `_fix_beat_structure` | CTA가 마지막이 아닐 수 있다(실측 job d01f6567) |
  | 3위 | `caption_lines` | 스키마에 아예 없어 자막 호흡을 모델이 안 끊어준다 |
  | 4위 | `dedup_clips_global`·`ensure_sources_used` | 소스 편중·B롤 반복 방어가 프롬프트 한 줄뿐 |
- ⚠️ **내 08-18 `_verify_fits` 배선에 부분 구멍**: B만 `_chronological_respine`을 타는데
  `respined` 표식이 붙은 비트는 `_repick_weak_beats`·`_reconcile_weak_beats`가 **건너뛴다**.
  실측(최근 20잡 112비트): respined 7건(6%)이고 **훅·CTA는 0건**(앵커라 대상 아님) →
  사장님이 지적한 훅 문제는 그대로 고쳐진다. body 6%만 재픽을 못 받는다.
- ⏭ **다음 작업 1순위 = `_fill_beat_screen_time`을 B에도 배선**(위 감사 1위).
  지시문·판정축보다 근본이다 — 화면이 대사보다 짧으면 뒤 클립이 당겨져 밀림이 누적된다.
- ⏭ 사장님이 한 편 돌려 훅 비트에 `fit_evidence=role_mismatch` + 화면 배치 반영 확인.

## 2026-08-18 — 화면길이 보장을 확정대본 경로에도
- 감사 1순위 처리: `build_edit_plan`에 `_fill_beat_screen_time` 배선(target_seconds 재계산 뒤).
- 대조 실측: 수정 OFF 화면 2.0초/필요 10.5초 → ON 10.5초 충족. 회귀 테스트 2건 추가, 회귀 0.

## 2026-08-18 — 2단계 재료가 딴 작업 것으로 뜨던 것
- 제보: 1단계=다이소 자석 네일펜 5편인데 2단계 재료가 '홈데코랩/장모님 댁'.
- 실측: handoff·_load_work_sources·aipick 전부 정상(pick_id=DcIrfnOzHre 네일).
  틀린 건 저장된 state.s2뿐 — seed=옛 홈데코랩(DbmCnyTTobw), drafts=또 다른 작업의 카드지갑.
- 뿌리: S2가 메모리라 작업을 갈아타도 살아남는데 스냅샷에 주인 표시가 없어,
  '따로 만들기'(WORK_ID=null) 직후 저장이 옛 S2를 새 작업에 담았다.
- 처방: 스냅샷에 work_id 새김 + 남의 작업이면 복원 안 함 + 새 작업 분기에서 S2 리셋.
