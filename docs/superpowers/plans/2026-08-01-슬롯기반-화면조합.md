# 슬롯 기반 화면 조합 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 화면(세트)과 대사(비트)를 처음부터 1:1 불변 슬롯으로 묶어, 파이프라인 도중 인덱스
산술로 여러 번 재짝짓기하다 생기는 화면 중복·맥락 어긋남 버그를 구조적으로 제거한다.

**Architecture:** `_pick_timeline`(소스순서+시간순 이어붙이기)을 Gemini 1회 호출 기반
`_pick_slot_sequence`로 교체한다. 이 함수가 두 영상의 태깅 조각 전체를 보고 스토리 순서
seg_id 리스트를 반환하면, 그 순서·개수가 이후 절대 안 바뀐다. `_assign_timeline`의 인덱스
비례 배분과 반복 호출을 제거하고, `_split_long_beats`가 화면까지 나누던 동작을 제거한다.
TTS·자막·5단계 UI 구성·`d.beats` 응답 스키마는 그대로 유지 — 순수 백엔드 배선 교체.

**Tech Stack:** Python, `pipeline.atoms.key_vault`(Gemini 캐스케이드 키풀), pytest.

---

## 사전 확인 사항 (모든 태스크 공통)

- 작업 폴더: `C:\Users\CH\Desktop\로또의 주식\.tracks\대본퀄v3` (트랙 폴더, 이미 생성됨)
- 대상 파일: `shopping_shorts/edit_plan.py`
- 테스트 파일: `shopping_shorts/tests/test_rewrite_mix.py`
- 테스트 실행: 트랙 폴더에서 `python3 -m pytest shopping_shorts/tests/test_rewrite_mix.py -v`
- **주의**: `test_rewrite_mix.py`에 이미 미커밋 변경사항이 있다 — 오늘 낮 디버깅 세션에서
  추가한 `test_split_growing_beats_does_not_duplicate_screens`. 이 테스트는 `_split_long_beats_and_groups`라는
  아직 없는 함수를 요구하는데, 이번 계획에서는 그 함수 대신 `_split_long_beats`를 완전히
  제거하는 방향으로 가므로 **Task 1에서 이 테스트를 지우고 새로 쓴다.**

---

### Task 1: 기존 미커밋 변경 정리 + 새 실패 테스트로 교체

**Files:**
- Modify: `shopping_shorts/tests/test_rewrite_mix.py`

- [ ] **Step 1: 오늘 낮에 추가했던 임시 테스트를 제거**

`test_rewrite_mix.py`에서 `test_split_growing_beats_does_not_duplicate_screens` 함수
전체(주석 포함, `_split_long_beats_and_groups`를 참조하는 블록)를 삭제한다. 이 함수는
`test_rewrite_mix_runs_even_when_backbone_is_on` 바로 앞에 있다.

- [ ] **Step 2: git status로 깨끗한 상태 확인**

Run: `git -C . status --porcelain shopping_shorts/tests/test_rewrite_mix.py`
Expected: 아무 출력 없음(원래 커밋 상태로 복귀) — 파일을 손으로 원복했으니 diff가 없어야
정상이다. 만약 diff가 남아있으면 `git diff shopping_shorts/tests/test_rewrite_mix.py`로
확인 후 완전히 되돌린다.

---

### Task 2: `_pick_slot_sequence` — Gemini 1회 호출로 슬롯 시퀀스 결정

**Files:**
- Modify: `shopping_shorts/edit_plan.py` (새 함수를 `_pick_timeline` 바로 뒤, 라인 378
  부근에 추가)
- Test: `shopping_shorts/tests/test_rewrite_mix.py`

이 태스크는 `_pick_timeline`을 **아직 교체하지 않는다** — 새 함수를 나란히 추가하고
단위 테스트로 검증한 뒤, Task 4에서 실제 호출부를 교체한다(작은 단위로 검증 가능하게).

- [ ] **Step 1: 실패하는 테스트 작성**

`test_rewrite_mix.py` 맨 위 `from shopping_shorts import edit_plan as ep` 아래,
`_sm` 헬퍼 정의 다음에 이 테스트를 추가한다(파일 어디든 상단 근처, 다른 `test_` 함수들과
같은 레벨):

```python
def test_pick_slot_sequence_returns_all_segments_once():
    """Gemini가 고른 순서가 무엇이든, 입력된 seg_id 전부가 정확히 한 번씩 나와야 한다."""
    sm = _sm([("v0-1", "가나다요", 2.0), ("v0-2", "라마바요", 2.0),
              ("v1-1", "사아자요", 2.0), ("v1-2", "차카타요", 2.0)])

    def fake_call(prompt, schema):
        # Gemini 응답 흉내: 소스를 교차하는 순서
        return {"order": ["v1-1", "v0-1", "v1-2", "v0-2"]}

    seq = ep._pick_slot_sequence(sm, call=fake_call)
    ids = [s["seg_id"] for s in seq]
    assert sorted(ids) == sorted(sm.keys())
    assert ids == ["v1-1", "v0-1", "v1-2", "v0-2"]


def test_pick_slot_sequence_drops_unknown_ids_from_model():
    """모델이 없는 seg_id를 지어내면 그 항목만 버리고 나머지는 순서 유지."""
    sm = _sm([("v0-1", "가나다요", 2.0), ("v0-2", "라마바요", 2.0)])

    def fake_call(prompt, schema):
        return {"order": ["v0-1", "v9-99", "v0-2"]}

    seq = ep._pick_slot_sequence(sm, call=fake_call)
    assert [s["seg_id"] for s in seq] == ["v0-1", "v0-2"]


def test_pick_slot_sequence_falls_back_when_call_fails():
    """Gemini 호출이 실패(None 반환)하면 _pick_timeline과 동등한 폴백(시간순 이어붙이기)으로."""
    sm = _sm([("v0-1", "가나다요", 2.0), ("v0-2", "라마바요", 2.0)])

    def fake_call(prompt, schema):
        return None

    seq = ep._pick_slot_sequence(sm, call=fake_call)
    assert [s["seg_id"] for s in seq] == ["v0-1", "v0-2"]
```

- [ ] **Step 2: 테스트가 실패하는지 확인**

Run: `python3 -m pytest shopping_shorts/tests/test_rewrite_mix.py -k test_pick_slot_sequence -v`
Expected: 3개 다 `AttributeError: module 'shopping_shorts.edit_plan' has no attribute
'_pick_slot_sequence'`로 실패.

- [ ] **Step 3: 최소 구현 작성**

`edit_plan.py`의 `_pick_timeline` 함수(라인 331-377) 바로 뒤에 추가:

```python
_SLOT_SEQ_SCHEMA = {
    "type": "object",
    "properties": {
        "order": {"type": "array", "items": {"type": "string"}},
    },
    "required": ["order"],
}


def _slot_seq_prompt(seg_map):
    lines = ["아래는 두 영상에서 뽑은 장면 조각들이다. 각 조각의 역할(무슨 내용인지)을 보고,",
             "하나의 자연스러운 스토리로 이어지도록 순서를 정하라. 같은 제품이라도 두 영상이",
             "서로 다른 훅·전개를 가질 수 있으니, 소스에 얽매이지 말고 내용 흐름으로 판단하라.",
             "모든 조각을 반드시 한 번씩만 써라 — 순서만 정하고 빼거나 지어내지 마라."]
    for sid, s in seg_map.items():
        lines.append(f"- [{sid}] {s.get('shot_role', '')} · {s.get('scene_desc', '')} "
                      f"· 원본 대사: {s.get('text', '')}")
    lines.append("\n반드시 JSON {\"order\": [seg_id, ...]} 형식으로만 답하라.")
    return "\n".join(lines)


def _pick_slot_sequence(seg_map, target_seconds=None, call=None):
    """두 영상의 태깅 조각을 Gemini 1회 호출로 스토리 순서로 정렬한다(2026-08-01).

    _pick_timeline(소스순서+시간순 강제 이어붙이기)을 대체 — 두 영상의 서사가 다르면
    억지로 이어붙던 문제를 없앤다. 모델이 모르는 seg_id를 지어내면 버리고, 실제 seg_map에
    있는데 모델이 빠뜨린 것은 원래 순서(입력 순서)대로 뒤에 이어붙인다(fail-open).
    call이 없거나 실패(None 반환)하면 원본 시간순(dict 삽입 순서)으로 폴백한다."""
    if not seg_map:
        return []
    caller = call or _vault_call
    resp = caller(_slot_seq_prompt(seg_map), _SLOT_SEQ_SCHEMA)
    order = (resp or {}).get("order") if isinstance(resp, dict) else None
    if not order:
        return [dict(s) for s in seg_map.values()]
    seen = set()
    out = []
    for sid in order:
        if sid in seg_map and sid not in seen:
            out.append(dict(seg_map[sid]))
            seen.add(sid)
    for sid, s in seg_map.items():          # 모델이 빠뜨린 것 뒤에 보충
        if sid not in seen:
            out.append(dict(s))
            seen.add(sid)
    return out
```

- [ ] **Step 4: 테스트 통과 확인**

Run: `python3 -m pytest shopping_shorts/tests/test_rewrite_mix.py -k test_pick_slot_sequence -v`
Expected: 3개 다 PASS.

- [ ] **Step 5: 전체 회귀 테스트 확인**

Run: `python3 -m pytest shopping_shorts/tests/test_rewrite_mix.py -v`
Expected: 기존 테스트 전부 그대로 PASS(아직 `_pick_timeline` 호출부를 안 바꿨으므로 회귀
없음이 당연하다).

- [ ] **Step 6: 커밋**

```bash
git add shopping_shorts/edit_plan.py shopping_shorts/tests/test_rewrite_mix.py
git commit -m "feat(대본퀄v3): _pick_slot_sequence — Gemini 1회 호출로 슬롯 순서 결정

_pick_timeline의 소스순서+시간순 강제 이어붙이기를 대체할 함수. 두 영상의
서사가 다르면 억지로 이어붙던 문제 해결. 아직 호출부는 안 바꿈(다음 태스크)."
```

---

### Task 3: 슬롯을 곧바로 "화면+대사 한 쌍" 그룹으로 묶기 (`_slots_to_groups`)

`_pick_slot_sequence`는 seg 낱개 리스트를 반환한다. 기존 `_rewrite_block`·`_assign_timeline`은
"세트(문장 하나가 시작~끝나는 구간 묶음)" 단위(`groups: [[seg,...], ...]`)를 받는다. 이 태스크는
seg 리스트를 세트로 묶는 로직을 `_pick_timeline`에서 그대로 재사용 가능한 형태로 분리한다.

**Files:**
- Modify: `shopping_shorts/edit_plan.py`
- Test: `shopping_shorts/tests/test_rewrite_mix.py`

- [ ] **Step 1: `_pick_timeline`의 그룹핑 로직을 별도 함수로 뺀다 (리팩터, 테스트 불변)**

`edit_plan.py` 라인 367-376(`_pick_timeline` 안의 "한 세트 = 원본 대사 한 문장이..."
주석부터 `return groups`까지)을 아래처럼 별도 함수로 추출한다.

기존 `_pick_timeline` 끝부분(라인 367-377):
```python
    # ★한 세트 = 원본 대사 **한 문장이 시작해서 끝나는 장면까지**(2026-07-31 사장님).
    #   시간으로 끊으면 문장 중간에서 화면이 갈려 말과 그림이 어긋난다. 원본이 한 문장을
    #   말하는 동안 쓴 화면 전부가 한 덩어리다 — 그 자리에 우리 문장 하나를 갈아끼운다.
    groups, cur = [], []
    for s in picked:
        cur.append(s)
        if _ends_sentence(s.get("text")) and _secs(cur) >= _MIN_LINE_SECS:
            groups.append(cur); cur = []
    if cur:
        (groups[-1].extend(cur) if groups else groups.append(cur))
    return groups
```

이걸 아래처럼 바꾼다(동작 100% 동일, 이름만 분리):

```python
    return _group_by_sentence(picked)


def _group_by_sentence(picked):
    """seg 리스트를 '원본 대사 한 문장이 시작~끝나는 구간'으로 묶는다(2026-07-31 사장님).

    _pick_timeline과 _pick_slot_sequence 둘 다 이 그룹핑을 재사용한다 — 세트 정의는
    화면을 어떤 순서로 고르든(시간순이든 Gemini 판단이든) 동일해야 하기 때문."""
    groups, cur = [], []
    for s in picked:
        cur.append(s)
        if _ends_sentence(s.get("text")) and _secs(cur) >= _MIN_LINE_SECS:
            groups.append(cur); cur = []
    if cur:
        (groups[-1].extend(cur) if groups else groups.append(cur))
    return groups
```

- [ ] **Step 2: 기존 테스트로 리팩터가 안전한지 확인 (아직 새 테스트 안 씀)**

Run: `python3 -m pytest shopping_shorts/tests/test_rewrite_mix.py -v`
Expected: 전부 PASS(순수 리팩터라 동작 변화 없음 — `test_source_order_is_preserved_within_a_source`
등 `_pick_timeline`을 쓰는 기존 테스트가 이걸 검증한다).

- [ ] **Step 3: 커밋**

```bash
git add shopping_shorts/edit_plan.py
git commit -m "refactor(대본퀄v3): _pick_timeline의 문장 그룹핑을 _group_by_sentence로 분리

_pick_slot_sequence 결과도 같은 방식으로 세트를 묶어야 하므로 재사용 가능하게 추출.
동작 변화 없음(기존 테스트 전부 green)."
```

- [ ] **Step 4: `_pick_slot_sequence`가 그룹까지 반환하도록 확장하는 실패 테스트 작성**

`test_rewrite_mix.py`에 추가:

```python
def test_pick_slot_sequence_groups_by_sentence():
    """_pick_slot_sequence 결과도 _pick_timeline과 같은 문장 단위 그룹으로 묶여야 한다."""
    sm = _sm([("v0-1", "가나다요", 2.0), ("v0-2", "라마바요", 2.0),
              ("v1-1", "사아자요", 2.0)])

    def fake_call(prompt, schema):
        return {"order": ["v0-1", "v1-1", "v0-2"]}

    groups = ep._pick_slot_groups(sm, call=fake_call)
    ids = [[s["seg_id"] for s in g] for g in groups]
    assert ids == [["v0-1"], ["v1-1"], ["v0-2"]]   # 각자 "요"로 끝나는 문장 → 세트 하나씩
```

- [ ] **Step 5: 실패 확인**

Run: `python3 -m pytest shopping_shorts/tests/test_rewrite_mix.py -k test_pick_slot_sequence_groups_by_sentence -v`
Expected: `AttributeError: has no attribute '_pick_slot_groups'`

- [ ] **Step 6: 최소 구현**

`_pick_slot_sequence` 함수 바로 뒤에 추가:

```python
def _pick_slot_groups(seg_map, target_seconds=None, call=None):
    """_pick_slot_sequence로 순서를 정하고 _group_by_sentence로 세트를 묶은 최종 결과.
    build_edit_plan이 _pick_timeline 대신 부르는 진입점."""
    picked = _pick_slot_sequence(seg_map, target_seconds=target_seconds, call=call)
    return _group_by_sentence(picked)
```

- [ ] **Step 7: 통과 확인**

Run: `python3 -m pytest shopping_shorts/tests/test_rewrite_mix.py -v`
Expected: 전부 PASS.

- [ ] **Step 8: 커밋**

```bash
git add shopping_shorts/edit_plan.py shopping_shorts/tests/test_rewrite_mix.py
git commit -m "feat(대본퀄v3): _pick_slot_groups — 슬롯 순서+문장그룹핑 진입점 완성"
```

---

### Task 4: `_assign_timeline` 반복 호출 제거 — 슬롯이 곧 비트가 되게 배선

지금 `build_edit_plan`은 `_pick_timeline`으로 `tl_groups`를 만들고, `_ground_candidate` 직후와
후처리 뒤 **두 번** `_assign_timeline(plan["beats"], tl_groups)`를 부른다(라인 2185,
2218-2219, 2275-2276). 이 태스크는 `tl_groups`를 `_pick_slot_groups`로 교체하고, 비트를
"이미 세트 개수만큼만" 만들도록 프롬프트 지시를 강화해 재호출을 안전하게 만든다.

**주의**: `_assign_timeline` 자체는 완전히 제거하지 않는다 — 여전히 grounding 직후 최초
1회는 필요하다(모델이 만든 beats의 화면을 세트로 교체하는 역할). 제거하는 건 "두 번째
호출"과 그 사이 비트 개수를 바꾸는 `_split_long_beats`다.

**Files:**
- Modify: `shopping_shorts/edit_plan.py:2185, 2218-2219, 2263-2276` (build_edit_plan 안)
- Modify: `shopping_shorts/edit_plan.py:1503-1562` (`_split_long_beats`)
- Test: `shopping_shorts/tests/test_rewrite_mix.py`

- [ ] **Step 1: 실패 테스트 — 화면 개수가 늘어나도(split 없이) 중복이 없어야 함을 고정**

`test_rewrite_mix.py`에 추가:

```python
def test_assign_timeline_called_once_never_duplicates():
    """_assign_timeline이 grounding 직후 딱 한 번만 불리면(재호출 없음), n==len(groups)가
    항상 성립하므로 화면 중복이 구조적으로 생길 수 없다 — split이 비트를 늘리지 않는지 검증."""
    sm = _sm([("v-0", "가나다요", 2.0), ("v-1", "라마바요", 2.0),
              ("v-2", "사아자요", 2.0), ("v-3", "차카타요", 2.0)])
    g = ep._pick_timeline(sm, 30)
    assert len(g) == 4
    long_narr = "긴 문장 하나입니다. 그리고 이어지는 두번째 문장입니다."
    beats = [
        {"narration": "첫줄", "beat_idx": 0, "primary": {"seg_id": "v-0"}, "alternates": []},
        {"narration": "둘째줄", "beat_idx": 1, "primary": {"seg_id": "v-1"}, "alternates": []},
        {"narration": long_narr, "beat_idx": 2, "primary": {"seg_id": "v-2"},
         "alternates": [{"seg_id": "v-2b"}]},
        {"narration": "넷째줄", "beat_idx": 3, "primary": {"seg_id": "v-3"}, "alternates": []},
    ]
    ep._assign_timeline(beats, g)
    beats = ep._fix_beat_structure(beats)   # split이 더는 개수를 안 늘려야 한다
    assert len(beats) == 4                  # 불변식: 그룹 개수와 항상 같다
    seg_ids = [b["primary"]["seg_id"] for b in beats]
    assert len(seg_ids) == len(set(seg_ids))
```

- [ ] **Step 2: 실패 확인**

Run: `python3 -m pytest shopping_shorts/tests/test_rewrite_mix.py -k test_assign_timeline_called_once_never_duplicates -v`
Expected: FAIL — `assert len(beats) == 4`에서 실패(지금 `_split_long_beats`가 긴 비트를
쪼개 5개로 늘리기 때문). 실패 사유가 이거인지 `-v` 출력으로 반드시 확인한다.

- [ ] **Step 3: `_split_long_beats`에서 "화면까지 나누는" 동작 제거**

`edit_plan.py`의 `_split_long_beats`(라인 1503-1562)를 읽고, 비트를 쪼개는 부분(문장을
앞/뒤로 나눠 **새 비트를 만들고 alternates의 컷을 그 새 비트로 옮기는** 로직)을 제거한다.
대신 "화면은 그대로 두고, 자막줄만 무효화"하는 동작만 남긴다:

```python
def _split_long_beats(beats):
    """긴 비트의 자막줄을 무효화한다(2026-08-01 재설계 — 화면은 절대 안 나눈다).

    이전엔 문장 경계로 비트 자체를 쪼개 화면도 나눴는데, 이러면 비트 수가 늘어나
    _assign_timeline 재호출 시 세트 수(불변)보다 많아져 화면 중복이 생겼다(2026-07-31
    실측 job 8226822c5b09). 새 원칙: 화면(슬롯)은 파이프라인 전체에서 불변 — 긴 비트는
    자막만 여러 줄로 나눠 보여주고 화면은 그대로 공유한다."""
    if not beats:
        return beats
    for b in beats:
        narr = (b.get("narration") or "").strip()
        if len(narr) > _LONG_BEAT_CHARS:
            b["caption_lines"] = None
    return beats
```

- [ ] **Step 4: 테스트 통과 확인**

Run: `python3 -m pytest shopping_shorts/tests/test_rewrite_mix.py -k test_assign_timeline_called_once_never_duplicates -v`
Expected: PASS.

- [ ] **Step 5: `_split_long_beats` 자체의 기존 테스트 확인 (있다면)**

Run: `grep -rn "_split_long_beats\b" shopping_shorts/tests/`
이 이름을 직접 호출하는 다른 테스트 파일이 있으면 그 파일도 함께 실행해 회귀를 확인한다.

Run: `python3 -m pytest shopping_shorts/tests/ -k split_long -v`
Expected: 남아있는 관련 테스트가 있다면 새 (단순화된) 동작에 맞게 기대값을 조정한다 —
"비트 개수가 늘어난다"를 검증하던 테스트가 있다면 그 assertion을 "개수는 그대로,
caption_lines만 None"으로 고친다.

- [ ] **Step 6: 전체 회귀 테스트**

Run: `python3 -m pytest shopping_shorts/tests/test_rewrite_mix.py -v`
Expected: 전부 PASS.

- [ ] **Step 7: 커밋**

```bash
git add shopping_shorts/edit_plan.py shopping_shorts/tests/test_rewrite_mix.py
git commit -m "fix(대본퀄v3): _split_long_beats가 화면을 나누지 않게 — 슬롯 불변식 확보

근본원인(2026-07-31 실측 job 8226822c5b09): split이 비트를 늘려 n>len(groups)가 되면
_assign_timeline 재호출 시 화면이 중복 배정됐다. split은 이제 자막줄만 무효화하고
화면(비트 개수)은 절대 안 바꾼다 — 화면-대사 슬롯 불변식을 파이프라인 전체에서 보장."
```

---

### Task 5: `build_scene_first_plan`에서 `_pick_timeline` → `_pick_slot_groups` 교체

**주의(계획 작성 중 실측 정정)**: `_pick_timeline`을 실제로 호출하는 함수는
`build_edit_plan`(라인 2033, 리라이트 믹스 관련 코드 없음)이 아니라
**`build_scene_first_plan`**(라인 2151)이다. 이 함수가 `call=None` 파라미터를 받고
`_call = call or _vault_call`(라인 2172)로 캐스케이드 폴백을 만든 뒤, 라인 2185에서
`_pick_timeline(seg_map, target_seconds)`를 부른다. 아래 스텝은 전부
`build_scene_first_plan` 기준이다.

**Files:**
- Modify: `shopping_shorts/edit_plan.py:2185` (`build_scene_first_plan` 안)

- [ ] **Step 1: 호출부 교체**

`edit_plan.py` 라인 2185:
```python
    tl_groups = _pick_timeline(seg_map, target_seconds) if REWRITE_MIX else []
```
를 아래로 교체:
```python
    tl_groups = _pick_slot_groups(seg_map, target_seconds, call=_call) if REWRITE_MIX else []
```

(`_call`은 이 함수보다 위쪽 라인 2172에서 이미 `_call = call or _vault_call`로 정의되어
있으므로 그대로 참조 가능하다.)

- [ ] **Step 2: 관련 기존 테스트로 통합 동작 확인**

Run: `python3 -m pytest shopping_shorts/tests/test_rewrite_mix.py -v`
Expected: 전부 PASS. (`test_rewrite_mix_runs_even_when_backbone_is_on` 등 `build_edit_plan`을
통째로 부르는 테스트가 있다면, 그 테스트에서 쓰는 `call=` 목(fake)이 `_slot_seq_prompt`
형식의 프롬프트에도 그럴듯한 JSON을 반환하는지 확인 — 반환을 안 하면 `_pick_slot_sequence`가
폴백(원본 시간순)으로 동작하므로 테스트가 깨지진 않아야 정상이다.)

- [ ] **Step 3: 통합 테스트 하나 추가 — end-to-end로 화면 중복 없음 재확인**

먼저 `test_rewrite_mix_runs_even_when_backbone_is_on`(기존 테스트, 라인 133 부근)을 읽고
`build_scene_first_plan`을 실제로 어떤 인자로 부르는지, `call=` fake가 어떤 스키마 분기로
어떤 응답을 주는지 그대로 참고한다(이미 이 파일 안에 있는 검증된 호출 패턴이므로 새로
추측하지 않는다). 그 패턴을 그대로 따라, 아래 뼈대에 실제 맞는 인자로 채운다:

```python
def test_scene_first_plan_end_to_end_has_no_duplicate_screens():
    """실제 build_scene_first_plan 경로(슬롯 결정 → 대본 생성 → 화면 배정)를 다 통과한
    뒤에도 같은 화면이 두 비트 이상에 배정되면 안 된다."""
    sm = _sm([("v0-1", "가나다요", 2.0), ("v0-2", "라마바요", 2.0),
              ("v1-1", "사아자요", 2.0), ("v1-2", "차카타요", 2.0)])
    source_scripts = [{"video_id": "v0", "full_text": "가나다요 라마바요"},
                      {"video_id": "v1", "full_text": "사아자요 차카타요"}]

    def fake_call(prompt, schema):
        if "order" in str(schema):
            return {"order": ["v0-1", "v1-1", "v0-2", "v1-2"]}
        # 대본 생성 콜 흉내 — 기존 test_rewrite_mix_runs_even_when_backbone_is_on의
        # fake_call이 반환하는 후보(candidates) 스키마를 그대로 참고해 맞춘다.
        return {"candidates": [{"hook": "훅", "beats": [
            {"narration": "첫줄이에요", "seg_ids": ["v0-1"], "role": "hook"},
            {"narration": "둘째줄이에요", "seg_ids": ["v1-1"], "role": "body"},
            {"narration": "셋째줄이에요", "seg_ids": ["v0-2"], "role": "body"},
            {"narration": "댓글 남겨주세요", "seg_ids": ["v1-2"], "role": "cta"},
        ]}]}

    result = ep.build_scene_first_plan(source_scripts, reference_text="", target_seconds=8,
                                       n_candidates=1, call=fake_call)
    cand = result["candidates"][0]
    plan = cand["plan"]
    seg_ids = [b["primary"]["seg_id"] for b in plan["beats"]]
    assert len(seg_ids) == len(set(seg_ids))
```

**이 스텝의 응답 스키마(`candidates`/`drafts` 등 정확한 키 이름)는 실행자가
`test_rewrite_mix_runs_even_when_backbone_is_on`의 실제 fake_call 구현을 읽고 그대로
맞춰야 한다** — 위 코드는 구조를 보여주는 뼈대이고, 정확한 필드명은 기존 테스트에서
확인한 값으로 교체한다.

- [ ] **Step 4: 테스트 실행 및 통과 확인**

Run: `python3 -m pytest shopping_shorts/tests/test_rewrite_mix.py -k end_to_end -v`
Expected: PASS. 실패하면 fake_call의 스키마 분기 조건(`"order" in str(schema)`)이 실제
스키마 구조와 안 맞을 수 있으니, `_SLOT_SEQ_SCHEMA`와 대본 생성 스키마를 실제로 비교해
분기 조건을 정확히 맞춘다.

- [ ] **Step 5: 전체 회귀 테스트**

Run: `python3 -m pytest shopping_shorts/tests/ -k "edit_plan or rewrite_mix" -v`
Expected: 전부 PASS. 실패하는 게 있으면 원인을 파악해 고친다(새 함수 시그니처 변경이
다른 호출부에 영향을 줬을 가능성).

- [ ] **Step 6: 커밋**

```bash
git add shopping_shorts/edit_plan.py shopping_shorts/tests/test_rewrite_mix.py
git commit -m "feat(대본퀄v3): build_edit_plan이 _pick_slot_groups 사용 — 슬롯 방식 전환 완료

_pick_timeline(소스순서+시간순 강제)을 _pick_slot_groups(Gemini 판단 순서)로 교체.
화면-대사 슬롯이 1:1 불변으로 파이프라인 전체를 관통 — 오늘 낮 실측 버그(job
8226822c5b09 화면중복)의 근본 원인 제거. TTS·자막·UI 5단계 구성은 그대로."
```

---

### Task 6: `_flag_offtopic`을 참고 지표로 완화

**Files:**
- Modify: `shopping_shorts/edit_plan.py:461-479`
- Test: `shopping_shorts/tests/test_rewrite_mix.py`

지금 `_flag_offtopic`은 낱말이 하나도 안 겹치면 `fit`을 2 이하로 깎고 `forced=True`를
세운다(라인 477-479). 이 값을 하류(재작성·스왑 로직)가 읽어 화면이나 대본을 강제로 바꾸는
연쇄가 있었다. 오늘 원칙(완벽 매칭 불필요, 흐름만 중요)에 따라 **검사 자체는 남기되, 화면을
바꾸는 하류 연쇄를 끊는다** — 이 태스크는 `_flag_offtopic`이 세우는 값은 그대로 두고
(UI가 경고 배지로 쓰므로), 대신 화면을 실제로 바꾸는 하류 로직이 있는지 확인 후 있으면
제거한다.

- [ ] **Step 1: `forced` 필드를 실제로 소비하는 곳 확인**

Run: `grep -rn '\["forced"\]\|\.get("forced")\|forced=True' shopping_shorts/*.py`

이 grep 결과를 읽고, `forced`를 근거로 **화면(primary/alternates)을 바꾸거나 비트를
재생성시키는** 코드가 있으면 그 파일과 라인을 적는다. UI 표시용(`⚠️` 배지)으로만 쓰이면
이 태스크에서 손댈 게 없다 — 그 경우 Step 2로 건너뛰고 "확인만 하고 변경 없음"으로 커밋
없이 태스크를 종료한다.

- [ ] **Step 2: (화면을 바꾸는 하류 로직이 있었다면) 그 강제 연쇄 제거**

발견된 코드가 있다면, 그 로직이 `forced`/`fit<=2`를 근거로 화면을 바꾸는 부분만 제거하고
(재작성 시도 자체는 남겨도 됨 — 대본만 다시 쓰는 건 원칙에 어긋나지 않음), 이 스텝은
실제 발견 내용에 따라 구체적인 diff를 작성한다.

- [ ] **Step 3: 회귀 테스트**

Run: `python3 -m pytest shopping_shorts/tests/test_rewrite_mix.py -v`
Expected: 전부 PASS (`test_offtopic_line_is_flagged`, `test_reworded_line_is_not_flagged`
포함 — 이 두 테스트는 `_flag_offtopic`이 플래그를 세우는 것 자체는 계속 검증하므로 안
깨져야 한다).

- [ ] **Step 4: 커밋 (변경이 있었을 경우만)**

```bash
git add shopping_shorts/edit_plan.py
git commit -m "fix(대본퀄v3): offtopic 플래그가 화면을 강제로 바꾸던 연쇄 제거

완벽한 낱말 단위 매칭을 강제하지 않기로 함(2026-08-01 설계 원칙 2) — fit 점수는
UI 경고 배지용으로만 남기고, 하류가 이를 근거로 화면을 바꾸는 동작은 제거."
```

---

### Task 7: 검증 — 실제 job 생성해 품질 리뷰

**이 태스크는 자동화된 pytest가 아니라 실제 데이터로 하는 수동 검증이다.** 스펙의 "검증
계획" 3항목을 확인한다.

- [ ] **Step 1: 서버에서 트랙 코드로 실제 job 하나 생성**

트랙 폴더 코드를 서버 테스트 환경에 반영할 방법이 마땅치 않다면(트랙 브랜치는 아직
main이 아님), 로컬에서 `shopping_shorts.edit_plan.build_edit_plan`을 실제 seg_map(서버
DB에서 이전에 확인한 job의 것 등)으로 직접 호출해 결과를 확인한다:

```bash
python3 -c "
from shopping_shorts.edit_plan import build_scene_first_plan
# 실제 seg_map/source_scripts는 서버 DB의 기존 job에서 가져오거나,
# script_extract.py로 실제 영상 2개를 새로 태깅해 사용한다.
"
```

이 스텝은 실제 소재(레시피류 등, 이전 실측에 쓰인 job과 유사한 것)가 필요하므로, 사용자
(사장님)에게 테스트용 영상 2개를 어디서 가져올지 확인 후 진행한다.

- [ ] **Step 2: 세 가지 확인**

1. 화면 중복이 사라졌는가 — `[b["primary"]["seg_id"] for b in plan["beats"]]`에 중복 없음
2. 스토리 흐름(문제→조리→완성 등 큰 단계)이 화면 순서와 같은 방향인가 — 사람이 직접
   `plan["beats"]`를 순서대로 읽어보고 판단(자동화 불가, 육안 리뷰)
3. 길이 재생성이 과도하게 반복되지 않는가 — 로그에서 재시도 횟수 확인

- [ ] **Step 3: 결과에 따라 분기**

- 세 가지 다 기대 이상이면 → Task 8(배포)로 진행
- 하나라도 기대 이하면 → 스펙의 "2단계 방식 전환"(역할 라벨링 1차 호출 + 순서 조립 2차
  호출 분리)을 브레인스토밍부터 다시 논의 — 이 계획은 여기서 멈추고 사용자에게 보고한다.

---

### Task 8: 배포 — 게이트 통과 후 main 병합

Task 7의 검증을 통과했을 때만 진행한다.

- [ ] **Step 1: 트랙 폴더에서 전체 상태 확인**

Run: `git -C . status --porcelain`
Expected: 커밋 안 된 변경이 없어야 한다(Task 1~6에서 매 단계 커밋했으므로 보통 깨끗함).
남은 변경이 있으면 먼저 `git add`+`commit`.

- [ ] **Step 2: 핸드오프 갱신**

`handoff/대본퀄v3.md`에 오늘(2026-08-01) 섹션을 새로 추가한다 — 완료 항목(슬롯 기반
화면조합 전환, 근본원인·수정 내용), 검증 결과(Task 7 요약), 남은 것(있다면)을 적는다.

- [ ] **Step 3: 커밋**

```bash
git add handoff/대본퀄v3.md
git commit -m "docs(대본퀄v3): 슬롯 기반 화면조합 완료 — 핸드오프 갱신"
```

- [ ] **Step 4: finish 실행**

```bash
py tools/track.py finish 대본퀄v3
```

게이트(문법 → `import shopping_shorts.app` → pytest)가 통과하면 자동으로 main에 병합되고
3분 내 서버에 자동 배포된다. 실패하면 게이트 로그를 읽고 원인을 고친 뒤 다시 `finish`를
시도한다 — 실패해도 라이브는 무사하고 트랙 폴더도 그대로 남는다(CLAUDE.md 배포 규칙 참조).

- [ ] **Step 5: 배포 확인**

3분 대기 후:
```bash
ssh -i /c/Users/CH/crawling_bot_client/LightsailDefaultKey-ap-northeast-2.pem ubuntu@3.39.179.148 \
  "cd /home/ubuntu/lotto-stock-wiki && git log -1 --oneline"
```
Expected: 방금 병합한 커밋(또는 그 머지 커밋)의 해시가 서버 HEAD로 나와야 한다.

---

## Self-Review 메모 (계획 작성자용, 이미 반영됨)

- **스펙 커버리지**: 원칙 1(슬롯 불변) → Task 3~5, 원칙 2(완벽매칭 완화) → Task 6, 원칙 3
  (재생성만으로 길이 흡수) → Task 4의 `_split_long_beats` 단순화가 곧 이 원칙의 구현,
  원칙 4(실사렌더 안 함) → 이번 계획 전체가 텍스트 태깅 기반, 원칙 5(UI 불변) → 어느
  태스크도 `produce.html`/`mix_pipeline.py`의 단계 순서를 안 건드림. 전부 커버됨.
- **타입 일관성**: `_pick_slot_sequence(seg_map, target_seconds=None, call=None)` →
  `_pick_slot_groups(seg_map, target_seconds=None, call=None)` → `build_edit_plan` 호출부
  `_pick_slot_groups(seg_map, target_seconds, call=_call)` 순서로 시그니처 일관됨.
- **정정 이력**: 초안에서 `_pick_timeline` 호출부를 `build_edit_plan`(라인 2033)으로 잘못
  짚었다가, 계획 작성 중 재확인해 실제로는 `build_scene_first_plan`(라인 2151)임을 바로잡음
  — Task 5, Task 7이 정확한 함수명으로 수정됨.
- **Task 5 Step 3의 불확실성**: `build_scene_first_plan`의 정확한 `call` 응답 스키마(후보
  구조의 정확한 키 이름)는 기존 `test_rewrite_mix_runs_even_when_backbone_is_on`의 실제
  구현을 보고 맞추도록 명시함(placeholder가 아니라 "무엇을 확인하고 어떻게 조정할지"가
  분명한 지시).
