# 폰트추가 — 제작소 폰트 늘리기

## 2026-08-27 (회사PC) — 폰트 4종 추가, 라이브

36종 → **40종**. 요청: "아네모네 / 간판체 / 이야기 / 분필 / 카페24 단정해"

### 넣은 것 (4종)

| 화면 이름 | 파일 | group | 출처·라이선스 |
|---|---|---|---|
| 카페24 아네모네 | `Cafe24Ohsquare.ttf` | impact | 카페24 공식, 상업용 무료(영상·자막 명시 허용) |
| 카페24 단정해 | `Cafe24Danjunghae.ttf` | gothic | 〃 |
| 간판체 | `KCCGanpan.otf` | retro | KCC간판체(한국저작권위원회) **CC-BY = 저작자 표시 필요** |
| 손글씨 분필 | `HakgyoansimBunpil.ttf` | hand | 학교안심 분필(KERIS) **OFL** |

### ★안 넣은 것 — "이야기"

특정 폰트로 안 좁혀졌다(카페24에 이야기 없음, 온글잎 계열도 확정 불가).
**추측으로 넣지 않았다**(0순위 규칙). 사장님 결정 = "빼고 4종만 진행".
→ 다시 하려면 **눈누 링크나 폰트 파일을 직접 받아서** 넣어라.

### ★함정 (다음에 폰트 넣을 때 그대로 밟는다)

1. **아네모네의 영문명은 `Ohsquare`다.** 이름으로 URL을 못 만든다.
   카페24 다운로드 URL 패턴(실측 200 OK):
   `https://img.cafe24.com/csdstatic/freefonts/download/kr/Cafe24{영문명}-v2.0.zip`
   확인된 것: Ohsquare / Danjunghae / Simplehae / Dangdanghae / Ssurround /
   Syongsyong / Shiningstar / Oneprettynight. (Ssukssuk·Meongi·Decobox는 404)

2. **★간판체·분필은 배포본이 woff2뿐이다 → 그대로 넣으면 조용히 죽는다.**
   `video_assemble._font_ref`가 폰트 파일을 그대로 ffmpeg에 넘기는데
   **ffmpeg·PIL은 woff2를 못 읽는다**. 오류가 안 나고 기본폰트로 나온다.
   → `fontTools`로 변환해야 한다(이 프로젝트 .venv에 이미 있다):
   ```python
   from fontTools.ttLib import TTFont
   f = TTFont('X.woff2'); f.flavor = None; f.save('X.ttf')   # CFF면 .otf
   ```

3. **noonnu CDN 경로는 추측하지 마라.** jsdelivr 경로를 손으로 지으면 전부 404.
   noonnu 페이지를 받아서 실제 @font-face URL을 뽑아라:
   `curl -sL -A "Mozilla/5.0" https://noonnu.cc/font_page/{번호} | grep -oE "https://[^\"')]*\.(woff2|woff|ttf|otf)"`

4. **콘솔이 cp949라 한글 결과가 깨져 읽힌다.** 검증 스크립트는 `PYTHONIOENCODING=utf-8`.
   (깨진 출력을 "실패"로 오독하기 쉽다 — 실제론 통과였다)

### 넣는 절차 (정본은 fonts.json 하나 — 0순위-B)

```
① shopping_shorts/static/fonts/ 에 ttf/otf 를 넣는다 (woff2면 위 2번으로 변환)
② shopping_shorts/static/fonts.json 에 한 줄 추가 (name/file/css/group/star)
③ py tools/sync_fonts.py          ← produce.html @font-face + HC_FONTS, produce_intro.html 자동 반영
④ py tools/sync_fonts.py --check  ← 최신인지 검사
```
**서버·렌더는 손댈 필요 없다** — static이 루트 마운트라 `/fonts/*.ttf`가 바로 열리고,
`video_assemble`·`deco_frame`은 파일명을 받아 `_FONT_DIR`에서 찾는다(목록이 없다).

### 실측 검증 (짐작 아님)

- 한글 글리프: 4종 **누락 0자** (희귀자 `뷁·똠` 포함) — `font_glyphs.missing_glyphs`
- 렌더 경로: `_font_ref`가 4종 모두 **자기 폰트**로 해석(기본폰트 폴백 아님)
- **ffmpeg drawtext로 실제 구워서 눈으로 확인** — 두부(네모X) 없음
- `pytest test_fonts_manifest.py` **9 passed**, `sync_fonts.py --check` 최신 40종
- 커밋 `60ca1394a`

### ⏭ 다음

- [ ] **"이야기" 폰트 확정** — 사장님께 눈누 링크/파일 받기
- [ ] 사장님 육안 확인 후 자주 쓸 것에 `"star": true` (즐겨찾기 상단 고정)
- [ ] **간판체는 CC-BY** — 저작자 표시 의무가 있다. 고객 영상에 대량으로 쓸 거면
      표기 방법을 정하거나, 부담되면 빼는 것도 검토(나머지 3종은 표시 의무 없음)
- [ ] 분필은 이름과 달리 **거친 분필 질감이 아니라 얇은 마카/펜 느낌**이다(실측).
      칠판 질감을 원하셨으면 다른 폰트를 찾아야 한다

---

## 2026-08-27 (2차) — 사장님 "렌더까지 테스트해야되, 어제 사고있었어"

08-26 폰트검수 사고(옥말랑 두부 / 꾸미기틀 공백 ⊠)를 감안해 **렌더 경로 3개를 전부** 태웠다.
1차 보고는 raw drawtext까지만이었다 — 그건 파이프라인이 아니다.

### 폰트를 쓰는 곳 3개 (handoff/폰트검수.md 3차에서 확인된 목록)

| 경로 | 쓰는 곳 | 이번 검증 |
|---|---|---|
| `video_assemble._burn_captions` | 자막·헤드카피 (ffmpeg) | ✅ **영상 4편 실렌더 + 프레임 육안확인** |
| `deco_frame.render` | 꾸미기 틀 채널명·제목 (PIL) | ✅ **틀 4장 실렌더 + 육안확인** |
| `tools/sync_fonts.py` | 목록 등록 단계 | ✅ 한글 가드 통과 |

### ★파이프라인 전체 렌더 — 핸드오프에 "아직 없다"고 적혀 있던 것

```
_burn_captions(base.mp4, plan, tts, out, work,
               headcopy={'text':'이거 진짜 좋아요','font':F},
               caption_style={'font':F,'size':70})
→ 4종 전부 mp4 생성 성공 (50~59KB)
→ 프레임 추출해 눈으로 확인: 헤드카피·자막 모두 정상, 두부 0, ⊠ 0, 띄어쓰기 정상
→ 증거: scratchpad/burn_sheet.png (4분할), burn_*.mp4
```

**★폴백 안 일어난 것까지 확인했다**(핵심 — 두부는 안 나도 글꼴이 바뀌면 사고다):
- work에 복사된 `font_*.ttf` 크기가 **원본과 정확히 일치** → 그 폰트가 실제로 쓰였다
- stderr `[폰트]` 경고 **0건** → `_font_ref`가 기본폰트로 되돌리지 않았다

### 꾸미기 틀(PIL) — 빙그레·리디바탕 함정 재발 여부

`deco_frame.render`에 `ch_font`/`title_font` override로 4종을 넣어 렌더.
→ **띄어쓰기 정상**(⊠ 없음), 한글 정상. 증거: `scratchpad/deco_crop.png`
→ `lacks_space_glyph` 4종 전부 False = 공백 우회가 필요 없는 폰트다

### 서버(라이브) 실측

```
git log -1        5928d38f2 Merge branch 'track/폰트추가'
폰트 파일 4개      로컬과 바이트 크기 동일 (2538588/1733404/1702416/2398720)
fonts.json        40종
서버에서 글리프판정 4종 전부 missing=NONE, nospace=False
```
⚠️ **비로그인 curl로는 판별 불가** — `/fonts/*.ttf`가 307→로그인페이지(8840바이트 html)를
   준다. 기존 BMJUA.ttf도 똑같이 나온다. 서버에 들어가서 직접 봐야 한다.

### ⚠️ 이번에 밟은 함정 (다음에도 밟는다)

- `font_glyphs`는 **파일이 없으면 조용히 "정상"을 반환한다**(`판정 실패(정상으로 간주)`).
  main 폴더에서 파일을 지운 채 검사해서 "전부 정상"이 나왔다 — 경로를 확인하고 판정해라.
- `_burn_captions`의 `tts_paths`는 **list가 아니라 dict**(`{beat_idx: path}`),
  beat에는 `beat_idx` 키가 필수다. list로 주면 `KeyError: 'beat_idx'`.
