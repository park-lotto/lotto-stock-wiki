# 조합 대본 요소 기반 업그레이드 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 제작소 "조합 생성" 초안을 요소(훅·전개·인물·CTA 등) 단위로 다시 써서 업그레이드할 수 있게 한다.

**Architecture:** 이미 있는 요소 엔진(`generate_variations` + `analyze_structure` + `ELEM_LABELS`)을 재사용한다. 근본 구멍인 "조합 초안에 `draft_id`가 없음"을 메우고(save_draft), 초안을 요소 분해→고른 요소만 변형해 재생성하는 엔드포인트를 추가한 뒤, 조합 초안 카드에 요소 버튼을 붙인다.

**Tech Stack:** Python 3 / FastAPI / SQLite(Store) / Gemini(google.genai) / 순수 JS(produce.html) / pytest

## Global Constraints

- 모든 편집은 트랙 폴더 `.tracks/조합대본업그레이드/` 절대경로로만. main 폴더 코드 파일 금지(흡수 방지).
- `produce.html`·`app.py`·`script_generate.py`는 타 트랙에서 hot → 편집을 국소 함수에 가둔다.
- 요소 모드는 **유지(keep) / 변형(free) 2단계만** — 강화·랜덤·카테고리 세분화 추가 금지(YAGNI).
- 기존 7요소 생성·단일 경로 refine 회귀 없음(기준선 테스트 그대로).
- `store.save_draft` 시그니처(고정): `save_draft(draft_id, customer_id, source_shortcode, parent_draft_id, hook, script_text, edit_instruction, edit_mode)`.
- 요소 키(고정): `hook, twist, development, characters, appeal, tone, devices` + 신규 `cta`.

---

### Task 1: CTA 요소 추가 (script_generate + structure_analyze)

**Files:**
- Modify: `shopping_shorts/script_generate.py:50-58` (ELEM_LABELS)
- Modify: `shopping_shorts/structure_analyze.py:21-60` (_SCHEMA), `:70-98` (_PROMPT)
- Test: `shopping_shorts/tests/test_cta_element.py`

**Interfaces:**
- Produces: `script_generate.ELEM_LABELS["cta"] == "마무리/CTA"`, `"cta" in script_generate.ELEM_KEYS`. `structure_analyze._SCHEMA["properties"]["cta"]` 존재(문자열, **required 아님** — 기존 분석본 하위호환).

- [ ] **Step 1: Write the failing test**

```python
# shopping_shorts/tests/test_cta_element.py
"""CTA를 요소 목록·구조분석 스키마에 추가했는지 — 요소 업그레이드가 CTA를 다루려면 필요."""
from shopping_shorts import script_generate
from shopping_shorts import structure_analyze


def test_cta_in_elem_labels():
    assert script_generate.ELEM_LABELS.get("cta") == "마무리/CTA"
    assert "cta" in script_generate.ELEM_KEYS


def test_cta_in_structure_schema_optional():
    props = structure_analyze._SCHEMA["properties"]
    assert "cta" in props and props["cta"]["type"] == "string"
    # 기존 분석본(cta 없음)이 깨지지 않도록 required에는 넣지 않는다
    assert "cta" not in structure_analyze._SCHEMA["required"]
```

- [ ] **Step 2: Run test to verify it fails**

Run: `cd shopping_shorts && python -m pytest tests/test_cta_element.py -v`
Expected: FAIL (`cta` 키 없음 / 스키마에 cta 없음)

- [ ] **Step 3: Implement — ELEM_LABELS에 cta 추가**

`shopping_shorts/script_generate.py`의 `ELEM_LABELS` 끝(`"devices": "설득장치",` 다음 줄)에 추가:

```python
ELEM_LABELS = {
    "characters": "등장 주변인물",
    "twist": "발상전환",
    "development": "전개방식",
    "hook": "훅",
    "appeal": "어필포인트",
    "tone": "말투/어미",
    "devices": "설득장치",
    "cta": "마무리/CTA",
}
```

- [ ] **Step 4: Implement — structure_analyze 스키마·프롬프트에 cta 추가**

`shopping_shorts/structure_analyze.py` `_SCHEMA["properties"]`에 `tone` 다음 줄에 추가(required는 건드리지 않음):

```python
        "tone": {"type": "string"},
        "cta": {"type": "string"},
```

`_PROMPT`의 `- tone:` 항목 바로 다음에 지시 한 줄 추가:

```
- cta: 마무리 행동유도(CTA) 방식/문장. 예: 댓글유도("궁금하면 댓글"), 저장유도, 팔로우유도,
  핵심을 다 밝히지 않고 궁금증 남기기. 없으면 "".
```

- [ ] **Step 5: Run test to verify it passes**

Run: `cd shopping_shorts && python -m pytest tests/test_cta_element.py -v`
Expected: PASS (2 passed)

- [ ] **Step 6: Commit**

```bash
git -C .tracks/조합대본업그레이드 add shopping_shorts/script_generate.py shopping_shorts/structure_analyze.py shopping_shorts/tests/test_cta_element.py
git -C .tracks/조합대본업그레이드 commit -m "feat(조합대본업그레이드): CTA를 요소 목록·구조분석 스키마에 추가"
```

---

### Task 2: 조합 초안 서버 저장 → draft_id 부여 (app.py mix 엔드포인트)

**Files:**
- Modify: `shopping_shorts/app.py:2740-2745` (`api_produce_script_mix` 반환 직전)
- Test: `shopping_shorts/tests/test_mix_draft_id.py`

**Interfaces:**
- Consumes: `store.save_draft(...)`(Global Constraints 시그니처), `script_generate.generate_mix`.
- Produces: `POST /api/produce/script/mix` 응답의 각 draft에 `draft_id`(12자 hex) 포함. 저장은 `source_shortcode=None, parent_draft_id=None, edit_mode="mix"`.

- [ ] **Step 1: Write the failing test**

```python
# shopping_shorts/tests/test_mix_draft_id.py
"""조합 생성 초안이 서버에 저장돼 draft_id를 받는지 — 요소 업그레이드의 전제."""
import pathlib
import tempfile

import pytest
from fastapi.testclient import TestClient

from shopping_shorts import app as appmod
from shopping_shorts import script_generate
from shopping_shorts.store import Store


@pytest.fixture
def client(monkeypatch):
    tmp = pathlib.Path(tempfile.mkdtemp()) / "t.db"
    monkeypatch.setattr(appmod, "DB_PATH", str(tmp))
    Store(str(tmp))  # 스키마 생성
    monkeypatch.setattr(
        script_generate, "generate_mix",
        lambda sources, target_seconds=20, n=3: [
            {"hook": "훅1", "script": "대본1", "applied": "조합"},
            {"hook": "훅2", "script": "대본2", "applied": "조합"},
        ])
    return TestClient(appmod.app), str(tmp)


def test_mix_attaches_draft_id_and_saves(client):
    c, dbp = client
    body = {"sources": [
        {"shortcode": "a", "full_text": "가나다 대본 A"},
        {"shortcode": "b", "full_text": "라마바 대본 B"}], "target_seconds": 20, "n": 3}
    r = c.post("/api/produce/script/mix", json=body)
    assert r.status_code == 200, r.text
    drafts = r.json()["drafts"]
    assert drafts and all(d.get("draft_id") for d in drafts)
    # 서버에 실제로 저장됐는지
    saved = Store(dbp).get_draft(drafts[0]["draft_id"])
    assert saved and saved["script_text"] == "대본1" and saved["edit_mode"] == "mix"
```

- [ ] **Step 2: Run test to verify it fails**

Run: `cd shopping_shorts && python -m pytest tests/test_mix_draft_id.py -v`
Expected: FAIL (`draft_id` 없음 → assert 실패)

- [ ] **Step 3: Implement — 반환 직전 save_draft 루프 추가**

`shopping_shorts/app.py`의 `api_produce_script_mix` 마지막을 아래로 교체(현재 `return {"ok": True, "drafts": drafts}` 한 줄을 대체):

```python
    store = Store(DB_PATH)
    cid = _cid(request)
    for dr in drafts:
        did = uuid.uuid4().hex[:12]
        store.save_draft(did, cid, None, None, dr.get("hook", ""), dr.get("script", ""), None, "mix")
        dr["draft_id"] = did
    return {"ok": True, "drafts": drafts}
```

(`uuid`·`Store`·`_cid`는 app.py에 이미 import/정의됨 — 단일 경로 `api_wiki_generate`가 같은 패턴을 쓴다.)

- [ ] **Step 4: Run test to verify it passes**

Run: `cd shopping_shorts && python -m pytest tests/test_mix_draft_id.py -v`
Expected: PASS

- [ ] **Step 5: Commit**

```bash
git -C .tracks/조합대본업그레이드 add shopping_shorts/app.py shopping_shorts/tests/test_mix_draft_id.py
git -C .tracks/조합대본업그레이드 commit -m "feat(조합대본업그레이드): 조합 초안 save_draft로 draft_id 부여"
```

---

### Task 3: 요소 업그레이드 엔드포인트 `/api/wiki/draft/upgrade` (app.py)

**Files:**
- Modify: `shopping_shorts/app.py` (`api_draft_refine` 다음, `:1111` 부근에 새 함수 추가)
- Test: `shopping_shorts/tests/test_draft_upgrade.py`

**Interfaces:**
- Consumes: `store.get_draft`, `store.get_element_options`, `analyze_structure`, `script_generate.generate_variations`, `store.save_draft`, `script_generate.ELEM_KEYS/ELEM_LABELS`.
- Produces: `POST /api/wiki/draft/upgrade` body `{draft_id, elements:[key,...]}` → `{ok, draft_id, script_text, hook}`. 고른 요소만 `free`(변형), 나머지 `keep`(유지)로 `generate_variations(n=1)`. 새 버전은 `parent_draft_id=draft_id`, `edit_mode="upgrade"`.

- [ ] **Step 1: Write the failing test**

```python
# shopping_shorts/tests/test_draft_upgrade.py
"""요소 업그레이드 — 고른 요소만 free로 generate_variations 호출하고 새 버전 저장."""
import pathlib
import tempfile

import pytest
from fastapi.testclient import TestClient

from shopping_shorts import app as appmod
from shopping_shorts import script_generate
from shopping_shorts.store import Store


@pytest.fixture
def client(monkeypatch):
    tmp = pathlib.Path(tempfile.mkdtemp()) / "t.db"
    monkeypatch.setattr(appmod, "DB_PATH", str(tmp))
    st = Store(str(tmp))
    st.save_draft("d1", "cust", None, None, "옛훅", "옛 대본 전체", None, "mix")
    monkeypatch.setattr(appmod, "analyze_structure", lambda text: {"product_category": "레시피", "hook_type": "경고형"})
    captured = {}

    def fake_var(structure, full_text, elem_modes, category_lookup, **kw):
        captured["elem_modes"] = elem_modes
        captured["n"] = kw.get("n")
        return [{"hook": "새훅", "script": "새 대본", "applied": "훅 변형"}]

    monkeypatch.setattr(script_generate, "generate_variations", fake_var)
    return TestClient(appmod.app), str(tmp), captured


def test_upgrade_only_selected_element_is_free(client):
    c, dbp, captured = client
    r = c.post("/api/wiki/draft/upgrade", json={"draft_id": "d1", "elements": ["hook"]})
    assert r.status_code == 200, r.text
    j = r.json()
    assert j["ok"] and j["script_text"] == "새 대본"
    # 고른 hook만 free, n=1
    assert captured["elem_modes"] == {"hook": "free"} and captured["n"] == 1
    # 새 버전이 parent를 가리키며 저장됨
    saved = Store(dbp).get_draft(j["draft_id"])
    assert saved["parent_draft_id"] == "d1" and saved["edit_mode"] == "upgrade"


def test_upgrade_rejects_empty_elements(client):
    c, _, _ = client
    r = c.post("/api/wiki/draft/upgrade", json={"draft_id": "d1", "elements": []})
    assert r.status_code == 422


def test_upgrade_unknown_draft_404(client):
    c, _, _ = client
    r = c.post("/api/wiki/draft/upgrade", json={"draft_id": "nope", "elements": ["hook"]})
    assert r.status_code == 404
```

- [ ] **Step 2: Run test to verify it fails**

Run: `cd shopping_shorts && python -m pytest tests/test_draft_upgrade.py -v`
Expected: FAIL (404 for all — 엔드포인트 없음)

- [ ] **Step 3: Implement — 새 엔드포인트 추가**

`shopping_shorts/app.py`의 `api_draft_refine`(끝 `:1110`) 다음에 추가:

```python
@app.post("/api/wiki/draft/upgrade")
def api_draft_upgrade(request: Request, body: dict):
    """초안을 '요소 단위'로 업그레이드 — 고른 요소만 변형, 나머지 유지해 재생성.

    body: {draft_id, elements: [element_key, ...]}. element_key는 ELEM_KEYS 중 하나.
    초안을 analyze_structure로 요소 분해 → 고른 요소만 free(변형) → generate_variations(n=1)
    → 새 버전으로 저장(parent=draft_id, mode="upgrade")."""
    store = Store(DB_PATH)
    draft_id = body.get("draft_id", "")
    elements = [e for e in (body.get("elements") or []) if e in script_generate.ELEM_KEYS]
    cur = store.get_draft(draft_id)
    if not cur:
        return JSONResponse(status_code=404, content={"ok": False, "error": "해당 초안 없음"})
    if not elements:
        return JSONResponse(status_code=422, content={"ok": False, "error": "바꿀 요소를 하나 이상 고르세요"})
    base = cur.get("script_text") or ""
    structure = analyze_structure(base) or {}
    category_lookup = store.get_element_options(structure.get("product_category") or "")
    elem_modes = {e: "free" for e in elements}
    drafts = script_generate.generate_variations(
        structure, base, elem_modes, category_lookup, mode="remake", subject="", n=1)
    if not drafts:
        return JSONResponse(status_code=502, content={"ok": False, "error": "재생성 실패(Gemini 키 소진 또는 오류) — 잠시 후 재시도"})
    dr = drafts[0]
    new_id = uuid.uuid4().hex[:12]
    labels = ", ".join(script_generate.ELEM_LABELS[e] for e in elements)
    store.save_draft(new_id, _cid(request), cur.get("source_shortcode"), draft_id,
                      dr.get("hook", ""), dr.get("script", ""), "요소변형: " + labels, "upgrade")
    return {"ok": True, "draft_id": new_id, "script_text": dr.get("script", ""), "hook": dr.get("hook", "")}
```

- [ ] **Step 4: Run test to verify it passes**

Run: `cd shopping_shorts && python -m pytest tests/test_draft_upgrade.py -v`
Expected: PASS (3 passed)

- [ ] **Step 5: Commit**

```bash
git -C .tracks/조합대본업그레이드 add shopping_shorts/app.py shopping_shorts/tests/test_draft_upgrade.py
git -C .tracks/조합대본업그레이드 commit -m "feat(조합대본업그레이드): /api/wiki/draft/upgrade 요소 업그레이드 엔드포인트"
```

---

### Task 4: produce.html 조합 초안 카드 — 텍스트박스 + 요소 버튼 + 업그레이드/자유입력 배선

**Files:**
- Modify: `shopping_shorts/static/produce.html:1599-1618` (`renderDrafts`/`useDraft`)
- Test: `shopping_shorts/tests/test_produce_mix_upgrade.py`

**Interfaces:**
- Consumes: `/api/wiki/draft/upgrade`, `/api/wiki/draft/refine`, `/api/wiki/draft/edit`, `STATE`, `setScriptMode`, `refreshFinalPeek`, `saveWork`, `lenText`, `esc`, `logPick`.
- Produces: JS 함수 `renderDrafts`(요소 버튼 포함), `mixToggleElem`, `mixOnEdit`, `mixUpgrade`, `useDraftCard`, 상수 `MIX_ELEMS`.

- [ ] **Step 1: Write the failing test**

```python
# shopping_shorts/tests/test_produce_mix_upgrade.py
"""조합 초안 카드에 요소 버튼·업그레이드 배선이 실렸는지(문자열 앵커 — 이 파일의 다른 mix 테스트와 동일 방식)."""
import pathlib

PRODUCE_HTML = pathlib.Path(__file__).resolve().parents[1] / "static" / "produce.html"


def test_mix_upgrade_wiring_present():
    t = PRODUCE_HTML.read_text(encoding="utf-8")
    assert "const MIX_ELEMS=" in t
    assert "function mixUpgrade(" in t
    assert "function mixToggleElem(" in t
    assert "function useDraftCard(" in t
    assert "/api/wiki/draft/upgrade" in t
    # 요소 8개 키가 버튼 목록에 다 있는지
    for k in ["hook", "development", "twist", "characters", "appeal", "tone", "devices", "cta"]:
        assert "'%s'" % k in t or '"%s"' % k in t
```

- [ ] **Step 2: Run test to verify it fails**

Run: `cd shopping_shorts && python -m pytest tests/test_produce_mix_upgrade.py -v`
Expected: FAIL (`MIX_ELEMS`/`mixUpgrade` 없음)

- [ ] **Step 3: Implement — renderDrafts/useDraft 교체 + 신규 함수**

`shopping_shorts/static/produce.html`에서 현재 `renderDrafts`(1600–1609)와 `useDraft`(1610–1618) 두 함수를 아래로 교체:

```javascript
// 요소 버튼 목록 — 키는 백엔드 ELEM_KEYS와 일치, 순서만 사용성순.
const MIX_ELEMS=[['hook','훅'],['development','전개방식'],['twist','발상전환'],['characters','주변인물'],['appeal','어필포인트'],['tone','말투'],['devices','설득장치'],['cta','마무리/CTA']];
// 초안 렌더(조합 경로). 카드마다 편집 textarea + 요소 버튼 + 업그레이드/자유입력.
function renderDrafts(drafts, boxId){
  window._drafts = drafts;
  const eb='padding:5px 9px;margin:2px;border:1px solid var(--line);border-radius:14px;background:#0d1420;color:var(--txt);cursor:pointer;font-size:12px';
  document.getElementById(boxId).innerHTML = (drafts||[]).map((dr,i)=>`
    <div class="mixdraft" data-draft-id="${dr.draft_id||''}" data-idx="${i}" style="border:1px solid var(--line);border-radius:10px;padding:12px;margin-bottom:8px;background:#0d1420">
      <div style="font-weight:600;color:#9dc4ff;margin-bottom:4px">초안 ${i+1} · ${esc(dr.hook||'')}</div>
      <textarea class="mixtext" style="width:100%;min-height:90px;background:#0d1420;border:1px solid var(--line);color:var(--txt);border-radius:8px;padding:8px;font-size:13px;line-height:1.5" onblur="mixOnEdit(this)">${esc(dr.script||'')}</textarea>
      <div class="hint" style="margin:6px 0">${esc(dr.applied||'')} · <span class="mixlen">${lenText(dr.script||'')}</span></div>
      <div class="hint" style="margin:6px 0 4px">✨ 손볼 요소를 고르고 [업그레이드] — 여러 개 고르면 전부 새로 씁니다(나머지는 유지)</div>
      <div class="mixelems">${MIX_ELEMS.map(([k,l])=>`<button type="button" class="mixelem" data-key="${k}" data-on="" style="${eb}" onclick="mixToggleElem(this)">${l}</button>`).join('')}</div>
      <div style="display:flex;gap:6px;margin-top:6px;flex-wrap:wrap">
        <input type="text" class="mixinst" placeholder="추가 지시(선택): 예) 마지막을 궁금하게" style="flex:1;min-width:120px;background:#0d1420;border:1px solid var(--line);color:var(--txt);border-radius:8px;padding:8px">
        <button type="button" class="btn-next" style="padding:8px 14px" onclick="mixUpgrade(this)">🔄 업그레이드</button>
      </div>
      <button class="btn-next" style="padding:8px 14px;margin-top:8px;background:linear-gradient(90deg,#1c5a2e,#0d3a1c)" onclick="useDraftCard(this)">이 대본 사용 →</button>
    </div>`).join('');
}
function mixToggleElem(btn){
  const on=btn.dataset.on==='1'; btn.dataset.on=on?'':'1';
  btn.style.background=on?'#0d1420':'var(--accent2,#3a6df0)'; btn.style.color=on?'var(--txt)':'#fff';
}
function mixOnEdit(ta){
  const card=ta.closest('.mixdraft'); const i=+card.dataset.idx;
  if(window._drafts&&window._drafts[i]) window._drafts[i].script=ta.value;
  const lp=card.querySelector('.mixlen'); if(lp) lp.textContent=lenText(ta.value);
  const did=card.dataset.draftId;
  if(did){ fetch('/api/wiki/draft/edit',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({draft_id:did,script_text:ta.value})}).then(r=>r.json()).then(d=>{ if(d.ok) card.dataset.draftId=d.draft_id; }).catch(()=>{}); }
}
async function mixUpgrade(btn){
  const card=btn.closest('.mixdraft'); const did=card.dataset.draftId;
  if(!did){ alert('이 초안은 서버에 저장된 버전이 없어 업그레이드를 지원하지 않습니다. 직접 편집하거나 확정하세요.'); return; }
  const elements=[...card.querySelectorAll('.mixelem[data-on="1"]')].map(b=>b.dataset.key);
  const inst=(card.querySelector('.mixinst').value||'').trim();
  if(!elements.length && !inst){ alert('손볼 요소를 고르거나 지시문을 입력하세요.'); return; }
  const ta=card.querySelector('.mixtext');
  btn.disabled=true; const o=btn.textContent; btn.textContent='⏳ 생성 중…';
  try{
    let d;
    if(elements.length){
      d=await (await fetch('/api/wiki/draft/upgrade',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({draft_id:did,elements})})).json();
    }else{
      d=await (await fetch('/api/wiki/draft/refine',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({draft_id:did,mode:'rewrite',instruction:inst})})).json();
    }
    if(!d.ok){ alert(d.error||'업그레이드 실패'); return; }
    ta.value=d.script_text; card.dataset.draftId=d.draft_id;
    const i=+card.dataset.idx; if(window._drafts&&window._drafts[i]) window._drafts[i].script=d.script_text;
    const lp=card.querySelector('.mixlen'); if(lp) lp.textContent=lenText(d.script_text);
    card.querySelectorAll('.mixelem[data-on="1"]').forEach(b=>{ b.dataset.on=''; b.style.background='#0d1420'; b.style.color='var(--txt)'; });
    card.querySelector('.mixinst').value='';
  }catch(e){ alert('네트워크 오류'); }
  finally{ btn.disabled=false; btn.textContent=o; }
}
function useDraftCard(btn){
  const card=btn.closest('.mixdraft'); const i=+card.dataset.idx;
  const ta=card.querySelector('.mixtext');
  STATE.script=(ta?ta.value:'') || ((window._drafts||[])[i]||{}).script || '';
  STATE.script_from_wiki='mix';   // 조합 초안 = 위키 대본 여러 개에서 나옴. 원본을 하나로 특정할 수 없다.
  setScriptMode('manual'); refreshFinalPeek(); saveWork();
  try{ logPick('S2', {picked: i, source: 'mix'}); }catch(e){}
}
```

- [ ] **Step 4: Run test to verify it passes**

Run: `cd shopping_shorts && python -m pytest tests/test_produce_mix_upgrade.py -v`
Expected: PASS

- [ ] **Step 5: 회귀 — 기존 mix 관련 produce 테스트가 여전히 통과하는지**

Run: `cd shopping_shorts && python -m pytest tests/test_produce_mix_regen.py tests/test_produce_mix_seconds_js.py -v`
Expected: PASS (앵커 `function genMix(){`·`function useDraft`는 제거됨 — `useDraft` 앵커를 쓰는 테스트가 있으면 그 테스트가 새 함수명을 참조하도록 같이 고친다. 실패하면 해당 테스트의 앵커를 `useDraftCard`로 갱신.)

- [ ] **Step 6: Commit**

```bash
git -C .tracks/조합대본업그레이드 add shopping_shorts/static/produce.html shopping_shorts/tests/test_produce_mix_upgrade.py
git -C .tracks/조합대본업그레이드 commit -m "feat(조합대본업그레이드): 조합 초안 카드에 요소 버튼·업그레이드 배선"
```

---

### Task 5: 라이브 그라운딩(서버 실호출) — 요소 업그레이드가 실제로 도는지

로컬은 Gemini 키 경로가 없을 수 있어 단위 테스트는 mock이다. 실동작은 서버에서 확인한다.

**Files:** (없음 — 검증 단계)

- [ ] **Step 1: 병합 게이트 통과로 라이브 반영**

Run: `cd .tracks/조합대본업그레이드 && python ../../tools/track.py finish 조합대본업그레이드`
(main 폴더 기준이면 `py tools/track.py finish 조합대본업그레이드`)
Expected: 게이트 통과(신규 실패 0) → main 병합 → 3분 뒤 라이브. 실패 시 라이브 무사, 고치고 재시도.

- [ ] **Step 2: 서버 실측 — 조합 초안 draft_id 부여 확인**

라이브에서 조합 생성 1회 실행 후, 서버 DB에 mix 초안이 저장됐는지:
```bash
ssh -i C:/Users/TheRose/crawling_bot_client/LightsailDefaultKey-ap-northeast-2.pem ubuntu@3.39.179.148 \
  "python3 - <<'PY'
import sqlite3, glob
for f in glob.glob('/home/ubuntu/lotto-stock-wiki/shopping_shorts/**/*.db', recursive=True):
    try:
        c=sqlite3.connect(f)
        if c.execute(\"SELECT name FROM sqlite_master WHERE name='script_drafts'\").fetchone():
            print(f, c.execute(\"SELECT count(*) FROM script_drafts WHERE edit_mode='mix'\").fetchone())
    except Exception: pass
PY"
```
Expected: `edit_mode='mix'` 행이 1개 이상.

- [ ] **Step 3: 사장님 라이브 육안**

제작소 → 조합 생성 → 초안 카드에서 [훅][마무리/CTA] 버튼 켠 뒤 [🔄 업그레이드] → 대본이 그 요소만 바뀌어 새로 써지는지. 자유입력창에 "마지막을 궁금하게" 쳐도 되는지.

---

## Self-Review

**Spec coverage:**
- 요소 8개(기존 7+CTA) → Task 1 ✅
- 조합 초안 draft_id 부여(근본 구멍) → Task 2 ✅
- analyze→generate_variations 재사용 요소 업그레이드 → Task 3 ✅
- 렌더러에 요소 버튼+복수선택(전체 적용)+자유입력 → Task 4 ✅
- 유지/변형 2단계만(강화 세분화 비목표) → Task 3 elem_modes={free}, 나머지 keep 기본 ✅
- 라이브 그라운딩 → Task 5 ✅

**비목표(스펙 §YAGNI 준수):** 요소 3단계 세분화·초안 3개 일괄·요소 드래그 UI 없음. **단일 화면(`pmRenderDrafts`) 요소 버튼 패리티는 이번 범위 밖** — 단일 경로엔 이미 자유입력 refine이 있고, 필요 시 Task 4의 함수를 재사용하는 후속 작업으로 붙인다(hot 파일 seam 최소화).

**Placeholder scan:** 없음(모든 스텝 실제 코드/명령 포함).

**Type consistency:** `save_draft` 8-인자 시그니처 Task 2·3 일치. `generate_variations(structure, full_text, elem_modes, category_lookup, mode=, subject=, n=)` Task 3·테스트 일치. 프론트 `MIX_ELEMS` 키 ↔ 백엔드 `ELEM_KEYS`(cta 포함) 일치. 엔드포인트 경로 `/api/wiki/draft/upgrade` Task 3·4 일치.
