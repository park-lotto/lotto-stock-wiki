# 키사망차단 — 죽은 키를 다시 부르지 않게, 429는 표시로 분산

## 2026-09-03 (완료·라이브 12b02af79)

### 무엇이 문제였나 (서버 reference.db/api_events 실측)

| 증상 | 실측 |
|---|---|
| 죽은 키 재호출 | `…nIWJaw`가 09-01 22:10부터 403 PERMISSION_DENIED인데 09-02 **92건** · 09-03 **21건** 다시 호출 |
| rpm 쏠림 | 09-03 rpm 429 **1,062건 중 874건(82%)**이 키 3개(JN_aSw·TmqZTA·hLyVig)에 몰림. 쓰는 키는 92개 |
| (별건) ElevenLabs | 09-01 402 57건 · 09-02 402 112건 + auth_dead 80건 = 잔액 소진. **09-03은 실패 0 = 회복됨** |

**뿌리는 하나였다**: `dashboard/server.py:_gemini_text`가 실패해도 **아무 표시를 남기지 않는다.**
403이든 429든 `continue`만 해서 다음 요청이 같은 목록을 받아 같은 키를 또 때린다.
`rotated()`는 시작점만 옮길 뿐이라 이걸 못 막는다. 그리고 `pipeline/atoms/key_vault.py`에는
**'영구 사망' 개념이 아예 없었다** — 쇼핑쇼츠 `comment_gen`은 09-02에 지문 기반
`_mark_key_dead`를 받았는데 vault(대시보드)만 그 처방을 못 받았다.

### 어떻게 고쳤나 — 대기 없이

```
key_vault.mark_failure(key, exc)      ← 표시 판정을 여기서만 한다(0순위-B)
   401/403 → mark_dead(지문)          영구. 만료 없음
   429     → mark_exhausted(retry_delay_seconds(exc))   서버가 준 시각만큼만
   그 외   → 아무것도 안 한다(503 등)
```
표시만 남기면 다음 호출의 `get_live_keys`가 그 키를 빼준 목록을 주므로 **대기 0으로** 분산된다.
페이서(`pick_paced_key`, 키당 12초)는 쓰지 않았다 — 대화형 화면이 느려진다. 오히려 죽은 키에서
헛되이 왕복하던 시간이 사라져 빨라진다.

- `get_live_keys`가 사망 지문을 거른다 / `without_dead()`로 **최후 폴백 경로**도 막았다
  (`_gemini_interactive_keys` · `_summary_keys` · `_briefing_keys` — ★briefing은 `get_keys`를
   직접 써서 잠금조차 통째로 무시하고 있었다)
- `vector_db.embed_text`도 같은 함정(429가 표시 없이 continue, 403은 raise) → `mark_failure`로 통일
- `mark_exhausted` 상한 **6h → 25h**. 코드 주석이 예고했던 함정이 그대로 터졌다 —
  `retry_delay_seconds`를 넘기기 시작했으므로 일일 소진(자정까지 최대 24h)이 깎이면 안 된다

### 검증
- 신규 `shopping_shorts/tests/test_key_vault_dead.py` 4건 + 기존 키 테스트 28건 통과
  (`test_lock_is_clamped`는 6h→25h로 갱신 — 설계가 바뀐 것이지 회귀가 아니다)
- **서버 실동작 확인**(배포 후, tmp 상태파일): 403 → live에서 빠지고 `dead_keys`에 지문 기록,
  429 → `exhausted`에 만료ts 기록. `auto_deploy.log 10:07:02 stockbrain 재시작완료 12b02af79`

⏭ 다음
- **`GEMINI_API_KEY_9`(서버 `.env`) = 죽은 키 `…nIWJaw`.** 코드가 이제 자동 제외하지만
  .env에서 빼는 건 사람 몫이다(사장님 판단 대기). 로컬 .env엔 없다 — 서버에만 있다(서버 38 / 로컬 15)
- 배포 후 실측할 것: 내일 `auth_dead` 재호출이 0으로 떨어지는지, rpm 상위 키 편차가 줄었는지
  ```sql
  select key_tail,outcome,count(*) from api_events where day='2026-09-04' and outcome!='ok' group by 1,2 order by 3 desc;
  ```
- 쇼핑쇼츠 web/worker는 "고객 접속 중"으로 재시작 연기됐다 → `vector_db` 변경은 다음 재시작 후 적용
