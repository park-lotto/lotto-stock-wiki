# 자막제거2단계 — 핸드오프

**소유 트랙**: 자막제거2단계 — 이 파일은 이 트랙 세션만 수정한다. 다른 트랙은 읽기만.

## 2026-08-27 — 2단계 자막제거를 완성본 1편 청소로 (완료·라이브)

사장님 제보: "자막제거가 우리 어제 수정하고나서 더 오래걸려", "3단계 완성본 30초만
짤라서 돌리는건데 소스별로 안하고".

### 원인 (서버 워커 로그 실측)
`journalctl -u 'shopping-shorts-worker@*' | grep vmake-ai` 에 전부 찍혀 있다.
(웹 서비스 로그가 아니라 **워커** 로그다 — 여기서 한참 헤맸다)

- 08-26에 만든 완성본 1편 청소(`_final_clean_fn`)를 **`run_render`(3단계)에만** 달았다.
  `run_clean_sources`(2단계 버튼)는 `_FINAL_CLEAN`을 보지도 않고 늘 소스별/합본을 청소.
- 2단계를 누르면 `clean_sources`가 채워지고 → 3단계는 `already=True`로 완성본 경로를
  **건너뛴다**. 즉 2단계를 쓰는 사람에겐 08-26 개선이 통째로 없었다.
- 08-25 커밋 `3a573e381`(과금 1회로 맞추려 소스를 이어붙임)이 2단계에 그대로 걸려
  ① 병렬(ThreadPoolExecutor) → 직렬, ② 최대 해상도 업스케일로 파일이 부풂.

실측: `join_all.mp4` 569MB → **595초(10분)** / 27MB → 139초 / 37MB → 200초

### 고친 것
- `run_clean_sources`: 소스를 안 건드리고 **조립한 완성본 1편**을 `clean_fn` 자리에서 청소.
  `clean_sources`를 **일부러 비워 둔다** → 3단계가 같은 완성본 경로를 타고, 편성이 같으면
  `final_clean_{sig}.mp4` 재사용으로 재과금 0 (2·3단계 work 경로가 같음을 확인).
- `assemble_clean_video(clean_fn=...)`: 청소된 소스가 없어도 원본으로 조립해 그걸 청소.
  clean_fn을 쓸 땐 예외를 **안 삼킨다** — 삼키면 유료 청소 실패가 ready로 남는다.
- `_clean_strategy(job)` 하나로 판단 단일화(0순위-B). `_FINAL_CLEAN`을 직접 읽는 곳이
  늘면 테스트가 잡는다(grep 계약).
- 서버 env `MT_AI_POLL_EXTEND_STEP_MS=10000` — 완료를 늦게 알아채던 손해 30초 → 10초.

커밋: `93cc90e98`(본체) · `cb2f12b00`(판단 단일화) → main 병합, 워커 10:03:06 재시작 확인.

### 실측 결과 (배포 후)
**30.5초 완성본 → 130.2초(2분 10초).** 569MB 합본 595초 대비 **4.6배**.
★"VMake 1초당 9초"(08-26 커밋 기재)는 **틀린 값**이다 — 실측은 **1초당 약 4.3초**.
★VMake는 동시 요청에 줄서기 없음(00:46~00:49 두 건이 51초 겹쳐 돌고 둘 다 완료).

### ⏭ 다음
- 사장님 실사용 체감 확인. 다르면 그 job_id로 재측정.
- 3단계 직행 경로(2단계 안 거침)도 같은 방식으로 실측 대조 — 아직 안 재봤다.
- `SHORTS_CLEAN_SPAN`(쓰이는 구간만 자르기)은 켜져 있으나 완성본 경로에선 무의미해졌다.
  소스별 폴백 때만 쓰인다 — 정리할지 판단 필요.
- 되돌림: `SHORTS_CLEAN_FINAL=0` → 옛 소스별 경로(테스트로 고정돼 있다).

### 2026-08-27 (이어서) — 파생 회귀 3건 + 캡컷 B안 (완료·라이브)

★clean_sources를 "자막제거 했나"의 판정으로 쓰던 곳이 여럿이라, 그걸 비우자 조용히 번졌다.
  한 곳 고치면 다음이 나오는 식으로 세 번 반복했다 — 판단이 흩어져 있던 탓(0순위-B).

| 자리 | 증상 | 고침 |
|---|---|---|
| app.py clean_thumb | AFTER 칸 404(검은 화면) | 완성본에서 뽑기 |
| app.py poster·beatframe | 5·6단계 배경에 원본 자막·워터마크 | 〃 |
| app.py capcut | 캡컷에 원본 자막이 나감 | 완성본을 컷별로 잘라 넘김(B안) |

판단을 뽑아낸 함수 (여기만 고치면 전부 따라온다):
  mix_pipeline._clean_strategy(job)      — 완성본이냐 소스별이냐
  mix_pipeline._beat_materials(beat)     — 화면 재료 판정(scene_override 우선+alternates)
  app._clean_frame_src(job, work, i)     — 청소된 화면을 어디서 뜰지 + 캐시 태그
  ★셋 다 "또 손으로 적으면 실패하는" grep 계약 테스트가 걸려 있다.

캡컷 B안 (사장님 확정): 완성본을 _beat_timeline의 t0·dur로 잘라 조각으로 넘긴다.
  VMake 재호출 0 = 추가 과금 0. 자르기 31초(실측 6조각), 재사용 시 0초.
  못 하는 것: 캡컷에서 컷을 원본 범위 **밖으로 늘리기**(조각 뒤에 여분이 없다).
  A안(캡컷 때만 소스별 재청소)은 1편 10P가 되어 기각. 필요하면 job 옵션으로 되살릴 수 있다.
  ★자르기 실패는 원본 폴백이 아니라 **차단**한다 — 자막 남은 결과물이 조용히 나가면 안 된다.

실측(라이브 검증): 조각 6개 cc0 5.67초/cc1 8.00초/cc2 5.20초 — 타임라인과 일치,
프레임에 자막·워터마크 없음. 웹 재시작 11:14:21, HTTP 200.

### ⏭ 다음 (갱신)
- 사장님 캡컷 실사용 확인 — 컷 늘리기가 실제로 필요하면 A안을 job 옵션으로.
- "장면 한두 개가 아예 안 지워졌다" 제보 → job id 받으면 갈라본다:
    ① SHORTS_CLEAN_SPAN(쓰이는 구간만 자르기) 판정 어긋남 → **완성본 경로에선 이미 해소**
    ② VMake가 못 지운 경우 → 우리가 못 고친다
- SHORTS_CLEAN_SPAN은 소스별 폴백에서만 쓰인다 — 정리할지 판단 필요.
