# 기획 — 숏템 제작소 단계 개편 (8단계 → 9단계)

> 2026-08-16 · 트랙 `장면라벨` 세션이 작성 · **사장님이 2단계 세션에 전달할 문서**
> 대상 파일: `shopping_shorts/static/produce.html` (단계 구조가 전부 이 한 파일에 있다)

---

## 1. 무엇을 바꾸나 (사장님 지시)

**대본 쪽을 강화하려고 단계를 하나 늘린다.** 지금 1단계에 "영상 고르기"와 "AI 대본 선택"이
한 화면에 뭉쳐 있어 대본이 제 자리를 못 갖는다.

| | 지금 (8단계) | 바꾼 뒤 (9단계) |
|---|---|---|
| 1 | 영상/대본출력 | **영상추출/분석** ← 소스별 분석 카드 |
| 2 | 영상대본MIX | **대본** ← 신설(대본 고르기·고치기·강화) |
| 3 | 고품질 자막제거 | **영상대본MIX** ← 장면 편집 |
| 4 | TTS음성 | 고품질 자막제거 |
| 5 | 자막꾸미기 | TTS음성 |
| 6 | 썸네일 | 자막꾸미기 |
| 7 | SEO해시테크 | 썸네일 |
| 8 | 최종렌더 | SEO해시테크 |
| 9 | — | 최종렌더 |

### 분담 (사장님 결정)
- **이 세션(장면라벨 트랙)**: 1단계(영상추출/분석) · 3단계(영상대본MIX)
- **다른 세션**: 2단계(대본)

---

## 2. ★먼저 알아야 할 것 — 지금 코드가 어떻게 생겼나 (전부 실측)

### 2-1. 표시 순서와 실제 패널이 이미 분리돼 있다 (다행이다)

```js
// produce.html:1128~1131
const STEP_LABELS = ["영상/대본출력","영상대본MIX","고품질 자막제거","TTS음성",
                     "자막꾸미기","썸네일","SEO해시테크","최종렌더"];
const ORB_TO_PANEL = [0, 7, 1, 2, 3, 4, 5, 6];      // 표시 오브 → 실제 data-step
const PANEL_TO_ORB = Object.fromEntries(ORB_TO_PANEL.map((p,o)=>[p,o]));
```

**`data-step`(물리 패널 번호)은 화면 순서와 다르다.** 화면 2번째 = `data-step="7"`.
2026-07-23에 매칭 패널을 새로 넣으면서 물리 번호를 안 건드리려고 매핑을 둔 것이다.

→ **이 매핑 덕분에 순서 바꾸기가 비교적 안전하다.** 새 패널은 `data-step="8"`로 붙이고
   `ORB_TO_PANEL`만 고치면 된다. **기존 패널의 data-step은 절대 다시 번호 매기지 마라.**

### 2-2. ⚠️ 그런데 `cur`(패널 번호)로 하드코딩된 분기가 많다 — 여기가 지뢰

```js
// produce.html:1165~1171 — showPanel()
if(cur===1) refreshSub();       // 자막제거
if(cur===2) loadTtsBeats();     // TTS
if(cur===3) initHeadcopy();     // 자막꾸미기
if(cur===4) initThumb();        // 썸네일
if(cur===5) initSeo();          // SEO
if(cur===6) refreshFinal();     // 최종렌더
if(cur===7) initMatch();        // 매칭(장면 편집)
```

그리고 `jump(n)`이 박힌 곳(**n은 오브 인덱스**, data-step 아님):
```
:4366  jump(1)   // 매칭 완료 → 화면 붙이기로 자동 이동
:5599  jump(3)   // 꾸미기로
:6077  jump(0)   // "1단계로 가기"
:6109  jump(0)   // "1단계에서 영상 매칭 확인"
:6262  jump(0)   // "영상 매칭을 다시"
```

**`data-step`을 안 바꾸면 `cur===n` 분기는 손댈 필요가 없다.**
**`jump(n)`은 오브 인덱스라 순서가 바뀌면 전부 재검토 대상이다.** 이게 핵심 위험이다.

### 2-3. 게이트(stepLocked) — 과금 안전장치라 반드시 같이 손봐야 한다

```js
// produce.html:5044~5053
function stepLocked(i){        // i = data-step (패널 번호)
  if(i === 7) return false;    // 매칭 패널은 예외(여기서 미리보기를 '만드는' 자리)
  return i >= 1 && !!MIX_JOB && !canGoNext();
}
```
목적: **유료 자막제거 우회 방지** — 미리보기 확인 전엔 뒷 단계로 못 간다.
새 패널(대본)을 넣으면 이 예외 목록에 넣을지 판단이 필요하다(아래 §4).

### 2-4. 1단계 패널은 좌우 2단이라 가르기 쉽다

```
<section class="panel" data-step="0">        produce.html:567
  ◀ 왼쪽 : #poolList(영상 재료 그리드) · URL 직접 추가       → 새 1단계에 남는다
  결과 ▶ : #aiPick(AI PICK 대본) · 고급(스마트믹스 토글)    → 새 2단계로 간다
```

---

## 3. 순서 — ★골격을 먼저, 내용은 나중에 (충돌 방지의 핵심)

**두 세션이 같은 파일을 동시에 고치면 `STEP_LABELS`·`ORB_TO_PANEL` 같은 줄에서 반드시
병합 충돌이 난다.** 그래서 이렇게 간다:

```
[1] 장면라벨 세션이 "골격"만 만들어 라이브에 올린다   ← 먼저, 짧게
      · STEP_LABELS 9개로
      · ORB_TO_PANEL 재배열
      · <section class="panel" data-step="8"> 빈 패널 추가(= 새 2단계 대본 자리)
      · jump(n) 5곳 재검토
      · stepLocked 판단
      · ★빈 패널엔 "여기는 대본 단계입니다(작업 중)" 한 줄만 넣는다
[2] 골격이 main에 들어간 뒤 두 세션이 각자 자기 칸을 채운다  ← 이때부터 병렬
      · 장면라벨 세션 : data-step="0"(영상추출/분석) · data-step="7"(영상대본MIX)
      · 대본 세션     : data-step="8"(대본) — 빈 패널 안쪽만 채우면 된다
```

**[2]에서는 서로 다른 `<section>` 안만 고치므로 충돌이 안 난다.**

---

## 4. 판단이 필요한 것 (골격 만들 때 결정)

### 4-1. 새 2단계(대본)를 게이트에서 어떻게 다룰까
지금 `stepLocked`는 "MIX_JOB이 있고 미리보기 확인 전이면 1번 이상 전부 잠금"이다.
대본 단계는 **미리보기를 만들기 전에 거쳐야 하는 자리**라, 매칭 패널처럼 **예외로 열어둬야**
흐름이 막히지 않는다. → `if(i === 7 || i === 8) return false;` 로 가는 게 맞아 보인다.
(최종 판단은 골격 작업자가 실제 흐름을 태워보고.)

### 4-2. `jump(0)` 5곳의 문구
"1단계로 가기 / 1단계에서 영상 매칭 확인" 같은 안내가 있는데, 개편 뒤 **매칭은 3단계**다.
문구와 목적지를 함께 고쳐야 한다(안 고치면 "1단계로 가라"는데 거기 매칭이 없다).

### 4-3. `jump(1)`(:4366) — 매칭 완료 후 자동 이동
지금은 오브 1 = 매칭. 개편 뒤 오브 1 = 대본이므로 **오브 2로 바꿔야** 한다.
아니면 대본 단계를 거치게 할지(사장님 판단) — 흐름상 **대본 → 장면**이 맞으니
매칭 완료 후엔 **대본(오브 1)로 보내는 게** 자연스러울 수 있다.

---

## 5. 새 1단계에 들어갈 것 (장면라벨 세션이 준비 완료)

사장님이 보여주신 경쟁 프로그램 화면처럼, **소스 영상마다 분석 카드**를 보여준다.
그 재료가 되는 추출 필드를 **이미 만들어 뒀다**(트랙 `장면라벨`, 커밋 `0793a6604`):

```
source_brief = {
  product : "휴대용 에스프레소 추출기"          ← 무슨 제품인가
  role    : "에스프레소 추출 및 서빙 과정 시연"   ← 이 소스가 맡은 몫
  core    : "하트 모양 추출구를 통한 시각적 연출"  ← 요지 한 줄
  summary : "휴대용 버너로 가열해 …"             ← 2~3문장
}
segments[].label = "바텀업 추출 시연"            ← 장면마다 12자 짧은 이름
```

★**무자막·외국어 영상도 된다**(사장님 지적 사항) — 실측: 자막 글자수 **0자**인 소스에서
화면만 보고 위 4개를 전부 채웠다. 소스마다 `role`이 갈리는 것도 확인(s0="기능 시연 및
실사용 후기" vs s2="추출 및 서빙 과정 시연").

⚠️ **옛 작업(이미 태깅된 497건)에는 이 필드가 없다** → `{}`로 떨어진다(회귀 0).
새로 담는 영상부터 채워진다. 화면은 **비어 있을 때를 반드시 견뎌야 한다.**

---

## 6. 대본 세션에 전할 것 (요약)

```
□ 골격(9단계 재배열)은 장면라벨 세션이 먼저 만들어 main에 올린다 — 기다렸다 시작할 것
□ 골격이 올라가면 <section class="panel" data-step="8"> 안쪽만 채우면 된다
□ data-step 번호는 절대 다시 매기지 마라 — 화면 순서는 ORB_TO_PANEL이 정한다
□ 그 패널로 옮겨올 것: 지금 1단계 오른쪽의 #aiPick(AI PICK 대본) + 고급(스마트믹스 토글)
□ 대본 강화 항목은 사장님이 화면 보고 정하기로 함 (문장별 고치기·훅만 다시 뽑기·길이 조절 등)
□ 작업은 자기 트랙 폴더에서. main 폴더 파일은 건드리지 마라(흡수 사고)
```

---

## 7. 검증 (골격 작업자가 반드시)

```
□ 9개 오브가 순서대로 뜨는가 (라벨·번호)
□ 각 오브를 눌렀을 때 맞는 패널이 뜨는가 (ORB_TO_PANEL 매핑)
□ 기존 흐름이 안 깨지는가: 영상 담기 → 매칭 → 미리보기 → 음성 → … → 최종렌더
□ jump() 5곳이 의도한 자리로 가는가 (특히 :4366 자동 이동)
□ 게이트: 미리보기 확인 전 뒷 단계가 잠기는가 (과금 우회 방지가 살아 있는가)
□ 새로고침 뒤 작업 복원(_restoreWork)이 맞는 단계로 돌아오는가
□ pytest 기준선 대비 실패 증가 0
```
⚠️ `PANEL_COUNT`는 `document.querySelectorAll('.panel').length`라 패널을 추가하면
   자동으로 9가 된다 — 손으로 고치지 마라(주석이 그 이유를 설명하고 있다).
