# 작업 지시 — 장면매칭이 화면을 보게 하라

너는 이 폴더(`.tracks/장면그림`)에서만 일한다. 다른 폴더는 읽기만 하고 절대 고치지 마라.
다른 AI가 다른 트랙에서 동시에 일하고 있다.

## 배경 — 먼저 읽어라

`C:\Users\TheRose\Desktop\코덱스_장면매칭_안내문.md` 를 **전부** 읽어라.
방식은 이미 **②로 확정됐다**(실측 근거가 그 문서에 있다). 다시 고민하지 마라.

## 할 일 — 한 줄이다

`shopping_shorts/edit_plan.py`의 `verify_beat_screens()`가 지금 모델에게
**텍스트**(`scene_desc[:200]`)를 넘긴다. 이걸 **그림 1장**으로 바꿔라.

그림은 이미 있다:
- `shopping_shorts/app.py:5647` → `work/seg_thumbs/{seg_id}.jpg` 캐시 (서버에 1,339개)
- 없으면 `app.py:5515 _seg_strip_thumb` 또는 `frame_extract.extract_frame_at`으로 뽑는다
- 소스 영상은 최근 job **10/10**에 남아 있다(실측). 새로 받지 마라.

이미지를 싣는 호출 패턴은 이미 있다:
- `shopping_shorts/frame_script.py:461·541` → `types.Part.from_bytes`
- 키 회전: `frame_script.py:373 _call_with_key_rotation`
- ⚠️ `edit_plan._vault_call_once`는 **문자열 전용**이라 그대로는 이미지를 못 싣는다

## 반드시 지킬 것

```
□ 기존 계약을 깨지 마라 — tests/test_verify_screens.py 8개가 못 박고 있다:
    화면을 교체하지 않는다(표시만) / 스위치 꺼지면 호출 0 / visual_verb=False 제외 /
    모델이 죽어도 원본 그대로(fail-open)
□ 스위치는 계속 기본 OFF(screen_verify_enabled) — 검증 안 된 걸 라이브에 켜지 마라
□ 그림을 못 구하면 **종전대로 텍스트로** 돈다(회귀 0)
□ except Exception: pass 금지 — 예산 테스트가 있다. 이유를 stderr로 남겨라
□ 같은 판단을 두 곳에 적지 마라
□ 텍스트 판정과 이미지 판정을 **둘 다 로그로 남겨라** — 나중에 갈린 것만 사람이 본다
```

## 안내문이 짚은 함정 (그대로 옮긴다)

1. `verify_beat_screens`가 "화면 검증"이라 불리면서 텍스트만 본다 — 그게 지금 고칠 대상이다
2. 검증 뒤 `_fill_beat_screen_time`(edit_plan.py:4161)이 또 채우고, **`store.py:195·205`가
   계획을 읽을 때마다 `_fill`을 다시 돈다** — plan 안에서만 검증하면 store 출구에서 무효가 된다
3. `Recall@K`는 이 방식에선 못 잰다

## 끝나면

```
git -C . add -A
git -C . commit -m "..."
```
`finish`는 **하지 마라** — 사람이 확인한 뒤에 한다.
무엇을 고쳤고 무엇을 확인 못 했는지 마지막에 3줄로 보고하라.

## 하지 말 것

정답지 없이 "정확도가 올랐다"고 주장하기 / `fit`을 근거로 쓰기 /
낱말 겹침으로 자동 교체 붙이기 / 다른 트랙 폴더 건드리기
