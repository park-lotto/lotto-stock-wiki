# 인스타GraphQL복구 로그

## 2026-08-04
- 제작소 "대본 매칭 취소" 근본원인 규명: 세션 만료 아님(로그인된 username이 응답에 박혀 있음).
  인스타가 `/api/v1/media/{pk}/info/`를 폐지하고 JSON 대신 HTML 반환 → 소스 다운로드 전멸.
- GraphQL 가로채기 폴백(code 일치 검사 필수 — 광고풀이 섞여 와 엉뚱한 영상을 받는다) 구현.
  실측 라이브 실패 → 패치본 7.50MB 성공. 릴 5/5 확보.
- 다운로드가 `INSTAGRAM_SCRAPER` 킬스위치를 무시하고 Apify로 폴백하던 것 수정 —
  안 쓰는 Apify의 "17개 토큰 소진"이 진짜 원인을 가리고 있었다.
- 실패 사유 관리자 전용화(`error_detail`) + `ops_alert.py` 신규(관리자 쪽지·텔레, 쿨다운 30분).
- 라이브 배포 0cef0d736. ⏭ Gemini 키 1개 403 교체 필요.
