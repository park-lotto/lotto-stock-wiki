# 핸드오프 — 제작소 2단계 "대본" 화면 (트랙 `대본UI2단계`)

> 짝문서(정본): `기획_2단계_대본UI_페이블인계.md` · 목업 `mockup_2단계_대본.html`
> 골격(9단계 개편)은 `장면라벨` 세션이 이미 main에 올려놨다 — 이 트랙은 **`data-step="8"` 안쪽만** 만든다.

---

## 2026-08-16 (회사PC) — Phase 1 구현 ★여기부터 이어받기

### 착수 전 실측한 것 (추측 아님)
- `produce.html:1189` = `<section class="panel" data-step="8">` **골격 이미 main에 있음**, 안쪽은 placeholder 한 줄뿐이었다
- `STEP_LABELS[1]="대본생성"`, `ORB_TO_PANEL=[0,8,7,1,2,3,4,5,6]` → **화면 2단계 = 물리 패널 8**
- 서버 API 현황: `/api/script/styles`·`/api/script/style/templates` **2개만 존재**.
  기획 §4-2의 `[바꾸기] 후보 API`·`부분 재생성 API`·`대표 훅 문장`은 **아직 없다**(채널전략분석 세션 몫)

### 완료 — 패널 8 안쪽 전부 (커밋 `d457b2873`)
`shopping_shorts/static/produce.html` 한 파일만 수정. 새 클래스는 전부 `s2-` 접두사(충돌 0).

| 밴드 | 무엇 | 데이터 원천 |
|---|---|---|
| ① 씨앗 | 카드줄 + **상시 뼈대 밴드**(첫 문장·구조 스트립·훅유형·자수·길이·조회) | `window._aiPick` 하나 |
| ② 스타일 | 통짜 카드줄(이름·설명·구조 뱃지·근거편수·밀도·✅검증/⚠️타소재) + CTA 헌장 고정칩 | `GET /api/script/styles` |
| ③ 결과 대본 | **역할이 보이는 행**(🎣미끼 😰찔림 💡반전 ✨증거 📢약속), 문장 직접 편집, 탭/좌우 2모드 | 생성 응답의 `beats` |

- **게이트 = 성적표가 아니라 고칠 곳 안내**: 통과=`✅ 구조 지킴` 뱃지 하나 / 실패=못 지킨 항목 표시 +
  **그 칸에 빨간 테두리** + 그 자리에 고칠 버튼. (체크리스트 ✓✓✓ 안 만든다 — 기획 §3)
- **[바꾸기] = 은행 완성문장이 아니라 그 스타일의 문장틀**(`/api/script/style/templates`).
  `{가족}`·`{장소}` 빈칸을 `.s2-gap`으로 눈에 보이게 표시. (쌀밥 대본에 삼겹살 훅이 뜨던 문제 회피 — `app.py:9288` 설명)
- **생성은 기존 `/api/wiki/generate`를 그대로 쓴다**(pmRunGen과 같은 엔드포인트 = 판정·프롬프트 한 벌, 0순위-B).
  스타일 0개=AI에게 맡김(기존 경로), 1~2개=그만큼 안이 나온다.
- **확정 = `STATE.script` 세팅 + `jump(PANEL_TO_ORB[7])`.** ★`startProduceMix()`(매칭)를 **여기서 안 건다** — 기획 §6.

### 실측 검증 (로컬 127.0.0.1:8899, 실브라우저)
- 패널 렌더 OK / 씨앗 뼈대 밴드 값 정상(`훅 유형 단정형 · 원본 26자 · 41초 · 557만`)
- 역할 행 3개 라벨·문장 정상, A안 `✅ 구조 지킴` / B안 `⚠️ hook 틀 불일치` + **실패 칸만 빨간 테두리 1개** + fix 블록
- 좌우 모드 = grid 2칼럼(742.5px×2), 탭 숨김, 칼럼별 확정 버튼 노출
- 콘솔 에러 0건 / `node --check` 통과 / `pytest -k "produce or skm_ui or step"` **258 passed, 8 skipped**
- ⚠️ 트랙 폴더 DB는 비어 있어 `/api/script/styles`가 **0건**을 준다(코드 문제 아님, 데이터 문제). 스타일 카드 실물은 라이브에서 봐야 한다.

---

## ⏭ 남은 것

1. **사장님 육안 확인** — 라이브에서 1단계에 영상 담고 → 2단계로 이동 → 스타일 3종이 카드로 뜨는지, 대본 만들기까지.
2. **기획 §6 분리 미착수 (★장면라벨 세션과 맞물림)** — 1단계의 `#aiPick` 블록과 `startFromAiPick()`을 갈라야 한다.
   지금 `startFromAiPick`(`produce.html:3800~`)은 대본 확보 + `playTheater` + `startProduceMix()`가 한 덩어리다.
   2단계엔 **대본 확보만** 남고, 매칭·극장·`matchProgress`는 3단계로 가야 한다. **누가 언제 옮길지 그 세션과 확인 후 진행.**
   (지금은 1단계 버튼이 그대로 살아 있어 두 경로가 공존한다 — 회귀는 없지만 최종 그림은 아니다)
3. **서버가 만들어 줘야 하는 것**(채널전략분석 세션 몫, 오면 바로 붙일 자리 준비됨):
   - 부분 재생성(`[훅만 다시]`) — 지금은 "전체 다시 만들기"만 있다
   - 틀의 `{빈칸}` AI 자동 채움 — 지금은 틀을 넣고 사장님이 빈칸을 직접 고친다
   - 스타일 카드용 대표 훅 문장·썸네일 / 씨앗↔스타일 '결 맞춤' 판정(`si-style` 칩은 데이터가 없어 **숨겨뒀다** — 없는 걸 있는 척 안 채운다)
4. 스타일 2개 고정 vs 1개 허용 — 지금은 **0~2개 자유**(0=AI에게 맡김). 기획 §6 "아직 정할 것".
