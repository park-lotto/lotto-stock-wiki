# 해외HOT Apify 발굴전환 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 해외HOT 발굴 소스를 레딧 → 틱톡/CN(샤오홍슈·도우인) Apify로 전환하고, "안 터진 + 급상승(참여속도)" 깔때기와 제목 있는 카드 UI로 재설계한다.

**Architecture:** 기존 자산(`apify_client` 토큰로테이션 · `tiktok_search.search_full` · `gap_check` 선점 · `store`/job/폴링/🌍탭) 재활용. 신규는 CN `search_full` 2개 · 참여속도 전용 빌더 `build_overseas_items` · 안터진 상한 필터 · CN gap_check 번역 · job 재작성 · 카드 UI.

**Tech Stack:** Python 3.12, pytest, Apify(clockworks~tiktok-scraper, zen-studio~douyin-search-scraper, zen-studio~rednote-search-scraper), 순수 JS(index.html).

**설계:** `docs/superpowers/specs/2026-07-26-해외HOT-Apify발굴전환-design.md`
**작업 폴더:** `.tracks/해외HOT`. 커밋은 `git -C .tracks/해외HOT`.

---

## 스파이크 결과 (Task 1 완료 — 2026-07-26 서버 실측)

| | TikTok | 도우인 | 샤오홍슈 |
|---|---|---|---|
| 조회수 | ✅ playCount 실값(220만) | ❌ playCount=0 | ❌ 필드 없음 |
| 좋아요 | ✅ | ✅ diggCount(45K) | ✅ liked_count(5K) |
| 댓글 | ✅ | ✅ commentCount | ✅ comments_count |
| 수집 | (미추출) | ✅ collectCount(40K) | ✅ collected_count |
| 공유 | (미추출) | ✅ shareCount | ✅ shared_count |
| 게시시각 | `createTimeISO`(ISO) | `createTime`(유닉스), ISO=None | `timestamp`(유닉스) |
| video_id | `video_id`(=id) | `id` | `id` |

**결론(설계 반영):**
- CN은 조회수를 안 줌 → 랭킹 기준을 **참여속도 = (좋아요+댓글+수집+공유)/경과h** 로 통일(`build_overseas_items`).
  좋아요 자릿수가 세 플랫폼 유사(29K/45K/5K)라 공정.
- CN 게시시각은 유닉스 → search_full에서 ISO로 변환.
- 틱톡 관련성 육안 확인 양호("New Kitchen Find #amazonkitchen #kitchengadget", 6일전, 220만뷰).
- 서버 Apify 토큰 17개 `/etc/shopping-shorts.env`(systemd EnvironmentFile)에 있음 — 즉시 가동 가능.

**틱톡 search_full 확정 스키마**(build_overseas_items 입력): `{video_id, url, channel_title, title, thumbnail, published_at(ISO), views, likes, comments}` — collects/shares는 없음(0 처리).

---

## Task 1: 스파이크 ✅ 완료

위 "스파이크 결과"에 기록 완료. 후속 태스크가 이 값을 사용.

---

## Task 2: 시드 재구조화 (레딧 → 플랫폼별 키워드)

**Files:** Modify `shopping_shorts/overseas_seeds.json` · Test `shopping_shorts/tests/test_overseas_seeds.py`

- [ ] **Step 1: 새 시드 JSON 작성**

`shopping_shorts/overseas_seeds.json` 전체 교체:
```json
{
  "주방/레시피":   {"tiktok": ["kitchen gadgets", "kitchen hacks"], "cn": ["厨房神器", "厨房好物"]},
  "살림/생활꿀템": {"tiktok": ["home gadgets", "cleaning hacks"],  "cn": ["生活好物", "居家好物"]},
  "인테리어/DIY":  {"tiktok": ["diy home", "room makeover"],       "cn": ["家居改造", "手工DIY"]},
  "가전/도구":     {"tiktok": ["cool gadgets", "useful tools"],    "cn": ["神器", "工具"]},
  "뷰티/셀프케어": {"tiktok": ["beauty hacks", "skincare tips"],   "cn": ["美妆好物", "护肤"]},
  "정리/수납":     {"tiktok": ["organization hacks", "storage"],   "cn": ["收纳神器", "整理"]}
}
```

- [ ] **Step 2: 테스트 작성** — `shopping_shorts/tests/test_overseas_seeds.py` 전체 교체:
```python
from shopping_shorts.overseas_seeds import load_seeds, CATEGORIES


def test_seeds_have_platform_keyword_lists():
    seeds = load_seeds()
    assert seeds
    for cat, cfg in seeds.items():
        assert isinstance(cfg.get("tiktok"), list) and cfg["tiktok"], f"{cat}: tiktok 키워드 필요"
        assert isinstance(cfg.get("cn"), list) and cfg["cn"], f"{cat}: cn 키워드 필요"


def test_categories_derived_from_keys():
    assert CATEGORIES == list(load_seeds().keys())


def test_no_reddit_subreddits_left():
    for cfg in load_seeds().values():
        assert "subreddits" not in cfg
```

- [ ] **Step 3: 실행** — `cd .tracks/해외HOT && python -m pytest shopping_shorts/tests/test_overseas_seeds.py -v` → 3 passed.

- [ ] **Step 4: Commit**
```bash
git -C .tracks/해외HOT add shopping_shorts/overseas_seeds.json shopping_shorts/tests/test_overseas_seeds.py
git -C .tracks/해외HOT commit -m "feat(해외HOT): 시드 재구조화 — 레딧 서브레딧→플랫폼별 키워드"
```

---

## Task 3: 참여속도 빌더 `build_overseas_items` (ranking.py 신규)

**Files:** Modify `shopping_shorts/ranking.py` · Test `shopping_shorts/tests/test_build_overseas_items.py`

> CN은 조회수 부재 → 랭킹 기준을 참여(좋아요+댓글+수집+공유)로 통일. `build_tiktok_items`와 별개
> 함수(기존 틱톡 호출부 회귀 방지). 입력 raw 스키마 = 각 search_full 출력.

- [ ] **Step 1: 실패 테스트 작성** — `shopping_shorts/tests/test_build_overseas_items.py` 생성:
```python
from datetime import datetime, timezone
from shopping_shorts.ranking import build_overseas_items


def _raw(**kw):
    base = {"video_id": "v1", "title": "kitchen gadget", "published_at": "2026-07-25T12:00:00Z",
            "views": 0, "likes": 100, "comments": 10, "collects": 40, "shares": 5,
            "channel_title": "a", "thumbnail": "t", "url": "u", "media_platform": "douyin"}
    base.update(kw); return base


def test_speed_uses_engagement_not_views():
    now = datetime(2026, 7, 26, 12, 0, tzinfo=timezone.utc)  # 24h 후
    items = build_overseas_items([_raw()], lambda sc: None, lambda sc: None, now=now)
    it = items[0]
    assert it["base_count"] == 155          # 100+10+40+5
    assert round(it["speed"], 1) == round(155 / 24, 1)
    assert it["platform"] == "douyin"       # media_platform 보존
    assert it["views"] == 0                  # 표시용, CN은 0


def test_out_of_window_excluded():
    now = datetime(2026, 8, 20, 12, 0, tzinfo=timezone.utc)  # >336h
    assert build_overseas_items([_raw()], lambda sc: None, lambda sc: None, now=now) == []


def test_accel_from_prev_delta():
    now = datetime(2026, 7, 26, 12, 0, tzinfo=timezone.utc)
    items = build_overseas_items([_raw()], lambda sc: 55, lambda sc: 50, now=now)
    assert items[0]["delta"] == 100          # 155 - prev_base 55
    assert items[0]["accel"] == 50           # delta 100 - prev_delta 50
```

- [ ] **Step 2: 실패 확인** — `python -m pytest shopping_shorts/tests/test_build_overseas_items.py -v` → FAIL (no attribute).

- [ ] **Step 3: 구현** — `shopping_shorts/ranking.py` 하단(`build_reddit_items` 뒤)에 추가:
```python
def build_overseas_items(raw, prev_base, prev_delta, now=None, window_hours=336):
    """해외HOT 다중플랫폼(틱톡/도우인/샤오홍슈) raw → 공통 item.

    CN(도우인·샤오홍슈)은 조회수를 안 줘서(2026-07-26 실측) 랭킹 기준을 '참여'로 통일한다:
    base = 좋아요+댓글+수집+공유, speed = base/경과h. views는 표시용(없으면 0).
    platform은 raw.media_platform 보존(gap 번역분기·UI 뱃지용). window 336h=14일."""
    now = now or datetime.now(timezone.utc)
    items = []
    for r in raw:
        ts = r.get("published_at")
        if not ts:
            continue
        age = hours_since(ts, now=now)
        if age > window_hours or age < 0:
            continue
        likes = int(r.get("likes") or 0)
        comments = int(r.get("comments") or 0)
        collects = int(r.get("collects") or 0)
        shares = int(r.get("shares") or 0)
        views = int(r.get("views") or 0)
        base = likes + comments + collects + shares
        sc = r.get("video_id") or ""
        prev = prev_base(sc)
        is_new = prev is None
        delta = base if is_new else base - prev
        prev_d = prev_delta(sc)
        accel = None if prev_d is None else delta - prev_d
        items.append({
            "platform": r.get("media_platform") or "tiktok",
            "shortcode": sc,
            "name": r.get("channel_title"),
            "username": r.get("channel_title"),
            "inpock": "",
            "followers": None,
            "thumbnail": r.get("thumbnail", ""),
            "video_url": "",
            "url": r.get("url", ""),
            "comments": comments,
            "likes": likes,
            "collects": collects,
            "shares": shares,
            "views": views,
            "base_count": base,
            "age_hours": round(age, 1),
            "delta": delta,
            "is_new": is_new,
            "accel": accel,
            "speed": base / age if age > 0 else float(base),
            "density": (collects + shares) / base if base else 0.0,
            "category": r.get("category") or "",
            "caption": r.get("title", ""),
        })
    return items
```

- [ ] **Step 4: 통과 확인** — `python -m pytest shopping_shorts/tests/test_build_overseas_items.py -v` → PASS.

- [ ] **Step 5: Commit**
```bash
git -C .tracks/해외HOT add shopping_shorts/ranking.py shopping_shorts/tests/test_build_overseas_items.py
git -C .tracks/해외HOT commit -m "feat(해외HOT): build_overseas_items — 참여속도 랭킹(조회수부재 CN 대응)"
```

---

## Task 4: 도우인 `search_full`

**Files:** Modify `shopping_shorts/douyin_search.py` · Test `shopping_shorts/tests/test_douyin_search.py`

> 실측 필드: `id`·`createTime`(유닉스)·`statistics.{playCount,diggCount,commentCount,collectCount,shareCount}`·`authorMeta.nickName`·`videoMeta.cover`·`itemTitle`. createTime→ISO 변환.

- [ ] **Step 1: 실패 테스트** — `shopping_shorts/tests/test_douyin_search.py` 하단 추가:
```python
def test_search_full_maps_engagement_schema(monkeypatch):
    monkeypatch.setattr(douyin_search, "APIFY_TOKENS", ["fake-key"])

    def fake_run(payload, tokens, timeout, poll_interval, actor=None):
        return [{
            "url": "https://www.douyin.com/video/999", "type": "video", "id": "999",
            "itemTitle": "厨房神器", "createTime": 1774442495,
            "videoMeta": {"cover": "https://c.jpg", "duration": 30},
            "authorMeta": {"nickName": "홍길동"},
            "statistics": {"playCount": 0, "diggCount": 800, "commentCount": 50,
                           "collectCount": 300, "shareCount": 20},
        }]
    monkeypatch.setattr(douyin_search, "_run_with_rotation", fake_run)

    out = douyin_search.search_full("厨房神器", max_results=40)
    assert len(out) == 1
    r = out[0]
    assert r["video_id"] == "999" and r["media_platform"] == "douyin"
    assert r["published_at"] == "2026-03-25T15:21:35Z"   # 1774442495 → ISO(UTC)
    assert r["likes"] == 800 and r["comments"] == 50 and r["collects"] == 300 and r["shares"] == 20
    assert r["views"] == 0 and r["channel_title"] == "홍길동" and r["thumbnail"] == "https://c.jpg"


def test_search_full_skips_rows_without_id(monkeypatch):
    monkeypatch.setattr(douyin_search, "APIFY_TOKENS", ["fake-key"])
    monkeypatch.setattr(douyin_search, "_run_with_rotation",
                        lambda *a, **k: [{"url": "u", "type": "video", "createTime": 1}])
    assert douyin_search.search_full("x") == []
```

- [ ] **Step 2: 실패 확인** — `python -m pytest shopping_shorts/tests/test_douyin_search.py::test_search_full_maps_engagement_schema -v` → FAIL.

- [ ] **Step 3: 구현** — `shopping_shorts/douyin_search.py` 상단 import 아래 헬퍼 + 하단 함수 추가:
```python
from datetime import datetime, timezone


def _iso(unix_ts):
    """유닉스초 → ISO(UTC). 변환 불가면 빈 문자열."""
    try:
        return datetime.fromtimestamp(int(unix_ts), tz=timezone.utc).strftime("%Y-%m-%dT%H:%M:%SZ")
    except (TypeError, ValueError, OSError):
        return ""


def search_full(keyword, max_results=40, token=None, timeout=180, poll_interval=5):
    """키워드 → build_overseas_items 스키마 raw dict 리스트. video_id 없는 행 제외.
    도우인은 조회수(playCount)를 안 주므로 views=0(표시용), 랭킹은 참여로 간다."""
    tokens = [token] if token else APIFY_TOKENS
    if not tokens:
        raise RuntimeError("douyin_search: APIFY_TOKEN이 설정되지 않았습니다")
    payload = {"keywords": [keyword], "maxResultsPerQuery": max_results, "sort": "general"}
    items = _run_with_rotation(payload, tokens, timeout, poll_interval, actor=_ACTOR)
    out = []
    for item in items:
        vid = item.get("id")
        url = item.get("url")
        if not vid or not url or item.get("type") != "video":
            continue
        st = item.get("statistics") or {}
        out.append({
            "video_id": str(vid),
            "title": item.get("itemTitle") or item.get("previewTitle") or item.get("text", ""),
            "published_at": _iso(item.get("createTime")),
            "views": _num(st.get("playCount")) or 0,
            "likes": _num(st.get("diggCount")) or 0,
            "comments": _num(st.get("commentCount")) or 0,
            "collects": _num(st.get("collectCount")) or 0,
            "shares": _num(st.get("shareCount")) or 0,
            "channel_title": _first(item, ("authorMeta", "nickName"), ("authorMeta", "name")) or "",
            "thumbnail": (item.get("videoMeta") or {}).get("cover", ""),
            "url": url,
            "media_platform": "douyin",
        })
    return out
```

- [ ] **Step 4: 통과 확인** — `python -m pytest shopping_shorts/tests/test_douyin_search.py -v` → 전부 PASS.

- [ ] **Step 5: Commit**
```bash
git -C .tracks/해외HOT add shopping_shorts/douyin_search.py shopping_shorts/tests/test_douyin_search.py
git -C .tracks/해외HOT commit -m "feat(해외HOT): douyin_search.search_full — 참여 스키마+유닉스ISO"
```

---

## Task 5: 샤오홍슈 `search_full`

**Files:** Modify `shopping_shorts/xiaohongshu_search.py` · Test `shopping_shorts/tests/test_xiaohongshu_search.py`(없으면 생성)

> 실측 필드: `id`·`timestamp`(유닉스)·`engagement.{liked_count,comments_count,collected_count,shared_count}`(view 없음)·`author.nickname`·`title/desc`·`video.url_720p`·`images[cover_image_index]`.

- [ ] **Step 1: 실패 테스트** — `shopping_shorts/tests/test_xiaohongshu_search.py` 에 추가(파일 없으면 생성):
```python
import pytest
from shopping_shorts import xiaohongshu_search as xhs


def test_search_full_maps_engagement_schema(monkeypatch):
    monkeypatch.setattr(xhs, "APIFY_TOKENS", ["fake-key"])

    def fake_run(payload, tokens, timeout, poll_interval, actor=None):
        return [{
            "url": "https://www.xiaohongshu.com/explore/abc", "type": "video", "id": "abc",
            "title": "厨房好物", "timestamp": 1779088572,
            "video": {"url_720p": "https://v.mp4", "duration_seconds": 25},
            "images": [{"url": "https://cover.jpg"}], "cover_image_index": 0,
            "author": {"nickname": "김철수"},
            "engagement": {"liked_count": 300, "comments_count": 12,
                           "collected_count": 90, "shared_count": 7},
        }]
    monkeypatch.setattr(xhs, "_run_with_rotation", fake_run)

    out = xhs.search_full("厨房好物", max_results=40)
    assert len(out) == 1
    r = out[0]
    assert r["video_id"] == "abc" and r["media_platform"] == "xiaohongshu"
    assert r["published_at"] == "2026-05-18T00:36:12Z"   # 1779088572 → ISO(UTC)
    assert r["likes"] == 300 and r["comments"] == 12 and r["collects"] == 90 and r["shares"] == 7
    assert r["views"] == 0 and r["channel_title"] == "김철수" and r["thumbnail"] == "https://cover.jpg"


def test_search_full_skips_non_video(monkeypatch):
    monkeypatch.setattr(xhs, "APIFY_TOKENS", ["fake-key"])
    monkeypatch.setattr(xhs, "_run_with_rotation",
                        lambda *a, **k: [{"url": "u", "type": "normal", "id": "x"}])
    assert xhs.search_full("x") == []
```

- [ ] **Step 2: 실패 확인** — `python -m pytest shopping_shorts/tests/test_xiaohongshu_search.py::test_search_full_maps_engagement_schema -v` → FAIL.

- [ ] **Step 3: 구현** — `shopping_shorts/xiaohongshu_search.py` 상단 import 아래 `_iso` 헬퍼(Task4와 동일 코드) + 하단 함수 추가:
```python
from datetime import datetime, timezone


def _iso(unix_ts):
    """유닉스초 → ISO(UTC). 변환 불가면 빈 문자열."""
    try:
        return datetime.fromtimestamp(int(unix_ts), tz=timezone.utc).strftime("%Y-%m-%dT%H:%M:%SZ")
    except (TypeError, ValueError, OSError):
        return ""


def search_full(keyword, max_results=40, token=None, timeout=180, poll_interval=5):
    """키워드 → build_overseas_items 스키마 raw dict. 영상 노트만, video_id 없으면 제외.
    샤오홍슈는 조회수 필드가 없어 views=0(표시용), 랭킹은 참여로 간다."""
    tokens = [token] if token else APIFY_TOKENS
    if not tokens:
        raise RuntimeError("xiaohongshu_search: APIFY_TOKEN이 설정되지 않았습니다")
    payload = {"keywords": [keyword], "maxResults": max_results,
               "noteType": "video", "topUpFromOtherSorts": False}
    items = _run_with_rotation(payload, tokens, timeout, poll_interval, actor=_ACTOR)
    out = []
    for item in items:
        vid = item.get("id")
        url = item.get("url")
        video = item.get("video") or {}
        if not vid or not url or item.get("type") != "video" or not video.get("url_720p"):
            continue
        eng = item.get("engagement") or {}
        out.append({
            "video_id": str(vid),
            "title": item.get("title") or item.get("desc", ""),
            "published_at": _iso(item.get("timestamp")),
            "views": 0,
            "likes": _num(_first(eng, "liked_count", "likedCount")) or 0,
            "comments": _num(_first(eng, "comments_count", "commentsCount", "comment_count")) or 0,
            "collects": _num(_first(eng, "collected_count", "collectedCount")) or 0,
            "shares": _num(_first(eng, "shared_count", "sharedCount")) or 0,
            "channel_title": _first(item, ("author", "nickname"), ("author", "name")) or "",
            "thumbnail": _cover(item, video),
            "url": url,
            "media_platform": "xiaohongshu",
        })
    return out
```
> `_first`는 dict 하나에도 문자열 키 경로로 동작(기존 구현 확인). `eng`가 dict라 `_first(eng, "liked_count")` 정상.

- [ ] **Step 4: 통과 확인** — `python -m pytest shopping_shorts/tests/test_xiaohongshu_search.py -v` → PASS.

- [ ] **Step 5: Commit**
```bash
git -C .tracks/해외HOT add shopping_shorts/xiaohongshu_search.py shopping_shorts/tests/test_xiaohongshu_search.py
git -C .tracks/해외HOT commit -m "feat(해외HOT): xiaohongshu_search.search_full — 참여 스키마+유닉스ISO"
```

---

## Task 6: 깔때기 순수함수 `overseas_funnel.py`

**Files:** Create `shopping_shorts/overseas_funnel.py` · Test `shopping_shorts/tests/test_overseas_funnel.py`

- [ ] **Step 1: 실패 테스트** — `shopping_shorts/tests/test_overseas_funnel.py` 생성:
```python
from shopping_shorts.overseas_funnel import passes_format, passes_relevance, under_view_ceiling, BLOCK_WORDS


def test_format_requires_url_and_id():
    assert passes_format({"url": "u", "video_id": "1"}) is True
    assert passes_format({"url": "", "video_id": "1"}) is False
    assert passes_format({"url": "u", "video_id": ""}) is False


def test_relevance_allow_and_block():
    assert passes_relevance({"title": "kitchen gadget you need"}, ["kitchen", "gadget"]) is True
    assert passes_relevance({"title": "my dance challenge"}, ["kitchen"]) is False   # 차단어
    assert passes_relevance({"title": "random vlog"}, ["kitchen"]) is False           # 허용어 없음
    assert passes_relevance({"title": "厨房神器好物"}, ["厨房", "神器"]) is True       # 중국어 허용어


def test_view_ceiling_only_applies_when_views_known():
    assert under_view_ceiling({"views": 500000}, ceiling=3000000) is True
    assert under_view_ceiling({"views": 9000000}, ceiling=3000000) is False
    assert under_view_ceiling({"views": 0}, ceiling=3000000) is True   # CN(뷰없음)은 통과


def test_block_words_present():
    assert "dance" in BLOCK_WORDS and "prank" in BLOCK_WORDS
```

- [ ] **Step 2: 실패 확인** — `python -m pytest shopping_shorts/tests/test_overseas_funnel.py -v` → FAIL.

- [ ] **Step 3: 구현** — `shopping_shorts/overseas_funnel.py` 생성:
```python
"""해외HOT 무료 깔때기 — 형식 하드컷·관련성 게이트·'안터진' 조회수 상한. 순수함수라
overseas_hot_jobs에서 조합해 쓰고 단위테스트가 쉽다. (설계 §4 STAGE1·2, §5 상한)"""

BLOCK_WORDS = ("dance", "prank", "storytime", "story time", "lip sync", "lipsync",
               "giveaway", "sub for sub", "follow me", "asmr")

DEFAULT_VIEW_CEILING = 3_000_000   # 이미 터진 것 제외(설계 §5). 국가감이라 튜닝대상.


def passes_format(item):
    """재생 URL과 video_id가 있어야 통과."""
    return bool(item.get("url")) and bool(item.get("video_id"))


def passes_relevance(item, allow_words):
    """제목에 카테고리 허용어 ≥1 있고 차단어 없으면 통과. 허용어 대소문자 무시."""
    title = (item.get("title") or "")
    low = title.lower()
    if any(b in low for b in BLOCK_WORDS):
        return False
    if not allow_words:
        return True
    return any(w.lower() in low for w in allow_words)


def under_view_ceiling(item, ceiling=DEFAULT_VIEW_CEILING):
    """조회수가 상한 이하여야 '아직 안 터진'. 조회수 0/미제공(CN)은 통과(상한 판단 불가)."""
    v = int(item.get("views") or 0)
    return v == 0 or v <= ceiling
```

- [ ] **Step 4: 통과 확인** — `python -m pytest shopping_shorts/tests/test_overseas_funnel.py -v` → PASS.

- [ ] **Step 5: Commit**
```bash
git -C .tracks/해외HOT add shopping_shorts/overseas_funnel.py shopping_shorts/tests/test_overseas_funnel.py
git -C .tracks/해외HOT commit -m "feat(해외HOT): 깔때기 순수함수 — 형식·관련성·안터진 상한"
```

---

## Task 7: gap_check CN 번역 지원

**Files:** Modify `shopping_shorts/gap_check.py` · Test `shopping_shorts/tests/test_gap_check.py`

> CN은 중국어 제목 → 번역 후 검색. `video_analysis.translate_keyword` 사용.
> ⚠️ **번역 방향 확인**: Step3 전 서버에서 `translate_keyword("厨房神器")`가 한국어를 내는지 1회 확인.
> 방향 반대면(KO→ZH 전용) 아래 구현이 자동으로 '미확인' 반환(번역결과에 한글 없으면) → 안전.

- [ ] **Step 1: 실패 테스트** — `shopping_shorts/tests/test_gap_check.py` 하단 추가:
```python
def test_gap_badge_translates_cn(monkeypatch):
    from shopping_shorts import gap_check, youtube_search, video_analysis
    monkeypatch.setattr(video_analysis, "translate_keyword", lambda k, **kw: "주방 꿀템")
    seen = {}
    def fake_search(q, max_results=10):
        seen["q"] = q
        return [{"title": "한국어 재편집본"}]
    monkeypatch.setattr(youtube_search, "search", fake_search)
    assert gap_check.gap_badge("厨房神器", translate=True) == "이미유입"
    assert seen["q"] == "주방 꿀템"


def test_gap_badge_degrades_when_translation_not_korean(monkeypatch):
    from shopping_shorts import gap_check, video_analysis
    monkeypatch.setattr(video_analysis, "translate_keyword", lambda k, **kw: "厨房神器")  # 방향반대
    assert gap_check.gap_badge("厨房神器", translate=True) == "미확인"
```

- [ ] **Step 2: 실패 확인** — `python -m pytest shopping_shorts/tests/test_gap_check.py -v` → FAIL.

- [ ] **Step 3: 구현** — `shopping_shorts/gap_check.py` 의 `gap_badge` 교체:
```python
def gap_badge(title, min_korean=1, translate=False):
    """제목 → '🔥선점가능' | '이미유입' | '미확인'.

    translate=True면(CN 영상) 제목을 한국어로 번역해 검색한다 — 중국어 제목 그대로면
    한글 재편집본을 못 찾아 거짓 '선점가능'이 나온다. 번역결과에 한글이 없으면(빈값·방향반대)
    검색 무의미 → '미확인'."""
    q = (title or "").strip()
    if not q:
        return "미확인"
    if translate:
        from shopping_shorts import video_analysis
        try:
            q = (video_analysis.translate_keyword(q) or "").strip()
        except Exception:
            q = ""
        if not _has_korean(q):
            return "미확인"
    try:
        results = youtube_search.search(q, max_results=10)
    except Exception:
        return "미확인"
    korean = sum(1 for r in results if _has_korean(r.get("title")))
    return "이미유입" if korean >= min_korean else "🔥선점가능"
```

- [ ] **Step 4: 통과 확인** — `python -m pytest shopping_shorts/tests/test_gap_check.py -v` → PASS(기존 테스트도 유지).

- [ ] **Step 5: Commit**
```bash
git -C .tracks/해외HOT add shopping_shorts/gap_check.py shopping_shorts/tests/test_gap_check.py
git -C .tracks/해외HOT commit -m "feat(해외HOT): gap_check CN 제목 번역 후 선점검색"
```

---

## Task 8: `overseas_hot_jobs` 재작성 (Apify 3플랫폼 + 깔때기)

**Files:** Modify `shopping_shorts/overseas_hot_jobs.py` · Test `shopping_shorts/tests/test_overseas_hot_jobs.py`

- [ ] **Step 1: 테스트 재작성** — `test_run_collects_ranks_and_saves` 교체:
```python
def test_run_collects_ranks_and_saves(monkeypatch, tmp_path):
    now = datetime(2026, 7, 26, 12, 0, tzinfo=timezone.utc)
    published = "2026-07-25T12:00:00Z"

    def fake_tt(kw, max_results=40):
        return [{"video_id": "tt_" + kw, "title": kw + " kitchen gadget", "published_at": published,
                 "views": 500000, "likes": 900, "comments": 30, "collects": 0, "shares": 0,
                 "channel_title": "a", "thumbnail": "", "url": "https://tt", "media_platform": "tiktok"}]
    def fake_dy(kw, max_results=40):
        return [{"video_id": "dy_" + kw, "title": kw, "published_at": published,
                 "views": 0, "likes": 800, "comments": 20, "collects": 300, "shares": 10,
                 "channel_title": "b", "thumbnail": "", "url": "https://dy", "media_platform": "douyin"}]

    monkeypatch.setattr(job.tiktok_search, "search_full", fake_tt)
    monkeypatch.setattr(job.douyin_search, "search_full", fake_dy)
    monkeypatch.setattr(job.xiaohongshu_search, "search_full", lambda kw, max_results=40: [])
    monkeypatch.setattr(job.gap_check, "gap_badge", lambda title, **kw: "🔥선점가능")
    monkeypatch.setattr(job, "load_seeds",
                        lambda: {"주방/레시피": {"tiktok": ["kitchen"], "cn": ["厨房"]}})
    monkeypatch.setattr(job, "DB_PATH", str(tmp_path / "t.db"))
    monkeypatch.setattr(job, "_now", lambda: now)

    job._run()
    from shopping_shorts.store import Store
    items, _ = Store(str(tmp_path / "t.db")).load_overseas_feed()
    assert items, "피드에 항목이 저장돼야 한다"
    assert all("grade" in it and it.get("category") == "주방/레시피" for it in items)
    assert {it["platform"] for it in items} == {"tiktok", "douyin"}
    assert items[0]["gap_badge"] == "🔥선점가능"
```

- [ ] **Step 2: 실패 확인** — `python -m pytest shopping_shorts/tests/test_overseas_hot_jobs.py -v` → FAIL.

- [ ] **Step 3: 재작성** — `shopping_shorts/overseas_hot_jobs.py` 의 import·상수·`_collect_category`·`_run`의 수집·gap 부분 교체 (`_merge_rotate`·`start`·`status`·`_now` 유지):

import 블록 교체:
```python
import threading
import time
from datetime import datetime, timezone

from shopping_shorts.config import DB_PATH
from shopping_shorts.store import Store
from shopping_shorts.overseas_seeds import load_seeds
from shopping_shorts import tiktok_search, douyin_search, xiaohongshu_search
from shopping_shorts import gap_check, overseas_funnel
from shopping_shorts.ranking import build_overseas_items, apply_grades, sort_by
```
상수 교체(`_CAP`·`_REQ_PAUSE`·`_GAP_CAP` 부분):
```python
_CAP = 120          # 피드 최대 유지 개수(로테이션)
_PER_KEYWORD = 40   # 카테고리·플랫폼·키워드당 수집 상한(=과금단위)
_REQ_PAUSE = 0.5    # Apify 호출 간 간격
_GAP_CAP = 40       # 선점검색 배치당 상한(유튜브 무료쿼터 보호) — 생존자 상위만
```
`_collect_category` 교체:
```python
def _collect_category(cat, cfg, store):
    allow = list(cfg.get("tiktok", [])) + list(cfg.get("cn", []))
    raw = []
    for kw in cfg.get("tiktok", []):
        raw += tiktok_search.search_full(kw, max_results=_PER_KEYWORD)
        time.sleep(_REQ_PAUSE)
    for kw in cfg.get("cn", []):
        raw += douyin_search.search_full(kw, max_results=_PER_KEYWORD)
        time.sleep(_REQ_PAUSE)
        raw += xiaohongshu_search.search_full(kw, max_results=_PER_KEYWORD)
        time.sleep(_REQ_PAUSE)
    # STAGE 1·2·상한: 형식·관련성·안터진
    kept = [r for r in raw
            if overseas_funnel.passes_format(r)
            and overseas_funnel.passes_relevance(r, allow)
            and overseas_funnel.under_view_ceiling(r)]
    for r in kept:
        r["category"] = cat   # 시드 카테고리 고정
    items = build_overseas_items(
        kept,
        prev_base=lambda sc: store.prev_base_platform("overseas", sc),
        prev_delta=lambda sc: store.prev_delta_platform("overseas", sc),
        now=_now(),
    )
    return apply_grades(items)
```
`_run` 안 정렬은 `sort_by(all_items, "속도")` 유지. gap 루프 교체(CN 번역):
```python
        checked = 0
        for it in merged:
            if it.get("gap_badge"):
                continue
            if checked >= _GAP_CAP:
                break
            is_cn = it.get("platform") in ("douyin", "xiaohongshu")
            it["gap_badge"] = gap_check.gap_badge(
                it.get("caption") or it.get("title") or "", translate=is_cn)
            checked += 1
```
> `save_run_platform`은 `i["base_count"]` 사용(build_overseas_items가 채움) — 기존 코드 그대로 동작.

- [ ] **Step 4: 통과 확인** — `python -m pytest shopping_shorts/tests/test_overseas_hot_jobs.py -v` → PASS.

- [ ] **Step 5: 회귀 확인** — `python -m pytest shopping_shorts/tests/test_overseas_api.py shopping_shorts/tests/test_overseas_tab_smoke.py -v` → PASS.

- [ ] **Step 6: Commit**
```bash
git -C .tracks/해외HOT add shopping_shorts/overseas_hot_jobs.py shopping_shorts/tests/test_overseas_hot_jobs.py
git -C .tracks/해외HOT commit -m "feat(해외HOT): job 재작성 — Apify 3플랫폼 수집+깔때기+CN 선점번역"
```

---

## Task 9: 카드 UI 재설계

**Files:** Modify `shopping_shorts/static/index.html` · Test `shopping_shorts/tests/test_overseas_tab_smoke.py`

- [ ] **Step 1: `OVERSEAS_CATS` 교체**
```javascript
const OVERSEAS_CATS = ["전체","주방/레시피","살림/생활꿀템","인테리어/DIY","가전/도구","뷰티/셀프케어","정리/수납"];
```

- [ ] **Step 2: `renderOverseas` 카드 그리드로 교체**
```javascript
function renderOverseas(){
  const rows=overseasItems
    .filter(i=>overseasFilter==='전체'||i.category===overseasFilter)
    .map(i=>{
      const gap = i.gap_badge==='🔥선점가능'
        ? '<span class="ov-gap ov-gap-hot">🔥선점가능</span>'
        : (i.gap_badge==='이미유입' ? '<span class="ov-gap">이미유입</span>' : '');
      const metric = (i.views||0)>0
        ? ('조회 ' + ((i.views>=10000)?(Math.round(i.views/1000)/10)+'만':i.views))
        : ('반응 ' + (i.base_count||0));
      const thumb = i.thumbnail
        ? `<img src="/api/thumb?url=${encodeURIComponent(i.thumbnail)}" loading="lazy">`
        : '<div class="ov-noimg">▶</div>';
      const title = (i.caption||i.title||'').replace(/</g,'&lt;');
      return `<a class="ov-card" href="${i.url||'#'}" target="_blank" rel="noopener">
        <div class="ov-thumb">${thumb}<span class="ov-plat">${i.media_platform||i.platform||''}</span>${gap}</div>
        <div class="ov-meta">
          <div class="ov-title">${title}</div>
          <div class="ov-sub">${metric} · 속도 ${Math.round(i.speed||0)} · <b>${i.grade||'—'}</b></div>
        </div></a>`;
    }).join('');
  document.getElementById('overseasList').innerHTML =
    rows ? `<div class="ov-grid">${rows}</div>`
         : '<div class="empty">아직 수집된 영상이 없어요 — "지금 업데이트"를 눌러주세요.</div>';
}
```

- [ ] **Step 3: 카드 CSS 추가** — `<style>` 블록 안에:
```css
.ov-grid{display:grid;grid-template-columns:repeat(auto-fill,minmax(180px,1fr));gap:12px}
.ov-card{background:var(--card,#181b22);border:1px solid var(--line,#262b36);border-radius:12px;overflow:hidden;text-decoration:none;color:inherit;display:flex;flex-direction:column;transition:border-color .15s,transform .1s}
.ov-card:hover{border-color:#37e0bd;transform:translateY(-2px)}
.ov-thumb{position:relative;aspect-ratio:9/12;background:#0b0d11;display:flex;align-items:center;justify-content:center}
.ov-thumb img{width:100%;height:100%;object-fit:cover}
.ov-noimg{color:#4a5162;font-size:26px}
.ov-plat{position:absolute;left:6px;bottom:6px;background:rgba(0,0,0,.72);color:#fff;font-size:10px;padding:2px 6px;border-radius:5px}
.ov-gap{position:absolute;right:6px;top:6px;background:rgba(0,0,0,.6);color:#cfd6e4;font-size:10px;padding:2px 6px;border-radius:5px}
.ov-gap-hot{background:#0d3b33;color:#37e0bd;font-weight:600}
.ov-meta{padding:8px 9px 10px}
.ov-title{font-size:12.5px;line-height:1.35;font-weight:600;display:-webkit-box;-webkit-line-clamp:2;-webkit-box-orient:vertical;overflow:hidden;min-height:34px}
.ov-sub{margin-top:6px;color:var(--sub,#8b94a7);font-size:11px}
```

- [ ] **Step 4: 스모크 테스트 갱신·실행** — `test_overseas_tab_smoke.py` 가 표 마크업(`<th>`·`renderOverseas` 내용)을 검사하면 카드 마크업(`ov-card`/`ov-grid`) 기준으로 문자열 수정. 먼저 파일을 읽어 검사 대상 문자열 확인 후 교체.
Run: `python -m pytest shopping_shorts/tests/test_overseas_tab_smoke.py -v` → PASS.

- [ ] **Step 5: 육안 확인** — `python -m shopping_shorts.app` → `/` → 🌍해외HOT 탭 렌더 확인(데이터 없으면 빈 메시지). 깨짐 없으면 통과.

- [ ] **Step 6: Commit**
```bash
git -C .tracks/해외HOT add shopping_shorts/static/index.html shopping_shorts/tests/test_overseas_tab_smoke.py
git -C .tracks/해외HOT commit -m "feat(해외HOT): 카드 UI 재설계 — 제목·큰썸네일·반응/조회·선점뱃지"
```

---

## Task 10: 배포 + 라이브 검증

- [ ] **Step 1: 전체 테스트 게이트** — `cd .tracks/해외HOT && python -m pytest shopping_shorts/tests/ -q 2>&1 | tail -15` → 신규 통과, 실패가 기준선(10건) 이하.

- [ ] **Step 2: finish** — `cd "로또의 주식 루트" && py tools/track.py finish 해외HOT` → 게이트 통과 시 main 병합 → 3분 자동배포.

- [ ] **Step 3: 라이브 수집·검증** — 배포 반영 후 서버에서 실수집 1회 + 분포 확인:
```bash
KEY="C:/Users/CH/crawling_bot_client/LightsailDefaultKey-ap-northeast-2.pem"
ssh -i "$KEY" ubuntu@3.39.179.148 'cd /home/ubuntu/lotto-stock-wiki && set -a && sudo cat /etc/shopping-shorts.env > /tmp/ss.env && . /tmp/ss.env; set +a; rm -f /tmp/ss.env; python3 -c "
from shopping_shorts import overseas_hot_jobs as j; j._run()
from shopping_shorts.store import Store; from shopping_shorts.config import DB_PATH
from collections import Counter
feed,_=Store(DB_PATH).load_overseas_feed()
print(\"총\", len(feed))
print(\"카테고리\", dict(Counter(i.get(\"category\") for i in feed)))
print(\"플랫폼\", dict(Counter(i.get(\"platform\") for i in feed)))
"'
```
Expected: 총>0, **여러 카테고리 분산**(2개 독점 아님), 플랫폼 tiktok/douyin/xiaohongshu 섞임.

- [ ] **Step 4: 육안 최종** — shoppingshorts.duckdns.org → 🌍해외HOT → 카드 제목·썸네일·반응·선점뱃지·카테고리필터·클릭재생 동작 확인.

---

## 오픈 리스크

- CN 관련성(중국어 키워드가 꿀템을 잘 잡는지) — Task 10 실수집 분포로 확인, 나쁘면 시드 키워드 튜닝.
- 번역 방향(Task 7 주의) — 실측으로 확인, 반대면 CN gap은 '미확인' 유지(Phase 2 고도화).
- 참여속도 랭킹이 실제로 "안터진+급상승"을 잘 뽑는지 — 라이브 며칠 스냅샷 누적 후 accel 정착 관찰.
