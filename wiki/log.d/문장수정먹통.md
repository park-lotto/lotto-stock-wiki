## 2026-09-06 문장수정 먹통 수리
고객 "위의 대사를 반복 / 수정·삭제 먹통" = UI 아니라 **beat_idx 중복**([0,1,3,2,5,5,5]).
`_rebuild_beats_by_lines`의 `dict(base)`가 번호까지 복제(저장 출구라 재부여 뒤에 돎).
재부여 + `_find_beat` 차단 + `/renumber` 탈출구 + 편집 중 재생차단. 회귀테스트 3건.
고객 잡은 서버에서 [0..6]으로 수리(백업함). 실측: 1438잡 중 중복 6잡.
