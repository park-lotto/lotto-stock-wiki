# 모션레벨훅 트랙 로그

## 2026-07-29
- P2(인벤토리 모션레벨 매핑) 완료. subagent-driven development(Task1~5, 각각 spec+품질 2단 리뷰) 사용.
  설계서 원안(whisper→시각컷 재베이스)을 실측 기반으로 재조정 — 추출 단계에서 컷+모션 미리 계산해
  seg에 `motion_level` 필드로 저장, 인벤토리는 별도 필드/프롬프트 suffix로만 노출. `scene_desc`
  문자열 불변 보장(앵커 dedup 키 오염 방지, 리뷰가 핵심 리스크로 확인).
  게이트: `python -m pytest shopping_shorts/ -q` 14 failed+2540 passed — 14건 전부 base(P2 이전)에서
  `.env` 실키로 대조 재현한 사전 결함, P2발 신규 회귀 0건(worktree 대조 검증).
- (집PC, 밤) 1단계 대본 문장품질 튜닝: `_scene_first_candidates` 실측(합성 인벤토리 2개 소재 ×
  3회, 총 15개 후보) — 대화 인용에서 따옴표가 자주 빠져("엄마가 보더니 진짜 곱네 하시더라고요")
  누구 말인지 안 보이는 뭉개진 문장이 나오는 게 재현 패턴이었음. 프롬프트의 대화인용 지시를
  BAD/GOOD 예시 쌍으로 못박아 수정 → 재실측(15개 후보) 전부 정상 따옴표 인용으로 개선 확인.
  pytest: test_edit_plan_build 2건 실패는 기존 `.env` 실키 결함(수정 전 base에서도 동일 재현,
  회귀 아님) 확인.
- (밤) 추출 주 제품 오인 프롬프트 수정: 배경 소품(인형)·진짜 동물에 낚여 "강아지 선풍기"처럼
  엉뚱한 제품명을 짓던 실사고 대응. `script_extract._PROMPT` scene_desc에 "주 제품=반복 클로즈업·
  손에 들린 것, 배경 소품/동물/사람은 맥락일 뿐" 블록 추가. `product_identify._PROMPT`(Lens
  역검색)도 동일 오인 전파 확인해 같은 취지 문장 보강. 가드 테스트 2개 추가(양쪽 프롬프트에
  "배경 소품"·"동물" 문구 존재 확인). ⚠️프롬프트 문자열만 검증됨 — 실제 제미니 행동 변화는
  원본 영상 shortcode 미기록으로 재현·검증 불가, 재발 시 shortcode 기록해 재조사할 것.
- (밤) 백본모드 표절·서브소스낭비 수정(사장님 회사PC 관찰): ①`_plagiarism_flags` 감지만 되고
  `_score_candidate` 미반영 → `_plagiarism_penalty` 신설해 비례감점 배선(TDD). ②`ensure_sources_used`
  가 행위 불일치 시 의도적으로 삽입 안 함(sync 우선, 기존 테스트로 확인) → 코드로 못 고치는 부분을
  `_backbone_order_block` 프롬프트 보강(BAD/GOOD·서브 소스 명시 나열·"각각 최소 한 번씩"·리액션
  대체 유도)으로 커버. 관련 테스트 86개 통과, 전체 스위트 14 failed(기존 baseline과 동일)+2776
  passed, 신규 회귀 0. ⚠️②는 프롬프트뿐이라 라이브 관측 필요.
