# 직접대본씨앗 — handoff

> **소유 트랙**: 직접대본씨앗 — 이 파일은 이 트랙 세션만 수정한다.

## 2026-09-01 — 「직접 쓰기」 대본이 생성에서 막히던 것

사장님 제보(캡처): 2단계에서 「직접 쓰기」에 대본을 붙여넣었는데
**"먼저 씨앗 영상을 고르세요 (직접 쓴 대본은 아직 생성 대상이 아닙니다)"**가 뜨고
'이 스타일로 대본 만들기'가 통째로 막혔다.

### 원인 — 막고 있던 건 화면의 판정 한 줄

`s2Generate()`가 `if(!s || !s.shortcode)`로 **shortcode만** 봤다. 직접 쓴 씨앗은
`s2ManualSeed()`가 `shortcode:''`로 만든다(AI PICK이 덮지 않게 하려고 일부러 그렇게 둔 값) →
영원히 통과 못 함.

**서버는 원래부터 된다**: `app.py api_wiki_generate`는 위키에 shortcode가 없어도
body의 `base_script`가 있으면 그걸로 생성한다(주석·코드 실측). 즉 서버는 안 건드려도 됐다.

### 한 것

- `s2SeedReady(s)` 신설 — 씨앗 준비 판정을 **한 곳**으로(0순위-B).
  담긴 영상(shortcode) **또는** 내 대본(text)이 있으면 통과.
- 안내 문구를 사실에 맞게: "담긴 영상 카드 또는 「직접 쓰기」".
- 테스트 4건(`test_s2_manual_seed_gate.py`) — 직접 쓴 대본 통과·영상 씨앗 종전대로·
  빈 씨앗 차단·옛 문구 잔존 금지. **사보타주(판정을 shortcode만 보게 되돌림)로 1건 빨개짐 확인**.

## ⏭ 다음

- ⚠️ **직접 쓴 대본도 AI가 다시 쓴다**(씨앗이지 최종본이 아니다). 사장님이 "손 안 대고
  그대로 쓰기"를 원하시면 별건으로 만들어야 한다 — 아직 요청 확인 안 받음.
- 별건: 대본에 엉뚱한 소재("새송이")가 섞여 나오는 문제는 **이 트랙이 아니다**.
  오늘 main 커밋에 관련 수정 없음(실측). 다른 세션 진행 중이거나 미병합.

## 2026-09-01 (2차) — "씨앗이 내 대본인데 씨앗을 고르라고 한다"

사장님 캡처: 원문 칸엔 내 대본(25자)이 보이는데 **카드 선택은 1번 영상**에 켜져 있었다.

### 원인 2겹

1. `s2RenderSeeds()`가 매번 `S2.seeds=[]`로 **새로 만든다**(출처=AI PICK 응답).
   `s2ManualSeed()`가 push한 내 대본 카드는 다음 렌더에 **조용히 사라졌다**.
2. `s2SeedOn()`이 shortcode가 없으면 `s.pick`으로 폴백 → 내 대본이 씨앗인데도
   **대표(AI PICK) 카드가 켜진 것처럼** 보였다. 화면이 진실과 다르게 말한 것.

### 한 것
- 보관처 `S2.manualSeed` 한 곳(0순위-B). `s2RenderSeeds`가 매번 다시 얹는다.
- `s2SeedOn`: 같은 객체 → 켜짐 / 내 대본이 씨앗이면 영상 카드는 전부 꺼짐(본문 대조로
  새로고침 복원까지 커버) / 나머지는 종전대로.
- 카드 이름 `✍ 직접 쓴 대본`, 넣는 즉시 `saveWork()`.
- 테스트 4건 추가(총 8건).
