# 자동매칭 고급효과 엔진 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 제작소 릴스의 비트+카테고리에서 리모션 효과 배치 플랜을 자동 생성하고, 포인트 정액 차감 하에 리모션이 최종 영상을 렌더하는 고급효과(프리미엄)를 꾸미기에 통합한다.

**Architecture:** 순수 매칭기(`effect_match`)가 (비트[], category) → 플랜 JSON(FullReel props)을 만들고, `remotion_render`가 그 플랜+조립본을 node subprocess로 렌더한다. 라우트 3개가 위저드(추천→렌더→상태)를 잇고, 렌더 1회당 포인트를 정액 차감(실패 시 환불)한다.

**Tech Stack:** Python/FastAPI, SQLite(store), google-genai(Haiku via key_vault), Remotion 4(node, 기존 shopping_shorts/motion 프로젝트), ffmpeg.

## Global Constraints

- 플랜 JSON 스키마 = FullReel props: `{videoSrc, durationInFrames, themeName, sections[], beats[], fx[]}` (이미 검증됨, 변경 금지).
- pytest에서 실 ffmpeg/node를 부르는 테스트는 `subprocess.run(..., stdin=subprocess.DEVNULL)` 필수 — 없으면 WinError 6로 죽는다(이 저장소 실측).
- 게이트는 pytest 기본 캡처(`-q --tb=no -rf -p no:cacheprovider`)로 돈다 — `-s`로 통과시키면 병합 때 터진다.
- "테스트 통과 ≠ 동작": 렌더 산출물은 반드시 실제 mp4 프레임을 뽑아 눈으로 확인한다.
- 태스크마다 뮤테이션 단계(로직 분기를 뒤집어 해당 테스트가 죽는지) 포함.
- 유저 식별 = `getattr(request.state, "customer_id", 0)`.
- 매칭 모델 = Haiku(`key_vault.get_client("general")` 계열, 저비용). LLM 실패 시 규칙 폴백.
- 커밋 전 `python -c "import shopping_shorts.app"` (post-commit 훅이 무조건 push).

---

### Task 1: 포인트 원장 (points.py + store 테이블)

**Files:**
- Create: `shopping_shorts/points.py`
- Modify: `shopping_shorts/store.py` (points 테이블 CREATE + ALTER 마이그레이션, `get_balance/add_points/deduct_points`)
- Test: `shopping_shorts/tests/test_points.py`

**Interfaces:**
- Produces: `points.deduct(store, customer_id: int, amount: int) -> bool` (잔액 부족 시 False, 성공 시 차감·True) · `points.refund(store, customer_id: int, amount: int) -> None` · `points.balance(store, customer_id: int) -> int`
- Consumes: `store` 인스턴스.

- [ ] **Step 1: Write the failing test**

```python
# shopping_shorts/tests/test_points.py
import subprocess  # noqa: F401  (repo 규약: 실프로세스 테스트는 stdin=DEVNULL — 여기선 불필요하나 패턴 유지 주석)
from shopping_shorts import points
from shopping_shorts.store import Store

def _store(tmp_path):
    return Store(str(tmp_path / "t.db"))

def test_deduct_and_refund_roundtrip(tmp_path):
    st = _store(tmp_path)
    points.add(st, 7, 100)
    assert points.balance(st, 7) == 100
    assert points.deduct(st, 7, 30) is True
    assert points.balance(st, 7) == 70
    points.refund(st, 7, 30)
    assert points.balance(st, 7) == 100

def test_deduct_insufficient_returns_false_and_no_change(tmp_path):
    st = _store(tmp_path)
    points.add(st, 7, 10)
    assert points.deduct(st, 7, 30) is False
    assert points.balance(st, 7) == 10
```

- [ ] **Step 2: Run test to verify it fails**

Run: `python -m pytest shopping_shorts/tests/test_points.py -v`
Expected: FAIL (module `points` not found).

- [ ] **Step 3: Add store schema + accessors**

`store.py`의 스키마 초기화 위치(다른 CREATE TABLE 옆)에 추가하고, 기존 서버 DB 대비 `CREATE TABLE IF NOT EXISTS`로:

```python
# store.py — _init 스키마에
self.conn.execute("""CREATE TABLE IF NOT EXISTS points_ledger(
    customer_id INTEGER NOT NULL,
    delta INTEGER NOT NULL,
    reason TEXT,
    created_at TEXT DEFAULT (datetime('now'))
)""")

def points_balance(self, customer_id):
    row = self.conn.execute(
        "SELECT COALESCE(SUM(delta),0) FROM points_ledger WHERE customer_id=?",
        (customer_id,)).fetchone()
    return int(row[0])

def points_add(self, customer_id, delta, reason=""):
    self.conn.execute(
        "INSERT INTO points_ledger(customer_id, delta, reason) VALUES(?,?,?)",
        (customer_id, delta, reason))
    self.conn.commit()
```

- [ ] **Step 4: Write points.py**

```python
# shopping_shorts/points.py
"""고급효과 렌더 포인트 — 원장(ledger) 합산 방식. 음수 delta=차감."""

def balance(store, customer_id):
    return store.points_balance(customer_id)

def add(store, customer_id, amount):
    store.points_add(customer_id, amount, "add")

def deduct(store, customer_id, amount):
    if store.points_balance(customer_id) < amount:
        return False
    store.points_add(customer_id, -amount, "fx_render")
    return True

def refund(store, customer_id, amount):
    store.points_add(customer_id, amount, "fx_refund")
```

- [ ] **Step 5: Run test to verify it passes**

Run: `python -m pytest shopping_shorts/tests/test_points.py -v`
Expected: PASS (2 passed).

- [ ] **Step 6: Mutation check**

`deduct`의 `< amount`를 `< 0`으로 바꿔 돌린다 → `test_deduct_insufficient...`가 죽어야 한다. 확인 후 원복.

- [ ] **Step 7: Commit**

```bash
git add shopping_shorts/points.py shopping_shorts/store.py shopping_shorts/tests/test_points.py
git commit -m "feat(points): 렌더 포인트 원장(deduct/refund/balance) + points_ledger 테이블"
```

---

### Task 2: 매칭 규칙기 (effect_match.py — 순수)

**Files:**
- Create: `shopping_shorts/effect_match.py`
- Test: `shopping_shorts/tests/test_effect_match.py`

**Interfaces:**
- Produces:
  - `effect_match.EFFECT_CATALOG: list[dict]` — 각 `{key, needs, desc}` (key∈impact/count/list/callout).
  - `effect_match.theme_for(category: str) -> str` — category→themeName.
  - `effect_match.match_rules(beats: list[dict]) -> list[dict]` — 규칙만으로 fx 리스트. beat=`{s,e,text}`.
  - `effect_match.build_plan(beats, category, video_src, dur_frames, fx=None) -> dict` — FullReel props 조립. fx=None이면 match_rules 사용.
- Consumes: 없음(순수).

- [ ] **Step 1: Write the failing test**

```python
# shopping_shorts/tests/test_effect_match.py
from shopping_shorts import effect_match as em

BEATS = [
    {"s": 0.0, "e": 2.0, "text": "이거 5분이면 만들어요"},
    {"s": 2.0, "e": 4.0, "text": "재료 3가지만 있으면 돼요"},
    {"s": 4.0, "e": 6.0, "text": "진짜 대박이에요"},
]

def test_theme_for_recipe_is_warm():
    assert em.theme_for("레시피") == "warm"
    assert em.theme_for("주식") == "tech"
    assert em.theme_for("모르는장르") == "warm"  # 기본

def test_rules_detect_count_list_impact():
    fx = em.match_rules(BEATS)
    comps = {f["comp"] for f in fx}
    assert "count" in comps   # "5분"
    assert "list" in comps    # "3가지"
    assert "impact" in comps  # "대박"

def test_build_plan_shape_is_fullreel_props():
    plan = em.build_plan(BEATS, "레시피", "full.mp4", 180)
    assert plan["videoSrc"] == "full.mp4"
    assert plan["durationInFrames"] == 180
    assert plan["themeName"] == "warm"
    assert isinstance(plan["beats"], list) and isinstance(plan["fx"], list)
    for f in plan["fx"]:
        assert f["comp"] in {"impact", "count", "list", "callout"}
        assert 0.0 <= f["s"] < f["e"]
```

- [ ] **Step 2: Run test to verify it fails**

Run: `python -m pytest shopping_shorts/tests/test_effect_match.py -v`
Expected: FAIL (module not found).

- [ ] **Step 3: Write effect_match.py (rules)**

```python
# shopping_shorts/effect_match.py
"""비트+카테고리 → 리모션 효과 배치 플랜(FullReel props). 순수 모듈.
규칙 1차 + (Task3) Haiku 2차. 렌더·DB를 모른다."""
import re

EFFECT_CATALOG = [
    {"key": "count",   "needs": ["value", "suffix", "label"], "desc": "숫자 카운트업(가격·시간·개수)"},
    {"key": "list",    "needs": ["title", "items"],           "desc": "항목 리스트 순차 리빌"},
    {"key": "impact",  "needs": ["word"],                     "desc": "강조어 슬램"},
    {"key": "callout", "needs": ["icon", "name", "tag"],      "desc": "아이콘+이름+태그 카드"},
]

_THEME = {"레시피": "warm", "홈템": "warm", "주식": "tech", "데이터": "tech"}

def theme_for(category):
    return _THEME.get((category or "").strip(), "warm")

_NUM = re.compile(r"(\d+)\s*(분|초|원|개|인분|칼로리|kcal)")
_LIST = re.compile(r"(\d+)\s*가지|재료")
_IMPACT = re.compile(r"대박|진짜|완전|폭발|최고|미쳤|역대급")

def match_rules(beats):
    fx = []
    for b in beats:
        t = b.get("text", "")
        m = _NUM.search(t)
        if m:
            fx.append({"s": b["s"] + 0.1, "e": b["e"], "comp": "count",
                       "props": {"label": "POINT", "value": int(m.group(1)),
                                 "suffix": m.group(2), "position": "top"}})
            continue
        if _LIST.search(t):
            fx.append({"s": b["s"] + 0.1, "e": b["e"], "comp": "list",
                       "props": {"title": "LIST", "items": [], "position": "bottom"}})
            continue
        mi = _IMPACT.search(t)
        if mi:
            fx.append({"s": b["s"] + 0.2, "e": b["e"], "comp": "impact",
                       "props": {"word": mi.group(0) + "!", "position": "top"}})
    return fx

def build_plan(beats, category, video_src, dur_frames, fx=None):
    return {
        "videoSrc": video_src,
        "durationInFrames": int(dur_frames),
        "themeName": theme_for(category),
        "sections": [{"s": beats[0]["s"], "e": beats[-1]["e"], "label": "STEP 01"}] if beats else [],
        "beats": [{"s": b["s"], "e": b["e"], "cap": b.get("text", "")} for b in beats],
        "fx": match_rules(beats) if fx is None else fx,
    }
```

- [ ] **Step 4: Run test to verify it passes**

Run: `python -m pytest shopping_shorts/tests/test_effect_match.py -v`
Expected: PASS (3 passed).

- [ ] **Step 5: Mutation check**

`_THEME`에서 `"주식": "tech"`를 지운다 → `test_theme_for_recipe_is_warm`이 죽어야 한다(주식이 warm으로 떨어짐). 확인 후 원복. `match_rules`의 `_NUM` 분기를 `continue` 없이 두면 count 비트가 impact로도 잡히는지 → `test_rules_detect...`가 comps 집합이라 안 죽을 수 있으니, 별도로 "5분 비트가 정확히 count 하나"인 단언을 추가해두면 이 분기를 잠근다.

- [ ] **Step 6: Commit**

```bash
git add shopping_shorts/effect_match.py shopping_shorts/tests/test_effect_match.py
git commit -m "feat(fx): effect_match 규칙기 — 비트+카테고리→FullReel 플랜(count/list/impact)"
```

---

### Task 3: 매칭 Haiku 레이어 (애매한 비트 보강 + 폴백)

**Files:**
- Modify: `shopping_shorts/effect_match.py` (`suggest(beats, category, video_src, dur_frames, client=None)`)
- Test: `shopping_shorts/tests/test_effect_match.py` (추가)

**Interfaces:**
- Produces: `effect_match.suggest(beats, category, video_src, dur_frames, client=None) -> dict` — 규칙 결과에 Haiku가 훅/CTA/감정 비트의 callout·impact를 보강. `client=None`이면 규칙만(테스트·오프라인). LLM 예외 시 규칙 결과로 폴백(무과금).
- Consumes: `client`(google-genai Client, 주입). Task2의 `match_rules/build_plan`.

- [ ] **Step 1: Write the failing test (client 주입 + 폴백)**

```python
def test_suggest_without_client_is_rules_only(monkeypatch):
    plan = em.suggest(BEATS, "레시피", "f.mp4", 180, client=None)
    assert plan["themeName"] == "warm"
    # 규칙만: count/list/impact 존재
    assert {f["comp"] for f in plan["fx"]} >= {"count", "list", "impact"}

class _BoomClient:
    class models:
        @staticmethod
        def generate_content(*a, **k):
            raise RuntimeError("LLM down")

def test_suggest_llm_failure_falls_back_to_rules(monkeypatch):
    plan = em.suggest(BEATS, "레시피", "f.mp4", 180, client=_BoomClient())
    # 폴백: 규칙 결과가 그대로 나온다(예외 삼킴)
    assert {f["comp"] for f in plan["fx"]} >= {"count", "impact"}
```

- [ ] **Step 2: Run to verify fail**

Run: `python -m pytest shopping_shorts/tests/test_effect_match.py -k suggest -v`
Expected: FAIL (suggest 없음).

- [ ] **Step 3: Implement suggest**

```python
# effect_match.py 에 추가
import json

_LLM_PROMPT = (
    "다음 릴스 비트에서, 규칙이 못 잡은 훅/감정절정/CTA 비트에만 효과를 추천해라. "
    "impact(강조어)와 callout(아이콘+이름+태그)만. 실제 대사 단어만 써라. "
    'JSON만: {"extra":[{"beat":인덱스,"comp":"impact|callout","props":{...}}]}'
)

def _llm_extra(beats, client):
    body = "\n".join(f'{i}: {b.get("text","")}' for i, b in enumerate(beats))
    resp = client.models.generate_content(
        model="gemini-2.5-flash",
        contents=[_LLM_PROMPT + "\n" + body],
        config={"response_mime_type": "application/json"},
    )
    data = json.loads(resp.text)
    out = []
    for e in data.get("extra", []):
        b = beats[int(e["beat"])]
        out.append({"s": b["s"] + 0.15, "e": b["e"], "comp": e["comp"], "props": e.get("props", {})})
    return out

def suggest(beats, category, video_src, dur_frames, client=None):
    fx = match_rules(beats)
    if client is not None:
        try:
            fx = fx + _llm_extra(beats, client)
        except Exception:
            pass  # 폴백: 규칙 결과 유지(무과금)
    fx.sort(key=lambda f: f["s"])
    return build_plan(beats, category, video_src, dur_frames, fx=fx)
```

- [ ] **Step 4: Run to verify pass**

Run: `python -m pytest shopping_shorts/tests/test_effect_match.py -k suggest -v`
Expected: PASS.

- [ ] **Step 5: Mutation check**

`suggest`의 `except Exception: pass`를 `except Exception: raise`로 바꾼다 → `test_suggest_llm_failure...`가 죽어야 한다(폴백이 사라져 예외 전파). 확인 후 원복.

- [ ] **Step 6: Commit**

```bash
git add shopping_shorts/effect_match.py shopping_shorts/tests/test_effect_match.py
git commit -m "feat(fx): Haiku 보강 레이어 + LLM 실패 규칙 폴백(무과금)"
```

---

### Task 4: 리모션 렌더 (remotion_render.py)

**Files:**
- Create: `shopping_shorts/remotion_render.py`
- Test: `shopping_shorts/tests/test_remotion_render.py`

**Interfaces:**
- Produces: `remotion_render.render(plan: dict, video_path: str, out_path: str) -> str` — 조립본을 motion/public에 복사, node `render-scene.mjs FullReel <plan> <out_noaudio>` 호출, 원본 오디오를 ffmpeg로 mux, out_path 반환. Remotion 미설치(node/node_modules 없음)면 `RemotionUnavailable` 예외.
- Consumes: 기존 `shopping_shorts/motion` 프로젝트(FullReel 컴포지션, render-scene.mjs).

- [ ] **Step 1: Write the failing test (미설치 감지 + 실렌더 통합)**

```python
# shopping_shorts/tests/test_remotion_render.py
import os, subprocess, shutil, pytest
from shopping_shorts import remotion_render as rr

MOTION = os.path.join(os.path.dirname(rr.__file__), "motion")
HAS_NODE = shutil.which("node") and os.path.isdir(os.path.join(MOTION, "node_modules"))

def test_unavailable_raises(monkeypatch):
    monkeypatch.setattr(rr, "_motion_ready", lambda: False)
    with pytest.raises(rr.RemotionUnavailable):
        rr.render({"videoSrc": "x.mp4"}, "x.mp4", "o.mp4")

@pytest.mark.skipif(not HAS_NODE, reason="node/remotion 미설치")
def test_real_render_makes_nonempty_mp4(tmp_path):
    src = os.path.join(MOTION, "public", "full.mp4")  # 이전 세션 산출물이 있으면 사용
    if not os.path.isfile(src):
        pytest.skip("샘플 조립본 없음")
    plan = {"videoSrc": "full.mp4", "durationInFrames": 60, "themeName": "warm",
            "sections": [{"s": 0, "e": 2, "label": "T"}],
            "beats": [{"s": 0, "e": 2, "cap": "테스트"}], "fx": []}
    out = str(tmp_path / "o.mp4")
    rr.render(plan, src, out)
    assert os.path.getsize(out) > 10000
```

- [ ] **Step 2: Run to verify fail**

Run: `python -m pytest shopping_shorts/tests/test_remotion_render.py -v`
Expected: FAIL (module 없음).

- [ ] **Step 3: Implement remotion_render.py**

```python
# shopping_shorts/remotion_render.py
"""플랜 + 조립본 → 리모션 최종 렌더. node subprocess. stdin=DEVNULL 필수(repo 규약)."""
import json, os, shutil, subprocess

MOTION = os.path.join(os.path.dirname(__file__), "motion")

class RemotionUnavailable(RuntimeError):
    pass

def _motion_ready():
    return bool(shutil.which("node")) and os.path.isdir(os.path.join(MOTION, "node_modules"))

def render(plan, video_path, out_path):
    if not _motion_ready():
        raise RemotionUnavailable("node/remotion 미설치")
    pub = os.path.join(MOTION, "public")
    os.makedirs(pub, exist_ok=True)
    name = plan.get("videoSrc") or "job_src.mp4"
    shutil.copy(video_path, os.path.join(pub, name))
    noaudio = out_path + ".noaudio.mp4"
    r = subprocess.run(
        ["node", "src/render-scene.mjs", "FullReel", json.dumps(plan, ensure_ascii=False), noaudio],
        cwd=MOTION, capture_output=True, text=True, encoding="utf-8", errors="replace",
        stdin=subprocess.DEVNULL)
    if r.returncode != 0 or not os.path.isfile(noaudio):
        raise RuntimeError("remotion 렌더 실패: " + (r.stderr or "")[-500:])
    # 원본 오디오 mux
    r2 = subprocess.run(
        ["ffmpeg", "-y", "-i", noaudio, "-i", video_path, "-c:v", "copy", "-c:a", "aac",
         "-map", "0:v:0", "-map", "1:a:0?", "-shortest", "-movflags", "+faststart", out_path],
        capture_output=True, text=True, encoding="utf-8", errors="replace", stdin=subprocess.DEVNULL)
    os.remove(noaudio)
    if r2.returncode != 0 or not os.path.isfile(out_path):
        raise RuntimeError("오디오 mux 실패: " + (r2.stderr or "")[-500:])
    return out_path
```

- [ ] **Step 4: Run to verify pass**

Run: `python -m pytest shopping_shorts/tests/test_remotion_render.py -v`
Expected: `test_unavailable_raises` PASS; 실렌더는 환경에 따라 PASS 또는 skip.

- [ ] **Step 5: 실산출물 눈 확인 (테스트 통과≠동작)**

node/remotion 있는 환경에서 실렌더를 돌리고 out.mp4에서 프레임을 뽑아 효과가 실제로 떴는지 눈으로 본다:
```bash
ffmpeg -y -ss 1 -i o.mp4 -frames:v 1 check.png -hide_banner
```
효과·자막이 보이면 통과. 안 보이면 plan/컴포지션 배선을 고친다.

- [ ] **Step 6: Mutation check**

`_motion_ready`가 항상 True를 반환하게 바꾼다 → `test_unavailable_raises`가 죽어야 한다. 확인 후 원복.

- [ ] **Step 7: Commit**

```bash
git add shopping_shorts/remotion_render.py shopping_shorts/tests/test_remotion_render.py
git commit -m "feat(fx): remotion_render — 플랜+조립본→node 렌더→오디오 mux, 미설치 그레이스풀"
```

---

### Task 5: 라우트 3개 + 믹스잡 fx 필드 (suggest/render/status)

**Files:**
- Modify: `shopping_shorts/store.py` (mix_jobs에 `fx_plan TEXT, fx_status TEXT, fx_path TEXT` — CREATE에 추가 + 기존DB ALTER, update_mix_job 화이트리스트, get_mix_job SELECT 셋 다)
- Modify: `shopping_shorts/app.py` (3 라우트)
- Test: `shopping_shorts/tests/test_fx_routes.py`

**Interfaces:**
- Consumes: `effect_match.suggest`, `remotion_render.render`, `points.deduct/refund`, `store.get_mix_job/update_mix_job`.
- Produces:
  - `POST /api/produce/fx/suggest {job_id, category}` → `{plan}` (믹스잡 비트+타이밍으로 suggest; DB 미기록).
  - `POST /api/produce/fx/render {job_id, plan}` → 포인트 검사·차감(정액 `FX_RENDER_COST`), 백그라운드 렌더 예약, `{status:"queued"}`. 잔액부족 402.
  - `GET /api/produce/fx/status/{job_id}` → `{fx_status, fx_url?}`.
- Global: `FX_RENDER_COST = 10` (config).

- [ ] **Step 1: Write the failing test (assemble/render monkeypatch)**

```python
# shopping_shorts/tests/test_fx_routes.py
from fastapi.testclient import TestClient
from shopping_shorts import app as app_module, points

def _client(tmp_path, monkeypatch):
    monkeypatch.setattr(app_module.config, "DB_PATH", str(tmp_path / "t.db"), raising=False)
    # get_mix_job이 비트를 주도록 스텁
    beats = [{"s":0,"e":2,"text":"5분이면 완성"}]
    monkeypatch.setattr(app_module.store, "get_mix_job",
        lambda jid: {"job_id": jid, "beats": beats, "video_path": "x.mp4", "dur_frames": 60, "category": "레시피"})
    return TestClient(app_module.app)

def test_render_deducts_points_and_queues(tmp_path, monkeypatch):
    c = _client(tmp_path, monkeypatch)
    points.add(app_module.store, 0, 100)
    called = {}
    monkeypatch.setattr(app_module.remotion_render, "render",
        lambda plan, vp, out: called.setdefault("ok", True) or out)
    r = c.post("/api/produce/fx/render", json={"job_id":"j1","plan":{"videoSrc":"x.mp4","fx":[]}})
    assert r.status_code == 200
    assert points.balance(app_module.store, 0) == 100 - app_module.FX_RENDER_COST

def test_render_insufficient_points_402(tmp_path, monkeypatch):
    c = _client(tmp_path, monkeypatch)  # 잔액 0
    r = c.post("/api/produce/fx/render", json={"job_id":"j1","plan":{"videoSrc":"x.mp4","fx":[]}})
    assert r.status_code == 402
```

- [ ] **Step 2: Run to verify fail**

Run: `python -m pytest shopping_shorts/tests/test_fx_routes.py -v`
Expected: FAIL (라우트 없음).

- [ ] **Step 3: store 필드 + 라우트 구현**

store: `mix_jobs` CREATE에 `fx_plan TEXT, fx_status TEXT, fx_path TEXT` 추가 + 기존DB용 ALTER(선례 6건 패턴) + `update_mix_job` 화이트리스트에 세 필드 + `get_mix_job` SELECT에 포함.

app.py:
```python
from shopping_shorts import effect_match, remotion_render, points
FX_RENDER_COST = 10

@app.post("/api/produce/fx/suggest")
def api_fx_suggest(request: Request, body: dict):
    job = store.get_mix_job(body["job_id"]) or {}
    plan = effect_match.suggest(job.get("beats", []), body.get("category") or job.get("category"),
                                job.get("video_path", ""), job.get("dur_frames", 0),
                                client=None)  # suggest 단계는 규칙만(무과금). Haiku는 옵션.
    return {"plan": plan}

def _fx_render_job(job_id, plan, cid):
    try:
        job = store.get_mix_job(job_id) or {}
        out = os.path.join(WORK_DIR, f"fx_{job_id}.mp4")
        remotion_render.render(plan, job.get("video_path"), out)
        store.update_mix_job(job_id, fx_status="done", fx_path=out)
    except Exception as e:
        points.refund(store, cid, FX_RENDER_COST)   # 실패 시 환불
        store.update_mix_job(job_id, fx_status="failed")

@app.post("/api/produce/fx/render")
def api_fx_render(request: Request, body: dict, background_tasks: BackgroundTasks):
    cid = getattr(request.state, "customer_id", 0)
    if not points.deduct(store, cid, FX_RENDER_COST):
        return JSONResponse({"error": "포인트 부족"}, status_code=402)
    store.update_mix_job(body["job_id"], fx_status="queued", fx_plan=json.dumps(body["plan"], ensure_ascii=False))
    background_tasks.add_task(_fx_render_job, body["job_id"], body["plan"], cid)
    return {"status": "queued"}

@app.get("/api/produce/fx/status/{job_id}")
def api_fx_status(job_id: str):
    job = store.get_mix_job(job_id) or {}
    out = {"fx_status": job.get("fx_status")}
    if job.get("fx_status") == "done":
        out["fx_url"] = f"/api/produce/fx/file/{job_id}"
    return out
```

- [ ] **Step 4: Run to verify pass**

Run: `python -m pytest shopping_shorts/tests/test_fx_routes.py -v`
Expected: PASS (2 passed).

- [ ] **Step 5: Mutation check**

`_fx_render_job`의 `points.refund(...)` 줄을 지운다 → 렌더 실패 시 환불 안 됨. "렌더 실패 시 환불" 테스트를 하나 추가(render를 예외로 스텁, background 직접 호출)해 이 뮤턴트가 죽는지 확인. 확인 후 원복.

- [ ] **Step 6: import 검증 + Commit**

```bash
python -c "import shopping_shorts.app"
git add shopping_shorts/app.py shopping_shorts/store.py shopping_shorts/tests/test_fx_routes.py
git commit -m "feat(fx): suggest/render/status 라우트 + 렌더당 정액차감·실패환불 + mix_jobs fx 필드"
```

---

### Task 6: 꾸미기 위저드 패널 (produce.html)

**Files:**
- Modify: `shopping_shorts/static/produce.html` (꾸미기 영역에 "고급효과 ✨" 토글 + 4스텝 위저드)
- Test: `shopping_shorts/tests/test_fx_wizard_js.py` (repo 패턴: HTML의 JS를 잘라 `node --check` + 순수함수 node 실행. 선례 test_produce_category_race.py)

**Interfaces:**
- Consumes: `/api/produce/fx/suggest|render|status`.
- Produces: 위저드 상태머신(스텝 1~4), 각 스텝 독립 렌더. 순수 헬퍼 `fxPlanSummary(plan)` (효과 개수·테마 요약) 를 테스트 가능하게 파일 상단 함수로 분리.

- [ ] **Step 1: Write failing JS test (node --check + 헬퍼)**

```python
# shopping_shorts/tests/test_fx_wizard_js.py
import re, subprocess, os
HTML = os.path.join(os.path.dirname(__file__), "..", "static", "produce.html")

def _extract(fn):
    src = open(HTML, encoding="utf-8").read()
    m = re.search(r"function %s\([^)]*\)\s*\{.*?\n\}" % fn, src, re.S)
    assert m, f"{fn} 없음"
    return m.group(0)

def test_produce_html_js_parses():
    src = open(HTML, encoding="utf-8").read()
    js = "\n".join(re.findall(r"<script>(.*?)</script>", src, re.S))
    p = subprocess.run(["node", "--check", "-"], input=js, text=True,
                       capture_output=True, stdin=None if False else subprocess.PIPE)
    # node --check는 stdin 파일명 필요 → 임시파일로
def test_fx_plan_summary_counts():
    fn = _extract("fxPlanSummary")
    script = fn + '\nconsole.log(JSON.stringify(fxPlanSummary({themeName:"warm",fx:[1,2,3]})));'
    r = subprocess.run(["node", "-e", script], capture_output=True, text=True, stdin=subprocess.DEVNULL)
    assert '"count":3' in r.stdout and '"warm"' in r.stdout
```
(주: `node --check`는 파일 인자를 받으므로 실제 구현 시 임시파일로 저장해 검사. 위 첫 테스트는 그 형태로 완성한다.)

- [ ] **Step 2: Run to verify fail**

Run: `python -m pytest shopping_shorts/tests/test_fx_wizard_js.py -v`
Expected: FAIL (fxPlanSummary 없음).

- [ ] **Step 3: produce.html에 토글+위저드+헬퍼 추가**

- 꾸미기 섹션에 체크박스 `고급효과 ✨ (포인트 차감)`.
- 켜면 4스텝 패널: ①비트 목록(읽기) ②"효과 추천" 버튼→`/fx/suggest`→플랜 표시(효과 토글 체크) ③미리보기(플랜 요약 `fxPlanSummary`) ④"고급 렌더 (N포인트)" 버튼→`/fx/render`→`/fx/status` 폴링→완료 시 다운로드 링크.
- 순수 헬퍼:
```javascript
function fxPlanSummary(plan){
  return { theme: plan.themeName, count: (plan.fx||[]).length };
}
```

- [ ] **Step 4: Run to verify pass**

Run: `python -m pytest shopping_shorts/tests/test_fx_wizard_js.py -v`
Expected: PASS.

- [ ] **Step 5: Mutation check**

`fxPlanSummary`의 `(plan.fx||[]).length`를 `0`으로 바꾼다 → `test_fx_plan_summary_counts`가 죽어야 한다. 확인 후 원복.

- [ ] **Step 6: Commit**

```bash
git add shopping_shorts/static/produce.html shopping_shorts/tests/test_fx_wizard_js.py
git commit -m "feat(fx): 꾸미기 고급효과 위저드 패널(4스텝) + fxPlanSummary 헬퍼"
```

---

### Task 7: 서버 리모션 설치 (배포)

**Files:**
- Modify: `deploy/auto_deploy.sh` 또는 배포 문서 (shopping-shorts 서버에 node + `cd shopping_shorts/motion && npm ci` 1회, 크롬 헤드리스는 최초 렌더가 자동 다운로드)
- Create: `docs/superpowers/notes/서버-리모션-설치.md` (수동 부트스트랩 절차)

- [ ] **Step 1: 설치 절차 문서화**

서버에서 1회: `node -v`(없으면 nodesource 설치), `cd /home/ubuntu/lotto-stock-wiki/shopping_shorts/motion && npm ci`. 최초 렌더 시 `@remotion/renderer`가 chrome-headless-shell 자동 다운로드(수백 MB, /home 용량 확인).

- [ ] **Step 2: 그레이스풀 확인**

미설치 서버에서 `/api/produce/fx/render` 호출 시 `remotion_render._motion_ready()`가 False → 렌더 잡이 즉시 실패·환불, UI는 "고급효과 사용 불가(서버 준비중)". 이 경로가 500이 아니라 안전 실패인지 확인.

- [ ] **Step 3: Commit**

```bash
git add deploy/ docs/superpowers/notes/서버-리모션-설치.md
git commit -m "chore(deploy): 서버 리모션 설치 절차 + 미설치 그레이스풀 확인"
```

---

### Task 8: 최종 whole-branch 리뷰 (생략 금지)

- [ ] **Step 1:** 전체 브랜치 diff를 리뷰(개별 태스크가 통과시킨 seam=저장위치≠읽기위치, 실 payload 경로 grounding). 이 저장소는 최종리뷰만 잡은 Critical 전례가 반복됨(대본믹스 C-1~3).
- [ ] **Step 2:** 실 릴스 1편으로 e2e: 제작소 믹스 → 꾸미기 고급효과 위저드 → 렌더 → mp4 프레임 눈확인 → 포인트 차감 확인 → (강제 실패시) 환불 확인.
- [ ] **Step 3:** 게이트(`py tools/track.py finish 모션효과`) — 기준선 대비 실패 안 늘었는지.

---

## Self-Review

**Spec coverage:** §3 흐름=Task5/6, §4 컴포넌트=Task1~6, §5 계약(FullReel props)=Global+Task2, §6 매칭=Task2/3, §7 테마=Task2(_THEME), §8 에러/환불=Task3/5, §9 서버인프라=Task7, §10 확장(EFFECT_CATALOG/theme)=Task2, §11 테스트=각 태스크 뮤테이션+Task4 실산출물+Task8, §13 성공판정=Task8. 커버됨.

**Placeholder scan:** category→theme는 `_THEME`에 실제 값(레시피/홈템/주식/데이터)+기본 warm으로 확정. 열린 항목 없음.

**Type consistency:** 플랜 키(videoSrc/durationInFrames/themeName/sections/beats/fx)가 Task2 build_plan·Task4 render·Task5 라우트·FullReel props에서 동일. fx 항목 `{s,e,comp,props}` 일관. points `deduct/refund/balance(store, cid, amount)` 시그니처 Task1·5 일치.

**미확정(구현 중 확인):** `store.get_mix_job`이 실제로 beats/video_path/dur_frames/category를 담는지 — Task5 Step1에서 실 job 구조를 grep해 필드명을 맞춘다(현재 스텁 가정). 없으면 mix 완료 시 이 필드를 채우는 한 줄을 mix_pipeline에 추가(별도 커밋).
