# AGENTS.md — 이 저장소에서 일하는 AI 에이전트 규칙

> Codex(및 AGENTS.md를 읽는 도구)용 규칙이다. **상세 규칙 원본은 `CLAUDE.md`** 이며,
> 아래와 충돌하면 `CLAUDE.md`가 이긴다. 세션을 시작하면 `CLAUDE.md`를 먼저 읽어라.

## 0순위 — 추측 금지

파일·경로·코드·수치·상태를 말하기 전에 **직접 열어보고 확인한다.** 확인 못 했으면
"확인 안 했습니다"라고 말한다. 기억·짐작으로 사실을 말하지 않는다.

"고쳤다 / 된다 / 완료"는 **실제로 돌려본 결과**를 보고 말한다. 문법 검사·테스트 통과는
확인이 아니다 — 화면·서버·산출물에서 눈으로 본 것만 확인이다.

## 사용자에게 보이는 글은 전부 한국어

질문·선택지·요약·완료 보고 전부 한국어. 코드·파일경로·명령어·에러 원문·git 커밋 본문은 원문 그대로.

## ★코드 작업은 반드시 트랙 폴더에서 (가장 중요)

**이 폴더(저장소 루트)에서 코드를 고치지 마라.** 여러 세션이 같은 디스크를 보기 때문에,
루트에서 고치면 다른 세션이 커밋할 때 내 미완성 코드까지 같이 라이브로 나간다(실제 사고 다수).

```bash
py tools/track.py start <트랙명>     # .tracks/<트랙명> 폴더 + track/<트랙명> 브랜치 생성
py tools/track.py list              # 열린 트랙 목록
py tools/track.py finish <트랙명>    # 검사 통과해야만 main 병합 = 배포
```

- 편집은 전부 `.tracks/<트랙명>/...` 경로로 한다. 루트의 코드 파일은 **하나도** 건드리지 않는다.
- git 명령은 전부 `-C`로: `git -C .tracks/<트랙명> add -A` / `commit` / `status`
- 내 트랙 폴더 안에서는 `git add -A`가 안전하다(남의 파일이 없다).
- 커밋 전 `git -C .tracks/<트랙명> status --porcelain`으로 내 파일만 있는지 눈으로 본다.
- 작업이 **끝났을 때만** `finish`. 진행 중이면 커밋만 해둔다(트랙 브랜치에 남아 유실 없음).

### 예외 — 트랙 폴더에서 하면 안 되는 것

`raw/` 읽기, 옵시디언 위키(`wiki/`) 편집, DB(`shopping_shorts/data/`) 확인은 **루트에서** 한다.
git이 추적하지 않는 것들이라 트랙 폴더에는 낡거나 비어 있다.

## 배포

- 서버는 **`main`만** 추적한다. 다른 브랜치에 커밋하면 서버에 영영 안 간다.
- 배포 = `py tools/track.py finish <트랙명>` (검사 통과 → main 병합).
- **서버 파일을 SSH로 직접 고치지 마라.** git에 안 남아 다음 pull에 덮인다.
- 서버에서 `git add`/`commit` 금지 — 워킹트리가 더러워지면 자동배포가 통째로 멈춘다.
- 자동배포는 새벽 2~6시 창에서만 돈다. 낮에 급하면 사장님 확인 후 수동 배포.

## 같은 판단을 두 곳에 적지 마라

같은 결정이 코드 두 군데에 적히면 언젠가 반드시 어긋난다. 특히:

```python
if A: x = 1
x = 2        # ← A가 통째로 죽는다. 이 모양을 보면 즉시 의심하라
```

같은 일을 하는 코드가 파일 2개 이상에 있으면 **함수 하나로 뽑아 공용화**한다.
남의 코드와 다르게 짜기 전에 **왜 저긴 저렇게 했는지 먼저 읽어라.**

## 시작 전에 과거 기록부터 찾는다

```bash
py tools/find_work.py <주제어>
```

핸드오프·트랙로그·규칙·메모리를 훑어 관련 기록을 최신순으로 준다. 나온 파일을 읽고 착수한다.
단, **기록은 쓸 당시의 사실**이다 — 코드·서버가 아직 그런지 확인하고 답하라.

## 작업이 끝나면 남긴다

- `handoff/<트랙>.md` — 무엇을·왜·다음 할 일 (내 트랙 파일만 수정)
- `wiki/log.d/<트랙>.md` — 1~3줄 요약

기록이 없으면 다음 세션이 같은 조사를 처음부터 다시 한다.
