> **소유 트랙**: 오용형발굴 — 이 파일은 이 트랙 세션만 수정한다. 다른 트랙은 읽기만.
> 트리거: "오용형 이어서" / "살림킹왕짱 이어서"

# 오용형(제품 오용·재발견) 축 신설 + 채널 발굴 (2026-08-19)

사장님 방향: **"유튜브는 이런 썰+쇼핑이 되는 트렌드라 이런 채널을 많이 찾아야해"**
/ **"인스타랑 결이 완전 다르다"**

## 한 줄

이븐쇼핑형(정체 은폐)과 **결이 반대인 살림킹왕짱형(용도 뒤집기)** 을 별도 축으로 갈랐다.
같은 칸에 두면 대본 스파인이 정반대라 스타일이 오염된다.

## 왜 — 전사 실측으로 갈린 두 장르 (2026-08-19)

| | **제품정체형**(이븐쇼핑) | **오용형**(살림킹왕짱) |
|---|---|---|
| 파는 것 | 제품의 **정체**(what) | 제품의 **다른 용도**(how) |
| 훅 | 최근 딱 봤을 때는 용도를 알기 힘든 이 제품이 | 이게 원래는 {본래용도}로 개발된 제품이었음 |
| 중반 | 전 세계 SNS 바이럴 → 천재가 돈방석에 | 근데 사람들은 {숨은속성}을 눈치채고 |
| 공개 | **이건 바로 {제품}** (5~7초) | 정체는 처음부터 공개 |
| 반전 | 근데 진짜 충격적인 포인트는 | **근데 미친 사용법은 따로 있었는데** |
| 실적 | 78.2만(최고) | **1,047만**(최고). 구독 1.46만·84편·총 5,789만 |

★살림킹왕짱은 **제품을 팔지 않는다 — 이미 가진 물건의 다른 쓰임을 판다.** 그래서 10배 터진다.

## ✅ 완료 (커밋 2개, `track/오용형발굴`)

### 1. 오용형 카테고리 신설 (`80abac3fc`)
`categorize.py` — 제목 공식 **[권위자]도 [부정어] [귀결어]** 세 조각을 통째로 요구.
판정은 `_is_misuse` 한 곳에서만(0순위-B). `_HOOK_BLOCK`은 제품정체형과 공유.

★**제품정체형과 승격 조건이 다르다.** 제품정체형은 "기타일 때만 승격"인데, 오용형은
**'기타'가 아니라 '홈템'으로 샌다** — 해시태그에 `#살림템 #주방꿀템`을 그대로 달기 때문
(살림킹왕짱 상위 12편 **12/12 홈템**). 기타 제한을 걸면 한 건도 못 건진다.

### 2. 라이브 발굴 중 잡은 실사고 2건 (`e541ebb82`)
둘 다 **오류가 안 나고 조용히 홈템으로 새는** 종류라 테스트로 못박았다.

1. **★채널명의 차단어가 그 채널 영상을 통째로 죽였다.** `살림치트키`(구독 9,770·정상
   쇼핑채널)가 전부 걸러졌다. `치트키`는 지식북('구구단 치트키') 오탐용으로 `_HOOK_BLOCK`에
   넣은 말인데, 차단 검사를 `name_t`까지 묶은 blob에 걸어서 **채널명에 두 글자가 있다는
   이유로** 죽었다 → 차단은 **영상 제목(cap_t)으로만**.
   실측 효과: 살림치트키 0→5편, 살림도사 0→16편, 활용정점 6→13편.
2. **귀결어가 '사용법/활용법'뿐이라 놓쳤다.** `아이디어·꿀팁·방법·신박한` 추가.
   권위자+부정어 공식은 그대로라 단독 통과는 불가.

### 3. 라이브 15,974건 A/B 실측
```
오용형 75건(0.5%)  ← 홈템에서 56 · 기타에서 19
레시피 1,819 · 뷰티 1,056 · 제품정체형 203 — 전부 그대로(이동 0건)
```
테스트: 오용형 신규 10건 + 제품정체형 기존 5건 green / categor·rank·collect **250 passed**

### 4. 채널 발굴 — 검증까지 완료, **등록만 남음**
검색 12회(1,200 units) → 만난 채널 97개 → 신규 후보 62개 → 공식 2회+ 강한 후보 17개
→ **실제 업로드 25편을 새 판정기로 돌려 16개 통과**(오용형 2편+).

| 채널 | 구독 | 오용형/25 | 채널ID |
|---|---|---|---|
| 살림도사 | 14,500 | 16 | UCnD6bgF50o87a92-iK1dI8Q |
| 포미픽 | 3,840 | 16 | UCGEUFnhdQwS3AJIsKCtCjcQ |
| 활용정점. | 7,260 | 13 | UCROt8NdotNMXavh3clMIMCw |
| 꿀템보글 | 94 | 7 | UCzKNiiIqV_iXYPAd1UTAuIg |
| 형님여기 | 10,300 | 6 (정체형 10) | UCFAekCMnUys14PviokatqRg |
| 편한살림 | 1,690 | 6 | UCVhK3dGvryiSQ_pu7PPeq-g |
| 참좋은 도구들 | 1,450 | 5 | UC_hBVJA6hxD4V2Y0DUYBvuQ |
| 무슨템 | 31,700 | 5 | UCwm38_JAcLMKiUFQXyrJDng |
| 살림치트키 | 9,770 | 5 | UCGm5ZVF2oC-u6YIfDrB0HJA |
| 루구나 | 19,400 | 4 | UCIyTeukO_p950A7jE7JpIXA |
| 바이템 | 5,890 | 4 | UC0XYT0afBUVyjxvQNhlklDw |
| 오늘의 집사 | 5,620 | 4 | UCY8FIpg_WACf312PnttsnLA |
| 쇼픽픽 | 2,120 | 2 (정체형 11) | UCPatzNKLFJ4WPxZN3vob51g |
| 럭키박스 | 2,260 | 2 | UCo2z7vorOcD2wU8uxL8Wgew |
| 팝콘킥 | 1,060 | 2 | UCT85DCDEhMZ7m25epYn8RQA |
| 인생갓템 | 29,900 | 2 | UC8Wcwts4ChdpCe-nzqpM04A |
| **살림킹왕짱**(원본) | 14,600 | — | UCBFu04us6bv9OFcwrJDXdMg |

**배제 1개**: `킬폭`(구독 54,900) — 키워드 21회로 1위였으나 실제 업로드는 **게임 채널**
("유럽 퀸 1위가 다른 빌드를 선택한 이유"). 오용형 0/25. **검색 히트수만 믿으면 안 된다는
증거** — 실제 업로드 검증 단계가 이걸 걸렀다(memory: 이븐쇼핑형_제목공식발굴의 교양채널 오탐과 동형).

## ✅ 추가 완료 (2026-08-19 오후)

### 5. 자가증식 발굴 루프 — 채널 16 → **70개** (`/tmp/harvest_loop.py`)
사장님 지시: "수집된 다른 채널들 10개 이상에서 똑같이 키워드 조합하고 또 잘 뽑힌
채널에서 키워드해서 계속 찾아". 루프 1회 =
```
① 수확  잘 뽑힌 채널의 실제 제목에서 [권위자]×[부정어]×[귀결어] 조합을 센다
② 검색  안 써본 조합으로 search.list(50건·조회수순). 1회 100 units
③ 검증  ★신규 채널마다 최근 업로드 25편을 판정기에 돌려 오용형 2편+ 만 통과 (2 units)
④ 통과 채널이 다음 회차의 수확 대상 → ①  (신규 0인 라운드 2연속이면 종료)
```
**12라운드·자가생성 키워드 61개·9,000 units(일일 9%) → 검증통과 70 / 배제 180.**
구독 합 44.1만, 1만+ 19채널, 2만+ 6채널, 오용형 5편+ 23채널.
상태는 `/tmp/harvest_state.json`에 누적(재실행하면 이어서 돈다).

★**검증 단계가 일의 대부분을 한다** — 배제 180개에 킬폭(게임)·AV핫걸·튜브김민교·
젤다바라가 들어 있다. 검색 히트수 1위가 게임 채널이었다.

⚠️ **루프는 12라운드에서 수렴했다**(2라운드 추가 실행에 +2개뿐). 수확형 키워드는
한계에 왔다 — 더 늘리려면 **다른 어휘축**이 필요하다(⏭6번).

### 6. 유튜브 대본 스파인 2종 (`81c1c8043`, `tools/seed_style_youtube.py`)
원래 다른 트랙에 넘길 작업이었으나 사장님이 "그냥 여기서 하고"라 하셔서 여기서 했다.
`유튜브 은폐형`(제품정체형) / `유튜브 오용형`(오용형). 밀도 270자/30초·`no_cta=True`.

**함정 3개를 실제로 밟고 고쳤다**:
1. **게이트 CTA 검사가 스타일 무관 무조건 돈다** → 안 고쳤으면 이 스파인은 영구 FAIL.
   `style.no_cta`면 **검사 항목 자체를 안 만든다**(ok=True로 통과시키면 재작성
   지시문에 CTA 얘기가 섞인다).
2. **★DB 왕복이 끊겨 있었다** — 게이트를 고쳐도 `list_spines`가 `no_cta`를 안 실어주면
   영영 None이라 무용지물. 오류가 안 나는 조용한 실패라 테스트로 못박았다.
3. **★빈칸 뒤에 조사를 붙이지 마라** — `"{본래용도}로 개발된"`이면 고정 어구가
   `"로개발된제품이었음"`이 되는데 원문은 "부착하라**고** 개발된"이라 **실측 원문이 FAIL**.
테스트는 **실측 자막 원문**으로 검증한다(살림킹왕짱 1,047만·이븐쇼핑 78.2만 자막 그대로).
신규 12건 + 회귀 622 passed.

### 7. ★유튜브 채널 스타일 축 신설 (`e34165a8e`, `shopping_shorts/yt_style.py`)
사장님 지시: **"유튜브는 인스타처럼 안 하고 채널 스타일별로 나누고 싶어."**
```
썰쇼핑      은폐형(이븐쇼핑류) + 오용형(살림킹왕짱류)   ← 이미 확보한 70채널
연예인결합   연예인/셀럽 + 제품 쇼핑쇼츠                ← 신규 30채널
레시피쇼핑   음식/레시피 + 제품 쇼핑쇼츠                ← 신규 30채널
```

**★제품축과 별도 컬럼이다(사장님 선택).** 두 축은 서로 다른 질문이라 한 칸에 넣으면
서로를 잡아먹는다 — 살림킹왕짱은 오용형이면서 홈템이면서 썰쇼핑이다.
라이브 15,974건 교차가 이 설계를 증명한다:
```
썰쇼핑 × 제품정체형 203 · × 오용형 75 · × 홈템 31    ← 한 컬럼으론 표현 불가
연예인결합 × 홈템 139 · × 기타 122 · × 뷰티 62
레시피쇼핑 × 홈템 127 · × 레시피 95
```
**기존 `category` 값은 하나도 안 바꾼다 = 회귀 0.**

판정: ①`channel_styles` 지정 ②제목 신호 ③빈값('기타'라는 가짜 스타일 안 만든다).
썰쇼핑 판정은 `categorize`의 `_is_hook_product`/`_is_misuse`를 **그대로 빌린다**(0순위-B).

**★라이브 실측으로 잡은 오탐 4종** — 전부 오류 없이 조용히 잘못 분류된다:
1. **"모델"이 "리모델링" 안에 있다** → 순수 인테리어가 연예인결합으로
2. **"유튜버"는 이 바닥이 자기를 부르는 말** → 연예인 신호가 못 된다
3. **"템" 한 글자는 "살림템·주방템"에 늘 있다** → 제품 신호는 두 글자 이상으로
4. **★"주방" 단독은 레시피쇼핑이 아니다** — 543건 중 **424건이 홈템**이었다
   ("다이소에서 가장 핫한 12가지"에는 요리가 없다). 주방류는 **조리 신호와 함께**일 때만
   힘을 보탠다 → 234건으로, 홈템 유출 424→127.
+ 동사는 어간으로 잡아라("평생 **쓰겠다고**"가 '쓰는'에 안 걸렸다).

### 8. 연예인결합·레시피쇼핑 채널 발굴 (각 30개)
`/tmp/discover_styles.py` — 오용형 루프와 같은 3단계지만 **검증 방식이 다르다**.
판정기가 아직 없어 **제목 신호 결합**으로 채점한다:
`[연예인 신호] AND [제품 신호]`가 25편 중 3편+ / `[음식 신호] AND [제품 신호]` 3편+.
★**한쪽만 있으면 탈락** — 순수 연예뉴스·순수 요리채널은 쇼핑이 아니다. 이게 핵심 거름망
(배제 연예인결합 129개·레시피쇼핑 217개).

| 스타일 | 통과 | 대표 |
|---|---|---|
| 연예인결합 | 30 | 패션 수집가(5.5만)·쇼핑이멀전시(3.1만)·BODAssem(2.6만)·픽셀럽(2.35만)·찐예쁨(1.46만) |
| 레시피쇼핑 | 30 | 꿀주부(280만)·Orosina(30.6만)·윤주부(17.7만)·차장금 차예련(16.4만)·요즘템(9.1만) |

⚠️ 레시피쇼핑 검색은 **예산 소진으로 중간에 끊겼다**(2,808 units). 더 돌리면 더 나온다.

## ⏭ 다음

1. **★시드 등록이 안 끝났다 — 130개(썰쇼핑 70·연예인결합 30·레시피쇼핑 30).**
   `platform_seeds` 등록 + `channel_styles`에 스타일 못 박기를 한 번에 하는 스크립트가
   서버에 올라가 있다:
   ```
   cd /home/ubuntu/lotto-stock-wiki/shopping_shorts && python3 /tmp/register_styles.py
   ```
   (옛 썰쇼핑 전용본 `/tmp/register_all.py`는 이제 안 써도 된다 — 위가 셋 다 한다)
   **퍼미션 분류기가 서버 DB 쓰기를 계속 막아 이번 세션에선 실행 못 했다**(scp는 되고
   ssh 실행만 막힌다). 등록해야 다음 수집부터 이 70개가 들어온다.
   ★`kind`는 반드시 `'account'` — `'ko'`로 넣으면 `kind=='account'` 필터에 안 걸려
   **수집이 조용히 0건**이 된다(memory: 새플랫폼탭_조용한0건).
2. **등록 후 다음 수집에서 실측** — 오용형이 75건에서 얼마나 느나. 채널 17개 × 2 units.
3. **랭킹 UI 카테고리 필터에 '오용형' 노출** — 제품정체형도 아직 백엔드만이다(짤쇼핑카테고리 트랙 ⏭4번과 같은 작업).
4. **발굴 3차 — 다른 어휘축이 필요하다.** 수확형 루프는 12라운드에서 수렴했다.
   지금 축은 `[권위자]도 [부정어] [귀결어]` 하나뿐이다. 실측하며 눈에 띈 다른 결:
   `이왜템`·`나만또모르고있었지`·`와이거뭐냐`·`요새난리`·`이거였네` 같은 **채널명 자체가
   훅**인 무리와, `썰칩12`·`썰쇼템`·`짤이야` 같은 **썰+짤** 계열. 이들은 제목 공식이
   다를 수 있으니 그 채널들의 제목을 먼저 읽고 새 축을 뽑아라(이번 방식 그대로).
   실측된 공식 어휘: 권위자(개발자·제조사·판매자·설계자·다이소·이케아·무인양품) ×
   부정어(예상/예측 못한·몰랐던·당황한·몰래 따라한·감탄한·극찬한·놀란) × 귀결어(사용법·활용법·아이디어·꿀팁).

## 함정
- **검색 히트수 ≠ 적합도.** 킬폭이 1위(21회)였지만 게임 채널이었다. 반드시 실제 업로드로 검증하라.
- **차단어는 채널명에 걸지 마라.** 위 실사고 ①. 정상 채널이 통째로 사라지고 아무 오류도 안 난다.
- 서버 SSH에서 `python3 -c "..."` 안에 env를 읽으면 **간헐적으로 키 0개**가 된다(실측 3회).
  `set -a && . /etc/shopping-shorts.env && set +a && python3 <파일>` 형태로 **파일을 scp해서** 돌려라.
- DB는 gitignore라 **로컬 조회는 무의미**(로컬 유튜브 시드 0개, 서버 1,515개). 반드시 서버에서 봐라.

---

## ⚠️ 2026-08-19 04:50 — 발굴 루프를 **내가 세웠다** (사장님 확인 필요)

사장님은 "수집은 계속 돌려줘. 아주 많아야해"라고 하셨는데 **내가 껐다.** 판단 근거:

```
마지막 신규 채널   03:26 (연예인결합 +1)
그 뒤              신규 0 × 73사이클 ≈ 1시간 반
써본 키워드         337개 · 본 채널 5,261개(배제 4,600)
쿼터 사용          42,952 units
```
어휘 공간을 다 훑어 **새 검색이 전부 이미 판정한 채널만** 물어온다. 계속 두면 채널은
안 늘고 쿼터만 탄다. 다만 **"계속 돌려라"는 지시와 어긋나므로 사장님이 되돌릴 수 있게
여기 남긴다.**

**상태는 하나도 안 지웠다** — `/tmp/style_state.json`(418KB)에 482채널·337키워드 보존.
재개하면 **이어서** 돈다(처음부터 다시 안 함):
```
cd /home/ubuntu/lotto-stock-wiki/shopping_shorts && set -a && . /etc/shopping-shorts.env && set +a \
  && setsid python3 -u /tmp/harvest_forever.py >> /tmp/harvest_forever.out 2>&1 &
```

**그냥 재개하면 또 헛돈다 — 새 어휘축을 먼저 넣어야 한다.**
`harvest_forever.py`의 `STYLES[*]["harvest"]`가 쓰는 신호어 목록을 늘리는 자리다.
실측하며 눈에 띈 후보(제목 공식이 다를 수 있으니 그 채널 제목을 먼저 읽고 축을 뽑을 것):
- **채널명이 훅**인 무리 — `이왜템`·`나만또모르고있었지`·`와이거뭐냐`·`요새난리`·`이거였네`
- **썰+짤 계열** — `썰칩12`(2.25만)·`썰쇼템`(1.37만)·`짤이야`

**keeper(PID 273568)는 계속 돈다** — 이건 헛돌지 않는다. 새 수집이 들어오면 등록하고,
전체를 재판정하고, `shot_type` 태깅이 채워지는 순간 직촬 채널을 걸러낸다.
사장님이 손으로 지운 채널은 영구 차단(표식 없는 행은 복구 대상에서 제외).


---

## ✅ 2026-08-19 13:35 — 발굴 3차: 새 어휘축 넣고 루프 **재개함** (회사 세션)

앞 세션이 세운 이유("어휘 공간을 다 훑어 헛돈다")는 맞았다. 그래서 **재개 전에 축부터 넓혔다.**

### 어떻게 넓혔나 — 추측이 아니라 실측으로 구멍을 셌다

핸드오프 ⏭4가 지목한 후보 채널 8곳의 **실제 업로드 제목**을 YouTube API로 읽고
(이왜템 1,070 · 나만또모르고있었지 1,410 · 와이거뭐냐 1,180 · 요새난리 928 ·
이거였네 1,740 · 썰칩12 22,500 · 썰쇼템 13,700 · 짤이야 6,740),
그 54편에 현재 판정기를 돌려 **놓친 28편의 결손 조각을 하나씩 셌다.**

```
확장 전  26/54 통과 (48%)
확장 후  42/54 통과 (78%)      대조군(뉴스·게임·설교·다큐) 오탐 0건
```

### ★가장 큰 구멍은 새 단어가 아니라 **띄어쓰기 변형**이었다

`예상 못 한`(공백 2개). 목록엔 `예상 못한`·`예상못한`만 있었다.
썰쇼템(1.37만)은 이 표기를 주력으로 쓰는데 **한 편도 안 걸리고 있었다.**
→ 단어 목록만 보면 "있다"고 착각한다. **실제 제목을 돌려봐야 보인다.**

넓힌 것(`shopping_shorts/categorize.py` — 어휘의 유일한 출처):
- 권위자: 기획자·제작자·시공업체·직원·담당자·만든 사람·살림/요리 고수·고인물·전 세계
- 부정어: 예상 못 한·상상 못 한·예상치 못한·몰랐을·몰래쓰는·당황해버린·놀라버린·인정한·소름 돋는
- 귀결어: 반전·발상·실수·활용도·활용 방법
- 차단어: 게임·피파·프로게이머 등(오탐 1건 제거). ⚠️**`롤`은 넣지 마라** —
  "롤 비닐을 박살내버린 분리수거 필수템"(요새난리) 같은 진짜 쇼핑 제목이 통째로 죽는다.

회귀 테스트 `shopping_shorts/tests/test_misuse_axis_2026_08_19.py` —
**어휘 목록이 아니라 실측 제목 13편**을 박았다(목록만 박으면 띄어쓰기 구멍이 또 난다).

### 재개 결과 — 첫 사이클에서 바로 늘었다

```
[13:32] C108 [썰쇼핑] +5 (누적 107) 검색어 ['직원도 감탄한 활용법', '기획자도 놀란 활용법']
```
직전 세션은 **73사이클 연속 신규 0**이었다. 새 축에서 나온 검색어가 즉시 새 채널을 물어왔다.
보유: 썰쇼핑 107 · 연예인결합 220 · 레시피쇼핑 160.

### ★스크립트를 리포로 들여왔다

`harvest_forever.py`·`register_styles.py`가 서버 `/tmp`에만 있었다 — **재부팅 한 번이면
발굴 루프가 통째로 사라진다.** 이제 정본은 리포다:
```
scripts/harvest_styles_forever.py       (실행: cd 리포 && set -a && . /etc/shopping-shorts.env && set +a && setsid python3 -u scripts/harvest_styles_forever.py >> /tmp/harvest_forever.out 2>&1 &)
scripts/register_discovered_styles.py
```
⚠️ 상태파일 `/tmp/style_state.json`은 **아직 /tmp에 있다** — 이것도 유실 위험이 남는다.

### ⏭ 남은 것

1. **시드 등록**(앞 세션 ⏭1 그대로) — `python3 scripts/register_discovered_styles.py`.
   `kind`는 반드시 `'account'`.
2. **상태파일을 /tmp 밖으로** — 재부팅이면 482채널·337키워드가 날아간다.
3. 랭킹 UI 카테고리 필터에 오용형·제품정체형 노출(백엔드만 있음).

---

## ✅ 2026-08-20 — 히트작 200편 골격 실측 (자료 보관 완료)

사장님 지시: **"3편은 이 자료를 꺼낼 수 있게 잘 저장만 해둬라"** — 대본 작업은 안 함.

### 자료 위치 (영상 소재 + 스파인 설계 근거)

```
raw/analysis/썰쇼핑_히트작200_2026-08-20/     ← README.md 부터 읽어라
```
보고 문서(사장님이 보신 것): https://claude.ai/code/artifact/2ac9ef2d-e20e-4b89-a6f4-050c37ec0aea

### 무엇을 쟀나

채널 107곳 → yt-dlp 2,428편 → **조회수 상위 200편 자막 전량**(100%) → 골격 판정
→ 상위 31편은 Gemini로 슬롯·필요장면까지 분해. **YouTube API 쿼터 0원.**

### ★결론 — 내 예상이 틀렸고 실측이 바로잡았다

| | 편수 | 평균 조회수 |
|---|---|---|
| **오용형 계열** | **92편(46%)** | **151.8만** |
| 은폐형(이븐쇼핑류) | 15편(7%) | 27.0만 |

사장님이 예시로 주신 이븐쇼핑(은폐형)은 **소수파**다. 상위 1,200만·1,000만·910만이
전부 오용형. → **스파인은 오용형부터 만들어야 한다.**

- 필요 장면(용도) **평균 3.3개 / 중앙값 3 / 범위 2~5** → 해외원본 3~5편이 맞다는 게 실측됨
- **훅은 장면 자유 58%** (사장님이 두 영상 보고 짚으신 것이 200편에서 확인됨)
- 용도 칸은 대사↔화면이 정확히 묶인다(장면 필수)

### ⏭ 다음 (우선순위)

1. **Gemini 키 풀리면 200편 전체 분해** — 31편에서 429로 끊겼다. '기타 39%'를 갈라낸다
2. **오용형 스파인 등록** (92편·152만 = 근거 가장 두꺼움)
3. **`shot_role` 축 확장** — 6개뿐이라 라이브 7,574장면 중 **45%가 '사용중'에 뭉침**.
   껍질까기와 가지치기가 구분 안 돼 "장점1 자리엔 이 장면" 지시가 불가능하다
4. **`sources[:3]` 상한 해제** (app.py:11287·11332·11396) — 평균 3.3장면이 필요한데 3편에서 자른다
5. 담기 화면에 "필요한 장면 / 있는 것 / 없는 것" 표시 (사장님 아이디어 — 무분별 수집 방지)

### 함정

- YouTube API는 `harvest_styles_forever.py`(PID 412546, 08-19 13:32~)가 쿼터를 다 써서 403 → **yt-dlp로 우회**
- Gemini 키 하루 한도 → 31/60편에서 429
- 윈도 콘솔 cp949 → 결과는 반드시 파일로 출력
- `script_extracts` 581건은 **대부분 인스타** — 유튜브 은폐형 표본이 아니다

### 2026-08-20 커밋 기록 (전부 origin/main 반영 확인)

| 커밋 | 내용 | 확인 |
|---|---|---|
| `12ef7b100` | 히트작 200편 골격 실측 자료 보관 (raw/analysis/) | origin/main ✅ |
| `495f1ff95` | 렌즈 화면 수리 — S2 초기화 전 저장이 스크립트를 죽였다 | origin/main ✅ |

서버 반영 실측: HEAD `342dae617` · 수정 반영=1 · 서비스 active · S2 에러 0건(4분간)

⚠️ **`finish`는 exit 0이어도 병합 안 됐을 수 있다**(이번에도 1차 시도가 조용히 실패했다).
판정은 반드시 `git branch -r --contains <커밋>`으로 하라(메모리: reference_finish_exit0_미병합).

### 라이브 버그 수리 — 렌즈가 결과를 못 받던 것 (`495f1ff95`)

사장님 제보: 렌즈 화면에 `Unexpected token '<'`, 플랫폼 카운트 전부 0.
**렌즈 쿼터 소진으로 보였으나 실잔량 750회로 멀쩡했다** (키 5개, `account_searches_left()`).

진짜 원인 — 화면 스크립트 TDZ 사망:
```
[화면에러] Cannot access 'S2' before initialization   produce:9497
```
`_s2Snapshot()`은 **function 선언이라 끌어올려져** `typeof _s2Snapshot === 'function'`
가드(app 3848줄)를 통과하는데, 그 안에서 읽는 `S2`는 **const라 안 끌어올려진다**(9488줄).
로딩 중 선언줄에 닿기 전 `saveWork()`가 한 번이라도 돌면 여기서 죽고, **그 아래 스크립트가
통째로 멈춰** 렌즈 응답을 화면이 처리 못 한다. 카운트 0·`Unexpected token '<'`가 전부 이 결과.

★**`typeof`로는 못 막는다** — 처음에 그렇게 고쳤다가 node로 돌려보고 틀린 걸 잡았다.
  TDZ 변수는 `typeof`도 ReferenceError를 던진다(실측). → `try { S2; } catch(e){ return null; }`
  선언 전 호출=null / 선언 후=정상 동작을 node로 양쪽 확인했다.

⚠️ 문법은 유효해 `node --check`가 통과시킨다(런타임 오류).
   같은 계열: 메모리 `reference_render죽으면_전화면공백`.

---

## ✅ 2026-08-20 (이어서) — 재료 상한이 **두 벌로 어긋나 있던 것** 수리 (다음할일 #4 완료)

### 처음 본 것과 실제 원인이 달랐다

다음할일 #4는 "`sources[:3]` 상한 해제"였다. 열어보니 값을 올리는 문제가 아니었다 —
**같은 판단이 두 벌로 적혀 이미 어긋나 있었다**(0순위-B가 말하는 바로 그 모양).

| 어디 | 값 | 결과 |
|---|---|---|
| `app.py _FACTS_MAX_SOURCES` | **5** | 재료(슬롯)는 5편까지 뽑았다 |
| `script_generate _mix_source_block(sources[:3])` ×3 | **3** | 프롬프트 그릇이 3편에서 잘랐다 |
| `app.py _sources_for_generate(limit=3)` | **3** | 애초에 3편만 모아 넘겼다 |
| `app.py` 은행 예산 `_sc = ...[:3]` ×2 | **3** | 재료 분량을 3편치로 잘못 재 은행 예산이 헐거웠다 |

→ **5편 뽑아 3편만 넣고 있었다.** 어느 한 곳도 "버그"처럼 안 보이는 게 이 함정의 특징이다.

### 고친 방법 — 정하는 곳을 하나로

- `script_generate.SOURCE_MAX = 5` **한 곳에서만** 정한다
- `app.py._FACTS_MAX_SOURCES = script_generate.SOURCE_MAX` — 별칭일 뿐, 숫자를 다시 안 적는다
- `_sources_for_generate(limit=_FACTS_MAX_SOURCES)` / 은행 예산 `_sc` 2곳도 같은 상수

**5의 근거 두 가지가 같은 값을 가리킨다** (그래서 5로 확정):
- 사장님 지시(2026-08-19) "영상은 최대 5개까지만. 더 넣어봐야 의미없다"
- 히트작 200편 실측 — 필요 장면 평균 3.3 / 중앙값 3 / **범위 2~5**

⚠️ 더 올리지 마라: 재료 1편당 프롬프트 약 2.8천자(본문 800 + 장면 20줄)라 5편이 이미 ~14천자다.

### 실측 검증

```
SOURCE_MAX = 5 / _FACTS_MAX_SOURCES = 5
재료 6편 넣은 가짜 job → _sources_for_generate 반환 5편   (고치기 전 3편)
```
`ast.parse` 양쪽 통과 · `import shopping_shorts.app` 정상.

### 함정 (다음 세션이 밟지 말 것)

- **트랙 폴더가 최신 main에 안 맞춰져 있었다.** 옛 `app.py` 위에 고쳐 놓고 push하니
  non-fast-forward. 머지하니 main이 그 사이 같은 영역을 리팩토링(`_facts_per_source`·
  `_insta_slots` 신설)해 충돌. → **손대기 전에 `git fetch && git log HEAD..origin/main`부터 봐라.**
- 충돌은 **main을 정본으로 통째 채택**하고 내 변경만 다시 얹었다(`git checkout origin/main -- <파일>`).
  충돌 마커를 손으로 지우면 남의 리팩토링을 되돌린다.

### ⏭ 남은 것

1. Gemini 키 풀리면 200편 전체 분해 (31편에서 429로 끊김)
2. 오용형 스파인 등록 (92편·152만 — 근거 가장 두꺼움)
3. `shot_role` 축 확장 — 6개뿐이라 7,574장면 중 45%가 '사용중'에 뭉침
4. ~~`sources[:3]` 상한 해제~~ ✅ 완료
5. 담기 화면에 "필요한 장면 / 있는 것 / 없는 것" 표시
6. ~~해외 원본 담기 테스트~~ → **`watch_local.py`는 존재한 적 없다.** 2026-08-21 항목 참조

---

## ✅ 2026-08-20 (이어서2) — 오용형 스파인 점검 → **반전이 죽던 버그** 발견·수리

### 먼저: "오용형 스파인 등록"은 **이미 돼 있었다**

다음할일 #2였지만, 라이브를 직접 조회하니 `spine id=56 유튜브 오용형`이 이미
approved·source_count 4·beat_roles/templates 완비 상태였다. (DB는 `reference.db`다 —
`app.db`의 spine 테이블은 0건이니 헷갈리지 마라.)

그래서 실제로 한 일은 **200편 실측과 라이브 스파인을 대조**하는 것이었고, 거기서 버그가 나왔다.

### 실측 대조 결과 (raw/analysis/썰쇼핑_히트작200_2026-08-20, 오용형 정밀분해 20편)

| 항목 | 실측 | 라이브 스파인 |
|---|---|---|
| 사용처 개수 | 평균 3.35 (2개 5편·3개 6편·4개 6편·5개 3편) | 최대 4개 수용 |
| 역할 분포 | 나열 26 · **고조 22** · 클라이맥스 19 | cases의 '심지어'가 고조를 담당 |
| 등장 순서 | 거의 `나열 → 고조 → 클라이맥스` | 동일 |
| 클라이맥스가 마지막 | **14/20편** | twist가 마지막 |

★**고조는 별도 슬롯이 아니어도 된다** — 22건 중 21건이 "또 다른 용도"였다(FDA 안전성
1건만 신뢰 근거). 지금 틀이 `cases`의 "심지어 {용도3}까지"로 이미 담고 있다.
처음엔 "고조 슬롯이 빠졌다"고 봤는데 실측을 세어 보고 틀린 걸 잡았다.

### ★진짜 버그 — 나열과 반전이 같은 사용처를 두 번 말했다

`용도끝 = _last(misuses)`인데 `cases`도 앞에서 3개까지 쓴다. 사용처가 2~3개면 겹친다:

```
misuses 3개 → cases "…고수들은 슬라이딩 신발장까지 심지어 2층 수납함까지 한다는 거"
              twist "근데 미친 사용법은 따로 있었는데 2층 수납함"      ← 같은 말
```

"twist는 cases가 이미 말한 것을 또 말하면 안 된다"는 규칙은 **주석에 적혀 있었는데**
뽑는 방식이 그걸 못 지켰다. 적어두는 것만으로는 안 지켜진다는 사례가 하나 더 늘었다.
실측 분포로 **20편 중 11편(55%)**이 이 구간이었다.

**수리**: `spine_fill._cases_and_last()` — 반전 몫(마지막 1개)을 먼저 떼고 나머지만 나열에 준다.
사용처가 1개뿐이면 반전 몫을 비워 twist를 `missing`으로 넘긴다(겹친 문장보다 낫다).

### 부작용을 같이 처리했다 (이게 더 중요하다)

cases가 쓰는 사례가 하나씩 줄어 **사용처 3개 구간에서 '심지어'가 사라진다.**
`script_gate`의 「고조 심화(1회)」는 대본 전체에서 고조어 **정확히 1회**를 요구하므로
그대로 두면 실측 30% 구간이 통째로 FAIL이었다. 기존 테스트가 이걸 잡아줬다.

→ `tools/spine_add_escalator_variant.py` (멱등): cases에 **2슬롯 고조 변형**을
3슬롯 변형 바로 뒤에 끼운다. `pick_template`은 "슬롯이 다 차는 첫 번째"를 고르니 순서가 우선순위다.

```
사용처 4~5개 → 3슬롯 변형(심지어 1회)      종전 그대로
사용처 3개   → 2슬롯 고조 변형(심지어 1회)  ★이번에 살린 구간
사용처 2개   → 1슬롯 변형(고조어 0회)       종전 그대로
```

**실측 확인**: 3·4·5개 전부 심지어 1회 / 겹침 0 / 두 번 돌려도 멱등. 관련 테스트 456건 통과.

### ⚠️ 함정 (다음 세션이 반드시 볼 것)

- **`tools/seed_style_youtube.py`를 그냥 재실행하지 마라 — 라이브가 퇴화한다.**
  그 시드는 낡았다: 라이브 twist는 `{용도끝}`인데 시드는 `{용도}`이고, `cases`·`bait`·`title`은
  시드에 **아예 없다**. `set_spine_style(templates=...)`가 통째로 덮어쓴다.
  그래서 이번엔 시드를 안 건드리고 별도 멱등 마이그레이션을 만들었다.
- **spine 테이블은 `reference.db`에 있다**(`app.db`는 0건).
- 사용처 5개면 4번째가 버려진다(cases 3 + twist 1). 실측 15%라 이번엔 안 건드렸다.

### ⏭ 남은 것

0. ~~서버 마이그레이션 실행~~ ✅ **완료** (2026-08-20, `reference.db` 백업 후 실행 → cases 4개→5개)
   라이브 스파인으로 최종 실측한 결과:

   | 사용처 | 고조어 | 반전 겹침 | cases 문장 |
   |---|---|---|---|
   | 2개 | 0회 | 없음 | "빨래 바구니로 쓰더라고요" |
   | 3개 | **1회** | 없음 | "…고수들은 **심지어** 슬라이딩 신발장까지" |
   | 4개 | 1회 | 없음 | "…슬라이딩 신발장까지 **심지어** 2층 수납함까지" |
   | 5개 | 1회 | 없음 | 위와 동일(4번째는 미사용) |

   ⚠️ 사용처 2개 구간은 고조어 0회라 게이트에 걸린다 — **종전부터 그랬고 이번 변경과 무관**하다.
   재료가 얇은 문제라 별건으로 봐야 한다(실측 20편 중 5편 = 25%).
1. Gemini 키 풀리면 200편 전체 분해 (31편에서 429)
2. `shot_role` 축 확장 — 6개뿐이라 7,574장면 중 45%가 '사용중'에 뭉침
3. 담기 화면에 "필요한 장면 / 있는 것 / 없는 것" 표시
4. **해외 원본 담기 테스트 — 사장님이 담아주시면 바로 돌린다**

---

## ✅ 2026-08-20 (이어서3) — 200편 전량 분해 + shot_role 축 확장

### 1. 200편 전체 분해 완료 (다음할일 #1)

1차에 31편에서 429로 끊긴 것을 **이어받기 판**으로 채웠다 → 198편 성공(실패 2).
`raw/analysis/썰쇼핑_히트작200_2026-08-20/report_gemini200.txt`

★**429는 일일 소진이 아니라 분당 한도였다.** 3연속 429로 멈춘 직후 같은 키로 단발 호출을
하니 정상 응답했다(실측). 1차 스크립트가 성급히 포기한 것 + `err`를 **누적**으로 세는 버그
(연속이 아니었다)가 겹친 결과다. 이어받기 판은 20/40/80초 물러섰다 재시도하고, 10편마다
중간 저장하며, 이미 한 것은 `video_id`로 건너뛴다.

★**규칙판정과 그림이 크게 다르다** — '기타 39%'가 갈라졌다:

| 골격 | Gemini 198편 | 규칙판정 200편 |
|---|---|---|
| 오용형 | **112편(56.6%)** | 92편(46%) |
| 권위자형 | **47편(23.7%)** | 4편(2%) |
| 은폐형 | 31편(15.7%) | 15편(7%) |
| 기타 | 4편(2.0%) | 77편(39%) |

- **권위자형 4편 → 47편**. 문구 규칙이 이 갈래를 거의 못 잡았다 → **전용 스파인이 없다**(새 1순위)
- 오용형 용도 개수 평균 **3.34**(31편 표본 3.35와 사실상 동일 = 표본 6배로 늘려도 안 흔들림)
- **클라이맥스가 마지막인 편 86/112(77%)** → 앞서 한 반전 몫 선분리 수정이 전량으로 재확인됐다
- ⚠️ 평균 조회수는 표본이 넓어져 내려갔다(오용형 151.8만 → 88.7만). **갈래 비교용으로만 써라.**

### 2. `shot_role` 축 확장 (다음할일 #2) — 사장님 확정: 행위 축

라이브 7,813장면 중 **사용중 3,541(45.3%)**이 한 통에 뭉쳐 "장점1 자리엔 이 장면"을
지시할 수 없었다. 그 3,541건의 장면설명 서술어를 세어 나온 갈래로 쪼갰다:

```
설치  고정30 부착19 설치17 장착16 결합16 조립12
정리  정리33 청소32 정돈11 수납10 제거10
조작  조작20 회전16 조절16 터치11
도포  분사20 도포17
실증  확인35
```

**어휘가 네 군데에 흩어져 있었다**(frame_script enum+프롬프트 / script_extract VOCAB /
edit_plan 슬롯 / backbone 판정표) → 새 모듈 `shopping_shorts/shot_roles.py` **한 곳**이 정한다.

★**슬롯 정의는 손대지 않았다.** `roles=["사용중"]`로 적힌 자리가 아홉 군데인데, 축을 늘릴
때마다 아홉 곳에 값을 더하면 언젠가 하나를 빠뜨린다(그 슬롯만 조용히 안 채워진다).
넓히는 판단을 `shot_roles.matches()` 하나에 뒀다.

★**`사용중`을 지우지 않았다** — 라이브 3,541건이 그 값이고 슬롯이 그걸 건다. 다섯에 안 드는
일반 사용 장면을 받는 자리로 남긴다(fail-open). 옛 값 `조리` 220건도 정규화로 흡수된다.

테스트 4,946건 통과. (실패 1건 `test_product_prefetch::TestRelayQueueAsync`는 **전체 부하에서만**
깨지는 flaky — 단독 실행 20/20 통과, 이번 변경과 무관.)

### ⏭ 남은 것

1. **★재태깅 실행** — `tools/retag_shot_roles.py` (배포 후 서버에서)
   ```
   python3 tools/retag_shot_roles.py --dry --limit 40    # 눈으로 먼저
   python3 tools/retag_shot_roles.py                      # 전량 4,951건 / 호출 약 124회
   ```
   dry 실측(40건): 조작17·사용중13·정리4·실증2·설치2·도포2 = **67%가 갈라졌다**.
   품질도 맞다("버튼 누르자 불이 들어옴"→실증, "바닥에 놓여 있음"→사용중으로 억지로 안 끼움).
   ★영상을 다시 안 본다 — `scene_desc`·`action` 텍스트만 쓴다(원본이 사라진 옛 추출본도 된다).
   ★옛 값은 `shot_role_prev`에 남긴다 → 되돌릴 수 있다.
2. **권위자형 스파인 등록** ← 실측이 새로 만든 1순위(47편·23.7%인데 전용 스파인 없음)
3. 담기 화면에 "필요한 장면 / 있는 것 / 없는 것" 표시
4. **해외 원본 담기 테스트 — 사장님이 담아주시면 바로 돌린다**

### ⚠️ 함정 (이번에 내가 밟은 것)

- **서버에 파일을 직접 cp하지 마라.** dry-run을 급히 하려고 `shot_roles.py`·`retag_shot_roles.py`를
  서버에 복사했는데, 이건 CLAUDE.md가 금지한 핫패치다 — 워킹트리가 더러워지면
  `auto_deploy.sh`가 pull 실패로 **조용히 스킵**돼 모든 배포가 멈춘다(2026-07-14 실사고 재현).
  즉시 `rm`으로 원복하고 워킹트리가 깨끗한 것을 확인했다. **정식 경로는 커밋 → finish → 자동배포다.**

---

## ✅ 2026-08-20 (이어서4) — 발명품형 스파인 신설 (라이브 spine id=60)

### 실측이 또 가설을 뒤집었다

"권위자형 47편(23.7%)이 2위인데 전용 스파인이 없다" → 만들려고 자막 원문을 뜯었더니
**47편이 단일 골격이 아니었다**:

| 갈래 | 편수 | 처리 |
|---|---|---|
| 문장이 오용형과 사실상 같음 | 8편 | 기존 spine 56이 이미 커버 — 안 건드림 |
| **발명품형(진짜 다른 갈래)** | **23편** | ★이번에 등록 |
| 판정 보류(어느 어구도 뚜렷하지 않음) | 16편 | 안 건드림 |

★**은폐형 31편 중 23편도 같은 계열 어구를 쓴다.** 차이는 "정체를 숨기느냐" 하나뿐이다:

```
은폐형(55)   bait(정체 숨김) → authority → reveal(정체 공개) → benefit → twist
발명품형(60) title → story(탄생 배경) → authority → benefit → escalate → twist
```
reveal 자리에 **탄생 배경**이 오고, 은폐형은 고조가 twist 문장 안에 붙어 있는데
발명품형은 **고조와 반전이 따로 선다**(실측 문장이 그렇다).

### 탄생 배경이 이 갈래의 본체다 → `{계기}` 슬롯 신설

```
"혼자서 신발을 신고 싶다는 뇌성마비 소년의 간절한 편지 한 통에 탄생한 이 신발이…"  (260만)
"비만 오면 짐 챙기다 생지옥 되던 육아맘에 빡쳐서 탄생한 이 물건이…"              (590만)
```
`sul_facts.origin_story`가 영상에서 뽑는다. **없으면 비운다** — 계기를 안 쓰는 story 변형이
대신 걸린다. ⚠️지어낸 미담이 대본에 박히는 게 이 갈래 최대 사고다(확인할 길도 없다).

### ★가장 중요한 함정 — 자격 검사를 나눴다

`sul_material_problem`은 **"원래 용도를 뒤집는가"**(`misuse_genre`)를 묻는다.
발명품형은 뒤집는 이야기가 아니라 `misuse_genre=false`다 → 같은 트랙에 두면
**영영 조립이 안 된다.** 스파인은 있는데 죽어 있고, **오류가 안 나서 눈에 안 띈다**
(핸드오프에 반복해 적힌 "라이브에서만 죽었다" 패턴).

→ `INVENTION_CATEGORIES` + `_is_invention_context` + `invention_material_problem`으로 갈래를 분리.
   발명품형 자격 = **장점 3개**(benefit·escalate·twist에 하나씩) + **제품명**(제목이 {제품}을 쓴다).

### 라이브 실측 (추측 아님)

```
spine id=60 / roles ['title','story','authority','benefit','escalate','twist']
fit ['발명품형'] · src 23 · conceal False · no_cta True
list_style_spines 노출 True · 갈래판정 invention=True, sul=False · 재료자격 ''
조립 결과 게이트 14/14 통과 (252자)
```
계기 없는 소재도 14/14(251자). 장점 2개뿐이면 twist가 비어 조립 대상에서 빠진다(의도된 동작).
테스트 576건 통과.

⚠️ **게이트가 못 잡은 어색함이 2건 있었다** — "편지 한 통에서 **빡쳐** 탄생한",
   "벗겨진다**까지 되니까**"(문장형 효능 뒤 조사 비문). 문장틀·글자수·고조어를 다 통과했다.
   조립본은 **읽어봐야** 한다. 게이트 통과는 품질 보증이 아니다.

### ⏭ 남은 것

1. 담기 화면에 "필요한 장면 / 있는 것 / 없는 것" 표시
2. **해외 원본 담기 테스트 — 사장님이 담아주시면 바로 돌린다**
3. (선택) 권위자형 판정 보류 16편 재분류 — 새 갈래가 더 나올 수 있다
4. (선택) `{계기}` 실전 수율 관측 — origin_story가 실제로 몇 %나 뽑히는지

---

## ✅ 2026-08-20 (이어서5) — 스타일 목록에 "지금 재료로 몇 칸" 표시

사장님 아이디어(무분별 수집 방지). 지금까지는 대본을 **다 돌려본 뒤에야** "재료가
모자랍니다"를 알았다 — 그만큼 기다린 뒤에 안다는 뜻이라 일단 많이 담고 보게 됐다.

### ★"담기 화면"이 아니라 **스타일 고르는 자리**에 붙였다

담는 시점엔 붙일 수가 없다: 재료 캐시 키가 `full_text`의 md5인데 담기 직후엔 1단계
분석 전이라 `full_text`가 없다. 실제로 값을 보여줄 수 있는 가장 이른 지점이
제작소의 스타일 드롭다운이라 거기에 달았다.

```
✅ 다이소 내부인형 · 근거 1편 · 재료 7/8칸 — 없는 것: price
✅ 가족갈등 반전형 · 근거 4편 · 재료 10/10칸 ✔
⚠️ 유튜브 오용형 · 근거 4편 · 아직 분석 전입니다
```

### 배선

- `/api/script/styles?job=<id>` → 각 스타일에 `cover{done,total,missing,ready,why}`
- **`job` 없이 부르면 종전과 동일**(회귀 0). 없는 job이어도 목록은 안 막힌다(실측 200)
- 화면은 `MIX_JOB`이 있을 때만 job을 붙인다

### ⚠️ 두 가지를 코드가 강제하게 했다

1. **모델을 부르지 않는다** — `_facts_per_source(cache_only=True)`. 여기서 추출을 돌리면
   드롭다운 한 번 여는 데 담긴 영상 수만큼 Gemini를 때린다. 고르기 **전에** 보는 값이다.
   → 테스트가 추출기를 부르면 실패하도록 막아뒀다(`test_분석_전이면_모델을_안_부르고…`).
2. **칸 판정은 한 벌만** — `_slots_for_spine` + `spine_fill.coverage`. 화면과 조립이
   다르게 판단하면 "된다더니 눌러보면 안 되는" 상태가 된다(0순위-B).

### 라이브 실측에서 잡은 성능 문제

처음 배포본은 **스파인마다 슬롯을 다시 계산**했다(insta 재료 조립 로그가 7번 찍혔다).
슬롯은 스파인이 아니라 **갈래**가 정하는데 목록 API는 스파인 수만큼 부른다.
`_assembled_drafts`의 `slots_by_track`과 같은 방식으로 갈래별 캐시를 뒀다.

```
수정 후 라이브: 소요 0.01초 / 갈래캐시 3개 / 반복 로그 7번 → 1번
```
회귀 테스트가 "같은 갈래는 1번만"을 강제한다. 테스트 549건 통과.

### ⏭ 남은 것

1. ~~해외 원본 담기 테스트 (`watch_local.py` 준비됨)~~ → **그 파일은 존재한 적 없다.** 2026-08-21 항목 참조
2. (선택) 권위자형 판정 보류 16편 재분류 — 새 갈래가 더 나올 수 있다
3. (선택) `{계기}` 실전 수율 관측 — `origin_story`가 실제로 몇 %나 뽑히는지
4. (선택) 재태깅된 행위 축(설치·조작·도포·정리·실증)을 `edit_plan` 슬롯이 **구분해서**
   쓰게 하기 — 지금은 `matches()`가 계열로 뭉뚱그려 받는다. 축은 갈렸지만 "장점1 자리엔
   설치 장면" 같은 **구분 지시**는 아직 안 쓴다(다음 단계).

---

## ✅ 2026-08-20 (이어서6) — 행위 축을 슬롯이 **구분해서** 쓰게

재태깅으로 축은 갈렸는데(설치·조작·도포·정리·실증) 배치는 여전히 뭉뚱그려 집고 있었다.
"장점1 자리엔 이 장면"을 지시하려면 슬롯이 갈래를 골라야 한다.

### 좁히면 안 되고 넓히면 의미가 없다 → **우선순위**로 풀었다

`_pick`은 역할이 안 맞으면 그 슬롯을 **비운다**(억지로 안 넣는다). 그래서 roles를
좁히기만 하면 그 갈래가 없는 소재에서 슬롯이 통째로 빈다. 대신 순서를 줬다:

```
뷰티 사용    ["도포", "조작", "사용중"]      바르는 손이 본체
청소 사용    ["정리", "도포", "사용중"]      닦고 치우는 손
기능실증     ["실증", "조작", "설치", "사용중"]
요리 과정    ["조작", "도포", "사용중"]
요리 재료    ["정리", "조작", "사용중", "기타"]
범용 핵심실증 ["실증", "사용중"]
```
**마지막 `사용중`이 계열 전체로 넓혀져 폴백이 된다 = 회귀 0.**
⚠️ 마지막 `사용중`을 빼지 마라 — 빼면 그 슬롯이 비는 소재가 생긴다.

### ★실측으로 잡은 버그 — 축을 쪼개고도 구분이 불가능했다

`shot_roles.expand`를 "사용 계열 값이면 전부 넓힌다"로 짰었다. 그러면 `도포`를 요구해도
조작·정리·설치가 **다 걸린다**:

```
expand(['도포']) = ['도포','사용중','설치','실증','정리','조작']   ← 전부
실측: 뷰티 '사용' 슬롯이 크림 바르는 컷을 두고 "뚜껑을 돌려 연다"(조작)를 집었다.
      도포가 있든 없든 결과가 같았다 = 구분이 0이었다.
```
→ 넓히는 건 **`사용중`을 요구할 때뿐**이다(`사용중`은 갈래를 안 가리는 폴백 이름).
이 버그는 모든 슬롯이 `사용중` 하나였을 땐 증상이 안 났다 — 축을 실제로 쓰기 시작하니 드러났다.

### is_key를 우선순위보다 먼저 본다

종전 동작이 그랬다(roles 전체에서 keyed를 찾았다). 여기서 순서를 앞세우면 1순위 갈래에
실증컷이 없을 때 실증컷을 놓친다 — 그건 회귀다. 테스트가 이걸 지킨다.

### 라이브 실측 — 표시만 바뀐 게 아니다

```
라이브 script_extracts로 옛 방식 vs 새 방식 배치 비교
→ 1,758건 중 911건(51.8%)에서 실제 배치가 달라졌다

예) kitchen_tool 실증 자리
    옛: "아이들이 엘리베이터 버튼 장난감을 누르며 노는 모습"
    새: "장난감의 7번 버튼을 누르자 불이 들어오는 모습"   ← 효과를 보여주는 컷
```
테스트 4,968건 전부 통과(앞서 flaky였던 `test_product_prefetch`도 이번엔 통과).

### ⏭ 남은 것

1. **해외 원본 담기 테스트 — 사장님이 담아주시면 바로 돌린다**
2. (선택) 권위자형 판정 보류 16편 재분류
3. (선택) `{계기}` 실전 수율 관측 — `origin_story`가 실제로 몇 %나 뽑히는지
4. (선택) `scene_lab.html`의 요리 편향 분류(굽기/재료/마무리)를 새 행위 축으로 교체
   — 지금은 화면 분류와 배치 축이 **다른 말**을 쓴다(그 파일 주석도 요리 편향을 인정한다)

---

## ✅ 2026-08-21 — 해외 원본을 **어떻게 찾나**에 답을 냈다 (집PC 세션)

사장님 질문: **"어떻게 해외계정을 찾을껀데"** → 답은 **렌즈**다. 실측으로 증명했다.

### ★먼저 — 핸드오프가 6세션 동안 거짓말을 하고 있었다

`⏭1`에 **"`watch_local.py` 준비됨"** 이 6번 반복돼 있었다. 실제로 찾아보니:

```
트랙 tools/·scripts/   없음
git ls-files            없음
git log --all           커밋된 적 자체가 없음   ← 존재한 적 없는 파일
main 폴더               없음
```

**이 항목이 6세션 이월된 이유가 이것이다.** "준비됐다"고 적혀 있으니 아무도 다시 안 봤고,
사장님만 기다렸다. 그리고 "담아주시면"만 있고 **뭘 담아야 하는지가 정해진 적이 없었다.**
→ 위 ⏭1 문구에서 `watch_local.py` 표현을 지웠다. 없는 파일을 근거로 삼지 마라.

### 진짜 목적은 따로 있었다 (최초 맥락을 다시 읽어서 확인)

"해외 원본 담기 테스트"는 `SOURCE_MAX=5` 수리(재료 5편 뽑아 3편만 넣던 어긋남)가
실제 담기에서 먹히는지 보는 것이다. 별도 스크립트가 필요한 일이 아니었다.

정의는 이 트랙이 아니라 **`handoff/썰쇼핑대본재료.md`** 에 있었다(사장님 말씀 그대로):
> "이븐쇼핑 자막을 보고 분석하고 => 결국 **외국 무자막에서 장면을 보면서 태깅**을 하고"

즉 해외 원본 = 이븐쇼핑·활용정점이 **가져다 쓴 그 원본 시연 클립**. 말이 없고 BGM+손동작뿐.

### 실측 1 — 렌즈는 해외 원본을 실제로 물어온다 (6클릭, 952→934)

씨앗을 **두 가지로 갈라** 같은 제품에 각각 넣었다. 이게 답을 줬다.

| 제품 | 씨앗 | 결과 | 한국 | 해외 |
|---|---|---|---|---|
| 실리콘뚜껑 | 프레임 | 9 | 4 | **5** |
| 실리콘뚜껑 | 썸네일 | 24 | 21 | 3 |
| 매직랩 | 프레임 | **36** | 12 | **24** |
| 매직랩 | 썸네일 | 20 | 6 | 14 |
| 북엔드 | 프레임 | 6 | 1 | **5** |
| 북엔드 | 썸네일 | 25 | 19 | 6 |

★**한글 표지 썸네일을 그대로 넣으면 한국 카피본만 온다.** 북엔드 썸네일은 25건 중 19건이
한국이었고 **1위가 씨앗 영상 자기 자신**(`U5ee0EsBfww`)이었다. 나머지도 `salim_dosa`·
`꿀템줍줍` 같은 같은 소재 한국 릴스다.

★**프레임(자막 걷어낸 원본 화면)을 넣으면 뒤집힌다.** 썰쇼핑 영상은 위 40%가 가짜 앱UI+
한글 자막, **아래 60%가 원본**이다 → `crop=iw:ih*0.42:0:ih*0.40`으로 원본만 잘라 씨앗으로 썼다.
(실리콘뚜껑 프레임에서 전자레인지 라벨이 **태국어**로 찍힌 게 보인다 — 원본이 해외라는 증거)

⚠️ 프레임 씨앗도 **무엇이 화면을 차지하느냐**가 결과를 정한다:
- 실리콘뚜껑 9건이 전부 *전자레인지*(SK매직 리뷰·"How Do Microwave Ovens Work?") — 뚜껑이
  아니라 전자레인지를 봤다. **내 씨앗 선정 실수.**
- 매직랩 36건에 가방·모자·속옷이 섞였다 — 아이 옷에 붙이는 장면이라 **옷**으로 읽혔다.
- 북엔드 6건은 `Broom Holder Wall Mount`·`Door hanger Heavy Duty Metal`·`spoon rest` —
  건수는 적어도 **벽걸이·후크 계열로 정확히 맞았다.**

보고 화면: `out/렌즈_오용형_해외원본_실측.html` (씨앗 이미지+결과 120건 전량, 초록=해외)

### 실측 2 — 담기 → 무자막 태깅까지 **전부 됐다**

북엔드 해외분 4편을 cid0에 담고 prewarm을 태웠다.

```
grab_instagram_68a075a9277d   자막 0자   장면 4개   ← 무자막 원본
grab_instagram_8a3372c3f8e6   자막330자  장면 7개
grab_instagram_96c0756bc4df   자막 25자  장면 5개
grab_youtube_4bf770895785     자막295자  장면 9개
```

★**자막 0자짜리가 화면만 보고 4장면을 뽑았다** — 이 트랙이 목표한 동작 그대로다:
```
사용 후 거치 시연  화장실 바닥을 청소한 여성이 벽면 거치대에 대걸레를 공중 부양하여 거치
하중 테스트 시연  벽면 거치대 아래 걸이에 무거운 생수통 두 개를 걸어 내구성 테스트
다양한 도구 거치  빗자루, 밀대, 청소 솔과 샤워 볼을 차례대로 틈새와 고리에 걸어 수납
공간 활용성 확인  문 뒤편 좁은 벽면에 수납된 도구들, 문을 여닫아도 간섭 없음
```
`SOURCE_MAX=5`도 살아있음을 확인(4편이 안 잘리고 그대로 넘어갔다).

### ★실측 3 — 대본은 안 나왔다. **자격 검사가 옳게 막았다**

```
스파인 id=56 유튜브 오용형 / beat_roles ['title','bait','origin','notice','cases','twist']
못 한 이유: 이 영상은 '원래 용도를 뒤집는' 오용형이 아닙니다(제품 소개·사용법 안내)
채워진 슬롯: 0
```

**버그가 아니다.** 내가 담은 4편은 빗자루걸이·도어행거·스푼받침·수세미 —
전부 **제품을 원래 용도대로 쓰는 소개 영상**이라 뒤집을 용도가 없다.

★내 실수의 정체: `merge_sul` 주석이 **"같은 소재의 영상만 합쳐라"** 라고 이미 경고하고
있었는데(서로 다른 제품 6편 → "말이 안 되는 대본" 실측 기록), 나는 **결이 비슷하다**는
이유로 서로 다른 제품 4편을 담았다. 오용형이 요구하는 건 "결이 비슷한 제품들"이 아니라
**한 제품의 여러 용도**다. 자격 검사(`sul_material_problem`)가 그걸 잡아냈다.

### 지금 어디까지 왔나

| | 상태 |
|---|---|
| 해외 원본을 어디서 찾나 | ✅ **렌즈** — 씨앗은 반드시 **자막 걷어낸 프레임** |
| 담기 → 무자막 장면 태깅 | ✅ 자막 0자도 4장면 |
| `SOURCE_MAX=5` 수리 | ✅ 살아 있음 |
| 오용형 대본 조립 | ❌ **씨앗 선정 실패** — 같은 제품 여러 편이 필요 |

### ⏭ 다음 (회사에서 이어서 — 한 번이면 끝난다)

1. **★같은 제품 하나로 3~5편 모아 다시 돌린다.** 렌즈 **4클릭 남음**(잔량 934).
   - 씨앗은 **자막 걷어낸 프레임**으로(썸네일 금지 — 한국 카피본만 온다)
   - 제품이 **화면 대부분을 차지하는 컷**을 골라라(전자레인지·옷에 밀린 실패 2건 참고)
   - 후보: 도어행거·빗자루걸이 계열(해외에 같은 물건 많음) 또는 사장님 지정 제품
   - 담고 → prewarm → `_slots_for_spine(sp56, sources, store)` 로 검증
2. (선택) 권위자형 판정 보류 16편 재분류
3. (선택) `{계기}` 실전 수율 관측 — `origin_story`가 실제로 몇 %나 뽑히는지
4. (선택) `scene_lab.html`의 요리 편향 분류를 새 행위 축으로 교체

### 재현 도구 (전부 scratchpad에 있고 서버 `/tmp`에도 올림)

```
run_lens.py    씨앗 6장 × 렌즈 → /tmp/lens_out.json   (PYTHONPATH=/home/ubuntu/lotto-stock-wiki 필수)
do_grab.py     mix_basket_add + _enrich_grab + _enqueue_prewarm
do_script.py   get_extract → _slots_for_spine(56) → spine_fill.fill
```

### 함정 (이번에 내가 밟은 것)

- **핸드오프에 적힌 "준비됨"을 믿지 마라.** 존재한 적 없는 파일이 6세션을 잡아먹었다(0순위 규칙).
- `ssh "python3 -c"`는 `cd` 해도 **모듈을 못 찾는다** → `PYTHONPATH=/home/ubuntu/lotto-stock-wiki` 명시.
  스크립트는 `scp`로 보내 실행(메모리 `reference_오용형_판정함정`과 같은 함정).
- `Store`에 `get_script_spine`은 **없다**. 스파인 조회는 **`list_spines()`**.
- `ffmpeg -ss`를 **입력 앞**에 두면 webm에서 같은 프레임만 나온다 → 출력 seek(`-i` 뒤)로.
  MD5로 프레임이 실제로 다른지 확인할 것.

---

## ✅ 2026-08-21 — 해외 원본 담기 테스트 (사장님 담아주심) → 실사고 5건 발견·수리

사장님이 **구명 팔찌 소재 7편**을 담아주셨다(cid 0, 01:20~01:24).
축영상 = `3RxLl0NeR2Q`(이븐쇼핑 "한국 천재의 발명").
⚠️ 그중 1편(`lens_instagram_e58bzs`)은 **미니 다리미** 영상이었다(자막 실측: "이게 진짜 미니
다리미인 거 있죠"). 재료에 `170도 자동 온도 조절`·`다리미`가 섞여 들어왔다 → 빼고 6편으로 진행.

### ★1. 새 행위 축이 실제 태깅에 **하나도 안 쓰이고 있었다** (가장 큼)

`shot_roles` 모듈을 만들 때 `frame_script`만 고쳤는데 **실제 태깅 경로는 `script_extract`**다.
그래서 재태깅으로 5,015건을 갈라놓고도 **새로 분석되는 것은 전부 옛 어휘로 돌아왔다.**

실측 증거: 오늘 01:22~01:26 분석된 3편이 `before/사용중/after`만 달고 나왔고,
"레버를 작동시키는 모습"(조작)조차 '사용중'이었다. 프롬프트엔 이렇게 적혀 있었다:

```
· "사용중" = 손이 재료/도구를 다루는 과정(조리·바르기·닦기·조립)   ← 다섯 갈래가 한 줄에
```
→ enum·프롬프트를 `shot_roles` 목록에서 **생성**하게 바꿨다. 테스트 3건이 재발을 막는다.
수리 후 사장님 소재에서 `사용중→조작/설치/정리/실증`으로 정확히 갈렸다.

### ★2. 모델이 "비워라"는 지시를 **값으로 되뱉었다** (대본에 박힐 뻔)

5편 중 **4편**이 이랬다:
```
origin_country = "중국 또는 정보 없음 (영상 불명확함으로 비움이나 중국어로 추정
                  가능하나 보수적으로 비움 적용) ※주의: 지…"
```
대본의 `{나라}에서 바이럴이 터지며…` 빈칸에 저 문장이 통째로 들어간다.
**게이트는 문장틀만 보므로 못 잡는다.**
→ `sul_facts._clean_short()`가 저장 단일출구에서 막는다(지시문 냄새 + 길이 상한).
★자르지 않고 **버린다** — 12자로 자르면 "중국 또는 정보 없"이 남아 더 나쁘다.

### ★3. 1차 수리 후 `'null'`이 남았다

4자라 길이 필터를 그냥 통과했다. `_EMPTY_WORDS`(null·none·없음·미상…)로 추가 차단.

### ★4. 은폐형이 오용형 검사에 막혀 있었다

발명품형을 갈라놓을 때 **은폐형에 같은 문제가 그대로 남아** 있었다. 사장님 소재에서
"이 영상은 오용형이 아닙니다"로 통째로 차단됐다(은폐형은 뒤집는 갈래가 아니다).
→ `CONCEAL_CATEGORIES`·`conceal_material_problem` 신설, 자격 뼈대는
`_benefit_material_problem` 하나로 모았다(갈래마다 함수를 따로 두면 다음 갈래에서 또 난다).
필요 효능 칸수만 다르다: **발명품형 3 / 은폐형 2** — 라이브 templates에서 센 수다.

### ★5. 회귀 하나를 테스트가 잡아줬다 (중요)

은폐형은 `{제품}·{효능}·{나라}`를 **쿠팡 재료에서도** 받는데, 자격 검사가 영상 재료만 보고
판정해 그 경로를 막았다. → **슬롯을 먼저 만들고 그 위에서 판정**한다(슬롯은 두 재료를 합친
결과라 템플릿이 실제로 쓰는 것과 같다).
그리고 자격 미달이어도 슬롯을 버리지 않는다 — 조립은 막되 **"어느 칸이 왜 안 됐는지"**는
말해야 한다(`probs_by_track`이 조립만 막는다).

### 최종 결과 — 사장님 소재로 대본 2갈래

```
=== 유튜브 발명품형  게이트 14/14 (261자) ===
  [title]     한국 천재가 만들어 떼돈 번 손목 구명조끼의 정체
  [story]     누가 봐도 그냥 평범해 보이는 이 안전용품 하나가 조용히 퍼지기 시작했는데
  [authority] 한국에서 바이럴이 터지며 매출이 폭발했다는데 이게 진짜 물건인 이유가 있음
  [benefit]   이게 진짜 말도 안 되는 게 익수 시 즉각적으로 구조가 가능해…
  [escalate]  심지어 구명조끼보다 훨씬 가벼워 활동성이 뛰어나다
  [twist]     근데 진짜 충격적인 포인트는 따로 있는데 …압도적 부력을 제공한다

=== 유튜브 은폐형  게이트 15/15 (235자) ===
  [bait]   최근 딱 봤을 때는 도저히 용도를 알기 힘든 이 제품이
  [reveal] 이건 바로 손목 구명조끼
```
커버리지: 발명품형 **6/6칸 OK** · 오용형/은폐형은 자격으로 정확히 갈림.
테스트 5,003건 전부 통과.

### 부수 수리

- 은폐형 twist 비문: "…제공한다**까지 된다는 거**"(문장형 효능 뒤 조사) →
  `tools/spine55_fix_twist_josa.py`로 라이브 수정. 발명품형에서 이미 겪은 것과 같은 계열.

### ⚠️ 함정

- **`git merge origin/main` 충돌 시 한쪽만 채택하지 마라.** 다른 세션이 같은 줄에
  `source_text=`(수치근거 재료)를 넣었고 나는 `_blocked`(조립 차단)를 넣었다.
  한쪽만 고르면 상대 수정이 조용히 사라진다 → **둘 다 살렸다.**

### ⏭ 남은 것

1. **`origin_story`(탄생 배경)가 안 뽑힌다** — 발명품형 story가 폴백("누가 봐도 평범해
   보이는")으로 나온다. 실측 히트작은 여기가 본체다("뇌성마비 소년의 편지 한 통에 탄생한").
   6편에 개발 비화가 없어서인지, 추출이 못 잡는 건지 **원인 규명이 남았다.**
2. **재료에 다른 소재가 섞이면 걸러낼 방법이 없다** — 사장님이 다리미 1편을 담자
   `170도 자동 온도 조절`이 재료로 들어왔다. 지금은 담긴 것 전부(최대 5편)를 쓴다.
   메모리 `치아바타를 뽑았는데 도마 얘기가 나온다`와 같은 계열. **소재 이질 감지가 필요하다.**
3. (선택) 권위자형 판정 보류 16편 재분류
4. (선택) `scene_lab.html` 요리 편향 분류를 새 행위 축으로 통일

---

## ✅ 2026-08-21 (이어서) — 남은 2건 처리

### ①`origin_story` 미추출 — **버그가 아니었다. 내 설계가 틀렸다**

원인을 캐보니 축영상 자막에 **탄생 배경이 아예 없었다**:

> "**해경과 해군이 주목한** 한국 천재의 발명품 … **이걸 개발한 한국의 한 천재가 떼돈을
> 벌었다**는데 … 이건 바로 손목 구명조끼"

"천재가 만들었다"는 **수식만 있고 계기가 없다**. 내 추출 프롬프트에 정확히
"그런 경우 비워라"라고 적어뒀으니 **제대로 동작한 것**이다.

★그래서 23편 전체를 세어봤더니 **내 어제 판단이 틀렸다**:

| | 보유율 |
|---|---|
| 탄생 배경(계기) | **2/23편 = 9%** |
| 권위(누가 주목·화제) | **19/23편 = 83%** |

상위 2편(590만 우산홀더 "육아맘에 빡쳐서 탄생한" · 260만 나이키 "뇌성마비 소년의 편지")이
인상적이어서 "탄생 배경이 본체"라고 봤는데, **91%는 계기가 없다.** 진짜 본체는 권위였다.
→ `tools/spine60_fix_story.py`로 story·authority를 실측 결로 교체(라이브 반영):

```
story     : {계기} 변형 2개(9%용, 맨 앞) → "최근 이 {제품}이 전 세계 SNS에서 …바이럴이 폭발하며"
authority : "이걸 개발한 {나라}의 천재가 떼돈을 벌었다는데 그럴 만한 이유가 있음"
```
authority 꼬리("그럴 만한 이유가 있음")는 효능이 짧은 소재에서 말 밀도 하한(172자)에
못 미치던 걸 메운다(나이키 소재 167자 → 238자). 실측: 계기 없음 14/14 · 계기 있음 14/14.

**교훈: 상위 몇 편의 인상으로 골격을 정하지 마라. 표본 전체를 세라.**

### ②재료에 다른 소재가 섞이는 것 — **주석의 가정을 검사로 바꿨다**

`merge_sul` 주석에 이렇게 적혀 있었다:

> ⚠️ 같은 소재의 영상만 합쳐라. … 실무 호출부는 담긴 영상들을 넘기므로
> **이 조건이 자연히 지켜진다**

그 가정이 깨졌다. 사장님이 구명 팔찌 6편 사이에 미니 다리미 한 편을 담자
`170도 자동 온도 조절`·`다리미`가 그대로 재료가 됐다.

→ `spine_fill.split_by_subject()`: 제품명+분류어의 `bigram`(기존 공용 판정 한 벌)으로 다수결.

```
실측 6편 → 주 소재 4편(손목 구명조끼·구명 손목 밴드·구명 팔찌·손목 착용형 구명 장치)
           딴 소재 2편(미니 다리미 · 스마트워치)
```
- **버리지 않고 갈라서 돌려준다** → 화면이 "소재가 다른 영상 N편은 재료에서 뺐습니다(…)"를
  말한다(조용한 폴백 금지)
- **아무 편도 안 겹치면 판정을 포기하고 전부 쓴다** — 임의로 고르면 멀쩡한 재료를 무더기로 버린다
- 한 편뿐이면 비교 대상이 없어 그대로 통과
- 조립 경로 2곳과 커버리지 경로가 **같은 검사**를 쓴다(테스트가 강제)

**라이브 검증 — 다리미를 다시 넣고 돌렸다:**
```
제품: 손목 구명조끼 | 제품군: 안전용품 | 나라: 한국
게이트 14/14 (259자) · 대본에 '다리미'·'170도' 흔적 = False
```

### ⏭ 남은 것

1. (선택) 권위자형 판정 보류 16편 재분류 — 새 갈래가 더 나올 수 있다
2. (선택) `scene_lab.html` 요리 편향 분류(굽기/재료/마무리)를 새 행위 축으로 통일
3. (선택) `{계기}` 실전 수율 관측 — 9%가 실제로도 그런지
