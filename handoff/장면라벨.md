# 장면라벨 — 핸드오프

> 제작소 9단계 개편 + 소스 분석(1단계) + 장면 짧은이름(3단계).
> 2단계(대본)는 **다른 세션**이 맡는다 — 이 트랙은 1·3단계만.

---

## 2026-08-16 밤 (집 PC 마감) ★★회사에서 여기부터

**코드는 전부 main에 있고 라이브 반영 끝. `git pull`만 하면 바로 이어진다.**
(트랙 미커밋 0 · 미병합 0 · 서버 `ecfc22a0a` 12:17 반영)

### 오늘 밤에 한 것

| 커밋 | 내용 |
|---|---|
| `95b0b5792` | **활용 포인트(use_point)** 태깅 + 1단계 상하 재배치 + 잘림 버그 수정 |
| `951580ddb` | 1단계 구조 정리 — 빈 곳 제거(패널 1124→882px) |
| `555a81c81` | 3단계 구조 — 출처 배지·자르기 발견성·스크롤 |
| `3ac7e45c3` | 장면 편집 배치 — 맨 위 타임프레임 / 아래 [미리보기+영상조각] |
| `ecfc22a0a`(머지) | 미리보기 칸 스크롤 제거 — 영상이 남는 높이에 맞춰 준다 |

### 지금 3단계 배치 (사장님 지시대로)
```
[영상 전체 타임프레임 — 왼쪽 끝 → 오른쪽 끝, 한 줄]
[미리보기 300px] [영상 조각]      ← 각자 자기 칸 안에서만 스크롤
```
대본은 3단계에서 뺐다(2단계 담당, `#mixScriptPick` display:none).

### 1단계 소스 분석 카드
- 기본 접힘(제품 한 줄 + 장면 수) → 누르면 역할·핵심·요약 펼침
- 타임스탬프 목록은 뺐다(구간 확인은 3단계 몫)
- 담은 영상 썸네일 상단, 가로 5~6개(4:5로 잘라 높이 절약)

---

## ⚠️ ★★shortcode를 추측하지 마라 (오늘 밤 실사고)

커피 job 3개를 재태깅해 DB에 넣었는데 **화면에 안 떴다.** 원인:
URL에서 틱톡 영상 ID를 뽑아 키를 만들었는데(`lens_tiktok_7458060642738605355`)
**시스템은 짧은 해시를 쓴다**(`lens_tiktok_1jw6i6i`). 키가 달라 DB엔 있는데 화면은 못 찾았다.

**올바른 방법**: `produce_works.state_json`의 `handoff[].shortcode`를 **그대로** 읽어 쓴다.
```python
cols=[r[1] for r in c.execute('PRAGMA table_info(produce_works)')]
row=dict(zip(cols, c.execute('SELECT * FROM produce_works ORDER BY rowid DESC LIMIT 1').fetchone()))
codes=[h['shortcode'] for h in json.loads(row['state_json'])['handoff'] if h.get('useFootage')]
```
재태깅 스크립트: 서버 `/tmp/retag_by_work.py` (같은 실수 반복 방지용으로 이 방식만 쓴다)

---

## 태깅 현황 (실측)

**라이브 코드는 정상 — 다만 DB엔 대부분 아직 옛 태깅이다.**
전체 499건 중 새 필드(`source_brief`/`label`/`use_point`)가 있는 것은
**방금 재태깅한 커피 job 3건뿐**. 마지막 일반 태깅이 08-15 15:45이고 배포가 08-16 새벽이라
그 뒤로 새로 담은 영상이 없었다.

커피 job(`8873eeb48a08`) 재태깅 결과 — 사장님 요구 3가지 전부 확인:

| 소스 | 키 | 세그 | label | 자막 | 제품 |
|---|---|---|---|---|---|
| 1 인스타(한국어) | `Db_2V-mzT44` | 17 | 17 | 있음 | 휴대용 에스프레소 메이커 |
| 2 틱톡 | `lens_tiktok_1jw6i6i` | 10 | 10 | 12자 | 휴대용 직화식 에스프레소 메이커 세트 |
| 3 틱톡(무자막) | `lens_tiktok_1cfb55` | 10 | 10 | **0자** | 바텀업 휴대용 에스프레소 메이커 |

①스토리 흐름 분석 ②한국어 대본 분석 ③**외국어·무자막도 장면 태깅** — 셋 다 됨.

`use_point`는 사실 나열이 아니라 **쓸 자리**를 짚는다(사장님 "어떤 상황에 필요할지"):
> `[바텀업 추출 개시]` → "하단에서 위로 솟아오르는 추출 형태를 시각적으로 전달하는 **훅에 쓰기 좋습니다**"
> `[완성된 커피 따르기]` → "**영상 마무리 부분에** 넣기 적합합니다"

### ⏭ 회사에서 이어서 할 것

1. **1단계 태깅 → 2단계 대본이 실제로 좋아지는지 확인** ← 사장님 관심사
   ("1단계에서 잘해야 그걸 토대로 2단계 대본이 잘 뽑힌다")
   재태깅된 커피 job으로 대본을 뽑아 use_point가 실제로 반영되는지 육안 판정.
   ⚠️ 숫자 지표(컷 수·소스 종류)로는 측정이 안 된다 — 이미 A/B 5회로 확인했다(아래 참조).
2. **옛 작업 496건 재태깅 여부** — 사장님 판단 대기. 새로 담는 영상은 자동으로 채워진다.
3. ✂ 자르기 발견성은 개선했지만(버튼에 글자·색) 사장님 실사용 확인 필요.
4. 소스별 갈래(장면 편집 아래를 소스별로 나누기) — 미착수.

---

## 2026-08-16 (회사 PC) ★여기부터 이어받기

### 무엇을 했나 (전부 라이브 배포)

| 커밋 | 내용 |
|---|---|
| `53889ed62` | 장면에 짧은 이름(`label`) 태그 추가 — 12자, 카드에 찍고 매칭에 쓰려고 |
| `2a7643c7d` | label을 대본 생성 인벤토리(`_build_inventory`)에 `쓰임:`으로 실음 |
| `ee96a3f7d` | 그 칸의 사용법 설명을 프롬프트 3곳에 붙임(문구는 `_INVENTORY_LABEL_HINT` 한 벌) |
| `0793a6604` | 영상 단위 요약 `source_brief`(제품·역할·핵심·요약) 추출 |
| `443473880` | 기획서 — 8→9단계 개편(분담·충돌방지 순서·지뢰 목록) |
| `81fbad714` | **9단계 골격** — 대본이 자기 단계를 가짐 |
| `6125209c5` | **1단계 소스 분석 화면** + `/api/produce/source_brief` |
| `dfa961b57` | **3단계 장면 카드에 짧은 이름** + 분석 카드 정렬 |

### 지금 단계 구조 (라이브)

```
1 영상추출/분석  2 대본(빈 칸)  3 영상대본MIX  4 자막제거  5 TTS
6 자막꾸미기     7 썸네일       8 SEO          9 최종렌더
```
- **물리 패널 번호(`data-step`)는 안 바꿨다** — `cur===n` 분기가 파일 곳곳에 있다.
  화면 순서는 `ORB_TO_PANEL = [0, 8, 7, 1, 2, 3, 4, 5, 6]`이 정한다. 대본 = 신규 패널 **8**.
- **단계 번호를 손으로 적지 마라**: `stepName('mix')` → "3단계(영상대본MIX)",
  `stepGo('mix')` → 그 단계로 이동, 패널 제목은 `syncPanelTitles()`가 채운다.
  (개편 전엔 "1단계에서 영상 매칭 확인" 같은 문구가 16곳에 박혀 전부 거짓말이 됐다)

### ⏭ 남은 것

1. **2단계(대본) 내용** — 다른 세션 담당. `<section class="panel" data-step="8">` 안쪽만 채우면 된다.
   지금 1단계 오른쪽의 `#aiPick`(AI PICK) + 고급(스마트믹스 토글)을 그리로 옮기는 것이 원래 계획.
2. **1단계 왼쪽 재료 칸이 좁다(320px)** — 오른쪽 분석 카드가 넓어지면서 대비된다.
   사장님 정렬 기준("빈 곳 없이")으로 보면 손봐야 할 자리.
3. **✂ 자르기 발견성** — 트림바가 썸네일 클릭(`playKey`가 `seg:`)일 때만 뜬다. 사장님도 못 찾으셨다.
4. **소스별 갈래** — 장면 편집 아래가 결(완성품·굽기)로만 묶여 있어 **어느 영상에서 왔는지** 모른다.
5. **옛 작업 497건엔 `source_brief`·`label`이 없다** — 화면은 견디지만(빈 카드/묘사 폴백)
   다시 태깅해야 채워진다. 일괄 재태깅을 할지는 사장님 판단.

---

## 매칭 개선 시도와 그 결과 (다음 세션이 같은 실험을 반복하지 않도록)

**사장님 질문**: "훅 대본에 맞는 장면을 제미니가 어떻게 판별하게 프롬프트를 주나?"

**실측으로 확인한 답**: 순서가 반대다. 문장을 쓰고 장면을 찾는 게 아니라,
`_scene_first_candidates`가 **장면 인벤토리를 통째로 보여주고 "이 재료로 대본을 써라,
각 비트는 seg_ids로 화면을 지목해라"**고 시킨다. 즉 훅 문장은 장면을 본 뒤 쓰인다.
(`_build_scene_spine`은 `shot_role`+`is_key`로 기계적으로 뽑는 별도 경로 — 주경로가 아니다)

**문제**: 인벤토리의 `화면:` 묘사가 서로 비슷해 고를 축이 흐리다.
실측(커피 job) — 완성품 5개가 전부 "크레마가 얹어진 잔 / 덮인 컵 / 크레마 표면"이고
화면 라벨도 다섯 개 모두 `후킹용·CTA용`이었다. 사람도 AI도 구별을 못 한다.

**한 것**: 12자 짧은 이름(`label`)을 뽑아 인벤토리에 `쓰임:`으로 실었다.

**★그런데 A/B 실측에서 유의미한 차이는 없었다** (커피 job, 각 5회):

| | 컷 | 소스 종류 |
|---|---|---|
| B 라벨 없음 | 11.0 | 2.60 |
| A 라벨 있음 | 11.4 | 2.80 |

1회 비교에선 커 보였지만(컷 8→11) 5회 평균으로 사라졌다 — **우연이었다.**
프롬프트에 사용법 설명까지 붙이고 다시 5회 재도 마찬가지.

**해석**: 컷 수·소스 종류는 `planClips` 배분 규칙이 정하는 값이라 **라벨이 바꿀 수 있는
축이 애초에 아니다.** "효과 없음"이 아니라 **"이 지표로는 측정 불가"**다.
라벨이 바꾸는 건 "고른 장면이 그 문장에 맞느냐"인데 숫자로 안 잡힌다.
→ **판정은 사장님 육안으로.** 다음 세션이 이 A/B를 다시 돌릴 필요는 없다.

**확실한 이득 하나**: 카드 밑 31자 묘사가 12자 이름으로 바뀌어 **읽힌다**(3단계).

---

## 태깅 — 우리가 저쪽보다 못하지 않다 (실측 대조)

사장님이 경쟁 프로그램 화면을 보시고 "태깅이 굉장히 잘돼 있다"고 하셨는데, 나란히 놓으면:

| | 저쪽 | 우리(라이브 실측 `Db_2V-mzT44`) |
|---|---|---|
| 라벨 | `토큰 투입 핵심 시연` | `메탈 플레이트 중앙의 하트 문양에서…`(31자) |
| 변화 | (없음) | `하트 모양 틈새로 에스프레소가 아래에서 위로 솟아오른다` |
| 특장점 | (없음) | `독특한 바텀업 방식으로 아래에서부터 차오릅니다` |
| 실증·행위·역할 | (없음) | is_key=True · 누르다 · 사용중 |

**정보는 우리가 더 많았다**(품질 점수 `tag_qa` 1.0). **짧은 이름만 없었던 것**이고 이제 갖췄다.

### 왜 저쪽 라벨이 그렇게 나오나 — 지시문 차이다(같은 Gemini)
- 우리 옛 지시: "화면에 **무엇이 보이는지**" → 사진 캡션이 나온다
- 필요한 지시: "이 장면이 **영상에서 하는 일**" → 역할이 나온다
- 3원칙: ①역할을 물어라 ②형식 예시를 줘라(…시연/…강조/…확인) ③**영상 전체를 먼저 파악**시켜라
  ("도입"·"마무리"는 순서를 알아야 나온다 → 그래서 `source_brief`를 먼저 쓰게 했다)

### ★무자막·외국어도 된다 (사장님 지적 사항, 실측)
`745025fd.mp4` — **자막 글자수 0자**인데 화면만 보고 전부 채웠다:
```
제품: 휴대용 에스프레소 추출기
역할: 에스프레소 추출 및 서빙 과정 시연
핵심: 하트 모양 추출구를 통한 시각적인 에스프레소 추출 연출
```
같은 job의 s0(자막 249자)은 역할이 "제품 기능 시연 및 실사용 후기" — **소스별 몫이 갈린다.**

---

## 정렬 원칙 (사장님 2026-08-16 지시 — 앞으로 모든 화면에 적용)

```
□ 가로·세로 높이 딱 맞추기, 4px/8px 배수로 통일
□ 빈 곳 없이 — 컬럼 바닥선을 맞춘다
□ 튀어나오는 곳 없이 — 긴 문장 말줄임, 버튼이 카드 밖으로 안 흐르게, 가로 스크롤 0
판정: 모서리가 한 줄로 떨어지고 / 빈 여백 덩어리 없고 / 상자 밖 요소 0
```

### 이번에 밟은 함정 (CSS)
- **`grid-auto-rows:1fr`이 없으면** 카드가 내용 길이대로 제각각(실측 282/282/92/92) → 아래가 계단
- **flex 자식에 `min-height:0`이 없으면** 안 줄어들어 **카드 밖으로 146px 샌다**
- **`<details>`가 닫혀도** 안의 `ul`이 자리를 차지한다 → `:not([open]) ul{display:none}`
- 요약처럼 길이가 들쭉날쭉한 글은 **줄 수 클램프**(전문은 `title`)로 바닥선을 고정

---

## 검증 도구 (다음에도 그대로 쓰면 된다)

로컬 서버 띄우고 Playwright로:
```js
// 정렬 3기준 자동 판정
const cards=[...document.querySelectorAll('.srcCard')];
const rows={}; cards.forEach(c=>{const b=c.getBoundingClientRect();
  (rows[Math.round(b.top)]||=[]).push(Math.round(b.bottom));});
Object.values(rows).every(v=>new Set(v).size===1)   // ① 모서리 한 줄
// ② 상자 밖: 자식 rect가 부모 rect를 넘는지 (outR/outB/outL > 1)
// ③ document.documentElement.scrollWidth > innerWidth  → 가로 스크롤
```
⚠️ **`over` 값이 음수면 안쪽이다** — 부호를 안 보면 오탐 48건이 나온다(실제로 한 번 헷갈렸다).

⚠️ **로컬 DB엔 데이터가 없다**(gitignore). 화면 검증용 시드:
`scratchpad/seed_brief.py` — 서버에서 실제로 뽑은 값을 넣는다(가짜를 지어내지 않는다).

⚠️ **SHORTS Gemini 키는 로컬에 0개**다(`/etc/shopping-shorts.env`에만). 실제 추출 검증은
서버에서 `set -a && . /etc/shopping-shorts.env && set +a && python3 /tmp/...` 로.
라이브 코드는 건드리지 말고 `/tmp/labtest`에 **py 파일만** 복사해 돌려라
(`cp -r shopping_shorts`는 데이터까지 17GB를 복사한다 — 한 번 밟았다).
