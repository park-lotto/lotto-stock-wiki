# 인사이트2 트랙 로그

- 2026-08-15 — [구현] **③차트 오버레이(A·B층)**. `scripts/chart_ta.py` 신설 —
  캔들에서 도형을 숫자로 뽑는다(zigzag 스윙 → 지지·저항 군집, 추세선 회귀, 돌파 판정,
  이평·RSI·구간위치·거래대금비). `/api/insights2/keydays`에 `ta` 블록으로 붙이고
  화면에 요약 카드 + 캔버스 오버레이(점선 지지·저항, 돌파 지점 금색 원) + 토글 추가.
  ★**신뢰도 3층을 코드로 강제**: A(사실)는 그대로 쓰고, B(추세선)는 **R²를 반드시 병기**하며,
  C(역헤드앤숄더 등 이름 패턴)는 **일부러 안 만들었다** — 한 번 잘못 이름 붙이면 신뢰가 무너진다.
  ★스파이크에서 못 봤던 것을 실측으로 잡음: 현대차 저점 연결선이 **R²=0.99인데 종가와 +81.8%**
  벌어졌다. 적합도가 좋아도 현재가에서 멀면 판단에 못 쓴다(과거 스윙 기울기를 외삽하며 튄 것).
  → `usable` 판정 추가(R²≥0.5 · |gap|≤30% · 마지막 스윙 60일 이내). 못 쓰는 선은
  **그리지 않되 이유를 화면에 적는다**("⚠️현재가에서 너무 멂 — 안 그림"). 조용히 빼면 왜 없는지 모른다.
  실측: 현대차·삼성전자 4개 선 전부 usable=false → 필터가 제 역할. 대신 **지지·저항은 잘 나온다**
  (현대차 483,480에 **25회 접촉**, 차트에서 실제로 그 선에 막힌 게 눈에 보임).
  "이후 고점 대비 +0.0%"를 '눌림'이라 부르면 오해라 **'돌파 후 고점 부근'**으로 문구 분기.

- 2026-08-15 — [구현] **차트 + 「중요한 날」 마커·토글 UI**. `/api/insights2/keydays` 신설
  (캔들+발언+수급을 한 번에), `stock.html`에 캔버스 차트 직접 구현(라이브러리 없음).
  **중요한 날 점수화**: 가격 ±3%/±5% · 거래량 2배/3배 · 발언 평소 2배 · 외국인 순매수 상위10%
  · 60일 신고가/신저가 → 합산 2점 이상만 마커(180일 중 30개). 매일 찍으면 못 본다.
  레이어 토글 3개(중요한 날·발언·수급), 호버 미니카드, 클릭 시 그날 원문 패널, 카드 목록 병행
  (차트를 안 봐도 되게). **자료 없는 구간은 캔버스를 어둡게 칠하고 안내문**을 단다 —
  "조용했던 시기"로 오해하면 안 된다(발언 2026-05-27~ / 수급 2026-07-03~ / 캔들 2025-11-20~).
  ★실측 확인(브라우저): 2026-07-08 클릭 → 종가 462,500 · -3.5% · **60일 신저가인데 외국인 +745억**,
  같은 날 키움 TP 700,000(▼)·DS투자 TP 840,000(▲)로 **두 증권사가 반대로** 움직인 것이 원문으로 뜸.
  ★색 관례 수정: 캔들은 한국식(상승 빨강)으로 그려놓고 숫자만 초록/빨강이라 같은 화면에서
  모순이었다 → 가격·수급 숫자를 섹터맵(`--up1:#c0392b`/`--dn1:#1a5276`)과 같은 한국식으로.
  발언 부호 점은 '의견'이라 인사이트2와 같은 초록=강세 유지.

- 2026-08-15 — [구현] **종목검색 페이지 `/stock` 신설**(운영자 전용). 검색어 하나로 발언+수급을
  모아 5칸(일정·판단·쟁점·수급·깨지는조건)으로 정리한다. 새 파일 `pipeline/atoms/stock_search.py`
  (검색 정규화) · `scripts/stock_flow.py`(주체별 5/20일) · `dashboard/stock_brief.py`(슬롯 종합)
  · `dashboard/stock.html`. 서버에 `/stock` · `/api/insights2/stock` · `/api/insights2/brief` 추가.
  ★**검색 정규화 실측**: 현대차 503건(현대차증권 오염 58)→462건(오염 0), **현대자동차 18→462건**
  (별칭 역방향 확장으로 25배 회수), 하이닉스→SK하이닉스 정규화. 별칭 사전 5→20개(적재·검색 공용
  한 파일이라 어긋날 수 없다).
  ★**LLM은 슬롯만 만든다** — 2026-06-22 페이지 파괴 사고(수율14%의 진짜 원인) 재발을 막으려
  정해진 칸의 값만 JSON으로 받고 화면은 코드가 그린다. 파싱이 실패해도 그 칸만 비고 원문·수급은
  그대로 산다. 각 항목이 원자 id를 들고 와 화면에서 원문을 되짚는다(브라우저로 확인).
  ★**갈라 읽기가 자동 생성된다**: "외국인이 20일 +1,761억을 담는 동안 기관은 -1,090억을 팔았습니다
  (합치면 +671억) … 매수의 79%가 최근 5일에 몰려" — 빈집 게이지가 기관+외인 합산이라 가려지던 것을
  코드가 갈라서 말한다. 목업에서 사람이 쓰던 문장을 함수가 만든다.
  실측 확인: `/api/insights2/brief?q=현대차` ok=true(gemini-3-flash-preview), 8/26 CEO 인베스터
  데이를 D-11로 포착, 쟁점 양쪽+pivot, 근거 클릭 시 DS투자증권 리포트 원문 3건 펼침.
  ⚠️ 남은 것: 빈집 등급(A/B/C/D)은 전종목 분포가 필요해 **미산출**(엑셀 파이프라인 5/27 정지).
  화면엔 "등급 미산출 · 방향만"으로 정직하게 표시. 공매도·또래비교는 미착수.


- 2026-08-15 — [UI] **가독성 일괄 개선**(사장님 "텍스트가 눈에 안 들어온다"). 원인 두 갈래로 갈라 고침:
  ①**글자가 작았다** — 본문 기준 14→16px, 여기 맞춰 31곳 재조정(헤드라인 26→29, 발언 12.5→14.5,
  채널명 13→15, 시각 11.5→13, 태그 10.5→12, 판세 이름 13→14.5, 칩 12→13.5 등).
  ②**회색이 흐렸다**(대비 부족 — 이게 나머지 절반) `--mut #8b97a8→#aab6c6`, `--dim #5a6575→#7d8a9c`.
  더불어 폭 `1180→1440px`(텔레는 채널을 옆으로 세우는 화면이라 넓을수록 한 번에 더 보인다),
  텔레 컬럼 `268→318px`·높이 `560→660px`, 발언 잘림 3줄→4줄, 판세 이름칸 78→92px(글자 커지면 잘림).
  브라우저 실측 확인(유튜브·텔레 양 탭).
  ※ 종목검색 페이지(키워드 종합 인사이트)는 설계 목업까지만 하고 **구현하면서 손보기로** 사장님과 정리.

- 2026-08-15 — [구현] **텔레 전용 화면 완성**(채널별 세로 컬럼 + 시간순). 유튜브 화면은 미변경
  (`CAT==='telegram'` 분기, 유튜브 탭 정상 브라우저 확인). 실측 `채널 15·발언 302`, 시간대 칩
  `장 전 77 / 장중 59 / 마감 후 121`(+시각없음 45), 마감 후 클릭 시 채널 12·15:32~23:22만 남음.
  ★핸드오프엔 "`목표가 None None`은 렌더링 때 걷어낼 것"이라 적혀 있었지만 실제로 보니 그 머리말은
  쓰레기가 아니라 **구조 정보**(`[중계:신한투자증권] 목표가 18,000원 매수 /`)였다 → 걷어내는 대신
  `_ins2_split_head`로 **relay·tp·opinion 필드로 분해**해 값 있는 것만 배지(🎯·↻)로 띄웠다.
  실측 971건 None/null 잔존 0. 빈값 판정은 `_ins2_blank` 한 곳에서만(실측 `None` 60 + **`null` 8**
  — 한쪽만 막으면 샌다). 부호 접두어도 `[긍정]`·`긍정:` 두 모양 다 제거. 같은 채널·같은 시각
  중복 문장(신한리서치 08:09 ×5)은 **표시만** ×N으로 접음(판세 집계는 원본 건수 유지).
  ⏱ 값의 뜻은 `ts_kind`(play/clock)로 API가 알려줘 `▶딥링크`/`🕘글자`로 갈라 그린다.
  ★부수 수정: `_ins2_require_admin`이 `_AUTH_ON`을 안 봐 **인증 OFF인 로컬에서도 403**이라
  화면을 볼 수 없었다 → 다른 관리자 경로(server.py:744,:818)와 같이 통과 처리(라이브는 잠금 그대로).
  다음=사장님 화면 피드백 / 리포트 화면(tp·opinion·relay 재료 이미 확보).

- 2026-08-15 — [구현·배포] **인사이트 새 UI `/insights2` 신설**(운영자 전용, 커밋 `76166c41e`+잠금).
  기존 `/insights`는 한 줄도 안 건드리고 옆에 새로 만듦(155줄 순수 추가). `GET /insights2` +
  `GET /api/insights2/daily?category=` — 기존 insights API가 4단 드릴다운용이라 화면 하나에
  4번 호출해야 해서, **판세 3칸(시장·섹터·종목)+문서별 발언을 한 방에** 주는 API를 새로 팜.
  사장님 피드백 **"난독·텍스트 과다"** → 설명박스 11개 제거, 위계를 크기로(26px 헤드라인),
  섹터·종목은 문장 대신 **막대+숫자**, **페이지 이동 0**(전부 제자리 펼침·ESC), 문서카드
  아래 결론 상시 노출. 접근제어는 미들웨어 기본차단 + `_ins2_require_admin` 2중 잠금
  (게이트 4케이스 실측 통과). 라이브 DB 38MB 복사해 5개 소스 전부 HTTP 200 확인
  (news 08-14 문서77·발언409 / report 로봇5:0·소비내수0:2 부호 정상).
  ⚠️**미완=데이터층 3건**: ①타임스탬프 전소스 0(raw엔 11,105개 있는데 원자화가 버림 —
  `youtube_ingest.py`가 파이프라인에 미연결된 고아, 비용 0으로 회수 가능) ②`sector='시장'`
  원자 없어 시장칸 0 ③`asset`에 문장이 종목명으로 들어옴. 크롤 재설계 때 같이 정리 예정.
  다음=사무실에서 라이브 보고 화면 피드백 → 텔레(채널별 컬럼)·리포트(증권사별 표) 개별 설계.
  핸드오프 `handoff/인사이트2.md`.
- 2026-08-15 — [조사] **텔레는 시각이 이미 잘 뽑히고 있다**(실측 `msg_ts` 4,421/6,285 = 70%,
  유튜브 `yt_timestamp` 72/4,644 = 1.5%와 대조). 단 **의미가 다르다** — 유튜브는 영상 재생위치라
  딥링크(`&t=`)가 되지만 텔레는 **메시지 발송 시각**이라 딥링크 0건, 대신 장전/장중/마감
  시간순 정렬에 쓴다. ★유튜브가 깨진 원인도 이것 — `POST_PROMPT`의 `ts`가 원래 텔레용으로
  설계됐는데 유튜브가 빌려 써서 `msg_ts`로 샜다(텔레는 자기 것이라 정상). → **작업 순서 변경:
  텔레 화면을 먼저** 만든다(선행작업 0, 지금 데이터로 바로 가능). 유튜브는 배선 고친 뒤.
  ⚠️텔레 잔여이슈: `speaker` 0건 / 내용에 `"[중계:신한] 목표가 None None /"` 쓰레기 섞임.
