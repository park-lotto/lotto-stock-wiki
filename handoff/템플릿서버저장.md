# 템플릿서버저장 — 제작소 프리셋 3종 계정(서버) 저장

## 2026-09-17 (TheRose PC)
- 제보: 이윤정님이 회사 PC에서 저장한 '본문 텍스트 템플릿'이 집 PC에서 사라짐.
- 원인: produce.html의 내 프리셋(hc_my_presets)·내 템플릿(fr_my_templates)·자막 프리셋(cap_presets)이
  전부 localStorage(브라우저 저장)였다. 서버 API 없음. 잡·대본·완성본은 서버 저장이라 이 셋만 예외였다.
- 조치: `/api/produce/presets/{kind}` GET/POST 추가(app.py, customer_prefs `preset::<kind>` 키, 내 채널명과 같은 방식).
  produce.html은 PRESETS 캐시 + `_presetSave`(전체 교체 POST) + `loadAccountPresets`(로드 시 GET,
  옛 localStorage 분은 {merge:[...]}로 계정에 합친 뒤 localStorage 삭제).
- ★함정: `loadPresets`가 이미 있었다(Whisper 역할 프리셋 13882행). 같은 이름으로 덮어 이관이 조용히 안 돌았다
  → `loadAccountPresets`로 개명. (memory: 같은이름_함수를_새로만들어_기존것을_죽였다 재발)
- 검증: pytest 3건 통과 + 로컬 uvicorn(8851) 실브라우저 — 저장/삭제/이관 서버 반영·UI 표시 실측.
- ⏭ 배포본(shoppingshorts.duckdns.org) 실측 — 자동배포는 새벽 2~6시 창. 이윤정님 계정으로 회사·집 양쪽 접속 시
  기존 것이 한 계정에 합쳐지는지 확인.
