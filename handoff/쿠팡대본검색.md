# 쿠팡대본검색 — 카드 🎬 쿠팡 대본검색 + 발굴 채널 판매채널 링크 자동

## 2026-09-26 (CH PC) — 트랙 커밋, 배포 승인 대기

사장님 요청(카드 화면 보고): ① 판매채널이 카드마다 있고 없는 이유 ② 쿠팡검색이 캡션 "간장 대신 이 마법의 비밀 소스"인데
"스텐 후라이팬"으로 나옴 ③ 기본 쿠팡검색은 두고 "살 물건 없음" 회색은 없애고, 대본으로 찾는 버튼을 따로.

### 조사로 확정한 사실
- 카드 🛒판매채널 = `index.html:2586` `i.inpock`. 값은 **벤치마킹 엑셀 4열(인포크링크)**에서만 왔다(434채널 중 413에 있음).
  발굴·등록 채널(`discovered_channels`)은 코드가 `inpock:""`로 넣어 절대 안 떴다. 스크린샷 9장 중 함은서·홈쏘굿·shop._.dami·이고 EGO가
  그 경우(엑셀에 없음). 48시간·이번주 탭(`hits_since`, reel_history)은 칸 자체가 없어 아무 카드도 안 뜬다(그대로 둠).
- 프로필을 열 때(`instagram_playwright._fetch_profiles_playwright`) 받는 graphql user 응답에 `external_url`/`bio_links`가 같이 있다 →
  **추가 요청 0**으로 링크를 얻는다. 매일 릴스 수집은 프로필을 안 연다(거기엔 얹을 것 없음).
- 쿠팡검색: 카드의 미리 판독(`identify_shop_many`)은 썸네일+캡션만. 클릭 시 `/api/coupang/identify`가 대본·캡션·썸네일 근거 우선.
  창 안 "🎬 영상 보고 정확히"(`_cfDeep`)가 이미 대본 추출→재판독을 한다. 후라이팬 오류는 `identify_from_evidence` 프롬프트에
  글 > 힌트 우선순위가 없어 힌트(썸네일 물체)를 답한 것.

### 고친 것
| 파일 | 내용 |
|---|---|
| `instagram_playwright.py` | `profile_link(u)` + 프로필 응답에 `link` 필드(추가 호출 0) |
| `store.py` | `discovered_channels.inpock` 칸(마이그레이션) · `add_discovered(..., inpock)` 빈값으로 안 지움 · `set_discovered_inpock` · `discovered_channels()`가 링크를 실음 → collect union 메타 → 카드 |
| `service.py` | `enrich_discovered_profile(username, store, fetch)` + `_async`(pytest 가드). adopt·auto_register에서 호출 |
| `app.py` | 등록 3경로(발굴 담기·확장 채널추가·URL 등록)에서 비동기 보강, 팔로워 조회가 프로필 열 때 링크도 저장. 안내 문구 이름 변경 |
| `coupang_query.py` | `_EVIDENCE_PROMPT`: 대본 > 캡션 > 키워드 > 썸네일 힌트, 글이 숨긴 물건은 힌트로 채우지 말고 빈값 |
| `sidebar.js` | 회색 "살 물건 없음" 제거(자료 만료 표시는 유지) · 창 버튼 "🎬 대본으로 다시 찾기" · `ssCoupangFind.script(opts)`(열자마자 대본 추출→판독) |
| `index.html` | 카드에 한 줄짜리 `🎬 쿠팡 대본검색` 버튼(`.cp-script-btn`, 점선 주황) |
| `scripts/backfill_discovered_inpock.py` | 기존 발굴 채널 1회 채우기(채널당 프로필 1회, `--limit/--dry`) |
- 테스트: `test_discovered_inpock.py` 5개 신규 · `test_coupang_no_product`의 회색 계약을 뒤집음 · 이름 변경 반영. 관련 그룹 통과.

### ⏭ 배포 후 할 일
1. 서버에서 `py scripts/backfill_discovered_inpock.py` 1회(발굴 채널 수만큼 프로필 열기, 2초 간격). 다음 수집부터 카드에 붙는다.
2. 라이브 확인: 카드에 🎬 쿠팡 대본검색 보임 · 어묵볶음(살림꿀팁) 카드에서 눌러 소스가 나오는지 · 회색 버튼 없음.
3. 미구현: `_historyCard`(즐겨찾기·기록 카드)엔 쿠팡 버튼이 원래 없다 — 필요하면 같은 버튼 추가.

### 배포 결과 (2026-09-26 14:00~)
- main 병합 `ce26eb9f2` → 서버 pull 13:5x. `sidebar.js` 라이브 응답 md5 = 로컬(4296053a, 새 문구 4회·회색 처리 0회). `index.html` 서버 디스크 md5 = 로컬(97af65a6, 버튼 2회). 웹·워커 재시작은 고객 접속으로 연기(최대 30분 뒤 강제).
- **백필 0건**: `python3 -m scripts.backfill_discovered_inpock`(316채널) 돌렸으나 링크 저장 0. 원인은 내 코드가 아니라 **서버의 프로필 열기 자체가 빈 dict**를 돌려준다 —
  `fetch_profiles(["seoyseoa.pping"])` → `{}`(user 응답 0건). 세션/프록시는 `_discover_session_proxy()`가 `ig_sessions/reference/28803564236.json` + webshare kr-11을 골랐고,
  프로필 페이지가 `instagram.com/`로 튕겨 graphql이 안 잡힌다. 같은 경로가 발굴 채널 **팔로워**도 채우므로(카드에 팔로워 줄이 없는 것과 일치) 이미 전부터 죽어 있던 상태.
  → 인스타 세션 풀 복구(메모리 `인스타세션고갈`) 뒤 백필을 다시 돌리면 된다. 코드 쪽은 세션만 살면 추가 요청 0으로 동작.
  ⚠️ 프로필 응답에 `external_url`이 실제로 실리는지는 세션이 살아야 확인된다(아직 미실측).
- 라이브 클릭 확인(어묵볶음 카드 🎬 쿠팡 대본검색 → 소스가 나오는지)은 브라우저 확장 미연결이라 못 했다 — 사장님이 눌러 보고 알려주시면 된다.
