# 인스타가격검사 트랙 로그

## 2026-08-21
- 사장님 육안 지적 "심지어 저렴한 가격밖에 안 하는거있죠" → 실측하니 **두 안 모두** 같은 값.
  `{가격}`에 형용사 "저렴한 가격"이 들어갔다. 장면게이트(면제)·문장틀검사(틀만 봄) 둘 다 비껴감.
  → `insta_facts.is_price`(숫자+돈단위 요구)·`drop_fake_price`로 **판정**을 세웠다(프롬프트 아님).
- "버리면 문맥 깨지나"(사장님 질문) 실측: A안(54)은 슬롯 없는 대체 변형이 이미 받고 있었고,
  B안(57)만 변형 1개라 칸이 빠졌다 → 57에 대체문 신설. **못 채운 칸 0개**로 조립이 끝까지 간다
  (종전엔 price 하나가 비면 조립을 통째로 버리고 AI가 가격을 지어냈다).
- 덤: spine 57 밀도 226 → 297 (인스타문형학습 인계 건). 정본 1곳만 고쳐 서버 재시드로 반영.
- ★`finish`가 병합 거부 — pytest 수집오류(rc=2). **게이트가 옳았다.** 진범은 내 것이 아니라
  `test_harvest_novel_style`이 수확 스크립트를 `exec_module`로 통째 실행한 것(키·서버경로·cp949).
  **모든 트랙의 병합이 이것 하나에 막혀 있었다.** 판정 로직 원본(`yt_style`)을 직접 import하도록
  테스트만 고쳐 해소(스크립트는 남의 트랙 소유라 한 줄도 안 건드림). 4991 passed.
- ⚠️ `finish` 출력만 믿을 뻔했다 — `git branch -r --contains`로 대조해야 병합 여부를 안다.
