# 쇼핑쇼츠 소통 큐 (기능 ②) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** ①이 수집한 릴스로, 캡션 기반 맞춤 댓글 3개를 미리 생성하고, 3가지 정렬로 소통 대상을 리스트/진행 모드로 보여주며, 완료를 DB에 기록·감출 수 있는 반자동 소통 큐를 만든다.

**Architecture:** 기능 ①의 `shopping_shorts/` 패키지를 확장한다. `comment_gen.py`(Gemini로 캡션→댓글 3개)를 `service.collect()`에 끼워 수집 시점에 draft를 미리 생성해 SQLite에 저장. `outreach.py`가 캐시된 릴스+draft를 결합해 3가지 정렬·완료필터를 적용한 큐를 만든다. `app.py`에 `/api/outreach`·`/api/comment/done` 추가, `static/outreach.html`이 리스트+진행 모드 프론트(클립보드 복사·새탭 오픈)를 담당.

**Tech Stack:** Python 3.12, FastAPI, SQLite. Gemini는 기존 `pipeline.atoms.key_vault`(키 로테이션 내장) + `google-genai` SDK 재사용, 모델 `gemini-3.1-flash-lite`. 프론트 순수 HTML/JS.

**작업 폴더:** `shopping_shorts/` (기능 ① 확장). 기능 ① 파일은 건드리지 않되, `store.py`·`service.py`·`app.py`는 확장(기존 함수/테스트 보존).

---

## 파일 구조

```
shopping_shorts/
├── (기능 ① 파일들 — 변경 없음: channels/categorize/ranking/apify_client/config)
├── comment_gen.py        # (신규) Gemini 캡션→댓글 3개
├── outreach.py           # (신규) 소통 큐: 정렬 3옵션 + 완료필터
├── store.py              # (확장) comment_drafts, commented 테이블 + 메서드
├── service.py            # (확장) collect() 안에서 draft 생성·저장
├── app.py                # (확장) /api/outreach, /api/comment/done
├── static/
│   ├── index.html        # (①, 변경 없음)
│   └── outreach.html     # (신규) 소통 큐 프론트 (리스트+진행)
└── tests/
    ├── test_comment_gen.py  # (신규) 프롬프트·파싱 단위테스트 (Gemini 호출은 목)
    ├── test_outreach.py     # (신규) 정렬 3옵션·완료필터
    └── test_store.py        # (확장) 새 테이블 메서드 테스트 추가
```

**테스트 전략:** `outreach`(순수 정렬/필터)·`store`(로컬 SQLite)·`comment_gen`의 파싱/프롬프트 조립부는 단위 테스트. Gemini 실제 호출은 네트워크라 목(monkeypatch)으로 대체. `service`/`app`은 통합(스모크).

---

## Task 1: store.py 확장 — comment_drafts + commented 테이블

**Files:**
- Modify: `shopping_shorts/store.py`
- Test: `shopping_shorts/tests/test_store.py` (기존 파일에 추가)

- [ ] **Step 1: 실패하는 테스트 추가** (`test_store.py` 맨 아래에 append; 기존 import/테스트 유지)

```python
import json


def test_comment_drafts_roundtrip(tmp_path):
    s = Store(tmp_path / "t.db")
    assert s.get_drafts("sc1") == []           # 없으면 빈 리스트
    s.save_drafts("sc1", ["댓글A", "댓글B", "댓글C"])
    assert s.get_drafts("sc1") == ["댓글A", "댓글B", "댓글C"]
    # 덮어쓰기
    s.save_drafts("sc1", ["새댓글"])
    assert s.get_drafts("sc1") == ["새댓글"]

def test_drafts_bulk_map(tmp_path):
    s = Store(tmp_path / "t.db")
    s.save_drafts("a", ["x"])
    s.save_drafts("b", ["y", "z"])
    m = s.drafts_map(["a", "b", "c"])
    assert m["a"] == ["x"] and m["b"] == ["y", "z"]
    assert m.get("c", []) == []

def test_commented_mark_and_query(tmp_path):
    s = Store(tmp_path / "t.db")
    assert s.commented_set() == set()
    s.mark_commented("sc1")
    s.mark_commented("sc1")   # 중복 무시 (INSERT OR IGNORE)
    s.mark_commented("sc2")
    assert s.commented_set() == {"sc1", "sc2"}
```

- [ ] **Step 2: 실패 확인**

Run: `cd "/c/Users/CH/Desktop/로또의 주식" && python -m pytest shopping_shorts/tests/test_store.py -v`
Expected: 새 3개 FAIL (`AttributeError: 'Store' object has no attribute 'get_drafts'`), 기존 3개 PASS

- [ ] **Step 3: 구현** — `store.py`의 `_init_schema`에 테이블 2개 추가, 메서드 추가.

`_init_schema` 안, 기존 `snapshots` 인덱스 생성 바로 다음에 추가:
```python
            c.execute("""
                CREATE TABLE IF NOT EXISTS comment_drafts (
                    shortcode TEXT PRIMARY KEY,
                    drafts_json TEXT NOT NULL
                )
            """)
            c.execute("""
                CREATE TABLE IF NOT EXISTS commented (
                    shortcode TEXT PRIMARY KEY,
                    commented_at TEXT
                )
            """)
```

파일 상단 import에 `import json` 추가 (없으면). 클래스에 메서드 추가:
```python
    def save_drafts(self, shortcode, drafts):
        """댓글 후보 리스트 저장(덮어쓰기). drafts: list[str]."""
        with self._conn() as c:
            c.execute(
                "INSERT INTO comment_drafts(shortcode, drafts_json) VALUES(?,?) "
                "ON CONFLICT(shortcode) DO UPDATE SET drafts_json=excluded.drafts_json",
                (shortcode, json.dumps(drafts, ensure_ascii=False)),
            )

    def get_drafts(self, shortcode):
        """저장된 댓글 후보 리스트. 없으면 []."""
        with self._conn() as c:
            row = c.execute(
                "SELECT drafts_json FROM comment_drafts WHERE shortcode=?", (shortcode,)
            ).fetchone()
        return json.loads(row[0]) if row else []

    def drafts_map(self, shortcodes):
        """여러 shortcode의 draft를 한 번에 dict{shortcode:list}로."""
        if not shortcodes:
            return {}
        qs = ",".join("?" * len(shortcodes))
        with self._conn() as c:
            rows = c.execute(
                f"SELECT shortcode, drafts_json FROM comment_drafts WHERE shortcode IN ({qs})",
                tuple(shortcodes),
            ).fetchall()
        return {sc: json.loads(dj) for sc, dj in rows}

    def mark_commented(self, shortcode):
        """소통 완료 기록 (중복 무시)."""
        with self._conn() as c:
            c.execute(
                "INSERT OR IGNORE INTO commented(shortcode, commented_at) "
                "VALUES(?, datetime('now'))",
                (shortcode,),
            )

    def commented_set(self):
        """완료된 shortcode 집합."""
        with self._conn() as c:
            rows = c.execute("SELECT shortcode FROM commented").fetchall()
        return {r[0] for r in rows}
```

- [ ] **Step 4: 통과 확인**

Run: `cd "/c/Users/CH/Desktop/로또의 주식" && python -m pytest shopping_shorts/tests/test_store.py -v`
Expected: 6 passed (기존 3 + 신규 3)

- [ ] **Step 5: Commit**

```bash
git add shopping_shorts/store.py shopping_shorts/tests/test_store.py
git commit -m "feat(shorts): store에 comment_drafts·commented 테이블 추가"
```

---

## Task 2: comment_gen.py — Gemini 캡션→댓글 3개

**Files:**
- Create: `shopping_shorts/comment_gen.py`
- Test: `shopping_shorts/tests/test_comment_gen.py`

기존 `pipeline.atoms.key_vault`(키 로테이션 내장) 재사용. 캡션+메타 → 자연스러운 한국어 댓글 3개(JSON). Gemini 실제 호출은 테스트에서 목.

- [ ] **Step 1: 실패하는 테스트 작성** (`test_comment_gen.py`)

```python
from shopping_shorts import comment_gen


def test_build_prompt_includes_caption_and_category():
    p = comment_gen.build_prompt("좁은 원룸 수납 꿀팁 대공개", "오후살림", "생활용품")
    assert "좁은 원룸 수납 꿀팁" in p
    assert "생활용품" in p
    assert "3" in p  # 3개 요청 명시

def test_parse_response_valid_json():
    raw = '["이거 어디 제품이에요?", "저도 원룸인데 참고돼요", "수납 꿀팁 감사해요"]'
    out = comment_gen.parse_response(raw)
    assert out == ["이거 어디 제품이에요?", "저도 원룸인데 참고돼요", "수납 꿀팁 감사해요"]

def test_parse_response_object_with_comments_key():
    raw = '{"comments": ["a", "b", "c"]}'
    assert comment_gen.parse_response(raw) == ["a", "b", "c"]

def test_parse_response_bad_returns_empty():
    assert comment_gen.parse_response("not json") == []
    assert comment_gen.parse_response("") == []

def test_generate_uses_client(monkeypatch):
    # Gemini 호출을 목으로 대체 — 실제 네트워크 없음
    class FakeResp:
        text = '["댓글1", "댓글2", "댓글3"]'
    class FakeModels:
        def generate_content(self, **kw): return FakeResp()
    class FakeClient:
        models = FakeModels()
    monkeypatch.setattr(comment_gen, "_get_client", lambda: FakeClient())
    out = comment_gen.generate("캡션텍스트", "채널", "뷰티")
    assert out == ["댓글1", "댓글2", "댓글3"]

def test_generate_empty_caption_still_tries(monkeypatch):
    class FakeResp:
        text = '["안녕하세요 잘 보고가요", "영상 좋네요", "팔로우하고가요"]'
    class FakeClient:
        class models:
            @staticmethod
            def generate_content(**kw): return FakeResp()
    monkeypatch.setattr(comment_gen, "_get_client", lambda: FakeClient())
    out = comment_gen.generate("", "채널명", "기타")
    assert len(out) == 3
```

- [ ] **Step 2: 실패 확인**

Run: `cd "/c/Users/CH/Desktop/로또의 주식" && python -m pytest shopping_shorts/tests/test_comment_gen.py -v`
Expected: FAIL — `ImportError` / `AttributeError`

- [ ] **Step 3: 구현** — `shopping_shorts/comment_gen.py`

```python
"""Gemini로 릴스 캡션 기반 자연스러운 댓글 후보 생성. 기존 key_vault 재사용."""
import json
import time
from google.genai import types
from pipeline.atoms import key_vault

_GROUP = "general"
_MODEL = "gemini-3.1-flash-lite"

_PROMPT = """너는 인스타에서 활발히 소통하는 진짜 사람이다.
아래 릴스를 방금 본 팔로워처럼, 영상 내용에 실제로 반응하는 한국어 댓글 3개를 만들어라.

[채널] {channel}
[카테고리] {category}
[영상 캡션] {caption}

규칙:
- 3개 각각 톤을 다르게: 하나는 질문형, 하나는 공감형, 하나는 칭찬/저장 언급형
- 영상 내용(캡션)에 구체적으로 반응할 것. 내용과 무관한 범용 문구 금지
- 1~2줄, 짧고 자연스럽게. 존댓말. 이모지는 0~1개만
- 광고·홍보·링크 금지. 봇처럼 보이는 정형 문구 금지
- 반드시 JSON 배열로만 출력: ["댓글1", "댓글2", "댓글3"]
"""


def _get_client():
    return key_vault.get_client(_GROUP)


def build_prompt(caption, channel, category):
    return _PROMPT.format(
        caption=(caption or "(캡션 없음 — 채널·카테고리로 유추)"),
        channel=channel or "",
        category=category or "기타",
    )


def parse_response(raw):
    """Gemini 응답 텍스트 → 댓글 list[str]. 실패 시 []."""
    if not raw:
        return []
    try:
        data = json.loads(raw)
    except (json.JSONDecodeError, ValueError):
        return []
    if isinstance(data, list):
        return [str(x) for x in data]
    if isinstance(data, dict) and isinstance(data.get("comments"), list):
        return [str(x) for x in data["comments"]]
    return []


def generate(caption, channel, category, max_retries=4):
    """캡션→댓글 3개. Gemini 쿼터 초과 시 key_vault 로테이션. 최종 실패 시 []."""
    prompt = build_prompt(caption, channel, category)
    for attempt in range(max_retries):
        try:
            resp = _get_client().models.generate_content(
                model=_MODEL,
                contents=prompt,
                config=types.GenerateContentConfig(response_mime_type="application/json"),
            )
            return parse_response(resp.text)
        except Exception as e:
            m = str(e)
            if key_vault.is_daily_exhausted_error(e):
                if key_vault.rotate(_GROUP):
                    continue
                return []
            if key_vault.is_quota_error(e):
                time.sleep(62)
                continue
            if attempt < max_retries - 1 and any(c in m for c in ("503", "UNAVAILABLE", "overloaded")):
                time.sleep((attempt + 1) * 5)
                continue
            return []  # 그 외 에러는 이 영상만 빈 결과로 스킵 (전체 중단 안 함)
    return []
```

- [ ] **Step 4: 통과 확인**

Run: `cd "/c/Users/CH/Desktop/로또의 주식" && python -m pytest shopping_shorts/tests/test_comment_gen.py -v`
Expected: 6 passed

- [ ] **Step 5: Commit**

```bash
git add shopping_shorts/comment_gen.py shopping_shorts/tests/test_comment_gen.py
git commit -m "feat(shorts): Gemini 캡션→댓글 3개 생성기 (key_vault 재사용)"
```

---

## Task 3: outreach.py — 소통 큐 (정렬 3옵션 + 완료필터)

**Files:**
- Create: `shopping_shorts/outreach.py`
- Test: `shopping_shorts/tests/test_outreach.py`

순수 함수: 릴스 항목 리스트 + draft맵 + 완료집합 → 정렬·필터·draft결합된 큐.

- [ ] **Step 1: 실패하는 테스트 작성** (`test_outreach.py`)

```python
from shopping_shorts.outreach import goldilocks_score, build_queue


def _item(sc, age, comments, speed):
    return {"shortcode": sc, "age_hours": age, "comments": comments, "speed": speed,
            "name": sc, "url": "u", "thumbnail": "", "category": "기타", "caption": ""}


def test_goldilocks_prefers_recent_low_comments():
    # 최근(age 낮음) + 댓글 적음 → 높은 점수
    fresh_quiet = goldilocks_score({"age_hours": 2, "comments": 10})
    old_loud = goldilocks_score({"age_hours": 40, "comments": 5000})
    assert fresh_quiet > old_loud

def test_sort_latest():
    items = [_item("a", 30, 100, 3), _item("b", 2, 50, 25), _item("c", 10, 999, 99)]
    q = build_queue(items, drafts_map={}, commented=set(), sort="latest")
    assert [i["shortcode"] for i in q] == ["b", "c", "a"]  # age 오름차순

def test_sort_hot():
    items = [_item("a", 30, 100, 3), _item("b", 2, 50, 25), _item("c", 10, 999, 99)]
    q = build_queue(items, drafts_map={}, commented=set(), sort="hot")
    assert [i["shortcode"] for i in q] == ["c", "b", "a"]  # speed 내림차순

def test_sort_goldilocks():
    items = [_item("fresh_quiet", 2, 10, 5), _item("old_loud", 40, 5000, 3)]
    q = build_queue(items, drafts_map={}, commented=set(), sort="goldilocks")
    assert q[0]["shortcode"] == "fresh_quiet"

def test_hide_done_filters_commented():
    items = [_item("a", 2, 10, 5), _item("b", 3, 20, 6)]
    q = build_queue(items, drafts_map={}, commented={"a"}, sort="latest", hide_done=True)
    assert [i["shortcode"] for i in q] == ["b"]

def test_show_done_marks_but_keeps():
    items = [_item("a", 2, 10, 5), _item("b", 3, 20, 6)]
    q = build_queue(items, drafts_map={}, commented={"a"}, sort="latest", hide_done=False)
    codes = {i["shortcode"]: i for i in q}
    assert set(codes) == {"a", "b"}
    assert codes["a"]["done"] is True
    assert codes["b"]["done"] is False

def test_drafts_attached():
    items = [_item("a", 2, 10, 5)]
    q = build_queue(items, drafts_map={"a": ["댓글1", "댓글2"]}, commented=set(), sort="latest")
    assert q[0]["drafts"] == ["댓글1", "댓글2"]
```

- [ ] **Step 2: 실패 확인**

Run: `cd "/c/Users/CH/Desktop/로또의 주식" && python -m pytest shopping_shorts/tests/test_outreach.py -v`
Expected: FAIL — `ImportError`

- [ ] **Step 3: 구현** — `shopping_shorts/outreach.py`

```python
"""소통 큐 — 릴스+draft 결합, 3가지 정렬, 완료 필터. 순수 함수."""


def goldilocks_score(item):
    """방금 올라왔는데(age 낮음) 아직 댓글 적은(comments 낮음) 것이 높은 점수."""
    age = item.get("age_hours") or 0
    comments = item.get("comments") or 0
    return (1.0 / (1.0 + comments)) * (1.0 / (1.0 + age))


def _sort_key(sort):
    if sort in ("latest", "최신"):
        return lambda i: (i.get("age_hours") if i.get("age_hours") is not None else 1e9), False
    if sort in ("hot", "터진"):
        return lambda i: (i.get("speed") or 0), True
    if sort in ("goldilocks", "골디락스"):
        return goldilocks_score, True
    return lambda i: (i.get("age_hours") if i.get("age_hours") is not None else 1e9), False


def build_queue(items, drafts_map, commented, sort="latest", hide_done=True):
    """릴스 항목 → 소통 큐. draft 결합, 완료 표시/필터, 정렬 적용.

    - drafts_map: {shortcode: [댓글...]}  - commented: set(shortcode)
    - hide_done=True면 완료 항목 제외, False면 done 플래그만 달고 유지.
    """
    out = []
    for i in items:
        sc = i.get("shortcode", "")
        done = sc in commented
        if done and hide_done:
            continue
        item = dict(i)
        item["done"] = done
        item["drafts"] = drafts_map.get(sc, [])
        out.append(item)

    keyfn, reverse = _sort_key(sort)
    out.sort(key=keyfn, reverse=reverse)
    return out
```

- [ ] **Step 4: 통과 확인**

Run: `cd "/c/Users/CH/Desktop/로또의 주식" && python -m pytest shopping_shorts/tests/test_outreach.py -v`
Expected: 8 passed

- [ ] **Step 5: Commit**

```bash
git add shopping_shorts/outreach.py shopping_shorts/tests/test_outreach.py
git commit -m "feat(shorts): 소통 큐 정렬 3옵션·완료필터·draft결합"
```

---

## Task 4: service.py 확장 — 수집 시 draft 미리 생성

**Files:**
- Modify: `shopping_shorts/service.py`

`collect()`가 랭킹 항목을 만든 뒤, 각 항목 캡션으로 댓글 draft를 생성해 저장한다. 네트워크(Gemini)라 단위테스트 없음(스모크는 Task 6).

- [ ] **Step 1: 구현** — `service.py` 수정

파일 상단 import에 추가:
```python
from shopping_shorts.comment_gen import generate as _gen_comments
```

`collect()`에서 `apply_grades(all_items)` 다음, `store.save_run(...)` 앞에 draft 생성 블록 추가:
```python
    apply_grades(all_items)

    # 댓글 draft 미리 생성 (기존 draft 없는 항목만 — 재수집 시 중복 호출·비용 방지)
    for it in all_items:
        sc = it["shortcode"]
        if not store.get_drafts(sc):
            drafts = _gen_comments(it.get("caption", ""), it.get("name"), it.get("category"))
            if drafts:
                store.save_drafts(sc, drafts)
```

주의: `build_items`가 반환하는 item에는 `caption` 키가 없다(현재 ranking.py는 caption을 저장 안 함). **`ranking.build_items`의 item dict에 `"caption": r.get("caption", "")`를 추가**해야 draft 생성이 캡션을 받는다. `ranking.py`의 `items.append({...})` 안에 `"caption": r.get("caption", ""),` 한 줄 추가하고, 아래 Step 2에서 확인.

- [ ] **Step 2: ranking.py에 caption 필드 추가 + 회귀 테스트 확인**

`shopping_shorts/ranking.py`의 `build_items` 내부 `items.append({...})` 딕셔너리에 다음 줄 추가 (category 줄 근처):
```python
            "caption": r.get("caption", ""),
```
그 다음 기존 랭킹 테스트가 여전히 통과하는지 확인 (caption 추가는 기존 assert에 영향 없음):

Run: `cd "/c/Users/CH/Desktop/로또의 주식" && python -m pytest shopping_shorts/tests/test_ranking.py -v`
Expected: 8 passed (기존 그대로)

- [ ] **Step 3: import·구문 확인**

Run: `cd "/c/Users/CH/Desktop/로또의 주식" && python -c "from shopping_shorts.service import collect; from shopping_shorts.ranking import build_items; print('ok')"`
Expected: `ok`

- [ ] **Step 4: Commit**

```bash
git add shopping_shorts/service.py shopping_shorts/ranking.py
git commit -m "feat(shorts): 수집 시 댓글 draft 미리 생성 + ranking에 caption 필드"
```

---

## Task 5: app.py 확장 — 소통 API

**Files:**
- Modify: `shopping_shorts/app.py`

- [ ] **Step 1: 구현** — `app.py`에 엔드포인트 추가

상단 import에 추가:
```python
from shopping_shorts.outreach import build_queue
from shopping_shorts.store import Store
from shopping_shorts.config import DB_PATH
```

정적 마운트(`app.mount(...)`) **앞에** 다음 엔드포인트 추가:
```python
@app.get("/api/outreach")
def api_outreach(sort: str = "latest", hide_done: bool = True):
    """소통 큐 반환 — 마지막 수집 릴스 + draft 결합, 정렬·완료필터."""
    store = Store(DB_PATH)
    items = _LAST["items"]
    drafts = store.drafts_map([i["shortcode"] for i in items])
    commented = store.commented_set()
    queue = build_queue(items, drafts_map=drafts, commented=commented,
                        sort=sort, hide_done=hide_done)
    return {"ok": True, "count": len(queue), "items": queue}


@app.post("/api/comment/done")
def api_comment_done(shortcode: str):
    """소통 완료 기록."""
    store = Store(DB_PATH)
    store.mark_commented(shortcode)
    return {"ok": True, "shortcode": shortcode}
```

- [ ] **Step 2: 서버 스모크** (수집 없이 빈 큐라도 200)

Run:
```
cd "/c/Users/CH/Desktop/로또의 주식"
python -m uvicorn shopping_shorts.app:app --port 8852 > /tmp/uv_o.log 2>&1 &
UVPID=$!; sleep 4
echo "outreach:"; curl -s "http://127.0.0.1:8852/api/outreach?sort=latest"
echo ""; echo "done:"; curl -s -X POST "http://127.0.0.1:8852/api/comment/done?shortcode=testsc"
echo ""; kill $UVPID 2>/dev/null
```
Expected: outreach → `{"ok":true,"count":0,"items":[]}` (아직 수집 캐시 비어있음), done → `{"ok":true,"shortcode":"testsc"}`. 서버 못 뜨면 cat /tmp/uv_o.log 하고 BLOCKED 보고.

- [ ] **Step 3: Commit**

```bash
git add shopping_shorts/app.py
git commit -m "feat(shorts): 소통 큐 API (/api/outreach, /api/comment/done)"
```

---

## Task 6: outreach.html — 소통 큐 프론트 (리스트 + 진행 모드)

**Files:**
- Create: `shopping_shorts/static/outreach.html`
- Modify: `shopping_shorts/static/index.html` (사이드바에 "소통 큐" 링크 추가)

- [ ] **Step 1: index.html 사이드바에 링크 추가**

`shopping_shorts/static/index.html`에서 `<div class="menu-item">📁 레퍼런스 모음집</div>` 줄 바로 다음에 추가:
```html
      <div class="menu-item" onclick="location.href='/outreach.html'" style="cursor:pointer">💬 소통 큐</div>
```

- [ ] **Step 2: outreach.html 작성** — `shopping_shorts/static/outreach.html`

```html
<!doctype html>
<html lang="ko">
<head>
<meta charset="utf-8">
<meta name="viewport" content="width=device-width, initial-scale=1">
<title>소통 큐 — 쇼핑쇼츠</title>
<style>
  :root{--bg:#0a0e14;--panel:#111722;--line:#1e2735;--txt:#e6edf3;--sub:#8b98a9;--accent:#2f81f7;--ok:#2ea043;}
  *{box-sizing:border-box;margin:0;padding:0}
  body{background:var(--bg);color:var(--txt);font-family:'Malgun Gothic',system-ui,sans-serif;display:flex;min-height:100vh}
  .sidebar{width:230px;background:var(--panel);border-right:1px solid var(--line);padding:16px;flex-shrink:0}
  .sidebar h1{font-size:14px;color:var(--sub);margin-bottom:16px}
  .menu-item{padding:8px 12px;border-radius:8px;font-size:13px;margin-bottom:2px;cursor:pointer}
  .menu-item.active{background:linear-gradient(90deg,#153a6b,#0d2340);color:#7db4ff}
  .main{flex:1;padding:28px 36px;overflow:auto}
  .head h2{font-size:24px;margin-bottom:6px}
  .desc{color:var(--sub);font-size:13px;margin-bottom:18px}
  .toolbar{display:flex;gap:10px;align-items:center;margin-bottom:18px;flex-wrap:wrap}
  select,button{background:var(--panel);color:var(--txt);border:1px solid var(--line);padding:9px 14px;border-radius:8px;font-size:13px;cursor:pointer}
  button.primary{background:var(--accent);border-color:var(--accent);color:#fff}
  .toggle{display:flex;align-items:center;gap:6px;font-size:12px;color:var(--sub)}
  .cards{display:grid;grid-template-columns:repeat(auto-fill,minmax(320px,1fr));gap:16px}
  .card{background:var(--panel);border:1px solid var(--line);border-radius:12px;overflow:hidden;display:flex;flex-direction:column}
  .card.done{opacity:.45}
  .card img{width:100%;height:150px;object-fit:cover;background:#000}
  .card .body{padding:12px}
  .meta{font-size:12px;color:var(--sub)}
  .ch{font-weight:600;font-size:14px;margin:4px 0}
  .cap{font-size:12px;color:var(--sub);margin:6px 0;max-height:34px;overflow:hidden}
  .draft{background:#0d131c;border:1px solid var(--line);border-radius:8px;padding:8px;margin:6px 0;font-size:13px;display:flex;justify-content:space-between;gap:8px;align-items:center}
  .draft button{padding:4px 10px;font-size:11px;flex-shrink:0}
  .row{display:flex;gap:8px;margin-top:10px}
  .row button{flex:1}
  .empty{color:var(--sub);text-align:center;padding:60px;grid-column:1/-1}
  /* 진행 모드 */
  .progress-wrap{max-width:560px;margin:0 auto}
  .progress-wrap .card img{height:220px}
  .counter{text-align:center;color:var(--sub);font-size:13px;margin-top:12px}
</style>
</head>
<body>
  <aside class="sidebar">
    <h1>🛍️ 쇼핑쇼츠</h1>
    <div class="menu-item" onclick="location.href='/'">📊 레퍼런스 랭킹</div>
    <div class="menu-item active">💬 소통 큐</div>
  </aside>
  <main class="main">
    <div class="head"><h2>💬 소통 큐</h2></div>
    <div class="desc">레퍼런스 릴스에 직접 댓글을 달아 계정을 키웁니다. 문구는 참고용 — 붙여넣기·전송은 직접.</div>
    <div class="toolbar">
      <select id="sort" onchange="reload()">
        <option value="latest">🆕 최신 업로드순</option>
        <option value="hot">🔥 갓 올라온 터진 영상</option>
        <option value="goldilocks">🎯 골디락스 (선점 효율)</option>
      </select>
      <label class="toggle"><input type="checkbox" id="hideDone" checked onchange="reload()"> 완료 감추기</label>
      <button class="primary" id="modeBtn" onclick="toggleMode()">▶ 진행 시작</button>
      <span id="status" style="color:var(--sub);font-size:12px"></span>
    </div>
    <div id="view"><div class="cards"><div class="empty">불러오는 중…</div></div></div>
  </main>
<script>
let STATE = {items:[], mode:"list", idx:0};

function copyText(t, btn){
  navigator.clipboard.writeText(t).then(()=>{ if(btn){const o=btn.textContent;btn.textContent="복사됨✓";setTimeout(()=>btn.textContent=o,1200);} });
}
function openAndCopy(item){
  if(item.drafts && item.drafts[0]) navigator.clipboard.writeText(item.drafts[0]);
  window.open(item.url, "_blank");
}
async function markDone(sc){
  await fetch(`/api/comment/done?shortcode=${encodeURIComponent(sc)}`, {method:"POST"});
}

function cardHtml(i, idx){
  const drafts = (i.drafts||[]).map(d=>
    `<div class="draft"><span>${d}</span><button onclick="copyText(${JSON.stringify(d).replace(/"/g,'&quot;')}, this)">복사</button></div>`).join("")
    || `<div class="draft" style="color:#c98">문구 생성 실패 — 직접 작성</div>`;
  return `<div class="card ${i.done?'done':''}" id="c_${i.shortcode}">
    <img src="${i.thumbnail||''}" onerror="this.style.display='none'">
    <div class="body">
      <div class="meta">#${idx+1} · ${i.category} · ${Math.round(i.age_hours)}시간 전 · 댓글 ${(i.comments||0).toLocaleString()}</div>
      <div class="ch">${i.name||i.username||''} ${i.done?'✅':''}</div>
      <div class="cap">${(i.caption||'').slice(0,60)}</div>
      ${drafts}
      <div class="row">
        <button class="primary" onclick='openAndCopy(${JSON.stringify(i)})'>🔗 게시물 열기+복사</button>
        <button onclick="doneCard('${i.shortcode}')">✓ 완료</button>
      </div>
    </div></div>`;
}

function renderList(){
  const v=document.getElementById("view");
  if(!STATE.items.length){ v.innerHTML='<div class="cards"><div class="empty">큐가 비었어요. 먼저 레퍼런스 랭킹에서 수집하세요.</div></div>'; return; }
  v.innerHTML = `<div class="cards">${STATE.items.map((i,x)=>cardHtml(i,x)).join("")}</div>`;
}
async function doneCard(sc){
  await markDone(sc);
  const el=document.getElementById("c_"+sc);
  if(document.getElementById("hideDone").checked){ if(el) el.remove(); }
  else if(el){ el.classList.add("done"); }
}

function renderProgress(){
  const v=document.getElementById("view");
  const list=STATE.items.filter(i=>!i.done);
  if(!list.length){ v.innerHTML='<div class="empty">오늘 소통 완료 🎉</div>'; return; }
  if(STATE.idx>=list.length) STATE.idx=list.length-1;
  const i=list[STATE.idx];
  v.innerHTML=`<div class="progress-wrap">${cardHtml(i, STATE.idx)}
    <div class="counter">${STATE.idx+1} / ${list.length}</div></div>`;
}
async function doneCardProgress(sc){
  await markDone(sc);
  const it=STATE.items.find(x=>x.shortcode===sc); if(it) it.done=true;
  renderProgress();
}
function toggleMode(){
  STATE.mode = STATE.mode==="list" ? "progress" : "list";
  STATE.idx=0;
  document.getElementById("modeBtn").textContent = STATE.mode==="list" ? "▶ 진행 시작" : "☰ 리스트로";
  render();
}
function render(){ STATE.mode==="list" ? renderList() : renderProgress(); }

async function reload(){
  const sort=document.getElementById("sort").value;
  const hide=document.getElementById("hideDone").checked;
  document.getElementById("status").textContent="불러오는 중…";
  const d=await (await fetch(`/api/outreach?sort=${sort}&hide_done=${hide}`)).json();
  STATE.items=d.items||[]; STATE.idx=0;
  document.getElementById("status").textContent=`${d.count}건`;
  render();
}
// 진행 모드에서 완료 버튼이 doneCard를 부르므로, 진행모드일 땐 다른 핸들러로 위임
document.addEventListener("click", ()=>{}, false);
reload();
</script>
</body>
</html>
```

> 참고: 위 `cardHtml`의 「완료」 버튼은 `doneCard()`를 호출한다. 진행 모드에서는 `doneCard`가
> 아이템을 제거/회색처리하지만 진행 카운터 갱신이 필요하므로, 진행 모드 렌더 시 완료 버튼의
> onclick을 `doneCardProgress`로 바꾼다. 이를 위해 `renderProgress()`에서 생성한 카드의
> 완료 버튼 onclick을 `doneCardProgress('${i.shortcode}')`로 교체하는 처리를 구현자가 추가할 것
> (간단히: renderProgress에서 cardHtml 결과 문자열의 `doneCard('` → `doneCardProgress('` 치환).

구현자 주의: 위 참고대로 `renderProgress()` 안에서
```javascript
  v.innerHTML=`<div class="progress-wrap">${cardHtml(i, STATE.idx).replace("doneCard('", "doneCardProgress('")}
    <div class="counter">${STATE.idx+1} / ${list.length}</div></div>`;
```
로 작성해 진행 모드 완료가 카운터를 갱신하도록 한다.

- [ ] **Step 3: 서버 스모크 — outreach.html 서빙 확인**

Run:
```
cd "/c/Users/CH/Desktop/로또의 주식"
python -m uvicorn shopping_shorts.app:app --port 8853 > /tmp/uv_o2.log 2>&1 &
UVPID=$!; sleep 4
echo "outreach.html:"; curl -s -o /dev/null -w "%{http_code}" http://127.0.0.1:8853/outreach.html
echo ""; kill $UVPID 2>/dev/null
```
Expected: `200`

- [ ] **Step 4: Commit**

```bash
git add shopping_shorts/static/outreach.html shopping_shorts/static/index.html
git commit -m "feat(shorts): 소통 큐 프론트 (리스트+진행 모드, 클립보드·새탭)"
```

---

## Task 7: 전체 테스트 + 실제 수집 스모크

**Files:** 없음 (검증만)

- [ ] **Step 1: 전체 단위 테스트**

Run: `cd "/c/Users/CH/Desktop/로또의 주식" && python -m pytest shopping_shorts/tests/ -v`
Expected: 모두 PASS (① 23 + store확장 3 + comment_gen 6 + outreach 8 = 40개 내외). 실패 시 BLOCKED 보고.

- [ ] **Step 2: 실제 소량 수집 + draft 생성 스모크 (사용자 토큰 필요)**

> ⚠️ 실제 Apify + Gemini 호출. 사용자가 `APIFY_TOKEN` 설정 + `.env`에 GEMINI 키 있어야 함.

Run (사용자):
```
$env:APIFY_TOKEN="apify_api_xxx"
python -c "from shopping_shorts.service import collect; items=collect(limit_channels=5); print(len(items),'건'); print('draft샘플:', items[0].get('caption','')[:30] if items else '없음')"
python -c "from shopping_shorts.store import Store; from shopping_shorts.config import DB_PATH; s=Store(DB_PATH); import itertools; ks=list(s.commented_set()); print('완료기록:', len(ks)); print('draft 있는 항목 확인 필요 — 웹에서 확인')"
```
확인 포인트: collect가 에러 없이 끝나고, 이후 소통 큐 화면(`/outreach.html`)에서 각 카드에 댓글 3개가 뜨는지.

- [ ] **Step 3: 통합 서버 스모크 — 수집→소통큐 흐름**

Run:
```
cd "/c/Users/CH/Desktop/로또의 주식"
APIFY_TOKEN=$APIFY_TOKEN python -m uvicorn shopping_shorts.app:app --port 8854 > /tmp/uv_full.log 2>&1 &
UVPID=$!; sleep 4
curl -s -X POST "http://127.0.0.1:8854/api/collect?limit=5" -o /dev/null -w "collect:%{http_code}\n"
curl -s "http://127.0.0.1:8854/api/outreach?sort=goldilocks" | python -c "import sys,json; d=json.load(sys.stdin); print('큐',d['count'],'건, 첫항목 draft수:', len(d['items'][0]['drafts']) if d['items'] else 0)"
kill $UVPID 2>/dev/null
```
Expected: collect 200, 큐에 draft가 붙은 항목이 나옴 (draft수 1~3).

- [ ] **Step 4: Commit (있으면)** — 검증만이면 커밋 없음. log/README 갱신 시:

```bash
git add shopping_shorts/README.md
git commit -m "docs(shorts): 소통 큐 사용법 README 추가"
```

---

## 완료 기준

- [ ] `python -m pytest shopping_shorts/tests/` 전부 통과 (40개 내외)
- [ ] 수집 시 각 릴스에 댓글 draft 3개가 SQLite에 저장됨
- [ ] `/outreach.html`에서 정렬 3옵션 전환 작동, 카드에 draft 3개 표시
- [ ] 「복사」·「게시물 열기+복사」·「완료」 동작, 「완료 감추기」 토글 작동
- [ ] 진행 모드 전환 + 한 개씩 넘기기 + 카운터 갱신
- [ ] 완료한 항목이 DB에 기록되고 (토글 ON) 다음 로드 시 안 뜸
```
