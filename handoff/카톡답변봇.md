> **소유 트랙**: 카톡답변봇 — 이 파일은 이 트랙 세션만 수정한다. 다른 트랙은 읽기만.

# 핸드오프 — 카카오 오픈채팅 답변봇

- 갱신: 2026-08-25 (회사PC, 야간 자동진행)
- 설계: `docs/superpowers/specs/2026-08-25-카톡-답변봇-design.md`
- 계획: `docs/superpowers/plans/2026-08-25-카톡-답변봇.md`

## ⏭ 사장님이 하실 일 (이것만 하면 봇이 돕니다)

### ① 서버에 비밀키 넣기 — 이걸 안 하면 봇이 **아예 안 돕니다**(의도된 잠금)

```bash
# 1) 아무 긴 문자열이나 하나 만든다(이 명령이 만들어 준다)
py -c "import secrets; print(secrets.token_urlsafe(32))"

# 2) 서버에 넣는다 (아래 <키> 자리에 위에서 나온 값)
ssh -i C:/Users/CH/crawling_bot_client/LightsailDefaultKey-ap-northeast-2.pem ubuntu@43.200.48.69
sudo sh -c 'echo "KAKAO_BOT_SECRET=<키>" >> /etc/shopping-shorts.env'
sudo systemctl restart shopping-shorts
```

⚠️ **git에 넣지 마라.** 제미니 키와 같은 곳(`/etc/shopping-shorts.env`)에만 둔다.

### ② 공기계에 봇 앱 설치

1. 공기계에 **메신저봇R** 설치
2. 새 스크립트 → `shopping_shorts/userscript/kakao_bot.js` 내용 붙여넣기
3. 파일 맨 위 `SECRET = "여기에_서버와_같은_비밀키"` 를 ①에서 만든 값으로
4. 안드로이드 설정 → **알림 접근 권한** 허용
5. **배터리 최적화 제외** (안 하면 폰이 잠들면서 봇이 멈춘다)
6. 오픈채팅방에서 `!질문 테스트` → 답이 오는지 확인

### ③ 자료 채우기 (봇이 답을 하려면 필요)

지금 `bot_qa`가 **0건**이라 봇은 뭘 물어도 "확인해서 알려드릴게요"만 합니다. 이건 고장이
아니라 **지어내지 않게 만든 설계**입니다.

```bash
py tools/bot_qa_draft.py --dry-run --limit 5   # 먼저 이걸로 프롬프트만 확인(무료)
py tools/bot_qa_draft.py --limit 30            # 진짜 초안 30개 생성(제미니 사용)
```
→ `shoppingshorts.duckdns.org/bot_admin.html` 에서 ✅승인 / ✏️수정 / 🗑️버림

**30개만 먼저 뽑아 방향을 보시라**고 계획에 적어뒀습니다. 개발 용어가 섞이면
`tools/bot_qa_draft.py`의 `_PROMPT_HEAD`를 고치고 다시 뽑으면 됩니다(초안은 버려도 됩니다).

## 무엇을 만들었나

```
[카톡방]              [공기계 폰]                [우리 서버]
!질문 ... ──알림──> kakao_bot.js ──HTTPS──> POST /api/kakao/ask
                     (배달만, 22줄)              ├ 긴급정지 확인
                                                 ├ !호출인가
                                                 ├ 하루 상한(사람당)
                                                 ├ 민감어면 사람 연결
                                                 ├ 승인된 자료에서 검색
                                                 ├ 자료 있으면 제미니, 없으면 안 부름
                     답장  <────────────────────┘ 못 답하면 미답변 목록에 적재
```

| 파일 | 책임 |
|---|---|
| `shopping_shorts/bot_qa.py` | 순수 판정(호출어·민감어·길이·검색) — DB 없이 테스트 |
| `shopping_shorts/bot_answer.py` | 자료→프롬프트→제미니. **모델 교체 지점 1곳** |
| `shopping_shorts/store.py` | `bot_qa`·`bot_unanswered` 테이블 + CRUD 6개 |
| `shopping_shorts/app.py` | `/api/kakao/ask` + 관리자 3개 + `/bot_admin.html` |
| `shopping_shorts/static/bot_admin.html` | 검수 화면 |
| `shopping_shorts/userscript/kakao_bot.js` | 폰 스크립트(22줄) |
| `tools/bot_qa_draft.py` | 핸드오프 → Q&A 초안 |

## 설계에서 중요한 것 3가지

**① 폰은 배달만 한다.** 판정·검색·생성이 전부 서버에 있어서, 카톡이 업데이트돼 폰
스크립트가 깨져도 답변 품질 코드는 무사하다. 테스트(`test_phone_script_stays_thin`)가
폰 스크립트 30줄을 강제하고, 거기 "환불·결제·포인트" 같은 판정어가 들어가면 실패시킨다.

**② 지어내지 않는다.** 승인된 자료에서 검색해 근거가 약하면 **제미니를 아예 안 부른다**.
`bot_answer.answer()`가 None을 주고 라우트가 "확인해서 알려드릴게요"로 넘긴다.
못 답한 질문은 `bot_unanswered`에 쌓여 **다음에 채울 목록**이 된다(같은 질문은 count 증가).

**③ 돈 얘기는 AI를 안 거친다.** 환불·결제·계좌·입금·카드·청구·요금제·해지·탈퇴가 들어오면
검색도 생성도 건너뛰고 바로 운영자 연결로 간다(잘못 답하면 분쟁).

## 야간 진행 중 잡은 것 (계획이 틀렸던 부분)

1. **`!질문` 정규식 버그** — `!질문`만 치면 "질문"을 질문 내용으로 읽었다. 키워드 뒤에
   공백/끝일 때만 벗기게 수정.
2. **★비밀키 미설정 = 라우트 개방** — 종전 비교식은 미설정("")과 헤더없음("")이 같아져
   **아무나 부를 수 있었다**(실측 200). 사장님이 키를 나중에 넣는 게 정상 경로라
   그 사이가 위험했다 → 키가 비면 무조건 401.
3. **`DASH_PASS` 없으면 인증이 통째로 꺼진다** — 관리자 가드 테스트가 가드 없이도
   통과하는 가짜 green이 될 뻔했다. 게다가 `monkeypatch.setenv`는 **import 시점에 읽히는
   값이라 효과가 없어**, 코드베이스 관례대로 `_AUTH_ON`을 직접 패치로 교정.
4. **중복 함수 신설 시도** — 계획이 `_bot_admin_denied`를 새로 만들라 했는데 이미
   `_require_admin`이 10곳에서 쓰이고 있었다(0순위-B 위반) → 기존 것 재사용.
5. **★`Utils.getWebText2` 시그니처가 없는 것이었다** — 계획의 4인자 호출은 존재하지 않는다.
   공식 문서상 그건 **인자 2개짜리 레거시**(url, 인코딩)라 POST·헤더를 못 싣는다.
   대안 `Http.request`도 본문 키가 문서에 없어 → **jsoup 직접 호출**로 교정.
6. **테스트 격리 버그** — 하루 상한 테스트가 **우연히** 통과하고 있었다(앞 테스트의
   카운트 이월). 단언을 약하게 만들지 않고 픽스처에서 상태를 비우게 수정.
7. `.format()` 중괄호 함정은 **실측 결과 사실이 아니었다**(150개 전부 무사). 그래도
   더 안전한 문자열 이어붙이기로 두되, 사실이 아니었다는 걸 기록해 둔다.

## 검증

- 봇 테스트 **64 passed** / 회귀 `-k "store or challenge or app_produce or bot"` **379 passed, 0 failed**
- JS 문법 `node --check` rc=0 (검수 화면·폰 스크립트 둘 다)
- 되돌림 검사: 비밀키 가드를 빼면 실제로 FAIL
- ⚠️ **실기 미확인**: 폰에서 실제로 도는지는 사장님이 ①②를 하신 뒤에야 알 수 있다.
  특히 jsoup 호출은 메신저봇R 실기에서 한 번 확인이 필요하다.

## ⏭ 남은 것

- 실기 확인(위 ①②)
- 자료 채우기(위 ③) — 상위 50개면 질문의 대부분을 먹는다
- 봇 이름·말투는 안 정했다(지금은 존댓말 상담원 톤)
- 체험단방 "내 현황"은 **안 만들었다** — 카톡 닉네임과 챌린지 계정을 이을 방법이 없어
  잘못 이으면 남의 현황이 보인다. `/challenge` 링크만 안내한다.
