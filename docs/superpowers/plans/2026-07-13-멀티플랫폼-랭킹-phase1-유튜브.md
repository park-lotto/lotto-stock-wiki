# 멀티플랫폼 랭킹 Phase 1 (유튜브) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 레퍼런스 랭킹에 유튜브 플랫폼을 무료(YouTube Data API + yt-dlp)로 추가한다. 인스타 패턴을 어댑터로 일반화하고, 속도/가속/참여율 지표를 조회수 기반으로 계산한다.

**Architecture:** 플랫폼별 fetcher(`youtube_client`)가 공통 item 스키마를 반환 → `ranking.build_youtube_items`가 조회수 기반 지표 계산 → 기존 `apply_grades`/`sort_by`/랭킹 UI 재사용. `service.collect(platform)`으로 분기. 믹스 다운로드는 URL로 yt-dlp/Apify 분기.

**Tech Stack:** Python 3, FastAPI, SQLite, YouTube Data API v3(`config.YOUTUBE_API_KEYS`), yt-dlp, pytest.

## Global Constraints

- 브랜치 **main** 고정. `git add`는 자기 파일만(`-A` 금지). 커밋 전 `git branch --show-current`=main 확인.
- 인스타 경로(`apify_client`, 기존 `build_items`) **무변경** — 어댑터만 추가.
- YouTube Data API 키: `config.YOUTUBE_API_KEYS`(env `YOUTUBE_API_KEY`, `YOUTUBE_API_KEY_2`…) 재사용. 쿼터 초과 시 다음 키 회전.
- 무료 원칙: 유튜브 다운로드/수집에 Apify 사용 금지.
- item `platform` 필드 표준값: `"instagram" | "youtube" | "tiktok"`.
- 테스트는 실제 API/네트워크 호출 없이 mock으로. 각 태스크 끝에 커밋.

---

## File Structure

- `shopping_shorts/youtube_client.py` (신규) — YouTube Data API 어댑터. 키워드 검색·통계 조회. 순수 HTTP, 키 회전.
- `shopping_shorts/ranking.py` (수정) — `build_youtube_items` 추가(조회수 기반 지표). 기존 함수 무변경.
- `shopping_shorts/store.py` (수정) — `platform_seeds` 테이블 + CRUD, `save_run`/`prev_comments`를 platform 스코프로.
- `shopping_shorts/service.py` (수정) — `collect(platform="instagram")` 분기.
- `shopping_shorts/app.py` (수정) — `/api/collect`·`/api/reference`에 `platform`, `/api/seeds` 3종.
- `shopping_shorts/static/index.html` (수정) — 플랫폼 탭 + 유튜브 키워드 시드 UI.
- `shopping_shorts/mix_pipeline.py` (수정) — 다운로드 URL 분기(yt-dlp).
- `shopping_shorts/media_download.py` (신규) — `download_any(url, dest)`: URL로 yt-dlp/Apify 선택.
- 테스트: `tests/test_youtube_client.py`, `tests/test_ranking_youtube.py`, `tests/test_store_seeds.py`, `tests/test_media_download.py`, `tests/test_app_platform.py`.

---

### Task 1: youtube_client — 키워드 검색 + 통계

**Files:**
- Create: `shopping_shorts/youtube_client.py`
- Test: `shopping_shorts/tests/test_youtube_client.py`

**Interfaces:**
- Consumes: `config.YOUTUBE_API_KEYS` (list[str])
- Produces:
  - `search_shorts(keywords: list[str], published_after_iso: str, max_per_kw: int = 20, token=None) -> list[dict]`
    반환 dict(원시): `{"video_id","channel_id","channel_title","title","description","thumbnail","published_at","views","likes","comments"}`

- [ ] **Step 1: Write the failing test**

```python
# tests/test_youtube_client.py
from shopping_shorts import youtube_client as yc


def test_parse_search_and_stats(monkeypatch):
    # search.list 응답(검색) + videos.list 응답(통계)을 mock
    search_resp = {"items": [
        {"id": {"videoId": "vid1"},
         "snippet": {"channelId": "ch1", "channelTitle": "살림TV",
                     "title": "오이 보관법", "description": "꿀팁",
                     "publishedAt": "2026-07-12T00:00:00Z",
                     "thumbnails": {"high": {"url": "http://t/vid1.jpg"}}}}
    ]}
    stats_resp = {"items": [
        {"id": "vid1", "statistics": {"viewCount": "10000", "likeCount": "500", "commentCount": "40"}}
    ]}

    calls = {"n": 0}
    def fake_get(url, params=None, timeout=None):
        calls["n"] += 1
        class R:
            status_code = 200
            def json(self_inner):
                return search_resp if "search" in url else stats_resp
        return R()
    monkeypatch.setattr(yc.requests, "get", fake_get)

    items = yc.search_shorts(["살림꿀팁"], "2026-07-11T00:00:00Z", max_per_kw=20, token="KEY")
    assert len(items) == 1
    it = items[0]
    assert it["video_id"] == "vid1"
    assert it["channel_title"] == "살림TV"
    assert it["views"] == 10000 and it["likes"] == 500 and it["comments"] == 40
    assert it["thumbnail"] == "http://t/vid1.jpg"
```

- [ ] **Step 2: Run test to verify it fails**

Run: `py -m pytest shopping_shorts/tests/test_youtube_client.py -q`
Expected: FAIL (module/함수 없음)

- [ ] **Step 3: Write minimal implementation**

```python
# shopping_shorts/youtube_client.py
"""YouTube Data API v3 어댑터 — 키워드로 인기 Shorts 발굴 + 통계.

무료(쿼터 내). config.YOUTUBE_API_KEYS를 순서대로 시도(쿼터 초과 시 다음 키).
검색(search.list)은 통계가 없어 videos.list로 조회수·좋아요·댓글을 채운다.
"""
import requests
from shopping_shorts.config import YOUTUBE_API_KEYS

_SEARCH_URL = "https://www.googleapis.com/youtube/v3/search"
_VIDEOS_URL = "https://www.googleapis.com/youtube/v3/videos"


def _stats(video_ids, token):
    """videos.list(statistics) → {video_id: {views,likes,comments}}."""
    out = {}
    for i in range(0, len(video_ids), 50):          # API 상한 50개/호출
        chunk = video_ids[i:i + 50]
        r = requests.get(_VIDEOS_URL, params={
            "part": "statistics", "id": ",".join(chunk), "key": token}, timeout=30)
        if r.status_code != 200:
            continue
        for it in r.json().get("items", []):
            s = it.get("statistics", {})
            out[it["id"]] = {
                "views": int(s.get("viewCount") or 0),
                "likes": int(s.get("likeCount") or 0),
                "comments": int(s.get("commentCount") or 0),
            }
    return out


def search_shorts(keywords, published_after_iso, max_per_kw=20, token=None):
    """키워드별로 최신·인기 영상 검색 → 통계 채운 원시 dict 리스트."""
    tokens = [token] if token else YOUTUBE_API_KEYS
    if not tokens:
        raise RuntimeError("YOUTUBE_API_KEY 미설정")
    tok = tokens[0]
    raw = []
    for kw in keywords:
        r = requests.get(_SEARCH_URL, params={
            "part": "snippet", "q": kw, "type": "video", "videoDuration": "short",
            "order": "viewCount", "publishedAfter": published_after_iso,
            "maxResults": min(max_per_kw, 50), "key": tok}, timeout=30)
        if r.status_code != 200:
            continue
        for it in r.json().get("items", []):
            vid = (it.get("id") or {}).get("videoId")
            sn = it.get("snippet") or {}
            if not vid:
                continue
            raw.append({
                "video_id": vid, "channel_id": sn.get("channelId"),
                "channel_title": sn.get("channelTitle"),
                "title": sn.get("title"), "description": sn.get("description"),
                "thumbnail": ((sn.get("thumbnails") or {}).get("high") or {}).get("url", ""),
                "published_at": sn.get("publishedAt"),
            })
    stats = _stats([r["video_id"] for r in raw], tok)
    for r in raw:
        r.update(stats.get(r["video_id"], {"views": 0, "likes": 0, "comments": 0}))
    return raw
```

- [ ] **Step 4: Run test to verify it passes**

Run: `py -m pytest shopping_shorts/tests/test_youtube_client.py -q`
Expected: PASS

- [ ] **Step 5: Commit**

```bash
git add shopping_shorts/youtube_client.py shopping_shorts/tests/test_youtube_client.py
git commit -m "feat(shopping_shorts): youtube_client — 키워드 Shorts 검색+통계(Data API)"
```

---

### Task 2: ranking — 유튜브 item 빌더(조회수 기반 지표)

**Files:**
- Modify: `shopping_shorts/ranking.py` (끝에 함수 추가)
- Test: `shopping_shorts/tests/test_ranking_youtube.py`

**Interfaces:**
- Consumes: `youtube_client.search_shorts` 원시 dict, `ranking.hours_since`
- Produces:
  - `build_youtube_items(raw, prev_base, prev_delta, now=None, window_hours=48) -> list[dict]`
    공통 item(platform="youtube", base_count=views): speed=views/age, density=(likes+comments)/views, accel=Δ−prevΔ. `apply_grades`/`sort_by`가 그대로 소비.

- [ ] **Step 1: Write the failing test**

```python
# tests/test_ranking_youtube.py
from datetime import datetime, timezone
from shopping_shorts import ranking


def test_build_youtube_items_views_based():
    now = datetime(2026, 7, 13, tzinfo=timezone.utc)
    raw = [{
        "video_id": "vid1", "channel_id": "ch1", "channel_title": "살림TV",
        "title": "오이 보관법", "description": "d", "thumbnail": "http://t.jpg",
        "published_at": "2026-07-12T00:00:00Z",  # 24h 전
        "views": 24000, "likes": 1200, "comments": 24,
    }]
    items = ranking.build_youtube_items(raw, prev_base=lambda sc: None,
                                        prev_delta=lambda sc: None, now=now)
    it = items[0]
    assert it["platform"] == "youtube"
    assert it["shortcode"] == "vid1"
    assert it["base_count"] == 24000 and it["views"] == 24000
    assert it["speed"] == 1000.0                       # 24000 / 24h
    assert round(it["density"], 4) == round(1224 / 24000, 4)   # (likes+comments)/views
    assert it["is_new"] is True and it["url"].endswith("vid1")
```

- [ ] **Step 2: Run test to verify it fails**

Run: `py -m pytest shopping_shorts/tests/test_ranking_youtube.py -q`
Expected: FAIL (build_youtube_items 없음)

- [ ] **Step 3: Write minimal implementation** (ranking.py 끝에 추가)

```python
def build_youtube_items(raw, prev_base, prev_delta, now=None, window_hours=48):
    """유튜브 원시 dict → 공통 item(조회수 기반 지표). 48h 이내만.

    prev_base(shortcode)->int|None: 직전 base_count(조회수). prev_delta 동일.
    speed=조회수/경과h, density=(좋아요+댓글)/조회수, accel=Δ−직전Δ.
    """
    now = now or datetime.now(timezone.utc)
    items = []
    for r in raw:
        ts = r.get("published_at")
        if not ts:
            continue
        age = hours_since(ts, now=now)
        if age > window_hours or age < 0:
            continue
        views = int(r.get("views") or 0)
        sc = r.get("video_id") or ""
        prev = prev_base(sc)
        is_new = prev is None
        delta = views if is_new else views - prev
        prev_d = prev_delta(sc)
        accel = None if prev_d is None else delta - prev_d
        likes = int(r.get("likes") or 0)
        comments = int(r.get("comments") or 0)
        items.append({
            "platform": "youtube",
            "shortcode": sc,
            "name": r.get("channel_title"),
            "username": r.get("channel_id"),
            "inpock": "",
            "followers": None,
            "thumbnail": r.get("thumbnail", ""),
            "video_url": "",                       # 인라인은 mix 다운로드 시 해석
            "url": f"https://www.youtube.com/watch?v={sc}",
            "comments": comments,
            "likes": likes,
            "views": views,
            "base_count": views,
            "age_hours": round(age, 1),
            "delta": delta,
            "is_new": is_new,
            "accel": accel,
            "speed": views / age if age > 0 else float(views),
            "density": (likes + comments) / views if views else 0.0,
            "category": "",
            "caption": r.get("title", ""),
        })
    return items
```

- [ ] **Step 4: Run test to verify it passes**

Run: `py -m pytest shopping_shorts/tests/test_ranking_youtube.py -q`
Expected: PASS

- [ ] **Step 5: Commit**

```bash
git add shopping_shorts/ranking.py shopping_shorts/tests/test_ranking_youtube.py
git commit -m "feat(shopping_shorts): ranking.build_youtube_items — 조회수 기반 지표"
```

---

### Task 3: store — platform_seeds + platform 스코프 이력

**Files:**
- Modify: `shopping_shorts/store.py`
- Test: `shopping_shorts/tests/test_store_seeds.py`

**Interfaces:**
- Produces:
  - `add_seed(platform: str, kind: str, value: str)` / `remove_seed(platform, value)` / `list_seeds(platform) -> list[dict{kind,value}]`
  - `save_run_platform(platform, run_date, rows)` / `prev_base_platform(platform, shortcode)->int|None` / `prev_delta_platform(platform, shortcode)->int|None`
  - (인스타 기존 `save_run`/`prev_comments`는 무변경 — platform='instagram' 기본으로 간주)

- [ ] **Step 1: Write the failing test**

```python
# tests/test_store_seeds.py
from shopping_shorts.store import Store


def test_seed_crud(tmp_path):
    st = Store(str(tmp_path / "t.db"))
    st.add_seed("youtube", "keyword", "살림꿀팁")
    st.add_seed("youtube", "keyword", "냉장고정리")
    st.add_seed("tiktok", "account", "@salim")
    yt = [s["value"] for s in st.list_seeds("youtube")]
    assert yt == ["살림꿀팁", "냉장고정리"]
    assert st.list_seeds("tiktok")[0]["value"] == "@salim"
    st.remove_seed("youtube", "살림꿀팁")
    assert [s["value"] for s in st.list_seeds("youtube")] == ["냉장고정리"]


def test_platform_scoped_snapshots(tmp_path):
    st = Store(str(tmp_path / "t.db"))
    st.save_run_platform("youtube", "2026-07-13 10:00",
                         [{"shortcode": "v1", "base": 100, "delta": 100}])
    st.save_run_platform("youtube", "2026-07-13 11:00",
                         [{"shortcode": "v1", "base": 300, "delta": 200}])
    assert st.prev_base_platform("youtube", "v1") == 300     # 최신
    assert st.prev_delta_platform("youtube", "v1") == 200
    assert st.prev_base_platform("youtube", "nope") is None
```

- [ ] **Step 2: Run test to verify it fails**

Run: `py -m pytest shopping_shorts/tests/test_store_seeds.py -q`
Expected: FAIL

- [ ] **Step 3: Write minimal implementation**

`_init_schema` 안, `settings` 테이블 생성 다음에 추가:

```python
            c.execute("""
                CREATE TABLE IF NOT EXISTS platform_seeds (
                    platform TEXT NOT NULL,
                    kind TEXT NOT NULL,
                    value TEXT NOT NULL,
                    added_at TEXT,
                    PRIMARY KEY (platform, value)
                )
            """)
            c.execute("""
                CREATE TABLE IF NOT EXISTS platform_snapshots (
                    id INTEGER PRIMARY KEY AUTOINCREMENT,
                    platform TEXT NOT NULL,
                    run_date TEXT NOT NULL,
                    shortcode TEXT NOT NULL,
                    base INTEGER,
                    delta INTEGER
                )
            """)
            c.execute("CREATE INDEX IF NOT EXISTS idx_psnap ON platform_snapshots(platform, shortcode, id)")
```

메서드(클래스 아무 곳, 예: `saved_set` 다음)에 추가:

```python
    # ── 플랫폼 발굴 시드(유튜브 키워드/채널, 틱톡 계정) ──
    def add_seed(self, platform, kind, value):
        with self._conn() as c:
            c.execute("INSERT OR IGNORE INTO platform_seeds(platform, kind, value, added_at) "
                      "VALUES(?,?,?, datetime('now'))", (platform, kind, value))

    def remove_seed(self, platform, value):
        with self._conn() as c:
            c.execute("DELETE FROM platform_seeds WHERE platform=? AND value=?", (platform, value))

    def list_seeds(self, platform):
        with self._conn() as c:
            rows = c.execute("SELECT kind, value FROM platform_seeds WHERE platform=? "
                             "ORDER BY added_at ASC, rowid ASC", (platform,)).fetchall()
        return [{"kind": r[0], "value": r[1]} for r in rows]

    # ── 플랫폼 스코프 스냅샷(가속 계산용) ──
    def save_run_platform(self, platform, run_date, rows):
        with self._conn() as c:
            c.executemany(
                "INSERT INTO platform_snapshots(platform, run_date, shortcode, base, delta) "
                "VALUES(?,?,?,?,?)",
                [(platform, run_date, r["shortcode"], r.get("base"), r.get("delta")) for r in rows])

    def prev_base_platform(self, platform, shortcode):
        with self._conn() as c:
            row = c.execute("SELECT base FROM platform_snapshots WHERE platform=? AND shortcode=? "
                            "ORDER BY id DESC LIMIT 1", (platform, shortcode)).fetchone()
        return row[0] if row else None

    def prev_delta_platform(self, platform, shortcode):
        with self._conn() as c:
            row = c.execute("SELECT delta FROM platform_snapshots WHERE platform=? AND shortcode=? "
                            "ORDER BY id DESC LIMIT 1", (platform, shortcode)).fetchone()
        return row[0] if row else None
```

- [ ] **Step 4: Run test to verify it passes**

Run: `py -m pytest shopping_shorts/tests/test_store_seeds.py -q`
Expected: PASS

- [ ] **Step 5: Commit**

```bash
git add shopping_shorts/store.py shopping_shorts/tests/test_store_seeds.py
git commit -m "feat(shopping_shorts): store platform_seeds + 플랫폼 스코프 스냅샷"
```

---

### Task 4: service.collect — 유튜브 분기

**Files:**
- Modify: `shopping_shorts/service.py`
- Test: `shopping_shorts/tests/test_collect_youtube.py`

**Interfaces:**
- Consumes: `youtube_client.search_shorts`, `ranking.build_youtube_items`, `apply_grades`, store seeds/snapshots
- Produces: `collect(platform="instagram", limit_channels=None)` — platform="youtube"면 유튜브 경로

- [ ] **Step 1: Write the failing test**

```python
# tests/test_collect_youtube.py
from shopping_shorts import service


def test_collect_youtube(monkeypatch, tmp_path):
    monkeypatch.setattr(service, "DB_PATH", str(tmp_path / "t.db"))
    from shopping_shorts.store import Store
    Store(str(tmp_path / "t.db")).add_seed("youtube", "keyword", "살림꿀팁")

    def fake_search(keywords, published_after_iso, **kw):
        assert keywords == ["살림꿀팁"]
        return [{"video_id": "v1", "channel_title": "살림TV", "channel_id": "c1",
                 "title": "t", "thumbnail": "x",
                 "published_at": "2026-07-12T00:00:00Z",
                 "views": 24000, "likes": 100, "comments": 10}]
    monkeypatch.setattr(service, "yt_search", fake_search)

    items = service.collect(platform="youtube")
    assert len(items) == 1
    assert items[0]["platform"] == "youtube" and items[0]["base_count"] == 24000
    assert "grade" in items[0] and "score" in items[0]
```

- [ ] **Step 2: Run test to verify it fails**

Run: `py -m pytest shopping_shorts/tests/test_collect_youtube.py -q`
Expected: FAIL

- [ ] **Step 3: Write minimal implementation**

service.py 상단 import에 추가:
```python
from shopping_shorts.youtube_client import search_shorts as yt_search
from shopping_shorts.ranking import build_youtube_items
from datetime import timedelta
```

`collect` 시그니처를 `def collect(platform="instagram", limit_channels=None):` 로 바꾸고, 함수 첫 줄에서 분기:
```python
    if platform == "youtube":
        return _collect_youtube()
```

파일에 함수 추가:
```python
def _collect_youtube():
    """유튜브 키워드 시드로 인기 Shorts 발굴 → 조회수 기반 랭킹."""
    store = Store(DB_PATH)
    keywords = [s["value"] for s in store.list_seeds("youtube") if s["kind"] == "keyword"]
    if not keywords:
        return []
    now = datetime.now(timezone.utc)
    after = (now - timedelta(hours=WINDOW_HOURS)).strftime("%Y-%m-%dT%H:%M:%SZ")
    raw = yt_search(keywords, after)
    items = build_youtube_items(
        raw,
        prev_base=lambda sc: store.prev_base_platform("youtube", sc),
        prev_delta=lambda sc: store.prev_delta_platform("youtube", sc),
        now=now, window_hours=WINDOW_HOURS,
    )
    apply_grades(items)
    run_date = now.strftime("%Y-%m-%d %H:%M")
    store.save_run_platform("youtube", run_date,
                            [{"shortcode": i["shortcode"], "base": i["base_count"], "delta": i["delta"]} for i in items])
    store.save_last_run_platform("youtube", items, now.isoformat())
    return items
```

store.py에 플랫폼별 last_run 저장/조회 추가(`last_run` 테이블에 platform 없으므로 settings 재사용):
```python
    def save_last_run_platform(self, platform, items, collected_at):
        import json
        with self._conn() as c:
            c.execute("INSERT OR REPLACE INTO settings(key, value) VALUES(?,?)",
                      (f"last_run::{platform}", json.dumps({"items": items, "collected_at": collected_at}, ensure_ascii=False)))

    def load_last_run_platform(self, platform):
        import json
        v = self.get_setting(f"last_run::{platform}", "")
        if not v:
            return [], None
        d = json.loads(v)
        return d.get("items", []), d.get("collected_at")
```

- [ ] **Step 4: Run test to verify it passes**

Run: `py -m pytest shopping_shorts/tests/test_collect_youtube.py -q`
Expected: PASS

- [ ] **Step 5: Commit**

```bash
git add shopping_shorts/service.py shopping_shorts/store.py shopping_shorts/tests/test_collect_youtube.py
git commit -m "feat(shopping_shorts): service.collect(platform=youtube) + 플랫폼 last_run"
```

---

### Task 5: media_download — URL로 yt-dlp/Apify 분기

**Files:**
- Create: `shopping_shorts/media_download.py`
- Test: `shopping_shorts/tests/test_media_download.py`

**Interfaces:**
- Consumes: `apify_client.fetch_single_reel`, yt-dlp(subprocess), `frame_extract.download_video`
- Produces: `download_any(url: str, dest_dir: str) -> str` (mp4 경로). instagram=Apify 해석 후 다운로드, youtube/tiktok=yt-dlp.

- [ ] **Step 1: Write the failing test**

```python
# tests/test_media_download.py
from shopping_shorts import media_download as md


def test_route_instagram(monkeypatch, tmp_path):
    called = {}
    monkeypatch.setattr(md, "_download_instagram", lambda url, d: called.setdefault("ig", url) or str(tmp_path / "ig.mp4"))
    monkeypatch.setattr(md, "_download_ytdlp", lambda url, d: (_ for _ in ()).throw(AssertionError("ytdlp 호출됨")))
    out = md.download_any("https://www.instagram.com/reel/ABC/", str(tmp_path))
    assert called["ig"].endswith("/reel/ABC/")


def test_route_youtube(monkeypatch, tmp_path):
    called = {}
    monkeypatch.setattr(md, "_download_ytdlp", lambda url, d: called.setdefault("yt", url) or str(tmp_path / "yt.mp4"))
    md.download_any("https://www.youtube.com/watch?v=vid1", str(tmp_path))
    assert "youtube.com" in called["yt"]


def test_route_tiktok(monkeypatch, tmp_path):
    called = {}
    monkeypatch.setattr(md, "_download_ytdlp", lambda url, d: called.setdefault("tt", url) or str(tmp_path / "tt.mp4"))
    md.download_any("https://www.tiktok.com/@u/video/123", str(tmp_path))
    assert "tiktok.com" in called["tt"]
```

- [ ] **Step 2: Run test to verify it fails**

Run: `py -m pytest shopping_shorts/tests/test_media_download.py -q`
Expected: FAIL

- [ ] **Step 3: Write minimal implementation**

```python
# shopping_shorts/media_download.py
"""소스 URL을 플랫폼별로 다운로드 — instagram=Apify, youtube/tiktok=yt-dlp(무료)."""
import subprocess
import sys
import uuid
from pathlib import Path


def _download_instagram(url, dest_dir):
    from shopping_shorts.apify_client import fetch_single_reel
    from shopping_shorts.frame_extract import download_video
    raw = fetch_single_reel(url)
    if not raw or not raw.get("videoUrl"):
        raise RuntimeError(f"인스타 영상 해석 실패: {url}")
    return str(download_video(raw["videoUrl"], Path(dest_dir)))


def _download_ytdlp(url, dest_dir):
    out = str(Path(dest_dir) / (uuid.uuid4().hex[:8] + ".%(ext)s"))
    r = subprocess.run(
        [sys.executable, "-m", "yt_dlp", "-f", "mp4/bestvideo+bestaudio/best",
         "--no-playlist", "-o", out, url],
        capture_output=True, text=True, timeout=300)
    if r.returncode != 0:
        raise RuntimeError(f"yt-dlp 실패({url}): {r.stderr[-300:]}")
    files = sorted(Path(dest_dir).glob(Path(out).stem.split('.')[0] + "*"))
    if not files:
        raise RuntimeError(f"yt-dlp 산출물 없음: {url}")
    return str(files[0])


def download_any(url, dest_dir):
    u = (url or "").lower()
    if "instagram.com" in u:
        return _download_instagram(url, dest_dir)
    if "youtube.com" in u or "youtu.be" in u or "tiktok.com" in u:
        return _download_ytdlp(url, dest_dir)
    raise RuntimeError(f"지원하지 않는 URL: {url}")
```

- [ ] **Step 4: Run test to verify it passes**

Run: `py -m pytest shopping_shorts/tests/test_media_download.py -q`
Expected: PASS

- [ ] **Step 5: Commit**

```bash
git add shopping_shorts/media_download.py shopping_shorts/tests/test_media_download.py
git commit -m "feat(shopping_shorts): media_download — URL별 yt-dlp/Apify 분기"
```

---

### Task 6: mix_pipeline — 다운로드를 download_any로 교체

**Files:**
- Modify: `shopping_shorts/mix_pipeline.py:42-52` (소스 다운로드 루프)
- Test: `shopping_shorts/tests/test_mix_pipeline.py` (기존 파일에 케이스 추가)

**Interfaces:**
- Consumes: `media_download.download_any`
- Produces: (동작 변경) 믹스 소스가 instagram 외 youtube/tiktok URL도 허용

- [ ] **Step 1: Write the failing test**

```python
# tests/test_mix_pipeline.py 에 추가
def test_mix_accepts_youtube_url(monkeypatch, tmp_path):
    from shopping_shorts import mix_pipeline as mp
    got = {}
    monkeypatch.setattr(mp, "download_any", lambda url, d: got.setdefault("urls", []).append(url) or str(tmp_path / "x.mp4"))
    # _prepare_sources 만 단위 검증(전체 잡 아님): youtube URL도 통과해야 함
    urls = ["https://www.youtube.com/watch?v=a", "https://www.tiktok.com/@u/video/1"]
    paths = mp._prepare_sources(urls, tmp_path)
    assert len(paths) == 2 and got["urls"] == urls
```

- [ ] **Step 2: Run test to verify it fails**

Run: `py -m pytest shopping_shorts/tests/test_mix_pipeline.py::test_mix_accepts_youtube_url -q`
Expected: FAIL (`_prepare_sources`/`download_any` 없음)

- [ ] **Step 3: Write minimal implementation**

mix_pipeline.py 상단 import에 추가: `from shopping_shorts.media_download import download_any`

기존 소스 다운로드 루프(42~52줄, `if "instagram.com" not in url: raise ...` 블록)를 아래 순수함수 호출로 교체:

```python
def _prepare_sources(urls, work):
    """소스 URL들을 플랫폼 무관하게 다운로드 → {video_id: mp4경로}. 캡션은 별도."""
    from pathlib import Path
    video_paths = {}
    for i, url in enumerate(urls):
        vid = _source_video_id(i)
        d = Path(work) / vid
        d.mkdir(parents=True, exist_ok=True)
        video_paths[vid] = download_any(url, str(d))
    return video_paths
```

그리고 기존 루프 자리에서 `video_paths = _prepare_sources(job["urls"], work)` 로 호출(캡션은 유튜브/틱톡의 경우 title/description 사용 — instagram만 fetch_single_reel 캡션이 필요하면 download_any가 반환하도록 후속 확장, Phase1은 대본추출이 영상에서 재전사되므로 캡션 없이도 동작).

- [ ] **Step 4: Run test to verify it passes**

Run: `py -m pytest shopping_shorts/tests/test_mix_pipeline.py -q`
Expected: PASS (기존 케이스 포함 전부)

- [ ] **Step 5: Commit**

```bash
git add shopping_shorts/mix_pipeline.py shopping_shorts/tests/test_mix_pipeline.py
git commit -m "feat(shopping_shorts): 믹스 소스 다운로드를 플랫폼 무관 download_any로"
```

---

### Task 7: app.py — platform 파라미터 + 시드 API

**Files:**
- Modify: `shopping_shorts/app.py` (`/api/collect`, `/api/reference` + 시드 3종)
- Test: `shopping_shorts/tests/test_app_platform.py`

**Interfaces:**
- Consumes: `service.collect(platform)`, store seeds/last_run_platform
- Produces: `/api/collect?platform=`, `/api/reference?platform=`, `/api/seeds`(GET/POST/DELETE)

- [ ] **Step 1: Write the failing test**

```python
# tests/test_app_platform.py
from fastapi.testclient import TestClient
from shopping_shorts import app as appmod


def _client(tmp_path, monkeypatch):
    monkeypatch.setattr(appmod, "DB_PATH", str(tmp_path / "t.db"))
    return TestClient(appmod.app)


def test_seed_endpoints(tmp_path, monkeypatch):
    c = _client(tmp_path, monkeypatch)
    assert c.post("/api/seeds", json={"platform": "youtube", "kind": "keyword", "value": "살림꿀팁"}).json()["ok"]
    g = c.get("/api/seeds?platform=youtube").json()
    assert g["items"][0]["value"] == "살림꿀팁"
    c.request("DELETE", "/api/seeds", json={"platform": "youtube", "value": "살림꿀팁"})
    assert c.get("/api/seeds?platform=youtube").json()["items"] == []


def test_reference_platform(tmp_path, monkeypatch):
    c = _client(tmp_path, monkeypatch)
    r = c.get("/api/reference?platform=youtube").json()
    assert r["ok"] and r["items"] == []
```

- [ ] **Step 2: Run test to verify it fails**

Run: `py -m pytest shopping_shorts/tests/test_app_platform.py -q`
Expected: FAIL

- [ ] **Step 3: Write minimal implementation**

`api_collect`에 `platform: str = "instagram"` 파라미터 추가하고 `collect(limit_channels=limit)` → `collect(platform=platform, limit_channels=limit)`. instagram일 때만 기존 draft/save_last_run 유지, 그 외엔 items만 반환(유튜브는 _collect_youtube가 자체 저장).

`api_reference`에 `platform: str = "instagram"`; instagram은 기존 `load_last_run()`, 그 외 `Store(DB_PATH).load_last_run_platform(platform)`.

시드 엔드포인트 추가:
```python
@app.get("/api/seeds")
def api_seeds(platform: str = "youtube"):
    return {"ok": True, "items": Store(DB_PATH).list_seeds(platform)}


@app.post("/api/seeds")
def api_seeds_add(body: dict):
    p = (body.get("platform") or "").strip(); v = (body.get("value") or "").strip()
    if not p or not v:
        return JSONResponse(status_code=422, content={"ok": False, "error": "platform·value 필요"})
    Store(DB_PATH).add_seed(p, (body.get("kind") or "keyword").strip(), v)
    return {"ok": True}


@app.delete("/api/seeds")
def api_seeds_remove(body: dict):
    Store(DB_PATH).remove_seed((body.get("platform") or "").strip(), (body.get("value") or "").strip())
    return {"ok": True}
```

- [ ] **Step 4: Run test to verify it passes**

Run: `py -m pytest shopping_shorts/tests/test_app_platform.py -q`
Expected: PASS

- [ ] **Step 5: Commit**

```bash
git add shopping_shorts/app.py shopping_shorts/tests/test_app_platform.py
git commit -m "feat(shopping_shorts): /api collect·reference platform 파라미터 + 시드 API"
```

---

### Task 8: index.html — 플랫폼 탭 + 유튜브 키워드 시드 UI

**Files:**
- Modify: `shopping_shorts/static/index.html`

**Interfaces:**
- Consumes: `/api/collect?platform=`, `/api/reference?platform=`, `/api/seeds`
- Produces: (UI) 상단 플랫폼 탭, 유튜브 탭에서 키워드 추가/삭제 + "지금 수집"

- [ ] **Step 1: 플랫폼 탭 추가** — 랭킹 제목 아래에 탭 3개 삽입(인스타 기본 active):

```html
<div class="ctypes" id="platformTabs" style="margin-bottom:12px">
  <div class="ctype active" data-platform="instagram" onclick="switchPlatform('instagram',this)">📷 인스타</div>
  <div class="ctype" data-platform="youtube" onclick="switchPlatform('youtube',this)">▶️ 유튜브</div>
  <div class="ctype" data-platform="tiktok" onclick="switchPlatform('tiktok',this)">🎵 틱톡</div>
</div>
<div id="seedPanel" style="display:none;margin-bottom:12px">
  <input id="seedInput" type="text" placeholder="검색 키워드 (예: 살림꿀팁)" style="padding:8px;border-radius:6px;border:1px solid var(--line);background:#0d131c;color:var(--txt)">
  <button onclick="addSeed()">+ 키워드</button>
  <span id="seedList" style="margin-left:8px;font-size:12px;color:var(--sub)"></span>
</div>
```

- [ ] **Step 2: JS — 플랫폼 전환/수집/시드** (STATE에 `platform:'instagram'` 추가):

```javascript
let PLATFORM='instagram';
function switchPlatform(p, el){
  PLATFORM=p;
  document.querySelectorAll('#platformTabs .ctype').forEach(x=>x.classList.remove('active'));
  el.classList.add('active');
  document.getElementById('seedPanel').style.display = (p==='instagram')?'none':'block';
  if(p!=='instagram') loadSeeds();
  loadReference();          // 기존 초기 로드 함수 — platform 반영하도록 아래 수정
}
async function loadSeeds(){
  const d=await (await fetch('/api/seeds?platform='+PLATFORM)).json();
  document.getElementById('seedList').textContent = (d.items||[]).map(s=>s.value).join(' · ') || '키워드 없음';
}
async function addSeed(){
  const v=document.getElementById('seedInput').value.trim(); if(!v) return;
  await fetch('/api/seeds',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({platform:PLATFORM,kind:(PLATFORM==='tiktok'?'account':'keyword'),value:v})});
  document.getElementById('seedInput').value=''; loadSeeds();
}
```

기존 `loadReference()`/`doCollect()`의 fetch URL에 `?platform='+PLATFORM` (또는 `&platform=`)를 붙이도록 수정. 카드 렌더는 `i.base_count`/`i.views` 있으면 그대로 표시(속도/가속/참여율 라벨 동일).

- [ ] **Step 3: 로컬 확인** — `py -m uvicorn shopping_shorts.app:app --port 8850` 띄우고 브라우저로 탭 전환·유튜브 키워드 추가·"지금 수집" 눈 확인(YOUTUBE_API_KEY 있는 환경). 없으면 시드 저장·탭 전환까지만 확인.

- [ ] **Step 4: Commit**

```bash
git add shopping_shorts/static/index.html
git commit -m "feat(shopping_shorts): 랭킹 플랫폼 탭 + 유튜브 키워드 시드 UI"
```

---

### Task 9: 서버 배포 — yt-dlp 설치 + 반영

**Files:**
- Modify: `shopping_shorts/requirements.txt` (yt-dlp 추가)

- [ ] **Step 1: requirements에 yt-dlp 추가**

`shopping_shorts/requirements.txt` 끝에 `yt-dlp` 한 줄 추가.

- [ ] **Step 2: 전체 테스트**

Run: `py -m pytest shopping_shorts/tests/ -q`
Expected: 신규 테스트 포함 PASS (apify_client 기존 baseline 실패 6건은 무관 — 별도 이슈).

- [ ] **Step 3: Commit + push**

```bash
git add shopping_shorts/requirements.txt
git commit -m "chore(shopping_shorts): yt-dlp 의존성(유튜브/틱톡 무료 다운로드)"
git pull --rebase --autostash origin main && git push origin main
```

- [ ] **Step 4: 서버 배포**

```bash
ssh -i <KEY> ubuntu@3.39.179.148 '
cd /home/ubuntu/lotto-stock-wiki && git fetch origin main -q && git reset --hard origin/main
sudo python3 -m pip install --break-system-packages yt-dlp
sudo systemctl restart shopping-shorts && systemctl is-active shopping-shorts'
```

- [ ] **Step 5: 서버 스모크** — `/api/seeds?platform=youtube`가 200/401(로그인 보호), 서비스 active 확인. YOUTUBE_API_KEY가 `/etc/shopping-shorts.env`에 있는지 확인(없으면 추가 필요 — 사용자에게 안내).

---

## Self-Review

- **스펙 커버리지**: 어댑터(T1), 지표 일반화(T2), 시드·스냅샷(T3), collect 분기(T4), 믹스 다운로드(T5·T6), API(T7), 탭·발굴 UI(T8), yt-dlp 배포(T9) — 스펙 §3~8 커버. 틱톡(Phase 2)은 이 플랜 범위 밖(명시).
- **플레이스홀더**: 없음(코드 인라인). T6의 캡션 처리는 Phase1 범위에서 "영상 재전사로 대체" 명시.
- **타입 일관성**: `search_shorts`(T1)→`build_youtube_items`(T2)→`collect`(T4) dict 키 일치. `download_any`(T5)→`_prepare_sources`(T6) 일치. `add_seed/list_seeds`(T3)→API(T7)→UI(T8) 일치.
- **주의**: `service.collect` 시그니처 변경(platform 추가) → 기존 `app.api_collect` 호출부도 T7에서 함께 수정(누락 시 인스타 수집 깨짐).
