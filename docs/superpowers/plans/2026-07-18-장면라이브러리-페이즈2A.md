# 장면 라이브러리 페이즈2-A 구현 계획 — 매칭 · 컷어웨이 · 검수판

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 대본 비트마다 라이브러리 짤을 AI가 골라 나레이션 위에 컷어웨이로 자동 배치하고, 사장님이 검수판에서 확인·해제한 뒤 렌더한다.

**Architecture:** 매칭은 순수함수 `scene_match.match_scene_assets`로 `build_edit_plan` 직후 `run_mix_job`에 삽입해 비트에 `cutaway`를 주석 단다. 렌더는 `run_render`가 asset_id→경로를 해석해 `assemble`에 넘기고, `_render_mix`가 비트 클립 **안에서** 자산을 풀프레임 오버레이한다(비트 길이 불변 → 자막 t0 싱크 불변). 검수판은 저장된 plan을 읽어 표시하고 토글을 plan에 되쓴다.

**Tech Stack:** Python 3.14, FastAPI, ffmpeg(subprocess), pytest, google-genai(`edit_plan._vault_call` 캐스케이드), 순수 HTML/JS(produce.html).

## Global Constraints

- **클립 규격 통일**: 자산·비트 클립 모두 `720x1280 / libx264 / aac / 30fps / yuv420p`. concat이 `-c copy`(`video_assemble.py:372`)라 규격이 다르면 안 붙는다.
- **Gemini 키풀은 `edit_plan._vault_call` 캐스케이드만 사용** — `comment_gen.SHORTS_GEMINI_KEYS`(좁은 전용풀) 직접 사용 금지(소진 회피가 존재 이유).
- **비트 길이(`tts_dur`) 불변**: 컷어웨이는 비트 클립 안 오버레이. 비트 길이·TTS 오디오를 절대 안 건드린다 → 자막 t0 싱크 불변(설계 §4.1, 최대 리스크의 구조적 소거).
- **자산 오디오는 버린다**(비디오만 오버레이). 나레이션이 계속 흐름 = b-roll. → 오디오 `-ar`/`-ac` 선행작업은 2-A 비범위.
- **저신뢰 자동배치 금지**: 점수 < 임계는 `asset_suggestions`에 제안만.
- **표절 게이트**: 후보는 `asset_type=clip` 且 `source_origin ∈ {짜집기,촬영원본}`인 자산뿐(`모름`/NULL 제외).
- **ffmpeg subprocess는 `stdin=subprocess.DEVNULL`** — 없으면 윈도우에서 OSError WinError 6로 죽는다(pytest fd 캡처와 충돌).
- **파일 수정은 Edit 툴로**(Python write_text로 통째 재작성 금지 — CRLF 오염).
- **테스트 기준선**: 병합 판정은 "전부 green"이 아니라 "병합 전보다 실패가 늘지 않았나"(기준선 ~10건 이미 깨져 있음).

**설계 변경(스펙 §9 해소)**: 비트에는 `category`가 없다(beat = `beat_idx·role·narration·target_seconds·primary·alternates·effect·fit`). 따라서 매칭 1차 필터에서 **category 축을 뺀다**. 1차 필터 = `clip` + `source_origin`. 후보는 항상 여러 개이므로 "후보 1개 자동배치" 경계 문제도 소멸 — Gemini가 narration↔자산 태그로 항상 점수 매긴다.

---

## 자료 형태 (구현자 참고 — 읽기 전용)

**비트**(`edit_plan.build_edit_plan(...)["beats"][i]`):
```python
{"beat_idx": 0, "role": "훅", "narration": "이거 한 스푼이면...", "target_seconds": 2.5,
 "primary": {"video_id": 0, "seg_id": "s3", "start": 3.0, "end": 5.5},
 "alternates": [...], "effect": "cut", "fit": 0}
```

**자산**(`Store.list_scene_assets(customer_id, asset_type="clip")[i]`):
```python
{"id": 5, "asset_type": "clip", "render_mode": "cutaway", "media_path": ".../abc.mp4",
 "poster_path": ".../abc.jpg", "duration": 1.8, "keep_original_audio": 0,
 "title": "감자튀김", "scene_desc": "...", "role": "훅", "category": "레시피",
 "subject": "감자튀김", "tone": "...", "keywords": ["감자","튀김"],
 "source_kind": "wiki", "source_ref": "Da...", "source_start_frame": 54,
 "source_origin": "짜집기", "created_at": "..."}
```

**plan**(build_edit_plan 반환): `{"structure", "beats", "plagiarism_flags", "detected_type", "affiliate_target"}`.

---

## Task 1: `scene_match.match_scene_assets` — 순수 매칭

**Files:**
- Create: `shopping_shorts/scene_match.py`
- Test: `shopping_shorts/tests/test_scene_match.py`

**Interfaces:**
- Consumes: `edit_plan._vault_call(prompt, schema, max_tries=4) -> dict | None`(`edit_plan.py:238` — 캐스케이드 키풀).
- Produces: `match_scene_assets(plan: dict, assets: list[dict], *, threshold: float = 0.6, vault_call=None) -> dict` — plan을 **복사**해 각 비트에 `beat["cutaway"] = {"asset_id": int, "score": float}`(점수≥임계) 또는 없음, 그리고 `plan["asset_suggestions"] = [{"beat_idx","asset_id","score"}]`(임계 미만)를 붙여 반환. `vault_call`은 주입용(기본 `edit_plan._vault_call`) — 테스트가 스텁으로 대체.

- [ ] **Step 1: 실패 테스트 작성**

```python
# shopping_shorts/tests/test_scene_match.py
import copy
from shopping_shorts import scene_match


def _plan(*narrations):
    return {"structure": "template",
            "beats": [{"beat_idx": i, "role": "본문", "narration": n, "target_seconds": 2.0,
                       "primary": {"video_id": 0, "seg_id": f"s{i}", "start": 0.0, "end": 2.0},
                       "alternates": [], "effect": "cut", "fit": 0}
                      for i, n in enumerate(narrations)]}


def _asset(aid, subject, origin="짜집기", atype="clip", **kw):
    a = {"id": aid, "asset_type": atype, "source_origin": origin, "subject": subject,
         "scene_desc": subject, "keywords": [subject], "category": "레시피",
         "role": "본문", "duration": 1.5, "media_path": f"/x/{aid}.mp4"}
    a.update(kw)
    return a


def test_high_score_autoplaces_cutaway():
    plan = _plan("감자를 채 썬다")
    assets = [_asset(5, "채 썬 감자")]
    # 스텁: 비트0 → 자산5 점수 0.9
    fake = lambda prompt, schema, **k: {"best_asset_id": 5, "score": 0.9}
    out = scene_match.match_scene_assets(plan, assets, threshold=0.6, vault_call=fake)
    assert out["beats"][0]["cutaway"] == {"asset_id": 5, "score": 0.9}
    assert out.get("asset_suggestions", []) == []


def test_low_score_goes_to_suggestions_not_autoplace():
    plan = _plan("자전거를 탄다")
    assets = [_asset(7, "공유 자전거")]
    fake = lambda prompt, schema, **k: {"best_asset_id": 7, "score": 0.3}
    out = scene_match.match_scene_assets(plan, assets, threshold=0.6, vault_call=fake)
    assert "cutaway" not in out["beats"][0]
    assert out["asset_suggestions"] == [{"beat_idx": 0, "asset_id": 7, "score": 0.3}]


def test_first_filter_excludes_non_clip_and_unknown_origin():
    plan = _plan("무언가")
    assets = [_asset(1, "x", atype="sfx"), _asset(2, "y", origin="모름"),
              _asset(3, "z", origin=None)]
    calls = []
    fake = lambda prompt, schema, **k: calls.append(prompt) or {"best_asset_id": 0, "score": 0.0}
    out = scene_match.match_scene_assets(plan, assets, vault_call=fake)
    # 후보 0 → Gemini 호출조차 안 함, cutaway 없음
    assert calls == []
    assert "cutaway" not in out["beats"][0]


def test_vault_none_fails_quietly():
    plan = _plan("감자")
    assets = [_asset(5, "채 썬 감자")]
    out = scene_match.match_scene_assets(plan, assets, vault_call=lambda *a, **k: None)
    assert "cutaway" not in out["beats"][0]
    assert out["asset_suggestions"] == []


def test_does_not_mutate_input_plan():
    plan = _plan("감자")
    snap = copy.deepcopy(plan)
    scene_match.match_scene_assets(plan, [_asset(5, "채 썬 감자")],
                                   vault_call=lambda *a, **k: {"best_asset_id": 5, "score": 0.9})
    assert plan == snap  # 원본 불변(순수함수)


def test_unknown_best_asset_id_is_ignored():
    plan = _plan("감자")
    assets = [_asset(5, "채 썬 감자")]
    # 모델이 후보에 없는 id를 뱉으면 배치 안 함(방어)
    out = scene_match.match_scene_assets(plan, assets,
                                         vault_call=lambda *a, **k: {"best_asset_id": 999, "score": 0.9})
    assert "cutaway" not in out["beats"][0]
```

- [ ] **Step 2: 테스트 실패 확인**

Run: `python -m pytest shopping_shorts/tests/test_scene_match.py -q`
Expected: FAIL — `ModuleNotFoundError: No module named 'shopping_shorts.scene_match'`

- [ ] **Step 3: 최소 구현**

```python
# shopping_shorts/scene_match.py
"""대본 비트 ↔ 라이브러리 자산 매칭(순수함수).

1차 필터(무료·기계적): asset_type=clip 且 source_origin ∈ {짜집기,촬영원본}.
  (비트에 category가 없으므로 category 축은 안 쓴다 — 설계 §9 해소.)
2차 판정(Gemini _vault_call 캐스케이드): 비트 나레이션 + 후보 다축 태그 →
  {best_asset_id, score}. 점수 ≥ 임계 → 자동배치(cutaway), < 임계 → 제안만.

저신뢰 자동배치 금지: 오배치 손해가 비대칭(엉뚱한 짤이 라이브 영상에 박히는 것 >
짤 안 쓰는 것). 캐스케이드가 None(키 소진) → 조용한 실패(자동배치 없음, 예외 없음).
"""
import copy

from . import edit_plan

_ALLOWED_ORIGIN = ("짜집기", "촬영원본")

_MATCH_SCHEMA = {
    "type": "object",
    "properties": {
        "best_asset_id": {"type": "integer"},
        "score": {"type": "number"},
    },
    "required": ["best_asset_id", "score"],
}


def _candidates(assets):
    return [a for a in assets
            if a.get("asset_type") == "clip" and a.get("source_origin") in _ALLOWED_ORIGIN]


def _prompt(narration, cands):
    lines = [f"- id={a['id']}: 소재={a.get('subject')}, 장면={a.get('scene_desc')}, "
             f"키워드={','.join(a.get('keywords') or [])}" for a in cands]
    joined = "\n".join(lines)
    return (
        "다음 나레이션에 화면으로 곁들일 b-roll 짤을 후보 중에서 고른다.\n"
        f"나레이션: \"{narration}\"\n"
        f"후보:\n{joined}\n"
        "나레이션이 말하는 사물·동작과 가장 잘 맞는 후보의 id와 적합도(0~1)를 반환하라. "
        "맞는 게 없으면 score를 낮게. 보이는 것만 근거로, 억지로 고르지 마라."
    )


def match_scene_assets(plan, assets, *, threshold=0.6, vault_call=None):
    vault_call = vault_call or edit_plan._vault_call
    plan = copy.deepcopy(plan)
    cands = _candidates(assets)
    by_id = {a["id"]: a for a in cands}
    suggestions = []
    if cands:
        for beat in plan["beats"]:
            res = vault_call(_prompt(beat["narration"], cands), _MATCH_SCHEMA)
            if not res:
                continue  # 키 소진 등 → 조용한 실패
            aid = res.get("best_asset_id")
            score = float(res.get("score") or 0.0)
            if aid not in by_id:
                continue  # 모델이 후보 밖 id를 뱉음 → 방어
            if score >= threshold:
                beat["cutaway"] = {"asset_id": aid, "score": score}
            else:
                suggestions.append({"beat_idx": beat["beat_idx"], "asset_id": aid, "score": score})
    plan["asset_suggestions"] = suggestions
    return plan
```

- [ ] **Step 4: 테스트 통과 확인**

Run: `python -m pytest shopping_shorts/tests/test_scene_match.py -q`
Expected: PASS (6 passed)

- [ ] **Step 5: 커밋**

```bash
git add shopping_shorts/scene_match.py shopping_shorts/tests/test_scene_match.py
git commit -m "feat(scene): match_scene_assets 순수 매칭 — 비트↔자산 다축 판정"
```

---

## Task 2: 컷어웨이 렌더 — `_render_mix` 오버레이 + `assemble` 배선

**Files:**
- Modify: `shopping_shorts/video_assemble.py` (`assemble` 시그니처, `_render_mix` 비트 루프)
- Test: `shopping_shorts/tests/test_cutaway_render.py`

**Interfaces:**
- Consumes: 기존 `_render_mix(edit_plan, tts_paths, source_video_paths, work)`, `_run_ffmpeg`, `_probe_duration`, `_pick_segment`, `_kenburns_vf`/`_base_zoom_vf`.
- Produces: `assemble(..., cutaway_paths=None)` 와 `_render_mix(..., cutaway_paths=None)` — `cutaway_paths: dict[int, str]`(beat_idx → 자산 media 절대경로). 해당 비트는 자산을 풀프레임 오버레이(창=[0, min(자산길이, tts_dur)]), 비트 길이·TTS 오디오 불변.

- [ ] **Step 1: 실패 테스트 작성 (실 ffmpeg)**

```python
# shopping_shorts/tests/test_cutaway_render.py
import subprocess
from pathlib import Path
import pytest
from shopping_shorts import video_assemble as va


def _mk_video(path, color, dur, size="720x1280"):
    subprocess.run(["ffmpeg", "-y", "-v", "error", "-f", "lavfi",
                    "-i", f"color=c={color}:s={size}:r=30:d={dur}",
                    "-c:v", "libx264", "-pix_fmt", "yuv420p", str(path)],
                   check=True, capture_output=True, stdin=subprocess.DEVNULL)


def _mk_audio(path, dur):
    subprocess.run(["ffmpeg", "-y", "-v", "error", "-f", "lavfi",
                    "-i", f"sine=frequency=440:duration={dur}",
                    "-c:a", "aac", str(path)], check=True, capture_output=True,
                   stdin=subprocess.DEVNULL)


def _frame_rgb(video, t):
    """t초 프레임의 평균 RGB(대략) — 색으로 오버레이 여부 판정."""
    import re
    out = subprocess.run(["ffmpeg", "-v", "error", "-ss", str(t), "-i", str(video),
                          "-frames:v", "1", "-vf", "scale=1:1", "-f", "rawvideo",
                          "-pix_fmt", "rgb24", "-"], capture_output=True,
                         stdin=subprocess.DEVNULL).stdout
    return tuple(out[:3]) if len(out) >= 3 else (0, 0, 0)


def _plan_one_beat(tts_dur):
    return {"structure": "t", "beats": [
        {"beat_idx": 0, "role": "본문", "narration": "x", "target_seconds": tts_dur,
         "primary": {"video_id": 0, "seg_id": "s0", "start": 0.0, "end": tts_dur},
         "alternates": [], "effect": "cut", "fit": 0}]}


def test_cutaway_keeps_beat_length_and_overlays(tmp_path):
    src = tmp_path / "src.mp4"; _mk_video(src, "red", 4)         # primary=빨강
    asset = tmp_path / "asset.mp4"; _mk_video(asset, "green", 1.5)  # 자산=초록
    tts = tmp_path / "t0.mp3"; _mk_audio(tts, 2.5)
    work = tmp_path / "w"; work.mkdir()

    mix = va._render_mix(_plan_one_beat(2.5), {0: str(tts)}, {0: str(src)}, work,
                         cutaway_paths={0: str(asset)})

    # ① 비트 길이 불변 = TTS 길이(2.5초) — 자막 싱크 자물쇠
    dur = va._probe_duration(mix)
    assert abs(dur - 2.5) < 0.15
    # ② 창 안(t=0.5)은 자산(초록), 창 밖(t=2.0, 자산 1.5초 종료 후)은 원본(빨강)
    r_in = _frame_rgb(mix, 0.5); r_out = _frame_rgb(mix, 2.0)
    assert r_in[1] > r_in[0] and r_in[1] > r_in[2]      # 초록 우세
    assert r_out[0] > r_out[1] and r_out[0] > r_out[2]  # 빨강 우세


def test_no_cutaway_path_is_unchanged(tmp_path):
    src = tmp_path / "src.mp4"; _mk_video(src, "red", 4)
    tts = tmp_path / "t0.mp3"; _mk_audio(tts, 2.5)
    work = tmp_path / "w"; work.mkdir()
    mix = va._render_mix(_plan_one_beat(2.5), {0: str(tts)}, {0: str(src)}, work,
                         cutaway_paths=None)
    assert abs(va._probe_duration(mix) - 2.5) < 0.15
    assert _frame_rgb(mix, 0.5)[0] > _frame_rgb(mix, 0.5)[1]  # 전부 빨강(오버레이 없음)
```

- [ ] **Step 2: 테스트 실패 확인**

Run: `python -m pytest shopping_shorts/tests/test_cutaway_render.py -q`
Expected: FAIL — `TypeError: _render_mix() got an unexpected keyword argument 'cutaway_paths'`

- [ ] **Step 3: `_render_mix`에 컷어웨이 오버레이 추가**

`video_assemble.py`의 `_render_mix` 시그니처를 바꾸고(Edit 툴), 비트 루프에서 컷어웨이 분기를 넣는다.

시그니처(`def _render_mix(edit_plan, tts_paths, source_video_paths, work):`)를:
```python
def _render_mix(edit_plan, tts_paths, source_video_paths, work, cutaway_paths=None):
```
로 바꾼다. 비트 루프에서 기존 `cmd = [...]; _run_ffmpeg(cmd); beat_clips.append(clip)` 부분(대략 `video_assemble.py:354-364`)을 아래로 교체:

```python
        cutaway = (cutaway_paths or {}).get(idx)
        if cutaway:
            # 컷어웨이: 원본 위에 자산을 풀프레임 오버레이. 창=[0, min(자산길이, tts_dur)].
            # 비트 길이(tts_dur)·TTS 오디오는 그대로 → 자막 t0 싱크 불변(설계 §4.1).
            # 자산 오디오는 버린다(나레이션이 계속 흐름 = b-roll).
            asset_dur = _probe_duration(cutaway)
            win = min(asset_dur, tts_dur)
            fc = (
                f"[0:v]{vf}[base];"
                f"[2:v]scale=720:1280:force_original_aspect_ratio=increase,"
                f"crop=720:1280,setpts=PTS-STARTPTS[ov];"
                f"[base][ov]overlay=0:0:enable='between(t,0,{win:.3f})'[vout]"
            )
            cmd = [
                "ffmpeg", "-y",
                *loop, "-ss", f"{start:.3f}", "-i", str(src),
                "-i", str(tts),
                "-i", str(cutaway),
                "-filter_complex", fc, "-r", "30",
                "-map", "[vout]", "-map", "1:a:0",
                "-t", str(tts_dur),
                "-c:v", "libx264", "-c:a", "aac", "-pix_fmt", "yuv420p", str(clip),
            ]
        else:
            cmd = [
                "ffmpeg", "-y",
                *loop, "-ss", f"{start:.3f}", "-i", str(src),
                "-i", str(tts),
                "-vf", vf, "-r", "30",
                "-map", "0:v:0", "-map", "1:a:0",
                "-t", str(tts_dur),
                "-c:v", "libx264", "-c:a", "aac", "-pix_fmt", "yuv420p", str(clip),
            ]
        _run_ffmpeg(cmd)
        beat_clips.append(clip)
```

- [ ] **Step 4: `assemble`이 `cutaway_paths`를 `_render_mix`로 전달**

`assemble` 시그니처에 `cutaway_paths=None`을 더하고(맨 끝 인자), `_render_mix` 호출에 전달한다:
```python
def assemble(edit_plan, tts_paths, source_video_paths, out_path, clean_fn=None,
             headcopy=None, caption_style=None, deco=None, cutaway_paths=None):
```
그리고 `mix_raw = _render_mix(edit_plan, tts_paths, source_video_paths, work)` 를
`mix_raw = _render_mix(edit_plan, tts_paths, source_video_paths, work, cutaway_paths=cutaway_paths)` 로 바꾼다.

- [ ] **Step 5: 테스트 통과 확인**

Run: `python -m pytest shopping_shorts/tests/test_cutaway_render.py -q`
Expected: PASS (2 passed)

- [ ] **Step 6: 회귀 — 기존 video_assemble 테스트**

Run: `python -m pytest shopping_shorts/tests/ -k "assemble or render_mix or mix_render" -q`
Expected: 기준선과 동일(신규 실패 0). 실패가 늘면 시그니처 기본값(None) 누락 의심.

- [ ] **Step 7: 커밋**

```bash
git add shopping_shorts/video_assemble.py shopping_shorts/tests/test_cutaway_render.py
git commit -m "feat(scene): 컷어웨이 오버레이 렌더 — 비트 길이 불변(자막 싱크 자물쇠)"
```

---

## Task 3: 파이프라인 배선 — `run_mix_job` 매칭 삽입 + `run_render` 경로 해석

**Files:**
- Modify: `shopping_shorts/mix_pipeline.py` (`run_mix_job`, `run_render`)
- Test: `shopping_shorts/tests/test_mix_cutaway_wiring.py`

**Interfaces:**
- Consumes: `scene_match.match_scene_assets`(Task 1), `assemble(..., cutaway_paths=...)`(Task 2), `Store.list_scene_assets`, `Store.get_scene_asset`.
- Produces: 배선만 — 새 공개 API 없음. `run_mix_job`이 매칭된 plan을 저장, `run_render`가 `beat["cutaway"]["asset_id"]`→`media_path`를 해석해 `assemble`에 `cutaway_paths`로 전달.

- [ ] **Step 1: 실패 테스트 작성 (seam — 저장위치=읽기위치)**

```python
# shopping_shorts/tests/test_mix_cutaway_wiring.py
from shopping_shorts import mix_pipeline


def test_run_render_resolves_cutaway_asset_paths(monkeypatch, tmp_path):
    # plan의 beat[cutaway].asset_id가 assemble에 media_path로 넘어가는지(seam).
    plan = {"structure": "t", "beats": [
        {"beat_idx": 0, "narration": "x", "target_seconds": 2.0, "role": "본문",
         "primary": {"video_id": 0, "seg_id": "s0", "start": 0, "end": 2},
         "alternates": [], "effect": "cut", "fit": 0,
         "cutaway": {"asset_id": 5, "score": 0.9}}]}

    captured = {}
    def fake_assemble(edit_plan, tts_paths, source_video_paths, out_path, **kw):
        captured["cutaway_paths"] = kw.get("cutaway_paths")
        Path = __import__("pathlib").Path
        Path(out_path).write_bytes(b"x")
        return out_path
    monkeypatch.setattr(mix_pipeline, "assemble", fake_assemble)

    # get_scene_asset(5) → media_path 스텁
    class FakeStore:
        def __init__(self, *a, **k): pass
        def get_mix_job(self, jid): return {"edit_plan": plan, "status": "ready",
                                            "customer_id": 0, "voice": None, "video_type": "x"}
        def get_scene_asset(self, aid, customer_id=0): return {"id": aid, "media_path": f"/lib/{aid}.mp4"}
        def update_mix_job(self, *a, **k): pass
    monkeypatch.setattr(mix_pipeline, "Store", FakeStore)
    # tts/source 준비 스텁 — run_render이 이들을 만드는 경로를 우회(최소 배선 확인만)
    monkeypatch.setattr(mix_pipeline, "_prepare_render_inputs",
                        lambda *a, **k: ({0: "/tts/0.mp3"}, {0: "/src/0.mp4"}, plan), raising=False)

    mix_pipeline.run_render("job1", str(tmp_path / "db"), str(tmp_path))
    assert captured["cutaway_paths"] == {0: "/lib/5.mp4"}
```

> ⚠️ 구현자 주의: `run_render`의 실제 내부(입력 준비 방식)를 먼저 읽어라(`mix_pipeline.py:284-337`). 위 테스트의 `_prepare_render_inputs` 스텁 지점은 **현재 코드에 맞춰 조정**한다 — 핵심 단언은 "`beat["cutaway"]["asset_id"]=5`가 `cutaway_paths={0:"/lib/5.mp4"}`로 assemble에 도달"이다. run_render이 tts/source를 인라인으로 만들면, 그 부분을 monkeypatch로 잘라내고 asset 해석+assemble 호출만 남겨 단언한다.

- [ ] **Step 2: 테스트 실패 확인**

Run: `python -m pytest shopping_shorts/tests/test_mix_cutaway_wiring.py -q`
Expected: FAIL — assemble이 `cutaway_paths`를 안 받거나 None.

- [ ] **Step 3: `run_render`에 asset 경로 해석 추가**

`run_render`(`mix_pipeline.py:284`)에서 `assemble(plan, tts_paths, source_video_paths, str(out_path), clean_fn=clean_fn, ...)` 호출 **직전**에 삽입(Edit 툴):
```python
        # 컷어웨이: 비트에 붙은 asset_id를 media_path로 해석해 assemble에 넘긴다.
        # 저장위치(match가 쓴 beat["cutaway"]) = 읽기위치(여기) — seam 일치.
        cutaway_paths = {}
        for beat in plan["beats"]:
            cut = beat.get("cutaway")
            if cut:
                asset = store.get_scene_asset(cut["asset_id"], customer_id=job.get("customer_id", 0))
                if asset and asset.get("media_path"):
                    cutaway_paths[beat["beat_idx"]] = asset["media_path"]
```
그리고 `assemble(...)` 호출에 `cutaway_paths=cutaway_paths` 인자를 추가한다.

- [ ] **Step 4: `run_mix_job`에 매칭 삽입**

`run_mix_job`(`mix_pipeline.py:162`)에서 `build_edit_plan(...)` 직후, `_synthesize_beats` 앞에 삽입:
```python
    from shopping_shorts.scene_match import match_scene_assets
    assets = store.list_scene_assets(customer_id=job.get("customer_id", 0), asset_type="clip")
    if assets:
        plan = match_scene_assets(plan, assets)
```
(job의 customer_id 접근 방식은 현재 코드에 맞춘다 — `job = store.get_mix_job(job_id)`가 이미 있으면 재사용.)

- [ ] **Step 5: 테스트 통과 확인 + 회귀**

Run: `python -m pytest shopping_shorts/tests/test_mix_cutaway_wiring.py shopping_shorts/tests/test_mix_pipeline.py -q`
Expected: 신규 테스트 PASS, 기존 mix_pipeline 실패는 기준선과 동일(신규 회귀 0).

- [ ] **Step 6: 커밋**

```bash
git add shopping_shorts/mix_pipeline.py shopping_shorts/tests/test_mix_cutaway_wiring.py
git commit -m "feat(scene): 파이프라인 배선 — run_mix_job 매칭 삽입 + run_render 경로 해석"
```

---

## Task 4: 검수판 — API + `produce.html`

**Files:**
- Modify: `shopping_shorts/app.py` (컷어웨이 토글 엔드포인트)
- Modify: `shopping_shorts/static/produce.html` (검수 단계 표시·토글)
- Test: `shopping_shorts/tests/test_scene_review_api.py`

**Interfaces:**
- Consumes: `Store.get_mix_job`/`update_mix_job`(plan 저장), `Store.get_scene_asset`.
- Produces: `POST /api/produce/mix/{job_id}/cutaway` body `{"beat_idx": int, "asset_id": int|null}` — 저장된 plan의 해당 비트 `cutaway`를 설정(asset_id) 또는 제거(null). 200 `{"ok": true}`. 소유권은 세션 cid로 격리.

> ⚠️ 병합 충돌 최소화: 다른 트랙(모션효과·싱크검수판)이 같은 `produce.html` 프리뷰를 동시 편집 중. **검수 단계에만 국한된 최소 블록**으로 추가하고, 공용 프리뷰 영역·기존 ID를 건드리지 않는다.

- [ ] **Step 1: 실패 테스트 작성 (API)**

```python
# shopping_shorts/tests/test_scene_review_api.py
import pytest
from fastapi.testclient import TestClient
from shopping_shorts import app as app_mod


@pytest.fixture
def client(tmp_path, monkeypatch):
    from shopping_shorts.store import Store
    db = tmp_path / "t.db"; Store(db)
    monkeypatch.setattr(app_mod, "DB_PATH", db)
    return TestClient(app_mod.app), Store(db)


def _job_with_beat(store):
    plan = {"structure": "t", "beats": [
        {"beat_idx": 0, "narration": "감자", "target_seconds": 2.0, "role": "본문",
         "primary": {"video_id": 0, "seg_id": "s0", "start": 0, "end": 2},
         "alternates": [], "effect": "cut", "fit": 0}]}
    jid = store.create_mix_job(customer_id=0)          # 실제 시그니처에 맞춰 조정
    store.update_mix_job(jid, edit_plan=plan, status="ready_for_review")
    return jid


def test_toggle_sets_and_clears_cutaway(client):
    c, store = client
    jid = _job_with_beat(store)
    # 설정
    r = c.post(f"/api/produce/mix/{jid}/cutaway", json={"beat_idx": 0, "asset_id": 5})
    assert r.status_code == 200 and r.json()["ok"] is True
    assert store.get_mix_job(jid)["edit_plan"]["beats"][0]["cutaway"]["asset_id"] == 5
    # 제거
    r = c.post(f"/api/produce/mix/{jid}/cutaway", json={"beat_idx": 0, "asset_id": None})
    assert r.status_code == 200
    assert "cutaway" not in store.get_mix_job(jid)["edit_plan"]["beats"][0]


def test_toggle_bad_beat_idx_422(client):
    c, store = client
    jid = _job_with_beat(store)
    r = c.post(f"/api/produce/mix/{jid}/cutaway", json={"beat_idx": 99, "asset_id": 5})
    assert r.status_code == 422
```

> ⚠️ `create_mix_job`/`update_mix_job`/`get_mix_job`의 실제 시그니처를 `store.py`에서 먼저 확인해 테스트를 맞춘다(edit_plan 저장이 JSON 직렬화인지 등).

- [ ] **Step 2: 테스트 실패 확인**

Run: `python -m pytest shopping_shorts/tests/test_scene_review_api.py -q`
Expected: FAIL — 404(라우트 없음).

- [ ] **Step 3: 엔드포인트 구현**

`app.py`에 라우트 추가(기존 `/api/produce/mix/...` 근처):
```python
@app.post("/api/produce/mix/{job_id}/cutaway")
def api_mix_cutaway(job_id: str, request: Request, body: dict):
    """검수판에서 비트의 컷어웨이를 설정(asset_id) 또는 제거(null). plan에 되쓴다."""
    store = Store(DB_PATH)
    job = store.get_mix_job(job_id)
    if not job or job.get("customer_id", 0) != _cid(request):
        return JSONResponse(status_code=404, content={"ok": False, "error": "작업 없음"})
    plan = job.get("edit_plan") or {}
    beats = plan.get("beats") or []
    try:
        bi = int(body.get("beat_idx"))
    except (TypeError, ValueError):
        return JSONResponse(status_code=422, content={"ok": False, "error": "beat_idx 필요"})
    hit = next((b for b in beats if b.get("beat_idx") == bi), None)
    if hit is None:
        return JSONResponse(status_code=422, content={"ok": False, "error": "beat_idx 범위 밖"})
    aid = body.get("asset_id")
    if aid is None:
        hit.pop("cutaway", None)
    else:
        asset = store.get_scene_asset(int(aid), customer_id=_cid(request))
        if not asset:
            return JSONResponse(status_code=422, content={"ok": False, "error": "자산 없음"})
        hit["cutaway"] = {"asset_id": int(aid), "score": hit.get("cutaway", {}).get("score", 1.0)}
    store.update_mix_job(job_id, edit_plan=plan)
    return {"ok": True}
```

- [ ] **Step 4: 테스트 통과 확인**

Run: `python -m pytest shopping_shorts/tests/test_scene_review_api.py -q`
Expected: PASS (2 passed)

- [ ] **Step 5: `produce.html` 검수 UI (최소·독립 블록)**

검수 단계에서 각 비트 렌더 시, `beat.cutaway`가 있으면 자산 썸네일(`/api/scene/{asset_id}/poster`)+subject+score와 "이 짤 빼기" 버튼을, `plan.asset_suggestions`의 해당 beat 제안엔 "＋ 이 짤 넣기" 링크를 붙인다. 클릭 → 위 엔드포인트 호출 후 그 비트 행만 다시 그린다. 기존 프리뷰 영역·전역 ID는 건드리지 않는다(별도 컨테이너 클래스 `scene-cutaway`로 국한).

- [ ] **Step 6: 커밋**

```bash
git add shopping_shorts/app.py shopping_shorts/static/produce.html shopping_shorts/tests/test_scene_review_api.py
git commit -m "feat(scene): 검수판 — 컷어웨이 토글 API + produce.html 표시"
```

---

## Task 5: 실 재료 grounding (합성 fixture ≠ 완료)

**Files:** (코드 변경 없음 — 관측·기록)

- [ ] **Step 1**: 라이브 12개 자산 + 실 대본으로 매칭을 1회 돌려(로컬 서버 `uvicorn ... --port 8872` 또는 서버 실라우트), `beat["cutaway"]` 배정이 **눈으로 말이 되는지** 확인. subject↔narration 오배치 사례를 기록.
- [ ] **Step 2**: 컷어웨이가 실제로 얹힌 완성 mp4 1편을 렌더해, ffprobe로 **전체 길이가 매칭 전과 동일**(자막 싱크 불변)한지 + 육안으로 b-roll이 나레이션 구간에 뜨는지 확인.
- [ ] **Step 3**: 관측 결과를 `.superpowers/sdd/`(또는 handoff)에 기록. 오배치가 잦으면 임계값·프롬프트를 조정(코드가 아니라 값 튜닝 우선).

> **Gate**: 합성 테스트 통과는 완료가 아니다(트랙 교훈). 실 자산·실 대본으로 매칭 정확도와 싱크 불변을 관측해야 페이즈2-A 완료.

---

## Self-Review (계획 작성자 체크)

- **스펙 커버리지**: §3 매칭→Task1 / §4 컷어웨이→Task2 / §2 배선→Task3 / §5 검수판→Task4 / §7·§9 실검증→Task5. 갭 없음.
- **§9 미확정 해소**: 비트에 category 없음 확인 → 1차 필터에서 category 축 제거(Global Constraints에 명시), "후보 1개" 경계 소멸.
- **타입 일관성**: `match_scene_assets(plan, assets, *, threshold, vault_call)` → `beat["cutaway"]={asset_id,score}` / `plan["asset_suggestions"]=[{beat_idx,asset_id,score}]`. `_render_mix(...,cutaway_paths)` / `assemble(...,cutaway_paths)` = `dict[beat_idx→media_path]`. 토글 API도 같은 `beat["cutaway"]` 키. 일관.
- **플레이스홀더**: 없음(실 코드·실 명령). Task3/4의 "현재 코드에 맞춰 조정" 주의는 배선 지점 확인 지시이지 플레이스홀더가 아님 — 단언 대상은 명시됨.
