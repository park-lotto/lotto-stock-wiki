# 컷편집경합 — 작업 로그

## 2026-09-02 (회사PC) — 3단계 컷 편집이 완성본에 안 들어가던 것
사장님 제보 "구간편집 해제 후 1초 단위 컷 조정 → 완성본 미리보기 → 적용 안 됨".

라이브에서 요청 순서를 재서 원인 확정: **저장보다 렌더가 먼저 출발**했다.
  0ms 렌더시작(preview) / 901ms 편집저장(apply) — autoApply가 1.2초 디바운스라
  그 안에 [완성본 만들기]를 누르면 서버는 옛 편성으로 렌더를 시작한다.

★데이터·배선은 멀쩡했다: 화면 ✋1.0+3.5, 서버 fixed_lens 저장됨,
천천히 렌더하면 실제 컷 경계도 ≈1.0초(프레임 실측). **타이밍만** 틀렸다
→ "될 때도 있고 안 될 때도" 있는 것처럼 보인 이유.

고침: _askRender가 flushApply()를 await 후 startPreview(). 디바운스는 유지하고
"지금 비우기"만 추가. 전송 중이면 재전송 예약까지 기다린다(applyServer가 busy면
즉시 return하므로 이 대기가 없으면 마지막 편집이 빠진다). 9단계 renderFinal에도 동일 적용.

라이브 검증: 수정 전 0ms렌더/901ms저장 → 수정 후 0ms저장/34ms렌더. fixed_lens 1.5 일치.
가드 6건(수정 전 4건 FAIL 확인). 커밋 7917fa60c · 0d513d0e1.

⚠️정적 코드로 오판할 뻔한 것: toggleStretch·toggleOne은 saveWork 호출이 안 보이지만
실측하면 render() 사슬에서 저장된다 — 눌러서 네트워크를 봐야 한다.
⚠️배포 경합: main이 약 10분마다 바뀌는데 게이트도 약 10분이라 finish가 계속 진다.
0 차이로 맞춘 직후 즉시 finish 할 것.
