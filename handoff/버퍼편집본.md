# 버퍼편집본 — 캡컷에서 고친 영상을 Buffer 예약으로 보내기

## 2026-09-22 구현 (트랙 커밋, **라이브 배포 전 — 사장님 승인 대기**)
사장님: "캡컷으로 보내서 작업한걸 버퍼에 다시 올릴수있게"

- `shopping_shorts/edited_video.py` — 저장·판정 한 곳. 작업폴더 `edited.mp4`+`edited.json`.
  ffprobe로 영상 확인(10분·500MB 상한) → faststart → 저장. 새 렌더 시 `mark_stale`(파일은 남김: 이미 예약된 게시물 보호).
- API: `GET/POST/DELETE /api/mix/edited/{job}`, 미리보기 `/api/mix/edited/{job}/video`, 공개 `/api/share/e/{sid}`(인증 예외).
- `/api/buffer/schedule`: 편집본 있으면 `/api/share/e/` 주소, `source:"final"`이면 원본.
- 8단계 Buffer 화면: 올리기·편집본/원본 고르기·바꾸기·지우기.
- `disk_cleanup._FINAL_VIDEO_FILES`에 edited.mp4(7일 보관, 완성본과 같음).

## 확인한 것(로컬 8861, 인증 끔)
- 가짜 mp4 거절 / 진짜 업로드 200 / faststart True / `/api/share/e` HEAD·GET 200, 받은 파일 9.0초
- 예약 분기: edited→share/e, final→share/v, stale→share/v (★Buffer 호출은 가짜로 대체해서 잰 것)
- 브라우저(Playwright): 올리기·고르기·지우기 동작, pageerror 0, 화면 캡처 확인
- pytest 청소·공유·버퍼 31 passed

## ⏭ 남은 것
- 라이브 배포(승인 후 `py tools/track.py finish 버퍼편집본`) → 라이브에서 실제 캡컷 mp4 올리고
  25일 뒤 예약 → Buffer에 편집본이 붙었는지 확인 → deletePost
