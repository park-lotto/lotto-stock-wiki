# 제미니과금 — handoff

**2026-08-16 (회사PC)** · 트랙 `제미니과금` / 폴더 `.tracks/제미니과금`

## 목적 (사장님 원문)

> "사용자들도 직접 버텍스를 등록하게끔할꺼고 실제 돌려봐야하니까
>  얼마나 제작소에서도 소모되는지 봐야해. 통계내볼꺼야"

= 크레딧 아끼는 문제가 **아니다**. 사용자가 자기 Vertex 계정을 등록해 자기
크레딧으로 제작소를 돌리게 하는 **제품 기능**이고, 그러려면 "영상 1편에 얼마"를
실측으로 알아야 한다.

**목적 확정(사장님 답변)**: 비용 전가 + 한도 회피 **둘 다**.
→ 사용자 키 우선, 없으면 사장님 키 폴백.
**통계 단위**: 영상 1편당 / 기능별 / 사용자별 누적 / 일자별 추이 — 4종 전부.

---

## ✅ 완료 (커밋 `ae8304326`)

### 1. 계측 — `shopping_shorts/usage_meter.py` (신규)

**왜 래퍼인가**: `generate_content(` 호출부가 **34곳**(실측)이라 거기마다 찍으면
34벌이 되고 새 호출부마다 반드시 빠뜨린다(CLAUDE.md 0순위-B). 클라이언트를 만드는
곳은 **6곳뿐**이라 거기서 감싸면 34곳이 자동으로 잡힌다.

- `MeteredClient` — `.models.generate_content`만 가로채 `usage_metadata`를 기록.
  `files`·`count_tokens` 등은 `__getattr__`로 그대로 통과.
- `usage_meter.track(op=, job_id=, customer_id=)` — 스레드로컬 문맥.
  호출부를 안 고치고 "누가 부른 콜인지" 붙이려는 장치. **중첩되면 안쪽이 이긴다.**
- `gemini_usage` 테이블 — ts/day/op/job_id/customer_id/model/in·out_tokens/krw/auth
- 단가는 `_PRICING` **한 곳**에만 있다. 구글이 바꾸면 여기만 고쳐라.
- `USAGE_METER=0` 환경변수로 끌 수 있다.
- ⚠️ 계측 실패는 전부 삼키고 본작업을 진행한다 — 단 `log.warning`으로 남긴다
  (침묵 except가 SQL 오류를 먹어 라이브 0건이 된 2026-08-10 사고 재발 방지).

**배선된 6곳**: `comment_gen.py:146` · `similarity.py:31` · `video_analysis.py:99`
· `longform_shorts.py:170` · `key_vault.py:169`(주식위키 공용 — 방어적 import)

**문맥 심은 곳**: `mix_pipeline.run_mix_job`(영상 1편 = job_id 단위, 내부를
`_run_mix_job_inner`로 분리) · `archive_tagger.tag_one`(op="태깅")

### 2. 통계 — `tools/usage_report.py` (신규)

```
py tools/usage_report.py               # 4종 전부
py tools/usage_report.py --days 7      # 최근 7일
py tools/usage_report.py --op          # 기능별만
py tools/usage_report.py --db <경로>   # 서버 DB 직접
```

검증(가짜 데이터 50행): 4종 전부 정상 출력, "영상 1편 평균 → $300이면 N편" 환산 포함.

---

## 📌 실측 (2026-08-16, 서버 count_tokens)

| 항목 | 값 |
|---|---|
| 태깅 1건 입력 | **1,580 토큰** (n=5, 이미지 46KB~300KB 전부 동일) |
| 태깅 1건 비용 | **0.679원** (flash-lite, 출력 60토큰 가정) |
| 미태깅 잔량 | **약 33만 건** (channel_archive 175,708 + platform_snapshots 130,447 + 기타) |
| 33만 건 전부 | **약 22만원** |
| $300(44만원) | **약 64만 건** — 다 하고 절반 남음 |

★ **이미지 용량과 토큰이 무관하다** — 300KB도 46KB도 1,580토큰. 구글이 타일
단위로 세기 때문. **썸네일을 압축해도 비용이 안 준다.**

### ⚠️ 내가 틀렸던 것 (사장님 지적으로 정정 — 같은 실수 반복 금지)

| 내가 말한 것 | 사실 |
|---|---|
| "썸네일 만료라 태깅 불가" | **틀림.** `/api/thumb`가 디스크에 영구 저장(`app.py:4195`), 70,519장/6.1GB 이미 있음. 내가 잰 건 CDN 직결이라 403이었을 뿐 |
| "프록시 결제가 선행조건" | **틀림.** 25GB 결제 완료, 이미 한 바퀴 크롤 끝 |
| "재크롤 필요" | **틀림.** 무료 다운로드로 충분. 크론이 자동 갱신(4일 지난 채널). 재크롤해도 `vision_tags`는 shortcode가 PK라 **재태깅 비용 0** |

→ `handoff/히트작크롤.md` §3·§4를 **먼저 읽었으면** 안 틀렸다. 0순위-A2 위반.

### $300 크레딧 — 공식 확인 결과

- **AI Studio(API 키) 방식엔 못 쓴다.** 공식 문서 2곳이 명시적 배제:
  `ai.google.dev/gemini-api/docs/billing` ("can't be used towards the Gemini API
  or AI Studio") · `docs.cloud.google.com/free/docs/free-cloud-credits`
- 2026-03 이전 발급분은 경과조치로 됐다 — 그래서 "된다"는 커뮤니티 글이 돌아다닌다.
- **Vertex AI(액세스 토큰)로 가야 크레딧이 먹는다.** 90일 만료.
- 구글이 **모델별 RPD 표를 없앴다**. 유료는 건수가 아니라 **지출 한도**로 제어
  (Tier 1 = 10분당 $10). 즉 "무료 500건 vs 유료 N건" 비교는 성립하지 않는다.
- 단가: flash-lite $0.25/$1.50, 3.5-flash $0.50/$3.00 (입력/출력, 1M토큰)
- 이미지 258토큰(≤384px, 초과분은 768×768 타일당 258) · 영상 **초당 263토큰**

---

## ⏭ 다음 (우선순위 순)

1. **라이브에서 실데이터 수집** — 배포 후 제작소 몇 편·태거를 돌리고
   `py tools/usage_report.py --db <서버DB>`로 **실제** 영상 1편 단가를 확정한다.
   지금 숫자는 태깅만 실측이고 제작 파이프라인은 아직 미측정.
2. **Vertex 전환** — `_client_for_key()` 계열 6곳에 인증 분기 추가.
   `usage_meter.wrap(..., auth="vertex")`로 이미 구분 필드는 준비돼 있다.
3. **사용자 Vertex 등록 UI** — 계정별 서비스계정 JSON 저장 + 호출 시 분기.
   ⚠️ 서비스계정 JSON은 비밀정보다. 저장·전송 방식을 설계 단계에서 정해야 한다.
4. **대시보드 통계 화면** — 지금은 CLI뿐. `usage_report`의 쿼리를 그대로 쓰면 된다.

### 설계 미결 (사장님 결정 필요)
- 사용자 키가 없을 때: 제작 차단 vs 사장님 키로 폴백(무료 체험분 제공)
- 폴백 시 무료 한도를 얼마로 줄 것인가 (영상 N편? 금액 N원?)

---

## 함정 기록

- **트랙 폴더 한글경로**: Bash에서 `cd .tracks/제미니과금`이 깨진다(cp949).
  PowerShell 또는 절대경로를 써라.
- **CDN 직결로 썸네일 만료를 판정하지 마라** — 403이 나도 서버 캐시엔 있다.
  실제 태거는 `video_analysis.fetch_thumb_bytes`를 쓴다. 판정은 그 경로로.
- **`count_tokens`는 과금이 거의 0** — 실측할 때 `generate_content` 대신 쓰면
  돈 안 쓰고 토큰을 잰다.
