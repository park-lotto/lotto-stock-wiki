> **소유 트랙**: 유료게이트 — 이 파일은 이 트랙 세션만 수정한다.
> 트리거: "유료게이트 이어서" / "판매 게이트 이어서"

## 판매용 유료게이트 — 2026-07-19 (Task1~6 + 설계 재검토 4보강, ★라이브 병합됨)

**✅ main 병합됨 = 라이브 게이팅 작동 중.** Task1~6(스키마·OAuth·게이트·비용방어·화면·admin) +
설계 재검토 보강(아래)까지 전부 main. 서버에 DASH_PASS/GOOGLE_* 있어야 실제로 돈다(없으면 전원 full).

### 설계 (페이블+Opus 리뷰 반영)
`docs/superpowers/specs/2026-07-19-유료게이트-design.md` / 계획 `docs/superpowers/plans/2026-07-19-유료게이트.md`
- 3단계: **체험**(구글 로그인 후 N일=전기능) → **무료**(만료 후 레퍼런스 랭킹만) → **유료**(결제 시 사장님이 pro 승격).
- 통합판정 `access_level(cid)`: pro거나 체험중이면 full, 아니면 ranking_only. 사장님 cid0=영구 full+admin.
- 비용 방어(체험/다계정이 SerpApi·렌더 태우는 것): 계정별 일일 크레딧 + 전역 상한.

### ✅ 완료 (Task1~5, 전부 커밋·테스트 통과, 전체스위트 9실패=기준선·비기준선0)
- **T1**(f9732f3ca) customers에 plan/full_access_until/google_sub/email + settings·usage, `access_level`, `/api/me`. 가입시 체험 자동시작.
- **T2**(680cddcd9) deny-by-default 게이트: 무료는 `GET /api/reference·thumb·video·/`만, 나머지 402. `_FREE_EXACT_GET`/`_FREE_EXACT_ANY`/`_FREE_PREFIX`.
- **T3**(ea21937a2) 계정별 일일 크레딧(usage, `check_and_count`) 렌즈·렌더 배선 + 전역상한 텔레알림. **+Opus 리뷰 5수정**: `/api/grab` 등급확인(게이트 우회차단)·DASH_PASS fail-open 경고·기존고객 유예체험·게이트 메서드인지+prefix경계·중복 get_setting 제거.
- **T4**(3b415b7e7) `/admin`(cid0 전용) 고객목록+사용량+[pro승격]/[무료] + 설정폼(체험일수·크레딧·연락처). `/api/admin/customers·set_plan·settings`.
- **T5**(cb5d2e130) `sidebar.js`(6페이지 공유)에 게이팅: ranking_only면 유료메뉴 🔒+만료모달, 체험중이면 'D-N' 배너, window.fetch 402 공통모달. `/api/me`에 연락처.
- 테스트: test_paywall_access/gate/usage/admin 18개 통과. node 문법 통과.

- **T6**(adf65b7e6) 구글 OAuth: `/auth/google/login`·`/callback`(라이브러리없이 code→token→userinfo, state쿠키 CSRF), `Store.get_or_create_by_google`, 레퍼런스 스타일 로그인페이지(다크카드+구글버튼·운영자비번 접이식). `_AUTH_ON=DASH_PASS or GOOGLE_CLIENT_ID`. config에 GOOGLE_*.
- **T6 Opus 보안리뷰 반영**(9197eb753): 🔴CRITICAL=구글전용배포(DASH_PASS 빈값)서 admin/빈비번 백도어 → `/api/login`에 DASH_PASS 빈값 가드. + email_verified 신뢰배제·state try/except·username 절단제거·google_sub 부분유니크. 28 페이월 테스트 통과.

### ⚠️ 회귀 주의 (finish 전 필독)
- 전체 스위트 단독=**9실패(기준선: apify/edit_plan/mix_pipeline, 키·네트워크 없어 원래 깨짐)**.
- 전체 병렬 실행 시 `test_produce_*`·scene·pick_log 등 **node 서브프로세스 JS-슬라이스 테스트가 flaky**하게 무더기 실패(부하·타이밍, WinError6 계열). **단독 실행하면 92개 전부 통과** — 내 코드 무관. finish 게이트가 이 flaky로 막히면 해당 파일 단독 재실행으로 확인.

### ✅ 설계 재검토 4보강 (2026-07-19, 커밋 3bcff2f1e → main b1ae6d590, 페이블 애드버세리얼 리뷰 반영)
설계 §1(비용게이트 최우선)과 코드가 어긋난 지점을 코드 대조로 찾아 보강:
- **전역 상한 실차단**: `global_incr_and_alert`는 90% 알림만 하고 **안 막았다** → `_global_over_cap(op)`로
  lens·render·script 실행 전 429 실차단(다계정 SerpApi/렌더 남용 캡). [app.py]
- **script(대본추출) 크레딧 배선**: `check_and_count("script")` 호출이 어디에도 없어 **무제한**이었고
  /api/me·admin은 캡된 척 거짓표시 → `extract_script`·`produce/extract_from_url`·`lens/trace_url`
  **캐시미스 지점**에 배선(전역+계정). [app.py]
- **실패 크레딧 환불**: `Store.usage_decr`/`refund_daily_credit`. lens·script=try/finally ok-flag,
  render=`run_mix_job` except. `mix_jobs`에 **customer_id·render_charge_day** 컬럼 신설.
- **연락처 빈값 기동경고 + 만료모달 fallback**: 수동승격 결제 깔때기의 막다른길 방지. [app.py 기동훅, sidebar.js]
- **페이블 리뷰가 잡은 버그 수정**: (B)환불이 과금 안 한 호출자(produce 2단계·auto_run)까지 돌아 전역
  카운터를 갉던 것 → `render_charge_day` 있는 job만 그 날짜로 환불. (G1)mix/start 검증 전 과금 →
  검증 먼저. (G2)finally 환불 예외가 원래 502/500 가림 → 자체 예외 삼킴.
- 테스트 신규 8개(test_paywall_usage·test_app_mix). 전체 기준선 유지(병합 게이트 통과).

### ⚠️ 남은 설계 질문 (다음 세션 결정)
`/api/produce/...mix/start`(제작소 2단계)·`auto_run.py`(자동배치)는 render 파이프라인을 돌리면서
**render 크레딧을 안 건다**(과금 우회). 지금은 '오환불 방지'만 했다 — **얘네도 과금할지**는 설계 판단:
2단계는 앞에서 script를 이미 과금했을 수 있고, auto_run은 사장님 배치라 과금이 오히려 이상할 수 있다.

### ⏭ 다음
1. **★사장님 직접**: 구글 클라우드 콘솔 OAuth 클라이언트(웹) 발급 → redirect `https://shoppingshorts.duckdns.org/auth/google/callback` → CLIENT_ID/SECRET을 서버 `.env`(`GOOGLE_CLIENT_ID`·`GOOGLE_CLIENT_SECRET`).
2. **미배선(후속)**: 대본추출 크레딧 — 캐시미스 지점에만 카운트.
3. **시각 그라운딩**: 병합 후 라이브 ranking_only 계정으로 🔒·배너·만료모달·구글로그인 실측(로컬은 _AUTH_ON off라 재현 불가).
4. **런칭 결정 후 병합**: `py tools/track.py finish 유료게이트`. 병합 순간 라이브 게이팅 시작 → **DASH_PASS가 서버에 있어야** admin 접근 가능(구글전용이면 admin 진입수단 별도 필요).

### 주의
- 병합 전 **DASH_PASS가 서버에 설정돼 있어야** 게이트가 돈다(없으면 전원 full — 시작 경고 추가함).
- 트라이얼 기간·크레딧은 하드코딩 아니라 `/admin` 설정값(기본 trial_days=7, lens5/render2/script10). 사장님 조정 가능.
