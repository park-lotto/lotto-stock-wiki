# 제작소빈페이지 — 핸드오프

- 갱신: 2026-08-24 (회사PC) / 트랙: 제작소빈페이지 / main 병합됨 (`2defd4476`, merge `5804e9a3b`)

## 사장님 요청
"숏템제작소 버튼을 누르면 마지막에 담긴 제작소 페이지가 뜨는데, 그냥 새로운 작업하려니
빈 페이지로 띄워줘."

## ✅ 한 것

`shopping_shorts/static/sidebar.js` — NAV의 '🎬 숏템 제작소' 항목에 `go` 필드 신설.

```js
{ icon: "🎬", text: "숏템 제작소", href: "/produce", go: "/produce?new=1", free: true },
```

- **원인**: 메뉴가 쿼리 없는 `/produce`로 보냈다 → `_bootRestore`가
  `_bootAccountLatestOrLocal()`을 타서 로컬 WORK_ID(있으면 서버본까지 재복원)나
  계정 최신작업을 이어받는다. 2026-07-26 크로스기기 싱크의 **의도된 동작**이지
  버그가 아니다 — 다만 "새로 시작"에는 안 맞는다.
- **수정**: 클릭 목적지만 `?new=1`로. 이건 '+ 새 작업'이 이미 쓰던 검증된 경로다
  (`clearWork()` → 빈 작업 → URL에서 new 제거, cad0e7b0의 작업복제 방지 유지).

### ★href를 안 바꾼 이유 (여기서 실수하기 쉽다)
`href`에 `?new=1`을 박으면 안 된다. 실제로 해보고 실측했다:

| 무너지는 것 | 이유 |
|---|---|
| 제작소에서 메뉴 활성표시(`active`) 사라짐 | 판정식이 `it.href === path`인데 `path`는 **쿼리 없는 pathname** (sidebar.js:186) |
| `data-ss-href`가 유료게이트 화이트리스트와 어긋남 | 서버 `_FREE_EXACT_GET`은 쿼리 없는 경로 |

→ `href`는 매칭·게이트용(쿼리 없음), `go`는 이동용. 없으면 `href`로 폴백이라 다른 항목 무영향.

또 **active여도 `go`가 있으면 onclick을 살렸다** — 제작소를 보고 있을 때도 이 버튼으로
빈 작업에 가야 한다(종전엔 같은 경로라 onclick이 아예 안 붙었다).

## 안 건드린 것 (일부러)
- **랭킹의 '제작소로 보내기'** (`static/index.html:3531`) — 재료를 sessionStorage에 담고
  **쿼리 없는 `/produce`**로 가야 `_consumeProduceHandoff`가 집는다. `?new=1`을 붙이면
  `clearWork()`가 돌지만 `_consumeProduceHandoff`는 그대로 불리므로 재료는 살긴 한다.
  그래도 불필요한 변경이라 손대지 않았다.
- **기존 작업 접근로** — 사이드바 '내 작업'(`?work=<id>`)에 그대로 남는다. 유실 0.
  `clearWork()`는 로컬 `sessionStorage['produce_work']`만 지우고 서버 레코드는 안 건드린다.

## 검증 (실행 결과)
- node 하네스 실측 렌더 (`test_sidebar_works.py`의 `_run`):
  - `/produce`: `<div class="ss-item active" data-ss-href="/produce" data-ss-free="1" onclick="location.href='/produce?new=1'">`
  - `/`(랭킹): 같은 onclick, active 없음 — 정상
- **회귀 테스트 4개 신설** (`test_sidebar_works.py` 하단)
- **사보타주로 테스트가 진짜 잡는지 확인**: `go` 제거 → 2건 실패 / `href`에 `?new=1` → 4건 실패
- `pytest -k "sidebar or nav or produce"`: **352 passed, 8 skipped, 0 failed**
- 병합 확인: `git branch -r --contains 2defd4476` → `origin/main` 포함 (exit 0만 믿지 않았다)

## ⏭ 남은 것
- 사장님 라이브 육안 확인 — 3분 뒤 stockbrain 자동배포. **강력 새로고침(Ctrl+F5)** 필요할 수 있다
  (sidebar.js는 정적파일이라 브라우저 캐시가 옛것을 붙잡을 수 있음).
- (선택) 반대 요구가 나오면 — "이어서 하기"를 원할 땐 '내 작업' 목록의 맨 위를 누르면 된다.
  그게 불편하다 하시면 메뉴를 둘로 쪼개는 안(제작소 / 이어서 하기)을 검토.

---

## 2026-08-24 (2차) — 빈 영상풀에 재료 가지러 갈 버튼 두 개

사장님: "이게 여기 누르면 레퍼런스랭킹으로 갈지 영상즐겨찾기로 갈지 버튼 두개 만들어줘"
(1단계 '담은 영상'이 빈 상태 스크린샷과 함께)

### ✅ 한 것 — `shopping_shorts/static/produce.html` `#poolEmpty` (약 1100줄)

글 한 줄뿐이던 빈 상태에 버튼 2개. 재료가 오는 길이 실제로 이 둘뿐이고 양쪽 다
`sendToProduce()`가 붙어 있다.

| 버튼 | 이동 | 클래스 |
|---|---|---|
| 📊 레퍼런스 랭킹에서 찾기 | `/` | `.btn-next` (주 동선) |
| ⭐ 영상 즐겨찾기에서 담기 | `/collection` | `.btn-prev` |

- 공용 버튼 클래스라 **모바일 44/46px 터치타깃 규칙(line 627/762)을 그냥 탄다**.
- ★버튼은 반드시 `#poolEmpty` **안**에 둔다 — `renderPool()`이 토글하는 건
  `poolEmpty` 하나뿐이라(produce.html:5515) 밖으로 새면 재료를 담은 뒤에도 남는다.

### 검증 (브라우저 실측 — playwright, 정적 검토 아님)
- 1600px: 두 버튼 44px, 가로 나란히 / 콘솔 에러 0
- 390px: 세로로 쌓임, 46px, `body.scrollWidth == 390` (가로 넘침 없음)
- 클릭 → 실제로 `/`·`/collection`으로 이동
- `HANDOFF.push(...)` + `renderPool()` → `display:none`, 비우면 다시 `block`
- `pytest -k "produce or sidebar or nav or pool"`: **387 passed, 8 skipped, 0 failed**
- 라이브: 서버 디스크 MD5 = origin/main MD5 (`b37c678b…`) 일치

### ★테스트를 두 번 쓴 이유 (다음에 또 밟을 함정)
회귀 테스트 6개(`shopping_shorts/tests/test_pool_empty_cta.py`).
**첫 버전은 문자열 검색이라 버튼을 `poolEmpty` 밖으로 빼도 통과했다** — 무용지물.
사보타주로 잡아내고 `HTMLParser`로 부분트리를 잘라 "안에 있는지"까지 보게 고쳤다.
그 사보타주가 진짜 버그인 것도 브라우저로 확인(재료 담은 뒤에도 버튼 2개가 남음).

파서 함정 2개 (여기서 두 번 틀렸다):
1. **공태그(`input`·`img`)를 깊이에 세면** 부분트리가 영영 안 닫혀 버튼이 57개 잡힌다.
2. **끝을 음수 센티널로만 표시하면** `handle_starttag`이 그걸 몰라 계속 모은다 → `done` 플래그.

→ 교훈: 마크업 테스트를 문자열 `in`으로 쓰지 마라. **사보타주로 실패를 확인**하기 전엔
   그 테스트가 뭘 지키는지 모른다.

### ⏭ 남은 것
- 사장님 라이브 육안 확인. **Ctrl+F5** 필요할 수 있다(produce.html 캐시).
- 빈 상태 문구를 "쓸 영상을 담으러 가볼까요 — 담은 뒤 **제작소로 보내기**를 누르면
  여기에 쌓입니다."로 바꿨다. 마음에 안 드시면 한 줄만 고치면 된다.
