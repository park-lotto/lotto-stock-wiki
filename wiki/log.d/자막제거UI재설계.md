
## 2026-08-19 자막제거 "실패" 오표시 수리
- 사장님 "자막제거 왜 실패?" → **실패가 아니라 성공을 실패로 표시한 우리 버그**였다.
  25분 걸린 정상 작업(자막 3개 전부 제거 성공)이 10분 시점부터 빨간 실패 문구를 띄웠다.
- 원인: 판정이 `mix_jobs.updated_at`(10분)만 봤는데 워커는 `job_queue.heartbeat_at`만 찍는다
  → 10분 넘는 정상 작업은 무조건 실패 표시. `store.task_is_alive()` 신설해 하트비트 기준으로 판정.
- 곁들여: VMake 간헐 `10101` 자동 재시도(2회, 재과금 없음) + "N편 중 M편 완료" 진행 표시.
- VMake 잔액은 890 남음(소진 아님) — 실패 원인이 아니었다.
