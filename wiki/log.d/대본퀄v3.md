
## 2026-07-31
- 대본 프롬프트 대정리(8045→2436자) + '변화' 필드 신설 + 원본에서 가져오기 강제
- ★리라이트 믹스: 원본 문장 세트가 소스 단위, 대사만 갈아끼우고 화면은 코드가 원본 순서로 배정
- 라이브가 backbone_base=1이라 새 코드가 안 돌던 것 발견·수정, 말에 맞는 컷 우선 배치
- 추출 조용한 실패(커버리지<55%) 검출·재추출·화면 경고, 후킹 중복 차단

## 2026-08-01
- 소스가 통째로 사라지던 버그(after 태그를 끝세트로 오판) 수정 + 후보 개수 상한. 라이브 DB로
  하네스를 라이브 경로에 맞춰 재실측 — 소재 3종 × 후보 3개 전건 통과(순서위반0·중복0·상한3).

## 2026-08-01 (오후)
- 라이브 실측으로 결함 6건 잡아 배포: 소스 통째 누락(after 오판)·후보 4~6개·filler 대본 유출·
  세트 버리기→옮기기·마무리 조리 재방송·G3 슬롯 관측. 별건으로 장면 라이브러리 자동 컷어웨이가
  role만 보고 '감자튀김'을 아이스크림 영상에 덮던 것 발견 → 기본 OFF.
- 다음: 정보 과적재(비트당 클립 9개·문장 하나에 메시지 5개)가 길이 초과·첫 화면 어긋남까지
  만드는 한 뿌리 — 비트당 클립 수 상한 + 메시지 수 제한.
- ★0순위 추가: 선택↔저장 불일치 확정(job 7c2a51467c65 — 렌더 자막은 B안인데 edit_plan은 A안).
  선택은 렌더엔 가고 계획서엔 안 남는다. 사후 분석이 계속 어긋나는 뿌리.
- ★확정: 후보 3개가 화면을 공유한다(A/B 클립 14개 동일, 순서만 셔플) — "대본만 다르고 영상은 같다".
  선택↔저장 불일치는 미확정으로 낮춤(A안 렌더에선 일치했다).

## 2026-08-01 (밤, 집PC)
- ★두더지가 계속 나온 이유 규명: 슬롯 대공사는 옳았고, 그 위의 낡은 층 5개(백본 화면
  후처리)가 일하고 버려지며 슬롯을 깨뜨리고 있었다 → 슬롯 경로에서 스킵(G2). 반사실
  대조로 "스킵해도 최종 화면 100% 동일"을 소재 5종×후보 16개로 증명.
- 과적재: 훅이 2.3초 말하는데 화면 7초 붙던 것을 대사 길이만큼만 붙이게 수정.
- ⚠️실측이 진단을 뒤집었다 — 길이초과의 본체는 클립 과다가 아니라 **대사가 김**
  (result 96자=12.8초 필요 vs 화면 6.1초). 클립 상한 3개를 걸었다면 프리즈가 돌아왔다.
- 배포 f08c60942. 다음=사장님 육안 확인 → 대사 길이 제한 → 후보 화면 차별화.

## 2026-08-02
- v3 동결(태그 v3-final, origin 푸시) → v4 시작점 확보. 되돌리기: git checkout v3-final -- shopping_shorts/
- 짐벌 job 실측으로 "후보 3개가 같은 이유" 확정: 슬롯이 이야기 뼈대를 하나만 만들고
  대사 3벌이 그 위에 얹힌다. 대본은 원본 순서·요점을 따라가며 말투만 바꾸므로(설계 의도)
  뼈대가 같으면 후보도 같다. 훅만 창작이라 훅만 다르다.
- v4 = 슬롯을 3벌 뽑기. 재료 확인됨(세트 7개 중 4개 사용, 남은 3개에 다른 이야기가 통째로 있음).

## 2026-08-02 (오후) — v4 라이브
- 슬롯을 후보 수만큼 뽑는다. 벌0은 v3 그대로(프롬프트 무변경, 코드경로 동일 확인),
  벌1·2는 Gemini에게 "남은 세트로 다른 이야기"를 별도 호출로 받는다.
- 라이브 실측(짐벌 재실행): 후보 간 화면 겹침 0~2칸(v3는 3~4칸), 소스 패턴이 갈렸다.
- 맥락 개선: 코드 조합은 흐름을 못 봐서 3연속 몰림·짧은 길이가 났다 → 모델이 조합하되
  시간순·자리·커버리지·길이는 코드가 재검증. 3연속 AAAB→ABB, 15.5초→26~29초.
- 배포 4b0e6e88c(v4) → 34ebfedd3(맥락 개선). 롤백 기준점 태그 v3-final.

## 2026-08-02 (밤) — v5 라이브
- v4 실측에서 AAAAAABBBBBBBB(소스 뭉침) 발견. 원인은 모델이 아니라 코드였다 —
  모델은 이미 A/B 교차를 주는데 _sort_sets_in_story_order가 (video_id,시각) 정렬로 뭉갰다.
- keep_order=True로 모델 자리는 두고 같은 소스끼리만 시간순 교환 → ABAB 교차 복원.
  불변식 소재 3종 전건 통과(처음). 배포 9b36930c0.
- 태그: v3-final(후보 동일) / v4-final(소스 뭉침) — 언제든 되돌린다.
- v5 2차(f880758d1): 두 컷씩 묶어 번갈기("3연속 금지"만 적으니 1개씩 번갈라는 뜻으로
  읽혀 잘게 쪼개졌다) + 첫 세트가 원본 맨 앞이 안 되게. 소재에 따라 첫 장면은 일부 겹침
  (억지로 바꾸면 훅이 어색해져 그 선은 안 넘었다).
- ★"두 컷 묶으면 설명이 끊기지 않나" 우려를 실측: 전환 지점 연결성 0.16 vs 연속 0.20,
  차이 0.04로 무해. 세트마다 대사를 새로 쓰므로 소스가 바뀌어도 흐름이 유지된다.

## 2026-08-03
- 대본 각색: 후보 3개가 "미국 목조주택 보수용 점토"를 그대로 반복하던 것 → 역할 부여
  ("숏폼 벤치마킹 전문가, 시청자가 원본을 봐도 못 알아채게 각색") + 앞 후보가 쓴 표현을
  다음 호출에 알려주기. 통째 복사 사라짐, 겹침 39→14~18. 배포 5f86d3975.
- ★제약을 얹는 대신 '무슨 일을 하는 건지'를 알려주는 방식(제약 과적재 회피).
- CTA 고정: 부품은행 CTA 버킷에 남의 채널 '프로필 👉 @아이디'가 섞여 우리에게 없는
  유입 경로를 안내하던 것 → 은행에서 버킷 제거 + 코드 교정. 9/9 댓글 유도 확인.
  ★교체를 생성 단계에 넣었다가 길이 재생성을 죽였다(테스트 4건) — 최종 후보 확정 뒤로 이동.
- 금지어 오탐: '완벽해요'가 '완벽함'에 걸려 심사위원 최고점 후보가 0점 반려되던 것 수정.
- v5 동결(태그 v5-final, 라이브 25cb48a94). 남은 것: 화면-대사 불일치 · fit 위주 채점.

## 2026-08-03 (사무실) 라이브 7건: 따로만들기 유실2·틱톡담기버튼·CTA중복제거·인스타캡션카드·CTA낚시훅·썸네일 이전영상배지·인스타 다운로드 이중화. 원칙 확정=대본자유·화면교차고정. 다음=각색 프롬프트 전후대조.

## 2026-08-05 — v6 어미·문장연결 말맛 교정 (예시 대량생산)
- 사장님 지적: 대본 어미가 `~더라고요/~돼요/~죠`만 돌려써 밋밋하고 문장이 뚝뚝 끊긴다.
- 원인 실측: 부품은행 ending 버킷에 `더라고요` 계열이 **표기만 다른 5종으로 203회** 과포화.
  + 비트당 21자라 한 비트=한 문장 → 끊김이 구조적으로 강제됨.
- 서버 mix_jobs에서 실제 대본 40개를 읽기전용으로 뽑아 3차까지 교정. 통과 26→24→**32/40**.
- 사장님 2차 피드백(장점묶음감탄·명사끊기CTA·화면지목·글쎄제거) 전부 3차에 반영 확인(실측).
- ★교훈: 금지어를 대안 예시로 같이 주면 예시가 이긴다(2차에서 `더라고요` 16→30 악화).
- 산출물: `docs/어미교정/` (README에 4차 할 일 3개 명시). 미완 — 회사 PC에서 이어감.

## 2026-08-05 (사무실) — v6 어미교정 4차 완료
- 3차 신규 결함 3개(CTA뒤 명사끊기·별표·보이시죠 쏠림)+길이 이탈 전부 해소, 전규칙 40/40.
- ★교훈: 고정 명사 목록 체커가 "이 스팀기!"를 놓쳐 가짜 40/40 — 일반 판정으로 교체, 육안 필수.
- 남은 것(승인 대기): A 프롬프트 few-shot 주입 / B 부품은행 더라고요 5종 통합(근본).

## 2026-08-05 (사무실, 저녁) — 채널 스타일 시스템 라이브
- 6채널 183개 분석 → 메종/채이/스탠다드 3각, trio 모드(후보별 상이 스타일)+리라이트 2차패스+추천 감점.
- 사고 3건 해결: 키래치 오탐 24/24, 리라이트 조용한 폴백(길이 게이트), 서버 IP 변경(43.200.48.69).
- (마감) 3안 스타일 반복실측 9/9 · durfill 워커선점→mix 9분 대기 사고 해소(선점 없음이 뿌리).
- [대본퀄v3] 2026-08-06 00:10(집PC): "스타일 안 갈림"의 원인은 스타일 설계가 아니라 **429**였다 — 후보 A/B/C가 전부 keys[0]으로 나가 3초 안에 같은 키 연타(키 18개가 놀고 있었음) → apply_restyle 3회 재시도 전부 튕겨 조용히 원본 폴백. _vault_call에 key_offset 추가 + 후보 루프 call 5곳 전부 교체 + 워커 PID 혼합. 인스타 세션 독점 가드(durfill·prewarm)도 claim_next의 같은 UPDATE 문 안에 추가 — 다중 워커에서 같은 계정 동시접속 플래그 방지. 신규 테스트 9건 통과, 전체 기준선 27→20(안 늘어남). 라이브 5a6bd409d.
- [대본퀄v3] 2026-08-06 02:10(집PC): 속도 최적화 4건 라이브. ①후보3개 병렬(라이브 실측 완료순 3→2→1 뒤집힘=병렬증거, 30~36초→18초) ②★추출캐시가 한번도 적중 안하던 것(shortcode로 저장하는데 "s0"으로 조회 → 408건이 통째로 놀고 매번 재전사, 118초 중 85초) ③배포가 워커에 영영 안닿던 것(배경작업이 끊이지 않아 무한연기 + 삭제된 구 유닛명 restart) ④ffmpeg 스레드 자동상한. "추출↔대본 동시병렬"은 진짜 의존이라 불가.
- [대본퀄v3] 2026-08-06 04:00(집PC): "대본 안 뽑힘" 원인 = ffmpeg 무한대기(4.1초 mp3에 18분, atempo+apad+loudnorm 3개 다 있을 때만, 3회 재현) → 전 호출에 timeout 30초. ★finish 게이트가 통째로 무력화돼 있던 것 발견: 미구현 모듈 import가 pytest 수집을 중단(rc=2)시켜 3300건이 0건 실행 → 그 상태로 병합 1건이 검증 없이 나갔다. importorskip + compare() 양방향 오판 수정(main 폴더 직접 커밋 b4f9acab4, 닭과달걀 해소). ⚠️pytest-xdist는 한글경로에서 rc=3 유발하니 설치 금지.
- [대본퀄v3] 2026-08-07(회사PC): C안=홈테리어픽 교체 라이브(684a1a6a6→main 85990fbd1). script_wiki 35편 중 조회수 상위 3편(271만·265만·178만) few-shot으로 _STYLES에 hometerior 추가, _TRIO 3번을 standard→hometerior(standard는 단일지정용으로 잔존). ★스타일만 바꿔 넣었으면 C안이 더 나빠졌다 — 홈테리어픽은 **끝문장 합쇼체가 서명**(35편중 34편, 다른 3채널은 0건)인데 style_penalty가 스타일을 몰라 시킨대로 쓴 C안을 0.06 감점 → 재현 결과 C 0.50→0.44 < A 0.48로 A가 추천되던 구조. style_name 인자로 허용편수까지 면제(미전달 시 종전 동일). 구도도 '지인 목격담'(standard 짝)인 채라 문제해결담 추가+_STYLE_FRAME 매칭. few-shot 4·5위는 같은 대본 재업로드라 제외. 검증: 스태시 대조로 23 failed=기준선 동일·passed 3377→3382. 롤백은 서버 env SCRIPT_TRIO=maison,chae,standard(배포 불필요).

- 2026-08-09: judge 변별력 — candidate_judge.rank() 비교채점(동점 금지)로 3후보 동점(0.467×3) 해소, 두 경로 배선+테스트 6건, 게이트 통과 main 병합
