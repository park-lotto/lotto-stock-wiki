# 대본UI2단계

2026-08-16: 제작소 2단계 "대본" 패널(data-step=8) 안쪽 구현 — 목업 이식 + 실배선. 씨앗 밴드(AI PICK 1원천),
스타일 카드(/api/script/styles), 역할이 보이는 결과 대본 행(beats), 게이트=고칠 곳 안내(실패 칸 빨간 테두리),
[바꾸기]=스타일 문장틀({빈칸} 강조). 확정은 STATE.script 세팅 후 3단계로 — 매칭은 안 건다(기획 §6).
실브라우저 실측(pass/fail·좌우모드·콘솔0) + pytest 258 passed. 남은 건 1단계 #aiPick 분리(장면라벨 세션과 조율)와
부분 재생성·빈칸 자동채움 API(채널전략분석 세션 몫).

2026-08-17: ★씨앗(대본 1편 지정) 폐지 — 사장님 방침과 어긋난 걸 발견해 되돌림. 진짜 원인은 app.py:2256 _src가
1편만 넘긴 것(그릇은 3편). 재료 4종 실배선: 표현사전 신설(spine.voice_json + tools/extract_style_voice.py,
라이브 스타일 3종 저장 완료·CTA 제외 필터 2회 보강), 담긴 영상 전부, job_id 전달(쿠팡 재료), 장면 태깅
(label·use_point·source_brief) 대본 주입. 라이브 실측: job_id 전송 확인·생성 267자 게이트 통과·말버릇 6개.
테스트 16건 신설. 남은 건 사장님 육안 판정과 1단계 #aiPick 분리(장면라벨 세션 조율).

2026-08-17(마감): 사장님 제보 4건 근본수정·라이브 — ①2개 골랐는데 1안만(서버가 카테고리로 재차 걸러 타소재를
조용히 버림, 화면 방침과 어긋남) ②AI에게 맡김 나열식(옛 생성기라 beats 없음→auto_style로 스타일 경로 태움)
③'무슨 영상인지 모르겠다'(응답에 materials 실어 쓴 재료·편별 첫문장 표시) ④'치아바타 뽑았는데 도마'(재료를
전부로 넓히며 주제 고정 안 한 내 실수→주제는 [대본 1]로 못 박음). 대본 3안 평가도 수행(근거 없는 주장·
스타일 미스매치 지적, 상품 미연결 지적은 실측 후 정정). 남은 건 쿠팡 재료가 대본에 실리는지 확인과 상태표시 UI.

2026-08-17(B안): [바꾸기]가 문장틀을 원문 그대로 꽂아 화면에 {가족} 중괄호가 박히던 것 —
버그가 아니라 "빈칸은 AI가 채운다"가 미구현이었다(⏭4번). 사장님 B안 선택으로 **빈칸 치환이 아니라
틀 구조만 빌려 그 칸을 재생성**: regen_one_beat + POST /api/script/beat/regen 신설, 재료 조립은
_materials_for_generate로 뽑아 전체 생성과 한 벌(0순위-B). [훅만 다시] 부분 재생성이 같은 물건이라
함께 완료. ★함정: _sanitize가 {가족}→(가족)으로 소독해 빈칸 신호가 죽는다(틀 줄만 예외) /
분량을 칸평균으로 주면 훅이 2~3문장으로 부푼다(그 칸 길이 기준으로) / 원문 그대로 반환은 실패로 침 /
어미 다르면(~뻔했어요 vs ~뻔했거든요) 틀판정 FAIL인데 그 어미가 스타일 말버릇이라 지킨 문장이 벌받았다
→ 꼬리어미 제거·서명어구 한군데 갈라짐 허용. 실측: 실Gemini 미끼 틀 4/4 통과(전 2/4), 게이트 회귀
20건(차단 11건 유지), 신규 테스트 18건, 전체 pytest 기준선과 실패 목록 동일(11건, 늘어난 실패 0).

2026-08-17(라이브 후 제보 2건): ①"네트워크 오류"=실은 서버 500(NameError _scene_block) — 재료조립을
_materials_for_generate로 뽑을 때 응답 만드는 줄이 그 변수를 아직 쓰고 있었다. ★내 검증이 못 잡은 이유가
교훈: 테스트·HTTP점검이 전부 422/404에서 끝나 **성공 경로의 마지막 줄에 한 번도 도달 안 함** → 스타일 경로를
200까지 끝까지 타는 테스트 4건 신설(버그 되살려 실패하는 것까지 확인). ②미끼 칸이 대본 전체를 삼킴(5줄+CTA)
— 재생성 프롬프트가 _MIX_PROMPT("30초 새 대본 … 0초 훅→끝 CTA")를 깔고 있어 '한 줄만'을 덧붙여도 앞 지시가
이겼다 → 재료 블록만 쓰는 전용 프롬프트로 교체 + **판정 2종**(길이 1.8배/+40자 초과, 비CTA칸의 '남겨주')로
되돌리기. 라이브 실측: 미끼 24~26자 한 문장·CTA침범 0. cta칸 오탐 없음 확인.
