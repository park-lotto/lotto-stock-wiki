# 네이버 클립 — 레퍼런스 랭킹 6번째 플랫폼

**2026-08-30 (사무실 PC) · 트랙 `네이버클립` · 라이브 반영 완료**

---

## 무엇을 왜 만들었나

핀터레스트 옆에 **🟩 네이버클립** 탭(관리자 전용)을 신설했다. 국내 숏폼이라 번역 없이
바로 참고한다. 사장님 목적은 **"뷰티에서 어떤 영상이 비슷하게 터지는지"** 보는 것.

수집 경로 — **무료·로그인/프록시/브라우저 전부 불필요, HTTP 2번이 전부**:

```
① 목록  s.search.naver.com/p/clip/4/api/tab/more
        ?query=<키워드>&ssc=tab.m_clip.all&start=N        ← 서명·쿠키 없이 200, 24건/페이지
② 상세  creatorhub-api.naver.com/api/v7.0/clipviewer/card
        ?seedMediaId=<32자 ID>&serviceType=CLIP&mediaType=VOD   ← 인증 없이 200
        → 정확한 조회수·좋아요·댓글·발행시각 + mp4 직링크(1080x1920까지)
```

---

## ⚠️ 다음 세션이 반드시 알아야 할 함정 5개

```
□ yt-dlp는 이 경로를 못 쓴다(Unsupported URL). yt-dlp가 아는 tv.naver.com/v/{clipNo}는
  옛 서비스고, 지금 클립은 clip.naver.com + 32자 mediaId 기반의 별개 서비스다.

□ 목록 API의 sort 파라미터는 넣어도 무시된다(rel/date/view/popular 전부 같은 순서
  = RELATED 고정). 인기순은 우리가 조회수로 정렬한다.

□ 파서는 썸네일 img를 앵커로 삼는다. data-media-id를 순서대로 세면 어긋난다 —
  실측 '뷰티' 1페이지: 속성 id 24개 중 2개는 vod 없는 껍데기라 상세가 통째로 빈다.

□ 1차 정렬은 목록의 반올림값('14.7만') 기준이라, 상세로 정확값(146,844)을 받은 뒤
  **다시 정렬**해야 한다. 안 하면 '인기순'이 거짓말이 된다.

□ mp4 URL에는 만료가 붙는다(hdnts=exp=...). 저장해 두고 나중에 받으면 실패한다 —
  받을 때 ②를 다시 부른다.
```

---

## 파일

| 파일 | 내용 |
|---|---|
| `shopping_shorts/naverclip_search.py` | **신규** 수집 모듈(목록·상세·MPD 파싱) |
| `shopping_shorts/kw_backends.py` | `naverclip_videos()` |
| `shopping_shorts/kw_search.py` | `_CHAIN`에 `"naverclip"` 등록 |
| `shopping_shorts/app.py` | `POST /api/naverclip/collect` + `_NAVERCLIP_BEAUTY_KWS` |
| `shopping_shorts/static/index.html` | 탭·설명·지표·수집패널·`ncCollect()` |
| `shopping_shorts/tests/test_kw_search.py` | 배선 검사에 naverclip 추가 |

**썸네일/영상 화이트리스트에 `pstatic.net`을 넣었다**(app.py `_ALLOWED_THUMB_HOSTS`·
`_ALLOWED_VIDEO_HOSTS`). 안 넣으면 카드가 전부 검게 뜬다(실측 400 8건).
xhscdn·douyinpic·gstatic·pinimg에 이어 **5번째 반복된 사고**다 —
새 플랫폼을 붙일 때 이 목록을 같이 고쳐라.

---

## 규모 — 실측

- **한 키워드의 천장은 약 500건**('뷰티' 520건/40페이지 29초). 페이지가 깊어질수록
  수확이 23→6건으로 줄다가 신규 0으로 끝난다. **늘리려면 키워드를 쪼개야 한다.**
- 뷰티 하위 키워드 20개(`_NAVERCLIP_BEAUTY_KWS`) → **2,935건 / 59초**(서버 실측)
- 키워드 6스레드 + 상세 8스레드. 순차면 7분이라 웹 요청이 죽는다.

---

## 📊 뷰티 2,525편 분석 결과 (사장님 관심사)

**네이버 클립은 대부분 안 터진다** — 중앙값 **86회**, 53%가 100회 미만.
1만+ 10.7% · 10만+ 2.6% · 평균(9,810)과 중앙값(86)이 100배 차이.

**해시태그** (기준선 터짐률 10.7%를 이겨야 의미 있음)

| 터지는 태그 | 터짐률 | | 죽은 태그 | 터짐률 |
|---|---|---|---|---|
| #다이어트(57회) | **56.1%** | | #인생쿠션(67회) | 0.0% |
| #셀프네일(44) | 34.1% | | #밀착력좋은쿠션(52) | 0.0% |
| #연예인(45) | 26.7% | | #모공커버(49) | 0.0% |
| #네일아트(90) | 26.7% | | #기미커버쿠션(44) | 0.0% |
| #젤네일(42) | 26.2% | | #속눈썹펌(41) | 0.0% |

**★태그는 적을수록 터진다** — 1~2개 31.2% / 3~4개 18.8% / 5~7개 10.8% / 8개+ **5.6%**.
통념과 정반대다(중앙 조회수는 21배 차이).

**길이**(10만 기준) — 0~15초 1.3% / 15~30초 2.0% / 45~60초 **7.5%** / 60~90초 **8.8%**.
짧을수록 좋다가 아니다.

**터진 영상의 공통 구조**: ①변신(before/after) ②싼 대체법(다이소) ③통념 반박
("오히려 망친다") ④내부자/전문가가 몰래 알려주는 것. **제품 자랑이 아니라 몰랐던 사실.**

**크리에이터**: 1,228명 중 10만+ 보유자는 42명(3.4%)뿐인데 그들은 반복해서 터뜨린다
(더바디영 4/5, 아랄라 5/8, 동네 의사 이상욱 3/5) — 운이 아니라 방법이다.

**네이버 데이터랩 교차**(상대비율, 요청 간 비교 불가 주의)

| 주제 | 검색수요 | 클립 터짐률 | 판정 |
|---|---|---|---|
| 속눈썹펌·연장 | **100(최상위)** | **0.0%** | 🎯 최대 공백 |
| 올리브영 | 70~100 | 수집 39편뿐 | 🎯 공백 |
| 다이어트 | 46~64 | 56.1% | ✅ 금맥(8월 연중최고 64.0) |
| 피부관리 | 17.5→10.6 **-39%** | 중간 | ⚠️ 수요 이탈 |
| 쿠션 | 6~9(바닥) | 0.0% | ❌ 두 지표 모두 사망 |
| 하이라이터·블러셔 | 3.11→0.83 **-73%** | 중간 | ❌ 급속 소멸 |
| 연예인템 | 0.03(거의 0) | 26.2% | 🔍 검색 아닌 **피드 소비형** |

---

## ⏭ 다음에 할 것

```
□ 1. 기본 키워드 20개 중 **5개가 0건**이다(립메이크업·피부관리·눈썹정리·
      다이소뷰티·모공관리). 특히 '피부관리'는 터진 영상 태그에 10편이나 있어
      0건이 이상하다 — **파서 문제일 가능성**. 아직 안 갈랐다.
      확인법: naverclip_search._parse_list(_fetch_list('피부관리',1)) 직접 호출.

□ 2. 속눈썹펌·올리브영 **공백 분석** — 수요 최상위인데 왜 아무도 못 터뜨리나.
      이게 사장님 목적(뭘 만들면 되나)에 제일 가깝다.

□ 3. 카드 인라인 재생 미검증 — mp4 만료(hdnts=exp) 때문에 실패할 수 있다.
      실패하면 재생 시점에 카드 API를 다시 부르는 식으로 고친다.

□ 4. (선택) 크론 자동 갱신 — 지금은 버튼 누를 때만 담긴다.
```

---

## 🔧 운영 메모 (헛짚기 방지)

**"아무것도 안 뜬다" 제보가 오면 순서대로 봐라:**

```
① 데이터가 있나 — 코드가 아니라 데이터가 0건일 수 있다.
   실사고 2026-08-30: 탭은 열렸는데(GET /api/reference 200) POST collect가
   **한 번도 안 눌린 상태**였다. 빈 목록이 정상 동작이었다.
   확인: sudo journalctl -u shopping-shorts | grep naverclip

② 데이터 저장 위치 = reference.db 의 **settings 테이블**, key = `last_run::naverclip`
   (last_run 테이블이 아니다 — 거긴 인스타 전용 id=1 한 줄뿐).
   확인:
     python3 -c "import sqlite3,json;c=sqlite3.connect('file:shopping_shorts/data/reference.db?mode=ro',uri=True);
     print(len(json.loads(c.execute(\"select value from settings where key='last_run::naverclip'\").fetchone()[0])['items']))"

③ 화면 기본 탭이 **48시간 히트작**이다. 수집분은 최근 몇 달치가 섞여 있어
   대부분 필터에 걸린다 — **역대 히트작**으로 바꿔서 봐야 다 보인다.

④ 프로세스가 옛 코드일 수 있다. auto_deploy가 **고객 접속 중이면 재시작을 미룬다**
   (MAX_DEFER_SEC=1800, 30분 넘으면 강제). 디스크 코드 ≠ 도는 코드.
   확인: systemctl show shopping-shorts -p ActiveEnterTimestamp
```

**2026-08-30 시점 라이브 상태**: `last_run::naverclip` **2,935건**(14:25 UTC 저장).
서버에서 직접 수집 스크립트를 돌려 채웠다 — 화면 버튼은 재시작 후에 눌러야
2,900건짜리 새 로직이 돈다(그전엔 옛 로직 20건).

---

# 2026-08-31 — 채널 수집 축 신설 (트랙 `네이버클립채널`, 라이브)

## 무엇을 했나

사장님이 준 **벤치마킹 채널 17개**를 매일 훑는 축을 만들었다. 기존 키워드 검색과
**다른 축**이다 — 키워드는 "뷰티에서 뭐가 터지나"(넓게), 채널은 "이 15명이 오늘
뭘 올렸나"(좁게·매일).

커밋 `a74add40f` → `a9f020784`(merge).

## ★경로 — 브라우저 네트워크 관찰로 찾았다 (인증·프록시 불필요)

```
① 핸들 → 프로필   creatorhub-api.naver.com/api/v7.0/clip/profiles?clipId=<핸들>
                  → 닉네임·팔로워·게시물수·영상수  (헤더 없이 200)
② 프로필 → 세션   .../feed/content  (단수)  → body.session.id
③ 세션 → 목록     .../feed/contents (복수) + sessionId&count=N  → 61건/회
```

### 함정 3개 (여기서 시간을 다 썼다)

```
□ ②를 건너뛰고 ③을 바로 부르면 400 `Session not found or expired`.
  세션을 먼저 받아야 한다. ②의 응답에도 카드가 1건 들어 있다(버리면 최신 1편 손실).

□ ③은 `x-creator-hub-sid: clip` 헤더가 필요하다. 없으면 400.
  (①은 이 헤더 없이도 200이라, ①만 테스트하면 함정을 못 본다)

□ 채널 HTML(clip.naver.com/@핸들)은 **JS 껍데기**다 — 32자 mediaId 0개,
  `__NEXT_DATA__`도 없다. 파싱하려 들지 마라. API만이 답이다.
```

### ★내가 낸 버그 — 주소를 지어냈다 (테스트로 박아둠)

채널 경로를 새로 짜면서 영상 주소를 `clip.naver.com/clips/mv/video/<id>`로
**지어냈는데 404**였다. 후보 7종(clips/·shorts/·video/·clip/ …)을 다 때려봐도
전부 404. 그런데 **검색 경로에는 이미 되는 주소가 있었다**:

```
https://m.naver.com/shorts?serviceType=CLIP&mediaType=VOD&seedMediaId=<id>
```

→ `clip_url()` 하나로 공용화하고 검색·채널이 같이 쓰게 했다(0순위-B).
   실접속 HTTP 200 확인. 테스트 `test_clip_url_is_single_source`가 재발을 막는다.

## 사장님 목록 17개 → 실제 15개

```
· 팁모하랑 → 팁모아랑(daillio)이 정확한 이름이다 (오타)
· 미미홀릭 = 사오공픽 — 핸들이 mimipick99인 게 증거(개명한 것으로 보인다). 중복.
· 꽃보다클립은 네이버 클립에 없다 (다른 플랫폼 채널로 보인다)
```

| 채널 | 핸들 | 팔로워 | 영상 |
|---|---|---:|---:|
| 진리뷰티 | `youn-youn` | 7,088 | 332 |
| 후다닥홈 | `hudadak_home` | 6,610 | 714 |
| 아름다름뷰티 | `areumdareum_beauty` | 4,784 | 340 |
| 결이고운 | `pqk2yxfjtn` | 4,511 | 144 |
| 스킨힐스 | `skinheals` | 3,296 | 139 |
| 예쁨한스푼 | `beautyspoon_c` | 1,525 | 310 |
| 핑카의 하루하루 | `pingca_life` | 1,278 | 262 |
| 미소의꿀템찾기 | `mirrorlifetem` | 914 | 132 |
| 동안습관연구소 | `armeee` | 404 | 237 |
| 템템바라 | `temtembara` | 363 | 307 |
| 예쁨더하기 | `zipkootem` | 189 | 156 |
| 사오공픽 | `mimipick99` | 131 | 184 |
| 팁모아랑 | `daillio` | 95 | 170 |
| 퀸즈살롱 | `queenssalon` | 22 | 88 |
| 하루홈 | `haruhomee` | 18 | 45 |
| **합계** | | **31,228** | **3,560** |

⚠️ **팔로워순으로 정렬하지 마라.** 퀸즈살롱(22명)·하루홈(18명)이 목록에 있는 건
사장님이 **영상 품질**로 골랐기 때문이다(냐미님: "팔로워는 적은데 영상 잘 만든다").

## API (관리자 전용)

```
POST /api/naverclip/channels/collect   {handles?, per_channel?, reset?}
     시드 없으면 기본 15채널. 저장은 키워드 수집과 같은 계약(platform="naverclip").
POST /api/naverclip/channels/seed      {handles[] | defaults:true | remove:"핸들"}
     ★없는 핸들은 거부한다 — 오타를 심으면 매일 0건을 긁는다.
```

시드는 `platform_seeds`(platform="naverclip", kind="account") — 틱톡·유튜브와 같은 표.

## 실측

- 15채널 **프로필 조회 1.1초** / **영상 915건 4.2초**(병렬 6)
- E2E 3채널 63건 8.7초, **썸네일·재생주소 63/63 확보**
- 목록엔 조회수만 온다 — 좋아요·댓글·썸네일·길이는 카드 API에만 있어 상세 보강이 필수

테스트 10건(`test_naverclip_channel.py`), 전부 실패 재현 후 통과. 37 passed(회귀 없음).

## ✅ 화면 버튼 + 매일 크론 (커밋 838738702 → 72d9a958f, 라이브)

**화면** — 🟩 네이버클립 패널의 키워드 줄 아래 구분선을 두고 붙였다:
`[👥 채널 수집] [⭐ 기본 15채널 등록] [채널 추가 입력칸] [➕ 추가]`
브라우저 실측: **15채널 915건 수집**, API 조회 915/915 정상(썸네일·URL 포함).

**크론** — 04:00 `daily_batch`에 얹었다(**새 크론을 만들지 않는다**).
킬스위치 `NAVERCLIP_AUTO_COLLECT`(기본 OFF, 서버 env에서 켠다) ·
`NAVERCLIP_AUTO_PER_CHANNEL`(기본 60). 실패해도 배치 전체를 안 죽인다.

```
★켜는 법: /etc/shopping-shorts.env 에 NAVERCLIP_AUTO_COLLECT=1 추가 후 재시작
```

**0순위-B** — 수집 로직을 `app.collect_channels()` 한 곳으로 뽑아 화면·크론이 같은
코드를 부른다. 크론이 자기 서버에 HTTP를 쏘게 하면 인증·타임아웃이 얽힌다.

### ★내가 낸 버그 — 오류 없이 0건 (브라우저에서 눌러 보고서야 잡혔다)

`store.list_seeds`는 **dict 리스트**인데 `for _k, v, _a in ...`로 튜플 언패킹했다.
`v`가 컬럼 이름 문자열(`"value"`)이 되어 **채널 15개에서 0건**이 나왔다.
예외도 없고 서버 로그에도 흔적이 0이었다. **목킹 테스트는 시드 경로를 안 타
(빈 시드로 통과)** — 실제로 버튼을 눌러 보지 않았으면 라이브로 나갔다.
→ `test_seeds_are_read_as_dicts`로 박아뒀다.

## ⏭ 다음에 할 것

```
□ 1. ★"비슷한 채널 뽑기"(사장님 목적) — 설계가 필요하다. 기준 후보:
      해시태그 겹침? 검색 노출 겹침? 채널 소개문 유사도? 아직 정하지 않았다.
□ 2. 서버에서 NAVERCLIP_AUTO_COLLECT=1 켜기 (지금은 꺼져 있어 버튼으로만 돈다)
□ 3. 이전 세션 숙제: 기본 키워드 20개 중 5개가 0건인 원인(파서 문제 의심),
      속눈썹펌·올리브영 공백 분석.
```

---

# 2026-08-31 — 조회수 옆 '+undefined' 수정 (트랙 `델타언디파인드`, 라이브)

커밋 `712756527` → `abca842a9`(merge).

## 증상과 원인

사장님 제보: **"조회수가 잘 안 따라오는 것 같다. undefined 댓글도 확인"**

카드의 조회수 옆 증가분이 `+undefined`로 찍혔다. 화면이
`${i.is_new?'신규':'+'+i.delta}`인데 **delta가 없으면 문자열 결합이 그대로
"+undefined"**가 된다(JS는 오류 없이 통과시킨다).

### 왜 어떤 플랫폼만 그런가 (서버 실측)

| 플랫폼 | delta 키 없음 |
|---|---|
| youtube | 0 / 8,000 |
| tiktok | 0 / 8 |
| **naverclip** | **4,492 / 4,492** |
| **pinterest** | **2,259 / 2,259** |

유튜브·인스타·틱톡은 `ranking.py`를 거치며 `platform_snapshots`(직전 수집의
조회수)와 비교해 delta·is_new를 얻는다. **네이버클립·핀터레스트는 그 경로를
안 타고** 바로 저장해서 두 키가 통째로 없었다.

## 처방 — 두 겹

수집만 고치면 **이미 저장된 6,751건은 그대로 undefined**라 화면도 함께 막았다.

```
① ranking.attach_delta()  — 증가분 규칙을 한 곳에(0순위-B).
   ★음수를 만들지 않는다 — 플랫폼이 집계를 보정해 조회수가 줄어 보이는 일이
     있는데 "-30" 증가분은 화면에서 거짓말이 된다.
② store._fill_delta()     — save/merge_last_run_platform **저장 함수 안**에서 채운다.
   ★호출부마다 붙이면 새 플랫폼이 생길 때 또 빠뜨린다(락을 여기 둔 것과 같은 이유).
   store.prev_base_map()으로 직전 조회수를 한 방에 읽는다(4,000건 단건조회 방지).
③ 화면 _delta(i) 헬퍼     — 값이 없으면 **아무것도 안 그린다**. +0도 안 그린다.
④ ★함께 발견: `i.comments.toLocaleString()`이 comments 없으면 **render를 통째로
   죽인다** — 조용한 오표기가 아니라 화면이 빈다. Number(i.comments||0)로 가드.
```

## 검증

```
서버   1차 신규 delta=조회수 / 2차 A 175-100=+75, C 신규 — 실동작 확인
브라우저 delta 없는 항목·comments 키 없는 항목 포함 4장 렌더
        → undefined 0개 · NaN 0개 · render 오류 없음
        → "조회수 500 +75" / "조회수 30 신규" 정상
★사보타주 옛 코드 그대로 실행 → "+undefined" 재현, comments는 TypeError로 사망
```

테스트 8건 신규. 58 passed(naverclip·pinterest 포함, 회귀 없음).

## ⚠️ 옛 수집분은 배지가 안 뜬다

`_fill_delta`는 **새로 수집할 때** 채운다. 이미 저장된 네이버클립 4,492건·
핀터레스트 2,259건은 delta가 없으므로 화면에서 **증가분 배지가 빈칸**이다
(undefined는 사라졌지만 숫자도 없다). 다음 수집부터 정상으로 뜬다.
