# 인용 엔진 스파이크 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 유튜브 URL + 주제를 넣으면 `quotes_candidates.json`(골든 발언 후보 + 스탠스·근거·tier·heat·화면근거)을 뱉는 CLI 엔진을 만들어, "영상 하나에서 장면+대사를 실제로 뽑을 수 있는가"를 실데이터로 검증한다. UI 없음.

**Architecture:** `scripts/yt_agents/quote_extractor.py` 단일 모듈 + 순수함수 위주. 외부 I/O(yt-dlp 메타/자막, ffmpeg 프레임, Gemini 채점/비전)는 각각 얇은 함수로 분리하고, 순수 로직(자막 파싱·heatmap 매핑·tier 배정)은 완전 TDD, I/O는 커맨드/파싱 유닛테스트 + 실URL 스모크로 검증한다. 기존 `gemini_client.py`(18키 폴백) 재활용.

**Tech Stack:** Python 3.14, `python -m yt_dlp`(2026.03.17, PATH 바이너리 아님·모듈), ffmpeg 8.1, `google-genai`(gemini_client 경유), pytest 9.

## Global Constraints

- yt-dlp는 **`python -m yt_dlp`**로만 호출 (PATH에 `yt-dlp` 바이너리 없음).
- Gemini 호출은 반드시 `scripts/yt_agents/gemini_client.py` 경유 (18키 폴백 이미 구현됨). 새 API 키 로직 만들지 말 것.
- 골든 루브릭 = **필수3(주장 명확·근거 동반·입장 존재) 모두 통과해야 골든**, 가점3(구체성·화면근거·화자신뢰). score 0~6.
- tier 정렬: **T1 has_visual(자료구동) > T2 heat 스파이크 > T3 루브릭통과**. 겹치면 최상위.
- 억지 금지: 이 스파이크는 발언 **후보 추출까지만**. 스토리라인/클러스터링은 범위 밖.
- ts 내부표현 = **초(float)**, 출력 JSON에는 `ts_sec`(float)와 `ts`("MM:SS") 둘 다.
- 테스트: `python -m pytest tests/yt_agents/ -v`. 실URL 스모크는 `-m smoke`로 분리(기본 스킵, 명시 실행).
- 실데이터 원칙: 합성 fixture 통과 = 완료 아님. 각 I/O 함수는 실URL 스모크로 반드시 한 번 확인.

---

### Task 1: 스키마 + 자막 파싱 (순수)

**Files:**
- Create: `scripts/yt_agents/quote_extractor.py`
- Create: `tests/yt_agents/__init__.py` (빈 파일)
- Create: `tests/yt_agents/test_quote_extractor.py`
- Create: `tests/yt_agents/fixtures/sample.ko.vtt`

**Interfaces:**
- Produces:
  - `@dataclass Segment(start: float, text: str)`
  - `parse_vtt(vtt_text: str) -> list[Segment]` — VTT 문자열 → 세그먼트. 큐 시작시각(초)과 텍스트만. 빈 줄/태그(`<c>`, `<00:00:00.000>`) 제거.
  - `to_mmss(sec: float) -> str` — 73.4 → "01:13".

- [ ] **Step 1: fixture VTT 작성**

`tests/yt_agents/fixtures/sample.ko.vtt`:
```
WEBVTT

00:00:12.000 --> 00:00:15.000
HBM 공급부족은 최소 내년까지 갑니다

00:03:40.500 --> 00:03:44.000
밸류 부담은 <c>분명히</c> 있습니다
```

- [ ] **Step 2: 실패 테스트 작성**

`tests/yt_agents/test_quote_extractor.py`:
```python
from pathlib import Path
import sys
sys.path.insert(0, str(Path(__file__).resolve().parents[2] / "scripts" / "yt_agents"))
import quote_extractor as qe

FIX = Path(__file__).parent / "fixtures"

def test_parse_vtt_basic():
    segs = qe.parse_vtt((FIX / "sample.ko.vtt").read_text(encoding="utf-8"))
    assert len(segs) == 2
    assert segs[0].start == 12.0
    assert segs[0].text == "HBM 공급부족은 최소 내년까지 갑니다"
    assert segs[1].start == 220.5
    assert segs[1].text == "밸류 부담은 분명히 있습니다"   # 태그 제거됨

def test_to_mmss():
    assert qe.to_mmss(73.4) == "01:13"
    assert qe.to_mmss(220.5) == "03:40"
```

- [ ] **Step 3: 실패 확인**

Run: `python -m pytest tests/yt_agents/test_quote_extractor.py -v`
Expected: FAIL (`ModuleNotFoundError: quote_extractor` 또는 `AttributeError`)

- [ ] **Step 4: 최소 구현**

`scripts/yt_agents/quote_extractor.py`:
```python
"""인용 엔진 스파이크 — URL → 골든 발언 후보(quotes_candidates.json).
전문가 인용 몽타주 영상용. 기존 gemini_client(18키 폴백) 재활용."""
from __future__ import annotations
import re
from dataclasses import dataclass, field


@dataclass
class Segment:
    start: float   # seconds
    text: str


def to_mmss(sec: float) -> str:
    m, s = divmod(int(sec), 60)
    return f"{m:02d}:{s:02d}"


_TS = re.compile(r"(\d\d):(\d\d):(\d\d)\.(\d{3})\s*-->")
_TAG = re.compile(r"<[^>]+>")


def parse_vtt(vtt_text: str) -> list[Segment]:
    """WEBVTT → Segment 리스트. 큐 시작시각(초) + 태그제거 텍스트."""
    segs: list[Segment] = []
    lines = vtt_text.splitlines()
    i = 0
    while i < len(lines):
        m = _TS.search(lines[i])
        if not m:
            i += 1
            continue
        h, mm, ss, ms = (int(x) for x in m.groups())
        start = h * 3600 + mm * 60 + ss + ms / 1000
        i += 1
        buf = []
        while i < len(lines) and lines[i].strip() and not _TS.search(lines[i]):
            buf.append(_TAG.sub("", lines[i]).strip())
            i += 1
        text = " ".join(x for x in buf if x)
        if text:
            segs.append(Segment(start=start, text=text))
    return segs
```

- [ ] **Step 5: 통과 확인**

Run: `python -m pytest tests/yt_agents/test_quote_extractor.py -v`
Expected: PASS (2 passed)

- [ ] **Step 6: 커밋**

```bash
git add scripts/yt_agents/quote_extractor.py "tests/yt_agents/__init__.py" tests/yt_agents/test_quote_extractor.py tests/yt_agents/fixtures/sample.ko.vtt
git commit -m "feat(quote-engine): VTT 파싱 + 스키마 스캐폴드"
```

---

### Task 2: yt-dlp 메타 + heatmap 수집 (I/O, T2 실측)

**Files:**
- Modify: `scripts/yt_agents/quote_extractor.py`
- Modify: `tests/yt_agents/test_quote_extractor.py`
- Create: `tests/yt_agents/fixtures/info_with_heatmap.json`

**Interfaces:**
- Consumes: `Segment`, `parse_vtt`
- Produces:
  - `fetch_info(url: str) -> dict` — `python -m yt_dlp --skip-download --dump-single-json <url>` 결과 파싱. `{"title","channel","duration","webpage_url","heatmap"}` 서브셋 반환. heatmap 없으면 `heatmap=None`.
  - `parse_heatmap(info: dict) -> list[dict]` — info의 `heatmap` → `[{"start":float,"end":float,"value":float}]` 정규화. 없으면 `[]`.

- [ ] **Step 1: fixture 작성 (heatmap 있는 케이스)**

`tests/yt_agents/fixtures/info_with_heatmap.json`:
```json
{
  "title": "반도체 고점 논쟁",
  "channel": "전문가채널",
  "duration": 600,
  "webpage_url": "https://youtu.be/xxxx",
  "heatmap": [
    {"start_time": 0.0, "end_time": 30.0, "value": 0.2},
    {"start_time": 200.0, "end_time": 230.0, "value": 0.95}
  ]
}
```

- [ ] **Step 2: 실패 테스트 작성**

`tests/yt_agents/test_quote_extractor.py`에 추가:
```python
import json

def test_parse_heatmap_present():
    info = json.loads((FIX / "info_with_heatmap.json").read_text(encoding="utf-8"))
    hm = qe.parse_heatmap(info)
    assert len(hm) == 2
    assert hm[1]["start"] == 200.0 and hm[1]["value"] == 0.95

def test_parse_heatmap_absent():
    assert qe.parse_heatmap({"title": "x"}) == []
```

- [ ] **Step 3: 실패 확인**

Run: `python -m pytest tests/yt_agents/test_quote_extractor.py -v -k heatmap`
Expected: FAIL (`AttributeError: parse_heatmap`)

- [ ] **Step 4: 구현**

`quote_extractor.py`에 추가:
```python
import json
import subprocess


def fetch_info(url: str) -> dict:
    """yt-dlp 단일 JSON 메타 (다운로드 없이). heatmap 포함."""
    cmd = ["python", "-m", "yt_dlp", "--skip-download",
           "--dump-single-json", "--no-warnings", url]
    out = subprocess.run(cmd, capture_output=True, text=True,
                         encoding="utf-8", errors="replace", timeout=90)
    if out.returncode != 0:
        raise RuntimeError(f"yt-dlp 메타 실패: {out.stderr[:300]}")
    raw = json.loads(out.stdout)
    return {
        "title": raw.get("title", ""),
        "channel": raw.get("channel") or raw.get("uploader", ""),
        "duration": raw.get("duration", 0),
        "webpage_url": raw.get("webpage_url", url),
        "heatmap": raw.get("heatmap"),
    }


def parse_heatmap(info: dict) -> list[dict]:
    """info.heatmap → [{start,end,value}]. 없으면 []."""
    hm = info.get("heatmap")
    if not hm:
        return []
    out = []
    for b in hm:
        out.append({
            "start": float(b.get("start_time", 0.0)),
            "end": float(b.get("end_time", 0.0)),
            "value": float(b.get("value", 0.0)),
        })
    return out
```

- [ ] **Step 5: 통과 확인**

Run: `python -m pytest tests/yt_agents/test_quote_extractor.py -v -k heatmap`
Expected: PASS (2 passed)

- [ ] **Step 6: 실URL 스모크 (T2 실측 — heatmap 제공 여부)**

Run:
```bash
python -c "import sys; sys.path.insert(0,'scripts/yt_agents'); import quote_extractor as qe; info=qe.fetch_info('https://www.youtube.com/watch?v=6fehifjxOcs'); print('title:',info['title'][:40]); print('dur:',info['duration']); print('heatmap buckets:', len(qe.parse_heatmap(info)))"
```
Expected: title/duration 출력됨. **heatmap buckets 개수 기록** — 0이면 이 영상은 T2 미제공(폴백 확인), >0이면 T2 가동 가능. **결과를 커밋 메시지에 적을 것.**

- [ ] **Step 7: 커밋**

```bash
git add scripts/yt_agents/quote_extractor.py tests/yt_agents/test_quote_extractor.py tests/yt_agents/fixtures/info_with_heatmap.json
git commit -m "feat(quote-engine): yt-dlp 메타+heatmap 수집 (T2 실측: <buckets>개)"
```

---

### Task 3: 자막 다운로드 → 세그먼트 (I/O)

**Files:**
- Modify: `scripts/yt_agents/quote_extractor.py`
- Modify: `tests/yt_agents/test_quote_extractor.py`

**Interfaces:**
- Consumes: `parse_vtt`, `Segment`
- Produces:
  - `class TranscriptUnavailable(Exception)`
  - `get_transcript(url: str, lang: str = "ko", workdir: str | None = None) -> list[Segment]` — `python -m yt_dlp --skip-download --write-auto-sub --sub-lang <lang> --sub-format vtt` 로 자막 받아 `parse_vtt`. 자막 파일 없으면 `TranscriptUnavailable` 발생(→ 후속: Whisper 폴백은 스파이크 범위 밖, 메시지에 명시).

- [ ] **Step 1: 실패 테스트 (커맨드 빌더 검증)**

`test_quote_extractor.py`에 추가:
```python
def test_transcript_cmd_builder():
    # 내부 커맨드 빌더가 python -m yt_dlp + auto-sub + lang 을 쓰는지
    cmd = qe._transcript_cmd("https://youtu.be/x", "ko", "/tmp/wd")
    assert cmd[:3] == ["python", "-m", "yt_dlp"]
    assert "--write-auto-sub" in cmd
    assert "ko" in cmd
```

- [ ] **Step 2: 실패 확인**

Run: `python -m pytest tests/yt_agents/test_quote_extractor.py -v -k transcript_cmd`
Expected: FAIL (`AttributeError: _transcript_cmd`)

- [ ] **Step 3: 구현**

`quote_extractor.py`에 추가:
```python
import glob
import os
import tempfile


class TranscriptUnavailable(Exception):
    pass


def _transcript_cmd(url: str, lang: str, workdir: str) -> list[str]:
    outtmpl = os.path.join(workdir, "%(id)s.%(ext)s")
    return ["python", "-m", "yt_dlp", "--skip-download",
            "--write-auto-sub", "--write-sub", "--sub-lang", lang,
            "--sub-format", "vtt", "--no-warnings", "-o", outtmpl, url]


def get_transcript(url: str, lang: str = "ko", workdir: str | None = None) -> list[Segment]:
    """yt-dlp 자막(auto 포함) 다운로드 → Segment. 없으면 TranscriptUnavailable."""
    own = workdir is None
    workdir = workdir or tempfile.mkdtemp(prefix="qe_sub_")
    try:
        cmd = _transcript_cmd(url, lang, workdir)
        subprocess.run(cmd, capture_output=True, text=True,
                       encoding="utf-8", errors="replace", timeout=120)
        vtts = glob.glob(os.path.join(workdir, f"*{lang}*.vtt")) or \
               glob.glob(os.path.join(workdir, "*.vtt"))
        if not vtts:
            raise TranscriptUnavailable(
                f"{url}: {lang} 자막 없음 (후속: Whisper 폴백은 스파이크 범위 밖)")
        return parse_vtt(open(vtts[0], encoding="utf-8").read())
    finally:
        if own:
            import shutil
            shutil.rmtree(workdir, ignore_errors=True)
```

- [ ] **Step 4: 통과 확인**

Run: `python -m pytest tests/yt_agents/test_quote_extractor.py -v -k transcript_cmd`
Expected: PASS

- [ ] **Step 5: 실URL 스모크 (자막 봇차단 실측)**

Run:
```bash
python -c "import sys; sys.path.insert(0,'scripts/yt_agents'); import quote_extractor as qe; segs=qe.get_transcript('https://www.youtube.com/watch?v=6fehifjxOcs'); print('segments:', len(segs)); print(segs[0].start, segs[0].text[:30]) if segs else print('none')"
```
Expected: segments 개수 출력. **0 또는 TranscriptUnavailable이면 = yt-dlp 자막 봇차단 확인**(메모리 [[project_crawl_dashboard_server]] 경고와 일치하는지 기록). 이 경우 후속에서 Gemini `call_video` 경로로 전환 결정.

- [ ] **Step 6: 커밋**

```bash
git add scripts/yt_agents/quote_extractor.py tests/yt_agents/test_quote_extractor.py
git commit -m "feat(quote-engine): 자막 다운로드→세그먼트 (자막 실측: <n>개)"
```

---

### Task 4: 골든 발언 채점 (Gemini, 루브릭)

**Files:**
- Modify: `scripts/yt_agents/quote_extractor.py`
- Modify: `tests/yt_agents/test_quote_extractor.py`
- Create: `tests/yt_agents/fixtures/gemini_score_reply.json`

**Interfaces:**
- Consumes: `Segment`
- Produces:
  - `@dataclass QuoteCandidate(source, ts, text, stance, evidence, score, reasons, has_visual=False, heat=0.0, tier=3, media=None)` — 타입: `source:str, ts:float, text:str, stance:str, evidence:str, score:int, reasons:list[str], has_visual:bool, heat:float, tier:int, media:dict|None`
  - `build_score_prompt(topic: str, segments: list[Segment]) -> str` — 루브릭 채점 프롬프트 문자열.
  - `parse_score_reply(reply: dict, source: str) -> list[QuoteCandidate]` — Gemini JSON → 골든(golden=true)만 QuoteCandidate로.
  - `score_quotes(topic, segments, source) -> list[QuoteCandidate]` — build_score_prompt → gemini_client.call_json → parse_score_reply.

- [ ] **Step 1: Gemini 응답 fixture**

`tests/yt_agents/fixtures/gemini_score_reply.json`:
```json
{
  "quotes": [
    {"ts": 12.0, "text": "HBM 공급부족은 최소 내년까지 갑니다",
     "golden": true, "stance": "강세", "evidence": "수급",
     "specific": true, "reasons": ["명확한 판단", "수급 근거", "기간 구체"]},
    {"ts": 220.5, "text": "밸류 부담은 분명히 있습니다",
     "golden": true, "stance": "약세", "evidence": "밸류",
     "specific": false, "reasons": ["판단 명확", "밸류 근거"]},
    {"ts": 300.0, "text": "뭐 지켜봐야죠",
     "golden": false, "stance": "중립", "evidence": "기타",
     "specific": false, "reasons": ["물타기 — 필수 탈락"]}
  ]
}
```

- [ ] **Step 2: 실패 테스트**

```python
def test_parse_score_reply_filters_golden():
    reply = json.loads((FIX / "gemini_score_reply.json").read_text(encoding="utf-8"))
    cands = qe.parse_score_reply(reply, source="채널/URL")
    assert len(cands) == 2                      # golden=false 1개 제외
    c0 = cands[0]
    assert c0.stance == "강세" and c0.evidence == "수급"
    assert c0.score >= 4                         # 필수3(3) + 구체성 가점(1)
    assert cands[1].score == 3                   # 필수3만, 구체성 없음

def test_build_score_prompt_has_rubric():
    p = qe.build_score_prompt("반도체 고점인가", [qe.Segment(12.0, "HBM 부족")])
    assert "필수" in p and "강세" in p and "12" in p
```

- [ ] **Step 3: 실패 확인**

Run: `python -m pytest tests/yt_agents/test_quote_extractor.py -v -k score`
Expected: FAIL

- [ ] **Step 4: 구현**

`quote_extractor.py`에 추가:
```python
@dataclass
class QuoteCandidate:
    source: str
    ts: float
    text: str
    stance: str
    evidence: str
    score: int
    reasons: list[str] = field(default_factory=list)
    has_visual: bool = False
    heat: float = 0.0
    tier: int = 3
    media: dict | None = None


def build_score_prompt(topic: str, segments: list[Segment]) -> str:
    lines = "\n".join(f"[{to_mmss(s.start)}|{s.start:.1f}] {s.text}" for s in segments)
    return f"""너는 주식 영상 편집자다. 주제 "{topic}"에 대한 아래 자막에서 '골든 발언'만 골라라.

골든 규칙 — 필수 3개를 모두 넘어야 golden=true:
1) 주장 명확: 판단이 있는가 (인사말·물타기·"지켜봐야죠"는 탈락)
2) 근거 동반: 왜인가 (근거유형=수급|실적|밸류|공급망|기타)
3) 입장 존재: stance=강세|약세|중립|전환 중 하나로 분류 가능

가점(specific): 숫자·날짜·종목·조건이 있으면 true.

자막(각 줄 [MM:SS|초] 텍스트):
{lines}

JSON만 출력:
{{"quotes":[{{"ts":<초 float>,"text":"...","golden":true|false,
"stance":"강세|약세|중립|전환","evidence":"수급|실적|밸류|공급망|기타",
"specific":true|false,"reasons":["..."]}}]}}"""


def parse_score_reply(reply: dict, source: str) -> list[QuoteCandidate]:
    out: list[QuoteCandidate] = []
    for q in reply.get("quotes", []):
        if not q.get("golden"):
            continue
        score = 3 + (1 if q.get("specific") else 0)   # 필수3 통과=3, 구체성 가점
        out.append(QuoteCandidate(
            source=source,
            ts=float(q.get("ts", 0.0)),
            text=q.get("text", ""),
            stance=q.get("stance", "중립"),
            evidence=q.get("evidence", "기타"),
            score=score,
            reasons=q.get("reasons", []),
        ))
    return out


def score_quotes(topic: str, segments: list[Segment], source: str) -> list[QuoteCandidate]:
    import gemini_client
    prompt = build_score_prompt(topic, segments)
    reply = gemini_client.call_json(prompt)
    return parse_score_reply(reply, source)
```

- [ ] **Step 5: 통과 확인**

Run: `python -m pytest tests/yt_agents/test_quote_extractor.py -v -k score`
Expected: PASS (2 passed)

- [ ] **Step 6: 커밋**

```bash
git add scripts/yt_agents/quote_extractor.py tests/yt_agents/test_quote_extractor.py tests/yt_agents/fixtures/gemini_score_reply.json
git commit -m "feat(quote-engine): 골든 루브릭 Gemini 채점 + 필터"
```

---

### Task 5: heatmap 매핑 + tier 배정 (순수)

**Files:**
- Modify: `scripts/yt_agents/quote_extractor.py`
- Modify: `tests/yt_agents/test_quote_extractor.py`

**Interfaces:**
- Consumes: `QuoteCandidate`, `parse_heatmap` 결과
- Produces:
  - `HEAT_THRESHOLD = 0.7`
  - `apply_heatmap(cands: list[QuoteCandidate], heatmap: list[dict]) -> None` — 각 후보 ts를 담는 버킷의 value를 `cand.heat`에 세팅(제자리 수정).
  - `assign_tiers(cands: list[QuoteCandidate]) -> None` — has_visual→tier1, elif heat>=HEAT_THRESHOLD→tier2, else tier3 (제자리).

- [ ] **Step 1: 실패 테스트**

```python
def _c(ts, has_visual=False):
    return qe.QuoteCandidate(source="s", ts=ts, text="t", stance="강세",
                             evidence="수급", score=3, has_visual=has_visual)

def test_apply_heatmap_sets_heat():
    cands = [_c(210.0), _c(5.0)]
    hm = [{"start":0.0,"end":30.0,"value":0.2},{"start":200.0,"end":230.0,"value":0.95}]
    qe.apply_heatmap(cands, hm)
    assert cands[0].heat == 0.95
    assert cands[1].heat == 0.2

def test_assign_tiers_priority():
    c_vis = _c(210.0, has_visual=True); c_vis.heat = 0.95
    c_hot = _c(210.0); c_hot.heat = 0.9
    c_plain = _c(5.0); c_plain.heat = 0.1
    qe.assign_tiers([c_vis, c_hot, c_plain])
    assert c_vis.tier == 1     # 자료구동 최우선(heat 높아도)
    assert c_hot.tier == 2     # 스파이크
    assert c_plain.tier == 3   # 루브릭만
```

- [ ] **Step 2: 실패 확인**

Run: `python -m pytest tests/yt_agents/test_quote_extractor.py -v -k "heatmap_sets or tiers"`
Expected: FAIL

- [ ] **Step 3: 구현**

```python
HEAT_THRESHOLD = 0.7


def apply_heatmap(cands: list[QuoteCandidate], heatmap: list[dict]) -> None:
    for c in cands:
        for b in heatmap:
            if b["start"] <= c.ts < b["end"]:
                c.heat = b["value"]
                break


def assign_tiers(cands: list[QuoteCandidate]) -> None:
    for c in cands:
        if c.has_visual:
            c.tier = 1
        elif c.heat >= HEAT_THRESHOLD:
            c.tier = 2
        else:
            c.tier = 3
```

- [ ] **Step 4: 통과 확인**

Run: `python -m pytest tests/yt_agents/test_quote_extractor.py -v -k "heatmap_sets or tiers"`
Expected: PASS

- [ ] **Step 5: 커밋**

```bash
git add scripts/yt_agents/quote_extractor.py tests/yt_agents/test_quote_extractor.py
git commit -m "feat(quote-engine): heatmap 매핑 + T1>T2>T3 tier 배정"
```

---

### Task 6: 프레임 캡처 + 화면근거 비전 (I/O)

**Files:**
- Modify: `scripts/yt_agents/quote_extractor.py`
- Modify: `tests/yt_agents/test_quote_extractor.py`

**Interfaces:**
- Consumes: `QuoteCandidate`
- Produces:
  - `_capture_cmd(stream_url: str, ts: float, out_path: str) -> list[str]` — ffmpeg seek 캡처 커맨드.
  - `capture_frame(url: str, ts: float, out_path: str) -> str` — `python -m yt_dlp -g`로 스트림 URL 얻어 ffmpeg로 ts에서 정지프레임 1장 저장. out_path 반환.
  - `detect_visuals(frame_paths: list[str]) -> list[bool]` — Gemini 비전으로 각 프레임에 "리포트/차트/슬라이드" 있는지 판정.

- [ ] **Step 1: 실패 테스트 (커맨드 빌더)**

```python
def test_capture_cmd_seeks_before_input():
    cmd = qe._capture_cmd("http://stream", 210.0, "/tmp/f.png")
    assert cmd[0] == "ffmpeg"
    i_ss = cmd.index("-ss"); i_i = cmd.index("-i")
    assert i_ss < i_i                      # 빠른 seek: -ss 가 -i 앞
    assert "210.0" in cmd and "/tmp/f.png" in cmd
    assert "-frames:v" in cmd
```

- [ ] **Step 2: 실패 확인**

Run: `python -m pytest tests/yt_agents/test_quote_extractor.py -v -k capture_cmd`
Expected: FAIL

- [ ] **Step 3: 구현**

```python
def _capture_cmd(stream_url: str, ts: float, out_path: str) -> list[str]:
    # -ss 를 -i 앞에 두면 키프레임 기준 빠른 seek (전체 디코드 안 함)
    return ["ffmpeg", "-y", "-ss", f"{ts}", "-i", stream_url,
            "-frames:v", "1", "-q:v", "2", out_path]


def _stream_url(url: str) -> str:
    out = subprocess.run(["python", "-m", "yt_dlp", "-g", "-f",
                          "best[height<=720]", "--no-warnings", url],
                         capture_output=True, text=True,
                         encoding="utf-8", errors="replace", timeout=90)
    if out.returncode != 0 or not out.stdout.strip():
        raise RuntimeError(f"스트림 URL 실패: {out.stderr[:200]}")
    return out.stdout.strip().splitlines()[0]


def capture_frame(url: str, ts: float, out_path: str) -> str:
    stream = _stream_url(url)
    r = subprocess.run(_capture_cmd(stream, ts, out_path),
                       capture_output=True, text=True,
                       encoding="utf-8", errors="replace", timeout=90)
    if r.returncode != 0 or not os.path.exists(out_path):
        raise RuntimeError(f"프레임 캡처 실패 @ {ts}s: {r.stderr[:200]}")
    return out_path


def detect_visuals(frame_paths: list[str]) -> list[bool]:
    """각 프레임에 리포트/차트/슬라이드가 있는지 Gemini 비전 판정."""
    from google.genai import types
    import gemini_client
    results: list[bool] = []
    for p in frame_paths:
        data = open(p, "rb").read()
        contents = types.Content(parts=[
            types.Part(inline_data=types.Blob(mime_type="image/png", data=data)),
            types.Part(text='이 화면에 리포트·표·차트·슬라이드 같은 "자료"가 '
                            '떠 있으면 {"visual":true}, 사람 얼굴/배경만이면 '
                            '{"visual":false} JSON만 출력.'),
        ])
        resp = gemini_client._generate("gemini-3-flash-preview", contents,
                                       types.GenerateContentConfig(temperature=0))
        results.append(bool(gemini_client._parse_json_text(resp.text).get("visual")))
    return results
```

- [ ] **Step 4: 통과 확인**

Run: `python -m pytest tests/yt_agents/test_quote_extractor.py -v -k capture_cmd`
Expected: PASS

- [ ] **Step 5: 실URL 스모크 (프레임 1장 실제 저장)**

Run:
```bash
python -c "import sys; sys.path.insert(0,'scripts/yt_agents'); import quote_extractor as qe; p=qe.capture_frame('https://www.youtube.com/watch?v=6fehifjxOcs', 60.0, 'scratch_frame.png'); import os; print('saved', p, os.path.getsize(p), 'bytes')"
```
Expected: `saved scratch_frame.png <N> bytes` (N>0). 실패 시 = 스트림 접근 봇차단 → 후속에서 `call_video` 캡처 대안 검토. 확인 후 `rm scratch_frame.png`.

- [ ] **Step 6: 커밋**

```bash
git add scripts/yt_agents/quote_extractor.py tests/yt_agents/test_quote_extractor.py
git commit -m "feat(quote-engine): ffmpeg 프레임 캡처 + Gemini 화면근거 비전"
```

---

### Task 7: 오케스트레이터 + CLI + 실URL E2E

**Files:**
- Modify: `scripts/yt_agents/quote_extractor.py`
- Modify: `tests/yt_agents/test_quote_extractor.py`

**Interfaces:**
- Consumes: 위 전부
- Produces:
  - `candidate_to_dict(c: QuoteCandidate) -> dict` — 출력 스키마(`ts`="MM:SS", `ts_sec`=float, source/text/tier/has_visual/heat/stance/evidence/score/media).
  - `extract(url: str, topic: str, capture: bool = False, out_path: str | None = None) -> dict` — 전 파이프라인 → `{"topic","source","quotes":[...]}`. capture=True면 tier<=2 후보만 프레임 캡처+비전으로 has_visual 갱신 후 재-tier.
  - `__main__`: `python -m quote_extractor <url> --topic "..." [--capture] [--out path]`

- [ ] **Step 1: 실패 테스트 (직렬화)**

```python
def test_candidate_to_dict_shape():
    c = qe.QuoteCandidate(source="채널/URL", ts=220.5, text="밸류 부담",
                          stance="약세", evidence="밸류", score=3,
                          has_visual=True, heat=0.9, tier=1)
    d = qe.candidate_to_dict(c)
    assert d["ts"] == "03:40" and d["ts_sec"] == 220.5
    assert d["tier"] == 1 and d["has_visual"] is True
    assert d["stance"] == "약세" and d["source"] == "채널/URL"
```

- [ ] **Step 2: 실패 확인**

Run: `python -m pytest tests/yt_agents/test_quote_extractor.py -v -k to_dict`
Expected: FAIL

- [ ] **Step 3: 구현**

```python
def candidate_to_dict(c: QuoteCandidate) -> dict:
    return {
        "source": c.source, "ts": to_mmss(c.ts), "ts_sec": c.ts,
        "text": c.text, "tier": c.tier, "has_visual": c.has_visual,
        "heat": c.heat, "stance": c.stance, "evidence": c.evidence,
        "score": c.score, "reasons": c.reasons, "media": c.media,
    }


def extract(url: str, topic: str, capture: bool = False,
            out_path: str | None = None) -> dict:
    info = fetch_info(url)
    source = f"{info['channel']} / {info['webpage_url']}"
    segments = get_transcript(url)
    cands = score_quotes(topic, segments, source)
    apply_heatmap(cands, parse_heatmap(info))
    assign_tiers(cands)

    if capture and cands:
        import shutil
        wd = tempfile.mkdtemp(prefix="qe_cap_")
        try:
            targets = [c for c in cands if c.tier <= 2] or cands
            paths = []
            for i, c in enumerate(targets):
                fp = os.path.join(wd, f"cap_{i:03d}.png")
                try:
                    capture_frame(url, c.ts, fp); paths.append((c, fp))
                except Exception:
                    pass
            if paths:
                flags = detect_visuals([p for _, p in paths])
                for (c, _), v in zip(paths, flags):
                    c.has_visual = c.has_visual or v
                assign_tiers(cands)   # 화면근거 반영 재-tier
        finally:
            shutil.rmtree(wd, ignore_errors=True)

    cands.sort(key=lambda c: (c.tier, -c.score, -c.heat))
    doc = {"topic": topic, "source": source,
           "quotes": [candidate_to_dict(c) for c in cands]}
    if out_path:
        import json as _j
        open(out_path, "w", encoding="utf-8").write(
            _j.dumps(doc, ensure_ascii=False, indent=2))
    return doc


if __name__ == "__main__":
    import argparse, json as _j
    ap = argparse.ArgumentParser()
    ap.add_argument("url")
    ap.add_argument("--topic", required=True)
    ap.add_argument("--capture", action="store_true")
    ap.add_argument("--out")
    a = ap.parse_args()
    doc = extract(a.url, a.topic, capture=a.capture, out_path=a.out)
    print(_j.dumps(doc, ensure_ascii=False, indent=2)[:2000])
    print(f"\n총 {len(doc['quotes'])}개 후보 "
          f"(T1={sum(q['tier']==1 for q in doc['quotes'])} "
          f"T2={sum(q['tier']==2 for q in doc['quotes'])} "
          f"T3={sum(q['tier']==3 for q in doc['quotes'])})")
```

- [ ] **Step 4: 통과 확인**

Run: `python -m pytest tests/yt_agents/test_quote_extractor.py -v`
Expected: 전체 PASS

- [ ] **Step 5: 실URL E2E 스모크 (스파이크의 최종 검증 — "진짜 되는가")**

Run:
```bash
python -m quote_extractor "https://www.youtube.com/watch?v=6fehifjxOcs" --topic "반도체 고점인가" --out scratch_quotes.json 2>&1 | tail -20
```
(cwd = `scripts/yt_agents`, 또는 `python scripts/yt_agents/quote_extractor.py ...`)
Expected: `scratch_quotes.json` 생성 + 후보 개수·tier 분포 출력. **골든 발언 텍스트가 실제로 주장+근거를 담고 있는지 눈으로 검수**([[feedback_verify_with_real_data]]). capture 경로는 `--capture`로 별도 1회 확인.

- [ ] **Step 6: 스파이크 결과 기록 + 커밋**

`docs/superpowers/specs/2026-07-05-전문가인용몽타주-design.md` §9(미확정)에 실측 결과 3줄 추가:
- yt-dlp 자막 제공: O/X (Task3 스모크)
- heatmap(T2) 제공: N buckets (Task2 스모크)
- 골든 채점 품질: 육안 검수 소감

```bash
git add scripts/yt_agents/quote_extractor.py tests/yt_agents/test_quote_extractor.py "docs/superpowers/specs/2026-07-05-전문가인용몽타주-design.md"
git commit -m "feat(quote-engine): E2E 오케스트레이터+CLI, 스파이크 실측 기록"
```

---

## Self-Review

**Spec coverage:**
- §4.2 엔진 흐름(URL→자막→Gemini 채점→프레임 비전) → Task 2·3·4·6·7 ✅
- §5.1 골든 루브릭(필수3+가점) → Task 4(build_score_prompt/parse_score_reply) ✅
- §5.2 3계층 tier(T1>T2>T3) + heatmap → Task 5 ✅
- §6 quotes.json 스키마 → Task 7(candidate_to_dict) ✅ (스파이크는 `quotes_candidates.json`, 픽 전 후보 단계)
- T2 heatmap 실측 → Task 2 Step6 스모크 ✅
- 범위 밖(의도적): 스튜디오 UI, 인용 픽, 클러스터/스토리, 씬빌드/렌더 → 후속 플랜.

**Placeholder scan:** 모든 코드 스텝 실코드 포함, TODO/TBD 없음. Whisper 폴백은 "범위 밖"으로 명시적 제외(placeholder 아님). ✅

**Type consistency:** `Segment(start,text)`, `QuoteCandidate(source,ts,text,stance,evidence,score,reasons,has_visual,heat,tier,media)` 전 태스크 일관. `parse_heatmap`→`[{start,end,value}]`가 `apply_heatmap`에서 동일 키 사용. `to_mmss`/`ts_sec` 출력 일관. ✅
