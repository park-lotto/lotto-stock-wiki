# B1프레임태깅 — 핸드오프

_최종: 2026-09-04 밤 · 상태: **1·2·3단계 전부 main 병합, 전부 스위치 뒤(기본 꺼짐) — 라이브 동작 불변.** 다음은 사장님 실사용·서버 실측_
_설계·실측 원문: `docs/superpowers/specs/2026-09-04-3단계매칭-파서재설계-design.md` §9 (이 트랙에 같이 커밋)_

## 무엇을 · 왜

사장님(09-04): "3단계 매칭이 안 맞는다 → 근본 대책 → 1단계부터 차근차근." 1단계 정확도를 로컬 소스 4편(세그 52개)으로
재 보니 **묘사가 틀린 게 아니라 묘사가 붙은 칸이 한 칸 밀려 있었다**(통째 업로드 방식이 컷 목록에 묘사를 배정하며
한 칸 밀거나 한 컷을 건너뜀). 컷별 프레임 태깅(B1, 07-29)은 받은 프레임을 묘사하므로 이 오배정이 구조적으로 불가능
→ 켜야 하는데, **B1은 설계대로 돈 적이 없었다.**

## 고친 것 (`shopping_shorts/frame_script.py`, `tag_qa_frames.py`)

| # | 버그(실측) | 수리 |
|---|---|---|
| 1 | `extract_frame_at(video, dir, mid)`를 파일명 없이 불러 `frame_hint.jpg`에 전부 덮어씀 → 모든 컷이 **마지막 프레임 한 장**으로 태깅 | 구간·장마다 `seg{i:03d}_{j}.jpg` |
| 2 | `_default_boundaries`가 `detect_cuts`의 `(start_frame,end_frame)` 튜플을 `float()` → TypeError 삼킴 → **항상 구간 1개** | fps로 초 변환(옛 초 형식도 수용) + `split_long_spans`(원테이크 상한 `MAX_SPAN_SEC=7`) |
| 3 | `response_schema`(enum) 강제 호출이 gemini-3.5-flash에서 **120초 초과**(판정 4/4·태깅 1/1 504) → 옛 추출로 조용히 폴백 | 스키마 없이 JSON만 받고 `normalize_tags`가 값 검증(shot_role→`shot_roles.normalize`). 모델 순서 `TAG_MODELS=(lite, 3.5-flash)` **한 곳** — `tag_qa_frames._judge`도 같은 상수 |
| 4 | (2차 실측) 컷당 프레임 2~3장을 따로 실으면 모델이 **이미지 1장=구간 1개**로 세어 묘사가 2배 압축·밀림 | 시작·중간·끝 3장을 **띠 한 장**(`make_strip`, PIL)으로 합쳐 **구간당 이미지 1장**. 띠 실패면 중간 한 장 |

`tag_frames(frame_groups, caption, segs)` — 이제 **구간별 묶음(list of list, 길이 1)**. 옛 평평한 목록도 받는다.

## 실측 (로컬 잡 409f894230c6, 4편, 예비풀 키·Whisper 없음)

| 소스 | 기존 세그 | 수리 B1 | 눈으로 대조 |
|---|---|---|---|
| s0 | 12 | 12 / 10초 | 11.37 "스패출러로 크림 질감"(✓ 종전 "말차 녹색"✗) · 12.27 "말차와 초코 섞는"(둘 다) · 14.07 "3종 완성 쿠키"(순서까지) |
| s3 | 24 | 27 / 26초 | 12.73 "레시피 재료 명시"(✓ 종전 "버터 누름"✗) · 13.87 "버터 손가락으로 확인"(✓) |
| s2 | 15 | 25 / 25초 | 컷 21개 전부 살림(미검안) |
| s1 | 13 | **14** / 23초 | 원테이크(컷 3개) → 6.4초 균등 분할, 흐름 읽힘(종전 B1은 51초 1구간) |

테스트: `tests/test_b1_frame_fixes.py` 12건 + 기존 frame_script·tag_qa_frames 갱신 = 48 passed.
프로브: 세션 scratchpad `stage1_probe.py`(정확도 판정) · `contact_sheet.py`(시작·중간·끝 띠 눈검사) · `b1_track_probe.py`(트랙 코드 실행).
⚠️로컬 함정: SHORTS 풀 0개(`_current_key_and_idx` None → 조용히 빈 값, 예비풀 키로 우회) / 한글 절대경로는 ffmpeg stderr cp949 크래시(ASCII 상대경로로).

## ⏭ 남은 것 (순서대로 — 사장님과 하나씩)
1. **서버 실측**: 최근 소스 30편에 수리 B1 vs 기존 추출 정확도(`tag_qa_frames` 전수) — 회사 PC SSH 22 막힘, 집 PC/사장님.
2. **말 트랙**: 서버 GROQ 키로 Whisper 단어 전사 실측(이번엔 못 봄) + **플랫폼 자막 트랙 받기**(`media_download`에 없음) + 외국어 원문·번역 둘 다.
3. **1차 브리프(스토리)** → 2차 컷 묘사에 브리프 주입(설계 §9-2). 무대본 외국 영상의 맥락 문제.
4. 켜기: `frame_extract_enabled=1` + 믹스 경로(`mix_pipeline.py:1053` `_use_frames`)도 같은 플래그인지 확인.
5. 그 다음 2단계(본 것만 쓰기 + 줄마다 장면 필수 + 게이트), 3단계(추측 층 삭제).

## ▶ 다른 PC에서 이어서 하는 법 (2026-09-04 저녁 기록)

**말할 것**: `"매칭 파서 이어서"` 또는 `"B1프레임태깅 이어서"`
→ Claude가 할 일: `py tools/find_work.py 매칭 파서` → 이 파일 + 설계서 §9 읽기 → `.tracks/B1프레임태깅` 없으면
`py tools/track.py start B1프레임태깅` → 아래 순서 1번부터.

**어디에 뭐가 있나**
| 것 | 위치 | 비고 |
|---|---|---|
| 설계·실측 전체 | `docs/superpowers/specs/2026-09-04-3단계매칭-파서재설계-design.md` §1~§10 | §9 = 1단계, §10 = 대본 퀄리티(별도 트랙) |
| 수리한 코드 | `shopping_shorts/frame_script.py`, `tag_qa_frames.py` (main `24c2648e8`) | 플래그 `frame_extract_enabled` **OFF** |
| 측정 스크립트 | `tools/probes/stage1_frame_accuracy.py`(묘사↔프레임 판정) · `contact_sheet.py`(눈검사 띠) · `b1_extract_probe.py`(수리 B1 실행) | 결과는 `out/probes/` |
| 이 PC 메모리 | `~/.claude/projects/.../memory/reference_B1프레임태깅_조용한버그3_띠합치기.md` | **메모리는 PC마다 따로다** — 핵심은 전부 이 파일과 설계서에 있다 |

**PC별 함정(둘 다 이 파일에서 우회 코드 있음)**
- SHORTS 풀 키가 0개인 PC(.env에 SHORTS_GEMINI_KEY 없음)는 `comment_gen._current_key_and_idx()`가 None → 판정·태깅이
  **로그 한 줄만 남기고 빈 값**. 프로브는 `keyroute.gemini_keys("general")[0]`로 우회한다.
- 한글 절대경로로 ffmpeg를 부르면 stderr cp949 크래시(`scene_cut._boundary_frames`) → 프로브는 저장소 루트로 chdir 후 ASCII 상대경로.
- 회사 PC는 서버 SSH 22 타임아웃(메모리 `reference_SSH막힘_사이트는정상`). 서버 실측은 되는 PC에서.
- 로컬 DB(`shopping_shorts/data/reference.db`)에 잡이 1개(409f894230c6, 08-15)뿐. 30편 실측은 서버 DB/영상 필요.

**지금 상태(09-04 밤) — 코드는 다 들어갔다. 남은 건 켜서 재는 것**
| 단계 | 스위치(관리자 설정 `/api/admin/settings`) | 켜기 전 할 것 |
|---|---|---|
| 1단계 B1 컷별 프레임 태깅(두 번 보기·번역) | `frame_extract_enabled=1` | 서버 30편에 `tools/probes/b1_extract_probe.py` + `stage1_frame_accuracy.py`로 기존 vs B1 정확도 |
| 2단계 본 것만 쓰기 | `script_grounded_enabled=admin` | 없음 — 사장님 계정만 켜서 대본 뽑아 보기 |
| 3단계 붙어 온 장면 그대로 | `edl_inherit_enabled=admin` | 2단계와 **같이** 켠다(출처가 와야 상속) |

**순서**
1. 사장님 계정만 2·3단계 켜기(`admin`) → 제품형 소스로 2단계 대본 확정 → 3단계 칸타임라인에서 **컷 바꾼 횟수** 세기(이게 시험지).
2. 서버(SSH 되는 곳)에서 1단계 30편 실측 → B1 우위면 `frame_extract_enabled=1`.
3. 손 횟수가 만족스러우면 2·3단계 `1`(전체). 한 달 무사고 뒤 옛 층(respine·verify·repick·reconcile) 삭제.
4. 별도: 플랫폼 자막 트랙(릴레이 에이전트), 대본 퀄리티 트랙(설계 §10 — 모델 급 비교·선택 기록).

## ✅ 2차 (2026-09-04 밤) — 두 번 보기(1차 브리프 → 2차 컷 태깅) + 기존 추출과 같은 필드

사장님 질문("대사 없는 외국 영상, 컷만 따로 보면 못 쓰지 않나")의 답을 코드로. 설계 §9-2.

| 바꾼 것 | 위치 |
|---|---|
| **1차 브리프**: 컷 대표 프레임 격자 한 장(칸마다 실제 초 표기) + 캡션 + 전사 → `source_brief{product, role, core, summary, flow, confidence}` | `frame_script._gemini_story_brief`, `make_grid(times=)` |
| **2차 태깅에 브리프 주입** (`brief_block`) — "큰 그림 안에서 label을 정해라", 불명이면 보수 지시 | `_gemini_tag_frames(…, brief)` |
| **필드 정의 공용화(0순위-B)**: `_PROMPT`에서 `_BRIEF_GUIDE`·`_SEG_FIELD_GUIDE`를 뽑아 통째 업로드 추출과 B1이 **같은 문장** 사용. B1이 label·use_point·action·change·has_effect도 낸다(종전엔 셋이 빈칸 = 3단계 인벤토리 재료 손실) | `script_extract.py`(내용 불변, 위치만) |
| `_BRIEF_KEYS`에 flow·confidence 추가(스키마·정규화) | `script_extract.py` |
| **배치 호출** `TAG_BATCH=12` — 필드 8개×27구간 한 호출은 lite가 504·JSON 깨짐으로 상위 모델 폴백(80~90초). 12구간씩이면 s3 27구간 68초 | `_gemini_tag_frames` |
| `loads_lenient` — JSON 뒤 쓰레기·코드펜스 허용(실측 "Extra data: line 17"). 판정기도 공용 | `frame_script`, `tag_qa_frames` |

실측(로컬 4편): 브리프 flow가 실제 시각과 일치(s3 "0~13초 시식 → 13~41초 반죽 → 41~55초 굽기 → 55~69초 시식" — 재료표 12.73초·짜기 42.83초·오븐 49초와 맞음). 컷 짝은 그대로 정확(s3 12.73 "레시피 재료 소개", 13.87 "버터 눌러보고 설탕 붓는다").
테스트 64 green(`test_b1_frame_fixes.py` 18건). 프로브: `tools/probes/b1_extract_probe.py`가 2패스 결과(브리프 포함)를 낸다.
⚠️ 남은 것: 원테이크 강제분할 구간(6.4초)이 여러 동작을 담아 묘사가 길어짐 — 상한을 5초로 줄일지는 서버 실측 후.

## ✅ 3차 (2026-09-04 밤) — 말 트랙: 언어 자동 감지 + 외국어 한국어 번역(text_ko)

| 바꾼 것 | 위치 |
|---|---|
| `transcribe_words(mp3, language="ko")` — **None이면 언어 자동 감지**. 기본값은 종전 "ko"(TTS 검수용, 회귀 0). B1은 None으로 부른다 | `asr_check.py`, `frame_script.extract_script_frames` |
| 전사가 외국어(한글 비율 <30%)면 한 호출로 구간별 번역 → `text_ko`. 원문 `text`는 보존. 결과에 `full_text_ko` | `frame_script.is_foreign_text`, `_gemini_translate`(injectable `translate=`) |
| `_assign_seg_ids`가 `text_ko`를 싣는다 | `script_extract.py` |
| 3단계 인벤토리 `말:` · 2단계 장면 목록 `말:` · 대본 재료 `full_text`가 **text_ko/full_text_ko 우선** | `edit_plan._build_inventory`, `script_generate._mix_source_block`, `app.py`(대본 재료 조립 1곳) |

실측: `_gemini_translate(["Mix the softened butter with 90g powdered sugar", "", "把黄油打发到发白，加入蛋清", "Bake at 170°C for 12 minutes"])`
→ `['부드러워진 버터에 슈가파우더 90g을 넣고 섞어주세요', '', '버터를 하얗게 될 때까지 휘핑한 뒤, 달걀흰자를 넣어주세요', '170°C에서 12분 동안 구워주세요']`.
Whisper 자동 감지는 로컬 GROQ 키가 없어 **모킹 테스트만**(서버 실측 필요). 테스트 +6.

**⏭ 플랫폼 자막 트랙 받기는 미착수** — 서버는 유튜브를 사장님 PC 릴레이(`_download_via_relay`)로 받는 구조라 자막도 릴레이 에이전트가
같이 받아 올려야 한다(yt-dlp `--write-subs --write-auto-subs`). 인스타(Apify)·틱톡은 자막 트랙 자체가 드물다. Whisper 자동 감지가 먼저 서버에서 검증되면 그때.

## ✅ 4차 (2026-09-04 밤) — 2단계 '본 것만 쓰기'(grounded) — **플래그 뒤, 기본 꺼짐**

사장님: "제품형 대본은 소스 영상에 보이는 것으로만 써야 한다. 대본을 썼는데 장면이 없다는 건 말이 안 된다."

| 바꾼 것 | 위치 |
|---|---|
| 스위치 `script_grounded_enabled`: ""/"0" 끔(기본) · `admin` 관리자만 · `11,42` 고객 목록 · `1` 전체. 관리자 설정(`/api/admin/settings`) 허용 키에 추가(`frame_extract_enabled`도) | `app._script_grounded`, `_ADMIN_SETTING_KEYS`, 호출부 2곳 |
| 장면 목록 **전부**(상한 60) + 쓰임·변화·활용, 말은 text_ko 우선 | `script_generate._mix_source_block(full_scenes=True)`, `_scene_line_full` |
| 규칙 `_GROUNDED_RULE`: 장점·효과·동작·결과 줄은 src_seg 필수(needs_scene=true), 훅·감정·연결·가격·약속은 면제, 없는 번호 금지, 여러 장면은 쉼표(첫 번째가 대표). 레시피는 느슨 | `script_generate.generate_one_style(grounded=)` |
| 게이트 **'장면 근거'**(지시와 판정은 짝): 지어낸 번호 반려 · needs_scene인데 빈칸 반려 · 제품형은 3분의 1 이상 장면 | `script_gate.scene_grounding_check`, `parse_src_segs` |
| 스키마 `needs_scene`(선택) · 산출 `src_seg`=대표, `src_segs`=전체 | `_STYLE_SCHEMA`, generate_one_style 끝 |

실측(로컬, 실제 모델·스타일 블록은 합성): 5칸(첫말·문제·시연·결과·약속) 대본에서 시연 `s3-8,s3-9,s3-10,s3-16`(믹서·체·짜기),
결과 `s3-19,s3-20,s3-3,s3-24`(오븐·단면) — **장면이 붙은 줄의 내용이 실제 장면과 맞았다.** 모델이 한 줄에 장면을 여럿 적어
처음엔 게이트가 통째로 "없는 번호"로 반려 → 쉼표 목록 허용. 절반 비율은 5칸 구조에서 시연·결과가 맞아도 2/5로 반려 → 3분의 1.
플래그 밖 프롬프트·게이트는 종전과 글자 단위로 같다(테스트가 고정). 테스트 `test_script_grounded.py` 11건.

**⏭ 2단계 남은 것**: ①서버에서 사장님 계정만 켜서(`admin`) 실사용 → 손대는 횟수 세기 ②조립 경로(`_assembled_drafts`, 스파인 조립)는
grounded 밖 — 켜면 생성기 경로만 적용됨 ③3단계는 이 src_seg를 `_apply_beat_sources`가 받는다(자리 잇기 수리 `track/출처장면배선` 미병합 — 함께 봐야 함).

## ✅ 5차 (2026-09-04 밤) — 3단계 '붙어 온 장면 그대로 쓰기'(inherit) — **플래그 뒤, 기본 꺼짐** + 출처 자리잇기 병합

| 바꾼 것 | 위치 |
|---|---|
| `build_inherit_plan(source_scripts, given_script, beat_sources)`: 줄=비트, 출처 있는 줄은 primary=대표·alternates=나머지, 없는 줄(훅·감정·가격·약속)은 앞 비트 장면의 **다음 컷**(같은 소스·시간순·미사용)으로 b-roll(`inherited=False, fit=3`). **Gemini 0회. respine·verify·repick·reconcile 호출 없음**(테스트가 고정). 화면 길이 보루만 유지. 못 이으면 None → 옛 경로 | `edit_plan.build_inherit_plan` |
| 잡의 `script_structure`가 `_plan_and_tts`까지 온다(호출부 2곳). `inherit_scenes`+`beat_sources`면 상속, 아니면 종전 | `mix_pipeline._plan_and_tts` |
| 스위치 `edl_inherit_enabled`(값 규약은 2단계와 같음) → 믹스 잡 생성 시 `script_structure.inherit_scenes=True` 표식. `_setting_gate` 공용(2단계 `_script_grounded`도 이걸 쓴다) | `app._setting_gate`, 믹스 생성 API, `_ADMIN_SETTING_KEYS` |
| 2단계 확정이 `segs`(장면 전부)도 beat_sources에 실어 보낸다 | `produce.html` s2Confirm |
| **`track/출처장면배선` d4d2fd06e 체리픽** — 저장 출구 `_apply_beat_sources`가 역할 이름 대신 **자리**로 잇는다(3%→55%). 플래그 없이 종전 경로에도 적용(그 브랜치의 테스트 포함) | `store.py` |

테스트 `test_inherit_plan.py` 5건 + `test_beat_source_follow.py`(체리픽) green. 앱 임포트·produce.html 인라인 스크립트 문법 OK.
켜는 법: 관리자 설정 `edl_inherit_enabled=admin`(사장님만) — 2단계 `script_grounded_enabled=admin`과 **같이** 켜야 출처가 온다.
그러면 1단계(B1은 아직 OFF·별도) → 2단계 본 것만 쓰기 → 3단계 상속이 사장님 계정에서 한 줄로 이어진다.

**⏭ 남은 것**: ①서버에서 사장님 계정 실사용(2·3단계 admin) → 3단계 화면에서 컷 바꾸는 횟수 세기 ②B1 서버 30편 실측 후 `frame_extract_enabled=1`
③상속 경로에서 옛 층 코드 삭제는 한 달 무사고 뒤(설계 §"통과하면") ④조립 경로(`_assembled_drafts`)는 grounded 밖.

## ✅ 6차 (2026-09-04 밤) — 시험지 자동화: 3단계 교체 기록(scene_swaps) + 화면 배지 + 대본 A/B 프로브

- 종전 `swap_log` API는 화면이 **한 번도 안 불러** 비어 있었다(grep 0). → 서버가 `scene_lab/apply`에서 적용 전후 **첫 조각이 바뀐 비트**를
  DB `scene_swaps`에 자동 기록(`edit_plan.scene_swap_rows`, `store.add_scene_swaps`). 픽 로직엔 안 쓴다(08-29 사장님 확정).
- 점수판: `GET /api/admin/scene_swaps?days=30` → 생성기별 {jobs, swaps, per_job}(잡당 손 횟수 = 시험지 점수) + 최근 행.
- 3단계 화면 배지: `📎 2단계 장면`(inherited) / `🎞 b-roll`(broll_fill) — 어느 컷이 상속인지 보인다.
- `tools/probes/script_ab_samples.py`: 같은 소스로 lite vs 3.5-flash 대본을 눈가림 짝(A/B)으로 → `out/probes/script_ab/<job>.md` + 정답 json(§10-1).
테스트 `test_scene_swaps.py` 3건. ⚠️실제 화면에서 컷을 바꿔 행이 쌓이는 것까지는 **못 돌려봤다**(로컬 DB 잡 1개·UI 조작 미실시) — 서버 실사용 때 확인.
- **실측(09-05 새벽, 로컬 앱 :8899, 인증 OFF=관리자)**: `POST scene_lab/409f894230c6/apply` beat3 `["s2-3","s2-12"]` → `{"applied":1,"swapped":1}`,
  `GET /api/admin/scene_swaps` → 행 1건(old s3-4 → new s2-3, per_job 1.0). 같은 편성 재적용 → `swapped 0`. `revert` OK.
  `/produce`에 배지 마크업 서빙 확인(문자열 1건). **브라우저에서 버튼을 눌러 본 것은 아니다**(API 호출 실측).

## ✅ 7차 (2026-09-05 새벽) — 정확도 숫자(같은 판정기·같은 4편 75구간) + 띠에 구간 번호

| 방식 | 판정기(lite, 스키마 없음) | 눈검사 |
|---|---|---|
| 기존 통째 업로드(08-15 추출본) | 45/52 = **86%** | 11건 중 7 맞음·2 부분·2 틀림(한 칸 밀림) |
| B1 띠·1호출(배치 전) | 42/75 = **56%** | s3 10번~·s2 17번~ 한 칸씩 밀림(띠 하나 건너뛰면 뒤가 전부 밀림) |
| **B1 배치 12 + 브리프** | 75/75 = **100%** | s2 16~24번 9구간 전부 일치(컨택트 시트) |
| **B1 + 띠에 `#번호 초` 표기**(현재 코드) | 75/75 = **100%** | 회귀 없음 |

★교훈: "고쳤다" 직후 재지 않았으면 56%짜리를 켰을 것이다. 판정기(lite)는 관대하니 절대값보다 **같은 잣대의 상대 비교**로 읽어라.
띠 번호(`make_strip(label=)`): 모델이 이미지 순서를 세지 않고 그림에 박힌 #번호로 seg_no를 답한다(전체 번호 → 묶음 오프셋 되돌림).
로컬 앱 헤드리스 브라우저 실측: 3단계 편집안에 배지 "2단계 장면"·"b-roll" 렌더, JS 오류 0.
⚠️ s3(27구간)는 실행마다 133~280초로 흔들린다(lite 실패→3.5-flash 폴백 시). 서버 실측 때 시간도 같이 재라.
