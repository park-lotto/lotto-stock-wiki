# 즐겨찾기복구
- 2026-09-04 즐겨찾기·랭킹 카드 전부 빈 화면(전 회원). 뿌리=오늘 병합된 '🛒 쿠팡에 있나?' 버튼의 `slice(0,60)`이 이모지 서로게이트를 반으로 잘라 `encodeURIComponent`가 URIError → render 통째 중단. 데이터 무사(API 532KB). `cpKw()`(코드포인트 단위 자르기+try/catch)로 두 페이지 수정.
