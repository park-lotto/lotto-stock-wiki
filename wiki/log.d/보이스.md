# 보이스 — 작업 로그

- 2026-07-15 — [리뷰·수정·배포] **자연화엔진 whole-branch 최종리뷰 → seam 10건 전건 수정**(`13eaf79c`). 근본원인=튜닝 작업대와 실렌더가 **각자 합성 파이프라인을 조립**해 인자가 갈림: 동결 프로파일이 렌더에 0%반영(S1·조용한 실패) / 재기동마다 프로파일 NULL 증발(S2, startup seed가 excluded로 덮음→COALESCE) / 작업대가 기본성우 Rachel로 합성(S3) / 렌더에 N-best ranker 미배선=크레딧만 n배·take0 고정(S4) / 작업대 speed·후처리 누락(S5) / beat_index 불일치(S6) / **튜닝 API 공개도메인 무인증 노출**(S7) / 미리듣기 v2 vs 렌더 v3(S8) / 재롤 캐시히트(S9) / seed·n_best를 merge 전 raw dict에서 읽음(S10). 해법=공유 단일경로 `mix_pipeline.synthesize_line()`. **회귀테스트 3종 신규** — preset→job스냅샷→렌더를 통과하는 테스트가 없어 S1이 유닛리뷰를 통과했음(그 seam을 잠금). 469통과·신규실패0, 실서버DB grounding으로 S1/S2/S2b/S7 FIXED 확인. 교훈: whole-branch 리뷰 생략했으면 사장님이 귀로 맞춘 값이 영상에 하나도 반영 안 됐을 것([[feedback_whole_branch_review_catches_seams]]).
- 2026-07-15 — [수정] **eleven_v3 연속성 400**(`7e053a18`). 핸드오프의 "미지원이면 payload 무시라 안전" 가정이 **틀림** — 실측 프로브: text+model 200 / +voice_settings 200 / +seed 200 / **+previous_text 400**(`not yet supported with the 'eleven_v3' model`). tts.py가 v3에서 자동 drop(speaker_boost와 같은 자리)+회귀테스트. 작업대 ▶합성 전면실패의 직접원인이었고, **렌더 경로도 진작 같은 400으로 죽고 있었을 것**. GROQ_API_KEY 서버 추가·실동작 확인(서비스 프로세스 environ까지 확인 — 내 셸 env로만 테스트하면 서비스가 키를 갖는지 증명 안 됨).
- 2026-07-15 — [결함발견] **강도 슬라이더가 실제로 안 먹음**(사장님 관측→재현). intensity 0.3/0.7/1.0 출력이 완전 동일, 0에서만 off = on/off 스위치. 원인: 추임새·감정곡선=`<0.15` 임계형+cap 1 / 끝음=마침표를 …로 바꾸는데 **코퍼스 10줄에 마침표 0개** → 영원히 무발동 / 구어체=`int(hits×intensity)`라 짧은 문장은 1.0에서만 발동 / 끊어읽기=연결어미 없는 문장엔 무효 / 발음사전 비어있음 / **억양은 미구현**(`return text`). 8개 중 실동작 3개뿐. **유닛테스트가 통과한 이유=각 스테이지를 자기한테 맞는 입력으로 격리 테스트, 진짜 코퍼스로 통과시킨 적 없음** — 배선이 아닌 "내용의 seam". 핸드오프의 "규칙 정교화는 코드수정 최소" 가정도 틀림. 사장님 승인=제대로 재설계(비례형 강도·코퍼스 현실화·억양 구현). 상세=handoff/보이스.md
