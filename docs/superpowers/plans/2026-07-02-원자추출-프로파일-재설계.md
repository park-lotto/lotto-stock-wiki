# 원자추출 프로파일 재설계 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 원자 추출 파이프라인이 소스타입 단위(텔레/유튭/리포트/블로그/뉴스)로만 질문지를
분기하던 걸, 채널/콘텐츠 성격 단위로 분기하도록 하고, 지금 조용히 유실되는 슬롯을
`structured_fields`에 보존한다. 새 채널 추가 시 반드시 거치는 온보딩 루프를 만든다.

**Architecture:** `pipeline/atoms/db.py`의 `atoms` 테이블에 `structured_fields`(JSON TEXT)
컬럼을 추가하고, 기존 각 소스의 원자 변환 함수들이 프로필별 구조화 슬롯을 이 컬럼에
채우도록 확장한다. 유튜브는 신규 `profiles.py`로 채널별 프로필(데이트레이딩 등)을
선택하는 분기를 추가한다. 텔레그램/포스트형이 공유하는 `questionnaire_to_atoms_tg()`의
insight 타입 유실 버그(leading_sectors/noise_ratio/quote)를 고쳐서 텔레·유튭·블로그·
뉴스 4개 소스가 동시에 혜택을 받는다. 마지막으로 채널 온보딩 스킬(마크다운 가이드)을
만들고, 사용자가 지정한 우선순위 텔레그램 2채널(태린이아빠 주식투자/요약하는 고잉)에
직접 실행한다.

**Tech Stack:** Python, SQLite(`atoms.db`), Gemini API(`google-genai`), pytest

## Global Constraints

- 이번 스펙 범위: 공통틀(DB+프로필레지스트리) + 유튜브 마이그레이션 + 리포트 마이그레이션
  + 텔레그램 우선순위 2채널(**태린이아빠 주식투자**, **요약하는 고잉**) 소급 재검토만.
  블로그·뉴스 프롬프트 자체 변경, 텔레그램 나머지 채널, `youtube_ingest.py` 삭제는
  이번 스펙 범위 밖(다음 스펙).
- 프로필 판정은 **하이브리드**: registry.json에 채널 기본 프로필 태그, 콘텐츠가 다르면
  Gemini가 콘텐츠 단위로 재판단해 오버라이드 가능.
- 질문지 슬롯은 반드시 `atoms` 컬럼이거나 `structured_fields` 키로 저장되거나, 저장하지
  않는 이유가 명시돼야 한다(조용한 유실 금지).
- 채널 온보딩 루프(STEP1~7)는 사람 확인이 들어가는 반자동 프로세스다 — 완전 자동화
  배치 스크립트로 만들지 않는다.
- 스펙 문서: `docs/superpowers/specs/2026-07-02-원자추출-프로파일-재설계-design.md`

---

### Task 1: `structured_fields` 컬럼 — DB 스키마 + insert_atom 지원

**Files:**
- Modify: `pipeline/atoms/db.py`
- Test: `pipeline/atoms/test_structured_fields.py` (신규)

**Interfaces:**
- Produces: `insert_atom(atom: dict)`가 `atom["structured_fields"]`로 `dict` 또는
  이미 직렬화된 `str`을 받아 DB에 JSON 문자열로 저장. 없으면 `NULL`.

- [ ] **Step 1: 실패하는 테스트 작성**

`pipeline/atoms/test_structured_fields.py` 새로 생성:

```python
import json
from pipeline.atoms.db import get_conn, init_db, insert_atom


def _base_atom(**kw):
    a = {
        "id": "test_sf_atom_1", "date": "2026-07-02", "source_type": "report",
        "source_name": "테스트", "source_trust": "A", "raw_file": "x.md",
        "layer": "L5", "sector": "기타", "asset": "삼성전자", "asset_level": "stock",
        "signal": "neutral", "event_type": "report", "magnitude": "minor",
        "content_type": "analysis", "strength_score": 1, "validity_type": "permanent",
        "validity_until": None, "is_active": 1, "content": "테스트 내용", "relations": [],
    }
    a.update(kw)
    return a


def test_insert_atom_stores_structured_fields_dict_as_json():
    init_db()
    insert_atom(_base_atom(id="test_sf_atom_dict",
                            structured_fields={"rating": "BUY", "tp_new": "100000"}))
    conn = get_conn()
    row = conn.execute("SELECT structured_fields FROM atoms WHERE id=?",
                        ("test_sf_atom_dict",)).fetchone()
    conn.close()
    assert row is not None
    assert json.loads(row["structured_fields"]) == {"rating": "BUY", "tp_new": "100000"}


def test_insert_atom_without_structured_fields_stores_null():
    init_db()
    insert_atom(_base_atom(id="test_sf_atom_none"))
    conn = get_conn()
    row = conn.execute("SELECT structured_fields FROM atoms WHERE id=?",
                        ("test_sf_atom_none",)).fetchone()
    conn.close()
    assert row is not None
    assert row["structured_fields"] is None
```

- [ ] **Step 2: 테스트 실패 확인**

Run: `"C:\Users\TheRose\AppData\Local\Python\bin\python.exe" -m pytest pipeline/atoms/test_structured_fields.py -v`
Expected: FAIL — `sqlite3.OperationalError: table atoms has no column named structured_fields`
(또는 `insert_atom`이 unexpected keyword `structured_fields`로 실패)

- [ ] **Step 3: 최소 구현 작성**

`pipeline/atoms/db.py`에서 `_YT_COLUMNS` 정의 바로 아래에 추가:

```python
_STRUCTURED_COLUMNS = [
    ("structured_fields", "TEXT"),
]
```

`migrate_db()`의 `for name, decl in _TG_COLUMNS + _SYNTH_COLUMNS + _YT_COLUMNS:` 줄을
다음으로 교체:

```python
    for name, decl in _TG_COLUMNS + _SYNTH_COLUMNS + _YT_COLUMNS + _STRUCTURED_COLUMNS:
```

`insert_atom()`에서 `a.setdefault("deeplink", None)` 바로 아래에 추가:

```python
    sf = a.get("structured_fields")
    if isinstance(sf, dict):
        sf = json.dumps(sf, ensure_ascii=False)
    a["structured_fields"] = sf
```

`INSERT OR REPLACE INTO atoms` SQL문의 컬럼 목록 `speaker, yt_timestamp, deeplink)`를
`speaker, yt_timestamp, deeplink, structured_fields)`로, VALUES 목록의
`:speaker, :yt_timestamp, :deeplink)`를 `:speaker, :yt_timestamp, :deeplink, :structured_fields)`로
수정.

- [ ] **Step 4: 테스트 통과 확인**

Run: `"C:\Users\TheRose\AppData\Local\Python\bin\python.exe" -m pytest pipeline/atoms/test_structured_fields.py -v`
Expected: 2 passed

- [ ] **Step 5: 커밋**

```bash
git add pipeline/atoms/db.py pipeline/atoms/test_structured_fields.py
git commit -m "feat(atoms): structured_fields 컬럼 추가 — 구조화 슬롯 유실 방지 저장소"
```

---

### Task 2: 유튜브 프로필 레지스트리 — `pipeline/atoms/profiles.py`

**Files:**
- Create: `pipeline/atoms/profiles.py`
- Test: `pipeline/atoms/test_profiles.py`

**Interfaces:**
- Consumes: 없음 (순수 함수 + `youtube_registry.json` 파일 읽기)
- Produces: `youtube_channel_profile(channel_name: str) -> str | None` — registry의
  `profile` 필드를 읽어 반환, 없으면 `None`(=기존 POST_PROMPT 경로 사용을 의미).
  `YOUTUBE_PROFILES: dict[str, dict]` — 프로필명 → `{"prompt": str, "slots": list[str]}`.
  이번 태스크에서는 아직 빈 딕셔너리로 시작(Task 3에서 데이트레이딩 프로필 채움).

- [ ] **Step 1: 실패하는 테스트 작성**

`pipeline/atoms/test_profiles.py` 새로 생성:

```python
import json
import tempfile
from pathlib import Path

from pipeline.atoms import profiles


def test_youtube_channel_profile_returns_none_when_field_missing(monkeypatch):
    fake_registry = {"업사이클": {"trust": "B", "url": "https://x"}}
    with tempfile.TemporaryDirectory() as td:
        p = Path(td) / "youtube_registry.json"
        p.write_text(json.dumps(fake_registry, ensure_ascii=False), encoding="utf-8")
        monkeypatch.setattr(profiles, "_YOUTUBE_REGISTRY_PATH", p)
        profiles._YOUTUBE_REGISTRY_CACHE.clear()
        assert profiles.youtube_channel_profile("업사이클") is None


def test_youtube_channel_profile_returns_tagged_value(monkeypatch):
    fake_registry = {"어떤채널": {"trust": "B", "url": "https://x", "profile": "데이트레이딩"}}
    with tempfile.TemporaryDirectory() as td:
        p = Path(td) / "youtube_registry.json"
        p.write_text(json.dumps(fake_registry, ensure_ascii=False), encoding="utf-8")
        monkeypatch.setattr(profiles, "_YOUTUBE_REGISTRY_PATH", p)
        profiles._YOUTUBE_REGISTRY_CACHE.clear()
        assert profiles.youtube_channel_profile("어떤채널") == "데이트레이딩"


def test_youtube_channel_profile_unknown_channel_returns_none(monkeypatch):
    with tempfile.TemporaryDirectory() as td:
        p = Path(td) / "youtube_registry.json"
        p.write_text("{}", encoding="utf-8")
        monkeypatch.setattr(profiles, "_YOUTUBE_REGISTRY_PATH", p)
        profiles._YOUTUBE_REGISTRY_CACHE.clear()
        assert profiles.youtube_channel_profile("존재안함") is None
```

- [ ] **Step 2: 테스트 실패 확인**

Run: `"C:\Users\TheRose\AppData\Local\Python\bin\python.exe" -m pytest pipeline/atoms/test_profiles.py -v`
Expected: FAIL — `ModuleNotFoundError: No module named 'pipeline.atoms.profiles'`

- [ ] **Step 3: 최소 구현 작성**

`pipeline/atoms/profiles.py` 새로 생성:

```python
"""소스별 콘텐츠 프로필 레지스트리 — 채널 성격에 맞는 전용 질문지 선택.
채널 온보딩 스킬(.agents/skills/channel-onboard/)이 이 레지스트리를 채운다."""
import json
from pathlib import Path

_DIR = Path(__file__).parent
_YOUTUBE_REGISTRY_PATH = _DIR / "youtube_registry.json"
_YOUTUBE_REGISTRY_CACHE: dict = {}

# 프로필명 -> {"prompt": Gemini 질문지 문자열, "slots": 저장해야 할 슬롯 키 목록}
# 채널 온보딩 스킬로 검증된 프로필만 여기 등록한다.
YOUTUBE_PROFILES: dict[str, dict] = {}


def youtube_channel_profile(channel_name: str) -> str | None:
    """youtube_registry.json에서 채널의 기본 프로필 태그를 조회.
    등록 안 됐거나 profile 필드가 없으면 None(기존 POST_PROMPT 경로 사용)."""
    if not _YOUTUBE_REGISTRY_CACHE:
        try:
            _YOUTUBE_REGISTRY_CACHE.update(
                json.loads(_YOUTUBE_REGISTRY_PATH.read_text(encoding="utf-8")))
        except (OSError, json.JSONDecodeError):
            pass
    entry = _YOUTUBE_REGISTRY_CACHE.get((channel_name or "").strip())
    if not isinstance(entry, dict):
        return None
    return entry.get("profile")
```

- [ ] **Step 4: 테스트 통과 확인**

Run: `"C:\Users\TheRose\AppData\Local\Python\bin\python.exe" -m pytest pipeline/atoms/test_profiles.py -v`
Expected: 3 passed

- [ ] **Step 5: 커밋**

```bash
git add pipeline/atoms/profiles.py pipeline/atoms/test_profiles.py
git commit -m "feat(atoms): 유튜브 채널 프로필 레지스트리(profiles.py) 신설"
```

---

### Task 3: 데이트레이딩 유튜브 프로필 — 질문지 + 원자 변환

**Files:**
- Modify: `pipeline/atoms/profiles.py`
- Test: `pipeline/atoms/test_profiles.py`

**Interfaces:**
- Consumes: `pipeline.atoms.sector_classify.sectors_list()` (기존 텔레/리포트 질문지와
  동일하게 섹터 목록을 프롬프트에 삽입)
- Produces: `YOUTUBE_PROFILES["데이트레이딩"]` 딕셔너리에 `"prompt"` 채워짐.
  `daytrading_atoms(q: dict, meta: dict) -> list[dict]` — Gemini가 채운 데이트레이딩
  질문지 dict를 원자로 변환. `meta`는 `{date, channel, source_type, trust, raw_file}`
  형태(기존 `post_ingest.py`의 `meta` dict와 동일 키).

- [ ] **Step 1: 실패하는 테스트 작성**

`pipeline/atoms/test_profiles.py`에 추가:

```python
def test_daytrading_atoms_maps_slots_into_structured_fields():
    q = {
        "trades": [
            {"name": "삼성전자", "entry_price": "71000", "stop_loss": "69500",
             "target_price": "75000", "hold_period": "2~3일",
             "chart_pattern": "20일선 지지 후 반등", "sector": "반도체",
             "quote": "71000원 부근에서 분할매수 진입"}
        ]
    }
    meta = {"date": "2026-07-02", "channel": "테스트채널", "source_type": "youtube",
            "trust": "B", "raw_file": "x.md"}
    from pipeline.atoms.profiles import daytrading_atoms
    atoms = daytrading_atoms(q, meta)
    assert len(atoms) == 1
    a = atoms[0]
    assert a["asset"] == "삼성전자"
    assert a["source_type"] == "youtube"
    import json
    sf = json.loads(a["structured_fields"])
    assert sf["entry_price"] == "71000"
    assert sf["stop_loss"] == "69500"
    assert sf["target_price"] == "75000"
    assert sf["hold_period"] == "2~3일"
    assert sf["chart_pattern"] == "20일선 지지 후 반등"


def test_daytrading_atoms_skips_entry_without_name():
    q = {"trades": [{"name": "", "entry_price": "1000"}]}
    meta = {"date": "2026-07-02", "channel": "테스트채널", "source_type": "youtube",
            "trust": "B", "raw_file": "x.md"}
    from pipeline.atoms.profiles import daytrading_atoms
    assert daytrading_atoms(q, meta) == []
```

- [ ] **Step 2: 테스트 실패 확인**

Run: `"C:\Users\TheRose\AppData\Local\Python\bin\python.exe" -m pytest pipeline/atoms/test_profiles.py -v`
Expected: 2개 신규 FAIL — `ImportError: cannot import name 'daytrading_atoms'`

- [ ] **Step 3: 최소 구현 작성**

`pipeline/atoms/profiles.py` 상단 import에 추가:

```python
import hashlib
from .sector_classify import sectors_list, resolve_sector
```

`YOUTUBE_PROFILES: dict[str, dict] = {}` 줄을 다음으로 교체:

```python
_DAYTRADING_PROMPT = """너는 데이트레이딩(단기매매) 유튜브 영상 자막을 '정해진 칸'에
옮겨 적는 사람이다. 판단하지 말고 칸을 채워라. 잡담·인사는 버려라.
철칙: 없으면 null(지어내지 마라) / 수치는 원문 그대로 /
각 매매에 quote(근거 원문 문장)와 sector를 붙여라. sector는 아래 목록에서 정확히
하나(모르면 "기타"): """ + " / ".join(sectors_list()) + """

## 칸
trades: [{
  name(종목명), entry_price(진입가), stop_loss(손절가), target_price(목표가),
  hold_period(보유기간, 예: "2~3일"), chart_pattern(진입 근거가 된 차트패턴·기술적 신호),
  sector, quote
}]

## 출력
trades 배열만 채운 JSON 1개. 다른 텍스트 금지."""

YOUTUBE_PROFILES: dict[str, dict] = {
    "데이트레이딩": {
        "prompt": _DAYTRADING_PROMPT,
        "slots": ["name", "entry_price", "stop_loss", "target_price",
                  "hold_period", "chart_pattern", "sector", "quote"],
    },
}


def _mk_daytrading_id(meta: dict, i: int) -> str:
    raw = f"{meta.get('channel', '')}_{meta.get('date', '')}_{meta.get('raw_file', '')}_dt_{i}"
    return "atom_yt_dt_" + hashlib.md5(raw.encode()).hexdigest()[:12]


def daytrading_atoms(q: dict, meta: dict) -> list[dict]:
    """데이트레이딩 프로필 질문지 dict -> 원자 리스트. entry_price/stop_loss 등은
    atoms 공통 컬럼이 아니라 structured_fields JSON으로 원본 그대로 보존한다."""
    atoms: list[dict] = []
    for i, t in enumerate(q.get("trades") or []):
        name = (t.get("name") or "").strip()
        if not name:
            continue
        sector = resolve_sector(name, t.get("sector"), is_foreign=False)[0][0]
        structured = {
            "entry_price": t.get("entry_price"), "stop_loss": t.get("stop_loss"),
            "target_price": t.get("target_price"), "hold_period": t.get("hold_period"),
            "chart_pattern": t.get("chart_pattern"),
        }
        content = (
            f"진입 {t.get('entry_price')} / 손절 {t.get('stop_loss')} / "
            f"목표 {t.get('target_price')} / 보유 {t.get('hold_period')} — "
            f"{t.get('chart_pattern') or ''}"
        ).strip()
        atoms.append({
            "id": _mk_daytrading_id(meta, i),
            "date": meta["date"], "source_type": meta.get("source_type", "youtube"),
            "source_name": meta["channel"], "source_trust": meta.get("trust", "C"),
            "raw_file": meta.get("raw_file", ""), "layer": "L5",
            "sector": sector, "asset": name, "asset_level": "stock",
            "signal": "bullish", "event_type": "report", "magnitude": "major",
            "content_type": "fact", "strength_score": 3, "validity_type": "event",
            "validity_until": None, "is_active": 1,
            "content": content, "relations": [],
            "structured_fields": structured,
        })
    return atoms
```

- [ ] **Step 4: 테스트 통과 확인**

Run: `"C:\Users\TheRose\AppData\Local\Python\bin\python.exe" -m pytest pipeline/atoms/test_profiles.py -v`
Expected: 5 passed

- [ ] **Step 5: 커밋**

```bash
git add pipeline/atoms/profiles.py pipeline/atoms/test_profiles.py
git commit -m "feat(atoms): 데이트레이딩 유튜브 프로필 질문지+원자변환 추가"
```

---

### Task 4: `post_ingest.py`에 유튜브 프로필 분기 연결

**Files:**
- Modify: `pipeline/atoms/post_ingest.py`
- Test: `pipeline/atoms/test_post_ingest_helpers.py`

**Interfaces:**
- Consumes: `profiles.youtube_channel_profile(name)`(Task 2), `profiles.YOUTUBE_PROFILES`,
  `profiles.daytrading_atoms(q, meta)`(Task 3)
- Produces: `ingest_post()`가 source_type=="youtube"이고 채널 프로필이 등록된 경우
  전용 프롬프트+변환 함수로 분기. 프로필 없으면(대부분의 채널) 기존
  `POST_PROMPT`+`questionnaire_to_atoms_tg` 경로 그대로.

- [ ] **Step 1: 실패하는 테스트 작성**

`pipeline/atoms/test_post_ingest_helpers.py`에 다음 테스트 함수를 추가(기존 파일에
이미 있는 import/헬퍼 패턴을 따라간다 — 파일을 먼저 Read해서 상단 import 구조를
확인한 뒤 그 스타일대로 추가할 것):

```python
def test_ingest_post_routes_youtube_daytrading_profile(monkeypatch, tmp_path):
    import json
    from pipeline.atoms import post_ingest, profiles

    monkeypatch.setattr(profiles, "youtube_channel_profile", lambda name: "데이트레이딩")

    called = {}

    def fake_extract_daytrading(md_path):
        called["path"] = md_path
        return {"trades": [{"name": "삼성전자", "entry_price": "71000",
                             "stop_loss": "69500", "quote": "진입"}]}

    monkeypatch.setattr(post_ingest, "_extract_daytrading", fake_extract_daytrading)
    monkeypatch.setattr(post_ingest, "insert_atom", lambda a: called.setdefault("inserted", []).append(a))
    monkeypatch.setattr(post_ingest, "embed_and_store", lambda a: None)
    monkeypatch.setattr(post_ingest, "_mark_processed", lambda *a, **k: None)
    monkeypatch.setattr(post_ingest, "_save_artifact", lambda *a, **k: None)

    md = tmp_path / "2026-07-02_1_테스트채널.md"
    md.write_text("**채널**: 테스트채널\n**날짜**: 2026-07-02\n", encoding="utf-8")
    cfg = {"source_type": "youtube", "dir": "raw/yt", "header_label": "채널",
           "registry": "youtube_registry.json"}

    n = post_ingest.ingest_post(md, cfg)
    assert n == 1
    assert called["inserted"][0]["asset"] == "삼성전자"
    assert json.loads(called["inserted"][0]["structured_fields"])["entry_price"] == "71000"


def test_ingest_post_falls_back_to_general_when_daytrading_finds_no_trades(monkeypatch, tmp_path):
    """하이브리드 오버라이드: 채널은 데이트레이딩 프로필이지만 이 영상은 매매
    언급이 없으면(trades 빈 배열) 일반 POST_PROMPT 경로로 폴백해야 한다."""
    from pipeline.atoms import post_ingest, profiles

    monkeypatch.setattr(profiles, "youtube_channel_profile", lambda name: "데이트레이딩")
    monkeypatch.setattr(post_ingest, "_extract_daytrading", lambda p: {"trades": []})
    monkeypatch.setattr(post_ingest, "extract_post",
                         lambda p: {"target_kind": "market", "market_direction": "혼조"})
    called = {}
    monkeypatch.setattr(post_ingest, "insert_atom", lambda a: called.setdefault("inserted", []).append(a))
    monkeypatch.setattr(post_ingest, "embed_and_store", lambda a: None)
    monkeypatch.setattr(post_ingest, "_mark_processed", lambda *a, **k: None)
    monkeypatch.setattr(post_ingest, "_save_artifact", lambda *a, **k: None)

    md = tmp_path / "2026-07-02_1_테스트채널.md"
    md.write_text("**채널**: 테스트채널\n**날짜**: 2026-07-02\n", encoding="utf-8")
    cfg = {"source_type": "youtube", "dir": "raw/yt", "header_label": "채널",
           "registry": "youtube_registry.json"}

    n = post_ingest.ingest_post(md, cfg)
    assert n == 1
    assert called["inserted"][0]["asset_level"] == "market"
```

- [ ] **Step 2: 테스트 실패 확인**

Run: `"C:\Users\TheRose\AppData\Local\Python\bin\python.exe" -m pytest pipeline/atoms/test_post_ingest_helpers.py -v -k "daytrading_profile or falls_back"`
Expected: 2개 FAIL — `AttributeError: module 'pipeline.atoms.post_ingest' has no attribute '_extract_daytrading'`

- [ ] **Step 3: 최소 구현 작성**

`pipeline/atoms/post_ingest.py`의 import 블록에 추가(`from .telegram_stance import
deactivate_prior_stance` 바로 아래):

```python
from .profiles import youtube_channel_profile, daytrading_atoms, YOUTUBE_PROFILES
```

`extract_post` 바로 아래(혹은 `_save_artifact` 함수 앞)에 새 함수 추가:

```python
def _extract_daytrading(md_path: Path) -> dict:
    """유튜브 데이트레이딩 프로필 전용 질문지 추출."""
    text = md_path.read_text(encoding="utf-8", errors="replace")
    from .atomizer import _get_client, _rotate_key
    from google.genai import types
    import json as _json
    prompt = YOUTUBE_PROFILES["데이트레이딩"]["prompt"]
    for _attempt in range(4):
        try:
            resp = _get_client().models.generate_content(
                model="gemini-3.1-flash-lite", contents=[text, prompt],
                config=types.GenerateContentConfig(response_mime_type="application/json"),
            )
            return _json.loads(resp.text or "{}")
        except Exception as e:
            _m = str(e)
            if any(c in _m for c in ("429", "RESOURCE_EXHAUSTED")):
                if "PerDay" in _m or "limit: 500" in _m:
                    if _rotate_key():
                        continue
                else:
                    import time
                    time.sleep(62)
                    continue
            print(f"  [WARN] 데이트레이딩 추출 실패: {e}")
            return {}
    return {}
```

`ingest_post()` 함수 전체를 다음으로 교체:

```python
def ingest_post(md_path: Path, cfg: dict) -> int:
    h = _parse_post_header(md_path, cfg["header_label"])
    profile = youtube_channel_profile(h["source_name"]) if cfg["source_type"] == "youtube" else None

    atoms = None
    if profile and profile in YOUTUBE_PROFILES:
        q = _extract_daytrading(md_path)
        if q and q.get("trades"):
            _save_artifact(q, cfg["source_type"], h["date"], h["title"])
            meta = {
                "date": h["date"], "channel": h["source_name"],
                "source_type": cfg["source_type"], "trust": post_trust(cfg["registry"], h["source_name"]),
                "raw_file": str(md_path),
            }
            atoms = daytrading_atoms(q, meta)
        # else: 채널은 데이트레이딩 프로필이지만 이 영상은 매매 언급이 없음(예: 시황
        # 잡담 영상) — 하이브리드 오버라이드: 일반 POST_PROMPT 경로로 폴백한다.
        # (반대 방향 — 프로필 없는 채널인데 이 영상만 데이트레이딩 — 은 이번 스펙
        # 범위 밖. 대부분 채널이 profile=null이라 항상 일반 경로를 타므로 영향 없음.)

    if atoms is None:
        q = extract_post(md_path)
        if not q or not q.get("target_kind"):
            print(f"  [WARN] 빈/미라우팅 질문지: {md_path.name}")
            _mark_processed(md_path, cfg["source_type"], 0)
            return 0
        _save_artifact(q, cfg["source_type"], h["date"], h["title"])
        meta = {
            "date": h["date"], "channel": h["source_name"], "type": q["target_kind"],
            "source_type": cfg["source_type"], "trust": post_trust(cfg["registry"], h["source_name"]),
            "raw_file": str(md_path),
        }
        atoms = questionnaire_to_atoms_tg(q, meta)

    for a in atoms:
        if a.get("stance_key"):
            deactivate_prior_stance(a["stance_key"], keep_id=a["id"])
        insert_atom(a)
        try:
            embed_and_store(a)
        except Exception as e:
            print(f"  [WARN] embed 실패: {e}")
    _mark_processed(md_path, cfg["source_type"], len(atoms))
    return len(atoms)
```

- [ ] **Step 4: 테스트 통과 확인**

Run: `"C:\Users\TheRose\AppData\Local\Python\bin\python.exe" -m pytest pipeline/atoms/test_post_ingest_helpers.py -v`
Expected: 전체 통과(신규 2개 포함, 기존 테스트도 회귀 없이 통과)

- [ ] **Step 5: 커밋**

```bash
git add pipeline/atoms/post_ingest.py pipeline/atoms/test_post_ingest_helpers.py
git commit -m "feat(atoms): post_ingest가 유튜브 채널 프로필로 분기하도록 연결"
```

---

### Task 5: `youtube_registry.json`에 `profile` 필드 추가 (전부 null — 동작 변화 없음)

**Files:**
- Modify: `pipeline/atoms/youtube_registry.json`
- Test: `pipeline/atoms/test_profiles.py`

**Interfaces:** 없음 (데이터 파일만 변경)

- [ ] **Step 1: 실패하는 테스트 작성**

`pipeline/atoms/test_profiles.py`에 추가:

```python
def test_real_youtube_registry_entries_have_profile_key():
    """모든 채널이 profile 키를 명시적으로 갖고 있어야 한다(null이어도 됨) —
    온보딩 스킬이 값을 채우기 전까지는 null로 '아직 검토 안 됨'을 표시."""
    import json
    from pathlib import Path
    reg_path = Path(__file__).parent / "youtube_registry.json"
    reg = json.loads(reg_path.read_text(encoding="utf-8"))
    missing = [name for name, v in reg.items() if "profile" not in v]
    assert missing == [], f"profile 키 없는 채널: {missing}"
```

- [ ] **Step 2: 테스트 실패 확인**

Run: `"C:\Users\TheRose\AppData\Local\Python\bin\python.exe" -m pytest pipeline/atoms/test_profiles.py::test_real_youtube_registry_entries_have_profile_key -v`
Expected: FAIL — `missing`에 20개 채널 전부 나열됨

- [ ] **Step 3: 최소 구현 — 마이그레이션 스크립트 실행**

아래 1회성 스크립트를 실행해 모든 기존 채널에 `"profile": null`을 추가한다(이미
`profile` 키가 있는 채널은 건드리지 않음 — 재실행해도 안전):

```bash
"C:\Users\TheRose\AppData\Local\Python\bin\python.exe" -c "
import json
from pathlib import Path
p = Path('pipeline/atoms/youtube_registry.json')
reg = json.loads(p.read_text(encoding='utf-8'))
for v in reg.values():
    v.setdefault('profile', None)
p.write_text(json.dumps(reg, ensure_ascii=False, indent=2) + '\n', encoding='utf-8')
print('done', len(reg), 'channels')
"
```

- [ ] **Step 4: 테스트 통과 확인**

Run: `"C:\Users\TheRose\AppData\Local\Python\bin\python.exe" -m pytest pipeline/atoms/test_profiles.py -v`
Expected: 전체 통과(6 passed)

- [ ] **Step 5: 커밋**

```bash
git add pipeline/atoms/youtube_registry.json pipeline/atoms/test_profiles.py
git commit -m "chore(atoms): youtube_registry.json에 profile 필드 추가(전부 null, 동작불변)"
```

---

### Task 6: 리포트 — 구조화 슬롯을 `structured_fields`로 보존 (stock/sector/market 3종)

**Files:**
- Modify: `pipeline/atoms/questionnaire.py`
- Test: `pipeline/atoms/test_questionnaire_to_atoms.py`

**Interfaces:**
- Consumes: `_stock_atom(meta, sector, name, content, *, strong, signal, i, tag,
  structured_fields=None)` (시그니처 확장)
- Produces: stock/sector/market 3종 모두 원자에 `structured_fields`가 채워짐(리포트에
  없는 슬롯은 값이 `null`인 채로 키는 존재 — "값이 없다"와 "안 뽑는다"를 구분).

- [ ] **Step 1: 실패하는 테스트 작성**

`pipeline/atoms/test_questionnaire_to_atoms.py`에 추가(파일 상단에 이미 `FIX`, `META`,
`_result_for` 헬퍼가 있으므로 재사용):

```python
def test_stock_report_populates_structured_fields():
    import json
    atoms = questionnaire_to_atoms(_result_for(0), META)  # 현대차/기아
    stock_atoms = [a for a in atoms if a["asset_level"] == "stock"]
    assert stock_atoms
    sf = json.loads(stock_atoms[0]["structured_fields"])
    assert "rating" in sf and "tp_new" in sf and "thesis" in sf


def test_sector_report_populates_structured_fields():
    import json
    atoms = questionnaire_to_atoms(_result_for(1), META)  # MLCC
    sec_atoms = [a for a in atoms if a["asset_level"] == "sector"]
    assert sec_atoms
    sf = json.loads(sec_atoms[0]["structured_fields"])
    assert "sector_view" in sf and "top_picks" in sf


def test_market_report_populates_structured_fields():
    import json
    atoms = questionnaire_to_atoms(_result_for(2), META)  # 로봇/방산/조선 데일리
    mkt_atoms = [a for a in atoms if a["asset_level"] == "market"]
    assert mkt_atoms
    sf = json.loads(mkt_atoms[0]["structured_fields"])
    assert "market_direction" in sf and "recommended_sectors" in sf
```

- [ ] **Step 2: 테스트 실패 확인**

Run: `"C:\Users\TheRose\AppData\Local\Python\bin\python.exe" -m pytest pipeline/atoms/test_questionnaire_to_atoms.py -v -k structured_fields`
Expected: 3개 FAIL — `TypeError: 'NoneType' object is not subscriptable`(structured_fields
키 자체가 없어서 `json.loads(None)`이 터짐)

- [ ] **Step 3: 최소 구현 작성**

`questionnaire.py`의 `_stock_atom` 함수를 다음으로 교체:

```python
def _stock_atom(meta, sector, name, content, *, strong, signal, i, tag,
                 structured_fields=None) -> dict:
    return _base(
        meta,
        id=_mk_id(meta["date"], meta["broker"], meta["raw_file"], tag, i),
        sector=_norm_sector(sector),
        asset=name,
        asset_level="stock",
        signal=signal,
        magnitude="major" if strong else "minor",
        strength_score=4 if strong else 2,
        content=content,
        structured_fields=structured_fields,
    )
```

`questionnaire_to_atoms()`의 `if kind == "stock":` 블록에서 `content = " / ".join(p
for p in parts if p)` 줄 바로 아래에 추가:

```python
            structured = {
                "code": s.get("code"), "rating": s.get("rating"),
                "rating_changed": s.get("rating_changed"),
                "tp_new": s.get("tp_new"), "tp_prev": s.get("tp_prev"),
                "tp_direction": s.get("tp_direction"),
                "earnings_outlook": s.get("earnings_outlook"),
                "estimate_revision": s.get("estimate_revision"),
                "next_catalyst": s.get("next_catalyst"),
                "thesis": s.get("thesis"), "valuation_basis": s.get("valuation_basis"),
                "risk": s.get("risk"), "supply_comment": s.get("supply_comment"),
            }
```

그리고 같은 블록의 `atoms.append(_stock_atom(...))` 호출을 다음으로 교체:

```python
            atoms.append(_stock_atom(
                meta, sec, name, content,
                strong=bool(s.get("tp_new")), signal=sig, i=i, tag="stk",
                structured_fields=structured,
            ))
```

`elif kind == "sector":` 블록에서 `atoms.append(_base(` 첫 번째 호출(섹터 대표 원자,
`id=_mk_id(..., "sec", 0)`)에 `content=f"[{q.get('sector_view')}] {thesis}",` 줄 바로
아래에 추가:

```python
            structured_fields={
                "sector_view": q.get("sector_view"), "thesis": q.get("thesis"),
                "timeline": q.get("timeline"), "top_picks": q.get("top_picks"),
                "risk": q.get("risk"),
            },
```

`elif kind == "market":` 블록의 첫 번째 `atoms.append(_base(` 호출(시장 대표 원자,
`id=_mk_id(..., "mkt", 0)`)에 `content=q.get("market_direction") or "",` 줄 바로
아래에 추가:

```python
            structured_fields={
                "macro_vars": q.get("macro_vars"),
                "recommended_sectors": q.get("recommended_sectors"),
                "style": q.get("style"), "event_calendar": q.get("event_calendar"),
                "top_picks": q.get("top_picks"), "risk": q.get("risk"),
            },
```

- [ ] **Step 4: 테스트 통과 확인**

Run: `"C:\Users\TheRose\AppData\Local\Python\bin\python.exe" -m pytest pipeline/atoms/test_questionnaire_to_atoms.py -v`
Expected: 전체 통과 (기존 7개 + 신규 3개 = 10 passed; 단, `test_report_stock_no_hint_falls_back_to_기타`는
2026-07-02 세션에서 이미 확인된 사전 존재 실패이므로 이 테스트만 계속 실패해도 이
태스크의 책임이 아님 — 나머지 9개는 반드시 통과)

- [ ] **Step 5: 커밋**

```bash
git add pipeline/atoms/questionnaire.py pipeline/atoms/test_questionnaire_to_atoms.py
git commit -m "feat(atoms): 리포트 stock/sector/market 구조화 슬롯을 structured_fields로 보존"
```

---

### Task 7: 텔레그램/포스트 공유 — insight 타입 유실 필드 복구

**Files:**
- Modify: `pipeline/atoms/telegram_questionnaire.py`
- Test: `pipeline/atoms/test_telegram_ingest_helpers.py`

**배경:** `questionnaire_to_atoms_tg()`는 텔레그램뿐 아니라 `post_ingest.py`(유튜브/
블로그/뉴스, Task4에서 프로필 미지정 채널)도 공유해서 쓴다. `insight` 타입의
`leading_sectors`(배열), `noise_ratio`(0~1), 최상위 `quote`(대표 한 문장)가 지금
어디에도 저장 안 되고 사라진다 — 이 4개 소스 전체가 혜택을 받는 태스크.

**Interfaces:**
- Consumes: `_base(meta, **kw)`(기존, `structured_fields` kwarg는 이미 `d.update(kw)`로
  통과됨 — `_base` 자체 수정 불필요)
- Produces: `questionnaire_to_atoms_tg()`의 insight 분기가 `leading_sectors`/
  `noise_ratio`/`quote`를 담은 요약 원자("insight_summary")를 추가로 생성.

- [ ] **Step 1: 실패하는 테스트 작성**

`pipeline/atoms/test_telegram_ingest_helpers.py`를 먼저 Read해서 기존 import/헬퍼
패턴 확인 후, 다음 테스트를 추가:

```python
def test_insight_leading_sectors_and_noise_ratio_preserved():
    import json
    from pipeline.atoms.telegram_questionnaire import questionnaire_to_atoms_tg
    q = {
        "leading_sectors": ["반도체", "2차전지"],
        "stance": [], "methods": [], "stocks_mentioned": [],
        "noise_ratio": 0.4,
        "quote": "지금은 반도체 순환매가 핵심이다",
    }
    meta = {"date": "2026-07-02", "channel": "요약하는 고잉", "type": "insight",
            "source_type": "telegram", "trust": "C", "raw_file": "x.md"}
    atoms = questionnaire_to_atoms_tg(q, meta)
    summary = [a for a in atoms if a["id"].endswith("_insightsum_0")]
    assert len(summary) == 1, f"insight_summary 원자가 안 만들어짐: {[a['id'] for a in atoms]}"
    sf = json.loads(summary[0]["structured_fields"])
    assert sf["leading_sectors"] == ["반도체", "2차전지"]
    assert sf["noise_ratio"] == 0.4
    assert sf["quote"] == "지금은 반도체 순환매가 핵심이다"


def test_insight_summary_skipped_when_all_empty():
    from pipeline.atoms.telegram_questionnaire import questionnaire_to_atoms_tg
    q = {"stance": [], "methods": [], "stocks_mentioned": []}
    meta = {"date": "2026-07-02", "channel": "요약하는 고잉", "type": "insight",
            "source_type": "telegram", "trust": "C", "raw_file": "x.md"}
    atoms = questionnaire_to_atoms_tg(q, meta)
    assert not any(a["id"].endswith("_insightsum_0") for a in atoms)
```

- [ ] **Step 2: 테스트 실패 확인**

Run: `"C:\Users\TheRose\AppData\Local\Python\bin\python.exe" -m pytest pipeline/atoms/test_telegram_ingest_helpers.py -v -k insight_leading_sectors`
Expected: FAIL — `insight_summary 원자가 안 만들어짐`(assertion 메시지로 확인)

- [ ] **Step 3: 최소 구현 작성**

`telegram_questionnaire.py`의 `elif ctype == "insight":` 블록 맨 끝, `add_stocks(q.get("stocks_mentioned"), "name", "comment")` 줄 바로 아래에 추가:

```python
        leading_sectors = q.get("leading_sectors") or []
        noise_ratio = q.get("noise_ratio")
        top_quote = q.get("quote")
        if leading_sectors or noise_ratio is not None or top_quote:
            atoms.append(_base(
                meta, id=_mk_id(meta, "insightsum", 0),
                asset_level="insight_summary", content_type="opinion",
                content=top_quote or "",
                strength_score=_strength(trust, "stance", 1),
                structured_fields={
                    "leading_sectors": leading_sectors,
                    "noise_ratio": noise_ratio,
                    "quote": top_quote,
                },
            ))
```

- [ ] **Step 4: 테스트 통과 확인**

Run: `"C:\Users\TheRose\AppData\Local\Python\bin\python.exe" -m pytest pipeline/atoms/test_telegram_ingest_helpers.py -v`
Expected: 전체 통과(신규 2개 포함, 기존 테스트 회귀 없음)

- [ ] **Step 5: 커밋**

```bash
git add pipeline/atoms/telegram_questionnaire.py pipeline/atoms/test_telegram_ingest_helpers.py
git commit -m "fix(atoms): insight 타입의 leading_sectors/noise_ratio/quote 유실 복구"
```

---

### Task 8: 슬롯 유실 방지 정적 체크

**Files:**
- Create: `pipeline/atoms/test_slot_coverage.py`

**Interfaces:** 없음 (테스트 전용, 순수 문자열 매칭 — 프로덕션 코드 아님)

- [ ] **Step 1: 실패하는 테스트로 시작 — 먼저 의도적으로 깨지는 매핑을 확인**

이 태스크는 "새 실패 테스트 작성 후 구현"이 아니라 이미 만든 프로필들이 실제로
슬롯을 다 저장하는지 검증하는 정적 체크다. `pipeline/atoms/test_slot_coverage.py`
새로 생성:

```python
"""프로필 질문지의 모든 슬롯이 원자 변환 코드 어딘가에서 실제로 읽히는지 검사.
새 프로필을 추가할 때 이 파일에도 슬롯 목록을 등록해야 한다 — 등록 안 하면
이 테스트가 실패해서 '조용한 유실'을 막는다."""
import inspect


def _source_of(fn) -> str:
    return inspect.getsource(fn)


def test_daytrading_profile_slots_all_read_in_conversion_code():
    from pipeline.atoms.profiles import YOUTUBE_PROFILES, daytrading_atoms
    slots = YOUTUBE_PROFILES["데이트레이딩"]["slots"]
    src = _source_of(daytrading_atoms)
    missing = [s for s in slots if f'"{s}"' not in src and f"'{s}'" not in src]
    assert missing == [], f"질문지 슬롯인데 변환 코드에서 안 읽는 필드: {missing}"


def test_report_stock_slots_all_read_in_conversion_code():
    from pipeline.atoms.questionnaire import questionnaire_to_atoms
    slots = ["code", "rating", "rating_changed", "tp_new", "tp_prev", "tp_direction",
             "earnings_outlook", "estimate_revision", "next_catalyst", "thesis",
             "valuation_basis", "risk", "supply_comment"]
    src = _source_of(questionnaire_to_atoms)
    missing = [s for s in slots if f'"{s}"' not in src and f"'{s}'" not in src]
    assert missing == [], f"리포트 stock 질문지 슬롯인데 변환 코드에서 안 읽는 필드: {missing}"


def test_telegram_insight_slots_all_read_in_conversion_code():
    from pipeline.atoms.telegram_questionnaire import questionnaire_to_atoms_tg
    slots = ["leading_sectors", "stance", "methods", "stocks_mentioned",
             "noise_ratio", "quote"]
    src = _source_of(questionnaire_to_atoms_tg)
    missing = [s for s in slots if f'"{s}"' not in src and f"'{s}'" not in src]
    assert missing == [], f"텔레그램 insight 질문지 슬롯인데 변환 코드에서 안 읽는 필드: {missing}"
```

- [ ] **Step 2: 테스트 실행 확인**

Run: `"C:\Users\TheRose\AppData\Local\Python\bin\python.exe" -m pytest pipeline/atoms/test_slot_coverage.py -v`
Expected: 3 passed (Task 3·6·7이 이미 모든 슬롯을 읽도록 구현했으므로 별도 구현
없이 바로 통과 — 통과하지 않으면 Task 3/6/7 구현이 불완전한 것이므로 해당
태스크로 돌아가 누락된 슬롯을 찾아 고친다)

- [ ] **Step 3: 커밋**

```bash
git add pipeline/atoms/test_slot_coverage.py
git commit -m "test(atoms): 프로필 질문지 슬롯 유실 방지 정적 체크 추가"
```

---

### Task 9: 채널 온보딩 스킬 문서 작성

**Files:**
- Create: `.agents/skills/channel-onboard/SKILL.md`

**Interfaces:** 없음 (마크다운 가이드 문서 — Claude가 대화 세션에서 직접 실행하는
반자동 프로세스. 코드/테스트 대상 아님)

- [ ] **Step 1: 스킬 디렉터리의 기존 스킬 형식 확인**

Run: `"C:\Users\TheRose\AppData\Local\Python\bin\python.exe" -c "print(open(r'.agents/skills/ingest-pipeline/SKILL.md', encoding='utf-8').read()[:500])"`

기존 `.agents/skills/*/SKILL.md`의 frontmatter 형식(name/description 등)을 그대로
따라간다.

- [ ] **Step 2: `.agents/skills/channel-onboard/SKILL.md` 작성**

```markdown
---
name: channel-onboard
description: 새 크롤링 채널을 추가하거나 기존 채널의 원자추출 질문지를 재검토할 때
  사용. 샘플 콘텐츠를 직접 분석해 채널 성격에 맞는 질문지를 설계하고, Gemini로
  시험 추출한 뒤 검수까지 마치고서야 프로덕션에 등록한다.
---

# 채널 온보딩 — 성격 분석 → 질문지 설계 → 검수 → 시험 → 재검수 → 등록

## 언제 쓰나

- 새 텔레그램/유튜브 채널을 크롤 대상에 추가할 때
- 기존 채널의 원자추출 품질이 의심될 때(정보가 새고 있다고 판단될 때)

절대 이 루프를 건너뛰고 채널을 registry.json에 등록하지 않는다.

## STEP 1 — 샘플 수집

- 유튜브: `/watch` 스킬로 그 채널의 최근 영상 3~5개를 확인(프레임+자막)
- 텔레그램: `raw/telegram/{채널명}/` 최근 메시지 파일 중 최근 20개 정도를 직접 Read

## STEP 2 — 채널 성격 분석

샘플을 보고 다음을 판단해 사용자에게 요약 보고:
- 이 채널이 전형적으로 어떤 정보를 담는가(시황요약형/종목추천형/데이트레이딩형/
  리서치형/잡담형 등)
- `pipeline/atoms/profiles.py`(유튜브) 또는 `pipeline/atoms/telegram_questionnaire.py`의
  `QUESTIONNAIRES`(텔레그램)에 이미 맞는 프로필이 있는지, 없으면 신규가 필요한지

## STEP 3 — 질문지 설계

기존 프로필을 재사용하거나, 신규 프로필을 설계한다. 신규 프로필은 반드시 다음
두 가지를 산출물로 만든다:
1. Gemini 프롬프트(질문 슬롯 목록) — Task3의 `_DAYTRADING_PROMPT` 형식을 참고
2. 슬롯 → 저장 위치 매핑표: 각 슬롯이 `atoms` 컬럼인지 `structured_fields` 키인지
   명시. **모든 슬롯이 매핑돼야 한다** — 매핑 안 되는 슬롯은 프롬프트에서 아예 뺀다.

## STEP 4 — 1차 검수

STEP3 산출물(질문지 + 매핑표)을 사용자에게 보여주고 확인받는다(`AskUserQuestion`
또는 평문으로 물어보기). 확인 전에는 다음 단계로 넘어가지 않는다.

## STEP 5 — Gemini 시험 실행

STEP1 샘플 중 3~5개에 실제로 그 질문지로 Gemini를 돌려본다(기존
`extract_telegram`/`extract_post`/`_extract_daytrading` 패턴을 참고해 임시 스크립트로
실행하거나 파이썬 인터랙티브로 직접 호출).

## STEP 6 — 2차 검수

Gemini 결과를 원문과 대조:
- 슬롯이 실제로 채워지는가(빈 값만 나오면 프롬프트가 안 맞는 것)
- `quote` 필드가 원문에 실제로 존재하는 문장인가(지어내지 않았는가)
- `pipeline/atoms/verify_questionnaire.py`의 인용대조 패턴을 참고해 직접 대조

결과를 사용자에게 보여주고 확인받는다. 실패하면 STEP3로 돌아가 질문지를 수정한다.

## STEP 7 — 등록

통과하면 registry.json(텔레그램: `telegram_channels.json`의 `type` 필드 / 유튜브:
`youtube_registry.json`의 `profile` 필드)에 프로필명을 등록한다. 신규 프로필을
만들었다면 `pipeline/atoms/profiles.py`(유튜브) 또는
`pipeline/atoms/telegram_questionnaire.py`의 `QUESTIONNAIRES`(텔레그램)에 코드로
반영하고 `pipeline/atoms/test_slot_coverage.py`에 슬롯 커버리지 체크를 추가한다.
```

- [ ] **Step 3: 문법 확인**

Run: `"C:\Users\TheRose\AppData\Local\Python\bin\python.exe" -c "
import re
t = open(r'.agents/skills/channel-onboard/SKILL.md', encoding='utf-8').read()
assert t.startswith('---'), 'frontmatter 없음'
assert 'name: channel-onboard' in t
print('OK')
"`
Expected: `OK`

- [ ] **Step 4: 커밋**

```bash
git add .agents/skills/channel-onboard/SKILL.md
git commit -m "feat(skills): 채널 온보딩 스킬 문서 신설 — 성격분석→질문지설계→검수 루프"
```

---

### Task 10 (컨트롤러 직접 실행, 서브에이전트 위임 아님): 텔레그램 2채널 온보딩 실행

**Files:** 없음 (실행 결과에 따라 `pipeline/atoms/telegram_channels.json` 수정 가능성)

**주의:** 이 태스크는 사용자 확인(STEP4/STEP6)이 필수로 들어가는 대화형 프로세스라
서브에이전트에 위임하지 않고 컨트롤러(나)가 직접 실행한다. Task 1~9 완료·리뷰 후
별도 대화 흐름으로 진행.

- [ ] **Step 1**: `/channel-onboard` 스킬(Task9 산출물)을 **태린이아빠 주식투자**
  채널에 대해 STEP1~7 실행
- [ ] **Step 2**: 같은 스킬을 **요약하는 고잉** 채널에 대해 STEP1~7 실행
- [ ] **Step 3**: 두 채널 모두, 현재 `insight` 프로필이 실제로 맞는지 / 신규 프로필이
  필요한지 결론과 근거를 사용자에게 보고
- [ ] **Step 4**: 필요 시 `pipeline/atoms/telegram_channels.json` 갱신, 커밋

---
