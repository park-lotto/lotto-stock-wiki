# OPUS 지시서 — 숏템 제작소 UI 확대(데스크톱) + 모바일 대응

> 작성: 2026-08-15 (페이블, 트랙 `제작소가로폭`)
> 전제: **가로폭 상한 제거는 이미 끝났다**(아래 §0). 이 지시서는 그 다음 작업이다.
> 대상 파일: `shopping_shorts/static/produce.html` 한 파일 (필요 시 `theme.css`)
> 작업 폴더: **`C:\Users\CH\Desktop\로또의 주식\.tracks\제작소가로폭`** — main 폴더 절대 금지

---

## 0. 이미 된 것 (건드리지 마라 · 되돌리지 마라)

`produce.html`의 가로폭 상한 3곳을 제거했다. **실측 검증 완료.**

| 셀렉터 | 전 | 후 |
|---|---|---|
| `.steps` | `max-width:1120px` | 없음 |
| `.panel` | `max-width:1120px` | 없음 |
| `.nav` | `max-width:1120px` | 없음 |

**실측 결과(Playwright, 뷰포트 2000×1100):**
- 패널 폭 1120 → **1666px**
- 1단계: 재료 레일 320px 고정 유지 ✅ / 결과 칼럼 **1306px**로 확장 ✅
- 2단계: 미리보기 400px 고정 유지 ✅ / 대본 칼럼 **1202px**로 확장 ✅
- **대본 A·B·C 카드 3장이 한 줄에 나란히**(각 389px, 전부 top=406) — 전엔 C안이 아래로 떨어졌다
- 1280px에서 가로 스크롤 없음(`scrollWidth 1265 < 1280`), 카드는 세로로 접힘 = 좁은 화면 회귀 없음
- 390px에서도 가로 넘침 없음(`scrollWidth 375`)

**원리**: 9:16 세로 영상 레일들은 각자 `max-width`(320px/400px)를 갖고 있고,
옆 칼럼은 `flex:1 1 300px`에 상한이 없다. 그래서 패널 상한만 풀면 **늘어난 폭을 옆 칼럼이 먹는다.**
→ 아래 작업에서 이 구조(세로영상=고정 / 텍스트칼럼=신축)를 깨지 마라.

---

## 1. 사장님 지시 (원문 그대로)

1. "화면을 넓혔으니 지금 답답했던 UI나 작았던 것 **큼직하게** 사용자들이 편리하게 쓸 수 있게 개선"
2. "**모바일**로도 만드는 사람들 있으니 그것도 해줘야 돼"

핵심 철학(메모리 `feedback_꾸미기_딸깍프리셋철학`): **딸깍 = 원클릭.**
60대도 쓴다 — 작은 글씨·작은 버튼이 가장 큰 적이다.

---

## 2. 지금 무엇이 작은가 — **전부 Playwright 실측값** (추측 아님)

### 2-1. 글자 크기 분포 (2단계 패널 + 사이드바, 실제 렌더된 것만)

| 크기 | 개수 |
|---|---|
| **9px** | 1 |
| **11px** | 12 ← 가장 많다 |
| **12px** | 8 |
| 13px | 3 |
| 14px | 1 |
| 15px | 6 |
| 15.5px | 9 |
| 18px | 1 |

→ **11~12px가 20개**로 최다. 패널 제목이 18px밖에 안 된다.

### 2-2. 주요 요소 실측

| 요소 | 지금 |
|---|---|
| 패널 제목 `h3` | **18px** |
| 패널 설명 `.sub` | 14px |
| 대본 카드 본문 | 15.5px |
| `.main` 여백 | 24px 32px |
| 사이드바 폭 | 270px |
| 다음 버튼 | 86×42px, 글자 14px |
| 미리보기 슬롯 | 400×520px (min-height 520px 고정) |

### 2-3. 손가락으로 누르기 작은 것 — **44px 미만이 15개**

| 버튼 | 높이 |
|---|---|
| ✏ 대본 고치기 (카드마다) | **23px** |
| 🎞 이 대본으로 따로 만들기 | **23px** |
| ↩ 되돌리기 | 29px |
| 🔐 관리페이지 / ⚙️ 내 계정 / ↩ 로그아웃 | 34px |
| 📂 내 작업목록 / ＋ 새 작업 | 35px |
| 🌙 민트·블랙 | 37px |
| ← 이전 / 다음 → | 42px |

**기준**: 터치 타깃 최소 44×44px (모바일 접근성 통설).
23px짜리 버튼이 대본 카드마다 2개씩 = 사장님이 가장 자주 누르는 자리다.

---

## 3. ★모바일 — 지금 상태 (실측)

### 3-1. 치명적 사실: **produce.html에는 폭 기반 미디어쿼리가 하나도 없다**

```
grep '@media' produce.html  →  전부 prefers-reduced-motion (3건)
                               max-width 미디어쿼리 0건
```

반면 랭킹 페이지 `index.html`에는 있다:
- `@media (max-width:768px)` — 사이드바를 **가로띠**로 눕히고, 메뉴 라벨을 숨긴다(`.menu-group .label{display:none}`)
- `@media (max-width:420px)` — 추가 축소

→ **제작소만 모바일 대응이 통째로 빠져 있다.** 사이드바(`sidebar.js` 주입)는 공통이지만
**모바일 CSS는 페이지마다 각자 갖고 있는 구조**라 제작소엔 없다.

### 3-2. 390×844(아이폰)에서 눈으로 확인된 문제

스크린샷: `mobile_390.png` (프로젝트 루트, 페이블이 찍음)

| # | 증상 | 원인 |
|---|---|---|
| **M1** | 사이드바가 화면 맨 위 가로띠인데 **메뉴가 잘려 안 보인다** — "레퍼런스 랭킹 / 영상 즐…"에서 끊김 | 제작소에 `index.html`의 768px 미디어쿼리가 없다 |
| **M2** | **8단계 진행바 라벨이 2~3줄로 쪼개짐** — "영상/대 본출력", "고품질 자막제 거", "최종랜 더" | 390px에 오브 8개를 균등 분배 |
| **M3** | **빈 미리보기 자리가 세로 500px+ 차지** — 스크롤이 무의미하게 길다 | `#mixPreviewPanel{min-height:520px}`가 모바일에서도 그대로 |
| **M4** | 버튼 23~35px — 손가락으로 누르기 작다 | §2-3과 동일 |

**단, 가로 넘침(overflow)은 없다** — `scrollWidth 375 < 390`. 즉 레이아웃이 깨진 게 아니라 **작고 답답한 것**이 문제다.

---

## 4. 해야 할 일

### T1. 데스크톱 — 큼직하게 (넓어진 폭을 실제 가독성으로 바꾼다)

폭만 넓히면 **글자만 작아 보인다.** 폭에 맞춰 크기도 올려야 개선이 체감된다.

- 패널 제목 `h3` 18px → **22~24px**
- 패널 설명 `.sub` 14px → **15~16px**
- 11px 무리(칩·힌트·라벨) → **12.5~13px** (9px짜리 1개는 최소 11px로)
- 대본 카드 본문 15.5px 유지 or **16px** (읽는 글이라 여기가 제일 중요)
- `.main` 여백 24/32 → 넓은 화면에서 **28/40** 정도로 숨통

**★단 넓은 화면에서만 키워라.** `@media (min-width:1600px)` 안에서 올리면 노트북(1280)에선
지금 그대로라 회귀 위험이 0이다. 이 방식을 권한다.

### T2. 버튼 — 44px 이상으로

§2-3 표의 15개. 특히 **23px짜리 2개(대본 고치기 / 따로 만들기)를 최우선**으로.
`padding`을 키워 높이를 올려라(글자 크기만 키우면 줄바꿈이 깨진다).

### T3. ★모바일 미디어쿼리 신설 — `index.html`을 그대로 베껴라 (0순위-B)

**새로 설계하지 마라.** 랭킹 페이지 `index.html:161~180`에 이미 검증된 모바일 규칙이 있다.
같은 사이드바(`sidebar.js`)를 쓰므로 **그 규칙을 제작소에 이식**하는 게 정답이다.
두 벌로 다르게 짜면 반드시 어긋난다(CLAUDE.md 0순위-B).

```
@media (max-width: 768px){
  /* ① 사이드바: index.html과 동일 규칙 이식 (M1 해결) */
  /* ② 진행바: 오브 8개를 가로 스크롤 띠로 (M2) — 라벨이 쪼개지면 안 읽힌다 */
  /* ③ #mixPreviewPanel min-height 520px → auto (M3) */
  /* ④ .main padding 축소, 버튼 높이 44px 확보 (M4) */
}
```

**진행바(M2) 판단이 필요한 지점**: 8개를 좁은 폭에 다 넣으면 반드시 쪼개진다. 선택지 —
(a) 가로 스크롤 띠(현재 단계로 자동 스크롤), (b) 현재 단계만 크게 + 나머지는 점, (c) `2/8` 표기.
**(a)를 권한다** — 단계 이동이 제작소의 핵심 조작이라 전부 눌러야 한다.

### T4. 검증 — 실행 결과로만 말해라 (CLAUDE.md 0순위)

```
py -m uvicorn shopping_shorts.app:app --port 8891    # 트랙 폴더에서
```
Playwright로 **네 폭 전부** 재라: **2000 / 1600 / 1280 / 390**
- `document.documentElement.scrollWidth <= window.innerWidth` (가로 넘침 0)
- 44px 미만 터치 타깃 개수 — **15개에서 얼마로 줄었는지 수치로 보고**
- 대본 카드 3장이 넓은 화면에서 여전히 한 줄인지(top 값 동일)
- 세로영상 레일이 320/400px 유지되는지

⚠️ **cp949 함정**: `python -c` 출력에 한글·— 넣으면 `UnicodeEncodeError`. `PYTHONIOENCODING=utf-8` 붙여라.
⚠️ 로그인 없이 `/produce.html`을 열면 JS가 잠시 뒤 `/`로 리다이렉트한다. **로드 직후 바로** 측정하면 된다.

---

## 5. 넘어야 할 함정 (실측으로 이미 밟은 것)

1. **`.panel`은 8개가 `display:none`으로 겹쳐 있다** — `data-step` 속성으로 지정해 열어야 잰다.
   화면 2번 = `data-step="7"`(내부 인덱스 어긋남, `ORB_TO_PANEL[1]=7`).
2. **세로 영상 레일의 `max-width`를 지우지 마라** — 9:16 영상이 가로로 늘어나 우스워진다.
   넓어진 폭은 **텍스트 칼럼이 먹어야** 한다.
3. **produce.html 복원 슬라이스가 윈도우 명령줄 32KB에 붙어 있다**(핸드오프 기록).
   `[_restoreWork ~ _consumeProduceHandoff)` 구간에 코드를 더하면 `node -e`가 WinError 206으로 죽는다.
   **CSS만 건드리면 무관**하지만, JS를 그 구간에 넣지 마라.
4. **장면 편집은 iframe**(`scene_lab.html`)이다 — 부모 CSS가 안 먹는다. 그 안을 키우려면
   `scene_lab.html`을 따로 고쳐야 한다(이번 범위 밖으로 둘지 판단할 것).

---

## 6. 마무리

```
git -C .tracks/제작소가로폭 status --porcelain    # 내 파일만인지 눈으로
git -C .tracks/제작소가로폭 add -A && commit
py tools/track.py finish 제작소가로폭             # 게이트 통과 시 main → 3분 뒤 라이브
```
handoff/제작소가로폭.md + wiki/log.d/제작소가로폭.md 남길 것.
