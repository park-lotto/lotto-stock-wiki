# P1 — 진입 즉시 자동분석 확정 + 진행 UI 구현 계획

> **For agentic workers:** REQUIRED SUB-SKILL: superpowers:subagent-driven-development (recommended) or superpowers:executing-plans. Steps use `- [ ]`.

**Goal:** 제작소 진입 시 담긴 영상 전부(4개 초과 포함)를 자동 분석하고, 분석 중 진행 개수를 보이며, 일부만 성공해도 분석된 결과(AI PICK/비교카드)를 띄운다.

**Architecture:** 프론트(`static/produce.html`) 전용 변경. 기존 `/api/produce/autoload` 엔드포인트·안전장치(DB 래치·크레딧 게이트·호출당 상한 4)는 그대로 두고, 프론트가 **여러 배치로 나눠 부르는 바운드 루프**(`autoloadAllFootage`)로 교체한다. 재귀는 절대 안 만든다(2026-07-26 사고 교훈).

**Tech Stack:** Vanilla JS (produce.html), Python 테스트 하네스(node subset eval, 기존 `test_produce_*_js` 패턴).

## Global Constraints
- **트랙 격리**: 편집은 전부 `.tracks/모션레벨훅/...` 절대경로. main 폴더 코드 금지.
- **재귀 금지**: `refreshStep0`을 재귀 호출하지 않는다. autoload 루프는 **유한 반복**(최대 3회)으로만.
- **페이지당 1회 래치**: `_autoloadTried`(모듈 변수) 유지 — HANDOFF 재대입에도 안 날아감.
- **게이트**: `python -m pytest shopping_shorts/ -q` 신규 회귀 0(기준선 13건 대비).
- 커밋은 `git -C .tracks/모션레벨훅`. 커밋 전 `git status --porcelain`로 내 파일만 확인.

---

### Task 1: `autoloadAllFootage()` — 여러 배치 바운드 루프

**Files:**
- Modify: `shopping_shorts/static/produce.html` (`autoloadFootageScripts` 근처 ~2852)
- Test: `shopping_shorts/tests/test_produce_autoload_batches_js.py` (Create)

**Interfaces:**
- Consumes: 전역 `HANDOFF`(각 `{shortcode,url,useFootage,...}`), `fetch`(POST `/api/produce/autoload`, 응답 `{added, results:[{shortcode,status}]}`).
- Produces: `async function autoloadAllFootage()` → `{added:number, already:number, analyzed:boolean, failed:string[]}`. `analyzed` = 재조회 가치(added>0 또는 already 존재). 응답에 `skipped_cap` 상태가 남아 있으면 다음 배치를 한 번 더 부른다(최대 3배치=12개). 실패 코드(`failed_download`/`failed_empty`/`failed_limit`)는 `failed`에 모은다.

- [ ] **Step 1: 실패 테스트 작성**

`shopping_shorts/tests/test_produce_autoload_batches_js.py`:

```python
"""P1 — autoloadAllFootage가 4개 초과 소스를 여러 배치로 끝까지 분석하는지(무한루프 없이)."""
import json, pathlib, shutil, subprocess, pytest

PRODUCE_HTML = pathlib.Path(__file__).resolve().parents[1] / "static" / "produce.html"
NODE = shutil.which("node")
_START = "async function autoloadAllFootage(){"
_END = "// 자동적재가 도는 동안 보여줄 과도 상태"

_HARNESS = r"""
'use strict';
global.HANDOFF = Array.from({length:6}, (_,i)=>({shortcode:'S'+i, url:'https://x/'+i, useFootage:true}));
let _calls = 0;
global._callLog = [];
global.fetch = async (url, opt)=>{
  _calls++;
  const body = JSON.parse(opt.body);
  global._callLog.push(body.items.map(x=>x.shortcode));
  // 1차: 앞 4개 added, 뒤는 서버가 호출당 상한(4)으로 skipped_cap
  // 2차: 남은 2개 added
  if(_calls===1){
    return { json: async ()=>({ added:4, results:[
      {shortcode:'S0',status:'added'},{shortcode:'S1',status:'added'},
      {shortcode:'S2',status:'added'},{shortcode:'S3',status:'added'},
      {shortcode:'S4',status:'skipped_cap'},{shortcode:'S5',status:'skipped_cap'} ]}) };
  }
  return { json: async ()=>({ added:2, results:[
    {shortcode:'S4',status:'added'},{shortcode:'S5',status:'added'} ]}) };
};
"""

_DRIVER = r"""
(async function(){
  const out = await autoloadAllFootage();
  console.log(JSON.stringify({ out, calls: global._callLog.length, log: global._callLog }));
})();
"""

def _run(tmp_path):
    src = PRODUCE_HTML.read_text(encoding="utf-8")
    sl = src[src.index(_START):src.index(_END)]
    js = tmp_path / "t.js"
    js.write_text(_HARNESS + sl + _DRIVER, encoding="utf-8")
    out = subprocess.run([NODE, str(js)], capture_output=True, text=True,
                         encoding="utf-8", errors="replace",
                         stdin=subprocess.DEVNULL, timeout=30)
    assert out.returncode == 0, out.stderr
    return json.loads(out.stdout.strip().splitlines()[-1])

@pytest.mark.skipif(not NODE, reason="node 없음")
def test_autoload_covers_all_via_multiple_batches(tmp_path):
    got = _run(tmp_path)
    assert got["out"]["added"] == 6          # 6개 전부 분석됨(4+2)
    assert got["out"]["analyzed"] is True
    assert 2 <= got["calls"] <= 3            # 여러 배치, 무한루프 아님
```

- [ ] **Step 2: 테스트 실패 확인**

Run: `cd .tracks/모션레벨훅 && python -m pytest shopping_shorts/tests/test_produce_autoload_batches_js.py -q`
Expected: FAIL (`autoloadAllFootage` 앵커 없음 → substring index 에러).

- [ ] **Step 3: `autoloadAllFootage` 구현**

`produce.html`의 `autoloadFootageScripts` 함수 **바로 아래**(닫는 `}` 다음, `renderAutoloadingState` 주석 앞)에 삽입:

```javascript
// P1(2026-07-29): 담긴 소스 전부를 여러 배치로 끝까지 분석한다. 서버는 호출당 4개 상한
// (_AUTOLOAD_MAX_PER_CALL)이라 5개 이상이면 skipped_cap이 남는데, 그럴 때만 한 번 더 부른다.
// ★유한 반복(최대 3배치=12개). refreshStep0을 재귀로 부르지 않는다(2026-07-26 사고 방지).
async function autoloadAllFootage(){
  let added=0, already=0, failed=[];
  for(let batch=0; batch<3; batch++){
    const items = HANDOFF.filter(h=>h.useFootage && h.url).map(h=>({
      url: h.url, video_url: h.video_url||'', shortcode: h.shortcode||'', name: h.name||'',
      thumbnail: h.thumbnail||'', caption: h.caption||'', category: h.category||'',
      followers: h.followers||0, comments: h.comments||0,
    }));
    if(!items.length) break;
    let d;
    try{
      const r = await fetch('/api/produce/autoload', {
        method:'POST', headers:{'Content-Type':'application/json'}, body: JSON.stringify({items}) });
      d = await r.json();
    }catch(e){ break; }
    const results = (d && d.results) || [];
    added += (d && d.added) || 0;
    already += results.filter(x=>x.status==='already').length;
    failed.push(...results.filter(x=>/^failed_/.test(x.status)).map(x=>x.shortcode));
    // 상한에 걸려 못 태운 게 남았을 때만 다음 배치. 없으면 끝.
    if(!results.some(x=>x.status==='skipped_cap')) break;
  }
  return { added, already, failed, analyzed: (added>0 || already>0) };
}
```

- [ ] **Step 4: 테스트 통과 확인**

Run: `cd .tracks/모션레벨훅 && python -m pytest shopping_shorts/tests/test_produce_autoload_batches_js.py -q`
Expected: PASS (1 passed).

- [ ] **Step 5: 커밋**

```bash
git -C .tracks/모션레벨훅 add shopping_shorts/static/produce.html shopping_shorts/tests/test_produce_autoload_batches_js.py
git -C .tracks/모션레벨훅 commit -m "feat(제작소 P1): autoloadAllFootage 여러 배치 바운드 루프 — 4개 초과 소스도 끝까지 분석"
```

---

### Task 2: `refreshStep0`이 새 루프 사용 + 진행 개수 표시

**Files:**
- Modify: `shopping_shorts/static/produce.html` (`refreshStep0` ~2836, `renderAutoloadingState` ~2869)

**Interfaces:**
- Consumes: `autoloadAllFootage()`(Task 1).
- Produces: 없음(내부 배선). `renderAutoloadingState(total)` — 인자 `total:number`(분석 대상 개수)로 "총 N개 영상 분석 중" 문구.

- [ ] **Step 1: `renderAutoloadingState`에 개수 인자 추가**

`produce.html` `renderAutoloadingState`를 다음으로 교체:

```javascript
function renderAutoloadingState(total){
  const el=document.getElementById('aiPick'); if(!el) return;
  const n = Number(total)||0;
  el.innerHTML = `<div class="skm-card empty">
      <h2><span class="skm-spin">🔄</span> 담은 영상 ${n?('<b>'+n+'개</b> '):''}대본을 분석하고 있어요</h2>
      <div class="hint" style="text-align:center;color:var(--sub)">처음 담은 영상은 30초~1분쯤 걸립니다. 끝나면 AI PICK 카드가 나타나요.</div>
    </div>`;
}
```

- [ ] **Step 2: `refreshStep0`이 새 루프 호출**

`refreshStep0`의 autoload 블록(2836~2843)을 다음으로 교체:

```javascript
    if((!d || d.pick_id==null) && !_autoloadTried){
      _autoloadTried = true;
      const total = HANDOFF.filter(h=>h.useFootage && h.url).length;
      renderAutoloadingState(total);
      const got = await autoloadAllFootage();     // ★여러 배치로 전부 분석(Task 1)
      if(got.analyzed){
        try{ d = await (await fetch('/api/produce/aipick'+(q==='?'?'':q))).json(); }catch(e){}
      }
    }
```

- [ ] **Step 3: node로 배선 무결성 확인(구문·앵커)**

Run:
```bash
cd .tracks/모션레벨훅 && python - <<'PY'
src=open('shopping_shorts/static/produce.html',encoding='utf-8').read()
assert 'async function autoloadAllFootage(){' in src
assert 'renderAutoloadingState(total)' in src
assert 'const got = await autoloadAllFootage();' in src
# 옛 단일 호출이 refreshStep0에 남아있지 않은지(중복 방지)
import re
blk=src[src.index('async function refreshStep0(){'):src.index('let _autoloadTried')]
assert 'autoloadFootageScripts()' not in blk, '옛 단일 호출이 refreshStep0에 남음'
print('wiring OK')
PY
```
Expected: `wiring OK`

- [ ] **Step 4: 관련 회귀 테스트 통과 확인**

Run: `cd .tracks/모션레벨훅 && python -m pytest shopping_shorts/tests/ -q -k "produce"`
Expected: 신규 실패 0(기존 통과 유지).

- [ ] **Step 5: 커밋**

```bash
git -C .tracks/모션레벨훅 add shopping_shorts/static/produce.html
git -C .tracks/모션레벨훅 commit -m "feat(제작소 P1): 진입 자동분석이 전 소스 배치 루프 사용 + 진행 개수 표시"
```

---

### Task 3: 게이트 + 배포

- [ ] **Step 1: 전체 게이트(로컬 예행)**

Run: `cd .tracks/모션레벨훅 && python -m pytest shopping_shorts/ -q 2>&1 | tail -5`
Expected: 신규 실패 0(기준선 13 대비 늘지 않음).

- [ ] **Step 2: finish로 병합·배포**

```bash
cd "C:/Users/TheRose/Desktop/로또의 주식" && py tools/track.py finish 모션레벨훅
```
Expected: 게이트 통과 → main 병합·push → 3분 뒤 라이브.

- [ ] **Step 3: 라이브 육안검증(사장님)**

제작소에 영상 5개 담기(4개 초과) → Ctrl+F5 → "총 5개 대본을 분석하고 있어요" 스피너 → 끝나면 AI PICK 카드 + 비교 스트립 등장(일부 실패해도 성공분은 뜬다).

## Self-Review
- 스펙 §4①(자동분석 앞당김)·§5 P1 커버: Task1/2가 배치 루프+진행UI 구현. ✓
- 비목표 준수: autoload 엔드포인트·크레딧 로직 불변, 프론트 루프만. ✓
- 재귀 금지·유한 반복(최대 3배치) 명시. ✓
- 플레이스홀더 없음(코드 전량 기재). ✓
- 타입 일관: `autoloadAllFootage()`→`{added,already,failed,analyzed}` Task1 정의와 Task2 `got.analyzed` 사용 일치. ✓
