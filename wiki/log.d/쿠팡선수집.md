# 쿠팡선수집

2026-08-17: 1단계 영상분석 뒤에 쿠팡 상세·리뷰를 미리 긁어 2단계 대본 재료로 대기시키는 구조 신설·라이브.
설계가 실측으로 두 번 바뀜 — ①서버는 AWS서울 한국IP인데도 쿠팡 403(브라우저 지문 차단) → 사장님 PC 릴레이
②PC가 제미니 분석까지 하려니 키가 서버에만 있음 → PC는 긁기만, 이미지 1400px 축소 전송, 분석은 서버.
검색어는 1단계 태깅의 source_brief.product(3단계 affiliate_target 안 기다림), 카테고리는 홈템·기타만.
무자막 해외영상도 대상(failed_empty여도 화면 분석은 됨), 이름 못 잡거나 검색 빈손이면 렌즈 승격(유료 1회).
★끝단 실측(무자막 도우인 실제 영상): 담기→큐잉(고독스 C100)→Playwright 수집(상세4·리뷰8)→356KB 전송→
서버 분석·저장 done. specs·satisfy·complaint 확보, 렌즈 호출 0. 버그 3건 수정(app.py import json 누락으로
NameError가 except에 삼켜짐 / 릴레이가 cp949 콘솔 em dash로 즉사 / 릴레이 재시작 누락 → 재시도 API 신설).
남은 건 2단계 대본에 실제로 실리는지 확인과 재료 상태 표시 UI.

## 2026-08-16 (집PC) — 재료가 대본에 안 실리던 진짜 원인 해결 (라이브 1ca8d224a)
"2단계에 실리는지 확인"을 하러 갔다가 **재료가 있어도 영원히 못 찾는 버그**를 잡았다. 저장은 DB의
shortcode 그대로(`grab_douyin_…`)인데 조회는 URL 정규식 추론(인스타·유튜브·틱톡 셋뿐)이라 도우인·
샤오홍슈는 캐시키가 0개였다 — 같은 판단이 두 군데 다르게 적힌 0순위-B. `Store.shortcodes_for_url()`
신설해 **추론 대신 DB에 적힌 것을 읽게** 했다(정규식은 폴백, 회귀 0). 실측 394건: 캐시적중 130→248건
(+118, 1.9배), 새로 적중 인스타46·유튜브29·틱톡26·샤오홍슈14·도우인3. ★정규식 있는 플랫폼조차
`grab_`·짧은해시 키는 못 찾아 매번 Gemini 재추출 중이었다. 회귀테스트 5개(반사실 검증 확인).

## 2026-08-17 (집PC) — 3단계 매칭 대수술 (전부 라이브)
사장님 실사용 제보를 따라가며 6건 수정. ①1단계 카드가 "분석 중"에 박제(캐시가 미완 응답까지
저장) ②3단계 자동 시작(매칭 걸 방법이 세 곳 다 "저쪽 소관"이라 아무데도 없었다) ③restyle 원인
계측 신설 ④훅 장면매칭에 역할 기준(대사행위 5/5 None → 어긋남 판정·교정 전부 무력) ⑤축을 원본
쇼곰에 맞춤(완성→조리→완성. 해결·결과를 완성으로 보던 게 틀렸다) + 소스별 시간순 정렬 + 썸네일
`1-3` 마킹 + [서버에 반영] 버튼 제거(자동저장) ⑥**확정 대본 재작성 차단** — `if scene_first:`가
먼저 걸려 given_script 분기를 못 타서 2단계 확정본이 버려지고 새로 쓰이고 있었다.
★실측: 매칭 8분48초 → **2분34초**, 확정 대본(390자) 그대로 유지, 최종렌더 정상(1080x1920 39.4초).
메인 프롬프트에 화면배치 축 재투입(07-31 결정 뒤집기 — 사장님 지시. 그때와 달리 지금은
source_brief·label·use_point·change 등 근거가 실재한다). 팔레트 머리글도 소재별로 분리
(젤펜인데 "재료·반죽 준비"가 뜨던 것). 매칭 완료 후 3단계 직행(2단계 되돌림 제거).
