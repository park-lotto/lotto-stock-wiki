# 영상제작소 모션효과 Phase 2a — 프리셋 팩 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 꾸미기에서 완성스타일 카드 하나를 누르면 전환·스티커·색감·텍스트 모션까지 함께 적용되는 "모션 팩"을 붙인다.

**Architecture:** Phase 1이 만든 "타임드 투명 레이어(qtrle .mov + ffmpeg overlay)" 뼈대는 그대로 쓴다. 새로 만드는 건 **정책 → 계획**을 계산하는 순수 함수(`motion_packs.py`)뿐이다. `deco.motion`에는 `pack_id`만 저장하고, 실제 레이어(start/dur)는 **렌더 시점**에 TTS 실길이 타임라인으로 계산한다. 런타임 Node 의존은 0을 유지한다(서버 RAM 830MB).

**Tech Stack:** Python 3(순수 함수 + pytest), ffmpeg(overlay/drawtext), Remotion(빌드타임 자산 생성기), 바닐라 JS(produce.html)

## Global Constraints

- **브랜치는 `main` 고정.** 커밋 전 `git branch --show-current`로 확인. (CLAUDE.md 배포규칙 #1)
- **`git add -A` 금지.** `produce.html`은 "영상제작소 대본·영상믹스 통합" 트랙이 동시 편집 중 — **자기 파일만 stage**하고, 남의 hunk가 섞이면 `git apply --cached -R`로 격리한다. (CLAUDE.md 배포규칙 #6)
- **서버 SSH로 `git add`/`commit` 금지.** 서버는 `git pull --ff-only` 전용. push만 하면 3분 크론이 자동배포. (CLAUDE.md 배포규칙 #9)
- **자산 포맷은 qtrle `.mov`(argb) 고정.** VP9 알파 webm 금지 — 제품 빌더가 디코더 플래그를 안 주므로 알파가 죽어 불투명 박스로 합성된다. (Phase 1 하드-원 교훈)
- **`concat -c copy` 무변경.** 재인코딩 concat은 2GB 서버를 죽인 전력이 있다(2026-07-12). 전환은 xfade가 아니라 오버레이로만 처리한다.
- 테스트 실행은 **repo 루트에서** `python -m pytest`(절대임포트).

---

## File Structure

| 파일 | 책임 | 태스크 |
|---|---|---|
| `shopping_shorts/motion_packs.py` (신규) | **정책 → 계획**. 팩 로드 + 타임라인 → 레이어/색감/텍스트 정책 계산. 순수 함수. | 2, 3 |
| `shopping_shorts/assets/motion/packs.json` (신규) | 팩 카탈로그(큐레이션). manifest.json(자산 빌드산출물)과 분리. | 3, 7 |
| `shopping_shorts/video_assemble.py` (수정) | `_beat_timeline` 추출 / 색감필터 순서 / 헤드카피 enable | 1, 4, 5 |
| `shopping_shorts/mix_pipeline.py` (수정) | `pack_id` → `build_plan` → 기존 `resolve_layers` 배선 | 6 |
| `shopping_shorts/motion/` (수정) | 팩용 전환·스티커 컴포지션 추가 | 7 |
| `shopping_shorts/static/produce.html` (수정) | `FULL_PRESETS[i].motion` + 고급 팩 교체/끄기 | 8 |
| `shopping_shorts/tests/test_motion_packs.py` (신규) | `motion_packs` 단위 | 2, 3 |
| `shopping_shorts/tests/test_beat_timeline.py` (신규) | 타임라인 동치성 가드 | 1 |

**역할 분리**: `motion_packs.py`(정책→계획) ↔ `motion_assets.py`(레이어→실경로). 서로 모른다.

---

## Task 1: `_beat_timeline()` 추출 — 자막·모션 타임라인 단일화

지금 비트 경계 `t0` 누적이 `_burn_captions` 안에 인라인돼 있다(video_assemble.py:575-583). 모션이 이걸 다시 계산하면 **전환이 자막과 밀린다.** 헬퍼로 뽑아 양쪽이 같은 걸 쓰게 한다.

**Files:**
- Modify: `shopping_shorts/video_assemble.py:575-583`
- Test: `shopping_shorts/tests/test_beat_timeline.py`

**Interfaces:**
- Consumes: 없음
- Produces: `_beat_timeline(edit_plan, tts_paths) -> list[dict]`
  각 항목 `{"beat_idx": int, "t0": float, "dur": float, "narration": str, "role": str}`
  Task 2·6이 이 반환형에 의존한다.

- [ ] **Step 1: 실패하는 테스트 작성**

`shopping_shorts/tests/test_beat_timeline.py` 신규:

```python
from unittest.mock import patch

from shopping_shorts.video_assemble import _beat_timeline


def _plan():
    return {"beats": [
        {"beat_idx": 0, "role": "hook", "narration": "훅 문장"},
        {"beat_idx": 1, "role": "body", "narration": "본문 문장"},
        {"beat_idx": 2, "role": "cta", "narration": "마무리"},
    ]}


def test_t0_누적으로_비트_경계를_만든다():
    tts = {0: "a.mp3", 1: "b.mp3", 2: "c.mp3"}
    with patch("shopping_shorts.video_assemble._probe_duration", side_effect=[3.0, 4.0, 2.0]):
        tl = _beat_timeline(_plan(), tts)
    assert [x["t0"] for x in tl] == [0.0, 3.0, 7.0]
    assert [x["dur"] for x in tl] == [3.0, 4.0, 2.0]
    assert [x["beat_idx"] for x in tl] == [0, 1, 2]
    assert tl[0]["role"] == "hook"
    assert tl[1]["narration"] == "본문 문장"


def test_tts없는_비트는_건너뛴다():
    tts = {0: "a.mp3", 2: "c.mp3"}          # 비트1 tts 없음
    with patch("shopping_shorts.video_assemble._probe_duration", side_effect=[3.0, 2.0]):
        tl = _beat_timeline(_plan(), tts)
    assert [x["beat_idx"] for x in tl] == [0, 2]
    assert [x["t0"] for x in tl] == [0.0, 3.0]


def test_비트가_없으면_빈리스트():
    assert _beat_timeline({"beats": []}, {}) == []
```

- [ ] **Step 2: 테스트 실패 확인**

Run: `python -m pytest shopping_shorts/tests/test_beat_timeline.py -v`
Expected: FAIL — `ImportError: cannot import name '_beat_timeline'`

- [ ] **Step 3: 헬퍼 구현**

`video_assemble.py`에서 `_burn_captions` 정의(558행) **바로 앞**에 추가:

```python
def _beat_timeline(edit_plan, tts_paths):
    """비트별 전체 타임라인 [{beat_idx, t0, dur, narration, role}, ...].

    자막(_burn_captions)과 모션(motion_packs)이 **같은 경계**를 쓰도록 하는 단일 출처.
    여기서 중복 계산하면 전환이 자막과 어긋난다. tts 없는 비트는 건너뛴다(기존 동작).
    """
    timeline = []
    t0 = 0.0
    for beat in edit_plan["beats"]:
        idx = beat["beat_idx"]
        tts = tts_paths.get(idx)
        if not tts:
            continue
        dur = _probe_duration(tts)
        timeline.append({
            "beat_idx": idx,
            "t0": t0,
            "dur": dur,
            "narration": beat.get("narration", ""),
            "role": beat.get("role", ""),
        })
        t0 += dur
    return timeline
```

- [ ] **Step 4: 테스트 통과 확인**

Run: `python -m pytest shopping_shorts/tests/test_beat_timeline.py -v`
Expected: PASS (3 passed)

- [ ] **Step 5: `_burn_captions`가 헬퍼를 쓰도록 교체 (동작 동일)**

`video_assemble.py:575-583`의 인라인 루프를 교체한다.

교체 전:
```python
    t0 = 0.0
    for beat in edit_plan["beats"]:
        idx = beat["beat_idx"]
        tts = tts_paths.get(idx)
        if not tts:
            continue
        dur = _probe_duration(tts)
        filters.extend(_caption_drawtexts(beat.get("narration", ""), dur, work, idx, t0, caption_style))
        t0 += dur
```

교체 후:
```python
    timeline = _beat_timeline(edit_plan, tts_paths)
    for b in timeline:
        filters.extend(_caption_drawtexts(b["narration"], b["dur"], work, b["beat_idx"],
                                          b["t0"], caption_style))
```

- [ ] **Step 6: 회귀 없음 확인 (기존 자막 테스트)**

Run: `python -m pytest shopping_shorts/tests/ -k "caption or assemble or burn" -v`
Expected: PASS — 기존 자막 테스트 전부 통과(실패 0). 실패하면 교체가 잘못된 것이니 멈추고 원인부터 찾는다.

- [ ] **Step 7: 커밋**

```bash
git branch --show-current   # main 확인
git add shopping_shorts/video_assemble.py shopping_shorts/tests/test_beat_timeline.py
git commit -m "refactor(motion): _beat_timeline 추출 — 자막·모션 타임라인 단일 출처"
```

---

## Task 2: `motion_packs.build_plan` — 전환·스티커 배치 정책

**Files:**
- Create: `shopping_shorts/motion_packs.py`
- Test: `shopping_shorts/tests/test_motion_packs.py`

**Interfaces:**
- Consumes: Task 1의 `_beat_timeline` 반환형 `[{beat_idx, t0, dur, narration, role}]`
- Produces:
  - `build_plan(pack: dict, timeline: list) -> dict`
    반환 `{"layers": list, "color_filter": str|None, "caption_effect": str|None, "headcopy_enable": str|None}`
    `layers` 항목은 `motion_assets.resolve_layers`의 입력형 `{asset_id, start, dur}`.
  - Task 3이 `load_packs`를 같은 모듈에 추가하고, Task 6이 `build_plan`을 호출한다.

- [ ] **Step 1: 실패하는 테스트 작성**

`shopping_shorts/tests/test_motion_packs.py` 신규:

```python
from shopping_shorts.motion_packs import build_plan

TL = [                              # 경계: 3.0, 7.0 (첫 비트 시작 0.0은 경계 아님)
    {"beat_idx": 0, "t0": 0.0, "dur": 3.0, "narration": "훅", "role": "hook"},
    {"beat_idx": 1, "t0": 3.0, "dur": 4.0, "narration": "본문", "role": "body"},
    {"beat_idx": 2, "t0": 7.0, "dur": 2.0, "narration": "마무리", "role": "cta"},
]

PACK_EVERY = {
    "id": "dynamic_pop",
    "transition": {"asset_id": "swipe_left", "dur": 0.5, "lead": 0.25, "policy": "every_beat"},
    "sticker": {"asset_id": "sparkle", "dur": 1.0, "policy": "hook_climax"},
    "color_filter": "eq=saturation=1.15",
}


def test_every_beat는_모든_경계에_전환을_넣는다():
    p = build_plan(PACK_EVERY, TL)
    tr = [L for L in p["layers"] if L["asset_id"] == "swipe_left"]
    assert [round(L["start"], 3) for L in tr] == [2.75, 6.75]     # t0 - lead
    assert all(L["dur"] == 0.5 for L in tr)


def test_hook_climax_스티커는_첫비트와_마지막비트_시작에():
    p = build_plan(PACK_EVERY, TL)
    st = [L for L in p["layers"] if L["asset_id"] == "sparkle"]
    assert [round(L["start"], 3) for L in st] == [0.0, 7.0]        # 비트 시작
    assert all(L["dur"] == 1.0 for L in st)


def test_hook_climax_전환은_첫경계와_마지막경계만():
    pack = {**PACK_EVERY, "transition": {**PACK_EVERY["transition"], "policy": "hook_climax"}}
    tr = [L for L in build_plan(pack, TL)["layers"] if L["asset_id"] == "swipe_left"]
    assert [round(L["start"], 3) for L in tr] == [2.75, 6.75]      # 경계 2개뿐이라 동일


def test_lead가_0아래로_내려가면_0으로_clamp():
    tl = [{"beat_idx": 0, "t0": 0.0, "dur": 0.1, "narration": "", "role": "hook"},
          {"beat_idx": 1, "t0": 0.1, "dur": 3.0, "narration": "", "role": "body"}]
    tr = [L for L in build_plan(PACK_EVERY, tl)["layers"] if L["asset_id"] == "swipe_left"]
    assert tr[0]["start"] == 0.0                                   # 0.1 - 0.25 → clamp


def test_policy_none이면_해당_레이어_없음():
    pack = {"id": "x",
            "transition": {"asset_id": "swipe_left", "dur": 0.5, "lead": 0.2, "policy": "none"},
            "sticker": {"asset_id": "sparkle", "dur": 1.0, "policy": "none"}}
    assert build_plan(pack, TL)["layers"] == []


def test_빈_타임라인이면_레이어_없음():
    assert build_plan(PACK_EVERY, [])["layers"] == []


def test_비트가_하나면_경계가_없어_전환은_없지만_스티커는_있다():
    tl = [{"beat_idx": 0, "t0": 0.0, "dur": 3.0, "narration": "훅", "role": "hook"}]
    p = build_plan(PACK_EVERY, tl)
    assert [L["asset_id"] for L in p["layers"]] == ["sparkle"]


def test_color_filter를_그대로_넘긴다():
    assert build_plan(PACK_EVERY, TL)["color_filter"] == "eq=saturation=1.15"
    assert build_plan({"id": "x"}, TL)["color_filter"] is None
```

- [ ] **Step 2: 테스트 실패 확인**

Run: `python -m pytest shopping_shorts/tests/test_motion_packs.py -v`
Expected: FAIL — `ModuleNotFoundError: No module named 'shopping_shorts.motion_packs'`

- [ ] **Step 3: 구현**

`shopping_shorts/motion_packs.py` 신규:

```python
"""모션 프리셋 팩: 정책 → 렌더 계획.

팩은 {전환·스티커·색감·텍스트모션} 한 세트이고, 이 모듈은 팩 정책과 비트 타임라인만으로
레이어 배치를 계산하는 **순수 함수**다(ffmpeg·Node·DB 의존 0).
자산 파일 해석은 motion_assets.resolve_layers의 몫 — 여기선 asset_id만 다룬다.
"""
import json
from pathlib import Path

DEFAULT_ASSETS_DIR = str(Path(__file__).parent / "assets" / "motion")


def _boundaries(timeline):
    """비트 경계 시각 리스트(첫 비트 시작 0.0은 경계가 아니다)."""
    return [b["t0"] for b in timeline[1:]]


def _pick(items, policy):
    """policy에 따라 배치 대상만 고른다."""
    if not items or policy == "none":
        return []
    if policy == "every_beat":
        return items
    if policy == "hook_climax":
        return [items[0]] if len(items) == 1 else [items[0], items[-1]]
    return []


def build_plan(pack, timeline):
    """팩 + 타임라인 → {layers, color_filter, caption_effect, headcopy_enable}.

    - 전환: 비트 경계마다 start = 경계 - lead (0 아래 clamp), dur = transition.dur
    - 스티커: 대상 비트 **시작**에 start = t0, dur = sticker.dur
    - layers는 motion_assets.resolve_layers 입력형({asset_id, start, dur}) — 위치는 매니페스트 default.
    """
    pack = pack or {}
    layers = []

    tr = pack.get("transition") or {}
    if tr.get("asset_id"):
        lead = float(tr.get("lead") or 0)
        dur = float(tr.get("dur") or 0.5)
        for t in _pick(_boundaries(timeline), tr.get("policy")):
            layers.append({"asset_id": tr["asset_id"],
                           "start": max(0.0, t - lead),
                           "dur": dur})

    st = pack.get("sticker") or {}
    if st.get("asset_id"):
        dur = float(st.get("dur") or 1.0)
        for b in _pick(list(timeline), st.get("policy")):
            layers.append({"asset_id": st["asset_id"],
                           "start": b["t0"],
                           "dur": dur})

    return {
        "layers": layers,
        "color_filter": pack.get("color_filter") or None,
        "caption_effect": (pack.get("caption") or {}).get("effect") or None,
        "headcopy_enable": _headcopy_enable(pack, timeline),
    }


def _headcopy_enable(pack, timeline):
    """헤드카피 노출 정책 → ffmpeg enable 식(또는 None=전체 표시).
    hook_only: 첫 비트 구간에만.
    """
    policy = (pack.get("headcopy") or {}).get("policy")
    if policy != "hook_only" or not timeline:
        return None
    first = timeline[0]
    return f"between(t,0,{first['t0'] + first['dur']:.3f})"
```

- [ ] **Step 4: 테스트 통과 확인**

Run: `python -m pytest shopping_shorts/tests/test_motion_packs.py -v`
Expected: PASS (8 passed)

- [ ] **Step 5: 커밋**

```bash
git add shopping_shorts/motion_packs.py shopping_shorts/tests/test_motion_packs.py
git commit -m "feat(motion): motion_packs.build_plan — 전환·스티커 배치 정책(순수함수)"
```

---

## Task 3: `load_packs` + `packs.json` 스켈레톤

팩 **값**(실제 3~4종)은 Task 7에서 레퍼런스 분석으로 채운다. 여기선 로더와 스키마만 굳힌다.

**Files:**
- Modify: `shopping_shorts/motion_packs.py`
- Create: `shopping_shorts/assets/motion/packs.json`
- Test: `shopping_shorts/tests/test_motion_packs.py` (추가)

**Interfaces:**
- Consumes: Task 2의 `motion_packs` 모듈, `DEFAULT_ASSETS_DIR`
- Produces: `load_packs(assets_dir=DEFAULT_ASSETS_DIR) -> dict` — `{pack_id: pack}`. Task 6·8이 쓴다.

- [ ] **Step 1: 실패하는 테스트 작성**

`shopping_shorts/tests/test_motion_packs.py` **맨 아래에 추가**:

```python
import json

from shopping_shorts.motion_packs import load_packs


def test_packs_json을_id로_인덱싱한다(tmp_path):
    (tmp_path / "packs.json").write_text(json.dumps({"packs": [
        {"id": "a", "name": "A"}, {"id": "b", "name": "B"},
    ]}), encoding="utf-8")
    packs = load_packs(str(tmp_path))
    assert set(packs) == {"a", "b"}
    assert packs["a"]["name"] == "A"


def test_파일이_없으면_빈dict(tmp_path):
    assert load_packs(str(tmp_path)) == {}


def test_id없는_항목은_무시(tmp_path):
    (tmp_path / "packs.json").write_text(json.dumps({"packs": [
        {"name": "no id"}, {"id": "ok"},
    ]}), encoding="utf-8")
    assert set(load_packs(str(tmp_path))) == {"ok"}


def test_실제_packs_json이_로드되고_스키마를_지킨다():
    packs = load_packs()
    assert packs, "assets/motion/packs.json이 비었다"
    for pid, p in packs.items():
        assert p["id"] == pid
        assert p.get("name")
        assert p.get("intensity") in ("low", "mid", "high")
        for key in ("transition", "sticker"):
            sub = p.get(key) or {}
            if sub:
                assert sub.get("policy") in ("every_beat", "hook_climax", "none")
```

- [ ] **Step 2: 테스트 실패 확인**

Run: `python -m pytest shopping_shorts/tests/test_motion_packs.py -k "load_packs or packs_json or id없는 or 실제" -v`
Expected: FAIL — `ImportError: cannot import name 'load_packs'`

- [ ] **Step 3: 로더 구현**

`motion_packs.py`의 `_boundaries` 정의 **바로 앞**에 추가:

```python
def load_packs(assets_dir=DEFAULT_ASSETS_DIR):
    """assets_dir/packs.json을 읽어 {pack_id: pack}으로 인덱싱. 없으면 {}.
    packs.json(큐레이션)은 manifest.json(자산 빌드산출물)과 수명주기가 달라 파일을 분리한다.
    """
    ppath = Path(assets_dir) / "packs.json"
    if not ppath.exists():
        return {}
    data = json.loads(ppath.read_text(encoding="utf-8"))
    return {p["id"]: p for p in data.get("packs", []) if p.get("id")}
```

- [ ] **Step 4: `packs.json` 스켈레톤 작성**

`shopping_shorts/assets/motion/packs.json` 신규. **현재 존재하는 자산(`swipe_left`, `sparkle`)만** 참조한다 — Task 7에서 자산과 값을 채운다.

```json
{
  "packs": [
    {
      "id": "dynamic_pop",
      "name": "다이나믹 팝",
      "intensity": "high",
      "transition": {"asset_id": "swipe_left", "dur": 0.5, "lead": 0.25, "policy": "every_beat"},
      "sticker": {"asset_id": "sparkle", "dur": 1.0, "policy": "hook_climax"},
      "caption": {"effect": "pop"},
      "headcopy": {"policy": "hook_only"},
      "color_filter": "eq=saturation=1.15:contrast=1.05"
    },
    {
      "id": "minimal_chic",
      "name": "미니멀 시크",
      "intensity": "low",
      "transition": {"asset_id": "swipe_left", "dur": 0.4, "lead": 0.2, "policy": "hook_climax"},
      "sticker": {"asset_id": "sparkle", "dur": 0.8, "policy": "none"},
      "caption": {"effect": "fade"},
      "headcopy": {"policy": "always"},
      "color_filter": "eq=saturation=0.95:contrast=1.02"
    }
  ]
}
```

- [ ] **Step 5: 테스트 통과 확인**

Run: `python -m pytest shopping_shorts/tests/test_motion_packs.py -v`
Expected: PASS (12 passed)

- [ ] **Step 6: 커밋**

```bash
git add shopping_shorts/motion_packs.py shopping_shorts/assets/motion/packs.json shopping_shorts/tests/test_motion_packs.py
git commit -m "feat(motion): load_packs + packs.json 스켈레톤(팩 값은 Task7에서 근거로 채움)"
```

---

## Task 4: 색감 필터를 drawtext **앞**으로 (Phase 1 동작 변경)

지금 `color_filter`가 자막 drawtext **뒤**에 붙어 **색보정이 자막에까지 걸린다**(video_assemble.py:620, 626-627). 팩 색감은 소스 영상에만 걸려야 한다.

**Files:**
- Modify: `shopping_shorts/video_assemble.py:574`, `:619-627`
- Test: `shopping_shorts/tests/test_motion_color_order.py`

**Interfaces:**
- Consumes: 없음
- Produces: 없음(내부 동작 변경). `deco.motion.color_filter` 계약은 그대로.

- [ ] **Step 1: 실패하는 테스트 작성**

`shopping_shorts/tests/test_motion_color_order.py` 신규:

```python
from unittest.mock import patch

from shopping_shorts import video_assemble


def _run(deco, tmp_path):
    """_burn_captions를 ffmpeg 없이 돌리고, 실행된 커맨드를 돌려준다."""
    seen = {}

    def fake_run(cmd, cwd=None):
        seen["cmd"] = cmd
        return None

    plan = {"beats": [{"beat_idx": 0, "role": "hook", "narration": "훅 문장"}]}
    with patch.object(video_assemble, "_run_ffmpeg", fake_run), \
         patch.object(video_assemble, "_resolve_font", return_value=str(tmp_path / "f.ttf")), \
         patch.object(video_assemble, "shutil"), \
         patch.object(video_assemble, "_probe_duration", return_value=3.0):
        video_assemble._burn_captions("in.mp4", plan, {0: "a.mp3"}, str(tmp_path / "out.mp4"),
                                      tmp_path, headcopy=None, caption_style=None, deco=deco)
    return seen["cmd"]


def test_색감필터는_scale직후_drawtext앞에_온다(tmp_path):
    cmd = _run({"motion": {"color_filter": "eq=saturation=1.5"}}, tmp_path)
    vf = cmd[cmd.index("-vf") + 1]
    assert "eq=saturation=1.5" in vf
    assert vf.index("eq=saturation=1.5") < vf.index("drawtext"), \
        "색감이 drawtext 뒤에 오면 자막색이 물든다"
    assert vf.index("scale=") < vf.index("eq=saturation=1.5")


def test_색감필터가_없으면_vf_무변경(tmp_path):
    cmd = _run({}, tmp_path)
    vf = cmd[cmd.index("-vf") + 1]
    assert "eq=" not in vf
```

- [ ] **Step 2: 테스트 실패 확인**

Run: `python -m pytest shopping_shorts/tests/test_motion_color_order.py -v`
Expected: FAIL — `test_색감필터는_scale직후_drawtext앞에_온다`가 AssertionError(색감이 뒤에 있음)

- [ ] **Step 3: 구현 — 색감을 filters 리스트에 삽입**

`video_assemble.py:574` 아래(`filters = [...]` 다음 줄)에 삽입:

```python
    filters = [f"scale={_OUT_W}:{_OUT_H}:force_original_aspect_ratio=increase,crop={_OUT_W}:{_OUT_H}"]
    # 색감 필터는 drawtext보다 **앞**에 온다 — 뒤에 두면 자막·헤드카피까지 색보정에 물든다.
    _color_filter = ((deco or {}).get("motion") or {}).get("color_filter") or ""
    if _color_filter.strip():
        filters.append(_color_filter.strip())
```

`:617`의 `color_filter` 재계산과 `:620`·`:626`의 뒤쪽 부착을 제거한다.

교체 전:
```python
    color_filter = (motion.get("color_filter") or "").strip()
    has_motion = bool(motion_layers)
    if not has_bgm and not has_overlay and not has_motion:
        base_vf = f"{vf},{color_filter}" if color_filter else vf
```
교체 후:
```python
    has_motion = bool(motion_layers)
    if not has_bgm and not has_overlay and not has_motion:
        base_vf = vf
```

교체 전:
```python
    inputs = ["-i", str(in_video)]
    base_vf = f"{vf},{color_filter}" if color_filter else vf
    fc = [f"[0:v]{base_vf}[v0]"]
```
교체 후:
```python
    inputs = ["-i", str(in_video)]
    fc = [f"[0:v]{vf}[v0]"]
```

- [ ] **Step 4: 테스트 통과 확인**

Run: `python -m pytest shopping_shorts/tests/test_motion_color_order.py -v`
Expected: PASS (2 passed)

- [ ] **Step 5: 회귀 확인**

Run: `python -m pytest shopping_shorts/tests/ -k "motion or assemble or caption" -v`
Expected: PASS — 실패 0

- [ ] **Step 6: 커밋**

```bash
git add shopping_shorts/video_assemble.py shopping_shorts/tests/test_motion_color_order.py
git commit -m "fix(motion): 색감필터를 drawtext 앞으로 — 자막이 색보정에 물들던 것 수정"
```

---

## Task 5: 헤드카피 `hook_only` enable + 폰트 스킵 경고

**Files:**
- Modify: `shopping_shorts/video_assemble.py:534-541`(`_headcopy_drawtext_parts`), `:570-572`(폰트 조기 return), `:584-585`(호출부)
- Test: `shopping_shorts/tests/test_motion_headcopy_enable.py`

**Interfaces:**
- Consumes: Task 2의 `build_plan` 반환 키 `headcopy_enable`
- Produces: `_headcopy_drawtext_parts(hc, work, enable=None) -> list[str]` — Task 6이 `deco.motion._headcopy_enable`로 넘긴다.

- [ ] **Step 1: 실패하는 테스트 작성**

`shopping_shorts/tests/test_motion_headcopy_enable.py` 신규:

```python
from unittest.mock import patch

from shopping_shorts.video_assemble import _headcopy_drawtext_parts


def _hc():
    return {"text": "이거 실화?", "color": "#FFE100", "size": 70, "x": 50, "y": 12}


def test_enable이_없으면_기존처럼_enable절이_안붙는다(tmp_path):
    with patch("shopping_shorts.video_assemble._segmented_drawtext",
               return_value=["drawtext=textfile=hc_0.txt:y=100"]):
        parts = _headcopy_drawtext_parts(_hc(), tmp_path)
    assert parts == ["drawtext=textfile=hc_0.txt:y=100"]
    assert "enable" not in parts[0]


def test_enable을_주면_모든_조각에_붙는다(tmp_path):
    with patch("shopping_shorts.video_assemble._segmented_drawtext",
               return_value=["drawtext=a", "drawtext=b"]):
        parts = _headcopy_drawtext_parts(_hc(), tmp_path, enable="between(t,0,3.000)")
    assert parts == ["drawtext=a:enable='between(t,0,3.000)'",
                     "drawtext=b:enable='between(t,0,3.000)'"]
```

- [ ] **Step 2: 테스트 실패 확인**

Run: `python -m pytest shopping_shorts/tests/test_motion_headcopy_enable.py -v`
Expected: FAIL — `test_enable을_주면...`이 `TypeError: _headcopy_drawtext_parts() got an unexpected keyword argument 'enable'`

- [ ] **Step 3: 구현**

`video_assemble.py:534-541` 교체:

```python
def _headcopy_drawtext_parts(hc, work, enable=None):
    """헤드카피 drawtext 필터 리스트 — _segmented_drawtext 래퍼(기본색 오렌지).
    hc['highlight_rules']가 있으면 단어별 강조, 없으면 세그먼트 1개(기존과 동일).
    enable(ffmpeg between 식)이 주어지면 노출 구간을 제한한다(팩 headcopy.policy=hook_only).
    None이면 기존대로 영상 전체 고정 표시.
    """
    parts = _segmented_drawtext(
        hc.get("text", ""), hc, work, "hc", hc.get("x", 50), hc.get("y", 14),
        highlight_rules=hc.get("highlight_rules"), default_color="0xFF8800",
    )
    if not enable:
        return parts
    return [f"{p}:enable='{enable}'" for p in parts]
```

`:584-585` 호출부 교체:

```python
    if headcopy and (headcopy.get("text") or "").strip():
        # enable 없으면 전체 표시(기존). 팩이 hook_only면 렌더 파생값 _headcopy_enable이 온다.
        hc_enable = ((deco or {}).get("motion") or {}).get("_headcopy_enable")
        filters.extend(_headcopy_drawtext_parts(headcopy, work, enable=hc_enable))
```

- [ ] **Step 4: 폰트 스킵 경고 추가**

`video_assemble.py:570-572` 교체 — **동작은 그대로 두고 로그만 추가**한다. 이 경로는 모션·색감·오버레이·BGM을 통째로 스킵하는데 지금은 조용히 지나가서 원인 추적이 안 된다.

```python
    if not font:
        print("[motion] 폰트 미해결 — 자막·모션·색감·오버레이·BGM 전부 스킵하고 원본 복사",
              file=sys.stderr)
        shutil.copy(in_video, out_path)
        return str(out_path)
```

`video_assemble.py`에는 **`sys` import가 없다**(현재 `os, re, shutil, subprocess, textwrap, uuid`만).
import 블록(14-20행)의 `import shutil` 위에 알파벳 순으로 `import sys`를 추가한다 — 상수 정의보다 **위**(E402 회피).

- [ ] **Step 5: 테스트 통과 확인**

Run: `python -m pytest shopping_shorts/tests/test_motion_headcopy_enable.py -v`
Expected: PASS (2 passed)

- [ ] **Step 6: 회귀 확인**

Run: `python -m pytest shopping_shorts/tests/ -k "headcopy or caption or assemble or motion" -v`
Expected: PASS — 실패 0

- [ ] **Step 7: 커밋**

```bash
git add shopping_shorts/video_assemble.py shopping_shorts/tests/test_motion_headcopy_enable.py
git commit -m "feat(motion): 헤드카피 hook_only enable 지원 + 폰트 스킵 경고로그"
```

---

## Task 6: 배선 — `pack_id` → `build_plan` → `resolve_layers`

**Files:**
- Modify: `shopping_shorts/mix_pipeline.py:251-255`
- Test: `shopping_shorts/tests/test_mix_pipeline_pack.py`

**Interfaces:**
- Consumes: `_beat_timeline`(Task 1), `load_packs`·`build_plan`(Task 2·3), `resolve_layers`(기존)
- Produces: 없음(배선). `assemble`에 넘어가는 `deco`가 완성형이 된다.

- [ ] **Step 1: 실패하는 테스트 작성**

`shopping_shorts/tests/test_mix_pipeline_pack.py` 신규:

```python
from shopping_shorts.mix_pipeline import _apply_motion_pack

TL = [
    {"beat_idx": 0, "t0": 0.0, "dur": 3.0, "narration": "훅", "role": "hook"},
    {"beat_idx": 1, "t0": 3.0, "dur": 4.0, "narration": "본문", "role": "body"},
]

PACKS = {"p1": {
    "id": "p1",
    "transition": {"asset_id": "swipe_left", "dur": 0.5, "lead": 0.25, "policy": "every_beat"},
    "caption": {"effect": "pop"},
    "headcopy": {"policy": "hook_only"},
    "color_filter": "eq=saturation=1.2",
}}


def test_pack_id가_레이어와_색감_자막효과를_채운다():
    deco, cs = _apply_motion_pack({"motion": {"pack_id": "p1"}}, {}, TL, PACKS)
    assert [L["asset_id"] for L in deco["motion"]["layers"]] == ["swipe_left"]
    assert deco["motion"]["color_filter"] == "eq=saturation=1.2"
    assert deco["motion"]["_headcopy_enable"] == "between(t,0,3.000)"
    assert cs["effect"] == "pop"


def test_사용자_지정값이_팩보다_우선():
    deco, cs = _apply_motion_pack(
        {"motion": {"pack_id": "p1", "color_filter": "eq=contrast=2"}},
        {"effect": "slide"}, TL, PACKS)
    assert deco["motion"]["color_filter"] == "eq=contrast=2"
    assert cs["effect"] == "slide"


def test_수동_레이어는_팩_레이어_뒤에_보존된다():
    manual = {"asset_id": "sparkle", "start": 9.0, "dur": 1.0}
    deco, _ = _apply_motion_pack(
        {"motion": {"pack_id": "p1", "layers": [manual]}}, {}, TL, PACKS)
    assert [L["asset_id"] for L in deco["motion"]["layers"]] == ["swipe_left", "sparkle"]


def test_모르는_pack_id는_모션없이_통과():
    deco, cs = _apply_motion_pack({"motion": {"pack_id": "없음"}}, {}, TL, PACKS)
    assert deco["motion"].get("layers", []) == []
    assert cs == {}


def test_pack_id가_없으면_deco_무변경():
    src = {"motion": {"layers": [{"asset_id": "sparkle", "start": 1.0, "dur": 1.0}]}}
    deco, cs = _apply_motion_pack(src, {}, TL, PACKS)
    assert deco["motion"]["layers"] == src["motion"]["layers"]


def test_motion이_아예_없어도_안전():
    deco, cs = _apply_motion_pack({}, None, TL, PACKS)
    assert deco == {}
    assert cs is None
```

- [ ] **Step 2: 테스트 실패 확인**

Run: `python -m pytest shopping_shorts/tests/test_mix_pipeline_pack.py -v`
Expected: FAIL — `ImportError: cannot import name '_apply_motion_pack'`

- [ ] **Step 3: 헬퍼 구현**

`mix_pipeline.py`의 `run_render` 정의 **바로 앞**에 추가:

```python
def _apply_motion_pack(deco, caption_style, timeline, packs):
    """deco.motion.pack_id → 팩 정책으로 레이어·색감·자막효과·헤드카피 노출을 채운다.

    핵심: 레이어는 **저장하지 않고 렌더 시점에 만든다**(비트 경계는 TTS 실길이로 정해지므로).
    저장되는 건 pack_id뿐. 사용자가 직접 지정한 값(color_filter·caption effect)이 팩보다 우선한다.
    반환: (deco, caption_style) — 원본을 변형하지 않은 얕은 복사본.
    """
    motion = (deco or {}).get("motion") or {}
    pack_id = motion.get("pack_id")
    if not pack_id:
        return deco, caption_style
    pack = packs.get(pack_id)
    if not pack:
        print(f"[motion] 모르는 pack_id={pack_id!r} — 모션 없이 진행", file=sys.stderr)
        return deco, caption_style

    p = build_plan(pack, timeline)
    new_motion = {**motion, "layers": p["layers"] + list(motion.get("layers") or [])}
    if not motion.get("color_filter"):
        new_motion["color_filter"] = p["color_filter"]
    if p["headcopy_enable"]:
        new_motion["_headcopy_enable"] = p["headcopy_enable"]   # DB 저장 안 함(렌더 파생값)
    deco = {**deco, "motion": new_motion}

    if p["caption_effect"] and not (caption_style or {}).get("effect"):
        caption_style = {**(caption_style or {}), "effect": p["caption_effect"]}
    return deco, caption_style
```

import 블록(`mix_pipeline.py:11-21`)의 `from shopping_shorts.motion_assets import ...` 줄 **바로 아래**에 추가:

```python
from shopping_shorts.motion_packs import build_plan, load_packs
```

`sys`는 이미 import돼 있다(5행) — 추가 불필요.
**주의**: `MOTION_ASSETS_DIR = DEFAULT_ASSETS_DIR`(25행)은 import 블록 **뒤**에 있어야 한다(E402).
새 import를 25행 아래에 넣지 말 것.

- [ ] **Step 4: `run_render`에 배선**

`mix_pipeline.py:251-255`의 모션 해석 블록 교체.

교체 전:
```python
        # 모션 레이어(전환·스티커): asset_id → 실경로·기본배치 해석
        motion = deco.get("motion") or {}
        if motion.get("layers"):
            resolved = resolve_layers(motion["layers"], MOTION_ASSETS_DIR)
            deco = {**deco, "motion": {**motion, "layers": resolved}}
        assemble(plan, tts_paths, source_video_paths, str(out_path), clean_fn=clean_fn,
                 headcopy=job.get("headcopy"), caption_style=job.get("caption_style"),
                 deco=deco)
```

교체 후:
```python
        # 모션 팩: pack_id → 비트 타임라인으로 레이어 생성(렌더 시점에만 알 수 있음)
        caption_style = job.get("caption_style")
        timeline = _beat_timeline(plan, tts_paths)
        deco, caption_style = _apply_motion_pack(deco, caption_style, timeline, load_packs())
        # 모션 레이어(전환·스티커): asset_id → 실경로·기본배치 해석
        motion = deco.get("motion") or {}
        if motion.get("layers"):
            resolved = resolve_layers(motion["layers"], MOTION_ASSETS_DIR)
            deco = {**deco, "motion": {**motion, "layers": resolved}}
        assemble(plan, tts_paths, source_video_paths, str(out_path), clean_fn=clean_fn,
                 headcopy=job.get("headcopy"), caption_style=caption_style,
                 deco=deco)
```

`_beat_timeline` import를 추가한다 — `mix_pipeline.py:18`이 이미 `assemble`을 import 중이므로 **그 줄에 합친다**:

```python
from shopping_shorts.video_assemble import assemble, _beat_timeline
```

- [ ] **Step 5: 테스트 통과 확인**

Run: `python -m pytest shopping_shorts/tests/test_mix_pipeline_pack.py -v`
Expected: PASS (6 passed)

- [ ] **Step 6: 앱 부팅·전체 회귀 확인**

Run: `python -c "from shopping_shorts.app import app; print('boot ok')"`
Expected: `boot ok`

Run: `python -m pytest shopping_shorts/tests/ -q`
Expected: 신규 실패 0 (기존 실패가 있으면 그 목록을 그대로 보고할 것 — 새로 만든 실패만 책임진다)

- [ ] **Step 7: 커밋**

```bash
git add shopping_shorts/mix_pipeline.py shopping_shorts/tests/test_mix_pipeline_pack.py
git commit -m "feat(motion): pack_id→build_plan→resolve_layers 배선(레이어는 렌더시점 생성)"
```

---

## Task 7: 팩 자산 + 실제 팩 값 채우기 (레퍼런스 근거)

지금 자산은 `swipe_left`·`sparkle` 둘뿐이라 팩 2종이 같은 전환을 쓴다. 레퍼런스를 근거로 팩을 확정하고 부족한 자산을 만든다.

**Files:**
- Modify: `shopping_shorts/assets/motion/packs.json`, `shopping_shorts/assets/motion/manifest.json`
- Create: `shopping_shorts/motion/src/<신규컴포지션>.tsx` (분석 결과에 따라)
- Modify: `shopping_shorts/motion/src/Root.tsx`, `shopping_shorts/motion/src/render-library.mjs`

**Interfaces:**
- Consumes: Task 3의 `packs.json` 스키마, `load_packs` 스키마 테스트
- Produces: 확정된 팩 3~4종. Task 8이 `FULL_PRESETS`에서 `id`로 참조한다.

- [ ] **Step 1: 레퍼런스 랭킹 상위 영상에서 모션 패턴 추출**

레퍼런스 랭킹은 DB 테이블이 아니라 `settings`의 `last_run::<platform>`에 JSON으로 들어있다
(`store.load_last_run_platform(platform) -> (items, collected_at)`). DB는 `shopping_shorts/config.py`의
`DB_PATH`(= `shopping_shorts/data/reference.db`).

Run:
```bash
python -c "
from shopping_shorts.config import DB_PATH
from shopping_shorts.store import Store
items, at = Store(DB_PATH).load_last_run_platform('instagram')
print('collected_at:', at, '| items:', len(items))
items = sorted(items, key=lambda x: x.get('score') or 0, reverse=True)
for r in items[:10]:
    print(r.get('shortcode'), round(r.get('score') or 0, 2), (r.get('caption') or '')[:40].replace(chr(10), ' '))
"
```
Expected: 상위 10개 shortcode·score 출력. **비어 있으면**(`items: 0`) 대시보드에서 "지금 수집"을
먼저 돌리거나, `'tiktok'`/`'youtube'` 플랫폼으로 바꿔 시도한다.

상위 10개 영상을 받아 **전환 유형·컷 리듬(컷/초)·타이포 등장 방식·색감**을 관찰한다.
영상 다운로드는 기존 `download_video`, Gemini 영상분석은 `script_extract.extract_script(video_path,
video_id, caption="")` 패턴을 참고해 프롬프트만 모션용으로 바꿔 구성한다.

**관찰 결과를 이 계획서 아래 "관찰 로그"에 적는다**(팩 값의 근거를 남기기 위함 — Phase 2b 분석기가 이걸 자동화한다).

- [ ] **Step 2: 팩 3~4종 확정**

관찰에서 나온 군집으로 팩을 정의한다. 각 팩은 반드시:
- `intensity`가 서로 다를 것(팩 = 곡 템포)
- `transition.policy`가 서로 다를 것(강도가 팩 정체성)
- `color_filter`가 관찰된 색감을 반영할 것

`packs.json`을 갱신한다. **스키마는 Task 3 그대로**(`load_packs` 스키마 테스트가 지킨다).

- [ ] **Step 3: 부족한 자산 컴포지션 추가**

팩이 `swipe_left`·`sparkle` 외 자산을 요구하면 `motion/src/`에 컴포지션을 만들고:
- `Root.tsx`에 `<Composition>` 등록
- `render-library.mjs`의 `LIB` 배열에 `{id, file}` 추가
- `manifest.json`에 `{id, type, file, default}` 추가

**투명 자산 규칙(Phase 1 하드-원)**: `drawbox`는 알파를 안 박는다 → 불투명 도형을 투명 캔버스에 `overlay=format=auto`로 합성할 것. 출력은 ProRes4444 → qtrle `.mov`(argb).

- [ ] **Step 4: 자산 렌더**

Run:
```bash
cd shopping_shorts/motion && npm run render:library
```
Expected: `shopping_shorts/assets/motion/`에 팩이 참조하는 `.mov`가 전부 생성됨.

Run(알파 확인):
```bash
ffprobe -v error -select_streams v:0 -show_entries stream=pix_fmt,codec_name -of csv=p=0 shopping_shorts/assets/motion/<new>.mov
```
Expected: `qtrle,argb`

- [ ] **Step 5: 스키마 테스트 통과 확인**

Run: `python -m pytest shopping_shorts/tests/test_motion_packs.py -v`
Expected: PASS — 특히 `test_실제_packs_json이_로드되고_스키마를_지킨다`

Run(팩이 참조하는 자산이 전부 실존하는지):
```bash
python -c "
from shopping_shorts.motion_packs import load_packs
from shopping_shorts.motion_assets import load_manifest
m, missing = load_manifest(), []
for pid, p in load_packs().items():
    for k in ('transition', 'sticker'):
        aid = (p.get(k) or {}).get('asset_id')
        if aid and aid not in m: missing.append((pid, k, aid))
print('missing:', missing or 'none')
"
```
Expected: `missing: none`

- [ ] **Step 6: 커밋**

```bash
git add shopping_shorts/assets/motion/ shopping_shorts/motion/
git commit -m "feat(motion): 팩 3~4종 확정 + 자산 추가(레퍼런스 랭킹 상위 관찰 근거)"
```

---

## Task 8: 꾸미기 UI — 스타일 카드에 팩 내장

**Files:**
- Modify: `shopping_shorts/static/produce.html:495`(`FULL_PRESETS`), `:627-640`(`applyFullPreset`)

**Interfaces:**
- Consumes: Task 7의 확정 팩 `id`
- Produces: `STATE.deco.motion = {pack_id}` — 기존 `saveHeadcopy()`가 `/api/produce/mix/settings`로 저장(신규 엔드포인트 없음)

- [ ] **Step 1: `FULL_PRESETS`에 `motion` 필드 추가**

각 프리셋에 팩을 매핑한다. 필드가 없으면 모션 없음(하위호환). 예:

```javascript
  {name:'임팩트 옐로', chip:'#FFE100', motion:'dynamic_pop',
    hc:{font:'TmonMonsori.ttf',color:'#FFE100',weight:900,size:76,x:50,y:12,outline:true,outline_color:'#000000',outline_w:9},
```

```javascript
  {name:'모던 화이트', chip:'#FFFFFF', motion:'minimal_chic',
```

**18개 전부에 매핑한다.** 기준: 강한 색·굵은 폰트 → `dynamic_pop` / 차분한 화이트·세리프 → `minimal_chic` / 나머지는 Task 7에서 확정된 팩으로.

- [ ] **Step 2: `applyFullPreset`이 팩을 STATE에 반영**

`produce.html:627-640` 교체:

```javascript
function applyFullPreset(i){
  const p = FULL_PRESETS[i];
  applyConfig(p.hc, p.cap);
  STATE.deco = STATE.deco || {extra_texts: []};
  // 모션 팩: pack_id만 저장한다. 실제 레이어는 렌더 시점에 비트 타임라인으로 계산됨(서버).
  if(p.motion){ STATE.deco.motion = {...(STATE.deco.motion||{}), pack_id: p.motion}; }
  else if(STATE.deco.motion){ delete STATE.deco.motion.pack_id; }
  if(p.extra){
    // 프리셋의 extra는 텍스트가 비어있음(스타일만 지정) — 기존 문구가 있으면 유지, 없으면 빈칸
    const prevText = (STATE.deco.extra_texts[0] && STATE.deco.extra_texts[0].text) || '';
    STATE.deco.extra_texts = p.extra.map((e,idx)=>({...e, text: idx===0 ? prevText : (e.text||'')}));
  } else {
    STATE.deco.extra_texts = [];
  }
  renderExtraTexts(); renderExtraPreview();
  syncMotionPicker();
  document.querySelectorAll('.fullPreset').forEach((b,idx)=>b.style.outline = idx===i?'2px solid var(--accent2)':'none');
}
```

- [ ] **Step 3: 고급 접기에 팩 교체·끄기 UI**

"세부 직접 다듬기" 접기 영역 안에 추가:

```html
<div style="margin-top:10px">
  <label style="font-size:12px;opacity:.8">모션 팩</label>
  <select id="motionPack" onchange="pickMotionPack(this.value)"
          style="width:100%;padding:6px;border-radius:6px">
    <option value="">모션 끄기</option>
  </select>
</div>
```

스크립트에 추가:

```javascript
// 모션 팩 목록(서버 packs.json과 id가 일치해야 함 — Task7에서 확정)
const MOTION_PACKS=[{id:'dynamic_pop',name:'다이나믹 팝'},{id:'minimal_chic',name:'미니멀 시크'}];
function syncMotionPicker(){
  const sel=document.getElementById('motionPack'); if(!sel) return;
  if(sel.options.length<=1){
    MOTION_PACKS.forEach(p=>{ const o=document.createElement('option');
      o.value=p.id; o.textContent=p.name; sel.appendChild(o); });
  }
  sel.value = ((STATE.deco||{}).motion||{}).pack_id || '';
}
function pickMotionPack(id){
  STATE.deco = STATE.deco || {extra_texts: []};
  STATE.deco.motion = STATE.deco.motion || {};
  if(id) STATE.deco.motion.pack_id = id; else delete STATE.deco.motion.pack_id;
  saveHeadcopy();
}
```

`initHeadcopy()` 끝에 `syncMotionPicker();`를 추가한다.

- [ ] **Step 4: JS 문법 검증**

Run:
```bash
node --check <(sed -n '/<script>/,/<\/script>/p' shopping_shorts/static/produce.html | sed '1d;$d')
```
Expected: 출력 없음(문법 OK). 파일에 `<script>` 블록이 여러 개면 각각 확인한다.

- [ ] **Step 5: 커밋 (공유파일 — 내 hunk만)**

```bash
git branch --show-current                     # main 확인
git status --short shopping_shorts/static/produce.html
git add -p shopping_shorts/static/produce.html   # 내 hunk만 선택(대본모달 트랙 hunk 제외)
git diff --cached --name-only                 # produce.html 하나만인지 확인
git commit -m "feat(motion): 완성스타일 카드에 모션 팩 내장 + 고급 팩 교체/끄기"
```

---

## Task 9: 실렌더 육안 grounding + 배포

격리 테스트는 배선 버그(저장위치≠읽기위치)를 못 잡는다. **실제 payload 경로**로 확인한다. ([[feedback_whole_branch_review_catches_seams]])

**Files:** 없음(검증·배포)

**Interfaces:**
- Consumes: Task 1~8 전부

- [ ] **Step 1: 로컬 서버 기동**

Run(**repo 루트에서** — 절대임포트):
```bash
python -m uvicorn shopping_shorts.app:app --port 8899
```
Expected: 부팅 에러 없음

- [ ] **Step 2: 실제 믹스 1건 렌더**

브라우저로 `http://localhost:8899/produce` → 대본·소스 넣고 5단계 꾸미기에서 **'임팩트 옐로'(dynamic_pop)** 카드 클릭 → 렌더.

- [ ] **Step 3: 최종 mp4 육안 확인 (완료 정의)**

`work/<job_id>/final.mp4`를 열어 **네 가지**를 눈으로 확인한다:

1. **전환이 비트 경계에 맞는가** — 자막이 바뀌는 그 지점에 전환이 오는가(밀리면 Task 1 타임라인 공유가 깨진 것)
2. **자막이 색감에 물들지 않는가** — Task 4의 성공 정의
3. **헤드카피가 훅 구간에만 나오는가**(dynamic_pop = hook_only)
4. **스티커가 훅·마지막에 뜨는가**

- [ ] **Step 4: 팩 끄기 회귀 확인**

고급에서 "모션 끄기" → 재렌더 → 전환·스티커·색감이 전부 사라지고 **Phase 1 이전과 동일한 영상**이 나오는지 확인.

- [ ] **Step 5: 배포**

```bash
git branch --show-current      # main
git push origin main
```
서버 크론(3분)이 자동 pull + `shopping-shorts` 재시작한다. **서버에서 직접 `git add`/`checkout` 하지 말 것**(배포가 통째로 멈춘다 — CLAUDE.md #9).

- [ ] **Step 6: 라이브 확인**

Run:
```bash
ssh -i C:\Users\TheRose\crawling_bot_client\LightsailDefaultKey-ap-northeast-2.pem ubuntu@3.39.179.148 \
  "cd /home/ubuntu/lotto-stock-wiki && git log --oneline -1 && git status --short && tail -3 /tmp/auto_deploy.log"
```
Expected: HEAD가 내 커밋 · `git status --short` 깨끗함 · 배포 로그에 pull 성공

`https://shoppingshorts.duckdns.org/produce`에서 꾸미기 카드가 뜨는지 확인.

- [ ] **Step 7: 문서 갱신 + 커밋**

`NEXT_SESSION.md`의 모션효과 섹션을 Phase 2a 완료 / Phase 2b(상시 분석기·Remotion 텍스트렌더) 미착수로 갱신하고, `wiki/log.md`에 1~3줄 기록한다.

```bash
git add NEXT_SESSION.md wiki/log.md
git commit -m "docs(motion): Phase2a 완료 — 팩 엔진·자산·UI 배포"
git push origin main
```

---

## 관찰 로그 (Task 7에서 채움)

> 팩 값의 근거. Phase 2b 분석기가 이 관찰을 자동화한다.
> 형식: `영상 shortcode | 전환유형 | 컷/초 | 타이포 등장 | 색감` — Task 7 Step 1에서 채운다.

(비어 있음 — Task 7 실행 시 기록)
