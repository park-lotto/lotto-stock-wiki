# 자막↔음성 싱크 자동교정 (1단계) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax.

**Goal:** 비트 안 자막 조각의 표시 시간을 글자수 추정이 아니라 실제 말한 타이밍(ASR 워드 타임스탬프)으로 찍어 자막↔음성 미세 드리프트를 없앤다. 화면 변경 없음.

**Architecture:** ①`asr_check.transcribe_words`가 Whisper에서 워드 타임스탬프를 받고 ②순수 모듈 `caption_sync.py`가 (대본↔ASR 단어) difflib 정렬로 각 자막 구절의 실제 표시 시간을 계산하며 ③`_caption_durations`가 그 값을 받으면 쓰고 없으면 현행 글자수 비례로 폴백 ④`_synthesize_beats`가 비트 합성 직후 계산해 `beat["cap_durs"]`에 저장, 자막 렌더는 그 값을 읽는다(저장위치=읽기위치).

**Tech Stack:** Python 3, pytest, GROQ Whisper(verbose_json), difflib.

## Global Constraints

- **파일은 `.tracks/자막싱크/` 안에서만 수정.** main 폴더 코드 금지.
- **`.py`는 LF로 쓴다**(`newline='\n'`). CRLF면 git status에 유령 M.
- **회귀 0 원칙**: GROQ 키 없음/ASR 실패/신뢰도 미달이면 **현행과 바이트 동일 출력**. 로컬은 키가 없어 실개발이 이 경로다.
- **자막 문구·줄바꿈·조각 경계(`_caption_segments`)는 바꾸지 않는다.** 오직 각 조각의 **표시 시간**만.
- **새 렌더 경로를 만들지 마라.** 미리보기·최종이 같은 `_synthesize_beats`를 타야 같은 자막 타이밍을 본다(2026-07-15 seam 10건 교훈).
- **회귀 기준선(트랙 시작 실측 필수)**: 착수 전 `python -m pytest shopping_shorts/tests/ -q 2>&1 | tail -3`로 실패 수를 적어둔다. "전부 green"이 아니라 "실패가 늘지 않았나"로 판정.
- 스펙: `docs/superpowers/specs/2026-07-18-자막-음성싱크-자동교정-design.md`.

## File Structure

- **`shopping_shorts/asr_check.py`** (수정) — `transcribe_words(mp3_path)` 추가. 기존 `transcribe`/`diff_words`/`mismatch_score` 불변.
- **`shopping_shorts/caption_sync.py`** (신규) — 순수 정렬 함수 `phrase_durs_from_words(narration, words, total_dur)`. `video_assemble._caption_segments` import.
- **`shopping_shorts/video_assemble.py`** (수정) — `_caption_durations`에 `real_durs=None` 인자, `_caption_drawtexts`에 `real_durs=None` 인자, `_burn_captions` 호출부에서 `beat.get("cap_durs")` 전달.
- **`shopping_shorts/mix_pipeline.py`** (수정) — `_synthesize_beats`에 비트별 cap_durs 계산·저장.
- 테스트: `shopping_shorts/tests/test_transcribe_words.py`, `test_caption_sync.py`, `test_caption_durations_real.py`, `test_caption_sync_wiring.py` (신규).

---

### Task 1: `asr_check.transcribe_words` — Whisper 워드 타임스탬프

**Files:**
- Modify: `shopping_shorts/asr_check.py`
- Test: `shopping_shorts/tests/test_transcribe_words.py` (create)

**Interfaces:**
- Produces: `transcribe_words(mp3_path) -> list[dict] | None`. 성공 시 `[{"word": str, "start": float, "end": float}, …]`(발화 순서), 키 없음·실패·형식오류 시 `None`.

- [ ] **Step 1: 실패하는 테스트를 쓴다**

`shopping_shorts/tests/test_transcribe_words.py`:

```python
"""transcribe_words — Whisper verbose_json 워드 타임스탬프. 키 없으면 None(graceful)."""
import shopping_shorts.asr_check as ac


def test_no_key_returns_none(monkeypatch):
    monkeypatch.setattr(ac.config, "GROQ_API_KEY", "", raising=False)
    assert ac.transcribe_words("x.mp3") is None


def test_parses_word_list(monkeypatch, tmp_path):
    monkeypatch.setattr(ac.config, "GROQ_API_KEY", "k", raising=False)
    mp3 = tmp_path / "a.mp3"; mp3.write_bytes(b"ID3")

    class _Resp:
        def raise_for_status(self): pass
        def json(self):
            return {"words": [{"word": "귤은", "start": 0.0, "end": 0.4},
                              {"word": "손으로", "start": 0.4, "end": 0.9}]}

    monkeypatch.setattr(ac.requests, "post", lambda *a, **k: _Resp())
    out = ac.transcribe_words(str(mp3))
    assert out == [{"word": "귤은", "start": 0.0, "end": 0.4},
                   {"word": "손으로", "start": 0.4, "end": 0.9}]


def test_malformed_returns_none(monkeypatch, tmp_path):
    monkeypatch.setattr(ac.config, "GROQ_API_KEY", "k", raising=False)
    mp3 = tmp_path / "a.mp3"; mp3.write_bytes(b"ID3")

    class _Resp:
        def raise_for_status(self): pass
        def json(self): return {"text": "no words here"}   # 워드 없음

    monkeypatch.setattr(ac.requests, "post", lambda *a, **k: _Resp())
    assert ac.transcribe_words(str(mp3)) is None


def test_exception_returns_none(monkeypatch, tmp_path):
    monkeypatch.setattr(ac.config, "GROQ_API_KEY", "k", raising=False)
    mp3 = tmp_path / "a.mp3"; mp3.write_bytes(b"ID3")
    def _boom(*a, **k): raise RuntimeError("net")
    monkeypatch.setattr(ac.requests, "post", _boom)
    assert ac.transcribe_words(str(mp3)) is None
```

- [ ] **Step 2: 실패 확인**

Run: `python -m pytest shopping_shorts/tests/test_transcribe_words.py -q`
Expected: FAIL (`AttributeError: module 'shopping_shorts.asr_check' has no attribute 'transcribe_words'`)

- [ ] **Step 3: 구현**

`shopping_shorts/asr_check.py`의 `transcribe` 함수 **아래에** 추가:

```python
def transcribe_words(mp3_path):
    """GROQ Whisper verbose_json으로 워드 타임스탬프 재전사.
    성공 → [{"word","start","end"}, …](발화 순서). 키 없음·실패·워드 없음 → None.
    (transcribe와 같은 graceful 계약: 예외를 삼켜 None. 호출부가 폴백한다.)"""
    if not config.GROQ_API_KEY:
        return None
    try:
        with open(mp3_path, "rb") as f:
            r = requests.post(
                _GROQ_URL,
                headers={"Authorization": f"Bearer {config.GROQ_API_KEY}"},
                files={"file": (mp3_path, f, "audio/mpeg")},
                data={"model": _MODEL, "language": "ko",
                      "response_format": "verbose_json",
                      "timestamp_granularities[]": "word"},
                timeout=60)
        r.raise_for_status()
        raw = r.json().get("words")
        if not raw:
            return None
        out = []
        for w in raw:
            if "word" in w and "start" in w and "end" in w:
                out.append({"word": w["word"], "start": float(w["start"]),
                            "end": float(w["end"])})
        return out or None
    except Exception:
        return None
```

- [ ] **Step 4: 통과 확인**

Run: `python -m pytest shopping_shorts/tests/test_transcribe_words.py -q`
Expected: PASS (4 passed)

- [ ] **Step 5: 뮤테이션**

`if not raw: return None`을 `raw = raw or []`로 바꿔 → `test_malformed_returns_none`가 죽는지 확인(죽어야 함). 되돌린다.

- [ ] **Step 6: 커밋**

```bash
git add shopping_shorts/asr_check.py shopping_shorts/tests/test_transcribe_words.py
git commit -m "feat(자막싱크): asr_check.transcribe_words — Whisper 워드 타임스탬프"
```

---

### Task 2: `caption_sync.phrase_durs_from_words` — 정렬로 구절 실시간 산출 (핵심)

**Files:**
- Create: `shopping_shorts/caption_sync.py`
- Test: `shopping_shorts/tests/test_caption_sync.py` (create)

**Interfaces:**
- Consumes: `video_assemble._caption_segments(narration) -> list[str]` (기존).
- Produces: `phrase_durs_from_words(narration, words, total_dur) -> list[float] | None`. `words`는 Task 1의 `[{"word","start","end"}]`. 반환은 각 자막 구절의 **표시 시간**(초) 리스트로 `len == len(_caption_segments(narration))`, 총합 ≈ `total_dur`. 정렬 신뢰도 미달이면 `None`.

**설계 근거(구현자 필독):**
- 구절 i는 **자기 첫 단어가 발화되는 순간부터 다음 구절 첫 단어 발화 직전까지** 화면에 있다(중간 쉼도 현재 구절이 유지) → `durs[i] = start(seg i+1) - start(seg i)`, 마지막은 `total_dur - start(마지막 seg)`. 이래야 자막이 "말을 따라" 넘어간다.
- 대본 단어(=자막)와 ASR 단어(=말한 것)는 다르다(추임새·태그). `difflib.SequenceMatcher`로 **대본 단어열 ↔ ASR 단어열**을 정렬해 각 대본 단어의 시각을 얻는다. 매칭 안 된 대본 단어는 앞뒤 시각 선형 보간. ASR-only(추임새)는 무시.
- `diff_words`는 hyp 인덱스를 안 남겨 재사용 불가 → SequenceMatcher를 직접 쓴다(같은 difflib).

- [ ] **Step 1: 실패하는 테스트를 쓴다**

`shopping_shorts/tests/test_caption_sync.py`:

```python
"""phrase_durs_from_words — 대본↔ASR 정렬로 각 자막 구절의 실제 표시시간."""
import shopping_shorts.caption_sync as cs
from shopping_shorts.video_assemble import _caption_segments


def _w(word, s, e): return {"word": word, "start": s, "end": e}


def test_len_matches_segments_and_sums_total():
    narr = "귤은 손으로 까요 이제 시작합니다"
    words = [_w("귤은", 0.0, 0.4), _w("손으로", 0.4, 0.9), _w("까요", 0.9, 1.4),
             _w("이제", 2.0, 2.3), _w("시작합니다", 2.3, 3.0)]
    durs = cs.phrase_durs_from_words(narr, words, 3.0)
    assert durs is not None
    assert len(durs) == len(_caption_segments(narr))
    assert abs(sum(durs) - 3.0) < 1e-6
    assert all(d >= 0 for d in durs)


def test_caption_follows_voice_not_charcount():
    # 두 구절, 두번째가 글자수는 적지만 실제로는 늦게(2.0초) 시작 → 첫 구절이 길어야.
    narr = "이거 때문에 대박"
    words = [_w("이거", 0.0, 0.3), _w("때문에", 0.3, 0.7), _w("대박", 2.0, 2.5)]
    durs = cs.phrase_durs_from_words(narr, words, 2.5)
    segs = _caption_segments(narr)
    assert len(durs) == len(segs)
    # '대박' 구절은 2.0에 시작 → 그 앞 구절(들)의 합이 2.0에 가깝다.
    assert abs(sum(durs[:-1]) - 2.0) < 0.05


def test_filler_insertion_absorbed():
    # ASR이 앞에 추임새 '와'를 넣어도(대본엔 없음) 정렬이 밀리지 않는다.
    narr = "요새 이거 유행이에요"
    words = [_w("와", 0.0, 0.2), _w("요새", 0.2, 0.6), _w("이거", 0.6, 1.0),
             _w("유행이에요", 1.0, 1.8)]
    durs = cs.phrase_durs_from_words(narr, words, 1.8)
    assert durs is not None and len(durs) == len(_caption_segments(narr))


def test_low_confidence_returns_none():
    # ASR이 완전히 딴 소리 → 매칭률 낮음 → None(호출부가 글자수로 폴백).
    narr = "귤은 손으로 까요"
    words = [_w("사과", 0.0, 0.5), _w("바나나", 0.5, 1.0), _w("포도", 1.0, 1.5)]
    assert cs.phrase_durs_from_words(narr, words, 1.5) is None


def test_empty_words_returns_none():
    assert cs.phrase_durs_from_words("귤은 까요", [], 1.0) is None
```

- [ ] **Step 2: 실패 확인**

Run: `python -m pytest shopping_shorts/tests/test_caption_sync.py -q`
Expected: FAIL (`ModuleNotFoundError: shopping_shorts.caption_sync`)

- [ ] **Step 3: 구현**

`shopping_shorts/caption_sync.py` (신규):

```python
"""자막 구절 타이밍을 실제 음성(ASR 워드 타임스탬프)에 맞춘다.

순수·결정적. video_assemble._caption_segments가 만든 구절 경계는 그대로 두고,
각 구절의 '표시 시간'만 실제 말한 시각으로 다시 찍는다.
"""
import difflib

from .video_assemble import _caption_segments, _strip_punct

_MIN_MATCH_RATIO = 0.5   # 대본 단어 중 이만큼도 정렬 안 되면 신뢰불가 → None


def _norm(tok):
    return _strip_punct(tok)


def phrase_durs_from_words(narration, words, total_dur):
    """narration의 각 자막 구절 표시시간 리스트(초) 반환. len == 구절 수, 합 ≈ total_dur.
    ASR 정렬 신뢰도 미달이면 None(호출부가 글자수 폴백)."""
    segs = _caption_segments(narration)
    if not segs or not words or total_dur <= 0:
        return None

    ref = narration.split()
    hyp = [w["word"] for w in words]
    # 각 hyp 단어의 시작 시각(초). 자막은 '시작 시각'만 쓴다(구절 전환 시점).
    hyp_start = [float(w["start"]) for w in words]

    # 대본 단어 ↔ ASR 단어 정렬. 각 ref 인덱스에 대응 hyp 시작시각을 채운다.
    ref_time = [None] * len(ref)
    matched = 0
    sm = difflib.SequenceMatcher(a=[_norm(t) for t in ref], b=[_norm(t) for t in hyp])
    for tag, i1, i2, j1, j2 in sm.get_opcodes():
        if tag in ("equal", "replace"):
            span = min(i2 - i1, j2 - j1)
            for k in range(span):
                ref_time[i1 + k] = hyp_start[j1 + k]
                if tag == "equal":
                    matched += 1
    if matched < _MIN_MATCH_RATIO * len(ref):
        return None

    # 못 채운 ref 시각을 앞뒤 아는 값으로 선형 보간(양끝은 0/total_dur로 클램프).
    _interp(ref_time, total_dur)

    # 각 구절의 첫 ref 단어 시작 시각. 구절→ref 단어 범위는 순서대로 소비.
    seg_starts, w_at = [], 0
    for seg in segs:
        n = len(seg.split())
        seg_starts.append(ref_time[w_at] if w_at < len(ref_time) else total_dur)
        w_at += n

    # durs[i] = 다음 구절 시작 - 이 구절 시작(마지막은 total_dur까지). 단조·비음수 보장.
    durs = []
    for i, s in enumerate(seg_starts):
        nxt = seg_starts[i + 1] if i + 1 < len(seg_starts) else total_dur
        durs.append(max(0.0, nxt - s))
    # 합을 total_dur로 정규화(보간 오차 흡수).
    tot = sum(durs)
    if tot <= 0:
        return None
    return [d * total_dur / tot for d in durs]


def _interp(times, total_dur):
    """None 항목을 앞뒤 아는 값으로 선형 보간. 양끝 None은 0.0/total_dur로."""
    n = len(times)
    if times[0] is None:
        times[0] = 0.0
    if times[-1] is None:
        times[-1] = total_dur
    i = 0
    while i < n:
        if times[i] is not None:
            i += 1
            continue
        j = i
        while j < n and times[j] is None:
            j += 1
        lo = times[i - 1]
        hi = times[j] if j < n else total_dur
        gap = j - (i - 1)
        for k in range(i, j):
            times[k] = lo + (hi - lo) * (k - (i - 1)) / gap
        i = j
```

> `_strip_punct`는 `video_assemble.py`에 이미 있다(`_caption_segments`가 씀). 없으면 import 에러로 드러나니 그때 `video_assemble`에서 정확한 이름을 확인해 맞춘다.

- [ ] **Step 4: 통과 확인**

Run: `python -m pytest shopping_shorts/tests/test_caption_sync.py -q`
Expected: PASS (5 passed)

- [ ] **Step 5: 뮤테이션 (2종)**

1. `durs.append(max(0.0, nxt - s))`를 `durs.append(max(0.0, s - nxt))`(부호 반전)로 → `test_caption_follows_voice_not_charcount` 사망 기대.
2. `if matched < _MIN_MATCH_RATIO * len(ref): return None`을 `if False:`로 → `test_low_confidence_returns_none` 사망 기대.

각 확인 후 되돌린다.

- [ ] **Step 6: 커밋**

```bash
git add shopping_shorts/caption_sync.py shopping_shorts/tests/test_caption_sync.py
git commit -m "feat(자막싱크): caption_sync — 대본↔ASR 정렬로 구절 실시간 산출"
```

---

### Task 3: `_caption_durations`/`_caption_drawtexts`에 `real_durs` 합류

**Files:**
- Modify: `shopping_shorts/video_assemble.py` (`_caption_durations`, `_caption_drawtexts`)
- Test: `shopping_shorts/tests/test_caption_durations_real.py` (create)

**Interfaces:**
- Consumes: (없음 — 순수 함수 변경)
- Produces: `_caption_durations(segs, dur, real_durs=None)`; `_caption_drawtexts(narration, dur, work, idx, t0=0.0, style=None, real_durs=None)`.

**규약:** `real_durs`가 오고 `len(real_durs) == len(segs)`면 글자수 대신 그 값을 base로 쓰되 **기존 하한(`_CAP_MIN_DUR`)·총합 보정 로직은 그대로 재사용**. `real_durs`가 None이거나 길이 불일치면 **현행과 바이트 동일**(방어적 폴백).

- [ ] **Step 1: 실패하는 테스트를 쓴다**

`shopping_shorts/tests/test_caption_durations_real.py`:

```python
"""_caption_durations의 real_durs 합류 — 오면 쓰고, None/길이불일치면 현행 그대로."""
from shopping_shorts.video_assemble import _caption_durations, _caption_segments


def test_none_is_byte_identical_to_current():
    segs = ["여러분", "오이 절대", "냉장고에 그냥 두지 마세요"]
    base = _caption_durations(segs, 5.0)              # 현행
    same = _caption_durations(segs, 5.0, real_durs=None)
    assert same == base


def test_real_durs_used_when_given():
    segs = ["가", "나", "다"]
    out = _caption_durations(segs, 3.0, real_durs=[2.0, 0.5, 0.5])
    # 글자수 비례(균등 1.0/1.0/1.0)가 아니라 real 쪽으로 기운다.
    assert out[0] > out[1] and out[0] > out[2]
    assert abs(sum(out) - 3.0) < 1e-6


def test_length_mismatch_falls_back():
    segs = ["가", "나", "다"]
    base = _caption_durations(segs, 3.0)
    out = _caption_durations(segs, 3.0, real_durs=[2.0, 1.0])   # 길이 2 ≠ 3
    assert out == base


def test_min_dur_floor_still_applies_with_real():
    segs = ["가", "나"]
    out = _caption_durations(segs, 4.0, real_durs=[3.99, 0.01])
    assert min(out) >= 0.25 - 1e-9        # _CAP_MIN_DUR 하한 유지
    assert abs(sum(out) - 4.0) < 1e-6
```

- [ ] **Step 2: 실패 확인**

Run: `python -m pytest shopping_shorts/tests/test_caption_durations_real.py -q`
Expected: FAIL (`TypeError: _caption_durations() got an unexpected keyword argument 'real_durs'`)

- [ ] **Step 3: 구현**

`video_assemble.py`의 `_caption_durations` 시그니처와 `raw` 계산부를 교체. 현재:

```python
def _caption_durations(segs, dur):
    ...
    weights = [max(1, len(s.replace("\n", ""))) for s in segs]
    total_w = sum(weights)
    raw = [dur * w / total_w for w in weights]
```

를 다음으로:

```python
def _caption_durations(segs, dur, real_durs=None):
    ...
    if real_durs is not None and len(real_durs) == len(segs) and sum(real_durs) > 0:
        # 실제 말한 타이밍(ASR). 총합을 dur로 맞춘 뒤 아래 하한·회수 로직 공용.
        s = sum(real_durs)
        raw = [dur * d / s for d in real_durs]
    else:
        weights = [max(1, len(s.replace("\n", ""))) for s in segs]
        total_w = sum(weights)
        raw = [dur * w / total_w for w in weights]
```

(그 아래 `floored = [max(_CAP_MIN_DUR, r) ...]` 이하 하한·회수 로직은 **그대로**.)

그리고 `_caption_drawtexts` 시그니처와 durs 계산 한 줄을 교체. 현재:

```python
def _caption_drawtexts(narration, dur, work, idx, t0=0.0, style=None):
    segs = _caption_segments(narration)
    ...
    durs = _caption_durations(segs, dur)
```

를:

```python
def _caption_drawtexts(narration, dur, work, idx, t0=0.0, style=None, real_durs=None):
    segs = _caption_segments(narration)
    ...
    durs = _caption_durations(segs, dur, real_durs=real_durs)
```

- [ ] **Step 4: 통과 확인**

Run: `python -m pytest shopping_shorts/tests/test_caption_durations_real.py -q`
Expected: PASS (4 passed)

회귀: `python -m pytest shopping_shorts/tests/ -q -k "caption or naturalize" 2>&1 | tail -3` → 실패 수 기준선 이하.

- [ ] **Step 5: 뮤테이션**

`len(real_durs) == len(segs)` 가드를 지우고 무조건 real 사용 → `test_length_mismatch_falls_back`가 죽는지(IndexError/불일치). 되돌린다.

- [ ] **Step 6: 커밋**

```bash
git add shopping_shorts/video_assemble.py shopping_shorts/tests/test_caption_durations_real.py
git commit -m "feat(자막싱크): _caption_durations real_durs 합류 + drawtexts 통과"
```

---

### Task 4: 배선 — `_synthesize_beats`에서 계산·저장, 렌더에서 읽기

**Files:**
- Modify: `shopping_shorts/mix_pipeline.py` (`_synthesize_beats`)
- Modify: `shopping_shorts/video_assemble.py` (`_burn_captions`의 `_caption_drawtexts` 호출부)
- Test: `shopping_shorts/tests/test_caption_sync_wiring.py` (create)

**Interfaces:**
- Consumes: `asr_check.transcribe_words`, `caption_sync.phrase_durs_from_words`(Task 1·2), `_probe_duration`(기존).
- Produces: `beat["cap_durs"] : list[float] | None` (edit_plan_json에 실림). 렌더가 `beat.get("cap_durs")`를 `_caption_drawtexts(..., real_durs=...)`로 전달.

- [ ] **Step 1: 실패하는 테스트를 쓴다**

`shopping_shorts/tests/test_caption_sync_wiring.py`:

```python
"""배선 — 합성 지점에서 cap_durs를 계산·저장하고, 키 없으면 미설정(회귀0)."""
import shopping_shorts.mix_pipeline as mp


def test_synth_sets_cap_durs_when_asr_available(monkeypatch, tmp_path):
    beats = [{"beat_idx": 0, "narration": "귤은 손으로 까요"}]
    # synthesize_line·probe·transcribe_words를 스텁으로: 실렌더/네트워크 없이 배선만 검증.
    monkeypatch.setattr(mp, "synthesize_line", lambda *a, **k: None)
    monkeypatch.setattr(mp, "_probe_duration", lambda p: 1.5, raising=False)
    monkeypatch.setattr(mp.asr_check, "transcribe_words",
                        lambda p: [{"word": "귤은", "start": 0.0, "end": 0.5},
                                   {"word": "손으로", "start": 0.5, "end": 1.0},
                                   {"word": "까요", "start": 1.0, "end": 1.5}])
    mp._synthesize_beats(beats, tmp_path, voice={})
    assert isinstance(beats[0]["cap_durs"], list) and len(beats[0]["cap_durs"]) >= 1


def test_synth_leaves_cap_durs_absent_without_asr(monkeypatch, tmp_path):
    beats = [{"beat_idx": 0, "narration": "귤은 손으로 까요"}]
    monkeypatch.setattr(mp, "synthesize_line", lambda *a, **k: None)
    monkeypatch.setattr(mp, "_probe_duration", lambda p: 1.5, raising=False)
    monkeypatch.setattr(mp.asr_check, "transcribe_words", lambda p: None)  # 키 없음 흉내
    mp._synthesize_beats(beats, tmp_path, voice={})
    assert beats[0].get("cap_durs") is None
```

- [ ] **Step 2: 실패 확인**

Run: `python -m pytest shopping_shorts/tests/test_caption_sync_wiring.py -q`
Expected: FAIL (cap_durs 미설정 / import 에러)

- [ ] **Step 3: 구현**

`mix_pipeline.py` 상단 import에 다음이 없으면 추가: `from . import asr_check, caption_sync`. 그리고 `_probe_duration`이 `mix_pipeline`에 없으면 `from .video_assemble import _probe_duration`.

`_synthesize_beats` 루프 안, `beat["tts_path"] = str(out)` **다음 줄에** 추가:

```python
        # 자막 타이밍용: 실제 말한 워드 시각으로 구절 표시시간 계산(실패/키없음 → 미설정=폴백).
        beat["cap_durs"] = None
        words = asr_check.transcribe_words(str(out))
        if words:
            dur = _probe_duration(str(out))
            beat["cap_durs"] = caption_sync.phrase_durs_from_words(
                beat["narration"], words, dur)   # None일 수 있음 → 폴백
```

`video_assemble.py`의 `_burn_captions`에서 `_caption_drawtexts(b["narration"], b["dur"], ...)` 호출에 `real_durs=b.get("cap_durs")`를 추가한다. 정확한 호출 위치는 `grep -n "_caption_drawtexts(" video_assemble.py`로 찾아 인자 하나만 얹는다.

- [ ] **Step 4: 통과 확인**

Run: `python -m pytest shopping_shorts/tests/test_caption_sync_wiring.py -q`
Expected: PASS (2 passed)

전체 회귀: `python -m pytest shopping_shorts/tests/ -q 2>&1 | tail -3` → 실패 수가 기준선보다 늘지 않음.

- [ ] **Step 5: 뮤테이션 (seam)**

`_burn_captions`에서 방금 얹은 `real_durs=b.get("cap_durs")`를 `real_durs=None`으로 되돌리면 → cap_durs가 렌더에 반영 안 됨(저장≠읽기). 이걸 잡는 테스트가 있어야 seam이 봉인된다. **없으면** `test_caption_sync_wiring.py`에 다음을 추가:

```python
def test_render_reads_stored_cap_durs(monkeypatch):
    """저장된 cap_durs가 자막 draw에 real_durs로 전달되는가(저장위치=읽기위치)."""
    import shopping_shorts.video_assemble as va
    captured = {}
    monkeypatch.setattr(va, "_caption_drawtexts",
                        lambda *a, **k: captured.update(real=k.get("real_durs")) or [])
    b = {"narration": "귤은 까요", "dur": 1.0, "t0": 0.0, "cap_durs": [0.6, 0.4]}
    va._burn_captions_one(b)   # ← 아래 주: 실제 헬퍼명에 맞춰 호출
    assert captured["real"] == [0.6, 0.4]
```

> ⚠️ `_burn_captions`가 비트를 순회하는 구조면 단일-비트 진입점(`_burn_captions_one` 같은)이 없을 수 있다. 그 경우 이 테스트는 `_burn_captions` 전체를 최소 입력으로 호출하도록 맞추거나, 호출부에서 `real_durs`를 넘기는지 **소스 문자열 assert**(`inspect.getsource`)로 대체한다 — 구현자가 실제 구조를 보고 택한다. **핵심은 "cap_durs가 draw까지 real_durs로 흐르는지"를 죽는 테스트로 잠그는 것.**

- [ ] **Step 6: 커밋**

```bash
git add shopping_shorts/mix_pipeline.py shopping_shorts/video_assemble.py shopping_shorts/tests/test_caption_sync_wiring.py
git commit -m "feat(자막싱크): 합성지점 cap_durs 계산·저장 + 렌더 읽기 배선(seam 봉인)"
```

---

### Task 5: 실 mp3 grounding + 신뢰도 임계 튜닝

**Files:**
- Create: `scratchpad/capsync_probe.py` (커밋 안 함)
- Modify: (필요 시) `shopping_shorts/caption_sync.py`의 `_MIN_MATCH_RATIO`

**목적:** 이 계획의 모든 유닛테스트는 **가짜 워드 타임**으로 돈다 — Whisper 한국어 워드 분절이 우리 `narration.split()` 어절과 실제로 얼마나 맞는지(=정렬률), 침묵제거가 시각을 얼마나 흔드는지는 **실 mp3로만** 안다.

- [ ] **Step 1: 키 유무 실측**

`python -c "from shopping_shorts import config; print('GROQ:', bool(config.GROQ_API_KEY))"`
- **키 없으면(로컬 예상):** 실 grounding 불가. `capsync_probe.py`는 건너뛰고 **"실 mp3 미관측 — 로컬 GROQ 키 0개, 서버에서 확인 예정"**을 리포트에 정직히 적는다. Task 완료 조건은 "유닛 green + 회귀0 + 미관측 명시".

- [ ] **Step 2: (키 있을 때만) 실 비트 mp3로 관측**

`scratchpad/capsync_probe.py`: 실제 job의 `beat_*.mp3` 하나를 `transcribe_words`로 받아, 그 비트 narration으로 `phrase_durs_from_words`를 돌려 (a) None 폴백 빈도 (b) 정렬률 (c) durs가 자막을 실제로 앞당기는지 육안 확인. 침묵 많은 비트·감정태그 많은 비트를 골라 본다.

- [ ] **Step 3: 임계 조정(관측 근거 있을 때만)**

관측상 정상 비트가 자주 None으로 떨어지면 `_MIN_MATCH_RATIO`를 내리고, 엉뚱 정렬이 통과하면 올린다. **근거 없이 숫자 바꾸지 마라** — 못 정하면 현행 0.5 유지하고 "미관측"으로 남긴다.

- [ ] **Step 4: 리포트 + 커밋(테스트만)**

관측 결과(또는 "미관측" 사유)를 `.superpowers/sdd/capsync-progress.md`에 적는다. 코드 변경이 있으면 커밋:

```bash
git add shopping_shorts/caption_sync.py
git commit -m "tune(자막싱크): _MIN_MATCH_RATIO 실측 조정" || echo "변경 없음(미관측)"
```

---

## Self-Review (작성자 체크)

- **스펙 커버리지:** §A(transcribe_words)=Task1 / §B(정렬 순수함수)=Task2 / §C(_caption_durations real_durs)=Task3 / §D(계산·저장·읽기 단일경로)=Task4 / 회귀0·폴백=Task1·2·3·4 각 테스트 / 실데이터 미확정 3건=Task5. 누락 없음.
- **플레이스홀더:** 모든 코드 스텝에 실제 코드. Task4 Step5의 `_burn_captions_one`은 **구조 미확인 지점**이라 명시적으로 "구현자가 실제 헬퍼명 확인해 맞추거나 소스 assert로 대체"라고 대안까지 적음(막연한 TODO 아님).
- **타입 일관성:** `cap_durs`(list|None)·`transcribe_words`(list|None)·`phrase_durs_from_words`(list|None)·`real_durs` 인자명이 Task1→2→3→4에서 동일.
- **미확정(정직):** Whisper 한국어 워드 분절 정확도·`_MIN_MATCH_RATIO` 실측값 — Task5에서 실데이터로, 못 정하면 현행 유지. `_probe_duration`·`_burn_captions` 호출부 정확 위치 — 구현자가 grep으로 확인(계획에 명시).
