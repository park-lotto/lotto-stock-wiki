# 자막제거1콜 — 핸드오프

**소유 트랙**: 자막제거1콜 — 2026-08-27.

## 자막제거가 영상 1편당 2콜 나가던 것 (완료·라이브·실측 검증)

사장님: "이사람은 그냥 장면도 안바꾸고 클릭만 처음부터 끝까지 한번씩해서 한다는데"
        "이건 고객과의 신용이야"

### 실측 (DB 사본 + VMake만 가짜, 과금 0)
```
수정 전:  clean sig=306324a7 / render sig=bad66168  → VMake 2콜
수정 후:  clean sig=cbd17047 / render sig=cbd17047  → VMake 1콜
배포 후:  라이브 코드로 재확인 → 1콜
```
검증 스크립트: `/tmp/verify_sig.py` (서버). DB는 `sqlite backup`으로 떠야 한다 —
파일 복사만 하면 WAL 때문에 readonly가 된다. 실행은 **ubuntu 권한**으로
(`sudo`로 돌리면 readonly). env는 `/tmp/envfile`에 풀어 `set -a`로 읽혔다.

### 뿌리 두 개 — ①만 고치면 안 낫는다
① **TTS 확정이 청소보다 늦다.** `_synthesize_beats`가 TTS 실측 발화초로
   `beat["target_seconds"]`를 덮어쓰는데(mix_pipeline:552), 편성 서명이 그 값을 본다.
   자막제거(4단계)가 렌더 직전 TTS 보장보다 먼저라 서명이 반드시 한 번 바뀌었다.
② ★**`_apply_hook_inpoint`가 멱등이 아니다.** 첫 장면 start를 영상 피크로 옮기는데
   **현재 start를 기준**으로 피크를 다시 찾아 부를 때마다 더 밀렸다:
   `1회 0.0 → 0.1`, `2회 0.1 → 0.8`.
   ①만 고쳤을 때 여전히 2콜이었고 그때 드러났다.
   ★이건 자막제거와 별개로도 버그다 — **렌더를 두 번 하면 훅이 계속 뒤로 밀린다.**

### 고친 것
- `run_clean_sources`: 청소 전에 **TTS + 훅 시작점**을 확정하고 편성을 저장.
  두 함수 모두 렌더가 쓰는 것을 그대로 부른다(0순위-B — 갈리면 서명이 또 어긋난다).
- `_apply_hook_inpoint`: 처음 start를 `hook_orig_start`에 남기고 **항상 그것으로** 계산.
- 계약 테스트(`test_clean_once_per_video.py`): 순서(TTS→청소)·편성 저장·호출 형태 일치·
  **훅 멱등성**·서명 불변을 고정. 뒤집히면 편당 2콜로 되돌아간다.

### 덤
2단계 미리보기가 실제 결과와 **컷 길이까지** 같아진다(종전엔 추정 초로 조립).

## ⏭ 다음
- 고객 실사용에서 `"완성본 재사용(편성 그대로, 과금 0)"` 로그가 뜨는지 확인.
- VMake 크레딧 소모가 실제로 절반이 되는지 며칠 관찰(cid57 사례: 4일 58콜 ≈ 2,900크레딧).
- ★교훈: 과금·고객 신용이 걸린 수정은 **실제로 돌려 숫자를 세기 전엔 완료라고 말하지 않는다.**
  이번에 코드 모양만 보고 "됐다"고 두 번 말했고, 둘 다 실측에서 틀렸다.

---

## 2026-09-01 — 아직도 2콜이었다. 뿌리는 **단계 순서**였다 (라이브)

김용덕님(cid57) "렌더 실패" 제보에서 시작. 실패 job `6e5c4ba980ba`는 자막제거
크레딧 소진(60002)이고, 등록 키 3개가 전부 소진이었다.

### 08-27 수정은 살아 있다 (회귀 아님)
같은 job을 서버에서 재현(DB 사본·자막제거 업체 API만 가짜·과금 0):
```
자막제거 [sig] 88fecb25dd0ba6db → 호출
렌더     [sig] 88fecb25dd0ba6db → "완성본 재사용(편성 그대로, 과금 0)"
서로 다른 서명 수: 1 → 1콜(정상)
```
**보이스를 안 바꾸면 지금도 1콜이다.** 처음에 "회귀"라고 단정했다가 재현에서 틀렸다.

### 진짜 뿌리 — 단계 순서가 뒤집혀 있다
`produce.html:3301` `STEP_LABELS`: **4=고품질 자막제거 / 5=TTS음성**.
자막제거는 조립된 완성본 1편을 청소하는데 컷 길이가 음성 발화초에 맞춰져 있다.
→ 순서대로 누르면 청소 뒤에 보이스를 고르게 되고, `target_seconds`가 갱신돼
   편성 서명이 달라져 **렌더가 조용히 다시 청소한다**.

라이브 로그 실측 (job `119092fa678d`):
```
12:46:29 POST /api/produce/mix/clean  → 12:50 final_clean_88fecb25dd0ba6db (1콜)
12:50:46 POST /api/mix/voice          ← ★보이스 변경
12:52:55 POST /api/mix/render         → 12:55 final_clean_eaa0c1a08f1dcfb5 (또 1콜)
```
서버 `mix_jobs/` 전수: 최근 60개 중 13개가 `final_clean_*` 2개, `a36ed3a9522f`는 3개.
= **크레딧·시간이 2~3배.** 08-27 핸드오프의 "클릭만 한번씩 하는 사람"이 그대로 당하고 있었다.

### 고친 것 — 화면 2곳 (단계 순서는 안 건드림)
- `startCleanPreview`: 음성 미선택이면 "음성 먼저 정하면 한 번으로 끝난다" 고지 후 선택.
- `applyVoice`: `clean_status='ready'`면 "다시 청소가 필요하다(크레딧 추가)" 고지 후 선택.
둘 다 **막지 않는다** — 재청소 자체는 화면상 맞다. 말없이 태우지 않게만 한다.

### ★단계 순서를 바꾸지 않은 이유
`step`은 DB `produce_works.step`에 저장된 **고객 진행상태**다. 순서를 밀면 진행 중
작업이 어긋난다 — `produce.html:9530`에 "열린 작업 8개 전부 step=0으로 파괴" 전례.
`data-step`도 이미 물리 순서와 논리 번호가 어긋나 있다(`:3036`).

### 함정 두 개 (다음 세션이 밟지 마라)
- ★**Bash heredoc이 `
`을 실제 개행으로 먹었다.** JS 문자열이 통째로 깨졌고
  그대로 나갔으면 제작소 화면 전체가 죽는다. **게이트는 HTML/JS 문법을 안 본다** —
  `node --check`로 인라인 `<script>`를 직접 검사해서 잡았다. 반드시 할 것.
- 사용자 노출 UI 파일은 **주석까지 벤더명 금지**(`test_no_vmake_anywhere_in_produce_html`).

## ⏭ 다음
- ★**브라우저 클릭 실측 미완**: `confirm` 모달이라 자동화가 막힌다. 라이브에서
  ①음성 없이 자막제거 누르기 ②자막제거 후 보이스 바꾸기 — 두 안내가 뜨는지 눈으로 확인.
- 며칠 뒤 `mix_jobs/`에서 `final_clean_*` 2개짜리 job이 줄었는지 다시 센다(숫자로 확인).
- 김용덕님(cid57) 자막제거 키 3개 전부 소진 — 충전 안내 필요.
- 근본책은 단계 순서 교정(4↔5). `step` 마이그레이션이 필요해 별도 트랙으로.
