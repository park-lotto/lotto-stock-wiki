# 히트작크롤 — handoff

## 2026-08-06 저녁 (회사PC) — ★크롤 재개 성공: 주거용 프록시+새 세션으로 경고 0

### 오늘 한 것 (전부 실측)
1. **Webshare 주거용 10GB($27.50) 결제** — 사장님. 474채널≈8GB 기준.
2. **크론 재개**(매일 10:30) + 수동 회차 6번 — **오늘 총 249채널+, 릴스 3만+ 수집, scraping_warning 0건**.
   아카이브 78,265 → 10만+ 릴스. 어제 "계정이 1~2시간 만에 죽는다"와 정반대 —
   **계정별 고정 한국 주거용 IP + 깨끗한 세션** 조합이 정답으로 확정.
3. **계정 체제**: 크롤 3개 로테이션(36259840846·37488247878·issi98921=41170560843, 각각 kr-1/2/3)
   + **랭킹 수집 전용 1개**(36997798201 → `/home/ubuntu/instagram_session.json`). 역할 분리라 한쪽 죽어도 다른 쪽 무사.
4. **로테이션 코드 라이브**(1d2b482fe): 채널마다 계정 순환(부하 1/N), 경고 계정만 제외, IP는 전역 인덱스 고정.
5. **랭킹 공백 사고 복구**(별건, 트랙 `랭킹미리보기복구`): 09시 수집이 만료세션으로 0건→캐시 [] 덮음.
   세션 교체로 15시 215건 복구 + "0건이면 캐시 미갱신" 가드 라이브(d0d743e08).

### 함정 (재발 주의)
- **세션 목록은 회차 시작 시 스냅샷** — 죽은 세션 파일을 도중에 치워도 그 회차엔 반영 안 됨. 재시작 필요.
- 죽은 세션은 `/home/ubuntu/ig_sessions_dead/`로 격리(38718393284). 랭킹용 예비는 `ig_sessions_reserve/`.
- `pkill -f channel_archive`를 ssh로 치면 **자기 원격 셸도 죽는다** — `channel_arch[i]ve` 패턴으로.

### ⏭ 다음
- 남은 ~250채널: 크론(10:30)+수동 회차로 1~2일 내 완주. 계정 추가되면 `python scripts/ig_session_capture.py` → scp → 즉시 로테이션 합류.
- **병렬 크롤**(계정 3개 동시, 3배 속도) — 코드 필요, 사장님 원하면 착수.
- 21:00 랭킹 수집이 새 계정으로 정상 규모(~500건) 도는지 확인.
- 태깅: 20키 소진 상태, 내일 09:12 크론 자동 재개. 키 추가는 여전히 유효(키1=하루500건).

---

**2026-08-06 (회사PC)** · 트랙 `히트작크롤` / 폴더 `.tracks/히트작크롤`

## 방침 (사장님 확정)

- **1바퀴(채널당 상위 40개) 먼저 완주** → 그 다음 바퀴는 여유 있을 때.
  474채널 전부에 태그가 생겨야 검색·매칭이 전 채널에서 돈다(깊이우선 금지).
- **키는 계속 만든다** — 구글 프로젝트를 따로 파서 추가. 만든 만큼 그대로 빨라진다.
- **제작소 예약 3개** — 병렬 3개를 돌려야 하므로 태거가 손 못 대게 뺀다.

## 오늘 라이브 반영 (전부 배포됨)

| 커밋 | 내용 |
|---|---|
| `ab077d521` | 크롤 대역폭 −42% (이미지·미디어·폰트 차단) |
| `cf01169b8` | 레퍼런스 수집분 → 히트작 아카이브 자동 적재 |
| `cdb779c9e` | 태거 병렬화(8워커) |
| `8867f2241` | ★비전 API 키 로테이션 누락 수정 (9곳) |
| `73d6d60dd` | 키 스캔 상한 30→200 + 분당한도 리미터 |

서버 설정: `SHORTS_PRODUCE_RESERVED=3` / 죽은 키 11번 주석처리(`#DEAD_403_2026-08-06#`).
백업: `/etc/shopping-shorts.env.bak.*`

## ★실측으로 확정된 것 (추측 금지 — 여기서 두 번 틀렸다)

### 한도는 두 겹이다
| 한도 | 값 | 뚫는 법 |
|---|---|---|
| 분당(RPM) | 키당 15건 (16번째에 429) | 리미터 — **완료** |
| **하루(RPD)** | **키당 약 500건** | **키 개수뿐** |

근거: 코드의 `limit: 500` · 08-04 실적 9,944건 ≈ 21키×500 ·
**태거를 완전히 멈추고 2분 쉬어도 전 키 429**(0.3초 즉시 거절 = 처리조차 안 됨).

⚠️ **오진 이력**: "태거 정지 45초 후 성공"을 보고 분당 한도로 결론냈다가 틀렸다.
그때 성공한 건 아직 하루치가 남아 있던 키였다. 리미터는 분당 벽만 뚫는다.

### 대역폭 (프록시 = 유일한 과금처)
- 채널당 **31.88MB / 2,400건**(외삽 아닌 전량 실측), 건당 13.6KB + 채널당 고정비 약 10MB
- 474채널 ≈ **8GB** → **10GB 플랜 $27.50**(25GB $65 불필요). 선불, 다 쓰면 1GB로 내려 **1회성**
- ⚠️ **stylesheet는 절대 차단하지 마라** — CSS를 막으면 스크롤이 무한로딩을 못 깨워
  같은 30스크롤에서 372건 → 12건으로 죽는다(실측). 대역폭보다 손실이 크다.

### 채널 수
```
엑셀 440 + 발굴등록 295 = 합집합 697
  − removed 26 − done 186 − gone 35  →  ★남은 크롤 대상 474
```
⚠️ `load_channels()`는 **엑셀만** 읽는다(440). 실제 대상은 `pick_targets()`로 봐라.

### error 38채널 판별 완료
- **35개 = page_gone(진짜 삭제)** → `gone` 마킹 완료, 재크롤 대상에서 영구 제외
- 3개 = 프록시 터널 오류로 판정 불가(`ggung_unni` · `living.tori` · `panda._.home_`) — error 유지
- 오탐 아님 확인: 같은 시각 onshome_official 36건 정상 vs bangterior 0건

## 현재 상태 (2026-08-06 03:00)

```
아카이브    78,265릴스 / 518채널   (done 186 · error 3 · gone 35)
태깅 완료   18,833건 (24%)
남은 크롤   474채널
세션 슬롯   41170560843 ✅살아있음(E2E 검증) / 38718393284 ❌만료
프록시      10개 출구 kr-1~kr-10 전부 정상(KT·SKB·LG U+)
```

**오늘 태깅 불가** — 20개 키 하루치 소진. KST 09:00 리셋, **크론이 09:12 자동 재개**
(`run_tagger.sh`, `--per-channel 40 --rounds 0`) — 방침과 일치하므로 손댈 것 없다.

## ⏭ 내일 할 일

1. **★키 생성 후 추가** (많을수록 좋다). `/etc/shopping-shorts.env`에
   `SHORTS_GEMINI_KEY_25=`, `_26=` … 로 붙이면 된다(상한 200까지 스캔).
   키 값만 주시면 Claude가 넣는다. 추가 후 태거 재시작하면 즉시 반영.
   - 목표 환산: **키 1개 = 하루 500건**
   - 1바퀴 = 474 × 40 ≈ **19,000건** → 20키 2일 / 40키 1일 / 80키 반나절
2. **프록시 10GB 결제**($27.50) ← 사장님. 현재 0.79/1.0GB로 잔여 0.21GB뿐
3. **Firefox 프로필 10개 분리 + 로그인** ← 사장님
   ★한 프로필에서 계정을 갈아타면 **로그아웃 순간 직전 세션이 무효화**된다
   (38718393284이 그렇게 죽었다). `about:profiles`로 프로필을 나눠야 동시에 산다.
4. 세션 적립(`scripts/ig_session_capture.py`) → 서버 `/home/ubuntu/ig_sessions/` 복사
5. **계정 한도 실측** — 계정당 몇 채널에서 scraping_warning이 뜨는지. 이게 나와야 페이스 확정
6. 크론 재활성화: `#DISABLED_scraping_warning#` 주석 제거

## 남은 이슈

- **썸네일 시한부** — 만료되면 그 릴스는 **영영 태깅 불가**(조회수·링크·영상은 무사).
  태그는 한 번 뽑으면 영구자산이라 **태깅이 크롤을 따라가야 한다**. 지금 76%가 미태깅.
- **product 판독 605건뿐** — 백필이 680건에서 멈춘 것도 키 로테이션 버그가 원인일 수 있다
  (오늘 고쳤으므로 재실행하면 달라질 것). 미검증.
- 병렬 워커는 **8이 최적**. 16으로 올리면 오히려 느려진다(실측 39 → 36건/분).

## 관련

- 이전 인계: `handoff/제품명검색.md` (계정 로테이션·Apify 비교·실측표)
- memory: `project_역대히트작_아카이브` · `reference_서버_SHORTS키_systemd전용`
