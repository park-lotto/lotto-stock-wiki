# 숏템엔진 계획 1 — brainbulb를 채널 엔진(channelkit) + 채널 spec으로 쪼개기

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** `.tracks/뇌전구벤치`의 뇌전구 전용 brainbulb를 **공통 엔진 `shopping_shorts/channelkit/` + 1호 채널 `shopping_shorts/channel_presets/brainbulb/`** 로 쪼개되, 뇌전구 테스트 243개가 전과 같이 통과한다.

**Architecture:** 엔진 모듈들은 지금처럼 `from . import spec`을 쓰되 그 `spec`이 **프록시**(현재 채널 spec으로 위임, PEP 562 모듈 `__getattr__`)가 된다. 단계 순서(`STEPS`)·켤 린터 규칙(`LINT_RULES`)·채널 전용 단계 핸들러(`STEP_HANDLERS`)는 채널 spec이 정한다. 뇌전구 전용 모듈(images·photos·photocheck·community)은 `channel_presets/brainbulb/`로 옮기고 그 단계들은 핸들러로 등록한다. 옛 경로 `shopping_shorts.brainbulb`는 호환 shim으로 남겨 `brainbulb_api.py`·화면이 그대로 돈다.

**Tech Stack:** Python 3.14(`py`), pytest, git worktree(`tools/track.py`). 설계: `docs/superpowers/specs/2026-09-25-숏템엔진-design.md`.

**기준선(실측 2026-09-25):** `track/뇌전구벤치` = 커밋 `3b5466f50`, main보다 62 앞·1,244 뒤. 아래 9개 테스트 파일 = **243 passed, 58.5s**.

```
shopping_shorts/tests/test_brainbulb_core.py test_brainbulb_golden.py test_brainbulb_pipeline.py test_brainbulb_frames.py
test_brainbulb_images_fallback.py test_brainbulb_photo_filters.py test_brainbulb_photocheck.py test_brainbulb_photos.py test_brainbulb_review3.py
```
이하 "**BB 테스트**"라 부른다. 실행은 항상 트랙 폴더에서:
`PYTHONUTF8=1 py -m pytest shopping_shorts/tests/test_brainbulb_*.py -q -p no:cacheprovider`

**라이브 주의(0순위-A1c):** brainbulb·`brainbulb_api.py`·`/brainbulb` 페이지는 **main에 없다.** 이 트랙을 `finish`하면 전부 라이브(app.py 라우트)로 나간다 → **finish는 사장님 승인 후에만.** 이 계획은 트랙 브랜치 커밋까지다.

---

## 파일 구조 (끝난 뒤)

```
shopping_shorts/channelkit/                 ← git mv shopping_shorts/brainbulb
  __init__.py  registry.py(신규)  spec.py(프록시로 교체)
  pipeline.py  lint.py  layout.py  ass_gen.py  render.py  review.py  frames.py  measure.py
  timing.py  voice.py  sfx.py  prompt.py  providers.py  make.py
shopping_shorts/channel_presets/__init__.py        ← 신규(빈 파일)
shopping_shorts/channel_presets/brainbulb/
  __init__.py  spec.py(git mv)  fonts/(git mv)  steps.py(신규)
  images.py  photos.py  photocheck.py  community.py   (git mv)
shopping_shorts/brainbulb/__init__.py       ← 호환 shim (옛 import 경로 유지)
shopping_shorts/tests/test_channelkit_registry.py (신규)
tools/channelkit/parity_check.py (신규)
```

---

### Task 0: 트랙 `숏템엔진` 만들기 + 뇌전구벤치 병합 + 기준선

`track.py start`는 항상 origin/main에서 분기한다(`tools/track.py:276`). 그래서 만든 뒤 `track/뇌전구벤치`를 병합해 온다. 1,244커밋 차이라 충돌이 날 수 있다 — app.py의 brainbulb 라우트(21293~21377줄 부근)가 유력.

- [ ] **Step 1: 트랙 생성** (main 폴더에서)

```bash
cd "/c/Users/CH/Desktop/로또의 주식" && py tools/track.py start 숏템엔진
```
Expected: `.tracks/숏템엔진` 생성, 브랜치 `track/숏템엔진`, `.env` 복사 메시지.

- [ ] **Step 2: 뇌전구벤치 병합**

```bash
cd "/c/Users/CH/Desktop/로또의 주식/.tracks/숏템엔진" && git fetch origin && git merge --no-edit origin/track/뇌전구벤치
```
Expected: `Merge made by the 'ort' strategy.` 충돌이 나면 `git status --short | grep '^UU'`로 목록을 보고 **양쪽 다 살리는 방향**으로 푼다(brainbulb 라우트는 뇌전구벤치 쪽, 나머지는 main 쪽). app.py 충돌은 `_brainbulb_page`·`brainbulb_api` 블록이 한 번만 남는지 확인. 풀 수 없으면 멈추고 보고.

- [ ] **Step 3: import 게이트 + BB 테스트 기준선**

```bash
cd "/c/Users/CH/Desktop/로또의 주식/.tracks/숏템엔진" && PYTHONUTF8=1 py -c "import shopping_shorts.app; print('app ok')" && PYTHONUTF8=1 py -m pytest shopping_shorts/tests/test_brainbulb_*.py -q -p no:cacheprovider 2>&1 | tail -2
```
Expected: `app ok` / `243 passed`. 숫자가 다르면 병합이 뭔가 깨뜨린 것 — 여기서 고친다(다음 태스크로 넘기지 않는다).

- [ ] **Step 4: 기준선 커밋**

```bash
git add -A && git commit -m "숏템엔진: track/뇌전구벤치 병합 — brainbulb 기준선 243 passed"
```

---

### Task 1: 패키지 이름 바꾸기 `brainbulb` → `channelkit` + 호환 shim

**Files:**
- Move: `shopping_shorts/brainbulb/` → `shopping_shorts/channelkit/` (통째로, fonts 포함)
- Create: `shopping_shorts/brainbulb/__init__.py` (shim)
- Modify: `shopping_shorts/channelkit/__init__.py` docstring 첫 줄

- [ ] **Step 1: git mv**

```bash
cd "/c/Users/CH/Desktop/로또의 주식/.tracks/숏템엔진" && git mv shopping_shorts/brainbulb shopping_shorts/channelkit && mkdir shopping_shorts/brainbulb
```

- [ ] **Step 2: shim 작성** — `shopping_shorts/brainbulb/__init__.py`

```python
"""호환 shim — 옛 경로 `shopping_shorts.brainbulb.<모듈>`을 `shopping_shorts.channelkit`으로 넘긴다.

brainbulb는 2026-09-25에 채널 엔진(channelkit) + 채널 spec(channel_presets/brainbulb)으로 쪼개졌다.
brainbulb_api.py·static/brainbulb.html·옛 테스트가 이 경로를 쓴다. 새 코드는 channelkit을 직접 import.
"""
import importlib
import sys

_KIT = "shopping_shorts.channelkit"
_NAMES = ["pipeline", "spec", "lint", "layout", "ass_gen", "render", "review", "frames", "measure",
          "timing", "voice", "sfx", "prompt", "providers", "make",
          "images", "photos", "photocheck", "community"]


def __getattr__(name):
    if name in _NAMES:
        mod = importlib.import_module(f"{_KIT}.{name}")
        sys.modules[f"{__name__}.{name}"] = mod
        return mod
    raise AttributeError(name)


# `from shopping_shorts.brainbulb import X` 는 위 __getattr__로 되지만
# `import shopping_shorts.brainbulb.providers` (서브모듈 import 문법)은 sys.modules에 미리 있어야 한다.
for _n in _NAMES:
    try:
        sys.modules[f"{__name__}.{_n}"] = importlib.import_module(f"{_KIT}.{_n}")
    except ImportError:
        pass
```

- [ ] **Step 3: channelkit docstring 첫 줄 바꾸기** — `shopping_shorts/channelkit/__init__.py` 1행

```python
"""channelkit — 채널 규격 숏폼을 서버 없이 만드는 공통 엔진. (전 이름 brainbulb, 2026-09-25 쪼갬)
```
(나머지 docstring 그대로.)

- [ ] **Step 4: BB 테스트**

```bash
PYTHONUTF8=1 py -m pytest shopping_shorts/tests/test_brainbulb_*.py -q -p no:cacheprovider 2>&1 | tail -2
```
Expected: `243 passed`. 실패가 `ModuleNotFoundError: shopping_shorts.brainbulb.xxx`면 shim의 `_NAMES`에 그 이름 추가.

- [ ] **Step 5: 화면 경로 확인**

```bash
PYTHONUTF8=1 py -c "import shopping_shorts.app, shopping_shorts.brainbulb_api; from shopping_shorts.brainbulb import pipeline, spec; print(pipeline.__name__, spec.CANVAS_W)"
```
Expected: `shopping_shorts.channelkit.pipeline 1080`

- [ ] **Step 6: 커밋**

```bash
git add -A && git commit -m "channelkit: brainbulb 패키지를 channelkit으로 이름 바꿈 + 옛 경로 shim (테스트 243 동일)"
```

---

### Task 2: 채널 레지스트리 + spec 프록시 + `channel_presets/brainbulb/spec.py`

**Files:**
- Create: `shopping_shorts/channelkit/registry.py`
- Move: `shopping_shorts/channelkit/spec.py` → `shopping_shorts/channel_presets/brainbulb/spec.py`, `channelkit/fonts/` → `channel_presets/brainbulb/fonts/`
- Create: `shopping_shorts/channelkit/spec.py` (프록시), `shopping_shorts/channel_presets/__init__.py`, `shopping_shorts/channel_presets/brainbulb/__init__.py`
- Test: `shopping_shorts/tests/test_channelkit_registry.py`

- [ ] **Step 1: 실패 테스트** — `shopping_shorts/tests/test_channelkit_registry.py`

```python
# -*- coding: utf-8 -*-
"""channelkit.registry — 엔진 모듈이 보는 `spec`은 현재 채널 spec으로 위임되는 프록시다."""
import importlib
import sys
import types

import pytest

from shopping_shorts.channelkit import registry, spec


def _fake_channel(name, **consts):
    """테스트용 채널 spec 모듈을 sys.modules에 심는다."""
    mod = types.ModuleType(f"shopping_shorts.channel_presets.{name}.spec")
    for k, v in consts.items():
        setattr(mod, k, v)
    pkg = types.ModuleType(f"shopping_shorts.channel_presets.{name}")
    pkg.__path__ = []
    sys.modules[f"shopping_shorts.channel_presets.{name}"] = pkg
    sys.modules[f"shopping_shorts.channel_presets.{name}.spec"] = mod
    return mod


@pytest.fixture(autouse=True)
def _restore():
    yield
    registry.use(registry.DEFAULT)


def test_default_is_brainbulb():
    registry.use(registry.DEFAULT)
    assert registry.current().__name__ == "shopping_shorts.channel_presets.brainbulb.spec"
    assert spec.CANVAS_W == 1080 and spec.CANVAS_H == 1920


def test_use_switches_proxy():
    _fake_channel("fakech", CANVAS_W=720, CANVAS_H=1280)
    registry.use("fakech")
    assert spec.CANVAS_W == 720
    registry.use(registry.DEFAULT)
    assert spec.CANVAS_W == 1080


def test_missing_attr_raises_and_getattr_default_works():
    _fake_channel("fakech2", CANVAS_W=1)
    registry.use("fakech2")
    with pytest.raises(AttributeError):
        spec.NO_SUCH_CONSTANT
    assert getattr(spec, "FACE_MODEL", None) is None


def test_fonts_dir_moved_with_channel():
    registry.use(registry.DEFAULT)
    import os
    assert os.path.isfile(os.path.join(spec.FONTS_DIR, "SBAggroB.ttf"))
    assert "channels" in spec.FONTS_DIR.replace("\\", "/")
```

- [ ] **Step 2: 실패 확인**

```bash
PYTHONUTF8=1 py -m pytest shopping_shorts/tests/test_channelkit_registry.py -q -p no:cacheprovider 2>&1 | tail -3
```
Expected: `ImportError: cannot import name 'registry'`

- [ ] **Step 3: spec·fonts 이동 + 패키지 파일**

```bash
mkdir -p shopping_shorts/channel_presets/brainbulb && git mv shopping_shorts/channelkit/spec.py shopping_shorts/channel_presets/brainbulb/spec.py && git mv shopping_shorts/channelkit/fonts shopping_shorts/channel_presets/brainbulb/fonts
printf '"""channels — 채널마다 한 폴더. spec.py(규격 상수·STEPS·LINT_RULES·STEP_HANDLERS) + 그 채널만의 단계 모듈."""\n' > shopping_shorts/channel_presets/__init__.py
printf '"""brainbulb(뇌전구) — 1호 채널. 규격 근거: channel/volcano/뇌전구_역분석_8편_2026-09-12.md"""\n' > shopping_shorts/channel_presets/brainbulb/__init__.py
```
`channel_presets/brainbulb/spec.py`의 `FONTS_DIR = os.path.join(os.path.dirname(__file__), "fonts")`(9행)는 **그대로** — 파일과 fonts가 같이 옮겨졌으니 맞는 경로다.

- [ ] **Step 4: registry** — `shopping_shorts/channelkit/registry.py`

```python
# -*- coding: utf-8 -*-
"""현재 채널 — 엔진 모듈이 `from . import spec`으로 보는 값은 여기 등록된 채널 spec으로 위임된다.

왜 프록시인가: 엔진 20모듈이 79개 상수를 `spec.X`로 읽는다(2026-09-25 실측). 함수마다 spec을 넘기면
전부 고쳐야 하고, 채널을 바꾸는 자리는 pipeline.run_step 하나면 된다.
★숨은 전역 상태다 — 바꾸는 곳은 `use()` 하나뿐이어야 한다(0순위-B). 테스트는 fixture로 되돌린다.
"""
import importlib

DEFAULT = "brainbulb"
_current = None


def use(name):
    """채널 spec 모듈을 현재 채널로. → 모듈"""
    global _current
    _current = importlib.import_module(f"shopping_shorts.channel_presets.{name}.spec")
    return _current


def current():
    if _current is None:
        use(DEFAULT)
    return _current


def name():
    return current().__name__.split(".")[-2]
```

- [ ] **Step 5: spec 프록시** — `shopping_shorts/channelkit/spec.py`

```python
# -*- coding: utf-8 -*-
"""spec 프록시 — `spec.X`를 현재 채널(registry.current())의 X로 넘긴다. 상수는 channel_presets/<이름>/spec.py에.

PEP 562 모듈 __getattr__. `getattr(spec, "X", None)`도 그대로 된다(안쪽 AttributeError가 기본값으로 떨어짐).
"""
from . import registry


def __getattr__(name):
    if name.startswith("__"):
        raise AttributeError(name)
    return getattr(registry.current(), name)


def __dir__():
    return sorted(set(dir(registry.current())))
```

- [ ] **Step 6: 테스트**

```bash
PYTHONUTF8=1 py -m pytest shopping_shorts/tests/test_channelkit_registry.py shopping_shorts/tests/test_brainbulb_*.py -q -p no:cacheprovider 2>&1 | tail -2
```
Expected: `247 passed`(243 + 4). BB 테스트 중 `spec.FONTS_DIR`·`spec.MEME_FILE` 경로 실패가 나면 그 테스트가 `channelkit/fonts`를 하드코딩한 것 — 테스트를 `spec.FONTS_DIR`로 고친다(값을 바꾸지 않는다).

- [ ] **Step 7: 커밋**

```bash
git add -A && git commit -m "channelkit: 채널 레지스트리 + spec 프록시, 뇌전구 spec·fonts를 channel_presets/brainbulb로"
```

---

### Task 3: 단계 순서(`STEPS`)와 채널 전용 단계 핸들러를 spec이 정한다

**Files:**
- Modify: `shopping_shorts/channelkit/pipeline.py` (`STEPS` 상수 → `steps()` 함수, 핸들러 dispatch, `channel=` 인자)
- Modify: `shopping_shorts/channel_presets/brainbulb/spec.py` (`STEPS` 추가)
- Modify: `shopping_shorts/brainbulb_api.py:111,115,287,324` (`pipeline.STEPS` → `pipeline.steps()`)
- Test: `shopping_shorts/tests/test_channelkit_registry.py` (추가)

- [ ] **Step 1: 실패 테스트 추가** — `test_channelkit_registry.py` 끝에

```python
def test_steps_come_from_channel_spec(tmp_path):
    from shopping_shorts.channelkit import pipeline
    registry.use(registry.DEFAULT)
    assert pipeline.steps()[0] == "setup" and "review" in pipeline.steps()

    calls = []

    def _echo(job, d, wd, kw):
        calls.append(kw.get("source_text"))
        d["echo"] = {"n": len(calls)}
        return None                       # None = 정상 진행

    _fake_channel("stepch", STEPS=["setup", "echo"], STEP_HANDLERS={"echo": _echo},
                  FONTS_DIR=registry.use(registry.DEFAULT).FONTS_DIR, STYLE_FONT=registry.current().STYLE_FONT)
    registry.use("stepch")
    r = pipeline.run_step(str(tmp_path), "setup", source_text="소재")
    assert r["status"] == "ok" and r["next_step"] == "echo"
    r = pipeline.run_step(str(tmp_path), "echo", source_text="소재")
    assert r["status"] == "ok" and r["next_step"] is None
    assert pipeline.load(str(tmp_path))["data"]["echo"] == {"n": 1}


def test_run_step_channel_kwarg_switches(tmp_path):
    from shopping_shorts.channelkit import pipeline
    _fake_channel("kwch", STEPS=["setup"], STEP_HANDLERS={},
                  FONTS_DIR=registry.use(registry.DEFAULT).FONTS_DIR, STYLE_FONT=registry.current().STYLE_FONT)
    pipeline.run_step(str(tmp_path), "setup", source_text="x", channel="kwch")
    assert registry.name() == "kwch"
```

- [ ] **Step 2: 실패 확인**

```bash
PYTHONUTF8=1 py -m pytest shopping_shorts/tests/test_channelkit_registry.py -q -p no:cacheprovider 2>&1 | tail -3
```
Expected: `AttributeError: module ... has no attribute 'steps'`

- [ ] **Step 3: pipeline.py 고치기**

(a) 18행 `STEPS = [...]` 를 지우고 아래로 바꾼다:
```python
def steps():
    """현재 채널의 단계 순서 — spec.STEPS. (전엔 여기 상수였다. 채널마다 다르므로 spec으로 옮김 2026-09-25)"""
    return list(spec.STEPS)
```
(b) 파일 안의 `STEPS.index(` / `STEPS[` / `len(STEPS)` 를 전부 `steps().index(` / `steps()[` / `len(steps())` 로. 대상 함수: `_invalidate_after`, `_fail_stay`, `run_step` 끝의 `nxt`, `reset_to`, `next_step`.
```bash
grep -n "STEPS" shopping_shorts/channelkit/pipeline.py
```
Expected: `steps()` 정의 외에 `STEPS` 단독 참조 0줄(`spec.STEPS`는 남는다).

(c) `run_step` 시그니처에 `channel=None` 추가하고 첫 줄에:
```python
def run_step(wd, step, *, source_text=None, llm=None, tts=None, imagegen=None, sfx_dir=None, meme_dir=None, bg_image=None, fonts_dir=None, log=print, min_cuts=None, reviewer=None, channel=None):
    """한 단계만 실행. → 응답 dict {status: ok|need_input|failed, step, next_step, fail?, need?}
    channel: 채널 이름(channel_presets/<이름>). None이면 현재 등록된 채널(기본 brainbulb)."""
    if channel:
        registry.use(channel)
```
import 줄에 `registry` 추가: `from . import spec, registry, prompt, lint, ...`

(d) `else: raise ValueError(f"모르는 단계: {step}")` 바로 **위**에 채널 핸들러 분기:
```python
        elif step in (getattr(spec, "STEP_HANDLERS", None) or {}):
            kw = {"source_text": source_text, "llm": llm, "tts": tts, "imagegen": imagegen, "sfx_dir": sfx_dir,
                  "meme_dir": meme_dir, "bg_image": bg_image, "fonts_dir": fonts_dir, "log": log,
                  "min_cuts": min_cuts, "reviewer": reviewer}
            r = spec.STEP_HANDLERS[step](job, d, wd, kw)
            if r is not None:                 # 핸들러가 need_input/failed 응답을 돌려주면 그대로
                if r.get("status") == "failed":
                    _fail_stay(job, step)
                    save(wd, job)
                return r
            _invalidate_after(job, step)
```
핸들러 계약(docstring에 적을 것): `fn(job, d, wd, kw) -> None | resp dict`. `d`는 `job["data"]`. 정상이면 `d[step]`에 산출물을 넣고 None.

(e) `run_all(wd, **kw)`는 `channel`이 kw에 있어도 그대로 `run_step`에 넘어가므로 손댈 것 없음.

- [ ] **Step 4: 뇌전구 spec에 STEPS** — `channel_presets/brainbulb/spec.py` 맨 아래

```python
# ── 단계 순서 (전엔 channelkit/pipeline.py 상수. 채널마다 다르므로 여기로 2026-09-25) ─────
STEPS = ["setup", "script", "layout", "lint", "prompts", "images", "voice", "timing", "subtitle", "sfx", "frames", "render", "review"]
STEP_HANDLERS = {}     # Task 5에서 prompts·images·frames가 여기로 온다
```

- [ ] **Step 5: brainbulb_api.py** — `pipeline.STEPS` 4곳(111·115·287·324행)을 `pipeline.steps()`로.

```bash
sed -i 's/pipeline\.STEPS/pipeline.steps()/g' shopping_shorts/brainbulb_api.py && grep -n "pipeline.steps()" shopping_shorts/brainbulb_api.py | wc -l
```
Expected: `4`

- [ ] **Step 6: 테스트**

```bash
PYTHONUTF8=1 py -m pytest shopping_shorts/tests/test_channelkit_registry.py shopping_shorts/tests/test_brainbulb_*.py -q -p no:cacheprovider 2>&1 | tail -2
```
Expected: `249 passed`. BB 테스트가 `pipeline.STEPS`를 쓰면(기준선 grep에선 0건) `steps()`로 고친다.

- [ ] **Step 7: 커밋**

```bash
git add -A && git commit -m "channelkit: STEPS·STEP_HANDLERS를 채널 spec이 정한다, run_step(channel=)"
```

---

### Task 4: 켤 린터 규칙을 spec이 고른다 (`LINT_RULES`)

**Files:**
- Modify: `shopping_shorts/channelkit/lint.py` (`RULES` 리스트 → `ALL_RULES` dict + `rules()`)
- Modify: `shopping_shorts/channel_presets/brainbulb/spec.py` (`LINT_RULES`)
- Test: `test_channelkit_registry.py` 추가

- [ ] **Step 1: 실패 테스트 추가**

```python
def test_lint_rules_chosen_by_channel():
    from shopping_shorts.channelkit import lint
    registry.use(registry.DEFAULT)
    assert [r.id for r in lint.rules()] == list(registry.current().LINT_RULES)
    assert len(lint.rules()) == 24

    bb = registry.current()
    _fake_channel("lintch", LINT_RULES=["title_punct", "comma"],
                  **{k: getattr(bb, k) for k in dir(bb) if k.isupper() and k != "LINT_RULES"})
    registry.use("lintch")
    assert [r.id for r in lint.rules()] == ["title_punct", "comma"]
    issues, _ = lint.lint({"title": {"h1": "제목?", "h2": "둘째"}, "groups": []}, do_layout=False)
    assert {i.rule for i in issues} <= {"title_punct", "comma"}
    assert "title_punct" in {i.rule for i in issues}
    assert "- " in lint.prompt_block() and lint.prompt_block().count("\n") == 1
```

- [ ] **Step 2: 실패 확인**

```bash
PYTHONUTF8=1 py -m pytest shopping_shorts/tests/test_channelkit_registry.py::test_lint_rules_chosen_by_channel -q -p no:cacheprovider 2>&1 | tail -3
```
Expected: `AttributeError: module ... lint has no attribute 'rules'`

- [ ] **Step 3: lint.py** — `RULES = [ Rule(...), ... ]` 리스트 이름을 `_RULE_LIST`로 바꾸고 바로 아래에:

```python
ALL_RULES = {r.id: r for r in _RULE_LIST}     # 규칙 창고. 어떤 걸 켤지는 채널 spec.LINT_RULES


def rules():
    """현재 채널이 켠 규칙, spec.LINT_RULES 순서대로. 모르는 id는 즉시 KeyError(조용히 빠지지 않게)."""
    return [ALL_RULES[i] for i in spec.LINT_RULES]


RULES = _RULE_LIST      # 옛 이름 — 전체 목록이 필요한 곳(문서·테스트)만. 판정은 rules()를 쓴다
```
그리고 `prompt_block()`의 `for r in RULES` → `for r in rules()`, `lint()`의 `for r in RULES:` → `for r in rules():`.

```bash
grep -n "in RULES\|in rules()" shopping_shorts/channelkit/lint.py
```
Expected: `in rules()` 2줄, `in RULES` 0줄.

- [ ] **Step 4: 뇌전구 spec에 LINT_RULES** — `channel_presets/brainbulb/spec.py` 끝(실측 2026-09-25, lint.py RULES 순서 그대로 24개)

```python
# ── 켜는 린터 규칙 — channelkit/lint.py ALL_RULES 의 id. 순서 = 반려 사유 나열 순서 ─────
LINT_RULES = [
    "title_punct", "comma", "enum", "nonwhite_run", "formal", "ending_declared", "speaker_voice",
    "ending_mix", "h2_abstract", "card", "words", "layout", "punch", "last_standalone", "copy",
    "cut_count", "slot_count", "slot_seq", "card_img", "example_copy", "first_open", "last_closed",
    "line1_end", "meme_ratio",
]
```

- [ ] **Step 5: 골든 포함 테스트**

```bash
PYTHONUTF8=1 py -m pytest shopping_shorts/tests/test_channelkit_registry.py shopping_shorts/tests/test_brainbulb_*.py -q -p no:cacheprovider 2>&1 | tail -2
```
Expected: `250 passed`. 골든(`test_brainbulb_golden.py`)이 깨지면 LINT_RULES 순서·누락을 `_RULE_LIST`와 대조.

- [ ] **Step 6: 커밋**

```bash
git add -A && git commit -m "channelkit: 린터 규칙 창고(ALL_RULES) + 채널이 고르는 LINT_RULES"
```

---

### Task 5: 뇌전구 전용 모듈을 `channel_presets/brainbulb/`로 옮기고 단계를 핸들러로 등록

**Files:**
- Move: `channelkit/{images,photos,photocheck,community}.py` → `channel_presets/brainbulb/`
- Create: `shopping_shorts/channel_presets/brainbulb/steps.py` (prompts·images·frames 핸들러)
- Modify: `channelkit/pipeline.py` (prompts·images·frames 분기 삭제, import 정리), `channel_presets/brainbulb/spec.py`(STEP_HANDLERS), `shopping_shorts/brainbulb/__init__.py`(shim 경로), `brainbulb_api.py:164`
- Modify: 옮긴 모듈 안 `from . import spec` → `from shopping_shorts.channelkit import spec`

`frames.py`는 밈·사진을 프레임에 얹는 엔진 기능이라 channelkit에 남긴다(뜨거운사람들도 프레임을 만든다). 단계 **본문**만 핸들러로 간다.

- [ ] **Step 1: 이동 + import 고치기**

```bash
for m in images photos photocheck community; do git mv shopping_shorts/channelkit/$m.py shopping_shorts/channel_presets/brainbulb/$m.py; done
sed -i 's/^from \. import spec, prompt as _prompt$/from shopping_shorts.channelkit import spec, prompt as _prompt/' shopping_shorts/channel_presets/brainbulb/images.py
sed -i 's/^from \. import spec$/from shopping_shorts.channelkit import spec/' shopping_shorts/channel_presets/brainbulb/photos.py shopping_shorts/channel_presets/brainbulb/photocheck.py
grep -n "^from \. import\|^from shopping_shorts.channelkit import" shopping_shorts/channel_presets/brainbulb/*.py
```
Expected: images·photos·photocheck 각 1줄 `from shopping_shorts.channelkit import ...`, community는 spec을 안 쓴다(실측: requests만). 옮긴 모듈끼리 서로 부르는 줄(`from . import photos` 등)이 있으면 그대로 두면 된다(같은 패키지).

- [ ] **Step 2: steps.py** — `shopping_shorts/channel_presets/brainbulb/steps.py` (pipeline.py의 prompts·images·frames 분기 본문을 **그대로** 옮긴다. 아래는 옮긴 뒤 모양)

```python
# -*- coding: utf-8 -*-
"""뇌전구 전용 단계 — prompts(그림 지시) · images(생성+검수 재시도) · frames(프레임 합성).
channelkit.pipeline 은 spec.STEP_HANDLERS 로 여기를 부른다. 계약: fn(job, d, wd, kw) -> None | 응답 dict.
본문은 2026-09-25 channelkit/pipeline.py 에서 글자 그대로 옮겼다(동작 변경 없음).
"""
from shopping_shorts.channelkit import spec, frames as _frames, pipeline as _pl
from . import images as _images, photocheck as _photocheck


def prompts(job, d, wd, kw):
    llm, imagegen, log = kw["llm"], kw["imagegen"], kw["log"]
    if llm is None or imagegen is None:
        d["prompts"] = {"skipped": True, "why": "llm 또는 imagegen 없음 — 사진 없이(검은 슬롯) 진행"}
    else:
        s = dict(d["script"]["script"]); s["groups"] = d["layout"]["groups"]
        d["prompts"] = _images.make_prompts(s, d["setup"]["source_text"], llm, log=log)


def images(job, d, wd, kw):
    imagegen, reviewer, log = kw["imagegen"], kw["reviewer"], kw["log"]
    if d["prompts"].get("skipped") or imagegen is None:
        d["images"] = {"files": {}, "skipped": True}
        return None
    files = _images.generate_all(d["prompts"]["prompts"], wd, imagegen, sources=d["prompts"].get("sources"), log=log)
    if reviewer is not None:
        topic = ((d["script"]["script"].get("title") or {}).get("h1") or "")
        searched = _images.searched_map(files)
        subs = _pl._subtitles_by_slot(d["script"]["script"])
        wants = {}
        for k, s in (d["prompts"].get("sources") or {}).items():
            q = (s or {}).get("query") or ""
            wants[str(k)] = f"검색: {q}" if q else "생성 이미지"
        rounds = []
        chk = _photocheck.check(files, subs, reviewer=reviewer, log=log, wants=wants, topic=topic, searched=searched)
        for n in range(spec.PHOTO_RETRY_MAX):
            if not chk["retry"]:
                break
            reasons = {r["slot"]: _photocheck.fail_reason(r["review"]) for r in chk["reviewed"] if r.get("review", {}).get("failed")}
            rounds.append({"round": n + 1, "retry": list(chk["retry"]), "reasons": reasons})
            log(f"[brainbulb.images] 재시도 {n + 1}/{spec.PHOTO_RETRY_MAX} — 슬롯 {chk['retry']}")
            files = _images.regenerate(chk["retry"], d["prompts"]["prompts"], wd, imagegen, files, log=log, reasons=reasons)
            chk = _photocheck.check({k: files[k] for k in chk["retry"] if k in files}, subs, reviewer=reviewer, log=log,
                                    wants=wants, topic=topic, searched=searched)
        chk["rounds"] = rounds
        chk["unresolved"] = list(chk["retry"])
        if chk["unresolved"]:
            log(f"[brainbulb.images] ★{spec.PHOTO_RETRY_MAX}회에도 못 고친 슬롯: {chk['unresolved']} — 그대로 쓴다(편을 멈추지 않는다)")
        d["photo_check"] = chk
    d["images"] = {"files": files}


def frames(job, d, wd, kw):
    s = dict(d["script"]["script"]); s["groups"] = d["layout"]["groups"]
    d["frames"] = _frames.build(wd, d["timing"], s, d["images"]["files"], meme_dir=kw["meme_dir"], log=kw["log"],
                                card_img=d["script"]["script"].get("card_img"))


HANDLERS = {"prompts": prompts, "images": images, "frames": frames}
```
★옮길 때 pipeline.py의 원문 주석(실측 2026-09-13·14·16 사유)도 같이 가져간다 — 위 코드는 줄을 줄여 보인 것이고, 실제 파일엔 원문 주석을 붙인다.

- [ ] **Step 3: pipeline.py에서 세 분기 삭제 + import 정리**

`elif step == "prompts":` … `elif step == "images":` … (`_invalidate_after(job, "images")`까지) 와 `elif step == "frames":` 블록을 지운다. import 줄에서 `images as _images, frames as _frames, photocheck as _photocheck` 중 `_images`·`_photocheck`를 뺀다(`_frames`는 다른 데서 안 쓰면 같이 뺀다):
```bash
grep -n "_images\|_photocheck\|_frames" shopping_shorts/channelkit/pipeline.py
```
Expected: 0줄.

- [ ] **Step 4: spec 등록 + shim + api 경로**

`channel_presets/brainbulb/spec.py` 끝의 `STEP_HANDLERS = {}`를:
```python
from . import steps as _steps          # noqa: E402 — 맨 아래: steps가 channelkit을 import하고 channelkit.spec은 이 모듈을 본다
STEP_HANDLERS = _steps.HANDLERS
```
shim `shopping_shorts/brainbulb/__init__.py`의 `_NAMES`에서 `images, photos, photocheck, community`를 빼고 별도 매핑:
```python
_CH = "shopping_shorts.channel_presets.brainbulb"
_CH_NAMES = ["images", "photos", "photocheck", "community", "steps"]
```
`__getattr__`와 아래 루프가 `_CH_NAMES`는 `_CH`에서 찾도록 두 줄씩 추가(같은 모양).
`brainbulb_api.py:164` `from shopping_shorts.brainbulb import providers, images as _images` 는 shim이 처리하므로 그대로 둔다.

- [ ] **Step 5: 순환 import 확인 + 전체 테스트**

```bash
PYTHONUTF8=1 py -c "from shopping_shorts.channelkit import pipeline, spec; print(sorted(spec.STEP_HANDLERS))" && PYTHONUTF8=1 py -m pytest shopping_shorts/tests/test_channelkit_registry.py shopping_shorts/tests/test_brainbulb_*.py -q -p no:cacheprovider 2>&1 | tail -2
```
Expected: `['frames', 'images', 'prompts']` / `250 passed`. 순환 import로 죽으면 `steps.py`의 `from shopping_shorts.channelkit import pipeline as _pl`을 함수 안 지역 import로 바꾼다.

- [ ] **Step 6: 커밋**

```bash
git add -A && git commit -m "channel_presets/brainbulb: images·photos·photocheck·community 이동, prompts·images·frames 단계를 STEP_HANDLERS로"
```

---

### Task 6: 동일성 검사 도구 + 실제 한 편 dry-run

골든 테스트는 린터·프롬프트만 본다. **렌더까지** 전과 같은지 한 편으로 잰다(0순위-A1). 이 트랙엔 실제 API 키가 없어도 `make.py`가 llm/tts 없으면 need_input에서 멈추므로, **기준선 산출물**과 대조할 것은 `sub.ass` 생성과 프레임 합성이다.

**Files:**
- Create: `tools/channelkit/parity_check.py`

- [ ] **Step 1: 도구**

```python
# -*- coding: utf-8 -*-
"""channelkit 쪼개기 동일성 — 골든 픽스처 5편의 (린터 issues, 레이아웃 줄나눔, sub.ass 텍스트)를 해시로 찍는다.

  py tools/channelkit/parity_check.py --write out/channelkit_parity/baseline.json   # 쪼개기 전(뇌전구벤치)에서
  py tools/channelkit/parity_check.py --compare out/channelkit_parity/baseline.json  # 쪼갠 뒤(숏템엔진)에서

두 실행의 해시가 전부 같아야 "전과 같다". 다르면 어느 편·어느 항목인지 찍는다.
옛 패키지(shopping_shorts.brainbulb)와 새 패키지 둘 다에서 돌도록 import를 폴백한다.
"""
import argparse
import glob
import hashlib
import json
import os
import sys

ROOT = os.path.dirname(os.path.dirname(os.path.dirname(os.path.abspath(__file__))))
sys.path.insert(0, ROOT)
try:
    from shopping_shorts.channelkit import lint, layout, ass_gen, timing, spec
except ImportError:
    from shopping_shorts.brainbulb import lint, layout, ass_gen, timing, spec

FIX = os.path.join(ROOT, "shopping_shorts", "tests", "fixtures", "brainbulb")


def _h(obj):
    return hashlib.sha256(json.dumps(obj, ensure_ascii=False, sort_keys=True, default=str).encode()).hexdigest()[:16]


def measure():
    out = {}
    for d in sorted(glob.glob(os.path.join(FIX, "*"))):
        name = os.path.basename(d)
        p = os.path.join(d, "payload.json")     # 골든 픽스처 = 볼케이노 next_payload(title·groups 포함, 실측 2026-09-25)
        if not os.path.isfile(p):
            continue
        with open(p, encoding="utf-8") as fh:
            script = json.load(fh)
        issues, laid = lint.lint(script, source_text="", fonts_dir=spec.FONTS_DIR)
        groups = laid["groups"]
        cut_secs = [2.0] * len(groups)
        tm = timing.build(3.0, cut_secs, groups)
        ass = ass_gen.build(script["title"], script["title"].get("card", ""), tm, fonts_dir=spec.FONTS_DIR)
        out[name] = {"issues": _h([i.__dict__ for i in issues]), "lines": _h([g.get("lines") for g in groups]), "ass": _h(ass)}
    return out


def main():
    ap = argparse.ArgumentParser(); ap.add_argument("--write"); ap.add_argument("--compare")
    a = ap.parse_args()
    cur = measure()
    if a.write:
        os.makedirs(os.path.dirname(a.write), exist_ok=True)
        with open(a.write, "w", encoding="utf-8") as fh:
            json.dump(cur, fh, ensure_ascii=False, indent=1)
        print(f"wrote {len(cur)}편 → {a.write}"); return 0
    with open(a.compare, encoding="utf-8") as fh:
        base = json.load(fh)
    bad = [(n, k) for n in base for k in base[n] if cur.get(n, {}).get(k) != base[n][k]]
    print(f"편 {len(base)} · 항목 {sum(len(v) for v in base.values())} · 불일치 {len(bad)}")
    for n, k in bad:
        print(f"  ✗ {n}.{k}: {base[n][k]} → {cur.get(n, {}).get(k)}")
    return 1 if bad else 0


if __name__ == "__main__":
    sys.exit(main())
```
픽스처 파일은 `payload.json`(실측: title·groups·card_img·slots 키). `timing.build`·`ass_gen.build` 인자는 pipeline.py 실측(`timing.build(v["card_sec"], v["cut_secs"], groups)` / `ass_gen.build(title, card, timing, fonts_dir=)`)과 같다.

- [ ] **Step 2: 기준선은 쪼개기 전 폴더에서** (`.tracks/뇌전구벤치`, 옛 import 폴백)

```bash
cp tools/channelkit/parity_check.py "/c/Users/CH/Desktop/로또의 주식/.tracks/뇌전구벤치/parity_check_tmp.py" && cd "/c/Users/CH/Desktop/로또의 주식/.tracks/뇌전구벤치" && PYTHONUTF8=1 py parity_check_tmp.py --write "/c/Users/CH/Desktop/로또의 주식/.tracks/숏템엔진/out/channelkit_parity/baseline.json"; rm parity_check_tmp.py; cd "/c/Users/CH/Desktop/로또의 주식/.tracks/숏템엔진"
```
Expected: `wrote 5편 → …baseline.json`. (`ROOT` 계산이 파일 위치 기준이라 임시 복사본은 `ROOT`가 트랙 루트가 된다 — 루트에 복사했으므로 `dirname` 3번이면 한 단계 위로 간다. 임시본에선 `ROOT = os.path.dirname(os.path.abspath(__file__))` 한 줄로 고쳐 쓴다.)

- [ ] **Step 3: 쪼갠 뒤 대조**

```bash
PYTHONUTF8=1 py tools/channelkit/parity_check.py --compare out/channelkit_parity/baseline.json
```
Expected: `편 5 · 항목 15 · 불일치 0`, rc 0. 불일치가 있으면 그 항목 모듈(lint/layout/ass_gen)의 diff를 `git diff 3b5466f50 -- shopping_shorts/channelkit/<모듈>.py`로 본다 — 쪼개기는 동작을 바꾸면 안 된다.

- [ ] **Step 4: `.gitignore`에 `/out/channelkit_parity/` 추가, 커밋**

```bash
printf '\n# channelkit 동일성 기준선(로컬 실측) — 커밋 금지\n/out/channelkit_parity/\n' >> .gitignore
git add -A && git commit -m "tools/channelkit/parity_check.py — 쪼개기 전후 골든 5편 동일성(불일치 0)"
```

---

### Task 7: 문서·핸드오프·기록

**Files:**
- Modify: `handoff/숏템엔진.md`, `wiki/log.d/숏템엔진.md`
- Modify: `docs/superpowers/specs/2026-09-25-숏템엔진-design.md` §2 (프록시 결정 추가)

- [ ] **Step 1: 설계 §2에 결정 한 줄 추가**

```
- 엔진 모듈의 `spec`은 **프록시**(`channelkit/registry.py`가 정한 현재 채널로 위임). 바꾸는 자리는 `registry.use()`·`pipeline.run_step(channel=)` 둘뿐. 이유: 20모듈·79상수를 인자로 넘기면 전부 고쳐야 했다(2026-09-25 실측).
```

- [ ] **Step 2: handoff 갱신** — `handoff/숏템엔진.md`에 섹션 추가

```
## 계획 1 완료 (날짜) — 엔진 쪼개기
- channelkit/(엔진) + channel_presets/brainbulb/(1호) + shim shopping_shorts/brainbulb. 테스트 250 passed(기준 243+7). parity 5편 불일치 0.
- 채널 추가 방법: channel_presets/<이름>/spec.py 에 STEPS·LINT_RULES·STEP_HANDLERS + 규격 상수. 엔진은 안 건드린다.
- ⚠ finish(main 병합)는 사장님 승인 후 — brainbulb API·페이지가 라이브로 나간다.
## ⏭ 다음: 계획 2 — 뜨거운사람들 20편 실측 → channel_presets/hotpeople/spec.py
```

- [ ] **Step 3: log.d 한 줄 + 커밋**

```bash
git add -A && git commit -m "handoff: 숏템엔진 계획 1(엔진 쪼개기) 완료 기록"
```

- [ ] **Step 4: 사장님께 보고** — "쪼개기 끝. 테스트 250, parity 0. main 병합(라이브 반영)은 승인 주시면 finish." 승인 전엔 finish 하지 않는다.

---

## 자체 점검 (작성 후)

- 설계 §2 구조 → Task 1·2·5 / §2 원칙 1(spec 상수) → Task 2 / 원칙 2(린터 판정) → Task 4 / 원칙 3(채널 추가=spec) → Task 3·5 / §5 "골든 동일" → Task 0 기준선·Task 6 parity / §9 트랙 → Task 0.
- 이름 일관성: `registry.use/current/name/DEFAULT` · `pipeline.steps()` · `spec.STEPS/LINT_RULES/STEP_HANDLERS` · `lint.rules()/ALL_RULES` · 핸들러 계약 `fn(job, d, wd, kw)` — Task 3·4·5·6에서 같은 이름.
- 계획 2(뜨거운사람들)는 이 계획 완료 후 20편 실측 값을 넣어 따로 쓴다(설계 §3·§4).
