# 폰트검수

## 2026-08-26
- 최종렌더 한글 두부(네모X) 해결. 원인은 깨진 파일이 아니라 **영문 전용 폰트**(OkMallangW.ttf,
  글리프 81개·한글 0자)를 한글 폰트 목록에 넣어둔 것. 정상 TTF라 브라우저·ffmpeg가 오류 없이 로드했다.
- 두 겹 방어: sync_fonts.py가 한글 글리프 없는 폰트를 목록에서 막고(영문 전용은 "latin": true),
  video_assemble._font_ref가 렌더 직전 못 그리는 문구면 기본폰트로 폴백한다.
- 폰트 해석이 자막·헤드카피에 따로 적혀 있던 것을 _font_ref 하나로 통합(0순위-B).
- 판정은 이름·크기가 아니라 .notdef 마스크 대조(Pillow만 — fontTools는 서버에 없다).
