# P5 대본 스토리 강제 + CTA 보강 + 자기검증 루프 — 구현 계획

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 대본 초안(단일 리메이크·조합 모두)이 ①인과 사슬이 말이 되고 ②훅에서 던진 이야기가 끝까지 이어지며 ③마지막에 소재 맞춤 CTA("비법 궁금하면 댓글에 'OO'")가 항상 나오게 한다. AI가 스스로 3축 검수해 미달 초안만 자동 보정한다.

**Architecture:** `script_generate.py` 단일 파일 안에서 (a) 스토리 헌장 공통 블록을 `_GEN_PROMPT`·`_MIX_PROMPT` 양쪽에 주입 + 스키마로 스토리 선언 강제, (b) key_vault 캐스케이드 기반 `_call_json` 헬퍼 위에 판정 1콜(`_JUDGE_PROMPT`) + 미달 초안만 수정 1콜(`_FIX_PROMPT`)을 얹은 `_verify_and_fix`를 `generate_mix`/`generate_variations` 반환 직전에 배선. 판정 불능 시 fail-open(생성은 절대 안 죽음).

**Tech Stack:** Python(FastAPI 서버 모듈), google.genai(Gemini, response_schema JSON 강제), pytest(monkeypatch mock).

**작성:** 2026-07-19 페이블(설계 검증·계획) → 오푸스(집행). 설계: `docs/superpowers/specs/2026-07-19-2트랙모델-장면스파인-design.md` §2-4 + §7(페이블 보강).

---

## 집행자(오푸스)가 알아야 할 검증된 사실 — 대화 없이도 이해되게

1. **사장님 통증**: "대본 뽑기" 초안이 스토리가 헐겁다 — 인과·상관관계가 안 맞고, 훅과 끝이 안 이어지고, 레시피 영상의 정석 CTA("비법 궁금하면 댓글에 OO 남겨주세요")가 흐지부지. 감지된 소재에 손으로 넣어야 겨우 고쳐짐.
2. **★적용 범위 (오푸스 초안 설계의 구멍)**: 사장님 스크린샷 화면 = `generate_variations` → `_GEN_PROMPT` 경로(단일 리메이크/이식). 장면스파인 설계 P5가 지목한 건 `_MIX_PROMPT`(조합). **둘 다 고쳐야 한다** — 공통 블록(`_STORY_RULES_CORE`/`_STORY_DECLARE`)으로.
3. **키 경로 이원화**: `generate_mix`→`_generate_drafts`(key_vault 캐스케이드) / `generate_variations`→comment_gen 키풀 직접. 새 판정·수정 콜은 **전부 key_vault 캐스케이드**(`_call_json`)로 통일(2026-07-13 소진 사고 예방책 재사용).
4. **⚠️테스트 실키 유출 함정**: 트랙 `.env`에 실키 45개. 검증 루프를 배선하면 기존 mock 테스트(`_wire`류는 comment_gen만 패치)가 key_vault 캐스케이드로 **실제 Gemini를 때린다**. Task 4 Step 1(헬퍼에 캐스케이드 차단 추가)을 배선보다 먼저 해야 한다.
5. **"레시피라고 다 같지 않다"(사장님)**: CTA 문구 하드코딩 금지. 패턴 메뉴(레시피/비법형→댓글키워드형 필수, 그 외→저장·팔로우·궁금증형)를 프롬프트로 주고 모델이 소재 맞춰 고른다. `cta_line`/`cta_keyword` 선언 필드가 쌓이면 나중 패턴 라이브러리 재료가 된다(이번엔 UI 없음 — YAGNI).
6. **프롬프트 조립 함정**: `_GEN_PROMPT`/`_MIX_PROMPT`는 `.format()`을 쓴다. 헌장 블록은 **모듈 레벨 문자열 연결**로 끼워 넣고, 헌장 텍스트에 `{`·`}`를 절대 넣지 마라(format이 깨진다).
7. **트랙 격리**: 편집은 전부 `C:\Users\CH\Desktop\로또의 주식\.tracks\장면스파인\` 절대경로. git은 `git -C`. main 폴더 코드 파일은 손대지 마라.

---

### Task 0: 트랙 상태 확인 + 베이스라인

**Files:** 없음 (확인만)

- [ ] **Step 1: 트랙 폴더 동기화 상태 확인**

Run: `git -C "C:\Users\CH\Desktop\로또의 주식\.tracks\장면스파인" status --porcelain && git -C "C:\Users\CH\Desktop\로또의 주식\.tracks\장면스파인" log --oneline -2`

Expected: 계획·핸드오프 문서 커밋(페이블)이 HEAD 근처, 코드 파일 미변경. (2026-07-19 페이블이 origin/main 병합 완료 — 4698b6da8)

- [ ] **Step 2: 대상 테스트 베이스라인 green 확인**

Run: `cd "C:\Users\CH\Desktop\로또의 주식\.tracks\장면스파인" && py -m pytest shopping_shorts/tests/test_script_generate.py -q`

Expected: 전부 PASS (2026-07-19 기준 26개). 실패가 있으면 멈추고 원인 파악 먼저.

---

### Task 1: `_call_json` 헬퍼 추출 (리팩터 — 행동 불변)

`_generate_drafts`의 key_vault 캐스케이드 루프를 스키마 인자화해서, 판정·수정 콜이 재사용할 공용 헬퍼로 만든다.

**Files:**
- Modify: `shopping_shorts/script_generate.py:26-47` (`_generate_drafts`)

- [ ] **Step 1: `_generate_drafts`를 `_call_json` + 래퍼로 재구성**

`script_generate.py`의 기존 `_generate_drafts` 전체(26~47줄)를 아래로 교체:

```python
def _call_json(prompt, schema):
    """key_vault 캐스케이드 키풀로 JSON 1콜. 소진키는 마킹하고 다음 키로.
    무키·전부실패면 {} (호출부는 반드시 빈 dict 허용 — fail-open)."""
    keys = key_vault.get_live_keys_cascade(_GEN_GROUP)
    for key in keys:
        try:
            resp = key_vault.get_client_for_key(key).models.generate_content(
                model=_MODEL, contents=prompt,
                config=types.GenerateContentConfig(
                    response_mime_type="application/json", response_schema=schema),
            )
            return json.loads(resp.text)
        except Exception as e:  # noqa: BLE001 — 생성 실패는 치명적 아님
            if key_vault.is_daily_exhausted_error(e) or key_vault.is_account_disabled_error(e):
                key_vault.mark_exhausted(key_vault._owner_group(key) or _GEN_GROUP, key)
                continue
            if key_vault.is_quota_error(e):
                continue  # 순간 rate limit — 다음 키로
            return {}
    return {}


def _generate_drafts(prompt):
    """key_vault 캐스케이드 키풀로 대본 초안 리스트 생성. 무키·전부실패면 []."""
    return _call_json(prompt, _SCHEMA).get("drafts", [])
```

(주의: `_SCHEMA`는 파일 아래쪽에 정의돼 있지만 `_generate_drafts`는 호출 시점에 참조하므로 문제없음 — 기존 코드도 같은 구조였다.)

- [ ] **Step 2: 회귀 확인**

Run: `cd "C:\Users\CH\Desktop\로또의 주식\.tracks\장면스파인" && py -m pytest shopping_shorts/tests/test_script_generate.py -q`

Expected: Task 0과 동일하게 전부 PASS.

- [ ] **Step 3: Commit**

```bash
git -C "C:\Users\CH\Desktop\로또의 주식\.tracks\장면스파인" add shopping_shorts/script_generate.py
git -C "C:\Users\CH\Desktop\로또의 주식\.tracks\장면스파인" commit -m "refactor(장면스파인): _generate_drafts에서 _call_json 헬퍼 추출 — 판정·수정 콜 재사용 준비"
```

---

### Task 2: 스토리 헌장 공통 블록 + 스키마 스토리 선언 강제 (TDD)

**Files:**
- Modify: `shopping_shorts/script_generate.py` (`_SCHEMA` items·`_GEN_PROMPT`·`_MIX_PROMPT`, 새 상수 2개)
- Test: `shopping_shorts/tests/test_script_generate.py`

- [ ] **Step 1: 실패하는 테스트 3개 작성**

`test_script_generate.py` 맨 끝에 추가:

```python
# ---- P5: 스토리 헌장 + 스키마 강제 (2026-07-19) ----

def test_schema_requires_story_declaration_fields():
    # 모델이 대본을 쓰기 전에 스토리(인물·사건·결말)와 CTA를 '선언'하게 스키마로 강제
    req = script_generate._SCHEMA["properties"]["drafts"]["items"]["required"]
    for f in ("story_person", "story_event", "story_resolution", "cta_line", "cta_keyword"):
        assert f in req


def test_gen_prompt_has_story_contract():
    # 사장님 스크린샷 통증 경로(_GEN_PROMPT 단일 리메이크)에도 헌장이 들어가야 한다
    for kw in ("한 스토리", "인과 사슬", "CTA", "남겨주세요"):
        assert kw in script_generate._GEN_PROMPT


def test_mix_prompt_single_story_rule():
    # P5: 인물1·사건1·결말1 — 조합이라도 인물·사건은 절대 섞지 않는다
    assert "인물 1명" in script_generate._MIX_PROMPT
    assert "섞지 마라" in script_generate._MIX_PROMPT
    assert "인과 사슬" in script_generate._MIX_PROMPT
```

- [ ] **Step 2: 실패 확인**

Run: `cd "C:\Users\CH\Desktop\로또의 주식\.tracks\장면스파인" && py -m pytest shopping_shorts/tests/test_script_generate.py -q -k "story or contract or single_story"`

Expected: 3 FAIL (KeyError 또는 assert 실패).

- [ ] **Step 3: 헌장 상수 2개를 `_GEN_PROMPT` 정의 바로 위에 추가**

⚠️ 헌장 텍스트에 `{`·`}` 금지 — `.format()`이 깨진다.

```python
# 스토리 헌장 — _GEN_PROMPT·_MIX_PROMPT 공통 주입(모듈 레벨 문자열 연결).
# 사장님 요구(2026-07-19): 인과·상관관계가 말이 되고, 훅이 끝까지 이어지고,
# 레시피류의 정석 CTA("비법 궁금하면 댓글에 'OO'")가 스토리 흐름 안에서 나와야 한다.
# CTA는 하드코딩 1문구가 아니라 소재 적합형 선택 — "레시피라고 다 같지 않다".
_STORY_RULES_CORE = """- ★한 스토리 원칙: 대본 전체가 인물 1명·사건 1개·결말 1개의 '하나의 이야기'다.
  훅에서 던진 궁금증/문제가 전개에서 원인→전환점으로 풀리고 결말에서 해소돼야 한다.
  각 문장은 앞 문장의 결과나 이유여야 한다(인과 사슬). 뜬금없는 소재 점프,
  근거 없는 효능 비약, 훅 따로 본문 따로 전개 금지.
- ★CTA 원칙: 마지막 문장은 반드시 행동유도(CTA)로 끝난다. 핵심 비법 하나는 본문에서
  다 밝히지 말고 아껴서, CTA가 그 아낀 비법과 자연스럽게 이어지게 하라.
  · 소재가 레시피/비법/방법형이면 반드시 댓글 키워드형: "…궁금하면 댓글에 'OO' 남겨주세요"
    (OO = 소재에서 뽑은 2~6자 키워드. 예: 바나나 간식 영상이면 '바나나').
  · 그 외 소재면 저장유도·팔로우유도·궁금증남기기 중 소재에 가장 어울리는 것 하나."""

_STORY_DECLARE = """- 각 초안에 반드시 채워라: story_person(주인공 1명), story_event(사건 한 줄),
  story_resolution(결말 한 줄), cta_line(마지막 CTA 문장 그대로),
  cta_keyword(댓글 유도 키워드 — 댓글형 CTA가 아니면 빈 문자열)."""
```

- [ ] **Step 4: `_SCHEMA` draft items에 선언 필드 5개 추가**

`_SCHEMA`의 items를 다음으로 교체:

```python
            "items": {
                "type": "object",
                "properties": {
                    "hook": {"type": "string"},
                    "script": {"type": "string"},
                    "applied": {"type": "string"},
                    "story_person": {"type": "string"},
                    "story_event": {"type": "string"},
                    "story_resolution": {"type": "string"},
                    "cta_line": {"type": "string"},
                    "cta_keyword": {"type": "string"},
                },
                "required": ["hook", "script", "applied", "story_person", "story_event",
                             "story_resolution", "cta_line", "cta_keyword"],
            },
```

- [ ] **Step 5: `_GEN_PROMPT` 개편 (헌장 연결)**

`_GEN_PROMPT` 전체를 아래로 교체 (기존 규칙 5줄 유지 + 헌장 연결, `{n}`·`{full_text}`·`{elems}`·`{topic_line}` 자리 유지):

```python
_GEN_PROMPT = """너는 한국 쇼핑 숏폼(살림·요리·인테리어) 대본 작가다. 아래 'S급 원본 대본'의
검증된 구조를 빌려, 20초 분량(약 45~70단어)의 새 대본 초안 {n}개를 만들어라.

[S급 원본 대본]
{full_text}

[구조 요소별 지시 — 유지/변형]
{elems}

[{topic_line}]

규칙:
- 각 초안은 실제로 읽을 나레이션(구어체). 0초 훅부터 끝 CTA까지 이어지게.
- 실제 말하듯 자연스러운 추임새·감탄사를 적재적소에 넣어라(예: "와~", "진짜", "정말",
  "헐", "대박", "어머", "이거 봐", "글쎄"). 문어체처럼 딱딱하지 않게, 말투/어미 요소와 어울리게.
- '변형' 요소는 원본을 베끼지 말고 참신하게 바꾸고, '유지' 요소는 그 강점을 그대로 살려라.
- 주변인물을 쓸 땐 오버하지 말고 자연스럽게(예: "농원 하는 언니가", "김밥집 사장님이",
  "병원 하는 지인이"). 억지 설정·과장 금지.
- 초안끼리 서로 다르게(훅·전개를 다양하게 시도).
""" + _STORY_RULES_CORE + "\n" + _STORY_DECLARE + """
각 초안: hook(첫 훅 한 줄), script(전체 나레이션 대본), applied(무엇을 유지/변형했는지 한 줄).
JSON만 출력."""
```

- [ ] **Step 6: `_MIX_PROMPT` 개편 (P5 인물1·사건1·결말1 + 헌장 연결)**

`_MIX_PROMPT` 전체를 아래로 교체 (`{seconds}`·`{words}`·`{n}`·`{sources}` 자리 유지):

```python
_MIX_PROMPT = """너는 한국 쇼핑 숏폼 대본 작가다. 아래 여러 개의 검증된 S급 대본이 있다.
약 {seconds}초 분량(대략 {words}단어)의 새 대본 초안 {n}개를 만들어라.

[재료 대본들]
{sources}

규칙:
- ★초안마다 먼저 '인물 1명·사건 1개·결말 1개'를 정하라. 다른 대본에서는 훅 방식·표현·
  말투·전개 리듬만 빌리고, 인물과 사건은 절대 섞지 마라(대본 A의 인물이 대본 B의
  사건을 겪으면 안 된다).
- 특정 대본을 통째로 베끼지 말고, 좋은 부분만 골라 하나의 이야기로 녹여 새로 써라.
- 실제로 읽을 구어체 나레이션(0초 훅 → … → 끝 CTA). 억지 설정·과장 금지.
- 초안끼리 서로 다르게(정한 인물·사건·훅을 다양하게).
""" + _STORY_RULES_CORE + "\n" + _STORY_DECLARE + """
각 초안: hook(첫 훅 한 줄), script(전체 나레이션 대본), applied(어느 대본에서 무엇을 빌렸고
어떤 한 스토리로 유지했는지 한 줄).
JSON만 출력."""
```

- [ ] **Step 7: 전체 테스트 확인 (새 3개 + 기존 회귀)**

Run: `cd "C:\Users\CH\Desktop\로또의 주식\.tracks\장면스파인" && py -m pytest shopping_shorts/tests/test_script_generate.py -q`

Expected: 전부 PASS (기존 `test_gen_prompt_includes_interjection_guidance`·`test_remake_mode_prompt_locks_subject` 등이 프롬프트 유지 문구를 지키는지 자동 검증해준다).

- [ ] **Step 8: Commit**

```bash
git -C "C:\Users\CH\Desktop\로또의 주식\.tracks\장면스파인" add shopping_shorts/script_generate.py shopping_shorts/tests/test_script_generate.py
git -C "C:\Users\CH\Desktop\로또의 주식\.tracks\장면스파인" commit -m "feat(장면스파인): P5 스토리 헌장 — 인과사슬·훅→끝·소재적합 CTA를 단일·조합 프롬프트 공통 강제 + 스키마 스토리 선언"
```

---

### Task 3: 판정·수정 프롬프트 + `_verify_and_fix` 오케스트레이터 (TDD)

판정 1콜(전 초안 일괄 3축 채점) → 미달 초안만 수정 1콜. 판정 불능이면 원본 그대로(fail-open).

**Files:**
- Modify: `shopping_shorts/script_generate.py` (`_SUBJECT_PROMPT` 아래에 신규 블록 추가)
- Test: `shopping_shorts/tests/test_script_generate.py`

- [ ] **Step 1: 실패하는 테스트 작성 (헬퍼 + 4케이스)**

`test_script_generate.py` 맨 끝에 추가:

```python
# ---- P5: 자기검증 루프 (2026-07-19) ----

def _seq_call_json(mp, responses):
    """script_generate._call_json을 '호출 순서대로 응답'하는 가짜로 교체.
    기록된 (prompt, schema) 리스트를 돌려줘 프롬프트 내용 검증에 쓴다."""
    calls = []
    def fake(prompt, schema):
        calls.append((prompt, schema))
        return responses[min(len(calls) - 1, len(responses) - 1)]
    mp.setattr(script_generate, "_call_json", fake)
    return calls


def test_verify_fixes_failing_draft(monkeypatch):
    drafts = [{"hook": "h0", "script": "인과 붕괴 대본", "applied": "a"}]
    calls = _seq_call_json(monkeypatch, [
        {"verdicts": [{"idx": 0, "causality_ok": False, "hook_to_end_ok": True,
                       "cta_ok": False, "fix_instruction": "마지막을 댓글 CTA로 끝내라"}]},
        {"hook": "h1", "script": "고쳐진 대본. 궁금하면 댓글에 '비법' 남겨주세요"},
    ])
    out = script_generate._verify_and_fix(drafts, seconds=20)
    assert out[0]["script"].startswith("고쳐진 대본")
    assert out[0]["hook"] == "h1"
    assert "자동 보정" in out[0]["applied"]
    assert "마지막을 댓글 CTA로 끝내라" in calls[1][0]  # 판정 지적이 수정 프롬프트에 들어감


def test_verify_keeps_passing_drafts(monkeypatch):
    drafts = [{"hook": "h", "script": "멀쩡한 대본", "applied": "a"}]
    _seq_call_json(monkeypatch, [
        {"verdicts": [{"idx": 0, "causality_ok": True, "hook_to_end_ok": True,
                       "cta_ok": True, "fix_instruction": ""}]},
    ])
    out = script_generate._verify_and_fix(drafts)
    assert out[0]["script"] == "멀쩡한 대본"  # 통과 초안은 손대지 않는다


def test_verify_fail_open_when_judge_dies(monkeypatch):
    # 판정 콜 실패(키 소진 등) → 초안 생성 자체는 절대 죽으면 안 된다
    drafts = [{"hook": "h", "script": "s", "applied": "a"}]
    _seq_call_json(monkeypatch, [{}])
    assert script_generate._verify_and_fix(drafts)[0]["script"] == "s"


def test_verify_bad_idx_ignored(monkeypatch):
    drafts = [{"hook": "h", "script": "s", "applied": "a"}]
    _seq_call_json(monkeypatch, [
        {"verdicts": [{"idx": 7, "causality_ok": False, "hook_to_end_ok": False,
                       "cta_ok": False, "fix_instruction": "x"}]},
    ])
    assert script_generate._verify_and_fix(drafts)[0]["script"] == "s"
```

- [ ] **Step 2: 실패 확인**

Run: `cd "C:\Users\CH\Desktop\로또의 주식\.tracks\장면스파인" && py -m pytest shopping_shorts/tests/test_script_generate.py -q -k verify`

Expected: 4 FAIL (`AttributeError: ... has no attribute '_verify_and_fix'`).

- [ ] **Step 3: 판정·수정 블록 구현**

`script_generate.py`의 `_SUBJECT_SCHEMA` 정의 **앞**(=`_REFINE` 블록 뒤)에 추가:

```python
# ---- P5 자기검증 루프: 초안 3축 판정(인과·훅→끝·CTA) 후 미달만 1회 자동 보정 ----

_JUDGE_SCHEMA = {
    "type": "object",
    "properties": {
        "verdicts": {
            "type": "array",
            "items": {
                "type": "object",
                "properties": {
                    "idx": {"type": "integer"},
                    "causality_ok": {"type": "boolean"},
                    "hook_to_end_ok": {"type": "boolean"},
                    "cta_ok": {"type": "boolean"},
                    "fix_instruction": {"type": "string"},
                },
                "required": ["idx", "causality_ok", "hook_to_end_ok", "cta_ok",
                             "fix_instruction"],
            },
        }
    },
    "required": ["verdicts"],
}

_JUDGE_PROMPT = """너는 한국 쇼핑 숏폼 대본 품질 검수자다. 아래 초안들을 초안별로
3가지 축으로 엄격히 판정하라.

[초안들]
{drafts}

판정 축:
1. causality_ok — 인과·상관관계가 말이 되나? 각 문장이 앞 문장에서 자연스럽게
   이어지나. 근거 없는 효능 비약, 뜬금없는 소재 점프가 있으면 실패.
2. hook_to_end_ok — 훅에서 던진 궁금증/문제/이야기가 끝까지 끊기지 않고 이어져
   해소되나. 훅 따로 본문 따로면 실패. 인물이나 사건이 중간에 바뀌면 실패.
3. cta_ok — 마지막이 행동유도(CTA) 문장으로 끝나고, 그 CTA가 본문 이야기와
   인과로 이어지나. 댓글 키워드형이면 'OO 남겨주세요'의 키워드가 실제로 명시돼
   있어야 통과.

하나라도 실패면 fix_instruction에 무엇을 어떻게 고칠지 구체적 수정 지시를
한국어 1~2문장으로 써라. 전부 통과면 fix_instruction은 빈 문자열.
idx는 초안 번호(0부터). JSON만 출력."""

_FIX_SCHEMA = {
    "type": "object",
    "properties": {"hook": {"type": "string"}, "script": {"type": "string"}},
    "required": ["hook", "script"],
}

_FIX_PROMPT = """너는 한국 쇼핑 숏폼 대본 작가다. 아래 대본이 검수에서 지적받았다.
지적을 고쳐 통째로 다시 써라. 구어체 나레이션, 원래 분량(약 {words}단어) 유지.

[대본]
{script}

[검수 지적]
{instruction}

규칙:
""" + _STORY_RULES_CORE + """
다음 JSON으로만 출력: {{"hook": "첫 훅 한 줄", "script": "다시 쓴 전체 대본"}}"""


def _verify_and_fix(drafts, seconds=20):
    """초안들을 3축(인과·훅→끝·CTA) 판정하고 미달 초안만 1회 재작성.
    판정 1콜 + 미달 수 만큼 수정 콜. 판정 실패 시 원본 그대로(fail-open) —
    검증 루프가 생성 기능을 죽이면 안 된다."""
    if not drafts:
        return drafts
    words = max(15, round(seconds * 2.3))
    block = "\n\n".join(f"[초안 {i}]\n{d.get('script', '')}" for i, d in enumerate(drafts))
    verdicts = _call_json(_JUDGE_PROMPT.format(drafts=block), _JUDGE_SCHEMA).get("verdicts", [])
    for v in verdicts:
        i = v.get("idx")
        if not isinstance(i, int) or not (0 <= i < len(drafts)):
            continue
        if v.get("causality_ok") and v.get("hook_to_end_ok") and v.get("cta_ok"):
            continue
        instruction = ((v.get("fix_instruction") or "").strip()
                       or "인과가 끊긴 부분을 잇고 마지막을 본문과 이어지는 CTA로 끝내라.")
        fixed = _call_json(
            _FIX_PROMPT.format(script=drafts[i].get("script", ""),
                               instruction=instruction, words=words), _FIX_SCHEMA)
        if (fixed.get("script") or "").strip():
            drafts[i]["script"] = fixed["script"].strip()
            drafts[i]["hook"] = (fixed.get("hook") or drafts[i].get("hook") or "").strip()
            drafts[i]["applied"] = ((drafts[i].get("applied") or "") + " · 검수 후 자동 보정").strip(" ·")
    return drafts
```

(주의: `_FIX_PROMPT`는 `_STORY_RULES_CORE`를 쓰므로 반드시 그 정의(Task 2) **뒤**에 위치해야 한다. `{{`·`}}`는 format 이스케이프 — 그대로 쓸 것.)

- [ ] **Step 4: 테스트 통과 확인**

Run: `cd "C:\Users\CH\Desktop\로또의 주식\.tracks\장면스파인" && py -m pytest shopping_shorts/tests/test_script_generate.py -q`

Expected: 전부 PASS.

- [ ] **Step 5: Commit**

```bash
git -C "C:\Users\CH\Desktop\로또의 주식\.tracks\장면스파인" add shopping_shorts/script_generate.py shopping_shorts/tests/test_script_generate.py
git -C "C:\Users\CH\Desktop\로또의 주식\.tracks\장면스파인" commit -m "feat(장면스파인): 자기검증 루프 — 판정 1콜(인과·훅→끝·CTA) + 미달 초안만 자동 보정, fail-open"
```

---

### Task 4: 배선 + 기존 테스트 실키 유출 차단 (TDD)

⚠️ **Step 1을 배선(Step 4~5)보다 먼저** — 안 하면 기존 mock 테스트가 트랙 `.env`의 실키 45개로 실제 Gemini를 때린다.

**Files:**
- Modify: `shopping_shorts/script_generate.py` (`generate_mix`·`generate_variations` 반환부)
- Test: `shopping_shorts/tests/test_script_generate.py` (`_wire`·`_capture_prompt` 보강 + 배선 테스트 2개)

- [ ] **Step 1: 테스트 헬퍼에 key_vault 캐스케이드 차단 추가**

`test_script_generate.py`의 `_wire`와 `_capture_prompt` 각각의 함수 본문 첫 줄에 추가:

```python
    mp.setattr(script_generate.key_vault, "get_live_keys_cascade", lambda group: [])
```

(캐스케이드가 빈 리스트 → `_call_json`이 `{}` → `_verify_and_fix`가 fail-open으로 원본 그대로 → 기존 단언 전부 유지된다.)

- [ ] **Step 2: 실패하는 배선 테스트 2개 작성**

`test_script_generate.py` 맨 끝에 추가:

```python
def test_generate_variations_runs_verify(monkeypatch):
    h = {}
    _capture_prompt(monkeypatch, h)
    monkeypatch.setattr(script_generate, "_verify_and_fix",
                        lambda drafts, seconds=20: [dict(d, applied="verified") for d in drafts])
    out = script_generate.generate_variations(_STRUCT, "원본", {}, {}, mode="remake")
    assert out[0]["applied"] == "verified"


def test_generate_mix_runs_verify(monkeypatch):
    monkeypatch.setattr(comment_gen, "SHORTS_GEMINI_KEYS", ["k"])
    monkeypatch.setattr(script_generate, "_generate_drafts",
                        lambda p: [{"hook": "h", "script": "s", "applied": "a"}])
    monkeypatch.setattr(script_generate, "_verify_and_fix",
                        lambda drafts, seconds=20: [dict(d, applied="verified") for d in drafts])
    srcs = [{"name": "a", "full_text": "t1", "structure": {}},
            {"name": "b", "full_text": "t2", "structure": {}}]
    out = script_generate.generate_mix(srcs)
    assert out[0]["applied"] == "verified"
```

- [ ] **Step 3: 실패 확인**

Run: `cd "C:\Users\CH\Desktop\로또의 주식\.tracks\장면스파인" && py -m pytest shopping_shorts/tests/test_script_generate.py -q -k runs_verify`

Expected: 2 FAIL (`applied == "verified"` 단언 실패 — 아직 배선 전).

- [ ] **Step 4: `generate_mix` 배선**

`generate_mix`의 마지막 줄 `return _generate_drafts(prompt)`를 다음으로 교체:

```python
    return _verify_and_fix(_generate_drafts(prompt), seconds)
```

- [ ] **Step 5: `generate_variations` 배선**

`generate_variations`의 재시도 루프 안 성공 반환줄
`return json.loads(resp.text).get("drafts", [])`를 다음으로 교체:

```python
            return _verify_and_fix(json.loads(resp.text).get("drafts", []))
```

- [ ] **Step 6: 전체 테스트 확인**

Run: `cd "C:\Users\CH\Desktop\로또의 주식\.tracks\장면스파인" && py -m pytest shopping_shorts/tests/test_script_generate.py -q`

Expected: 전부 PASS (기존 26 + 신규 9 = 35개 전후).

- [ ] **Step 7: Commit**

```bash
git -C "C:\Users\CH\Desktop\로또의 주식\.tracks\장면스파인" add shopping_shorts/script_generate.py shopping_shorts/tests/test_script_generate.py
git -C "C:\Users\CH\Desktop\로또의 주식\.tracks\장면스파인" commit -m "feat(장면스파인): 단일·조합 초안 생성에 자기검증 루프 배선 + 테스트 실키 유출 차단"
```

---

### Task 5: 버튼 문구 지연 반영 (판정+수정 콜만큼 길어짐)

**Files:**
- Modify: `shopping_shorts/static/produce.html` (버튼 라벨 문자열만 — 로직 불변)

- [ ] **Step 1: 현재 문구 위치 확인**

Run: `grep -n "10~20초" "C:\Users\CH\Desktop\로또의 주식\.tracks\장면스파인\shopping_shorts\static\produce.html"`

Expected: "초안 생성 (10~20초)" 류 버튼 라벨 1개 이상.

- [ ] **Step 2: `(10~20초)` → `(15~40초)`로 교체**

찾은 모든 초안/조합 생성 버튼 라벨의 `(10~20초)`를 `(15~40초)`로 Edit. 다른 코드 변경 금지 (produce.html은 다트랙 hot — seam 최소화).

- [ ] **Step 3: Commit**

```bash
git -C "C:\Users\CH\Desktop\로또의 주식\.tracks\장면스파인" add shopping_shorts/static/produce.html
git -C "C:\Users\CH\Desktop\로또의 주식\.tracks\장면스파인" commit -m "chore(장면스파인): 초안 생성 버튼 소요시간 문구 15~40초 — 자기검증 루프 지연 반영"
```

---

### Task 6: 게이트 통과 → 라이브 배포 → 실검증

- [ ] **Step 1: 핸드오프·로그 갱신**

`handoff/장면스파인.md`(트랙 폴더)의 ⏭ 미결에서 P5 항목을 ✅ 완료로 옮기고, `wiki/log.d/장면스파인.md`에 1줄 추가:

```
2026-07-19 P5+CTA+자기검증 라이브 — 스토리 헌장(단일·조합 공통)+스키마 선언 강제+판정·보정 루프. 실검증: 사장님 초안 생성 육안.
```

- [ ] **Step 2: 커밋 + finish (게이트)**

```bash
git -C "C:\Users\CH\Desktop\로또의 주식\.tracks\장면스파인" add -A
git -C "C:\Users\CH\Desktop\로또의 주식\.tracks\장면스파인" commit -m "docs(장면스파인): P5 완료 핸드오프·로그"
cd "C:\Users\CH\Desktop\로또의 주식" && py tools/track.py finish 장면스파인
```

Expected: 게이트(문법→import→pytest) 통과 → main 병합 → 3분 내 라이브. **베이스라인 실패 3건은 기존 것**(타트랙 headcopy·인코딩·exhausted mock) — "실패가 늘지 않았나"로 판정되므로 통과 정상.

- [ ] **Step 3: 라이브 실검증 (fablize — 실행 증거 없이 완료 주장 금지)**

3분 후 shoppingshorts.duckdns.org 제작소에서:
1. 사장님 스크린샷과 같은 소재(바나나 간식류)로 **"원본 소재 그대로" 초안 생성** → 초안 3개 모두 ①마지막이 CTA 문장 ②댓글 키워드가 실제 명시("댓글에 '바나나'" 류) ③훅의 이야기가 끝까지 한 인물·한 사건인지 육안.
2. 위키 대본 2개로 **조합 생성** → 같은 3축 육안 + 인물·사건이 안 섞였는지.
3. 어색한 초안이 나오면: 판정 프롬프트(`_JUDGE_PROMPT`)의 해당 축 설명을 구체화하는 게 1차 튜닝 지점 — 코드 구조는 안 바꿔도 된다.

- [ ] **Step 4: 사장님 보고**

✅ 완료 보고: 오늘 한 것(P5+CTA+자기검증 라이브) / 다음 할 것(P2b·P3은 P1·P2 실렌더 확인 후) / 커밋·병합 여부. 실검증 1·2의 실제 결과(스크린샷 또는 초안 텍스트)를 증거로 첨부.

---

## 자기 리뷰 체크 (페이블, 작성 직후)

- 스펙 커버리지: 설계 §2-4(P5) ✓ Task 2 / §7-①(양 경로) ✓ Task 2 / §7-②(스키마) ✓ Task 2 / §7-③(CTA 메뉴) ✓ 헌장 / §7-④(판정+수정, fail-open) ✓ Task 3 / §7-⑤(테스트 격리) ✓ Task 4 Step 1. 조합대본-요소업그레이드 설계의 CTA 요소(ELEM_LABELS "cta")는 **이미 코드에 있음** — 추가 작업 불필요(실측 script_generate.py:58).
- 타입 일관성: `_verify_and_fix(drafts, seconds=20)` 시그니처가 테스트·배선 전부 동일 ✓ / `_call_json(prompt, schema)` ✓ / `_seq_call_json` 반환 calls[i] = (prompt, schema) ✓.
- 플레이스홀더: 없음. 모든 코드 스텝에 실제 코드 포함.
- 범위: 단일 파일 중심(script_generate.py) + 테스트 + 버튼 문구 1건 — 단일 계획 적정. P2b·P3·패턴 라이브러리 UI는 명시적 비목표.
