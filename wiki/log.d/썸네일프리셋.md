# 썸네일프리셋 트랙 로그

## 2026-09-02
- 7단계 [썸네일 생성] 바로 아래 "🖼 내 프리셋" 신설 — 만든 구성(글자·스티커·도형·배지)을
  담아 다음 영상에 딸깍 한 번에 얹는다. 배경은 안 담는다(다른 영상에 쓸 것이라).
- 서버 저장(계정별): `thumb_presets` 테이블 + 라우트 5개. 그림은 job 폴더가 아니라
  `data/thumb_presets/`에 따로 — job 폴더는 disk_cleanup이 지운다.
- ★기존 HC_PRESETS('한 번에 스타일 적용')와 **다른 물건**: 저건 글자 1개의 색, 이건 구성 전체.
