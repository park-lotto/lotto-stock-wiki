# yt-stable-launcher

- 임시 PTY 서버가 대화 턴과 함께 종료되는 문제를 확인했다.
- 정식 포트 `8090`에서 서버를 숨김 분리 프로세스로 시작하는 `dashboard/launch_yt.ps1`을 추가했다.
- 저장소 절대경로를 하드코딩하지 않고 `$PSScriptRoot`에서 계산하므로 한글 경로 깨짐을 피한다.
- 이미 서버가 실행 중이면 재사용하고, 새로 시작할 때는 `/yt` HTTP 200을 확인한 후 브라우저를 연다.
