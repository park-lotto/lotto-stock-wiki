# 장면그림
- 2026-09-09: 최종 저장 출구에서 primary JPEG 1장 검증, 텍스트 폴백·두 판정 JSONL·중복 호출 방지 구현. screen_verify_enabled 기본 OFF, 화면 교체 없음.
- 계약/회귀 테스트 78개 통과 및 앱 import 확인. 실제 모델 키·소스·ffmpeg가 없어 라이브 판정/비용/시간 미측정. finish 미실행.
- git add는 트랙 밖 Git index.lock 쓰기 권한 거부로 실패하여 커밋 미완료. 변경 파일은 트랙에 보존.

## 2026-09-10
- 아스트라(코덱스) 구현분 서버 실측 완료: 비트 17개 중 텍스트 vs 그림 판정 **2건 갈림**, 둘 다 그림이 맞았다(프레임 육안 확인).
- ★이미지 판정은 comment_gen 키풀을 쓴다 — 비면 로그 없이 조용히 죽는다(로컬 17/17 null로 재현).
- finish 보류: 표본 확대 + 텍스트 호출 유지 여부 결정 뒤.
- (2) 표본 확대: job 60건·240칸. 갈림 24건(10.9%) — 사진이 더 잡음 14 / 풀어줌 10. 눈으로 본 4건 모두 사진이 맞았다. ★CTA 자막('안전' vs '나도') 불일치는 글자로는 원리상 못 잡는다.
- (3) ★어긋난 22칸 중 21칸(95%)은 같은 영상 안에 맞는 장면이 이미 있었다 — 재료·대본 문제가 아니라 고르기 문제. "이걸로 바꾸세요" 추천이 가능하다(자동 교체는 금지, 09-08 낱말겹침 기각 교훈).
- (4) 자동 교체 구현·실측: 어긋난 칸 20개 자동 교체, 3개는 못 찾아 빨간불 유지. "스마트폰 거치" 칸이 실제로 폰 꽂힌 화면으로 바뀐 것 육안 확인. 원본 남기고 되돌리기 버튼 제공.
- (5) ★날짜로 갈라 재니 09-09 job 12% → 09-10 job 2%. 앞단(B1 태깅·재료 추출) 배포가 반영된 뒤 어긋남이 6분의 1. 검사는 그물일 뿐 본체가 아니다. 시간=job당 20초(글자 판정 빼면).
- (6) 글자 판정 제거(칸당 2회→1회) + 칸 동시 검사(job당 21초→4.7초). ★배치는 2.9초였으나 뺐고, 뺀 근거("배치가 판정을 흔든다")는 틀렸다 — 동시 물음에서도 같은 비율로 흔들린다. 경계선 훅 문장에서 모델 답이 5% 안팎 흔들리는 게 원인.
- (7) 페이블·아스트라 교차검토 → 결함 6건 발견·전부 수정. ★페이블이 실행으로 찾은 것: _apply_beat_sources가 저장마다 교체를 되돌려 무한 왕복 + 되돌리기 버튼 무효. screen_locked 신설로 사람 결정을 잠근다. 두 AI 공통 권고 = 검사만 먼저 켜고 autofix는 사람 눈 판정 뒤.
