# 트랙격리 — 작업 로그

- 2026-07-15 — [설계·구현] **동시세션 충돌차단 트랙격리 페이즈1(상태 계층 분리)**: 한 PC 6세션이 같은 워킹트리로 같은 앱을 동시개발해 매일 나던 커밋 섞임·문서 덮임을 구조적으로 차단. 브레인스토밍→스펙(83852528)→계획(e50ccdac·54ce74fe)→SDD. **B안 채택 근거**: 사고 기록이 전부 git·문서 계층이고 파일 동시편집 사고는 0건 — 공유트리가 편집을 계속 통합시키고 있으므로 건드리지 않고 깨지는 두 계층만 기계로 막는다. A안(세션당 worktree)은 produce.html(이틀 20회 접촉)을 6갈래로 갈라 3-way 병합을 새로 만들어 기각. **실측이 설계를 바꾼 3건**: ①post-commit=`git push` 무조건 → 커밋=즉시 라이브, 검증은 commit-msg(push 이전)에서만 유효 ②core.hooksPath가 이미 .git/hooks 절대경로 + CRLF pre-commit 가동 중 → hooksPath 바꾸면 둘 다 조용히 죽음(shim으로 얹을 것) ③6세션이 .git·인덱스 공유 → fetch "0 behind" 확인 몇 초 뒤 커밋 시점엔 이미 남이 밀어넣음(401fe180..80487982 실측) → 락은 commit이 아니라 **add부터** 감싸야 함. 자체리뷰 교정: raw/는 CLAUDE.md가 PC간 공유용으로 의도 추적하므로 전면 denylist 금지, 전용 트랙에만 허용. **구현**: 대시보드(git 밖) `dashboard_common.py` 신설·`--track`(반드시 optional, 필수화하면 5세션 stop훅 전멸)·dedup키 (date,category,track)·file_lock·원자쓰기 / 렌더 트랙별 서브카드 / stop훅 active_tracks. 26테스트 green, 옛 명령형 EXIT=0, **같은 날 2트랙 공존 실측**. 문서: NEXT_SESSION 12섹션→handoff/<트랙>.md(366줄≥원본348, 유실0), log.md 동결+log.d/, tools/log_view.py(stdout 전용=덮임 불가). **CLAUDE.md의 `git add -A` 지시 제거** — 배포규칙 #6과 모순이었고 커밋 흡수 사고의 진원. ⚠️ 세션 리밋(14:00)으로 서브에이전트가 죽어 컨트롤러 인라인 구현 — **opus 리뷰 미실시**, 리셋 후 필수. 다음=페이즈2(track.py·commit-msg훅·safe_commit, 경고전용→거부모드는 전세션 공지 후). 상세=handoff/트랙격리.md
- 2026-07-16 — [구현·실전검증] **트랙폴더+병합게이트 롤아웃 1~3단계 완료** (90aa3fa1·7553895e·c339cd49·544d3576). 어제 밤 설계문서(605a6f92)만 넣고 구현 0으로 끝났던 것을 이어받음. **락/신호등(옛 페이즈2)은 폐기** — 흡수 기전이 타이밍이 아니라 공유 파일이라 락은 '언제 커밋하나'만 조율하고 '파일 안에 뭐가 들었나'는 못 바꾼다. **설계에서 실제로 바꾼 것**: 병합·게이트를 원본 폴더가 아니라 **전용 stage 폴더**에서 한다 — 원본에서 하면 게이트(pytest)가 도는 수 분간 원본이 '반쯤 병합된' 상태로 남아 옆 세션이 그걸 커밋하면 흡수가 그 자리에서 재발. 동시 finish는 **git의 non-fast-forward 거절이 원자적 신호등**이라 사람이 지킬 규약 불필요. 부수효과=main 폴더가 dirty해도 병합 안 막히고 남의 미커밋 코드도 안 딸려감(전환기에 결정적). **★stage 근거 실측 증명**: 공유 main 폴더 기준선 11건 vs stage(origin/main) 10건 — 차이 1건(test_build_edit_plan_exhausted_returns_empty)은 origin에선 멀쩡하고 공유 폴더에서만 깨짐 = 옆 세션 미커밋 편집이 만든 실패. 원설계대로였으면 **멀쩡한 병합을 오탐으로 막았을 것**. **2단계 드라이런이 잡은 결함**: 게이트가 기준선 다 모으고도 'ℹ️'에서 cp949 UnicodeEncodeError로 죽음 = 이 PC에서 한 번도 완주 못 하던 상태(pytest 출력캡처가 인코딩을 바꿔 52 green으로도 안 잡힘 — 실제로 돌려보기 전엔 모르는 종류). **★3단계 실전이 잡은 설계오류**: §5.3의 "트랙 브랜치 push는 무해하다"가 정반대 — `worktree add -b track/X origin/main`이 upstream을 origin/main으로 박아 트랙 커밋마다 post-commit이 '트랙을 main으로' 밀려 함(게이트 통째 우회). 지금 안전한 건 순전히 push.default unset(=simple)이 거절해준 **우연**. 수정+오프라인 경로까지 테스트. **검증(합성 아님)**: 진짜 저장소에 진짜 ImportError 주입→게이트가 막음(exit 1, origin/main 불변·main폴더 미오염 실측) / 정상작업→통과→병합(544d3576 부모2개=--no-ff)→트랙·stage 자동정리. 57테스트, 뮤테이션 21종 전부 죽음(살아남은 2종은 매번 *내 테스트*가 죽은 테스트임을 드러내 함께 수정 — 이 저장소 '통과하는데 결함 못잡는 테스트' 5·6번째 사례). **자동 커밋 주체 3개 실측 등재**(설계 §5.4): .claude/settings.json SessionEnd 훅 `git add .`+push(세션 끝날 때마다, 104커밋 — 대부분 raw/·CRLF churn이나 최근 20건 중 6건이 코드 1~3개 쓸어담음) / post-commit / 서버 autopilot `git add -A`+push origin main(크론 5회/일). CLAUDE.md의 'git add -A 금지'는 Claude 행동만 겨냥해 훅은 못 막는다 — **폴더 분리가 훅을 자동 무해화**(트랙 폴더 안만 쓸어담고 track/ 브랜치로 감). ⏭ **4단계(CLAUDE.md 갱신+전 트랙 전환)는 타 세션 영향 있어 사용자 승인·전세션 공지 필요.** 별건 의심(미검증): autopilot_deploy.deploy_local이 add -A 후 commit 실패 시 서버에 전부 staged로 남겨 배포를 조용히 멈출 수 있음(7/14 실사고 원인?). 상세=handoff/트랙격리.md
- 2026-07-16 — [4단계·전환완료] **트랙폴더가 기본 작업방식이 됨**(e4471cad·0be945c7). CLAUDE.md 갱신(옛 절차는 전환 전 세션용으로 details 보존) + **어제 세션들이 main 폴더에 남긴 고아 WIP 4건을 각 트랙 폴더로 이전**. 주인은 파일별 마지막 커밋으로 확정(추측 아님): df82027c→보이스 2건 / 945068ef→대본믹스통합 1건 / 70d8c41a→모션효과 1건. 절차=**복사→sha 대조+트랙 폴더 변경목록이 그 트랙 파일과 정확히 일치 확인→그 다음에만 main checkout --**(먼저 지우면 복구불가), 3/3 통과. **결과: main 폴더 코드 미커밋 0건 = 흡수의 재료가 사라짐.** 열린 트랙 3개(lotto-보이스·lotto-대본믹스통합·lotto-모션효과), 전부 upstream이 origin/track/<이름>으로 걸림 = **upstream 지뢰 수정이 실전에서 확인**. ★이전 중 3번째 cp949 구멍 발견: track.start()를 스크립트에서 직접 부르자 worktree 만든 직후 print에서 크래시 — 폴더는 생기고 예외는 나는 어정쩡한 상태. make_output_safe()를 main()에서만 불렀던 게 원인(CLI 전용이라는 가정이 API 직접호출로 깨짐) → 세 진입점 각각에서 호출, 61 green. **오늘만 cp949로 3번 죽음(드라이런/CLI/API직접) — 전부 실제로 돌려야만 보이는 종류.** ⏭ 각 트랙은 자기 폴더에서 재개(트랙격리 자신도 앞으론 트랙 폴더에서 — 오늘은 도구 부트스트랩이라 main에서 함). SessionEnd 훅은 그냥 둠(판단): 폴더분리가 코드흡수는 무해화하나 훅이 크롤봇 raw/의 집↔회사 동기화도 겸해서, 지금 지우면 raw/ 동기화가 끊긴다 → raw/ 전용 대체재 붙인 뒤 정리(페이즈3). 상세=handoff/트랙격리.md
- 2026-07-16 — [폴더위치 확정] **트랙 폴더를 프로젝트 안 `.tracks/<트랙명>`으로 이동**(48f66305·85754537). 처음엔 형제 폴더(Desktop\lotto-<이름>)로 만들었으나 사장님이 '안에 있어야 연결된다'고 지적 → **그게 옳았고 사고를 막았다**: 대시보드 stop훅이 경로로 프로젝트를 판별하는데 match_category를 직접 돌려보니 `.tracks/보이스`→스탁브레인 ✓ / `Desktop\lotto-보이스`→**None ✗** = 밖에 뒀으면 트랙 세션마다 대시보드 기록이 조용히 빠졌을 것. 점(.)폴더인 이유=이 폴더가 옵시디언 볼트(.obsidian 확인)고 .md 11,120개·트랙폴더 하나 883MB라 그냥 넣으면 트랙마다 중복노트 1만여개 인덱싱 — 옵시디언은 점 폴더 자동제외. 3중방어(.gitignore /.tracks/ + 도구 _IGNORABLE에 .tracks/ + 점폴더). 이동 시 WIP 보존을 git diff --numstat 일치로 확인(+32/-9·+32/-2·+36/-0·+23/-2, 데스크톱 잔재 0). **★폴더 규칙 확정**: 트랙 폴더엔 git 추적물(CLAUDE.md·.claude/settings.json·.agents/skills·tools·wiki·handoff)이 전부 따라와 규칙·스킬·훅이 그대로 도는 똑같은 세션이 된다. 하지만 **안 따라오는 것**=raw/(크롤봇이 main에만 씀, 트랙 13,504 vs main 14,572=**1,068개 뒤처짐**)·shopping_shorts/data(빈 DB)·.obsidian(볼트는 main뿐) → **코드=트랙 폴더 / 위키·인제스트=main 폴더**. 안 적어두면 다음 세션이 1,068개 뒤처진 raw/로 틀린 분석을 낸다 → CLAUDE.md에 표로 박음. 테스트 3건 추가(폴더가 프로젝트 안인지/트랙 폴더가 main의 미커밋 코드로 안 잡히는지/stage도 .tracks 안인지), 64 green. ⚠️위치를 안으로 옮겨도 DB는 연결 안 됨(worktree는 어디 있든 독립 체크아웃, data/는 gitignore) — 별건.
