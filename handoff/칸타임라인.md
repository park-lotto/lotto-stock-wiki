# 칸타임라인 — 자막·TTS·컷 한 시간축 (캡컷 대체)

## 2026-08-29 (회사, 밤샘 자율) — T1~T6 구현·실측 완료

설계: `docs/superpowers/specs/2026-08-29-칸타임라인-design.md` (사장님 A안 확정)
계획: `docs/superpowers/plans/2026-08-29-칸타임라인.md`

### 된 것 (전부 로컬 실job 030d90a7a97a로 브라우저 실측)
- **T1 타임라인 뷰**: 선택 칸 머리 아래 3레인(컷·자막·음성+재생선). `scene_timeline.js`
  신규(계산은 전부 기존 함수 — planClips·beatFill·capsOf·curT). 재생선이 curT()를 따라
  움직이고 지금 읽는 구절이 켜짐. 컷 폭 합=기대값 일치, 리드인 0.424s 위치 반영.
- **T2 컷 카드↔필름식 펼침**: 컷 클릭 → 같은 폭 안에 실프레임(캡컷식). `filmroll.js`에
  `filmframes()` 헬퍼(구간 [a,b]→n장, 캐시). 실측 4프레임 517ms·재펼침 2ms.
- **T3→T6에서 일원화**: 경계 저장은 **기존** `/api/produce/mix/{job}/caplines`(08-25)
  하나로. ★내가 처음에 중복 라우트를 만들었다가 발견하고 지웠다(아래 함정 참조).
  응답에 새 시간표(captions=_lab_captions)·cap_src 추가.
- **T4 ✂ 경계 편집 UI**: 어절 칩 + 경계(‖/·) 클릭 = 끊기/합치기 → 서버 재계산(재TTS 0원)
  → 화면 연쇄 갱신(⑨). 실측: 6구절→7→6 왕복, 초는 실발화 시각.
- **T5 🔁 고정길이 박스 교체**: 컷의 🔁 → 소스 필름에 길이 잠긴 박스(끌기만 가능, 핸들·
  Esc·Q/W/E 잠금) → 영상 갈아껴도 유지 → [🔁 이 장면으로 교체]=기존 replaceRoll 경로.
  실측: 2.00초 박스 199px 이동에도 폭 유지, 교체 후 새 컷 정확히 2.00초.
  교체 로그 `POST .../swap_log` → job 폴더 swap_log.jsonl (기록만 — 픽 로직 무변경!).
- **T6 cap_src 표면화**: `_beat_words_src()`가 precise/asr/estimate 반환, 3곳+caplines에
  기록, 타임라인 음성 레인에 🎯정밀싱크/👂받아쓰기/≈추정 배지. 테스트 4+2건.

### ⏭ 남은 것
- **T7 싱크 감사 배치(⑦b)**: 최종 음성 기준 구절 전환 오차를 job마다 실측·기록 — 독립
  태스크라 미착수. "몇 %가 실제로 어긋나는지"는 이걸 돌려야 숫자가 나온다.
- 사장님 육안 확인 + 실사용 피드백(레인 높이·배율 등 취향 조정).
- 2단계 후보: 타임라인 위 직접 트리밍 / 전체(칸 이어붙임) 타임라인 / 구절 클릭 시크.

### 함정 (다음 세션 필독)
- ★**경계 저장 API는 caplines 하나다** — `/api/mix/scene_lab/.../caption_lines`를 다시
  만들지 마라(내가 만들었다 지웠다). produce.html 자막수정재배분 트랙도 같은 API를 쓴다.
- 로컬 검증법: 서버에서 job 폴더 tar(final/preview 제외)+mix_jobs DB행 json으로 복사,
  경로를 로컬로 치환(`scratchpad/fix_paths.py` 참고), `DASH_PASS= uvicorn ... --port 8852`.
  tts_path가 서버 절대경로라 안 바꾸면 tts_dur이 전부 None으로 온다.
- 타임라인 좌표는 off(스크롤) 개념 자체가 없다 — 칸 폭 고정 pps, 자식 절대좌표.
  filmroll off 이중차감 사고(08-29)의 재발 방지 규약.
- planClips는 모자람을 마지막 컷에 흡수한다 — 타임라인은 그 늘어난 꼬리를 빗금으로
  표시한다(보정 후 합계만 보여주면 정보 0 — ★보정후합계 메모리).
