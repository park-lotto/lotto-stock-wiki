# 재발버그 근절 — 핸드오프

> **소유 트랙**: 재발버그근절 — 이 파일은 이 트랙 세션만 수정한다.
> 지시서: `handoff/OPUS_지시서_재발버그.md`(페이블 작성) — A~G 7항목

## 2026-08-19 — A·B·D·E·F1·F2·G 전부 처리

사장님: "매일 버그·한번 고친게 계속 손대는 이유를 모르겠어" → 페이블이 총점검·지시서,
오푸스가 실행. **핵심 원칙: 고치는 게 아니라 '못 나게' 만든다** — 지시서 진단이
"고치기만 하고 가드를 안 둬서 매번 되살아났다"였으므로 **전 항목에 가드 테스트**를 넣었다.

### ✅ A. node -e 명령줄 상한 (재발 3회 → 차단)

- ★**지시서는 6파일이라 했으나 실측 11파일 17곳**(그쪽 grep이 좁았다).
- `tests/js_harness.py` 신설 — 구현은 `test_sidebar_works._run`(검증된 것)을 그대로 옮김.
  `run_js()` + `run_js_proc()`(CompletedProcess 반환 어댑터 — 기존 호출부 무수정 이관용).
- 12파일 이관. 단언·결과처리는 손대지 않고 **호출 형태만** 교체.
- `test_no_node_dash_e.py` — tests/ 전체를 훑어 `node -e`가 있으면 실패. **4회째 차단.**

### ✅ G. 예열 영구래치 (라이브 9건)

- `prewarm._is_transient()` — 5xx·429·타임아웃만 일시로. **애매하면 영구**(크레딧 보호).
- 다운로드·추출 두 경로에서 일시면 `autoload_rollback_attempt`(횟수 반환).
- `scripts/prewarm_unlatch.py` — 이미 물린 것 구제. **기본은 미리보기**, `--apply`로 적용.
  - 라이브 dry-run: 9건 중 **3건만** 해제 대상(6건은 영구실패 2 + 이미 대본 있음 4).
  - ⏭ **아직 --apply 안 했다** — 배포 후 서버에서 한 번 돌려야 한다(아래 "남은 일").

### ✅ B. 대본생성 성공경로 스모크

- `test_generate_success_path.py` 5개. AI만 mock, 재료조립·장면블록은 **진짜 코드**가 돈다.
- ★**가짜 green 두 번 방지**:
  ① mock 반환이 **리스트**여야 한다(dict면 502로 빠져 성공경로를 못 달린다) — app.py에서 계약 확인.
  ② 08-16 버그(source_brief dict에 `.strip()`)를 **일부러 되살리니 이 테스트가 실패**했다.
     = 그때 있었으면 사장님이 6번 클릭할 일이 없었다.

### ✅ E. TDZ 화면 공백

- ★**지시서가 놓친 사실**: `test_ranking_render_tdz.py`가 이미 있고 잘 돈다(08-18 사고 직후 생성,
  실제로 render()를 **실행**까지 한다). 새로 안 만들고 그대로 뒀다.
- 진짜 구멍은 **produce.html**(30일 250커밋, 최상위 함수 349개)이 무방비였던 것.
  → `test_produce_tdz_scan.py` — 두 파일 **전 함수** 정적 스캔.
- ★오탐을 실측으로 3단계 제거(가드가 시끄러우면 아무도 안 본다):
  객체 키 `{size:1}` / 속성 접근 `L.size` / **콜백 파라미터** `.map((L,i)=>`(다중행이라 줄 단위
  `=>` 검사로는 못 걸렀다) → **최종 오탐 0건**, 08-18 실제 사고 코드는 정확히 검출.

### ✅ D. 씨앗 캐리오버

- ★**지시서 예상과 달리 이미 통합돼 있었다** — 복원 경로 3곳(3785·3824·4349)이
  **전부 `_s2Hydrate` 하나만** 부른다. 소유권 검사(work_id)·"없으면 비운다"도 구현됨.
- 진짜 구멍은 **가드가 없던 것**(08-18에 같은 날 두 번 고쳤는데 테스트 0개).
  → `test_produce_s2_persist.py`에 5개 추가: 남의 스냅샷 거부 / seed 함께 비우기 /
    내 작업은 정상 복원 / 옛 스냅샷(work_id 없음) 복원 / 복원 경로 단일성.
- 검증: 08-18 버그(seed 미삭제)를 되살리니 **2개가 실패** → 원복하니 통과.

### ✅ F-1. 조용한 실패 상한

- ★**실측 117개**(지시서 226은 `except` 절 전체를 센 값 — 실제 `except: pass`는 117).
- `test_silent_except_budget.py` — 파일별 현재치를 천장으로 **늘어나는 것만** 차단.
  줄었는데 상한이 그대로여도 실패시킨다(되돌아감 방지).
- 가드 실효 확인: 일부러 1개 추가 → 실패, 원복 → 통과.

### ✅ F-2. 선별 사유로깅

- **일괄 수정 안 했다**(지시서 지시대로). 사고 경로만 골라 봤다:
  - `mix_pipeline`의 `ops_alert` 실패 2곳 → 사유 로깅 추가.
    알림이 조용히 죽으면 "사고 났는데 아무도 모른다"가 되는데, 그게 이 알림을 만든 이유였다.
  - `store.py`는 **손댈 것이 없었다** — 7곳 중 5곳이 `_init_schema`(멱등 마이그레이션,
    삼키는 게 의도), 2곳은 `ValueError`/`TypeError` 좁은 예외에 폴백까지 있다. 정상.

## 검증

```
신규 테스트  36개(A 2·G 17·B 5·E 5·D 5 + F1 2)   ※ 이관 12파일 74개 별도 통과
전체         4,767 passed / 1 failed
             그 1건 test_edit_plan_build::test_build_edit_plan_exhausted_returns_empty 는
             변경 전 main에서도 실패한다(지시서에도 명시된 기준선)
```

## ⏭ 남은 일

```
□ ★서버에서 래치 해제 1회 실행(배포 반영 후):
    cd /home/ubuntu/lotto-stock-wiki && python3 -m scripts.prewarm_unlatch          # 미리보기
    cd /home/ubuntu/lotto-stock-wiki && python3 -m scripts.prewarm_unlatch --apply  # 적용
  → 3건이 다음 예열부터 재시도된다(나머지 6건은 그대로 두는 게 맞다).
□ test_edit_plan_build 기준선 1건 — B가 같은 영역이라 되살릴지 판단(이번엔 손대지 않았다).
□ F-2 확대는 서두르지 마라 — 117개 중 대부분이 의도된 것이다. 사고가 난 경로에서만 늘려라.
```

## 배운 것(다음 세션이 밟지 말 것)

- **지시서·핸드오프의 수치도 검증하라.** 이번에 3개가 틀렸다(node -e 6→11파일,
  except:pass 226→117, E는 이미 가드 존재, D는 이미 통합 완료). 실측이 먼저다.
- **가드는 "실제로 잡는지" 확인해야 가드다.** 이번엔 A·B·D·F1 넷 다 옛 버그를 일부러
  주입해 실패를 확인하고 원복했다. 안 하면 통과가 무의미하다.
- **오탐 0을 만들 때까지 다듬어라.** 시끄러운 가드는 곧 무시되고, 무시되면 없는 것과 같다.

## 2026-08-19 (2) — 남은 일 2건 마감

### ✅ 서버 예열 래치 — 풀 대상 0건, 아무것도 바꾸지 않았다

라이브 dry-run 실측: 래치 6건 중 **풀 대상 0건**. 영구실패 2(도우인 yt-dlp 실패·
음성/자막 둘 다 없음) + 이미 대본 있음 4. **`--apply` 불필요.**
전날 기록의 "9건 중 3건 해제 대상"은 그 사이 자연 해소된 것으로 보이나
**어떤 경로로 풀렸는지는 확인하지 않았다**(추정).

### ✅ test_edit_plan_build 기준선 1건 — 낡은 테스트였다. 고쳤다

★진짜 정체: **이 테스트가 실제 Gemini API를 호출하고 있었다.**
`build_edit_plan`은 2026-07-13에 comment_gen 전용키 → `_vault_call`(key_vault
캐스케이드)로 옮겼는데, 이 테스트만 **옛 방식(comment_gen 전용키)만 막아** 소진
흉내가 통하지 않았다. 그래서 라이브 키로 진짜 호출이 나가 beats가 채워졌다.
같은 파일의 `_fake_gemini`는 이미 `_vault_call`을 패치하고 있었다(line 32) —
**같은 파일 안에서 두 방식이 공존**한 것이 놓친 이유다.

수리: 소진 흉내를 `_vault_call` 한 곳으로 통일(0순위-B).
- 검증: 8 passed (종전 1 failed). 실행시간 **7.6초 → 1.4초** = 실호출이 사라진 증거.
- `_normalize_video_type("product_reveal") == "generic"` 실측 확인 — 기대값 자체는 옳았다.

### ⚠️ 발견했으나 손대지 않은 것 — 다음 세션이 판단하라

**테스트가 라이브 API 키를 소모할 수 있다.** `conftest.py`의 autouse는 DNS만
가짜로 바꿀 뿐(`offline_dns`) 실제 HTTP를 막지 않는다 — 위 사고가 그 증거다.
`_current_key_and_idx`만 패치하고 `_vault_call`은 안 막은 테스트 파일이 13개 있다
(grep 기준). **다만 그 13개가 실제로 API를 때리는지는 확인하지 않았다** — 그 모듈들이
`_vault_call`을 안 쓰면 정상이다. 오탐 가능성이 높으니 **일괄 수정 금지**(F-2 교훈).
전역 네트워크 차단 픽스처는 효과가 크지만 대량 테스트를 깨뜨릴 수 있어 사장님 판단 필요.

## ⏭ 남은 일 (갱신)

```
□ (선택) 테스트 전역 네트워크 차단 픽스처 도입 여부 판단 — 위 ⚠ 참조
□ A~G 7항목·기준선 1건은 전부 마감. 이 트랙은 접어도 된다.
```
