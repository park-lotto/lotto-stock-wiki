
---

## 2026-09-05 저녁 — "완성본 만들어도 자막제거·꾸미기에 반영 안 됨"의 진짜 뿌리 (라이브 실측·배포 완료)

사장님 제보: "자막제거 후 마음에 안 들어 3단계에서 한 장면 수정 → 완성본 만들기 → 다시
자막제거·장면꾸미기 가도 적용이 안 됨" (박세현 등 고객도 동일)

### 라이브 재현 (job fb62adf0aad0, 관리자 계정, 실제 렌더까지 돌림)
| 시점 | 실측 |
|---|---|
| 장면편집 후·렌더 전 | 자막제거 AFTER 검은 칸, `clean_clips` = stale·컷 0 |
| 최종 렌더 완료(15:33) | `final_clean_a873ba2b….mp4` 생성, 화면 정상 복구(stale=false·컷 20) |
| **페이지를 다시 열자** | 다시 stale — hook 재료가 `[film_s1_0.74_1.67, lens_…-4]` → `[lens_…-4]`로 줄고 서명 a873ba…→2716cd… |

서버 로그의 자동 apply POST 시각(15:35:46)이 `edit_plan` updated_at과 정확히 일치 →
**화면 자동저장(autoApply, 1.2초 디바운스)이 조각을 지운다**가 확정.

### 뿌리
09-05 낮 수정(`b35b85b45`)은 검증 통과한 `extra_segs`를 `plan["scene_lab"]`에 저장까지 했지만
**다시 읽는 곳이 없었다.** `apply_scene_lab`의 병합 재료가 클라 payload 하나뿐이라, 화면이
EXTRA를 못 채운 채 저장하면 `film_` id가 seg_map에 없어 걸러지고 scene_override에서 영구 소멸.
→ 편성 서명이 바뀌어 **방금 만든 청소본이 그 자리에서 낡은 것**이 된다.

### 고친 것 (`edit_plan.apply_scene_lab`, main 병합 `786c39bb1`)
저장된 `plan["scene_lab"]["extra_segs"]`를 먼저 깔고 클라 payload를 위에 얹는다
(같은 id는 클라 우선 — 사람이 고친 구간·이름 반영). 저장본은 이 함수가 이미 검증한 값.

### 검증
- 테스트: 자동저장에도 조각 유지 / 클라 값 우선 2건 추가 → `test_scene_lab_film_seg_persist` 7 passed
- 사보타주 1회로 해당 테스트만 빨개짐(빈 단언 아님) · scene_lab·edit_plan·clean 계열 312 passed
- **라이브 실측(배포 후 16:06)**: scene_lab을 실제로 열어 자동저장(`scene_lab_at` 07:06:33Z)이
  돈 뒤에도 `film_s1_0.74_1.67`이 편성·segments에 유지(구간·라벨까지). 화면에 검은 칸 없음.
- 배포: 자동배포가 "고객 접속 중"으로 웹 재시작을 계속 연기 → 사장님 지시로 강제 재시작(16:04:32).
  ⚠️ `DEPLOY_NOW` 플래그 경로는 `/home/ubuntu/DEPLOY_NOW`(repo 안이 아니다). 그리고 그 플래그는
  **pull 시간창**용이라 "웹 재시작 연기"는 못 푼다 — 재시작은 `sudo systemctl restart shopping-shorts`.

### ⏭ 남은 것
1. **`clean_video_path`가 재청소해도 갱신되지 않는다**(별건, 미수정). 실측 job `1a1d5f143140`:
   청소본이 11:06→11:17→11:38로 세 번 바뀌는 동안 경로는 11:06 파일 고정. 이 값을 쓰는 화면
   (9단계 썸네일 배경 `/api/produce/thumb/frames` 등)은 계속 옛 청소본을 쓴다.
2. 화면 쪽 EXTRA 복원(서버 편성으로 열릴 때 `hydrateExtra` 미호출 경로)은 그대로 두었다 —
   서버가 안전망을 갖게 됐으므로 급하지 않다. 다만 근본적으론 화면도 채우는 게 맞다.
