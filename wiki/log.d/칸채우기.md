# 칸채우기

## 2026-08-16
- 장면 편집에 **✨채우기**(그 칸 대사를 AI가 읽고 화면 선택) · **🗑고른 것 빼기**(체크 다중 삭제) · **칸 아래 대본 표시** 라이브. 채우기 엔진은 새로 안 짓고 `_repick_weak_beats`(대사↔화면 재선정)와 같은 원리로 `fill_beat_scenes`를 붙임.
- 버그 3건 근본수정: 한 장면 몫을 `min(effLen, MAX_SHOT)`로(한 컷만 차던 것) / 비운 칸은 칸 역할로 결 결정(엉뚱한 것 고르던 것) / 남은 부족분 < `MIN_CLIP`이면 그만 담기(담았는데 안 나오던 것).
- ★중복 판정을 **phash → 설명 겹침**으로 통일. 실측(job 8873eeb48a08, 465쌍) 같은 장면 쌍이 거리 11인데 거리 13에 전혀 다른 쌍이 있어 해시 문턱을 못 올린다. 채우기·AI 결과·최초 배치(`_fill_beat_screen_time`) 세 곳이 같은 원리를 쓴다.
- 검증 경로 확보: `/scene_lab.html?job=<잡ID>` 단독 열기 + 스냅샷·복구로 사장님 편성 안 건드리고 실측.
