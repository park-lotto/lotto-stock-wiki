# 보이스 — 작업 로그

- 2026-07-16 — [리뷰·병합·배포] **T6(억양) 완결 — opus 재리뷰 통과(Critical0/Important0) → 병합 `096ab513` → 라이브**. 리뷰어가 내가 못 한 검증까지 함: `_STAGES` 튜플 교체로 제대로 주입 / `_EMPHASIS_WORDS` 6개 경계 스윕해 **과소발동 없음** / 자매결함 **구조적 불가**(`_emotion_arc` 뒤에 도는 스테이지는 `_intonation` 하나뿐) / 구현자 주석 두 논증 반증 시도했으나 둘 다 참. **Minor1=내가 심은 죽은 프로브의 씨앗**(`88aca807`): 리뷰어 보고도 안 믿고 재현했는데 **내 프로브가 먼저 틀렸음**(프로파일 키를 `{"stages":{...}}`로 넣어 오버라이드가 통째 무시 — `arc=False`인데 태그가 나와서 발각). 올바른 키로 재현하니 리뷰어가 맞음: `_p`가 결함을 가리는 진짜 이유는 fillers가 아니라 **`emotion_arc`를 끄기 때문** / **`beat_index=1`이 테스트의 생명줄**(bi=0이면 추임새 `음, `이 태그와 강조어 사이에 끼어 lookbehind 앞글자가 `]`가 아니라 `,`가 되어 **버그판으로도 유령이 안 생김**=조용히 죽은 프로브). ★★**오늘 하루에 "통과하는데 결함 못 잡는" 패턴을 4번 목격**: ①`절대값이 커요` 프로브 ②내 docstring의 인과 반전 ③내 검증 프로브의 잘못된 키 ④**내 배포 감지기**(grep `…\]`가 주석의 `(?<=[^\s,.!?…])`에 매칭돼 "배포 반영됨" 오보 — 서버는 버그판 그대로였음). 이 저장소의 1번 실패 유형이고 **검증 도구 자체도 예외가 아니다** — 감지기를 정규식 정의만 파싱하게 고치고 **버그판 서버에서 `BUGGY`를 뱉는지 변별력부터 확인**한 뒤 사용. ⚠️**게이트 경합(트랙격리 트랙 이슈)**: 1차 finish가 게이트 3번 다 통과했는데 매번 옆 트랙이 먼저 main에 들어와 **재시도 3회 소진→중단**. 안전장치 작동이지 실패는 아니나(라이브 무사) **게이트가 느릴수록 경합 창이 넓어지는데 재시도는 3회 고정** — 재시도 증량 또는 게이트 가속(883MB 체크아웃 재사용) 필요.

- 2026-07-16 — [수정·검증] **T6(억양) 재리뷰 4건 회수·커밋**(`ca92f9c8`, `.tracks/보이스`에서 작업 → `track/보이스`로 커밋, 흡수0). **어제 커밋한 억양(`df82027c`)이 기본 프로파일에서 오작동하던 것**: `_emotion_arc`가 먼저 붙인 `[curious] ` 태그의 **닫는 대괄호를 lookbehind가 "앞에 실질 단어 있음"으로 오인** → `[curious], 진짜 대박이에요`처럼 앞에 아무 단어도 없는 자리에 유령 쉼표를 넣고 `applied`에 "강조 포즈 1건"으로 **거짓 계상**. 첫 비트는 추임새 `음, `이 방패라 안 보이고 2번째 비트부터 노출(강조어로 시작하는 훅/CTA는 쇼핑대본에 흔함). 수정=lookbehind에 `\]` 추가 + 순서 봉인 + 죽은 프로브 살림 + `_bump`를 실측 계수로. **뮤테이션 3종 직접 실행**: `\]`제거→유령포즈 테스트만 죽고 실패메시지가 결함 자체를 재현 / `cands[:take]`→`[-take:]`→봉인 단언만 죽음 / **`_bump(n)`→`take`는 살아남음** — 죽은 테스트가 아니라 구조상 `n==take`(삽입은 항상 문자열을 바꿈)라 **증명 대상이 아닌 행동 no-op**임을 확인(정직하게 남김). 회귀 113 passed, 최신 main 11커밋 얹은 뒤에도 동일. ★교훈 재확인: 이번에 고친 죽은 프로브(`절대값이 커요`는 맨 앞이라 **버그 원안 정규식으로도 매칭 0**)가 "통과하는데 결함 못잡는 테스트"의 또 한 사례.
- 2026-07-15 — [리뷰·수정·배포] **자연화엔진 whole-branch 최종리뷰 → seam 10건 전건 수정**(`13eaf79c`). 근본원인=튜닝 작업대와 실렌더가 **각자 합성 파이프라인을 조립**해 인자가 갈림: 동결 프로파일이 렌더에 0%반영(S1·조용한 실패) / 재기동마다 프로파일 NULL 증발(S2, startup seed가 excluded로 덮음→COALESCE) / 작업대가 기본성우 Rachel로 합성(S3) / 렌더에 N-best ranker 미배선=크레딧만 n배·take0 고정(S4) / 작업대 speed·후처리 누락(S5) / beat_index 불일치(S6) / **튜닝 API 공개도메인 무인증 노출**(S7) / 미리듣기 v2 vs 렌더 v3(S8) / 재롤 캐시히트(S9) / seed·n_best를 merge 전 raw dict에서 읽음(S10). 해법=공유 단일경로 `mix_pipeline.synthesize_line()`. **회귀테스트 3종 신규** — preset→job스냅샷→렌더를 통과하는 테스트가 없어 S1이 유닛리뷰를 통과했음(그 seam을 잠금). 469통과·신규실패0, 실서버DB grounding으로 S1/S2/S2b/S7 FIXED 확인. 교훈: whole-branch 리뷰 생략했으면 사장님이 귀로 맞춘 값이 영상에 하나도 반영 안 됐을 것([[feedback_whole_branch_review_catches_seams]]).
- 2026-07-15 — [수정] **eleven_v3 연속성 400**(`7e053a18`). 핸드오프의 "미지원이면 payload 무시라 안전" 가정이 **틀림** — 실측 프로브: text+model 200 / +voice_settings 200 / +seed 200 / **+previous_text 400**(`not yet supported with the 'eleven_v3' model`). tts.py가 v3에서 자동 drop(speaker_boost와 같은 자리)+회귀테스트. 작업대 ▶합성 전면실패의 직접원인이었고, **렌더 경로도 진작 같은 400으로 죽고 있었을 것**. GROQ_API_KEY 서버 추가·실동작 확인(서비스 프로세스 environ까지 확인 — 내 셸 env로만 테스트하면 서비스가 키를 갖는지 증명 안 됨).
- 2026-07-15 — [결함발견] **강도 슬라이더가 실제로 안 먹음**(사장님 관측→재현). intensity 0.3/0.7/1.0 출력이 완전 동일, 0에서만 off = on/off 스위치. 원인: 추임새·감정곡선=`<0.15` 임계형+cap 1 / 끝음=마침표를 …로 바꾸는데 **코퍼스 10줄에 마침표 0개** → 영원히 무발동 / 구어체=`int(hits×intensity)`라 짧은 문장은 1.0에서만 발동 / 끊어읽기=연결어미 없는 문장엔 무효 / 발음사전 비어있음 / **억양은 미구현**(`return text`). 8개 중 실동작 3개뿐. **유닛테스트가 통과한 이유=각 스테이지를 자기한테 맞는 입력으로 격리 테스트, 진짜 코퍼스로 통과시킨 적 없음** — 배선이 아닌 "내용의 seam". 핸드오프의 "규칙 정교화는 코드수정 최소" 가정도 틀림. 사장님 승인=제대로 재설계(비례형 강도·코퍼스 현실화·억양 구현). 상세=handoff/보이스.md

## 2026-07-17 속삭임 톤 — SDD 6태스크 완료·main 병합
- 사장님 청취 판정(성우 14명 전건) → 베스트 5명만 whisper, 탈락 3명(한나·주하·지아나)은 `[whispers]` 미작동. 프리셋 47개.
- 엔진 `_whisper` 스테이지(노브 하나 `roles`: 전체=ASMR / `["반전"]`=강조) + `best` 플래그 하나가 순서·⭐ 동시 담당 + 작업대 role 노브.
- ⏭ **소리는 아직 아무도 못 들음** = 잔여 작업 전부. 로컬 불가(ElevenLabs 키 0개, 키 없으면 무음 mp3를 조용히 씀) → 라이브에서 확인.
- 🔑 교훈: `git worktree` 기준선은 `.env`가 없어 오염된다 — 없는 회귀를 쫓을 뻔했다.

## 2026-07-17 (이어서) 성우별 말 속도 + 숫자 읽기 — 라이브 반영(58c3223b)
- 사장님 "릴스 속도는 이것보다 빨라야" → 잣대(음절÷발화초)로 실측: 레퍼런스 6.95 vs 우리 4.50 = 65%. 사장님 귀가 맞았다.
- 숫자로는 speed 2.0이 레퍼런스지만 **사장님이 들어보고 1.5로 확정** — 2.0은 ffmpeg가 시간축 1.67배 늘린 소리다. 숫자가 아니라 귀가 게이트.
- 사장님 "목소리별로 기본속도 다른거 아닌가" → **데이터는 정반대**였다: 설정은 47개 전부 1.15로 통일인데 실제 말속도는 성우마다 달랐다. → 성우별로 각자 재서 12가지 값으로 세팅, 14명 전원 5.57~5.95(1.07배)로 모임.
- 숫자 읽기 결함(사장님 스크린샷): 천 단위 쉼표 미인식 + _int_to_sino가 0~9999만 지원 → 만·억·조 확장. 19,900원 → "만 구천구백원".
- ⏭ 다음: 속삭임을 **영상별로** 고르기(성우별은 이미 작업대 role 체크박스로 됨). 설계 §6이 미뤄둔 "새 저장층"이 이제 필요.
