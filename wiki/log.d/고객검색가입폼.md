## 2026-08-24 고객 찾기 퍼지 검색 + 가입 이름·전화

- 고객 찾기: `indexOf` 한 방 → 글자 순서만 맞으면 걸리는 퍼지 매칭(`the`/`tbk`로 이메일 찾음).
  칸을 가로지르지 않게 필드 단위로만 잇는다(안 그러면 무관한 고객이 섞임).
- 가입 이름·전화: 진짜 원인은 폼이 아니라 **구글 로그인이 email만 받는 것**(179명 빈칸).
  `/welcome` 신설 + `welcome_due` 컬럼(기본 0 → 기존 고객 미차단).
- 테스트 6건 추가, 전체 5,177 passed(남은 실패 2건은 기존). 라이브 배포.

## 2026-08-24 (2차) 성별·연령대 + 기존고객 백필

- 이름·전화·성별·연령대 4종 필수. 기존 179명도 **다음 접속 때** 한 번 받는다
  (판정을 플래그 → '실제로 비었는가'로 바꿈. 이미 있는 값은 미리 채워 보여준다).
- ★마무리 게이트는 **접근권한 판정 뒤**에 둬야 한다 — 앞에 뒀더니 303이
  관리자·유료 게이트를 앞질러 무관한 테스트 36건이 깨졌다.
- ★화면 이동(Sec-Fetch-Mode: navigate)만 붙잡는다. API를 막으면 열린 화면이 깨진다.
- 관리자표에 성별·연령 열 + 검색 포함. 전체 5,180 passed. 라이브.
