# VMake 자막제거 연동 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 믹스된 최종 영상의 원본 하드섭(박힌 자막)을 VMake API로 제거한 뒤 우리 자막을 굽는 단계를, 켜고 끌 수 있는 옵션으로 믹스 파이프라인에 추가한다.

**Architecture:** 렌더를 `믹스(자막X) → [옵션]VMake제거 → 우리자막` 3단계로 분리. VMake API 호출은 `vmake_client.py` 어댑터 한 파일에 격리(실제 엔드포인트·서명·폴링 스펙은 내일 확보, 그때까지 추정 스펙+mock 테스트). 개인 API 키는 DB settings 테이블에 저장. 처리 5~15분은 기존 백그라운드 job + 상태 폴링 패턴으로 흡수.

**Tech Stack:** Python, FastAPI, SQLite(store), ffmpeg(video_assemble), requests(VMake REST), pytest.

---

## File Structure

- **Create** `shopping_shorts/vmake_client.py` — VMake API 어댑터. `remove_subtitles(video_path, api_key, poll_timeout=1200) -> clean_path`. 서명 인증·업로드·job 폴링·결과 다운로드 캡슐화. 스펙 불확실 부분을 여기 가둠.
- **Create** `shopping_shorts/tests/test_vmake_client.py` — 서명 생성·폴링·실패 처리 (requests mock).
- **Modify** `shopping_shorts/store.py` — settings 테이블+헬퍼, mix_jobs에 `subtitle_removal`·`clean_video_path` 컬럼+마이그레이션, create/get/update_mix_job 반영.
- **Modify** `shopping_shorts/tests/test_video_assemble.py` — `_render_mix`/`_burn_captions` 분리 테스트.
- **Modify** `shopping_shorts/video_assemble.py` — `assemble`을 `_render_mix`+`_burn_captions`로 분리, `assemble`은 조립 + VMake 훅.
- **Modify** `shopping_shorts/mix_pipeline.py` — `run_render`에 자막제거 단계+`removing_subtitles` 상태.
- **Create** `shopping_shorts/tests/test_mix_render_flow.py` — 렌더 흐름 ON/OFF 분기 (VMake·ffmpeg mock).
- **Modify** `shopping_shorts/app.py` — `/api/settings/vmake_key` 저장·조회, `/api/mix/start`에 `subtitle_removal` 플래그.
- **Modify** `shopping_shorts/static/*.html` (믹스 UI) — 개인키 입력란 + 자막제거 토글.

---

## Task 1: store — settings 테이블 + 헬퍼

**Files:**
- Modify: `shopping_shorts/store.py` (스키마 `_init_schema`, 새 메서드)
- Test: `shopping_shorts/tests/test_store_settings.py` (신규)

- [ ] **Step 1: 실패 테스트 작성**

Create `shopping_shorts/tests/test_store_settings.py`:
```python
from shopping_shorts.store import Store


def test_set_and_get_setting(tmp_path):
    s = Store(str(tmp_path / "t.db"))
    assert s.get_setting("vmake_api_key") is None      # 기본 None
    s.set_setting("vmake_api_key", "abc123")
    assert s.get_setting("vmake_api_key") == "abc123"


def test_set_setting_overwrites(tmp_path):
    s = Store(str(tmp_path / "t.db"))
    s.set_setting("vmake_api_key", "first")
    s.set_setting("vmake_api_key", "second")
    assert s.get_setting("vmake_api_key") == "second"


def test_get_setting_default(tmp_path):
    s = Store(str(tmp_path / "t.db"))
    assert s.get_setting("missing", default="fallback") == "fallback"
```

- [ ] **Step 2: 실패 확인**

Run: `python -m pytest shopping_shorts/tests/test_store_settings.py -q`
Expected: FAIL — `AttributeError: 'Store' object has no attribute 'set_setting'`

- [ ] **Step 3: 스키마에 settings 테이블 추가**

In `shopping_shorts/store.py` `_init_schema`, mix_jobs `CREATE TABLE` 블록 바로 다음에 추가:
```python
            c.execute("""
                CREATE TABLE IF NOT EXISTS settings (
                    key TEXT PRIMARY KEY,
                    value TEXT
                )
            """)
```

- [ ] **Step 4: get_setting/set_setting 메서드 추가**

`update_mix_job` 메서드 바로 뒤에 추가:
```python
    def get_setting(self, key, default=None):
        """전역 설정값 조회(예: vmake_api_key). 없으면 default."""
        with self._conn() as c:
            row = c.execute("SELECT value FROM settings WHERE key=?", (key,)).fetchone()
        return row[0] if row else default

    def set_setting(self, key, value):
        """전역 설정값 저장(upsert)."""
        with self._conn() as c:
            c.execute(
                "INSERT INTO settings(key, value) VALUES(?,?) "
                "ON CONFLICT(key) DO UPDATE SET value=excluded.value",
                (key, value),
            )
```

- [ ] **Step 5: 통과 확인**

Run: `python -m pytest shopping_shorts/tests/test_store_settings.py -q`
Expected: PASS (3 passed)

- [ ] **Step 6: 커밋**

```bash
git add shopping_shorts/store.py shopping_shorts/tests/test_store_settings.py
git commit -m "feat(shopping_shorts): store settings 테이블 + get/set_setting (vmake 키 저장용)"
```

---

## Task 2: store — mix_jobs 자막제거 필드 + 마이그레이션

**Files:**
- Modify: `shopping_shorts/store.py` (스키마, 마이그레이션, create/get/update_mix_job)
- Test: `shopping_shorts/tests/test_store_settings.py` (mix_job 필드 테스트 추가)

- [ ] **Step 1: 실패 테스트 작성**

Append to `shopping_shorts/tests/test_store_settings.py`:
```python
def test_mix_job_subtitle_removal_default(tmp_path):
    s = Store(str(tmp_path / "t.db"))
    s.create_mix_job("j1", ["u"], 30, "template")
    job = s.get_mix_job("j1")
    assert job["subtitle_removal"] is False       # 기본 꺼짐
    assert job["clean_video_path"] is None


def test_mix_job_subtitle_removal_true(tmp_path):
    s = Store(str(tmp_path / "t.db"))
    s.create_mix_job("j2", ["u"], 30, "template", subtitle_removal=True)
    assert s.get_mix_job("j2")["subtitle_removal"] is True


def test_update_mix_job_clean_path(tmp_path):
    s = Store(str(tmp_path / "t.db"))
    s.create_mix_job("j3", ["u"], 30, "template")
    s.update_mix_job("j3", clean_video_path="/x/clean.mp4")
    assert s.get_mix_job("j3")["clean_video_path"] == "/x/clean.mp4"
```

- [ ] **Step 2: 실패 확인**

Run: `python -m pytest shopping_shorts/tests/test_store_settings.py -q`
Expected: FAIL — `create_mix_job() got an unexpected keyword argument 'subtitle_removal'` 및 KeyError

- [ ] **Step 3: 스키마 컬럼 추가 + 기존 DB 마이그레이션**

In `_init_schema`, mix_jobs `CREATE TABLE`의 `updated_at TEXT NOT NULL` 다음 줄에 컬럼 2개 추가:
```python
                    updated_at TEXT NOT NULL,
                    subtitle_removal INTEGER NOT NULL DEFAULT 0,
                    clean_video_path TEXT
```
그리고 `_init_schema`의 모든 CREATE 실행이 끝난 직후(같은 `with self._conn() as c:` 블록 끝, 함수 반환 전)에 기존 DB용 마이그레이션 추가:
```python
            # 기존 DB(컬럼 없이 생성됨)용 마이그레이션 — 있으면 무시.
            for col, ddl in (
                ("subtitle_removal", "INTEGER NOT NULL DEFAULT 0"),
                ("clean_video_path", "TEXT"),
            ):
                try:
                    c.execute(f"ALTER TABLE mix_jobs ADD COLUMN {col} {ddl}")
                except sqlite3.OperationalError:
                    pass  # 이미 존재
```
파일 상단에 `import sqlite3`가 없으면 추가.

- [ ] **Step 4: create_mix_job에 파라미터 추가**

`create_mix_job` 시그니처와 INSERT를 교체:
```python
    def create_mix_job(self, job_id, urls, target_seconds, structure, subtitle_removal=False):
        """새 믹스 job 생성. 초기 status='downloading'."""
        now = datetime.now(timezone.utc).isoformat()
        with self._conn() as c:
            c.execute(
                "INSERT INTO mix_jobs(job_id, urls_json, target_seconds, structure, "
                "status, created_at, updated_at, subtitle_removal) VALUES(?,?,?,?,?,?,?,?)",
                (job_id, json.dumps(urls, ensure_ascii=False), target_seconds,
                 structure, "downloading", now, now, 1 if subtitle_removal else 0),
            )
```

- [ ] **Step 5: get_mix_job에 필드 추가**

`get_mix_job`의 SELECT에 컬럼 2개, 반환 dict에 2개 추가:
```python
            row = c.execute(
                "SELECT job_id, urls_json, target_seconds, structure, status, error, "
                "extract_json, edit_plan_json, video_path, created_at, updated_at, "
                "subtitle_removal, clean_video_path "
                "FROM mix_jobs WHERE job_id=?", (job_id,),
            ).fetchone()
        if not row:
            return None
        return {
            "job_id": row[0], "urls": json.loads(row[1]), "target_seconds": row[2],
            "structure": row[3], "status": row[4], "error": row[5],
            "extract": json.loads(row[6]) if row[6] else None,
            "edit_plan": json.loads(row[7]) if row[7] else None,
            "video_path": row[8], "created_at": row[9], "updated_at": row[10],
            "subtitle_removal": bool(row[11]), "clean_video_path": row[12],
        }
```

- [ ] **Step 6: update_mix_job 화이트리스트에 clean_video_path 추가**

`update_mix_job`의 첫 화이트리스트 루프를 교체:
```python
        for k in ("status", "error", "video_path", "clean_video_path"):
            if k in fields:
                cols.append(f"{k}=?"); vals.append(fields[k])
```

- [ ] **Step 7: 통과 확인**

Run: `python -m pytest shopping_shorts/tests/test_store_settings.py -q`
Expected: PASS (6 passed)

- [ ] **Step 8: 커밋**

```bash
git add shopping_shorts/store.py shopping_shorts/tests/test_store_settings.py
git commit -m "feat(shopping_shorts): mix_jobs에 subtitle_removal/clean_video_path 필드+마이그레이션"
```

---

## Task 3: vmake_client — 서명 생성

**Files:**
- Create: `shopping_shorts/vmake_client.py`
- Test: `shopping_shorts/tests/test_vmake_client.py`

**주의(스펙 미확정):** 서명 방식은 조사 기반 추정(HMAC-SHA256(app_key+timestamp+nonce)). 내일 실제 문서 받으면 `_sign`만 교체. 이 태스크는 "결정적 서명 함수"라는 계약만 고정한다.

- [ ] **Step 1: 실패 테스트 작성**

Create `shopping_shorts/tests/test_vmake_client.py`:
```python
from shopping_shorts import vmake_client as vc


def test_sign_deterministic():
    # 같은 입력 → 같은 서명(결정적)
    a = vc._sign("appkey", "secret", "1700000000", "nonce123")
    b = vc._sign("appkey", "secret", "1700000000", "nonce123")
    assert a == b
    assert isinstance(a, str) and len(a) >= 16      # 해시 문자열


def test_sign_changes_with_input():
    base = vc._sign("appkey", "secret", "1700000000", "n1")
    assert base != vc._sign("appkey", "secret", "1700000000", "n2")   # nonce
    assert base != vc._sign("appkey", "secret", "1700000001", "n1")   # timestamp
    assert base != vc._sign("appkey", "other", "1700000000", "n1")    # secret


def test_auth_headers_shape():
    h = vc._auth_headers("appkey", "secret", timestamp="1700000000", nonce="n1")
    assert h["X-App-Key"] == "appkey"
    assert h["X-Timestamp"] == "1700000000"
    assert h["X-Nonce"] == "n1"
    assert "X-Sign" in h and h["X-Sign"]
```

- [ ] **Step 2: 실패 확인**

Run: `python -m pytest shopping_shorts/tests/test_vmake_client.py -q`
Expected: FAIL — `ModuleNotFoundError: shopping_shorts.vmake_client`

- [ ] **Step 3: vmake_client 서명부 구현**

Create `shopping_shorts/vmake_client.py`:
```python
"""VMake(vmake.ai) 자막제거 API 어댑터.

이 파일이 VMake API의 불확실한 부분(엔드포인트·서명·폴링 스펙)을 전부 가둔다.
나머지 코드는 remove_subtitles(video_path, api_key) -> clean_path 만 본다.

⚠️ 실제 엔드포인트/서명 알고리즘/폴링 스펙은 개발자 문서(로그인 뒤)로 확정 예정.
현재는 조사 기반 추정값. 문서 확보 시 _API_BASE / _sign / submit·poll만 교체하면 된다.
"""
import hashlib
import hmac

_API_BASE = "https://open.vmake.ai/api/v1"   # ⚠️ 추정 — 문서로 확정


def _sign(app_key, secret, timestamp, nonce):
    """서명 문자열 생성(결정적). ⚠️ 실제 알고리즘은 문서로 확정 — 지금은
    HMAC-SHA256(secret, app_key+timestamp+nonce)로 가정."""
    msg = f"{app_key}{timestamp}{nonce}".encode()
    return hmac.new(secret.encode(), msg, hashlib.sha256).hexdigest()


def _auth_headers(app_key, secret, timestamp, nonce):
    """VMake 서명 인증 헤더 4종."""
    return {
        "X-App-Key": app_key,
        "X-Timestamp": timestamp,
        "X-Nonce": nonce,
        "X-Sign": _sign(app_key, secret, timestamp, nonce),
    }
```

- [ ] **Step 4: 통과 확인**

Run: `python -m pytest shopping_shorts/tests/test_vmake_client.py -q`
Expected: PASS (3 passed)

- [ ] **Step 5: 커밋**

```bash
git add shopping_shorts/vmake_client.py shopping_shorts/tests/test_vmake_client.py
git commit -m "feat(shopping_shorts): vmake_client 서명 인증(추정 스펙, 문서로 확정 예정)"
```

---

## Task 4: vmake_client — remove_subtitles (제출→폴링→다운로드, requests mock)

**Files:**
- Modify: `shopping_shorts/vmake_client.py`
- Test: `shopping_shorts/tests/test_vmake_client.py`

**설계:** `remove_subtitles`는 3개 내부 스텝(`_submit`→job_id, `_poll`→result_url, `_download`→파일)을 호출한다. 테스트는 이 3개를 monkeypatch로 갈아끼워 오케스트레이션만 검증(실제 네트워크 없음).

- [ ] **Step 1: 실패 테스트 작성**

Append to `shopping_shorts/tests/test_vmake_client.py`:
```python
import pytest


def test_remove_subtitles_orchestration(tmp_path, monkeypatch):
    calls = {}
    monkeypatch.setattr(vc, "_submit", lambda video, key: calls.setdefault("job", "JOB1") or "JOB1")
    monkeypatch.setattr(vc, "_poll", lambda job, key, timeout: "http://x/result.mp4")
    def fake_dl(url, dest):
        calls["dl"] = (url, str(dest))
        open(dest, "wb").write(b"clean")
        return str(dest)
    monkeypatch.setattr(vc, "_download", fake_dl)

    out = tmp_path / "clean.mp4"
    result = vc.remove_subtitles(str(tmp_path / "in.mp4"), "key:secret", out_path=str(out))
    assert result == str(out)
    assert calls["job"] == "JOB1"
    assert calls["dl"][0] == "http://x/result.mp4"
    assert out.read_bytes() == b"clean"


def test_remove_subtitles_no_key_raises(tmp_path):
    with pytest.raises(ValueError, match="API 키"):
        vc.remove_subtitles(str(tmp_path / "in.mp4"), "", out_path=str(tmp_path / "o.mp4"))


def test_poll_timeout_raises(monkeypatch):
    # _request가 항상 pending을 주면 timeout 예외
    monkeypatch.setattr(vc, "_request", lambda *a, **k: {"status": "pending"})
    with pytest.raises(TimeoutError):
        vc._poll("JOB1", "key:secret", timeout=0)      # timeout=0 → 즉시 초과
```

- [ ] **Step 2: 실패 확인**

Run: `python -m pytest shopping_shorts/tests/test_vmake_client.py -q`
Expected: FAIL — `remove_subtitles`/`_submit`/`_poll`/`_download`/`_request` 미정의

- [ ] **Step 3: remove_subtitles 오케스트레이션 구현**

Append to `shopping_shorts/vmake_client.py`:
```python
import time
import urllib.request


def _split_key(api_key):
    """대시보드 등록 키는 'app_key:secret' 형식으로 저장(VMake 발급 형태 추정).
    콜론 없으면 app_key=secret=키 전체로 폴백."""
    if ":" in api_key:
        app, secret = api_key.split(":", 1)
        return app, secret
    return api_key, api_key


def _request(method, path, api_key, **kw):
    """VMake REST 호출(서명 헤더 부착) → JSON dict. ⚠️ 실패 시 응답 본문을
    예외 메시지에 담아 원인을 삼키지 않는다(지난 exit255 교훈)."""
    import json as _json
    import secrets as _secrets
    import requests
    app, secret = _split_key(api_key)
    ts = str(int(time.time()))
    nonce = _secrets.token_hex(8)
    headers = _auth_headers(app, secret, ts, nonce)
    resp = requests.request(method, f"{_API_BASE}{path}", headers=headers, timeout=60, **kw)
    if resp.status_code >= 400:
        raise RuntimeError(f"VMake API {resp.status_code}: {resp.text[:500]}")
    return resp.json()


def _submit(video_path, api_key):
    """영상 업로드+자막제거 job 제출 → job_id. ⚠️ 엔드포인트/필드 문서로 확정."""
    with open(video_path, "rb") as f:
        data = _request("POST", "/video/remove-subtitles", api_key, files={"file": f})
    return data.get("job_id") or data.get("id")


def _poll(job_id, api_key, timeout):
    """완료까지 상태 폴링 → 결과 URL. timeout(초) 초과 시 TimeoutError."""
    deadline = time.time() + timeout
    while True:
        data = _request("GET", f"/video/jobs/{job_id}", api_key)
        status = data.get("status")
        if status in ("done", "success", "completed"):
            return data.get("result_url") or data.get("output_url")
        if status in ("failed", "error"):
            raise RuntimeError(f"VMake job 실패: {data}")
        if time.time() >= deadline:
            raise TimeoutError(f"VMake job {job_id} {timeout}s 내 미완료(마지막: {status})")
        time.sleep(10)


def _download(url, dest):
    """결과 영상 다운로드 → dest 경로."""
    urllib.request.urlretrieve(url, dest)
    return str(dest)


def remove_subtitles(video_path, api_key, out_path, poll_timeout=1200):
    """video_path의 하드섭을 VMake로 제거 → out_path에 저장하고 그 경로 반환.
    api_key 없으면 ValueError. 처리 실패/타임아웃은 상위로 raise(mix_pipeline이
    status='failed'+error로 저장)."""
    if not api_key:
        raise ValueError("VMake API 키가 등록되지 않았습니다")
    job_id = _submit(video_path, api_key)
    result_url = _poll(job_id, api_key, poll_timeout)
    return _download(result_url, out_path)
```

- [ ] **Step 4: 통과 확인**

Run: `python -m pytest shopping_shorts/tests/test_vmake_client.py -q`
Expected: PASS (6 passed)

- [ ] **Step 5: 커밋**

```bash
git add shopping_shorts/vmake_client.py shopping_shorts/tests/test_vmake_client.py
git commit -m "feat(shopping_shorts): vmake remove_subtitles 제출→폴링→다운로드(mock 검증)"
```

---

## Task 5: video_assemble — assemble을 _render_mix + _burn_captions로 분리

**Files:**
- Modify: `shopping_shorts/video_assemble.py:188-231` (`assemble`)
- Test: `shopping_shorts/tests/test_video_assemble.py`

**배경:** 현재 `assemble`은 비트마다 `_caption_vf`(우리 자막)를 굽고 concat한다. 자막제거를 하려면 "자막 없는 믹스"와 "자막 굽기"를 분리해야 한다. `_render_mix`(자막 없이 비트 렌더+concat) → `_burn_captions`(완성 영상 위에 우리 자막). `assemble`은 이 둘 + (옵션)VMake를 조립.

- [ ] **Step 1: 실패 테스트 작성 (분리 함수 존재+계약)**

Append to `shopping_shorts/tests/test_video_assemble.py`:
```python
import inspect


def test_render_mix_and_burn_captions_exist():
    # assemble이 두 단계로 분리되어 재사용 가능해야 한다.
    assert callable(getattr(va, "_render_mix", None))
    assert callable(getattr(va, "_burn_captions", None))
    # _render_mix는 자막을 굽지 않는다(narration/caption 관련 인자를 받지 않음).
    params = inspect.signature(va._render_mix).parameters
    assert "edit_plan" in params and "tts_paths" in params and "source_video_paths" in params


def test_burn_captions_signature():
    # _burn_captions(in_video, edit_plan, tts_paths, out_path) 형태
    params = list(inspect.signature(va._burn_captions).parameters)
    assert params[0] == "in_video"
    assert "edit_plan" in params and "out_path" in params
```

- [ ] **Step 2: 실패 확인**

Run: `python -m pytest shopping_shorts/tests/test_video_assemble.py -k "render_mix or burn_captions" -q`
Expected: FAIL — `_render_mix`/`_burn_captions` 미정의

- [ ] **Step 3: assemble 리팩터 (분리)**

`shopping_shorts/video_assemble.py`의 `assemble` 함수(188~231행 전체)를 아래로 교체.
`_render_mix`는 기존 비트 렌더 루프에서 **`-vf vf` 대신 자막 없는 `base` 필터만** 쓰고, `_burn_captions`는 완성 영상에 자막을 얹는다. `assemble`은 조립만.
```python
def _render_mix(edit_plan, tts_paths, source_video_paths, work):
    """각 비트를 [소스영상+TTS]로 렌더(우리 자막 없음) → concat → mix_raw.mp4 경로.
    자막을 굽지 않으므로 이후 VMake 자막제거가 우리 자막을 지우지 않는다."""
    base_vf = f"scale={_OUT_W}:{_OUT_H}:force_original_aspect_ratio=increase,crop={_OUT_W}:{_OUT_H}"
    beat_clips = []
    for beat in edit_plan["beats"]:
        idx = beat["beat_idx"]
        tts = tts_paths.get(idx)
        if not tts:
            continue
        tts_dur = _probe_duration(tts)
        ref = _pick_segment(beat, tts_dur, source_video_paths)
        src = source_video_paths[ref["video_id"]]
        src_dur = _probe_duration(src)
        clip = work / f"beat_{idx}.mp4"
        start = ref["start"]
        if start + tts_dur > src_dur:
            start = max(0.0, src_dur - tts_dur)
        loop = ["-stream_loop", "-1"] if src_dur + 0.05 < tts_dur else []
        cmd = [
            "ffmpeg", "-y",
            *loop, "-ss", f"{start:.3f}", "-i", str(src),
            "-i", str(tts),
            "-vf", base_vf, "-r", "30",
            "-map", "0:v:0", "-map", "1:a:0",
            "-t", str(tts_dur),
            "-c:v", "libx264", "-c:a", "aac", "-pix_fmt", "yuv420p", str(clip),
        ]
        _run_ffmpeg(cmd)
        beat_clips.append(clip)
    if not beat_clips:
        raise RuntimeError("video_assemble: 렌더할 비트가 없습니다")
    concat_txt = work / "concat_mix.txt"
    concat_txt.write_text("".join(f"file '{c.as_posix()}'\n" for c in beat_clips), encoding="utf-8")
    mix_raw = work / "mix_raw.mp4"
    _run_ffmpeg(["ffmpeg", "-y", "-f", "concat", "-safe", "0", "-i", str(concat_txt),
                 "-c", "copy", str(mix_raw)])
    return str(mix_raw)


def _burn_captions(in_video, edit_plan, tts_paths, out_path, work):
    """완성된 믹스 영상(in_video) 위에 우리 자막(_caption_vf)을 비트 타이밍대로 굽는다.
    비트 경계는 각 비트 tts 길이 누적으로 계산해 drawtext enable 구간을 전체 타임라인
    기준으로 배치한다. 폰트 없으면 자막 없이 원본 복사."""
    font = _resolve_font()
    if font:
        try:
            shutil.copy(font, work / "font.ttf")
        except OSError:
            font = None
    if not font:
        shutil.copy(in_video, out_path)
        return str(out_path)
    # 비트별 시작오프셋 = 앞 비트들 tts 길이 합
    filters = [f"scale={_OUT_W}:{_OUT_H}:force_original_aspect_ratio=increase,crop={_OUT_W}:{_OUT_H}"]
    t0 = 0.0
    for beat in edit_plan["beats"]:
        idx = beat["beat_idx"]
        tts = tts_paths.get(idx)
        if not tts:
            continue
        dur = _probe_duration(tts)
        seg_vf = _caption_vf(beat.get("narration", ""), dur, True, work, idx)
        # _caption_vf는 base(scale/crop)+drawbox+drawtext를 반환한다. 여기선 이미 규격이
        # 맞으므로 base 부분을 떼고 drawbox/drawtext만 t0 오프셋을 더해 이어붙인다.
        for part in seg_vf.split(","):
            if part.startswith("drawbox") or part.startswith("drawtext"):
                filters.append(_shift_enable(part, t0))
        t0 += dur
    vf = ",".join(filters)
    _run_ffmpeg(["ffmpeg", "-y", "-i", str(in_video), "-vf", vf, "-r", "30",
                 "-c:v", "libx264", "-c:a", "copy", "-pix_fmt", "yuv420p", str(out_path)],
                cwd=str(work))
    return str(out_path)


def assemble(edit_plan, tts_paths, source_video_paths, out_path, clean_fn=None):
    """EDL → 최종 mp4. 1)믹스(자막X) 2)clean_fn(있으면 자막제거) 3)우리 자막.
    clean_fn(mix_raw_path)->clean_path 를 주면 그 사이에 자막제거가 끼워진다(없으면 생략).
    반환: out_path."""
    work = Path(out_path).parent / f"asm_{uuid.uuid4().hex[:8]}"
    work.mkdir(parents=True, exist_ok=True)
    mix_raw = _render_mix(edit_plan, tts_paths, source_video_paths, work)
    base_video = clean_fn(mix_raw) if clean_fn else mix_raw
    return _burn_captions(base_video, edit_plan, tts_paths, out_path, work)
```

- [ ] **Step 4: 헬퍼 `_run_ffmpeg` / `_shift_enable` 추가**

`_probe_duration` 함수 바로 뒤에 추가(기존 `subprocess.run(capture_output=True, check=True)`를 stderr 남기는 헬퍼로 통일 — 지난 exit255 교훈):
```python
def _run_ffmpeg(cmd, cwd=None):
    """ffmpeg 실행. 실패 시 stderr를 예외에 담아 원인을 삼키지 않는다."""
    r = subprocess.run(cmd, capture_output=True, text=True, cwd=cwd)
    if r.returncode != 0:
        raise RuntimeError(f"ffmpeg 실패(exit {r.returncode}): {r.stderr[-1000:]}")
    return r


def _shift_enable(drawtext_part, offset):
    """drawtext/drawbox 필터의 enable='between(t,a,b)' 구간을 offset만큼 민다.
    enable이 없으면(drawbox 등 상시) 그대로 반환."""
    import re
    m = re.search(r"between\(t,([\d.]+),([\d.]+)\)", drawtext_part)
    if not m:
        return drawtext_part
    a = float(m.group(1)) + offset
    b = float(m.group(2)) + offset
    return drawtext_part[:m.start()] + f"between(t,{a:.2f},{b:.2f})" + drawtext_part[m.end():]
```

- [ ] **Step 5: 통과 확인 (분리 테스트 + 기존 자막 테스트 회귀)**

Run: `python -m pytest shopping_shorts/tests/test_video_assemble.py -q`
Expected: PASS (기존 17 + 신규 2 = 19)

- [ ] **Step 6: 실제 렌더 눈 검증 (스모크)**

Run:
```bash
python - <<'PY'
from shopping_shorts import video_assemble as va
import inspect
print("split ok:", callable(va._render_mix), callable(va._burn_captions))
PY
```
Expected: `split ok: True True`

- [ ] **Step 7: 커밋**

```bash
git add shopping_shorts/video_assemble.py shopping_shorts/tests/test_video_assemble.py
git commit -m "refactor(shopping_shorts): assemble을 믹스/자막굽기로 분리(+VMake 훅 clean_fn)"
```

---

## Task 6: mix_pipeline — run_render에 자막제거 단계

**Files:**
- Modify: `shopping_shorts/mix_pipeline.py:114-138` (`run_render`)
- Test: `shopping_shorts/tests/test_mix_render_flow.py` (신규)

- [ ] **Step 1: 실패 테스트 작성**

Create `shopping_shorts/tests/test_mix_render_flow.py`:
```python
from shopping_shorts import mix_pipeline as mp


class FakeStore:
    def __init__(self, job):
        self._job = job
        self.updates = []

    def get_mix_job(self, job_id):
        return self._job

    def update_mix_job(self, job_id, **fields):
        self.updates.append(fields)
        self._job.update(fields)


def _statuses(store):
    return [u["status"] for u in store.updates if "status" in u]


def test_render_off_skips_vmake(monkeypatch, tmp_path):
    job = {"job_id": "j", "urls": ["u"], "edit_plan": {"beats": [{"beat_idx": 0, "tts_path": "t"}]},
           "subtitle_removal": False}
    store = FakeStore(job)
    monkeypatch.setattr(mp, "Store", lambda p: store)
    monkeypatch.setattr(mp, "_resolve_sources", lambda job, work: {"s0": "x.mp4"})
    called = {"clean": 0}
    def fake_assemble(plan, tts, sources, out, clean_fn=None):
        if clean_fn:
            called["clean"] += 1
        open(out, "w").write("v")
        return out
    monkeypatch.setattr(mp, "assemble", fake_assemble)

    mp.run_render("j", str(tmp_path / "db"), str(tmp_path))
    assert called["clean"] == 0                       # VMake 안 부름
    assert "removing_subtitles" not in _statuses(store)
    assert _statuses(store)[-1] == "done"


def test_render_on_calls_vmake(monkeypatch, tmp_path):
    job = {"job_id": "j", "urls": ["u"], "edit_plan": {"beats": [{"beat_idx": 0, "tts_path": "t"}]},
           "subtitle_removal": True}
    store = FakeStore(job)
    monkeypatch.setattr(mp, "Store", lambda p: store)
    monkeypatch.setattr(mp, "_resolve_sources", lambda job, work: {"s0": "x.mp4"})
    monkeypatch.setattr(mp, "_vmake_key", lambda store: "appkey:secret")
    seen = {}
    def fake_remove(video, key, out_path, **kw):
        seen["video"] = video; open(out_path, "w").write("clean"); return out_path
    monkeypatch.setattr(mp, "remove_subtitles", fake_remove)
    def fake_assemble(plan, tts, sources, out, clean_fn=None):
        # clean_fn을 실제로 호출해 VMake 경유 확인
        if clean_fn:
            clean_fn("mix_raw.mp4")
        open(out, "w").write("v"); return out
    monkeypatch.setattr(mp, "assemble", fake_assemble)

    mp.run_render("j", str(tmp_path / "db"), str(tmp_path))
    assert seen["video"] == "mix_raw.mp4"             # VMake가 mix_raw 받음
    assert "removing_subtitles" in _statuses(store)
    assert _statuses(store)[-1] == "done"


def test_render_on_no_key_fails(monkeypatch, tmp_path):
    job = {"job_id": "j", "urls": ["u"], "edit_plan": {"beats": [{"beat_idx": 0, "tts_path": "t"}]},
           "subtitle_removal": True}
    store = FakeStore(job)
    monkeypatch.setattr(mp, "Store", lambda p: store)
    monkeypatch.setattr(mp, "_resolve_sources", lambda job, work: {"s0": "x.mp4"})
    monkeypatch.setattr(mp, "_vmake_key", lambda store: "")      # 키 없음
    monkeypatch.setattr(mp, "assemble", lambda *a, **k: (_ for _ in ()).throw(AssertionError("호출되면 안됨")) if k.get("clean_fn") is None else None)

    mp.run_render("j", str(tmp_path / "db"), str(tmp_path))
    assert _statuses(store)[-1] == "failed"
    assert any("키" in (u.get("error") or "") for u in store.updates)
```

- [ ] **Step 2: 실패 확인**

Run: `python -m pytest shopping_shorts/tests/test_mix_render_flow.py -q`
Expected: FAIL — `_resolve_sources`/`_vmake_key`/`remove_subtitles` 미정의, 흐름 미구현

- [ ] **Step 3: run_render 재구성 + 헬퍼**

`shopping_shorts/mix_pipeline.py` 상단 import에 추가:
```python
from shopping_shorts.vmake_client import remove_subtitles
```
`run_render`를 아래로 교체하고, 위에 헬퍼 2개를 추가:
```python
def _resolve_sources(job, work):
    """다운로드된 소스 mp4 경로 맵 {video_id: path}. 없으면 예외."""
    source_video_paths = {}
    for i in range(len(job["urls"])):
        vid = _source_video_id(i)
        mp4 = next((work / vid).glob("*.mp4"), None)
        if mp4 is None:
            raise RuntimeError(f"소스 영상 없음: {vid} (다운로드 디렉터리에 mp4 없음)")
        source_video_paths[vid] = str(mp4)
    return source_video_paths


def _vmake_key(store):
    """등록된 VMake 개인키(DB settings). 없으면 빈 문자열."""
    return store.get_setting("vmake_api_key", "") or ""


def run_render(job_id, db_path, work_root):
    """확인된 EDL을 최종 mp4로 렌더. subtitle_removal이 켜져 있으면 믹스 후
    VMake로 원본 자막을 제거하고 그 위에 우리 자막을 굽는다. 완료 시 status='done'."""
    store = Store(db_path)
    job = store.get_mix_job(job_id)
    if not job or not job.get("edit_plan"):
        return
    work = Path(work_root) / job_id
    try:
        store.update_mix_job(job_id, status="rendering")
        plan = job["edit_plan"]
        tts_paths = {b["beat_idx"]: b["tts_path"] for b in plan["beats"] if b.get("tts_path")}
        source_video_paths = _resolve_sources(job, work)
        out_path = work / "final.mp4"

        clean_fn = None
        if job.get("subtitle_removal"):
            key = _vmake_key(store)
            if not key:
                raise RuntimeError("자막 제거가 켜져 있으나 VMake 개인키가 등록되지 않았습니다")
            def clean_fn(mix_raw):                        # noqa: E306
                store.update_mix_job(job_id, status="removing_subtitles")
                clean_path = str(work / "clean.mp4")
                out = remove_subtitles(mix_raw, key, out_path=clean_path)
                store.update_mix_job(job_id, clean_video_path=out)
                return out

        assemble(plan, tts_paths, source_video_paths, str(out_path), clean_fn=clean_fn)
        store.update_mix_job(job_id, status="done", video_path=str(out_path))
    except Exception as e:
        traceback.print_exc(file=sys.stderr)
        store.update_mix_job(job_id, status="failed", error=str(e))
```
기존 `run_render`의 소스 해석 인라인 블록은 `_resolve_sources`로 대체되었으므로 삭제.

- [ ] **Step 4: 통과 확인**

Run: `python -m pytest shopping_shorts/tests/test_mix_render_flow.py -q`
Expected: PASS (3 passed)

- [ ] **Step 5: 커밋**

```bash
git add shopping_shorts/mix_pipeline.py shopping_shorts/tests/test_mix_render_flow.py
git commit -m "feat(shopping_shorts): run_render에 VMake 자막제거 단계(옵션)+removing_subtitles 상태"
```

---

## Task 7: app.py — 개인키 저장 엔드포인트 + mix start 플래그

**Files:**
- Modify: `shopping_shorts/app.py:438-451` (`/api/mix/start`), 신규 엔드포인트
- Test: `shopping_shorts/tests/test_app_vmake.py` (신규)

- [ ] **Step 1: 실패 테스트 작성**

Create `shopping_shorts/tests/test_app_vmake.py`:
```python
from fastapi.testclient import TestClient
from shopping_shorts import app as appmod


def _client(tmp_path, monkeypatch):
    monkeypatch.setattr(appmod, "DB_PATH", str(tmp_path / "t.db"))
    return TestClient(appmod.app)


def test_set_and_get_vmake_key(tmp_path, monkeypatch):
    c = _client(tmp_path, monkeypatch)
    r = c.post("/api/settings/vmake_key", json={"key": "appkey:secret"})
    assert r.status_code == 200 and r.json()["ok"] is True
    # 조회 시 존재 여부만 노출(키 원문은 마스킹)
    g = c.get("/api/settings/vmake_key")
    assert g.json()["configured"] is True
    assert "secret" not in str(g.json())              # 원문 미노출


def test_get_vmake_key_unset(tmp_path, monkeypatch):
    c = _client(tmp_path, monkeypatch)
    assert c.get("/api/settings/vmake_key").json()["configured"] is False


def test_mix_start_passes_subtitle_removal(tmp_path, monkeypatch):
    c = _client(tmp_path, monkeypatch)
    captured = {}
    def fake_create(job_id, urls, target, structure, subtitle_removal=False):
        captured["sr"] = subtitle_removal
    monkeypatch.setattr(appmod.Store, "create_mix_job", lambda self, *a, **k: fake_create(*a, **k))
    monkeypatch.setattr(appmod, "run_mix_job", lambda *a, **k: None)
    c.post("/api/mix/start", json={"urls": ["u1", "u2"], "target_seconds": 30,
                                   "structure": "template", "subtitle_removal": True})
    assert captured["sr"] is True
```

- [ ] **Step 2: 실패 확인**

Run: `python -m pytest shopping_shorts/tests/test_app_vmake.py -q`
Expected: FAIL — 404(엔드포인트 없음), subtitle_removal 미전달

- [ ] **Step 3: 엔드포인트 구현**

`shopping_shorts/app.py`의 `api_mix_start`(487행)를 아래로 교체(기존 URL 2개 검증·`_MIX_WORK_DIR` 유지, subtitle_removal만 추가):
```python
@app.post("/api/mix/start")
def api_mix_start(background_tasks: BackgroundTasks, body: dict):
    """믹스 job 시작. body: {urls:[...], target_seconds:int, structure, subtitle_removal:bool}."""
    urls = [u for u in (body.get("urls") or []) if u]
    if len(urls) < 2:
        return JSONResponse(status_code=422, content={"ok": False, "error": "레퍼런스 URL 2개 이상 필요"})
    target = int(body.get("target_seconds") or 30)
    structure = body.get("structure") if body.get("structure") in ("template", "free") else "template"
    subtitle_removal = bool(body.get("subtitle_removal", False))
    job_id = uuid.uuid4().hex[:12]
    Store(DB_PATH).create_mix_job(job_id, urls, target, structure, subtitle_removal=subtitle_removal)
    background_tasks.add_task(run_mix_job, job_id, DB_PATH, _MIX_WORK_DIR)
    return {"ok": True, "job_id": job_id}
```
(work_root 변수는 기존과 동일하게 `_MIX_WORK_DIR`을 쓴다.)

`/api/mix/result` 엔드포인트 근처에 설정 엔드포인트 2개 추가:
```python
@app.post("/api/settings/vmake_key")
def api_set_vmake_key(body: dict):
    key = (body.get("key") or "").strip()
    Store(DB_PATH).set_setting("vmake_api_key", key)
    return {"ok": True}


@app.get("/api/settings/vmake_key")
def api_get_vmake_key():
    key = Store(DB_PATH).get_setting("vmake_api_key", "")
    return {"ok": True, "configured": bool(key)}      # 원문은 노출하지 않음
```

- [ ] **Step 4: 통과 확인**

Run: `python -m pytest shopping_shorts/tests/test_app_vmake.py -q`
Expected: PASS (3 passed)

- [ ] **Step 5: 커밋**

```bash
git add shopping_shorts/app.py shopping_shorts/tests/test_app_vmake.py
git commit -m "feat(shopping_shorts): vmake 개인키 저장/조회 API + mix start subtitle_removal 플래그"
```

---

## Task 8: 대시보드 UI — 개인키 입력 + 자막제거 토글

**Files:**
- Modify: 믹스 UI HTML (Task 8-Step 1에서 파일 확정)

- [ ] **Step 1: 믹스 UI 파일 찾기**

Run: `grep -rln "mix/start\|목표 길이\|여러 영상 믹스" shopping_shorts/static/`
Expected: 믹스 UI html 파일 경로 1개 이상. 그 파일을 대상으로 한다.

- [ ] **Step 2: 자막제거 토글 + 키 입력 UI 추가**

찾은 HTML의 믹스 시작 폼(목표 길이 근처)에 추가. `fetch('/api/mix/start')` 호출부에 `subtitle_removal` 포함:
```html
<label style="display:flex;align-items:center;gap:8px;margin-top:12px">
  <input type="checkbox" id="subtitleRemoval"> 원본 자막 제거 (VMake)
</label>
<div style="margin-top:8px">
  <input type="password" id="vmakeKey" placeholder="vmake.ai 개인 API 키 (app_key:secret)"
         style="width:100%">
  <button type="button" onclick="saveVmakeKey()">키 저장</button>
  <span id="vmakeKeyState"></span>
</div>
<script>
async function saveVmakeKey() {
  const key = document.getElementById('vmakeKey').value.trim();
  const r = await fetch('/api/settings/vmake_key', {method:'POST',
    headers:{'Content-Type':'application/json'}, body: JSON.stringify({key})});
  document.getElementById('vmakeKeyState').textContent = r.ok ? '저장됨' : '실패';
}
async function loadVmakeKeyState() {
  const r = await fetch('/api/settings/vmake_key');
  const j = await r.json();
  document.getElementById('vmakeKeyState').textContent = j.configured ? '등록됨' : '미등록';
}
loadVmakeKeyState();
</script>
```
믹스 시작 fetch body에 추가:
```javascript
  subtitle_removal: document.getElementById('subtitleRemoval').checked,
```

- [ ] **Step 3: 수동 확인 (서버 기동)**

Run: `python -m uvicorn shopping_shorts.app:app --port 8848` 후 브라우저에서 믹스 화면에 토글·키 입력란이 보이고, 키 저장 시 '등록됨' 표시되는지 확인.
Expected: UI 정상 노출, 키 저장 동작.

- [ ] **Step 4: 커밋**

```bash
git add shopping_shorts/static/
git commit -m "feat(shopping_shorts): 믹스 UI에 VMake 자막제거 토글+개인키 입력"
```

---

## Task 9: 전체 회귀 + 문서 반영

- [ ] **Step 1: 전체 테스트**

Run: `python -m pytest shopping_shorts/tests/ -q`
Expected: 기존 210 + 신규(store 6, vmake 6, assemble 2, render 3, app 3 = 20) 전부 PASS

- [ ] **Step 2: NEXT_SESSION에 "내일 VMake 문서로 실스펙 교체" 기록**

`NEXT_SESSION.md`에 추가: vmake_client.py의 `_API_BASE`/`_sign`/`_submit`/`_poll` 엔드포인트·필드명을 실제 개발자 문서로 교체 + 실제 키로 1회 end-to-end 렌더 눈검증.

- [ ] **Step 3: 커밋**

```bash
git add NEXT_SESSION.md
git commit -m "docs: VMake 실스펙 교체 핸드오프(내일 회사서 키/문서)"
```

---

## ⚠️ 내일 실제 API 확보 후 (이 계획 밖, 별도)

1. 개발자 문서로 `vmake_client.py`의 `_API_BASE`, `_sign` 알고리즘, `_submit`/`_poll` 엔드포인트·응답 필드명 확정·교체.
2. 실제 키 등록 → 진짜 소스로 1회 렌더 → 자막 제거+우리 자막 프레임 눈 검증(로컬 ffmpeg).
3. 서버 배포 후 라이브 1건 검증.
