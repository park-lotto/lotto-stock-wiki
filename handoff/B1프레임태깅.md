# B1프레임태깅 — 핸드오프

_최종: 2026-09-04 밤 · 상태: **1·2·3단계 전부 main 병합, 전부 스위치 뒤(기본 꺼짐) — 라이브 동작 불변.** 다음은 사장님 실사용·서버 실측_
_설계·실측 원문: `docs/superpowers/specs/2026-09-04-3단계매칭-파서재설계-design.md` §9 (이 트랙에 같이 커밋)_

## 무엇을 · 왜

사장님(09-04): "3단계 매칭이 안 맞는다 → 근본 대책 → 1단계부터 차근차근." 1단계 정확도를 로컬 소스 4편(세그 52개)으로
재 보니 **묘사가 틀린 게 아니라 묘사가 붙은 칸이 한 칸 밀려 있었다**(통째 업로드 방식이 컷 목록에 묘사를 배정하며
한 칸 밀거나 한 컷을 건너뜀). 컷별 프레임 태깅(B1, 07-29)은 받은 프레임을 묘사하므로 이 오배정이 구조적으로 불가능
→ 켜야 하는데, **B1은 설계대로 돈 적이 없었다.**

## 고친 것 (`shopping_shorts/frame_script.py`, `tag_qa_frames.py`)

| # | 버그(실측) | 수리 |
|---|---|---|
| 1 | `extract_frame_at(video, dir, mid)`를 파일명 없이 불러 `frame_hint.jpg`에 전부 덮어씀 → 모든 컷이 **마지막 프레임 한 장**으로 태깅 | 구간·장마다 `seg{i:03d}_{j}.jpg` |
| 2 | `_default_boundaries`가 `detect_cuts`의 `(start_frame,end_frame)` 튜플을 `float()` → TypeError 삼킴 → **항상 구간 1개** | fps로 초 변환(옛 초 형식도 수용) + `split_long_spans`(원테이크 상한 `MAX_SPAN_SEC=7`) |
| 3 | `response_schema`(enum) 강제 호출이 gemini-3.5-flash에서 **120초 초과**(판정 4/4·태깅 1/1 504) → 옛 추출로 조용히 폴백 | 스키마 없이 JSON만 받고 `normalize_tags`가 값 검증(shot_role→`shot_roles.normalize`). 모델 순서 `TAG_MODELS=(lite, 3.5-flash)` **한 곳** — `tag_qa_frames._judge`도 같은 상수 |
| 4 | (2차 실측) 컷당 프레임 2~3장을 따로 실으면 모델이 **이미지 1장=구간 1개**로 세어 묘사가 2배 압축·밀림 | 시작·중간·끝 3장을 **띠 한 장**(`make_strip`, PIL)으로 합쳐 **구간당 이미지 1장**. 띠 실패면 중간 한 장 |

`tag_frames(frame_groups, caption, segs)` — 이제 **구간별 묶음(list of list, 길이 1)**. 옛 평평한 목록도 받는다.

## 실측 (로컬 잡 409f894230c6, 4편, 예비풀 키·Whisper 없음)

| 소스 | 기존 세그 | 수리 B1 | 눈으로 대조 |
|---|---|---|---|
| s0 | 12 | 12 / 10초 | 11.37 "스패출러로 크림 질감"(✓ 종전 "말차 녹색"✗) · 12.27 "말차와 초코 섞는"(둘 다) · 14.07 "3종 완성 쿠키"(순서까지) |
| s3 | 24 | 27 / 26초 | 12.73 "레시피 재료 명시"(✓ 종전 "버터 누름"✗) · 13.87 "버터 손가락으로 확인"(✓) |
| s2 | 15 | 25 / 25초 | 컷 21개 전부 살림(미검안) |
| s1 | 13 | **14** / 23초 | 원테이크(컷 3개) → 6.4초 균등 분할, 흐름 읽힘(종전 B1은 51초 1구간) |

테스트: `tests/test_b1_frame_fixes.py` 12건 + 기존 frame_script·tag_qa_frames 갱신 = 48 passed.
프로브: 세션 scratchpad `stage1_probe.py`(정확도 판정) · `contact_sheet.py`(시작·중간·끝 띠 눈검사) · `b1_track_probe.py`(트랙 코드 실행).
⚠️로컬 함정: SHORTS 풀 0개(`_current_key_and_idx` None → 조용히 빈 값, 예비풀 키로 우회) / 한글 절대경로는 ffmpeg stderr cp949 크래시(ASCII 상대경로로).

## ⏭ 남은 것 (순서대로 — 사장님과 하나씩)
1. **서버 실측**: 최근 소스 30편에 수리 B1 vs 기존 추출 정확도(`tag_qa_frames` 전수) — 회사 PC SSH 22 막힘, 집 PC/사장님.
2. **말 트랙**: 서버 GROQ 키로 Whisper 단어 전사 실측(이번엔 못 봄) + **플랫폼 자막 트랙 받기**(`media_download`에 없음) + 외국어 원문·번역 둘 다.
3. **1차 브리프(스토리)** → 2차 컷 묘사에 브리프 주입(설계 §9-2). 무대본 외국 영상의 맥락 문제.
4. 켜기: `frame_extract_enabled=1` + 믹스 경로(`mix_pipeline.py:1053` `_use_frames`)도 같은 플래그인지 확인.
5. 그 다음 2단계(본 것만 쓰기 + 줄마다 장면 필수 + 게이트), 3단계(추측 층 삭제).

## ▶ 다른 PC에서 이어서 하는 법 (2026-09-04 저녁 기록)

**말할 것**: `"매칭 파서 이어서"` 또는 `"B1프레임태깅 이어서"`
→ Claude가 할 일: `py tools/find_work.py 매칭 파서` → 이 파일 + 설계서 §9 읽기 → `.tracks/B1프레임태깅` 없으면
`py tools/track.py start B1프레임태깅` → 아래 순서 1번부터.

**어디에 뭐가 있나**
| 것 | 위치 | 비고 |
|---|---|---|
| 설계·실측 전체 | `docs/superpowers/specs/2026-09-04-3단계매칭-파서재설계-design.md` §1~§10 | §9 = 1단계, §10 = 대본 퀄리티(별도 트랙) |
| 수리한 코드 | `shopping_shorts/frame_script.py`, `tag_qa_frames.py` (main `24c2648e8`) | 플래그 `frame_extract_enabled` **OFF** |
| 측정 스크립트 | `tools/probes/stage1_frame_accuracy.py`(묘사↔프레임 판정) · `contact_sheet.py`(눈검사 띠) · `b1_extract_probe.py`(수리 B1 실행) | 결과는 `out/probes/` |
| 이 PC 메모리 | `~/.claude/projects/.../memory/reference_B1프레임태깅_조용한버그3_띠합치기.md` | **메모리는 PC마다 따로다** — 핵심은 전부 이 파일과 설계서에 있다 |

**PC별 함정(둘 다 이 파일에서 우회 코드 있음)**
- SHORTS 풀 키가 0개인 PC(.env에 SHORTS_GEMINI_KEY 없음)는 `comment_gen._current_key_and_idx()`가 None → 판정·태깅이
  **로그 한 줄만 남기고 빈 값**. 프로브는 `keyroute.gemini_keys("general")[0]`로 우회한다.
- 한글 절대경로로 ffmpeg를 부르면 stderr cp949 크래시(`scene_cut._boundary_frames`) → 프로브는 저장소 루트로 chdir 후 ASCII 상대경로.
- 회사 PC는 서버 SSH 22 타임아웃(메모리 `reference_SSH막힘_사이트는정상`). 서버 실측은 되는 PC에서.
- 로컬 DB(`shopping_shorts/data/reference.db`)에 잡이 1개(409f894230c6, 08-15)뿐. 30편 실측은 서버 DB/영상 필요.

**지금 상태(09-04 밤) — 코드는 다 들어갔다. 남은 건 켜서 재는 것**
| 단계 | 스위치(관리자 설정 `/api/admin/settings`) | 켜기 전 할 것 |
|---|---|---|
| 1단계 B1 컷별 프레임 태깅(두 번 보기·번역) | `frame_extract_enabled=1` | 서버 30편에 `tools/probes/b1_extract_probe.py` + `stage1_frame_accuracy.py`로 기존 vs B1 정확도 |
| 2단계 본 것만 쓰기 | `script_grounded_enabled=admin` | 없음 — 사장님 계정만 켜서 대본 뽑아 보기 |
| 3단계 붙어 온 장면 그대로 | `edl_inherit_enabled=admin` | 2단계와 **같이** 켠다(출처가 와야 상속) |

**순서**
1. 사장님 계정만 2·3단계 켜기(`admin`) → 제품형 소스로 2단계 대본 확정 → 3단계 칸타임라인에서 **컷 바꾼 횟수** 세기(이게 시험지).
2. 서버(SSH 되는 곳)에서 1단계 30편 실측 → B1 우위면 `frame_extract_enabled=1`.
3. 손 횟수가 만족스러우면 2·3단계 `1`(전체). 한 달 무사고 뒤 옛 층(respine·verify·repick·reconcile) 삭제.
4. 별도: 플랫폼 자막 트랙(릴레이 에이전트), 대본 퀄리티 트랙(설계 §10 — 모델 급 비교·선택 기록).

## ✅ 2차 (2026-09-04 밤) — 두 번 보기(1차 브리프 → 2차 컷 태깅) + 기존 추출과 같은 필드

사장님 질문("대사 없는 외국 영상, 컷만 따로 보면 못 쓰지 않나")의 답을 코드로. 설계 §9-2.

| 바꾼 것 | 위치 |
|---|---|
| **1차 브리프**: 컷 대표 프레임 격자 한 장(칸마다 실제 초 표기) + 캡션 + 전사 → `source_brief{product, role, core, summary, flow, confidence}` | `frame_script._gemini_story_brief`, `make_grid(times=)` |
| **2차 태깅에 브리프 주입** (`brief_block`) — "큰 그림 안에서 label을 정해라", 불명이면 보수 지시 | `_gemini_tag_frames(…, brief)` |
| **필드 정의 공용화(0순위-B)**: `_PROMPT`에서 `_BRIEF_GUIDE`·`_SEG_FIELD_GUIDE`를 뽑아 통째 업로드 추출과 B1이 **같은 문장** 사용. B1이 label·use_point·action·change·has_effect도 낸다(종전엔 셋이 빈칸 = 3단계 인벤토리 재료 손실) | `script_extract.py`(내용 불변, 위치만) |
| `_BRIEF_KEYS`에 flow·confidence 추가(스키마·정규화) | `script_extract.py` |
| **배치 호출** `TAG_BATCH=12` — 필드 8개×27구간 한 호출은 lite가 504·JSON 깨짐으로 상위 모델 폴백(80~90초). 12구간씩이면 s3 27구간 68초 | `_gemini_tag_frames` |
| `loads_lenient` — JSON 뒤 쓰레기·코드펜스 허용(실측 "Extra data: line 17"). 판정기도 공용 | `frame_script`, `tag_qa_frames` |

실측(로컬 4편): 브리프 flow가 실제 시각과 일치(s3 "0~13초 시식 → 13~41초 반죽 → 41~55초 굽기 → 55~69초 시식" — 재료표 12.73초·짜기 42.83초·오븐 49초와 맞음). 컷 짝은 그대로 정확(s3 12.73 "레시피 재료 소개", 13.87 "버터 눌러보고 설탕 붓는다").
테스트 64 green(`test_b1_frame_fixes.py` 18건). 프로브: `tools/probes/b1_extract_probe.py`가 2패스 결과(브리프 포함)를 낸다.
⚠️ 남은 것: 원테이크 강제분할 구간(6.4초)이 여러 동작을 담아 묘사가 길어짐 — 상한을 5초로 줄일지는 서버 실측 후.

## ✅ 3차 (2026-09-04 밤) — 말 트랙: 언어 자동 감지 + 외국어 한국어 번역(text_ko)

| 바꾼 것 | 위치 |
|---|---|
| `transcribe_words(mp3, language="ko")` — **None이면 언어 자동 감지**. 기본값은 종전 "ko"(TTS 검수용, 회귀 0). B1은 None으로 부른다 | `asr_check.py`, `frame_script.extract_script_frames` |
| 전사가 외국어(한글 비율 <30%)면 한 호출로 구간별 번역 → `text_ko`. 원문 `text`는 보존. 결과에 `full_text_ko` | `frame_script.is_foreign_text`, `_gemini_translate`(injectable `translate=`) |
| `_assign_seg_ids`가 `text_ko`를 싣는다 | `script_extract.py` |
| 3단계 인벤토리 `말:` · 2단계 장면 목록 `말:` · 대본 재료 `full_text`가 **text_ko/full_text_ko 우선** | `edit_plan._build_inventory`, `script_generate._mix_source_block`, `app.py`(대본 재료 조립 1곳) |

실측: `_gemini_translate(["Mix the softened butter with 90g powdered sugar", "", "把黄油打发到发白，加入蛋清", "Bake at 170°C for 12 minutes"])`
→ `['부드러워진 버터에 슈가파우더 90g을 넣고 섞어주세요', '', '버터를 하얗게 될 때까지 휘핑한 뒤, 달걀흰자를 넣어주세요', '170°C에서 12분 동안 구워주세요']`.
Whisper 자동 감지는 로컬 GROQ 키가 없어 **모킹 테스트만**(서버 실측 필요). 테스트 +6.

**⏭ 플랫폼 자막 트랙 받기는 미착수** — 서버는 유튜브를 사장님 PC 릴레이(`_download_via_relay`)로 받는 구조라 자막도 릴레이 에이전트가
같이 받아 올려야 한다(yt-dlp `--write-subs --write-auto-subs`). 인스타(Apify)·틱톡은 자막 트랙 자체가 드물다. Whisper 자동 감지가 먼저 서버에서 검증되면 그때.

## ✅ 4차 (2026-09-04 밤) — 2단계 '본 것만 쓰기'(grounded) — **플래그 뒤, 기본 꺼짐**

사장님: "제품형 대본은 소스 영상에 보이는 것으로만 써야 한다. 대본을 썼는데 장면이 없다는 건 말이 안 된다."

| 바꾼 것 | 위치 |
|---|---|
| 스위치 `script_grounded_enabled`: ""/"0" 끔(기본) · `admin` 관리자만 · `11,42` 고객 목록 · `1` 전체. 관리자 설정(`/api/admin/settings`) 허용 키에 추가(`frame_extract_enabled`도) | `app._script_grounded`, `_ADMIN_SETTING_KEYS`, 호출부 2곳 |
| 장면 목록 **전부**(상한 60) + 쓰임·변화·활용, 말은 text_ko 우선 | `script_generate._mix_source_block(full_scenes=True)`, `_scene_line_full` |
| 규칙 `_GROUNDED_RULE`: 장점·효과·동작·결과 줄은 src_seg 필수(needs_scene=true), 훅·감정·연결·가격·약속은 면제, 없는 번호 금지, 여러 장면은 쉼표(첫 번째가 대표). 레시피는 느슨 | `script_generate.generate_one_style(grounded=)` |
| 게이트 **'장면 근거'**(지시와 판정은 짝): 지어낸 번호 반려 · needs_scene인데 빈칸 반려 · 제품형은 3분의 1 이상 장면 | `script_gate.scene_grounding_check`, `parse_src_segs` |
| 스키마 `needs_scene`(선택) · 산출 `src_seg`=대표, `src_segs`=전체 | `_STYLE_SCHEMA`, generate_one_style 끝 |

실측(로컬, 실제 모델·스타일 블록은 합성): 5칸(첫말·문제·시연·결과·약속) 대본에서 시연 `s3-8,s3-9,s3-10,s3-16`(믹서·체·짜기),
결과 `s3-19,s3-20,s3-3,s3-24`(오븐·단면) — **장면이 붙은 줄의 내용이 실제 장면과 맞았다.** 모델이 한 줄에 장면을 여럿 적어
처음엔 게이트가 통째로 "없는 번호"로 반려 → 쉼표 목록 허용. 절반 비율은 5칸 구조에서 시연·결과가 맞아도 2/5로 반려 → 3분의 1.
플래그 밖 프롬프트·게이트는 종전과 글자 단위로 같다(테스트가 고정). 테스트 `test_script_grounded.py` 11건.

**⏭ 2단계 남은 것**: ①서버에서 사장님 계정만 켜서(`admin`) 실사용 → 손대는 횟수 세기 ②조립 경로(`_assembled_drafts`, 스파인 조립)는
grounded 밖 — 켜면 생성기 경로만 적용됨 ③3단계는 이 src_seg를 `_apply_beat_sources`가 받는다(자리 잇기 수리 `track/출처장면배선` 미병합 — 함께 봐야 함).

## ✅ 5차 (2026-09-04 밤) — 3단계 '붙어 온 장면 그대로 쓰기'(inherit) — **플래그 뒤, 기본 꺼짐** + 출처 자리잇기 병합

| 바꾼 것 | 위치 |
|---|---|
| `build_inherit_plan(source_scripts, given_script, beat_sources)`: 줄=비트, 출처 있는 줄은 primary=대표·alternates=나머지, 없는 줄(훅·감정·가격·약속)은 앞 비트 장면의 **다음 컷**(같은 소스·시간순·미사용)으로 b-roll(`inherited=False, fit=3`). **Gemini 0회. respine·verify·repick·reconcile 호출 없음**(테스트가 고정). 화면 길이 보루만 유지. 못 이으면 None → 옛 경로 | `edit_plan.build_inherit_plan` |
| 잡의 `script_structure`가 `_plan_and_tts`까지 온다(호출부 2곳). `inherit_scenes`+`beat_sources`면 상속, 아니면 종전 | `mix_pipeline._plan_and_tts` |
| 스위치 `edl_inherit_enabled`(값 규약은 2단계와 같음) → 믹스 잡 생성 시 `script_structure.inherit_scenes=True` 표식. `_setting_gate` 공용(2단계 `_script_grounded`도 이걸 쓴다) | `app._setting_gate`, 믹스 생성 API, `_ADMIN_SETTING_KEYS` |
| 2단계 확정이 `segs`(장면 전부)도 beat_sources에 실어 보낸다 | `produce.html` s2Confirm |
| **`track/출처장면배선` d4d2fd06e 체리픽** — 저장 출구 `_apply_beat_sources`가 역할 이름 대신 **자리**로 잇는다(3%→55%). 플래그 없이 종전 경로에도 적용(그 브랜치의 테스트 포함) | `store.py` |

테스트 `test_inherit_plan.py` 5건 + `test_beat_source_follow.py`(체리픽) green. 앱 임포트·produce.html 인라인 스크립트 문법 OK.
켜는 법: 관리자 설정 `edl_inherit_enabled=admin`(사장님만) — 2단계 `script_grounded_enabled=admin`과 **같이** 켜야 출처가 온다.
그러면 1단계(B1은 아직 OFF·별도) → 2단계 본 것만 쓰기 → 3단계 상속이 사장님 계정에서 한 줄로 이어진다.

**⏭ 남은 것**: ①서버에서 사장님 계정 실사용(2·3단계 admin) → 3단계 화면에서 컷 바꾸는 횟수 세기 ②B1 서버 30편 실측 후 `frame_extract_enabled=1`
③상속 경로에서 옛 층 코드 삭제는 한 달 무사고 뒤(설계 §"통과하면") ④조립 경로(`_assembled_drafts`)는 grounded 밖.

## ✅ 6차 (2026-09-04 밤) — 시험지 자동화: 3단계 교체 기록(scene_swaps) + 화면 배지 + 대본 A/B 프로브

- 종전 `swap_log` API는 화면이 **한 번도 안 불러** 비어 있었다(grep 0). → 서버가 `scene_lab/apply`에서 적용 전후 **첫 조각이 바뀐 비트**를
  DB `scene_swaps`에 자동 기록(`edit_plan.scene_swap_rows`, `store.add_scene_swaps`). 픽 로직엔 안 쓴다(08-29 사장님 확정).
- 점수판: `GET /api/admin/scene_swaps?days=30` → 생성기별 {jobs, swaps, per_job}(잡당 손 횟수 = 시험지 점수) + 최근 행.
- 3단계 화면 배지: `📎 2단계 장면`(inherited) / `🎞 b-roll`(broll_fill) — 어느 컷이 상속인지 보인다.
- `tools/probes/script_ab_samples.py`: 같은 소스로 lite vs 3.5-flash 대본을 눈가림 짝(A/B)으로 → `out/probes/script_ab/<job>.md` + 정답 json(§10-1).
테스트 `test_scene_swaps.py` 3건. ⚠️실제 화면에서 컷을 바꿔 행이 쌓이는 것까지는 **못 돌려봤다**(로컬 DB 잡 1개·UI 조작 미실시) — 서버 실사용 때 확인.
- **실측(09-05 새벽, 로컬 앱 :8899, 인증 OFF=관리자)**: `POST scene_lab/409f894230c6/apply` beat3 `["s2-3","s2-12"]` → `{"applied":1,"swapped":1}`,
  `GET /api/admin/scene_swaps` → 행 1건(old s3-4 → new s2-3, per_job 1.0). 같은 편성 재적용 → `swapped 0`. `revert` OK.
  `/produce`에 배지 마크업 서빙 확인(문자열 1건). **브라우저에서 버튼을 눌러 본 것은 아니다**(API 호출 실측).

## ✅ 7차 (2026-09-05 새벽) — 정확도 숫자(같은 판정기·같은 4편 75구간) + 띠에 구간 번호

| 방식 | 판정기(lite, 스키마 없음) | 눈검사 |
|---|---|---|
| 기존 통째 업로드(08-15 추출본) | 45/52 = **86%** | 11건 중 7 맞음·2 부분·2 틀림(한 칸 밀림) |
| B1 띠·1호출(배치 전) | 42/75 = **56%** | s3 10번~·s2 17번~ 한 칸씩 밀림(띠 하나 건너뛰면 뒤가 전부 밀림) |
| **B1 배치 12 + 브리프** | 75/75 = **100%** | s2 16~24번 9구간 전부 일치(컨택트 시트) |
| **B1 + 띠에 `#번호 초` 표기**(현재 코드) | 75/75 = **100%** | 회귀 없음 |

★교훈: "고쳤다" 직후 재지 않았으면 56%짜리를 켰을 것이다. 판정기(lite)는 관대하니 절대값보다 **같은 잣대의 상대 비교**로 읽어라.
띠 번호(`make_strip(label=)`): 모델이 이미지 순서를 세지 않고 그림에 박힌 #번호로 seg_no를 답한다(전체 번호 → 묶음 오프셋 되돌림).
로컬 앱 헤드리스 브라우저 실측: 3단계 편집안에 배지 "2단계 장면"·"b-roll" 렌더, JS 오류 0.
⚠️ s3(27구간)는 실행마다 133~280초로 흔들린다(lite 실패→3.5-flash 폴백 시). 서버 실측 때 시간도 같이 재라.

## ✅ 8차 (2026-09-05 새벽) — 서버 실측을 SSH 없이: 관리자 API `probe/frame_accuracy` + 키 회전 + 한글경로 ffmpeg 수리

- **왜**: 회사·집 PC 모두 서버 22번 포트가 막혀 1단계 30편 실측을 못 했다. 443은 열려 있다 → 서버가 스스로 재게.
- `shopping_shorts/probe_frame_accuracy.py`: 최근 mix_jobs에서 mp4가 남은 소스 N개 → [기존 세그 vs B1 실행] 같은 판정기(전수) →
  `data/probes/frame_accuracy_<ts>.json` + 상태(진행률·요약). 백그라운드 스레드, 동시 1건. 키 없으면 사유를 상태에 남긴다.
- API(관리자): `POST /api/admin/probe/frame_accuracy {n}` 시작 / `GET …?start=1&n=30` 시작+상태(로그인된 브라우저에서 주소 한 번) / `GET …` 폴링.
- `frame_script._gemini_tag_frames`: **묶음마다 키를 새로 고른다**(라운드로빈) + 429/503이면 같은 모델을 다른 키로 1회 재시도.
  종전엔 한 영상의 모든 묶음이 같은 키 → 503 스파이크에 s3가 133~280초.
- `scene_cut`: ffmpeg/ffprobe 출력 디코딩을 UTF-8(errors=replace)로 — 한글 절대경로에서 cp949 크래시로 컷 검출이 죽던 것(로컬 전용 함정, 서버 무관).

**로컬 앱 실측(:8899, SHORTS_GEMINI_KEY 1개 주입, 4편)** — 같은 코드가 서버에서 돈다:
| 소스 | 기존 | B1 | 세그 | B1 소요 |
|---|---|---|---|---|
| s0 | 0.900 | **1.0** | 12→13 | 44s |
| s1 | 0.889 | **1.0** | 13→14 | 58s |
| s2 | 0.958 | **1.0** | 15→25 | 85s |
| s3 | 1.0 | (판정 실패 None — 27장 판정이 키 1개로 시간 초과) | 24→27 | 95s |
요약 classic_avg 0.937 / b1_avg 1.0 / b1_better 3 / classic_better 0. 전사·번역은 0자(로컬 GROQ 없음) — 서버에선 채워진다.

**서버에서 돌리는 법(사장님, 1분)**: 로그인된 브라우저에서
`https://shoppingshorts.duckdns.org/api/admin/probe/frame_accuracy?start=1&n=30` 열기 → 몇 분 뒤 같은 주소에서 `start` 빼고 새로고침 → `summary` 확인.
(키 수십 개가 돌므로 편당 20~40초 예상. 결과 파일은 서버 `shopping_shorts/data/probes/`.)

## ✅ 9차 (2026-09-05 새벽) — "100%"에 거짓이 있었다 → 실패를 숨기지 않게 + 독립 리뷰 반영

**들킨 것**: 라벨 일치를 재다가, 번호 띠 실행의 s3가 27구간 중 **24구간 묘사가 비어 있는데**(503으로 묶음 둘 실패) 판정기가
빈 묘사를 '맞음'으로 세어 100%가 찍힌 걸 발견. 배치 전용 실행의 s1도 14구간 중 12구간이 비어 있었다. → 앞서 보고한 100%는
**s1·s3에 한해 거짓**이었다(s0·s2는 눈검사로 맞음).

**고친 것**
| 무엇 | 어디 |
|---|---|
| 판정기: 빈 묘사는 대상에서 제외(`_usable`), 12장 배치(`JUDGE_BATCH`), 커버리지 80% 미만이면 점수 None | `tag_qa_frames`, `probe_frame_accuracy._judge_all` |
| 태깅: 묶음 실패 시 반으로 갈라 재시도, 빈 묘사 25% 넘으면 **태깅 실패**로 폴백(`EMPTY_FAIL_RATIO`), `tag_empty_ratio` 결과에 기록, 묶음 상대번호 응답 복구 | `frame_script` |
| 프로브: 태깅 실패는 점수 아닌 오류로, 평균은 둘 다 판정된 영상에서만(`compared`·`unjudged`) | `probe_frame_accuracy` |
| **독립 리뷰(에이전트) 반영**: 상속 계획이 게이트 재픽을 또 지나던 것 차단(H1) · 명시 출처는 첫·끝 컷 허용(H2) · 외국어 말 차단 규칙이 text_ko도 지움(M4, 08-14 규칙 유지) · 캐시에 full_text_ko(M5) · 임시폴더 정리(M6) · 장면 0개면 grounded 강등(M7) · 게이트 문구 상수화(L1) · GET 재실행 방지(L4) | 각 파일 |
| 3단계 b-roll을 규칙표(_ROLE_WANT_SHOTS)로, 구간 길이별 프레임 1/3/5장, 묶음별 키 회전 | `edit_plan`, `frame_script` |

**정직한 숫자(로컬 앱, 키 1개, 같은 판정기, 빈 묘사 따로)** — 리뷰 반영 전 실행: classic 0.911 / B1 1.0 (compared 4, B1 빈 1/79).
⚠️리뷰 반영 후 재실측은 이어서(아래 10차).
리뷰가 남긴 미결: `frame_extract_enabled`는 고객 게이트 없이 **전역**(켜면 담기·프리웜·믹스 전부) / 태그 필드 목록이 4곳에 중복(L2) /
일시오류 판정 문자열이 파일마다 따로(L2) / 통합 테스트(상속 계획이 저장출구까지) 없음(L3).

## ✅ 10차 (2026-09-05 새벽) — 리뷰 반영 후 정직한 재실측(로컬 앱, 키 1개, 같은 판정기, 빈 묘사·판정 커버리지 따로)

| 소스 | 기존 | B1 | 판정 커버리지 | B1 빈 묘사 | B1 소요 |
|---|---|---|---|---|---|
| s0 | 0.750 | **1.0** | 12/12 | 1/13 | 48s |
| s1 | 0.889 | **0.923** | 13/13 | 0/14 | 69s |
| s2 | **1.0** | 0.958 | 24/24 | 0/25 | 105s |
| s3 | 0.864 | **1.0** | 26/26 | 0/27 | 68s |
**요약: compared 4 · classic_avg 0.876 · b1_avg 0.970 · B1 우위 3 / 기존 우위 1 · unjudged 0 · B1 빈 묘사 1/79**

★앞서 보고한 "100%"는 틀렸다. 진짜는 **0.97 대 0.876**(+0.09). 판정기(lite)는 관대해 절대값은 후하고, 같은 잣대의 상대 비교로만 읽어라.
목표표 1번(묘사 95%↑)은 B1 0.97로 로컬에선 충족, 목표 3번(라벨 80%↑)은 B1 s0~s2에서 결 92~100%·쓰임 92~100%(s3는 당시 빈 묘사라 무효 → 재측정 필요).
남은 것: **서버 30편**(위 URL 한 번) · Whisper 전사(서버) · 라벨 일치 재측정(`scratchpad/label_agreement.py`를 tools/probes로 옮길 것).

## ✅ 11차 (2026-09-05 새벽) — 태깅 모델 A/B(lite 우선 vs 3.5-flash 우선) + 편차 경고

| 순서 | 판정 평균 | 판정 실패 | 빈 묘사 | 4편 소요 |
|---|---|---|---|---|
| lite-first(현재) | 0.938 (s0 0.75·s1 1.0·s2 1.0·s3 1.0) | 0 | 1 | 189s |
| flash-first | 1.0 (s0·s2·s3) | s1 판정 커버리지 1/13 → None | 1 | 306s (s0 134s, 503 재시도) |
결론: 정확도 차이는 **잡음 안**(같은 s0가 실행마다 1.0 → 0.75 → 1.0로 흔들린다), 상위 모델 우선은 1.6배 느리고 503에 더 취약.
→ **lite 우선 유지**. ★한 편 점수는 ±0.25 흔들리므로 4편으로 우열을 말하지 마라 — 서버 30편이 최소 단위다.
프로브 `tools/probes/label_agreement.py`(결·쓰임 일치) 추가. 로컬에서 클로드가 할 수 있는 항목은 여기까지 — 남은 건 전부 서버.

## ✅ 12차 (2026-09-05 오전) — 관리페이지 카드 "🎞 1단계 정확도 측정" (사장님 "주소 치는 거 말고 job처럼")
`/admin` 상단에 카드: 편수 입력 + ▶ 측정 시작 → 진행률(15초 폴링) → 요약 표(비교 편수·classic·B1·우위·미판정·빈 묘사·편당 초) + 편별 상세.
로컬 앱 헤드리스 브라우저 실측: 카드 렌더, 시작 클릭 → "측정 중… 0/1", 버튼 잠김, JS 오류 0. 서버에선 10:13에 사장님이 URL로 시작한 30편 측정이 도는 중.

## ✅ 13차 (2026-09-05 오전) — 고객 신고(cid 260 "3단계에서 계속 실패", work d43ae07da0a2) 대응 도구

- 화면 캡처의 실패 문구 "매칭 실패 — 음성 서비스가 잠시 몰려…"는 `_USER_ERROR_RULES`가 **ElevenLabs/타입캐스트 HTTP 오류 전부**(401 키·403·422 목소리·429·5xx)를
  한 문장으로 뭉갠 것 → `_TTS_VENDOR_RULES`로 원인별 분리(비정상사용 차단·권한·키 미인식·목소리 없음·몰림·장애). 잔액 소진(`quota_exceeded` 추가)은 종전 충전 안내가 먼저.
- **🩺 작업 진단 카드**(`/admin`) + `GET /api/admin/diag/work/{work|job}`: 잡 상태·원문 오류·단계·계획(생성기·비트·상속·TTS)·작업 폴더·연결 신고를 한 장에.
  → 다음 신고부터 SSH 없이 원인 조회. 이 PC는 SSH 22 막힘·크롬 확장 미연결이라 이번 건은 사장님이 조회해 붙여 주거나 확장 연결 뒤 내가 읽는다.
- 이 고객 건의 **진짜 원인은 아직 모른다**(원문 오류 미확인). 진단 카드로 `job.error` 원문을 보면 위 규칙 중 어느 것인지 바로 갈린다.

## ✅ 14차 (2026-09-05 오전) — 고객 cid 260 원인 확정 + 서버 30편 결과 + 타입캐스트 403 대응

**원인(Playwright MCP의 로그인 세션으로 관리자 API 직접 조회)**: `/api/admin/production?days=14` — 이 고객 잡 **22건 중 21건 failed, 전부**
`403 Client Error: Forbidden for url: https://api.typecast.ai/v1/text-to-speech/with-timestamps`. 등록 키(id 567, typecast)는 검사 "ok" —
검사가 `/v1/voices`(요금제 없이도 200)만 봤기 때문. 즉 **타입캐스트 API 요금제·크레딧 문제**이고 고객이 풀어야 한다(또는 기본 목소리로).
4일 전 신고(#15 "이용자가 많아 안 된다")도 같은 병이었는데 "처리됨"으로 닫혔다.

**고친 것** (`typecast_tts.synthesize`, `app._probe_typecast_synth`, `_explain_key_failure`, `_TTS_VENDOR_RULES`)
- with-timestamps 403/404 → 일반 `/v1/text-to-speech`로 한 번 더(정렬 없음 → 자막 ASR 강등, 영상은 나온다)
- 키 등록 검사 = 실제 합성 "안녕"(2크레딧). 종전 "확인 버튼이 돈 쓰면 안 된다" 결정을 **뒤집음** — 14일 막힘이 더 나쁘다
- 문구: 타입캐스트 403 → "요금제·크레딧 확인, 급하면 기본 목소리(ElevenLabs)" (종전 "키 값을 다시 확인"은 오진)
테스트 `test_typecast_403.py` 4건. ⚠️실제 타입캐스트 키로는 못 돌려봤다(로컬에 키 없음) — 서버 실사용에서 확인.

**서버 30편 정확도(10:13 시작, 완료)**: compared 25 · **classic 0.894 / B1 0.971** · B1 우위 10 / 기존 우위 4 / 동점 11 · unjudged 5 · B1 빈 묘사 24/480(5%) · 편당 29.6초.
★빈 묘사가 편마다 정확히 1개 = 끝의 아주 짧은 조각(0.03초 등)에 프레임을 못 뽑는 것 → 3구간짜리 짧은 영상은 33%로 "태깅 실패" 오판(s2). 다음 수리: 0.5초 미만 끝 조각은 앞 구간에 합치기.
목표(묘사 95%)는 B1 0.971로 **서버에서도 충족**(판정기 관대함 감안 상대 비교). 라벨 80%·전사 90%는 아직 안 쟀다.

## 15차 (2026-09-05 낮, 회사PC) — 박준영(cid 260) 403 재조사 + 배포 시간창 발견 + 1단계 수리 2건

**사장님 정정**: 박준영님은 일레븐랩스 **무료** 요금제, 타입캐스트는 **스튜디오 '일반 프로' 구독**(키·플랜·크레딧 정상이라 하심).
★타입캐스트는 스튜디오 플랜과 **API 플랜이 별개**(공식 pricing/api: 따로 구독·따로 과금, API Free=월 15k~30k 크레딧, 1글자=1크레딧).
→ 프로 구독이어도 API 플랜은 Free일 수 있고 크레딧 소진이 유력. 진단 API가 `/v1/users/me/subscription`으로 plan·크레딧을 직접 본다. → "요금제" 진단 틀림,
"ElevenLabs로 바꾸세요" 우회도 무효. 문구 둘 다 고침(app._TTS_VENDOR_RULES).

**실측(apiwatch, SSH 없이)**: typecast 합성 ok 312건(다른 키는 서버에서 정상 → 서버 IP 차단 아님) /
auth_dead 634건 전부 cid 260(09-04 17:46 KST ~ 09-05 11:11 KST). 타입캐스트 프리셋 6종 전부 `tc_` 내장 목소리
(필재·김건·박창수·용식이·문정·발키리) → "uc_ 커스텀 목소리라 남의 키로 거부" 가설은 약해짐.
문서상 403 정의 없음(402=크레딧, 404=voice 없음). **남은 가설: 그 계정의 /v1/voices 목록에 프리셋 voice_id가 없다**
(계정별 목록이 다를 수 있음) — 확정은 아래 진단 API로.

**만든 것**
- `typecast_tts.raise_with_body`: 4xx 본문을 잡 오류에 실음(종전 "403 Forbidden for url"만 남아 사유 불명)
- `GET /api/admin/diag/typecast/{cid}?voice_id=&model_id=`: 회원 키로 /v1/voices → with-timestamps → 일반 순으로 실제 합성.
  status·본문 300자·voice_in_list·own_key(키 값은 절대 안 실음). diag_work에 `voice` 스냅샷(voice_id·engine·tc_/uc_).
- 1단계: `merge_slivers`(0.5초 미만 조각 앞 구간에 합침 — 서버 30편 중 24편 끝 0.03초 빈 묘사), `transcript_status`
  (no_groq_key / audio_extract_failed / asr_none: HTTP 429 … / asr_empty) + 프로브 요약 집계, `asr_check.last_error()`.

**★배포 시간창**: `deploy/auto_deploy.sh` KST 02~06시 밖에선 shopping_shorts 커밋을 붙잡는다(09-04 16:00 도입).
오늘 10:17 이후 병합이 전부 대기라 진단 API가 404였다. `[긴급]` 제목 커밋(5be1e7dc5)으로 넘김.
갈라 보는 법: `/admin`·`/produce`는 디스크 서빙이라 새 문자열 유무 = pull 여부. (memory: 배포시간창_긴급커밋)

**⏭ 이어서**
1. finish22~24 병합 확인(`git merge-base --is-ancestor`) → 서버 `/admin`에 diagCard 뜨면 재시작 완료.
2. Playwright(관리자 세션)로 `/api/admin/diag/work/d43ae07da0a2` → voice_id 확인 → `/api/admin/diag/typecast/260?voice_id=<그 값>`
   → 403 본문·voice_in_list로 원인 확정 → 사장님께 보고, 답장은 사장님 승인 후.
3. 1단계: 서버 프로브 재측정(`/api/admin/probe/frame_accuracy?start=1&n=30&force=1`) → transcript_status 집계로 전사 0/30 원인.

## 16차 (2026-09-05 낮) — cid 260 원인 **확정**(진단 API 실측)

배포 확인(12:41 서버 재시작)됨. Playwright 관리자 세션으로 `/api/admin/diag/typecast/260` 실측:
- 타입캐스트: **plan lite, 크레딧 200k 중 51,338 사용(148,662 남음)**, with-timestamps·일반 **둘 다 200**(오디오 반환). → 타입캐스트는 **지금 정상**.
- auth_dead 634건은 09-04 08:46 ~ 09-05 02:11 구간에만 찍히고 그 뒤 멈춤(일시 거부였고 지금 회복).
- **진짜 실패는 일레븐**: 최근 잡 28034ace1c28의 목소리가 클로이차(eleven_v3)로 잡혀
  `402 Payment Required api.elevenlabs.io`(무료 계정 크레딧 소진). diag_work의 job.error가 그대로 보여줌.
- 고객 문구는 이미 정확(`_user_facing_error`가 일레븐 402 → "ElevenLabs 크레딧 부족, 충전 후 재시도").

**결론(사장님 보고)**: 이분은 일레븐 무료라 일레븐 목소리로는 계속 402. **타입캐스트 성우(ssfm)로 만들면 됨**
(타입캐스트 키·크레딧 정상). 4단계에서 목소리를 타입캐스트로 바꾸도록 안내하거나, 무료 일레븐 회원은
기본 목소리를 타입캐스트로 두는 방안 검토(별건).

**⏭ 남음**: (1) 무료 일레븐 회원 기본 목소리 정책 — 사장님 결정 사항. (2) 1단계 서버 프로브 재측정
(transcript_status·라벨 일치율은 배포됨 — `/api/admin/probe/frame_accuracy?start=1&n=30&force=1`).

## 17차 (2026-09-05 저녁, 회사PC) — 1→2→3단계를 **실영상 15편**으로 끝까지 돌려 매칭 결함 6건 수리

**왜**: 그때까지 잰 건 전부 판정기(AI가 AI 채점) 숫자뿐이고, **이 흐름이 실제로 도는 걸 본 적이 없었다.**
사장님: "진짜 분석이 잘된건지를 내가 봐야지" / "실제 영상을 분석해봐, 조각별로 나누고, 로컬에서".

### 만든 프로브 2개 (둘 다 로컬 전용, 라이브 무관)
| 도구 | 하는 일 |
|---|---|
| `tools/probes/b1_eye_check.py <video>` | B1 실행 → **띠 그림 + 그 구간 묘사**를 나란히 놓은 눈검사 HTML. 숫자 말고 사람이 본다 |
| `tools/probes/b1_full_flow.py <결과폴더> [스타일id]` | 1단계 결과 → 2단계 grounded 대본 → 3단계 상속 배치까지 **끝까지** 돌려 `flow.html`(대사↔장면↔그림) |
| `tools/probes/_style57.py` / `_style60.py` | 서버 `/api/script/styles` 원문(다이소 내부인형 / 유튜브 발명품형). **발명한 값이 아니라 라이브가 쓰는 그 형태** |

로컬 함정: SHORTS 풀 0개 PC는 예비풀 키를 **돌려 써야** 한다(하나 고정이면 429로 23구간 전부 빈 묘사) ·
`shopping_shorts/.env`가 트랙 폴더에 없으면 GROQ 키 0개(전사 no_groq_key) — `start`가 복사하는 루트 `.env`와 **다른 파일**이다.

### 수리 6건 (전부 테스트 + 되돌리면 실패 확인)
| # | 결함 | 어디 |
|---|---|---|
| 1 | **전사가 아예 안 돌던 것** — GROQ 업로드 파일명에 Path를 넘겨 requests가 `.translate()`로 죽음. 서버 30편 전사 0/30의 뿌리. 기존 테스트가 전부 str을 넘겨 놓쳤다 | `asr_check.transcribe_words` |
| 2 | **외국 영상 번역·브리프가 429에 통째로 죽던 것** — 태깅만 키 회전이 있고 번역·브리프는 **키 하나 고정**(같은 키로 모델만 바꿔 또 실패). 공용 `_call_with_key_rotation`·`_is_transient`로 3곳 통일 | `frame_script` |
| 3 | **b-roll이 대사를 한 글자도 안 읽던 것** — 결만 맞고 내용은 딴소리("스티커가 들뜬다"에 인형 팔찌). 기존 `_word_hits`(낱말 판정 단일 소스) 재사용 | `edit_plan._fill_for` |
| 4 | **훅 채점 없음** — 사장님 기준(실증+핵심 +3 / 완성·after +2 / 반전 표현 +2 / 문제 +2 / has_effect −3 / 1초 미만 −2) | `edit_plan._hook_score` |
| 5 | **훅 후보에 `실증`이 빠져 있던 것** — 규칙표는 `완성/after`만, 프롬프트는 "완성 또는 실증". **둘이 어긋나 채점을 붙여도 무력** | `_ROLE_WANT_SHOTS` |
| 6 | **게이트 CTA 어간이 `"남겨주"` 하나** — spine 57 자신의 cta 템플릿 6개 중 2개("달아주시면"·"물어봐 주시면")를 **자기 게이트가 떨어뜨렸다**. 청하는 말 6종으로 확장 | `script_gate` |

곁가지: `_build_inventory`가 `has_effect`를 안 실어 훅 감점이 무력이던 것도 수리(다른 빌더는 싣고 있었다).

### 실측 (영상 15편, 결과 JSON은 `out/probes/b1_eye/*/`에 커밋)
| 소재 | 길이/구간 | 게이트 | 매칭 |
|---|---|---|---|
| 홍대코랩 생수병 세척기(랭킹) | 30s / **19** | ✅ | **6칸 전부 다른 컷**, 말↔그림 정확 — 지금까지 최고 |
| 힌디어 태깅건(즐겨찾기) | 65s / 12 | ✅ | 번역 수리 후 `text_ko` **0/12 → 12/12**, 브리프도 채워짐 |
| 쇼핑천재 방수패드(사장님 지정) | **21s** / 9 | ✅ | 좋음. twist만 중복 |
| 손목보호대 3편(단일 제품) | 59~93s / 14~44 | ✅ | 6칸 중 4~5칸 상속 |
| 청담샵 비비크림(랭킹) | 18s / **5** | ✅ | **구간 부족 → 중복 발생** |
| 다이소 나열형 6편 | 41~68s / 14~37 | 4/6 | 물건이 계속 바뀌어 훅이 딴 제품을 집기도 |

★**"소스가 많아야 하나"의 답**: 아니다. **구간 수**가 갈림길이다(영상 길이가 아니라 컷 전환 밀도).
6칸 스타일이면 **8~10구간**은 있어야 겹치지 않는다. 21초·9구간이 18초·5구간보다 낫다.

★**장면 분석이 대본에 준 영향**(A/B/C 실측, 같은 영상·같은 스타일):
- A 전사+장면 / C 전사만 → **대본 글은 거의 같다**(원본 나레이션에 이미 다 있으니까)
- B **장면만(전사 0)** → 같은 수준의 대본이 나온다 ← 무자막·외국 영상에서 이게 전부다
- **장면 분석의 본체는 글이 아니라 매칭**(src_seg). C에는 그게 없어 3단계가 다시 추측한다

★라이브 확인: `/api/script/styles`(화면용)는 `no_cta`·`hook_3s`를 **응답에 안 싣는다**. 대본 생성은
`list_style_spines()`를 직접 써서 무사하지만, 프로브가 그 API를 쓰면 유튜브 스타일이 CTA 검사로 **영구 FAIL**한다
(서버 실제 값은 `/api/pattern/spines`로 확인: 발명품형 `no_cta:true`).

### ⏭ 남은 것
1. **같은 컷이 두 칸에 중복** — 구간이 적은 소스에서 2단계가 같은 번호를 두 줄에 적는다. 2단계에서 막아야 한다
2. **한 구간에 두 장면이 섞임** — `MAX_SPAN_SEC=7.0`이라 6.7초가 안 쪼개져 "우산에 키링을 매달고, …침실 전경"이 한 묘사에.
   상한 5초로 줄여 재측정할 것(구간 수·묘사 품질을 함께 본다)
3. 소스 2편 이상 섞으면 중복이 사라지는지
4. 캐시가 `shortcode` 단일 키(`get_extract`)라 **"사장님만 켜기" 게이트가 캐시로 샌다** — 방식 구분을 키에 넣어야 한다
5. 서버 프로브 재측정(전사 수리 반영), `frame_extract_enabled` 켜기 판단

## 18차 (2026-09-05 밤, 집PC) — 남은 것 1번 **같은 컷이 두 칸에 중복** 해결

**실측이 원인 짐작을 뒤집었다.** 17차 때 "구간이 적은 소스에서 난다"고 적었지만 15편 flow.json을 세어 보니:

| 근거 | 숫자 |
|---|---|
| 장면 붙은 칸 | 66개 중 **중복 6개(9%)** |
| 중복 6건 중 **이웃한 두 칸** | **5건** |
| 44구간 영상(tmp_src15)에서 중복 | 발생 |
| 44구간 영상(tmp_src9)에서 중복 | 0 |
| 15편 중 재고<필요칸(중복 불가피) | **0편** |

→ **재고 부족이 아니다.** 진짜 원인: `_GROUNDED_RULE`에 "같은 장면 두 번 쓰지 마라"가 **없었고**
`scene_grounding_check`도 **안 봤다**(지시·판정 둘 다 부재). 44개가 있어도 모델이 같은 걸 또 집었다.

### 고친 것 — 지시와 판정을 **짝으로**(메모리 `규칙은있는데_판정이없다`의 반대 짝)
| 무엇 | 어디 |
|---|---|
| 지시: "**한 장면은 한 줄에만** — 앞줄 대표 번호를 뒷줄에서 또 대표로 쓰지 마라(보조는 겹쳐도 된다)" | `script_generate._GROUNDED_RULE` |
| 판정: 대표 장면(`sids[0]`)만 보고 중복 시 반려 + 어느 칸끼리인지 지시문에 실어 재작성 | `script_gate.scene_grounding_check` |
| 면제: `len(ids) >= with_scene`이 아니면(재고<칸) 통과 — 장면 부족 소재를 영영 반려하지 않는다. 레시피도 면제 | 같은 곳 |

### 검증 (0순위-A1 — 실제로 돌렸다)
- 테스트 5건 신설, `test_script_grounded.py` 17건 green. **사보타주로 판정을 끄면 실패**(가짜 통과 아님).
- 관련 스위트 82건 green(script_grounded·b1_frame_fixes·inherit_plan·gate_correction_loop·gate_no_cta_style·script_gate_topic·edit_plan·edit_plan_pure).
- **실측 15편에 새 판정 적용: 중복 6편 전부 잡고 멀쩡한 9편은 통과(오탐 0)**.
- **실제 대본 재생성 2편**(모델 호출):
  - tmp_src15(44구간, 종전 s0-38 두 칸) → s0-36/s0-37/s0-39 **중복 0**, 게이트 3회 시도 후 통과 = 반려→재작성이 실제로 작동
  - tmp_src14(**구간 5개** 빠듯) → s0-1/s0-3/s0-4 **중복 0**, **1회 통과**(지시문만으로 해결)

⚠️로컬 함정 재확인: `shopping_shorts/.env` 없어 예비풀 키 **1개** — 여러 편 연속 실행은 429. 2편만 실측.
⚠️편집 함정: heredoc으로 `
`을 파일에 쓰면 실제 개행으로 풀려 문자열이 두 동강 난다(이번에 밟음, 메모리 `heredoc_개행이스케이프_붕괴`).

### ⏭ 남은 것 (17차 목록에서 1번 완료, 나머지 그대로)
2. 한 구간에 두 장면 섞임 — `MAX_SPAN_SEC=7.0` → 5초 재측정. **실측: 372구간 중 5초 초과 57개(15%), 7초 초과 0개**
   (전부 강제분할 조각). tmp_src21은 5구간 중 5개가 초과 = 화면이 거의 안 바뀌는 소재
3. 소스 2편 이상 섞으면 중복이 사라지는지
4. 캐시가 `shortcode` 단일 키라 "사장님만 켜기" 게이트가 캐시로 샌다
5. 서버 프로브 재측정(전사 수리 반영), `frame_extract_enabled` 켜기 판단

## 19차 (2026-09-05 밤, 집PC) — 남은 것 **2번 실측 후 보류 · 4번 캐시 방식 구분 해결**

### 2번(한 구간에 두 장면 섞임) — 재보니 작은 문제라 **5초로 줄이지 않기로**
| 단계 | 숫자 |
|---|---|
| 전체 구간 | 372개 |
| 5초 초과(강제분할 조각) | 55개 · **7초 초과는 0개** |
| 그중 묘사가 "전환된다"고 **스스로 말하는 것** | **7개** |
| 그중 **실제 대본에 쓰인 것** | **3개(0.8%)** |

⚠️첫 판정("~하고/~며"까지 섞임으로 셈)은 31%였는데 **과장이었다** — 한 동작을 자세히 쓴 것까지 셌다.
정직한 지문은 "전환·바뀌며·이어서·비추기". 5초로 줄이면 55개가 더 잘게 쪼개져 **호출·비용이 늘고
18차에서 고친 중복이 되살아날 위험**이 있는데 얻는 건 3개 → 사장님과 상의해 **보류**(숙제로 남김).

### 4번 — 캐시가 방식을 구분하지 않아 **켜도 안 켜지고 꺼도 안 꺼지던 것**
핸드오프엔 "사장님만 켜기가 샌다"고 적혀 있었으나 실측하니 `frame_extract_enabled`엔 **admin 옵션이 없다**
(`== "1"`만 본다 — `script_extract._frame_flag_on`). 진짜 문제는 더 컸다: 캐시 키가 `shortcode` 하나뿐이라

| 상황 | 종전 동작 |
|---|---|
| 켜기 전 분석한 영상을 켠 뒤 다시 열면 | **옛 방식 결과가 그대로** (B1이 안 돈다) |
| 켰다가 끄면 | **B1 결과가 계속** (되돌려지지 않는다) |

→ 5번(서버 재측정 후 켜기 판단)이 여기 막힌다. 켰는데 옛 결과가 나오면 "효과 없다"고 잘못 읽는다.

| 고친 것 | 위치 |
|---|---|
| `script_extracts.extract_method` 컬럼 추가(마이그레이션). **NULL = classic으로 친다** — 전부 무효화하면 라이브 재추출 폭풍·크레딧 소모 | `store.py` 마이그레이션 |
| `save_script(..., method=)` 저장, `get_extract(..., method=)`는 **방식이 다르면 None**(=캐시 미스). method 안 주면 종전과 동일(회귀 0) | `store.py` |
| **방식 이름 단일 출처** `script_extract.current_method()` + `METHOD_CLASSIC/FRAMES` — 호출부 8곳이 각자 판단하면 반드시 어긋난다(0순위-B) | `script_extract.py` |
| 배선: 재추출을 결정하는 조회 4곳·저장 5곳에 `method=` (app.py 7 + prewarm.py 2) | `app.py`, `prewarm.py` |

★**읽기 전용 소비처엔 일부러 안 붙였다**(`_coupang_evidence`·`_extract_as_source_item`·`_enrich_job_extract`·
`api_product_prefetch_retry`) — 붙이면 멀쩡히 저장된 대본을 "방식이 다르다"고 못 읽어 쿠팡 근거·AI PICK이
조용히 죽는다. 테스트가 **양방향으로** 지킨다(붙어야 할 곳 / 붙으면 안 되는 곳).

### 검증 (0순위-A1)
- 새 테스트 9건. **사보타주 3회로 진짜 잡는지 확인**: 제작소 조회에서 `method=` 제거 → 실패 ✅ /
  읽기 전용에 `method=` 추가 → 실패 ✅
- ⚠️**첫 배선 테스트는 가짜였다** — 내가 지어낸 함수 이름(`extract_from_url` 등)이 실제와 달라
  사보타주해도 통과했다. AST로 실제 이름을 뽑아 교정(`api_produce_extract_from_url`·`run_prewarm` 등).
- 실제 Store(임시 DB)로 5개 시나리오 재현: 켜기→캐시 미스 ✅ / B1 저장 후 조회 ✅ / 끄기→미스 ✅ /
  옛 행 classic 그대로 씀 ✅ / 옛 행 frames는 재추출 ✅
- 관련 스위트 111건 green, `app.py` import OK(라우트 434), `prewarm` import OK.

### ⏭ 남은 것
2. (보류) 한 구간에 두 장면 섞임 — 실측 3/372. 5초 상한은 비용·중복 재발 위험이 커서 미실행
3. ~~소스 2편 이상~~ → **20차에서 완료**
5. **서버 프로브 재측정 → `frame_extract_enabled` 켜기 판단** ← 이제 캐시가 안 막는다

## 20차 (2026-09-05 밤, 집PC) — 남은 것 **3번: 소스 2편 이상** + 그때 드러난 소스 편중 수리

### 만든 것: `tools/probes/b1_two_sources.py`
소스 2편을 **라이브 형태 그대로** 섞어 1→2→3단계를 끝까지 돌린다.
★`seg_id`를 소스마다 다시 매긴다(`s0-*`/`s1-*`) — 라이브는 `_assign_seg_ids(video_id, …)`가 그렇게 붙이는데
result.json은 둘 다 `s0-`으로 저장돼 있어 그냥 섞으면 **번호가 충돌해 엉뚱한 소스의 장면을 집는다**.

### 3번의 답: **중복은 2편에서도 0** (3조합 실측)
| 조합 | 재고 | 2단계 중복 | 3단계 |
|---|---|---|---|
| 빠듯 2편(5+9) | 14구간 | 0 | 성공 |
| 넉넉 2편(19+22) | 41구간 | 0 | 성공 |
| 같은 제품 2편(14+44) | 58구간 | 0 | 성공 |

### 그 과정에서 발견 — **2단계가 늘 첫 소스만 썼다**
3조합 **전부** 2단계가 고른 대표 장면이 s0뿐이었다(58구간짜리에서도 s1 사용 0).
사장님 지시(2026-08-17, `_sources_for_generate` 주석)와 정면으로 어긋난다:
> "한 편만 넣으면 그 한 편에 끌려가 편협해진다. 담긴 것을 다 넣으면 고를 일이 없어진다 = 복불복이 사라진다"

원인은 **18차 중복과 똑같은 병** — 지시도 판정도 없어 앞에서부터 채우면 그만이었다.

| 고친 것 | 위치 |
|---|---|
| 지시: "재료 대본이 **여러 영상**이면 장면도 여러 영상에서 골라 써라. **한 영상에서만** 다 가져오지 마라. 다만 소재가 다른 제품이면 억지로 섞지 말고 그 줄에 맞는 장면을" | `_GROUNDED_RULE` |
| 판정: `source_count` 인자 — 장면 가진 소스가 2편 이상인데 대표를 한 소스에서만 뽑았으면 반려(어느 소스를 안 썼는지 지시문에) | `script_gate.scene_grounding_check` |
| 면제: 장면 붙은 줄 < 소스 수면 불가능하므로 통과. 레시피 면제. `source_count` 안 주면 종전과 동일 | 같은 곳 |
| 배선: `script_gate.check(source_count=)` → `scene_grounding_check`, `generate_one_style`이 **장면을 실제로 가진 소스 수**를 센다(장면 없는 소스는 고를 수 없으니 제외) | `script_gate`, `script_generate` 2곳 |

### 검증 (0순위-A1)
- 새 테스트 9건(판정 7 + **배선 2**). 관련 스위트 **133건 green**.
- ★배선 테스트가 필요했던 이유: 판정 테스트만 있을 땐 **배선을 지워도 7건이 통과**했다(사보타주로 확인).
  AST로 `script_gate.check`·`scene_grounding_check` 호출에 `source_count=`가 있는지 본다 → 지우면 빨개진다 ✅
- **실제 대본 재생성(모델 호출) 2조합**:
  - 손목보호대 2편(14+44) — 종전 s0만 5개 → **s0 2 + s1 2**, 게이트 False(4회) → **True(2회)**, 중복 0.
    2번 영상에서만 나오는 사실이 대본에 들어왔다("두께가 얇아 일상 착용 불편 없음"·"좌우 구분 없이 양손")
  - 비비크림+src40(5+9) — **s0 3 + s1 2**, 중복 0, 3단계 s0 4 + s1 4
  - ⚠️남은 게이트 실패는 **문체 검사**("심지어" 고조 연결어)로 소스 분산과 무관

### 곁: 사장님 질문에 대한 답(기록)
- **"새 방식은 창의성이 떨어지나"** → 17차 A/B/C 실측상 **대본 글은 A(말+장면)와 C(말만)가 거의 같다**.
  제한하는 건 '어느 화면을 붙일지'이지 '어떤 말을 쓸지'가 아니다(훅·감정·가격·마무리는 `needs_scene=false`로 자유).
  단 창의성을 점수로 잰 것은 아니다 — **켜기 전 사장님이 직접 몇 편 뽑아 보는 것이 가장 확실**하다.
- **"이런 것 없이 지금까지 어떻게 했나"** → 1·2·3단계 스위치가 **전부 꺼져 있다**(실측: 빈 값).
  오늘 고친 것들은 전부 **아직 안 켠 새 경로 안**의 문제이고 고객 대본은 옛 경로로 나간다.

## 21차 (2026-09-05 밤) — 병합 게이트가 막은 회귀 4건 수리 + 소스분산 판정 완화

### 게이트가 라이브를 지켰다 (`finish` 중단, exit 0이지만 **미병합**)
`git merge-base --is-ancestor`로 확인 = 미병합. 로그의 `중단:` 줄에 실패 4건.
기준선 대조(origin/main 워크트리): **30 passed** → 4건 전부 **우리 트랙에서 생긴 회귀**.

| 실패 | 원인 | 누가 |
|---|---|---|
| `test_prewarm_transient` 1건 | 테스트의 `FakeStore.get_extract(self, code)`가 새 `method=`를 못 받음 → 라이브는 TypeError로 죽었을 것 | 19차(내) |
| `test_role_shot_match` 3건 | `_LIVE_SHOT_ROLES`를 **손으로 적어둔 목록**(7개)이라 코드에 `실증`이 추가된 뒤 어긋남 → **멀쩡한 코드를 테스트가 반려** | 17차(회사) |

수리:
- `FakeStore.get_extract(self, code, method=None)` — 진짜 Store와 인자 모양을 맞춘다
- `_LIVE_SHOT_ROLES = set(_shot_roles.SHOT_ROLES) | {"조리"}` — **정의처에서 가져온다**(0순위-B).
  손 목록은 어휘가 늘 때마다 반드시 썩는다
- 훅 단언을 지시문에 맞춘다: `shots[:2] == ("완성","after")` + `"실증" in shots`.
  사전 지시(`_SCENE_PLACEMENT_RULES`)는 처음부터 "**'역할:완성' 또는 '실증:Y'**로 연다"였는데
  규칙표만 좁아 **지시대로 한 후보가 판정에서 벌받고 있었다**. 사보타주로 단언이 여전히 진짜임을 확인.

### 20차 소스분산 판정을 완화했다 — **내가 낸 결함**
사장님이 최종 매칭을 보시고 지적: 비비크림 대본에 방수패드 화면이 붙었다.
실측 원인: 판정이 "두 소스를 **대표로** 다 썼나"만 봐서, 소재가 다른 제품이면 모델이 통과하려고
**억지로 섞었다**(8칸 중 4칸이 딴 제품). 지시문엔 이미 "다른 제품이면 억지로 섞지 마라"가 있는데
판정이 그걸 안 봤다 — 지시와 판정이 다른 축(0순위-B).

→ 판정은 **대표+보조 전부**로 본다. 다른 소스를 **아예 안 봤을 때만** 반려한다.

| 실측(같은 조합 재생성) | 완화 전 | 완화 후 |
|---|---|---|
| 비비크림+방수패드: 딴 제품 화면 | **4칸** | **1칸**(가격 줄만 — 특정 화면이 필요 없는 자리) |
| 핵심 줄(시연·결과) | 딴 제품 섞임 | **전부 비비크림** |
| 손목보호대 2편(같은 제품) | s0 2+s1 2 | **s0 3+s1 2** (중복 0, 매칭 정확) |

테스트 +2(완화 후 통과 / 아예 안 보면 여전히 반려). 관련 스위트 **153건 green**.
