## 2026-09-05 — 장면편집 ↩ 되돌리기 (한 단계씩)
- scene_lab에 되돌리기 스택. render() 끝의 saveWork() **한 출구**에만 달았다(0순위-B).
- loadWork 복원부를 applyWork(w)로 분리 — 저장본 불러오기와 되돌리기가 같은 함수를 쓴다.
- snapWork에서 at(시각) 제외(안 그러면 매번 달라져 "변경 없음" 판정이 무력화). BUILD_TAG F29.
- ⚠️ 라이브 실측은 못 함 — 자동배포 시간창(2~6시) 밖이라 대기 중. 다음 세션이 확인.
