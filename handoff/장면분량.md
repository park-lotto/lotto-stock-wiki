# 장면분량 — 핸드오프

> 소유 트랙: 장면분량 — 이 파일은 이 트랙 세션만 수정한다. 다른 트랙은 읽기만.
> 영역 분담(2026-09-16 사장님): **이 트랙 = 대본을 장면에 매칭(3단계 상속·채우기·scene_grounding_check)** /
> 대본게이트 트랙 = 대본 생성·FATAL_CHECKS. `scene_grounding_check`를 바꿀 때 `scene_descs` 인자만 두면 안 부딪힌다.

_최종: 2026-09-18 아침(집 밤샘) · 커밋 `3ec6e2fcb`까지 · **회사에서 이어서 한다**_

---

## 🔴 2026-09-18 집 세션 — 여기부터 읽어라 (회사에서 이어할 것)

### 한 것 (전부 실측 확인)

| # | 무엇 | 결과 |
|---|---|---|
| 1 | **컷 밋밋함 수리** | 컷당 6.5초 → **1.76초**, 7컷 → **14컷** |
| 2 | **효과음 자산 투입** | 뇌전구 팩 79개 등록, 배치 **0/7칸 → 7/7칸** |
| 3 | **효과음 이븐쇼핑 맞춤** | 길이 중앙 **96ms**(기준 80ms), 200ms이하 **100%** |
| 4 | **대본 프리셋** | 스파인 55에 원문 뼈대 6~10개씩 → 조합 **2,304만** |
| 5 | **어미 판정 게이트** | 같은 어미 3줄 연속 반려 |
| 6 | **고객 상속 ON** | `edl_inherit_enabled` admin → **1** (라이브 적용) |
| 7 | **규칙 A0 신설·A1 확대** | CLAUDE.md |

### ★뿌리 3개 (다음에 또 헷갈릴 것)

1. **밋밋함은 백본 경로 전용 문제였다.** 최근 60잡 실측: 줄당 컷 중앙값 **3.0**으로 일반 경로는 원래 정상.
   컷 1개가 절반 넘는 잡 4개(7%)가 **전부 백본 실증본**이었다. 실사용 경로(2단계 `src_seg`)는 모델이
   줄당 **2~4개**를 잘 적는다 — 내가 "구조적으로 1개"라고 단정한 건 `src_segs`를 안 본 오판.
2. **렌더에는 2.2초 라운드로빈이 이미 있다**(`_plan_beat_clips:940`) — 단 `len(segments)>1`일 때만.
   컷이 1개면 else로 빠져 통째 재생된다. 그래서 고칠 자리는 렌더가 아니라 **컷 배급**이었다.
3. **어색한 어미는 내가 조립한 것이었다.** `~해 버림`·`~는 거임`·`~였음`은 이븐쇼핑 원문에 없다.
   원문 13편에서 문장을 통째로 떼어 쓰니 해결(`~라는데/~다는 거/~는데/~이라고`).

### ★씨앗 구조 — 내가 두 번 틀린 곳

- **씨앗은 "대본 결"을 주지 않는다.** 실사례 work `40148f06f529`(루프 이어플러그): 씨앗이
  `grab_tiktok_b980ca6ad3ad`인데 **해외 틱톡**이고 대본 원문은 **`🎵`** 한 글자다.
  그런데 결과 대본은 이븐쇼핑 은폐형(`~임`, CTA 없음) — **말투·골격은 선택한 스파인이 정한다.**
  씨앗은 **제품·소재를 고정**하는 역할이다.
- ⚠️내 실패: 해외 소스 중 말 많은 것을 씨앗으로 골라 돌렸더니, 중국 밤 보관법 영상에서 '지퍼백'을
  제품으로 뽑아 **없는 천재·떼돈을 지어냈다**(job `sd31b299ef77`). 소재와 스파인이 안 맞으면 이렇게 된다.
- 사장님 미해결 질문: **"밤 보관법 소재에 발명품형을 고르는 고객이 있을 수 있잖아"** — 아직 아무도 안 막는다.

### 실측 수치 (근거)

- 이븐쇼핑 효과음: 컷 경계 그 자리(Δ중앙 **0ms**), 반짝 **80%**/붐 17%, 길이 중앙 **80ms**
- 뇌전구 팩 79개: 길이 중앙 **421ms**·200ms이하 **29%** → 그냥 쓰면 1.5초 드럼이 컷을 덮는다.
  **200ms 이하 23개만** 쓰도록 `_SFX_MAX_SECS=0.20`으로 거른다(이름 아닌 **잰 길이**로)
- 훅 효과음 = **`pop4`** (뇌전구 8편 전 구간 동일, `i%4==0`)
- 어미 분포(히트작 200편·문장 986개): 최다 `~음` **9.3%** — 어느 어미도 10%를 안 넘는다
- 대본 코퍼스: `script_wiki` **4,986편**(인스타 2,657·유튜브 916) + 히트작 자막 200편

### ⏭ 회사에서 이어할 것

1. **프리셋 대량화** — 55번만 했다. 나머지 유튜브형 7개(68·67·66·65·69·60·56)와 **인스타 스파인**도.
   재료는 이미 있다: `raw/analysis/썰쇼핑_히트작200_2026-08-20/hits_subs.json` + `script_wiki` 4,986편.
   오용형 재료는 이미 캐뒀다(title 47·origin 26·notice 22) — **56번에 넣으면 된다**(근거 4편뿐이라 빈약).
2. **모델 없이 껍데기 채우기** — 시험은 했는데 **어간+어미 결합이 깨진다**(`끝는데`·`만들다는 거`).
   조사는 받침으로 자동 선택하면 되지만(성공), 활용은 형태소 수준이라 코드로 다 못 잡는다.
   → `{효능}`을 **어미까지 완성된 형태**로 받는 쪽이 현실적이다.
3. **소재↔스파인 불일치** — 사장님 판단 필요(경고만 할지, 자동 조정할지, 그냥 둘지)
4. 밈 **0/7칸** — 57장 등록했는데 안 붙는다. 컷어웨이는 Gemini가 "나레이션이 말하는 그 사물을
   실제로 보여주는가"로 판정하는데 pepe 밈은 제품을 안 보여줘 전부 탈락하는 것으로 보인다(미확인)

### 산출물 (바탕화면 `백본확인/`)

영상 9편 — `씨앗_해외8편_밤보관.mp4`(전 과정 실증) · `효과음_이븐쇼핑맞춤.mp4` · `조각냄_수리본.mp4` ·
`A2_유튜브_골격적용.mp4`/`B2_인스타_골격적용.mp4`(스타일 비교) · `씨앗원본_s4_밤보관법.mp4`(원본) 외
대본 3건 — `이븐쇼핑_대본10개.txt`(코드만으로 생성) · `이븐쇼핑_모델생성5개.txt` · `케이크장난감_세트5개.txt`

---

## 결론 먼저 (2026-09-16 기록 — 아래는 그대로 둔다)

1. **근본 방향이 틀렸다** — 사장님이 07-19에 "문장마다 장면 찾기 자체가 오류"라고 했는데, 그 뒤 두 달간
   14번 전부 "문장↔장면 매칭을 더 잘하는 법"이었다(오늘 내 것 포함). 채우기(`_fill_beat_screen_time`)는
   07-31·07-31·08-10·08-16·08-18·08-26 여섯 번 고쳤다. 전부 두더지.
2. **맞는 방향 = 백본-먼저**(07-21 설계 `docs/superpowers/specs/2026-07-21-대본학습층-응용-백본인터리브-팩토리-design.md`,
   "다음 최우선"이라 적혀 있었으나 미구현). 오늘 **손으로 1편 실증 성공** — job `bb1cc3402937`.
3. 사장님이 정리한 구조: **`[훅] + [특징1..N] + [CTA]`. 각색 = 훅 갈아끼우기 + 특징 순서 섞기 + 특징별 말 바꿔쓰기 +
   화면은 서브 우선.** 특징마다 원본에 (말, 화면) 짝이 이미 있어 빈칸이 안 생긴다.

## 오늘 실측 (전부 서버 reference.db·실제 job)

### 사장님 제보 job 26698eb0a362 (팬케이크) — 왜 중복·불일치가 났나
- 2단계 지목: 10줄 → 10컷 12.9초 / 대사 24.8초 = **결손 52%**
- 3단계 `_fill_beat_screen_time`이 빈 11.9초를 16컷으로 메움 → 최종 26컷, 같은 그림 15/26
- 대조군 68dc7e13dc23(볼펜): 결손 17% → 중복 0. **결손률과 덧붙은 컷이 정확히 비례**
- 뿌리 2: `build_inherit_plan`(09-04 신설)만 배속 보정을 못 받아 목표 초 **1.7배** 과대(실측 9.69자/초 vs 5.7 계산).
  08-10에 4곳 고치고 7곳 빠뜨린 것. → `edit_plan.narr_secs()` 단일 출처로 상속 경로만 통일(**유지, 라이브**)

### 시도하고 철회한 것 — 2단계 분량 요구 + 게이트 판정
같은 조건(재료 26698·스파인 52·grounded) 4회: 분량판정 있음 83% / 충돌제거 83% / 철회본 78% / 무관판정OFF 80%.
**차이 없음.** 모델이 지시를 안 따르고 게이트는 3회 반려 뒤 passed=False로 그냥 돌려준다.
→ 메모리 `reference_prompt_says_but_nobody_checks` 그대로. 철회 커밋 e2904f384.
⚠️ 내가 중간에 "역효과(48→83%)"라고 보고한 건 **오판** — 원본은 given_script 경로, 나는 생성 경로를 재고 있었다.

### 백본-먼저 실증 — job bb1cc3402937 (한셉트 제로 MK2, 원본 tjhKrXqsTGI)
- 재료: 기존 job 68dc7e13dc23의 URL 7편(원본 유튜브 1 + 렌즈로 담은 서브 6) 그대로
- 대본 9줄 손으로(훅 [66] 답답했잖아 + 특징 순서 지갑→접착제→육각형→펜촉→54부품→정밀→거치대→CTA)
- `create_mix_job(given_script=, script_structure={beat_sources:[{role,seg,segs}], inherit_scenes:True}, customer_id=0)`
  → `run_mix_job` → `run_render`. 스크립트 `out/backbone_job_mk2.py`(서버 /tmp에도)
- 결과: **상속 9/9 · 지정 11컷 → 최종 13컷 · 같은 그림 0 · 빈칸 0**(화면이 대사의 2.2배 여유). 23.6초 1080×1920
- 산출물(out/ + 바탕화면): `백본_한셉트제로MK2.html`(뼈대+훅4종) · `특징정리_한셉트제로MK2/index.html`(46특징→10묶음, 컷 이미지 36장)
  · `백본실증_한셉트MK2_bb1cc3402937.mp4` · `비교_원본vs각색_한셉트MK2.mp4`(좌 원본/우 각색, 소리 좌우 채널)

### 41컷 눈 판정 (컷판정_시트.jpg)
- 그대로 씀 **21**(틱톡 0ddb76·422b8d·8f40be·a427b7) / 크롭하면 씀 **16**(원본 전부 — 상단 뉴스 UI **15%**=165행, 남는 608×915)
  / 폐기 **4**(be4dc2 2컷 상단 **53%** 일본어 로고 인트로 · xhs 2컷 다른 제품 피젯펜)
- 앵커 5동작 중 접기·육각형·펼치기는 서브 깨끗한 컷으로 덮이고, **펜촉·필기만 원본 크롭 필요**
- ★태깅 스키마에 "화면 가림(UI/로고/큰 자막)" 판정이 **없다**(is_key·shot_role·scene_desc·label·use_point·change·action·text뿐)

## 밤에 더 한 것 (2026-09-16 저녁~밤)

- **재렌더 v2·v4 완료** — 로고 컷(be4dc2) → 422b8d-1 교체 + 원본 3컷 상단 크롭(`out/backbone_job_mk2_v2.py`) + 이븐쇼핑 프레임.
  ★원본 UI는 **30%**(눈으로 잰 것 — 픽셀 휴리스틱은 두 번 다 틀렸다: 흰 바만 15%, 파란 바가 채도를 속임).
  이븐쇼핑 프레임(제목 포함) 불투명은 **17%**라 혼자선 못 덮고, 크롭 15%+프레임 17%로 덮었다(v4 `bb16f14a984d` 깨끗).
  ★deco 저장은 `update_mix_job(deco=dict)` — `deco_json=`은 화이트리스트에 없어 **에러 없이 무시**된다(코드 주석에 적힌 함정, 렌더 1회 헛돎).
- **조립 자동화 완성·실증** `shopping_shorts/backbone_assemble.py`(커밋 c7fa690d9) — 모델 2회(특징 묶기+순서 / 문장) + 코드(컷 지정).
  자동 job `ba630a537511`: 묶음 5 · 7줄 전부 서브 화면 · **7컷→7컷 덧붙음 0 · 같은 그림 0** · 24.8초 렌더. 서버 `/tmp/bb_auto.py`로 돌렸고 API/UI 배선은 없다.
  사장님: "**자동은 나중에**" — 트랙 브랜치에 커밋만. 남은 구멍: a427b7-0 'Hancept Z' 워터마크가 들어감(화면가림 태깅 필요) · 제목이 제품명 그대로라 2줄.

## 마무리 결론 (2026-09-16 밤, 사장님 "자동은 나중에, 대본·매칭 마무리")

- **채우기 정렬 수정 병합** — 낱말 겹침(09-08 기각) 제거, 지목 컷 바로 다음 컷 우선. 실측 팬케이크 26→23컷·볼펜 20→16컷·손조립 역행 2→0. **소폭.**
  '같은 그림'을 맨 앞 기준으로 올렸다가 되돌림(다른 소스 0.9초 조각이 앞으로 와 08-18 "눈 아프다" 충돌). 같은 그림 회피는 같은 소스 안에서만.
- ★**"대본 끝 = 매칭 끝"은 아직 아니다.** 2단계가 줄당 컷 1개(0.6~1초)만 주고 대사는 2.5초라 빈칸이 구조적. 채우기를 어떻게 고쳐도 땜질.
  재료가 대사보다 짧으면(팬케이크: 주 소스 19s vs 대사 24.8s) 어떤 채우기도 못 살린다.
- 오늘 빈칸 0을 만든 유일한 방법 = **코드가 앞에서 컷을 대사 길이만큼 지정**(`backbone_assemble.assign_cuts`, 자동 job 7→7컷).
- 고객 완성본 2개 눈 확인(63dba 미싱기·f6d32 대파): 숫자는 엉망(덧붙 20·15)이지만 재료 1~2편이라 팬케이크만큼 안 튄다.
  눈에 걸리는 건 **원본 하드자막·워터마크·같은 그림 반복** — 매칭보다 이게 먼저 보인다.

## 2026-09-17 낮(회사) — 컷 이어붙이기 통합 + ★집 세션 작업 흡수

- ★**집 세션(09-16 22:59~09-17 11:37)이 이 트랙 브랜치에 커밋 5개**를 남겼는데 핸드오프는 안 고쳤다(커밋 메시지가 기록).
  나는 낮에 "집 작업 없다"고 답했다 — **origin/main만 보고 origin/track/장면분량을 안 봤다**(규칙 A0 위반 그대로).
  · `ffad9c56a` 컷 이어붙이기(1차 루프 안) + `MIN_GOOD_CUT_SECS=1.2` 정본(컷 100,658개 실측: 중앙 1.67s·1.2 미만 29.3%)
  · `48aceec59` 조립기 줄당 컷 2개 이상(`MIN_CUTS_PER_LINE`) — "밋밋함" 7→18컷, mp4 미확인
  · `7881b089d` 효과음 벤치마크 순환(뇌전구 팩 79개 등록, `_SFX_MAX_SECS` 0.2) + **라이브 `edl_inherit_enabled` admin→1**(사장님 지시, A/B 3건)
  · `55a25ace0` CLAUDE.md 0순위-A0 신설·A1 확대
- 나도 같은 날 같은 자리에 컷 이어붙이기를 만들었다(`6023c7975`, 2차 패스·2.2초 상한). 병합은 충돌 없이 됐지만 **두 구현이 공존** →
  집 1차 블록을 걷어내고 정책(1.2초 뒤로 밀기·막지 않음·실측 근거)을 내 `_extend_refs_to_narration`에 이식.
  ★집의 1차 시점은 기존 테스트 2개를 깬다(스크래치 실측: 앞 줄이 훅·CTA b-roll 후보를 먹어 CTA s0-6→s0-4). 집은 게이트를 안 돌렸다.
- 집의 `scene_fill_audit.py` 확장(조각남 축 avg_cut_secs·short_pct)에 `MIN_GOOD_CUT_SECS` import 누락 → 폴백 import로 수리.
- 실측(합친 코드, 서버 사본 재편성): 팬케이크 26→23컷·역행 9→6·짧은컷 46→52% / 볼펜 20→19·짧 30→47% / 손조립 13→18·짧 15→6%.
  **소폭·혼재** — 재료가 짧은 컷 위주면 이어붙일수록 짧은 컷이 는다. 상속 경로의 한계 = 재료. ⚠️mp4는 집도 나도 못 봤다.
- 라이브 스위치 실측: `edl_inherit_enabled=1` · `script_grounded_enabled=admin`(고객은 2단계 지목이 없어 상속할 게 없다 — 켜나 안 켜나 동일)

### ★배포 전 실측 결과 (09-17 저녁) — "나아졌나?" → **아니다**
사장님 "뭘 배포를 해 테스트 검증도 안 하고" → 새 코드(사본)로 팬케이크 26698을 실제 렌더(`vfca7ee0ced3`, 25.2s)하고
어제 편성(26컷)도 라이브 코드로 렌더해 나란히 붙였다(`out/팬케이크_비교_옛코드vs새코드.mp4`).
**화면으로는 구분이 안 간다** — 반죽 섞기가 뒤에 튀어나오는 것·팬 위 팬케이크 반복·원본 하드자막(일/한) 노출 전부 그대로.
숫자 26→23컷은 눈에 안 보이는 차이. **사장님 제보는 못 잡았다.** 효과가 난 건 서브 컷이 긴 케이스(손조립·자동조립 — "늘어짐")뿐.
이유: 대사 24.8s > 주 소스 19s, 대본이 주 소스 13컷 중 10개를 이미 지정 → 이을 이웃 컷이 없어 다른 소스로 감.
**배포 보류 — 사장님 판단 대기.** 회귀는 없으니(466) 내보내도 나빠지진 않지만 "고쳐졌다"는 아니다.

## ⏭ 다음 (순서대로)

0-a. ~~배포 뒤 재렌더 확인~~ → **배포 전에 했다(위). 결과: 제보 미해결.**
0-b. `script_grounded_enabled`를 고객에게 켤지 — 사장님 결정. 상속만 켜선 고객 결손 100%가 그대로다.
0. ~~`assign_cuts`를 2단계 경로에~~ → 컷 이어붙이기로 **완료**(위). 남은 근본은 "같은 동작" 정보(특징 묶음) → ③ 백본 대본을 2단계에 — 2단계가 src_seg 1개를 주면 코드가 같은 소스 시간순 다음 컷을
   대사 길이(`narr_secs`)만큼 이어 붙여 beat_sources로 넘긴다. 자리는 `build_inherit_plan`의 `_ids_of` 직후 한 곳(0순위-B).
   그러면 `_fill_beat_screen_time`이 돌 일이 없다. 검증은 `tools/scene_fill_audit.py`로 팬케이크 26698 재편성(목표: 덧붙음 0~2).
1. **사장님 결정 대기** — 고객에게 `script_grounded_enabled`·`edl_inherit_enabled`를 `1`로 켤지(지금 둘 다 `admin`).
   근거: 고객 job 25건 중 15건 결손 100%(지목 0). 켜면 사장님 경로(결손 17~48%)로. 리스크: 장면 없는 재료면 "재료부터 다시"로 멈춤
2. **화면가림 태깅** — B1 태깅에 "상단 UI / 로고·인트로 / 큰 자막 / 워터마크 → 영역" 추가. 조립·매칭 모두 이걸로 컷을 거른다
3. 넘겨받은 무관 판정(`_shares_word`, aeba863fa, fixture `tests/fixtures/pancake_c393b1c7c2f9.json`) — **이번엔 안 넣었다**.
   b6228cac8이 되돌렸는데 이유가 커밋에 없다. 되돌린 이유를 대본게이트 세션에 확인한 뒤 판단. 오늘 실측에선 결손에 영향 0(있으나 없으나 78~83%)
4. 조립 자동화 배선(사장님 OK 후) — 제작소 "원본 던지기" 버튼 → `backbone_assemble.assemble` → `/api/produce/mix/start` 입력
5. 별건: `_speech_speed` 1.30 vs 실측 1.70 — 게이트 대본 상한과 짝이라 대본 길이가 같이 변함

## ⚠️ 이 트랙에서 밟은 함정

- ★**서버에 shopping_shorts를 통째로 복사하면 data 297G가 딸려와 디스크 100%** → 라이브 No space. 두 번 냈고 대본게이트 세션이 지웠다.
  A/B 사본은 `tar --exclude=data` + `ln -s data` + `ln -s .env`. 메모리 `reference_서버AB테스트가_디스크를_채웠다`
- ★디스크 찼을 때 `du` 큰 폴더부터 보면 범인을 오인한다 — **방금 내가 만든 것**부터. 고객 완성본 삭제를 제안할 뻔했다
- finish 게이트 락: 앞 프로세스가 pytest 워커 16개 띄운 채 55분 정지(CPU 0)하면 뒤가 전부 막힌다. 판정=45초 CPU 증가 0
- 같은 파일(script_gate·script_generate)을 두 트랙이 동시에 고쳐 충돌 2회. 영역 분담으로 해결
- 하네스 인자 형태 짐작 금지 — `generate_one_style(sources, style, target_seconds, ...)`, `_call_json`(not `_vault_call`)
- given_script는 **줄마다 마침표 하나** — `script_sentences`가 문장으로 쪼개 줄 수 ≠ beat_sources 수면 상속이 None 폴백
- `_speech_cps()`가 script_gate에 이미 있다(edit_plan 상수×배속). 새로 만들지 말 것
- 실측 도구 `tools/scene_fill_audit.py`(서버 /tmp에도) — 결손%·덧붙은 컷·같은 그림. "고쳤다" 판정은 이 숫자로만

## 2026-09-17 밤 — 이븐쇼핑 스파인(55) 백본 조립 5건 실증 (사장님 job, 소스 충분한 것)

- 결과 6편 `out/이븐쇼핑_백본5건/` + 바탕화면. job: 카메라 bbf7d09efe01 · 행주 bbe01c98b69c · 수납함 bb6e2cec1cff · 미니후드 bbcaf6aa81c3 · 레인지후드 v1 bbde60b16c5c(귀마개 오염) / v2 bb96ebf48c42(귀마개 소스 제외).
- 눈으로 본 것: 장면↔대사 대체로 맞음(박스→박스, 다이얼→다이얼, 필터→필터). 어긋난 곳은 "박스를 열어보니/구성품이" 같은 **소스에 없는 행위 줄**에 다른 컷이 붙는 것(미니후드 2줄). 원본 하드자막·중국어 자막은 그대로(화면가림 태깅 = 다른 트랙).
- 함정 2개: ① `/tmp/ab`에 `assets`·`static` 심링크 없으면 "[motion] 폰트 미해결 → 원본 복사"(자막·틀 전부 빠짐). ② 틀 그림은 `data/frame_cache/<cache_key>.png` 재사용 → 폰트 없던 시점 틀이 캐시되면 제목이 깨알로 남는다. 캐시 지우고 재렌더로 해결.
- 사장님 job 소스에 **다른 제품 영상**(레인지후드 job의 유튜브 NBsOouTUWsI=귀마개)이 섞이면 조립기가 그 컷을 씀 → 소스 제품 일치 검사 없음(미결).
- 미니후드 1차는 Gemini 503으로 groups_empty, 재시도 통과.
- 스크립트: `out/bb_batch5.py`(서버 `/tmp/bb_batch5.py`, `/tmp/bb_batch2.py`, `/tmp/rerender2.py`).
- (같은 날 밤 추가) 사장님 "이븐쇼핑 스타일 아니네" → 조립기가 스파인 훅 한 줄만 빌리고 자체 [훅]+[특징]+[댓글CTA]로 쓰던 것 확인. `write_lines`에 스파인 `beat_roles`+`templates` 그대로 쓰는 경로 추가(`_spine_style/_feature_roles/_spine_plan/_spine_prompt`, 테스트 6개). 카메라 재실증 job **bb4c2734ce80**(out/이븐쇼핑_백본5건/이븐스파인_카메라_*.mp4): 대본은 천재→평범→떼돈→「이건 바로 NORDECO」→한계→효능×5→"이러니 떼돈을 벌었다고" 꼴로 정확. 남은 문제: 11줄이라 34.8초(목표 25), 패키지·구성품 줄에 거리 컷(그룹 컷 부족→보충), 마지막 2줄에 남의 채널 CTA 하드자막·얼굴 컷.
- (밤 2차) 사장님 "장면 일치가 잘 안 되는데" → 줄별 컷 대조(`/tmp/inspect_cuts.py`)로 뿌리 3개 실측·수정(커밋 b665221fe·cfde86127·37f6cafb1): ① 구조 줄(정체·떼돈)이 먼저 돌며 특징 묶음 컷을 먹음 → 특징 줄 먼저 배정 ② 스파인 프롬프트 group이 순서번호(원번호 아님) → 원번호 ③ 묶음 밖 컷=찌꺼기(남의 CTA·얼굴)라 구조 줄이 그걸 받음 → 외관 컷을 구조 줄 몫으로 줄당 2컷 예약, 사람·댓글 컷 맨 뒤. 5차 job **bbe3eb2fe683**: 11줄 전부 제품 컷(inspect 출력 기준). 남은=길이 33~35초(목표 25), 하드자막 컷(다른 트랙), 모델 재호출마다 묶음이 달라져 회차 비교가 흔들림.

### ⏭ 집에서 이어서 (2026-09-17 밤 마감)
- 서버 A/B 폴더 `/tmp/ab`는 트랙 최신 `backbone_assemble.py`가 scp로 올라가 있음(assets·static 심링크 포함). 실행 스크립트 `out/bb_batch5.py`(5건 배치), 서버 `/tmp/bb_spine1.py`(카메라 1건), `/tmp/inspect_cuts.py`(줄별 컷 대조 — job id만 바꿔 쓰기).
- 다음: ① 새 규칙으로 행주·수납함·미니후드·레인지후드 4건 재조립 → 줄별 대조로 판정 ② 25초 길이(효능 줄 3~4개로 제한, `_spine_plan`의 n_feat 상한) ③ 하드자막·남의 CTA 컷 배제는 화면가림 태깅 트랙과 합의.
- 배포 보류 그대로(사장님 지시). finish 하지 말 것 — 트랙 브랜치에만.
- (09-18 낮) 어미 결합 오류 수리(758a95937): 프롬프트에 어간 규칙 + `_bad_join/_fix_join/_repair_joins`(기계수리→줄 재작성 1회). 재조립 수납함 bbb6bcbbbeb8·미니후드 bbf5ff87bb8d: '는는데'류 0건. 잔여: 틀 `{효능3}데다가 {효능4}`는 끝 어미가 모델 몫이라 "챙긴다는."처럼 끊길 수 있음(틀 자체에 끝말 추가 필요). 4건 새 규칙 실증: 행주 bb214c1e0cd6·수납함 bba3a1e8f7a1·미니후드 bbe6fcfabec1·레인지후드 bbcb7dffecf3 — 장면 8~10/10줄 제품 컷.
- (09-18 오후) 프리셋 확장 도구 `tools/spine_presets/harvest_templates.py`(원문→역할별 틀 수확, 원문검증·슬롯없음 거르기·제외어, `--apply`). 집 서버 스크립트 4개 `tools/spine_presets/`에 보관. **적용**: 56(+29→제품어 13개 제거, 제목 11·bait 10·origin 9·notice 7·cases 6·twist 9), 60(+5), 65(+2). **미적용**: 68·67·66·69 — script_wiki 유튜브 재료 3~9편, 존댓말·사투리 섞여 결 안 맞음 → 그 훅으로 원문 수집이 먼저. 히트작 200편 분포: 오용형 92·권위자 12·은폐형(이븐) 15·기타 76.
- (09-18 저녁) 사장님 구조 확인 "유형 고르면 그 유형 스파인들 순번대로": **없던 것** → `pick_hook_spine(style=, seed=)` 추가(6d3ab61d0). 실증 발명품형 seed 0/1/2 → 60/65/66 순번 정확(job bb182ae5c1cf·bb16bd703bda·bbe36afd10c0). 유형 태그 현황: 오용형=56 1개, 발명품형=60·65·66, 제품정체형=55; 67·68·69는 유형 대신 카테고리(홈템)만 붙어 있어 **유형 붙여야 고를 수 있음**. 대본: 60 자연스러움 / 65 "만드는다는 거"·"부드러워해 버림"(어미 결합 잔재, 수리 패턴 추가 필요) / 66 틀이 원문 아닌 조립체라 "~음/~거" 단조 → 원문 재료 필요. 자막 852편 PC 수집 중(`raw/analysis/썰쇼핑_자막확장_2026-09-18`).
- (09-18 저녁2) 자막 781편 확보(`raw/analysis/썰쇼핑_자막확장_2026-09-18/hits_subs.json`, 서버 `/tmp/ab/raw/...`). 수확 적용: 60(+5)·65(+11)·69(+14)·68(+6)·67(+6)·66(+2)·56(+6), 파편·제품어 11개 손으로 제거. 유형 태그: 67=목격담형·68=가성비형·69=만능템형. **오용형 스파인 4개 신설**(56 뼈대 복제): 70「OO도 예상 못한 미친 활용법」(재료 107편)·71「OO의 실수」(9)·72「직원도 몰래 쓰는 활용법」(11)·73「OO도 감탄한 천재 아이디어」(24) → 수확 중. 스크립트 `tools/spine_presets/{make_misuse_spines.py,harvest_all.sh,harvest_new.sh}`.
- (09-18 밤) 70~73 재수확(56 틀 씨앗+필터 강화) → 칸별 9~14개, 훅(title)은 스파인별로 히트작 제목 꼴 부여({권위자} 슬롯). 오용형 순번 0~4(56·70·71·72·73) 수납함 5편 실증 돌리는 중(`/tmp/bb_misuse.py`).
- (09-18 밤2) 오용형 5스파인 대본 3차(렌더 없이): 6줄 고정·훅 각각 다름·존댓말 0(job bbfc2bdbe78f·bb06508066f7·bb6e413074fa·bbd3cfdd12de·bb53f68f0e87). 수정: 조립체/존댓말 틀 제거(스파인당 15개), 특징 줄 반복 상한 2·효능 칸<3이면 안 늘림, 슬롯 이름 누출 제거, 유형으로 고를 땐 hook_3s 없는 인스타 스파인도 후보. ★수납함처럼 오용 사례가 없는 소재에 오용형을 주면 "비밀 금고"처럼 화면에 없는 용도를 지어냄 = 소재↔스파인 불일치(사장님 결정 대기). 인스타 스파인 7개 수확 시작(`/tmp/harvest_insta.sh`, corpus=insta 300자↑ 긴 원문 25편).
- (09-18 밤3) 인스타 스파인 수확: '*'(긴 원문순)은 외국어·무관 원문이라 실패(59에 외국어 15개 들어갔다 제거, 한글비율 필터 추가). 훅 문구로 세니 52(32편)·58(19)·62(21)·53(8)만 재료 있음, 54·59·61은 0~4편 → 보류. 4개 훅 문구로 재수확 중(`/tmp/harvest_insta3.sh`). 인스타 원문(한글 150자↑) 1,597편.
- (09-18 밤4) 인스타 수확 결과: 52(+14→찌꺼기 제거)·53(+16→제거) 보강, 58·62는 재료 18편인데 원문검증 통과 0(모델이 바꿔 씀). 행주 job(인스타 소스)으로 지인증언형·권유지시형·금지경고형 순번0 대본 실증 중(`/tmp/bb_insta_script.py`).
- (09-18 밤 마감) 인스타 실증(행주 job, 대본만): 지인증언형 bb22636a44f0·권유지시형 bbdd4bb0a0ff·금지경고형 bb5569edbc0f — 순번 고르기·존댓말 유지 OK. 금지경고형 자연스러움. 지인증언형 "알고 보니" 3연속 → more 칸 없는 틀은 안 늘리게 수정. 권유지시형(53)은 레시피용(fit ['권유지시형','레시피'])이라 행주에 "드세요·별미" = **소재↔스파인 불일치의 실례**.
### ⏭ 다음
1. 사장님 결정: 소재↔스파인 안 맞을 때(경고/자동교체/그냥 둠) + 레시피 전용 스파인(53)을 물건에 안 보이게 할지
2. 58·62(인스타)·66·67·68·69(유튜브): 재료 부족/원문검증 0 → 원문 더 담기(랭킹 채널 등록) 후 재수확
3. 고객 화면에 "유형 고르기" 연결(3단계 백본 경로) → 그때 배포
- (09-18 밤 추가) 인스타 오류 2개 수정·재검증: 지인증언형 bb07b67fad21 "알고 보니" 1회(반복 사라짐) / 권유지시형은 레시피 전용(53) 제외 → 후보가 없어 무작위 폴백으로 67 「충격 먹었음」(유튜브 반말)이 뽑힘 = **권유지시형 물건용 스파인이 없다**(원문 담아 만들 것). 금지경고형 bb217f9ba224 정상.

## ✅ 2026-09-18 유튜브 대본 스타일 마감
- **유형 6개 · 스타일(스파인) 15개**, 전부 카메라/수납함 소재로 대본 1편 이상 실증(렌더 없이):
  오용형 5(56·70·71·72·73) / 제품정체형 4(55·74·75·76) / 발명품형 3(60·65·66) / 목격담 67 / 가성비 68 / 만능템 69.
- 구조: 유형 고르기 → `pick_hook_spine(style=, seed=)`가 그 유형 스타일을 id순 순번 → 스타일 안 칸마다 문장틀을 순번(시작점 = seed + 스파인id×7).
- 오늘 고친 것(전부 실측): 스파인별 시작점 엇갈림 · 가격/본래용도 지어내기 금지 · 제품명 한국어·공개 줄 1회 · 틀 밖 반복 상한 · 오용형/인스타형 줄 안 늘림 · 슬롯 누출 · 레시피 전용 스파인 물건 제외 · 결합 깨지는 틀 꼴(`}버렸`·`}해 버려`·`}데다가` 등) 전 스파인 제거.
- 남은 약점: 66·67·68·69는 원문 13~38편이라 틀이 얇음(문장 반복 위험) · 25초 길이 미조정(정체형 10~11줄 ≈ 30초).
- 스크립트: `tools/spine_presets/{make_identity_spines.py,harvest_identity.sh,make_misuse_spines.py,harvest_*.sh}`, 수확 도구 `harvest_templates.py`.

## ⚠️ 2026-09-18 저녁 — 스파인 DB 편집이 라이브에 즉시 반영됨(스스로 발견)
- 오늘 서버 `reference.db` spine 틀을 고치고 새 스파인 70~76을 approved로 만들었다 → 라이브 코드 5곳(bank_assemble·script_generate·spine_fill·script_gate·app 스타일목록)이 바로 읽음 = **배포 보류와 무관하게 고객 대본에 반영**.
- 70~76은 유형 태그만 있어 소재 선택 화면에선 안 보이고 소재 없이 열면 보임. 기존 55·56·60·65~69·52~62는 고친 틀이 고객 생성에 쓰임(고장 틀 제거+원문 틀 추가 방향).
- 되돌리기 기준: `/home/ubuntu/spine_backup/spine_2026-09-18_after_edits.json`(현재) · `reference.db.bak-script-pickup-grounding-20260914-143448`(09-14, 집 세션 55 프리셋 이전).
- **사장님 결정 대기**: 그대로 둘지 / 70~76만 숨길지(pending) / 09-14로 되돌릴지.
- 점검기 `tools/spine_presets/audit_spines.py` 신설(회원 100명 조합·첫 줄 겹침·틀 결함). 1차 43건 → 수리 후 22건, 남은 건 전부 재료 부족(52·53·54·59·62 인스타, 66·67·69·76 유튜브).
- CLAUDE.md 0순위-A1b(스스로 점검) 추가 + 전역 CLAUDE.md에 같은 규칙.
- (결정 2026-09-18) 사장님: 스파인 DB 라이브 반영 "상관없을 것 같은데" → **그대로 둠**(70~76 포함).

## 2026-09-18 저녁 — 유형별 영상 끝까지 실증(자가점검 2회)
- 1차(bb0d060aa9f6·bbe5746fff17·bb38c10bcca9) 제가 먼저 보고 탈락: 길이 18/30/29초·발명품형에 오용형 말투 섞임·제품명 반복 → `_fit_length`(읽는 초 기준 가운데 특징 줄부터 빼기)·`_one_full_name`·스타일 섞임 검사(점검기)·비오용형 스파인 오용형 말투 32개 제거.
- 2차(오용형 bb8edef70215 20.7초 · 정체형 bb931fdbf7c8 24.8초 · 발명품형 bb5dc9e215de 19.6초): 줄별 컷 대조 일치(레버→레버, 언박싱→박스, 결과물→복고 영상). 바탕화면 `유형v2_*.mp4`.
- ★남은 가장 큰 화면 결함: 원본에 구워진 **남의 채널 글씨**(@살림장착 "떼돈 번 제품"·"댓글에 찰칵"·중국어 자막) — 화면가림 트랙 몫. 조립기에서 그 소스 컷을 빼는 임시책은 태깅에 글씨 정보가 없어 못 함.
- 오용형을 카메라(오용 사례 없는 제품)에 주면 억지 줄("온갖 곳에 붙이기") → 소재↔스타일 불일치 결정 대기.
- 읽는 초 추정(narr_secs)은 실제 TTS보다 길게 나옴: 추정 30.9초 → 실제 24.8초.
- (저녁) 사장님 "씨앗=백본이면 백본과 다르게 보여야": 실측 도구 `tools/spine_presets/diff_from_seed.py`(원본 컷 비율·원본 6글자 조각 겹침). 한국어 원본(행주 s0 지인증언형)으로 3유형 → **원본 컷 0/21~31 · 문장 겹침 0%**. ★카메라·수납함은 원본이 영어·러시아어라 이 검사 무의미(처음에 그걸로 재고 넘어갈 뻔함). 특징 순서 섞기가 **안 되고 있던 것** 발견(머리말엔 '순서 섞기', build_groups는 '원본 순서 지켜라') → `_shuffle_middle`(양끝 고정·가운데 seed 섞기). 이중어미 '주게 해 주' 수리.
- (결정 2026-09-18) 사장님 **성공 기준: "원본(씨앗)이랑만 완전 다르면 된다"**, 자막은 당연히 지운다(화면가림 쪽). → 판정 도구 `diff_from_seed.py`(원본 컷 0%·원본 6글자 조각 겹침 0% 목표). 서브 영상과의 유사성은 기준 아님.
- ★(정정 2026-09-18 저녁) 앞서 적은 "원본 컷 0%"는 **측정 오류**였다 — 컷 번호 접두어(DdOayfhAnpx)와 소스 번호(s0)를 비교해 늘 0. **같은 원인이 조립기에도**: `_seg_index`의 vid가 컷 접두어라 백본(s0)과 절대 안 맞아 '서브 먼저·원본 나중'이 09-16부터 통째로 무력 → 씨앗 s0 백본인데 원본 컷 10/29·12/22. vid를 소스 번호로 고침(테스트 추가). 검증: 같은 도구로 고치기 전 job 재측정 = 10/29·12/22·옛 방식 9/9 → 고친 뒤 0/24·0/23·0/23(진짜).
- 옛 방식(라이브 경로, 복제 job old57c3a72e6)은 **9줄 전부 원본 컷** — 사장님 기준(원본과 완전히 다름) 탈락.
