# 렌즈정확도 트랙 로그

## 2026-08-21
- 메인 `/api/lens/search`에만 빠져 있던 AI 유사도 채점(`judge_same_product`)을 붙였다 — 샤오홍슈·도우인·유튜브엔 이미 있었다. `_lens_judge` 한 곳으로 공용화(0순위-B) + 관련도순 정렬(인스타 우선은 2차 키로 보존).
- ★async 경로는 `to_thread` 필수 — `_lens_finalize`는 이벤트루프에 남은 함수라 블로킹 Gemini를 그 안에서 부르면 동시 요청이 굶는다.
- ⚠️ `test_assembled_drafts` 2건은 **`.env`(실키)가 있는 폴더에서만** 깨진다(실측: 치우면 14 passed). 대본 어미가 실제 Gemini를 타서 그렇다 — 게이트가 이걸로 막혀도 코드 고치지 마라.

## 2026-08-22
- 일본 로케일 추가(서버 env, LENS_MAX_CALLS도 같이 4로 — 안 올리면 4번째가 굶는다). 실측: 뷰티템 88→110장(틱톡+11), 스콘 29→31장(0) — **소재별 편차 크다**.
- 렌즈 검색국가 고르기 라이브. 랭킹 상단 칩 → 한국만이면 SerpApi 4회→1회. `_resolve_locales` 한 곳에서 판정(0순위-B).
- ★함정: 예산을 로케일 수로 자르면(min) 기존 재시도가 죽는다(비례식으로) / app.py가 lens_discover를 모듈 import 안 해 엔드포인트만 런타임 NameError(import 통과라 게이트가 못 잡음) / 테스트에 ja:jp 쓰면 로컬 3벌이라 환경 탄다.
