# 핀터레스트 탭 — 핸드오프

**최종 갱신: 2026-08-29 밤 (회사PC) · 트랙 `.tracks/핀터레스트탭`**

---

## 이 트랙이 왜 있나 (사장님, 2026-08-28)

> "깨끗한 영상들이 많아서 / 나만보게 비공개탭"

핀터레스트 영상은 **자막·워터마크가 없는 원본**이 많다. 실측:
- 720x1280 세로 9:16 — 우리 쇼츠 규격 그대로
- 어떤 건 자막·워터마크·로고 **0개** (완전히 깨끗)
- 어떤 건 하단 워터마크 있음 — **전부 깨끗하진 않다**

→ 깨끗한 건 **자막제거(VMake) 비용이 0원**이 된다. 크레딧 소진으로 고객 신고까지 났던 그 비용.

**사장님 결정 3가지** (테스트로 박아뒀다 — `tests/test_pinterest.py`)
1. 워터마크 있는 것도 **다 담고 눈으로 고른다** (자동 필터 안 함)
2. 랭킹에 핀터레스트 탭 — **관리자만** 보이게 (비공개)
3. 키워드는 **영어 먼저**

---

## 지금 상태 (라이브)

| 항목 | 값 |
|---|---|
| 총 수집 | **1,870개** (2026-08-29 밤샘, 시작 302 → 1,870) |
| **쇼핑몰 핀** | **465개 — 전부 영상 있음** |
| ↳ 테무 | **308** (temu.to 247 · temu.com 59 · app.temu.com 2) |
| ↳ 아마존 | 141 (amazon.com 74 · amzn.to 63 · 기타 4) |
| ↳ 기타 | shopee 10 · lightinthebox 4 |
| 영상 길이 | 중앙값 **16.6초** · 5~40초가 **431개**(쇼츠 규격) |
| 수집 방식 | 서버 Playwright headless chromium · **익명(로그인 불필요)** |
| 병렬 | **12워커** — 12키워드 약 30~40초(목적지 조회 포함) |

**★브라우저로 라이브 검증 완료**(2026-08-29): 필터 버튼 5개 정상 동작,
`더 보기 (200 / 301)`처럼 필터 후 개수를 정확히 표시, 썸네일 186개 전부 로드(실패 0).

### ★ 검색어가 적중률을 결정한다 (실측)

핀 설명에 `temu`가 있으면 **실제로 테무 링크가 붙어 있다**. 그래서 검색어만 바꿔도:

| 검색어 | 적중률 |
|---|---|
| `temu storage hack` / `temu gadgets must have` | **4/4 = 100%** |
| `temu survival gadget` / `temu glue gadget` | 3/3 = 100% |
| `temu small space hack` / `temu apartment hack` / `temu cabinet organizer` | 2/2 = 100% |
| `temu space saving` | **7/8 = 88%** |
| `temu storage idea` | 4/5 = 80% |
| `temu food container` / `temu kitchen organizer` / `temu cooking gadget` | 75% |
| (기존 공구·차량 계열 전체) | **18%** |

**★잘 되는 축 / 안 되는 축이 갈린다**(12차까지 실측):
- ✅ **`temu` + 수납·정리·주방** — 50~100%. 가장 확실하다.
- ❌ **`aliexpress` 축은 통째로 죽었다** — 7개 검색어 전부 0/N. 핀터레스트엔 알리가 거의 없다(465개 중 1개).
- ❌ 취미·반려 계열(낚시·고양이·우산)은 10% 안팎.
- ❌ 우리 원래 검색어(`diy tool invention` 0/18, `satisfying gadget demo` 0/14)는 영상은 깨끗하지만 쇼핑몰 링크가 없다.

→ 기본 검색어(`DEFAULT_KEYWORDS`)를 **이 순서로 바꿔뒀다**(커밋 `c2aabc5e2`).
   사장님이 그냥 버튼만 눌러도 쇼핑몰 핀이 잘 걸린다.

## ★ 대량 수집의 진짜 병목 — 여기서 시간을 제일 많이 썼다

### 병목은 IP가 아니라 **브라우저 기동 + 고정 sleep**이었다

`pinterest_crawl._crawl()`은 키워드마다 `sync_playwright()` → `chromium.launch()`를 **새로** 연다.
거기에 `for _ in range(scrolls): wait_for_timeout(1500)` + 마지막 2000ms
= **키워드당 약 9.5초가 순수 대기**다.

→ 12키워드 순차 **70.1초** vs 12워커 병렬 **18.1초**. **결과 손실 0**.

**프록시는 필요 없다.** (주거용 프록시 `REDDIT_PROXY`는 살아있다 — `p.webshare.io:80`,
exit IP 103.43.76.167, 핀터레스트 200 확인. 하지만 병렬로 이미 충분히 빠르다.)

### ★ scrolls를 줄이면 안 된다 (내가 틀렸던 가설)

"스크롤이 낭비"라고 생각해 `scrolls=1`로 벤치마크했더니 **결과가 34% 날아갔다**
(`diy tool invention` 15개 → **0개**). **scrolls=5는 유지해야 한다.**

### 검색어당 첫 묶음에서 잘린다 (구조적 한계)

스크롤을 4→15회로 늘려도 `BaseSearchResource/get`이 **1회만** 호출된다.
즉 **키워드 하나당 10~27개가 상한**이다. 늘리는 방법은 **키워드 수를 늘리는 것뿐**.

→ 그래서 "10개씩 하고 올리고" 방식이 유일한 길이고, `/api/pinterest/collect`는
**누적 저장**(load_last_run_platform + pin_id 중복제거)으로 만들어져 있다. `reset`으로만 비운다.

---

## ★ 알리·테무 상품 겨냥 — **해결. 화면에서 골라 쓸 수 있다**

사장님 질문: "알리 테무에서 나오는 상품들을 중점으로 어떻게 찾을수있나"

### 답: 핀 상세 페이지의 `"domain":` 필드가 목적지다

낮에 낸 "불가능" 결론은 **틀렸다**. 그때는 ①**검색 응답**만 봤고(거긴 정말 없다 —
핀 키는 `id·images·videos`뿐, 25개 전수 확인) ②상세 페이지는 서버 IP 때문에
**껍데기만 받고 있었다**. 주거용 프록시로 열면 목적지가 정확히 있다:

```
"link":"https://temu.to/m/u9c4kk5ldcu"   "domain":"temu.to"   siteName":"Temu"
```

★판정은 반드시 **`"domain":` 필드로만** — 문자열 검색은 오탐이다
(`amazon` 히트 하나가 실은 CSP 헤더의 `m.media-amazon.com`이었다).

### 전량 실측 (624개 · 262초 · 실패 0)

| 목적지 | 개수 |
|---|---|
| Uploaded by user(링크 없음) | **234** |
| instagram.com | **212** |
| amzn.to / amazon.com | 54 |
| **temu.to / temu.com** | **22** |
| aliexpress.com | 1 |
| lightinthebox·shopee 등 | 나머지 |

**쇼핑몰 핀 85개**를 골라냈고 품질이 좋다:
- **85개 전부 영상 있음**(0개 누락)
- **중앙값 15.8초**, 5~40초가 77개 = **쇼츠 규격 그대로**
- 카테고리: 차량템 47 · 알리테무신박템 38

⚠️단 **아마존이 테무의 2.5배고 알리는 1개뿐**이다. 핀터레스트에선 아마존·테무가 주류다.

### 라이브에 붙인 것

- 수집이 핀마다 목적지를 읽어 `pin_dest`·`pin_link`로 담는다
  (병렬16, 40건 7.8초 = 건당 0.19초 · 판별성공 39/40 실측 — 배치당 10초쯤 추가).
  ⚠️목적지는 **덤**이라 실패해도 영상 수집은 그대로 산다.
- 화면에 필터 버튼: `전체 / 🛒 쇼핑몰 / 테무 / 알리 / 아마존`.
  기존 `.ftog` 스타일 재사용(새 CSS 안 만듦), 활성 클래스 `on`.
- **이미 모은 624개도 백필 완료** — 622개에 목적지가 채워졌다(다시 크롤 안 함,
  전수 조사 결과 재사용). API 확인: 624건 중 622건 `pin_dest`, 쇼핑몰 85건.
- ★서버 `SHOP_DOMAINS`와 화면 `PIN_SHOP_KEYS`가 **어긋나면 테스트가 잡는다**
  (같은 판단을 두 곳에 적은 대가를 테스트로 봉인 — 0순위-B).

## ★ 라이브 버그를 고쳤다 — 핀 상세조회가 8/8 실패 중이었다

다른 트랙이 만든 `pin_video_info`(렌즈·다운로드 공용)가 **라이브에서 100% 실패**하고 있었다.
두 겹이었다:

1. **brotli** — 서버에 brotli 1.2.0이 깔려 requests가 `br`을 자동 광고하는데
   urllib3 2.0.7과의 조합에서 디코딩이 깨진다(`ContentDecodingError`).
   → `Accept-Encoding: gzip, deflate` 명시.
   ⚠️**간헐적**이다. 첫 조사 때 24/25가 이걸로 날아가 "쇼핑몰 링크 0/25"라는
   **가짜 판정**을 냈다 — 측정 실패를 결론으로 착각하지 마라.

2. **★진짜 원인은 IP** — 200으로 열려도 `ld+json`이 0개다. 헤더를 브라우저처럼
   갖춰도 안 되고 **IP만 바꾸면 된다**:

   | 조건 | 결과 |
   |---|---|
   | 데이터센터 IP(서버 직결) | **None 10/10** |
   | 주거용 프록시(`REDDIT_PROXY`) | **dict 10/10** |

   → `reddit_source._proxies()` 재사용(0순위-B).

**⚠️1번만 고치면 오히려 나빠진다.** 그 함수 계약이 *dict=영상 확정 / None=영상 아님
확정(렌즈가 잘라냄) / 예외=판정불가(안 자름)* 세 갈래라, brotli만 고치면
**예외 → None**이 되어 멀쩡한 핀 영상을 렌즈가 **잘라내기 시작한다**.

수정 후 라이브 함수로 재측정: **dict 10 · None 0 · 예외 0** (mp4 직링크 정상).
커밋 `cfbd1ae62`, origin/main 병합 확인(`git branch -r --contains`).

## 파일 위치

| 무엇 | 어디 |
|---|---|
| 크롤러 | `shopping_shorts/pinterest_crawl.py` |
| 테스트(사장님 결정 봉인) | `shopping_shorts/tests/test_pinterest.py` |
| 엔드포인트 | `app.py` 의 `/api/pinterest/collect` (`_require_admin`) |
| 탭 UI | `shopping_shorts/static/index.html` (`data-platform="pinterest"`, admin-only) |
| **병렬 러너(서버)** | `/tmp/run_pin12.py` — `python3 /tmp/run_pin12.py '["kw",...]' [reset] [카테고리]` |
| 키워드 묶음(서버) | `/tmp/kw_car.json` `/tmp/kw_car2.json` `/tmp/kw_dessert.json` `/tmp/kw_ali.json` (각 60개) |

---

## ⏭ 남은 일

1. ~~수집 엔드포인트 병렬화~~ **완료**(`2a58db408`). 화면 버튼도 12워커, 키워드 상한 8→12.
2. ~~동시 수집 경합~~ **완료**(`c011b5dda`). 재현으로 확인하고 고쳤다:
   고치기 전 = 두 수집이 각 50개 담았는데 **50개만 남음**(한쪽 전멸) →
   지금 = 셋 동시에 돌려도 **150/150 보존**. 읽기~쓰기를 한 트랜잭션(`BEGIN IMMEDIATE`)으로 묶었고,
   락은 호출부가 아니라 **저장을 담당하는 store**에 뒀다(`merge_last_run_platform`).
3. ~~알리·테무 판별~~ **완료**(`c4a8031ac`). 위 참조.
4. **사장님 눈으로 확인 필요** — 화면에서 [🛒 쇼핑몰] 눌러 85개가 제대로 뜨는지.
   ⚠️이 커밋은 배포 대기 중이었다(서버가 고객 접속 중이라 웹 재시작을 연기).
5. **`age_hours`가 핀터레스트 항목에 없다**(624개 전부 없음). 지금은 상한 8000까지
   7,376개 여유라 무해하지만, 상한에 닿으면 `_newest_first`가 정렬을 못 해
   **어떤 게 잘릴지 예측 불가**가 된다. 그때 가서 timestamp→age_hours를 채워야 한다.
6. **수집 검색어별 적중률 분석이 막혀 있다** — 452건(72%)에 `keyword`가 없다.
   내가 어젯밤 쓴 임시 러너 `/tmp/run_pin12.py`가 라이브 코드를 손으로 베끼면서
   `"keyword"`를 빠뜨린 탓이다(0순위-B의 실제 피해). **러너는 폐기 처리했다**
   (실행하면 안내만 띄우고 멈춘다). 앞으로 수집은 화면 버튼이나 엔드포인트로만 하면
   keyword가 남으므로, 데이터가 더 쌓이면 "어떤 검색어가 쇼핑몰 핀을 잘 무나"를 잴 수 있다.

## ⚠️ 이 트랙에서 밟은 함정

- **`finish`는 exit 0이어도 병합 안 됐을 수 있다.** 실제로 1차 시도가 271건 실패로 조용히
  죽었다(백그라운드로 던져서 못 봤다). 실패는 전부 `test_produce_thumb_ui.py`라 내 변경과
  무관했고, main이 앞서간 게 원인. `git merge origin/main --no-edit` 후 재시도 → 통과.
  **판정은 반드시 `git branch -r --contains <sha>`.**
- **`save_last_run_platform(platform, items, collected_at)`** — `collected_at` 빠뜨려서
  5배치 전부 저장이 날아갔다(크롤은 성공했는데). 시그니처 확인하고 쓸 것.
- **`re.sub(count=1)`** 로 일괄치환하면 한 줄에 여러 개 있는 건 놓친다 → `re.subn`.
- **서버는 8코어/30GB다** (메모리의 4코어/16GB는 낡았다). `nproc`로 확인할 것.
