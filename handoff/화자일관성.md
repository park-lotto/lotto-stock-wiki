# 핸드오프 — 화자일관성

- 갱신: 2026-08-23 (PC: 사무실)
- 트랙: `화자일관성` / 브랜치 `track/화자일관성`
- 커밋: `67f09fc24`

---

## 사장님 제보

> "친구네 갔다가 충격먹고 남편턱이 달라졌다하고 저도 해봤다는게 / 말이되는건지"

화면(가족갈등 반전형 A안):
```
첫말 : 저 친구네 집 갔다가 충격 받았잖아요.
상황 : 남편 턱이 예전보다 훨씬 깔끔하게 달라진 거예요.   ← 누구 남편? 내 남편으로 읽힌다
결과 : 저도 해보니까 자극 없이 밀리는 거죠.              ← 화자가 직접 쓴 사람이 된다
```
여성 화자가 남성용 면도기를 자기 턱에 미는 그림 = 말이 안 된다.

---

## ★내가 처음에 헛짚은 것 2건 (기록해 둔다)

1. **"틀 조립이 원인이다"** → **틀림.** 조립은 08-21에 이미 껐다(라이브 DB 실측
   `assemble_off=1`). 화면의 "틀 조립 못함"은 정상 표시다.
   → **08-22 핸드오프의 "조립이 범인, 생성기는 멀쩡"이라는 전제가 이번 건으로 깨졌다.**
     생성기도 깨진 대본을 낸다.
2. **규칙 기반(정규식) 판정** → **라이브 27개에 돌려보니 오탐 2건**(멀쩡한 다이소
   대본을 "3인칭 소유격 누락"으로 잡음). 이 코드베이스 원칙대로 오탐이 미탐보다
   나쁘다 → 규칙안 폐기, LLM 판정으로 갔다.

---

## 진짜 원인 — 규칙은 있는데 **판정이 없었다**

`script_generate.py` `_STORY_RULES_CORE`에 사장님 규칙이 **이미 정확히** 있었다:

> "★★★탄탄한 회수 — 훅에서 등장시킨 그 인물·갈등이 결말에서 반드시 회수돼야 한다.
>   중간에 슬그머니 다른 인물로 갈아타 시작한 갈등을 버리지 마라"

그런데 게이트가 내는 검사를 전수로 뽑으니:
```
구간순서 · 문장틀준수 · CTA단어유도 · CTA금지 · 말끝(반말체)
말밀도 · 고조심화 · 소재일치 · 훅3초(서론금지·정체은폐) · 수치근거
→ `화자|speaker|주어|인칭` grep = 0건
```

**규칙은 프롬프트에만 있고 지켰는지 보는 판정이 없었다.**
라이브 실증(`work_id 57043b8fc843`, 제보와 같은 면도기 건): 게이트 6개 검사를
**전부 통과**. 글자 수와 연결어 개수만 세고 있었다.

→ 미검출 → 재작성 루프 안 걸림 → 아무도 안 고침
  (메모리 `판정축_하나면_교정이_통째로죽는다`와 **똑같은 모양**)

---

## 고친 것 (커밋 67f09fc24)

| # | 무엇 | 어디 |
|---|---|---|
| 1 | `check(speaker_judge=)` — '화자 일관성' 검사 신설 | `script_gate.py` |
| 2 | `_speaker_judge` — 재작성 루프 안에서 판정 → 교정까지 배선 | `script_generate.py:439` |
| 3 | `_STORY_RULES_CORE`에 **이번 실패작을 예시로** 박음 | `script_generate.py` |
| 4 | `prior_verdict()` — 재단 후 판정이 사라지던 것 | `script_gate.py` |

**★4번이 조용한 함정이었다.** 길이 초과 대본은 재단 뒤 게이트를 **다시** 돈다
(`script_generate.py:471`). 거기에 판정기를 안 넘기면 앞서 찾은 화자 실패가
`checks`에서 **조용히 사라진다**(판정은 했는데 화면·재작성에 안 실림).
그렇다고 판정기를 다시 넘기면 **유료 호출이 두 배**다. 재단은 군더더기 부사만
덜어내 화자를 못 바꾸므로 → 앞 판정을 물려준다.

### fail-open 이 핵심이다
`_call_json`은 무키·소진·응답오류를 **전부 `{}`로** 돌려준다. 판정 못 했는데
FAIL로 잡으면 **키가 마른 날 대본이 통째로 막힌다**. 판정 불가 = 통과.
판정기 예외도 삼킨다(대본 생성을 죽이면 안 된다).

---

## 검증 (실데이터 — 합성 아님)

```
제보 대본        → FAIL. why: "친구 남편 얘기하다 화자 본인 경험담으로 전환"
고친 대본        → 통과
오탐 검사 3건    → 3/3 통과 (정규식이 오탐했던 2건 + 게이트 통과했던 라이브 1건)
테스트           → 신설 9건 / 관련 537건 통과
전체 pytest      → 기준선 2실패 → 내 코드 1실패 (늘지 않았다)
```
⚠️ `test_assembled_drafts.py`의 1건은 **내 것이 아니다** — 원본 코드로 되돌려
   돌려보니 거기선 2건 실패했다(순서 의존 flaky).

---

## ⏭ 다음에 할 일

1. **사장님 육안 확인** ← 여기부터. 대본을 다시 뽑아 화자가 끝까지 한 사람인지,
   3인칭이 나오면 "친구 남편"처럼 누구 것인지 밝히는지 본다.
2. **판정 비용 관측.** 대본 1편당 LLM 1콜이 늘었다(제미니 과금 계측에 잡힌다 —
   메모리 `제미니과금_계측`). 재작성이 걸리면 그만큼 더 는다. 실측해서 부담되면
   `_speaker_judge`를 **마지막 시도에만** 태우는 방법이 있다.
3. **다른 스타일도 같은 구멍이 있는지.** 이번엔 화자만 봤다. "인과 다리"(각 문장이
   앞 문장의 결과·이유)도 헌장에는 있는데 판정은 없다 — 같은 방식으로 붙일 수 있다.

## 관련 파일
- 검사: `shopping_shorts/script_gate.py` `check(speaker_judge=)` · `prior_verdict()`
- 판정기: `shopping_shorts/script_generate.py` `_speaker_judge` · `_SPEAKER_PROMPT`
- 프롬프트 예시: `_STORY_RULES_CORE` (⚠️ 중괄호 금지 — `.format()`이 깨진다)
- 테스트: `shopping_shorts/tests/test_speaker_consistency.py` (9건)

---

# 2026-08-23 (배포 후기) — ★`finish` exit 0이 세 번 나왔는데 뜻이 매번 달랐다

라이브 반영 완료: 커밋 `f3f498afa` · 웹 재시작 03:07:01 KST.
서버에서 **직접 임포트해 실행**으로 확인했다(파일 존재 확인이 아니라):
```
check 인자에 speaker_judge : True
prior_verdict / _speaker_judge 존재 : True
화자검사 실제 실행         : ('화자 일관성', False)  ← 깨진 문장을 잡는다
```

## exit 0 = 병합 아님 (3번 다 exit 0, 뜻은 전부 달랐다)

| 시도 | 출력 | 실제 |
|---|---|---|
| 1차 | **아무것도 없음** | 락 대기하다 조용히 종료. **병합 X** |
| 2차 | "게이트 실패 — 병합을 버렸다" | 테스트 1건 실패로 **반려**. 라이브는 무사 |
| 3차 | "main에 병합 완료 — push됨" | **병합 O** ✅ |

→ 판정은 **반드시** `git branch -r --contains <커밋> | grep origin/main`으로.
  (메모리 `finish_exit0_미병합`이 이미 경고하던 것을 그대로 밟았다)

## 2차 실패는 내 것이 아니었다 (실측으로 갈랐다)

반려 사유: `test_mix_pipeline.py::test_mix_accepts_youtube_url`
```
mix_pipeline.py가 내 모듈을 쓰나  → 안 씀(script_gate·speaker grep 0건)
내 브랜치에서 그 파일 3회         → 12 passed × 3
★내 코드 없는 main 폴더에서       → 1 failed — 그런데 **다른 테스트**
                                    (test_run_render_happy_path)
```
**깨지는 테스트가 매번 바뀐다 = 내 코드 원인이 아니다**(고정으로 깨져야 한다).
당시 세션 5개가 동시에 pytest를 돌려 CPU 포화 → 부하성 플래키.
그냥 재시도하니 통과했다.

## ★트랙 브랜치가 origin에 백업 안 돼 있었다

CLAUDE.md는 "트랙 커밋은 origin에 백업되니 finish 안 해도 유실 없다"고 하는데
**실제로는 안 올라가 있었다**:
```
원격 track/화자일관성 → e06c7c5cc (남의 '워커자동조정' 병합, 무관한 커밋)
내 커밋 716935a43     → 원격에 없음 (로컬 디스크에만)
```
→ `git push origin track/<트랙명>`으로 직접 백업했다(fast-forward, force 아님).
**작업이 길어지면 finish 전에 한 번 push해 두는 게 안전하다.**

## finish_out.txt 는 건드리지 않았다

main 폴더에 `M finish_out.txt`가 세션 시작부터 떠 있다. 확인해보니
**diff가 CRLF/LF 줄바꿈뿐**(글자는 동일)이고 내용은 `대본칸설명` 트랙의 finish 로그다.
남의 트랙 파일이라 커밋하지 않았다. (누가 `finish` 출력을 리포 안으로 리다이렉트해
만든 부산물 — 원래 git에 있으면 안 되는 파일로 보인다)
