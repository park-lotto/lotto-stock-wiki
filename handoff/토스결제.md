# 토스결제 — 2026-09-15

## 한 것 (전부 라이브)
- `/pay/toss` 카드결제: 결제자 정보(성함·연락처·이메일)+환불정책·약관 동의 → `POST /api/pay/toss/order`(금액·상품명은 서버가 정함) → 토스 결제창 v2 → `/pay/toss/success`에서 **금액 대조 후** 시크릿키 승인 → `toss_payments` 테이블 기록 + 관리자 알림(ops_alert toss_paid)
- 사전신청(notice_1gi) 제출 시 sessionStorage로 결제자 칸 미리 채움
- 결제 안내(/pay) 맨 위 「💳 카드로 결제하기 (테스트)」 — 테스트 키면 (테스트) 표기
- 랜딩·/pricing: "가격 문의" → `_toss_order_name_amount()` 값(숏템메이커 1기 참가비 770,000원) + 카드결제 버튼 (토스 심사 "상품금액=결제금액")
- 금액·상품명 변경: settings `toss_amount` / `toss_order_name` (화면·결제 한 곳에서 같이 바뀜)
- 키: 서버 `/etc/shopping-shorts.env` TOSS_CLIENT_KEY / TOSS_SECRET_KEY (지금 **테스트 키 = 개발 연동 체험 상점**)
- 테스트 승인 1건 성공(09-15 13:50, mId tvivarepublica)

## 상태
- 토스 상점관리자: **주식회사 메이커스랩 심사중** → 개발자센터엔 체험 상점만. 라이브 키 없음

## ⏭
- [ ] 심사 통과 → 개발자센터 상점 선택에 메이커스랩 생기면 그 **라이브 키**로 env 교체 → 웹 재시작 → 소액 실결제·취소로 확인
- [ ] 심사에서 상품 이미지 요구 시 요금 카드에 이미지
- [ ] 결제 완료 → 회원 이용권 자동 부여는 아직 없음(지금은 알림만, 사장님 수동 승인)
