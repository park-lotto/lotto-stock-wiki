> **소유 트랙**: 레퍼런스랭킹개편 — 이 파일은 이 트랙 세션만 수정한다. 다른 트랙은 읽기만.
> 트리거: "레퍼런스랭킹개편 이어서" / "랭킹 검색 이어서"

## /refs 인스타 관리 강화 — 2026-07-24 ✅ 라이브 (브라우저 검증)

사장님: "채널명·구독자수·활동여부 보여야, 인스타 기본 저장분도, 관리 더 편하게, 정렬 잘되게."
구독자수는 Apify 과금이라 사장님이 **제외 선택**. 나머지 전부 무료로 구현.

### ✅ 완료·라이브 (main 07cb3eb7b)
- **엑셀 기본 + 손등록 통합**: `/api/refs/instagram` = `merge_tracked(load_channels()엑셀, discovered, removed)`.
  → refs 인스타 목록 50개(등록만) → **488개**(엑셀 기본까지). 사장님 "기본 저장된 거"가 이제 다 보임.
- **활동여부(무료)**: `store.instagram_activity_map()` = reel_history GROUP BY username MAX(last_seen).
  🟢활동중(≤2일)/N일 전(≤7)/🔴잠잠(≤30)/💤30일+. Apify 재조회 없음.
- **소스 뱃지**: [기본](엑셀)/[등록](손등록). **활동순 정렬** 기본 + 등록순·이름순. 검색칸.
- 표(칩→테이블): #·채널·활동·등록일·삭제. 삭제버튼 nowrap.
- 파일: `app.py`(/api/refs/instagram + merge_tracked import) · `store.py`(instagram_activity_map + added_at 노출)
  · `static/refs.html`(리치 표) · `test_channel_manage.py`(added_at 키 반영)

### ⚠️ 배포 교훈 (2026-07-24)
- 동시세션 finish 데드락 겪음 → CLAUDE.md에 **finish 전역 OS 파일락**(`_finish_gate_lock`) 추가됨(딴 세션이).
  이제 finish는 자동 순번대기 — **남의 finish kill·수동신호 불필요**. 그냥 걸어두면 락이 순서대로 돌린다.
- ★내가 `_merge` 폴더를 `rm -rf`해서 worktree 등록이 stale→"missing but already registered worktree" 에러.
  해법: `git worktree prune` 후 finish. (원칙: 함부로 _merge 폴더 손대지 말 것)

### 활동여부 버그수정 (2026-07-24, main d823836a6+2f522877a) ✅ 검증완료
- ★"활동중 0일전"이 전 채널 다 뜨던 버그: `last_seen`(매 수집마다 갱신되는 수집시각)을 활동일로
  오용 → **first_seen**(영상이 수집에 처음 잡힌 시점≈게시 근사)으로 교체(store.instagram_activity_map).
- 임계값(사장님): **하루 이내 🟢 / 2일째부터 🔴** / 30일+ 💤 / 수집이력없음.
- `upload_ts`(실제 게시시각=item.timestamp/createdAt) 컬럼+저장 추가(reel_history) — 형식(epoch/ISO)
  확정 전이라 활동계산엔 아직 미사용, 다음 수집부터 채워짐. 확인 후 활동 정밀화 가능.
- **브라우저 실측 검증(JS 카운트)**: 488개 중 🟢170 / 🔴20("🔴 2일째 새 영상 없음") / 수집이력없음298.
  다 초록 아님 = 임계값 실작동 확인.

### ⏭ 다음
1. **수집 커버리지**: "수집 이력 없음" 298개 = 최근 수집에서 안 잡힌 채널(수집 cap/활동성 필터 밖).
   이 채널들도 훑을지는 수집 커버리지 얘기 — 원하면 별도.
2. **틱톡 실등록 테스트**: 틱톡 account 시드 0개(옛 키워드캐시). @핸들 붙여넣기→지금수집→랭킹 확인.
3. **유튜브 채널기반 수집**(Phase 2) — 지금 refs 유튜브는 등록만.
4. upload_ts 형식 확인 후 활동 정밀화 / 구독자수(사장님 원하면 Apify) / 랭킹화면 기존 등록칸 /refs 일원화.
- 설계: `docs/superpowers/specs/2026-07-24-레퍼런스채널-통합관리페이지-design.md`

## 레퍼런스 채널 통합 관리페이지 /refs — 2026-07-24 ✅ 라이브 (브라우저 검증)

사장님: "인스타 채널처럼 등록해놓고 쭉 뜨게. 관리페이지 하나 만들고 엑셀처럼 등록." 확정 3(전용페이지·여러줄붙여넣기·3플랫폼통합).

### ✅ 완료·라이브 (main c8b2b144a, 커밋 43ad481a0)
- **새 페이지 `/refs`**(관리자 전용, `_require_admin` — voice_tune 패턴 route). 인스타·유튜브·틱톡 3탭.
- 탭별 **여러 줄 붙여넣기 textarea → 「일괄 등록」**(엑셀 한 열 복붙) + 결과요약(추가/중복/실패) + 등록목록 표(핸들 칩·✕삭제).
- **백엔드 신규 로직 0** — 전부 기존 API 재사용: 인스타=`/api/reference/register`(비용0, URL→username 파싱)
  +`/api/discover/added`+`/api/prune/remove`, 틱톡·유튜브=`/api/seeds`(kind=account, INSERT OR IGNORE).
- 사이드바(sidebar.js) "📋 레퍼런스 채널 관리" **admin 전용 메뉴**(ss-admin-only, is_admin일 때만 노출).
  `_pwLockSidebar`는 level=ranking_only만 도니 admin엔 🔒 안 붙음.
- 파일: `shopping_shorts/static/refs.html`(신규) · `app.py`(_refs_page route 2개) · `static/sidebar.js`(메뉴+admin노출)
- 검증(브라우저 그라운딩): 인스타 탭 등록된 50개 칩 표시·삭제 / 틱톡 탭 placeholder `@salim__mari`로 전환·도움말.

### ⏭ 다음
1. **틱톡 실등록 테스트**: 지금 틱톡 **account 시드 0개**(아까 랭킹에 뜬 todaytable_kr 등은 **옛 키워드검색 캐시**,
   계정 아님). refs 틱톡 탭에서 @핸들 붙여넣기 → 랭킹 「지금 수집」 → 계정 기반 무료수집 뜨는지 확인.
2. **유튜브 채널기반 수집**(Phase 2) — 지금 refs 유튜브 탭은 등록만 저장(수집은 키워드 기반이라 account 시드 안 훑음).
3. 랭킹 화면 기존 등록칸은 남겨둠(되돌리기 안전) — 혼란 시 나중에 /refs로 일원화.
- 설계: `docs/superpowers/specs/2026-07-24-레퍼런스채널-통합관리페이지-design.md`

## 5플랫폼 확장 Phase 1 (틱톡 자동 랭킹 노출) — 2026-07-24 ✅ 병합·라이브

**목표/제약(사장님)**: 인스타·유튜브·틱톡·샤오훙슈·도우인 5토글 / 랭킹방식 수집 / 숏폼만 /
**과금 없는 구조(Apify·건당과금 금지)**. yt-dlp 실증 후 Phase 1만 먼저 배포.

### ✅ 완료·라이브 (commit 순, main = 4c9079466 병합)
- `4967a13a` 틱톡 자동수집을 **무료 계정경로(yt-dlp 계정시드)만**으로. 유료 키워드검색(Apify)은
  `_collect_tiktok(include_paid_keywords=True)` 옵트인으로만 — 자동 경로에서 분리(삭제 아님).
- `58a859ce` yt-dlp 채널목록 간헐 빈응답(실측) → 최대 3회 재시도+백오프, 부분실패 허용.
- `b2b36d23`·(교체) 틱톡 검색 테스트를 옵트인 경로로 갱신. **계정시드 테스트는 무료경로 유지**.
- `ff8e6c02` 플랫폼 토글 노출 + `DESC_BY_PLATFORM`/`TAB_TIP` 플랫폼별 설명·지표 툴팁.
- `caadec60` 최종리뷰 Important 3건: ①플랫폼 전환 시 이전 카드 잔존(`STATE.items=d.items||[]` 무조건
  갈아끼움) ②유령 Apify 지출게이트가 무료수집을 429로 막던 것 제거(**일일제한 남용가드는 유지**) ③틱톡
  기본 라디오를 죽은 유료 `키워드검색`→`계정 시드`로.
- `dd7153e3` 계약 뒤집기: `test_ranking_platform_tabs_hidden`(숨김 계약, 07-21) →
  `test_ranking_platform_tabs`(노출 계약, 07-24). 사장님이 직접 뒤집은 요구라 게이트가 옛 계약으로 막던 것 해소.

### ⏭ 다음
1. **★브라우저 그라운딩(관리자 로그인 필요)** — 라이브 `/`는 유료게이트라 curl로 확인 불가.
   관리자로 로그인 → **틱톡 탭** 전환 → 시드 계정 등록(`@핸들`) → `지금 수집` → yt-dlp 랭킹이
   실제로 뜨는지 눈으로 확인. 미반영처럼 보이면 서버 파일엔 이미 있음(배포됨), 로그인 문제일 뿐.
2. **Phase 2 — 유튜브 채널기반 전환**: 공식 Data API로 채널 최근 Shorts(≤60s). 토글에 유튜브.
   기존 `_collect_youtube` 키워드시드 경로는 발굴용으로 남기고 채널기반을 얹는다.
3. **Phase 3 — 샤오훙슈·도우인 담기 랭킹**: 딥링크 검색(0원) → 담기 → 담긴 URL yt-dlp 지표추출 → 랭킹.
   (실증: CN 2종은 프로필 extractor 없음 → "채널 훑기"는 불가, "담아서 재기" 레인 B로.)
- 설계: `docs/superpowers/specs/2026-07-24-레퍼런스랭킹-5플랫폼확장-design.md` (강도지표 매핑·레인A/B·안정성)


## 랭킹 검색 정확도 개편 — 2026-07-19 (집PC 세션, 회사서 이어감)

**전부 커밋·병합·라이브 배포 완료.** 코드는 origin/main에 다 있음(회사 PC는 `git pull`만 하면 됨).
검색 3단 개선을 순서대로 얹었다: ①단어경계 ②상위어확장 ③썸네일 비전태그.

### ✅ 완료·라이브 (커밋 순)
1. **한국어 단어경계 매칭** (`f6ca9e19`·`456bf38a`): "가지"가 들어가지(동사)·2가지(수량)·
   여러가지·가지고(가지다)·가지런(부사)에 걸리던 거짓양성 제거. 실데이터 1→0건 확증.
   검색 시 카테고리 필터 무시(전역 검색)도 추가.
2. **상위어 확장 사전 TAXONOMY** (`1795adcf`): 빵→베이글·식빵, 야채→가지·오이, 가구→소파,
   과일→딸기 등 18개 상위어. 상위어 치면 하위종류까지. 한글자 충돌어(무·파·감·배) 제외.
3. **썸네일 비전 주제태그** (`9e0acab2` 병합): 캡션 대신 Gemini(flash-lite)가 썸네일을 읽어
   {subject, keywords} 저장 → 검색은 태그 위주(있으면 캡션 무시), 없으면 캡션 백업.
   빵빵한사운드→'빵' 오검·자막only 누락 둘 다 겨냥.
   - `video_analysis.subject_tags_vision`+`fetch_thumb_bytes` / `store.vision_tags`(shortcode 캐시)
   - `app.py` api_collect background `_tag_new_items`(1회 상한 60) + 읽기응답 `_attach_vision_tags`
   - `index.html` matchesSearch 태그우선 + `_pollVisionTags`(수집후 자동승격)
   - `scripts/backfill_vision_tags.py` (서버 실행용)

### ⏭ 다음 (회사서 이어서)
1. **★비전 태그 실관측·검증** — 로컬은 SHORTS_GEMINI_KEY 없고 썸네일URL 만료라 실 Gemini
   태깅을 **아직 눈으로 못 봤다**(배선만 12건 테스트 통과). 서버엔 shorts키 있음(댓글생성이 씀).
   → 대시보드 하드리프레시 → **"지금 수집" 클릭**(백그라운드 60개 태깅) → 1~2분 뒤 "빵"/"오이"
     검색해서 스피커 사라졌나·오이 잡히나 확인. 태그가 엉뚱하면 `_SUBJECT_TAGS_PROMPT` 튜닝.
   → 203개 다 채우려면 지금수집 2~3회, 또는 서버서 `python -m scripts.backfill_vision_tags`.
2. **남은 정밀도 이슈**(관측되면): 과일→스콘(블루베리스콘=반쯤맞음) 같은 경계 케이스는
   비전태그로 대부분 해소될 것. 실측 후 판단.

### 파일
- `shopping_shorts/static/index.html` (matchesSearch/_koreanWordHit/TAXONOMY/_expandQuery/render/doCollect/_pollVisionTags)
- `shopping_shorts/video_analysis.py` (subject_tags_vision/fetch_thumb_bytes)
- `shopping_shorts/store.py` (vision_tags 테이블+save/get/vision_tags_map)
- `shopping_shorts/app.py` (api_collect 훅·_tag_new_items·_attach_vision_tags·api_reference 조인)
- `scripts/backfill_vision_tags.py`
- 테스트: tests/test_ranking_search_js.py · test_store_vision_tags.py · test_vision_tags_pipeline.py
- 설계(구두합의, 문서화 스킵): 수집직후 백그라운드 태깅 / 태그위주+캡션백업
