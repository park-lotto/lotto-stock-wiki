# 발음교정 전역사전 + 관리자 작업대 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 어색한 문장(연음)을 관리자 작업대에서 재표기해 전역 발음사전에 저장하면, 모든 보이스·모든 렌더에서 영구 적용된다.

**Architecture:** 전역 사전은 서버 DB(`settings` kv, JSON)에 산다. 공유 합성 경로(`synthesize_line`)가 렌더 직전 프로파일의 `pronunciation.dict`에 전역 사전을 병합(`overlay`)한다. 관리자 작업대(voice_tune.html)가 붙여넣기→AI 제안→A/B→저장 UI를 제공한다.

**Tech Stack:** FastAPI, SQLite(Store), 기존 `narration_naturalize._pronunciation`(구절 치환), `edit_plan._vault_call`(Gemini), vanilla JS.

## Global Constraints

- 코드 작업은 전부 `.tracks/보이스/...` 절대경로. main 폴더 코드 파일 금지.
- git은 `git -C .tracks/보이스`. 커밋 전 `git -C .tracks/보이스 status --porcelain`로 내 파일만 확인.
- 전역 사전은 **서버 DB에만** 저장(`settings` 테이블). git-tracked 파일에 서버가 쓰면 배포 pull이 막힘(CLAUDE.md 사고 #9).
- 관리자 판정: `request.state.customer_id == 0`(=사장님). 기존 `_require_admin(request)` 재사용 — 거부 시 403 JSONResponse 반환, 통과 시 None.
- 기존 `synthesize_line` 기본 동작 불변(새 인자 기본 None → overlay 안 함) — 기존 테스트 회귀 0.
- Gemini는 `edit_plan._vault_call(prompt, schema)` 재사용(무키/실패 시 None → graceful).

---

### Task 1: `pron_corrections` 모듈 (전역 사전 load/save/overlay)

**Files:**
- Create: `shopping_shorts/pron_corrections.py`
- Test: `shopping_shorts/tests/test_pron_corrections.py`

**Interfaces:**
- Consumes: `Store.get_setting(key, default)` / `Store.set_setting(key, value)` (기존).
- Produces:
  - `load(store) -> dict`  # {phrase: respelling}, 없거나 손상 시 {}
  - `save(store, entries: dict) -> None`
  - `overlay(profile: dict|None, global_dict: dict) -> dict`  # profile.pronunciation.dict에 전역 병합(per-preset 우선), 원본 불변

- [ ] **Step 1: 실패 테스트 작성**

```python
# shopping_shorts/tests/test_pron_corrections.py
"""전역 발음교정 사전(2026-07-22). 구절→재표기를 profile.pronunciation.dict에
병합해 모든 렌더에 적용. per-preset 명시 항목은 전역보다 우선(더 구체적)."""
from shopping_shorts import pron_corrections as pc


class FakeStore:
    def __init__(self, initial=None):
        self._kv = dict(initial or {})
    def get_setting(self, key, default=None):
        return self._kv.get(key, default)
    def set_setting(self, key, value):
        self._kv[key] = value


def test_save_then_load_roundtrip():
    s = FakeStore()
    pc.save(s, {"좋은데요": "조은데요"})
    assert pc.load(s) == {"좋은데요": "조은데요"}


def test_load_missing_returns_empty():
    assert pc.load(FakeStore()) == {}


def test_load_corrupt_returns_empty():
    assert pc.load(FakeStore({"global_pron_dict": "{not json"})) == {}


def test_overlay_merges_global_into_profile():
    prof = {"pronunciation": {"on": True, "dict": {}}}
    out = pc.overlay(prof, {"좋은데요": "조은데요"})
    assert out["pronunciation"]["dict"]["좋은데요"] == "조은데요"
    assert prof["pronunciation"]["dict"] == {}          # 원본 불변


def test_overlay_per_preset_wins_on_collision():
    prof = {"pronunciation": {"on": True, "dict": {"AS": "에이에스(프리셋)"}}}
    out = pc.overlay(prof, {"AS": "에이에스(전역)"})
    assert out["pronunciation"]["dict"]["AS"] == "에이에스(프리셋)"


def test_overlay_empty_global_returns_profile_unchanged():
    prof = {"pronunciation": {"on": True, "dict": {}}}
    assert pc.overlay(prof, {}) is prof


def test_overlay_creates_pronunciation_when_absent():
    out = pc.overlay({}, {"좋은데요": "조은데요"})
    assert out["pronunciation"]["dict"] == {"좋은데요": "조은데요"}
```

- [ ] **Step 2: 실패 확인**

Run: `python -m pytest shopping_shorts/tests/test_pron_corrections.py -q`
Expected: FAIL (`ModuleNotFoundError: pron_corrections`)

- [ ] **Step 3: 모듈 구현**

```python
# shopping_shorts/pron_corrections.py
"""전역 발음교정 사전(2026-07-22, 트랙 보이스).

어색한 구절→재표기를 서버 DB(settings kv, JSON)에 한 벌로 저장하고, 공유 합성
경로가 렌더 직전 profile.pronunciation.dict 위에 병합한다. 연음 재표기는 텍스트의
성질이라 보이스와 무관 → 전역 하나로 모든 렌더·모든 보이스에 적용(설계 §2-A).

git 파일이 아니라 DB에 두는 이유: 사장님은 라이브 서버(소리 나는 곳)에서 교정하는데
서버가 git-tracked 파일에 쓰면 git status가 더러워져 auto_deploy pull이 막힌다(사고 #9).
DB(gitignore data/)는 배포 pull에 안 지워져 서버 편집이 안전하다(기존 프로파일과 동일).
"""
import copy
import json

_KEY = "global_pron_dict"


def load(store):
    """전역 발음사전 {phrase: respelling}. 없거나 JSON 손상 시 {}(graceful)."""
    raw = store.get_setting(_KEY, None)
    if not raw:
        return {}
    try:
        d = json.loads(raw)
        return d if isinstance(d, dict) else {}
    except Exception:
        return {}


def save(store, entries):
    """전역 발음사전 저장(전체 교체). entries: {phrase: respelling}."""
    store.set_setting(_KEY, json.dumps(entries or {}, ensure_ascii=False))


def overlay(profile, global_dict):
    """전역 사전을 profile.pronunciation.dict 아래에 병합한 새 profile 반환(원본 불변).

    per-preset 명시 항목이 전역보다 우선한다(더 구체적). 전역이 빈 dict면 원본 그대로
    반환(no-op). 방어적: 어떤 예외에도 원본 profile을 반환한다 — 교정은 부가기능이라
    죽이면 안 된다(기존 _synthesize_beats probe 폴백과 같은 원칙)."""
    if not global_dict:
        return profile
    try:
        out = copy.deepcopy(profile) if profile else {}
        pron = out.get("pronunciation")
        if not isinstance(pron, dict):
            pron = {"on": True, "dict": {}}
            out["pronunciation"] = pron
        merged = dict(global_dict)                 # 전역 먼저
        merged.update(pron.get("dict") or {})      # per-preset이 덮어씀(우선)
        pron["dict"] = merged
        pron.setdefault("on", True)
        return out
    except Exception:
        return profile
```

- [ ] **Step 4: 통과 확인**

Run: `python -m pytest shopping_shorts/tests/test_pron_corrections.py -q`
Expected: PASS (7 passed)

- [ ] **Step 5: 커밋**

```bash
git -C .tracks/보이스 add shopping_shorts/pron_corrections.py shopping_shorts/tests/test_pron_corrections.py
git -C .tracks/보이스 commit -m "feat(보이스): 전역 발음교정 사전 모듈(load/save/overlay)"
```

---

### Task 2: `synthesize_line`에 overlay 배선 (렌더·작업대 공통 적용)

**Files:**
- Modify: `shopping_shorts/mix_pipeline.py` (synthesize_line, _synthesize_beats, run_mix_job/run_preview/run_render 호출부)
- Test: `shopping_shorts/tests/test_pron_global_applies.py`

**Interfaces:**
- Consumes: `pron_corrections.overlay`, `pron_corrections.load` (Task 1).
- Produces: `synthesize_line(..., global_pron=None)` / `_synthesize_beats(..., global_pron=None)` — global_pron은 {phrase: respelling} 평문 dict. None/{}이면 옛 동작.

- [ ] **Step 1: 실패 테스트 작성**

```python
# shopping_shorts/tests/test_pron_global_applies.py
"""전역 발음사전이 실제 합성 텍스트에 반영되는지(2026-07-22).
synthesize_line이 global_pron을 받으면 그 구절이 재표기돼 TTS로 넘어간다."""
from shopping_shorts import mix_pipeline as mp


def test_global_pron_respells_text_into_tts(monkeypatch, tmp_path):
    seen = {}
    def fake_synth_best(text, out_path, **kw):
        seen["text"] = text
        open(out_path, "wb").write(b"x")
        return out_path
    monkeypatch.setattr(mp.tts, "synthesize_best", fake_synth_best)
    monkeypatch.setattr(mp.audio_post, "post_process", lambda *a, **k: a[1])

    out = tmp_path / "b.mp3"
    mp.synthesize_line("이건 좋은데요", out,
                       voice={"voice_id": "v", "speed": 1.0},
                       global_pron={"좋은데요": "조은데요"})
    assert "조은데요" in seen["text"]
    assert "좋은데요" not in seen["text"]


def test_no_global_pron_keeps_text(monkeypatch, tmp_path):
    seen = {}
    monkeypatch.setattr(mp.tts, "synthesize_best",
                        lambda text, out_path, **kw: (seen.__setitem__("text", text),
                                                      open(out_path, "wb").write(b"x"), out_path)[-1])
    monkeypatch.setattr(mp.audio_post, "post_process", lambda *a, **k: a[1])
    out = tmp_path / "b.mp3"
    mp.synthesize_line("이건 좋은데요", out, voice={"voice_id": "v", "speed": 1.0})
    assert "좋은데요" in seen["text"]      # 전역 미지정 → 원문 유지
```

- [ ] **Step 2: 실패 확인**

Run: `python -m pytest shopping_shorts/tests/test_pron_global_applies.py -q`
Expected: FAIL (`synthesize_line() got an unexpected keyword argument 'global_pron'`)

- [ ] **Step 3: synthesize_line + _synthesize_beats 수정**

`shopping_shorts/mix_pipeline.py` 상단 import에 추가:
```python
from shopping_shorts import pron_corrections
```

`synthesize_line` 시그니처에 `global_pron=None` 추가하고, `merge_profile` 직후 overlay:
```python
def synthesize_line(narration, out_path, *, voice=None, profile=None, beat_role=None,
                    beat_index=None, beat_total=None, previous_text=None, next_text=None,
                    ranker=asr_ranker, global_pron=None):
    voice_id, settings, speed, extra_tempo, trim, prof_v, model_id, pace_mode = _voice_params(voice)
    prof = merge_profile(profile if profile is not None else prof_v)
    # 전역 발음교정을 profile 위에 병합(설계 §2-A) — 렌더·작업대 공통 choke.
    prof = pron_corrections.overlay(prof, global_pron or {})
    natural = naturalize(narration, prof, beat_role=beat_role,
                         beat_index=beat_index, beat_total=beat_total)
```
(이하 기존 본문 유지 — n_best floor·synthesize_best·post_process 그대로.)

`_synthesize_beats` 시그니처에 `global_pron=None` 추가하고, 내부 두 `synthesize_line(` 호출에 `global_pron=global_pron` 전달:
```python
def _synthesize_beats(beats, tts_dir, *, voice, skip_existing=False, global_pron=None):
    ...
        synthesize_line(
            beat["narration"], out, voice=voice, beat_role=beat.get("role"),
            beat_index=i, beat_total=total,
            previous_text=beats[i - 1]["narration"] if i > 0 else None,
            next_text=beats[i + 1]["narration"] if i < total - 1 else None,
            global_pron=global_pron,
        )
```
(conform 재합성 경로의 두 번째 `synthesize_line(` 호출에도 `global_pron=global_pron` 추가.)

- [ ] **Step 4: 통과 확인**

Run: `python -m pytest shopping_shorts/tests/test_pron_global_applies.py shopping_shorts/tests/test_mix_naturalize.py shopping_shorts/tests/test_mix_nbest_autoavoid.py -q`
Expected: PASS (기존 회귀 0)

- [ ] **Step 5: 렌더 호출부에서 전역 사전 로드·전달**

`run_mix_job` / `run_preview` / `run_render` 각각 `store = Store(db_path)` 다음 줄에 전역 사전을 한 번 로드하고, 그 함수 안의 `_synthesize_beats(...)` 호출에 `global_pron=_gpron` 전달:
```python
    store = Store(db_path)
    _gpron = pron_corrections.load(store)
    ...
    _synthesize_beats(beats, tts_dir, voice=voice, global_pron=_gpron)   # 각 호출부
```
(각 함수에 `_synthesize_beats(` 호출이 있으므로 셋 다 동일 패턴으로 추가. skip_existing 인자를 쓰는 호출은 그 인자와 함께 `global_pron=_gpron`도 붙인다.)

- [ ] **Step 6: 회귀 확인 + 커밋**

Run: `python -m pytest shopping_shorts/tests/test_mix_pipeline.py shopping_shorts/tests/test_mix_conform.py -q`
Expected: 기존 baseline(`test_run_render_happy_path` headcopy 1건)만 실패, 신규 실패 0.

```bash
git -C .tracks/보이스 add shopping_shorts/mix_pipeline.py shopping_shorts/tests/test_pron_global_applies.py
git -C .tracks/보이스 commit -m "feat(보이스): synthesize_line에 전역 발음교정 overlay 배선(렌더+작업대 공통)"
```

---

### Task 3: 전역 사전 CRUD 엔드포인트 (관리자 게이트)

**Files:**
- Modify: `shopping_shorts/app.py` (엔드포인트 3개 추가 — 기존 `/api/voice-tune/*` 근처)
- Test: `shopping_shorts/tests/test_pron_api.py`

**Interfaces:**
- Consumes: `pron_corrections.load/save` (Task 1), `_require_admin` (기존), `Store(DB_PATH)`.
- Produces:
  - `GET /api/pron/global` → `{"entries": [{"phrase":..., "respelling":...}]}`
  - `POST /api/pron/global` body `{"phrase","respelling"}` → `{"ok": true}` (빈값·no-op은 400)
  - `DELETE /api/pron/global/{phrase}` → `{"ok": true}`

- [ ] **Step 1: 실패 테스트 작성**

```python
# shopping_shorts/tests/test_pron_api.py
"""전역 발음사전 CRUD API(2026-07-22). 관리자(customer_id==0) 전용."""
import pytest
from starlette.testclient import TestClient


@pytest.fixture
def client(monkeypatch):
    from shopping_shorts import app as appmod
    # 인증 미들웨어가 customer_id를 심는다 — 테스트는 관리자(0)로 강제.
    async def _admin_mw(request, call_next):
        request.state.customer_id = 0
        return await call_next(request)
    monkeypatch.setattr(appmod, "_pron_test_admin", 0, raising=False)
    return TestClient(appmod.app)


def test_post_then_get_roundtrip(client):
    r = client.post("/api/pron/global", json={"phrase": "좋은데요", "respelling": "조은데요"})
    assert r.status_code == 200 and r.json()["ok"] is True
    got = client.get("/api/pron/global").json()["entries"]
    assert {"phrase": "좋은데요", "respelling": "조은데요"} in got


def test_reject_empty_or_noop(client):
    assert client.post("/api/pron/global", json={"phrase": "", "respelling": "x"}).status_code == 400
    assert client.post("/api/pron/global", json={"phrase": "가", "respelling": "가"}).status_code == 400


def test_delete_removes(client):
    client.post("/api/pron/global", json={"phrase": "AS", "respelling": "에이에스"})
    assert client.delete("/api/pron/global/AS").json()["ok"] is True
    assert all(e["phrase"] != "AS" for e in client.get("/api/pron/global").json()["entries"])
```

> **주의(실행자):** 이 프로젝트 인증 미들웨어가 `request.state.customer_id`를 심는 방식은
> `shopping_shorts/tests/`의 기존 API 테스트(예: admin customers 테스트)를 먼저 열어
> 그 fixture 패턴을 그대로 따를 것. 위 fixture는 골격이며, 기존 테스트가 쓰는 실제
> 인증 우회(예: 헤더/세션/monkeypatch 대상)를 확인해 맞춘다. (기존 `_require_admin`을
> 통과시키는 방법을 재현하는 게 핵심.)

- [ ] **Step 2: 실패 확인**

Run: `python -m pytest shopping_shorts/tests/test_pron_api.py -q`
Expected: FAIL (404 — 라우트 없음)

- [ ] **Step 3: 엔드포인트 구현**

`shopping_shorts/app.py` 상단 import에 `from shopping_shorts import pron_corrections` 추가(없으면). `/api/voice-tune/profile` 엔드포인트 아래에:
```python
@app.get("/api/pron/global")
def api_pron_global_list(request: Request):
    denied = _require_admin(request)
    if denied:
        return denied
    d = pron_corrections.load(Store(DB_PATH))
    return {"entries": [{"phrase": k, "respelling": v} for k, v in d.items()]}


@app.post("/api/pron/global")
async def api_pron_global_save(request: Request):
    denied = _require_admin(request)
    if denied:
        return denied
    body = await request.json()
    phrase = (body.get("phrase") or "").strip()
    respelling = (body.get("respelling") or "").strip()
    if not phrase or not respelling or phrase == respelling:
        return JSONResponse({"error": "구절/재표기가 비었거나 동일합니다"}, status_code=400)
    store = Store(DB_PATH)
    d = pron_corrections.load(store)
    d[phrase] = respelling
    pron_corrections.save(store, d)
    return {"ok": True}


@app.delete("/api/pron/global/{phrase}")
def api_pron_global_delete(request: Request, phrase: str):
    denied = _require_admin(request)
    if denied:
        return denied
    store = Store(DB_PATH)
    d = pron_corrections.load(store)
    d.pop(phrase, None)
    pron_corrections.save(store, d)
    return {"ok": True}
```

- [ ] **Step 4: 통과 확인**

Run: `python -m pytest shopping_shorts/tests/test_pron_api.py -q`
Expected: PASS (3 passed)

- [ ] **Step 5: 커밋**

```bash
git -C .tracks/보이스 add shopping_shorts/app.py shopping_shorts/tests/test_pron_api.py
git -C .tracks/보이스 commit -m "feat(보이스): 전역 발음사전 CRUD API(관리자 게이트)"
```

---

### Task 4: AI 교정 제안 엔드포인트

**Files:**
- Modify: `shopping_shorts/app.py` (`POST /api/pron/suggest`)
- Test: `shopping_shorts/tests/test_pron_suggest.py`

**Interfaces:**
- Consumes: `edit_plan._vault_call(prompt, schema)` (기존 Gemini 경로), `_require_admin`.
- Produces: `POST /api/pron/suggest` body `{"text"}` → `{"suggestions":[{"phrase","respelling","reason"}]}`. 무키/실패 → `{"suggestions": []}`.

- [ ] **Step 1: 실패 테스트 작성**

```python
# shopping_shorts/tests/test_pron_suggest.py
"""AI 발음 교정 제안(2026-07-22). Gemini가 어색 구절→재표기 후보를 낸다.
무키/실패면 빈 목록(수동 입력 폴백) — 작업대가 안 죽는다."""
import pytest
from starlette.testclient import TestClient


@pytest.fixture
def client():
    from shopping_shorts import app as appmod
    return TestClient(appmod.app)


def test_suggest_returns_vault_result(client, monkeypatch):
    from shopping_shorts import app as appmod
    monkeypatch.setattr(appmod.edit_plan, "_vault_call",
                        lambda prompt, schema, **k: {"suggestions": [
                            {"phrase": "좋은데요", "respelling": "조은데요", "reason": "연음"}]})
    # 관리자 우회는 기존 API 테스트 fixture 패턴을 따를 것(Task 3 주의 참고).
    r = client.post("/api/pron/suggest", json={"text": "이건 좋은데요"})
    assert r.status_code == 200
    assert r.json()["suggestions"][0]["respelling"] == "조은데요"


def test_suggest_graceful_when_no_key(client, monkeypatch):
    from shopping_shorts import app as appmod
    monkeypatch.setattr(appmod.edit_plan, "_vault_call", lambda *a, **k: None)
    r = client.post("/api/pron/suggest", json={"text": "이건 좋은데요"})
    assert r.status_code == 200 and r.json()["suggestions"] == []
```

- [ ] **Step 2: 실패 확인**

Run: `python -m pytest shopping_shorts/tests/test_pron_suggest.py -q`
Expected: FAIL (404)

- [ ] **Step 3: 엔드포인트 구현**

`shopping_shorts/app.py`에 `from shopping_shorts import edit_plan`가 없으면 추가. 그리고:
```python
_PRON_SUGGEST_SCHEMA = {
    "type": "object",
    "properties": {"suggestions": {"type": "array", "items": {
        "type": "object",
        "properties": {"phrase": {"type": "string"}, "respelling": {"type": "string"},
                       "reason": {"type": "string"}},
        "required": ["phrase", "respelling"]}}},
    "required": ["suggestions"],
}


@app.post("/api/pron/suggest")
async def api_pron_suggest(request: Request):
    denied = _require_admin(request)
    if denied:
        return denied
    body = await request.json()
    text = (body.get("text") or "").strip()
    if not text:
        return {"suggestions": []}
    prompt = (
        "다음 한국어 나레이션에서 TTS가 이어발음(연음)·발음을 어색하게 읽을 만한 구절을 찾아라. "
        "각 구절마다 성우가 자연스럽게 읽도록 소리 나는 대로 다시 쓴 '재표기'를 제시하라. "
        "예: '좋은데요'→'조은데요', '같이'→'가치'. 원문에 실제로 있는 구절만. 없으면 빈 배열.\n"
        f"나레이션: {text}\n출력은 스키마 JSON만."
    )
    result = edit_plan._vault_call(prompt, _PRON_SUGGEST_SCHEMA)
    if not result or not isinstance(result.get("suggestions"), list):
        return {"suggestions": []}
    # 원문에 실제로 있는 구절만 통과(환각 제거) + no-op 제거.
    out = [s for s in result["suggestions"]
           if s.get("phrase") and s.get("respelling")
           and s["phrase"] in text and s["phrase"] != s["respelling"]]
    return {"suggestions": out}
```

- [ ] **Step 4: 통과 확인**

Run: `python -m pytest shopping_shorts/tests/test_pron_suggest.py -q`
Expected: PASS (2 passed)

- [ ] **Step 5: 커밋**

```bash
git -C .tracks/보이스 add shopping_shorts/app.py shopping_shorts/tests/test_pron_suggest.py
git -C .tracks/보이스 commit -m "feat(보이스): AI 발음교정 제안 엔드포인트(환각·no-op 필터)"
```

---

### Task 5: 작업대 페이지·API 관리자 게이트

**Files:**
- Modify: `shopping_shorts/app.py` (voice_tune 페이지 라우트 + voice-tune API 게이트)
- Test: `shopping_shorts/tests/test_voice_tune_admin_gate.py`

**Interfaces:**
- Consumes: `_require_admin`, `FileResponse`, `_STATIC`, `_NOCACHE` (기존).
- Produces: `GET /voice_tune` 와 `GET /voice_tune.html` 둘 다 관리자 아니면 403. voice-tune·pron API도 관리자 전용.

- [ ] **Step 1: 실패 테스트 작성**

```python
# shopping_shorts/tests/test_voice_tune_admin_gate.py
"""작업대(발음교정)는 관리자(customer_id==0) 전용(2026-07-22)."""
from starlette.testclient import TestClient


def test_non_admin_blocked_from_voice_tune_page():
    from shopping_shorts import app as appmod
    c = TestClient(appmod.app)
    # 비관리자(로그인했지만 customer_id != 0)를 모사 — 기존 인증 테스트 fixture 패턴을 따를 것.
    r = c.get("/voice_tune", follow_redirects=False)
    assert r.status_code in (403, 401, 302)   # 관리자 아님 → 접근 불가


def test_non_admin_blocked_from_pron_api():
    from shopping_shorts import app as appmod
    c = TestClient(appmod.app)
    r = c.get("/api/pron/global")
    assert r.status_code in (403, 401, 302)
```

> **실행자 주의:** 인증 미들웨어가 미로그인/비관리자를 어떻게 처리하는지(302 리다이렉트 vs
> 401)를 기존 테스트로 먼저 확인하고 assert를 그에 맞춰 좁힐 것. 핵심 불변식은 "비관리자는
> 작업대 페이지·pron API에 못 들어간다".

- [ ] **Step 2: 실패 확인**

Run: `python -m pytest shopping_shorts/tests/test_voice_tune_admin_gate.py -q`
Expected: FAIL (page가 mount로 무게이트 서빙되어 200)

- [ ] **Step 3: 페이지 라우트 추가(관리자 게이트)**

`shopping_shorts/app.py`의 클린 URL 라우트 블록(`for _pg in (...)` 근처, StaticFiles mount **이전**)에:
```python
def _voice_tune_page(request: Request):
    denied = _require_admin(request)
    if denied:
        return denied
    return FileResponse(_STATIC / "voice_tune.html", media_type="text/html", headers=_NOCACHE)

app.add_api_route("/voice_tune", _voice_tune_page, include_in_schema=False)
app.add_api_route("/voice_tune.html", _voice_tune_page, include_in_schema=False)
```
(명시 라우트가 `app.mount("/", StaticFiles...)`보다 먼저 매칭되므로 raw `.html` 접근도 막힌다.)

voice-tune API 4개(`/api/voice-tune/corpus|preview|synth|profile/*`)에도 각 함수 첫 줄에 게이트 추가:
```python
    denied = _require_admin(request)
    if denied:
        return denied
```
(request 인자가 없는 함수는 `request: Request`를 시그니처에 추가.)

- [ ] **Step 4: 통과 확인**

Run: `python -m pytest shopping_shorts/tests/test_voice_tune_admin_gate.py -q`
Expected: PASS

- [ ] **Step 5: 커밋**

```bash
git -C .tracks/보이스 add shopping_shorts/app.py shopping_shorts/tests/test_voice_tune_admin_gate.py
git -C .tracks/보이스 commit -m "feat(보이스): 작업대 페이지·voice-tune/pron API 관리자 게이트"
```

---

### Task 6: 작업대 발음교정 캡처 UI

**Files:**
- Modify: `shopping_shorts/static/voice_tune.html` (상단에 발음교정 카드 추가)

**Interfaces:**
- Consumes: `/api/voice-tune/synth`, `/api/pron/suggest`, `/api/pron/global` (GET/POST/DELETE).

- [ ] **Step 1: 발음교정 카드 마크업·스크립트 추가**

`#left` 최상단(`<div class="row"><select id="preset">` **위**)에 카드 삽입:
```html
<div class="card" id="pron-lab" style="border-color:#0a7">
  <b>🩺 발음교정 작업대</b> (전역 — 모든 보이스·영상 적용)
  <textarea id="pl-text" rows="2" style="width:100%;margin:6px 0" placeholder="어색하게 읽히는 문장을 붙여넣으세요"></textarea>
  <div class="row">
    <button onclick="plSynth('orig')">▶ 원본 합성</button>
    <button onclick="plSuggest()">🤖 AI 교정 제안</button>
  </div>
  <div id="pl-suggest" style="font-size:13px;margin:4px 0"></div>
  <div class="row">구절 <input id="pl-phrase" style="flex:1"> → 재표기 <input id="pl-respell" style="flex:1"></div>
  <div class="row">
    <button onclick="plSynth('fixed')">▶ 교정 합성(A/B)</button>
    <button onclick="plSave()">💾 전역 사전에 저장</button>
  </div>
  <audio id="pl-au-orig" controls style="height:32px;width:100%"></audio>
  <audio id="pl-au-fixed" controls style="height:32px;width:100%"></audio>
  <div id="pl-list" style="font-size:12px;margin-top:6px"></div>
  <p id="pl-msg" style="color:#0a7;margin:4px 0"></p>
</div>
```

`<script>` 안(기존 `init()` 위)에 함수 추가:
```javascript
function plApplyFix(t){const p=pl_phrase.value.trim(),r=pl_respell.value.trim();
  return (p&&r)?t.split(p).join(r):t;}
async function plSynth(which){
  const raw=pl_text.value.trim(); if(!raw)return;
  const text=which==='fixed'?plApplyFix(raw):raw;
  const r=await fetch('/api/voice-tune/synth',{method:'POST',headers:{'Content-Type':'application/json'},
    body:JSON.stringify({text,profile:withMeta(),preset_id:preset.value,line_id:'pl-'+which,nonce:Date.now()})});
  const j=await r.json();
  document.getElementById('pl-au-'+which).src=j.audio_url+'?t='+Date.now();
}
async function plSuggest(){
  const raw=pl_text.value.trim(); if(!raw)return;
  pl_suggest.textContent='분석 중…';
  const r=await fetch('/api/pron/suggest',{method:'POST',headers:{'Content-Type':'application/json'},
    body:JSON.stringify({text:raw})});
  const s=(await r.json()).suggestions||[];
  if(!s.length){pl_suggest.textContent='AI 제안 없음 — 직접 입력하세요';return;}
  pl_suggest.innerHTML=s.map(x=>`<a href="#" onclick="pl_phrase.value='${x.phrase}';pl_respell.value='${x.respelling}';return false">
    ${x.phrase} → <b>${x.respelling}</b></a> <span style="color:#999">${x.reason||''}</span>`).join('<br>');
}
async function plSave(){
  const phrase=pl_phrase.value.trim(),respelling=pl_respell.value.trim();
  const r=await fetch('/api/pron/global',{method:'POST',headers:{'Content-Type':'application/json'},
    body:JSON.stringify({phrase,respelling})});
  pl_msg.textContent=(r.ok)?'저장 완료 ✓ (이후 모든 영상에 적용)':'실패: '+(await r.json()).error;
  plLoadList();
}
async function plDelete(phrase){
  await fetch('/api/pron/global/'+encodeURIComponent(phrase),{method:'DELETE'});
  plLoadList();
}
async function plLoadList(){
  const e=(await (await fetch('/api/pron/global')).json()).entries||[];
  pl_list.innerHTML='<b>전역 사전('+e.length+')</b><br>'+e.map(x=>
    `${x.phrase} → ${x.respelling} <a href="#" onclick="plDelete('${x.phrase}');return false" style="color:#e33">삭제</a>`).join('<br>');
}
```

`init()` 마지막에 `plLoadList();` 호출 추가.

- [ ] **Step 2: 실제 브라우저에서 구동 확인 (grounding — 정적 점검 금지)**

서버를 띄우고(관리자 세션) `/voice_tune` 접속 → 발음교정 카드가 뜨는지 → 문장 붙여넣고 [원본 합성]/[AI 교정 제안]/[교정 합성]/[저장]/목록 삭제가 도는지 눈으로 확인. (fablize 렌더 산출물 규칙: 실제 구동 관찰 없이 완료 처리 금지.)

- [ ] **Step 3: 커밋**

```bash
git -C .tracks/보이스 add shopping_shorts/static/voice_tune.html
git -C .tracks/보이스 commit -m "feat(보이스): 작업대 발음교정 캡처 UI(붙여넣기·AI제안·A/B·전역저장·목록)"
```

---

### Task 7: produce.html 보이스 패널 접기

**Files:**
- Modify: `shopping_shorts/static/produce.html` (보이스 프리셋 패널을 `<details>`로 래핑)

- [ ] **Step 1: 패널 위치 확인**

Run: `grep -n "vpTone\|보이스\|프리셋\|영상 매칭을 완료" shopping_shorts/static/produce.html | head`
→ 보이스 프리셋 목록을 그리는 컨테이너(예: `<div id="voicePanel">` 또는 해당 섹션 헤더)를 찾는다.

- [ ] **Step 2: `<details>`로 래핑**

찾은 보이스 프리셋 섹션 컨테이너 전체를 감싼다(기본 접힘 — `open` 속성 없음):
```html
<details style="margin:8px 0">
  <summary style="cursor:pointer;font-weight:600">🎙 보이스 선택 (평소엔 접어둠 · 눌러서 펼치기)</summary>
  <!-- 기존 보이스 프리셋 패널 마크업 그대로 -->
</details>
```
기존 마크업·id·onclick 핸들러는 **한 글자도 바꾸지 않는다**(순수 래핑 — 회귀 위험 최소).

- [ ] **Step 3: 브라우저 구동 확인**

`/produce` 접속 → 보이스 패널이 기본 접힘으로 뜨는지, 펼치면 기존 기능(프리셋 선택·미리듣기)이 그대로 도는지 확인.

- [ ] **Step 4: 커밋**

```bash
git -C .tracks/보이스 add shopping_shorts/static/produce.html
git -C .tracks/보이스 commit -m "feat(보이스): produce 보이스 프리셋 패널 접기(details)"
```

---

## 최종 검증 (전체 태스크 후)

- [ ] `python -m pytest shopping_shorts/tests/ -q` → 신규 실패 0 (기존 baseline: apify 6·edit_plan_build 2·headcopy 1만).
- [ ] 라이브 반영: `py tools/track.py finish 보이스` (게이트 통과 시 3분 내 라이브).
- [ ] 라이브에서 관리자로 `/voice_tune` → 어색 문장 하나 등록 → 그 구절 든 영상 뽑아 교정 반영 청취.

## Self-Review 결과 (작성자)

- 스펙 커버리지: A(Task1·2)·B(Task3·4·5·6)·C(Task7)·D(Task1 overlay 방어 + Task2 test = 메커니즘 잠금, 영속=DB). 전부 태스크 있음.
- 플레이스홀더: 없음(코드 전부 실체). 단 Task3·5의 인증 fixture는 "기존 테스트 패턴을 따르라"는 지시로 남김 — 이 프로젝트 고유 인증 우회 방식은 구현 시 기존 테스트에서 복제해야 정확(추측 코드가 더 위험).
- 타입 일관성: `overlay(profile, global_dict)`·`load(store)`·`global_pron` dict·엔드포인트 JSON 키(`phrase`/`respelling`/`entries`/`suggestions`) 전 태스크 일치.
