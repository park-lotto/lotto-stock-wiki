# PC등록해제

## 2026-09-15
- 관리자 회원관리 🖥 배지에 **PC 골라 해제** 붙임 — API는 처음부터 `slot`을 받았는데
  화면이 안 보내 전부해제만 됐다. 이제 번호를 고르면 그 칸만 빠지고 쓰던 PC는 남는다.
- `slot` 검증 추가(비숫자 400·범위밖 400·미등록 404) + 응답에 `removed`/`left`
  — 전엔 slot=99가 0건 삭제인데 `ok:True`였다.
- 곁버그: `loadCustomers`는 **없는 함수**였고 typeof 방어에 걸려 해제·기간설정 후
  화면 갱신이 조용히 안 됐다 → `refreshCustomers()` 한 곳으로 뽑아 수리.
- 실측: 실제 브라우저 클릭 12 + 배지갱신 4 + pytest 24 passed, 사보타주로 테스트 유효성 확인.
- ⏭ 박복래님 실제 처리는 미완 — 라이브 DB 조회가 막혀 어느 PC가 안 쓰는지 못 봤다.
  배포 후 관리페이지에서 마지막 접속시각 보고 고르면 된다.
