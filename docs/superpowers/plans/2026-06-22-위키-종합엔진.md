# 위키 종합엔진 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 매일 들어오는 원자를 종목/섹터 페이지 골드 포맷에 자동으로 녹이는 종합엔진을 만든다 (반도체 1섹터 프로토타입).

**Architecture:** 추출 메타 보존(source_pub·certainty) → 입력QC가 정제+asset_level 라우팅 → stock_synth/sector_synth가 Sonnet 차분병합 → verify_gate가 certainty로 등급·자동/스테이징 결정 → rollup이 섹터 인덱스 생성. 출처는 실존 raw_file만, 모르면 "없음".

**Tech Stack:** Python 3.11, SQLite, **Claude Code(`claude -p`, Max 구독)로 Sonnet 종합** — 추가 API 과금 없음. `google-genai`(Gemini, 기존), pytest.

## Global Constraints

- 설계서: `docs/superpowers/specs/2026-06-22-위키-종합엔진-design.md` (이 계획의 근거)
- 출처 규칙: 모든 사실 행은 **입력 원자의 실존 `raw_file`만** 인용. 합치거나 날조 금지.
- 검증 규칙: 모든 주장에 검증등급. `certainty`가 등급 기본값. report(A)만 자동승격. 고위험(파트너십·납품·M&A)은 WebSearch 통과 시만 🔴.
- 정직 규칙: 원자에 없는 내용 생성 금지. 없으면 "위키에 없음".
- **종합 LLM은 Claude Code로 실행** (Max 구독 포함, 추가 API 과금 0). 원자 API(`anthropic` SDK) 직접 호출 금지.
  - 프로토타입: 서브에이전트가 직접 종합.
  - 프로덕션: `claude -p --model sonnet <프롬프트>` 헤드리스(Max). Python은 프롬프트만 빌드.
- 신규 코드는 `pipeline/atoms/` 아래, 테스트는 `pipeline/atoms/test_*.py` (기존 패턴).
- DB 컬럼 추가는 `migrate_db()` 멱등 ALTER 패턴 사용.
- 프로토타입 범위 = 반도체. 멤버십 = `wiki/L5_섹터/반도체/stock/` 폴더.

---

## File Structure

| 파일 | 책임 |
|------|------|
| `pipeline/atoms/db.py` (수정) | atoms에 `source_pub`,`certainty` 컬럼 멱등 추가 |
| `pipeline/atoms/synth_qc.py` (신규) | 원자 정제(중복·깨짐) + asset_level 라우팅 |
| `pipeline/atoms/verify_gate.py` (신규) | certainty→등급, auto/staging 라우팅 |
| `pipeline/atoms/synth_engine.py` (신규) | stock_synth·sector_synth (Sonnet 차분병합) |
| `pipeline/atoms/synth_rollup.py` (신규) | 섹터 인덱스 생성 |
| `pipeline/atoms/synth_run.py` (신규) | 오케스트레이터 (반도체 1섹터 실행) |
| `pipeline/atoms/test_synth_*.py` (신규) | 각 컴포넌트 테스트 |

---

## Task 1: DB 메타 필드 추가 (source_pub, certainty)

**Files:**
- Modify: `pipeline/atoms/db.py` (migrate_db의 컬럼 리스트)
- Test: `pipeline/atoms/test_synth_schema.py`

**Interfaces:**
- Produces: atoms 테이블에 `source_pub TEXT`, `certainty TEXT DEFAULT '불명'` 컬럼.

- [ ] **Step 1: Write the failing test**

```python
# pipeline/atoms/test_synth_schema.py
from pipeline.atoms.db import get_conn, init_db

def test_meta_columns_exist():
    init_db()
    conn = get_conn()
    cols = {r[1] for r in conn.execute("PRAGMA table_info(atoms)").fetchall()}
    conn.close()
    assert "source_pub" in cols
    assert "certainty" in cols
```

- [ ] **Step 2: Run test to verify it fails**

Run: `python -m pytest pipeline/atoms/test_synth_schema.py -v`
Expected: FAIL (source_pub 컬럼 없음)

- [ ] **Step 3: Add columns to migrate_db**

`pipeline/atoms/db.py`의 `_TG_COLUMNS` 리스트 아래에 추가하고 migrate에 포함:

```python
# db.py 상단 _TG_COLUMNS 정의 근처
_SYNTH_COLUMNS = [
    ("source_pub", "TEXT"),
    ("certainty", "TEXT DEFAULT '불명'"),
]
```

`migrate_db()` 함수의 루프를 두 리스트 모두 돌게 수정:

```python
def migrate_db():
    """기존 DB에 신규 필드 멱등 추가 (이미 있으면 무시)."""
    conn = get_conn()
    for name, decl in _TG_COLUMNS + _SYNTH_COLUMNS:
        try:
            conn.execute(f"ALTER TABLE atoms ADD COLUMN {name} {decl}")
        except sqlite3.OperationalError:
            pass  # 이미 존재
    conn.commit()
    conn.close()
```

- [ ] **Step 4: Run test to verify it passes**

Run: `python -m pytest pipeline/atoms/test_synth_schema.py -v`
Expected: PASS

- [ ] **Step 5: Commit**

```bash
git add pipeline/atoms/db.py pipeline/atoms/test_synth_schema.py
git commit -m "feat(synth): atoms에 source_pub·certainty 메타 컬럼 추가"
```

---

## Task 2: 입력QC — 정제 + 라우팅 (synth_qc)

**Files:**
- Create: `pipeline/atoms/synth_qc.py`
- Test: `pipeline/atoms/test_synth_qc.py`

**Interfaces:**
- Consumes: 원자 dict 리스트 (DB Row→dict, 키: content, asset, asset_level, raw_file).
- Produces:
  - `dedupe(atoms: list[dict]) -> list[dict]` — content 유사 중복 1개로 병합(raw_file 다중 보존 `sources` 키).
  - `route(atoms: list[dict]) -> dict` — `{"stock": {종목명: [원자...]}, "sector": [원자...]}`. asset_level=stock & asset 실존 → stock, 그 외(sector/market/theme/generic) → sector.

- [ ] **Step 1: Write the failing test**

```python
# pipeline/atoms/test_synth_qc.py
from pipeline.atoms.synth_qc import dedupe, route

def test_dedupe_merges_same_content_keeps_sources():
    atoms = [
        {"content": "SK하이닉스 12단 HBM4E 샘플 출하", "raw_file": "raw/telegram/a.md"},
        {"content": "SK하이닉스 12단 HBM4E 샘플 출하", "raw_file": "raw/telegram/b.md"},
    ]
    out = dedupe(atoms)
    assert len(out) == 1
    assert set(out[0]["sources"]) == {"raw/telegram/a.md", "raw/telegram/b.md"}

def test_route_splits_stock_vs_sector():
    atoms = [
        {"asset": "SK하이닉스", "asset_level": "stock", "content": "x"},
        {"asset": "반도체", "asset_level": "sector", "content": "y"},
        {"asset": "", "asset_level": "market", "content": "z"},
    ]
    r = route(atoms)
    assert "SK하이닉스" in r["stock"]
    assert len(r["sector"]) == 2  # 반도체(generic) + market
```

- [ ] **Step 2: Run test to verify it fails**

Run: `python -m pytest pipeline/atoms/test_synth_qc.py -v`
Expected: FAIL (synth_qc 모듈 없음)

- [ ] **Step 3: Write minimal implementation**

```python
# pipeline/atoms/synth_qc.py
"""입력QC: 원자 정제(중복) + asset_level 라우팅. 설계 §4.1."""
from difflib import SequenceMatcher

# generic asset = 종목이 아닌 섹터/테마 키워드 (종목페이지로 못 감)
_GENERIC = {"반도체", "메모리 반도체", "MLCC", "2차전지", "바이오", "조선", "방산"}
_SIM = 0.92  # content 유사도 임계


def dedupe(atoms: list[dict]) -> list[dict]:
    out: list[dict] = []
    for a in atoms:
        hit = None
        for o in out:
            if SequenceMatcher(None, a["content"], o["content"]).ratio() >= _SIM:
                hit = o
                break
        if hit:
            hit.setdefault("sources", [hit.get("raw_file")])
            if a.get("raw_file") and a["raw_file"] not in hit["sources"]:
                hit["sources"].append(a["raw_file"])
        else:
            a = dict(a)
            a["sources"] = [a.get("raw_file")] if a.get("raw_file") else []
            out.append(a)
    return out


def route(atoms: list[dict]) -> dict:
    stock: dict[str, list] = {}
    sector: list = []
    for a in atoms:
        asset = (a.get("asset") or "").strip()
        level = a.get("asset_level") or "sector"
        if level == "stock" and asset and asset not in _GENERIC:
            stock.setdefault(asset, []).append(a)
        else:
            sector.append(a)
    return {"stock": stock, "sector": sector}
```

- [ ] **Step 4: Run test to verify it passes**

Run: `python -m pytest pipeline/atoms/test_synth_qc.py -v`
Expected: PASS (2 passed)

- [ ] **Step 5: Commit**

```bash
git add pipeline/atoms/synth_qc.py pipeline/atoms/test_synth_qc.py
git commit -m "feat(synth): 입력QC 중복제거+asset_level 라우팅"
```

---

## Task 3: 검증게이트 — certainty→등급 (verify_gate)

**Files:**
- Create: `pipeline/atoms/verify_gate.py`
- Test: `pipeline/atoms/test_verify_gate.py`

**Interfaces:**
- Consumes: 원자 dict (키: source_trust, certainty, content).
- Produces:
  - `grade(atom: dict) -> dict` — `{"grade": "검증완료|미검증|설", "route": "auto|staging", "high_risk": bool}`.
  - 규칙: source_trust=='A' → auto/검증완료. certainty=='단독설' → 설. certainty in (관측,전망,불명) → 미검증. 고위험 키워드(파트너십·납품·인수·수주·계약) → high_risk=True & route=staging (WebSearch 전까지).

- [ ] **Step 1: Write the failing test**

```python
# pipeline/atoms/test_verify_gate.py
from pipeline.atoms.verify_gate import grade

def test_report_A_auto_verified():
    g = grade({"source_trust": "A", "certainty": "사실", "content": "TP 상향"})
    assert g["grade"] == "검증완료" and g["route"] == "auto"

def test_telegram_rumor_staged():
    g = grade({"source_trust": "C", "certainty": "단독설", "content": "최태원 머스크 회동 예정"})
    assert g["grade"] == "설" and g["route"] == "staging"

def test_high_risk_claim_flagged():
    g = grade({"source_trust": "C", "certainty": "관측", "content": "삼성과 납품 계약 체결"})
    assert g["high_risk"] is True and g["route"] == "staging"
```

- [ ] **Step 2: Run test to verify it fails**

Run: `python -m pytest pipeline/atoms/test_verify_gate.py -v`
Expected: FAIL (verify_gate 없음)

- [ ] **Step 3: Write minimal implementation**

```python
# pipeline/atoms/verify_gate.py
"""검증게이트: certainty·신뢰도→등급, auto/staging 라우팅. 설계 §4.3."""

_HIGH_RISK = ("파트너십", "납품", "인수", "수주", "계약", "합병", "M&A")
_GRADE_BY_CERTAINTY = {
    "사실": "검증완료", "관측": "미검증", "전망": "미검증",
    "단독설": "설", "불명": "미검증",
}


def grade(atom: dict) -> dict:
    trust = atom.get("source_trust", "D")
    cert = atom.get("certainty", "불명")
    content = atom.get("content", "")
    high_risk = any(k in content for k in _HIGH_RISK)

    g = _GRADE_BY_CERTAINTY.get(cert, "미검증")
    # report A + 사실 → 자동 검증완료 (단 고위험은 여전히 확인)
    if trust == "A" and cert == "사실" and not high_risk:
        return {"grade": "검증완료", "route": "auto", "high_risk": False}
    if high_risk:
        return {"grade": g, "route": "staging", "high_risk": True}
    if g == "검증완료":  # A 아닌데 사실 주장 → 신뢰도 따라
        route = "auto" if trust in ("A", "B") else "staging"
        return {"grade": g, "route": route, "high_risk": False}
    return {"grade": g, "route": "staging", "high_risk": False}
```

- [ ] **Step 4: Run test to verify it passes**

Run: `python -m pytest pipeline/atoms/test_verify_gate.py -v`
Expected: PASS (3 passed)

- [ ] **Step 5: Commit**

```bash
git add pipeline/atoms/verify_gate.py pipeline/atoms/test_verify_gate.py
git commit -m "feat(synth): 검증게이트 certainty→등급+staging 라우팅"
```

---

## Task 4: 종합엔진 — stock_synth (Sonnet 차분병합)

**Files:**
- Create: `pipeline/atoms/synth_engine.py`
- Test: `pipeline/atoms/test_synth_engine.py`

**Interfaces:**
- Consumes: `route()` 출력의 stock 묶음, `grade()`.
- Produces:
  - `_build_prompt(page_md: str, atoms: list[dict]) -> str` (순수함수, 테스트 대상).
  - `synth_stock(page_path: Path, atoms: list[dict], *, dry_run=False) -> str` (Sonnet 호출, updated_md 반환).
- **TDD 주의**: Sonnet 호출은 비결정적이라 *프롬프트 빌더*를 단위 테스트한다. 실제 LLM 호출은 dry_run/통합 테스트로 분리.

- [ ] **Step 1: Write the failing test (프롬프트 빌더)**

```python
# pipeline/atoms/test_synth_engine.py
from pipeline.atoms.synth_engine import _build_prompt

def test_prompt_includes_page_atoms_and_rules():
    page = "# SK하이닉스\n## 최신 이벤트\n| 날짜 | 내용 | 출처 | 신호 |\n"
    atoms = [{"content": "12단 HBM4E 샘플 출하", "date": "2026-06-19",
              "sources": ["raw/telegram/a.md"], "certainty": "관측"}]
    p = _build_prompt(page, atoms)
    # 핵심 규칙·데이터가 프롬프트에 들어갔는지
    assert "12단 HBM4E" in p
    assert "raw/telegram/a.md" in p          # 실존 출처 전달
    assert "날조" in p or "실존" in p          # 출처 날조 금지 규칙
    assert "위키에 없" in p or "지어내" in p    # 정직 규칙
```

- [ ] **Step 2: Run test to verify it fails**

Run: `python -m pytest pipeline/atoms/test_synth_engine.py -v`
Expected: FAIL (synth_engine 없음)

- [ ] **Step 3: Write implementation (빌더 + Sonnet 호출)**

```python
# pipeline/atoms/synth_engine.py
"""종합엔진: 종목/섹터 페이지를 신규 원자로 차분병합. 설계 §4.2/4.2b."""
import os
from pathlib import Path

_MODEL = "claude-sonnet-4-6"

_RULES = """너는 주식 위키 종합 편집자다. 아래 [현재 페이지]에 [신규 원자]를 녹여 갱신본을 출력하라.
철칙:
1. 출처는 신규 원자의 sources에 있는 **실존 파일명만** 인용. 두 파일을 합치거나 새 파일명을 지어내지 마라.
2. 원자에 없는 사실을 만들지 마라. 모르면 적지 마라. (할루시네이션 금지)
3. certainty가 '관측/전망/단독설'이면 평서문 사실로 쓰지 말고 `⚠️미검증` 또는 `(설)`을 붙여라.
4. 섹션 규칙: 최신 이벤트=7일 롤링 표 상단 추가 / 증권사 컨센서스=증권사당 1행 덮어쓰기 / 리포트 논거=30일 롤링 WHY보존 / 종합 스토리=최신 단락 상단 추가.
5. 기존 골드 내용은 보존하고 신규만 추가/갱신하라.
출력은 갱신된 마크다운 전체. 설명 문장 없이 마크다운만."""


def _build_prompt(page_md: str, atoms: list[dict]) -> str:
    lines = ["[신규 원자]"]
    for a in atoms:
        srcs = " · ".join(a.get("sources") or [a.get("raw_file", "?")])
        cert = a.get("certainty", "불명")
        lines.append(f"- [{a.get('date','?')}] (certainty={cert}) {a['content']}  ← {srcs}")
    atoms_block = "\n".join(lines)
    return f"{_RULES}\n\n[현재 페이지]\n{page_md}\n\n{atoms_block}"


def _call_sonnet(prompt: str) -> str:
    """Claude Code 헤드리스로 종합 (Max 구독, 추가 API 과금 없음).
    원자 anthropic SDK 직접호출 금지 — claude -p 사용."""
    import subprocess
    r = subprocess.run(
        ["claude", "-p", "--model", "sonnet", prompt],
        capture_output=True, text=True, encoding="utf-8",
    )
    if r.returncode != 0:
        raise RuntimeError(f"claude -p 실패: {r.stderr[:300]}")
    return r.stdout.strip()


def synth_stock(page_path: Path, atoms: list[dict], *, dry_run: bool = False) -> str:
    page_md = page_path.read_text(encoding="utf-8")
    prompt = _build_prompt(page_md, atoms)
    if dry_run:
        return prompt
    updated = _call_sonnet(prompt)
    page_path.write_text(updated, encoding="utf-8")
    return updated
```

- [ ] **Step 4: Run test to verify it passes**

Run: `python -m pytest pipeline/atoms/test_synth_engine.py -v`
Expected: PASS

- [ ] **Step 5: Commit**

```bash
git add pipeline/atoms/synth_engine.py pipeline/atoms/test_synth_engine.py
git commit -m "feat(synth): stock_synth 프롬프트빌더+Sonnet 차분병합"
```

---

## Task 5: 섹터 종합 — sector_synth

**Files:**
- Modify: `pipeline/atoms/synth_engine.py` (sector_synth 추가)
- Test: `pipeline/atoms/test_synth_engine.py` (테스트 추가)

**Interfaces:**
- Consumes: `route()` 출력의 sector 리스트, 섹터 페이지의 "시장 국면" 섹션.
- Produces: `synth_sector(sector_page: Path, atoms: list[dict], *, dry_run=False) -> str`.

- [ ] **Step 1: Write the failing test**

```python
# test_synth_engine.py 에 추가
from pipeline.atoms.synth_engine import _build_sector_prompt

def test_sector_prompt_targets_market_section():
    page = "# 반도체 섹터\n## 시장 국면\n"
    atoms = [{"content": "반도체 ETF 1조달러 손실", "date": "2026-06-06",
              "sources": ["raw/news/x.md"], "certainty": "사실"}]
    p = _build_sector_prompt(page, atoms)
    assert "시장 국면" in p
    assert "반도체 ETF" in p
    assert "raw/news/x.md" in p
```

- [ ] **Step 2: Run test to verify it fails**

Run: `python -m pytest pipeline/atoms/test_synth_engine.py::test_sector_prompt_targets_market_section -v`
Expected: FAIL

- [ ] **Step 3: Add sector functions to synth_engine.py**

```python
# synth_engine.py 에 추가
_SECTOR_RULES = """너는 섹터 위키 편집자다. [현재 섹터페이지]의 "시장 국면·테마 코멘트" 섹션에 [신규 원자]를 녹여라.
철칙은 종목과 동일(실존출처만·할루시네이션금지·미검증표기). 개별 종목 깊이가 아니라 섹터 전체 국면·수급·테마 흐름을 종합하라.
출력은 갱신된 마크다운 전체."""


def _build_sector_prompt(page_md: str, atoms: list[dict]) -> str:
    lines = ["[신규 원자]"]
    for a in atoms:
        srcs = " · ".join(a.get("sources") or [a.get("raw_file", "?")])
        lines.append(f"- [{a.get('date','?')}] (certainty={a.get('certainty','불명')}) {a['content']}  ← {srcs}")
    return f"{_SECTOR_RULES}\n\n[현재 섹터페이지]\n{page_md}\n\n" + "\n".join(lines)


def synth_sector(sector_page: Path, atoms: list[dict], *, dry_run: bool = False) -> str:
    page_md = sector_page.read_text(encoding="utf-8")
    prompt = _build_sector_prompt(page_md, atoms)
    if dry_run:
        return prompt
    updated = _call_sonnet(prompt)
    sector_page.write_text(updated, encoding="utf-8")
    return updated
```

- [ ] **Step 4: Run test to verify it passes**

Run: `python -m pytest pipeline/atoms/test_synth_engine.py -v`
Expected: PASS (2 passed)

- [ ] **Step 5: Commit**

```bash
git add pipeline/atoms/synth_engine.py pipeline/atoms/test_synth_engine.py
git commit -m "feat(synth): sector_synth 섹터 국면 종합 추가"
```

---

## Task 6: 롤업 — 섹터 인덱스 (synth_rollup)

**Files:**
- Create: `pipeline/atoms/synth_rollup.py`
- Test: `pipeline/atoms/test_synth_rollup.py`

**Interfaces:**
- Consumes: `wiki/L5_섹터/{섹터}/stock/*.md` 종목 페이지들.
- Produces: `build_index(sector_dir: Path) -> str` — 각 종목 페이지의 종합스토리 첫 줄을 모은 섹터 인덱스 마크다운 (종목명·최신신호·한줄).

- [ ] **Step 1: Write the failing test**

```python
# pipeline/atoms/test_synth_rollup.py
from pathlib import Path
from pipeline.atoms.synth_rollup import build_index

def test_index_collects_stock_oneliner(tmp_path):
    sd = tmp_path / "stock"; sd.mkdir()
    (sd / "stock_AAA.md").write_text(
        "# AAA\n## 종합 스토리\n> **2026-06-22 갱신**: 강한 모멘텀\n", encoding="utf-8")
    md = build_index(tmp_path)
    assert "AAA" in md
    assert "강한 모멘텀" in md
```

- [ ] **Step 2: Run test to verify it fails**

Run: `python -m pytest pipeline/atoms/test_synth_rollup.py -v`
Expected: FAIL

- [ ] **Step 3: Write minimal implementation**

```python
# pipeline/atoms/synth_rollup.py
"""롤업: 종목 페이지 종합스토리 첫줄 → 섹터 인덱스. 설계 §4.4."""
import re
from pathlib import Path


def _first_story_line(md: str) -> str:
    m = re.search(r"## 종합 스토리\s*\n>\s*(.+)", md)
    return m.group(1).strip() if m else ""


def build_index(sector_dir: Path) -> str:
    rows = ["# 섹터 인덱스 (자동 롤업)", "", "| 종목 | 최신 한줄 |", "|------|----------|"]
    for f in sorted((sector_dir / "stock").glob("stock_*.md")):
        name = f.stem.replace("stock_", "")
        line = _first_story_line(f.read_text(encoding="utf-8"))
        if line:
            rows.append(f"| {name} | {line[:120]} |")
    return "\n".join(rows)
```

- [ ] **Step 4: Run test to verify it passes**

Run: `python -m pytest pipeline/atoms/test_synth_rollup.py -v`
Expected: PASS

- [ ] **Step 5: Commit**

```bash
git add pipeline/atoms/synth_rollup.py pipeline/atoms/test_synth_rollup.py
git commit -m "feat(synth): 섹터 인덱스 롤업"
```

---

## Task 7: 오케스트레이터 + 질문 7종 수용 테스트 (synth_run)

**Files:**
- Create: `pipeline/atoms/synth_run.py`
- Create: `pipeline/atoms/test_synth_acceptance.py`

**Interfaces:**
- Consumes: 위 전부 (synth_qc, verify_gate, synth_engine, synth_rollup).
- Produces: `run_sector(sector="반도체", *, days=14, dry_run=False)` — 그날 변동 종목만 stock_synth, 섹터레벨은 sector_synth, 끝에 rollup. staging은 `pipeline/atoms/staging/`에 적재.

- [ ] **Step 1: Write the orchestrator**

```python
# pipeline/atoms/synth_run.py
"""오케스트레이터: 반도체 1섹터 종합 실행. 설계 §3."""
import argparse, json
from datetime import date, timedelta
from pathlib import Path

from .db import get_conn, init_db
from .synth_qc import dedupe, route
from .verify_gate import grade
from .synth_engine import synth_stock, synth_sector
from .synth_rollup import build_index

_ROOT = Path(__file__).parent.parent.parent
_STAGING = Path(__file__).parent / "staging"


def _load_atoms(sector: str, days: int) -> list[dict]:
    cutoff = (date.today() - timedelta(days=days)).isoformat()
    conn = get_conn()
    rows = conn.execute(
        "SELECT date,content,asset,asset_level,raw_file,source_trust,certainty "
        "FROM atoms WHERE sector=? AND is_active=1 AND date>=?", (sector, cutoff)).fetchall()
    conn.close()
    return [dict(r) for r in rows]  # get_conn은 row_factory=sqlite3.Row 사용


def _find_stock_page(sector: str, name: str) -> Path | None:
    p = _ROOT / "wiki" / "L5_섹터" / sector / "stock" / f"stock_{name}.md"
    return p if p.exists() else None


def run_sector(sector: str = "반도체", *, days: int = 14, dry_run: bool = False):
    init_db()
    atoms = dedupe(_load_atoms(sector, days))
    for a in atoms:  # 검증등급 부여
        a["verdict"] = grade(a)
    routed = route(atoms)
    _STAGING.mkdir(exist_ok=True)

    # 종목 종합 (auto만 페이지로, staging은 적재)
    for name, items in routed["stock"].items():
        page = _find_stock_page(sector, name)
        if not page:
            continue
        autos = [a for a in items if a["verdict"]["route"] == "auto"]
        staged = [a for a in items if a["verdict"]["route"] == "staging"]
        if autos:
            synth_stock(page, autos, dry_run=dry_run)
        if staged:
            (_STAGING / f"{name}.json").write_text(
                json.dumps(staged, ensure_ascii=False, indent=2), encoding="utf-8")

    # 섹터 종합
    sector_page = _ROOT / "wiki" / "L5_섹터" / sector / f"sector_{sector}.md"
    sec_autos = [a for a in routed["sector"] if a["verdict"]["route"] == "auto"]
    if sector_page.exists() and sec_autos:
        synth_sector(sector_page, sec_autos, dry_run=dry_run)

    # 롤업
    index = build_index(_ROOT / "wiki" / "L5_섹터" / sector)
    if not dry_run:
        (_ROOT / "wiki" / "L5_섹터" / sector / "index_auto.md").write_text(index, encoding="utf-8")
    print(f"[synth] {sector}: 종목 {len(routed['stock'])} / 섹터원자 {len(routed['sector'])} / staging {len(list(_STAGING.glob('*.json')))}")


if __name__ == "__main__":
    ap = argparse.ArgumentParser()
    ap.add_argument("--sector", default="반도체")
    ap.add_argument("--days", type=int, default=14)
    ap.add_argument("--dry-run", action="store_true")
    args = ap.parse_args()
    run_sector(args.sector, days=args.days, dry_run=args.dry_run)
```

- [ ] **Step 2: Write the acceptance test (질문 7종 — 구조 검증)**

수용 테스트는 LLM 비결정성을 피해 **페이지가 7종 질문에 답할 구조를 갖췄는지** 검증한다.

```python
# pipeline/atoms/test_synth_acceptance.py
from pathlib import Path

ROOT = Path(__file__).parent.parent.parent
SK = ROOT / "wiki/L5_섹터/반도체/stock/stock_SK하이닉스.md"

def test_q1_q7_structure_present():
    md = SK.read_text(encoding="utf-8")
    assert "## 종합 스토리" in md          # Q1 현재
    assert "증권사 컨센서스" in md          # Q2 히스토리
    assert "출처" in md                     # Q3 근거
    assert "충돌" in md or "모순" in md      # Q7 충돌

def test_no_fabricated_source_merge():
    # 날조 출처 패턴(파일 두 개를 ·로 한 .md 안에 합침) 금지
    md = SK.read_text(encoding="utf-8")
    import re
    bad = re.findall(r"[가-힣A-Za-z]+\.md\s*·\s*[가-힣A-Za-z]+(?!\.md)", md)
    assert not bad, f"날조 의심 출처: {bad}"
```

- [ ] **Step 3: Run dry-run on 반도체 (수동 확인)**

Run: `python -m pipeline.atoms.synth_run --sector 반도체 --days 14 --dry-run`
Expected: 프롬프트 생성·라우팅 카운트 출력, 에러 없음. (실제 Sonnet 호출 없음)

- [ ] **Step 4: Run acceptance tests**

Run: `python -m pytest pipeline/atoms/test_synth_acceptance.py -v`
Expected: PASS

- [ ] **Step 5: Commit**

```bash
git add pipeline/atoms/synth_run.py pipeline/atoms/test_synth_acceptance.py
git commit -m "feat(synth): 오케스트레이터+질문7종 수용테스트 (반도체)"
```

---

## Task 8: 반도체 실전 1회 + 사람 검수

**Files:**
- 실행만 (코드 변경 없음). 결과는 wiki 종목 페이지.

- [ ] **Step 1: Claude Code 헤드리스 가용 확인 (Max 구독, API 키 불필요)**

Run: `claude -p --model sonnet "한 단어로 답: 작동"`
Expected: 정상 응답 (별도 ANTHROPIC_API_KEY 불필요 — Max 구독 사용)
- 만약 nested 세션 문제로 `claude -p`가 막히면: 서브에이전트가 직접 종합(프롬프트=`synth_stock(dry_run=True)` 출력)하고 결과를 페이지에 기록.

- [ ] **Step 2: 실전 실행 (auto만 반영)**

Run: `python -m pipeline.atoms.synth_run --sector 반도체 --days 14`
Expected: 종목 페이지 갱신, staging/*.json 적재, index_auto.md 생성
- 비용: Claude Code(Max) 사용 → 추가 API 과금 없음

- [ ] **Step 3: 사람 검수 (5종목 표본)**

SK하이닉스·삼성전자·한미반도체·삼성전기 페이지 열어 확인:
- 출처가 실존 파일명인가 (404 없는가)
- 미검증 주장에 ⚠️ 붙었는가
- 기존 골드 내용 안 날아갔는가

- [ ] **Step 4: 합격이면 커밋 / 불합격이면 결함 기록**

```bash
git add "wiki/L5_섹터/반도체/" pipeline/atoms/staging/
git commit -m "synth: 반도체 1섹터 종합엔진 실전 1회 (검수 통과)"
```

---

## Self-Review (작성자 점검 완료)

- **스펙 커버리지**: §4.0(메타)→T1, §4.1(qc)→T2, §4.3(게이트)→T3, §4.2(stock)→T4, §4.2b(sector)→T5, §4.4(롤업)→T6, §3 오케스트레이션·§2 질문7종→T7, 검수→T8. ✅
- **미커버**: §4.0의 *Gemini 추출 프롬프트에 source_pub·certainty 추가*는 LLM 비결정 작업이라 별도(T1은 컬럼만). → **알려진 한계**: 프로토타입은 기존 원자(certainty=불명)로 돌리며, 불명은 verify_gate가 자동승격 안 함(안전). 추출 프롬프트 보강은 반도체 검증 후 별도 태스크.
- **플레이스홀더**: 없음 (모든 스텝 실제 코드/명령 포함).
- **타입 일관성**: `route()`→`{stock,sector}`, `grade()`→`{grade,route,high_risk}`, `synth_stock/synth_sector(page,atoms,dry_run)` 전 태스크 일치. ✅
