# 1기 챌린지 관리 시스템 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 1기 멤버가 하루 2영상 챌린지 영상을 링크로 제출하고, 사장님이 달성현황과 영상을 한 화면에서 확인하는 시스템.

**Architecture:** 제출은 **저장 먼저·수집 나중**. 링크가 들어온 순간 `challenge_submission`에 행이 생기고 달성 카운트가 확정된다(수집 실패가 카운트를 갉아먹지 않는다). 썸네일·조회수는 기존 `api_reference_adopt` 경로를 재사용해 백그라운드로 채운다. 판정 로직은 `challenge.py`에 모으고 `app.py` 라우트는 얇게 둔다(app.py는 이미 13,000줄 초과).

**Tech Stack:** FastAPI · SQLite(`Store`) · pytest + `fastapi.testclient` · 바닐라 JS(빌드 없음)

**설계 문서:** `docs/superpowers/specs/2026-08-24-1기챌린지-관리시스템-design.md`

**작업 폴더:** `C:\Users\CH\Desktop\로또의 주식\.tracks\챌린지관리`
모든 편집·git은 이 폴더에서. main 폴더의 코드 파일은 **하나도 건드리지 않는다**(흡수 방지).

---

## 코드베이스에서 알아야 할 것 (실측 확인됨)

새로 짜지 말고 **이미 있는 것을 재사용**한다. 아래는 전부 직접 읽어서 확인한 사실이다.

| 무엇 | 어디 | 쓰는 법 |
|---|---|---|
| 로그인 회원 식별 | `app.py:1052` `_cid(request)` | `cid = _cid(request)` → 0이면 비로그인/사장님 |
| 관리자 가드 | `app.py:8375` `_require_admin(request)` | `denied = _require_admin(request)` → truthy면 그대로 return |
| 플랫폼 판별 | `app.py:8940` `_grab_platform(url)` | `"youtube"/"tiktok"/"instagram"/""` 반환 |
| 영상 1건 수집 | `app.py:9968` `api_reference_adopt` | 내부에서 `probe_grab_meta` + 인스타 보강. 이 흐름을 참고 |
| 스키마 생성 | `store.py:1365` `ensure_paywall_schema()` | 테스트에서 `s.ensure_paywall_schema()` 호출 패턴 |
| 세션 쿠키 | `app.py` `_sign_session(cid, exp)` | 테스트 로그인 위조에 사용 |

**테스트 작성 패턴** — `tests/test_paywall_gate.py:1-20`을 그대로 따른다:

```python
from datetime import datetime, timezone
from fastapi.testclient import TestClient
from shopping_shorts import app as appmod
from shopping_shorts.store import Store


def _cookie(cid):
    exp = int(datetime.now(timezone.utc).timestamp()) + 3600
    return appmod._sign_session(cid, exp)


def _setup(tmp_path, monkeypatch):
    monkeypatch.setattr(appmod, "DB_PATH", str(tmp_path / "t.db"))
    monkeypatch.setattr(appmod, "_AUTH_ON", True)
    monkeypatch.setattr(appmod, "DASH_SECRET", "test-secret-xyz")
    s = Store(str(tmp_path / "t.db"))
    s.ensure_paywall_schema()
    return s
```

### ⚠️ 이 코드베이스에서 반복해서 사고를 낸 것들

- **같은 판단을 두 번 적지 마라**(CLAUDE.md 0순위-B). `submit_day`(하루 판정)와 `dedup_key`(중복 판정)는 **저장 시점에 한 번** 계산해 컬럼에 넣는다. 조회할 때 다시 계산하지 않는다.
- **테스트에 고정 날짜를 박지 마라** — 며칠 뒤 반드시 깨진다. 상대 날짜(`timedelta`)를 쓴다.
- **`except: pass`로 오류를 삼키지 마라** — SQL 오류가 묻혀 라이브에서 0건이 된 전례가 있다. 수집 실패는 **삼키되 반드시 `fetch_status='failed'`로 기록**한다.
- **`finish`는 exit 0이어도 병합이 안 됐을 수 있다** — 판정은 `git branch -r --contains <커밋>`으로.

---

## 파일 구조

| 파일 | 책임 |
|---|---|
| `shopping_shorts/challenge.py` | **신설.** 순수 판정 로직 — KST 날짜, 중복키, 기간 검사, 달성 집계. DB·HTTP를 모른다 → 테스트가 쉽다 |
| `shopping_shorts/store.py` | 테이블 2개 + CRUD 메서드 |
| `shopping_shorts/app.py` | 라우트 8개(얇게) + 백그라운드 수집 |
| `shopping_shorts/static/challenge.html` | 멤버 제출 화면 |
| `shopping_shorts/static/challenge_admin.html` | 관리 화면(탭 2개) |
| `shopping_shorts/static/sidebar.js` | 메뉴 1줄 |
| `shopping_shorts/tests/test_challenge.py` | 판정 로직 테스트 |
| `shopping_shorts/tests/test_challenge_api.py` | 라우트·권한 테스트 |

`challenge.py`가 DB를 모르게 만드는 것이 핵심이다. 그래야 Task 1~2의 테스트가 DB 없이 즉시 돌고, 판정 규칙이 한 곳에만 존재한다.

---

## Task 1: 판정 로직 — KST 날짜와 기간

**Files:**
- Create: `shopping_shorts/challenge.py`
- Test: `shopping_shorts/tests/test_challenge.py`

- [ ] **Step 1: 실패하는 테스트를 쓴다**

`shopping_shorts/tests/test_challenge.py`를 새로 만든다:

```python
"""1기 챌린지 — 판정 로직(DB·HTTP 없음)."""
from datetime import datetime, timezone, timedelta

from shopping_shorts import challenge


def test_kst_day_shifts_at_utc_1500():
    """UTC 15:00 = KST 익일 00:00 — 이 경계가 하루 판정을 가른다."""
    # UTC 2026-08-24 14:59 → 아직 KST 8/24
    t1 = datetime(2026, 8, 24, 14, 59, tzinfo=timezone.utc)
    assert challenge.kst_day(t1) == "2026-08-24"
    # UTC 2026-08-24 15:00 → KST로는 벌써 8/25
    t2 = datetime(2026, 8, 24, 15, 0, tzinfo=timezone.utc)
    assert challenge.kst_day(t2) == "2026-08-25"


def test_kst_day_naive_datetime_treated_as_utc():
    """tzinfo 없는 값이 들어와도 UTC로 보고 계산한다(조용히 하루가 밀리면 안 된다)."""
    naive = datetime(2026, 8, 24, 15, 0)
    assert challenge.kst_day(naive) == "2026-08-25"


def test_in_period_inclusive_both_ends():
    """시작일·종료일 당일은 포함이다."""
    assert challenge.in_period("2026-08-24", "2026-08-24", "2026-09-23") is True
    assert challenge.in_period("2026-09-23", "2026-08-24", "2026-09-23") is True
    assert challenge.in_period("2026-08-23", "2026-08-24", "2026-09-23") is False
    assert challenge.in_period("2026-09-24", "2026-08-24", "2026-09-23") is False


def test_in_period_open_ended_when_unset():
    """기간을 아직 설정 안 했으면 막지 않는다 — 설정 누락이 제출을 막으면 안 된다."""
    assert challenge.in_period("2026-08-24", "", "") is True
    assert challenge.in_period("2026-08-24", "2026-08-01", "") is True
    assert challenge.in_period("2026-07-01", "2026-08-01", "") is False
```

- [ ] **Step 2: 실패를 확인한다**

Run: `py -m pytest shopping_shorts/tests/test_challenge.py -v`
Expected: FAIL — `ModuleNotFoundError: No module named 'shopping_shorts.challenge'`

- [ ] **Step 3: 최소 구현**

`shopping_shorts/challenge.py`를 새로 만든다:

```python
"""1기 챌린지 — 순수 판정 로직.

DB도 HTTP도 모른다. 여기 있는 함수는 값만 받아 값만 돌려준다.
그래야 규칙이 한 곳에만 존재하고(0순위-B), 테스트가 DB 없이 즉시 돈다.
"""
from datetime import datetime, timezone, timedelta

KST = timezone(timedelta(hours=9))


def kst_day(dt=None):
    """UTC 시각 → 한국 날짜 문자열 'YYYY-MM-DD'.

    ★하루 2영상의 '하루'가 여기서 정해진다. 저장 시점에 한 번 불러
    컬럼에 넣고, 조회할 때 다시 계산하지 않는다 — 계산이 두 군데 있으면
    언젠가 어긋난다(0순위-B).
    """
    if dt is None:
        dt = datetime.now(timezone.utc)
    if dt.tzinfo is None:
        dt = dt.replace(tzinfo=timezone.utc)
    return dt.astimezone(KST).strftime("%Y-%m-%d")


def in_period(day, start, end):
    """day가 챌린지 기간 안인가. 전부 'YYYY-MM-DD' 문자열(사전순=시간순).

    start/end가 비어 있으면 그쪽 끝은 열어둔다 — 사장님이 아직 기간을
    설정하지 않았다고 해서 멤버 제출이 막히면 안 된다.
    """
    if start and day < start:
        return False
    if end and day > end:
        return False
    return True
```

- [ ] **Step 4: 통과를 확인한다**

Run: `py -m pytest shopping_shorts/tests/test_challenge.py -v`
Expected: PASS — 4 passed

- [ ] **Step 5: 커밋**

```bash
git add shopping_shorts/challenge.py shopping_shorts/tests/test_challenge.py
git commit -m "챌린지: KST 하루 판정·기간 검사 (판정 로직 신설)"
```

---

## Task 2: 판정 로직 — 중복키와 달성 집계

**Files:**
- Modify: `shopping_shorts/challenge.py`
- Modify: `shopping_shorts/tests/test_challenge.py`

- [ ] **Step 1: 실패하는 테스트를 쓴다**

`shopping_shorts/tests/test_challenge.py` **끝에 이어서** 추가한다:

```python
def test_video_code_per_platform():
    """★플랫폼마다 영상 코드를 뽑는다.

    app.py의 _media_code는 **인스타 전용**이고, 매칭 실패 시 빈 문자열이 아니라
    URL을 통째로 돌려준다(실측: 'https://youtu.be/xyz' → 'https://youtu.be/xyz').
    그걸 그대로 shortcode로 쓰면 dedup_key가 'sc:<URL>'이 되어 같은 영상의
    다른 URL 형태를 중복으로 못 잡는다. 그래서 여기서 직접 뽑는다.
    """
    ig = "https://www.instagram.com/reel/ABC123/?igsh=zz"
    assert challenge.video_code(ig, "instagram") == "ABC123"
    assert challenge.video_code("https://instagram.com/p/ABC123/", "instagram") == "ABC123"
    # 유튜브 — 짧은주소·shorts·watch 전부 같은 코드
    assert challenge.video_code("https://youtu.be/xyz789", "youtube") == "xyz789"
    assert challenge.video_code("https://www.youtube.com/shorts/xyz789", "youtube") == "xyz789"
    assert challenge.video_code("https://www.youtube.com/watch?v=xyz789", "youtube") == "xyz789"
    # 틱톡 — 긴 주소는 영상 id가 있다
    assert challenge.video_code("https://www.tiktok.com/@who/video/7412345678901234567",
                                "tiktok") == "7412345678901234567"
    # 틱톡 단축주소는 코드가 없다 → 빈 문자열(dedup_key가 URL 폴백으로 간다)
    assert challenge.video_code("https://vt.tiktok.com/ZSabc123/", "tiktok") == ""


def test_dedup_key_prefers_shortcode():
    """shortcode가 있으면 그것을 쓴다 — 같은 영상의 다른 URL 형태도 한 건으로 잡힌다."""
    a = challenge.dedup_key("https://www.instagram.com/reel/ABC123/", "ABC123")
    b = challenge.dedup_key("https://instagram.com/reel/ABC123/?igsh=xyz", "ABC123")
    assert a == b == "sc:ABC123"


def test_dedup_key_falls_back_to_normalized_url():
    """shortcode를 못 뽑으면(틱톡 단축링크 등) 정규화한 URL로."""
    a = challenge.dedup_key("https://VT.TikTok.com/ZSabc123/", "")
    b = challenge.dedup_key("https://vt.tiktok.com/ZSabc123?_r=1", "")
    assert a == b
    assert a.startswith("url:")


def test_dedup_key_different_videos_differ():
    assert challenge.dedup_key("https://vt.tiktok.com/AAA/", "") != \
           challenge.dedup_key("https://vt.tiktok.com/BBB/", "")


def test_summarize_counts_per_day():
    """제출 목록 → 날짜별 개수와 달성 여부."""
    subs = [
        {"submit_day": "2026-08-24"},
        {"submit_day": "2026-08-24"},
        {"submit_day": "2026-08-25"},
    ]
    got = challenge.summarize(subs, goal=2)
    assert got["by_day"] == {"2026-08-24": 2, "2026-08-25": 1}
    assert got["done_days"] == 1        # 8/24만 2개 달성
    assert got["total"] == 3


def test_summarize_empty():
    got = challenge.summarize([], goal=2)
    assert got["by_day"] == {}
    assert got["done_days"] == 0
    assert got["total"] == 0
```

- [ ] **Step 2: 실패를 확인한다**

Run: `py -m pytest shopping_shorts/tests/test_challenge.py -v`
Expected: FAIL — `AttributeError: module 'shopping_shorts.challenge' has no attribute 'dedup_key'`

- [ ] **Step 3: 최소 구현**

`shopping_shorts/challenge.py` **끝에 이어서** 추가한다:

```python
import urllib.parse


def dedup_key(url, shortcode=""):
    """같은 영상을 두 번 낸 것인지 판정하는 키. 저장 시점에 확정한다.

    ① shortcode가 있으면 그것으로 — 같은 영상의 다른 URL 형태(쿼리·모바일
       도메인)도 한 건으로 잡힌다.
    ② 없으면(틱톡 단축링크 vt.tiktok.com 등) 정규화한 URL로.

    ⚠️ ②는 완벽하지 않다 — 같은 영상을 단축링크와 원본링크로 각각 내면 둘 다
    통과한다. 틱톡 단건 수집을 붙이면 ①로 수렴한다. 지금은 알고 간다.
    """
    sc = (shortcode or "").strip()
    if sc:
        return "sc:" + sc
    p = urllib.parse.urlparse((url or "").strip())
    host = (p.hostname or "").lower()
    path = (p.path or "").rstrip("/").lower()
    return "url:" + host + path


def summarize(subs, goal=2):
    """제출 목록 → {by_day, done_days, total}.

    subs는 'submit_day' 키를 가진 dict 목록(DB 행 그대로 넣어도 된다).
    """
    by_day = {}
    for s in subs:
        d = s.get("submit_day")
        if not d:
            continue
        by_day[d] = by_day.get(d, 0) + 1
    return {
        "by_day": by_day,
        "done_days": sum(1 for n in by_day.values() if n >= goal),
        "total": sum(by_day.values()),
    }
```

`import urllib.parse`는 파일 맨 위 import 블록으로 옮겨도 된다(스타일 통일).

- [ ] **Step 4: 통과를 확인한다**

Run: `py -m pytest shopping_shorts/tests/test_challenge.py -v`
Expected: PASS — 9 passed

- [ ] **Step 5: 커밋**

```bash
git add shopping_shorts/challenge.py shopping_shorts/tests/test_challenge.py
git commit -m "챌린지: 중복 판정키·달성 집계"
```

---

## Task 3: DB 스키마와 CRUD

**Files:**
- Modify: `shopping_shorts/store.py` (테이블 생성부 + 메서드)
- Test: `shopping_shorts/tests/test_challenge_store.py`

- [ ] **Step 1: 실패하는 테스트를 쓴다**

`shopping_shorts/tests/test_challenge_store.py`를 새로 만든다:

```python
"""1기 챌린지 — 저장소."""
import pytest

from shopping_shorts.store import Store


@pytest.fixture()
def st(tmp_path):
    return Store(str(tmp_path / "t.db"))


def test_add_and_list_members(st):
    st.add_challenge_member(11, cohort="1기")
    st.add_challenge_member(22, cohort="1기")
    ms = st.list_challenge_members()
    assert [m["customer_id"] for m in ms] == [11, 22]
    assert ms[0]["cohort"] == "1기"
    assert ms[0]["active"] == 1


def test_add_member_twice_is_idempotent(st):
    st.add_challenge_member(11)
    st.add_challenge_member(11)
    assert len(st.list_challenge_members()) == 1


def test_is_challenge_member(st):
    st.add_challenge_member(11)
    assert st.is_challenge_member(11) is True
    assert st.is_challenge_member(99) is False


def test_deactivate_member_hides_from_active_list(st):
    st.add_challenge_member(11)
    st.set_challenge_member_active(11, False)
    assert st.is_challenge_member(11) is False
    assert st.list_challenge_members(active_only=False)[0]["active"] == 0
    assert st.list_challenge_members() == []


def test_add_submission_returns_id_and_lists(st):
    sid = st.add_challenge_submission(
        customer_id=11, url="https://youtu.be/abc", platform="youtube",
        shortcode="abc", dedup_key="sc:abc", submit_day="2026-08-24")
    assert sid > 0
    rows = st.list_challenge_submissions(customer_id=11)
    assert len(rows) == 1
    assert rows[0]["url"] == "https://youtu.be/abc"
    assert rows[0]["submit_day"] == "2026-08-24"
    assert rows[0]["fetch_status"] == "pending"


def test_duplicate_dedup_key_rejected_for_same_member(st):
    st.add_challenge_submission(11, "https://youtu.be/abc", "youtube",
                                "abc", "sc:abc", "2026-08-24")
    dup = st.add_challenge_submission(11, "https://youtube.com/shorts/abc", "youtube",
                                      "abc", "sc:abc", "2026-08-25")
    assert dup == 0          # 0 = 중복이라 저장 안 함
    assert len(st.list_challenge_submissions(customer_id=11)) == 1


def test_same_video_allowed_for_different_members(st):
    """다른 사람이 같은 영상을 낸 것은 별개다(중복 판정은 사람 단위)."""
    assert st.add_challenge_submission(11, "https://youtu.be/abc", "youtube",
                                       "abc", "sc:abc", "2026-08-24") > 0
    assert st.add_challenge_submission(22, "https://youtu.be/abc", "youtube",
                                       "abc", "sc:abc", "2026-08-24") > 0


def test_update_submission_meta_marks_ok(st):
    sid = st.add_challenge_submission(11, "https://youtu.be/abc", "youtube",
                                      "abc", "sc:abc", "2026-08-24")
    st.update_challenge_submission_meta(
        sid, title="제목", thumb="https://x/t.jpg", channel="채널",
        views=1234, likes=10, comments=3, fetch_status="ok")
    row = st.list_challenge_submissions(customer_id=11)[0]
    assert row["title"] == "제목"
    assert row["views"] == 1234
    assert row["fetch_status"] == "ok"


def test_failed_fetch_keeps_the_row(st):
    """수집이 실패해도 제출 자체는 살아 있어야 한다 — 카운트가 새면 안 된다."""
    sid = st.add_challenge_submission(11, "https://vt.tiktok.com/x", "tiktok",
                                      "", "url:vt.tiktok.com/x", "2026-08-24")
    st.update_challenge_submission_meta(sid, fetch_status="failed")
    rows = st.list_challenge_submissions(customer_id=11)
    assert len(rows) == 1
    assert rows[0]["fetch_status"] == "failed"


def test_list_all_submissions_across_members(st):
    st.add_challenge_submission(11, "https://youtu.be/a", "youtube", "a", "sc:a", "2026-08-24")
    st.add_challenge_submission(22, "https://youtu.be/b", "youtube", "b", "sc:b", "2026-08-24")
    assert len(st.list_challenge_submissions()) == 2
```

- [ ] **Step 2: 실패를 확인한다**

Run: `py -m pytest shopping_shorts/tests/test_challenge_store.py -v`
Expected: FAIL — `AttributeError: 'Store' object has no attribute 'add_challenge_member'`

- [ ] **Step 3: 테이블을 만든다**

`shopping_shorts/store.py`에서 `CREATE TABLE IF NOT EXISTS deposit_claim` 블록을 찾는다(대략 315행). 그 블록이 끝나는 `""")` 바로 **다음 줄에** 아래를 넣는다 — 같은 `c.execute("""...""")` 형태를 유지한다:

```python
            # 1기 챌린지(2026-08-24) — 하루 2영상 업로드 챌린지 참가자·제출물.
            c.execute("""
                CREATE TABLE IF NOT EXISTS challenge_member (
                    id INTEGER PRIMARY KEY AUTOINCREMENT,
                    customer_id INTEGER NOT NULL UNIQUE,
                    cohort TEXT NOT NULL DEFAULT '1기',
                    active INTEGER NOT NULL DEFAULT 1,
                    created_at TEXT NOT NULL
                )
            """)
            c.execute("""
                CREATE TABLE IF NOT EXISTS challenge_submission (
                    id INTEGER PRIMARY KEY AUTOINCREMENT,
                    customer_id INTEGER NOT NULL,
                    url TEXT NOT NULL,
                    platform TEXT NOT NULL,
                    shortcode TEXT,
                    dedup_key TEXT NOT NULL,
                    submit_day TEXT NOT NULL,
                    submitted_at TEXT NOT NULL,
                    title TEXT, thumb TEXT, channel TEXT,
                    views INTEGER, likes INTEGER, comments INTEGER,
                    fetch_status TEXT NOT NULL DEFAULT 'pending'
                )
            """)
            c.execute("CREATE UNIQUE INDEX IF NOT EXISTS idx_chal_sub_dedup "
                      "ON challenge_submission(customer_id, dedup_key)")
            c.execute("CREATE INDEX IF NOT EXISTS idx_chal_sub_day "
                      "ON challenge_submission(submit_day)")
            c.execute("CREATE INDEX IF NOT EXISTS idx_chal_sub_cust "
                      "ON challenge_submission(customer_id, submit_day)")
```

- [ ] **Step 4: CRUD 메서드를 만든다**

`shopping_shorts/store.py`에서 `def list_prereg(self):` 메서드를 찾는다(대략 4991행). 그 메서드가 끝난 **다음 줄에** 아래를 넣는다(들여쓰기는 클래스 메서드 = 공백 4칸):

```python
    # ── 1기 챌린지 (2026-08-24) ────────────────────────────────────
    def add_challenge_member(self, customer_id, cohort="1기"):
        """참가자 등록 → id. 이미 있으면 다시 활성화만 하고 끝(idempotent)."""
        now = datetime.now(timezone.utc).isoformat(timespec="seconds")
        with self._conn() as c:
            cur = c.execute(
                "INSERT INTO challenge_member(customer_id, cohort, active, created_at) "
                "VALUES(?,?,1,?) ON CONFLICT(customer_id) DO UPDATE SET active=1",
                (int(customer_id), cohort, now))
            return cur.lastrowid

    def set_challenge_member_active(self, customer_id, active):
        """참가 해제/재개. 행을 지우지 않는다 — 제출 이력은 남는다."""
        with self._conn() as c:
            c.execute("UPDATE challenge_member SET active=? WHERE customer_id=?",
                      (1 if active else 0, int(customer_id)))

    def is_challenge_member(self, customer_id):
        with self._conn() as c:
            r = c.execute("SELECT 1 FROM challenge_member "
                          "WHERE customer_id=? AND active=1",
                          (int(customer_id),)).fetchone()
        return r is not None

    def list_challenge_members(self, active_only=True):
        q = ("SELECT customer_id, cohort, active, created_at FROM challenge_member "
             + ("WHERE active=1 " if active_only else "") + "ORDER BY id")
        with self._conn() as c:
            rows = c.execute(q).fetchall()
        return [{"customer_id": r[0], "cohort": r[1], "active": r[2],
                 "created_at": r[3]} for r in rows]

    def add_challenge_submission(self, customer_id, url, platform, shortcode,
                                 dedup_key, submit_day):
        """제출 저장 → id. 같은 사람이 같은 영상을 또 내면 0을 돌려준다.

        ★수집(썸네일·조회수)은 여기서 하지 않는다. 링크가 들어온 순간 행이
        생기고 달성 카운트가 확정된다 — 수집 실패가 카운트를 갉아먹으면
        '올렸는데 미달성' 사고가 난다.
        """
        now = datetime.now(timezone.utc).isoformat(timespec="seconds")
        with self._conn() as c:
            cur = c.execute(
                "INSERT OR IGNORE INTO challenge_submission"
                "(customer_id, url, platform, shortcode, dedup_key, submit_day,"
                " submitted_at, fetch_status) VALUES(?,?,?,?,?,?,?,'pending')",
                (int(customer_id), url, platform, shortcode or "",
                 dedup_key, submit_day, now))
            return cur.lastrowid if cur.rowcount else 0

    def update_challenge_submission_meta(self, sub_id, *, title=None, thumb=None,
                                         channel=None, views=None, likes=None,
                                         comments=None, fetch_status=None):
        """수집 결과를 나중에 채운다. None인 것은 건드리지 않는다."""
        sets, args = [], []
        for col, val in (("title", title), ("thumb", thumb), ("channel", channel),
                         ("views", views), ("likes", likes), ("comments", comments),
                         ("fetch_status", fetch_status)):
            if val is not None:
                sets.append(f"{col}=?")
                args.append(val)
        if not sets:
            return
        args.append(int(sub_id))
        with self._conn() as c:
            c.execute(f"UPDATE challenge_submission SET {', '.join(sets)} WHERE id=?",
                      args)

    def list_challenge_submissions(self, customer_id=None, start=None, end=None):
        """제출 목록 — 최신순. customer_id를 주면 그 사람 것만."""
        q = ("SELECT id, customer_id, url, platform, shortcode, submit_day,"
             " submitted_at, title, thumb, channel, views, likes, comments,"
             " fetch_status FROM challenge_submission")
        where, args = [], []
        if customer_id is not None:
            where.append("customer_id=?")
            args.append(int(customer_id))
        if start:
            where.append("submit_day>=?")
            args.append(start)
        if end:
            where.append("submit_day<=?")
            args.append(end)
        if where:
            q += " WHERE " + " AND ".join(where)
        q += " ORDER BY id DESC"
        with self._conn() as c:
            rows = c.execute(q, args).fetchall()
        return [{"id": r[0], "customer_id": r[1], "url": r[2], "platform": r[3],
                 "shortcode": r[4], "submit_day": r[5], "submitted_at": r[6],
                 "title": r[7], "thumb": r[8], "channel": r[9], "views": r[10],
                 "likes": r[11], "comments": r[12], "fetch_status": r[13]}
                for r in rows]
```

- [ ] **Step 5: 통과를 확인한다**

Run: `py -m pytest shopping_shorts/tests/test_challenge_store.py -v`
Expected: PASS — 10 passed

- [ ] **Step 6: 기존 테스트가 안 깨졌는지 본다**

Run: `py -m pytest shopping_shorts/tests/test_app.py -q 2>&1 | tail -5`
Expected: 통과 개수가 작업 전과 같다. 스키마만 추가했으므로 영향이 없어야 한다.

- [ ] **Step 7: 커밋**

```bash
git add shopping_shorts/store.py shopping_shorts/tests/test_challenge_store.py
git commit -m "챌린지: 테이블 2개(참가자·제출물) + CRUD"
```

---

## Task 4: 제출 API — 저장까지

**Files:**
- Modify: `shopping_shorts/app.py`
- Test: `shopping_shorts/tests/test_challenge_api.py`

수집은 다음 Task에서 붙인다. 여기서는 **저장이 확실히 되는 것**만 만든다.

- [ ] **Step 1: 실패하는 테스트를 쓴다**

`shopping_shorts/tests/test_challenge_api.py`를 새로 만든다:

```python
"""1기 챌린지 — 제출 API·권한."""
from datetime import datetime, timezone, timedelta

import pytest
from fastapi.testclient import TestClient

from shopping_shorts import app as appmod
from shopping_shorts.store import Store


def _cookie(cid):
    exp = int(datetime.now(timezone.utc).timestamp()) + 3600
    return appmod._sign_session(cid, exp)


@pytest.fixture()
def env(tmp_path, monkeypatch):
    monkeypatch.setattr(appmod, "DB_PATH", str(tmp_path / "t.db"))
    monkeypatch.setattr(appmod, "_AUTH_ON", True)
    monkeypatch.setattr(appmod, "DASH_SECRET", "test-secret-xyz")
    st = Store(str(tmp_path / "t.db"))
    st.ensure_paywall_schema()
    # 수집은 Task5에서 붙인다 — 여기서는 아무 일도 안 하게 막아둔다.
    monkeypatch.setattr(appmod, "_challenge_fetch_async", lambda *a, **k: None,
                        raising=False)
    return st


def _client():
    return TestClient(appmod.app)


def test_submit_requires_membership(env):
    """참가자가 아니면 403 — 아무 회원이나 제출하면 명단이 더러워진다."""
    c = _client()
    r = c.post("/api/challenge/submit", params={"url": "https://youtu.be/abc"},
               cookies={"dash_auth": _cookie(77)})
    assert r.status_code == 403


def test_submit_saves_and_counts(env):
    env.add_challenge_member(77)
    c = _client()
    r = c.post("/api/challenge/submit", params={"url": "https://youtu.be/abc"},
               cookies={"dash_auth": _cookie(77)})
    assert r.status_code == 200, r.text
    body = r.json()
    assert body["ok"] is True
    assert body["today"] == 1
    rows = env.list_challenge_submissions(customer_id=77)
    assert len(rows) == 1
    assert rows[0]["platform"] == "youtube"


def test_submit_duplicate_rejected(env):
    env.add_challenge_member(77)
    c = _client()
    p = {"url": "https://youtu.be/abc"}
    ck = {"dash_auth": _cookie(77)}
    assert c.post("/api/challenge/submit", params=p, cookies=ck).status_code == 200
    r2 = c.post("/api/challenge/submit", params=p, cookies=ck)
    assert r2.status_code == 422
    assert "이미" in r2.json()["error"]
    assert len(env.list_challenge_submissions(customer_id=77)) == 1


def test_submit_rejects_unsupported_url(env):
    env.add_challenge_member(77)
    c = _client()
    r = c.post("/api/challenge/submit", params={"url": "https://example.com/x"},
               cookies={"dash_auth": _cookie(77)})
    assert r.status_code == 422
    assert len(env.list_challenge_submissions(customer_id=77)) == 0


def test_submit_outside_period_rejected(env):
    """기간 밖이면 422. ★고정 날짜를 쓰지 않는다 — 상대 날짜로 만든다."""
    env.add_challenge_member(77)
    yesterday = (datetime.now(timezone.utc) + timedelta(days=-40)).strftime("%Y-%m-%d")
    long_ago = (datetime.now(timezone.utc) + timedelta(days=-70)).strftime("%Y-%m-%d")
    env.set_setting("challenge_start", long_ago)
    env.set_setting("challenge_end", yesterday)      # 이미 끝난 챌린지
    c = _client()
    r = c.post("/api/challenge/submit", params={"url": "https://youtu.be/abc"},
               cookies={"dash_auth": _cookie(77)})
    assert r.status_code == 422
    assert "기간" in r.json()["error"]


def test_submit_inside_period_ok(env):
    env.add_challenge_member(77)
    start = (datetime.now(timezone.utc) + timedelta(days=-1)).strftime("%Y-%m-%d")
    end = (datetime.now(timezone.utc) + timedelta(days=30)).strftime("%Y-%m-%d")
    env.set_setting("challenge_start", start)
    env.set_setting("challenge_end", end)
    c = _client()
    r = c.post("/api/challenge/submit", params={"url": "https://youtu.be/abc"},
               cookies={"dash_auth": _cookie(77)})
    assert r.status_code == 200, r.text


def test_mine_returns_today_count_and_list(env):
    env.add_challenge_member(77)
    c = _client()
    ck = {"dash_auth": _cookie(77)}
    c.post("/api/challenge/submit", params={"url": "https://youtu.be/a"}, cookies=ck)
    c.post("/api/challenge/submit", params={"url": "https://youtu.be/b"}, cookies=ck)
    r = c.get("/api/challenge/mine", cookies=ck)
    assert r.status_code == 200
    d = r.json()
    assert d["today"] == 2
    assert d["goal"] == 2
    assert len(d["items"]) == 2
    assert d["is_member"] is True


def test_mine_for_non_member_says_so(env):
    """참가자가 아니어도 200으로 안내한다 — 화면이 깨지면 안 된다."""
    r = _client().get("/api/challenge/mine", cookies={"dash_auth": _cookie(77)})
    assert r.status_code == 200
    assert r.json()["is_member"] is False
```

`set_setting`은 `store.py:4419`, `get_setting`은 `store.py:4413`에 있다(실측).

- [ ] **Step 2: 실패를 확인한다**

Run: `py -m pytest shopping_shorts/tests/test_challenge_api.py -v`
Expected: FAIL — 404(라우트 없음) 또는 `AttributeError`

- [ ] **Step 3: 라우트를 만든다**

`shopping_shorts/app.py`에서 `@app.get("/api/reference/adopt"` 라인을 찾는다(대략 9968행). 그 **바로 앞에** 아래를 넣는다:

```python
# ── 1기 챌린지 (2026-08-24) ──────────────────────────────────────────
# 하루 2영상 챌린지. 판정 로직은 shopping_shorts/challenge.py에 모아 두었다
# (app.py가 13,000줄을 넘어 더 얹지 않는다). 여기 라우트는 얇게 유지한다.
_CHALLENGE_PLATFORMS = ("instagram", "youtube", "tiktok")


def _challenge_period(store):
    """설정된 기간·목표. 없으면 열린 기간·목표 2."""
    try:
        goal = int(store.get_setting("challenge_daily_goal", 2) or 2)
    except (TypeError, ValueError):
        goal = 2
    return (store.get_setting("challenge_start", "") or "",
            store.get_setting("challenge_end", "") or "",
            goal)


@app.post("/api/challenge/submit")
def api_challenge_submit(request: Request, url: str = ""):
    """멤버가 챌린지 영상 링크를 낸다.

    ★저장 먼저, 수집 나중. 링크가 들어온 순간 행이 생기고 달성 카운트가
    확정된다 — 인스타 수집은 수십 초가 걸리고 간헐적으로 실패하는데, 거기에
    카운트를 묶으면 '올렸는데 미달성' 사고가 난다.
    """
    cid = _cid(request)
    store = Store(DB_PATH)
    if not store.is_challenge_member(cid):
        return JSONResponse(status_code=403,
                            content={"ok": False, "error": "1기 챌린지 참가자만 제출할 수 있어요"})
    u = (url or "").strip()
    platform = _grab_platform(u)
    if platform not in _CHALLENGE_PLATFORMS:
        return JSONResponse(status_code=422, content={
            "ok": False,
            "error": "인스타그램·유튜브·틱톡 영상 주소를 넣어주세요"})
    start, end, goal = _challenge_period(store)
    day = challenge.kst_day()
    if not challenge.in_period(day, start, end):
        return JSONResponse(status_code=422,
                            content={"ok": False, "error": "지금은 챌린지 기간이 아니에요"})
    code = challenge.video_code(u, platform)
    key = challenge.dedup_key(u, code)
    sub_id = store.add_challenge_submission(cid, u, platform, code, key, day)
    if not sub_id:
        return JSONResponse(status_code=422,
                            content={"ok": False, "error": "이미 제출한 영상이에요"})
    _challenge_fetch_async(sub_id, u, platform)
    today = sum(1 for s in store.list_challenge_submissions(customer_id=cid)
                if s["submit_day"] == day)
    return {"ok": True, "id": sub_id, "today": today, "goal": goal, "day": day}


@app.get("/api/challenge/mine")
def api_challenge_mine(request: Request):
    """내 제출 목록 + 오늘 카운트. 참가자가 아니어도 200으로 안내한다."""
    cid = _cid(request)
    store = Store(DB_PATH)
    start, end, goal = _challenge_period(store)
    if not store.is_challenge_member(cid):
        return {"ok": True, "is_member": False, "items": [], "today": 0,
                "goal": goal, "start": start, "end": end}
    items = store.list_challenge_submissions(customer_id=cid)
    day = challenge.kst_day()
    summary = challenge.summarize(items, goal=goal)
    return {"ok": True, "is_member": True, "items": items,
            "today": summary["by_day"].get(day, 0), "goal": goal,
            "done_days": summary["done_days"], "total": summary["total"],
            "start": start, "end": end, "day": day}
```

`app.py` 상단 import 블록(대략 45행 `from shopping_shorts.apify_client import ...` 근처)에 아래를 추가한다:

```python
from shopping_shorts import challenge
```

★**`app.py:212`의 `_media_code`를 쓰지 마라.** 실측 결과 그것은 인스타 전용이고,
매칭 실패 시 빈 문자열이 아니라 **URL을 통째로** 돌려준다:

```
'https://www.instagram.com/reel/ABC123/'  ->  'ABC123'                    ✅
'https://youtu.be/xyz789'                 ->  'https://youtu.be/xyz789'   ❌
'https://vt.tiktok.com/ZSabc/'            ->  'https://vt.tiktok.com/ZSabc/'  ❌
```

그 값을 shortcode로 쓰면 dedup_key가 `sc:<URL>`이 되어 같은 영상의 다른 URL
형태를 중복으로 못 잡는다. Task 2에서 만든 `challenge.video_code(url, platform)`을
쓴다 — 플랫폼별로 뽑고, 못 뽑으면 빈 문자열을 준다(그때 dedup_key가 URL 폴백).

- [ ] **Step 4: 수집 함수의 자리만 만든다**

아직 수집은 하지 않는다. Task 5에서 채운다. 위 라우트 블록 **바로 앞에** 넣는다:

```python
def _challenge_fetch_async(sub_id, url, platform):
    """제출 영상의 썸네일·조회수를 백그라운드로 채운다(Task5에서 구현).

    지금은 아무 것도 하지 않는다 — fetch_status는 'pending'으로 남는다.
    """
    return None
```

- [ ] **Step 5: 통과를 확인한다**

Run: `py -m pytest shopping_shorts/tests/test_challenge_api.py -v`
Expected: PASS — 8 passed

- [ ] **Step 6: ★유료게이트 — 반드시 화이트리스트에 넣어야 한다**

**실측 결과(2026-08-24, Task 4 구현 중 발견): "1기 멤버는 pro라 통과한다"는 틀렸다.**

`challenge_member` 등록과 결제등급(`access_level`)은 **완전히 별개 테이블**이다.
챌린지 참가자로 등록해도 결제 이력이 없으면 `access_level`이 `ranking_only`가 되고,
미들웨어(`app.py:8047`)가 `_ranking_only_blocked`로 **402**를 먼저 뱉는다
(deny-by-default라 새 경로는 자동 차단).

→ `app.py:6543` `_FREE_EXACT_ANY`에 두 경로를 넣는다:

```python
                   # ★1기 챌린지(2026-08-24): 참가자격은 challenge_member 여부로 핸들러 안에서
                   #   따로 막는다(403). 결제등급(ranking_only)과는 무관해야 한다 — 챌린지
                   #   멤버가 전부 pro는 아니다. 여기 안 넣으면 등급 402가 먼저 막아버린다.
                   "/api/challenge/submit", "/api/challenge/mine"}
```

**보안 경계는 유지된다**: 비참가자는 핸들러 안 `is_challenge_member`가 403으로 막는다
(실측 확인). 결제등급 게이트만 비켜갈 뿐, 아무나 쓸 수 있게 되는 것이 아니다.

Run: `py -m pytest shopping_shorts/tests/test_paywall_gate.py -q 2>&1 | tail -5`
Expected: 17 passed (기준선 그대로)

- [ ] **Step 7: 커밋**

```bash
git add shopping_shorts/app.py shopping_shorts/tests/test_challenge_api.py
git commit -m "챌린지: 제출·내역 API (저장 먼저, 수집은 다음)"
```

---

## Task 5: 백그라운드 수집 붙이기

**Files:**
- Modify: `shopping_shorts/app.py` (`_challenge_fetch_async`)
- Modify: `shopping_shorts/tests/test_challenge_api.py`

- [ ] **Step 1: 실패하는 테스트를 쓴다**

`shopping_shorts/tests/test_challenge_api.py` **끝에 이어서** 추가한다:

```python
def test_fetch_fills_meta_on_success(env, monkeypatch):
    """수집이 되면 썸네일·조회수가 채워지고 ok가 된다."""
    env.add_challenge_member(77)
    monkeypatch.setattr(appmod, "probe_grab_meta",
                        lambda u: {"title": "제목", "thumbnail": "https://x/t.jpg",
                                   "channel": "채널", "view_count": 1234,
                                   "like_count": 10, "comment_count": 3},
                        raising=False)
    sid = env.add_challenge_submission(77, "https://youtu.be/abc", "youtube",
                                       "abc", "sc:abc", "2026-08-24")
    appmod._challenge_fetch(sid, "https://youtu.be/abc", "youtube")
    row = env.list_challenge_submissions(customer_id=77)[0]
    assert row["fetch_status"] == "ok"
    assert row["title"] == "제목"
    assert row["views"] == 1234


def test_fetch_failure_keeps_submission(env, monkeypatch):
    """★수집이 터져도 제출은 남고 카운트는 유지된다 — 이게 가장 중요하다."""
    env.add_challenge_member(77)

    def _boom(u):
        raise RuntimeError("수집 실패")

    monkeypatch.setattr(appmod, "probe_grab_meta", _boom, raising=False)
    sid = env.add_challenge_submission(77, "https://youtu.be/abc", "youtube",
                                       "abc", "sc:abc", "2026-08-24")
    appmod._challenge_fetch(sid, "https://youtu.be/abc", "youtube")   # 예외가 새면 안 된다
    rows = env.list_challenge_submissions(customer_id=77)
    assert len(rows) == 1
    assert rows[0]["fetch_status"] == "failed"


def test_tiktok_is_skipped_not_failed(env):
    """틱톡은 단건 조회 함수가 없다 — 시도하지 않고 skipped로 둔다."""
    env.add_challenge_member(77)
    sid = env.add_challenge_submission(77, "https://vt.tiktok.com/x", "tiktok",
                                       "", "url:vt.tiktok.com/x", "2026-08-24")
    appmod._challenge_fetch(sid, "https://vt.tiktok.com/x", "tiktok")
    assert env.list_challenge_submissions(customer_id=77)[0]["fetch_status"] == "skipped"
```

- [ ] **Step 2: 실패를 확인한다**

Run: `py -m pytest shopping_shorts/tests/test_challenge_api.py -k fetch -v`
Expected: FAIL — `AttributeError: module has no attribute '_challenge_fetch'`

- [ ] **Step 3: 구현한다**

`shopping_shorts/app.py`의 `_challenge_fetch_async` 스텁을 아래로 **교체**한다:

```python
def _challenge_fetch(sub_id, url, platform):
    """제출 영상의 썸네일·조회수를 채운다(동기). 테스트가 직접 부른다.

    ★실패해도 예외를 밖으로 내보내지 않는다 — 제출 행은 이미 저장돼 있고,
    수집은 장식이다. 다만 조용히 삼키지 말고 fetch_status에 반드시 남긴다
    (except: pass가 SQL 오류를 삼켜 라이브에서 0건이 된 전례가 있다).
    """
    import sys as _sys
    store = Store(DB_PATH)
    if platform == "tiktok":
        # 틱톡은 단건 조회 함수가 없다(fetch_account_videos는 계정 단위).
        # 시도조차 하지 않으므로 'failed'가 아니라 'skipped'다.
        store.update_challenge_submission_meta(sub_id, fetch_status="skipped")
        return
    try:
        meta = probe_grab_meta(url) or {}
        # ★키 이름 주의(실측 media_download.py:141-145): probe_grab_meta는 yt-dlp의
        #   view_count·like_count·comment_count·uploader를 **이미 바꿔서** 준다
        #   → views·likes·comments·channel. view_count로 읽으면 조용히 항상 None이다.
        store.update_challenge_submission_meta(
            sub_id,
            title=meta.get("title") or None,
            thumb=meta.get("thumbnail") or None,
            channel=meta.get("channel") or None,
            views=meta.get("views"),
            likes=meta.get("likes"),
            comments=meta.get("comments"),
            fetch_status="ok")
    except Exception as e:  # noqa: BLE001 — 수집 실패가 제출을 무효로 만들지 않는다
        print(f"[challenge] 수집 실패 sub_id={sub_id} url={url}: {e!r}", file=_sys.stderr)
        store.update_challenge_submission_meta(sub_id, fetch_status="failed")


```

★**raw 스레드를 쓰지 마라.** 이 코드베이스는 요청당 후속작업에 FastAPI의
`BackgroundTasks`를 쓴다(실측 15곳). 특히 `app.py:8966 _enrich_grab`이
**똑같은 일**(yt-dlp로 썸네일·조회수 보강)을 그 방식으로 한다 — 같은 일을
다르게 짜지 마라(0순위-B).

그래서 `_challenge_fetch_async` 스텁은 **지우고**, 라우트가 `BackgroundTasks`를
직접 받는다. `api_challenge_submit`의 시그니처와 호출부를 이렇게 바꾼다:

```python
@app.post("/api/challenge/submit")
def api_challenge_submit(request: Request, background_tasks: BackgroundTasks,
                         url: str = ""):
```

그리고 `_challenge_fetch_async(sub_id, u, platform)` 자리를:

```python
    background_tasks.add_task(_challenge_fetch, sub_id, u, platform)
```

`BackgroundTasks`는 `app.py` 상단에 이미 import돼 있다(실측 — `api_collect` 등이 쓴다).

⚠️ Task 4의 테스트 픽스처는 `_challenge_fetch_async`를 monkeypatch로 막아
두었다. 그 줄을 **`_challenge_fetch`를 막는 것으로 바꿔라**:

```python
    monkeypatch.setattr(appmod, "_challenge_fetch", lambda *a, **k: None,
                        raising=False)
```

안 바꾸면 Task 4 테스트가 진짜 yt-dlp를 호출해 느려지고 네트워크에 의존한다.

`probe_grab_meta`가 import돼 있는지 확인한다:

Run: `grep -n "probe_grab_meta" shopping_shorts/app.py | head -3`
`adopt`가 이미 쓰므로 있어야 한다. 없으면 import를 추가한다.

- [ ] **Step 4: 통과를 확인한다**

Run: `py -m pytest shopping_shorts/tests/test_challenge_api.py -v`
Expected: PASS — 11 passed

- [ ] **Step 5: 커밋**

```bash
git add shopping_shorts/app.py shopping_shorts/tests/test_challenge_api.py
git commit -m "챌린지: 백그라운드 수집(실패해도 제출·카운트 유지)"
```

---

## Task 6: 관리 API — 달성현황·영상·참가자

**Files:**
- Modify: `shopping_shorts/app.py`
- Modify: `shopping_shorts/tests/test_challenge_api.py`

- [ ] **Step 1: 실패하는 테스트를 쓴다**

`shopping_shorts/tests/test_challenge_api.py` **끝에 이어서** 추가한다:

```python
def _admin_env(env, monkeypatch):
    """cid 5를 관리자로 만든다."""
    monkeypatch.setattr(appmod, "_is_admin", lambda cid: cid == 5)
    return env


def test_board_requires_admin(env, monkeypatch):
    _admin_env(env, monkeypatch)
    r = _client().get("/api/challenge/board", cookies={"dash_auth": _cookie(77)})
    assert r.status_code == 403


def test_board_shows_each_member_per_day(env, monkeypatch):
    _admin_env(env, monkeypatch)
    env.add_challenge_member(11)
    env.add_challenge_member(22)
    env.add_challenge_submission(11, "https://youtu.be/a", "youtube", "a", "sc:a", "2026-08-24")
    env.add_challenge_submission(11, "https://youtu.be/b", "youtube", "b", "sc:b", "2026-08-24")
    env.add_challenge_submission(22, "https://youtu.be/c", "youtube", "c", "sc:c", "2026-08-24")
    r = _client().get("/api/challenge/board", cookies={"dash_auth": _cookie(5)})
    assert r.status_code == 200
    d = r.json()
    rows = {m["customer_id"]: m for m in d["members"]}
    assert rows[11]["by_day"]["2026-08-24"] == 2
    assert rows[11]["done_days"] == 1        # 목표 2 달성
    assert rows[22]["by_day"]["2026-08-24"] == 1
    assert rows[22]["done_days"] == 0        # 1개는 미달성
    assert d["goal"] == 2


def test_videos_requires_admin(env, monkeypatch):
    _admin_env(env, monkeypatch)
    r = _client().get("/api/challenge/videos", cookies={"dash_auth": _cookie(77)})
    assert r.status_code == 403


def test_videos_sorted_by_views(env, monkeypatch):
    """★조회수순 정렬.

    함정(2026-08-24 뮤테이션 테스트로 발견): 먼저 넣은 s1에 낮은 조회수를 주면
    `list_challenge_submissions`의 기본 정렬(ORDER BY id DESC)만으로도 우연히
    [999, 10]이 나와 **정렬 코드를 지워도 통과한다**(거짓 green).
    → id순과 조회수순이 **반대**가 되게 값을 준다.
    """
    _admin_env(env, monkeypatch)
    env.add_challenge_member(11)
    s1 = env.add_challenge_submission(11, "https://youtu.be/a", "youtube", "a", "sc:a", "2026-08-24")
    s2 = env.add_challenge_submission(11, "https://youtu.be/b", "youtube", "b", "sc:b", "2026-08-24")
    env.update_challenge_submission_meta(s1, views=999, fetch_status="ok")
    env.update_challenge_submission_meta(s2, views=10, fetch_status="ok")
    r = _client().get("/api/challenge/videos", params={"sort": "views"},
                      cookies={"dash_auth": _cookie(5)})
    assert [i["views"] for i in r.json()["items"]] == [999, 10]


def test_videos_filter_by_member(env, monkeypatch):
    _admin_env(env, monkeypatch)
    env.add_challenge_member(11)
    env.add_challenge_member(22)
    env.add_challenge_submission(11, "https://youtu.be/a", "youtube", "a", "sc:a", "2026-08-24")
    env.add_challenge_submission(22, "https://youtu.be/b", "youtube", "b", "sc:b", "2026-08-24")
    r = _client().get("/api/challenge/videos", params={"member": 11},
                      cookies={"dash_auth": _cookie(5)})
    items = r.json()["items"]
    assert len(items) == 1 and items[0]["customer_id"] == 11


def test_add_member_requires_admin(env, monkeypatch):
    _admin_env(env, monkeypatch)
    r = _client().post("/api/challenge/member", params={"customer_id": 11},
                       cookies={"dash_auth": _cookie(77)})
    assert r.status_code == 403
    assert env.list_challenge_members() == []


def test_admin_can_add_and_remove_member(env, monkeypatch):
    _admin_env(env, monkeypatch)
    ck = {"dash_auth": _cookie(5)}
    c = _client()
    assert c.post("/api/challenge/member", params={"customer_id": 11},
                  cookies=ck).status_code == 200
    assert env.is_challenge_member(11) is True
    assert c.post("/api/challenge/member",
                  params={"customer_id": 11, "active": 0},
                  cookies=ck).status_code == 200
    assert env.is_challenge_member(11) is False
```

- [ ] **Step 2: 실패를 확인한다**

Run: `py -m pytest shopping_shorts/tests/test_challenge_api.py -k "board or videos or member" -v`
Expected: FAIL — 404

- [ ] **Step 3: 구현한다**

`shopping_shorts/app.py`의 `api_challenge_mine` 함수 **바로 뒤에** 추가한다:

```python
@app.get("/api/challenge/board")
def api_challenge_board(request: Request):
    """달성현황 — 멤버 × 날짜 그리드용 데이터(관리자 전용)."""
    denied = _require_admin(request)
    if denied:
        return denied
    store = Store(DB_PATH)
    start, end, goal = _challenge_period(store)
    subs = store.list_challenge_submissions(start=start or None, end=end or None)
    by_cust = {}
    for s in subs:
        by_cust.setdefault(s["customer_id"], []).append(s)
    members = []
    for m in store.list_challenge_members():
        cid = m["customer_id"]
        summary = challenge.summarize(by_cust.get(cid, []), goal=goal)
        cust = store.get_customer(cid) or {}
        members.append({
            "customer_id": cid,
            "name": cust.get("name") or cust.get("username") or f"#{cid}",
            "email": cust.get("email") or "",
            "by_day": summary["by_day"],
            "done_days": summary["done_days"],
            "total": summary["total"],
        })
    return {"ok": True, "members": members, "goal": goal,
            "start": start, "end": end, "today": challenge.kst_day()}


@app.get("/api/challenge/videos")
def api_challenge_videos(request: Request, sort: str = "recent",
                         member: int = 0, platform: str = ""):
    """제출 영상 카드 목록(관리자 전용). sort=recent|views"""
    denied = _require_admin(request)
    if denied:
        return denied
    store = Store(DB_PATH)
    items = store.list_challenge_submissions(customer_id=member or None)
    if platform:
        items = [i for i in items if i["platform"] == platform]
    if sort == "views":
        items.sort(key=lambda i: i.get("views") or 0, reverse=True)
    names = {}
    for i in items:
        cid = i["customer_id"]
        if cid not in names:
            cust = store.get_customer(cid) or {}
            names[cid] = cust.get("name") or cust.get("username") or f"#{cid}"
        i["member_name"] = names[cid]
    return {"ok": True, "items": items}


@app.get("/api/challenge/members")
def api_challenge_members(request: Request):
    """참가자 목록(관리자 전용)."""
    denied = _require_admin(request)
    if denied:
        return denied
    store = Store(DB_PATH)
    out = []
    for m in store.list_challenge_members(active_only=False):
        cust = store.get_customer(m["customer_id"]) or {}
        out.append({**m,
                    "name": cust.get("name") or cust.get("username") or f"#{m['customer_id']}",
                    "email": cust.get("email") or ""})
    return {"ok": True, "members": out}


@app.post("/api/challenge/member")
def api_challenge_member_set(request: Request, customer_id: int, active: int = 1):
    """참가자 등록/해제(관리자 전용). active=0이면 해제(이력은 남는다)."""
    denied = _require_admin(request)
    if denied:
        return denied
    store = Store(DB_PATH)
    if active:
        store.add_challenge_member(int(customer_id))
    else:
        store.set_challenge_member_active(int(customer_id), False)
    return {"ok": True, "customer_id": int(customer_id), "active": bool(active)}
```

`get_customer`가 있는지 확인한다:

Run: `grep -n "def get_customer" shopping_shorts/store.py`
`app.py:8291`이 이미 쓰고 있으므로 있어야 한다. 반환 dict의 키(`name`·`username`·`email`)도 확인하고, 다르면 위 코드를 실제 키에 맞춘다.

- [ ] **Step 4: 통과를 확인한다**

Run: `py -m pytest shopping_shorts/tests/test_challenge_api.py -v`
Expected: PASS — 18 passed

- [ ] **Step 5: 커밋**

```bash
git add shopping_shorts/app.py shopping_shorts/tests/test_challenge_api.py
git commit -m "챌린지: 관리 API(달성현황·영상·참가자)"
```

---

## Task 7: 멤버 제출 화면

**Files:**
- Create: `shopping_shorts/static/challenge.html`
- Modify: `shopping_shorts/app.py` (라우트 1개)

- [ ] **Step 1: 라우트를 추가한다**

`shopping_shorts/app.py`에서 다른 정적 페이지 라우트를 찾아 패턴을 확인한다:

실측 패턴은 `app.py:8788` `_admin_page`다 — `STATIC_DIR` 같은 상수는 **없다**.
경로를 매번 `Path(__file__).parent / "static"`으로 만들고, `media_type`에
**charset을 명시**한다(빠지면 한글이 깨진다).

`api_challenge_member_set` 뒤에 추가한다:

```python
@app.get("/challenge", response_class=HTMLResponse)
def page_challenge():
    """멤버 제출 화면 — 단톡방에 이 주소를 공유한다."""
    return FileResponse(Path(__file__).parent / "static" / "challenge.html",
                        media_type="text/html; charset=utf-8")


@app.get("/challenge/admin", response_class=HTMLResponse)
def page_challenge_admin():
    """관리 화면. 데이터 API가 관리자를 검사하므로 페이지 자체는 열어둔다
    (비관리자가 열면 목록이 비고 안내가 뜬다)."""
    return FileResponse(Path(__file__).parent / "static" / "challenge_admin.html",
                        media_type="text/html; charset=utf-8")
```

- [ ] **Step 2: 화면을 만든다**

★썸네일 프록시 실측(2026-08-24 확인):
- 파라미터는 **`?url=`**다(`app.py:5579`). `?u=`로 쓰면 422로 전부 깨진다.
- 허용 CDN(`app.py:5381 _ALLOWED_THUMB_HOSTS`)에 인스타(`cdninstagram.com`·
  `fbcdn.net`)·유튜브(`ytimg.com`)·틱톡(`tiktokcdn.com`)이 **이미 있다** →
  챌린지 3플랫폼은 추가 등록 없이 뜬다. (과거 화이트리스트 누락으로 카드가
  검게 뜬 사고가 3회 있었다 — 새 플랫폼을 붙일 때만 확인하면 된다.)

`shopping_shorts/static/challenge.html`을 새로 만든다. 테마 변수는 기존 페이지(`theme.css`)를 그대로 쓴다:

```html
<!doctype html>
<meta charset="utf-8">
<meta name="viewport" content="width=device-width, initial-scale=1">
<title>1기 챌린지 — 영상 제출</title>
<link rel="stylesheet" href="/static/theme.css">
<style>
  /* ★팔레트는 index.html·produce.html과 같은 토큰을 쓴다(실측 index.html:12-27).
     theme.css에는 --bg/--txt/--panel/--line이 **없다**(--mint/--aurora만) —
     각 페이지가 인라인 :root로 정의한다. fallback에만 기대면 라이트 모드에서
     글자가 안 보인다. 그래서 여기서도 같은 토큰을 그대로 선언한다. */
  :root{--bg:#0a0f0d;--panel:#101815;--line:#1e2b26;--txt:#eaf3f0;--sub:#8fa1a0;
    --accent:#37e0bd;--accent2:#1f9e7a;--fire:#ff6b6b;--ok:#37e0bd;--gold:#f0c453;
    --inset:#0c1412;--hover:#131d19;--shorts-ar:9/16;color-scheme:dark;
    --chal-0-bg:#3a1a1a;--chal-0-fg:#ff8a80;
    --chal-1-bg:#3a3018;--chal-1-fg:#e9b45c;
    --chal-2-bg:#12312a;--chal-2-fg:#5fd1a9;}
  :root[data-theme="light"]{--bg:#f4faf7;--panel:#ffffff;--line:#d5e6df;--txt:#0f1a16;--sub:#5c726d;
    --accent:#12b886;--accent2:#0ca678;--fire:#e0524a;--ok:#12b886;--gold:#b8860b;
    --inset:#eef7f3;--hover:#eef7f3;color-scheme:light;
    --chal-0-bg:#fdeaea;--chal-0-fg:#b3261e;
    --chal-1-bg:#fdf2df;--chal-1-fg:#8a6100;
    --chal-2-bg:#e1f3ec;--chal-2-fg:#0a7d66;}
  body{margin:0;font-family:'IBM Plex Sans KR','Malgun Gothic',system-ui,sans-serif;
       background:var(--bg);color:var(--txt)}
  .wrap{max-width:720px;margin:0 auto;padding:24px 16px 60px}
  h1{font-size:22px;margin:0 0 4px}
  .sub{color:var(--sub);font-size:14px;margin-bottom:20px}
  .today{display:flex;align-items:baseline;gap:10px;background:var(--panel);
         border:1px solid var(--line);border-radius:14px;padding:18px 20px;margin-bottom:18px}
  .today b{font-size:38px;line-height:1}
  .today .goal{font-size:18px;color:var(--sub)}
  .today .done{margin-left:auto;font-size:13px;color:var(--sub);text-align:right}
  .box{display:flex;gap:8px;margin-bottom:8px}
  .box input{flex:1;min-width:0;padding:13px 14px;font-size:15px;border-radius:10px;
             border:1px solid var(--line);background:var(--inset);color:inherit}
  .box button{padding:13px 22px;font-size:15px;font-weight:700;border:0;border-radius:10px;
              background:#2f80ed;color:#fff;cursor:pointer}
  .box button:disabled{opacity:.5;cursor:default}
  .msg{min-height:20px;font-size:14px;margin-bottom:22px}
  .msg.ok{color:#5fd1a9} .msg.no{color:#ff8a80}
  .day{margin:22px 0 10px;font-size:13px;color:var(--sub);
       border-top:1px solid var(--line);padding-top:12px}
  .cards{display:grid;grid-template-columns:repeat(auto-fill,minmax(150px,1fr));gap:12px}
  .card{background:var(--panel);border:1px solid var(--line);
        border-radius:12px;overflow:hidden;display:flex;flex-direction:column}
  .card .th{aspect-ratio:var(--shorts-ar);background:var(--inset);display:block;
            object-fit:cover;width:100%}
  .card .no-th{aspect-ratio:var(--shorts-ar);display:flex;align-items:center;justify-content:center;
               color:var(--sub);font-size:26px;background:var(--inset)}
  .card .t{font-size:12px;padding:8px 9px;line-height:1.4;
           display:-webkit-box;-webkit-line-clamp:2;-webkit-box-orient:vertical;overflow:hidden}
  .card .m{font-size:11px;color:var(--sub);padding:0 9px 9px;margin-top:auto}
  .empty{color:var(--sub);font-size:14px;padding:30px 0;text-align:center}
</style>
<div class="wrap">
  <h1>🔥 1기 챌린지</h1>
  <div class="sub" id="period">하루 2영상 · 올린 영상 링크를 넣어주세요</div>

  <div class="today">
    <div><b id="todayN">0</b><span class="goal"> / <span id="goalN">2</span></span></div>
    <div style="font-size:13px;color:var(--sub)">오늘</div>
    <div class="done">달성한 날 <b id="doneN">0</b>일<br>총 <span id="totalN">0</span>개</div>
  </div>

  <div class="box">
    <input id="url" type="url" placeholder="인스타·유튜브·틱톡 영상 주소를 붙여넣기"
           autocomplete="off" enterkeyhint="send">
    <button id="go">제출</button>
  </div>
  <div class="msg" id="msg"></div>

  <div id="list"><div class="empty">아직 제출한 영상이 없어요</div></div>
</div>
<script>
const $ = s => document.querySelector(s);
const esc = s => String(s == null ? '' : s).replace(/[&<>"]/g,
  c => ({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;'}[c]));

function say(text, ok) {
  const m = $('#msg');
  m.textContent = text;
  m.className = 'msg ' + (ok ? 'ok' : 'no');
}

function cardHtml(it) {
  const th = it.thumb
    ? `<img class="th" src="/api/thumb?url=${encodeURIComponent(it.thumb)}" alt="" loading="lazy">`
    : `<div class="no-th">🎬</div>`;
  const views = (it.views || it.views === 0) && it.fetch_status === 'ok'
    ? `조회 ${Number(it.views).toLocaleString()}` : '';
  const badge = it.fetch_status === 'ok' ? '' :
    (it.platform === 'tiktok' ? '틱톡' : '정보 수집 중');
  return `<a class="card" href="${esc(it.url)}" target="_blank" rel="noopener">
    ${th}
    <div class="t">${esc(it.title || it.url)}</div>
    <div class="m">${esc(views || badge)}</div>
  </a>`;
}

function render(d) {
  $('#todayN').textContent = d.today || 0;
  $('#goalN').textContent = d.goal || 2;
  $('#doneN').textContent = d.done_days || 0;
  $('#totalN').textContent = d.total || 0;
  if (d.start || d.end) {
    $('#period').textContent = `하루 ${d.goal || 2}영상 · ${d.start || ''} ~ ${d.end || ''}`;
  }
  if (!d.is_member) {
    $('#list').innerHTML = '<div class="empty">1기 챌린지 참가자만 제출할 수 있어요.<br>'
      + '참가 신청은 사장님께 문의해주세요.</div>';
    $('#go').disabled = true;
    return;
  }
  const items = d.items || [];
  if (!items.length) {
    $('#list').innerHTML = '<div class="empty">아직 제출한 영상이 없어요</div>';
    return;
  }
  const byDay = {};
  items.forEach(i => { (byDay[i.submit_day] = byDay[i.submit_day] || []).push(i); });
  $('#list').innerHTML = Object.keys(byDay).sort().reverse().map(day =>
    `<div class="day">${esc(day)} · ${byDay[day].length}개</div>
     <div class="cards">${byDay[day].map(cardHtml).join('')}</div>`
  ).join('');
}

async function load() {
  try {
    const d = await (await fetch('/api/challenge/mine')).json();
    render(d);
  } catch (e) {
    say('불러오지 못했어요 — 새로고침 해주세요', false);
  }
}

async function submit() {
  const url = $('#url').value.trim();
  if (!url) { say('영상 주소를 넣어주세요', false); return; }
  $('#go').disabled = true;
  say('제출 중…', true);
  try {
    const r = await fetch('/api/challenge/submit?url=' + encodeURIComponent(url),
                          { method: 'POST' });
    const d = await r.json();
    if (!r.ok) { say(d.error || '제출하지 못했어요', false); return; }
    $('#url').value = '';
    say(`제출 완료! 오늘 ${d.today}개`, true);
    await load();
    // 수집이 끝나면 썸네일이 채워진다 — 잠깐 뒤 한 번만 다시 읽는다.
    setTimeout(load, 6000);
  } catch (e) {
    say('제출하지 못했어요 — 잠시 후 다시 시도해주세요', false);
  } finally {
    $('#go').disabled = false;
  }
}

$('#go').addEventListener('click', submit);
$('#url').addEventListener('keydown', e => { if (e.key === 'Enter') submit(); });
load();
</script>
```

- [ ] **Step 3: 문법을 검사한다**

★JS 검사에 문법검사만 두지 마라 — `let` 선언 전 사용(TDZ)은 `node --check`가 통과시킨다. 다음 Step의 실제 열어보기가 진짜 검증이다.

Run:
```bash
py -c "import re,io; s=io.open('shopping_shorts/static/challenge.html',encoding='utf-8').read(); m=re.search(r'<script>(.*?)</script>', s, re.S); io.open('/tmp/ch.js','w',encoding='utf-8').write(m.group(1))"
node --check /tmp/ch.js
```
Expected: 출력 없음(통과)

- [ ] **Step 4: 실제로 열어서 확인한다**

정적 검토는 관찰이 아니다. 서버를 띄우고 브라우저로 연다.

```bash
py -m uvicorn shopping_shorts.app:app --port 8899
```
브라우저에서 `http://127.0.0.1:8899/challenge`를 연다.

⚠️ **8899 포트에 옛 uvicorn이 떠 있으면 옛 코드가 응답한다** — 전례가 있다. 먼저 확인한다:
```bash
netstat -ano | findstr :8899
```
떠 있으면 그 프로세스를 끄고 새로 띄운다.
(2026-08-24 실측: 8899·8849 둘 다 비어 있었다. 그래도 매번 확인해라 — 다른 세션이 띄운다.)

⚠️ **`DASH_PASS`를 비워야 한다**(실측 `app.py:6454`: 비어 있으면 인증 OFF = 로컬 개발 모드).
이때 `_AUTH_ON`이 꺼져 `customer_id`가 **0(사장님)**으로 고정된다 →
`_is_admin(0)`이 True라 **관리 화면도 그냥 열린다**. 멤버 화면을 제대로 보려면
챌린지 참가자로 cid 0을 등록해 두면 된다:
```bash
py -c "from shopping_shorts.store import Store; s=Store('shopping_shorts/data/app.db'); s.add_challenge_member(0); print(s.list_challenge_members())"
```

확인할 것:
- 한글이 깨지지 않는다(charset)
- "오늘 0 / 2"가 보인다
- 참가자가 아니면 안내 문구가 뜨고 제출 버튼이 비활성이다
- 화면이 가로로 스크롤되지 않는다(모바일 폭에서도)

- [ ] **Step 5: 커밋**

```bash
git add shopping_shorts/static/challenge.html shopping_shorts/app.py
git commit -m "챌린지: 멤버 제출 화면 /challenge"
```

---

## Task 8: 관리 화면 (탭 2개)

**Files:**
- Create: `shopping_shorts/static/challenge_admin.html`

라우트는 Task 7에서 이미 만들었다.

- [ ] **Step 1: 화면을 만든다**

`shopping_shorts/static/challenge_admin.html`을 새로 만든다:

```html
<!doctype html>
<meta charset="utf-8">
<meta name="viewport" content="width=device-width, initial-scale=1">
<title>1기 챌린지 — 관리</title>
<link rel="stylesheet" href="/static/theme.css">
<style>
  /* ★팔레트는 index.html·produce.html과 같은 토큰을 쓴다(실측 index.html:12-27).
     theme.css에는 --bg/--txt/--panel/--line이 **없다**(--mint/--aurora만) —
     각 페이지가 인라인 :root로 정의한다. fallback에만 기대면 라이트 모드에서
     글자가 안 보인다. 그래서 여기서도 같은 토큰을 그대로 선언한다. */
  :root{--bg:#0a0f0d;--panel:#101815;--line:#1e2b26;--txt:#eaf3f0;--sub:#8fa1a0;
    --accent:#37e0bd;--accent2:#1f9e7a;--fire:#ff6b6b;--ok:#37e0bd;--gold:#f0c453;
    --inset:#0c1412;--hover:#131d19;--shorts-ar:9/16;color-scheme:dark;
    --chal-0-bg:#3a1a1a;--chal-0-fg:#ff8a80;
    --chal-1-bg:#3a3018;--chal-1-fg:#e9b45c;
    --chal-2-bg:#12312a;--chal-2-fg:#5fd1a9;}
  :root[data-theme="light"]{--bg:#f4faf7;--panel:#ffffff;--line:#d5e6df;--txt:#0f1a16;--sub:#5c726d;
    --accent:#12b886;--accent2:#0ca678;--fire:#e0524a;--ok:#12b886;--gold:#b8860b;
    --inset:#eef7f3;--hover:#eef7f3;color-scheme:light;
    --chal-0-bg:#fdeaea;--chal-0-fg:#b3261e;
    --chal-1-bg:#fdf2df;--chal-1-fg:#8a6100;
    --chal-2-bg:#e1f3ec;--chal-2-fg:#0a7d66;}
  body{margin:0;font-family:'IBM Plex Sans KR','Malgun Gothic',system-ui,sans-serif;
       background:var(--bg);color:var(--txt)}
  .wrap{max-width:1400px;margin:0 auto;padding:20px 16px 60px}
  h1{font-size:20px;margin:0 0 14px}
  .tabs{display:flex;gap:6px;margin-bottom:16px;border-bottom:1px solid var(--line)}
  .tab{padding:10px 18px;font-size:14px;font-weight:700;cursor:pointer;
       border:0;background:none;color:var(--sub);border-bottom:2px solid transparent}
  .tab.on{color:var(--txt);border-bottom-color:#2f80ed}
  .pane{display:none} .pane.on{display:block}

  .gridwrap{overflow-x:auto;border:1px solid var(--line);border-radius:12px}
  table{border-collapse:separate;border-spacing:0;font-size:13px;width:max-content;min-width:100%}
  th,td{padding:7px 9px;white-space:nowrap;border-bottom:1px solid var(--line)}
  thead th{position:sticky;top:0;background:var(--panel);z-index:2;font-size:12px}
  .who{position:sticky;left:0;background:var(--panel);z-index:1;text-align:left;
       min-width:150px;font-weight:600}
  thead .who{z-index:3}
  .cell{width:34px;text-align:center;font-weight:700;border-radius:5px}
  /* 달성 칸 색 — 토큰으로 둔다(라이트 모드에서 어두운 하드코딩 색은 안 읽힌다). */
  .c0{background:var(--chal-0-bg);color:var(--chal-0-fg)}
  .c1{background:var(--chal-1-bg);color:var(--chal-1-fg)}
  .c2{background:var(--chal-2-bg);color:var(--chal-2-fg)}
  .rate{text-align:right;color:var(--sub)}

  .bar{display:flex;gap:8px;align-items:center;margin-bottom:14px;flex-wrap:wrap}
  select,input[type=search]{padding:8px 10px;font-size:13px;border-radius:8px;
       border:1px solid var(--line);background:var(--inset);color:inherit}
  .cards{display:grid;grid-template-columns:repeat(auto-fill,minmax(170px,1fr));gap:14px}
  .card{background:var(--panel);border:1px solid var(--line);
        border-radius:12px;overflow:hidden;display:flex;flex-direction:column}
  .card .th{aspect-ratio:var(--shorts-ar);width:100%;object-fit:cover;display:block;
            background:var(--inset)}
  .card .no-th{aspect-ratio:var(--shorts-ar);display:flex;align-items:center;justify-content:center;
               font-size:28px;color:var(--sub);background:var(--inset)}
  .card .t{font-size:12px;padding:8px 9px 4px;line-height:1.4;
           display:-webkit-box;-webkit-line-clamp:2;-webkit-box-orient:vertical;overflow:hidden}
  .card .m{font-size:11px;color:var(--sub);padding:0 9px 9px;margin-top:auto}
  .who-tag{font-size:11px;color:#2f80ed;padding:0 9px}
  .empty{color:var(--sub);padding:40px 0;text-align:center;font-size:14px}
</style>
<div class="wrap">
  <h1>🔥 1기 챌린지 관리</h1>
  <div class="tabs">
    <button class="tab on" data-p="board">달성현황</button>
    <button class="tab" data-p="videos">영상</button>
  </div>

  <div class="pane on" id="p-board">
    <div class="bar" id="boardInfo"></div>
    <div class="gridwrap"><table id="grid"></table></div>
  </div>

  <div class="pane" id="p-videos">
    <div class="bar">
      <select id="sort">
        <option value="recent">최신순</option>
        <option value="views">조회수순</option>
      </select>
      <select id="member"><option value="0">전체 멤버</option></select>
      <select id="platform">
        <option value="">전체 플랫폼</option>
        <option value="instagram">인스타</option>
        <option value="youtube">유튜브</option>
        <option value="tiktok">틱톡</option>
      </select>
      <span id="vcount" style="color:var(--sub);font-size:13px"></span>
    </div>
    <div id="cards"><div class="empty">불러오는 중…</div></div>
  </div>
</div>
<script>
const $ = s => document.querySelector(s);
const esc = s => String(s == null ? '' : s).replace(/[&<>"]/g,
  c => ({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;'}[c]));

function dayRange(start, end, today) {
  // 시작~오늘(또는 종료일 중 이른 쪽)까지의 날짜 목록. 미래 칸은 그리지 않는다.
  const last = (end && end < today) ? end : today;
  if (!start || start > last) return [today];
  const out = [], d = new Date(start + 'T00:00:00Z');
  const stop = new Date(last + 'T00:00:00Z');
  while (d <= stop && out.length < 400) {
    out.push(d.toISOString().slice(0, 10));
    d.setUTCDate(d.getUTCDate() + 1);
  }
  return out;
}

async function loadBoard() {
  let d;
  try {
    const r = await fetch('/api/challenge/board');
    if (r.status === 403) {
      $('#grid').innerHTML = '<tr><td class="empty">관리자만 볼 수 있어요</td></tr>';
      return;
    }
    d = await r.json();
  } catch (e) {
    $('#grid').innerHTML = '<tr><td class="empty">불러오지 못했어요</td></tr>';
    return;
  }
  const goal = d.goal || 2;
  const days = dayRange(d.start, d.end, d.today);
  const members = d.members || [];
  $('#boardInfo').textContent =
    `참가자 ${members.length}명 · 목표 하루 ${goal}개 · ${d.start || ''} ~ ${d.end || ''}`;

  const head = '<thead><tr><th class="who">멤버</th>'
    + days.map(x => `<th>${esc(x.slice(5))}</th>`).join('')
    + '<th class="rate">달성</th></tr></thead>';
  const body = members.map(m => {
    const cells = days.map(day => {
      const n = m.by_day[day] || 0;
      const cls = n >= goal ? 'c2' : (n > 0 ? 'c1' : 'c0');
      return `<td class="cell ${cls}">${n}</td>`;
    }).join('');
    const pct = days.length ? Math.round(m.done_days / days.length * 100) : 0;
    return `<tr><td class="who">${esc(m.name)}</td>${cells}`
      + `<td class="rate">${m.done_days}/${days.length} (${pct}%)</td></tr>`;
  }).join('');
  $('#grid').innerHTML = head + '<tbody>'
    + (body || `<tr><td class="empty" colspan="${days.length + 2}">참가자가 없어요</td></tr>`)
    + '</tbody>';

  // 영상 탭의 멤버 필터도 같이 채운다.
  const sel = $('#member');
  if (sel.options.length <= 1) {
    members.forEach(m => {
      const o = document.createElement('option');
      o.value = m.customer_id; o.textContent = m.name;
      sel.appendChild(o);
    });
  }
}

function cardHtml(it) {
  const th = it.thumb
    ? `<img class="th" src="/api/thumb?url=${encodeURIComponent(it.thumb)}" alt="" loading="lazy">`
    : `<div class="no-th">🎬</div>`;
  const views = (it.fetch_status === 'ok' && (it.views || it.views === 0))
    ? `조회 ${Number(it.views).toLocaleString()}`
    : (it.platform === 'tiktok' ? '틱톡' : '수집 중');
  return `<a class="card" href="${esc(it.url)}" target="_blank" rel="noopener">
    ${th}
    <div class="who-tag">${esc(it.member_name || '')}</div>
    <div class="t">${esc(it.title || it.url)}</div>
    <div class="m">${esc(it.submit_day)} · ${esc(views)}</div>
  </a>`;
}

async function loadVideos() {
  const q = new URLSearchParams({
    sort: $('#sort').value,
    member: $('#member').value,
    platform: $('#platform').value,
  });
  try {
    const r = await fetch('/api/challenge/videos?' + q);
    if (r.status === 403) {
      $('#cards').innerHTML = '<div class="empty">관리자만 볼 수 있어요</div>';
      return;
    }
    const d = await r.json();
    const items = d.items || [];
    $('#vcount').textContent = `${items.length}개`;
    $('#cards').innerHTML = items.length
      ? '<div class="cards">' + items.map(cardHtml).join('') + '</div>'
      : '<div class="empty">제출된 영상이 없어요</div>';
  } catch (e) {
    $('#cards').innerHTML = '<div class="empty">불러오지 못했어요</div>';
  }
}

document.querySelectorAll('.tab').forEach(t => {
  t.addEventListener('click', () => {
    document.querySelectorAll('.tab').forEach(x => x.classList.remove('on'));
    document.querySelectorAll('.pane').forEach(x => x.classList.remove('on'));
    t.classList.add('on');
    $('#p-' + t.dataset.p).classList.add('on');
    if (t.dataset.p === 'videos') loadVideos();
  });
});
['#sort', '#member', '#platform'].forEach(s =>
  $(s).addEventListener('change', loadVideos));

loadBoard();
</script>
```

- [ ] **Step 2: 문법을 검사한다**

Run:
```bash
py -c "import re,io; s=io.open('shopping_shorts/static/challenge_admin.html',encoding='utf-8').read(); m=re.search(r'<script>(.*?)</script>', s, re.S); io.open('/tmp/cha.js','w',encoding='utf-8').write(m.group(1))"
node --check /tmp/cha.js
```
Expected: 출력 없음

- [ ] **Step 3: 실제로 열어서 확인한다**

로컬 서버(8899)를 띄운 상태에서 데이터를 심고 확인한다:

```bash
py -c "
from shopping_shorts.store import Store
s = Store('shopping_shorts/data/app.db')
s.add_challenge_member(1)
print(s.list_challenge_members())
"
```

`http://127.0.0.1:8899/challenge/admin`을 연다.

확인할 것:
- 탭 2개가 전환된다
- 달성현황 그리드에서 **멤버 이름 열이 가로 스크롤 시 왼쪽에 고정**된다(sticky)
- 날짜 헤더가 세로 스크롤 시 위에 고정된다
- 칸 색이 0=빨강 / 1=노랑 / 2+=초록으로 갈린다
- 영상 탭에서 정렬·필터가 동작한다
- 페이지가 가로로 넘치지 않는다(그리드는 자기 상자 안에서 스크롤)

- [ ] **Step 4: 커밋**

```bash
git add shopping_shorts/static/challenge_admin.html
git commit -m "챌린지: 관리 화면(달성현황 그리드 + 영상 카드)"
```

---

### 사이드바를 어느 화면에 넣나 (2026-08-24 실측 판단)

```
help.html        사이드바 0   ← 공개 안내 페이지(로그인 없이 열림)
collection.html  사이드바 1   ← 앱 내부 페이지
archive.html     사이드바 1
produce.html     사이드바 8
```

- **멤버 화면 `/challenge`** — 단톡방 링크로 바로 들어오는 단일 목적 화면이다.
  `help.html`처럼 **사이드바 없이** 간다. 폰에서 링크를 누른 사람에게 앱 전체
  메뉴를 보여줄 이유가 없다.
- **관리 화면 `/challenge/admin`** — 사장님이 앱 안에서 쓰는 도구다.
  `archive.html`처럼 **사이드바를 넣는다**: `<script src="/sidebar.js?v=2"></script>`
  (경로는 `/static/sidebar.js`가 아니라 **`/sidebar.js`** — 실측 `app.py:6528`에
  전용 라우트가 있고 무료 화이트리스트에도 들어 있다.)

그래도 Task 9에서 **멤버 화면 링크를 메뉴에 넣는다** — 앱을 쓰다가 챌린지로
건너가는 통로가 필요하기 때문이다(단톡방 링크를 잃어버린 사람도 찾아간다).

## Task 9: 사이드바 메뉴

**Files:**
- Modify: `shopping_shorts/static/sidebar.js`

- [ ] **Step 1: 구조를 확인한다 (아래는 2026-08-24 실측)**

`sidebar.js:31`의 `NAV` 배열이 메뉴를 만든다. 항목 속성은 **`icon`·`text`·`href`·`free`·`admin`** 이다
(`label`이 아니라 **`text`**다 — `label`은 그룹 이름에 쓰인다):

```javascript
  var NAV = [
    { label: "리서치", items: [
      { icon: "📊", text: "레퍼런스 랭킹",   href: "/", free: true },
      ...
      { icon: "📋", text: "레퍼런스 채널 관리", href: "/refs", admin: true },
    ] },
    { label: "제작", items: [
      { icon: "🎬", text: "숏템 제작소",     href: "/produce", free: true },
    ] },
    { label: "소통", items: [ ... ] },
```

- `free: true` → 잠금(🔒) 대상에서 뺀다. **서버 화이트리스트(`_FREE_EXACT_GET`)와 짝**이다 —
  서버만 열고 여기를 안 열면 체험 사용자는 눌러도 페이월 모달만 본다. 챌린지는 1기(유료)
  멤버 전용이므로 `free`를 **붙이지 않는다**.
- `admin: true` → 관리자에게만 보인다(`display:none`으로 숨김).

- [ ] **Step 2: 메뉴를 추가한다**

"제작" 그룹의 `items` 배열에 두 줄을 추가한다(`숏템 제작소` 다음 줄):

```javascript
      { icon: "🔥", text: "1기 챌린지",      href: "/challenge" },
      { icon: "🏁", text: "챌린지 관리",     href: "/challenge/admin", admin: true },
```

관리 화면은 `admin: true`라 사장님에게만 보인다 — 별도 분기를 만들 필요가 없다
(`sidebar.js:180`이 `it.admin`을 이미 처리한다).

- [ ] **Step 3: 문법을 검사한다**

Run: `node --check shopping_shorts/static/sidebar.js`
Expected: 출력 없음

★이것만으로 안심하지 마라 — TDZ 오류는 통과한다. 다음 Step이 진짜 검증이다.

- [ ] **Step 4: 실제로 확인한다**

로컬 서버에서 아무 페이지(`/produce` 등)를 열고 사이드바에 항목이 뜨는지, **다른 메뉴가 사라지지 않았는지** 본다.

★`sidebar.js`는 모든 페이지가 쓴다. 여기서 오류가 나면 **전 화면의 사이드바가 통째로 죽는다**. 브라우저 콘솔에 오류가 없는지 반드시 확인한다.

- [ ] **Step 5: 커밋**

```bash
git add shopping_shorts/static/sidebar.js
git commit -m "챌린지: 사이드바 메뉴"
```

---

## Task 10: 기간 설정을 admin에 노출

**Files:**
- Modify: `shopping_shorts/app.py` (`_ADMIN_SETTING_KEYS`)
- Modify: `shopping_shorts/static/admin.html`

- [ ] **Step 1: 설정 키를 허용목록에 넣는다**

`shopping_shorts/app.py:8381` 근처의 `_ADMIN_SETTING_KEYS` 집합을 찾아, 마지막 항목 뒤에 추가한다:

```python
                       # 1기 챌린지(2026-08-24) — 기간·하루 목표
                       "challenge_start", "challenge_end", "challenge_daily_goal",
```

- [ ] **Step 2: 테스트를 쓴다**

`shopping_shorts/tests/test_challenge_api.py` 끝에 추가한다:

```python
def test_admin_can_set_challenge_period(env, monkeypatch):
    """기간을 admin 설정으로 저장할 수 있다 — 2기·3기를 코드 수정 없이 연다."""
    _admin_env(env, monkeypatch)
    monkeypatch.setattr(appmod, "_is_admin", lambda cid: cid == 5)
    assert "challenge_start" in appmod._ADMIN_SETTING_KEYS
    assert "challenge_end" in appmod._ADMIN_SETTING_KEYS
    assert "challenge_daily_goal" in appmod._ADMIN_SETTING_KEYS
```

- [ ] **Step 3: 통과를 확인한다**

Run: `py -m pytest shopping_shorts/tests/test_challenge_api.py -v`
Expected: PASS — 19 passed

- [ ] **Step 4: admin.html에 입력칸을 추가한다**

실측: `admin.html:77`의 `SETTING_FIELDS` 배열 하나가 설정칸을 전부 만든다.
`[키, 라벨]` 쌍을 추가하기만 하면 된다 — 별도 HTML·핸들러가 필요 없다.

`admin.html:82`의 마지막 줄(`['biz_email','[약관] 문의 이메일']];`)을 아래로 바꾼다:

```javascript
  ['biz_name','[약관] 상호'],['biz_owner','[약관] 대표자'],['biz_regno','[약관] 사업자등록번호'],['biz_addr','[약관] 주소'],['biz_sales_no','[약관] 통신판매업신고번호'],['biz_email','[약관] 문의 이메일'],
  ['challenge_start','[챌린지] 시작일(YYYY-MM-DD)'],['challenge_end','[챌린지] 종료일(YYYY-MM-DD)'],['challenge_daily_goal','[챌린지] 하루 목표 영상 수']];
```

★`app.py:8420`이 `_ADMIN_SETTING_KEYS`로 보여줄 키를 거른다 — Step 1을
먼저 해야 칸이 화면에 뜬다(저장 가능한 키와 보여줄 키가 한 곳에서 나온다).

- [ ] **Step 5: 실제로 확인한다**

로컬에서 `/admin`을 열고 세 칸이 보이는지, 값을 넣고 저장하면 유지되는지 확인한다.

- [ ] **Step 6: 커밋**

```bash
git add shopping_shorts/app.py shopping_shorts/static/admin.html shopping_shorts/tests/test_challenge_api.py
git commit -m "챌린지: 기간·목표를 admin 설정으로"
```

---

## Task 11: 참가자 등록 UI

**Files:**
- Modify: `shopping_shorts/static/admin.html`

API는 Task 6에서 만들었다(`GET/POST /api/challenge/member(s)`). 여기서는 화면만 붙인다.

- [ ] **Step 1: 실제 패턴 (2026-08-24 실측 — 이대로 따라라)**

`admin.html:134` `renderCustomers()`가 행을 그린다. 버튼은 **인라인 `onclick`**을 쓴다
(이벤트 위임이 아니다 — 이 파일의 방식을 따라라, 0순위-B):

```javascript
          <button class=bpro onclick="setPlan(${c.id},'pro')">pro</button>
          ${c.code_admin?'':(c.is_admin
            ? `<button class=badm onclick="setAdmin(${c.id},false)">관리자해제</button>`
            : `<button class=badm onclick="setAdmin(${c.id},true)">관리자지정</button>`)}
```

핸들러도 같은 모양이다(`admin.html:263` `setAdmin`): confirm → fetch → `cMsg`에
결과 표시 → `load()`로 목록 새로고침.

- [ ] **Step 2: 챌린지 참가 토글을 추가한다**

행 템플릿의 마지막 `<td>` 안, `관리자지정` 버튼 블록 **다음에** 넣는다:

```javascript
          <button class=bchal onclick="setChallenge(${c.id},${c.challenge?'false':'true'})">${c.challenge?'🔥챌린지해제':'챌린지참가'}</button>
```

핸들러는 `setAdmin`(admin.html:263) **바로 아래에** 같은 모양으로 추가한다:

```javascript
// 1기 챌린지 참가 토글(2026-08-24) — 참가자만 /challenge에서 영상을 제출할 수 있다.
async function setChallenge(cid,flag){
  const r=await fetch(`/api/challenge/member?customer_id=${cid}&active=${flag?1:0}`,{method:'POST'});
  const m=document.getElementById('cMsg');
  if(r.ok){m.textContent=flag?'✅ 챌린지 참가 등록':'✅ 챌린지 참가 해제';m.className='msg';load();}
  else{m.textContent='❌ 실패';m.className='msg err';}
}
```

⚠️ 버튼을 추가해도 `<td>` 개수는 그대로다(마지막 칸 안에 들어간다) — `colspan=14`는
건드리지 마라.

`c.challenge` 값을 주려면 고객 목록 API가 그것을 실어야 한다.
실측: `app.py:8393` `_admin_customers`의 루프 변수는 `cu`, 저장소는 `st`다.
`cu["is_admin"] = _is_admin(cu["id"])` 줄 **바로 아래에** 한 줄 추가한다:

```python
        cu["challenge"] = st.is_challenge_member(cu["id"])      # 1기 챌린지 참가 여부(2026-08-24)
```

- [ ] **Step 3: 실제로 확인한다**

로컬 `/admin`에서:
- 고객 행에 "챌린지 참가" 버튼이 보인다
- 누르면 "🔥 챌린지 참가중"으로 바뀐다
- 새로고침해도 유지된다
- `/challenge/admin` 달성현황에 그 사람이 나타난다

- [ ] **Step 4: 기존 admin 테스트가 안 깨졌는지 본다**

Run: `py -m pytest shopping_shorts/tests/test_admin_customer_mgmt.py shopping_shorts/tests/test_admin_payload.py -q 2>&1 | tail -5`
Expected: 작업 전과 같은 통과 개수

- [ ] **Step 5: 커밋**

```bash
git add shopping_shorts/static/admin.html shopping_shorts/app.py
git commit -m "챌린지: admin 고객표에 참가 토글"
```

---

## Task 12: 전체 검증과 배포

- [ ] **Step 1: 챌린지 테스트 전부**

Run: `py -m pytest shopping_shorts/tests/test_challenge.py shopping_shorts/tests/test_challenge_store.py shopping_shorts/tests/test_challenge_api.py -v`
Expected: 전부 PASS (약 38개)

- [ ] **Step 2: 기준선과 대조한다**

★"전부 green"이 아니라 **"병합 전보다 실패가 늘지 않았나"**로 본다(기존 실패가 이미 있다).

먼저 내 변경 없이 기준선을 잰다:
```bash
git stash
py -m pytest shopping_shorts/tests -q 2>&1 | tail -3   # 기준선 기록
git stash pop
```
⚠️ 트랙 폴더에서 `git stash`는 다른 세션의 옛 stash가 pop돼 충돌을 낼 수 있다(전례 있음).
대신 안전하게: 별도 임시 클론에서 기준선을 재거나, `git stash list`로 **비어 있음을 먼저 확인**하고 쓴다.

그 다음 현재 상태:
```bash
py -m pytest shopping_shorts/tests -q 2>&1 | tail -3
```
실패 개수가 기준선보다 **늘지 않았으면** 통과.

- [ ] **Step 3: 브라우저 최종 확인**

로컬 서버에서 실제로 한 바퀴 돈다(정적 검토는 관찰이 아니다):

1. `/admin` → 고객 하나를 챌린지 참가로 지정
2. `/admin` → 챌린지 시작일·종료일 설정
3. 그 계정으로 `/challenge` → 유튜브 링크 제출 → "오늘 1/2" 표시
4. 6초 뒤 썸네일이 채워지는지
5. 같은 링크 재제출 → "이미 제출한 영상이에요"
6. `/challenge/admin` → 달성현황에 노랑 칸 1개, 영상 탭에 카드 1개

- [ ] **Step 4: 커밋하고 병합한다**

```bash
git -C "C:\Users\CH\Desktop\로또의 주식\.tracks\챌린지관리" status --porcelain   # 내 파일만인지 눈으로 확인
git -C "C:\Users\CH\Desktop\로또의 주식\.tracks\챌린지관리" add -A
git -C "C:\Users\CH\Desktop\로또의 주식\.tracks\챌린지관리" commit -m "챌린지: 최종 검증"
py tools/track.py finish 챌린지관리
```

- [ ] **Step 5: 병합됐는지 직접 대조한다**

★`finish`는 **exit 0이어도 병합이 안 됐을 수 있다**(게이트 락 대기로 조용히 끝난 전례 2회).

```bash
git -C "C:\Users\CH\Desktop\로또의 주식\.tracks\챌린지관리" log -1 --format=%H
git branch -r --contains <위에서 나온 커밋 해시>
```
`origin/main`이 목록에 있으면 병합 성공. 없으면 다시 `finish`.

- [ ] **Step 6: 라이브 확인 (3분 뒤)**

서버 크론이 3분마다 배포한다. `shoppingshorts.duckdns.org/challenge`를 실제로 열어 확인한다.

⚠️ 비로그인 `curl`은 없는 라우트에도 401을 준다 — **브라우저로 로그인 상태에서** 확인해야 판별된다.

- [ ] **Step 7: 기록을 남긴다**

```bash
# handoff/챌린지관리.md — 무엇을·왜·다음 할 일
# wiki/log.d/챌린지관리.md — 1~3줄 요약
```
다음 세션이 `find_work.py 챌린지`로 찾는다. 기록이 없으면 처음부터 다시 조사한다.

---

## 안 하는 것 (YAGNI)

- 채널 자동 크롤(멤버가 안 내도 자동 인정) — 100채널 매일 크롤은 비용·차단 리스크
- 순위·포인트·상품 지급
- 미달성자 알림 — 운영해보고 필요하면
- 영상 품질 평가 — 챌린지는 꾸준함이 목적
- 틱톡 단건 수집 — 링크만 저장으로 시작
