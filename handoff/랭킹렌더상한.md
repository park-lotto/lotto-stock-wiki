> **소유 트랙**: 랭킹렌더상한 — 이 파일은 이 트랙 세션만 수정한다. 다른 트랙은 읽기만.
> 트리거: "랭킹 렌더 이어서" / "채널당 상한 이어서" / "버퍼 이어서"

# 랭킹 화면 버퍼링 — 렌더 상한 + 채널당 상한 (2026-08-18)

사장님 제보: **"인스타에서 유튜브 넘어가면 버퍼가 엄청심해"**
→ **"체널을 이렇게 많이 노출을 시킬필요가"** → **"체널당 최신영상 2개까지만"**
→ **"검증된 체널들은 최신순이 맞는건데. 조회수터진것도 물론 필요하고 어찌해야지"**

## 원인 — 서버가 아니라 브라우저였다 (실측)

| | 카드 |
|---|---|
| 인스타 | 161장 |
| 유튜브 | **5,681장** (35배) |

서버 `/api/reference` 프로파일 = **0.14초** (load 0.06 · vision_tags 0.01 · durations 0.01 ·
json.dumps 0.06, 3.1MB) — **서버는 무죄**다.
`render()`의 `items.map(...)`에 상한이 없어 5,681장을 `innerHTML` 한 방에 만들고
이미지 5,681개가 동시에 걸린다(브라우저는 호스트당 6개 병렬 → 대기줄 947겹).

★어제 채널을 540 → 1,515개로 늘렸으니 **놔두면 더 심해질** 자리였다.

## 완료 (커밋 2개)

### `8160434fb` 렌더 상한 200장
- `RENDER_STEP=200` + `[더 보기]`로 200장씩 이어붙임(전체 건수 표시)
- ★탭·검색·필터가 바뀌면 상한 리셋(`render(opts.keepCap)`) — 안 하면 유튜브에서 늘린
  상한이 인스타 탭까지 따라가 같은 증상 재발
- 인스타(161장)는 상한에 안 걸려 종전과 동일

### `486be68c5` 채널당 2개 상한 (토글, 기본 켜짐)
- 한 채널이 최대 **53장** 도배(50장+ 채널만 19개) → **5,681 → 1,347장(-76%)**, 채널 888개 유지
- ★**어떤 2개인지는 정렬 탭이 정한다**(0순위-B). 정렬 뒤에 자르므로:
  `🕒 최신순 → 최신 2개` / `👁 조회수순 → 터진 2개`
  실측 **두 탭 겹침 62%(38% 다름)** → 하나로 못 박으면 나머지를 통째로 잃는다
- 순서 계약: **정렬 → 채널당 2개 → 렌더 상한 200장** (테스트로 못 박음)
- 접힌 건수 표시("채널당 2개만 (4,334건 접힘)") — 안 알리면 '사라졌다'로 읽힌다
- `perch`는 `STATE.filters`가 아닌 전용 플래그 + `renderFilterButtons` 가드
  (안 그러면 새로고침 때 꺼진 것처럼 보인다)

**검증**: 신규 11건 + 관련 406 passed · JS 문법 0오류
+ **실제 index.html 코드 블록을 추출해 라이브 5,681건으로 시뮬레이션**
  (최신순 1347 / 조회수 1347 / 토글끔 5681 · 채널 888 유지 · 접힘 4,334 정확)

최종: `5,681장 → [정렬] → [채널당 2개] 1,347 → [상한 200] 실제 200장` = **부하 28분의 1**

## ⏭ 다음

1. **★사장님 체감 확인** — 200장·2개가 적/과한지. 숫자만 바꾸면 된다
   (`RENDER_STEP` / `PER_CHANNEL_MAX`). 사장님 "해보고 내가 수정 불편하면"
2. **인스타에도 적용할지** — 지금은 161장이라 상한에 안 걸리지만, 채널당 2개는
   인스타에서도 도배 방지 효과가 있다(적용 여부는 체감 후 판단)
3. 접기 UI 고도화(옵션): 카드에 "이 채널 12장 ▾" 달아 그 자리에서 펼치기

## 함정
- **`finish`가 exit 0이어도 병합 안 됐을 수 있다** — 이번에 실제로 겪었다(출력이 통째로
  비어 있었고 `git branch -r --contains`로 확인하니 main에 없었다). 오늘처럼 세션이
  몰리면 게이트 락 대기가 길다. **판정은 반드시 `git log origin/main`으로.**
- **트랙 폴더에서 `git stash` 금지** — 다른 세션의 옛 stash가 pop돼 `mix_pipeline.py`에
  UU 충돌이 났다(내가 안 건드린 파일). 기준선 확인은 `git show`나 별도 폴더로.
- 필터 버튼 클래스는 `active`가 아니라 **`on`** 이다(기존 `.ftog` 규약).

## 관련
- `handoff/짤쇼핑카테고리.md` — 제품정체형 축·429 버그·채널 1,515개(같은 날 작업)
