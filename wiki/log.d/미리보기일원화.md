# 미리보기일원화 — 작업 로그

## 2026-08-21 (회사PC)

제작소 3단계 확정 미리보기가 **가조립 재생 뒤엔 안 보이던 것** 수리. 자리 스위치가 5곳에
흩어져 `startPreview`만 구멍이었다(`_sceneLabPrep`이 `display:none`으로 꺼두는데 되돌리는
코드가 없음) → 판정처 `_pvShow('play'|'video')` 하나로 모으고 호출부 4곳을 태웠다.
브라우저 실측으로 인과 증명: 옛 코드 로더 0×0 → 새 코드 440×782.

★**테스트가 통과했는데 기능은 안 먹은 사례**. iframe 재생기 숨김(Task 4)은 파일에 문자열이
있는지만 검사해 6건 green이었지만, 실제로는 `scene_lab.html:1526`이 `host.style.display=''`로
무조건 되살려 440×417로 보였다. 게다가 그 재생기는 **2026-08-16 사장님 배치 지시**로 둔
것이었다(주석에 명시) — 조사 때 부모 호출 경로만 보고 "아무도 안 쓴다"고 오판했다.
사장님 판단으로 Task 4 철회(revert), 1~3만 배포.

## 2026-08-22 (회사PC)

사장님 재지적("저걸 누르면 미리보기 재생했던 곳에서 하라니까")으로 방향 정정. 확정 결과를
부모 좌측 레일이 아니라 **장면편집 안 재생 자리**로 옮기고 `[가조립|확정본]` 탭으로 나눠
쓰게 했다. 통로는 `contentWindow` 직접 호출(postMessage는 08-15에 버린 경로), 레일은
`_RAIL_OFF`로 폴백 전용. 브라우저 실측 완료(로더·영상 426×757, 탭 전환 시 소리 정지).

★08-21에 "iframe 재생기는 08-16 배치 지시라 못 건드린다"고 철회했던 판단이 **틀렸다** —
그 지시가 바로 "장면 편집 안에서 재생하라"였다. 확정본도 거기 가는 게 맞았다.
★계획서 오류 3건을 구현 에이전트가 잡음: 폴백이 스스로 0×0이 되는 버그, 실패 경로 3곳
누락(⏳ 로더가 영원히 도는 것만 보였을 것), ⏳ 중복. **자리를 옮길 땐 성공 경로만 옮기면 안 된다.**

## 2026-08-22 — 3단계 레이아웃 압축 (Task 1~3)
- 확정 버튼(#mixPreviewCta)·매칭 진행바(MIX_STAGE_ORDER) 삭제 → **[자막+음성 완성본] 탭이
  렌더 입구**. #confirmBody가 빌 때만 렌더해 20초+ 유료 이중과금을 막는다(로더 HTML도 "안 빔").
- 아래 소스 카드 9:16 → 4:5(위 카드와 동일). 비율 정의처를 `--card-ar` 한 곳으로(0순위-B).
- iframe 150vh/1100px → `calc(100vh - 96px)`/560px — 화면보다 큰 상자라 **바깥이 먼저**
  길어지던 스크롤 두 겹 해소. 여백은 4/8 배수로만 축소(0으로 만들지 않음).
- ★함정: `_askRender`에 `_outerPlayer()`를 쓰면 안 된다(_RAIL_OFF=true라 늘 null → 렌더 영영 안 걸림).
- ★계획서 누락: 빈칸 거절(실패 경로)이 확정본 자리에 안 뜨면 "눌렀는데 무반응". `_pvFail` + 채워지면 걷기.
- 커밋 97236ec66 · 3868b4218. 전체 5062 passed(유일 실패 test_assembled_drafts는 변경 전 커밋도 동일 — 실측 증명).
- ⏭ 브라우저 실측은 사장님이 직접. 높이 값 근거는 `_SL_FRAME_H` 주석에.

## 2026-08-23 (회사PC)

담은 영상 ▶ "사라짐" 수리. 원인은 **렌즈 해시를 영상 ID로 착각**(`lens_instagram_quypyf`를
서버에 보냄 → ok:false). 실측 43/59건(73%)이 이 경우였고 전부 url은 갖고 있었다.
`_poolPlayId` 판정처 신설로 url에서 뽑게 함. 브라우저 실측: ▶ 클릭 5초 → readyState 4,
28.8초 영상 실제 재생(반사실도 확인 — 옛 ID는 ok:false).
카드 지표(조회수·댓글·팔로워)는 `_poolStat`·즐겨찾기 값이 이미 있었고 **넘기는 줄에서만**
빠져 있었다 → 3필드 추가.
★계획서 오류: 이미 있는 `_poolPlayFail`을 모르고 새 배너를 만들라고 해 UI가 두 겹 될 뻔했다.
