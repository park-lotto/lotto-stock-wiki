- 2026-08-30 (회사): 필름 자막띠 "안 맞음" 수리 라이브(204150968). 원인=자막 끝시각을 **다음 자막 시작으로 지어냄** → 말 사이 공백이 앞 자막에 먹힘(실측 +2.3/+3.0/+20.9초). 세그의 end를 그대로 쓰게 고침, 4개 전부 일치. 테스트 3건(js_harness). ⚠️게이트 오탐(기준선 443~643 춤춤·파이썬 17개 경합)이라 배포는 손으로 — 단 import·문법·테스트는 직접 확인
- 2026-08-30 (회사, 2차): 필름 **검정 칸** 수리 라이브(22ac40736). 원인=`loadedmetadata`(readyState 1=길이만 앎)만 기다리고 drawImage → 픽셀 없어 검정, 그게 **캐시에 구워져 영영 검정**. ★빠른 로컬선 재현 안 됨(CDP 400kbps+CPU4배 필요) — 실측 16칸 전부 검정 → 0, 네트워크 풀리면 60칸 자동 회복. canShoot(readyState>=2) 한 곳 판정, 못 그리면 **안 그린다**(빈칸은 회복, 캐시된 검정은 불가). filmframes도 동일+빈결과 캐시금지. 테스트 3건(26건 통과), 게이트 정상통과
