
## 2026-08-22
- [바꾸기] 길이 폭주 차단: 상한 `max(per*1.8, per+40)`이 **거의 언제나 +40이 이겨** 게이트가
  사실상 무력(10자 칸이 5배까지 허용). 1.35배 + 12자로 조이고 두 군데 중복식을
  `_beat_len_cap()` 하나로 통합. 프롬프트에 상한을 **숫자로** 명시(재시도가 성공하도록).
- "생성 실패(키 소진 또는 응답 오류)"가 거짓말이던 것 수정 — 로그에 429가 없는데도 그 문구가
  떠서 사장님이 키 회복을 헛되이 기다렸다. `_call_json`이 모든 실패를 `{}`로 뭉개던 것을
  사유별로 올려 6가지로 갈라 말한다. 회귀 0(인자 안 주면 종전 동작).
- 검증: 기준선 2 failed/5037 passed = 수정 후 동일. 라이브 DB는 reference.db임을 확인.
- (2차) [바꾸기] AI 자동생성을 맨 위로·크게 / 안 제목 16px 강조 / 보기 기본값 좌우.
  DB: 은폐형↔발명품형 제목 후킹이 `{제품군}` vs `{제품}` 한 글자 차이로 사실상 동일 → 발명품형 교체.
  나열 추천형은 status=pending으로 숨김(코드 무수정).
- ★함정: finish가 45분간 조용했던 건 게이트 결함이 아니라 **트랙 폴더 dirty**(`M finish_out.txt`,
  내용 동일·개행만 다름)로 **시작조차 안 한 것**. finish가 조용하면 로그의 `중단:` 줄부터 찾아라.
- (3차) 캡컷 완전 이관. ★캡컷·ZIP이 `primary`만 봐서 **화면의 3분의 1만** 나가던 것 수리
  (실측 19→7). 같은 병이 **세 군데**(타임라인·ZIP·**소스 복사**)였고 소스 복사 누락은
  E2E 돌리다 발견 — alternates 소스가 폴더에 없어 조각이 조용히 사라졌다.
  렌더 계획 블록을 `plan_beat_clips_for()`로 뽑아 셋이 공유(0순위-B).
- 미디어 보관함(`draft_materials`)을 빈 배열로 내보내던 것 채움 — 형식은 실제 캡컷
  프로젝트를 열어 실측. 조각·완성본·소스·TTS를 담고, 완성본은 보관함에만(타임라인에
  올리면 TTS와 겹쳐 두 번 들림). mp3는 metetype=music, 길이 따로 잼.
