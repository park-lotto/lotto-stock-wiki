# 핸드오프 — 쿠팡 상품 재료 선수집 (트랙 `쿠팡선수집`)

> 사장님 설계(2026-08-17): *"처음에 영상을 추출할 때 이미 정보를 뽑잖아. 그럼 그때 뒤에서
> 물건템이면 쿠팡 상세랑 리뷰까지 추출하는 게 어떤가. 그럼 대본 뽑을 때 시간이 절약되잖아.
> 레시피 뷰티는 빼고 상품 관련만."*

---

## ✅ 2026-08-16 집PC — 재료가 대본에 안 실리던 진짜 원인을 잡았다 (라이브)

핸드오프 ①번("고독스 C100으로 대본 뽑아 `쿠팡 재료` 라벨 확인")을 검증하러 들어갔다가
**재료가 있어도 영원히 못 찾는 버그**를 찾아 고쳤다. 커밋 `61d57bf8a` → 라이브 `1ca8d224a`.

**증상**: 고독스 C100 재료를 상세4·리뷰8까지 다 긁어
`product_facts_grab_douyin_b26e5b24ee36`에 저장해뒀는데 대본이 한 번도 못 찾았다.
화면엔 그냥 "재료 없음" — **조용한 실패**라 아무도 몰랐다.

**원인**(0순위-B 그대로 — 같은 판단이 두 군데, 서로 다르게):

| | 규칙 |
|---|---|
| 저장 (`_enqueue_prefetch`) | DB의 shortcode 그대로 → `grab_douyin_b26e5b24ee36` |
| 조회 (`_cache_keys_for_url`) | URL 정규식 추론 → 인스타·유튜브·틱톡 **셋뿐** |

도우인·샤오홍슈는 정규식에 아예 없어 **캐시 키가 0개**였다. 게다가 `grab_` 접두사와
짧은해시(`lens_tiktok_1jw6i6i`)는 URL에서 **원리적으로 유도 불가능**하다.

**고친 방법**: 추론하지 말고 **적힌 것을 읽는다.** `mix_basket`·`script_wiki`에
URL↔shortcode가 이미 짝으로 저장돼 있다 → `Store.shortcodes_for_url()` 신설,
`_cache_keys_for_url`이 DB를 먼저 본다. 정규식은 폴백으로 남겨 회귀 0.
**플랫폼이 늘어도 정규식을 다시 안 고쳐도 된다 = 썩지 않는다.**

**실측**(라이브 DB 담긴 항목 394건):

```
캐시 적중  130건 → 248건  (+118, 1.9배)   회귀 0건
새로 적중  인스타46 · 유튜브29 · 틱톡26 · 샤오홍슈14 · 도우인3
```

★**정규식이 있는 인스타·유튜브·틱톡조차** `grab_`·짧은해시 키는 못 찾고 있었다.
이 캐시 미스는 곧 **매번 Gemini 재추출**이라 비용·시간을 계속 흘리고 있었다.

**라이브 확인**: 서버에서 직접 실행 → 캐시키 `['grab_douyin_b26e5b24ee36']`,
재료 적중(스펙5·리뷰3). 서비스·워커3 재시작 완료.

**남은 것**
- ⏭ 화면에서 실제로 대본을 뽑아 **③ 라벨에 `쿠팡 재료`가 뜨는지** 육안 확인
  (배선·재료는 위에서 실측 끝. 남은 건 화면 눈으로 보는 것뿐)
- ⏭ 2단계 재료 상태 표시 UI(`⏳ 받는 중` / `✅ 준비됨`) — 미착수
- ⚠️ 검증 당시 최근 mix_job 60개에 고독스가 담긴 게 없었다(최신 job 08-15).
  라벨을 보려면 **1단계에서 그 영상을 담는 것부터** 해야 한다.

---

## 🏠 집에서 이어받기 — 이 3줄부터

```
cd <저장소 루트>\로또의 주식
git pull
py tools/track.py start 쿠팡선수집        # 폴더 없으면 생성(최신 main 기준)
트랙.bat 쿠팡선수집
```

### ⚠️ 집에서 **반드시** 해야 하는 것 — 릴레이 켜기

이 기능은 **사장님 PC에서 릴레이가 돌아야만** 동작한다(쿠팡이 서버를 막는다, 아래 §1).

```
py scripts/coupang_relay_client.py
```

- 토큰은 `.env`의 `COUPANG_RELAY_TOKEN`. **`.env`는 git에 안 올라간다** —
  집 PC에 없으면 서버에서 꺼내 넣어라:
  ```
  ssh -i <키> ubuntu@43.200.48.69 "grep '^COUPANG_RELAY_TOKEN=' /etc/shopping-shorts.env"
  ```
- 켜졌는지 확인: `GET /api/coupang/relay/status` → `"online": true`
- **창을 닫으면 멈춘다.** 꺼져 있으면 일감이 큐에 남아 대기할 뿐 아무것도 안 깨진다.

---

## 1. 왜 이 구조인가 — 실측이 길을 정했다 (두 번 바뀜)

**① 서버는 쿠팡을 못 긁는다**

| 경로 | 결과 |
|---|---|
| 서버 직결 (AWS 서울 = **한국 IP**, `ipinfo.io/country` → KR) | **403** |
| 서버 + 독일 주거용 프록시(기존 기록) | 403 |
| 사장님 PC + 헤드풀 Chrome | 200 |

한국 IP인데도 막힌다 → **IP가 아니라 브라우저 지문**을 본다. `collect_raw`가 Playwright로
실제 Chrome을 띄우는 이유다. 그래서 기존 릴레이(PC가 서버에 롱폴링)를 쓴다.

**② PC가 분석까지 하려니 키가 없다**

처음엔 PC가 제미니 분석까지 하게 짰는데 돌려보니 `제미니 키 0개`로 멈췄다 —
분석 키(`SHORTS_GEMINI_KEY`)는 **서버 `/etc/shopping-shorts.env`에만** 있다.
키를 PC로 복사하면 관리 지점이 둘이 된다(0순위-B) → **PC는 긁기만, 분석은 서버**.

## 2. 흐름 (전 구간 실동작 확인 완료)

```
1단계 담기 → 영상 분석 끝
   ↓ (홈템·기타만) 제품명으로 큐잉        ← app.py _queue_product_prefetch
릴레이(PC)가 물어감
   ↓ 쿠팡 검색 → 1위 상품 → 상세·리뷰 Playwright 수집
   ↓ 이미지 긴 변 1400px JPEG로 축소 → base64 전송(상한 12MB, nginx 20m)
서버
   ↓ _analyze_relay_raw → product_facts.analyze (제미니)
   ↓ 캐시 저장  product_facts_<shortcode>
2단계 대본 생성 → _prefetched_facts_for_job이 꺼내 프롬프트에 실음
```

**검색어**: 1단계 태깅의 `source_brief.product`. 3단계 `affiliate_target`을 기다릴 필요가
없다 — 태깅이 이미 제품명을 뽑는다(`script_extract.py:92`).

**렌즈 승격**(유료 SerpApi, **한 번만**): 이름을 못 잡았거나 검색이 **빈손**일 때만
화면 역검색으로 이름을 다시 잡는다. 상품은 찾았는데 분석만 빈 경우는 이름 문제가
아니므로 재시도하지 않는다.

**무자막 해외영상도 대상**: 전사가 비면 적재가 `failed_empty`로 끝나지만(빈 대본 캐시
방지 — 그 판정은 옳고 안 건드렸다) **화면 분석은 됐다**. 재료가 제일 아쉬운 게 그런
영상이라 포함한다. 영상 자체를 못 받은 상태(`failed_download`·`skipped_latched`·
`failed_limit`)만 제외.

## 3. 끝단 실측 (2026-08-17, 무자막 도우인 실제 영상)

```
담기 → [product_prefetch] 큐잉: 고독스 C100 감독용 뷰파인더 겸 측광기 (기타 / grab_douyin_b26e5b24ee36)
     → 쿠팡: GODOX 고독스 C100 투명 뷰파인더 카메라
     → 상세 4장 · 리뷰 8건 → 356KB 전송 → 서버 분석 → 상태 done
```

저장된 재료(**자막 한 줄 없는 영상에서**):
- `specs` 모델명 Godox C100 / 순수 광학 뷰파인더 / HUD 투명 디스플레이 / 표시 데이터 / 사진·비디오
- `satisfy` 감성사진 찍기 좋음 / 옛날 하두리 느낌 / 잘만 찍으면 깨끗
- `complaint` 안경닦이로 렌즈를 닦고 찍어야 함

★렌즈를 안 썼다 — 태깅만으로 브랜드·모델까지 잡혔다(유료 호출 0).

## 4. 이 작업에서 잡힌 진짜 버그 3건

1. **`app.py`에 `import json`이 아예 없었다.** `NameError`가 `except`에 삼켜져 **조용히
   빈 값**이 나오고 있었다 — 테스트가 잡았다.
2. **릴레이가 윈도우 콘솔에서 시작하자마자 죽었다.** cp949가 `—`를 못 찍어 첫 `print`에서
   `UnicodeEncodeError`(exit 1). → 출력 스트림 UTF-8 + `errors="replace"`.
   (특수문자를 빼는 건 답이 아니다 — 상품명·리뷰에 어떤 글자가 올지 모른다)
3. **릴레이를 배포 후 재시작 안 해 옛 코드로 돌았다**(내 실수). 그 탓에 첫 실측이
   `제미니 키 0개`로 헛돌았다 → 그 계기로 **재시도 API**를 만들었다(아래).

## 5. 운영 도구

**선수집 재시도** — 선수집은 담기 때 한 번만 걸린다(쿠팡 소프트 차단 방지).
릴레이가 꺼져 있었거나 실패한 건은 이걸로 되살린다.

```
curl -X POST http://127.0.0.1:8849/api/coupang/relay/prefetch_retry \
  -H 'Content-Type: application/json' \
  -d '{"shortcode":"<코드>","token":"<COUPANG_RELAY_TOKEN>"}'
```

⚠️ 경로가 `/api/coupang/relay/` 아래인 이유: 그 접두사가 미들웨어 인증 예외이고
**엔드포인트가 토큰을 검사**하는 기존 규칙이 있다. 인증 방식을 새로 만들지 않았다.

**상태 확인**
```python
Store(DB_PATH).get_setting("product_prefetch_<코드>")   # queued|lens|done|empty
Store(DB_PATH).get_setting("product_facts_<코드>")      # {"facts":…, "product":…}
```

## ⏭ 남은 것

1. **⑧ 마지막 구간 미확인** — 2단계에서 그 영상(`grab_douyin_b26e5b24ee36`)으로 대본을
   뽑아 **재료 표시줄에 `쿠팡 재료`가 뜨는지 + 문장에 specs·complaint가 반영되는지** 확인.
   (①~⑦은 실동작 확인 완료)
2. **2단계 재료 상태 표시 UI 미착수** — `⏳ 상품 재료 받는 중` / `✅ 준비됨(스펙 N·리뷰 N)`.
   재료가 실제로 대본에 실리는 걸 확인한 뒤에 붙이기로 했다.
3. **고객용으로는 이 구조가 안 통한다** — 릴레이는 사장님 PC다. 고객 서비스로 가려면
   한국 출구(주거용 프록시)나 다른 방법이 필요하다. 지금은 사장님 전용.
4. 릴레이가 꺼져 있을 때 큐에 쌓인 일감은 **서버 재시작으로 사라진다**(메모리 큐).
   실제로 문제가 되면 DB 큐로 올릴 것 — 지금은 재시도 API로 충분.
