# 속도점검 — 박현옥님 "전체적으로 느려졌다" (2026-09-18)

## 라이브 반영 완료 (main `6a95ab9a4`, 서버 pull·재시작·실측)
| | 전 | 후 |
|---|---|---|
| 웹 메인 스레드 CPU | 99.9% | 0.0% (10초 간격 3회) |
| 첫 페이지 응답(서버 안) | 평균 0.46초 / 최대 0.79 | 평균 0.02 / 최대 0.04 |
| 메인 스레드가 하는 일(py-spy) | `_auth_guard → Store() → _init_schema` | `select` 대기 |

## 뿌리 (전부 실측)
서버 자체는 놀고 있었다(16코어 부하 4, 메모리 23G 여유, 디스크 61%, 스틸 0). 문제는 웹 프로세스 하나.
1. **요청마다 DB 스키마 초기화** — `Store.__init__`이 매번 `_init_schema`(1,274줄·SQL 116개)를 돌렸고 1회 가드가 없었다.
   `Store()`는 app.py에 419곳, 인증 미들웨어만 요청당 2번 → 요청 하나에 SQL 232개가 **이벤트 루프에서**.
   코드 주석 "소규모 스케일에선 허용"(Opus 리뷰 P1)이 고객 11명 동시 접속에서 깨졌다.
   → `Store._schema_ready`(프로세스당 경로당 1회, lock). 419곳 안 고치고 전부 빨라짐. 테스트용 `Store.reset_schema_cache()`.
2. **죽은 썸네일 폭주** — 한 고객(59.15.1.245, 제작소 job 90a2d24edf02)이 10분에 `/api/thumb` 2,563건, 그중 2,505건 404.
   404 한 번에 oembed + 유튜브 대체 규격 여러 번(각 6초 제한) **바깥 조회** → 스레드풀(40)이 죽은 주소로 꽉 참.
   브라우저 캐시는 못 쓴다(404가 `no-store`인 이유: 복구 뒤 카드가 까맣게 남던 사고) → 서버가 5분 기억(`_THUMB_NEG`).
   ⏭ **클라이언트 쪽 원인 미수정**: 제작소 화면이 상태 폴링마다 썸네일 목록을 다시 그려 같은 404를 재요청한다(produce.html).

## 감시 — "누가 말해줘야 작동이 되면 안 되잖아"
기존 5분 크론 `capacity_watch`에 `web_latency_ms`·`web_main_cpu` 표본 + `server_verdict()` 즉시 판정 추가.
danger(디스크<30GB / 응답>1000ms / 메인CPU>85%) → `ops_alert.raise_alert(server_disk|server_slow)` 텔레그램+쪽지(30분 쿨다운, signature로 재도배 방지).
실측 1회 실행: 42.6ms / 0.0% / `ok`. ★그전엔 용량 verdict가 화면용이라 디스크 100%(09-16)도 경보가 안 갔다.

## 알려진 기준선(제 회귀 아님)
- `test_capacity_watch` verdict 5건, `test_produce_thumb_ui`·`test_produce_thumb_font_load`(node WinError 6) — origin/main에서도 실패.
- 테스트 5곳이 "같은 프로세스에서 `Store()` 재생성 = 재기동"을 가정 → `reset_schema_cache()` 삽입으로 유지.

## ⏭ 다음
- 제작소 화면 썸네일 재요청 루프(폴링마다 목록 재렌더) 수정 — 서버 5분 기억은 완화일 뿐.
- `[apiwatch] danger`가 떠 있다 — 어떤 키인지 확인(이준연님 "렌즈 검색 다 썼다"와 연관 가능, 09-18 아침 미처리).
- 디스크 반복 점유 주인(/tmp/abtest·/tmp/ab 사본) 세션 확인 — 감시로 이제 잡히긴 한다.

## 추가(같은 날 오후) — 관측판에 "누구 때문에 기다리나" 두 칸 (main `93903e896`, 라이브·브라우저 확인)
사장님 "100명 기준 서버인데 4개밖에 렌더가 안 되나". 실측으로 갈라 본 것:
- 동시 상한은 **워커 14개**(`SHORTS_WORKERS=14`). 4개가 최대가 아니다. 단 렌더 4개면 부하 14/16 → **전속력은 4~5개**.
- **'계정당 동시 1개'**(07-30 공평 분배, 관리자 2개) 때문에 같은 고객의 두 번째 작업은 규칙상 대기한다.
  14:25 실측: 진행 1·대기 1이 **둘 다 고객 204**. 서버 탓 아님.
- 14일: 렌더 1,158건·고객 57명·하루 83건, 동시 최대 8, 피크 시간당 15건/고객 7명. "대기 100시간"은 규칙 대기와
  진짜 혼잡이 섞여 있는데 표본에 고객 구분이 없어 못 갈랐다 → **그래서 칸을 추가했다.**
- `capacity_samples.run_customers`(동시에 도는 서로 다른 고객) · `wait_customers`(자기 작업이 하나도 안 도는데 기다리는 고객).
  ops.html 카드 2개 + 날짜 표 2열(동시 고객 ≥5 / 남 때문에 대기 >0 이면 강조). 라이브 첫 표본: 1명 / **0명**.
- ⏭ **일주일 뒤 판단**: '동시 고객 최대'가 5를 자주 넘거나 '남 때문에 대기'가 자주 뜨면 서버 사양 업(같은 서버를 큰 요금제로;
  서버 추가는 큐·파일이 로컬 디스크라 코드 공사가 큼). 그 전엔 돈 쓸 근거가 없다. Lightsail 상위 요금제·가격은 미확인.
- 박선정님(cid 174) 작업 5건 정상, 완성 3편 mean_volume −16dB(무음 아님).
- ⏭ `[apiwatch] danger` = elevenlabs **무음 폴백 7건**(09-18 12:41:45~49, cid 0, job 없음) — 재시작 직후 4초에 몰린 시스템 호출로
  보이나 **어떤 시작 루틴인지 미특정**. 09-16 16:28 UTC에도 같은 패턴. 재시작 직후 고객 작업이 TTS를 부르면 무음이 나갈 위험.
