# 쿠팡검색 — 카드 판독 근거

## ✅ 2026-09-06 (집PC) — "살 물건 없음"의 진짜 원인은 프롬프트가 아니었다

### 사장님 제보
> "쿠팡검색에서 살물건없음이 왜이렇게 많은가" (스크린샷: 7개 중 5개가 회색)
> → "레시피도 완성품과 재료 도구를 나오게 / 제품도 안나오는것 빨리 수정"

### ① 프롬프트 수정 (커밋 `16efa93ee`, 라이브)
`_SHOP_PROMPT`가 "청소·수납 **방법**만 알려주는 영상"·"순수 정보 전달"을 빈 문자열
조건으로 둬서 레시피·세탁 영상이 통째로 걸러졌다. 모델의 `why`가 증거를 줬다 —
*"정보성 영상으로, 특정 상품을 홍보하지 않아…"*. 지시대로 거부한 것이다.
```
카테고리별 '살 물건 없음':  레시피 82.9% · 뷰티 56.8% · 홈템 27.4% · 전체 28.0%
```
→ 레시피는 완성품/재료/조리도구를, 세탁·청소는 세제·도구를 답하게 했다.
→ **"정보성 영상이라서"는 빈 문자열의 이유가 못 된다**고 명시했다.
→ 캐시 629건을 비워 재판독시켰다(값 있던 1,665건은 보존).

### ★② 그런데 화면은 거의 안 바뀐다 — 진짜 병목은 데이터였다 (커밋 `8eae4cd27`)
프롬프트 A/B에서 55% 개선을 보고 배포했는데 **실제 함수로는 8%**였다.
12건이 **4초**에 끝난 게 단서 — 비전 모델을 부르면 그럴 수 없다. **안 부른 것**이었다.
```
재판독 대기 3,191건
  ★인스타 썸네일 만료 + 캡션 없음 = 2,771건 (87%)  ← 근거 0, 모델 호출 자체가 불가
  caption 원본이 빈 것            2,870건 (92%)
```
**★내 A/B 표본이 틀렸다**: `LENGTH(caption)>40`으로 골랐는데 실제는 92%가 비어 있다.
표본 조건이 모집단과 다르면 개선율은 거짓말이다 — **진짜 호출 경로로 재라.**

**고친 것**
- `fetch_thumb_bytes`에 보관함(`shopping_shorts/data/thumbs`, gitignore). 받아올 때 남기고
  CDN이 죽으면 보관본으로 답한다. 판독 5곳이 전부 이 함수를 거쳐 한 곳만 고치면 된다.
- `identify_shop_many(out_no_evidence=[])` → 근거 0인 카드를 따로 알려준다.
  서버가 `no_evidence`로 내려보내고 화면은 **"자료 만료"**로 표시(살 물건 없음과 갈랐다).
- 테스트 7개, **둘 다 사보타주로 검증**(보관본 읽기 제거 / no_evidence 전달 차단 → 각각 빨개짐).

⚠️ **이미 만료된 2,771건은 못 살린다** — URL이 죽어 지금 받아도 실패. 사장님 판단:
   "과거는 두고 어쩔수없으니 앞으로 진행해". **앞으로 담는 것만** 지킨다.

### ✅ 배포 실측 (서버 배포본으로 직접 실행)
```
보관함:    10,012 → 10,016장  (+4장 실제 저장)
자료 만료:  인스타 4건이 정확히 no_evidence로 분류
판독:      유튜브에서 '멜라민 스펀지' 회수
```
커밋 `8eae4cd27` + `6ccc4559e`(긴급) → main 병합, 서버 HEAD `f9685c944` 확인.

### ★배포 중 발견해 고친 것 — `data/thumbs`는 이미 주인이 있었다
그 폴더는 **2026-07-18부터 영상 프레임·그리드**가 쓰던 곳이다(실측 2GB, 1만 장).
거기 보관함을 같이 담으면 나중에 정리하다 서로 지운다 → **`data/thumb_cache`로 분리**.
⚠️ 처음에 "보관함이 벌써 2GB"로 오독했다 — 남의 파일이었고 내 보관본은 4장이었다.
   **폴더를 새로 쓸 땐 이미 쓰는 사람이 있는지 먼저 봐라.**

### ✅ 화면까지 실측 확인 완료 (2026-09-06 17:40)
```
/sidebar.js  HTTP 200 · 96,405바이트 · "자료 만료" 포함 · 디스크와 md5 동일
```
⚠️ **`/static/sidebar.js`로 확인하지 마라 — 404다.** 정적 파일은 루트 마운트라
   올바른 주소는 `/sidebar.js`. 이걸 몰라 "반영 안 됨"으로 30분 오판했다.
   **grep 전에 HTTP 코드와 바이트 수를 먼저 봐라**(0바이트에 grep하면 영원히 0).

### ⏭ 남은 것 (회사에서 이어받을 것)

**1순위 — 캡션이 92% 비어 있다** ← 여기가 진짜 큰 건이다
```
재판독 대기 3,191건 중 caption 원본이 빈 것 2,870건 (92%)
  · caption_body()가 걸러낸 건 11건뿐 = 함수 문제가 아니다
  · 수집 시점에 안 담는 것으로 보인다 → 수집 코드를 봐야 한다
```
썸네일이 만료돼도 **캡션만 있으면 판독된다**(실측: 캡션만으로 '말차가루'·'크림치즈' 회수).
즉 캡션을 담기 시작하면 앞으로분 판독률이 한 번 더 크게 오른다.
착수: `reel_history.caption`을 채우는 수집 경로를 찾아 왜 비는지 실측.

**2순위 — 보관함 용량 정책**
```
지금 무제한. 평균 198KB → 1만 장이면 2GB. 서버 여유는 339GB라 급하진 않다.
정할 것: 상한(예: 30GB) + 오래된 것부터 정리(mtime 기준) 또는 그냥 두기.
```

**보류 — 과거분 2,771건**
사장님 판단: "과거는 두고 어쩔수없으니 앞으로 진행해". URL이 죽어 회복하려면
인스타 재수집이 필요하다(계정·프록시 비용). 필요해지면 그때 산정.


## ✅ 2026-09-05 (회사PC) — 캡션을 근거로 얹기 + 버튼 '쿠팡검색' · 커밋 `b7de8f8ab`

### 사장님 제보
> "쿠팡 버튼 이름은 쿠팡검색으로 바꾸고 / 지금 단어뽑은거 너무일반화된 키워드고
> 이거 대본을 안보면 정확한 내용을 뽑을수가 없어서 아까 작업을한거같은데"

스크린샷: 버튼에 `🛒 벽선반 쿠팡?` · `🛒 청소용 스펀지 쿠팡?` · `🛒 프라이팬 쿠팡?`

### 진짜 원인 (실측 — 사장님 진단이 맞았다)
프리워밍(`/api/coupang/identify_batch`)이 **썸네일 이미지만** 서버로 보내고 있었다.
캡션은 카드에 이미 그려져 있는데(`index.html:2217`) 요청 본문이
`{shortcode, thumbnail}` 두 개뿐이라 **버려졌다**.

게다가 그게 쓰던 `product_name._PROMPT`는 이렇게 지시한다:
> ★가장 중요: 화면에 크게 박힌 **자막·문구·타이포그래피는 완전히 무시하라**

이건 **08-04에 "같은 제품 영상 모으기"용**으로 만든 것이다. 그쪽은 `same_product`가
핵심어 하나만 겹쳐도 같다고 보므로 범주어("벽선반")가 **오히려 맞다**.
쿠팡 검색은 정반대로 **살 물건 하나를 특정**해야 한다 — 목적이 다른 함수를
그대로 빌려 쓴 것이 진짜 원인이다.

★근거 우선 판독(`_coupang_evidence`, app.py:4895)은 **09-04에 이미 만들어져 있었다**.
프리워밍만 옛 썸네일 경로에 남아 있었다 — 전형적인 **0순위-B**(같은 판단이 두 군데).
사장님이 "아까 작업을 한 것 같은데"라고 하신 게 정확했다.

### 넣은 것
| 파일 | 내용 |
|---|---|
| `coupang_query.py` | `caption_body()` 신설 — 캡션에서 해시태그·상투구를 뗀 본문. **규칙은 여기 한 곳**(프리워밍·클릭 경로 둘 다 이걸 쓴다) |
| `product_name.py` | `identify_shop_many()` + `_SHOP_PROMPT` 신설. 썸네일과 캡션을 **한 번의 호출**에 같이 준다 |
| `store.py` | `vision_tags.shop_product`·`shop_product_at` 컬럼 + `save_shop_product`/`shop_products_map` |
| `app.py` | `identify_batch`가 caption 수신 → `identify_shop_many` 호출. `_coupang_evidence`도 `caption_body` 경유 |
| `static/sidebar.js` | 프리워밍이 caption 실어 보냄. 라벨 `쿠팡?` → `쿠팡검색`, 툴팁 "썸네일에서" 문구 제거 |
| `static/index.html`·`collection.html` | 호출부가 caption 전달 + 기본 라벨 `🛒 쿠팡검색` |
| `tests/test_coupang_caption_evidence.py` | 신규 12건 |

### 왜 대본을 미리 안 뽑나 (사장님과 확인한 것)
| | 모델 호출 | 시간 | 고객 비용 |
|---|---|---|---|
| 프리워밍(캡션 얹기) | **1회 (종전과 동일)** | +0초 | **0** |
| 🎬 영상 보고 정확히 | 2회(영상업로드+판독) | 20~60초 | **0.1P** (`pricing.OP_SCRIPT`) |

대본 추출은 **영상 파일을 통째로 업로드**한다. `app.py:2633~`에 대본에만 붙은
방어막이 그 증거다 — `check_and_count` · `_charge_or_402` · `_global_over_cap` ·
실패 시 `refund_credit`. 카드 48장에 미리 돌릴 물건이 아니다.
→ 대본이 필요한 카드는 기존 `🎬 영상 보고 정확히` 버튼으로 **1장씩 선택**.

### 함정 (다음 사람이 밟지 말 것)
- ⚠️ **`product_name.identify_many`의 프롬프트·캐시를 재사용하지 마라.**
  그건 아카이브 유사도(`app.py:19716`·`:19774`)와 `product_backfill`이 같이 쓴다.
  `vision_tags.product`에 쿠팡용 이름을 쓰면 **묶기 기준이 조용히 바뀐다**.
  그래서 `shop_product`로 칸을 갈랐다(테스트로 못 박음).
- ⚠️ **`hook_harvest.clean_hook_candidate`를 재사용하지 않았다** — 저건 **첫 줄만**
  본다(훅 한 줄이 목적). 제품명은 2~3번째 줄에 오는 경우가 많다(실측: 첫 줄이
  "🧷댓글 남겨주시면 최저가 링크…"이고 제품 얘기는 그 아래).
- ⚠️ **호출부 배선을 빠뜨리면 아무 변화가 없다.** 서버·모델을 아무리 고쳐도
  `index.html`·`collection.html`이 caption을 안 실으면 도착을 안 한다. 실제로
  두 곳 다 `{shortcode, thumbnail}`만 보내고 있었다.
- ⚠️ 로컬 `last_run`의 shortcode가 **전체 URL**이라(`https://…/p/XXX/`)
  `identify_batch`의 `os.path.basename`이 빈 문자열을 만든다 → 로컬 테스트에서
  0건이 나온다. **기존 동작이고 내 변경과 무관**하지만 테스트할 때 헷갈린다.
- ⚠️ 한글을 손으로 다시 치지 마라 — 패치 스크립트에서 '캡션'을 두 번 오타냈다.
  **원본에서 정규식으로 뽑아 조립**하는 방식으로 해결(scratchpad/patch_ev4.py).

### 검증 (실측)
- 신규 **12 passed** / 관련 스위트 **75 passed**(아카이브 유사도 포함, 회귀 0)
- ★**사보타주 5종 전부 빨개짐** — 캡션 미전송 / 해시태그 미제거 / 묶기용 칸 오염 /
  캡션전용 카드 누락 / 판정실패 캐시. 빈 단언이 아님을 확인.
- 실브라우저(headless Chromium, 로컬 :8931):
  - 카드 버튼 **7개 전부 `🛒 쿠팡검색`**
  - `identify_batch` 요청에 **caption 7/7건 실림**(종전 0건)
  - 판독 응답이 오면 `🛒 3단 조립식 벽선반 쿠팡검색`으로 갱신
  - **pageerror 0**
- 캡션 본문 회수율(로컬 `last_run` 10건): 해시태그·상투구 떼고 **6/10건** 남음

### ⚠️ 한계 — 이걸로 다 해결되지 않는다
실측 10건 중 캡션이 **제품 특정에 실제로 도움되는 건 2~3건**이다:

| 캡션 본문 | 판정 |
|---|---|
| "슬라이더를 밀어서 쓰는 방식… 지우개" | ✅ 확실 |
| "폰에 꽂기만 하면… 노래방 마이크" | ✅ 확실 |
| "냉장고만 열면 뭐있는지 모르겠고" | ⚠️ 문제만 말함, 제품명 없음 |
| "세제를 물티슈에 묻혀 방충망을…" | ⚠️ **방법 영상 — 팔 물건이 없음** |
| "계양구청 근처 점심 맛집" | ❌ **음식점 — 쿠팡 대상 아님** |

즉 **근거 부족만이 원인이 아니다** — 애초에 팔 물건이 없는 영상이 섞여 있다.
캡션이 없으면 종전과 똑같이 썸네일로 답하므로 **나빠지지는 않는다**.
⚠️ 표본이 **10건**이고 로컬 옛 수집분이다. 라이브 비율은 다를 수 있다.

### 게이트가 한 번 막았다 (2026-09-05) — 커밋 `b308761f6`
`finish`가 **기존 테스트 2건**을 잡아 병합을 버렸다(라이브 무사). 둘 다 옛 동작을
못 박은 단언이라 새 동작으로 갱신하되 **더 조이는 쪽**으로 고쳤다.

★이 과정에서 **가짜 단언을 세 번** 만들었다 — 사보타주가 아니었으면 전부 통과했다:
| 시도 | 왜 가짜였나 |
|---|---|
| `"쿠팡검색" in html` | 다른 버튼 문구에 걸려 **라벨을 되돌려도 통과** |
| `"caption:" in html` | 이 파일에 `caption:`이 **9번** 나온다(담기·아카이브 등 무관한 곳) |
| 창을 좁힌 뒤 `"caption" in _win` | **내가 쓴 주석**에 "caption"이 있어 통과 |

최종: cp-btn 정규식 / 프리워밍 **호출부**(`ssCoupangPrewarm(`) 주변 창 /
**주석 줄 제외** 후 `caption\s*:` 속성 검사. 사보타주 3종 전부 빨개짐 확인.

⚠️ **사보타주 자체도 틀릴 수 있다.** 처음엔 정규식이 `🛒 쇼핑몰`(첫 매치)과
   collection의 다른 표기를 겨눠 아무것도 안 바꿔놓고 "가드가 없다"고 오판할 뻔했다.
   → **사보타주가 진짜 그 코드를 바꿨는지 먼저 확인**하라(`new != s` 단언).

⚠️ heredoc에서 `\n`이 실제 개행으로 풀려 테스트 파일을 두 동강 냈다(또 밟았다).
   → 패치는 **스크래치패드 파일로 써서 실행**하라(메모리 `★heredoc \n 붕괴`).

## ✅ 2026-09-05 (2차) — '살 물건 없음' 회색 처리 · 커밋 `2ad194c24`

사장님 "해봐". 맛집·방법영상까지 그냥 '쿠팡검색' 버튼이라 헛클릭이 나던 것.

### 무엇이 문제였나
판독은 **이미** 이런 영상에 빈 문자열로 답하고 있었다(`_SHOP_PROMPT`에 박아둠).
그런데 화면이 `if (!name) return;`으로 **조용히 건너뛰어** 아무 표시가 없었다.
실측 10건 중 **4건**이 이런 영상이다(방충망 닦는 법·점심 맛집 등).

### ★같이 잡은 버그 — 캡션만 있는 카드가 **브라우저에서** 잘리고 있었다
프리워밍 필터가 `it.thumbnail &&`를 요구해, 1차 작업에서 서버·모델에 캡션 경로를
붙여놓고도 **캡션만 있는 카드는 요청에 실리지도 않았다**.
→ 배선 누락은 **층마다** 확인해야 한다. 서버만 고치면 안 된다(1차 때 이미 한 번
   같은 함정을 적어놨는데 필터라는 다른 층에서 또 났다).

### 넣은 것
| 파일 | 내용 |
|---|---|
| `app.py` | batch 응답에 `no_product` 추가. ⚠️`products`의 기존 규약(`""`=없음)은 **그대로 두고 덧붙이기만** — 옛 호출부 보호 |
| `sidebar.js` | 빈 제품명 → `🛒 살 물건 없음` 회색(opacity .45 + grayscale). 회색 버튼 클릭 시 판독 재실행 없이 이유 안내. 필터를 `thumbnail \|\| caption`으로 |
| `index.html`·`collection.html` | 버튼이 `noProduct`를 모달에 전달 |
| `tests/test_coupang_no_product.py` | 신규 7건 |

**버튼을 없애지는 않았다** — 판독이 틀렸을 때 사장님이 직접 찾을 길은 남긴다.

### 검증 (실측)
- 신규 **7** + 관련 스위트 **78 passed**(회귀 0)
- ★**사보타주 6종 전부 빨개짐**: 서버 no_product 제거 / 조용히 건너뛰기(옛 동작) /
  회색 미표시 / 모달 noProduct 무시 / 버튼 미전달 / 캡션 카드 자르기(옛 버그)
- ⚠️처음엔 `grayscale **or** opacity`로 써서 사보타주를 못 잡았다(하나만 남아도 통과).
  둘 다 요구하도록 고쳐 재확인 — **이 세션에서 가짜 단언 4번째**다.
- 실브라우저(로컬 :8933): 제품 있음 = 주황 굵게 / 없음 = 회색(실측 opacity `0.45`,
  filter `grayscale(1)`), 클릭 시 "팔 만한 물건이 안 보입니다" 안내, **pageerror 0**.
  ★그 '없음' 카드는 **캡션만 있는 카드**였다 — 종전이면 판독조차 안 됐다.
- 스크린샷으로 두 상태 대비 확인

### ⏭ 남은 것
1. **사장님 육안 확인** — 라이브에서 버튼이 `🛒 쿠팡검색`으로 뜨는지, 판독된
   제품명이 종전보다 구체적인지(예: "벽선반" → "3단 조립식 벽선반").
   ⚠️ 이미 판독된 카드는 **옛 캐시(`shop_product`가 아직 없음)**라 새로 판독된다.
2. **라이브 데이터로 캡션 회수율 재측정** — 로컬 10건은 표본이 너무 작다.
   회사PC는 DB가 비어 있고 SSH 포트22가 막혀(타임아웃) 못 쟀다.
3. ~~"쿠팡 대상 아님" 화면 표시~~ → **완료**(2차, `2ad194c24`). 회색 처리 + 클릭 시 안내.
4. 유튜브·틱톡 카드의 `caption`에 뭐가 담기는지 **미확인** — 인스타 기준으로만 봤다.
