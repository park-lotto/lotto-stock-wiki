# 자막싱크 2단계 음성 프리뷰 구현 계획

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 제작소 3단계(TTS)에서 "이 음성으로 전체 생성" 뒤, 비트별 프레임+실음성+자막을 같이 재생해 TTS가 자연스러운지·자막이 맞는지 확인하고 딸깍으로 고치는 음성 프리뷰를 만든다.

**Architecture:** 새 렌더 경로 없이 기존 조각을 잇는다 — 프레임=`/api/mix/beatframe`, 음성=`/api/mix/tts`, 비트데이터=`/api/mix/result`. 신규는 (a) 자막 수동 보정값 `cap_offset`을 최종 자막 굽기(`_burn_captions`)까지 흘리는 seam, (b) 그 값을 저장하는 라우트, (c) 비트 하나만 톤/성우 바꿔 재생성+재동기하는 라우트, (d) `renderTtsBeats`를 프리뷰로 업그레이드하는 프론트. 최종 렌더는 재합성 없이 비트의 `tts_path` mp3를 재사용하므로 비트 하나를 덮어쓰면 최종까지 그대로 간다.

**Tech Stack:** Python/FastAPI(app.py), ffmpeg 필터(video_assemble.py), mix_pipeline.py, 바닐라 JS(static/produce.html), pytest + TestClient + 정적 문자열 assert(프론트).

## Global Constraints

- 편집은 전부 `C:\Users\TheRose\Desktop\로또의 주식\.tracks\자막싱크\...` 절대경로. `.py`는 LF. main 폴더 코드 무접촉.
- **새 렌더 경로 금지** — `assemble`/`_burn_captions` 단일 경로에 `cap_offset`만 더한다.
- **`cap_offset` 미설정(기본 0.0) → 1단계와 자막 출력 바이트 동일**(회귀 0). 테스트로 잠근다.
- **프리뷰 = 최종 WYSIWYG** — 프론트 자막 구절은 서버가 내려준 `cap_segments`(= `_caption_segments(narration)`)만 쓴다. 프론트에서 분할 재구현 금지.
- **자막 문구/줄바꿈/구절 경계(`_caption_segments`) 불변** — 오직 표시 시각만 바뀐다.
- produce.html 편집은 **`#ttsBeats` 블록 + 신규 함수/라우트로만** 국한(4트랙 동시편집). 성우 변경은 영상 전체(비트별 성우 혼용 안 함).
- git은 트랙 폴더로: `git -C .tracks/자막싱크 add … && commit`. 커밋 전 `git -C .tracks/자막싱크 status --porcelain`으로 내 트랙 파일만 확인. **git stash 금지**(워크트리 공유 스택).
- 로컬 GROQ/ElevenLabs 키 0개 → regen·ASR 재동기 실경로는 서버에서만 관측(테스트는 monkeypatch).

---

### Task 1: `cap_offset` — 자막 수동 보정값을 최종 굽기까지 흘리는 seam

**Files:**
- Modify: `shopping_shorts/video_assemble.py` — `_caption_drawtexts`(263), `_beat_timeline`(653), `_burn_captions`(707-709)
- Test: `shopping_shorts/tests/test_caption_offset.py` (신규)

**Interfaces:**
- Produces: `_caption_drawtexts(narration, dur, work, idx, t0=0.0, style=None, real_durs=None, cap_offset=0.0)` — cap_offset(초)을 각 구절/바 enable 시각에 더한다. `_beat_timeline` 각 항목에 `"cap_offset": beat.get("cap_offset", 0.0)`. `_burn_captions`가 `cap_offset=b.get("cap_offset", 0.0)` 전달.

- [ ] **Step 1: Write the failing test**

`shopping_shorts/tests/test_caption_offset.py`:
```python
from shopping_shorts import video_assemble as va


def _starts(parts):
    """drawtext 필터들에서 between(t,START,END)의 START만 뽑는다(바 제외)."""
    import re
    out = []
    for p in parts:
        if "drawbox" in p:
            continue
        m = re.search(r"between\(t,([\d.]+),", p)
        if m:
            out.append(float(m.group(1)))
    return out


def test_cap_offset_zero_is_byte_identical():
    n = "여러분 오이 절대 냉장고에 그냥 두지 마세요 이 방법은 진짜 좋아요"
    base = va._caption_drawtexts(n, 6.0, None, 0, t0=2.0)
    same = va._caption_drawtexts(n, 6.0, None, 0, t0=2.0, cap_offset=0.0)
    assert base == same


def test_cap_offset_shifts_all_caption_starts():
    n = "여러분 오이 절대 냉장고에 그냥 두지 마세요 이 방법은 진짜 좋아요"
    base = _starts(va._caption_drawtexts(n, 6.0, None, 0, t0=2.0))
    pushed = _starts(va._caption_drawtexts(n, 6.0, None, 0, t0=2.0, cap_offset=0.3))
    assert len(base) == len(pushed) and len(base) >= 2
    for b, p in zip(base, pushed):
        assert abs((p - b) - 0.3) < 0.01   # 모든 구절이 +0.3초


def test_cap_offset_clamps_start_nonnegative():
    n = "여러분 오이 절대 두지 마세요"
    pulled = _starts(va._caption_drawtexts(n, 6.0, None, 0, t0=0.0, cap_offset=-5.0))
    assert all(s >= 0.0 for s in pulled)   # 당겨도 음수 시각 없음
```

- [ ] **Step 2: Run test to verify it fails**

Run: `python -m pytest shopping_shorts/tests/test_caption_offset.py -v`
Expected: FAIL — `test_cap_offset_shifts_all_caption_starts` (cap_offset 인자 없음 → TypeError), 나머지도 수집 단계 통과 후 실패.

- [ ] **Step 3: Add `cap_offset` to `_caption_drawtexts`**

`_caption_drawtexts` 시그니처를 바꾼다(263줄):
```python
def _caption_drawtexts(narration, dur, work, idx, t0=0.0, style=None, real_durs=None, cap_offset=0.0):
```
바 enable(290-293줄)을 cap_offset만큼 민다:
```python
        _bs = max(0.0, t0 + cap_offset)
        parts.append(
            f"drawbox=x=0:y=ih-{_BAR_H}:w=iw:h={_BAR_H}:color=black@0.82:t=fill:"
            f"enable='between(t,{_bs:.2f},{t0 + dur + 0.5 + cap_offset:.2f})'"
        )
```
구절 start/end(296·298줄)에 cap_offset을 더하고 start는 0으로 클램프:
```python
        start = max(0.0, t + t0 + cap_offset)
        t += d
        end = (dur + 0.5 if i == len(segs) - 1 else t) + t0 + cap_offset
```
(cap_offset=0.0이면 `t0 + 0.0`의 `.2f`가 기존과 동일 → 바이트 동일.)

- [ ] **Step 4: Thread `cap_offset` through `_beat_timeline` and `_burn_captions`**

`_beat_timeline`의 timeline dict(671-678줄)에 필드 추가:
```python
            "cap_durs": beat.get("cap_durs"),
            "cap_offset": beat.get("cap_offset", 0.0),
```
`_burn_captions`의 호출(708-709줄)에 인자 추가:
```python
        filters.extend(_caption_drawtexts(b["narration"], b["dur"], work, b["beat_idx"],
                                          b["t0"], caption_style, real_durs=b.get("cap_durs"),
                                          cap_offset=b.get("cap_offset", 0.0)))
```

- [ ] **Step 5: Run tests to verify they pass**

Run: `python -m pytest shopping_shorts/tests/test_caption_offset.py -v`
Expected: PASS (3/3)

- [ ] **Step 6: Mutation check — 바이트 동일이 진짜인지**

`_caption_drawtexts`에서 `start = max(0.0, t + t0 + cap_offset)`의 `+ cap_offset`을 잠깐 지우면 `test_cap_offset_shifts_all_caption_starts`가 죽어야 한다(죽으면 테스트 유효). 확인 후 되돌린다.

- [ ] **Step 7: Regression + commit**

Run: `python -m pytest shopping_shorts/tests/ -k "caption or beat_timeline or naturalize" -q`
Expected: 기존 통과 수 유지(신규 실패 0).
```bash
git -C .tracks/자막싱크 add shopping_shorts/video_assemble.py shopping_shorts/tests/test_caption_offset.py
git -C .tracks/자막싱크 commit -m "feat(자막싱크): cap_offset — 자막 수동 보정값을 _burn_captions까지 흘림(0=바이트동일)"
```

---

### Task 2: `/api/mix/result`에 `cap_segments` 추가 (WYSIWYG 원천)

**Files:**
- Modify: `shopping_shorts/app.py` — `/api/mix/result` 응답 빌더(1291-1294)
- Test: `shopping_shorts/tests/test_result_cap_segments.py` (신규)

**Interfaces:**
- Consumes: `video_assemble._caption_segments(narration) -> list[str]`
- Produces: `/api/mix/result`의 각 beat에 `"cap_segments": list[str]`(= `_caption_segments(narration)`), 그리고 `{**b}`로 이미 실리는 `cap_durs`·`cap_offset`.

- [ ] **Step 1: Write the failing test**

`shopping_shorts/tests/test_result_cap_segments.py`:
```python
from shopping_shorts import video_assemble as va


def test_result_builder_adds_cap_segments(monkeypatch):
    from shopping_shorts import app as appmod

    narr = "여러분 오이 절대 냉장고에 그냥 두지 마세요"

    class FakeStore:
        def __init__(self, *a, **k): pass
        def get_mix_job(self, job_id):
            return {"edit_plan": {"structure": "hook", "beats": [
                {"beat_idx": 0, "narration": narr, "role": "hook"}]}}
    monkeypatch.setattr(appmod, "Store", FakeStore)

    res = appmod.api_mix_result("job1")
    beat = res["beats"][0]
    assert beat["cap_segments"] == va._caption_segments(narr)
    assert len(beat["cap_segments"]) >= 1
```
(⚠️ 구현 전 `api_mix_result`의 실제 함수명·시그니처를 `grep -n "def api_mix_result\|/api/mix/result" app.py`로 확인하고 이름을 맞춘다. 다르면 테스트의 `appmod.api_mix_result("job1")`를 실제 함수명으로 고친다.)

- [ ] **Step 2: Run test to verify it fails**

Run: `python -m pytest shopping_shorts/tests/test_result_cap_segments.py -v`
Expected: FAIL — `KeyError: 'cap_segments'`.

- [ ] **Step 3: Add `cap_segments` in the beat loop**

`app.py`의 result 빌더 루프(1292-1294)를 바꾼다:
```python
    from shopping_shorts.video_assemble import _caption_segments
    beats = []
    for b in plan["beats"]:
        beats.append({**b, "plagiarism_flag": b["beat_idx"] in flags,
                      "tts_preview_url": f"/api/mix/tts/{job_id}/{b['beat_idx']}",
                      "cap_segments": _caption_segments(b.get("narration", ""))})
```
(import는 함수 상단이나 파일 상단 기존 import 블록에 둔다 — 순환 import 없음: video_assemble은 app을 import 안 함.)

- [ ] **Step 4: Run test to verify it passes**

Run: `python -m pytest shopping_shorts/tests/test_result_cap_segments.py -v`
Expected: PASS

- [ ] **Step 5: Regression + commit**

Run: `python -m pytest shopping_shorts/tests/ -k "result or mix" -q`
Expected: 신규 실패 0.
```bash
git -C .tracks/자막싱크 add shopping_shorts/app.py shopping_shorts/tests/test_result_cap_segments.py
git -C .tracks/자막싱크 commit -m "feat(자막싱크): /api/mix/result에 cap_segments(= _caption_segments) — 프리뷰 WYSIWYG 원천"
```

---

### Task 3: `POST /api/mix/caption_offset/{job}/{beat_idx}` — 보정값 저장

**Files:**
- Modify: `shopping_shorts/app.py` — 신규 라우트(기존 `/api/mix/tts` 라우트 1438 근처)
- Test: `shopping_shorts/tests/test_caption_offset_route.py` (신규)

**Interfaces:**
- Consumes: `Store.get_mix_job(job_id)`, `Store.update_mix_job(job_id, edit_plan=plan)`
- Produces: `POST /api/mix/caption_offset/{job_id}/{beat_idx}` body `{"offset": float}` → 해당 beat의 `cap_offset` 저장, `{"ok": True, "offset": <clamped>}`. 범위 밖 job/beat → 404. offset는 [-2.0, 2.0]로 클램프.

- [ ] **Step 1: Write the failing test**

`shopping_shorts/tests/test_caption_offset_route.py`:
```python
def test_caption_offset_saved_on_beat(monkeypatch):
    from shopping_shorts import app as appmod
    saved = {}

    class FakeStore:
        def __init__(self, *a, **k): pass
        def get_mix_job(self, job_id):
            return {"edit_plan": {"beats": [
                {"beat_idx": 0, "narration": "가"}, {"beat_idx": 1, "narration": "나"}]}}
        def update_mix_job(self, job_id, **fields):
            saved.update(fields)
    monkeypatch.setattr(appmod, "Store", FakeStore)

    res = appmod.api_mix_caption_offset("job1", 1, {"offset": 0.3})
    assert res["ok"] is True and abs(res["offset"] - 0.3) < 1e-9
    beat1 = saved["edit_plan"]["beats"][1]
    assert abs(beat1["cap_offset"] - 0.3) < 1e-9


def test_caption_offset_clamped_and_404(monkeypatch):
    from shopping_shorts import app as appmod
    from starlette.responses import JSONResponse

    class FakeStore:
        def __init__(self, *a, **k): pass
        def get_mix_job(self, job_id):
            return {"edit_plan": {"beats": [{"beat_idx": 0, "narration": "가"}]}}
        def update_mix_job(self, job_id, **fields): pass
    monkeypatch.setattr(appmod, "Store", FakeStore)

    clamped = appmod.api_mix_caption_offset("job1", 0, {"offset": 99})
    assert clamped["offset"] == 2.0                      # 상한 클램프
    missing = appmod.api_mix_caption_offset("job1", 5, {"offset": 0.1})
    assert isinstance(missing, JSONResponse) and missing.status_code == 404
```

- [ ] **Step 2: Run test to verify it fails**

Run: `python -m pytest shopping_shorts/tests/test_caption_offset_route.py -v`
Expected: FAIL — `AttributeError: module ... has no attribute 'api_mix_caption_offset'`.

- [ ] **Step 3: Implement the route**

`app.py`에서 `/api/mix/tts` 라우트 근처에 추가:
```python
@app.post("/api/mix/caption_offset/{job_id}/{beat_idx}")
def api_mix_caption_offset(job_id: str, beat_idx: int, body: dict):
    """비트 자막의 수동 시각 보정값(초)을 저장. 최종 _burn_captions가 읽어 반영."""
    offset = max(-2.0, min(2.0, float(body.get("offset") or 0.0)))
    store = Store(DB_PATH)
    job = store.get_mix_job(job_id)
    if not job or not job.get("edit_plan"):
        return JSONResponse(status_code=404, content={"ok": False, "error": "작업 없음"})
    plan = job["edit_plan"]
    beat = next((b for b in plan["beats"] if b["beat_idx"] == beat_idx), None)
    if beat is None:
        return JSONResponse(status_code=404, content={"ok": False, "error": "비트 없음"})
    beat["cap_offset"] = offset
    store.update_mix_job(job_id, edit_plan=plan)
    return {"ok": True, "offset": offset}
```

- [ ] **Step 4: Run test to verify it passes**

Run: `python -m pytest shopping_shorts/tests/test_caption_offset_route.py -v`
Expected: PASS (2/2)

- [ ] **Step 5: Regression + commit**

Run: `python -m pytest shopping_shorts/tests/ -k "caption or mix" -q`
Expected: 신규 실패 0.
```bash
git -C .tracks/자막싱크 add shopping_shorts/app.py shopping_shorts/tests/test_caption_offset_route.py
git -C .tracks/자막싱크 commit -m "feat(자막싱크): POST caption_offset — 비트 자막 수동보정값 저장(±2초 클램프)"
```

---

### Task 4: 비트 하나 톤/성우 바꿔 재생성 + 자막 재동기

**Files:**
- Modify: `shopping_shorts/mix_pipeline.py` — 신규 `resynth_one_beat`(파일 끝, `resynth_tts_job` 근처)
- Modify: `shopping_shorts/app.py` — 신규 라우트 `POST /api/mix/tts/{job}/{beat_idx}/regen` + import에 `resynth_one_beat` 추가
- Test: `shopping_shorts/tests/test_resynth_one_beat.py` (신규)

**Interfaces:**
- Consumes: `_synthesize_beats`가 쓰는 per-beat 블록(synthesize_line → tts_path → asr_check.transcribe_words → caption_sync.phrase_durs_from_words), `Store.get_mix_job/update_mix_job`.
- Produces: `mix_pipeline.resynth_one_beat(job_id, beat_idx, voice_override, db_path, work_root) -> None` — 그 비트만 `voice_override`(voice dict)로 재합성해 **같은 `beat_{idx}.mp3`에 덮어쓰고**, `beat["tts_path"]`·`beat["cap_durs"]`·`beat["voice_override"]`를 갱신, plan 저장. 라우트: `POST /api/mix/tts/{job_id}/{beat_idx}/regen` body `{"voice_id","settings","speed"}` → 렌더 중이 아니면 백그라운드 실행, `{"ok": True}`; 렌더 중이면 409.

- [ ] **Step 1: Write the failing test**

`shopping_shorts/tests/test_resynth_one_beat.py`:
```python
from pathlib import Path
from shopping_shorts import mix_pipeline as mp


def test_resynth_one_beat_overwrites_only_target(monkeypatch, tmp_path):
    calls = {"synth": [], "asr": 0}

    def fake_synth_line(narr, out, **kw):
        Path(out).write_bytes(b"MP3"); calls["synth"].append((narr, kw.get("voice")))
    monkeypatch.setattr(mp, "synthesize_line", fake_synth_line)
    monkeypatch.setattr(mp, "_probe_duration", lambda p: 3.0)
    monkeypatch.setattr(mp.asr_check, "transcribe_words", lambda p: [{"word": "가", "start": 0.0, "end": 3.0}])
    monkeypatch.setattr(mp.caption_sync, "phrase_durs_from_words", lambda n, w, d: [3.0])

    plan = {"beats": [
        {"beat_idx": 0, "narration": "가나", "cap_durs": None},
        {"beat_idx": 1, "narration": "다라", "cap_durs": None}]}

    class FakeStore:
        def __init__(self, *a, **k): pass
        def get_mix_job(self, j): return {"edit_plan": plan, "status": "ready_for_review"}
        def update_mix_job(self, j, **f): pass
    monkeypatch.setattr(mp, "Store", FakeStore)

    ov = {"voice_id": "V2", "settings": {"stability": 0.2}, "speed": 1.0}
    mp.resynth_one_beat("job1", 1, ov, "db", str(tmp_path))

    # 비트 1만 재합성
    assert len(calls["synth"]) == 1 and calls["synth"][0][0] == "다라"
    assert calls["synth"][0][1] == ov              # voice_override로 합성
    b1 = plan["beats"][1]
    assert b1["cap_durs"] == [3.0]                  # 재동기됨
    assert b1["voice_override"] == ov               # 저장됨
    assert b1["tts_path"].endswith("beat_1.mp3")
    assert "voice_override" not in plan["beats"][0]  # 비트0 무접촉
```

- [ ] **Step 2: Run test to verify it fails**

Run: `python -m pytest shopping_shorts/tests/test_resynth_one_beat.py -v`
Expected: FAIL — `module 'shopping_shorts.mix_pipeline' has no attribute 'resynth_one_beat'`.

- [ ] **Step 3: Implement `resynth_one_beat`**

`mix_pipeline.py` 끝(`resynth_tts_job` 뒤)에 추가:
```python
def resynth_one_beat(job_id, beat_idx, voice_override, db_path, work_root):
    """비트 하나만 voice_override로 재합성해 같은 mp3에 덮어쓰고 자막을 재동기한다.
    최종 렌더는 재합성 없이 이 mp3(beat['tts_path'])를 재사용하므로 교정이 그대로 남는다."""
    store = Store(db_path)
    job = store.get_mix_job(job_id)
    if not job or not job.get("edit_plan"):
        return
    plan = job["edit_plan"]
    beat = next((b for b in plan["beats"] if b["beat_idx"] == beat_idx), None)
    if beat is None:
        return
    total = len(plan["beats"])
    i = next(k for k, b in enumerate(plan["beats"]) if b["beat_idx"] == beat_idx)
    work = Path(work_root) / job_id
    tts_dir = work / "tts"
    tts_dir.mkdir(parents=True, exist_ok=True)
    out = tts_dir / f"beat_{beat_idx}.mp3"
    try:
        synthesize_line(
            beat["narration"], out, voice=voice_override, beat_role=beat.get("role"),
            beat_index=i, beat_total=total,
            previous_text=plan["beats"][i - 1]["narration"] if i > 0 else None,
            next_text=plan["beats"][i + 1]["narration"] if i < total - 1 else None,
        )
        beat["tts_path"] = str(out)
        beat["voice_override"] = voice_override
        beat["cap_durs"] = None
        words = asr_check.transcribe_words(str(out))
        if words:
            beat["cap_durs"] = caption_sync.phrase_durs_from_words(
                beat["narration"], words, _probe_duration(str(out)))
        store.update_mix_job(job_id, edit_plan=plan)
    except Exception as e:
        traceback.print_exc(file=sys.stderr)
```

- [ ] **Step 4: Run test to verify it passes**

Run: `python -m pytest shopping_shorts/tests/test_resynth_one_beat.py -v`
Expected: PASS

- [ ] **Step 5: Add the route (rendering guard)**

`app.py` import 라인의 mix_pipeline import에 `resynth_one_beat` 추가(예: `from shopping_shorts.mix_pipeline import ... resynth_one_beat`). `/api/mix/tts` 라우트 근처에 추가:
```python
@app.post("/api/mix/tts/{job_id}/{beat_idx}/regen")
def api_mix_tts_regen(job_id: str, beat_idx: int, body: dict, background_tasks: BackgroundTasks):
    """비트 하나만 톤/성우 바꿔 재생성(+자막 재동기). 렌더 중이면 거부(P1-9 방지)."""
    store = Store(DB_PATH)
    job = store.get_mix_job(job_id)
    if not job or not job.get("edit_plan"):
        return JSONResponse(status_code=404, content={"ok": False, "error": "작업 없음"})
    if job.get("status") in ("rendering", "removing_subtitles"):
        return JSONResponse(status_code=409, content={"ok": False, "error": "렌더 중에는 재생성할 수 없어요"})
    override = {"voice_id": body.get("voice_id"), "settings": body.get("settings"),
               "speed": body.get("speed")}
    background_tasks.add_task(resynth_one_beat, job_id, beat_idx, override, DB_PATH, _MIX_WORK_DIR)
    return {"ok": True}
```
(⚠️ `status` 값 문자열 `"rendering"`/`"removing_subtitles"`가 실제 status 코드와 맞는지 `grep -n 'status=' mix_pipeline.py`로 확인. 다르면 실제 렌더 상태값으로 교체. 확인 안 되면 가드는 넣되 주석으로 표시.)

- [ ] **Step 6: Regression + commit**

Run: `python -m pytest shopping_shorts/tests/ -k "resynth or mix or tts" -q`
Expected: 신규 실패 0.
```bash
git -C .tracks/자막싱크 add shopping_shorts/mix_pipeline.py shopping_shorts/app.py shopping_shorts/tests/test_resynth_one_beat.py
git -C .tracks/자막싱크 commit -m "feat(자막싱크): resynth_one_beat + regen 라우트 — 비트 하나 톤/성우 재생성+재동기(렌더중 거부)"
```

---

### Task 5: produce.html — `renderTtsBeats`를 프리뷰로 (🟢🟡 + 프레임+자막 재생)

**Files:**
- Modify: `shopping_shorts/static/produce.html` — `renderTtsBeats`(2470), `playAllBeats`(2512) 및 `#ttsBeats` 마크업
- Test: `shopping_shorts/tests/test_voice_preview_js.py` (신규, 정적 문자열 assert — 기존 `test_preview_stepper_js.py` 방식)

**Interfaces:**
- Consumes: `/api/mix/result`의 beat 필드 `narration`·`tts_preview_url`·`cap_segments`·`cap_durs`·`cap_offset`, 프레임 `/api/mix/beatframe/{job}/{i}`.
- Produces: 각 비트 행에 🟢(cap_durs 있음)/🟡(없음) 배지 + `id="beatRow{i}"` 유지. 상단에 9:16 프레임 미리보기 `id="vpFrame"`(img) + 자막 오버레이 `id="vpCap"`. `playAllBeats`가 재생 중 비트의 프레임을 `vpFrame`에, 그 비트 `cap_segments`를 `cap_durs`(+cap_offset) 타이밍으로 `vpCap`에 순차 표시. 재생한 비트는 `_vpHeard`(Set)에 기록.

- [ ] **Step 1: Write the failing test**

`shopping_shorts/tests/test_voice_preview_js.py`:
```python
from pathlib import Path
HTML = (Path(__file__).resolve().parents[1] / "static" / "produce.html").read_text(encoding="utf-8")

def test_status_badge_from_cap_durs():
    # 🟢/🟡를 cap_durs 유무로 판정하는 코드가 있다
    assert "cap_durs" in HTML
    assert "🟢" in HTML and "🟡" in HTML

def test_preview_frame_and_caption_elems():
    assert 'id="vpFrame"' in HTML       # 9:16 프레임
    assert 'id="vpCap"' in HTML         # 자막 오버레이
    assert "/api/mix/beatframe/" in HTML

def test_uses_server_cap_segments_not_client_split():
    # 자막 구절은 서버 cap_segments를 쓴다(프론트에서 _caption_segments 재구현 금지)
    assert "cap_segments" in HTML

def test_heard_tracking_exists():
    assert "_vpHeard" in HTML           # 들어본 비트 기록(게이트용)
```

- [ ] **Step 2: Run test to verify it fails**

Run: `python -m pytest shopping_shorts/tests/test_voice_preview_js.py -v`
Expected: FAIL — `vpFrame`/`_vpHeard`/🟢 등 미존재.

- [ ] **Step 3: Upgrade `renderTtsBeats` markup**

`renderTtsBeats`(2470-2494) 안에서 행 생성과 컨테이너를 바꾼다. 행(`rows`, 2478-2482)에 상태 배지를 넣고(서버 cap_segments/cap_durs를 dataset에 실어 재생기가 읽게):
```javascript
    const rows=beats.map((b,i)=>{
      const synced = Array.isArray(b.cap_durs) && b.cap_durs.length;
      const badge = synced ? '🟢' : '🟡';
      const badgeTip = synced ? '자막 자동맞춤 됨' : '자막 자동맞춤 불확실 — 들어보세요';
      return `
      <div id="beatRow${i}" data-idx="${i}" style="border:1px solid var(--line);border-radius:9px;padding:9px;margin-bottom:6px;background:#0d1420;transition:background .15s">
        <div style="font-size:13px;margin-bottom:4px"><span title="${badgeTip}">${badge}</span> <b>${i+1}.</b> ${esc(b.narration||'')}</div>
        ${b.tts_preview_url?`<audio id="beatAudio${i}" preload="metadata" src="${esc(b.tts_preview_url)}?t=${Date.now()}" style="display:none"></audio><button class="btn-prev" style="padding:4px 10px" onclick="vpPlayBeat(${i})">▶ 이 장면</button>`:'<span class="hint">음성 없음</span>'}
      </div>`;
    }).join('');
```
컨테이너(2484-2491)에 9:16 프레임 미리보기 블록을 맨 위에 넣고 `cap_segments`/`cap_durs`/`cap_offset`를 전역에 보관:
```javascript
    VP_BEATS = beats;                 // 전역: 재생기가 cap_segments/cap_durs/cap_offset 읽음
    _vpHeard = _vpHeard || new Set();
    el.innerHTML=`
      <div style="display:flex;gap:12px;flex-wrap:wrap;align-items:flex-start">
        <div style="position:relative;width:180px;aspect-ratio:9/16;background:#000;border-radius:10px;overflow:hidden;flex:0 0 auto">
          <img id="vpFrame" style="width:100%;height:100%;object-fit:cover" onerror="this.style.display='none'">
          <div id="vpCap" style="position:absolute;left:0;right:0;bottom:8%;text-align:center;color:#fff;font-weight:700;font-size:13px;text-shadow:0 2px 4px #000;padding:0 6px"></div>
        </div>
        <div style="flex:1;min-width:220px">
          <div style="display:flex;align-items:center;gap:12px;flex-wrap:wrap;margin-bottom:8px">
            <div><span class="hint">영상 길이</span> <b style="color:var(--accent2)">${vidLen.toFixed(1)}초</b></div>
            <div><span class="hint">음성 길이</span> <b id="voiceLen" style="color:var(--ok)">…</b></div>
            <div id="fitMsg" class="hint">음성 길이 측정 중…</div>
            <button onclick="playAllBeats()" class="btn-prev" style="padding:6px 12px;margin-left:auto">▶ 전체 재생</button>
          </div>
          ${rows}
        </div>
      </div>`;
    _updateVoiceLen(beats.length, vidLen);
```
파일에 전역 선언 추가(스크립트 상단, 다른 `let` 근처):
```javascript
let VP_BEATS=[]; let _vpHeard=new Set();
```

- [ ] **Step 4: Implement the karaoke player**

`playAllBeats`(2512)를 프레임+자막까지 굴리도록 교체하고, 단일 비트 재생 `vpPlayBeat`를 추가한다. `_caption_durations`와 같은 규칙은 서버 cap_durs를 쓰되, 없으면 글자수 비례로 근사(프리뷰 표시용 — 최종은 서버가 정확히 함):
```javascript
function _vpShowFrame(i){
  const f=document.getElementById('vpFrame');
  if(f && typeof MIX_JOB!=='undefined' && MIX_JOB){ f.style.display='block'; f.src='/api/mix/beatframe/'+MIX_JOB+'/'+i; }
}
function _vpCapDurs(b){
  const segs=b.cap_segments||[]; if(!segs.length) return [];
  if(Array.isArray(b.cap_durs) && b.cap_durs.length===segs.length) return b.cap_durs.slice();
  const w=segs.map(s=>Math.max(1,(s||'').replace(/\s/g,'').length)); const tw=w.reduce((a,c)=>a+c,0);
  return w.map(x=>x/tw);   // 표시용 근사(합=1, 오디오 길이에 맞춰 스케일)
}
// 오디오 currentTime에 맞춰 그 비트 자막 구절을 순차 표시(cap_offset 반영)
function _vpDriveCaption(aud, b){
  const cap=document.getElementById('vpCap'); if(!cap) return;
  const segs=b.cap_segments||[]; const rel=_vpCapDurs(b);
  const off=(+b.cap_offset||0);
  const upd=()=>{
    const dur=aud.duration||rel.reduce((a,c)=>a+c,0)||1;
    const durs=rel.map(r=> (Array.isArray(b.cap_durs)&&b.cap_durs.length)? r : r*dur);
    let t=aud.currentTime-off, acc=0, cur=segs[0]||'';
    for(let k=0;k<segs.length;k++){ if(t>=acc){ cur=segs[k]; } acc+=durs[k]; }
    cap.textContent=cur;
  };
  aud.ontimeupdate=upd; upd();
}
function vpPlayBeat(i){
  const b=VP_BEATS[i]; if(!b) return;
  const a=document.getElementById('beatAudio'+i); if(!a) return;
  // 다른 오디오 정지
  let k=0; while(document.getElementById('beatAudio'+k)){ const o=document.getElementById('beatAudio'+k); if(o!==a){o.pause();} k++; }
  _vpShowFrame(i); _vpDriveCaption(a,b); _vpHeard.add(i);
  const r=document.getElementById('beatRow'+i);
  document.querySelectorAll('[id^=beatRow]').forEach(x=>x.style.background='#0d1420');
  if(r) r.style.background='#1a2740';
  a.currentTime=0; a.play().catch(()=>{});
}
function playAllBeats(){
  const auds=[]; let i=0; while(document.getElementById('beatAudio'+i)){ auds.push(document.getElementById('beatAudio'+i)); i++; }
  if(!auds.length) return;
  auds.forEach(a=>{ a.pause(); a.currentTime=0; a.onended=null; });
  let cur=0;
  const playOne=()=>{
    if(cur>=auds.length){ document.querySelectorAll('[id^=beatRow]').forEach(x=>x.style.background='#0d1420'); return; }
    vpPlayBeat(cur);
    auds[cur].onended=()=>{ cur++; playOne(); };
  };
  playOne();
}
```

- [ ] **Step 5: Run tests to verify they pass**

Run: `python -m pytest shopping_shorts/tests/test_voice_preview_js.py shopping_shorts/tests/test_preview_stepper_js.py -v`
Expected: 신규 4/4 PASS, 기존 스텝퍼 테스트도 여전히 PASS(공용 스텝퍼 무접촉).

- [ ] **Step 6: Commit**

```bash
git -C .tracks/자막싱크 add shopping_shorts/static/produce.html shopping_shorts/tests/test_voice_preview_js.py
git -C .tracks/자막싱크 commit -m "feat(자막싱크): TTS 프리뷰 — 🟢🟡 + 프레임+자막 가라오케 재생(서버 cap_segments/cap_durs)"
```

---

### Task 6: produce.html — 고치기 버튼 + 부드러운 게이트

**Files:**
- Modify: `shopping_shorts/static/produce.html` — 비트 행에 고치기 UI, 다음-단계 게이트
- Test: `shopping_shorts/tests/test_voice_preview_fix_js.py` (신규, 정적 문자열 assert)

**Interfaces:**
- Consumes: Task 3 `POST /api/mix/caption_offset/{job}/{beat_idx}`, Task 4 `POST /api/mix/tts/{job}/{beat_idx}/regen`, `_vpHeard`(Task 5).
- Produces: 선택된 비트에 `vpNudge(i, dir)`(자막 당기기/밀기 ±0.1초), `vpResetOffset(i)`(되돌리기), `vpRegenTone(i)`(톤 드롭다운→regen), `vpGateNext()`(안 들어본 🟡 있으면 confirm 1회 후 다음 단계).

- [ ] **Step 1: Write the failing test**

`shopping_shorts/tests/test_voice_preview_fix_js.py`:
```python
from pathlib import Path
HTML = (Path(__file__).resolve().parents[1] / "static" / "produce.html").read_text(encoding="utf-8")

def test_fix_functions_exist():
    for fn in ["function vpNudge", "function vpResetOffset", "function vpRegenTone", "function vpGateNext"]:
        assert fn in HTML, fn

def test_nudge_calls_offset_route():
    assert "/api/mix/caption_offset/" in HTML

def test_regen_calls_regen_route():
    assert "/regen" in HTML and "/api/mix/tts/" in HTML

def test_gate_checks_heard_yellow():
    # 안 들어본 🟡가 있으면 confirm으로 한 번 묻는다(막지 않음)
    assert "_vpHeard" in HTML and "confirm(" in HTML

def test_nudge_step_is_point_one():
    assert "0.1" in HTML   # ±0.1초 스텝
```

- [ ] **Step 2: Run test to verify it fails**

Run: `python -m pytest shopping_shorts/tests/test_voice_preview_fix_js.py -v`
Expected: FAIL — `vpNudge` 등 미존재.

- [ ] **Step 3: Add fix controls to each beat row**

Task 5의 `rows` 템플릿에서 각 행 하단에 고치기 버튼 줄을 추가한다(음성 있을 때만). `▶ 이 장면` 버튼 뒤에 이어붙임:
```javascript
        ${b.tts_preview_url?`
        <div style="margin-top:6px;display:flex;gap:6px;flex-wrap:wrap;align-items:center">
          <button class="btn-prev" style="padding:3px 8px" onclick="vpNudge(${i},-1)">◀ 자막 당기기</button>
          <button class="btn-prev" style="padding:3px 8px" onclick="vpNudge(${i},1)">자막 밀기 ▶</button>
          <button class="btn-prev" style="padding:3px 8px" onclick="vpResetOffset(${i})">↩︎ 되돌리기</button>
          <select id="vpTone${i}" style="padding:3px"><option value="stable">안정</option><option value="natural">자연</option><option value="expressive">표현</option><option value="whisper">속삭임</option></select>
          <button class="btn-prev" style="padding:3px 8px" onclick="vpRegenTone(${i})">🔁 톤 바꿔 다시</button>
          <span id="vpMsg${i}" class="hint"></span>
        </div>`:''}
```

- [ ] **Step 4: Implement the fix handlers**

`vpPlayBeat` 근처에 추가한다:
```javascript
async function vpNudge(i,dir){
  const b=VP_BEATS[i]; if(!b) return;
  b.cap_offset=Math.max(-2,Math.min(2, (+b.cap_offset||0) + dir*0.1));
  const a=document.getElementById('beatAudio'+i); if(a) _vpDriveCaption(a,b);   // 즉시 미리보기 반영
  const m=document.getElementById('vpMsg'+i); if(m) m.textContent='자막 '+(b.cap_offset>=0?'+':'')+b.cap_offset.toFixed(1)+'초';
  try{ await fetch('/api/mix/caption_offset/'+MIX_JOB+'/'+b.beat_idx,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({offset:b.cap_offset})}); }catch(e){}
}
async function vpResetOffset(i){
  const b=VP_BEATS[i]; if(!b) return; b.cap_offset=0;
  const a=document.getElementById('beatAudio'+i); if(a) _vpDriveCaption(a,b);
  const m=document.getElementById('vpMsg'+i); if(m) m.textContent='자동값으로 되돌림';
  try{ await fetch('/api/mix/caption_offset/'+MIX_JOB+'/'+b.beat_idx,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({offset:0})}); }catch(e){}
}
async function vpRegenTone(i){
  const b=VP_BEATS[i]; if(!b) return;
  const tone=document.getElementById('vpTone'+i).value;
  const v=_voiceForTone(tone);   // 현재 성우의 그 톤 variant → {voice_id,settings,speed}
  if(!v){ const m=document.getElementById('vpMsg'+i); if(m) m.textContent='이 성우는 그 톤이 없어요'; return; }
  const m=document.getElementById('vpMsg'+i); if(m) m.textContent='🔁 다시 만드는 중…';
  try{
    await fetch('/api/mix/tts/'+MIX_JOB+'/'+b.beat_idx+'/regen',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(v)});
    setTimeout(()=>{ renderTtsBeats(); }, 4000);   // 재생성+재동기 후 새로고침
  }catch(e){ if(m) m.textContent='❌ 실패'; }
}
function vpGateNext(){
  const yellow=(VP_BEATS||[]).map((b,i)=>i).filter(i=>!(Array.isArray(VP_BEATS[i].cap_durs)&&VP_BEATS[i].cap_durs.length));
  const unheard=yellow.filter(i=>!_vpHeard.has(i));
  if(unheard.length && !confirm('🟡 '+unheard.length+'개 장면을 아직 안 들어봤어요. 그냥 다음으로 갈까요?')) return;
  goStep(3);   // 꾸미기(4단계, index 3)로
}
```
`_voiceForTone(tone)`는 현재 화면의 성우 프리셋에서 그 variant를 찾는 헬퍼다. 기존 성우 선택 상태(`VOICE`)와 `/api/voice-presets`의 variants를 쓴다:
```javascript
function _voiceForTone(tone){
  // VOICE_GROUPS: api_voice_presets 응답 캐시(성우별 variants). 없으면 현재 VOICE로 폴백.
  const g=(typeof VOICE_GROUPS!=='undefined'?VOICE_GROUPS:[]).find(x=>x.group_id===(VOICE&&VOICE.group_id));
  const v=g && (g.variants||[]).find(x=>x.variant===tone);
  if(v) return {voice_id:v.voice_id||v.base_voice_id, settings:v.settings||v.voice_settings, speed:v.speed||v.default_speed};
  return (VOICE&&VOICE.voice_id)?{voice_id:VOICE.voice_id,settings:VOICE.settings,speed:VOICE.speed}:null;
}
```
(⚠️ 구현자는 `grep -n "VOICE_GROUPS\|VOICE=\|group_id\|variants" produce.html`로 실제 성우 상태 변수명·구조를 확인해 `_voiceForTone`을 맞춘다. 다르면 실제 변수로 교체하되, "현재 성우의 tone variant → voice dict" 계약은 유지. 톤 variant를 못 찾으면 null 반환→버튼이 '그 톤 없어요' 표시.)

- [ ] **Step 5: Wire the gate to the next-step button**

3단계(TTS)의 "다음 단계" 버튼 onclick을 `vpGateNext()`로 바꾼다. `grep -n 'goStep(3)\|다음 단계\|data-step="TTS"' produce.html`로 TTS 단계의 다음 버튼을 찾아 그 onclick만 교체(다른 단계 버튼 건드리지 말 것). 못 찾으면 TTS 패널 하단에 `<button class="btn-next" onclick="vpGateNext()">다음 단계 (꾸미기) →</button>` 추가.

- [ ] **Step 6: Run tests to verify they pass**

Run: `python -m pytest shopping_shorts/tests/test_voice_preview_fix_js.py -v`
Expected: 5/5 PASS

- [ ] **Step 7: Regression + commit**

Run: `python -m pytest shopping_shorts/tests/ -k "js or preview or caption or mix" -q`
Expected: 신규 실패 0.
```bash
git -C .tracks/자막싱크 add shopping_shorts/static/produce.html shopping_shorts/tests/test_voice_preview_fix_js.py
git -C .tracks/자막싱크 commit -m "feat(자막싱크): 프리뷰 고치기 — 자막 당기기/밀기·톤 재생성·부드러운 게이트"
```

---

### Task 7: 서버 grounding (로컬 키 0개면 미관측 명시)

**Files:**
- Create: `scratchpad/vp_probe.md` (커밋 안 함 — 관측 노트)

**목적:** 유닛/정적 테스트는 다 초록이어도, ①프론트 가라오케 자막이 오디오와 실제로 맞는지 ②regen이 서버에서 진짜 그 비트만 바꾸고 재동기하는지 ③cap_offset 딸깍이 최종 렌더 자막을 옮기는지는 **실서버(키 있음)에서만** 안다.

- [ ] **Step 1: 로컬 키 확인**

Run: `python -c "from shopping_shorts import config; print('GROQ', bool(config.GROQ_API_KEY), 'EL', bool(config.ELEVENLABS_API_KEY))"`
- 둘 다 False(로컬 예상)면: 실 grounding 불가. `.superpowers/sdd/`(또는 handoff)에 **"프리뷰·regen·cap_offset 실경로 미관측 — 로컬 키 0개, finish 후 서버에서 확인"**을 남기고 Task 완료 조건 = "유닛/정적 green + 회귀 0 + 미관측 명시".

- [ ] **Step 2: (키 있을 때만) 실 관측**

라이브(shoppingshorts.duckdns.org) 제작소에서 실제 대본으로 "전체 생성" → 프리뷰에서: 전체 재생 시 프레임 전환·자막이 음성과 붙나 / 🟡 비트에서 당기기·밀기가 자막을 옮기나 / 톤 바꿔 다시 후 그 비트만 새 음성+재동기되나 / 최종 렌더에 cap_offset이 반영되나. 어긋나면 `_MIN_MATCH_RATIO`·오프셋 스텝·regen 대기시간을 실측으로 조정.

- [ ] **Step 3: 관측(또는 미관측 사유) 기록**

관측 결과를 handoff/자막싱크.md에 남긴다. 코드 변경이 있으면 커밋.

---

## Self-Review

**1. Spec coverage:**
- §A 자리·데이터원천 → Task 2(cap_segments)·Task 5(프레임·음성·result 재사용). ✅
- §B 🟢🟡(cap_durs 유무·정직) → Task 5 배지 + badgeTip 문구. ✅
- §C 전체재생·가라오케 → Task 5 playAllBeats/_vpDriveCaption. ✅
- §D 당기기/밀기·톤재생성·성우전체 → Task 6(vpNudge/vpRegenTone) + 기존 applyVoice(성우 전체). ✅
- §E seam(cap_offset 최종까지·저장=읽기·프론트 서버구절) → Task 1 + Task 2 + Task 3. ✅
- §F 게이트 → Task 6 vpGateNext. ✅
- 회귀·안전(cap_offset=0 바이트동일·regen 실패시 유지·렌더중 거부) → Task 1 test·Task 4 route guard. ✅
- 미확정(가라오케 동기·regen 동기/폴링·키0 미관측) → Task 5 근사 + Task 6 setTimeout + Task 7. ✅

**2. Placeholder scan:** 코드 스텝은 전부 실제 코드. `⚠️` 표시된 3곳(api_mix_result 함수명 / status 값 / VOICE 상태 변수)은 "grep으로 실제값 확인해 맞춤 + 못 찾으면 이렇게"까지 명시한 실행지시(막연한 TODO 아님).

**3. Type consistency:** `cap_offset`(float, 기본 0.0) — Task1(_caption_drawtexts/_beat_timeline)·Task3(라우트 저장)·Task6(프론트) 동일. `voice_override`(dict {voice_id,settings,speed}) — Task4·Task6 `_voiceForTone` 동일. `cap_segments`(list[str]) — Task2 생산, Task5 소비. `VP_BEATS`/`_vpHeard` — Task5 정의, Task6 소비. 일치.
