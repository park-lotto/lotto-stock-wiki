# 리포트 질문지 추출 파이프라인 (Plan A) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 증권사 리포트 PDF를 "고정 질문지"로 추출해 라우팅·fan-out 후 원자(atoms)로 저장한다. 기존 "중요한 걸 골라라" 방식의 누락 문제를 제거한다.

**Architecture:** Gemini가 PDF를 읽어 `target_kind`(stock/sector/market)를 스스로 판별하고 타입별 슬롯을 채운 질문지 JSON 1장을 반환한다. 결정론적 후처리(코드백필·한국상장사필터·fan-out)로 질문지를 여러 원자로 전개하고, 기존 `taxonomy.py` 스키마에 매핑해 `atoms.db`에 저장한다. 질문지 원본은 감사·골든셋용으로 파일에 보존한다.

**Tech Stack:** Python 3, google-genai (gemini-3.1-flash-lite), sqlite3, 기존 `pipeline/atoms/*`

## Global Constraints

- 모델: `gemini-3.1-flash-lite` (현 파이프라인과 동일, `pipeline/atoms/atomizer.py` 참조)
- 기존 `pipeline/atoms/pdf_ingest.py`는 **수정 금지** (검증 전까지 병존, Task 6에서 교체)
- 원자 스키마는 `pipeline/atoms/schema.sql` 컬럼만 사용 (스키마 변경 없음)
- 모든 수치·종목명·날짜는 축자 보존, 추출 슬롯에 없으면 null
- fan-out 종목은 **한국 상장사만** (wisereport 코드맵에 있는 것만)
- 테스트는 결정론적 후처리 로직에만 (Gemini 호출은 골든셋=Plan B 담당). 사용자 규칙: 테스트 최소화.

---

### Task 1: 종목명→코드 맵 (codemap.py)

wisereport parsed.json들이 `"삼성화재[000810]"` 형태로 종목명·코드를 이미 담고 있다. 이를 종목명→코드 맵으로 만들어 (1) 코드 백필, (2) 한국 상장사 필터에 쓴다.

**Files:**
- Create: `pipeline/atoms/codemap.py`
- Test: `pipeline/atoms/test_codemap.py`

**Interfaces:**
- Produces: `code_for(name: str) -> str | None` — 한국 상장사면 6자리 코드, 아니면 None
- Produces: `is_korean_stock(name: str) -> bool`

- [ ] **Step 1: 실패 테스트 작성**

`pipeline/atoms/test_codemap.py`:
```python
from pipeline.atoms.codemap import code_for, is_korean_stock

def test_known_korean_stock_returns_code():
    # 삼성전기는 wisereport에 반드시 존재 (2026-06 리포트 다수)
    assert code_for("삼성전기") == "009150"

def test_foreign_name_returns_none():
    assert code_for("Murata") is None
    assert is_korean_stock("Murata") is False

def test_whitespace_tolerant():
    assert code_for("  삼성전기 ") == "009150"
```

- [ ] **Step 2: 실패 확인**

Run: `cd "C:/Users/TheRose/Desktop/로또의 주식" && python -m pytest pipeline/atoms/test_codemap.py -v`
Expected: FAIL — `ModuleNotFoundError: codemap`

- [ ] **Step 3: 구현**

`pipeline/atoms/codemap.py`:
```python
"""종목명 → 코드 맵. 소스: raw/wisereport/*_parsed.json 의 '종목명[코드]' 컬럼."""
import json
import re
from pathlib import Path

_WISE = Path(__file__).parent.parent.parent / "raw" / "wisereport"
_CACHE: dict[str, str] = {}


def _build() -> dict[str, str]:
    m: dict[str, str] = {}
    for f in _WISE.glob("*_parsed.json"):
        try:
            data = json.loads(f.read_text(encoding="utf-8"))
        except (json.JSONDecodeError, OSError):
            continue
        for row in data.get("corp", []):
            if len(row) < 2:
                continue
            mt = re.match(r"\s*(.+?)\s*\[(\d{6})\]", str(row[1]))
            if mt:
                m[mt.group(1).strip()] = mt.group(2)
    return m


def _ensure() -> None:
    if not _CACHE:
        _CACHE.update(_build())


def code_for(name: str) -> str | None:
    if not name:
        return None
    _ensure()
    return _CACHE.get(name.strip())


def is_korean_stock(name: str) -> bool:
    return code_for(name) is not None
```

- [ ] **Step 4: 통과 확인**

Run: `python -m pytest pipeline/atoms/test_codemap.py -v`
Expected: PASS (3 passed). 만약 `code_for("삼성전기")`가 None이면 wisereport에 해당 종목이 없는 것 — 테스트의 종목명을 실제 존재하는 것으로 교체(예: `삼성화재`/`000810`).

- [ ] **Step 5: 커밋**

```bash
git add pipeline/atoms/codemap.py pipeline/atoms/test_codemap.py
git commit -m "feat(atoms): 종목명→코드 맵 (wisereport 기반) + 한국상장사 필터"
```

---

### Task 2: 질문지 추출 프롬프트 + Gemini 래퍼 (questionnaire.py 1부)

검증 완료된 질문지 프롬프트(이 대화의 `_spike_questionnaire.py` 최종본)를 정식 모듈로 옮긴다.

**Files:**
- Create: `pipeline/atoms/questionnaire.py`

**Interfaces:**
- Produces: `QUESTIONNAIRE_PROMPT: str`
- Produces: `extract_questionnaire(pdf_path: Path) -> dict` — 채워진 질문지 dict 반환 (실패 시 `{}`)

- [ ] **Step 1: 모듈 작성 (프롬프트 + 추출 함수)**

`pipeline/atoms/questionnaire.py`:
```python
"""리포트 질문지 추출 — Gemini가 PDF를 고정 슬롯에 옮긴다."""
import json
from pathlib import Path

from google import genai
from google.genai import types

from .atomizer import _load_gemini_key

_MODEL = "gemini-3.1-flash-lite"

QUESTIONNAIRE_PROMPT = """너는 증권사 리포트를 '정해진 질문지'에 옮겨 적는 사람이다.
무엇이 중요한지 판단하지 마라. 칸을 채우는 것이 전부다.

## 철칙
1. 칸에 해당하는 내용이 리포트에 없으면 그 값은 null. 절대 지어내지 마라.
2. 모든 수치·날짜·종목명은 원문 그대로(축자). 반올림·재서술 금지.
3. 핵심 슬롯에는 "quote"(근거가 된 원문 문장 그대로)를 짝으로 붙여라.
4. 면책조항·독립성 고지·보유율(1% 미만 등)·법적 경고 문구는 완전히 무시하라.
   설령 그것이 요약의 전부처럼 보여도, 본문에서 실제 분석 내용을 찾아 채워라.
5. 증권사명·애널리스트명은 PDF 표지/머리글/꼬리글에서 찾아라. (파일명엔 없을 수 있음)

## STEP 0 — target_kind 판별
- "stock"  : 특정 종목에 목표주가가 제시됨
- "sector" : 단일 업종/테마 전반의 전망 (개별 목표가 없이 업종을 논함)
- "market" : 시장 전체/매크로/전략, 또는 2개 이상 섹터를 묶은 데일리·위클리
판별 근거는 네이버 카테고리나 제목이 아니라 본문 내용이다.

## STEP 1 — 공통 헤더
broker(증권사), analyst(애널리스트), report_date

## STEP 2 — target_kind별 질문지

[stock] 각 종목별로 stocks 배열:
  name, code(PDF에 6자리 있으면, 없으면 null),
  rating, rating_changed,
  tp_new, tp_prev, tp_direction(up/down/flat/null),
  earnings_outlook(이번·차기 분기 실적·판매량·매출·영익 전망. 컨센/YoY/QoQ 수치가
    하나라도 있으면 반드시 채워라. 빈칸으로 넘기지 마라),
  estimate_revision, next_catalyst,
  thesis(최대 3개 배열), valuation_basis, risk, supply_comment, quote

[sector]
  sector_name, sector_view(비중확대/중립/축소),
  thesis(배열), timeline(배열),
  top_picks: [{name, reason, tp}]
    — 본문에 매수·선호·수혜·주목·탑픽·1선/2선으로 종목명이 거론된 곳을 빠짐없이.
      단 하나의 종목명도 없을 때만 null.
  risk, quote

[market]
  market_direction, macro_vars(배열),
  recommended_sectors: [{sector, stance, reason}],
  style, event_calendar: [{date, event}],
  top_picks(거론 종목 배열 또는 null), risk, quote

## 출력
질문지 1장을 JSON 객체 하나로. target_kind에 해당하는 칸만 채우고 나머지는 생략.
다른 텍스트 절대 금지."""


def extract_questionnaire(pdf_path: Path) -> dict:
    """PDF를 Gemini로 읽어 채워진 질문지 dict 반환. 실패 시 {}."""
    client = genai.Client(api_key=_load_gemini_key())
    try:
        with open(pdf_path, "rb") as fh:
            fobj = client.files.upload(
                file=fh, config=types.UploadFileConfig(mime_type="application/pdf")
            )
    except Exception as e:
        print(f"  [WARN] 업로드 실패: {e}")
        return {}
    try:
        resp = client.models.generate_content(
            model=_MODEL,
            contents=[fobj, QUESTIONNAIRE_PROMPT],
            config=types.GenerateContentConfig(response_mime_type="application/json"),
        )
        return json.loads(resp.text)
    except Exception as e:
        print(f"  [WARN] 추출 실패: {e}")
        return {}
    finally:
        try:
            client.files.delete(name=fobj.name)
        except Exception:
            pass
```

- [ ] **Step 2: 임포트 스모크 체크**

Run: `python -c "from pipeline.atoms.questionnaire import QUESTIONNAIRE_PROMPT, extract_questionnaire; print('ok', len(QUESTIONNAIRE_PROMPT))"`
Expected: `ok <숫자>` 출력 (임포트 에러 없음)

- [ ] **Step 3: 커밋**

```bash
git add pipeline/atoms/questionnaire.py
git commit -m "feat(atoms): 질문지 추출 프롬프트 + Gemini 래퍼"
```

---

### Task 3: 질문지 → 원자 변환 + fan-out (questionnaire.py 2부)

채워진 질문지를 결정론적으로 원자 리스트로 전개한다. stock=종목별 강한 원자, sector=섹터 원자 1 + 탑픽 fan-out, market=시장 원자 1 + 추천섹터·탑픽 fan-out. 해외 종목은 제외, 코드 백필.

**Files:**
- Modify: `pipeline/atoms/questionnaire.py` (함수 추가)
- Test: `pipeline/atoms/test_questionnaire_to_atoms.py`
- Create: `pipeline/atoms/fixtures/spike_out.json` (이 대화의 `_spike_out.json` 3종을 복사 — 픽스처 겸 골든셋 씨앗)

**Interfaces:**
- Consumes: `code_for`, `is_korean_stock` (Task 1)
- Produces: `questionnaire_to_atoms(q: dict, meta: dict) -> list[dict]`
  - `meta` = `{"date": str, "broker": str, "raw_file": str}`
  - 반환 원자 dict는 `insert_atom`이 받는 컬럼 키를 모두 가짐

- [ ] **Step 1: 픽스처 준비**

`_spike_out.json`(루트)을 픽스처로 복사:
```bash
mkdir -p pipeline/atoms/fixtures
cp _spike_out.json pipeline/atoms/fixtures/spike_out.json
```

- [ ] **Step 2: 실패 테스트 작성**

`pipeline/atoms/test_questionnaire_to_atoms.py`:
```python
import json
from pathlib import Path
from pipeline.atoms.questionnaire import questionnaire_to_atoms

FIX = json.loads(
    (Path(__file__).parent / "fixtures" / "spike_out.json").read_text(encoding="utf-8")
)
META = {"date": "2026-06-19", "broker": "테스트", "raw_file": "x.md"}


def _result_for(idx):
    return FIX[idx]["result"]


def test_stock_report_makes_one_atom_per_stock():
    atoms = questionnaire_to_atoms(_result_for(0), META)  # 현대차/기아
    assets = {a["asset"] for a in atoms}
    assert "현대차" in assets and "기아" in assets
    for a in atoms:
        assert a["asset_level"] == "stock"
        assert a["strength_score"] >= 3  # 목표가 동반 = 강한 신호


def test_sector_report_fans_out_korean_picks_only():
    atoms = questionnaire_to_atoms(_result_for(1), META)  # MLCC, picks 5개(해외 포함)
    levels = [a["asset_level"] for a in atoms]
    assert "sector" in levels  # 섹터 대표 원자 1개
    stock_assets = {a["asset"] for a in atoms if a["asset_level"] == "stock"}
    assert "삼성전기" in stock_assets       # 한국 상장사 → 포함
    assert "Murata" not in stock_assets     # 해외 → 제외
    for a in atoms:
        if a["asset_level"] == "stock":
            assert a["strength_score"] <= 2  # 거론 레벨 = 약한 신호


def test_market_report_fans_out_sectors_and_picks():
    atoms = questionnaire_to_atoms(_result_for(2), META)  # 로봇/방산/조선 데일리
    sectors = {a["sector"] for a in atoms if a["asset_level"] == "sector"}
    assert {"로봇", "방산", "조선"} & sectors
    stock_assets = {a["asset"] for a in atoms if a["asset_level"] == "stock"}
    assert "삼성전자" in stock_assets
```

- [ ] **Step 3: 실패 확인**

Run: `python -m pytest pipeline/atoms/test_questionnaire_to_atoms.py -v`
Expected: FAIL — `questionnaire_to_atoms` 미정의

- [ ] **Step 4: 구현 (questionnaire.py에 추가)**

```python
import hashlib
from .codemap import code_for, is_korean_stock

_SECTOR_LIST = [
    "반도체", "조선", "로봇", "방산", "바이오", "전력", "2차전지",
    "자동차", "통신", "AI소프트웨어", "우주", "소비내수", "미용",
    "LNG", "신재생", "기타",
]


def _norm_sector(s: str) -> str:
    if not s:
        return "기타"
    for k in _SECTOR_LIST:
        if k in s:
            return k
    return "기타"


def _mk_id(date: str, broker: str, tag: str, i: int) -> str:
    raw = f"{date}_{broker}_{tag}_{i}"
    return "atom_" + hashlib.md5(raw.encode()).hexdigest()[:12]


def _base(meta: dict, **kw) -> dict:
    """원자 공통 필드 + 기본값. kw로 덮어쓴다."""
    d = {
        "date": meta["date"],
        "source_type": "report",
        "source_name": meta["broker"],
        "source_trust": "A",
        "raw_file": meta["raw_file"],
        "layer": "L5",
        "sector": "기타",
        "asset": "",
        "asset_level": "stock",
        "signal": "neutral",
        "event_type": "report",
        "magnitude": "minor",
        "content_type": "analysis",
        "strength_score": 1,
        "validity_type": "permanent",
        "validity_until": None,
        "is_active": 1,
        "content": "",
        "relations": [],
    }
    d.update(kw)
    return d


def _stock_atom(meta, sector, name, content, *, strong, signal, i, tag) -> dict:
    return _base(
        meta,
        id=_mk_id(meta["date"], meta["broker"], tag, i),
        sector=_norm_sector(sector),
        asset=name,
        asset_level="stock",
        signal=signal,
        magnitude="major" if strong else "minor",
        strength_score=4 if strong else 2,
        content=content,
    )


def questionnaire_to_atoms(q: dict, meta: dict) -> list[dict]:
    kind = q.get("target_kind")
    atoms: list[dict] = []

    if kind == "stock":
        for i, s in enumerate(q.get("stocks", [])):
            name = (s.get("name") or "").strip()
            if not name:
                continue
            sig = {"up": "bullish", "down": "bearish"}.get(s.get("tp_direction"), "neutral")
            parts = [
                f"투자의견 {s.get('rating')}({s.get('rating_changed') or ''})",
                f"목표가 {s.get('tp_prev')}→{s.get('tp_new')}" if s.get("tp_new") else "",
                s.get("earnings_outlook") or "",
                "; ".join(s.get("thesis") or []),
                f"리스크: {s.get('risk')}" if s.get("risk") else "",
            ]
            content = " / ".join(p for p in parts if p)
            atoms.append(_stock_atom(
                meta, _guess_sector_from_stock(name), name, content,
                strong=bool(s.get("tp_new")), signal=sig, i=i, tag="stk",
            ))

    elif kind == "sector":
        sec = _norm_sector(q.get("sector_name"))
        thesis = "; ".join(q.get("thesis") or [])
        atoms.append(_base(
            meta,
            id=_mk_id(meta["date"], meta["broker"], "sec", 0),
            sector=sec, asset=q.get("sector_name") or sec, asset_level="sector",
            signal="bullish" if "확대" in (q.get("sector_view") or "") else "neutral",
            event_type="report", magnitude="major", strength_score=4,
            content=f"[{q.get('sector_view')}] {thesis}",
        ))
        for i, p in enumerate(q.get("top_picks") or []):
            name = (p.get("name") or "").strip()
            if not is_korean_stock(name):
                continue
            atoms.append(_stock_atom(
                meta, sec, name, f"{sec} 섹터리포트 탑픽 거론: {p.get('reason') or ''}",
                strong=False, signal="bullish", i=i, tag="secpick",
            ))

    elif kind == "market":
        atoms.append(_base(
            meta,
            id=_mk_id(meta["date"], meta["broker"], "mkt", 0),
            sector="기타", asset="시장", asset_level="market",
            signal="neutral", event_type="macro", magnitude="major", strength_score=3,
            content=q.get("market_direction") or "; ".join(
                rs.get("reason", "") for rs in q.get("recommended_sectors") or []
            ),
        ))
        for i, rs in enumerate(q.get("recommended_sectors") or []):
            sec = _norm_sector(rs.get("sector"))
            atoms.append(_base(
                meta,
                id=_mk_id(meta["date"], meta["broker"], "mktsec", i),
                sector=sec, asset=rs.get("sector") or sec, asset_level="sector",
                signal="bullish" if "확대" in (rs.get("stance") or "") else "neutral",
                event_type="report", magnitude="minor", strength_score=2,
                content=f"시황리포트 추천섹터: {rs.get('reason') or ''}",
            ))
        for i, name in enumerate(q.get("top_picks") or []):
            name = (name or "").strip()
            if not is_korean_stock(name):
                continue
            atoms.append(_stock_atom(
                meta, _guess_sector_from_stock(name), name,
                "시황리포트 탑픽 거론", strong=False, signal="bullish", i=i, tag="mktpick",
            ))

    return atoms


def _guess_sector_from_stock(name: str) -> str:
    """종목 섹터 추정 — 현재는 기타. 후속에서 종목→섹터 맵 연결."""
    return "기타"
```

- [ ] **Step 5: 통과 확인**

Run: `python -m pytest pipeline/atoms/test_questionnaire_to_atoms.py -v`
Expected: PASS (3 passed)

- [ ] **Step 6: 커밋**

```bash
git add pipeline/atoms/questionnaire.py pipeline/atoms/test_questionnaire_to_atoms.py pipeline/atoms/fixtures/spike_out.json
git commit -m "feat(atoms): 질문지→원자 변환 + fan-out (한국상장사 필터·강도구분)"
```

---

### Task 4: 오케스트레이터 + 질문지 아티팩트 보존 (report_ingest.py)

MD → PDF 다운로드 → 추출 → 질문지 JSON 보존 → 원자 변환 → DB 저장. CLI 제공. 기존 `pdf_ingest.py`의 다운로드·메타 함수를 재사용한다.

**Files:**
- Create: `pipeline/atoms/report_ingest.py`

**Interfaces:**
- Consumes: `extract_questionnaire`, `questionnaire_to_atoms` (Task 2·3),
  `pdf_ingest._extract_pdf_url`, `pdf_ingest._extract_meta`, `pdf_ingest._download_pdf`,
  `db.init_db`, `db.insert_atom`, `vector_db.embed_and_store`
- Produces: `ingest_report(md_path: Path) -> int` — 저장된 원자 수
- Produces: 질문지 아티팩트 `raw/report_q/{date}/{stem}.json`

- [ ] **Step 1: 구현**

`pipeline/atoms/report_ingest.py`:
```python
"""리포트 질문지 인제스트 오케스트레이터.

사용법:
    python -m pipeline.atoms.report_ingest raw/report/2026-06-19/xxx.md
    python -m pipeline.atoms.report_ingest --dir "C:/.../crawling_bot_data/2026-06-19/reports" --limit 5
"""
import sys
import json
import argparse
import tempfile
from pathlib import Path

from .pdf_ingest import _extract_pdf_url, _extract_meta, _download_pdf
from .questionnaire import extract_questionnaire, questionnaire_to_atoms
from .db import init_db, insert_atom
from .vector_db import embed_and_store

_Q_ROOT = Path(__file__).parent.parent.parent / "raw" / "report_q"


def _save_questionnaire(q: dict, meta: dict, stem: str) -> None:
    out_dir = _Q_ROOT / meta["date"]
    out_dir.mkdir(parents=True, exist_ok=True)
    payload = {"meta": meta, "questionnaire": q}
    (out_dir / f"{stem}.json").write_text(
        json.dumps(payload, ensure_ascii=False, indent=2), encoding="utf-8"
    )


def ingest_report(md_path: Path) -> int:
    url = _extract_pdf_url(md_path.read_text(encoding="utf-8-sig", errors="replace"))
    if not url:
        print(f"  [SKIP] PDF URL 없음: {md_path.name}")
        return 0
    m = _extract_meta(md_path)
    meta = {"date": m["date"], "broker": m["broker"], "raw_file": str(md_path)}

    with tempfile.TemporaryDirectory() as tmp:
        pdf = Path(tmp) / (md_path.stem + ".pdf")
        if not _download_pdf(url, pdf):
            return 0
        q = extract_questionnaire(pdf)

    if not q:
        print("  [WARN] 빈 질문지")
        return 0

    # 증권사명을 PDF 추출값으로 보강 (MD가 깨졌을 수 있음)
    if q.get("broker"):
        meta["broker"] = q["broker"]

    _save_questionnaire(q, meta, md_path.stem)
    atoms = questionnaire_to_atoms(q, meta)
    for a in atoms:
        insert_atom(a)
        try:
            embed_and_store(a)
        except Exception:
            pass
    return len(atoms)


def main():
    ap = argparse.ArgumentParser()
    ap.add_argument("file", nargs="?")
    ap.add_argument("--dir")
    ap.add_argument("--limit", type=int, default=10)
    args = ap.parse_args()
    init_db()

    if args.file:
        targets = [Path(args.file)]
    elif args.dir:
        targets = sorted(Path(args.dir).glob("*.md"))[: args.limit]
    else:
        print("file 또는 --dir 필요")
        return

    total = 0
    for i, f in enumerate(targets, 1):
        print(f"[{i}/{len(targets)}] {f.name}")
        n = ingest_report(f)
        total += n
        print(f"  → {n}개 원자")
    print(f"\n완료: {len(targets)}개 파일, {total}개 원자")


if __name__ == "__main__":
    main()
```

- [ ] **Step 2: 실데이터 1건 스모크 (Gemini 실호출)**

Run:
```bash
python -m pipeline.atoms.report_ingest "C:/Users/TheRose/crawling_bot_data/2026-06-19/reports/2026-06-19_1007_MLCC 산업, 앞으로 어떤 포인트를 봐야할까.md"
```
Expected: `→ N개 원자` (N≥2: 섹터 원자 1 + 삼성전기 등 한국 탑픽), 그리고 `raw/report_q/2026-06-19/...json` 생성됨.

- [ ] **Step 3: 저장 결과 확인**

Run:
```bash
python -c "from pipeline.atoms.db import query_atoms; [print(a['asset_level'], a['asset'], a['strength_score']) for a in query_atoms(days=3, source=None) if a['source_type']=='report'][:10]" 2>/dev/null || python -m pipeline.atoms.query "MLCC" --n 5
```
Expected: 방금 저장된 MLCC 섹터/삼성전기 원자가 보임.

- [ ] **Step 4: 커밋**

```bash
git add pipeline/atoms/report_ingest.py
git commit -m "feat(atoms): 리포트 질문지 인제스트 오케스트레이터 + 질문지 아티팩트 보존"
```

---

### Task 5: 3종 타입 배치 검증 (회귀 안전망 씨앗)

stock·sector·market 각 1건을 실제로 돌려 fan-out·라우팅이 설계대로 동작하는지 사람이 1회 확인한다. (Plan B 골든셋의 기초)

**Files:** 없음 (실행 검증만)

- [ ] **Step 1: 3종 실행**

```bash
python -m pipeline.atoms.report_ingest "C:/Users/TheRose/crawling_bot_data/2026-06-19/reports/2026-06-19_0706_현대차와 기아, 기대감과 실적의 밸런스.md"
python -m pipeline.atoms.report_ingest "C:/Users/TheRose/crawling_bot_data/2026-06-19/reports/2026-06-19_1008_안녕하세요 데일리에요(로봇방산조선).md"
```

- [ ] **Step 2: 질문지 아티팩트 육안 확인**

`raw/report_q/2026-06-19/` 의 3개 JSON을 열어 확인:
- 현대차/기아: `target_kind=stock`, 목표가 760,000/290,000, earnings_outlook 채워짐
- MLCC: `target_kind=sector`, top_picks에 삼성전기 포함
- 데일리: `target_kind=market`, recommended_sectors=로봇·방산·조선, top_picks 한국주만 원자화됨

Expected: 위 3가지가 모두 충족. 하나라도 어긋나면 Task 2 프롬프트 또는 Task 3 매핑을 수정 후 재실행.

- [ ] **Step 3: 스파이크 파일 정리**

검증 통과 시 임시 스파이크 제거:
```bash
git rm -f _spike_questionnaire.py _spike_out.json 2>/dev/null; rm -f _spike_questionnaire.py _spike_out.json
git add -A
git commit -m "chore: 질문지 스파이크 정리 (정식 모듈로 이관 완료)"
```

---

### Task 6: 기존 pdf_ingest 교체 (cutover)

새 파이프라인이 검증됐으므로 기존 진입점을 새것으로 연결한다. (호출처가 있으면 안전하게 위임)

**Files:**
- Modify: `pipeline/atoms/pdf_ingest.py` (main을 report_ingest로 위임하는 deprecation 경로)

- [ ] **Step 1: 호출처 확인**

Run: `grep -rn "pdf_ingest" --include=*.py . | grep -v "pipeline/atoms/pdf_ingest.py"`
Expected: 호출처 목록. 없으면 Step 2만, 있으면 각 호출처를 report_ingest로 변경.

- [ ] **Step 2: pdf_ingest.main에 안내 추가**

`pipeline/atoms/pdf_ingest.py` main 최상단에 추가:
```python
    print("[DEPRECATED] pdf_ingest → report_ingest 사용 권장: "
          "python -m pipeline.atoms.report_ingest --dir <폴더>")
```

- [ ] **Step 3: 커밋**

```bash
git add pipeline/atoms/pdf_ingest.py
git commit -m "refactor(atoms): pdf_ingest deprecate → report_ingest 안내"
```

---

## Self-Review (수행 완료)

**1. Spec coverage:**
- 라우터(내용기반) → Task 2 프롬프트 STEP 0 ✅
- 타입별 질문지 3종 → Task 2 ✅
- fan-out(탑픽·추천섹터→원자) → Task 3 ✅
- 질문지→원자(taxonomy 매핑) → Task 3 ✅
- 코드 백필·한국상장사 필터 → Task 1 + Task 3 ✅
- 증권사 PDF 보강 → Task 4 Step1 ✅
- 질문지 아티팩트 보존(골든셋·검증 입력) → Task 4 ✅
- 3단 검증 → **Plan B로 분리** (의도된 범위 밖)

**2. Placeholder scan:** `_guess_sector_from_stock`은 의도적으로 "기타" 반환(종목→섹터 맵은 후속). 플레이스홀더 아님 — 동작하는 기본값. 그 외 TODO 없음.

**3. Type consistency:** `code_for`/`is_korean_stock`(Task1) → Task3에서 동일 시그니처 사용. `questionnaire_to_atoms(q, meta)`/`extract_questionnaire(pdf_path)` Task2·3·4 일관. 원자 dict 키 = `schema.sql`/`insert_atom` 컬럼과 일치. ✅

**미해결(구현 중 결정):**
- `query_atoms`에 `source` 파라미터 없음 → Task4 Step3 확인 쿼리는 `python -m pipeline.atoms.query` 폴백 사용.
- 종목→섹터 맵(`_guess_sector_from_stock`)은 Plan A에서 "기타"로 두고 후속 개선.
