# 핸드오프 — 제작소트림 (문장별 트림)

- 날짜: 2026-07-22
- 트랙: 제작소트림
- 상태: **코드 6태스크 완료, 최종 whole-branch 리뷰 통과(Ready to merge). finish 게이트 실행 예정.**

## 완료한 것

제작소 검토 페이지에서 **문장(비트)마다 끝/앞의 조용한 부분을 버튼으로 자르는** 기능. 캡컷 없이
웹에서 "목소리 끝난 뒤 화면만 머무는 무음 꼬리"를 제거한다.

- 비파괴 설계: 비트 dict의 `head_trim`/`tail_trim`(초)만 저장. 원본 TTS mp3 안 건드림. 되돌리기 = 값 0.
- 단일 출처 `_beat_effective_dur`(=`_effective_dur(probe−head−tail, floor 0.4)`)를 `_beat_timeline`·
  `_render_mix` 둘 다에서 사용 → 영상·자막·다음비트 t0·캡컷이 자동 동반 축소.
- 렌더: 뒤트림=`-t tts_dur`, 앞트림=`-ss head` (양 ffmpeg 브랜치). 캡컷: 뒤=source_dur, 앞=source_start.
- UI: 문장 카드에 `✂ 끝 조용한 부분 자르기`(자동) + `뒤/앞 −0.3초`(누적) + `되돌리기`. 버튼만(시니어).
- 엔드포인트 `POST /api/produce/mix/{job}/trim`(auto/nudge/reset·409·하한가드).

**커밋 범위**: `9aaafe8b1..bcd49a918` (Task1~6, 각 태스크 개별 리뷰 통과). 전체회귀 **111 passed**, app import OK.

## ⚠️ 남은 것 (follow-up 티켓, 병합 차단 아님)

최종 리뷰가 모두 "follow-up"으로 트리아지. 우선순위 순:

1. **(d) 하한가드/404/422/head-edge 회귀 테스트** — 가드 산술은 리뷰어가 손으로 2번 검증(정확)했으나
   자동 테스트 공백. 한 개 회귀 테스트 추가 권장(가장 먼저).
2. **(obs) 트림 후 표시값 stale** — 트림해도 `target_seconds`("영상 길이")·`sync_gap`("대사가 영상보다 X초
   깁니다") 경고가 합성 시점 값 그대로. **렌더·캡컷 출력은 정확**, 표시만 어긋남. sync_gap 경고를 트림으로
   지우려는 사용자가 헷갈릴 수 있음. `mix_pipeline.py:118,188` 근처에서 트림 반영해 재계산.
3. **(e) 되돌리기가 head+tail 둘 다 트림 시 한 클릭에 한 엣지만** — loadMixReview 재렌더로 두 번 누르면
   해결되나 살짝 혼란. 둘 다 지우는 옵션 검토.
4. **(a) 무 tts_path 비트 무제한 nudge** — 렌더·타임라인이 `if not tts: continue`로 건너뛰어 무의미(inert).
   사실상 won't-fix.
5. **(b) `mode` 미검증** — 크래프트 요청만 도달, UI는 auto/nudge/reset만 보냄. allowlist 추가 여유될 때.
6. **(c) `int(beat_idx)` float 절삭** — 형제 라우트(/sfx·/cutaway)와 동일 캐스트, UI는 항상 int.

## ➡️ 다음 세션 방향 (2026-07-22 사장님 결정) — "간단편집 강화"

트림은 라이브 갔지만 **"기능이 안 좋다"** 는 피드백. 핵심 진단:
- **가장 큰 문제 = 발견성(discoverability).** 편집 버튼(트림·🎞화면교체)이 **「영상 매칭」 단계의
  #mixReview 문장 카드**에만 있어, 완성본(최종검수·렌더) 화면을 보는 사장님 눈엔 **안 보인다.**
  (사장님이 "🎞 버튼이 어디?"라고 물음 = 카드 자체를 못 찾음.)
- 방향 결정: **캡컷 클론(웹 타임라인 편집기) 아님.** 간단편집을 버튼으로 제대로 채운다(시니어 유지).

**후보 우선순위(아직 사장님이 최종 선택 안 함 — 다음 세션 첫 질문):**
1. **완성본 화면에서 바로 편집** — 렌더 끝난 뒤에도 문장별 편집(화면교체·트림·순서) 버튼을 눈앞에.
   지금 최대 문제. (#mixReview를 최종검수 단계에도 노출하거나, 편집 진입 버튼)
2. **글자 고치면 그 문장만 재녹음** — 인라인 텍스트 수정 → 그 비트만 재TTS.
   ★배관 이미 있음: `POST /api/mix/tts/{job}/{beat}/regen` + `resynth_one_beat`(현재는 톤·성우만,
   narration 텍스트 override 추가하면 됨). 검토카드 narration은 현재 읽기전용.
3. **장면 순서 바꾸기/빼기** — 문장 ↑↓ / 빼기 버튼.
4. **트림 UI 다듬기** — 더 크게·명확히, 미리보기 연동 개선.

다음 세션: 위 4개 중 우선순위 확정 → brainstorming → writing-plans → 서브에이전트 실행(같은 트랙 `제작소트림`).

## ⚠️ 실렌더 grounding (수동 확인 필요)

단위 테스트는 `_probe_duration`을 mock하므로 **실제 ffmpeg `-ss`/`-t` 동작은 미검증**. 단, 트림 없을 때
기본 경로는 기존과 byte-identical(리뷰어 확인). 라이브 후 눈으로 확인:
- 제작소 → 영상 매칭 → 문장 카드 `✂ 끝 조용한 부분 자르기` → **미리보기에서 그 문장 꼬리가 짧아지나**.
- 캡컷 보내기 → 음성 트랙 길이/시작이 트림대로인지.
