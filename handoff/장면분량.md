# 장면분량 — 핸드오프

> 소유 트랙: 장면분량 — 이 파일은 이 트랙 세션만 수정한다. 다른 트랙은 읽기만.
> 영역 분담(2026-09-16 사장님): **이 트랙 = 대본을 장면에 매칭(3단계 상속·채우기·scene_grounding_check)** /
> 대본게이트 트랙 = 대본 생성·FATAL_CHECKS. `scene_grounding_check`를 바꿀 때 `scene_descs` 인자만 두면 안 부딪힌다.

> ## 📌 이어받기 (2026-09-23 00:40 KST 집 PC CH — 09-22 저녁 트랙 전부 라이브 배포)
> **트랙명: `장면분량`** · 브랜치 `track/장면분량` · **main = 트랙 = `19063a3e3`**(미커밋 0, 트랙 폴더는 finish로 main에 맞춤)
> ```
> git pull                                  # main 폴더
> 트랙.bat 장면분량                          # 트랙 폴더 없으면: py tools/track.py start 장면분량
> git -C .tracks/장면분량 pull origin track/장면분량
> ```
> 라이브 = main `19063a3e3`(AI 장면 매칭·역할별 컷 배정·말맛 센서스 포함) · 스위치 `story_writer_enabled='admin'` **켜져 있음**
> 이상하면 먼저 끈다: 서버 DB `set_setting('story_writer_enabled','')`
> 첫 할 일: 사장님 계정으로 제작소 2단계 생성 → 결과 `note.matcher`가 `ai`인지(`code(이유)`면 AI 실패) → 줄별 컷 대조 → 아래 🔵 결정 1~3

_최종: 2026-09-22 낮(PC CH) · 트랙 커밋 `afd2087fa` — 아래 🟣 블록이 최신. 09-21 블록의 "finish 하지 마라"는 09-22 새벽 사장님 지시("내거에만 라이브스위치 켜고 … 관리자만 라이브 스위치라고 / 지금해")로 **관리자 스위치 뒤 배포에 한해** 풀렸다_

> 읽는 순서: 🔵 09-22 저녁(매칭=AI·컷 리듬·불변식) → 🟠 말맛 센서스(09-22 낮) → 🟣 이야기 작가 라이브 배선(09-22) → 🟢 단순 필자 하네스(+➕ 훅·스타일 경로) → 🔴×5 실제 경로 포획 → 🔴×4 필자 비교 → 🔴×3 씨앗 뼈대 수리(단순 필자로 가면 안 씀) → 이하 옛 기록
> 사장님 결정 대기: ①HTML 3종(바탕화면 `단순필자_*_2026-09-21.html`) 읽고 판정 ②대본 쓰기 단계를 단순 필자+Gemini 3.6 Flash(Vertex)로 갈지 ③서버용 Vertex 서비스계정 키

## 🔵 2026-09-22 저녁(회사) — 컷 리듬 · 줄=칸=지목컷 불변식 · **AI 장면 매칭** · 집에서 정할 것 4건

> ⚠️ 이 블록은 집 세션(09-22 밤)이 **복원**한 것이다. 회사 세션은 22:51 커밋 `40ffc9fe6`에 "🔵 블록이 오늘 저녁 정리"라고
> 썼지만 실제 커밋엔 읽는 순서 한 줄만 들어갔고 본문은 없었다(본문 문구 grep 0건). 사장님이 찍어 보낸 회사 화면 사진 2장 +
> 트랙 커밋 7건 + `wiki/log.d` 한 줄로 되살렸다. 회사 PC에 미커밋 본문이 남아 있을 수 있다 — 회사에서 `git status` 확인.

### 오늘 한 것 (사진 기준 · 커밋으로 대조)
- **라이브(관리자만, main 20:32 병합까지)**: 이야기 작가 말투·신호어·길이 프리셋(🟠 말맛 센서스) · 컷 리듬 1차(`docs/cut_rhythm_2026-09-22.md`, 히트작 11채널 실측 → 홀드·컷 수 규칙, `check_cut_rhythm_hold.py`) · 줄=칸=지목컷 불변식(저장 출구 관문 우회 — 창작 되돌림·순서 재배분·화면 채우기 건너뜀, job e8984a5c99fb 뿌리).
- **트랙(미배포, main보다 9커밋 앞)**:
  | 커밋 | 내용 |
  |---|---|
  | `2a0c4241d` 20:38 | 2단계 배정: 고조 칸 안 불편 줄(moment/what_happens/before)은 문제·before 컷 먼저, 없애버림 줄만 특징 컷 — 사장님 "걸레 장면 많은데 왜 안 됐지" |
  | `52f4a5cbf` 20:41 | 반전 줄도 재료 번호(`twist_feat`)를 받아 그 특징 컷이 붙는다 |
  | `978e2d0d5` 21:13 | **`shopping_shorts/ai_match.py` 신설** — 대본 줄 전체+컷 목록을 한 번에 주고 줄마다 고르게(호출 1회). 코드는 실재·중복·시간만 검산, 빈 줄만 코드 매칭. 배선 `story_writer.py:628~645`(`n["matcher"]`="ai"/"code(이유)") |
  | `3eddfa603`~`a87d52204` 21:22~21:34 | 주인공 규칙("A가 아니라 B → B") · 매칭 모델 `MODEL="gemini-3.5-flash"`(`ai_match.py:35`) · `_call_json` model 인자 · `tools/seed_analyzer/match_compare.py`(Vertex Gemini 3종 + Claude CLI 2종 나란히, 장면 영상 포함) · `clip_segs.py` |
- 실측 문서: `docs/cut_rhythm_2026-09-22.md` · `docs/nova_writer_script_2026-09-22.md`(노바 시안 문·순서) · `docs/insta_style_2026-09-22.md` · `docs/tone_census_2026-09-22/cut_rhythm_top11.json`(히트작 길이 분포).
- 모델 5종 비교 HTML은 **커밋에 없다**(회사 바탕화면에만 있을 가능성 — 미확인).

### ✅ 09-23 00:24 배포됨 (사장님 "4부터" = 4번 승인)
- `finish` 게이트 통과(기준선 실패 24건 그대로) → main `19063a3e3` → 서버 00:24 pull · **worker 00:38 재시작완료** · **웹 00:55 재시작완료**(고객 접속 연기 1799초 → 강제). 서버 실측: `shopping-shorts` active · `ai_match.py` MODEL 줄 존재 · story_writer에 ai_match 배선 1곳 · HTTP 200 · 00:55 이후 error/traceback 0.
- **병합 충돌 1곳 — `video_assemble.py` 컷 리듬 홀드**: 트랙 `370508332`(20:36, "원본 5초 이어틀기 + 두 번째 컷") vs main `30d0c5ed2`(20:19 장면폰트 트랙, "첫 조각 소스 하나로 비트 끝까지 이어틀기", job 956a 정지 재현, `tools/check_cut_rhythm_hold.py`).
  **main 쪽 채택.** 이유: 검사 도구가 "홀드 비트는 s4 하나로 끝까지"를 요구해 둘이 양립 불가 + 트랙 방식은 조각이 하나뿐이면 5초 뒤 정지가 재발(사장님이 본 바로 그 증상). 트랙 테스트 `test_hold는_첫_조각_하나로_문장_전체를_이어_튼다`의 9초 대사 기대값을 "v1 하나로 9초 실프레임"으로 고쳤다.
  ⚠️ 히트작 최장 홀드 중앙 4.9초(`docs/cut_rhythm_2026-09-22.md`)라는 근거로 5초 상한을 다시 원하시면 — "둘째 컷이 있을 때만 5초에서 끊고, 없으면 끝까지"로 넣고 check 도구도 같이 고쳐야 한다.
- 실측: 관련 테스트 66 passed · `check_cut_rhythm_hold.py` 전부 통과 · `import shopping_shorts.app` OK. **라이브에서 이야기 작가 job은 아직 안 돌려봤다**(웹 재시작 뒤 첫 할 일).

### ⏭ 남은 결정 3건 (사진 원문 그대로)
1. **매칭 모델**: 3.1 flash-lite(4초) vs 3.6 flash(38초, 애매한 재료에 유리). 현재 라이브 코드는 3.5-flash(`ai_match.py:35`).
2. **반전 근거를 재료 번호 필수로 할지** — 씨앗의 "커튼·침대" 같은 재료 밖 사례가 빠질 수 있음.
3. **인스타 한입썰 길이, 미끼 두 칸 쪼개기.**
4. ~~트랙 커밋 배포 승인~~ → 09-23 00:24 완료.

### ⚠️ 함정 5건 (사진 원문 — 상세는 회사 세션만 안다)
- 대기 스크립트 자기 명령줄 매칭
- 게이트 로그 "실패" 문구
- claude CLI는 빈 폴더에서 stdin으로
- Vertex Claude 쿼터 0
- heredoc 이스케이프
- (메모리) "저장 출구 관문이 계획을 다시 고친다" — 회사 PC 메모리 폴더에만 있다(집 PC `memory/`에 없음, grep 0건).

## 🟠 2026-09-22 낮 — **말맛 센서스**(사장님 "1번부터 테스트 / 반말→존댓말 바뀜·어색한 것 안 들어가게") · 트랙에만 있음, 배포 승인 대기

도구 `tools/seed_analyzer/tone_census.py` — 관리자 job 10건을 이야기 작가로 돌려(서버·읽기 전용, `SW_PATH=/tmp/sw_new.py`로 고친 코드 시험)
씨앗과 결과를 같은 자로 잰다: 줄 끝 어미 분포(존/반/연결/기타)·한 편 안 뒤섞임·씨앗↔결과 말투 다름·의태어 밀도·어절/줄·설명문·권유·신호어 중복·따옴표.
결과 JSON/HTML: `docs/tone_census_2026-09-22/round1~6 · corpus60(_r2)` · 바탕화면 `말맛센서스_코퍼스60편_2026-09-22.html` · 바탕화면 `말맛센서스_round4_2026-09-22.html`

| 라운드 | 고친 것 | 실측(10편) |
|---|---|---|
| 1 | (고치기 전) | 뒤섞임 0 · **씨앗 존댓말인데 결과 반말 2편**(차량용 홀더 씨앗) · 설명문 0 |
| 2 | `_POLITE_WORD`에 `~더라고요·거든요·는데요…` 추가 + 붙은 전사 어미 뒤 끊기(`_POLITE_SPLIT`) | 말투 다름 0 |
| 3 | 인스타 신호어를 before가 아니라 **after 줄**에 붙임 | "게다가 전에는…" 4/7 → 0 |
| 4 | 신호어 뒤 접속사 제거(`_LEAD_CONJ`) | 겹침 0 · 권유 CTA 1편 |
| 5 | 사장님 기준(청자에게 말 거는 반말 X / 혼잣말·소개체 O): 썰 브리프 "반말체"를 **실측 어미 목록**으로 교체(520편: 청자 반말 4%, 훅 명사형, 본문 ~는 거·~는데·~다는데·~버림·~였음) + 센서스에 청자반말 지표 | 청자반말 줄 0 · 권유 0 |
| 6 | **히트작 코퍼스 무작위 60편**(`--corpus 60 --seed 11`, 인스타30·유튜브30, 재료 컷 없이 말투만): 1차 권유 CTA 9·뒤섞임 2 → 인스타 브리프에 **CTA 지시가 없었다** → 실측 틀("댓글에 낱말 남겨주세요" 35%, 권유 1%) + before/after 완결 문장 지시 | 2차 60편: 권유 0 · 뒤섞임 1("~는데.") · 청자반말 1 · 인스타 35편 전부 "댓글 남겨주세요" 꼴 / 관리자 10편(round6): 전부 0 |

뿌리: 라이브 `seed_platform`이 인스타 대표 어미 **"~더라고요"를 안 세고 있었다**(docstring엔 95~98%라 적어 놓고). 히트작 1,116편(모델 라벨) 대조:
인스타 존댓말 놓침 **115→17/610**, 옛 썰 판정 유튜브 420편 중 새 판정이 인스타로 넘긴 24편은 전부 실제 존댓말("~인데요/~보세요"). 문턱(3개·3%)은 유지 — 2개로 낮추면 썰 소개체 12편이 넘어간다.
인스타 신호어 근거: 히트작 641편 신호어 146개 중 과거 불편("전에는") 앞에 온 것 0.

사장님 질문("형용사·접속사·부사·어미 더 디벨롭해야 하나")에 대한 실측 답: 품사 분석은 안 하고 있고, 씨앗 전사 통째 + 플랫폼 고정 규칙으로 말투를 맞춘다.
10편 기준 어미 일관성은 위 수정 후 0 결함. `analyze.py`의 씨앗별 `endings/openers` 추출은 라이브에 안 붙어 있다(호출 0곳) — 다음 보강 후보.

⚠️ 아직: 사장님 계정 화면 확인은 여전히 안 됐다(🟣 ⏭1). 이 수정은 트랙 커밋에만 있다 — **라이브 배포는 사장님 승인 후 `finish`**.

---

## 🟣 2026-09-22 — **이야기 작가**(대본 먼저 → 컷은 뒤에) · 여기부터 읽어라

### 경위 (09-22 새벽 세션이 주간 한도로 03:25에 끊김 — 핸드오프 없이. 낮 세션이 대화기록에서 복원)
끊긴 세션 기록: `C:\Users\CH\.claude\projects\C--Users-CH-Desktop-------\eacf4d96-3c00-4623-8ef7-7447bafb32bb.jsonl`
(이 PC는 계정 폴더가 둘이다 — `.claude`(한도 소진)·`.claude-sub`. 다른 계정 세션 기록은 저쪽 폴더에 있다.)

사장님 결정(09-22 새벽, 원문 요지):
- 저렴한 제미나이론 안 된다 → 씨앗 영상 하나를 잡고 **분석 프롬프트를 제대로** / 노바 작가처럼 **씨앗으로만 대본을 쓰고 나머지 영상은 화면 매칭만**
- 회원 100명 차별화 = B안 + 신호어는 **같은 단어를 세트·순번으로**("심지어 / 이게 대박인게 / 진짜 미친 포인트는…")
- 고조는 억지 없이·설명문 금지·단문 아님 / "억지로 썼나 검사"가 아니라 **그렇게 쓸 수밖에 없는 프롬프트**
- 인스타는 메종·홈테리어픽 분석 — 썰보다 더 스토리·상황극 / 수치·효능·인물(다이소 인맥 등) 화면 밖에서 가져와도 된다("후킹싸움")
- 마지막 지시 4건: ①**내 계정에만 라이브 스위치** ②대본 나오고 매칭 ③4초 AI 영상(Vertex) — 없는 장면을 어떻게 잡을지 + 믹스 화면에서 직접 눌러 생성 ④화면 카드 빈칸 태그(`{적용대상}`)를 예시 문장으로

### 한 것 — ①② (`afd2087fa`)
- `shopping_shorts/story_writer.py`: `make_drafts(spines, job, seconds, job_id)` = 특징+불편 추출 1회(`extract_feats`) → 자동 1안(씨앗 결, `seed_platform`으로 썰/인스타 판정) + 고른 스타일 1안 → `backbone_assemble.assign_cuts`. 화면은 씨앗 영상을 안 쓴다(`_drop_seed`).
- `app.py` 2단계 생성: 스위치 **`story_writer_enabled`**(`_setting_gate` — ""=끔 / "admin"=관리자만). 백본보다 먼저 보고, 못 만들면 이유를 `materials.backbone_skipped`에 싣고 백본/옛 경로.
- 필자 모델 = `script_generate._call_json` = `gemini-3.1-flash-lite`. **어젯밤 시험대도 같은 호출**이었다(사장님이 "지금 좋아"라고 한 결과가 이 모델).
- 점검 도구: `py tools/seed_analyzer/live_link_check.py <job.json> [스파인id]` — 줄·컷·화면설명 대조. job.json은 서버에서 읽기 전용으로 받는다(`_enrich_job_extract` 결과의 backbone_main·extract).

### 실제 job(13d4cab55fba 아일릿 펀치, 재료 5편·씨앗 뺀 쓸 컷 27개·48.8초)로 잡은 결함 — 전부 테스트로는 안 보였다
| 결함 | 실측 | 고침 |
|---|---|---|
| `write()`가 특징을 `_to_lines`에 안 넘김 | 고조 줄이 전부 구조 줄 취급 → 근거 컷을 훅·미끼가 가져감 | 넘김 + 테스트 |
| 근거를 **글로** 적게 함 | 모델이 풀어 써서("원터치 타공으로 수선 고민 해결") 글자 대조 2칸 중 1칸 실패 | 재료에 번호, 스키마 `feat`(정수)로 답하게 |
| `target_seconds` 미전달 | 45.7초 대본 → 컷 없는 줄 4 | 분량 지시 + `_fit_length`(고조 칸을 뒤에서 통째로) |
| 모델이 분량 지시를 안 지킴 | 목표 25초에 29~54초 | 한도 = min(목표×1.5, 재료 화면×0.65) — ★65%는 이 job 1건 근거 |
| 뒤 줄이 빈손 | 앞 줄이 최소 2컷+여유로 다 씀 | `_share_cuts`(빼도 대사를 덮는 컷만 옮김) |
| 말투 오판(인스타 씨앗을 썰로) | 전사 524자에 문장부호 2개 | 어절 단위 `_POLITE_WORD` |
| "심지어" 두 번 | 모델이 반전 줄을 신호어로 시작 | `_drop_repeat_signal` |

최종 6편(3회×2안): 29.0~38.3초 · 씨앗 컷 0 · 중복 컷 0 · **컷 없는 줄 0이 4편, 1줄이 2편**(둘 다 이미 고조 1칸이라 더 못 뺌 — 재료가 48.8초뿐).

### ⚠️ 아직 안 잰 것 (추측으로 메우지 마라)
- **job 1건·인스타 씨앗뿐이다.** 썰(반말) 씨앗 job, 재료가 넉넉한 job으로 다시 재야 한다.
- 이 job은 `backbone_main`이 없어 씨앗이 "한국어 전사가 가장 긴 영상" = **신박템 3가지 나열 영상**으로 잡혔고, 인스타 안 훅이 "신박템 세 가지"를 따라 쓴 회가 있었다. 사장님이 씨앗을 직접 고르는 실제 흐름에선 다를 수 있다 — 미확인.
- 고른 스타일의 `hook_angle`/`flow`는 스파인의 이름·`beat_roles`에서 기계적으로 만든 것(`_style_of`). 어젯밤 시험대는 손으로 쓴 5개 스타일 설명이었다 — 품질 차이 미확인.
- 줄↔컷 **내용** 일치: 재료 번호는 맞게 걸리지만(고조 줄에 타공·압착 컷), 이 재료는 컷이 거의 다 '압착 시연'이라 변별이 안 된다. 다른 job에서 눈으로 볼 것.
- 길이: 목표 25초에 실제 29~38초.

### 배포 상태 (2026-09-22 04:20 KST 실측)
- `finish` 통과 → main `fce834a42` → 서버 HEAD 동일, 웹 04:20:02 재시작(고객 접속 중이라 스크립트가 20분 미뤘다 — 강제 재시작 안 함), http 200, 서버 추적 파일 변경 없음.
- 스위치 **`story_writer_enabled='admin'` 켬**(전 값 `''`). 판정 대조: 관리자(0) True / 비관리자 400명 중 True 0명.
- 배포된 서버 코드로 실제 job 2건(읽기 전용·저장 없음): `73387018ca85` 실리콘 랩(재료 8편, 인스타 8줄 31.7초, 빈 줄 0, "비닐 랩 엉킴" 줄←일회용 랩 상자 컷·"쭉 늘어나니까" 줄←당기는 컷 5개) / `524372dd1368` 무선 고데기(씨앗이 **중국어 전사**, 썰 9줄 37.7초, 빈 줄 0).
- ★**화면(브라우저)에서는 아직 못 눌러봤다** — 이 세션(보조 계정)엔 크롬 확장이 연결 안 됨. 로그인 쿠키를 서버에서 만들어 내는 건 사장님 로그인 위조라 안 했다. → 사장님이 제작소 2단계에서 생성해 `made_by: 이야기작가` 안이 뜨는지 봐야 끝이다.

### ⏭ 다음
1. **사장님 계정으로 제작소 2단계 실제 생성** → 화면 확인. 이상하면 먼저 끈다: `story_writer_enabled=''`
2. 위 ⚠️ 재기 + 새 발견: ①목표 25초 한도 때문에 **고조가 1칸으로 잘리는 일이 잦다**(사장님 "고조 한두 개면 심심하다"와 충돌 — 길이 목표를 늘릴지 결정 필요) ②모델이 직접 쓴 줄끼리 같은 신호어("심지어")로 두 번 시작(미끼·반전) — 지금 제거기는 코드가 박은 신호어와의 중복만 본다 ③씨앗 전사가 외국어일 때(중국어) 결을 못 가져온다 — `full_text_ko` 유무 확인
3. ③4초 AI 영상 — 설계부터(없는 장면 = `no_cut_lines`·`_group_of` -1 줄이 출발점 후보) ④화면 카드 빈칸 태그

---

## 🟢 2026-09-21 밤 — **단순 필자 하네스**(지시 5줄·가드 4개) · 여기부터 읽어라

사장님: "새로 쓴 대본처럼 프롬프트를 최대한 간단하게 주고 하네스를 해줘 / 규칙이 길다고 좋은 게 아니고 가드가 세다고 좋은 게 아니다 / 꼭 필요한 지시와 가드만."

- `tools/simple_writer/` — `brief.txt`(지시 전문) · `dump_materials.py`(서버: 라이브와 같은 함수로 재료를 읽기 전용으로 내놓음) · `harness.py`(이 PC: 프롬프트 조립 → Vertex 필자 1회 → 코드 가드 → 걸리면 1회만 다시 → 비교 HTML)
  실행: `py tools/simple_writer/harness.py --works <작업ID,…> --models gemini-3.6-flash,gemini-3.1-flash-lite`
- 가드 4개(전부 코드·모델 0회): ①첫 줄=씨앗 첫 문장 ②줄을 나눴나 ③씨앗 통째 베낌 30% 미만 ④재료에 없는 숫자 없음. **길이 가드 없음.**
- 근거(`tools/draft_census.py`, 고객 후보 1,639편/14일): 재작성 사유의 **70%가 '말 밀도'** 하나, 편당 평균 쓰기 3.13회(최대치 4회가 50%), 그러고도 **가드 탈락 표시로 나간 것이 712/1,321**. 오용형 스타일은 9편(1%)뿐 — "이상한 대본의 뿌리=오용 거푸집"은 1건 보고 부풀린 것이었다(정정).
- 실측(관리자 작업 10건, 결과 `docs/writer_compare_2026-09-21/round5_하네스_결과.html`):
  | | 첫 시도 통과 | 최종 통과 | 편당 호출 | 시간 | 베낌 | 편당 비용 |
  |---|---|---|---|---|---|---|
  | gemini-3.6-flash | 10/10 | 10/10 | 1.00 | 13초 | 4.5% | $0.0105 |
  | gemini-3.1-flash-lite(지금 필자) | 4/10 | 8/10 | 1.60 | 3초 | 17.0% | $0.0013 |
  프롬프트 3,300~4,500자(라이브 26,369자), 호출 1회(라이브 편당 약 4회+분류 1회).
- ★하네스에서 내가 낸 결함 2개(고침): 가드가 '첫 줄 빼고' 재서 **한 덩어리 복사를 0%로 통과**시켰다 / 지시문의 예시 말버릇("이건 바로"…)을 무료 필자가 그 말버릇이 없는 씨앗에도 집어넣었다(10건 중 6건) → 예시 삭제.
- ⚠️ 아직: "가드 통과"는 고장이 없다는 뜻이지 잘 썼다는 뜻이 아니다 — 사장님이 HTML을 읽고 판정해야 한다. 씨앗 선정이 재료와 다른 제품을 고르는 작업이 있다(89a1172e1a55: 재료는 테이블인데 씨앗은 냄비 접시) — `seed_source`가 '한국어 전사가 가장 긴 영상'을 고르기 때문. CTA가 씨앗에서 반복되면 대본에도 반복된다("댓글 아무거나 남겨주세요"×4).
- ⏭ 사장님 판정 뒤: 이 필자를 실제 프로그램 대본 쓰기 단계에 **관리자 스위치 뒤로** 붙이기(서버엔 Vertex 인증이 없다 — 서비스계정 키 필요). 고객 경로라 승인 없이 라이브 금지.

### ➕ 2026-09-21 밤 추가 — 하네스에 **훅 유형**·**고객이 고른 스타일 경로** (결과 `docs/writer_compare_2026-09-21/round6·round7`)
- `--hooks "틀1||틀2"`: 첫 줄을 씨앗에서 안 가져오고 훅 틀을 코드가 하나 준다. 프롬프트 +1줄·호출 +0. 훅 틀은 188개(승인 스타일 26개 중 19개, 중앙값 21자).
  ⚠ 훅은 말투·화자를 끌고 온다 — 존댓말 1인칭 훅을 반말 소개체 씨앗에 얹으면 첫 줄만 따로 논다(3건 중 2건). 자동 안에선 씨앗과 말투가 같은 훅만 고를 것.
- `--styles 61,59,52,67`: **스타일 선택은 유지된다.** 승인 스타일엔 히트작 원문이 0건이고 실체는 `칸 순서 + 칸별 문장틀 3~11개`다 → 코드가 칸마다 틀 하나씩 골라 8~10줄 '스타일 뼈대'를 만들고, 모델엔 "빈칸 채우기가 아니다 — 첫 줄만 틀 그대로, 나머지는 같은 말투로 재료 보고 새로 써라"(`brief_style.txt`). 담은 영상은 전부 재료.
- 실측(재료 5 × 스타일 4): 3.6 Flash 20/20 통과(첫 시도 19)·1.05회·11초·$0.0102 / 무료 필자 16/20·1.35회. 말투 섞임 없음(존댓말 스타일 8/8줄, 유튜브 반말 0/5줄).
- ★읽어야 보이는 차이: 무료 필자는 뼈대를 **빈칸 채우기로** 취급해 틀 문장을 말이 안 돼도 그대로 둔다 — "도대체 왜 안 고쳐지나 싶어서"(스테이플러), "그냥 두기만 하면 되는 거라"(빗), "가성비밖에 안 한다는 점이에요", "새집처럼 …영수증 정리까지 환해진거 있죠", 재료에 없는 "만 원대". 3.6 Flash는 같은 자리를 재료에 맞게 고쳐 쓴다("심지어 {가격}…" → "심지어 가방에 쏙 들어가는 크기라…"). **사장님이 말한 '이상한 말'의 정체가 이것**이고, 여기서는 모델 차이가 크다.
- 가드 보강: 한글로 쓴 값("만 원대"·"반값")도 재료에 없으면 잡는다.

---

## 🔴🔴🔴🔴🔴 2026-09-21 심야 — **여기부터 읽어라**: 이상한 대본의 뿌리는 모델이 아니라 **프로그램이 모델에게 주는 입력**

사장님: "시험에선 차이가 크지 않은데 실제 프로그램에서 돌리면 이상한 대본이 나온다."
`tools/live_path_capture.py`로 고객 실제 경로(`/api/wiki/generate` 스타일 경로)를 격리 프로세스에서 그대로 돌려 프롬프트·원본 응답·최종 대본을 가로챘다(work 7f7d2393eb0b, 스타일 70·72). 원문 `docs/writer_compare_2026-09-21/round4_실제경로/`.

- **대본 2편에 모델 9회**: 제품군 분류 1(14,546자) + 편당 [쓰기 26,369자 → 화자·소재 판정 1,2xx자 → 길이 게이트(말 밀도 129~185자) 탈락 → 다시 쓰기 26,5xx자 → 다시 판정].
- **프롬프트 26,369자**(시험실 지시문 7,769자의 3.4배): 소스 블록 [대본1~7] 약 18,000자("훅: (미상)" 같은 빈 필드 포함) + 스타일 지시 939 + 남의 히트 대본 3편 1,244 + 장면 6,644 + 은행 1,360 + 규칙.
- ★**이상한 대본의 정체**: 고른 스타일 70 「OO도 예상 못한 미친 활용법」은 `미끼→유래→발견→사례(초보/고수)→고조→재반전`이라는 **오용 이야기 거푸집**이다. 이 빗엔 딴 용도가 없는데 거푸집이 최우선("충돌하면 이 스타일이 이긴다")이라 모델이 **원래 용도를 '미친 활용법'이라고 우기는 이야기를 지어낸다**("초보들은 기껏해야 두피에 토닉을 바르는 용도로만 / 고수들은 마사지 도구로").
- ★**같은 프롬프트를 3.6 Flash·2.5 Pro에 그대로 넣어도 똑같이 나온다**(same_prompt_*.json). → 필자 교체만으론 안 고쳐진다.
- 백본 경로엔 "오용형은 재료에 딴 용도 장면이 있을 때만"(`assemble`의 alt_use 가드)이 있는데 **고객 경로엔 없다**(0순위-B).
- 시험실 결과 요약: 깨끗한 지시문(`round3_고조칸/brief2.txt`)이면 무료 필자도 베낌 42→27%로 좋아지고 3.6 Flash는 7%. 즉 지시문 효과가 모델 효과만큼 크다.

### ⏭ 다음(사장님과 방향 합의 필요 — 고객 경로라 승인 없이 라이브 금지)
1. 스타일 적합 판정을 고객 경로에도: 이야기 거푸집 스타일(오용형 등)은 재료에 그 이야기가 있을 때만. 없으면 훅만 빌리고 본문은 재료대로.
2. 호출 9 → 3: 길이는 지시문+코드 트림으로(길이 때문에 26,000자 재호출 금지), 화자·소재는 코드 검사, 실패 때만 1회 재작성.
3. 프롬프트 26,000 → 8,000자대: 빈 필드·남의 히트 대본 3편·은행·중복 규칙 제거, 재료는 컷 표 하나로.
4. 그 뒤에 필자(3.6 Flash, Vertex) 교체를 같은 시험으로 잰다.

---

## 🔴🔴🔴🔴 2026-09-21 밤 — **여기부터 읽어라**: 뿌리는 경로가 아니라 **필자(모델)**였다

사장님: "아무리 좋은 대본을 벤치해도 결과물이 안 나온다 / 너가 직접 보고 쓴 것만 좋고 제미니가 쓴 건 꽝".
- 대본 필자 = `gemini-3.1-flash-lite`(`comment_gen.py:23` → `script_generate._MODEL`). 제미니 최소 등급에 JSON 양식 채우기+금지 규칙 12줄.
- 같은 재료(work 7f7d2393eb0b)·같은 새 프롬프트("다 읽고 한 편 써라")로 4필자 × 2회 — 원문 `docs/writer_compare_2026-09-21/`
  - flash-lite: 프롬프트를 바꿔도 씨앗 문장 거의 복사·훅(출산 맘)과 본문(사람들) 따로 → **프롬프트 탓이 아니라 모델 탓**
  - haiku: lite와 같은 급(1회는 복사, 1회는 범용). 115~135초(이유 미확인)
  - sonnet: 1회는 논리 깨짐("원래 …채워 넣고"), 1회는 씨앗 거의 전문 복사(남편 선물 줄 100%)
  - **opus: 2회 다 재료 s2의 "출산 후 탈모…손으로 하려니 묻고 흐르고"를 찾아 훅↔본문을 이음**, 복사 0, 끝을 끊어 반복재생. 13~15초
- ⚠️ 재료 1건·필자당 2회뿐이다. 재료를 넓혀 확인해야 한다.
- 서버에 `claude` CLI 2.1.201 설치됨(/usr/bin/claude), `dashboard/briefing_weather.py`가 `claude -p`+제미니 폴백 구조를 이미 쓴다. 인증파일 07-21자 — 토큰 스테일 미확인.
- ⏭ 사장님 결정: 필자를 Opus로 바꿀지(Max 구독 한도 vs API 과금). OK면 재료 5건+로 넓혀 격리 확인 → 대본 쓰기 한 단계만 교체(특징 묶기·컷 배정·검사는 그대로).
- 부수 발견: 씨앗 뼈대 프롬프트 첫 줄이 "조회수 0회가 나온"으로 나간다(origin_from_seed의 views가 비어 있음) — 미수정.

---

## 🔴🔴🔴 2026-09-21 저녁 — **여기부터 읽어라** (아래 "오후 최종"보다 최신 · 라이브 변화 0)

### 한 것 — ⏭1번(씨앗 뼈대 A안 결함)을 **격리 테스트까지** 끝냄. ★라이브 미배포(사장님 승인 대기)
트랙 브랜치에만 있다. 라이브 상태는 "오후 최종" 블록 그대로(고객·사장님 예전 화면, `_s2SeedAuto()` 끔).

| 고친 것(`backbone_assemble.py`) | 왜 |
|---|---|
| `write_lines_from_origin`: origin이 씨앗(`seed=True`)이면 **전제·상황표·cast_gap을 건너뛴다** | 상황표는 남의 히트작 인물을 새로 짜는 장치 — 씨앗(같은 제품)엔 거꾸로 작동해 화자가 바뀌었다 |
| 씨앗용 규칙: 화자·말투 원문 그대로 / 1인칭 사연·새 인물 금지 / 칸 첫머리 관용구 글자 그대로 | 〃 ("원문 제품 이야기 한 조각도 남기지 마라"도 씨앗일 땐 뺌) |
| `seed_voice_gap`(신규, 모델 호출 0회): 칸 수 · 말투(비율+한결같은 씨앗은 줄 단위) · 고정 관용구 · 본문 6어절 통째 옮김 | 어긋나면 1회 재작성, 그래도 남으면 **반려**(assemble_clean이 `note.cast.issues`로 본다) |
| `origin_from_seed`: 관용구를 `fixed`로 **원문 글자 그대로** 뽑음(원문에 없는 말은 버림) · 빈손이면 1회 재시도 · 관용구뿐인 칸은 다음 칸에 붙임 · 실패는 `note.seed_origin_failed` | 첫 어절만 세면 "이게 말도 안 되는 게→이게 정말 편한 게"를 못 잡는다 / 칸 나누기가 6~7칸으로 흔들림 |
| `_one_full_name`: reveal 줄이 없으면 **처음 나온 제품 이름은 남긴다** | 원문형·씨앗 경로엔 reveal 역할이 없어 "이건 바로 빗"이 3회 중 3회 나왔다 |
| `assemble` meta 이름표: 씨앗 뼈대면 "씨앗 그대로" | 스파인 이름("유튜브 「OO을 구원한…」")이 떴다 |

### 격리 실측 (work `7f7d2393eb0b`, 씨앗 s6 두피 액체빗 · 도구 `tools/seed_origin_patchcheck.py`)
| | 고치기 전(라이브 파일, `--live`) | 고친 뒤(v5, 3회) |
|---|---|---|
| 말투(존댓말 비율) | 씨앗 0.00 → **0.80** ("…소문난 스칼프 도포 빗이라네요") | 0.00 → **0.00** (통과 2회 모두) |
| 칸 수 | 6 → 5 | 6 → 6 |
| 관용구 3개 | 1개만 | **3/3** |
| 6어절 겹침 | 1.3% | 1.3~1.4% |
| 이름표 | 스파인 이름 | 씨앗 그대로 |
| 통과 | 1/1 | **2/3** — 1회는 통째 옮김이 재작성 뒤에도 남아 반려(라이브는 후보 스파인 수만큼 더 시도한다) |

### ⚠️ 남은 것 — 숫자에 안 잡히는데 읽으면 보이는 것
- **훅과 본문의 대상이 다르다**: 훅은 씨앗 그대로 "출산 맘들…"(enforce_hook, 09-20 사장님 지시)인데 본문은 "두피 고민 많은 사람들/바쁜 현대인들". 씨앗 끝 칸(남편 선물→사랑받기)도 "가방에 쏙"으로 바뀐다. "[재료] 화면에 없으면 빼라"가 이긴다 — 프롬프트 한 줄("훅이 부른 대상을 바꾸지 마라")로는 안 고쳐졌다(v5 2/2). **사장님 결정 필요**: 씨앗의 대상·사연을 재료에 없어도 살릴지.
- 제품 이름이 회마다 다르다(스칼프 케어 롤링 헤어 빗/에센스 주입 빗) — `build_groups`의 product가 매번 달라서다. 씨앗 brief의 product("두피 액체빗")로 고정할지는 미정.

### 함정(이번에 밟은 것)
- 격리 폴더에 `shopping_shorts/`가 있어 **스크립트를 거기 두면 라이브 패키지를 가린다**(ImportError). 도구가 `sys.path`에서 자기 폴더를 빼고 cwd를 앞에 둔다.
- 점검 도구가 칸을 **따로** 나누면 대본을 만든 칸과 수가 달라 비교가 무효(7 vs 6) → `note["seed_cells"]`를 읽는다.
- heredoc으로 파이썬에 한글을 넘기면 cp949로 깨진다 — 한글이 든 치환은 Edit 도구로.

### ⏭ 다음
1. **사장님 확인**: 위 전/후 대본을 보고 OK면 `finish`(승인 후). 관리자 스위치 뒤 경로라 고객 화면 변화는 없지만 **승인 없이 올리지 않는다**.
2. 위 ⚠️ 두 가지(대상·사연 / 제품 이름 고정) 결정 → 격리 테스트
3. B안 방향(①씨앗 뼈대+스타일은 훅만 / ②틀 유지+길이만) — 사장님 선택 대기(변동 없음)
4. 씨앗 자동 재점화(`_s2SeedAuto`) — 1~3 OK 뒤, 승인 후 배포

---

## 🔴🔴 2026-09-21 오후 최종 — **여기부터 읽어라** (아래 "낮" 블록보다 최신)

### 지금 라이브 상태 (15:45 반영, 사장님 계정으로 라이브에서 직접 눌러 확인)
- **고객·사장님 모두 어제(09-20) 21:22 이전 화면**: 2단계 「📌 픽업영상 대본(기본값)」 + 스타일. 픽업은 끄고 켤 수 있고,
  끄면 스타일 2개. 안은 최대 2개. 안내 문구도 예전 것("N개 스타일 → N안").
- **씨앗 자동 안은 꺼짐** — 판단은 `produce.html` `_s2SeedAuto()` **한 곳**(지금 `return false`). 켜면 🎯 자동 안 잠금·
  픽업 폐지·씨앗 고르면 화면 재료에서 뺌·새 안내 문구가 한꺼번에 돌아온다.
- **서버 백본 경로는 그대로 `backbone_script_enabled = admin`** — 사장님이 스타일을 고르면 서버가 백본(씨앗 뼈대·재료 태깅·
  씨앗 화면 제외)으로 만든다. 고객은 예전 스타일 경로(틀 조립 시도 → 제미니 generate_one_style → 게이트).

### ★사고 — 내 09-20 화면 변경이 고객에게 샜다 (재발 방지)
`126e6c07d`(09-20 21:05, 21:22 배포)가 produce.html에서 **관리자 구분 없이** 픽업 대본을 없앴다. 대신 넣은 '씨앗 유형 자동'은
서버에선 관리자만 만들어져 **고객은 약 17시간 주력 안(픽업)을 잃었다**(09-20 21:22 ~ 09-21 14:36).
같은 꼴 2건 더: `24bada30c` 안내 문구, `fc6f97a76` 씨앗→useFootage 끄기 — 전부 스위치 없이 화면에 들어갔다.
→ **서버가 관리자 스위치 뒤인 기능은 화면도 같은 스위치 뒤에 둬라(짝).** CLAUDE.md 0순위-A1c(고객 라이브 승인) 참고.

### 커밋(전부 main·라이브)
`52f4f682b` 씨앗 화면 제외 관리자만 · `00cd1372f` 고객 픽업 복구 · `310935664` 주석 내부용어 제거(test_skm_ui) ·
`60b160393` 안내 문구 고객 예전 · `fdec7645c` **사장님도 예전 버전 — 분기 6곳을 `_s2SeedAuto()` 하나로, 끔**

### 함정(오늘 밟은 것)
- test_s2_an_count_label_js는 `s2AnCount`를 **한 줄로** 뽑고, `hint` 블록~`function s2ClearStyles()` 구간을 잘라 node로 돌린다
  → 여러 줄로 쓰거나 그 구간 끝에 함수를 끼우면 깨진다. node엔 `window`가 없다(`typeof` 가드).
- test_skm_ui: produce.html에 **"백본" 글자 금지**(주석 포함 — 고객 브라우저로 내려간다).
- `_merge-<트랙>` 폴더를 손으로 지우면 git worktree 등록이 남아 finish가 "missing but already registered"로 죽는다 → `git worktree prune`.
- 서버 반영은 **새벽 2~6시**뿐. 낮엔 커밋 제목 `[긴급]` 또는 `touch /home/ubuntu/DEPLOY_NOW`(사장님 승인).
- produce.html 관련 테스트의 **기존 실패 3건**(origin/main에서도 실패): test_s2_mine_draft 1 · test_s2_review_fallback_ui 2.

### ⏭ 다음 (사장님 결정 대기 포함)
1. 씨앗 뼈대 A안 결함(화자 1인칭 존댓말로 바뀜·관용구 칸 누락) — 격리 테스트(patchcheck)로만. 뿌리: 원문형 프롬프트의
   "원문의 인물 배치를 베끼지 마라"+상황표 규칙이 씨앗(같은 제품)일 땐 반대로 작동 → `origin.get("seed")`면 끄기
2. B안(틀형) 방향 — ①씨앗 뼈대+스타일은 훅만(추천) / ②틀 유지+길이만 — **사장님 선택**
3. 씨앗 자동을 다시 켤지(`_s2SeedAuto`) — 1·2가 격리 테스트로 사장님 OK 받은 뒤에만, **승인 후 배포**

---

## 🔴 2026-09-21 낮(회사) — 여기부터 읽어라 (계정 교체로 세션 끊길 수 있음)

### ★사장님 규칙(오늘 새로): 대본 수정은 **라이브 올리지 말고 격리 테스트**
트랙에서 고친 `backbone_assemble.py`를 서버 `/home/ubuntu/patchcheck/shopping_shorts/`에 올리고
importlib로 그 파일을 불러 실제 재료로 돌린다(스크립트 예: 세션 scratchpad `v3_check.py`). 결과를 보여주고 **"올려" 할 때만** finish.
서버 반영은 **새벽 2~6시 시간창**뿐 — 낮엔 `touch /home/ubuntu/DEPLOY_NOW`(사장님 승인 필요). 오늘 "3분 뒤 반영"은 남의 [긴급]에 편승한 것이었다.

### 오늘 배포된 것 (라이브, 12:35 웹 재시작, 서버 HEAD 41a56e327에 포함)
| 커밋 | 무엇 |
|---|---|
| 9312b68db | 원문형 경로: 재료 태깅 전문(material_block)·훅 빈칸 원문 낱말(_tpl_slots)·이어지는 칸 마침표 안 붙임·칸 수 고정 |
| fc6f97a76 | 고른 스타일 틀형도 태움(app.py picked_c 필터 해제)·씨앗 내용 투입(seed_block)·**화면에서 씨앗 제외**(produce.html s2PickSeed→useFootage off, assemble _drop_seed)·seed_source 한 곳 |
| 31dea2df6 | 자동 안 뼈대=씨앗 전사(origin_from_seed, use_seed_origin 인자)·틀 경로에도 material_block·길이 씨앗 기준 |

배포본 실측: 씨앗 컷 0·재료 5편 전부 사용(work 7f7d2393eb0b 재료로 검증).

### ⏭ 남은 것 (사장님 화면 확인 결과, 12:40)
1. **A안(씨앗 뼈대)** — ①씨앗 관용구 칸 빠짐("이건 바로 OO"/"이게 말도 안 되는 게"/"근데 진짜 미친 포인트는"), 씨앗 7덩이→5줄
   ②**화자 바뀜**: 반말 소개체 → "저도 아내 쓰라고 장만했습니다" 남편 1인칭 존댓말.
   뿌리(코드 확인): write_lines_from_origin 프롬프트의 "사람·장소는 [상황표]를 따른다. 원문의 인물 배치를 베끼지 마라"
   + build_cast/cast_gap — 남의 히트작용 규칙이 씨앗(같은 제품)일 땐 반대로 작동. **origin.get("seed")면 상황표 끄고 씨앗 화자·말투 그대로**
   ③라벨이 스파인 이름("히트작 발명품형…")으로 뜸 → "씨앗 그대로"로
2. **B안(틀형)** — 17.3초·"구멍의 정체"·"진짜 깜짝 놀랐다고" 얇음. 틀 조각이 짧은 구조 한계.
   제안 ①B도 씨앗 뼈대 + 고른 스타일은 훅만 바꿈(추천) / ②틀 유지+길이만 — **사장님 선택 대기**
3. 틀 경로 "거기다"→"거다" 오기(후처리 4함수 재현 안 됨 → 모델 출력단 추정), 어미 `~는 거` 4줄 연속
4. 3단계 EDL은 기존 job 옛 배치 — 새 매칭해야 씨앗 제외 반영

---

## 🔴 2026-09-18 집 세션 — 여기부터 읽어라 (회사에서 이어할 것)

### 한 것 (전부 실측 확인)

| # | 무엇 | 결과 |
|---|---|---|
| 1 | **컷 밋밋함 수리** | 컷당 6.5초 → **1.76초**, 7컷 → **14컷** |
| 2 | **효과음 자산 투입** | 뇌전구 팩 79개 등록, 배치 **0/7칸 → 7/7칸** |
| 3 | **효과음 이븐쇼핑 맞춤** | 길이 중앙 **96ms**(기준 80ms), 200ms이하 **100%** |
| 4 | **대본 프리셋** | 스파인 55에 원문 뼈대 6~10개씩 → 조합 **2,304만** |
| 5 | **어미 판정 게이트** | 같은 어미 3줄 연속 반려 |
| 6 | **고객 상속 ON** | `edl_inherit_enabled` admin → **1** (라이브 적용) |
| 7 | **규칙 A0 신설·A1 확대** | CLAUDE.md |

### ★뿌리 3개 (다음에 또 헷갈릴 것)

1. **밋밋함은 백본 경로 전용 문제였다.** 최근 60잡 실측: 줄당 컷 중앙값 **3.0**으로 일반 경로는 원래 정상.
   컷 1개가 절반 넘는 잡 4개(7%)가 **전부 백본 실증본**이었다. 실사용 경로(2단계 `src_seg`)는 모델이
   줄당 **2~4개**를 잘 적는다 — 내가 "구조적으로 1개"라고 단정한 건 `src_segs`를 안 본 오판.
2. **렌더에는 2.2초 라운드로빈이 이미 있다**(`_plan_beat_clips:940`) — 단 `len(segments)>1`일 때만.
   컷이 1개면 else로 빠져 통째 재생된다. 그래서 고칠 자리는 렌더가 아니라 **컷 배급**이었다.
3. **어색한 어미는 내가 조립한 것이었다.** `~해 버림`·`~는 거임`·`~였음`은 이븐쇼핑 원문에 없다.
   원문 13편에서 문장을 통째로 떼어 쓰니 해결(`~라는데/~다는 거/~는데/~이라고`).

### ★씨앗 구조 — 내가 두 번 틀린 곳

- **씨앗은 "대본 결"을 주지 않는다.** 실사례 work `40148f06f529`(루프 이어플러그): 씨앗이
  `grab_tiktok_b980ca6ad3ad`인데 **해외 틱톡**이고 대본 원문은 **`🎵`** 한 글자다.
  그런데 결과 대본은 이븐쇼핑 은폐형(`~임`, CTA 없음) — **말투·골격은 선택한 스파인이 정한다.**
  씨앗은 **제품·소재를 고정**하는 역할이다.
- ⚠️내 실패: 해외 소스 중 말 많은 것을 씨앗으로 골라 돌렸더니, 중국 밤 보관법 영상에서 '지퍼백'을
  제품으로 뽑아 **없는 천재·떼돈을 지어냈다**(job `sd31b299ef77`). 소재와 스파인이 안 맞으면 이렇게 된다.
- 사장님 미해결 질문: **"밤 보관법 소재에 발명품형을 고르는 고객이 있을 수 있잖아"** — 아직 아무도 안 막는다.

### 실측 수치 (근거)

- 이븐쇼핑 효과음: 컷 경계 그 자리(Δ중앙 **0ms**), 반짝 **80%**/붐 17%, 길이 중앙 **80ms**
- 뇌전구 팩 79개: 길이 중앙 **421ms**·200ms이하 **29%** → 그냥 쓰면 1.5초 드럼이 컷을 덮는다.
  **200ms 이하 23개만** 쓰도록 `_SFX_MAX_SECS=0.20`으로 거른다(이름 아닌 **잰 길이**로)
- 훅 효과음 = **`pop4`** (뇌전구 8편 전 구간 동일, `i%4==0`)
- 어미 분포(히트작 200편·문장 986개): 최다 `~음` **9.3%** — 어느 어미도 10%를 안 넘는다
- 대본 코퍼스: `script_wiki` **4,986편**(인스타 2,657·유튜브 916) + 히트작 자막 200편

### ⏭ 회사에서 이어할 것

1. **프리셋 대량화** — 55번만 했다. 나머지 유튜브형 7개(68·67·66·65·69·60·56)와 **인스타 스파인**도.
   재료는 이미 있다: `raw/analysis/썰쇼핑_히트작200_2026-08-20/hits_subs.json` + `script_wiki` 4,986편.
   오용형 재료는 이미 캐뒀다(title 47·origin 26·notice 22) — **56번에 넣으면 된다**(근거 4편뿐이라 빈약).
2. **모델 없이 껍데기 채우기** — 시험은 했는데 **어간+어미 결합이 깨진다**(`끝는데`·`만들다는 거`).
   조사는 받침으로 자동 선택하면 되지만(성공), 활용은 형태소 수준이라 코드로 다 못 잡는다.
   → `{효능}`을 **어미까지 완성된 형태**로 받는 쪽이 현실적이다.
3. **소재↔스파인 불일치** — 사장님 판단 필요(경고만 할지, 자동 조정할지, 그냥 둘지)
4. 밈 **0/7칸** — 57장 등록했는데 안 붙는다. 컷어웨이는 Gemini가 "나레이션이 말하는 그 사물을
   실제로 보여주는가"로 판정하는데 pepe 밈은 제품을 안 보여줘 전부 탈락하는 것으로 보인다(미확인)

### 산출물 (바탕화면 `백본확인/`)

영상 9편 — `씨앗_해외8편_밤보관.mp4`(전 과정 실증) · `효과음_이븐쇼핑맞춤.mp4` · `조각냄_수리본.mp4` ·
`A2_유튜브_골격적용.mp4`/`B2_인스타_골격적용.mp4`(스타일 비교) · `씨앗원본_s4_밤보관법.mp4`(원본) 외
대본 3건 — `이븐쇼핑_대본10개.txt`(코드만으로 생성) · `이븐쇼핑_모델생성5개.txt` · `케이크장난감_세트5개.txt`

---

## 결론 먼저 (2026-09-16 기록 — 아래는 그대로 둔다)

1. **근본 방향이 틀렸다** — 사장님이 07-19에 "문장마다 장면 찾기 자체가 오류"라고 했는데, 그 뒤 두 달간
   14번 전부 "문장↔장면 매칭을 더 잘하는 법"이었다(오늘 내 것 포함). 채우기(`_fill_beat_screen_time`)는
   07-31·07-31·08-10·08-16·08-18·08-26 여섯 번 고쳤다. 전부 두더지.
2. **맞는 방향 = 백본-먼저**(07-21 설계 `docs/superpowers/specs/2026-07-21-대본학습층-응용-백본인터리브-팩토리-design.md`,
   "다음 최우선"이라 적혀 있었으나 미구현). 오늘 **손으로 1편 실증 성공** — job `bb1cc3402937`.
3. 사장님이 정리한 구조: **`[훅] + [특징1..N] + [CTA]`. 각색 = 훅 갈아끼우기 + 특징 순서 섞기 + 특징별 말 바꿔쓰기 +
   화면은 서브 우선.** 특징마다 원본에 (말, 화면) 짝이 이미 있어 빈칸이 안 생긴다.

## 오늘 실측 (전부 서버 reference.db·실제 job)

### 사장님 제보 job 26698eb0a362 (팬케이크) — 왜 중복·불일치가 났나
- 2단계 지목: 10줄 → 10컷 12.9초 / 대사 24.8초 = **결손 52%**
- 3단계 `_fill_beat_screen_time`이 빈 11.9초를 16컷으로 메움 → 최종 26컷, 같은 그림 15/26
- 대조군 68dc7e13dc23(볼펜): 결손 17% → 중복 0. **결손률과 덧붙은 컷이 정확히 비례**
- 뿌리 2: `build_inherit_plan`(09-04 신설)만 배속 보정을 못 받아 목표 초 **1.7배** 과대(실측 9.69자/초 vs 5.7 계산).
  08-10에 4곳 고치고 7곳 빠뜨린 것. → `edit_plan.narr_secs()` 단일 출처로 상속 경로만 통일(**유지, 라이브**)

### 시도하고 철회한 것 — 2단계 분량 요구 + 게이트 판정
같은 조건(재료 26698·스파인 52·grounded) 4회: 분량판정 있음 83% / 충돌제거 83% / 철회본 78% / 무관판정OFF 80%.
**차이 없음.** 모델이 지시를 안 따르고 게이트는 3회 반려 뒤 passed=False로 그냥 돌려준다.
→ 메모리 `reference_prompt_says_but_nobody_checks` 그대로. 철회 커밋 e2904f384.
⚠️ 내가 중간에 "역효과(48→83%)"라고 보고한 건 **오판** — 원본은 given_script 경로, 나는 생성 경로를 재고 있었다.

### 백본-먼저 실증 — job bb1cc3402937 (한셉트 제로 MK2, 원본 tjhKrXqsTGI)
- 재료: 기존 job 68dc7e13dc23의 URL 7편(원본 유튜브 1 + 렌즈로 담은 서브 6) 그대로
- 대본 9줄 손으로(훅 [66] 답답했잖아 + 특징 순서 지갑→접착제→육각형→펜촉→54부품→정밀→거치대→CTA)
- `create_mix_job(given_script=, script_structure={beat_sources:[{role,seg,segs}], inherit_scenes:True}, customer_id=0)`
  → `run_mix_job` → `run_render`. 스크립트 `out/backbone_job_mk2.py`(서버 /tmp에도)
- 결과: **상속 9/9 · 지정 11컷 → 최종 13컷 · 같은 그림 0 · 빈칸 0**(화면이 대사의 2.2배 여유). 23.6초 1080×1920
- 산출물(out/ + 바탕화면): `백본_한셉트제로MK2.html`(뼈대+훅4종) · `특징정리_한셉트제로MK2/index.html`(46특징→10묶음, 컷 이미지 36장)
  · `백본실증_한셉트MK2_bb1cc3402937.mp4` · `비교_원본vs각색_한셉트MK2.mp4`(좌 원본/우 각색, 소리 좌우 채널)

### 41컷 눈 판정 (컷판정_시트.jpg)
- 그대로 씀 **21**(틱톡 0ddb76·422b8d·8f40be·a427b7) / 크롭하면 씀 **16**(원본 전부 — 상단 뉴스 UI **15%**=165행, 남는 608×915)
  / 폐기 **4**(be4dc2 2컷 상단 **53%** 일본어 로고 인트로 · xhs 2컷 다른 제품 피젯펜)
- 앵커 5동작 중 접기·육각형·펼치기는 서브 깨끗한 컷으로 덮이고, **펜촉·필기만 원본 크롭 필요**
- ★태깅 스키마에 "화면 가림(UI/로고/큰 자막)" 판정이 **없다**(is_key·shot_role·scene_desc·label·use_point·change·action·text뿐)

## 밤에 더 한 것 (2026-09-16 저녁~밤)

- **재렌더 v2·v4 완료** — 로고 컷(be4dc2) → 422b8d-1 교체 + 원본 3컷 상단 크롭(`out/backbone_job_mk2_v2.py`) + 이븐쇼핑 프레임.
  ★원본 UI는 **30%**(눈으로 잰 것 — 픽셀 휴리스틱은 두 번 다 틀렸다: 흰 바만 15%, 파란 바가 채도를 속임).
  이븐쇼핑 프레임(제목 포함) 불투명은 **17%**라 혼자선 못 덮고, 크롭 15%+프레임 17%로 덮었다(v4 `bb16f14a984d` 깨끗).
  ★deco 저장은 `update_mix_job(deco=dict)` — `deco_json=`은 화이트리스트에 없어 **에러 없이 무시**된다(코드 주석에 적힌 함정, 렌더 1회 헛돎).
- **조립 자동화 완성·실증** `shopping_shorts/backbone_assemble.py`(커밋 c7fa690d9) — 모델 2회(특징 묶기+순서 / 문장) + 코드(컷 지정).
  자동 job `ba630a537511`: 묶음 5 · 7줄 전부 서브 화면 · **7컷→7컷 덧붙음 0 · 같은 그림 0** · 24.8초 렌더. 서버 `/tmp/bb_auto.py`로 돌렸고 API/UI 배선은 없다.
  사장님: "**자동은 나중에**" — 트랙 브랜치에 커밋만. 남은 구멍: a427b7-0 'Hancept Z' 워터마크가 들어감(화면가림 태깅 필요) · 제목이 제품명 그대로라 2줄.

## 마무리 결론 (2026-09-16 밤, 사장님 "자동은 나중에, 대본·매칭 마무리")

- **채우기 정렬 수정 병합** — 낱말 겹침(09-08 기각) 제거, 지목 컷 바로 다음 컷 우선. 실측 팬케이크 26→23컷·볼펜 20→16컷·손조립 역행 2→0. **소폭.**
  '같은 그림'을 맨 앞 기준으로 올렸다가 되돌림(다른 소스 0.9초 조각이 앞으로 와 08-18 "눈 아프다" 충돌). 같은 그림 회피는 같은 소스 안에서만.
- ★**"대본 끝 = 매칭 끝"은 아직 아니다.** 2단계가 줄당 컷 1개(0.6~1초)만 주고 대사는 2.5초라 빈칸이 구조적. 채우기를 어떻게 고쳐도 땜질.
  재료가 대사보다 짧으면(팬케이크: 주 소스 19s vs 대사 24.8s) 어떤 채우기도 못 살린다.
- 오늘 빈칸 0을 만든 유일한 방법 = **코드가 앞에서 컷을 대사 길이만큼 지정**(`backbone_assemble.assign_cuts`, 자동 job 7→7컷).
- 고객 완성본 2개 눈 확인(63dba 미싱기·f6d32 대파): 숫자는 엉망(덧붙 20·15)이지만 재료 1~2편이라 팬케이크만큼 안 튄다.
  눈에 걸리는 건 **원본 하드자막·워터마크·같은 그림 반복** — 매칭보다 이게 먼저 보인다.

## 2026-09-17 낮(회사) — 컷 이어붙이기 통합 + ★집 세션 작업 흡수

- ★**집 세션(09-16 22:59~09-17 11:37)이 이 트랙 브랜치에 커밋 5개**를 남겼는데 핸드오프는 안 고쳤다(커밋 메시지가 기록).
  나는 낮에 "집 작업 없다"고 답했다 — **origin/main만 보고 origin/track/장면분량을 안 봤다**(규칙 A0 위반 그대로).
  · `ffad9c56a` 컷 이어붙이기(1차 루프 안) + `MIN_GOOD_CUT_SECS=1.2` 정본(컷 100,658개 실측: 중앙 1.67s·1.2 미만 29.3%)
  · `48aceec59` 조립기 줄당 컷 2개 이상(`MIN_CUTS_PER_LINE`) — "밋밋함" 7→18컷, mp4 미확인
  · `7881b089d` 효과음 벤치마크 순환(뇌전구 팩 79개 등록, `_SFX_MAX_SECS` 0.2) + **라이브 `edl_inherit_enabled` admin→1**(사장님 지시, A/B 3건)
  · `55a25ace0` CLAUDE.md 0순위-A0 신설·A1 확대
- 나도 같은 날 같은 자리에 컷 이어붙이기를 만들었다(`6023c7975`, 2차 패스·2.2초 상한). 병합은 충돌 없이 됐지만 **두 구현이 공존** →
  집 1차 블록을 걷어내고 정책(1.2초 뒤로 밀기·막지 않음·실측 근거)을 내 `_extend_refs_to_narration`에 이식.
  ★집의 1차 시점은 기존 테스트 2개를 깬다(스크래치 실측: 앞 줄이 훅·CTA b-roll 후보를 먹어 CTA s0-6→s0-4). 집은 게이트를 안 돌렸다.
- 집의 `scene_fill_audit.py` 확장(조각남 축 avg_cut_secs·short_pct)에 `MIN_GOOD_CUT_SECS` import 누락 → 폴백 import로 수리.
- 실측(합친 코드, 서버 사본 재편성): 팬케이크 26→23컷·역행 9→6·짧은컷 46→52% / 볼펜 20→19·짧 30→47% / 손조립 13→18·짧 15→6%.
  **소폭·혼재** — 재료가 짧은 컷 위주면 이어붙일수록 짧은 컷이 는다. 상속 경로의 한계 = 재료. ⚠️mp4는 집도 나도 못 봤다.
- 라이브 스위치 실측: `edl_inherit_enabled=1` · `script_grounded_enabled=admin`(고객은 2단계 지목이 없어 상속할 게 없다 — 켜나 안 켜나 동일)

### ★배포 전 실측 결과 (09-17 저녁) — "나아졌나?" → **아니다**
사장님 "뭘 배포를 해 테스트 검증도 안 하고" → 새 코드(사본)로 팬케이크 26698을 실제 렌더(`vfca7ee0ced3`, 25.2s)하고
어제 편성(26컷)도 라이브 코드로 렌더해 나란히 붙였다(`out/팬케이크_비교_옛코드vs새코드.mp4`).
**화면으로는 구분이 안 간다** — 반죽 섞기가 뒤에 튀어나오는 것·팬 위 팬케이크 반복·원본 하드자막(일/한) 노출 전부 그대로.
숫자 26→23컷은 눈에 안 보이는 차이. **사장님 제보는 못 잡았다.** 효과가 난 건 서브 컷이 긴 케이스(손조립·자동조립 — "늘어짐")뿐.
이유: 대사 24.8s > 주 소스 19s, 대본이 주 소스 13컷 중 10개를 이미 지정 → 이을 이웃 컷이 없어 다른 소스로 감.
**배포 보류 — 사장님 판단 대기.** 회귀는 없으니(466) 내보내도 나빠지진 않지만 "고쳐졌다"는 아니다.

## ⏭ 다음 (순서대로)

0-a. ~~배포 뒤 재렌더 확인~~ → **배포 전에 했다(위). 결과: 제보 미해결.**
0-b. `script_grounded_enabled`를 고객에게 켤지 — 사장님 결정. 상속만 켜선 고객 결손 100%가 그대로다.
0. ~~`assign_cuts`를 2단계 경로에~~ → 컷 이어붙이기로 **완료**(위). 남은 근본은 "같은 동작" 정보(특징 묶음) → ③ 백본 대본을 2단계에 — 2단계가 src_seg 1개를 주면 코드가 같은 소스 시간순 다음 컷을
   대사 길이(`narr_secs`)만큼 이어 붙여 beat_sources로 넘긴다. 자리는 `build_inherit_plan`의 `_ids_of` 직후 한 곳(0순위-B).
   그러면 `_fill_beat_screen_time`이 돌 일이 없다. 검증은 `tools/scene_fill_audit.py`로 팬케이크 26698 재편성(목표: 덧붙음 0~2).
1. **사장님 결정 대기** — 고객에게 `script_grounded_enabled`·`edl_inherit_enabled`를 `1`로 켤지(지금 둘 다 `admin`).
   근거: 고객 job 25건 중 15건 결손 100%(지목 0). 켜면 사장님 경로(결손 17~48%)로. 리스크: 장면 없는 재료면 "재료부터 다시"로 멈춤
2. **화면가림 태깅** — B1 태깅에 "상단 UI / 로고·인트로 / 큰 자막 / 워터마크 → 영역" 추가. 조립·매칭 모두 이걸로 컷을 거른다
3. 넘겨받은 무관 판정(`_shares_word`, aeba863fa, fixture `tests/fixtures/pancake_c393b1c7c2f9.json`) — **이번엔 안 넣었다**.
   b6228cac8이 되돌렸는데 이유가 커밋에 없다. 되돌린 이유를 대본게이트 세션에 확인한 뒤 판단. 오늘 실측에선 결손에 영향 0(있으나 없으나 78~83%)
4. 조립 자동화 배선(사장님 OK 후) — 제작소 "원본 던지기" 버튼 → `backbone_assemble.assemble` → `/api/produce/mix/start` 입력
5. 별건: `_speech_speed` 1.30 vs 실측 1.70 — 게이트 대본 상한과 짝이라 대본 길이가 같이 변함

## ⚠️ 이 트랙에서 밟은 함정

- ★**서버에 shopping_shorts를 통째로 복사하면 data 297G가 딸려와 디스크 100%** → 라이브 No space. 두 번 냈고 대본게이트 세션이 지웠다.
  A/B 사본은 `tar --exclude=data` + `ln -s data` + `ln -s .env`. 메모리 `reference_서버AB테스트가_디스크를_채웠다`
- ★디스크 찼을 때 `du` 큰 폴더부터 보면 범인을 오인한다 — **방금 내가 만든 것**부터. 고객 완성본 삭제를 제안할 뻔했다
- finish 게이트 락: 앞 프로세스가 pytest 워커 16개 띄운 채 55분 정지(CPU 0)하면 뒤가 전부 막힌다. 판정=45초 CPU 증가 0
- 같은 파일(script_gate·script_generate)을 두 트랙이 동시에 고쳐 충돌 2회. 영역 분담으로 해결
- 하네스 인자 형태 짐작 금지 — `generate_one_style(sources, style, target_seconds, ...)`, `_call_json`(not `_vault_call`)
- given_script는 **줄마다 마침표 하나** — `script_sentences`가 문장으로 쪼개 줄 수 ≠ beat_sources 수면 상속이 None 폴백
- `_speech_cps()`가 script_gate에 이미 있다(edit_plan 상수×배속). 새로 만들지 말 것
- 실측 도구 `tools/scene_fill_audit.py`(서버 /tmp에도) — 결손%·덧붙은 컷·같은 그림. "고쳤다" 판정은 이 숫자로만

## 2026-09-17 밤 — 이븐쇼핑 스파인(55) 백본 조립 5건 실증 (사장님 job, 소스 충분한 것)

- 결과 6편 `out/이븐쇼핑_백본5건/` + 바탕화면. job: 카메라 bbf7d09efe01 · 행주 bbe01c98b69c · 수납함 bb6e2cec1cff · 미니후드 bbcaf6aa81c3 · 레인지후드 v1 bbde60b16c5c(귀마개 오염) / v2 bb96ebf48c42(귀마개 소스 제외).
- 눈으로 본 것: 장면↔대사 대체로 맞음(박스→박스, 다이얼→다이얼, 필터→필터). 어긋난 곳은 "박스를 열어보니/구성품이" 같은 **소스에 없는 행위 줄**에 다른 컷이 붙는 것(미니후드 2줄). 원본 하드자막·중국어 자막은 그대로(화면가림 태깅 = 다른 트랙).
- 함정 2개: ① `/tmp/ab`에 `assets`·`static` 심링크 없으면 "[motion] 폰트 미해결 → 원본 복사"(자막·틀 전부 빠짐). ② 틀 그림은 `data/frame_cache/<cache_key>.png` 재사용 → 폰트 없던 시점 틀이 캐시되면 제목이 깨알로 남는다. 캐시 지우고 재렌더로 해결.
- 사장님 job 소스에 **다른 제품 영상**(레인지후드 job의 유튜브 NBsOouTUWsI=귀마개)이 섞이면 조립기가 그 컷을 씀 → 소스 제품 일치 검사 없음(미결).
- 미니후드 1차는 Gemini 503으로 groups_empty, 재시도 통과.
- 스크립트: `out/bb_batch5.py`(서버 `/tmp/bb_batch5.py`, `/tmp/bb_batch2.py`, `/tmp/rerender2.py`).
- (같은 날 밤 추가) 사장님 "이븐쇼핑 스타일 아니네" → 조립기가 스파인 훅 한 줄만 빌리고 자체 [훅]+[특징]+[댓글CTA]로 쓰던 것 확인. `write_lines`에 스파인 `beat_roles`+`templates` 그대로 쓰는 경로 추가(`_spine_style/_feature_roles/_spine_plan/_spine_prompt`, 테스트 6개). 카메라 재실증 job **bb4c2734ce80**(out/이븐쇼핑_백본5건/이븐스파인_카메라_*.mp4): 대본은 천재→평범→떼돈→「이건 바로 NORDECO」→한계→효능×5→"이러니 떼돈을 벌었다고" 꼴로 정확. 남은 문제: 11줄이라 34.8초(목표 25), 패키지·구성품 줄에 거리 컷(그룹 컷 부족→보충), 마지막 2줄에 남의 채널 CTA 하드자막·얼굴 컷.
- (밤 2차) 사장님 "장면 일치가 잘 안 되는데" → 줄별 컷 대조(`/tmp/inspect_cuts.py`)로 뿌리 3개 실측·수정(커밋 b665221fe·cfde86127·37f6cafb1): ① 구조 줄(정체·떼돈)이 먼저 돌며 특징 묶음 컷을 먹음 → 특징 줄 먼저 배정 ② 스파인 프롬프트 group이 순서번호(원번호 아님) → 원번호 ③ 묶음 밖 컷=찌꺼기(남의 CTA·얼굴)라 구조 줄이 그걸 받음 → 외관 컷을 구조 줄 몫으로 줄당 2컷 예약, 사람·댓글 컷 맨 뒤. 5차 job **bbe3eb2fe683**: 11줄 전부 제품 컷(inspect 출력 기준). 남은=길이 33~35초(목표 25), 하드자막 컷(다른 트랙), 모델 재호출마다 묶음이 달라져 회차 비교가 흔들림.

### ⏭ 집에서 이어서 (2026-09-17 밤 마감)
- 서버 A/B 폴더 `/tmp/ab`는 트랙 최신 `backbone_assemble.py`가 scp로 올라가 있음(assets·static 심링크 포함). 실행 스크립트 `out/bb_batch5.py`(5건 배치), 서버 `/tmp/bb_spine1.py`(카메라 1건), `/tmp/inspect_cuts.py`(줄별 컷 대조 — job id만 바꿔 쓰기).
- 다음: ① 새 규칙으로 행주·수납함·미니후드·레인지후드 4건 재조립 → 줄별 대조로 판정 ② 25초 길이(효능 줄 3~4개로 제한, `_spine_plan`의 n_feat 상한) ③ 하드자막·남의 CTA 컷 배제는 화면가림 태깅 트랙과 합의.
- 배포 보류 그대로(사장님 지시). finish 하지 말 것 — 트랙 브랜치에만.
- (09-18 낮) 어미 결합 오류 수리(758a95937): 프롬프트에 어간 규칙 + `_bad_join/_fix_join/_repair_joins`(기계수리→줄 재작성 1회). 재조립 수납함 bbb6bcbbbeb8·미니후드 bbf5ff87bb8d: '는는데'류 0건. 잔여: 틀 `{효능3}데다가 {효능4}`는 끝 어미가 모델 몫이라 "챙긴다는."처럼 끊길 수 있음(틀 자체에 끝말 추가 필요). 4건 새 규칙 실증: 행주 bb214c1e0cd6·수납함 bba3a1e8f7a1·미니후드 bbe6fcfabec1·레인지후드 bbcb7dffecf3 — 장면 8~10/10줄 제품 컷.
- (09-18 오후) 프리셋 확장 도구 `tools/spine_presets/harvest_templates.py`(원문→역할별 틀 수확, 원문검증·슬롯없음 거르기·제외어, `--apply`). 집 서버 스크립트 4개 `tools/spine_presets/`에 보관. **적용**: 56(+29→제품어 13개 제거, 제목 11·bait 10·origin 9·notice 7·cases 6·twist 9), 60(+5), 65(+2). **미적용**: 68·67·66·69 — script_wiki 유튜브 재료 3~9편, 존댓말·사투리 섞여 결 안 맞음 → 그 훅으로 원문 수집이 먼저. 히트작 200편 분포: 오용형 92·권위자 12·은폐형(이븐) 15·기타 76.
- (09-18 저녁) 사장님 구조 확인 "유형 고르면 그 유형 스파인들 순번대로": **없던 것** → `pick_hook_spine(style=, seed=)` 추가(6d3ab61d0). 실증 발명품형 seed 0/1/2 → 60/65/66 순번 정확(job bb182ae5c1cf·bb16bd703bda·bbe36afd10c0). 유형 태그 현황: 오용형=56 1개, 발명품형=60·65·66, 제품정체형=55; 67·68·69는 유형 대신 카테고리(홈템)만 붙어 있어 **유형 붙여야 고를 수 있음**. 대본: 60 자연스러움 / 65 "만드는다는 거"·"부드러워해 버림"(어미 결합 잔재, 수리 패턴 추가 필요) / 66 틀이 원문 아닌 조립체라 "~음/~거" 단조 → 원문 재료 필요. 자막 852편 PC 수집 중(`raw/analysis/썰쇼핑_자막확장_2026-09-18`).
- (09-18 저녁2) 자막 781편 확보(`raw/analysis/썰쇼핑_자막확장_2026-09-18/hits_subs.json`, 서버 `/tmp/ab/raw/...`). 수확 적용: 60(+5)·65(+11)·69(+14)·68(+6)·67(+6)·66(+2)·56(+6), 파편·제품어 11개 손으로 제거. 유형 태그: 67=목격담형·68=가성비형·69=만능템형. **오용형 스파인 4개 신설**(56 뼈대 복제): 70「OO도 예상 못한 미친 활용법」(재료 107편)·71「OO의 실수」(9)·72「직원도 몰래 쓰는 활용법」(11)·73「OO도 감탄한 천재 아이디어」(24) → 수확 중. 스크립트 `tools/spine_presets/{make_misuse_spines.py,harvest_all.sh,harvest_new.sh}`.
- (09-18 밤) 70~73 재수확(56 틀 씨앗+필터 강화) → 칸별 9~14개, 훅(title)은 스파인별로 히트작 제목 꼴 부여({권위자} 슬롯). 오용형 순번 0~4(56·70·71·72·73) 수납함 5편 실증 돌리는 중(`/tmp/bb_misuse.py`).
- (09-18 밤2) 오용형 5스파인 대본 3차(렌더 없이): 6줄 고정·훅 각각 다름·존댓말 0(job bbfc2bdbe78f·bb06508066f7·bb6e413074fa·bbd3cfdd12de·bb53f68f0e87). 수정: 조립체/존댓말 틀 제거(스파인당 15개), 특징 줄 반복 상한 2·효능 칸<3이면 안 늘림, 슬롯 이름 누출 제거, 유형으로 고를 땐 hook_3s 없는 인스타 스파인도 후보. ★수납함처럼 오용 사례가 없는 소재에 오용형을 주면 "비밀 금고"처럼 화면에 없는 용도를 지어냄 = 소재↔스파인 불일치(사장님 결정 대기). 인스타 스파인 7개 수확 시작(`/tmp/harvest_insta.sh`, corpus=insta 300자↑ 긴 원문 25편).
- (09-18 밤3) 인스타 스파인 수확: '*'(긴 원문순)은 외국어·무관 원문이라 실패(59에 외국어 15개 들어갔다 제거, 한글비율 필터 추가). 훅 문구로 세니 52(32편)·58(19)·62(21)·53(8)만 재료 있음, 54·59·61은 0~4편 → 보류. 4개 훅 문구로 재수확 중(`/tmp/harvest_insta3.sh`). 인스타 원문(한글 150자↑) 1,597편.
- (09-18 밤4) 인스타 수확 결과: 52(+14→찌꺼기 제거)·53(+16→제거) 보강, 58·62는 재료 18편인데 원문검증 통과 0(모델이 바꿔 씀). 행주 job(인스타 소스)으로 지인증언형·권유지시형·금지경고형 순번0 대본 실증 중(`/tmp/bb_insta_script.py`).
- (09-18 밤 마감) 인스타 실증(행주 job, 대본만): 지인증언형 bb22636a44f0·권유지시형 bbdd4bb0a0ff·금지경고형 bb5569edbc0f — 순번 고르기·존댓말 유지 OK. 금지경고형 자연스러움. 지인증언형 "알고 보니" 3연속 → more 칸 없는 틀은 안 늘리게 수정. 권유지시형(53)은 레시피용(fit ['권유지시형','레시피'])이라 행주에 "드세요·별미" = **소재↔스파인 불일치의 실례**.
### ⏭ 다음
1. 사장님 결정: 소재↔스파인 안 맞을 때(경고/자동교체/그냥 둠) + 레시피 전용 스파인(53)을 물건에 안 보이게 할지
2. 58·62(인스타)·66·67·68·69(유튜브): 재료 부족/원문검증 0 → 원문 더 담기(랭킹 채널 등록) 후 재수확
3. 고객 화면에 "유형 고르기" 연결(3단계 백본 경로) → 그때 배포
- (09-18 밤 추가) 인스타 오류 2개 수정·재검증: 지인증언형 bb07b67fad21 "알고 보니" 1회(반복 사라짐) / 권유지시형은 레시피 전용(53) 제외 → 후보가 없어 무작위 폴백으로 67 「충격 먹었음」(유튜브 반말)이 뽑힘 = **권유지시형 물건용 스파인이 없다**(원문 담아 만들 것). 금지경고형 bb217f9ba224 정상.

## ✅ 2026-09-18 유튜브 대본 스타일 마감
- **유형 6개 · 스타일(스파인) 15개**, 전부 카메라/수납함 소재로 대본 1편 이상 실증(렌더 없이):
  오용형 5(56·70·71·72·73) / 제품정체형 4(55·74·75·76) / 발명품형 3(60·65·66) / 목격담 67 / 가성비 68 / 만능템 69.
- 구조: 유형 고르기 → `pick_hook_spine(style=, seed=)`가 그 유형 스타일을 id순 순번 → 스타일 안 칸마다 문장틀을 순번(시작점 = seed + 스파인id×7).
- 오늘 고친 것(전부 실측): 스파인별 시작점 엇갈림 · 가격/본래용도 지어내기 금지 · 제품명 한국어·공개 줄 1회 · 틀 밖 반복 상한 · 오용형/인스타형 줄 안 늘림 · 슬롯 누출 · 레시피 전용 스파인 물건 제외 · 결합 깨지는 틀 꼴(`}버렸`·`}해 버려`·`}데다가` 등) 전 스파인 제거.
- 남은 약점: 66·67·68·69는 원문 13~38편이라 틀이 얇음(문장 반복 위험) · 25초 길이 미조정(정체형 10~11줄 ≈ 30초).
- 스크립트: `tools/spine_presets/{make_identity_spines.py,harvest_identity.sh,make_misuse_spines.py,harvest_*.sh}`, 수확 도구 `harvest_templates.py`.

## ⚠️ 2026-09-18 저녁 — 스파인 DB 편집이 라이브에 즉시 반영됨(스스로 발견)
- 오늘 서버 `reference.db` spine 틀을 고치고 새 스파인 70~76을 approved로 만들었다 → 라이브 코드 5곳(bank_assemble·script_generate·spine_fill·script_gate·app 스타일목록)이 바로 읽음 = **배포 보류와 무관하게 고객 대본에 반영**.
- 70~76은 유형 태그만 있어 소재 선택 화면에선 안 보이고 소재 없이 열면 보임. 기존 55·56·60·65~69·52~62는 고친 틀이 고객 생성에 쓰임(고장 틀 제거+원문 틀 추가 방향).
- 되돌리기 기준: `/home/ubuntu/spine_backup/spine_2026-09-18_after_edits.json`(현재) · `reference.db.bak-script-pickup-grounding-20260914-143448`(09-14, 집 세션 55 프리셋 이전).
- **사장님 결정 대기**: 그대로 둘지 / 70~76만 숨길지(pending) / 09-14로 되돌릴지.
- 점검기 `tools/spine_presets/audit_spines.py` 신설(회원 100명 조합·첫 줄 겹침·틀 결함). 1차 43건 → 수리 후 22건, 남은 건 전부 재료 부족(52·53·54·59·62 인스타, 66·67·69·76 유튜브).
- CLAUDE.md 0순위-A1b(스스로 점검) 추가 + 전역 CLAUDE.md에 같은 규칙.
- (결정 2026-09-18) 사장님: 스파인 DB 라이브 반영 "상관없을 것 같은데" → **그대로 둠**(70~76 포함).

## 2026-09-18 저녁 — 유형별 영상 끝까지 실증(자가점검 2회)
- 1차(bb0d060aa9f6·bbe5746fff17·bb38c10bcca9) 제가 먼저 보고 탈락: 길이 18/30/29초·발명품형에 오용형 말투 섞임·제품명 반복 → `_fit_length`(읽는 초 기준 가운데 특징 줄부터 빼기)·`_one_full_name`·스타일 섞임 검사(점검기)·비오용형 스파인 오용형 말투 32개 제거.
- 2차(오용형 bb8edef70215 20.7초 · 정체형 bb931fdbf7c8 24.8초 · 발명품형 bb5dc9e215de 19.6초): 줄별 컷 대조 일치(레버→레버, 언박싱→박스, 결과물→복고 영상). 바탕화면 `유형v2_*.mp4`.
- ★남은 가장 큰 화면 결함: 원본에 구워진 **남의 채널 글씨**(@살림장착 "떼돈 번 제품"·"댓글에 찰칵"·중국어 자막) — 화면가림 트랙 몫. 조립기에서 그 소스 컷을 빼는 임시책은 태깅에 글씨 정보가 없어 못 함.
- 오용형을 카메라(오용 사례 없는 제품)에 주면 억지 줄("온갖 곳에 붙이기") → 소재↔스타일 불일치 결정 대기.
- 읽는 초 추정(narr_secs)은 실제 TTS보다 길게 나옴: 추정 30.9초 → 실제 24.8초.
- (저녁) 사장님 "씨앗=백본이면 백본과 다르게 보여야": 실측 도구 `tools/spine_presets/diff_from_seed.py`(원본 컷 비율·원본 6글자 조각 겹침). 한국어 원본(행주 s0 지인증언형)으로 3유형 → **원본 컷 0/21~31 · 문장 겹침 0%**. ★카메라·수납함은 원본이 영어·러시아어라 이 검사 무의미(처음에 그걸로 재고 넘어갈 뻔함). 특징 순서 섞기가 **안 되고 있던 것** 발견(머리말엔 '순서 섞기', build_groups는 '원본 순서 지켜라') → `_shuffle_middle`(양끝 고정·가운데 seed 섞기). 이중어미 '주게 해 주' 수리.
- (결정 2026-09-18) 사장님 **성공 기준: "원본(씨앗)이랑만 완전 다르면 된다"**, 자막은 당연히 지운다(화면가림 쪽). → 판정 도구 `diff_from_seed.py`(원본 컷 0%·원본 6글자 조각 겹침 0% 목표). 서브 영상과의 유사성은 기준 아님.
- ★(정정 2026-09-18 저녁) 앞서 적은 "원본 컷 0%"는 **측정 오류**였다 — 컷 번호 접두어(DdOayfhAnpx)와 소스 번호(s0)를 비교해 늘 0. **같은 원인이 조립기에도**: `_seg_index`의 vid가 컷 접두어라 백본(s0)과 절대 안 맞아 '서브 먼저·원본 나중'이 09-16부터 통째로 무력 → 씨앗 s0 백본인데 원본 컷 10/29·12/22. vid를 소스 번호로 고침(테스트 추가). 검증: 같은 도구로 고치기 전 job 재측정 = 10/29·12/22·옛 방식 9/9 → 고친 뒤 0/24·0/23·0/23(진짜).
- 옛 방식(라이브 경로, 복제 job old57c3a72e6)은 **9줄 전부 원본 컷** — 사장님 기준(원본과 완전히 다름) 탈락.
- ★(정정2) 원본 컷 비율은 **최종 편집안(edit_plan beats의 primary+alternates)** 기준으로 잰다 — beat_sources(대본 지목 컷)만 세면 3단계에서 붙는 컷이 빠진다. 행주 실측: 옛 방식(라이브) 최종 34컷 중 원본 14(원본 화면 15.8/45.3초 ≈35%) / 새 정체형 0/27 · 새 발명품형 0/24. 비교 영상 바탕화면 `비교_행주_4칸_최종.mp4`.
- 대본 수리: 나라→'해외' 바꿀 때 조사(해외은→해외는)·'한국'은 제외(시청자 기준 말), '비맞던' 틀 제거. 테스트 34.

## 2026-09-18 밤 — 점검도구·스타일 전수 통과·오용형 구조
- 반복 점검 `tools/spine_presets/check_repeats.py`(렌더 정본 plan_beat_clips_for로 완성본 컷 순서 → 같은 구간 재등장=되돌아옴 FAIL). 케이크 616ee19b6187(사장님 라이브 job, 안 건드림) 2곳·옛 방식 2곳 / 새 방식 행주 4편 0곳. 같은 영상 연속 컷은 프레임 대조 결과 다른 그림이라 참고만.
- 대본 일괄 점검 `batch_scripts.py`(렌더 없음): 42회 중 40 나옴, 실패 2 = 503 즉시 포기 → assemble에 503 재시도(4·10초) 추가, 재실행 6/6.
- ★사장님 결정: 떼돈·바이럴·품절대란 등 과장 표현 **써도 됨, 더 센 후킹도 가능**. 구체 가짜 수치만 막음(memory feedback_과장표현은_스타일이다).
- 오용형(56·70~73): cases 틀이 초보/중수/고수 세트 조각이라 용도 1개 → cases=초보, escalation=고수(라이브 설명표에 있는 이름), twist 유지. 원문 183편 전환 중앙값 3. 끊긴 틀(~면서/~지만) 원문 뒷말로 끝맺음, 알레르기·들고가기·속성2개 틀 제거. `_fit_length` 특징 줄 최소 3. 실측 7편 전부 초보→고수→반전. 도구 `fix_misuse_tiers.py`.
- 유튜브 약한 6개(66~69·74·76) 원문 틀 50개(서브에이전트, `curated/yt_weak_0918.json`) + 71 첫 줄 3개 → **유튜브 15·인스타 7 전부 audit 통과**(5회 반복 확인). 인스타 6개 45틀(`curated/insta_0918.json`).
- 점검기에 끊긴문장·세트조각 검사, 69 깨진 사투리 틀 3개 제거(`drop_templates.py`), 나라 이름은 물건 앞이면 빼고 사람 앞이면 '해외'.
- 보기 페이지: 바탕화면 `유튜브_대본스타일.html`·`인스타_대본스타일.html`(`spine_viewer.py`).
- 백업(서버 /tmp): spine_backup_insta_0918_1821 · misuse_0918_1925(★2차 반영이 덮어씀 — 원본은 PC 스크래치 yt_spines.json) · misuse_0918_192903 · ytweak_0918_215822/215927 · 71_0918_220307 · drop_0918_221113.
### ⏭ 다음
1. 고객 연결 자리 = 2단계 `/api/wiki/generate`(스타일 경로, `_materials_for_generate`가 job extract 보유) — **대본게이트 트랙 영역**이라 사장님 결정 필요(어느 트랙이 넣나·화면에서 유형 고르기 방식). 3단계(mix/start)는 script_structure(beat_sources+inherit_scenes)만 받으면 그대로 됨.
2. 사장님 결정 대기: 활용법 없는 물건에 오용형 제외 여부.
3. 남은 문장 어색함: 69 사투리 "싹 끝나는 겨" 류, 드문 이음 오류("끼우기만 설치해 주는데").

## ⏭ 집에서 이어서 (2026-09-18 밤 마감, 트랙 = 장면분량)
사장님 결정(09-18 밤): ① 새 방식은 **사장님 계정에만 먼저**(2단계, 대본게이트 트랙엔 알리기) ② 오용형은 **재료에 딴 용도 장면이 있을 때만**.
- 된 것(19c508956): build_groups가 alt_use(딴 용도 장면 유무)를 판정 → 오용형 스파인인데 false면 같은 seed로 제품정체형, note.style_switched에 기록. `to_draft()` = 2단계 초안 모양(beats[].text/src_seg/src_segs, made_by="백본"). 테스트 39.
- 남은 연결(코드 위치 확인 끝):
  1. `app.py` `/api/wiki/generate` 스타일 분기(`_materials_for_generate` 뒤, `_assembled_drafts` 앞 ≈3610줄): `_setting_gate(store, "backbone_script_enabled", cid)`이고 `_job.extract`가 있으면 고른 스파인마다 `ba.assemble(ba.sources_from_extract(_job["extract"]), 씨앗vid, store, spine_id=sid, seed=_jid)` → `ba.to_draft` → 기존과 같이 save_draft·응답. 실패하면 옛 경로로 가되 응답 materials에 이유(조용한 폴백 금지). 씨앗 vid = backbone_main 인덱스 → s{n}, 없으면 한국어 제일 긴 소스.
  2. 화면·3단계는 무수정: produce.html s2Confirm이 beats를 줄로·src_seg(s)를 beat_sources로 mix/start에 보냄. 서버 `edl_inherit_enabled='1'`(확인) → inherit_scenes 켜짐.
  3. 서버 설정 `backbone_script_enabled='admin'`은 배포 뒤에 넣는다(배포 전 넣어도 코드가 없어 무해).
  4. 실측: 사장님 계정으로 실제 2단계→3단계→렌더 1편, check_repeats·diff_from_seed로 판정 → 그 다음 finish(배포는 사장님 확인).
  5. 대본게이트 트랙에 알림(같은 함수 앞단에 분기 하나 추가한다는 것).
- 오용형 문턱 실측 아직: alt_use 판정이 볼펜·후드(false)·커튼고리류(true)에서 맞는지 batch로 확인할 것.
- 참고: `raw/analysis/썰쇼핑_채널확장_2026-09-18/run.log`가 수정 상태(PC 자막 수집 중인 로그) — 커밋 안 함.

## 2026-09-19 — 백본 2단계 연결 · 대본 스타일 점검 · 히트작 대본 지도
- 2단계 `/api/wiki/generate`에 `backbone_script_enabled` 분기(`_backbone_drafts`) — 커밋 d362a1ae7, **미배포**(finish·서버 설정 admin·실측 1편 남음). 대본게이트 트랙에 알릴 것.
- 스타일 점검: `sample_all.py`(스타일마다 실제 대본 1편) + `spine_viewer.py all` → 바탕화면 `대본스타일_전체점검.html`. 승인 22개 전부 대본 나옴. 걸림: 오용형은 볼펜 재료라 74로 바뀌어 미검증 / 70·74 틀 "매출" 4줄(사장님 결정 대기, 스파인DB=라이브라 안 고침) / 59 "1초".
- ★사장님 지적: 유형당 5개가 오용형만 됨(나머지 1~4개), 점검이 '고장 여부'만 보고 '잘 썼나'를 안 봤다 → 방향을 **히트작 대본을 정답지로** 바꿈.
- 히트작 지도: 코퍼스 = script_extracts(조회수 있는 1,147) + 썰쇼핑 자막 781 → 1,922편. 10만+ & 메종 전부 592편을 `classify_hits.py`(Gemini, 서버 /tmp/ab)로 유형·흐름·잘쓴이유 분류 → `hits_report.py` → 바탕화면 `히트작_대본지도.html`.
  · 메종 홈디노: channel_archive 384편 중 전사 42편뿐 — 나머지 전사가 필요. 특징: ~더라고요 40/42, 지인+반응 훅 최다, 상위10 CTA 2편.
  · 새 유형 후보: 전문가지인형 24편 · 업계충격형 8편 · 비밀폭로형 4편(표본 적음). 흐름은 칸 순서로는 유형당 42~57가지라 '재료 조합'(계기·불편·권위/사회증거·심지어)으로 묶음.
### ⏭ 다음
1. 사장님: 새 유형 후보 확정 · "매출" 문장 처리
2. 메종 나머지 342편 전사(조회수 상위부터)
3. 유형마다 흐름 5개 → 스타일 5개(원문 문장), pending으로 넣고 히트작 옆 나란히 판정

## ★2026-09-19 사장님 확정 방향 — 대본 = 히트작 틀 옮기기 (방향 잃지 말 것)
- 방식: 히트작 **한 편 통째가 틀**. 칸 순서·말투·연결어 유지, 제품 이야기만 재료로 바꿔 씀(`transpose_hit.py`). 칸 조각 섞기(`fill_templates.py`)는 실패로 폐기.
  · 훅은 글자 고정·빈칸만 교체(권위어 개발자·본사 등은 고정) / CTA 반복 제거 / 원본이 끊겨 끝나면 **반복재생 기법 그대로**.
- 분류: 대분류 4(썰형·체험후기형·추천지시형·레시피형) → 유형 → 틀(그 유형 히트작 전부, 회원마다 안 쓴 틀 순번).
- 선택: **안1 = 씨앗 유형 자동("이 영상에 딱 맞는 스타일입니다")**, 안2 = 직접 선택("이 영상엔 ○○ 장면이 없어 어색할 수 있어요. 해당 장면을 추가하시면 좋아요"). 어색하면 안 씀.
- 현황: 지인증언형 97편·오용형 94편(틀 가능 69) 템플릿화 끝. 소재 12종 시험 통과. 씨앗 판정 `seed_type.py` 28건 시험.
### ⏭ 순서 (사장님 확정)
0. **장면 붙이기** — 옮긴 대본 줄마다 컷 지정(assign_cuts 재사용) → 실제 렌더 1편 눈 확인
1. 나열형 추가 · 레시피 판정 보강(음식 소재로) · 외국어 씨앗 처리(번역본 또는 체험후기형 기본)
2. 나머지 유형 템플릿화
3. 2단계 연결(사장님 계정만): 안1 자동 + 안2 선택 문구

## ★2026-09-20 — 대본 = 원문형 스파인(히트작 한 편) 확정판. 다음 세션은 여기만 읽어도 된다
### 무엇이 정해졌나 (사장님 확정, 되돌리지 말 것)
- **스파인 = 히트작 원문 한 편**(원문 cells + 칸 순서 + 훅 고정 문구 + 말투 + 유형). 빈칸 문장틀 조립은 **폐기**
  (실측: 칸 조각 조립 15편 전부 문장 깨짐 / 스파인 78·79는 5줄 중 3줄 오류 — "통잠이 쏙 올라오는데").
- 대본 생성 = `backbone_assemble.write_lines_from_origin`: ①원문 전제 읽기 → ②이 제품 상황표(build_cast) →
  ③원문 말투로 쓰기 → ④상황표와 대조(cast_gap), 어긋나면 1회 재작성. 검사는 **치명 오류 7가지**만 본다.
- **여러 스파인 중 통과본만** 쓴다 = `assemble_clean(want=N)`. 통과본 없으면 빈 목록(옛 경로로 가고 이유를 화면에).
- 인스타(존댓말) CTA는 **"궁금하시면 댓글에 'OO' 남겨주세요"로 코드 고정**(`fix_insta_cta`).
- 훅은 코드가 고정(`enforce_hook`) — 반응 낱말 바꾸면 원문으로 되돌린다. 반복재생 끝(마지막 끊김)은 유지.
- 화면(produce.html): **픽업영상 대본 폐지**, 「🎯 이 영상에 딱 맞는 스타일」 자동 1안 고정(끌 수 없음) + 직접 1개 선택.
  안내 문구 = "…1안을 만듭니다 · 대본을 읽어보시고 어색한 부분이 보이면 간단하게 수정해주세요."
- 2단계 서버(app._backbone_drafts): 씨앗 유형 판정(`ba.seed_type`) → 그 유형 원문형 스파인(`ba.origin_spines`)에서
  자동 1안, 사용자가 고른 원문형 스파인에서 1안. 스위치는 `backbone_script_enabled`(기본 끔).
### 지금 상태 (서버 reference.db, 전부 status=pending)
- 원문형 스파인 **374개 / 15유형**. 100명 겹침: 금지경고·지인증언·내자랑 3명, 오용 10명, 정체의문 17명, 물건발견 1개뿐.
- 도구(tools/spine_presets): classify_hits(유형·흐름 분류) · templatize_hits(칸 자르기+원문검증) ·
  import_origin_spines(자격검사 후 스파인 등록) · tag_origin_spines(전제·필요장면·훅범용·소재 태그) ·
  transpose_hit/transpose_render(초기 실증) · sample5page/bestpage/spinepage(보기 페이지)
- 코퍼스: /tmp/corpus.json(1,922편) · /tmp/hits_cls_all.json(분류 1,238편, 조회수 2만+) · /tmp/tpl*.json(유형별 칸 자른 것)
- 산출물(바탕화면): 대본샘플_원문형스파인.html · 히트작_대본지도.html · 대본스타일_전체점검.html · 대본_통과본만.html
### ⏭ 다음 순서
1. **사장님이 버릴 스파인 고르기** — 대본샘플_원문형스파인.html 보고. 그 뒤 승인분만 status=approved.
2. 배포: track finish → main(자동배포는 새벽 2~6시, 급하면 DEPLOY_NOW는 사장님 확인 뒤) → 서버 설정
   `backbone_script_enabled='admin'` → 사장님 계정으로 2단계→3단계 **실제 렌더 1편 확인**(0순위-A1).
3. 약한 유형 보강: 정체의문형 6개, 물건발견형 0개(옛 카드 1장뿐) — 코퍼스 문턱을 더 낮춰 채운다.
4. 남은 흠: 갑오징어 job은 씨앗 유형 판정이 빈 값(외국어·짧은 원문) → 판정 실패 시 기본 유형 정하기.
### 집에서 스파인 받기 (2026-09-20)
- 정본은 서버 DB(reference.db)다. 파일 사본: `tools/spine_presets/curated/origin_spines_2026-09-20.json` (374개, 커밋됨).
- 다른 PC·DB에 넣기: `python3 tools/spine_presets/restore_origin_spines.py tools/spine_presets/curated/origin_spines_2026-09-20.json --db <DB경로> --apply`
  (이름이 같으면 갱신, 없으면 새로 넣음. status는 덤프 그대로 pending.)

---

## ★2026-09-21 새벽(집) — 이븐쇼핑 틀로 정착. 회사에서 여기부터 읽어라

### 사장님 지시로 방향이 바뀐 지점 (되돌리지 말 것)
사장님: **"이거 완전 거꾸로 품질이 가고있는거야 단순하게 처음했을때가 좋았던거야"**
→ 09-20 확정이던 **"스파인 = 히트작 원문 한 편 통째 복제"는 사실상 폐기 방향**이다.
   대신 **09-17 `b9fcaa1ff` 방식(스파인 문장틀 + 재료의 특징)**으로 돌아간다.

**왜 꼬였나(실측)**: `backbone_assemble.py`가 09-16 **276줄 → 09-21 1,032줄**(4배).
커밋 20건이 전부 증상 덧칠(이중어미·끊김·조사·순서섞기…). 모델 호출도 2회 → 스파인당 4~5회.
결정적으로 `write_lines` 401~402줄에 **`origin` 분기 두 줄**이 얹히면서, 원문형 스파인이면
**틀 기반에 닿기도 전에 가로채진다**. 틀 기반 경로(`roles, tpl`)는 **멀쩡히 살아 있다**.

```python
origin = spine_origin(hook_spine)
if origin:                                  # ← ★이 두 줄이 가로챈다
    return write_lines_from_origin(...)
roles, tpl = _spine_style(hook_spine)       # ← 사장님이 원하신 틀 기반
```

### 조사 결과 (전부 실측 — 다시 조사하지 마라)
- **이븐쇼핑 22편**: 발명품형 15 · 제품정체형 3 · 오용형 3 · 다이소 1.
  유형은 껍데기고 **문장 구조는 하나** — `천재` 22/22(100%) · `최근` 21/22 · `말도 안 되는` 21/22 · `이건 바로` 17/22.
  훅 끝말만 바뀐다: …천재의 발명품 / …때돈 제품의 정체 / …천재 주부의 활용법.
- **썰계열 261편**(채널명 아닌 **말투 표지**로 고름): 방구석꿀템24·이븐쇼핑22·활용정점17·살림장착15…
  훅 머리 축 7개 — 브랜드지목43·제품정체34·천재발명32·개발자당황31·천재활용14·떼돈13·의문6(+기타88).
- **관용구 포화 확인**: 반전도입 가짓수 = 이븐22편 3 → 계열261편 12 → **+스파인368개 17** → 전체1242편 20.
  재료 4.8배에 +3개뿐 = **더 모아도 안 는다.** 스파인 칸 2,448개(10만 자)가 코퍼스보다 풍부했다.
- **인스타도 같은 방식 성립**(641편): 증언 5가지(친구가101·엄마가37…) · 공감도입 4 · 반전도입 4 · CTA 2.
  ⚠️섞지 마라 — 유튜브=반말·CTA없음 / 인스타=존댓말·CTA필수(`남겨주세요` 404회).

### 이번에 한 것 (배포 완료 · 라이브 실측)
| | 내용 |
|---|---|
| 특징 개수 | 5~7 → **3~4개**(`MIN_GROUPS`·`MAX_GROUPS`). 프롬프트 "너무 뭉치지 마라"→"제일 센 장점만" |
| 장면별 특징 | `_source_block`에 세그 `product_benefits`·`use_point` 추가 — 장면↔장점 연결 |
| 스파인 55 틀 | reveal 1→5 · twist 5→11 · limit 5→14 · more 7→11 · fame 7→11 · land 6→8 → **3.3억 조합** |
| 자막 잡음 | `spine_origin`에서 `[음악]` 제거(읽는 자리에서) + 라이브 DB 3개 정리 |
| 스위치 | `backbone_script_enabled='admin'` — 관리자만 |

**실측(같은 재료 두피빗, seed만 바꿔 5편)**: 첫 줄 **5/5 전부 다름** ·
title·land 5/5 · fame·reveal·twist 4/5 · more 3/5 · limit 2/5→도입 5종 추가 후 재분산.
테스트 9건 통과. 게이트 실패 22건 = 기준선 그대로.

### ⏭ 회사에서 이어할 것 (순서대로)
1. **씨앗 유형 배선 — 제일 급함.** `seed_type`이 화면에서 고른 씨앗을 **무시**한다
   (`backbone_assemble.py:1011`). `backbone_main`이 None이면 **한국어 중 가장 긴 소스**를 임의로 집는다.
   실사고: 화면 씨앗=살림킹왕짱(오용형)인데 s4「신박템 3가지」로 판정 → **제품정체형** →
   오용형 46개가 후보에도 못 들어감. "이 영상에 딱 맞는 스타일"이 거짓말이 된다.
2. **`origin` 분기를 설정 스위치로.** 기본을 틀 기반으로 돌린다(지금은 원문형이 가로챔).
   ★사장님이 새 대본 보고 "이게 맞다" 하신 뒤에 켤 것 — 내 판단으로 라이브 동작 바꾸지 말 것.
3. **스타일 축 7개를 대본 스타일로 등록.** 몸통(미끼→공개→고조) 공유, **훅 머리만** 갈아끼움.
   재료: `/tmp/even_parts.json`(칸 부품) · `/tmp/even_phrases.json`(관용구 17/7/7/4/4).
   도구: `tools/spine_presets/even_parts.py` · `even_phrases.py`(둘 다 커밋됨).
4. **재료 거르기.** 빈 전사가 "대본 N편"으로 셈된다 — 실측: 스펀지잡 6편 중 3편이 4·10·12자
   ("Thank you." "Bye." "Altyazı M.K."=Whisper 환각), 두피빗잡 8편 중 7편이 0~20자.
   `{출처}`에 "액체"가 들어간 원인이 이것. 말 없는 영상은 재료에서 빼고 화면에 경고.
5. **「바꾸기」 404 수리.** `/api/script/beat/regen`이 `list_style_spines(status="approved")`로 찾는데
   원문형 368개는 전부 pending → "없는 스타일" 404. 백본 생성은 pending도 쓰는데 바꾸기는 안 쓴다(0순위-B).
6. 인스타 적용 — **유튜브가 검증된 뒤에.**

### 스파인 거르기 (페이지만 만들어둠, DB 미반영)
`tools/spine_presets/reviewpage.py` → 바탕화면 `대본샘플_원문형스파인.html`(368개 전수).
368개 중 **115개 걸러짐**: 끝잘림 101 · 인스타CTA 12 · `[음악]` 4 · 존대반말섞임 2.
⚠️말투 혼재 판정은 한 번 틀렸다 — `~했는데` 같은 **연결어미를 반말로 오판**해 133개가 걸렸다.
   연결어미를 빼니 2개로 줄었다. 판정 고칠 때 실제 대본을 눈으로 볼 것.

### 잘린 자막 살리기 (Whisper) — 절반 실패
`tools/spine_presets/rescue_cut_subs.py`(서버 `/tmp/`에도). 결과 **살림 20 · 건너뜀 29 · 실패 52**.
실패는 봇차단이 아니라 **"This video is unavailable"(영상 삭제/비공개)** — 재시도해도 안 된다.
⚠️내가 `download:RuntimeError`로 뭉개 적어 봇차단으로 오진했다. 사유를 갈라 적도록 고쳤다.
뿌리: 유튜브 자동자막(src=ytsub)은 **601편 중 483편(80%)**이 끝이 잘린다(인스타는 5%).

### 라이브 상태
- main `3d4846151` 배포·재시작 완료(HTTP 200). `backbone_script_enabled='admin'`.
- 원문형 스파인 **368개 전부 pending** — 고객에겐 안 나간다.
