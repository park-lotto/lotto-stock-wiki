# 모션레벨훅 (3초 훅 자동 In-Point) — 핸드오프

> 소유 트랙: **모션레벨훅** · 이 파일은 이 트랙 세션만 수정.
> 최종 갱신: 2026-07-29 (집PC) · P2 완료·병합 대기

**트리거**: "모션레벨훅 이어서" → 이 파일 + `docs/superpowers/specs/2026-07-28-하이브리드-장면인벤토리-design.md` 읽고 시작

---

## 한 줄
숏템메이커 믹스의 **3초 훅을 준비동작 스킵 → 모션 피크에서 자동 시작**하게 만들었다(P1). 이어서 **대본 인벤토리에도 모션레벨을 노출**했다(P2). **둘 다 subagent-driven development로 구현·리뷰 완료, finish 대기.**

## ✅ P1 완료·라이브 (origin/main 병합)
- **모션레벨**: `scene_cut.py` — `frame_motion`(ffmpeg YDIF, ss/to 윈도우), `motion_thresholds`·`level_of`·`cut_motion`(LOW/MED/PEAK), `peak_time_in_window`(윈도우 내 피크 시각, 클램프·실패시 무변경).
- **훅 자동 In-Point**: `video_assemble.py` — `_render_mix` 진입서 `_apply_hook_inpoint` 호출 → 훅 비트(beats[0]) primary.start를 **모션 피크 + delta**로 이동. 순수함수 `pick_hook_start`, `_hook_delta`. 옵션(ii) 지연계산(훅 소스만 ffmpeg 1패스). **훅 세그먼트 내에서만·유출0·실패시 원위치.**
- **프리뷰 0.2초 미세조정 UI**: `static/produce.html` — 믹스 미리보기에 `🎯 훅 시작점 [◀0.2초][0.2초▶]` → `hook_inpoint_delta` 누적 → `saveWork`(POST /api/produce/works) → 재프리뷰. `_workState`·복원 2곳에 필드 반영. ⚠️라벨은 인라인 계산(리프레시 JS테스트가 함수 부분집합만 node평가 → `_fmtHookDelta` 외부의존 금지).
- **테스트**: `tests/test_motion_level.py`(순수 8). 게이트 통과(기준선13 신규0). 1차 finish는 JS ReferenceError로 막혔다가 인라인화로 재통과.
- **실영상 그라운딩**: 37d67b0e6015.mp4(17.2s) 훅[0~4s] 피크=1.03s → 앞1초 스킵. 사장님 육안 "바꾼 게 더 낫다".

## ✅ P2 완료 (2026-07-29, 집PC — subagent-driven development, Task1~5)
**설계서 §3 P2 원안("whisper 세그먼트를 시각컷으로 재베이스")을 재조정**했다 — 실측 결과 대본 생성 단계(`edit_plan.py:_build_inventory`)엔 영상 파일 경로가 없어 그 자리서 ffmpeg를 못 돌린다. 대신 **추출 단계(`script_extract.py:extract_script`, 이미 video_path 보유)에서 컷+모션을 계산해 seg마다 `motion_level` 필드를 미리 박아 DB(`segments_json`)에 저장**하는 방식으로 바꿨다. 인벤토리 seg_id 체계는 완전 불변.

- **T1** `scene_cut.map_segments_to_motion_levels(segments, cuts_labeled, fps)` — seg-컷 교집합 최대 매핑 순수함수. 동률은 먼저 나온 컷(시간순 앞쪽) 채택.
- **T2** `script_extract._assign_seg_ids(video_id, raw_segments, motion_map=None)` — motion_map(`{seg_id: level}`)을 받아 `motion_level` 필드 주입. 기본값 `None`이라 하위호환.
- **T3** `_boundary_hint` 반환형을 `str`→`(hint, cuts, fps)`로 확장(detect_cuts 중복 호출 방지) + `_compute_motion_map` 신설 + `extract_script` 배선. frame_motion은 별도 1회 더 돈다(총 ffmpeg 2패스 — 3패스에서 줄인 것).
- **T4** `edit_plan._build_inventory`가 `seg_map`과 프롬프트 라인(`| 모션:{level}`)에 motion_level 노출. **`scene_desc` 문자열 자체는 완전 불변** — `_claim_key`가 scene_desc를 토큰화해 앵커 dedup 키로 쓰므로, 모션 정보를 문자열에 섞으면 무관한 세그먼트끼리 오염될 위험이 있었음(설계 단계에서 발견, 코드로 격리).
- **T5** `_dedup_anchors`가 motion_level 필드에 영향받지 않음을 회귀 테스트로 고정.
- **게이트**: 커밋 `23eb1e388`~`f39e38970`. `python -m pytest shopping_shorts/ -q` 전체 실행 결과 14 failed(apify_client 6·tiktok_search 2·mix_pipeline 4·edit_plan_build 2) + 2540 passed. **14건 전부 base(35598ce86, P2 시작 전)에서 `.env` 실키로 대조 재현 — 사전 존재 결함, P2발 신규 회귀 0건** 확인(worktree로 직접 대조).
- **각 태스크 spec-reviewer + code-quality-reviewer 통과**(Ready to merge: Yes, Critical/Important 0건 — 리뷰가 짚은 사소한 docstring 문구는 즉시 반영).

## ⚠️ 함정(다시 밟지 말 것)
- `_apply_hook_inpoint`는 반드시 try/except·유출0(소스 밖 금지)·실패시 원위치 — 렌더 안 죽인다.
- produce.html의 `_renderMixReviewBody`는 리프레시 회귀테스트(node로 함수 부분집합 평가)를 타므로 **외부 함수 의존 금지**(인라인).
- 추출 파이프라인은 인스타수집·AI픽자동적재 세션이 동시 수정 → **트랙 격리 + 어댑터 최소침습** 필수.
- **`_build_inventory`류 함수를 건드릴 때 `scene_desc` 문자열에 아무것도 섞지 마라** — `_claim_key`/`_is_face_seg`가 그 문자열을 그대로 읽는다. 새 메타데이터는 항상 별도 dict 키로.
- **`python -m pytest shopping_shorts/` 전체 게이트는 `.env`(실키) 유무에 따라 결과가 달라진다** — `test_edit_plan_build.py::test_build_edit_plan_exhausted_returns_empty`처럼 `_current_key_and_idx`만 monkeypatch하고 `key_vault` 캐스케이드(`_vault_call`이 쓰는 `get_live_keys_cascade`)는 안 막는 테스트가 있어, 실키가 있으면 진짜 API 호출이 나가 실패한다. 이건 **P2가 만든 게 아니라 원래 있던 결함** — 회귀 판정 시 `.env` 유무를 맞춰서 base와 대조할 것(이번엔 worktree로 직접 검증함).

## ⏭ 다음
- **P3(OCR)**: 사장님이 제외 결정(2026-07-28) — 스킵.
- **P4 Selective Vision 룰화**: `_VISION_TAG_CAP`(전량 상한) → PEAK 훅후보·무자막 B롤만 이미지 전송하는 룰 기반으로. 절감폭은 실측 검증 필요(설계서 §3 P4).
- **B. 피크 기준 보강**: 지금은 모션(움직임 양)만. 색대비·before/after 반전 가중 추가(로컬, Vision 없이) — 아직 미착수.
- **motion_level 실사용 관측**: 지금은 프롬프트에 `| 모션:PEAK` 텍스트로만 노출됨 — Gemini가 실제로 이 신호를 대본 생성에 반영하는지는 아직 라이브 관측 전. 배포 후 실수집 영상으로 확인 필요.

전체 설계·비목표(마스킹·OCR·업스케일 제외)·P3/P4는 `docs/superpowers/specs/2026-07-28-하이브리드-장면인벤토리-design.md`.
P2 실행계획: `docs/superpowers/plans/2026-07-29-인벤토리-모션레벨-매핑-P2.md`.

---
## ⚠️ 크로스세션 신호 (2026-07-29) — 하이브리드장면인벤토리 트랙 읽어라
**추출 입력전환(영상 통째 업로드 → 파이썬 컷+프레임 + 오디오 Whisper 전사 → 제미니는 프레임만 태깅)은
모션레벨훅 트랙이 담당한다.** 설계: docs/superpowers/specs/2026-07-29-프레임태깅-추출전환-design.md
- 여기서 `extract_script_frames`(플래그 on/off)를 신설하고 script_extract/video_analysis 입력부를 바꾼다.
- 하이브리드 트랙은 **태깅 후단(모션레벨·인벤토리 시각컷 재베이스·OCR)**만 — 추출 입력부는 겹치지 마라.
- 출력 스키마는 기존 extract_script와 100% 동일하게 유지하므로 후단은 영향 없음.

---
## ⏭ 집에서 이어서 (2026-07-29 저녁 마감)

### 오늘 다음 할 것: 1단계 대본 "문장 표현 품질" 수정
- **핵심 진단(실측)**: 오늘 배포한 **스파인(장면 먼저→대본)은 성공**. job cfba13cc 실측: 비트 순서 문제→해결→결과→CTA로 말 되고, 장면↔멘트 맞음(선풍기 멘트에 선풍기 화면), fit4~5, CTA 마지막. e5840bfe의 "완성 멘트에 기계 화면" 어긋남 해소됨.
- **남은 문제 = 문장 표현(말투) 품질**: "동료가 왜 있나 알겠네요", B안 "남편이 화냈냈더니 버튼 한번에 태도가 바로 바뀌네요" 처럼 **어색·깨진 문장**. 이건 구조가 아니라 **제미니가 대본 문장을 쓰는 품질** = `edit_plan._scene_first_candidates` 프롬프트의 말투/스토리 영역(오늘 스파인 변경과 무관, 별개 튜닝).
- **집에서**: `_scene_first_candidates` 프롬프트에서 "생생한 스토리감(~그런거 있죠?·~더라구요·~거든요)" + 금지어 + 어색문장 방지를 다듬는다. 부품은행 어미/부사 재주입도 검토. 결과는 완주 1회로 A/B/C 후보 문장 매끄러움 육안.

### 라이브 상태 (안전)
- 오늘 라이브 병합: **장면스파인 먼저 · 추출 60→180초 · 워커 · extract_auto · B1 프레임태깅(+503 폴백)**.
- **B1 플래그 `frame_extract_enabled='0'`(꺼짐)** — 추출은 검증된 옛 경로(영상 통째 시청 + 503 폴백). B1은 라이브 밖 실측 후에만 재점화. **검증 없이 라이브 켜지 말 것**(오늘 실수 교훈: 제미니 503 겹쳐 "대본 구조 비어" 에러 유발함).
- 미커밋·미푸시 0. HEAD ecf7c573d origin/main 병합됨.

### 제미니가 영상 "보는" 시점 (헷갈리지 말 것)
- **1단계 추출(분석)에서만 영상 통째 시청** → 장면·나레이션 뽑아 캐시(script_extracts, shortcode 전역·cid 무관).
- **2단계 매칭/믹스는 영상 안 봄** — 저장된 텍스트로만 대본·장면 조립. 캐시 있으면 1단계 0초.

---
## ✅ 1단계 대본 문장품질 튜닝 완료 (2026-07-29 밤, 집PC)
- **재현**: 합성 인벤토리 2개 소재(무선실링팬·전동다지기) × 총 3회 완주(후보 15개)로 실제 API 호출 관찰.
  **패턴 확정**: 대화 인용에서 따옴표가 거의 매번 빠짐 — "엄마가 옆에서 보더니 진짜 곱네 하시더라고요"
  류로 누구 말인지 안 보이게 뭉개짐. 회사에서 본 "동료가 왜 있나 알겠네요" 류 어색함의 근본원인으로
  추정(따옴표 없는 인용이 앞 문장과 붙어 문법이 깨진 것처럼 읽힘).
- **수정**: `edit_plan.py:769` 대화인용 지시를 예시 하나에서 **BAD/GOOD 쌍**으로 강화(다른 규칙들도
  이 형식이라야 실제로 지켜졌던 전례를 따름).
- **검증**: 같은 2소재로 재실측(후보 15개) — 전부 정상 따옴표 인용으로 개선 확인(재현율 0/6 → 15/15 근사).
- **게이트**: `pytest test_scene_spine.py test_hook_visual.py test_edit_plan_build.py` — 실패 2건은
  `test_edit_plan_build.py`의 `.env` 실키 결함(수정 전 base에서도 동일 재현, 회귀 아님 확인 — git stash 대조).
- **미검증**: "화냈냈더니" 같은 활용형 글리치는 이번 재현에서 안 나타남 — 빈도 낮은 개별 글리치로 보고
  보류. 실사용(라이브)에서 계속 나오면 재조사.

---
## ⏭ 추가 발견(2026-07-29): 추출이 배경 소품에 낚여 주 제품 오인
- **증상**: 핫도그모양 선풍기(Gou) 영상인데 책상 위 **강아지 인형(배경 소품)** 때문에 제미니가 주 제품을 "강아지 선풍기"로 오인 → 대본 3안 전부 "강아지 선풍기"로 고정(사장님 실측 스샷).
- **뿌리**: `script_extract._PROMPT`의 scene_desc/제품식별이 **전면·손에 든·주인공 제품 vs 배경 소품**을 구분하는 지시가 약함. 기존 "헷갈리는 물체 단정 금지"만으론 배경 소품 낚임을 못 막음.
  - ★보강 사례: 배경 소품(인형)뿐 아니라 **프레임 안 진짜 동물(코기)**도 제품으로 오인함(파란 모자모양 선풍기인데 옆 강아지 때문에 "강아지 선풍기"). 3개 소스가 서로 다른 제품이면 대본이 뒤섞이는 것도 별도 고려 필요(아직 미착수).

### ✅ 수정 완료 (2026-07-29 밤)
- `script_extract._PROMPT` scene_desc 지시에 "★주 제품 판별" 블록 추가 — 주 제품=반복 클로즈업·손에
  들리는 물건, 배경 소품(인형·장식)·진짜 동물·사람은 맥락일 뿐 제품 아님을 명시 + 선풍기/강아지
  구체 예시.
- `product_identify._PROMPT`(Lens 역검색, 검색 키워드 정밀도용)도 같은 오인 전파 확인해 동일 취지
  문장 추가 — 기존엔 "배경 소품·조리도구"만 언급, 동물·사람 케이스가 빠져 있었음.
- **가드 테스트 추가**: `test_prompt_main_product_vs_background_prop_guard` (script_extract·
  product_identify 양쪽) — 프롬프트에 "배경 소품"·"동물" 문구가 있는지 확인.
- **⚠️ 검증 한계**: 이건 **프롬프트 문자열에 지시가 들어갔는지**만 확인한 것 — 실제 제미니가 이
  지시를 지켜서 "강아지 선풍기" 오인을 멈추는지는 **미검증**(원본 영상 shortcode가 기록에 없어
  재현 불가 — 문장품질 수정 때처럼 합성 인벤토리로 대체 테스트할 수 없는 유형, 실제 문제 영상의
  프레임을 봐야 함). **다음에 같은 오인이 실사용에서 또 나오면 그 영상 shortcode를 기록해두고
  재현·재조사할 것.**

---
## ✅ 백본모드 표절·서브소스낭비 수정 (2026-07-30, 회사PC 관찰 → 집PC 조사·수정)

**사장님 관찰 2건**: ①백본-베이스 대본이 원본 대사를 거의 그대로 베낀다 ②서브 소스 컷이 "교체/
삽입"되기는커녕 3영상 중 백본만 거의 다 들어가고 나머지는 버려지는 경우가 많다.

systematic-debugging으로 정적분석해 원인이 서로 다름을 확인:

- **① 표절**: `_plagiarism_flags`(edit_plan.py)가 n-gram 겹침을 감지는 하는데 `_score_candidate`가
  그 값을 안 읽음 — 표절 후보가 감점 없이 그대로 추천될 수 있었다. **바로 위(2026-07-29) 금지어
  버그와 같은 클래스**(주석에 이미 그 교훈이 적혀있었는데 표절엔 적용 안 돼 있었음): "프롬프트로만
  막으면 새는데 채점에서 강제로 걸러야 안 샌다."
  - **수정**: `_plagiarism_penalty()` 신설(겹침 초과분 비례감점, 최대 0.3 — 금지어처럼 즉시 0점은
    과함, 연속값이라 `_length_penalty`식). `_score_candidate(source_full_texts=...)` 배선,
    `build_scene_first_plan` 호출부에 `src_texts` 연결.
  - TDD: 실패 테스트(표절후보 vs 오리지널후보 점수차 없음)로 red 확인 → 구현 → green.

- **② 서브 낭비**: 코드 안전망(`ensure_sources_used`)이 있긴 한데 **의도적으로 보수적**
  (`test_ensure_sources.py::test_ensure_no_action_match_leaves_untouched` — 서브 클립 행위가
  나레이션과 안 맞으면 억지 삽입 안 함, sync 우선). 그래서 서브 소스 액션 어휘가 안 맞으면 코드도
  못 구제 — **프롬프트(LLM 판단)가 유일한 레버**인데 기존 지시가 예시 없는 추상 한 문단뿐이었다.
  - **수정**(`_backbone_order_block`): (a) BAD/GOOD 패러프레이즈 예시 추가 (b) seg_id 접두사로
    서브 식별하는 규칙 명시 (c) **서브 소스를 이름으로 나열**하고 "각각 최소 한 번씩 등장" 강제
    (d) 행위가 안 맞을 땐 특정 동작을 안 지목하는 "리액션/감상 문장"으로 대체하라고 유도 —
    이러면 `ensure_sources_used`의 "행위 없는(n_act=None) 비트만 구제" 조건과 잘 맞물림(코드
    안전망과 프롬프트 지시가 서로 보완).
  - 가드 테스트: BAD/GOOD·서브 소스 식별·서브 목록 명시·"각각 최소 한 번씩"·"리액션/감상 문장"
    문구 존재 확인 + 서브 소스 없는 단일영상 케이스 별도 테스트.

- **게이트**: 관련 테스트 86개 통과(scene_first_quality·backbone_base·scene_first_gen·banned_phrase·
  candidate_length·cut_rhythm·scene_spine·ensure_sources·continuous_playback·script_extract·
  product_identify). 전체 `pytest shopping_shorts/` 14 failed(기존 baseline과 동일 시그니처:
  apify_client 6·tiktok_search 2·edit_plan_build 2·mix_pipeline 4) + 2776 passed — 신규 회귀 0건.
- **⚠️ 검증 한계**: ①은 채점 로직이라 유닛테스트로 확실히 검증됨(수학적으로 감점이 걸림).
  ②는 **프롬프트 지시일 뿐**이라 실제 제미니가 서브 소스를 실제로 더 잘 쓰는지는 라이브 관측 필요
  — 다음 백본모드 대본 생성 시 서브 소스 활용률을 눈으로 확인할 것. 계속 버려지면 `ensure_sources_used`
  의 action-matching을 더 느슨하게 푸는 것(싱크 리스크 감수)을 사장님과 논의해야 함(이번엔 손 안 댐).
