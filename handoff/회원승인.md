# 핸드오프 — 회원승인 (화이트리스트/대기실)

- 갱신: 2026-07-21 (직장 PC)
- 트랙: `회원승인` / 브랜치 `track/회원승인` (origin에 전부 푸시됨)
- 세션요약: shopping_shorts에 "사장님 승인한 사람만 사용" 대기실 방식 구현 중. SDD 서브에이전트 방식, 8태스크 중 **Task 1~6 완료·리뷰통과, Task 7~8 남음**.

## 집에서 이어가는 법 (다른 PC)
```
트랙.bat 회원승인          # 또는 py tools/track.py start 회원승인
```
→ `track/회원승인` 브랜치를 새 워크트리로 체크아웃(Task1~6 커밋 다 들어있음).
⚠️ `.superpowers/sdd/`(원장·브리프·리포트·diff)는 **gitignore라 안 따라온다** → 아래 진행표 + 커밋로그 + 계획문서로 재구성. `.env`는 `start`가 자동복사(안 되면 `copy .env .tracks\회원승인\`).

## 문서 (git 추적됨 — 집에서 보임)
- 설계: `docs/superpowers/specs/2026-07-21-회원승인-화이트리스트-design.md`
- 페이블검토 지시(결함6건): `docs/superpowers/specs/2026-07-21-회원승인-페이블검토-지시.md`
- 구현계획(8태스크 전체 코드): `docs/superpowers/plans/2026-07-21-회원승인-화이트리스트.md`

## 완료 (커밋 SHA)
| Task | 내용 | 커밋 | 리뷰 |
|---|---|---|---|
| 1 | approved_at 컬럼+기존계정 백필 | 777248f74 | ✅ Approved |
| 2 | create_customer(approved=)+get_customer approved_at | 4da5c72f4 | ✅ |
| 3 | access_level에 pending(승인전 전면차단) | d6f7fe944 | ✅ |
| 4 | 구글·아이디가입=대기계정(체험 승인시 시작) | 15180d9df | ✅ |
| 5 | approve_customer(멱등)+POST /api/admin/approve | 31371b4ac | ✅ |
| 6 | 미들웨어 pending 게이트+대기실 화면(_PENDING_HTML) | dd08ce5a0 | ⚠️ 리뷰 완료: 게이트 자체 정확(폴백·회귀 없음)이나 아래 2건으로 "Needs fixes" |

### Task 6 리뷰 결과 (마감 후 도착 — 반드시 처리)
- 🔴 **Critical: `/api/grab` pending 우회** — `/api/grab`은 `_AUTH_ALLOW`라 미들웨어 게이트를 우회하고, 자체 체크가 `access_level=="ranking_only"`만 막아 **pending(미승인) 계정도 담기+유료 백그라운드 크롤이 성공**. → **이건 이미 Task 7이 고칠 항목**(`!= "full"`로). 리뷰가 Task7 필요성을 재확인. Task7 하면 자동 해소.
- 🟡 **Important: 브랜드명 불일치(사장님 결정 필요)** — `_PENDING_HTML`이 `<title>승인 대기중 · 숏템탑스`를 **하드코딩**. 그런데 같은 날 커밋 `9f63a3a21`이 라이브 브랜드를 **"숏템박스"**로 바꿈. 대기화면만 옛 이름 노출 → ①canonical 이름 확인(숏템박스로 통일?) ②`_PENDING_HTML`을 `_fill_brand(...)` 통과시켜 하드코딩 제거(다른 페이지 방식과 일치). 계획/설계문서의 "숏템탑스"도 stale.
- 🟢 Minor: `GET /logout`이 CSRF 무방비(`<img src="/logout">`로 강제 로그아웃 가능). 저영향(멱등·세션삭제뿐)이나 보안 게이트 태스크라 기록.
- ✅ 잘된 점(재작업 불필요): pending 분기가 ranking_only 앞에 있고 모든 하위경로 return(폴백 없음), access_level 1쿼리 통합, /logout이 dash_auth 쿠키 실제 삭제(path 일치)·_AUTH_ALLOW에 없음(미인증도 500 안 남).
- 현재 전체 테스트: `py -m pytest shopping_shorts/tests/test_paywall_*.py -q` = **40 passed**(기준선 대비 회귀0)
- Task6 특이사항: `/logout` 백엔드 핸들러가 **원래 없었다**(HTML 링크만 존재, 404). pending 탈출구로 `GET /logout`(쿠키삭제→/login 303) 신규 추가함.

## ⏭ 남은 작업 (집에서)
1. **Task 6 리뷰 마무리**: `dd08ce5a0` diff를 리뷰(게이트 우회 없나·신규 /logout 안전한가). 문제없으면 원장에 완료 기록.
2. **Task 7**: `/api/grab` pending 구멍 막기 — `app.py` 3865행 부근 `if access_level(cid) == "ranking_only":` 를 `!= "full"`로(pending도 차단). 계획문서 Task7에 전체 코드+테스트. ★Task6 리뷰가 이 구멍을 Critical로 재확인 — 우선순위 높음.
3. **브랜드명 결정 + _PENDING_HTML 수정**(Task6 Important): 라이브가 "숏템박스"로 바뀜(9f63a3a21). 대기화면 하드코딩 "숏템탑스"를 `_fill_brand` 통과로 바꿔 통일. 사장님께 canonical 이름 확인.
4. **Task 8**: `list_customers`에 approved_at 노출 + `static/admin.html`에 "승인 대기중" 섹션+[승인] 버튼. pending 행은 set_plan UI 숨김. 계획문서 Task8.
4. **최종 whole-branch 리뷰**(가장 강한 모델) → Minor 모음 fix wave.
5. **실제 렌더 그라운딩**: 로컬 기동 → 신규 가입이 대기실에 갇히나 → 사장님 /admin에서 [승인] → 열리나 눈으로 확인.
6. **`py tools/track.py finish 회원승인`** → 게이트(문법·import·pytest) 통과 시 main 병합 → 3분 뒤 라이브.

## 최종 fix wave로 모은 Minor (전부 비차단)
- store.py:2387 `get_or_create_by_google` docstring "가입시 체험 자동시작" stale → 이제 승인시 시작.
- `/api/admin/approve`: 없는 customer_id면 200 `{customer:None}` 반환(404 아님). 사소.
- Task1 테스트: 재시작 시 신규 pending 계정 approved_at NULL 유지 property 미커버(코드검증됨). 회귀테스트 추가 고려.

## SDD 절차 메모
- 스크립트: `<skill>/subagent-driven-development/scripts/` 의 `task-brief PLAN N` / `review-package BASE HEAD`.
- 각 태스크: 브리프추출 → 구현 서브에이전트(sonnet) → review-package → 리뷰 서브에이전트(sonnet) → Minor는 원장기록·최종triage.
- BASE(브랜치 시작): `ddc3de659` 직전 아님 — 각 태스크 BASE는 직전 태스크 HEAD.
