# 꾸미기 트랙 로그

2026-08-18: 꾸미기(6단계) 디벨롭 — 고정카피(대본→AI 후보 4개, 대본해시 캐시로 재방문 과금 0) +
템플릿 12종(PNG, 전체/첫장면 선택) + 4탭 UI. ★착수 전 실측에서 **템플릿 렌더 배관이 이미 다 있음**을
확인(video_assemble의 overlay·_motion_layer_filters) → 새로 만들지 않고 연결만 해서 "전체/첫장면 둘 다"가
공짜가 됐다. 밟은 함정: ①정적 마운트가 루트라 URL은 /templates/(/static/은 404 — 계획 단계 실측으로 사전
차단) ②geom 딕셔너리를 12개가 참조 공유(렌더 배선 얹히면 터질 자리) ③PIL rectangle 양끝 포함이라 190→191px
④AI가 준 비문자열 값 7이 `7 or ""`를 통과해 .strip() 크래시(None은 falsy라 1차 점검을 통과했다 — truthy
비문자열이 구멍) ⑤템플릿 합류는 has_motion 판정 **위**에 넣어야 함(아래면 템플릿만 고를 때 통째 스킵)
⑥템플릿과 사장님 로고는 별도 슬롯(합치면 로고가 조용히 사라짐, 실측 overlay 필터 2개 확인).
검증: 신규 49건 전부 되돌림 시 실패 확인(가짜 green 방지), 전체 4346 passed·실패는 기준선 1건 그대로.
★실화면 스크린샷으로만 잡힌 것: 템플릿 카드 12장이 전부 검정칸(투명 PNG + 어두운 배경 + cover). DOM 검사는 전부 정상이었다 — "로드됐다"는 "보인다"가 아니다.

## 2026-08-18 — '내용물 있는 틀' 신설
빈 색띠 12종만으론 실제 잘되는 포맷(띠 안에 채널명·☰🔍·[광고]·제목·조회수)이 안 나왔다. `deco_frame.py` 신설 —
★그림 그리는 곳을 하나로 두고 미리보기(`/api/produce/frame.png`)와 렌더(`mix_pipeline._template_layer`)가
**같은 캐시 파일**을 쓰게 묶었다(CSS 흉내 금지 = "미리보기랑 다르게 나왔다"를 구조적으로 차단).
함께: 헤드카피 2줄 고정(two_lines, 40자→26자), 🗑 문구 지우기, 폰트·색상·크기 기본 펼침(이미 있던 걸 못 찾으신 것),
틀 위/아래 띠 각각 조절, 미리보기 축소·여백 제거. 게이트가 내 버그 2건(중복제거 무력화·자동채움 검사구간 침범)을 잡았다.
미검증=브라우저 실화면·실렌더 종단. 남은 것=포맷 B(형광 훅)·C(카드형, 영상 축소라 렌더 레이아웃 변경 필요할 수 있음).

## 2026-08-19 — 헤드카피 폰트 미리보기 + 6단계 확대 (4ab7516c7)
- 빠른조절 폰트를 밋밋한 select → **각 줄이 자기 글씨체로 보이는 메뉴**(hover 즉시 미리보기, 취소 시 복귀)
- 6단계만 큼직하게: 미리보기 300→460px(1600px+), 탭·문구칸·슬라이더 확대. 좁은 화면 회귀 0
- 함정: **인라인 커스텀 프로퍼티(--pvw)는 CSS가 !important로도 못 이긴다** → 정의를 .decoMirror로 이동
- 폰트 진짜 적용 판정은 캔버스 글자폭 실측으로(22종 21개 서로 다른 폭, 폴백 0건)

## 2026-08-20 — 썰쇼핑 20채널 실측 틀 + 맞춤 헤드카피 (fbb383529)
- 서버 썰쇼핑 태깅 107채널 → 상위 20개 실제 썸네일을 **픽셀 실측**해 틀 20종 신설(원장 docs/reference/)
- 틀+헤드카피를 **한 벌**로 정의(띠 높이가 달라 y가 같으면 글자가 띠에 먹힌다)
- 함정: 화면이 bar_h 190을 우기고 앞 틀 값을 물고 감(3종 눌러도 전부 268px) / **select는 옵션이 비면 .value가 조용히 무시** / 슬라이더 상한 초과는 잘림 / 강조 필드는 keyword
- 신규 14건 + 회귀 217 passed. 되돌림 검사로 3건 FAIL 확인(가짜 green 아님)

## 2026-08-20 — ★썸네일 말고 **실제 영상 59편**을 제미니에 읽혀 틀 20종 재작성 (f124ae53f)
- 사장님 지적이 정확했다: 썸네일만 보고 손으로 찍은 값은 살림킹왕짱이 통째로 뒤집혀 있었다(분홍띠+흰글씨 → 실제는 흰띠+분홍글씨)
- 20채널×3편=59편을 Gemini에 올려 띠·아이콘·헤드라인·자막바를 읽고 **생성기로 코드를 찍어낸다**(손수정 금지). 원장 docs/reference/
- 실측이 드러낸 것: 띠색 17·헤드색 18·아이콘 6가지, **띠 없는 풀블리드 4곳**, 흰 외곽선, 헤드 뒤 흰박스 9곳, 아이콘 좌우 뒤집힌 채널
- 함정: **`p.get("bar_h")`가 0을 falsy로 버려** 풀블리드가 190px 띠를 씀 / 제미니가 아이콘명을 한/영/이모지로 섞어 줌 / 흰띠+흰글씨(대비 0)
- 서버 IP는 유튜브가 봇 차단 → **내 PC에서 받아 scp**. 19건+회귀 222 passed

## 2026-08-20 — 헤드라인 글꼴 채널별 실측 (a9847a175)
- ★'무슨 폰트냐'고 물으면 없는 이름을 지어낸다 → **우리 22종 견본 시트를 영상과 같이 보여주고 번호로 고르게**. 59편 판정 → 20채널 **7종**
- 동점 11곳은 임의 선택임을 원장에 `agree`로 표기(단 후보가 전부 비슷한 굵은 고딕이라 무해). 원장 docs/reference/썰쇼핑_헤드라인글꼴.json
- ★렌더해서야 잡힌 것: **원본 크기 그대로 쓰면 폭 넘침**(108px→상한 90) / **글자색==외곽선색이면 글자가 먹힌다**(활용정점)
- 23건+회귀 222 passed. 되돌림 검사 2건 FAIL 확인

## 2026-08-20 — 틀을 고르면 '완성된 상태'로 (43ab44f51) ✅라이브
- 문제: 고른 뒤 제목·조회수가 빈 채라 **흰 제목블록이 아예 안 그려져** "띠 하나만 있고 아래 텅 빈" 그림(실측은 20곳 중 17곳이 제목블록 사용)
- PRESETS에 has_head/demo_views/demo_comments를 싣고 frPick이 미리 채움. **사장님이 적어둔 값은 안 덮음**(999만 유지 확인)
- ★`placeholder="살림킹왕짱"` 고정이 "디자인이 실제 채널과 다르다"의 원인 하나(럭키박스 틀인데 안내문은 살림킹왕짱)
- 26건+회귀 215 passed. 서버 대조: 프리셋20·글꼴7종·has_head 17 반영 완료

## 2026-08-20 — 헤드카피 뭉갬·가림·위치튐 3건 (8f4dcf52d) ✅라이브
- ★뭉갬의 원인은 두께가 아니라 **그리는 방식**: text-shadow 4방향은 두꺼워지면 검은 사본이 글자 **위로** 올라와 메운다 → `paint-order:stroke fill`로 진짜 테두리. **스크린샷으로만 잡혔다**
- ★가림: 띠만 피하면 안 되고 **흰 제목블록**까지 + y는 글자 **한가운데**라 절반 더 내려야 + 크기는 **720→1920 환산** 필요. 20/20 실패 → 0건
- 위치튐: applyHeadcopySet이 y를 무조건 덮어씀 → HC_Y_TOUCHED(5경로). 52% 유지 확인
- 30건+회귀 233 passed. 서버 대조 완료(외곽선 5.8~9.6%, y 26~44%)

- **2026-08-22** 자막꾸미기를 직접다듬기 맨 위로 올리고 기본 펼침 / 헤드카피·모션·워터마크는 접힘. 자막 칸을 ①글씨 ②자리 ③테두리·박스·효과 3그룹으로 정리(id·핸들러 무수정). 브라우저 실측: 파싱오류 0·id 14개 보존·pageerror 0. 커밋 7abd28258
