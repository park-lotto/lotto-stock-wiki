# 구절이어틀기 — 구절 맞춤 칸이 끊기지 않고 원본을 이어서 (2026-09-26, 사장님 PC)

## 사장님 결정
- "구절이 나눠져도 쭉 이어지게 / 0.8초 태깅이어도 2초 구절이면 0.8초부터 쭉 이어서" — 가져온 영상이 잘 만든 편집본이라 뒤 장면도 흐름이다.
- 구절 수만큼 카드(주황 박스)가 따로 있어야 앞/뒤/둘 다를 골라 수정 가능. 두 카드는 겹치지 않게 이어진 구간.
- 살짝(0.35초 이하) 넘치면 다음 장면 몇 프레임 대신 조각 안에서 살짝 느리게(09-17 이윤정님 '튐' 유지).

## 원인 (라이브 실측)
- 미리보기 `scene_play.js finish()`가 **항상** 읽는 길이를 조각 끝으로 잘랐다 → 09-24 '릴 뒤 이어 쓰기'는 렌더에만 돌고 미리보기는 멈춤.
- 같은 조각에 구절이 이어 붙으면 09-21 규칙이 끝 0.1초 앞으로 **되감아** 버텼다 → 미리보기 0.1초를 1.5초로(정지), 렌더는 0.77초 뒤로 튐.
- 카드 X·📦는 조각 단위라 같은 조각 카드가 전부 같이 빠지고/바뀌었다. 카드 썸네일도 조각 기준이라 둘 다 같은 그림.

## 고침 (track/구절이어틀기)
- `video_assemble._plan_phrase_clips` + `scene_play.js planClips` 구절 분기: 같은 조각 연속이면 앞 컷 끝에서 이어서, 조각 끝을 넘으면 릴 계속, pos=보여준 곳. 릴 길이 모르면 종전.
- `_piece_end_limit`(서버) + `finish()`(화면): 계획이 이미 조각 밖을 읽기로 한 컷이면 상한 = 원본 끝(속도·전환 여유가 다시 자르지 않게).
- `scene_lab.html splitBeatCards`: X/두 번 클릭/📦 때 같은 조각이 카드 여러 장이면 카드마다 film_ 조각으로 쪼갠 뒤 그 카드만. 화면 흐름 불변.
- `cardThumbId`: 카드 썸네일 = 그 카드가 트는 구간의 film_ id(서버 seg_thumb가 id에서 풂). 저장 안 건드림.
- R4(phrase_min_cut) 칸은 쪼개지 않음 — 라이브 2,251칸 중 0칸. ⚠️ 켤 때: ensure_clip_anchor가 durs 없이 phrase_owners를 불러 R4 묶음을 모른다(기존 빈틈) + 쪼개기를 묶음 단위로.

## 검증 도구 (tools/phrase_continue/)
- `dump_jobs.py` (서버, 읽기 전용) → 최근 N일 mix_jobs + 영상·음성 길이 sqlite
- `check_phrase_continue.py <dump>` 진짜 scene_lab.html(POST 차단) planClips vs plan_beat_clips_for 전수 대조. `PC_ROOT=<main 체크아웃>`로 기준선.
- `check_card_split.py <dump>` 제보 칸(job 44cdff8f7502 칸5) 카드 이어짐·썸네일·📦·X·저장→서버계획. 옛 코드 ①②⑥ 실패 / 새 코드 전부 통과.

## 알아둘 동작
- 이어진 카드의 뒤 카드 X → 그 자리는 앞 카드 조각의 이어지는 구간으로 다시 채워진다(규칙상 구절보다 짧으면 원본을 이어서). 화면상 X가 '안 빠진 것'처럼 보인다. 그 카드만 다른 장면으로 바꾸는 길은 📦 교체.
- 원본 영상이 끝나면(최근 4일 약 65컷) 종전대로 남은 만큼 느리게/정지.

## 전수 대조 결과 (라이브 덤프 294작업·2,251칸, 라이브 코드 → 새 코드)
- 구절 맞춤 칸 구조 불일치 618 → 39 · 미리보기 멈춤 컷 598 → 42(렌더 42와 동일) · 끝 컷 0.06초 이하 차이는 반올림(별도 집계)
- ⚠️ 구절 맞춤 **끈** 칸 불일치 350 → 324는 **원래 있던 것**(8d190969ca85·d220f65a66a6로 기준선과 결과 동일 확인). 수동 컷·홀드 경로 — 다음 과제.

## 2026-09-26 11시 — ★라이브 회귀: 장면이 원본 영상 맨 끝이면 이어 틀기 카드가 검정 (사장님 캡처)
- job 7c6c929065ba 칸10: 영상5(s4) 8.07~9.45초 하나만 남기자(영상 길이 9.45초) 2~4번째 카드가 9.45초 너머를 읽어 검정.
- 내 실수: 스캔에서 '원본 끝 도달 65컷'을 세고도 화면 모습을 확인 안 했다.
- 고침: 서버·화면 둘 다 st > 릴-0.1이면 st=릴-0.1(마지막 프레임에서 버팀 = 09-21 정지와 같은 그림). 썸네일도 따라 원본 안.
- 검사 `check_reel_end.py`: 배포본 ①②③ 실패(9.45~9.75 요청) / 수정본 통과. pytest 143·카드검사·오늘 34작업 대조 이상 없음.
- 사장님 제안 검토 중(칸2 인형→리모컨 0.6초): 인형 1.4초로 구절 2.01초를 채우려면 1.44배. 비교 영상 Desktop\칸2_비교\ A현재/B 1.44배/C 1.15배+끝정지.
