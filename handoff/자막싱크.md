# 자막싱크 트랙 핸드오프

_최종 갱신: 2026-08-06_

## ✅ 2026-08-06 — 자막 드리프트 3종 수정 (커밋 bf4dcad73)

사장님 제보 "자막 타임스탬프가 약간씩 안 맞는다" → 원인 3개를 **실측 재현**으로 확정하고 전부 수정.
7/18 핸드오프의 "로컬 키0라 ASR 실경로 미관측"이 바로 이 구멍이었다.

| # | 원인 | 실측 드리프트 | 수정 |
|---|---|---|---|
| ① | `phrase_durs_from_words`가 첫 구절 시작(리드인)을 버리고 합을 total_dur로 **정규화** | -0.50 → -0.20s (자막이 먼저 뜸) | `PhraseTiming(durs, lead_in)` 반환, 렌더러가 `lead_in`부터 시작. 정규화 제거 |
| ② | `rescale`이 **선형사상 1개** — 속도감 모드는 내부 무음까지 잘라 조각별 왜곡 | +0.11 → +0.28s **누적** | `measure_removed_spans`로 잘릴 구간 선측정→`.cuts.json`, 조각별 보정 |
| ③ | `cap_durs`가 합성 시 1회 계산 후 **무효화 없음** → 수동 트림 뒤 어긋남 | 트림량 비례 | `_adjust_caps_for_trim`이 `head_trim`을 리드인에서 갚음 |

**②의 함정(꼭 기억)**: `silenceremove`는 내부 무음을 **삭제가 아니라 `stop_duration`(0.3s)까지 줄인다**.
통째로 사라진다고 계산하면 이번엔 자막이 **반대로 빨라진다**(감지 2.2s vs 실제 삭제 1.433s).
`measure_removed_spans`가 `(start, end - 0.3)`을 내는 이유. 잔차는 `_PACE_TAIL_PAD` 기준 균등배분으로 흡수.

- **결과**: 최대오차 **0.276s → 0.037s**(7.5배, 누적 사라짐). E2E 렌더 경로 드리프트 **0.00s**.
- ①과 ②는 **부호가 반대**라 비트마다 상쇄·증폭이 갈렸다 → "완전히 틀린 게 아니라 약간씩" 증상의 정체.
- 미리보기(`produce.html` `_vpDriveCaption`)도 `cap_lead`를 쓰게 해 "미리보기≠최종영상" 차단.
- 테스트: 신규 `tests/test_caption_drift.py` 9건(실 ffmpeg 포함). 전체 3278 passed, **신규 회귀 0**
  (기준선 18건 실패 그대로 — `test_voice_params_extracts_pace_mode` 등은 이전부터 깨져 있던 것).

### ⏭ 남은 것
- **서버 실영상 육안 확인**(로컬은 ElevenLabs 키 없어 TTS 사이드카 실경로 미관측 — ②의 실데이터
  검증은 서버에서). 한 편 뽑아 자막이 말과 붙는지 들어볼 것.
- `cap_offset` 딸깍(수동 당기기/밀기)은 **상수 이동**이라 원래 ②의 누적 드리프트를 못 잡았다.
  이제 필요 없어졌는지, 아니면 잔차용으로 남길지 관찰 후 판단.

## ✅ 이번 세션 완료(전부 라이브 병합)

1. **1단계 — 자막↔음성 싱크 자동교정**(origin/main 422a00a8). 비트 안 자막 표시시간을 글자수추정→ASR 워드타임스탬프로. 무키 폴백 바이트동일=회귀0.
2. **2단계 — 제작소 음성 프리뷰**(origin/main b30c6ffd). TTS 단계 "이 음성으로 전체 생성" 후 #ttsBeats에 프리뷰: 비트별 프레임+실음성+자막 가라오케 재생·🟢맞음/🟡불확실·당기기/밀기 딸깍(cap_offset)·톤 재생성(job voice 스냅샷 병합=소리 일관)·부드러운 게이트. SDD 6태스크+opus 태스크리뷰+whole-branch 최종리뷰(Critical 1건=프레임 URL /produce 누락까지 잡아 수정). ⚠️로컬 키0라 ASR·regen·가라오케 실경로는 미관측 → 서버서 실영상 grounding 필요.
3. **자막 전환 겹침 수정**(origin/main, tail 인자). 각 비트 마지막 자막의 0.5초 여운이 다음 비트로 넘어와 겹치던 것 → 마지막 비트만 여운, 중간 비트 tail=0. 신규 테스트3(no-overlap 속성)+회귀 317 passed.

## ⏭ 미결(사장님 결정/후속)

- **★"와 와" 더블 (우리 버그·원인확정·미수정)**: 추임새 멱등가드 `_LEADING_INTERJECTION_PAT`(narration_naturalize.py:1174)가 **쉼표 있을 때만**(`와,`) 인식. 원문 "와 진짜…"(쉼표 없는 문두 추임새)는 못 걸러 앞에 "와," 또 붙음 → "와, 와 진짜…". **고칠 자리=그 정규식이 쉼표 없이 띄어쓰기로 시작하는 문두 추임새도 잡게.** ⚠️narration_naturalize.py=보이스 트랙 영역 → 자막싱크서 고칠지/보이스로 넘길지 사장님 결정 대기. (덤: 훅 문장이 헤드카피+첫 대사 중복 = edit_plan 생성 이슈, 별건.)
- **①장면 전환 빛줄기/겹침 = 소스 릴스 자체 크로스페이드**(우리 코드는 하드 concat + 하드 오버레이, fade 0). 우리 버그 아님. 대응=크로스페이드 심한 소스 회피.
- **②하단 검정 = 검정바 아님**(Y16~26·cropdetect 전구간 720:1280 테두리없음=소스 어두운 하단). 없애려면 하단 크롭/밝기보정뿐(소스 손실).
- **#4 출력 720×1280**(_OUT_W/_OUT_H, video_assemble.py:25). 1080×1920은 한 줄 변경이나 파일2배·렌더↑ 트레이드오프. 사장님 결정.
- **2단계 후속 Minor**: `.superpowers/sdd/capsync2-progress.md` 롤업(regen char-cost 표시·오디오정지 루프·tautology 테스트 강화·out-of-order 픽스처 등).

## ⚠️ 환경/운영

- **git stash 금지**: 트랙 worktree는 refs/stash 공유 → 남 세션 작업 pop됨(2026-07-18 실사고, stash@{0}에 남세션 app.py+produce.html 파킹됨—원세션 재적용 필요). 회귀비교는 커밋 후 diff. 메모리 `reference_git_stash_shared_across_worktrees`.
- **대시보드 아티팩트 재발행**: 오늘 429(frame_daily_push_cap_reached) 한도. dashboard_cli·로컬 html은 갱신됨, url=8ea243dd로 **내일 재발행**만 남음.
- 트랙 폴더 raw/ CRLF churn이 finish 재정렬·merge를 막음(배포 무관). 정리 이연.
