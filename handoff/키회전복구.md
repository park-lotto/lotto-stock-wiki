# 키회전복구 — auto 커밋이 되돌린 키 관리 개선을 되살린다

## 2026-09-02 — 무엇이 사라졌었나

`83be8aa62 "auto: session changes"`(9/1 15:24)가 여러 파일을 **옛 상태로 통째 되돌렸다.**
다른 파일은 이후 재작업으로 회복됐지만 `pipeline/atoms/key_vault.py`만 남아 있었다.

사라진 것(실측 — main에 함수 자체가 없었다):
- `_EXHAUST_TTL_S` / `_exhausted_map` / `_live_exhausted` — 소진 잠금을 **하루 낙인 → TTL 30분**으로
  바꾼 것(f8170b7a1). 되돌려진 동안 멀쩡한 키가 한국 자정까지 잠겨 있었다.
- `mark_exhausted(retry_after)` · `_load_state` 날짜무시 · `get_live_keys` TTL 필터
- `rotated()` + `_RR_CURSOR = os.getpid()` — 키 목록 회전(3ae3b2b6c).
  없으면 `for key in keys` 호출부가 **항상 keys[0]**부터 시작해 앞쪽 키만 얻어맞는다
  (실측 당시: 쓸 수 있는 키 82개 중 21개만 사용, rpm 990건의 88%가 그 쏠림)
- 호출부 4곳의 회전 적용: `dashboard/server.py`(3곳) · `vector_db.py` · `keyroute.py`

## 어떻게 복구했나

되돌리기 **직전 커밋 `3c512b1447`의 원문 그대로** 되살렸다. 단, 그 뒤에 정당하게 들어온
작업은 건드리지 않았다 — `pick_paced_key`, `seconds_until_quota_reset`,
`retry_delay_seconds`의 일일소진 분기는 그대로 둔 채 합쳤다.

테스트 `tests/atoms/test_key_vault.py::test_state_resets_on_new_day`는 **옛 설계**
("날짜가 바뀌면 리셋")를 검사하고 있었다 — TTL 설계로 바뀐 뒤 아무도 안 고쳐서 방치돼 있었다.
`test_old_format_lock_expires_by_ttl`로 갱신했다(옛 형식은 기본 TTL 동안 잠기고,
만료시각이 지난 기록은 자동으로 풀린다).

⚠️ **남겨둔 함정(주석으로 명시)**: `mark_exhausted`의 잠금 상한은 6시간인데
`retry_delay_seconds`는 일일 소진에 최대 24시간을 준다. 지금은 호출부가 전부 2인자로
불러서(=retry_after를 안 넘겨서) 차이가 없다. 넘기기 시작하면 상한도 25시간으로 올려야
일일 소진 키가 6시간 뒤 풀려 또 얻어맞는 일이 안 생긴다.

## 검증
- 키 관련 테스트 28건 통과(`tests/atoms/test_key_vault.py` + `shopping_shorts/tests/test_vault_key_spread.py`)
- 회전 실동작 실측: 시작키 `K4→K0→K1→K2→K3→K4`(5키 순환), 원소 보존, 키 0·1개도 안전, PID 씨딩 확인
- import: key_vault · vector_db · keyroute · shopping_shorts.app 정상
  (`dashboard.server`는 main 폴더에서도 같은 `briefing_detect` 경로 오류 — 내 변경과 무관)

⏭ 다음
- 배포 후 실측: 최근 5분에 실제로 쓰인 키 종류 수가 늘었는지, rpm 429가 줄었는지
- **재발 방지가 진짜 숙제다** — "auto: session changes"가 남의 최신 작업을 되돌리는 사고가
  이번이 처음이 아니다(memory `feedback_auto_commit_staged_conflict_broke_live`).

### 게이트가 잡아준 것 — 테스트도 같이 되돌려져 있었다

첫 `finish`에서 `test_byok_gemini_wiring.py` 2건이 깨졌다. 원인은 내 회귀가 아니라
**그 테스트도 83be8aa62에 같이 되돌려져 있던 것**이다: 회전이 살아나면 키 순서가
호출마다 달라지므로 9/1에 `sorted(...)`(집합 비교)로 바꿔 뒀는데, 되돌림으로
순서 고정 비교(`== ["OWNER-1","OWNER-2"]`)로 돌아가 있었다.

되돌리기 직전 원문으로 복구했다(바뀐 것은 비교 4줄 + 이유 주석뿐, 테스트 함수 목록 동일).
→ 관련 테스트 37건 통과.

★교훈: 회귀 복구는 **코드만 되살리면 안 된다** — 그 코드에 맞춰 고쳐졌던 테스트도
  같이 되돌려져 있을 수 있다. 게이트의 "새로 깨진 테스트"를 내 탓으로만 보지 말고
  되돌림 커밋이 그 파일도 만졌는지 확인하라.

### 배포 후 실측 — 복구는 동작하나 rpm은 안 줄었다(원인은 별개)

서버 실측(2026-09-02, `reference.db/api_events`, KST):

| 구간 | 호출 | 쓰인 키 | rpm 비율 |
|---|---|---|---|
| 13:45~14:30 옛 코드 | 1,076건 | 86종 | 13.9% |
| 14:30~15:15 전환기 | 569건 | 75종 | 21.3% |
| 15:15~ 새 코드 | 359건 | 78종 | 24.8% |

**회전 자체는 정상 동작한다**(서버에서 직접 확인: 시작키 K2→K3→K4→K0→K1→K2,
실제 풀 27키에서 연속 호출 첫 키 다름). 그런데 rpm은 오히려 늘었다.

★쏠림이 남아 있다: 새 코드 구간 평균 4.6건/키인데 상위 키는 25건(5.4배).
★rpm 89건 중 **67건이 `proc=server`**(주식위키 대시보드) — 쇼핑쇼츠 web은 8건뿐.

**진짜 원인(코드 확인)**: `dashboard/server.py:_gemini_text`가 페이서를 통째로 우회한다.
```python
for k in keys:                        # 회전된 목록이라 시작점은 다르다(여기까진 맞다)
    client = key_vault.get_client_for_key(k)
    ...
    except Exception:                 # 429가 나면
        continue                      # ★대기도 없고 mark_exhausted도 없이 즉시 다음 키
```
쇼핑쇼츠 쪽은 2026-09-01에 `pick_paced_key`로 이 우회를 막았는데(커밋 3569534d2 —
대본생성·SEO·썸네일문구·부품은행), **대시보드는 그 처방을 못 받았다.**
429 한 번에 키를 연달아 태우니 회전으로 시작점을 옮겨도 rpm이 안 준다.

⏭ **손대지 않았다 — 판단이 필요하다**
`_gemini_text`의 키 루프에 `pick_paced_key`를 넣으면 rpm은 줄지만 **대화형 응답이
느려질 수 있다**(키당 최소 간격 12초). 대시보드는 사용자가 기다리는 화면이라
그 맞바꿈을 사장님이 정해야 한다. 쇼핑쇼츠 고객 서비스 쪽 rpm은 8건으로 미미하다.
