# 폰트검수

## 2026-08-26
- 최종렌더 한글 두부(네모X) 해결. 원인은 깨진 파일이 아니라 **영문 전용 폰트**(OkMallangW.ttf,
  글리프 81개·한글 0자)를 한글 폰트 목록에 넣어둔 것. 정상 TTF라 브라우저·ffmpeg가 오류 없이 로드했다.
- 두 겹 방어: sync_fonts.py가 한글 글리프 없는 폰트를 목록에서 막고(영문 전용은 "latin": true),
  video_assemble._font_ref가 렌더 직전 못 그리는 문구면 기본폰트로 폴백한다.
- 폰트 해석이 자막·헤드카피에 따로 적혀 있던 것을 _font_ref 하나로 통합(0순위-B).
- 판정은 이름·크기가 아니라 .notdef 마스크 대조(Pillow만 — fontTools는 서버에 없다).
- (2차) 37종 전부 실제 ffmpeg 렌더로 눈 확인 — 두부는 옥말랑 하나뿐, 빙그레·리디바탕은 공백만 ⊠(기존 우회 있음).
  사장님 결정으로 옥말랑을 목록에서 제거(37→36종, 파일은 가드 시험용으로 남김).
  실사용 어휘 121자 점검에서 완성형 폰트 10종이 '뷁똠뎊' 누락 → 1차 가드(한 글자라도 폴백)가 과해서
  _FONT_FALLBACK_RATIO=0.5로 완화. 두부 한 글자보다 글꼴 통째 교체가 더 큰 사고다.
- (3차) "렌더에서 안되는거 없겠어?" → 폰트 경로가 셋인데 둘만 고쳤던 것을 발견.
  deco_frame(꾸미기 틀)에 공백 우회가 없어 빙그레·리디바탕 채널명·제목의 띄어쓰기가 ⊠였다(별개 실버그).
  ★판정기는 공백을 검사에서 빼기 때문에 이 사고를 못 잡는다 — 36종을 눈으로 렌더해 찾았다.
  font_glyphs.py 신설(판정 단일 출구), video_assemble은 위임 껍데기로, deco_frame 연결. 525건 통과.
