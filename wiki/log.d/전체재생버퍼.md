# 전체재생버퍼

## 2026-08-16
- 3단계 전체 재생 이음매 버퍼 근본수정 라이브. 원인 ①음성만 예열이 없어 칸 이음매에서 mp3를 그제야 받음(화면은 A/B 예열됨) — `curT`·자막이 음성을 시계로 써서 진행바까지 정지 ②다음 칸 미리 앉히기가 `_slot=0` 고정이라 같은 소스면 재생 중인 `<video>`에 `currentTime`을 꽂음.
- 처방: 음성도 A/B 두 벌(`ttsEl`/`seatTts`)로 다음 칸 선로딩, 칸 넘김 전용 슬롯(`handoffSlot` 2↔3 교대), 첫 칸만 예전 슬롯 유지(`preSeated`).
- 남은 것: 사장님 육안 확인.
