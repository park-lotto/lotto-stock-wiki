> **소유 트랙**: 대본믹스통합 — 이 파일은 대본믹스통합 트랙 세션만 수정한다. 다른 트랙은 읽기만.
> 원본: NEXT_SESSION.md에서 2026-07-15 분리. 규칙: `handoff/README.md`

## 🆕 작업파일 — 6태스크 구현·병합·라이브 + 최종리뷰 Critical 2건 수정 (2026-07-17)

**사장님 제보**: *"뒤로가기 하니까 작업물이 지워지는데 ... 내일 다시 들어와도 기록남고
그대로 진행할수있게 해줘야한다."*

> ⚠️ **이 섹션이 없어서 다음 세션이 헛돌 뻔했다.** 창(`0fa3e228`)이 14:49에 죽었는데 핸드오프에
> 작업파일이 **한 줄도 없어**, 복구 세션이 아래 "미리보기 게이트"의 ⏭를 보고 **이미 끝난 최종
> 리뷰를 또 하려 했다**. 이 파일의 맨 위 교훈("⏭를 그대로 믿고 착수하면 이미 끝난 일을 또 한다")이
> 정확히 재현됐다. → **기능을 만들면 그날 바로 여기 섹션을 만들어라.**

### 상태: 전부 main 병합·라이브

- 구현 6태스크 원장: `.superpowers/sdd/works-task-{1..6}-{brief,report}.md`
- 리뷰 대상 diff: `.superpowers/sdd/works-final-review.diff`(커밋 11개)
- 최종 whole-branch 리뷰 = **창이 죽어 13:42에 diff만 뽑고 못 돌린 것** → 2026-07-17 복구 세션이 돌림.
- 수정 커밋 `c350acdd` → main `2dfce39c`. 게이트 **10 failed / 963 passed**(기준선 10건과 동일 파일·이름, 회귀 0).

### 🔴 최종 whole-branch 리뷰가 Critical 2건을 잡았다 — 태스크별 테스트 62건은 전부 green이었다

**각 태스크는 각각 옳았다. 결함은 태스크와 태스크 사이에만 있었다.** 이 트랙에서 최종 리뷰가
개별 리뷰를 뚫고 이음새를 잡은 **세 번째** 사례(앞선 둘: I-1/I-2, 미리보기 게이트 C-1~C-3).

| | 결함 | 수정 |
|---|---|---|
| **C-1** | **"+ 새 작업"이 새 작업이 아니라 직전 작업을 덮어썼다** — `sidebar.js:103`이 `/produce`로만 가는데 `cad0e7b0`이 `_consumeProduceHandoff`에 `WORK_ID` 복원을 넣어, 새 작업을 누르면 작업 A가 열리고 이후 저장이 전부 A를 덮음(제목까지 교체). `clearWork()`는 **정의만 되고 호출부 0곳**이었다 | `c350acdd` |
| **C-2** | **복원이 렌더설정을 안 되살려** `renderFinal`이 `saveHeadcopy()`로 빈 STATE를 job에 POST → 서버의 headcopy·caption_style·deco를 **null로 덮음**. 게다가 `subtitle_removal`은 job에 True로 남아 **유료 VMake가 도는데 화면은 "꺼짐"** 표시(몰래 과금) | `c350acdd` |

**🔑 C-1의 아이러니**: 사장님이 *"작업물이 지워진다"* 고 제보해서 만든 기능이, **바로 그 방식으로 작업을 지웠다.**

**🔑 C-1은 설계 긴장이었다 — 코드 실수가 아니다.** 새로고침도 "+ 새 작업"도 **똑같은 `/produce`**로
온다. 신호가 없으면 코드가 구분할 방법이 **원리적으로 없다**. 그래서 복제를 막으려고 복원을 켜자
"+ 새 작업"이 덮어쓰기가 됐다. → `?new=1`로 가르고 `history.replaceState`로 즉시 지운다
(안 지우면 그 화면 새로고침마다 새 작업이 생겨 복제가 **반대 방향**으로 되살아난다).
기존 복제방지 자물쇠(`test_reopening_does_not_duplicate_the_work`)는 **그대로 살아 있다**(69건 전부 유지).

**🔑 C-2에서 확정한 원칙**: **렌더설정의 주인은 작업파일이 아니라 job이다.** 작업파일에 복제하면
진실의 원천이 둘이 되어 새 불일치가 난다. → 서버가 job에서 읽어 내려주고(`/api/produce/works/{id}`의
`settings`) 복원이 그걸 STATE에 꽂는다. ⚠️ **`/api/mix/status`에 실으면 안 된다** — 그 라우트는
인증이 없어(`request`를 안 받음) job_id만 알면 열린다. 작업 조회 라우트는 `_cid`로 소유자를 확인한다.

### ✅ 미리보기 게이트는 안 뚫렸다 (네 번째 우회로 없음)

`jump()`·`go()`·`refreshNextBtn()` 셋 다 같은 조건을 검사하고, `_restoreWork`는 `cur` 대입 **전에**
서버에서 `preview_status`를 재동기하며 **못 읽으면 `null`로 둬 잠긴 채**로 만든다("모르면 안전한 쪽").
게이트에 세 번 데인 흔적이 코드에 주석으로 남아 실제로 제 역할을 했다.

### 🔑 이번에 새로 확정한 교훈

1. **★죽은 테스트가 또 나왔다 — 여섯 번째, 또 새로운 방식.**
   `test_isolation_get_and_delete_are_404_for_other_customer`는 delete 라우트의 `_cid`를 **통째로
   지워도 17/17 통과**했다. 인자를 빼면 `customer_id`가 조용히 **기본값 0**으로 떨어지는데, 그
   테스트가 고객 1·2만 써서 `WHERE customer_id=0`이 아무것도 못 맞혀 **의도한 가드가 아니라
   "0은 아무것도 소유 안 함"이라는 엉뚱한 사실에 걸려** 404가 났다.
   ⚠️ **기존 데이터는 전부 고객0(사장님)에 귀속**된다(`store.py:443`) → 저 가드가 사라지면 아무
   고객이나 **사장님 작업을 삭제**할 수 있는데 스위트는 green. → **고객0을 피해자로 세운 회귀테스트**
   추가(`test_produce_work_seams.py`). **현행 코드는 정상**(라이브 취약점 아님).
   → **격리 테스트는 기본값이 피해자일 때로도 써라.**
2. **★내 하네스가 거짓 실패를 냈다.** `_bootRestore` 테스트에서 `WORK_ID`를 미리 채워놓고 부팅을
   흉내냈는데, 실제 페이지는 `produce.html:1294`에서 **항상 `null`로 시작**한다(낡은 정체성은
   **sessionStorage에만** 산다). 하마터면 **프로덕션에 도달 불가한 방어층을 심을 뻔**했다.
   → **하네스가 실패를 내면 프로덕션을 고치기 전에 "이 상태가 실제로 존재 가능한가"부터 물어라.**
3. **뮤테이션 5종 전부 죽음**(주석 처리 포함 — 이 트랙에서 삭제는 잡고 주석은 놓친 전례 때문에 병행).
   되돌린 뒤 3파일 **md5 바이트 일치** 확인.

### ⏭ 남은 것 (작업파일)

1. **★라이브 grounding** — 배포 후 실제로 눌러볼 것. 볼 것: ①"+ 새 작업"이 진짜 빈 화면으로 뜨나
   (URL에서 `new=1`이 사라지나) ②그 상태로 저장하면 사이드바에 **작업이 2건**이 되나(1건이면 C-1 재발)
   ③작업 A를 다시 열면 꾸미기가 살아 있나 ④자막제거 표시가 job과 일치하나.
2. **⚠️`produce_works` 행 0건**(2026-07-17 15시 실측) — 미사용인지 **저장이 조용히 깨진 건지 미구분**.
   라이브에서 직접 저장해보고 행이 느는지 볼 것. 안 늘면 그게 다음 결함이다.
3. **범위 밖(선재 결함, 이 트랙 아님)**: `/api/produce/mix/settings`(`app.py`)가 `request`를 안 받아
   **`_cid` 검사가 없다** — 남의 job_id로 헤드카피·꾸미기·**자막제거**를 바꿀 수 있다(`f2d123ac` 소행).
   작업파일이 job_id를 왕복시키며 노출면이 넓어졌다. **코드점검 트랙 소관으로 넘길 것.**
4. **Minor(리뷰 판정, 병합 차단 아님)**:
   - 저장 유실창 1초: `saveWork`가 1초 디바운스인데 `beforeunload`/`sendBeacon`이 없어, 사이드바
     클릭(`location.href` 전환)이 대기 중 타이머를 죽인다 → 마지막 1초 편집이 서버에 못 감.
   - 삭제한 작업이 API 직접호출로 되살아남(`store.py:1355` INSERT 폴백). 현재 UI엔 삭제 버튼이
     없어 도달 불가.

---

## ✅ 제작소 대본 재생성 배선 — main 병합 완료·라이브 대기 (2026-07-16)

**사장님 지적**: *"선택 1개 그대로 쓰기는 원본영상이랑 그대로 쓴다는건데 의미가 없는거고 / 원본대본을
대본즐겨찾기에 기능들을 여기에도 두고 대본을 재생성하는 작업 / 제미니 자동기능은 빼고"*

**병합 실측 확인**: `origin/main` = `4918f2b2`(Merge) ← `d241a75e`. `git branch -r --contains d241a75e`에
`origin/main` 있음 / 테스트 5파일 전부 `origin/main`에 존재 / `origin/main`의 `produce.html`에
`openGenFromWiki`·`PM_GEN`·`script_from_wiki` 참조 20건.

**🚨 라이브엔 아직 안 감** — 서버 배포가 별개 이유로 막힘(맨 아래 참조). 병합은 됐으니 유실 위험 없고
배포가 풀리면 자동 반영된다.

- 설계: `docs/superpowers/specs/2026-07-16-제작소-대본재생성-배선-design.md`
- 계획: `docs/superpowers/plans/2026-07-16-제작소-대본재생성-배선.md`
- 원장(태스크별 리뷰 전문): `.tracks/대본믹스통합/.superpowers/sdd/progress.md`

### 정찰이 뒤집은 전제 — 신규 개발이 아니라 배선이었다

즐겨찾기(`library.html`)의 "이 구조로 대본 생성" 패널은 **이미 `produce.html`에 `pmModal`로 이식돼 있었다**
(7/15 Task3이 한 것 — 아래 섹션). 다만 왼쪽 "📝뽑기" 경로에만 배선돼 있어 mix 탭 목록은
`useSingleScript()` = 그대로 복사로 남아 있었다. 백엔드 `/api/wiki/generate`도 shortcode만 주면 위키에서
구조를 읽는 폴백을 이미 갖고 있었다(7/15 `bcd32029`) → **백엔드 수정 0**.

### 커밋 (전부 origin/main에 있음)

| | 커밋 | 내용 |
|---|---|---|
| T1 | `e68b359d` | 제미니 자동 뿌리째 제거(탭·`SCRIPT_MODE_HTML.gemini`·`genGemini` + `app.py` 라우트 + `script_generate.py`의 `generate_from_topic`·`_TOPIC_PROMPT`) |
| T2 | `f27c394f` `9ae10bfe` | `pmModal`에 소재칸 이식(`pmSubject`·`pmPrefillSubject`·`pmRunGen`이 `subject` 전송) + 경합 레이스 테스트 |
| T3 | `8f3ebe89` | **mix 1개 선택 → `openGenFromWiki()`가 `pmModal` 열기**, `useSingleScript()` 삭제, 전역 `PM_FROM_WIKI` |
| T4 | `e1fd9887` `42457669` | `STATE.script_from_wiki`로 위키담기 오귀속 차단 + 세션복원 배선 |
| 최종 | `d241a75e` | whole-branch 리뷰 지적 3건(세대토큰 `PM_GEN`·카테고리 오귀속·테스트 인코딩) |

게이트: **10 failed / 715 passed** — 실패 10건은 착수 전부터 깨져 있던 기준선과 **동일 파일·동일 이름**. 회귀 0.

### 🔑 이 트랙에서 확정된 교훈

**1. 최종 whole-branch 리뷰가 개별 리뷰 6번이 통과시킨 이음새 2건을 잡았다.** "최종리뷰 생략금지"의 재증명.
- **I-1**: `openScriptModal`이 10~30초 `extract_from_url`을 await하는데 continuation이 **모달 주인 확인 없이**
  `PM_*` 재료를 덮어썼다. 뽑기 대기 중 닫고 mix로 대본을 고르면 `PM_SHORTCODE`만 위키 것이고 재료는 전부
  남의 영상 것 + **설계가 명시 금지한 `pmUseRaw` 버튼이 mix 모달에 등장**.
  ⚠️ T3 리뷰는 *"양쪽 진입점이 전역 7개를 진입 시 전부 재설정하니 오염 불가"*로 통과시켰는데 —
  **동기 진입에 한해서만 맞는 말이었다.** 이게 이음새의 정의다.
- **I-2**: `_currentSrcItem()`의 소비자가 둘인데(파일 주석에 명시) T4가 `saveScriptToWiki`만 막고
  `onFinalCategoryChange`는 무방비 → 위키 대본의 카테고리 교정이 **무관한 영상의 `script_extracts.category`를
  DB에 영속 기록**.

**2. 계획서가 확정 경로를 넷이라 세었는데 다섯째가 있었다**(`_consumeProduceHandoff` 세션복원). `saveWork`
페이로드에 플래그가 없어 **새로고침 한 번이면** T4가 막으려던 오귀속이 되살아났다. 계획서·구현자·자체리뷰
전부 놓쳤고 태스크 리뷰가 잡았다.

**3. "테스트 통과" ≠ "테스트가 결함을 잡음"이 이 트랙에서 두 번 났다.**
- T2: 경합 가드를 **통째로 지워도** 3개 테스트가 전부 통과(시나리오가 shortcode 하나로 한 번만 불러 조건이 항상 참).
- T4: `text.count("STATE.script_from_wiki") >= 4` 문자열 카운트라, 대입줄을 **삭제 대신 주석 처리**하면
  기능이 죽어도 통과(주석 안 문자열도 셈).
→ **뮤테이션은 "삭제"뿐 아니라 "주석 처리"도 해봐야 한다**(실무에서 더 흔한 회귀 패턴).

**4. `PM_GEN`을 올리는 위치는 load-bearing.** 재리뷰어의 반사실 실험: 검증 **전에** 올리면 조기 return이
살아있는 뽑기를 무효화해 모달이 스피너에 영구 정지. 현재(검증 후) 배치가 유일한 정답.

**5. ⚠️ CLAUDE.md 문구 오류**: *"트랙 폴더 안에선 `git add -A`도 안전하다"*는 **틀리다**. 트랙 폴더에도
`raw/` 크롤 데이터가 딸려온다. "안전"은 *남의 코드*가 없다는 뜻이지 *raw/가 없다*는 뜻이 아니다.
→ 파일을 하나씩 지정해 add할 것.

### ✅ 라이브 육안검증 — 4/5 통과, 1건 재현실패 (2026-07-16 밤, 실측)

**전제가 바뀌었다: 배포는 이미 풀렸다.** 서버 HEAD=`3349db35`(로컬 최신)이고 `git merge-base --is-ancestor
4918f2b2 HEAD`=YES. 병합 14:30 → 서비스 재기동 15:31이라 `app.py` 변경도 로드됨. `produce.html`은
`static/`(templates/ 아님 — 경로 주의)이라 디스크에서 바로 서빙. **아래 검증은 전부 라이브
shoppingshorts.duckdns.org 실측.**

1. **✅ 위키 폴백 실증** — `structure:null`+`base_script:''`로 `/api/wiki/generate?shortcode=DaqqSdPz7YT`
   → **200, 3.4초, 진짜 한국어 초안 1건**. 반대로 위키에 없는 `Daq9_j1zSNJ`+재료 없음 → **404**
   ("위키에 없는 항목"). 폴백 게이트 양방향 확인.
   🔑 **그런데 전제가 반쯤 틀렸다**: `script_extracts` 49건 중 `script_wiki`에도 있는 shortcode는
   **13건(27%)뿐**. `app.py:917`이 위키를 **먼저** 보고 있으면 body를 통째로 무시하므로 —
   **I-1 잔여 피해는 27%에서만 무해했고, 나머지 73%는 오염 body를 채택했을 것**. I-1 수정이 필수였음이 수치로 확인.
   (참고: `script_wiki`는 전부 `customer_id=0`, `script_extracts`엔 그 컬럼 자체가 없음 → 고객 스코프는 추가 필터 아님.)
2. **✅ 새로고침 후 위키담기 계속 숨김 (T4 Critical 실증)** — mix→확정 후 `produce_work`에
   `script_from_wiki:"DayiR8dTn2W"` 실림 → 새로고침 → **대본 183자 복원됐는데도 버튼 숨김**.
   **반사실로 못 박음**: 같은 페이로드에서 플래그만 `null`로 바꾸면 **버튼이 나타난다**
   → 숨김의 원인이 T4 플래그임이 확정. 죽은 테스트 아님.
   ⚠️ **함정**: `_consumeProduceHandoff`(`produce.html:1224`)는 복원을 **`w.handoff.length`가 있을 때만**
   한다. HANDOFF=왼쪽 **영상풀**이라 **영상을 안 넘기고 대본만 넘기면 복원이 통째로 스킵**된다.
   처음에 이걸 모르고 검증했다가 "버튼 숨김 ✅"를 얻었는데 **대본도 길이 0이라 아무것도 증명 못한 통과**였다.
   → 검증 전 **영상 즐겨찾기(`/collection`)에서 영상 1개를 먼저 넘겨야** 유효하다.
3. **✅ `pmSubjectRow`가 라디오 레이아웃 안 깸** — 모달 렌더 확인, `offsetParent` 기준 표시됨.
4. **✅ 소재 자동감지가 mix 유래 shortcode에 쓸 만한 값을 줌** — 실측 3회 전부 유의미:
   "공간 활용 접이식 다용도 협탁" / "공간 활용 접이식 테이블" / "공간 활용형 이동식 협탁 책상".
5. **❌ 재현실패(미검증)** — 뽑기 대기 중 mix 진입 시나리오. 클릭은 했으나
   `performance.getEntriesByType('resource')`에 **`extract_from_url` 요청 0건** = continuation이 애초에
   안 돌았다 → **통과로 세면 안 됨**. 재시도 시 **네트워크 엔트리로 추출이 실제 발생했는지 먼저 확인**할 것.

**덤으로 확인된 것**
- **T1(제미니 자동 제거) 라이브 확인**: 대본 탭이 `직접작성`/`우리 시스템으로 믹스` **2개뿐**.
  `genGemini` 심볼 0건. 남은 "제미니"는 주석 한 줄(`// 초안 렌더(제미니·믹스 공유)`)뿐 — 무해.
- **T3 라이브 확인**: `openGenFromWiki`=**function**, `useSingleScript`=**undefined**.
  버튼 문구가 "선택 1개 **그대로 쓰기**"→**"♻️ 선택 1개로 새 대본 생성"**, 안내문도
  "1개 = 그 대본의 구조를 빌려 **새로 씁니다**". **사장님 지적이 화면에 그대로 반영됨.**
- **I-1 수정 확인**: mix 모달에서 `pmUseRaw`는 **DOM에 존재조차 안 함**(숨김보다 강함). 영상풀이
  차 있어도 동일.
- **E2E 초안 생성**: 믹스 1개 선택 → 모달 → 초안 3개(전부 다른 훅, 소재는 유지·표현만 새로) → 확정 183자.

## 🔴 사장님 제보 2건 처리 (2026-07-16 밤) — 둘 다 main 병합·배포

### ① 결함: 뽑기가 "위키에 담겨있어야 한다"고 함 → 수정(`20cce5a8`)

**사장님 말**: *"뽑기를 하면 위키영상에 담겨있어야 한다고 나오고있고"*

**라이브 재현 완료** — 404 문구가 글자까지 일치(`위키에 없는 항목 — 먼저 S급으로 저장하세요`).

**인과사슬**: `openScriptModal`이 모달을 **먼저 열고** `PM_BASE_STRUCT=null; PM_BASE_SCRIPT=''`로
초기화한 뒤(1297줄) `extract_from_url`을 await한다(콜드 10~70초, **실측 70초**). 그 대기창 내내
`pmRun`이 활성이라 누르면 `pmRunGen`이 재료 없이 쏘고 → `api_wiki_generate`가 위키에서 직행 URL을
못 찾아 404. **뽑기는 위키 없이 되는 게 설계**(`bcd32029` 폴백)인데 정반대 지시가 사용자에게 갔다.

**원인 = 두 진입점의 비대칭**: mix(`openGenFromWiki`)는 재료를 **동기로** 채우고 열며 내용이 비면
아예 안 연다(1170줄 가드). 뽑기만 비어 있는 채로 열려 있었다.

**🔑 함정(내가 30분 헤맴)**: 사장님 화면의 `감지된 소재: "소재 감지 중…"`은 **정적 HTML의
placeholder**(`library`가 아니라 `produce.html:418`)다. "소재 감지가 돌았다"는 증거가 아니라
**아무것도 안 채워진 초기 상태**다. 이걸 증거로 오독하면 `pmPrefillSubject`가 실행됐다고
잘못 추론해 원인을 영영 못 찾는다.

**그날 추출 실패의 근인은 우리 코드 아님**: 서버 로그 `script_extract: 미분류 오류로 빈 결과 반환 —
ServerError("503 UNAVAILABLE. 'This model is currently experiencing high demand'")` = **Gemini 과부하**.
일시적. 하지만 **실패했든 대기 중이든 버튼이 열려 있는 것 자체가 결함**이고 그게 오해를 만들었다.

**수정**: 뽑기는 재료 오기 전 `pmRun` disabled → 추출 도착 시 **백엔드 폴백 조건과 같은 식**
(`structure`가 dict이거나 `base_script`가 안 빔)일 때만 해제. 실패·빈재료면 잠긴 채로 진짜 원인 표시.
mix는 진입 시 명시적 해제(뽑기가 잠가둔 채 가져갈 수 있다).
테스트 5건(`test_produce_gen_button_gate.py`), 뮤테이션 3종 전부 죽음.
**기존 하네스 2개(`test_produce_pm_gen_race.py`·`test_produce_mix_regen.py`)에 `pmRun` 추가** —
실제 페이지(424줄)엔 늘 있던 것이라 하네스 구멍이었다(`Cannot set properties of null`로 터졌음).

### ② 기능: 링크를 대본 즐겨찾기에 직접 등록 → 구현(`f6bedc4f`)

**사장님 말**: *"링크를 대본즐겨찾기에 등록하는게 하나 있어야 한다"*
**사장님 결정**: URL 직접 등록 / 카테고리는 **자동 추론하되 교정 가능**.

**★또 배선이었다(T3와 같은 패턴) — 백엔드 신규 0**:
```
① /api/produce/extract_from_url {url}   → 추출 + 카테고리 자동추론 + 구조분석 + 캐시
② 통제 어휘(/api/wiki/categories) 드롭다운으로 교정          ← I-4 준수, 자유입력 없음
③ /api/produce/save_to_wiki {url, category} → 캐시 히트라 즉시. 원본만 저장(C-1 수정본) + 재학습
```
**★하중 가정**: ①과 ③이 shortcode를 `sha1(url)[:12]`로 **각자 손으로 적어둔 같은 식**이라
③이 ①의 캐시를 맞힌다(app.py `633`·`769`). 한쪽만 바뀌면 **조용히** 깨져 영상을 다시 받고
Gemini를 또 부른다 — 느려지는 정도가 아니라 **비용 2배 + 재추출 실패 시 등록 자체가 깨짐**.
→ `test_library_add_by_url.py`가 이 식을 고정한다. **건드리면 이 테스트부터 볼 것.**

⚠️ **미검증**: 라이브 육안. Gemini 503이 떠 실제 URL 등록 E2E를 아직 못 봤다.

### ③ Gemini 503으로 뽑기가 전면 불가 → 모델 폴백 (`d8362688`) — ✅ 라이브 실증

**사장님 말**: *"제미니 API키 많자나 확인하고 없으면 얘기해"* → **답은 정반대였다. 키 문제가 아니다.**

**★실측(서버, 같은 순간)**:
- 전용 키풀 **16개 전부 살아있음**. 그중 5개를 각각 호출 → `gemini-3.5-flash`가 **5/5 전부 503**
  (`UNAVAILABLE, "This model is currently experiencing high demand"`).
  → **503은 키가 아니라 모델 용량. 키 로테이션으로는 절대 안 풀린다**(429와 혼동 금지).
- 같은 키·같은 영상으로 **`gemini-3.1-flash-lite`는 정상** — 실제 `extract_script`를 그 모델로
  태워 구간 6개·본문 208자 확보(**`response_schema`까지 지켜짐** = 폴백이 실용 가능).
- `gemini-2.5-flash`=404(없음) / `gemini-2.0-flash`=429. 즉 **쓸 수 있는 대체재는 lite뿐**이었다.

**🔑 함정 2개**:
- **로컬·SSH 셸에서 키를 세면 0개로 나온다** — 서비스는 `.env`가 아니라 **`/etc/shopping-shorts.env`**
  (systemd `EnvironmentFiles`)를 쓴다. 이걸 모르고 "키가 없다"고 보고하면 완전한 오진이다.
  (키가 0이면 애초에 Gemini를 부르지도 못해 503이 안 나온다 — 이 모순이 힌트였다.)
- **로그의 "미분류 오류로 빈 결과 반환"이 거짓말이었다.** 503은 이미 분류돼 있었고 재시도가
  소진된 것뿐인데, 저 문구 때문에 원인을 한참 못 찾았다 → "재시도 소진 또는 미분류 오류"로 정정.

**수정**: primary(`_MODEL`)로 503을 **2번** 겪은 뒤 `_FALLBACK_MODEL`로 폴백. 바로 안 내려가는
이유는 503이 spike라 곧 원래 모델이 살아나는 일이 잦고 그동안은 품질 좋은 쪽이 낫기 때문.
폴백 후엔 sleep 없음(다른 모델이라 앞 모델 혼잡과 무관). **`_MODEL`은 안 건드린다**
(`video_analysis`·`similarity` 공유).

**✅ 라이브 exit proof (2026-07-17 00:04, 서버 로그)**:
```
00:04:24  script_extract: gemini-3.5-flash 503 반복 → gemini-3.1-flash-lite로 폴백
00:04:43  POST /api/produce/extract_from_url  200 OK
```
몇 시간째 502로 죽던 같은 영상이 **66.5초에 구간 9개·본문 316자** 확보.

**★★또 죽은 테스트 발견·수정**: 공유 `_MODEL` 오염 뮤턴트가 **전체 실행에선 살아남고 단독
실행에선 죽었다** — 앞 테스트가 먼저 `_MODEL`을 오염시켜 뒤 테스트의 `before`가 이미 오염값이라
비교가 무의미해진 것. 픽스처가 매 테스트 `_MODEL`을 원복하게 해 순서 무관하게 만듦.
→ **교훈: 전역을 건드리는 결함의 테스트는 반드시 단독 실행으로도 확인할 것.**

## 🆕 제작소 1단계 미리보기 게이트 — Task 1~5 구현·병합 (2026-07-17 새벽)

**사장님 요청**: *"여기까지 오면 영상을 미리보기 할수있는게 있어야한다. 여기서 오케이되면 다음단계
자막제거를 한다."* / *"미리보기를 안해보고 넘길수는있는데 상식적으로 잘되었는지 뭘보고 그걸 다음단계로
넘기겠나. 돈내고 만드는사람들인데."*

- 스펙 `e33ae70c` · 계획 `8d9a2a12` · 구현 `5510d70b`(store) `dbf5e9b7`(run_preview)
  `60e9ba79`(라우트) `b950405b`(프론트) → **main 병합됨**
- 원장: `.tracks/대본믹스통합/.superpowers/sdd/preview-progress.md`

### ★설계 핵심 — 또 배선이었다(오늘 세 번째)

`assemble()`이 이미 3토막이라 **새 파라미터가 필요 없었다**:
```
1) _render_mix        → 컷 이어붙임(원본자막 그대로)   ← 1단계 미리보기가 딱 이것
2) clean_fn(선택)     → 유료 VMake 자막제거            ← 안 넘기면 0원
3) _burn_captions     → 우리 자막 굽기
```
`run_preview` = `assemble(clean_fn=None, deco={})`. 그게 전부다.

### 🔑 사장님 논리가 내 것을 세 번 이겼다 (스펙 §10에 기록)

1. **"1단계에서 자막 빼자"**(내) vs **"자막제거는 유료라 컷·대본 틀리면 돈 낭비"**(사장님) →
   미리보기는 **두 겹이어도 공짜**가 맞다. 미관 제약보다 비용 제약이 이긴다.
2. **"무음이 렌더가 조금 빠르다"**(내 추측) → 코드를 보니 **TTS가 컷 길이의 원천**
   (`_render_mix`가 `-t tts_dur`)이라 `tts_paths`를 비우면 전 비트가 스킵돼 영상이 안 나온다.
   **무음이 오히려 손이 더 간다.** → 음성 넣고 `<video controls muted>`로 음소거 기본
   (사장님 최초 '무음' 선호와 '음성넣고'를 둘 다 만족).
3. **"다음 버튼 안 막기"**(내, 개발자 편의) → 사장님이 제품 관점으로 뒤집음. **막는다.**

### ⚠️ 이 작업에서 잡은 함정·죽은 테스트

- **`nextBtn`이 아니라 `btnNext`**(`produce.html:395`). 계획 초안이 틀렸고 착수 전 소스로 잡았다.
  그대로 갔으면 `getElementById`가 null → **게이트가 조용히 아무것도 안 잠갔을 것**.
- **Task5 하네스가 `PREVIEW_STATUS`를 중복 선언**해 SyntaxError로 죽을 뻔(착수 전 계획 점검서 발견).
- **★죽은 테스트 1건 잡음**: "`ready` 검사 제거" 뮤턴트가 처음엔 **살아남았다**. 기존 404 테스트는
  `preview_path`가 `None`이라 **다른 가드**에 걸려 통과했을 뿐. 진짜 위험은 **재렌더 중 옛 경로가
  남아 옛 mp4가 서빙되는 것** — 사장님이 바뀐 줄 알고 **옛 영상 보고 OK**를 누른다. 그 시나리오로
  테스트를 다시 써서 뮤턴트를 죽였다(`test_serve_preview_404_while_rerendering_even_if_old_file_exists`).
- **`update_mix_job` 화이트리스트**(`store.py`)에 컬럼을 안 넣으면 쓰기가 **에러 없이 조용히 무시**된다.

### 실행 방식 메모

**서브에이전트 SDD로 시작했으나 계정 세션 한도(4am 리셋)로 디스패치가 막혀 컨트롤러가 인라인으로 구현.**
죽기 전 아무것도 안 했음(커밋·파일·보고서 0) 확인 후 착수했다.
→ **태스크별 리뷰가 없다.** 최종 whole-branch 리뷰에서 반드시 함께 볼 것(생략금지 — 이 트랙에서
개별리뷰 6번이 통과시킨 이음새를 최종리뷰가 잡은 전례).

각 태스크 뮤테이션은 전부 돌렸다: Task1 3종 / Task2 2종 / Task3 2종 / Task5 3종 **전부 죽음**.
게이트: **10 failed / 754 passed** (기준선 10건과 동일 파일·이름, 통과 731→754). 회귀 0.

### 🔴 최종 리뷰가 Critical 3건을 잡았다 — 리뷰를 안 돌렸으면 "완료"로 넘어갈 뻔했다

라이브 실증(mp4 5.06MB 생성 · VMake 0건)은 **성공했는데도 게이트 자체가 뚫려 있었다.**
서브에이전트가 세션한도로 죽어 **태스크별 리뷰를 하나도 못 한** 대가를 최종 리뷰가 다 받아냈다.

| | 결함 | 수정 |
|---|---|---|
| **C-1** | `renderSteps()`가 **모든 스텝 칩**에 `onclick="jump(i)"`를 박는데 `jump()`엔 가드가 없어 **칩 한 번 클릭으로 게이트 통째 우회** | `fbcaff7a` |
| **C-2** | `PREVIEW_POLL`을 `clearInterval` 없이 덮어써 더블클릭 시 고아 인터벌 → **2.5초마다 `<video>` 재생성** → 재생이 0초로 리셋 + 5MB 재다운로드 → **못 본 채 게이트 열림** | `fbcaff7a` |
| **C-3** | `startProduceMix`가 `PREVIEW_STATUS`를 초기화 안 해 **재매칭 중 수십 초~수 분간 게이트가 열려 있음** → J2를 못 본 채 유료 자막제거행 | `b0979004` |
| I-1 | 렌더 중 서버 재시작 시 `rendering`이 영구히 남아 **§7.1 탈출구도 안 열림** → `updated_at` 10분 타임아웃 | `fbcaff7a` |
| I-2 | 라우트가 읽기만 해 TOCTOU → 잘린 mp4가 `ready`로 통과 가능 → 동기 쓰기 | `fbcaff7a` |
| M-1 | `run_preview`가 `headcopy`(5단계 꾸미기)·`caption_style`을 넘겨 `deco={}`와 자기모순 → 안 넘김 | `fbcaff7a` |

### 🔑 이 트랙이 새로 확정한 교훈

**1. 같은 종류의 누락이 두 번 반복됐다.**
- 1차: `go()`에 *"다른 경로로도 불릴 수 있다"*는 주석까지 달고 **정작 그 다른 경로(`jump()`)를 놓쳤다.**
  두 겹을 같은 문에 달고 옆문은 열어뒀다.
- 2차: 재매칭의 **'끝'(`loadMixReview`)만 봉인하고 '시작'(`startProduceMix`)을 놓쳤다.**
  `loadMixReview`의 주석이 스스로 그 위험을 적어놨는데도.
→ **상태 초기화는 "그 상태가 무의미해지는 순간"(job이 바뀌는 그 줄)에 걸어라. 나중이 아니라.**
→ **가드를 달 땐 그 상태를 바꾸는 지점을 grep으로 전부 세라**(`MIX_JOB *=` → 초기화 제외 1곳뿐임을 확인).

**2. ★테스트가 그 코드를 지나가지도 않았다.** 하네스가 `fetch`를 reject·타이머를 no-op으로 스텁해
**`startPreview`·`pollPreview`가 한 줄도 실행되지 않았다.** 뮤테이션 3종을 통과시켰지만 애초에 그 길을
안 밟았다. C-3도 같은 이유(`startProduceMix`가 슬라이스 밖) — **34건이 다 green인데 구멍이 살아 있었다.**
→ **비동기 배선은 가짜 시계로 실제 구동해야 한다**(`_videoWrites`를 `innerHTML` setter에 물려 실제 쓰기 횟수를 셈).

**3. 내 라이브 검증에도 한계가 있었다.** 미리보기를 **한 번만** 눌러서 C-2(더블클릭)가 안 드러났다 —
리뷰어가 정확히 지적했다.

**4. 살아남은 뮤턴트가 항상 결함인 건 아니다.** `startPreview`의 `clearInterval` 제거가 안 죽길래 팠더니,
세대토큰이 먼저 막아 **도달 불가한 방어층**이었다(기존 L-1과 동형). 결함 아님 → **지우지 말 것**(M-5).

**5. 수정 에이전트 보고와 내 실측이 갈렸다.** 에이전트는 "타이머 개수로 잡힌다"고 했으나 내 뮤턴트에선
안 죽었다. **보고를 믿지 말고 직접 심어라.**

### ✅ 라이브 실증 (배포 후 재관측, 서버 HEAD `65cb0ff2`, 재기동 08:06)

- **게이트가 실제로 잠긴다**(실행 관측, 정적 확인 아님): 다음 버튼 `disabled`+"미리보기를 먼저 확인하세요" /
  **`jump(1)`(스텝칩) 막힘** / `go(1)` 막힘 / 미리보기 후 `jump(1)` 통과.
- 앞선 관측(수정 전 버전): mp4 5,059,790 bytes · 브라우저 디코딩 23.9초·720x1280 · **VMake journalctl 0건** ·
  기존 `status` 오염 없음 · 마이그레이션 실 DB(job 20행) 정상 · 화면에 원본자막 남고 우리자막 굽힘.
- 💰 비용 0으로 검증 — TTS 7/7이 살아있는 기존 job `a344328eb235` 재활용, ffmpeg만 돌림.

### ⏭ 남은 것

0. **⚠️ M-1(headcopy·caption_style 제거) 이후 버전으로 라이브 재관측 안 함** — 재리뷰어 지적.
   자막이 여전히 정상으로 굽히는지 실제 job으로 한 번 더 볼 것(앞 관측은 인자를 빼기 **전** 버전).
1. ~~**★Task 6 — 라이브 grounding**~~ ✅ 완료(위 참조)
   Task 1~5는 **`assemble`을 전부 가짜화**했으므로 **실제로 mp4가 나오는 걸 아무도 본 적이 없다.**
   볼 것: ①마이그레이션이 서버 실 DB(`reference.db`, 실 job 행 있음)에서 도는가 — 안 되면 즉시 500
   ②`다음`이 처음엔 잠기나 ③영상이 진짜 재생되나 ④열자마자 조용한가 ⑤음소거 풀면 나레이션 들리나
   ⑥미리보기 후 `다음`이 열리나 ⑦원본 자막 보이나(= VMake 안 썼다는 증거) ⑧콘솔 에러 0
   **★⑨VMake 미호출 증거**: `journalctl | grep -i vmake` **0건** — 한 건이라도 나오면 "0원" 전제가 깨진 것.
2. **최종 whole-branch 리뷰** — 태스크별 리뷰를 못 했으므로 특히 중요.
3. **★링크 등록 라이브 E2E** — 이제 폴백이 있어 Gemini 503이어도 가능. 볼 것: 추출 성공 →
   카테고리 드롭다운에 추론값 선택돼 뜨나 → 등록 후 카드 목록에 뜨나 → `script_wiki` +1.
2. **검증5 재현** — 뽑기 대기 중 mix 진입. `performance.getEntriesByType('resource')`로
   **`extract_from_url`이 실제로 떴는지 먼저 확인**(지난번 0건 = continuation 미발생 = 미검증).
3. **Low 2건** (병합 차단 아님, 최종리뷰 판정) — 아래 그대로.
4. ~~배포 막힘~~ ✅ 해소됨(위 참조).

**Low 2건 (병합 차단 아님, 최종리뷰 판정)**
- **L-1**: 신규 가드 2개(2차 가드·`catch` 가드)가 무커버 — 강탈 시나리오가 1차 가드에서 return해 도달 안 함.
  가드 자체는 옳으나 나중에 "죽은 코드"로 정리되면 막을 테스트가 없다.
- **L-2**(Info): `pmLoadElementOptions`의 `#pmToggles` 잔여 경합(마지막 writer 승). 요소 드롭다운만 영향,
  `PM_*` 재료·`pmUseRaw`엔 무관. 기존 패턴이고 요소 토글 UX는 설계상 범위 밖.

**의도적으로 남긴 것**
- **M-3**: `onScriptInput()`이 플래그를 안 건드림 → 위키 대본 확정 후 손으로 고쳐써도 버튼이 계속 숨겨짐.
  방향이 "숨김 쪽"이라 **안전한 stale**. 고치려면 *"손으로 고쳐쓴 위키 대본은 누구 것인가"*라는 설계 질문이
  열리고, 잘못 고치면 안전한 stale이 위험한 오귀속으로 뒤집힌다.

### ⚠️ 이 트랙 작업 시 주의 (2026-07-16 추가)

- **범위 밖(설계가 명시)**: `genMix` 엔진 · 왼쪽 뽑기 경로의 `pmUseRaw` **동작 자체** · `library.html` ·
  `_currentSrcItem()`의 **폴백 순서**(호출자 가드는 OK) · 요소 토글/모드 UX 재설계.
- 테스트 관례: `produce.html`의 JS를 **재구현하지 말 것**. 앵커로 실제 소스를 잘라 Node로 실행
  (`test_produce_category_race.py`·`test_produce_subject.py`·`test_produce_mix_regen.py`·
  `test_produce_wiki_attribution.py`·`test_produce_pm_gen_race.py`).
- 뮤테이션 되돌릴 때 `git checkout --` 금지(미커밋 유실). **바이트 단위로** 되돌리고 md5 대조
  (텍스트 모드로 다시 쓰면 CRLF/LF가 변형돼 `git status`에 `M`이 남는다 — 실제로 겪음).

### 🚨 배포 막힘 (내 트랙 소관 아님 — 트랙격리 페이즈3)

**실측(2026-07-16 14:27)**: 서버 HEAD가 `1b907d7d`에 정지. `/tmp/auto_deploy.log`:
```
Aborting
Updating 1b907d7d..c8ebb87a
2026-07-16 14:27:02 pull실패(작업트리충돌?) 스킵-수동확인필요
```
**원인**(카테고리분류 트랙 규명): `.claude/settings.json`의 SessionEnd 훅이 `git add .`으로 `raw/`까지 담아
main에 push(`a133fcba` = 906파일) → 서버는 크롤봇이 `raw/`를 계속 쓰는 중이라 `pull --ff-only` 구조적 거부
→ **쇼핑쇼츠·스탁브레인 두 서비스 배포 동시 정지**. 세션이 끝날수록 블로커가 늘어 악화된다.

**모순**: CLAUDE.md는 `git add -A` 금지를 최상위 규칙으로 박아놨는데 **훅이 매번 그걸 한다** — 트랙 시스템이
막으려던 흡수를 훅이 자동 생성하는 셈.

---

## 🎬 영상제작소 대본·영상믹스 단계 통합 — ✅ 5태스크 완료·배포·라이브검증 (2026-07-15 집PC)

**트리거**: "대본믹스통합 트랙 이어서"

**목표(달성)**: 영상제작소(`produce.html`)의 "1·대본"+"2·영상"을 하나의 "제작소"(좌=영상풀·우=결과) 2단으로 통합.
넘어온 영상=재료풀, 각 영상에 📝뽑기(대본)·🎬담기(화면) 겸용. 대본 뽑기는 도서관 생성모달 재사용.
⭐위키저장 다리로 제작소서도 학습 반영.

- 스펙 `docs/superpowers/specs/2026-07-15-쇼핑쇼츠-영상제작소-대본믹스-통합-design.md`
- 계획 `docs/superpowers/plans/2026-07-15-쇼핑쇼츠-영상제작소-대본믹스-통합.md`
- **진행 원장 `.superpowers/sdd/progress.md`** 맨 아래 "영상제작소 대본·영상믹스 통합" 섹션 (전 태스크 상세·리뷰결과·실측치)

### ✅ 완료 (전부 커밋·배포·라이브 검증)

| 태스크 | 커밋 | 내용 |
|---|---|---|
| Task1 | `9e74e892`+`b0128340` | 백엔드 `/api/produce/save_to_wiki` (last_run 비의존) |
| Task2 | `2e121144`(흡수) | 단계병합 골격 — STEPS 8→7, data-step 시프트, 좌우2단 |
| Task3 백엔드 | `bcd32029` | `/api/wiki/generate` 위키미저장 폴백 |
| Task3 프론트 | `3c074f82` | **도서관 생성모달 pm 네임스페이스 이식** — `openScriptModal` 스텁 교체 |
| Task4 | `74bd8a24`(⚠️타세션 커밋에 흡수) + `6d33bf0c`(I-A fix) | `saveScriptToWiki` 배선 |
| I-1/I-2 | `e0a1932f` | category·structure 전파 |
| **C-1 fix** | `0deb810c` | **위키저장이 원본을 파괴하던 Critical 수정** |

전 태스크 opus 리뷰 통과(스펙✅/품질Approved/Critical0). 최종 whole-branch 리뷰 = `.superpowers/sdd/review-final-daebon-mix.md`

### 🔬 라이브 실측 (shoppingshorts.duckdns.org, 실제 즐겨찾기 경로 — 가짜 핸드오프 아님)
- 📝뽑기 실추출: 콜드 41s → 200, 실제 한국어 대본 / 캐시 0s
- 초안생성 3개(3s, 실제 제미니 리메이크) → 확정 → ⭐위키저장 200 → `script_wiki` 10→11 → `_relearn_category` 실행
- 🎬담기 2개 → mixUrl 자동반영
- I-1/I-2 exit proof: `PM_CATEGORY` ""→**"레시피"** / `PM_BASE_STRUCT` null→구조5키 / 드롭다운 "⏳학습된 카테고리 아직 없음"→**🎲랜덤+지식 권위자·실전 검증자·현실 타겟·반전 인물**

### 🔴 이 트랙에서 터진 사고 2건 (교훈)
1. **C-1 데이터 파괴 (내 라이브 검증이 실제로 일으킴)**: ⭐위키저장이 리메이크를 **원본 shortcode에 덮어씀**
   → 원본 대본 소실(캐시히트는 재추출 안 해 복구불가) + 위키(S급 창고)에 우리 출력 + **자기출력 학습루프**.
   `element_category_stats` 레시피 24→23이 "학습 반영 증명"이 아니라 **오염 코퍼스 학습 결과**였음.
   개별 태스크 리뷰 4번과 라이브 grounding이 전부 통과시킨 걸 **whole-branch 리뷰가 잡음**.
   → 사용자 결정으로 **위키저장=원본만**(도서관 `/api/wiki/save`와 동일 의미)으로 수정(`0deb810c`). 내 리메이크는 `script_drafts`에 있어 유실 없음.
2. **커밋 흡수**: Task4 코드가 타세션 커밋 `74bd8a24`("우리믹스 썸네일")에 쓸려들어감. `git log -S`로 확인.
   post-commit 훅이 무조건 push라 이미 라이브+후속커밋 쌓임 → **히스토리 정리 안 함**(force push는 동시세션·서버 파괴). 코드 유실 0.

### 🩺 조사 결론: "네트워크 오류"는 코드결함 아님 (환경)
콜드추출 41s 도중 **타세션 커밋으로 auto_deploy가 서비스 재시작** → 요청 끊김 → Apache 502 HTML → `r.json()` throw → catch가 '네트워크 오류'로 표시.
타임스탬프 일치로 확정(auto_deploy `11:00:14 재시작완료` ↔ Apache `11:00:14.46 AH00898`, MainPID 2162696→2163698). 재시도 시 41s 200 정상.
반증한 가설: pmLoadElementOptions 예외(직접호출 결과 예외없음) / Apache 타임아웃(ProxyTimeout 600·Timeout 300, 19s는 경계아님) / Gemini·APIFY 키(추출 실제 성공) / 백엔드 버그.

### ✅ 2026-07-16 (회사PC) — 위 ⏭ 중 4건이 **이미 끝나 있었음**(핸드오프가 낡았던 것)

> ⚠️ **교훈**: 이 파일의 ⏭를 그대로 믿고 착수하면 이미 끝난 일을 또 한다.
> 착수 전 **코드·DB를 실제로 확인**할 것. 오늘 1·2·I-B·I-4가 전부 그랬다.

1. **C-1 회귀확인 ✅** — 라이브 버튼을 누르지 않고(누르면 수정이 틀렸을 때 원본을 또 파괴함)
   코드 판독으로 확인: `body.script_text`는 **주석에만** 존재하고 코드가 안 읽는다. 캐시히트 시
   `save_script` 미호출. 가드 테스트 `test_produce_save_to_wiki_original.py` 6건 통과 +
   **뮤테이션(C-1 버그 재주입)으로 정확히 그 단언에서 죽는 것 확인** = 죽은 테스트 아님.
2. **오염 복구 ✅ (7/15에 이미 완료돼 있었음)** — 서버 DB 실측(시각=KST):
   | 시각 | extract 내용 |
   |---|---|
   | 11:04 | 오염: "맛은 **끝내줘요**… **요리 고수 언니가 비법이라며**" / hook_type=**권위인용형** / characters=[요리 고수 언니] |
   | 15:14 | 백업 채취(=오염본이 담김. **이 백업엔 원본이 없다** — 복구는 재추출로만 가능) |
   | 15:27 | C-1 수정 커밋 `0deb810c` |
   | **15:31** | **재추출 = 원본 회복**: "맛은 **미쳤어요**. **친구가 맨날 해달래요**" / hook_type=**공감형** / characters=[친구] |
   | 22:07 | `element_category_stats` 레시피 재계산 = **깨끗한 코퍼스 기준** |
   → `script_extracts`·`script_wiki` 양쪽 다 원본. **추가 조치 불필요.**
   🔑 **함정**: `script_extracts`에 `full_text` 컬럼은 **없다**(텍스트는 `script_json` 안에).
   `.get('full_text')`로 조회하면 None→"len=0"이 나와 **데이터가 빈 줄 착각한다**(내가 오늘 그랬음).
3. **I-B 라벨고착 ✅** — `produce.html:1480`에 `if(!b.disabled)` 가드 이미 들어가 있음.
4. **I-4 자유입력 고아버킷 ✅** — `prompt()` 제거되고 `#finalCategory`(통제 어휘)만 씀(`produce.html:1494` 주석).

**I-2 ✅ 완료·배포**(`b792792f` → main `5907a24a`, 게이트 통과):
지연 문제(클릭당 120s×3)는 Task 8이 BackgroundTasks로 이미 해소했고, **남아 있던 건 비용** —
실패를 기록 안 해 구조분석이 안 되는 영상은 클릭마다 백그라운드로 Gemini를 또 불렀다.
→ 실패 시 `structure_analyzed_at`만 찍고 `structure_json`은 NULL 유지(`store.mark_structure_attempted`).
대화형 경로는 재예약 중단, **재시도는 `daily_batch`가 가져감**(`extracts_missing_structure`가
`structure_json IS NULL`로 고르므로 자격 유지). `{}` 저장은 백필 영구제외라 절대 금지 — 그대로 지킴.
테스트 3건 신규, 뮤테이션 2종(가드 제거·mark 제거) 전부 죽음.

### ⏭ 남은 것 (다음 세션)

**리뷰가 남긴 잔여 (`.superpowers/sdd/review-final-daebon-mix.md`)**
- ~~**I-2**~~ ✅ 2026-07-16 완료·배포(위 참조).
- **I-3 ← 이게 진짜 남은 것**: `categorize()`는 None을 안 뱉음 — `categorize.py:86`이 항상 문자열 반환,
  전부 0점이면 `"기타"` 폴백. 따라서 `app.py:614·636`의 `or None`은 **죽은 코드**(2026-07-16 실측 확인).
  결과: 틀린 추측이 **DB에 영구 고착**(재유추 안 함)하고 모달에 표시조차 안 돼 **교정 경로가 없다**.
  ⚠️ 착수 시 주의: `categorize()`는 **레퍼런스 랭킹과 공유**하는 함수라 반환값을 None으로 바꾸면
  그쪽이 같이 흔들린다 → 호출부에서 `"기타"`를 미상으로 다루거나, 모달에 노출해 교정 경로를 여는
  쪽이 안전. **설계 선택이 필요하니 사장님 확인 후 착수**(고아 학습 버킷 문제와 얽힘).
- ~~**I-4**~~ ✅ 이미 반영돼 있었음(`prompt()` 제거, 통제 어휘만).
- Minor triage: ~~I-B 라벨고착~~ ✅ 이미 반영(`produce.html:1480`) / `.sent` no-op=후속 / legacy A/B=후속(백엔드 `{"A":"remake","B":"transplant"}` 매핑 실재해 무해하나 도서관과 어휘 분기=미래함정) / `subject` 미이식=후속 / 죽은코드 `_pmDrafts`·`data-idx`만 삭제 / `pmRestoreVersion`=**안 고침**(도서관 원본과 동일, 여기서만 고치면 갈라짐)
- 확인불가: 콜드추출 실제 증가폭(`analyze_structure` 추가분, 추정 41→약50초 — 실측 필요)

**타트랙 인계받은 것**: `saveScriptToWiki()`에 `thumbnail: src.thumbnail` 추가하면 CDN썸네일도 붙음(없어도 `/api/wiki/poster`가 대신해 무해) — 대본소재고정 트랙이 남긴 메모.

### ⚠️ 이 트랙 작업 시 주의
- **재개 방법**: `py tools/track.py start 대본믹스통합` 으로 폴더를 **새로 만들고** 시작한다 —
  2026-07-16 `finish`가 게이트 통과 후 트랙 폴더·브랜치를 정리해 지웠다(설계대로).
- ~~`produce.html` 동시편집 주의~~ → 트랙 폴더 도입으로 **구조적으로 해소**(각자 자기 폴더).
- 트랙 폴더 커밋은 `track/대본믹스통합`으로만 간다 = **커밋해도 라이브 아님**. 라이브는 `finish`뿐.
- ⚠️ **뮤테이션 되돌릴 때 `git checkout -- <파일>` 쓰지 마라** — 미커밋 수정까지 통째로 날아간다
  (2026-07-16 실제로 app.py 수정을 날려 재작업함). 심은 문자열을 **정확히 역치환**할 것.
- `.superpowers/sdd/` 파일명은 태스크번호만 쓰면 타 계획서와 충돌 — 이 트랙은 `task-N-<주제>-*.md`로 씀.
- 테스트 경로는 `shopping_shorts/tests/` (계획서에 적힌 `tests/shopping_shorts/`는 **존재하지 않는 경로**).
