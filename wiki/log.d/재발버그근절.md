# 재발버그근절 — 작업 로그

## 2026-08-19
OPUS 지시서 A~G 7항목 전부 처리. 원칙은 "고치지 말고 못 나게" — 전 항목에 가드 테스트.
A) node -e 공용화(지시서 6파일→실측 11파일 17곳)+금지 가드로 4회째 차단
B) 대본생성 성공경로 스모크(08-16 버그 주입→실패 확인) C) TTS는 이미 해결·확인만
D) 씨앗 캐리오버는 이미 _s2Hydrate로 통합돼 있었고 **가드만 없었다** → 5개 추가
E) index.html은 이미 가드 있음, 무방비였던 produce.html(함수 349개) 전함수 TDZ 스캔(오탐 0)
F1) except:pass 실측 117개(지시서 226은 과대) → 늘어나는 것만 차단 F2) ops_alert 실패 2곳 사유로깅
G) 예열 래치 일시/영구 분리 + 복구 스크립트(라이브 9건 중 3건만 해제 대상)
검증: 신규 36개 / 전체 4,772 passed(1건은 기존부터 깨진 edit_plan).
⏭ 서버에서 prewarm_unlatch --apply 1회 필요.
