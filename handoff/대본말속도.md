# 대본말속도 — "2개 골랐는데 1개만 나온다"의 근본수정 (2026-09-18)

> 소유 트랙: 대본말속도. 이 파일은 이 트랙 세션만 수정한다.
> 앞선 기록: `handoff/대본게이트.md`(09-16 같은 증상, 되돌려짐) · `handoff/대본안수.md`(08-31)
> · `handoff/대본교정출구.md`(09-16 0안/502) · `handoff/대본소재이탈2.md`(09-11)

## 사장님 제보
"박복례님 대본이 한개만 나온다는데 이거 못고치는거지?" → "2가지를 선택했는데 1개만 나옴"
→ "그동안 못고친걸 이렇게 쉽게 할수있다고?" → "근본해결을 추측이아닌 확신으로 찾아"

## 이 증상은 8번째다
08-17 · 08-31 · 09-02 · 09-11 · 09-15 · 09-16(두 번) · 09-17.
**매번 원인을 다르게 진단했고, 같은 진단이 두 번 나온 적이 없다.**
진단은 프론트 표시 → 외부 쿼터 → 서버 필터 → 데이터/스파인 → 게이트 정책 → 파이프라인 구조로
계속 아래로 내려갔는데, **"게이트가 믿는 말속도가 틀렸다"는 층은 한 번도 지목된 적이 없다.**

## 원인 (전부 실측)

### 1. 게이트가 말속도를 30% 느리게 안다 ★뿌리
| 출처 | 값 |
|---|---|
| 게이트가 **실제로 쓰던** 값 | **7.41자/초** (`_SYLLABLES_PER_SEC 5.7 × _speech_speed 1.30`) |
| `script_gate.py:104` 주석이 적은 값 | 8.19자/초 |
| 라이브 TTS **실측** | **9.69자/초** (`edit_plan.narr_secs` 주석, 최근 60 job·비트 389개) |

→ "25초"를 시켜놓고 **19.1초**를 요구했다. 상한 185자 = 실제 음성 19.1초.

박복래 work `4124ceb3a88a` 스타일 54의 4번 시도와 실제 음성 길이:
| 글자 | 판정 | 실제 음성 |
|---|---|---|
| 209자 | 반려 | 21.6초 |
| 191자 | 반려 | 19.7초 |
| 222자 | 반려 | 22.9초 |
| 187자 | 반려(재단 후) | 19.3초 |

**요청한 25초보다 짧은데 "너무 길다"고 4번 다 죽였다.** → 빈손 → `generate_by_styles`가
조용히 건너뜀(`script_generate.py:1413`) → 1안.

### 2. `말 밀도`가 압도적 1위 사인 (라이브 대본 1,912개)
| 검사 | 재작성 유발 | 최종 사망 |
|---|---|---|
| **말 밀도** | **3,010** | **386** |
| 고조 심화 | 429 | 105 |
| 수치 근거 | 328 | 57 |
| 사실 근거 | 61 | **0** |

★09-14~09-16 사흘간 `[긴급]` 커밋 8개가 매달린 「사실 근거」는 **사망 0**이다. 엉뚱한 곳을 고치고 있었다.
★검사 19개 중 **5개는 한 번도 안 걸렸다**(사람이 나온다·쓰임이 번진다·재료 밖 판매처·훅 3초 서론금지·훅 3초).
★재작성 4회까지 간 안이 701개인데 그중 **53.5%가 끝내 실패**한다 = "엄청 늦다"의 정체.

### 3. 종전 창을 통과한 대본은 17초짜리였다
라이브 924개 중 종전 창(129~185) 통과분 514개의 **실제 음성 13.3~19.1초(중앙 17.4초)**.
**25초를 요청해도 라이브에 나가는 건 17초짜리였다.**

## 고친 것 (커밋 `8b9320aa5`)

| 파일 | 무엇 |
|---|---|
| `edit_plan.py` | `_speech_speed` 기본값 **1.30 → 1.70** (= 실측 9.69 ÷ 5.7, 역산 그대로). env `SCRIPT_SPEECH_SPEED`로 되돌림 가능 |
| `script_gate.py` | `density_target`에서 `_FILL_FLOOR` 바닥 제거 — 시간 강제는 `density_range._LEN_FLOOR` 한 곳에서만(0순위-B) |
| `script_gate.py` | `density_range`에서 **창 뒤집힘** 차단 — 느린 스타일이 `169~86` 같은 통과 불가능한 창을 받고 있었다 |
| `tests/test_length_and_wow_gate.py` | 가드 4개 신설 (전부 사보타주 검증) |
| `tests/test_bank_assemble.py` | `== cap` → `<= cap` 계약 정정 (우연한 등식이라 상수 바꿀 때마다 깨졌다) |

### 왜 바닥을 목표에서 뺐나
`_FILL_FLOOR`가 `density_target` 안에 있으면 느린 스타일이 전부 바닥에 붙는다 —
실측(25초): 밀도 **240·264·300이 전부 232**로 같아졌다. 08-24가 되살린 병
(`reference_길이단위_raw_norm_혼용`: "스타일 11개가 100% 같은 창")의 재발이다.
바닥을 없앤 게 아니다 — 09-09의 "25초에 9.6초 대본 금지"는 `density_range`가 계속 막는다.

## 검증 (실제 생성 — 서버·실제 키·실제 재료)
- **박복래 work `4124ceb3a88a` 스타일 54/59 → 2/2안** (각 198자, 음성 20.4초). 종전 1/2.
- **A/B 8작업 16안**: 현행 14/16·시도 55회 → 새것 **16/16·시도 31회**(재작성 44% 감소)
- 새로 반려되는 292개는 **전부 정당하다** — 중앙 15.5초·최소 4.2초. 09-09에 사장님이
  제보한 "10초가 안 되는 대본" 그것이고, 종전엔 통과하고 있었다.
- 사보타주 검증: 속도 되돌림 → `test_말속도는_라이브_실측과_맞는다` 빨개짐 /
  `hi = min(max(hi, lo), cap)` 제거 → `test_창은_절대_뒤집히지_않는다` 빨개짐.
- 관련 **822 passed / 0 failed**.

## ⏭ 다음 (아직 안 함)
- [ ] **`finish` 안 했다.** 영상 길이가 실제로 바뀌는 변경이라 사장님 판단을 받고 병합한다.
      09-16에 같은 영역이 한 번 되돌려졌다(`b6228cac8`, "수없이 테스트해서 검증돼야 내보내는 것").
- [ ] 병합 후 라이브에서 **실제 렌더 영상 길이**를 눈으로 확인(지금은 대본 글자수까지만 검증).
- [ ] **DB `chars_per_30s`가 실제와 안 맞는다** — 실측 중앙값은 138~192자인데 DB는 73~241 주장.
      「요놈 하나면」은 DB norm 73인데 실제 143(2.0배). 스타일별 밀도의 신뢰성은 별건으로.
- [ ] 죽은 검사 5개 제거 검토 (한 번도 안 걸림)
- [ ] 사망 0인 검사 4개(사실 근거·구간 순서·주제 단일성)는 재작성을 안 시키게 → 호출 절감
- [ ] `_trim_to_budget` 재단 성공률 **4%** — 부사 27개만 지운다. 목표 도달 보장 없음.

## 관련 메모리
`reference_길이단위_raw_norm_혼용` — "증상이 반복되는데 매번 숫자를 조정해서 고치고 있다면,
고치는 게 아니라 두더지를 잡고 있는 것이다." 이번엔 숫자 조정이 아니라 **실측과의 정합**이다.
