# 자막제거토글클릭 — 핸드오프

## 2026-09-04 — "자막제거 버튼 활성화가 안 된다" 제보 다수 → 스위치 클릭이 통째로 무효였다 (완료·main 병합 b19eec736)

### 원인
09-04 새벽 `자막제거잔액바로가기` 트랙이 우상단을 [크레딧 아이콘 / 토글] 세로 2단으로 개편하며
토글을 제목 `label.hbar` 밖으로 뺐다. 투명 체크박스 `.sw-input`(absolute, opacity 0)이 DOM상
`.sw-track`(position:relative) **앞**이라 뒤 형제 track이 위에 그려졌다 → 스위치를 눌러도
포인터가 `.sw-knob`/`.sw-track`(span)에 떨어져 체크박스는 안 바뀐다.
라이브 실측(elementFromPoint at track 중앙) = `sw-knob`. 예전엔 label 안에 있어 label이 대신 눌러줬다.
제목 글자(for=)를 누르면 켜지므로 "어디를 눌러야 되는지 모르겐" 상태였다(사장님 캡처: 옆 빈자리를 눌러야 켜짐).

### 고친 것 (`produce.html`)
- `.sw-input{... z-index:1}` — 입력을 track 위로. 주입 후 elementFromPoint = `subToggle` 확인.
- `.hright` flex-direction row — 사장님 "크레딧 버튼·스위치 옆으로 나란히".
- `tests/test_sub_toggle_clickable.py` 3건(기존 파일은 z-index 검사 실패 확인 → 수정 후 통과).

### 함께 조사한 것 (같은 세션)
- 박선정(cid 174) 채칼세트 job 07b4631ff098: subtitle_removal=false·clean 기록 없음 → 원본으로 렌더. 위 버그로 스위치를 못 켠 것과 정합.
- 박세현(cid 340) 캡컷 안됨: 서버 매니페스트·재료 25개 전부 200. 고객 PC 측 문제, 캡컷 서버 계측 0건(→ 계측 추가 제안).
- `refreshSub()`가 3단계 그릴 때마다 STATE 값을 job에 무조건 써넣음(12886) — 옛 탭/기기가 켜진 값을 꺼짐으로 되돌릴 수 있는 경로. 아직 안 고침.

### ⏭ 다음
- [ ] 라이브에서 실제 마우스로 스위치 클릭 → 켜짐 확인(브라우저 자동화는 창 배율 문제로 실클릭 검증 실패, hit-test만 확인)
- [ ] refreshSub의 무조건 쓰기 제거(사용자가 움직였을 때만 저장) + 최종렌더 직전 스위치 꺼짐 확인문구
- [ ] 스위치 켬/끔·청소 시작을 고객 활동기록에 값과 함께 남기기 / 캡컷 보내기 단계별 서버 계측
