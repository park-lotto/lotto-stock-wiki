# 문장수정 먹통 (beat_idx 중복)

## 2026-09-06 (회사PC) — 고객 제보 수리

고객 제보(26.피규어, work=c55d6f8988e8 / job=3ec9df659411):
> "아래칸에 있는 대본을 안 읽고, 위의 대사를 반복해요.
>  그래서 수정하려고 했는데 수정도 안되고, 삭제도 안되고, 먹통입니당"

### 진짜 원인 = 화면이 아니라 데이터
`edit_plan.beats`의 `beat_idx`가 **[0,1,3,2,5,5,5]** (5가 셋).
하류가 전부 유일 키로 쓰는데 검사가 없어 조용히 남의 칸에 작업했다.
- 저장 → `next(...)`가 첫 5를 집어 6번 칸 수정이 5번에 저장
- 삭제 → `!= beat_idx` 필터라 5번 셋이 한꺼번에 삭제
- 미리듣기 → 첫 5의 mp3 → "위의 대사를 반복"

실측: edit_plan 있는 **1438잡 중 6잡**이 중복(꼬리 쪽에 몰림).

### 발생처
`edit_plan._rebuild_beats_by_lines` — `nb = dict(base)`가 beat_idx까지 복사.
한 칸 대사가 대본 N줄에 걸치면 그 칸이 N칸의 원본 → 번호 N개 복제.
**저장 출구**(store._ensure_screen_time → enforce_script_order)에서 불려
`_fix_beat_structure` 재부여보다 뒤라 아무도 안 고쳐줬다.

### 고친 것
1. `edit_plan.py` `_rebuild_beats_by_lines` 유일 출구에서 enumerate 재부여
2. `app.py` `_find_beat` 공용 조회 — 겹치면 조용히 고르지 않고 막는다(저장·삭제·미리듣기 3곳)
3. `app.py` `/api/mix/scene_lab/{job}/renumber` + 화면 '번호 정리하기' 버튼(탈출구)
   ★번호 바뀐 칸의 tts_path·tts_ver는 버린다(안 버리면 남의 목소리)
4. `produce.html` 편집 중 `vpPlayBeat` 차단 — 편집칸이 `.ttsRowMain`(onclick) 안이라
   글자 고치려 클릭만 해도 재생+scrollIntoView가 돌아 버튼이 손 밑에서 움직였다
5. `produce.html` 삭제 시 편집칸 먼저 닫기(안 닫으면 renderTtsBeats가 즉시 return)
6. 회귀 테스트 `shopping_shorts/tests/test_beat_idx_unique.py` (3건)

### 실측·검증
- 사보타주: 재부여 빼면 `[0,5,5,5]` 재현 → 넣으면 `[0,1,2,3]`
- `_find_beat` 양방향: 중복 차단 / 유일번호 정상조회 / 정상잡 회귀 없음
- 실제 브라우저 로드: SyntaxError 0, 새 함수 3개 정의됨
- 고객 잡: 서버에서 `[0..6]`으로 정리(백업 `/home/ubuntu/beatidx_backup_3ec9df659411_*.json`)
  TTS 실존 0개라 잃은 것 없음, 대사 7줄 순서 그대로

### ⏭ 남은 것
- 나머지 중복 5잡은 **손대지 않았다**(사장님 판단: 고객 잡 1건만).
  b875c3731b73·6c0e9194cdd7·e2458d76dbb3 = status done + TTS 실존 → 재부여 위험
  1572bd6e8292·98b910755039 = TTS 0개라 안전하나 미처리
- 고객에게 "번호 정리하기" 버튼 안내 여부 결정

### 서버
IP가 43.200.48.69 → **3.35.251.172**로 바뀜(CLAUDE.md는 다른 세션이 갱신함).
