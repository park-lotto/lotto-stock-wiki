
## 2026-08-31
자막 미리보기↔최종렌더 어긋남 5건 수정. 뿌리=`_ui_px`가 테두리 두께 0을 기본값 9로 되살려
미리보기엔 없는 검정 테두리가 렌더에만 붙은 것(흰 자막이라 여태 안 걸림). 테두리 판정을
`_outline_parts()` 한 곳으로, 미리보기는 text-stroke로 통일. 렌더 테스트 66건 통과.
필름 펼치기(src)가 무반응인 뿌리 = toggleRoll의 anchor 가드. 소스별 헤더 버튼엔 `.srcfilm`
표식이 없고 제작소 안은 tight도 아니라 통째 return. anchor가 실제 필요한 cut에만 가드 적용.
라이브 실측으로 확인(가드 우회 시 "영상 1 전체" 필름 정상 펼침).
구절맞춤에서 조각 수 < 구절 수면 마지막 컷이 남은 구절을 통째로 덮어 뒤쪽 말에 화면 전환이
없던 것 수정. 첫 바퀴 1:1 유지 + 두 바퀴째부터 조각 뒷부분 이어 쓰기(화면·서버 짝).
실측: 컷 3→6/4/5, 합계=자막 길이, 담은 장면 전부 사용.
