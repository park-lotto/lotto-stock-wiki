# 영상제작 대시보드 ①기획 단계 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** `/yt` 단독 대시보드에서 아이디어 입력 → 유튜브 Data API 기반 터진영상 위젯(기여도/성과도 실수치) → 기획서 자동생성(QC 재작업, SSE 실시간표시) → 확정까지 실제로 동작하게 만든다.

**Architecture:** 기존 `scripts/yt_agents/agent_plan.py`(Gemini 기획 생성+검수)를 그대로 재사용하되, 새 얇은 래퍼 `plan_stage.py`가 이벤트를 yield하며 `pipeline.py`와 같은 `out/yt_pipeline_{pid}/state.json` 스키마에 기록한다. 터진영상은 새 `hot_clips.py`가 YouTube Data API v3(`requests`, 기존 `scripts/yt_trend/step1_fetch.py` 패턴 재사용)로 직접 조회해 채널 자체 평균 대비 % 계산. FastAPI에 `POST /yt/hot_clips`(일반 JSON), `POST /yt/generate_plan`(SSE, `dashboard/studio.html`의 `studio_generate` 패턴 재사용), `GET /yt`(정적 서빙) 3개 엔드포인트 추가. 프론트는 새 `dashboard/yt.html` 단독 페이지.

**Tech Stack:** Python 3.14 / FastAPI / `requests`(YouTube Data API v3) / 기존 `scripts/yt_agents/gemini_client.py`(Gemini) / SSE(`StreamingResponse`, `text/event-stream`) / vanilla JS(기존 STOCK BRAIN 페이지 패턴, 프레임워크 없음)

## Global Constraints

- `YOUTUBE_API_KEY`는 `.env`에 이미 있음 — 재사용, 새로 발급 불필요.
- 기여도/성과도 계산식: **채널 자체 최근 N개 영상 평균조회수 대비 %** (조회수÷구독자 방식 아님) — 2026-07-03 스펙 확정사항.
- 이번 플랜 범위는 **①기획 단계만** — 대본/리모션/녹음/자막/렌더는 다음 플랜.
- `/yt`는 STOCK BRAIN 공통 네비바에 탭 추가 안 함(단독 페이지) — 사용자 명시 확정.
- 기존 `out/yt_pipeline_{pid}/state.json` 스키마·경로 그대로 재사용, 새 DB 금지.
- 데이터 저장/파일 인코딩은 프로젝트 관례대로 전부 `encoding="utf-8"` 명시(cp949 오류 방지, 이 세션에서 반복적으로 발견된 버그 패턴).

---

## Task 1: `hot_clips.py` — YouTube Data API 검색 + 기여도/성과도 계산

**Files:**
- Create: `scripts/yt_agents/hot_clips.py`
- Test: `tests/yt_agents/test_hot_clips.py`

**Interfaces:**
- Produces:
  - `search_videos(query: str, max_results: int = 10) -> list[dict]` — 각 dict: `{video_id, title, channel_id, channel_title, published_at, thumbnail}`
  - `get_video_stats(video_ids: list[str]) -> dict[str, dict]` — key=video_id, value: `{view_count, like_count, comment_count}`
  - `get_channel_stats(channel_ids: list[str]) -> dict[str, dict]` — key=channel_id, value: `{subscriber_count, video_count}`
  - `get_channel_recent_avg_views(channel_id: str, n: int = 10) -> float` — 그 채널 최근 n개 영상 평균 조회수(0이면 0.0 반환, ZeroDivisionError 없음)
  - `find_hot_clips(query: str) -> list[dict]` — 최종 결과. 각 dict: `{video_id, title, channel_title, thumbnail, view_count, subscriber_count, view_pct_above_avg, contribution_grade, performance_grade}`
    - `view_pct_above_avg` = `round((view_count - avg) / avg * 100, 1)` (avg가 0이면 0.0)
    - `contribution_grade`: `view_pct_above_avg >= 700` → `"Great"`, `>= 200` → `"Good"`, 그 외 `"Normal"`
    - `performance_grade`: `like_count`를 같은 방식(채널 평균 좋아요 대비 %)으로 계산해 같은 등급 기준 적용

**Step 1: 실패하는 테스트 작성 — `search_videos`**

```python
# tests/yt_agents/test_hot_clips.py
import pytest
from unittest.mock import patch, MagicMock
import sys
from pathlib import Path
sys.path.insert(0, str(Path(__file__).parent.parent.parent))

from scripts.yt_agents import hot_clips


def _mock_response(json_data):
    m = MagicMock()
    m.json.return_value = json_data
    m.raise_for_status.return_value = None
    return m


def test_search_videos_returns_parsed_list():
    api_response = {
        "items": [
            {
                "id": {"videoId": "abc123"},
                "snippet": {
                    "title": "미장 폭락 5%, 지금 사도 될까",
                    "channelId": "UCxyz",
                    "channelTitle": "3protv",
                    "publishedAt": "2026-07-01T00:00:00Z",
                    "thumbnails": {"default": {"url": "http://x/thumb.jpg"}},
                },
            }
        ]
    }
    with patch("scripts.yt_agents.hot_clips.requests.get", return_value=_mock_response(api_response)) as mock_get:
        results = hot_clips.search_videos("반도체 조정", max_results=5)

    assert len(results) == 1
    assert results[0]["video_id"] == "abc123"
    assert results[0]["title"] == "미장 폭락 5%, 지금 사도 될까"
    assert results[0]["channel_id"] == "UCxyz"
    assert results[0]["channel_title"] == "3protv"
    assert results[0]["thumbnail"] == "http://x/thumb.jpg"
    called_params = mock_get.call_args.kwargs["params"]
    assert called_params["q"] == "반도체 조정"
    assert called_params["maxResults"] == 5
    assert called_params["order"] == "viewCount"
```

**Step 2: 테스트 실패 확인**

Run: `cd "c:\Users\TheRose\Desktop\로또의 주식" && py -m pytest tests/yt_agents/test_hot_clips.py::test_search_videos_returns_parsed_list -v`
Expected: FAIL — `ModuleNotFoundError: No module named 'scripts.yt_agents.hot_clips'`

**Step 3: `search_videos` 최소 구현**

```python
# scripts/yt_agents/hot_clips.py
"""유튜브 Data API 기반 터진 영상 탐지 — 채널 자체 평균 대비 % 계산.
2026-07-03: agent_plan.search_hot_clips()(Google검색+Gemini 추정)는 숫자가 부정확해서
실제 API 수치로 교체."""
import os
from pathlib import Path

import requests

ROOT = Path(__file__).parent.parent.parent
API_BASE = "https://www.googleapis.com/youtube/v3"


def _api_key() -> str:
    key = os.environ.get("YOUTUBE_API_KEY", "")
    if key:
        return key
    env_file = ROOT / ".env"
    if env_file.exists():
        for line in env_file.read_text(encoding="utf-8").splitlines():
            if line.startswith("YOUTUBE_API_KEY=") and not line.startswith("#"):
                return line.split("=", 1)[1].strip()
    return ""


def search_videos(query: str, max_results: int = 10) -> list[dict]:
    """유튜브 검색 — 관련도 높은 순, 최근 것 우선."""
    params = {
        "part": "snippet",
        "q": query,
        "type": "video",
        "regionCode": "KR",
        "relevanceLanguage": "ko",
        "order": "viewCount",
        "maxResults": max_results,
        "key": _api_key(),
    }
    r = requests.get(f"{API_BASE}/search", params=params)
    r.raise_for_status()
    out = []
    for item in r.json().get("items", []):
        sn = item["snippet"]
        out.append({
            "video_id": item["id"]["videoId"],
            "title": sn["title"],
            "channel_id": sn["channelId"],
            "channel_title": sn["channelTitle"],
            "published_at": sn["publishedAt"],
            "thumbnail": sn.get("thumbnails", {}).get("default", {}).get("url", ""),
        })
    return out
```

**Step 4: 테스트 통과 확인**

Run: `py -m pytest tests/yt_agents/test_hot_clips.py::test_search_videos_returns_parsed_list -v`
Expected: PASS

**Step 5: `get_video_stats` 테스트 작성 + 구현**

```python
# tests/yt_agents/test_hot_clips.py 에 추가
def test_get_video_stats_returns_dict_by_id():
    api_response = {
        "items": [
            {"id": "abc123", "statistics": {"viewCount": "124000", "likeCount": "3400", "commentCount": "210"}},
            {"id": "def456", "statistics": {"viewCount": "5000", "likeCount": "80"}},  # commentCount 없을 수도 있음
        ]
    }
    with patch("scripts.yt_agents.hot_clips.requests.get", return_value=_mock_response(api_response)):
        stats = hot_clips.get_video_stats(["abc123", "def456"])

    assert stats["abc123"] == {"view_count": 124000, "like_count": 3400, "comment_count": 210}
    assert stats["def456"] == {"view_count": 5000, "like_count": 80, "comment_count": 0}
```

```python
# hot_clips.py 에 추가
def get_video_stats(video_ids: list[str]) -> dict[str, dict]:
    if not video_ids:
        return {}
    params = {"part": "statistics", "id": ",".join(video_ids), "key": _api_key()}
    r = requests.get(f"{API_BASE}/videos", params=params)
    r.raise_for_status()
    out = {}
    for item in r.json().get("items", []):
        st = item["statistics"]
        out[item["id"]] = {
            "view_count": int(st.get("viewCount", 0)),
            "like_count": int(st.get("likeCount", 0)),
            "comment_count": int(st.get("commentCount", 0)),
        }
    return out
```

Run: `py -m pytest tests/yt_agents/test_hot_clips.py -v`
Expected: 두 테스트 모두 PASS

**Step 6: `get_channel_stats` 테스트 작성 + 구현**

```python
# tests/yt_agents/test_hot_clips.py 에 추가
def test_get_channel_stats_returns_dict_by_id():
    api_response = {
        "items": [
            {"id": "UCxyz", "statistics": {"subscriberCount": "82000", "videoCount": "420"}},
        ]
    }
    with patch("scripts.yt_agents.hot_clips.requests.get", return_value=_mock_response(api_response)):
        stats = hot_clips.get_channel_stats(["UCxyz"])

    assert stats["UCxyz"] == {"subscriber_count": 82000, "video_count": 420}
```

```python
# hot_clips.py 에 추가
def get_channel_stats(channel_ids: list[str]) -> dict[str, dict]:
    if not channel_ids:
        return {}
    params = {"part": "statistics", "id": ",".join(channel_ids), "key": _api_key()}
    r = requests.get(f"{API_BASE}/channels", params=params)
    r.raise_for_status()
    out = {}
    for item in r.json().get("items", []):
        st = item["statistics"]
        out[item["id"]] = {
            "subscriber_count": int(st.get("subscriberCount", 0)),
            "video_count": int(st.get("videoCount", 0)),
        }
    return out
```

Run: `py -m pytest tests/yt_agents/test_hot_clips.py -v`
Expected: 3개 테스트 모두 PASS

**Step 7: `get_channel_recent_avg_views` 테스트 작성 + 구현 (0으로 나누기 방지 케이스 포함)**

```python
# tests/yt_agents/test_hot_clips.py 에 추가
def test_get_channel_recent_avg_views_computes_average():
    search_response = {
        "items": [
            {"id": {"videoId": "v1"}, "snippet": {"title": "a", "channelId": "UCxyz", "channelTitle": "c",
                                                     "publishedAt": "2026-06-01T00:00:00Z", "thumbnails": {}}},
            {"id": {"videoId": "v2"}, "snippet": {"title": "b", "channelId": "UCxyz", "channelTitle": "c",
                                                     "publishedAt": "2026-06-02T00:00:00Z", "thumbnails": {}}},
        ]
    }
    stats_response = {
        "items": [
            {"id": "v1", "statistics": {"viewCount": "1000", "likeCount": "10"}},
            {"id": "v2", "statistics": {"viewCount": "3000", "likeCount": "30"}},
        ]
    }
    with patch("scripts.yt_agents.hot_clips.requests.get") as mock_get:
        mock_get.side_effect = [_mock_response(search_response), _mock_response(stats_response)]
        avg_view, avg_like = hot_clips.get_channel_recent_avg_views("UCxyz", n=10)

    assert avg_view == 2000.0
    assert avg_like == 20.0


def test_get_channel_recent_avg_views_returns_zero_when_no_videos():
    with patch("scripts.yt_agents.hot_clips.requests.get", return_value=_mock_response({"items": []})):
        avg_view, avg_like = hot_clips.get_channel_recent_avg_views("UCempty", n=10)

    assert avg_view == 0.0
    assert avg_like == 0.0
```

```python
# hot_clips.py 에 추가
def get_channel_recent_avg_views(channel_id: str, n: int = 10) -> tuple[float, float]:
    """그 채널 최근 n개 영상의 평균 조회수·평균 좋아요. 영상 없으면 (0.0, 0.0)."""
    params = {
        "part": "snippet", "channelId": channel_id, "type": "video",
        "order": "date", "maxResults": n, "key": _api_key(),
    }
    r = requests.get(f"{API_BASE}/search", params=params)
    r.raise_for_status()
    video_ids = [item["id"]["videoId"] for item in r.json().get("items", [])]
    if not video_ids:
        return 0.0, 0.0
    stats = get_video_stats(video_ids)
    views = [s["view_count"] for s in stats.values()]
    likes = [s["like_count"] for s in stats.values()]
    return (sum(views) / len(views), sum(likes) / len(likes))
```

Run: `py -m pytest tests/yt_agents/test_hot_clips.py -v`
Expected: 5개 테스트 모두 PASS

**Step 8: `find_hot_clips` 오케스트레이션 테스트 작성 + 구현**

```python
# tests/yt_agents/test_hot_clips.py 에 추가
def test_find_hot_clips_computes_grades():
    with patch("scripts.yt_agents.hot_clips.search_videos") as m_search, \
         patch("scripts.yt_agents.hot_clips.get_video_stats") as m_vstats, \
         patch("scripts.yt_agents.hot_clips.get_channel_recent_avg_views") as m_avg:
        m_search.return_value = [
            {"video_id": "v1", "title": "이거 놓치면 후회", "channel_id": "UCsmall",
             "channel_title": "소형채널", "published_at": "2026-06-28T00:00:00Z", "thumbnail": "t1"},
        ]
        m_vstats.return_value = {"v1": {"view_count": 41000, "like_count": 2000, "comment_count": 50}}
        m_avg.return_value = (500.0, 100.0)  # 평소 대비 크게 튐

        results = hot_clips.find_hot_clips("반도체 조정")

    assert len(results) == 1
    r = results[0]
    assert r["video_id"] == "v1"
    assert r["view_count"] == 41000
    assert r["view_pct_above_avg"] == pytest.approx((41000 - 500) / 500 * 100, rel=0.01)
    assert r["contribution_grade"] == "Great"  # >=700%
    assert r["performance_grade"] == "Great"  # (2000-100)/100*100 = 1900% >=700
```

```python
# hot_clips.py 에 추가
def _grade(pct: float) -> str:
    if pct >= 700:
        return "Great"
    if pct >= 200:
        return "Good"
    return "Normal"


def find_hot_clips(query: str) -> list[dict]:
    """검색 → 통계 조회 → 채널 평균 대비 % 계산 → 등급 부여."""
    videos = search_videos(query)
    if not videos:
        return []
    stats = get_video_stats([v["video_id"] for v in videos])

    results = []
    for v in videos:
        st = stats.get(v["video_id"], {"view_count": 0, "like_count": 0, "comment_count": 0})
        avg_view, avg_like = get_channel_recent_avg_views(v["channel_id"])

        view_pct = round((st["view_count"] - avg_view) / avg_view * 100, 1) if avg_view else 0.0
        like_pct = round((st["like_count"] - avg_like) / avg_like * 100, 1) if avg_like else 0.0

        results.append({
            "video_id": v["video_id"],
            "title": v["title"],
            "channel_title": v["channel_title"],
            "thumbnail": v["thumbnail"],
            "view_count": st["view_count"],
            "view_pct_above_avg": view_pct,
            "contribution_grade": _grade(view_pct),
            "performance_grade": _grade(like_pct),
        })
    return results
```

Run: `py -m pytest tests/yt_agents/test_hot_clips.py -v`
Expected: 6개 테스트 모두 PASS

**Step 9: 커밋**

```bash
git add scripts/yt_agents/hot_clips.py tests/yt_agents/test_hot_clips.py
git commit -m "feat(yt): hot_clips.py — 유튜브 Data API 기반 터진영상 탐지(채널 평균 대비 %)"
```

---

## Task 2: `POST /yt/hot_clips` 엔드포인트

**Files:**
- Modify: `dashboard/server.py`
- Test: `tests/test_yt_dashboard.py`

**Interfaces:**
- Consumes: `hot_clips.find_hot_clips(query: str) -> list[dict]` (Task 1)
- Produces: `POST /yt/hot_clips` — body `{"q": "반도체 조정"}` → `{"results": [...]}`

**Step 1: 실패하는 테스트 작성**

```python
# tests/test_yt_dashboard.py
import sys
from pathlib import Path
from unittest.mock import patch

import pytest
from fastapi.testclient import TestClient

sys.path.insert(0, str(Path(__file__).parent.parent))
sys.path.insert(0, str(Path(__file__).parent.parent / "dashboard"))

import server as server_module


@pytest.fixture
def client():
    return TestClient(server_module.app)


def test_hot_clips_endpoint_returns_results(client):
    fake_results = [{"video_id": "v1", "title": "테스트", "view_count": 1000,
                      "view_pct_above_avg": 50.0, "contribution_grade": "Normal",
                      "performance_grade": "Normal", "channel_title": "c", "thumbnail": ""}]
    with patch("server.find_hot_clips", return_value=fake_results):
        resp = client.post("/yt/hot_clips", json={"q": "반도체 조정"})

    assert resp.status_code == 200
    assert resp.json()["results"] == fake_results
```

**Step 2: 테스트 실패 확인**

Run: `py -m pytest tests/test_yt_dashboard.py::test_hot_clips_endpoint_returns_results -v`
Expected: FAIL — `404` (엔드포인트 없음) 또는 `ImportError`

**Step 3: 엔드포인트 구현**

`dashboard/server.py` 상단 import 구역(기존 `from nlm_bridge import (...)` 근처)에 추가:

```python
sys.path.insert(0, os.path.join(ROOT, "scripts", "yt_agents"))
from hot_clips import find_hot_clips
```

`dashboard/server.py`에 새 섹션 추가(기존 `/api/insights/*` 엔드포인트들 근처, 파일 끝 `if __name__` 이전 아무 곳):

```python
# ── §5 유튜브 영상제작 대시보드 (/yt) ──────────────────────────
@app.post("/yt/hot_clips")
async def api_yt_hot_clips(req: Request):
    body = await req.json()
    q = (body.get("q") or "").strip()
    if not q:
        return JSONResponse(content={"error": "검색어 필요"}, status_code=400)

    def _do():
        return find_hot_clips(q)

    results = await run_in_threadpool(_do)
    return JSONResponse(content={"results": results})
```

**Step 4: 테스트 통과 확인**

Run: `py -m pytest tests/test_yt_dashboard.py::test_hot_clips_endpoint_returns_results -v`
Expected: PASS

**Step 5: 커밋**

```bash
git add dashboard/server.py tests/test_yt_dashboard.py
git commit -m "feat(yt): POST /yt/hot_clips 엔드포인트"
```

---

## Task 3: `plan_stage.py` — 기획 생성 단일 스테이지 러너 (이벤트 yield)

**Files:**
- Create: `scripts/yt_agents/plan_stage.py`
- Test: `tests/yt_agents/test_plan_stage.py`

**Interfaces:**
- Consumes: `agent_plan.run(idea: str, feedback: str = '') -> str`, `agent_plan.qc(plan_text: str) -> dict`(기존 `scripts/yt_agents/agent_plan.py`, 시그니처 변경 없음)
- Produces: `run_plan_stage(idea: str, references: list[dict] = None, pipeline_id: str = None) -> Iterator[dict]` — 각 yield는 `{"type": "step"|"done"|"error", ...}` 형태 이벤트. 마지막 `"done"` 이벤트에 `{"type":"done","pid":...,"plan_text":...,"qc_score":...}` 포함.

**Step 1: 실패하는 테스트 작성**

```python
# tests/yt_agents/test_plan_stage.py
import sys
from pathlib import Path
from unittest.mock import patch

sys.path.insert(0, str(Path(__file__).parent.parent.parent))

from scripts.yt_agents import plan_stage


def test_run_plan_stage_yields_done_on_first_pass(tmp_path, monkeypatch):
    monkeypatch.setattr(plan_stage, "OUT", tmp_path)

    with patch("scripts.yt_agents.plan_stage.agent_plan.run", return_value="# 기획서\n내용") as m_run, \
         patch("scripts.yt_agents.plan_stage.agent_plan.qc", return_value={"total": 8, "passed": True, "feedback": ""}):
        events = list(plan_stage.run_plan_stage("반도체 조정", references=[], pipeline_id="test123"))

    types = [e["type"] for e in events]
    assert "step" in types
    assert types[-1] == "done"
    done = events[-1]
    assert done["qc_score"] == 8
    assert done["plan_text"] == "# 기획서\n내용"
    m_run.assert_called_once()

    # state.json 기록 확인
    state_file = tmp_path / "yt_pipeline_test123" / "state.json"
    assert state_file.exists()
    import json
    state = json.loads(state_file.read_text(encoding="utf-8"))
    assert state["steps"]["plan"]["status"] == "done"
    assert state["steps"]["plan"]["qc_score"] == 8
```

**Step 2: 테스트 실패 확인**

Run: `py -m pytest tests/yt_agents/test_plan_stage.py -v`
Expected: FAIL — `ModuleNotFoundError: No module named 'scripts.yt_agents.plan_stage'`

**Step 3: 최소 구현**

```python
# scripts/yt_agents/plan_stage.py
"""기획 단계만 실행하는 얇은 러너 — SSE 이벤트 yield.
pipeline.py의 STEP1 로직을 그대로 따르되(같은 state.json 스키마), 웹에서 실시간
진행상황을 보여줄 수 있게 print 대신 이벤트를 yield한다. pipeline.py는 안 건드림(최소침습).
주의: pipeline.py처럼 `from . import agent_plan`(상대import, 패키지경로로만 로드 가능)를
쓰지 않는다 — dashboard/server.py가 sys.path에 scripts/yt_agents 디렉터리를 직접 추가해
flat하게 import하므로(Task 4), 여기서도 sys.path 기반 절대import로 통일한다."""
import json
import sys
from datetime import datetime
from pathlib import Path

sys.path.insert(0, str(Path(__file__).parent))
import agent_plan

ROOT = Path(__file__).parent.parent.parent
OUT = ROOT / "out"
THRESHOLD = 7
MAX_RETRY = 3


def _state_path(pid):
    return OUT / f"yt_pipeline_{pid}" / "state.json"


def _load_state(pid, idea):
    p = _state_path(pid)
    if p.exists():
        return json.loads(p.read_text(encoding="utf-8"))
    return {"id": pid, "idea": idea, "created": datetime.now().isoformat(), "steps": {}, "files": {}}


def _save_state(pid, state):
    p = _state_path(pid)
    p.parent.mkdir(parents=True, exist_ok=True)
    p.write_text(json.dumps(state, ensure_ascii=False, indent=2), encoding="utf-8")


def _format_references(references: list[dict]) -> str:
    if not references:
        return ""
    lines = ["\n\n=== 사용자가 담은 터진 영상 레퍼런스 (참고할 것) ==="]
    for r in references:
        lines.append(f"- \"{r.get('title','')}\" ({r.get('channel_title','')}) "
                      f"— 채널 평균 대비 {r.get('view_pct_above_avg', 0):+.0f}%")
    return "\n".join(lines)


def run_plan_stage(idea: str, references: list[dict] = None, pipeline_id: str = None):
    pid = pipeline_id or datetime.now().strftime("%Y%m%d_%H%M%S")
    idea_with_refs = idea + _format_references(references or [])
    state = _load_state(pid, idea)

    yield {"type": "step", "id": "plan", "status": "running", "attempt": 1, "max_attempt": MAX_RETRY}

    plan_dir = OUT / f"yt_pipeline_{pid}"
    plan_dir.mkdir(parents=True, exist_ok=True)
    plan_file = plan_dir / "plan.md"

    qc_feedback, total_score, attempt = "", 0, 0
    for attempt in range(MAX_RETRY):
        plan_text = agent_plan.run(idea_with_refs, feedback=qc_feedback)
        plan_file.write_text(plan_text, encoding="utf-8")

        qc = agent_plan.qc(plan_text)
        total_score = qc.get("total", 0)
        passed = total_score >= THRESHOLD

        yield {
            "type": "step", "id": "plan", "status": "running",
            "attempt": attempt + 1, "max_attempt": MAX_RETRY,
            "qc_score": total_score, "feedback": qc.get("feedback", ""),
        }
        if passed:
            break
        qc_feedback = qc.get("feedback", "")

    state["steps"]["plan"] = {"status": "done", "qc_score": total_score, "attempts": attempt + 1}
    state["files"]["plan"] = str(plan_file)
    _save_state(pid, state)

    yield {"type": "done", "pid": pid, "plan_text": plan_text, "qc_score": total_score}
```

**Step 4: 테스트 통과 확인**

Run: `py -m pytest tests/yt_agents/test_plan_stage.py -v`
Expected: PASS

**Step 5: QC 미달로 재시도하는 케이스 테스트 추가**

```python
# tests/yt_agents/test_plan_stage.py 에 추가
def test_run_plan_stage_retries_until_pass(tmp_path, monkeypatch):
    monkeypatch.setattr(plan_stage, "OUT", tmp_path)

    qc_results = [
        {"total": 5, "passed": False, "feedback": "레퍼런스 인용 부족"},
        {"total": 8, "passed": True, "feedback": ""},
    ]
    with patch("scripts.yt_agents.plan_stage.agent_plan.run", return_value="# 기획서") as m_run, \
         patch("scripts.yt_agents.plan_stage.agent_plan.qc", side_effect=qc_results):
        events = list(plan_stage.run_plan_stage("아이디어", references=[], pipeline_id="test456"))

    running_events = [e for e in events if e["type"] == "step" and "qc_score" in e]
    assert len(running_events) == 2
    assert running_events[0]["qc_score"] == 5
    assert running_events[1]["qc_score"] == 8
    assert events[-1]["qc_score"] == 8
    assert m_run.call_count == 2
    # 두번째 호출은 feedback을 받아야 함
    assert "레퍼런스 인용 부족" in m_run.call_args_list[1].kwargs["feedback"]
```

Run: `py -m pytest tests/yt_agents/test_plan_stage.py -v`
Expected: 2개 테스트 모두 PASS

**Step 6: 레퍼런스 포맷팅 테스트 추가**

```python
# tests/yt_agents/test_plan_stage.py 에 추가
def test_format_references_includes_title_and_pct():
    refs = [{"title": "미장 폭락 5%", "channel_title": "3protv", "view_pct_above_avg": 210.0}]
    text = plan_stage._format_references(refs)
    assert "미장 폭락 5%" in text
    assert "3protv" in text
    assert "+210%" in text


def test_format_references_empty_list_returns_empty_string():
    assert plan_stage._format_references([]) == ""
```

Run: `py -m pytest tests/yt_agents/test_plan_stage.py -v`
Expected: 4개 테스트 모두 PASS

**Step 7: 커밋**

```bash
git add scripts/yt_agents/plan_stage.py tests/yt_agents/test_plan_stage.py
git commit -m "feat(yt): plan_stage.py — 기획 단계 이벤트yield 러너(레퍼런스 주입 포함)"
```

---

## Task 4: `POST /yt/generate_plan` SSE 엔드포인트

**Files:**
- Modify: `dashboard/server.py`
- Test: `tests/test_yt_dashboard.py`

**Interfaces:**
- Consumes: `plan_stage.run_plan_stage(idea, references, pipeline_id) -> Iterator[dict]` (Task 3)
- Produces: `POST /yt/generate_plan` — body `{"idea": str, "references": [...]}` → SSE 스트림, 각 줄 `data: {...}\n\n`

**참고**: `dashboard/server.py`의 기존 `studio_generate` SSE 엔드포인트(파일 내 `text/event-stream` 검색하면 찾을 수 있음)가 같은 패턴 — generator를 `StreamingResponse`로 감싸는 방식을 그대로 따른다.

**Step 1: 실패하는 테스트 작성**

```python
# tests/test_yt_dashboard.py 에 추가
def test_generate_plan_streams_sse_events(client):
    def fake_events(idea, references, pipeline_id):
        yield {"type": "step", "id": "plan", "status": "running", "attempt": 1}
        yield {"type": "done", "pid": "test789", "plan_text": "# 완성", "qc_score": 8}

    with patch("server.run_plan_stage", side_effect=fake_events):
        resp = client.post("/yt/generate_plan", json={"idea": "반도체 조정", "references": []})

    assert resp.status_code == 200
    assert "text/event-stream" in resp.headers["content-type"]
    body = resp.text
    assert '"type": "step"' in body or '"type":"step"' in body
    assert '"type": "done"' in body or '"type":"done"' in body
```

**Step 2: 테스트 실패 확인**

Run: `py -m pytest tests/test_yt_dashboard.py::test_generate_plan_streams_sse_events -v`
Expected: FAIL — 404

**Step 3: 엔드포인트 구현**

`dashboard/server.py` import 구역에 추가:

```python
from plan_stage import run_plan_stage
```

`/yt/hot_clips` 엔드포인트 바로 아래에 추가:

```python
@app.post("/yt/generate_plan")
async def api_yt_generate_plan(req: Request):
    body = await req.json()
    idea = (body.get("idea") or "").strip()
    references = body.get("references") or []
    if not idea:
        return JSONResponse(content={"error": "아이디어 필요"}, status_code=400)

    def _stream():
        try:
            for event in run_plan_stage(idea, references=references):
                yield f"data: {json.dumps(event, ensure_ascii=False)}\n\n"
        except Exception as e:
            yield f"data: {json.dumps({'type': 'error', 'message': str(e)[:200]}, ensure_ascii=False)}\n\n"

    return StreamingResponse(_stream(), media_type="text/event-stream")
```

(`json`, `StreamingResponse`는 `dashboard/server.py`에 이미 import돼 있음 — `studio_generate` 엔드포인트가 이미 쓰고 있으므로 중복 import 불필요, 없으면 파일 상단에 추가.)

**Step 4: 테스트 통과 확인**

Run: `py -m pytest tests/test_yt_dashboard.py -v`
Expected: 2개 테스트 모두 PASS

**Step 5: 커밋**

```bash
git add dashboard/server.py tests/test_yt_dashboard.py
git commit -m "feat(yt): POST /yt/generate_plan SSE 엔드포인트"
```

---

## Task 5: `dashboard/yt.html` — 프론트엔드 (기획 페이지)

**Files:**
- Create: `dashboard/yt.html`
- Modify: `dashboard/server.py` (`GET /yt` 라우트 추가)

**Interfaces:**
- Consumes: `POST /yt/hot_clips`(Task 2), `POST /yt/generate_plan`(Task 4)

**Step 1: `GET /yt` 라우트 추가**

`dashboard/server.py`의 `/yt/hot_clips` 엔드포인트 위에 추가:

```python
@app.get("/yt", response_class=HTMLResponse)
def yt_page():
    p = os.path.join(HERE, "yt.html")
    if not os.path.exists(p):
        return "<h1>yt.html 준비중</h1>"
    with open(p, encoding="utf-8") as f:
        return f.read()
```

**Step 2: `yt.html` 작성 — STOCK BRAIN 다크/골드 테마, 2026-07-03 브레인스토밍에서 확정된 6단계 흐름(아이디어→레퍼런스→생성버튼→진행상황→기획서→확정) 그대로**

```html
<!DOCTYPE html>
<html lang="ko">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>영상제작 스튜디오 — 로또의 주식</title>
<script src="https://cdn.jsdelivr.net/npm/marked/marked.min.js"></script>
<style>
:root {
  --gold: #d4af37; --gold-dim: #9a7d28; --bg: #0a0a0a;
  --card: #16161a; --line: #2a2a30; --txt: #e8e6e0; --muted: #8a8a92;
}
* { box-sizing: border-box; margin: 0; padding: 0; }
body { background: var(--bg); color: var(--txt); font-family: 'Segoe UI','Malgun Gothic',sans-serif; }
.topbar { height: 54px; background: var(--card); border-bottom: 1px solid var(--line);
  display: flex; align-items: center; padding: 0 28px; justify-content: space-between; }
.topbar .brand { font-size: .92rem; font-weight: 700; color: var(--gold); }
#app { max-width: 900px; margin: 0 auto; padding: 28px; }
.label { font-size: .74rem; color: var(--muted); font-weight: 700; text-transform: uppercase;
  letter-spacing: .6px; margin: 18px 0 8px; }
.mock-input, input[type=text] { width: 100%; padding: 12px 16px; background: var(--card);
  border: 1px solid var(--line); border-radius: 10px; color: var(--txt); font-family: inherit;
  font-size: .9rem; }
.btn { padding: 10px 18px; border-radius: 9px; cursor: pointer; font-family: inherit;
  font-size: .84rem; font-weight: 700; color: #0a0a0a; background: var(--gold);
  border: none; }
.btn:disabled { opacity: .5; cursor: default; }
.btn.secondary { color: var(--txt); background: var(--card); border: 1px solid var(--line); }
table { width: 100%; border-collapse: collapse; font-size: .82rem; margin-top: 10px; }
th, td { padding: 8px 6px; text-align: left; border-bottom: 1px solid var(--line); }
th { color: var(--muted); font-weight: 600; font-size: .74rem; }
.grade-great { color: var(--gold); font-weight: 700; }
.grade-good { color: #22c55e; font-weight: 700; }
.grade-normal { color: var(--muted); }
.card { background: var(--card); border: 1px solid var(--line); border-radius: 12px;
  padding: 16px; margin-top: 10px; }
.qc-line { font-size: .82rem; color: var(--muted); padding: 4px 0; }
.qc-line.pass { color: var(--gold); }
.plan-box h1 { font-size: 1.1rem; color: var(--gold); margin-bottom: 8px; }
.plan-box { font-size: .88rem; line-height: 1.7; }
.action-row { display: flex; gap: 8px; margin-top: 14px; }
</style>
</head>
<body>

<div class="topbar">
  <span class="brand">🎬 영상제작 스튜디오</span>
  <a href="/" style="color:var(--muted);font-size:.8rem;text-decoration:none">← 메인으로</a>
</div>

<div id="app">
  <div class="label">① 아이디어</div>
  <input type="text" id="idea-input" placeholder="예: 어젯밤 메타발 반도체 조정, 오늘 아침 리포트는 정반대였다">

  <div class="label">② 터진 영상 (선택사항)</div>
  <div style="display:flex;gap:8px">
    <input type="text" id="hotclip-query" placeholder="검색어: 반도체 조정" style="flex:1">
    <button class="btn secondary" onclick="searchHotClips()">🔥 실시간 탐지</button>
  </div>
  <div id="hotclip-results"></div>
  <div id="hotclip-picked" style="font-size:.78rem;color:var(--muted);margin-top:6px"></div>

  <div class="label">③ 생성</div>
  <button class="btn" id="generate-btn" onclick="generatePlan()">🎬 기획서 생성</button>

  <div id="progress-area"></div>
  <div id="plan-area"></div>
</div>

<script>
let pickedRefs = [];

async function searchHotClips() {
  const q = document.getElementById('hotclip-query').value.trim();
  if (!q) return;
  const box = document.getElementById('hotclip-results');
  box.innerHTML = '<p style="color:var(--muted);font-size:.82rem">검색 중…</p>';
  try {
    const res = await fetch('/yt/hot_clips', {
      method: 'POST', headers: {'Content-Type': 'application/json'},
      body: JSON.stringify({q})
    });
    const data = await res.json();
    if (data.error) { box.innerHTML = `<p style="color:#e88">${data.error}</p>`; return; }
    renderHotClips(data.results);
  } catch (e) {
    box.innerHTML = `<p style="color:#e88">검색 실패: ${e.message}</p>`;
  }
}

function gradeClass(g) {
  if (g === 'Great') return 'grade-great';
  if (g === 'Good') return 'grade-good';
  return 'grade-normal';
}

function renderHotClips(results) {
  const box = document.getElementById('hotclip-results');
  if (!results.length) { box.innerHTML = '<p style="color:var(--muted);font-size:.82rem">결과 없음</p>'; return; }
  let html = '<table><tr><th></th><th>제목</th><th>채널</th><th>조회수</th><th>기여도</th><th>성과도</th></tr>';
  results.forEach((r, i) => {
    html += `<tr>
      <td><button class="btn secondary" style="font-size:.75rem;padding:4px 10px" onclick='pickRef(${JSON.stringify(r)})'>담기</button></td>
      <td>${r.title}</td><td>${r.channel_title}</td>
      <td>${r.view_count.toLocaleString()}<br><span style="font-size:.75rem;color:var(--muted)">(${r.view_pct_above_avg >= 0 ? '+' : ''}${r.view_pct_above_avg}%)</span></td>
      <td class="${gradeClass(r.contribution_grade)}">${r.contribution_grade}</td>
      <td class="${gradeClass(r.performance_grade)}">${r.performance_grade}</td>
    </tr>`;
  });
  html += '</table>';
  box.innerHTML = html;
}

function pickRef(r) {
  if (pickedRefs.find(x => x.video_id === r.video_id)) return;
  pickedRefs.push(r);
  document.getElementById('hotclip-picked').textContent =
    `📥 ${pickedRefs.length}건 담김: ` + pickedRefs.map(x => x.title).join(' · ');
}

async function generatePlan() {
  const idea = document.getElementById('idea-input').value.trim();
  if (!idea) { alert('아이디어를 입력하세요'); return; }
  const btn = document.getElementById('generate-btn');
  btn.disabled = true; btn.textContent = '생성 중…';

  const progressBox = document.getElementById('progress-area');
  const planBox = document.getElementById('plan-area');
  progressBox.innerHTML = '<div class="card" id="progress-card"></div>';
  planBox.innerHTML = '';
  const progressCard = document.getElementById('progress-card');

  try {
    const resp = await fetch('/yt/generate_plan', {
      method: 'POST', headers: {'Content-Type': 'application/json'},
      body: JSON.stringify({idea, references: pickedRefs})
    });
    const reader = resp.body.getReader();
    const decoder = new TextDecoder();
    let buf = '';
    while (true) {
      const {done, value} = await reader.read();
      if (done) break;
      buf += decoder.decode(value, {stream: true});
      const parts = buf.split('\n\n');
      buf = parts.pop();
      for (const part of parts) {
        if (!part.startsWith('data: ')) continue;
        const event = JSON.parse(part.slice(6));
        handleEvent(event, progressCard, planBox);
      }
    }
  } catch (e) {
    progressCard.innerHTML += `<div class="qc-line" style="color:#e88">실패: ${e.message}</div>`;
  } finally {
    btn.disabled = false; btn.textContent = '🎬 기획서 생성';
  }
}

function handleEvent(event, progressCard, planBox) {
  if (event.type === 'step') {
    const passed = event.qc_score !== undefined && event.qc_score >= 7;
    const line = document.createElement('div');
    line.className = 'qc-line' + (passed ? ' pass' : '');
    line.textContent = event.qc_score !== undefined
      ? `시도 ${event.attempt}/${event.max_attempt} · 점수 ${event.qc_score}/9${passed ? ' → 통과' : ''}`
      : `생성 중… (시도 ${event.attempt}/${event.max_attempt})`;
    progressCard.appendChild(line);
  } else if (event.type === 'done') {
    planBox.innerHTML = `
      <div class="label">④ 완성된 기획서 (QC ${event.qc_score}/9)</div>
      <div class="card plan-box">${marked.parse(event.plan_text)}</div>
      <div class="action-row">
        <button class="btn">✅ 이 기획으로 대본 진행 →</button>
        <button class="btn secondary" onclick="generatePlan()">🔁 다시 생성</button>
      </div>`;
  } else if (event.type === 'error') {
    progressCard.innerHTML += `<div class="qc-line" style="color:#e88">오류: ${event.message}</div>`;
  }
}
</script>
</body>
</html>
```

**Step 3: 수동 검증 (렌더 산출물이라 실제 브라우저 구동 확인 필수 — verification-grounding-pack 규칙)**

```bash
cd "c:\Users\TheRose\Desktop\로또의 주식\dashboard" && py server.py
```

브라우저에서 `http://localhost:8090/yt` 접속 후:
1. 아이디어 입력란에 텍스트 입력
2. "🔥 실시간 탐지" 클릭 → 검색어 입력 후 결과 테이블 뜨는지 확인 (실제 YOUTUBE_API_KEY로 진짜 API 호출됨)
3. 결과 중 하나 "담기" 클릭 → 하단에 "N건 담김" 표시되는지 확인
4. "🎬 기획서 생성" 클릭 → 진행상황(시도 N/3, 점수) 실시간으로 쌓이는지, 완료 시 기획서 마크다운 렌더되는지 확인
5. 브라우저 개발자도구 콘솔에 JS 에러 없는지 확인

**Step 4: 커밋**

```bash
git add dashboard/yt.html dashboard/server.py
git commit -m "feat(yt): yt.html — 기획 페이지 프론트엔드(터진영상 위젯+SSE 진행표시)"
```

---

## Self-Review 체크리스트 (구현자가 각 태스크 완료 후 확인)

- [ ] Task 1: `hot_clips.py`의 6개 테스트 모두 실제 통과하는지(`py -m pytest tests/yt_agents/test_hot_clips.py -v`)
- [ ] Task 3: `plan_stage.py`가 `pipeline.py`의 기존 `agent_plan.run/qc` 시그니처를 변경하지 않았는지(다른 스테이지가 나중에 pipeline.py를 그대로 쓸 수 있어야 함)
- [ ] Task 4: SSE 엔드포인트가 예외 발생 시에도 스트림이 깨지지 않고 `{"type":"error"}` 이벤트로 마무리되는지
- [ ] Task 5: 실제 브라우저에서 YOUTUBE_API_KEY 진짜 호출로 결과가 뜨는지 (모킹 테스트 통과와 별개로 반드시 실행 확인)
- [ ] 전체: `git status`로 atoms.db 등 무관한 데이터 파일이 실수로 스테이징되지 않았는지 커밋마다 확인
