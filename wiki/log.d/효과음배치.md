
## 2026-08-21 (회사PC)
- 효과음 시스템은 07-22에 이미 라이브였고 **sfx 자산 0개**라 안 붙던 것 — 구현이 아니라 등록 문제였다.
- `transition` 타점 신설(칸 끝=다음 칸 시작, 훅 기본값) · API를 빼기전용→교체·타점변경 · 재매칭이 수동선택 안 덮게 보호 · 3단계 칸 도구줄에 선택칸+▶미리듣기 · 재생 시 타점 맞춰 실제 믹스. 전부 배포·브라우저 검증.
- ⏭ 타이밍은 **활성 탭 미검증**(자동화 탭 hidden → setTimeout 클램프로 0.8초 늦게 측정). 실제 효과음 파일·sfx_volume 튜닝도 남음.

## 2026-08-29 — 타점 기본값을 실측으로 교정 (효과음타점 트랙)
- 대세 채널 3편(이븐쇼핑·긍정템·공가미) 오디오 실측 → **효과음은 역할이 아니라 컷에 붙는다**:
  컷 55개 중 52개(95%)에 소리, 타점은 컷 경계 Δ중앙값 **-12ms**(먼저 27/나중 25로 반반), 길이 20~200ms.
- `_SFX_POSITION` 기본값 last→**transition** (역할 무관). 라이브 f77d9a98a, 서버 확인.
  종전 "last(문장 맺는 자리)"는 검증 안 된 추측이었다. last는 칸별 수동 선택으로 그대로 남음.
- ★실렌더 검증: 칸 경계 3.0/6.0/9.0s → 결과물에서 2.96/5.96/8.93s에 울림(hop 10ms+mp3 지연).
- ★검출기는 합성 정답파일로 **먼저 검증하고** 썼다(5/5, 음성만 파일 0오탐). 처음 두 번은
  대역을 잘못 잡아 0건이 나왔다 — 그대로 믿었으면 "효과음 없다"고 틀리게 답할 뻔했다.
- 밀도 실측: 이븐쇼핑 40건/분·긍정템 38건/분·공가미 9건/분. 컷이 빠른 채널일수록 빽빽.
- ⏭ **자산이 여전히 테스트톤 2개뿐** — UI는 자산 0개면 줄을 아예 안 낸다(scene_lab.html:2251).
  검증용 6종(띠용·반짝·스와이프·붐·톡·딩)을 만들어 `바탕화면/효과음_샘플`에 뒀다. 사장님 확인 후 등록.
