
## 2026-08-26 — "오늘도 자막제거 실패" 제보 처리
- 원인: 고장 아님. cid57의 vmake 키가 옛 편법(`service='vmake_paused'`)으로 꺼진 채 남아
  "키 없음" 판정 → 본사 키로 돌며 매회 5P 차감 → 잔액 2.1P에서 막힘(6회 재시도).
- 조치①: 라이브 DB에서 id=45를 `vmake`/`ok`로 복구. 서비스 env로 실측 검수 —
  복호 1건·`is_user=True`·`should_charge=False` 확인(복호 문제 아니었음).
- 조치②: `clean_failure_kind`에 `no_points` 추가. 08-25에 `no_credit`을 갈랐는데도
  우리 포인트 부족이 `unknown`으로 새어 "잠시 후 다시 시도"가 나가던 재발을 막았다.
  화면은 재시도 버튼 없이 "본인 키 등록하면 차감 없음"으로 유도. 테스트 신규 9건.
- 덤: 이 트랙의 앞선 커밋 9c3980da5가 produce.html에 'VMake'를 넣어 브랜드 정책
  게이트를 깨뜨린 상태였다(main엔 0건) — 그래서 그 문구 수정이 여태 라이브에 못 갔다. 제거.
