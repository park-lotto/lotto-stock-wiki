# 숏템엔진 — 핸드오프 (2026-09-25 시작, CH PC)

## 목표 (사장님 확정 2026-09-25)
볼케이노 방식 = **공통 엔진 하나 + 채널마다 spec 한 장**. 고른 채널을 100% 벤치마킹해 양산. 내부용 먼저, 상품화 나중.
접근 A 채택: `.tracks/뇌전구벤치` brainbulb(3,982줄·20모듈·테스트 9개)를 `shopping_shorts/channelkit/`(엔진) + `shopping_shorts/channel_presets/brainbulb/`(1호) 로 쪼개고, 2호 `channel_presets/hotpeople/`(뜨거운사람들) 추가.

## 확정된 것
- 트랙명 `숏템엔진` (track/뇌전구벤치에서 분기) · 표본 20편 실측 · 기계+설정표 분리
- 씨앗 = 인물명·한 줄 주제 → 웹 리서치(출처 없는 문장은 대본 금지) → 대본 린터 → 소스 자동 검색·다운로드·장면 매칭 → 레이아웃 상수 → 렌더(BGM) → 검수
- 사람 자리 2곳: script 확정 · footage 후보 확정

## 뜨거운사람들 실측 (2026-09-24, 최근 50편 flat + 2편 다운로드)
구독 21,000 / 조회 중앙값 81,000·10만↑ 23/50·최고 210만 / 08-02→09-24 50편(매일) / 46~62초 /
컷 17(3.4초)·33(1.8초) / **나레 없음** — Whisper base 2편: "아 아 아"·팝송 가사 = BGM만 /
레이아웃 흰 바탕·좌상단 로고·2줄 헤드라인(검정+빨강|노랑 강조)·가로 슬롯·2줄 자막(강조어 빨강) / 반말 ~음·~임·ㄷㄷ·ㅋㅋ
리스크: 소스 클립 확보(인물→맞는 장면) · 방송 클립 저작권/재사용 콘텐츠 판정

## 옆 세션(Desktop/볼케이노작업)과의 경계
- 옆 세션은 볼케이노 11채널 역분석을 **문서로만**(`카피/docs/`), 뇌전구벤치 코드 수정 금지(쪼개는 중). 전달문: `Desktop/볼케이노작업/숏템엔진_진행상황.md`
- 쪼개진 뒤 새 채널은 `channel_presets/<이름>/spec.py`로 얹는다.

## ⏭ 다음
- 설계 섹션 3~5(사람 자리·소스 확보·검증) 확인 → spec 문서 `docs/superpowers/specs/2026-09-25-숏템엔진-design.md` → writing-plans
- `py tools/track.py start 숏템엔진` (뇌전구벤치 브랜치에서 분기하도록 확인)

## 2026-09-25 계획 1 완료 — 엔진 쪼개기 (트랙 `숏템엔진`, 커밋 21c68343a..eb9d5ce12, **main 미병합**)
- 구조: `shopping_shorts/channelkit/`(엔진) + `shopping_shorts/channel_presets/brainbulb/`(1호: spec·fonts·steps·images·photos·photocheck·community) + 옛 경로 shim `shopping_shorts/brainbulb/__init__.py`.
- 채널 추가 = `channel_presets/<이름>/spec.py`에 `STEPS`·`LINT_RULES`·`STEP_HANDLERS`(fn(job,d,wd,kw)) + 규격 상수. `registry.use(name)` 또는 `run_step(channel=)`.
- 검증: 테스트 276 passed(brainbulb 243 + 레지스트리 7 + 채널로더 26) · `tools/channelkit/parity_check.py` 골든 5편 15항목 불일치 0(쪼개기 전 스냅샷 21c68343a 대비, 값 하나 바꾸면 rc=1 잡힘 확인) · `import shopping_shorts.app` OK.
- ⚠ 실제 렌더(mp4)는 이번에 안 돌렸다 — 동일성은 린터·줄나눔·ASS 해시까지. 렌더 경로 코드는 안 바뀌었지만 실측은 계획 2 첫 편에서.

### 함정 (재발 방지)
- `shopping_shorts/channels.py`(벤치마킹 엑셀 채널 로더, app.py 등 7곳 import)가 있어 패키지 이름 `channels/`는 쓰면 앱이 죽는다 → `channel_presets/`.
- `channel_presets/brainbulb/steps.py`는 import를 **함수 안에서** 한다 — 맨 위에 두면 steps→pipeline→lint→spec 프록시→brainbulb.spec→steps 순환.
- 쪼개며 상대 import 4곳이 깨질 뻔했다. 특히 `channelkit/frames._face_focus`의 `from . import photos`는 except에 먹혀 **얼굴 중심 크롭이 조용히 꺼질 뻔**.

### 남은 부채 (계획 2에서)
- 엔진이 채널을 직접 부르는 곳 2개: `channelkit/frames.py`(얼굴 검출 photos.face_box) · `channelkit/make.py`(images). 얼굴 검출은 공용으로 옮길 것(뜨거운사람들도 필요).
- job.json에 채널 이름이 저장 안 됨 → 재개 시 `channel=`을 다시 넘겨야 함. setup 때 기록하도록.
- main 병합(finish)은 **사장님 승인 후** — brainbulb API·`/brainbulb` 페이지가 라이브로 나간다.

## ⏭ 다음: 계획 2 — 뜨거운사람들 20편 실측 → `channel_presets/hotpeople/spec.py` + footage 단계

## 2026-09-25 계획 2 착수 — 뜨거운사람들 원본 10편 실측 완료
- 결과: `channel/hotpeople/역분석_2026-09-25.md` · 도구: 트랙 `tools/hotpeople/measure/` (표본 mp4는 scratchpad, 커밋 안 함)
- 핵심: 틀 A(흰 바탕) 9/10 — 슬롯 1080×790 @ y483 고정, 자막 y≈1285 2줄 중심, 자막 1개 ≈ 2.2초 ≈ 컷 1개, 나레 없음, 첫 자막 형광펜 9/9, "하지만 그는 달랐음." 전환 3/6편.
- 틀 B(검정 바탕·존댓말) 1편은 제외. 헤드라인이 9~23초 뒤 사라지는 편 4/10.
- 미확정: 자막 글꼴(주아 IoU 0.638 1위지만 원본이 더 굵음) · BGM · 슬롯 크롭 방식 · 글자수↔자막 길이 관계.
## ⏭ 다음: 계획 2 문서화 — channel_presets/hotpeople/spec.py + 린터 규칙 + footage 단계

## 2026-09-25 새벽 계획 2 — 뜨거운사람들 채널 첫 편 완성 (트랙 `숏템엔진`, **main 미병합**)
- 완성본: `.tracks/숏템엔진/out/hotpeople/안세영/out/final.mp4` (57.5초·26컷·1080×1920) · 바탕화면 `숏템엔진_뜨거운사람들틀_안세영_v001.mp4` · 원본 대조 `out/compare.png` · 검수 시트 `out/review.png`
- 실행: `py -m shopping_shorts.channel_presets.hotpeople.make --seed "인물 | 주제" --workdir out/hotpeople/<이름>` (`--from <단계>` 재개)
- 단계: setup → research(위키 ko+en) → script(클로드, 규칙 11개) → footage(유튜브 검색·앞 8분 영상만·장면 자르기·**시트별 설명 → 글 대조**·검사 후 틀린 칸 재선택) → render(PIL 판+ffmpeg, 소스 아래 18% 잘라 박힌 자막 제거) → review(mp4 실측)
- 검증: 테스트 272 passed(hotpeople 19 포함) · 뇌전구 동일성 불일치 0 · app import OK · 좌표 테스트는 원본 실측 숫자(1285/80)로 — spec끼리 비교하던 헛검사를 사보타주로 잡음

### 함정 (재발 방지)
- 120칸 시트 4장 + 자막 26개를 한 번에 주면 제미니 두 모델 모두 **번호를 순서대로 찍는다**(10,11,12…). 그림은 한 장씩 "무엇이 보이나"만, 짝짓기는 글로.
- 3.1-flash-lite는 번호를 **문자열**("108")로 준다 · 503 과부하 잦음 → `_call`이 재시도·대체 모델(2.5-flash)·**답 모양(key) 확인**. 모양 확인 없을 땐 엉뚱한 답을 받아 26/26 못 골랐다.
- 모델이 못 골라 30% 넘게 순서대로 메우면 **멈춘다**(조용히 완료 금지).
- 주아체에 가운뎃점(·) 없음 → □. `hp_glyphs` 규칙이 대본에서 반려.
- `--download-sections`는 한 편 4~5분 — 한 편 8영상 상한(`POLICY_FOOTAGE_MAX_VIDEOS`), 받아 둔 영상 재사용.
- ⚠ 03:30 C: 드라이브 0바이트 — 주범은 Claude 데스크톱 VM `rootfs.vhdx`(8.3GB, 계속 커짐). 내가 `%TEMP%\pytest-of-CH` 를 지워 2.1GB 확보(다른 세션 테스트 임시 폴더도 같이 지웠을 수 있음).

### 남은 것
- 26컷 중 7컷에 소스의 작은 자막 한 줄 잔존(아래 18% 위쪽) — 장면 설명의 [TEXT] 표시를 짝짓기에서 더 강하게 피하거나 자막 위치 검출 후 크롭
- BGM 없음(무음) — `HOTPEOPLE_BGM_DIR` 폴더 지정 필요. 원본 BGM 음량 미측정
- 자막 글꼴 굵기 미확정(주아 + 외곽선 1px) · 헤드라인이 원본보다 굵어 보임
- 엔진→채널 직접 참조 2곳(frames 얼굴검출·make images)
- main 병합(라이브)은 사장님 승인 후

## 2026-09-25 배경음악 — 원본 곡 찾기 완료
- Shazam 인식(shazamio, 프로젝트 .venv 3.12에 설치 — 3.14는 빌드 실패) 원본 10편 × 3구간: **4곡뿐**. Hero(Bonnie Tyler) 3 · Unstoppable(Sia) 3 · The Final Countdown(Europe) 2 · 버릇처럼 셋을 센다(KCM) 1 · 검정틀 1편 인식 안 됨.
- 교차상관으로 곡 속 시작초 실측: Hero 7.1s(3편 7.03~7.34, 상관 0.8) · Unstoppable 10.4s · Final Countdown 30~38s · KCM 94.5s(후렴). 속도 변경 없음(1s·40s 오프셋 동일).
- 음량: 원본 통합음량 중앙 **-12.1 LUFS**(내 잠정값 -24는 틀렸었다). spec `BGM_TRACKS`·`BGM_LUFS`, 곡 파일은 `.tracks/숏템엔진/out/hotpeople/bgm/`(gitignore).
- 안세영 편 재렌더: KCM 곡이 뽑힘(3:3:2:1 가중 추첨), -13.3 LUFS, 완성본 Shazam으로 곡 확인. 바탕화면 `숏템엔진_뜨거운사람들틀_안세영_v002_음악.mp4`
