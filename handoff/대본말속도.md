# 대본말속도 — 조사와 수정 (2026-09-18)

> 소유 트랙: 대본말속도. 이 파일은 이 트랙 세션만 수정한다.
> 앞선 기록: `handoff/대본게이트.md`(09-16 같은 증상, 되돌려짐) · `handoff/대본안수.md`(08-31)
> · `handoff/대본교정출구.md`(09-16) · `handoff/대본소재이탈2.md`(09-11)

## 사장님 제보의 결말: **대본 개수는 시스템 문제가 아니었다**

박복래(cid 205) work `4124ceb3a88a` 실측 — DB에 **대본 2개가 정상 저장돼 있다**:

```
picked = [54, 59]   저장된 drafts = 2
  [0] 물건 발견형  190자   passed=False
  [1] 사회증거형  284자   passed=False
```

화면(`produce.html:18563`)은 `S2.drafts` 전부를 탭으로 그린다 → 2개면 A안·B안 두 탭.
현행 라이브 코드로 같은 재료를 4번 돌렸고 **매번 2안**이 나왔다.

★**「말 밀도」는 치명 검사가 아니다.** `FATAL_CHECKS`는 소재 일치·주제 단일성·재료 밖 판매처·사실 근거뿐.
`fatal_fail` 주석: *재시도 뒤에도 통과 못 하면 **길이가 가장 가까운 안을 그냥 내보낸다**(fail-open)*.
즉 **길이로는 안이 죽지 않는다** → "2개 골랐는데 1개"의 원인이 길이라는 내 가설은 틀렸다.

남은 가능성(확인 못 함): 화면에서 탭을 못 보셨거나 / 23:24 첫 시도 상태는 23:28 새로고침으로
덮여 DB에 안 남음 / 다른 화면을 말씀하신 것.

## 내가 틀린 것 — 네 번 (전부 실측으로 기각)

| # | 주장 | 실측 |
|---|---|---|
| 1 | 커밋 `fa36afe3b`의 N→1 폴백이 범인 | `made_by=None` — 폴백 안 탔음 |
| 2 | 천장(cap)을 두 번 쓰는 구조 | 시뮬레이션 +0.0%p로 완전 기각 |
| 3 | **말속도 실측이 9.69인데 7.41로 잘못 안다** | **실제 TTS 318비트 측정 = 7.42.** 현행이 맞음 |
| 4 | 말 밀도가 1위 사인 | 치명 아님 · fail-open |

★3번: `edit_plan.py` **주석에 적힌 9.69를 검증 없이 믿었다.** 0순위 위반.
말속도 변경 커밋 `8b9320aa5`는 `d6dc91725`로 **되돌렸다.**

## 그래도 찾은 진짜 결함 — narr_secs 단위 어긋남 (커밋 `70f50d684`)

분모 7.41자/초는 **norm(공백 제외)** 기준인데 분자만 `len(text.strip())`으로 **공백을 셌다.**
2026-08-24 `4bad94421`가 script_gate를 통일할 때 **이 함수가 빠졌다**
(메모리 `reference_길이단위_raw_norm_혼용`이 경고한 그 병).

실측(라이브 완성 job 25개·비트 210개, TTS 실파일 길이 대조):

| | 전 | 후 |
|---|---|---|
| 계획 vs 실제 | 791초 vs 608초 = **30% 과대** | 614초 vs 608초 = **1% 과대** |
| 부풀려진 비트 | **197/210 (94%)** | 20/210 (10%) |
| 렌더가 대본 없이 채울 화면 | **183초** (job당 7.3초) | **5초** |

가드 2개 신설 + 사보타주 검증 완료. 관련 802 passed.
(`test_s2_review_fallback_ui` 1건 실패는 **기준선** — 내 변경을 stash하면 오히려 2건 실패)

## 다음 할 일

- [ ] **`finish` 안 했다.** 컷 배치가 바뀌는 변경이라 **실제 렌더 영상을 눈으로 본 뒤** 판단받는다.
      (0순위-A1. 지금 검증은 "계획 초"까지다 — 영상은 아직 안 봤다)
- [ ] 박복래님께 화면 확인: "대본 결과에 A안/B안 탭 두 개가 보이시나요?"

## 파다가 확인된 별건 (전부 실측, 아직 안 고침)

- 검사 19개 중 **5개는 한 번도 안 걸렸다** — 사람이 나온다 · 쓰임이 번진다 · 재료 밖 판매처 ·
  훅 3초 서론금지 · 훅 3초
- **「사실 근거」는 최종 사망 0건**인데 09-14~16 사흘간 긴급 커밋 8개가 거기 매달렸다
- 재작성 4회까지 간 안이 701개, 그중 **53.5%가 끝내 실패** = "엄청 늦다"의 정체
- `_trim_to_budget` 재단 성공률 **4%** — 부사 27개만 지운다(목표 도달 보장 없음)
- DB `chars_per_30s`가 실제와 안 맞는다 — 실측 중앙 138~192자인데 DB는 73~241 주장
- **서버 디스크가 100%였다**(내가 만든 `/tmp/newgate` 248G). 삭제해 61%로 복구.
  01:59~02:03 유튜브 쿠키 사본·핀터레스트 크롤 실패 → 메모리 `reference_서버tmp_전체복사금지`
