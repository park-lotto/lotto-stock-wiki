# TTS파형자르기

2026-09-22: TTS 패널에 **진짜 파형**(서버 ffmpeg 피크 120막대) + 무음 자르기를 넣었다 —
앞뒤 끝은 분홍 손잡이 드래그(`mode=set` 신설), 문장 중간 쉼은 노란 빗금 클릭 토글,
상단에 「✂ 무음 전부 자르기」 일괄 버튼. 렌더는 `assemble()` 진입부에서 `tts_paths`를
잘린 mp3로 갈아끼워 렌더·자막·캡컷 셋이 자동으로 따라오게 했다(세 곳에 같은 논리를 안 심는다).
`▶` 재생도 자른 결과를 그대로 들려주게 고쳤다(종전엔 늘 원본 전체를 틀어 "잘랐는데 그대로"였다).

★도중에 **무음 감지가 한 번도 동작한 적 없었음**을 발견 — `audio_post`가 ffmpeg stderr(UTF-8)를
cp949로 디코드하다 리더 스레드에서 죽고 `except`가 삼켜, 한글 경로에서 늘 `[]`/`0.0`을 반환했다.
`video_assemble`이 2026-07-16에 이미 고친 함정(`_FF_TEXT`)을 이 파일만 못 받은 것(0순위-B).
기존 테스트가 **가짜 stderr 문자열**만 파싱해 검사해서 영영 못 잡았다. → `_FF_TEXT` 적용 3곳 +
전수 도구 `tools/audit_subprocess_encoding.py` + 진짜 mp3로 도는 회귀 테스트.
두 검사 모두 **고치기 전 코드에서 실패하는지 먼저 시험**했다(도구 3건 rc=1 / 테스트 2 failed).

실측이 설계를 바꿨다: 앞뒤 끝 무음 **0.00초**(pace_mode가 이미 걷어냄) / 중간 **12구간 2.59초**
→ 끝만 자르는 A안은 이 환경에서 아무 일도 안 한다. 그래서 중간 자르기(B)까지 구현.

검증: 브라우저 실기동 12항목 OK(JS오류 0, 드래그 0→0.861 서버 일치) / **실제 렌더 29.05→27.13초,
완성본 무음 2.48→0.33초**(결과물을 직접 쟀다) / 자막은 컷 뒤만 정확히 당겨짐(누적오차 0) /
전체 27 failed·8055 passed 중 겹치는 3건은 stash 대조로 **기존 실패** 확인(회귀 아님).

커밋 `65fa2b70f` origin 푸시 완료. **main 미병합 = 라이브 영향 없음**(승인 대기, 0순위-A1c).
다음: 사장님이 `out/무음자르기_후.mp3`를 듣고 숨 `_GAP_KEEP`(0.06초, `audio_post.py:334`) 판정.
