# 소스펼치기 — 장면편집 영상소스 '다 보기 ↔ 줄여 보기' (2026-09-18, 라이브)

사장님: "하단 영상소스가 원래 펼쳐져 있었는데 지금은 2개만 보인다 — 접기/펼치기 버튼으로 다 보이게".
원인: 09-14 `120a84526`이 목록 높이를 화면에 맞춰 묶고(`fitPalette` → `--pal-h`, 최소 600px) 안에서 스크롤하게 바꿈.
처방: 툴바에 `⤓ 영상소스 다 보기`(`togglePalAll`) — 켜면 `--pal-h:none`, 부모 produce는 0.7초마다 body 높이로 iframe을 늘림.
선택은 localStorage `sceneLab:palAll` 기억. main `03a2634a5`.
라이브 실측(관리자 작업 b08ef18a6994, 소스 8묶음): 단독 화면 보이는 묶음 2→8 / 제작소 안 iframe 1,489→4,264px,
목록 안쪽 스크롤 사라짐, 다시 누르면 600px로 복귀. 시험 중 자동저장은 fetch 가로채 차단.
