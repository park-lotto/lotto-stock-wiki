# 장중 시황 브리핑 엔진 (Phase 0: 관측·보정) 구현 계획

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 이벤트 감지 기반 장중 시황 브리핑 엔진의 Phase 0(관측·보정) 파이프라인을 구축해, 실데이터로 며칠 돌리며 잡힘/노이즈를 운영자가 피드백·튜닝할 수 있게 한다.

**Architecture:** 대시보드 서버(`dashboard/server.py`)의 30초 폴링 루프에서, ① 세션페이즈 판정 → ② market_flow+heatmap 스냅샷 정규화 → ③ 이벤트 디텍터(순수 로직) → ④ 발동 시 종합엔진(`claude -p` Opus/Sonnet 번갈아 + Gemini 폴백) → ⑤ 스토리상태 영속 + 라이브 패널 반영 + 텔레그램 다이제스트. 순수 로직 모듈은 TDD, 서버 통합은 실호출 스모크테스트로 검증.

**Tech Stack:** Python 3.12, FastAPI(기존 dashboard), pytest, `claude` CLI 헤드리스(Max 구독 인증, 서버 설치·검증 완료), 기존 `kis_api`/`global_api`/`sector_heatmap`/`_gemini_text`/telegram sendMessage.

## Global Constraints

- 단위: 투자자/프로그램 수급은 **백만원**. 임계 상수는 이름에 단위 명시(`_MW` 접미사 = 백만원).
- 모든 디텍터는 **첫런(baseline=None)·데이터동결·전필드0·거래일전환**을 반드시 가드.
- 종합엔진은 서버에 `ANTHROPIC_API_KEY` **없어야** Max 구독 인증 사용(API 과금 방지). 전용 최소 작업디렉토리 `/home/ubuntu/briefing_agent/`에서 `claude -p` 실행.
- 임계 상수 초기값(spec §5.2 그대로): `MIN_INVESTOR_TURN_MW=30_000`, `PROG_JUMP_MW=50_000`, `PROG_FLIP_MIN_MW=30_000`, `IDX_NEWHIGH_MARGIN=0.0005`, `IDX_REBOUND_PCT=0.006`, `IDX_PULLBACK_PCT=0.006`, `NQ_DELTA_PP=0.4`, `NQ_ABS_LEVELS=[1.0,2.0]`, `DECOUPLE_MIN_SLOPE_PP=0.15`, `SECTOR_SURGE_PP=2.0`, `SECTOR_LEAD_ABS=3.0`, `EVENT_RECOOLDOWN_S=600`.
- 테스트는 `dashboard/`를 sys.path에 넣는 기존 `conftest.py` 규칙을 따른다. 새 테스트는 `tests/test_briefing_*.py`.
- 커밋은 태스크 끝에서. 브랜치는 `main` 직접 금지 — `git checkout -b feat/briefing-engine` 먼저(커밋 전 `git branch --show-current`로 확인).
- spec: `docs/superpowers/specs/2026-07-04-시황-브리핑-엔진-design.md`

---

## 파일 구조

- `dashboard/briefing_phase.py` (신규) — 세션 페이즈 판정. 순수 함수.
- `dashboard/briefing_events.py` (신규) — 스냅샷 정규화 + 6종 디텍터 + 노이즈 플래그. 순수 로직.
- `dashboard/briefing_weather.py` (신규) — 종합엔진: 프롬프트 빌드 + `claude -p` 호출 + JSON 파싱 + Gemini 폴백.
- `dashboard/briefing_digest.py` (신규) — 텔레그램 다이제스트(major 즉시 / minor 15분 배치) + 일일요약 포매팅·전송.
- `dashboard/briefing_state.py` (신규) — 오늘의 스토리 상태 load/save/reset(`output/weather_state.json`).
- `dashboard/server.py` (수정) — `_poll_briefing` 내 `_insight_run_synthesis` 경로를 새 오케스트레이터로 교체 + `/api/market_briefing` insight 확장.
- `dashboard/market.html` (수정) — "실시간 브리핑" 패널을 배지+내러티브+전환점으로 교체.
- `tests/test_briefing_phase.py`, `tests/test_briefing_events.py`, `tests/test_briefing_weather.py`, `tests/test_briefing_digest.py`, `tests/test_briefing_state.py` (신규).
- 서버: `/home/ubuntu/briefing_agent/`(claude -p 전용 최소 cwd).

---

## 브랜치 준비 (모든 태스크 전 1회)

- [ ] **Step 1: 브랜치 생성**

```bash
cd "/c/Users/TheRose/Desktop/로또의 주식"
git checkout -b feat/briefing-engine
git branch --show-current   # feat/briefing-engine 확인
```

---

## Task 1: 세션 페이즈 판정 (`briefing_phase.py`)

**Files:**
- Create: `dashboard/briefing_phase.py`
- Test: `tests/test_briefing_phase.py`

**Interfaces:**
- Produces: `session_phase(now: datetime) -> str` — 반환값 ∈ `{"premarket","intraday","closed","weekend"}`.

- [ ] **Step 1: 실패 테스트 작성**

```python
# tests/test_briefing_phase.py
import datetime as dt
from briefing_phase import session_phase

def test_weekend_saturday():
    assert session_phase(dt.datetime(2026, 7, 4, 14, 0)) == "weekend"   # 토요일

def test_premarket():
    assert session_phase(dt.datetime(2026, 7, 6, 8, 20)) == "premarket"  # 월 08:20

def test_intraday():
    assert session_phase(dt.datetime(2026, 7, 6, 11, 0)) == "intraday"   # 월 11:00

def test_closed():
    assert session_phase(dt.datetime(2026, 7, 6, 16, 0)) == "closed"     # 월 16:00

def test_early_morning_is_closed():
    assert session_phase(dt.datetime(2026, 7, 6, 6, 0)) == "closed"      # 월 06:00 (장전 이전)
```

- [ ] **Step 2: 실패 확인**

Run: `python -m pytest tests/test_briefing_phase.py -v`
Expected: FAIL — `ModuleNotFoundError: No module named 'briefing_phase'`

- [ ] **Step 3: 구현**

```python
# dashboard/briefing_phase.py
"""장중 시황 브리핑 — 세션 페이즈 판정. 시각(+요일)으로 프레이밍을 결정한다."""
import datetime as _dt


def session_phase(now: _dt.datetime) -> str:
    """반환: premarket|intraday|closed|weekend. 한국 장 기준(평일 09:00~15:30).
    premarket 08:00~08:50, intraday 08:50~15:30, 그 외 평일은 closed, 주말은 weekend."""
    if now.weekday() >= 5:          # 5=토, 6=일
        return "weekend"
    hm = now.hour * 60 + now.minute
    if 8 * 60 <= hm < 8 * 60 + 50:
        return "premarket"
    if 8 * 60 + 50 <= hm < 15 * 60 + 30:
        return "intraday"
    return "closed"
```

- [ ] **Step 4: 통과 확인**

Run: `python -m pytest tests/test_briefing_phase.py -v`
Expected: PASS (5 passed)

- [ ] **Step 5: 커밋**

```bash
git add dashboard/briefing_phase.py tests/test_briefing_phase.py
git commit -m "feat(briefing): 세션 페이즈 판정(premarket/intraday/closed/weekend)"
```

---

## Task 2: 스냅샷 정규화 (`briefing_events.py` — build_snapshot)

market_flow 캐시 dict + heatmap sectors 리스트를 디텍터가 쓸 정규화 snap으로 변환.

**Files:**
- Create: `dashboard/briefing_events.py`
- Test: `tests/test_briefing_events.py`

**Interfaces:**
- Consumes: market_flow dict(키 `J_price,Q_price,J_investor,Q_investor,J_prog,Q_prog,NQ` — 각 `kis_api`/`global_api` 반환형), heatmap sectors `[{name, avg_rate, stocks:[{name,code,change_rate,price}]}]`, index bars `[{t,price}]`.
- Produces: `build_snapshot(mf: dict, sectors: list, j_bars: list, q_bars: list, ts: str) -> dict` — spec §5.1 snap 구조.

- [ ] **Step 1: 실패 테스트 작성**

```python
# tests/test_briefing_events.py
from briefing_events import build_snapshot

def _mf():
    return {
        "J_price": {"price": 8088.0, "change_rate": 5.76},
        "Q_price": {"price": 868.0, "change_rate": 0.19},
        "J_investor": {"외인": -220000, "기관": 440000},   # 백만원
        "Q_investor": {"외인": -20200, "기관": -103700},
        "J_prog": {"합계": -936700}, "Q_prog": {"합계": -92200},
        "NQ": {"change_rate": 1.10},
    }

def test_build_snapshot_shape():
    bars = [{"t": "090000", "price": 8000.0}, {"t": "094500", "price": 7950.0},
            {"t": "140000", "price": 8088.0}]
    snap = build_snapshot(_mf(), [{"name": "반도체", "avg_rate": 3.4, "stocks": []}],
                          bars, bars, ts="14:00")
    assert snap["idx"]["J"]["price"] == 8088.0
    assert snap["idx"]["J"]["rate"] == 5.76
    assert snap["hi_lo"]["J"]["day_high"] == 8088.0
    assert snap["hi_lo"]["J"]["day_low"] == 7950.0
    assert snap["inv"]["J"]["외인"] == -220000
    assert snap["prog"]["J"] == -936700
    assert snap["nq"]["rate"] == 1.10
    assert snap["sect"]["반도체"] == 3.4

def test_build_snapshot_missing_fields_safe():
    snap = build_snapshot({}, [], [], [], ts="09:00")
    assert snap["idx"]["J"]["price"] == 0
    assert snap["hi_lo"]["J"]["day_high"] == 0
    assert snap["sect"] == {}
```

- [ ] **Step 2: 실패 확인**

Run: `python -m pytest tests/test_briefing_events.py -v`
Expected: FAIL — `ModuleNotFoundError: No module named 'briefing_events'`

- [ ] **Step 3: 구현**

```python
# dashboard/briefing_events.py
"""장중 시황 브리핑 — 스냅샷 정규화 + 이벤트 디텍터(순수 로직, AI 없음)."""

# ── 임계 상수 (단위 명시) ──────────────────────────────
MIN_INVESTOR_TURN_MW = 30_000   # 300억 (백만원)
PROG_JUMP_MW         = 50_000   # 500억
PROG_FLIP_MIN_MW     = 30_000   # 300억
IDX_NEWHIGH_MARGIN   = 0.0005   # 0.05%
IDX_REBOUND_PCT      = 0.006    # 저점 대비 +0.6%p
IDX_PULLBACK_PCT     = 0.006    # 고점 대비 -0.6%p
NQ_DELTA_PP          = 0.4
NQ_ABS_LEVELS        = [1.0, 2.0]
DECOUPLE_MIN_SLOPE_PP = 0.15
SECTOR_SURGE_PP      = 2.0
SECTOR_LEAD_ABS      = 3.0


def _f(v, d=0.0):
    try:
        return float(v)
    except Exception:
        return d


def build_snapshot(mf: dict, sectors: list, j_bars: list, q_bars: list, ts: str) -> dict:
    """market_flow 캐시 + heatmap sectors + 지수 bars → 디텍터용 정규화 snap."""
    mf = mf or {}

    def _idx(k):
        p = mf.get(k) or {}
        return {"price": _f(p.get("price")), "rate": _f(p.get("change_rate"))}

    def _hilo(bars):
        prices = [_f(b.get("price")) for b in (bars or []) if _f(b.get("price")) > 0]
        if not prices:
            return {"day_high": 0.0, "day_low": 0.0}
        return {"day_high": max(prices), "day_low": min(prices)}

    def _inv(k):
        d = mf.get(k) or {}
        return {"외인": _f(d.get("외인")), "기관": _f(d.get("기관"))}

    def _prog(k):
        d = mf.get(k) or {}
        return _f(d.get("합계"))

    return {
        "ts": ts,
        "idx":   {"J": _idx("J_price"), "Q": _idx("Q_price")},
        "hi_lo": {"J": _hilo(j_bars), "Q": _hilo(q_bars)},
        "inv":   {"J": _inv("J_investor"), "Q": _inv("Q_investor")},
        "prog":  {"J": _prog("J_prog"), "Q": _prog("Q_prog")},
        "nq":    {"rate": _f((mf.get("NQ") or {}).get("change_rate"))},
        "sect":  {s.get("name"): _f(s.get("avg_rate")) for s in (sectors or []) if s.get("name")},
    }
```

- [ ] **Step 4: 통과 확인**

Run: `python -m pytest tests/test_briefing_events.py -v`
Expected: PASS (2 passed)

- [ ] **Step 5: 커밋**

```bash
git add dashboard/briefing_events.py tests/test_briefing_events.py
git commit -m "feat(briefing): market_flow+heatmap 스냅샷 정규화"
```

---

## Task 3: 이벤트 디텍터 — 수급·프로그램 (`briefing_events.py` — detect_flow)

**Files:**
- Modify: `dashboard/briefing_events.py`
- Test: `tests/test_briefing_events.py`

**Interfaces:**
- Produces: `detect_flow(prev: dict, curr: dict) -> list[dict]` — 투자자 부호전환 + 프로그램 급변 이벤트. 각 이벤트 `{"type","market","label","major","value"}`.
- `sign(x)` 헬퍼는 이 파일 내부 정의.

- [ ] **Step 1: 실패 테스트 추가**

```python
# tests/test_briefing_events.py 에 추가
from briefing_events import detect_flow

def _snap(inv_j_ext=0, prog_j=0):
    return {"ts": "11:00",
            "inv": {"J": {"외인": inv_j_ext, "기관": 0}, "Q": {"외인": 0, "기관": 0}},
            "prog": {"J": prog_j, "Q": 0}}

def test_investor_sign_flip_fires():
    prev = _snap(inv_j_ext=-50000)   # 순매도 500억
    curr = _snap(inv_j_ext=+40000)   # 순매수 400억 (부호전환, 300억↑)
    evs = detect_flow(prev, curr)
    assert any(e["type"] == "investor_flip" and e["market"] == "J" for e in evs)

def test_investor_flip_too_small_ignored():
    prev = _snap(inv_j_ext=-50000)
    curr = _snap(inv_j_ext=+10000)   # 순매수 100억 (300억 미만) → 무시
    assert not any(e["type"] == "investor_flip" for e in detect_flow(prev, curr))

def test_investor_first_run_silent():
    assert detect_flow(None, _snap(inv_j_ext=+40000)) == []

def test_prog_jump_fires():
    prev = _snap(prog_j=0)
    curr = _snap(prog_j=+60000)      # +600억 급증
    assert any(e["type"] == "prog_shift" for e in detect_flow(prev, curr))

def test_prog_small_move_ignored():
    prev = _snap(prog_j=0)
    curr = _snap(prog_j=+10000)      # +100억 (500억 미만, 부호전환 아님)
    assert not any(e["type"] == "prog_shift" for e in detect_flow(prev, curr))
```

- [ ] **Step 2: 실패 확인**

Run: `python -m pytest tests/test_briefing_events.py -k "flow or investor or prog" -v`
Expected: FAIL — `cannot import name 'detect_flow'`

- [ ] **Step 3: 구현 추가**

```python
# dashboard/briefing_events.py 에 추가
def _sign(x):
    return (x > 0) - (x < 0)


def detect_flow(prev: dict, curr: dict) -> list[dict]:
    """투자자 부호전환 + 프로그램 급변. 첫런(prev=None)은 침묵."""
    if not prev:
        return []
    out = []
    for mkt in ("J", "Q"):
        for who in ("외인", "기관"):
            p = (prev.get("inv", {}).get(mkt, {}) or {}).get(who, 0.0)
            c = (curr.get("inv", {}).get(mkt, {}) or {}).get(who, 0.0)
            if p and c and _sign(p) != _sign(c) and abs(c) >= MIN_INVESTOR_TURN_MW:
                side = "순매수" if c > 0 else "순매도"
                nm = "코스피" if mkt == "J" else "코스닥"
                out.append({"type": "investor_flip", "market": mkt, "major": True,
                            "value": c,
                            "label": f"{nm} {who} {('순매도→' if c>0 else '순매수→')}{side} 전환"})
        p = prev.get("prog", {}).get(mkt, 0.0)
        c = curr.get("prog", {}).get(mkt, 0.0)
        flip = (p and c and _sign(p) != _sign(c) and abs(c) >= PROG_FLIP_MIN_MW)
        jump = abs(c - p) >= PROG_JUMP_MW
        if flip or jump:
            nm = "코스피" if mkt == "J" else "코스닥"
            eok = round(c / 100)   # 백만원 → 억
            out.append({"type": "prog_shift", "market": mkt, "major": flip,
                        "value": c, "label": f"{nm} 프로그램 순매수 {eok:+,}억"})
    return out
```

- [ ] **Step 4: 통과 확인**

Run: `python -m pytest tests/test_briefing_events.py -k "flow or investor or prog" -v`
Expected: PASS (5 passed)

- [ ] **Step 5: 커밋**

```bash
git add dashboard/briefing_events.py tests/test_briefing_events.py
git commit -m "feat(briefing): 디텍터 - 투자자 부호전환+프로그램 급변"
```

---

## Task 4: 이벤트 디텍터 — 지수 분기점 (`briefing_events.py` — detect_index)

**Files:**
- Modify: `dashboard/briefing_events.py`
- Test: `tests/test_briefing_events.py`

**Interfaces:**
- Produces: `detect_index(prev: dict, curr: dict, legstate: dict) -> list[dict]` — 신고/신저/반등/되돌림. `legstate`(mutable dict)로 leg당 1회 발동 추적. `{"type","market","label","major"}`.

- [ ] **Step 1: 실패 테스트 추가**

```python
# tests/test_briefing_events.py 에 추가
from briefing_events import detect_index

def _isnap(price, hi, lo):
    return {"ts":"11:00","idx":{"J":{"price":price,"rate":0}, "Q":{"price":0,"rate":0}},
            "hi_lo":{"J":{"day_high":hi,"day_low":lo}, "Q":{"day_high":0,"day_low":0}}}

def test_index_new_high():
    prev = _isnap(8000, 8000, 7900)
    curr = _isnap(8010, 8010, 7900)   # 신고가(직전 고점 8000 돌파)
    assert any(e["type"]=="index_newhigh" for e in detect_index(prev, curr, {}))

def test_index_rebound_once_per_leg():
    leg = {}
    prev = _isnap(7900, 8000, 7900)
    curr = _isnap(7948, 8000, 7900)   # 저점7900 대비 +0.61% → 반등
    first = detect_index(prev, curr, leg)
    assert any(e["type"]=="index_rebound" for e in first)
    # 같은 leg에서 재발동 안 함
    again = detect_index(curr, _isnap(7950, 8000, 7900), leg)
    assert not any(e["type"]=="index_rebound" for e in again)

def test_index_first_run_silent():
    assert detect_index(None, _isnap(8000,8000,7900), {}) == []
```

- [ ] **Step 2: 실패 확인**

Run: `python -m pytest tests/test_briefing_events.py -k index -v`
Expected: FAIL — `cannot import name 'detect_index'`

- [ ] **Step 3: 구현 추가**

```python
# dashboard/briefing_events.py 에 추가
def detect_index(prev: dict, curr: dict, legstate: dict) -> list[dict]:
    """지수 신고/신저/반등/되돌림. legstate로 leg당 1회. 첫런 침묵."""
    if not prev:
        return []
    out = []
    for mkt in ("J", "Q"):
        c = curr.get("idx", {}).get(mkt, {}) or {}
        hl_prev = prev.get("hi_lo", {}).get(mkt, {}) or {}
        price = c.get("price", 0.0)
        if price <= 0:
            continue
        nm = "코스피" if mkt == "J" else "코스닥"
        ph, pl = hl_prev.get("day_high", 0.0), hl_prev.get("day_low", 0.0)
        if ph > 0 and price > ph * (1 + IDX_NEWHIGH_MARGIN):
            out.append({"type": "index_newhigh", "market": mkt, "major": False,
                        "label": f"{nm} 장중 신고가 경신"})
            legstate[f"{mkt}_leg"] = "up"
        elif pl > 0 and price < pl * (1 - IDX_NEWHIGH_MARGIN):
            out.append({"type": "index_newlow", "market": mkt, "major": False,
                        "label": f"{nm} 장중 신저가 경신"})
            legstate[f"{mkt}_leg"] = "down"
        # 반등: 저점 대비 +REBOUND, leg가 아직 rebound 발동 안 함
        if pl > 0 and price >= pl * (1 + IDX_REBOUND_PCT) and legstate.get(f"{mkt}_reb") != pl:
            out.append({"type": "index_rebound", "market": mkt, "major": True,
                        "label": f"{nm} 저점 대비 +{IDX_REBOUND_PCT*100:.1f}% 반등, 낙폭 만회"})
            legstate[f"{mkt}_reb"] = pl
        # 되돌림: 고점 대비 -PULLBACK
        if ph > 0 and price <= ph * (1 - IDX_PULLBACK_PCT) and legstate.get(f"{mkt}_pb") != ph:
            out.append({"type": "index_pullback", "market": mkt, "major": True,
                        "label": f"{nm} 고점 대비 -{IDX_PULLBACK_PCT*100:.1f}% 되돌림, 상승분 반납"})
            legstate[f"{mkt}_pb"] = ph
    return out
```

- [ ] **Step 4: 통과 확인**

Run: `python -m pytest tests/test_briefing_events.py -k index -v`
Expected: PASS (3 passed)

- [ ] **Step 5: 커밋**

```bash
git add dashboard/briefing_events.py tests/test_briefing_events.py
git commit -m "feat(briefing): 디텍터 - 지수 신고/신저/반등/되돌림(leg당 1회)"
```

---

## Task 5: 이벤트 디텍터 — 미선물·디커플링·섹터 (`briefing_events.py`)

**Files:**
- Modify: `dashboard/briefing_events.py`
- Test: `tests/test_briefing_events.py`

**Interfaces:**
- Produces:
  - `detect_nq(prev, curr) -> list[dict]`
  - `detect_decouple(j_bars, nq_bars) -> list[dict]` — 우리지수 vs NQ 슬로프 부호 비교.
  - `detect_sector(prev, curr, sectors_full) -> list[dict]` — 섹터 급변 + top_movers/news 동봉. `sectors_full`은 heatmap 원본(stocks 포함).

- [ ] **Step 1: 실패 테스트 추가**

```python
# tests/test_briefing_events.py 에 추가
from briefing_events import detect_nq, detect_decouple, detect_sector

def test_nq_delta_fires():
    prev = {"nq": {"rate": 0.2}}
    curr = {"nq": {"rate": 0.7}}      # +0.5%p 변화
    assert any(e["type"]=="nq_move" for e in detect_nq(prev, curr))

def test_decouple_opposite_slope():
    j_bars  = [{"price":8000},{"price":7990},{"price":7980}]   # 하락
    nq_bars = [100.0, 100.3, 100.6]                            # 상승
    evs = detect_decouple(j_bars, nq_bars)
    assert any(e["type"]=="decouple" for e in evs)

def test_sector_surge_with_drivers():
    prev = {"sect": {"반도체": 1.0}}
    curr = {"sect": {"반도체": 3.2}}   # +2.2%p 급등
    full = [{"name":"반도체","avg_rate":3.2,
             "stocks":[{"name":"한미반도체","change_rate":8.2,"price":100},
                       {"name":"이수페타시스","change_rate":6.1,"price":100}]}]
    evs = detect_sector(prev, curr, full)
    surge = [e for e in evs if e["type"]=="sector_surge"]
    assert surge and surge[0]["sector"] == "반도체"
    assert surge[0]["top_movers"][0]["name"] == "한미반도체"

def test_sector_no_change_silent():
    assert detect_sector({"sect":{"반도체":3.0}}, {"sect":{"반도체":3.1}}, []) == []
```

- [ ] **Step 2: 실패 확인**

Run: `python -m pytest tests/test_briefing_events.py -k "nq or decouple or sector" -v`
Expected: FAIL — `cannot import name 'detect_nq'`

- [ ] **Step 3: 구현 추가**

```python
# dashboard/briefing_events.py 에 추가
def detect_nq(prev: dict, curr: dict) -> list[dict]:
    if not prev:
        return []
    p = prev.get("nq", {}).get("rate", 0.0)
    c = curr.get("nq", {}).get("rate", 0.0)
    out = []
    if abs(c - p) >= NQ_DELTA_PP:
        out.append({"type": "nq_move", "major": False,
                    "label": f"나스닥선물 {c:+.2f}% (직전 대비 {c-p:+.2f}%p)"})
    else:
        for lv in NQ_ABS_LEVELS:
            if (p < lv <= c) or (p > -lv >= c):
                out.append({"type": "nq_move", "major": True,
                            "label": f"나스닥선물 {c:+.2f}% (±{lv:.0f}% 돌파)"})
                break
    return out


def _slope(vals):
    """마지막 4개 값의 단순 기울기(첫→끝 변화율 %). 값 부족 시 0."""
    v = [x for x in (vals or [])[-4:] if x]
    if len(v) < 2 or not v[0]:
        return 0.0
    return (v[-1] - v[0]) / v[0] * 100


def detect_decouple(j_bars: list, nq_bars: list) -> list[dict]:
    """우리지수(bars: [{price}]) vs NQ(bars: [float]) 슬로프 부호 엇갈림."""
    js = _slope([_f(b.get("price")) for b in (j_bars or [])])
    ns = _slope([_f(x) for x in (nq_bars or [])])
    if abs(js) < DECOUPLE_MIN_SLOPE_PP or abs(ns) < DECOUPLE_MIN_SLOPE_PP:
        return []
    if _sign(js) == _sign(ns):
        return []
    if ns > 0 and js < 0:
        return [{"type": "decouple", "major": True,
                 "label": "미선물은 오르는데 코스피 약세 — 디커플링"}]
    return [{"type": "decouple", "major": True,
             "label": "미선물 약세인데 코스피 버팀 — 디커플링"}]


def detect_sector(prev: dict, curr: dict, sectors_full: list) -> list[dict]:
    """섹터 직전 대비 급변(+/-2%p) 감지 + top_movers 동봉. news 매칭은 오케스트레이터에서 붙임."""
    if not prev:
        return []
    ps = prev.get("sect", {}) or {}
    out = []
    full_by = {s.get("name"): s for s in (sectors_full or [])}
    for name, cav in (curr.get("sect", {}) or {}).items():
        pav = ps.get(name)
        if pav is None:
            continue
        delta = cav - pav
        if abs(delta) >= SECTOR_SURGE_PP:
            full = full_by.get(name, {})
            movers = sorted(
                [{"name": st.get("name"), "rate": _f(st.get("change_rate"))}
                 for st in (full.get("stocks") or []) if _f(st.get("price")) > 0],
                key=lambda m: m["rate"], reverse=True)[:3]
            arrow = "급등" if delta > 0 else "급락"
            out.append({"type": "sector_surge", "sector": name, "delta": round(delta, 1),
                        "avg": round(cav, 1), "major": True, "top_movers": movers,
                        "label": f"{name} 직전 대비 {delta:+.1f}%p {arrow}"})
    return out
```

- [ ] **Step 4: 통과 확인**

Run: `python -m pytest tests/test_briefing_events.py -k "nq or decouple or sector" -v`
Expected: PASS (4 passed)

- [ ] **Step 5: 커밋**

```bash
git add dashboard/briefing_events.py tests/test_briefing_events.py
git commit -m "feat(briefing): 디텍터 - 미선물·디커플링·섹터급변(근거동봉)"
```

---

## Task 6: 통합 디텍터 + 쿨다운 + 노이즈 플래그 (`briefing_events.py`)

**Files:**
- Modify: `dashboard/briefing_events.py`
- Test: `tests/test_briefing_events.py`

**Interfaces:**
- Produces:
  - `detect_all(prev, curr, sectors_full, j_bars, nq_bars, phase, state) -> list[dict]` — 페이즈별로 디텍터 선택 실행 + 이벤트타입 쿨다운(`state["cooldown"]` mutable, `state["leg"]`). weekend/closed에선 지수·수급 디텍터 끔.
  - `flag_noise(event, snaps_after) -> bool` — 발동 후 스냅샷들에서 지표 되돌림 여부.

- [ ] **Step 1: 실패 테스트 추가**

```python
# tests/test_briefing_events.py 에 추가
from briefing_events import detect_all

def _full_snap(**kw):
    base = {"ts":"11:00",
            "idx":{"J":{"price":kw.get("jp",8000),"rate":0},"Q":{"price":800,"rate":0}},
            "hi_lo":{"J":{"day_high":kw.get("jh",8000),"day_low":kw.get("jl",7900)},
                     "Q":{"day_high":800,"day_low":790}},
            "inv":{"J":{"외인":kw.get("jext",0),"기관":0},"Q":{"외인":0,"기관":0}},
            "prog":{"J":0,"Q":0}, "nq":{"rate":kw.get("nq",0)},
            "sect":kw.get("sect",{})}
    return base

def test_detect_all_cooldown_blocks_repeat():
    st = {"cooldown": {}, "leg": {}}
    prev = _full_snap(jext=-50000); curr = _full_snap(jext=+40000)
    first = detect_all(prev, curr, [], [], [], "intraday", st)
    assert any(e["type"]=="investor_flip" for e in first)
    # 같은 타입 즉시 재발동은 쿨다운으로 차단
    second = detect_all(curr, _full_snap(jext=-40000), [], [], [], "intraday", st)
    assert not any(e["type"]=="investor_flip" for e in second)

def test_detect_all_weekend_skips_flow():
    st = {"cooldown": {}, "leg": {}}
    prev = _full_snap(jext=-50000); curr = _full_snap(jext=+40000)
    evs = detect_all(prev, curr, [], [], [], "weekend", st)
    assert not any(e["type"]=="investor_flip" for e in evs)
```

- [ ] **Step 2: 실패 확인**

Run: `python -m pytest tests/test_briefing_events.py -k "detect_all" -v`
Expected: FAIL — `cannot import name 'detect_all'`

- [ ] **Step 3: 구현 추가**

```python
# dashboard/briefing_events.py 에 추가
import time as _time


def detect_all(prev, curr, sectors_full, j_bars, nq_bars, phase, state) -> list[dict]:
    """페이즈별 디텍터 실행 + 이벤트타입 쿨다운. state={'cooldown':{}, 'leg':{}}."""
    cooldown = state.setdefault("cooldown", {})
    leg = state.setdefault("leg", {})
    evs = []
    # weekend/closed: 시세 기반 디텍터 끔(뉴스만 트리거 — 오케스트레이터에서 처리)
    if phase in ("intraday", "premarket"):
        if phase == "intraday":
            evs += detect_flow(prev, curr)
            evs += detect_index(prev, curr, leg)
            evs += detect_sector(prev, curr, sectors_full)
        evs += detect_nq(prev, curr)
        evs += detect_decouple(j_bars, nq_bars)
    # 이벤트타입 쿨다운 (major는 강도 커지면 예외적으로 통과)
    now = _time.time()
    passed = []
    for e in evs:
        key = e.get("type") + ":" + str(e.get("market", "") or e.get("sector", ""))
        last = cooldown.get(key, 0)
        if now - last >= EVENT_RECOOLDOWN_S:
            cooldown[key] = now
            passed.append(e)
    return passed


def flag_noise(event: dict, snaps_after: list) -> bool:
    """발동 후 스냅샷들에서 지표가 임계 아래로 되돌아가거나 반대전환이면 True(의심노이즈).
    Phase0 관측용 근사치 — 최종 판단은 운영자."""
    if not snaps_after:
        return False
    t = event.get("type")
    if t == "investor_flip":
        mkt, val = event.get("market"), event.get("value", 0)
        for s in snaps_after:
            back = (s.get("inv", {}).get(mkt, {}) or {}).get("외인", 0)
            if val and _sign(back) != _sign(val):   # 곧바로 반대전환
                return True
    if t == "sector_surge":
        name = event.get("sector")
        base = event.get("avg", 0) - event.get("delta", 0)
        for s in snaps_after:
            if abs(s.get("sect", {}).get(name, base) - base) < SECTOR_SURGE_PP / 2:
                return True   # 급등분 절반 이상 반납
    return False
```

- [ ] **Step 4: 통과 확인**

Run: `python -m pytest tests/test_briefing_events.py -v`
Expected: PASS (전체 통과)

- [ ] **Step 5: 커밋**

```bash
git add dashboard/briefing_events.py tests/test_briefing_events.py
git commit -m "feat(briefing): 통합 디텍터+페이즈게이팅+쿨다운+노이즈플래그"
```

---

## Task 7: 스토리 상태 영속 (`briefing_state.py`)

**Files:**
- Create: `dashboard/briefing_state.py`
- Test: `tests/test_briefing_state.py`

**Interfaces:**
- Produces: `load_state(path) -> dict`, `save_state(path, state)`, `reset_state(date_str) -> dict`, `is_new_day(state, date_str) -> bool`. state 구조 spec §7.

- [ ] **Step 1: 실패 테스트 작성**

```python
# tests/test_briefing_state.py
import json, os
from briefing_state import load_state, save_state, reset_state, is_new_day

def test_reset_state_shape():
    st = reset_state("2026-07-06")
    assert st["date"] == "2026-07-06"
    assert st["turning_points"] == []
    assert st["verdict"] is None
    assert st["baseline"] is None

def test_save_and_load_roundtrip(tmp_path):
    p = str(tmp_path / "w.json")
    st = reset_state("2026-07-06")
    st["verdict"] = {"tone": "🟢", "line": "테스트", "ts": "11:00"}
    save_state(p, st)
    got = load_state(p)
    assert got["verdict"]["line"] == "테스트"

def test_is_new_day():
    st = reset_state("2026-07-06")
    assert is_new_day(st, "2026-07-07") is True
    assert is_new_day(st, "2026-07-06") is False

def test_load_missing_returns_none(tmp_path):
    assert load_state(str(tmp_path / "nope.json")) is None
```

- [ ] **Step 2: 실패 확인**

Run: `python -m pytest tests/test_briefing_state.py -v`
Expected: FAIL — `ModuleNotFoundError: No module named 'briefing_state'`

- [ ] **Step 3: 구현**

```python
# dashboard/briefing_state.py
"""장중 시황 브리핑 — 오늘의 스토리 상태 영속(재시작해도 스토리 유지)."""
import json, os


def reset_state(date_str: str) -> dict:
    return {"date": date_str, "session_phase": None, "verdict": None,
            "narrative": "", "turning_points": [], "baseline": None,
            "surfaced_news_ids": [], "last_synth_ts": 0.0, "updated_at": ""}


def is_new_day(state: dict, date_str: str) -> bool:
    return not state or state.get("date") != date_str


def load_state(path: str):
    try:
        with open(path, encoding="utf-8") as f:
            return json.load(f)
    except Exception:
        return None


def save_state(path: str, state: dict) -> None:
    try:
        os.makedirs(os.path.dirname(path), exist_ok=True)
        tmp = path + ".tmp"
        with open(tmp, "w", encoding="utf-8") as f:
            json.dump(state, f, ensure_ascii=False)
        os.replace(tmp, path)
    except Exception:
        pass
```

- [ ] **Step 4: 통과 확인**

Run: `python -m pytest tests/test_briefing_state.py -v`
Expected: PASS (4 passed)

- [ ] **Step 5: 커밋**

```bash
git add dashboard/briefing_state.py tests/test_briefing_state.py
git commit -m "feat(briefing): 오늘의 스토리 상태 영속(load/save/reset)"
```

---

## Task 8: 종합엔진 — 프롬프트 빌드 + 파싱 (`briefing_weather.py`)

`claude -p` 호출은 subprocess라 단위테스트에선 프롬프트 빌드와 응답 파싱만 검증(호출 자체는 Task 12 서버 스모크에서 실측).

**Files:**
- Create: `dashboard/briefing_weather.py`
- Test: `tests/test_briefing_weather.py`

**Interfaces:**
- Produces:
  - `build_prompt(facts: dict, events: list, story: dict, news: list, phase: str) -> str`
  - `parse_result(raw: str) -> dict|None` — claude `--output-format json`의 `.result`(모델이 낸 JSON 문자열)를 파싱 → `{verdict:{tone,line}, narrative, new_turning_points, used_news_ids}`.

- [ ] **Step 1: 실패 테스트 작성**

```python
# tests/test_briefing_weather.py
from briefing_weather import build_prompt, parse_result

def test_build_prompt_includes_events_and_story():
    p = build_prompt(
        facts={"J": {"rate": 5.76, "current": 8088}},
        events=[{"label": "외인 순매도→순매수 전환"}],
        story={"verdict": {"line": "약세 지속"}, "turning_points": []},
        news=["삼성전자 8조 기판 투자 공시"], phase="intraday")
    assert "외인 순매도→순매수 전환" in p
    assert "삼성전자 8조" in p
    assert "약세 지속" in p          # 직전 스토리가 프롬프트에 들어감

def test_parse_result_valid_json():
    raw = '{"verdict":{"tone":"🟢위험선호","line":"외인 매수 유지"},"narrative":"...","new_turning_points":[{"ts":"11:20","label":"외인 전환","major":true}],"used_news_ids":["a1"]}'
    d = parse_result(raw)
    assert d["verdict"]["line"] == "외인 매수 유지"
    assert d["new_turning_points"][0]["ts"] == "11:20"

def test_parse_result_with_codefence():
    raw = '```json\n{"verdict":{"tone":"t","line":"l"},"narrative":"n","new_turning_points":[],"used_news_ids":[]}\n```'
    assert parse_result(raw)["verdict"]["line"] == "l"

def test_parse_result_garbage_returns_none():
    assert parse_result("모델이 그냥 문장으로 답함") is None
```

- [ ] **Step 2: 실패 확인**

Run: `python -m pytest tests/test_briefing_weather.py -v`
Expected: FAIL — `ModuleNotFoundError: No module named 'briefing_weather'`

- [ ] **Step 3: 구현 (프롬프트+파싱; 호출부는 다음 스텝)**

```python
# dashboard/briefing_weather.py
"""장중 시황 브리핑 — 종합엔진. claude -p(Opus/Sonnet, Max 구독) 호출 + Gemini 폴백."""
import json, re, subprocess

_SYS = """너는 '로또의 스탁브레인' 장중 시황 브리핑 작성자다.
관심종목만 보는 투자자에게 지금 시장 전체 흐름을 한눈에 알려준다.
철칙:
- 아래 '재료(사실)'에 있는 것만 써라. 없으면 지어내지 마라.
- 단순 나열 금지. "그래서 지금 분위기가 어떻다"는 판단까지.
- 아침부터 지금까지의 흐름을 이어서 서술하라(직전 브리핑 대비 무엇이 바뀌었나).
매 순간 답할 것: 돈이 들어오나 / 외인기관 스탠스 전환 / 분기점 / 프로그램 / 미선물 / 나스닥 디커플링 / 외부충격.
출력은 반드시 아래 JSON 객체 하나만(설명 금지):
{"verdict":{"tone":"<🟢/🟡/🔴 + 한단어>","line":"<핵심 한줄>"},"narrative":"<이모지 문단 2~3개>","new_turning_points":[{"ts":"HH:MM","label":"...","major":true|false}],"used_news_ids":[]}"""


def build_prompt(facts: dict, events: list, story: dict, news: list, phase: str) -> str:
    ev = "\n".join(f"- {e.get('label','')}" for e in (events or [])) or "(없음)"
    nw = "\n".join(f"- {n}" for n in (news or [])) or "(없음)"
    tp = "\n".join(f"- {t.get('ts','')} {t.get('label','')}"
                   for t in (story or {}).get("turning_points", [])) or "(없음)"
    prevv = ((story or {}).get("verdict") or {}).get("line", "(없음)")
    return f"""{_SYS}

## 세션: {phase}
## 지수/수급 사실
{json.dumps(facts, ensure_ascii=False)}
## 직전 대비 새 이벤트
{ev}
## 오늘 시장 뉴스 후보(S급만 골라 녹여라)
{nw}
## 오늘의 스토리(직전)
직전 판정: {prevv}
전환점:
{tp}
"""


def parse_result(raw: str):
    """claude .result(모델이 낸 JSON 문자열) 파싱. 코드펜스/잡음 관용 처리."""
    if not raw:
        return None
    s = raw.strip()
    m = re.search(r"\{.*\}", s, re.DOTALL)
    if not m:
        return None
    try:
        d = json.loads(m.group(0))
    except Exception:
        return None
    if not isinstance(d, dict) or "verdict" not in d or "narrative" not in d:
        return None
    d.setdefault("new_turning_points", [])
    d.setdefault("used_news_ids", [])
    return d
```

- [ ] **Step 4: 통과 확인**

Run: `python -m pytest tests/test_briefing_weather.py -v`
Expected: PASS (4 passed)

- [ ] **Step 5: 커밋**

```bash
git add dashboard/briefing_weather.py tests/test_briefing_weather.py
git commit -m "feat(briefing): 종합엔진 프롬프트 빌드+결과 파싱"
```

---

## Task 9: 종합엔진 — claude -p 호출 + Gemini 폴백 (`briefing_weather.py`)

**Files:**
- Modify: `dashboard/briefing_weather.py`

**Interfaces:**
- Produces: `synthesize(facts, events, story, news, phase, model, gemini_fn, cwd, claude_bin) -> dict|None` — model ∈ {"opus","sonnet"}. `claude -p` 성공 시 파싱 결과, 실패/타임아웃 시 `gemini_fn(prompt)` 폴백. 반환에 `{"_model","_latency_ms"}` 메타 포함.

- [ ] **Step 1: 구현 추가 (호출부)**

```python
# dashboard/briefing_weather.py 에 추가
import time as _time


def synthesize(facts, events, story, news, phase, model="opus",
               gemini_fn=None, cwd="/home/ubuntu/briefing_agent",
               claude_bin="claude", timeout=90) -> dict | None:
    """claude -p(Max 구독) 호출 → 파싱. 실패 시 gemini_fn 폴백. 메타(_model,_latency_ms) 포함."""
    prompt = build_prompt(facts, events, story, news, phase)
    t0 = _time.time()
    raw = None
    try:
        proc = subprocess.run(
            [claude_bin, "-p", prompt, "--model", model,
             "--output-format", "json", "--permission-mode", "bypassPermissions"],
            cwd=cwd, capture_output=True, encoding="utf-8", errors="replace", timeout=timeout)
        if proc.returncode == 0 and proc.stdout:
            envelope = json.loads(proc.stdout)
            raw = envelope.get("result", "")
    except Exception:
        raw = None
    parsed = parse_result(raw) if raw else None
    used = f"claude:{model}"
    if parsed is None and gemini_fn is not None:
        try:
            g = gemini_fn(prompt)
            parsed = parse_result((g or {}).get("analysis", "")) if isinstance(g, dict) else parse_result(g)
            used = "gemini"
        except Exception:
            parsed = None
    if parsed is not None:
        parsed["_model"] = used
        parsed["_latency_ms"] = int((_time.time() - t0) * 1000)
    return parsed
```

- [ ] **Step 2: 스모크(문법·임포트) 확인**

Run: `python -c "import sys; sys.path.insert(0,'dashboard'); import briefing_weather; print('ok')"`
Expected: `ok`

- [ ] **Step 3: 커밋**

```bash
git add dashboard/briefing_weather.py
git commit -m "feat(briefing): claude -p(Opus/Sonnet) 호출 + Gemini 폴백"
```

---

## Task 10: 텔레그램 다이제스트 (`briefing_digest.py`)

**Files:**
- Create: `dashboard/briefing_digest.py`
- Test: `tests/test_briefing_digest.py`

**Interfaces:**
- Produces:
  - `format_trigger(events, result) -> str` — 한 트리거의 다이제스트 텍스트.
  - `DigestBatcher(minor_interval_s=900)` — `.add(events, result, now)`가 major는 즉시 반환(전송대상), minor는 버퍼링 후 interval 지나면 묶음 반환.
  - `format_daily(calib_rows) -> str` — 일일요약.
- Consumes: 텔레 전송은 오케스트레이터가 기존 sendMessage로. 이 모듈은 텍스트만 생성.

- [ ] **Step 1: 실패 테스트 작성**

```python
# tests/test_briefing_digest.py
from briefing_digest import format_trigger, DigestBatcher, format_daily

def test_format_trigger_has_events_and_verdict():
    txt = format_trigger(
        [{"label": "외인 순매도→순매수 전환", "major": True}],
        {"verdict": {"tone": "🟢위험선호", "line": "외인 매수 유지"},
         "narrative": "9:45 저점 이후 반등.\n미선물 우호적.", "_model": "claude:opus"})
    assert "외인 순매도→순매수 전환" in txt
    assert "외인 매수 유지" in txt
    assert "opus" in txt

def test_batcher_major_immediate_minor_buffered():
    b = DigestBatcher(minor_interval_s=900)
    now = 1000.0
    imm = b.add([{"label": "big", "major": True}], {"verdict": {"line": "x"}}, now)
    assert imm is not None                      # major 즉시
    buf = b.add([{"label": "small", "major": False}], {"verdict": {"line": "y"}}, now + 10)
    assert buf is None                          # minor는 버퍼
    flush = b.add([{"label": "small2", "major": False}], {"verdict": {"line": "z"}}, now + 1000)
    assert flush is not None and "small" in flush  # interval 지나 묶음 방출

def test_format_daily_counts():
    rows = [{"fired_events": [{"type": "investor_flip"}], "noise_flag": False},
            {"fired_events": [{"type": "sector_surge"}], "noise_flag": True}]
    txt = format_daily(rows)
    assert "2" in txt      # 총 2회
    assert "노이즈" in txt
```

- [ ] **Step 2: 실패 확인**

Run: `python -m pytest tests/test_briefing_digest.py -v`
Expected: FAIL — `ModuleNotFoundError: No module named 'briefing_digest'`

- [ ] **Step 3: 구현**

```python
# dashboard/briefing_digest.py
"""장중 시황 브리핑 — 텔레그램 다이제스트(major 즉시 / minor 15분 배치) + 일일요약."""


def format_trigger(events: list, result: dict) -> str:
    labels = " · ".join(e.get("label", "") for e in (events or []))
    v = (result or {}).get("verdict", {}) or {}
    first_line = ((result or {}).get("narrative", "") or "").split("\n")[0]
    model = (result or {}).get("_model", "")
    head = f"🔔 {labels}" if labels else "🔔 시황 갱신"
    return f"{head}\n[{v.get('tone','')}] {v.get('line','')}\n{first_line}\n({model})"


class DigestBatcher:
    """major 이벤트는 즉시, minor는 minor_interval_s마다 묶어서 방출."""
    def __init__(self, minor_interval_s: int = 900):
        self.interval = minor_interval_s
        self._buf = []
        self._last_flush = 0.0

    def add(self, events: list, result: dict, now: float):
        is_major = any(e.get("major") for e in (events or []))
        text = format_trigger(events, result)
        if is_major:
            return text
        self._buf.append(text)
        if self._last_flush == 0.0:
            self._last_flush = now
        if now - self._last_flush >= self.interval:
            out = "📋 최근 브리핑 묶음\n\n" + "\n\n".join(self._buf)
            self._buf = []
            self._last_flush = now
            return out
        return None


def format_daily(calib_rows: list) -> str:
    from collections import Counter
    total = len(calib_rows or [])
    noise = sum(1 for r in (calib_rows or []) if r.get("noise_flag"))
    by_type = Counter(e.get("type") for r in (calib_rows or [])
                      for e in r.get("fired_events", []))
    lines = [f"📊 오늘 시황 브리핑 관측 요약",
             f"총 발동 {total}회 · 의심노이즈 {noise}건", "타입별:"]
    lines += [f"  - {k}: {v}" for k, v in by_type.most_common()]
    return "\n".join(lines)
```

- [ ] **Step 4: 통과 확인**

Run: `python -m pytest tests/test_briefing_digest.py -v`
Expected: PASS (3 passed)

- [ ] **Step 5: 커밋**

```bash
git add dashboard/briefing_digest.py tests/test_briefing_digest.py
git commit -m "feat(briefing): 텔레 다이제스트(major즉시/minor15분배치)+일일요약"
```

---

## Task 11: 오케스트레이터 배선 (`server.py`)

`_poll_briefing` 안 `_insight_run_synthesis` 호출부를 새 엔진으로 교체. market_flow 캐시 + build_heatmap으로 스냅샷 → 디텍터 → 발동 시 Opus/Sonnet 번갈아 종합 → 스토리 갱신·영속 + insight 저장 + 스냅샷/다이제스트.

**Files:**
- Modify: `dashboard/server.py` (`_poll_briefing` 내부, `_insight_run_synthesis` 근처 ~3038~3095)

**Interfaces:**
- Consumes: 위 모든 모듈, `_prewarm_cache["market_flow"]`, `sector_heatmap.build_heatmap`, `kis_api.get_index_minutebar`, `global_api.get_nasdaq_futures().bars`, `_gemini_text`, `_briefing_set_insight`/`_briefing_load`, `_NEWS_FEED`, `_recent_topick_mentions`, 텔레 `BOT_TOKEN`/`CHAT_ID`.
- Produces: 새 함수 `_weather_tick()` (30초 루프에서 호출), 전역 `_weather` 상태.

- [ ] **Step 1: 모듈 임포트 + 전역 추가 (server.py 상단 import 블록 근처)**

```python
# dashboard/server.py — import 섹션에 추가
import briefing_phase, briefing_events as _bev, briefing_weather as _bw
import briefing_digest as _bdig, briefing_state as _bstate

_WEATHER_STATE_PATH = os.path.join(ROOT, "output", "weather_state.json")
_WEATHER_CALIB_DIR  = os.path.join(ROOT, "output", "weather_calib")
_BRIEFING_AGENT_CWD = os.environ.get("BRIEFING_AGENT_CWD", "/home/ubuntu/briefing_agent")
_weather = {"detect": {"cooldown": {}, "leg": {}}, "state": None,
            "batcher": _bdig.DigestBatcher(900), "model_toggle": 0,
            "calib_today": [], "synth_count": 0, "last_synth_ts": 0.0}
_WEATHER_MIN_INTERVAL_S = 90
_WEATHER_DAILY_CAP = 60
```

- [ ] **Step 2: 텔레 전송 헬퍼 확인/추가**

server.py에 텔레 전송 함수가 이미 있으면 재사용. 없으면 추가:

```python
# dashboard/server.py — 없을 때만 추가
def _weather_tg(text: str):
    import urllib.request, urllib.parse
    token = os.environ.get("BOT_TOKEN", ""); chat = os.environ.get("CHAT_ID", "") or os.environ.get("OWNER_CHAT_ID", "")
    if not token or not chat:
        return
    try:
        data = urllib.parse.urlencode({"chat_id": chat, "text": text}).encode()
        urllib.request.urlopen(f"https://api.telegram.org/bot{token}/sendMessage", data=data, timeout=8)
    except Exception:
        pass
```

- [ ] **Step 3: `_weather_tick()` 구현 (server.py, `_poll_briefing` 정의 근처)**

```python
# dashboard/server.py
def _weather_tick():
    """30초 틱: 스냅샷→디텍터→(발동 시)종합→스토리·insight·다이제스트. Phase0 관측."""
    now = datetime.now()
    phase = briefing_phase.session_phase(now)
    date_str = now.strftime("%Y-%m-%d")

    # 스토리 상태 로드/리셋
    st = _weather["state"] or _bstate.load_state(_WEATHER_STATE_PATH)
    if _bstate.is_new_day(st or {}, date_str):
        st = _bstate.reset_state(date_str)
        _weather["detect"] = {"cooldown": {}, "leg": {}}
        _weather["calib_today"] = []
        _weather["synth_count"] = 0
    _weather["state"] = st

    # 스냅샷 구성
    mf = (_prewarm_cache.get("market_flow") or {}).get("data") or {}
    try:
        from sector_heatmap import build_heatmap
        hm = build_heatmap()
        sectors = hm.get("sectors", []) if isinstance(hm, dict) else []
    except Exception:
        sectors = []
    j_bars = mf.get("J_bars") or []
    nq_bars = (mf.get("NQ") or {}).get("bars") or []
    curr = _bev.build_snapshot(mf, sectors, j_bars, mf.get("Q_bars") or [],
                               ts=now.strftime("%H:%M"))
    prev = st.get("baseline")

    # 데이터 동결/무효 가드: 지수가 0이면(장전·KIS다운) 시세 디텍터 무의미
    live_ok = curr["idx"]["J"]["price"] > 0

    events = []
    if live_ok:
        events = _bev.detect_all(prev, curr, sectors, j_bars, nq_bars, phase, _weather["detect"])

    # S급 뉴스 후보 (오늘자 topick + news_feed 제목)
    news = []
    try:
        atoms_db = os.path.join(ROOT, "pipeline", "atoms", "atoms.db")
        news = [t.get("content", "")[:120] for t in _recent_topick_mentions(atoms_db, limit=8)]
    except Exception:
        pass

    # 발동 조건: 이벤트 OR 페이즈변화 OR 심박(30분)
    phase_changed = st.get("session_phase") != phase
    heartbeat = time.time() - _weather["last_synth_ts"] >= 1800
    fire = bool(events) or phase_changed or (heartbeat and live_ok)

    # 비용/한도 가드
    if fire and (time.time() - _weather["last_synth_ts"] < _WEATHER_MIN_INTERVAL_S):
        fire = False
    if fire and _weather["synth_count"] >= _WEATHER_DAILY_CAP and not events:
        fire = False   # 캡 도달 후 심박은 생략, 실이벤트만

    if fire:
        # Opus/Sonnet 번갈아, major 이벤트엔 둘 다(A/B) — 패널엔 Opus
        has_major = any(e.get("major") for e in events)
        facts = {"phase": phase, "J": curr["idx"]["J"], "Q": curr["idx"]["Q"],
                 "inv": curr["inv"], "prog": curr["prog"], "nq": curr["nq"]}
        models = ["opus", "sonnet"] if has_major else \
                 (["opus"] if _weather["model_toggle"] % 2 == 0 else ["sonnet"])
        _weather["model_toggle"] += 1
        results = {}
        for m in models:
            r = _bw.synthesize(facts, events, st, news, phase, model=m,
                               gemini_fn=_gemini_text, cwd=_BRIEFING_AGENT_CWD,
                               claude_bin=CLAUDE_BIN or "claude")
            if r:
                results[m] = r
        _weather["last_synth_ts"] = time.time()
        _weather["synth_count"] += 1

        primary = results.get("opus") or (results.get("sonnet") if results else None)
        if primary:
            # 스토리 갱신
            st["verdict"] = {**primary["verdict"], "ts": now.strftime("%H:%M")}
            st["narrative"] = primary.get("narrative", "")
            for tp in primary.get("new_turning_points", []):
                st["turning_points"].append(tp)
            st["session_phase"] = phase
            st["baseline"] = curr
            st["updated_at"] = now.strftime("%H:%M")
            _bstate.save_state(_WEATHER_STATE_PATH, st)
            # 라이브 패널 반영(insight 필드 확장)
            _briefing_set_insight(BRIEFING_PATH, {
                "ts": now.strftime("%H:%M"),
                "verdict": st["verdict"], "narrative": st["narrative"],
                "turning_points": st["turning_points"][-6:],
                "session_phase": phase})
            # 관측 로그 + 다이제스트
            row = {"ts": now.strftime("%H:%M"), "fired_events": events,
                   "models": list(results.keys()), "noise_flag": False,
                   "verdict": st["verdict"]}
            _weather["calib_today"].append(row)
            _weather_calib_append(date_str, row, results)
            digest_result = results.get("opus") or primary
            # A/B면 둘 다 붙여서
            if has_major and "sonnet" in results:
                digest_result = {**digest_result,
                                 "narrative": digest_result.get("narrative", "") +
                                 f"\n---[sonnet] {results['sonnet']['verdict'].get('line','')}"}
            msg = _weather["batcher"].add(events, digest_result, time.time())
            if msg:
                _weather_tg(msg)
    else:
        # 발동 안 해도 baseline은 갱신(다음 델타 기준)
        if live_ok and prev is not None:
            st["baseline"] = curr
    _weather["state"] = st


def _weather_calib_append(date_str, row, results):
    try:
        os.makedirs(_WEATHER_CALIB_DIR, exist_ok=True)
        rec = dict(row)
        rec["outputs"] = {m: {"verdict": r.get("verdict"), "latency_ms": r.get("_latency_ms"),
                              "model": r.get("_model")} for m, r in results.items()}
        with open(os.path.join(_WEATHER_CALIB_DIR, f"{date_str}.jsonl"), "a", encoding="utf-8") as f:
            f.write(json.dumps(rec, ensure_ascii=False) + "\n")
    except Exception:
        pass
```

- [ ] **Step 4: `_poll_briefing`에서 기존 insight 호출을 `_weather_tick`으로 교체**

기존:
```python
                if time.time() - _insight_last_run["ts"] >= 900:
                    _insight_last_run["ts"] = time.time()
                    _insight_run_synthesis(curr)
```
교체:
```python
                _weather_tick()
```
(주의: `_weather_tick`은 자체적으로 market_flow 캐시를 읽으므로 `curr` 인자 불필요. `_insight_run_synthesis`/`_compute_index_shape` 등 기존 함수는 당장 삭제하지 말고 남겨둔다 — 롤백 안전.)

- [ ] **Step 5: 문법·임포트 스모크**

Run: `python -c "import sys; sys.path.insert(0,'dashboard'); sys.path.insert(0,'scripts'); import ast; ast.parse(open('dashboard/server.py',encoding='utf-8').read()); print('syntax ok')"`
Expected: `syntax ok`

- [ ] **Step 6: 커밋**

```bash
git add dashboard/server.py
git commit -m "feat(briefing): 오케스트레이터 배선(_weather_tick) - 디텍터→종합→스토리→다이제스트"
```

---

## Task 12: 서버 셋업 + 실호출 스모크 (Opus/Sonnet)

**Files:** (서버 원격, 코드변경 없음)

- [ ] **Step 1: 전용 최소 작업디렉토리 생성**

```bash
ssh -i "C:/Users/TheRose/crawling_bot_client/LightsailDefaultKey-ap-northeast-2.pem" -o StrictHostKeyChecking=no ubuntu@3.39.179.148 "mkdir -p /home/ubuntu/briefing_agent && ls -la /home/ubuntu/briefing_agent"
```
Expected: 빈 디렉토리 생성 확인

- [ ] **Step 2: Opus/Sonnet 각각 실호출 스모크 (JSON 파싱 가능 확인)**

```bash
ssh -i "C:/Users/TheRose/crawling_bot_client/LightsailDefaultKey-ap-northeast-2.pem" -o StrictHostKeyChecking=no ubuntu@3.39.179.148 "
cd /home/ubuntu/briefing_agent
for M in opus sonnet; do
  echo \"=== \$M ===\"
  timeout 90 claude -p 'Output ONLY this JSON: {\"verdict\":{\"tone\":\"🟢test\",\"line\":\"ok\"},\"narrative\":\"n\",\"new_turning_points\":[],\"used_news_ids\":[]}' --model \$M --output-format json --permission-mode bypassPermissions 2>&1 | python3 -c 'import sys,json; e=json.load(sys.stdin); print(\"model:\", list(e.get(\"modelUsage\",{}).keys()), \"result_ok:\", bool(e.get(\"result\")))'
done
"
```
Expected: opus → `model: ['claude-opus-4-8'] result_ok: True`, sonnet → `model: ['claude-sonnet-...'] result_ok: True`

- [ ] **Step 3: (검증만, 커밋 없음)** 결과 기록

두 모델 모두 `result_ok: True`면 통과. Sonnet 모델ID 로그로 확인(요금제 라우팅 정상).

---

## Task 13: API + 프론트 패널 (`server.py` + `market.html`)

**Files:**
- Modify: `dashboard/server.py` (`/api/market_briefing`는 이미 insight 반환 — 확장 필드 그대로 실림, 별도 수정 최소)
- Modify: `dashboard/market.html` (실시간 브리핑 패널 렌더)

**Interfaces:**
- Consumes: `/api/market_briefing` 응답 `insight = {verdict:{tone,line,ts}, narrative, turning_points:[{ts,label,major}], session_phase}`.

- [ ] **Step 1: 프론트 렌더 함수 교체 (market.html, `renderBelowPanel`의 인사이트/브리핑 부분)**

기존 인사이트 렌더(헤드라인+본문) 자리를 배지+내러티브+전환점으로:
```javascript
// market.html — 실시간 브리핑 렌더 (insight 객체 사용)
function renderWeatherPanel(insight){
  if(!insight) return '';
  const v = insight.verdict||{};
  const phaseTag = insight.session_phase==='weekend'||insight.session_phase==='closed'
    ? ' <span style="color:#888;font-size:11px;">(지난 장 기준)</span>' : '';
  const tps = (insight.turning_points||[]).map(t=>
    `<div class="wx-tp"><span class="wx-tp-ts">${esc(t.ts||'')}</span> ${esc(t.label||'')} ${t.major?'★':''}</div>`
  ).join('');
  const body = _mdMini(insight.narrative||'');   // 기존 초경량 마크다운 재사용
  return `
    <div class="wx-verdict">${esc(v.tone||'')} · ${esc(v.line||'')}
      <span class="wx-ts">${esc(v.ts||'')}${phaseTag}</span></div>
    <div class="wx-narr">${body}</div>
    ${tps?`<div class="wx-tp-title">오늘의 전환점</div>${tps}`:''}`;
}
```
그리고 기존 인사이트 렌더 호출부를 `renderWeatherPanel(_marketInsight)`로 교체(`_marketInsight`는 이미 `d.insight`에서 세팅됨). CSS는 기존 카드 톤(골드/다크)에 맞춰 `.wx-verdict`(굵게 15px), `.wx-narr`(문단), `.wx-tp`(11px 회색) 추가.

- [ ] **Step 2: 로컬 문법 확인 (HTML은 브라우저 검증이 본검증)**

Run: `python -c "t=open('dashboard/market.html',encoding='utf-8').read(); assert 'renderWeatherPanel' in t; print('ok')"`
Expected: `ok`

- [ ] **Step 3: 배포**

```bash
scp -i "C:/Users/TheRose/crawling_bot_client/LightsailDefaultKey-ap-northeast-2.pem" -o StrictHostKeyChecking=no \
  "dashboard/briefing_phase.py" "dashboard/briefing_events.py" "dashboard/briefing_weather.py" \
  "dashboard/briefing_digest.py" "dashboard/briefing_state.py" "dashboard/server.py" "dashboard/market.html" \
  ubuntu@3.39.179.148:/home/ubuntu/lotto-stock-wiki/dashboard/
ssh -i "C:/Users/TheRose/crawling_bot_client/LightsailDefaultKey-ap-northeast-2.pem" -o StrictHostKeyChecking=no ubuntu@3.39.179.148 "cd /home/ubuntu/lotto-stock-wiki && /home/ubuntu/venv/bin/python -m py_compile dashboard/server.py dashboard/briefing_*.py && echo SYNTAX_OK && sudo systemctl restart stockbrain.service && sleep 4 && sudo systemctl is-active stockbrain.service"
```
Expected: `SYNTAX_OK` + `active`

- [ ] **Step 4: 실브라우저 렌더 검증 (fablize 그라운딩 — 정적확인만으론 완료 아님)**

`https://stockbrain1.duckdns.org/market` 새로고침 → 실시간 브리핑 패널에 **판정 배지 + 내러티브 + 전환점** 렌더 확인. (장중이면 실데이터, 주말이면 "지난 장 기준" 라벨 + 뉴스 기반 갱신). API 확인:
```bash
curl -s -m 20 -b "dash_auth=<쿠키>" "https://stockbrain1.duckdns.org/api/market_briefing" | python3 -c "import json,sys; d=json.load(sys.stdin); print(d.get('insight',{}).get('verdict'), '| tp:', len(d.get('insight',{}).get('turning_points',[])))"
```
Expected: verdict 객체 + turning_points 배열

- [ ] **Step 5: 커밋**

```bash
git add dashboard/market.html
git commit -m "feat(briefing): 실시간 브리핑 패널 - 판정배지+내러티브+전환점 렌더"
```

---

## Task 14: 관측 가동 확인 + 일일요약 크론

**Files:**
- Modify: `dashboard/server.py` (일일요약 전송을 `_weather_tick`의 closed 전환 시 1회 발송으로)

- [ ] **Step 1: closed 전환 시 일일요약 1회 발송 로직 추가 (`_weather_tick` 내 phase_changed 처리부)**

```python
# _weather_tick 내, phase_changed 감지 지점에 추가
if phase == "closed" and st.get("session_phase") == "intraday":
    try:
        _weather_tg(_bdig.format_daily(_weather.get("calib_today", [])))
    except Exception:
        pass
```

- [ ] **Step 2: 배포·재시작**

```bash
scp -i "C:/Users/TheRose/crawling_bot_client/LightsailDefaultKey-ap-northeast-2.pem" -o StrictHostKeyChecking=no "dashboard/server.py" ubuntu@3.39.179.148:/home/ubuntu/lotto-stock-wiki/dashboard/server.py
ssh -i "C:/Users/TheRose/crawling_bot_client/LightsailDefaultKey-ap-northeast-2.pem" -o StrictHostKeyChecking=no ubuntu@3.39.179.148 "cd /home/ubuntu/lotto-stock-wiki && /home/ubuntu/venv/bin/python -m py_compile dashboard/server.py && echo OK && sudo systemctl restart stockbrain.service && sleep 4 && sudo journalctl -u stockbrain.service --no-pager -n 5 | tail -5"
```
Expected: `OK` + 서비스 정상

- [ ] **Step 3: 관측 로그 생성 확인 (평일 장중 or 강제 1틱 후)**

```bash
ssh -i "C:/Users/TheRose/crawling_bot_client/LightsailDefaultKey-ap-northeast-2.pem" -o StrictHostKeyChecking=no ubuntu@3.39.179.148 "ls -la /home/ubuntu/lotto-stock-wiki/output/weather_calib/ 2>/dev/null; tail -2 /home/ubuntu/lotto-stock-wiki/output/weather_calib/*.jsonl 2>/dev/null || echo '아직 트리거 없음(장외면 정상)'"
```
Expected: 장중이면 jsonl 라인, 장외면 안내 문구

- [ ] **Step 4: 커밋 + 브랜치 마무리**

```bash
git add dashboard/server.py
git commit -m "feat(briefing): 장마감 시 일일 관측요약 텔레 발송"
git log --oneline -14
```
`superpowers:finishing-a-development-branch`로 병합/PR 결정.

---

## 자체검토 (Self-Review)

- **스펙 커버리지**: 세션페이즈(T1)·스냅샷(T2)·디텍터6종(T3~5)·쿨다운/노이즈(T6)·스토리영속(T7)·종합엔진+claude-p+폴백(T8~9)·다이제스트(T10)·오케스트레이터(T11)·서버셋업(T12)·API/패널(T13)·일일요약(T14) → spec §4~§13 전부 커버.
- **플레이스홀더**: 각 스텝에 실제 코드·테스트·명령 포함, TBD 없음.
- **타입 일관성**: `build_snapshot`→`detect_*`→`detect_all`→`synthesize`→`_weather_tick`의 snap/event/result 키가 스텝 간 일치(`type/market/major/label/value`, `verdict/narrative/new_turning_points`).
- **미커버 리스크**: 디커플링 "NQ 선반전"의 정밀 구분은 v1에서 슬로프 부호 비교로 근사(spec §5.2 note와 일치). 임계 튜닝은 Phase0 관측으로.
