# 핸드오프 — 대본프롬프트단순화 (백본-A 믹스 대개편)

- 갱신: 2026-07-27 (집PC) · 트랙: `대본프롬프트단순화` (`.tracks/대본프롬프트단순화`)
- 라이브: shoppingshorts.duckdns.org (systemd `shopping-shorts`, 서버 3.39.179.148, DB=`shopping_shorts/data/reference.db`)

## 한 줄 요약
"대본 만질수록 이상해진다" → 진단(제약 과적재) → **3단계로 대개편 배포 완료**, 실측 결과 확 좋아짐(점수 0.60→0.82, 밋밋한 기능나열 → 대화·인물·인과 있는 진짜 이야기).

## ✅ 배포 완료 (main, 라이브)
1. **레버1 — 프롬프트 급진 단순화** (커밋 `dcdea5045`)
   - ★필수 17개 과적재 제거 → 5단계 스파인(훅→문제→해결→결과→CTA) + 훅 4패턴(부정/손실회피/권위/결과선공개) + 소스교차믹스. 제품을 "참고용"→"중심"으로. 은행/8비트/반전2번/171자하드컷 제거.
2. **레버2 — 백본-A 흐름 계승 + Replace/Insert + 훅CTA 교체 + 은행 우수라인** (커밋 `51f2bf7b7`)
   - `backbone.backbone_flow`에 원본대사 gist(text) 추가 → `_backbone_order_block`이 "백본 흐름(순서+대사)" 넘기고 "창의변형·순서계승·베끼기금지".
   - Replace/Insert LLM 명시. 은행 `parts_block`만 "참고·변형 양념"으로 재주입(avoid·winners·spine은 OFF).
   - `backbone.swap_hook_cta_for_differentiation`(+`_middle_source_clip`) 신규 → 훅=비-A 소스 최고장면 / CTA=중간 소스(원본 엔딩 회피). ping_pong 후처리 마지막 배선(화면만).
3. **과삭제 복구** (커밋 `d03b7258d`) — 레버1이 스토리 강제를 다 걷어내 '기능 자막 나열'로 회귀 → `_scene_first_candidates` 스파인 앞에 `[★이야기 필수]` 린 복구(인과·대화1회·인물·나열금지).

## 실측 (2026-07-27 재생성)
- 선물 3D피규어 소재: A/B/C 점수 0.637/0.744/0.82, 28~31초, 대화 인용·인물·인과 정상. 밋밋 해소 확인.
- 은행: reference.db 승인부품 3491개, `parts_block(Store(DB_PATH))` 실출력 806자 확인(정상). ⚠️첫 밋밋 결과는 레버2 배포(01:33) 전 생성분이었음.

## ⏭ 미결 (회사서 이어가기)
- [ ] **★#1 AI PICK 수동선택 무시 버그 = "대본즐겨찾기 선 끊기"** (사장님 실측 2026-07-27):
      맨오른쪽 영상을 클릭(pickFootage→bbMain)해도 AI PICK 카드·원본해부가 자동픽(가운데)으로 남음.
      **근본원인**: `app.py:5909 _load_work_sources` → AI PICK 소스 = `wiki_list(도서관) ∩ 담긴codes` **교집합**.
      담김이어도 **도서관에 없으면 소스에서 빠져** forced(bbMain)를 build_aipick이 못 찾고 자동픽 폴백.
      **fix 방향(주의)**: 교집합을 그냥 끊으면 안 됨 — 도서관에 없는 담긴 영상은 full_text/structure가
      없어 백본 불가(AI PICK 빈 화면 위험). "선 끊기"는 **담긴 영상을 온디맨드 분석→소스 승격**(또는
      produce_script_picks에 저장된 대본/구조를 직접 소스로 실어주기)까지 함께 배선해야 안전.
      최소안: bbMain(forced) 지정 영상은 도서관 밖이어도 담김(picks) 데이터로 소스에 강제 포함 +
      분석 미완이면 그 자리서 analyze. ⚠️AI PICK 소싱은 라이브 핵심이라 반드시 테스트 후 배포.
- [ ] **오타 방지**: C안 CTA키워드 '주억'(→추억) 오타. 프롬프트에 "cta_keyword 맞춤법 확인" or 후처리 교정 한 줄.
- [ ] **은행 실사용 눈으로 확인**: 매칭 후 리뷰 "은행이 뭘 댔나"(plan.bank_injected/bank_snapshot)로 실제 반영 확인.
- [ ] **원본 대조 정밀 튜닝**: 원본(백본 A) 대본 붙여 원본↔생성 차이 더 좁히기.
- [ ] **B안(완전 2단계) 옵션**: A안(현행) 부족하면 `generate_backbone_script`(backbone.py:253, 미배선) 배선 = 컷 시퀀스 확정 → 대사 세공. 단 rich 내레이터 유지(밋밋 함정).

## 백본(메인) 선택 구조 (질문 나왔던 것)
- 기본 **자동**(pick_backbone: 인스타/유튜브만 후보 + coverage 0.6+참여도 0.4, 없으면 세그최다 폴백).
- **수동**: 제작소 재료카드 썸네일 클릭=pickFootage → bbMain=true → 서버에 forced로 강제(produce.html:2981/2696). AI PICK 뱃지=그 영상=백본(같은 것).

## 핵심 파일
- `shopping_shorts/edit_plan.py` — `_scene_first_candidates`(프롬프트), `_backbone_order_block`, `_apply_anchor_grain`, `build_scene_first_plan`(ping_pong 후처리)
- `shopping_shorts/backbone.py` — `backbone_flow`, `swap_hook_cta_for_differentiation`, `_middle_source_clip`, `pick_backbone`
- `shopping_shorts/mix_pipeline.py:629` — bank_context=parts_block only, avoid_hooks=None
- `shopping_shorts/bank_assemble.py` — parts_block(80)/avoid_block(126,OFF)/winners_block(152,OFF)
- 설계: `docs/superpowers/specs/2026-07-26-대본프롬프트-단순화-Gemini종합-design.md`, `2026-07-27-백본A믹스-흐름계승-훅CTA교체-design.md`
- 백업(옛 프롬프트 전문): 집PC 바탕화면 `대본생성_전체프롬프트.txt`

## 재개 방법
회사 PC에서 `git pull` 후, 이 트랙으로 이어가려면 `py tools/track.py start 대본프롬프트단순화`(폴더 없으면) → 편집은 `.tracks/대본프롬프트단순화/...` 절대경로. 트리거: "대본프롬프트 단순화 이어서" / "오타 방지 넣자" / "B안 2단계 분리 하자".
