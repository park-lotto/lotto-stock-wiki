# 장면실험실 로그

## 2026-08-14~15 (집 PC)
- 회사 조립(3·3·3·3·2)을 git으로 넘겨받아 이어감. scene_lab UI 전면 개편 — 버튼 전멸(드래그 중심),
  좌우 두 칸→한 칸 합침, 장면 가운데 자르기(✂), 미리보기 정지/구간이동/창이동.
- ★라이브 버그 수정: 대본을 고쳐도 옛 자막 타이밍(cap_durs·cap_lead)이 안 지워져 자막이 음성보다
  늦게 끝나던 것 → `mix_pipeline.invalidate_caption_meta()` 한 곳으로 모아 3경로에서 호출.
  테스트 3562 pass/8 fail(=수정 전과 동일, 회귀 0). **미커밋.**
- 실측: planClips는 되돌아가지 않고 부족분을 마지막 컷에 더한다. 자르면 부족이 오히려 커진다
  (칸1: 0.6초 자르면 부족 0.9→2.5초). 자르기는 "더 담기/늘려 채우기"와 세트여야 한다.
- 진행중: 늘려채우기 토글·자동채우기·`1.6/1.6초` 표기 / 서버 배선(편집을 실제 렌더에 반영).
- 2026-08-15: 토글·자동채우기·1.6/1.6표기 완료(회귀0) + 서버배선(scene_override 얹기·트림 예산·apply.py) 구현, 테스트 10건 green, 라이브 미적용(커밋 대기)
- 2026-08-15(2): 제작소 2단계 정식 이식(사장님 B안). UI 원본을 shopping_shorts/static/scene_lab.html
  한 파일로 이관(build.py는 데이터 인라인만), 제작소 2단계 ② 자리에 iframe 임베드. 서버 배선:
  segments에 shot_role·is_key·phash 추가, /api/mix/scene_lab/{job}(+phash·src·apply/revert),
  phash는 seg_thumb 생성 시 캐시(fetch.py와 비트 동일 실측). 신규 테스트 10건 green,
  기준선(8 fail) 대비 회귀 0, Playwright 실화면 확인. **미커밋**(사장님 확인 대기).
