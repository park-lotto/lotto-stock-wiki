## 2026-08-30 장면꾸미기확대

- 6단계 이름 `자막꾸미기`→`장면꾸미기`(화면 5곳: produce·intro의 STEP_LABELS·h3 + 테스트 기대값).
  도크 라벨은 STEP_LABELS에서 자동 생성돼 함께 바뀐다.
- 장면 **부분확대** 추가 — 보고 싶은 곳을 그냥 누르면 그 자리가 확대(1→1.5→2→2.5→3→1),
  슬라이더·＋－·원래대로, 캡컷과 같은 단축키(Alt+Shift +/−/Z), 확대 중 배경 끌기=패닝.
- ★`transform:scale`로 구현 — `PREVIEW_W=380`(서버 렌더와 짝, 12곳 사용)을 못 건드리므로
  컨테이너를 키우는 방식은 글자만 안 커져 틀린다. scale은 자식 전부를 비례 확대.
- ★기존 글자 드래그 무수정 공존 — 좌표가 getBoundingClientRect 기준이라 scale이 이미 반영됨(실측 4/4).
- ★패닝 계수는 유도(Δo=-dr/(Z-1)) — 짐작한 첫 식은 Z=1.5에서 6배 틀렸다.
- 캡컷 조사: 배율 퍼센트 UI는 실제로 **없다**(널리 퍼진 정보 반증), 확대 후 드래그하면
  화면이 아니라 **요소가 끌려가 사고**가 난다 → 우리는 글자/배경 분리 + "보기만 커져요" 명시로 우위.
- 검증: 43 passed / 콘솔 0 / 전체 6,142 passed, 2 failed(기준선 동일, 내 회귀 아님).
