
## 2026-07-20 내보내기캡컷 T1
- 캡컷 편집용 내보내기 T1 라이브(origin/main e81678b1b). export_bundle.py(_beat_timeline 단일출처 SRT·스크립트+primary구간 ffmpeg컷→ZIP)·/api/mix/export(?part=)·video?dl=1·7단계 UI. deny-by-default 자동보호. 테스트13(순수+실ffmpeg+HTTP). 웹앱이라 캡컷 자동실행 불가→다운로드ZIP. 후속 T2 draft·T3 bat.

## 2026-07-20 내보내기캡컷 T2
- 원클릭 캡컷 보내기 라이브(origin/main e1a97062b). capcut_draft.py(편집안→draft_content.json 3트랙+동반material)·/api/mix/capcut(매니페스트)·produce.html sendToCapCut(File System Access API+IndexedDB핸들 2회차원클릭). 실캡컷8.9.1.3802에 테스트draft 찍어 홈에뜨고 영상+자막 렌더 육안확인=포맷검증. 테스트20건. 웹앱도 showDirectoryPicker로 캡컷폴더 직접쓰기 가능(레퍼런스 방식).

## 2026-07-20 내보내기캡컷 설정자동화
- 실E2E성공(원클릭→실32초 프로젝트 캡컷생성). 제스처버그(showDirectoryPicker await뒤 호출) 수정. 상대경로 실패=절대경로 필수 확정. 캡컷 저장위치 자동화: globalSetting의 currentCustomDraftPath를 static/capcut_setup.bat이 세팅([char]92 더블백슬래시·.bak백업). 새 설정=캡컷설치→bat더블클릭→폴더한번지정→원클릭.

## 2026-08-25 내보내기캡컷 .bat 인코딩 사고 + 간단안내
- 고객 제보 "캡컷 자동설정 .bat 실행하면 한글 깨지고 폴더 안 만들어짐". 원인=라이브 파일이 UTF-8+LF+chcp 65001 → cmd가 코드페이지 변경 후 바이트오프셋으로 되감아 재파싱하며 한글 중간에서 줄이 쪼개짐(515자 powershell 줄이 특히). 로컬 사본은 CRLF라 통과, `* text=auto`가 서버만 LF로 만들어 라이브에서만 터짐 = 한 달간 미발견. 라이브 파일 그대로 실행해 고객 캡쳐와 동일 에러 재현.
- 고침 3건 라이브: ①CP949 저장+chcp 제거+CRLF, .gitattributes `*.bat binary`(재발차단 핵심) ②setup.html 2단계에 자동설정 다운로드 버튼 직접 배치(전엔 제작소 9단계까지 가야만 받을 수 있었다 — "최초 1회" 설정인데 영상 한 편을 만들어야 했음) ③.bat 실패이유 분기 안내(미설치/로그인전)+성공시 currentCustomDraftPath 되읽어 출력. for/f 안 powershell 따옴표 파서에러 → findstr로 교체.
- 신규 `capcut_easy.html`(3단계 요약 안내) + setup·produce에서 링크. ★static 28개 중 capcut_easy·capcut_manual만 `meta charset` 없어 한글 깨짐 — 실렌더로 발견해 둘 다 추가.
- 운영규칙 확정: 캡컷을 꺼야 하는 건 .bat 실행 때뿐. 보내기는 켜져 있어도 되고, 껐다 켜는 건 "보낸 후"(캡컷은 켤 때 목록을 읽는다).
- 2026-08-28 캡컷 내보내기에 자막스타일·워터마크·꾸미기틀 반영(고객제보 '템플릿 안 따라온다'). 뿌리=caption_style/deco를 아예 안 넘김(참조 0건). 틀은 deco_frame PNG를 영상트랙에 얹어 해결(쌓임순서 영상<틀<자막). 되돌림 검증 완료 72 passed. 남은건 캡컷 실물확인·위치 좌표계 실측·폰트 동봉.

## 2026-08-30 캡컷 템플릿·자막스타일 재개통
08-28 긴급차단이 원인 오진이었음을 배포 이력으로 규명(고객 실패 3건은 스타일 코드가
라이브 되기 전에 발생, 스타일이 돌던 37분간 요청 0건). 진짜 결함 2개(조각 캐시 서명 누락
P1-3, 남의 PC 폰트 경로)를 고치고 기본 켬으로 복구. 실물 캡컷 6개 draft 재인식 확인, 73건 통과.
★교훈: auto_deploy는 고객 접속 중이면 웹 재시작을 연기한다 — 커밋 시각 ≠ 코드가 도는 시각.

## 2026-08-30 (2)
- 전구간 점검: 렌더엔 있고 캡컷엔 없던 머리카피·BGM·효과음·컷어웨이·장면확대 5종을 붙임.
- 판단은 렌더 함수 재사용(sfx_events_for·resolve_deco_media·headcopy_layer_png 추출). 테스트 75→83, 되돌림 확인 완료.
- 미해결: clip.transform 좌표계 미실측이라 캡컷 내 '위치'는 여전히 못 보낸다.

## 2026-09-02
- 고객 "캡컷으로 보내면 안된다" 조사 — 캡컷 프로젝트 69K/00:00 = JSON만 가고 재료(mp4·mp3 65MB) 미전송. 서버 조립본은 정상 실측.
- `sendToCapCut()`에 진행바(N/총계)·이탈경고·재료0개 중단·실패지점 표시 추가. node --check 통과, 브라우저 실동작은 미검증.
- ★진짜 원인 확정: fetch 응답 상태를 안 봐서 401/404 본문(24바이트)을 mp4로 저장 → 캡컷에 "폴더만 있고 빈 통". res.ok+최소크기 검사로 봉인, 실패 시 상태코드와 할 일을 고객 말로 표시.
- test_dom_owner_budget에 걸려 진행표시를 _ccState+_ccRender 하나로 정리(자리 하나에 그리는 함수 하나).
- ★진짜 원인 재확정: 서버 로그 capcut_asset 전부 200 = 재료는 다 갔다. draft의 절대경로를 표준값으로 '가정'한 것이 문제 — 고객이 캡컷 기본 폴더를 고르면 미디어를 못 찾아 빈 통. draft_meta_info.json의 draft_root_path를 읽어 실경로 탐지(_ccDetectRoot), 로컬 폴더 2곳 실측 통과.

## 2026-09-03 캡컷 "파일 손상됨 22개"
- 범인: ffmpeg가 소스 끝 너머 -ss에도 rc=0 + 262바이트 껍데기 → `size>0`이 성공이라 판정.
- mp3(copy)는 멀쩡하고 cut_*.mp4(ffmpeg)만 깨진 것이 원인을 갈랐다.
- `_playable()`(ffprobe 스트림 확인) 도입, 껍데기는 삭제 후 False. ZIP도 같은 함수라 동시 해결.
- 검증: 신설 3건(사전 실패 확인)·캡컷 회귀 78건·실제 조립 E2E 통과.

## 2026-09-03 (2) 캡컷 자막 바탕·크기
- 배경박스 필드가 0 고정이라 한 번도 안 갔다 → background_style/color/alpha 매핑. 글자 크기는 스크린샷 실측 1.7배 → 렌더 px÷8.0 상수 한 곳. 테스트 64건 통과, 실물 확인은 배포 후.
- (3) 캡컷 열어둔 채 재전송 → 파일 덮어쓰기 거부 에러. _ccWriteFile(지우고 재시도+캡컷 끄기 안내)로 봉인, node 4경로 검증.
- (3) 캡컷 켠 채 재전송 → 덮어쓰기 거부: _ccWriteFile 재시도+안내. 전송돼도 자막 그대로 → 캡컷이 같은 이름 프로젝트를 캐시로 되살려 덮어씀(실측). 프로젝트명에 HHMM 접미로 매번 새 프로젝트.
