# 발굴팔로워

## 2026-08-15
- 발굴 카드 "구독자 0" 제보 → **라벨이 아니라 데이터가 죽어 있었다**(라이브 피드 49건 전원 followers=0).
  원인: 프로필 조회만 구 단일 세션에 남아(검색은 08-10에 로테이션 이관 완료) 그 계정이 로그아웃 →
  프로필 페이지가 계정선택 화면으로 떨어져 graphql 캡처 0건. A/B: 구세션 실패 / ig_sessions 슬롯 1,846 정상.
- 고침: `_fetch_profiles_playwright`를 `_discover_session_proxy()` 짝(계정↔IP)으로 통일 + 회귀테스트 추가(13 passed).
- 카드 포맷을 레퍼런스(index.html)와 통일: `👥 팔로워 N`(없으면 `—`), `시간당댓글`·`팔로워당댓글`로 라벨 일치.
