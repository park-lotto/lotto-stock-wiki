# 대본믹스통합 — 작업 로그

- 2026-07-15 — [구현·검증] **영상제작소 대본·영상믹스 통합 SDD 5태스크 완료 + Critical 1건 발견·수정**: 제작소(`produce.html`)의 "1·대본"+"2·영상"을 하나의 "제작소"(좌=재료풀·우=결과)로 통합 완료. Task3 프론트=도서관 생성모달 `pm` 네임스페이스 이식(`3c074f82`, `openScriptModal` alert 스텁 교체) / Task4=`saveScriptToWiki` 배선(코드는 타세션 커밋 `74bd8a24`에 흡수됨, `git log -S`로 확인, post-commit 훅이 즉시 push해 히스토리 정리 불가·코드유실0) + I-A 수정(`6d33bf0c`, 422시 버튼 라벨 '⏳저장 중…' 영구고착). 전 태스크 opus 리뷰 통과(스펙✅/품질Approved/Critical0). **라이브 실측**(실제 즐겨찾기 경로, 가짜 핸드오프 아님): 📝뽑기 콜드추출 41s→200 실대본, 초안3개 제미니 리메이크, 확정, ⭐위키저장 200, 🎬담기2개→mixUrl. **리뷰가 잡은 I-1/I-2 수정**(`e0a1932f`): 핸드오프에 category가 아예 없어(`/api/mix/basket`이 shortcode·url·thumbnail·name·caption만 반환) 요소 드롭다운이 영구 "학습된 카테고리 없음"이고 백엔드가 조용히 free 폴백 — 서버엔 레시피 옵션 24개가 멀쩡히 있는데 제작소만 못 집던 배선 문제. 해법이 예상보다 작았음: `store.get_extract()`가 이미 category+structure 반환, `categorize(name,caption)`이 랭킹과 동일 함수, basket에 name·caption 있음 → **스키마·basket·collection.html 무변경**으로 `extract_from_url` 한 곳만 수정. exit proof: `PM_CATEGORY` ""→"레시피", `PM_BASE_STRUCT` null→구조5키, 드롭다운→🎲랜덤+지식 권위자·실전 검증자·현실 타겟·반전 인물. 🔴 **최종 whole-branch 리뷰가 Critical(C-1) 발견 — 개별 리뷰 4번과 라이브 grounding이 전부 통과시킨 것**: ⭐위키저장이 리메이크를 **원본 shortcode에 덮어써** 원본 대본 소실(캐시히트는 재추출 안 해 복구불가)+위키(S급 창고)에 우리 출력+**자기출력 학습루프**(`element_raw_values`=script_wiki ∪ script_extracts). **내 라이브 검증이 실제로 1건 일으킴**(`lens_instagram_1uwwsav` 원본"미쳤어요"→리메이크"끝내줘요", 서버DB 실측) — 앞서 "학습 반영 증명"이라 보고한 `element_category_stats` 24→23이 실은 **오염 코퍼스 학습 결과**였음. 사용자 결정으로 **위키저장=원본만**(도서관 `/api/wiki/save`와 동일 의미, 내 리메이크는 `script_drafts`에 있어 유실0)으로 수정(`0deb810c`, body.script_text/structure/segments 완전 무시, 리뷰 I-1 `pickScript`가 false로 안 돌아가 항상 첫 영상 저장하던 것도 `STATE.script_src_idx`로 수정, 버튼 문구 "이 원본 영상 위키에 담기"로 변경=사고의 UX적 뿌리 제거). 오염 1건은 백업 후 재추출 복구. **조사 결론**: 📝뽑기의 "네트워크 오류"는 코드결함 아님 — 콜드추출 41s 도중 타세션 커밋으로 auto_deploy가 서비스를 재시작해 요청이 끊겨 Apache 502 HTML→`r.json()` throw(타임스탬프 일치로 확정: auto_deploy `11:00:14 재시작완료` ↔ Apache `11:00:14.46 AH00898`, MainPID 2162696→2163698). 프록시 타임아웃·키 문제·JS 예외·백엔드 버그 4가설 반증. 잔여=analyze_structure 음성캐싱 없어 클릭마다 Gemini 재호출 / categorize 오분류가 DB 영구고착+교정경로 없음 / prompt 자유입력이 고아 학습버킷 / I-B 라벨 stale. 상세=`handoff/대본믹스통합.md`

- 2026-07-16 — [검증·수정·배포] **핸드오프 ⏭ 4건이 이미 끝나 있었고, 진짜 남은 1건(I-2)만 고쳐 배포**(`b792792f` → main `5907a24a`, 게이트 통과). ①**C-1 회귀확인**: 라이브 버튼을 안 누르고(틀렸으면 원본을 또 파괴) 코드 판독으로 확인 — `body.script_text`는 주석에만 있고 코드가 안 읽음, 캐시히트 시 `save_script` 미호출. 가드 6건 통과 + **뮤테이션으로 C-1 버그를 재주입하니 정확히 그 단언에서 죽음**(죽은 테스트 아님 증명). ②**오염(`lens_instagram_1uwwsav`)은 7/15에 이미 복구돼 있었음** — 서버 DB 실측 타임라인(KST): 11:04 오염(hook=권위인용형·characters=[요리 고수 언니]=AI가 끼운 주변인물) → 15:14 백업(=**오염본**이라 여기서 원본 복구 불가, 재추출만이 길) → 15:27 C-1 수정 → **15:31 재추출로 원본 회복**(hook=공감형·characters=[친구]) → 22:07 `element_category_stats` 레시피 **깨끗한 코퍼스로 재계산**. 추가 조치 불필요. 🔑함정: `script_extracts`엔 `full_text` 컬럼이 **없다**(텍스트는 `script_json` 안) → `.get('full_text')`가 None→"len=0"이라 **데이터가 빈 줄 오독**했다가 바로잡음. ③**I-B·I-4도 이미 반영돼 있었음**(`produce.html:1480` `!b.disabled` 가드 / `prompt()` 제거). ④**I-2 실체 규명·수정**: 지연(클릭당 120s×3)은 Task8이 BackgroundTasks로 이미 해소했고 **남은 건 비용** — 실패를 기록 안 해 구조분석 실패 영상은 클릭마다 백그라운드로 Gemini 재호출. → 실패 시 `structure_analyzed_at`만 찍고 `structure_json`은 NULL 유지(`store.mark_structure_attempted`), 대화형 경로는 재예약 중단, **재시도는 `daily_batch`가 인수**(`extracts_missing_structure`가 `structure_json IS NULL`로 골라 자격 유지). `{}` 저장=백필 영구제외라 계속 금지. 테스트 3건 신규·뮤테이션 2종 전부 죽음. ⑤**기준선 정직 확인**: 전체 10 failed는 내 수정을 stash한 HEAD에서도 동일 10건(apify_client·edit_plan_build·mix_pipeline·mix_render_flow) = 내 회귀 아님을 실측 증명. **⏭ 다음=I-3**(`categorize()`가 항상 문자열 반환→`or None`은 죽은 코드, 틀린 추측이 DB에 영구 고착·교정 경로 없음. **단 레퍼런스 랭킹과 공유 함수라 반환값 변경은 위험 → 설계 선택 필요, 사장님 확인 후 착수**). **🔑교훈 2개**: handoff ⏭를 믿고 착수하면 끝난 일을 또 한다(코드·DB 실확인 먼저) / 뮤테이션 되돌릴 때 `git checkout -- <파일>` 금지(미커밋 수정까지 날아가 app.py 재작업함).

- 2026-07-16 — [분리] I-3을 파보니 죽은 코드는 증상이고 병은 따로였음(키워드 분류 54%가 학습 코퍼스 다수 차지) → **별도 트랙 `카테고리분류`로 분리·구현·배포**(ac99a520 → main 53facafa). 이 폴더는 다른 세션이 "제작소 대본 재생성 배선"으로 쓰는 중이라 충돌 회피. 상세=handoff/카테고리분류.md
- [대본믹스통합] 2026-07-16 — [설계·구현·리뷰·병합] **제작소 mix탭 대본 재생성 배선 완료**(origin/main 4918f2b2 ← d241a75e, 커밋 10개). 사장님 지적("선택 1개 그대로 쓰기는 원본영상이랑 그대로 쓴다는건데 의미가 없다")에서 출발. ★정찰이 전제를 뒤집음: 즐겨찾기 생성패널이 **이미 produce.html에 pmModal로 이식돼 있었고**(7/15 Task3) 왼쪽 뽑기 경로에만 배선됨 → 신규개발 아니라 **배선**. 백엔드도 shortcode로 위키 구조를 읽는 폴백을 이미 보유 → **백엔드 수정 0**. T1 제미니자동 뿌리째 제거 / T2 pmModal 소재칸 이식(pmRunGen이 subject를 안 보내 제작소 리메이크는 소재가 안 고정됐음 — 소재고정 트랙 작업이 library.html에만 반영됐던 구멍) / T3 mix 1개선택→openGenFromWiki로 pmModal(useSingleScript 삭제) / T4 STATE.script_from_wiki로 위키담기 오귀속 차단. 게이트 10 failed/715 passed=기준선과 동일 실패명, 회귀 0. ★★**최종 whole-branch 리뷰가 개별리뷰 6번이 통과시킨 이음새 2건을 잡음** = "최종리뷰 생략금지"의 재증명. I-1: openScriptModal이 10~30초 extract를 await하는데 continuation이 모달 주인 확인 없이 PM_* 재료를 덮어씀 → 뽑기 대기중 mix로 넘어가면 재료가 남의 영상 것이 되고 **설계가 명시 금지한 pmUseRaw가 mix 모달에 등장**. T3 리뷰가 "양쪽이 진입 시 전역 7개 전부 재설정하니 오염 불가"로 통과시켰으나 **동기 진입에 한해서만 맞았음**. I-2: _currentSrcItem 소비자가 둘인데 T4가 하나만 막아 카테고리 교정이 무관한 영상의 category를 DB에 영속 기록. ★내 계획서 구멍: 확정 경로를 넷이라 세었으나 다섯째(세션복원)가 있어 **새로고침 한 번이면** T4가 막으려던 오귀속이 되살아났음(태스크 리뷰가 잡음). ★교훈 재확인: "테스트 통과"≠"테스트가 결함을 잡음"이 두 번 남 — 경합가드를 통째로 지워도 3테스트 통과 / text.count 문자열카운트라 대입줄을 **삭제 아닌 주석처리**하면 기능 죽어도 통과. **뮤테이션은 삭제뿐 아니라 주석처리도 해볼 것**. ★CLAUDE.md 문구 오류 발견: "트랙 폴더 안에선 git add -A도 안전"은 틀림 — raw/ 크롤데이터가 딸려옴("남의 코드가 없다"는 뜻이지 "raw/가 없다"가 아님). 🚨**라이브엔 아직 안 감** — 서버 배포 막힘 실측(HEAD 1b907d7d 정지, 로그에 "pull실패(작업트리충돌?) 스킵-수동확인필요"). SessionEnd 훅이 git add . 으로 raw까지 main에 push → 크롤봇이 raw를 쓰는 서버가 pull 거부(트랙격리 페이즈3 소관). 병합은 안전, 배포 풀리면 자동반영. **육안검증 5건 미실시**(백엔드 폴백이 실제로 도는지·새로고침 후 버튼 상태 등) — 완료 처리 안 함.

## 2026-07-16 (밤) — 라이브 육안검증 4/5 통과
- 배포 막힘은 이미 해소(서버 HEAD=3349db35, 병합본 4918f2b2 포함, 재기동 15:31 > 병합 14:30).
- 위키폴백 E2E 실증(structure:null → 200/3.4s). 단 extracts 49건 중 wiki에 있는 건 13건(27%)뿐 → I-1 잔여피해는 73%에서 실재했을 것.
- T4 Critical 실증 + 반사실(플래그만 null→버튼 등장)로 죽은테스트 아님 확정.
- 검증5(뽑기 대기 중 mix 진입)는 extract 요청 0건 = continuation 미발생 → 재현실패로 남김.

## 2026-07-16 (밤) — 사장님 제보 2건 처리·배포
- ① 뽑기가 "위키에 저장하라"는 정반대 안내를 하던 결함 수정(20cce5a8). 원인=모달을 먼저 열고 재료를 10~70초 기다리는 동안 초안생성 버튼이 열려 있어 재료 없이 404. 그날 추출실패 근인은 Gemini 503(우리 코드 아님). 뮤테이션 3종 죽음.
  🔑 함정: "소재 감지 중…"은 정적 HTML placeholder(produce.html:418)라 실행 증거가 아니다 — 오독하면 원인을 못 찾는다.
- ② 링크 직접 등록 구현(f6bedc4f). 또 배선이었다 — extract_from_url(자동추론) → 통제어휘 교정 → save_to_wiki(캐시히트). 백엔드 신규 0.
  ★하중 가정: 두 라우트가 sha1(url)[:12]를 각자 적어둔 같은 식이라 캐시가 맞는다. 어긋나면 조용히 비용 2배.
- 미검증: 링크 등록 라이브 E2E(Gemini 503으로 못 봄).
