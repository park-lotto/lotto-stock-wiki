# 제작소총점검 — 작업 로그

## 2026-08-19
사장님 "제작소 반복오류 총점검" 지시. 라이브 DB·로그 실측 후 지시서 작성
(`handoff/제작소총점검_지시서.md`) + 1~3순위 직접 수정.
① EDL 빈 원인을 4가지로 갈랐다(종전엔 "추출 실패 또는 키 소진"으로 뭉갬 — 실측 13건
   대부분이 **추출 9,091자 성공** 상태였다) ② 배경작업(durfill 등) 중단을 '고장'에서
   분리(43건 오탐이 오류표 1위로 진짜 고장을 가림 — durfill은 done 248/24h 732건 정상
   동작 중) ③ 다운로드 실패를 사람 말로 번역(/404 리다이렉트 = 주소가 아니라 원본 삭제).
★③은 처음에 담기 차단으로 잘못 접근했다가 기존 테스트에 걸려 되돌렸다 —
`rednote.com/search_result/`는 정상 경로였다. 테스트가 옳았다.
검증: 신규 20개 + 전체 4,675 passed(1 실패는 기존부터 깨져 있던 edit_plan 건).
