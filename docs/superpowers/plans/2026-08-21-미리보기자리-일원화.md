# 3단계 미리보기 자리 일원화 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 3단계(영상대본MIX)에서 9:16 재생 상자가 **화면에 하나만** 있게 하고, 🎬확정 결과가 ▶️전체재생과 **같은 자리**에 나오게 한다.

**Architecture:** 자리를 차지할 수 있는 것은 두 가지다 — ①가조립 재생기(`#player`, scene_play.js) ②확정 결과 영상(`#mixPreview`). 지금은 이 둘을 껐다 켜는 판단이 **5곳에 흩어져** 있어 확정 경로만 구멍이 났다. 판단처를 `_pvShow(which)` **함수 하나**로 모으고(0순위-B), 모든 경로가 그것만 부르게 한다. iframe 안 중복 재생기(`#playerhost`)는 임베드일 때 숨긴다 — 이미 `data-embed-hide` 표식이 붙어 있으나 **읽는 코드가 없어** 죽어 있다(실측: 코드베이스 전수검색 1건 = 속성 그 줄뿐).

**Tech Stack:** 바닐라 JS (produce.html / scene_lab.html / scene_play.js), pytest + node JS 하네스(`shopping_shorts/tests/js_harness.py`)

---

## 배경 — 왜 상자가 두 개로 보이나 (실측)

사장님 제보: "미리보기 전체재생을 다 하고 믹스확정 버튼을 누르잖아. 그럼 그 미리보기 자리에서 믹스확정이 돌아가게 하면 안 되냐."

현재 자리 스위치 **5곳**(전부 실측 확인):

| # | 위치 | 하는 일 |
|---|---|---|
| ① | `produce.html:6603-6608` `_sceneLabPrep` | 가조립 켤 때 `#mixPreviewPanel` 열고 `#mixPreview`를 **끈다**. ★레일(`#mixPreviewRail`)은 안 연다 |
| ② | `produce.html:6659-6662` `sceneLabPlayClosed` | 재생 닫히면 `#mixPreview`를 **켠다**(내용 있을 때만) |
| ③ | `scene_play.js:564` | `#player`에 `.on`을 **켠다** |
| ④ | `scene_play.js:423-424` | `#player`에서 `.on`을 **끄고** ②를 부른다 |
| ⑤ | `produce.html:7350-7352` `startPreview` | `_pvOpen()` 부르고 `#mixPreview.innerHTML`에 로더를 쓴다 — ★①이 꺼둔 `display:none`을 **되돌리지 않는다** |

**⑤가 구멍이다.** 가조립을 한 번이라도 재생하면 ①이 `#mixPreview`를 `display:none`으로 꺼놓는데, 그 뒤 🎬확정을 누르면 `startPreview`는 `innerHTML`만 채우고 display를 안 켠다. `_pvOpen()`은 레일과 패널만 열 뿐 `#mixPreview` 자신은 안 건드린다(`produce.html:4287-4290` 실측).

또 하나: iframe(`scene_lab.html:383`)의 `#playerhost`가 안 숨겨져 **같은 화면에 9:16 상자가 위아래 두 개**로 보인다.

**사장님 결정(2026-08-21):** "확정 결과로 갈아끼운다" — 같은 자리를 번갈아 쓴다. ▶️전체재생 → 가조립(공짜) / 🎬확정 → ⏳로더 → 진짜 mp4.

---

## File Structure

| 파일 | 책임 | 변경 |
|---|---|---|
| `shopping_shorts/static/produce.html` | 자리 주인 판정처(`_pvShow`) 신설, 5곳을 그것으로 교체 | 수정 |
| `shopping_shorts/static/scene_lab.html` | 임베드일 때 자기 재생기 숨김 | 수정 |
| `shopping_shorts/tests/test_preview_seat_js.py` | 새 규약을 pytest가 강제(js_harness 사용) | 신규 |
| `shopping_shorts/tests/test_preview_visible.js` | 기존 5단언 유지 + 파이썬 래퍼가 없어 게이트에서 안 돌던 것을 위 py가 흡수 | 수정 없음(호출만 추가) |

⚠️ `scene_play.js`는 **건드리지 않는다.** ③④는 `#player`의 `.on` 클래스만 만지고 `#mixPreview`는 안 만진다 — 자리 판정과 무관하다. ④가 부르는 `sceneLabPlayClosed`만 갈아끼우면 된다.

---

### Task 1: 자리 판정처 `_pvShow` 신설 (테스트 먼저)

**Files:**
- Create: `shopping_shorts/tests/test_preview_seat_js.py`
- Modify: `shopping_shorts/static/produce.html` (`_pvOpen`/`_pvClose` 바로 아래, 4294행 뒤)

- [ ] **Step 1: 실패하는 테스트를 쓴다**

`shopping_shorts/tests/test_preview_seat_js.py` 새로 만든다:

```python
"""3단계 미리보기 '자리'는 하나다 — 주인 판정처가 한 곳인지 강제한다(2026-08-21).

사장님 제보: "미리보기 전체재생 다 하고 믹스확정 누르잖아. 그 미리보기 자리에서
믹스확정이 돌아가게 하면 안 되냐."

실측 원인: 자리를 껐다 켜는 판단이 5곳에 흩어져 startPreview 경로만 구멍이 났다
(_sceneLabPrep이 #mixPreview를 display:none으로 꺼두는데 startPreview가 안 되돌림).
0순위-B(같은 판단 두 번 쓰기 금지) 그대로라 판정처를 _pvShow 하나로 모았다.
"""
import pathlib

from shopping_shorts.tests.js_harness import requires_node, run_js

pytestmark = requires_node

_HTML = pathlib.Path(__file__).resolve().parents[1] / "static" / "produce.html"


def _code():
    """주석을 걷어낸 produce.html 본문 — 주석 속 문구에 정규식이 걸리지 않게."""
    src = _HTML.read_text(encoding="utf-8")
    return "\n".join(l for l in src.split("\n") if not l.strip().startswith("//"))


def test_자리_판정처가_하나다():
    code = _code()
    assert "function _pvShow(" in code, "_pvShow 판정처가 없다"


def test_확정_경로가_판정처를_쓴다():
    """startPreview는 자기가 display를 만지지 말고 _pvShow('video')를 불러야 한다."""
    code = _code()
    body = code.split("async function startPreview()")[1].split("\nasync function")[0]
    assert "_pvShow('video')" in body, "startPreview가 _pvShow('video')를 안 부른다"


def test_가조립_경로가_판정처를_쓴다():
    code = _code()
    body = code.split("function _sceneLabPrep(")[1].split("\nfunction ")[0]
    assert "_pvShow('play')" in body, "_sceneLabPrep이 _pvShow('play')를 안 부른다"
    assert "style.display" not in body, "_sceneLabPrep이 아직 display를 직접 만진다"


def test_판정처_밖에서_자리를_직접_안만진다():
    """_pvShow 정의 안을 뺀 나머지에서 #mixPreview의 display를 직접 만지면 안 된다."""
    code = _code()
    head, rest = code.split("function _pvShow(", 1)
    outside = head + rest.split("\n}", 1)[1]
    assert "pv.style.display" not in outside, "_pvShow 밖에서 #mixPreview display를 만진다"


def test_기존_가시성_규약도_그대로다():
    """test_preview_visible.js는 파이썬 래퍼가 없어 게이트에서 안 돌았다 — 여기서 돈다."""
    js = _HTML.parent.parent / "tests" / "test_preview_visible.js"
    out = run_js(js.read_text(encoding="utf-8"))
    assert "FAIL 0" in out, out
```

- [ ] **Step 2: 실패를 확인한다**

```
cd "C:/Users/CH/Desktop/로또의 주식/.tracks/미리보기일원화"
py -m pytest shopping_shorts/tests/test_preview_seat_js.py -v
```

기대: `test_자리_판정처가_하나다`·`test_확정_경로가_판정처를_쓴다`·`test_가조립_경로가_판정처를_쓴다`·`test_판정처_밖에서...` **4건 FAIL**(_pvShow 없음), `test_기존_가시성_규약도_그대로다` **PASS**.

⚠️ 마지막 1건이 처음부터 PASS인 것은 정상이다 — 기존 규약은 이미 지켜지고 있고, 이 테스트는 게이트에서 안 돌던 것을 끌어온 것뿐이다(회귀 방지용).

- [ ] **Step 3: `_pvShow`를 만든다**

`produce.html` 4294행 `function _pvClose(){...}` 바로 **아래**에 붙인다:

```javascript
// ★자리의 주인을 정하는 곳은 여기 하나다(2026-08-21 사장님 "그 미리보기 자리에서
//   믹스확정이 돌아가게 하면 안 되냐"). 9:16 상자는 화면에 **하나**고, 그 자리에
//   들어설 수 있는 건 둘 중 하나뿐이다:
//     'play'  = 가조립 재생기(#player, scene_play.js가 .on으로 켠다) — 공짜
//     'video' = 확정 결과 mp4(#mixPreview) — 서버 렌더 20초+
//   예전엔 이 판단이 5곳에 흩어져 있었고, 그래서 startPreview 경로만 구멍이 났다:
//   _sceneLabPrep이 #mixPreview를 display:none으로 꺼두는데 startPreview는 innerHTML만
//   채우고 display를 안 되돌려, **가조립을 한 번이라도 재생한 뒤엔 확정 결과가 안 보였다**.
//   (0순위-B: 같은 판단을 두 번 적으면 반드시 어긋난다)
function _pvShow(which){
  _pvOpen();                                   // 레일·패널은 늘 함께 연다(정의는 _pvOpen 한 곳)
  const pv = document.getElementById('mixPreview');
  if (pv) pv.style.display = (which === 'video') ? '' : 'none';
  const hint = document.getElementById('mixPreviewHint');
  if (hint) hint.style.display = 'none';
}
```

- [ ] **Step 4: 1번 단언이 통과하는지 본다**

```
py -m pytest shopping_shorts/tests/test_preview_seat_js.py::test_자리_판정처가_하나다 -v
```

기대: PASS. 나머지 3건은 아직 FAIL(호출부를 안 바꿨다).

- [ ] **Step 5: 커밋**

```bash
git add shopping_shorts/tests/test_preview_seat_js.py shopping_shorts/static/produce.html
git commit -m "미리보기 자리 판정처 _pvShow 신설 — 5곳 흩어진 판단을 한 곳으로(0순위-B)"
```

---

### Task 2: 확정 경로(`startPreview`)를 판정처에 태운다

**Files:**
- Modify: `shopping_shorts/static/produce.html:7350` (`startPreview` 안)

- [ ] **Step 1: 호출을 바꾼다**

`produce.html:7350`의 이 줄을

```javascript
  if(typeof _pvOpen==='function') _pvOpen();   // 좌측 프리뷰 자리 확보(레일까지 — 안 열면 렌더는 되는데 0x0이라 안 보인다)
```

이렇게 바꾼다:

```javascript
  // ★자리를 '확정 결과'로 넘겨받는다 — 가조립을 재생한 뒤였다면 #mixPreview가 꺼져 있어
  //   로더를 innerHTML에 써도 안 보였다(2026-08-21 사장님 제보의 직접 원인).
  if(typeof _pvShow==='function') _pvShow('video');
```

- [ ] **Step 2: `_renderPreviewVideo`도 같이 태운다**

`produce.html:4297`의

```javascript
  if(typeof _pvOpen==='function') _pvOpen();
```

를 바꾼다:

```javascript
  if(typeof _pvShow==='function') _pvShow('video');
```

⚠️ 새로고침 복원도 이 함수를 타므로(주석 4380행 "새로고침 복원과 단일 출처"), 복원 경로도 자동으로 같은 규약을 얻는다.

- [ ] **Step 3: 테스트를 돌린다**

```
py -m pytest shopping_shorts/tests/test_preview_seat_js.py -v
```

기대: `test_확정_경로가_판정처를_쓴다` PASS 추가. `test_가조립_경로가...` 는 아직 FAIL.

- [ ] **Step 4: 커밋**

```bash
git add shopping_shorts/static/produce.html
git commit -m "확정 미리보기가 자리를 넘겨받게 — 가조립 재생 뒤 결과가 안 보이던 것 수리"
```

---

### Task 3: 가조립 경로(`_sceneLabPrep`·`sceneLabPlayClosed`)를 판정처에 태운다

**Files:**
- Modify: `shopping_shorts/static/produce.html:6603-6608`, `:6659-6662`

- [ ] **Step 1: `_sceneLabPrep` 끝의 4줄을 한 줄로 바꾼다**

`produce.html:6603-6608`의

```javascript
  const pvPanel = document.getElementById('mixPreviewPanel');
  if (pvPanel) pvPanel.style.display = '';
  const pvHint = document.getElementById('mixPreviewHint');
  if (pvHint) pvHint.style.display = 'none';
  const pv = document.getElementById('mixPreview');
  if (pv) pv.style.display = 'none';
```

를 이렇게 바꾼다:

```javascript
  _pvShow('play');   // 자리를 가조립 재생기에 넘긴다(판정은 _pvShow 한 곳)
```

★이 교체로 **레일도 함께 열린다** — 예전엔 패널만 열고 레일(`#mixPreviewRail`)은 안 열어, `_pvOpen`과 판단이 갈려 있었다.

- [ ] **Step 2: `sceneLabPlayClosed`를 바꾼다**

`produce.html:6659-6662`의

```javascript
function sceneLabPlayClosed(){
  const pv = document.getElementById('mixPreview');
  if (pv && pv.innerHTML.trim()) pv.style.display = '';
}
```

를 이렇게 바꾼다:

```javascript
// 가조립 재생을 닫으면 자리를 되돌린다 — **확정 결과가 이미 있을 때만**.
// 없으면(아직 확정 전) 자리를 비워 두는 게 맞다: 빈 <div>를 켜봐야 보여줄 게 없다.
function sceneLabPlayClosed(){
  const pv = document.getElementById('mixPreview');
  if (pv && pv.innerHTML.trim()) _pvShow('video');
}
```

- [ ] **Step 3: 테스트를 돌린다**

```
py -m pytest shopping_shorts/tests/test_preview_seat_js.py -v
```

기대: **5건 전부 PASS.**

- [ ] **Step 4: 기존 가시성 테스트도 여전히 통과하는지 직접 확인**

```
node shopping_shorts/tests/test_preview_visible.js
```

기대: `결과: PASS 5 / FAIL 0`, rc=0.

⚠️ 이 테스트의 마지막 단언("정의 밖에서 패널을 직접 만지지 않는다")은 `_pvOpen` 정의 앞부분과 `_renderPreviewVideo` 뒷부분만 검사한다 — `_sceneLabPrep`은 그 사이에 있어 원래 안 걸렸다. 그래서 Task 1의 `test_판정처_밖에서_자리를_직접_안만진다`가 그 빈틈을 메운다.

- [ ] **Step 5: 커밋**

```bash
git add shopping_shorts/static/produce.html
git commit -m "가조립 재생도 자리 판정처를 쓰게 — 레일이 안 열리던 어긋남도 함께 해소"
```

---

### Task 4: iframe 안 중복 재생기 숨기기 (상자 두 개의 직접 원인)

**Files:**
- Modify: `shopping_shorts/static/scene_lab.html:383` 부근 + 임베드 판정 스크립트

- [ ] **Step 1: 실패하는 테스트를 쓴다**

`shopping_shorts/tests/test_preview_seat_js.py` **끝에** 붙인다:

```python
def test_임베드일때_iframe_재생기를_숨긴다():
    """제작소 안(iframe)에선 scene_lab 자기 재생기를 숨긴다 — 안 그러면 9:16 상자가 둘.

    data-embed-hide 표식은 2026-08-16부터 마크업에 있었지만 **읽는 코드가 없어**
    죽어 있었다(실측: 코드베이스 전수검색 1건 = 속성 붙은 그 줄뿐).
    """
    lab = (_HTML.parent / "scene_lab.html").read_text(encoding="utf-8")
    code = "\n".join(l for l in lab.split("\n") if not l.strip().startswith("//"))
    assert "data-embed-hide" in code, "표식이 사라졌다"
    assert "querySelectorAll('[data-embed-hide]')" in code, "표식을 읽는 코드가 없다"
    assert "window.parent !== window" in code, "임베드 판정이 없다"
```

- [ ] **Step 2: 실패를 확인한다**

```
py -m pytest shopping_shorts/tests/test_preview_seat_js.py::test_임베드일때_iframe_재생기를_숨긴다 -v
```

기대: FAIL — `표식을 읽는 코드가 없다`.

- [ ] **Step 3: 표식을 읽는 코드를 넣는다**

`scene_lab.html`에서 `<script src="/scene_play.js"></script>`(437행)와 그 다음 `<script>` 블록 **사이**에 새 `<script>`로 붙인다.

⚠️ 437행 바로 다음 줄은 `<script>` **여는 태그**다(그 안은 "한 파일 두 모드" 로직). 그 안에 끼워 넣지 말고 독립 블록으로 넣어라 — 아래 코드는 그 자체로 완결된 `<script>`다:

```html
<script>
// ★제작소(iframe) 안에서는 이 화면의 재생기를 숨긴다(2026-08-21 사장님 "왜 하나가 더 있나").
//   재생은 이미 **바깥 큰 자리**에서 난다 — playAllHere/playBeatHere가 부모의
//   sceneLabPlayAll/sceneLabPlay를 직접 부르기 때문이다(이 파일 위쪽 참조).
//   그래서 여기 재생기는 자리만 차지하고 아무도 안 쓴다 = 9:16 상자가 두 개로 보였다.
//   data-embed-hide 표식은 2026-08-16부터 마크업에 있었으나 **읽는 코드가 없어** 죽어 있었다.
//   ⚠️단독으로 열었을 때(window.parent === window)는 이 화면이 유일한 재생기다 — 숨기면 안 된다.
(function(){
  if (window.parent === window) return;            // 단독 열기 — 그대로 둔다
  document.querySelectorAll('[data-embed-hide]').forEach(function(el){
    el.style.display = 'none';
  });
})();
</script>
```

- [ ] **Step 4: 테스트를 돌린다**

```
py -m pytest shopping_shorts/tests/test_preview_seat_js.py -v
```

기대: **6건 전부 PASS.**

- [ ] **Step 5: 커밋**

```bash
git add shopping_shorts/static/scene_lab.html shopping_shorts/tests/test_preview_seat_js.py
git commit -m "임베드 시 iframe 자체 재생기 숨김 — data-embed-hide가 미구현이라 상자가 둘이었다"
```

---

### Task 5: 브라우저 실측 (정적 검토는 관찰이 아니다)

**Files:** 없음 — 라이브 확인

⚠️ CLAUDE.md: "HTML은 반드시 브라우저에서 열어 확인 — 정적 검토는 관찰이 아니다." 테스트가 6건 PASS여도 이 단계 전엔 **완료라고 말하지 않는다**.

- [ ] **Step 1: 로컬 서버를 띄운다**

```
cd "C:/Users/CH/Desktop/로또의 주식/.tracks/미리보기일원화"
py -m uvicorn shopping_shorts.app:app --port 8899
```

⚠️ 함정 2개(메모리 실측): ①옛 uvicorn이 8899를 잡고 있으면 **옛 코드가 응답**한다 — 띄우기 전 포트를 확인해라 ②`DASH_PASS`가 비어 있어야 401이 안 뜬다.

- [ ] **Step 2: 3단계를 연다**

`http://localhost:8899/produce?work=<기존 work id>` 에서 3단계(영상대본MIX)로 간다.

- [ ] **Step 3: 눈으로 확인할 것 4가지**

```
□ 9:16 검은 상자가 화면에 **하나**만 보인다 (예전엔 위아래 두 개)
□ ▶️전체 재생 → 그 자리에서 가조립이 돈다
□ 🎬대본×영상믹스 확정 → **같은 자리**가 ⏳로더로 바뀐다
   ★이게 이번 수리의 핵심이다. 예전엔 가조립을 한 번 재생한 뒤엔 로더가 안 보였다
□ 렌더가 끝나면 그 자리에 mp4가 뜨고 재생된다
```

- [ ] **Step 4: 재발 조건을 일부러 밟아본다**

원래 사고 순서 그대로: **▶️전체 재생 → (닫지 않고) 🎬확정**. 예전엔 이 순서에서 `#mixPreview`가 `display:none`인 채라 로더가 안 보였다. 지금은 보여야 한다.

- [ ] **Step 5: 스크린샷을 남기고 커밋**

확인된 화면을 사장님께 보고한다. 코드 변경이 없으면 커밋할 것도 없다.

---

### Task 6: 좌측 레일 폭 — 사장님 확인 후 결정

**Files:** 미정 (`produce.html:1755` `#mixPreviewRail`)

⚠️ **이 태스크는 Task 5 보고 뒤 사장님 판단을 받고 착수한다.** 지레 하지 마라.

상자가 하나가 되면 3단계 가로 배치가 달라진다. `#mixPreviewRail`이 `var(--shorts-pv-w,440px)`를 고정으로 먹는데, iframe 안 `#playerhost`(같은 440px)가 사라지면 장면 편집이 그만큼 넓게 쓸 수 있다.

- [ ] **Step 1: Task 5 스크린샷을 사장님께 보이고 묻는다**

"상자가 하나가 됐습니다. 남은 440px을 장면 편집이 쓰게 넓힐까요, 지금 배치가 편하신가요?"

- [ ] **Step 2: 지시가 오면 그때 계획을 잇는다**

⚠️ `--shorts-pv-w`의 정의처는 `scene_play.js:23` `PV_W` **한 곳**이다(0순위-B). 폭을 바꾼다면 거기서 바꾸고, 마크업에 숫자를 다시 적지 마라 — 2026-08-19에 마크업↔복귀부가 갈려 사고가 났다.

---

## 완료 기준

```
□ py -m pytest shopping_shorts/tests/test_preview_seat_js.py -v   → 6 passed
□ node shopping_shorts/tests/test_preview_visible.js              → PASS 5 / FAIL 0
□ 브라우저 실측 4항목(Task 5 Step 3) 전부 눈으로 확인
□ ▶️전체재생 → 🎬확정 순서에서 로더가 보인다(재발 조건, Task 5 Step 4)
□ py tools/track.py finish 미리보기일원화
   ★exit 0이어도 병합 아닐 수 있다 → git branch -r --contains <커밋>으로 판정
□ handoff/미리보기일원화.md + wiki/log.d/미리보기일원화.md 기록
```

## 손대지 않는 것

- `scene_play.js` — `.on` 클래스만 만지고 `#mixPreview`는 안 만진다. 자리 판정과 무관하다.
- `#player`/`#vidbox`/`#subbox` 등 id — scene_play.js가 참조한다. 지우면 그 코드가 죽는다.
- 썸네일(7단계) 프레임 추출 — 별개 기능이다. 이번 조사에서 확인했듯 자막 없는 배경을 따로 쓰므로 미리보기와 합칠 수 없다.
