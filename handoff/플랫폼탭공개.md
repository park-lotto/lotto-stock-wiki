# 플랫폼탭공개 — 랭킹 플랫폼 탭 공개 + 네이버클립 벤치채널 확장

## 2026-09-08 (사장님 지시)

### 지시
- "메인화면에 핀터레스트는 왜없앴나. 모두 공개로해라"
- "네이버클립 벤치시트 시트 추가해서 이것도 카테고리 개방"
- (보고 뒤) "틱톡·샤오홍슈·도우인 3개는 다시 닫는다"

### 완료
1. **플랫폼 탭 노출 = 5개** — 인스타·유튜브·쓰레드·핀터레스트·네이버클립.
   핀터레스트·네이버클립은 `admin-only` 클래스도 뗐다(고객에게도 보인다).
   틱톡·샤오홍슈·도우인은 `display:none` 유지 — 실데이터 0, demo_ 더미 8건뿐.
   계약 이력은 `shopping_shorts/tests/test_ranking_platform_tabs.py` 맨 위에 누적.
2. **`_NAVERCLIP_BENCH_CHANNELS` 15개 → 100개** (`app.py`).
   사장님 엑셀(`네이버클립벤치시트_2609.xlsx`, 101행)의 naver.me 단축링크를
   서버(한국 IP)에서 따라가 `link.naver.com/bridge?url=...clip.naver.com/@<핸들>`에서
   핸들 추출 → 99/101 성공. 표본 10개 프로필 조회 전부 OK.
3. `collect_channels`의 `handles[:60]` → `[:120]`. 안 늘리면 뒤 40채널이 조용히 잘린다.

### 못 한 것 / 확인 필요
- **리뷰매니아** 채널: 시트 URL이 `https://naver.me/IMZCc2니` — 끝에 한글이 붙은 **오타**라
  못 넣었다. 사장님께 링크를 다시 받아야 한다.
- 시트 마지막 행은 채널이 아니라 gemini 링크 메모 → 제외(정상).
- **라이브 실측 미완료**: 지금(20:5x KST)은 자동배포 시간창(2~6시) 밖이라 서버에 아직
  안 올라갔다. 사장님이 "새벽까지 기다린다"고 하셨다.
  → 다음 세션 할 일: 배포 반영 확인 후 ①랭킹 화면에서 5개 탭이 보이는지
    ②네이버클립 채널 수집을 눌러 100채널이 실제로 걷히는지 실측.
- **서버 워킹트리에 커밋 안 된 수정 5개 파일 239줄**(`bank_assemble.py`,
  `product_facts.py`, `script_gate.py`, `script_generate.py`, `store.py`)이 남아 있다.
  누구 작업인지 몰라 사장님 지시대로 **그대로 뒀다**.
  ⚠️이게 남아 있으면 `git pull --ff-only`가 실패해 **모든 배포가 통째로 막힐 수 있다**.
  새벽 배포가 안 됐으면 여기부터 봐라(`/tmp/auto_deploy.log`에 `pull실패` 여부).

### 별건(같은 세션 조사만 함, 미수정)
고객 제보 2건 — 원인은 코드에서 확인, 수정은 사장님 승인 대기:
- 필름 ⬆적용이 기존 컷을 **교체하지 않고 추가**한다(`scene_lab.html` commitRoll `l.push`).
  그래서 박스를 다시 잘라 적용하면 옛 긴 컷이 남아 전체재생이 옛것으로 보인다.
  (교체는 🔁 `replaceRoll`). 고칠 곳: 겹치는 film_ 조각이 있으면 추가 대신 교체.
- 15인치 노트북에서 필름 하단이 잘린다 — 촘촘히 모드 `#topright{overflow:hidden}` +
  `#rollbay .frwin` 고정높이. 지금 우회는 브라우저 축소(Ctrl+-) 또는 [⛶넓게 보기].
