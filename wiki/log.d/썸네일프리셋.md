# 썸네일프리셋 트랙 로그

## 2026-09-01
썸네일 편집칸을 재료 3탭으로 정리 · ⭐내 프리셋(계정별 settings 키에 JSON, 상한 24) 저장·적용·삭제 ·
🖼 인트로 체크를 개인 마지막값으로 기억(작업값 우선, 없으면 기본값 — 판단은 화면 한 곳). 테스트 7건 사보타주 확인.

## 2026-09-02
- 7단계 [썸네일 생성] 바로 아래 "🖼 내 프리셋" 신설 — 만든 구성(글자·스티커·도형·배지)을
  담아 다음 영상에 딸깍 한 번에 얹는다. 배경은 안 담는다(다른 영상에 쓸 것이라).
- 서버 저장(계정별): `thumb_presets` 테이블 + 라우트 5개. 그림은 job 폴더가 아니라
  `data/thumb_presets/`에 따로 — job 폴더는 disk_cleanup이 지운다.
- ★기존 HC_PRESETS('한 번에 스타일 적용')와 **다른 물건**: 저건 글자 1개의 색, 이건 구성 전체.

## 2026-09-05
- 썸네일 편집칸 정리(사장님 "산만하다"): ✨강조효과에서 **붓 밑줄 제거**(밑줄·형광펜·네온 3개),
  😀스티커의 **직접입력 칸 제거**.
- ★중복 '내 프리셋' 하나로 통합 — 09-01 글자스타일판과 09-02 구성전체판이 **같은 id
  (myThumbPresets)·같은 변수명(MY_THUMB_PRESETS)**을 써서, 병합되는 순간 `let` 재선언
  SyntaxError로 **제작소 스크립트가 통째로 죽는 자리**였다. 구성전체판(서버 thumb_presets)만 남겼다.
- 썸네일 프리셋 11종 → **21종**. 추가분은 색만 다른 게 아니라 틀이 다르다(색박스 4 · 색외곽선 3 ·
  단색임팩트 2 · 하단큰자막 1). ★HC_PRESETS 배열 **끝에** 붙였다(applyPreset이 raw 인덱스를 쓴다).
- ⚠️병합 중 app.py에 충돌마커가 커밋될 뻔했다(tail -3만 보고 add -A) — 렌더 재요청 구간
  양쪽(인트로 기본값 + video_path="" 무효화)을 다 살려 해소. out/…신청폼_갱신.gs의 마커는
  origin/main에 이미 있던 것으로 같이 정리.
