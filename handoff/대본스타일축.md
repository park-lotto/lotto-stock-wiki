> **소유 트랙**: 대본스타일축 — 이 파일은 이 트랙 세션만 수정한다. 다른 트랙은 읽기만.
> 트리거: "대본스타일축 이어서" / "대본 스타일 축 계속" / "새 대본 틀 만들자"

# 대본 스타일 축 발굴 (2026-09-05, 회사PC)

사장님 지시: **"대본 스타일을 많이 만들고 스타일 템플릿도 지금처럼 하고 내가 볼 수 있게 계속 만들어봐"**
→ 라이브 반영 없이 **로컬 문서로만** 만든다. 배선·등록은 다른 세션.

---

## ⏭ 다음에 할 일 (집에서 이어서)

### 1. 나머지 축의 전문+템플릿 만들기 ← 여기부터
`out/대본틀_전문과템플릿_7종.html`에 7개까지 만들었다. 남은 것:
```
□ 셀프 반박형 (25편)      — 파스타 쿠커 "에이 전자레인지로 돌리면 면 불어서 맛없는 거 아님?"
□ 제조사도 몰랐던 쓰임 (22편) — 타공기 "원래는 가죽 구멍 뚫을 때 쓰던"
□ 회사 실수 대박형 (6편·경계) — 씨앗연필 217만
□ 의문 훅 (320편)         — 스위치라 독립 틀이 아님. 다른 틀에 얹는 법으로 정리
□ 관찰자 어미 (81편)       — 스위치
□ 어디서 샀냐고 (63편)      — 스위치(마무리 칸). 01번 틀에 이미 들어있음
```

### 2. 조회수와 연결 (아직 못 했다)
지금 12축은 **"많이 쓰인다"이지 "잘 된다"가 아니다.** script_wiki 대부분에 조회수가 안 붙어 있다.
붙이는 법: `reel_history.shortcode` 또는 `source_enrichment.url`로 조인(양쪽 다 커버율이 낮다).

### 3. 트렌드 필터 (미착수)
`reel_history.upload_ts`는 **전부 비어 있다**. 대신 `source_enrichment.upload_date`는 7,154건 있다.
그걸로 "최근 3개월"만 좁혀 다시 재면 트렌드 반영이 된다.

### 4. templates 문장까지 대조 (미완)
기존 44개와는 **이름·situation_type·beat_roles로만** 대조했다.
`templates_json`까지 봐야 "이름은 다른데 문장이 같은" 중복을 잡는다.

---

## ✅ 이번에 한 것

### 산출물 — 전부 `out/` + 바탕화면 (라이브 미반영)
| 파일 | 내용 | 상태 |
|---|---|---|
| `대본틀_전문과템플릿_7종.html` | **실제 대본 전문 ↔ 빈칸 템플릿** 나란히 | ★최종본 |
| `대본스타일축_12종.html` | 반복 편수로 검증한 축 12개 | 근거 문서 |
| `대본스타일_실측v2.html` | 라이브 DB 재측정 + 장비축 반증 | 근거 문서 |
| `유튜브_대본틀_10종.html` | 유튜브 히트작 10종 | ⚠️ 7개가 기존과 겹침 |
| `대본스타일카드_후킹5종.html` · `대본스타일틀_4종.html` | 장비축 기반 | ⚠️ **근거 반증됨** |

### ★ 실측으로 뒤집힌 것 — 장비축 배율은 여기서 안 통한다
08-25 `장비템축` 실측(유튜브 공구채널 2곳 624편)을 **라이브 9,189편으로 재측정**:

| 축 | 장비축 | 라이브 |
|---|---:|---:|
| 비유격상 | 10.97x | **1.09x** |
| 정체은폐 | 4.63x | **0.82x**(뒤집힘) |
| 극단수식 | 2.57x | **0.87x**(뒤집힘) |
| 후킹 보유율(최상위) | 100% | **42.6%** |
| 후킹 개수 | 1개가 최고 | **2개가 더 높음**(반대) |

→ **장비축 결론은 유튜브 장비·공구 한정.** 인스타 본진(홈템·뷰티·레시피)에 옮기면 안 된다.
일치한 건 하나뿐: **소유자작(나도·셀프·DIY)이 마이너스**(0.80x / 0.89x).

### ★ 스타일 판정 기준을 세웠다 — 사장님 지적에서 나왔다
사장님: **"해외 거주자형 이런 건 그냥 하나의 대본 아니야? 스타일이 아니고"** → 맞았다.
기준: **반복 3편 이상 + 소재 2종 이상**. 기존 스파인도 근거 편수를 갖고 있다(식당사장님형 58편).

한국어 대본 1,971편으로 실측한 반복 편수:
```
의문훅 320 · 관찰자어미 81 · 어디서샀냐 63 · 솔직혹평 45 · 남의집비교 38 ·
오해반전 35 · 목격충격 28(소재7종) · 셀프반박 25 · 제조사도몰랐 22 ·
이게뭐냐고 19 · 인생템꼽기 19 · 절대버리지 13
탈락: 해외거주자 2편 · 대사인용 2편   ← 스타일 아님
```

### ★ 축에 두 종류가 있다 (이번 최대 수확)
- **독립 틀** — 대본 전체 구조를 정한다 (솔직혹평·남의집비교·오해반전·목격충격 등 8개)
- **스위치** — 어느 틀에나 얹는 부품 (의문훅 320 / 관찰자어미 81 / 어디서샀냐 63 / 셀프반박 25)

**기존 44개는 전부 독립 틀뿐이다.** 스위치 4개 × 독립 틀 8개로 조합이 크게 늘어난다.

### 기존 44개의 가장 큰 공백 = 화자
등록 스파인은 화자가 거의 전부 **"직접(나)"**. 그런데 히트작 상위(217만·65만)는
**전지적 관찰자 · 제품 분석가**다. 말투(`~해버림`·`~했다고`)만 바꿔도 광고 색이 빠진다.

---

## 📂 데이터 위치 (전부 서버)

```
서버: ubuntu@3.35.251.172  (★nslookup 먼저 — IP는 계속 바뀐다)
키:   C:\Users\TheRose\crawling_bot_client\LightsailDefaultKey-ap-northeast-2.pem
DB:   /home/ubuntu/lotto-stock-wiki/shopping_shorts/data/reference.db  (573MB)
```

| 테이블 | 건수 | 쓸모 |
|---|---:|---|
| `script_wiki` | 3,197 | **대본 전문 + structure_json**(훅유형·전개·화자·반전). 한국어 1,971편. 이번 분석의 본체 |
| `reel_history` | 13,490 | 인스타 캡션+조회수. `upload_ts`는 **전부 빔** |
| `source_enrichment` | 7,226 | 유튜브 채널·구독자·**upload_date**(7,154건 있음) |
| `spine` | 44 | 현재 등록 스타일(승인 35·대기 9). `templates_json`·`beat_roles_json` |
| `platform_snapshots` | 347,951 | 유튜브 46,268개 영상 조회수 추이 |

⚠️ **로컬 `reference.db`(2.8MB)는 인스타 댓글 스냅샷뿐**이다. 분석은 반드시 서버에서.

### 기존 템플릿 빈칸 변수 22종 (새로 만들지 말 것)
```
{효과} {적용대상} {사용법} {불편함} {제품군} {나라} {차별점} {효능} {가격} {제품}
{효능3} {본래용도} {속성} {효능2} {용도끝} {용도} {적용대상들} {용도2} {수치} {계기} {용도3} {용도들}
```

---

## 🔗 별건 — 다른 세션에 넘긴 것

**제작소 2단계가 스타일 자동 추천을 안 한다** (사장님: "초보자들이 영상 넣고 뭘 눌러야 할지 모르는 거 아닌가")
- 서버는 이미 준비돼 있다: `fit=검증/타소재` 판정 · `_spine_cover()` 재료 커버리지 · `style_spine_rank` 순위
  (`app.py:19019` 주석: **"첫 번째가 곧 추천이다"**)
- 그런데 **2단계 화면이 `job=`을 안 보낸다** → 커버리지가 안 온다
  ```
  produce.html:11217 (도서관)   job 보냄 ✅
  produce.html:17212 (제작소2단계) job 안 보냄 ❌
  ```
- `S2.picked=[]`로 시작해 **1위 스타일이 선택돼 있지 않다**. 켜지는 건 `📌 픽업영상 대본` 하나뿐.
- 배선 3개면 된다: `job=` 붙이기 / 1위 기본 선택 / 카드에 "이 영상으로 6칸 참" 표시

---

## 🏠 집에서 이어가기 — 키 옮기기

`Desktop\집으로_키.7z` (4KB, 암호 `lotto0905!`) 안에 2개:
- `LightsailDefaultKey-ap-northeast-2.pem` → 집 PC `C:\Users\<사용자>\crawling_bot_client\`
- `.env` (GROQ_API_KEY 포함 69줄) → 집 PC `로또의 주식\` 루트

7z는 파일명까지 암호화(`-mhe=on`)돼 있어 열기 전엔 내용이 안 보인다.
**옮긴 뒤 카톡·메일에서 파일을 지울 것.**
