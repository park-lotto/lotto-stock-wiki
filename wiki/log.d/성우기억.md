# 성우기억 — 작업 로그

## 2026-09-02
- TTS 이중 차감 차단. 원인은 `create_mix_job`이 voice를 안 넣어 3단계 1차 합성이 항상
  미나(_DEFAULT_VOICE 폴백)로 나가고, 고객이 4단계에서 바꾸면 `resynth_tts_job`이
  skip_existing 없이 전 비트를 재합성한 것 → 편당 2회.
- `customers.last_voice_json` + `get/set_last_voice` + `create_mix_job` 선주입(호출부 4곳이라
  INSERT 한 곳에서만). `/api/voice-presets`가 last_voice를 내려 화면도 미리 선택.
- ★"TTS 단계 앞당기기"는 기각 — 순서는 이미 09-01에 옮겼고, 진짜 원인은 `_plan_and_tts`가
  EDL과 TTS를 함께 돌리는 것이라 순서로는 안 풀린다(컷 길이가 실 TTS 발화초에 묶여 분리 불가).
- 검증: 신규 13건 + 사보타주 red 확인, UI는 node로 실제 실행 13항목, 회귀 350 passed 0 failed.
