# 대본수정미리보기

2026-08-19 ✏대본수정이 "안 되던" 진짜 원인 = 기능 부재가 아니라 **고치는 도중 화면이 갈아엎여**
글자가 날아간 것. `pollPhash`가 4초마다 최대 20회 `render()`를 부르고, 그게 칸 DOM을 통째로
바꿔 `contenteditable`을 지웠다(`waitNarrRegen` 40초 폴링도 같은 입구). → 배경 다시그리기를
`repaint()` 문으로 모아 **편집 중이면 미루고 닫을 때 한 번 갚는다**. 실측 편집 중 3회→0회.

같은 날 미리보기(장면 편집 iframe) 크기가 계속 바뀌던 것도 수리 — 마크업 `150vh/1100px` ↔
넓게보기 복귀부 `88vh(min-height 없음)`로 **같은 판단이 두 곳에 다르게** 적혀 있었다(0순위-B).
기준값을 `_SL_FRAME_H`/`_SL_FRAME_MINH` 두 줄로 모으고 `_applySceneLabFrameSize`만 크기를
입히게 함(마크업에서 높이 제거 = 두 벌이 생길 수 없음). 커밋 `4e79ae95d`, 신설 테스트 9건,
관련 회귀 392 passed.

2026-08-19 (2차) 저장한 대본이 새로고침하면 옛 문장으로 돌아가던 것 수리. "반영 완료"가 떴는데도
문장·자막이 옛것 = 저장 실패가 아니라 **로컬 오버라이드(NARR)가 서버 문장을 이긴 것**.
saveNarr는 delete하지만 그 전에 saveWork가 localStorage에 적어둬, loadWork가 새로고침마다
옛 문장을 되살려 서버 새 문장을 가렸다(자막은 서버 것이라 짝이 어긋남). `_pruneStaleNarr()`를
loadWork·loadSaved·waitNarrRegen 3곳에 배선. 커밋 `54adeb07e`, 신설 5건, 회귀 356 passed.
⚠️하네스 subprocess는 encoding='utf-8' 필수(cp949로 한글 깨짐).
