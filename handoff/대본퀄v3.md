# 대본퀄v3 — 핸드오프

## ✅ v5 최신 (2026-08-02 23:15, `f880758d1`) — 두 컷 묶기 + 첫 장면

### 사장님 지시 두 가지
- **"두컷씩 써봐"** — `A1 A2 B1 B2 A3 A4` 정도는 괜찮다. 한 영상만 오래 나오면 시청자가
  '비슷한 영상'으로 느낀다.
- **"첫 장면은 기억에 많이 남는 거라 다른 장면으로 시작할 수 있는 아이디어 없나"**

### 고친 것 (둘 다 `_alt_seq_prompt`)
- "3연속 금지"만 적었더니 **1개씩 번갈라는 뜻으로 읽혀** 리듬이 잘게 쪼개졌다 →
  "한 영상을 1~2개씩 쓰고 넘어가라(`A A B B A A`도 좋다), 단 3연속 이상은 마라".
  실측: `B1 B2 A11 B12 A1 B7` — 덩어리로 번갈아 간다. 역전 0 유지.
- "첫 세트를 원본 맨 앞으로 시작하지 마라" + 코드로도 **다른 벌이 쓴 첫 세트면 뒤에서
  당겨온다**(버리지 않는다). ⚠️소재에 따라 갈린다 — gimbal은 3벌 모두 다르고, 다른
  소재는 일부 겹친다(첫 세트가 유일한 선택지면 못 바꾼다. **억지로 바꾸면 훅이
  어색해져 그 선은 안 넘었다** — 사장님 우선순위가 "스토리가 말이 되느냐"이므로).

### ★사장님 우려를 실측으로 해소했다
> "a1 a2는 기능설명하다가 안 끝났는데 b1 b2는 흐름 안 이어지게 다른 얘기하고 그런 건 없을까?"

`scratchpad/flow_probe.py` — 소스 전환 지점 16건 연결성 **0.16** vs 같은 소스 연속 **0.20**.
**차이 0.04로 전환이 더 나쁘지 않다.** 대본을 직접 읽어도 전환 4회짜리가
"문제→해결→스펙→부가가치→CTA"로 정상적으로 흐른다.
**이유**: 세트마다 대사를 **새로 쓴다**(원본 문장을 붙여넣는 게 아니다). 모델이 전체
순서를 보고 이어지게 쓰므로 화면 소스가 바뀌어도 대사는 자기 흐름을 유지한다.
⚠️표본이 작고(소재 2종·전환 16건) 판정 기준이 근사치라 단정은 아니다.

---

## v5 1차 (2026-08-02 22:12, `9b36930c0`) — 소스 뭉침 해소

### 문제 (v4 실측)
job `636dd36cf2db` 패턴이 **`AAAAAABBBBBBBB`** — 앞 6컷 전부 A영상, 뒤 8컷 전부 B영상.
시청자에겐 "영상 두 개를 앞뒤로 이어붙인 것"으로 보인다. 사장님이 원한 `a1 b3 a5` 교차가 없다.

### ★원인이 예상과 달랐다
모델이 뭉쳐서 주는 게 아니라 **이미 교차해서 주고 있었다**(실측 `s1-1 → s0-12 → s0-9 → s1-7`).
`_sort_sets_in_story_order`의 정렬 키가 `(video_id, 시각)`이라 소스가 1차 기준이 돼
**코드가 그 교차를 소스별로 뭉쳐 덮어썼다.** 맥락 보라고 모델을 부르고(v4 개선) 그 결과를
코드가 뭉갠 꼴이다.

### 고친 것
`keep_order=True`(ai_story 경로): 모델이 정한 **자리는 그대로 두고 같은 소스끼리만**
시간순으로 교환한다. 자리를 안 옮기니 A/B 교차 리듬이 보존되고, "같은 소스 안 시간 역전
금지" 불변식은 계속 지켜진다. 코드 조합 경로(`keep_order=False`)는 종전대로.

### 실측
| | v4 | v5 |
|---|---|---|
| fixture_live | 소스별로 뭉침 | **`A1 B5 A8 B14`**(최대연속 1) |
| 불변식 3소재 | 2/3 통과 | **3/3 전건 통과** ← 처음 |
| 전체 스위트 | 17 failed | 17 failed(기준선 동일) |

**남은 한계**: gimbal 소재는 여전히 `AAAB`. 다만 **벌0(v3 그대로)도 `AAAB`**라 A 세트가
원본 앞쪽에 몰린 소재 특성이다(v5가 만든 문제가 아니다).

### ⏭ 다음 — 사장님 육안 확인
소파베드 소재를 다시 돌려 `A B A B` 교차가 실제 영상에서 확인되는지.

### 아직 안 고친 것 (v4 평가에서 나온 것)
1. **비트 안에서 컷이 앞뒤로 튄다** — `B9(12.9초) → B6(9.4초) → B10(15.9초)`.
   `_order_clips_by_words`가 "대사에 맞는 컷을 앞으로" 당기는 **의도된 동작**(2026-07-31
   사장님 지시). 지금 보시니 튀는 게 거슬린다는 지적 → 선택지: ①비트 안도 시간순 강제
   ②주 화면만 말에 맞추고 보조는 시간순(제 추천) ③그대로.
2. **같은 장면이 두 번** — v4 job 후보2에서 비트3 보조 `B9` + 비트4 주 `B9`.
   ⚠️하네스는 **주 화면만** 검사해 이걸 못 잡는다.
3. **점수가 품질과 어긋난다** — 박람회 후보가 대사는 제일 생생한데 0.2 최하점.

### 롤백 (태그 전부 origin에 있음)
```
git checkout v4-final -- shopping_shorts/   # v4로(소스 뭉침 있음)
git checkout v3-final -- shopping_shorts/   # v3로(후보 3개가 같음)
py tools/track.py finish 대본퀄v3            # 3분 뒤 라이브
```

---

## ✅ v4 (2026-08-02, 태그 `v4-final`) — 후보 3개가 진짜로 다른 이야기를 한다

**배포**: `34ebfedd3`(17:09 재시작). 앞선 `4b0e6e88c`(16:12)가 v4 1차, 이번이 맥락 개선분.

### 무엇이 바뀌었나

```
v3:  슬롯 1벌 → 대사만 3벌      → 후보 3개가 같은 화면·같은 이야기(훅만 다름)
v4:  슬롯 3벌 → 벌마다 대사      → 후보마다 다른 이야기
     ├ 벌0 = _pick_slot_groups 그대로  ★v3와 코드 경로 동일(프롬프트 무변경)
     ├ 벌1·2 = Gemini에게 "남은 세트로 다른 이야기를 짜라"(별도 호출)
     └ 실패 시 코드 조합 → 그것도 안 되면 벌0 복제(= v3와 동일, 나빠지지 않는다)
```

**★"3개 중 1개는 그대로 둔다"(사장님 지시)를 지키려고 `_set_seq_prompt`를 한 글자도 안
건드렸다.** 그게 벌0을 만드는 프롬프트라, 손대면 약속이 깨진다. 그래서 별도 호출이다.

### 실측

- **벌0 == v3 확인**: 같은 Gemini 응답을 주입해 코드 경로만 비교 → seg_id 완전 일치.
- **라이브 job `8ac2eef287f2`**(짐벌 재실행): 후보0↔1 겹침 **0칸**, 0↔2는 2칸.
  어제 v3는 3~4칸 겹치고 첫 화면이 셋 다 같았다. 소스 패턴도 `BBABB / AAABB / ABBBB`로 갈렸다.
  ⚠️추천이 재조합 후보(0.724)로 갔다 — "재조합은 점수가 낮을 것"이라던 내 예상이 틀렸다.
- **맥락 개선분**: 3연속 몰림 `AAAB/ABBB` → `ABB/ABBA/AABB`. 길이도 3세트 15.5초 →
  4~6세트 26~29초로 교정(모델이 짧게 주면 코드가 채운다).
- 불변식: 소재 3종 중 2종 전건 통과. 1건 NG는 **하네스의 벌 매칭 오판**
  (슬롯 자체는 12벌 전부 역전 0으로 직접 확인).
- 전체 스위트 **17 failed = 기준선 동일**(늘린 실패 0). 테스트 9건 신설.

### 아는 한계

- **3연속이 완전히는 안 사라진다**: 짐벌 소재는 여전히 `AAAB`가 나오는데 **벌0도 `AAAB`**다.
  A 세트가 원본 앞쪽에 몰린 소재라 시간순을 지키면 불가피 — v4가 만든 문제가 아니다.
- **여유가 없으면 못 갈른다**: 세트를 다 쓰는 job(10건 중 1건)은 벌0 복제라 v3와 같다.
- **과금**: 슬롯 호출 1→2회(여유 있을 때만), 대본 생성 1→벌 수만큼. 사장님이 "그냥
  확실하게"로 택한 방식.

### ⏭ 다음 — 사장님 육안 확인
즐겨찾기 → 제작소로 같은 영상을 보내 후보 3개를 본다. **태깅은 캐시되고 슬롯부터 새로
도므로** v3와 같은 태깅 위에서 비교된다(조건 고정 = 비교가 정확하다).
볼 것: ①첫 화면이 다른가 ②같은 영상 3연속이 없나 ③**이야기가 억지스럽지 않은가**

### 롤백
```
git checkout v3-final -- shopping_shorts/   # 코드만 v3로
py tools/track.py finish 대본퀄v3            # 3분 뒤 라이브 복구
```

---

## 🔖 v3 = 완료·동결 (태그 `v3-final`)

**되돌리는 법** (v4가 잘못되면):
```
git checkout v3-final -- shopping_shorts/     # 코드만 v3로
git reset --hard v3-final                     # 통째로 v3로 (주의: 이후 커밋 버림)
```
`v3-final`은 origin에 푸시돼 있다 — 어느 PC/웹에서든 접근 가능.

### v3 최종 상태 (실측 기준)
- 슬롯 기반: **장면 확정 → 대사 생성** 순서 정착. 화면을 뒤에서 다시 만지지 않는다.
- G2: 백본 화면 후처리 5종을 슬롯 경로에서 스킵(일하고 버려지던 층).
- 과적재: `_assign_timeline`이 **대사 길이만큼만** alternates를 붙인다.
- **싱크 양호**: 짐벌 job 실측 비트당 화면-대사 차이 대부분 ±2초 이내(어제는 −3.3초까지 났다).
- 프리즈 0 · 장면반복 0 · CTA 끝맺음 정상 · 소스 전부 사용.

---

## 🎯 v4 = 후보 3개를 **진짜로** 다르게 (설계 대기)

### 문제 (2026-08-02 실측으로 확정)

**후보 3개가 같은 이야기를 한다.** 훅만 다르고 나머지가 같다.

짐벌 job(`8712570702b8`, 소스 2개) 실측:
```
후보0:  s1-1 → s1-5 → s1-9 → s0-9      (B B B A)
후보1:  s1-1 → s1-5 → s1-9 → s0-10     (B B B A)
후보2:  s1-1 → s1-6 → s1-9 → s0-10     (B B B A)
```
소스1이 앞 3컷을 다 차지하고 **소스0은 마지막 1컷만** 쓰인다
(프롬프트의 "영상마다 최소 한 세트"라는 최소조건만 겨우 만족).

### 왜 그런가 — 원인 확정

**이야기 뼈대가 하나뿐이다.** 슬롯 단계에서 Gemini가 세트 7개 중 4개를 골라
**스토리 하나**를 짜고, 그 위에 대사 3벌이 얹힌다. 그러니 순서도 요점도 같다.

그리고 **대본은 원본 이야기를 따라간다**(설계 의도이기도 하다 — "대사를 창조하지 마라"):

| 화면 | 원본이 한 말 | 우리 대본(후보1) |
|---|---|---|
| s1-1 | 친구가 유튜브 한다면서 | 제발 무거운 짐벌 들고 다니지 마세요 ← **훅만 창작** |
| s1-5 | 흔들림 방지 기능이 따로 있는 거라면서 | 카메라가 다 비슷할 줄 알았는데, 흔들림 방지 기능이 |
| s1-9 | 아무리 흔들어도 각도가 그대로 유지되고 | 걷거나 움직여도 각도가 딱 유지되고 화질도 미쳤는데 |
| s0-10 | 들고 다니기에도 진짜 편할 것 같다고 | 짐벌 일체형이라 들고 다니기도 진짜 편하거든요 |

→ 표절은 아니지만 독립 창작도 아니다. **원본 순서·요점을 따라가며 말투만 바꾼다.**
→ 그래서 **뼈대가 같으면 후보도 같을 수밖에 없다.** 대사 프롬프트만 만져선 표현만 바뀐다.

### v4 방향 — 슬롯을 **3벌** 뽑는다

**재료가 이미 있다.** 짐벌 job은 세트 7개 중 4개만 썼고 **3개가 남았다**(대부분 s0).
그리고 s0에는 **완전히 다른 이야기가 통째로** 있다:
> "인플루언서 형이랑 박람회 갔는데 사람들이 줄을 길게 서 있더라 → 촬영 전문가용 초신상 →
>  헐리우드 카메라 넣어서 화질이 미쳤다"  ← 지금은 이게 통째로 버려진다

```
v4 후보A = s1 뼈대 (친구 이야기)     ← 지금 나오는 것
v4 후보B = s0 뼈대 (박람회 이야기)   ← 완전히 다른 영상·다른 이야기
v4 후보C = 섞기
```

**바꾸는 것**: `_set_seq_prompt`(edit_plan.py:545)에 "서로 다른 스토리 3개를 짜라"를 요구하고,
`_pick_slot_groups`(:644)가 3벌을 돌려주게. 후보마다 다른 슬롯을 받는다.

**안 바꾸는 것**(★중요 — 여기서 헷갈렸다):
- **장면 → 대사 순서 그대로.** 대본에 장면을 맞추는 게 아니다.
- 세트를 쪼개지 않는다(통째로 쓰거나 안 쓰거나).
- 슬롯 이후 화면을 다시 만지는 층은 계속 꺼진 채(G2 유지).
- Gemini 호출 횟수 1회 그대로(한 번에 3벌 요구) → 과금 거의 안 는다.

### ⚠️ v4의 제약 — 기대치를 미리 낮춰둘 것 (2026-08-02 정리)

**구조상 확정된 한계**
1. **여유가 없으면 못 갈른다.** 실측 10건 중 1건은 세트를 다 써서 남는 게 없다 → 후보가 똑같이 나온다.
2. **여유 2개로는 "조금 다른" 정도.** 평균 여유가 2개라, 세트 5개 중 2개만 바뀌면 **3/5는 그대로 겹친다.**
   완전히 다른 3편이 아니라 "일부 장면이 다른 3편"이다. 여유 3개짜리(10건 중 3건)에서만 확실히 갈린다.
3. **후보 B·C가 A보다 어색할 수 있다.** Gemini가 지금 그 조합을 고르는 건 그게 제일 말이 되기
   때문이다 — 2등·3등 조합은 당연히 덜 자연스럽다.
4. **소스가 1개면 효과 반감.** 짐벌 job은 소스 2개라 "친구 이야기/박람회 이야기"로 갈렸다.
   소스 1개면 같은 영상 안에서 순서만 바뀌는 것이라 이야기가 크게 안 달라진다.

**해봐야 아는 위험**
5. **프롬프트를 건드린다.** 이 트랙은 프롬프트를 만질 때마다 새 문제가 났다(메모리:
   "만질수록 이상해짐=제약 과적재"). **지금 잘 나오는 후보 A까지 나빠질 위험**이 실재한다.
6. **응답이 3배로 길어져 실패율이 오를 수 있다.** 형식이 틀리면 폴백으로 떨어져
   (`slot_source != gemini`) 지금보다 못한 결과가 나온다.
7. **3벌이 말이 되는지 자동 검증 수단이 없다.** 사람이 읽어야 안다.

**→ 위험을 줄이는 설계**: **후보 A는 지금 방식 그대로 두고, B·C만 여유분으로 만든다.**
실패해도 A는 현재 품질이 보장된다. (사장님 확인 대기 중인 사항)

**롤백**: `v3-final` 태그가 있으므로 언제든 되돌린다.

### ✅ 착수 전 확인 1 — 세트 여유 실측 완료 (2026-08-02, 서버 최근 10 job)

```
job           날짜        세트 선택 여유
208258cb4944  08-02        7    5    2
632c1fca6642  08-02        7    5    2
5927215eaa76  08-02        7    5    2
8712570702b8  08-01        7    4    3   ← 짐벌
63f11057d967  08-01        7    5    2   ← 김밥
a1df232de367  08-01        4    4    0   ← 유일하게 여유 없음
7c2a51467c65  08-01        4    3    1
e46b405cfff8  08-01        7    4    3
a75c22f644ad  08-01        7    4    3
9c2076e18252  08-01        6    4    2
```
**여유 3개+ 3건 · 1~2개 6건 · 0개 1건 · 평균 2.0개.**
→ **10건 중 9건에 여유가 있다. "여유 있을 때만 갈른다"로 설계해도 대부분 커버된다.**
→ 세트 7개 중 4개만 쓰는 job이 여럿(목표 30초를 채우면 나머지를 안 쓴다) — 재료가
   충분한데 절반 가까이 버려지고 있다.

### ⏭ 착수 전 확인 2 — 아직 안 함
슬롯 3벌이 각각 **말이 되는 이야기인지** 검증할 방법(지금은 사람이 읽어야 안다).
여유분을 억지로 끼우면 흐름이 깨질 수 있으므로, 3벌 중 품질 미달을 걸러낼 기준이 필요하다.

### v4 백로그 (우선순위 낮음)
- **길이 압축**: 계획 28.4초 → 실제 렌더 22.3초(−6초). 속도감 모드인지 말이 빨라진 건지 미확인.
- **점수가 품질과 어긋남**: 김밥 job에서 C안이 읽기엔 제일 자연스러운데 0.167 최하점.
  (`fit` 오탐 — 낱말 겹침 0으로 강등되는 알려진 한계)
- 자막 조각이 문장 중간에서 잘려 말이 안 이어져 보이는 건(짐벌 영상 실측) — 자막 분할 트랙 소관.

---

## 2026-08-01 (집PC, 밤) — G2 걷어냄 + 과적재 부분해결 배포. ★실측이 진단을 뒤집었다

### 왜 두더지가 계속 나왔나 — 답이 나왔다

사장님 질문("두더지 안 하려고 대공사한 거 아닌가")에 대한 답: **대공사(슬롯)는 자기 몫을
했다.** 화면-대사 어긋남은 실제로 안 나오고 있다. 문제는 그 공사가 **한 층만** 고쳤고,
그 위에 **일하고 버려지는 낡은 층 5개**가 그대로 남아 슬롯을 깨뜨리고 있었다는 것이다.

### ✅ 배포 완료 (`f08c60942`, 서버 23:00 재시작 확인)

1. **G2 — 백본 화면 후처리 5종을 슬롯 경로에서 스킵** (`c47908648`)
   `order_by_backbone`·`dedup_and_balance`·`ensure_sources_used`·`dedup_clips_global`·
   `swap_hook_cta_for_differentiation`은 primary/alternates만 만지는데, 바로 뒤
   `_assign_timeline`(2회차)이 그 둘을 통째로 재설정해 **결과가 예외 없이 소멸**했다.
   - **실측 증명**(`scratchpad/g2_probe.py`, 반사실 대조): 소재 5종 × 후보 16개 전부
     "스킵했을 때의 최종 화면 == 실제 최종 화면" **차이 0건**.
   - ★**단순히 '5종 실행 전/후'를 비교하면 안 된다** — `_assign_timeline`이 우연히 같은
     세그를 고른 경우를 '살아남았다'로 오판한다. 초안이 실제로 그래서 **한 번 "반증"이라고
     잘못 보고했다**. 반드시 **스킵한 비트에 같은 `_assign_timeline`을 걸어** 대조할 것.
   - 검사가 무는지도 확인(고의 차이 주입 → 9건 검출).
   - 삭제가 아니라 **스킵**이다(`if not tl_groups:`). `REWRITE_MIX=0`이면 종전대로 전부 돈다.
   - `ping_pong_reconcile`은 안 건드렸다 — 나레이션도 재작성하므로 스코프 밖.

2. **과적재 — 화면을 대사가 필요한 만큼만 붙인다** (`bb5071fb3`)
   `_assign_timeline`이 chunk 전체를 alternates로 넣어 훅이 **2.3초 말하는데 화면 7.0초
   (클립 7개)**였다. 트림 후 6개/5.8초, CTA 3개/3.2초 → 2개/3.0초.

### ⚠️⚠️ 실측이 진단을 뒤집었다 — 다음 세션이 제일 먼저 알아야 할 것

핸드오프의 옛 진단은 "한 비트에 정보를 몰아넣어 계획이 40.2초"였고 **비트당 클립 수 상한**이
처방이었다. **재보니 반대였다**(`scratchpad/overload_probe.py`):

```
[result] 클립 5 · 96자 · 화면 6.1s / 필요 12.8s  ★화면이 절반도 안 된다
[cta   ] 클립 3 · 36자 · 화면 3.2s / 필요  5.4s  ★부족
[hook  ] 클립 7 · 20자 · 화면 7.0s / 필요  2.3s  ←과적재는 여기뿐
```

**대부분의 비트는 화면이 대사보다 짧다. 길이 초과의 본체는 클립 과다가 아니라 "대사가 김"이다.**
→ **클립 상한 3개를 걸었다면 프리즈가 대량으로 돌아왔을 자리다.** 그래서 개수가 아니라
  **길이**를 기준으로 삼았고, 필요분에 못 미치면 **하나도 안 자른다**.
  여유는 `backbone._LEN_TOL`(0.35)을 그대로 썼다 — 같은 현상을 두 곳이 다른 기준으로 재면
  판정이 엇갈린다. 실 TTS가 길어지면 `_refill_beats_to_tts`가 실측 길이로 다시 채운다(안전망).

### ⏭ 다음 (우선순위 순)

1. **사장님 육안 확인** — 배포본으로 영상을 뽑아 카탈로그 낭독·말 뭉갬이 줄었는지 체감.
   이게 다음 작업의 방향을 정한다.
2. **대사 길이 제한**(길이초과의 본체, 미착수) — 비트당 목표 글자 수를 화면 길이에 맞춰
   프롬프트에서 제한. ⚠️프롬프트를 건드리는 일이라 대본 품질 전체에 영향이 간다 —
   이 트랙은 프롬프트를 만질 때마다 새 두더지가 나왔다. 전후 대조 필수.
3. **후보 3개가 화면을 공유**(원인 확정, 설계 미착수) — `edit_plan.py:2516` `_pick_slot_groups`가
   **후보 루프 밖**에서 1회만 돌아 후보 3개가 `tl_groups`를 같은 객체로 공유한다.
   `_assign_timeline`엔 후보 index 인자가 아예 없다(비트 인덱스로만 자름).
   → 사장님 결정: **차별화한다**. 후보마다 Gemini를 다시 부르면 n배 과금이므로,
     1회 호출로 얻은 `sets`를 후보별로 다르게 **고르거나 오프셋**하는 쪽이 저렴하다.
4. **A·B·C 품질게이트**(미착수) — A=저장된 계획서 자동 채점(`tag_qa` 방식, 순수계산·비용0),
   B=파이프라인 내 게이트, C=서버 주기 관측. ⚠️2번이 끝나면 채점 기준이 바뀌므로 그 뒤에.

### 🔧 이 트랙 하네스 쓸 때 걸리는 함정 (오늘 다 밟았다)

- **트랙 폴더 DB는 비어 있다** → `ping_pong`/`backbone_base`가 False가 돼 **측정하려는 경로를
  아예 안 탄다**. 서버 사본을 받아라(36MB):
  `scp -i C:/Users/CH/crawling_bot_client/LightsailDefaultKey-ap-northeast-2.pem ubuntu@3.39.179.148:/home/ubuntu/lotto-stock-wiki/shopping_shorts/data/reference.db shopping_shorts/data/reference.db`
  ★SSH 키는 `C:\Users\CH\...`다(CLAUDE.md의 `C:\Users\TheRose\...` 경로는 이 PC엔 없다).
- **키풀이 "소진"으로 잠겨 후보 0개가 조용히 나온다** — `pipeline/atoms/.gemini_key_state.json`에
  당일 소진 표시가 남으면 `get_live_keys_cascade`가 0개를 돌려주고 **에러 없이 후보 0개**가 된다.
  키 자체는 멀쩡할 수 있다(오늘 실제로 그랬다 — 직접 호출은 성공). `{"date":"...","exhausted":{}}`로
  비우면 즉시 복구. **후보 0개면 측정이 아니라 사고다** — 그대로 "통과"로 읽지 마라.
- **윈도우 cp949 콘솔** — 이모지·한글 출력이 `UnicodeEncodeError`로 죽는다.
  `sys.stdout.reconfigure(encoding="utf-8")` + `PYTHONIOENCODING=utf-8`.
- **`finish` 게이트가 pytest를 건너뛴다**(numpy 미설치, rc=2). "게이트 통과"가 **테스트를
  돌렸다는 뜻이 아니다.** 오늘은 `git stash` 대조로 직접 쟀다 — 전후 **17 failed 동일**,
  내 변경이 늘린 실패 0.
- **테스트가 "아무 일도 안 일어나서" 통과할 수 있다** — 후보 0개면 루프 본문이 안 돌아
  "5종 호출 0회"가 자동 성립한다. `_run()`에 후보 0개 가드를 박아뒀다.
  그리고 **함수만 검증하는 테스트는 배선 한 줄을 지워도 통과한다**(실제로 확인) —
  배선 테스트를 따로 둘 것(`test_trim_is_actually_wired_into_assign_timeline`).

### 검증 도구
- `scratchpad/g2_probe.py <fixture>` — 반사실 대조. 후처리를 스킵했을 때와 실제를 비교.
- `scratchpad/overload_probe.py <fixture>` — 비트별 클립 수·화면 길이 대 대사 필요 길이.
- `scratchpad/live_recheck.py <fixture>` — 불변식(중복·순서위반·소스·CTA끝·slot_source).

### 롤백
`REWRITE_MIX=0` → 옛 경로(5종이 다시 돈다) / `BLOCK_MIX=0` → 옛 스파인.

---

## 2026-08-01 (사무실 PC, 오후) — 라이브 실측으로 결함 6건 잡고 배포. 다음은 "세 문제 한 뿌리"

### 오늘 오후에 고친 것 (전부 main 반영·라이브)
1. `70987315a` **소스가 통째로 사라짐** — `after`는 영상 중간에도 붙는 태그인데 그것만으로
   끝세트로 몰아 한 소스가 전부 잘렸다. 끝세트 판정을 "마지막 seg가 `완성`" 또는 "세트 전체가
   완성/after"로 좁혔다.
2. `1e4ab731b` **후보가 4~6개로 나감** — 길이·말투 재생성은 *고르기 위해* 더 뽑는 장치인데
   합친 걸 그대로 내보냈다. 내보내는 개수만 `n_candidates`로 자른다(추천은 반드시 보존).
3. `124a2759a` **`filler`가 대본으로 나감** — 세트 5개를 요구했는데 모델이 11개를 만들며
   남는 자리를 `filler` 글자로 채웠다. 자리표시자 제거 + 비트 수를 슬롯 수에 맞춤 +
   CTA 중복 제거 + 세트 과분할 합치기.
4. `f18123489` **G3 관측** — `plan["slot_info"]`에 sets/chosen/kept/**filtered**/
   picked_by_source/**sources_unused** 기록. 조용히 새던 부분 필터가 처음으로 숫자에 남았다.
5. `9697e4849` **자리 교정을 '버리기'→'옮기기'** — 13초짜리 9세그 세트를 통째로 버려 대본이
   3비트로 쪼그라들었다. 완성은 뒤로, 문제는 앞으로 **옮긴다**(재료를 안 버린다).
6. `b5245139c` **마무리에 조리 과정이 재방송** — CTA 비트가 클립 8개로 소스의 조리 전과정을
   끌어왔다. `_broll_segs` 정렬 첫 키에 "비트 성격에 맞는 shot_role인가"를 넣었다.
   ★버리지 않고 **순서만** 미룬다 — 재료를 줄이면 프리즈가 돌아온다(폴백을 테스트로 못박음).
7. `09954c8e5` **장면 라이브러리 자동 컷어웨이 기본 OFF**(`scene_library_auto_enabled`) —
   ★대본과 무관한 별개 결함이었다. 매칭이 **role만** 봐서 요거트 아이스크림 영상 위에
   "감자튀김"(role=훅) 자산이 풀프레임으로 덮였고, 자산에 박힌 원본 자막까지 나갔다.
   스위치가 없어 자산이 등록만 돼 있으면 모든 영상에 적용됐다. 자산 데이터는 안 지웠다.

### 지금 수준 (가방/비치백 job `e46b405cfff8`, C안 기준)
기계적 지표는 전부 통과: 4비트 · `filler` 0 · 중복 0 · 소스 2/2 · CTA끝 · `tag_qa` 1.0/1.0 ·
`slot_info` 잘림 0. **태깅이 실제로 대본을 끌어온 지점도 확인**했다 —
"칸막이"↔디바이더 사은품 화면, "구멍에 걸어두기"↔키링·고리 다는 화면, "여름 감성"↔비치타올·
선풍기·에어팟. 오늘 아침(감자 섞임·filler 6개·3비트 토막)에 비하면 "고장난 영상"에서
"평범한 영상"이 됐다.


### ⏭⏭ 0순위 — 후보 3개가 **화면을 공유한다**(확정) + 선택↔저장 불일치(미확정)

**(가) 후보 3개의 화면이 사실상 같다 — 확정.** 실측(job `7c2a51467c65`):
```
A안 클립 14: s1-1 s1-10 s1-9 s1-3 s1-4 s1-2 s1-5 s1-7 s1-8 s1-6 s0-4 s0-5 s0-6 s0-7
B안 클립 14: s1-1 s1-10 s1-6 s1-3 s1-4 s1-7 s1-9 s1-2 s1-5 s1-8 s0-4 s0-5 s0-6 s0-7
```
**같은 14개 세그, 순서만 셔플.** 사장님 지적 그대로 "대본만 다르고 영상은 같다"이다.
슬롯 설계상 화면은 코드가 세트 순서대로 배정하고 후보 3개가 그 슬롯을 공유하므로,
차별화가 **대사에만** 있다. `따로 만들기`로 3편을 만들어도 그림이 거의 같은 영상이 나온다
→ 후보를 3개 뽑는 의미가 반감된다. **화면 차별화를 설계에 넣을지 결정이 필요하다.**
+ 앞 장면 중복: 첫 두 클립 `s1-1`(발등 각질 밀림) → `s1-10`이 둘 다 발등 각질이라
  같은 그림이 두 번 나온다.

**(나) 선택↔저장 불일치 — ★미확정(오늘 두 번 뒤집었다).**

| | 값 |
|---|---|
| 화면 선택 · **렌더 자막**("피부과 다니냐는 소리 듣게") | **B안(index 1)** |
| 서버 `edit_plan.candidate_index` | **0 = A안** |
| 추천(`recommended`) | C안(index 2) |

한때 "렌더 자막은 B안인데 edit_plan은 A안"으로 확정 결함이라고 적었으나, 직후 A안으로
다시 렌더한 파일(`7c2a51467c65 (1).mp4`)에서는 **자막=A안 · edit_plan=0(A안)로 일치**했다.
즉 시점 차이였을 가능성이 크다. **확정하려면 렌더 직후 DB를 즉시 읽어야 한다**(파일이 여러
벌이면 대조가 계속 어긋난다).
→ 이후 단계(5단계 최종 렌더·자막 편집·재편집)와 **모든 사후 분석**이 계획서를 보면
   다른 대본을 본다. 실제로 이것 때문에 이 세션이 B안 영상을 놓고 A안/B안 계획서를 분석해
   틀린 보고를 두 번 했다.
⚠️ 한 번 "렌더가 3벌이라 시점 차이"라고 물렀다가 다시 뒤집은 건이다 —
   재확인은 **렌더 1회짜리 job + 자막 프레임**으로 할 것(DB만 보면 또 헷갈린다).

참고(같은 job 심사위원 평가, 셋 다 낮음 · 이유가 동일):
"전형적인 판매 유도용 설명체로 **훅이 부족**하며 제품 사용 부위와 **화면이 일치하지 않고
나열식 구성**이라 몰입감이 떨어짐" — 아래 세 문제 진단과 정확히 같은 지적이다.
점수는 규칙점수(매칭75%+품질25%)와 심사위원을 반반 섞은 값이고, 추천은 말투·감각어 하한을
통과한 후보 중 최고점이다(A 0.43 / B 0.167 / C 0.51★추천).

### ⏭ 다음 세션(집): **세 문제가 한 뿌리다 — 정보 과적재**
1. **한 비트에 정보를 몰아넣는다**(1순위) — 전개 문장 하나에 칸막이+젖은용품+구멍걸이+
   33색+사은품 5종이 다 들어가고 클립이 **9개**다(CTA도 7개). 카탈로그 낭독이 된다.
2. **계획이 40.2초인데 목표는 30초** → 26초로 압축돼 말이 뭉갠다. 1번의 결과다.
3. **전개 비트의 첫 화면이 어긋난다** — 문장은 "칸막이로 수납"인데 화면은 닦는 장면.
   뒤 클립은 맞는데 **첫 1~2초**가 어긋나면 바로 느껴진다.
→ **비트당 클립 수 상한 + 문장 하나의 메시지 수 제한**을 잡으면 셋이 같이 줄어든다.
   ⚠️ 클립 수를 줄이면 화면이 짧아져 **프리즈가 돌아올 수 있다** — `_refill`이 채우던 몫이라,
   상한은 "conform 후 실 TTS 길이"와 함께 봐야 한다. 하네스 전후 대조 필수.

### 그 다음 백로그
- 훅이 화면에 없는 내용을 지어낸다(B안 "메쉬백/물 뚝뚝"은 두 영상 어디에도 없음)
- `fit` 오탐 — "똑바로 서 있어"↔"안정감 있게 서 있는 가방"이 낱말 겹침 0으로 fit=2 강등
  (`_flag_offtopic`의 어간 2글자 비교 한계). 지표를 그대로 믿으면 안 된다.
- 결과 비트가 훅에서 연 문제를 안 닫는다
- 2차 리뷰 잔여: **G4**(`_assign_timeline` 빌림 방향, 5줄) · **G2**(죽은 후처리 지층 정리,
  가장 큰 공사 — 라이브 설정 전후 9/9 재실측 + 긴 소재 필수)
- 장면라이브러리 매칭을 소재·카테고리까지 보게 고치기(그 트랙 소관)

### 검증 도구
`scratchpad/live_recheck.py <fixture.json>` — **서버 `reference.db` 사본**을 열어
`mix_pipeline._plan_and_tts`와 같은 인자를 만든다(플래그를 손으로 쓰지 마라).
소재 fixture: `scratchpad/fixture_job_*.json` 5개(서버 실제 job에서 받은 것).
⚠️ DB 사본이 없으면 `bank_context`가 비어 **라이브와 다른 프롬프트로 재게 된다**.

### 롤백
`REWRITE_MIX=0` / `BLOCK_MIX=0` / 장면라이브러리는 `scene_library_auto_enabled=1`로 되켬.

## 2026-08-01 (집PC, 이어서) — Task 7 재실측 통과 → 배포

### 고친 것
- `70987315a` **소스가 통째로 사라지던 문제** — `_filter_misplaced_sets`가 "마지막 seg가
  `완성` **또는 `after`**"면 끝세트로 보고 잘랐다. `after`는 영상 중간에도 붙는 태그라
  (실측 소스 roles = `after·사용중·문제·사용중·문제·사용중·after`) 중간 세트가 전부 잘렸고,
  **두 영상을 담았는데 한 영상만 나갔다**. Gemini는 정작 두 소스를 제대로 번갈아 골랐다
  (`youtube-1 → insta-5 → youtube-8 → insta-14`). 4세트→2세트로 길이도 반토막 나
  길이 재생성이 후보를 불린 것도 같은 뿌리.
  → 끝세트 판정을 **마지막 seg가 `완성`** 이거나 **세트 전체가 완성/after**일 때만으로 좁힘.
- (이번 커밋) **후보 개수 상한** — 재생성(길이·말투)은 *고르기 위해* 후보를 더 뽑는 장치인데
  합친 걸 그대로 내보내 사장님 화면에 4~6개가 떴다. 선택 로직은 그대로 두고 **내보내는
  개수만** `n_candidates`로 자른다. 추천 후보는 점수와 무관하게 반드시 남긴다.

### 검증 (★하네스를 라이브에 맞추는 것부터)
`scratchpad/live_recheck.py` — **서버 `reference.db` 사본을 열어** `mix_pipeline._plan_and_tts`와
같은 순서로 같은 인자를 만든다(플래그를 손으로 쓰지 않는다). v1은 `bank_context=""`,
`is_recipe=False`로 불러 **라이브와 다른 프롬프트로 잴 뻔했다** — 이 트랙이 두 번 당한 함정.
실측 인자: `ping_pong=True · backbone_base=True · judge=True · bank_context 790~873자`.

불변식도 정정했다: 소스별 시간순은 불변식이 **아니다**(스펙: "Gemini가 정한 순서·개수가
이후 절대 불변"). Gemini가 고른 세트 순서를 가로채 최종 비트와 대조하는 것으로 바꿨다 —
v1/v2는 소스 시간순을 역전으로 세어 정상 동작을 NG로 오판했다.

소재 3종(원래 것 + 서버 실제 job `422881d601c6`·`a98a4c7c3ad1`) × 후보 3개 = 9개 계획:
**순서위반 0 · 화면중복 0 · CTA끝 · slot_source=gemini · 후보 3개(상한)** 전건 통과.

테스트: `test_rewrite_mix` 42 + 상한 회귀 1건. 전체 스위트 15 failed는 기준선 —
같은 조건에서 HEAD 대조 9/9 동일(**내 변경이 늘린 실패 0**).

### ⏭ 남은 것
1. **3소스 job에서 소스 2/3만 쓰임**(`a98a4c7c3ad1`, 후보 3개 전부). 구조적 삭제가 아니라
   Gemini의 예산 내 선택("전부 다 쓸 필요는 없다"고 프롬프트가 허용) — 담은 영상을 다
   쓰게 할지는 **사장님 결정**. 바꾼다면 `_set_seq_prompt`에 "담은 영상은 되도록 한 번씩"
   한 줄 + 사후 검증.
2. 중간 CTA 중복 / `fit=2` 다수 / A↔B 세트단위 인터리브 / 자막 조각 2~3어절 / 자막제거 우선순위.

### 롤백
`REWRITE_MIX=0` → 덩어리 믹스 / `BLOCK_MIX=0` → 옛 스파인.

## 2026-08-01 (집PC) — 슬롯 기반 화면조합 재설계 + 버그 9건 수정 (진행 중, 배포 전)

### 배경
07-31 화면-대사 어긋남 버그가 하루 종일 여러 자리에서 반복 재발(핑퐁 순서섞임·백본
미적용·split후 화면중복 등) — 근본원인 재진단: 화면 리스트와 대사 리스트를 따로 두고
인덱스 산술로 여러 번 재짝짓기하는 구조. **슬롯(화면+대사 1:1 불변 쌍)** 설계로 전환.
설계: `docs/superpowers/specs/2026-08-01-슬롯기반-화면조합-design.md`
계획: `docs/superpowers/plans/2026-08-01-슬롯기반-화면조합.md`

### 오늘 한 일 (전부 커밋+push 완료, 아직 finish/배포 안 함)

Subagent-driven-development로 진행. 매 태스크마다 구현→spec-reviewer 독립검증 2단계.

1. `_pick_slot_sequence`/`_pick_slot_groups` 신설 — Gemini 1회 호출로 슬롯 순서 결정
2. `_split_long_beats`가 화면을 나누던 동작 제거(화면 불변 확보)
3. `build_scene_first_plan`이 `_pick_timeline` 대신 `_pick_slot_groups` 사용
4. **실측(job 8226822c5b09) 검증 중 잔존 버그 2건 추가 발견·수정**:
   - `ping_pong_reconcile`이 두 비트에 같은 클립 배정하던 문제 (`9e2d1dd6b`)
   - `_assign_timeline` 배분 알고리즘 자체가 n≠len(groups)일 때 화면 중복시키는 근본버그
     (`14a8f7e67`) — 10/10 무중복 실측 확인
5. **외부 코드리뷰(Fable) 1차 발견 F1~F4 전부 수정**:
   - F1: 문장그룹핑이 Gemini 재정렬 뒤에 일어나 "프랑켄 문장" 생길 수 있던 구조 역전
     (`8760ddbc8`) — 소스별 시간순 문장세트 먼저 → Gemini에겐 세트단위 순서만 질의
   - F2: target_seconds 죽은 인자라 모든 조각 강제사용→비트폭증 (`8760ddbc8`, F1과 함께)
   - F3: Gemini 실패 폴백이 안전장치 없이 전체 이어붙이기였음 → `_pick_timeline` 직접 폴백
   - F4: CTA비트의 지정화면이 `_assign_timeline`에 덮어써지던 문제 + offtopic 검사대상
     불일치 (`de9411a04`) — `screen_pinned` 플래그로 재배정 제외
6. **F5(순서 상식검증 부재, Fable 미지적분)**: Gemini가 고른 세트순서에 완성/문제 컷
   위치 사후필터 추가 (`d56a70d3d`) — 폴백에만 있던 안전장치를 정상경로에도
7. **조용한 품질저하 가시화**: `_pick_slot_groups`가 (groups, slot_source) 튜플 반환,
   `plan["slot_source"]`에 gemini/fallback_empty_order/fallback_filtered_empty/empty_input
   기록 (`a5b249860`)

### 최신 커밋 (origin/track/대본퀄v3에 push 완료)
`a5b249860` (HEAD) ← `d56a70d3d` ← `de9411a04` ← `8760ddbc8` ← `14a8f7e67` ← `9e2d1dd6b`
← `d9603aabf` ← `eb36994d6` ← `4dae7a4db` ← `00e4b6cd1` ← `412374ae2`

### ⏭ 다음에 할 것 (순서대로)
1. **최종 재실측** — Task 7을 F1~F5 반영 후 다시 돌리기. 서버 라이브 설정값
   (`ping_pong`·`backbone_base`·`REWRITE_MIX` 실제 값)을 먼저 조회해서 정확히 맞춰서
   실측할 것(이 트랙 07-31 주석에 "오프라인검증을 백본꺼진상태로만 해서 라이브에서
   안돌았다" "기본설정 측정이라 CTA누락 9/20을 못봤다" 실사고 2건 있음 — 세 번째로
   같은 함정에 빠지지 말 것). **긴 소재(세트 15~30개급)로도 확인** — F1·F2가 짧은
   소재(job 8226822c5b09, ~22세그)에서는 안 드러났던 결함이었음.
   추가로 실제 영상 열어서 태깅(scene_desc)이 화면과 맞는지 1~2개 job 육안 대조.
2. 재실측 통과하면 → `handoff/대본퀄v3.md` 갱신 → `py tools/track.py finish 대본퀄v3`로 배포
3. 실측 결과 기대 이하면 → 스펙의 "2단계 방식 전환"(역할 라벨링/순서조립 분리) 재검토
4. (실측 통과 후, 선택) F5' — 옛 후처리 지층 정리(핑퐁·dedup_and_balance·
   ensure_sources_used·swap_hook_cta 등 슬롯 불변식 확보 후 필요없어진 보정단계 걷어내기)

### 별도 트랙으로 분리(이 트랙 범위 아님, 메모리 기록됨)
- **태깅 QA 게이트**(Fable 설계, `docs`에 별도 문서 없음 — 스크래치패드에만 있었음,
  필요시 사장님께 재요청): script_extract 태깅 결과를 코드로 채점(Layer1, 0원)+프레임
  대조 스팟체크(Layer2)+감사도구(Layer3 tools/tag_audit.py). "태깅이 진짜 천장인가"를
  숫자로 답하는 게 목적. 부수발견: `mix_pipeline.py`가 `extract_auto` 디스패처를
  우회하고 `extract_script` 직접 호출 — B1 프레임추출 플래그가 믹스경로에 미적용.
  **다른 세션에서 진행 예정**(사장님 확정).
- **대본품질 피드백루프**(장기): tone_score 등 프록시 지표 대신 실제 업로드 성과
  (조회수·시청유지)를 대본선택에 되먹이는 루프 없음. memory
  `project_대본품질_피드백루프` 참조. 별도 브레인스토밍 필요.

### 롤백
환경변수만으로 즉시: `REWRITE_MIX=0` → 덩어리 믹스 / `+BLOCK_MIX=0` → 옛 스파인.
(07-31 기록과 동일, 오늘 변경도 이 스위치 영향권 안에 있음 — REWRITE_MIX=0이면
오늘 건드린 슬롯 로직 전체가 실행 안 됨)

---

## 2026-07-31 (사무실 PC) — 대본·믹스 구조 재설계

### 오늘 한 일 (전부 배포 완료, 게이트 통과)

**대본 내용**
- `20821d1d0` **`변화` 필드 신설** — 사물이 주어인 상태변화("프린팅이 갈라진다")를 추출에 기록.
  기존 `action`(30개)이 전부 사람 손동작이라 영상의 진짜 포인트를 못 담던 문제.
  검증: 레퍼런스 mp4 실제 Gemini 추출 11구간 전부 채워짐.
- `b42e05824` **옛 캐시 자동 승격** — `change` 키가 없으면 옛 스키마로 보고 재추출(영상당 1회).
  안 하면 도서관에 쌓인 영상이 영원히 옛 데이터로 남는다.
- `60bab94dd` **프롬프트 대정리** 8,045자 → 2,436자. 서로 싸우던 스타일 규칙 전부 제거
  (어미 4종·감각어 4개·강조어 2회·역할별 글자수·자가점검 6항목·5단계 스파인·훅 4패턴).
  은행을 규칙이 아니라 **벤치마킹 대상**으로 제시.
- `3e20c7cc0` **"대사를 창조하지 마라, 원본에서 가져와라"** — 후보 차별화도 "다른 이야기
  지어내기"가 아니라 "원본의 어느 대목을 앞세우느냐"로 재정의.

**믹스 구조 — 오늘의 본론**
- `8ca24ad7f` ★**리라이트 믹스**: 원본 대사 **한 문장이 시작해서 끝나는 장면까지 = 한 세트**.
  그 자리 대사를 우리 말로 갈아끼우고, **화면은 코드가 원본 순서로 배정**한다.
  레퍼런스 프로그램 역분석 결과가 근거 — 그쪽은 원본 자막을 바꿔 말할 뿐이라 어긋날 수가 없다.
- `c4880232b` 소스 전환 시 **앞 소스의 마무리 컷** 제거(중간에 마무리 그림이 오는 것 차단).
- `7d33bc653` **결 불일치 검사** — 우리 문장이 그 자리 원본 대사·화면·변화와 한 낱말도
  안 겹치면 `fit≤2·forced·offtopic` 표시.
- `bbd13e38d` 핑퐁 후처리(`order_by_backbone`·`dedup_*`·`swap_hook_cta`)가 화면을 다시
  뒤섞던 것 차단 — **맨 마지막에 화면 재배정**.
- `1375d8d44` ★**백본 ON에서도 리라이트 믹스를 타게**. 라이브가 `backbone_base_enabled=1`이라
  새 코드가 **한 번도 안 돌고 있었다**(오프라인 검증을 백본 꺼진 상태로만 해서 못 봄).
- `c1b4d09a8` **말에 맞는 컷을 비트 안에서 앞으로**(사장님 제안: 대본 핵심단어 ↔ 장면 태깅 매칭).
  + 뒤 소스의 **도입부(문제·더러운 상태)**도 제외.
- `bd95a9019` **추출 조용한 실패 검출** — 커버리지(구간합÷영상길이) < 0.55면 1회 재추출,
  그래도 낮으면 `/api/mix/status`가 `weak_sources`로 알리고 후보 카드 위에 경고 표시.
- (이번 커밋) **후킹 중복 차단** — 리라이트 믹스에선 `_lead_with_hook`을 끈다(첫 세트가 이미
  훅이라 hook 필드를 덧붙이면 같은 말이 두 번). 프롬프트에도 "훅은 한 문장, 같은 뜻 반복 금지".

### 검증 상태
- **계획 단계**(라이브 동일 설정 ping_pong·backbone_base ON, 실 Gemini): 컷 29개·중복 0·
  같은 소스 인접쌍 시간순 26/26 → 말맞춤 도입 후 20~23/25(의도된 대가), **fit 전 비트 5**.
- **라이브 영상**(job `fd8bd0f2a5c1`): 화면이 번호 오름차순으로 붙고 중복 0 — 순서 문제 해결 확인.
- 검증 스크립트: `scratchpad/live_check.py` (라이브 플래그로 직접 생성해 순서·중복 검사).

### ⏭ 다음에 할 것
1. **세트 < 비트일 때 화면 중복** — job `8226822c5b09`에서 재발. [0]훅과 [1]문제가 같은 컷,
   [3]과 [4]가 같은 컷. 원인: `_assign_timeline`이 세트보다 비트가 많으면 같은 세트를 나눠 씀.
   → 비트를 세트 수에 맞춰 **합치는** 게 맞다(지금은 반대로 화면을 늘려 씀).
2. **후보가 6개로 불어남** — 길이·말투 미달 시 3개를 더 뽑아 붙이는 재생성 결과(정상 동작이나
   사장님에겐 혼란). 재생성분을 교체할지 추가할지 결정 필요.
3. **추천 이유 표시** — 추천은 `말투(tone≥0.8)·감각어(≥3)` 통과 후보 중 최고점
   (`edit_plan.py:2338-2343`, 의도된 설계). 화면엔 규칙 점수만 보여 "0.457 두고 0.387 추천"으로
   오해된다 → 카드에 "말투 기준 통과" 뱃지.
4. **자막 조각 길이** 3~4어절 → **2~3어절**(레퍼런스 실측 4~8자).
5. **A↔B 세트 단위 인터리브** — 레퍼런스는 소스를 6번 오간다. 지금은 A 다 쓰고 B(한 번만 전환).
   세트 단위로 번갈아 배치하면 원본 순서를 지키면서 오갈 수 있다.
6. **자막제거 우선순위 재검토** — 레퍼런스 완성품 2편 모두 **원본 자막을 안 지우고** 덮거나
   위아래로 분리했다.

### 롤백
환경변수만으로 즉시: `REWRITE_MIX=0` → 덩어리 믹스 / `+BLOCK_MIX=0` → 옛 스파인.

### 교훈 (오늘 두 번 반복한 실수)
**검증 하네스를 반드시 라이브 설정으로 맞출 것.** 오전엔 스마트믹스 OFF 경로를 쟀고,
저녁엔 백본 OFF 경로로 확인해 "고쳤다 → 여전히 어긋남"을 반복했다.
라이브 settings: `ping_pong_enabled=1 · bank_enabled=1 · backbone_base_enabled=1 ·
frame_extract_enabled=0`.
