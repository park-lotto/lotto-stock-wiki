# 필름박스좌표 — Q/W 박스 확대단계별 위치 어긋남 수정

## 2026-08-29 (회사)
- 증상(사장님): "확대 한 칸 1초만 정확, 0.5·0.25·0.2·0.1·2·5초는 Q/W가 정확히 안 찍힘".
- 원인(하네스 실측 확정): applyW가 frboxes 층에 translateX(-off)를 주는데,
  drawBoxes·드래그가 자식 left를 secToX(=off를 또 뺌)로 그려 **off 이중 차감**.
  어긋난 거리가 각 단계에서 정확히 off와 일치(0.5초 618px / 0.25초 2243px / 0.2초 418px).
  1·2·5초는 필름 폭 < 창이라 off=0 → 안 드러났을 뿐.
- 수정: 박스 자식 좌표를 절대(t×pps)로 — caps·use 층과 같은 규약(filmroll.js 2곳).
- 검증: scratchpad/filmtest 하네스(Playwright, 7단계 전부·스크롤 포함) 박스↔빨간선 차 최대 4.3px
  (= 시각 0.01초 저장 반올림. 실제 잘리는 시각과 일치).
- ⚠️왜 별도 트랙인가: .tracks/필름롤러 폴더에서 다른 세션이 "✂ 쓰는 구간 다듬기" 작업 중이었고
  내 add -A 커밋(e7ea3483d)에 그쪽 미완성 useToBox가 흡수됨(finish가 dirty로 막아 라이브 무사).
  그 브랜치는 손대지 않고 이 트랙에 수정만 옮겨 배포. e7ea3483d는 track/필름롤러에 남아 있음 —
  그쪽 세션이 기능 완성 후 finish하면 같은 내용이라 충돌 없이 합쳐진다.
