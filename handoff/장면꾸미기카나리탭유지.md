# 장면꾸미기 관리자 카나리 탭 간 유지

- `sessionStorage`는 새 탭에 전달되지 않아 작업을 다른 탭으로 열면 구 장면꾸미기로 돌아가는 문제가 있었다.
- 명시적으로 `?scene_style_canary=1`을 연 관리자 브라우저는 `localStorage`에 opt-in을 기억해 이후 다른 탭·다른 작업에서도 카나리를 유지한다.
- `?scene_style_canary=0`으로 명시 해제할 수 있다. 관리자 API·현재 job 정확 일치·청소본 서명 게이트는 그대로다.
