# 미리보기낡음 — 최종렌더 뒤 3단계 수정이 미리보기에 안 보임 (2026-09-04)

사장님: "최종렌더 후 3단계 가서 수정 후 미리보기를 하니까 수정이 안 된 걸로 나오고, 다시 최종렌더까지 가면 수정이 된 걸로 나온다"

## 원인 (라이브 실측 job fb62adf0aad0, status=done·preview ready)
- 최종렌더 뒤 3단계로 돌아오면 produce가 **이미 있는 preview.mp4**를 scene_lab 확정본 칸에 넣는다(showConfirmVideo).
  이 경로는 _askRender를 안 거쳐 PENDING_SIG가 비어 있고 → `RENDERED_SIG = PENDING_SIG`로 **지문이 빈 채** 남는다.
- applyServer의 낡음 판정은 `!!RENDERED_SIG && _sig !== RENDERED_SIG` → 지문이 비면 **영영 거짓**.
  실측: 편성을 바꿔 저장해도 확정본 영상 그대로 + "이 편성 그대로 영상이 만들어집니다" 문구.
- _askRender는 확정본 칸이 비어 있을 때만 렌더를 걸므로 탭을 눌러도 옛 영상. 최종렌더는 무조건 새로 만들어 거기엔 반영.

## 고친 것 (scene_lab.html showConfirmVideo)
지문이 비면 `_editSig()`(현재 편성)를 이 영상의 지문으로 삼는다. 이후 진짜 바뀐 저장에서만 지워진다.
라이브 주입 실측: 같은 편성 저장=영상 유지 / 바뀐 편성 저장=영상 지움+"다시 눌러 새로 만들어 주세요".
테스트 36건 통과(신설 1). 
⚠️ 남은 틈: 편집 상태가 안 실린 순간에 채워지면 빈 지문 → 첫 편집 저장에서 걸러짐(공짜 미리보기 1회 더).
