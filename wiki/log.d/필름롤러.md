# 필름롤러

## 2026-08-26
- 3단계 필름 롤러 라이브. 사장님 "3단계가 너무 허접" → 실측하니 **자르기에 화면이 없었다**
  (✂는 재생 시작일 뿐, #trimbar는 한 줄 텍스트. 진행바 끌며 숫자로 타이밍을 맞춰야 했다).
- 6가지: ①위 훅 컷 🎞 펼침 ②아래 카드 [필름 펼치기] 하나만(고르기·자르기 제거)
  ③filmroll.js 부품 공용 ④필름형 토글+임의구간 담기 ⑤모든 조각(첫·끝 edge 표식)
  ⑥영상당 가로 한 줄. 조작=손잡이 끌기 스크러빙·우클릭 2번 구간·확대하면 1초→0.2초.
- ★⑤가 설계보다 넓었다: prompt_block만 막으면 될 줄 알았는데 seg_map 전체를 도는 곳이
  **9군데**(spine·blocks·timeline·sentence_sets·screen_time·repick·auto_fill·ground 2곳).
  안 막았으면 CTA컷이 AI 자동배치에 조용히 섞였다. edge 판정은 _is_edge_seg 한 곳으로.
- ★④ 새 조각은 4곳 짝: 화면·저장(hydrateExtra로 복원 2경로 통합)·전송·서버 병합
  + seg_thumb의 film_ id 파싱. 서버가 모르는 id를 조용히 버려서 "화면엔 담겼는데
  렌더엔 없다"가 나는 구조였다. FIXLEN이 저장을 빼먹어 새로고침에 사라지는 상태.
- 검증: 실제 마우스로 6가지 전부·JS오류 0·서버테스트 신규18/전체5,615 통과.
  ⚠️문법검사만으론 부족(데모에서 TDZ·CSS혼입으로 죽었는데 node --check는 통과시킴).
- 2차(같은 날): ①펼침을 맨 위 #rollbay로(카드 옆 150px에 눌려 제구실 못 함 → 1,161px)
  ②재생을 큰 미리보기로. 임의 구간이라 seg_id가 없어 `_pv_` 임시조각으로 기존 경로
  (playSegHere→부모)에 태웠다. ★임시조각은 EXTRA에 안 넣는다(담기와 미리듣기를 가른다).
  filmroll.js는 opt.onPlay 있으면 위임 — 부품은 부모를 모른다.
- 3차(집): ①🔁 '이 조각을 이 구간으로' — 조각 범위 넓히기를 **넓히지 않고 갈아끼우기**로
  풀었다(TRIMS는 가운데를 버리는 구멍이라 구조상 못 넓힌다. 넓히려면 trimPieces·
  _lab_trim_pieces에 같은 규칙 두 벌 = 0순위-B 금지형). commitRoll과 한 줄만 다르다.
  ②조각 만드는 규칙을 _makeFilmSeg 한 곳으로. ③핸드오프가 '유일한 미검증'으로 남긴
  '렌더까지 닿는가'를 테스트로 고정 — scene_override는 seg_map을 안 뒤지고 좌표만 쓴다.
- 검증: 하네스 재구성(인라인DATA+ffmpeg 30초 영상), 버튼 조건 3가지·교체·EXTRA 적재
  실브라우저 확인, JS예외 0, 서버테스트 5,623 passed(+8, 기존 실패 2건 그대로).
