# 테스트시한폭탄 — 작업 로그

## 2026-08-26 (집PC) — 30일 만료 고정날짜 수정, 라이브

`test_aipick_fresh_comments` 2건이 **코드를 안 건드렸는데 오늘 깨졌다**(어제까진 통과).
원인: 테스트가 `"2026-07-26T00:00:00"`을 박아뒀는데 `reel_history`는 30일 지난 행을
**저장하는 순간** 지운다(`store.py:2646`) → 딱 30일 뒤인 08-26부터 저장 즉시 삭제.
재현으로 확정(고정날짜 저장 후 조회 `{}` vs 현재시각 `{'AA':15430}`).

고침: 고정날짜 2곳 → `_now()`. ★**나머지 3곳은 안 고쳤다** — settings만 보는 테스트라
보존창과 무관(8 passed로 확인). 안전한 걸 혹시 몰라 고치면 그게 새 버그다.

재발방지 `test_no_expiring_fixture_dates` 신설. ★**일부러 되돌려 잡는지 확인**
(되돌림 1 failed / 원복 2 passed) — 항상 통과하는 테스트는 방어가 아니다.

검증 5577 passed / 2 failed(aipick 사라짐). 남은 2건은 `test_assembled_drafts`로
실AI 호출이라 실행마다 1~2건 흔들린다(변경 전 트리 3회 대조 완료, 내 탓 아님).
⏭ 그 불안정 테스트 고정은 별건.
