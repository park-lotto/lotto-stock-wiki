# 장면스파인 로그

- 2026-07-19: 사장님 완성영상 실측(22프레임)→장면 시간순 무시·억지슬로우 진단. 페이블 2트랙 모델 설계(화면=시간순 스파인/대본=스토리/앵커만 결합, 원클릭 완전자동 확정). **P1 슬로우제거**(_speed_and_freeze 상한1.15+freeze, 1abe25133)·**P2 시간순 스파인**(_chronological_respine, 86ce4b578) 라이브 배포. 다음=사장님 재렌더 육안→P2b 자동 reconcile·P3 앵커·P5 대본 한 스토리.
- 2026-07-19: **P5+CTA+자기검증 라이브**(오푸스 집행, 페이블 설계·검증·계획). 스토리 헌장을 단일(_GEN_PROMPT)·조합(_MIX_PROMPT) 프롬프트 공통 주입 — 인과 사슬·훅→끝 이어짐·소재적합 CTA(레시피=댓글키워드형) 강제. 스키마 required에 스토리 선언 5필드. 자기검증 _verify_and_fix: 판정 1콜(3축)→미달만 수정, fail-open. 페이블 교정=스크린샷 경로가 _MIX가 아니라 _GEN(양쪽 공통 헌장)+실키유출 차단. 테스트 35 green. 실검증=사장님 초안 생성 육안.
- 2026-07-20(집PC): 사장님 재렌더 육안 피드백에서 네 겹 도미노 처방·라이브. **① freeze 죽은정지→켄번즈 움직이는 홀드**(_extend_with_frozen_motion, tpad→zoompan 한체인 정상 실측, a8e1d8aaf). **A/B 렌즈 근본차단**: `_is_watchable`이 인스타를 무조건 통과시켜 Google Lens의 `instagram.com/popular/{제목슬러그}` SEO페이지가 즐겨찾기로 샘→매칭 배치 전체 사망. `_IG_PERMALINK_RE`로 입구 차단(3e4c1145c). **C 매칭 소스별 격리**(_prepare_sources 스킵·경고, 라이브서 매칭성공 실측). **D 스킵 일관성**(_resolve_sources도 결측 스킵, 미리보기가 스킵된 s1 찾다 죽던 후속, 3b7d1f3db). 다음=사장님 미리보기 재시도(D증명)·②엔딩 respine 정렬키. ⚠공유트랙 stash대피 사고→작업단위 즉시커밋 교훈.
- 2026-07-20: P5 실검증 ✅(밥솥식빵 소재→CTA·한스토리·인과 다 정상). 레퍼런스 쇼츠 분석(누런가전 화이트닝 AOxMICERXb0, 25.8초 광고)=화면이 작업 시간순(비포→도포→랩→벗김→애프터) 완벽 흐름·앵커/자유 교과서적. 배울점=CTA "댓글 아무글자나"가 키워드형보다 참여문턱 낮음→P5 CTA 2종분기(키워드형/아무글자형) 후보 발견(사장님 검토중).

- 2026-07-20 회사PC: ② 엔딩 딴 영상 버그 수정 라이브(main 5dbf4a58b). respine 꼬리 비트를 앵커로 고정 — CTA 나레이션이 딴 소스 릴에 얹히던 전역정렬 결함 해소. 재현 before/after 실측, 테스트 7 green, 게이트 통과(신규실패 0). 세션 시작 시 같은 트랙 2세션 충돌 발견→단독소유 정리+최신동기화.

- 2026-07-26 집PC(3): **무자막 해외영상 특장점 추출 라이브**(c43d8bdc0→병합 13e79cc4a). 집PC(2)가 회사용으로 써둔 지시서를 회사가 안 해서 집에서 집행. 근본=`script_extract._PROMPT`의 text 정의가 "나레이션(없으면 자막)"이라 **자막 없으면 text를 비움** → full_text 0자 → `generate_mix`가 `if full_text.strip()`으로 소스를 통째 제외(Gemini는 화면 이해는 정확히 하는데 그걸 특장점 문장으로 승격하는 지시가 없던 것). 처방 3겹+캐시: ①`product_benefits` 필드(세그별+최상위, list/str 정규화, 환각가드) ②제외 게이트를 `full_text or product_benefits`로 완화+프롬프트 주입 ③**지시서에 없던 edit_plan** — 라이브 경로는 scene_first라 ②만으론 안 닿아서 `_build_inventory` 팔레트 라인에 `| 특장점:` + `_source_benefits_block` 프롬프트 주입. mix_pipeline 캐시경로도 폴백 전달(script_json 통째 저장이라 store 스키마 무변경). **실키 결정적 검증: full_text 0자+특장점만인 소스 2개 → 예전엔 [](둘 다 제외), 이제 초안 생성되고 화면 특장점이 훅·인과·CTA 스토리로 녹음.** 제품 없는 클립은 []로 fail-open(환각가드 실동작). 신규 18 green, 전체 12 failed=baseline 동일(부모커밋 대조 실증). ⚠️**finish 게이트가 numpy 수집오류(rc=2)로 pytest를 조용히 건너뛴다** — 게이트 믿지 말고 직접 baseline 대조할 것.
