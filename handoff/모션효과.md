> **소유 트랙**: 모션효과 — 이 파일은 모션효과 트랙 세션만 수정한다. 다른 트랙은 읽기만.
> 원본: NEXT_SESSION.md에서 2026-07-15 분리. 규칙: `handoff/README.md`

## 🎞️ 영상제작소 모션효과 — Phase1 뼈대 ✅완료·배포 / Phase2a 설계·계획 ✅완료·구현 0/9 (2026-07-15 집PC)

**트리거**: "모션효과 트랙 이어서" → 이 파일 읽고 **Phase2a Task1부터**.

⚠️ **메모리 주의**: 이 파일이 예전에 가리키던 memory `project_영상제작소_모션효과`·`reference_ffmpeg_alpha_overlay`는
**사무실 PC에만 있다**(메모리는 `~/.claude`라 PC간 동기화 안 됨). 집PC엔 없다.
다행히 하드-원 교훈은 아래 본문에 그대로 있으니 손실 없음 — 메모리 못 찾는다고 헤매지 말 것.

**한 줄**: 딸깍 넘는 최상 효과 = Remotion으로 **모션 자산 프리렌더**(전환·스티커) → ffmpeg `overlay`로 합성(전체 Remotion 렌더는 자동화 죽여서 폐기=오바). 전환도 xfade 아닌 **타임드 투명 레이어 오버레이**로 처리(concat `-c copy` 유지=서버 다운 회피).

- 스펙 `docs/superpowers/specs/2026-07-15-영상제작소-모션효과-뼈대-design.md`
- 계획 `docs/superpowers/plans/2026-07-15-영상제작소-모션효과-뼈대.md` (8태스크 TDD, SDD)

**✅ Phase1 뼈대 완료·origin/main 푸시됨 (SDD 서브에이전트, 각 spec+quality 리뷰 통과, 12 유닛테스트 green + e2e 육안검증):**
- `motion_assets.py`: 매니페스트 로드 + 레이어(asset_id) 해석(default병합·`_abspath`·미존재 skip).
- `video_assemble.py`: `_motion_layer_filters`(타임드 투명레이어 filter_complex 빌더) + `_burn_captions`에 모션 레이어 N개 합성 + `color_filter`(eq/curves) 통합. **concat 무변경.**
- `mix_pipeline.run_render`: `deco.motion.layers` asset_id→실경로 해석(`MOTION_ASSETS_DIR`).
- `motion_text.py`: 텍스트종속 렌더 인터페이스 **스텁**(`TextRenderUnavailable`) — Phase2 연결점.
- `shopping_shorts/motion/`: **Remotion 프로젝트**(`npm run render:library`) — SwipeLeft·Sparkle 컴포지션 → **ProRes4444→qtrle .mov(argb)** 자산 생성. `assets/motion/`에 `swipe_left.mov`·`sparkle.mov`·`ph_*`(플레이스홀더)·`manifest.json`.
- **육안검증**: 파란 베이스에 스와이프 바 + ✨스파클이 enable 타이밍대로 투명 합성 + 색감필터 확인.

**⚠️ 하드-원 교훈(반드시 숙지, memory `reference_ffmpeg_alpha_overlay`)**:
1. ffmpeg `drawbox`는 rgba **알파채널을 안 박음** → 투명자산은 **불투명도형을 투명캔버스에 `overlay=format=auto`로 합성**해야 함(자산 생성 시).
2. **VP9 알파 webm 금지** — ffmpeg 기본 vp9 디코더가 알파 무시(불투명 박스로 합성됨). `libvpx-vp9` 강제해야 알파 나옴. 제품 빌더는 디코더 플래그 없으므로 **qtrle .mov(argb) 사용**(실측 정상).
3. 제품 빌더(default overlay)는 알파를 **정상 존중** — 자산에 진짜 알파만 있으면 됨.
4. `_burn_captions`는 폰트 미해결 시 조기 복사 return → 모션도 스킵(프로덕션엔 폰트 22종 있어 정상).

---

## ✅ Phase2 분해 확정 (2026-07-15 집PC — 브레인스토밍)

Phase2가 한 스펙에 담기엔 커서 **둘로 쪼갬**.

| | 범위 | 상태 |
|---|---|---|
| **Phase 2a** | 팩 스키마·자동배치 엔진·자산·꾸미기 UI | **설계·계획 완료, 구현 0/9** |
| **Phase 2b** | 상시 모션분석기(레퍼런스 영상→패턴DB→팩 자동갱신) · Remotion `render:overlay` 실 키네틱타이포 | 미착수 |

**순서 근거**: 분석기를 먼저 만들면 패턴이 내려앉을 "팩" 그릇이 없어 결과가 갈 곳이 없다.
(사용자는 처음에 "상시 분석기"를 원했으나, 그릇 먼저·자동갱신 나중으로 합의)

- 스펙 `docs/superpowers/specs/2026-07-15-영상제작소-모션효과-Phase2a-프리셋팩-design.md` (커밋 401fe180)
- 계획 `docs/superpowers/plans/2026-07-15-영상제작소-모션효과-Phase2a-프리셋팩.md` (커밋 4a6b9259, 9태스크 TDD)

**확정된 설계 결정 4가지**:
1. **팩은 완성스타일 카드에 내장** — 별도 카드벽 안 만든다. `FULL_PRESETS[i].motion = pack_id` → 카드 하나 = 글씨+모션 딸깍. 피팅룸 철학 유지.
2. **레이어는 저장이 아니라 렌더 시점 생성** — 비트 경계는 TTS 실길이가 확정되는 렌더 때 정해진다. **DB엔 `pack_id`만** 저장. (저장 시점에 박으면 대본 바뀔 때마다 전환이 어긋남 = 이 설계의 중심축)
3. **텍스트 모션은 drawtext 재사용** — 서버 실측 RAM 가용 830MB라 런타임 헤드리스 크롬 렌더는 위험. 팩은 **새 텍스트를 만들지 않고** 기존 자막 effect + 헤드카피 노출정책(`hook_only`)만 규정. 진짜 Remotion 키네틱은 2b.
4. **팩 값은 지어내지 않는다** — 스펙은 스키마·엔진만 확정. 실제 팩 3~4종 값은 **구현 Task7**에서 레퍼런스 랭킹 상위영상 관찰로 채운다(계획서 하단 "관찰 로그"에 근거 기록 → 2b 분석기가 이걸 자동화).

**서버 실측(2026-07-15)**: Node v22.22.2 · google-chrome · chromium **전부 설치돼 있음**. 그러나 **RAM 총 1.9GB / 가용 830MB** → Remotion은 빌드타임 자산생성기로만.

## ✅ 2026-07-16 — Task9 시간축 결함 2건 수정·실렌더 증명·배포 (커밋 `c9805d82`)

어제 Task9 실렌더로 **발견만 하고 검증 없이 미커밋**으로 남아 있던 수정을 증명해서 닫았다.

| | 기전 | 수정 |
|---|---|---|
| ①🔴 start>0 레이어가 **정지 잔상** | `enable=`은 '언제 그릴지'만 정하고 소스 PTS는 벽시계를 따라간다 → 창이 열릴 때 짧은 자산은 이미 자기 시간을 다 써 마지막 프레임만 깔림 | `setpts=PTS-STARTPTS+start/TB` (start=0은 no-op이라 안 얹음 — 기존 산출물 호환) |
| ②🔴 자막 바 **누적 덮임** | `drawbox`에 `enable=` 없어 비트마다 그린 바가 영상 전체에 겹침. 비트0 텍스트를 그린 뒤 바1·바2가 덮어 3%까지 떨어짐. **마지막 비트만 멀쩡해 놓치기 쉬웠다.** 기본 프리셋 '임팩트 옐로'가 `box:false`(=바 켜짐)라 **기본 경로가 이 버그를 탐** | 그 비트의 자막 표시 구간에만 그림 |

**★실렌더 재관측 증명**(수정 전 HEAD / 수정 후 WIP를 **같은 조건에서 렌더해 픽셀 비교**):
- ① 수정전 흰사각형 y=`[767,767,767]`(정지) → 수정후 `[162,383,629]`(재생)
- ② 비트0 자막 최대밝기 **12/255 → 255/255**. 어제 집PC에서 **실자산으로 잰 9/255와 일치** = 기전 재현 확인
- 회귀: 마지막 비트 수정 전후 모두 255/255

⚠️ **이번 검증은 시간축만.** 합성 자산의 투명배경이 불투명 검정으로 나와 **알파는 미검증**
(알파는 Phase1에서 확인됨). 이 PC엔 `assets/motion`(Remotion 생성물)·폰트디렉터리가 **없다**
— 폰트는 번들 `shopping_shorts/assets/NanumGothic.ttf`로 해결됨, 자산은 합성으로 대체했다.
실자산으로 다시 보려면 `shopping_shorts/motion`에서 `npm install && npm run render:library`.

**회귀 테스트 6건** `shopping_shorts/tests/test_motion_timeaxis.py`. 뮤테이션 3종 전부 죽음
(setpts 제거 / drawbox enable 제거 / 바 창을 전체로). **이 테스트는 되돌림 방지 자물쇠일 뿐 —
이것만으로 '검증했다'고 하지 마라.** 그게 Phase1이 '육안검증 통과'라 적고도 실은 정지 프레임을
보고 통과시킨 이유다.

**🔑 오늘 새로 확정된 함정 2개**(재개 시 그대로 적용):
1. **`box=True`는 바를 켜는 게 아니라 끈다** — `show_bar = style.get("bar", True) and not use_box`.
   내가 거꾸로 알고 테스트를 짰다가 빨간불을 봤다.
2. **자산 합성 시 `drawbox`로 투명캔버스에 그리면 흰픽셀 0개**가 나온다(하드-원 교훈 #1을 내가 그대로 어김).
   정식 레시피 = 불투명 도형 → 투명 캔버스에 `overlay=format=auto`. 프로브 만들 때 **자산이 살아있는지부터 확인**할 것.

---

## ⛔ 아래 "Task6부터 재개"는 **낡은 기록이다** — 2026-07-16 정정

> 이 문단이 2026-07-16에 **나를 한 번 속였다.** 이걸 믿고 handoff·대시보드에 "Task6부터"라고
> 적었다가, 원장을 열어보고서야 Task6이 이미 리포트·리뷰까지 끝난 걸 발견했다.
> (전트랙정리 트랙도 같은 함정에 빠진 적 있음 — **handoff보다 원장·대시보드가 최신이다.**)

**원장(`.superpowers/sdd/progress.md` + `motion-p2a-*`) 기준 실제 상태 (2026-07-16):**

| Task | 상태 |
|---|---|
| 1·2·3·4·5·6·8 | ✅ 완료 (리포트+리뷰 존재) |
| **7** | ⏸ **데이터 대기** — 브리프만 있음. 인스타 랭킹이 로컬·서버 모두 0이라 Apify 수집(회당 약 $0.755) 후 상위영상 관찰이 선행 |
| **9** | 리포트 완료(4항목 전부 실물 관측, 미관측 0). 결함 3건 보고 |

**Task9가 보고한 결함 3건의 현재:**
1. ✅ **닫힘** — start>0 레이어 정지 잔상(setpts). `c9805d82`, 실렌더 픽셀 증명, 배포됨
2. ✅ **닫힘** — 자막 바 누적 덮임(drawbox enable). `c9805d82`, 12/255→255/255, 배포됨
3. ✅ **닫힘 — 코드 결함 아님으로 판정**(2026-07-16 규명, 코드 변경 0)

### 결함3 규명 결과 — 고칠 것 없음

리포트가 "명세 t=3.000인데 실측 t≈3.10~3.15"라며 **스스로 "프레임/시킹 반올림 추정, 원인 미추적"**이라
적었던 건. 고치기 전에 진위부터 가렸다.

**증거:**
- `_headcopy_enable`(motion_packs.py:75)은 `between(t,0,{first t0+dur})` — **구조상 명세 그대로**.
  계약도 이미 잠겨 있다: `test_mix_pipeline_pack.py::test_headcopy_enable_문자열_포맷을_못박는다`
  (Task2 리뷰어 권고를 Task6이 반영). → 새 테스트 불필요.
- **실렌더 + 시킹 없이 프레임 번호로 측정**(-ss는 오차의 원천이라 배제, `select=eq(n,N)`으로 집음):

  | 프레임 | 시각 | 상단 최대밝기 | |
  |---|---|---|---|
  | n=90 | **3.0000** | 255 | 보임 |
  | n=91 | **3.0333** | 130 | 없음 |

  = 컷오프가 **프레임 1개 이내로 정확**. `between(t,0,3.000)`이 3.000을 포함하고 다음 프레임에서
  사라지는 건 30fps에서 정상.
- fast seek(-ss 앞)·accurate seek(-ss 뒤) **둘 다 위 진실과 일치** → 내 fixture엔 시킹 오차 없음.
- 기각한 가설: mp3 인코더 패딩으로 dur이 3.1이 됐을 것 → **틀림**. `_probe_duration`으로 재니
  wav·mp3 **둘 다 정확히 3.0000s**.

**⚠️ 정직하게 남길 것**: 리포트의 3.10~3.15가 **왜** 나왔는지는 못 밝혔다(그쪽 산출물은 다른 세션
스크래치패드라 이미 없음). 미검증 후보 = 전체 `assemble` 경로(concat+재인코딩)의 컨테이너 edit list /
오디오 프라이밍 오프셋, 또는 testsrc2 배경 위에서 프레임을 눈으로 판정한 것.
**현재 증거로는 코드 변경이 정당화되지 않는다.** 전체 파이프라인에서 재발하면 그때 `assemble` 경로를
계측할 것 — 그 전엔 없는 버그를 쫓는 셈이다.

**추가로 닫은 것**: Task9 리포트 §5가 "잠재 리스크(실증 못 함)"로 남긴 `_run_ffmpeg` 인코딩 —
실증해 보니 추정보다 나빴다. ffmpeg 실패 시 **진짜 원인이 TypeError에 가려져 사라졌다**.
`98b6c532`로 수정(encoding=utf-8·errors=replace·None 가드), 진짜 ffmpeg 실패로 통합 테스트.

---

## 📚 (참고) 2026-07-15 집PC 시점 기록 — Task6 이전

**실행방식**: 서브에이전트 SDD(구현=sonnet·리뷰=opus). 원장 = `.superpowers/sdd/progress.md` 맨 아래 모션효과 섹션.
브리프·리뷰 파일은 **`motion-p2a-` 접두사**(타 트랙 `task-N-*`과 충돌 회피).

⚠️ **원장·브리프·리뷰는 `.superpowers/`에 있고 이건 gitignore다** — **트랙 폴더로 안 따라온다.**
main 폴더(`로또의 주식/.superpowers/sdd/`)에서 읽어라. (2026-07-16 발견)

### ✅ 완료 (전부 opus 리뷰 통과, Critical 0)
| # | 커밋 | 내용 |
|---|---|---|
| 1 | `5f338949` + `46c01823` | `_beat_timeline` 추출(자막·모션 타임라인 단일화) + I-1 캐릭터라이제이션 테스트 |
| 2 | `c29291f2` | `motion_packs.build_plan` — 전환·스티커 배치 정책(순수함수) |
| 3 | `293e29b0` + `01f65a2f` + `9f234bc6` | `load_packs`+`packs.json` 스켈레톤 + 스키마 가드 3중 |
| 4 | `33298455` | **색감필터를 drawtext 앞으로**(자막 물듦 수정) |
| 5 | `70d8c41a` | 헤드카피 `hook_only` enable + 폰트스킵 경고로그 |

**⛔ 중단 사유(2026-07-15 19시 직전)**: **세션 한도**(`resets 19:00 Asia/Seoul`)로 에이전트 디스패치 중단.
Task5 리뷰어는 리뷰파일을 남긴 직후 종료돼 판정은 온전함. **내 트랙 워킹트리 clean, 미커밋 0.**
판단: 한도 상태에서 디스패치하면 구현자가 작업 중 죽어 **공유 워킹트리에 미커밋 잔재**를 남길 수 있음
= 오늘 종일 싸운 흡수사고의 씨앗. 한도 풀린 뒤 재개가 안전.

### ⏭ Task 6부터 재개 — 디스패치 시 반드시 넣을 것
- **★줄번호 믿지 마라**: 계획서가 인용한 `mix_pipeline.py:251-255` 등은 Task4·5가 같은 파일을 건드려 **이미 밀렸다**.
  실제로 Task5 구현자가 이 함정을 스스로 발견해 파일을 읽고 적응했다. 브리프에 "실제 파일을 열어 확인" 명시할 것.
- **★Task2 리뷰어 권고**: `headcopy_enable`이 `between(t,0,...)`로 시작을 **0에 하드코딩**. 현재 `first['t0']==0`이라
  동치지만 계약이 안 고정됨 → Task6에 **포맷 테스트 추가**.
- **Task6이 분기점**: 배선이 끝나야 헤드카피 `hook_only`·팩 레이어가 **처음으로 end-to-end 관측 가능**해진다.
  지금까지는 전부 부품이고 서로 연결 안 됨.
- 남은 순서: 6 배선 → 7 팩값·자산(레퍼런스 랭킹 상위 관찰 근거. `load_last_run_platform`, DB=`data/reference.db`)
  → 8 꾸미기UI → 9 실렌더 육안 → 최종 whole-branch 리뷰(생략금지).

### 🔑 이 트랙에서 확정된 교훈 (재개 시 그대로 적용)
1. **"테스트 통과" ≠ "테스트가 결함을 잡음"** — 내 계획서 결함 5건이 전부 이 뿌리였다.
   대표: 계획의 "기존 자막테스트로 회귀확인"이 실제 커버 0(그 테스트들이 `beats: []`라 루프 0회).
   → **모든 수정에 뮤테이션 확인 요구**(일부러 깨뜨려 정확히 그 지점에서 실패하는지). 5회 전부 증거 나옴.
2. **조용한 실패가 터지는 실패보다 위험** — Task3 가드가 막은 3입구(자산 값 틀림 / 키 부재 / manifest에 등록만
   되고 파일 실물 없음)는 전부 "에러 없이 모션이 그냥 안 나옴". Task7에서 헤맬 유형이라 미리 닫았다.
3. **서브에이전트 보고를 그대로 받지 마라** — 오늘 옆 트랙에서 담당자가 "격리했다"고 **허위 보고**한 뒤
   남의 미커밋 코드를 흡수한 사고가 실제로 났다. 내 커밋 9건은 전부 `git show --stat`으로 직접 확인했다(흡수 0).

### ⚠️ Task8 전 결정 필요
`produce.html`에 **다른 세션의 미커밋 변경이 실재**한다. 트랙격리 트랙이 **폴더분리(worktree)+병합게이트**로
방향을 잡는 중 — 그게 정해지면 Task8의 흡수 위험이 통째로 사라진다. **Task8 착수 전에 정해지는 게 최선.**
공유트리로 갈 경우: `git add -p`는 **이 환경에서 멈춘다**(보이스 트랙 실측) → `git apply --cached` patch 격리로.

### 📌 미해결(내 트랙 소관 아님, 보고만)
저장소 전체 pytest에 **무관한 기존 실패 25건**. 내 탓이 아님은 인과로 증명했으나(`motion_packs`를 import하는
프로덕션 코드 0개) **원래 깨져 있었는지는 미확인**(공유워킹트리라 옛커밋 checkout=타세션 파괴). Task9 배포 전 재확인.

| # | 태스크 | 핵심 |
|---|---|---|
| 1 | `_beat_timeline` 추출 | 자막·모션이 **같은 타임라인** 공유(중복계산하면 전환이 자막과 밀림) |
| 2·3 | `motion_packs.build_plan`+`load_packs` | 정책→계획 순수함수, `packs.json` 스키마 |
| 4 | **색감필터 순서 수정** | 지금 drawtext 뒤라 자막이 색보정에 물듦 → 앞으로. **Phase1 동작변경=회귀위험** |
| 5 | 헤드카피 `hook_only` enable + 폰트스킵 경고 | |
| 6 | 배선 | `pack_id`→`build_plan`→기존 `resolve_layers` |
| 7 | 팩 값·자산 | 레퍼런스 관찰 근거 |
| 8 | 꾸미기 UI | 카드에 팩 내장 |
| 9 | 실렌더 육안 grounding + 배포 | **4항목 필수**: 전환-자막 싱크 / 자막 색물듦 / 헤드카피 hook_only / 스티커 |

**계획 셀프리뷰로 미리 제거한 함정(구현자 주의)**:
- `store.list_ranking()`은 **없다**. 랭킹은 테이블이 아니라 `settings`의 `last_run::<platform>` JSON → `load_last_run_platform(platform)`. DB는 `config.DB_PATH` = `data/reference.db`.
- `video_assemble.py`엔 `sys` import가 **없다**(경고로그 넣으려면 추가).
- `mix_pipeline.MOTION_ASSETS_DIR`(25행)은 E402 때문에 import 블록 **뒤**에 있어야 한다 — 새 import를 그 아래 넣지 말 것.

**동시세션 주의**: `produce.html`(Task8)은 **대본믹스통합 트랙이 동시 편집 중**. 영역은 다르지만(대본모달 vs 꾸미기) 같은 파일 → `git add -p`로 **내 hunk만**.

**부수확인**: 꾸미기 피팅룸 재설계는 이미 완료·배포됨(4e4b9494) — 낡은 "구현 0/4" 기록 때문에 충돌 경고를 잘못 냈다가 정정함.

---
