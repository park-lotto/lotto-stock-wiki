# CN 검색 통합 파서 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 호출부가 백엔드(Playwright/Apify/프록시)를 모르게 하는 단일 CN 검색 진입점을 만들고, 렌즈 모달의 샤오홍슈·도우인 버튼이 외부 이동 대신 그 자리에 카드를 띄우게 한다.

**Architecture:** `cn_search.search()` 하나가 진입점. 플랫폼별 백엔드 사슬(`_CHAIN`)을 순서대로 시도해 **0건이면 다음 백엔드로 폴백**한다(무료 Playwright → 유료 Apify). 반환 스키마는 기존 `xiaohongshu_search.search`와 동일해 프론트 카드 렌더링을 그대로 재사용한다.

**Tech Stack:** Python 3.12 / FastAPI / Playwright(sync) / Apify REST / pytest

**작업 폴더:** `C:\Users\CH\Desktop\로또의 주식\.tracks\CN검색통합`
모든 경로는 이 폴더 기준. **main 폴더의 파일은 하나도 건드리지 않는다**(흡수 방지).

---

## File Structure

| 파일 | 책임 |
|---|---|
| `shopping_shorts/cn_search.py` (신규) | 단일 진입점 + 백엔드 사슬. 폴백 판정과 meta 집계만 한다 |
| `shopping_shorts/cn_backends.py` (신규) | 백엔드 4개(PW/Apify × 샤오/도우) 구현. 프록시·세션 결정이 여기 |
| `shopping_shorts/tests/test_cn_search.py` (신규) | 폴백·스키마·meta 검증(네트워크 없음) |
| `shopping_shorts/app.py` (수정) | `/api/lens/cn/search`를 cn_search로 교체 |
| `shopping_shorts/static/index.html` (수정) | 후보 버튼 → 인앱 카드 |

**왜 2파일로 나누나:** 사슬 판정(cn_search)과 실제 긁기(cn_backends)는 바뀌는 이유가 다르다.
백엔드가 늘어도 cn_search는 안 바뀌고, 폴백 규칙이 바뀌어도 백엔드는 안 바뀐다.

---

### Task 1: 백엔드 계약 + Apify 백엔드

**Files:**
- Create: `shopping_shorts/cn_backends.py`
- Test: `shopping_shorts/tests/test_cn_search.py`

- [ ] **Step 1: 실패하는 테스트를 쓴다**

`shopping_shorts/tests/test_cn_search.py`:

```python
"""CN 검색 통합 파서 — 폴백·스키마·meta 검증.

★실브라우저·실Apify를 부르지 않는다. 백엔드를 주입해 대체한다
(test_xiaohongshu_playwright.py와 동일 원칙)."""
from shopping_shorts import cn_backends


def test_normalize_fills_all_schema_keys():
    """백엔드가 뭘 빠뜨려도 스키마 키는 전부 채워진다(프론트가 조용히 깨지지 않게)."""
    row = cn_backends.normalize({"url": "https://x.com/1", "title": "t"}, "douyin")
    for key in ("platform", "url", "title", "thumbnail", "play_url",
                "channel", "likes", "views", "duration", "is_short"):
        assert key in row, f"{key} 누락"
    assert row["platform"] == "douyin"
    assert row["likes"] is None          # 없으면 None(0으로 위조하지 않는다)
    assert row["is_short"] is True       # duration 불명 → 숏폼으로 본다
```

- [ ] **Step 2: 실패를 확인한다**

Run: `py -m pytest shopping_shorts/tests/test_cn_search.py -v`
Expected: FAIL — `ModuleNotFoundError: No module named 'shopping_shorts.cn_backends'`

- [ ] **Step 3: 최소 구현**

`shopping_shorts/cn_backends.py`:

```python
"""CN 검색 백엔드 4종(Playwright·Apify × 샤오홍슈·도우인).

★계정과 프록시는 **함께** 정한다(0순위-B). 따로 정하면 어긋난다 —
2026-08-09 인스타 실사고(계정↔IP 불일치로 챌린지)와 같은 함정.

각 백엔드의 계약: (keyword, max_results) -> list[dict]
  - 성공: normalize()를 거친 dict 리스트
  - 실패/차단: 빈 리스트를 돌려준다(예외를 밖으로 던지지 않는다).
    폴백 판정은 cn_search가 한다 — 백엔드는 '못 가져왔다'만 알린다.
"""
import os

_SHORT_MAX_SECS = 90

_SCHEMA_KEYS = ("url", "title", "thumbnail", "play_url",
                "channel", "likes", "views", "duration")


def _num(v):
    """정수로 바꿀 수 있으면 int, 아니면 None. '1.2만' 같은 문자값은 버린다."""
    if isinstance(v, bool):
        return None
    try:
        return int(float(v))
    except (TypeError, ValueError):
        return None


def normalize(row, platform):
    """백엔드 원본 dict → 프론트가 기대하는 고정 스키마.

    ★키를 항상 전부 채운다. 빠진 키가 있으면 프론트 카드가 조용히 깨지는데,
    화면에선 '결과가 안 나온다'로만 보여 원인을 못 찾는다."""
    out = {"platform": platform}
    for k in _SCHEMA_KEYS:
        out[k] = row.get(k)
    out["title"] = out["title"] or ""
    out["channel"] = out["channel"] or ""
    out["likes"] = _num(out["likes"])
    out["views"] = _num(out["views"])
    dur = _num(out["duration"])
    out["duration"] = dur
    out["is_short"] = dur is None or dur <= _SHORT_MAX_SECS
    return out
```

- [ ] **Step 4: 통과를 확인한다**

Run: `py -m pytest shopping_shorts/tests/test_cn_search.py -v`
Expected: PASS (1 passed)

- [ ] **Step 5: 커밋**

```bash
git -C ".tracks/CN검색통합" add shopping_shorts/cn_backends.py shopping_shorts/tests/test_cn_search.py
git -C ".tracks/CN검색통합" commit -m "feat(cn): 백엔드 공통 스키마 normalize"
```

---

### Task 2: Apify 백엔드 2종 (기존 모듈 재사용)

**Files:**
- Modify: `shopping_shorts/cn_backends.py`
- Test: `shopping_shorts/tests/test_cn_search.py`

- [ ] **Step 1: 실패하는 테스트를 쓴다**

`shopping_shorts/tests/test_cn_search.py` 끝에 추가:

```python
def test_apify_backend_normalizes_and_swallows_errors(monkeypatch):
    """Apify가 터져도 예외가 밖으로 안 나간다(폴백은 cn_search가 판단)."""
    class Boom:
        @staticmethod
        def search(keyword, max_results=10):
            raise RuntimeError("APIFY_TOKEN이 설정되지 않았습니다")

    monkeypatch.setattr(cn_backends, "douyin_search", Boom)
    assert cn_backends.apify_douyin("蒜泥保存", 8) == []


def test_apify_backend_maps_rows(monkeypatch):
    class Ok:
        @staticmethod
        def search(keyword, max_results=10):
            return [{"url": "https://www.douyin.com/video/1", "title": "t",
                     "likes": "12", "duration": 30}]

    monkeypatch.setattr(cn_backends, "douyin_search", Ok)
    rows = cn_backends.apify_douyin("蒜泥保存", 8)
    assert len(rows) == 1
    assert rows[0]["platform"] == "douyin"
    assert rows[0]["likes"] == 12
    assert rows[0]["is_short"] is True
```

- [ ] **Step 2: 실패를 확인한다**

Run: `py -m pytest shopping_shorts/tests/test_cn_search.py -v`
Expected: FAIL — `AttributeError: module 'shopping_shorts.cn_backends' has no attribute 'douyin_search'`

- [ ] **Step 3: 구현**

`shopping_shorts/cn_backends.py` 상단 import에 추가:

```python
from shopping_shorts import douyin_search, xiaohongshu_search
```

파일 끝에 추가:

```python
def _apify(mod, platform, keyword, max_results):
    """Apify 액터 1회 실행. 실패는 빈 리스트로 삼킨다(계약).

    비용(2026-08-17 실측): 도우인 $0.04005/회 · 샤오홍슈 $0.098/회.
    무료 한도는 계정당 월 $5이고 **이월되지 않는다**."""
    try:
        rows = mod.search(keyword, max_results=max_results) or []
    except Exception:
        return []
    return [normalize(r, platform) for r in rows]


def apify_douyin(keyword, max_results):
    return _apify(douyin_search, "douyin", keyword, max_results)


def apify_xiaohongshu(keyword, max_results):
    return _apify(xiaohongshu_search, "xiaohongshu", keyword, max_results)
```

- [ ] **Step 4: 통과를 확인한다**

Run: `py -m pytest shopping_shorts/tests/test_cn_search.py -v`
Expected: PASS (3 passed)

- [ ] **Step 5: 커밋**

```bash
git -C ".tracks/CN검색통합" add shopping_shorts/cn_backends.py shopping_shorts/tests/test_cn_search.py
git -C ".tracks/CN검색통합" commit -m "feat(cn): Apify 백엔드 2종"
```

---

### Task 3: 샤오홍슈 Playwright 백엔드 (무료 경로)

**Files:**
- Modify: `shopping_shorts/cn_backends.py`
- Test: `shopping_shorts/tests/test_cn_search.py`

실측 근거(2026-08-17 서버): `rednote.com/search_result?keyword=…`에서
`section.note-item` 18개 중 `a.cover[href]` 17개 추출 성공. 비용 $0.

- [ ] **Step 1: 실패하는 테스트를 쓴다**

```python
def test_pw_xiaohongshu_builds_absolute_urls():
    """카드가 주는 href는 상대경로(/search_result/…) → 절대 URL로 만들어야 담기가 된다."""
    cards = [{"url": "/search_result/abc?xsec_token=T", "title": "蒜泥",
              "thumb": "https://img/1.jpg", "likes": "12"}]
    rows = cn_backends._pw_xhs_rows(cards)
    assert rows[0]["url"] == "https://www.rednote.com/search_result/abc?xsec_token=T"
    assert rows[0]["platform"] == "xiaohongshu"
    assert rows[0]["thumbnail"] == "https://img/1.jpg"
    assert rows[0]["likes"] == 12


def test_pw_xiaohongshu_drops_cards_without_url():
    """URL 없는 카드는 버린다(담기가 불가능한 카드를 화면에 올리지 않는다)."""
    assert cn_backends._pw_xhs_rows([{"url": None, "title": "x"}]) == []
```

- [ ] **Step 2: 실패를 확인한다**

Run: `py -m pytest shopping_shorts/tests/test_cn_search.py -v`
Expected: FAIL — `AttributeError: ... has no attribute '_pw_xhs_rows'`

- [ ] **Step 3: 구현**

`shopping_shorts/cn_backends.py`에 추가:

```python
_XHS_BASE = "https://www.rednote.com"

# rednote.com 검색결과 카드에서 뽑을 것. 셀렉터는 2026-08-17 서버 실측으로 확정
# (section.note-item 18개 / a.cover[href] 17개). xiaohongshu.com은 게스트 로그인벽이라
# 국제용 rednote.com만 쓴다(2026-07-29 실측).
_XHS_EXTRACT = """
() => [...document.querySelectorAll('section.note-item')].map(el => {
  const a = el.querySelector('a.cover[href]') || el.querySelector('a[href]');
  const img = el.querySelector('img');
  const t = el.querySelector('.title, [class*=title]');
  const like = el.querySelector('.like-wrapper, [class*=like]');
  return {
    url: a ? a.getAttribute('href') : null,
    title: (t ? t.innerText : (el.innerText || '').split('\\n')[0] || '').trim(),
    thumb: img ? img.getAttribute('src') : null,
    likes: like ? (like.innerText || '').trim() : null,
  };
})
"""


def _pw_xhs_rows(cards):
    """추출된 카드 배열 → 스키마 dict. URL 없는 카드는 버린다."""
    out = []
    for c in cards or []:
        href = c.get("url")
        if not href:
            continue
        url = href if href.startswith("http") else _XHS_BASE + href
        out.append(normalize({"url": url, "title": c.get("title"),
                              "thumbnail": c.get("thumb"),
                              "likes": c.get("likes")}, "xiaohongshu"))
    return out


def pw_xiaohongshu(keyword, max_results):
    """로그인 세션으로 rednote 검색결과를 긁는다. 비용 0.

    세션이 죽으면 카드가 0개가 되고, cn_search가 Apify로 폴백한다.
    ⚠️세션 판정을 `"loggedIn":false` 문자열로 하지 마라 — HTML 다른 곳에도 그 문자열이
    있어 오탐한다(2026-08-17 실측). 판정은 **카드 수**로 한다."""
    from urllib.parse import quote

    from playwright.sync_api import sync_playwright

    from shopping_shorts import config

    session = getattr(config, "XIAOHONGSHU_SESSION_PATH", "")
    if not session or not os.path.exists(session):
        return []
    url = f"{_XHS_BASE}/search_result?keyword={quote(keyword)}"
    try:
        with sync_playwright() as p:
            browser = p.chromium.launch(headless=True)
            ctx = browser.new_context(storage_state=session)
            page = ctx.new_page()
            page.goto(url, timeout=60000, wait_until="domcontentloaded")
            page.wait_for_timeout(9000)      # SSR 카드가 채워질 여유(실측 필요)
            cards = page.evaluate(_XHS_EXTRACT)
            ctx.close()
            browser.close()
    except Exception:
        return []
    return _pw_xhs_rows(cards)[:max_results]
```

(`import os`는 Task 1에서 이미 넣었다 — 중복 추가하지 마라.)

- [ ] **Step 4: 통과를 확인한다**

Run: `py -m pytest shopping_shorts/tests/test_cn_search.py -v`
Expected: PASS (5 passed)

- [ ] **Step 5: 커밋**

```bash
git -C ".tracks/CN검색통합" add shopping_shorts/cn_backends.py shopping_shorts/tests/test_cn_search.py
git -C ".tracks/CN검색통합" commit -m "feat(cn): 샤오홍슈 Playwright 백엔드(무료)"
```

---

### Task 4: 도우인 Playwright 백엔드 (CN프록시 + 세션을 짝으로)

**Files:**
- Modify: `shopping_shorts/cn_backends.py`
- Test: `shopping_shorts/tests/test_cn_search.py`

실측(2026-08-17): CN/HK 프록시면 캡차·로그인벽은 사라지나 **검색 API를 안 쏜다**(로그인 필요).
따라서 현재는 항상 빈손 → Apify로 폴백된다. **세션이 생기면 이 함수가 그대로 살아난다.**

- [ ] **Step 1: 실패하는 테스트를 쓴다**

```python
def test_pw_douyin_returns_empty_without_session(monkeypatch):
    """세션 파일이 없으면 브라우저를 아예 띄우지 않는다(헛돈·헛시간 방지)."""
    monkeypatch.setattr(cn_backends.config, "DOUYIN_SESSION_PATH", "", raising=False)
    assert cn_backends.pw_douyin("蒜泥保存", 8) == []


def test_douyin_proxy_and_session_decided_together(monkeypatch):
    """계정↔IP가 어긋나지 않게 둘을 한 함수에서 함께 정한다(0순위-B)."""
    monkeypatch.setenv("WEBSHARE_USER", "u")
    monkeypatch.setenv("WEBSHARE_PASS", "p")
    pair = cn_backends.douyin_context_kw("/tmp/s.json")
    assert pair["storage_state"] == "/tmp/s.json"
    assert pair["proxy"]["server"].startswith("http://")
    assert "-cn-" in pair["proxy"]["username"]     # 도우인은 반드시 중국 출구
```

- [ ] **Step 2: 실패를 확인한다**

Run: `py -m pytest shopping_shorts/tests/test_cn_search.py -v`
Expected: FAIL — `AttributeError: ... has no attribute 'pw_douyin'`

- [ ] **Step 3: 구현**

`shopping_shorts/cn_backends.py`에 추가:

```python
from shopping_shorts import config
from shopping_shorts.channel_archive import playwright_proxy_kw

# 도우인 전용 출구 국가. 한국 IP로는 캡차+로그인벽이 뜨고, CN/HK면 사라진다(2026-08-17 실측).
_DOUYIN_CC = os.getenv("DOUYIN_PROXY_COUNTRY", "cn")


def douyin_context_kw(session_path):
    """★계정(세션)과 프록시를 **함께** 돌려준다.

    따로 정하면 어긋난다 — 2026-08-09 인스타 실사고에서 계정은 로테이션이 고르고
    프록시는 다른 코드가 정해 계정↔IP가 틀어졌고, 그게 본인확인 챌린지로 나타났다.
    한 함수에서 같이 정하면 그 사고가 성립하지 않는다."""
    kw = {"storage_state": session_path, "locale": "zh-CN",
          "timezone_id": "Asia/Shanghai"}
    user, pw = os.getenv("WEBSHARE_USER", ""), os.getenv("WEBSHARE_PASS", "")
    if user and pw:
        host = os.getenv("WEBSHARE_HOST", "p.webshare.io:80")
        kw["proxy"] = playwright_proxy_kw(
            f"http://{user}-{_DOUYIN_CC}-1:{pw}@{host}")
    return kw


def pw_douyin(keyword, max_results):
    """도우인 검색을 CN프록시+세션으로 긁는다.

    ⚠️2026-08-17 현재 도우인 세션이 없어 항상 빈손이고 Apify로 폴백된다.
    세션이 생기면(QR 로그인) 이 경로가 그대로 살아나 비용이 0이 된다 — _CHAIN 수정 불필요.
    카드에 href가 없으므로 DOM이 아니라 **검색 API 응답을 후킹**한다."""
    from playwright.sync_api import sync_playwright

    session = getattr(config, "DOUYIN_SESSION_PATH", "")
    if not session or not os.path.exists(session):
        return []

    captured = []

    def _on_response(resp):
        if "/aweme/" not in resp.url or "search" not in resp.url:
            return
        try:
            captured.append(resp.json())
        except Exception:
            pass

    from urllib.parse import quote
    try:
        with sync_playwright() as p:
            browser = p.chromium.launch(
                headless=True,
                args=["--disable-blink-features=AutomationControlled"])
            ctx = browser.new_context(**douyin_context_kw(session))
            ctx.add_init_script(
                "Object.defineProperty(navigator,'webdriver',{get:()=>undefined});")
            page = ctx.new_page()
            page.on("response", _on_response)
            page.goto(f"https://www.douyin.com/search/{quote(keyword)}",
                      timeout=70000, wait_until="domcontentloaded")
            page.wait_for_timeout(12000)
            ctx.close()
            browser.close()
    except Exception:
        return []
    return _douyin_rows(captured)[:max_results]


def _douyin_rows(payloads):
    """검색 API 응답 → 스키마 dict(중복 aweme_id 제거)."""
    seen, out = set(), []
    for data in payloads or []:
        lst = data.get("data") or data.get("aweme_list") or []
        if not isinstance(lst, list):
            continue
        for it in lst:
            aw = it.get("aweme_info") or it.get("aweme") or it
            if not isinstance(aw, dict):
                continue
            aid = aw.get("aweme_id")
            if not aid or aid in seen:
                continue
            seen.add(aid)
            stats = aw.get("statistics") or {}
            out.append(normalize({
                "url": f"https://www.douyin.com/video/{aid}",
                "title": aw.get("desc"),
                "channel": (aw.get("author") or {}).get("nickname"),
                "likes": stats.get("digg_count"),
                "views": stats.get("play_count"),
            }, "douyin"))
    return out
```

`shopping_shorts/config.py`에 추가(파일 끝):

```python
# 도우인 로그인 세션(Playwright storage_state). 없으면 CN 검색은 Apify로 폴백한다.
DOUYIN_SESSION_PATH = os.getenv("DOUYIN_SESSION_PATH", "/home/ubuntu/douyin_session.json")
```

- [ ] **Step 4: 통과를 확인한다**

Run: `py -m pytest shopping_shorts/tests/test_cn_search.py -v`
Expected: PASS (7 passed)

- [ ] **Step 5: 커밋**

```bash
git -C ".tracks/CN검색통합" add shopping_shorts/cn_backends.py shopping_shorts/config.py shopping_shorts/tests/test_cn_search.py
git -C ".tracks/CN검색통합" commit -m "feat(cn): 도우인 Playwright 백엔드(CN프록시+세션 짝)"
```

---

### Task 5: 사슬 + 폴백 판정 (핵심)

**Files:**
- Create: `shopping_shorts/cn_search.py`
- Test: `shopping_shorts/tests/test_cn_search.py`

- [ ] **Step 1: 실패하는 테스트를 쓴다**

```python
from shopping_shorts import cn_search


def test_falls_back_when_first_backend_returns_empty():
    """0건이면 다음 백엔드로 넘어간다(A안 — 사장님 확정)."""
    calls = []

    def empty(kw, n):
        calls.append("empty"); return []

    def ok(kw, n):
        calls.append("ok")
        return [{"platform": "douyin", "url": "u", "title": "t"}]

    rows, meta = cn_search._run_chain([empty, ok], "kw", 8)
    assert calls == ["empty", "ok"]
    assert len(rows) == 1
    assert meta["backend"] == "ok"


def test_stops_at_first_backend_that_returns_rows():
    """1순위가 성공하면 유료 백엔드를 부르지 않는다(돈 안 쓴다)."""
    calls = []

    def ok(kw, n):
        calls.append("ok")
        return [{"platform": "xiaohongshu", "url": "u", "title": "t"}]

    def paid(kw, n):
        calls.append("paid"); return []

    rows, meta = cn_search._run_chain([ok, paid], "kw", 8)
    assert calls == ["ok"]
    assert meta["backend"] == "ok"


def test_backend_exception_falls_through():
    """백엔드가 예외를 던져도 사슬이 죽지 않는다."""
    def boom(kw, n):
        raise RuntimeError("network down")

    def ok(kw, n):
        return [{"platform": "douyin", "url": "u", "title": "t"}]

    rows, meta = cn_search._run_chain([boom, ok], "kw", 8)
    assert len(rows) == 1


def test_all_backends_empty_returns_empty_not_error():
    rows, meta = cn_search._run_chain([lambda k, n: [], lambda k, n: []], "kw", 8)
    assert rows == []
    assert meta["backend"] is None


def test_search_merges_platforms_and_reports_meta(monkeypatch):
    """호출부는 백엔드를 모른다 — items 하나와 meta만 받는다."""
    monkeypatch.setattr(cn_search, "_CHAIN", {
        "xiaohongshu": [lambda k, n: [{"platform": "xiaohongshu", "url": "x", "title": "a"}]],
        "douyin": [lambda k, n: [{"platform": "douyin", "url": "d", "title": "b"}]],
    })
    res = cn_search.search("蒜泥保存", max_results=8)
    assert res["count"] == 2
    assert {i["platform"] for i in res["items"]} == {"xiaohongshu", "douyin"}
    assert res["meta"]["xiaohongshu"]["n"] == 1
```

- [ ] **Step 2: 실패를 확인한다**

Run: `py -m pytest shopping_shorts/tests/test_cn_search.py -v`
Expected: FAIL — `ModuleNotFoundError: No module named 'shopping_shorts.cn_search'`

- [ ] **Step 3: 구현**

`shopping_shorts/cn_search.py`:

```python
"""CN(샤오홍슈·도우인) 검색 단일 진입점.

호출부는 **어느 백엔드가 도는지 모른다.** Playwright(무료)를 먼저 시도하고
0건이면 Apify(유료)로 폴백한다.

★어느 백엔드를 쓸지는 아래 _CHAIN **한 곳에서만** 정한다(0순위-B: 같은 판단을
두 번 적으면 언젠가 어긋난다). 도우인 세션이 생기면 pw_douyin이 성공하기 시작해
자동으로 비용이 0이 된다 — 이 표도, 호출부도, 프론트도 고칠 필요가 없다.

비용(2026-08-17 실측): 도우인 Apify $0.04005/회 · 샤오홍슈 Apify $0.098/회.
Apify 무료한도는 계정당 월 $5이며 **이월되지 않는다**(안 쓰면 소멸).
"""
from concurrent.futures import ThreadPoolExecutor

from shopping_shorts import cn_backends

# 회당 비용(달러). meta에 실어 화면에 노출한다 — 비용이 조용히 새는 걸 막는다.
_COST = {"apify_douyin": 0.04005, "apify_xiaohongshu": 0.098}

_CHAIN = {
    "xiaohongshu": [cn_backends.pw_xiaohongshu, cn_backends.apify_xiaohongshu],
    "douyin": [cn_backends.pw_douyin, cn_backends.apify_douyin],
}


def _run_chain(chain, keyword, max_results):
    """사슬을 순서대로 시도. **0건이면 다음으로 넘어간다**(A안).

    빈손도 폴백 사유다 — 회당 $0.04이고 월 500회가 리셋되므로,
    돈을 아끼려다 결과를 못 보는 쪽이 더 비싼 손해다."""
    for fn in chain:
        try:
            rows = fn(keyword, max_results) or []
        except Exception:
            continue                      # 백엔드 하나가 죽어도 사슬은 계속
        if rows:
            name = getattr(fn, "__name__", "unknown")
            return rows, {"backend": name, "n": len(rows),
                          "cost_usd": _COST.get(name, 0)}
    return [], {"backend": None, "n": 0, "cost_usd": 0}


def search(keyword, max_results=10):
    """키워드 → 샤오홍슈+도우인 결과(플랫폼 병렬).

    반환: {"items": [...], "count": N, "keyword": kw, "meta": {플랫폼: {...}}}
    items의 각 dict는 기존 렌즈 카드와 같은 스키마다(프론트 재사용)."""
    kw = (keyword or "").strip()
    if not kw:
        return {"items": [], "count": 0, "keyword": "", "meta": {}}
    n = max(1, min(int(max_results or 10), 60))

    with ThreadPoolExecutor(max_workers=2) as ex:
        futures = {p: ex.submit(_run_chain, chain, kw, n)
                   for p, chain in _CHAIN.items()}
        results = {p: f.result() for p, f in futures.items()}

    items, meta = [], {}
    for platform, (rows, m) in results.items():
        items.extend(rows)
        meta[platform] = m
    return {"items": items, "count": len(items), "keyword": kw, "meta": meta}
```

- [ ] **Step 4: 통과를 확인한다**

Run: `py -m pytest shopping_shorts/tests/test_cn_search.py -v`
Expected: PASS (12 passed)

- [ ] **Step 5: 커밋**

```bash
git -C ".tracks/CN검색통합" add shopping_shorts/cn_search.py shopping_shorts/tests/test_cn_search.py
git -C ".tracks/CN검색통합" commit -m "feat(cn): 백엔드 사슬 + 0건 폴백"
```

---

### Task 6: 엔드포인트 교체

**Files:**
- Modify: `shopping_shorts/app.py:5181-5207` (`api_lens_cn_search`)

- [ ] **Step 1: 기존 본문을 확인한다**

Run: `py -c "import re;s=open('shopping_shorts/app.py',encoding='utf-8').read();i=s.find('async def api_lens_cn_search');print(s[i-40:i+1200])"`
Expected: 현재 구현(ThreadPoolExecutor로 두 모듈 직접 호출)이 보인다

- [ ] **Step 2: 교체한다**

`shopping_shorts/app.py`의 `@app.post("/api/lens/cn/search")` 데코레이터 **아래 함수 전체**를 아래로 바꾼다:

```python
async def api_lens_cn_search(request: Request, keyword: str = Form(""),
                              max_results: int = Form(8)):
    """중국어 검색어 1개 → 샤오홍슈+도우인. **백엔드는 cn_search가 정한다**
    (Playwright 무료 우선 → 0건이면 Apify 폴백). 2026-08-17 통합.

    프론트는 어느 백엔드가 돌았는지 몰라도 되고, meta로 비용만 표시한다."""
    kw = (keyword or "").strip()
    if not kw:
        return {"ok": True, "items": [], "count": 0, "keyword": "", "meta": {}}
    n = max(1, min(int(max_results or 8), 60))
    # ★to_thread 필수 — 백엔드가 Playwright·Apify를 **블로킹**으로 부른다.
    #   안 하면 이벤트루프가 막혀 다른 렌즈 요청이 전부 굶는다(/api/lens/yt와 같은 이유).
    res = await asyncio.to_thread(cn_search.search, kw, n)
    for r in res["items"]:
        r["match"] = None          # 렌즈 카드 계약(제목 매칭은 프론트가 안 씀)
    return {"ok": True, **res}
```

`shopping_shorts/app.py` 상단 import(58행 부근 `from shopping_shorts import douyin_search, xiaohongshu_search` 다음 줄)에 추가:

```python
from shopping_shorts import cn_search
```

- [ ] **Step 3: import가 깨지지 않았는지 확인한다**

Run: `py -c "import shopping_shorts.app; print('import OK')"`
Expected: `import OK`

- [ ] **Step 4: 기준선 대비 회귀가 없는지 확인한다**

Run: `py -m pytest shopping_shorts/tests/ -q 2>&1 | tail -5`
Expected: 신규 12건 통과. **기준선 실패가 늘지 않았으면 통과**(전부 green이 목표가 아니다).

- [ ] **Step 5: 커밋**

```bash
git -C ".tracks/CN검색통합" add shopping_shorts/app.py
git -C ".tracks/CN검색통합" commit -m "feat(cn): /api/lens/cn/search를 통합 파서로 교체"
```

---

### Task 7: 프론트 — 버튼이 외부로 안 나가고 카드로

**Files:**
- Modify: `shopping_shorts/static/index.html` (후보 행 렌더 · 2600~2620행 부근)

- [ ] **Step 1: 현재 후보 버튼 렌더를 확인한다**

Run: `py -c "s=open('shopping_shorts/static/index.html',encoding='utf-8').read();i=s.find('_lensSearchUrl(\'xiaohongshu\'');print(s[i-900:i+700])"`
Expected: `📕 샤오홍슈`·`🎬 도우인`이 `<a href=... target=_blank>`로 렌더되는 코드가 보인다

- [ ] **Step 2: 인앱 검색 함수를 추가한다**

`index.html`의 `lensMoreCnKeywords` 함수 **바로 위**에 추가:

```javascript
// 📕🎬 인앱 CN 검색(2026-08-17) — 후보를 눌러도 **페이지를 떠나지 않는다**.
// 서버 cn_search가 무료(Playwright)를 먼저 쓰고 0건일 때만 Apify로 폴백하므로
// 프론트는 어느 백엔드가 돌았는지 몰라도 된다(meta로 비용만 표시).
async function lensCnSearch(shortcode, zh){
  const st=LENS_STATE[shortcode]; if(!st || st.cnSearching) return;
  st.cnSearching=true; st.cnSearchKw=zh; renderLens(shortcode);
  try{
    const fd=new FormData();
    fd.append('keyword', zh);
    fd.append('max_results', 8);
    const d=await (await fetch('/api/lens/cn/search',{method:'POST',body:fd})).json();
    const st2=LENS_STATE[shortcode]; if(!st2) return;
    const got=(d.items||[]);
    // 이미 있는 URL은 빼고 합류(같은 후보를 두 번 눌러도 안 쌓인다)
    const have=new Set((st2.items||[]).map(i=>i.url));
    st2.items=(st2.items||[]).concat(got.filter(i=>i.url && !have.has(i.url)));
    st2.cnCost=(d.meta&&Object.values(d.meta).reduce((a,m)=>a+(m.cost_usd||0),0))||0;
  }catch(e){ /* 실패해도 렌즈 나머지는 정상 */ }
  finally{
    const st3=LENS_STATE[shortcode]; if(st3) st3.cnSearching=false;
    // 늦은 응답이 다른 카드의 모달을 덮어쓰지 않게(형제 fetchLensYt와 같은 가드)
    const root=document.getElementById('lensRoot');
    if(root && root.dataset.sc===shortcode) renderLens(shortcode);
  }
}
```

- [ ] **Step 3: 버튼을 링크에서 인앱 호출로 바꾼다**

`index.html`에서 아래 두 줄을 찾는다:

```javascript
            ${c.zh?`<a href="${xhs}" target="_blank" rel="noopener" style="${lnk}">📕 샤오홍슈</a>
            <a href="${dy}" target="_blank" rel="noopener" style="${lnk}">🎬 도우인</a>`:''}
```

아래로 바꾼다(`zh`는 이미 `&quot;`로 이스케이프돼 있다 — 2026-07-19 사고 재발 방지):

```javascript
            ${c.zh?`<button onclick="lensCnSearch('${shortcode}','${zh}')" style="${lnk};cursor:pointer;border:1px solid var(--line);background:transparent">📕🎬 여기서 찾기</button>
            <a href="${xhs}" target="_blank" rel="noopener" style="${lnk};opacity:.6" title="사이트에서 열기">↗</a>`:''}
```

- [ ] **Step 4: 렌더가 깨지지 않는지 확인한다**

Run: `py -c "s=open('shopping_shorts/static/index.html',encoding='utf-8').read();print('lensCnSearch:',s.count('lensCnSearch'));print('balanced:',s.count('{')-s.count('}'))"`
Expected: `lensCnSearch: 2` (정의 1 + 호출 1)

- [ ] **Step 5: 커밋**

```bash
git -C ".tracks/CN검색통합" add shopping_shorts/static/index.html
git -C ".tracks/CN검색통합" commit -m "feat(cn): 후보 버튼 → 인앱 카드(외부 이동 제거)"
```

---

### Task 8: 서버 실측 검증 (진짜 게이트)

로컬 테스트는 배선만 증명한다. **실제로 카드가 뜨는지는 서버에서만 알 수 있다**
(로컬은 APIFY·세션 키가 없다).

- [ ] **Step 1: 병합**

```bash
py tools/track.py finish CN검색통합
```
Expected: 게이트 통과 후 main 병합. 3분 뒤 라이브 자동배포.

- [ ] **Step 2: 서버에서 통합 파서를 직접 돌린다**

```bash
ssh -i "C:/Users/CH/crawling_bot_client/LightsailDefaultKey-ap-northeast-2.pem" ubuntu@43.200.48.69 \
  "cd /home/ubuntu/lotto-stock-wiki && sudo bash -c 'set -a; . /etc/shopping-shorts.env; set +a; /usr/bin/python3 -c \"
import sys; sys.path.insert(0,\\\"/home/ubuntu/lotto-stock-wiki\\\")
from shopping_shorts import cn_search
r=cn_search.search(\\\"蒜泥保存\\\", 8)
print(\\\"count:\\\", r[\\\"count\\\"])
print(\\\"meta :\\\", r[\\\"meta\\\"])
\"'"
```

Expected(2026-08-17 실측 기준):
- `xiaohongshu`: `backend=pw_xiaohongshu`, `n≥8`, `cost_usd=0`
- `douyin`: `backend=apify_douyin`, `n≥8`, `cost_usd=0.04`

⚠️ 샤오홍슈가 `apify_xiaohongshu`로 나오면 **세션이 죽은 것**이다(쿠키 재추출 필요).
판정을 문자열이 아니라 이 meta로 한다.

- [ ] **Step 3: 화면에서 확인한다**

shoppingshorts.duckdns.org → 아무 영상 → 🔍 렌즈 → 후보 행의 `📕🎬 여기서 찾기` 클릭.
Expected: **페이지를 떠나지 않고** 아래에 샤오홍슈·도우인 카드가 합류한다.

- [ ] **Step 4: 기록을 남긴다**

`handoff/CN검색통합.md`와 `wiki/log.d/CN검색통합.md`에 결과·비용·남은 일을 적는다.

---

## 별건: 죽은 Apify 토큰 정리 (사장님 확인 후)

17개 중 **6개가 401 무효, 6개가 중복**이다(2026-08-17 실행 검증).
무효 토큰이 로테이션 앞쪽에 있으면 매 검색마다 헛때려 느려지고 원인이 숨는다.

서버 `/etc/shopping-shorts.env`에서 아래를 걷어낸다:
- 401 무효: `…1vUZQD` `…0bKVDt` `…4DpAJV` `…2lBaDI` `…21MkD4` `…1HSxOh`
- 중복: `…4w7hFQ` `…1atbro` `…3VmSdD` 각 1벌씩

남기는 것(실행 검증 통과 4계정): `…4DT6FN` `…4w7hFQ` `…3VmSdD` `…0liNLo`
(`…1atbro`는 한도소진이나 09-08 리셋되므로 남긴다)

⚠️ **서버 env 변경은 git에 안 남는다.** 사장님 확인 후 진행하고, 변경 내용을
`handoff/CN검색통합.md`에 반드시 적어둔다(다음 세션이 모르면 미궁이 된다).
