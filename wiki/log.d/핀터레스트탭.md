
## 2026-08-29 (회사PC)
- 핀터레스트 수집 **624개** 도달(302→624). 디저트·알리테무 키워드 배치 완료, 배치당 44~50개 신규.
- **병렬 12워커 확정**: 12키워드 70.1초→18.1초, 결과 손실 0. 병목은 IP가 아니라 브라우저 기동+고정 sleep(키워드당 9.5초).
- ★`scrolls=1` 축소는 **실패**(결과 34% 손실) — scrolls=5 유지.
- ★**알리·테무 링크 판별 불가 확정**: 익명 검색 응답 핀에 link/domain/tracked_link가 **0/25**. 키(node_id·images·videos·id 등)만 온다. 검색어 겨냥만 가능.
- 핸드오프 신규 작성: `handoff/핀터레스트탭.md`

## 2026-08-29 밤 (회사PC)
- **수집 엔드포인트 병렬화 라이브**(`2a58db408`): 화면 버튼도 12워커. 키워드 상한 8→12.
  ★프로세스풀 아닌 **스레드풀** — 웹서버 안 fork 위험을 피했고 실측 속도가 같다(12.8 vs 12.5초).
- **★라이브 버그 발견·수정**(`cfbd1ae62`): 타 트랙의 `pin_video_info`가 영상핀 **8/8 전부 실패** 중이었다.
  ①brotli 자동광고로 ContentDecodingError ②★진짜 원인은 **데이터센터 IP**(직접 None 10/10 / 주거용프록시 dict 10/10).
  ⚠️①만 고치면 예외→None이 되어 렌즈가 멀쩡한 영상을 **잘라내기 시작**한다. 수정 후 dict 10/0/0.
- **★알리·테무 판별: 낮의 "불가능" 결론이 뒤집혔다.** 상세페이지에 `"link":"https://temu.to/..."`,
  `"domain":"temu.to"`가 정확히 있다(단 `"domain":` 필드로만 판정 — 문자열은 CSP 오탐).
  ⚠️다만 밀도가 낮다(표본 60): 링크없음 29 · instagram 16 · amzn 3 · **temu 2** · **aliexpress 0** = 쇼핑몰 3/60.
- 테스트 22→25개. 충돌 해소 시 타 트랙(렌즈 핀 상세조회) 테스트 5개 모두 보존.
- **★알리·테무 해결**(`c4a8031ac`): 수집이 핀 목적지(`pin_dest`·`pin_link`)를 담고, 화면에 쇼핑몰 필터
  (전체/🛒쇼핑몰/테무/알리/아마존) 추가. 기존 624개도 **백필 완료**(622개 채움, 재크롤 없이 전수조사 결과 재사용).
  전량 실측: Uploaded by user 234 · instagram 212 · amzn 54 · **temu 22** · aliexpress 1 → **쇼핑몰 85개**
  (전부 영상·중앙값 15.8초·쇼츠 규격). 비용 건당 0.19초. 서버·화면 목록 불일치는 테스트가 잡는다.
- **동시 수집 경합 해소**(`c011b5dda`): 재현으로 확인(각 50개 담았는데 50개만 남음) → `BEGIN IMMEDIATE`
  한 트랜잭션(`merge_last_run_platform`). 셋 동시에도 150/150 보존. 락은 호출부 아닌 **store**에.
- ⚠️내 실수 기록: 어젯밤 임시 러너가 라이브 코드를 베끼며 `keyword`를 빠뜨려 **452건(72%)의 검색어가 유실**됐다
  (검색어별 적중률 분석이 막혔다). 러너는 폐기 처리. 앞으로 수집은 **라이브 엔드포인트 함수로만**.
- 테스트 17→33개.
- **밤샘 수집 302→957개, 쇼핑몰 핀 187개(전부 영상)**. 테무 104 · 아마존 72.
  ★검색어가 적중률을 결정한다: `temu gadgets must have` 4/4=100%, `temu storage hack` 4/4=100%,
  `temu home gadgets` 5/9=56% vs 기존 공구·차량 계열 18%. 원리 = 핀 설명에 temu가 있으면 실제 링크도 테무.
  → `DEFAULT_KEYWORDS`를 실측 적중률 순으로 교체(`c2aabc5e2`, 12개 상한 준수).
- 서버 재시작은 02:33에 자동 강제(고객 접속으로 30분 연기 후 상한 초과) → 필터 라이브 확인.
- **밤샘 12차 수집: 302 → 1,870개 · 쇼핑몰 465개**(테무 308·아마존 141, 전부 영상·중앙값 16.6초).
  ★축이 갈린다: `temu`+수납·정리·주방 = 50~100% / **`aliexpress` 축은 전멸**(7개 검색어 0/N, 465개 중 알리 1개)
  / 취미·반려(낚시·고양이·우산) 10%대 / 원래 쓰던 `diy tool invention` 0/18.
- **브라우저 라이브 검증 완료**: 필터 5개 동작, `더 보기 (200/301)` 정확, 썸네일 186개 전부 로드(실패 0).
