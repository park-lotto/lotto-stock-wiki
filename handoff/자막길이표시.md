# 자막길이표시 — 핸드오프

**최종: 2026-08-17 (회사 PC) · 라이브 병합 완료**

## 무엇을 · 왜

제작소 2단계(장면 편집) 칸 헤더에 `1. hook  5.1초`처럼 **숫자 하나만** 떠서,
그 칸이 자막 길이를 채웠는지 모자라는지 화면에서 알 수 없었다.
사장님 요청: "원래 자막 5초, 대본이 4.9면 `4.9/5`로 보여주고 늘려채우면 `5/5`로 맞나?"

## 조사 결과 — 요청대로 하면 정보가 0이다

`planClips`(`shopping_shorts/static/scene_play.js:241`)는 부족분을 **반드시 0으로 만든다**:

| 늘려 채우기 | 처리 | 합계 |
|---|---|---|
| OFF | `clips[마지막].dur += short` — 마지막 컷만 늘림 | = 자막 길이 |
| ON | `c.dur *= ttsDur/filled` — 전 컷 비례 배분 | = 자막 길이 |

즉 화면의 `5.1초`(= 컷 합계)는 **언제나 자막 길이와 같다.** `4.9/5`는 원래 안 나오고
늘 `5.0/5.0`만 뜬다. 토글은 "총합을 맞추냐"가 아니라 **"모자란 시간을 마지막 컷에
몰아주냐, 전체에 고르게 퍼뜨리냐"**의 차이다.

→ 그래서 왼쪽을 **늘리기 전 진짜 재료**(`effLen` 합)로 바꿨다. 이래야 "늘려서 때우는
중"인지가 숫자로 드러난다.

## 완료 (라이브)

`shopping_shorts/static/scene_lab.html` renderBand(1027줄 부근) + CSS(76줄 부근)

```
2.5/5.0초 · 2.5초 모자람   빨강(dbad)  마지막 컷만 늘어나 그 장면만 오래 나옴
2.5/5.0초 · 늘려 채움      노랑(dwarn) 전 컷 고르게 늘리는 중
5.0/5.0초                  초록(dok)   다 채움 (남으면 툴팁에 "남는 N초는 안 나옵니다")
0.0/5.0초 · 장면 없음      빨강(dbad)  컷 0장 = 늘릴 대상 없음 → 무음 화면
```

각 상태에 **툴팁**으로 무슨 일이 벌어지는지와 다음 행동을 적어뒀다.

★함정: 컷 0장이면 `planClips`가 `clips.length` 검사로 보정을 건너뛰어 실제론 화면이
빈 채로 남는다. 그래서 `STRETCH[i] && clips.length > 0`로 갈라야 "늘려 채움"이
거짓말이 안 된다.

- 커밋 `b64ee3c77` → main 병합 `5e79ddca4` (push 완료, 서버 3분 내 반영)
- 검증: node --check green / 4케이스 실측 / test_merge_split_playback 5 passed /
  기준선 대조(git stash) `test_edit_plan_build` 1건 실패는 **변경 전에도 동일**(회귀 0)

## ⏭ 다음

- **사장님 브라우저 육안 확인** — 실화면에서 색·문구가 의도대로 보이는지.
- **0순위-B 대상(중복 판단)**: 같은 파일 아래 편집 뷰(`scene_lab.html:1197~1209`)에
  `멘트 N초 · N초 부족 — 장면 N장 더 담아주세요` 표기가 **따로** 있다. 같은 판단을
  두 군데서 계산 중 → 언젠가 어긋난다. 한 함수(`beatFill(i)` 같은)로 뽑는 게 맞다.
  이번엔 사장님이 보는 화면만 고치는 게 목적이라 손대지 않았다.
- 표기를 `1장=1컷` 모드에서도 확인 — 그 모드는 비례 배분이라 토글이 무의미하다.
