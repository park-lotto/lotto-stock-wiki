# 자막길이표시 로그

## 2026-08-17 (회사 PC)
- 제작소 2단계 칸 헤더 길이 표기를 `5.1초`(합계 하나) → **`담은재료/자막` 짝**으로 바꿈.
  `scene_lab.html:1027` renderBand. 라이브 병합 완료(b64ee3c77 → main 5e79ddca4).
- ★핵심 발견: **`sec/need`로 짝을 만들면 의미가 없다.** `planClips`(scene_play.js:241)가
  부족분을 반드시 0으로 만들기 때문 — 토글 OFF면 마지막 컷에 몰고(`clips[last].dur += short`),
  ON이면 전 컷 비례배분(`c.dur *= ttsDur/filled`). 즉 **합계는 언제나 자막 길이와 같아**
  늘 `5.0/5.0`만 뜬다. 사장님이 기대한 `4.9/5`는 원래 안 나오는 값이었다.
  → 왼쪽을 **늘리기 전 진짜 재료**(`effLen` 합)로 두어야 정보가 생긴다.
- 4상태: `2.5/5.0 · 2.5초 모자람`(빨강) / `2.5/5.0 · 늘려 채움`(노랑) /
  `5.0/5.0`(초록) / `0.0/5.0 · 장면 없음`(빨강).
- ★함정: 컷 0장이면 `planClips`가 `clips.length` 검사로 보정을 **통째로 건너뛴다** →
  토글이 켜져 있어도 실제론 무음 화면. `STRETCH[i] && clips.length > 0`로 갈라야
  "늘려 채움"이 거짓말이 안 된다.
- 검증: node --check green / 4케이스 실측(부족·딱맞음·남음·0장) /
  test_merge_split_playback 5 passed / **기준선 대조(git stash)** — `test_edit_plan_build`
  1건 실패는 변경 전에도 동일(회귀 0).
- **(같은 날 2차) 0순위-B 합치기 완료** — 타임프레임 줄과 편집 뷰가 같은 판단을 두 번
  계산하던 걸 `beatFill(i, clips)` 한 함수로 통일. 커밋 `6667b8d10` → main `a09a2e85a`,
  서버 재시작 23:50:08 확인.
  - ★실측으로 **진짜 불일치를 찾았다**: 두 기준은 **0.6초 이상 0.8초 미만** 장면에서만
    갈린다. `effLen`이 이미 MIN_CLIP(0.6) 미만을 버리므로 0.5초짜리는 양쪽 다 빠지고
    0.9초짜리는 양쪽 다 들어간다. 딱 그 틈(0.7초)에서만 **위=2.70 / 아래=2.00**.
    → 엄격한 쪽(okIds, tooShort 0.8초 제외)으로 통일.
  - ⚠️ 처음엔 0.5초짜리로 재현될 줄 알았는데 **양쪽 다 걸러져 일치했다.** 짐작으로 "이미
    어긋나 있다"고 쓸 뻔했고, 실제로 케이스를 돌려보고서야 틈이 0.6~0.8초임을 알았다.
    주석·커밋 메시지도 실측값으로 고쳐 적었다.
  - 재발 방지 테스트 4건 신설 `shopping_shorts/tests/test_beat_fill_label.py` —
    원본 함수를 **정규식으로 끌어다** 돌린다(복사본을 만들면 원본이 바뀌어도 통과한다).
  - 검증: 인라인 script 78,873자 `node --check` green / 4케이스 두 화면 값 일치 /
    482 passed(신규 4건 포함), 기준선과 동일한 `test_edit_plan_build` 1건만 실패(회귀 0).
- ⏭ 브라우저 실화면 육안 확인(사장님) — Ctrl+Shift+R. 모자란 칸이 있어야 색·문구가 보인다.
