# 생성 순응 검열 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 영상 믹스 생성 시 은행(스파인·부품)이 실제로 대본을 형성했는지 매 job 관측해 검열 대시보드 "생성 순응" 섹션에 신호등으로 보고한다.

**Architecture:** 결정적 순수함수 3층(사용 스냅샷·구조 순응·판단 감지)으로 job당 레코드를 만들고, 은행이 주입된 job 중 N=10에 1편만 제미니로 아크/부품 순응을 채점한다. 레코드는 `bank_usage_recent`(최근 K=50)에 쌓고 집계해 `bank_usage_audit_last`에 저장, `/api/bank/ingest_report`가 실어 나른다. 전부 부가 관측이라 실패는 삼키고 생성 자체엔 영향 0.

**Tech Stack:** Python(FastAPI·SQLite store), 바닐라 JS(static/index.html), pytest.

## Global Constraints

- 모든 신규 로직은 **순수함수**(계산·판정)로 두고 저장·조회·Gemini 호출은 호출부가 한다 — 기존 `gemini_audit.py` 패턴 준수.
- **회귀 0**: `bank_enabled != "1"`이면 스냅샷·append를 전혀 하지 않아 예전과 동일하게 동작. 관측 로직의 개별 실패는 `traceback.print_exc(file=sys.stderr)`로 삼키고 생성 결과를 그대로 반환.
- store 설정 저장은 문자열만 — JSON은 `json.dumps(obj, ensure_ascii=False)`, 읽기는 `json.loads`.
- 제미니 호출 주입점은 `pattern_bank._default_call(prompt, schema, max_key_tries=None)`(키풀 라운드로빈 이미 적용). 테스트는 `call` 인자로 fake 주입.
- 상수(한자리): 샘플 비율 `N=10`, 창 크기 `K=50`, 부품 임계 `PARTS_MIN=5`, 아크 최소 비트 `BEAT_MIN=5`. 신호등: bank_used 0.5·conformance 0.7·plagiarism 0.1·arc 2.5(5점 만점).
- 작업 폴더는 트랙 폴더 `.tracks/수집검열` — 모든 경로는 그 안. git은 `git -C .tracks/수집검열`.

## 파일 구조

| 파일 | 책임 |
|---|---|
| `shopping_shorts/bank_assemble.py` (수정) | `bank_usage_snapshot` 추가 — 주입 내역 계측 dict(읽기 재사용). |
| `shopping_shorts/bank_usage_audit.py` (신규) | 결정적 순수함수: `structural_conformance`·`judge_snapshot`·`usage_health`·`compute_usage_audit`. |
| `shopping_shorts/bank_compliance.py` (신규) | 샘플 LLM 순응 채점 `judge_compliance`(call 주입). |
| `shopping_shorts/store.py` (수정) | `append_bank_usage` — bank_usage_recent 링버퍼(K cap). |
| `shopping_shorts/mix_pipeline.py` (수정) | `_record_bank_usage` 헬퍼 + 생성 경로 배선(스냅샷·순응·샘플게이트·집계·저장). |
| `shopping_shorts/app.py` (수정) | `/api/bank/ingest_report` payload에 `usage_audit` 첨부. |
| `shopping_shorts/static/index.html` (수정) | `_renderBankPanel`에 "생성 순응" 섹션. |
| `shopping_shorts/tests/test_bank_usage_snapshot.py` (신규) | 스냅샷 케이스. |
| `shopping_shorts/tests/test_bank_usage_audit.py` (신규) | 순응·집계·신호등 경계값. |
| `shopping_shorts/tests/test_bank_compliance.py` (신규) | 순응 채점 프롬프트·폴백. |
| `shopping_shorts/tests/test_bank_usage_wiring.py` (신규) | `_record_bank_usage`·`append_bank_usage` 배선. |

---

### Task 1: 사용 스냅샷 `bank_usage_snapshot`

**Files:**
- Modify: `shopping_shorts/bank_assemble.py` (파일 끝에 함수 추가)
- Test: `shopping_shorts/tests/test_bank_usage_snapshot.py`

**Interfaces:**
- Consumes: `store.get_setting(key, default=None)`, `store.pick_spine_for_category(category, status="approved", min_sources=3)`, `store.list_pattern_items(bucket=, status="approved", order_by="perf", limit=)`, `store.recent_script_usage(limit=8)`, `STYLE_BUCKETS=("hook","ending","adverb","cta","price")`.
- Produces: `bank_usage_snapshot(store, category, k=5) -> dict {bank_enabled:bool, spine_present:bool, spine_beats:int, parts_by_bucket:dict, parts_total:int, avoid_present:bool, category:str, empty:bool}`

- [ ] **Step 1: Write the failing test**

```python
# shopping_shorts/tests/test_bank_usage_snapshot.py
from shopping_shorts.bank_assemble import bank_usage_snapshot


class FakeStore:
    def __init__(self, enabled="1", spine=None, items=None, recent=None):
        self._enabled = enabled
        self._spine = spine
        self._items = items or {}   # bucket -> list of item dicts
        self._recent = recent or {"hooks": [], "persons": [], "ctas": []}

    def get_setting(self, key, default=None):
        return self._enabled if key == "bank_enabled" else default

    def pick_spine_for_category(self, category, status="approved", min_sources=3):
        return self._spine

    def list_pattern_items(self, bucket=None, status=None, order_by=None, limit=None):
        return list(self._items.get(bucket, []))

    def recent_script_usage(self, limit=8):
        return self._recent


def test_snapshot_empty_when_no_spine_no_parts():
    snap = bank_usage_snapshot(FakeStore(spine=None, items={}), "recipe")
    assert snap["spine_present"] is False
    assert snap["parts_total"] == 0
    assert snap["empty"] is True
    assert snap["category"] == "recipe"


def test_snapshot_filled_when_spine_and_parts():
    spine = {"beat_chain": ["a", "b", "c"], "situation_type": "x"}
    items = {"hook": [{"text": "h1"}, {"text": "h2"}], "cta": [{"text": "c1"}]}
    recent = {"hooks": ["old"], "persons": [], "ctas": []}
    snap = bank_usage_snapshot(FakeStore(spine=spine, items=items, recent=recent), "recipe")
    assert snap["spine_present"] is True
    assert snap["spine_beats"] == 3
    assert snap["parts_by_bucket"]["hook"] == 2
    assert snap["parts_total"] == 3
    assert snap["avoid_present"] is True
    assert snap["empty"] is False


def test_snapshot_disabled_flag():
    snap = bank_usage_snapshot(FakeStore(enabled=""), "recipe")
    assert snap["bank_enabled"] is False
```

- [ ] **Step 2: Run test to verify it fails**

Run: `cd .tracks/수집검열 && python -m pytest shopping_shorts/tests/test_bank_usage_snapshot.py -v`
Expected: FAIL with `ImportError: cannot import name 'bank_usage_snapshot'`

- [ ] **Step 3: Write minimal implementation**

`bank_assemble.py` 끝에 추가(기존 `_sample_bucket`/`assemble_bank_context`는 그대로 두고 조회만 재사용):

```python
def bank_usage_snapshot(store, category, k=5):
    """생성 프롬프트에 은행이 무엇을 주입했나 — 계측 dict(읽기만, Gemini 없음).
    empty=True면 스파인·부품 통째로 비어 bank_context가 '' = 은행 무용."""
    enabled = store.get_setting("bank_enabled", "") == "1"
    spine = store.pick_spine_for_category(category) if category else None
    spine_beats = len((spine or {}).get("beat_chain") or []) if spine else 0
    parts_by_bucket = {}
    for b in STYLE_BUCKETS:
        pool = store.list_pattern_items(bucket=b, status="approved", order_by="perf",
                                        limit=max(k * _POOL_MULT, _POOL_MIN))
        n = min(len(pool), k)
        if n:
            parts_by_bucket[b] = n
    parts_total = sum(parts_by_bucket.values())
    rec = store.recent_script_usage()
    avoid_present = bool(rec.get("hooks") or rec.get("persons") or rec.get("ctas"))
    return {
        "bank_enabled": enabled,
        "spine_present": bool(spine),
        "spine_beats": spine_beats,
        "parts_by_bucket": parts_by_bucket,
        "parts_total": parts_total,
        "avoid_present": avoid_present,
        "category": category or "",
        "empty": (not spine) and parts_total == 0,
    }
```

- [ ] **Step 4: Run test to verify it passes**

Run: `cd .tracks/수집검열 && python -m pytest shopping_shorts/tests/test_bank_usage_snapshot.py -v`
Expected: PASS (3 passed)

- [ ] **Step 5: Commit**

```bash
git -C .tracks/수집검열 add shopping_shorts/bank_assemble.py shopping_shorts/tests/test_bank_usage_snapshot.py
git -C .tracks/수집검열 commit -m "feat(수집검열): bank_usage_snapshot — 생성 시 은행 주입 내역 계측"
```

---

### Task 2: 구조 순응 `structural_conformance`

**Files:**
- Create: `shopping_shorts/bank_usage_audit.py`
- Test: `shopping_shorts/tests/test_bank_usage_audit.py`

**Interfaces:**
- Consumes: 선택된 후보의 `plan` dict(`beats` 리스트, 각 beat `{role, narration, fit, seg_ids}`; `plagiarism_flags` 리스트), snapshot(Task 1), story dict(`{hook, cta_line, ...}`). `edit_plan._STRONG_OPENER_TOKENS`(튜플, lazy import).
- Produces: `structural_conformance(chosen_plan, snapshot, story) -> dict {beat_ok:bool, opener_ok:bool, cta_ok:bool, plagiarism_hits:int, conformant:bool}`

- [ ] **Step 1: Write the failing test**

```python
# shopping_shorts/tests/test_bank_usage_audit.py
from shopping_shorts.bank_usage_audit import structural_conformance


def _plan(beats, plag=None):
    return {"beats": beats, "plagiarism_flags": plag or []}


def test_conformant_when_opener_cta_beats_ok():
    beats = [{"narration": "이거 진짜 대박인데요?"}, {"narration": "b"},
             {"narration": "c"}, {"narration": "d"}, {"narration": "e"}]
    snap = {"spine_beats": 3}
    story = {"cta_line": "프로필 링크 확인하세요"}
    conf = structural_conformance(_plan(beats), snap, story)
    assert conf["beat_ok"] is True
    assert conf["opener_ok"] is True
    assert conf["cta_ok"] is True
    assert conf["plagiarism_hits"] == 0
    assert conf["conformant"] is True


def test_not_conformant_weak_opener_no_cta_short():
    beats = [{"narration": "매번 이렇게 하던 참이었거든요"}, {"narration": "b"}]
    snap = {"spine_beats": 5}
    story = {"cta_line": ""}
    conf = structural_conformance(_plan(beats), snap, story)
    assert conf["beat_ok"] is False       # 2 < max(5,5)
    assert conf["opener_ok"] is False
    assert conf["cta_ok"] is False
    assert conf["conformant"] is False


def test_plagiarism_breaks_conformance():
    beats = [{"narration": "이거 몰라서 손해 봤잖아요"}] + [{"narration": str(i)} for i in range(5)]
    story = {"cta_line": "댓글 남겨주세요"}
    conf = structural_conformance(_plan(beats, plag=[{"beat": 0}]), {"spine_beats": 3}, story)
    assert conf["plagiarism_hits"] == 1
    assert conf["conformant"] is False
```

- [ ] **Step 2: Run test to verify it fails**

Run: `cd .tracks/수집검열 && python -m pytest shopping_shorts/tests/test_bank_usage_audit.py -v`
Expected: FAIL with `ModuleNotFoundError: No module named 'shopping_shorts.bank_usage_audit'`

- [ ] **Step 3: Write minimal implementation**

```python
# shopping_shorts/bank_usage_audit.py
"""생성 순응 검열 — 빼쓰기(생성) 시 은행이 대본을 실제로 형성했나를 결정적으로 측정.

순수함수만(계산·판정). 저장·조회·Gemini는 호출부(mix_pipeline)가 한다.
기존 gemini_audit.py(넣기 쪽 검열)의 빼기 쪽 대응."""

_PARTS_MIN = 5
_BEAT_MIN = 5
# 신호등 임계 — 하나라도 걸리면 그 색(적신호가 주의를 이긴다).
_BANK_USED_MIN = 0.5
_CONFORMANCE_MIN = 0.7
_PLAGIARISM_MAX = 0.1
_ARC_MIN = 2.5   # 5점 만점


def _opener_ok(narration):
    """beats[0]이 강한 오프너인가 — edit_plan의 토큰 재사용."""
    from shopping_shorts.edit_plan import _STRONG_OPENER_TOKENS
    head = (narration or "").lstrip()[:14]
    return any(t.strip() and t.strip() in head for t in _STRONG_OPENER_TOKENS)


def structural_conformance(chosen_plan, snapshot, story):
    """선택 후보가 아크가 규정하는 '모양'을 가졌나 — LLM 없이."""
    beats = (chosen_plan or {}).get("beats") or []
    need = max((snapshot or {}).get("spine_beats") or 0, _BEAT_MIN)
    beat_ok = len(beats) >= need
    opener_ok = bool(beats) and _opener_ok(beats[0].get("narration"))
    cta_ok = bool(((story or {}).get("cta_line") or "").strip())
    plagiarism_hits = len((chosen_plan or {}).get("plagiarism_flags") or [])
    conformant = beat_ok and opener_ok and cta_ok and plagiarism_hits == 0
    return {"beat_ok": beat_ok, "opener_ok": opener_ok, "cta_ok": cta_ok,
            "plagiarism_hits": plagiarism_hits, "conformant": conformant}
```

- [ ] **Step 4: Run test to verify it passes**

Run: `cd .tracks/수집검열 && python -m pytest shopping_shorts/tests/test_bank_usage_audit.py -v`
Expected: PASS (3 passed)

- [ ] **Step 5: Commit**

```bash
git -C .tracks/수집검열 add shopping_shorts/bank_usage_audit.py shopping_shorts/tests/test_bank_usage_audit.py
git -C .tracks/수집검열 commit -m "feat(수집검열): structural_conformance — 결정적 구조 순응 판정"
```

---

### Task 3: 판단 감지 + 신호등 + 집계 `judge_snapshot`·`usage_health`·`compute_usage_audit`

**Files:**
- Modify: `shopping_shorts/bank_usage_audit.py`
- Test: `shopping_shorts/tests/test_bank_usage_audit.py` (케이스 추가)

**Interfaces:**
- Consumes: `sf["candidates"]` 리스트(각 `{score:float, recommended:bool, judge?:{script_quality,scene_sync,storyline,total}}`), Task 1·2의 레코드 리스트.
- Produces:
  - `judge_snapshot(candidates) -> dict {judged:bool, total:float|None, axes:dict|None, best_gap:float|None}`
  - `usage_health(agg) -> dict {level:str, reasons:list}`
  - `compute_usage_audit(recent) -> dict` (아래 필드)

- [ ] **Step 1: Write the failing test** (같은 파일에 추가)

```python
from shopping_shorts.bank_usage_audit import judge_snapshot, usage_health, compute_usage_audit


def test_judge_snapshot_picks_recommended_and_gap():
    cands = [{"score": 0.4, "recommended": False},
             {"score": 0.8, "recommended": True,
              "judge": {"script_quality": 4, "scene_sync": 3, "storyline": 5, "total": 0.8}}]
    js = judge_snapshot(cands)
    assert js["judged"] is True
    assert js["total"] == 0.8
    assert js["axes"]["storyline"] == 5
    assert abs(js["best_gap"] - 0.4) < 1e-9


def test_judge_snapshot_none_when_no_judge():
    js = judge_snapshot([{"score": 0.5, "recommended": True}])
    assert js["judged"] is False
    assert js["total"] is None
    assert js["best_gap"] is None   # 후보 1개


def test_usage_health_red_when_bank_unused():
    h = usage_health({"bank_used_rate": 0.49, "conformance_pass_rate": 0.9,
                      "plagiarism_rate": 0.0, "compliance": None})
    assert h["level"] == "🔴"
    assert any("무용" in r for r in h["reasons"])


def test_usage_health_yellow_on_low_conformance():
    h = usage_health({"bank_used_rate": 0.9, "conformance_pass_rate": 0.69,
                      "plagiarism_rate": 0.0, "compliance": None})
    assert h["level"] == "🟡"


def test_usage_health_green_all_pass():
    h = usage_health({"bank_used_rate": 0.9, "conformance_pass_rate": 0.8,
                      "plagiarism_rate": 0.05, "compliance": {"arc_avg": 3.5}})
    assert h["level"] == "🟢"


def test_compute_usage_audit_aggregates():
    recent = [
        {"snapshot": {"empty": False, "spine_present": True, "parts_total": 10},
         "conformance": {"conformant": True, "plagiarism_hits": 0},
         "judge": {"judged": True, "total": 0.8}, "compliance": None},
        {"snapshot": {"empty": True, "spine_present": False, "parts_total": 0},
         "conformance": {"conformant": False, "plagiarism_hits": 1},
         "judge": {"judged": False, "total": None},
         "compliance": {"arc_follow": 4, "flavor_follow": 3, "verbatim_copy": False}},
    ]
    agg = compute_usage_audit(recent)
    assert agg["n"] == 2
    assert agg["bank_used_rate"] == 0.5
    assert agg["plagiarism_rate"] == 0.5
    assert agg["judge_avg"] == 0.8            # judged 레코드만
    assert agg["compliance"]["arc_avg"] == 4.0
    assert agg["compliance"]["n"] == 1
    assert "level" in agg["health"]


def test_compute_usage_audit_empty_is_none_safe():
    agg = compute_usage_audit([])
    assert agg["n"] == 0
    assert agg["judge_avg"] is None
    assert agg["compliance"] is None
    assert agg["health"]["level"] == "🟢"
```

- [ ] **Step 2: Run test to verify it fails**

Run: `cd .tracks/수집검열 && python -m pytest shopping_shorts/tests/test_bank_usage_audit.py -v`
Expected: FAIL with `ImportError: cannot import name 'judge_snapshot'`

- [ ] **Step 3: Write minimal implementation** (`bank_usage_audit.py`에 추가)

```python
def judge_snapshot(candidates):
    """심사위원(candidate_judge) 결과를 그대로 기록 — 추가 계산 없음."""
    cands = candidates or []
    rec = next((c for c in cands if c.get("recommended")), cands[0] if cands else None)
    jr = (rec or {}).get("judge") if rec else None
    scores = sorted((c.get("score") or 0.0) for c in cands)
    best_gap = round(scores[-1] - scores[-2], 3) if len(scores) >= 2 else None
    if not jr:
        return {"judged": False, "total": None, "axes": None, "best_gap": best_gap}
    return {"judged": True, "total": jr.get("total"),
            "axes": {k: jr.get(k) for k in ("script_quality", "scene_sync", "storyline")},
            "best_gap": best_gap}


def usage_health(agg):
    """집계 지표 → {level, reasons}. 지표가 None이면 그 항목은 판정 제외."""
    reasons = []
    red = yellow = False
    bur = agg.get("bank_used_rate")
    if bur is not None and bur < _BANK_USED_MIN:
        red = True
        reasons.append(f"은행 사용률 {round(bur * 100)}% — 절반+ job에서 은행 무용(승인 부품·스파인 부족)")
    cpr = agg.get("conformance_pass_rate")
    if cpr is not None and cpr < _CONFORMANCE_MIN:
        yellow = True
        reasons.append(f"구조 순응 {round(cpr * 100)}% — 훅/CTA/비트 누락분")
    pr = agg.get("plagiarism_rate")
    if pr is not None and pr > _PLAGIARISM_MAX:
        yellow = True
        reasons.append(f"표절율 {round(pr * 100)}% — 은행을 그대로 베낀 조각 섞임")
    comp = agg.get("compliance")
    if comp and comp.get("arc_avg") is not None and comp["arc_avg"] < _ARC_MIN:
        yellow = True
        reasons.append(f"순응샘플 아크 {comp['arc_avg']}/5 — 제미니가 주입 아크 무시 경향")
    level = "🔴" if red else ("🟡" if yellow else "🟢")
    return {"level": level, "reasons": reasons}


def _mean(vals):
    vals = [v for v in vals if v is not None]
    return round(sum(vals) / len(vals), 3) if vals else None


def compute_usage_audit(recent):
    """job 레코드 리스트 → 집계 지표 + 신호등. 빈 리스트도 None-safe."""
    recent = recent or []
    n = len(recent)

    def rate(pred):
        return round(sum(1 for r in recent if pred(r)) / n, 3) if n else None

    snaps = [r.get("snapshot") or {} for r in recent]
    confs = [r.get("conformance") or {} for r in recent]
    judged = [r.get("judge") for r in recent if (r.get("judge") or {}).get("judged")]
    comps = [r.get("compliance") for r in recent if r.get("compliance")]
    compliance = None
    if comps:
        compliance = {
            "arc_avg": _mean([c.get("arc_follow") for c in comps]),
            "flavor_avg": _mean([c.get("flavor_follow") for c in comps]),
            "verbatim_rate": round(sum(1 for c in comps if c.get("verbatim_copy")) / len(comps), 3),
            "n": len(comps),
        }
    agg = {
        "n": n,
        "bank_used_rate": round(sum(1 for s in snaps if not s.get("empty")) / n, 3) if n else None,
        "spine_present_rate": round(sum(1 for s in snaps if s.get("spine_present")) / n, 3) if n else None,
        "parts_avg": _mean([s.get("parts_total") for s in snaps]),
        "conformance_pass_rate": round(sum(1 for c in confs if c.get("conformant")) / n, 3) if n else None,
        "plagiarism_rate": rate(lambda r: (r.get("conformance") or {}).get("plagiarism_hits", 0) > 0),
        "judge_avg": _mean([j.get("total") for j in judged]),
        "compliance": compliance,
    }
    agg["health"] = usage_health(agg)
    return agg
```

- [ ] **Step 4: Run test to verify it passes**

Run: `cd .tracks/수집검열 && python -m pytest shopping_shorts/tests/test_bank_usage_audit.py -v`
Expected: PASS (전체 통과)

- [ ] **Step 5: Commit**

```bash
git -C .tracks/수집검열 add shopping_shorts/bank_usage_audit.py shopping_shorts/tests/test_bank_usage_audit.py
git -C .tracks/수집검열 commit -m "feat(수집검열): judge_snapshot·usage_health·compute_usage_audit 집계·신호등"
```

---

### Task 4: 샘플 LLM 순응 채점 `judge_compliance`

**Files:**
- Create: `shopping_shorts/bank_compliance.py`
- Test: `shopping_shorts/tests/test_bank_compliance.py`

**Interfaces:**
- Consumes: 주입된 `bank_context`(문자열, 이미 스파인 charter + 부품 블록 포함), 선택 후보의 `beats` 리스트, `call(prompt, schema)` 주입점.
- Produces: `judge_compliance(bank_context, beats, call) -> dict|None {arc_follow:int, flavor_follow:int, verbatim_copy:bool, reason:str}`

- [ ] **Step 1: Write the failing test**

```python
# shopping_shorts/tests/test_bank_compliance.py
from shopping_shorts.bank_compliance import judge_compliance


def test_judge_compliance_returns_scores():
    captured = {}

    def fake_call(prompt, schema):
        captured["prompt"] = prompt
        return {"arc_follow": 4, "flavor_follow": 3, "verbatim_copy": False, "reason": "ok"}

    beats = [{"narration": "이거 대박"}, {"narration": "비법은"}]
    out = judge_compliance("★학습된 아크: 상황=요리\n[승인된 부품] 훅: h1", beats, call=fake_call)
    assert out["arc_follow"] == 4
    assert out["verbatim_copy"] is False
    assert "요리" in captured["prompt"]        # 주입 은행자료가 프롬프트에
    assert "비법은" in captured["prompt"]       # 생성 대본이 프롬프트에


def test_judge_compliance_clamps_and_none_on_failure():
    assert judge_compliance("ctx", [{"narration": "x"}], call=lambda p, s: None) is None
    out = judge_compliance("ctx", [{"narration": "x"}],
                           call=lambda p, s: {"arc_follow": 9, "flavor_follow": -2, "verbatim_copy": True})
    assert out["arc_follow"] == 5     # 0~5로 클램프
    assert out["flavor_follow"] == 0
    assert out["verbatim_copy"] is True


def test_judge_compliance_empty_beats_none():
    assert judge_compliance("ctx", [], call=lambda p, s: {"arc_follow": 3}) is None
```

- [ ] **Step 2: Run test to verify it fails**

Run: `cd .tracks/수집검열 && python -m pytest shopping_shorts/tests/test_bank_compliance.py -v`
Expected: FAIL with `ModuleNotFoundError: No module named 'shopping_shorts.bank_compliance'`

- [ ] **Step 3: Write minimal implementation**

```python
# shopping_shorts/bank_compliance.py
"""샘플 LLM 순응 채점 — 은행 주입 job 중 N에 1편만, 제미니가 주입 아크/부품을 실제로
따랐는지 채점(그대로 베낀 건 오히려 감점). 순수함수: call 주입점으로 실호출 회피."""
import sys

_SCORE = {"type": "integer"}   # 0~5
_COMPLIANCE_SCHEMA = {
    "type": "object",
    "properties": {
        "arc_follow": _SCORE,
        "flavor_follow": _SCORE,
        "verbatim_copy": {"type": "boolean"},
        "reason": {"type": "string"},
    },
    "required": ["arc_follow", "flavor_follow", "verbatim_copy"],
}


def _script_block(beats):
    return "\n".join(f"{i+1}. {(b.get('narration') or '').strip()}"
                     for i, b in enumerate(beats or []))


def _prompt(bank_context, beats):
    return (
        "너는 한국 쇼핑 숏폼 대본 감수자다. 아래 '주입 은행자료'는 대본 생성 시 참고하라고 "
        "넣어준 학습된 아크·부품이다. 생성된 대본이 그 아크(이야기 골격·비트 흐름)를 따랐는지, "
        "부품의 결(훅 톤·CTA 스타일)을 참고했는지 0~5로 채점해라(5=충실).\n"
        "★단 그대로 베낀 건 순응이 아니라 표절이다 — 문장을 거의 그대로 옮겼으면 "
        "verbatim_copy=true로 표시하고 점수를 낮춰라.\n\n"
        f"[주입 은행자료]\n{(bank_context or '')[:1800]}\n\n"
        f"[생성된 대본]\n{_script_block(beats)}\n\n"
        "arc_follow·flavor_follow(0~5 정수)·verbatim_copy(bool)·reason(한 줄) JSON만 출력.")


def judge_compliance(bank_context, beats, call):
    """주입 은행자료 vs 생성 대본 순응 채점. 실패/빈 beats/무키 → None(결정적 신호로 폴백)."""
    if not beats:
        return None
    try:
        res = call(_prompt(bank_context, beats), _COMPLIANCE_SCHEMA)
    except Exception as e:  # noqa: BLE001
        print(f"bank_compliance.judge_compliance: {e!r}", file=sys.stderr)
        return None
    if not res or not isinstance(res, dict):
        return None

    def _c(k):
        try:
            return max(0, min(5, int(res.get(k, 0))))
        except (TypeError, ValueError):
            return 0

    return {"arc_follow": _c("arc_follow"), "flavor_follow": _c("flavor_follow"),
            "verbatim_copy": bool(res.get("verbatim_copy")),
            "reason": (res.get("reason") or "").strip()}
```

- [ ] **Step 4: Run test to verify it passes**

Run: `cd .tracks/수집검열 && python -m pytest shopping_shorts/tests/test_bank_compliance.py -v`
Expected: PASS (3 passed)

- [ ] **Step 5: Commit**

```bash
git -C .tracks/수집검열 add shopping_shorts/bank_compliance.py shopping_shorts/tests/test_bank_compliance.py
git -C .tracks/수집검열 commit -m "feat(수집검열): judge_compliance — 샘플 LLM 순응 채점"
```

---

### Task 5: 링버퍼 `store.append_bank_usage`

**Files:**
- Modify: `shopping_shorts/store.py` (`recent_script_usage`(2673) 근처, 다른 set_setting 헬퍼 옆)
- Test: `shopping_shorts/tests/test_bank_usage_wiring.py`

**Interfaces:**
- Consumes: `self.get_setting`, `self.set_setting`.
- Produces: `Store.append_bank_usage(self, record, cap=50) -> list` (append 후 최근 cap개 리스트 반환)

- [ ] **Step 1: Write the failing test**

```python
# shopping_shorts/tests/test_bank_usage_wiring.py
from shopping_shorts.store import Store


def test_append_bank_usage_caps_and_persists(tmp_path):
    store = Store(str(tmp_path / "t.db"))
    for i in range(55):
        lst = store.append_bank_usage({"i": i}, cap=50)
    assert len(lst) == 50
    assert lst[0]["i"] == 5        # 앞 5개 밀려남
    assert lst[-1]["i"] == 54
    # 재조회로 영속 확인
    import json
    saved = json.loads(store.get_setting("bank_usage_recent"))
    assert len(saved) == 50
```

- [ ] **Step 2: Run test to verify it fails**

Run: `cd .tracks/수집검열 && python -m pytest shopping_shorts/tests/test_bank_usage_wiring.py::test_append_bank_usage_caps_and_persists -v`
Expected: FAIL with `AttributeError: 'Store' object has no attribute 'append_bank_usage'`

- [ ] **Step 3: Write minimal implementation** (`store.py`에 메서드 추가)

```python
    def append_bank_usage(self, record, cap=50):
        """생성 순응 job 레코드를 bank_usage_recent 링버퍼에 append(최근 cap개 유지). 반환=현재 리스트."""
        import json
        raw = self.get_setting("bank_usage_recent", "")
        try:
            lst = json.loads(raw) if raw else []
        except Exception:
            lst = []
        if not isinstance(lst, list):
            lst = []
        lst.append(record)
        lst = lst[-cap:]
        self.set_setting("bank_usage_recent", json.dumps(lst, ensure_ascii=False))
        return lst
```

- [ ] **Step 4: Run test to verify it passes**

Run: `cd .tracks/수집검열 && python -m pytest shopping_shorts/tests/test_bank_usage_wiring.py::test_append_bank_usage_caps_and_persists -v`
Expected: PASS

- [ ] **Step 5: Commit**

```bash
git -C .tracks/수집검열 add shopping_shorts/store.py shopping_shorts/tests/test_bank_usage_wiring.py
git -C .tracks/수집검열 commit -m "feat(수집검열): store.append_bank_usage 링버퍼(K=50)"
```

---

### Task 6: 생성 경로 배선 `mix_pipeline._record_bank_usage`

**Files:**
- Modify: `shopping_shorts/mix_pipeline.py` (`_record_bank_usage` 헬퍼 신규 + 생성 경로 449·480 근처 배선)
- Test: `shopping_shorts/tests/test_bank_usage_wiring.py` (케이스 추가)

**Interfaces:**
- Consumes: Task 1~5 전부. `pattern_bank._default_call`, `bank_usage_audit`, `bank_compliance`.
- Produces: `_record_bank_usage(store, snapshot, bank_context, rec, candidates, sample_n=10, cap=50, call=None) -> None` (내부에서 conformance·judge_snapshot·샘플게이트·compliance·append·compute·set_setting("bank_usage_audit_last") 전부 수행)

- [ ] **Step 1: Write the failing test** (같은 파일에 추가)

```python
import json
from shopping_shorts import mix_pipeline
from shopping_shorts.store import Store


def test_record_bank_usage_writes_audit_and_samples(tmp_path):
    store = Store(str(tmp_path / "t.db"))
    snap = {"empty": False, "spine_present": True, "spine_beats": 3, "parts_total": 8}
    rec = {"recommended": True, "score": 0.8,
           "plan": {"beats": [{"narration": "이거 대박인데요?"}] + [{"narration": str(i)} for i in range(5)],
                    "plagiarism_flags": []},
           "story": {"cta_line": "프로필 확인"},
           "judge": {"script_quality": 4, "scene_sync": 4, "storyline": 4, "total": 0.8}}
    calls = {"n": 0}

    def fake_call(prompt, schema):
        calls["n"] += 1
        return {"arc_follow": 4, "flavor_follow": 3, "verbatim_copy": False}

    # counter가 1이 되는 첫 호출 → 샘플됨(N=10, %N==1)
    mix_pipeline._record_bank_usage(store, snap, "ctx", rec, [rec], sample_n=10, call=fake_call)
    audit = json.loads(store.get_setting("bank_usage_audit_last"))
    assert audit["n"] == 1
    assert audit["bank_used_rate"] == 1.0
    assert audit["conformance_pass_rate"] == 1.0
    assert calls["n"] == 1                          # 첫 job 샘플됨
    assert audit["compliance"]["arc_avg"] == 4.0


def test_record_bank_usage_skips_llm_between_samples(tmp_path):
    store = Store(str(tmp_path / "t.db"))
    snap = {"empty": False, "spine_present": True, "spine_beats": 3, "parts_total": 8}
    rec = {"recommended": True, "score": 0.5,
           "plan": {"beats": [{"narration": "b"}], "plagiarism_flags": []},
           "story": {"cta_line": ""}}
    calls = {"n": 0}

    def fake_call(prompt, schema):
        calls["n"] += 1
        return {"arc_follow": 3, "flavor_follow": 3, "verbatim_copy": False}

    for _ in range(5):
        mix_pipeline._record_bank_usage(store, snap, "ctx", rec, [rec], sample_n=10, call=fake_call)
    assert calls["n"] == 1                          # 1번째만 샘플(%10==1), 2~5는 스킵
    audit = json.loads(store.get_setting("bank_usage_audit_last"))
    assert audit["n"] == 5
```

- [ ] **Step 2: Run test to verify it fails**

Run: `cd .tracks/수집검열 && python -m pytest shopping_shorts/tests/test_bank_usage_wiring.py -k record_bank_usage -v`
Expected: FAIL with `AttributeError: module 'shopping_shorts.mix_pipeline' has no attribute '_record_bank_usage'`

- [ ] **Step 3: Write minimal implementation**

(a) `mix_pipeline.py`에 헬퍼 추가(모듈 함수, `_generate_mix_job` 위/근처):

```python
def _record_bank_usage(store, snapshot, bank_context, rec, candidates,
                       sample_n=10, cap=50, call=None):
    """생성 순응 검열 레코드 1건을 만들어 링버퍼에 쌓고 집계를 저장한다(부가 관측, 실패 무해).
    은행 주입 job 중 sample_n에 1편만 제미니 순응 채점(counter % N == 1)."""
    from shopping_shorts import bank_usage_audit, bank_compliance
    plan = (rec or {}).get("plan") or {}
    story = (rec or {}).get("story") or {}
    conf = bank_usage_audit.structural_conformance(plan, snapshot, story)
    jsnap = bank_usage_audit.judge_snapshot(candidates)
    compliance = None
    if not snapshot.get("empty"):
        counter = 0
        try:
            counter = int(store.get_setting("bank_compliance_counter", "0") or "0")
        except (TypeError, ValueError):
            counter = 0
        counter += 1
        store.set_setting("bank_compliance_counter", str(counter))
        if counter % sample_n == 1:
            if call is None:
                from shopping_shorts import pattern_bank
                call = pattern_bank._default_call
            compliance = bank_compliance.judge_compliance(bank_context, plan.get("beats"), call)
    record = {"snapshot": snapshot, "conformance": conf, "judge": jsnap, "compliance": compliance}
    recent = store.append_bank_usage(record, cap=cap)
    import json as _json
    agg = bank_usage_audit.compute_usage_audit(recent)
    store.set_setting("bank_usage_audit_last", _json.dumps(agg, ensure_ascii=False))
```

(b) 생성 경로 배선 — `mix_pipeline.py` 449번째 줄(`avoid_hooks = None`) 바로 뒤에 스냅샷 변수 초기화:

```python
        bank_context = ""
        avoid_hooks = None
        bank_snapshot = None        # ← 추가: 생성 순응 검열용 주입 스냅샷
```

`bank_context = "\n\n".join(...)`(457) 다음, 같은 try 블록 안에 스냅샷 캡처 추가:

```python
                bank_context = "\n\n".join(x for x in _blocks if x)
                bank_snapshot = bank_assemble.bank_usage_snapshot(store, video_type or "")  # ← 추가
```

그리고 `store.record_script_usage(...)` 블록(479-482)의 `except` 다음, 여전히 `if sf["candidates"]:` 안에 순응 기록 추가:

```python
            except Exception:
                traceback.print_exc(file=sys.stderr)
            # 생성 순응 검열(부가 관측, 실패해도 job 안 죽인다) — 은행이 켜졌던 job만
            if bank_snapshot is not None:
                try:
                    _record_bank_usage(store, bank_snapshot, bank_context, rec, sf["candidates"])
                except Exception:
                    traceback.print_exc(file=sys.stderr)
```

- [ ] **Step 4: Run test to verify it passes**

Run: `cd .tracks/수집검열 && python -m pytest shopping_shorts/tests/test_bank_usage_wiring.py -v`
Expected: PASS (전체)

- [ ] **Step 5: Run import smoke + commit**

Run: `cd .tracks/수집검열 && python -c "import shopping_shorts.app"`
Expected: 에러 없이 종료(배선이 import를 안 깼는지).

```bash
git -C .tracks/수집검열 add shopping_shorts/mix_pipeline.py shopping_shorts/tests/test_bank_usage_wiring.py
git -C .tracks/수집검열 commit -m "feat(수집검열): mix_pipeline 생성 순응 배선 — 스냅샷·순응·샘플게이트·집계"
```

---

### Task 7: 리포트 payload에 `usage_audit` 첨부

**Files:**
- Modify: `shopping_shorts/app.py:204-215` (`api_bank_ingest_report`)
- Test: `shopping_shorts/tests/test_bank_usage_wiring.py` (케이스 추가)

**Interfaces:**
- Consumes: `bank_usage_audit_last` 설정(Task 6이 저장).
- Produces: `/api/bank/ingest_report`의 `report`에 `usage_audit` 키(있을 때만).

- [ ] **Step 1: Write the failing test** (같은 파일에 추가)

```python
def test_ingest_report_attaches_usage_audit(tmp_path, monkeypatch):
    import json
    from shopping_shorts import app as app_mod
    store = Store(str(tmp_path / "t.db"))
    store.set_setting("bank_usage_audit_last",
                      json.dumps({"n": 3, "bank_used_rate": 1.0, "health": {"level": "🟢", "reasons": []}}))
    monkeypatch.setattr(app_mod, "DB_PATH", str(tmp_path / "t.db"))
    out = app_mod.api_bank_ingest_report()
    assert out["report"]["usage_audit"]["n"] == 3
```

- [ ] **Step 2: Run test to verify it fails**

Run: `cd .tracks/수집검열 && python -m pytest shopping_shorts/tests/test_bank_usage_wiring.py::test_ingest_report_attaches_usage_audit -v`
Expected: FAIL (`KeyError: 'usage_audit'`)

- [ ] **Step 3: Write minimal implementation** (`api_bank_ingest_report` 교체)

```python
@app.get("/api/bank/ingest_report")
def api_bank_ingest_report():
    """'지금 수집' 후 은행 적재 결과 + 생성 순응 검열(usage_audit)을 함께 보고."""
    import json as _json
    store = Store(DB_PATH)
    raw = store.get_setting("bank_ingest_last", "")
    report = {"status": "none"}
    if raw:
        try:
            report = _json.loads(raw)
        except Exception:
            report = {"status": "none"}
    ua_raw = store.get_setting("bank_usage_audit_last", "")
    if ua_raw:
        try:
            report["usage_audit"] = _json.loads(ua_raw)
        except Exception:
            pass
    return {"ok": True, "report": report}
```

- [ ] **Step 4: Run test to verify it passes**

Run: `cd .tracks/수집검열 && python -m pytest shopping_shorts/tests/test_bank_usage_wiring.py::test_ingest_report_attaches_usage_audit -v`
Expected: PASS

- [ ] **Step 5: Commit**

```bash
git -C .tracks/수집검열 add shopping_shorts/app.py shopping_shorts/tests/test_bank_usage_wiring.py
git -C .tracks/수집검열 commit -m "feat(수집검열): ingest_report에 usage_audit 첨부"
```

---

### Task 8: 대시보드 "생성 순응" 섹션

**Files:**
- Modify: `shopping_shorts/static/index.html:746-765` (`_renderBankPanel`)
- Test: `node --check`(구문) + 육안(브라우저)

**Interfaces:**
- Consumes: `rep.usage_audit`(Task 7). 기존 헬퍼 `_pct(x)`(0~1 → %) 재사용.

- [ ] **Step 1: `_renderBankPanel`에 순응 섹션 추가**

`_renderBankPanel`의 `return html;`(764) 바로 앞에 삽입:

```javascript
  const u=rep.usage_audit;
  if(u && u.n){
    const uh=u.health||{};
    const c=u.compliance;
    html+=`<br>🎬 <b>생성 순응 ${uh.level||''}</b> (최근 ${u.n}편) `
      +`은행 사용률 ${_pct(u.bank_used_rate)}`
      +` · 구조순응 ${_pct(u.conformance_pass_rate)}`
      +` · 표절 ${_pct(u.plagiarism_rate)}`
      +(u.judge_avg!=null?` · 심사 ${(u.judge_avg*5).toFixed(1)}/5`:'');
    if(c){ html+=`<br><span style="color:var(--sub)">순응샘플(${c.n}) `
      +`아크 ${c.arc_avg}/5 · 부품 ${c.flavor_avg}/5`
      +(c.verbatim_rate?` · 표절 ${_pct(c.verbatim_rate)}`:'')+`</span>`; }
    (uh.reasons||[]).forEach(r=>{ html+=`<br><span style="color:#c0392b">⚠️ ${r}</span>`; });
  }
```

- [ ] **Step 2: JS 구문 검증**

Run: `cd .tracks/수집검열 && node --check shopping_shorts/static/index.html 2>&1 || echo "note: HTML은 순수 JS 아님 — <script> 블록만 검사 대상"`

> ⚠️ `index.html`은 HTML이라 `node --check`가 통째론 실패한다. 대신 편집한 `_renderBankPanel` 함수 본문을 임시 `.js`로 떼어 검사하거나, 브라우저 콘솔에 SyntaxError 없는지 확인한다. 최소 게이트: 삽입한 블록의 백틱·중괄호 짝이 맞는지 눈으로 확인(기존 `if(a){...}` 블록과 동일 패턴).

- [ ] **Step 3: 브라우저 육안 확인(라이브 배포 후)**

수집 화면(instagram 플랫폼)에서 '지금 수집' 후 패널에 `🎬 생성 순응` 줄이 뜨는지. 생성 이력이 있어야 `usage_audit`가 채워지므로, 영상 믹스를 1편 이상 만든 뒤라야 보인다.

- [ ] **Step 4: Commit**

```bash
git -C .tracks/수집검열 add shopping_shorts/static/index.html
git -C .tracks/수집검열 commit -m "feat(수집검열): 대시보드 생성 순응 섹션"
```

---

### Task 9: 전체 회귀 + finish

**Files:** 없음(검증·배포)

- [ ] **Step 1: 신규 테스트 전체 통과**

Run: `cd .tracks/수집검열 && python -m pytest shopping_shorts/tests/test_bank_usage_snapshot.py shopping_shorts/tests/test_bank_usage_audit.py shopping_shorts/tests/test_bank_compliance.py shopping_shorts/tests/test_bank_usage_wiring.py -v`
Expected: 전부 PASS

- [ ] **Step 2: 관련 회귀 확인(은행·믹스 배선 안 깨졌나)**

Run: `cd .tracks/수집검열 && python -m pytest shopping_shorts/tests/test_mix_bank_wiring.py shopping_shorts/tests/test_bank_assemble.py shopping_shorts/tests/test_gemini_audit.py -q`
Expected: 병합 전 기준선 대비 신규 실패 0.

- [ ] **Step 3: import 스모크**

Run: `cd .tracks/수집검열 && python -c "import shopping_shorts.app; print('import ok')"`
Expected: `import ok`

- [ ] **Step 4: finish(게이트 통과 시 main 병합 → 라이브)**

Run: `py tools/track.py finish 수집검열`
Expected: 문법 → `import shopping_shorts.app` → pytest 게이트 통과 → origin/main 병합. 게이트가 막으면 라이브는 무사, 고치고 재실행.

- [ ] **Step 5: 라이브 반영 확인 후 핸드오프·대시보드 갱신**

3분 뒤 shoppingshorts.duckdns.org에서 수집 후 `🎬 생성 순응` 섹션 확인. `handoff/수집검열.md` NEXT 갱신(생성 순응 라이브·후속=흡수 성공률 개선).

---

## Self-Review

**1. Spec coverage:**
- A 사용 스냅샷 → Task 1 ✅
- B 구조 순응 → Task 2 ✅
- C 판단 감지 → Task 3(judge_snapshot) ✅
- D 샘플 LLM → Task 4(judge_compliance) + Task 6(샘플 게이트 counter %N==1) ✅
- E 기록·집계 → Task 5(append_bank_usage) + Task 3(compute_usage_audit) + Task 6(저장) ✅
- F 대시보드 → Task 7(payload) + Task 8(패널) ✅
- 에러 처리(회귀0·부가 관측 실패 삼킴) → Task 6 배선의 try/except + bank_enabled 게이트 ✅
- 테스트 → 각 Task TDD + Task 9 회귀 ✅

**2. Placeholder scan:** 코드 스텝 전부 실제 코드. Task 8 Step 2는 도구 한계(HTML이라 node --check 통짜 불가)를 명시하고 대체 게이트 제시 — 플레이스홀더 아님.

**3. Type consistency:**
- `structural_conformance` 반환 키(beat_ok/opener_ok/cta_ok/plagiarism_hits/conformant)를 Task 3 집계·Task 6에서 동일 사용 ✅
- `judge_snapshot` 반환(judged/total/axes/best_gap)을 compute_usage_audit이 `judge.judged`·`judge.total`로 읽음 ✅
- `judge_compliance` 반환(arc_follow/flavor_follow/verbatim_copy)을 compute가 동일 키로 집계 ✅
- 레코드 shape `{snapshot,conformance,judge,compliance}`를 Task 6 생성 ↔ Task 3 compute_usage_audit 소비 일치 ✅
- 샘플 게이트 `counter % sample_n == 1` → 첫 job(counter=1) 샘플, Task 6 테스트가 이 규칙 검증 ✅

## 알려진 한계(스코프 밖, 후속)

- `usage_audit` 패널은 기존 폴링(수집 'done' 시)에 얹혀 렌더되므로, 수집을 한 번도 안 하면 생성 순응만 따로는 안 보인다. 항상노출이 필요하면 별도 폴링/엔드포인트로 분리(후속).
- judge는 현재 `backbone_base` 모드에서만 켜지므로 `judge_avg`는 그 모드 job에서만 채워진다(설계 의도, None-safe).
