# 재생바자리 — 재생바가 인스타 댓글창을 덮던 것

## 2026-09-02 사장님 제보
스크린샷에 화살표로 표시: 재생바(⏸ 1x ▬▬ 0:10/0:21 2026-09-01)를
**담기 버튼 바로 아래**로 옮겨달라. 이유: **"댓글을 못 써서"**.

## 원인
`grab_logic.js` `syncSeekBar()`가 재생바를 `position:fixed; bottom:174px` **고정**으로 뒀다.
인스타는 화면 아래쪽에 **댓글 입력창**을 두므로, 창 크기·기기에 따라 재생바가 그 위를
덮어 댓글을 못 쓰는 상태가 됐다.

★담기 버튼(`ss-grab-btn`)도 `bottom:18px` 고정이라 **둘 사이 간격이 창마다 달라진다.**
  자리를 두 곳에 따로 적어두면 언젠가 반드시 어긋난다(0순위-B).

## 고친 것
`_placeSeekBar(box)` 신설 — **담기 버튼의 실제 화면 좌표를 읽어**(getBoundingClientRect)
그 10px 아래, 오른쪽 끝을 맞춰 붙인다. `bottom`은 `auto`로 풀어 옛 고정값을 확실히 없앤다.
`syncSeekBar`가 매 주기 호출하므로 **창을 줄이거나 화면이 바뀌어도 담기 버튼을 따라간다.**

안전장치 두 가지 — 어느 쪽이든 종전 자리를 그대로 두고 다음 주기에 다시 시도한다:
- 담기 버튼이 아직 안 그려졌을 때
- 숨어 있어 좌표가 0일 때

## 검증
- 새 테스트 4건(node로 좌표 계산 직접 실행): 담기 아래 10px·오른쪽 정렬·bottom 해제 /
  버튼 없으면 안 건드림 / 좌표 0이면 안 건드림 / 호출부가 실제로 걸려 있는지
- 유저스크립트 관련 기존 61건 통과
- `node --check` 통과

⏭ 배포 후 **인스타 릴스에서 눈으로 확인 필요** — 유저스크립트는 브라우저 확장이라
   자동화 브라우저로는 재현이 안 된다. 사장님이 새로고침 후 봐주셔야 한다.
   ⚠️유저스크립트는 브라우저에 캐시되므로 **탬퍼몽키에서 스크립트 갱신**이 필요할 수 있다.

## 2026-09-03 — 검은 판 재발 3번째, 뿌리를 잡았다

사장님: "어제 저거 고치라고 그렇게 했는데 왜 못한거냐 / 몇번 재시도했는데 못한거야"
스샷 3장(유튜브·인스타 릴·인스타 /reel/ 직접주소): 재생바 밑으로 검은 판이 영상 바닥까지.

### 진짜 원인 (코드 실측, 서버=로컬 MD5 동일이라 캐시 아님)
같은 tick에서 **자리를 두 함수가 따로 정했다**:
- `_placeSeekBar`(09-02 신설) → `top` + `right`, bottom을 auto !important로
- 바로 뒤 `_dockBtns`(09-01 신설) → `left` + `bottom`을 덮어씀(`style.bottom=`은 important를 지운다)
→ top·bottom·left·right 넷 다 걸려 고정 배치 상자가 그 사이만큼 늘어남. 09-02 "높이 못박기"
(height:auto)는 top+bottom 동시 배치에선 무력. 검증도 `_placeSeekBar` 하나만 떼어 돌려 못 잡았다.
**0순위-B 위반이 원인이다.**

### 고친 것
- `_placeSeekBar` 삭제. `_dockBtns`가 도킹 줄 마지막 버튼 한 칸(52px) 아래에 `top/left`만 걸고
  `bottom/right`는 auto. 영상 없으면 종전 자리(right18/bottom174)로 되돌리며 top/left는 비운다.
- `/reel/` 직접주소 화면: 댓글 입력창(textarea)을 품은 조상 칸은 dialog처럼 폭 가드 면제
  (단 화면폭 85% 넘는 칸은 버림). → 버튼 줄이 댓글판 바깥 오른쪽으로.
- LOGIC_VER 20260908→20260909.
- 테스트: `test_seekbar_below_grab.py` 전면 재작성(두 함수 순서대로 돌린 **최종 스타일** 검사,
  자리 함수가 둘 이상이면 실패), `test_grab_btn_dock.py` +3. 14 passed / 관련 99 passed.

### 배포
b051c2bbe → finish 통과 → 13:23 서버 `/grab_logic.js` LOGIC_VER 20260909 확인(curl).

### ⏭ 미확인
- **실브라우저 눈 확인은 못 했다.** 자동화 탭은 유튜브가 빈 화면으로 뜨고 인스타 로그인 재현 불가.
  사장님이 새로고침(탬퍼몽키는 1분 캐시) 후 봐주셔야 한다. 특히 /reel/ 직접주소의 textarea
  가정은 DOM 실측이 아니라 추정 — 여전히 댓글판 위에 뜨면 그 화면 DOM을 봐야 한다.
