# 좋은장면우선 로그

## 2026-09-06 (새벽, 집PC)
- 사장님: "좋은 장면이 다양한데 국내 영상 위주다. 씨앗은 그대로, 모든 장면매칭은 좋은장면 우선."
- 실측(라이브 dbddcce899bb, 레트로 카메라, 소스 7편·97구간): 씨앗(국내 10구간·249자)이 4칸을 가져가고
  **말이 가장 풍부한 외국 소스 3편(50구간·1,969자·번역 완료)이 0칸**이었다.
- 원인: `_mix_source_block` 끝의 "주제는 [대본 1], 나머지는 참고만"이 **주제 고정과 그림 선택을 뭉뚱그렸다**.
  그 문구는 2026-08-17 사고(치아바타→도마)를 막는 정당한 것인데, 같은 제품을 여러 각도로 찍은 소스의
  **장면까지** 막고 있었다.
- 고침: 주제는 그대로 고정하되 **"장면(그림)은 예외 — 그 줄을 가장 잘 보여주는 장면을 소스 구분 없이"** 추가.
- ★검증: 실제 대본 재생성 — 같은 제품 2편은 s0만 5개 → **s0 3 + s1 2**로 고루 씀(게이트 통과·중복 0),
  다른 제품 2편은 안 섞음(정상). 테스트 3건 + 사보타주 확인, 관련 120건 green.
- ⚠️`py - <<EOF`로 `\n`을 쓰면 실제 개행으로 풀려 문자열이 깨진다(오늘 2번 밟음).

## 2026-09-06 (낮) — 라이브 장애 2건(둘 다 내 배포 실수) + 채우기 진단
- 🔴①AI 키 소진: 1단계를 **전체 고객**에게 켠 것(429 3시간 2,054건). 새 방식은 **구간마다** AI를 부른다
  (옛 방식은 영상 1편당 1번) — 구간 114개짜리 영상 하나가 키를 태웠다. `frame_extract_enabled=0`으로 되돌림.
  ★1단계엔 `admin` 옵션이 아예 없다(`== "1"`만 봄) → 켜려면 전체뿐이었다. ⏭`_setting_gate`로 갈 것.
- 🔴②컷 목록 빈 화면(약 40분): `const bounds`를 아래에서 재대입 → planClips가 죽어 render 사슬 전멸.
  서버는 전부 정상이었고(구간 97·편성 9·썸네일 43KB) **Playwright로 브라우저 콘솔을 봐서** 잡았다.
  const→let 한 글자. ★배경은 **낮 12:51 수동 배포** — 서버는 새벽 2~6시에만 배포하는데 그걸 어겨
  다른 세션의 컷 작업이 절반만 들어갔다.
- 재발 방지: `test_scene_play_runs.py` — scene_play.js를 node로 **실제 실행**(node --check는 문법만 본다).
  ★이 테스트가 두 번 가짜였다(인자 타입·전역 덮어쓰기) — 사보타주로만 드러났다.
- ✅1차 수정 라이브 실측: 외국 영상 **0칸 → 3칸**, 3개 영상 고루, 대본↔장면 **6/6 정확**.
- 채우기(✨) 진단: ★**오늘 고친 것과 다른 경로**(edit_plan.fill_beat_scenes) — 달라지는 게 없다.
  실측은 6장 중 5장 정확(83%)으로 나쁘지 않았다 → 불만은 **키 소진 시 규칙 폴백** 등 다른 조건일 가능성.
  ⏭불만이 나온 실제 job으로 재현해야 한다.
