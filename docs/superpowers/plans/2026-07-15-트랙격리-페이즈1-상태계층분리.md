# 트랙 격리 페이즈1 — 상태 계층 분리 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 6개 동시 세션이 서로 덮어쓰는 상태 문서(대시보드 카드·핸드오프·로그)를 트랙별로 분리해, 덮임 사고를 구조적으로 불가능하게 만든다.

**Architecture:** 공유 단일 파일을 트랙별 파일로 쪼갠다. 대시보드는 `log.json`에 `track` 필드를 추가하고 dedup 키를 `(date, category)` → `(date, category, track)`로 바꿔 카테고리 안에 트랙별 서브카드를 렌더한다. 핸드오프·로그는 `handoff/<트랙>.md`·`wiki/log.d/<트랙>.md`로 소유권을 나누고, 합쳐 보는 뷰는 **읽기 전용 생성**만 한다(파일을 덮어쓰지 않는다).

**Tech Stack:** Python 3 (표준 라이브러리만), pytest, git

**설계 문서:** `docs/superpowers/specs/2026-07-15-동시세션-충돌차단-트랙격리-design.md` (§4.3 상태 계층, §5 롤아웃 페이즈1)

## Global Constraints

- **`--track`은 반드시 선택(optional) 인자다.** 지금 돌고 있는 5개 세션의 stop 훅은 `--track` 없는 옛 명령형을 쓴다. 필수로 만들면 그 세션들이 즉시 전부 실패한다. 기본값 `"(미지정)"`.
- **`track` 없는 기존 `log.json` 엔트리와 하위호환.** 항상 `e.get("track") or "(미지정)"`로 읽는다. 절대 `e["track"]`로 읽지 않는다.
- **`C:\Users\TheRose\.claude\`는 git 저장소가 아니다** (실측: `git rev-parse` → `fatal: not a git repository`). 대시보드 파일은 버전관리가 없으므로 **커밋 스텝이 없다.** 대신 각 대시보드 태스크는 수정 전 `.bak` 백업을 남긴다.
- **저장소(`로또의 주식`) 작업은 커밋 = 즉시 푸시 = 3분 뒤 라이브다** (`.git/hooks/post-commit`이 `git push`를 무조건 실행). 커밋 전에 staged 내용을 반드시 확인한다.
- **`git add`는 내 파일만.** `git add -A` 절대 금지 (6세션이 `.git`과 인덱스를 공유한다).
- **`git commit -- <경로> -m "..."` 형식은 실패한다** — `--` 뒤는 전부 pathspec으로 먹힌다. `git add <경로>` 후 `git commit -m "..."`를 쓴다.
- 대시보드 경로는 공백·한글이 없다: `C:\Users\TheRose\.claude\dashboard\`. 저장소 경로는 공백·한글이 있다: `C:\Users\TheRose\Desktop\로또의 주식\` → 셸에서 항상 따옴표로 감싼다.
- 이 계획은 **다른 5개 세션이 돌아가는 중에 실행된다.** 남의 파일을 건드리는 스텝은 없어야 한다.

---

### Task 1: 대시보드 CLI — track 필드 + 원자적 쓰기 + 락

**Files:**
- Create: `C:\Users\TheRose\.claude\dashboard\dashboard_common.py`
- Modify: `C:\Users\TheRose\.claude\dashboard\dashboard_cli.py`
- Test: `C:\Users\TheRose\.claude\dashboard\test_dashboard_cli.py`

**Interfaces:**
- Consumes: 없음 (첫 태스크)
- Produces:
  - `dashboard_common.DEFAULT_TRACK = "(미지정)"`
  - `dashboard_common.entry_track(entry: dict) -> str` — Task 2도 이걸 import한다
  - `add_entry(category: str, summary: str, next_step: str, track: str | None = None) -> None`
  - `save_log(log: list[dict]) -> None` — temp+replace 원자적 쓰기
  - `file_lock(timeout: float = 10.0, stale: float = 60.0)` — 컨텍스트 매니저
  - `log.json` 엔트리 스키마: `{"date", "category", "track", "summary", "next"}`

> **`dashboard_common.py`를 새로 만드는 이유**: `DEFAULT_TRACK`·`entry_track`을 CLI와 렌더러가 모두 쓴다. `dashboard_cli`가 이미 `render_dashboard`를 import하므로 반대 방향 import를 추가하면 순환이 된다. 양쪽이 공용 모듈을 import하는 게 유일하게 깨끗한 방법이다.

- [ ] **Step 1: 수정 전 백업**

```bash
cd "C:/Users/TheRose/.claude/dashboard"
cp dashboard_cli.py dashboard_cli.py.bak
cp log.json log.json.bak
```

기대: 두 `.bak` 파일 생성. (이 디렉터리는 git이 아니므로 이게 유일한 되돌리기 수단이다.)

- [ ] **Step 2: 실패하는 테스트 작성**

`test_dashboard_cli.py`에 아래를 **추가**한다(기존 테스트는 지우지 않는다).

```python
def test_add_entry_records_track(tmp_path, monkeypatch):
    _config, log_path, _html = _init_files(tmp_path, monkeypatch)
    cli.add_entry("스탁브레인", "요약", "다음", track="꾸미기")
    log = json.loads(log_path.read_text(encoding="utf-8"))
    assert log[0]["track"] == "꾸미기"


def test_add_entry_different_tracks_coexist_same_day(tmp_path, monkeypatch):
    _config, log_path, _html = _init_files(tmp_path, monkeypatch)
    cli.add_entry("스탁브레인", "꾸미기 요약", "꾸미기 다음", track="꾸미기")
    cli.add_entry("스탁브레인", "보이스 요약", "보이스 다음", track="보이스")
    log = json.loads(log_path.read_text(encoding="utf-8"))
    assert len(log) == 2
    tracks = {e["track"] for e in log}
    assert tracks == {"꾸미기", "보이스"}


def test_add_entry_same_track_same_day_replaces(tmp_path, monkeypatch):
    _config, log_path, _html = _init_files(tmp_path, monkeypatch)
    cli.add_entry("스탁브레인", "첫 요약", "첫 다음", track="꾸미기")
    cli.add_entry("스탁브레인", "수정 요약", "수정 다음", track="꾸미기")
    log = json.loads(log_path.read_text(encoding="utf-8"))
    assert len(log) == 1
    assert log[0]["summary"] == "수정 요약"


def test_add_entry_without_track_defaults(tmp_path, monkeypatch):
    _config, log_path, _html = _init_files(tmp_path, monkeypatch)
    cli.add_entry("스탁브레인", "요약", "다음")
    log = json.loads(log_path.read_text(encoding="utf-8"))
    assert log[0]["track"] == "(미지정)"


def test_add_entry_replaces_legacy_entry_without_track_field(tmp_path, monkeypatch):
    _config, log_path, _html = _init_files(tmp_path, monkeypatch)
    today = date.today().isoformat()
    log_path.write_text(json.dumps([
        {"date": today, "category": "스탁브레인", "summary": "옛것", "next": "옛다음"}
    ], ensure_ascii=False), encoding="utf-8")
    cli.add_entry("스탁브레인", "새것", "새다음")
    log = json.loads(log_path.read_text(encoding="utf-8"))
    assert len(log) == 1
    assert log[0]["summary"] == "새것"
```

> 마지막 테스트가 핵심 하위호환 검사다. `track` 필드가 없는 옛 엔트리를 `e["track"]`로 읽으면 `KeyError`로 죽는다.

- [ ] **Step 3: 테스트 실패 확인**

```bash
cd "C:/Users/TheRose/.claude/dashboard"
py -m pytest test_dashboard_cli.py -v
```

기대: `test_add_entry_records_track` 등이 FAIL — `add_entry() got an unexpected keyword argument 'track'`

- [ ] **Step 4: 구현**

먼저 `dashboard_common.py`를 새로 만든다:

```python
"""CLI와 렌더러가 공유하는 트랙 헬퍼.

dashboard_cli가 render_dashboard를 import하므로, 양쪽이 쓰는 것은
여기에 두어야 순환 import를 피할 수 있다.
"""

DEFAULT_TRACK = "(미지정)"


def entry_track(entry):
    """하위호환: track 필드가 없는 옛 엔트리도 안전하게 읽는다.

    entry["track"]로 읽으면 2026-07-15 이전 엔트리에서 KeyError로 죽는다.
    """
    return entry.get("track") or DEFAULT_TRACK
```

`dashboard_cli.py` 상단 import에 추가:

```python
import os
import time
from contextlib import contextmanager

from dashboard_common import DEFAULT_TRACK, entry_track
```

`LOG_PATH` 정의 아래에 추가:

```python
LOCK_PATH = BASE / "log.lock"


@contextmanager
def file_lock(timeout=10.0, stale=60.0):
    """log.json 읽기-수정-쓰기 구간을 직렬화한다.

    6개 세션이 동시에 add_entry를 호출하면 lost update가 난다.
    stale: 세션이 죽어 락이 남은 경우 이 초 뒤 자동 해제.
    """
    start = time.monotonic()
    while True:
        try:
            fd = os.open(LOCK_PATH, os.O_CREAT | os.O_EXCL | os.O_WRONLY)
            os.write(fd, str(os.getpid()).encode())
            os.close(fd)
            break
        except FileExistsError:
            try:
                if time.time() - LOCK_PATH.stat().st_mtime > stale:
                    LOCK_PATH.unlink()
                    continue
            except FileNotFoundError:
                continue
            if time.monotonic() - start > timeout:
                raise SystemExit("log.json 락 획득 실패 — 다른 세션이 쓰는 중. 잠시 후 재시도.")
            time.sleep(0.1)
    try:
        yield
    finally:
        try:
            LOCK_PATH.unlink()
        except FileNotFoundError:
            pass
```

`save_log`를 원자적 쓰기로 교체:

```python
def save_log(log):
    tmp = LOG_PATH.with_name(LOG_PATH.name + ".tmp")
    tmp.write_text(json.dumps(log, ensure_ascii=False, indent=2), encoding="utf-8")
    tmp.replace(LOG_PATH)
```

`add_entry`를 교체 (`entry_track`은 `dashboard_common`에서 import했다):

```python
def add_entry(category, summary, next_step, track=None):
    config = load_config()
    if category not in category_names(config):
        raise SystemExit(f"unknown category: {category!r}, must be one of {category_names(config)}")
    track = track or DEFAULT_TRACK
    today = date.today().isoformat()
    with file_lock():
        log = load_log()
        log = [
            e for e in log
            if not (e["date"] == today and e["category"] == category and entry_track(e) == track)
        ]
        log.append({
            "date": today,
            "category": category,
            "track": track,
            "summary": summary,
            "next": next_step,
        })
        log = prune(log)
        save_log(log)
    render()
```

`main()`의 `add_p`에 인자 추가 + 호출부 수정:

```python
    add_p.add_argument("--track", default=None)
```

```python
    if args.command == "add":
        add_entry(args.category, args.summary, args.next_step, track=args.track)
```

> `--track`에 `required=True`를 절대 붙이지 않는다 (Global Constraints 참조).

- [ ] **Step 5: 테스트 통과 확인**

```bash
cd "C:/Users/TheRose/.claude/dashboard"
py -m pytest test_dashboard_cli.py -v
```

기대: 신규 5개 + 기존 4개 전부 PASS.

> 기존 `test_add_entry_same_day_replaces_not_duplicates`는 track 없이 두 번 호출하므로 둘 다 `"(미지정)"`이 되어 여전히 `len(log) == 1`로 통과해야 한다. 만약 실패하면 dedup 키 구현이 잘못된 것이다.

- [ ] **Step 6: 옛 명령형이 안 깨지는지 실측**

```bash
cd "C:/Users/TheRose/.claude/dashboard"
py dashboard_cli.py add --category "위키창고" --summary "하위호환 스모크" --next "확인용"
```

기대: 에러 없이 종료. 이것이 **지금 돌고 있는 5세션이 쓰는 정확한 명령형**이다. 여기서 실패하면 즉시 `.bak`으로 롤백한다.

확인 후 원상복구:
```bash
py -c "import json,pathlib; p=pathlib.Path('log.json'); d=json.loads(p.read_text(encoding='utf-8')); d=[e for e in d if e.get('summary')!='하위호환 스모크']; p.write_text(json.dumps(d,ensure_ascii=False,indent=2),encoding='utf-8')"
py dashboard_cli.py render
```

---

### Task 2: 대시보드 렌더 — 카테고리 안에 트랙별 서브카드

**Files:**
- Modify: `C:\Users\TheRose\.claude\dashboard\render_dashboard.py`
- Test: `C:\Users\TheRose\.claude\dashboard\test_render_dashboard.py`

**Interfaces:**
- Consumes: Task 1의 `dashboard_common.DEFAULT_TRACK`·`dashboard_common.entry_track`, `log.json` 스키마(`track` 필드)
- Produces:
  - `tracks_for(log, category) -> list[str]` — 해당 카테고리에 기록이 있는 트랙명, 최근 활동 순
  - `_latest_entry(log, category, track) -> dict | None` — **시그니처 변경**(track 인자 추가)
  - `_mini_log(log, category, track, days=7) -> str` — **시그니처 변경**(track 인자 추가)
  - `build_html(config, log) -> str` — 시그니처 유지

- [ ] **Step 1: 수정 전 백업**

```bash
cd "C:/Users/TheRose/.claude/dashboard"
cp render_dashboard.py render_dashboard.py.bak
```

- [ ] **Step 2: 실패하는 테스트 작성**

`test_render_dashboard.py`에 추가:

```python
def test_build_html_renders_each_track_separately():
    config = {"categories": [{"name": "스탁브레인", "path_hints": []}]}
    today = date.today().isoformat()
    log = [
        {"date": today, "category": "스탁브레인", "track": "꾸미기",
         "summary": "꾸미기 요약", "next": "꾸미기 다음"},
        {"date": today, "category": "스탁브레인", "track": "보이스",
         "summary": "보이스 요약", "next": "보이스 다음"},
    ]
    result = build_html(config, log)
    assert "꾸미기 요약" in result
    assert "보이스 요약" in result
    assert "꾸미기 다음" in result
    assert "보이스 다음" in result


def test_build_html_shows_track_names():
    config = {"categories": [{"name": "스탁브레인", "path_hints": []}]}
    today = date.today().isoformat()
    log = [{"date": today, "category": "스탁브레인", "track": "장면라이브러리",
            "summary": "s", "next": "n"}]
    result = build_html(config, log)
    assert "장면라이브러리" in result


def test_build_html_legacy_entry_without_track_still_renders():
    config = {"categories": [{"name": "스탁브레인", "path_hints": []}]}
    today = date.today().isoformat()
    log = [{"date": today, "category": "스탁브레인", "summary": "옛 요약", "next": "옛 다음"}]
    result = build_html(config, log)
    assert "옛 요약" in result


def test_build_html_escapes_track_name():
    config = {"categories": [{"name": "스탁브레인", "path_hints": []}]}
    today = date.today().isoformat()
    log = [{"date": today, "category": "스탁브레인", "track": "<script>",
            "summary": "s", "next": "n"}]
    result = build_html(config, log)
    assert "<script>" not in result
    assert "&lt;script&gt;" in result


def test_build_html_minilog_is_per_track():
    config = {"categories": [{"name": "스탁브레인", "path_hints": []}]}
    today = date.today().isoformat()
    log = [{"date": today, "category": "스탁브레인", "track": "꾸미기",
            "summary": "s", "next": "n"}]
    result = build_html(config, log)
    assert "●" in result
```

- [ ] **Step 3: 테스트 실패 확인**

```bash
cd "C:/Users/TheRose/.claude/dashboard"
py -m pytest test_render_dashboard.py -v
```

기대: `test_build_html_renders_each_track_separately` FAIL — 현재 `_latest_entry`가 카테고리당 1개만 반환하므로 "보이스 요약"이 결과에 없다.

- [ ] **Step 4: 구현**

`render_dashboard.py`를 아래로 교체한다(`build_html`의 CSS/구조는 유지하되 트랙 루프 추가).

```python
"""Pure HTML rendering for the daily project dashboard."""
from datetime import date, timedelta
import html

from dashboard_common import DEFAULT_TRACK, entry_track


def tracks_for(log, category):
    """해당 카테고리에 기록이 있는 트랙명을 최근 활동 순으로 반환."""
    latest = {}
    for e in log:
        if e["category"] != category:
            continue
        t = entry_track(e)
        if t not in latest or e["date"] > latest[t]:
            latest[t] = e["date"]
    return sorted(latest, key=lambda t: (latest[t], t), reverse=True)


def _mini_log(log, category, track, days=7):
    today = date.today()
    marks = []
    for i in range(days - 1, -1, -1):
        d = (today - timedelta(days=i)).isoformat()
        has_entry = any(
            e["date"] == d and e["category"] == category and entry_track(e) == track
            for e in log
        )
        marks.append("●" if has_entry else "○")
    return "".join(marks)


def _latest_entry(log, category, track):
    matches = [
        e for e in log
        if e["category"] == category and entry_track(e) == track
    ]
    if not matches:
        return None
    return sorted(matches, key=lambda e: e["date"])[-1]


def _track_block(log, category, track):
    latest = _latest_entry(log, category, track)
    summary = html.escape(latest["summary"]) if latest else "(기록 없음)"
    next_step = html.escape(latest["next"]) if latest else "(기록 없음)"
    mini = _mini_log(log, category, track)
    label = ""
    if track != DEFAULT_TRACK:
        label = f'<h3 class="track">{html.escape(track)}</h3>'
    return f"""
  <div class="trackblock">
    {label}
    <p class="summary"><strong>현재:</strong> {summary}</p>
    <p class="next"><strong>다음:</strong> {next_step}</p>
    <p class="minilog">{mini}</p>
  </div>"""


def build_html(config, log):
    cards = []
    for cat in config["categories"]:
        name = cat["name"]
        tracks = tracks_for(log, name)
        if not tracks:
            body = _track_block(log, name, DEFAULT_TRACK)
        else:
            body = "".join(_track_block(log, name, t) for t in tracks)
        cards.append(f"""
<section class="card">
  <h2>{html.escape(name)}</h2>{body}
</section>""")
    body = "\n".join(cards)
    return f"""<title>프로젝트 대시보드</title>
<style>
:root {{ color-scheme: light dark; }}
body {{ font-family: system-ui, sans-serif; margin: 0; padding: 2rem; background: #fff; color: #111; }}
@media (prefers-color-scheme: dark) {{ body {{ background: #111; color: #eee; }} }}
:root[data-theme="dark"] body {{ background: #111; color: #eee; }}
:root[data-theme="light"] body {{ background: #fff; color: #111; }}
.grid {{ display: grid; grid-template-columns: repeat(auto-fit, minmax(240px, 1fr)); gap: 1rem; max-width: 100%; }}
.card {{ border: 1px solid #8888; border-radius: 8px; padding: 1rem; overflow-x: auto; }}
.card h2 {{ margin: 0 0 0.5rem; font-size: 1.1rem; }}
.trackblock {{ border-top: 1px dashed #8886; padding-top: 0.6rem; margin-top: 0.6rem; }}
.trackblock:first-of-type {{ border-top: 0; padding-top: 0; margin-top: 0; }}
.track {{ margin: 0 0 0.3rem; font-size: 0.95rem; opacity: 0.85; }}
.minilog {{ font-size: 1.2rem; letter-spacing: 0.2rem; }}
</style>
<div class="grid">
{body}
</div>
"""
```

- [ ] **Step 5: 테스트 통과 확인**

```bash
cd "C:/Users/TheRose/.claude/dashboard"
py -m pytest test_render_dashboard.py test_dashboard_cli.py -v
```

기대: 전부 PASS. 기존 `test_build_html_missing_entry_shows_placeholder`(빈 log → "(기록 없음)")도 통과해야 한다 — `tracks` 비었을 때 `DEFAULT_TRACK` 블록을 렌더하는 분기가 이걸 지킨다.

- [ ] **Step 6: 실제 데이터로 렌더 육안 확인**

```bash
cd "C:/Users/TheRose/.claude/dashboard"
py dashboard_cli.py render
py -c "print(open('dashboard.html',encoding='utf-8').read()[:800])"
```

기대: 실제 `log.json`(옛 엔트리 = track 없음)으로도 예외 없이 렌더되고, 카드 내용이 보인다.

> 합성 픽스처 통과만으로 완료 처리하지 않는다 (memory `feedback_verify_with_real_data`).

---

### Task 3: stop 훅 — 트랙 지정 안내

**Files:**
- Modify: `C:\Users\TheRose\.claude\dashboard\stop_hook.py`
- Test: `C:\Users\TheRose\.claude\dashboard\test_stop_hook.py`

**Interfaces:**
- Consumes: 없음 (독립)
- Produces: `active_tracks(cwd: str) -> list[str]` — `<cwd>/.sessions/*.json`에서 트랙명 수집. 디렉터리 없으면 `[]`.

> `.sessions/`는 페이즈2(`track.py`)에서 생기지만, 훅은 **없으면 조용히 빈 목록**을 반환해 페이즈1에서도 정상 동작해야 한다.

- [ ] **Step 1: 수정 전 백업**

```bash
cd "C:/Users/TheRose/.claude/dashboard"
cp stop_hook.py stop_hook.py.bak
```

- [ ] **Step 2: 실패하는 테스트 작성**

`test_stop_hook.py`에 추가:

```python
import json


def test_active_tracks_empty_when_no_sessions_dir(tmp_path):
    import stop_hook
    assert stop_hook.active_tracks(str(tmp_path)) == []


def test_active_tracks_reads_session_files(tmp_path):
    import stop_hook
    d = tmp_path / ".sessions"
    d.mkdir()
    (d / "꾸미기.json").write_text(
        json.dumps({"track": "꾸미기", "paths": []}, ensure_ascii=False), encoding="utf-8")
    (d / "보이스.json").write_text(
        json.dumps({"track": "보이스", "paths": []}, ensure_ascii=False), encoding="utf-8")
    assert sorted(stop_hook.active_tracks(str(tmp_path))) == sorted(["꾸미기", "보이스"])


def test_active_tracks_skips_corrupt_file(tmp_path):
    import stop_hook
    d = tmp_path / ".sessions"
    d.mkdir()
    (d / "broken.json").write_text("{not json", encoding="utf-8")
    (d / "정상.json").write_text(
        json.dumps({"track": "정상", "paths": []}, ensure_ascii=False), encoding="utf-8")
    assert stop_hook.active_tracks(str(tmp_path)) == ["정상"]
```

- [ ] **Step 3: 테스트 실패 확인**

```bash
cd "C:/Users/TheRose/.claude/dashboard"
py -m pytest test_stop_hook.py -v
```

기대: FAIL — `module 'stop_hook' has no attribute 'active_tracks'`

- [ ] **Step 4: 구현**

`stop_hook.py`의 `match_category` 아래에 추가:

```python
def active_tracks(cwd):
    """<cwd>/.sessions/*.json 에서 트랙명을 모은다.

    .sessions/ 는 페이즈2에서 생긴다. 없으면 빈 목록(페이즈1에서도 정상 동작).
    손상된 파일은 조용히 건너뛴다 — 훅이 죽으면 세션 종료가 막힌다.
    """
    d = Path(cwd) / ".sessions"
    if not d.is_dir():
        return []
    tracks = []
    for f in sorted(d.glob("*.json")):
        try:
            data = json.loads(f.read_text(encoding="utf-8"))
        except (json.JSONDecodeError, OSError):
            continue
        name = data.get("track")
        if name:
            tracks.append(name)
    return tracks
```

`main()`의 `reason` 생성부를 교체:

```python
    tracks = active_tracks(payload.get("cwd", ""))
    if tracks:
        hint = "활성 트랙: " + ", ".join(tracks) + " 중 본인 것"
    else:
        hint = "본인 작업 트랙명(예: 꾸미기, 보이스, 장면라이브러리)"
    reason = (
        f"이 세션은 '{category}' 프로젝트 작업입니다. 종료 전에 다음을 하세요: "
        f'1) `py "{cli_path}" add --category "{category}" --track "<{hint}>" '
        f'--summary "<오늘 한 일 1~3줄>" --next "<다음 할 일>"` 실행 '
        f"{publish_step}."
    )
```

- [ ] **Step 5: 테스트 통과 확인**

```bash
cd "C:/Users/TheRose/.claude/dashboard"
py -m pytest test_stop_hook.py -v
```

기대: 전부 PASS (기존 테스트 포함).

- [ ] **Step 6: 훅 실제 호출 스모크**

```bash
cd "C:/Users/TheRose/.claude/dashboard"
echo '{"cwd":"C:\\Users\\TheRose\\Desktop\\로또의 주식","stop_hook_active":false}' | py stop_hook.py
```

기대: `{"decision": "block", "reason": "... --track ..."}` JSON 출력. **훅이 예외로 죽으면 모든 세션의 종료가 막힌다** — 여기서 실패하면 즉시 `.bak` 롤백.

---

### Task 4: 핸드오프 분리 — `handoff/<트랙>.md`

**Files:**
- Create: `handoff/모션효과.md`, `handoff/장면라이브러리.md`, `handoff/보이스.md`, `handoff/렌즈유사영상.md`, `handoff/대본위키.md`, `handoff/틱톡발굴.md`, `handoff/레퍼런스랭킹.md`, `handoff/VMake자막제거.md`, `handoff/꾸미기.md`
- Create: `handoff/README.md`
- Modify: `NEXT_SESSION.md` (정적 인덱스로 축약)
- Modify: `CLAUDE.md` (저장 트리거를 handoff로)

> ⚠️ **실행 전제**: 이 태스크와 Task 5는 다른 세션이 쓰는 파일(`NEXT_SESSION.md`·`wiki/log.md`·`CLAUDE.md`)을 건드린다. **다른 세션이 전부 닫힌 상태에서만 실행한다.** (2026-07-15 사용자 확인: 마감 후 진행)

**Interfaces:**
- Consumes: 없음
- Produces: `handoff/<트랙>.md` 규약 — 파일명의 트랙명이 곧 소유자. 그 트랙 세션만 자기 파일을 수정한다.

> **NEXT_SESSION.md를 자동생성하지 않는 이유**: 지금 5개 세션이 `NEXT_SESSION.md`를 직접 편집 중이다. 생성기가 덮어쓰면 그 편집이 유실된다 — 이 계획이 없애려는 바로 그 사고다. 대신 **손으로 한 번 쓰는 정적 인덱스**로 만든다. 생성기 없음(YAGNI).

- [ ] **Step 1: 현재 NEXT_SESSION.md 트랙 구조 확인**

```bash
cd "C:/Users/TheRose/Desktop/로또의 주식"
grep -n "^## " NEXT_SESSION.md
```

기대: 트랙별 `## ` 섹션 목록이 나온다. **이 목록이 실제 생성할 파일 목록의 근거다** — 위 Files 목록과 다르면 실제 출력을 따른다(다른 세션이 그새 섹션을 추가했을 수 있다).

- [ ] **Step 2: 섹션별로 handoff 파일 생성**

각 `## ` 섹션의 내용을 그대로(요약·재작성 금지) `handoff/<트랙>.md`로 옮긴다. 파일 첫 줄에 소유자 표기를 넣는다:

```markdown
> **소유 트랙**: 꾸미기 — 이 파일은 꾸미기 트랙 세션만 수정한다. 다른 트랙은 읽기만.
> 원본: NEXT_SESSION.md에서 2026-07-15 분리
```

- [ ] **Step 3: `handoff/README.md` 작성**

```markdown
# handoff — 트랙별 인수인계

한 PC에서 여러 Claude 세션이 같은 워킹트리로 동시에 작업한다.
`NEXT_SESSION.md` 한 장에 전부 적으면 세션끼리 덮어쓴다(2026-07-15 실사고).

## 규칙

1. **자기 트랙 파일만 수정한다.** `handoff/<트랙>.md`
2. 남의 트랙 파일은 **읽기만**. 고칠 게 보이면 그 트랙에 메모를 남기지 말고 사용자에게 보고.
3. 새 트랙은 새 파일을 만든다. 기존 파일에 얹지 않는다.
4. 커밋할 때 `git add handoff/<내트랙>.md` — `git add -A` 금지.

설계: `docs/superpowers/specs/2026-07-15-동시세션-충돌차단-트랙격리-design.md`
```

- [ ] **Step 4: NEXT_SESSION.md를 정적 인덱스로 교체**

```markdown
# NEXT_SESSION — 이어서 할 일

> ⚠️ **내용은 `handoff/<트랙>.md`로 옮겼습니다** (2026-07-15).
> 이 파일은 목록만 유지합니다. **여기에 작업 내용을 쓰지 마세요** — 세션끼리 덮어씁니다.
> 자기 트랙 파일에 쓰세요. 규칙: `handoff/README.md`

| 트랙 | 파일 |
|---|---|
| 모션효과 | `handoff/모션효과.md` |
| 장면 라이브러리 | `handoff/장면라이브러리.md` |
| 보이스 | `handoff/보이스.md` |
| 렌즈 유사영상 발굴 | `handoff/렌즈유사영상.md` |
| 대본 위키 | `handoff/대본위키.md` |
| 틱톡 발굴 | `handoff/틱톡발굴.md` |
| 레퍼런스 랭킹 | `handoff/레퍼런스랭킹.md` |
| VMake 자막제거 | `handoff/VMake자막제거.md` |
| 꾸미기 (완료) | `handoff/꾸미기.md` |
```

> Step 1의 실제 섹션 목록에 맞춰 행을 조정한다.

- [ ] **Step 5: CLAUDE.md 저장 트리거 교체 (이게 없으면 마이그레이션이 하룻밤에 원복된다)**

`CLAUDE.md`의 저장 트리거 블록에 `□ 1. NEXT_SESSION.md 갱신 (덮어쓰기)`가 있다. **이걸 안 고치면 다음에 켜지는 세션들이 CLAUDE.md 지시대로 `NEXT_SESSION.md`에 또 덮어쓴다** — 분리한 게 무의미해진다.

현재 위치 확인:
```bash
cd "C:/Users/TheRose/Desktop/로또의 주식"
grep -n "NEXT_SESSION.md 갱신" CLAUDE.md
```

해당 줄을 아래로 교체한다:

```
□ 1. handoff/{내 트랙}.md 갱신 (내 트랙 파일만! NEXT_SESSION.md는 목록일 뿐 — 거기 쓰면 타세션과 덮어씀)
```

같은 블록의 `□ 2. wiki/log.md 기록 (1~3줄)`도 교체(Task 5와 짝):

```
□ 2. wiki/log.d/{내 트랙}.md 에 1~3줄 추가 (log.md는 동결 아카이브)
```

`CLAUDE.md`는 공유 파일이므로 **이 두 줄 외에는 한 글자도 건드리지 않는다.**

```bash
git diff --stat CLAUDE.md
```
기대: 삽입·삭제가 각 2줄 근처. 그보다 크면 남의 내용을 건드린 것 — 멈춘다.

- [ ] **Step 6: 유실 없는지 검증**

```bash
cd "C:/Users/TheRose/Desktop/로또의 주식"
git show HEAD:NEXT_SESSION.md | wc -l
cat handoff/*.md | wc -l
```

기대: `handoff/*.md` 합계 줄 수가 원본 이상(소유자 헤더가 붙으므로 더 크다). **원본보다 작으면 내용을 흘린 것** — 멈추고 원인을 찾는다.

트랙명 누락 검사:
```bash
git show HEAD:NEXT_SESSION.md | grep -c "^## "
ls handoff/*.md | wc -l
```
기대: `handoff` 파일 수 = 섹션 수 + 1 (README).

- [ ] **Step 7: 커밋**

```bash
cd "C:/Users/TheRose/Desktop/로또의 주식"
git add handoff NEXT_SESSION.md CLAUDE.md
git diff --cached --name-only
```

기대 출력: `handoff/...`, `NEXT_SESSION.md`, `CLAUDE.md`만. **다른 파일이 보이면 남의 것이 섞인 것** — `git restore --staged <경로>`로 빼고 다시 확인.

```bash
git commit -m "refactor(handoff): NEXT_SESSION.md를 트랙별 handoff/로 분리 + CLAUDE.md 저장트리거 교체

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

> 커밋 즉시 `post-commit` 훅이 push한다. 되돌릴 수 없으니 Step 5 검증을 반드시 먼저 통과시킨다.

---

### Task 5: 로그 분리 — `wiki/log.d/<트랙>.md` + 읽기 전용 병합 뷰

**Files:**
- Create: `wiki/log.d/README.md`
- Create: `tools/log_view.py`
- Create: `tools/test_log_view.py`
- Modify: `wiki/log.md` (상단에 안내 배너만 추가)

**Interfaces:**
- Consumes: 없음
- Produces:
  - `tools/log_view.py`: `parse_entries(text: str, track: str) -> list[dict]` (키: `date`, `track`, `line`), `collect(log_dir: Path) -> list[dict]`, `render(entries: list[dict]) -> str`
  - `wiki/log.d/<트랙>.md` 규약: 한 줄에 한 항목, `- YYYY-MM-DD — 내용` 형식

> **`wiki/log.md`를 생성하지 않는 이유**: 1,500줄 넘는 히스토리이고 지금도 다른 세션이 append 중이다. 재생성하면 그 append가 유실된다. `log.md`는 **동결된 아카이브**로 두고, 신규 기록만 `wiki/log.d/`에 쌓는다. 병합 뷰는 **stdout으로만** 출력한다 — 어떤 파일도 쓰지 않으므로 덮임이 원천적으로 불가능하다.

- [ ] **Step 1: 실패하는 테스트 작성**

`tools/test_log_view.py`:

```python
import sys
from pathlib import Path

sys.path.insert(0, str(Path(__file__).resolve().parent))

import log_view


def test_parse_entries_extracts_date_and_line():
    text = "- 2026-07-15 — 꾸미기 완료\n- 2026-07-14 — 카드 작업\n"
    entries = log_view.parse_entries(text, "꾸미기")
    assert len(entries) == 2
    assert entries[0]["date"] == "2026-07-15"
    assert entries[0]["track"] == "꾸미기"
    assert "꾸미기 완료" in entries[0]["line"]


def test_parse_entries_ignores_non_entry_lines():
    text = "# 제목\n\n> 소유 트랙: 꾸미기\n- 2026-07-15 — 진짜 항목\n"
    entries = log_view.parse_entries(text, "꾸미기")
    assert len(entries) == 1
    assert "진짜 항목" in entries[0]["line"]


def test_collect_reads_all_track_files(tmp_path):
    d = tmp_path / "log.d"
    d.mkdir()
    (d / "꾸미기.md").write_text("- 2026-07-15 — 꾸미기 것\n", encoding="utf-8")
    (d / "보이스.md").write_text("- 2026-07-14 — 보이스 것\n", encoding="utf-8")
    entries = log_view.collect(d)
    assert len(entries) == 2
    assert {e["track"] for e in entries} == {"꾸미기", "보이스"}


def test_collect_skips_readme(tmp_path):
    d = tmp_path / "log.d"
    d.mkdir()
    (d / "README.md").write_text("- 2026-07-15 — 이건 규칙문서\n", encoding="utf-8")
    (d / "꾸미기.md").write_text("- 2026-07-15 — 진짜\n", encoding="utf-8")
    entries = log_view.collect(d)
    assert len(entries) == 1
    assert entries[0]["track"] == "꾸미기"


def test_render_sorts_newest_first_and_tags_track():
    entries = [
        {"date": "2026-07-14", "track": "보이스", "line": "- 2026-07-14 — 보이스 것"},
        {"date": "2026-07-15", "track": "꾸미기", "line": "- 2026-07-15 — 꾸미기 것"},
    ]
    out = log_view.render(entries)
    assert out.index("꾸미기 것") < out.index("보이스 것")
    assert "[꾸미기]" in out
```

- [ ] **Step 2: 테스트 실패 확인**

```bash
cd "C:/Users/TheRose/Desktop/로또의 주식"
py -m pytest tools/test_log_view.py -v
```

기대: FAIL — `ModuleNotFoundError: No module named 'log_view'`

- [ ] **Step 3: 구현**

`tools/log_view.py`:

```python
"""wiki/log.d/<트랙>.md 조각을 합쳐 최신순으로 보여준다 (읽기 전용).

어떤 파일도 쓰지 않는다. 파일을 쓰면 동시 세션이 서로 덮어쓴다
— 그게 이 도구가 존재하는 이유다.

사용: py tools/log_view.py [--days 7]
"""
import argparse
import re
import sys
from datetime import date, timedelta
from pathlib import Path

BASE = Path(__file__).resolve().parent.parent
LOG_DIR = BASE / "wiki" / "log.d"

_ENTRY_RE = re.compile(r"^-\s*(\d{4}-\d{2}-\d{2})\s")


def parse_entries(text, track):
    entries = []
    for line in text.splitlines():
        m = _ENTRY_RE.match(line.strip())
        if not m:
            continue
        entries.append({"date": m.group(1), "track": track, "line": line.rstrip()})
    return entries


def collect(log_dir):
    entries = []
    for f in sorted(Path(log_dir).glob("*.md")):
        if f.stem == "README":
            continue
        entries.extend(parse_entries(f.read_text(encoding="utf-8"), f.stem))
    return entries


def render(entries):
    rows = sorted(entries, key=lambda e: e["date"], reverse=True)
    out = []
    for e in rows:
        body = e["line"].lstrip("- ").strip()
        out.append(f"- [{e['track']}] {body}")
    return "\n".join(out)


def main(argv=None):
    parser = argparse.ArgumentParser()
    parser.add_argument("--days", type=int, default=0,
                        help="최근 N일만 (0=전체)")
    args = parser.parse_args(argv)
    entries = collect(LOG_DIR)
    if args.days > 0:
        cutoff = (date.today() - timedelta(days=args.days - 1)).isoformat()
        entries = [e for e in entries if e["date"] >= cutoff]
    print(render(entries))


if __name__ == "__main__":
    main(sys.argv[1:])
```

- [ ] **Step 4: 테스트 통과 확인**

```bash
cd "C:/Users/TheRose/Desktop/로또의 주식"
py -m pytest tools/test_log_view.py -v
```

기대: 5개 전부 PASS.

- [ ] **Step 5: `wiki/log.d/README.md` 작성**

```markdown
# wiki/log.d — 트랙별 작업 로그

`wiki/log.md`는 **동결된 아카이브**입니다(2026-07-15까지). 새 기록은 여기에 씁니다.

## 왜

여러 세션이 `log.md` 맨 위에 동시에 append하면 rebase 충돌이 100% 납니다.
실제로 2026-07-15 stash 복원이 타세션 기록 24줄을 지울 뻔했습니다.

## 규칙

1. 자기 트랙 파일만 수정: `wiki/log.d/<트랙>.md`
2. 한 줄에 한 항목: `- YYYY-MM-DD — 내용`
3. 합쳐 보기: `py tools/log_view.py --days 7` (읽기 전용, 파일 안 씀)
4. `git add wiki/log.d/<내트랙>.md` — `git add -A` 금지

설계: `docs/superpowers/specs/2026-07-15-동시세션-충돌차단-트랙격리-design.md`
```

- [ ] **Step 6: `wiki/log.md` 상단에 배너 추가**

`wiki/log.md` **맨 위**에 아래 3줄을 삽입한다. 기존 내용은 **한 줄도 지우지 않는다**.

```markdown
> ⚠️ **이 파일은 2026-07-15부로 동결된 아카이브입니다.** 새 기록은 `wiki/log.d/<트랙>.md`에 쓰세요.
> 여러 세션이 이 파일 맨 위에 동시에 append하면 충돌·유실이 납니다. 합쳐 보기: `py tools/log_view.py --days 7`

```

- [ ] **Step 7: 실데이터 스모크**

```bash
cd "C:/Users/TheRose/Desktop/로또의 주식"
mkdir -p wiki/log.d
printf -- '- 2026-07-15 — 트랙격리 페이즈1 상태계층 분리 구현\n' > wiki/log.d/트랙격리.md
py tools/log_view.py --days 7
```

기대: `- [트랙격리] 2026-07-15 — 트랙격리 페이즈1 상태계층 분리 구현` 출력. 예외 없음.

- [ ] **Step 8: 커밋**

```bash
cd "C:/Users/TheRose/Desktop/로또의 주식"
git add tools/log_view.py tools/test_log_view.py wiki/log.d wiki/log.md
git diff --cached --name-only
```

기대: 위 4개 경로만. **`wiki/log.md`에 내 배너 2줄만 들어갔는지** 반드시 확인:

```bash
git diff --cached --stat wiki/log.md
```
기대: `2 insertions(+)`, **삭제 0**. 삭제가 있으면 남의 기록을 지운 것이다 — 멈춘다.

```bash
git commit -m "refactor(log): wiki/log.md 동결 + 트랙별 log.d 분리 + 읽기전용 병합뷰

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

## 페이즈1 완료 기준

- [ ] `py -m pytest` — 대시보드 3파일 + `tools/test_log_view.py` 전부 green
- [ ] `--track` 없는 옛 명령형이 여전히 동작 (Task 1 Step 6)
- [ ] 두 트랙이 같은 날 각자 카드를 갖는다 (실제 `log.json`으로 확인)
- [ ] stop 훅이 예외 없이 JSON을 뱉는다 (Task 3 Step 6)
- [ ] `handoff/*.md` 합계 줄 수 ≥ 원본 `NEXT_SESSION.md`
- [ ] `CLAUDE.md` 저장 트리거가 `handoff/{트랙}.md`·`wiki/log.d/{트랙}.md`를 가리킨다 (**없으면 다음 세션들이 옛 파일에 다시 덮어써 마이그레이션이 원복된다**)
- [ ] `wiki/log.md` diff에 삭제 0
- [ ] 실제로 두 세션이 각각 `--track`으로 기록 → 카드 2개 공존 확인 (**합성 테스트만으로 완료 처리 금지**)

## 페이즈2로 넘길 것

- `tools/track.py` (claim/list/release) + `.sessions/`
- `deploy/githooks/commit-msg` + shim 설치 (`tools/install_hooks.py`)
- `tools/safe_commit.py` (락, add~commit 구간)
- 봇 데이터 전용 트랙(`크롤데이터`) 정책

페이즈2 착수 전 **사용자가 전 세션에 공지**해야 한다(훅 거부 모드 전환은 돌고 있는 세션을 깨뜨린다).
