# 독립워커 작업큐 구현 계획

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 긴 작업(믹스 5종·해외HOT 수집)을 FastAPI 서버 프로세스 밖의 별도 워커로 빼서, 배포 재시작(`systemctl restart`)에 죽지 않게 한다.

**Architecture:** SQLite에 `job_queue` 테이블을 두고, 서버는 `enqueue`만 한다. 별도 systemd 서비스로 뜨는 `worker.py`가 큐를 폴링해 **한 번에 하나씩** 실행하며 30초마다 heartbeat를 찍는다. 워커가 죽으면 heartbeat가 멈추고, 다음 워커가 그 작업을 `failed`로 정리해 화면에 "실패 — 다시 시도"를 띄운다.

**Tech Stack:** Python 3.12 / SQLite 3.45(서버 실측, `RETURNING` 지원 확인) / systemd / FastAPI / pytest

**설계문서:** `docs/superpowers/specs/2026-07-29-독립워커-작업큐-design.md`

## Global Constraints

- **작업 폴더는 `.tracks/해외HOT` 하나뿐이다.** main 폴더(`로또의 주식/shopping_shorts/...`)는 절대 수정 금지 — 흡수 사고의 원인이다.
- 테스트는 `shopping_shorts/tests/` 아래에 둔다(`tests/` 아님).
- `task` 값은 `'mix'|'retype'|'render'|'preview'|'clean'|'overseas'` 여섯 개뿐이다.
- `state` 값은 `'queued'|'running'|'done'|'failed'` 네 개뿐이다.
- **자동 재시도를 넣지 마라.** 실패는 화면에 표시하고 사장님이 다시 누른다(무한루프 사고 전례).
- 나머지 17개 짧은 백그라운드 작업은 건드리지 않는다.
- `Store._conn()`은 `sqlite3.connect(self.db_path)`를 반환하고 `with` 블록이 커밋한다 — 기존 패턴을 따른다.
- 전체 pytest 스위트는 기준선 실패 13건이 이미 있다. 게이트 기준은 "**실패가 늘지 않았나**"다.
- **P3(Task 4)부터 라이브 동작이 실제로 바뀐다.** 그 전 태스크는 아무도 안 쓰는 코드라 안전하다.

---

### Task 1: 큐 테이블 + enqueue/claim_next

**Files:**
- Modify: `shopping_shorts/store.py` (테이블 생성부에 CREATE TABLE + 메서드 2개)
- Test: `shopping_shorts/tests/test_job_queue.py` (신규)

**Interfaces:**
- Consumes: 없음
- Produces:
  - `Store.enqueue(task: str, args: dict) -> int` — 큐 id 반환
  - `Store.claim_next() -> dict | None` — `{'id': int, 'task': str, 'args': dict}` 또는 `None`

- [ ] **Step 1: 실패하는 테스트를 쓴다**

`shopping_shorts/tests/test_job_queue.py` 신규 생성:

```python
"""독립워커 작업큐(2026-07-29) — 배포 재시작에 안 죽는 실행기의 대기열."""
from shopping_shorts.store import Store


def test_enqueue_returns_id_and_claim_returns_it(tmp_path):
    st = Store(str(tmp_path / "t.db"))
    qid = st.enqueue("mix", {"job_id": "j1"})
    assert isinstance(qid, int) and qid > 0
    got = st.claim_next()
    assert got["id"] == qid
    assert got["task"] == "mix"
    assert got["args"] == {"job_id": "j1"}


def test_claim_next_returns_none_when_empty(tmp_path):
    st = Store(str(tmp_path / "t.db"))
    assert st.claim_next() is None


def test_claim_next_takes_oldest_first(tmp_path):
    st = Store(str(tmp_path / "t.db"))
    st.enqueue("mix", {"job_id": "first"})
    st.enqueue("render", {"job_id": "second"})
    assert st.claim_next()["args"]["job_id"] == "first"
    assert st.claim_next()["args"]["job_id"] == "second"


def test_claim_next_never_hands_same_job_twice(tmp_path):
    """워커가 둘이어도 같은 일을 두 번 집으면 안 된다 — claim은 원자적이어야 한다."""
    st = Store(str(tmp_path / "t.db"))
    st.enqueue("mix", {"job_id": "only"})
    first = st.claim_next()
    second = st.claim_next()
    assert first is not None
    assert second is None
```

- [ ] **Step 2: 실패를 확인한다**

Run: `python -m pytest shopping_shorts/tests/test_job_queue.py -v`
Expected: FAIL — `AttributeError: 'Store' object has no attribute 'enqueue'`

- [ ] **Step 3: 최소 구현을 넣는다**

`store.py`의 테이블 생성 블록(다른 `CREATE TABLE IF NOT EXISTS`들이 있는 곳) 끝에 추가:

```python
            # 독립워커 작업큐(2026-07-29) — 긴 작업(믹스·해외HOT 수집)을 서버 프로세스 밖으로
            # 뺀다. 예전엔 BackgroundTasks라 배포의 systemctl restart가 진행 중 작업을
            # SIGKILL했다(2026-07-29 실측: 하루에 수집 1건·믹스 1건 사망).
            c.execute("""
                CREATE TABLE IF NOT EXISTS job_queue (
                    id           INTEGER PRIMARY KEY AUTOINCREMENT,
                    task         TEXT NOT NULL,
                    args_json    TEXT NOT NULL,
                    state        TEXT NOT NULL,
                    error        TEXT,
                    created_at   TEXT NOT NULL,
                    claimed_at   TEXT,
                    heartbeat_at TEXT,
                    finished_at  TEXT
                )
            """)
            c.execute("CREATE INDEX IF NOT EXISTS idx_job_queue_state "
                      "ON job_queue(state, id)")
```

메서드는 `store.py` 안 다른 메서드들 곁에 추가(파일 끝 근처, 기존 스타일에 맞춰):

```python
    def enqueue(self, task, args):
        """작업을 대기열에 넣고 큐 id를 반환한다."""
        with self._conn() as c:
            cur = c.execute(
                "INSERT INTO job_queue(task, args_json, state, created_at) "
                "VALUES(?,?, 'queued', datetime('now'))",
                (task, json.dumps(args, ensure_ascii=False)))
            return cur.lastrowid

    def claim_next(self):
        """가장 오래된 대기 작업 하나를 원자적으로 'running'으로 바꾸고 돌려준다.

        UPDATE ... RETURNING 한 문장이라 워커가 여러 개여도 같은 작업을 두 번 집지 않는다
        (SQLite 3.35+ 필요, 서버 3.45 실측 확인). 없으면 None."""
        with self._conn() as c:
            row = c.execute(
                "UPDATE job_queue SET state='running', "
                "       claimed_at=datetime('now'), heartbeat_at=datetime('now') "
                " WHERE id = (SELECT id FROM job_queue WHERE state='queued' "
                "             ORDER BY id LIMIT 1) "
                "RETURNING id, task, args_json").fetchone()
            if not row:
                return None
            return {"id": row[0], "task": row[1], "args": json.loads(row[2])}
```

> `json`은 `store.py`에 이미 import돼 있는지 확인하고, 없으면 파일 상단에 추가한다.

- [ ] **Step 4: 통과를 확인한다**

Run: `python -m pytest shopping_shorts/tests/test_job_queue.py -v`
Expected: 4 passed

- [ ] **Step 5: 커밋**

```bash
git add shopping_shorts/store.py shopping_shorts/tests/test_job_queue.py
git commit -m "feat(워커): job_queue 테이블 + enqueue/claim_next(원자적)"
```

---

### Task 2: heartbeat / finish / reap_stale / queue_status

**Files:**
- Modify: `shopping_shorts/store.py` (메서드 4개 추가)
- Test: `shopping_shorts/tests/test_job_queue.py` (기존 파일에 추가)

**Interfaces:**
- Consumes: `Store.enqueue`, `Store.claim_next` (Task 1)
- Produces:
  - `Store.heartbeat(qid: int) -> None`
  - `Store.finish(qid: int, ok: bool, error: str | None = None) -> None`
  - `Store.reap_stale(minutes: int = 2) -> int` — 정리한 개수 반환
  - `Store.queue_status(task: str, args_match: dict | None = None) -> dict | None` — `{'id','state','position','error'}` 또는 `None`

- [ ] **Step 1: 실패하는 테스트를 쓴다**

`shopping_shorts/tests/test_job_queue.py` 끝에 추가:

```python
def test_finish_ok_marks_done(tmp_path):
    st = Store(str(tmp_path / "t.db"))
    qid = st.enqueue("mix", {"job_id": "j1"})
    st.claim_next()
    st.finish(qid, True)
    assert st.queue_status("mix", {"job_id": "j1"})["state"] == "done"


def test_finish_fail_keeps_error(tmp_path):
    st = Store(str(tmp_path / "t.db"))
    qid = st.enqueue("mix", {"job_id": "j1"})
    st.claim_next()
    st.finish(qid, False, "터졌음")
    got = st.queue_status("mix", {"job_id": "j1"})
    assert got["state"] == "failed"
    assert got["error"] == "터졌음"


def test_reap_stale_fails_only_dead_running(tmp_path):
    """heartbeat가 오래 멈춘 running만 failed로. 방금 뛴 건 안 건드린다."""
    import sqlite3
    st = Store(str(tmp_path / "t.db"))
    dead = st.enqueue("mix", {"job_id": "dead"})
    alive = st.enqueue("mix", {"job_id": "alive"})
    st.claim_next(); st.claim_next()
    con = sqlite3.connect(str(tmp_path / "t.db"))
    con.execute("UPDATE job_queue SET heartbeat_at=datetime('now','-10 minutes') WHERE id=?", (dead,))
    con.commit(); con.close()

    n = st.reap_stale(minutes=2)
    assert n == 1
    assert st.queue_status("mix", {"job_id": "dead"})["state"] == "failed"
    assert st.queue_status("mix", {"job_id": "alive"})["state"] == "running"


def test_heartbeat_keeps_job_alive(tmp_path):
    import sqlite3
    st = Store(str(tmp_path / "t.db"))
    qid = st.enqueue("mix", {"job_id": "j1"})
    st.claim_next()
    con = sqlite3.connect(str(tmp_path / "t.db"))
    con.execute("UPDATE job_queue SET heartbeat_at=datetime('now','-10 minutes') WHERE id=?", (qid,))
    con.commit(); con.close()

    st.heartbeat(qid)                 # 다시 뛴다
    assert st.reap_stale(minutes=2) == 0


def test_queue_status_position_counts_ahead(tmp_path):
    """내 앞에 대기·진행 중인 게 몇 개인지 — 화면 '앞에 N개 대기 중'용."""
    st = Store(str(tmp_path / "t.db"))
    st.enqueue("mix", {"job_id": "a"})
    st.enqueue("mix", {"job_id": "b"})
    st.enqueue("mix", {"job_id": "c"})
    assert st.queue_status("mix", {"job_id": "a"})["position"] == 0
    assert st.queue_status("mix", {"job_id": "c"})["position"] == 2


def test_queue_status_none_when_absent(tmp_path):
    st = Store(str(tmp_path / "t.db"))
    assert st.queue_status("mix", {"job_id": "없음"}) is None
```

- [ ] **Step 2: 실패를 확인한다**

Run: `python -m pytest shopping_shorts/tests/test_job_queue.py -v -k "finish or reap or heartbeat or position or absent"`
Expected: FAIL — `AttributeError: 'Store' object has no attribute 'finish'`

- [ ] **Step 3: 최소 구현을 넣는다**

`store.py`의 `claim_next` 바로 뒤에 추가:

```python
    def heartbeat(self, qid):
        """워커가 살아있다는 신호. 30초마다 찍는다 — 멈추면 reap_stale이 죽은 걸로 본다."""
        with self._conn() as c:
            c.execute("UPDATE job_queue SET heartbeat_at=datetime('now') WHERE id=?", (qid,))

    def finish(self, qid, ok, error=None):
        """작업 종료 기록. ok=False면 error를 남겨 화면이 '실패 — 다시 시도'를 띄운다."""
        with self._conn() as c:
            c.execute(
                "UPDATE job_queue SET state=?, error=?, finished_at=datetime('now') WHERE id=?",
                ("done" if ok else "failed", None if ok else (error or "알 수 없는 오류"), qid))

    def reap_stale(self, minutes=2):
        """heartbeat가 minutes분 넘게 안 뛴 running을 failed로 정리하고 개수를 반환한다.

        워커가 SIGKILL로 죽으면 heartbeat가 멈춘다 — 그걸 잡아 화면에 실패를 알린다.
        (예전엔 조용히 멈춰 '되고 있나?'를 알 수 없었다, 2026-07-29 실사고)"""
        with self._conn() as c:
            cur = c.execute(
                "UPDATE job_queue SET state='failed', error='워커가 중단됐습니다', "
                "       finished_at=datetime('now') "
                " WHERE state='running' "
                "   AND (heartbeat_at IS NULL "
                "        OR datetime(heartbeat_at) < datetime('now', ?))",
                (f"-{int(minutes)} minutes",))
            return cur.rowcount

    def queue_status(self, task, args_match=None):
        """이 작업의 큐 상태. position=내 앞에 있는 queued/running 개수(화면 대기순번).

        같은 task+args가 여러 번 큐에 들어갔으면 가장 최근 것을 본다."""
        with self._conn() as c:
            if args_match is None:
                row = c.execute(
                    "SELECT id, state, error FROM job_queue WHERE task=? "
                    "ORDER BY id DESC LIMIT 1", (task,)).fetchone()
            else:
                row = c.execute(
                    "SELECT id, state, error FROM job_queue "
                    " WHERE task=? AND args_json=? ORDER BY id DESC LIMIT 1",
                    (task, json.dumps(args_match, ensure_ascii=False))).fetchone()
            if not row:
                return None
            qid, state, error = row
            ahead = c.execute(
                "SELECT COUNT(*) FROM job_queue "
                " WHERE id < ? AND state IN ('queued','running')", (qid,)).fetchone()[0]
            return {"id": qid, "state": state, "position": ahead, "error": error}
```

- [ ] **Step 4: 통과를 확인한다**

Run: `python -m pytest shopping_shorts/tests/test_job_queue.py -v`
Expected: 10 passed (Task 1의 4건 + 신규 6건)

- [ ] **Step 5: 커밋**

```bash
git add shopping_shorts/store.py shopping_shorts/tests/test_job_queue.py
git commit -m "feat(워커): heartbeat/finish/reap_stale/queue_status — 죽으면 화면에 실패 표시"
```

---

### Task 3: 워커 프로세스 + systemd 유닛

**Files:**
- Create: `shopping_shorts/worker.py`
- Create: `deploy/shopping-shorts-worker.service`
- Test: `shopping_shorts/tests/test_worker_loop.py` (신규)

**Interfaces:**
- Consumes: `Store.claim_next`, `Store.heartbeat`, `Store.finish`, `Store.reap_stale` (Task 1·2)
- Produces:
  - `worker.TASKS` — `dict[str, Callable[[dict], None]]`
  - `worker.run_one(store) -> bool` — 작업 하나 처리했으면 `True`, 큐가 비었으면 `False`

- [ ] **Step 1: 실패하는 테스트를 쓴다**

`shopping_shorts/tests/test_worker_loop.py` 신규 생성:

```python
"""워커 루프(2026-07-29) — 한 번에 하나씩, 실패해도 워커는 안 죽는다."""
from shopping_shorts import worker
from shopping_shorts.store import Store


def test_run_one_returns_false_when_queue_empty(tmp_path):
    st = Store(str(tmp_path / "t.db"))
    assert worker.run_one(st) is False


def test_run_one_executes_task_and_marks_done(tmp_path, monkeypatch):
    st = Store(str(tmp_path / "t.db"))
    called = []
    monkeypatch.setitem(worker.TASKS, "mix", lambda a: called.append(a["job_id"]))
    st.enqueue("mix", {"job_id": "j1"})

    assert worker.run_one(st) is True
    assert called == ["j1"]
    assert st.queue_status("mix", {"job_id": "j1"})["state"] == "done"


def test_worker_survives_task_exception(tmp_path, monkeypatch):
    """작업이 터져도 워커는 죽지 않고 failed로 기록만 남긴다."""
    st = Store(str(tmp_path / "t.db"))
    def boom(a):
        raise RuntimeError("펑")
    monkeypatch.setitem(worker.TASKS, "mix", boom)
    st.enqueue("mix", {"job_id": "j1"})

    assert worker.run_one(st) is True          # 예외가 밖으로 안 샌다
    got = st.queue_status("mix", {"job_id": "j1"})
    assert got["state"] == "failed"
    assert "펑" in got["error"]


def test_unknown_task_fails_gracefully(tmp_path):
    st = Store(str(tmp_path / "t.db"))
    st.enqueue("없는작업", {})
    assert worker.run_one(st) is True
    assert st.queue_status("없는작업")["state"] == "failed"


def test_run_one_processes_one_at_a_time(tmp_path, monkeypatch):
    """1GB 서버라 동시 실행은 금물 — 한 번 호출에 딱 하나만 처리한다."""
    st = Store(str(tmp_path / "t.db"))
    done = []
    monkeypatch.setitem(worker.TASKS, "mix", lambda a: done.append(a["job_id"]))
    st.enqueue("mix", {"job_id": "a"})
    st.enqueue("mix", {"job_id": "b"})

    worker.run_one(st)
    assert done == ["a"]
```

- [ ] **Step 2: 실패를 확인한다**

Run: `python -m pytest shopping_shorts/tests/test_worker_loop.py -v`
Expected: FAIL — `ModuleNotFoundError: No module named 'shopping_shorts.worker'`

- [ ] **Step 3: 워커를 만든다**

`shopping_shorts/worker.py` 신규 생성:

```python
"""독립워커 — 긴 작업을 FastAPI 서버 프로세스 **밖에서** 실행한다(2026-07-29).

왜 있나: 예전엔 믹스·해외HOT 수집이 서버 안 BackgroundTasks로 돌아서, 배포의
`systemctl restart shopping-shorts`가 진행 중 작업을 SIGKILL했다. 화면은 실패도 못 띄우고
조용히 멈췄다(2026-07-29 실사고, 하루 3건). 이제 서버는 큐에 넣기만 하고 여기서 실행한다.

1GB 서버라 **한 번에 하나씩만** 돈다. 서버를 키우면 이 유닛을 복제해 띄우면 된다
(claim_next가 원자적이라 같은 작업을 두 번 집지 않는다).
"""
import json
import logging
import threading
import time

from shopping_shorts import mix_pipeline, overseas_hot_jobs
from shopping_shorts.config import DB_PATH
from shopping_shorts.store import Store

log = logging.getLogger("worker")

_MIX_WORK_DIR = DB_PATH.parent / "mix_jobs"

POLL_SEC = 5           # 큐가 비었을 때 다시 볼 때까지
HEARTBEAT_SEC = 30     # 살아있다는 신호 주기(reap_stale 2분보다 충분히 짧게)

TASKS = {
    "mix":      lambda a: mix_pipeline.run_mix_job(a["job_id"], DB_PATH, _MIX_WORK_DIR),
    "retype":   lambda a: mix_pipeline.retype_mix_job(a["job_id"], a["video_type"],
                                                      DB_PATH, _MIX_WORK_DIR),
    "render":   lambda a: mix_pipeline.run_render(a["job_id"], DB_PATH, _MIX_WORK_DIR),
    "preview":  lambda a: mix_pipeline.run_preview(a["job_id"], DB_PATH, _MIX_WORK_DIR),
    "clean":    lambda a: mix_pipeline.run_clean_sources(a["job_id"], DB_PATH, _MIX_WORK_DIR),
    "overseas": lambda a: overseas_hot_jobs._run(),
}


def run_one(store):
    """대기 작업 하나를 실행한다. 처리했으면 True, 큐가 비었으면 False.

    작업이 터져도 예외를 밖으로 내보내지 않는다 — 워커가 죽으면 큐 전체가 멈춘다."""
    job = store.claim_next()
    if not job:
        return False
    qid, task, args = job["id"], job["task"], job["args"]
    stop = threading.Event()

    def _beat():
        while not stop.wait(HEARTBEAT_SEC):
            try:
                store.heartbeat(qid)
            except Exception:
                log.exception("heartbeat 실패 qid=%s", qid)

    beater = threading.Thread(target=_beat, daemon=True)
    beater.start()
    try:
        fn = TASKS.get(task)
        if fn is None:
            store.finish(qid, False, f"모르는 작업: {task}")
            return True
        log.info("작업 시작 qid=%s task=%s args=%s", qid, task, json.dumps(args, ensure_ascii=False))
        fn(args)
        store.finish(qid, True)
        log.info("작업 완료 qid=%s task=%s", qid, task)
    except Exception as e:
        log.exception("작업 실패 qid=%s task=%s", qid, task)
        store.finish(qid, False, repr(e))
    finally:
        stop.set()
    return True


def main():
    logging.basicConfig(level=logging.INFO,
                        format="%(asctime)s %(levelname)s %(name)s %(message)s")
    store = Store(DB_PATH)
    log.info("워커 시작 — db=%s", DB_PATH)
    while True:
        try:
            store.reap_stale()
            if not run_one(store):
                time.sleep(POLL_SEC)
        except Exception:
            log.exception("워커 루프 오류 — 계속 돈다")
            time.sleep(POLL_SEC)


if __name__ == "__main__":
    main()
```

- [ ] **Step 4: systemd 유닛 파일을 만든다**

`deploy/shopping-shorts-worker.service` 신규 생성:

```ini
[Unit]
Description=Shopping Shorts Job Worker (독립워커 — 배포 재시작에 안 죽는 긴 작업 실행기)
After=network.target

[Service]
Type=simple
User=ubuntu
WorkingDirectory=/home/ubuntu/lotto-stock-wiki
EnvironmentFile=/etc/shopping-shorts.env
ExecStart=/usr/bin/python3 -m shopping_shorts.worker
Restart=always
RestartSec=5
# 재시작 시 진행 중 작업을 최대 30분 기다린다(믹스 렌더가 길다)
TimeoutStopSec=1800
StandardOutput=journal
StandardError=journal

[Install]
WantedBy=multi-user.target
```

- [ ] **Step 5: 통과를 확인한다**

Run: `python -m pytest shopping_shorts/tests/test_worker_loop.py -v`
Expected: 5 passed

- [ ] **Step 6: 커밋**

```bash
git add shopping_shorts/worker.py deploy/shopping-shorts-worker.service shopping_shorts/tests/test_worker_loop.py
git commit -m "feat(워커): worker.py + systemd 유닛 — 한 번에 하나씩, 실패해도 안 죽는다"
```

---

### Task 4: 서버를 enqueue로 전환 (라이브 동작 변경 지점)

**Files:**
- Modify: `shopping_shorts/app.py` (5곳: `:2146`, `:2469`, `:2567`, `:2614`, `:2641`)
- Modify: `shopping_shorts/overseas_hot_jobs.py` (`start()`)
- Test: `shopping_shorts/tests/test_enqueue_wiring.py` (신규)

**Interfaces:**
- Consumes: `Store.enqueue`, `Store.queue_status` (Task 1·2)
- Produces: 없음 (기존 API 응답 계약 유지)

- [ ] **Step 1: 실패하는 테스트를 쓴다**

`shopping_shorts/tests/test_enqueue_wiring.py` 신규 생성:

```python
"""서버가 직접 실행하지 않고 큐에 넣는지(2026-07-29 독립워커 전환)."""
import shopping_shorts.overseas_hot_jobs as job
from shopping_shorts.store import Store


def test_overseas_start_enqueues_instead_of_thread(monkeypatch, tmp_path):
    db = str(tmp_path / "t.db")
    monkeypatch.setattr(job, "DB_PATH", db)
    job._JOB.update(status="idle", phase="", count=0, error=None, started=0.0)
    started = []
    monkeypatch.setattr(job.threading, "Thread",
                        lambda *a, **k: (_ for _ in ()).throw(
                            AssertionError("스레드를 띄우면 안 된다 — 큐에 넣어야 한다")))

    res = job.start()
    assert res["status"] == "running"
    st = Store(db)
    q = st.queue_status("overseas")
    assert q is not None and q["state"] == "queued"


def test_overseas_start_does_not_double_enqueue(monkeypatch, tmp_path):
    """이미 큐에 있으면 또 넣지 않는다 — 버튼 두 번 눌러도 한 번만 돈다."""
    db = str(tmp_path / "t.db")
    monkeypatch.setattr(job, "DB_PATH", db)
    job._JOB.update(status="idle", phase="", count=0, error=None, started=0.0)
    job.start()
    job._JOB.update(status="idle", started=0.0)   # 메모리 상태를 지워도
    job.start()                                    # 큐를 보고 막아야 한다

    st = Store(db)
    with st._conn() as c:
        n = c.execute("SELECT COUNT(*) FROM job_queue WHERE task='overseas'").fetchone()[0]
    assert n == 1
```

- [ ] **Step 2: 실패를 확인한다**

Run: `python -m pytest shopping_shorts/tests/test_enqueue_wiring.py -v`
Expected: FAIL — `AssertionError: 스레드를 띄우면 안 된다` (현재는 Thread를 띄운다)

- [ ] **Step 3: 해외HOT을 큐로 바꾼다**

`overseas_hot_jobs.py`의 `start()`를 교체:

```python
def start():
    """수집을 큐에 넣는다. 실제 실행은 독립워커가 한다(2026-07-29).

    예전엔 여기서 threading.Thread를 띄웠는데, 그 스레드가 FastAPI 서버 프로세스 안에
    있어서 배포의 systemctl restart에 SIGKILL됐다(실측: 16:03 시작 → 16:09 배포에 사망).
    이제 서버가 죽어도 워커가 계속 돈다."""
    store = Store(DB_PATH)
    q = store.queue_status("overseas")
    if q and q["state"] in ("queued", "running"):
        with _LOCK:
            elapsed = int(time.time() - _JOB["started"]) if _JOB["started"] else 0
        return {"status": "running", "elapsed": elapsed}
    with _LOCK:
        _JOB.update(status="running", phase="대기 중", count=0, error=None, started=time.time())
    store.enqueue("overseas", {})
    return {"status": "running", "elapsed": 0}
```

> `Store`·`DB_PATH`는 이 파일 상단에서 이미 import돼 있다. `threading` import는 `_LOCK`이
> 계속 쓰므로 남긴다.

- [ ] **Step 4: 믹스 5곳을 큐로 바꾼다**

`app.py`에서 아래 5줄을 각각 교체한다. **`job_id` 변수명은 각 함수의 것을 그대로 쓴다.**

```python
# app.py:2146
- background_tasks.add_task(run_mix_job, job_id, DB_PATH, _MIX_WORK_DIR)
+ Store(DB_PATH).enqueue("mix", {"job_id": job_id})

# app.py:2469
- background_tasks.add_task(retype_mix_job, job_id, video_type, DB_PATH, _MIX_WORK_DIR)
+ Store(DB_PATH).enqueue("retype", {"job_id": job_id, "video_type": video_type})

# app.py:2567
- background_tasks.add_task(run_render, job_id, DB_PATH, _MIX_WORK_DIR)
+ Store(DB_PATH).enqueue("render", {"job_id": job_id})

# app.py:2614
- background_tasks.add_task(run_preview, job_id, DB_PATH, _MIX_WORK_DIR)
+ Store(DB_PATH).enqueue("preview", {"job_id": job_id})

# app.py:2641
- background_tasks.add_task(run_clean_sources, job_id, DB_PATH, _MIX_WORK_DIR)
+ Store(DB_PATH).enqueue("clean", {"job_id": job_id})
```

⚠️ 각 엔드포인트의 `background_tasks: BackgroundTasks` 인자는 **다른 add_task가 남아 있으면
지우지 말 것.** 해당 함수에서 add_task가 완전히 사라진 경우에만 인자를 정리한다(안 지워도
동작에는 문제없다 — 지울 거면 호출부 서명 확인 필수).

- [ ] **Step 5: 통과를 확인한다**

Run:
```bash
python -m pytest shopping_shorts/tests/test_enqueue_wiring.py shopping_shorts/tests/test_job_queue.py shopping_shorts/tests/test_worker_loop.py shopping_shorts/tests/test_overseas_hot_jobs.py shopping_shorts/tests/test_overseas_api.py -v
python -c "import shopping_shorts.app"
```
Expected: 전부 pass, import 오류 없음

- [ ] **Step 6: 커밋**

```bash
git add shopping_shorts/app.py shopping_shorts/overseas_hot_jobs.py shopping_shorts/tests/test_enqueue_wiring.py
git commit -m "feat(워커): 믹스5종·해외HOT수집을 큐로 전환 — 서버는 넣기만"
```

---

### Task 5: 화면에 대기순번·실패 표시

**Files:**
- Modify: `shopping_shorts/app.py` (`/api/overseas/status`, 믹스 상태 엔드포인트)
- Modify: `shopping_shorts/static/index.html` (해외HOT 진행 표시)
- Test: `shopping_shorts/tests/test_queue_api.py` (신규)

**Interfaces:**
- Consumes: `Store.queue_status` (Task 2)
- Produces: 상태 API 응답에 `queue` 필드 — `{'state': str, 'position': int, 'error': str|None}`

- [ ] **Step 1: 실패하는 테스트를 쓴다**

`shopping_shorts/tests/test_queue_api.py` 신규 생성:

```python
"""상태 API가 큐 정보를 실어주는지(2026-07-29) — 화면 '앞에 N개 대기 중'의 근거."""
import shopping_shorts.overseas_hot_jobs as job
from shopping_shorts.store import Store


def test_overseas_status_includes_queue(monkeypatch, tmp_path):
    db = str(tmp_path / "t.db")
    monkeypatch.setattr(job, "DB_PATH", db)
    job._JOB.update(status="idle", phase="", count=0, error=None, started=0.0)
    Store(db).enqueue("overseas", {})

    s = job.status()
    assert s["queue"]["state"] == "queued"
    assert s["queue"]["position"] == 0


def test_overseas_status_queue_none_when_never_queued(monkeypatch, tmp_path):
    db = str(tmp_path / "t.db")
    monkeypatch.setattr(job, "DB_PATH", db)
    job._JOB.update(status="idle", phase="", count=0, error=None, started=0.0)
    assert job.status()["queue"] is None
```

- [ ] **Step 2: 실패를 확인한다**

Run: `python -m pytest shopping_shorts/tests/test_queue_api.py -v`
Expected: FAIL — `KeyError: 'queue'`

- [ ] **Step 3: 상태에 큐를 싣는다**

`overseas_hot_jobs.py`의 `status()`를 수정 — 기존 반환 dict에 `queue`를 추가한다:

```python
def status():
    with _LOCK:
        base = {"status": _JOB["status"], "phase": _JOB["phase"], "count": _JOB["count"],
                "error": _JOB["error"],
                "elapsed": int(time.time() - _JOB["started"]) if _JOB["started"] else 0}
    # 큐 상태를 같이 준다 — 화면이 '앞에 N개 대기 중'·'실패, 다시 시도'를 띄우는 근거.
    # 워커가 죽으면 reap_stale이 failed로 바꾸므로 여기서 그대로 드러난다.
    base["queue"] = Store(DB_PATH).queue_status("overseas")
    return base
```

> 기존 `status()`의 반환 키를 **바꾸지 말 것** — 프론트가 `phase`·`count`를 쓴다.
> 실제 파일의 기존 키 구성을 먼저 읽고 그대로 유지한 채 `queue`만 더한다.

- [ ] **Step 4: 화면에 표시한다**

`static/index.html`의 해외HOT 진행 표시(`overseasStatus`를 갱신하는 곳, `_startOverseasPoll`
부근)에서 응답의 `queue`를 읽어 문구를 만든다:

```javascript
      // 큐 상태 우선 표시(2026-07-29 독립워커) — 대기 중이면 순번, 죽었으면 실패를 알린다.
      // 예전엔 워커가 죽어도 화면이 조용히 멈춰 '되고 있나?'를 알 수 없었다.
      const q = d.queue;
      if(q && q.state === 'queued'){
        el.textContent = q.position > 0 ? `앞에 ${q.position}개 대기 중` : '곧 시작합니다';
      } else if(q && q.state === 'failed'){
        el.textContent = `실패했습니다 — 다시 눌러주세요 (${q.error||''})`;
      } else {
        el.textContent = `${d.phase||''} ${d.count||0}건 (${d.elapsed||0}s)`;
      }
```

> 위 코드의 `el`·`d`는 그 자리에서 이미 쓰이는 변수명에 맞춘다. 기존 표시 로직을 지우지
> 말고 **else 가지로 남긴다**(수집이 실제로 도는 동안은 지금 문구가 맞다).

- [ ] **Step 5: 통과를 확인한다**

Run:
```bash
python -m pytest shopping_shorts/tests/test_queue_api.py shopping_shorts/tests/test_overseas_api.py -v
```
그리고 JS 문법 검사:
```bash
python -c "
import re
h=open('shopping_shorts/static/index.html',encoding='utf-8').read()
m=re.findall(r'<script>(.*?)</script>', h, re.S)
open('_idx_check.js','w',encoding='utf-8').write(max(m,key=len))
" && node --check _idx_check.js && rm -f _idx_check.js
```
Expected: 테스트 pass, `node --check` 오류 없음

- [ ] **Step 6: 커밋**

```bash
git add shopping_shorts/overseas_hot_jobs.py shopping_shorts/static/index.html shopping_shorts/tests/test_queue_api.py
git commit -m "feat(워커): 화면에 대기순번·실패 표시 — 조용한 멈춤 제거"
```

---

### Task 6: 배포 가드를 큐 기준으로 교체

**Files:**
- Modify: `deploy/auto_deploy.sh` (렌더 체크 블록 + 재시작 부분)

**Interfaces:**
- Consumes: `job_queue` 테이블 (Task 1)
- Produces: 없음

- [ ] **Step 1: 가드 판정을 큐 기준으로 바꾼다**

`auto_deploy.sh`의 파이썬 렌더 체크(`SELECT COUNT(*) FROM mix_jobs ...`)를 교체한다:

```python
    n = con.execute(
        # [2026-07-29 독립워커] 예전엔 mix_jobs의 rendering 상태만 봐서, 해외HOT 수집(메모리에만
        # 있음)과 믹스 매칭 단계가 그물에 안 걸려 배포가 그걸 죽였다(실사고 3건).
        # 이제 긴 작업은 전부 job_queue를 거치므로 여기 하나만 보면 된다.
        # heartbeat가 2분 넘게 안 뛰면 죽은 워커다 — 좀비가 배포를 영영 막지 않는다.
        "SELECT COUNT(*) FROM job_queue "
        "WHERE state='running' "
        "AND datetime(heartbeat_at) > datetime('now','-2 minutes')").fetchone()[0]
```

- [ ] **Step 2: 재시작 대상에 워커를 넣는다**

`sudo systemctl restart shopping-shorts` 부분을 수정한다. **서버는 항상 재시작**하고,
**워커만 진행 중이면 미룬다**:

```bash
  # [2026-07-29 독립워커] 서버(shopping-shorts)는 이제 안에 긴 작업이 없으므로 언제든 재시작
  # 해도 안전하다. 워커만 진행 중인 작업이 있으면 미룬다(위 job_queue 체크).
  sudo systemctl restart shopping-shorts >>"$LOG" 2>&1 && echo "$(date '+%F %T') shopping-shorts 재시작완료 $(git rev-parse --short HEAD)" >>"$LOG"
  sudo systemctl restart shopping-shorts-worker >>"$LOG" 2>&1 && echo "$(date '+%F %T') worker 재시작완료 $(git rev-parse --short HEAD)" >>"$LOG"
```

> ⚠️ **가드에 걸린 경우(작업 진행 중)에는 워커 재시작만 건너뛰고 서버는 재시작한다.**
> 기존 스크립트는 "배포를 통째로 미루는" 구조이므로, 그 분기를 "서버만 재시작하고 워커는
> 다음 크론에" 로 바꾼다. 스크립트의 실제 분기 구조를 읽고 그에 맞게 최소 수정할 것.

- [ ] **Step 3: 문법을 확인한다**

Run: `bash -n deploy/auto_deploy.sh`
Expected: 출력 없음(문법 정상)

- [ ] **Step 4: 커밋**

```bash
git add deploy/auto_deploy.sh
git commit -m "fix(배포): 가드를 job_queue 기준으로 — 수집·매칭도 이제 보호된다"
```

---

### Task 7: 서버에 워커 설치 + 라이브 검증

**Files:**
- Modify: 없음 (서버 설치·검증만)

**Interfaces:**
- Consumes: Task 1~6 전부
- Produces: 없음

- [ ] **Step 1: 전체 관련 테스트를 돌린다**

Run:
```bash
python -m pytest shopping_shorts/tests/test_job_queue.py shopping_shorts/tests/test_worker_loop.py \
                 shopping_shorts/tests/test_enqueue_wiring.py shopping_shorts/tests/test_queue_api.py \
                 shopping_shorts/tests/test_overseas_hot_jobs.py shopping_shorts/tests/test_overseas_api.py \
                 shopping_shorts/tests/test_store.py -v
python -c "import shopping_shorts.app"
python -c "import shopping_shorts.worker"
```
Expected: 전부 pass

- [ ] **Step 2: finish 게이트로 배포**

Run: `py tools/track.py finish 해외HOT`
Expected: 게이트 통과(실패가 기준선 13건에서 **늘지 않음**) → main 병합

- [ ] **Step 3: 서버에 systemd 유닛을 설치한다**

배포 반영(3분) 후 SSH로:
```bash
sudo cp /home/ubuntu/lotto-stock-wiki/deploy/shopping-shorts-worker.service /etc/systemd/system/
sudo systemctl daemon-reload
sudo systemctl enable --now shopping-shorts-worker
sudo systemctl status shopping-shorts-worker --no-pager
```
Expected: `active (running)`

- [ ] **Step 4: 워커가 큐를 집는지 실측한다**

SSH로:
```bash
cd /home/ubuntu/lotto-stock-wiki && set -a && . /etc/shopping-shorts.env && set +a
python3 -c "
from shopping_shorts.store import Store
from shopping_shorts.config import DB_PATH
print('큐 넣기:', Store(DB_PATH).enqueue('overseas', {}))
"
sleep 20
sudo journalctl -u shopping-shorts-worker --since '-2 min' --no-pager | tail -10
```
Expected: 로그에 `작업 시작 ... task=overseas`

- [ ] **Step 5: 죽여도 살아나는지 확인한다 (이 작업의 핵심)**

SSH로:
```bash
sudo systemctl restart shopping-shorts     # 서버만 재시작 — 워커는 안 죽어야 한다
sleep 5
sudo systemctl is-active shopping-shorts-worker
cd /home/ubuntu/lotto-stock-wiki && set -a && . /etc/shopping-shorts.env && set +a
python3 -c "
from shopping_shorts.store import Store
from shopping_shorts.config import DB_PATH
print(Store(DB_PATH).queue_status('overseas'))
"
```
Expected: 워커 `active`, 큐 상태가 `running`(또는 이미 `done`) — **`failed`가 아니어야 한다**

- [ ] **Step 6: 사장님 육안 확인**

해외HOT에서 "지금 업데이트" → 진행 표시에 **"앞에 N개 대기 중"** 또는 기존 진행 문구가
뜨는지. 그 사이 다른 트랙이 배포해도 수집이 끝까지 가는지.
