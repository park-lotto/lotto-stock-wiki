# 사용자 키 등록(BYOK) + 포인트 차감 구현 계획

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 사용자가 자기 API 키를 등록하면 그 작업은 무료로, 등록 안 하면 포인트를 차감해서 쓰게 한다.

**Architecture:** 키 선택과 과금 판단을 `keyroute.keys_for()` 한 곳으로 묶어 두 판단이 절대 어긋나지 않게 한다. 키는 Fernet으로 암호화해 `customer_keys` 테이블에 저장하고, 포인트는 기존 `points_ledger`를 100배 정수로 확장해 재사용한다. 화면은 `/settings` 신규 페이지에 탭 2개.

**Tech Stack:** Python 3 · FastAPI · SQLite(`shopping_shorts/data/reference.db`) · cryptography(Fernet, 신규) · 순수 JS(빌드 없음)

**작업 폴더:** `C:\Users\CH\Desktop\로또의 주식\.tracks\키등록포인트`
모든 경로는 이 폴더 기준. **main 폴더의 파일은 하나도 건드리지 마라** (흡수 사고의 재료).

**설계 문서:** `docs/superpowers/specs/2026-08-17-사용자키등록-포인트차감-design.md`

---

## 시작 전 필독 — 이 코드베이스의 함정

실제 코드를 읽고 확인한 것들이다. 모르고 짜면 반드시 밟는다.

| # | 함정 | 근거 |
|---|---|---|
| 1 | `customer_id`가 **int 0과 문자열 "0"으로 섞여** 들어온다. `== 0` 비교하면 사장님이 무료 등급으로 떨어진다 | `app.py:6813 _as_cid` 주석의 2026-07-30 실사고 |
| 2 | Gemini 소진표시가 **인덱스 기반**(`comment_gen.py:68`). 사용자별 키풀이 생기면 "3번 소진"이 누구의 3번인지 모른다 | `comment_gen.py:52,68` |
| 3 | 키 생존 확인에 SDK `models.list`를 쓰면 **살아있는 키도 전부 실패**로 나온다 | `comment_gen.py:95` 주석 실측 |
| 4 | `points_ledger.delta`가 INTEGER다. 0.1P를 못 담는다 | `store.py:752` |
| 5 | `points.py:15 deduct`가 reason을 `"fx_render"`로 **하드코딩**한다. 그대로 쓰면 모든 차감이 fx_render로 기록된다 | `points.py:12-16` |
| 6 | `run_clean_sources(job_id, db_path, work_root)`에 **customer_id가 없다**. 누구 포인트를 깎을지 모른다 | `mix_pipeline.py:1314` |
| 7 | 자막제거는 **소스 1편당 VMake 1콜**. 소스 3개면 1,500원 | `mix_pipeline.py:1257-1270` |
| 8 | `/api/produce/mix/clean`은 **큐에 넣기만** 한다. 라우트에선 소스 개수를 모른다 | `app.py:3706-3720` |
| 9 | 테스트에 **고정 날짜 금지** — 며칠 뒤 반드시 깨진다 | 메모리 `테스트_시한폭탄` |
| 10 | 한글 경로라 Bash에서 `cd .tracks/키등록포인트`가 cp949로 깨진다. **PowerShell 또는 절대경로** | `handoff/제미니과금.md` |

**테스트 실행 규칙:** 이 프로젝트는 기준선에 이미 실패가 있다. **"전부 green"이 아니라 "내 작업 전보다 실패가 늘지 않았나"**로 판단한다. 새로 만든 테스트는 반드시 통과해야 한다.

---

## 파일 구조

**신규 생성 (5개)**

| 파일 | 책임 |
|---|---|
| `shopping_shorts/keycrypt.py` | 키 암복호·마스킹·지문. 다른 아무것도 안 한다 |
| `shopping_shorts/keyroute.py` | **누구의 어떤 키를 쓸지 + 과금할지**를 정하는 유일한 곳 |
| `shopping_shorts/pricing.py` | 작업별 포인트 단가 (admin 설정 우선, 기본값 폴백) |
| `shopping_shorts/static/settings.html` | 설정 화면 (탭 2개) |
| `shopping_shorts/tests/test_byok_*.py` | 테스트 5파일 |

**수정**

| 파일 | 무엇을 |
|---|---|
| `store.py` | `customer_keys` 테이블 + CRUD 6개 |
| `points.py` | reason 인자화 + 100배 정수 헬퍼 |
| `app.py` | `/settings` 라우트 + API 7개 + 차감 배선 |
| `mix_pipeline.py` | `_vmake_key` → `keyroute`, `run_clean_sources`에 cid |
| `comment_gen.py` | 소진표시를 인덱스 → key_hash |
| `tts.py` · `youtube_client.py` · `similarity.py` · `video_analysis.py` · `product_facts.py` | `keyroute` 경유 |
| `requirements.txt` | `cryptography` 추가 |
| `sidebar.js` | 설정 메뉴 추가 |

---

## Task 1: 암호화 유틸 (`keycrypt.py`)

사용자 키를 평문으로 DB에 두면 안 된다. 이 프로젝트엔 양방향 암호화가 **아직 없다**(pbkdf2 해시·HMAC 서명만 있고 `cryptography` 미설치).

**Files:**
- Create: `shopping_shorts/keycrypt.py`
- Create: `shopping_shorts/tests/test_byok_keycrypt.py`
- Modify: `shopping_shorts/requirements.txt`

- [ ] **Step 1: cryptography 설치**

```bash
py -m pip install cryptography
```

Expected: `Successfully installed cryptography-...`

- [ ] **Step 2: requirements.txt에 추가**

`shopping_shorts/requirements.txt` 마지막 줄에 추가:

```
cryptography
```

- [ ] **Step 3: 실패하는 테스트 작성**

Create `shopping_shorts/tests/test_byok_keycrypt.py`:

```python
"""키 암복호 — 평문이 DB에 남지 않는지, 마스터키 없으면 꺼지는지."""
import importlib
import pytest
from shopping_shorts import keycrypt


def _reload(monkeypatch, master):
    """마스터키를 바꿔 모듈을 다시 읽는다(모듈 로드 시점에 키를 잡으므로)."""
    if master is None:
        monkeypatch.delenv("BYOK_MASTER_KEY", raising=False)
    else:
        monkeypatch.setenv("BYOK_MASTER_KEY", master)
    return importlib.reload(keycrypt)


_KEY = "NZAowCs7o9LHVnJdZbxrVmYI7MHqyPFkydIUd1mc8To="   # 유효한 Fernet 키(44자). 테스트 전용


def test_roundtrip(monkeypatch):
    kc = _reload(monkeypatch, _KEY)
    plain = "AQ.Ab8RNxxxxxxxxxxxxxxxxxxxxkz1MA"
    token = kc.encrypt(plain)
    assert token != plain              # 평문이 그대로 남으면 안 된다
    assert kc.decrypt(token) == plain


def test_same_plain_gives_different_ciphertext(monkeypatch):
    """같은 키를 두 번 넣어도 암호문이 달라야 한다(Fernet은 IV를 쓴다)."""
    kc = _reload(monkeypatch, _KEY)
    assert kc.encrypt("same") != kc.encrypt("same")


def test_mask_hides_middle(monkeypatch):
    kc = _reload(monkeypatch, _KEY)
    masked = kc.mask("AQ.Ab8RNxxxxxxxxxxxxxxxxxxxxkz1MA")
    assert masked.startswith("AQ.Ab8RN")
    assert masked.endswith("kz1MA")
    assert "•" in masked
    assert "xxxxxxxx" not in masked     # 가운데가 실제로 가려졌나

def test_mask_short_key_does_not_leak(monkeypatch):
    """짧은 키는 앞뒤를 보여주면 통째로 노출된다 — 전부 가린다."""
    kc = _reload(monkeypatch, _KEY)
    assert "abc" not in kc.mask("abc")


def test_fingerprint_is_stable_and_distinct(monkeypatch):
    kc = _reload(monkeypatch, _KEY)
    assert kc.fingerprint("key-a") == kc.fingerprint("key-a")
    assert kc.fingerprint("key-a") != kc.fingerprint("key-b")


def test_disabled_without_master_key(monkeypatch):
    """★마스터키가 없으면 평문 저장으로 폴백하지 않는다 — 기능을 끈다."""
    kc = _reload(monkeypatch, None)
    assert kc.enabled() is False
    with pytest.raises(RuntimeError):
        kc.encrypt("anything")


def test_enabled_with_master_key(monkeypatch):
    kc = _reload(monkeypatch, _KEY)
    assert kc.enabled() is True
```

- [ ] **Step 4: 테스트가 실패하는지 확인**

```bash
py -m pytest shopping_shorts/tests/test_byok_keycrypt.py -v
```

Expected: FAIL — `ModuleNotFoundError: No module named 'shopping_shorts.keycrypt'`

- [ ] **Step 5: keycrypt.py 구현**

Create `shopping_shorts/keycrypt.py`:

```python
"""사용자 API 키 암복호 — 평문을 DB에 두지 않기 위한 최소 유틸.

마스터키는 `/etc/shopping-shorts.env`의 BYOK_MASTER_KEY(서버 전용, git 미포함).
★키가 없으면 기능을 끈다 — 평문 저장으로 조용히 폴백하지 않는다. 조용한 폴백은
"되는 줄 알았는데 안 되는" 사고를 만든다(CLAUDE.md 0순위-B).

생성: python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
"""
import hashlib
import os

_MASTER = os.environ.get("BYOK_MASTER_KEY", "").strip()
_fernet = None
if _MASTER:
    try:
        from cryptography.fernet import Fernet
        _fernet = Fernet(_MASTER.encode())
    except Exception:       # 형식이 틀린 키 — 기능을 끄고 조용히 넘어가지 않는다
        _fernet = None


def enabled():
    """키 등록 기능을 쓸 수 있는가. False면 화면에서 등록란을 감춘다."""
    return _fernet is not None


def encrypt(plain):
    if _fernet is None:
        raise RuntimeError("BYOK_MASTER_KEY가 없어 키를 저장할 수 없습니다")
    return _fernet.encrypt(plain.encode()).decode()


def decrypt(token):
    if _fernet is None:
        raise RuntimeError("BYOK_MASTER_KEY가 없어 키를 읽을 수 없습니다")
    return _fernet.decrypt(token.encode()).decode()


def mask(plain):
    """화면 표시용. 앞8·뒤5만 남기고 가운데를 가린다.
    짧은 키(20자 미만)는 앞뒤를 보여주면 통째로 드러나므로 전부 가린다."""
    if not plain:
        return ""
    if len(plain) < 20:
        return "•" * 12
    return f"{plain[:8]}{'•' * 8}{plain[-5:]}"


def fingerprint(plain):
    """키 식별자(sha256 앞16자). 중복검사·소진표시에 쓴다.
    ★인덱스 번호를 쓰면 사용자별 풀이 섞인다(comment_gen.py:68의 기존 함정)."""
    return hashlib.sha256(plain.encode()).hexdigest()[:16]
```

- [ ] **Step 6: 테스트 통과 확인**

```bash
py -m pytest shopping_shorts/tests/test_byok_keycrypt.py -v
```

Expected: 7 passed

- [ ] **Step 7: 커밋**

```bash
git add shopping_shorts/keycrypt.py shopping_shorts/tests/test_byok_keycrypt.py shopping_shorts/requirements.txt
git commit -m "feat: 사용자 키 암복호 유틸 — 마스터키 없으면 기능을 끈다"
```

---

## Task 2: `customer_keys` 테이블 + CRUD

기존 `settings` 테이블은 `key TEXT PRIMARY KEY`라 **customer_id 축이 아예 없다**(`store.py:622`). 재사용 불가라 새로 만든다.

**Files:**
- Modify: `shopping_shorts/store.py` (스키마 + 메서드)
- Create: `shopping_shorts/tests/test_byok_store.py`

- [ ] **Step 1: 실패하는 테스트 작성**

Create `shopping_shorts/tests/test_byok_store.py`:

```python
"""customer_keys 저장·조회 — 사용자별로 갈리는지, 중복이 막히는지."""
import importlib
import pytest
from shopping_shorts.store import Store
from shopping_shorts import keycrypt

_KEY = "NZAowCs7o9LHVnJdZbxrVmYI7MHqyPFkydIUd1mc8To="   # 유효한 Fernet 키(44자). 테스트 전용


@pytest.fixture
def store(tmp_path, monkeypatch):
    monkeypatch.setenv("BYOK_MASTER_KEY", _KEY)
    importlib.reload(keycrypt)
    return Store(str(tmp_path / "t.db"))


def test_add_and_list(store):
    store.add_customer_key(1, "vmake", "appkey:secret")
    rows = store.list_customer_keys(1, "vmake")
    assert len(rows) == 1
    assert rows[0]["label"] == keycrypt.mask("appkey:secret")
    assert "secret" not in rows[0]["label"]      # 평문이 라벨로 새면 안 된다


def test_list_does_not_return_plaintext(store):
    """★목록 조회에 평문 키가 섞여 나오면 화면으로 샌다."""
    store.add_customer_key(1, "vmake", "appkey:secret")
    row = store.list_customer_keys(1, "vmake")[0]
    assert "key_enc" not in row
    assert "appkey:secret" not in str(row)


def test_users_are_isolated(store):
    """A가 넣은 키가 B에게 보이면 안 된다."""
    store.add_customer_key(1, "vmake", "user-one-key")
    store.add_customer_key(2, "vmake", "user-two-key")
    assert store.get_customer_keys_plain(1, "vmake") == ["user-one-key"]
    assert store.get_customer_keys_plain(2, "vmake") == ["user-two-key"]


def test_services_are_isolated(store):
    store.add_customer_key(1, "vmake", "vmake-key")
    store.add_customer_key(1, "gemini", "gemini-key")
    assert store.get_customer_keys_plain(1, "vmake") == ["vmake-key"]
    assert store.get_customer_keys_plain(1, "gemini") == ["gemini-key"]


def test_duplicate_key_is_rejected(store):
    assert store.add_customer_key(1, "gemini", "same-key") is True
    assert store.add_customer_key(1, "gemini", "same-key") is False   # 두 번째는 거절
    assert len(store.list_customer_keys(1, "gemini")) == 1


def test_gemini_holds_multiple_keys(store):
    """구글만 여러 개 — 하나가 한도에 걸리면 다음으로 넘어가야 한다."""
    for k in ("k1", "k2", "k3"):
        store.add_customer_key(1, "gemini", k)
    assert store.get_customer_keys_plain(1, "gemini") == ["k1", "k2", "k3"]


def test_delete(store):
    store.add_customer_key(1, "vmake", "bye-key")
    kid = store.list_customer_keys(1, "vmake")[0]["id"]
    store.delete_customer_key(1, kid)
    assert store.list_customer_keys(1, "vmake") == []


def test_delete_only_own_key(store):
    """★남의 키를 id만 알면 지울 수 있으면 안 된다."""
    store.add_customer_key(1, "vmake", "mine")
    kid = store.list_customer_keys(1, "vmake")[0]["id"]
    store.delete_customer_key(999, kid)                  # 다른 사용자가 시도
    assert len(store.list_customer_keys(1, "vmake")) == 1  # 그대로 남아야


def test_set_status(store):
    store.add_customer_key(1, "gemini", "probe-me")
    kid = store.list_customer_keys(1, "gemini")[0]["id"]
    store.set_customer_key_status(kid, "ok")
    assert store.list_customer_keys(1, "gemini")[0]["status"] == "ok"


def test_status_defaults_to_unknown(store):
    store.add_customer_key(1, "gemini", "fresh")
    assert store.list_customer_keys(1, "gemini")[0]["status"] == "unknown"
```

- [ ] **Step 2: 테스트가 실패하는지 확인**

```bash
py -m pytest shopping_shorts/tests/test_byok_store.py -v
```

Expected: FAIL — `AttributeError: 'Store' object has no attribute 'add_customer_key'`

- [ ] **Step 3: 스키마 추가**

`shopping_shorts/store.py` — `points_ledger` 테이블 생성 블록(`store.py:752` 근처) **바로 다음**에 추가:

```python
            # 사용자별 API 키(2026-08-17, BYOK) — settings는 key가 PK라 customer_id 축이
            # 없어 재사용 불가. key_enc는 Fernet 암호문이고 평문은 어디에도 안 남는다.
            # key_hash로 식별하는 이유: 인덱스 번호를 쓰면 사용자별 풀이 섞인다
            # (comment_gen.py:68이 인덱스 기반이라 겪는 문제).
            c.execute("""
                CREATE TABLE IF NOT EXISTS customer_keys (
                    id INTEGER PRIMARY KEY AUTOINCREMENT,
                    customer_id INTEGER NOT NULL,
                    service TEXT NOT NULL,
                    key_enc TEXT NOT NULL,
                    key_hash TEXT NOT NULL,
                    label TEXT,
                    status TEXT NOT NULL DEFAULT 'unknown',
                    checked_at INTEGER,
                    created_at INTEGER NOT NULL
                )
            """)
            c.execute("CREATE INDEX IF NOT EXISTS ix_ck_cust "
                      "ON customer_keys(customer_id, service)")
            c.execute("CREATE UNIQUE INDEX IF NOT EXISTS ux_ck_dup "
                      "ON customer_keys(customer_id, service, key_hash)")
```

- [ ] **Step 4: CRUD 메서드 추가**

`shopping_shorts/store.py` — `points_add` 메서드(`store.py:4485` 근처) **바로 다음**에 추가:

```python
    # ── 사용자별 API 키(2026-08-17, BYOK) ──
    # 평문은 이 클래스 밖으로 나가는 경로가 get_customer_keys_plain 하나뿐이다.
    # 화면용 list_customer_keys는 key_enc를 아예 안 실어 보낸다.
    def add_customer_key(self, customer_id, service, plain):
        """키 1개 저장. 이미 있는 키면 False(중복 거절)."""
        from shopping_shorts import keycrypt
        import time
        try:
            with self._conn() as c:
                c.execute(
                    "INSERT INTO customer_keys"
                    "(customer_id, service, key_enc, key_hash, label, created_at) "
                    "VALUES(?,?,?,?,?,?)",
                    (int(customer_id), service, keycrypt.encrypt(plain),
                     keycrypt.fingerprint(plain), keycrypt.mask(plain), int(time.time())),
                )
            return True
        except sqlite3.IntegrityError:      # ux_ck_dup — 같은 사용자가 같은 키를 또 넣음
            return False

    def list_customer_keys(self, customer_id, service=None):
        """화면용 목록. ★key_enc를 안 싣는다 — 평문 경로는 여기가 아니다."""
        q = ("SELECT id, service, label, status, checked_at, created_at "
             "FROM customer_keys WHERE customer_id=?")
        args = [int(customer_id)]
        if service:
            q += " AND service=?"
            args.append(service)
        q += " ORDER BY id"
        with self._conn() as c:
            rows = c.execute(q, args).fetchall()
        return [{"id": r[0], "service": r[1], "label": r[2], "status": r[3],
                 "checked_at": r[4], "created_at": r[5]} for r in rows]

    def get_customer_keys_plain(self, customer_id, service):
        """실제 호출에 쓸 평문 키 목록. 복호 실패한 행은 건너뛴다(마스터키 교체 등)."""
        from shopping_shorts import keycrypt
        with self._conn() as c:
            rows = c.execute(
                "SELECT key_enc FROM customer_keys WHERE customer_id=? AND service=? "
                "ORDER BY id", (int(customer_id), service)).fetchall()
        out = []
        for (enc,) in rows:
            try:
                out.append(keycrypt.decrypt(enc))
            except Exception:
                continue
        return out

    def delete_customer_key(self, customer_id, key_id):
        """★customer_id를 조건에 반드시 넣는다 — 안 넣으면 id만 알면 남의 키를 지운다."""
        with self._conn() as c:
            c.execute("DELETE FROM customer_keys WHERE id=? AND customer_id=?",
                      (int(key_id), int(customer_id)))

    def set_customer_key_status(self, key_id, status):
        import time
        with self._conn() as c:
            c.execute("UPDATE customer_keys SET status=?, checked_at=? WHERE id=?",
                      (status, int(time.time()), int(key_id)))

    def count_customer_keys(self, customer_id, service):
        with self._conn() as c:
            row = c.execute(
                "SELECT COUNT(*) FROM customer_keys WHERE customer_id=? AND service=?",
                (int(customer_id), service)).fetchone()
        return int(row[0])
```

- [ ] **Step 5: 테스트 통과 확인**

```bash
py -m pytest shopping_shorts/tests/test_byok_store.py -v
```

Expected: 10 passed

- [ ] **Step 6: 커밋**

```bash
git add shopping_shorts/store.py shopping_shorts/tests/test_byok_store.py
git commit -m "feat: customer_keys 테이블 — 사용자별 키를 암호문으로만 저장"
```

---

## Task 3: 단가표 (`pricing.py`)

단가를 코드에 박으면 실측 후 조정할 때마다 배포해야 한다. **admin 설정값 우선, 기본값 폴백**으로 만든다(기존 `_CREDIT_DEFAULTS` 패턴과 같다).

**Files:**
- Create: `shopping_shorts/pricing.py`
- Create: `shopping_shorts/tests/test_byok_pricing.py`

- [ ] **Step 1: 실패하는 테스트 작성**

Create `shopping_shorts/tests/test_byok_pricing.py`:

```python
"""단가표 — 내부는 100배 정수, 화면은 사람이 읽는 값."""
import pytest
from shopping_shorts import pricing
from shopping_shorts.store import Store


@pytest.fixture
def store(tmp_path):
    return Store(str(tmp_path / "t.db"))


def test_vmake_is_five_points(store):
    """자막제거 5P = 실비용 500원 ÷ (1P=100원). 유일하게 근거가 확실한 값."""
    assert pricing.cost(store, "vmake") == 500      # 5P × 100


def test_default_when_unset(store):
    assert pricing.cost(store, "mix") == 300        # 3P
    assert pricing.cost(store, "tts") == 100        # 1P
    assert pricing.cost(store, "lens") == 10        # 0.1P
    assert pricing.cost(store, "script") == 10      # 0.1P


def test_admin_override(store):
    """★배포 없이 숫자만 고칠 수 있어야 한다."""
    store.set_setting("point_cost_vmake", "700")
    assert pricing.cost(store, "vmake") == 700


def test_unknown_op_is_free(store):
    """모르는 작업에 임의 요금을 매기지 않는다 — 0이 안전한 기본값."""
    assert pricing.cost(store, "무료작업") == 0


def test_broken_setting_falls_back(store):
    """설정값이 깨져도 죽지 않고 기본값으로 돈다."""
    store.set_setting("point_cost_vmake", "이건숫자가아님")
    assert pricing.cost(store, "vmake") == 500


def test_negative_setting_is_ignored(store):
    """음수 단가는 충전이 돼버린다 — 막는다."""
    store.set_setting("point_cost_vmake", "-999")
    assert pricing.cost(store, "vmake") == 500


def test_to_display_converts_to_human(store):
    assert pricing.to_display(500) == 5
    assert pricing.to_display(10) == 0.1
    assert pricing.to_display(0) == 0


def test_display_drops_trailing_zero(store):
    """5.0P가 아니라 5P로 보여야 한다."""
    assert str(pricing.to_display(500)) == "5"
```

- [ ] **Step 2: 테스트가 실패하는지 확인**

```bash
py -m pytest shopping_shorts/tests/test_byok_pricing.py -v
```

Expected: FAIL — `ModuleNotFoundError: No module named 'shopping_shorts.pricing'`

- [ ] **Step 3: pricing.py 구현**

Create `shopping_shorts/pricing.py`:

```python
"""작업별 포인트 단가 — 내부는 100배 정수, 화면에서만 나눈다.

왜 100배인가: points_ledger.delta가 INTEGER라 0.1P를 못 담는다(store.py:752).
소수를 쓰면 반올림 오차가 잔액에 쌓이므로 정수로만 계산한다.

단가는 admin 설정(point_cost_{op})이 있으면 그걸 쓴다 — 실측 후 배포 없이 고치려고.
기본값은 자막제거만 실비용 근거가 있고(500원/편) 나머지는 임시값이다.
"""

# 1P = 100원 (마진 0, 원가대로). 값은 '포인트×100'.
_DEFAULTS = {
    "vmake":  500,   # 5P   — VMake 실비용 500원/편 (사장님 확인 2026-08-17)
    "mix":    300,   # 3P   — ⚠️임시값. 영상 1편 Gemini+TTS 미실측
    "tts":    100,   # 1P   — ⚠️임시값. 문자수 과금 미실측
    "lens":    10,   # 0.1P — SerpApi 키당 월 100회 무료, 남용 방지용
    "script":  10,   # 0.1P — Gemini 태깅 1건 0.679원 실측 기준(최소단위)
}


def cost(store, op):
    """이 작업 1회에 깎을 포인트(×100). 모르는 작업은 0(무료)."""
    if op not in _DEFAULTS:
        return 0
    try:
        raw = store.get_setting(f"point_cost_{op}")
        if raw is not None:
            v = int(raw)
            if v >= 0:              # 음수는 충전이 돼버린다
                return v
    except (TypeError, ValueError):
        pass                        # 깨진 설정값 — 기본값으로 간다
    return _DEFAULTS[op]


def to_display(internal):
    """내부값 → 화면용. 500 → 5, 10 → 0.1. 정수면 정수로 준다(5.0P 방지)."""
    v = internal / 100
    return int(v) if v == int(v) else round(v, 2)
```

- [ ] **Step 4: 테스트 통과 확인**

```bash
py -m pytest shopping_shorts/tests/test_byok_pricing.py -v
```

Expected: 8 passed

- [ ] **Step 5: 커밋**

```bash
git add shopping_shorts/pricing.py shopping_shorts/tests/test_byok_pricing.py
git commit -m "feat: 작업별 단가표 — admin 설정으로 배포 없이 조정"
```

---

## Task 4: 키 선택 + 과금 판단 (`keyroute.py`)

**이번 작업에서 가장 중요한 파일이다.** 키를 고르는 판단과 과금 여부를 **한 곳에서 함께** 정한다.

CLAUDE.md 0순위-B: 계정과 프록시를 따로 정했다가 5벌로 흩어져 로테이션이 통째로 죽은 사고가 있었다. **짝으로 움직여야 하는 값은 함께 정한다.**

**Files:**
- Create: `shopping_shorts/keyroute.py`
- Create: `shopping_shorts/tests/test_byok_keyroute.py`

- [ ] **Step 1: 실패하는 테스트 작성**

Create `shopping_shorts/tests/test_byok_keyroute.py`:

```python
"""키 선택 + 과금 판단 — 이 둘이 절대 어긋나면 안 된다."""
import importlib
import pytest
from shopping_shorts import keyroute, keycrypt
from shopping_shorts.store import Store

_KEY = "NZAowCs7o9LHVnJdZbxrVmYI7MHqyPFkydIUd1mc8To="   # 유효한 Fernet 키(44자). 테스트 전용


@pytest.fixture
def store(tmp_path, monkeypatch):
    monkeypatch.setenv("BYOK_MASTER_KEY", _KEY)
    importlib.reload(keycrypt)
    return Store(str(tmp_path / "t.db"))


def test_user_key_wins(store, monkeypatch):
    monkeypatch.setattr(keyroute, "_owner_keys", lambda svc: ["사장님키"])
    store.add_customer_key(7, "vmake", "내키")
    keys, is_user = keyroute.keys_for(store, 7, "vmake")
    assert keys == ["내키"]
    assert is_user is True


def test_no_user_key_uses_owner(store, monkeypatch):
    monkeypatch.setattr(keyroute, "_owner_keys", lambda svc: ["사장님키"])
    keys, is_user = keyroute.keys_for(store, 7, "vmake")
    assert keys == ["사장님키"]
    assert is_user is False


def test_never_falls_back(store, monkeypatch):
    """★사장님 지시: 폴백 없음. 사용자 키가 있으면 그것만 쓴다.
    사용자 키가 소진돼도 사장님 키로 넘어가면 안 된다 — 그러면 비용이 샌다."""
    monkeypatch.setattr(keyroute, "_owner_keys", lambda svc: ["사장님키1", "사장님키2"])
    store.add_customer_key(7, "gemini", "내키")
    keys, is_user = keyroute.keys_for(store, 7, "gemini")
    assert keys == ["내키"]
    assert "사장님키1" not in keys
    assert "사장님키2" not in keys


def test_string_customer_id(store, monkeypatch):
    """★cid가 문자열 "7"로 오는 경로가 있다(app.py:6813 실사고).
    int와 같게 다뤄야 사용자 키를 못 찾고 사장님 키로 새는 일이 없다."""
    monkeypatch.setattr(keyroute, "_owner_keys", lambda svc: ["사장님키"])
    store.add_customer_key(7, "vmake", "내키")
    keys, is_user = keyroute.keys_for(store, "7", "vmake")
    assert keys == ["내키"]
    assert is_user is True


def test_users_do_not_share(store, monkeypatch):
    monkeypatch.setattr(keyroute, "_owner_keys", lambda svc: ["사장님키"])
    store.add_customer_key(1, "vmake", "A키")
    keys, is_user = keyroute.keys_for(store, 2, "vmake")     # B는 등록 안 함
    assert keys == ["사장님키"]
    assert is_user is False


def test_gemini_returns_all_user_keys(store, monkeypatch):
    """구글은 여러 개 — 하나가 한도에 걸리면 다음으로 넘어가야 하니 전부 준다."""
    monkeypatch.setattr(keyroute, "_owner_keys", lambda svc: ["사장님키"])
    for k in ("k1", "k2", "k3"):
        store.add_customer_key(7, "gemini", k)
    keys, is_user = keyroute.keys_for(store, 7, "gemini")
    assert keys == ["k1", "k2", "k3"]
    assert is_user is True


def test_empty_when_nobody_has_keys(store, monkeypatch):
    monkeypatch.setattr(keyroute, "_owner_keys", lambda svc: [])
    keys, is_user = keyroute.keys_for(store, 7, "vmake")
    assert keys == []
    assert is_user is False


def test_should_charge_is_inverse_of_user_key(store, monkeypatch):
    """★과금 여부는 키 출처에서만 나온다 — 따로 판단하는 곳을 만들지 마라."""
    monkeypatch.setattr(keyroute, "_owner_keys", lambda svc: ["사장님키"])
    assert keyroute.should_charge(store, 7, "vmake") is True     # 사장님 키 → 과금
    store.add_customer_key(7, "vmake", "내키")
    assert keyroute.should_charge(store, 7, "vmake") is False    # 내 키 → 무료
```

- [ ] **Step 2: 테스트가 실패하는지 확인**

```bash
py -m pytest shopping_shorts/tests/test_byok_keyroute.py -v
```

Expected: FAIL — `ModuleNotFoundError: No module named 'shopping_shorts.keyroute'`

- [ ] **Step 3: keyroute.py 구현**

Create `shopping_shorts/keyroute.py`:

```python
"""누구의 어떤 키를 쓸지 정하는 **유일한 곳**.

★왜 한 곳인가 (CLAUDE.md 0순위-B)
키를 고르는 판단과 "과금할까"는 짝이다. 따로 적으면 반드시 어긋난다 —
계정과 프록시를 따로 정했다가 5벌로 흩어져 로테이션이 통째로 죽은 실사고가 있다.
그래서 keys_for()가 (키, 사용자키인가)를 **함께** 돌려주고,
should_charge()는 그 두 번째 값을 뒤집기만 한다. 과금을 따로 판단하지 마라.

★폴백 없음 (사장님 확정)
사용자 키가 있으면 그 키만 쓴다. 소진돼도 사장님 키로 안 넘어간다.
넘어가면 "키 등록했는데 사장님 돈이 나가는" 상태가 조용히 생긴다.
"""

_SERVICES = ("gemini", "vmake", "elevenlabs", "youtube")


def _as_cid(customer_id):
    """cid는 int 0과 문자열 "0"이 섞여 온다(app.py:6813의 2026-07-30 실사고).
    정규화 안 하면 사용자 키를 못 찾고 조용히 사장님 키로 샌다."""
    try:
        return int(customer_id)
    except (TypeError, ValueError):
        return 0


def _owner_keys(service):
    """사장님(회사) 키. 테스트에서 monkeypatch로 갈아끼운다."""
    from shopping_shorts import config
    if service == "gemini":
        return list(config.SHORTS_GEMINI_KEYS)
    if service == "youtube":
        return list(config.YOUTUBE_API_KEYS)
    if service == "elevenlabs":
        k = getattr(config, "ELEVENLABS_API_KEY", "")
        return [k] if k else []
    if service == "vmake":
        return []       # vmake는 store 설정에 있다 — keys_for에서 따로 읽는다
    return []


def _owner_vmake_key(store):
    """vmake만 env가 아니라 settings에 있다(app.py:2838로 등록한 전역 키)."""
    k = store.get_setting("vmake_api_key", "") or ""
    return [k] if k else []


def keys_for(store, customer_id, service):
    """(쓸 키 목록, 사용자 키인가) 반환.

    사용자 키가 하나라도 있으면 그것만 돌려준다. 없으면 사장님 키.
    둘 다 없으면 ([], False) — 호출부가 "설정 안 됨"으로 처리한다.
    """
    cid = _as_cid(customer_id)
    if cid:                                   # cid 0 = 사장님 본인이라 조회 안 함
        mine = store.get_customer_keys_plain(cid, service)
        if mine:
            return mine, True                 # ★여기서 끝. 사장님 키를 섞지 않는다
    owner = _owner_vmake_key(store) if service == "vmake" else _owner_keys(service)
    return owner, False


def should_charge(store, customer_id, service):
    """포인트를 깎아야 하는가. 사용자 키를 쓰면 안 깎는다.

    ★keys_for의 판단을 그대로 뒤집기만 한다 — 여기서 따로 판단하면 어긋난다."""
    _, is_user = keys_for(store, customer_id, service)
    return not is_user
```

- [ ] **Step 4: 테스트 통과 확인**

```bash
py -m pytest shopping_shorts/tests/test_byok_keyroute.py -v
```

Expected: 8 passed

- [ ] **Step 5: 커밋**

```bash
git add shopping_shorts/keyroute.py shopping_shorts/tests/test_byok_keyroute.py
git commit -m "feat: 키 선택과 과금 판단을 한 곳으로 — 폴백 없음"
```

---

## Task 5: 포인트 차감 확장

`points.py`가 지금 **reason을 "fx_render"로 하드코딩**한다(`points.py:15`). 그대로 쓰면 자막제거·영상제작이 전부 fx_render로 기록돼 "어디에 얼마 썼나"를 못 가린다.

**Files:**
- Modify: `shopping_shorts/points.py`
- Create: `shopping_shorts/tests/test_byok_points.py`

- [ ] **Step 1: 실패하는 테스트 작성**

Create `shopping_shorts/tests/test_byok_points.py`:

```python
"""포인트 차감 — 어디에 썼는지 남고, 실패하면 돌려받는다."""
import pytest
from shopping_shorts import points
from shopping_shorts.store import Store


@pytest.fixture
def store(tmp_path):
    return Store(str(tmp_path / "t.db"))


def test_deduct_records_reason(store):
    """★어디에 썼는지가 남아야 정산·문의 대응이 된다."""
    points.add(store, 1, 1000)
    assert points.deduct(store, 1, 500, "vmake") is True
    rows = store.points_history(1)
    assert rows[0]["reason"] == "vmake"


def test_deduct_reduces_balance(store):
    points.add(store, 1, 1000)
    points.deduct(store, 1, 500, "vmake")
    assert points.balance(store, 1) == 500


def test_deduct_blocked_when_short(store):
    points.add(store, 1, 300)
    assert points.deduct(store, 1, 500, "vmake") is False
    assert points.balance(store, 1) == 300      # 안 깎였나


def test_exact_balance_is_allowed(store):
    """딱 맞으면 써져야 한다 — 잔액 0으로 끝나는 건 정상."""
    points.add(store, 1, 500)
    assert points.deduct(store, 1, 500, "vmake") is True
    assert points.balance(store, 1) == 0


def test_refund_restores(store):
    points.add(store, 1, 1000)
    points.deduct(store, 1, 500, "vmake")
    points.refund(store, 1, 500, "vmake")
    assert points.balance(store, 1) == 1000


def test_refund_reason_is_marked(store):
    """환불인지 충전인지 구분돼야 한다."""
    points.add(store, 1, 1000)
    points.refund(store, 1, 500, "vmake")
    assert store.points_history(1)[0]["reason"] == "vmake_refund"


def test_zero_cost_does_not_write(store):
    """무료 작업이 원장을 더럽히면 안 된다."""
    points.add(store, 1, 1000)
    assert points.deduct(store, 1, 0, "free") is True
    assert len(store.points_history(1)) == 1     # 충전 1건뿐


def test_fx_render_still_works(store):
    """★기존 고급효과 렌더가 안 깨져야 한다(reason 인자 없이 호출)."""
    points.add(store, 1, 1000)
    assert points.deduct(store, 1, 10) is True
    assert store.points_history(1)[0]["reason"] == "fx_render"


def test_history_is_newest_first(store):
    points.add(store, 1, 1000)
    points.deduct(store, 1, 100, "lens")
    points.deduct(store, 1, 500, "vmake")
    reasons = [r["reason"] for r in store.points_history(1)]
    assert reasons[:2] == ["vmake", "lens"]
```

- [ ] **Step 2: 테스트가 실패하는지 확인**

```bash
py -m pytest shopping_shorts/tests/test_byok_points.py -v
```

Expected: FAIL — `TypeError: deduct() takes 3 positional arguments but 4 were given`

- [ ] **Step 3: points.py 수정**

Replace `shopping_shorts/points.py` 전체:

```python
"""포인트 원장 — 음수 delta=차감. 잔액은 delta 누적합(store.points_balance).

★단위: 내부값은 '포인트×100'이다(pricing.py 참조). points_ledger.delta가
INTEGER라 0.1P를 못 담기 때문. 화면에 보일 때만 pricing.to_display로 나눈다.

★reason: 어디에 썼는지 남긴다. 기본값 "fx_render"는 기존 고급효과 렌더
호출부(app.py:9538)가 인자 없이 부르고 있어 하위호환으로 남긴 것이다.
"""


def balance(store, customer_id):
    return store.points_balance(customer_id)


def add(store, customer_id, amount, reason="add"):
    store.points_add(customer_id, amount, reason)


def deduct(store, customer_id, amount, reason="fx_render"):
    """잔액이 모자라면 아무것도 안 하고 False. 0원 작업은 원장을 안 더럽힌다."""
    if amount <= 0:
        return True
    if store.points_balance(customer_id) < amount:
        return False
    store.points_add(customer_id, -amount, reason)
    return True


def refund(store, customer_id, amount, reason="fx"):
    """실패한 작업을 돌려준다. reason에 _refund가 붙어 충전과 구분된다."""
    if amount <= 0:
        return
    store.points_add(customer_id, amount, f"{reason}_refund")
```

- [ ] **Step 4: `points_history` 메서드 추가**

`shopping_shorts/store.py` — `count_customer_keys` 메서드(Task 2에서 추가한 것) **바로 다음**에 추가:

```python
    def points_history(self, customer_id, limit=50):
        """포인트 사용 내역(최신순). 화면의 '최근 사용'과 문의 대응에 쓴다."""
        with self._conn() as c:
            rows = c.execute(
                "SELECT delta, reason, created_at FROM points_ledger "
                "WHERE customer_id=? ORDER BY rowid DESC LIMIT ?",
                (int(customer_id), int(limit)),
            ).fetchall()
        return [{"delta": r[0], "reason": r[1], "created_at": r[2]} for r in rows]
```

- [ ] **Step 5: 테스트 통과 확인**

```bash
py -m pytest shopping_shorts/tests/test_byok_points.py -v
```

Expected: 9 passed

- [ ] **Step 6: 기존 FX 렌더가 안 깨졌는지 확인**

```bash
py -m pytest shopping_shorts/tests/ -k "fx or point" -v
```

Expected: 기존 테스트 전부 통과 (하위호환 확인)

- [ ] **Step 7: 커밋**

```bash
git add shopping_shorts/points.py shopping_shorts/store.py shopping_shorts/tests/test_byok_points.py
git commit -m "feat: 포인트 차감에 사용처 기록 — 기존 fx_render 호출은 그대로"
```

---

## Task 6: ★자막제거 차감 배선 (새는 곳 막기)

**지금 `/api/produce/mix/clean`에 크레딧 검사가 하나도 없다.** 소스 1편당 VMake 1콜(500원)이 나가는데 무제한으로 누를 수 있다. **이번 작업에서 가장 급한 부분이다.**

구조상 주의: 라우트(`app.py:3706`)는 큐에 넣기만 하고 실제 호출은 워커가 한다. **라우트 시점엔 소스 개수를 모른다.** 그래서 차감은 `_ensure_clean_sources`가 `todo`를 계산한 직후에 한다.

**Files:**
- Modify: `shopping_shorts/mix_pipeline.py:1185,1257,1314`
- Create: `shopping_shorts/tests/test_byok_vmake_charge.py`

- [ ] **Step 1: 실패하는 테스트 작성**

Create `shopping_shorts/tests/test_byok_vmake_charge.py`:

```python
"""자막제거 과금 — 소스 개수만큼 깎고, 캐시된 건 안 깎는다."""
import importlib
import pytest
from shopping_shorts import mix_pipeline as mp, keycrypt, points
from shopping_shorts.store import Store

_KEY = "NZAowCs7o9LHVnJdZbxrVmYI7MHqyPFkydIUd1mc8To="   # 유효한 Fernet 키(44자). 테스트 전용


@pytest.fixture
def store(tmp_path, monkeypatch):
    monkeypatch.setenv("BYOK_MASTER_KEY", _KEY)
    importlib.reload(keycrypt)
    s = Store(str(tmp_path / "t.db"))
    s.set_setting("vmake_api_key", "사장님키")
    return s


def test_charges_per_source(store, monkeypatch, tmp_path):
    """★소스 3개면 VMake가 3번 돈다 = 15P(1500). job당 1회가 아니다."""
    points.add(store, 5, 10000)
    charged = mp._charge_clean(store, 5, 3)
    assert charged == 1500
    assert points.balance(store, 5) == 8500


def test_blocked_when_short(store):
    """잔액이 모자라면 아예 시작하지 않는다 — 반만 청소되면 안 된다."""
    points.add(store, 5, 1000)          # 10P — 소스 3개(15P)엔 부족
    with pytest.raises(mp.NotEnoughPoints):
        mp._charge_clean(store, 5, 3)
    assert points.balance(store, 5) == 1000     # 안 깎였나


def test_user_key_is_free(store, monkeypatch):
    """★내 VMake 키를 등록했으면 한 푼도 안 깎인다."""
    points.add(store, 5, 10000)
    store.add_customer_key(5, "vmake", "내키")
    assert mp._charge_clean(store, 5, 3) == 0
    assert points.balance(store, 5) == 10000


def test_zero_sources_is_free(store):
    """전부 캐시돼 청소할 게 없으면 과금 0."""
    points.add(store, 5, 10000)
    assert mp._charge_clean(store, 5, 0) == 0
    assert points.balance(store, 5) == 10000


def test_refund_on_failure(store):
    points.add(store, 5, 10000)
    mp._charge_clean(store, 5, 2)
    assert points.balance(store, 5) == 9000
    mp._refund_clean(store, 5, 1000)
    assert points.balance(store, 5) == 10000


def test_owner_is_not_charged(store):
    """사장님 본인(cid 0)은 자기 키를 쓰므로 과금 대상이 아니다."""
    assert mp._charge_clean(store, 0, 3) == 0
```

- [ ] **Step 2: 테스트가 실패하는지 확인**

```bash
py -m pytest shopping_shorts/tests/test_byok_vmake_charge.py -v
```

Expected: FAIL — `AttributeError: module 'shopping_shorts.mix_pipeline' has no attribute '_charge_clean'`

- [ ] **Step 3: `_vmake_key`를 keyroute 경유로 바꾸기**

`shopping_shorts/mix_pipeline.py:1185` 의 `_vmake_key`를 찾아 교체:

```python
def _vmake_key(store, customer_id=0):
    """자막제거에 쓸 키. 사용자가 등록했으면 그 키, 아니면 사장님 키.
    ★keyroute가 유일한 판단처다 — 여기서 따로 고르지 마라(0순위-B)."""
    from shopping_shorts import keyroute
    keys, _ = keyroute.keys_for(store, customer_id, "vmake")
    return keys[0] if keys else ""
```

- [ ] **Step 4: 과금 헬퍼 추가**

`shopping_shorts/mix_pipeline.py` — `_vmake_key` **바로 다음**에 추가:

```python
class NotEnoughPoints(Exception):
    """포인트가 모자라 시작조차 못 함. 반만 청소되는 것보다 아예 안 하는 게 낫다."""


def _charge_clean(store, customer_id, n_sources):
    """자막제거 선차감. 깎은 액수를 반환(0=무료). 모자라면 NotEnoughPoints.

    ★소스 개수만큼 곱한다 — VMake는 소스 1편당 1콜이다(_ensure_clean_sources).
      job당 1회로 계산하면 소스 3개짜리에서 1,000원을 손해 본다.
    """
    from shopping_shorts import keyroute, points, pricing
    if n_sources <= 0:
        return 0
    if not keyroute.should_charge(store, customer_id, "vmake"):
        return 0                                    # 내 키 → 무료
    need = pricing.cost(store, "vmake") * n_sources
    if not points.deduct(store, customer_id, need, "vmake"):
        raise NotEnoughPoints(
            f"포인트가 부족합니다 (필요 {pricing.to_display(need)}P, "
            f"보유 {pricing.to_display(points.balance(store, customer_id))}P)")
    return need


def _refund_clean(store, customer_id, amount):
    """청소 실패 시 돌려준다. ★과금한 만큼만 — 안 깎은 호출자까지 환불하면 원장이 갉힌다."""
    from shopping_shorts import points
    if amount > 0:
        points.refund(store, customer_id, amount, "vmake")
```

- [ ] **Step 5: 테스트 통과 확인**

```bash
py -m pytest shopping_shorts/tests/test_byok_vmake_charge.py -v
```

Expected: 6 passed

- [ ] **Step 6: `_ensure_clean_sources`에 차감 붙이기**

`shopping_shorts/mix_pipeline.py:1257` — `todo` 계산 **직후**에 차감을 넣는다.
기존 `if todo:` 줄 앞에 삽입:

```python
    # ★todo가 확정된 뒤에 과금한다 — 캐시된 소스는 VMake를 안 타니 돈도 안 나간다.
    #   라우트에선 소스 개수를 모르므로(큐에 넣기만 한다) 여기가 유일한 계량 지점이다.
    charged = _charge_clean(store, customer_id, len(todo))
    try:
        if todo:
```

그리고 기존 `if todo:` 블록 전체를 한 단계 들여쓰고, 블록 끝(`store.update_mix_job(...)` 다음)에 추가:

```python
    except Exception:
        _refund_clean(store, customer_id, charged)   # 실패했으면 돌려준다
        raise
```

`_ensure_clean_sources` 시그니처도 바꾼다:

```python
def _ensure_clean_sources(store, job, job_id, work, key, customer_id=0):
```

- [ ] **Step 7: `run_clean_sources`에 customer_id 전파**

`shopping_shorts/mix_pipeline.py:1314` — job에서 cid를 읽어 넘긴다.
`key = _vmake_key(store)` 줄을 교체:

```python
        # ★워커는 HTTP 요청이 없어 request.state가 없다 — job 레코드에서 읽는다.
        customer_id = job.get("customer_id") or 0
        key = _vmake_key(store, customer_id)
```

그리고 `_ensure_clean_sources` 호출을 교체:

```python
        clean_map = _ensure_clean_sources(store, job, job_id, work, key, customer_id)
```

포인트 부족은 사용자에게 그대로 보여준다. `except Exception as e:` 블록 **위**에 추가:

```python
    except NotEnoughPoints as e:
        store.update_mix_job(job_id, clean_status="failed", clean_error=str(e))
        return
```

- [ ] **Step 8: 기존 자막제거 테스트가 안 깨졌는지 확인**

```bash
py -m pytest shopping_shorts/tests/test_clean_sources.py shopping_shorts/tests/test_mix_render_flow.py -v
```

Expected: 통과. `_vmake_key`를 monkeypatch하는 기존 테스트(`test_mix_render_flow.py:57,81`)가 인자 1개로 부르므로 `customer_id=0` 기본값이 그걸 받아준다.

- [ ] **Step 9: 커밋**

```bash
git add shopping_shorts/mix_pipeline.py shopping_shorts/tests/test_byok_vmake_charge.py
git commit -m "fix: 자막제거 무과금 구멍 차단 — 소스 개수만큼 선차감·실패 시 환불"
```

---

## Task 7: 나머지 키 조달 경로를 keyroute로

TTS·YouTube를 사용자 키로 배선한다. 둘 다 이미 인자 우선 패턴이 있어 안전하다.

### ⚠️ 범위 축소 — Gemini 전환은 이번에 하지 않는다 (2026-08-17 실측 후 결정)

착수 전 실측한 숫자가 계획을 뒤집었다:

| 항목 | 계획서 가정 | **실측** |
|---|---|---|
| `_current_key_and_idx`/`_mark_key_exhausted` 호출부 | "13개 모듈" | **44곳 / 15개 파일** |
| `video_analysis.py` 한 파일만 | — | 11곳 |

**안 하는 이유:**

1. **44곳 중 대부분이 크론·배치다**(태깅·백필·카테고리). 이건 회사 부담이라
   애초에 사용자 키가 필요 없다 — 고쳐도 얻는 게 없다.
2. **소진표시 상태파일이 15개 파일의 공유 자산이다.** 사용자별로 쪼개면
   라운드로빈·자가복구(2026-08-07 `키소진표시_오탐` 사고의 수정본)가 통째로
   흔들린다. 그 로직은 "키 23개인데 1번만 때리던 버그"를 고친 것이라
   깨뜨리면 손해가 크다.
3. **포인트가 이미 비용을 회수한다.** 사용자 Gemini 키가 없어도 대본·렌즈는
   포인트로 차감되므로 사장님 손해가 없다. 즉 급하지 않다.

**이번에 하는 것**: 화면에서 Gemini 키를 **받아 저장**은 한다(Task 2·8).
**안 하는 것**: 저장된 Gemini 키를 실제 호출에 쓰는 전환.

→ 후속 작업으로 `handoff/키등록포인트.md`에 남긴다. 그때는 소진표시를
   `key_hash` 기반으로 바꾸는 작업(§7-1)이 선행돼야 한다.

**Files:**
- Modify: `shopping_shorts/tts.py:59,64`
- Modify: `shopping_shorts/youtube_client.py:128`
- Modify: `shopping_shorts/comment_gen.py:185`
- Create: `shopping_shorts/tests/test_byok_routes.py`

- [ ] **Step 1: 실패하는 테스트 작성**

Create `shopping_shorts/tests/test_byok_routes.py`:

```python
"""각 서비스가 사용자 키를 실제로 집어가는지."""
import importlib
import pytest
from shopping_shorts import keycrypt, keyroute
from shopping_shorts.store import Store

_KEY = "NZAowCs7o9LHVnJdZbxrVmYI7MHqyPFkydIUd1mc8To="   # 유효한 Fernet 키(44자). 테스트 전용


@pytest.fixture
def store(tmp_path, monkeypatch):
    monkeypatch.setenv("BYOK_MASTER_KEY", _KEY)
    importlib.reload(keycrypt)
    return Store(str(tmp_path / "t.db"))


def test_tts_uses_user_key(store, monkeypatch):
    monkeypatch.setattr(keyroute, "_owner_keys", lambda svc: ["사장님TTS"])
    store.add_customer_key(3, "elevenlabs", "내TTS")
    keys, is_user = keyroute.keys_for(store, 3, "elevenlabs")
    assert keys == ["내TTS"] and is_user is True


def test_youtube_uses_user_key(store, monkeypatch):
    monkeypatch.setattr(keyroute, "_owner_keys", lambda svc: ["사장님YT"])
    store.add_customer_key(3, "youtube", "내YT")
    keys, is_user = keyroute.keys_for(store, 3, "youtube")
    assert keys == ["내YT"] and is_user is True


def test_gemini_uses_user_keys(store, monkeypatch):
    monkeypatch.setattr(keyroute, "_owner_keys", lambda svc: ["사장님G1", "사장님G2"])
    store.add_customer_key(3, "gemini", "내G1")
    store.add_customer_key(3, "gemini", "내G2")
    keys, is_user = keyroute.keys_for(store, 3, "gemini")
    assert keys == ["내G1", "내G2"] and is_user is True
    assert "사장님G1" not in keys


def test_batch_uses_owner_keys(store, monkeypatch):
    """★크론·태거는 회사 부담이므로 cid 0으로 돌아 사장님 키를 쓴다."""
    monkeypatch.setattr(keyroute, "_owner_keys", lambda svc: ["사장님G1"])
    keys, is_user = keyroute.keys_for(store, 0, "gemini")
    assert keys == ["사장님G1"] and is_user is False
```

- [ ] **Step 2: 테스트가 실패하는지 확인**

```bash
py -m pytest shopping_shorts/tests/test_byok_routes.py -v
```

Expected: 4 passed (keyroute만 쓰므로 이미 통과 — 배선 확인용 회귀 테스트다)

- [ ] **Step 3: tts.py 배선**

`shopping_shorts/tts.py` — `synthesize_tts` 함수에 `customer_id=0` 인자를 추가하고,
`config.ELEVENLABS_API_KEY`를 직접 읽던 두 곳(`tts.py:59,64`)을 교체:

```python
    # ★키는 keyroute가 정한다 — config 직참조는 사용자 키를 무시한다.
    from shopping_shorts import keyroute
    from shopping_shorts.store import Store
    from shopping_shorts.config import DB_PATH
    _keys, _ = keyroute.keys_for(Store(DB_PATH), customer_id, "elevenlabs")
    api_key = _keys[0] if _keys else ""
    if not api_key:
        # 기존 동작 유지 — 키가 없으면 무음 mock
        ...
```

기존 `config.ELEVENLABS_API_KEY` 참조를 `api_key`로 바꾼다.

- [ ] **Step 4: youtube_client.py 배선**

`shopping_shorts/youtube_client.py:128` — 이미 `token` 인자 우선 패턴이 있다.
호출부에서 사용자 키를 넘기도록 `customer_id`를 받는 래퍼를 추가:

```python
def _tokens_for(customer_id=0):
    """유튜브 키 — 사용자가 등록했으면 그것만."""
    from shopping_shorts import keyroute
    from shopping_shorts.store import Store
    from shopping_shorts.config import DB_PATH
    keys, _ = keyroute.keys_for(Store(DB_PATH), customer_id, "youtube")
    return keys
```

`tokens = [token] if token else list(YOUTUBE_API_KEYS)` 를 교체:

```python
    tokens = [token] if token else _tokens_for(customer_id)
```

- [ ] **Step 5: comment_gen.py 소진표시를 key_hash 기반으로**

**★이번 작업 최대 함정.** `comment_gen.py:52,68`이 인덱스로 소진을 관리한다.
사용자별 풀이 생기면 "3번 소진"이 누구의 3번인지 모른다.

`_mark_key_exhausted`와 `_live_key_indices`를 키 지문 기반으로 바꾼다:

```python
def _mark_key_exhausted(idx, keys=None):
    """소진 표시. ★인덱스가 아니라 키 지문으로 남긴다 — 사용자별 풀이 섞이지 않게.
    (기존: state["exhausted"]=[0,2,5] → 지금: {"sha16": true})"""
    from shopping_shorts import keycrypt
    pool = keys if keys is not None else SHORTS_GEMINI_KEYS
    if idx >= len(pool):
        return
    fp = keycrypt.fingerprint(pool[idx])
    with _STATE_LOCK:
        state = _load_state()
        if fp not in state["exhausted"]:
            state["exhausted"].append(fp)
            _save_state(state)


def _live_key_indices(keys=None):
    """살아있는 키의 인덱스. 지문으로 대조하므로 풀이 달라도 안 섞인다."""
    from shopping_shorts import keycrypt
    pool = keys if keys is not None else SHORTS_GEMINI_KEYS
    exhausted = set(_load_state()["exhausted"])
    live = [i for i, k in enumerate(pool)
            if keycrypt.fingerprint(k) not in exhausted]
    scarce = len(live) <= max(1, int(len(pool) * _REVIVE_BELOW_RATIO))
    if scarce and exhausted and pool:
        revived = _recheck_exhausted_keys()
        if revived:
            live = sorted(set(live) | set(revived))
    return live
```

⚠️ 기존 상태파일에 인덱스 정수가 들어 있다. `_load_state`에서 정수를 걸러낸다:

```python
        data.setdefault("exhausted", [])
        # 구 형식(인덱스 정수)이 섞여 있으면 버린다 — 지문과 의미가 다르다.
        data["exhausted"] = [x for x in data["exhausted"] if isinstance(x, str)]
        return data
```

- [ ] **Step 6: 테스트 통과 확인**

```bash
py -m pytest shopping_shorts/tests/test_byok_routes.py -v
py -m pytest shopping_shorts/tests/ -k "gemini or comment or tts" -v
```

Expected: 새 테스트 4 passed, 기존 테스트 기준선 유지

- [ ] **Step 7: 커밋**

```bash
git add shopping_shorts/tts.py shopping_shorts/youtube_client.py shopping_shorts/comment_gen.py shopping_shorts/tests/test_byok_routes.py
git commit -m "feat: TTS·유튜브·제미나이를 keyroute 경유로 + 소진표시를 지문 기반으로"
```

---

## Task 8: 설정 API

**Files:**
- Modify: `shopping_shorts/app.py`
- Create: `shopping_shorts/tests/test_byok_api.py`

- [ ] **Step 1: 실패하는 테스트 작성**

Create `shopping_shorts/tests/test_byok_api.py`:

```python
"""설정 API — 키가 화면으로 새지 않는지가 핵심."""
import importlib
import pytest
from fastapi.testclient import TestClient

_KEY = "NZAowCs7o9LHVnJdZbxrVmYI7MHqyPFkydIUd1mc8To="   # 유효한 Fernet 키(44자). 테스트 전용


@pytest.fixture
def client(tmp_path, monkeypatch):
    monkeypatch.setenv("BYOK_MASTER_KEY", _KEY)
    monkeypatch.setenv("SHORTS_DB", str(tmp_path / "t.db"))
    from shopping_shorts import keycrypt
    importlib.reload(keycrypt)
    from shopping_shorts import app as appmod
    importlib.reload(appmod)
    return TestClient(appmod.app)


def test_get_keys_never_returns_plaintext(client):
    """★가장 중요 — 평문이 GET으로 나가면 등록 자체가 무의미하다."""
    client.post("/api/settings/keys", json={"service": "vmake", "key": "비밀키값"})
    r = client.get("/api/settings/keys")
    assert r.status_code == 200
    assert "비밀키값" not in r.text


def test_get_keys_returns_mask(client):
    client.post("/api/settings/keys",
                json={"service": "gemini", "key": "AQ.Ab8RNxxxxxxxxxxxxxxxxxxxxkz1MA"})
    rows = client.get("/api/settings/keys").json()["keys"]
    assert any("•" in k["label"] for k in rows)


def test_add_rejects_empty(client):
    r = client.post("/api/settings/keys", json={"service": "vmake", "key": "   "})
    assert r.status_code == 422


def test_add_rejects_unknown_service(client):
    """★모르는 서비스명을 그대로 받으면 테이블에 쓰레기가 쌓인다."""
    r = client.post("/api/settings/keys", json={"service": "해킹", "key": "k"})
    assert r.status_code == 422


def test_delete(client):
    client.post("/api/settings/keys", json={"service": "vmake", "key": "지울키"})
    kid = client.get("/api/settings/keys").json()["keys"][0]["id"]
    assert client.post("/api/settings/keys/delete", json={"id": kid}).status_code == 200
    assert client.get("/api/settings/keys").json()["keys"] == []


def test_points_shows_balance_and_prices(client):
    r = client.get("/api/settings/points")
    assert r.status_code == 200
    body = r.json()
    assert "balance" in body
    assert body["prices"]["vmake"] == 5      # 화면용 값(내부 500)


def test_gemini_accepts_multiple_at_once(client):
    """구글 키는 여러 개를 한 번에 붙여넣을 수 있어야 한다."""
    r = client.post("/api/settings/keys",
                    json={"service": "gemini", "key": "k1\nk2\nk3"})
    assert r.json()["added"] == 3
```

- [ ] **Step 2: 테스트가 실패하는지 확인**

```bash
py -m pytest shopping_shorts/tests/test_byok_api.py -v
```

Expected: FAIL — 404 (라우트 없음)

- [ ] **Step 3: API 라우트 추가**

`shopping_shorts/app.py` — 기존 `/api/settings/vmake_key` 라우트(`app.py:2838`) **바로 다음**에 추가:

```python
# ── 사용자 키 등록(2026-08-17, BYOK) ──
_BYOK_SERVICES = ("gemini", "vmake", "elevenlabs", "youtube")


@app.get("/api/settings/keys")
def api_settings_keys_list(request: Request):
    """등록된 키 목록. ★평문은 절대 안 실어 보낸다 — 마스킹된 label만."""
    from shopping_shorts import keycrypt
    cid = _cid(request)
    return {"ok": True, "enabled": keycrypt.enabled(),
            "keys": Store(DB_PATH).list_customer_keys(cid)}


@app.post("/api/settings/keys")
def api_settings_keys_add(request: Request, body: dict):
    """키 등록. 구글은 줄바꿈으로 여러 개를 한 번에 받는다."""
    from shopping_shorts import keycrypt
    if not keycrypt.enabled():
        return JSONResponse(status_code=503,
                            content={"ok": False, "error": "키 저장이 설정되지 않았습니다 (관리자 문의)"})
    service = (body.get("service") or "").strip()
    if service not in _BYOK_SERVICES:
        return JSONResponse(status_code=422, content={"ok": False, "error": "알 수 없는 서비스"})
    raw = (body.get("key") or "").strip()
    if not raw:
        return JSONResponse(status_code=422, content={"ok": False, "error": "키를 입력하세요"})
    # 구글만 여러 개(줄바꿈·쉼표·공백 구분). 나머지는 통째로 키 1개다(vmake는 'ak:sk' 형식).
    parts = ([p.strip() for p in raw.replace(",", "\n").split() if p.strip()]
             if service == "gemini" else [raw])
    store = Store(DB_PATH)
    cid = _cid(request)
    added = sum(1 for p in parts if store.add_customer_key(cid, service, p))
    return {"ok": True, "added": added, "keys": store.list_customer_keys(cid)}


@app.post("/api/settings/keys/delete")
def api_settings_keys_delete(request: Request, body: dict):
    store = Store(DB_PATH)
    cid = _cid(request)
    store.delete_customer_key(cid, body.get("id"))     # cid 조건이 들어가 남의 키는 못 지운다
    return {"ok": True, "keys": store.list_customer_keys(cid)}


@app.post("/api/settings/keys/verify")
def api_settings_keys_verify(request: Request, body: dict):
    """저장 직후 실제로 도는지 1회 호출해 본다.
    ★SDK models.list를 쓰지 마라 — 살아있는 키도 전부 실패로 나온다(comment_gen.py:95)."""
    from shopping_shorts import keyroute
    store = Store(DB_PATH)
    cid = _cid(request)
    service = (body.get("service") or "").strip()
    if service not in _BYOK_SERVICES:
        return JSONResponse(status_code=422, content={"ok": False, "error": "알 수 없는 서비스"})
    keys, is_user = keyroute.keys_for(store, cid, service)
    if not keys or not is_user:
        return {"ok": False, "error": "등록된 키가 없습니다"}
    results = []
    for row, key in zip(store.list_customer_keys(cid, service), keys):
        alive = _probe_user_key(service, key)
        store.set_customer_key_status(row["id"], "ok" if alive else "bad")
        results.append({"id": row["id"], "status": "ok" if alive else "bad"})
    return {"ok": True, "results": results,
            "keys": store.list_customer_keys(cid, service)}


def _probe_user_key(service, key):
    """키가 실제로 도는지 REST로 확인. 판정은 HTTP 응답으로만 한다."""
    if service == "gemini":
        from shopping_shorts.comment_gen import _probe_key_alive
        return _probe_key_alive(key)
    if service == "elevenlabs":
        import urllib.request
        req = urllib.request.Request("https://api.elevenlabs.io/v1/user",
                                     headers={"xi-api-key": key})
        try:
            with urllib.request.urlopen(req, timeout=15) as r:
                return r.status == 200
        except Exception:
            return False
    if service == "youtube":
        import urllib.request
        url = ("https://www.googleapis.com/youtube/v3/videos"
               f"?part=id&id=dQw4w9WgXcQ&key={key}")
        try:
            with urllib.request.urlopen(url, timeout=15) as r:
                return r.status == 200
        except Exception:
            return False
    if service == "vmake":
        return ":" in key      # 형식만 본다(ak:sk). 실호출은 크레딧을 먹는다
    return False


@app.get("/api/settings/points")
def api_settings_points(request: Request):
    """잔액·단가표·최근 사용내역. 화면이 이거 하나로 다 그린다."""
    from shopping_shorts import points, pricing
    store = Store(DB_PATH)
    cid = _cid(request)
    prices = {op: pricing.to_display(pricing.cost(store, op))
              for op in ("vmake", "mix", "tts", "lens", "script")}
    return {"ok": True,
            "balance": pricing.to_display(points.balance(store, cid)),
            "prices": prices,
            "history": [{"delta": pricing.to_display(h["delta"]),
                         "reason": h["reason"], "at": h["created_at"]}
                        for h in store.points_history(cid, 20)]}
```

- [ ] **Step 4: 테스트 통과 확인**

```bash
py -m pytest shopping_shorts/tests/test_byok_api.py -v
```

Expected: 7 passed

- [ ] **Step 5: 커밋**

```bash
git add shopping_shorts/app.py shopping_shorts/tests/test_byok_api.py
git commit -m "feat: 키 등록·검증·포인트 조회 API — 평문은 GET으로 안 나간다"
```

---

## Task 9: 설정 화면

확정: 탭 2개 · 포인트는 **근거형** · 키 탭은 **전부 펼침** · 민트색 · 시니어 타이포(15px)

**Files:**
- Create: `shopping_shorts/static/settings.html`
- Modify: `shopping_shorts/app.py` (라우트)
- Modify: `shopping_shorts/static/sidebar.js` (메뉴)
- Create: `shopping_shorts/tests/test_byok_ui.py`

- [ ] **Step 1: 실패하는 테스트 작성**

Create `shopping_shorts/tests/test_byok_ui.py`:

```python
"""설정 화면 — 참고 화면을 베끼지 않았는지, 우리 색을 쓰는지."""
from pathlib import Path

_HTML = Path(__file__).parent.parent / "static" / "settings.html"


def test_page_exists():
    assert _HTML.exists()


def test_uses_our_mint_not_reference_green():
    """★사장님 지시 '똑같이 하면안되' — 참고 화면의 연두색을 쓰면 안 된다."""
    txt = _HTML.read_text(encoding="utf-8")
    assert "--accent" in txt or "37e0bd" in txt
    assert "a3e635" not in txt.lower()      # 참고 화면 연두색
    assert "쇼핑팩토리" not in txt           # 참고 화면 브랜드


def test_has_two_tabs():
    txt = _HTML.read_text(encoding="utf-8")
    assert "포인트" in txt and "내 키 등록" in txt


def test_no_coupang_or_buffer_tab():
    """연동이 없는 서비스의 빈 탭을 만들지 않는다(설계 §2-2)."""
    txt = _HTML.read_text(encoding="utf-8")
    assert "쿠팡 파트너스" not in txt
    assert "Buffer" not in txt


def test_key_input_is_password_type():
    """키 입력칸이 평문으로 보이면 어깨너머로 샌다."""
    txt = _HTML.read_text(encoding="utf-8")
    assert 'type="password"' in txt


def test_explains_free_when_registered():
    """등록 유인이 화면에 있어야 한다."""
    txt = _HTML.read_text(encoding="utf-8")
    assert "무료" in txt or "0 P" in txt


def test_says_optional():
    """★60대 배려 — 등록을 안 해도 된다는 걸 알려야 한다."""
    txt = _HTML.read_text(encoding="utf-8")
    assert "안 하셔도" in txt or "않으셔도" in txt
```

- [ ] **Step 2: 테스트가 실패하는지 확인**

```bash
py -m pytest shopping_shorts/tests/test_byok_ui.py -v
```

Expected: FAIL — `assert _HTML.exists()`

- [ ] **Step 3: settings.html 작성**

Create `shopping_shorts/static/settings.html`.

기존 `produce.html`의 `:root` 변수 블록을 그대로 복사해 쓴다(색이 따로 놀면 안 된다).
구조는 설계 문서 §3의 목업을 그대로 옮긴다:

- 탭 2개: `💎 포인트` / `🔑 내 키 등록`
- 포인트 탭: 잔액 카드(민트 그라데이션) → "무엇에 얼마가 드나요" → 항목별 카드(자막제거 5P·영상제작 3P를 크게, 나머지 묶음) → 무료 항목 → 하단 고지 3줄
- 키 탭: 안내 배너("안 하셔도 포인트로 그냥 쓰실 수 있어요") → 서비스 카드 4개 전부 펼침 → 각 카드에 `type="password"` 입력칸 + 저장 + `▶ 받는 방법 영상`
- 데이터는 `/api/settings/points`·`/api/settings/keys` 두 번 호출로 채운다
- 저장 후 `/api/settings/keys/verify`를 불러 `✓ 정상` / `✗ 키가 틀렸습니다` 표시

`<script src="/static/sidebar.js"></script>`를 포함해 사이드바를 붙인다.

- [ ] **Step 4: 라우트 추가 — ★수동 라우트를 쓰지 마라**

이 프로젝트는 **클린 URL 자동 등록 루프**가 이미 있다(`app.py:10215`). 실측:

```python
for _pg in ("discover", "find", "library", "mix", "outreach", "produce", "collection",
            "scene_library", "pattern_bank", "longform"):
    app.add_api_route(
        f"/{_pg}",
        (lambda n=_pg: FileResponse(_STATIC / f"{n}.html", media_type="text/html",
                                    headers=_NOCACHE)),
        include_in_schema=False,
    )
```

**튜플에 `"settings"`를 넣기만 하면 된다.** `_NOCACHE` 헤더도 자동으로 붙는다
— 이건 "UI 고쳤는데 브라우저 캐시 때문에 안 바뀜"(2026-07-14 실사고)을 막는 장치라
수동 라우트를 따로 만들면 그 방어가 빠진다.

먼저 위치를 확인한다:
```bash
grep -n 'for _pg in (' shopping_shorts/app.py
```

그 튜플에 `"settings"`를 추가:
```python
for _pg in ("discover", "find", "library", "mix", "outreach", "produce", "collection",
            "scene_library", "pattern_bank", "longform", "settings"):
```

- [ ] **Step 5: 사이드바 메뉴 추가**

`shopping_shorts/static/sidebar.js` — `NAV` 배열(`sidebar.js:31`)에 그룹 추가:

```javascript
    { label: "설정", items: [
      { icon: "⚙️", text: "내 설정",  href: "/settings", free: true },
    ] },
```

`free: true`인 이유: 포인트를 충전하려면 무료 등급도 들어갈 수 있어야 한다.

- [ ] **Step 6: 테스트 통과 확인**

```bash
py -m pytest shopping_shorts/tests/test_byok_ui.py -v
```

Expected: 7 passed

- [ ] **Step 7: 실제 화면 확인 (필수)**

```bash
py -m uvicorn shopping_shorts.app:app --port 8899
```

브라우저에서 `http://localhost:8899/settings` 를 연다. 확인할 것:
- 탭 2개가 뜨고 전환되는가
- 잔액이 보이는가 (0P라도)
- 키 입력칸에 글자를 치면 `•`로 가려지는가
- 사이드바가 붙어 있는가

⚠️ **화면은 눈으로 봐야 한다.** 테스트 통과는 파일 내용만 본 것이다.

- [ ] **Step 8: 커밋**

```bash
git add shopping_shorts/static/settings.html shopping_shorts/app.py shopping_shorts/static/sidebar.js shopping_shorts/tests/test_byok_ui.py
git commit -m "feat: 설정 화면 — 포인트·키 등록 탭 2개"
```

---

## Task 10: 나머지 작업 차감 배선 + 일일 상한 조정

**Files:**
- Modify: `shopping_shorts/app.py` (렌즈·대본·믹스 차감)
- Create: `shopping_shorts/tests/test_byok_charge_wiring.py`

- [ ] **Step 1: 실패하는 테스트 작성**

Create `shopping_shorts/tests/test_byok_charge_wiring.py`:

```python
"""나머지 작업 과금 + 일일 상한 — 키가 있어도 상한은 걸린다."""
import importlib
import pytest
from shopping_shorts import keycrypt, keyroute, points, pricing
from shopping_shorts.store import Store

_KEY = "NZAowCs7o9LHVnJdZbxrVmYI7MHqyPFkydIUd1mc8To="   # 유효한 Fernet 키(44자). 테스트 전용


@pytest.fixture
def store(tmp_path, monkeypatch):
    monkeypatch.setenv("BYOK_MASTER_KEY", _KEY)
    importlib.reload(keycrypt)
    return Store(str(tmp_path / "t.db"))


def test_lens_charges_when_owner_key(store, monkeypatch):
    monkeypatch.setattr(keyroute, "_owner_keys", lambda svc: ["사장님키"])
    points.add(store, 4, 1000)
    assert keyroute.should_charge(store, 4, "gemini") is True


def test_script_free_with_user_key(store, monkeypatch):
    monkeypatch.setattr(keyroute, "_owner_keys", lambda svc: ["사장님키"])
    store.add_customer_key(4, "gemini", "내키")
    assert keyroute.should_charge(store, 4, "gemini") is False


def test_daily_limit_defaults_are_ten(store):
    """★사장님 지시: 본인 키가 있어도 영상제작·렌즈검색은 하루 10회."""
    from shopping_shorts.app import _CREDIT_DEFAULTS, _CREDIT_PRO_DEFAULTS
    assert _CREDIT_DEFAULTS["lens"] == 10
    assert _CREDIT_DEFAULTS["render"] == 10
    assert _CREDIT_PRO_DEFAULTS["lens"] == 10
    assert _CREDIT_PRO_DEFAULTS["render"] == 10
```

- [ ] **Step 2: 테스트가 실패하는지 확인**

```bash
py -m pytest shopping_shorts/tests/test_byok_charge_wiring.py -v
```

Expected: FAIL — `assert 5 == 10` (기본값이 아직 lens:5, render:2)

- [ ] **Step 3: 일일 상한 기본값 조정**

`_CREDIT_DEFAULTS` 위치를 grep으로 찾아라(실측 `app.py:6938`):

```python
# 사장님 지시(2026-08-17): 본인 키가 있어도 영상제작·렌즈검색은 하루 10회.
# 서버 보호 목적이라 포인트와 별개로 유지한다. admin에서 조정 가능.
_CREDIT_DEFAULTS = {"lens": 10, "render": 10, "script": 10}
_CREDIT_PRO_DEFAULTS = {"lens": 10, "render": 10, "script": 200}
```

- [ ] **Step 4~6: 차감 배선 — ★실측한 8곳**

계획서 초안의 줄번호는 전부 틀렸다. **2026-08-17 실측**:

| # | 라우트 | 줄 | 기존 op | 넣을 포인트 op |
|---|---|---|---|---|
| 1 | `/api/extract_script` | 1695 | script | `pricing.OP_SCRIPT` |
| 2 | `/api/produce/extract_from_url` | 1872 | script | `pricing.OP_SCRIPT` |
| 3 | `/api/mix/start` | 2646 | render | `pricing.OP_MIX` |
| 4 | `/api/mix/candidate/clone` | 2778 | render | `pricing.OP_MIX` |
| 5 | `/api/lens/search` | 4991 | lens | `pricing.OP_LENS` |
| 6 | `/api/lens/trace_url` | 5148 | lens | `pricing.OP_LENS` |
| 7 | `/api/produce/autoload` | 8385 | script | `pricing.OP_SCRIPT` |
| 8 | `/api/produce/mix/start` | 8681 | render | `pricing.OP_MIX` |

**★`prewarm.py:128`은 제외한다.** 예열은 사장님 배치라 사용자에게 과금하면 안 된다.

각 지점의 `check_and_count(...)` **직후**에 같은 모양을 넣는다(op만 다름):

```python
    # 포인트 차감(2026-08-17) — 일일 상한과 별개다. 내 키를 쓰면 안 깎인다.
    _denied = _charge_or_402(cid, pricing.OP_LENS, keyroute.SVC_GEMINI)
    if _denied:
        return _denied
```

**★같은 코드를 8번 적지 마라(0순위-B).** 헬퍼를 하나 만들어 쓴다:

```python
def _charge_or_402(customer_id, op, service):
    """포인트를 깎는다. 부족하면 402 응답을 반환(호출부가 그대로 return).
    깎을 필요가 없으면(내 키 사용) None.

    ★차감 여부는 keyroute가 정한다 — 여기서 따로 판단하면 어긋난다."""
    store = Store(DB_PATH)
    if not keyroute.should_charge(store, customer_id, service):
        return None
    need = pricing.cost(store, op)
    if points.deduct(store, customer_id, need, op):
        return None
    return JSONResponse(status_code=402, content={
        "ok": False,
        "error": f"포인트가 부족합니다 (필요 {pricing.to_display(need)}P, "
                 f"보유 {pricing.to_display(points.balance(store, customer_id))}P)"})
```

**service 매핑**: 대본·렌즈는 Gemini/SerpApi를 쓰므로 `SVC_GEMINI`,
영상제작(mix)은 Gemini+TTS 둘 다 쓰지만 **대표로 `SVC_GEMINI`** 를 본다
(둘 다 등록해야 무료로 하려면 판단이 복잡해진다 — 지금은 단순하게 간다).

- [ ] **Step 7: 테스트 통과 확인**

```bash
py -m pytest shopping_shorts/tests/test_byok_charge_wiring.py -v
py -m pytest shopping_shorts/tests/test_paywall_usage.py shopping_shorts/tests/test_paywall_gate.py -v
```

Expected: 새 테스트 3 passed, 기존 페이월 테스트 기준선 유지

- [ ] **Step 8: 커밋**

```bash
git add shopping_shorts/app.py shopping_shorts/tests/test_byok_charge_wiring.py
git commit -m "feat: 렌즈·대본·영상제작 포인트 차감 + 일일 상한 10회로"
```

---

## Task 11: 전체 검증 및 병합

- [ ] **Step 1: 새 테스트 전체 실행**

```bash
py -m pytest shopping_shorts/tests/test_byok_*.py -v
```

Expected: 전부 통과 (약 60개)

- [ ] **Step 2: 기준선 대조**

```bash
py -m pytest shopping_shorts/tests/ 2>&1 | tail -20
```

작업 **전** 실패 개수와 비교한다. **늘지 않았으면 통과**다.
⚠️ "전부 green"을 기대하지 마라 — 기준선에 이미 실패가 있다(apify·mix_pipeline 등 키·네트워크 없어 원래 깨짐).

- [ ] **Step 3: 서버 환경변수 안내**

`BYOK_MASTER_KEY`를 서버에 넣어야 키 등록이 켜진다:

```bash
py -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
```

출력값을 서버 `/etc/shopping-shorts.env`에 추가:

```
BYOK_MASTER_KEY=<위에서 나온 값>
```

⚠️ **이 값을 잃으면 등록된 키를 전부 복호할 수 없다.** 따로 보관할 것.
⚠️ git에 커밋하지 마라.

- [ ] **Step 4: 핸드오프 기록**

Create `handoff/키등록포인트.md`:

```markdown
# 키등록포인트 — handoff

**2026-08-17** · 트랙 `키등록포인트`

## 목적
사용자가 자기 API 키를 등록하면 무료, 안 하면 포인트 차감.
"60대는 그런 거 못하니 그냥 비용 내고 쓰게" — 두 갈래를 다 연다.

## 확정 정책
- 1P = 100원, 마진 0
- 자막제거 5P (VMake 실비용 500원/편, 사장님 확인)
- 폴백 없음 — 사용자 키가 있으면 그것만
- 일일 상한 유지 — 키가 있어도 영상제작·렌즈 하루 10회
- 회원 0명 상태에서 시작 → 처음부터 차감 켜고 출시

## ⏭ 다음
1. **영상제작·TTS 실단가 확정** — 지금은 임시값(3P·1P).
   `py tools/usage_report.py`로 실측 후 admin `point_cost_mix`·`point_cost_tts` 조정
2. **키 받는 방법 영상 촬영** — 화면의 `▶ 받는 방법 영상` 링크가 비어 있다
3. **초기 지급 포인트** 결정
4. 쿠팡·Buffer 연동 후 3번째 탭

## 함정
- Gemini 소진표시를 인덱스 → key_hash로 바꿨다. 구 형식 정수는 _load_state가 거른다
- `BYOK_MASTER_KEY` 잃으면 등록된 키 전부 복호 불가
- 자막제거는 소스 1편당 VMake 1콜 — 차감도 소스 개수만큼
```

Create `wiki/log.d/키등록포인트.md`:

```markdown
## 2026-08-17 — 사용자 키 등록(BYOK) + 포인트 차감
자기 키 등록하면 무료, 안 하면 포인트. keyroute가 키 선택과 과금을 함께 정해
어긋나지 않게 했다. 자막제거 무과금 구멍(소스당 500원)을 막은 게 제일 큰 건.
단가는 admin 설정이라 실측 후 배포 없이 조정 가능.
```

- [ ] **Step 5: 커밋 및 병합**

```bash
git add handoff/키등록포인트.md wiki/log.d/키등록포인트.md
git commit -m "docs: 키등록포인트 핸드오프"
```

main 폴더에서:

```bash
py tools/track.py finish 키등록포인트
```

⚠️ 게이트가 막으면 라이브는 무사하다. 고치고 다시 `finish`.

---

## 자체 점검

**설계 커버리지**

| 설계 항목 | Task |
|---|---|
| §4-2 암호화 유틸 | 1 |
| §4-1 customer_keys | 2 |
| §2-3 단가 | 3 |
| §5-1 keyroute 단일 진입점 | 4 |
| §4-3 포인트 확장 | 5 |
| §6-2 자막제거 구멍 | 6 |
| §5-2 키 조달 6곳 | 6, 7 |
| §7-1 인덱스 함정 | 7 |
| §3 화면 | 9 |
| §6-1 차감 지점 | 6, 10 |
| §6-4 일일 상한 | 10 |
| §8 테스트 | 전 태스크 |

**범위 밖 확인** — 쿠팡·Buffer·Typecast·Vertex·자동결제는 어느 태스크에도 없다(설계 §9와 일치).

**미해결로 남기는 것** — 영상제작·TTS 단가는 실측 없이 임시값이다. Task 11 §4에 후속으로 명시했고, admin 설정이라 배포 없이 고칠 수 있다.
