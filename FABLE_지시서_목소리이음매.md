# Fable 지시서 — 통짜 합성 호흡 뭉갬 수정 (2026-09-06 인계)

사장님 자리비움. **끝까지 진행할 것.**

## 지금 상태 (건드리지 마라 — 이미 안전하다)

```
TTS_JOINED   삭제됨 (off)  · 워커 12/12 꺼짐 확인 · 롤백 후 통짜 로그 0건
서비스       active        · 백업 /etc/shopping-shorts.env.bak.20260906
코드         무변경         · audio_post.py 원본과 동일 확인
```

## 할 일 — `_TAIL_PAD` 값을 실측으로 정한다

**원인은 이미 특정됐다. 다시 조사하지 마라.**

`shopping_shorts/tts_joined.py:177`
```python
_TAIL_PAD = 0.08   # ← 이것이 문장 사이 호흡을 뭉갠다
```

발화 경계에서 뒤 0.08초만 남기고 잘라 **문장이 쉼 없이 맞붙는다** → 기관총처럼 튄다.

**고객 실물 실측 (칸 사이 쉼 = 앞칸 끝무음 + 뒷칸 앞무음):**

| 방식 | 칸사이 쉼 |
|---|---|
| 통짜 4건 평균 | **25 ms** (최악 `2d591a0bfd40` = 3 ms) |
| 비트별 3건 평균 | **60 ms** ← 목표 |

**목표: 통짜도 60 ms가 나오는 `_TAIL_PAD` 값을 찾는다.**
`_HEAD_PAD`(0.03)도 같이 봐라 — 쉼은 앞칸 끝 + 뒷칸 앞의 **합**이다.

실측 스크립트: 서버 `/tmp/find_pad.py` (PAD 0.08~0.32를 실제로 잘라 쉼을 잰다)
⚠️ 값을 추측하지 마라. 실제로 잘라서 재라.

## ★검증 조건 (이번엔 반드시 — 내가 여기서 틀렸다)

- [ ] **고객 실제 작업물**로 판정. 내가 만든 샘플로 판정 금지
- [ ] **칸 사이 쉼(ms)** 을 반드시 잴 것 — **음량 dB만으로는 이 병을 못 본다**
- [ ] 비트별 기준선 **60 ms**와 대조
- [ ] **사장님 청취 확인 뒤에** 켤 것. 그 전엔 켜지 마라

### 내가 틀린 이유 (같은 함정 반복 금지)

"이음매 튐 74% 감소"로 오보했다. 쓴 지표 3개가 전부 **음량 차이(dB)** 였다:
- A) 이음매 레벨 점프 10.12 → 2.59 dB (-74%)
- B) EBU 라우드니스 출렁임 0.75 → 0.33 dB (-56%)
- C) 절대 파형 불연속 17074 → 21462 (**+26%, 유일하게 악화**)

진짜 증상은 **"쉼이 사라진 것" = 시간축**이라 A·B에 안 잡혔다.
**오히려 쉼이 없어서 A가 좋아 보였다**(무음이 없으면 레벨차가 안 생긴다).
C가 혼자 악화를 가리켰는데 "억양이 살아난 것"으로 편하게 해석하고 넘겼다.

→ **혼자 반대 방향을 가리키는 지표를 편한 쪽으로 해석하지 마라.**

## 막다른 길 — 다시 시도하지 마라

**무음컷 크로스페이드는 효과 0이다.** 같은 날 두 번 시도해 둘 다 실패했다:

1. `silenceremove`에 `window=0.02` → **md5 바이트까지 동일**.
   그게 이미 기본값이고, 애초에 크로스페이드가 아니라 **무음 판정 평활화** 값이다(문서 오독).
2. 직접 `acrossfade` 구현 → 프로토타입 "55% 개선"이 **지표 오염**이었다.
   평균 정규화를 빼고 절대값으로 재니 14522 vs 14494 = **0.2%, 사실상 동일**.

**결정적 근거**: 원본 음성의 가장 큰 점프 200개 중 **컷 근처 0개(0%)**.
전부 말소리 한복판이고, 후처리 전 ElevenLabs 원본에 이미 35~37배 스파이크가 있다.
→ 클릭음은 우리 코드가 만드는 게 아니다.

## 참고 — 통짜 자체는 값어치가 있다

톤·음색 일관성은 실측으로 확인됐다(칸마다 `[curious]`/`[warm]`/`[excited]` 태그가
달라 비트별은 톤이 계단처럼 바뀐다). **버리지 말고 호흡만 살리면 된다.**

## 피해 고객 (사장님 판단 대기)

전부 `ready_for_review` — 재제작하면 정상 음성이 나온다.

| 시각 | job | customer_id |
|---|---|---|
| 17:01 | 9963b40329a9 | 113 |
| 17:13 | 2d591a0bfd40 | 113 |
| 17:16 | c273b9a05b91 | 201 |
| 17:23 | 81a783c2eeac | 359 |
| 17:14 | (8비트 331자, job 미확인 — 로그로 찾아라) | ? |

## 트랙

`.tracks/목소리이음매` / 브랜치 `track/목소리이음매`
상세 기록: `handoff/목소리이음매.md` 맨 아래 "🔴 2026-09-06" 절
