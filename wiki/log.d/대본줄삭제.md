# 대본줄삭제

## 2026-08-22
- 2단계 결과 대본에 **[🗑 빼기]** 신설 — 종전엔 글자를 전부 지워 빈 칸으로 두는 방법뿐이라
  화면에 빈 행이 남아 지워졌는지 알 수 없었다. 라이브(`c835e04a9` → main `75ab9daaf`).
- 조사: **3단계(장면 편집)엔 칸 삭제가 아예 없다**(비우기=담긴 영상만 뺀다). 정공법은 2단계에서 빼기.
- 함정: `s2Beats`는 `dr.beats`가 비면 **파생 배열**을 준다 → splice해도 안 남는다(옛 생성기 경로).
  지우기 전에 `dr.beats`로 못박는다. 실브라우저 검증 + pytest 321 passed.
