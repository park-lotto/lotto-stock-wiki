# 부품은행 (대본 학습층) — 핸드오프

**트랙**: `부품은행` / branch `track/부품은행`
**설계**: `docs/superpowers/specs/2026-07-21-대본학습층-부품은행-스파인라이브러리-design.md`(코어)
**검토**: `docs/superpowers/reviews/2026-07-21-대본학습층-페이블검토.md`
**계획**: `docs/superpowers/plans/2026-07-21-부품은행-Phase0-plan.md`(완료·라이브) / `...-Phase1-plan.md`(이번 세션)

---

## 2026-07-23 (집PC — 숏템메이커 UI 리뉴얼 브레인스토밍, 이 트랙에 임시 탑승)

**⏭ 미결**: `docs/superpowers/specs/2026-07-23-숏템메이커-UI리뉴얼-design.md` 의 "미결" 섹션 그대로.
회사 PC에서 **"숏템메이커 리뉴얼 이어서"** 로 재개 — 스펙+목업(`assets-숏템메이커목업/`) 읽고
①분석 연출 A/B/둘다 결정 ②빈 상태 화면 ③스펙 확정 후 writing-plans → 전용 트랙 분리 검토.
그 외 이 세션 라이브 반영: 제작소 문구 정리(뽑기→대본·스마트믹스 힌트 삭제·매칭완료 스피너 done, 7d156f09c).

---

## 2026-07-21 (Phase 1 구현 — 트랙 커밋, 아직 finish 전)

**Phase 1 = 4서브시스템, 10태스크 전부 TDD 구현·커밋 완료.** 각 태스크 개별 green.

| 태스크 | 내용 | 파일 | 상태 |
|---|---|---|---|
| T1 | F1 행위 동사 통제어휘 사전 | `action_dict.py` | ✅ 3/3 |
| T2 | 세그먼트 action 태깅(추출 enum + 결정적 폴백 + 인벤토리 노출) | `script_extract.py`·`edit_plan.py` | ✅ 5/5 |
| T3 | perf/spine 저장·조회 store 메서드 | `store.py` | ✅ 5/5 |
| T4+T6 | perf_score 모듈(perf_from_item R3매트릭스 + 로그백분위×최근성 A2) + ingest_script perf배관 | `perf_score.py`·`pattern_bank.py` | ✅ 6/6 |
| T5 | 야간 자동크롤 결합(우승작×대본 조인 자동흡수) | `daily_batch.py` | ✅ |
| T7 | 야간 부품 perf_score 재계산(R4 클린 소스만) | `daily_batch.py` | ✅ |
| T8 | 스파인 클러스터링(enum힌트 `__NEW__` 센티넬, A1) | `spine_cluster.py` | ✅ 3/3 |
| T9 | 야간 스파인 반영+승격게이트(신규 pending생성·source_count 재집계, 자동승인 금지) | `daily_batch.py` | ✅ |
| T10 | daily_batch.run 4스텝 배선 + import 게이트 | `daily_batch.py` | ✅ 5/5(통합) |

**핵심 설계 반영**:
- **[R4]** 통계·perf·fit_categories는 `category_source IN ('user','gemini')`만 — keyword 추측(54%정확) 제외. T5·T7·T9에 배선.
- **[A1]** 스파인 승격 게이트 = `source_count>=3 AND status='approved'`. 야간배치는 **절대 자동승인 안 함**(pending만 생성). 부품 MIN_SAMPLES=1과 분리.
- **[A2]** perf_score = 플랫폼내 로그백분위 × 최근성감쇠 + views/followers 보정. 상수 모듈 상단 노출.
- **[R3]** perf 필드별 nullable. saves=전무·followers=인스타전용·댓글=도우인/샤오홍슈 없음. perf_from_item이 0 지어내지 않고 None.
- 모든 Gemini 콜은 `call=`/`ingest_fn=` 주입점 — 테스트는 실 Gemini 안 침.

**검증 근거**(이번 세션 tool 관측):
- 신규 테스트 전부 green(위 표). import 게이트 `import shopping_shorts.app` OK.
- edit_plan_build 2건 실패는 **기준선**(pristine HEAD에서도 동일 실패, plagiarism_flags — 내 변경 무관, 실측 확인).
- ⏳ 전체 회귀(1700+) 백그라운드 실행 중 — finish 전 신규 실패 0 확정 필요.

**아키텍처**: 새 배치 스텝은 전부 `daily_batch.run()` 순차호출(기존 관례). 크론 = `python -m shopping_shorts.daily_batch`(서버측 crontab, repo엔 스케줄러 없음).

---

## 2026-07-21 (추가 증분 — 큐레이션 UI + 대화체 스코어러)

Phase 1 라이브 후 이어서 저위험 추가 증분 2건 구현·커밋(finish 병합 중):
- **큐레이션 UI**(`app.py`+`pattern_bank.html`): `POST /api/pattern/spine/status`(승인/기각) 신규. 스파인 카드에 **승격게이트 시각화 🟢(approved+source≥3=생성가능)/🟡(소스충분·승인대기)/🔴(소스부족)** + perf 배지. 인간 게이트가 이제 화면에서 실행됨. node --check로 JS 2블록 문법 검증. (test_app_spine_status 3/3)
- **대화체 스코어러**(`tone_score.py`, A3): 규칙기반 1차(무과금) — 문어체 종결어미(narration_naturalize._SPOKEN_MAP 재사용)+AI냄새(F5 시드)+어미 다양성, 승인 코퍼스 어미 매칭 보너스, 경계선(0.4~0.7)만 Gemini 재판(call 주입). Phase2 부품이나 독립 순수모듈로 선구현. (test_tone_score 6/6)

## 2026-07-21 (Phase 2 시작 — 생성이 은행에서 조립, 기본 off)

★방향 재정렬: 진짜 목표=믹스+대본 자연스럽게=**백본-인터리브**([[feedback_anchor_to_real_goal_backbone_interleave]]). 부품은행은 그 토대. 사장님 "부품은행도 계속"으로 Phase 2 진행.

계획: `docs/superpowers/plans/2026-07-21-부품은행-Phase2-plan.md` (PART A 토대=위험0 / PART B 생성배선=고위험).
- **T1 pick_spine_for_category**(store): 승격게이트(approved+source≥3+fit_categories) 강제 스파인 선택자. (3/3)
- **T2 bank_assemble.py**: spine_charter+parts_block(top-k perf)+assemble_bank_context. 중괄호 소독(.format 안전). 은행 비면 ''→회귀0. (6/6)
- **T3 script_generate 주입**: `generate_variations`/`generate_mix`에 `bank_context=""`(기본 off). `_GEN_PROMPT`/`_MIX_PROMPT`에 `{bank}` 자리. 빈값이면 프롬프트 무변화(회귀0, 가짜Gemini로 실측). 브레이스 가드 테스트에 bank="" 추가. (3/3 + 기존 45 green)
- **T7 app 배선**: `POST /api/wiki/generate`가 `use_bank`(기본 False) 받아 assemble_bank_context 호출→generate_variations에 전달. (2/2)

**전체 연결 완성(기본 off)**: 승인부품·스파인 → assemble → use_bank=True일 때만 대본생성 프롬프트에 실림 → 자연스러운 대본. **켜기 전엔 라이브 생성 무변화.** 실 Gemini 산출은 서버 관측이 최종증명(mock테스트라).

**아직 안 한 것**: T4(대화체 스코어러를 _verify_and_fix 재생성루프 cap-2에 배선)·T5(B4 표절가드 우회)·T6(F4 골든셋)·produce/mix 라우트 use_bank·UI 토글. use_bank를 실제로 켜려면 UI 토글 + 승인부품이 쌓여야 함.

## 2026-07-21 (★백본-인터리브 정립 — 방향 확정, 흔들리지 말 것)

**진짜 목표 = 믹스+대본 자연스럽게 = 백본-인터리브**(오푸스 스펙 §8-2·8-3·8-6, `2026-07-21-대본학습층-부품은행-스파인라이브러리-design.md` + 응용 스펙). 부품은행은 그 루프의 "대본 변주" 재료.

**★핵심 오해 정정**: "화면 갈아끼운 뒤 대본을 화면에 재-fit"이 **아니다**(내가 2번 틀림). 정답 = **행위(action)가 화면·대본 공통 못**:
1. 백본 영상 1개 = 비트별 **행위 순서를 스파인으로 고정**(재배열 금지), 나레이션은 원래 제 화면과 맞아 싱크 상속.
2. 화면 = 각 비트 **그 행위의 클립을 전 소스 풀 best-of-N**으로(같은 행위·다른 픽셀=차별화 1층).
3. 대본 = 백본 나레이션 가져와 **부품은행으로 말투·POV·훅만 변주, 행위 지목은 고정** → 화면과 자동 매칭.
→ 행위 못 덕에 **재-fit 루프 불필요**. F1 행위사전이 토대인 이유.

**착수 게이트 = A7 스파이크**: 실제 영상의 비트 행위 태깅 → 서브 풀 커버율 실측. 커버 안 되면 best-of-N 스왑 불가.

**구현(트랙 커밋)**:
- **A7 엔진 `backbone.py`**: `coverage(backbone, pool)`=행위 커버율(covered/uncovered/pct) · `pick_clips_for_action(action, pool, exclude_video)`=best-of-N 행위 스왑 · `segment_action`(저장태그→F1폴백) · `action_pool`. 순수함수, F1 위. (test_backbone 5/5)

**⏭ 다음 (백본-인터리브)**:
1. A7 커버율을 **실제 사장님 감자영상 3개**로 재기 — 그 영상들 추출 세그먼트(server DB script_extracts) 필요. 앱 엔드포인트(basket 소스→backbone 지정→coverage 리포트)로 화면에서 보이게.
2. `edit_plan.build_scene_first_plan`에 **`backbone_source` 옵션** 배선: 백본=원순서 행위 스파인(재배열 금지), 나머지=행위 스왑 풀. `_ground_candidate`/respine 대신 백본 순서 사용.
3. 대본 변주(부품은행)를 비트 행위 고정 하에 결합. 차별화 게이트(화면중첩·POV·훅).

## 2026-07-21 (★핑퐁 확정 + 실데이터 증명)

**진짜 목표 재확정(사장님 명료화, 흔들리지 말 것)**: 페이블과 정한 그대로 = **핑퐁(리컨사일/콘폼 루프)**. 새 발견 아님. 3영상 장면조각화(장면라이브러리)→제일 나은 영상=백본 스토리라인→**붙이기 전 '대본 쓰기↔장면 자르기' 왕복하며 서로맞춤+대본 품질↑**→붙이고 렌더. 기존 `_reconcile_weak_beats`(edit_plan.py:598)가 이 씨앗(한방향·1패스·fit≤3만).

**실데이터 증명(서버 reference.db, banana job 4a6f2208ebbb)**: fit이 거짓말. 비트2 "바나나 썰어 넣고"(자르다) ↔ 화면 "팬케이크 뒤집는"(뒤집다)이 **fit5**. 내 엔진 `beat_action_mismatch`가 이걸 MISMATCH로 정확히 잡음(비트1/3/4는 말에 손동작 없어 보류=오탐 안냄, 그건 순서문제→백본순서로). ⚠️실 DB=`data/reference.db`(app.db는 빈 것). 서버엔 sqlite3 CLI 없음→python3 sqlite3 모듈로.

**핑퐁 조각 구현(`backbone.py`, test_backbone 12개)**: `coverage`(A7 커버율)·`pick_clips_for_action`(장면 스왑)·`beat_action_mismatch`(fit 거짓말 잡기)·`reconcile_beat_by_action`(스왑 or 재작성 분기)·`segment_action`(저장태그 신뢰). action_dict에 요리행위(뒤집다·굽다·볶다·끓이다·뿌리다·올리다) 추가. **아직 트랙 커밋만, 라이브 아님.**

**⏭ 남은 것(핑퐁 루프 조립+배선)**: ①조각들을 **반복 루프**로 — 대본↔장면 왕복, 매 회 action 재판정+장면스왑, 못하면 나레이션 재작성(_reconcile 확장), 대본 품질게이트(tone_score+부품은행) ②백본 순서 고정(비트1/3/4 순서문제) — `build_scene_first_plan`에 backbone_source ③실 믹스 경로(mix_pipeline._plan_and_tts→build_scene_first_plan) opt-in 배선 ④finish. F1 사전 커버 넓히기(요리 외 상품군).

## 2026-07-21 (★핑퐁 완성 — 조립+배선+스위치)

핑퐁 루프 완성·배선(기본 off·회귀0):
- **`backbone.ping_pong_reconcile(beats, pool, rewrite_call, max_rounds)`**: 대본↔장면 왕복. 매 라운드 action 불일치 비트(fit 안 믿음) → 같은 행위 클립 있으면 **화면 스왑**, 없으면 **나레이션 재작성**(rewrite_call). (test_backbone 12)
- **배선**: `edit_plan.build_scene_first_plan(ping_pong=False opt-in)` + `_bb_rewrite`(재작성 콜백, _RECONCILE_SCHEMA 재사용) → `mix_pipeline._plan_and_tts(ping_pong=)` 관통. (test_scene_first_pingpong 2)
- **스위치**: 스키마 컬럼 없이 **전역 설정** `ping_pong_enabled='1'`로 on(mix_pipeline caller가 `store.get_setting`으로 읽음). positional row 인덱스 안 건드림.
- 신규 회귀 0(기준선: edit_plan_build 2·mix_pipeline happy_path 1 = 전부 pristine HEAD서도 실패, 내 변경 무관). import OK.

**켜는 법**: 서버/DB에서 `store.set_setting('ping_pong_enabled','1')` (또는 settings 테이블 직접). scene_first 믹스일 때만 작동. 끄면 `'0'`/삭제.

**⏭ 남은 것**: ①실 Gemini로 서버 관측(mock테스트라 실동작 미검증 — ping_pong_enabled 켜고 바나나 재믹스→비트2 화면이 자르는 클립으로 바뀌나) ②백본 순서 고정(비트1/3/4 순서문제=backbone_source, 아직 미배선) ③UI 토글(전역설정→제작소 버튼) ④F1 사전 상품군 확장 ⑤대본 품질게이트(tone_score) 핑퐁에 결합(현재 화면·대본 매칭만, 품질↑는 부품주입 use_bank 별도).

## 2026-07-21 (백본+대본품질+길이맞춤 3부 완성)

★핑퐁 라이브 작동 확인(job 332c4bc4b189, 비트2 action_fixed 스왑) 후, 사장님 지시로 3부 추가(전부 근거=페이블 §8-2·8-7 D14·D13):
- **A 길이맞춤**(`backbone.py`): narration_seconds(음절/5.7)·clip_seconds·length_status(over/under/ok)·fill_clips_to_cover. 핑퐁에 결합=스왑 후 화면이 대사보다 짧으면 같은 행위 클립 더 붙여 채움(대사 중 화면 끊김=넘어감 방지).
- **B 재미강도**(`tone_score.fun_intensity`, D14): 비포애프터·반전·극적대비·손실회피 강한장치 판정, 없으면 needs_regen. 생성 `_verify_and_fix`에 결합=초안에 fun/tone 주석+품질순 정렬(추천안=최고품질).
- **C 백본순서**(`backbone.py`): pick_backbone(최다 세그먼트 영상)·order_by_backbone(백본 시간순으로 movable body 화면 재배치, 꼬리·action_fixed·비백본화면 제외). build_scene_first_plan 핑퐁 블록에 결합(핑퐁 후 순서고정). '결과 말하는데 조리중간 화면'류 순서어긋남 잡음.
- 135 passed(영향영역), import OK. 다 ping_pong opt-in 안(전역스위치)이라 켰을 때만 작동.

## 2026-07-21 (백본 선정규칙 + 반복/편중 해소)

- **반복·편중 진단**(job 9c72·9612 서버 실측): 소스부족 아님. s1 편중(16중12~15)·s2=0·seg 반복. dedup은 반복만 고침, "s2 아예 안씀"은 Gemini 선택편중이라 못잡음 → **서브 의무삽입 필요(미구현)**.
- **dedup_and_balance**(라이브): 쓴 seg_id 재사용금지+덜쓴 소스 우선 교체. 핑퐁 경로 결합.
- **백본 선정규칙**(사장님 확정): forced(UI지정) 우선 → **인스타/유튜브(한글대본)만 백본**, 샤오홍슈·도우인=서브전용 → 댓글수 최다(세그 tiebreak). `pick_backbone(sources, meta, forced)`+`platform_of(url)`. **파이프라인 배선완료**: mix job urls_json→`_backbone_meta_from_job`→플랫폼 자동판별. `backbone_forced=job.get('backbone_main')` 통로 준비(UI 토글이 채울 자리).
- **백본-베이스 코어 커밋**(미배선): backbone_flow·scene_inventory·generate_backbone_script(흐름따라 100% 우리대본, 있는 장면만). 확정스펙 `docs/superpowers/specs/2026-07-21-백본베이스-확정스펙.md`.

## 2026-07-22 (퀄리티개선 P0 — 훅 로테이션 + 믹스 은행배선 + novelty, 트랙 커밋·미finish)

사장님 제보 "영상믹스 결과물 거의 반복" + "훅 8개뿐 단조로움" → 전면진단 후 P0 3종 TDD 구현.
진단·개선방향 브리프: `docs/superpowers/specs/2026-07-21-퀄리티개선-진단과-개선방향.md`(★실행지도, P0~P2).
**반복의 뿌리 3가지(코드근거)**: ①훅이 헌장에 하드코딩돼 매번 동일주입 ②믹스(scene_first)가
은행을 한 글자도 안 읽음 ③job 간 "최근 뭘 썼나" 기억 전무.

| P0 | 내용 | 파일 | 상태 |
|---|---|---|---|
| P0-1 | 훅 로테이션 — top-k 고정 → 상위 perf 풀에서 랜덤샘플(`_sample_bucket`) | `bank_assemble.py` | ✅ |
| P0-2 | 믹스 경로 은행 배선 — `_scene_first_candidates`/`build_scene_first_plan`에 bank_context 관통, `_plan_and_tts`가 `bank_enabled=1`일 때 조립·전달 | `edit_plan.py`·`mix_pipeline.py` | ✅ |
| P0-3 | novelty 메모리 — `script_usage` 테이블+record/recent, `avoid_block`(최근 훅·인물·CTA "다르게"), 채택후보 story 항상 기록+회피블록 주입 | `store.py`·`bank_assemble.py`·`mix_pipeline.py` | ✅ |

- 신규 테스트 22개(test_mix_bank_wiring 11 + test_novelty_memory 11) 전부 green.
- 전체회귀 신규실패 0 — **9 baseline만**(edit_plan_build×2·mix happy_path×1·apify cp949×6, 전부 pristine에서 stash로 동일 실패 실측). import OK.
- **경로 검증**: 제작소 "우리 시스템으로 믹스"는 항상 `scene_first=True`(app.py:4310) → 반복나던 그 경로가 배선한 분기임 확정.

**★라이브로 가려면(다 gated off·미finish)**:
1. `py tools/track.py finish 부품은행` (게이트 통과 → 3분 뒤 라이브).
2. 서버 DB에서 `store.set_setting('bank_enabled','1')` — 안 켜면 회귀0 그대로(은행 미주입).
3. 훅이 승인돼 있어야 top-k 풀에 뜸: 프로덕션 `python -m shopping_shorts.scripts.seed_pattern_bank` 1회 + 큐레이션 페이지 승인. **훅 풀 아직 8개뿐** → 로테이션 효과 제한적(P2 훅수확이 근본).

## 2026-07-22 (P0 라이브 + UI토글 + 훅수확 + P1 — "전부 순서대로" 실행)

사장님 "전부 순서대로 다 해" → P0 finish(라이브)에 이어 UI토글·훅수확·P1까지 연속 구현.

- **✅ P0 라이브(finish 1차, main d305f87fc)**: 훅로테이션·믹스은행배선·novelty 3종이 main에.
- **UI 토글**: 제작소 매칭시작 버튼 위 '✨ 스마트 믹스' 체크박스 → `GET/POST /api/settings/smart_mix`가
  `bank_enabled`+`ping_pong_enabled` 함께 on/off. 이제 **사장님이 화면에서 딸깍**으로 켠다(DB수동 불필요).
  produce.html JS 2블록 node --check OK. (test_app_smart_mix_toggle 3/3)
- **훅 자동수확(P2)**: `hook_harvest.py` — 우승작 캡션 첫줄→훅후보(해시태그컷·이모지제거·길이/한글필터)
  +taxonomy(target_callout/shock/discovery/warning/praise/confession/question, '자~OO하시는분들'=target_callout)
  →hook 버킷 pending. `daily_batch.run`에 스텝 추가(무과금). 승인해야 로테이션 풀에 뜸=P0-1 재료 자동증식.
  (test_hook_harvest 11/11)
- **P1 품질게이트**: `_score_candidate`에 `_candidate_quality`(tone_score 대화체+fun_intensity 재미D14)
  결합, 매칭0.75+품질0.25. scene_first 추천안이 말투좋고 재미장치 있는 후보가 됨. (test_scene_first_quality 3/3)
- **P1 서브 의무삽입**: `backbone.ensure_sources_used` — 안 쓰인 소스(s2=0)를 같은 행위로 강제삽입
  (sync보존·억지mismatch 금지). ping_pong 블록 dedup 뒤 배선. (test_ensure_sources 4/4)

## 2026-07-22 (P2 추가 — F1확장·taxonomy UI·novelty스코어, "배포까지 내 추천으로")

사장님 "배포까지 끝까지 묻지말고 너의 추천으로" → finish 1·2차(P0+UI토글+훅수확+P1 라이브) 후 P2 3종:

- **F1 상품군 확장**: `action_dict`에 비요리 행위 10종(두드리다·짜다·붙이다·접다·걸다·쌓다·밀다·신다·털다·
  정리하다). 요리 어휘와 안 겹치게 stem 선택. 이제 뷰티·수납·청소·의류 영상도 ping_pong·서브의무삽입·
  백본이 작동(전엔 요리만). (test_action_dict_nonfood 6/6, 요리 회귀 green)
- **훅 taxonomy 큐레이션 UI**: `pattern_bank.html` 카드에 hook_type 배지(타깃콜아웃·충격·발견·경고·
  극찬·고백·질문). 유형별로 보고 다양하게 승인. node --check OK.
- **novelty 스코어 감점**: `_score_candidate(avoid_hooks=)` — 첫 비트(훅)가 최근 훅과 n-gram 겹치면
  감점(최대 0.3). P0-3 프롬프트 회피의 이중방어. build_scene_first_plan·_plan_and_tts 관통(bank_enabled·회귀0).

**남은 것(다음 세션 P2 잔여)**: 훅 성과역전파(먹히는 훅이 더 자주 뽑히는 자기강화 — ⚠️우리 생성영상의
성과 피드백 파이프가 없어 신호원 확보가 선행, 현재는 speculative라 미착수)·CTA/스파인 로테이션 scene_first
주입(CTA는 parts_block로 이미 로테이션, 스파인은 category 매칭 필요)·백본-베이스 배선(generate_backbone_script).

**★서버 켜는 법(라이브 후)**: 제작소 '✨ 스마트 믹스' 체크 = 끝. 훅은 seed 8개+수확분을 큐레이션에서
승인해야 로테이션 효과↑. 실 Gemini 산출은 서버 관측이 최종증명(mock테스트라).

## ⏭ 다음 (미결)
**★UI 토글(사장님 요청)**: produce.html 재료카드에 '메인(백본) 지정' 토글 → mix start시 backbone_main(s0/s1/s2 index) 전송→job 저장. ⚠️produce.html JS 문법 틀리면 제작소 안열림(전례)→node --check 필수.
**★서브 의무삽입**: 백본 흐름에 s0/s2 강제 끼워 s2=0 해결(dedup으론 안됨).
**백본-베이스 배선**: generate_backbone_script를 믹스경로에 → 대본 100% 우리것.
그외: 댓글수 meta(현재 플랫폼만)·F1 상품군 사전확장·best-of-N 품질랭크.


1. **★finish 전 전체회귀 신규실패 0 확정** → `py tools/track.py finish 부품은행` → 3분 뒤 라이브.
2. **크롤 우승작 자동흡수는 "대본이 이미 추출된 우승작"만** 흡수(조인키 shortcode, opportunistic). 대부분 크롤아이템은 스크립트 없어 자연 스킵 — 커버리지가 낮으면 우승작 자동 스크립트추출 파이프가 후속 필요(현 스코프 밖).
3. **큐레이션 UI 노출**: Phase1 산출(perf_score·스파인 pending)은 `pattern_bank.html`에 아직 안 붙음 — 스파인 승인 버튼/perf 배지 노출은 Phase 2 착수 시 or 별도 UI 태스크.
4. **Phase 2**(생성이 스파인+부품 은행에서 조립 + 대화체 스코어러, F4 골든셋 선구축) / **응용 스펙**(백본인터리브, A7 행위태깅 커버율 스파이크가 게이트 — F1 사전이 그 v1 토대).
5. 실 Gemini 경로(extract_script action enum·spine 클러스터)는 mock 테스트라 **서버 실행 관측이 최종 증명** — 야간배치 1회 실행 로그 확인 권장.
