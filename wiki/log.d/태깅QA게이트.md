# 태깅QA게이트 작업 로그

## 2026-08-01 (집PC)
- 태깅 QA Layer 1 완성·푸시(`5c9f33986`). `tag_qa.validate_extract` 7개 검사로 추출 결과를
  프롬프트 지침 대비 채점 → 0.6 미만이면 flags를 프롬프트에 얹어 딱 1회 교정 재호출.
  결과는 어떤 경로로도 안 버린다(fail-open) — QA는 기록 장치지 차단 장치가 아니다.
- 배경: `script_extract`가 스키마만 통과하면 태깅을 무조건 채택했다. 슬롯 기반 화면조합
  (대본퀄v3)이 이 층 위에 전부 서 있는데 여기만 검증이 0줄이었다.
- 신규 테스트 19개 green, 기존 스위트 새 실패 0(기준선 stash 대조 확인).
- 다음: Layer 3 `tools/tag_audit.py`(태깅층 기준선 측정 — 대본퀄v3 Task 7 실측 전에 필요),
  이어서 Layer 2 프레임 대조.

## 2026-08-01 (회사PC, 이어서) — Layer 3
- `tools/tag_audit.py` 신설. 저장된 추출을 채점해 **score 분포·최빈 flags·최악 5건**을 표로.
- ★기준선 규칙을 코드 머리말에 박음: **서버 DB로 통일**(로컬과 섞어 비교 금지), 측정 기준
  4줄(DB경로·갱신시각·측정시각·표본수)을 숫자와 함께 남길 것.
- ★코호트 분리가 이 도구의 핵심: `validate_extract`는 duration이 None이면 커버리지 검사
  (가중 0.20)를 건너뛴다. DB엔 영상이 없으니 재계산 점수는 실시간보다 **최대 0.20 높다**.
  → A(저장 점수=진짜 기준선) / B(레거시 재계산=상한 편향)를 절대 안 합친다.
  실측 데모에서 B가 평균 1.0으로 떴다 — 섞었으면 기준선이 후하게 잡혔을 것.
- 부수: 옛 스키마 행(`change`가 bool) 하나로 감사 전체가 죽던 것을 fail-open 처리('깨짐'으로
  집계). 윈도우 cp949 콘솔에서 이모지로 죽던 것도 stdout UTF-8 전환으로 해소(둘 다 실측).
- 테스트 11건 신규 green. 임시 DB 6행으로 출력 경로 끝까지 확인.
- **finish로 main 병합·서버 배포**(`ee6924949`, 게이트 기준선 13=신규 0) 후 **서버 DB 실측**:
  B코호트 50건 평균 0.858(중앙 0.85, 최저 0.6), **A코호트 0건**(Layer 1 배포 직후라 아직
  새 추출이 없음 = 진짜 기준선은 다음 회차).
- ★측정 함정 하나 더 발견: 최빈 flag `change 전멸` 41/50(82%)은 라이브 결함이 아니라
  **필드 도입일(07-31) 이전 행 40건**이었다. 도입 후만 보면 7/10 충족. 규칙 5번으로 문서화.
- ★**저장 누락 수정** — A코호트가 영원히 0건이 될 뻔했다. `_attach_qa`가 tag_qa를 붙여도
  저장부 3곳이 `{full_text, segments}`를 손으로 다시 만들어 저장 순간 버리고 있었다.
  `script_extract.storable()`(화이트리스트) 신설 후 prewarm·app 저장 3곳 교체. 읽기 전용
  3곳은 되쓰지 않아 무해함을 실측으로 갈라 안 건드렸다. 관측성 로그 한 줄도 추가.
  재발 차단은 AST 가드(축약 dict가 새로 생기면 실패) — **회귀를 주입해 fail, 원복해 pass까지
  확인**했다. 35건 green, mix_pipeline 1건 실패는 stash 대조로 기준선 확인.
- **Layer 2 코드 완료(플래그 `tag_qa_frames_enabled` 기본 0, 안 켬)**: `tag_qa_frames.py` 신설
  — 대표 세그 3개(훅/실증/결과) 프레임 1장씩, 모델 1회 판정, `tag_qa.frame_score`에 기록만.
  OFF일 때 호출 0회·예외 나도 추출 무사함을 스크립트로 실측. 테스트 16건.
  ⚠️실제 영상으로는 안 돌려봤다 — 켜기 전 라이브 밖에서 1건 실측 필요.
- **Layer 2 라이브 밖 실측 완료**(서버 원본 3건, 플래그 안 켬, 건당 8~10초·모델 1회).
  1차에서 **측정 노이즈** 발견: '틀림' 0건인데 '부분'이 많고 이유가 전부 "구간 묘사의 동작이
  이 한 장엔 없다"였다 — scene_desc는 구간 요약, 프레임은 중간 한 장이라 당연한 것을
  감점하고 있었다. 프롬프트 재정의 후 같은 3건 0.667/0.667/0.833 → **1.0/0.833/1.0**.
  ★남은 '부분' 1건은 진짜 오류("투명 가림막인데 화면은 불투명 철제") = Layer 2가 작동한다는 증거.
  ⚠️표본 3건·각 1회, 모델 비결정적이라 절대값은 표본 더 필요.
- **코드리뷰 대응 P1(F1·F2)** — 기준선 잡기 **전에** 점수식을 고정하라는 리뷰 지시대로 선착수.
  F1=커버리지가 겹친 세그를 이중 합산해 공백을 가리던 것 → 구간 합집합(`_covered_seconds`).
  F2=shot_role/change/is_key 3신호가 첫 것만 flags에 실려 재시도 프롬프트에서 나머지가
  누락되던 것 → 셋 분리, 가중치 0.07/0.05/0.03(합 0.15로 종전 유지).
  ★F1 테스트를 한 번 잘못 짰다(꼬리 검사가 먼저 걸려 회귀를 못 잡음) — 회귀 주입으로 발견해
  '꼬리 정상·중간만 공백' fixture로 교체. 두 결함 다 주입→fail, 원복→pass 확인. 57건 green.
  ⚠️점수식이 바뀌었으니 이전 B코호트 0.858과 비교 금지. F1은 A코호트에만 영향(B는 커버리지 스킵).
