# 제품명검색 (숏템내부검색) — handoff

**2026-08-04 (회사PC)** · 트랙 `제품명검색` / 폴더 `.tracks/제품명검색`

## 목적 (사장님 원문)

> "결론은 같은 영상이 과거에도 있던걸 그냥 찾아서 믹스할려고 하는게 목적"
> "같은제품이지. 우리가 2개 영상믹스할때 렌즈로 검색해서 같은제품영상을
> 믹스하는거자나 그거랑 같은개념인데 내부에서도 찾는다는거야"

= **렌즈로 하던 "같은 제품 영상 찾기"를 우리 아카이브 안에서** 한다.
'비슷한 소재'가 아니라 **같은 제품**이다. 이걸 헷갈리면 방향이 통째로 틀어진다.

## 완료 (전부 라이브)

1. **🏠 숏템내부검색 버튼** — 랭킹 메인카드 + 역대히트작. 🔍 파워검색(외부·유료 렌즈)과
   색으로 구분: 파워=금색, 내부=청록.
2. **제품명 판독** — `product_name.py`. 비전모델에 "자막 무시, 실물 제품만 상품명으로"
   지시. `vision_tags.product`에 캐시.
3. **2단 검색** — `api_archive_similar`: ①태그겹침 후보 → ②상위 60건 판독 → ③같은 제품 최상단.
4. **선판독 백필** — `product_backfill.py`. 조회수 상위부터 미리 판독.

## ⚠️ 실측으로 기각한 것들 (다시 시도하지 마라 — 근거 있음)

| 방식 | 결과 |
|---|---|
| 태그 토큰겹침만 | 제품명 완전일치 **0건**. '살림꿀팁'이 4,222건 중 15%라 분위기로 묶인다 |
| 태그텍스트 임베딩 | '비슷한 소재'는 잘 찾음(라멘→마라탕 0.708). **같은 제품과는 다른 축** |
| 썸네일 이미지 임베딩 | 자막 타이포에 끌려 **순위 뒤집힘**(같은제품 최저 0.613 < 다른제품 최고 0.727) |
| 영상 프레임 추출 | 가능성 높지만 14,000건 mp4 다운로드 필요 → 보류 |

**채택**: 비전모델 프롬프트로 자막 무시(사장님 아이디어). 임베딩은 지시를 못 받지만
flash-lite는 받는다. 실측 3쌍 전부 같은 제품으로 묶임.

## 📌 2026-08-05 17:30 회사PC → 집PC 인계 (히트작 계정 로테이션)

### 결론: 계정 로테이션 + 계정별 한국 주거용 IP로 간다 (Apify 불필요, 추가결제 거의 0)

Webshare 기존 플랜(Rotating Residential, ACTIVE)에 국가 접미사만 붙이면 한국
가정회선이 나온다 — **추가 구매 없음**. 실측: kr-1 LG POWERCOMM 122.44.2.179 /
kr-2 LG POWERCOMM 182.229.233.135 / kr-3 SK브로드밴드 114.206.62.46 /
kr-4 DACOM 211.241.126.192 / kr-5 SK브로드밴드 175.113.98.145. 전부 서로 다른 IP.

서버 env에 `WEBSHARE_USER=bhkvcxfo` / `WEBSHARE_PASS` / `WEBSHARE_COUNTRY=kr`,
`INSTAGRAM_SESSION_DIR=/home/ubuntu/ig_sessions` 설정 완료.

### 구현 완료 (커밋됨)
- `channel_archive.session_slots()` — ig_sessions/uid.json 목록
- `channel_archive.slot_proxy(i)` — 슬롯 i에 `-kr-(i+1)` 출구 고정 배정
- `crawl_channel(..., session_path=, proxy=)` — 세션·프록시 동시 적용(예전엔 배타 분기라 버그)
- `run()` — scraping_warning 감지 시 다음 계정+IP로 같은 채널 자동 재시도, 전부
  소진되면 회차 종료
- `scripts/ig_session_capture.py` — 모든 Firefox 프로필/컨테이너를 훑어 살아있는
  세션 전부 적립(WAL 포함 복사 필수 — 안 하면 방금 로그인분이 안 보인다)

**E2E 검증됨**: 계정 41170560843 + kr-2 → 릴스 24개 수집, scraping_warning 없음.

### ★집에서 이어서 할 것
1. **Firefox 프로필을 계정 수만큼 분리** (`about:profiles` → 새 프로필 → 그 창에서 로그인).
   ★한 프로필에서 계정을 갈아타면 **로그아웃 순간 직전 계정 세션이 무효화된다**(실측:
   적립해둔 bagcheon 세션이 홈으로 리다이렉트). 프로필을 나눠야 세션이 동시에 산다.
2. `python scripts/ig_session_capture.py` → `scripts/ig_sessions/*.json` 생성
3. 서버로 복사: `scp -i <pem> scripts/ig_sessions/*.json ubuntu@43.200.48.69:/home/ubuntu/ig_sessions/`
4. 크론 재활성화: 서버 crontab의 `#DISABLED_scraping_warning#` 주석 제거
   (`30 10 * * * /home/ubuntu/run_archive_crawl.sh`)
5. 계정당 몇 채널까지 버티는지 실측 → 필요한 계정 수 확정(현재 추정 3~5개)

### 현재 유효 슬롯
- 41170560843 ✅ (검증됨) / 38718393284 ❌ (로그아웃으로 무효화 — 재로그인 필요)

### ★미처리 (집에서 확인)
- **DuckDNS 갱신 안 됨** — shoppingshorts / stockbrain1 둘 다 구 IP(3.39.179.148)를
  가리켜 **사이트 접속 불가**. 새 IP `43.200.48.69`로 바꿔야 한다.
- **Lightsail 구 StaticIp-1 삭제 안 됨** — 미사용 고정IP는 시간당 과금된다.
- Webshare 대역폭 1GB 중 0.53GB 사용, 초과 경고 중. 전량 크롤엔 3GB쯤 필요(약 $7).
- 서버 IP 교체는 무효한 조치였다(AWS 대역이 아니라 계정 문제였음). 부작용은 없음.

## 📌 2026-08-05 16:20 히트작 — 전 경로 실측 완료, 결론: Apify 분리가 답

### 실측표 (모두 오늘 직접 측정)
| 경로 | 결과 |
|---|---|
| 계정쿠키 + 서버 | 계정 1~2시간 만에 영구 플래그 (helrou75·syospa123 2개 소실) |
| 계정쿠키 + 독일 주거용 프록시 | 동일 차단 (계정 플래그라 IP 무관) |
| 무계정 + 서버 AWS IP | 첫 요청 **429** |
| 무계정 + 집 IP | 24개 받고 401, 5분 뒤 재시도도 401 |
| 무계정 + 독일 주거용 IP | 12개 받고 401, **27분 재시도 전부 401** ← 쿨다운 30분 이상 |

무계정 방식 자체는 **작동 확인**(curl_cffi impersonate=chrome, 프로필HTML에서
`"profilePage_(\d+)"`로 uid → `/api/v1/feed/user/{uid}/?count=12&max_id=` 페이지네이션,
헤더 `X-IG-App-ID: 936619743392459`). play_count·like·comment·taken_at 다 나온다.
**단 IP당 12~24개 / 쿨다운 30분+** 라 대량엔 비현실적. `web_profile_info`는 IG측
스키마 에러로 400 고정(전역 현상).

### ★구조적 위험: 레퍼런스랭킹과 세션 공유
`instagram_playwright`도 `channel_archive`도 같은 `INSTAGRAM_SESSION_PATH`를 쓴다.
→ **히트작 크롤이 계정을 태우면 레퍼런스랭킹도 같이 죽는다.** 오늘 실제로 그랬다:
reel_history 8/5분 513건은 전부 **00시대** 수집분이고, 11시 이후 히트작 시도로 계정이
플래그돼 오늘 밤 00시 수집은 실패 예상.

### 권고 구조 (분리)
- 레퍼런스랭킹 = 계정 세션 유지, 하루 300~500건 저강도(지금껏 버텨온 수준). **히트작에 재사용 금지.**
- 히트작 아카이브 = **Apify 분리**. 계정·IP 무노출.

### Apify 실단가 (API 조회)
살아있는 계정 5개: ceremonial_anhinga(STARTER $60/월) + FREE 4개($5/월) = 월 $80 크레딧.
- `apidojo~instagram-scraper-api`: 게시물당 **$0.0005** + 채널당 $0.005(첫 10개 무료) ← 최저가
- `apify~instagram-reel-scraper`: 릴스당 $0.0026(FREE등급)
- 509채널 환산: 상위50개=2.5만건 **$15** / 상위200개=10만건 **$53** / 전량20만건 **$103**
→ 상위200개까지는 **현 크레딧으로 추가결제 없이** 가능.

### 부수 정리
- 서버 IP 3.39.179.148 → **43.200.48.69** 교체 완료(무효한 조치였지만 부작용 없음).
  ★DuckDNS 두 도메인 갱신 필요(shoppingshorts·stockbrain1) — 안 하면 사이트 접속 불가.
  구 StaticIp-1 삭제 필요(미사용 고정IP 과금).
- Gemini: daily_batch 로그에 429(무료등급 분당15회) + **403 PERMISSION_DENIED**(키 1개 구글 차단) 발견 — 별건.

## 📌 2026-08-05 14:50 ★정정: IP가 아니라 **계정**이 플래그된다 (앞 두 항목의 IP 결론은 오진)

같은 세션(syospa123)으로 시간차 실측:
| 시각 | 출구 | 결과 |
|---|---|---|
| 13:00 | 집 PC | 릴스 12개 정상 |
| 14:30 | 신 서버 IP / 주거용 프록시(DE, Deutsche Telekom) | scraping_warning |
| **14:45** | **집 PC (13:00과 동일 환경)** | **scraping_warning** ← 계정이 죽은 것 |

즉 IP 교체도 프록시도 무의미했다. **서버에서 크롤을 시도한 그 행위 자체로 계정이
1~2시간 만에 플래그**되고, 한 번 플래그되면 어느 IP에서도 안 된다.
helrou75(정지) → syospa123(반나절) 둘 다 같은 경로.

부수 확인: Webshare 프록시는 살아있고 주거용(Deutsche Telekom) 출구다 — 과거
"타임아웃" 기록과 달리 지금은 정상 연결된다. 그런데도 막히는 건 계정 문제라서다.

**실질 선택지는 둘뿐**:
(a) Apify — 우리 계정·IP 무노출. 토큰 4개·코드 기존재. 가장 확실.
(b) 계정 다회전 + 초저강도(계정당 하루 2~3채널) — 계정 소모전, 509채널이면 비현실적.

서버 IP는 43.200.48.69로 이미 교체됨(되돌릴 필요 없음, 부작용도 없음).
DuckDNS 갱신 필요(구 IP 가리키는 중). 크론은 비활성 유지.

## 📌 2026-08-05 14:20 히트작 크롤 — 서버 IP 교체해도 실패, 원인은 AWS 대역 자체

**결론: 인스타는 개별 IP가 아니라 AWS 데이터센터 대역을 막는다. IP 교체는 해법이 아니다.**

실측 3종 (모두 같은 부계정 세션 쿠키 사용):
| 출구 | 결과 |
|---|---|
| 집 PC(가정용 IP) | instagram/natgeo/koreanair/onshome_official 전부 릴스 12개 정상 |
| 구 서버 IP 3.39.179.148 | `accounts/scraping_warning` 챌린지 |
| **신 서버 IP 43.200.48.69** | **똑같이 `scraping_warning`** ← IP만 바꿔선 안 됨 |

계정 교체도 무효(helrou75 정지 → 신규 syospa123도 서버에서만 동일 차단). 계정만 태운다.

서버 IP는 43.200.48.69로 교체됨(StaticIp-2). SSH·shopping-shorts·stockbrain 정상 확인.
★DuckDNS 두 도메인(shoppingshorts / stockbrain1)이 아직 구 IP를 가리켜 사이트 접속 불가 —
사장님 수동 갱신 필요. 코드 하드코딩 IP는 SERVER_HOST 환경변수로 분리(36d948d7b).

**남은 선택지**: (a) 주거용 프록시 INSTAGRAM_PROXY (과거 Webshare DE 출구는 타임아웃 —
KR 출구로 재시도 필요) (b) Apify (토큰 4개·코드 기존재, 우리 IP/계정 무노출)
(c) 집 PC에서 크롤. 크론은 비활성 상태로 둠.

## 📌 2026-08-05 11:45 회사PC — ★1순위 진단 완료: 원인은 키 소진

- 서버에서 `comment_gen._load_state()` 확인: **키 23개 전부 exhausted (date=2026-08-05)**. 썸네일 403 아님.
- 범인은 **무한루프 태거**(--rounds 0, 00:12 시작): 09:00 리셋 직후부터 키를 선점 →
  09:40 백필은 잔량만 받아 680/3000에서 굶었다. 11시 현재 태거도 2바퀴째 전 채널 0/40 헛돎.
- 조치(회사PC):
  1. 헛돌던 태거 PID 1991903 kill (백업: /tmp/run_tagger.sh.bak.*)
  2. run_tagger.sh에 대기 가드 추가 — 백필(product_backfill)이 도는 동안 60s 폴링 대기.
     내일 09:10 백필이 리셋 키풀을 먼저 통째로 쓰고, 태거는 그 뒤 잔량으로 돈다.
- 남은 것: 키풀 중복 3쌍+죽은 키 2개 정리(새 키 필요), 크롤 재개 여부(사장님 결정) — 그대로 유효.

## 📌 2026-08-05 09:52 현재 상태 (집PC → 회사PC 인계)

| 작업 | 상태 |
|---|---|
| `archive_tagger` | **가동 중** (PID 1991903, 00:12 시작 9h40m째). 채널당 40/40 거의 만점 |
| `vision_tags` | 8,893 → **17,548건** (하룻밤 +8,655) |
| `product_backfill` | 09:40 수동 재실행 → **680건 처리 후 자체 중단**(6배치 연속 0건) |
| `channel_archive` (크롤) | **멈춤**. 08-04 15:40 이후 안 돔. 크론에 없음(수동 전용) |
| 아카이브 | 75,560릴스 / 186채널 (done 186 · error 38 / 목표 675) |

**★백필은 크론 고친 뒤에도 680건에서 멈췄다 — 아직 안 풀린 문제다.**
00:10분 실행(280건)보다는 나아졌지만 3,000 목표엔 한참 못 미친다. 리셋 직후인데도
멈췄다는 건 **원인이 "크론 시각" 하나가 아니라는 뜻**이다. 유력한 건 handoff 아래
적어둔 **키풀 중복 3쌍 + 죽은 키 2개**(16개 등록이지만 실제 13개) — 로테이션이
같은 키를 반복해 만나 실제 가용량이 광고보다 훨씬 적다. 아니면 조회수 상위 구간
썸네일이 4일 만료(403)로 죽었을 수도 있다. **둘을 가르는 진단이 다음 세션 1순위.**

## ⏭ 다음 세션

- **★1순위: 백필 680건 조기중단 원인 가르기.** 0건 배치의 실패 실물을 봐라 —
  키 소진(`comment_gen._load_state()["exhausted"]` 조회)인지 썸네일 403인지.
  로그 메시지는 둘을 구분 못 하고 "키 소진 또는 썸네일 만료"로 뭉뚱그린다.
- **백필 결과 확인**: `tail -30 /tmp/product_backfill.log`. 3,000건 목표.
  중단됐으면 그냥 재실행하면 이어진다(캐시가 거른다).
- **크롤 재개 여부 결정(사장님)**: 489채널 남음 × 5.7분 ≈ **46시간**. 차단 위험 때문에
  전체 vs 상위 N채널만 정해야 한다. 크롤이 멈춰 있으면 태거가 아무리 돌아도
  나머지 489채널은 아카이브에 안 들어온다.
- **error 38채널 `결과 0` 미진단**: 로그 재확인 결과 연속 폐지 패턴이 아니라 두 번의
  실행에 44건이 흩어져 있다 → 인스타 통로 폐지보다 개별 채널 문제(비공개·삭제) 쪽이
  유력. 확인 안 함.
- **효과 재측정**: `/tmp/ab_probe.py` 재실행 → 전후 비교. 직전 실측은 13→15건(+2)인데
  이는 제품명이 6%뿐이라 그랬다. 백필 후 훨씬 커져야 정상.
- **⚠️ 키풀 정리 필요 (사장님 확인 대기)** — 16개 중 정상 9 / 429 5 / **죽음 2**.
  8번=401 인증실패, 11번=403 "project denied". **★중복 등록 3쌍**(1↔14, 2↔15, 3↔16이
  같은 키) → 실제 키는 13개. 로테이션이 "다음 키"로 넘어가도 같은 키를 또 만난다.
  `/etc/shopping-shorts.env` 수정 필요 — 서버 설정이라 사장님 판단.
- 아카이브 태그 커버리지가 병목(현재 18%). 태거가 채널당 40개씩 도는 중.

## 배경 작업 + ★키 소진 문제 (2026-08-04 03:20 기준)

```
channel_archive  → 144/675채널 크롤 진행 중 (2.8분/채널, 키 안 씀 → 밤새 계속 돔)
archive_tagger   → ★멈춤. 556건 태깅 후 종료
product_backfill → ★멈춤. 330건에서 종료
```

**둘 다 멈춘 원인은 같다: Gemini 키 16개 중 15개가 오늘치 소진.**
썸네일은 정상 다운로드되는데(172KB 등) `_identify_one`이 `(None,None)`만 반환한다.
⚠️ 태거 로그는 이걸 "썸네일 만료 추정"으로 찍는다 — **오해하지 마라. 키 소진이다.**
(확인: `comment_gen._load_state()["exhausted"]` → 15개 idx가 들어있음)

**소진 표시는 UTC 날짜가 바뀌면 자동 해제**된다(`comment_gen._load_state`,
date가 오늘이 아니면 exhausted를 비운다) = **KST 09:00**. 그래서 크론을 걸어뒀다.

### ★2026-08-05 수정: 크론 시각이 9시간 어긋나 있었다

위 크론을 `10 0` / `12 0`으로 적었는데 **서버 크론은 KST(로컬)로 돈다**. 즉 주석은
"KST 09:10"이라 써놓고 실제로는 **KST 00:10 = 리셋 9시간 전**에 실행됐다.

```
(before) 10 0 * * *  → KST 00:10  ← 리셋 전. 키가 아직 소진 상태
(after)  10 9 * * *  → KST 09:10  ← 리셋 직후. 의도대로
```

- **백필**: 00:10 실행분은 280건에서 죽었다(첫 배치만 21건 = 남은 키 찌꺼기).
- **태거**: 살아난 건 **우연**이다. `--rounds 0` 무한반복이라 9시간을 버티다
  진짜 리셋 시각(09:00)을 넘겨서 그때부터 40/40으로 붙기 시작했다.
  백필은 단발이라 그걸 못 기다리고 죽었다.

2026-08-05 09:39 크론 수정 완료(`10 9` / `12 9`). 백업: 서버 `/tmp/cron.bak.1785890413`.

아침에 `tail -30 /tmp/product_backfill.log` / `/tmp/archive_tagger.log`로 확인.

⚠️ **`pgrep -f product_backfill`로 생사 판정하지 마라** — SSH로 던진 명령줄 자신이
패턴에 걸려 **항상 "실행 중"으로 나온다**(2026-08-05 두 번 오판). 반드시
`ps -eo pid,lstart,etime,cmd | grep ... | grep -v grep`으로 실제 PID를 눈으로 봐라.
(단 `run_tagger.sh` 안의 중복방지 pgrep은 스크립트 파일 안이라 정상 동작한다.)

모두 `/etc/shopping-shorts.env` 로드 후 실행해야 한다 → [[reference_서버_SHORTS키_systemd전용]]

## 오늘 밟은 함정

- **디스크 풀** — 90%까지 차서 태거·크롤러가 `[Errno 28]`로 죽어 있었다.
  mix_jobs 18G 정리(완성본 48개는 `/home/ubuntu/mixjob_finals_backup`에 백업 후
  미완성 147개+찌꺼기 삭제) → 73%. 백필에 디스크 가드 3GB를 넣은 이유.
- **`loading="lazy"`가 썸네일을 통째로 죽였다** — grid+aspect-ratio 안에서 lazy 로더가
  가시성 판정을 못 해 `/api/thumb` 요청이 **0건**이었다. 서버 문제로 오진했다가
  `performance.getEntriesByType('resource')`로 잡았다. 3군데 제거.
- **`crossorigin="anonymous"`** — same-origin 프록시에 붙이면 CORS 응답 취급이라
  이미지 196장이 전부 버려진다(fetch는 200인데 naturalWidth=0).
