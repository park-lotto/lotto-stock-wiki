2026-08-18: 제작소 9단계(최종렌더)에서 인포크링크에 바로 등록하는 모달 신설·라이브.
새 탭으로 나가던 `인포크링크 열기 ↗`를 없애고 iframe으로 창 안에 띄운다 — 링크는 열 때 자동 복사.
★가능 여부를 실측으로 먼저 확정했다: link.inpock.co.kr은 X-Frame-Options·CSP·meta CSP·프레임버스팅이
하나도 없고 세션 쿠키도 iframe에서 유지된다(사장님 계정 실로그인 확인). "막힐 것 같다"는 내 추정이 틀렸다.
모달은 pmModal의 .gen-bg 규약 그대로 + .gen-box-wide(1440px)만 추가(3단 레이아웃이 760px에선 눌림).
함정 기록: ①패널 제목 번호는 syncPanelTitles()가 자동 교정하므로 손대면 두 벌이 된다(고치려다 취소)
②clipboard는 조용히 실패하므로 성공을 가정하면 빈 클립보드로 붙여넣게 된다 ③final_link는 상품이
있어도 빌 수 있다(url·partner_url 둘 다 비면). pytest 33 passed(신규 9·회귀 0). 커밋 68e750bee.

2026-08-18(이어서): 8단계 저장이 끝나면 인포크에 붙여넣을 4줄(등록이름·DM타이틀·버튼이름·고지문구)이
자동 완성된다. 번호는 "마지막 번호 다음"으로 서버가 자동 부여(get_setting, 새 테이블 없음).
★실제 운영은 DM 버튼 → 인포크 페이지 → 번호 검색 → 쿠팡. 번호는 상품 이름 앞에 직접 붙인 것이고
한 번호에 상품이 여러 개일 수 있다(채이홈 588. 2개). 영상마다 바뀌는 건 번호·상품명뿐이라
인포크 자동화는 한 번만 세팅하면 된다. 함정: ①번호는 한번 받으면 고정(안 그러면 등록해둔 것과 어긋남)
②이름은 지어내지 말고 쿠팡 카드에서 온 실제 상품명 ③clipboard.writeText는 Promise라 동기 try/catch로
못 잡는다(기존 coupangCopy·inpockCopy 2곳에 같은 문제 남아있음). pytest 48 passed. 커밋 c7bc47084.
⏭긴 상품명 다듬기(원본 그대로 넣는 중) 사장님 판단 대기.
★게이트가 신규실패 13건으로 막았는데 로직 아니라 `node -e` 명령줄 상한(WinError 206) 재발(세번째).
work_restore·work_seams의 _run을 임시파일 실행으로. node -e 쓰는 테스트 8개가 아직 남은 시한폭탄.

2026-08-18: ★틀 안에서는 인포크 로그인이 안 된다(실측 확정). link-rest.inpock.co.kr은 자기 도메인
Origin만 허용 — 우리 도메인엔 Access-Control-Allow-Origin이 아예 안 온다 → 인포크가 "네트워크 오류"로
표시(계정 문제 아님). 가짜계정 프로브 시 "가입하지 않은 아이디"가 뜨는 것과 문구가 다른 게 증거.
어제 확인한 "세션 유지"는 맞지만 **만료됐을 때**는 못 본 것이었다. 해결=뚫지 않고 새 탭으로 안내
(🔑 로그인 링크 + ↻ 새로고침, src 재대입+캐시회피). 크로스오리진이라 로그인화면인지 판정 불가 →
안내는 항상 표시. pytest 23 passed. 커밋 4da4f3a8c.

2026-08-18(최종): ★iframe 방식 폐기. 진짜 원인은 CORS가 아니라 **쿠키**였다 — 인포크 쿠키 11개가
전부 SameSite=Lax(None 0개)라 크로스사이트 틀에 아예 안 실린다. 인포크 탭은 로그인 상태인데
틀 안에서만 "로그인 만료"로 보이고 재로그인해도 소용없다(사장님 실제로 겪음).
내 잘못: 프레임 삽입이 안 막힌다는 것만 보고 쿠키를 확인 안 한 채 "된다"고 결론냈다.
→ 새 탭으로 되돌림(원래 방식이 옳았다). 왜 안 되는지 주석·테스트로 못 박아 재시도 차단.
⚠️window.open(...,'noopener')는 성공해도 null 반환 → 반환값 판정하면 열렸는데 "팝업 차단"이 뜬다.
rel=noopener <a> 클릭으로 교체. pytest 47 passed. 커밋 77d074d47.
