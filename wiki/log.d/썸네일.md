# 썸네일 트랙 로그

## 2026-07-17 — 페이즈1 라이브 (f431eeb3)

- **기능이 0% 동작하던 걸 최종 whole-branch 리뷰가 잡았다.** `STATE.job_id`는 존재하지 않는
  변수(대입 0곳)라 썸네일 라우트 3개가 전부 404였다. 진짜는 `MIX_JOB`. 3줄 교체로 해결.
- **왜 못 봤나**: 테스트 하네스가 `global.STATE={job_id:'j1'}`로 없는 계약을 발명했고,
  앞 세션의 "라이브 실증"과 이 세션의 흐름 검증도 같은 줄을 손으로 심었다 — 셋이 똑같이 속았다.
  새 판정법: **"이 값을 하네스가 넣어주고 있나? 페이지가 진짜로 넣는지 확인했나?"**
- 함께 픽스: 프리셋이 세로위치 되돌림 / 최종선택이 조용히 실패(세션만료 401) / 추출 진행중
  재진입으로 ffmpeg 3벌 / 문구 미이스케이프 / 레이어 삭제 시 선택 튐 / 남의 .xls 덮어쓰기.
- 자물쇠: 슬라이스가 아니라 진짜 파일을 읽어 `STATE.job_id`를 금지(뮤턴트로 확인).
- 게이트 11 failed / 1050 passed = 기준선 동일, 회귀 0.

## 2026-07-18 — 사장님 실사용 제보 4건 배포

- 드래그 hit-test+상대이동(54ae16c4) / 잔상 clearRect(d1f275ed) / 매칭후 프레임=preview 폴백(bd7c6491, 라이브 수박작업 10장 검증) / 자막 미리보기 순차재생 A안(780dde2a).
- 교훈: 로컬서버 크기대조 먼저(유령 프로세스로 30분 헤맴) / 검증스크립트가 상태 주입 안 하나 확인.
- finish Aborting은 타세션 untracked+main폴더 미완료병합 탓, origin push는 매번 성공.
