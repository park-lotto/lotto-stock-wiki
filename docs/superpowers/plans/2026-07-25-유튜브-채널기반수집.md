# 유튜브 채널기반 수집 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** `/refs`에 등록만 되고 수집 안 되던 유튜브 채널(kind=account 시드)의 최근 Shorts를 Data API로 무료 수집해, 인스타와 동일한 레퍼런스 랭킹에 뜨게 한다.

**Architecture:** `youtube_client`에 채널→Shorts fetcher를 추가한다. fetcher는 키워드 경로(`search_shorts`)와 **동일한 raw dict**를 반환하므로 `build_youtube_items` 이하 파이프라인이 무변경으로 흐른다. `_collect_youtube`가 계정 시드 경로를 키워드 경로와 병합하고 video_id로 중복제거한다. channelId 해석은 소형 store 캐시로 1회만.

**Tech Stack:** Python, requests(YouTube Data API v3), sqlite3, pytest. 기존 `_first_ok` 키로테이션·`YOUTUBE_API_KEYS`·`platform_seeds` 재사용.

**설계문서:** `docs/superpowers/specs/2026-07-25-유튜브-채널기반수집-design.md`

---

## File Structure

- `shopping_shorts/youtube_client.py` — 추가: `_parse_duration_secs`, `_resolve_channel`, `fetch_channel_shorts`
- `shopping_shorts/store.py` — 추가: `youtube_channel_cache` 테이블 + `yt_cache_get`/`yt_cache_put`
- `shopping_shorts/service.py` — 수정: `_collect_youtube`가 계정 경로 병합 + video_id 중복제거
- `shopping_shorts/tests/test_youtube_channel.py` — 신규: 파서·resolve·fetch 테스트
- `shopping_shorts/tests/test_yt_cache_store.py` — 신규: 캐시 라운드트립
- `shopping_shorts/tests/test_collect_youtube.py` — 수정: 계정+키워드 병합·중복제거 테스트 추가

**핵심 계약**: `fetch_channel_shorts`가 반환하는 raw dict 키 = `search_shorts`와 동일:
`video_id, channel_id, channel_title, title, description, thumbnail, published_at, views, likes, comments`. 창(14일) 필터는 하지 않는다 — 다운스트림 `build_youtube_items`가 `window_hours`로 거른다(키워드 경로와 동일). fetcher는 **≤60초 숏폼 필터만** 한다.

---

## Task 1: duration 파서 (`_parse_duration_secs`)

ISO8601 `PT#H#M#S` → 초. ≤60초 숏폼 판정에 쓴다.

**Files:**
- Modify: `shopping_shorts/youtube_client.py` (상단 정규식 근처에 함수 추가)
- Test: `shopping_shorts/tests/test_youtube_channel.py` (신규)

- [ ] **Step 1: Write the failing test**

`shopping_shorts/tests/test_youtube_channel.py` 생성:

```python
from shopping_shorts import youtube_client as yc


def test_parse_duration_secs():
    assert yc._parse_duration_secs("PT59S") == 59
    assert yc._parse_duration_secs("PT1M") == 60
    assert yc._parse_duration_secs("PT1M1S") == 61
    assert yc._parse_duration_secs("PT2M") == 120
    assert yc._parse_duration_secs("PT1H2M3S") == 3723
    assert yc._parse_duration_secs("") is None
    assert yc._parse_duration_secs(None) is None
```

- [ ] **Step 2: Run test to verify it fails**

Run: `python -m pytest shopping_shorts/tests/test_youtube_channel.py::test_parse_duration_secs -v`
Expected: FAIL — `AttributeError: module ... has no attribute '_parse_duration_secs'`

- [ ] **Step 3: Write minimal implementation**

`youtube_client.py`의 `_YT_ID` 정규식 정의 아래에 추가:

```python
_DURATION = re.compile(r"PT(?:(\d+)H)?(?:(\d+)M)?(?:(\d+)S)?")


def _parse_duration_secs(iso):
    """ISO8601 재생시간(PT#H#M#S) → 초. 빈값/None → None."""
    if not iso:
        return None
    m = _DURATION.fullmatch(iso)
    if not m:
        return None
    h, mnt, s = (int(x) if x else 0 for x in m.groups())
    return h * 3600 + mnt * 60 + s
```

- [ ] **Step 4: Run test to verify it passes**

Run: `python -m pytest shopping_shorts/tests/test_youtube_channel.py::test_parse_duration_secs -v`
Expected: PASS

- [ ] **Step 5: Commit**

```bash
git -C .tracks/유튜브채널수집 add shopping_shorts/youtube_client.py shopping_shorts/tests/test_youtube_channel.py
git -C .tracks/유튜브채널수집 commit -m "feat(youtube): ISO8601 duration 파서 (≤60초 숏폼 판정용)"
```

---

## Task 2: channelId 해석 캐시 store 계층

`youtube_channel_cache` 테이블 + get/put. 해석 API 호출을 1회로 줄인다.

**Files:**
- Modify: `shopping_shorts/store.py` (스키마 line 493 뒤 + 메서드 `add_seed` 근처 line ~1108)
- Test: `shopping_shorts/tests/test_yt_cache_store.py` (신규)

- [ ] **Step 1: Write the failing test**

`shopping_shorts/tests/test_yt_cache_store.py` 생성:

```python
from shopping_shorts.store import Store


def test_yt_cache_roundtrip(tmp_path):
    s = Store(str(tmp_path / "t.db"))
    assert s.yt_cache_get("@handle") is None          # 미해석은 None
    s.yt_cache_put("@handle", "UCabc", "UUabc")
    assert s.yt_cache_get("@handle") == ("UCabc", "UUabc")


def test_yt_cache_put_overwrites(tmp_path):
    s = Store(str(tmp_path / "t.db"))
    s.yt_cache_put("@h", "UC1", "UU1")
    s.yt_cache_put("@h", "UC2", "UU2")               # 같은 seed 재해석 시 갱신
    assert s.yt_cache_get("@h") == ("UC2", "UU2")
```

- [ ] **Step 2: Run test to verify it fails**

Run: `python -m pytest shopping_shorts/tests/test_yt_cache_store.py -v`
Expected: FAIL — `AttributeError: 'Store' object has no attribute 'yt_cache_get'`

- [ ] **Step 3: Write minimal implementation**

`store.py`의 line 493 `CREATE INDEX ... idx_psnap` 바로 뒤에 테이블 추가:

```python
            c.execute("""
                CREATE TABLE IF NOT EXISTS youtube_channel_cache (
                    seed_value TEXT PRIMARY KEY,
                    channel_id TEXT,
                    uploads_playlist TEXT,
                    resolved_at TEXT
                )
            """)
```

`store.py`의 `add_seed`(line ~1108) 근처에 메서드 2개 추가:

```python
    def yt_cache_get(self, seed_value):
        """해석 캐시 조회 → (channel_id, uploads_playlist) 또는 None."""
        with self._conn() as c:
            row = c.execute("SELECT channel_id, uploads_playlist FROM youtube_channel_cache "
                            "WHERE seed_value=?", (seed_value,)).fetchone()
        return (row[0], row[1]) if row else None

    def yt_cache_put(self, seed_value, channel_id, uploads_playlist):
        """seed→채널 해석 결과 저장(재해석 시 갱신)."""
        with self._conn() as c:
            c.execute("INSERT OR REPLACE INTO youtube_channel_cache"
                      "(seed_value, channel_id, uploads_playlist, resolved_at) "
                      "VALUES(?,?,?, datetime('now'))",
                      (seed_value, channel_id, uploads_playlist))
```

- [ ] **Step 4: Run test to verify it passes**

Run: `python -m pytest shopping_shorts/tests/test_yt_cache_store.py -v`
Expected: PASS (2개)

- [ ] **Step 5: Commit**

```bash
git -C .tracks/유튜브채널수집 add shopping_shorts/store.py shopping_shorts/tests/test_yt_cache_store.py
git -C .tracks/유튜브채널수집 commit -m "feat(store): youtube_channel_cache 테이블 + get/put"
```

---

## Task 3: 채널 해석 (`_resolve_channel`)

seed(핸들/URL) → (channel_id, uploads_playlist). `/channel/UC..` URL은 API 없이 직접 파싱(0 units), `@handle`은 `channels.list?forHandle`.

**Files:**
- Modify: `shopping_shorts/youtube_client.py`
- Test: `shopping_shorts/tests/test_youtube_channel.py`

- [ ] **Step 1: Write the failing test (직접파싱 — API 미호출)**

`test_youtube_channel.py`에 추가:

```python
def test_resolve_channel_url_direct_no_api(monkeypatch):
    # /channel/UC.. URL은 정규식 직접 파싱 → requests 호출 0회, uploads=UU+id[2:]
    def boom(*a, **k):
        raise AssertionError("requests.get 호출되면 안 됨")
    monkeypatch.setattr(yc.requests, "get", boom)

    cid, uploads = yc._resolve_channel("https://www.youtube.com/channel/UCabc123def")
    assert cid == "UCabc123def"
    assert uploads == "UUabc123def"


def test_resolve_channel_handle_via_api(monkeypatch):
    # @handle → channels.list?forHandle → id + uploads 플레이리스트
    resp = {"items": [{"id": "UCxyz",
                       "contentDetails": {"relatedPlaylists": {"uploads": "UUxyz"}}}]}

    def fake_get(url, params=None, timeout=None):
        assert "channels" in url and params.get("forHandle") == "@salim"
        class R:
            status_code = 200
            def json(self_inner):
                return resp
        return R()
    monkeypatch.setattr(yc.requests, "get", fake_get)
    monkeypatch.setattr(yc, "YOUTUBE_API_KEYS", ["KEY1"])

    cid, uploads = yc._resolve_channel("@salim")
    assert cid == "UCxyz" and uploads == "UUxyz"


def test_resolve_channel_unresolvable(monkeypatch):
    # 해석 실패(빈 items) → (None, None), 예외 안 던짐
    def fake_get(url, params=None, timeout=None):
        class R:
            status_code = 200
            def json(self_inner):
                return {"items": []}
        return R()
    monkeypatch.setattr(yc.requests, "get", fake_get)
    monkeypatch.setattr(yc, "YOUTUBE_API_KEYS", ["KEY1"])

    assert yc._resolve_channel("@ghost") == (None, None)
```

- [ ] **Step 2: Run test to verify it fails**

Run: `python -m pytest shopping_shorts/tests/test_youtube_channel.py -k resolve -v`
Expected: FAIL — `_resolve_channel` 없음

- [ ] **Step 3: Write minimal implementation**

`youtube_client.py`에 추가(`_first_ok` 함수 아래). `_CHANNELS_URL`은 이미 정의돼 있음:

```python
# seed 문자열에서 채널 식별자 추출
_CH_ID = re.compile(r"/channel/(UC[A-Za-z0-9_-]{20,})")
_HANDLE = re.compile(r"@([A-Za-z0-9._-]+)")
_LEGACY_USER = re.compile(r"/user/([A-Za-z0-9._-]+)")


def _channel_from_api(param_key, param_val):
    """channels.list(forHandle|forUsername|id) → (channel_id, uploads) 또는 (None,None)."""
    data, _ = _first_ok(_CHANNELS_URL,
                        {"part": "contentDetails", param_key: param_val})
    items = (data or {}).get("items") or []
    if not items:
        return None, None
    cid = items[0].get("id")
    uploads = (((items[0].get("contentDetails") or {})
                .get("relatedPlaylists") or {}).get("uploads"))
    return cid, uploads


def _resolve_channel(seed):
    """seed(핸들/URL) → (channel_id, uploads_playlist). 실패 시 (None, None).

    /channel/UC.. URL은 API 없이 직접 파싱(uploads = UU + id[2:])."""
    if not seed:
        return None, None
    m = _CH_ID.search(seed)
    if m:
        cid = m.group(1)
        return cid, "UU" + cid[2:]          # 업로드 플레이리스트 규칙(UC→UU)
    m = _HANDLE.search(seed)
    if m:
        return _channel_from_api("forHandle", "@" + m.group(1))
    m = _LEGACY_USER.search(seed)
    if m:
        return _channel_from_api("forUsername", m.group(1))
    # 순수 핸들("salim" 등 @없음)도 forHandle로 시도
    return _channel_from_api("forHandle", "@" + seed.strip().lstrip("@"))
```

- [ ] **Step 4: Run test to verify it passes**

Run: `python -m pytest shopping_shorts/tests/test_youtube_channel.py -k resolve -v`
Expected: PASS (3개)

- [ ] **Step 5: Commit**

```bash
git -C .tracks/유튜브채널수집 add shopping_shorts/youtube_client.py shopping_shorts/tests/test_youtube_channel.py
git -C .tracks/유튜브채널수집 commit -m "feat(youtube): 채널 해석 _resolve_channel (URL 직접파싱 + @handle API)"
```

---

## Task 4: 채널 Shorts fetcher (`fetch_channel_shorts`)

업로드 플레이리스트 최근분 → videos.list로 지표·길이 채움 → ≤60초만 raw dict 반환. 캐시 콜백으로 해석 재사용.

**Files:**
- Modify: `shopping_shorts/youtube_client.py`
- Test: `shopping_shorts/tests/test_youtube_channel.py`

- [ ] **Step 1: Write the failing test**

`test_youtube_channel.py`에 추가:

```python
def _mk_get(playlist_resp, videos_resp, seen):
    def fake_get(url, params=None, timeout=None):
        seen.append(url)
        class R:
            status_code = 200
            def json(self_inner):
                if "playlistItems" in url:
                    return playlist_resp
                return videos_resp
        return R()
    return fake_get


def test_fetch_channel_shorts_schema_and_60s_filter(monkeypatch):
    # _resolve_channel은 캐시 콜백이 값을 주면 호출 안 됨
    monkeypatch.setattr(yc, "YOUTUBE_API_KEYS", ["KEY1"])
    playlist_resp = {"items": [
        {"contentDetails": {"videoId": "short1", "videoPublishedAt": "2026-07-24T00:00:00Z"}},
        {"contentDetails": {"videoId": "long1", "videoPublishedAt": "2026-07-24T00:00:00Z"}},
    ]}
    videos_resp = {"items": [
        {"id": "short1",
         "snippet": {"channelId": "UCx", "channelTitle": "살림TV", "title": "오이보관",
                     "description": "d", "publishedAt": "2026-07-24T00:00:00Z",
                     "thumbnails": {"high": {"url": "http://t/short1.jpg"}}},
         "contentDetails": {"duration": "PT45S"},
         "statistics": {"viewCount": "5000", "likeCount": "200", "commentCount": "15"}},
        {"id": "long1",
         "snippet": {"channelId": "UCx", "channelTitle": "살림TV", "title": "긴영상",
                     "description": "d", "publishedAt": "2026-07-24T00:00:00Z",
                     "thumbnails": {"high": {"url": "http://t/long1.jpg"}}},
         "contentDetails": {"duration": "PT3M"},
         "statistics": {"viewCount": "9000", "likeCount": "10", "commentCount": "1"}},
    ]}
    seen = []
    monkeypatch.setattr(yc.requests, "get", _mk_get(playlist_resp, videos_resp, seen))

    puts = []
    got = yc.fetch_channel_shorts(
        "@salim",
        cache_get=lambda s: ("UCx", "UUx"),                 # 캐시 히트 → resolve 스킵
        cache_put=lambda s, c, u: puts.append((s, c, u)))

    assert len(got) == 1                                    # 3분짜리 long1 제외
    it = got[0]
    assert it["video_id"] == "short1"
    assert it["channel_id"] == "UCx" and it["channel_title"] == "살림TV"
    assert it["views"] == 5000 and it["likes"] == 200 and it["comments"] == 15
    assert it["thumbnail"] == "http://t/short1.jpg"
    assert it["published_at"] == "2026-07-24T00:00:00Z"
    # 캐시 히트라 channels.list(해석) 호출 없음
    assert not any("channels" in u for u in seen)
    assert puts == []                                       # 히트면 put 안 함


def test_fetch_channel_shorts_resolves_and_caches(monkeypatch):
    # 캐시 미스 → _resolve_channel 호출 → cache_put 기록
    monkeypatch.setattr(yc, "_resolve_channel", lambda seed: ("UCy", "UUy"))
    playlist_resp = {"items": [
        {"contentDetails": {"videoId": "s1", "videoPublishedAt": "2026-07-24T00:00:00Z"}}]}
    videos_resp = {"items": [
        {"id": "s1",
         "snippet": {"channelId": "UCy", "channelTitle": "채널", "title": "t",
                     "description": "", "publishedAt": "2026-07-24T00:00:00Z",
                     "thumbnails": {"high": {"url": "u"}}},
         "contentDetails": {"duration": "PT30S"},
         "statistics": {"viewCount": "100", "likeCount": "2", "commentCount": "0"}}]}
    monkeypatch.setattr(yc.requests, "get", _mk_get(playlist_resp, videos_resp, []))

    puts = []
    got = yc.fetch_channel_shorts("@new", cache_get=lambda s: None,
                                  cache_put=lambda s, c, u: puts.append((s, c, u)))
    assert len(got) == 1 and got[0]["video_id"] == "s1"
    assert puts == [("@new", "UCy", "UUy")]                 # 해석 결과 캐시에 저장


def test_fetch_channel_shorts_unresolvable_returns_empty(monkeypatch):
    monkeypatch.setattr(yc, "_resolve_channel", lambda seed: (None, None))
    got = yc.fetch_channel_shorts("@ghost", cache_get=lambda s: None,
                                  cache_put=lambda s, c, u: None)
    assert got == []
```

- [ ] **Step 2: Run test to verify it fails**

Run: `python -m pytest shopping_shorts/tests/test_youtube_channel.py -k fetch -v`
Expected: FAIL — `fetch_channel_shorts` 없음

- [ ] **Step 3: Write minimal implementation**

`youtube_client.py`에 추가(`_resolve_channel` 아래). `_PLAYLIST_ITEMS_URL`은 신규 상수. `_VIDEOS_URL`은 기존 재사용:

```python
_PLAYLIST_ITEMS_URL = "https://www.googleapis.com/youtube/v3/playlistItems"


def fetch_channel_shorts(seed, max_videos=50, cache_get=None, cache_put=None):
    """채널 시드(핸들/URL)의 최근 Shorts(≤60초) → search_shorts와 동일한 raw dict 리스트.

    cache_get(seed)->(cid,uploads)|None / cache_put(seed,cid,uploads): 해석 캐시 콜백(선택).
    창(14일) 필터는 하지 않는다 — build_youtube_items가 window_hours로 거른다.
    비공개·삭제·해석실패 채널은 빈 리스트(예외 안 던짐)."""
    resolved = cache_get(seed) if cache_get else None
    if resolved:
        cid, uploads = resolved
    else:
        cid, uploads = _resolve_channel(seed)
        if uploads and cache_put:
            cache_put(seed, cid, uploads)
    if not uploads:
        return []

    pl, _ = _first_ok(_PLAYLIST_ITEMS_URL, {
        "part": "contentDetails", "playlistId": uploads,
        "maxResults": min(max_videos, 50)})
    vids = [((it.get("contentDetails") or {}).get("videoId"))
            for it in ((pl or {}).get("items") or [])]
    vids = [v for v in vids if v]
    if not vids:
        return []

    out = []
    for i in range(0, len(vids), 50):                       # videos.list 상한 50
        chunk = vids[i:i + 50]
        vd, _ = _first_ok(_VIDEOS_URL, {
            "part": "snippet,contentDetails,statistics", "id": ",".join(chunk)})
        for it in ((vd or {}).get("items") or []):
            secs = _parse_duration_secs((it.get("contentDetails") or {}).get("duration"))
            if secs is None or secs > 60:                   # 숏폼(≤60초)만
                continue
            sn = it.get("snippet") or {}
            st = it.get("statistics") or {}
            out.append({
                "video_id": it.get("id"),
                "channel_id": sn.get("channelId"),
                "channel_title": sn.get("channelTitle"),
                "title": sn.get("title"),
                "description": sn.get("description"),
                "thumbnail": ((sn.get("thumbnails") or {}).get("high") or {}).get("url", ""),
                "published_at": sn.get("publishedAt"),
                "views": int(st.get("viewCount") or 0),
                "likes": int(st.get("likeCount") or 0),
                "comments": int(st.get("commentCount") or 0),
            })
    return out
```

- [ ] **Step 4: Run test to verify it passes**

Run: `python -m pytest shopping_shorts/tests/test_youtube_channel.py -v`
Expected: PASS (전체: 파서1 + resolve3 + fetch3 = 7개)

- [ ] **Step 5: Commit**

```bash
git -C .tracks/유튜브채널수집 add shopping_shorts/youtube_client.py shopping_shorts/tests/test_youtube_channel.py
git -C .tracks/유튜브채널수집 commit -m "feat(youtube): fetch_channel_shorts (업로드플레이리스트→≤60초 raw dict, 해석캐시)"
```

---

## Task 5: `_collect_youtube` 계정 경로 병합 + 중복제거

계정 시드 경로를 키워드 경로와 함께 돌리고, video_id로 중복제거해 build로 넘긴다.

**Files:**
- Modify: `shopping_shorts/service.py` (`_collect_youtube`, line ~90-121; import line ~15)
- Test: `shopping_shorts/tests/test_collect_youtube.py`

- [ ] **Step 1: Write the failing test**

`test_collect_youtube.py`에 테스트 추가(기존 `test_collect_youtube`는 유지):

```python
def test_collect_youtube_merges_account_and_keyword(monkeypatch, tmp_path):
    monkeypatch.setattr(service, "DB_PATH", str(tmp_path / "t.db"))
    from shopping_shorts.store import Store
    st = Store(str(tmp_path / "t.db"))
    st.add_seed("youtube", "keyword", "살림꿀팁")        # 키워드 시드
    st.add_seed("youtube", "account", "@salim")         # 계정 시드

    # 키워드 경로: v1 + 겹치는 dup
    def fake_search(keywords, published_after_iso, **kw):
        return [
            {"video_id": "v1", "channel_title": "A", "channel_id": "c1", "title": "t",
             "thumbnail": "x", "published_at": "2026-07-24T00:00:00Z",
             "views": 1000, "likes": 5, "comments": 1},
            {"video_id": "dup", "channel_title": "A", "channel_id": "c1", "title": "t",
             "thumbnail": "x", "published_at": "2026-07-24T00:00:00Z",
             "views": 1000, "likes": 5, "comments": 1},
        ]
    monkeypatch.setattr(service, "yt_search", fake_search)

    # 계정 경로: v2 + 겹치는 dup(같은 video_id → 1개로 합쳐져야)
    def fake_channel(seed, **kw):
        assert seed == "@salim"
        return [
            {"video_id": "v2", "channel_title": "B", "channel_id": "c2", "title": "t",
             "thumbnail": "x", "published_at": "2026-07-24T00:00:00Z",
             "views": 2000, "likes": 9, "comments": 3},
            {"video_id": "dup", "channel_title": "B", "channel_id": "c2", "title": "t",
             "thumbnail": "x", "published_at": "2026-07-24T00:00:00Z",
             "views": 2000, "likes": 9, "comments": 3},
        ]
    monkeypatch.setattr(service, "yt_fetch_channel", fake_channel)

    items = service.collect(platform="youtube")
    ids = sorted(i["shortcode"] for i in items)
    assert ids == ["dup", "v1", "v2"]                   # dup 중복제거 → 3개
```

- [ ] **Step 2: Run test to verify it fails**

Run: `python -m pytest shopping_shorts/tests/test_collect_youtube.py -v`
Expected: FAIL — `service`에 `yt_fetch_channel` 속성 없음(AttributeError)

- [ ] **Step 3: Write minimal implementation**

`service.py` import(line ~15) 수정 — `fetch_channel_shorts`를 별칭으로 추가:

```python
from shopping_shorts.youtube_client import search_shorts as yt_search, fetch_channel_shorts as yt_fetch_channel
```

`service.py` `_collect_youtube`(line ~90-121) 교체. 키워드 raw 수집 뒤에 계정 경로·dedupe 삽입:

```python
def _collect_youtube():
    """유튜브 키워드 시드(발굴) + 계정 시드(채널기반) → 조회수 기반 랭킹(무료 Data API).

    키워드 시드 kind=언어코드(ko/en/ja/zh/ru), 예전 kind="keyword"는 ko로 취급.
    계정 시드 kind="account"(핸들/URL) → fetch_channel_shorts로 최근 Shorts(≤60초).
    두 경로 raw를 video_id로 중복제거 후 공통 파이프라인."""
    store = Store(DB_PATH)
    seeds = store.list_seeds("youtube")
    if not seeds:
        return []
    _LANGS = {"ko", "en", "ja", "zh", "ru"}
    by_lang = {}
    accounts = []
    for s in seeds:
        if s["kind"] == "account":
            accounts.append(s["value"])
        else:
            lang = s["kind"] if s["kind"] in _LANGS else "ko"
            by_lang.setdefault(lang, []).append(s["value"])
    now = datetime.now(timezone.utc)
    after = (now - timedelta(hours=YOUTUBE_WINDOW_HOURS)).strftime("%Y-%m-%dT%H:%M:%SZ")
    raw = []
    for lang, kws in by_lang.items():
        raw.extend(yt_search(kws, after, max_per_kw=YOUTUBE_MAX_PER_KW, lang=lang))
    for acc in accounts:
        raw.extend(yt_fetch_channel(acc, cache_get=store.yt_cache_get,
                                    cache_put=store.yt_cache_put))
    # video_id 중복제거(키워드·계정 양쪽서 같은 영상이 나올 수 있음) — 첫 등장 우선
    seen, deduped = set(), []
    for r in raw:
        vid = r.get("video_id")
        if not vid or vid in seen:
            continue
        seen.add(vid)
        deduped.append(r)
    items = build_youtube_items(
        deduped,
        prev_base=lambda sc: store.prev_base_platform("youtube", sc),
        prev_delta=lambda sc: store.prev_delta_platform("youtube", sc),
        now=now, window_hours=YOUTUBE_WINDOW_HOURS,
    )
    apply_grades(items)
    run_date = now.strftime("%Y-%m-%d %H:%M")
    store.save_run_platform("youtube", run_date,
                            [{"shortcode": i["shortcode"], "base": i["base_count"], "delta": i["delta"]} for i in items])
    store.save_last_run_platform("youtube", items, now.isoformat())
    return items
```

- [ ] **Step 4: Run test to verify it passes**

Run: `python -m pytest shopping_shorts/tests/test_collect_youtube.py -v`
Expected: PASS (기존 `test_collect_youtube` + 신규 `test_collect_youtube_merges_account_and_keyword`)

- [ ] **Step 5: Commit**

```bash
git -C .tracks/유튜브채널수집 add shopping_shorts/service.py shopping_shorts/tests/test_collect_youtube.py
git -C .tracks/유튜브채널수집 commit -m "feat(youtube): _collect_youtube에 계정 채널기반 경로 병합 + video_id 중복제거"
```

---

## Task 6: 전체 회귀 확인 + import 스모크

**Files:** 없음(검증만)

- [ ] **Step 1: youtube 관련 테스트 전체 실행**

Run: `python -m pytest shopping_shorts/tests/test_youtube_channel.py shopping_shorts/tests/test_yt_cache_store.py shopping_shorts/tests/test_collect_youtube.py shopping_shorts/tests/test_youtube_client.py -v`
Expected: 전부 PASS

- [ ] **Step 2: app import 스모크(finish 게이트와 동일 체크)**

Run: `python -c "import shopping_shorts.app"`
Expected: 에러 없음(exit 0)

- [ ] **Step 3: 기존 틱톡·인스타 수집 테스트 회귀 확인**

Run: `python -m pytest shopping_shorts/tests/test_collect_tiktok_free_only.py -v`
Expected: PASS(내 변경이 다른 플랫폼 경로를 안 건드렸음 확인)

---

## Self-Review 결과 (작성자 체크)

- **스펙 커버리지**: §3 아키텍처=Task5, §4 fetcher=Task1·3·4, §5 캐시=Task2, §6 collect변경=Task5, §7 에러(빈리스트 스킵)=Task4, §9 테스트 전부 매핑됨. §8 UI=신규없음(태스크 불요). ✅
- **플레이스홀더**: 없음(모든 코드 완전). ✅
- **타입 일관성**: `fetch_channel_shorts(seed, max_videos, cache_get, cache_put)` 시그니처 Task4 정의 = Task5 호출 일치. raw dict 키 = build_youtube_items 입력(`ranking.py:110-147`)과 일치. `yt_cache_get/put` Task2 정의 = Task5 사용 일치. ✅
- **범위**: 단일 플랜으로 충분(한 기능). ✅

## 실기 검증(구현 후, 별도 세션 — 계획 밖 참고)

로컬은 실 YOUTUBE_API_KEY·실 채널 필요. 구현·머지 후 서버 대시보드에서 관리자 로그인 →
`/refs` 유튜브 탭에 채널 등록 → 랭킹 `▶️유튜브` 탭 「지금 수집」 → 카드 뜨는지 육안 확인.
