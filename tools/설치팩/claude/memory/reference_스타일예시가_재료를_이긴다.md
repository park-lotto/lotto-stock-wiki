---
name: ""
description: 대본이 엉뚱한 제품으로 새면 코드 말고 프롬프트 분량부터 재라 — 스타일 블록의 히트대본 3편에 소재 격리 경고가 없었다
metadata: 
  node_type: memory
  type: reference
  originSessionId: 54d08b35-a43f-49b5-838c-d2dd03c7816e
  modified: 2026-09-07T06:48:36.902Z
---

2026-09-07 사장님 제보 "2단계 AI생성 누르면 완전 이상한 주제가 나온다".
재료 3편이 전부 '디지털 모래시계'인데 대본은 "3D 요술봉 카드케이스 / 여행 좋아하는 친구".

**판정은 코드 읽기가 아니라 프롬프트 분량 실측이었다** (work `01e725b98569`):

```
우리 재료(it.full_text)        233자
bank_assemble 은행           1,832자  ← 끝에 "여기 제품은 우리 소재가 아니다" 있음
style_profiles.style_block() 1,985자  ← ★경고 0줄. 대신 "★★★최우선 · 충돌하면 이 스타일이 이긴다"
```

스타일 블록은 **남의 히트 대본 3편 전문**(무인양품 지퍼백·불멸의 얼음·책상PC)을 통째로
싣는다. 남의 제품 이야기가 재료의 **16배**로, 스스로 "내가 이긴다"고 선언한 채 들어간다.
부품은행에는 처음부터 격리 문구가 있었고 스타일 블록에만 없었다 = 0순위-B.

**갈라보기가 답을 줬다**: 같은 요청·같은 재료인데 스타일 경로 안은 정상("잠깐, 디지털
모래시계 하나로…"), 픽업 경로 안만 이탈. 차이는 픽업 경로가 `_materials_for_generate`를
안 부르고 은행 예산(`source_chars`)도 안 건다는 것 — 2026-08-18 "재료 750자 vs 은행
2,822자" 사고 때 **스타일 경로만** 고치고 픽업 경로를 안 고쳤다.

**두 번째 오염(별건)**: `spine` 26~45번 20개가 구조 템플릿이 아니라 특정 제품 완성 대본이었다
(`situation_type`이 전부 `우승패턴: <제품명>`, 예: 38번 비트 "NFC 카드를 넣어 갖다 대면
결제되는 {3D 요술봉} 케이스 활용"). approved로 라이브에 있었다 → pending 해제.
백업 서버 `~/spine_backup_2026-09-07.json`. 남은 approved 15개는 전부 추상 구조.

## 다음에 같은 제보를 받으면
1. 그 work의 `produce_works.state_json` + `mix_jobs.extract_json`으로 **재료부터** 확인 —
   재료가 멀쩡하면 코드가 아니라 프롬프트 문제다.
2. `s2.drafts`의 `style_id` 유무로 어느 경로 산출물인지 가른다(있음=스타일, 없음=픽업).
3. 서버에서 `style_block()` / `assemble_bank_context()`를 실제로 조립해 **글자수를 재라**.
   재료보다 크면 그게 원인이다.
4. 프롬프트에 남의 대본을 싣는 자리를 새로 만들 때는 **격리 경고를 짝으로** 붙여라.

관련: [[조립vs생성기_빈칸이_문장을_깬다]] [[silent_fallback_pipeline_undo]]
