# 버텍스영상 — Vertex 키 발급 안내 영상(리모션) + 설명서 최신화 (2026-09-27, 사장님 PC)

사장님: "버텍스 api 내키등록에 그림으로 보는 발급 → 간단한 영상으로 리모션" / "너가 직접 플레이라이트로" / "알아서 끝까지해 오류없이"

## 한 것
- 실제 구글 콘솔을 Playwright로 돌며 캡처(새 계정=가입·2단계인증 / 카드 결제된 계정=API·서비스계정·키). 캡처 원본은 main 폴더 `.playwright-mcp/vertex_cap/`(git 안 감, 개인정보 있음).
- `tools/vertex_guide/prep.py`: 장면별 자르기+개인정보 모자이크+강조상자 좌표 → scenes.json
- `tools/vertex_guide/remotion/`: 1920×1080 영상(12장면+인트로/아웃로, 78.7초, 무음, 5.1MB) → `shopping_shorts/static/landing/vertex_guide.mp4`(+jpg 포스터). node_modules는 `shopping_shorts/motion/node_modules` 정션(gitignore).
  렌더: `node tools/vertex_guide/remotion/render.mjs <prep출력/public> <out.mp4>`
- `tools/vertex_guide/capture_settings.py`: 5단계 설정 카드 캡처(격리 DB, keycrypt만 켬, 가짜 JSON)
- 설명서 api_manual.html ⑧: 영상 카드 + **구글이 바꾼 이름 반영**(Vertex AI API→Agent Platform API, Vertex AI 사용자→Agent Platform 사용자) + **2단계 인증(MFA) 필수 카드 신설**(2026-09-22부터 차단 화면) + 계좌 유형 「개인」 + 메뉴 경로(마이페이지 › 🔑 내 키 등록)
- settings.html Vertex 카드에 「▶ 받는 방법 영상」(GUIDE_VIDEO.vertex)
- 검사 `tools/vertex_guide/check_pages.py`: 영상 실재생·새문구·비로그인 200·버튼 새탭·콘솔오류0 — 새 코드 전부 통과 / 옛 페이지 ①실패

## 사장님 계정에 남은 것
- 카드 결제된 계정 프로젝트 My First Project에 시험 서비스계정 `shorts-guide`(역할 Agent Platform 사용자, **키 없음**) — 지워도 됨(사장님 판단).
- 새 계정(parklotto7021): 무료체험 약관 동의만, 결제정보 미입력(가입 미완료).

## 발견(다른 트랙 몫)
- `shopping_shorts/motion/src/Root.tsx`에 병합 충돌 표식이 09-15 `auto: session changes`(6c178cd34)부터 main에 커밋돼 있어 기존 리모션 번들이 깨져 있다. 서버에서 이걸 쓰는 경로가 있는지 미확인.

## 2026-09-27 추가 — "카드 결제 안 되게" 안내 + 영상 되감기 안 됨
- 되감기: 서버 starlette 0.36.3 FileResponse가 Range 무시(실측 라이브 Range 요청에 200 전체) → 정적 마운트(_NoCacheStaticFiles)도
  기존 `_range_media_response`(08-15 API용)를 타게 함(_RANGE_STATIC_EXTS). /landing의 다른 영상(hero_wall 등)도 같이 고쳐짐.
  test_static_media_range.py(스파이로 헬퍼 경유 확인 — 로컬 starlette 1.3.1은 원래 Range 지원이라 응답만 보면 옛 코드도 통과함) 새 코드 통과/옛 코드 실패.
- 과금 차단: 구글 공식 문서 확인(체험 중 청구 없음·업그레이드 안 하면 자동 결제 없이 종료·예산 알림은 안 막음·결제 계정 폐쇄=그때까지만 청구).
  영상에 💳 두 장면(결제 개요 「업그레이드」 누르지 말 것/「무료 체험판 계정」 확인, 계정 관리 「결제 계정 폐쇄」 — 화면 실제 문구는 '폐쇄') + 설명서 카드.
  멈추면 대본은 키풀 폴백(vertex_route.py:315), AI 장면 생성만 불가.
- 영상 90.7초·6.1MB. check_pages ⑤ 앞·뒤 이동 추가.
