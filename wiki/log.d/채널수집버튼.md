# 채널수집버튼 로그

## 2026-08-18
- `📌 채널수집` 버튼을 인스타·틱톡 → **유튜브·쓰레드까지** 확장. 대상 판정은 유저스크립트
  한 곳, 어느 표에 넣을지는 서버(`add_by_url`) 한 곳(0순위-B).
- ★쓰레드는 담기도 안 되던 상태였다 — 서버는 이미 지원했는데 로더 `@match`·확장
  manifest에 도메인이 없어 스크립트가 아예 안 돌았다. 두 곳 다 추가(로더 v2.4.0 →
  **한 번 재설치 필요**).
- 함정: 로직 원본은 `userscript/grab_logic.js`고 `extension/grab_logic.js`는 08-03 사본이다.
- 테스트 11개 신규(pytest 6 + node 실행 5) 전부 통과.
