# 자막제거 — 소스 단위 청소 + 2단계 미리보기 썸네일 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 영상제작 2단계에서 소스 원본을 VMake로 자막제거해 캐시하고, 원본↔클린 비교 썸네일을 버튼으로 보여준다. 최종렌더는 캐시된 클린 소스를 재사용해 재과금 0.

**Architecture:** 자막제거는 소스 픽셀 속성이므로 mix_raw가 아니라 **소스 원본 단위**로 청소한다. `clean_sources`(`{video_id: 클린경로}`) 캐시에 담고, `run_render`는 `subtitle_removal`이 켜져 있으면 원본 대신 클린 소스로 조립한다(clean_fn 폐기). 폴링·상태는 기존 1단계 미리보기 배선(`preview_status`)의 판박이로 `clean_status`를 둔다.

**Tech Stack:** FastAPI, SQLite(store.py), ffmpeg(frame_extract.py), 순수 JS(produce.html), pytest + FastAPI TestClient + node --check.

**작업 폴더:** 모든 편집·git은 `.tracks/자막제거미리보기/` 안에서. 커밋 전 `git -C .tracks/자막제거미리보기 status --porcelain`로 내 파일만인지 확인.

**스펙:** `shopping_shorts/docs/superpowers/specs/2026-07-19-자막제거-소스단위-미리보기-design.md`
설계 대비 정제 1건: 폴링 상태는 **별도 GET 라우트 대신 기존 `/api/mix/status`에 얹는다**(코드 선례 §6.3 — "폴러를 둘로 만들지 않으려고").

---

## 파일 구조

- **Modify** `shopping_shorts/store.py` — mix_jobs에 `clean_sources_json`·`clean_status`·`clean_error` 컬럼 + get/update 배선 (Task 1)
- **Modify** `shopping_shorts/mix_pipeline.py` — `_clean_one`·`_ensure_clean_sources`·`run_clean_sources` 추가 + `run_render` 소스단위 전환 (Task 2·3)
- **Modify** `shopping_shorts/app.py` — `/api/produce/mix/clean`(POST)·`clean_thumb`(GET) 라우트 + `/api/mix/status`에 clean 필드 (Task 4·5)
- **Modify** `shopping_shorts/static/produce.html` — 2단계 버튼·비교 UI·폴링 JS (Task 6)
- **Test** `tests/test_clean_sources.py`(신규), `tests/test_app_clean.py`(신규), `tests/test_mix_render_flow.py`(수정), `tests/test_clean_wizard_js.py`(신규)

---

## Task 1: store.py — clean 필드 영속화

**Files:**
- Modify: `shopping_shorts/store.py:495-519` (마이그레이션), `:1517-1546` (get_mix_job), `:1548-1589` (update_mix_job)
- Test: `shopping_shorts/tests/test_clean_sources.py`

- [ ] **Step 1: 실패 테스트 작성**

`tests/test_clean_sources.py` 생성:

```python
from shopping_shorts.store import Store


def test_clean_fields_roundtrip(tmp_path):
    db = str(tmp_path / "t.db")
    s = Store(db)
    s.create_mix_job("j1", ["u"], 30, "template")
    # 초기값
    job = s.get_mix_job("j1")
    assert job["clean_status"] is None
    assert job["clean_sources"] is None
    assert job["clean_error"] is None
    # 갱신 후 왕복
    s.update_mix_job("j1", clean_status="ready", clean_error=None,
                     clean_sources={"s0": "/tmp/clean_src_s0.mp4"})
    job = s.get_mix_job("j1")
    assert job["clean_status"] == "ready"
    assert job["clean_sources"] == {"s0": "/tmp/clean_src_s0.mp4"}


def test_clean_status_failed_persists(tmp_path):
    s = Store(str(tmp_path / "t.db"))
    s.create_mix_job("j2", ["u"], 30, "template")
    s.update_mix_job("j2", clean_status="failed", clean_error="키 없음")
    job = s.get_mix_job("j2")
    assert job["clean_status"] == "failed"
    assert job["clean_error"] == "키 없음"
```

- [ ] **Step 2: 실패 확인**

Run: `python -m pytest shopping_shorts/tests/test_clean_sources.py -q`
Expected: FAIL — `KeyError: 'clean_status'` (get_mix_job에 키 없음)

- [ ] **Step 3: 마이그레이션 컬럼 추가**

`store.py`의 마이그레이션 튜플(현재 `("seo_json", "TEXT"),`로 끝나는 목록, :519)에 이어 추가:

```python
                ("seo_json", "TEXT"),
                # 자막제거 소스단위 청소 캐시(2026-07-19). clean_sources_json={video_id: 클린경로}.
                # status/preview_status와 섞지 않는다 — 최종렌더 폴링과 오인 방지(선례 §6.1).
                ("clean_sources_json", "TEXT"),
                ("clean_status", "TEXT"),   # null|cleaning|ready|failed
                ("clean_error", "TEXT"),
```

- [ ] **Step 4: get_mix_job에 SELECT·매핑 추가**

`get_mix_job`의 SELECT 끝(`"thumbnail_json, seo_json "`)을 확장:

```python
                "thumbnail_json, seo_json, "
                "clean_sources_json, clean_status, clean_error "
```

반환 dict의 `"seo": ...` 줄 다음에 추가:

```python
            "seo": json.loads(row[26]) if row[26] else None,
            "clean_sources": json.loads(row[27]) if row[27] else None,
            "clean_status": row[28], "clean_error": row[29],
        }
```

- [ ] **Step 5: update_mix_job 화이트리스트에 추가**

스칼라 화이트리스트(`"preview_status", "preview_path", "preview_error"):`)에 `clean_status`·`clean_error` 추가:

```python
                  "preview_status", "preview_path", "preview_error",
                  "clean_status", "clean_error"):
```

그리고 JSON 직렬화 블록(`if "seo" in fields:` 다음)에 clean_sources 추가:

```python
        if "clean_sources" in fields:
            cols.append("clean_sources_json=?")
            vals.append(json.dumps(fields["clean_sources"], ensure_ascii=False)
                        if fields["clean_sources"] else None)
```

- [ ] **Step 6: 통과 확인**

Run: `python -m pytest shopping_shorts/tests/test_clean_sources.py -q`
Expected: PASS (2 passed)

- [ ] **Step 7: 커밋**

```bash
git -C .tracks/자막제거미리보기 add shopping_shorts/store.py shopping_shorts/tests/test_clean_sources.py
git -C .tracks/자막제거미리보기 commit -m "feat(store): mix_jobs clean_sources·clean_status·clean_error 영속화"
```

---

## Task 2: mix_pipeline.py — run_clean_sources + 공유 헬퍼

**Files:**
- Modify: `shopping_shorts/mix_pipeline.py` (import 근처 + `run_preview` 정의 앞)
- Test: `shopping_shorts/tests/test_clean_sources.py` (이어서 추가)

- [ ] **Step 1: 실패 테스트 작성**

`tests/test_clean_sources.py`에 추가(파일 상단에 `from shopping_shorts import mix_pipeline as mp` 추가):

```python
class _FakeStore:
    def __init__(self, job):
        self._job = job
        self.updates = []
    def get_mix_job(self, _):
        return self._job
    def update_mix_job(self, _, **f):
        self.updates.append(f); self._job.update(f)
    def get_setting(self, *_a, **_k):
        return "appkey:secret"


def _patch_clean(monkeypatch, remove_calls):
    monkeypatch.setattr(mp, "_resolve_sources", lambda job, work: {"s0": "/orig/s0.mp4"})
    def fake_remove(video, key, out_path, **kw):
        remove_calls.append(video); open(out_path, "w").write("clean"); return out_path
    monkeypatch.setattr(mp, "remove_subtitles", fake_remove)


def test_run_clean_sources_cleans_and_caches(monkeypatch, tmp_path):
    job = {"job_id": "j", "urls": ["u"]}
    store = _FakeStore(job)
    monkeypatch.setattr(mp, "Store", lambda p: store)
    calls = []
    _patch_clean(monkeypatch, calls)
    mp.run_clean_sources("j", "db", str(tmp_path))
    assert calls == ["/orig/s0.mp4"]                 # 원본 소스에 돌았다
    assert job["clean_status"] == "ready"
    assert list(job["clean_sources"].keys()) == ["s0"]


def test_run_clean_sources_no_key_fails(monkeypatch, tmp_path):
    job = {"job_id": "j", "urls": ["u"]}
    store = _FakeStore(job)
    store.get_setting = lambda *a, **k: ""            # 키 없음
    monkeypatch.setattr(mp, "Store", lambda p: store)
    _patch_clean(monkeypatch, [])
    mp.run_clean_sources("j", "db", str(tmp_path))
    assert job["clean_status"] == "failed"
    assert "VMake" in job["clean_error"]


def test_ensure_clean_sources_skips_cached(monkeypatch, tmp_path):
    existing = tmp_path / "clean_src_s0.mp4"; existing.write_text("x")
    job = {"job_id": "j", "urls": ["u"], "clean_sources": {"s0": str(existing)}}
    store = _FakeStore(job)
    calls = []
    _patch_clean(monkeypatch, calls)
    out = mp._ensure_clean_sources(store, job, "j", tmp_path, "appkey:secret")
    assert calls == []                               # 캐시 존재 → 재호출 0
    assert out == {"s0": str(existing)}
```

- [ ] **Step 2: 실패 확인**

Run: `python -m pytest shopping_shorts/tests/test_clean_sources.py -q`
Expected: FAIL — `AttributeError: module ... has no attribute 'run_clean_sources'`

- [ ] **Step 3: 헬퍼·함수 구현**

`mix_pipeline.py` 상단 import에 ThreadPoolExecutor가 이미 있음(확인: `from concurrent.futures import ThreadPoolExecutor`). `run_preview` 정의(현재 271행) **바로 앞**에 추가:

```python
def _clean_one(item, key, work):
    """소스 하나를 VMake로 청소 → (video_id, 클린경로). ThreadPool 워커용(DB 미접근)."""
    vid, src = item
    out = str(Path(work) / f"clean_src_{vid}.mp4")
    return vid, remove_subtitles(src, key, out_path=out)


def _ensure_clean_sources(store, job, job_id, work, key):
    """clean_sources 맵을 채워 반환. 이미 있고 파일이 존재하면 스킵(재과금 0).
    각 스레드는 remove_subtitles만 하고 경로를 반환 → DB 저장은 취합 후 메인에서 1회(경합 없음)."""
    source_map = _resolve_sources(job, Path(work))
    cached = dict(job.get("clean_sources") or {})
    todo = [(vid, src) for vid, src in source_map.items()
            if not (cached.get(vid) and Path(cached[vid]).exists())]
    if todo:
        with ThreadPoolExecutor(max_workers=len(todo)) as ex:
            for vid, out in ex.map(lambda t: _clean_one(t, key, work), todo):
                cached[vid] = out
        store.update_mix_job(job_id, clean_sources=cached)
    return cached


def run_clean_sources(job_id, db_path, work_root):
    """2단계: 각 소스 원본을 VMake로 자막제거해 clean_sources에 캐시.
    BackgroundTasks로 불리므로 예외를 밖으로 안 던진다(clean_status로만 알린다)."""
    store = Store(db_path)
    job = store.get_mix_job(job_id)
    if not job:
        return
    try:
        work = Path(work_root) / job_id
        work.mkdir(parents=True, exist_ok=True)
        key = _vmake_key(store)
        if not key:
            store.update_mix_job(job_id, clean_status="failed",
                                 clean_error="VMake 개인키가 등록되지 않았습니다")
            return
        _ensure_clean_sources(store, job, job_id, work, key)
        store.update_mix_job(job_id, clean_status="ready", clean_error=None)
    except Exception as e:  # noqa: BLE001 — BackgroundTasks라 밖에서 아무도 안 받는다
        traceback.print_exc(file=sys.stderr)
        store.update_mix_job(job_id, clean_status="failed", clean_error=str(e))
```

- [ ] **Step 4: 통과 확인**

Run: `python -m pytest shopping_shorts/tests/test_clean_sources.py -q`
Expected: PASS (5 passed)

- [ ] **Step 5: 커밋**

```bash
git -C .tracks/자막제거미리보기 add shopping_shorts/mix_pipeline.py shopping_shorts/tests/test_clean_sources.py
git -C .tracks/자막제거미리보기 commit -m "feat(mix): run_clean_sources — 소스 원본 단위 VMake 청소+캐시"
```

---

## Task 3: run_render 소스단위 전환 (clean_fn 폐기)

**Files:**
- Modify: `shopping_shorts/mix_pipeline.py:328-372` (`run_render`의 clean_fn 블록)
- Test: `shopping_shorts/tests/test_mix_render_flow.py:21-60` (기존 2개 테스트 수정)

- [ ] **Step 1: 기존 테스트를 새 계약으로 수정**

`test_mix_render_flow.py`의 `test_render_off_skips_vmake`·`test_render_on_calls_vmake`를 아래로 **교체**(clean_fn 인자 제거, 소스 경로 검증):

```python
def test_render_off_uses_original_sources(monkeypatch, tmp_path):
    job = {"job_id": "j", "urls": ["u"], "edit_plan": {"beats": [{"beat_idx": 0, "tts_path": "t"}]},
           "subtitle_removal": False}
    store = FakeStore(job)
    monkeypatch.setattr(mp, "Store", lambda p: store)
    monkeypatch.setattr(mp, "_resolve_sources", lambda job, work: {"s0": "orig.mp4"})
    monkeypatch.setattr(mp, "remove_subtitles", lambda *a, **k: (_ for _ in ()).throw(AssertionError("불려선 안 됨")))
    seen = {}
    def fake_assemble(plan, tts, sources, out, clean_fn=None, **kw):
        seen["sources"] = sources; seen["clean_fn"] = clean_fn
        open(out, "w").write("v"); return out
    monkeypatch.setattr(mp, "assemble", fake_assemble)

    mp.run_render("j", str(tmp_path / "db"), str(tmp_path))
    assert seen["sources"] == {"s0": "orig.mp4"}          # 원본 그대로
    assert seen["clean_fn"] is None                        # clean_fn 폐기
    assert _statuses(store)[-1] == "done"


def test_render_on_uses_clean_sources(monkeypatch, tmp_path):
    job = {"job_id": "j", "urls": ["u"], "edit_plan": {"beats": [{"beat_idx": 0, "tts_path": "t"}]},
           "subtitle_removal": True}
    store = FakeStore(job)
    monkeypatch.setattr(mp, "Store", lambda p: store)
    monkeypatch.setattr(mp, "_resolve_sources", lambda job, work: {"s0": "orig.mp4"})
    monkeypatch.setattr(mp, "_vmake_key", lambda store: "appkey:secret")
    removed = []
    def fake_remove(video, key, out_path, **kw):
        removed.append(video); open(out_path, "w").write("clean"); return out_path
    monkeypatch.setattr(mp, "remove_subtitles", fake_remove)
    seen = {}
    def fake_assemble(plan, tts, sources, out, clean_fn=None, **kw):
        seen["sources"] = sources; seen["clean_fn"] = clean_fn
        open(out, "w").write("v"); return out
    monkeypatch.setattr(mp, "assemble", fake_assemble)

    mp.run_render("j", str(tmp_path / "db"), str(tmp_path))
    assert removed == ["orig.mp4"]                         # 원본 소스에 청소
    assert seen["clean_fn"] is None                        # mix_raw clean_fn 아님
    assert list(seen["sources"].values())[0].endswith("clean_src_s0.mp4")  # 클린 소스로 조립
    assert job["clean_status"] == "ready"
```

- [ ] **Step 2: 실패 확인**

Run: `python -m pytest shopping_shorts/tests/test_mix_render_flow.py -q`
Expected: FAIL — `test_render_on_uses_clean_sources`에서 sources가 아직 원본(orig.mp4)

- [ ] **Step 3: run_render 수정**

`run_render` 안에서 `source_video_paths = _resolve_sources(job, work)` 다음의 **clean_fn 블록 전체**(현재 331–341행: `clean_fn = None` ~ `return out`)를 아래로 교체:

```python
        source_video_paths = _resolve_sources(job, work)
        # 자막제거: 소스 원본을 미리(2단계) 또는 여기서(버튼 미사용 시) 청소해 그 소스로 조립한다.
        # mix_raw 위 clean_fn(구방식)은 폐기 — 소스단위여야 TTS/컷과 무관하게 캐시가 성립한다.
        if job.get("subtitle_removal"):
            key = _vmake_key(store)
            if not key:
                raise RuntimeError("자막 제거가 켜져 있으나 VMake 개인키가 등록되지 않았습니다")
            clean_map = _ensure_clean_sources(store, job, job_id, work, key)
            store.update_mix_job(job_id, clean_status="ready", clean_error=None)
            source_video_paths = {vid: clean_map.get(vid, p)
                                  for vid, p in source_video_paths.items()}
```

그리고 아래 `assemble(...)` 호출의 `clean_fn=clean_fn`를 `clean_fn=None`로 바꾼다(현재 370행):

```python
        assemble(plan, tts_paths, source_video_paths, str(out_path), clean_fn=None,
                 headcopy=job.get("headcopy"), caption_style=caption_style,
                 deco=deco, cutaway_paths=cutaway_paths)
```

- [ ] **Step 4: 통과 확인**

Run: `python -m pytest shopping_shorts/tests/test_mix_render_flow.py shopping_shorts/tests/test_clean_sources.py -q`
Expected: PASS (전부)

- [ ] **Step 5: 커밋**

```bash
git -C .tracks/자막제거미리보기 add shopping_shorts/mix_pipeline.py shopping_shorts/tests/test_mix_render_flow.py
git -C .tracks/자막제거미리보기 commit -m "refactor(mix): 최종렌더 자막제거를 소스단위 클린 재사용으로 전환(clean_fn 폐기)"
```

---

## Task 4: app.py — /clean 예약 라우트 + status 폴링 필드

**Files:**
- Modify: `shopping_shorts/app.py:39-40` (import), `:1315-1332` (status 라우트), `:1467` 근처(preview 라우트 뒤에 신규)
- Test: `shopping_shorts/tests/test_app_clean.py`

- [ ] **Step 1: 실패 테스트 작성**

`tests/test_app_clean.py` 생성:

```python
from fastapi.testclient import TestClient
from shopping_shorts import app as appmod


def _client(tmp_path, monkeypatch):
    monkeypatch.setattr(appmod, "DB_PATH", str(tmp_path / "t.db"))
    return TestClient(appmod.app)


def _job_with_plan(c, monkeypatch):
    appmod.Store(appmod.DB_PATH).create_mix_job("j", ["u"], 30, "template")
    appmod.Store(appmod.DB_PATH).update_mix_job("j", edit_plan={"beats": [{"beat_idx": 0}]})


def test_clean_schedules_and_marks_cleaning(tmp_path, monkeypatch):
    c = _client(tmp_path, monkeypatch)
    _job_with_plan(c, monkeypatch)
    monkeypatch.setattr(appmod, "run_clean_sources", lambda *a, **k: None)
    r = c.post("/api/produce/mix/clean", json={"job_id": "j"})
    assert r.status_code == 200 and r.json()["ok"] is True
    assert appmod.Store(appmod.DB_PATH).get_mix_job("j")["clean_status"] == "cleaning"


def test_clean_requires_plan(tmp_path, monkeypatch):
    c = _client(tmp_path, monkeypatch)
    r = c.post("/api/produce/mix/clean", json={"job_id": "nope"})
    assert r.status_code == 422


def test_status_exposes_clean_fields(tmp_path, monkeypatch):
    c = _client(tmp_path, monkeypatch)
    _job_with_plan(c, monkeypatch)
    appmod.Store(appmod.DB_PATH).update_mix_job("j", clean_status="ready", clean_error=None)
    d = c.get("/api/mix/status/j").json()
    assert d["clean_status"] == "ready"
    assert "clean_error" in d
```

- [ ] **Step 2: 실패 확인**

Run: `python -m pytest shopping_shorts/tests/test_app_clean.py -q`
Expected: FAIL — `/api/produce/mix/clean` 404, status에 clean_status 없음

- [ ] **Step 3: import에 run_clean_sources·_resolve_sources 추가**

`app.py`의 mix_pipeline import(39–40행)에 두 심볼 추가:

```python
from shopping_shorts.mix_pipeline import (run_mix_job, run_render, run_preview, retype_mix_job,
                                          _source_video_id, resynth_tts_job, resynth_one_beat,
                                          run_clean_sources, _resolve_sources)
```

- [ ] **Step 4: status 라우트에 clean 필드 추가**

`api_mix_status`의 return dict(1328–1332행)에 두 줄 추가:

```python
            "preview_status": job.get("preview_status"),
            "preview_error": job.get("preview_error"),
            "clean_status": job.get("clean_status"),
            "clean_error": job.get("clean_error")}
```

- [ ] **Step 5: /clean 라우트 추가**

`api_produce_mix_preview_file`(1477행) 정의 **뒤**에 추가:

```python
@app.post("/api/produce/mix/clean")
def api_produce_mix_clean(background_tasks: BackgroundTasks, body: dict):
    """2단계 자막제거 미리보기 — 각 소스 원본을 VMake로 청소해 캐시(유료).
    preview 라우트와 같은 중복예약 가드·동기 상태기록 패턴을 쓴다."""
    job_id = body.get("job_id")
    store = Store(DB_PATH)
    job = store.get_mix_job(job_id)
    if not job or not job.get("edit_plan"):
        return JSONResponse(status_code=422, content={"ok": False, "error": "매칭 먼저 실행하세요"})
    if job.get("clean_status") == "cleaning" and not _render_is_stale(job):
        return {"ok": True, "status": "cleaning"}       # 더블클릭 — VMake를 두 번 안 돌린다
    # 'cleaning'을 여기서 동기 기록(응답 전) — run 안에서 쓰면 이중예약된다(preview 라우트 주석 참조)
    store.update_mix_job(job_id, clean_status="cleaning", clean_error=None)
    background_tasks.add_task(run_clean_sources, job_id, DB_PATH, _MIX_WORK_DIR)
    return {"ok": True, "status": "cleaning"}
```

- [ ] **Step 6: 통과 확인**

Run: `python -m pytest shopping_shorts/tests/test_app_clean.py -q`
Expected: PASS (3 passed)

- [ ] **Step 7: 커밋**

```bash
git -C .tracks/자막제거미리보기 add shopping_shorts/app.py shopping_shorts/tests/test_app_clean.py
git -C .tracks/자막제거미리보기 commit -m "feat(app): /api/produce/mix/clean 예약 + status에 clean_status 폴링 필드"
```

---

## Task 5: app.py — clean_thumb (원본↔클린 비교 프레임)

**Files:**
- Modify: `shopping_shorts/app.py` (frame_extract import 확인 + clean 라우트 뒤 신규)
- Test: `shopping_shorts/tests/test_app_clean.py` (이어서 추가)

- [ ] **Step 1: 실패 테스트 작성**

`tests/test_app_clean.py`에 추가:

```python
def test_clean_thumb_clean_404_before_ready(tmp_path, monkeypatch):
    c = _client(tmp_path, monkeypatch)
    _job_with_plan(c, monkeypatch)                    # clean_status 미설정
    r = c.get("/api/mix/produce/clean_thumb/j?kind=clean".replace("/mix/produce", "/produce/mix"))
    assert r.status_code == 404


def test_clean_thumb_original_serves(tmp_path, monkeypatch):
    c = _client(tmp_path, monkeypatch)
    _job_with_plan(c, monkeypatch)
    monkeypatch.setattr(appmod, "_resolve_sources", lambda job, work: {"s0": "/orig/s0.mp4"})
    monkeypatch.setattr(appmod.frame_extract, "_probe_duration", lambda p: 4.0)
    img = tmp_path / "original.jpg"; img.write_bytes(b"\xff\xd8jpg")
    monkeypatch.setattr(appmod.frame_extract, "extract_frame_at",
                        lambda src, d, ts, filename="f.jpg": img)
    r = c.get("/api/produce/mix/clean_thumb/j?kind=original")
    assert r.status_code == 200
    assert r.headers["content-type"].startswith("image/")
```

- [ ] **Step 2: 실패 확인**

Run: `python -m pytest shopping_shorts/tests/test_app_clean.py -q`
Expected: FAIL — clean_thumb 라우트 404 (미구현) / `appmod.frame_extract` AttributeError

- [ ] **Step 3: frame_extract import 확인·추가**

`app.py` 상단 import에 `frame_extract`가 없으면 추가(있으면 생략):

```python
from shopping_shorts import frame_extract
```

- [ ] **Step 4: clean_thumb 라우트 추가**

Task 4에서 추가한 `/clean` 라우트 **뒤**에:

```python
@app.get("/api/produce/mix/clean_thumb/{job_id}")
def api_produce_mix_clean_thumb(job_id: str, kind: str = "original"):
    """대표 소스(첫 소스)의 중간지점 프레임 PNG. kind=original|clean.
    clean은 clean_status=ready + 클린파일 존재일 때만(아니면 404). 양쪽 같은 t로 정렬."""
    job = Store(DB_PATH).get_mix_job(job_id)
    if not job:
        return JSONResponse(status_code=404, content={"ok": False, "error": "job 없음"})
    work = _MIX_WORK_DIR / job_id
    vid = _source_video_id(0)
    if kind == "clean":
        src = (job.get("clean_sources") or {}).get(vid)
        if job.get("clean_status") != "ready" or not src or not Path(src).exists():
            return JSONResponse(status_code=404, content={"ok": False, "error": "클린 소스 없음"})
    else:
        try:
            src = _resolve_sources(job, work)[vid]
        except Exception:
            return JSONResponse(status_code=404, content={"ok": False, "error": "소스 없음"})
    dur = frame_extract._probe_duration(src) or 2.0
    frame = frame_extract.extract_frame_at(src, work / "clean_thumb", dur / 2,
                                           filename=f"{kind}.jpg")
    if not frame:
        return JSONResponse(status_code=404, content={"ok": False, "error": "프레임 추출 실패"})
    return FileResponse(str(frame), media_type="image/jpeg")
```

- [ ] **Step 5: 통과 확인**

Run: `python -m pytest shopping_shorts/tests/test_app_clean.py -q`
Expected: PASS (5 passed)

- [ ] **Step 6: 커밋**

```bash
git -C .tracks/자막제거미리보기 add shopping_shorts/app.py shopping_shorts/tests/test_app_clean.py
git -C .tracks/자막제거미리보기 commit -m "feat(app): clean_thumb — 원본↔클린 비교 프레임 서빙"
```

---

## Task 6: produce.html — 2단계 버튼·비교 UI·폴링

**Files:**
- Modify: `shopping_shorts/static/produce.html:213` (2단계 섹션), `:2455-2470` (refreshSub/onSubToggle), `:2453` 뒤(신규 JS)
- Test: `shopping_shorts/tests/test_clean_wizard_js.py`

- [ ] **Step 1: 실패 테스트 작성**

`tests/test_clean_wizard_js.py` 생성:

```python
import os
import re
import subprocess
import tempfile

HTML = os.path.join(os.path.dirname(__file__), "..", "static", "produce.html")


def _script_src():
    src = open(HTML, encoding="utf-8").read()
    return "\n".join(re.findall(r"<script>(.*?)</script>", src, re.S))


def test_clean_preview_markers_present():
    html = open(HTML, encoding="utf-8").read()
    assert 'id="btnCleanPreview"' in html
    assert 'id="cleanPreview"' in html
    js = _script_src()
    assert "function startCleanPreview" in js
    assert "function pollClean" in js
    assert "/api/produce/mix/clean" in js
    assert "clean_thumb" in js


def test_script_still_parses():
    js = _script_src()
    with tempfile.NamedTemporaryFile("w", suffix=".js", delete=False, encoding="utf-8") as f:
        f.write(js); path = f.name
    try:
        r = subprocess.run(["node", "--check", path], capture_output=True, text=True,
                           stdin=subprocess.DEVNULL)
        assert r.returncode == 0, r.stderr
    finally:
        os.unlink(path)
```

- [ ] **Step 2: 실패 확인**

Run: `python -m pytest shopping_shorts/tests/test_clean_wizard_js.py -q`
Expected: FAIL — `btnCleanPreview` 마커 없음

- [ ] **Step 3: 2단계 섹션에 버튼·박스 추가**

`produce.html`의 `<div id="subState" ...></div>`(213행) **뒤**에 추가:

```html
      <div id="subState" class="hint" style="margin-top:8px"></div>
      <div id="cleanPreviewWrap" style="margin-top:12px">
        <button id="btnCleanPreview" class="btn" onclick="startCleanPreview()" disabled>자막제거 미리보기</button>
        <div id="cleanPreview" class="hint" style="margin-top:10px"></div>
      </div>
```

- [ ] **Step 4: refreshSub·onSubToggle에 버튼 활성 배선**

`refreshSub`(2456행)를 교체:

```javascript
function refreshSub(){
  const cb=document.getElementById('subToggle'); if(cb) cb.checked=STATE.subtitleRemoval;
  const s=document.getElementById('subState');
  if(s) s.textContent = MIX_JOB ? '' : '⚠️ 2단계에서 영상 매칭을 먼저 실행하세요.';
  const btn=document.getElementById('btnCleanPreview');
  if(btn) btn.disabled = !(STATE.subtitleRemoval && MIX_JOB);   // 켜짐+매칭됨일 때만
}
```

`onSubToggle`(2461행)의 `STATE.subtitleRemoval = ...` 다음 줄에 버튼 갱신 추가:

```javascript
  STATE.subtitleRemoval = document.getElementById('subToggle').checked;
  const cbtn=document.getElementById('btnCleanPreview');
  if(cbtn) cbtn.disabled = !(STATE.subtitleRemoval && MIX_JOB);
```

- [ ] **Step 5: 폴링 JS 추가**

`pollPreview` 함수 닫는 `}`(2453행) **뒤**에 추가:

```javascript
// ── 2단계 자막제거 미리보기(소스 원본 청소) ──────────────
let CLEAN_POLL=null, CLEAN_GEN=0;
async function startCleanPreview(){
  const box=document.getElementById('cleanPreview');
  const btn=document.getElementById('btnCleanPreview');
  if(!MIX_JOB){ box.textContent='⚠️ 2단계에서 영상 매칭을 먼저 실행하세요.'; return; }
  clearInterval(CLEAN_POLL); CLEAN_POLL=null;
  const myGen=++CLEAN_GEN, myJob=MIX_JOB;
  if(btn) btn.disabled=true;
  box.innerHTML='<div class="hint">⏳ 자막 제거 중… VMake AI (최대 ~1시간)</div>';
  try{
    const d=await (await fetch('/api/produce/mix/clean',{method:'POST',
      headers:{'Content-Type':'application/json'}, body:JSON.stringify({job_id:myJob})})).json();
    if(CLEAN_GEN!==myGen) return;
    if(!d.ok){ box.innerHTML='<div class="hint" style="color:var(--fire)">⚠️ '+esc(d.error||'청소 실패')+'</div>'; if(btn) btn.disabled=false; return; }
    CLEAN_POLL=setInterval(()=>pollClean(myGen,myJob),3000); pollClean(myGen,myJob);
  }catch(e){ if(CLEAN_GEN!==myGen) return; box.innerHTML='<div class="hint" style="color:var(--fire)">네트워크 오류</div>'; if(btn) btn.disabled=false; }
}
async function pollClean(gen,job){
  const box=document.getElementById('cleanPreview');
  const btn=document.getElementById('btnCleanPreview');
  try{
    const d=await (await fetch('/api/mix/status/'+job)).json();
    if(CLEAN_GEN!==gen || MIX_JOB!==job) return;
    if(d.clean_status==='ready'){
      clearInterval(CLEAN_POLL); CLEAN_POLL=null;
      const t=Date.now(), u='/api/produce/mix/clean_thumb/'+encodeURIComponent(job);
      box.innerHTML='<div style="display:flex;gap:10px;align-items:flex-start">'
        +'<figure style="margin:0"><img src="'+u+'?kind=original&t='+t+'" style="width:150px;border-radius:8px;background:#000"><figcaption class="hint" style="text-align:center">원본(자막 있음)</figcaption></figure>'
        +'<figure style="margin:0"><img src="'+u+'?kind=clean&t='+t+'" style="width:150px;border-radius:8px;background:#000"><figcaption class="hint" style="text-align:center">자막 제거됨</figcaption></figure>'
        +'</div>';
      if(btn) btn.disabled=false;
    } else if(d.clean_status==='failed'){
      clearInterval(CLEAN_POLL); CLEAN_POLL=null;
      box.innerHTML='<div class="hint" style="color:var(--fire)">⚠️ 자막제거 실패 — '+esc(d.clean_error||'')+'</div>';
      if(btn) btn.disabled=false;
    }
  }catch(e){ /* 다음 tick 재시도 */ }
}
```

- [ ] **Step 6: 통과 확인**

Run: `python -m pytest shopping_shorts/tests/test_clean_wizard_js.py -q`
Expected: PASS (2 passed). node 미설치면 `test_script_still_parses`는 skip 처리(선례대로 node 필요) — 로컬에 node 있으면 통과.

- [ ] **Step 7: 커밋**

```bash
git -C .tracks/자막제거미리보기 add shopping_shorts/static/produce.html shopping_shorts/tests/test_clean_wizard_js.py
git -C .tracks/자막제거미리보기 commit -m "feat(produce): 2단계 자막제거 미리보기 버튼+원본↔클린 비교 썸네일"
```

---

## Task 7: 전체 회귀 + 수동 검증

- [ ] **Step 1: 관련 테스트 전체 실행**

Run:
```bash
python -m pytest shopping_shorts/tests/test_clean_sources.py shopping_shorts/tests/test_app_clean.py \
  shopping_shorts/tests/test_mix_render_flow.py shopping_shorts/tests/test_clean_wizard_js.py \
  shopping_shorts/tests/test_app_vmake.py -q
```
Expected: 전부 PASS

- [ ] **Step 2: import 스모크**

Run: `python -c "import shopping_shorts.app"`
Expected: 에러 없음(라우트·import 배선 확인)

- [ ] **Step 3: 수동 구동 검증(verify 스킬)**

VMake 키가 등록된 환경에서 서버를 띄우고 2단계까지 진행 → "자막제거 미리보기" 클릭 →
⏳ 표시 → ready 시 원본/클린 이미지 2장이 뜨는지 육안 확인. 렌더 산출물이므로
실제 구동 없이 완료 처리 금지(fablize). 실측 결과를 handoff에 1줄 기록.

---

## Self-Review 체크

- **스펙 커버리지**: 데이터모델(T1) / run_clean_sources·캐시(T2) / 최종렌더 재사용(T3) /
  /clean·폴링(T4) / clean_thumb 비교(T5) / 프런트 버튼·비교(T6) — 스펙 5개 섹션 모두 태스크 있음. ✅
- **placeholder**: 없음(모든 코드 스텝에 실코드). ✅
- **타입 일관성**: `clean_sources`(dict)·`clean_status`(str)·`clean_error`(str) / `_ensure_clean_sources`·
  `run_clean_sources`·`_clean_one` 이름이 T2 정의와 T3·T4 사용에서 일치. ✅
- **회귀 리스크**: 기존 `test_mix_render_flow.py`의 두 테스트는 구방식(mix_raw clean_fn)을 검증하므로
  T3에서 **교체**(신계약). `test_app_vmake.py`는 키 저장/start 플래그라 영향 없음. ✅
