## 2026-08-29 (밤샘)
- 칸 타임라인 T1~T6 구현: 3레인 뷰+재생선 / 컷 필름식 펼침(filmframes) / ✂어절 경계 편집(기존 caplines API로 일원화, 재TTS 0원) / 🔁 고정길이 박스 교체+swap_log / cap_src(정밀·받아쓰기·추정) 배지. 전부 로컬 실job 브라우저 실측. 매칭 로직 무변경.
- 무음표시·≈추정배지·좁은블록 처리 + 싱크 3층 실측(Whisper 0.24s·화면 27ms) + ★음슴체 훅독식 근본수리(초장문 호흡분할 배분, 실사고 job 2개 서버 복구).
- 조각펼치기 fit해제·우클릭겹침 박스·끌기랙(panW)·미리보기 폭 조절(pvgrip, 변수 두 벌 둘 다 밈). 전부 실측 검증, 배포 진행.
- 타임라인 컷 가장자리 드래그 = 길이 직접 조절(기존 FIXLEN/setFix 재사용 — 정한 컷 유지·나머지 비례축소·합계불변). ✋ 클릭 = 자동 복귀. 실측 [2,1,1.5,2.92]→[2.77,0.86,1.29,2.5]→원복.
- ✋컷 길이 영속화 — saveWork에 fixlen 포함 + 복원 3경로를 hydrateFixlen 하나로(★저장가드 규약) + setFix가 저장까지. 실측: 드래그 2.66s→새로고침→복원.
- 재생선 손잡이 드래그=seekTo 한 벌 재사용(끌면 일시정지, 실측 2.00s 안착) / 펼치기 기본 0.5초 / ★자막 하한(_CAP_MIN_DUR)이 정밀 실측을 늘려 음성이 자막을 앞서던 것 수리(실측 job fa0f: 0.68→1.0으로 0.32s 밀림) — 실측 있으면 하한 생략, 테스트 갱신 72 green.
- 스페이스=선택 칸 재생/일시정지(필름 열려 있으면 필름롤 몫 — 양보). 끌어놓은 지점부터 재개 실측(2.5s→재개 3.0s·자막 일치).
- ★구절 맞춤(기본 켬, 칸 토글): 컷 경계=자막 구절 경계 1:1(리드인=첫컷·꼬리=끝컷), 파란 테두리=들어가는 카드, 화면 planClips↔서버 _plan_phrase_clips 짝(✋수동 우선, 표식 없는 옛 job 무변경). 📦박스 버튼(컷 길이 잠금박스, 영상 갈아도 유지)+Esc 해제. 미리보기 tight 기본 330→400px. 실측: 경계 4곳 일치·s0→s1 유지·Esc 해제.
- ★B 분할규칙(사장님 표본 승인): 의존명사('만에'·'거였…')·관형형(는/된/한/ㄹ받침)·부정부사(안/못) 견인, '건·게·것·때문에' 뒤=숨 자리, 어절캡 4→5. 118 tests green.
- 우선순위 뒤집기: 구절맞춤 켬=✋ 무시(서버 동일), ✋ 만지면 그 칸 구절맞춤 자동 꺼짐(체크 보임) — "마지막 카드 비활성" 사고 해소.
- 카드 🎞펼치기 제거(폭 잘림) — 📦박스가 겸함: 누르면 그 영상 필름 자동 펼침+잠금박스가 조각 위치에(실측 0.23s 일치).
- 📦박스 다듬기: 열리면 잠금박스가 화면 가운데(깊은 조각 16.9s 실측 정중앙, 앞쪽 조각은 클램프 한계 내 표시)·테두리 5px·타임라인 컷의 rep 강조 제거.
- ④레이아웃 고정: 상단 밴드 높이 규칙 3벌→1벌 통합(0순위-B), 값=모든 창 켜진 높이 고정 min(880px,86vh)·미리보기 450px("소스 한 줄 보장" 560px 결정 번복 — 사장님 "모든 창 크기 고정"). 필름 펼친 채 타임라인·카드·필름행 전부 보임 실측.
- 미리보기 자동 맞춤: 밴드 고정(880) 번복 → 오른쪽 열 높이에 9:16 역산(관찰자+디바운스, 수동 pvW 우선). 아래선 gap 0px 실측.
- 첫 화면 영상1 필름 자동 펼침 — autoOpenRoll이 없는 필드(DATA.videos)를 봐서 조용히 무산되던 것 수리(src_duration에서 뽑음).
