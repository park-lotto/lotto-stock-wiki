# 테스트시한폭탄 — 30일 지나면 저절로 깨지는 고정 날짜

**2026-08-26 (집PC) · 완료**

## 무엇을 · 왜

`test_aipick_fresh_comments` 2건이 **아무도 코드를 안 건드렸는데 오늘 깨졌다.**
어제까진 통과했다.

### 원인 (재현으로 확정)

테스트가 수집 시각으로 `"2026-07-26T00:00:00"`을 박아뒀다. 그런데 `reel_history`는
last_seen이 30일 지난 행을 **저장하는 그 순간** 지운다(`store._record_history` 끝,
`store.py:2646`). 딱 30일 뒤인 **08-26부터 저장 즉시 삭제**된다.

```
고정날짜(07-26) 저장 후 조회: {}          ← 사라짐
  30일 컷오프: 2026-07-26T17:01:04       ← 17시간 차이로 만료
현재시각 저장 후 조회: {'AA': 15430}      ← 정상
```

## 고친 것

고정 날짜 2곳 → `_now()`(현재 시각). **이 테스트가 보는 건 "최신 크롤값을 쓰는가"지
특정 날짜가 아니다** — 상대 시각이 오히려 의도에 맞다. 이유는 `_now()` 독스트링에 남겼다.

## ★안 고친 것 (전수조사했다)

`save_last_run`에 날짜를 넘기는 곳이 **3군데 더** 있다:
```
test_last_run_cap.py:39      "2026-08-24"
test_reference_adopt.py:70   "2026-08-18T00:00:00+00:00"
test_store.py:109            "2026-07-09T10:00:00"   ← 48일 전인데 멀쩡
```
**전부 안전하다** — `load_last_run`(settings 테이블, 단일 행)만 보는 테스트라
보존창과 무관하다. 8 passed로 확인했다.

> **안전한 걸 "혹시 몰라서" 고치지 마라.** 그게 새 버그의 씨앗이다.
> 위험한 건 `latest_comments`·`_load_work_sources`처럼 **reel_history를 조회**하는 테스트뿐이다.

## 재발 방지 — ★실제로 잡는지 확인했다

`test_no_expiring_fixture_dates.py` 신설. 보존창을 타는 자리에 날짜 리터럴이 있으면 실패.

**항상 통과하는 테스트는 방어가 아니다.** 그래서 일부러 되돌려 봤다:
```
고정날짜로 되돌림 → 1 failed  (잡아냄)
원복             → 2 passed
```
보존창이 30일에서 바뀌면 그것도 알려준다(전제 검사 포함).

## 검증

```
5577 passed / 2 failed   ← aipick 2건 사라짐
```
남은 2건은 `test_assembled_drafts` — **실 AI 호출이라 실행마다 1~2건으로 흔들린다.**
변경 전 트리(main 폴더)에서 3회 돌려 동일하게 흔들림을 확인했다(1·1·2건). 내 탓 아님.

## 커밋

`0c21a01d3` → main 병합

## ⏭ 남은 것 (별건, 이 트랙 밖)

`test_assembled_drafts`가 실 AI를 호출해 불안정하다. 기준선을 흔들어 **다음 사람이
"내가 깼나?"를 매번 확인해야 한다.** 스텁으로 고정하거나 마커로 분리할 가치가 있다 —
다만 그 테스트가 무엇을 지키려는지 먼저 읽어야 한다(성급히 끄면 진짜 회귀를 놓친다).
