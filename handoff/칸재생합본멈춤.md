# 칸 하나만 재생했는데 다음 칸까지 이어서 재생된다 (이연정님 제보 2026-09-21)

## 제보
"영상 대본mix에서 예전에는 그 칸 클릭하면 그 칸만 재생됐는데, 지금은 그 다음 칸까지 이어서 재생이 계속 된다."

## 재현 (라이브, 관리자 테스트 job 393e0ff51de0)
칸1 끝은 3.267초인데 `playKey='beat:0'`·컷 3/3이 끝난 뒤에도 합본 시각이 4.74 → 11.06초로 계속 흘렀다(칸 2·3·4까지).

## 뿌리
- 2026-09-21 03:00 `ae104497b`부터 **칸별 재생도 합본**(전 칸의 화면+음성을 한 파일로 구운 것)으로 튼다(`playBeat`의 `pvxAttach`).
- 조각 시절엔 그 칸의 mp3가 끝나면 저절로 멎었다. 합본은 한 파일이라 혼자 안 멈춘다.
- '이 칸 끝' 신호(`pvxAudio().onended`)를 거는 곳이 **전체 재생(`runAllFrom`) 하나뿐**이었고,
  `step()`은 합본을 일부러 안 세운다(전체 재생이 칸0에 멈추던 사고 때문 — 그 주석 그대로 둠).
- 기각한 가설: ①같은 날 내 짝(owner) 수정 — 재생/정지 코드를 안 건드렸고 재현에서 컷은 3/3으로 정상 종료
  ②다음 칸 음성을 따로 틂 — 흐른 것은 합본 시각 자체 ③playKey가 'all'로 바뀜 — 'beat:0' 그대로였다.

## 수정 (main `ba466af64`, 커밋 987307acf)
`static/scene_play.js playBeat`: `const a = playTts(i,0); if (a && a._pvx) a.onended = () => { if (playKey === 'beat:'+i) a.pause(); }`
— 전체 재생과 **같은 신호**를 쓴다(0순위-B). 그 사이 주인이 바뀌었으면(전체 재생·다른 칸) 안 건드린다.
테스트 `tests/test_beat_play_stops_at_beat_end.py` 2건(node로 실제 playBeat·pvxAudio 실행) — 고치기 전 코드에서 1건 실패 확인.
재생 관련 회귀 402 passed. 게이트 통과.

## 검증
- 로컬 실브라우저(job pse_e2e): 칸1 2.73초(끝 2.733)에서 멈춤 · 다시 누르면 처음부터 · 칸3 13.15초(끝 13.167)에서 멈춤 · 전체 재생은 칸 0→1로 넘어감.
- **라이브(18:1x, 같은 job 393e0ff51de0)**: 새 코드 서빙 확인 → 칸1 **3.27초에서 멈춤**(고치기 전 11초까지 흐름) · 칸3 10.88초(끝 10.9)에서 멈춤 · 전체 재생 칸 0→1→2 정상.
- JS만 바뀌어 웹 재시작 불필요(스크립트 주소에 mtime이 붙는다). 고객은 화면을 새로 열면(새로고침) 적용된다.

## 메모
- 검증용 헤드리스 브라우저가 합본(blob) 받는 도중 js를 자주 찌르면 데몬이 죽고 about:blank로 돌아간다 — goto 뒤 25초 기다렸다가 한 번에 재라.
