# 출처장면배선 로그

## 2026-08-31 (집 PC)
- 사장님 질문 "1단계→2단계→3단계 사슬인데 정말 좋아질 방법 없나, 두더지하지 말고"에서 출발.
- **조사 결론: 1단계 이미지 분석은 문제가 아니다.** 세그 22,765개 중 scene_desc 빈 것 1개(0.0%),
  묘사도 정확하다. 못 하는 건 **그 그림을 한 칸짜리 라벨에 우겨넣는 것**.
- ★라벨(shot_role)이 묘사와 따로 논다 — 일치율 실측:
  도포52·설치47·정리45 / 완성28·조작25·before22 / **문제15·실증7**.
  `실증` 3,740개 중 93%가 안 맞는다. 원인은 **한 장면=한 라벨**인데 실제 장면은 성질이
  여럿이고(정리+문제+실증 동시), 축도 섞여 있다(동작 4개 vs 상태·기능 3개).
- ★**출처장면(src_seg)이 86% 버려지고 있었다** — 배선은 다 있는데 **역할 이름을 두 단계가
  따로 지어서**(0순위-B) 이름 같은 hook·cta만 통과했다. 44잡 중 36잡이 '일부만 겹침',
  전부 겹치는 잡은 0개. 한쪽 영어·한쪽 한글이라 0건인 잡도 있었다.
- 처방: **자리(순서)로 잇는다**. 개수가 같으면 i번째끼리(양쪽 다 대본을 순서대로 쪼개므로),
  다르면 종전대로 이름으로. 근거는 실측 80잡 — 개수 같은 37잡 중 **33잡이 역할결과==자리결과**,
  나머지 4잡은 **역할 매칭이 틀린 쪽**이었다(job 36a02e5a: 2단계 proof 3번칸 vs 3단계 proof 5번칸).
- 검증: ★고친 함수를 라이브 329건에 직접 돌림(`/tmp` 격리 사본) — **off 3% → on 55%**.
  ⚠️내가 처음 말한 97%는 시뮬 과장이었다. 남은 45%는 EDL 재고 밖 88건·개수 불일치 65건.
  테스트 10건 green(신규 5) / 관련 영역 309 passed·실패 0. 커밋 `d4d2fd06e` (**미배포**).
- ★훅 기준이 실측과 반대라는 것도 찾았다(미착수) — 첫 3초 실측(홈템 396·레시피 94편):
  홈템은 **문제+before 31%**로 여는데 지시문은 "완성/실증으로 열고 준비동작으로 열지 마라".
  **`is_key`는 역효과**(홈템 상위25% 9% vs 하위25% 22%) — 정의가 "기능·효과를 보여주는가"라
  후킹이 아니라 **설명**이다. 장르 무관 공통은 **motion_level=PEAK**(홈템94·레시피86%)인데
  훅 규칙이 그 값을 한 번도 안 본다. ⚠️유튜브 썰은 태깅 재고가 없어 못 쟀다.
- ★사장님 지적으로 폐기한 내 안 2개: "짧은 컷 병합"·"같은 소스 몰기". 히트작 컷 중앙값이
  1.13초라 묶으면 아마추어처럼 되고, 몰면 같은 영상 티가 난다. **실측이 내 추측을 뒤집었다.**
