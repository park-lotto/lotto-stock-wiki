# CN검색통합 트랙 로그

## 2026-08-17 — CN 검색 통합 파서 (구현 완료, 서버 실측 대기)

- `cn_search.search()` 단일 진입점 신설. 백엔드(Playwright 무료 → Apify 유료) 순서는
  `_CHAIN` **한 곳에서만** 정한다(0순위-B). **0건이면 폴백**(A안, 크레딧 이월 안 되므로).
- 렌즈 후보 버튼이 외부 사이트로 나가던 것 → **인앱 카드 합류**로 교체(`📕🎬 여기서 찾기`).
  외부 링크는 작은 `↗`로 남김. 비용($0.04/무료)을 화면에 표시.
- 실측: 샤오홍슈 Playwright 17건 $0 / 도우인 Apify 12건 $0.04005. Webshare는 cn·hk·tw 전부
  사용 가능(추가구매 불필요). Apify 정상 4계정 $16.79, 매달 $20 리셋.
- 부수: 샤오홍슈 세션이 7/29부터 죽어 있던 것 복구(`id_token`이 진짜 인증 쿠키).
- 함정 3건 기록 → `handoff/CN검색통합.md`: `inf`=OverflowError 크래시 / normalize가 try 밖 /
  프론트 `zh` 미이스케이프(설계서가 틀렸고 코드가 맞았다).
