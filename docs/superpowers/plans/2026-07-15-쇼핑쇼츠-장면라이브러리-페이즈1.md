# 장면 라이브러리(재사용 짤 뱅크) 페이즈1 — 자산 뱅크 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 좋은 장면(화면짤·효과음·오버레이)을 구간컷으로 잘라 다축 태그와 함께 저장하고, 관리 UI에서 보고 편집할 수 있는 **자산 뱅크**를 만든다. (AI 자동배치·렌더는 페이즈2)

**Architecture:** 신규 `scene_assets.py`(순수 미디어/AI 유틸, `frame_extract.py` 골격을 따름) + `store.py`의 `_init_schema`에 `scene_assets` 테이블 + `app.py`에 `/api/scene/*` 라우트 + 신규 `static/scene_library.html` 관리 페이지 + 기존 페이지(레퍼런스랭킹·영상제작소)에 저장 버튼 다리. 저장은 **2스텝**(prepare=클립컷+Gemini 태그초안 → 사람 확인 → commit=DB 확정)이라 재추출이 없다.

**Tech Stack:** Python 3 / FastAPI / SQLite(sqlite3 직접) / ffmpeg(subprocess) / google-genai(Gemini) / 바닐라 JS(템플릿 리터럴 + innerHTML)

**스펙:** `docs/superpowers/specs/2026-07-15-쇼핑쇼츠-장면라이브러리-재사용짤뱅크-design.md`

---

## Global Constraints

이 섹션의 요구사항은 **모든 태스크에 암묵적으로 포함**된다.

1. **작업 디렉토리**: 저장소 루트는 `C:\Users\TheRose\Desktop\로또의 주식`. 코드는 `shopping_shorts/` 하위. 테스트 실행·서버 기동은 **반드시 저장소 루트에서**(절대임포트 `shopping_shorts.*`).
   - 테스트: `python -m pytest shopping_shorts/tests/test_scene_assets.py -v`
   - 서버: `python -m uvicorn shopping_shorts.app:app --port 8899`
2. **공유 워킹트리 — `git add -A` 절대 금지.** 다른 세션/PC가 같은 파일을 초단위로 커밋 중이다. 반드시 `git commit -m "메시지" -- <내 경로만>` pathspec 커밋. ⚠️ **`--` 뒤는 전부 경로로 해석된다** — `git commit -- <경로> -m "msg"`처럼 쓰면 `-m`과 메시지를 파일명으로 읽어 `error: pathspec '-m' did not match any file(s)`로 실패한다(2026-07-15 실측). **`-m`이 먼저, `--`가 마지막.** 커밋 전 `git status`로 남의 hunk 확인, 커밋 후 `git show --stat HEAD`로 내 파일만 들어갔는지 확인.
3. **브랜치는 `main` 고정.** 커밋 전 `git branch --show-current`로 확인.
4. **DB 접근은 `Store` 클래스 인스턴스 메서드로만.** 이 저장소엔 `init_db()`도 `row_factory`도 **없다**. 모든 조회는 위치 인덱스(`r[0]`, `r[1]`…). 호출부는 매번 `Store(DB_PATH)`를 새로 생성한다.
5. **Gemini 호출은 `edit_plan._vault_call` 캐스케이드 재사용.** `comment_gen.SHORTS_GEMINI_KEYS`(좁은 단일키풀)를 직접 쓰지 말 것 — 소진 회피가 `_vault_call`의 존재 이유다(`edit_plan.py:241-242`).
6. **Gemini 스키마는 `response_schema` + 명시적 `required` 필수.** `response_mime_type`만 주면 Gemini가 필드를 생략한다(`video_analysis.py:26` 경고). 코드펜스 벗기는 헬퍼는 이 저장소에 없다 — 만들지 말 것.
7. **AI 실패는 예외가 아니라 "형태가 온전한 빈 값"으로 폴백.** 무키/실패/쿼터 어떤 경우든 파이프라인이 안 끊긴다.
8. **경로 상수는 `app.py`에**(store.py 아님). 기존 `_WIKI_MEDIA_DIR`(app.py:470) 옆에 나란히.
9. **인증**: `/api/scene/*`는 `_AUTH_ALLOW`에 **넣지 않는다** → 자동으로 로그인 보호됨. 라우트는 `request: Request`를 받아 `_cid(request)`로 customer_id를 얻는다. `Depends()`는 이 저장소에서 안 쓴다.
10. **경로 보안**: 클라이언트가 준 문자열을 파일 경로로 쓰지 않는다. `token`은 32자 hex만 허용(정규식 검증), 경로는 서버가 재구성.
11. **응답 관용구**: 성공 `{"ok": True, ...}`, 실패 `JSONResponse(status_code=..., content={"ok": False, "error": "..."})`.
12. **클립 규격 통일**: `720x1280 / libx264 / aac / 30fps / yuv420p`. 페이즈2 concat이 `-c copy`라(`video_assemble.py:346`) 규격이 다르면 **붙지 않는다**.

---

## File Structure

| 파일 | 책임 | 신규/수정 |
|------|------|-----------|
| `shopping_shorts/scene_assets.py` | 클립컷·오디오추출·썸네일·Gemini 자동태깅 (순수 함수, 상태 없음) | **신규** |
| `shopping_shorts/store.py` | `scene_assets` 테이블 + CRUD + 멀티테넌시 격리 | 수정 |
| `shopping_shorts/app.py` | `/api/scene/*` 라우트, 경로 상수, 페이지 라우트 | 수정 |
| `shopping_shorts/static/scene_library.html` | 관리 UI(그리드·필터·업로드·태그 편집 모달) | **신규** |
| `shopping_shorts/static/sidebar.js` | 네비 항목 1줄 추가 | 수정 |
| `shopping_shorts/static/index.html` | 레퍼런스랭킹 카드 → 저장 다리 | 수정 |
| `shopping_shorts/tests/test_scene_assets.py` | scene_assets.py 유닛 | **신규** |
| `shopping_shorts/tests/test_store_scene.py` | store CRUD·격리 유닛 | **신규** |
| `shopping_shorts/tests/test_app_scene.py` | API 라우트 유닛 | **신규** |

**의도적 divergence (계획서에 명시)**: `library.html`의 `.cards`는 이름과 달리 **세로 flex 스택**(카드 1개/행)이다. 장면 라이브러리는 진짜 타일 그리드가 필요하므로 `display:grid; grid-template-columns:repeat(auto-fill,minmax(190px,1fr))`를 쓴다. 또 `library.html:11-16`의 `.sidebar`/`.menu-item` CSS는 sidebar.js가 `.ss-nav`를 주입하므로 **죽은 코드** — 새 페이지에 복사하지 말 것.

---

## Task 1: store — `scene_assets` 테이블 + CRUD

**Files:**
- Modify: `shopping_shorts/store.py` (테이블은 `_init_schema` 안 `script_wiki` 블록 뒤 ≈159줄, CRUD는 `wiki_shortcodes` 뒤 ≈922줄)
- Test: `shopping_shorts/tests/test_store_scene.py` (신규)

**Interfaces:**
- Consumes: `LEGACY_CUSTOMER_ID`(store.py:12), `Store._conn()`(store.py:21-22)
- Produces: 후속 태스크가 의존하는 시그니처 —
  - `Store.add_scene_asset(asset: dict, customer_id=LEGACY_CUSTOMER_ID) -> int` (새 row id)
  - `Store.list_scene_assets(customer_id=LEGACY_CUSTOMER_ID, asset_type=None, category=None, role=None) -> list[dict]`
  - `Store.get_scene_asset(asset_id: int, customer_id=LEGACY_CUSTOMER_ID) -> dict | None`
  - `Store.update_scene_asset(asset_id: int, tags: dict, customer_id=LEGACY_CUSTOMER_ID) -> bool`
  - `Store.delete_scene_asset(asset_id: int, customer_id=LEGACY_CUSTOMER_ID) -> bool`
  - 반환 dict 키: `id, asset_type, render_mode, media_path, poster_path, duration, keep_original_audio, title, scene_desc, role, category, subject, tone, keywords, source_kind, source_ref, created_at` (`keywords`는 **list**로 변환해 반환, DB엔 콤마 문자열)

**설계 노트 — 기존 패턴과의 차이(의도적)**: `saved`/`script_wiki`는 `PRIMARY KEY (customer_id, shortcode)` 복합키다. 장면 자산은 자연키가 없고 같은 소스에서 여러 자산을 뽑으므로 **`id INTEGER PRIMARY KEY AUTOINCREMENT` + `customer_id` 일반 컬럼 + 인덱스**를 쓴다. 따라서 `_migrate_personal_tables` 루프에는 **추가하지 않는다**(구버전 DB에 이 테이블이 없어 `not cols`로 스킵됨).

- [ ] **Step 1: 실패하는 테스트 작성**

`shopping_shorts/tests/test_store_scene.py` 신규:

```python
import pytest
from shopping_shorts.store import Store, LEGACY_CUSTOMER_ID


@pytest.fixture
def store(tmp_path):
    return Store(tmp_path / "t.db")


def _asset(**over):
    a = {"asset_type": "clip", "render_mode": "cutaway",
         "media_path": "data/scene_assets/abc.mp4", "poster_path": "data/scene_assets/abc.jpg",
         "duration": 2.5, "keep_original_audio": 1, "title": "가루 한스푼",
         "scene_desc": "흰 가루를 숟가락으로 떠 그릇에 넣음", "role": "비법공개",
         "category": "레시피", "subject": "가루(밀가루·설탕류)", "tone": "비밀스러운·궁금",
         "keywords": ["숟가락", "가루", "클로즈업"], "source_kind": "reference",
         "source_ref": "ABC123"}
    a.update(over)
    return a


def test_add_returns_id_and_get_roundtrips(store):
    aid = store.add_scene_asset(_asset())

    assert isinstance(aid, int) and aid > 0
    got = store.get_scene_asset(aid)
    assert got["title"] == "가루 한스푼"
    assert got["asset_type"] == "clip"
    assert got["render_mode"] == "cutaway"
    assert got["duration"] == 2.5
    assert got["keywords"] == ["숟가락", "가루", "클로즈업"]  # 콤마문자열 → list 복원
    assert got["created_at"]  # datetime('now')로 자동 채움


def test_list_filters_by_type_category_role(store):
    store.add_scene_asset(_asset(title="A", asset_type="clip", category="레시피", role="비법공개"))
    store.add_scene_asset(_asset(title="B", asset_type="sfx", category="레시피", role="반응"))
    store.add_scene_asset(_asset(title="C", asset_type="clip", category="가전", role="비법공개"))

    assert {a["title"] for a in store.list_scene_assets()} == {"A", "B", "C"}
    assert {a["title"] for a in store.list_scene_assets(asset_type="clip")} == {"A", "C"}
    assert {a["title"] for a in store.list_scene_assets(category="레시피")} == {"A", "B"}
    assert {a["title"] for a in store.list_scene_assets(role="비법공개")} == {"A", "C"}
    assert {a["title"] for a in store.list_scene_assets(asset_type="clip", category="레시피")} == {"A"}


def test_customer_isolation(store):
    mine = store.add_scene_asset(_asset(title="내것"), customer_id=7)
    store.add_scene_asset(_asset(title="남것"), customer_id=8)

    assert [a["title"] for a in store.list_scene_assets(customer_id=7)] == ["내것"]
    assert store.get_scene_asset(mine, customer_id=8) is None       # 남의 것 못 읽음
    assert store.update_scene_asset(mine, {"title": "탈취"}, customer_id=8) is False
    assert store.delete_scene_asset(mine, customer_id=8) is False   # 남의 것 못 지움
    assert store.get_scene_asset(mine, customer_id=7)["title"] == "내것"  # 그대로


def test_update_changes_only_given_tags(store):
    aid = store.add_scene_asset(_asset())

    assert store.update_scene_asset(aid, {"title": "설탕 한스푼", "keywords": ["설탕"]}) is True

    got = store.get_scene_asset(aid)
    assert got["title"] == "설탕 한스푼"
    assert got["keywords"] == ["설탕"]
    assert got["scene_desc"] == "흰 가루를 숟가락으로 떠 그릇에 넣음"  # 안 준 필드는 유지
    assert got["media_path"] == "data/scene_assets/abc.mp4"          # 미디어 필드는 편집 대상 아님


def test_update_ignores_unknown_and_protected_fields(store):
    aid = store.add_scene_asset(_asset())

    # media_path/customer_id/id는 태그 편집으로 못 바꾼다(경로 조작 차단)
    store.update_scene_asset(aid, {"media_path": "../../etc/passwd", "customer_id": 99,
                                   "id": 12345, "존재안함": "x", "title": "정상"})

    got = store.get_scene_asset(aid)
    assert got["media_path"] == "data/scene_assets/abc.mp4"
    assert got["id"] == aid
    assert got["title"] == "정상"


def test_delete_removes(store):
    aid = store.add_scene_asset(_asset())

    assert store.delete_scene_asset(aid) is True
    assert store.get_scene_asset(aid) is None
    assert store.delete_scene_asset(aid) is False  # 두 번째는 False


def test_sfx_has_null_render_mode(store):
    aid = store.add_scene_asset(_asset(asset_type="sfx", render_mode=None, poster_path=None))

    got = store.get_scene_asset(aid)
    assert got["render_mode"] is None
    assert got["poster_path"] is None
```

- [ ] **Step 2: 테스트 실패 확인**

Run: `python -m pytest shopping_shorts/tests/test_store_scene.py -v`
Expected: FAIL — `AttributeError: 'Store' object has no attribute 'add_scene_asset'`

- [ ] **Step 3: 테이블 추가**

`shopping_shorts/store.py`의 `_init_schema` 안, `script_wiki` CREATE 블록 **바로 뒤**(≈159줄, `c.execute("""CREATE TABLE IF NOT EXISTS script_wiki ... """)` 다음 줄)에 삽입:

```python
            # 장면 라이브러리 — 재사용 짤 뱅크(2026-07-15). 자주 쓰는 후킹 장면(놀라는짤·
            # 효과음·"비법 한 스푼" 등)을 구간컷으로 저장해 믹스 때 재사용한다.
            # saved/script_wiki와 달리 자연키(shortcode)가 없고 한 소스에서 여러 자산을
            # 뽑으므로 AUTOINCREMENT id를 PK로 쓰고 customer_id는 일반 컬럼+인덱스로 격리.
            # 매칭 키는 한 단어 라벨이 아니라 다축(role·category·subject·keywords·scene_desc)
            # — "한스푼"이 가루↔액체↔세제로 섞이는 사고를 막는 이 설계의 심장.
            c.execute("""
                CREATE TABLE IF NOT EXISTS scene_assets (
                    id INTEGER PRIMARY KEY AUTOINCREMENT,
                    customer_id INTEGER NOT NULL DEFAULT 0,
                    asset_type TEXT NOT NULL,
                    render_mode TEXT,
                    media_path TEXT NOT NULL,
                    poster_path TEXT,
                    duration REAL,
                    keep_original_audio INTEGER NOT NULL DEFAULT 0,
                    title TEXT,
                    scene_desc TEXT,
                    role TEXT,
                    category TEXT,
                    subject TEXT,
                    tone TEXT,
                    keywords TEXT,
                    source_kind TEXT,
                    source_ref TEXT,
                    created_at TEXT
                )
            """)
            c.execute("CREATE INDEX IF NOT EXISTS idx_scene_assets_type "
                      "ON scene_assets(customer_id, asset_type)")
            c.execute("CREATE INDEX IF NOT EXISTS idx_scene_assets_cat "
                      "ON scene_assets(customer_id, category)")
```

- [ ] **Step 4: CRUD 헬퍼 추가**

`shopping_shorts/store.py`의 `wiki_shortcodes` 메서드 **뒤**(≈922줄)에 삽입. `_WIKI_COLS`/`_wiki_row` 패턴을 그대로 따른다:

```python
    # ── 장면 라이브러리(재사용 짤 뱅크) — customer_id별로 독립(2026-07-15) ──
    _SCENE_COLS = ("id, asset_type, render_mode, media_path, poster_path, duration, "
                   "keep_original_audio, title, scene_desc, role, category, subject, "
                   "tone, keywords, source_kind, source_ref, created_at")

    # 태그 편집으로 바꿀 수 있는 필드만. media_path/customer_id/id는 제외 —
    # 클라이언트가 준 값이 파일 경로가 되면 traversal이 열린다.
    _SCENE_EDITABLE = ("title", "scene_desc", "role", "category", "subject", "tone",
                       "keywords", "render_mode", "keep_original_audio")

    def _scene_row(self, r):
        return {"id": r[0], "asset_type": r[1], "render_mode": r[2], "media_path": r[3],
                "poster_path": r[4], "duration": r[5], "keep_original_audio": r[6],
                "title": r[7], "scene_desc": r[8], "role": r[9], "category": r[10],
                "subject": r[11], "tone": r[12],
                "keywords": [k for k in (r[13] or "").split(",") if k],
                "source_kind": r[14], "source_ref": r[15], "created_at": r[16]}

    @staticmethod
    def _scene_keywords(v):
        """keywords는 list로 주고받고 DB엔 콤마 문자열로 눕힌다."""
        if isinstance(v, (list, tuple)):
            return ",".join(str(k).strip() for k in v if str(k).strip())
        return (v or "").strip()

    def add_scene_asset(self, asset, customer_id=LEGACY_CUSTOMER_ID):
        """장면 자산 1개 저장 → 새 id. asset=다축 태그 dict(§4 스키마)."""
        with self._conn() as c:
            cur = c.execute(
                "INSERT INTO scene_assets(customer_id, asset_type, render_mode, media_path, "
                "poster_path, duration, keep_original_audio, title, scene_desc, role, category, "
                "subject, tone, keywords, source_kind, source_ref, created_at) "
                "VALUES(?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,datetime('now'))",
                (customer_id, asset.get("asset_type"), asset.get("render_mode"),
                 asset.get("media_path"), asset.get("poster_path"),
                 float(asset.get("duration") or 0.0), int(asset.get("keep_original_audio") or 0),
                 asset.get("title"), asset.get("scene_desc"), asset.get("role"),
                 asset.get("category"), asset.get("subject"), asset.get("tone"),
                 self._scene_keywords(asset.get("keywords")),
                 asset.get("source_kind"), asset.get("source_ref")),
            )
            return cur.lastrowid

    def list_scene_assets(self, customer_id=LEGACY_CUSTOMER_ID, asset_type=None,
                          category=None, role=None):
        """이 고객의 장면 자산(최근순). 타입·카테고리·역할로 좁힐 수 있다."""
        where, params = ["customer_id=?"], [customer_id]
        for col, val in (("asset_type", asset_type), ("category", category), ("role", role)):
            if val:
                where.append(f"{col}=?")
                params.append(val)
        with self._conn() as c:
            rows = c.execute(
                f"SELECT {self._SCENE_COLS} FROM scene_assets WHERE {' AND '.join(where)} "
                f"ORDER BY id DESC", params,
            ).fetchall()
        return [self._scene_row(r) for r in rows]

    def get_scene_asset(self, asset_id, customer_id=LEGACY_CUSTOMER_ID):
        with self._conn() as c:
            r = c.execute(
                f"SELECT {self._SCENE_COLS} FROM scene_assets WHERE id=? AND customer_id=?",
                (asset_id, customer_id),
            ).fetchone()
        return self._scene_row(r) if r else None

    def update_scene_asset(self, asset_id, tags, customer_id=LEGACY_CUSTOMER_ID):
        """다축 태그 편집. _SCENE_EDITABLE 화이트리스트 밖 필드는 조용히 무시.
        바꾼 게 있으면 True, 대상이 없거나(남의 것 포함) 바꿀 게 없으면 False."""
        sets, params = [], []
        for col in self._SCENE_EDITABLE:
            if col in tags:
                sets.append(f"{col}=?")
                params.append(self._scene_keywords(tags[col]) if col == "keywords"
                              else tags[col])
        if not sets:
            return False
        params += [asset_id, customer_id]
        with self._conn() as c:
            cur = c.execute(
                f"UPDATE scene_assets SET {', '.join(sets)} WHERE id=? AND customer_id=?",
                params,
            )
            return cur.rowcount > 0

    def delete_scene_asset(self, asset_id, customer_id=LEGACY_CUSTOMER_ID):
        with self._conn() as c:
            cur = c.execute("DELETE FROM scene_assets WHERE id=? AND customer_id=?",
                            (asset_id, customer_id))
            return cur.rowcount > 0
```

- [ ] **Step 5: 테스트 통과 확인**

Run: `python -m pytest shopping_shorts/tests/test_store_scene.py -v`
Expected: 7 passed

- [ ] **Step 6: 기존 테스트 회귀 없음 확인**

Run: `python -m pytest shopping_shorts/tests/ -q 2>&1 | tail -5`
Expected: 기존 대비 **신규 실패 0**. (기존에 이미 깨져 있던 것이 있으면 그대로 두고 보고만 한다)

- [ ] **Step 7: 커밋**

```bash
git status --short shopping_shorts/store.py shopping_shorts/tests/test_store_scene.py
git commit -m "feat(scene): scene_assets 테이블 + 다축태그 CRUD(멀티테넌시 격리)" -- shopping_shorts/store.py shopping_shorts/tests/test_store_scene.py
git show --stat HEAD
```

---

## Task 2: `scene_assets.py` — 미디어 유틸(구간컷·오디오추출·썸네일)

**Files:**
- Create: `shopping_shorts/scene_assets.py`
- Test: `shopping_shorts/tests/test_scene_assets.py` (신규)

**Interfaces:**
- Consumes: `frame_extract.extract_frame_at(video_path, dest_dir, timestamp_sec, filename)`(frame_extract.py:47, 실패 시 `None`)
- Produces:
  - `make_clip(src_path, start, end, out_path) -> Path` (실패 시 `RuntimeError`)
  - `extract_audio(clip_path, out_path) -> Path` (실패 시 `RuntimeError`)
  - `make_poster(media_path, out_path) -> Path | None` (실패 시 조용히 `None`)
  - `probe_duration(path) -> float` (실패 시 `0.0`)

**설계 노트**: `frame_extract.py`의 골격을 그대로 따른다 — 순수함수, `subprocess.run(cmd, capture_output=True, check=False)`, `dest_dir.mkdir(parents=True, exist_ok=True)`, 치명적 실패는 `RuntimeError` / 없어도 되는 것은 조용한 `None`. 썸네일은 새로 짜지 않고 `frame_extract.extract_frame_at`에 위임한다(DRY).

- [ ] **Step 1: 실패하는 테스트 작성**

`shopping_shorts/tests/test_scene_assets.py` 신규:

```python
import pytest
from shopping_shorts import scene_assets


def _fake_run_ok(created=None):
    """ffmpeg 성공 시뮬 — 실제로 만들었을 파일을 테스트가 대신 생성."""
    calls = []

    def run(cmd, capture_output=True, check=False):
        calls.append(cmd)
        if created:
            created.write_bytes(b"out")

        class R:
            returncode = 0
            stderr = b""
        return R()
    run.calls = calls
    return run


def _fake_run_fail(cmd, capture_output=True, check=False):
    class R:
        returncode = 1
        stderr = b"ffmpeg: error"
    return R()


def test_make_clip_cuts_segment_and_normalizes_spec(monkeypatch, tmp_path):
    src = tmp_path / "src.mp4"
    src.write_bytes(b"fake")
    out = tmp_path / "clip.mp4"
    run = _fake_run_ok(created=out)
    monkeypatch.setattr(scene_assets.subprocess, "run", run)

    result = scene_assets.make_clip(src, 3.0, 5.5, out)

    assert result == out and out.exists()
    cmd = run.calls[0]
    joined = " ".join(str(x) for x in cmd)
    assert "-ss" in cmd and "3.000" in joined          # 시작점
    assert "-t" in cmd and "2.500" in joined           # 길이 = end - start
    # 페이즈2 concat이 -c copy라 규격이 다르면 안 붙는다 — 720x1280/30fps/libx264/aac 고정
    assert "scale=720:1280:force_original_aspect_ratio=increase,crop=720:1280" in joined
    assert "-r" in cmd and "30" in cmd
    assert "libx264" in cmd and "aac" in cmd and "yuv420p" in cmd


def test_make_clip_raises_on_ffmpeg_failure(monkeypatch, tmp_path):
    src = tmp_path / "src.mp4"
    src.write_bytes(b"fake")
    monkeypatch.setattr(scene_assets.subprocess, "run", _fake_run_fail)

    with pytest.raises(RuntimeError, match="ffmpeg"):
        scene_assets.make_clip(src, 0, 2, tmp_path / "clip.mp4")


def test_make_clip_rejects_non_positive_span(tmp_path):
    src = tmp_path / "src.mp4"
    src.write_bytes(b"fake")

    with pytest.raises(ValueError, match="구간"):
        scene_assets.make_clip(src, 5.0, 5.0, tmp_path / "clip.mp4")
    with pytest.raises(ValueError, match="구간"):
        scene_assets.make_clip(src, 5.0, 2.0, tmp_path / "clip.mp4")


def test_extract_audio_produces_mp3(monkeypatch, tmp_path):
    clip = tmp_path / "clip.mp4"
    clip.write_bytes(b"fake")
    out = tmp_path / "sfx.mp3"
    run = _fake_run_ok(created=out)
    monkeypatch.setattr(scene_assets.subprocess, "run", run)

    result = scene_assets.extract_audio(clip, out)

    assert result == out and out.exists()
    cmd = run.calls[0]
    assert "-vn" in cmd                       # 영상 버리고 오디오만
    assert "libmp3lame" in " ".join(str(x) for x in cmd)


def test_extract_audio_raises_on_failure(monkeypatch, tmp_path):
    clip = tmp_path / "clip.mp4"
    clip.write_bytes(b"fake")
    monkeypatch.setattr(scene_assets.subprocess, "run", _fake_run_fail)

    with pytest.raises(RuntimeError, match="ffmpeg"):
        scene_assets.extract_audio(clip, tmp_path / "sfx.mp3")


def test_make_poster_delegates_to_frame_extract(monkeypatch, tmp_path):
    media = tmp_path / "clip.mp4"
    media.write_bytes(b"fake")
    out = tmp_path / "poster.jpg"
    seen = {}

    def fake_extract(video_path, dest_dir, timestamp_sec, filename="frame_hint.jpg"):
        seen.update(video_path=video_path, dest_dir=dest_dir,
                    timestamp_sec=timestamp_sec, filename=filename)
        out.write_bytes(b"jpg")
        return out
    monkeypatch.setattr(scene_assets.frame_extract, "extract_frame_at", fake_extract)

    result = scene_assets.make_poster(media, out)

    assert result == out
    assert seen["video_path"] == media
    assert seen["dest_dir"] == tmp_path
    assert seen["timestamp_sec"] == 0
    assert seen["filename"] == "poster.jpg"


def test_make_poster_returns_none_on_failure(monkeypatch, tmp_path):
    media = tmp_path / "clip.mp4"
    media.write_bytes(b"fake")
    monkeypatch.setattr(scene_assets.frame_extract, "extract_frame_at",
                        lambda *a, **k: None)

    # 썸네일은 없어도 되는 것 — 예외 대신 None으로 파이프라인 유지
    assert scene_assets.make_poster(media, tmp_path / "poster.jpg") is None


def test_probe_duration_parses_ffprobe(monkeypatch, tmp_path):
    def fake_run(cmd, capture_output=True, check=False):
        assert "ffprobe" in cmd[0]

        class R:
            returncode = 0
            stdout = b"2.53\n"
            stderr = b""
        return R()
    monkeypatch.setattr(scene_assets.subprocess, "run", fake_run)

    assert scene_assets.probe_duration(tmp_path / "x.mp4") == 2.53


def test_probe_duration_returns_zero_on_failure(monkeypatch, tmp_path):
    def fake_run(cmd, capture_output=True, check=False):
        class R:
            returncode = 1
            stdout = b""
            stderr = b"err"
        return R()
    monkeypatch.setattr(scene_assets.subprocess, "run", fake_run)

    assert scene_assets.probe_duration(tmp_path / "x.mp4") == 0.0


def test_probe_duration_survives_missing_ffprobe(monkeypatch, tmp_path):
    # ffprobe가 없으면 run은 returncode가 아니라 FileNotFoundError를 던진다.
    # 길이는 표시용이므로 저장 자체가 터지면 안 된다.
    def boom(cmd, capture_output=True, check=False):
        raise FileNotFoundError("ffprobe")
    monkeypatch.setattr(scene_assets.subprocess, "run", boom)

    assert scene_assets.probe_duration(tmp_path / "x.mp4") == 0.0
```

- [ ] **Step 2: 테스트 실패 확인**

Run: `python -m pytest shopping_shorts/tests/test_scene_assets.py -v`
Expected: FAIL — `ModuleNotFoundError: No module named 'shopping_shorts.scene_assets'`

- [ ] **Step 3: 모듈 구현**

`shopping_shorts/scene_assets.py` 신규:

```python
"""장면 라이브러리(재사용 짤 뱅크) — 구간컷·오디오추출·썸네일·Gemini 자동태깅.

순수 함수만 둔다(DB·HTTP 없음). frame_extract.py와 같은 골격:
ffmpeg는 subprocess로, 치명적 실패는 RuntimeError, 없어도 되는 것은 조용한 None.
"""
import json
import subprocess
from pathlib import Path

from . import frame_extract

# 페이즈2 concat이 -c copy(video_assemble.py:346)라 자산 클립도 비트 클립과
# **같은 규격**이어야 붙는다. video_assemble._OUT_W/_OUT_H와 같은 값.
_OUT_W, _OUT_H = 720, 1280
_SPEC_VF = (f"scale={_OUT_W}:{_OUT_H}:force_original_aspect_ratio=increase,"
            f"crop={_OUT_W}:{_OUT_H}")


def probe_duration(path):
    """미디어 길이(초). 실패하면 0.0 — 길이는 표시용이라 파이프라인을 끊지 않는다.

    ffprobe가 아예 없으면 subprocess.run은 returncode가 아니라 FileNotFoundError를
    던지므로 OSError도 삼킨다(ffprobe 없는 환경에서도 저장은 되어야 한다)."""
    cmd = ["ffprobe", "-v", "error", "-show_entries", "format=duration",
           "-of", "default=noprint_wrappers=1:nokey=1", str(path)]
    try:
        r = subprocess.run(cmd, capture_output=True, check=False)
    except OSError:
        return 0.0
    if r.returncode != 0:
        return 0.0
    try:
        return float((r.stdout or b"").decode().strip())
    except ValueError:
        return 0.0


def make_clip(src_path, start, end, out_path):
    """src_path의 [start,end] 구간을 잘라 규격(720x1280/30fps/libx264/aac)으로 통일.

    페이즈2에서 이 클립이 비트 클립들과 concat -c copy로 붙으므로 규격이
    어긋나면 렌더가 깨진다. 구간이 비었거나 뒤집혔으면 ValueError, ffmpeg
    실패면 RuntimeError."""
    dur = float(end) - float(start)
    if dur <= 0:
        raise ValueError(f"scene_assets: 구간이 잘못됨(start={start}, end={end})")
    out_path = Path(out_path)
    out_path.parent.mkdir(parents=True, exist_ok=True)
    cmd = [
        "ffmpeg", "-y",
        "-ss", f"{float(start):.3f}", "-i", str(src_path),
        "-t", f"{dur:.3f}",
        "-vf", _SPEC_VF, "-r", "30",
        "-c:v", "libx264", "-c:a", "aac", "-pix_fmt", "yuv420p",
        str(out_path),
    ]
    r = subprocess.run(cmd, capture_output=True, check=False)
    if r.returncode != 0:
        raise RuntimeError(f"ffmpeg 구간컷 실패: {r.stderr}")
    return out_path


def extract_audio(clip_path, out_path):
    """클립에서 오디오만 뽑아 mp3로(효과음 자산 소스). ffmpeg 실패면 RuntimeError."""
    out_path = Path(out_path)
    out_path.parent.mkdir(parents=True, exist_ok=True)
    cmd = ["ffmpeg", "-y", "-i", str(clip_path), "-vn",
           "-c:a", "libmp3lame", "-q:a", "2", str(out_path)]
    r = subprocess.run(cmd, capture_output=True, check=False)
    if r.returncode != 0:
        raise RuntimeError(f"ffmpeg 오디오 추출 실패: {r.stderr}")
    return out_path


def make_poster(media_path, out_path):
    """첫 프레임 썸네일. 실패해도 None(썸네일은 없어도 목록이 뜬다).
    프레임 1장 추출은 frame_extract.extract_frame_at이 이미 하는 일이라 위임."""
    out_path = Path(out_path)
    return frame_extract.extract_frame_at(media_path, out_path.parent, 0,
                                          filename=out_path.name)
```

- [ ] **Step 4: 테스트 통과 확인**

Run: `python -m pytest shopping_shorts/tests/test_scene_assets.py -v`
Expected: 10 passed

- [ ] **Step 5: 커밋**

```bash
git status --short shopping_shorts/scene_assets.py shopping_shorts/tests/test_scene_assets.py
git commit -m "feat(scene): 구간컷·오디오추출·썸네일 유틸(규격 720x1280 통일)" -- shopping_shorts/scene_assets.py shopping_shorts/tests/test_scene_assets.py
```

---

## Task 3: `scene_assets.autotag` — Gemini 다축 태그 초안

**Files:**
- Modify: `shopping_shorts/scene_assets.py` (autotag 추가)
- Modify: `shopping_shorts/tests/test_scene_assets.py` (테스트 추가)

**Interfaces:**
- Consumes: `edit_plan._vault_call(prompt, schema, max_tries=4) -> dict | None`(edit_plan.py:238)
- Produces: `autotag(frame_paths: list, context: dict) -> dict` — 항상 `{scene_desc, role, subject, tone, keywords}` 키를 가진 dict. 실패/무키면 **전 필드 빈 값**(`keywords`는 `[]`).

**설계 노트 — `_vault_call`은 수정하지 않는다**: `_vault_call`은 `contents=prompt`로 인자를 그대로 넘긴다(`edit_plan.py:246-249`). google-genai의 `contents`는 파츠 리스트를 받으므로 **리스트를 넘기면 멀티모달이 그냥 된다** — 시그니처 변경 0줄. 이미지 파츠 구성은 `similarity.py:47-53`의 `types.Part.from_bytes(data=..., mime_type="image/jpeg")`를 따른다. 단 `similarity.py`는 좁은 `SHORTS_GEMINI_KEYS` 풀을 쓰므로 **파츠 구성만 빌리고 키풀은 `_vault_call`의 캐스케이드를 쓴다**(Global Constraint 5).

`role`은 스펙 §4의 통제어휘 7종으로 고정: `훅|반전|CTA|비법공개|반응|전환|본문`.

- [ ] **Step 1: 실패하는 테스트 작성**

`shopping_shorts/tests/test_scene_assets.py` 끝에 추가:

```python
def test_autotag_returns_draft_from_gemini(monkeypatch, tmp_path):
    f1 = tmp_path / "f1.jpg"
    f1.write_bytes(b"jpgbytes")
    seen = {}

    def fake_vault_call(prompt, schema, max_tries=4):
        seen["prompt"] = prompt
        seen["schema"] = schema
        return {"scene_desc": "흰 가루를 숟가락으로 떠 그릇에 넣음", "role": "비법공개",
                "subject": "가루(밀가루·설탕류)", "tone": "비밀스러운·궁금",
                "keywords": ["숟가락", "가루"]}
    monkeypatch.setattr(scene_assets.edit_plan, "_vault_call", fake_vault_call)

    got = scene_assets.autotag([f1], {"category": "레시피", "caption": "이거 한 스푼이면 끝"})

    assert got["scene_desc"] == "흰 가루를 숟가락으로 떠 그릇에 넣음"
    assert got["role"] == "비법공개"
    assert got["subject"] == "가루(밀가루·설탕류)"
    assert got["keywords"] == ["숟가락", "가루"]
    # 프레임을 실제로 실어보냈는지(멀티모달) — contents가 파츠 리스트여야 한다
    assert isinstance(seen["prompt"], list)
    assert any(not isinstance(p, str) for p in seen["prompt"])   # 이미지 파트 존재
    assert any(isinstance(p, str) and "레시피" in p for p in seen["prompt"])  # 맥락 주입
    # 필드 생략 방지 — mime_type만으론 Gemini가 필드를 빠뜨린다(video_analysis.py:26)
    assert set(seen["schema"]["required"]) == {"scene_desc", "role", "subject", "tone", "keywords"}


def test_autotag_returns_shaped_empty_when_no_key(monkeypatch, tmp_path):
    f1 = tmp_path / "f1.jpg"
    f1.write_bytes(b"jpgbytes")
    monkeypatch.setattr(scene_assets.edit_plan, "_vault_call", lambda *a, **k: None)

    got = scene_assets.autotag([f1], {"category": "레시피"})

    # 무키/실패여도 예외 아님 — 사람이 수기 입력할 수 있게 형태만 온전한 빈 값
    assert got == {"scene_desc": "", "role": "", "subject": "", "tone": "", "keywords": []}


def test_autotag_with_no_frames_skips_gemini(monkeypatch, tmp_path):
    called = []
    monkeypatch.setattr(scene_assets.edit_plan, "_vault_call",
                        lambda *a, **k: called.append(1) or {})

    got = scene_assets.autotag([], {"category": "레시피"})

    assert called == []   # 보낼 화면이 없으면 호출 자체를 안 함(비용 절약)
    assert got["keywords"] == []


def test_autotag_clamps_role_to_controlled_vocabulary(monkeypatch, tmp_path):
    f1 = tmp_path / "f1.jpg"
    f1.write_bytes(b"jpgbytes")
    monkeypatch.setattr(scene_assets.edit_plan, "_vault_call",
                        lambda *a, **k: {"scene_desc": "x", "role": "아무말역할",
                                         "subject": "s", "tone": "t", "keywords": "a,b"})

    got = scene_assets.autotag([f1], {})

    assert got["role"] == ""            # 통제어휘 밖은 버림(사람이 고르게)
    assert got["keywords"] == ["a", "b"]  # 문자열로 와도 list로 정규화
```

- [ ] **Step 2: 테스트 실패 확인**

Run: `python -m pytest shopping_shorts/tests/test_scene_assets.py -k autotag -v`
Expected: FAIL — `AttributeError: module 'shopping_shorts.scene_assets' has no attribute 'edit_plan'`

- [ ] **Step 3: autotag 구현**

`shopping_shorts/scene_assets.py`의 import 블록을 교체:

```python
import json
import subprocess
from pathlib import Path

from google.genai import types

from . import edit_plan, frame_extract
```

파일 끝에 추가:

```python
# 스펙 §4 통제어휘 — 모델이 자유롭게 지어내면 매칭 키가 흩어진다.
_ROLES = ("훅", "반전", "CTA", "비법공개", "반응", "전환", "본문")

_AUTOTAG_SCHEMA = {
    "type": "object",
    "properties": {
        "scene_desc": {"type": "string"},
        "role": {"type": "string"},
        "subject": {"type": "string"},
        "tone": {"type": "string"},
        "keywords": {"type": "array", "items": {"type": "string"}},
    },
    # required를 빼면 Gemini가 필드를 생략한다(video_analysis.py:26).
    "required": ["scene_desc", "role", "subject", "tone", "keywords"],
}

_AUTOTAG_PROMPT = """너는 쇼츠 편집자다. 아래 이미지들은 저장하려는 **장면 클립의 대표 프레임**이다.
이 장면을 나중에 다른 영상에 재사용할 수 있도록 **다축 태그**를 만들어라.

맥락: 카테고리={category} / 원본 캡션={caption} / 원본 대본={script}

규칙:
- scene_desc: 화면에 실제로 보이는 것만 한 문장으로. 추측·해석 금지.
  (나쁜 예: "비법을 공개하는 장면" / 좋은 예: "흰 가루를 숟가락으로 떠 그릇에 넣음")
- subject: 소재·대상을 **구체적으로**. "한 스푼" 같은 모호한 말 금지 —
  가루인지 액체인지 세제인지 밥인지 구분되게 써라. (예: "가루(밀가루·설탕류)")
  이게 틀리면 나중에 세제 짤이 요리 영상에 들어간다.
- role: 반드시 다음 중 하나만: {roles}
- tone: 이 장면의 분위기(예: "비밀스러운·궁금", "놀라는·과장")
- keywords: 매칭 보조 키워드 3~6개(화면에 보이는 사물·동작·구도)
"""


def autotag(frame_paths, context):
    """대표 프레임 + 맥락 → 다축 태그 초안 dict. 사람이 확인·수정할 **초안**이다.

    무키·실패·프레임 없음 어떤 경우든 예외 없이 {scene_desc, role, subject, tone,
    keywords} 형태의 빈 값을 돌려준다(호출부가 특수처리 안 하게 — edit_plan의
    '형태가 온전한 빈 값' 폴백 관례)."""
    empty = {"scene_desc": "", "role": "", "subject": "", "tone": "", "keywords": []}
    if not frame_paths:
        return empty
    parts = []
    for p in frame_paths:
        try:
            with open(p, "rb") as fh:
                parts.append(types.Part.from_bytes(data=fh.read(), mime_type="image/jpeg"))
        except OSError:
            continue
    if not parts:
        return empty
    parts.append(_AUTOTAG_PROMPT.format(
        category=context.get("category") or "(모름)",
        caption=(context.get("caption") or "(없음)")[:300],
        script=(context.get("script") or "(없음)")[:1000],
        roles="|".join(_ROLES),
    ))
    # _vault_call은 contents=prompt로 그대로 넘기므로 파츠 리스트가 그대로 먹는다.
    # 키풀은 캐스케이드(general→ingest→embed→briefing) — 전용키 소진 회피.
    raw = edit_plan._vault_call(parts, _AUTOTAG_SCHEMA)
    if not raw:
        return empty
    kw = raw.get("keywords")
    if isinstance(kw, str):
        kw = [k.strip() for k in kw.split(",") if k.strip()]
    role = (raw.get("role") or "").strip()
    return {
        "scene_desc": (raw.get("scene_desc") or "").strip(),
        "role": role if role in _ROLES else "",
        "subject": (raw.get("subject") or "").strip(),
        "tone": (raw.get("tone") or "").strip(),
        "keywords": [str(k).strip() for k in (kw or []) if str(k).strip()],
    }
```

- [ ] **Step 4: 테스트 통과 확인**

Run: `python -m pytest shopping_shorts/tests/test_scene_assets.py -v`
Expected: 14 passed (Task2의 10개 + autotag 4개)

- [ ] **Step 5: 커밋**

```bash
git commit -m "feat(scene): Gemini 다축태그 자동초안(캐스케이드 키풀·통제어휘 role·형태온전 폴백)" -- shopping_shorts/scene_assets.py shopping_shorts/tests/test_scene_assets.py
```

---

## Task 4: `/api/scene/*` 라우트

**Files:**
- Modify: `shopping_shorts/app.py` (경로 상수 ≈470줄 / 라우트는 `app.mount("/")` **위**, ≈1712줄 / 페이지 루프 ≈1723줄)
- Test: `shopping_shorts/tests/test_app_scene.py` (신규)

**Interfaces:**
- Consumes: Task1 `Store.add_scene_asset`·`list_scene_assets`·`get_scene_asset`·`update_scene_asset`·`delete_scene_asset` / Task2 `scene_assets.make_clip`·`extract_audio`·`make_poster`·`probe_duration` / Task3 `scene_assets.autotag` / 기존 `_cid(request)`(app.py:240)·`frame_extract.download_video`(frame_extract.py:8)
- Produces: HTTP 계약 —
  - `POST /api/scene/save/prepare` body `{source_kind, source_ref, src_url, start, end, category?, caption?, script?}` → `{ok, token, poster_url, duration, draft:{scene_desc,role,subject,tone,keywords}}`
  - `POST /api/scene/save/commit` body `{token, asset_type, render_mode?, keep_original_audio?, title, scene_desc, role, category, subject, tone, keywords[], source_kind, source_ref}` → `{ok, id}`
  - `POST /api/scene/upload` multipart `asset_type`, `title`, `file` → `{ok, id}`
  - `GET /api/scene/list?type=&category=&role=` → `{ok, items:[...]}`
  - `POST /api/scene/{asset_id}/update` body=태그 dict → `{ok}`
  - `POST /api/scene/{asset_id}/delete` → `{ok}`
  - `GET /api/scene/{asset_id}/media` → 파일(Range 지원) 또는 404

**⚠️ 보안 — 이 태스크의 핵심**:
1. `commit`은 클라이언트가 준 `media_path`를 **절대 받지 않는다**. `token`(32자 hex)만 받아 서버가 경로를 재구성한다. 토큰은 정규식 `^[0-9a-f]{32}$`로 검증 — 아니면 422.
2. 모든 라우트는 `_cid(request)`로 격리. `/api/scene/*`를 `_AUTH_ALLOW`에 **넣지 않는다**(Global Constraint 9).
3. `media` 라우트는 DB에서 `get_scene_asset(id, cid)`로 소유권을 먼저 확인한 뒤에만 파일을 낸다 — 남의 자산 id를 찍어도 404.

**설계 노트**: prepare가 만든 클립은 commit 전에는 DB에 없는 **고아 파일**이다. 사용자가 모달을 닫으면 남는다. 청소는 페이즈2로 미룬다(YAGNI) — 용량이 문제 되면 그때 `created_at` 없는 고아를 지우는 크론을 붙인다.

- [ ] **Step 1: 실패하는 테스트 작성**

`shopping_shorts/tests/test_app_scene.py` 신규:

```python
import re
import pytest
from fastapi.testclient import TestClient
from shopping_shorts import app as app_mod
from shopping_shorts.store import Store


@pytest.fixture
def client(tmp_path, monkeypatch):
    db = tmp_path / "t.db"
    Store(db)  # 스키마 생성
    monkeypatch.setattr(app_mod, "DB_PATH", db)
    monkeypatch.setattr(app_mod, "_SCENE_ASSETS_DIR", tmp_path / "scene_assets")
    return TestClient(app_mod.app)


def _mk_asset(client, tmp_path, **over):
    d = tmp_path / "scene_assets"
    d.mkdir(parents=True, exist_ok=True)
    media = d / "deadbeefdeadbeefdeadbeefdeadbeef.mp4"
    media.write_bytes(b"video")
    a = {"asset_type": "clip", "render_mode": "cutaway", "media_path": str(media),
         "title": "가루 한스푼", "category": "레시피", "role": "비법공개",
         "keywords": ["가루"], "source_kind": "reference", "source_ref": "ABC"}
    a.update(over)
    return Store(app_mod.DB_PATH).add_scene_asset(a)


def test_prepare_cuts_clip_and_returns_draft(client, tmp_path, monkeypatch):
    monkeypatch.setattr(app_mod.frame_extract, "download_video",
                        lambda url, dest: tmp_path / "src.mp4")
    (tmp_path / "src.mp4").write_bytes(b"src")
    monkeypatch.setattr(app_mod.scene_assets, "make_clip",
                        lambda src, s, e, out: (out.parent.mkdir(parents=True, exist_ok=True),
                                                out.write_bytes(b"clip"), out)[-1])
    monkeypatch.setattr(app_mod.scene_assets, "make_poster", lambda m, o: o)
    monkeypatch.setattr(app_mod.scene_assets, "probe_duration", lambda p: 2.5)
    monkeypatch.setattr(app_mod.scene_assets, "autotag",
                        lambda frames, ctx: {"scene_desc": "가루를 뜬다", "role": "비법공개",
                                             "subject": "가루", "tone": "궁금",
                                             "keywords": ["숟가락"]})

    r = client.post("/api/scene/save/prepare", json={
        "source_kind": "reference", "source_ref": "ABC",
        "src_url": "https://example.com/v.mp4", "start": 3.0, "end": 5.5,
        "category": "레시피", "caption": "한 스푼이면 끝"})

    assert r.status_code == 200
    d = r.json()
    assert d["ok"] is True
    assert re.fullmatch(r"[0-9a-f]{32}", d["token"])     # 서버가 발급한 토큰
    assert d["duration"] == 2.5
    assert d["draft"]["role"] == "비법공개"


def test_commit_rejects_bogus_token(client):
    for bad in ("../../etc/passwd", "abc", "", "x" * 32, "/tmp/evil"):
        r = client.post("/api/scene/save/commit", json={"token": bad, "title": "t",
                                                        "asset_type": "clip"})
        assert r.status_code == 422, f"token={bad!r}가 통과됨 — 경로조작 위험"
        assert r.json()["ok"] is False


def test_commit_rejects_client_supplied_media_path(client, tmp_path):
    # 토큰은 정상이지만 media_path를 끼워넣으면 무시돼야 한다
    d = tmp_path / "scene_assets"
    d.mkdir(parents=True, exist_ok=True)
    token = "a" * 32
    (d / f"{token}.mp4").write_bytes(b"clip")

    r = client.post("/api/scene/save/commit", json={
        "token": token, "asset_type": "clip", "title": "t",
        "media_path": "/etc/passwd"})

    assert r.status_code == 200
    got = Store(app_mod.DB_PATH).get_scene_asset(r.json()["id"])
    assert got["media_path"].endswith(f"{token}.mp4")
    assert "/etc/passwd" not in got["media_path"]


def test_commit_404_when_token_file_missing(client):
    r = client.post("/api/scene/save/commit", json={"token": "b" * 32, "asset_type": "clip",
                                                    "title": "t"})
    assert r.status_code == 404
    assert r.json()["ok"] is False


def test_commit_saves_and_list_returns_it(client, tmp_path):
    d = tmp_path / "scene_assets"
    d.mkdir(parents=True, exist_ok=True)
    token = "c" * 32
    (d / f"{token}.mp4").write_bytes(b"clip")

    r = client.post("/api/scene/save/commit", json={
        "token": token, "asset_type": "clip", "render_mode": "cutaway",
        "keep_original_audio": 1, "title": "가루 한스푼", "scene_desc": "가루를 뜬다",
        "role": "비법공개", "category": "레시피", "subject": "가루(밀가루·설탕류)",
        "tone": "궁금", "keywords": ["숟가락", "가루"],
        "source_kind": "reference", "source_ref": "ABC"})

    assert r.status_code == 200 and r.json()["ok"] is True
    items = client.get("/api/scene/list").json()["items"]
    assert len(items) == 1
    assert items[0]["title"] == "가루 한스푼"
    assert items[0]["keywords"] == ["숟가락", "가루"]


def test_commit_as_sfx_extracts_audio_from_clip(client, tmp_path, monkeypatch):
    """스펙 §5.3 — 효과음은 업로드뿐 아니라 잘라둔 화면짤에서 오디오만 뽑아도 만든다."""
    d = tmp_path / "scene_assets"
    d.mkdir(parents=True, exist_ok=True)
    token = "e" * 32
    (d / f"{token}.mp4").write_bytes(b"clip")
    seen = {}

    def fake_extract(clip, out):
        seen["clip"] = clip
        out.write_bytes(b"mp3")
        return out
    monkeypatch.setattr(app_mod.scene_assets, "extract_audio", fake_extract)

    r = client.post("/api/scene/save/commit", json={
        "token": token, "asset_type": "sfx", "title": "띠용", "render_mode": "cutaway"})

    assert r.status_code == 200
    assert seen["clip"].name == f"{token}.mp4"       # 같은 구간컷 재사용 — 재추출 없음
    got = Store(app_mod.DB_PATH).get_scene_asset(r.json()["id"])
    assert got["media_path"].endswith(f"{token}.mp3")
    assert got["render_mode"] is None                # sfx는 render_mode 없음(스펙 §4)
    assert got["poster_path"] is None


def test_commit_requires_title(client, tmp_path):
    d = tmp_path / "scene_assets"
    d.mkdir(parents=True, exist_ok=True)
    token = "d" * 32
    (d / f"{token}.mp4").write_bytes(b"clip")

    r = client.post("/api/scene/save/commit", json={"token": token, "asset_type": "clip",
                                                    "title": "  "})
    assert r.status_code == 422


def test_list_filters(client, tmp_path):
    _mk_asset(client, tmp_path, title="A", asset_type="clip", category="레시피")
    _mk_asset(client, tmp_path, title="B", asset_type="sfx", category="가전")

    assert len(client.get("/api/scene/list").json()["items"]) == 2
    got = client.get("/api/scene/list?type=sfx").json()["items"]
    assert [i["title"] for i in got] == ["B"]
    got = client.get("/api/scene/list?category=레시피").json()["items"]
    assert [i["title"] for i in got] == ["A"]


def test_update_edits_tags(client, tmp_path):
    aid = _mk_asset(client, tmp_path)

    r = client.post(f"/api/scene/{aid}/update", json={"title": "설탕 한스푼",
                                                      "keywords": ["설탕"]})

    assert r.status_code == 200 and r.json()["ok"] is True
    got = Store(app_mod.DB_PATH).get_scene_asset(aid)
    assert got["title"] == "설탕 한스푼"
    assert got["keywords"] == ["설탕"]


def test_delete_removes(client, tmp_path):
    aid = _mk_asset(client, tmp_path)

    assert client.post(f"/api/scene/{aid}/delete").json()["ok"] is True
    assert Store(app_mod.DB_PATH).get_scene_asset(aid) is None


def test_media_serves_file(client, tmp_path):
    aid = _mk_asset(client, tmp_path)

    r = client.get(f"/api/scene/{aid}/media")

    assert r.status_code == 200
    assert r.headers["content-type"] == "video/mp4"
    assert r.content == b"video"


def test_media_404_for_unknown_id(client):
    assert client.get("/api/scene/99999/media").status_code == 404


def test_upload_saves_sfx(client, tmp_path):
    r = client.post("/api/scene/upload",
                    data={"asset_type": "sfx", "title": "띠용"},
                    files={"file": ("a.mp3", b"mp3bytes", "audio/mpeg")})

    assert r.status_code == 200 and r.json()["ok"] is True
    got = Store(app_mod.DB_PATH).get_scene_asset(r.json()["id"])
    assert got["asset_type"] == "sfx"
    assert got["title"] == "띠용"
    assert got["media_path"].endswith(".mp3")


def test_upload_rejects_bad_extension(client):
    r = client.post("/api/scene/upload",
                    data={"asset_type": "sfx", "title": "나쁜것"},
                    files={"file": ("evil.exe", b"MZ", "application/octet-stream")})

    assert r.status_code == 422
    assert r.json()["ok"] is False


def test_scene_api_is_not_in_auth_allowlist():
    # /api/scene/*가 allowlist에 들어가면 남의 자산을 인증 없이 만질 수 있다
    for p in app_mod._AUTH_ALLOW:
        assert not p.startswith("/api/scene"), f"{p}가 allowlist에 있음"
```

- [ ] **Step 2: 테스트 실패 확인**

Run: `python -m pytest shopping_shorts/tests/test_app_scene.py -v`
Expected: FAIL — `AttributeError: module 'shopping_shorts.app' has no attribute '_SCENE_ASSETS_DIR'`

- [ ] **Step 3: 경로 상수 + import 추가**

`shopping_shorts/app.py:470`(`_WIKI_MEDIA_DIR` 줄) **바로 뒤**에 추가:

```python
_SCENE_ASSETS_DIR = Path(__file__).parent / "data" / "scene_assets"  # 장면 라이브러리 자산 영구보관
```

파일 상단 import 블록에서 `scene_assets`가 import돼 있는지 확인하고, 없으면 기존 `from . import ...` 줄에 추가(테스트가 `app_mod.scene_assets`·`app_mod.frame_extract`로 monkeypatch하므로 **모듈 객체로** import해야 한다):

```python
from . import frame_extract, scene_assets
```

- [ ] **Step 4: 라우트 구현**

`shopping_shorts/app.py`의 `# 정적 프론트 (마운트는 맨 마지막)` 주석 **위**(≈1712줄)에 삽입. ⚠️ `app.mount("/")` 아래에 두면 루트 캐치올에 먹혀 404가 난다.

```python
# ── 장면 라이브러리(재사용 짤 뱅크, 2026-07-15) ──
# 저장은 2스텝: prepare(구간컷+Gemini 태그초안) → 사람 확인 → commit(DB 확정).
# 재추출이 없고, 확인 없이 저장되지 않는다.
_SCENE_TOKEN_RE = re.compile(r"^[0-9a-f]{32}$")
_SCENE_EXT = {"clip": (".mp4",), "overlay": (".png", ".jpg", ".jpeg", ".webp", ".gif"),
              "sfx": (".mp3", ".wav", ".m4a", ".aac")}
_SCENE_MIME = {".mp4": "video/mp4", ".mp3": "audio/mpeg", ".wav": "audio/wav",
               ".m4a": "audio/mp4", ".aac": "audio/aac", ".png": "image/png",
               ".jpg": "image/jpeg", ".jpeg": "image/jpeg", ".webp": "image/webp",
               ".gif": "image/gif"}


def _scene_token_path(token, ext=".mp4"):
    """토큰 → 서버가 재구성한 자산 경로. 클라이언트 문자열을 경로로 쓰지 않기 위한 관문.
    토큰이 32자 hex가 아니면 None(호출부가 422)."""
    if not token or not _SCENE_TOKEN_RE.match(str(token)):
        return None
    return _SCENE_ASSETS_DIR / f"{token}{ext}"


@app.post("/api/scene/save/prepare")
def api_scene_save_prepare(request: Request, body: dict):
    """구간컷 + Gemini 다축 태그 초안 → 모달 프리필용. 아직 DB 저장 안 함.
    body: {source_kind, source_ref, src_url, start, end, category?, caption?, script?}"""
    src_url = (body.get("src_url") or "").strip()
    if not src_url:
        return JSONResponse(status_code=422, content={"ok": False, "error": "src_url 필요"})
    try:
        start, end = float(body.get("start") or 0), float(body.get("end") or 0)
    except (TypeError, ValueError):
        return JSONResponse(status_code=422, content={"ok": False, "error": "start/end 숫자 필요"})
    if end - start <= 0:
        return JSONResponse(status_code=422, content={"ok": False, "error": "구간이 잘못됨"})
    token = uuid.uuid4().hex
    _SCENE_ASSETS_DIR.mkdir(parents=True, exist_ok=True)
    clip = _SCENE_ASSETS_DIR / f"{token}.mp4"
    poster = _SCENE_ASSETS_DIR / f"{token}.jpg"
    try:
        with tempfile.TemporaryDirectory() as td:
            src = frame_extract.download_video(src_url, td)
            scene_assets.make_clip(src, start, end, clip)
    except Exception as e:  # noqa: BLE001 — 다운로드/ffmpeg 실패는 사용자에게 그대로 알림
        return JSONResponse(status_code=502, content={"ok": False, "error": f"구간컷 실패: {e}"})
    scene_assets.make_poster(clip, poster)
    draft = scene_assets.autotag([poster] if poster.exists() else [], {
        "category": body.get("category"), "caption": body.get("caption"),
        "script": body.get("script"),
    })
    return {"ok": True, "token": token, "duration": scene_assets.probe_duration(clip),
            "poster_url": f"/api/scene/prepared/{token}/poster", "draft": draft}


@app.get("/api/scene/prepared/{token}/poster")
def api_scene_prepared_poster(token: str):
    """commit 전 미리보기 썸네일. 토큰은 hex 검증 — 경로조작 차단."""
    p = _scene_token_path(token, ".jpg")
    if p is None or not p.exists():
        return Response(status_code=404, content=b"")
    return FileResponse(str(p), media_type="image/jpeg")


@app.post("/api/scene/save/commit")
def api_scene_save_commit(request: Request, body: dict):
    """사람이 확인·수정한 태그로 자산 확정 저장. media_path는 **토큰으로만** 재구성 —
    body의 media_path는 있어도 무시한다(경로조작 차단)."""
    p = _scene_token_path(body.get("token"))
    if p is None:
        return JSONResponse(status_code=422, content={"ok": False, "error": "token 형식 오류"})
    if not p.exists():
        return JSONResponse(status_code=404, content={"ok": False, "error": "준비된 클립 없음"})
    title = (body.get("title") or "").strip()
    if not title:
        return JSONResponse(status_code=422, content={"ok": False, "error": "title 필요"})
    asset_type = body.get("asset_type") or "clip"
    media = p
    poster = p.with_suffix(".jpg")
    if asset_type == "sfx":
        # 스펙 §5.3 — 효과음은 업로드 말고 **화면짤에서 오디오만 뽑는** 경로도 있다.
        # 같은 구간컷을 재사용하므로 재추출이 없다.
        try:
            media = scene_assets.extract_audio(p, p.with_suffix(".mp3"))
        except RuntimeError as e:
            return JSONResponse(status_code=502,
                                content={"ok": False, "error": f"오디오 추출 실패: {e}"})
        poster = None
    aid = Store(DB_PATH).add_scene_asset({
        "asset_type": asset_type,
        "render_mode": body.get("render_mode") if asset_type == "clip" else None,
        "media_path": str(media),
        "poster_path": str(poster) if (poster and poster.exists()) else None,
        "duration": scene_assets.probe_duration(media),
        "keep_original_audio": body.get("keep_original_audio"),
        "title": title, "scene_desc": body.get("scene_desc"), "role": body.get("role"),
        "category": body.get("category"), "subject": body.get("subject"),
        "tone": body.get("tone"), "keywords": body.get("keywords"),
        "source_kind": body.get("source_kind"), "source_ref": body.get("source_ref"),
    }, customer_id=_cid(request))
    return {"ok": True, "id": aid}


@app.post("/api/scene/upload")
async def api_scene_upload(request: Request, asset_type: str = Form(...),
                           title: str = Form(...), file: UploadFile = File(...),
                           category: str = Form(""), role: str = Form(""),
                           subject: str = Form(""), tone: str = Form("")):
    """효과음·오버레이 직접 업로드. 확장자 화이트리스트 밖은 거부."""
    if not (title or "").strip():
        return JSONResponse(status_code=422, content={"ok": False, "error": "title 필요"})
    allowed = _SCENE_EXT.get(asset_type)
    if not allowed:
        return JSONResponse(status_code=422, content={"ok": False, "error": "asset_type 오류"})
    ext = os.path.splitext(file.filename or "")[1].lower()
    if ext not in allowed:
        return JSONResponse(status_code=422,
                            content={"ok": False, "error": f"{asset_type}는 {'/'.join(allowed)}만"})
    token = uuid.uuid4().hex
    _SCENE_ASSETS_DIR.mkdir(parents=True, exist_ok=True)
    dest = _SCENE_ASSETS_DIR / f"{token}{ext}"
    dest.write_bytes(await file.read())
    poster = None
    if asset_type == "overlay":
        pp = _SCENE_ASSETS_DIR / f"{token}.jpg"
        poster = scene_assets.make_poster(dest, pp)
    aid = Store(DB_PATH).add_scene_asset({
        "asset_type": asset_type, "media_path": str(dest),
        "poster_path": str(poster) if poster else None,
        "duration": scene_assets.probe_duration(dest),
        "title": title.strip(), "category": category or None, "role": role or None,
        "subject": subject or None, "tone": tone or None,
        "source_kind": "upload", "source_ref": file.filename or "",
    }, customer_id=_cid(request))
    return {"ok": True, "id": aid}


@app.get("/api/scene/list")
def api_scene_list(request: Request, type: str = "", category: str = "", role: str = ""):
    """이 고객의 장면 자산 목록(필터 옵션)."""
    items = Store(DB_PATH).list_scene_assets(
        customer_id=_cid(request), asset_type=type or None,
        category=category or None, role=role or None)
    return {"ok": True, "items": items}


@app.post("/api/scene/{asset_id}/update")
def api_scene_update(request: Request, asset_id: int, body: dict):
    """다축 태그 편집. store가 화이트리스트로 media_path 등을 걸러낸다."""
    ok = Store(DB_PATH).update_scene_asset(asset_id, body, customer_id=_cid(request))
    if not ok:
        return JSONResponse(status_code=404, content={"ok": False, "error": "자산 없음"})
    return {"ok": True}


@app.post("/api/scene/{asset_id}/delete")
def api_scene_delete(request: Request, asset_id: int):
    ok = Store(DB_PATH).delete_scene_asset(asset_id, customer_id=_cid(request))
    if not ok:
        return JSONResponse(status_code=404, content={"ok": False, "error": "자산 없음"})
    return {"ok": True}


@app.get("/api/scene/{asset_id}/media")
def api_scene_media(request: Request, asset_id: int):
    """자산 미디어 서빙(FileResponse가 Range를 처리해 seek 됨).
    DB로 소유권을 먼저 확인 — 남의 id를 찍어도 404."""
    a = Store(DB_PATH).get_scene_asset(asset_id, customer_id=_cid(request))
    if not a or not a.get("media_path"):
        return Response(status_code=404, content=b"")
    f = Path(a["media_path"])
    if not f.exists():
        return Response(status_code=404, content=b"")
    return FileResponse(str(f), media_type=_SCENE_MIME.get(f.suffix.lower(),
                                                           "application/octet-stream"))


@app.get("/api/scene/{asset_id}/poster")
def api_scene_poster(request: Request, asset_id: int):
    a = Store(DB_PATH).get_scene_asset(asset_id, customer_id=_cid(request))
    if not a or not a.get("poster_path") or not Path(a["poster_path"]).exists():
        return Response(status_code=404, content=b"")
    return FileResponse(a["poster_path"], media_type="image/jpeg")
```

- [ ] **Step 5: 필요한 import 확인**

`app.py` 상단에 `re`, `uuid`, `tempfile`, `os`가 import돼 있는지 확인하고 없는 것만 추가. `Response`·`FileResponse`는 `fastapi.responses`에서, `Form`·`File`·`UploadFile`은 `fastapi`에서 온다(app.py:12-14에 이미 있음).

Run: `python -c "from shopping_shorts import app"` → Expected: 에러 없음

- [ ] **Step 6: 페이지 라우트 등록**

`shopping_shorts/app.py:1723`의 루프 튜플에 `"scene_library"` 추가:

```python
for _pg in ("discover", "find", "library", "mix", "outreach", "produce", "collection",
            "scene_library"):
```

- [ ] **Step 7: 테스트 통과 확인**

Run: `python -m pytest shopping_shorts/tests/test_app_scene.py -v`
Expected: 15 passed

- [ ] **Step 8: 전체 회귀 확인**

Run: `python -m pytest shopping_shorts/tests/ -q 2>&1 | tail -5`
Expected: 신규 실패 0

- [ ] **Step 9: 커밋**

```bash
git commit -m "feat(scene): /api/scene/* 라우트(2스텝 저장·토큰 경로재구성·소유권 격리)" -- shopping_shorts/app.py shopping_shorts/tests/test_app_scene.py
```

---

## Task 5: `scene_library.html` 관리 UI + 사이드바

**Files:**
- Create: `shopping_shorts/static/scene_library.html`
- Modify: `shopping_shorts/static/sidebar.js:5-18` (NAV 배열)

**Interfaces:**
- Consumes: Task4 `GET /api/scene/list`, `POST /api/scene/{id}/update`, `POST /api/scene/{id}/delete`, `POST /api/scene/upload`, `GET /api/scene/{id}/media`, `GET /api/scene/{id}/poster`
- Produces: 전역 함수 `loadScenes()`, `renderCard(a)`, `openEdit(id)`, `closeEdit()`, `saveEdit()`, `removeScene(id, btn)`, `doUpload()`

**설계 노트**: `library.html`의 관용구를 따른다 — 디자인 토큰 `:root` 변수, 템플릿 리터럴 + `innerHTML`, 모든 삽입값 `esc()` 통과, `.open` 클래스 토글 모달, 인라인 `onclick`. **단 두 가지는 의도적으로 다르게 한다**: (1) `.cards`를 진짜 타일 그리드로, (2) 모달 컨텍스트를 `document.querySelector('[onclick="..."]')` 역참조(library.html:254의 취약한 관용구) 대신 `data-id` + `window._editId`로 잡는다.

- [ ] **Step 1: 페이지 작성**

`shopping_shorts/static/scene_library.html` 신규:

```html
<!doctype html>
<meta charset="utf-8">
<title>장면 라이브러리</title>
<style>
  :root{--bg:#0a0e14;--panel:#111722;--line:#1e2735;--txt:#e6edf3;--sub:#8b98a9;--accent:#2f81f7;--gold:#ffd76b;}
  *{box-sizing:border-box;margin:0;padding:0}
  body{background:var(--bg);color:var(--txt);font-family:'Malgun Gothic',system-ui,sans-serif;display:flex;min-height:100vh}
  .main{flex:1;padding:28px 36px;overflow:auto}
  .head{display:flex;justify-content:space-between;align-items:center}
  .head h2{font-size:24px}
  .desc{color:var(--sub);font-size:13px;margin:6px 0 20px;line-height:1.6}
  .filters{display:flex;gap:8px;flex-wrap:wrap;margin-bottom:18px;align-items:center}
  select,input,textarea{background:#0d1420;color:var(--txt);border:1px solid var(--line);border-radius:8px;padding:8px 10px;font:inherit;font-size:13px}
  button{background:#1b2534;color:var(--txt);border:1px solid var(--line);border-radius:8px;padding:8px 12px;font:inherit;font-size:13px;cursor:pointer}
  button:hover{border-color:var(--accent)}
  button.primary{background:var(--accent);border-color:var(--accent)}
  /* library.html은 .cards가 세로 flex 스택이지만, 장면 자산은 썸네일 타일이 맞다 */
  .cards{display:grid;grid-template-columns:repeat(auto-fill,minmax(190px,1fr));gap:16px}
  .card{background:var(--panel);border:1px solid var(--line);border-radius:12px;overflow:hidden;display:flex;flex-direction:column}
  .card .thumb{width:100%;aspect-ratio:9/16;background:#060a10;object-fit:cover;display:block;cursor:pointer}
  .card .sfx-thumb{width:100%;aspect-ratio:9/16;display:flex;align-items:center;justify-content:center;font-size:40px;background:#060a10;cursor:pointer}
  .card .info{padding:10px;display:flex;flex-direction:column;gap:6px}
  .card .t{font-size:13px;font-weight:700;line-height:1.3}
  .card .m{font-size:11px;color:var(--sub);line-height:1.5}
  .badge{display:inline-block;font-size:10px;padding:2px 6px;border-radius:5px;font-weight:700}
  .b-replace{background:#123d24;color:#5ddb8e}
  .b-cutaway{background:#3d2b12;color:#e0a45e}
  .b-sfx{background:#2b1a3d;color:#b98ee0}
  .b-overlay{background:#122b3d;color:#5eb0e0}
  .card .acts{display:flex;gap:6px;margin-top:2px}
  .card .acts button{flex:1;padding:5px;font-size:11px}
  .empty{color:var(--sub);font-size:13px;padding:40px;text-align:center;grid-column:1/-1}
  /* 모달 — library.html의 .open 토글 패턴 */
  .m-bg{display:none;position:fixed;inset:0;background:rgba(0,0,0,.7);z-index:200;align-items:flex-start;justify-content:center;padding:24px;overflow:auto}
  .m-bg.open{display:flex}
  .m-box{background:var(--panel);border:1px solid var(--line);border-radius:14px;width:min(560px,100%);display:flex;flex-direction:column;max-height:92vh}
  .m-head{display:flex;justify-content:space-between;align-items:center;padding:14px 18px;border-bottom:1px solid var(--line)}
  .m-head b{font-size:15px}
  .m-head .x{background:none;border:none;color:var(--sub);font-size:20px;cursor:pointer}
  .m-body{padding:16px 18px;overflow:auto;display:flex;flex-direction:column;gap:12px}
  .fld{display:flex;flex-direction:column;gap:4px}
  .fld label{font-size:12px;color:var(--sub)}
  .fld .hint{font-size:11px;color:var(--sub);opacity:.8}
  .m-foot{padding:12px 18px;border-top:1px solid var(--line);display:flex;justify-content:flex-end;gap:8px}
</style>

<main class="main">
  <div class="head">
    <h2>🎞️ 장면 라이브러리</h2>
    <button class="primary" onclick="document.getElementById('upModal').classList.add('open')">⬆ 파일 업로드</button>
  </div>
  <div class="desc" id="desc">
    자주 쓰는 후킹 장면·효과음·오버레이를 모아두는 <b>재사용 짤 뱅크</b>입니다.
    믹스할 때 AI가 대본 비트에 맞는 걸 자동 배치합니다(페이즈2).
    <b style="color:var(--gold)">태그가 정확할수록 배치가 정확해집니다</b> — 특히 <b>소재</b>는 "한 스푼" 같은 모호한 말 대신 가루/액체/세제처럼 구분되게 쓰세요.
  </div>
  <div class="filters">
    <select id="fType" onchange="loadScenes()">
      <option value="">전체 타입</option>
      <option value="clip">화면짤</option>
      <option value="sfx">효과음</option>
      <option value="overlay">오버레이</option>
    </select>
    <select id="fCat" onchange="loadScenes()">
      <option value="">전체 카테고리</option>
      <option>레시피</option><option>생활용품</option><option>인테리어</option><option>가전</option>
    </select>
    <select id="fRole" onchange="loadScenes()">
      <option value="">전체 역할</option>
      <option>훅</option><option>반전</option><option>CTA</option><option>비법공개</option>
      <option>반응</option><option>전환</option><option>본문</option>
    </select>
  </div>
  <div class="cards" id="cards"><div class="empty">불러오는 중…</div></div>
</main>

<!-- 태그 편집 모달 -->
<div class="m-bg" id="editModal">
  <div class="m-box">
    <div class="m-head"><b>장면 태그 편집</b><button class="x" onclick="closeEdit()">✕</button></div>
    <div class="m-body">
      <div class="fld"><label>이름</label><input id="eTitle" placeholder="가루 한스푼"></div>
      <div class="fld"><label>화면 설명</label>
        <textarea id="eDesc" rows="2" placeholder="흰 가루를 숟가락으로 떠 그릇에 넣음"></textarea>
        <div class="hint">보이는 것만. 해석·추측 금지.</div></div>
      <div class="fld"><label>소재(가장 중요)</label>
        <input id="eSubject" placeholder="가루(밀가루·설탕류)">
        <div class="hint">이게 모호하면 세제 짤이 요리 영상에 들어갑니다.</div></div>
      <div class="fld"><label>역할</label>
        <select id="eRole">
          <option value="">(없음)</option>
          <option>훅</option><option>반전</option><option>CTA</option><option>비법공개</option>
          <option>반응</option><option>전환</option><option>본문</option>
        </select></div>
      <div class="fld"><label>카테고리</label>
        <select id="eCat">
          <option value="">(없음)</option>
          <option>레시피</option><option>생활용품</option><option>인테리어</option><option>가전</option>
        </select></div>
      <div class="fld"><label>분위기</label><input id="eTone" placeholder="비밀스러운·궁금"></div>
      <div class="fld"><label>키워드(콤마 구분)</label><input id="eKw" placeholder="숟가락, 가루, 클로즈업"></div>
      <div class="fld" id="eModeWrap"><label>화면짤 사용 방식</label>
        <select id="eMode">
          <option value="cutaway">컷어웨이(삽입 — 타임라인이 늘어남)</option>
          <option value="replace">화면교체(그 비트 화면을 이걸로)</option>
        </select></div>
      <div class="fld" id="eAudioWrap"><label><input type="checkbox" id="eAudio"> 원본 소리 살리기</label></div>
    </div>
    <div class="m-foot"><button onclick="closeEdit()">취소</button><button class="primary" onclick="saveEdit()">저장</button></div>
  </div>
</div>

<!-- 업로드 모달 -->
<div class="m-bg" id="upModal">
  <div class="m-box">
    <div class="m-head"><b>파일 업로드</b><button class="x" onclick="document.getElementById('upModal').classList.remove('open')">✕</button></div>
    <div class="m-body">
      <div class="fld"><label>타입</label>
        <select id="uType">
          <option value="sfx">효과음 (mp3/wav/m4a/aac)</option>
          <option value="overlay">오버레이 (png/jpg/webp/gif)</option>
        </select></div>
      <div class="fld"><label>이름</label><input id="uTitle" placeholder="띠용 효과음"></div>
      <div class="fld"><label>파일</label><input type="file" id="uFile"></div>
    </div>
    <div class="m-foot"><button class="primary" onclick="doUpload()">업로드</button></div>
  </div>
</div>

<script>
function esc(s){ return (s||'').replace(/[&<>"]/g, c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;'}[c])); }

var SCENES = [];

function badge(a){
  if(a.asset_type==='sfx') return '<span class="badge b-sfx">효과음</span>';
  if(a.asset_type==='overlay') return '<span class="badge b-overlay">오버레이</span>';
  return a.render_mode==='replace'
    ? '<span class="badge b-replace">화면교체</span>'
    : '<span class="badge b-cutaway">컷어웨이</span>';
}

function renderCard(a){
  const thumb = a.asset_type==='sfx'
    ? `<div class="sfx-thumb" onclick="preview(${a.id},'sfx')">🔊</div>`
    : `<img class="thumb" src="/api/scene/${a.id}/poster" loading="lazy"
            onclick="preview(${a.id},'${esc(a.asset_type)}')"
            onerror="this.replaceWith(Object.assign(document.createElement('div'),{className:'sfx-thumb',textContent:'🎞'}))">`;
  const meta = [a.category, a.role, a.subject].filter(Boolean).map(esc).join(' · ');
  return `<div class="card" data-id="${a.id}">
    ${thumb}
    <div class="info">
      <div class="t">${esc(a.title||'(이름 없음)')}</div>
      <div>${badge(a)} <span class="m">${a.duration?a.duration.toFixed(1)+'초':''}</span></div>
      <div class="m">${meta||'<i>태그 없음 — 편집하세요</i>'}</div>
      <div class="acts">
        <button onclick="openEdit(${a.id})">✏ 편집</button>
        <button onclick="removeScene(${a.id}, this)">삭제</button>
      </div>
    </div>
  </div>`;
}

function preview(id, type){
  const url = `/api/scene/${id}/media`;
  if(type==='sfx'){ new Audio(url).play(); return; }
  window.open(url, '_blank');
}

async function loadScenes(){
  const el = document.getElementById('cards');
  const q = new URLSearchParams({
    type: document.getElementById('fType').value,
    category: document.getElementById('fCat').value,
    role: document.getElementById('fRole').value,
  });
  try{
    const d = await (await fetch('/api/scene/list?'+q)).json();
    SCENES = d.items||[];
    if(!SCENES.length){ el.innerHTML='<div class="empty">아직 저장한 장면이 없습니다.<br>레퍼런스 랭킹·영상 제작소에서 구간을 잘라 담아보세요.</div>'; return; }
    el.innerHTML = SCENES.map(renderCard).join('');
  }catch(e){ el.innerHTML='<div class="empty">불러오기 실패 — 새로고침 해주세요.</div>'; }
}

function openEdit(id){
  const a = SCENES.find(x=>x.id===id);
  if(!a) return;
  window._editId = id;
  document.getElementById('eTitle').value = a.title||'';
  document.getElementById('eDesc').value = a.scene_desc||'';
  document.getElementById('eSubject').value = a.subject||'';
  document.getElementById('eRole').value = a.role||'';
  document.getElementById('eCat').value = a.category||'';
  document.getElementById('eTone').value = a.tone||'';
  document.getElementById('eKw').value = (a.keywords||[]).join(', ');
  document.getElementById('eMode').value = a.render_mode||'cutaway';
  document.getElementById('eAudio').checked = !!a.keep_original_audio;
  const isClip = a.asset_type==='clip';
  document.getElementById('eModeWrap').style.display = isClip?'':'none';
  document.getElementById('eAudioWrap').style.display = isClip?'':'none';
  document.getElementById('editModal').classList.add('open');
}

function closeEdit(){ document.getElementById('editModal').classList.remove('open'); }

async function saveEdit(){
  const id = window._editId;
  const body = {
    title: document.getElementById('eTitle').value.trim(),
    scene_desc: document.getElementById('eDesc').value.trim(),
    subject: document.getElementById('eSubject').value.trim(),
    role: document.getElementById('eRole').value,
    category: document.getElementById('eCat').value,
    tone: document.getElementById('eTone').value.trim(),
    keywords: document.getElementById('eKw').value.split(',').map(s=>s.trim()).filter(Boolean),
    render_mode: document.getElementById('eMode').value,
    keep_original_audio: document.getElementById('eAudio').checked?1:0,
  };
  if(!body.title){ alert('이름은 필수입니다.'); return; }
  try{
    const d = await (await fetch(`/api/scene/${id}/update`,{method:'POST',
      headers:{'Content-Type':'application/json'}, body:JSON.stringify(body)})).json();
    if(!d.ok){ alert(d.error||'저장 실패'); return; }
    closeEdit(); loadScenes();
  }catch(e){ alert('네트워크 오류'); }
}

async function removeScene(id, btn){
  if(!confirm('이 장면을 라이브러리에서 지울까요?')) return;
  try{
    const d = await (await fetch(`/api/scene/${id}/delete`,{method:'POST'})).json();
    if(!d.ok){ alert(d.error||'삭제 실패'); return; }
    btn.closest('.card').remove();
  }catch(e){ alert('네트워크 오류'); }
}

async function doUpload(){
  const f = document.getElementById('uFile').files[0];
  const title = document.getElementById('uTitle').value.trim();
  if(!f){ alert('파일을 골라주세요.'); return; }
  if(!title){ alert('이름을 입력해주세요.'); return; }
  const fd = new FormData();
  fd.append('asset_type', document.getElementById('uType').value);
  fd.append('title', title);
  fd.append('file', f);
  try{
    const d = await (await fetch('/api/scene/upload',{method:'POST', body:fd})).json();
    if(!d.ok){ alert(d.error||'업로드 실패'); return; }
    document.getElementById('upModal').classList.remove('open');
    document.getElementById('uTitle').value=''; document.getElementById('uFile').value='';
    loadScenes();
  }catch(e){ alert('네트워크 오류'); }
}

loadScenes();
</script>
<script src="/sidebar.js?v=2"></script>
```

- [ ] **Step 2: 사이드바 항목 추가**

`shopping_shorts/static/sidebar.js`의 "리서치" 그룹 마지막(≈10줄, `{ icon: "🔎", text: "신규채널 픽업", href: "/discover" },` 뒤)에 추가:

```javascript
      { icon: "🎞️", text: "장면 라이브러리", href: "/scene_library" },
```

(🎬는 "영상 제작소"가 이미 쓰므로 🎞️로 구분)

- [ ] **Step 3: 서버 기동 + 실브라우저 grounding**

⚠️ **정적 확인으로 끝내지 말 것.** 실제 렌더러에서 눈으로 본다.

Run(저장소 루트에서):
```bash
python -m uvicorn shopping_shorts.app:app --port 8899
```

브라우저로 `http://127.0.0.1:8899/scene_library` 접속해 다음을 **관측**:
1. 페이지가 뜨고 사이드바에 "🎞️ 장면 라이브러리"가 보이며 **활성 표시**된다(sidebar.js의 경로 정규화가 `/scene_library`와 매칭).
2. 자산 0개일 때 "아직 저장한 장면이 없습니다" 빈 상태가 뜬다.
3. **콘솔 에러 0개** (F12 → Console).
4. "⬆ 파일 업로드" → mp3 하나 골라 업로드 → 카드가 뜬다 → 🔊 클릭 시 소리가 난다.
5. "✏ 편집" → 태그 수정 → 저장 → 카드 메타가 갱신된다.
6. 삭제 → 카드가 사라지고 새로고침 후에도 없다.

- [ ] **Step 4: 커밋**

```bash
git commit -m "feat(scene): 장면 라이브러리 관리 UI(타일 그리드·태그편집·업로드) + 사이드바" -- shopping_shorts/static/scene_library.html shopping_shorts/static/sidebar.js
```

---

## Task 6: 저장 다리(레퍼런스 랭킹 → 장면 담기) + 통합 grounding

**Files:**
- Modify: `shopping_shorts/static/index.html` (랭킹 카드에 "🎞 장면 담기" 버튼 + 구간지정 모달)

**Interfaces:**
- Consumes: Task4 `POST /api/scene/save/prepare`, `POST /api/scene/save/commit`
- Produces: 전역 함수 `openSceneCut(shortcode, url, category)`, `runScenePrepare()`, `commitScene()`

**설계 노트**: 스펙 §5.1의 4개 진입점 중 **레퍼런스 랭킹(reference)만** 이 태스크에서 붙인다. 영상제작소(produce)는 `produce.html`이 지금 다른 세션의 대본믹스 통합 작업으로 대대적으로 재구성 중이라(NEXT_SESSION 🎬 트랙) 충돌이 확실하다 — **그 트랙이 끝난 뒤 별도 태스크로 붙인다**. 업로드 진입점은 Task5에서 이미 됐다. `url` 진입점은 스펙 §5.1에서 이미 페이즈2로 미뤄져 있다.

구간 지정은 재생 중인 `<video>`의 `currentTime`을 시작/끝으로 찍는 방식(별도 타임라인 UI 없이 — YAGNI).

- [ ] **Step 1: 랭킹 카드에 버튼 추가**

`shopping_shorts/static/index.html`에서 카드 액션 버튼들이 모인 곳(📝 대본추출 버튼 근처)을 찾아 옆에 추가. 카드 렌더 함수의 액션 영역에:

```javascript
<button onclick="openSceneCut('${sc}', '${esc(i.url||'')}', '${esc(i.category||'')}')">🎞 장면 담기</button>
```

- [ ] **Step 2: 구간지정 모달 마크업 추가**

`index.html`의 `</body>` 직전(다른 모달들 옆)에:

```html
<div class="m-bg" id="sceneModal" style="display:none;position:fixed;inset:0;background:rgba(0,0,0,.7);z-index:300;align-items:flex-start;justify-content:center;padding:24px;overflow:auto">
  <div style="background:#111722;border:1px solid #1e2735;border-radius:14px;width:min(560px,100%);color:#e6edf3">
    <div style="display:flex;justify-content:space-between;align-items:center;padding:14px 18px;border-bottom:1px solid #1e2735">
      <b>🎞 장면 담기</b>
      <button style="background:none;border:none;color:#8b98a9;font-size:20px;cursor:pointer" onclick="document.getElementById('sceneModal').style.display='none'">✕</button>
    </div>
    <div style="padding:16px 18px" id="sceneBody">
      <video id="sceneVid" controls playsinline style="width:100%;max-height:46vh;background:#000;border-radius:8px"></video>
      <div style="display:flex;gap:8px;align-items:center;margin:12px 0;font-size:13px">
        <button onclick="markScene('start')">⏱ 시작 찍기</button>
        <span id="sceneRange" style="color:#8b98a9">시작 — · 끝 —</span>
        <button onclick="markScene('end')">⏱ 끝 찍기</button>
      </div>
      <div id="sceneStep1">
        <button style="background:#2f81f7;border:none;color:#fff;border-radius:8px;padding:9px 14px;cursor:pointer" onclick="runScenePrepare()">✂ 자르고 AI 태그 뽑기</button>
        <div style="font-size:12px;color:#8b98a9;margin-top:6px">자른 뒤 태그 초안이 나옵니다. 확인·수정 후 저장하세요.</div>
      </div>
      <div id="sceneStep2" style="display:none;margin-top:12px;display:none;flex-direction:column;gap:10px"></div>
    </div>
  </div>
</div>
```

- [ ] **Step 3: JS 추가**

`index.html`의 `<script>` 안에:

```javascript
// ── 장면 담기(장면 라이브러리 다리, 2026-07-15) ──
var SCENE_CTX = {sc:'', url:'', category:'', start:null, end:null, token:null};

function openSceneCut(sc, url, category){
  SCENE_CTX = {sc:sc, url:url, category:category, start:null, end:null, token:null};
  document.getElementById('sceneVid').src = '/api/video?shortcode='+encodeURIComponent(sc);
  document.getElementById('sceneRange').textContent = '시작 — · 끝 —';
  document.getElementById('sceneStep1').style.display = '';
  document.getElementById('sceneStep2').style.display = 'none';
  document.getElementById('sceneModal').style.display = 'flex';
}

function markScene(which){
  const t = document.getElementById('sceneVid').currentTime;
  SCENE_CTX[which] = Math.round(t*100)/100;
  const f = v => v==null ? '—' : v.toFixed(2)+'초';
  document.getElementById('sceneRange').textContent =
    `시작 ${f(SCENE_CTX.start)} · 끝 ${f(SCENE_CTX.end)}`;
}

async function runScenePrepare(){
  if(SCENE_CTX.start==null || SCENE_CTX.end==null){ alert('시작과 끝을 찍어주세요.'); return; }
  if(SCENE_CTX.end - SCENE_CTX.start <= 0){ alert('끝이 시작보다 뒤여야 합니다.'); return; }
  const btn = event.target; btn.disabled = true; btn.textContent = '자르는 중…';
  try{
    const d = await (await fetch('/api/scene/save/prepare',{method:'POST',
      headers:{'Content-Type':'application/json'},
      body: JSON.stringify({source_kind:'reference', source_ref:SCENE_CTX.sc,
        src_url:SCENE_CTX.url, start:SCENE_CTX.start, end:SCENE_CTX.end,
        category:SCENE_CTX.category})})).json();
    if(!d.ok){ alert(d.error||'구간컷 실패'); return; }
    SCENE_CTX.token = d.token;
    showSceneDraft(d);
  }catch(e){ alert('네트워크 오류'); }
  finally{ btn.disabled = false; btn.textContent = '✂ 자르고 AI 태그 뽑기'; }
}

function showSceneDraft(d){
  const t = d.draft||{};
  const opt = (list, v) => list.map(x=>`<option ${x===v?'selected':''}>${x}</option>`).join('');
  document.getElementById('sceneStep1').style.display = 'none';
  const box = document.getElementById('sceneStep2');
  box.style.display = 'flex';
  box.innerHTML = `
    <div style="font-size:12px;color:#ffd76b">✂ ${d.duration?d.duration.toFixed(1):'?'}초 잘렸습니다. AI 태그 초안을 확인·수정하세요.</div>
    <input id="scTitle" placeholder="이름 (예: 가루 한스푼)" style="background:#0d1420;color:#e6edf3;border:1px solid #1e2735;border-radius:8px;padding:8px">
    <textarea id="scDesc" rows="2" placeholder="화면 설명" style="background:#0d1420;color:#e6edf3;border:1px solid #1e2735;border-radius:8px;padding:8px">${(t.scene_desc||'').replace(/</g,'&lt;')}</textarea>
    <input id="scSubject" placeholder="소재(구체적으로!)" value="${(t.subject||'').replace(/"/g,'&quot;')}" style="background:#0d1420;color:#e6edf3;border:1px solid #1e2735;border-radius:8px;padding:8px">
    <select id="scRole" style="background:#0d1420;color:#e6edf3;border:1px solid #1e2735;border-radius:8px;padding:8px">
      <option value="">(역할 없음)</option>${opt(['훅','반전','CTA','비법공개','반응','전환','본문'], t.role)}
    </select>
    <input id="scTone" placeholder="분위기" value="${(t.tone||'').replace(/"/g,'&quot;')}" style="background:#0d1420;color:#e6edf3;border:1px solid #1e2735;border-radius:8px;padding:8px">
    <input id="scKw" placeholder="키워드(콤마)" value="${(t.keywords||[]).join(', ').replace(/"/g,'&quot;')}" style="background:#0d1420;color:#e6edf3;border:1px solid #1e2735;border-radius:8px;padding:8px">
    <select id="scMode" style="background:#0d1420;color:#e6edf3;border:1px solid #1e2735;border-radius:8px;padding:8px">
      <option value="cutaway">컷어웨이(삽입)</option><option value="replace">화면교체</option>
    </select>
    <label style="font-size:12px;color:#8b98a9"><input type="checkbox" id="scAudio" checked> 원본 소리 살리기</label>
    <button style="background:#2f81f7;border:none;color:#fff;border-radius:8px;padding:9px 14px;cursor:pointer" onclick="commitScene()">💾 라이브러리에 저장</button>`;
}

async function commitScene(){
  const title = document.getElementById('scTitle').value.trim();
  if(!title){ alert('이름은 필수입니다.'); return; }
  const body = {
    token: SCENE_CTX.token, asset_type:'clip',
    render_mode: document.getElementById('scMode').value,
    keep_original_audio: document.getElementById('scAudio').checked?1:0,
    title: title,
    scene_desc: document.getElementById('scDesc').value.trim(),
    subject: document.getElementById('scSubject').value.trim(),
    role: document.getElementById('scRole').value,
    tone: document.getElementById('scTone').value.trim(),
    keywords: document.getElementById('scKw').value.split(',').map(s=>s.trim()).filter(Boolean),
    category: SCENE_CTX.category,
    source_kind:'reference', source_ref: SCENE_CTX.sc,
  };
  try{
    const d = await (await fetch('/api/scene/save/commit',{method:'POST',
      headers:{'Content-Type':'application/json'}, body:JSON.stringify(body)})).json();
    if(!d.ok){ alert(d.error||'저장 실패'); return; }
    document.getElementById('sceneModal').style.display='none';
    alert('장면 라이브러리에 담았습니다.');
  }catch(e){ alert('네트워크 오류'); }
}
```

- [ ] **Step 4: 중복 id / 전역함수 충돌 검사**

`index.html`은 이미 큰 파일이다. 새로 넣은 id·함수가 기존과 겹치면 조용히 깨진다.

Run:
```bash
grep -o 'id="[^"]*"' shopping_shorts/static/index.html | sort | uniq -d
grep -c "function openSceneCut\|function markScene\|function runScenePrepare\|function showSceneDraft\|function commitScene" shopping_shorts/static/index.html
```
Expected: 첫 명령 **출력 없음**(중복 id 0), 둘째 명령 **5**(각 함수 정의 1개씩)

- [ ] **Step 5: 통합 실렌더 grounding (이 태스크의 진짜 게이트)**

⚠️ 격리 테스트 말고 **실제 경로**로. 스펙 §11의 통합 검증.

Run(저장소 루트): `python -m uvicorn shopping_shorts.app:app --port 8899`

`http://127.0.0.1:8899/` 에서 **실제로 관측**:
1. 랭킹 카드의 "🎞 장면 담기" 클릭 → 모달에 영상이 뜨고 재생된다.
2. 재생 중 "⏱ 시작 찍기"/"⏱ 끝 찍기" → 시간이 표시된다.
3. "✂ 자르고 AI 태그 뽑기" → **실제로 클립이 잘리고** AI 태그 초안이 채워진다.
   - `data/scene_assets/<token>.mp4`가 생겼는지 파일로 확인.
   - 잘린 클립을 열어 **의도한 구간이 맞는지 눈으로 확인**.
   - `ffprobe data/scene_assets/<token>.mp4` → **720x1280 / 30fps** 확인 (페이즈2 concat이 -c copy라 규격이 틀리면 나중에 렌더가 깨진다).
   - AI 초안의 `subject`가 "한 스푼" 같은 모호한 말이 아니라 구체적인지 확인 — 모호하면 `_AUTOTAG_PROMPT`를 손본다.
4. 이름 넣고 "💾 라이브러리에 저장" → `/scene_library`에 그 장면이 **썸네일과 함께** 뜬다.
5. 콘솔 에러 0개.
6. **Gemini 무키 폴백 확인**: 키 없이도(또는 `_vault_call`이 None을 반환해도) prepare가 500이 아니라 **빈 초안으로 200**을 내는지.

- [ ] **Step 6: 커밋**

```bash
git status --short shopping_shorts/static/index.html
git commit -m "feat(scene): 레퍼런스 랭킹 → 장면 담기 다리(구간지정·AI초안 확인 후 저장)" -- shopping_shorts/static/index.html
```

- [ ] **Step 7: 전체 회귀 + push**

```bash
python -m pytest shopping_shorts/tests/ -q 2>&1 | tail -5
git branch --show-current          # main 확인
git pull --rebase --autostash origin main
git push origin main
```
Expected: 신규 실패 0, push 성공 → 서버 3분 크론이 자동 배포

---

## 페이즈1 완료 정의 (Exit Proof)

아래를 **관측**해야 완료다. 하나라도 "돌 것 같다"로 넘기지 않는다.

| # | 증거 |
|---|------|
| 1 | `python -m pytest shopping_shorts/tests/ -q` — 신규 실패 0 (Task1~4 유닛 36개 green: store 7 + scene_assets 14 + app 15) |
| 2 | 실서버에서 랭킹 → 구간컷 → AI 초안 → 저장 → `/scene_library`에 뜸 (Task6 Step5 육안) |
| 3 | 잘린 클립이 `ffprobe`로 **720x1280/30fps** — 페이즈2 concat `-c copy` 호환 |
| 4 | Gemini 무키/실패 시 prepare가 **빈 초안으로 200** (파이프라인 안 끊김) |
| 5 | 남의 `customer_id` 자산이 list/get/update/delete/media에서 전부 안 보임 (Task1·4 격리 테스트) |
| 6 | `token`에 `../` 등을 넣으면 422 (Task4 경로조작 테스트) |
| 7 | 브라우저 콘솔 에러 0 |

---

## 페이즈2로 미루는 것 (이 계획서 범위 밖)

- **AI 매칭·자동배치**(`edit_plan.match_scene_assets`, `mix_pipeline._plan_and_tts`의 asset_match 스텝)
- **렌더 4처리**(replace/cutaway/sfx/overlay) — 특히 **cutaway 자막 t0 싱크**가 최대 리스크(스펙 §7.1). 비트의 실제 화면 길이는 `primary.end - primary.start`가 아니라 나레이션에서 재계산된 `target_seconds`(`edit_plan.py:365-367`, 4.5자/초)임을 반드시 반영할 것.
- **믹스 검수 배치 패널**(스펙 §8.2)
- **영상제작소(produce) 저장 다리** — `produce.html` 대본믹스 통합 트랙이 끝난 뒤
- prepare 후 커밋 안 된 **고아 클립 청소** 크론
