# P1 — 훅 비주얼 강화 구현 계획

> **For agentic workers:** REQUIRED SUB-SKILL: superpowers:subagent-driven-development 또는 executing-plans. 스텝은 `- [ ]`.

**Goal:** 믹스 훅(첫 화면)이 준비동작·계량 대신 완성/핵심실증 장면으로 열리게 — 인벤토리에 이미 있는 `shot_role`/`is_key`를 훅 선택에 연결한다.

**Architecture:** `edit_plan.py` 프롬프트/인벤토리만 손댄다. 추출·respine·믹스 로직 무변경. 재업로드·새 Gemini 콜 없음(추출이 이미 영상 봐서 `shot_role`/`is_key` 태깅함).

**Tech Stack:** Python(edit_plan), pytest.

## Global Constraints
- 트랙 격리: 편집은 `.tracks/모션레벨훅/...` 절대경로. main 폴더 금지.
- **`scene_desc` 문자열 불변**: `_claim_key`/`_is_face_seg`가 그 문자열을 토큰화해 읽는다. 새 신호는 반드시 **별도 suffix**로만(모션레벨과 동일 방식).
- 프롬프트 과적재 금지: 훅 규칙은 2~3줄 린하게.
- 게이트: `python -m pytest shopping_shorts/ -q` 신규 회귀 0(기준선 대비).
- 커밋 `git -C .tracks/모션레벨훅`, 커밋 전 `status --porcelain`로 내 파일만 확인.

---

### Task 1: 인벤토리 라인에 shot_role·is_key 노출

**Files:**
- Modify: `shopping_shorts/edit_plan.py` (`_build_inventory` 98~101줄)
- Test: `shopping_shorts/tests/test_hook_visual.py` (Create)

**Interfaces:**
- Consumes: `seg_map[sid]["shot_role"]`("조리"/"완성"/"기타"), `["is_key"]`(bool) — 이미 존재(82~83줄).
- Produces: 인벤토리 라인에 `| 역할:{shot_role} | 실증:{Y|N}` suffix. `scene_desc`·seg_map 구조 불변.

- [ ] **Step 1: 실패 테스트 작성**

`shopping_shorts/tests/test_hook_visual.py`:
```python
"""P1 훅 비주얼: 인벤토리가 shot_role/is_key를 프롬프트에 노출하되 scene_desc는 불변."""
from shopping_shorts import edit_plan as ep


def _script():
    return [{"video_id": "v1", "segments": [
        {"seg_id": "v1-0", "start": 0, "end": 2, "text": "훅", "scene_desc": "인트로"},
        {"seg_id": "v1-1", "start": 2, "end": 4, "text": "재료 붓기", "scene_desc": "우유를 붓는다",
         "shot_role": "조리", "is_key": False},
        {"seg_id": "v1-2", "start": 4, "end": 6, "text": "완성", "scene_desc": "완성된 모찌",
         "shot_role": "완성", "is_key": True},
        {"seg_id": "v1-3", "start": 6, "end": 8, "text": "끝", "scene_desc": "인사"},
    ]}]


def test_inventory_exposes_shot_role_and_is_key():
    seg_map, block = ep._build_inventory(_script())
    # 가운데 세그(v1-1, v1-2)만 usable(첫·끝 제외)
    assert "역할:조리" in block and "역할:완성" in block
    assert "실증:Y" in block            # v1-2 is_key=True
    assert "실증:N" in block            # v1-1 is_key=False
    # scene_desc 문자열엔 역할/실증이 안 섞였다(_claim_key 오염 방지)
    assert seg_map["v1-2"]["scene_desc"] == "완성된 모찌"
    assert "역할" not in seg_map["v1-2"]["scene_desc"]
```

- [ ] **Step 2: 실패 확인**

Run: `cd .tracks/모션레벨훅 && python -m pytest shopping_shorts/tests/test_hook_visual.py::test_inventory_exposes_shot_role_and_is_key -q`
Expected: FAIL (`역할:` 없음).

- [ ] **Step 3: 구현** — `_build_inventory`의 `_ml_s` 다음, `lines.append` 직전에 suffix 추가:
```python
            _ml = seg.get("motion_level")
            _ml_s = f" | 모션:{_ml}" if _ml else ""
            # 훅 비주얼(2026-07-29): 추출이 영상 보고 태깅한 shot_role(완성/조리/기타)·is_key(핵심
            # 실증)를 라인에 노출 → 훅 규칙이 '완성/실증' 장면을 첫 화면으로 고르게 한다.
            # ★scene_desc 문자열엔 절대 안 섞는다(_claim_key 토큰 오염) — 모션처럼 별도 suffix로만.
            _role_s = f" | 역할:{seg.get('shot_role') or '기타'}"
            _key_s = f" | 실증:{'Y' if seg.get('is_key') else 'N'}"
            lines.append(
                f"[{sid}] ({length}s) 화면:{seg.get('scene_desc','')} | 말:{seg.get('text','')}"
                f"{_act_s}{_ben_s}{_ml_s}{_role_s}{_key_s}"
            )
```

- [ ] **Step 4: 통과 확인**

Run: `cd .tracks/모션레벨훅 && python -m pytest shopping_shorts/tests/test_hook_visual.py -q`
Expected: PASS.

- [ ] **Step 5: 커밋**
```bash
git -C .tracks/모션레벨훅 add shopping_shorts/edit_plan.py shopping_shorts/tests/test_hook_visual.py
git -C .tracks/모션레벨훅 commit -m "feat(대본 P1): 인벤토리에 shot_role·is_key 노출(scene_desc 불변)"
```

---

### Task 2: 훅 규칙 — 완성/is_key 우선, 준비동작 금지, 시간순 예외

**Files:**
- Modify: `shopping_shorts/edit_plan.py` (`_scene_first_candidates` 훅 섹션 — `[대본 뼈대 — 5단계 스파인]`의 ① 훅 줄 근처)

**Interfaces:**
- Consumes: Task 1의 인벤토리 라인(`역할:완성`·`실증:Y`).
- Produces: 없음(프롬프트 문자열 변경).

- [ ] **Step 1: 실패 테스트 작성** — `test_hook_visual.py`에 추가:
```python
def test_prompt_has_hook_visual_rule():
    import inspect
    src = inspect.getsource(ep._scene_first_candidates)
    assert "역할:완성" in src or "완성" in src   # 훅 규칙이 완성 장면을 지목
    assert "준비동작" in src                      # 준비동작·계량 금지 문구
    assert "시간순" in src                        # 훅은 시간순 예외 허용
```

- [ ] **Step 2: 실패 확인**

Run: `cd .tracks/모션레벨훅 && python -m pytest shopping_shorts/tests/test_hook_visual.py::test_prompt_has_hook_visual_rule -q`
Expected: FAIL.

- [ ] **Step 3: 구현** — `_scene_first_candidates` 프롬프트의 5단계 스파인 훅 줄
`"- ① 훅(0~3초): 스크롤을 멈추는 강한 한 방. 아래 훅 4패턴 중 이 소재에 가장 맞는 하나로.\n"`
바로 뒤에 훅 비주얼 규칙 2줄 삽입:
```python
        "- ① 훅(0~3초): 스크롤을 멈추는 강한 한 방. 아래 훅 4패턴 중 이 소재에 가장 맞는 하나로.\n"
        # 훅 비주얼(2026-07-29 사장님): 첫 화면이 '우유 붓기·재료 계량' 같은 준비동작이면 스크롤이
        # 안 멈춘다. 인벤토리의 역할:완성·실증:Y를 첫 화면으로 당긴다(훅만 시간순 예외).
        "  ★훅 비트(beats[0])의 seg_ids는 인벤토리에서 '역할:완성' 또는 '실증:Y'인 장면을 "
        "최우선으로 골라라. 준비동작·계량(재료 붓기·무게 재기·빈 그릇) 장면으로 훅을 열지 마라. "
        "완성/절정 장면이 영상 뒤쪽에 있어도 첫 화면으로 당겨라(훅은 시간순을 깨도 된다).\n"
```

- [ ] **Step 4: 통과 확인**

Run: `cd .tracks/모션레벨훅 && python -m pytest shopping_shorts/tests/test_hook_visual.py -q`
Expected: PASS(3건).

- [ ] **Step 5: 커밋**
```bash
git -C .tracks/모션레벨훅 add shopping_shorts/edit_plan.py shopping_shorts/tests/test_hook_visual.py
git -C .tracks/모션레벨훅 commit -m "feat(대본 P1): 훅 비트 규칙 — 완성/실증 장면 우선, 준비동작 금지, 훅 시간순 예외"
```

---

### Task 3: 게이트 + 배포

- [ ] **Step 1: 전체 게이트(로컬 예행)**

Run: `cd .tracks/모션레벨훅 && python -m pytest shopping_shorts/ -q 2>&1 | tail -5`
Expected: 신규 실패 0(기준선 대비). `.env` 실키 관련 edit_plan_build 2건은 기존 기준선(내 변경 무관) — 늘지 않았는지 확인.

- [ ] **Step 2: finish 배포**
```bash
cd "C:/Users/TheRose/Desktop/로또의 주식" && py tools/track.py finish 모션레벨훅
```
게이트 통과 시 병합됐는지 `git merge-base --is-ancestor <head> origin/main`로 확인(push 경쟁 밀리면 재-finish).

- [ ] **Step 3: 라이브 육안(사장님)**

새로 담아 믹스 → 첫 3초 프레임이 **완성/절정 장면**인지(현재=우유붓기 → 목표=완성 모찌). 프레임 추출로 대조.

## Self-Review
- 스펙 §5 P1 커버: Task1(인벤토리 노출)+Task2(훅 규칙). ✓
- 비목표 준수: 추출·respine·믹스 불변, edit_plan 프롬프트/인벤토리만. ✓
- scene_desc 불변 회귀 명시(Task1 test). ✓
- 플레이스홀더 없음(코드 전량). ✓
- 타입 일관: shot_role/is_key seg_map 기존 키 사용. ✓
