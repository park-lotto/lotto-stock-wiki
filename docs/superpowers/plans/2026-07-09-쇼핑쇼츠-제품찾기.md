# 쇼핑쇼츠 제품 찾기 (기능 ③ 소스매칭) 구현 계획

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 레퍼런스 랭킹의 영상 URL을 입력하면, 영상 자체를 Gemini로 분석해 키워드를 뽑고
플랫폼별 검색링크를 생성하며, 유튭은 실제로 수집해 Gemini로 유사도 검증까지 하는
`제품 찾기` 페이지를 구현한다.

**Architecture:** Apify가 이미 주는 `videoUrl`을 다운로드 → ffmpeg로 대표 프레임 추출 →
Gemini에 영상 파일 자체를 입력해 키워드/카테고리 추출 → 플랫폼별 URL 템플릿으로 검색링크
생성(API 불필요) → YouTube Data API로 실제 후보 수집 → Gemini로 후보-원본 프레임 비교해
유사도 재정렬 → "담기"로 소스풀 저장. 인스타/틱톡/SerpApi(Lens)/중국 플랫폼 실수집은 신규
키 확보 후 후속 계획.

**Tech Stack:** FastAPI, ffmpeg(subprocess), google-genai(File API), requests(YouTube Data API v3), SQLite.

## Global Constraints

- 신규 발급 필요: YouTube Data API 키(무료, Google Cloud Console) — Task 6부터 필요, 그 전엔 없어도 진행 가능.
- ffmpeg는 서버에 미설치 확인됨 — 배포 전 `sudo apt install -y ffmpeg` 필요(Task 2 완료 후 배포 단계에서).
- Gemini 호출은 기존 전용 키 풀 `shopping_shorts.config.SHORTS_GEMINI_KEYS`를 재사용한다(신규 키 그룹 만들지 않음).
- 이 계획은 SerpApi(Google Lens 실수집), Apify 검색전용 액터(인스타/틱톡), 더우인/샤오홍슈/Baidu
  실수집을 포함하지 않는다 — 설계문서 §8의 5~6단계이며 신규 키/액터 조사가 선행돼야 하는
  별도 후속 계획이다. 이번 계획은 검색링크 생성까지는 6개 플랫폼 전부 포함, 실수집은 YouTube만.
- 모든 신규 파일은 기존 `shopping_shorts/` 모듈 스타일(순수 함수 우선, 상태는 store.py로만)을 따른다.

---

### Task 1: video_url 저장 확장 + 신규 테이블 스키마

**Files:**
- Modify: `shopping_shorts/ranking.py:46-65` (`build_items` 함수)
- Modify: `shopping_shorts/store.py` (`_init_schema` + 신규 메서드)
- Test: `shopping_shorts/tests/test_ranking.py`, `shopping_shorts/tests/test_store.py`

**Interfaces:**
- Produces: `build_items()`가 반환하는 각 item dict에 `"video_url"` 키 추가(Apify `videoUrl` 필드 그대로).
- Produces: `Store.save_source_analysis(shortcode, keywords, frame_paths, analyzed_at)`,
  `Store.get_source_analysis(shortcode) -> dict|None`,
  `Store.save_candidates(shortcode, platform, candidates: list[dict])`,
  `Store.get_candidates(shortcode) -> list[dict]`,
  `Store.update_candidate_score(candidate_id, score)`,
  `Store.save_to_pool(origin_shortcode, candidate_id) -> None`,
  `Store.pool_items() -> list[dict]`.

- [ ] **Step 1: build_items에 video_url 추가 — 실패하는 테스트 작성**

`shopping_shorts/tests/test_ranking.py`에 추가:
```python
def test_build_items_includes_video_url():
    reels = [{"shortcode": "x", "timestamp": (NOW - timedelta(hours=1)).isoformat(),
              "commentsCount": 10, "likesCount": 0, "videoViewCount": 0,
              "displayUrl": "t.jpg", "url": "u", "caption": "",
              "videoUrl": "https://scontent.cdninstagram.com/video123.mp4"}]
    meta = {"name": "테스트", "followers": 100, "inpock": "", "username": "u"}
    items = build_items(reels, meta, prev_comments=lambda sc: None,
                        prev_delta=lambda sc: None, now=NOW, window_hours=48)
    assert items[0]["video_url"] == "https://scontent.cdninstagram.com/video123.mp4"
```

- [ ] **Step 2: 테스트 실패 확인**

Run: `python -m pytest shopping_shorts/tests/test_ranking.py::test_build_items_includes_video_url -v`
Expected: FAIL with `KeyError: 'video_url'`

- [ ] **Step 3: build_items에 필드 추가**

`shopping_shorts/ranking.py:46-65`의 dict에 한 줄 추가:
```python
        items.append({
            "shortcode": sc,
            "name": meta.get("name"),
            "username": meta.get("username"),
            "inpock": meta.get("inpock", ""),
            "followers": followers,
            "thumbnail": r.get("displayUrl", ""),
            "video_url": r.get("videoUrl", ""),
            "url": r.get("url", ""),
```
(기존 `"url"` 줄 바로 위에 `"video_url"` 줄 삽입, 나머지는 그대로.)

- [ ] **Step 4: 테스트 통과 확인**

Run: `python -m pytest shopping_shorts/tests/test_ranking.py -v`
Expected: 전체 PASS

- [ ] **Step 5: store.py 신규 테이블 + 메서드 — 실패하는 테스트 작성**

`shopping_shorts/tests/test_store.py`가 없으면 새로 만들고, 있으면 추가:
```python
import json
import pytest
from shopping_shorts.store import Store


@pytest.fixture
def store(tmp_path):
    return Store(tmp_path / "test.db")


def test_save_and_get_source_analysis(store):
    store.save_source_analysis(
        "sc1",
        keywords={"ko": ["바닥 청소"], "en": ["floor cleaner"], "zh": ["地板清洁剂"]},
        frame_paths=["/tmp/f1.jpg", "/tmp/f2.jpg"],
        analyzed_at="2026-07-09T00:00:00Z",
    )
    result = store.get_source_analysis("sc1")
    assert result["keywords"]["ko"] == ["바닥 청소"]
    assert result["frame_paths"] == ["/tmp/f1.jpg", "/tmp/f2.jpg"]


def test_get_source_analysis_missing_returns_none(store):
    assert store.get_source_analysis("nope") is None


def test_save_and_get_candidates(store):
    ids = store.save_candidates("sc1", "youtube", [
        {"url": "https://youtu.be/a", "title": "제목1", "thumbnail": "t1.jpg"},
        {"url": "https://youtu.be/b", "title": "제목2", "thumbnail": "t2.jpg"},
    ])
    assert len(ids) == 2
    candidates = store.get_candidates("sc1")
    assert len(candidates) == 2
    assert candidates[0]["platform"] == "youtube"
    assert candidates[0]["similarity_score"] is None


def test_update_candidate_score(store):
    ids = store.save_candidates("sc1", "youtube", [{"url": "u", "title": "t", "thumbnail": ""}])
    store.update_candidate_score(ids[0], 0.87)
    candidates = store.get_candidates("sc1")
    assert candidates[0]["similarity_score"] == 0.87


def test_save_to_pool_and_pool_items(store):
    ids = store.save_candidates("sc1", "youtube", [{"url": "u", "title": "t", "thumbnail": ""}])
    store.save_to_pool("sc1", ids[0])
    pool = store.pool_items()
    assert len(pool) == 1
    assert pool[0]["origin_shortcode"] == "sc1"
    assert pool[0]["url"] == "u"
```

- [ ] **Step 6: 테스트 실패 확인**

Run: `python -m pytest shopping_shorts/tests/test_store.py -v`
Expected: FAIL — `Store` object에 해당 메서드 없음(AttributeError)

- [ ] **Step 7: 스키마 + 메서드 구현**

`shopping_shorts/store.py`의 `_init_schema` 안, 기존 `last_run` 테이블 생성 다음에 추가:
```python
            c.execute("""
                CREATE TABLE IF NOT EXISTS source_analysis (
                    shortcode TEXT PRIMARY KEY,
                    keywords_json TEXT,
                    frames_json TEXT,
                    analyzed_at TEXT
                )
            """)
            c.execute("""
                CREATE TABLE IF NOT EXISTS source_candidates (
                    id INTEGER PRIMARY KEY AUTOINCREMENT,
                    source_shortcode TEXT,
                    platform TEXT,
                    url TEXT,
                    title TEXT,
                    thumbnail TEXT,
                    similarity_score REAL,
                    collected_at TEXT
                )
            """)
            c.execute("CREATE INDEX IF NOT EXISTS idx_candidates_source ON source_candidates(source_shortcode)")
            c.execute("""
                CREATE TABLE IF NOT EXISTS source_pool (
                    id INTEGER PRIMARY KEY AUTOINCREMENT,
                    origin_shortcode TEXT,
                    candidate_id INTEGER,
                    saved_at TEXT
                )
            """)
```

파일 끝(`load_last_run` 메서드 다음)에 추가:
```python
    def save_source_analysis(self, shortcode, keywords, frame_paths, analyzed_at):
        """영상 분석 결과(키워드+프레임 경로) 저장(덮어쓰기)."""
        with self._conn() as c:
            c.execute(
                "INSERT INTO source_analysis(shortcode, keywords_json, frames_json, analyzed_at) "
                "VALUES(?,?,?,?) ON CONFLICT(shortcode) DO UPDATE SET "
                "keywords_json=excluded.keywords_json, frames_json=excluded.frames_json, "
                "analyzed_at=excluded.analyzed_at",
                (shortcode, json.dumps(keywords, ensure_ascii=False),
                 json.dumps(frame_paths, ensure_ascii=False), analyzed_at),
            )

    def get_source_analysis(self, shortcode):
        """저장된 영상 분석 결과. 없으면 None."""
        with self._conn() as c:
            row = c.execute(
                "SELECT keywords_json, frames_json, analyzed_at FROM source_analysis WHERE shortcode=?",
                (shortcode,),
            ).fetchone()
        if not row:
            return None
        return {"keywords": json.loads(row[0]), "frame_paths": json.loads(row[1]), "analyzed_at": row[2]}

    def save_candidates(self, shortcode, platform, candidates):
        """플랫폼 검색 후보 저장. candidates: [{url,title,thumbnail}]. 저장된 id 리스트 반환."""
        ids = []
        with self._conn() as c:
            for cand in candidates:
                cur = c.execute(
                    "INSERT INTO source_candidates(source_shortcode, platform, url, title, "
                    "thumbnail, similarity_score, collected_at) VALUES(?,?,?,?,?,NULL,datetime('now'))",
                    (shortcode, platform, cand["url"], cand.get("title", ""), cand.get("thumbnail", "")),
                )
                ids.append(cur.lastrowid)
        return ids

    def get_candidates(self, shortcode):
        """이 소스에 대해 수집된 모든 후보(유사도 점수 내림차순, NULL은 뒤)."""
        with self._conn() as c:
            rows = c.execute(
                "SELECT id, platform, url, title, thumbnail, similarity_score FROM source_candidates "
                "WHERE source_shortcode=? ORDER BY (similarity_score IS NULL), similarity_score DESC",
                (shortcode,),
            ).fetchall()
        return [{"id": r[0], "platform": r[1], "url": r[2], "title": r[3],
                 "thumbnail": r[4], "similarity_score": r[5]} for r in rows]

    def update_candidate_score(self, candidate_id, score):
        """Gemini 검증 후 유사도 점수 갱신."""
        with self._conn() as c:
            c.execute("UPDATE source_candidates SET similarity_score=? WHERE id=?", (score, candidate_id))

    def save_to_pool(self, origin_shortcode, candidate_id):
        """확정 후보를 소스풀에 저장."""
        with self._conn() as c:
            c.execute(
                "INSERT INTO source_pool(origin_shortcode, candidate_id, saved_at) "
                "VALUES(?,?,datetime('now'))",
                (origin_shortcode, candidate_id),
            )

    def pool_items(self):
        """소스풀 전체 — candidate 정보와 조인."""
        with self._conn() as c:
            rows = c.execute("""
                SELECT p.origin_shortcode, c.platform, c.url, c.title, c.thumbnail,
                       c.similarity_score, p.saved_at
                FROM source_pool p JOIN source_candidates c ON p.candidate_id = c.id
                ORDER BY p.saved_at DESC
            """).fetchall()
        return [{"origin_shortcode": r[0], "platform": r[1], "url": r[2], "title": r[3],
                 "thumbnail": r[4], "similarity_score": r[5], "saved_at": r[6]} for r in rows]
```

- [ ] **Step 8: 테스트 통과 확인**

Run: `python -m pytest shopping_shorts/tests/test_store.py shopping_shorts/tests/test_ranking.py -v`
Expected: 전체 PASS

- [ ] **Step 9: Commit**

```bash
git add shopping_shorts/ranking.py shopping_shorts/store.py shopping_shorts/tests/test_ranking.py shopping_shorts/tests/test_store.py
git commit -m "feat(shopping_shorts): video_url 저장 + 제품찾기 신규 테이블(source_analysis/candidates/pool)"
```

---

### Task 2: 영상 다운로드 + ffmpeg 프레임 추출

**Files:**
- Create: `shopping_shorts/frame_extract.py`
- Test: `shopping_shorts/tests/test_frame_extract.py`

**Interfaces:**
- Consumes: 없음(순수 다운로드+subprocess 래퍼)
- Produces: `download_video(video_url, dest_dir) -> Path`(다운로드한 mp4 경로),
  `extract_frames(video_path, dest_dir, max_frames=6) -> list[Path]`(추출된 프레임 이미지 경로 리스트)

- [ ] **Step 1: 실패하는 테스트 작성**

`shopping_shorts/tests/test_frame_extract.py`:
```python
import subprocess
from pathlib import Path
import pytest
from shopping_shorts import frame_extract


def test_download_video_writes_file(monkeypatch, tmp_path):
    class FakeResp:
        status_code = 200
        def iter_content(self, chunk_size):
            yield b"fake video bytes"
        def raise_for_status(self):
            pass
    def fake_get(url, stream=True, timeout=None):
        assert url == "https://example.com/v.mp4"
        return FakeResp()
    monkeypatch.setattr(frame_extract.requests, "get", fake_get)

    path = frame_extract.download_video("https://example.com/v.mp4", tmp_path)

    assert path.exists()
    assert path.read_bytes() == b"fake video bytes"
    assert path.parent == tmp_path


def test_extract_frames_calls_ffmpeg_and_returns_existing_files(monkeypatch, tmp_path):
    video_path = tmp_path / "in.mp4"
    video_path.write_bytes(b"fake")

    calls = []
    def fake_run(cmd, capture_output=True, check=False):
        calls.append(cmd)
        # ffmpeg가 만들었을 프레임 파일들을 테스트가 직접 생성해 시뮬레이션
        for i in range(3):
            (tmp_path / f"frame_{i+1}.jpg").write_bytes(b"jpg")
        class FakeResult:
            returncode = 0
            stderr = b""
        return FakeResult()
    monkeypatch.setattr(frame_extract.subprocess, "run", fake_run)

    frames = frame_extract.extract_frames(video_path, tmp_path, max_frames=6)

    assert len(calls) == 1
    assert "ffmpeg" in calls[0][0]
    assert len(frames) == 3
    assert all(f.exists() for f in frames)


def test_extract_frames_raises_on_ffmpeg_failure(monkeypatch, tmp_path):
    video_path = tmp_path / "in.mp4"
    video_path.write_bytes(b"fake")

    def fake_run(cmd, capture_output=True, check=False):
        class FakeResult:
            returncode = 1
            stderr = b"ffmpeg: error decoding"
        return FakeResult()
    monkeypatch.setattr(frame_extract.subprocess, "run", fake_run)

    with pytest.raises(RuntimeError, match="ffmpeg"):
        frame_extract.extract_frames(video_path, tmp_path, max_frames=6)
```

- [ ] **Step 2: 테스트 실패 확인**

Run: `python -m pytest shopping_shorts/tests/test_frame_extract.py -v`
Expected: FAIL — `ModuleNotFoundError: No module named 'shopping_shorts.frame_extract'`

- [ ] **Step 3: 구현**

`shopping_shorts/frame_extract.py`:
```python
"""영상 다운로드 + ffmpeg 장면전환 기반 프레임 추출. 순수 함수."""
import subprocess
import uuid
from pathlib import Path
import requests


def download_video(video_url, dest_dir):
    """video_url → dest_dir에 mp4로 다운로드, 저장된 경로 반환."""
    dest_dir = Path(dest_dir)
    dest_dir.mkdir(parents=True, exist_ok=True)
    path = dest_dir / f"{uuid.uuid4().hex}.mp4"
    resp = requests.get(video_url, stream=True, timeout=60)
    resp.raise_for_status()
    with open(path, "wb") as f:
        for chunk in resp.iter_content(chunk_size=1 << 16):
            f.write(chunk)
    return path


def extract_frames(video_path, dest_dir, max_frames=6):
    """video_path에서 장면전환 감지로 대표 프레임을 최대 max_frames장 추출.

    감지된 장면전환이 max_frames보다 적으면 있는 만큼만 반환(빈 리스트일 수도 있음
    — 호출부가 처리). ffmpeg 실패 시 RuntimeError.
    """
    dest_dir = Path(dest_dir)
    dest_dir.mkdir(parents=True, exist_ok=True)
    pattern = dest_dir / "frame_%02d.jpg"
    cmd = [
        "ffmpeg", "-y", "-i", str(video_path),
        "-vf", f"select='gt(scene,0.3)',showinfo",
        "-vsync", "vfr", "-frames:v", str(max_frames),
        str(pattern),
    ]
    result = subprocess.run(cmd, capture_output=True, check=False)
    if result.returncode != 0:
        raise RuntimeError(f"ffmpeg 프레임 추출 실패: {result.stderr}")
    return sorted(dest_dir.glob("frame_*.jpg"))
```

- [ ] **Step 4: 테스트 통과 확인**

Run: `python -m pytest shopping_shorts/tests/test_frame_extract.py -v`
Expected: 전체 PASS

- [ ] **Step 5: Commit**

```bash
git add shopping_shorts/frame_extract.py shopping_shorts/tests/test_frame_extract.py
git commit -m "feat(shopping_shorts): 영상 다운로드 + ffmpeg 장면전환 프레임 추출"
```

---

### Task 3: 플랫폼별 검색링크 생성 (순수 함수, API 불필요)

**Files:**
- Create: `shopping_shorts/search_links.py`
- Test: `shopping_shorts/tests/test_search_links.py`

**Interfaces:**
- Produces: `build_search_links(keywords: dict) -> dict` — `keywords`는
  `{"ko": [...], "en": [...], "zh": [...]}` 형태, 반환은
  `{"youtube": [url,...], "google": [...], "tiktok": [...], "instagram": [...], "douyin": [...], "xiaohongshu": [...]}`.
- Produces: `lens_search_url(frame_public_url: str) -> str`.

- [ ] **Step 1: 실패하는 테스트 작성**

`shopping_shorts/tests/test_search_links.py`:
```python
import urllib.parse
from shopping_shorts.search_links import build_search_links, lens_search_url


def test_build_search_links_uses_korean_for_kr_platforms_and_translated_for_cn():
    keywords = {"ko": ["바닥 끈적임 제거"], "en": ["floor stickiness remover"], "zh": ["地板黏腻清除剂"]}
    links = build_search_links(keywords)

    assert "youtube" in links and "google" in links and "tiktok" in links
    assert "instagram" in links and "douyin" in links and "xiaohongshu" in links

    kw_ko_encoded = urllib.parse.quote("바닥 끈적임 제거")
    assert any(kw_ko_encoded in u for u in links["youtube"])
    assert all(u.startswith("https://www.youtube.com/results?search_query=") for u in links["youtube"])

    kw_zh_encoded = urllib.parse.quote("地板黏腻清除剂")
    assert any(kw_zh_encoded in u for u in links["douyin"])
    assert any(kw_zh_encoded in u for u in links["xiaohongshu"])


def test_build_search_links_empty_keywords_returns_empty_lists():
    links = build_search_links({"ko": [], "en": [], "zh": []})
    assert links["youtube"] == []
    assert links["douyin"] == []


def test_lens_search_url_encodes_frame_url():
    url = lens_search_url("https://example.com/frame1.jpg")
    assert url.startswith("https://lens.google.com/uploadbyurl?url=")
    assert "https%3A%2F%2Fexample.com%2Fframe1.jpg" in url
```

- [ ] **Step 2: 테스트 실패 확인**

Run: `python -m pytest shopping_shorts/tests/test_search_links.py -v`
Expected: FAIL — `ModuleNotFoundError`

- [ ] **Step 3: 구현**

`shopping_shorts/search_links.py`:
```python
"""키워드 → 플랫폼별 검색링크 URL 템플릿. 순수 함수, 외부 API 불필요."""
import urllib.parse


def _q(text):
    return urllib.parse.quote(text)


def build_search_links(keywords):
    """keywords: {"ko":[...], "en":[...], "zh":[...]} → 플랫폼별 검색 URL 리스트.

    한국어권 플랫폼(유튭·구글·인스타·틱톡)은 ko+en 키워드, 중국어권(더우인·샤오홍슈)은
    zh 키워드로 검색 URL을 만든다.
    """
    ko = keywords.get("ko", [])
    en = keywords.get("en", [])
    zh = keywords.get("zh", [])
    kr_terms = ko + en
    cn_terms = zh

    return {
        "youtube": [f"https://www.youtube.com/results?search_query={_q(k)}" for k in kr_terms],
        "google": [f"https://www.google.com/search?q={_q(k)}" for k in kr_terms],
        "tiktok": [f"https://www.tiktok.com/search?q={_q(k)}" for k in kr_terms],
        "instagram": [f"https://www.instagram.com/explore/search/keyword/?q={_q(k)}" for k in kr_terms],
        "douyin": [f"https://www.douyin.com/search/{_q(k)}" for k in cn_terms],
        "xiaohongshu": [f"https://www.xiaohongshu.com/search_result?keyword={_q(k)}" for k in cn_terms],
    }


def lens_search_url(frame_public_url):
    """프레임 이미지의 공개 URL → Google Lens 역검색 링크."""
    return f"https://lens.google.com/uploadbyurl?url={_q(frame_public_url)}"
```

- [ ] **Step 4: 테스트 통과 확인**

Run: `python -m pytest shopping_shorts/tests/test_search_links.py -v`
Expected: 전체 PASS

- [ ] **Step 5: Commit**

```bash
git add shopping_shorts/search_links.py shopping_shorts/tests/test_search_links.py
git commit -m "feat(shopping_shorts): 플랫폼별 검색링크 생성 순수함수(6개 플랫폼)"
```

---

### Task 4: Gemini 영상 분석 (키워드 추출)

**Files:**
- Create: `shopping_shorts/video_analysis.py`
- Test: `shopping_shorts/tests/test_video_analysis.py`

**Interfaces:**
- Consumes: `shopping_shorts.config.SHORTS_GEMINI_KEYS`(기능② 구축, `comment_gen.py`와 동일 전용 키 풀)
- Produces: `analyze_video(video_path, caption) -> dict` — 반환 형태
  `{"keywords": {"ko":[...], "en":[...], "zh":[...]}, "category": "생활용품/홈케어"}`.
  Gemini 실패 시(쿼터 소진 등) `{"keywords": {"ko":[],"en":[],"zh":[]}, "category": ""}` 빈 결과.

**참고:** File API 업로드 패턴은 `pipeline/atoms/pdf_ingest.py:148-174`(`client.files.upload` +
`generate_content(contents=[file_obj, prompt])` + `finally`에서 `files.delete`)를 그대로
따르되, **영상은 업로드 후 서버측 처리가 필요해 PDF와 달리 ACTIVE 상태가 될 때까지 폴링이
필요하다** — `client.files.get(name=file_obj.name).state.name`이 `"ACTIVE"`가 될 때까지
짧은 간격으로 재확인(PROCESSING 상태면 대기, FAILED면 예외).

- [ ] **Step 1: 실패하는 테스트 작성**

`shopping_shorts/tests/test_video_analysis.py`:
```python
import pytest
from shopping_shorts import video_analysis


class FakeFileObj:
    def __init__(self, name, state):
        self.name = name
        self.state = type("S", (), {"name": state})()


def test_analyze_video_uploads_polls_and_parses(monkeypatch, tmp_path):
    video_path = tmp_path / "v.mp4"
    video_path.write_bytes(b"fake")

    monkeypatch.setattr(video_analysis, "SHORTS_GEMINI_KEYS", ["fake-key"])

    states = iter(["PROCESSING", "ACTIVE"])  # 첫 폴링은 처리중, 두번째는 완료
    deleted = []

    class FakeFiles:
        def upload(self, file, config):
            return FakeFileObj("files/abc", "PROCESSING")
        def get(self, name):
            return FakeFileObj(name, next(states))
        def delete(self, name):
            deleted.append(name)

    class FakeModels:
        def generate_content(self, **kw):
            class R: text = '{"keywords":{"ko":["바닥 청소"],"en":["floor cleaner"],"zh":["地板清洁"]},"category":"생활용품/홈케어"}'
            return R()

    class FakeClient:
        files = FakeFiles()
        models = FakeModels()

    monkeypatch.setattr(video_analysis, "_client_for_key", lambda key: FakeClient())
    monkeypatch.setattr(video_analysis.time, "sleep", lambda s: None)

    result = video_analysis.analyze_video(video_path, caption="여름 바닥 청소")

    assert result["keywords"]["ko"] == ["바닥 청소"]
    assert result["category"] == "생활용품/홈케어"
    assert deleted == ["files/abc"]  # 업로드한 파일 정리됐는지


def test_analyze_video_no_keys_raises(monkeypatch, tmp_path):
    video_path = tmp_path / "v.mp4"
    video_path.write_bytes(b"fake")
    monkeypatch.setattr(video_analysis, "SHORTS_GEMINI_KEYS", [])
    with pytest.raises(RuntimeError, match="SHORTS_GEMINI_KEY"):
        video_analysis.analyze_video(video_path, caption="")


def test_analyze_video_failed_processing_returns_empty(monkeypatch, tmp_path):
    video_path = tmp_path / "v.mp4"
    video_path.write_bytes(b"fake")
    monkeypatch.setattr(video_analysis, "SHORTS_GEMINI_KEYS", ["fake-key"])

    class FakeFiles:
        def upload(self, file, config):
            return FakeFileObj("files/abc", "PROCESSING")
        def get(self, name):
            return FakeFileObj(name, "FAILED")
        def delete(self, name):
            pass

    class FakeClient:
        files = FakeFiles()

    monkeypatch.setattr(video_analysis, "_client_for_key", lambda key: FakeClient())
    monkeypatch.setattr(video_analysis.time, "sleep", lambda s: None)

    result = video_analysis.analyze_video(video_path, caption="")
    assert result == {"keywords": {"ko": [], "en": [], "zh": []}, "category": ""}
```

- [ ] **Step 2: 테스트 실패 확인**

Run: `python -m pytest shopping_shorts/tests/test_video_analysis.py -v`
Expected: FAIL — `ModuleNotFoundError`

- [ ] **Step 3: 구현**

`shopping_shorts/video_analysis.py`:
```python
"""Gemini에 영상 파일 자체를 입력해 제품/장면 키워드 추출 — 캡션 텍스트만 보는
tubefactory와의 차별화 핵심(설계문서 §1 참고). 전용 키 풀(comment_gen.py와 동일 패턴)."""
import json
import time
from google import genai
from google.genai import types
from shopping_shorts.config import SHORTS_GEMINI_KEYS

_MODEL = "gemini-3-flash"  # 비디오 입력 지원 모델
_EMPTY = {"keywords": {"ko": [], "en": [], "zh": []}, "category": ""}

_PROMPT = """이 영상을 보고 어떤 제품/장면을 다루는지 파악해라.

캡션(참고용, 영상 내용이 우선): {caption}

다음 JSON으로만 출력해라:
{{
  "keywords": {{
    "ko": ["핵심 키워드 3~5개, 한국어"],
    "en": ["같은 키워드의 영어 번역"],
    "zh": ["같은 키워드의 중국어(간체) 번역"]
  }},
  "category": "제품 카테고리 (예: 생활용품/홈케어, 뷰티, 주방가전 등)"
}}

키워드는 영상 속 실제 제품·행동·특징을 반영해야 한다(캡션 문구를 그대로 베끼지 말고
영상에서 실제로 보이는 것 기준). 다른 텍스트 없이 JSON만 출력."""

_client_cache = {}


def _client_for_key(key):
    if key not in _client_cache:
        _client_cache[key] = genai.Client(api_key=key)
    return _client_cache[key]


def _wait_until_active(client, file_obj, max_wait_s=60, poll_interval=2):
    waited = 0
    state = file_obj.state.name
    while state == "PROCESSING" and waited < max_wait_s:
        time.sleep(poll_interval)
        waited += poll_interval
        file_obj = client.files.get(name=file_obj.name)
        state = file_obj.state.name
    if state != "ACTIVE":
        raise RuntimeError(f"video_analysis: 파일 처리 실패(state={state})")
    return file_obj


def analyze_video(video_path, caption):
    """영상 파일 → {"keywords": {...}, "category": "..."}. 실패 시 빈 결과."""
    if not SHORTS_GEMINI_KEYS:
        raise RuntimeError("video_analysis: SHORTS_GEMINI_KEY가 설정되지 않았습니다")
    client = _client_for_key(SHORTS_GEMINI_KEYS[0])
    file_obj = None
    try:
        with open(video_path, "rb") as fh:
            file_obj = client.files.upload(file=fh, config=types.UploadFileConfig(mime_type="video/mp4"))
        file_obj = _wait_until_active(client, file_obj)
        prompt = _PROMPT.format(caption=caption or "(캡션 없음)")
        resp = client.models.generate_content(
            model=_MODEL,
            contents=[file_obj, prompt],
            config=types.GenerateContentConfig(response_mime_type="application/json"),
        )
        data = json.loads(resp.text)
        return {
            "keywords": {
                "ko": data.get("keywords", {}).get("ko", []),
                "en": data.get("keywords", {}).get("en", []),
                "zh": data.get("keywords", {}).get("zh", []),
            },
            "category": data.get("category", ""),
        }
    except Exception:
        return dict(_EMPTY)
    finally:
        if file_obj is not None:
            try:
                client.files.delete(name=file_obj.name)
            except Exception:
                pass
```

- [ ] **Step 4: 테스트 통과 확인**

Run: `python -m pytest shopping_shorts/tests/test_video_analysis.py -v`
Expected: 전체 PASS

- [ ] **Step 5: Commit**

```bash
git add shopping_shorts/video_analysis.py shopping_shorts/tests/test_video_analysis.py
git commit -m "feat(shopping_shorts): Gemini 영상 직접분석으로 키워드/카테고리 추출"
```

---

### Task 5: /api/find/analyze 엔드포인트 + find.html 1차 화면

**Files:**
- Modify: `shopping_shorts/app.py`
- Create: `shopping_shorts/static/find.html`
- Test: `shopping_shorts/tests/test_app.py`

**Interfaces:**
- Consumes: Task 1~4의 `build_items`(video_url), `frame_extract.download_video/extract_frames`,
  `video_analysis.analyze_video`, `search_links.build_search_links/lens_search_url`,
  `Store.save_source_analysis/get_source_analysis`.
- Produces: `POST /api/find/analyze?shortcode=...` — 분석 실행+저장, 결과 반환.
  `GET /api/find/frame/{filename}` — 프레임 이미지 서빙.

- [ ] **Step 1: 실패하는 테스트 작성**

`shopping_shorts/tests/test_app.py`에 추가(기존 테스트 픽스처 재사용 — 파일 상단의
`client`/로그인 우회 픽스처가 이미 있으면 그대로 사용, 없으면 기존 테스트의 패턴을 따라
`TestClient(app)` + 필요한 monkeypatch로 인증 우회):
```python
def test_find_analyze_downloads_extracts_analyzes_and_saves(monkeypatch, client, tmp_path):
    from shopping_shorts import app as app_module
    from shopping_shorts.store import Store
    from shopping_shorts.config import DB_PATH

    # 마지막 수집 결과에 분석 대상 아이템을 하나 심어둔다
    store = Store(DB_PATH)
    store.save_last_run([{
        "shortcode": "sc1", "video_url": "https://example.com/v.mp4",
        "caption": "여름 바닥 청소", "thumbnail": "t.jpg",
    }], "2026-07-09T00:00:00Z")

    monkeypatch.setattr(app_module, "download_video", lambda url, dest: tmp_path / "v.mp4")
    (tmp_path / "v.mp4").write_bytes(b"fake")
    monkeypatch.setattr(app_module, "extract_frames",
                         lambda video_path, dest, max_frames: [tmp_path / "frame_01.jpg"])
    (tmp_path / "frame_01.jpg").write_bytes(b"jpg")
    monkeypatch.setattr(app_module, "analyze_video", lambda path, caption: {
        "keywords": {"ko": ["바닥 청소"], "en": ["floor cleaner"], "zh": ["地板清洁"]},
        "category": "생활용품/홈케어",
    })

    r = client.post("/api/find/analyze", params={"shortcode": "sc1"})
    assert r.status_code == 200
    d = r.json()
    assert d["ok"] is True
    assert d["keywords"]["ko"] == ["바닥 청소"]
    assert "youtube" in d["search_links"]
    assert len(d["frame_urls"]) == 1

    # 저장도 됐는지
    saved = store.get_source_analysis("sc1")
    assert saved["keywords"]["ko"] == ["바닥 청소"]


def test_find_analyze_unknown_shortcode_404(client):
    r = client.post("/api/find/analyze", params={"shortcode": "nope"})
    assert r.status_code == 404
```

- [ ] **Step 2: 테스트 실패 확인**

Run: `python -m pytest shopping_shorts/tests/test_app.py -k find_analyze -v`
Expected: FAIL — 404 라우트 없음(405 또는 404)

- [ ] **Step 3: app.py에 엔드포인트 추가**

`shopping_shorts/app.py` 상단 import에 추가:
```python
from shopping_shorts.frame_extract import download_video, extract_frames
from shopping_shorts.video_analysis import analyze_video
from shopping_shorts.search_links import build_search_links, lens_search_url
from shopping_shorts.config import PUBLIC_BASE_URL
```

**주의**: `lens_search_url()`은 Google 서버가 인터넷에서 직접 fetch할 수 있는 **절대 URL**이
필요하다 — `/api/find/frame/...` 같은 상대경로를 그대로 넘기면 Google이 resolve 못 해서
동작하지 않는다. `frame_urls`(화면 `<img src>`용, 상대경로로 충분)와 `lens_links`용 절대
URL을 분리해야 한다. `shopping_shorts/config.py`에 먼저 추가:
```python
# 배포 도메인 — Google Lens 등 외부 서비스가 우리 프레임 이미지를 직접 fetch할 때 필요.
# 로컬 개발 시엔 기본값(localhost)이라 Lens 링크가 실제로는 안 열리는 게 정상(배포 후 확인).
PUBLIC_BASE_URL = os.environ.get("PUBLIC_BASE_URL", "http://127.0.0.1:8848")
```
서버 `/etc/shopping-shorts.env`에는 `PUBLIC_BASE_URL=https://shoppingshorts.duckdns.org` 추가(Task 7에서).

파일 어딘가(`/api/save` 엔드포인트 다음 등)에 추가:
```python
_FIND_TMP_DIR = Path(__file__).parent / "data" / "find_frames"


@app.post("/api/find/analyze")
def api_find_analyze(shortcode: str):
    """영상 다운로드 → 프레임 추출 → Gemini 분석 → 검색링크 생성, 결과 저장 후 반환."""
    store = Store(DB_PATH)
    items, _ = store.load_last_run()
    item = next((i for i in items if i["shortcode"] == shortcode), None)
    if not item:
        return JSONResponse(status_code=404, content={"ok": False, "error": "해당 항목 없음"})

    work_dir = _FIND_TMP_DIR / hashlib.sha1(shortcode.encode()).hexdigest()[:16]
    video_path = download_video(item["video_url"], work_dir)
    frame_paths = extract_frames(video_path, work_dir, max_frames=6)
    analysis = analyze_video(video_path, item.get("caption", ""))

    frame_urls = [f"/api/find/frame/{work_dir.name}/{p.name}" for p in frame_paths]
    frame_abs_urls = [f"{PUBLIC_BASE_URL}{u}" for u in frame_urls]
    store.save_source_analysis(
        shortcode, keywords=analysis["keywords"],
        frame_paths=[str(p) for p in frame_paths],
        analyzed_at=datetime.now(timezone.utc).isoformat(),
    )
    return {
        "ok": True,
        "keywords": analysis["keywords"],
        "category": analysis["category"],
        "frame_urls": frame_urls,
        "search_links": build_search_links(analysis["keywords"]),
        "lens_links": [lens_search_url(u) for u in frame_abs_urls],
    }


@app.get("/api/find/frame/{work_id}/{filename}")
def api_find_frame(work_id: str, filename: str):
    path = _FIND_TMP_DIR / work_id / filename
    if not path.exists():
        return JSONResponse(status_code=404, content={"ok": False})
    return FileResponse(path)
```

`from datetime import datetime, timezone`와 `import hashlib`, `from fastapi.responses import FileResponse`가
이미 import돼 있지 않으면 상단에 추가(기존 `import hashlib`가 이미 있으므로 `FileResponse`와
`datetime`만 확인).

- [ ] **Step 4: 테스트 통과 확인**

Run: `python -m pytest shopping_shorts/tests/test_app.py -k find_analyze -v`
Expected: 전체 PASS

- [ ] **Step 5: find.html 1차 화면 작성**

`shopping_shorts/static/find.html` — `outreach.html`의 로그인/레이아웃 구조를 그대로 복사해
사이드바에 "제품 찾기" 메뉴로 바꾸고, 본문은:
```html
<div class="toolbar">
  <input id="scInput" placeholder="분석할 항목의 shortcode 또는 레퍼런스에서 담기로 넘어옴">
  <button onclick="doAnalyze()">분석</button>
</div>
<div id="result"></div>
<script>
const params = new URLSearchParams(location.search);
const AUTO_SC = params.get("shortcode");

async function doAnalyze(){
  const sc = document.getElementById("scInput").value || AUTO_SC;
  if(!sc) return;
  document.getElementById("result").textContent = "분석 중… (영상 다운로드+Gemini 분석, 수십 초 소요)";
  const r = await fetch(`/api/find/analyze?shortcode=${encodeURIComponent(sc)}`, {method:"POST"});
  const d = await r.json();
  if(!d.ok){ document.getElementById("result").textContent = "❌ " + (d.error||"실패"); return; }
  renderResult(d);
}

function renderResult(d){
  const frames = d.frame_urls.map(u=>`<img src="${u}" style="width:100px;margin:4px">`).join("");
  const kw = d.keywords.ko.map(k=>`<span class="chip">${k}</span>`).join("");
  const linkRows = Object.entries(d.search_links).map(([platform, urls])=>
    `<div><b>${platform}</b>: ${urls.map(u=>`<a href="${u}" target="_blank">🔗</a>`).join(" ")}</div>`
  ).join("");
  document.getElementById("result").innerHTML = `
    <div>${frames}</div>
    <div>${kw}</div>
    <div>${linkRows}</div>
    <div>Lens: ${d.lens_links.map(u=>`<a href="${u}" target="_blank">🔍</a>`).join(" ")}</div>
  `;
}
if(AUTO_SC){ document.getElementById("scInput").value = AUTO_SC; doAnalyze(); }
</script>
```
(스타일은 `outreach.html`의 `<style>` 블록을 그대로 재사용 — 새로 만들지 말 것.)

- [ ] **Step 6: 로컬에서 브라우저로 확인**

로컬 서버 재시작 후 `http://127.0.0.1:8848/find.html?shortcode=<실제shortcode>`로 접속,
분석 결과(프레임 썸네일+키워드+링크)가 뜨는지 눈으로 확인.

- [ ] **Step 7: Commit**

```bash
git add shopping_shorts/app.py shopping_shorts/static/find.html shopping_shorts/tests/test_app.py
git commit -m "feat(shopping_shorts): /api/find/analyze + 제품찾기 1차 화면(프레임·키워드·검색링크)"
```

---

### Task 6: YouTube 실수집 + Gemini 유사도 검증

**Files:**
- Create: `shopping_shorts/youtube_search.py`
- Create: `shopping_shorts/similarity.py`
- Modify: `shopping_shorts/app.py` (`/api/find/collect`, `/api/find/candidates`)
- Modify: `shopping_shorts/config.py` (`YOUTUBE_API_KEY`)
- Test: `shopping_shorts/tests/test_youtube_search.py`, `shopping_shorts/tests/test_similarity.py`, `shopping_shorts/tests/test_app.py`

**Interfaces:**
- Produces: `youtube_search.search(keyword, max_results=10) -> list[dict]`(각 `{url,title,thumbnail}`)
- Produces: `similarity.score_candidate(frame_paths, candidate_thumbnail_url) -> float`(0~1)
- Consumes: `config.YOUTUBE_API_KEY`(신규, 없으면 `/api/find/collect?platform=youtube` 호출 시 에러 반환)

- [ ] **Step 1: config.py에 YOUTUBE_API_KEY 추가**

`shopping_shorts/config.py`의 `SHORTS_GEMINI_KEYS` 블록 다음에 추가:
```python
# YouTube Data API v3 (제품찾기 실수집용, 2026-07-09) — 무료 할당량, Google Cloud Console 발급
YOUTUBE_API_KEY = os.environ.get("YOUTUBE_API_KEY", "")
```

- [ ] **Step 2: youtube_search 실패하는 테스트 작성**

`shopping_shorts/tests/test_youtube_search.py`:
```python
import pytest
from shopping_shorts import youtube_search


def test_search_returns_normalized_candidates(monkeypatch):
    monkeypatch.setattr(youtube_search, "YOUTUBE_API_KEY", "fake-key")

    def fake_get(url, params, timeout):
        assert params["q"] == "floor cleaner"
        assert params["key"] == "fake-key"
        class FakeResp:
            def json(self):
                return {"items": [
                    {"id": {"videoId": "abc123"},
                     "snippet": {"title": "Floor Cleaner Review",
                                 "thumbnails": {"medium": {"url": "https://i.ytimg.com/vi/abc123/mq.jpg"}}}},
                ]}
            def raise_for_status(self): pass
        return FakeResp()
    monkeypatch.setattr(youtube_search.requests, "get", fake_get)

    results = youtube_search.search("floor cleaner", max_results=10)

    assert results == [{
        "url": "https://www.youtube.com/watch?v=abc123",
        "title": "Floor Cleaner Review",
        "thumbnail": "https://i.ytimg.com/vi/abc123/mq.jpg",
    }]


def test_search_no_key_raises(monkeypatch):
    monkeypatch.setattr(youtube_search, "YOUTUBE_API_KEY", "")
    with pytest.raises(RuntimeError, match="YOUTUBE_API_KEY"):
        youtube_search.search("x")
```

- [ ] **Step 3: 테스트 실패 확인**

Run: `python -m pytest shopping_shorts/tests/test_youtube_search.py -v`
Expected: FAIL — `ModuleNotFoundError`

- [ ] **Step 4: youtube_search.py 구현**

`shopping_shorts/youtube_search.py`:
```python
"""YouTube Data API v3로 키워드 검색 → 정규화된 후보 리스트."""
import requests
from shopping_shorts.config import YOUTUBE_API_KEY

_SEARCH_URL = "https://www.googleapis.com/youtube/v3/search"


def search(keyword, max_results=10):
    """키워드 → [{url, title, thumbnail}, ...]."""
    if not YOUTUBE_API_KEY:
        raise RuntimeError("youtube_search: YOUTUBE_API_KEY가 설정되지 않았습니다")
    resp = requests.get(_SEARCH_URL, params={
        "part": "snippet", "q": keyword, "type": "video",
        "maxResults": max_results, "key": YOUTUBE_API_KEY,
    }, timeout=15)
    resp.raise_for_status()
    data = resp.json()
    out = []
    for item in data.get("items", []):
        vid = item.get("id", {}).get("videoId")
        if not vid:
            continue
        snippet = item.get("snippet", {})
        thumb = snippet.get("thumbnails", {}).get("medium", {}).get("url", "")
        out.append({
            "url": f"https://www.youtube.com/watch?v={vid}",
            "title": snippet.get("title", ""),
            "thumbnail": thumb,
        })
    return out
```

- [ ] **Step 5: 테스트 통과 확인**

Run: `python -m pytest shopping_shorts/tests/test_youtube_search.py -v`
Expected: 전체 PASS

- [ ] **Step 6: similarity 실패하는 테스트 작성**

`shopping_shorts/tests/test_similarity.py`:
```python
import pytest
from shopping_shorts import similarity


def test_score_candidate_parses_gemini_score(monkeypatch, tmp_path):
    monkeypatch.setattr(similarity, "SHORTS_GEMINI_KEYS", ["fake-key"])
    frame = tmp_path / "f1.jpg"
    frame.write_bytes(b"jpg")

    class FakeClient:
        class models:
            @staticmethod
            def generate_content(**kw):
                class R: text = '{"score": 0.82, "reason": "같은 제품 병 모양"}'
                return R()

    monkeypatch.setattr(similarity, "_client_for_key", lambda key: FakeClient())

    score = similarity.score_candidate([frame], "https://example.com/candidate_thumb.jpg")
    assert score == 0.82


def test_score_candidate_on_error_returns_none(monkeypatch, tmp_path):
    monkeypatch.setattr(similarity, "SHORTS_GEMINI_KEYS", ["fake-key"])
    frame = tmp_path / "f1.jpg"
    frame.write_bytes(b"jpg")

    class FakeClient:
        class models:
            @staticmethod
            def generate_content(**kw):
                raise RuntimeError("quota")

    monkeypatch.setattr(similarity, "_client_for_key", lambda key: FakeClient())

    assert similarity.score_candidate([frame], "https://example.com/x.jpg") is None
```

- [ ] **Step 7: 테스트 실패 확인**

Run: `python -m pytest shopping_shorts/tests/test_similarity.py -v`
Expected: FAIL — `ModuleNotFoundError`

- [ ] **Step 8: similarity.py 구현**

`shopping_shorts/similarity.py`:
```python
"""수집된 후보를 원본 프레임과 Gemini로 비교해 유사도 점수 매김 — tubefactory에 없는
검증 단계(설계문서 §3-5). 전용 키 풀 재사용."""
import json
from google import genai
from google.genai import types
from shopping_shorts.config import SHORTS_GEMINI_KEYS

_MODEL = "gemini-3-flash"

_PROMPT = """첫 번째 이미지들은 원본 영상의 대표 장면이고, 마지막 이미지는 다른 곳에서 찾은
후보 영상의 썸네일이다. 후보가 원본과 같은 제품/장면을 다루고 있는지 판단해라.

JSON으로만 출력: {"score": 0.0~1.0 사이 유사도, "reason": "짧은 근거"}
같은 제품이 확실하면 0.8 이상, 관련은 있으나 다른 제품이면 0.3~0.6, 전혀 무관하면 0.2 이하."""

_client_cache = {}


def _client_for_key(key):
    if key not in _client_cache:
        _client_cache[key] = genai.Client(api_key=key)
    return _client_cache[key]


def score_candidate(original_frame_paths, candidate_thumbnail_url):
    """원본 프레임들 vs 후보 썸네일 URL → 0~1 유사도. 실패 시 None(미검증으로 남김)."""
    if not SHORTS_GEMINI_KEYS:
        return None
    try:
        parts = []
        for p in original_frame_paths:
            with open(p, "rb") as fh:
                parts.append(types.Part.from_bytes(data=fh.read(), mime_type="image/jpeg"))
        import requests
        cand_bytes = requests.get(candidate_thumbnail_url, timeout=15).content
        parts.append(types.Part.from_bytes(data=cand_bytes, mime_type="image/jpeg"))
        parts.append(_PROMPT)

        resp = _client_for_key(SHORTS_GEMINI_KEYS[0]).models.generate_content(
            model=_MODEL, contents=parts,
            config=types.GenerateContentConfig(response_mime_type="application/json"),
        )
        data = json.loads(resp.text)
        return float(data.get("score", 0.0))
    except Exception:
        return None
```

- [ ] **Step 9: 테스트 통과 확인**

Run: `python -m pytest shopping_shorts/tests/test_similarity.py -v`
Expected: 전체 PASS

- [ ] **Step 10: /api/find/collect + /api/find/candidates 엔드포인트 — 실패하는 테스트**

`shopping_shorts/tests/test_app.py`에 추가:
```python
def test_find_collect_youtube_saves_candidates_and_scores(monkeypatch, client):
    from shopping_shorts import app as app_module
    from shopping_shorts.store import Store
    from shopping_shorts.config import DB_PATH

    store = Store(DB_PATH)
    store.save_source_analysis("sc1", keywords={"ko": [], "en": ["floor cleaner"], "zh": []},
                                frame_paths=["/tmp/f1.jpg"], analyzed_at="2026-07-09T00:00:00Z")

    monkeypatch.setattr(app_module, "youtube_search_fn", lambda kw, max_results: [
        {"url": "https://youtube.com/watch?v=x", "title": "t", "thumbnail": "th.jpg"},
    ])
    monkeypatch.setattr(app_module, "score_candidate", lambda frames, thumb: 0.75)

    r = client.post("/api/find/collect", params={"shortcode": "sc1", "platform": "youtube"})
    assert r.status_code == 200
    assert r.json()["ok"] is True

    r2 = client.get("/api/find/candidates", params={"shortcode": "sc1"})
    d2 = r2.json()
    assert d2["items"][0]["similarity_score"] == 0.75


def test_find_collect_no_analysis_404(client):
    r = client.post("/api/find/collect", params={"shortcode": "never-analyzed", "platform": "youtube"})
    assert r.status_code == 404
```

- [ ] **Step 11: 테스트 실패 확인**

Run: `python -m pytest shopping_shorts/tests/test_app.py -k find_collect -v`
Expected: FAIL

- [ ] **Step 12: app.py에 엔드포인트 추가**

import 추가:
```python
from shopping_shorts.youtube_search import search as youtube_search_fn
from shopping_shorts.similarity import score_candidate
```

엔드포인트 추가:
```python
@app.post("/api/find/collect")
def api_find_collect(shortcode: str, platform: str):
    store = Store(DB_PATH)
    analysis = store.get_source_analysis(shortcode)
    if not analysis:
        return JSONResponse(status_code=404, content={"ok": False, "error": "먼저 분석이 필요합니다"})
    if platform != "youtube":
        return JSONResponse(status_code=400, content={"ok": False, "error": f"'{platform}' 실수집은 아직 미지원"})

    keyword = (analysis["keywords"]["en"] or analysis["keywords"]["ko"] or [""])[0]
    if not keyword:
        return {"ok": True, "count": 0}
    raw = youtube_search_fn(keyword, max_results=10)
    ids = store.save_candidates(shortcode, "youtube", raw)

    frame_paths = analysis["frame_paths"]
    for cand_id, cand in zip(ids, raw):
        score = score_candidate(frame_paths, cand["thumbnail"])
        if score is not None:
            store.update_candidate_score(cand_id, score)
    return {"ok": True, "count": len(ids)}


@app.get("/api/find/candidates")
def api_find_candidates(shortcode: str):
    store = Store(DB_PATH)
    return {"ok": True, "items": store.get_candidates(shortcode)}


@app.post("/api/find/save")
def api_find_save(candidate_id: int, shortcode: str):
    store = Store(DB_PATH)
    store.save_to_pool(shortcode, candidate_id)
    return {"ok": True}
```

- [ ] **Step 13: 테스트 통과 확인**

Run: `python -m pytest shopping_shorts/tests/test_app.py -v`
Expected: 전체 PASS

- [ ] **Step 14: find.html에 수집 버튼 + 후보 표시 추가**

`renderResult()` 다음에 플랫폼별 [수집] 버튼(youtube만 활성, 나머지는 "준비중" 표시)과
후보 그리드 렌더링 함수 추가 — 기존 `outreach.html`의 카드 그리드 CSS(`.cards`, `.card`)
재사용. `[담기]` 버튼은 `/api/find/save`에 `candidate_id`+`shortcode` POST.

- [ ] **Step 15: 로컬 확인 + Commit**

로컬에서 실제 YOUTUBE_API_KEY 발급 후(무료) `.env`에 추가, 브라우저로 분석→수집→후보
표시→담기까지 눈으로 확인.

```bash
git add shopping_shorts/youtube_search.py shopping_shorts/similarity.py shopping_shorts/config.py shopping_shorts/app.py shopping_shorts/static/find.html shopping_shorts/tests/test_youtube_search.py shopping_shorts/tests/test_similarity.py shopping_shorts/tests/test_app.py
git commit -m "feat(shopping_shorts): 유튭 실수집 + Gemini 유사도검증 + 소스풀 담기"
```

---

### Task 7: 배포 (ffmpeg 설치 + YOUTUBE_API_KEY/PUBLIC_BASE_URL 서버 반영)

**Files:**
- Modify: `deploy/shopping-shorts.env.example`

**Interfaces:** 없음(배포 절차).

- [ ] **Step 1: 서버에 ffmpeg 설치**

```bash
ssh -i <키경로> ubuntu@3.39.179.148 "sudo apt update && sudo apt install -y ffmpeg"
```

- [ ] **Step 2: env.example에 YOUTUBE_API_KEY/PUBLIC_BASE_URL 문서화**

`deploy/shopping-shorts.env.example`에 추가:
```
# 제품찾기(기능③) 유튭 실수집용 — Google Cloud Console에서 무료 발급 (2026-07-09)
YOUTUBE_API_KEY=
# Google Lens가 우리 프레임 이미지를 fetch할 때 쓰는 절대 URL 베이스(2026-07-09)
PUBLIC_BASE_URL=https://shoppingshorts.duckdns.org
```

- [ ] **Step 3: 서버 /etc/shopping-shorts.env에 실제 값 추가(SSH, git 커밋 아님)**

```bash
ssh -i <키경로> ubuntu@3.39.179.148 "sudo tee -a /etc/shopping-shorts.env <<'EOF'
YOUTUBE_API_KEY=<실제키>
PUBLIC_BASE_URL=https://shoppingshorts.duckdns.org
EOF"
```

- [ ] **Step 4: 커밋 + 배포 + 헬스체크**

```bash
git add deploy/shopping-shorts.env.example
git commit -m "chore(shopping_shorts): 제품찾기 배포 — ffmpeg/YOUTUBE_API_KEY 문서화"
git push origin main
```
그 다음 서버에서 `git pull --ff-only origin main && sudo systemctl restart shopping-shorts && curl -s localhost:8849/healthz`.

---

## 후속 계획 (이번 계획 범위 밖)

- SerpApi(Google Lens 실수집) 연동 — 유료 키 확보 후.
- Apify 인스타/틱톡 검색전용 액터 조사·연동 — 액터 선정 후.
- 더우인/샤오홍슈/Baidu 최선노력 수집 — 위 완료 후 별도 계획으로.
- "담기"(레퍼런스 랭킹 → 제품찾기) 버튼을 현재 tubefactory 핸드오프에서 `/find.html`로
  전환하는 결정 — 이번 계획의 기능이 tubefactory 대비 충분히 쓸만해진 뒤 사용자 판단.
