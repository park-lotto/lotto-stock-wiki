# 트랙: 바꾸기길이 (대본 바꾸기 · 502 문구 · 화면 UI · 캡컷 이관)

> ## 🔴 회사 PC에서 이어서 할 것 — 2026-08-23 밤 집PC에서 인계
>
> ```
> cd "C:\Users\CH\Desktop\로또의 주식"
> git fetch origin
> py tools/track.py start 바꾸기길이                      # 트랙 폴더가 없으면
> git -C .tracks/바꾸기길이 merge origin/track/바꾸기길이  # 폴더가 이미 있으면 이걸로 당긴다
> ```
>
> **마지막 커밋 `3d58d955f`** — origin/track/바꾸기길이에 **백업 완료(유실 없음)**
>
> ### ✅ 08-23 03:18 최종 — **오늘 작업 전부 라이브 반영 완료**
>
> 병합·서버 반영을 실측으로 확인했다. **남은 병합 작업 없다.**
>
> | 커밋 | 내용 | main | 서버 |
> |---|---|---|---|
> | `ef8db2f5d` | 조각계획 공용함수 | ✅ | ✅ |
> | `c9a8ff706` | 캡컷 완전 이관 | ✅ | ✅ |
> | `821a1bdf6` | 최종렌더 크기 정렬 | ✅ | ✅ |
>
> 서버 재시작 03:18(`e7dee22f7`), 코드 실물 확인(`_media_entry`·`plan_beat_clips_for`·`fin-w:260px`).
>
> → **아래 1번(병합 확인)은 건너뛰고 바로 2번 육안 확인부터 하면 된다.**
>
> ### 지금 어디까지 갔나
>
> | 작업 | 상태 |
> |---|---|
> | 바꾸기 길이 폭주 차단 | ✅ 라이브 |
> | "키 소진" 문구 원인별 분리 | ✅ 라이브 |
> | AI 자동생성 맨 위 · 안 제목 크게 · 좌우 기본 | ✅ 라이브 |
> | 제목후킹 중복 해소 · 나열추천형 숨김 (DB) | ✅ 라이브 |
> | **캡컷 완전 이관** | ✅ **라이브**(02:59 병합) — 육안 확인만 남음 |
| 9단계 최종렌더 크기 정렬(영상=썸네일) | ✅ **라이브**(03:13 병합·03:18 서버반영) |
>
> ### ★ 1. 맨 먼저 — 캡컷 커밋이 라이브에 갔는지 확인
>
> ```
> git fetch origin && git branch -r --contains c9a8ff706 | grep origin/main
> ```
>
> - **나오면** → 병합 완료. 아래 2번(육안 확인)으로 간다.
> - **안 나오면** → `py tools/track.py finish 바꾸기길이` 다시 걸어라.
>
> **집PC 마지막 상태(08-23 새벽): 미병합.** 커밋은 origin에 다 올라가 있고(`8034968df`)
> 라이브에만 안 갔다. 그때 다른 세션들이 계속 병합 중이라(01:27·01:39 `대본칸설명`·
> `안나오는조각`) 게이트 락이 물려 있었고, `finish`가 **출력 없이 exit 0**으로 바로
> 끝나기만 했다(줄을 안 서고 나가떨어짐). **회사에서 한산할 때 다시 걸면 된다.**
>
> ⚠️ **finish는 조용히 끝나도 병합이 안 된 경우가 있다.** 판정은 위 `--contains`로만 하고
> **exit code를 믿지 마라**(2026-08-22·23에 실제로 여러 번 겪었다).
> 로그에 `중단:`이 있으면 **트랙 폴더가 dirty**인 것 — 내용이 같아도 **개행(CRLF/LF)만 달라도**
> 게이트가 시작조차 안 한다. `git diff -w`가 비면 정규화만 커밋해서 풀어라.
>
> ### ★ 2. 그다음 — 캡컷 육안 확인 (이번 작업의 관문. 코드로 대신 못 한다)
>
> 제작소 7단계 → `🎬 캡컷으로 보내기` → 캡컷에서 프로젝트를 열고:
>
> 1. **보관함(라이브러리)에 조각이 다 있나** — `cut_00_0_훅.mp4` 꼴 + `final.mp4` + 소스 + TTS
> 2. **타임라인 화면 조각 수가 완성본과 같은가** — 예전엔 3분의 1만 갔다(19개 중 7개)
> 3. **조각을 끌어다 갈아끼워도 자막·TTS가 남나** ← **핵심 전제. 안 되면 설계부터 다시**
>
> 안 되면 볼 곳:
> - `shopping_shorts/capcut_draft.py` — 보관함(`build_meta`의 media) · 타임라인(`build_draft`)
> - `shopping_shorts/video_assemble.py` `plan_beat_clips_for` — 조각 계획(렌더와 공용)
>
> ### ★ 3. 남은 것 (급하지 않음)
>
> - [ ] 조각 파일명에 **길이 표기**(`_4.6초`)를 넣을지 — 설계엔 있었으나 보관함에 길이가
>       뜨므로 뺐다. 캡컷에서 길이가 안 보이면 그때 넣는다.
> - [ ] 조각 19개짜리 job에서 보관함이 너무 길어지지 않는지
> - [ ] **바꾸기 길이 상한 1.35배가 빡빡한지** 사장님 체감 — 빡빡하면
>       `script_generate.py` `_BEAT_LEN_MAX` 한 줄만 1.45로 올리면 된다
> - [ ] (무관) `test_assembled_drafts.py` 기존 실패 2건 — 기준선을 갉아먹는다
>
> ### 알아둘 것 (매번 헤맨 것들)
>
> - **라이브 DB는 `shopping_shorts/data/reference.db`** — 같은 폴더 `app.db`는 08-16에서
>   멈춘 껍데기(produce_works 0건)라 그걸 보면 "데이터가 없다"고 오진한다
> - 회사 PC SSH키: `C:/Users/CH/crawling_bot_client/LightsailDefaultKey-ap-northeast-2.pem`
>   (CLAUDE.md의 TheRose 경로는 집PC용)
> - 설계문서: `docs/superpowers/specs/2026-08-23-캡컷-완전이관-design.md`
> - **main 병합 ≠ 라이브 반영** — 서버는 1분 크론으로 pull하고, 접속자가 있으면 재시작을
>   미룬다. 서버에서 `git merge-base --is-ancestor <커밋> HEAD`로 확인할 것

## (이하 작업 이력 — 최신이 아래쪽)

### [바꾸기] 길이 폭주 + 502 거짓 문구

## 2026-08-22 (회사PC) — 라이브 반영

사장님 제보 2건이 **서로 다른 문제**였다.

### ① [바꾸기]가 문장을 2배로 늘림 → 수정

실사고: '방법' 칸 50자(4.6초) → 74자(9.0초). 아래 칸과 내용이 겹쳐 퀄리티가 무너졌다.

**길이 게이트는 원래 있었다.** 문제는 상한 `max(per*1.8, per+40)`이
**거의 언제나 `+40`이 이겨서** 사실상 없는 것과 같았던 것:

| 원문 | 옛 상한 | 실질 배수 | 새 상한 | 새 배수 |
|---|---|---|---|---|
| 10자 | 50자 | **5.0배** | 22자 | 2.2배 |
| 25자 | 65자 | **2.6배** | 34자 | 1.5배 |
| 37자 | 77자 | **2.1배** | 50자 | 1.4배 |
| 50자 | 90자 | 1.8배 | 68자 | 1.4배 |

→ **짧은 칸을 봐주려 넣은 `+40`이 정반대로 짧은 칸을 가장 헐렁하게** 만들었다.
   `1.8`은 50자 이상에서만 발동해 실제로는 거의 쓰이지도 않았다.

수정 (`script_generate.py`)
- `_BEAT_LEN_MAX` 1.8 → **1.35**, `+40자` → **`_BEAT_LEN_SLACK`=12자**
- 같은 식이 재시도 루프·최종 방어 **두 군데**에 따로 있던 것을
  **`_beat_len_cap()` 함수 하나로** 뽑음 (0순위-B)
- 프롬프트가 **상한을 숫자로** 말한다 — 종전엔 "N자 안팎"만 줘서 모델이
  어디까지가 통과인지 몰랐고 재시도 3회를 다 태워 502가 났다

### ② "생성 실패(키 소진 또는 응답 오류)"가 거짓말 → 수정

실측: 서버 로그에 429·quota **한 건도 없는데** 이 문구가 떴다.
키는 처음부터 멀쩡했으므로 "잠시 후 재시도"를 아무리 눌러도 영원히 안 풀린다.

원인 — `_call_json`이 ①키 없음 ②전부 소진 ③응답 오류를 **전부 `{}`로** 돌려줘
호출부가 원인을 구분할 방법 자체가 없었다. `generate_by_styles`도 예외를 `print`로만 흘렸다.

수정 — 사유를 실어 올린다(`note`/`reasons`, **안 주면 종전과 동일 = 회귀 0**).
`_gen_fail_message()`(app.py)가 6가지로 갈라 말한다:
키 0개 / 전부 소진 / 분당 한도 / **응답 오류(키 문제 아님)** / 게이트 반려 / 시도 자체 없음

## 검증 (실행 결과)

- 사장님 실사례 74자 → 새 상한 68자 초과로 **거부**, 짧게 고치면 통과 (스텁 실측)
- 프롬프트에 상한 숫자(67자) 실제 포함 확인
- 502 문구 6종 전부 원인별로 다르게 출력 확인
- 전체 pytest **기준선 2 failed/5037 passed = 수정 후 동일** (회귀 0)
  · 남은 2건은 `test_assembled_drafts.py`의 **기존 실패**로 이 작업과 무관
- 502 위험: 실측 초과율 12.6% → 3회 모두 실패 확률 **0.2%** (BEAT_REGEN_TRIES 유지)

## ⏭ 다음

- [ ] **사장님 육안 확인** — [바꾸기]를 눌렀을 때 길이가 제자리를 지키는지.
      1.35가 빡빡하면 1.45~1.5로 완화 가능(값 한 곳 `_BEAT_LEN_MAX`만 고치면 된다)
- [ ] 502가 실제로 뜰 때 새 문구가 원인을 맞게 말하는지 라이브에서 1회 확인
- [ ] (별건) `test_assembled_drafts.py` 기존 실패 2건 — 이 트랙과 무관하나 기준선을 갉아먹는다

## 참고 — 이번에 확인한 사실

- **라이브 DB는 `shopping_shorts/data/reference.db`** 다.
  `app.db`도 같은 폴더에 있지만 08-16에서 멈춘 껍데기(produce_works 0건).
  `mix_jobs`·`produce_works` 전부 reference.db에 있다. 서버 조사 시 여기를 봐라.
- 라이브 비트 길이 실측: **중앙값 25자 · 평균 25.5자 · p90 37자** (166비트)


---

## 2026-08-22 (2차) — 화면·데이터 4건

사장님 지시로 이어서 처리. **①②는 코드(배포 필요) / ③④는 DB(즉시 반영)**.

### ① [바꾸기] 패널: AI 자동 생성을 맨 위로
가장 많이 쓰는 동작이 **목록 맨 끝 점선 칸**에 있어서 틀 6개를 다 훑고 내려가야 했다
("선택이 너무 많다"). 맨 위로 올리고 56px·강조배경·왼쪽 띠로 격상.
문구도 `🎲 틀 없이` → **`✨ AI 자동 생성`**(부정형은 뭘 하는지 안 보인다).
아래 틀 목록과 `또는 문장틀 고르기` 가르개로 분리(**왼쪽 정렬** — 가운데는 혼자 어긋나 보였다).

### ② 안 제목 크게 + 보기 기본값 '좌우'
- `.s2-dname` 12px 흐린회색 → **16px·800·본문색**. 머리 높이 24→28px(글자 커지면 잘린다).
  스타일 이름은 `.sty` 13px 부제로 한 급 낮춤 — 'A안'이 주인공.
  `text-transform:uppercase` 제거(한글엔 무효인데 letter-spacing만 벌렸다).
- 기본 보기 `mode-tab` → **`mode-side`**.
  ⚠️ **기본값이 두 군데 짝**이다(0순위-B): 컨테이너 `class="mode-side"` + 버튼 `class="on"`.
  한쪽만 고치면 "화면은 좌우인데 버튼은 탭" 으로 어긋난다.

### ③ 은폐형 · 발명품형 제목 후킹이 사실상 동일 → 발명품형을 교체 (DB)
실측: `{제품군}` vs `{제품}` **한 글자만 달랐다**.
- 은폐형(id 55) `{나라} 천재가 만들어 떼돈 번 {제품군}의 정체` ← **유지**(정체 감추기는 은폐형 것)
- 발명품형(id 60) → **`이런 걸 만들어? {나라} 발명품 수준 차이 나네`** 로 변경

### ④ 나열 추천형(id 63) 숨김 (DB)
`status='approved'` → **`'pending'`**. `list_style_spines(status="approved")`가 거르므로
**코드 무수정**이고 되돌리기도 한 줄이다(이미 pending 7개가 같은 방식으로 숨어 있다).

> DB 백업: 서버 `/tmp/reference.db.bak_0420` (수정 전)
> 라이브 DB는 **reference.db**다 — app.db는 08-16에서 멈춘 껍데기.

## ⚠️ 이번에 밟은 함정 (다음 세션이 또 밟는다)

**`finish`가 45분간 "아무 말 없이" 병합을 안 했다.** 원인은 게이트 결함도, 테스트 실패도,
락 대기도 아니었다:

```
중단: 트랙 폴더가 dirty — 커밋 먼저 해라.
 M finish_out.txt
```

`finish_out.txt`(다른 세션 산출물)를 "내 것이 아니니 커밋에서 빼자"고 **일부러 남겨둔 것**이
막고 있었다. 게이트는 **파일 하나라도 dirty면 아예 시작조차 안 한다**.
`git diff -w`가 비어 있었다 = **내용은 한 글자도 안 다르고 개행(CRLF/LF)만 달랐다**
— 내 finish 실행이 그 파일을 다시 쓰면서 생긴 차이였다. 정규화만 커밋해서 풀었다.

교훈:
- **`finish`가 조용하면 로그부터 끝까지 읽어라.** "죽었다"고 단정하기 전에 `중단:` 줄을 찾아라
  (나는 "조용히 죽었다"고 두 번 오진했다 — 실제로는 한 번은 정상 대기, 한 번은 dirty 차단).
- `git checkout --`/`update-index --refresh`로는 CRLF 차이가 안 없어진다. **정규화를 커밋**해야 한다.
- 트랙 폴더는 **내 것이 아닌 파일도 깨끗해야** 한다 — 빼두면 게이트가 통째로 멈춘다.

---

## 2026-08-23 (3차) — 캡컷 완전 이관

설계: `docs/superpowers/specs/2026-08-23-캡컷-완전이관-design.md`

### ★핵심 발견: 캡컷·ZIP이 화면의 3분의 1만 내보내고 있었다

라이브 실측(`reference.db`): 조각 **19개**인 job이 캡컷엔 **7개**(18→8, 13→6).
비트 하나에 화면이 여러 개인데(`primary`+`alternates`, 실험실 편성이면 `scene_override`)
**`primary` 하나만** 봤다 → **캡컷에서 연 것과 완성본이 다른 영상**이었다.

같은 병이 **세 군데**(0순위-B):
| 자리 | 증상 |
|---|---|
| `capcut_draft` 타임라인 | primary만 올림 |
| `export_bundle` ZIP 조각 | primary만 자름 |
| `capcut_draft` **소스 복사** | ★E2E 돌리다 발견. alternates 소스가 폴더에 안 들어가 `asset_paths`에 없으니 그 조각을 **조용히 건너뜀**(화면 3개 중 2개만 올라감) |

→ 렌더의 계획 블록을 **`video_assemble.plan_beat_clips_for()`** 하나로 뽑고 셋이 그걸 쓴다.
⚠️ **`_plan_beat_clips`만 부르면 안 된다** — 앞뒤로 손상소스 제외·포인트비트 홀드·
   1장1컷·늘려채우기·여운 판단이 더 있다. 내보내기에 다시 적으면 **또 두 벌**이 된다.

### 미디어 보관함(사장님 "라이브러리")

`draft_meta_info.json`의 **`draft_materials`**가 그 자리인데 **빈 배열**로 내보내고 있었다.
형식은 **실제 캡컷 프로젝트를 열어 실측**(추측 아님):
`%LOCALAPPDATA%/CapCut/User Data/Projects/com.lveditor.draft/0618/draft_meta_info.json`

```
draft_materials = [{"type":0,"value":[항목...]}, {"type":1,"value":[]}, ... type 6까지]
항목 = {id, type:0, metetype:"video"|"music", file_Path, extra_info(보일 이름),
        duration(μs), width, height, roughcut_time_range, sub_time_range, item_source:1, ...}
```

담는 것: 조각(`cut_NN_i_역할.mp4`, 원본에서 자른 **깨끗한 화면**) + `final.mp4` + 소스 + TTS.
- **완성본은 보관함에만** — 타임라인에 올리면 TTS와 겹쳐 소리가 두 번 난다.
- mp3는 `metetype="music"`(video로 넣으면 캡컷이 잘못 읽는다).
- **TTS 길이는 따로 잰다** — `video_durs`엔 영상만 있어 0초로 들어가던 것 수정.

### 검증 (실행 결과)

- **E2E(실제 ffmpeg 소스)**: 화면 3개 비트 → 타임라인 **3조각**(0~2~4~6초, 빈틈·겹침 0),
  보관함 **7개**(조각3+완성본+소스2+TTS), TTS 6.00초
- 전체 pytest **2 failed / 5135 passed** = 기준선(2 failed / 5037) **동일 → 회귀 0**
  (남은 2건은 `test_assembled_drafts.py` 기존 실패, 무관)
- 신규 테스트 15건

## ⏭ 다음 (사장님 육안 — 코드로 대신 못 한다)

- [ ] 캡컷에서 열어 **①보관함에 조각이 다 있나 ②끌어다 갈아끼워지나 ③그때 자막·TTS가 남나**
- [ ] 조각 파일명이 `cut_00_0_훅.mp4` 꼴인데 **길이 표기는 안 넣었다**(설계엔 있었으나 보관함에
      길이가 뜨므로 뺐다). 캡컷에서 길이가 안 보이면 파일명에 넣는 것 재검토.
- [ ] 조각 개수가 많은 job(19개)에서 보관함이 너무 길어지지 않는지

---

## 2026-08-23 (4차) — 9단계 최종렌더 크기 정렬

사장님: "최종렌더 크기랑 썸네일 크기 동일하게 해서 옆으로"

**원인**: 크기를 정하는 곳이 **여섯 군데**에 따로 박혀 있었다(0순위-B).
마크업 flex-basis 300/200px · `<video>` max-width:300px 2곳 · 렌더중/대기 카드 300px 2곳 ·
`<img>` 200px. 9:16이라 폭이 1.5배 다르면 **높이도 1.5배** 벌어져 두 칸이 계단처럼 어긋났다.

**고침**: `--fin-w` 토큰 하나(260px)로 모으고 여섯 군데가 전부 그걸 본다.
인라인 하드코딩 전부 제거 → 앞으로 **한 줄만** 고치면 되고 한쪽만 어긋날 수 없다.
260px 이유: 300이면 우측 '최종 렌더' 칸이 좁고, 200이면 영상이 너무 작다. 3칸이 한 줄에 드는 값.

**검증(실제 렌더 실측)**: 1100px에서 **V 260x462 / T 260x459**(폭 동일, 높이 3px 차는
테스트 썸네일이 540x960이라 정확한 9:16이 아닌 탓) · 420px 모바일 세로 적층 정상 · node --check rc=0

✅ 병합·서버반영 완료(03:13 main / 03:18 재시작). 남은 건 사장님 육안 확인뿐.
