# 트랙: 내보내기캡컷 (최종렌더 다운로드 + 캡컷 내보내기)

## 2026-07-20 (4) — ✅ 최초설정 자동화 .bat 라이브 (Opus)

- **실 E2E 성공**: 원클릭 캡컷 보내기로 실제 32초 프로젝트가 캡컷에 생성됨(사장님 라이브 확인).
  제스처버그(showDirectoryPicker를 await/prompt 뒤에 호출) 수정 후 정상.
- **상대경로 실패 확정**: `##_draftpath_placeholder##`·파일명만 둘 다 Media Not Found → **절대경로 필수**.
  따라서 최초설정(폴더생성+캡컷 저장위치변경)을 못 없앰. 대신 **.bat으로 자동화**.
- **캡컷 저장위치 자동화 발견**: `%LOCALAPPDATA%\CapCut\User Data\Config\globalSetting`(평범 INI)의
  `currentCustomDraftPath=C:\\capcutproject\\CapCut Drafts`(더블백슬래시=Qt 이스케이프). 브라우저는 이걸
  못 바꾸지만 .bat은 가능.
  - `static/capcut_setup.bat`: 캡컷 종료확인→폴더생성→globalSetting 백업(.bak)후 그 한 줄만 교체
    (`[char]92`로 더블백슬래시 생성=이스케이프 레이어 우회, 다른줄·인코딩 UTF8-noBOM·줄수 온전).
  - produce.html `⚙ 캡컷 자동설정(최초 1회)` 다운로드 + sendToCapCut 경로 prompt 제거(경로 표준화).
- **새 설정 절차**: ①캡컷 설치·실행 ②⚙자동설정.bat 더블클릭(캡컷 끈 채) ③캡컷 켜고 툴서 폴더 한번 지정 → 영원히 원클릭.
- ⏭ 남은 것: 테스트draft 정리(`로또_캡컷테스트`·`상대경로A_플레이스홀더`·`상대경로B_그냥상대` — 사장님 삭제 가능) ·
  자막 위치/크기/한글폰트 튜닝(현재 en.ttf 기본, 캡컷이 한글 폴백) · 제작소에 그림 설정안내 패널(선택) ·
  .bat 실제 더블클릭 동작 사장님 확인.

## 2026-07-20 (3) — ✅ T2 원클릭 캡컷 보내기 라이브 (Opus, origin/main 병합 e1a97062b)

- **핵심 정정**: 레퍼런스는 데스크톱 앱 아니라 **웹앱 + File System Access API**(`showDirectoryPicker`,
  Chrome/Edge)로 캡컷 폴더에 직접 쓴다 → 웹앱도 가능(초기 "웹앱 불가·T3 bat" 전제 폐기). 설계문서 부록A.
- **완료**: 최종렌더 7단계 `🎬 캡컷으로 보내기`(Chrome/Edge). 클릭→(첫1회 폴더지정)→캡컷 Drafts폴더에
  `<project>/` 만들어 draft_content.json+에셋 직접쓰기. 폴더핸들 **IndexedDB 기억=2회차 원클릭**(60대 북극성).
  미지원 브라우저는 ZIP 폴백.
  - `shopping_shorts/capcut_draft.py`: 편집안→draft_content.json(영상/음성/자막 3트랙+동반material, μs단위).
    `assemble_draft_folder`(소스·TTS 복사+실길이 probe+draft/meta 쓰기). 동반ref 오디오5·텍스트1·비디오5.
  - `/api/mix/capcut/{job}?base=`(매니페스트: draft/meta 텍스트 인라인+에셋 URL) + `/api/mix/capcut_asset`.
    deny-by-default 자동보호. produce.html `sendToCapCut()`(FS Access API+IndexedDB 핸들).
  - **포맷 검증**: 실캡컷(8.9.1.3802)에 테스트 draft(`C:\capcutproject\CapCut Drafts\로또_캡컷테스트`) 찍어
    홈에 뜨고 영상+자막 렌더 육안확인. 테스트 20건(생성기11+엔드포인트) + JS 문법 OK. 게이트 통과(신규0).
- ⏭ **사장님 라이브 육안**(Chrome/Edge, 3분 뒤): 최종렌더→🎬캡컷으로 보내기→(첫1회 폴더 `C:\capcutproject\CapCut Drafts`
  지정)→캡컷 열면 프로젝트 뜨고 영상·음성·자막 트랙 다 있나. **실 job(여러 소스·긴 대본)로 열리는지**가 진짜 관문.
- ⏭ 후속(여력): 개인ID/폰트 무해값 실환경 재확인 · 자막 스타일(위치·크기·한글폰트) 튜닝 ·
  base 경로 자동감지(현재 기본값 `C:\capcutproject\CapCut Drafts` 프롬프트 확인) · 테스트 draft 정리.

## 2026-07-20 (2) — ✅ T1 구현·라이브 배포 (Opus, origin/main e81678b1b→병합 c0cb455e5)

- **완료**: 캡컷 편집용 내보내기 T1. 게이트 통과(신규0·기준선8)·push경합 자동재시도로 main 병합, 3분 뒤 라이브.
  - `shopping_shorts/export_bundle.py` 신규: `_beat_timeline` **단일 출처**로 SRT(비트 단위 큐)·스크립트,
    비트 primary 구간을 ffmpeg 재인코딩 컷 → `sources/beat_NN_역할.mp4`. 없는 재료는 건너뜀(500 금지).
  - `/api/mix/export/{job}`(`?part=sources|srt|script` 개별) + `/api/mix/video/{job}?dl=1`(첨부 다운로드).
    유료게이트 **deny-by-default가 자동 보호**(FREE 목록 밖) — 별도 등록 불필요(설계의 '등록 필수'는 반대였음).
  - produce.html 7단계 `📤 내보내기`: 캡컷용 ZIP·개별(소스/SRT/스크립트)·완성 MP4(렌더 후).
    `refreshFinal`→`renderExport({rendered:false})`, `pollFinal` done→`renderExport({rendered:true})`.
  - 테스트 13건: test_export_bundle(순수 로직 5 + 실 ffmpeg zip 4) + test_app_export(HTTP 배선 4). JS 문법 OK.
- ⏭ **사장님 라이브 육안**(3분 뒤): 제작소 7단계에 📤내보내기 버튼 뜨고 → 📦캡컷용 ZIP 받아 풀면
  sources/·tts/·captions.srt·script.txt·seo.txt·README·final.mp4 있나 → 캡컷에 올려 편집되나.
- ⏭ **후속 T2·T3**(설계문서 §3·§4): T2 캡컷 draft 자동생성(로컬 캡컷 draft_content.json 역공학 먼저,
  실제 열리는지 육안 확인 전 배포 금지) → T3 `캡컷에서열기.bat`(설치 없이 자동실행 근접).

## 2026-07-20 (1) — 설계 확정 (Fable)

- **설계문서 먼저 읽기**: `docs/superpowers/specs/2026-07-20-내보내기캡컷-design.md`
- 요지: 웹앱이라 "누르면 캡컷 자동실행"(레퍼런스=데스크톱 앱)은 원천 불가 →
  **3단 사다리**: T1 에셋 ZIP+다운로드(견고, 먼저 배포) → T2 캡컷 draft 자동생성(와우, 역공학 필요)
  → T3 ZIP 내 `캡컷에서열기.bat`(설치 없이 자동실행 근접).
- 재료는 전부 실재 확인됨(2026-07-20 코드 실측):
  - SRT ← `video_assemble._beat_timeline` (beat_idx·t0·dur·narration·cap_durs — 콘폼·여운 반영된 단일 출처)
  - 비트별 소스 컷 ← `_plan_beat_clips` 결과 재사용 (새 타임라인 계산 금지)
  - TTS ← `work/<job>/tts/beat_N.mp3` 이미 존재
  - SEO ← `store` `seo_json`
  - 최종영상 서빙 ← `/api/mix/video/{job_id}` (여기에 `?dl=1` attachment만 추가)

## ⏭ 착수 절차 (오푸스)

1. `py tools/track.py start 내보내기캡컷` → `.tracks/내보내기캡컷`에서 작업
2. T1: `GET /api/mix/export/{job_id}` (zip: final.mp4·sources/·tts/·captions.srt·script.txt·seo.txt·README)
   + `?part=sources|srt|script` 개별 + `/api/mix/video?dl=1` + 7단계 UI 버튼들 → finish(배포1)
3. T2: **로컬 캡컷으로 draft_content.json 역공학부터**(설계 §3 절차) — 실제 캡컷에서 열리는 것을
   육안 확인 전엔 ZIP에 넣지 말 것(기능 플래그)
4. T3: `캡컷에서열기.bat` (설계 §4) → finish(배포2)

## ⚠️ 주의

- **유료게이트 deny-by-default**: 새 엔드포인트는 허용목록 등록 없으면 잠긴다/또는 무료로 뚫린다 —
  유료게이트 트랙 방식 확인 후 최종렌더와 같은 tier로.
- work 폴더에 없는 재료는 **건너뛰고 README에 명시**(500 금지).
- 큰 zip 스트리밍·타임아웃 주의.
- produce.html은 여러 트랙이 만지는 hot 파일 — 트랙 폴더 격리 엄수, finish 전 `git fetch origin && git merge origin/main` 습관.
