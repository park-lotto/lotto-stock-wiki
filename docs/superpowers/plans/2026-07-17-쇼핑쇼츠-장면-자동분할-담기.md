# 장면 자동분할·골라담기 구현 계획

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 영상 하나를 넣으면 컷이 쪼개져 나오고, 쓸 것만 체크해서 한 번에 장면 라이브러리에 담는다.

**Architecture:** `ffmpeg select='gt(scene,0.3)'`가 컷 경계를 **프레임 번호로** 뽑고(Gemini에게 시간을 묻지 않는다), 컷마다 기존 `autotag`를 돌린다. 병합하지 않는다 — 단일컷이 기본이고 병합은 UI에서 사람이 고른다. 기존 2스텝 저장(prepare→commit)을 다건으로 넓히되 **단건 계약과 단발 토큰 성질은 그대로 둔다.**

**Tech Stack:** Python 3 / FastAPI / SQLite / ffmpeg 8.1.1 / Gemini(`edit_plan._vault_call` 캐스케이드 키풀) / 순수 JS(빌드 없음)

**스펙:** `docs/superpowers/specs/2026-07-16-쇼핑쇼츠-장면-자동분할-담기-design.md` (커밋 `04bb8fd2`)

## Global Constraints

- **트랙 폴더에서만 작업한다**: `C:\Users\TheRose\Desktop\로또의 주식\.tracks\장면라이브러리`. git은 전부 `-C`로. main 폴더의 코드 파일은 하나도 건드리지 마라.
- **`app.py`를 건드리는 커밋은 반드시 먼저 통과시킬 것**: `python -c "from shopping_shorts import app"`. 이 저장소는 post-commit 훅이 무조건 push한다.
- **초로 반올림하지 마라.** 경계·절단은 전부 프레임 번호. `-t {초}` 금지, `-frames:v {프레임수}` 사용. (스펙 §3.4)
- **경계 상한은 비디오 스트림 총 프레임 수.** `ffprobe -show_entries format=duration` 금지 — 오디오가 더 길면 화면 없는 꼬리가 붙는다. (스펙 §3.5)
- **문턱값은 `0.3` 고정.** 다른 트랙이 이미 쓰는 값이다. 낮춰도 쓸 만한 구간이 안 는다(실측). 새 감지기(PySceneDetect 등) 추가 금지 — ffmpeg와 동일함이 실측됐다.
- **최소 컷 길이 = `round(0.5 * fps)` 프레임.** 그보다 짧은 건 전환 찌꺼기다.
- **병합 금지.** 인접 컷을 코드가 이어붙이지 않는다.
- **단발 토큰 성질을 깨지 마라** (`app.py:2090-2104`의 uuid 재명명 = 이중제출 가드).
- **`role` 통제어휘**: `훅 · 반전 · CTA · 비법공개 · 반응 · 전환 · 본문`. 밖이면 `""`.
- **줄번호를 믿지 마라.** 이 계획의 줄번호는 작성 시점 기준이다. 반드시 파일을 열어 확인하고 고쳐라.
- **재현 안 된 결함은 고치지 마라.** `-ar`/`-ac` 불일치·`anullsrc`는 이 계획 범위 밖이다(스펙 §11).
- 테스트는 `pytest shopping_shorts/tests/<파일> -v`로 돌린다. 기존 green: `test_store_scene.py` 7 / `test_scene_assets.py` 16 / `test_app_scene.py` 30.

---

## File Structure

| 파일 | 책임 | 변경 |
|---|---|---|
| `shopping_shorts/scene_cut.py` | **신규.** fps·총프레임 조회, 컷 경계 검출, 프레임 절단. 순수 함수 — DB·HTTP 모름. | Create |
| `shopping_shorts/scene_assets.py` | `make_clip`을 프레임 정확히로 교체(내부만), `autotag` 프롬프트 개정 | Modify |
| `shopping_shorts/store.py` | `scene_assets`에 `source_start_frame`·`source_origin` 컬럼 추가 | Modify |
| `shopping_shorts/app.py` | `/api/scene/split` 신설, `prepare`·`commit` 확장 | Modify |
| `shopping_shorts/static/index.html` | 모달에 `자동 분할` 탭 | Modify |
| `shopping_shorts/tests/test_scene_cut.py` | **신규.** scene_cut 단위 테스트 | Create |
| `shopping_shorts/tests/test_scene_assets.py` | make_clip 프레임 정확성 회귀 | Modify |
| `shopping_shorts/tests/test_store_scene.py` | 새 컬럼 저장·조회 | Modify |
| `shopping_shorts/tests/test_app_scene.py` | split 라우트, commit 검증 | Modify |

**왜 `scene_cut.py`를 새로 파나:** `scene_assets.py`는 이미 절단·포스터·오디오추출·Gemini태그를 다 갖고 있다. 여기에 경계검출까지 넣으면 한 파일이 너무 많은 일을 한다. 경계검출은 **DB도 HTTP도 Gemini도 모르는 순수 함수**라 따로 두면 테스트가 쉽다.

---

## Task 1: scene_cut — fps·총프레임 조회

**Files:**
- Create: `shopping_shorts/scene_cut.py`
- Test: `shopping_shorts/tests/test_scene_cut.py`

**Interfaces:**
- Produces: `video_fps(path) -> float` / `video_frame_count(path) -> int`
  둘 다 실패 시 `RuntimeError`.

- [ ] **Step 1: 실패하는 테스트를 쓴다**

`shopping_shorts/tests/test_scene_cut.py` 생성:

```python
import subprocess
import pytest
from shopping_shorts import scene_cut


def _make_video(path, seconds=2, fps=30, with_audio=True, longer_audio=0.0):
    """테스트용 합성 영상. longer_audio>0이면 오디오가 비디오보다 그만큼 길다
    (실측: 릴스 3편 중 2편이 이 모양이었다 — 스펙 §3.5)."""
    cmd = ["ffmpeg", "-y", "-v", "error",
           "-f", "lavfi", "-i", f"testsrc=size=320x568:rate={fps}:duration={seconds}"]
    if with_audio:
        cmd += ["-f", "lavfi", "-i",
                f"sine=frequency=440:duration={seconds + longer_audio}"]
    cmd += ["-c:v", "libx264", "-pix_fmt", "yuv420p"]
    if with_audio:
        cmd += ["-c:a", "aac"]
    cmd += [str(path)]
    subprocess.run(cmd, check=True, capture_output=True)
    return path


def test_video_fps_reads_exact_rational(tmp_path):
    f = _make_video(tmp_path / "a.mp4", seconds=1, fps=30)
    assert scene_cut.video_fps(f) == 30.0


def test_video_frame_count_counts_video_not_audio(tmp_path):
    # 오디오가 0.5초 더 길다 — 컨테이너 길이를 쓰면 프레임 수가 부풀려진다
    f = _make_video(tmp_path / "b.mp4", seconds=2, fps=30, longer_audio=0.5)
    assert scene_cut.video_frame_count(f) == 60


def test_video_fps_raises_on_missing_file(tmp_path):
    with pytest.raises(RuntimeError):
        scene_cut.video_fps(tmp_path / "nope.mp4")
```

- [ ] **Step 2: 실패를 확인한다**

Run: `pytest shopping_shorts/tests/test_scene_cut.py -v`
Expected: FAIL — `ModuleNotFoundError: No module named 'shopping_shorts.scene_cut'`

- [ ] **Step 3: 최소 구현**

`shopping_shorts/scene_cut.py` 생성:

```python
"""컷 경계 검출·프레임 절단 — DB도 HTTP도 Gemini도 모르는 순수 함수.

★초로 반올림하지 마라. 30fps 영상은 프레임이 1/30초 간격에만 존재하므로
반올림된 초(4.13)는 '프레임이 없는 시각'이 되고, ffmpeg가 다음 프레임에
붙이면서 다음 컷의 첫 프레임이 딸려 들어온다(설계 §3.4 실측 증명).
"""
import re
import subprocess
from pathlib import Path


def _ffprobe(args):
    r = subprocess.run(["ffprobe", "-v", "error"] + args,
                       capture_output=True, text=True, check=False)
    if r.returncode != 0 or not r.stdout.strip():
        raise RuntimeError(f"ffprobe 실패: {r.stderr.strip() or '출력 없음'}")
    return r.stdout.strip()


def video_fps(path):
    """r_frame_rate를 유리수로 정확히 읽는다(30/1 → 30.0)."""
    raw = _ffprobe(["-select_streams", "v:0", "-show_entries", "stream=r_frame_rate",
                    "-of", "csv=p=0", str(path)])
    num, _, den = raw.partition("/")
    try:
        return int(num) / int(den or 1)
    except (ValueError, ZeroDivisionError) as e:
        raise RuntimeError(f"fps 해석 실패: {raw!r}") from e


def video_frame_count(path):
    """★비디오 스트림의 총 프레임 수. format=duration을 쓰면 안 된다 —
    그건 오디오와 비디오 중 긴 값이라 화면 없는 꼬리가 붙는다(설계 §3.5)."""
    raw = _ffprobe(["-select_streams", "v:0", "-count_frames",
                    "-show_entries", "stream=nb_read_frames",
                    "-of", "csv=p=0", str(path)])
    try:
        return int(raw)
    except ValueError as e:
        raise RuntimeError(f"프레임 수 해석 실패: {raw!r}") from e
```

- [ ] **Step 4: 통과를 확인한다**

Run: `pytest shopping_shorts/tests/test_scene_cut.py -v`
Expected: PASS 3건

- [ ] **Step 5: 커밋**

```bash
git -C "C:/Users/TheRose/Desktop/로또의 주식/.tracks/장면라이브러리" add shopping_shorts/scene_cut.py shopping_shorts/tests/test_scene_cut.py
git -C "C:/Users/TheRose/Desktop/로또의 주식/.tracks/장면라이브러리" commit -m "feat(scene): scene_cut — fps·총프레임을 비디오 스트림에서 정확히 읽기"
```

---

## Task 2: scene_cut — 컷 경계 검출

**Files:**
- Modify: `shopping_shorts/scene_cut.py`
- Test: `shopping_shorts/tests/test_scene_cut.py`

**Interfaces:**
- Consumes: `video_fps`, `video_frame_count` (Task 1)
- Produces: `detect_cuts(path, threshold=0.3, min_seconds=0.5) -> list[tuple[int, int]]`
  반환은 **프레임 번호 쌍** `(start_frame, end_frame)`, end는 **미포함(exclusive)**.

- [ ] **Step 1: 실패하는 테스트를 쓴다**

`test_scene_cut.py`에 추가:

```python
def _make_three_scene_video(path, fps=30):
    """색이 확 바뀌는 3장면(각 1초). 경계는 30·60프레임에 있어야 한다."""
    parts = []
    for color in ("red", "green", "blue"):
        p = path.parent / f"_{color}.mp4"
        subprocess.run(["ffmpeg", "-y", "-v", "error", "-f", "lavfi",
                        "-i", f"color=c={color}:size=320x568:rate={fps}:duration=1",
                        "-c:v", "libx264", "-pix_fmt", "yuv420p", str(p)],
                       check=True, capture_output=True)
        parts.append(p)
    lst = path.parent / "_list.txt"
    lst.write_text("".join(f"file '{p}'\n" for p in parts), encoding="utf-8")
    subprocess.run(["ffmpeg", "-y", "-v", "error", "-f", "concat", "-safe", "0",
                    "-i", str(lst), "-c", "copy", str(path)],
                   check=True, capture_output=True)
    return path


def test_detect_cuts_returns_frame_pairs_covering_video(tmp_path):
    f = _make_three_scene_video(tmp_path / "three.mp4")
    cuts = scene_cut.detect_cuts(f)
    assert cuts[0][0] == 0                      # 첫 컷은 0프레임부터
    assert cuts[-1][1] == scene_cut.video_frame_count(f)   # 마지막은 총 프레임까지
    for a, b in cuts:
        assert isinstance(a, int) and isinstance(b, int)   # ★초가 아니라 프레임
        assert b > a


def test_detect_cuts_finds_the_two_boundaries(tmp_path):
    f = _make_three_scene_video(tmp_path / "three.mp4")
    cuts = scene_cut.detect_cuts(f)
    starts = [a for a, _ in cuts]
    assert 30 in starts and 60 in starts        # 색이 바뀌는 지점


def test_detect_cuts_drops_fragments_shorter_than_min(tmp_path):
    f = _make_three_scene_video(tmp_path / "three.mp4")
    # 최소 2초 → 1초짜리 장면은 전부 탈락, 남는 게 없다
    assert scene_cut.detect_cuts(f, min_seconds=2.0) == []


def test_detect_cuts_never_exceeds_video_frames(tmp_path):
    # 오디오가 1초 더 긴 영상 — 경계가 비디오 끝을 넘으면 안 된다
    f = _make_video(tmp_path / "longaudio.mp4", seconds=2, fps=30, longer_audio=1.0)
    total = scene_cut.video_frame_count(f)
    for _, b in scene_cut.detect_cuts(f, min_seconds=0.1):
        assert b <= total
```

- [ ] **Step 2: 실패를 확인한다**

Run: `pytest shopping_shorts/tests/test_scene_cut.py -v -k detect_cuts`
Expected: FAIL — `AttributeError: module 'shopping_shorts.scene_cut' has no attribute 'detect_cuts'`

- [ ] **Step 3: 최소 구현**

`scene_cut.py`에 추가:

```python
_PTS_RE = re.compile(r"pts_time:([\d.]+)")

# 다른 트랙(frame_extract.py)이 이미 쓰는 값. 실측상 낮춰도 쓸 만한 구간은
# 안 늘고 부스러기만 폭증한다(설계 §3.3) — 건드리지 마라.
DEFAULT_THRESHOLD = 0.3
MIN_SECONDS = 0.5


def _boundary_frames(path, threshold, fps):
    """showinfo가 stderr로 뱉는 pts_time → 프레임 번호."""
    r = subprocess.run(
        ["ffmpeg", "-v", "info", "-i", str(path),
         "-vf", f"select='gt(scene,{threshold})',showinfo",
         "-vsync", "vfr", "-f", "null", "-"],
        capture_output=True, text=True, check=False)
    return {round(float(m) * fps) for m in _PTS_RE.findall(r.stderr)}


def detect_cuts(path, threshold=DEFAULT_THRESHOLD, min_seconds=MIN_SECONDS):
    """컷 경계를 프레임 번호로. 반환 (start_frame, end_frame) — end는 미포함.

    ★병합하지 않는다. 짧은 조각을 이웃과 이어붙이면 그 사람의 '편집 결정'까지
    가져오게 되고(설계 §3.2), 그건 표절 회피(§7)와 정면으로 부딪힌다.
    min_seconds보다 짧은 건 전환 찌꺼기이므로 그냥 버린다.
    """
    if not Path(path).exists():
        raise RuntimeError(f"소스 없음: {path}")
    fps = video_fps(path)
    total = video_frame_count(path)
    inner = {f for f in _boundary_frames(path, threshold, fps) if 0 < f < total}
    bounds = [0] + sorted(inner) + [total]
    floor = round(min_seconds * fps)
    return [(bounds[i], bounds[i + 1]) for i in range(len(bounds) - 1)
            if bounds[i + 1] - bounds[i] >= floor]
```

- [ ] **Step 4: 통과를 확인한다**

Run: `pytest shopping_shorts/tests/test_scene_cut.py -v`
Expected: PASS 7건

- [ ] **Step 5: 뮤테이션으로 테스트가 살았는지 확인한다**

이 저장소는 **통과하는데 결함을 못 잡는 테스트**가 여러 번 나왔다. 아래를 하나씩 넣고 **해당 테스트만** 죽는지 본다. 확인 후 반드시 되돌린다(`git checkout` 쓰지 마라 — 미커밋이 날아간다. 손으로 되돌려라).

1. `if 0 < f < total` → `if 0 < f`  → `test_detect_cuts_never_exceeds_video_frames`가 죽어야 한다
2. `>= floor` → `> 0`  → `test_detect_cuts_drops_fragments_shorter_than_min`이 죽어야 한다
3. `round(float(m) * fps)` → `round(float(m))`  → `test_detect_cuts_finds_the_two_boundaries`가 죽어야 한다

- [ ] **Step 6: 커밋**

```bash
git -C "C:/Users/TheRose/Desktop/로또의 주식/.tracks/장면라이브러리" add shopping_shorts/scene_cut.py shopping_shorts/tests/test_scene_cut.py
git -C "C:/Users/TheRose/Desktop/로또의 주식/.tracks/장면라이브러리" commit -m "feat(scene): 컷 경계를 프레임 번호로 검출 — 병합 안 함, 찌꺼기만 버림"
```

---

## Task 3: make_clip을 프레임 정확히로

**Files:**
- Modify: `shopping_shorts/scene_assets.py` (`make_clip`)
- Test: `shopping_shorts/tests/test_scene_assets.py`

**Interfaces:**
- Consumes: `scene_cut.video_fps` (Task 1)
- Produces: `make_clip(src_path, start, end, out_path)` — **시그니처 그대로**(초 받는다). 내부만 프레임 정확히로 바꾼다.

**왜 시그니처를 안 바꾸나:** `app.py`의 수동 컷 경로가 이미 이 함수를 초로 부른다. 시그니처를 바꾸면 그 경로가 깨진다. 그리고 **수동 컷도 똑같이 짜투리 버그를 갖고 있으므로**, 안에서 고치면 양쪽이 한 번에 낫는다.

- [ ] **Step 1: 실패하는 테스트를 쓴다**

`test_scene_assets.py`에 추가(맨 위 import에 `from shopping_shorts import scene_cut` 추가):

```python
def _count_frames(p):
    import subprocess
    r = subprocess.run(["ffprobe", "-v", "error", "-select_streams", "v:0",
                        "-count_frames", "-show_entries", "stream=nb_read_frames",
                        "-of", "csv=p=0", str(p)], capture_output=True, text=True, check=True)
    return int(r.stdout.strip())


def test_make_clip_cuts_exact_frame_count_on_unaligned_start(tmp_path):
    """★4.13초처럼 프레임 경계가 아닌 시각을 줘도 프레임이 새지 않아야 한다.
    실측 결함: -ss 4.13 → ffmpeg가 4.1333에 붙고 -t 1.47을 더해 5.6033이 되어
    5.600의 다음 컷 첫 프레임이 1장 딸려 들어왔다(설계 §3.4)."""
    src = tmp_path / "src.mp4"
    subprocess.run(["ffmpeg", "-y", "-v", "error", "-f", "lavfi",
                    "-i", "testsrc=size=320x568:rate=30:duration=8",
                    "-c:v", "libx264", "-pix_fmt", "yuv420p", str(src)],
                   check=True, capture_output=True)
    out = tmp_path / "clip.mp4"
    scene_assets.make_clip(src, 4.13, 5.60, out)
    # 프레임 124(=round(4.1333*30))부터 168(=round(5.60*30)) 직전까지 = 44프레임
    assert _count_frames(out) == 44
```

- [ ] **Step 2: 실패를 확인한다**

Run: `pytest shopping_shorts/tests/test_scene_assets.py -v -k exact_frame_count`
Expected: FAIL — 45프레임이 나온다(1장 더 들어옴)

- [ ] **Step 3: 구현**

`scene_assets.py` 상단에 `from shopping_shorts import scene_cut` 추가(순환 import 없음 — scene_cut은 아무것도 안 부른다). `make_clip` 본문의 `cmd` 조립을 아래로 교체한다. **줄번호를 믿지 말고 함수를 찾아 고쳐라.**

```python
def make_clip(src_path, start, end, out_path):
    """src_path의 [start,end) 구간을 잘라 규격(720x1280/30fps/libx264/aac)으로 통일.

    ★프레임 번호로 자른다. 초로 계산하면 프레임이 샌다 — 30fps 영상은 프레임이
    1/30초 간격에만 존재하므로 4.13 같은 값은 '프레임이 없는 시각'이고, ffmpeg가
    다음 프레임에 붙이면서 -t로 준 길이가 다음 컷을 물어온다(설계 §3.4 실측).

    페이즈2에서 이 클립이 비트 클립들과 concat -c copy로 붙으므로 규격이
    어긋나면 렌더가 깨진다. 구간이 비었거나 뒤집혔으면 ValueError, ffmpeg
    실패면 RuntimeError."""
    if float(end) - float(start) <= 0:
        raise ValueError(f"scene_assets: 구간이 잘못됨(start={start}, end={end})")
    fps = scene_cut.video_fps(src_path)
    a_fr = round(float(start) * fps)
    n_fr = round(float(end) * fps) - a_fr
    if n_fr <= 0:
        raise ValueError(f"scene_assets: 구간이 1프레임도 안 됨(start={start}, end={end})")
    out_path = Path(out_path)
    out_path.parent.mkdir(parents=True, exist_ok=True)
    cmd = [
        "ffmpeg", "-y",
        "-ss", f"{a_fr / fps:.6f}", "-i", str(src_path),
        "-frames:v", str(n_fr),          # ★-t {초} 금지 — 프레임 수로 끊는다
        "-vf", _SPEC_VF, "-r", "30",
        "-c:v", "libx264", "-c:a", "aac", "-pix_fmt", "yuv420p",
        str(out_path),
    ]
    r = subprocess.run(cmd, capture_output=True, check=False)
    if r.returncode != 0:
        raise RuntimeError(f"ffmpeg 구간컷 실패: {r.stderr}")
    return out_path
```

- [ ] **Step 4: 통과 + 회귀를 확인한다**

Run: `pytest shopping_shorts/tests/test_scene_assets.py -v`
Expected: PASS — 기존 16건 + 신규 1건 = 17건. **기존 16건이 하나도 깨지면 안 된다.**

- [ ] **Step 5: 뮤테이션**

`"-frames:v", str(n_fr)` → `"-t", f"{float(end)-float(start):.3f}"` 로 되돌려보고 `test_make_clip_cuts_exact_frame_count_on_unaligned_start`가 죽는지 확인한다. 죽지 않으면 테스트가 결함을 안 재고 있는 것이다. 확인 후 손으로 되돌린다.

- [ ] **Step 6: 커밋**

```bash
git -C "C:/Users/TheRose/Desktop/로또의 주식/.tracks/장면라이브러리" add shopping_shorts/scene_assets.py shopping_shorts/tests/test_scene_assets.py
git -C "C:/Users/TheRose/Desktop/로또의 주식/.tracks/장면라이브러리" commit -m "fix(scene): make_clip을 프레임 절단으로 — 짜투리 1프레임 제거(수동 컷도 함께 나음)"
```

---

## Task 4: 스키마 — source_start_frame·source_origin

**Files:**
- Modify: `shopping_shorts/store.py`
- Test: `shopping_shorts/tests/test_store_scene.py`

**Interfaces:**
- Produces: `add_scene_asset(asset, customer_id=...)`가 `asset["source_start_frame"]`(int|None)과 `asset["source_origin"]`(str) 을 저장. `list_scene_assets`가 그대로 돌려준다.

- [ ] **Step 1: 실패하는 테스트를 쓴다**

`test_store_scene.py`에 추가:

```python
def test_add_scene_asset_stores_source_start_frame_and_origin(tmp_store):
    aid = tmp_store.add_scene_asset({
        "asset_type": "clip", "render_mode": "cutaway",
        "media_path": "/x/a.mp4", "duration": 1.5, "title": "t",
        "source_kind": "instagram", "source_ref": "ABC",
        "source_start_frame": 124, "source_origin": "짜집기",
    })
    got = [a for a in tmp_store.list_scene_assets() if a["id"] == aid][0]
    assert got["source_start_frame"] == 124
    assert got["source_origin"] == "짜집기"


def test_add_scene_asset_defaults_origin_to_모름(tmp_store):
    """★기본값은 '모름'이다. 라우트가 '모름'을 막는다(설계 §7.2) —
    남의 촬영분이 라이브에 들어가는 쪽이 짤 하나 잃는 것보다 훨씬 나쁘다."""
    aid = tmp_store.add_scene_asset({
        "asset_type": "clip", "render_mode": "cutaway",
        "media_path": "/x/b.mp4", "duration": 1.0, "title": "t2",
    })
    got = [a for a in tmp_store.list_scene_assets() if a["id"] == aid][0]
    assert got["source_origin"] == "모름"
    assert got["source_start_frame"] is None
```

`tmp_store` 픽스처가 없으면 이 파일의 기존 테스트가 쓰는 방식을 그대로 따라라(파일을 열어 확인할 것).

- [ ] **Step 2: 실패를 확인한다**

Run: `pytest shopping_shorts/tests/test_store_scene.py -v -k source_start_frame`
Expected: FAIL — `KeyError` 또는 `sqlite3.OperationalError: no such column`

- [ ] **Step 3: 구현**

`store.py`에서 `scene_assets` 테이블을 만드는 곳(`CREATE INDEX ... idx_scene_assets_cat` 바로 뒤)에 **mix_jobs가 쓰는 것과 같은 패턴**으로 컬럼을 추가한다. **파일을 열어 실제 위치를 확인하라.**

```python
            # 짜집기 대응(2026-07-17, 설계 §7.1):
            #  · source_start_frame — 원본에서의 시작 **프레임**. 초로 저장하면
            #    '원본 순서대로 이어졌나' 판정에 반올림 오차가 낀다. 원본처럼 보이게
            #    만드는 건 '같은 소스'가 아니라 '원본 순서'다.
            #  · source_origin — 짜집기|촬영원본|모름. 사람이 고른다(AI 판정은
            #    실측 탈락 — 정답 아는 시험지에서 3편 전부 '촬영원본/확신 높음').
            for col, ddl in (
                ("source_start_frame", "INTEGER"),
                ("source_origin", "TEXT NOT NULL DEFAULT '모름'"),
            ):
                try:
                    c.execute(f"ALTER TABLE scene_assets ADD COLUMN {col} {ddl}")
                except sqlite3.OperationalError:
                    pass  # 이미 존재
```

`add_scene_asset`의 INSERT에 두 컬럼을 더한다:

```python
            cur = c.execute(
                "INSERT INTO scene_assets(customer_id, asset_type, render_mode, media_path, "
                "poster_path, duration, keep_original_audio, title, scene_desc, role, category, "
                "subject, tone, keywords, source_kind, source_ref, source_start_frame, "
                "source_origin, created_at) "
                "VALUES(?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,datetime('now'))",
                (customer_id, asset.get("asset_type"), asset.get("render_mode"),
                 asset.get("media_path"), asset.get("poster_path"),
                 float(asset.get("duration") or 0.0), int(asset.get("keep_original_audio") or 0),
                 asset.get("title"), asset.get("scene_desc"), asset.get("role"),
                 asset.get("category"), asset.get("subject"), asset.get("tone"),
                 self._scene_keywords(asset.get("keywords")),
                 asset.get("source_kind"), asset.get("source_ref"),
                 asset.get("source_start_frame"),
                 asset.get("source_origin") or "모름"),
```

- [ ] **Step 4: 통과 + 회귀**

Run: `pytest shopping_shorts/tests/test_store_scene.py -v`
Expected: PASS — 기존 7건 + 신규 2건 = 9건

- [ ] **Step 5: 기존 DB에 붙는지 실제로 확인한다**

마이그레이션은 **빈 DB에서만 테스트하면 거짓 안심**이다. 기존 행이 있는 DB에 컬럼이 붙는지 본다:

```bash
cd "C:/Users/TheRose/Desktop/로또의 주식/.tracks/장면라이브러리"
python -c "
from shopping_shorts.store import Store
import tempfile, os
p = os.path.join(tempfile.mkdtemp(), 't.db')
s = Store(p)                       # 새 스키마로 생성
s.add_scene_asset({'asset_type':'clip','media_path':'/x.mp4','duration':1.0,'title':'old'})
s2 = Store(p)                      # 재오픈 = 마이그레이션 재실행(멱등이어야)
rows = s2.list_scene_assets()
print('행수', len(rows), '| origin', rows[0]['source_origin'], '| frame', rows[0]['source_start_frame'])
assert rows[0]['source_origin'] == '모름'
print('OK')
"
```
Expected: `행수 1 | origin 모름 | frame None` → `OK`
(`Store` 생성자 인자가 다르면 파일을 열어 맞춰라.)

- [ ] **Step 6: 커밋**

```bash
git -C "C:/Users/TheRose/Desktop/로또의 주식/.tracks/장면라이브러리" add shopping_shorts/store.py shopping_shorts/tests/test_store_scene.py
git -C "C:/Users/TheRose/Desktop/로또의 주식/.tracks/장면라이브러리" commit -m "feat(scene): source_start_frame·source_origin 컬럼 — 표절 판정 재료"
```

---

## Task 5: /api/scene/split 라우트

**Files:**
- Modify: `shopping_shorts/app.py`
- Test: `shopping_shorts/tests/test_app_scene.py`

**Interfaces:**
- Consumes: `scene_cut.detect_cuts` (Task 2), 기존 `_reject_ssrf`, `frame_extract.download_video`, `scene_assets.make_poster`
- Produces: `POST /api/scene/split` — body `{src_url}` → `{ok, fps, total_frames, cuts:[{i, start_frame, end_frame, start, end, duration, poster_url}]}`.
  포스터는 `/api/scene/split/{token}/{i}/poster`로 받는다. **DB에 아무것도 안 쓴다.**

- [ ] **Step 1: 실패하는 테스트를 쓴다**

`test_app_scene.py`에 추가(기존 테스트가 쓰는 클라이언트·monkeypatch 방식을 그대로 따라라 — 파일을 열어 확인):

```python
def test_split_returns_frame_pairs_and_posters(client, monkeypatch, tmp_path):
    src = tmp_path / "s.mp4"
    subprocess.run(["ffmpeg", "-y", "-v", "error", "-f", "lavfi",
                    "-i", "testsrc=size=320x568:rate=30:duration=3",
                    "-c:v", "libx264", "-pix_fmt", "yuv420p", str(src)],
                   check=True, capture_output=True)
    monkeypatch.setattr("shopping_shorts.app.frame_extract.download_video",
                        lambda url, td: str(src))
    r = client.post("/api/scene/split", json={"src_url": "https://cdn.example.com/a.mp4"})
    assert r.status_code == 200
    d = r.json()
    assert d["ok"] is True
    assert d["fps"] == 30.0
    assert d["total_frames"] == 90
    assert len(d["cuts"]) >= 1
    c0 = d["cuts"][0]
    assert isinstance(c0["start_frame"], int) and isinstance(c0["end_frame"], int)
    assert c0["poster_url"].startswith("/api/scene/split/")


def test_split_rejects_internal_url(client):
    r = client.post("/api/scene/split", json={"src_url": "http://169.254.169.254/latest/meta-data/"})
    assert r.status_code == 422


def test_split_requires_src_url(client):
    assert client.post("/api/scene/split", json={}).status_code == 422
```

- [ ] **Step 2: 실패를 확인한다**

Run: `pytest shopping_shorts/tests/test_app_scene.py -v -k split`
Expected: FAIL — 404 (라우트 없음)

- [ ] **Step 3: 구현**

`app.py`의 `api_scene_save_prepare` **바로 위**에 추가. import에 `scene_cut`을 더한다(기존 `scene_assets` import 옆).

```python
_SCENE_SPLIT_DIR = _SCENE_ASSETS_DIR / "_split"   # 분할 미리보기 포스터(임시)


@app.post("/api/scene/split")
def api_scene_split(request: Request, body: dict):
    """소스 영상 → 컷 목록 + 컷별 포스터. **DB에 안 쓴다.**

    사장님이 보고 고르는 게 A안이다(설계 §4.1) — AI가 거르지 않는다.
    AI의 '짤로 쓸 만한가' 판단은 검증된 적이 없다."""
    src_url = (body.get("src_url") or "").strip()
    if not src_url:
        return JSONResponse(status_code=422, content={"ok": False, "error": "src_url 필요"})
    ssrf_err = _reject_ssrf(src_url)
    if ssrf_err:
        return JSONResponse(status_code=422, content={"ok": False, "error": ssrf_err})
    token = uuid.uuid4().hex
    out_dir = _SCENE_SPLIT_DIR / token
    out_dir.mkdir(parents=True, exist_ok=True)
    try:
        with tempfile.TemporaryDirectory() as td:
            src = frame_extract.download_video(src_url, td)
            fps = scene_cut.video_fps(src)
            total = scene_cut.video_frame_count(src)
            cuts = scene_cut.detect_cuts(src)
            out = []
            for i, (a, b) in enumerate(cuts):
                poster = out_dir / f"{i}.jpg"
                # 컷 대표 프레임 = 시작에서 3프레임 뒤(전환 잔상 회피)
                subprocess.run(
                    ["ffmpeg", "-y", "-v", "error", "-ss", f"{(a + 3) / fps:.6f}",
                     "-i", str(src), "-frames:v", "1", "-vf", "scale=180:-1", str(poster)],
                    check=False, capture_output=True)
                out.append({
                    "i": i, "start_frame": a, "end_frame": b,
                    "start": round(a / fps, 3), "end": round(b / fps, 3),
                    "duration": round((b - a) / fps, 3),
                    "poster_url": f"/api/scene/split/{token}/{i}/poster",
                })
    except Exception as e:  # noqa: BLE001 — 다운로드/ffmpeg 실패는 그대로 알림
        return JSONResponse(status_code=502, content={"ok": False, "error": f"분할 실패: {e}"})
    return {"ok": True, "token": token, "fps": fps, "total_frames": total, "cuts": out}


@app.get("/api/scene/split/{token}/{i}/poster")
def api_scene_split_poster(token: str, i: int):
    """분할 미리보기 썸네일. 토큰은 hex 검증 — 경로조작 차단."""
    if not re.fullmatch(r"[0-9a-f]{32}", token or ""):
        return Response(status_code=404, content=b"")
    p = _SCENE_SPLIT_DIR / token / f"{int(i)}.jpg"
    if not p.exists():
        return Response(status_code=404, content=b"")
    return FileResponse(str(p), media_type="image/jpeg")
```

`re`·`subprocess`가 `app.py`에 이미 import돼 있는지 확인하고 없으면 추가하라.

- [ ] **Step 4: 통과 + import 게이트 + 회귀**

```bash
cd "C:/Users/TheRose/Desktop/로또의 주식/.tracks/장면라이브러리"
python -c "from shopping_shorts import app"          # ★app.py 커밋 전 필수
pytest shopping_shorts/tests/test_app_scene.py -v
```
Expected: import 에러 없음 / 기존 30건 + 신규 3건 = 33건 PASS

- [ ] **Step 5: 커밋**

```bash
git -C "C:/Users/TheRose/Desktop/로또의 주식/.tracks/장면라이브러리" add shopping_shorts/app.py shopping_shorts/tests/test_app_scene.py
git -C "C:/Users/TheRose/Desktop/로또의 주식/.tracks/장면라이브러리" commit -m "feat(scene): /api/scene/split — 컷 목록+포스터, DB 미기록"
```

---

## Task 6: commit이 source_origin·render_mode를 강제

**Files:**
- Modify: `shopping_shorts/app.py` (`api_scene_save_prepare`, `api_scene_save_commit`)
- Test: `shopping_shorts/tests/test_app_scene.py`

**Interfaces:**
- Consumes: Task 4의 컬럼, Task 5의 프레임 정보
- Produces: `prepare` body에 `source_start_frame?` 수용 → `commit`이 `source_origin` 필수(`모름`이면 422), `asset_type=="clip"`일 때 `render_mode` 필수(없으면 422)

- [ ] **Step 1: 실패하는 테스트를 쓴다**

```python
def test_commit_rejects_unknown_origin(client, prepared_token):
    """★'모름'이면 막는다(설계 §7.2). 손해가 비대칭이다 — 짤 하나 잃는 것보다
    남의 촬영분이 라이브에 들어가는 게 훨씬 나쁘다."""
    r = client.post("/api/scene/save/commit", json={
        "token": prepared_token, "title": "t", "asset_type": "clip",
        "render_mode": "cutaway", "source_origin": "모름"})
    assert r.status_code == 422
    assert "출처" in r.json()["error"] or "origin" in r.json()["error"]


def test_commit_rejects_missing_origin(client, prepared_token):
    r = client.post("/api/scene/save/commit", json={
        "token": prepared_token, "title": "t", "asset_type": "clip",
        "render_mode": "cutaway"})
    assert r.status_code == 422


def test_commit_rejects_clip_without_render_mode(client, prepared_token):
    """페이즈1 리뷰 잔여: render_mode 없이 저장되면 배지가 NULL을 '컷어웨이'로
    거짓 표기한다(설계 §8)."""
    r = client.post("/api/scene/save/commit", json={
        "token": prepared_token, "title": "t", "asset_type": "clip",
        "source_origin": "짜집기"})
    assert r.status_code == 422


def test_commit_stores_origin_and_start_frame(client, prepared_token, store):
    r = client.post("/api/scene/save/commit", json={
        "token": prepared_token, "title": "t", "asset_type": "clip",
        "render_mode": "cutaway", "source_origin": "짜집기",
        "source_start_frame": 124})
    assert r.status_code == 200
    got = store.list_scene_assets()[-1]
    assert got["source_origin"] == "짜집기"
    assert got["source_start_frame"] == 124
```

`prepared_token`·`store` 픽스처가 없으면 기존 commit 테스트가 쓰는 방식을 그대로 따라 만든다(파일을 열어 확인).

- [ ] **Step 2: 실패를 확인한다**

Run: `pytest shopping_shorts/tests/test_app_scene.py -v -k "origin or render_mode"`
Expected: FAIL — 200이 돌아온다(검증 없음)

- [ ] **Step 3: 구현**

`api_scene_save_commit`에서 `title` 검증 **바로 뒤**에 추가:

```python
    origin = (body.get("source_origin") or "").strip()
    if origin not in ("짜집기", "촬영원본"):
        # ★'모름'과 미지정을 모두 막는다(설계 §7.2). AI 판정은 실측 탈락했고
        # (정답 아는 시험지에서 3편 전부 '촬영원본/확신 높음'), 사람만 안다.
        return JSONResponse(status_code=422, content={
            "ok": False, "error": "출처를 골라야 저장됩니다(짜집기/촬영원본)"})
    asset_type = (body.get("asset_type") or "clip").strip()
    render_mode = (body.get("render_mode") or "").strip()
    if asset_type == "clip" and not render_mode:
        return JSONResponse(status_code=422, content={
            "ok": False, "error": "clip은 render_mode가 필요합니다"})
```

`add_scene_asset`에 넘기는 dict에 두 필드를 더한다:

```python
        "source_origin": origin,
        "source_start_frame": (int(body["source_start_frame"])
                               if str(body.get("source_start_frame", "")).strip().isdigit()
                               else None),
```

`api_scene_save_prepare`가 `start_frame`을 돌려주게 한다. 프런트는 이 값을 commit 때 그대로 되돌려주고, 그게 `source_start_frame`으로 저장된다.

**★소스의 fps로 계산해야 한다.** 잘린 클립은 `-r 30`으로 통일되므로 클립의 fps로 계산하면 원본이 30fps가 아닐 때 틀린 프레임 번호가 저장된다. 소스 파일은 `with tempfile...` 블록이 끝나면 지워지므로 **블록 안에서** 구해 바깥 변수에 담는다.

`try:` 블록을 아래로 교체한다:

```python
    src_start_frame = None
    try:
        with tempfile.TemporaryDirectory() as td:
            src = frame_extract.download_video(src_url, td)
            # ★소스의 fps로 — 클립은 -r 30으로 통일되므로 클립 fps를 쓰면 틀린다
            src_start_frame = round(start * scene_cut.video_fps(src))
            scene_assets.make_clip(src, start, end, clip)
    except Exception as e:  # noqa: BLE001 — 다운로드/ffmpeg 실패는 사용자에게 그대로 알림
        return JSONResponse(status_code=502, content={"ok": False, "error": f"구간컷 실패: {e}"})
```

반환에 `start_frame`을 더한다:

```python
    return {"ok": True, "token": token, "start_frame": src_start_frame,
            "duration": scene_assets.probe_duration(clip),
            "poster_url": f"/api/scene/prepared/{token}/poster", "draft": draft}
```

이 값을 확인하는 테스트도 Step 1에 더한다:

```python
def test_prepare_returns_source_start_frame(client, monkeypatch, tmp_path):
    """★소스 fps 기준이어야 한다. 클립은 30fps로 통일되므로 클립 fps로 재면
    원본이 24fps일 때 틀린 번호가 저장된다."""
    src = tmp_path / "s24.mp4"
    subprocess.run(["ffmpeg", "-y", "-v", "error", "-f", "lavfi",
                    "-i", "testsrc=size=320x568:rate=24:duration=4",
                    "-c:v", "libx264", "-pix_fmt", "yuv420p", str(src)],
                   check=True, capture_output=True)
    monkeypatch.setattr("shopping_shorts.app.frame_extract.download_video",
                        lambda url, td: str(src))
    r = client.post("/api/scene/save/prepare", json={
        "src_url": "https://cdn.example.com/a.mp4", "start": 1.0, "end": 2.0})
    assert r.status_code == 200
    assert r.json()["start_frame"] == 24        # 30이 나오면 클립 fps로 잰 것
```

- [ ] **Step 4: 통과 + import 게이트 + 회귀**

```bash
cd "C:/Users/TheRose/Desktop/로또의 주식/.tracks/장면라이브러리"
python -c "from shopping_shorts import app"
pytest shopping_shorts/tests/test_app_scene.py -v
```
Expected: PASS. **기존 commit 테스트가 깨질 것이다** — `source_origin`이 없으니 422가 난다. 기존 테스트에 `"source_origin": "짜집기"`를 더해 고쳐라. 그게 정상이다(계약이 엄해진 것).

- [ ] **Step 5: 뮤테이션**

`if origin not in ("짜집기", "촬영원본")` → `if origin == "절대없는값"` 으로 바꿔 `test_commit_rejects_unknown_origin`이 죽는지 확인한다. 안 죽으면 테스트가 헛돈 것이다. 손으로 되돌린다.

- [ ] **Step 6: 커밋**

```bash
git -C "C:/Users/TheRose/Desktop/로또의 주식/.tracks/장면라이브러리" add shopping_shorts/app.py shopping_shorts/tests/test_app_scene.py
git -C "C:/Users/TheRose/Desktop/로또의 주식/.tracks/장면라이브러리" commit -m "feat(scene): commit이 출처·render_mode 강제 — 모름이면 저장 차단"
```

---

## Task 7: autotag 프롬프트 — scene_desc와 role의 재료 분리

**Files:**
- Modify: `shopping_shorts/scene_assets.py` (`_AUTOTAG_PROMPT`)
- Test: `shopping_shorts/tests/test_scene_assets.py`

**Interfaces:**
- Produces: `autotag` 반환 형태 **그대로**(`scene_desc, role, subject, tone, keywords`). 프롬프트만 바뀐다.

**왜:** 통제어휘엔 `비법공개`·`CTA`가 있는데 프롬프트가 `"나쁜 예: 비법을 공개하는 장면"`으로 **그 판정을 금지**한다. 금지는 옳다 — 화면만 주고 물으면 추측이다. **재료를 안 준 게 문제다.** 릴스는 무음 시청 매체라 **대사가 자막으로 화면에 박혀 있다**(실측 육안). 그걸 읽게 하면 된다. 추가 비용 0.

- [ ] **Step 1: 실패하는 테스트를 쓴다**

```python
def test_autotag_prompt_separates_evidence_for_desc_and_role():
    p = scene_assets._AUTOTAG_PROMPT
    assert "자막" in p                      # role의 근거가 자막임을 명시
    assert "나쁜 예" in p                   # scene_desc의 추측 금지는 유지
    assert "한 스푼" in p                   # subject 모호금지 규칙 유지(적대적 검증 통과분)
    assert "자막이 없으면" in p             # 자막 없으면 role 공란


def test_autotag_prompt_keeps_controlled_roles():
    p = scene_assets._AUTOTAG_PROMPT.format(
        category="레시피", caption="c", script="s", roles="|".join(scene_assets._ROLES))
    assert "비법공개" in p and "CTA" in p
```

- [ ] **Step 2: 실패를 확인한다**

Run: `pytest shopping_shorts/tests/test_scene_assets.py -v -k prompt`
Expected: FAIL — `"자막"`이 프롬프트에 없다

- [ ] **Step 3: 구현**

`_AUTOTAG_PROMPT`의 `규칙:` 블록에서 `scene_desc`·`role` 두 줄을 아래로 교체한다. **나머지 줄(subject·tone·keywords)은 손대지 마라** — subject 규칙은 페이즈1에서 적대적 조건으로 검증됐다.

```
규칙:
- scene_desc: **화면에 보이는 사물·동작만** 한 문장으로. 추측·해석 금지.
  자막 글자를 여기에 옮겨 적지 마라 — 여기는 '무엇이 보이는가'다.
  (나쁜 예: "비법을 공개하는 장면" / 좋은 예: "흰 가루를 숟가락으로 떠 그릇에 넣음")
- role: 반드시 다음 중 하나만: {roles}
  ★근거는 **화면에 박힌 자막 글자**다. 릴스는 소리 없이 보는 매체라 대사가 자막으로
  화면에 있다. 그 글자를 읽고 정하라.
  (예: "이것만 넣으면 되요" → CTA / "비법 한 스푼" → 비법공개)
  **자막이 없으면 role을 빈 문자열로 두라.** 화면만 보고 역할을 추측하지 마라 —
  틀린 role은 나중에 엉뚱한 자리에 짤이 꽂히게 만든다.
```

- [ ] **Step 4: 통과 + 회귀**

Run: `pytest shopping_shorts/tests/test_scene_assets.py -v`
Expected: PASS 전건

- [ ] **Step 5: ★실호출로 관측한다 (가짜 통과 금지)**

프롬프트 테스트는 **문자열만 본다.** Gemini가 실제로 그렇게 답하는지는 별개다. 이 트랙의 교훈이다 — "테스트 통과 = 동작"이 아니다.

`scratchpad/` 프로브를 만들어 **자막이 박힌 실 프레임**으로 호출하고 눈으로 확인한다:

```bash
cd "C:/Users/TheRose/Desktop/로또의 주식/.tracks/장면라이브러리"
python -c "
from shopping_shorts import scene_assets
# 자막이 박힌 실제 릴스 프레임 경로를 넣어라(main 폴더 scratchpad/fx_*/ 에 있음)
r = scene_assets.autotag(['<자막 있는 프레임.jpg>'], {'category':'레시피'})
print('scene_desc:', r['scene_desc'])
print('role      :', r['role'])
print('subject   :', r['subject'])
"
```
합격 기준: `scene_desc`에 자막 글자가 안 들어갔고, `role`이 통제어휘 안이거나 빈 값이다.
**키가 없어 못 돌면 "미관측"이라고 정직하게 보고하라.** 돌린 척하지 마라.

- [ ] **Step 6: 커밋**

```bash
git -C "C:/Users/TheRose/Desktop/로또의 주식/.tracks/장면라이브러리" add shopping_shorts/scene_assets.py shopping_shorts/tests/test_scene_assets.py
git -C "C:/Users/TheRose/Desktop/로또의 주식/.tracks/장면라이브러리" commit -m "feat(scene): role의 근거를 화면 자막으로 — scene_desc와 재료 분리"
```

---

## Task 8: UI — 자동 분할 탭

**Files:**
- Modify: `shopping_shorts/static/index.html` (모달 `:179-199`, JS `:726-820`)
- Test: 없음(프런트). **사장님 육안 확인이 게이트다.**

**Interfaces:**
- Consumes: `/api/scene/split`, `/api/scene/save/prepare`, `/api/scene/save/commit`

- [ ] **Step 1: 모달에 탭을 넣는다**

`<b>🎞 장면 담기</b>` 블록을 찾아(줄번호 믿지 말 것) 비디오 위에 탭 두 개를 넣는다. 기존 `시작 찍기`/`끝 찍기` 블록은 **`직접 자르기` 탭 안으로 그대로 옮긴다. 지우지 마라**(설계 §4.2 — AI가 놓친 걸 건지는 통로).

```html
<div class="scene-tabs">
  <button id="tabSplit" class="on" onclick="sceneTab('split')">자동 분할</button>
  <button id="tabManual" onclick="sceneTab('manual')">직접 자르기</button>
</div>
<div id="paneSplit">
  <button onclick="runSceneSplit()">✂ 컷 나누기</button>
  <div id="splitList"></div>
  <button id="btnMergePick" style="display:none" onclick="mergePicked()">↔ 이어서 하나로 담기</button>
  <button id="btnTagPicked" style="display:none" onclick="tagPicked()">자르고 AI 태그 뽑기</button>
</div>
<div id="paneManual" style="display:none">
  <!-- 기존 시작/끝 찍기 블록을 여기로 옮긴다 -->
</div>
```

- [ ] **Step 2: 분할·선택 JS를 넣는다**

`openSceneCut` 근처에 추가. `esc()`는 이 파일에 이미 있다(작은따옴표 차단 — 쓰지 않으면 XSS).

```javascript
let SPLIT = { token:null, fps:30, cuts:[], picked:new Set() };

async function runSceneSplit(){
  const r = await fetch('/api/scene/split', {method:'POST',
    headers:{'Content-Type':'application/json'},
    body: JSON.stringify({src_url: SCENE_CTX.videoUrl})});
  const d = await r.json();
  if(!d.ok){ alert('분할 실패: ' + (d.error||'')); return; }
  SPLIT = {token:d.token, fps:d.fps, cuts:d.cuts, picked:new Set()};
  document.getElementById('splitList').innerHTML = d.cuts.map(c => `
    <label class="cut">
      <input type="checkbox" onchange="pickCut(${c.i}, this.checked)">
      <img src="${esc(c.poster_url)}">
      <span>${c.i+1} · ${c.duration.toFixed(2)}s</span>
    </label>`).join('');
  syncSplitButtons();
}

function pickCut(i, on){ on ? SPLIT.picked.add(i) : SPLIT.picked.delete(i); syncSplitButtons(); }

function syncSplitButtons(){
  const n = SPLIT.picked.size;
  document.getElementById('btnTagPicked').style.display = n ? '' : 'none';
  // ★인접한 것을 2개 이상 골랐을 때만 병합 — 기계가 아니라 사람이 정한다(설계 §3.2)
  const s = [...SPLIT.picked].sort((a,b)=>a-b);
  const adjacent = s.length >= 2 && s.every((v,k)=> k===0 || v === s[k-1]+1);
  document.getElementById('btnMergePick').style.display = adjacent ? '' : 'none';
}
```

- [ ] **Step 3: 담기·병합을 붙인다**

```javascript
async function tagPicked(){
  for(const i of [...SPLIT.picked].sort((a,b)=>a-b)){
    const c = SPLIT.cuts[i];
    await prepareOne(c.start, c.end, c.start_frame);
  }
}

async function mergePicked(){
  const s = [...SPLIT.picked].sort((a,b)=>a-b);
  const a = SPLIT.cuts[s[0]], b = SPLIT.cuts[s[s.length-1]];
  await prepareOne(a.start, b.end, a.start_frame);   // 이어붙인 하나로
}

async function prepareOne(start, end, startFrame){
  const r = await fetch('/api/scene/save/prepare', {method:'POST',
    headers:{'Content-Type':'application/json'},
    body: JSON.stringify({src_url:SCENE_CTX.videoUrl, start, end,
      source_kind:SCENE_CTX.sourceKind, source_ref:SCENE_CTX.shortcode,
      category:SCENE_CTX.category})});
  const d = await r.json();
  if(!d.ok){ alert('구간컷 실패: ' + (d.error||'')); return; }
  d._start_frame = (d.start_frame != null) ? d.start_frame : startFrame;
  showSceneDraft(d);      // 기존 태그 초안 폼 재사용
}
```

`SCENE_CTX`의 실제 필드명을 파일에서 확인하고 맞춰라(`videoUrl`이 아닐 수 있다).

- [ ] **Step 4: 저장 폼에 출처를 넣는다**

`showSceneDraft`가 그리는 폼에 **출처 선택**을 더한다. 기본값은 비워둔다 — 고르지 않으면 서버가 422로 막는다(Task 6).

```html
<label>출처 <select id="fOrigin">
  <option value="">— 고르세요 —</option>
  <option value="짜집기">짜집기(떠도는 조각)</option>
  <option value="촬영원본">촬영원본(그 사람이 찍은 것)</option>
</select></label>
```

`commitScene()`이 보내는 body에 더한다:

```javascript
  source_origin: document.getElementById('fOrigin').value,
  source_start_frame: (SCENE_DRAFT && SCENE_DRAFT._start_frame) || null,
```

- [ ] **Step 5: ★사장님 육안 확인 (이것 없이 병합 금지)**

프런트는 테스트가 없다. **서버가 200을 준다고 화면이 도는 게 아니다** — 이 저장소에서 "테스트 다 통과했는데 실제론 무동작"이 이미 여러 번 나왔다.

확인할 것:
1. 랭킹 카드 → `🎞 장면 담기` → `컷 나누기` → 20초 릴스에서 **14~20개 카드**가 뜨나
2. 체크 → `자르고 AI 태그 뽑기` → 태그 초안이 뜨나
3. 출처를 안 고르고 저장 → **422로 막히나**
4. 인접한 것 2개 체크 → `이어서 하나로 담기` 버튼이 **나타나나** (떨어진 것 2개는 안 나타나야)
5. `직접 자르기` 탭 → 기존 시작/끝 찍기가 **그대로 되나**
6. 콘솔 에러 0

- [ ] **Step 6: 커밋**

```bash
git -C "C:/Users/TheRose/Desktop/로또의 주식/.tracks/장면라이브러리" add shopping_shorts/static/index.html
git -C "C:/Users/TheRose/Desktop/로또의 주식/.tracks/장면라이브러리" commit -m "feat(scene): 장면 담기 모달에 자동 분할 탭 — 전 컷 노출·수동 병합·출처 필수"
```

---

## Task 9: 실 릴스로 end-to-end 관측

**Files:** 없음(관측만)

**왜 별도 태스크인가:** 이 트랙의 가장 큰 교훈이다 — **"테스트 통과"는 동작의 증거가 아니다.** 페이즈1에서도 ffmpeg·Gemini가 전부 가짜화된 채 30테스트가 green이었고, 실제로 도는 걸 아무도 본 적이 없었다.

- [ ] **Step 1: 실 릴스로 담아본다**

라이브에서 릴스 **두세 편**을 실제로 담는다. 소스는 랭킹 카드 또는 도서관(`wiki_media`).

- [ ] **Step 2: 규격을 직접 확인한다**

```bash
ffprobe -v error -select_streams v:0 -show_entries stream=width,height,r_frame_rate,codec_name,pix_fmt -of csv=p=0 <담긴 클립.mp4>
```
Expected: `720,1280,30/1,h264,yuv420p` — 틀리면 페이즈2의 `concat -c copy`가 깨진다.

- [ ] **Step 3: ★짜투리가 없는지 확인한다 (이 트랙의 핵심 결함)**

클립의 **마지막 프레임**이 다음 컷이 아닌지 본다. **`-ss`로 뽑지 마라 — 프레임이 밀린다.** 순차 디코딩으로:

```bash
N=$(ffprobe -v error -select_streams v:0 -count_frames -show_entries stream=nb_read_frames -of csv=p=0 clip.mp4)
ffmpeg -v error -i clip.mp4 -vf "select='gte(n\,$((N-3)))'" -vsync 0 tail_%02d.jpg
```
마지막 3장을 **눈으로 본다.** 마지막 장이 딴 장면이면 실패다.

- [ ] **Step 4: 성공 판정**

**`scene_assets`가 두 자릿수가 됐나.** 그게 이 기능의 존재 이유이고 페이즈2의 착수 조건이다(설계 §13).

```bash
ssh -i C:\Users\TheRose\crawling_bot_client\LightsailDefaultKey-ap-northeast-2.pem ubuntu@3.39.179.148 \
  "cd /home/ubuntu/lotto-stock-wiki && python3 -c \"
from shopping_shorts.store import Store
from shopping_shorts import config
print(len(Store(config.DB_PATH).list_scene_assets()))\""
```
(`config` 경로가 다르면 파일을 열어 맞춰라. DB는 `reference.db`다 — `app.db`가 아니다.)

- [ ] **Step 5: 정직하게 보고한다**

안 된 게 있으면 **"미관측"이라고 쓴다.** 이 트랙에서 서브에이전트가 "이탈 없음"이라 보고했는데 실제론 지시가 반영 안 된 적이 있다. **보고는 실행으로 검증한다.**

---

## Task 10: 최종 whole-branch 리뷰

**Files:** 없음(리뷰)

**생략 금지.** 이 저장소에서 **개별 태스크 리뷰가 통과시킨 Critical을 최종 리뷰만 잡은 전례**가 있다. 개별 리뷰와 격리된 grounding은 **배선 버그(저장 위치 ≠ 읽기 위치)를 못 본다.**

- [ ] **Step 1: 브랜치 전체 diff를 opus 리뷰어에게 넘긴다**

```bash
git -C "C:/Users/TheRose/Desktop/로또의 주식/.tracks/장면라이브러리" diff origin/main...HEAD
```

리뷰어에게 반드시 넘길 것:
- 설계 문서 경로(`docs/superpowers/specs/2026-07-16-...-design.md`)
- **§15의 오진 표** — 같은 함정을 리뷰어도 밟을 수 있다
- 특히 볼 것: `make_clip` 변경이 **수동 컷 경로**를 깨지 않는지 / `commit`의 새 검증이 **기존 자산 편집(update)**을 막지 않는지 / split 포스터 고아 파일

- [ ] **Step 2: Critical·Important를 닫는다**

- [ ] **Step 3: 병합**

```bash
cd "C:/Users/TheRose/Desktop/로또의 주식"     # ★main 폴더에서
py tools/track.py finish 장면라이브러리
```
게이트: 문법 → `import shopping_shorts.app` → pytest. 기준은 **"전부 green"이 아니라 "병합 전보다 실패가 늘지 않았나"**다(기준선 10건은 이미 깨져 있다).

---

## 범위 밖 (건드리지 마라)

- **`-ar`/`-ac` 고정·`anullsrc`** — 재현 안 됐다(설계 §11.2). 실측 3편이 전부 `44100/2`였다. **재현 전에 고치면 §11.1을 반복한다**(없는 문제를 고치려다 오진).
- **PySceneDetect 등 새 감지기** — ffmpeg와 동일함이 실측됐다(§3.3).
- **고아 파일 청소** — split 포스터·미커밋 클립이 고아가 된다. 청소는 "커밋 안 된 클립"이 아니라 **"DB 미참조 파일 전부"**로 설계해야 한다(페이즈1 최종리뷰 결론). 별도 크론.
- **SSRF 계통** — 저장소 전체의 문제다.
- **페이즈2 `match_scene_assets`** — 재료가 두 자릿수가 된 뒤.
