
## 2026-09-10 — 가림막 '이 장면에만' (라이브 대기: 배포 시간창)
- 무엇: 가림막마다 `beat`(자막 칸)·`cut`(컷)을 붙이면 그 장면 시간에만 덮는다. 없으면 종전과 완전히 같다(캐시키·그림·ffmpeg 명령 테스트로 못박음).
- 화면: 효과 탭 `🎬 새로 얹는 건 지금 장면에만` 체크 + 편집기 맨 위 `🌐 전체 장면 / 🎬 이 장면만`. 알약에 `🎬N장면` 표시. 미리보기는 지금 장면 것만 보인다(자동재생 컷 전환 포함).
- 서버: `deco_frame.split_scene_masks` 한 곳에서 가름 → 전체용은 틀 그림, 장면용은 `mix_pipeline._scene_mask_layers`(시각=final_clip_pairs, 미리보기 장면 목록과 같은 함수) → `video_assemble._burn_captions`가 enable 창으로 얹음(흐림도 지원).
- 검증: tests/test_scene_masks.py 17개(실 ffmpeg 픽셀 포함) + 연관 802 통과. 서버 미리보기(DB 사본)에서 저장 확인, 격리 폴더 실제 렌더 → 2번 장면만 덮임 확인.
- ⏭ 한계: 캡컷 내보내기엔 장면전용 가림막이 **안 들어간다**(전체 가림막만). 필요하면 capcut_draft에 시간 구간 사진 세그먼트 추가.
- ⏭ 배포 후 라이브 화면에서 한 번 더 확인.
