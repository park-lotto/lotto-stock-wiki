# 장면꾸미기 UI·정밀 템플릿 디자인 시스템

## ★먼저 읽기 — 이 트랙이 고치는 **페이지**가 어디인가 (2026-09-21 정리. 여기서 여러 번 헷갈렸다)

**지금 작업 대상 = 새 편집기(②)의 오른쪽 "빠른 조절" → `채널명 칸` 슬라이더.** 사장님이 캡처로 직접 짚은 자리다.

고객이 제작소 **6단계 · 장면꾸미기**에서 보는 화면은 **두 개**다. 섞지 마라.

| | ① 옛 꾸미기(피팅룸) | ② 새 편집기 ← **이 트랙** |
|---|---|---|
| 고객이 가는 길 | 제작소 6단계에 들어가면 바로 보이는 화면(자막·헤드카피·템플릿·효과 탭) | 6단계 위쪽 회색 버튼 **「장면꾸미기 · 문구와 효과 편집」** → 팝업(iframe) |
| 파일 | `shopping_shorts/static/produce.html`(템플릿 탭 "📰 내용물 있는 틀" 2636행~) | `out/scene-style-ui-showcase.html` + **`out/precision20-ui.js`**(로직) + `out/precision20-data.js`(템플릿 60칸 좌표) |
| 연결 | — | `shopping_shorts/static/scene-style-produce.js`(`openSceneStyleEditor`) → `/api/produce/scene-style/assets/out/...?embedded=1` · 실제 자막은 `/api/produce/scene-style/context/{job_id}`(`app.py`, 관리자 **또는 job 주인**이면 통과 = 고객에게도 열려 있다) |
| 관리자 전용? | 아님 | 아님. 관리자 전용은 그 위 **카나리 영역**(`#sceneStyleCanary`)뿐 |
| 이번에 손댔나 | 안 댔다(재지도 않았다) | 댔다 |

②에서 깨진다고 짚인 곳: 오른쪽 패널 **"훅/본문 빠른 조절"**의 슬라이더 4개 중 `채널명 칸`(`data-fixed-range="channel"`), 그다음 `상단 제목칸`(`"top"`). 채널명 글자 ＋/−(`data-font-step`)는 **다른 컨트롤**이다 — 처음에 이걸 재느라 시간을 썼다.
템플릿은 왼쪽 목록의 **썰쇼핑형 20**(훅+본문 = 40칸) / **전장면 고정형 20**(20칸) = 60칸.

### 로컬에서 보는 주소(둘 다 이 트랙 폴더의 파일을 그대로 서빙 — 고치고 Ctrl+F5)
| 주소 | 무엇 | 언제 |
|---|---|---|
| `http://127.0.0.1:8771/out/scene-style-ui-showcase.html` | ②의 **샘플 작업대**(솔 사진·12장 고정·위에 A/B/C/D 시안 탭 — 고객에겐 없는 탭). 트랙 폴더에서 띄운 정적 서버(`py -m http.server 8771`) | 60칸 전수 검사·빠른 확인. 검사 도구 기본 주소 |
| `http://127.0.0.1:8772/produce.html` → 회색 버튼 | **서버 실제 job**(관리자 `93de4727fc85`, 자막 24개)으로 띄운 제작소. `py tools/serve_local_mirror.py`(선행 `py tools/mirror_live_job.py <job_id>`) | 고객 길 그대로 확인. 아직 칸 구조는 여기서 확인 안 함 |
| 라이브 `shoppingshorts.duckdns.org` | 고객 화면. **v182 그대로**(서버 MD5 `7ac372f8…` 확인) | 사장님 승인 전엔 건드리지 않는다 |

코드 위치: 전부 **`.tracks/장면꾸미기UI코덱스`**(브랜치 `track/장면꾸미기UI코덱스`). main·서버 변경 0. 로컬 캐시버스터는 `?v=190`(showcase.html 안).

## 2026-09-16 · 관리자 실데이터 LAB 네 출구 연결

- 관리자 본인 작업만 복사하는 `/scene_style_lab.html`을 추가했다. 원본 job은 읽기만 하며 스냅샷·MP4·CapCut 산출물은 `mix_jobs/_scene_style_lab/<lab_id>`에만 쓴다. 일반 고객은 페이지·API·프레임·영상·CapCut 재료 모두 404다.
- 현재 편성 서명과 일치하는 `clean_sources` 또는 `clean_final_path_for_plan()`만 허용한다. preview/video 원본 폴백은 없으며 원본 편성이 바뀌면 렌더와 내보내기를 409로 막는다.
- `hookCaptionMode=hidden`은 첫 비트의 음성·자막 시간과 문구를 보존한 채 말자막 레이어만 숨긴다. 본문 첫 자막은 실제 `caption_schedule()` 경계에서 바로 시작한다.
- 동일 scene-style 스냅샷/컨텍스트를 브라우저, MP4, CapCut 투명 PNG 오버레이, 관리자 전용 랜딩이 사용한다. CapCut 시험 초안은 네이티브 말자막을 만들지 않아 이중 자막이 없고 생성 JSON의 시작·길이를 μs 단위로 역검증한다.
- 로컬 실제 실측: 4.000초 청소 영상, 훅 1.800초/본문 2.200초. 훅 말자막 0개, 본문 첫 자막 1.800초(오차 0프레임), 편성 서명 `738df405dbc0ee00`. CapCut 텍스트 트랙 0개, 오버레이 범위 0–0.9/0.9–1.8/1.8–2.8/2.8–4.0초.
- 관리자 화면과 랜딩을 실제 Chromium으로 열어 MP4 재생과 청소본 프레임 연결을 눈으로 확인했다. MP4/랜딩의 화면 자막은 입력 계약만으로 자동 PASS 처리하지 않고 `실물 육안 확인`으로 표시한다. 증거와 재현 명령은 `docs/superpowers/evidence/2026-09-16-scene-style-lab-local-proof.md`.
- 새 스냅샷 저장 시 이전 MP4/CapCut 비교 결과를 즉시 무효화한다. 일반 제작소 CapCut 호출은 `scene_overlay_layers=None`이라 기존 네이티브 자막 동작을 보존한다.
- 리뷰 보강: 편성 서명과 별도로 TTS 파일 SHA-256·자막 타이밍 서명을 고정한다. MP4는 실제 파일 SHA-256·크기·재생시간 영수증을 저장하고 영상/랜딩 제공 때 재검사한다. 렌더 실패 상태도 UI에 노출한다.
- 동시 렌더는 요청 세대를 background task에 고정 전달하고 LAB별 잠금 안에서 스냅샷 저장·세대 확인·산출 필드 병합·완료 상태 기록을 수행한다. 더 늦은 요청이 생기면 이전 작업은 generation별 파일만 남기고 manifest를 갱신하지 못한다.
- 최신 main 병합 뒤 기존 썸네일/제목 Node 하네스 66개가 외부 장식 카탈로그의 `window` 의존으로 연쇄 실패했다. 제품은 카탈로그 로드 실패 시 빈 목록으로 안전 부팅하고, 장식 자체 테스트는 실제 `scene-decoration-catalog.js`를 읽도록 수정해 66개 전부 통과했다.
- 잔여 썸네일 슬라이스 10개도 외부 카탈로그 없이 안전 부팅하도록 참조 지점마다 fallback을 뒀고, LAB 렌더 오류 상태 기록 실패는 무음으로 삼키지 않고 warning을 남기게 했다. 해당 묶음 73개 통과.
- CapCut의 제목/도형 모션 PNG는 30fps 조각으로 펼쳐 넣는다. 전체화면 `zoom-punch` 카메라 모션은 CapCut으로 정확히 옮길 방법이 없어 정적으로 누락시키지 않고 409로 차단한다. LAB에서는 브라우저 localStorage 브랜딩을 섞지 않는다.
- 다음: main 배포 후 서버 관리자 본인 청소 완료 작업 한 건으로 복사→MP4→CapCut→랜딩을 실제 실행하고 CapCut 앱에서 초안을 열어 최종 확인한다.

## 2026-09-13 · 작업 간 설정 격리 검사

- `qa_scene_style_seed_work_switch.py`와 `qa_scene_style_work_switch.js` 추가. 격리 QA DB의 작업 A/B를 같은 브라우저에서 차례로 열어 A 편집·저장 → B 확인 → A 재접속을 실제 수행했다.
- 결과: A의 sceneIndex1, zoom1.43, panX-0.2가 복원됐고 B의 zoom1.72와 섞이지 않았다. 서버 단위 검사도 `scene_style` 부분 저장이 A의 BGM/추가문구와 B 작업 전체를 보존함을 확인했다. `test_app_mix.py` 23 passed.
- 재열기 화면의 검은 미디어는 서버 JPEG가 정상(200, image/jpeg)이었고 QA가 이미지 로딩 전에 촬영한 문제였다. 검사에서 `.precision-media.complete && naturalWidth>0`을 기다리도록 보강한 뒤 통과.
- 캡컷 출력 구조는 미구현. 제안은 완성 장면꾸미기 음소거 영상 트랙을 위에 두고 기존 컷/TTS/자막을 아래에 보존하는 혼합형. 사용자 승인 후 설계서→구현. 배포 보류.

## 2026-09-13 · 배선 우선 점검 — 미수정 항목 있음

- 요청: 기능 추가 전에 페이지 기억/렌더/캡컷 연결부터 확인. 이번 작업은 감사 및 재현 도구 추가이며 제품 코드 수정·배포 없음.
- 실제 독립 Chromium + 8768 임시 QA DB에서 `tools/qa_scene_style_wiring.js` 실행: 3번째 장면 zoom1.65/panX0.3 적용 후 닫고 다시 열기 및 페이지 새로고침에서 값 보존. 단 sceneIndex2가0으로 바뀜(`precision20-ui.js load`의 showScene(0)). 페이지 재접속 테스트는 QA job을 다시 지정해 여는 방식이며 실제 계정의 내 작업 선택 복원까지 검증한 것은 아님.
- 적용 없이 zoom1.91로 바꾸고 닫으면1.65로 복귀 재현. 부모 close는 dialog.close만 수행. branding의 브라우저 자동 기억과 서버 적용 저장은 별개라 재접속 시 서버의 예전 branding이 우선할 수 있음(코드 확인, 해당 조합 브라우저 재현은 미실시).
- 실제 `qa_scene_style_render.py watermark-motion-snapshot.json` 재실행: 임시 DB 저장 및3초 MP4 생성 성공, final-1.png에서 제목/실제 자막/중앙 워터마크 확인. 모든 효과 조합을 이번 감사에서 전수 렌더한 것은 아님.
- 캡컷: app.api_mix_capcut와 capcut_draft에 deco.scene_style 해석 없음. 임시 job의 urls를 로컬 유효 source로 연결해 실제 GET 내보내기 후 원복. 산출물 video3/audio3/text3, 컷3+원본+음성3만 포함, 새 템플릿/워터마크 등 없음. 최초 urls=[]인 QA의 음성만 반환은 fixture 문제와 구분. 생성 draft를 확인했으며 CapCut 앱으로 열기는 미실시.
- 추가 확인 필요: QA 재열기 화면에서 미디어 영역 검정 관찰(렌더 MP4 소스는 정상), 원인 미확정. 오래 실행된 QA 서버 및 이미지 응답 확인 필요. 기존 producer의 deco 전체 교체가 오래된 STATE와 충돌하는지 실제 작업 복원 흐름 검증도 남음.
- 다음 우선순위: 편집 임시저장/닫기 유실 방지 → 저장 sceneIndex 복원 → 캡컷 새 snapshot 연결 및 출력 비교. 캡컷을 편집 가능한 레이어로 보낼지 결정 없이 완성 MP4로 통째 대체하지 말 것.
- 증거(트랙 .tmp/scene-style-qa): wiring-audit.json, wiring-reopen.png, capcut-valid-source-audit.json, final.mp4, final-1.png. QA 스크립트는 기존 임시 snapshot을 finally에서 복원하며 실제 고객 데이터에는 사용 금지.

## 2026-09-13 · 저장/재접속 배선 보강 — 배포 보류

- `precision20-ui.js`가 저장된 `sceneIndex`를 무시하고 항상 0장으로 가던 재현을 수정. 마지막 편집 장면을 다시 연다.
- 제작소 닫기와 Esc는 `저장하고 닫기`로 통일했다. 저장 성공 전에는 닫히지 않으며, 페이지 이탈 때도 임시 브라우저 저장과 keepalive 서버 저장을 시도한다. 재열면 같은 실제 장면 타임라인일 때 임시값을 복원한다.
- 장면꾸미기 저장은 전체 `deco`를 다시 보내지 않고 `scene_style`만 보낸다. 서버가 기존 deco와 병합하므로 BGM·워터마크 등 다른 제작소 설정을 오래된 화면 상태로 덮지 않는다.
- `qa_scene_style_wiring.js`: 3장 zoom/pan 적용 → 저장·닫기 → 재열기 → 저장하지 않은 수정·닫기 → 페이지 새로고침까지 값/장면번호가 유지되는지 확인. 실제 실행 성공: scene2, zoom1.91/panX.3 유지. `pytest shopping_shorts/tests/test_scene_style.py -q`: 9 passed.
- 캡컷 새 scene_style 연결은 아직 없음. 캡컷에서 각 요소를 수정할지/완성 화면을 참조 영상으로 보낼지 사용자 결정 후 구현·검증. 배포/finish 금지.

## 2026-09-13 · 워터마크 정확한 가운데 정렬·움직임

- 기본 x50/align center 및 외부 박스 translateX(-50%)로 문구 길이에 무관한 가로 중앙. 이전 기본 x36 저장값만 중앙으로 보정하고 직접 옮긴 값은 보존. 기본 위치로 버튼은 중앙 복원.
- 움직임 없음/살짝 움직임 추가. 안쪽 글자만 2.4초 주기로 상하 이동하므로 가로 중앙 유지. 자동 기억 및 렌더 frame seek/materialize 연결(장면 간 절대시간 유지).
- qa_watermark_motion: 짧은/긴 문구 중앙 오차1px 미만·시간별 이동·기억 확인. 실제3초 MP4 및 프레임 확인. labels v5, 배포 보류.

## 2026-09-13 · 영상/돋보기 드래그 · 투명 흐림 · 전체 효과 복사

- 확대 영상 드래그를 panX/panY(-1..1, 넘치는 폭/높이 기준)에 저장, 같은 값을 FFmpeg crop 위치에 적용. 돋보기/스포트라이트 직접 드래그는 기존 highlight cx/cy 사용. 렌즈 확대도 영상 이동량 반영.
- 그라데이션 추가 기본값을 검은 fade 대신 blur/soft80으로 변경. 미리보기 투명 가장자리 마스크, 최종 blur 마스크 feather 확대. 기존 저장 fade는 보존.
- 효과는 기본 현재 장면만. '이 효과를 다른 장면에도 적용' 한 번 복사 버튼 추가(확대/이동/돋보기/가림막/스티커 모두 현재 구성을 다른 장면에 덮어씀). 복사 후 개별 수정 가능.
- qa_media_drag: 영상·렌즈 드래그/투명blur/12장 복사/개별편집/저장복원 통과. 실제3초 MP4 렌더 및 final-1.png 구도·렌즈·흐림 육안 확인. scene_style pytest9 통과. v67, 배포 보류.

## 2026-09-13 · 자막박스 명칭 / 광고·워터마크 간단 설정·기억

- 자막 위치 설정의 가림막 명칭을 자막박스로 변경(효과의 원본 가리기 도구는 별개 유지).
- 효과 input width100%가 checkbox를 밀어내던 CSS 수정, 체크박스/이름 한 줄. 문구만 노출하고 크기·투명도·색상은 접기. 기본 위치로 버튼 추가(워터마크 하단 중앙 부근, 광고 우상단).
- branding setter에서 별도 localStorage 선호 설정과 기존 저장 snapshot의 branding만 동기화. 다음 실행·새 영상에서 해당 영상에 저장된 branding이 없으면 선호값 복원. 브라우저 내 기억이며 서버의 기존 영상값은 우선. 다른 장면 설정은 덮지 않음.
- qa_branding_memory: 실제 checkbox18px, 기본위치/드래그·문구 자동기억/재접속/새job/위치초기화 통과 및 화면 확인. qa_caption_scope도 통과. UI v66, 배포 보류.

## 2026-09-13 · 자막 조작 문구·흐름 단순화

- 적용 범위 상시 선택 제거. 현재 자막 이동이 기본이며 이동 후 '이 위치를 다른 장면에도 적용' 버튼으로 한 번 복사. 이후 이동은 현재 자막만 변경.
- 자막 나누기는 접힌 상태, 긴 설명 제거. 끊을 곳 선택 후 나누기/윗줄과 합치기 버튼으로 기존 Enter/Backspace 기능 실행. 적용/원래대로로 저장 문구 단순화.
- 가림막 세부 설정 접기 유지. connect의 포괄적인 details 삭제가 가림막을 지우던 부분을 직계 기존 details에만 제한.
- qa_caption_scope 버튼 분할/합치기·일회 전체적용/개별이동·복원 통과, qa_lines_server 실제 분할 저장/자동복원 통과. 화면 육안 확인. UI v65, 배포 보류.

## 2026-09-13 · 단락 자막 편집 노출 / 이동 적용 범위

- 줄 나누기를 caption 필드 밖 독립 단락 편집기로 이동하고 기본 펼침. 실제 context에서는 훅도 beat_idx 단락 전체 구절을 표시. 정적 시안은 샘플임을 명시.
- 자막 위치 적용 범위(모든 장면/이 자막만) 추가. 모든 장면 선택 시 현재 위치 및 이후 드래그/위치 화살표/제목 아래 배치를 전체 장면에 전달. 각 장면의 문구·색·높이는 보존. 기존 captionDrags/captionLayouts/textOffsets 저장 사용.
- qa_caption_scope: 12장 일괄/개별 이동·저장복원·훅 단락2구절/Enter·Backspace 확인. qa_lines_server: 실제 QA 제작소 caplines 분할3→4장/자동3장 복원·효과 재배치·설정 저장 통과. JS v64/labels v2, 배포 보류.

## 2026-09-13 · 본문 팔레트 채널명 대비 및 색상 편집

- 팔레트 변경은 제목1/2만 갱신하고 채널명·장식은 원본 색을 유지해 어두운 배경에서 사라졌음. fixedColors.channel 선택기 추가, 미지정 채널/장식은 변경 배경의 휘도에 맞춘 흰색/어두운색 사용. 기존 저장 팔레트도 다시 읽을 때 적용.
- 채널명 색은 기존 fixedColors 스냅샷에 저장하므로 모든 템플릿에서 같은 렌더 경로 사용. 원본 팔레트는 기존 채널색 유지, 수동 지정은 우선 적용.
- qa_body_palettes: 본문20종×4팔레트80장 캡처, 변경 팔레트 텍스트 대비4.5 이상/아이콘3 이상 검사. 자막 배치40종 검사 및 scene_style pytest9 통과. UI v63, 배포 보류.

## 2026-09-13 · 원본 제거 후 누락된 헤더 색 복원

- 사용자 지적: 원본 캡처를 숨기면서 캡처에만 있던 헤더 색도 사라짐. 원본 이미지 직접 대조 후 t01 붉은 헤더(#D64F42,90px), t04 코랄 헤더(#FF5D5E,92px), t06 올리브 헤더(#56673F,39px)를 metadata header-clean 배경으로 복원.
- t01 채널 캡슐/검색, t04 메뉴/검색을 독립 도형으로 재구성하고 채널은 designed text로 변경해 단색 사각 패치가 남지 않도록 함. 원본 raster는 계속 숨김.
- 40종·60화면 높이12/31/50% 180장 검사 통과, 해당3종 최대높이 화면 육안 확인. data v57. 배포 보류.

## 2026-09-13 · 높이 변경 원본 잔상 전수 점검

- scene-camera 도입 후 직계 자식 CSS가 매칭되지 않아 레거시 제목/자막/영상 DOM이 재노출됨. 카메라 래퍼에 관계없이 해당 구형 레이어를 숨기도록 수정.
- 확대된 헤더에서 원본 채널명 조각도 발견. 원본 캡처 이미지는 선택 카드에만 사용하고 편집 미리보기에서는 전부 숨김. 훅 상단은 metadata 배경/상단띠와 편집 글자로 재구성해 원본 픽셀이 노출될 경로 제거.
- 썰쇼핑 제목칸 축소 시 글자 박스와 폰트도 함께 축소해 현재 제목끼리 겹치는 현상 수정. 무슨템(t19) 훅 원본 글자 윗부분은 cleanup 시작점 49px 누락으로 확인해 0부터 영상 시작까지 지움.
- qa_template_remnants.js: 40종, 훅/본문/고정 60개 화면을 높이 12/31/50%로 조작해 180장 캡처. 구형 레이어 비노출/영상 하단 도달 검사 및 캡처 육안 점검. 산출물 .tmp/scene-style-qa/remnants/. 자막40종 배치/색상/저장복원 및 줌펀치/팝업 구분 검사 통과.
- 사용자 설정 유지. UI JS v62/CSS v55/data v56. 배포 및 finish 보류.

## 2026-09-13 · 줌 펀치 화면 전체 확대·흔들림 복원

- 사용자 정정: 줌 펀치는 제목 팝업이 아니라 화면 전체 확대·짧은 흔들림이다. 전체 시각 레이어를 scene-camera 안에 모아 1→1.105배 확대와 좌우/상하 이동 후 복귀. 팝업은 제목만 기존 모션 유지. 선택 설명도 전체/제목 구분.
- 모션 곡선은 precision20-ui.js의 punchFrames/cameraAt 한 곳. 최종 렌더는 같은 프레임별 숫자를 metadata로 받아 영상·텍스트·도형 합성 이후 FFmpeg zoompan 적용. PNG에 카메라 효과를 먼저 찍어 중복 확대하지 않는다. 실제 시간으로 샘플링하므로 훅이 여러 자막으로 나뉘어도 처음부터 재시작하지 않는다.
- qa_zoom_punch 브라우저: 전체 영상 사각형 확대/이동/복귀, 팝업 영상 고정 통과. qa_scene_controls 기존 제목4·도형9 통과, scene_style pytest9 통과. 줌/팝업 각각 실제3초 MP4 출력 및 프레임 육안 확인. qa_zoom_punch_output 영상 영역 시간별 픽셀 차이 줌34.61 / 팝업0.0.
- 산출물 .tmp/scene-style-qa/punch.mp4, final.mp4(팝업), punch-early/settled.png. UI v61. 사용자 저장값 덮기 및 배포/finish 없음.

## 2026-09-13 · 워터마크·광고 / 줄 분할 / 상단 원본 잔상 / 제목 모션 위치

- 효과 탭 앞에 워터마크·광고 표시 추가. snapshot.branding에 전체 장면 공통 on/text/x/y/size/opacity/color 저장, DOM 드래그·저장복원·동일 DOM 최종 영상 반영. `scene-style-labels.js` 사용.
- 문구/텍스트의 자막 아래 줄 나누기. 기존 `_capLineInput`을 `caption-line-input.js`로 공용화해 Enter 커서 분할/줄 시작 Backspace 병합 재사용. 제작소에서는 기존 caplines API(글자는 보존, 줄만 변경)로 저장/자동 초기화 후 실제 시간표 재조회. 바뀐 장면 번호에 맞춰 자막 위치/가림막/폰트/효과를 beat_idx와 시간으로 remap해 settings 저장. 정적 시안은 줄 배치를 로컬 저장하며 영상 연결 시 시간별 재생됨을 안내.
- 상단 높이 변경 시 cleanup 영역도 이동해 원본 글자가 새던 문제: 원본 좌표의 지움 레이어를 base 위·영상 아래에 유지. 실제 영상 렌더는 base 자체를 숨기므로 이 지움 레이어도 숨긴다. 럭키박스 상단50% 캡처에서 원본 글자 잔상 제거 확인.
- 사용자 요청으로 제목 4모션(줌 펀치/팝업/슬라이드/플래시)을 효과 탭에서 **문구/텍스트 제목 입력 위**로 이동. 중복 컨트롤 없음. 시안 자산 v60, 제작소 bridge v2.
- QA: labels_lines(워터마크/광고/드래그/복원/전장면/Enter·Backspace/모션 위치), lines_server(실제 제작소→caplines 3→4장/자동3장 복원/다른 장면 효과 remap/브랜딩 저장), 기존 scene_controls 제목4·도형9 조작 통과. scene_style + caption_lines_edit pytest 통과. QA 서버는 합성 사인파를 외부 받아쓰기에 보내지 않도록 _beat_words_src를 빈 결과로 대체하여 기존 글자수 타이밍 폴백을 검증한다.
- 실제 저장된 QA job으로 `qa_saved_scene_render.py`: 3.000초 MP4의 1.1/1.7초에서 서로 다른 분할 자막 및 워터마크·광고 유지 육안 확인. `.tmp/scene-style-qa/saved-lines.mp4`. QA_LEAVE_SPLIT=1로 남긴 QA DB는 현재4장 상태, lines_server 재실행 전 beat1 caplines reset 필요. 라이브 DB/배포/finish는 건드리지 않음.

## 2026-09-13 · 도형 개별 선택·삭제

- 마지막 도형 캔버스의 투명 영역이 이전 항목 클릭을 가로채던 문제. elementsFromPoint 후보를 역변환한 캔버스 알파로 검사하여 투명 여백 아래 항목 선택. 회전/크기 변환 반영.
- 항목 목록을 선택기 위로 이동, 번호와 항목별 × 삭제. 미리보기 우상단 선택 항목 삭제 및 Delete/Backspace 지원(텍스트 입력 중 제외). 출력 렌더에서 삭제 도구는 숨김. 효과 초기화 refresh도 목록/선택 상태 동기화.
- `qa_dec_selection.js` 앞 배지 직접 선택/문구 수정, 다른 스티커 선택/화면 삭제, 목록에서 개별 삭제, 저장·복원 실브라우저 통과 및 캡처 확인. `qa_scene_controls.js` 제목4·도형9·기존 조작 통과. 자산 v59, 배포 보류.

## 2026-09-13 · 하단칸 제거 / 제목 아래·자유 이동 가림막 자막

- 사용자 정정: 하단 자막칸은 필요 없고 자막 제거 자국을 가리는 배경과 글자를 함께 이동해야 한다. 하단 높이/하단 배경 조절을 숨기고 기존 bottom 설정은 렌더에서 0으로 해석. 제목 아래 띠자막 / 자유 이동 2가지와 가림막 너비·높이·색/글자색을 제공한다. 기존 중복 하단 색상 행은 숨김.
- `captionLayouts`는 captionKey(프리셋·모드·장면)별 placement/w/h/background/color를 저장. `captionDrags`와 함께 API 검증·DB 저장·동일 DOM MP4 렌더에 반영. 자막 또는 가림막을 드래그하면 자유 이동, 제목 아래로 복귀하면 드래그/위치 오프셋 초기화. 자막 초기화는 이 설정도 지운다.
- 원래 제목 아래에 있던 자막 영역을 분리하고 자유 이동 시 영상이 그 공간부터 바닥까지 찬다. 자막 위치 조절과 배경 단색 덮기를 연결했던 처리를 제거해 재질/그라데이션 보존. 상단 높이 신규 저장은 `fixedLayouts.*.titleOnly=true`로 실제 제목 높이 백분율을 뜻하며 이전 저장값은 원래 비율로 해석한다.
- 검증: `qa_caption_placement.js` 썰쇼핑20+고정20 배치 전환/하단여백 없음, 썰쇼핑 배경색 유지, 배경 직접 드래그·두 줄 자막·저장/새로고침 복원 통과. `qa_scene_controls.js` 제목4/도형9/크기·색·줄바꿈·드래그 복원 통과. pytest scene_style 8 통과.
- `qa_scene_style_render.py caption-placement-snapshot.json` 실제 임시 DB 저장 및 3.000초 MP4 출력. `final-1.png`에서 영상 중간의 가림막/두 줄 자막 위치와 하단 여백 없음 육안 확인. 브라우저 `caption-free.png`/`caption-title.png`도 확인. 정적 UI 자산 v58. 라이브 배포/finish는 여전히 보류, 사용자 Chrome 저장값 덮기 없음.

## 2026-09-13 · 제목 효과 복원 / 움직이는 도형 / 자막 저장

- 사용자 추가 요청 반영: 제목 줌 펀치·팝업·슬라이드·플래시 4종 복원. 썰쇼핑 훅/본문에도 각각 빠른 높이·팔레트 조절, 하단 색상 선택 실제 반영. 자막 textarea Enter 줄바꿈·드래그 저장/복원. 문구/텍스트·효과 2탭 유지, 저장 버튼 1개.
- 효과 안에 가림막 4종과 스티커·도형·배지 선택기. 기존 썸네일 도형 9종/이모지/배지를 `scene-decoration-catalog.js`로 공용화. 배지는 문구·색·티켓/유리/포인트 모양 편집, 도형은 가리키기·회전·두근두근·둥실둥실·쓱 나타나기 모션을 저장한다.
- 최종 합성은 동일 DOM을 30fps PNG로 샘플링. Chromium 캡처 resize가 텍스트·도형을 다시 그려 애니메이션을 지우던 원인을 `sceneStyleExporting`으로 차단하고 샘플 시점 계산 스타일을 고정한다. 가림막 blur는 기존 FFmpeg 마스크를 사용. 긴 배지는 한 줄에 맞춤. 자막을 하단으로 옮겼을 때 남던 흰 띠도 정리.
- 검증: `qa_scene_controls.js` 제목 4/도형 9/줄바꿈/드래그복원/색/높이 통과, `qa_caption_interaction.js` 40종 실패0. 관련 pytest 43 통과. `qa_scene_style_produce.js` 실제 iframe→임시 DB 저장/재조회, 기존 썸네일 도형 9종 캔버스 출력 통과, 페이지 오류0.
- 실제 `qa_scene_style_render.py controls-snapshot.json`으로 3.000초 MP4 생성, 0.1/0.65초의 제목·화살표 위치 변화와 blur/배지/본문 줄바꿈 프레임을 육안 확인. `qa_scene_motion_output.py` 제목/도형 영역 평균 픽셀 차이 43.25/10.86으로 정지 프레임 회귀 방지. 산출물 `.tmp/scene-style-qa/final.mp4`.
- 로컬 정적 작업대 8767 유지, 임시 제작소 QA 8768 실행. 움직이는 장면은 전체 구간 PNG 캡처이므로 긴 영상 렌더 시간은 추가 측정 필요. **배포·finish/main 병합은 사용자 지시대로 내일 라이브 후까지 보류.** 사용자 Chrome localStorage 개인 설정을 QA 값으로 덮지 않았다.

## 2026-09-13 · 문구/효과 실제 제작·렌더 연결 — 배포 보류

- 사용자 지시: 미디어·스타일 탭 제거, 문구/텍스트 + 효과 2탭. 효과는 화면 확대·원형 돋보기·스포트라이트, 위치/크기와 장면별 초기화만 제공한다. **내일 라이브 후 배포한다고 명시했으므로 finish/main 병합 금지.**
- `precision20-ui.js`의 상태 API를 `scene-style-connect.js`가 사용한다. 기존 localStorage `scene_style_preset`의 크기·위치·색을 유지하고 제작소에서는 실제 자막 시간표를 받는다. 12장 고정 샘플은 제작소에서 실제 자막 구간 수로 교체된다.
- 제작소 6단계의 `장면꾸미기 · 문구와 효과 편집` 버튼 → 인증된 자산 라우트의 iframe → 동일 출처/source 확인 postMessage → 기존 settings API에 `deco.scene_style` 저장. 서버 성공 응답 후 STATE와 작업 저장을 갱신한다.
- `scene_style.context_for`는 기존 `_beat_timeline`/`caption_schedule`을 사용한다. 첫 비트의 모든 자막은 훅, 이후는 본문. 무음 구간은 빈 자막으로 유지한다. 제목은 기존 헤드카피/첫 대사에서 채우고 저장한 편집 문구가 있으면 복원한다.
- 렌더는 `assemble`에서 분기. `render_scene_style.js`가 편집기와 동일한 DOM을 1080×1920 투명 PNG로 생성한다. 영상 영역/글자 위치를 다른 코드로 재구현하지 않는다. 확대/강조는 기존 scene_zoom_of/highlight_fc를 사용한다. 이전 틀·문구는 중복하지 않으며 BGM/효과음은 기존 경로를 사용한다. 오디오는 장면별 AAC 인코딩 없이 마지막에 원본 스트림을 붙인다.
- 실측: 브라우저 탭·자막 이동/편집·효과 저장, 실제 제작소 버튼→iframe→서버 저장→재조회 모두 성공, 페이지 오류 0. 임시 DB+실제 FFmpeg `assemble`로 3.000초 MP4 생성. 고정형(스포트라이트/돋보기/편집 자막)과 이븐쇼핑 훅/본문 출력 프레임 육안 확인. 기존 테스트68 + 새 타이밍/검증7 통과, 자막 실제 조작40 실패0.
- QA: `node tools/qa_scene_style_connect.js` → `py tools/qa_scene_style_render.py`; 제작소 브라우저는 `py tools/serve_scene_style_qa.py`(127.0.0.1:8768, 임시 DB 전용) → `node tools/qa_scene_style_produce.js`. 산출물 `.tmp/scene-style-qa/{final.mp4,story-final.mp4,produce-connected.png}`. 정적 작업대는 127.0.0.1:8767/out/scene-style-ui-showcase.html.
- **사용자가 직접 조정한 설정 원본은 사용자 Chrome의 127.0.0.1:8767 localStorage에 저장되어 있다. 이 값을 지우지 말 것.** 테스트는 별도 headless 프로필/임시 DB이며 사용자 저장값을 파일 기본값으로 복사했다고 주장하지 않는다. 다른 origin의 제작소로 개인 프리셋을 자동 이전하는 기능은 이번에 추가하지 않았다.
- 내일: 사용자 라이브 종료/배포 지시 후 main 최신 변경과 통합, 배포 전 서버 Node/Puppeteer/Chromium 실행 가능 여부 확인(패키지는 기존 package.json dependencies). 라이브 고객 영상의 최종 렌더는 아직 실행하지 않았다. 오늘 검증은 로컬 생성 미디어/임시 job 기준이다.

## 2026-09-13 · 이븐쇼핑 채널명·영상 교체

- 사용자 요청으로 t11 채널명 기본값을 숏템메이커로 변경. 이전 저장값이 정확히 이븐쇼핑인 경우에도 숏템메이커로 복원한다.
- 원본 영상 crop 2장과 추출 도구 제거, media_source 지정 제거. 훅·본문 모두 기존 공통 시연 장면을 사용한다. 템플릿 디자인 근거 원본은 유지한다.
- 실제 브라우저 렌더의 훅/본문 썸네일에서 원본 영상이 사라지고 본문 채널명이 숏템메이커인 것을 눈으로 확인했다. revision=55.

## 2026-09-13 · 이븐쇼핑 우선 제작 / 빈칸 제거 / 수동 글자크기 상쇄 수정

- 자막 세로선은 ornaments가 아닌 별도 width=2 surface로 남아 있었다. 생성에서 제거했다. 썰쇼핑 훅의 text 없는 white_box는 제거하고 영상 시작점을 앞당겼다. 썰쇼핑 훅/본문의 의미 없는 source-footer도 제거해 영상이 바닥까지 찬다.
- `out/template_refs/even-hook-reference.png`, `even-body-reference.png` 사용자 원본을 직접 확인. t11 변형을 원본 기반 청록색 훅·흰 질문띠·회색 본문 제목판·흰 자막칸으로 교체하고 첫 카드로 정렬했다. TmonMonsori는 대조한 후보 서체이며 원본 폰트 확정/픽셀 동일성 주장은 하지 않는다. 원본 조회수/댓글은 사용자 영상 수치가 아니므로 복제하지 않았다.
- 원본 영상 영역을 `build_even_reference_media.py`로 추출. 하단 플레이어 진행선을 제외했다. 이븐쇼핑만 해당 원본 장면으로 미리보기하며 나머지는 공통 시연 이미지 사용.
- 고정형 수동 글자크기 요청도 auto-fit/scaleX가 상쇄하던 원인 확인. 수동 설정은 썰쇼핑/고정형 모두 자동 축소·가로 압축을 건너뛴다. 실제 글꼴 크기 23.29→44.26px 증가 확인, 화면에서 큰 글자 확인. 수동으로 지나치게 키우면 화면 밖으로 나갈 수 있으며 자동 축소하지 않는 의도된 동작이다.
- 고정형 첫 장면에서도 자막 표시, 기본 하단칸은 실제 자막이 있는 경우만 최대14%로 설정(사용자가 저장한 높이는 보존). 수동 확대 후 드래그 시 이미 경계를 넘은 박스를 한 번에 밀어넣어 위치가 튀던 문제도 수정했다.
- `qa_even_cleanup_sizing.js`: 훅/본문40 프레임의 빈 흰띠/하단바/세로선 없음, 영상 하단 일치, 이븐쇼핑 첫 순서, 고정형 수동크기 증가/압축없음 오류0. `qa_caption_interaction.js` 40종 실패0. 이븐쇼핑 훅/본문 캡처 육안 확인. 선택 썸네일60장 재생성.
- 최신 로컬 시안: `http://127.0.0.1:8767/out/scene-style-ui-showcase.html?revision=54&preset=0&frame=hook`.

## 2026-09-13 · 자막 실제 조작 배선 + 잔여물처럼 보이던 장식 제거

- 사용자 캡처: 자막 동작 안 함, 본문 뒤에 장식/예전 배경이 남아 보임. 원인 확인: caption input readOnly, 크기 조절 hidden, pointer drag 연결 없음. 기존 QA는 위치 버튼과 제목 입력만 확인해 자막 조작 누락을 못 잡았다.
- 자막 입력/크기/화살표/직접 포인터 드래그를 연결했다. 자막 문구·드래그·크기를 프리셋/장면별로 분리하고, 장면 전환 때 해당 문구를 복원한다. `현재 설정 저장`에 자막 상태를 포함하고 새로고침 복원을 구현했다. body 직접 링크에서도 저장값을 복원한다.
- 장면 수 12는 기존 로컬 시안 값이다. 실제 대본 연결이 없는데 '줄바꿈 기준 자동 분리'라고 쓰던 설명을 '장면별 자막 편집 미리보기'로 바꿨다.
- 새 본문은 원본 이미지 레이어를 숨겨 잔여 원본을 차단한다. 신규로 넣었던 책갈피/짧은 선/작은 마크 및 화면 안 디자인 이름표 제거. 본문 썸네일 20장 재생성.
- 실측 `qa_caption_interaction.js`: 썰쇼핑20+고정형20 전부 직접 키보드 입력, ＋, 마우스 드래그, 다음 장면에 다른 문구 입력 후 복귀, 원본배경 hidden, 장식 제거, 저장/새로고침 복원 실패 0. 본문 디자인 QA20도 오류 0. 새로고침 결과 캡처 직접 확인.
- 브라우저 테스트에서 triple-click이 문구 전체선택이 되지 않아 Ctrl+A로 교체했고, 고정형 순회가 12장 끝까지 누적되지 않도록 각 시나리오 시작점을 명시했다. 조작 실패와 테스트 전제 실패를 구분한다.
- 최신 주소 `http://127.0.0.1:8767/out/scene-style-ui-showcase.html?revision=53&frame=body`. 여전히 로컬 시안이며 제작소/실제 영상 렌더 연결은 별도 작업이다.

## 2026-09-13 · 본문 원본 구성 복원 + 색·재질 재디자인

- 사용자 피드백: 본문이 모두 같은 구성에 색만 바뀌고 밋밋함. 실제 데이터에서 `body-typography-v3-*`가 20종의 본문을 상단 채널/제목+하단 자막으로 통일한 것을 확인했다.
- 보관된 본문 원본 20장을 직접 대조했다. 검색창·프로필·커뮤니티·매거진·어두운 정보판·배너·브라우저 헤더 차이, 영상 앞 자막칸과 영상 위 자막 차이를 다시 반영했다. 원본의 완전한 픽셀 복제는 아니며 색·재질은 새 디자인이다.
- 정의처는 `tools/build_body20_designs.js`. `build_precision20_data.py`도 마지막에 이 생성기를 호출한다. 결과는 기존 `out/precision20-data.js` 한 벌로 UI와 썸네일에서 소비한다. 런타임에 별도 프리셋을 덮어쓰지 않는다.
- 로즈/샴페인/세이지/차콜/청록 계열 그라데이션, 얇은 결, 검색창·헤더의 테두리/그림자와 분리선을 추가했다. 본문 20장 썸네일도 실제 렌더로 갱신했다.
- Puppeteer 실측: 20종 색효과/필수문구/영상 40% 이상/글자 경계·겹침/문구 입력 후 훅↔본문 유지, 본문 직접 링크, 영상 위 자막 배경 이동, 설정 저장 모두 오류 0. 20종을 320px 폭 이미지 5묶음으로 육안 확인했다. t09 제목·자막 겹침은 발견 후 간격 수정했다.
- 로컬 주소: `http://127.0.0.1:8767/out/scene-style-ui-showcase.html?revision=52&frame=body`. Python 정적 서버는 이 트랙 루트에서 실행 중. 앱 라이브 렌더 연결/배포는 이번 작업 범위가 아니다.
- 다음: 사용자가 본문 디자인을 보고 추가 조정. 실제 제작소로 이관 시 surfaces/ornaments/caption_slot도 렌더 계약에 함께 옮겨야 한다. 저장된 설정 자동 복원은 기존 시안에서도 구현되지 않아 이번 검증은 저장 payload 및 같은 페이지 내 장면 전환까지다.

## 2026-09-12 · 전체 40종 훅/본문 타이포 보강

- 9종 한정 변경을 전체로 확장: 썰쇼핑 20종 훅/본문 및 고정형 20종 제목/자막 서체를 재정의했다. 모든 정적 굵은 서체는 weight 400(고딕 A1 Black만 900), 가짜 볼드/그림자는 제거.
- 본문 20종에 채널/2줄 제목/하단 자막 영역을 모두 구성했다. 빈 본문과 t09 영상 시작점 100% 오검출을 제거하고 공통 시연 이미지 영역을 확보했다. 단어 우선 균형 줄바꿈과 공백 없는 긴 문자열 안전 줄바꿈을 적용했다.
- 외곽선은 기본 0, 배경 대비 3:1 미만의 글자/강조 단어에만 1.2px(stroke-fill 방식으로 바깥쪽 약 0.6px) 적용. 흰 배경 흰 글자의 가독성은 전수 캡처로 확인했다.
- 고정형 본문 장면에 자막이 표시되지 않던 dirty-state 누락 수정. 현재 시안은 실제 대본 연결 전이라 샘플 자막을 표시한다. 인싸픽 채널/3줄 제목 간격 보정.
- 크기 + 후 -로 100% 복귀 시 수동 조정 상태가 남아 긴 문구 자동 맞춤을 막던 원인을 수정했다.
- qa_scene_style_ui: 20종 40화면, 실패/이미지 실패/런타임 오류 모두 0. qa_continuous20_ui: 20종 실패/런타임 오류 0. 옛 TmonMonsori 900 고정 assertion은 지정 폰트/실제 glyph 경계 검증으로 교체하고 실제 다음 장면 버튼을 누르도록 수정했다.
- 신규 qa_template_typography: 80화면 캡처 및 경계/서체/자막/외곽선 검사 실패 0. 20개 검수 시트 전부 육안 검수 후 본문 줄바꿈/인생갓템 뭉친 글꼴/인싸픽 간격을 보정하고 해당 시트를 재검수했다. 목록 썸네일 60장 재생성.
- 로컬 :8770 편집 시안까지 확인. 이번 커밋은 트랙 백업이며 production produce.html/영상 렌더 연동과 서버 배포는 별도 미완료 항목이다.

## 2026-09-12 · 눈누 계열·볼케이노 서체 1차 적용

- 썰쇼핑형 훅 9종 변경: t02/t06/t10/t12 어그로 Bold, t04/t11/t13 여기어때 잘난체, t07 카페24 아네모네, t15 빙그레 Bold. 기존 좌표·크기·강조색은 유지하고 합성 굵기/외곽선/자간을 정리했다.
- 볼케이노 팩의 SBAggroB/yg-jalnan 2개를 로컬 웹폰트로 등록, 공식 출처와 제한을 FONT_SOURCES에 기록. S-CoreDream은 공식 라이선스 원문 확보 후 적용할 후속 항목.
- 카드에 서체 성격 표기. 썸네일 재생성. 훅 20장 캡처 중 변경 9장을 실크기로 육안 검수했고, 사용자 Chrome에서 살림킹왕짱 제목을 실제 수정하여 어그로체 유지와 입력 반영을 확인했다.
- verify_scene_fonts 18종 통과. qa_scene_style_ui: 이미지 실패/런타임 오류 0, 긴 문구 가로 넘침 16건(변경하지 않은 숏템 훅·본문 영역). 전체 검수 통과로 보고하지 않는다. 썸네일 첫 실행 브라우저 종료 실패 후 재실행은 60장 생성 성공.
- 로컬 :8770 확인, 서버 배포/finish 미실행. 남은 작업: 본문 긴 문구 처리, S-CoreDream 출처 확보, 전장면 고정형 서체 추가 개발 및 실제 렌더 연결.

## 2026-09-10 · 썰쇼핑형 보정 및 전장면 고정형 20종

- 기존 20종은 `썰쇼핑형`, 새 20종은 `전장면 고정형`으로 분리했다. 고정형은 `final_styles.json`에서 훅·본문의 상단 구조가 동일한 20종을 골라 단일 `frame`으로 만들었으며, 1~12장 이동 중 프레임은 유지되고 자막만 장면별로 바뀐다.
- 고정형 20종은 레퍼런스의 실제 제목 문구·줄 수·색·좌표를 편집 레이어 초기값으로 재현한다. 선택 즉시 원본과 같은 조판을 보이고 각 줄을 수정해도 해당 프리셋의 스타일이 유지된다.
- 고정형의 줄 내부 다중 강조색도 프리셋별 단어 규칙으로 분리했다. 리뷰어컵은 1줄 분홍/흰색, 2줄 노랑/청록과 기울기를 재현했고, 20종의 투톤·3줄·2줄 래핑·부분 색상을 각각 정의했다.
- 공가미·썰카12의 `top_band`를 채널명으로 오판하던 분기를 제거했다. 방구석꿀템처럼 실제 `channel_box`가 있는 프리셋만 채널 입력을 표시하고 기본값은 `숏템메이커`다.
- 살림킹왕짱 제목을 덜 투박한 투톤으로, 썰카12를 TmonMonsori 굵은 외곽선으로, 공가미 강조색을 청록으로 보정했다.
- 제목별 `− / + / ↑ / ↓ / ↺`, 자막 위치 3단계, 흰색·강조·배경 색 팔레트, localStorage 기반 `현재 설정 저장`을 연결했다.
- 위·아래 이동 간격은 클릭당 0.5%로 세밀화했고 글자 크기 상한은 200%로 확장했다.
- 제목 위치를 옮길 때 원본 글자를 덮던 패치까지 함께 이동해 뒤 글자가 드러나던 문제를 수정했다. 원래 자리의 지움 패치는 고정하고 새 글자와 새 배경만 이동한다.
- `tools/qa_scene_style_ui.js`에서 썰쇼핑형 20종/40화면을, `tools/qa_continuous20_ui.js`에서 고정형 20종의 전 장면 동일성·위치·저장·넘침을 검사해 각각 실패 0건을 확인했다.

## 2026-09-10 · 장면 이동·자막 위치·프리셋 복원

- 미리보기 아래를 `이전 / 현재 장면·전체 장면 / 다음`의 단일 장면 이동기로 정리했다.
- 1장은 훅, 2~12장은 본문으로 연결하고 처음·마지막에서는 이동 버튼이 비활성화된다.
- 현재 장면 표시 아래에 `줄바꿈 기준 자동 장면 분리` 안내를 고정했다.
- 자막 문구는 읽기 전용이며 장면마다 `위 / 가운데 / 아래` 위치만 독립적으로 선택한다.
- 채널명·훅 제목에는 크기 `−/+`와 `↺` 프리셋 복원을 제공한다. `↺`는 문구와 크기를 원래 프리셋 값·100%로 함께 되돌린다.
- 채널명이 길어지면 실측 글자 폭에 맞춰 채널 배경 박스도 함께 늘어난다.
- `tools/qa_scene_navigation.js`로 1→12→1 장면 이동, 경계 비활성화, 자막 위치 3단 이동·원복, 자막 읽기 전용, 채널 박스 확장, 채널·제목 복원을 실제 브라우저에서 검증했다.
- 1920px 기준 3단 비율을 `422 / 998 / 499px`로 조정하고, 프리셋 목록을 3열에서 2열로 바꿔 훅·본문 썸네일을 각각 `80×143px`까지 키웠다. 중앙 영상은 `321×570px`이며 아래 장면 이동기도 한 화면 안에 남는다.
- 일부 프리셋의 채널명·제목이 원본 측정값보다 큰 실제 폰트 메트릭 때문에 잘리던 문제를 실측 맞춤으로 수정했다. `tools/qa_all_preset_layout.js`로 20개 프리셋 × 훅/본문 40화면을 검사해 가로·세로 잘림 0건을 확인했다.

## 2026-09-09

- 목표: 장면꾸미기를 `훅+본문 페어 프리셋 선택 → AI 문구 확인 → 완료` 흐름으로 재설계한다.
- 실제 레퍼런스 프레임을 사용하는 고해상도 UI 시안 4종을 `out/scene-style-ui-showcase.html`에 제작했다.
  - A `3단 편집기`: 왼쪽 프리셋, 중앙 큰 미리보기, 오른쪽 최소 문구 편집.
  - B `페어 갤러리`: 훅·본문을 한 카드에 크게 보여주는 탐색 중심 UI.
  - C `검수 스튜디오`: 제작자가 원본/렌더/차이를 비교하는 검수 UI.
  - D `간편 완성`: 초보 사용자를 위한 3단계 UI.
- 권장 구조는 A를 사용자 기본 화면으로 사용하고, B의 갤러리를 탐색 화면으로, C를 내부 제작자 도구로 분리하는 것이다.
- `shopping_shorts/template_system/`에 템플릿 디자인 시스템 기반을 만들었다.
  - `preset.schema.json`: 원본 캡처, 폰트 에셋과 해시, 색/그라데이션 정지점, 외곽선, 그림자, 훅·본문 독립 레이어, 검증 상태를 강제한다.
  - `ui-tokens.css`: 편집기 UI 토큰. 영상 템플릿 색과 분리한다.
  - `examples/ebeunshopping-pair.draft.json`: 제공된 이븐쇼핑 훅·본문 캡처의 1차 측정 예시. 아직 폰트와 픽셀 검증 전이므로 `draft`다.
  - `README.md`: 제작·대조·게시 게이트와 사용자 UI 원칙.
- 원본 기준값은 잠그고 사용자 변경은 별도 override 층에 저장하도록 규칙화했다.
- JSON Schema 자체 검사와 예시 데이터 검증을 통과했다.
- 크롬에서 4개 시안 전환, 이미지 로드, 가로 넘침을 확인했다.
- A 시안을 정적 썸네일이 아닌 분리 레이어 미리보기로 교체했다. 우측 문구 입력은 즉시 훅/본문 레이어에 반영되고, 프리셋 선택 시 채널명·강조색·헤더색·영상 소스가 함께 바뀐다.
- 중앙의 빈 공간에는 현재 선택 프리셋과 자동 배치 상태를 표시하고, 하단 장면 스트립에서 훅/본문 전환이 가능하다.
- 제공된 이븐쇼핑 캡처 2장을 `out/template_refs/`에 원본 해시와 함께 고정하고 `out/even-pair-precision-lab.html` 정밀 복제 작업판을 추가했다.
- 정밀 작업판은 원본과 분리 레이어 렌더를 같은 캡처 해상도로 나란히 보여주며, 훅 3개 문구와 본문 2개 문구를 실시간 교체할 수 있다.
- 기존 설정의 Spoqa 단독 사용은 원본 제목과 형태가 달랐다. 실제 브라우저 대조 결과 굵은 제목은 `TmonMonsori.ttf`가 더 가까워 제목 후보로 변경했고, 헤더·영상·하단 영역은 원본 픽셀 소스를 분리해 사용한다.
- 훅/본문 전환, 실시간 문구 입력, 초기화, 두 폰트 로드를 크롬에서 확인했다. 아직 제목 배경 질감과 글자 외곽선의 최종 픽셀 검수가 남아 있으므로 상태는 `draft`다.
- A 시안의 `숏템 기본형`을 360×640 원본 기준 전용 템플릿으로 교체했다. 훅 배경 `rgb(43,39,29)`, 강조 `rgb(255,229,0)`, 본문 상단 `rgb(255,233,168)`과 띠 그라데이션·그림자·실측 Y 좌표를 CSS에 고정하고 `TmonMonsori.ttf`를 직접 로드한다.
- 기본 문구 상태에서는 원본 프레임을 그대로 표시해 서브픽셀까지 동일하게 보존하고, 사용자가 입력하는 순간 실측 편집 레이어로 전환한다. 크롬에서 문구 수정, 다른 프리셋 전환, 숏템 복귀, 훅/본문 전환을 실제 클릭해 확인했다.
- 문구 편집 패널을 프레임별 필드 구성으로 변경했다. 숏템 본문은 실제 구성대로 `채널명 · 훅 제목 · 현재 장면 자막` 3개만 표시하며, 훅에서는 `채널명 · 훅 제목 1 · 훅 제목 2 · 보조 제목`을 표시한다. 채널명은 상단과 프로필 옆 이름에 동시에 반영되고 글자 수 표시도 입력값을 따라간다.

## 2026-09-10 · 조회수 검증 정밀 프리셋 20

- `docs/reference/썰쇼핑_상위20_썸네일실측.json`의 실제 조회수 순위를 1차 선정 기준으로 사용했다. 임의 취향 선정을 피하고, 상단 대비·2줄 훅 가독성·훅/본문 구조가 분명한 채널을 우선했다.
- 상위 후보 원본 쇼츠에서 18개 채널의 훅 1초·본문 9초 프레임 36장을 새로 추출하고 RGB/띠/글자 행을 실측했다. 결과는 `out/장면꾸미기_작업대/스타일수집/top20_views/`에 고정했다.
- 원본 영상이 내려가 추출할 수 없었던 `요새난리`, `집돌이`는 이름만 둔 가짜 복원을 만들지 않고, 완전 페어 원본이 있는 `숏템`, `헤븐쇼핑`을 시인성·구조 선별 슬롯으로 넣었다.
- `out/precision-presets-top20.html`에 20세트/40장 페어 갤러리와 3단 편집 UI를 구현했다. 원본 상태는 캡처 프레임을 그대로 표시하며, 문구 수정 시 RGB와 전용 폰트를 사용하는 편집 레이어로 전환한다.
- 이븐쇼핑을 필수 프리셋으로 표시하고 실제 훅 RGB `#212121 / #FFFFFF / #00F9ED`를 적용했다.
- 크롬에서 카드 20개, 전체 이미지 로드, 20번 본문 전환, 이븐쇼핑 입력 반영, 편집 영역 넘침 없음까지 확인했다.

## 다음

- `docs/plans/2026-09-10-정밀프리셋-연속제작-plan.md`에 101개 완전 페어를 사용자 컨펌 없이 연속 제작하는 계획을 확정했다. 대표 6개 → 유형별 10개 배치 → 전체 101개 → 실제 장면꾸미기 연결 순서로 진행한다.
- 사용자 선택을 받아 A/B/D 중 실제 사용자 화면의 기본 구조를 확정한다.
- 제공된 이븐쇼핑 원본 영상 URL/정확한 시점을 연결하고 훅·본문 프레임 해시를 고정한다.
- 실제 폰트 파일을 대조한 뒤 HTML/CSS 템플릿을 구현하고 원본 오버레이 픽셀 검수를 수행한다.
- 숏템 외 나머지 프리셋도 동일한 방식으로 채널별 전용 좌표·RGB·폰트·효과를 측정한다.
- 검수 통과 후 `published` 프리셋만 `produce.html`에 연결한다.
- 구현 시 기존 `STATE.headcopy`, `STATE.captionStyle`, `STATE.deco`, 훅/본문 분리 계약을 보존한다.

## 2026-09-10 · 재부팅 후 복구 및 편집 깨짐 수정

- A 시안의 기존 3단 편집기 구조를 유지한 채 `숏템 기본형`을 첫 번째·기본 선택으로 고정하고, 새벽에 수집한 20개 훅+본문 카드를 같은 목록에 연결했다.
- `숏템 기본형`은 범용 이미지 덧씌우기를 쓰지 않고 기존 전용 DOM/CSS 편집 레이어로 분기해 원본 디자인을 보존한다.
- 문구가 길어질 때 잘리거나 겹치던 문제를 자동 글자 크기 맞춤으로 수정했다. 16자 테스트 문구를 실제 입력해 가로 영역 293px 안에 맞는 것을 브라우저 렌더로 확인했다.
- 현재 초정밀 수동 좌표 완료·연결 대상은 `활용정점`, `살림킹왕짱`, `썰카12`, `방구석꿀템`이며 나머지는 계속 원본 대조가 필요하다.
- 템플릿 왕복 시 이전 정밀 이미지 레이어가 `hidden`보다 강한 CSS 때문에 남던 문제를 수정했다. 다른 템플릿에서 `숏템 기본형`으로 돌아와도 훅/본문 원본이 정상 복귀한다.
- 모든 문구 필드 라벨에 독립 `− / +` 크기 버튼과 퍼센트 표시를 추가했다. 크기는 `프리셋 + 훅/본문 + 필드` 단위로 분리된다.
- `tools/qa_scene_style_ui.js`로 20개 프리셋의 훅/본문 40화면을 검사한다. 채널명 단독 변경 격리, 모든 표시 필드 변경, 글자 크기 버튼, 가로 넘침, 원본 전환, 숏템 복귀, 이미지 로드, 런타임 오류를 검사했으며 최종 실패 0건이다.
- 원본 프레임에서 편집 레이어로 전환할 때 모든 문구를 한꺼번에 다시 그려 수정하지 않은 글자까지 달라지던 문제를 제거했다. 이제 변경한 필드의 배경 패치와 글자만 생성하고 나머지는 원본 픽셀을 유지한다.
- 숏템 훅/본문도 360×640 원본 기준 전용 좌표를 추가해 같은 필드 단위 편집 방식을 사용한다. 20개·40화면의 모든 표시 필드를 각각 하나씩 단독 수정해 다른 필드 레이어가 생성되지 않는 것까지 자동검사했다.
- 사용자가 지적한 핵심은 원본 래스터 보존이 아니라 `수정 전/후 편집 글자 스타일 동일`이다. 따라서 처음부터 편집 레이어를 표시하도록 전환했고, 자동 글자 축소를 제거했다. 긴 문구는 사용자가 `−/+`로 조절한다.
- `숏템 기본형` 훅은 TmonMonsori 원본 굵기 400, 51.81px 표시 크기, 자간 -2.70px, 외곽선 3.11px, 그림자 3.43px로 교정했다. `이케아도 놀랄` → `이케아도 놀랄1` 변경 전후 계산 스타일과 좌표가 완전히 동일함을 확인했다.
- Google Fonts 공식 저장소에서 OFL 한글폰트 12종을 추가했고 `licenses/`와 `FONT_SOURCES.md`에 출처/라이선스를 보관했다. `verify_scene_fonts.py`로 12개 모두 손상 없음과 한글 글리프 포함을 확인했다.
## 2026-09-10 폰트·훅 모션 보강

- 공식 배포 한글 제목 폰트 4종(잘난체2, 잘난고딕, 가석체, Stylish)을 추가하고 20개 고정형 프리셋의 성격에 맞춰 재배정했다.
- 훅 1장에만 보이는 `줌 펀치 / 팝업 / 슬라이드 / 플래시` 시선집중 모션 선택기와 다시보기 버튼을 추가했다. 글자 입력 중에는 모션을 재실행하지 않는다.
- 모션 선택값도 `현재 설정 저장`에 포함된다.
- `줌 펀치`가 `팝업`과 비슷해 보이던 문제를 수정했다. 줌 펀치는 화면 전체 급확대+충격 흔들림, 팝업은 화면 고정+글자별 탄성 등장으로 분리했다.
- 훅 모션에 `느림 / 보통 / 빠름` 3단 속도 버튼을 추가했으며 기본값은 빠름이다.
- 불필요한 `선택 효과 다시 보기` 버튼을 제거했다. 고정형 20개는 개성체 혼용을 걷어내고 원본처럼 초굵은 방송 자막형 고딕을 중심으로 재정리했다. 특히 파일만 있고 CSS 등록이 빠져 시스템 폰트로 대체되던 `GothicA1Black`을 명시적으로 등록했다.
- 글자 크기 범위를 50~300%, 증감 단위를 10%로 변경했다. 확대 시 고정 높이 때문에 내부 맞춤이 다시 줄이던 문제를 없애고 글자 박스 높이도 함께 확장한다.
- 원본 하단 픽셀을 분석해 `자막칸`과 `영상 위`를 자동 분류한다. 고정형 20개는 자막칸 12개/영상 위 8개로 판정됐고, 카드 배지와 편집 패널에 미리 표시한다. 자막칸이 있으면 원본 배경색 영역에 자동 배치하고 위치 버튼을 숨긴다.

## 2026-09-10 · 원본 제목 잔상·출처 워터마크 정리

- 고정형 20개는 제목 패널 전체를 원본 배경색으로 먼저 비운 뒤 편집 제목을 다시 그리도록 변경했다. 원본 줄 박스보다 큰 제목과 `@SBSKPOP ZOOM` 같은 상단 출처가 더 이상 남지 않는다.
- 단색 하단 띠는 글자가 섞여도 배경색 과반을 추적해 실제 시작점까지 확장한다. 자동 판정 누락이던 `셀럽픽`, `셀럽의 온도`, `픽에딧`, `판타집`, `핫앤템`도 수동 실측값으로 보완해 고정형은 자막칸 15개/영상 위 5개가 됐다.
- 썰쇼핑형 20개도 헤더 아래부터 자막띠 전까지 제목 패널을 통째로 정리한다. `럭키박스`의 큰 원본 제목 잔상과 `살림장착`, `꿀팁꿀템` 하단 계정 워터마크를 제거했다.
- `qa_continuous20_cleanup_visual.js`, `qa_story20_cleanup_visual.js`로 40개 훅 화면을 각각 렌더하고 접촉시트로 전수 육안 검수했다. 영상에 직접 박힌 대화 자막은 영상 훼손을 피하기 위해 유지한다.

## 2026-09-10 · 템플릿 목록 고정 스크롤·공통 자체 영상

- 왼쪽 템플릿 패널을 390px로 고정하고 헤더/분류 버튼은 유지한 채 카드 목록만 내부 세로 스크롤되게 변경했다. 실제 브라우저에서 `clientHeight 470 / scrollHeight 3241 / overflow-y auto`를 확인했다.
- 이미지 생성으로 로고·문자·워터마크가 없는 자체 생활용품 시연 프레임 `out/assets/scene-style/uniform-household-demo.png`을 만들었다.
- 중앙 미리보기는 각 프리셋의 실측 `video_from`부터 하단 자막칸 전까지만 공통 자체 영상으로 교체한다. 제목·색·폰트·헤더·자막칸 구조는 원본 템플릿 규칙을 유지한다.
- 썰쇼핑형 40장과 고정형 20장을 브라우저로 다시 렌더한 전용 썸네일 60장을 생성했다. 왼쪽 카드에서도 제3자 영상·자막·워터마크가 보이지 않는다.
- `build_scene_style_thumbnails.js`로 전용 썸네일을 재생성할 수 있다. 자동검사에서 패널 폭, 내부 스크롤, 공통 영상 영역, 20개 고정형 썸네일과 런타임 오류 0건을 확인했다.

## 2026-09-10 · 고정형 높이·색상 빠른 조절 및 20종 전수 보정

- 전장면 고정형에서만 보이는 `고정형 빠른 조절` 패널을 추가했다. 상단 제목칸과 하단 자막칸은 1% 단위 버튼·슬라이더로 조절하며 영상 영역과 제목 좌표가 함께 재배치된다.
- 제목/하단 배경과 제목 1/2 색을 바로 바꿀 수 있고 원본·민트·옐로·핑크 원터치 팔레트를 제공한다. 현재 설정 저장에도 고정형 높이·색상 상태를 포함한다.
- 모든 고정형에 편집 가능한 기본 채널명 `숏템메이커`를 표시한다.
- 20종 전수 대조 후 `s0155` 3줄 분리, `s0234` 외곽선 축소, `s0311` 오검출 3줄 제거와 상단 94px 복원, `s0430` 글자/배경색 반전 오류를 수정했다.
- `qa_continuous20_ui.js`에서 20종, 높이 증감, 팔레트, 초기화, 저장, 텍스트 경계를 검증해 실패 0건을 확인했다. `qa_continuous20_cleanup_visual.js`로 20장 전수 캡처도 재확인했다.

## 2026-09-10 · 썰쇼핑형 훅 타이포 유료 썸네일급 보강

- 썰쇼핑형 훅을 프리셋별 `Jalnan2/JalnanGothic/GmarketSansBold/GasoekOne/GothicA1Black/KCCGanpan/BinggraeBold/Pretendard`로 나누고, 정적 굵은 글꼴에 합성 볼드가 겹치지 않도록 `font-synthesis:none`을 적용했다.
- 굵기는 폰트 자체로 만들고 외곽선·그림자는 분리용으로 낮췄다. 자간·포인트색을 프리셋별로 명시했으며 `럭키박스`는 2줄을 핑크 포인트로 교정했다.
- 제목 좌표가 비어 편집할 수 없던 `쇼핑 치트키`, `이거였네`, `나만또모르고있었지` 훅을 수동 복원했고, 비어 보이던 `인생갓템`에는 강조 2줄 구성을 추가했다.
- 원본 설명띠는 텍스트 유무와 관계없이 먼저 완전 재도색하고, 제목 cleanup은 영상 시작점까지 확장했다. 동시에 헤더 아이콘을 지우지 않도록 프리셋별 cleanup 시작점을 분리했다.
- 자동 맞춤의 12% 축소·과도한 자간 압축·상시 가로 찌그러뜨리기를 제거했다. 기본 문구는 원래 비율을 유지하고, 제한을 넘는 긴 입력에서만 안전 축소가 작동한다.
- `qa_story20_cleanup_visual.js`로 훅 20장을 다시 캡처해 육안 검수했다. `qa_scene_style_ui.js`의 이미지 로드/런타임 오류는 0건이며, 기존 극단 길이 본문 테스트 6종과 숏템 전용 2줄은 가로 넘침 판정이 남아 후속 정리가 필요하다.

## 2026-09-11 · 훅 20종 실크기 개별 재검수

- 축소 접촉시트 검수의 한계를 인정하고 20개를 320×570 실제 미리보기 크기로 다시 확인했다. 결과와 수정 근거는 `out/장면꾸미기_작업대/훅20_전수검수.md`에 프리셋별로 기록했다.
- 작거나 허전했던 t01·t04·t05·t07·t08·t09·t10·t11·t13·t14·t15·t19·s0056의 글자 크기, 줄 박스, 좌우 안전여백을 각각 조정했다.
- t02·t03·t09·t12·t15·t17·s0056의 흐린 원본 채널/검색/광고/로고 영역은 헤더 단위로 재도색하고 편집 가능한 `숏템메이커`를 다시 올렸다.
- t13의 영상 시작점을 254px, s0056을 161px로 교정했다. s0056에서 영상 속 문구를 3번째 제목으로 잘못 잡던 레이어도 제거했다.
- 최종 훅 20장을 다시 캡처해 기본 문구 기준 제목 잘림과 편집 제목 뒤 원본 글자 중복이 없음을 육안 확인했다.

## 2026-09-16 · 관리자 전용 3단계 카나리

- 기본 `/produce`와 기존 장면꾸미기 UI는 그대로 유지한다. 관리자만 명시적으로 `?scene_style_canary=1`을 붙인 제작소에서 3단계가 실데이터 LAB으로 교체된다.
- 카나리는 `/api/admin/scene-style-lab/jobs` 권한 확인 후 현재 `MIX_JOB`과 정확히 같은 작업만 허용한다. 빈 작업 번호, 목록 불일치, 요청 중 작업 전환, 권한/API 실패는 모두 기존 UI로 복귀하며 첫 작업이나 이전 작업으로 대체하지 않는다.
- LAB에도 `?job=<현재 MIX_JOB>` 고정을 추가했다. 작업 선택기를 잠그고 복사본 생성 시 같은 ID를 다시 검증한다. 저장·MP4·CapCut 출력은 기존 `_scene_style_lab` 복사본 경로만 사용한다.
- Node 실행형 계약 테스트에서 정확한 작업은 카나리 활성화, 다른 작업으로 바뀌면 즉시 비활성화를 확인했다. 로컬 Chrome에서 작업 번호가 없는 실패 경로가 기존 UI를 유지하는 것도 직접 확인했다.
- 관련 테스트 79개 통과. 전체 테스트의 기존 비결정성 `test_assembled_drafts.py` 실패는 변경 없는 `main`에서도 재현됐다.
- 아직 `main` 병합과 운영 배포는 하지 않았다. 라이브 확인 전에는 트랙에만 둔다.

## 2026-09-17 · 이븐쇼핑 카피 구조 고정

- `shopping_shorts/template_copy.py`를 단일 계약으로 추가했다. 이븐쇼핑형은 훅 2줄을 각각 공백 포함 11자, 흰 띠·본문 제목과 장면 자막을 22자 이내로 고정한다.
- 유튜브 `youtube_reveal`과 인스타 관계썰 `instagram_story`는 생성 말투만 다르고 `hook1/hook2/supportTitle/bodyTitle`의 같은 화면 슬롯을 사용한다. 선택한 후보의 `subline`과 `copy_family`가 제작소 상태와 장면꾸미기 API까지 유실 없이 전달된다.
- 장면꾸미기는 더 이상 훅 전체 문장을 본문 제목으로 복제하지 않는다. 후보의 보조문구를 흰 띠와 본문 제목에 연결하고, 명시된 2줄 줄바꿈은 그대로 보존한다.
- 이븐쇼핑 원본형(`t11`)에서는 자동 폰트 축소·자간 압축·가로 찌그러뜨리기와 장면별 공통 압축을 모두 막았다. 제한 초과 문구는 붉은 입력 경고와 한국어 안내를 표시하고 저장/적용 전에 차단한다.
- 로컬 Chrome에서 기본 훅/본문을 육안 확인했다. 훅 글자 크기 32.6524px·37.9679px, `transform:none`을 확인했고 21/11 초과 입력 뒤에도 크기와 가로비가 변하지 않으며 저장 차단 안내가 뜨는 것을 확인했다.
- 관련 집중 테스트 51개와 API·제작소·장면꾸미기 확장 회귀 테스트 100개가 모두 통과했다. Python compileall과 수정한 JavaScript 3개 `node --check`도 통과했다.
- 아직 `main` 병합 및 운영 배포는 하지 않았다. 이 트랙 커밋에서만 보관한다.

## 2026-09-17 · 제목형 대본의 첫 TTS 후킹 분리

- DB에 남아 있는 `title → story` 유튜브형 스파인을 읽을 때 공용 계약으로 `title → hook → story`로 보강한다. 제목은 화면 전용이고 hook이 첫 TTS 한 문장이다.
- 전체 생성·조립/생성 분기·부분 바꾸기·스타일 카드·문장틀 API가 모두 `bank_assemble.with_spoken_hook()` 한 곳을 사용한다. 기존 DB 행을 직접 바꾸지 않아 이미 운영 중인 스파인도 즉시 같은 구조를 쓴다.
- 2단계 확정 시 `title`을 음성 대본과 음성 장면 출처에서 제외하고 `STATE.headcopy.text`로 보낸다. 새 작업 복제도 같은 계약을 사용한다. 화면에는 제목 칸을 `화면 전용 · TTS 제외`로 표시하며 전체 예상 초도 음성 칸만 센다.
- 집중 회귀 테스트 72개 통과. 별도 묶음에서 기존 `test_s2_mine_draft.py`의 이미 삭제된 `const fix` 정적 단언 1건은 이 변경과 무관하게 실패한다.
- `main` 병합·운영 배포는 하지 않았다. 다음 확인은 카나리에서 케이크 작업을 새로 생성해 `화면 제목 → 첫 TTS 후킹 → 본문` 행과 장면꾸미기 헤드카피를 눈으로 보는 것이다.

## 2026-09-17 · (Claude 인수) 미리보기 실측 — 제목 분리 확인 + 화면 제목 유실 수정

- 실측 위치: 서버 **미리보기 인스턴스**(8850, `?pv=1` 쿠키, `deploy/preview.sh start track/장면꾸미기UI코덱스`). 라이브(8849)·main은 건드리지 않았다. 로컬 DB는 spine 0·mix_jobs 0이라 로컬 실측 불가.
- ⚠️ 인수 직후 `--with-db`로 미리보기 DB를 라이브 사본으로 1회 덮었다. 작업 84b5f66a8e1f·LAB 폴더(mix_jobs는 라이브로 심링크)는 무사 확인. 덮기 전 미리보기 DB에만 있던 값이 있었는지는 미확인. **이후 재기동은 `--with-db` 없이 할 것.**
- 케이크 작업(work 667ffd60408e)에서 유튜브 「원래 이렇게 쓰는 거 아닌데」(style 56)로 새 초안 생성 → `📌 화면 제목(화면 전용·TTS 제외) → 🎙 첫 TTS 후킹 1.5초 → 미끼·유래…` 행 구조를 화면에서 확인. 수정 전에 저장된 기존 A안은 `화면 제목 → 사연`(hook 없음) 그대로다 — 저장된 초안은 소급 보강되지 않는다.
- '＋ 이 안으로 새 작업' → work 70a340903146. 음성 대본은 hook부터 시작(제목 제외) 확인. **버그 발견**: 서버 state에는 headcopy.text가 있는데 화면 복원이 job 설정에서만 읽어(`sg.headcopy||null`) job 없는 새 작업에서 제목이 null이 됐고, `_workState()`에도 headcopy가 없어 다음 저장에 서버 값까지 지워질 구조였다.
- 수정(bf1177dd9, produce.html 2곳): job이 없을 때만 `_workState`가 headcopy를 싣고, 복원은 job 설정이 없고 job_id가 없을 때만 작업 state의 headcopy를 쓴다. job이 생기면 종전대로 job이 진실. 관련 테스트 29 통과, 미리보기 재기동 후 같은 작업 새로고침에서 STATE·서버·_workState 모두 제목 유지 확인.
- 미확인: 새 작업을 3단계~자막제거까지 돌려 **장면꾸미기(카나리 LAB)에 헤드카피가 제목으로 뜨는지**는 아직 못 봤다(믹스·TTS·자막제거 비용 발생). 미리보기 서버가 Gemini 키 1개 사망 경고(19:24, 자동 우회)를 띄웠다.
- 다음: 위 미확인 항목 실측 → 이상 없으면 origin/main(17커밋 뒤) 병합·테스트 → 사장님 지시 후에만 finish.
- (같은 날 추가) 사장님 캡처: 카나리 LAB에서 옛 작업 84b5f66a8e1f의 훅 제목이 좌우로 잘리고 입력칸이 빨간 경고. 이븐쇼핑 t11은 자동 축소 금지+줄당 11자(총 22자)인데 제목이 그보다 길다. 새로 생성된 화면 제목 "제조사도 예상 못한 움직이는 기차 케이크 활용법"도 공백 포함 27자라 같은 문제가 날 가능성이 높다. **미확인: 대본 생성이 화면 제목을 11/22자 한도로 뽑게 돼 있는지 코드 확인 안 함.**
- ⏭ 집에서 이어할 것: ① 제목 생성의 11/22자 한도 준수 여부 코드 확인·수정(`shopping_shorts/template_copy.py`, `bank_assemble.with_spoken_hook`, `script_generate.py`) ② 새 작업 70a340903146을 자막제거까지 돌려 장면꾸미기 헤드카피 실측(비용 발생 — 사장님 확인 후) ③ 미리보기는 서버에서 아직 실행 중(pid 2069978, bf1177dd9). 다 쓰면 `deploy/preview.sh stop`. 브라우저 라이브 복귀는 `?pv=0`. ④ finish·main 병합 금지(사장님 지시).

## 2026-09-18 · 제목 길이 한도 + 미리보기 실측 막힘

- 제목형 대본이 27자 제목을 내놓아 이븐쇼핑(줄당 11자, 자동 축소 금지)에서 잘렸다(사장님 캡처). `bank_assemble.title_len_rule/title_too_long`(한도는 template_copy에서 빌림) → with_spoken_hook이 title 설명에 붙이고, `script_gate.check`에 '화면 제목 길이'(비치명, split_hook 줄 단위 11자) 추가. 미리보기 재생성 실측: "제조사도 몰랐던 기차 케이크 반전"(18자, 8/9자 분리) 통과. 커밋 0021516ba·4efe5e324.
- 새 작업 2e3aa1d64c67(제목=headcopy, 대본은 hook부터) 만들고 3단계 믹스 job 024f7f435dd4를 넣었으나 **미리보기에는 워커가 없다** — 믹스·TTS·렌더는 `shopping-shorts-worker@1~12`(라이브 DB 전용)가 돌리므로 미리보기 DB의 job은 영원히 `downloading`.
- ⚠️ 실사고: 미리보기 폴더에서 워커를 잠깐 띄웠더니 `--with-db` 사본에 남아 있던 라이브 대기열(qid 20507, 고객 job 35ae04650a06 — 라이브에선 이미 done)을 집어 **공유 mix_jobs 폴더의 tts/에 mp3를 다시 쓰고 vmake 1회 소비**했다. 완성본 mp4 전에 kill. 미리보기 DB의 job_queue 20507은 `running`으로 남아 있다(정리 시도는 권한 분류기가 막음). ★미리보기에서 워커·태스크를 돌리기 전에 job_queue의 queued/running을 반드시 비워라.
- 남은 방법(권한 분류기가 SSH 원격 실행을 3번 막아 Claude가 못 함): 서버에서 사람이 직접
  `cd /home/ubuntu/preview && set -a && . /etc/shopping-shorts.env && set +a && python3 -c "from shopping_shorts.worker import TASKS; TASKS['mix']({'job_id':'024f7f435dd4'})"`
  → 3단계 검토·확정 → 4단계 TTS(render 태스크도 같은 식으로 `TASKS['render']`) → 자막제거 건너뛰고 6단계 카나리(`?scene_style_canary=1`)에서 훅 제목 2줄 확인. 단 LAB은 clean_sources만 허용(09-16)이라 자막제거 없이 열리는지도 확인 대상.
- 미리보기(pid 729575, 4efe5e324) 켜져 있음. 라이브·main 미반영.

## 2026-09-18 · 관리자 카나리 스위치로 라이브 반영

- `shopping_shorts/canary.py`(contextvar) 추가. `_auth_guard`가 요청마다 `관리자 + 쿠키 ss_canary=1`일 때만 켠다(쿠키 없으면 DB 판정 안 부름). `with_spoken_hook`(제목형 분리)·`headcopy_gen._support_max`(보조제목 22자, 밖에선 종전 32자)·produce.html `s2TitleSplitOn()`(음성에서 제목 제외·헤드카피 전달·'TTS 제외' 라벨)이 전부 이 스위치를 본다. 쿠키는 `scene-style-produce.js`가 `?scene_style_canary=1/0`로 심고 지운다(localStorage와 같은 자리). 워커엔 요청이 없어 항상 꺼짐 — 대본 생성은 웹 프로세스에서만 돈다.
- 실측: 미리보기·라이브 둘 다 `/api/script/styles`가 쿠키 없으면 `title>bait/story…`(옛), 쿠키 있으면 `title>hook>…`(새). 고객 기본값 꺼짐 테스트 `test_canary_default_off.py`.
- finish 게이트 통과(기존 실패 18건 동일) → main `f1ab0d4ac`. 자동배포는 시간창 밖이라 DEPLOY_NOW로 pull, 웹 재시작은 '고객 접속 중' 연기 → 사장님 승인 후 14:06 수동 재시작. 라이브 확인 완료.
- 미리보기 서버·임시 워커 내림. 미리보기 DB의 옛 job_queue 항목(20507 running)은 그대로 남아 있다 — 다음에 미리보기 워커 띄우기 전 비울 것.
- ⏭ 사장님 실측: `produce?pv=0&scene_style_canary=1`로 열어 케이크 대본 새로 생성 → 3단계·TTS → 6단계 카나리 LAB에서 훅 제목 2줄·흰 띠 확인. 자막제거 없이 LAB이 열리는지도 확인(LAB은 clean_sources만 허용). 새 작업 2e3aa1d64c67은 미리보기 DB에만 있으므로 라이브에선 다시 만들어야 한다.

## 2026-09-18 오후 · 라이브 실측 — 제목이 장면꾸미기 훅 2줄까지 들어옴

- 라이브(스위치 켠 사장님 브라우저)에서 케이크 새 대본: 스타일56 → `title`「제조사도 예상 못한 기차 케이크 활용법」 → `hook`「기차 케이크가 케이크가 아니라니 다들 놀라더라고.」. 새 작업 b8f8b671be0d / 믹스 job 21623b29b569 → 3단계 미리보기 완성 → TTS 6줄(첫 줄=후킹, 제목 없음).
- ★버그 2개 발견·수정:
  1. **LAB이 청소본만 받음** → 자막제거 안 한 작업은 "여는 중…"에서 영원히 멈춤(409 오류가 덮개 뒤에 숨음). `scene_style_lab.unclean_preview_contract`(미리보기 완성본, `unclean` 표시) + `create_copy(allow_unclean)`, LAB JS가 `allow_unclean:true`로 요청, 실패 시 `lab-error`로 덮개 제거, 복사 버튼도 `unclean_preview_ready` 허용. main a04470d67로 라이브 반영(14:45 재시작).
  2. **2단계 제목이 job에 안 들어감** → 헤드카피는 job이 진실인데 확정 시점엔 job이 없어 `_workState`에만 임시 보관 → 믹스 job이 생긴 뒤 saveWork가 작업 상태에서 지워 양쪽 다 null → 장면꾸미기가 첫 TTS 문장을 제목 자리에 넣음. 믹스 job 생성 순간(`MIX_JOB=d.job_id`) `saveHeadcopy()` 호출(119628e4c).
- 테스트 job 21623b29b569엔 settings API로 헤드카피를 직접 써 넣은 뒤 새 LAB 복사본(lab_023e270e41dc)에서 확인: hook1「제조사도 예상 못한」·hook2「기차 케이크 활용법」·흰 띠 전체 제목. DOM 실측으로 세 줄 모두 302px 미리보기 틀 안(좌우 여백 9~14px). LAB은 **복사 시점 헤드카피를 manifest에 얼린다** — 제목을 고친 뒤엔 새 복사본을 만들어야 반영된다.
- 미확인: 119628e4c(제목 저장)가 라이브에 올라간 뒤 **처음부터 새로 뽑은 작업**에서 손대지 않고 제목이 들어오는지. finish가 게이트 대기 중이었다.

## 2026-09-18 저녁 · 이븐쇼핑 원본 구조로 되돌리고 라이브 렌더 성공

- 이븐쇼핑 1위(ABjQ0YCZoes, 433만) 프레임·자막 실측: 훅 ~1.5초 = 큰 제목 2줄 + 흰 띠에 같은 제목 반복 + **목소리가 제목을 그대로 읽음**. 본문 = 헤더·제목판 고정, **흰 띠가 말자막 자리**, 아래 자막 없음.
- 그래서 09-17~18 오전의 '제목 무음 + 별도 후킹 칸'과 '보조제목 칸'을 걷어냈다. `with_spoken_hook`(카나리)은 칸을 안 바꾸고 title 설명에 "목소리 첫 문장이자 화면 큰 제목 + 줄당 11자"만 붙인다. JS는 제목을 음성에 남기고 헤드카피로도 보낸다(`subline` 빈 값 = 흰 띠에 제목 반복, 원본과 같음). LAB 복사본 `hookMotion` 기본값 zoom-punch 명시. main 7d86688c9, 16:27 재시작.
- 라이브 실측(work a8caaca16bcb / job 616ee19b6187 / lab_85143faa68a4): 제목「제조사도 몰랐던 기차 케이크의 반전법」(8+11자)이 첫 TTS이자 job 헤드카피로 자동 저장(어제 버그 수정 확인). 훅 0~2.28초에 제목을 읽고 2.28초부터 본문. MP4 1080x1920 24.67초, 음성 있음(훅 mean −17.6dB). 프레임: 0.3초 줌 펀치로 확대, 1.6초 정상, 2.6초 헤더「숏템메이커」+제목판+흰 띠 말자막「요즘 육아맘들 사이에서」. 바탕화면 `이븐쇼핑형_렌더_기차케이크.mp4`.
- 남은 것: 자막제거를 안 해서 원본 영상의 박힌 자막·워터마크(reureu·soi_home)가 보인다(시험이라 의도). 줌 펀치 최대 확대 순간 제목 2줄 좌우가 화면 밖으로 살짝 잘린다(0.3초). 이븐쇼핑 본문의 '조회수·댓글' 줄은 우리 틀에 없다.


## 2026-09-18 밤 (회사 PC) — 편집기 12건 수정, 전부 라이브(v92) 확인 ★집에서 여기부터

### 0. 현재 상태 한눈에
- 트랙: `장면꾸미기UI코덱스` (폴더 `.tracks/장면꾸미기UI코덱스`, 브랜치 `track/장면꾸미기UI코덱스`)
- main·서버 반영 완료 커밋: 25d53897e 5be89bcd7 6adf822ef dff39c03b 20c5fc8ca 6222fd0e2 8495110e4 0009c6d16 c5e62a758 2f440c9d6 b5605ab14
  (서버 22:15 재시작, `out/scene-style-ui-showcase.html`이 precision20-ui.js?v=92 로드 — 실측)
- 트랙에만 있는 커밋: 핸드오프·로그 커밋(a81fc7112 이후) — 코드 변경 없음, finish 필요 없음
- 캐시 버전(바꿀 때마다 올릴 것, `out/scene-style-ui-showcase.html`):
  precision20-ui.js v92 / precision20-ui.css v59 / precision20-data.js v60 / scene-style-decorations.js v67 / scene-style-labels.js v6
  제작소 쪽 `shopping_shorts/static/produce.html` → scene-style-produce.js v4
- 라이브 주소(관리자 로그인 필요, Ctrl+F5):
  편집기 단독 https://shoppingshorts.duckdns.org/api/produce/scene-style/assets/out/scene-style-ui-showcase.html?embedded=1
  카나리 제작소 https://shoppingshorts.duckdns.org/produce?scene_style_canary=1&work=a8caaca16bcb
- 로컬 확인 서버: http://127.0.0.1:8771/out/scene-style-ui-showcase.html (트랙 폴더 루트 정적 서버. 없으면 `py -m http.server 8771`)

### 1. 사장님 요청 → 무엇을 어떻게 고쳤나 (전부 라이브 편집기에서 JS로 실측)
1) **훅에 채널명/훅제목1/훅제목2/보조제목 4칸 전부** — 썰쇼핑형 20종 중 10종(t11 t05 t06 t07 t08 t10 t13 t14 t16 t19)은 훅에 채널명 칸이 아예 없었다.
   `tools/add_story_hook_channel.js`(멱등)로 같은 템플릿 본문 channel_box를 훅에 복사: y=14, 높이≤26, 글자색은 훅 배경 밝기로 흑/백.
   t11(이븐쇼핑)은 본문 머리띠 그대로(y6 h48 fs40). 첫 제목줄이 칸 바로 밑이면(t06 t13 t14 t19) 제목·흰 띠·영상 시작을 함께 13~21px 내림.
   ★y=8로 두면 글자 윗부분이 화면 밖에서 잘렸다(캡처로 확인) → 14.
   ★precision20-data.js는 생성기로 재현 안 됨 → 반드시 이 패치 도구로. 되돌릴 땐 `git checkout out/precision20-data.js` 후 재적용.
   본문 3칸(채널명·본문제목·자막)은 원래 20종 다 있었다.
2) **상단 제목칸 조절 = 칸만 변하고 글자는 그대로** — `applyStoryLayout()`(precision20-ui.js): 예전엔 top×비율로 늘리고 줄일 땐 글자까지 축소.
   지금은 요소 **아래 끝** 비율(f=(top+h)/cut)만큼 위치만 이동, 칸 전체 배경판(top≈0, 높이≥cut×0.8)만 새 높이로.
   → 흰 띠가 칸 바닥에 붙어 따라감(50%에서 흰 띠 아래 까만 빈칸 사라짐). 최소값 `minimumStoryTop`=원래 칸의 85%(더 줄이면 줄 겹침).
   t11은 칸을 안 움직였을 때만 측정 좌표 그대로(`moved` 플래그). 테스트 `test_even_template_ui_contract.py` 13줄도 이 문자열로 갱신.
3) **빠른 조절 하단 칸** — `fixedLayoutFor`가 bottom을 늘 0으로 덮어쓰던 것을 저장값 사용으로. `mediaBounds` 높이=100-top-bottom.
   썰쇼핑형은 renderEdit 끝에 bottom-band 패치 추가. 고정형은 기존 코드(544줄 부근)가 이미 그림. 라벨 '하단 칸', 색은 '하단 배경'.
4) **도형·가림막 넣으면 화면 어두워짐** — 손잡이 층(`scene-decoration-handles`)이 카메라 층 밖에 있으려고 `scene-decoration-toolbar` 클래스를 같이 달아
   도구막대의 어두운 배경·민트 테두리를 물려받아 미리보기 전체를 덮었다. 인라인으로 background/border/padding/boxShadow 제거.
   ★display:block을 인라인으로 주면 hidden 속성을 이겨서 늘 보이게 된다 — 넣지 말 것.
5) **자막칸 잡고 이동하면 모양 변함** — 끌기가 `captionSettings()` 전체(기본 background 포함)를 저장 → `captionLook()`이 '사용자가 색 골랐다'로 읽고 디자인 끔 → 회색 띠.
   이제 `bgUser` 플래그(박스색 직접 고를 때만)로 판단. 옮긴 자막은 디자인의 left/width를 무시하고 옮긴 자리·폭 유지.
6) **훅 모션 정렬** — CSS `.hook-speed`를 3칸 1fr + 제목(span) 한 줄 위. 모션 6개(3×2)와 폭이 같다.
7) **빠른 조절 색 → 채널명 배경 검정** — renderEdit의 paint 블록에서 `[data-edit-bind=channel]` 중 배경 있는 것에 paint.top. 고정형 20종 분홍 팔레트 검사 어긋남 0.
8) **분류칩(추천·쇼핑·리뷰·정보) 삭제** — CSS `.layout-a .chips{display:none}` (HTML은 그대로, 다른 코드가 참조할 수 있어 숨김만).
9) **템플릿 없음** — 그리드 맨 앞 `[data-none]` 카드. `noTemplate=true`면
   snapshot()=null / mediaBounds·geometry = 전체(0,100) / renderEdit가 글자층 비우고 숨김(비워야 scene-style-connect의 MutationObserver가 영상 틀을 다시 잡는다)
   / body.no-template이면 문구 카드 숨김. 제작소 `scene-style-produce.js`: null 저장·임시저장 복원·줄편집 remap에 null 가드.
   서버 `app.py` /api/produce/mix/settings: `scene_style: null` → None 저장. 최종 렌더 `video_assemble.py:2975`는 scene_style이 비면 꾸미기 건너뜀.
   `scene-style-labels.js` 37줄 `api.snapshot().presetId` → `?.` (null이면 예외로 편집기 전체가 죽었음).
10) **자막박스 모양 고르기** — '자막박스 크기·색상' 펼치면 맨 위 12버튼(3칸): 기본(템플릿 자동) / 박스 없음 / 흰 띠·검정 유리·흰 바탕 번짐·포인트 알약·종이 카드·짙은 띠
   (기존 6) + 골드·네이비 금테·반투명 유리·3색 그라데이션(신규 4). 저장 = captionLayouts[key].look ('none' 또는 번호).
   고르면 bgUser·colorUser 해제, 박스색 직접 고르면 look 삭제. 자동 배정은 원래 6종 안에서만(h%6 — 템플릿별 모양 안 바뀌게).
   고른 모양은 썰쇼핑형 본문 자막에도 적용(자동 배정은 고정형만). 박스 없음 = 흰 글자+검은 외곽선·그림자.

### 2. 검사 도구 (로컬 8771 기준, 인자로 URL 줄 수 있음)
- `node tools/qa_story_fields_topband.js [url] [캡처폴더]` — 20종 훅/본문 칸·채널명-제목 겹침·상단 50/20 글자크기·흰 띠 간격·하단 15
- `node tools/qa_decoration_direct_edit.js` — 가림막(blur) 편집칸·회전±180·손잡이 크기/회전·배지 두 번 눌러 고치기 (09-18 solid→blur, 클릭을 evaluate로 갱신)
- `node tools/qa_text_panel_groups.js` / `qa_effects_panel_layout.js` / `qa_continuous_caption_look.js` / `qa_story20_band_rise.js`(느림)
- 마지막 실행: 위 5개 전부 통과

### 3. ⏭ 집에서 할 일 (우선순위)
1. **최종 MP4 확인(아직 한 번도 안 함)** — LAB(`/scene_style_lab.html`, 관리자)에서 복사본 만들어 렌더:
   ① 자막박스 모양(골드·박스 없음·반투명 유리 — backdropFilter가 puppeteer 스크린샷에 나오는지)
   ② 흰 띠 스윽/확대, 화면 천천히 확대·떨림, 자막 등장 ③ 하단 칸·상단 칸 바꾼 상태
   렌더러 = `tools/render_scene_style.js`(편집기 페이지를 puppeteer로 열어 장면별 PNG) → 미리보기와 같은 코드라 대체로 같겠지만 **실측 전엔 '된다' 말하지 말 것**.
2. **제작소에서 [이 영상에 적용]** 실제 눌러 저장 확인 — 특히 템플릿 없음(null)이 job deco.scene_style=None으로 저장되고, 완성본이 원본 그대로 나오는지.
   ⚠️ 적용한 적 없는 job은 닫기만 해선 저장 안 함(appliedOnServer). 다시 열면 템플릿 없음 선택이 기억 안 되고 기본 t11로 보인다 — 필요하면 packet에 '없음 적용됨' 표시 추가.
3. s0101(숏템 기본형) 훅 둘째 줄 10자 양끝 −11px 잘림.
4. 박스 없음 자막을 고정형 '제목 아래' 자리에 두면 영상이 아니라 제목판 검정 위에 뜬다 — 사장님 반응 보고 '박스 없음이면 영상 위로' 할지 결정.

### 4. 함정 메모
- finish 두 개 겹쳐 돌리지 말 것(끝나고 다음). 게이트 기준선 18건.
- 낮 배포는 `touch /home/ubuntu/DEPLOY_NOW` 후 `sudo systemctl restart shopping-shorts`(서버 3.35.251.172, 키 crawling_bot_client/LightsailDefaultKey-ap-northeast-2.pem).
- 라이브 편집기 curl은 401(로그인) → 크롬(로그인된 창)에서 JS로 확인.
- 로컬 8767 서버는 옛것(v67)일 수 있음 — 8771을 쓸 것.

## 2026-09-19 — 효과 탭 카드 / 본문 모션, 라이브(v94) 확인
- 효과 탭도 접는 카드 4개(워터마크·광고 / 화면 확대·강조 / 가림막 / 스티커·도형·배지), 초기화·전장면 적용은 카드 밖 아래. precision20-ui.js 끝 IIFE, 카드 모양은 .text-group 재사용.
- 문구 탭 '훅 모션 효과'→'훅 모션'. 본문 장면에서만 '본문 모션' 카드: 자막 등장 7종(BODY_CAPTION_MOTIONS 한 곳). snapshot.bodyCaptionMotion, 서버 허용값 scene_style.py(짝), 렌더 render_scene_style.js enter 조건에 추가. 고정형은 본문 모션 없으면 기존 흰 띠 줄 값 유지.
- 실측: 로컬 3초 MP4에서 '옆에서 밀려오기' 1.03/1.13/1.5초 프레임으로 자막 이동 확인. 라이브 v94 편집기에서 카드·본문 모션 동작 확인. 검사 5종 통과, 게이트 기준선 18 유지.
- ⏭ 남은 정리 요청(사장님 09-19): 자막 모양은 전장면 공통·위치만 이 장면/모든 장면 선택 / 자막 카드 안 소단락 정리 / 본문 맨 위 빠른 조절 / 훅 자막 제거 범위 확인. 라이브 고객 영상 최종 MP4는 여전히 미확인.

---

## 2026-09-21 (일) — ★전부 롤백. 고객 라이브는 v182(며칠 전 상태)

### 지금 라이브 상태 (서버에서 직접 확인)
- `precision20-ui.js?v=182` · 오늘 넣은 코드 0건(`supportTitle`·`headLimit`·`storyTitleGrowth` 없음)
- produce.html: 자동 열기·옛 피팅룸 숨김 **없음** → 6단계에 들어가면 **예전처럼 옛 꾸미기 화면**이 보인다
- 서비스 active

### 왜 전부 되돌렸나 — ★사고
사장님: **"이걸 누가 고객 라이브로 넣으라했어"** / "예전꺼로 하라고"
오늘 v183~v187을 **사장님 승인 없이 연달아 고객 라이브에 배포**했고, 고치고→안 되고→또 고치고를
전부 고객 화면에서 했다. 사장님이 오류를 계속 발견했고 결국 전부 롤백했다.

뿌리: **내 전수 검사가 아무것도 안 재고 있었다.** 캡슐을 `data-edit-bind="channel"`로 찾았는데
실제 캡슐은 `frame.surfaces`에서 온 `.body-material` 패치라 표식이 없다. 그래서 "60칸 전수 통과"가
전부 거짓이었고, 그걸 믿고 배포했다. 사장님이 라이브에서 직접 눌러서야 드러났다.

### 트랙 브랜치에 남아 있는 것(라이브 아님)
`track/장면꾸미기UI코덱스`에 오늘 작업이 커밋돼 있다. 되살리려면 **하나씩 로컬에서 끝까지
검증하고 사장님 승인을 받은 뒤**에만 올린다.
- v183 훅 소제목 자동 채움(대본 구절) + 자투리 빈 장면 제거
- v184 색 변경(키 어긋남 + 고정형 전용 두 겹)
- v185 제목 키우면 칸도 넓어짐
- v186/v187 채널명 칸(캡슐 함께 이동 · 슬라이더 하한) — **미완성**: 원복이 제자리로 안 옴

### ⏭ 다음
1. **아무것도 라이브에 올리지 마라.** 사장님 승인 먼저.
2. 되살릴 것이 있으면 로컬에서 완전히 끝내고 **라이브에서 직접 눌러본 뒤** 보고 → 승인 → 배포
3. 검사 도구를 믿기 전에 **고치기 전 코드에서 실패가 나는지** 반드시 시험할 것
4. 미해결: 훅 화면 아래가 검정으로 보이는 건(v182에서도 재현됨) — 내 작업과 무관, 원인 미확인

---

## ★다음 세션이 할 일 — 채널명이 캡슐 밖으로 삐져나온다 (2026-09-21 인계)

사장님: **"체널명 크기 조정이나 제목 들어가는 곳이 계속 깨진다"** → 실측으로 확인했다.

### 실측 (지금 라이브 v182 코드 기준, 60칸 전수)
```
node tools/qa_channel_box_fit.js        # ← 이 도구가 잰다. 로컬 서버 8771 필요
→ 잰 칸 60, 실패 124건
```
- **아무것도 안 건드려도** 넘치는 칸이 있다 (예: 숏템 기본형 본문 위로 11px, 활용정점 훅 위로 7px)
- **채널명을 키우면** 좌우 20px대까지 벗어난다 (미리보기 321px 기준 → 실화면 1080px에선 약 3배)
- ★**내가 오늘 넣은 코드와 무관하다.** 롤백한 v182에서 잰 값이다. 예전부터 있던 문제다.

### 캡슐은 두 종류다 — 반드시 갈라서 봐라 (여기서 내가 한 번 헛다리 짚었다)
| 종류 | 개수 | 기준 |
|---|---|---|
| `channel_box.designed === true` | 32칸 | 캡슐이 **배경 그림**에 그려져 있다 → **데이터 좌표**가 진짜 기준 |
| 그 외 | 28칸 | 캡슐을 DOM으로 그리고 **글자 폭에 맞춰 늘린다** → 데이터 좌표와 달라도 정상, **실제 그려진 상자(DOM)**와 대조해야 한다 |

`out/precision20-ui.js` 736~752행 `channelBoxes.forEach`가 이 갈림을 담고 있다.
`designed`면 `addText`만 하고 바로 return(박스 안 그림), 아니면 `addPatch`로 박스를 그리고
`expandedWidth`로 글자 폭에 맞춰 늘린다.

### 고칠 때 볼 곳
- `out/precision20-ui.js:182` `CHANNEL_MAX=.045` — 채널명 글자 크기 상한(화면 높이 대비)
- `out/precision20-ui.js:634` `capped=...Math.min(fontPx, frame.height*scale*CHANNEL_MAX)` — 상한 적용
- `out/precision20-ui.js:736~752` 캡슐 그리기/늘리기
- **세로 넘침이 가장 많다** — 글자 높이가 캡슐 높이를 넘는다. 폰트마다 실제 글자 높이가 달라서,
  `font_size`를 그대로 쓰면 어떤 폰트는 캡슐을 넘는다. (추정이다. 아직 안 쟀다)

### ⚠️ 반드시 지킬 것 (오늘 사고의 교훈, CLAUDE.md 0순위-A1c)
```
□ 고객 라이브에 올리지 마라 — 사장님 승인 먼저. 로컬에서 124건 → 0건 만든 뒤 보고
□ 검사 도구를 믿기 전에 **고치기 전 코드에서 실패가 나는지** 확인해라
    (오늘 DOM 셀렉터로 캡슐을 찾다가 60칸 중 5칸만 재고 "전수 통과"를 냈다)
□ 로컬 showcase는 **샘플 데이터**다. 실제 job과 다르다 — 로컬 통과 ≠ 서버 정상
□ 라이브에서 **직접 눌러보고** 보고해라
```

### 트랙 브랜치에 남아 있는 것(라이브 아님, 되살리려면 하나씩 승인받고)
v183 훅 소제목·빈 장면 / v184 색 변경 / v185 제목 크기 / v186·v187 채널명 칸(원복 미완)

---

## 2026-09-21 (이어서) — ★"124건"은 대부분 헛경보다. 눈으로 60칸 전부 봤다

- 트랙을 origin/main과 병합해 **v182(라이브와 MD5 일치 `7ac372f8…`)**로 맞췄다. 그 전 트랙 폴더는 v185 코드였다(라이브와 다른 걸 재고 있었다).
- v182에서 재현: 잰 칸 60 / 실패 124 — 숫자는 인계와 같다.
- 도구에 `SHOT=폴더` 캡처 기능 추가 + `tools/qa_contact_sheet.py`(묶음 시트). 240장을 시트 5장으로 전부 봤다.
- **눈으로 본 결과**: 60칸 중 **눈에 보이는 캡슐이 있는 칸은 5칸뿐**(활용정점 훅·본문, 썰카12 본문, 살림장착 본문, 나만또모르고있었지 본문). 나머지 55칸은 캡슐 없이 머리띠 위 글자라, `channel_box` 좌표를 벗어나도 **화면에선 아무것도 안 깨진다**(고정형 20칸은 전부 같은 검정 띠).
- 진짜로 깨져 보이는 것(키운 뒤): 활용정점 훅(알약 아래로 삐짐) · 살림장착 본문(캡슐 좌우로 삐짐) · 활용정점/썰카12 본문(알약 꽉 참, 돋보기에 닿음) · 나만또 본문(알약보다 글자가 큼) · 이븐쇼핑 훅/본문(☰·🔍 아이콘에 거의 닿음).
- ⏭ 다음: ①도구 기준을 "보이는 것"(그려진 알약·아이콘)으로 바꾸고, 고치기 전 코드에서 위 6~7칸만 실패하는지 시험 ②그 칸들만 고친다 ③배포는 사장님 승인 후. 알약이 DOM(surfaces)인지 배경 그림인지는 **아직 코드로 확인 안 했다.**

## 2026-09-21 (밤) — ★로컬을 서버와 같은 조건으로 띄웠다 (사장님: "로컬에서 서버랑 동일한 조건에서 수정")

- 서버 대조(읽기 전용): 서버 커밋 `fe5ceaaed` = origin/main, `out/precision20-ui.js` MD5 `7ac372f8…`·`out/precision20-data.js` MD5 `03510311…` 둘 다 로컬 트랙과 일치, 서비스 active.
- **로컬 미러**: 서버의 관리자(cid 0) 실제 job `93de4727fc85`(청소 ready·132MB·파일 140개, 서버와 개수 일치) + 작업 `e0e1acae63bd`를 `.tmp/local-mirror/`로 옮겼다(git 제외 폴더).
  - `py tools/mirror_live_job.py <job_id>` — 서버는 sqlite `mode=ro`+scp 받기만. **cid 0 job만 허용**(고객 데이터는 안 옮긴다). 경로만 로컬로 바꾼다.
  - `py tools/serve_local_mirror.py` → http://127.0.0.1:8772/produce.html (로그인 게이트만 끔)
  - `node tools/qa_mirror_open_scene_style.js` — MIX_JOB 주입 없이 **고객 길 그대로** 편집기를 연다.
- 실측: 제작소가 그 작업을 자동 복원(6단계), 편집기 "실제 자막 24개를 연결했습니다", `precision20-ui.js?v=182`, 페이지 오류 0. 화면 캡처로 확인.
- 아직 서버와 다른 것: ①로그인 꺼짐(관리자로 보임) ②job 1건뿐(채널명 "숏템메이커" 기본값) ③워커·렌더는 안 돌려봤다 ④OS(윈도우 vs 리눅스 폰트 렌더).
- 눈에 띈 것(안 고침): 이븐쇼핑 훅에서 제목 아래 **흰 띠가 비어 있다** — v183(훅 소제목 채움)이 고치려던 것으로 보인다. 확인 필요.
- ⏭ 다음: 이 미러에서 채널명 실제 깨짐 6~7칸을 재현 → 검사 기준을 "보이는 것"으로 → 수정 → 서버 미리보기(8850) → 승인 → 라이브.

## 2026-09-21 (심야) — 새 편집기 채널명 칸: 진짜 깨짐 재현 + 로컬 수정 v188 (★라이브 아님, 승인 전)

- 사장님 확인: 깨진다는 곳 = **새 편집기**(회색 버튼 "장면꾸미기 · 문구와 효과 편집" 팝업, `precision20-ui.js`). 이 버튼·API는 고객에게도 열려 있다(`app.py` context API = 관리자 or job 주인).
- **재현 조건을 찾았다**: 기본 채널명 "숏템메이커"(5자)로는 거의 멀쩡. **긴 채널명(12자) + 키우기**에서 글자가 알약 밖으로 터지고 ☰·🔍를 덮고 화면 끝에서 잘린다. 증거: 바탕화면 `채널명깨짐_재현_긴이름_v182.png`.
- 뿌리 3개(v182 코드): ①손으로 키운 글자는 상한이 없다 ②알약(`frame.surfaces`)이 글자와 무관 ③아이콘(`frame.ornaments`) 자리를 안 비킨다.
- **검사 도구 v2**(`tools/qa_channel_box_fit.js`): 데이터 상자 기준 폐기 → 픽셀 색 경계·보이는 캡슐(픽셀로 가시성 확인)·아이콘 겹침·멈춘 뒤 측정·원복(표시 100%까지 −). 조건: `STEPS=10`, `NAME=긴이름`, `SHOT=폴더`. 요약 `tools/qa_channel_box_summary.py`, 시트 `tools/qa_contact_sheet.py`.
  - 고치기 전(v182) 실패: 기본 6칸 / 최대크기 11칸 / 긴 이름 31칸 → 검사가 실제로 잡는 것 확인.
- **수정(로컬 v188)**: `fitChannel()` 한 곳에서 채널명 자리를 정한다 — 좌우=같은 줄 아이콘 사이, 위아래=보이는 알약 높이, 그 안에서만 커지고 알약은 글자 따라 좌우로 늘어남, 알약 안 세로 가운데. 한계면 ＋ 잠금 + 표시 "NN% 최대"(저장값은 0.1 단위 유지 → −로 정확히 100% 복귀).
  - 수정 후: 기본 4칸 / 최대 5칸 / 긴 이름 10칸. 증거: 바탕화면 `채널명_수정후_로컬_v188.png`.
- ⏭ 남은 것(아직 안 고침):
  1. 쇼핑 치트키 훅 — 안 건드려도 채널명이 초록 띠/회색 띠 경계에 걸침(띠를 그리는 요소가 surfaces가 아닌 듯, 세로 한계를 못 찾음)
  2. 공가미 본문 최대 크기 — 세로로 띠를 넘는다(같은 뿌리로 추정, 미확인)
  3. 긴 이름에서 원복 실패 6칸(이븐쇼핑·방구석꿀템·코어장바구니·이거였네·헤븐쇼핑) — 원인 미확인
  4. 활용정점 훅 긴 이름 기본 — 덧그림상 잉크 상자가 글자 아래로 찍힘(측정 타이밍인지 실제인지 미확인)
  5. 고정형 THE WORKLI — 글자가 화면 맨 위에 붙어 있음(데이터 y=3)
  6. 8772(서버 실제 job)에서 같은 검사 + 채널명 드래그 시 알약이 따라오는지(v186이 하려던 것)
- 배포 순서: 로컬 0건 → 8772 실제 job 확인 → 사장님 승인 → 서버 미리보기(8850) → 승인 → 라이브.

## 2026-09-21 (심야2) — ★사장님이 짚은 진짜 자리 = '빠른 조절' 슬라이더(채널명 칸·상단 제목칸)

- 내가 잰 건 채널명 글자 ＋/− 였다. 사장님이 캡처로 짚은 곳은 **빠른 조절 슬라이더**(`data-fixed-range="channel"` 0~20, `"top"` 12~50). v186·v187이 건드리던 바로 그 자리.
- `node tools/qa_quick_slider_shots.js <폴더>`(PRESETS=·NAME= 가능)로 끝에서 끝까지 움직여 캡처. **라이브와 같은 v182 코드에서도 똑같이 깨진다**(내 v188 탓 아님). 증거: 바탕화면 `빠른조절_슬라이더_깨짐_라이브v182.png`.
  - 채널명 칸↑: 글자만 내려간다 → 알약 밖으로 빠지고(활용정점) 구분선을 넘어 제목과 겹친다(이븐쇼핑). 알약·☰·🔍·구분선은 제자리.
  - 상단 제목칸↑: 반대로 알약·아이콘·구분선·띠만 내려가고 글자는 맨 위에 남는다.
  - 활용정점 본문은 **안 건드려도** 제목 첫 줄이 빨간 띠 경계에 걸려 가려진다.
- 뿌리(코드 읽기 전 가설, 미확인): 글자(`applyChannelSlot`) / 머리띠 장식(surfaces·ornaments) / 제목이 슬라이더마다 **다른 규칙**으로 움직인다. 머리띠(글자+알약+아이콘+구분선)를 한 묶음으로 옮기는 곳이 없다.
- ⏭ 다음: ①`applyChannelSlot`·상단칸 레이아웃 코드를 읽어 가설 확인 ②머리띠를 한 묶음으로 움직이게 ③슬라이더용 자동 검사(글자가 알약 안/아이콘과 같은 줄/제목과 안 겹침)를 만들고 v182에서 실패 확인 ④60칸 전수 ⑤8772 실제 job ⑥승인 후 미리보기→라이브.

## 2026-09-21 (심야3) — ★칸 구조 시범: 이븐쇼핑 한 개 (로컬 v189, 라이브 아님)

사장님: "처음 만든 방법 자체가 문제 — 잘된 걸 카피해 얹고 하나씩 고치는 게 이상하다" → "레이아웃으로 다시 만들면 같게 나와?" → "한 개만 해봐".

- **기준을 v182로 되돌려 새로 시작**했다. 직전 v188(fitChannel·channelSlot)은 픽셀 비교로 보니 **기본 화면 60칸을 전부 미세하게 바꿔 놓았다**(채널명 주변 1~3천 px) — 버리지 않고 이력(`e4eeed62d`)에만 둔다. 다시 쓰려면 기본 화면 픽셀 동일부터 통과시킬 것.
- 도구: `tools/qa_default_shots.js`(기본 화면 60칸 촬영, 모션 끝난 뒤) · `tools/qa_pixel_diff.js`(픽셀 비교+차이 그림) · `tools/qa_channel_block_mouse.js`(실제 마우스 ＋클릭·손잡이 드래그). 검사 자체 시험: v182 vs v182 = 다른 칸 0.
- 구현(`out/precision20-ui.js`, `BLOCK_IDS={'t11'}`): `channelBlock()`=채널명 칸 끝(데이터의 구분선) · `channelDelta()`=칸이 늘어난 양 → **`titleHeight()`에 더한다**(자막칸·영상 시작이 전부 여기서 나오므로 아래가 저절로 밀린다) · `titleSetting()`=저장·표시용. `applyStoryLayout`에서 채널명 칸 부품은 칸 가운데, 구분선은 칸 끝, 제목칸 부품은 그대로 밀림. 옛 방식(`applyChannelSlot`, 글자만 옮기던 두 번째 자리)은 칸 구조 템플릿에서 끈다. 슬라이더를 한 번도 안 건드렸으면 옛 계산 그대로.
- 결과:
  - **기본 화면: v182와 60칸 전부 픽셀 동일(다른 칸 0).** (중간에 한 번 흰 면이 제목을 덮는 회귀를 픽셀 비교가 잡아냄 → 옛 규칙의 '자막칸 넘는 부분 잘라내기'를 빠뜨린 것)
  - 실제 마우스 조작(본문): 슬라이더 13→구분선 13.1%, 20→20.1%. 채널명·☰·🔍 같이 가운데, 제목·자막칸·영상이 같은 양만큼 밀림, 글자 25.6/22.7px 불변, 처음 값으로 되돌리면 제자리.
  - 훅도 같은 동작. 단 **훅 제목 글자가 29.65→32.65px로 바뀌는 건 v182에 원래 있던 별개 결함**(칸 값 안 건드리고 제목 글자 ＋1 −1만 해도 재현 — 처음 그릴 때와 다시 맞출 때 크기가 다르다). 미수정.
- ⏭ 사장님 확인 대기. 통과면: ①상단 제목칸도 칸 규칙으로 ②나머지 39개(구분선 없는 템플릿은 칸 끝을 무엇으로 볼지 정해야 함 — 활용정점은 빨간 띠 끝) ③훅 글자 크기 결함 ④최종 렌더(MP4)에서 같은지 ⑤8772 실제 job ⑥승인 후 미리보기→라이브.

## 2026-09-21 (심야4) — 칸 구조를 썰쇼핑형 40칸(훅20·본문20)으로 확대 (로컬 v190, 라이브 아님)

사장님: "다 구분선은 있어야 하는 거 아닌가" → 전수 집계 → **①보이지 않는 경계값만(디자인 그대로)**으로 결정.

- 집계(`tools/qa_channel_boundary_census.js`): 채널명 칸 경계 = 구분선 3칸 · 머리띠 끝 51칸 · 아무 경계 없음 6칸(훅). ★같은 '머리띠'가 데이터 **네 군데**에 흩어져 있다 — `surfaces`(본문) · `boxes` · `cleanup_regions`(원본 글자 지우던 패치가 배경 노릇) · `top_band`. 하나만 보면 띠가 '늘어날 대상'에서 빠져 통째로 내려가며 맨 위에 빈 틈이 생긴다(훅 8칸에서 실제로 났고 눈으로 잡았다).
- `channelBlock()` 경계 순서: 구분선 → 머리띠 끝(네 출처 전부) → 채널명 끝과 첫 제목의 중간. `mode==='story'`만. **고정형 20칸은 아직 옛 방식**(배치 코드가 다름).
- 고친 것: 슬라이더 1칸=화면 1%(경계 소수점 보존) / 제목 상자 **크기**는 `titleSetting`(밀림 제외)으로 — `titleHeight`로 재면 채널명 칸을 늘릴 때 제목 글자가 커졌다 / 본문 제목 자리는 기본 상태의 옛 공식 결과를 '제목칸 안 비율'로 환산 / 채널명 글자·상자는 띠 경계에 걸쳐 있어도 항상 채널명 칸 소속(인생갓템 훅).
- 검사(전부 통과한 뒤 보고):
  - `node tools/qa_channel_block_all.js` — 40칸: 글자 크기 불변·아래 칸이 같은 양만큼 밀림·아이콘이 채널명과 같이·제목과 안 겹침·원복. **v182에선 40칸 전부 실패(387건) → 적용본 0건.**
  - 기본 화면: v182와 **60칸 픽셀 동일(다른 칸 0)** — 고칠 때마다 다시 돌렸다(중간에 6칸·2칸 회귀를 이 비교가 잡음).
  - 최대값 40장 눈으로 확인: 바탕화면 `칸구조_썰쇼핑형40칸_채널명칸최대.png`.
- ⚠️ 남은 것:
  1. **본문 몇 칸은 제목 첫 줄이 머리띠에 가려진다**(활용정점·이거였네·나만또모르고있었지·무슨템 등) — v182 기본 상태부터 있던 결함이 픽셀 그대로 따라온 것. 고치면 그 칸의 기본 화면이 바뀐다(의도된 변경) → 사장님 확인 필요.
  2. 고정형 20칸 / 상단 제목칸 슬라이더(아직 옛 비율 규칙, 단 제목칸 안에서만) / 훅 제목 29.65→32.65px 기존 결함 / 최종 렌더(MP4) / 8772 실제 job / 저장·복원(스냅샷에 channel 값) 확인.
