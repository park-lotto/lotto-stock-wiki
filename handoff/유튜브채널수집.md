> **소유 트랙**: 유튜브채널수집 — 이 파일은 이 트랙 세션만 수정한다. 다른 트랙은 읽기만.
> 트리거: "유튜브 채널수집 이어서" / "레퍼런스 유튜브 채널 이어서" / "인스타 수집 이어서" / "유튜브 큐레이션 이어서"

## ⏭ 집에서 이어하기 (2026-07-25 저녁 마감, 전부 origin/main 반영·백업 완료)

오늘 라이브 배포된 것(전부 검증됨, origin/main HEAD=601fb9c9c):

### 1. 렌즈→유튜브 채널 대량 벤치등록 + 카테고리 하베스터 (라이브)
- 렌즈 유사영상 유튜브 결과 → 채널 [전부 벤치등록]/[＋벤치] (`/api/seeds/from_youtube_videos`)
- `scripts/harvest_channels_by_category.py` — 카테고리별 상위영상→채널 수확(렌즈X, 무료).
  - `--from-ranking`: 저장 랭킹(username=channel_id)에서 무료 수확. 서버서 `python -m scripts.harvest_channels_by_category [top] [--from-ranking]`
- **서버에 유튜브 채널 462개 등록됨**(account 시드). 엑셀=`out/harvested_channels_top30.csv`

### 2. 채널 지표 리더보드 (라이브)
- 랭킹 화면 유튜브 탭: URL 목록 숨김 → **"📺 등록 채널 462개 · 📊 채널 지표·관리 열기"** 요약
- `/api/youtube/channels?sort=views|speed|density|accel|count` — 채널별 조회수·속도·참여율·가속 집계
- `ranking.aggregate_channels` + `service.youtube_channel_board`. 모달서 채널별 ✕제거.

### 3. 매일 유튜브 자동수집 타이머 (라이브)
- 서버 systemd `shopping-shorts-collect.timer` = **매일 08:10 KST**, `scripts/daily_youtube_collect.py`
- 무료 경로(계정·키워드·카테고리 프리셋)만. 재실행: 서버 `sudo systemctl start shopping-shorts-collect.service`

### 4. ★배포 게이트 7배 가속 (라이브)
- `merge_gate.py`에 pytest `-n auto`(xdist). 실측 직렬 312초→병렬 44초. 락으로 1게이트씩이라 스래싱0.
- ⚠️교훈: 배포큐 정체는 kill로 못 뚫음(타세션 재큐). 진행중 게이트 hang 오판 금지(짧은관찰 CPU정체≠hang).

### 5. ★인스타 지금수집 500 근본해결 = 수집 비동기화 (라이브·검증완료)
- **원인(실측)**: 엑셀 440채널·MAX_CHANNELS 200을 HTTP요청 안에서 동기 Apify수집(~13초/채널)→~40분→게이트웨이 타임아웃 500.
- **해결**: census와 동일 job 패턴. `/api/collect`=job_id 즉시반환+백그라운드, `/api/collect/status/{job_id}` 폴링. 전 플랫폼 공통.
  - store `collect_jobs`테이블+3메서드 / app `_run_collect_job`+status / index.html `doCollect`→`_pollCollect`
- **라이브 검증**: 서버서 `_run_collect_job('instagram',limit=3)` 실행=103초 done, 에러없음. 500 원천차단.
- ⚠️검증하느라 insta last_run이 3~5채널로 잠깐 바뀜 → **[지금수집] 한 번 누르면 200채널 전체 백그라운드 재수집**(진행배너 경과초, done시 자동갱신). 마감 시점 전체 수집 job b574829c9d77 running 중이었음(정상).

## 다음 후보 (우선순위)
1. **수집 부분갱신/병렬상향** (선택): 인스타 200채널이 다 긁힌 뒤에만 저장→완료 전 화면 안 바뀜. "수집되는 대로 부분 저장" 또는 Apify 병렬도 상향으로 체감 단축. 지금도 완료되면 정상 갱신됨(급하진 않음).
2. **차단(🚫)**: 렌즈/랭킹 카드에서 무관 채널을 수집 영구제외(blocklist 테이블 → `_collect_youtube` raw 필터). B의 나머지 절반.
3. **D. AI 관련성 필터**(Phase2): 제목·썸네일 Gemini로 "쇼핑·살림 재편집 쇼츠?" 판정→무관 걸러냄. 무거움.
4. **500채널 채우기**: 다음 08:10 자동수집(쿼터 리셋)이면 채널풀이 더 커져 462→500+ 자연 증가. 또는 harvest top 올려 재실행.

## 배포/서버 메모
- origin/main HEAD 601fb9c9c 이후. 서버 `ubuntu@3.39.179.148`, systemd shopping-shorts(:8849).
- 인스타 채널 소스: env `SHORTS_EXCEL=/home/ubuntu/lotto-stock-wiki/shopping_shorts/data/벤치마킹시트.xlsx`(서버 존재·444행). APIFY 토큰 17개 정상.
- ⚠️ 인라인 python 진단 시 **반드시 `set -a; . /etc/shopping-shorts.env; set +a`** 먼저 — 안 하면 EXCEL/APIFY가 로컬 기본값으로 폴백해 오진(오늘 실수).
