# 세션사망수리 — 작업 로그

- [2026-08-24] 레퍼런스 수집 68건까지 하락(08-17엔 161건). 원인은 채널·프록시가 아니라
  **레퍼런스 세션 7개 중 3개 사망**(인스타가 `sessionid=deleted`로 회수, 쿠키 만료일은 2027년).
  라운드로빈이라 3/7이 죽은 슬롯에 배정 → 성공률 43%. 같은 채널을 슬롯만 바꿔 돌려 확증
  (죽은 슬롯 0건 / 산 슬롯 10건, 실패 채널 6개도 산 슬롯에서 6/6 회수 — 죽은 채널 0개).
  ★진짜 원인은 `_detail_context`가 `slots[0]` 고정 + 프록시가 `elif`라, 0번만 AWS 맨 IP로
  매일 119~125건씩 나간 것. ①계정↔프록시 짝(→kr-12 한국 가정회선) ②`session_alive`·
  `live_slots`(인덱스 보존·전멸 시 원본 폴백) 신설. 라이브 `465c6cdf2`.
  ⚠️ 처음 계획한 "로테이션 붙이기"는 7계정이 서버IP를 돌려써 **전멸**시킬 뻔했다 — 짝이 먼저다.
