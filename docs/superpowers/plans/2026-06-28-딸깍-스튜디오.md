# 딸깍 스튜디오 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 버튼 하나로 아침 브리핑 카드를 생성하고(데이터→Gemini 히로이미지→카드렌더→PNG→텔레전송), 그 과정이 단계 애니메이션으로 흐르는 트렌디한 로컬 스튜디오를 만든다.

**Architecture:** 기존 `dashboard/server.py`(FastAPI:8090)에 스튜디오 페이지 + SSE 생성 엔드포인트를 추가한다. 생성 파이프라인은 독립 모듈 4개(`studio_data`, `gemini_image`, `card_render`, `studio_pipeline`)로 분리하고, PNG 추출·텔레그램 전송은 기존 `scripts/viz_card.py` 함수를 재사용한다.

**Tech Stack:** Python 3, FastAPI, uvicorn, Playwright(기존), google-genai(신규), 순수 HTML/CSS/JS(프런트), pytest(테스트).

## Global Constraints

- 파일 경로 기준 루트: `C:\Users\TheRose\Desktop\로또의 주식` (코드에서는 `ROOT`로 계산)
- 환경변수는 `.env`에서 읽는다: `GEMINI_API_KEY`, `GEMINI_API_KEY_2`(예비), `BOT_TOKEN`, `CHAT_ID` — 모두 설정 완료
- 카드 HTML의 캡처 대상 요소는 반드시 클래스 `.card` (기존 `viz_card.save_png`가 `.card`를 420px로 클립)
- 산출물 저장: `out/studio/` (PNG·HTML·index.json), `out/studio/img/`(히로 이미지 캐시)
- 디자인: 다크 차콜 배경 + 골드 액센트(`#d4af37` 계열), `Instrument Serif` 헤드라인, 큰 라운드 코너
- Gemini 이미지 모델: `gemini-2.5-flash-image`. 실패 시 골드 그라데이션 폴백으로 흐름 유지(에러 아님)
- 테스트 러너: `pytest`. 테스트는 `tests/studio/`
- 외부 호출(Gemini/Telegram)은 테스트에서 반드시 모킹

---

### Task 1: studio_data — 브리핑 데이터 확보

**Files:**
- Create: `scripts/studio_data.py`
- Test: `tests/studio/test_studio_data.py`

**Interfaces:**
- Consumes: `out/scenario_{date}.md` (daily_scenario.py 산출물)
- Produces: `get_briefing_data(date: str) -> dict` — 반환 키: `date`(str), `headline`(str), `market_mood`(str), `lead_sectors`(list[str]), `lines`(list[str]), `source`(str). 데이터가 없으면 빈 기본값 dict(예외 안 던짐).

- [ ] **Step 1: Write the failing test**

```python
# tests/studio/test_studio_data.py
from pathlib import Path
import scripts.studio_data as sd

def test_parses_scenario_md(tmp_path, monkeypatch):
    out = tmp_path / "out"
    out.mkdir()
    (out / "scenario_2026-06-28.md").write_text(
        "# 2026-06-28 시장 시나리오\n\n"
        "## 오늘의 한 줄\n반도체 강세 지속, 외인 순매수 유입\n\n"
        "## 주도 섹터\n- 반도체\n- 조선\n\n"
        "## 시나리오\n- 코스피 강보합 출발 예상\n- 대장주 중심 순환매\n",
        encoding="utf-8")
    monkeypatch.setattr(sd, "OUT_DIR", out)
    data = sd.get_briefing_data("2026-06-28")
    assert data["date"] == "2026-06-28"
    assert "반도체" in data["headline"]
    assert "반도체" in data["lead_sectors"]
    assert len(data["lines"]) >= 1
    assert data["source"] == "scenario_2026-06-28.md"

def test_missing_file_returns_defaults(tmp_path, monkeypatch):
    out = tmp_path / "out"; out.mkdir()
    monkeypatch.setattr(sd, "OUT_DIR", out)
    data = sd.get_briefing_data("2026-01-01")
    assert data["date"] == "2026-01-01"
    assert data["headline"] == ""
    assert data["lead_sectors"] == []
```

- [ ] **Step 2: Run test to verify it fails**

Run: `python -m pytest tests/studio/test_studio_data.py -v`
Expected: FAIL (`ModuleNotFoundError: scripts.studio_data` 또는 함수 없음)

- [ ] **Step 3: Write minimal implementation**

```python
# scripts/studio_data.py
"""브리핑 데이터 확보 — daily_scenario 산출물(out/scenario_{date}.md)을 파싱해 dict로."""
import re
from pathlib import Path

ROOT = Path(__file__).resolve().parent.parent
OUT_DIR = ROOT / "out"


def _section(md: str, title: str) -> list[str]:
    """## {title} 섹션의 본문 라인들(불릿 '-' 제거)을 반환."""
    lines = md.splitlines()
    out, capture = [], False
    for ln in lines:
        if ln.strip().startswith("## "):
            capture = title in ln
            continue
        if capture and ln.strip():
            out.append(ln.strip().lstrip("-").strip())
    return out


def get_briefing_data(date: str) -> dict:
    path = OUT_DIR / f"scenario_{date}.md"
    base = {"date": date, "headline": "", "market_mood": "",
            "lead_sectors": [], "lines": [], "source": ""}
    if not path.exists():
        return base
    md = path.read_text(encoding="utf-8")
    one_liner = _section(md, "오늘의 한 줄")
    sectors = _section(md, "주도 섹터")
    scenario = _section(md, "시나리오")
    base.update({
        "headline": one_liner[0] if one_liner else "",
        "market_mood": one_liner[0] if one_liner else "",
        "lead_sectors": sectors,
        "lines": scenario or one_liner,
        "source": path.name,
    })
    return base
```

- [ ] **Step 4: Run test to verify it passes**

Run: `python -m pytest tests/studio/test_studio_data.py -v`
Expected: PASS (2 passed)

- [ ] **Step 5: Commit**

```bash
git add scripts/studio_data.py tests/studio/test_studio_data.py
git commit -m "feat(studio): 브리핑 데이터 파서 studio_data"
```

---

### Task 2: gemini_image — Gemini API 히로 이미지 + 폴백

**Files:**
- Create: `scripts/gemini_image.py`
- Test: `tests/studio/test_gemini_image.py`

**Interfaces:**
- Consumes: `.env`의 `GEMINI_API_KEY`, Task 1의 `dict`(프롬프트 빌드용)
- Produces:
  - `build_prompt(data: dict) -> str`
  - `generate_hero(prompt: str, out_path: Path, api_key: str | None = None) -> dict` — 반환 `{"ok": bool, "path": str, "fallback": bool}`. 성공/실패 무관하게 항상 `out_path`에 PNG가 존재함(실패 시 골드 그라데이션 폴백).
  - `_fallback_gradient(out_path: Path) -> None` — Pillow로 골드→차콜 그라데이션 PNG 생성

- [ ] **Step 1: Write the failing test**

```python
# tests/studio/test_gemini_image.py
from pathlib import Path
import scripts.gemini_image as gi

def test_build_prompt_includes_sectors():
    p = gi.build_prompt({"lead_sectors": ["반도체", "조선"], "headline": "외인 순매수"})
    assert "반도체" in p
    assert "text" in p.lower() or "글자" in p  # "텍스트 없음" 지시 포함

def test_generate_hero_falls_back_when_no_key(tmp_path, monkeypatch):
    # API 키 없음 → 폴백 그라데이션이 생성되고 파일 존재
    monkeypatch.setattr(gi, "_call_gemini", lambda *a, **k: (_ for _ in ()).throw(RuntimeError("no key")))
    out = tmp_path / "hero.png"
    res = gi.generate_hero("prompt", out, api_key=None)
    assert res["fallback"] is True
    assert out.exists() and out.stat().st_size > 0

def test_generate_hero_uses_api_result(tmp_path, monkeypatch):
    out = tmp_path / "hero.png"
    monkeypatch.setattr(gi, "_call_gemini", lambda prompt, key: b"\x89PNG\r\n\x1a\nFAKEDATA")
    res = gi.generate_hero("prompt", out, api_key="fake")
    assert res["ok"] is True and res["fallback"] is False
    assert out.read_bytes().startswith(b"\x89PNG")
```

- [ ] **Step 2: Run test to verify it fails**

Run: `python -m pytest tests/studio/test_gemini_image.py -v`
Expected: FAIL (`ModuleNotFoundError: scripts.gemini_image`)

- [ ] **Step 3: Write minimal implementation**

```python
# scripts/gemini_image.py
"""Gemini API 히로 이미지 생성 (실패 시 골드 그라데이션 폴백)."""
from pathlib import Path

ROOT = Path(__file__).resolve().parent.parent
MODEL = "gemini-2.5-flash-image"


def _load_env() -> dict:
    env, ep = {}, ROOT / ".env"
    if ep.exists():
        for line in ep.read_text(encoding="utf-8").splitlines():
            if "=" in line and not line.startswith("#"):
                k, v = line.split("=", 1)
                env[k.strip()] = v.strip()
    return env


def build_prompt(data: dict) -> str:
    sectors = ", ".join(data.get("lead_sectors", [])[:3]) or "한국 증시"
    return (
        f"Minimal premium financial editorial illustration about Korean stock market. "
        f"Theme: {sectors}. Mood: {data.get('headline','')}. "
        f"Dark charcoal background with gold accents, abstract, cinematic, high-end. "
        f"No text, no letters, no numbers. (텍스트/글자 없음)"
    )


def _call_gemini(prompt: str, key: str) -> bytes:
    """google-genai로 이미지 1장 생성, PNG bytes 반환. 실패 시 예외."""
    from google import genai
    from google.genai import types
    client = genai.Client(api_key=key)
    resp = client.models.generate_content(
        model=MODEL, contents=prompt,
        config=types.GenerateContentConfig(response_modalities=["IMAGE"]),
    )
    for part in resp.candidates[0].content.parts:
        if getattr(part, "inline_data", None) and part.inline_data.data:
            return part.inline_data.data
    raise RuntimeError("Gemini 응답에 이미지 없음")


def _fallback_gradient(out_path: Path) -> None:
    from PIL import Image
    w, h = 840, 480
    img = Image.new("RGB", (w, h))
    px = img.load()
    top, bot = (212, 175, 55), (26, 26, 30)  # gold → charcoal
    for y in range(h):
        t = y / h
        r = int(top[0]*(1-t) + bot[0]*t)
        g = int(top[1]*(1-t) + bot[1]*t)
        b = int(top[2]*(1-t) + bot[2]*t)
        for x in range(w):
            px[x, y] = (r, g, b)
    out_path.parent.mkdir(parents=True, exist_ok=True)
    img.save(out_path, "PNG")


def generate_hero(prompt: str, out_path: Path, api_key: str | None = None) -> dict:
    out_path = Path(out_path)
    out_path.parent.mkdir(parents=True, exist_ok=True)
    key = api_key or _load_env().get("GEMINI_API_KEY", "")
    if key:
        try:
            data = _call_gemini(prompt, key)
            out_path.write_bytes(data)
            return {"ok": True, "path": str(out_path), "fallback": False}
        except Exception as e:
            print(f"  ⚠️  Gemini 이미지 실패, 폴백 사용: {e}")
    # 키 없음 or 실패 → 폴백
    try:
        _fallback_gradient(out_path)
    except Exception as e:
        print(f"  ⚠️  폴백 생성도 실패: {e}")
        return {"ok": False, "path": str(out_path), "fallback": True}
    return {"ok": True, "path": str(out_path), "fallback": True}
```

- [ ] **Step 4: Run test to verify it passes**

먼저 의존성 확인/설치:
```bash
python -c "import google.genai" 2>NUL || pip install google-genai
python -c "import PIL" 2>NUL || pip install pillow
```
Run: `python -m pytest tests/studio/test_gemini_image.py -v`
Expected: PASS (3 passed)

- [ ] **Step 5: Commit**

```bash
git add scripts/gemini_image.py tests/studio/test_gemini_image.py
git commit -m "feat(studio): Gemini 히로 이미지 생성 + 골드 그라데이션 폴백"
```

---

### Task 3: card_render — 브리핑 카드 HTML 합성

**Files:**
- Create: `scripts/card_render.py`
- Test: `tests/studio/test_card_render.py`
- Reference (읽기): `channel/strategy/briefing_카드_디자인스펙.md`

**Interfaces:**
- Consumes: Task 1의 `data: dict`, Task 2가 만든 `hero_path: Path`
- Produces: `render_briefing_card(data: dict, hero_path: Path) -> str` (HTML 문자열, 최상위 캡처요소 `.card`, 폭 420px). `save_card_html(html: str, out_path: Path) -> Path`.

- [ ] **Step 1: Write the failing test**

```python
# tests/studio/test_card_render.py
from pathlib import Path
import scripts.card_render as cr

DATA = {"date": "2026-06-28", "headline": "반도체 강세 지속",
        "lead_sectors": ["반도체", "조선"], "lines": ["코스피 강보합", "순환매"]}

def test_render_contains_card_and_content(tmp_path):
    hero = tmp_path / "hero.png"; hero.write_bytes(b"\x89PNG")
    html = cr.render_briefing_card(DATA, hero)
    assert 'class="card"' in html          # 캡처 대상 요소 존재
    assert "반도체 강세 지속" in html        # 헤드라인
    assert "코스피 강보합" in html           # 시나리오 라인
    assert "2026-06-28" in html             # 날짜
    assert "width:420px" in html.replace(" ", "")  # 폭 고정

def test_render_embeds_hero_as_file_uri(tmp_path):
    hero = tmp_path / "hero.png"; hero.write_bytes(b"\x89PNG")
    html = cr.render_briefing_card(DATA, hero)
    assert hero.as_uri() in html or "base64" in html

def test_save_card_html(tmp_path):
    p = cr.save_card_html("<html>x</html>", tmp_path / "c.html")
    assert p.exists() and "x" in p.read_text(encoding="utf-8")
```

- [ ] **Step 2: Run test to verify it fails**

Run: `python -m pytest tests/studio/test_card_render.py -v`
Expected: FAIL (`ModuleNotFoundError: scripts.card_render`)

- [ ] **Step 3: Write minimal implementation**

```python
# scripts/card_render.py
"""브리핑 카드 HTML 합성 — 다크+골드, Instrument Serif, 캡처요소 .card(420px)."""
from pathlib import Path

GOLD = "#d4af37"
BG   = "#1a1a1e"


def render_briefing_card(data: dict, hero_path: Path) -> str:
    hero_uri = Path(hero_path).as_uri()
    sectors = " · ".join(data.get("lead_sectors", [])[:3])
    lines_html = "".join(
        f'<li>{ln}</li>' for ln in data.get("lines", [])[:5]
    )
    return f"""<!doctype html><html lang="ko"><head><meta charset="utf-8">
<link href="https://fonts.googleapis.com/css2?family=Instrument+Serif&family=Noto+Sans+KR:wght@400;700&display=swap" rel="stylesheet">
<style>
  * {{ margin:0; padding:0; box-sizing:border-box; }}
  body {{ background:#000; }}
  .card {{ width:420px; background:{BG}; color:#fff; font-family:'Noto Sans KR',sans-serif;
           border-radius:24px; overflow:hidden; }}
  .hero {{ width:100%; height:240px; object-fit:cover; }}
  .body {{ padding:28px 26px 34px; }}
  .date {{ color:{GOLD}; font-size:14px; letter-spacing:2px; }}
  .headline {{ font-family:'Instrument Serif',serif; font-size:34px; line-height:1.2;
               margin:10px 0 6px; }}
  .sectors {{ color:{GOLD}; font-size:15px; font-weight:700; margin-bottom:18px; }}
  ul {{ list-style:none; }}
  li {{ font-size:16px; line-height:1.7; padding-left:18px; position:relative; color:#e8e8ea; }}
  li::before {{ content:"›"; color:{GOLD}; position:absolute; left:0; }}
  .foot {{ margin-top:22px; font-size:12px; color:#888; }}
</style></head><body>
  <div class="card">
    <img class="hero" src="{hero_uri}" alt="">
    <div class="body">
      <div class="date">{data.get('date','')}  ·  아침 브리핑</div>
      <div class="headline">{data.get('headline','')}</div>
      <div class="sectors">주도 섹터 — {sectors}</div>
      <ul>{lines_html}</ul>
      <div class="foot">로또의 주식 · STOCK BRAIN</div>
    </div>
  </div>
</body></html>"""


def save_card_html(html: str, out_path: Path) -> Path:
    out_path = Path(out_path)
    out_path.parent.mkdir(parents=True, exist_ok=True)
    out_path.write_text(html, encoding="utf-8")
    return out_path
```

- [ ] **Step 4: Run test to verify it passes**

Run: `python -m pytest tests/studio/test_card_render.py -v`
Expected: PASS (3 passed)

- [ ] **Step 5: Commit**

```bash
git add scripts/card_render.py tests/studio/test_card_render.py
git commit -m "feat(studio): 브리핑 카드 HTML 렌더 card_render"
```

---

### Task 4: studio_pipeline — 단계 오케스트레이션 + SSE 이벤트

**Files:**
- Create: `scripts/studio_pipeline.py`
- Test: `tests/studio/test_studio_pipeline.py`

**Interfaces:**
- Consumes: `studio_data.get_briefing_data`, `gemini_image.build_prompt`/`generate_hero`, `card_render.render_briefing_card`/`save_card_html`, `viz_card.save_png`/`send_telegram_photo`
- Produces: `generate_briefing(date: str) -> Iterator[dict]` — 각 단계마다 `{"type":"step","id":int,"status":"running"|"done"|"error","message":str}`를 yield, 마지막에 `{"type":"done","png":str,"html":str,"thumb":str,"sent_tg":bool}` 또는 `{"type":"error","message":str}`. 또한 `out/studio/index.json`에 이력 append. 헬퍼 `_append_index(entry: dict) -> None`.

- [ ] **Step 1: Write the failing test**

```python
# tests/studio/test_studio_pipeline.py
from pathlib import Path
import json
import scripts.studio_pipeline as sp

def test_pipeline_emits_all_steps_and_done(tmp_path, monkeypatch):
    studio = tmp_path / "out" / "studio"
    monkeypatch.setattr(sp, "STUDIO_DIR", studio)
    monkeypatch.setattr(sp, "IMG_DIR", studio / "img")
    # 외부/무거운 단계 모킹
    monkeypatch.setattr(sp.studio_data, "get_briefing_data",
        lambda d: {"date": d, "headline": "테스트", "lead_sectors": ["반도체"], "lines": ["라인1"]})
    monkeypatch.setattr(sp.gemini_image, "generate_hero",
        lambda prompt, out, **k: (Path(out).parent.mkdir(parents=True, exist_ok=True),
                                   Path(out).write_bytes(b"\x89PNG"),
                                   {"ok": True, "path": str(out), "fallback": True})[-1])
    monkeypatch.setattr(sp.viz_card, "save_png",
        lambda hp, pp: (Path(pp).write_bytes(b"\x89PNG"), True)[-1])
    monkeypatch.setattr(sp.viz_card, "send_telegram_photo", lambda pp, caption="": True)

    events = list(sp.generate_briefing("2026-06-28"))
    step_ids = [e["id"] for e in events if e["type"] == "step" and e["status"] == "done"]
    assert step_ids == [1, 2, 3, 4, 5]
    done = events[-1]
    assert done["type"] == "done" and done["sent_tg"] is True
    assert Path(done["png"]).exists()
    # 이력 기록 확인
    idx = json.loads((studio / "index.json").read_text(encoding="utf-8"))
    assert idx[0]["date"] == "2026-06-28"

def test_pipeline_partial_success_when_telegram_fails(tmp_path, monkeypatch):
    studio = tmp_path / "out" / "studio"
    monkeypatch.setattr(sp, "STUDIO_DIR", studio)
    monkeypatch.setattr(sp, "IMG_DIR", studio / "img")
    monkeypatch.setattr(sp.studio_data, "get_briefing_data",
        lambda d: {"date": d, "headline": "t", "lead_sectors": [], "lines": ["x"]})
    monkeypatch.setattr(sp.gemini_image, "generate_hero",
        lambda prompt, out, **k: (Path(out).parent.mkdir(parents=True, exist_ok=True),
                                   Path(out).write_bytes(b"\x89PNG"),
                                   {"ok": True, "fallback": True})[-1])
    monkeypatch.setattr(sp.viz_card, "save_png", lambda hp, pp: (Path(pp).write_bytes(b"\x89PNG"), True)[-1])
    monkeypatch.setattr(sp.viz_card, "send_telegram_photo", lambda pp, caption="": False)
    events = list(sp.generate_briefing("2026-06-28"))
    done = events[-1]
    assert done["type"] == "done" and done["sent_tg"] is False  # 부분 성공
    assert Path(done["png"]).exists()
```

- [ ] **Step 2: Run test to verify it fails**

Run: `python -m pytest tests/studio/test_studio_pipeline.py -v`
Expected: FAIL (`ModuleNotFoundError: scripts.studio_pipeline`)

- [ ] **Step 3: Write minimal implementation**

```python
# scripts/studio_pipeline.py
"""딸깍 생성 파이프라인 — 단계별 SSE 이벤트를 yield하고 산출물/이력을 남긴다."""
import sys, json
from pathlib import Path
from datetime import datetime

ROOT = Path(__file__).resolve().parent.parent
sys.path.insert(0, str(ROOT / "scripts"))

import studio_data, gemini_image, card_render
import viz_card

STUDIO_DIR = ROOT / "out" / "studio"
IMG_DIR = STUDIO_DIR / "img"

STEPS = {1: "수급·시황 데이터 수집", 2: "AI 히로이미지 생성",
         3: "카드 디자인 렌더링", 4: "이미지 추출(PNG)", 5: "텔레그램 채널 전송"}


def _append_index(entry: dict) -> None:
    STUDIO_DIR.mkdir(parents=True, exist_ok=True)
    idx_path = STUDIO_DIR / "index.json"
    idx = []
    if idx_path.exists():
        try:
            idx = json.loads(idx_path.read_text(encoding="utf-8"))
        except Exception:
            idx = []
    idx.insert(0, entry)  # 최신이 위
    idx_path.write_text(json.dumps(idx, ensure_ascii=False, indent=2), encoding="utf-8")


def _step(i, status, msg=""):
    return {"type": "step", "id": i, "status": status,
            "message": msg or STEPS[i]}


def generate_briefing(date: str):
    STUDIO_DIR.mkdir(parents=True, exist_ok=True)
    IMG_DIR.mkdir(parents=True, exist_ok=True)
    try:
        # ① 데이터
        yield _step(1, "running")
        data = studio_data.get_briefing_data(date)
        yield _step(1, "done")

        # ② Gemini 히로 이미지
        yield _step(2, "running")
        hero = IMG_DIR / f"{date}_hero.png"
        prompt = gemini_image.build_prompt(data)
        res = gemini_image.generate_hero(prompt, hero)
        msg = "폴백 이미지 사용" if res.get("fallback") else "AI 이미지 생성"
        yield _step(2, "done", msg)

        # ③ 카드 렌더
        yield _step(3, "running")
        html = card_render.render_briefing_card(data, hero)
        html_path = STUDIO_DIR / f"{date}_브리핑.html"
        card_render.save_card_html(html, html_path)
        yield _step(3, "done")

        # ④ PNG 추출
        yield _step(4, "running")
        png_path = STUDIO_DIR / f"{date}_브리핑.png"
        ok_png = viz_card.save_png(html_path, png_path)
        if not ok_png:
            yield _step(4, "error", "PNG 추출 실패(Playwright 확인)")
            yield {"type": "error", "message": "PNG 추출 실패"}
            return
        yield _step(4, "done")

        # ⑤ 텔레그램 전송 (실패해도 부분 성공)
        yield _step(5, "running")
        caption = f"📊 {date} 아침 브리핑\n{data.get('headline','')}"
        sent = viz_card.send_telegram_photo(png_path, caption)
        yield _step(5, "done" if sent else "error",
                    "전송 완료" if sent else "전송 실패(PNG는 저장됨)")

        entry = {"date": date, "type": "briefing",
                 "png": str(png_path), "html": str(html_path),
                 "thumb": str(png_path), "sent_tg": bool(sent),
                 "created_at": datetime.now().strftime("%Y-%m-%d %H:%M")}
        _append_index(entry)
        yield {"type": "done", "png": str(png_path), "html": str(html_path),
               "thumb": str(png_path), "sent_tg": bool(sent)}
    except Exception as e:
        yield {"type": "error", "message": str(e)}
```

- [ ] **Step 4: Run test to verify it passes**

Run: `python -m pytest tests/studio/test_studio_pipeline.py -v`
Expected: PASS (2 passed)

- [ ] **Step 5: Commit**

```bash
git add scripts/studio_pipeline.py tests/studio/test_studio_pipeline.py
git commit -m "feat(studio): 생성 파이프라인 + SSE 이벤트 + 이력 기록"
```

---

### Task 5: 서버 라우트 — /studio, SSE generate, gallery, regen-image

**Files:**
- Modify: `dashboard/server.py` (라우트 추가, 파일 끝 `uvicorn.run` 전)
- Test: `tests/studio/test_server_routes.py`

**Interfaces:**
- Consumes: `studio_pipeline.generate_briefing`, `dashboard/studio.html`(Task 6에서 생성 — 라우트는 파일 없으면 안내 문자열 반환)
- Produces: HTTP 라우트
  - `GET /studio` → studio.html
  - `POST /studio/generate?date=YYYY-MM-DD` → `text/event-stream` (각 이벤트 `data: {json}\n\n`)
  - `GET /studio/gallery` → `out/studio/index.json` 내용(JSON, 없으면 `[]`)
  - `GET /studio/file?path=...` → out/studio 내부 PNG 정적 서빙(미리보기용, 경로 탈출 차단)

- [ ] **Step 1: Write the failing test**

```python
# tests/studio/test_server_routes.py
from fastapi.testclient import TestClient
import dashboard.server as srv

def test_gallery_empty_returns_list(monkeypatch, tmp_path):
    monkeypatch.setattr(srv, "STUDIO_DIR", str(tmp_path))
    client = TestClient(srv.app)
    r = client.get("/studio/gallery")
    assert r.status_code == 200
    assert r.json() == []

def test_generate_streams_events(monkeypatch):
    def fake_gen(date):
        yield {"type": "step", "id": 1, "status": "done", "message": "x"}
        yield {"type": "done", "png": "a.png", "html": "a.html", "thumb": "a.png", "sent_tg": True}
    monkeypatch.setattr(srv, "generate_briefing", fake_gen)
    client = TestClient(srv.app)
    with client.stream("POST", "/studio/generate?date=2026-06-28") as r:
        body = "".join(chunk for chunk in r.iter_text())
    assert "data:" in body
    assert '"type": "done"' in body or '"type":"done"' in body
```

- [ ] **Step 2: Run test to verify it fails**

Run: `python -m pytest tests/studio/test_server_routes.py -v`
Expected: FAIL (`AttributeError`/라우트 404 또는 `generate_briefing` 미정의)

- [ ] **Step 3: Write minimal implementation**

`dashboard/server.py` 상단 import 영역에 추가:
```python
from fastapi import Request
from fastapi.responses import StreamingResponse, FileResponse
import sys as _sys
_sys.path.insert(0, os.path.join(ROOT, "scripts"))
from studio_pipeline import generate_briefing  # noqa: E402

STUDIO_DIR = os.path.join(ROOT, "out", "studio")
```

`uvicorn.run(...)` 직전(또는 다른 라우트들 아래)에 추가:
```python
# ── 딸깍 스튜디오 ─────────────────────────────────────────
@app.get("/studio", response_class=HTMLResponse)
def studio_page():
    p = os.path.join(HERE, "studio.html")
    if not os.path.exists(p):
        return "<h1>studio.html 준비중</h1>"
    with open(p, encoding="utf-8") as f:
        return f.read()


@app.get("/studio/gallery")
def studio_gallery():
    p = os.path.join(STUDIO_DIR, "index.json")
    if not os.path.exists(p):
        return JSONResponse(content=[])
    with open(p, encoding="utf-8") as f:
        return JSONResponse(content=json.load(f))


@app.post("/studio/generate")
def studio_generate(date: str):
    def event_stream():
        for ev in generate_briefing(date):
            yield f"data: {json.dumps(ev, ensure_ascii=False)}\n\n"
    return StreamingResponse(event_stream(), media_type="text/event-stream")


@app.get("/studio/file")
def studio_file(path: str):
    # out/studio 내부만 허용 (경로 탈출 차단)
    full = os.path.abspath(path)
    if not full.startswith(os.path.abspath(STUDIO_DIR)):
        return JSONResponse(content={"error": "허용되지 않은 경로"}, status_code=403)
    if not os.path.exists(full):
        return JSONResponse(content={"error": "없음"}, status_code=404)
    return FileResponse(full)
```

- [ ] **Step 4: Run test to verify it passes**

```bash
python -c "import fastapi.testclient" 2>NUL || pip install httpx
```
Run: `python -m pytest tests/studio/test_server_routes.py -v`
Expected: PASS (2 passed)

- [ ] **Step 5: Commit**

```bash
git add dashboard/server.py tests/studio/test_server_routes.py
git commit -m "feat(studio): /studio 페이지·SSE 생성·갤러리·파일 라우트"
```

---

### Task 6: studio.html — 트렌디 UI (갤러리 + 미리보기 + SSE 연출)

**Files:**
- Create: `dashboard/studio.html`
- Test: `tests/studio/test_studio_html.py`

**Interfaces:**
- Consumes: `GET /studio/gallery`, `POST /studio/generate`, `GET /studio/file?path=`
- Produces: 정적 HTML(검증은 핵심 요소/스크립트 존재 여부만)

- [ ] **Step 1: Write the failing test**

```python
# tests/studio/test_studio_html.py
from pathlib import Path

HTML = Path("dashboard/studio.html")

def test_html_has_core_structure():
    assert HTML.exists()
    s = HTML.read_text(encoding="utf-8")
    assert "작업목록" in s                       # 좌측 갤러리 제목
    assert "딸깍 생성" in s                       # 생성 버튼
    assert "/studio/generate" in s               # SSE 호출
    assert "/studio/gallery" in s                # 갤러리 로드
    assert "EventSource" in s or "ReadableStream" in s or "getReader" in s  # 스트림 처리
    assert "Instrument Serif" in s               # 디자인 토큰
```

- [ ] **Step 2: Run test to verify it fails**

Run: `python -m pytest tests/studio/test_studio_html.py -v`
Expected: FAIL (파일 없음)

- [ ] **Step 3: Write minimal implementation**

`dashboard/studio.html` 생성 (다크+골드, macOS 프레임, 2단 레이아웃, fetch 스트림으로 SSE 파싱, 단계 체크리스트 애니메이션, 완성 시 갤러리 갱신):
```html
<!doctype html><html lang="ko"><head><meta charset="utf-8">
<meta name="viewport" content="width=device-width, initial-scale=1">
<title>딸깍 스튜디오</title>
<link href="https://fonts.googleapis.com/css2?family=Instrument+Serif&family=Noto+Sans+KR:wght@400;700&display=swap" rel="stylesheet">
<style>
  :root{ --gold:#d4af37; --bg:#0e0e11; --panel:#17171c; --line:#26262e; }
  *{margin:0;padding:0;box-sizing:border-box;font-family:'Noto Sans KR',sans-serif;}
  body{background:#000;color:#eee;height:100vh;display:flex;align-items:center;justify-content:center;}
  .win{width:1120px;height:680px;background:var(--bg);border-radius:18px;overflow:hidden;
       box-shadow:0 30px 80px rgba(0,0,0,.6);display:flex;flex-direction:column;}
  .bar{height:44px;background:#121216;display:flex;align-items:center;gap:8px;padding:0 16px;border-bottom:1px solid var(--line);}
  .dot{width:12px;height:12px;border-radius:50%;}
  .r{background:#ff5f56;}.y{background:#ffbd2e;}.g{background:#27c93f;}
  .title{margin-left:12px;font-size:13px;color:#aaa;}
  .main{flex:1;display:flex;}
  .left{width:380px;border-right:1px solid var(--line);padding:22px;overflow:auto;}
  .left h2{font-size:14px;color:var(--gold);letter-spacing:1px;margin-bottom:16px;}
  .grid{display:grid;grid-template-columns:1fr 1fr;gap:12px;}
  .thumb{background:var(--panel);border-radius:12px;overflow:hidden;cursor:pointer;
         transition:transform .2s, box-shadow .2s;}
  .thumb:hover{transform:translateY(-4px);box-shadow:0 10px 24px rgba(0,0,0,.5);}
  .thumb img{width:100%;display:block;}
  .thumb .cap{padding:8px 10px;font-size:11px;color:#999;}
  .thumb.new{animation:drop .5s ease;}
  @keyframes drop{from{opacity:0;transform:translateY(-24px);}to{opacity:1;transform:none;}}
  .right{flex:1;padding:30px;display:flex;flex-direction:column;}
  .preview{flex:1;display:flex;align-items:center;justify-content:center;background:var(--panel);
           border-radius:16px;overflow:hidden;}
  .preview img{max-height:100%;border-radius:12px;}
  .preview .empty{color:#555;font-family:'Instrument Serif',serif;font-size:24px;}
  .steps{margin:20px 0;}
  .step{display:flex;align-items:center;gap:12px;padding:8px 0;color:#777;font-size:15px;}
  .step .ic{width:22px;height:22px;border-radius:50%;border:2px solid #333;display:flex;
            align-items:center;justify-content:center;font-size:12px;}
  .step.running{color:#fff;}.step.running .ic{border-color:var(--gold);color:var(--gold);}
  .step.done{color:#ddd;}.step.done .ic{background:var(--gold);border-color:var(--gold);color:#000;}
  .step.error{color:#ff6b6b;}.step.error .ic{border-color:#ff6b6b;color:#ff6b6b;}
  .btn{padding:16px;border:none;border-radius:14px;background:var(--gold);color:#000;
       font-size:17px;font-weight:700;cursor:pointer;transition:filter .15s;}
  .btn:hover{filter:brightness(1.08);}.btn:disabled{opacity:.5;cursor:wait;}
</style></head>
<body>
<div class="win">
  <div class="bar"><span class="dot r"></span><span class="dot y"></span><span class="dot g"></span>
    <span class="title">딸깍 스튜디오 — STOCK BRAIN</span></div>
  <div class="main">
    <div class="left"><h2>🎬 작업목록</h2><div class="grid" id="gallery"></div></div>
    <div class="right">
      <div class="preview" id="preview"><span class="empty">딸깍 한 번이면 끝</span></div>
      <div class="steps" id="steps">
        <div class="step" data-id="1"><span class="ic">1</span>수급·시황 데이터 수집</div>
        <div class="step" data-id="2"><span class="ic">2</span>AI 히로이미지 생성</div>
        <div class="step" data-id="3"><span class="ic">3</span>카드 디자인 렌더링</div>
        <div class="step" data-id="4"><span class="ic">4</span>이미지 추출(PNG)</div>
        <div class="step" data-id="5"><span class="ic">5</span>텔레그램 채널 전송</div>
      </div>
      <button class="btn" id="go">🟡 딸깍 생성</button>
    </div>
  </div>
</div>
<script>
const TODAY = new Date().toISOString().slice(0,10);
const fileUrl = p => "/studio/file?path=" + encodeURIComponent(p);

async function loadGallery(highlightFirst=false){
  const res = await fetch("/studio/gallery"); const items = await res.json();
  const g = document.getElementById("gallery"); g.innerHTML = "";
  items.forEach((it,i) => {
    const d = document.createElement("div");
    d.className = "thumb" + (highlightFirst && i===0 ? " new":"");
    d.innerHTML = `<img src="${fileUrl(it.thumb)}"><div class="cap">${it.date} · ${it.sent_tg?"전송✓":"저장"}</div>`;
    d.onclick = () => document.getElementById("preview").innerHTML = `<img src="${fileUrl(it.png)}">`;
    g.appendChild(d);
  });
  if(items[0]) document.getElementById("preview").innerHTML = `<img src="${fileUrl(items[0].png)}">`;
}

function setStep(id,status){
  const el = document.querySelector(`.step[data-id="${id}"]`);
  if(!el) return; el.classList.remove("running","done","error"); el.classList.add(status);
  if(status==="done") el.querySelector(".ic").textContent="✓";
}

document.getElementById("go").onclick = async () => {
  const btn = document.getElementById("go"); btn.disabled = true;
  document.querySelectorAll(".step").forEach(s=>{s.classList.remove("running","done","error");
    s.querySelector(".ic").textContent=s.dataset.id;});
  const resp = await fetch("/studio/generate?date="+TODAY, {method:"POST"});
  const reader = resp.body.getReader(); const dec = new TextDecoder(); let buf="";
  while(true){
    const {value,done} = await reader.read(); if(done) break;
    buf += dec.decode(value,{stream:true});
    let idx;
    while((idx = buf.indexOf("\n\n")) >= 0){
      const line = buf.slice(0,idx).trim(); buf = buf.slice(idx+2);
      if(!line.startsWith("data:")) continue;
      const ev = JSON.parse(line.slice(5).trim());
      if(ev.type==="step") setStep(ev.id, ev.status);
      else if(ev.type==="done") await loadGallery(true);
      else if(ev.type==="error") alert("오류: "+ev.message);
    }
  }
  btn.disabled = false;
};
loadGallery();
</script>
</body></html>
```

- [ ] **Step 4: Run test to verify it passes**

Run: `python -m pytest tests/studio/test_studio_html.py -v`
Expected: PASS (1 passed)

- [ ] **Step 5: Commit**

```bash
git add dashboard/studio.html tests/studio/test_studio_html.py
git commit -m "feat(studio): 트렌디 UI(갤러리+미리보기+SSE 단계연출)"
```

---

### Task 7: 통합 — 전체 테스트 + 실제 1회 생성 확인

**Files:**
- Create: `tests/studio/__init__.py` (빈 파일, 없으면 생성)
- Modify: 없음

- [ ] **Step 1: 패키지 인식 파일 보장**

`tests/studio/__init__.py`와 `tests/__init__.py`가 없으면 빈 파일로 생성.

- [ ] **Step 2: 전체 단위 테스트 통과 확인**

Run: `python -m pytest tests/studio/ -v`
Expected: 모든 테스트 PASS

- [ ] **Step 3: 서버 기동**

```bash
python dashboard/server.py
```
Expected: `http://localhost:8090` 기동. 브라우저에서 `http://localhost:8090/studio` 접속.

- [ ] **Step 4: 실제 딸깍 1회 (수동 검증)**

- `out/scenario_{오늘}.md`가 없으면 먼저 `python daily_scenario.py` 실행.
- `/studio`에서 "🟡 딸깍 생성" 클릭 → 5단계가 차례로 ✓ 되는지, 완성 카드가 갤러리에 슥 떨어지는지 확인.
- `out/studio/{오늘}_브리핑.png` 생성 확인.
- 텔레그램 채널에 카드 수신 확인(BOT_TOKEN/CHAT_ID 정상 시).
- Gemini 이미지: 실제 생성되면 히로 사진, 쿼터/오류면 골드 그라데이션(둘 다 정상).

- [ ] **Step 5: Commit**

```bash
git add tests/studio/__init__.py tests/__init__.py
git commit -m "test(studio): 패키지 초기화 + 통합 검증 완료"
```

---

## Self-Review

**1. Spec coverage**
- 목적/이중목적 → Task 4·6·7 ✓
- 첫 콘텐츠=브리핑 카드 → Task 1·3 ✓
- 하이브리드 생성+단계연출 → Task 4(SSE)·6(애니메이션) ✓
- 트렌디 UI(레퍼런스) → Task 6 ✓
- Gemini 히로이미지 + 폴백 → Task 2 ✓
- PNG 저장 + 텔레전송 → Task 4(viz_card 재사용) ✓
- 저장구조 out/studio + index.json → Task 4 ✓
- 에러/부분성공 → Task 4(텔레 실패 시 done.sent_tg=False) ✓
- 테스트 → 각 Task TDD + Task 7 통합 ✓
- 준비물 없음(.env 키 존재) → Global Constraints ✓

**2. Placeholder scan:** 모든 step에 실제 코드/명령 포함. TBD/TODO 없음. ✓

**3. Type consistency:**
- `get_briefing_data(date)->dict` 키(date/headline/lead_sectors/lines) — Task 2·3·4에서 동일 사용 ✓
- `generate_hero(prompt,out_path,api_key=None)->dict{ok,path,fallback}` — Task 4 호출부 일치 ✓
- `render_briefing_card(data,hero_path)->str`, `.card`/420px — `viz_card.save_png`의 `.card` 클립과 일치 ✓
- `generate_briefing(date)` yield 스키마(step/done/error) — Task 5 SSE·Task 6 JS 파싱 일치 ✓
- `viz_card.save_png(html_path,png_path)`, `send_telegram_photo(png_path,caption)` — 기존 시그니처 일치 ✓

> 주의: Windows에서 `2>NUL`은 cmd 기준. PowerShell이면 `2>$null`, Git Bash면 `2>/dev/null`로 대체.
