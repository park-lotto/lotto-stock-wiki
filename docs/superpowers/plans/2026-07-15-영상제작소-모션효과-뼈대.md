# 영상제작소 모션효과 파이프라인 뼈대 (Phase 1) — 구현 계획

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** shopping_shorts `/produce` 렌더에 "타임드 투명 레이어 N개 합성" 모션 엔진을 넣고, Remotion 자산생성기로 만든 전환·스티커를 ffmpeg overlay로 얹는 파이프라인 뼈대를 완성한다.

**Architecture:** 전환·스티커·(향후)키네틱타이포는 전부 "시작시간+길이 가진 투명 레이어"로 통일 — [video_assemble.py](../../../shopping_shorts/video_assemble.py) `_burn_captions`의 filter_complex에서 overlay 체인으로 합성. concat은 `-c copy` 유지(전환을 xfade가 아닌 오버레이로 처리해 재인코딩 회피). motion은 기존 `deco` dict 안 필드로 무임승차(store 스키마·신규 엔드포인트 불필요). 색감 필터만 base vf에 직접.

**Tech Stack:** Python 3 · FastAPI · ffmpeg(overlay/filter_complex) · pytest · Remotion(Node, 자산생성 전용)

**스펙 대비 단순화(승인됨):** ① store 변경 없음(motion은 deco_json blob에 포함). ② 신규 `/mix/motion` 엔드포인트 없음 — 기존 `/api/produce/mix/settings`의 `deco` 필드로 저장. ③ Python 엔진은 ffmpeg로 만든 **플레이스홀더 투명자산**으로 먼저 완결(Task 1~6, Node 무관) → 그 다음 Remotion으로 실자산 교체(Task 7). Node-on-서버 리스크가 앞 6개 태스크를 막지 않는다.

---

## 파일 구조

- Create `shopping_shorts/motion_assets.py` — 매니페스트 로드 + 레이어(asset_id) → 실경로·기본배치 해석. Node 무관 순수 Python.
- Create `shopping_shorts/motion_text.py` — `render_text_overlay(template, props, out_dir)` 스텁(텍스트 종속 렌더 인터페이스, Phase 2 실구현).
- Create `shopping_shorts/assets/motion/manifest.json` — 자산 카탈로그.
- Create `shopping_shorts/assets/motion/*.mov` — 자산(Task 6 플레이스홀더 → Task 7 Remotion 실자산).
- Create `shopping_shorts/motion/` — Remotion Node 프로젝트(package.json, src/, 렌더 스크립트).
- Modify `shopping_shorts/video_assemble.py` — `_motion_layer_filters` 빌더 추가 + `_burn_captions` 통합 + `color_filter`.
- Modify `shopping_shorts/mix_pipeline.py` `run_render` — `deco.motion.layers` asset_id → `_abspath` 해석.
- Test `shopping_shorts/tests/test_motion.py` — 신규 단위 테스트.

**테스트 실행 규약:** 모든 pytest는 리포지토리 루트에서 실행한다(`shopping_shorts.` 임포트 기준).
```
cd "C:/Users/CH/Desktop/로또의 주식"
python -m pytest shopping_shorts/tests/test_motion.py -v
```

---

## Task 1: 매니페스트 로드 + 레이어 해석 (motion_assets.py)

**Files:**
- Create: `shopping_shorts/motion_assets.py`
- Test: `shopping_shorts/tests/test_motion.py`

- [ ] **Step 1: 실패 테스트 작성**

`shopping_shorts/tests/test_motion.py` 신규 파일:

```python
import json
from pathlib import Path

from shopping_shorts.motion_assets import load_manifest, resolve_layers


def _make_assets(tmp_path):
    d = tmp_path / "motion"
    d.mkdir(parents=True)
    (d / "swipe_left.mov").write_bytes(b"\x00")   # 실물 있는 자산
    (d / "manifest.json").write_text(json.dumps({"assets": [
        {"id": "swipe_left", "type": "transition", "file": "swipe_left.mov",
         "default": {"width": 1080, "x": 50, "y": 50}},
        {"id": "sparkle", "type": "sticker", "file": "sparkle.mov",  # 파일 실물 없음
         "default": {"width": 300, "x": 50, "y": 40}},
    ]}, ensure_ascii=False), encoding="utf-8")
    return d


def test_load_manifest_indexes_by_id(tmp_path):
    d = _make_assets(tmp_path)
    m = load_manifest(str(d))
    assert m["swipe_left"]["type"] == "transition"
    assert m["swipe_left"]["default"]["width"] == 1080


def test_resolve_merges_default_and_abspath(tmp_path):
    d = _make_assets(tmp_path)
    # 사용자가 준 레이어: width만 재정의, 나머지는 default로 채워져야 함
    layers = [{"asset_id": "swipe_left", "start": 1.5, "dur": 0.6, "width": 720}]
    out = resolve_layers(layers, str(d))
    assert len(out) == 1
    L = out[0]
    assert L["_abspath"] == str(d / "swipe_left.mov")
    assert L["width"] == 720          # 레이어 값이 default를 이긴다
    assert L["x"] == 50 and L["y"] == 50   # default 채움
    assert L["start"] == 1.5 and L["dur"] == 0.6


def test_resolve_skips_unknown_and_missing_file(tmp_path):
    d = _make_assets(tmp_path)
    layers = [
        {"asset_id": "does_not_exist"},          # 매니페스트에 없음 → skip
        {"asset_id": "sparkle"},                 # 매니페스트엔 있으나 파일 실물 없음 → skip
    ]
    assert resolve_layers(layers, str(d)) == []
```

- [ ] **Step 2: 실패 확인**

Run: `cd "C:/Users/CH/Desktop/로또의 주식" && python -m pytest shopping_shorts/tests/test_motion.py -v`
Expected: FAIL — `ModuleNotFoundError: No module named 'shopping_shorts.motion_assets'`

- [ ] **Step 3: 최소 구현**

`shopping_shorts/motion_assets.py` 신규 파일:

```python
"""모션 자산 매니페스트 로드 + 레이어(asset_id) → 실경로·기본배치 해석.
Remotion(Node)과 무관한 순수 Python — 프리렌더된 자산 파일과 manifest.json만 읽는다."""
import json
import os
from pathlib import Path

# 기본 자산 폴더(리포 내 프리렌더 자산). 호출부가 override 가능.
DEFAULT_ASSETS_DIR = str(Path(__file__).parent / "assets" / "motion")


def load_manifest(assets_dir=DEFAULT_ASSETS_DIR):
    """assets_dir/manifest.json을 읽어 {asset_id: entry} dict로 인덱싱. 없으면 {}."""
    mpath = Path(assets_dir) / "manifest.json"
    if not mpath.exists():
        return {}
    data = json.loads(mpath.read_text(encoding="utf-8"))
    return {a["id"]: a for a in data.get("assets", []) if a.get("id")}


def resolve_layers(layers, assets_dir=DEFAULT_ASSETS_DIR):
    """deco.motion.layers(각 {asset_id, start?, dur?, x?, y?, width?, alpha?})를
    실제 합성 가능한 레이어 리스트로 해석한다.
    - 매니페스트의 default 배치를 채우되, 레이어가 준 값이 우선한다.
    - 실제 파일 경로를 '_abspath'로 붙인다.
    - 매니페스트에 없거나 파일 실물이 없는 asset_id는 조용히 skip(관용).
    """
    manifest = load_manifest(assets_dir)
    resolved = []
    for L in layers or []:
        aid = L.get("asset_id")
        entry = manifest.get(aid)
        if not entry:
            continue
        path = Path(assets_dir) / entry.get("file", "")
        if not path.exists():
            continue
        default = entry.get("default") or {}
        merged = {
            "asset_id": aid,
            "_abspath": str(path),
            "start": float(L.get("start") or 0),
            "dur": L.get("dur", None),   # None이면 전체 재생(enable 생략)
            "x": L.get("x", default.get("x", 50)),
            "y": L.get("y", default.get("y", 50)),
            "width": L.get("width", default.get("width")),
            "alpha": L.get("alpha", default.get("alpha", 1)),
        }
        resolved.append(merged)
    return resolved
```

- [ ] **Step 4: 통과 확인**

Run: `cd "C:/Users/CH/Desktop/로또의 주식" && python -m pytest shopping_shorts/tests/test_motion.py -v`
Expected: PASS (3 passed)

- [ ] **Step 5: 커밋**

동시세션 규칙: 커밋 전 `git status --short` 확인 → 내가 만든 파일만 add(`git add -A` 금지).
```bash
git add shopping_shorts/motion_assets.py shopping_shorts/tests/test_motion.py
git commit -m "feat(motion): 자산 매니페스트 로드 + 레이어 asset_id 해석(default병합·미존재 skip)"
```

---

## Task 2: 타임드 레이어 filter_complex 빌더 (_motion_layer_filters)

**Files:**
- Modify: `shopping_shorts/video_assemble.py` (신규 함수 추가, `_hex_to_ff` 근처 상단 헬퍼 영역)
- Test: `shopping_shorts/tests/test_motion.py`

- [ ] **Step 1: 실패 테스트 작성**

`shopping_shorts/tests/test_motion.py` 하단에 추가:

```python
from shopping_shorts.video_assemble import _motion_layer_filters


def test_motion_layer_filters_builds_overlay_chain():
    layers = [
        {"_abspath": "/a/swipe.mov", "start": 1.5, "dur": 0.6, "x": 50, "y": 50, "width": 720, "alpha": 1},
        {"_abspath": "/a/spark.mov", "start": 0, "dur": None, "x": 50, "y": 40, "width": 300, "alpha": 0.8},
    ]
    inputs, fc, vcur, nxt = _motion_layer_filters(layers, next_input_idx=1, vcur="v0")
    # 입력 2개가 인덱스 1,2로 추가된다
    assert inputs == ["-i", "/a/swipe.mov", "-i", "/a/spark.mov"]
    assert nxt == 3
    assert vcur == "mlv1"                       # 마지막 레이어 출력 라벨
    joined = ";".join(fc)
    # 첫 레이어: width 스케일 + enable(구간)
    assert "scale=720:-1" in joined
    assert "between(t,1.500,2.100)" in joined
    # 둘째 레이어: dur=None → enable 없음, alpha 0.8
    assert "colorchannelmixer=aa=0.80" in joined
    assert joined.count("enable=") == 1        # 전체재생 레이어는 enable 없음


def test_motion_layer_filters_empty_is_noop():
    inputs, fc, vcur, nxt = _motion_layer_filters([], next_input_idx=1, vcur="v0")
    assert inputs == [] and fc == [] and vcur == "v0" and nxt == 1
```

- [ ] **Step 2: 실패 확인**

Run: `cd "C:/Users/CH/Desktop/로또의 주식" && python -m pytest shopping_shorts/tests/test_motion.py -k motion_layer_filters -v`
Expected: FAIL — `ImportError: cannot import name '_motion_layer_filters'`

- [ ] **Step 3: 최소 구현**

`shopping_shorts/video_assemble.py`의 `_hex_to_ff` 함수 정의 바로 위(354행 근처)에 추가:

```python
def _motion_layer_filters(layers, next_input_idx, vcur):
    """해석완료 레이어 리스트(_abspath 보유) → filter_complex 조각.
    각 레이어를 vcur 위에 투명 오버레이로 얹는다. dur=None이면 enable 생략(전체 재생).
    반환: (input_args, fc_parts, vcur_out, next_input_idx_out).
    - input_args: ffmpeg에 추가할 ["-i", path, ...]
    - fc_parts: filter_complex 세미콜론 조각 리스트
    - vcur_out: 마지막 비디오 스트림 라벨(다음 필터가 이어받음)
    """
    input_args = []
    fc = []
    idx = next_input_idx
    for i, L in enumerate(layers or []):
        path = L.get("_abspath")
        if not path or not os.path.exists(path):
            continue
        input_args += ["-i", path]
        w = L.get("width")
        scale = f"scale={int(w)}:-1," if w else ""
        aa = max(0.0, min(1.0, float(L.get("alpha", 1))))
        xf = min(1.0, max(0.0, float(L.get("x", 50)) / 100.0))
        yf = min(1.0, max(0.0, float(L.get("y", 50)) / 100.0))
        lab, out = f"ml{i}", f"mlv{i}"
        fc.append(f"[{idx}:v]{scale}format=rgba,colorchannelmixer=aa={aa:.2f}[{lab}]")
        start = float(L.get("start") or 0)
        dur = L.get("dur")
        en = f":enable='between(t,{start:.3f},{start + float(dur):.3f})'" if dur is not None else ""
        fc.append(f"[{vcur}][{lab}]overlay=x=W*{xf:.4f}-w/2:y=H*{yf:.4f}-h/2{en}[{out}]")
        vcur = out
        idx += 1
    return input_args, fc, vcur, idx
```

주의: 이 함수는 `os` 모듈을 쓰므로 파일 상단에 이미 `import os`(13행)가 있는지 확인 — 있음.

- [ ] **Step 4: 통과 확인**

Run: `cd "C:/Users/CH/Desktop/로또의 주식" && python -m pytest shopping_shorts/tests/test_motion.py -k motion_layer_filters -v`
Expected: PASS (2 passed)

- [ ] **Step 5: 커밋**

```bash
git add shopping_shorts/video_assemble.py shopping_shorts/tests/test_motion.py
git commit -m "feat(motion): 타임드 투명레이어 filter_complex 빌더(_motion_layer_filters)"
```

---

## Task 3: _burn_captions에 모션 레이어 + 색감필터 통합

**Files:**
- Modify: `shopping_shorts/video_assemble.py` `_burn_captions` (약 576~613행)
- Test: `shopping_shorts/tests/test_motion.py`

현재 `_burn_captions`는 `has_bgm`/`has_overlay`가 하나라도 있을 때만 filter_complex 경로로 간다. 여기에 `has_motion`을 추가하고, base vf 끝에 `color_filter`를 덧붙인다.

- [ ] **Step 1: 실패 테스트 작성**

`_burn_captions`를 실제 ffmpeg 실행 없이 검증하려면 `_run_ffmpeg`를 monkeypatch해 명령을 가로챈다. `shopping_shorts/tests/test_motion.py` 하단에 추가:

```python
import shopping_shorts.video_assemble as va


def test_burn_captions_composites_motion_layers(tmp_path, monkeypatch):
    # 모션 자산 실물(빈 파일이어도 경로 존재하면 빌더가 포함)
    asset = tmp_path / "swipe.mov"
    asset.write_bytes(b"\x00")
    base = tmp_path / "base.mp4"
    base.write_bytes(b"\x00")

    captured = {}
    monkeypatch.setattr(va, "_run_ffmpeg", lambda cmd, **k: captured.setdefault("cmd", cmd))
    # ⚠️ _burn_captions는 폰트 미해결 시 조기 복사 후 return(538~540행) → 모션 코드에 도달 못 함.
    #    테스트를 실폰트에 의존시키지 않도록 폰트 해석을 강제하고 실제 복사는 no-op.
    monkeypatch.setattr(va, "_resolve_font", lambda: str(asset))
    monkeypatch.setattr(va.shutil, "copy", lambda *a, **k: None)
    # 폰트/자막 경로를 타지 않도록 자막 없는 최소 edit_plan
    edit_plan = {"beats": []}
    deco = {
        "motion": {
            "color_filter": "eq=saturation=1.2",
            "layers": [{"_abspath": str(asset), "start": 0.5, "dur": 0.6,
                        "x": 50, "y": 50, "width": 720, "alpha": 1}],
        }
    }
    out = tmp_path / "out.mp4"
    va._burn_captions(str(base), edit_plan, {}, str(out), tmp_path, None, None, deco)
    cmd = captured["cmd"]
    assert "-filter_complex" in cmd            # 모션 있으면 복합 경로
    fcx = cmd[cmd.index("-filter_complex") + 1]
    assert "eq=saturation=1.2" in fcx          # base vf에 색감필터 부착
    assert "overlay=" in fcx                   # 레이어 합성
    assert str(asset) in cmd                   # 자산이 입력으로 추가됨
```

- [ ] **Step 2: 실패 확인**

Run: `cd "C:/Users/CH/Desktop/로또의 주식" && python -m pytest shopping_shorts/tests/test_motion.py -k burn_captions -v`
Expected: FAIL — 현재 `-filter_complex`가 없음(모션 미지원, 단순 `-vf` 경로) → `KeyError`/assert 실패

- [ ] **Step 3: 구현 — _burn_captions 수정**

`shopping_shorts/video_assemble.py`에서 다음 블록(현재 576~586행)을 교체한다.

기존:
```python
    bgm = deco.get("bgm") or {}
    bgm_path = bgm.get("_abspath")
    has_bgm = bool(bgm_path and os.path.exists(bgm_path))
    overlay = deco.get("overlay") or {}
    ov_path = overlay.get("_abspath")
    has_overlay = bool(ov_path and os.path.exists(ov_path))
    if not has_bgm and not has_overlay:
        _run_ffmpeg(["ffmpeg", "-y", "-i", str(in_video), "-vf", vf, "-r", "30",
                     "-c:v", "libx264", "-c:a", "copy", "-pix_fmt", "yuv420p", str(out_path)],
                    cwd=str(work))
        return str(out_path)
    inputs = ["-i", str(in_video)]
    fc = [f"[0:v]{vf}[v0]"]
    vcur, idx = "v0", 1
```

교체 후:
```python
    bgm = deco.get("bgm") or {}
    bgm_path = bgm.get("_abspath")
    has_bgm = bool(bgm_path and os.path.exists(bgm_path))
    overlay = deco.get("overlay") or {}
    ov_path = overlay.get("_abspath")
    has_overlay = bool(ov_path and os.path.exists(ov_path))
    # 모션(전환·스티커 등 타임드 투명 레이어)과 색감 필터
    motion = deco.get("motion") or {}
    motion_layers = [L for L in (motion.get("layers") or []) if L.get("_abspath")]
    color_filter = (motion.get("color_filter") or "").strip()
    has_motion = bool(motion_layers)
    if not has_bgm and not has_overlay and not has_motion:
        base_vf = f"{vf},{color_filter}" if color_filter else vf
        _run_ffmpeg(["ffmpeg", "-y", "-i", str(in_video), "-vf", base_vf, "-r", "30",
                     "-c:v", "libx264", "-c:a", "copy", "-pix_fmt", "yuv420p", str(out_path)],
                    cwd=str(work))
        return str(out_path)
    inputs = ["-i", str(in_video)]
    base_vf = f"{vf},{color_filter}" if color_filter else vf
    fc = [f"[0:v]{base_vf}[v0]"]
    vcur, idx = "v0", 1
```

그리고 오버레이·BGM 블록 이후, `cmd = ["ffmpeg", ...]` 조립(현재 609행) **직전**에 모션 레이어 합성을 삽입한다. 현재 601행 `amap = None` 바로 위(즉 has_overlay 블록 끝난 뒤)에 다음을 추가:
```python
    if has_motion:                                    # 전환·스티커 등 타임드 투명 레이어
        m_inputs, m_fc, vcur, idx = _motion_layer_filters(motion_layers, idx, vcur)
        inputs += m_inputs
        fc += m_fc
```

- [ ] **Step 4: 통과 확인 + 회귀 확인**

Run: `cd "C:/Users/CH/Desktop/로또의 주식" && python -m pytest shopping_shorts/tests/test_motion.py shopping_shorts/tests/test_app_mix.py -v`
Expected: PASS (모션 신규 통과 + 기존 mix 테스트 회귀 없음)

- [ ] **Step 5: 커밋**

```bash
git add shopping_shorts/video_assemble.py shopping_shorts/tests/test_motion.py
git commit -m "feat(motion): _burn_captions에 타임드 레이어 합성 + 색감필터 통합(concat -c copy 유지)"
```

---

## Task 4: run_render가 deco.motion 레이어 경로 해석

**Files:**
- Modify: `shopping_shorts/mix_pipeline.py` `run_render` (약 187~199행, bgm/overlay _abspath 해석 뒤)
- Test: `shopping_shorts/tests/test_motion.py`

- [ ] **Step 1: 실패 테스트 작성**

`run_render`가 `deco.motion.layers`의 asset_id를 매니페스트로 해석해 `assemble`에 넘기는지, `assemble`을 monkeypatch해 검증. 하단에 추가:

```python
import json as _json
import shopping_shorts.mix_pipeline as mp
from shopping_shorts.store import Store


def test_run_render_resolves_motion_layers(tmp_path, monkeypatch):
    # 자산 폴더 + 매니페스트
    adir = tmp_path / "assets"
    adir.mkdir()
    (adir / "swipe_left.mov").write_bytes(b"\x00")
    (adir / "manifest.json").write_text(_json.dumps({"assets": [
        {"id": "swipe_left", "type": "transition", "file": "swipe_left.mov",
         "default": {"width": 1080, "x": 50, "y": 50}}]}), encoding="utf-8")
    # motion_assets가 이 폴더를 보도록 기본 경로 교체
    monkeypatch.setattr(mp, "MOTION_ASSETS_DIR", str(adir))

    db = tmp_path / "t.db"
    store = Store(db)
    store.create_mix_job("jm", ["u0"], 20, "free")
    store.update_mix_job("jm", status="ready_for_review",
        edit_plan={"structure": "free", "beats": [
            {"beat_idx": 0, "role": "훅", "narration": "n", "tts_path": str(tmp_path / "t.mp3")}]},
        deco={"motion": {"layers": [{"asset_id": "swipe_left", "start": 1.0, "dur": 0.5}]}})
    # 소스/ tts 실물 없이도 assemble 직전까지만 검증하도록 의존부 무력화
    work = tmp_path / "work" / "jm"; work.mkdir(parents=True)
    (work / "s0").mkdir(); (work / "s0" / "a.mp4").write_bytes(b"\x00")
    (tmp_path / "t.mp3").write_bytes(b"\x00")

    captured = {}
    monkeypatch.setattr(mp, "assemble", lambda *a, **k: captured.update(k) or "x")
    mp.run_render("jm", str(db), str(tmp_path / "work"))

    deco = captured["deco"]
    layer = deco["motion"]["layers"][0]
    assert layer["_abspath"].endswith("swipe_left.mov")   # 경로 해석됨
    assert layer["start"] == 1.0 and layer["dur"] == 0.5
```

- [ ] **Step 2: 실패 확인**

Run: `cd "C:/Users/CH/Desktop/로또의 주식" && python -m pytest shopping_shorts/tests/test_motion.py -k run_render_resolves_motion -v`
Expected: FAIL — `AttributeError: module ... has no attribute 'MOTION_ASSETS_DIR'` (아직 미도입)

- [ ] **Step 3: 구현 — run_render에 모션 해석 삽입**

`shopping_shorts/mix_pipeline.py` 상단 import에 추가(18행 `from shopping_shorts.video_assemble import assemble` 아래):
```python
from shopping_shorts.motion_assets import resolve_layers, DEFAULT_ASSETS_DIR

# 모션 자산 폴더(테스트가 monkeypatch로 교체 가능하도록 모듈 상수로 노출)
MOTION_ASSETS_DIR = DEFAULT_ASSETS_DIR
```

그리고 `run_render` 안에서 overlay `_abspath` 해석 블록(현재 194~198행) **바로 다음**, `assemble(...)` 호출 직전에 추가:
```python
        # 모션 레이어(전환·스티커): asset_id → 실경로·기본배치 해석
        motion = deco.get("motion") or {}
        if motion.get("layers"):
            resolved = resolve_layers(motion["layers"], MOTION_ASSETS_DIR)
            deco = {**deco, "motion": {**motion, "layers": resolved}}
```

- [ ] **Step 4: 통과 확인**

Run: `cd "C:/Users/CH/Desktop/로또의 주식" && python -m pytest shopping_shorts/tests/test_motion.py -k run_render_resolves_motion -v`
Expected: PASS

- [ ] **Step 5: 커밋**

```bash
git add shopping_shorts/mix_pipeline.py shopping_shorts/tests/test_motion.py
git commit -m "feat(motion): run_render가 deco.motion.layers asset_id를 실경로로 해석"
```

---

## Task 5: 텍스트 종속 렌더 스텁 (motion_text.py)

**Files:**
- Create: `shopping_shorts/motion_text.py`
- Test: `shopping_shorts/tests/test_motion.py`

Phase 2에서 붙일 per-video 키네틱타이포/콜아웃 렌더의 인터페이스만 확정한다. 뼈대에서는 호출 시 "미지원"을 명확히 반환해, 팩 단계 구현자가 시그니처를 그대로 채우게 한다.

- [ ] **Step 1: 실패 테스트 작성**

하단에 추가:
```python
from shopping_shorts.motion_text import render_text_overlay, TextRenderUnavailable
import pytest


def test_render_text_overlay_stub_raises(tmp_path):
    with pytest.raises(TextRenderUnavailable):
        render_text_overlay("KineticHook", {"text": "역대급세일"}, str(tmp_path))
```

- [ ] **Step 2: 실패 확인**

Run: `cd "C:/Users/CH/Desktop/로또의 주식" && python -m pytest shopping_shorts/tests/test_motion.py -k text_overlay_stub -v`
Expected: FAIL — `ModuleNotFoundError: shopping_shorts.motion_text`

- [ ] **Step 3: 최소 구현**

`shopping_shorts/motion_text.py`:
```python
"""텍스트 종속 모션 오버레이(키네틱 타이포·콜아웃)의 per-video 렌더 인터페이스.

Phase 1 뼈대에서는 스텁 — 서버 Node/헤드리스 크롬 가용성이 확인되면 Phase 2(팩)에서
Remotion `render:overlay`를 실제로 호출하도록 이 함수 본문을 채운다. 시그니처는 고정.
"""


class TextRenderUnavailable(RuntimeError):
    """텍스트 종속 오버레이 렌더가 아직 구성되지 않음(Node 미확인/미구현)."""


def render_text_overlay(template, props, out_dir):
    """template(예: 'KineticHook')과 props(예: {'text': ...})로 투명 오버레이 파일을
    렌더해 그 경로를 반환한다. Phase 1에서는 미구성이라 예외를 던진다.

    Phase 2 구현 방향:
      motion/ 프로젝트에서
      `npm run render:overlay -- --template=<template> --props=<json> --out=<out_dir>/x.webm`
      를 subprocess로 호출하고 out 경로를 반환.
    """
    raise TextRenderUnavailable(
        f"텍스트 종속 렌더 미구성(template={template!r}) — Phase 2에서 Remotion render:overlay 연결"
    )
```

- [ ] **Step 4: 통과 확인**

Run: `cd "C:/Users/CH/Desktop/로또의 주식" && python -m pytest shopping_shorts/tests/test_motion.py -v`
Expected: PASS (전체 통과)

- [ ] **Step 5: 커밋**

```bash
git add shopping_shorts/motion_text.py shopping_shorts/tests/test_motion.py
git commit -m "feat(motion): 텍스트종속 렌더 인터페이스 스텁(TextRenderUnavailable, Phase2 연결점)"
```

---

## Task 6: 플레이스홀더 자산 + 매니페스트 + end-to-end 육안 검증

Remotion 없이 ffmpeg만으로 투명 자산을 만들어 **엔진 전체가 실제로 합성되는지** 눈으로 확인한다(뼈대 완료 정의).

**Files:**
- Create: `shopping_shorts/assets/motion/manifest.json`
- Create: `shopping_shorts/assets/motion/ph_swipe.mov`, `ph_sparkle.mov` (플레이스홀더)
- Create(임시): `scratchpad`의 e2e 스크립트

- [ ] **Step 1: 자산 폴더 + 플레이스홀더 투명 MOV 생성**

qtrle 코덱은 알파를 보존하고 ffmpeg overlay가 안정적으로 처리한다. 리포 루트에서:
```bash
mkdir -p "shopping_shorts/assets/motion"
# 전환 플레이스홀더: 좌→우로 지나가는 반투명 흰 세로바(0.6s)
ffmpeg -y -f lavfi -i "color=c=white@0.0:s=720x1280:d=0.6:r=30" \
  -vf "format=rgba,drawbox=x='(t/0.6)*820-100':y=0:w=100:h=1280:color=white@0.85:t=fill" \
  -c:v qtrle "shopping_shorts/assets/motion/ph_swipe.mov"
# 스티커 플레이스홀더: 깜빡이는 노란 원(반짝이 대용, 1s)
ffmpeg -y -f lavfi -i "color=c=white@0.0:s=300x300:d=1:r=30" \
  -vf "format=rgba,drawbox=x=100:y=100:w=100:h=100:color=yellow@0.9:t=fill,fade=in:st=0:d=0.2:alpha=1" \
  -c:v qtrle "shopping_shorts/assets/motion/ph_sparkle.mov"
```
Expected: 두 .mov 파일 생성(각 수십~수백 KB).

- [ ] **Step 2: 매니페스트 작성**

`shopping_shorts/assets/motion/manifest.json`:
```json
{
  "assets": [
    {"id": "ph_swipe", "type": "transition", "file": "ph_swipe.mov",
     "default": {"width": 720, "x": 50, "y": 50}},
    {"id": "ph_sparkle", "type": "sticker", "file": "ph_sparkle.mov",
     "default": {"width": 300, "x": 50, "y": 40}}
  ]
}
```

- [ ] **Step 3: e2e 합성 스크립트 작성·실행**

`scratchpad`(세션 임시폴더)에 `e2e_motion.py` 작성 — 단색 베이스 영상 하나를 만들고, motion 레이어 2개를 얹어 최종 mp4를 뽑는다:
```python
import os, subprocess
from pathlib import Path
from shopping_shorts.motion_assets import resolve_layers, DEFAULT_ASSETS_DIR
from shopping_shorts import video_assemble as va

work = Path(os.environ["SCRATCH"]) / "asm"; work.mkdir(parents=True, exist_ok=True)
base = work / "base.mp4"
# 3초짜리 파란 베이스 + 무음 오디오(_burn_captions는 -c:a copy를 기대)
subprocess.run(["ffmpeg","-y","-f","lavfi","-i","color=c=blue:s=720x1280:d=3:r=30",
                "-f","lavfi","-i","anullsrc=r=44100:cl=stereo","-t","3",
                "-c:v","libx264","-c:a","aac","-pix_fmt","yuv420p",str(base)], check=True)
layers = resolve_layers([
    {"asset_id":"ph_swipe","start":1.0,"dur":0.6},          # 1.0s에 전환
    {"asset_id":"ph_sparkle","start":0.2,"dur":1.0,"x":50,"y":30},
], DEFAULT_ASSETS_DIR)
deco = {"motion": {"layers": layers, "color_filter": "eq=saturation=1.15"}}
out = work / "final.mp4"
va._burn_captions(str(base), {"beats":[]}, {}, str(out), work, None, None, deco)
# 전환 순간(1.2s)·스티커 순간(0.5s) 프레임 추출
subprocess.run(["ffmpeg","-y","-ss","1.2","-i",str(out),"-frames:v","1",str(work/"f_transition.png")], check=True)
subprocess.run(["ffmpeg","-y","-ss","0.5","-i",str(out),"-frames:v","1",str(work/"f_sticker.png")], check=True)
print("OUT", out)
print("FRAMES", work/"f_transition.png", work/"f_sticker.png")
```
실행:
```bash
cd "C:/Users/CH/Desktop/로또의 주식"
SCRATCH="C:/Users/CH/AppData/Local/Temp/claude/c--Users-CH-Desktop-------/ad42519e-4d1e-4a65-95c5-6ebedf74b7cc/scratchpad" \
  python "$SCRATCH/e2e_motion.py"
```
Expected: 에러 없이 `final.mp4` + 두 PNG 생성.

- [ ] **Step 4: 육안 검증(증거)**

Read 툴로 `f_transition.png`(파란 화면에 흰 세로바가 보여야 함)와 `f_sticker.png`(파란 화면 상단에 노란 사각형)를 **이미지로 열어 실제 오버레이가 찍혔는지 확인**한다. 색감필터(채도↑)로 파랑이 더 쨍한지도 함께 본다.
- 전환바·스티커가 **둘 다 보이면 뼈대 합성 검증 통과.**
- 안 보이면: (a) qtrle 알파가 overlay에서 안 먹었는지 `-filter_complex` 문자열 확인, (b) enable 구간 시간이 추출 시각과 겹치는지 확인, (c) **폰트 미해결로 `_burn_captions`가 조기 복사(538~540행)했는지** — `_resolve_font()`가 `static/fonts`를 찾는지 확인(못 찾으면 모션·오버레이·BGM 전부 드롭됨. 이건 기존 overlay/bgm과 공유하는 한계 — 프로덕션엔 폰트 22종 설치돼 정상).

- [ ] **Step 5: 커밋**

```bash
git add shopping_shorts/assets/motion/manifest.json \
        shopping_shorts/assets/motion/ph_swipe.mov \
        shopping_shorts/assets/motion/ph_sparkle.mov
git commit -m "feat(motion): 플레이스홀더 투명자산(qtrle) + 매니페스트, e2e 합성 육안검증 통과"
```
(scratchpad의 e2e 스크립트·PNG는 커밋하지 않는다.)

---

## Task 7: Remotion 자산 생성기 → 실자산(스와이프·반짝이) 교체

플레이스홀더를 빅채널급 실자산으로 교체. Remotion Node 프로젝트를 세우고 `render:library`로 투명 webm/mov를 뽑는다.

> **선행 확인:** `node --version`이 되는 환경에서 수행. 서버 배포는 이 자산이 **git에 프리렌더 파일로 커밋**되므로 서버에 Node 불필요(런타임 Node 의존 0). Node가 로컬에도 없으면 이 태스크는 보류하고 Task 6 플레이스홀더로 뼈대를 유지한다.

**Files:**
- Create: `shopping_shorts/motion/package.json`, `remotion.config.ts`, `src/Root.tsx`, `src/SwipeLeft.tsx`, `src/Sparkle.tsx`, `src/render-library.mjs`
- Modify: `shopping_shorts/assets/motion/manifest.json` (실자산 id 추가), 자산 파일 교체

- [ ] **Step 1: Remotion 프로젝트 스캐폴드**

`shopping_shorts/motion/package.json`:
```json
{
  "name": "shopping-shorts-motion",
  "private": true,
  "type": "module",
  "scripts": {
    "render:library": "node src/render-library.mjs"
  },
  "dependencies": {
    "@remotion/cli": "^4.0.0",
    "@remotion/renderer": "^4.0.0",
    "remotion": "^4.0.0",
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  }
}
```
설치: `cd shopping_shorts/motion && npm install` (Expected: node_modules 생성, 에러 없음).
`.gitignore`에 `shopping_shorts/motion/node_modules/` 추가.

- [ ] **Step 2: 전환 컴포지션 (SwipeLeft) — 투명 배경**

`shopping_shorts/motion/src/SwipeLeft.tsx`:
```tsx
import {AbsoluteFill, interpolate, useCurrentFrame, useVideoConfig} from 'remotion';

export const SwipeLeft: React.FC = () => {
  const frame = useCurrentFrame();
  const {durationInFrames, width} = useVideoConfig();
  // 화면 밖(오른쪽)에서 들어와 왼쪽으로 빠지는 밝은 바
  const x = interpolate(frame, [0, durationInFrames], [width, -width * 0.3]);
  return (
    <AbsoluteFill style={{backgroundColor: 'transparent'}}>
      <div style={{position: 'absolute', left: x, top: 0, width: width * 0.35,
        height: '100%', background: 'linear-gradient(90deg,rgba(255,255,255,0),#fff,rgba(255,255,255,0))',
        filter: 'blur(2px)', opacity: 0.9}} />
    </AbsoluteFill>
  );
};
```

- [ ] **Step 3: 스티커 컴포지션 (Sparkle) — 투명 배경**

`shopping_shorts/motion/src/Sparkle.tsx`:
```tsx
import {AbsoluteFill, interpolate, spring, useCurrentFrame, useVideoConfig} from 'remotion';

export const Sparkle: React.FC = () => {
  const frame = useCurrentFrame();
  const {fps} = useVideoConfig();
  const s = spring({frame, fps, config: {damping: 8}});
  const rot = interpolate(frame, [0, 30], [0, 45]);
  return (
    <AbsoluteFill style={{justifyContent: 'center', alignItems: 'center', backgroundColor: 'transparent'}}>
      <div style={{fontSize: 180, transform: `scale(${s}) rotate(${rot}deg)`}}>✨</div>
    </AbsoluteFill>
  );
};
```

- [ ] **Step 4: 루트 등록 + 라이브러리 렌더 스크립트**

`shopping_shorts/motion/src/Root.tsx`:
```tsx
import {Composition} from 'remotion';
import {SwipeLeft} from './SwipeLeft';
import {Sparkle} from './Sparkle';

export const RemotionRoot: React.FC = () => (
  <>
    <Composition id="SwipeLeft" component={SwipeLeft} durationInFrames={18} fps={30} width={720} height={1280} />
    <Composition id="Sparkle" component={Sparkle} durationInFrames={30} fps={30} width={300} height={300} />
  </>
);
```
`shopping_shorts/motion/remotion.config.ts`:
```ts
import {Config} from '@remotion/cli/config';
Config.setVideoImageFormat('png');   // 알파 보존
```
`shopping_shorts/motion/src/index.ts`:
```ts
import {registerRoot} from 'remotion';
import {RemotionRoot} from './Root';
registerRoot(RemotionRoot);
```
`shopping_shorts/motion/src/render-library.mjs` — 각 컴포지션을 알파 webm으로 렌더해 자산 폴더에 저장:
```js
import {bundle} from '@remotion/bundler';
import {renderMedia, selectComposition} from '@remotion/renderer';
import path from 'path';
import {fileURLToPath} from 'url';

const here = path.dirname(fileURLToPath(import.meta.url));
const outDir = path.resolve(here, '..', '..', 'assets', 'motion');
const LIB = [
  {id: 'SwipeLeft', file: 'swipe_left.webm'},
  {id: 'Sparkle', file: 'sparkle.webm'},
];

const serveUrl = await bundle({entryPoint: path.join(here, 'index.ts')});
for (const {id, file} of LIB) {
  const composition = await selectComposition({serveUrl, id});
  await renderMedia({
    composition, serveUrl,
    codec: 'vp9', pixelFormat: 'yuva420p',   // 알파 채널 webm
    outputLocation: path.join(outDir, file),
  });
  console.log('rendered', file);
}
```

- [ ] **Step 5: 라이브러리 렌더 실행**

```bash
cd "C:/Users/CH/Desktop/로또의 주식/shopping_shorts/motion" && npm run render:library
```
Expected: `assets/motion/swipe_left.webm`, `sparkle.webm` 생성, "rendered ..." 로그 2줄.

- [ ] **Step 6: 매니페스트에 실자산 등록**

`shopping_shorts/assets/motion/manifest.json`의 `assets`에 추가(플레이스홀더는 남겨두거나 제거 — 여기선 실자산을 정식 id로 추가):
```json
{"id": "swipe_left", "type": "transition", "file": "swipe_left.webm",
 "default": {"width": 720, "x": 50, "y": 50}},
{"id": "sparkle", "type": "sticker", "file": "sparkle.webm",
 "default": {"width": 300, "x": 50, "y": 40}}
```

- [ ] **Step 7: 실자산으로 e2e 재검증(육안)**

Task 6의 e2e 스크립트에서 asset_id를 `swipe_left`/`sparkle`로 바꿔 재실행 → 프레임 PNG를 Read로 열어 **VP9 알파 webm 오버레이가 정상 합성되는지** 확인.
- webm 알파가 overlay에서 검게 나오면: ffmpeg 입력에 `-c:v libvpx-vp9`가 자동 선택되는지, `format=rgba`가 빌더에 있는지 확인(빌더엔 이미 있음). 필요 시 매니페스트를 qtrle .mov 출력으로 전환(`codec:'prores', ...` 또는 렌더 후 ffmpeg로 mov 변환)해 대체.

- [ ] **Step 8: 커밋**

```bash
git add shopping_shorts/motion shopping_shorts/assets/motion/manifest.json \
        shopping_shorts/assets/motion/swipe_left.webm shopping_shorts/assets/motion/sparkle.webm \
        .gitignore
git commit -m "feat(motion): Remotion 자산생성기(render:library) + 실전환·스티커 자산, e2e 재검증"
```

---

## Task 8: 문서·로그 마무리

**Files:**
- Modify: `wiki/log.md`, `shopping_shorts/영상제작_NEXT.md`

- [ ] **Step 1: log.md 1~2줄 기록**

`wiki/log.md`에 추가:
```
- 2026-07-15 영상제작소 모션효과 뼈대(Phase1) 완성: 타임드 투명레이어 합성엔진(_motion_layer_filters)+매니페스트 해석+run_render 배선+Remotion 자산생성기. 전환=오버레이(concat -c copy 유지). 다음: Phase2 프리셋 팩(전환·타이포·콜아웃·색감).
```

- [ ] **Step 2: 영상제작_NEXT.md에 Phase2 이월 갱신**

`## ⏭ 남은 것`에 추가:
```
- 모션효과 Phase2: 프리셋 팩 정의(전환·키네틱타이포·콜아웃·색감 세트) + per-video 텍스트 렌더(motion_text.render_text_overlay 실구현, 서버 Node 확인 필요) + 위저드 팩 선택 UI(딸깍 통합)
```

- [ ] **Step 3: 전체 테스트 최종 확인**

Run: `cd "C:/Users/CH/Desktop/로또의 주식" && python -m pytest shopping_shorts/tests/ -v`
Expected: 전체 PASS(기존 회귀 없음 + 모션 신규 통과).

- [ ] **Step 4: 커밋 + 푸시**

```bash
git add wiki/log.md shopping_shorts/영상제작_NEXT.md
git commit -m "docs(motion): Phase1 뼈대 완료 로그 + Phase2 이월 기록"
git pull --rebase origin main
git push origin main
```

---

## 최종 검증 체크리스트(구현자용)

- [ ] `python -m pytest shopping_shorts/tests/ -v` 전체 PASS
- [ ] Task 6/7 e2e 프레임 PNG에서 전환·스티커가 **육안으로 보임**(렌더 산출물 실제 구동 확인)
- [ ] concat은 `-c copy` 그대로(재인코딩 미도입 — `_render_mix` 무변경 확인)
- [ ] 모션 없는 기존 job 렌더 경로 회귀 없음(has_motion=False 시 기존 동작)
- [ ] 매니페스트에 없는/파일없는 asset_id는 skip(에러 아님)
