# 유튜브 카테고리 프리셋 수집 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax.

**Goal:** 키워드를 안 치고 딸깍 한 번에 카테고리별 인기 Shorts를 조회수 랭킹으로 — 전체/카테고리별/개별 3모드.

**Architecture:** 카테고리별 큐레이션 키워드를 코드에 내장(`youtube_category_presets.py`). `_collect_youtube(categories)`가 프리셋 키워드를 기존 `search_shorts`(lang=ko) 경로로 흘려, 선행 채널기반 수집의 병합·video_id 중복제거 파이프라인을 그대로 탄다. app 엔드포인트에 `category` 파라미터, index.html에 수집 컨트롤 3개.

**Tech Stack:** Python, FastAPI, requests(YouTube Data API), pytest, vanilla JS.

**설계문서:** `docs/superpowers/specs/2026-07-25-유튜브-카테고리프리셋수집-design.md`

**카테고리 key(기존 CTYPES와 1:1):** `홈템` `비법형` `제품형` `혼합형`(뷰티) `기타`

---

## File Structure

- `shopping_shorts/youtube_category_presets.py` — 신규: `CATEGORY_KEYWORDS` dict + `preset_keywords(categories=None)`
- `shopping_shorts/service.py` — `_collect_youtube(categories=None)` + `collect(..., categories=None)`
- `shopping_shorts/app.py` — `api_collect`에 `category` 쿼리 파라미터
- `shopping_shorts/static/index.html` — 수집 컨트롤 3개 + `doCollect(category)`
- 테스트: `tests/test_youtube_presets.py`(신규), `tests/test_collect_youtube.py`(수정), `tests/test_app_collect_category.py`(신규)

---

## Task 1: 프리셋 데이터 모듈

**Files:**
- Create: `shopping_shorts/youtube_category_presets.py`
- Test: `shopping_shorts/tests/test_youtube_presets.py` (신규)

- [ ] **Step 1: Write the failing test**

`shopping_shorts/tests/test_youtube_presets.py`:

```python
from shopping_shorts import youtube_category_presets as p


def test_category_keys_match_ctypes():
    # UI 버튼 key(CTYPES)와 1:1 — 빠지거나 남으면 '전체=버튼 합'이 깨진다
    assert set(p.CATEGORY_KEYWORDS) == {"홈템", "비법형", "제품형", "혼합형", "기타"}


def test_every_category_has_keywords():
    for key, kws in p.CATEGORY_KEYWORDS.items():
        assert kws and all(isinstance(k, str) and k.strip() for k in kws), key


def test_preset_keywords_all():
    # None → 전 카테고리 union, 중복 없음
    allk = p.preset_keywords(None)
    flat = [k for kws in p.CATEGORY_KEYWORDS.values() for k in kws]
    assert set(allk) == set(flat)
    assert len(allk) == len(set(allk))          # 중복 제거됨


def test_preset_keywords_scoped():
    assert p.preset_keywords(["혼합형"]) == p.CATEGORY_KEYWORDS["혼합형"]


def test_preset_keywords_unknown_key():
    assert p.preset_keywords(["없는카테고리"]) == []
```

- [ ] **Step 2: Run test, verify FAIL**

Run: `cd "c:/Users/CH/Desktop/로또의 주식/.tracks/유튜브채널수집" && python -m pytest shopping_shorts/tests/test_youtube_presets.py -v`
Expected: FAIL (module not found).

- [ ] **Step 3: Write implementation**

`shopping_shorts/youtube_category_presets.py`:

```python
"""유튜브 카테고리별 프리셋 키워드 — 딸깍 수집용(사용자가 안 쳐도 되게 코드 내장).

키는 index.html CTYPES 버튼 key와 1:1(홈템/비법형/제품형/혼합형/기타). v1 한국어(lang=ko).
키워드는 초안 — 수집 결과 보고 조정한다. 편집 UI·언어별은 범위 밖(YAGNI).
"""

CATEGORY_KEYWORDS = {
    "홈템":   ["살림템", "주방템", "정리수납", "생활꿀템", "청소템"],
    "비법형": ["살림꿀팁", "청소꿀팁", "요리꿀팁", "자취꿀팁", "생활꿀팁"],
    "제품형": ["다이소추천", "쿠팡추천", "주방용품", "살림추천템"],
    "혼합형": ["뷰티꿀팁", "메이크업", "화장법", "다이소뷰티"],
    "기타":   ["자취요리", "냉장고정리", "집꾸미기"],
}


def preset_keywords(categories=None):
    """스코프에 맞는 프리셋 키워드 리스트(중복 제거, 순서 보존).

    categories=None → 전 카테고리 union / ["혼합형"] → 그 카테고리만 /
    알 수 없는 key는 조용히 무시(빈 기여)."""
    keys = list(CATEGORY_KEYWORDS) if categories is None else categories
    out, seen = [], set()
    for key in keys:
        for kw in CATEGORY_KEYWORDS.get(key, []):
            if kw not in seen:
                seen.add(kw)
                out.append(kw)
    return out
```

- [ ] **Step 4: Run test, verify PASS**

Run: `cd "c:/Users/CH/Desktop/로또의 주식/.tracks/유튜브채널수집" && python -m pytest shopping_shorts/tests/test_youtube_presets.py -v`
Expected: 5 passed.

- [ ] **Step 5: Commit**

```bash
git -C "c:/Users/CH/Desktop/로또의 주식/.tracks/유튜브채널수집" add shopping_shorts/youtube_category_presets.py shopping_shorts/tests/test_youtube_presets.py
git -C "c:/Users/CH/Desktop/로또의 주식/.tracks/유튜브채널수집" commit -m "feat(youtube): 카테고리 프리셋 키워드 데이터 + preset_keywords 헬퍼"
```

---

## Task 2: `_collect_youtube(categories)` + `collect` 프리셋 배선

**Files:**
- Modify: `shopping_shorts/service.py` (import + `_collect_youtube` + `collect`)
- Test: `shopping_shorts/tests/test_collect_youtube.py` (기존 2개 수정 + 신규 2개)

**주의(기존 테스트 깨짐 방지):** 기존 `test_collect_youtube`·`test_collect_youtube_merges_account_and_keyword`는
`service.collect(platform="youtube")`를 부른다. 이제 이 호출은 **프리셋 전체**도 검색하므로,
`fake_search`의 `assert keywords==["살림꿀팁"]`가 프리셋 키워드 때문에 깨진다. 두 기존 테스트는
**프리셋을 빈 리스트로 monkeypatch**해 수동시드 동작만 격리한다(아래 Step 1에서 반영).

- [ ] **Step 1: Write/'update the failing tests**

`shopping_shorts/tests/test_collect_youtube.py`를 아래로 만든다(기존 2개에 프리셋 격리 한 줄씩 추가 + 신규 2개):

```python
from shopping_shorts import service


def test_collect_youtube(monkeypatch, tmp_path):
    monkeypatch.setattr(service, "DB_PATH", str(tmp_path / "t.db"))
    monkeypatch.setattr(service, "preset_keywords", lambda categories=None: [])  # 프리셋 격리
    from shopping_shorts.store import Store
    Store(str(tmp_path / "t.db")).add_seed("youtube", "keyword", "살림꿀팁")

    def fake_search(keywords, published_after_iso, **kw):
        assert keywords == ["살림꿀팁"]
        return [{"video_id": "v1", "channel_title": "살림TV", "channel_id": "c1",
                 "title": "t", "thumbnail": "x",
                 "published_at": "2026-07-24T00:00:00Z",
                 "views": 24000, "likes": 100, "comments": 10}]
    monkeypatch.setattr(service, "yt_search", fake_search)

    items = service.collect(platform="youtube")
    assert len(items) == 1
    assert items[0]["platform"] == "youtube" and items[0]["base_count"] == 24000
    assert "grade" in items[0] and "score" in items[0]


def test_collect_youtube_merges_account_and_keyword(monkeypatch, tmp_path):
    monkeypatch.setattr(service, "DB_PATH", str(tmp_path / "t.db"))
    monkeypatch.setattr(service, "preset_keywords", lambda categories=None: [])  # 프리셋 격리
    from shopping_shorts.store import Store
    st = Store(str(tmp_path / "t.db"))
    st.add_seed("youtube", "keyword", "살림꿀팁")
    st.add_seed("youtube", "account", "@salim")

    def fake_search(keywords, published_after_iso, **kw):
        return [
            {"video_id": "v1", "channel_title": "A", "channel_id": "c1", "title": "t",
             "thumbnail": "x", "published_at": "2026-07-24T00:00:00Z",
             "views": 1000, "likes": 5, "comments": 1},
            {"video_id": "dup", "channel_title": "A", "channel_id": "c1", "title": "t",
             "thumbnail": "x", "published_at": "2026-07-24T00:00:00Z",
             "views": 1000, "likes": 5, "comments": 1},
        ]
    monkeypatch.setattr(service, "yt_search", fake_search)

    def fake_channel(seed, **kw):
        assert seed == "@salim"
        return [
            {"video_id": "v2", "channel_title": "B", "channel_id": "c2", "title": "t",
             "thumbnail": "x", "published_at": "2026-07-24T00:00:00Z",
             "views": 2000, "likes": 9, "comments": 3},
            {"video_id": "dup", "channel_title": "B", "channel_id": "c2", "title": "t",
             "thumbnail": "x", "published_at": "2026-07-24T00:00:00Z",
             "views": 2000, "likes": 9, "comments": 3},
        ]
    monkeypatch.setattr(service, "yt_fetch_channel", fake_channel)

    items = service.collect(platform="youtube")
    ids = sorted(i["shortcode"] for i in items)
    assert ids == ["dup", "v1", "v2"]


def test_collect_youtube_preset_all(monkeypatch, tmp_path):
    # categories=None(전체 딸깍) → 프리셋 전 키워드가 lang=ko로 검색된다
    monkeypatch.setattr(service, "DB_PATH", str(tmp_path / "t.db"))
    monkeypatch.setattr(service, "preset_keywords",
                        lambda categories=None: ["살림템", "메이크업"] if categories is None else [])
    seen = {}
    def fake_search(keywords, published_after_iso, **kw):
        seen["keywords"] = keywords
        seen["lang"] = kw.get("lang")
        return [{"video_id": "p1", "channel_title": "T", "channel_id": "c",
                 "title": "t", "thumbnail": "x",
                 "published_at": "2026-07-24T00:00:00Z",
                 "views": 500, "likes": 1, "comments": 0}]
    monkeypatch.setattr(service, "yt_search", fake_search)

    items = service.collect(platform="youtube")   # category 없음 = 전체
    assert seen["keywords"] == ["살림템", "메이크업"] and seen["lang"] == "ko"
    assert len(items) == 1 and items[0]["shortcode"] == "p1"


def test_collect_youtube_preset_scoped(monkeypatch, tmp_path):
    # categories=["혼합형"] → 그 카테고리 프리셋만
    monkeypatch.setattr(service, "DB_PATH", str(tmp_path / "t.db"))
    monkeypatch.setattr(service, "preset_keywords",
                        lambda categories=None: ["뷰티꿀팁"] if categories == ["혼합형"] else ["WRONG"])
    seen = {}
    def fake_search(keywords, published_after_iso, **kw):
        seen["keywords"] = keywords
        return []
    monkeypatch.setattr(service, "yt_search", fake_search)

    service.collect(platform="youtube", categories=["혼합형"])
    assert seen["keywords"] == ["뷰티꿀팁"]
```

- [ ] **Step 2: Run tests, verify the NEW ones FAIL**

Run: `cd "c:/Users/CH/Desktop/로또의 주식/.tracks/유튜브채널수집" && python -m pytest shopping_shorts/tests/test_collect_youtube.py -v`
Expected: `test_collect_youtube_preset_all`/`_scoped` FAIL (collect has no `categories` / preset not wired). The two isolated tests may already pass or error on `service.preset_keywords` not existing — that's fine, implementation adds it.

- [ ] **Step 3: Write implementation**

(a) `service.py` import line (현재 `from shopping_shorts.youtube_client import search_shorts as yt_search, fetch_channel_shorts as yt_fetch_channel`) 아래(또는 근처)에 추가:

```python
from shopping_shorts.youtube_category_presets import preset_keywords
```

(b) `def collect(platform="instagram", limit_channels=None):` 시그니처를 바꾸고 youtube 분기에 categories 전달:

```python
def collect(platform="instagram", categories=None, limit_channels=None):
```
그리고 그 함수 안 youtube 분기:
```python
    if platform == "youtube":
        return _collect_youtube(categories)
```
(tiktok/instagram 분기는 그대로 — categories 무시)

(c) `_collect_youtube`를 아래로 교체:

```python
def _collect_youtube(categories=None):
    """유튜브 카테고리 프리셋 + 수동 키워드 시드(발굴) + 계정 시드(채널기반) → 조회수 랭킹.

    categories=None → 프리셋 전 카테고리 union(전체 딸깍) / ["혼합형"] → 그 카테고리만.
    프리셋은 lang=ko로 검색. 수동 키워드 시드(kind=언어코드)·계정 시드는 항상 함께 수집.
    세 경로 raw를 video_id로 중복제거 후 공통 파이프라인."""
    store = Store(DB_PATH)
    seeds = store.list_seeds("youtube")
    _LANGS = {"ko", "en", "ja", "zh", "ru"}
    by_lang = {}
    accounts = []
    for s in seeds:
        if s["kind"] == "account":
            accounts.append(s["value"])
        else:
            lang = s["kind"] if s["kind"] in _LANGS else "ko"
            by_lang.setdefault(lang, []).append(s["value"])
    now = datetime.now(timezone.utc)
    after = (now - timedelta(hours=YOUTUBE_WINDOW_HOURS)).strftime("%Y-%m-%dT%H:%M:%SZ")
    raw = []
    # 카테고리 프리셋(한국어) — 딸깍 수집의 핵심
    preset = preset_keywords(categories)
    if preset:
        raw.extend(yt_search(preset, after, max_per_kw=YOUTUBE_MAX_PER_KW, lang="ko"))
    # 수동 키워드 시드(언어별)
    for lang, kws in by_lang.items():
        raw.extend(yt_search(kws, after, max_per_kw=YOUTUBE_MAX_PER_KW, lang=lang))
    # 계정 시드(채널기반)
    for acc in accounts:
        raw.extend(yt_fetch_channel(acc, cache_get=store.yt_cache_get,
                                    cache_put=store.yt_cache_put))
    # video_id 중복제거(프리셋·수동·계정 겹칠 수 있음) — 첫 등장 우선
    seen, deduped = set(), []
    for r in raw:
        vid = r.get("video_id")
        if not vid or vid in seen:
            continue
        seen.add(vid)
        deduped.append(r)
    items = build_youtube_items(
        deduped,
        prev_base=lambda sc: store.prev_base_platform("youtube", sc),
        prev_delta=lambda sc: store.prev_delta_platform("youtube", sc),
        now=now, window_hours=YOUTUBE_WINDOW_HOURS,
    )
    apply_grades(items)
    run_date = now.strftime("%Y-%m-%d %H:%M")
    store.save_run_platform("youtube", run_date,
                            [{"shortcode": i["shortcode"], "base": i["base_count"], "delta": i["delta"]} for i in items])
    store.save_last_run_platform("youtube", items, now.isoformat())
    return items
```

NOTE: 기존의 `if not seeds: return []` 조기반환은 **삭제**한다 — 시드가 없어도 프리셋으로 수집해야 딸깍이 된다. (프리셋·시드 모두 비면 raw가 비고 build가 빈 리스트를 낸다 — 무해)

- [ ] **Step 4: Run tests, verify PASS**

Run: `cd "c:/Users/CH/Desktop/로또의 주식/.tracks/유튜브채널수집" && python -m pytest shopping_shorts/tests/test_collect_youtube.py -v`
Expected: 4 passed.

- [ ] **Step 5: Commit**

```bash
git -C "c:/Users/CH/Desktop/로또의 주식/.tracks/유튜브채널수집" add shopping_shorts/service.py shopping_shorts/tests/test_collect_youtube.py
git -C "c:/Users/CH/Desktop/로또의 주식/.tracks/유튜브채널수집" commit -m "feat(youtube): _collect_youtube 카테고리 프리셋 수집(전체/카테고리별) + collect categories 인자"
```

---

## Task 3: app.py `category` 파라미터

**Files:**
- Modify: `shopping_shorts/app.py` (`api_collect` ~line 154, `collect(...)` 호출 ~line 198)
- Test: `shopping_shorts/tests/test_app_collect_category.py` (신규)

- [ ] **Step 1: Write the failing test**

먼저 기존 `shopping_shorts/tests/test_app_tiktok_guard.py`를 열어 **TestClient 생성 + 관리자 인증(_require_admin 통과) 방법**을 확인한다(이 저장소의 관례를 그대로 따른다). 그 패턴으로 아래 의도의 테스트를 `test_app_collect_category.py`에 작성한다:

의도: `POST /api/collect?platform=youtube&category=혼합형` 호출 시 `service.collect`(app이 import한 `collect`)가 `categories=["혼합형"]`로 불리고, `category` 없이 호출하면 `categories=None`으로 불린다.

구현 뼈대(인증/클라이언트 생성은 test_app_tiktok_guard.py에서 그대로 가져와 맞춘다):

```python
# test_app_tiktok_guard.py 의 client/admin 셋업을 그대로 재사용한다.
# 아래는 핵심 단언만 — 셋업은 그 파일 패턴에 맞춰 채운다.
from shopping_shorts import app as app_mod


def test_collect_passes_category(monkeypatch, <기존 픽스처들>):
    called = {}
    def fake_collect(platform="instagram", categories=None, limit_channels=None):
        called["platform"] = platform
        called["categories"] = categories
        return []
    monkeypatch.setattr(app_mod, "collect", fake_collect)
    # (관리자 인증된 TestClient로) 카테고리 지정 호출
    client.post("/api/collect?platform=youtube&category=혼합형")
    assert called["categories"] == ["혼합형"]
    # 카테고리 없이 = 전체
    client.post("/api/collect?platform=youtube")
    assert called["categories"] is None
```

- [ ] **Step 2: Run test, verify FAIL**

Run: `cd "c:/Users/CH/Desktop/로또의 주식/.tracks/유튜브채널수집" && python -m pytest shopping_shorts/tests/test_app_collect_category.py -v`
Expected: FAIL (아직 `category` 파라미터가 없어 categories가 항상 None).

- [ ] **Step 3: Write implementation**

`app.py` `api_collect` 시그니처(line 154)에 `category` 추가:

```python
def api_collect(request: Request, background_tasks: BackgroundTasks, limit: int | None = None, platform: str = "instagram", category: str | None = None):
```

`collect(...)` 호출(line ~198)을 categories 전달로 변경:

```python
        items = collect(platform=platform, categories=([category] if category else None), limit_channels=limit)
```

- [ ] **Step 4: Run test, verify PASS**

Run: `cd "c:/Users/CH/Desktop/로또의 주식/.tracks/유튜브채널수집" && python -m pytest shopping_shorts/tests/test_app_collect_category.py -v`
Expected: PASS.

- [ ] **Step 5: Commit**

```bash
git -C "c:/Users/CH/Desktop/로또의 주식/.tracks/유튜브채널수집" add shopping_shorts/app.py shopping_shorts/tests/test_app_collect_category.py
git -C "c:/Users/CH/Desktop/로또의 주식/.tracks/유튜브채널수집" commit -m "feat(youtube): /api/collect category 파라미터 → collect(categories=)"
```

---

## Task 4: index.html 수집 컨트롤 3개

**Files:**
- Modify: `shopping_shorts/static/index.html` (툴바 ~line 279-283, `doCollect` ~line 813, `switchPlatform` ~line 406)

- [ ] **Step 1: 카테고리 셀렉터 + 버튼 추가 (툴바)**

`<div class="toolbar">`(line 279) 안, `collectBtn`(line 280) 바로 다음에 유튜브 전용 컨트롤을 넣는다:

```html
      <span id="ytCatBox" style="display:none">
        <select id="ytCat" style="padding:6px;border-radius:6px;border:1px solid var(--line);background:var(--inset);color:var(--txt)">
          <option value="홈템">🏠 홈·살림템</option>
          <option value="비법형">🔥 비법·꿀팁형</option>
          <option value="제품형">🛒 제품추천형</option>
          <option value="혼합형">💄 뷰티</option>
          <option value="기타">📦 기타</option>
        </select>
        <button onclick="doCollect(document.getElementById('ytCat').value)">＋ 이 카테고리 수집</button>
      </span>
```

- [ ] **Step 2: `doCollect`가 category를 받도록 수정**

`async function doCollect(){`(line 813)를 `async function doCollect(category){`로 바꾸고, fetch URL에 category를 붙인다. line 817을 아래로 교체:

```javascript
    const qs = "/api/collect?platform="+PLATFORM + (category?("&category="+encodeURIComponent(category)):"");
    const r = await fetch(qs, {method:"POST"});
```

(기존 `collectBtn`의 `onclick="doCollect()"`는 그대로 — 인자 없이 호출 = 전체 프리셋. 인스타/틱톡도 category 없이 그대로 동작.)

- [ ] **Step 3: 유튜브일 때만 카테고리 셀렉터 노출 (`switchPlatform`)**

`switchPlatform`(line 406) 안, `document.getElementById('ttMode')...` 줄들 근처에 한 줄 추가:

```javascript
  document.getElementById('ytCatBox').style.display = (p==='youtube')?'inline':'none';
```

- [ ] **Step 4: 구조 검증(정적)**

Run: `cd "c:/Users/CH/Desktop/로또의 주식/.tracks/유튜브채널수집" && grep -c "ytCatBox\|doCollect(category)\|ytCat'" shopping_shorts/static/index.html`
Expected: ≥3 (ytCatBox 2회+, doCollect(category) 1회, ytCat 참조). 최소한 각 요소가 존재하는지 확인.
그리고 `python -c "import shopping_shorts.app"`로 서버 import 무결(HTML은 정적이라 파이썬엔 영향 없지만 회귀 확인).

- [ ] **Step 5: Commit**

```bash
git -C "c:/Users/CH/Desktop/로또의 주식/.tracks/유튜브채널수집" add shopping_shorts/static/index.html
git -C "c:/Users/CH/Desktop/로또의 주식/.tracks/유튜브채널수집" commit -m "feat(youtube): 수집 컨트롤 3개(전체 지금수집·카테고리 셀렉터+수집·개별 검색란)"
```

---

## Task 5: 회귀 + import 스모크

**Files:** 없음(검증만)

- [ ] **Step 1: 유튜브 전체 테스트**

Run: `cd "c:/Users/CH/Desktop/로또의 주식/.tracks/유튜브채널수집" && python -m pytest shopping_shorts/tests/test_youtube_presets.py shopping_shorts/tests/test_collect_youtube.py shopping_shorts/tests/test_youtube_channel.py shopping_shorts/tests/test_yt_cache_store.py shopping_shorts/tests/test_app_collect_category.py -v`
Expected: 전부 PASS.

- [ ] **Step 2: app import 스모크(finish 게이트와 동일)**

Run: `cd "c:/Users/CH/Desktop/로또의 주식/.tracks/유튜브채널수집" && python -c "import shopping_shorts.app"`
Expected: 에러 없음.

- [ ] **Step 3: 인스타/틱톡 회귀(내 변경이 다른 플랫폼 안 건드림)**

Run: `cd "c:/Users/CH/Desktop/로또의 주식/.tracks/유튜브채널수집" && python -m pytest shopping_shorts/tests/test_collect_tiktok_free_only.py shopping_shorts/tests/test_app_tiktok_guard.py -v`
Expected: PASS.

---

## Self-Review 결과 (작성자 체크)

- **스펙 커버리지**: §3 프리셋데이터=Task1, §4 아키텍처+스코프=Task2, §5 app=Task3·UI=Task4, §8 테스트 전부 매핑. ✅
- **플레이스홀더**: Task3 테스트 셋업만 "test_app_tiktok_guard.py 패턴 따름"으로 위임(저장소 고유 인증 셋업이라 불가피) — 단언은 명시. 그 외 완전 코드. ✅
- **타입 일관성**: `collect(platform, categories, limit_channels)` Task2 정의 = Task3 호출(`categories=[category] if category else None`) 일치. `preset_keywords(categories)` Task1 정의 = Task2 사용 일치. `doCollect(category)` Task4 정의 = 버튼 호출 일치. 카테고리 key(홈템/비법형/제품형/혼합형/기타) Task1·Task4 셀렉터 일치. ✅
- **기존 테스트 깨짐**: Task2가 기존 2개 테스트에 프리셋 격리 monkeypatch를 넣어 명시적으로 처리. ✅

## 배포(구현 후)

전 태스크 통과 → `py tools/track.py finish 유튜브채널수집` → 게이트(문법·import·pytest) 통과 시
main 병합 → 서버 크론 3분 → 라이브. 이후 브라우저에서 유튜브 탭 → [지금 수집](전체)·
[카테고리 수집] 눌러 조회수 랭킹 뜨는지 육안 확인.
