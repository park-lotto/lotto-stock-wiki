# 자막싱크 트랙 로그
- 2026-08-10(회사PC): 자막·TTS 배속 미보정 수정(rescale 클램프 완화)+구코드 job 3건 cap_durs 재계산. 3단계 자막제거 썸네일 확대·중앙정렬 라이브(04b7634fa).
- 2026-08-11 (회사PC): 제작소 새로고침 리셋 버그(사장님 제보 "대본 3안 전으로 돌아감") 뿌리 수정. refreshStep0가 _restoreWork 복원 도중 무조건 _pushWork → cur=0·_serverStep=null이라 강등가드 무력 → step=0이 서버 진행단계 파괴(실측: 당일 작업 8개 전부 step=0, 후보 3안은 DB에 살아있고 숨은 패널7에만 렌더). 수정=①밀린 저장 있을 때만 push ②_serverStep 복원 첫줄 시드. 라이브 검증: 열기→서버 step 7 유지, 새로고침→패널7+후보 복원(c2a49f410). 파괴된 작업 7개 step=7 API 복구.

## 2026-08-20 — "자막·TTS 안 맞고 끝나면 반복" 뿌리 제거
- 증상(사장님 제보, job 087e03b69dc2): 미리보기에서 자막↔음성 어긋남 + 끝나고 계속 반복
- 실측: hook 대본 105자/mp3 16.80초인데 target_seconds는 2.9초. beat2~6은 정상
- **뿌리 = 어제 커밋 3ef71cfa5 `enforce_scripted_narration`**. 문장 분리기가 마침표만 봐서
  구어체 8칸 대본(마침표 1개)을 2조각(141자·131자)으로만 잘랐고, 그 덩이가 1·2번 칸에 꽂혔다
- 2차: `resynth_one_beat`이 target_seconds를 안 갱신 → 초가 두 벌 → 화면 5배 부족 → 앞 장면 되풀이
- 고침: ①종결어미(요/죠) 분리 ②칸 예산 초과 조각 차단 ③재합성 시 렌더와 같은 싱크 마무리
  ④scene_lab 칸에 sync_gap 경고. 커밋 9b2cdc879·77d71da87 (main 병합)

## 2026-08-20(2) — "믹스확정 눌러도 원래대로" 뿌리 = MIX_POLL 고아 폴러
- 실측: POST /api/produce/mix/preview 200 · 서버 preview_status=ready인데 화면은 안내문
- 로그에 status·result·bank_injected가 매초 반복(정상 2.5초) = 폴러 겹침
- MIX_POLL에 setInterval 넣는 곳 3군데(4195·6367·8382)가 앞의 것을 안 죽여 고아 발생
  → 고아가 매초 loadMixReview() → PREVIEW_STATUS=null·PREVIEW_POLL kill·++PREVIEW_GEN
  → 로더·영상 삭제 + 진행 중 요청 무효. 확정이 성립 불가
- 고침: setMixPoll() 단일화(정의는 _MIX 슬라이스 안 — 게이트 하네스가 node로 부분집합 실행)
  + _renderMixReviewBody 안내문 덮어쓰기를 '렌더 중'엔 건너뛰기. 게이트 12/12
- 커밋 4a65c1244 (main 병합 fd4334a39)

## 2026-08-20(3) — 장면 교체(담기·빼기) 오류 총점검
- 제보: "두 번 더블클릭하면 빠지는 게 아니라 새로운 게 들어간다"
- ① dropCutFromBand가 indexOf로 첫 항목만 삭제 → 같은 조각이 여러 컷(늘려 채움)일 때
  화면엔 그대로 남고 배치만 재계산 = "새로운 게 들어간다". → 그 조각 전부 제거
- ② .tbcut의 stopPropagation이 부모 selBeat을 막아 조각을 눌러도 sel(담기는 칸)이 안 바뀜
  → 팔레트에서 담으면 이전 칸으로 들어감. → 조각 클릭이 칸을 고르게(단 230ms 타이머 안:
  즉시 render하면 DOM이 갈려 dblclick 빼기가 죽는다)
- ③ add()가 중복 조각을 조용히 무시 → 안내 한 줄
- 미수정: bandDrop도 같은 indexOf 방식(순서 바꾸기, 영향 작아 보류)
- 커밋 b1ea70b2c (main 병합 95e43ad15). node --check 통과, 브라우저 실동작은 미확인

## 2026-08-21 — 장면 빼기: 뺀 자리를 자동으로 안 채운다 + 무슨 일인지 알린다
- 제보: 훅의 한 장짜리를 두 번 클릭했더니 "다른 카드로 자동 변했다 / 더블클릭은 삭제 아닌가"
- 코드 확인: 더블클릭 = 삭제 맞다(scene_lab.html:1062 → dropCutFromBand). 서버 md5도 로컬과
  일치(d4d17c4e…) = 08-20 수정본이 라이브에 떠 있다. **자동으로 새 장면을 담는 코드는 없다**
  (autoFill은 onclick 전용). 즉 "교체"로 보인 건 담긴 게 늘어난 게 아니라, 남은 재료로
  planClips 라운드로빈이 **다시 배치**되며 다른 썸네일이 그 자리에 온 것
- 사장님 지시: "그냥 빼면 없애줘. 그래야 사용자들도 오류로 생각안하고 다시담지 자동채우지마"
- 고침: ①뺀 뒤 무슨 일이 났는지 한 줄 안내(sayDropped 하나로 — 빼기 경로가 둘이라 0순위-B)
  · 남은 게 있으면 "남은 N장으로 다시 배치했어요(새로 담지 않았습니다)"
  · 다 빠지면 "칸을 비웠습니다 — 자동으로 채우지 않습니다"
  ②delSeg도 같은 약속으로 통일 + 범위밖 인덱스 가드(splice가 엉뚱한 걸 지우던 여지 제거)
- 검증: 인라인 script 2블록 node --check 통과 + 함수를 떼어내 5개 사례 실행(한 장 빼기·
  3장 중 1장·없는 조각 헛동작 0·delSeg·범위밖) 전부 통과. node --check는 문법만 보므로
  실행 하네스를 따로 돌렸다(reference_render죽으면_전화면공백)

## 2026-08-21(3) — 3단계 전체 점검 → 버그리뷰 7건 수정
- 산출물: out/제작소3단계_버그리뷰_2026-08-21.html (바탕화면 사본). 브라우저 렌더 확인
  중 charset 누락으로 한글 전체가 깨지는 것 발견해 수정 — 정적 검토였으면 못 잡았다
- P1: boot이 서버 편성을 받아놓고 localStorage로 통째 덮던 것(오늘 겪은 어긋남 2건의 공통 뿌리)
  → edit_plan.scene_lab.at 신설 + API 노출 + 새 쪽 채택(여유 30초). 되살릴 땐 serverList
  (scene_override) 사용 — baseList는 AI 원본이라 "서버 것을 불러왔다"면서 엉뚱한 배치가 뜬다
- P2: 칸→칸 이동 불가(beatDrop이 dragFrom 무시) / 모드버튼 무확인 초기화 / 채우기 AI경로
  길이상한 없음 + 꽉 찬 칸 무반응
- P3: bandDrop indexOf(중복 제거로 조건 자체 제거) / reset 무확인 / 편집 중 자동 새로고침
- 검증: 브라우저 실측 5건 PASS, pytest 482 passed. 커밋 9013085c2, 배포 16:51
