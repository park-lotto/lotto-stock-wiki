# 꾸미기(6단계) 고정카피·템플릿·UI개선 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 꾸미기 화면에 ①대본 기반 AI 고정카피 후보 4개 ②PNG 템플릿 12종(전체/첫장면) ③4탭 UI를 붙인다.

**Architecture:** 템플릿 렌더 배관은 이미 존재한다(`video_assemble.py` `has_overlay` = 전체 구간, `_motion_layer_filters` = 타임드 구간). 새로 만들지 않고 **연결만** 한다. 고정카피는 `script_generate._call_json`(키풀·fail-open 완비)을 재사용하고, 캐시 키는 기존 `_script_hash`를 쓴다(0순위-B: 같은 판단을 두 번 적지 않는다). UI는 우측 컨트롤만 탭으로 가르고 **id는 전부 보존**한다(옮기기만, 지우지 않는다).

**Tech Stack:** FastAPI(app.py) · SQLite(store.py) · Pillow 12.3.0(PNG 생성) · ffmpeg(video_assemble) · 순수 JS(produce.html) · pytest + node 슬라이스 하네스

**작업 폴더:** `C:\Users\CH\Desktop\로또의 주식\.tracks\꾸미기` — **모든 경로는 이 폴더 기준.** main 폴더 파일은 하나도 건드리지 않는다.

---

## 손대기 전 실측한 사실 (추측 아님 — 전부 이 파일들을 열어 확인)

| 사실 | 위치 |
|---|---|
| 꾸미기 = 물리 패널 3 = 화면 6단계 `자막꾸미기` | `produce.html:985` `data-step="3"`, `STEP_LABELS[5]` |
| 진입 훅 | `produce.html:1556` `if(cur===3) initHeadcopy();` |
| 헤드카피 textarea | `produce.html:1016` `id="hcText"` `oninput="updateHC()"` |
| 오버레이(전체 구간) 렌더 | `video_assemble.py:1621` `if has_overlay:` |
| 타임드 레이어 렌더 | `video_assemble.py:1184` `_motion_layer_filters`, `dur=None`이면 enable 생략 |
| 오버레이 업로드 API | `app.py:9228` `/api/produce/mix/overlay` |
| 설정 저장 API | `app.py:9189` `/api/produce/mix/settings` — `deco` 통째 저장 |
| AI JSON 호출 | `script_generate.py:36` `_call_json(prompt, schema)` → **실패 시 `{}`**(fail-open) |
| 대본 해시(서버) | `app.py:3120` `_script_hash(text)` |
| 대본 해시(JS) | `produce.html:1573` `_scriptHash(text)` |
| Pillow | 12.3.0 설치 확인 |

⚠️ `_call_json`이 실패해도 `{}`를 주므로 **호출부가 "못 뽑았다"를 반드시 구분해 표시**해야 한다. 조용히 빈 카드를 띄우면 고장인지 준비중인지 사장님이 구분 못 한다.

---

## File Structure

| 파일 | 책임 | 신규/수정 |
|---|---|---|
| `tools/make_deco_templates.py` | 템플릿 PNG 12종 생성(재생성 가능) | 신규 |
| `shopping_shorts/templates/tpl_01.png` ~ `tpl_12.png` | 템플릿 원본(1080×1920 RGBA) | 신규(생성물) |
| `shopping_shorts/headcopy_gen.py` | 대본 → 고정카피 후보 4개(AI). **UI·HTTP 모름** | 신규 |
| `shopping_shorts/deco_templates.py` | 템플릿 12종 메타(id·이름·색·파일) 한 곳 | 신규 |
| `shopping_shorts/app.py` | 카피 API(캐시 포함) + 템플릿 목록 API | 수정 |
| `shopping_shorts/video_assemble.py` | `deco.template` 합성(전체/첫장면) | 수정 |
| `shopping_shorts/static/produce.html` | 4탭 UI + 카피 카드 + 템플릿 카드 | 수정 |
| `shopping_shorts/tests/test_headcopy_gen.py` | 카피 생성·폴백 | 신규 |
| `shopping_shorts/tests/test_headcopy_api.py` | 캐시 적중 시 **호출 0회** | 신규 |
| `shopping_shorts/tests/test_deco_template_render.py` | 템플릿 필터 배선(전체/첫장면/로고 공존) | 신규 |
| `shopping_shorts/tests/test_deco_tabs_ui.py` | 탭 개편 후 **id 보존** 회귀 | 신규 |

**의존 방향:** `headcopy_gen`·`deco_templates`는 순수 모듈(app을 import하지 않는다) → `app.py`가 둘을 쓴다 → `produce.html`이 HTTP로 부른다. 이 방향이면 테스트가 서버 없이 돈다.

---

## Task 1: 템플릿 메타 + PNG 생성기

**Files:**
- Create: `shopping_shorts/deco_templates.py`
- Create: `tools/make_deco_templates.py`
- Test: `shopping_shorts/tests/test_deco_templates_meta.py`

- [ ] **Step 1: 실패하는 테스트를 쓴다**

```python
# shopping_shorts/tests/test_deco_templates_meta.py
"""템플릿 메타 — 12종이 빠짐없이·중복 없이 정의돼 있는가."""
import pathlib

from shopping_shorts import deco_templates as dt


def test_twelve_templates():
    assert len(dt.TEMPLATES) == 12


def test_ids_unique_and_stable():
    ids = [t["id"] for t in dt.TEMPLATES]
    assert len(set(ids)) == 12, "id 중복 — 고른 템플릿이 엉뚱하게 바뀐다"
    assert ids[0] == "tpl_01" and ids[-1] == "tpl_12"


def test_every_template_has_name_and_file():
    for t in dt.TEMPLATES:
        assert t["name"], f"{t['id']} 이름 없음 — 카드에 빈칸이 뜬다"
        assert t["file"] == t["id"] + ".png"
        assert t["shape"] in ("top", "topbottom", "frame")


def test_get_returns_none_for_unknown():
    assert dt.get("tpl_99") is None, "없는 id에 None을 안 주면 호출부가 KeyError로 죽는다"
    assert dt.get("tpl_01")["id"] == "tpl_01"


def test_abs_path_points_into_static():
    p = dt.abs_path("tpl_01")
    assert p is not None and p.name == "tpl_01.png"
    assert p.parent.name == "templates"
```

- [ ] **Step 2: 실패를 확인한다**

Run: `py -m pytest shopping_shorts/tests/test_deco_templates_meta.py -q`
Expected: FAIL — `ModuleNotFoundError: No module named 'shopping_shorts.deco_templates'`

- [ ] **Step 3: 메타 모듈을 만든다**

```python
# shopping_shorts/deco_templates.py
"""꾸미기 템플릿 12종의 **유일한 정의처**.

★여기 한 곳에서만 정한다(0순위-B). PNG 생성기(tools/make_deco_templates.py)·
목록 API·렌더가 전부 이 표를 읽는다 — 색이나 이름을 다른 데 또 적으면 언젠가 어긋난다.
"""
import pathlib

# 1080x1920 기준. bar=띠 높이(px), border=테두리 두께(px).
_COLORS = [
    ("코랄", "#FF5A5F"),
    ("민트", "#22C7B8"),
    ("옐로", "#FFC531"),
    ("네이비", "#2B3A67"),
]
_SHAPES = [
    ("top", "상단 띠", {"bar": 190}),
    ("topbottom", "상단+하단 띠", {"bar": 165}),
    ("frame", "테두리", {"border": 26}),
]

TEMPLATES = []
_n = 0
for _shape, _slabel, _geom in _SHAPES:
    for _cname, _hex in _COLORS:
        _n += 1
        _tid = f"tpl_{_n:02d}"
        TEMPLATES.append({
            "id": _tid,
            "name": f"{_slabel} · {_cname}",
            "shape": _shape,
            "color": _hex,
            "geom": _geom,
            "file": _tid + ".png",
        })

_BY_ID = {t["id"]: t for t in TEMPLATES}
_DIR = pathlib.Path(__file__).resolve().parent / "static" / "templates"


def get(tpl_id):
    """없는 id면 None. ★KeyError를 던지지 않는다 — 옛 작업이 지워진 템플릿을 가리킬 수 있다."""
    return _BY_ID.get(tpl_id)


def abs_path(tpl_id):
    """렌더가 쓸 실제 파일 경로. 메타에 없으면 None(파일 존재 여부는 호출부가 확인)."""
    t = get(tpl_id)
    return (_DIR / t["file"]) if t else None
```

- [ ] **Step 4: 통과를 확인한다**

Run: `py -m pytest shopping_shorts/tests/test_deco_templates_meta.py -q`
Expected: PASS (5 passed)

- [ ] **Step 5: PNG 생성기를 만든다**

```python
# tools/make_deco_templates.py
"""꾸미기 템플릿 PNG 12종 생성 — 1080x1920 투명 RGBA.

★PNG만 커밋하면 "이게 어떻게 만들어졌는지" 아무도 모른다. 색·두께를 바꿀 땐
deco_templates.py를 고치고 이 스크립트를 다시 돌린다(손으로 그리지 않는다).

실행: py tools/make_deco_templates.py
"""
import pathlib
import sys

from PIL import Image, ImageDraw

sys.path.insert(0, str(pathlib.Path(__file__).resolve().parents[1]))
from shopping_shorts import deco_templates as dt   # noqa: E402

W, H = 1080, 1920
OUT = pathlib.Path(__file__).resolve().parents[1] / "shopping_shorts" / "static" / "templates"


def _rgba(hex_color, alpha=255):
    h = hex_color.lstrip("#")
    return (int(h[0:2], 16), int(h[2:4], 16), int(h[4:6], 16), alpha)


def draw(t):
    im = Image.new("RGBA", (W, H), (0, 0, 0, 0))
    d = ImageDraw.Draw(im)
    col = _rgba(t["color"])
    if t["shape"] == "top":
        d.rectangle([0, 0, W, t["geom"]["bar"]], fill=col)
    elif t["shape"] == "topbottom":
        b = t["geom"]["bar"]
        d.rectangle([0, 0, W, b], fill=col)
        d.rectangle([0, H - b, W, H], fill=col)
    else:  # frame
        b = t["geom"]["border"]
        d.rectangle([0, 0, W, b], fill=col)
        d.rectangle([0, H - b, W, H], fill=col)
        d.rectangle([0, 0, b, H], fill=col)
        d.rectangle([W - b, 0, W, H], fill=col)
    return im


def main():
    OUT.mkdir(parents=True, exist_ok=True)
    for t in dt.TEMPLATES:
        draw(t).save(OUT / t["file"], "PNG")
        print("wrote", t["file"], t["name"])
    print(f"총 {len(dt.TEMPLATES)}장 →", OUT)


if __name__ == "__main__":
    main()
```

- [ ] **Step 6: 실제로 12장을 만든다**

Run: `py tools/make_deco_templates.py`
Expected: `wrote tpl_01.png 상단 띠 · 코랄` … 12줄 + `총 12장 → ...static\templates`

- [ ] **Step 7: 12장이 실제로 생겼는지 눈으로 센다**

Run: `py -c "import pathlib;p=pathlib.Path('shopping_shorts/static/templates');f=sorted(p.glob('tpl_*.png'));print(len(f));print([x.name for x in f[:3]]);print('bytes',f[0].stat().st_size)"`
Expected: `12` / `['tpl_01.png', 'tpl_02.png', 'tpl_03.png']` / bytes > 0

- [ ] **Step 8: 투명 채널이 살아 있는지 확인한다** (알파가 죽으면 영상이 통째로 가려진다)

Run: `py -c "from PIL import Image;im=Image.open('shopping_shorts/templates/tpl_01.png');print(im.mode,im.size);print('mid-alpha',im.getpixel((540,960))[3])"`
Expected: `RGBA (1080, 1920)` / `mid-alpha 0` — 가운데가 **투명**이어야 한다

- [ ] **Step 9: 커밋**

```bash
git add shopping_shorts/deco_templates.py tools/make_deco_templates.py shopping_shorts/static/templates shopping_shorts/tests/test_deco_templates_meta.py
git commit -m "템플릿 12종 메타 + PNG 생성기 — 색·모양은 deco_templates 한 곳에서만 정한다"
```

---

## Task 2: 고정카피 생성 모듈

**Files:**
- Create: `shopping_shorts/headcopy_gen.py`
- Test: `shopping_shorts/tests/test_headcopy_gen.py`

- [ ] **Step 1: 실패하는 테스트를 쓴다**

```python
# shopping_shorts/tests/test_headcopy_gen.py
"""고정카피 생성 — AI가 죽어도 화면이 죽지 않는가(fail-open)를 함께 잠근다."""
from shopping_shorts import headcopy_gen


def test_returns_four_copies(monkeypatch):
    monkeypatch.setattr(headcopy_gen, "_call_json", lambda p, s: {"copies": [
        {"label": "짧은 훅형", "text": "이거 모르면 손해"},
        {"label": "숫자형", "text": "3초면 끝납니다"},
        {"label": "반전형", "text": "비싼 줄 알았는데"},
        {"label": "질문형", "text": "아직도 손으로 하세요?"},
    ]})
    out = headcopy_gen.suggest("대본 텍스트입니다")
    assert len(out) == 4
    assert out[0]["text"] == "이거 모르면 손해"
    assert out[0]["label"] == "짧은 훅형"


def test_empty_script_skips_ai_entirely(monkeypatch):
    """대본이 없으면 AI를 부르지 않는다 — 부르면 빈 재료로 지어내고 돈만 쓴다."""
    called = []
    monkeypatch.setattr(headcopy_gen, "_call_json", lambda p, s: called.append(1) or {})
    assert headcopy_gen.suggest("   ") == []
    assert called == [], "빈 대본인데 AI를 불렀다"


def test_ai_failure_returns_empty_not_crash(monkeypatch):
    """_call_json은 실패 시 {}를 준다(fail-open). 그걸 그대로 흘리면 화면이 깨진다."""
    monkeypatch.setattr(headcopy_gen, "_call_json", lambda p, s: {})
    assert headcopy_gen.suggest("대본") == []


def test_drops_blank_and_overlong(monkeypatch):
    """빈 문구·너무 긴 문구는 버린다 — 헤드카피는 화면에 크게 박히는 한 줄이다."""
    monkeypatch.setattr(headcopy_gen, "_call_json", lambda p, s: {"copies": [
        {"label": "a", "text": "  "},
        {"label": "b", "text": "정상 문구"},
        {"label": "c", "text": "가" * 200},
    ]})
    out = headcopy_gen.suggest("대본")
    assert [c["text"] for c in out] == ["정상 문구"]


def test_dedupes_identical_text(monkeypatch):
    monkeypatch.setattr(headcopy_gen, "_call_json", lambda p, s: {"copies": [
        {"label": "a", "text": "같은 문구"},
        {"label": "b", "text": "같은 문구"},
        {"label": "c", "text": "다른 문구"},
    ]})
    assert [c["text"] for c in headcopy_gen.suggest("대본")] == ["같은 문구", "다른 문구"]
```

- [ ] **Step 2: 실패를 확인한다**

Run: `py -m pytest shopping_shorts/tests/test_headcopy_gen.py -q`
Expected: FAIL — `ModuleNotFoundError: No module named 'shopping_shorts.headcopy_gen'`

- [ ] **Step 3: 모듈을 만든다**

```python
# shopping_shorts/headcopy_gen.py
"""확정 대본 → 헤드카피(고정카피) 후보 4개.

★생성 배관을 새로 만들지 않는다 — script_generate._call_json을 그대로 쓴다(키풀
로테이션·소진키 마킹·fail-open이 이미 검증된 코드다, 0순위-B).

⚠️ _call_json은 실패해도 {}를 돌려준다(fail-open). 그래서 여기서 **빈 리스트**로
정규화하고, "못 뽑았다"의 표시는 호출부(화면)가 한다 — 조용히 빈 카드를 띄우면
사장님이 고장인지 준비중인지 구분 못 한다.
"""
from shopping_shorts.script_generate import _call_json

_MAX_LEN = 40          # 헤드카피는 화면에 크게 박히는 한 줄 — 길면 줄이 무너진다
_WANT = 4

_SCHEMA = {
    "type": "object",
    "properties": {
        "copies": {
            "type": "array",
            "items": {
                "type": "object",
                "properties": {"label": {"type": "string"}, "text": {"type": "string"}},
                "required": ["label", "text"],
            },
        }
    },
    "required": ["copies"],
}

_PROMPT = """너는 쇼츠 영상의 **헤드카피**(영상 위에 크게 박히는 한 줄)를 쓴다.

아래 대본을 읽고 서로 **결이 다른** 후보 4개를 써라.
- 짧은 훅형: 궁금하게 만드는 짧은 한 방
- 숫자형: 숫자를 넣어 구체적으로
- 반전형: 예상과 다른 사실
- 질문형: 보는 사람에게 묻는다

규칙:
- 각 문구는 **{maxlen}자 이내**, 한 줄. 마침표로 끝내지 마라.
- 대본에 **없는 사실을 지어내지 마라**(가격·수치·효능을 새로 만들지 않는다).
- 이모지·해시태그·따옴표를 넣지 마라.

[대본]
{script}
"""


def suggest(script, want=_WANT):
    """대본 → [{label, text}] (최대 want개). 실패·무키·빈 대본이면 **빈 리스트**."""
    s = (script or "").strip()
    if not s:
        return []                      # 재료가 없으면 부르지 않는다(빈 재료로 지어낸다)
    data = _call_json(_PROMPT.format(script=s[:4000], maxlen=_MAX_LEN), _SCHEMA) or {}
    out, seen = [], set()
    for c in (data.get("copies") or []):
        text = (c.get("text") or "").strip()
        if not text or len(text) > _MAX_LEN or text in seen:
            continue
        seen.add(text)
        out.append({"label": (c.get("label") or "").strip() or "제안", "text": text})
        if len(out) >= want:
            break
    return out
```

- [ ] **Step 4: 통과를 확인한다**

Run: `py -m pytest shopping_shorts/tests/test_headcopy_gen.py -q`
Expected: PASS (5 passed)

- [ ] **Step 5: 커밋**

```bash
git add shopping_shorts/headcopy_gen.py shopping_shorts/tests/test_headcopy_gen.py
git commit -m "고정카피 생성 모듈 — _call_json 재사용, 실패는 빈 리스트로 정규화"
```

---

## Task 3: 카피 API + 대본 해시 캐시

**Files:**
- Modify: `shopping_shorts/app.py` (`/api/produce/mix/settings` 아래에 새 라우트 추가)
- Test: `shopping_shorts/tests/test_headcopy_api.py`

- [ ] **Step 1: 실패하는 테스트를 쓴다**

```python
# shopping_shorts/tests/test_headcopy_api.py
"""고정카피 API — ★캐시 적중 시 AI 호출이 **0회**인지가 이 테스트의 전부다.

자동 생성이라 캐시가 없으면 6단계를 오갈 때마다 과금된다.
"""
import pytest
from fastapi.testclient import TestClient

from shopping_shorts import app as app_mod

client = TestClient(app_mod.app)


@pytest.fixture
def spy(monkeypatch):
    calls = []

    def fake(script, want=4):
        calls.append(script)
        return [{"label": "짧은 훅형", "text": "이거 모르면 손해"}]

    monkeypatch.setattr(app_mod.headcopy_gen, "suggest", fake)
    return calls


def test_missing_script_is_422(spy):
    r = client.post("/api/produce/headcopy/suggest", json={"script": "  "})
    assert r.status_code == 422
    assert spy == [], "빈 대본인데 AI를 불렀다"


def test_first_call_generates(spy):
    r = client.post("/api/produce/headcopy/suggest", json={"script": "감자 대본입니다"})
    assert r.status_code == 200
    d = r.json()
    assert d["ok"] is True and d["cached"] is False
    assert d["copies"][0]["text"] == "이거 모르면 손해"
    assert len(spy) == 1


def test_same_script_hits_cache_zero_calls(spy):
    """★같은 대본이면 두 번째부터 호출 0회. 이게 깨지면 오갈 때마다 돈이 샌다."""
    body = {"script": "캐시 확인용 대본"}
    client.post("/api/produce/headcopy/suggest", json=body)
    assert len(spy) == 1
    r2 = client.post("/api/produce/headcopy/suggest", json=body)
    assert r2.json()["cached"] is True
    assert len(spy) == 1, f"캐시 적중인데 AI를 또 불렀다(호출 {len(spy)}회)"


def test_whitespace_variants_share_cache(spy):
    """앞뒤 공백·CRLF만 다른 건 같은 대본이다(_script_hash 규칙)."""
    client.post("/api/produce/headcopy/suggest", json={"script": "공백 테스트 대본"})
    n = len(spy)
    client.post("/api/produce/headcopy/suggest", json={"script": "  공백 테스트 대본  \r\n"})
    assert len(spy) == n, "공백만 다른 같은 대본에 다시 과금됐다"


def test_changed_script_regenerates(spy):
    client.post("/api/produce/headcopy/suggest", json={"script": "첫 번째 대본"})
    n = len(spy)
    client.post("/api/produce/headcopy/suggest", json={"script": "두 번째 대본"})
    assert len(spy) == n + 1, "대본이 바뀌었는데 옛 카피를 그대로 줬다"


def test_ai_empty_is_200_with_empty_list(monkeypatch):
    """못 뽑아도 500이 아니다 — 화면이 '못 뽑았어요'를 그릴 수 있어야 한다."""
    monkeypatch.setattr(app_mod.headcopy_gen, "suggest", lambda s, want=4: [])
    r = client.post("/api/produce/headcopy/suggest", json={"script": "실패용 대본 xyz"})
    assert r.status_code == 200
    assert r.json() == {"ok": True, "cached": False, "copies": []}
```

- [ ] **Step 2: 실패를 확인한다**

Run: `py -m pytest shopping_shorts/tests/test_headcopy_api.py -q`
Expected: FAIL — 404(라우트 없음) 또는 `AttributeError: module 'shopping_shorts.app' has no attribute 'headcopy_gen'`

- [ ] **Step 3: import를 추가한다**

`app.py`에서 `from shopping_shorts import comment_gen`이 있는 import 블록을 찾아, 그 바로 아래 줄에 추가:

```python
from shopping_shorts import headcopy_gen
```

- [ ] **Step 4: 라우트를 추가한다**

`app.py:9209` `api_produce_mix_settings`의 `return {"ok": True}` 바로 다음(빈 줄 2개 뒤)에 삽입:

```python
# 고정카피(헤드카피 후보) — 확정 대본에서 AI가 4개 뽑는다.
# ★캐시가 핵심이다: 꾸미기에 들어올 때마다 자동 생성이라, 캐시가 없으면 사장님이
#   6단계를 오갈 때마다 과금된다. 키는 **기존 _script_hash**를 쓴다(새 규칙을 만들지
#   않는다 — 0순위-B. 프론트 _scriptHash와도 같은 규칙이라 나중에 대조할 수 있다).
# ⚠️ 프로세스 메모리 캐시다. 재시작하면 비지만, "한 세션에서 오가는 동안 과금 0"이라는
#    목적은 달성한다. DB 컬럼을 새로 파지 않는 쪽을 택했다(스키마 변경 위험 > 이득).
_HEADCOPY_CACHE = {}
_HEADCOPY_CACHE_MAX = 200


@app.post("/api/produce/headcopy/suggest")
def api_produce_headcopy_suggest(body: dict):
    """확정 대본 → 헤드카피 후보. {script} → {ok, cached, copies:[{label,text}]}"""
    script = (body.get("script") or "").strip()
    if not script:
        return JSONResponse(status_code=422,
                            content={"ok": False, "error": "대본이 비어 있습니다"})
    key = _script_hash(script)
    if key in _HEADCOPY_CACHE:
        return {"ok": True, "cached": True, "copies": _HEADCOPY_CACHE[key]}
    copies = headcopy_gen.suggest(script)
    # 못 뽑은 것도 캐시하면 "다시 시도"가 영영 막힌다 → 성공했을 때만 담는다.
    if copies:
        if len(_HEADCOPY_CACHE) >= _HEADCOPY_CACHE_MAX:
            _HEADCOPY_CACHE.clear()      # 단순 상한 — LRU를 쓸 만큼 크지 않다
        _HEADCOPY_CACHE[key] = copies
    return {"ok": True, "cached": False, "copies": copies}
```

- [ ] **Step 5: 통과를 확인한다**

Run: `py -m pytest shopping_shorts/tests/test_headcopy_api.py -q`
Expected: PASS (6 passed)

- [ ] **Step 6: 커밋**

```bash
git add shopping_shorts/app.py shopping_shorts/tests/test_headcopy_api.py
git commit -m "고정카피 API + 대본해시 캐시 — 적중 시 AI 호출 0회(테스트로 잠금)"
```

---

## Task 4: 템플릿 목록 API

**Files:**
- Modify: `shopping_shorts/app.py`
- Test: `shopping_shorts/tests/test_deco_template_api.py`

- [ ] **Step 1: 실패하는 테스트를 쓴다**

```python
# shopping_shorts/tests/test_deco_template_api.py
"""템플릿 목록 API — 화면이 카드를 그릴 재료를 준다."""
from fastapi.testclient import TestClient

from shopping_shorts import app as app_mod

client = TestClient(app_mod.app)


def test_list_returns_twelve():
    r = client.get("/api/produce/templates")
    assert r.status_code == 200
    d = r.json()
    assert d["ok"] is True
    assert len(d["templates"]) == 12


def test_each_item_has_what_the_card_needs():
    items = client.get("/api/produce/templates").json()["templates"]
    for t in items:
        assert t["id"] and t["name"]
        assert t["url"].startswith("/templates/")
        assert t["url"].endswith(".png")


def test_png_actually_served():
    """목록에 있는데 파일이 없으면 카드가 깨진 이미지로 뜬다."""
    first = client.get("/api/produce/templates").json()["templates"][0]
    r = client.get(first["url"])
    assert r.status_code == 200
    assert r.content[:8] == b"\x89PNG\r\n\x1a\n", "PNG가 아니다"
```

- [ ] **Step 2: 실패를 확인한다**

Run: `py -m pytest shopping_shorts/tests/test_deco_template_api.py -q`
Expected: FAIL — 404

- [ ] **Step 3: import와 라우트를 추가한다**

Task 3에서 넣은 `from shopping_shorts import headcopy_gen` 아래에 추가:

```python
from shopping_shorts import deco_templates
```

Task 3의 `api_produce_headcopy_suggest` 아래(빈 줄 2개 뒤)에 삽입:

```python
@app.get("/api/produce/templates")
def api_produce_templates():
    """꾸미기 템플릿 12종 목록. 정의처는 deco_templates 하나뿐이다(0순위-B)."""
    return {"ok": True, "templates": [
        {"id": t["id"], "name": t["name"], "shape": t["shape"], "color": t["color"],
         "url": f"/templates/{t['file']}"}
        for t in deco_templates.TEMPLATES
    ]}
```

- [ ] **Step 4: 정적 경로를 확인한다** (★실측 완료 — 헷갈리기 쉬운 자리)

`app.py:11198`은 `app.mount("/", ...)` — 정적 파일이 **`/static`이 아니라 `/`에 마운트**돼 있다.
그래서 `static/templates/tpl_01.png`의 URL은 **`/templates/tpl_01.png`**다(`/static/...`로 쓰면 404).

Run: `py -c "from shopping_shorts import app as a; print([getattr(r,'path','') for r in a.app.routes if getattr(r,'name','')=='static'])"`
Expected: `['/']` — 위 URL 규칙이 맞다는 확인

- [ ] **Step 5: 통과를 확인한다**

Run: `py -m pytest shopping_shorts/tests/test_deco_template_api.py -q`
Expected: PASS (3 passed)

- [ ] **Step 6: 커밋**

```bash
git add shopping_shorts/app.py shopping_shorts/tests/test_deco_template_api.py
git commit -m "템플릿 목록 API — PNG가 실제로 서빙되는지까지 테스트"
```

---

## Task 5: 템플릿 렌더 배선 (전체 / 첫 장면만)

**Files:**
- Modify: `shopping_shorts/video_assemble.py:1590` 부근(`overlay = deco.get("overlay")` 아래)
- Modify: `shopping_shorts/mix_pipeline.py:1556` 부근(`_abspath` 해석부)
- Test: `shopping_shorts/tests/test_deco_template_render.py`

- [ ] **Step 1: 실패하는 테스트를 쓴다**

```python
# shopping_shorts/tests/test_deco_template_render.py
"""템플릿 합성 — 전체/첫장면, 그리고 ★사장님 로고와 공존하는가.

템플릿이 overlay 슬롯을 덮어쓰면 사장님이 올린 로고가 조용히 사라진다.
"""
from shopping_shorts import video_assemble as va


def test_template_layer_full_has_no_enable():
    """전체 구간이면 enable=이 붙으면 안 된다(붙으면 그 구간에만 뜬다)."""
    layers = [{"_abspath": "/tmp/tpl_01.png", "x": 50, "y": 50, "alpha": 1}]
    _inp, fc, _v, _i = va._motion_layer_filters(layers, 1, "v0")
    joined = ";".join(fc)
    assert "overlay=" in joined
    assert "enable=" not in joined


def test_template_layer_first_scene_has_enable_window():
    layers = [{"_abspath": "/tmp/tpl_01.png", "x": 50, "y": 50, "alpha": 1,
               "start": 0, "dur": 3.5}]
    _inp, fc, _v, _i = va._motion_layer_filters(layers, 1, "v0")
    joined = ";".join(fc)
    assert "enable='between(t,0.000,3.500)'" in joined


def test_template_and_logo_both_composited():
    """★둘 다 얹혀야 한다. 하나가 다른 하나를 대체하면 회귀다."""
    layers = [
        {"_abspath": "/tmp/tpl_01.png", "x": 50, "y": 50, "alpha": 1},
        {"_abspath": "/tmp/logo.png", "x": 80, "y": 10, "alpha": 0.9},
    ]
    inp, fc, vcur, idx = va._motion_layer_filters(layers, 1, "v0")
    assert inp.count("-i") == 2, "레이어 2개인데 입력이 2개가 아니다"
    assert ";".join(fc).count("overlay=") == 2
    assert vcur == "mlv1" and idx == 3


def test_layer_without_abspath_is_skipped():
    """경로 해석에 실패한 레이어를 그대로 태우면 ffmpeg가 통째로 죽는다."""
    inp, fc, vcur, _i = va._motion_layer_filters(
        [{"x": 50, "y": 50}], 1, "v0")
    assert inp == [] and fc == [] and vcur == "v0"
```

- [ ] **Step 2: 실패/통과를 확인한다**

Run: `py -m pytest shopping_shorts/tests/test_deco_template_render.py -q`
Expected: **대부분 PASS** — `_motion_layer_filters`는 이미 있는 함수다. 이 테스트는 **템플릿이 탈 배관이 실제로 이 동작을 하는지 확인**하는 것이 목적이다(신규 구현이 아니라 계약 고정). 만약 FAIL이 나오면 그 자체가 발견이니 원인을 먼저 확인하고 진행한다.

- [ ] **Step 3: 템플릿을 레이어로 합류시킨다**

`video_assemble.py`에서 `motion_layers = [L for L in (motion.get("layers") or []) if L.get("_abspath")]` 줄을 찾아, **그 다음 줄**에 추가:

```python
    # 🖼 꾸미기 템플릿 — 모션 레이어와 **같은 배관**에 얹는다(0순위-B: 합성 로직을
    # 두 벌로 만들지 않는다). dur이 있으면 그 구간만(첫 장면), 없으면 영상 전체.
    # ★deco.overlay(사장님이 올린 로고)와 **별도 슬롯**이다 — 둘 다 얹힌다.
    tpl = deco.get("template") or {}
    if tpl.get("_abspath"):
        tl = {"_abspath": tpl["_abspath"], "x": 50, "y": 50,
              "alpha": tpl.get("alpha", 1), "start": 0}
        if tpl.get("dur"):
            tl["dur"] = float(tpl["dur"])
        motion_layers = list(motion_layers) + [tl]
```

그리고 바로 아래 `has_motion = bool(motion_layers)`가 이 추가분을 반영하는지 확인한다(추가를 `has_motion` **위**에 넣어야 한다 — 아래에 넣으면 템플릿만 있을 때 합성이 통째로 스킵된다).

- [ ] **Step 4: 서버가 템플릿 파일 경로를 해석하게 한다**

`mix_pipeline.py:1556` 부근의 `ov = deco.get("overlay") or {}` 블록을 찾아, 그 블록 **다음**에 추가:

```python
        # 템플릿은 job 폴더가 아니라 **정적 자산**이다(모두가 같은 12장을 쓴다).
        tp = deco.get("template") or {}
        if tp.get("id"):
            from shopping_shorts import deco_templates
            _tp = deco_templates.abs_path(tp["id"])
            if _tp and _tp.exists():
                deco = {**deco, "template": {**tp, "_abspath": str(_tp)}}
```

- [ ] **Step 5: 통과를 확인한다**

Run: `py -m pytest shopping_shorts/tests/test_deco_template_render.py -q`
Expected: PASS (4 passed)

- [ ] **Step 6: import가 깨지지 않았는지 확인한다**

Run: `py -c "import shopping_shorts.app; import shopping_shorts.video_assemble; import shopping_shorts.mix_pipeline; print('import ok')"`
Expected: `import ok`

- [ ] **Step 7: 커밋**

```bash
git add shopping_shorts/video_assemble.py shopping_shorts/mix_pipeline.py shopping_shorts/tests/test_deco_template_render.py
git commit -m "템플릿 렌더 배선 — 모션 레이어 배관 재사용(전체/첫장면), 로고와 별도 슬롯"
```

---

## Task 6: 화면 — 고정카피 후보 카드

**Files:**
- Modify: `shopping_shorts/static/produce.html` (`hcText` textarea 위, `initHeadcopy` 내부)
- Test: `shopping_shorts/tests/test_headcopy_ui.py`

- [ ] **Step 1: HTML 자리를 만든다**

`produce.html:1015` `<div style="font-size:13px;margin-bottom:4px">헤드카피 문구 ...` 줄 **바로 위**에 삽입:

```html
          <!-- ✍ 고정카피 후보 — 대본에서 AI가 뽑는다. ★누를 때만 textarea에 들어간다
               (2026-07-19에 '자동 재채움'을 일부러 없앴다 — 자동으로 밀어넣으면 그 버그가 되살아난다) -->
          <div id="hcCopyBox" style="margin-bottom:10px">
            <div style="font-size:13px;font-weight:800;margin-bottom:6px">✍ 고정카피
              <span style="font-size:11px;color:var(--accent2);font-weight:600">— 누르면 아래 칸에 들어갑니다</span>
              <button type="button" class="tab" onclick="loadHeadcopySuggest(true)"
                style="min-width:auto;padding:3px 10px;font-size:11px;margin-left:6px">↻ 다시 뽑기</button>
            </div>
            <div id="hcCopyCards" style="display:flex;gap:6px;flex-wrap:wrap"></div>
            <div id="hcCopyMsg" class="hint" style="margin-top:4px"></div>
          </div>
```

- [ ] **Step 2: JS를 추가한다**

`produce.html`의 `function initHeadcopy(){` 바로 **위**에 삽입:

```javascript
// ✍ 고정카피 — 확정 대본에서 후보 4개. 캐시는 서버가 한다(같은 대본이면 호출 0회).
// ★textarea를 자동으로 채우지 않는다. 카드를 누를 때만 들어간다(2026-07-19 결정).
let HC_COPY_LOADED_HASH = null;
async function loadHeadcopySuggest(force){
  const box=document.getElementById('hcCopyCards'), msg=document.getElementById('hcCopyMsg');
  if(!box) return;
  const script=(STATE.script||'').trim();
  if(!script){ box.innerHTML=''; if(msg) msg.textContent='대본을 먼저 확정하면 문구를 뽑아드려요.'; return; }
  // 같은 대본이면 다시 안 부른다(서버 캐시가 있어도 왕복 자체를 아낀다).
  let h=''; try{ h=await _scriptHash(script); }catch(e){}
  if(!force && h && h===HC_COPY_LOADED_HASH) return;
  if(msg) msg.textContent='문구 뽑는 중…';
  try{
    const d=await (await fetch('/api/produce/headcopy/suggest',
      {method:'POST',headers:{'Content-Type':'application/json'},
       body:JSON.stringify({script})})).json();
    const list=(d&&d.copies)||[];
    if(!list.length){
      box.innerHTML='';
      // ★조용히 비우지 않는다 — 고장인지 준비중인지 구분되게 말해준다.
      if(msg) msg.innerHTML='<span style="color:var(--gold)">문구를 못 뽑았어요 — 아래 칸에 직접 입력하시거나 ↻ 다시 뽑기를 눌러주세요</span>';
      return;
    }
    HC_COPY_LOADED_HASH=h;
    box.innerHTML=list.map((c,i)=>
      `<button type="button" class="tab" onclick="useHeadcopy(${i})" style="min-width:auto;padding:7px 12px;font-size:12px;text-align:left;max-width:220px">`
      + `<span style="color:var(--accent2);font-size:10px">#${i+1} ${esc(c.label||'')}</span><br>${esc(c.text||'')}</button>`).join('');
    window._hcCopies=list;
    if(msg) msg.textContent = d.cached ? '' : '';
  }catch(e){
    box.innerHTML='';
    if(msg) msg.innerHTML='<span style="color:var(--fire)">문구를 불러오지 못했어요(네트워크)</span>';
  }
}
function useHeadcopy(i){
  const c=(window._hcCopies||[])[i]; if(!c) return;
  const t=document.getElementById('hcText'); if(!t) return;
  t.value=c.text||'';
  updateHC();          // 미리보기·STATE 반영은 기존 경로 그대로(새로 만들지 않는다)
  try{ logPick('S6copy', {picked:i, candidates:(window._hcCopies||[]).map(x=>x.text)}); }catch(e){}
}
```

- [ ] **Step 3: 진입 시 자동 호출을 배선한다**

`function initHeadcopy(){` 본문의 **마지막 줄 앞**에 추가:

```javascript
  loadHeadcopySuggest(false);   // 진입 시 자동(같은 대본이면 서버 캐시가 받아 호출 0회)
```

- [ ] **Step 4: 문법을 확인한다**

Run:
```bash
py -c "import re,sys;h=open('shopping_shorts/static/produce.html',encoding='utf-8').read();b=max(re.findall(r'<script>(.*?)</script>',h,re.S),key=len);open('/tmp/p.js','w',encoding='utf-8').write(b);print(len(b))"
node --check /tmp/p.js && echo SYNTAX_OK
```
Expected: `SYNTAX_OK`

- [ ] **Step 5: 회귀 테스트를 쓴다 (id 보존)**

```python
# shopping_shorts/tests/test_headcopy_ui.py
"""고정카피 UI — 자동 채움을 하지 않는가(2026-07-19 결정)를 소스로 잠근다."""
import pathlib

HTML = (pathlib.Path(__file__).resolve().parents[1] / "static" / "produce.html").read_text(encoding="utf-8")


def test_copy_box_exists():
    assert 'id="hcCopyCards"' in HTML
    assert 'id="hcCopyMsg"' in HTML


def test_textarea_id_preserved():
    """hcText가 사라지면 updateHC/applyConfig가 통째로 죽는다."""
    assert 'id="hcText"' in HTML


def test_suggest_does_not_autofill_textarea():
    """★loadHeadcopySuggest 안에서 hcText.value에 대입하면 자동채움 버그가 되살아난다."""
    i = HTML.index("async function loadHeadcopySuggest")
    j = HTML.index("function useHeadcopy", i)
    body = HTML[i:j]
    assert "t.value=" not in body and "hcText').value =" not in body


def test_use_headcopy_calls_updateHC():
    """카드를 눌렀을 때 미리보기 갱신을 기존 경로로 태우는지(새 경로를 만들면 어긋난다)."""
    i = HTML.index("function useHeadcopy")
    body = HTML[i:i + 600]
    assert "updateHC()" in body


def test_failure_message_is_visible():
    """못 뽑았을 때 조용히 비우지 않는다."""
    assert "문구를 못 뽑았어요" in HTML
```

- [ ] **Step 6: 통과를 확인한다**

Run: `py -m pytest shopping_shorts/tests/test_headcopy_ui.py -q`
Expected: PASS (5 passed)

- [ ] **Step 7: 커밋**

```bash
git add shopping_shorts/static/produce.html shopping_shorts/tests/test_headcopy_ui.py
git commit -m "화면: 고정카피 후보 카드 — 누를 때만 입력(자동채움 금지), 실패는 말해준다"
```

---

## Task 7: 화면 — 템플릿 카드 + 표시 구간

**Files:**
- Modify: `shopping_shorts/static/produce.html`
- Test: `shopping_shorts/tests/test_deco_template_ui.py`

- [ ] **Step 1: HTML 자리를 만든다**

`produce.html:1181` `🖼 이미지 오버레이` 블록을 찾아 그 **바로 위**에 삽입:

```html
              <!-- 🖼 템플릿 — 기본제공 12종. 사장님이 올린 로고(아래 이미지 오버레이)와 **별개**로 함께 얹힌다. -->
              <div style="background:var(--inset);border:1px solid var(--line);border-radius:10px;padding:10px 12px;margin-bottom:12px">
                <div style="font-size:13px;font-weight:800;margin-bottom:6px">🖼 템플릿
                  <span style="font-size:11px;color:var(--sub);font-weight:400">— 영상 위에 얹는 틀</span></div>
                <div id="tplCards" style="display:flex;gap:8px;flex-wrap:wrap;margin-bottom:8px"></div>
                <div style="font-size:12px;color:var(--sub)">표시 구간
                  <label style="margin-left:8px;cursor:pointer"><input type="radio" name="tplSpan" value="full" checked onchange="setTemplateSpan('full')"> 영상 전체</label>
                  <label style="margin-left:8px;cursor:pointer"><input type="radio" name="tplSpan" value="first" onchange="setTemplateSpan('first')"> 첫 장면만</label>
                </div>
              </div>
```

- [ ] **Step 2: JS를 추가한다**

Task 6에서 넣은 `function useHeadcopy(i){...}` 블록 **다음**에 삽입:

```javascript
// 🖼 템플릿 — 목록은 서버가 준다(정의처는 deco_templates 하나). 선택은 STATE.deco.template.
let TPL_LIST=[];
async function loadTemplates(){
  const box=document.getElementById('tplCards'); if(!box) return;
  if(!TPL_LIST.length){
    try{ const d=await (await fetch('/api/produce/templates')).json(); TPL_LIST=(d&&d.templates)||[]; }
    catch(e){ box.innerHTML='<span class="hint" style="color:var(--fire)">템플릿을 불러오지 못했어요</span>'; return; }
  }
  renderTemplateCards();
}
function renderTemplateCards(){
  const box=document.getElementById('tplCards'); if(!box) return;
  const cur=(STATE.deco&&STATE.deco.template&&STATE.deco.template.id)||'';
  const none=`<button type="button" class="tab${cur?'':' on'}" onclick="pickTemplate('')"
    style="min-width:auto;padding:6px 12px;font-size:12px">없음</button>`;
  box.innerHTML = none + TPL_LIST.map(t=>
    `<button type="button" class="tab${cur===t.id?' on':''}" onclick="pickTemplate('${t.id}')" title="${esc(t.name)}"
      style="min-width:auto;padding:0;overflow:hidden;width:52px;height:92px;background:#0a1017;position:relative">
      <img src="${t.url}" alt="${esc(t.name)}" style="width:100%;height:100%;object-fit:cover"></button>`).join('');
}
function pickTemplate(id){
  if(!STATE.deco) STATE.deco={extra_texts:[], motion:null};
  if(!id){ STATE.deco.template=null; }
  else {
    const span=(STATE.deco.template&&STATE.deco.template.span)||'full';
    STATE.deco.template={id:id, span:span};
  }
  renderTemplateCards(); renderTemplatePreview(); saveHeadcopy();
}
function setTemplateSpan(span){
  if(!STATE.deco||!STATE.deco.template) return;   // 고른 템플릿이 없으면 저장할 것도 없다
  STATE.deco.template.span=span;
  saveHeadcopy();
}
// 미리보기 — 실제 렌더와 같은 그림을 보여준다(9:16 위에 그대로 덮는다).
function renderTemplatePreview(){
  const pv=document.getElementById('hcPreview'); if(!pv) return;
  let el=document.getElementById('tplPreview');
  const t=STATE.deco&&STATE.deco.template;
  if(!t||!t.id){ if(el) el.style.display='none'; return; }
  const meta=TPL_LIST.find(x=>x.id===t.id); if(!meta) return;
  if(!el){
    el=document.createElement('img'); el.id='tplPreview';
    el.style.cssText='position:absolute;inset:0;width:100%;height:100%;object-fit:cover;pointer-events:none;z-index:4';
    pv.appendChild(el);
  }
  el.src=meta.url; el.style.display='';
}
```

- [ ] **Step 3: 진입 시 호출을 배선한다**

Task 6 Step 3에서 넣은 `loadHeadcopySuggest(false);` **바로 아래**에 추가:

```javascript
  loadTemplates();              // 템플릿 카드(목록은 1회만 읽고 캐시)
  renderTemplatePreview();      // 복원된 선택을 미리보기에 반영
```

- [ ] **Step 4: 문법을 확인한다**

Run:
```bash
py -c "import re;h=open('shopping_shorts/static/produce.html',encoding='utf-8').read();b=max(re.findall(r'<script>(.*?)</script>',h,re.S),key=len);open('/tmp/p.js','w',encoding='utf-8').write(b)"
node --check /tmp/p.js && echo SYNTAX_OK
```
Expected: `SYNTAX_OK`

- [ ] **Step 5: 테스트를 쓴다**

```python
# shopping_shorts/tests/test_deco_template_ui.py
"""템플릿 UI — 선택이 STATE.deco.template에 저장되고 로고 슬롯을 안 건드리는가."""
import pathlib

HTML = (pathlib.Path(__file__).resolve().parents[1] / "static" / "produce.html").read_text(encoding="utf-8")


def test_template_box_exists():
    assert 'id="tplCards"' in HTML
    assert 'name="tplSpan"' in HTML


def test_picks_into_template_slot_not_overlay():
    """★STATE.deco.overlay(사장님 로고)를 건드리면 로고가 사라진다."""
    i = HTML.index("function pickTemplate")
    j = HTML.index("function setTemplateSpan", i)
    body = HTML[i:j]
    assert "STATE.deco.template" in body
    assert "STATE.deco.overlay" not in body, "템플릿이 로고 슬롯을 덮어쓴다"


def test_span_saved():
    i = HTML.index("function setTemplateSpan")
    body = HTML[i:i + 400]
    assert "span" in body and "saveHeadcopy()" in body


def test_overlay_upload_still_present():
    """기존 이미지 오버레이 기능이 살아 있는가(옮기기만 했지 지우지 않았다)."""
    assert 'id="ovFile"' in HTML or "이미지 오버레이" in HTML
```

- [ ] **Step 6: 통과를 확인한다**

Run: `py -m pytest shopping_shorts/tests/test_deco_template_ui.py -q`
Expected: PASS (4 passed)

- [ ] **Step 7: 커밋**

```bash
git add shopping_shorts/static/produce.html shopping_shorts/tests/test_deco_template_ui.py
git commit -m "화면: 템플릿 카드 12종 + 표시구간(전체/첫장면) — 로고 슬롯 불가침"
```

---

## Task 8: 서버가 span을 실제 초로 바꾼다

**Files:**
- Modify: `shopping_shorts/mix_pipeline.py` (Task 5 Step 4에서 넣은 블록)
- Test: `shopping_shorts/tests/test_template_span.py`

`span:'first'`는 화면의 말이고, 렌더가 아는 건 `dur`(초)다. **그 변환을 한 곳에서** 한다.

- [ ] **Step 1: 실패하는 테스트를 쓴다**

```python
# shopping_shorts/tests/test_template_span.py
"""span('full'|'first') → dur(초) 변환. 화면의 말과 렌더의 말을 잇는 유일한 지점."""
from shopping_shorts import mix_pipeline as mp


def test_full_span_has_no_dur():
    out = mp._template_layer({"id": "tpl_01", "span": "full"}, first_beat_dur=3.2)
    assert out is not None
    assert "dur" not in out or out["dur"] is None


def test_first_span_uses_first_beat_duration():
    out = mp._template_layer({"id": "tpl_01", "span": "first"}, first_beat_dur=3.2)
    assert out["dur"] == 3.2


def test_first_span_without_beat_falls_back_to_full():
    """비트 길이를 모르면 '첫 장면만'을 흉내내다 0초로 사라지느니 전체로 둔다."""
    out = mp._template_layer({"id": "tpl_01", "span": "first"}, first_beat_dur=0)
    assert "dur" not in out or out["dur"] is None


def test_unknown_template_id_returns_none():
    assert mp._template_layer({"id": "tpl_99", "span": "full"}, first_beat_dur=3) is None


def test_no_template_returns_none():
    assert mp._template_layer(None, first_beat_dur=3) is None
    assert mp._template_layer({}, first_beat_dur=3) is None
```

- [ ] **Step 2: 실패를 확인한다**

Run: `py -m pytest shopping_shorts/tests/test_template_span.py -q`
Expected: FAIL — `AttributeError: module 'shopping_shorts.mix_pipeline' has no attribute '_template_layer'`

- [ ] **Step 3: 함수를 만들고 Task 5의 인라인 블록을 이걸로 교체한다**

`mix_pipeline.py`에 함수를 추가(모듈 최상단 함수들 근처):

```python
def _template_layer(tpl, first_beat_dur=0):
    """꾸미기 템플릿 → 렌더가 쓸 레이어 dict. 없거나 모르는 id면 None.

    ★span('full'|'first')을 dur(초)로 바꾸는 **유일한 지점**이다(0순위-B).
    화면은 'first'라고 말하고 렌더는 dur만 안다 — 변환이 두 곳에 생기면 어긋난다.
    """
    from shopping_shorts import deco_templates
    tpl = tpl or {}
    tid = tpl.get("id")
    if not tid:
        return None
    p = deco_templates.abs_path(tid)
    if not p or not p.exists():
        return None
    out = {"_abspath": str(p), "id": tid, "alpha": tpl.get("alpha", 1)}
    # 'first'인데 비트 길이를 모르면 전체로 둔다 — dur=0을 주면 화면에서 아예 안 보인다.
    if tpl.get("span") == "first" and first_beat_dur and first_beat_dur > 0:
        out["dur"] = float(first_beat_dur)
    return out
```

Task 5 Step 4에서 넣었던 인라인 블록을 **삭제하고** 다음으로 교체(같은 자리):

```python
        # 템플릿은 job 폴더가 아니라 **정적 자산**이다(모두가 같은 12장을 쓴다).
        # span→dur 변환은 _template_layer 한 곳에서만 한다.
        _first = 0
        try:
            _tb = (job.get("edit_plan") or {}).get("beats") or []
            _first = float(_tb[0].get("dur") or 0) if _tb else 0
        except Exception:
            _first = 0
        _tl = _template_layer(deco.get("template"), first_beat_dur=_first)
        if _tl:
            deco = {**deco, "template": {**(deco.get("template") or {}), **_tl}}
```

- [ ] **Step 4: 통과를 확인한다**

Run: `py -m pytest shopping_shorts/tests/test_template_span.py -q`
Expected: PASS (5 passed)

- [ ] **Step 5: 관련 테스트를 함께 돌린다**

Run: `py -m pytest shopping_shorts/tests/test_deco_template_render.py shopping_shorts/tests/test_template_span.py shopping_shorts/tests/test_deco_templates_meta.py -q`
Expected: PASS (전부)

- [ ] **Step 6: 커밋**

```bash
git add shopping_shorts/mix_pipeline.py shopping_shorts/tests/test_template_span.py
git commit -m "span→dur 변환을 _template_layer 한 곳으로 — 첫장면 길이는 첫 비트에서"
```

---

## Task 9: UI 4탭 개편 (★가장 위험한 태스크 — 옮기기만 한다)

**Files:**
- Modify: `shopping_shorts/static/produce.html:1014~1052` 부근(우측 컨트롤 영역)
- Test: `shopping_shorts/tests/test_deco_tabs_ui.py`

**규칙: 기능을 지우지 않는다. id를 바꾸지 않는다. 좌측 미러는 건드리지 않는다.**

- [ ] **Step 1: 먼저 회귀 테스트를 쓴다(개편 전에 잠근다)**

```python
# shopping_shorts/tests/test_deco_tabs_ui.py
"""★탭 개편 회귀 — 옮기는 과정에서 id가 사라지면 그 기능이 조용히 죽는다.

updateHC/applyConfig/renderStyleCards/saveMyPreset이 전부 getElementById로 잡는다.
이 테스트는 "개편 후에도 그 id들이 살아 있는가"만 본다.
"""
import pathlib

HTML = (pathlib.Path(__file__).resolve().parents[1] / "static" / "produce.html").read_text(encoding="utf-8")

# 없어지면 그 기능이 죽는 id들(실측으로 추린 목록)
REQUIRED_IDS = [
    "hcText", "hcPreview", "hcPreviewText", "capPreviewText",
    "fullPresets", "moreStylesBtn", "myPresets", "myPresetName",
    "highlightRuleList", "fxAdvOn", "fxWizard", "advDetails", "advBody",
    "hcFont", "hcColor", "hcWeight", "hcPresets",
    "tplCards", "hcCopyCards",
]


def test_all_required_ids_present():
    missing = [i for i in REQUIRED_IDS if f'id="{i}"' not in HTML]
    assert not missing, f"탭 개편이 이 id들을 없앴다(기능이 죽는다): {missing}"


def test_tab_bar_exists():
    assert 'id="decoTabs"' in HTML


def test_four_tab_panes():
    for pane in ("decoPaneCopy", "decoPaneStyle", "decoPaneTpl", "decoPaneFx"):
        assert f'id="{pane}"' in HTML, f"{pane} 없음"


def test_preview_mirror_untouched():
    """좌측 미러는 탭 밖에 있어야 한다 — 탭을 바꿔도 계속 보여야 한다."""
    i = HTML.index('id="hcPreview"')
    j = HTML.index('id="decoTabs"')
    assert i < j, "미리보기가 탭 안으로 들어갔다(탭 전환 시 사라진다)"
```

- [ ] **Step 2: 개편 전 상태로 돌려 실패를 확인한다**

Run: `py -m pytest shopping_shorts/tests/test_deco_tabs_ui.py -q`
Expected: FAIL — `decoTabs 없음` 계열(아직 안 만들었으므로). `test_all_required_ids_present`는 **PASS**여야 한다(개편 전이라 id가 다 살아 있다).

- [ ] **Step 3: 탭 바와 pane을 넣는다**

`produce.html:1014` `<div style="flex:1;min-width:300px">`(우측 컨트롤 시작) **바로 다음 줄**에 삽입:

```html
          <!-- 🗂 4탭 — 우측 컨트롤만 가른다. 좌측 미러는 탭 밖(항상 보인다). -->
          <div id="decoTabs" style="display:flex;gap:6px;margin-bottom:12px;flex-wrap:wrap">
            <button type="button" class="tab on" data-decotab="copy" onclick="decoTab('copy')" style="min-width:auto;padding:7px 14px;font-size:12px">✍ 문구</button>
            <button type="button" class="tab" data-decotab="style" onclick="decoTab('style')" style="min-width:auto;padding:7px 14px;font-size:12px">🎯 스타일</button>
            <button type="button" class="tab" data-decotab="tpl" onclick="decoTab('tpl')" style="min-width:auto;padding:7px 14px;font-size:12px">🖼 템플릿</button>
            <button type="button" class="tab" data-decotab="fx" onclick="decoTab('fx')" style="min-width:auto;padding:7px 14px;font-size:12px">✨ 효과</button>
          </div>
```

그 다음, 기존 블록들을 **잘라내지 말고 감싸기만** 한다. 각 블록의 앞뒤에 여는/닫는 div를 넣는다:

- `<div id="decoPaneCopy">` … `</div>` 로 감쌀 것: 고정카피 박스(`hcCopyBox`) + 헤드카피 문구 라벨·`hcText` + 강조 단어 박스
- `<div id="decoPaneStyle" style="display:none">` … `</div>`: 🎯 완성 스타일 + `moreStylesBtn` + ⭐ 내 프리셋 + `advDetails`(직접 다듬기) 전체
- `<div id="decoPaneTpl" style="display:none">` … `</div>`: 🖼 템플릿(Task 7) + 🖼 이미지 오버레이(기존)
- `<div id="decoPaneFx" style="display:none">` … `</div>`: `fxAdvBox`(고급효과) 전체

⚠️ **이동이 아니라 감싸기다.** 블록 내부의 HTML은 한 글자도 바꾸지 않는다 — id·onclick·style 전부 그대로.

- [ ] **Step 4: 탭 전환 JS를 추가한다**

Task 7의 `renderTemplatePreview` 함수 **다음**에 삽입:

```javascript
// 🗂 탭 전환 — 보이기/숨기기만 한다. 상태(STATE)는 아무것도 안 바꾼다.
function decoTab(name){
  const panes={copy:'decoPaneCopy', style:'decoPaneStyle', tpl:'decoPaneTpl', fx:'decoPaneFx'};
  Object.keys(panes).forEach(k=>{
    const el=document.getElementById(panes[k]);
    if(el) el.style.display=(k===name)?'':'none';
  });
  document.querySelectorAll('#decoTabs [data-decotab]').forEach(b=>{
    b.classList.toggle('on', b.getAttribute('data-decotab')===name);
  });
}
```

- [ ] **Step 5: 통과를 확인한다**

Run: `py -m pytest shopping_shorts/tests/test_deco_tabs_ui.py -q`
Expected: PASS (4 passed) — 특히 `test_all_required_ids_present`가 **계속 통과**해야 한다

- [ ] **Step 6: 문법을 확인한다**

Run:
```bash
py -c "import re;h=open('shopping_shorts/static/produce.html',encoding='utf-8').read();b=max(re.findall(r'<script>(.*?)</script>',h,re.S),key=len);open('/tmp/p.js','w',encoding='utf-8').write(b)"
node --check /tmp/p.js && echo SYNTAX_OK
```
Expected: `SYNTAX_OK`

- [ ] **Step 7: 기존 꾸미기 테스트가 안 깨졌는지 본다**

Run: `py -m pytest shopping_shorts/tests -k "style or deco or headcopy or preset or produce" -q`
Expected: 실패 0건(기준선에 이미 있던 것 제외)

- [ ] **Step 8: 커밋**

```bash
git add shopping_shorts/static/produce.html shopping_shorts/tests/test_deco_tabs_ui.py
git commit -m "꾸미기 4탭 개편 — 감싸기만 하고 id 전부 보존(회귀 테스트로 잠금)"
```

---

## Task 10: 실브라우저 검증 + 전체 회귀

**Files:** 없음(검증 전용)

- [ ] **Step 1: 포트 선점을 확인한다** (옛 프로세스가 응답하면 옛 코드를 보게 된다)

Run(PowerShell): `Get-NetTCPConnection -LocalPort 8899 -State Listen -ErrorAction SilentlyContinue`
Expected: 아무것도 안 나옴. 나오면 `Stop-Process -Id <OwningProcess> -Force`

- [ ] **Step 2: 로컬 서버를 띄운다**

Run: `DASH_PASS= py -m uvicorn shopping_shorts.app:app --host 127.0.0.1 --port 8899` (백그라운드)
확인: `curl -s -o /dev/null -w "%{http_code}" http://127.0.0.1:8899/produce` → `200`

- [ ] **Step 3: 서버가 내 코드를 주는지 확인한다**

Run: `curl -s http://127.0.0.1:8899/produce | grep -c "decoTabs"`
Expected: `1` 이상 (0이면 옛 프로세스가 응답 중)

- [ ] **Step 4: 템플릿 API·PNG를 실제로 받아본다**

Run: `curl -s http://127.0.0.1:8899/api/produce/templates | py -c "import sys,json;d=json.load(sys.stdin);print(len(d['templates']), d['templates'][0])"`
Expected: `12 {...'id': 'tpl_01'...}`

- [ ] **Step 5: 브라우저로 6단계를 연다**

Playwright로 `http://127.0.0.1:8899/produce`를 열고 다음을 **눈으로** 확인:
- 탭 4개가 보이고, 누르면 그 내용만 바뀐다
- 좌측 미리보기는 **탭을 바꿔도 계속 보인다**
- 🖼 템플릿 탭에서 카드 12개 + [없음]이 뜬다
- 템플릿을 누르면 미리보기에 즉시 덮인다 / [없음]을 누르면 사라진다
- 🎯 스타일 탭에서 완성 스타일 카드가 **예전처럼** 뜨고 적용된다(회귀 확인)
- 콘솔 에러 **0건**

- [ ] **Step 6: 전체 pytest 기준선을 대조한다**

Run: `py -m pytest shopping_shorts/tests -q 2>&1 | tail -5`
그리고 `git stash && py -m pytest shopping_shorts/tests -q 2>&1 | tail -3 && git stash pop`으로 **변경 전 실패 목록**과 비교.
Expected: **실패가 늘지 않았다**("전부 green"이 아니라 증가 0이 기준)

- [ ] **Step 7: 서버를 내린다**

Run(PowerShell): `Get-NetTCPConnection -LocalPort 8899 -State Listen -ErrorAction SilentlyContinue | ForEach-Object { Stop-Process -Id $_.OwningProcess -Force }`

- [ ] **Step 8: 핸드오프·로그를 남기고 배포한다**

```bash
# handoff/꾸미기.md 에 이번 작업 추가, wiki/log.d/꾸미기.md 에 1~3줄
git add -A && git commit -m "기록: 꾸미기 고정카피·템플릿·4탭"
cd "C:/Users/CH/Desktop/로또의 주식" && py tools/track.py finish 꾸미기
```
⚠️ `finish`가 exit 0을 줘도 병합됐다는 뜻이 아니다 — `git log origin/main --oneline -3`으로 **직접 확인**한다.

---

## Self-Review

**1. 스펙 커버리지**
| 스펙 항목 | 태스크 |
|---|---|
| §3 고정카피 AI 생성 | Task 2 |
| §3 진입 시 자동 + 캐시(호출 0회) | Task 3, Task 6 Step 3 |
| §3 빈칸 시작 유지(자동채움 금지) | Task 6 Step 2·5 (`test_suggest_does_not_autofill_textarea`) |
| §3 실패를 조용히 넘기지 않음 | Task 6 (`test_failure_message_is_visible`) |
| §4 PNG 12종 + 생성 스크립트 커밋 | Task 1 |
| §4 전체/첫장면 토글 | Task 7(화면) + Task 8(변환) |
| §4 로고와 별도 슬롯 | Task 5, Task 7 (`test_picks_into_template_slot_not_overlay`) |
| §5 4탭 + id 보존 | Task 9 |
| §6 회귀·캐시 실측·렌더 확인 | Task 10 |

**2. 플레이스홀더:** 없음(모든 코드 블록이 실제 내용). 유일한 서술형은 Task 9 Step 3의 "감싸기" 지시인데, 감쌀 대상 4개와 규칙(내부 무수정)을 명시했다.

**3. 타입 일관성:** `STATE.deco.template = {id, span, alpha?}` — Task 7이 만들고, Task 8 `_template_layer`가 읽고, Task 5가 `_abspath`를 붙인다. `span`은 `'full'|'first'` 두 값뿐이며 dur 변환은 Task 8 한 곳. 함수명 `_template_layer`·`_motion_layer_filters`·`loadHeadcopySuggest`·`useHeadcopy`·`pickTemplate`·`setTemplateSpan`·`decoTab` 전부 정의처와 호출처가 일치.

**4. 알려진 한계(의도한 것):**
- 카피 캐시는 프로세스 메모리다 — 서버 재시작 시 비지만, "한 세션에서 오가는 동안 과금 0"이라는 목적은 달성한다. DB 스키마 변경 위험을 지지 않는 쪽을 택했다.
- 미리보기의 템플릿은 `object-fit:cover`로 덮는다 — 실제 렌더(1080×1920 그대로 합성)와 비율이 같아 어긋나지 않는다.
