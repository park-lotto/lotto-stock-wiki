> **소유 트랙**: 장면라이브러리 — 이 파일은 장면라이브러리 트랙 세션만 수정한다. 다른 트랙은 읽기만.
> 규칙: `handoff/README.md`

## 🔊 효과음(sfx) 역할매칭 — **완성·라이브** (2026-07-22, 병합 `f1570b0e3`)

스펙 `docs/superpowers/specs/2026-07-22-쇼핑쇼츠-장면라이브러리-효과음-역할매칭-design.md` / 지시서 `shopping_shorts/docs/OPUS_지시서_효과음역할매칭_2026-07-22.md`.
`match_sfx`(역할매칭만·Gemini 0회·중복허용·타이밍 hook=첫/기타=마지막 자막세그먼트) → amix 다중트랙(sfx_volume 기본 60) → role 통제어휘 검증 → 업로드 드롭다운 + 검수판 🔊배지·빼기토글. 신규 테스트 32 green, 기존 실패 증가 0, 실 ffmpeg 검증됨.

**⏭ 남은 것**: ①`sfx_volume` 기본 60은 추정치 — 사장님 실제 효과음 파일 담아서 렌더 후 귀로 튜닝 ②"last 세그먼트" 타점이 모든 역할에 자연스러운지 실 대본으로 확인 ③sfx가 비트보다 길면 다음 비트로 흘러넘침(v1 알려진 한계, 스펙 §4.3).

---

## 🎬 장면 라이브러리(재사용 짤 뱅크) — **페이즈1 완료·배포·실사용 검증까지 끝** (2026-07-15)

**이어서 하려면**: 이 창에 `장면 라이브러리 트랙 이어서` 라고 치면 된다.

**한 줄**: 자주 쓰는 후킹 장면(놀라는짤·효과음·"비법 한 스푼" 등)을 구간컷으로 모아두고, 나중에 믹스할 때 AI가 대본 비트에 맞는 걸 자동배치. = "AI 큐레이션 재사용 짤 뱅크".

- 스펙 `docs/superpowers/specs/2026-07-15-쇼핑쇼츠-장면라이브러리-재사용짤뱅크-design.md`
- 계획 `docs/superpowers/plans/2026-07-15-쇼핑쇼츠-장면라이브러리-페이즈1.md` (6태스크 TDD)
- SDD 원장 `.superpowers/sdd/progress.md` 의 "장면 라이브러리 페이즈1" 섹션 (태스크별 리뷰·실증 기록)

---

## ✅ 완료 (전부 커밋·자동푸시됨)

| Task | 내용 | 커밋 |
|---|---|---|
| 1 | `scene_assets` 테이블 + 다축태그 CRUD(멀티테넌시 격리) | `a50a8ece` |
| 2 | 구간컷·오디오추출·썸네일 유틸(720x1280 규격 통일) | `19dde595` + `6ab0e134` |
| 3 | Gemini 다축태그 자동초안(캐스케이드 키풀·통제어휘 role·형태온전 폴백) | `7522ed12` + `5c1328e4` |
| 4 | `/api/scene/*` 라우트(2스텝 저장·토큰 경로재구성·소유권 격리) | `97c5b693` + `78ce7196` + `23e9f24f` |
| 5 | 관리 UI `scene_library.html` + 사이드바 | `a9d5f005` + `8cfca5f1` |
| 6 | 레퍼런스 랭킹 → 장면 담기 다리 | `d71788be` |

- 테스트: `test_store_scene.py` 7 / `test_scene_assets.py` 16 / `test_app_scene.py` 30 — 전부 green.
- 태스크별 opus 리뷰 전부 통과(스펙 ✅ / Critical 0). 리뷰가 잡은 Important 9건은 **전부 계획서·설계 층위 결함**이었고 수정 후 실증 완료.

---

## ✅ 실렌더 grounding — 부분 완료 (2026-07-15, 컨트롤러 직접 관측)

**① ffmpeg 실구동 + 규격 = 통과.** 1920x1080/30fps 소스를 실제로 잘라 `ffprobe`로 찍음:
`width=720 / height=1280 / codec=h264 / pix_fmt=yuv420p / r_frame_rate=30/1` — 페이즈2 `concat -c copy` 호환.
crop이 실제로 동작(가로 소스를 세로로 통일). 길이도 유효구간(1.0~3.5) 요청 시 **정확히 2.5초**.
(구간이 소스 끝을 넘으면 조용히 짧아짐 — 소스 5초에 3.0~5.5 요청 시 2.015초. UI는 재생중 영상에서
찍으므로 실사용에선 발생 안 하지만, 알아둘 것.)

**② Gemini autotag 실호출 = 통과.** 캐스케이드 키 14개 살아있음(`pipeline.atoms.key_vault`).
실제 이미지를 넣어 실응답 받음 — 스키마 5키 완비, `role`이 통제어휘 안.
**설계의 심장이 적대적 조건에서 버팀**: 일부러 거짓 맥락(`category="레시피"`, `caption="이거 한 스푼이면 끝"`)을
넣었는데 이미지엔 숟가락도 요리도 없었다. Gemini는 **낚이지 않고 보이는 것만 서술**:
> "정장을 입은 중년 남성이 검은 배경 앞에서 손을 주머니에 넣고 미소를 지으며 서 있고 주변에 금융 그래프와 차트 아이콘들이 떠 있다."
> subject="중년의 남성 비즈니스맨" / role="훅" / tone="전문적인·자신감 있는"

사람이 눈으로 본 것과 대조해 정확·무환각 확인. "보이는 것만, 추측·해석 금지" 규칙이 작동.

**③ 중요 부수확인**: 이 PC엔 `SHORTS_GEMINI_KEYS`(좁은 단일 키풀)가 **0개**다.
계획서가 `_vault_call`의 캐스케이드를 쓰라고 못박지 않았으면 autotag가 로컬에서 아예 안 돌았다.

**④ "가루↔액체↔세제" 유사물 변별 = 통과 (2026-07-15 추가 관측).** 서버의 도서관 영구보관 실 릴스
2편을 내려받아(`wiki_media/`, 읽기전용 scp) 실제로 잘라 태깅했다. 스펙 §1.1이 지목한 바로 그 함정:

| 영상 | 사람이 본 것 | AI `scene_desc` / `subject` |
|---|---|---|
| 감자전(레시피, "부침가루 넣지 마세요") | 팬에 채썬 감자, 손이 뒤집개로 누름 | "프라이팬에 담긴 채 썰린 감자를 뒤집개로 꾹꾹 눌러 펴고 있다" / **"채 썰린 감자"** |
| 생활용품 | 흰 대야에 **뿌연 흰 물**, 장갑 낀 손이 젓는 중 | "흰색 싱크대 볼에 담긴 물을 휘젓고 있다" / **"물, 투명한 비닐 장갑"** |

둘 다 사람 관측과 일치. **"흰 것"으로 뭉뚱그리면 세제↔음식이 섞이는 정확한 상황인데 소재가 완전히
갈렸다**(채 썰린 감자 vs 물). 게다가 AI는 뿌연 물을 보고 **"세제"라고 추측하지 않았다** — 화면에
세제 자체는 안 보이므로 "보이는 것만·추측 금지" 규칙에 충실. `category`(레시피/생활용품)까지
더하면 페이즈2 §6 1차 필터가 이 둘을 섞을 수 없다.
(실 릴스 원본은 이미 720x1280/30fps — 인스타 릴스 표준. 잘린 클립도 동일 규격 유지 확인.)

**⑤ 브라우저 UI 경로 = 실사용으로 해소 (2026-07-15 18:36, 사장님이 라이브에서 직접 사용).**
서버 `scene_assets`에 실제 자산 1건이 있다 — 랭킹의 실 릴스(`source_ref=DaxBfosJJ3h`)에서
**9.3초 구간을 잘라 "샘플"로 저장**, AI가 `subject="튀김 요리"` / `category=레시피` 부여.
서버에서 `ffprobe`로 확인: **`720x1280 / h264 / 30fps / 9.3초`** — 정상.
즉 랭킹 카드 → 🎞 장면 담기 → 구간 찍기 → 자르기+AI태그 → 저장 → 라이브러리 적재까지
**프로덕션에서 실제로 동작함이 사용자 사용으로 증명됨.**
(생성 시각이 C-1 수정 배포(17:39) 이후라 토큰 소비 경로도 탄 자산이다.)

### ⏭ 아직 미관측 (정직하게 남김)
- (해소됨 → 위 ⑤ 참조) ~~브라우저 UI 경로~~ — 2026-07-15 18:36 사장님 실사용으로 증명.

## ⏭ 남은 것 (페이즈1 마무리)

관측은 **전부 끝났다**(위 ①~⑤). 남은 건 행정적인 것뿐:

1. **수정분 재리뷰** — 최종 whole-branch 리뷰의 Critical 1 + Important 3을 `a1ec715e`로 닫았고,
   컨트롤러가 리뷰어 재현 시나리오를 그대로 재실행해 검증했다(같은 토큰 2회 commit → #2가 404 /
   자산 하나 삭제해도 다른 쪽 200 생존 / update로 sfx에 `render_mode='banana'` → 422).
   형식상 재리뷰가 남았으나 **머지 차단 사유는 없음**.
2. **Minor 4건 triage** (최종리뷰 `.superpowers/sdd/scene-p1-final-review.md`):
   sfx commit이 `.mp4`/`.jpg` 고아를 남김(DB가 몰라 삭제로도 안 지워짐 — **고아 청소는 "커밋 안 된
   클립"이 아니라 "DB 미참조 파일 전부"로 설계할 것**) / `/api/scene/list`가 서버 절대경로 노출 /
   clip을 `render_mode` 없이 commit 가능 + 배지가 NULL을 "컷어웨이"로 거짓표기 / 정규식 `re.match`+`$`가
   후행 개행 통과(경로구분자를 못 넣어 악용 불가, `fullmatch` 권장).

---

## 🗓 페이즈2 착수 조건 (2026-07-15 사장님 문의 "페이즈2는 언제?"에 대한 답)

**일정 미정 — 사장님 결정사항.** 지금 착수를 권하지 않는 이유 3가지:

1. **재료가 1개뿐이다.** 매칭 AI를 만들어도 고를 게 없으면 검증이 안 된다. 장면이 10~20개 쌓여야
   "이 비트엔 이 짤" 판정이 맞는지 볼 수 있다.
2. **페이즈2가 확장할 `video_assemble.py`를 모션효과 트랙이 지금 쓰고 있다**(Task9 실렌더 검증).
   지금 손대면 2026-07-15에만 3번 난 커밋 흡수 사고가 재발한다.
3. **트랙격리 트랙이 폴더분리를 세팅 중**이다. 그 뒤가 훨씬 안전하다.

**착수 전 필수 선행**: 오디오 `-ar`/`-ac`를 `video_assemble`과 **양쪽 함께** 고치기(아래 §2 참조,
실측 증명됨 — 한쪽만 고치면 오히려 어긋난다).

**추천 순서**: 장면 더 담기(재료 + 실사용 감각) → 폴더분리 → 모션효과가 `video_assemble` 손 뗌
→ 오디오 규격 → 페이즈2 설계.

---

## ⚠️ 다른 트랙·나중으로 넘긴 것 (여기서 하지 말 것)

1. **SSRF는 이 저장소의 계통적 문제 — 별도 트랙 필요.**
   `/api/scene/save/prepare`엔 1차 방어(scheme 검사 + 사설/루프백/링크로컬 IP 차단)를 넣었지만, `extract_script_by_url` 등 **기존 엔드포인트에 같은 구멍이 그대로** 있다. `/api/thumb`(app.py:1385-1390)만 호스트 allowlist로 막힌 상태 = 방어가 라우트마다 제각각.
   넣은 1차 방어도 **불완전**: TOCTOU, 리다이렉트 추적(302→내부망), IPv6 매핑 우회가 남는다. 서버가 AWS Lightsail이라 `169.254.169.254` 메타데이터가 실제 표적.
   → 공용 헬퍼로 뽑아 전 엔드포인트 일괄 적용 + `allow_redirects=False`.

2. **페이즈2로 넘긴 것** (계획서 "페이즈2로 미루는 것" 섹션에 상세):
   - AI 매칭·자동배치(`match_scene_assets`, `mix_pipeline`의 asset_match 스텝)
   - 렌더 4처리(replace/cutaway/sfx/overlay) — **cutaway 자막 t0 싱크가 최대 리스크**. 비트의 실제 화면 길이는 `primary.end - primary.start`가 아니라 나레이션에서 재계산된 `target_seconds`(`edit_plan.py:365-367`, 4.5자/초)임을 반드시 반영.
   - **오디오 규격 미고정**: `make_clip`과 `video_assemble` **둘 다** `-ar`/`-ac`가 없어 소스값이 상속된다. `concat -c copy`는 오디오 파라미터도 일치해야 안전 → **한쪽만 고치면 오히려 어긋난다. 양쪽 함께** + 실파일 concat 스모크로 먼저 재현할 것.
   - 믹스 검수 배치 패널
   - **영상제작소(produce) 저장 다리** — `produce.html` 대본믹스 통합 트랙이 끝난 뒤
   - prepare 후 커밋 안 된 고아 클립 청소 크론

---

## 📌 이 트랙에서 얻은 교훈

- **서브에이전트 보고를 그대로 믿으면 안 된다.** Task4 수정 때 `asset_type` 검증을 지시했는데 반영 안 된 채 "이탈 없음" 보고가 올라왔다. 컨트롤러가 직접 프로브를 날려서야 발견(악성 문자열이 200으로 저장됨). 보고는 반드시 실행으로 검증할 것.
- **파일을 파이썬 스크립트로 통째 재작성하지 말 것.** Windows에서 LF→CRLF로 바뀌어 phantom-modified가 되고 공유 워킹트리의 다른 세션 pull을 막는다. Edit로 부분 수정만.
- **`git commit -- <경로> -m "msg"`는 실패한다.** `--` 뒤는 전부 pathspec이라 `-m`과 메시지를 파일명으로 읽는다(`error: pathspec '-m' did not match`). **`-m`이 먼저, `--`가 마지막.**
- `.git/hooks/post-commit`이 무조건 `git push`다 → **커밋 = 즉시 라이브 배포.** `app.py`를 건드리는 커밋은 반드시 `python -c "from shopping_shorts import app"` 게이트를 먼저 통과시킬 것.
