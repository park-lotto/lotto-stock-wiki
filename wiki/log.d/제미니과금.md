# 제미니과금 — 작업 로그

2026-08-16 — [구현·실측] **Gemini 토큰 계측 + 통계 4종 라이브**(`ae8304326`).
호출부 34곳을 안 고치고 클라이언트 6곳을 래핑(`usage_meter.MeteredClient`) →
`gemini_usage` 테이블에 op/job_id/customer_id별로 쌓는다. `tools/usage_report.py`가
영상1편당·기능별·사용자별·일자별로 보여준다. 목적은 크레딧 절약이 아니라
**사용자가 자기 Vertex를 등록해 쓰게 하는 제품 기능**의 재료(사장님 지시).

실측: 태깅 1건 = 1,580토큰 = **0.679원** (이미지 용량과 무관 — 타일 과금).
미태깅 33만건 = 22만원, $300이면 64만건. ⚠️$300은 **API키 방식엔 못 쓴다**
(공식 문서 명시적 배제) — Vertex로 가야 먹는다.

⚠️ 내 오답 3건 정정: 썸네일은 서버에 영구저장돼 있고(70,519장/6.1GB), 프록시는
결제 완료됐고, 재크롤은 불필요하다. `handoff/히트작크롤.md`를 먼저 읽었으면
안 틀렸다(0순위-A2 위반).
