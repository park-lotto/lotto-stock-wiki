# 썰쇼핑 수집 넓히기 — 2026-09-08 (판정기 수리 + 발굴 재가동)

> 이전 세션(2026-09-04)에서 587편 채점 목록까지 만들고 사장님 답을 기다리다 멈췄었다.
> 이번 세션은 **그 채점 결과를 받아 판정기에 반영**하고, 멈춰 있던 발굴을 되살렸다.

---

## 사장님이 오늘 하신 말 (작업 순서 그대로)

1. "유튜브 썰쇼핑쪽으로 지금 터진영상들이 조회수순으로하면 몇일동안 계속 안바뀐다"
2. "너무 수집이 약한거 아닌가"
3. "유튜브 체널 수집 트랙없나? 썰쇼핑 많이 모아뒀는데 몇일전에"
4. "썰쇼핑계열을 신규로 계속 찾아내야하고 / 지금 수집을 돌려서 갱신해"
5. "이렇게 8000개 가까이있는데 왜이렇게 없어보이지 영상들이"
6. "아래 더보기를해서 클릭하면되게하고 그럼 렉없는거아닌가"
7. "이건 진짜 1분전인가" (카드의 N분 전)

---

## ① 판정기 — 이게 뿌리였다 (커밋 `0f0260859`)

**채널이 문제인 줄 알았는데 아니었다.** 사장님이 O로 찍은 145건의 채널 58개 중
**47개가 이미 시드에 있었다**(1,971개 중). 훑고는 있는데 분류가 안 되고 있었다.

종전 판정기 성적: **O 145건 중 2건(1%)만 통과.** 사실상 기능이 없었다.
원인은 `_HOOK_CONTEXT >= 2`(문맥어 두 개 요구):

    O 145건의 문맥어 적중 개수 = 0개 36건 · 1개 95건 · 2개 12건 · 3개 2건

이 장르 제목은 후킹이 목적이라 짧다("연필의 정체" / "가루의 정체") — 문맥어를 두 개
담을 자리가 없다. 조건 하나가 O의 90%를 죽이고 있었다.

**고친 것**
- `KEYWORDS['제품정체형']` 확장 — 놓친 143건에서 실제로 센 어휘만
  (발명품 36 · 천재계열 56 · 활용법 28 · 정체/비밀 다수, X측 출현 0~1건)
- 문맥어 2개 요구 제거. 오탐 방지는 **차단 3종**이 맡는다
  (`_HOOK_BLOCK` 교양·뉴스 / `_shopping_blocked` / 신설 `_CELEB_BLOCK`)
- `_CELEB_BLOCK` 신설 — X 24건의 공통점이 연예인 가십이었다
  ("홍진경이 김나영에게 선물한 …의 정체"). 제목 구조가 [사람]이 [선물한] [물건]이라
  **물건이 아니라 사람이 주어**다.
  ⚠️이름만으로 막지 마라 — 사장님은 "메시가 마신다는 비밀 음료"를 O로 찍었다.
- **판정 순서 교체(오용형 먼저)** — '활용법'을 제품정체형에 넣자 오용형 실측 12편이
  통째로 넘어갔다(게이트가 잡았다). 두 축은 대본 스파인이 정반대다.

**실측**: 사장님 라벨 O 2→118/145(81%) · X오탐 0→7/24 · 테스트 271 passed

## ② 라이브 재분류 (수집 0회)

`last_run::youtube` 8,884건을 새 판정기로 다시 분류. **썰쇼핑 549 → 619건.**
- 원본 백업: `settings[last_run::youtube__BAK_2026-09-08]` ← 되돌리려면 이걸 쓴다
- 새로 들어온 156건 출신: 홈템 105 · 기타 33 · 장비템 14 · 레시피 3 · 차량템 1
- ★교양·뉴스 0건 전수 확인(예전 '주방'·'만들기' 사고와 같은 지점)

⚠️**보고 정정 이력**: 처음에 "+156건"이라 했는데 실제는 **+70건**이다. 승격 조건
(`best=='기타' or best_score<=_CAP_W`)을 빼고 센 과대추정이었다. 판정 함수를 직접
부를 때와 `categorize()` 전체 경로는 결과가 다르다 — **비교는 반드시 전체 경로로**.

## ③ 발굴이 3주간 멈춰 있었다 (커밋 `1fe49dcfd`)

`scripts/harvest_styles_forever.py`가 **8/19에 손으로 띄운 뒤 한 번도 안 돌았다**
(크론·타이머·로그·상태파일 전부 없음). 다시 띄우니 27분에 45채널 — **그런데 전부
'신기템'이고 썰쇼핑은 0**이었다.

원인은 씨앗이다. 그 파일 주석은 이렇게 적혀 있었다:

    # 새 축은 씨앗이 없으면 영영 못 큰다 … 빈 풀은 스스로 못 벗어난다
    # (썰쇼핑도 같은 이유로 '살림킹왕짱'을 심어 뒀다)

**그런데 `_SEEDS`에는 신기템 2채널뿐이고 썰쇼핑은 한 줄도 없었다.** 주석이 말하는
대비책이 실제로는 빠져 있었다.

씨앗은 **사장님이 O로 찍은 채널**에서 골랐다(score = 그 채널의 O 편수):
공가미 7 · 뽀터언니 6 · 딸기라떼 6 · 왜 팔릴까 5 · 인생갓템 4 · 럭키박스 4 ·
달래샵 4 · 오늘꿀템 4 + 장르 원본 2곳(살림킹왕짱·살림도사)

**결과: 4분 만에 10 → 123채널.** 자가생성 검색어가 '개발자도 놀란 활용법',
'제조사도 미친 활용법', '이케아도 놀란 활용법' 식으로 뻗는다.

## ④ 화면이 깎이는 것 (커밋 `728bf0a16`)

사장님 "8000개 가까이 있는데 왜 이렇게 없어 보이지" → 실측:

    619건  썰쇼핑 필터 후
    315건  채널당 2개 적용   ← 304건이 여기서 접힌다
    200장  렌더 상한         ← 115건은 [더 보기]가 푼다

사장님 지적대로 [더 보기]는 그리기만 나눠 하므로 렉이 안 생긴다. 문제는 그게 푸는 게
**렌더 상한뿐**이고 `채널당 N개`는 안 풀린다는 것. 목록 끝에 두 버튼을 나란히 뒀다.

## ⑤ 카드 'N분 전'이 굳던 것 (커밋 `3ec233cc9`)

    수집 09:15 → 조회 17:57 (8.7시간 경과)
    화면 "2시간 전" = 실제 10.4시간 전 / "1분 전" = 실제 약 9시간 전

`age_hours`가 수집 순간 스냅샷인데 그 뒤 흐른 시간을 아무도 안 더했다. 발행 시각
(`posted_at`)이 정답이지만 **156건 중 0건**이라 그 경로가 한 번도 안 탔다.
→ `COLLECTED_AT_MS` + `ageHoursNow()` 신설, 표시(fmtAgo)와 정렬(recencyKey)이
   **같은 함수**를 쓰게 했다(0순위-B).

## ⑥ 발굴 지킴이 (`deploy/harvest_keepalive.sh`)

서버 크론 `*/10 * * * *`. 이미 돌고 있으면 아무것도 안 하고, 멈춰 있으면 다시 띄운다.
⚠️크론은 리포가 관리하지 않는다 — 서버를 새로 만들면 **손으로 다시 걸어야 한다**.

---

## ⏭ 다음에 할 일

- [ ] **사장님 미채점 418건.** `out/썰쇼핑_판정.html`에 그대로 있고 이미 찍은 169건은
      저장돼 있다. 더 찍어주시면 지금 놓치는 21건 같은 것을 더 잡는다.
- [ ] **발굴 채널을 시드에 자동 등록**하는 경로가 없다. 지금은 손으로 넣었다
      (130개 중 52개 신규 → 시드 2,026개). `scripts/register_discovered_styles.py`가
      있으니 그걸 크론에 붙이는 게 맞다.
- [ ] **`posted_at`을 수집 때 채운다.** ⑤는 화면 보정이지 근본 해결이 아니다.
      유튜브 Data API는 발행 시각을 주므로 수집기에서 담으면 된다.
- [ ] **오탐 7건**: "배관 수리비 아껴주는 꿀템 정체" 같은 경계 사례가 들어온다.
      '활용법'을 권위자 동반 조건으로 좁혀봤지만 **사장님이 O로 찍은 것도 함께 빠져**
      (118→117, 오탐 7→9) 넓은 쪽을 택했다. 라벨이 더 쌓이면 다시 판단할 것.
- [ ] 기간 탭의 "48시간"이 **발행이 아니라 수집 시각 기준**이다. 조회수순 상위 20개 중
      19개가 발행 48h 초과(최고 13.5일). 사장님은 "지금은 안내만" 하라고 하셨다.

## ⚠️ 함정 (먼저 읽어라)

- **어휘 하나로 붙이지 마라.** '천재·정체·비밀'은 교양·뉴스·연예인 글도 쓴다.
  넓힐 때는 **차단을 짝으로** 붙여라(이번에 `_CELEB_BLOCK`을 그렇게 넣었다).
- **정확도를 자동카테고리로 재지 마라 — 사장님 눈이 정답이다.**
- **판정 비교는 `categorize()` 전체 경로로.** 내부 함수만 부르면 승격 조건이 빠져
  숫자가 부풀어 오른다(이번에 156 vs 70으로 두 배 넘게 틀렸다).
- 발굴 상태는 `/tmp/style_state.json`이라 **재부팅에 날아간다.** 지킴이가 프로세스는
  되살리지만 누적 채널 목록은 못 되살린다 — 시드에 등록해 두는 게 진짜 보관이다.
