# 배포재시작 — 병합이 고객 작업을 끊지 않게

## 2026-09-04 (사장님 "둘 다 하자")

### 실사고
13:45~14:15 트랙 5개 연속 병합 → 워커 30분에 5회 재시작, 14:15 웹 강제 재시작. "다들 계속 안 된다" 제보.
- 제작 작업(mix·render·clean·preview)은 `_worker_busy` 무제한 연기로 **안 죽었다**(clean 7건 완료/0 실패).
- 끊긴 건 담기(prewarm) 3건 + 길이보정 2건. 큐 문구는 "자동 재시도 대상"인데 **담기는 재시도 코드가 없었다**(재큐 0건 실측).

### 고침
1. `store.reap_stale` → `_requeue_interrupted_prewarm`: 죽은 prewarm을 같은 행 그대로 `queued`로 되돌린다.
   `args_json._restart_retry` 상한 `PREWARM_RESTART_RETRIES=3`, 닿으면 종전대로 failed("고장 아님").
   테스트 3건 신설(test_reap_stale_reason 8건 통과).
2. `deploy/auto_deploy.sh` 배포 시간창: `shopping_shorts/`가 바뀐 커밋은 **KST 02~06시에만 가져온다**(밖에선 fetch만, reset 안 함 — HTML/파이썬 어긋남 방지). stockbrain만 바뀐 커밋은 즉시.
   긴급: 커밋 제목 `[긴급]`/`[deploy-now]` 또는 서버 `touch /home/ubuntu/DEPLOY_NOW`(배포 후 자동 삭제).
   값 조절: `/home/ubuntu/deploy_window.conf`에 `DEPLOY_WINDOW_START=..` 등 적으면 덮어씀(재배포 불필요).
   로그: `배포 대기(시간창 … 밖)` 한 줄(붙잡은 커밋 바뀔 때만).

### ⚠️ 알아둘 것
- 이 커밋 자체는 **옛 스크립트**로 배포되므로 한 번은 즉시 재시작된다. 그 다음부터 시간창 적용.
- 시간창 밖에 병합된 커밋은 새벽 2시에 한꺼번에 들어간다 → 새벽에 재시작 1회.
- 사장님이 낮에 급히 내보낼 땐 `[긴급]` 커밋이 가장 쉽다(track finish 병합 커밋 제목엔 안 붙으니 트랙 안 커밋 제목에 붙일 것 — `git log LOCAL..REMOTE` 전체를 본다).

### ⏭ 다음
- 새벽 2시 첫 시간창 배포가 실제로 나가는지 `/tmp/auto_deploy.log` 확인(`배포 대기` → `새커밋 … 배포시작`).
- 담기 재큐 실동작: 다음 배포 재시작 뒤 `job_queue`에서 `error like '자동으로 다시 시작%'` 행이 done 되는지.
- 웹 강제 재시작 시 웹 BackgroundTask(청소 등)가 죽는 문제는 별건(memory: mix_stage_staleness).
