# log — 단계바도크 트랙

## 2026-08-26 (집PC) — 제작소 9단계 단계바를 도크형으로 교체

- 사장님 "단계바가 레퍼런스 채널과 너무 똑같다" → 가로 유지, 동그라미+선(위저드 관용구)만 버림.
  목업 5벌(세로/묶음/게이지/체크리스트 → 가로 6종 → 아이콘 3종 → 컬러 3종 → 강화 3단)을
  거쳐 **K3 색흐름 · 1단(절제)** 확정. 이모지 대신 직접 그린 SVG(OS마다 다르게 보이는 문제).
- 이름 변경: `SEO해시테크`→**`제목·태그`**(하는 일과 어긋난 이름) / `최종렌더`→**`완성본`**
  (서비스 다른 곳에서 이미 쓰는 말로 통일). 패널 제목은 syncPanelTitles가 자동 반영.
- 옛 `.orbbar` CSS는 **안 지웠다** — renderSteps() 한 곳만 되돌리면 원상복구.
- ⚠️ `final-mock-v6.html`이 "정본"으로 적혀 있었지만 **7단계 시절에 멈춰 이미 썩어 있었다**.
  구조는 안 건드리고 단계바 정본이 옮겨간 곳만 명시.
- ⚠️ 8단계 해시(#) 아이콘이 뭉쳐 `≠`처럼 보였다 — **확대해 보고서야 발견**. 태그 모양으로 교체.
  아이콘은 SVG 코드 말고 실제 크기 렌더를 눈으로 봐야 한다.
- 실측: 로컬서버 브라우저 렌더 확인(도크 9칸·라벨 정확·진행띠 50%·폰 416px 가로스크롤),
  pytest test_skm_ui.py 43 passed. 병합 `3c93618c8`.
