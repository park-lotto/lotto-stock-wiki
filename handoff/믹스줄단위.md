# 믹스줄단위 트랙 핸드오프

_최종 갱신: 2026-09-03_

## ✅ 2026-09-03 — 2단계 칸(줄)이 3단계 칸이 되게 (사장님 제보 "훅이랑 문제가 믹스에서 합쳐진다")

**실측(서버 job 8b86200f50b3 / work fd124f0fb363)**: B안은 첫말/문제/반전/증거/시연/결과/가격/약속
8줄인데 `given_script`는 **공백으로 이어진 통짜**(개행 0). EDL(legacy scripted)이 9칸으로 다시 잘라
훅 한 칸 = "여러분 다이소 가면… + 필기하다 틀리면…"(5.5초), "…사라져요. / 이게 끝이에요."는 두 칸.
저장 출구 `enforce_script_order`는 마침표 분리 + 칸 길이 비례 배분이라 이걸 "제자리"로 통과시켰다.

**뿌리**: 칸 구조가 3단계로 안 넘어간다(`script_gate.py:442` full = " ".join). 증상(길이)이 아니라 **단위**를 고쳤다.

| 자리 | 바뀐 것 |
|---|---|
| `produce.html` `s2Confirm`·`s2ConfirmToNewWork` | `s2ScriptLines(dr)` — 칸 텍스트를 `\n`으로 넘긴다(칸 없는 내 대본은 원문 그대로) |
| `edit_plan.script_sentences` | 개행 2줄+면 **줄이 단위**(마침표 재분리 안 함). 통짜는 종전대로 |
| `edit_plan._SCRIPTED_PROMPT` | 줄 모드면 "줄 하나 = 비트 하나, 총 N개" 지시(`{line_rule}`) |
| `edit_plan.enforce_script_order` | 줄 모드에서 칸 수≠줄 수면 `_rebuild_beats_by_lines`로 줄마다 칸 하나 재조립(화면은 그 줄에서 온 칸 것을 모음). 글자가 다 맞아도 칸 수가 다르면 제자리 아님 |

테스트: `tests/test_script_lines_are_beats.py` 3건(실사고 모양 9칸→8줄 1:1). 대본·나레이션 묶음 275 passed.

## ⏭ 남은 것
- **서버 실영상 확인**: 배포 뒤 B안 확정→장면편집에서 1.hook이 첫말 한 줄(2.3초)만 갖는지 눈으로.
- 옛 작업(이미 확정된 통짜 대본)은 그대로 마침표 분리 — 재확정해야 줄 모드가 탄다.
- 사람이 3단계에서 칸을 고친 작업(`narration_manual`)은 재조립 대상에서 뺐다(칸 수 유지).
