# 에러백과 — handoff

## 2026-09-05 — 고객이 에러를 만나는 화면에 "뭐가 잘못됐는지" 설명을 띄운다

### 사장님 지시
"현재 고객들이 에러났던 구간들 내가 얘기했던거 다 수집해서 그 페이지에 디테일하게
설명문구를 넣어줘 뭐가 잘몬된건지" → (재확인) **"고객이 에러나는 페이지에 설명표시"**
= 관리자 백과가 아니라 **고객이 실제로 에러를 만나는 그 화면**에 설명이 뜨게.

### 만든 것
**`shopping_shorts/static/error_help.js` (신규, 24항목)** — 설명 문구 **정본 한 곳**.
같은 설명을 화면 4곳에 각각 적으면 반드시 어긋난다(0순위-B) → 화면은 부르기만 한다.

항목마다 3칸: `title`(무엇이 막혔나) / `why`(왜) / `todo`(지금 할 일).
**문구를 쓴 축**: 기다리면 풀리는 것과 기다려도 안 풀리는 것을 반드시 가른다
(08-22 사고 — "키 소진"이 오답이라 멀쩡한 키를 하염없이 기다렸다).

항목은 지어내지 않았다. 각 항목 `src:`에 근거가 된 제보 기록을 적어 뒀다:
- `preview_frozen` ← log.d/미리보기정지 09-02 고객 제보(왕혜원)
- `preview_black` ← log.d/미리보기검정 09-01
- `caption_remove_fail` ← log.d/자막제거문구 08-26
- `capcut_bat` ← log.d/내보내기캡컷 09-03
- `gen_api_error` ← log.d 08-22 (429 없는데 키소진으로 뜬 사고)
- `server_500` ← log.d/대본UI2단계 08-17 ("네트워크 오류"가 실은 서버 500)
- `gen_style_mismatch` ← log.d/대본UI2단계 08-17 (2개 골랐는데 1안만)
- `yt_blocked` ← memory reference_youtube_shorts_datacenter_block / log.d/유튜브추출장애 08-30
- `src_ai_busy` ← log.d 08-27 (영상 탓처럼 읽히던 문구)
- `tts_silent` ← handoff/API관측판 silent_fallback

**배선**: produce.html(제작소) 뭉갠 문구 36곳 + 렌더 실패 + 편집 저장 실패,
index.html·discover.html alert 4곳. 4개 화면(produce/index/discover/scene_lab)
head에 `.errHelp` 스타일 + 스크립트.

**`tools/error_help_preview.py`** — 정본을 node로 실행해 항목표를 받아 검수용 HTML 생성
(손으로 옮겨 적지 않는다 → 문구를 고치면 미리보기가 따라온다).
`py tools/error_help_preview.py` → `out/에러문구_미리보기.html` + 바탕화면 복사.

### 사장님 검수 반영
- **포인트 항목 전부 제거**(2026-09-05) — "지금 포인트는 전혀 사용안하고있는 제도야".
  `point_short` 삭제 + pick()의 포인트·크레딧 판정 삭제 + render_fail의 환불 문구 교체.
- 고객 화면에 나가면 안 될 표기 3곳 제거(마크다운 `**별표**` 노출, 내부표기 ★).

### 실측한 것 / 못 한 것
- ✅ `node --check`로 4개 화면 인라인 스크립트 **전 블록 통과** — 수정 중 실제로 깨뜨린
  2곳을 이 검사가 잡았다(heredoc이 `\n`을 진짜 개행으로 넣음 — CLAUDE.md에 적힌 그 함정).
- ✅ TestClient: `/error_help.js` 200, `/produce`·`/discover`·`/scene_lab`·`/` 에 주입 확인.
- ✅ 미리보기 HTML은 **브라우저에서 렌더 확인**(25항목 시점). 눈으로 보고 결함 3건 수리.
- ✅ main 병합 확인: `c46883bb5`, origin/main에 정본 파일 존재·produce에 ErrorHelp 36곳.
- ❌ **라이브 화면에서 실제 에러를 내서 문구가 뜨는지는 아직 안 봤다**(0순위-A1).
  자동배포 시간창이 새벽 2~6시라 그때 서버에 반영된다 → **배포 후 실측이 남았다**.

### ⏭ 다음
1. **새벽 배포 후 라이브 실측** — 제작소에서 통신을 끊고(개발자도구 오프라인) 버튼을 눌러
   `.errHelp` 상자가 실제로 그려지는지 눈으로 확인. `/error_help.js` 200도 라이브에서 확인.
2. `library.html`(도서관) 미배선 — 고객이 쓰는 화면인데 빠져 있다.
3. **서버 쪽 뭉갠 문구를 사유 코드로**: app.py 14863·14876 "생성 실패(키 소진 또는 응답 오류)"
   — 화면 사전이 알아채서 번역은 하지만, 서버가 사유를 주는 게 정석
   (`_gen_fail_message`가 이미 그 본보기다. 그 함수를 정본으로 승격 후보).
   handoff/API관측판 ⏭3번과 같은 일이다.
4. 새 제보가 오면 **error_help.js에 한 줄 추가**하고 `src:`에 근거 기록을 적는다.

### 함정
- 이 사전은 화면(JS)에만 있다 — 서버가 내려주는 문구 자체를 바꾸는 것과 다른 층이다.
  둘 다 손대면 또 두 벌이 된다. **서버는 사유 코드, 화면은 문구** 로 역할을 갈라라.
- `pick()`이 옛 문구를 정규식으로 알아본다. 서버 문구를 바꾸면 그 규칙도 같이 봐야 한다.
