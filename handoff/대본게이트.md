# 대본게이트 — 장면 0개면 즉시 중단 (2026-09-16)

## 왜 (사장님 제보)
- "2안 요청했는데 1안만 나왔다" + "대본이 엄청 늦게 나온다" (work `fb4d991d14e8`)
- "씨앗으로 대본 결 지키고, 그 아이템으로 해외영상 있는 장면 쓰면서 대본 쓰는 거 아니냐"

## 실측한 것
- 서버 로그 6시간 **19건** `[script_gate] 사실 근거 반려` — 사장님 건만이 아니라 상시 발생.
  반려 1건당 약 20초(`08:38:08→08:38:30`, `09:39:34→09:39:55`).
- 살아남은 안도 `tries` 3회(1차 `장면 근거` 실패 → 재작성 → 재단).
- 볼펜 건 화면 안내: `조립 못함: 영상에서 재료를 못 뽑았습니다` = **장면 추출 0개**.
  장면이 0이면 `grounded`가 꺼지고 → 근거 없이 쓴 대본이 사실 근거에 또 걸려
  **3회를 다 태우고 버려졌다.** 될 리 없는 생성을 도는 것이 느림의 큰 몫이었다.

## 고친 것 (이번 커밋)
- `script_generate.py` — 장면 0개면 강등 대신 **모델을 한 번도 안 부르고 즉시 중단**.
  `note["reason"]="장면없음"` + 원인 문구를 화면에 그대로 전달.
- `tests/test_script_grounded.py` — 옛 계약(강등)을 새 계약(중단·낭비 호출 0)으로 교체.
- 게이트: 690 passed / 0 failed (기준선도 0 failed였다).

## ❌ 시도했다가 되돌린 것 — 다음 사람이 같은 함정에 빠지지 말 것
`FATAL_CHECKS`에서 **`사실 근거`를 빼려다 테스트 20개가 깨졌다.**
"근거 없는 주장은 절대 못 나간다"는 09-09·09-11 사고로 세운 핵심 계약이고,
`test_script_claim_grounding.py` 등이 그걸 촘촘히 지키고 있다. **함부로 내리지 마라.**
안이 줄어드는 건 그 계약의 *의도된* 결과다 — 고칠 곳은 게이트가 아니라 **근거 쪽**이다.

## ⏭ 남은 것 (진짜 뿌리)
1. **장면 추출이 실패하는 원인** — 근거가 없으니 반려가 예정돼 있다. 여기가 본체.
2. **`needs_scene`은 모델이 스스로 정한다** — "친구네 집 갔다가"처럼 장면에 없는 스토리 줄을
   `needs_scene=false`로 적으면 `scene_grounding_check`를 그냥 빠져나간다(`min_ratio=0.34`).
   사장님이 말한 "이상한 내용"의 진짜 통로. **아직 안 막음.**
   (실측 job `26698eb0a362`: 10비트 중 5개가 대사↔화면 무관, 1개는 컷 중복)
3. `script_grounded_enabled` = **`admin`** — 일반 고객은 '본 것만 쓰기'가 꺼져 있다. 켤지 판단 필요.
4. 라이브 실측(0순위-A1) 미완 — 배포 후 실제 job으로 확인해야 한다.
