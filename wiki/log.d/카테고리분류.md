# 카테고리분류 — 작업 로그

- 2026-07-16 — [실측·구현·배포] **카테고리 분류를 키워드→Gemini로 이관**(`ac99a520` → main `53facafa`, 게이트 통과·라이브). 출발=대본믹스통합 I-3("`or None`이 죽은 코드")인데 파보니 **죽은 코드는 증상, 병은 따로**였음. ★라이브 실측(script_wiki 13건 정답): 키워드 **7/13=54%** / 채널명만 8% / **대본만 54%**(=내 '채널명이 이긴다' 가설 **반증**, 키워드 방식 자체의 천장) / **Gemini 10/13=77%**. ★왜 심각: `element_raw_values`(store.py:788-792)가 script_wiki UNION ALL script_extracts로 **둘 다** 읽어서, 코퍼스 구성이 레시피 확정8/추측10(56%)·생활용품 4/15(**79%**)·인테리어 1/4(**80%**)·가전 0/1(**100%**) = **학습의 다수가 54%짜리 추측**이었음. ★해법: `analyze_structure`가 **이미 같은 full_text를 Gemini에 보내므로** 거기 `product_category`를 얹어 **추가비용 ~0**. 통제어휘는 `categorize.KEYWORDS`에서 import(어휘 분기 방지)+**enum으로 못박아** 고아버킷 원천차단. `category_source`(user|gemini|keyword) 신설, **우선순위 user > gemini > keyword** — 사용자 교정은 절대 안 덮음. ★검증: 테스트 4건·뮤테이션 2종 전부 죽음·전체 683 passed/10 failed=기준선과 파일·건수 동일(신규 0)·**실 파이프라인 관측**(서버서 새 모듈로 진짜 Gemini 13회 호출: enum 정상·구조분석 안 깨짐·77% 재현). ★배포 안전근거: 기존 행 source=NULL이지만 extract 카테고리는 키워드추측/None뿐 사용자선택이 아님(save_to_wiki가 extract를 안 건드림 — 위키↔extract 어긋난 8건이 전부 extract=None)이라 승격해도 교정을 안 덮음. ⚠️표본 13건은 작음. **⏭ 다음**: 기존 keyword 행 재분류 배치 / **생활용품↔인테리어 경계**(Gemini 오답 3건 중 2건이 여기 — 어휘가 겹쳐 사람도 갈림, 사장님 판단 필요) / save_to_wiki가 사용자 카테고리를 extract에 전파. 🔑함정: Gemini 키는 systemd 주입이라 프로브 시 `set -a && . /etc/shopping-shorts.env`를 안 하면 조용히 NOKEY(정확도 0%로 보여 오판) / 문자열치환 뮤테이션이 들여쓰기를 깨면 IndentationError=아무것도 증명 못 함(조건식만 바꿀 것).

- 2026-07-16 — [통합·배포막힘] **인테리어+생활용품 → 홈템 통합**(사장님 결정). `8be0e46b` → main `c1c5f3e6`, 게이트 통과. 근거=Gemini 오답 3건 중 **2건이 그 경계**(인테리어 소품=생활용품이라 사람도 갈림) → 합치면 같은 13건 **77%→92%**(관측 데이터 재계산). `categorize` KEYWORDS·NAME_KEYWORDS 병합(양쪽 키워드 흡수)·`TOPIC_CTYPE` 홈템=혼합형(병합 전 인테리어=비법형·생활용품=제품형으로 **정반대**였으나 **ctype은 프로덕션 소비처 0**=테스트만 참조라 라이브 안 깨짐)·`ai_categorize` 중복어휘도 통합(안 하면 두 분류기 갈라짐)·`store` DB 이관(옛 라벨→홈템, 옛 통계버킷 삭제 — 코드만 바꾸면 옛 라벨이 통제어휘 밖으로 떨어져 학습에서 조용히 빠짐). **비가역이라 서버 백업 선행**(`/tmp/hometem_backup/pre_merge.json`, 72건). 테스트 5건 신규+낡은 기대치 7건 갱신, 688 passed/10 failed=기준선 동일. 🚨**서버 배포 막힘(내 코드 탓 아님)**: SessionEnd 훅의 `a133fcba auto: session changes`가 raw/ 57개를 main에 커밋 → 서버는 크롤봇이 raw/를 계속 쓰는 중이라 `pull --ff-only` 구조적 거부(`pull실패 스킵-수동확인필요`, **두 서비스 동시 정지**). 블로커 60=추적 raw/yt 14+untracked 크롤산출물 46. 사장님 승인 받아 **줄바꿈만 다른 20개 되돌림**(내용 동일 검증, 손실 0), 내용 다른 `.xls` 2개는 보존. **3회 시도 3회 실패**(되돌려도 크롤봇이 즉시 재작성=경합) → 일회성 정리로 안 풀림, **트랙격리 페이즈3 배포계층 소관**으로 넘김. 라이브 실증: I-2 ✅ / Gemini 분류 ✅ / 홈템 ❌대기(main엔 안전히 병합돼 유실 없음). 🔑함정: `git status --porcelain`은 untracked까지 세서 raw/yt가 105건으로 보임(실제 추적변경 22건) → 오진 주의, `git diff`로 볼 것 / 트랙폴더에서 `git add -A`는 raw/ 크롤데이터를 담음("내 폴더라 -A 안전"은 **남의 코드**가 없다는 뜻).
