# 소스분석 진행표시

## 2026-08-20 — 담자마자 분석이 돌고, 도는 게 보이게
- "새로고침해야 분석이 뜬다"의 원인은 화면 지연이 아니라 **분석 미시작**. `_autoloadTried`가
  페이지 수명 boolean이라 담기 후 재호출 때 자동분석을 통째로 건너뛰었다(→ 진행표시도 없음).
- 래치를 **담긴 묶음 지문**으로 바꿔 새 영상일 때만 풀리게(크레딧 소진 방지선은 유지).
  진행표시는 네트워크 왕복 전에 올리고, 스킵·예외 경로에서도 반드시 내린다.
- 가드 6건 신설 — 옛 코드로 되돌리면 5건 FAIL. 커밋 `55b61749b`.
- 배포 완료(8f082efe9, 01:47). 사장님 화면 9단계 확인 — 계속 8단계였던 건 **브라우저 캐시**
  (서버 `no-cache` 헤더는 정상 실측). Ctrl+Shift+R로 해소. ⏭담는 순간의 진행표시는 미확인.

## 2026-08-20 (2차, 회사PC)
- ★진범은 배선 누락이 아니라 **저장 레이스**였다(실측): 유입 저장(_pushWork)이 디바운스를
  우회해 refreshStep0이 못 기다림 → aipick이 work_id 없이 나감 → 서버가 계정 전체 픽으로
  폴백해 pick_id가 차서 자동분석 분기를 통째로 건너뜀(autoload 0회, 하네스로 재현).
- 수정 3곳: ①유입 저장 약속(window._ARRIVAL_PUSH)을 남기고 refreshStep0이 aipick 전에 await
  ②pick이 있어도 미분석 담긴 영상이 남으면 분기 개방 ③pending 판정을 _srcPendingCount 한 곳으로.
- 가드 5건 추가(총 11건, 실행가드는 실코드 node 실행) — 옛 코드로 되돌리면 5건 FAIL 실측.

## 2026-08-20 미리보기 재발버그 2건 근본수리
- 버그① "후킹 자막이 여러분에서 멈춤" = 미리보기 시계 이원화(화면은 seqTimer, 자막·시간·전체재생은 audio) —
  음성 없는 칸(404/409)에서 audio가 0초 고정이라 자막·시간이 얼고 전체재생이 정지. audioUsable() 한 곳으로
  판단을 모아 화면시계 폴백. 화면 속 "솔직히 기대 없이"는 원본 소각자막(오독 방지 안내 추가).
- 버그② "미리보기 크기 널뜀" = 크기 정의 9곳(레일 340px+클램프 293×520 vs 장면편집 408×725, 실측).
  scene_play.js PV_W/PV_AR 한 곳 → CSS 변수로 통일, 비율 전부 9:16 실측 확인.
- 가드 9건: 옛 코드로 되돌려 9건 전부 FAIL 실측 후 복구. 관련 회귀 482 passed 실패 0.
