# 추출실패오표기 (2026-08-27)

## 무엇을 · 왜
1단계 화면에 **"쓸 만한 재료가 안 나왔어요(화면·말 모두 비어 있음)"** 가 떴는데 영상은 멀쩡했다.

서버 로그 실측(2026-08-27 09:57~10:00, `journalctl -u shopping-shorts`):
```
script_extract: 빈 결과 반환(재시도 소진 또는 미분류 오류) — ServerError("504 DEADLINE_EXCEEDED ...")  ×5
script_extract: 빈 결과 반환(재시도 소진 또는 미분류 오류) — ClientError("499 CANCELLED ...")          ×1
```
`script_extract.py`의 재시도 분기가 **503/UNAVAILABLE/overloaded와 쿼터만** 다뤄서
504·499는 재시도 예산 4번을 하나도 안 쓰고 즉시 `dict(_EMPTY)`를 돌려줬다.
그 빈 결과를 `app.py`가 `has_usable_result`=False로 받아 **영상 탓 문구**로 적었다.
(메모리 `reference_나머지_통_이름이_회귀를_숨긴다`와 같은 모양 — 나머지 통이 원인을 덮었다)

## 고친 것 (커밋 3647a61f0 → main 병합 완료)
- `script_extract.py`: 일시장애 어휘를 `_TRANSIENT_MARKS` **한 곳**으로(0순위-B).
  504/DEADLINE/499/CANCELLED/500/INTERNAL도 503과 같이 폴백모델+재시도 대상.
  빈 결과에 `empty_reason`(api|unknown)·`empty_detail`을 실어 보낸다. 조회는 `empty_reason(result)`.
- `app.py` 자동적재: reason=api면 `status="deferred_api"` + `rollback_latch`(시도횟수 반환)
  + 문구 "AI 분석 서버가 제때 응답하지 못했어요 — 영상 문제가 아니니 잠시 후 다시 시도해 주세요".
  `_autoload_reason_ko`는 이 문구를 다시 감싸지 않고 그대로 통과시킨다.
- `prewarm.py`: 같은 구멍(예외가 아니라 **빈 결과로** 오는 경로)에 동일 처방 → `deferred_transient`.
- `static/produce.html`: "AI 서버가 잠깐 밀린 거예요 — 잠시 후 새로고침하면 자동으로 다시 분석해요."

## 검증
- 판정 함수 실측: 504·499·503 → True / 400 INVALID_ARGUMENT → False. `empty_reason` 왕복 확인.
- `track.py finish` 게이트 통과(실패 0건 증가) → main push 완료.
- ⏭ **라이브 실물 확인 안 했다** — 다음 504가 났을 때 화면이 새 문구로 뜨는지 봐야 한다.
  확인법: `journalctl -u shopping-shorts | grep -a "빈 결과 반환"` 뒤 그 shortcode의
  `produce_autoload.last_error`가 "AI 분석 서버…"인지 대조.

## ⏭ 다음
- 라이브에서 새 문구 실물 확인(위)
- 504가 잦으면 업로드 파일 크기·모델 타임아웃도 봐야 한다(재시도만으로는 근본책 아님)
