# 샤오홍슈 레퍼런스 채널 랭킹 — 핸드오프

- 갱신: 2026-07-29 (집PC → 회사 이어감) / 트랙: 샤오홍슈레퍼런스 (`track/샤오홍슈레퍼런스`)
- 목표: "레퍼런스 랭킹" 화면의 샤오홍슈 탭을(지금은 번역 딥링크 카드뿐) 인스타·틱톡처럼
  "계정 등록 → 자동 크롤 → 강도별 랭킹" 진짜 탭으로 만든다.
- 설계: `docs/superpowers/specs/2026-07-29-샤오홍슈-레퍼런스채널-design.md`
- 계획(7태스크): `docs/superpowers/plans/2026-07-29-샤오홍슈-레퍼런스채널.md`
- subagent-driven-development로 실행 중 — Task별 스펙리뷰+코드품질리뷰 둘 다 통과해야 다음 태스크로 감.

## ✅ 완료 (Task 1~4 + 버그수정, 전부 리뷰 통과·커밋됨)

| # | 내용 | 커밋 |
|---|---|---|
| 1 | 프로필 페이지 크롤 검증 스파이크(서버 실측) — 로그인월 없음, xsec_token 불필요, `__INITIAL_STATE__` JSON 파싱이 정답임을 확인 | (파일 없음, 서버 조사만) |
| 2 | `xiaohongshu_playwright.py` 크롤러 — JSON 파싱 방식(계획서 원안의 DOM정규식 아님, 스파이크로 더 나은 방법 발견해 교체) | `f508ead36` |
| 2-fix | 코드리뷰 Important 2건: 좋아요 축약표기("1.2만") 파싱, undefined 치환 문자열경계 | `51bbd4a2a` |
| 3 | `service.py`에 `collect(platform="xiaohongshu")` 배선 — `build_overseas_items` 48h로 재사용(새 랭킹함수 안 만듦) | `583c93f83` |
| 4 | `refs.html`에 샤오홍슈 채널등록 탭 추가 — 실제 브라우저(Playwright MCP)로 E2E 확인함 | `b04a5b829` |
| 버그 | 등록 URL이 `xiaohongshu.com`이면 지역차단 도메인이라 크롤 실패 — `rednote.com`으로 자동 정규화하는 `_normalize_profile_url()` 추가(리뷰 중 발견) | `9d4c9f415` |

**핵심 기술 결정**: 새 랭킹 로직 없음(`build_overseas_items` 재사용), 새 저장소 스키마 없음
(`platform_seeds`/`platform_snapshots`/`last_run::{platform}` 전부 이미 플랫폼 무관 범용),
`app.py`/`store.py`/`ranking.py` **전혀 안 건드림** — `xiaohongshu_playwright.py`(신규)와
`service.py`/`refs.html`(소폭 수정)만으로 끝남.

**노트ID published_at 트릭**: 샤오홍슈 프로필 그리드 응답엔 날짜 필드가 아예 없음(실측 확인).
대신 24자리 hex 노트ID가 MongoDB ObjectId 형식이라 **앞 8자리 hex = Unix 타임스탬프**임을
30개 노트 계정으로 교차검증 완료(날짜 순서 일치) — `_published_at_from_note_id()`가 이걸로 계산.

## ⏭ 남은 것 (회사에서 이어서)

- [x] **Task 5**: `index.html` 완료·배포됨 (2026-07-29 회사PC). `isTrend`에서 xiaohongshu 제거
      (도우인만 트렌드), `DESC_BY_PLATFORM`/`TAB_TIP`에 xiaohongshu 추가, `ttModeChange()` 프로필URL
      placeholder 분기. 커밋 `3b109f14f`. ⚠️ 병합 시 `config.py` 충돌 발생 — 해외HOT의 `XHS_*`/
      `OVERSEAS_*`와 내 `XIAOHONGSHU_*`가 같은 위치 추가라 **둘 다 살려 union 해소**(서로 다른 기능,
      다른 var명). 병합커밋 `28da8f237`.
- [x] **Task 7**: `finish` 게이트 통과 → main 병합(`ef668747c`)·배포됨. 기준선 실패 13건 그대로(새 실패 0).
- [ ] **Task 6 (남음, 서버 수동)**: 배포 반영(~3분) 후 admin으로 `/refs`에서 실제 샤오홍슈 계정 1~2개
      등록 → 레퍼런스 랭킹 📕샤오홍슈 탭 "지금 수집" → `count>0`·랭킹카드 정상(음수·NaN 없음) 확인.
      count==0이면 서버에서 프로필 셀렉터(`_NOTE_ID_RE`/`_parse_note_card`) 재조정. ⚠️아래 세션파일 경합 주의.

## 🆕 계정 발굴 리더보드 (2026-07-29, 별도 기능·배포됨)

설계: `docs/superpowers/specs/2026-07-29-샤오홍슈-계정발굴-design.md`
"내가 계정을 몰라도 카테고리별 잘하는 계정을 모아준다" — 검색발굴(포스트)을 작성자별 집계.

- **완료·배포**: `xiaohongshu_discovery`(집계 순수함수) + `service.discover/adopt/blacklist/auto_register`
  + `/api/xhs/discover·adopt·blacklist` + 📕샤오홍슈 `🔍 발굴` 패널(admin) + `daily_batch` 자동등록.
- 점수=참여도합(좋아요+댓글+수집+공유), 최소노트2, 삭제=영구블랙리스트(`platform_seeds` kind=xhs_blacklist).
- 킬스위치 `XIAOHONGSHU_AUTO_DISCOVER`(기본 true, Apify 과금), `XIAOHONGSHU_AUTO_TOP_N`(기본10).
- 전제확인: Apify 응답 `author.userid` 존재(테스트픽스처 실측) → 프로필URL 조립 OK.
- 테스트 통과. 해외HOT 잡은 안 건드림(`search_full` 직접호출로 격리).
- **★무료 전환(2026-07-29 2차)**: 발굴 검색을 `config.XHS_SCRAPER`에 맞춤 — 서버 기본 `playwright`라
  **무료 크롤로 발굴**(Apify 과금 안 함). `playwright_crawl`에 `channel_id`(user.user_id) 추출 추가.
  apify는 `XHS_SCRAPER=apify`일 때만. `_xhs_search_fn()`이 경로 선택.
- **남은 검증(서버)**: 배포 후 admin으로 📕샤오홍슈 → 🔍발굴 눌러 실제 계정 뜨는지 + 담기/삭제 육안.
  ⚠️ 발굴은 Apify 검색이라 실호출 시 과금 — 첫 확인은 1회만.

## ⚠️ 다음 세션이 반드시 알아야 할 것

1. **세션 파일 경합 주의(중요, 다른 세션이 실측함)**: `/home/ubuntu/rednote_session.json`을
   **해외HOT 트랙의 `playwright_crawl.py`(검색 크롤)와 이 트랙의 `xiaohongshu_playwright.py`
   (프로필 크롤)가 같이 쓴다.** 두 기능이 동시에 크롤 돌리면 서로 경합해서 **캡차·에러 없이
   조용히 0건**이 남는 사례가 관측됨(해외HOT 쪽 handoff에 기록됨). Task 6 E2E 할 때 해외HOT
   수집이 동시에 도는 중이면 아니라는 것부터 확인하고 재시도할 것.
2. **해외HOT은 이미 라이브 배포됨(다른 세션, 2026-07-29 밤)**: `overseas_hot_jobs.py`가
   `XHS_SCRAPER=playwright`로 서버에 이미 켜져 있다. 이 트랙(레퍼런스채널)과는 **다른 기능·다른
   코드 경로**지만 세션 파일만 공유 — 서로 건드리지 않음, 그냥 알아둘 것.
3. **서버 세션 만료 시**: 수동 QR 재로그인 필요(오늘 한 절차 반복) — `docs/superpowers/specs/
   2026-07-29-샤오홍슈-레퍼런스채널-design.md`에 재현 코드 있음.
4. 전체 pytest 스위트는 오래 걸림(2500+ 건, ~8분) — 기준선 실패 14건(`test_apify_client.py`·
   `test_collect_tiktok_search.py`·`test_edit_plan_build.py`·`test_mix_pipeline.py`, 전부 이번
   작업과 무관) 이미 확인됨. `finish` 게이트는 "실패 건수 안 늘었나"만 보므로 이 14건은 무시해도 됨.

## 결과물 자동 열기 대상 아님
이 트랙은 `out/` 결과물이 아니라 코드 기능 추가라 자동 열기 규칙 해당 없음.
