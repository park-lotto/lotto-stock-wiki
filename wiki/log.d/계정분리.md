# 계정분리 트랙 로그

## 2026-08-10 (회사PC 야간)
- 레퍼런스 수집 나흘 정지(8/6~8/9 전 회차 0건) 복구 → **417건**·214채널 라이브 반영.
  원인 2겹: ①`_scrape_one_playwright`가 로테이션 계정을 config로 덮어써 **계정↔IP 불일치**
  → 인스타 `update_risky_contactpoint` 챌린지 ②`INSTAGRAM_COLLECT_ROTATION` 미설정으로
  막힌 단일계정만 사용. 실측으로 갈림(단일 0건 vs 로테이션 12건).
- 관문 URL(`update_risky_contactpoint`·challenge·suspended)을 `login_wall`로 분류 수정.
  전엔 `not_found` 폴백이라 "채널 199개가 없어졌다"로 오독됐다. 아카이브도 같이 수정
  (전엔 챌린지면 빈 채로 done 처리 → 채널 영구 유실).
- 썸네일: 만료 자가복구(공개 oembed, 로그인 불필요) + 실패 no-store + THUMB_V 캐시버스터.
  커버리지 74%→98%(66건 복구). 랭킹 캐시 8/4자 508건으로 복원 후 신규 417건으로 갱신.
- 계정 풀 분리 배포(`ig_sessions/{archive,reference}`) — 폴더 비어 있어 현재는 폴백(동작 불변).
