# 신규채널픽업 (인스타 발굴)

## 2026-07-30 (사무실 PC)

### 발단
"신규채널 픽업이 21개밖에 안 들어온다 — 무슨 원리냐"

### 원리 (조사 결과)
왼쪽 메뉴 **🔎 신규채널 픽업** = `discover.html` → `/api/discover/update`
→ `discover_jobs._run` → `discovery.discover_multi`

```
해시태그 목록 순회 (playwright, 순차)
  → 태그당 SERP 캡처 약 24건 ≈ 고유계정 20 안팎
  → known 제외 (엑셀 채널 + 이미 발굴등록된 채널 전부)
  → 태그당 max_channels_per(=per, 12) 상한
  → 전체 max_total 상한
  → 후보 채널 릴스 수집 → build_items가 window_hours(=days*24) 넘은 릴스 폐기
     ⇒ 최근 N일 안에 릴스 올린 채널만 생존
  → _one_per_channel: 채널당 대표 릴스 1개
  = 최종 개수
```
수율 21은 구조상 정상이었다. **입구(해시태그 6개)가 좁은 게 뿌리.**
게다가 auto_register가 켜져 있으면 어제 픽업분이 전부 known이 돼 회차마다 숫자가 계속 줄어든다.

### 배포한 것 (b7425cb93 → main 8cceaf547, 서버 반영 확인)
1. **CATEGORIES 6 → 20개** (`discover_jobs.py`). 기존 6 + 자취요리·원룸인테리어·주방살림·청소템·
   정리수납·수납템·가성비템·쿠팡추천·쿠팡템·다이소추천·다이소템·육아템·캠핑템·반려견용품
2. **발굴 피드 3일 보존** — `discovery.merge_feeds(ttl_days=)`. 항목마다 `last_seen`을 찍고
   마지막 관측 후 3일 지난 채널만 탈락. 다시 잡히면 시계 리셋. `FEED_TTL_DAYS = 3`
3. **누적 cap = max_total × 3** — 예전 cap=max_total로는 하루치 정원을 넘는 순간 어제 채널이
   댓글수에 밀려 즉시 잘려서 "3일 보존"이 이름만 남았다
4. 테스트 2건 추가 (만료 탈락 / 재관측 리셋). `test_discovery.py` 11 passed.
   finish 게이트: 기준선 13건 그대로 = 신규 회귀 0.

### ⏭ 다음
- **라이브 육안 미실시** — 픽업 화면에서 업데이트 한 번 돌려 개수가 실제로 늘었는지 확인.
  ⚠️ playwright는 태그를 **순차로** 돈다(동시 실행 시 일부 태그가 조용히 0건, 07-30 실측).
  태그 6→20이면 **소요시간이 약 3배**. 너무 길면 12~15개로 줄여라.
- 그래도 부족하면: `max_channels_per`(per=12) / `max_total` 상향, days 2→3~4.

### 별건 — 메인 페이지 "🔍 신규 채널 발굴(해시태그, 무료)"
- 위치: 레퍼런스 랭킹 메인 → 플랫폼 탭 **인스타그램** + 관리자일 때만 (`index.html:330` igDiscoverPanel)
- 경로: `service.discover_instagram_accounts` → `instagram_discovery.discover_accounts`
- **라이브에서 한 번도 안 돌았다** (2026-07-30 서버 DB 실측: `ig_discovery_log` 테이블 자체 없음,
  `ig_discovery_bg_auto` 미설정, `ig_discovery_last_result` 비어있음, 관련 크론 없음)
- 여기 `min_posts=2` = "태그 목록 전체에서 게시물 2개 이상 걸린 계정만" 필터. 태그가 적으면
  대부분의 계정이 1회만 등장 → 품질게이트가 아니라 학살이 된다. 살릴 거면 `min_posts=1`로 내리거나
  `overseas_seeds.json`의 tiktok 키워드 확대. **픽업 화면과는 무관하다**(그쪽은 이 필터 안 씀).
