# 보이스 속삭임 톤 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 나레이션에 `[whispers]` 톤을 넣는다 — 노브 하나(`whisper.roles`)로 전체면 ASMR 영상, 일부면 강조 도구.

**Architecture:** `narration_naturalize.py`에 9번째 순수 스테이지 `_whisper`를 `_emotion_arc` 바로 뒤에 꽂아 `[감정][whispers]` 순서를 만든다. 프리셋에 `best` 플래그 하나를 추가해 **카드 순서(ORDER BY best DESC, created_at)와 ⭐ 배지를 동시에** 해결한다. 베스트 5명에게만 `whisper` variant 프리셋을 만들고, 샘플 mp3는 이미 합성된 것을 재사용한다.

**Tech Stack:** Python 3.14 · SQLite(store.py) · FastAPI(app.py) · pytest · 순수 바닐라 JS(produce.html)

## Global Constraints

- **설계 문서**: `docs/superpowers/specs/2026-07-16-보이스-속삭임톤-design.md` — 이 계획과 충돌하면 설계가 이긴다.
- **베스트 5명(사장님 청취 판정 확정)**: `kr-yooni`(유니) · `kr-mina`(미나) · `kr-chloecha`(클로이차) · `kr-hanabad`(하나바드) · `kr-kanna`(칸나). **이 5명만 whisper variant를 갖는다.**
- **나머지 9명은 그대로 둔다** — 기존 `stable`/`natural`/`expressive` 3톤 유지, 감추지 않는다, whisper만 없다.
- **알려진 v3 태그만 쓴다.** `[whispers]`는 실측 검증됨. 새 태그를 지어내면 성우가 그 글자를 그대로 읽는다.
- **태그 순서는 `[감정][whispers]` 고정** — 프로브에서 사장님이 채택한 순서(`[curious][whispers]`). 순서 민감도는 미검증이므로 바꾸지 않는다.
- **에코·리버브 금지** (사장님 판정 "에코는 빼자"). `audio_post`를 건드리지 않는다.
- **속삭임 강도 슬라이더 금지** — 태그는 켜지거나 꺼질 뿐 중간이 없다.
- **뮤테이션 필수**: 모든 수정에 대해 일부러 깨뜨려 **정확히 그 지점에서만** 실패하는지 확인. `whisper.roles`를 빈 목록으로 바꾸는 뮤턴트가 죽지 않으면 배선 테스트가 0개라는 뜻이다.
- **스테이지 뮤턴트는 `_STAGES` 튜플 자체를 교체할 것** — `_STAGES`가 함수객체를 직접 캡처하므로 모듈 속성 패치(`monkeypatch.setattr`)는 무효인데 테스트는 초록이 된다(거짓 안전).
- **검증은 순정 `merge_profile({})`로** — 격리 프로파일 `_p`는 `emotion_arc`를 꺼서 태그 관련 결함을 통째로 가린다.
- **트랙 폴더에서만 작업**: 모든 경로는 `C:\Users\TheRose\Desktop\로또의 주식\.tracks\보이스\...`. 커밋 전 `git -C .tracks/보이스 status --porcelain`으로 내 파일만 있는지 눈으로 확인.
- **회귀 기준선**: 보이스·자연화 관련 **113 passed**(2026-07-16 실측). 이보다 줄면 안 된다.

---

### Task 1: 엔진 — `_whisper` 스테이지

**Files:**
- Modify: `shopping_shorts/narration_naturalize.py` (DEFAULT_PROFILE:10-29, `_STAGES`:625-628, `_intonation` 뒤에 새 함수)
- Test: `shopping_shorts/tests/test_naturalize_whisper.py` (신규)

**Interfaces:**
- Consumes: `_bump(ctx, name, n)` · `merge_profile(profile)` · `normalize_role(role)` · `ctx["role_canon"]` · `ctx["caps"]`
- Produces: `_whisper(text, cfg, ctx) -> str` · `DEFAULT_PROFILE["whisper"] = {"on": True, "roles": ["반전"]}` · `applied["whisper"]` 카운트

**설계 → 코드 이름 매핑 (중요):** 설계 §3은 노브를 `whisper_roles`라 부른다. 코드에서는 **`whisper.roles`**(스테이지 `whisper`의 `roles` 키)로 구현한다 — `_STAGES` 루프가 `cfg = p.get(name, {}); if cfg.get("on")`으로 도는 탓에 스테이지는 반드시 `{"on": ...}` 모양의 dict여야 하기 때문이다. 다른 8개 스테이지와 같은 모양을 유지한다.

- [ ] **Step 1: Write the failing test**

`shopping_shorts/tests/test_naturalize_whisper.py` 생성:

```python
"""속삭임 스테이지 — 노브 하나(whisper.roles)의 두 설정값.

전체 role = ASMR 영상 / 일부 role = 강조 도구. 설계 2026-07-16-보이스-속삭임톤-design.md.
검증은 순정 merge_profile({})로 한다 — 격리 프로파일은 emotion_arc를 꺼서 태그 결함을 가린다.
"""
import pytest
from shopping_shorts.narration_naturalize import (
    naturalize_detail, merge_profile, DEFAULT_PROFILE, _STAGES,
)

ROLES = ["훅", "페인포인트", "반전", "실용", "CTA"]


def _d(text, role, bi, profile=None, total=5):
    return naturalize_detail(text, profile, beat_role=role, beat_index=bi, beat_total=total)


def test_default_profile_whispers_only_반전():
    """기본값은 강조 도구 — 반전 비트만 속삭이고 나머지는 안 속삭인다."""
    assert DEFAULT_PROFILE["whisper"] == {"on": True, "roles": ["반전"]}
    got = {r: "[whispers]" in _d("이건 진짜 물건이에요", r, i)["text"]
           for i, r in enumerate(ROLES)}
    assert got == {"훅": False, "페인포인트": False, "반전": True,
                   "실용": False, "CTA": False}


def test_all_roles_makes_asmr():
    """roles에 전체 role을 주면 5비트 전부 속삭임 = ASMR 영상."""
    p = {"whisper": {"on": True, "roles": ROLES}}
    for i, r in enumerate(ROLES):
        assert "[whispers]" in _d("이건 진짜 물건이에요", r, i, p)["text"], r


def test_tag_order_is_emotion_then_whispers():
    """설계 §3.2 — 사장님이 채택한 [curious][whispers] 순서를 문자열로 봉인.

    순서를 바꿔 검증한 적이 없으므로 관측된 순서를 고정한다.
    """
    p = {"whisper": {"on": True, "roles": ["훅"]},
         "caps": {"max_tags_total": 3, "max_tags_per_beat": 2, "max_fillers_per_text": 2}}
    out = _d("진짜 대박이에요", "훅", 1, p)["text"]
    assert out.startswith("[curious][whispers] "), out


def test_whisper_ignores_emotion_budget():
    """설계 §3.1 — whisper는 감정 예산과 별개 축.

    emotion_arc를 통째로 꺼도(=감정태그 0개) 속삭임은 그대로 나온다. 이게 깨지면
    'ASMR 톤인데 예산이 모자라 중간 비트가 안 속삭이는' 설계 위반이 된다.
    """
    p = {"whisper": {"on": True, "roles": ROLES}, "emotion_arc": {"on": False}}
    out = _d("이건 진짜 물건이에요", "실용", 3, p)["text"]
    assert "[whispers]" in out
    assert "[warm]" not in out


def test_off_means_no_whisper():
    p = {"whisper": {"on": False, "roles": ROLES}}
    assert "[whispers]" not in _d("이건 진짜 물건이에요", "반전", 2, p)["text"]


def test_empty_roles_means_no_whisper():
    """뮤테이션 앵커 — roles=[]로 만드는 뮤턴트가 반드시 여기서 죽는다."""
    p = {"whisper": {"on": True, "roles": []}}
    assert "[whispers]" not in _d("이건 진짜 물건이에요", "반전", 2, p)["text"]


def test_unknown_role_never_whispers():
    """미지 role(작업대 옛 코퍼스의 body/build)은 속삭이지 않는다.

    emotion_arc는 위치기반으로 폴백하지만 whisper는 폴백하지 않는다 — 속삭임은
    '이 비트가 무슨 역할인가'가 온전히 확정될 때만 켜는 게 맞다(모르면 안 속삭인다).
    """
    p = {"whisper": {"on": True, "roles": ROLES}}
    assert "[whispers]" not in _d("이건 진짜 물건이에요", "body", 1, p)["text"]


def test_applied_counts_whisper():
    """작업대가 '슬라이더 돌렸는데 왜 그대로냐'를 화면에서 보게 하는 계상."""
    p = {"whisper": {"on": True, "roles": ["반전"]}}
    assert _d("이건 진짜 물건이에요", "반전", 2, p)["applied"].get("whisper") == 1
    assert "whisper" not in _d("이건 진짜 물건이에요", "훅", 0, p)["applied"]


def test_per_beat_cap_blocks_second_tag():
    """max_tags_per_beat=1이면 감정태그가 이미 자리를 먹어 속삭임이 안 붙는다."""
    p = {"whisper": {"on": True, "roles": ["훅"]},
         "caps": {"max_tags_total": 3, "max_tags_per_beat": 1, "max_fillers_per_text": 2}}
    out = _d("진짜 대박이에요", "훅", 1, p)["text"]
    assert "[curious]" in out
    assert "[whispers]" not in out


def test_whisper_runs_right_after_emotion_arc():
    """스테이지 순서 봉인 — _STAGES 튜플에서 whisper가 emotion_arc 바로 뒤다.

    이 순서가 [감정][whispers]를 만든다. intonation보다 앞이어야 하는 이유도 여기 있다:
    intonation의 lookbehind가 ']'를 제외하므로 태그 뒤 강조어에 유령 쉼표가 안 생긴다.
    """
    names = [n for n, _ in _STAGES]
    assert names.index("whisper") == names.index("emotion_arc") + 1
    assert names.index("whisper") < names.index("intonation")


def test_no_ghost_comma_after_tags():
    """T6 결함의 자매 케이스 — 태그 2개 뒤 강조어에 유령 쉼표가 안 생긴다.

    beat_index=1이 생명줄이다(bi=0이면 추임새 '음, '이 태그와 강조어 사이에 끼어
    lookbehind가 보는 앞글자가 ']'가 아니라 ','가 되어 버그판으로도 재현이 안 된다).
    """
    p = {"whisper": {"on": True, "roles": ["훅"]},
         "caps": {"max_tags_total": 3, "max_tags_per_beat": 2, "max_fillers_per_text": 2}}
    out = _d("진짜 대박이에요", "훅", 1, p)["text"]
    assert "], 진짜" not in out
    assert "[whispers], " not in out
```

- [ ] **Step 2: Run test to verify it fails**

Run: `cd .tracks/보이스 && python -m pytest shopping_shorts/tests/test_naturalize_whisper.py -v`
Expected: FAIL — `KeyError: 'whisper'` / `AssertionError` (DEFAULT_PROFILE에 whisper 없음, `_STAGES`에 whisper 없음)

- [ ] **Step 3: Write minimal implementation**

`narration_naturalize.py` DEFAULT_PROFILE에 `intonation` 줄 **바로 뒤**(28행 `"intonation"` 다음, `caps` 앞)에 추가:

```python
    # 속삭임 — 노브 하나(roles)의 두 설정값이다(설계 2026-07-16 §3): 전체 role을 주면
    # 5비트 다 속삭이는 ASMR 영상, 일부만 주면 그 비트만 속삭이는 강조 도구. 목소리를
    # 낮추면 오히려 귀가 쏠린다는 게 근거다(사장님 프로브 청취: "집중력이 확 쏠린다").
    # 기본값이 ["반전"]인 이유: 일반 톤 영상에서 반전 비트 하나만 속삭이는 게 강조 도구의
    # 기본 쓸모다. 강도(intensity)가 없는 것은 설계다 — 태그는 켜지거나 꺼질 뿐 중간이 없다.
    "whisper":       {"on": True, "roles": ["반전"]},
```

`caps`의 `max_tags_per_beat`를 1 → 2로 (26행):

```python
    # max_tags_per_beat=2(2026-07-16): [감정][whispers] 두 개를 허용한다. 사장님 청취
    # 판정 "4번이 좋다"(=[curious][whispers])가 근거. 도배 위험은 없다 — 실측으로
    # 확인했듯 5비트 대본에서 감정태그가 붙는 건 훅·CTA 2비트뿐이고(나머지는 예산 밖),
    # whisper도 roles에 든 비트만 받는다. **3 이상으로 올리지 않는다** — 2가 필요한 최대치다.
    "caps": {"max_tags_total": 3, "max_tags_per_beat": 2, "max_fillers_per_text": 2},
```

`_intonation` 함수 **뒤**, `_STAGES` **앞**에 추가:

```python
# 이미 문자열 맨 앞에 붙어 있는 v3 태그 묶음(emotion_arc가 붙인 `[curious] ` 등).
# 속삭임 태그를 그 **뒤**에 꽂아 설계가 고정한 `[감정][whispers]` 순서를 만든다.
_LEADING_TAGS_PAT = re.compile(r"^((?:\[[^\]]+\])+)\s*")
_WHISPER_TAG = "[whispers]"


def _whisper(text, cfg, ctx):
    """roles에 든 role의 비트에 `[whispers]`를 붙인다(설계 2026-07-16 §3).

    **감정 예산과 별개 축이다(§3.1)** — `_emotion_arc`의 intensity·role 우선순위를 전혀
    보지 않는다. 얹었다면 ASMR 톤인데 예산이 모자라 중간 비트가 안 속삭이는 일이 생긴다.
    보는 캡은 `max_tags_per_beat` 하나뿐이고, 그건 "한 비트에 태그 몇 개까지"라는 별개 사실이다.

    **미지 role은 속삭이지 않는다** — `_emotion_arc`와 달리 위치기반 폴백을 하지 않는다.
    속삭임은 비트 역할이 확정될 때만 켜는 게 맞다(모르면 안 켠다). 작업대 옛 코퍼스의
    `body`/`build`가 실제로 이 경로를 탄다.
    """
    canon = ctx.get("role_canon")
    if canon is None or canon not in (cfg.get("roles") or []):
        return text
    # 이 비트에 이미 붙은 태그 수 + 우리 1개가 캡을 넘으면 붙이지 않는다. 붙였다가
    # `_enforce_total_tag_cap`이 사후에 지우면 applied에 "속삭임 1건"이라고 적어놓고
    # 실제로는 없는 거짓말이 된다(T6에서 같은 비대칭을 이미 한 번 고쳤다).
    cap = ctx["caps"].get("max_tags_per_beat", 2)
    if len(re.findall(r"\[[^\]]+\]", text)) + 1 > cap:
        return text
    m = _LEADING_TAGS_PAT.match(text)
    if m:
        out = f"{m.group(1)}{_WHISPER_TAG} {text[m.end():]}"
    else:
        out = f"{_WHISPER_TAG} {text}"
    _bump(ctx, "whisper", 1)
    return out
```

`_STAGES`를 교체(625-628행) — **`emotion_arc` 뒤, `intonation` 앞**:

```python
_STAGES = [("normalize", _normalize), ("spoken_style", _spoken_style),
           ("pronunciation", _pronunciation), ("phrasing", _phrasing),
           ("endings", _endings), ("fillers", _fillers),
           ("emotion_arc", _emotion_arc), ("whisper", _whisper),
           ("intonation", _intonation)]
```

- [ ] **Step 4: Run test to verify it passes**

Run: `cd .tracks/보이스 && python -m pytest shopping_shorts/tests/test_naturalize_whisper.py -v`
Expected: PASS (12 passed)

- [ ] **Step 5: 회귀 — 기준선이 안 줄었나**

Run: `cd .tracks/보이스 && python -m pytest shopping_shorts/tests/ -k "naturalize or voice or tts" -q`
Expected: 기준선 **113 passed** 이상. `max_tags_per_beat` 1→2를 문자열로 봉인한 기존 테스트가 있으면 그 테스트의 기대값을 2로 고치고, **왜 올렸는지**(사장님 청취 판정)를 주석으로 남긴다.

- [ ] **Step 6: 뮤테이션 — 테스트가 결함을 실제로 잡는가**

이 트랙의 규약이다: **"테스트 통과" ≠ "테스트가 결함을 잡음"**. `_STAGES` 튜플 자체를 교체하는 방식으로만 스테이지를 뮤턴트할 것(모듈 속성 패치는 무효인데 초록이 된다).

| 뮤턴트 | 죽어야 하는 테스트 |
|---|---|
| `_whisper`가 항상 `text` 반환 | `test_default_profile_whispers_only_반전` 외 다수 |
| `roles` 검사 제거(전 role 속삭임) | `test_default_profile_whispers_only_반전` **만** |
| `[whispers]`를 태그 **앞**에 삽입 | `test_tag_order_is_emotion_then_whispers` **만** |
| `_whisper`를 `_emotion_arc` **앞**으로 이동 | `test_tag_order_is_emotion_then_whispers` + `test_whisper_runs_right_after_emotion_arc` |
| 캡 검사 제거 | `test_per_beat_cap_blocks_second_tag` **만** |
| 미지 role도 속삭이게 | `test_unknown_role_never_whispers` **만** |

각 뮤턴트가 **정확히 그 테스트에서만** 죽는지 확인한다. 안 죽는 뮤턴트가 있으면 그건 죽은 테스트다 — 실패 메시지가 결함 자체를 재현하는지까지 눈으로 볼 것.

- [ ] **Step 7: Commit**

```bash
git -C .tracks/보이스 status --porcelain
git -C .tracks/보이스 add shopping_shorts/narration_naturalize.py shopping_shorts/tests/test_naturalize_whisper.py
git -C .tracks/보이스 commit -m "feat(보이스): 속삭임 스테이지 — 노브 하나(whisper.roles)로 ASMR/강조 두 설정값"
```

---

### Task 2: 저장 — `best` 플래그 (순서 + 배지를 한 필드로)

**Files:**
- Modify: `shopping_shorts/store.py` (마이그레이션:279-283, `upsert_voice_preset`:1240-1277, `_row_to_preset`:1279-1288, `list_voice_presets`:1299-1308, `get_voice_preset`:1290-1295)
- Test: `shopping_shorts/tests/test_store_voice_best.py` (신규)

**Interfaces:**
- Consumes: `Store(db_path)` · `upsert_voice_preset(p)` · `list_voice_presets(lang)`
- Produces: 프리셋 dict에 `"best": bool` 키 · `list_voice_presets`가 **베스트 먼저** 정렬해 반환

**왜 필드 하나인가:** 설계 §4.1 — `sort_order` + `recommended` 둘로 나누면 "베스트인데 순서는 뒤"라는 모순 상태가 표현 가능해지고, 그걸 막는 코드가 또 필요해진다. 하나가 두 일을 한다.

- [ ] **Step 1: Write the failing test**

`shopping_shorts/tests/test_store_voice_best.py` 생성:

```python
"""베스트 플래그 — 순서와 배지를 한 필드로(설계 §4.1).

★이 파일이 존재하는 이유: 카드 순서가 `voice_presets.json`의 배열 순서라고 **틀리게**
적었다가 실측으로 뒤집혔다. 진짜 순서는 `ORDER BY created_at`(DB 삽입 시각)이라 파일만
재배열하면 화면은 그대로다. 그 전제를 테스트로 못박는다.
"""
import pytest
from shopping_shorts.store import Store


def _p(pid, best=False, gid=None):
    return {"preset_id": pid, "group_id": gid or pid, "variant": "stable",
            "name": pid, "lang": "KR", "base_voice_id": "v1",
            "voice_settings": {"stability": 0.5}, "best": best}


@pytest.fixture
def store(tmp_path):
    return Store(str(tmp_path / "t.db"))


def test_best_roundtrips(store):
    store.upsert_voice_preset(_p("a", best=True))
    store.upsert_voice_preset(_p("b", best=False))
    got = {r["preset_id"]: r["best"] for r in store.list_voice_presets(lang="KR")}
    assert got == {"a": True, "b": False}


def test_best_defaults_false(store):
    """기존 42개 프리셋은 best를 안 적는다 — 빠지면 False여야 한다."""
    p = _p("a"); del p["best"]
    store.upsert_voice_preset(p)
    assert store.list_voice_presets(lang="KR")[0]["best"] is False


def test_best_sorts_first(store):
    """★핵심 — 베스트가 앞. 나중에 넣어도 앞으로 온다(created_at을 이긴다)."""
    store.upsert_voice_preset(_p("first_inserted", best=False))
    store.upsert_voice_preset(_p("later_but_best", best=True))
    assert [r["preset_id"] for r in store.list_voice_presets(lang="KR")] == [
        "later_but_best", "first_inserted"]


def test_created_at_still_breaks_ties(store):
    """베스트끼리는 기존 순서(created_at) 유지 — 정렬이 통째로 뒤집히면 안 된다."""
    store.upsert_voice_preset(_p("b1", best=True))
    store.upsert_voice_preset(_p("b2", best=True))
    assert [r["preset_id"] for r in store.list_voice_presets(lang="KR")] == ["b1", "b2"]


def test_migration_adds_column_to_existing_db(store):
    """옛 DB(best 컬럼 없음)에 컬럼을 붙여도 기존 행이 살아있다."""
    with store._conn() as c:
        c.execute("ALTER TABLE voice_presets DROP COLUMN best")
        c.execute("INSERT INTO voice_presets(preset_id, name, lang, base_voice_id, "
                  "group_id, variant) VALUES('old','old','KR','v1','old','stable')")
    s2 = Store(store.db_path)          # 재기동 = 마이그레이션 재실행
    rows = {r["preset_id"]: r["best"] for r in s2.list_voice_presets(lang="KR")}
    assert rows["old"] is False
```

- [ ] **Step 2: Run test to verify it fails**

Run: `cd .tracks/보이스 && python -m pytest shopping_shorts/tests/test_store_voice_best.py -v`
Expected: FAIL — `KeyError: 'best'` (컬럼·필드 없음)

- [ ] **Step 3: Write minimal implementation**

마이그레이션(279-283행) — `naturalize_profile_json` 선례와 같은 자리에 컬럼 추가:

```python
            # best(2026-07-16): 사장님 청취 판정으로 뽑은 베스트 성우. 이 한 필드가 두 일을
            # 한다 — 카드 정렬(ORDER BY best DESC)과 ⭐ 배지. 둘로 나누면(sort_order +
            # recommended) "베스트인데 순서는 뒤"라는 모순 상태가 표현 가능해진다(설계 §4.1).
            for col, ddl in (("group_id", "TEXT"), ("variant", "TEXT"),
                             ("naturalize_profile_json", "TEXT"),
                             ("best", "INTEGER NOT NULL DEFAULT 0")):
```

> ⚠️ 위 튜플은 **기존 코드의 실제 내용에 맞춰** 병합할 것. 279-283행을 먼저 읽고 이미 있는 항목은 그대로 두고 `("best", "INTEGER NOT NULL DEFAULT 0")`만 더한다.

`upsert_voice_preset` — INSERT 컬럼·VALUES·ON CONFLICT에 `best` 추가:

```python
                INSERT INTO voice_presets(preset_id, name, one_liner, lang, archetype,
                    base_voice_id, model_id, voice_settings_json, default_speed,
                    default_silence_trim, sample_file, source_ref, origin, created_at,
                    group_id, variant, naturalize_profile_json, best)
                VALUES(?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?)
```

ON CONFLICT 절에 추가(`variant=excluded.variant,` 뒤):

```python
                    best=excluded.best,
```

VALUES 인자 끝(`prof_json,` 뒤):

```python
                int(bool(p.get("best", False))),
```

`list_voice_presets` — SELECT에 `best` 추가하고 ORDER BY 교체:

```python
        q = ("SELECT preset_id,name,one_liner,lang,archetype,base_voice_id,model_id,"
             "voice_settings_json,default_speed,default_silence_trim,sample_file,"
             "source_ref,origin,created_at,group_id,variant,naturalize_profile_json,"
             "best FROM voice_presets")
        args = ()
        if lang:
            q += " WHERE lang=?"; args = (lang,)
        # 베스트가 앞, 그 안에선 기존 순서(삽입 시각) 유지. ★순서의 소스오브트루스는
        # 여기다 — assets/voice_presets.json의 배열 순서가 아니다(설계 §4.1의 실측).
        q += " ORDER BY best DESC, created_at"
```

`get_voice_preset`의 SELECT에도 `best`를 같은 위치(맨 끝)에 추가한다 — `_row_to_preset`이 인덱스로 읽으므로 **두 SELECT의 컬럼 순서가 반드시 같아야 한다**.

`_row_to_preset`에 추가:

```python
            "best": bool(r[17]) if len(r) > 17 else False,
```

- [ ] **Step 4: Run test to verify it passes**

Run: `cd .tracks/보이스 && python -m pytest shopping_shorts/tests/test_store_voice_best.py -v`
Expected: PASS (5 passed)

- [ ] **Step 5: 뮤테이션**

| 뮤턴트 | 죽어야 하는 테스트 |
|---|---|
| `ORDER BY best DESC, created_at` → `ORDER BY created_at` | `test_best_sorts_first` **만** |
| `ORDER BY best DESC, created_at` → `ORDER BY best DESC` | `test_created_at_still_breaks_ties`(SQLite가 tie 순서를 보장 안 하므로 불안정하면 이 뮤턴트는 건너뛰고 사유를 적을 것) |
| `int(bool(p.get("best", False)))` → `0` 고정 | `test_best_roundtrips` |
| `best=excluded.best` 제거 | (재upsert 경로 — 커버 없으면 테스트 추가하거나 "미커버"로 정직하게 적을 것) |

- [ ] **Step 6: Commit**

```bash
git -C .tracks/보이스 status --porcelain
git -C .tracks/보이스 add shopping_shorts/store.py shopping_shorts/tests/test_store_voice_best.py
git -C .tracks/보이스 commit -m "feat(보이스): 프리셋 best 플래그 — 순서(ORDER BY best DESC)와 배지를 한 필드로"
```

---

### Task 3: API — `best`를 그룹에 실어 내린다

**Files:**
- Modify: `shopping_shorts/app.py` (`api_voice_presets`:1339-1357)
- Test: `shopping_shorts/tests/test_api_voice_best.py` (신규)

**Interfaces:**
- Consumes: `Store.list_voice_presets(lang)` (Task 2가 정렬해 반환) · 프리셋 dict의 `best`
- Produces: `GET /api/voice-presets?lang=KR` → `{"ok": True, "groups": [{..., "best": bool, "variants": {...}}]}`

- [ ] **Step 1: Write the failing test**

`shopping_shorts/tests/test_api_voice_best.py` 생성:

```python
"""API가 best를 그룹에 실어 내리는가 + 정렬을 보존하는가.

Store가 정렬해 줘도 app.py가 dict로 묶는 과정에서 순서가 깨지면 화면이 안 바뀐다.
그 이음새를 잠근다(파이썬 dict는 삽입 순서를 보존하므로 통과해야 정상).
"""
from fastapi.testclient import TestClient
from shopping_shorts.app import app


def test_groups_carry_best_and_order(monkeypatch):
    rows = [
        {"preset_id": "kr-mina-stable", "group_id": "kr-mina", "variant": "stable",
         "name": "미나", "one_liner": "차분", "lang": "KR", "archetype": "차분",
         "base_voice_id": "v1", "voice_settings": {}, "default_speed": 1.0,
         "default_silence_trim": "mid", "sample_file": "kr-mina-stable.mp3", "best": True},
        {"preset_id": "kr-han-stable", "group_id": "kr-han", "variant": "stable",
         "name": "한", "one_liner": "캐주얼", "lang": "KR", "archetype": "캐주얼",
         "base_voice_id": "v2", "voice_settings": {}, "default_speed": 1.0,
         "default_silence_trim": "mid", "sample_file": None, "best": False},
    ]
    monkeypatch.setattr("shopping_shorts.app.Store",
                        lambda *_a, **_k: type("S", (), {"list_voice_presets": lambda s, lang=None: rows})())
    d = TestClient(app).get("/api/voice-presets?lang=KR").json()
    assert [g["group_id"] for g in d["groups"]] == ["kr-mina", "kr-han"]
    assert [g["best"] for g in d["groups"]] == [True, False]


def test_best_defaults_false_when_absent(monkeypatch):
    rows = [{"preset_id": "x-stable", "group_id": "x", "variant": "stable", "name": "x",
             "one_liner": "", "lang": "KR", "archetype": "", "base_voice_id": "v",
             "voice_settings": {}, "default_speed": 1.0, "default_silence_trim": "off",
             "sample_file": None}]
    monkeypatch.setattr("shopping_shorts.app.Store",
                        lambda *_a, **_k: type("S", (), {"list_voice_presets": lambda s, lang=None: rows})())
    assert TestClient(app).get("/api/voice-presets?lang=KR").json()["groups"][0]["best"] is False
```

- [ ] **Step 2: Run test to verify it fails**

Run: `cd .tracks/보이스 && python -m pytest shopping_shorts/tests/test_api_voice_best.py -v`
Expected: FAIL — `KeyError: 'best'`

- [ ] **Step 3: Write minimal implementation**

`api_voice_presets`의 `groups.setdefault` 블록(1346-1350행)에 `best` 추가:

```python
        g = groups.setdefault(gid, {
            "group_id": gid, "name": p["name"], "one_liner": p["one_liner"],
            "lang": p["lang"], "archetype": p["archetype"],
            # best는 성우(그룹)의 성질이라 첫 행에서 한 번만 집는다 — 같은 성우의
            # 3~4개 variant 행은 모두 같은 값이다. 순서는 Store가 이미 정해서
            # 줬고(ORDER BY best DESC), dict가 삽입 순서를 보존하므로 여기선 보존만 한다.
            "best": bool(p.get("best", False)),
            "default_variant": "stable", "variants": {},
        })
```

docstring도 정정한다(1340-1341행) — "3톤"이 아니라 베스트 5명은 4톤이다:

```python
    """성우별 그룹 목록(유저 노출용 — source_ref는 내부 전용이라 제외).

    성우 1명당 stable(기본 노출)/natural/expressive를 variants에 묶어 반환하고,
    베스트 5명은 whisper까지 4톤이다(2026-07-16 청취 판정). best=베스트 성우 플래그로,
    프론트가 ⭐ 배지에 쓴다. **순서는 여기서 정하지 않는다** — Store.list_voice_presets의
    ORDER BY best DESC, created_at이 정본이고 이 함수는 그 순서를 보존만 한다."""
```

- [ ] **Step 4: Run test to verify it passes**

Run: `cd .tracks/보이스 && python -m pytest shopping_shorts/tests/test_api_voice_best.py -v`
Expected: PASS (2 passed)

- [ ] **Step 5: 뮤테이션**

`"best": bool(p.get("best", False))` → `"best": False` 고정 → `test_groups_carry_best_and_order`**만** 죽어야 한다.

- [ ] **Step 6: Commit**

```bash
git -C .tracks/보이스 status --porcelain
git -C .tracks/보이스 add shopping_shorts/app.py shopping_shorts/tests/test_api_voice_best.py
git -C .tracks/보이스 commit -m "feat(보이스): /api/voice-presets가 best를 그룹에 실어 내림"
```

---

### Task 4: 프리셋 5개 + 샘플 mp3 재사용

**Files:**
- Modify: `shopping_shorts/assets/voice_presets.json` (42개 → 47개, 베스트 5명에 `best: true`)
- Create: `shopping_shorts/assets/voice_samples/kr-{mina,chloecha,hanabad,kanna,yooni}-whisper.mp3` (복사)
- Test: `shopping_shorts/tests/test_voice_presets_whisper.py` (신규)

**Interfaces:**
- Consumes: `voice_presets.load_presets_file()` · `SAMPLES_DIR`
- Produces: `preset_id` 5개 — `kr-mina-whisper` · `kr-chloecha-whisper` · `kr-hanabad-whisper` · `kr-kanna-whisper` · `kr-yooni-whisper`

**샘플을 재합성하지 않는다** — `out/whisper_all14/<group_id>_whisper.mp3`가 이미 실합성본이다(사장님이 이 파일들을 듣고 판정했다). 재합성하면 ElevenLabs 크레딧을 쓰고, **사장님이 판정한 그 소리가 아닌 다른 take가 나온다**(v3는 take 편차가 크다).

- [ ] **Step 1: Write the failing test**

`shopping_shorts/tests/test_voice_presets_whisper.py` 생성:

```python
"""whisper 프리셋 — 베스트 5명만, 나머지는 없다(사장님 청취 판정 2026-07-16)."""
from shopping_shorts.voice_presets import load_presets_file, SAMPLES_DIR

BEST = {"kr-yooni", "kr-mina", "kr-chloecha", "kr-hanabad", "kr-kanna"}
REJECTED = {"kr-hanna", "kr-juha", "kr-jiana"}      # [whispers] 안 먹힘


def _by_variant(v):
    return [p for p in load_presets_file() if p["variant"] == v]


def test_whisper_presets_are_exactly_the_best_five():
    assert {p["group_id"] for p in _by_variant("whisper")} == BEST


def test_rejected_voices_have_no_whisper():
    """탈락 3명에게 whisper가 생기면 안 된다 — 억지로 붙이면 품질이 떨어진다(설계 §8)."""
    assert not (REJECTED & {p["group_id"] for p in _by_variant("whisper")})


def test_best_flag_matches_best_five():
    """best 플래그는 성우 단위 — 그 성우의 모든 variant가 같은 값이어야 한다.

    어긋나면 API가 첫 행에서 집는 값이 어느 variant가 먼저 오느냐에 따라 달라진다.
    """
    for p in load_presets_file():
        assert p.get("best", False) is (p["group_id"] in BEST), p["preset_id"]


def test_non_best_voices_keep_three_tones():
    """나머지 9명은 감추는 게 아니라 그대로 둔다 — 3톤이 살아있는가."""
    groups = {}
    for p in load_presets_file():
        groups.setdefault(p["group_id"], set()).add(p["variant"])
    for gid, variants in groups.items():
        assert {"stable", "natural", "expressive"} <= variants, gid
        if gid not in BEST:
            assert "whisper" not in variants, gid


def test_whisper_presets_carry_full_role_profile():
    """whisper 프리셋 = roles가 전체 role = ASMR 영상(설계 §3 표)."""
    for p in _by_variant("whisper"):
        roles = p["naturalize_profile"]["whisper"]["roles"]
        assert set(roles) == {"훅", "페인포인트", "반전", "실용", "CTA"}, p["preset_id"]


def test_whisper_samples_exist_and_are_real_audio():
    """무음 mock이 아닌 실합성본인가 — 크기가 제각각이면 실합성이다.

    `synthesize_tts`는 키가 없으면 조용히 무음 mp3를 쓴다. 프로브를 짤 때 이 함정에
    실제로 걸렸다(가드가 없었으면 "속삭임이 한국어에 안 먹힌다"는 틀린 결론을 낼 뻔했다).
    """
    sizes = []
    for p in _by_variant("whisper"):
        f = SAMPLES_DIR / p["sample_file"]
        assert f.exists(), f
        assert f.stat().st_size > 10_000, f
        sizes.append(f.stat().st_size)
    assert len(set(sizes)) > 1, "전부 같은 크기 = 무음 mock 의심"


def test_total_is_47():
    assert len(load_presets_file()) == 47      # 42 + 5
```

- [ ] **Step 2: Run test to verify it fails**

Run: `cd .tracks/보이스 && python -m pytest shopping_shorts/tests/test_voice_presets_whisper.py -v`
Expected: FAIL — whisper variant 0개, `best` 키 없음, 파일 42개

- [ ] **Step 3: 샘플 mp3 복사**

```bash
cd ".tracks/보이스"
for g in mina chloecha hanabad kanna yooni; do
  cp "../../out/whisper_all14/kr-${g}_whisper.mp3" \
     "shopping_shorts/assets/voice_samples/kr-${g}-whisper.mp3"
done
ls -la shopping_shorts/assets/voice_samples/*-whisper.mp3
```

Expected: 5개 파일, 크기 제각각(40~52KB 범위, 전부 다른 값)

- [ ] **Step 4: 프리셋 JSON 갱신**

`assets/voice_presets.json`을 스크립트로 고친다(손으로 42개를 편집하지 말 것):

```python
# scratchpad/add_whisper.py 로 저장해 1회 실행
import json, copy
from pathlib import Path

P = Path("shopping_shorts/assets/voice_presets.json")
BEST = {"kr-yooni", "kr-mina", "kr-chloecha", "kr-hanabad", "kr-kanna"}
ALL_ROLES = ["훅", "페인포인트", "반전", "실용", "CTA"]

rows = json.loads(P.read_text(encoding="utf-8"))
assert len(rows) == 42, len(rows)

# ① 모든 프리셋에 best 플래그(성우 단위 — 그 성우의 모든 variant가 같은 값)
for p in rows:
    p["best"] = p["group_id"] in BEST

# ② 베스트 5명에게 whisper variant — stable을 본떠 만든다(감도·속도는 그대로 물려받고
#    다른 건 naturalize_profile.whisper.roles가 전체라는 것뿐, 설계 §4)
for gid in BEST:
    base = next(p for p in rows if p["group_id"] == gid and p["variant"] == "stable")
    w = copy.deepcopy(base)
    w.update({
        "preset_id": f"{gid}-whisper",
        "variant": "whisper",
        "sample_file": f"{gid}-whisper.mp3",
        "source_ref": "사장님 청취 판정 2026-07-16(베스트 5명) — out/whisper_all14 프로브",
        "naturalize_profile": {"whisper": {"on": True, "roles": ALL_ROLES}},
    })
    rows.append(w)

assert len(rows) == 47, len(rows)
P.write_text(json.dumps(rows, ensure_ascii=False, indent=2) + "\n", encoding="utf-8")
print("ok", len(rows))
```

Run: `cd .tracks/보이스 && python ../../scratchpad/add_whisper.py`
Expected: `ok 47`

> ⚠️ `naturalize_profile`은 upsert가 **COALESCE로 보존**한다 — 작업대에서 동결한 값이 소스오브트루스이기 때문(2026-07-15 리뷰 S2). 즉 **새 프리셋에만 적용되고 기존 프리셋의 동결값은 안 덮인다**. 이건 의도된 동작이다.

- [ ] **Step 5: Run test to verify it passes**

Run: `cd .tracks/보이스 && python -m pytest shopping_shorts/tests/test_voice_presets_whisper.py -v`
Expected: PASS (7 passed)

- [ ] **Step 6: Commit**

```bash
git -C .tracks/보이스 status --porcelain
git -C .tracks/보이스 add shopping_shorts/assets/voice_presets.json \
    shopping_shorts/assets/voice_samples/ shopping_shorts/tests/test_voice_presets_whisper.py
git -C .tracks/보이스 commit -m "feat(보이스): whisper 프리셋 5개(베스트) + best 플래그 42개 — 샘플은 판정본 재사용"
```

---

### Task 5: UI — whisper 탭 + ⭐ 배지 + **작업대 속삭임 노브**

**Files:**
- Modify: `shopping_shorts/static/produce.html` (`SEL_VARIANT` 주석:1570, `VARIANT_LABELS`:1571, `renderPresetCards`:1585-1604)
- Modify: `shopping_shorts/static/voice_tune.html` (`STAGES`:31-33, `defaultProfile`:36-46, `renderStages`:47~)

**Interfaces:**
- Consumes: `/api/voice-presets` 응답의 `groups[].best` · `groups[].variants.whisper` · 프로파일의 `whisper.roles`
- Produces: (화면만 — 다른 태스크가 의존하는 심볼 없음)

#### 5.0 ⚠️ 계획의 구멍 — 작업대에 속삭임 노브가 없었다 (T1 리뷰가 잡음)

원래 이 태스크는 `produce.html`만 건드리게 돼 있었다. **그건 설계 위반이다** — 설계 §3이
"이 노브는 프로파일에 들어가므로 **작업대에서 튜닝**되고, 새 저장층이 필요 없다"고 명시한다.

T1 리뷰가 실측으로 파급을 짚었다: `voice_tune.html:31-33`의 `STAGES`에 whisper가 없으면
저장되는 프로파일에 `whisper` 키가 **아예 없고** → 서버 `merge_profile`이 기본값
(`roles:["반전"]`)을 채운다 → **모든 프리셋이 반전 비트에서 속삭이는데 UI에 끌 수단이 없다**.
동결(`_frozen`)조차 새 스테이지 기본값에 대해선 동결이 아니다.

> 라이브 노출은 없다 — `finish`는 T6에서만 하므로 이 구멍은 병합 전에 닫힌다.
> 하지만 **닫지 않고 병합하면** 사장님이 못 끄는 속삭임이 42개 프리셋에 깔린다.

**다행히 기존 구조가 이미 받아준다(실측):**

| 사실 | 의미 |
|---|---|
| `renderStages`가 `('intensity' in s)`로 슬라이더를 조건부 렌더 | whisper는 intensity가 없으니 **자동으로 on/off만** 뜬다 — 분기 추가 불필요 |
| `defaultProfile()`에서 `normalize`·`pronunciation`·`fillers`가 자기 모양을 덮어쓰는 선례 | whisper도 같은 자리에서 `{on, roles}`로 덮으면 된다 |

**절대 하지 말 것**: `defaultProfile()`의 `STAGES.forEach(([k])=>p[k]={on:true,intensity:0.3})`가
whisper에 **intensity를 붙이게 두는 것**. 붙으면 (1)슬라이더가 뜨고 (2)"속삭임 강도"라는
설계가 금지한 개념이 생긴다(§6 — 태그는 켜지거나 꺼질 뿐 중간이 없다).

- [ ] **Step 0: 작업대에 속삭임 노브**

`STAGES`(31-33행)에 추가:

```javascript
const STAGES=[["normalize","정규화"],["spoken_style","구어체"],["pronunciation","연음/발음"],
 ["phrasing","끊어읽기"],["endings","끝음"],["fillers","추임새"],
 ["emotion_arc","감정곡선"],["whisper","속삭임"],["intonation","억양"]];
```

`defaultProfile()`에 whisper 모양 덮어쓰기(`p.fillers=...` 줄 뒤):

```javascript
 // 속삭임은 intensity가 없다(설계 §6: 태그는 켜지거나 꺼질 뿐 중간이 없다).
 // 위 forEach가 넣은 intensity를 여기서 걷어내지 않으면 renderStages의
 // ('intensity' in s) 검사에 걸려 있지도 않은 "속삭임 강도" 슬라이더가 뜬다.
 // roles = 속삭일 비트의 role 목록. 전체면 ASMR, 일부면 강조 도구(설계 §3).
 p.whisper={on:true,roles:["반전"]};
```

**role 선택 UI**: `renderStages`에서 whisper일 때만 role 체크박스 5개
(훅·페인포인트·반전·실용·CTA)를 슬라이더 자리에 그린다. 체크된 것들이 `profile.whisper.roles`가
된다. 전부 체크 = ASMR, 반전만 = 강조 도구 — **설계 §3 표의 두 설정값이 화면에서 그대로 만들어진다.**

- [ ] **Step 0b: 작업대 grounding**

작업대를 실제로 띄워 확인한다(정적 확인은 관측이 아니다):
- 속삭임 스테이지가 **감정곡선과 억양 사이**에 뜨나
- **강도 슬라이더가 안 뜨나**(뜨면 defaultProfile 덮어쓰기가 안 먹은 것)
- role 체크박스를 바꾸면 ▶합성 결과 텍스트의 `[whispers]` 위치가 따라 바뀌나
- 콘솔 에러 0

**감추는 코드를 넣지 말 것** — `filter(k => g.variants[k])`가 이미 존재하는 variant만 그린다. whisper 없는 9명은 자동으로 탭이 안 뜬다.

- [ ] **Step 1: 라벨·탭 배열 수정**

1570-1571행:

```javascript
let SEL_VARIANT = {}; // group_id -> 'stable'|'natural'|'expressive'|'whisper' (기본 노출은 stable, 나머지는 버튼으로 전환)
const VARIANT_LABELS = {stable:'안정', natural:'자연', expressive:'표현', whisper:'속삭임'};
```

1590행 — 배열에 `whisper` 추가:

```javascript
    // whisper는 베스트 5명에게만 프리셋이 있다(사장님 청취 판정 2026-07-16). filter가
    // 존재하는 variant만 그리므로 나머지 9명은 탭이 자동으로 안 뜬다 — 숨김 분기 불필요.
    const tabs=['stable','natural','expressive','whisper'].filter(k=>g.variants[k]).map(k=>
```

- [ ] **Step 2: ⭐ 배지**

1596행 교체:

```javascript
        <b>${esc(g.name)}</b>${g.best?' <span title="사장님 청취 판정 베스트" style="color:var(--accent2)">⭐</span>':''} — ${esc(g.one_liner||'')}
```

- [ ] **Step 3: 문법 검증**

Run: `cd .tracks/보이스 && node --check <(sed -n '/<script>/,/<\/script>/p' shopping_shorts/static/produce.html | sed '1d;$d') 2>&1 | head -3`

> node가 없거나 위 추출이 안 되면 브라우저 콘솔 에러 0으로 대체하고 **"문법검증 미실시"**를 정직하게 적을 것. 지어내지 말 것.

- [ ] **Step 4: Commit**

```bash
git -C .tracks/보이스 status --porcelain
git -C .tracks/보이스 add shopping_shorts/static/produce.html
git -C .tracks/보이스 commit -m "feat(보이스): 제작소 카드에 속삭임 탭 + 베스트 ⭐ 배지"
```

---

### Task 6: 라이브 grounding — 화면으로 본다

**Files:** (코드 수정 없음 — 관측만. 결함 발견 시 해당 Task로 되돌아간다)

**이 트랙의 규약**: 최종 게이트는 **사장님 귀**이고 자동테스트로 대체 불가다. 그리고 **"테스트 통과" ≠ "동작함"** — 2026-07-15에 동결 프로파일이 렌더에 **0% 반영**되는 결함(S1)이 유닛 리뷰를 그대로 통과한 전례가 있다. 그 seam이 여기서 다시 열릴 수 있다.

- [ ] **Step 1: 전체 회귀 + 게이트**

Run: `cd .tracks/보이스 && python -m pytest shopping_shorts/tests/ -q 2>&1 | tail -5`
Expected: **기준선 10 failed / 715 passed와 같은 실패 이름**. 실패가 늘었으면 병합 금지.

- [ ] **Step 2: 병합·배포**

```bash
cd "C:/Users/TheRose/Desktop/로또의 주식"
py tools/track.py finish 보이스
```

게이트(문법 → `import shopping_shorts.app` → pytest)를 통과해야만 main으로 간다. 막히면 라이브는 무사하고 트랙 폴더도 그대로다 — 고치고 다시 `finish`. 옆 트랙과 경합해 재시도 3회가 소진되면 그냥 다시 실행한다(실패 아님).

- [ ] **Step 3: 라이브 육안 확인** (배포 3분 뒤, https://shoppingshorts.duckdns.org)

| 볼 것 | 합격 |
|---|---|
| 베스트 5명이 카드 **맨 앞**인가 | 유니·미나·클로이차·하나바드·칸나가 위 |
| 그 5명에 **⭐**가 있나 | 보임 |
| 그 5명에 **속삭임 탭**이 있나 | 4번째 탭 |
| 나머지 9명은 **탭 3개**인가 | 속삭임 없음, 카드는 살아있음 |
| 탈락 3명(한나·주하·지아나)도 **살아있나** | 3톤 그대로 |
| 콘솔 에러 | 0 |

- [ ] **Step 4: ★소리로 확인 — 여기가 진짜 게이트**

속삭임 프리셋으로 **실제 비트를 합성**해서 듣는다. 화면에 탭이 떴다고 소리가 속삭이는 게 아니다 — 프리셋의 `naturalize_profile`이 **렌더 경로까지 실제로 흘러가는가**가 S1이 뚫었던 바로 그 구멍이다.

```
① 제작소에서 베스트 성우 하나 골라 '속삭임' 탭 선택
② 비트 하나 합성(▶)
③ 들어본다 — 5비트 전부 속삭이나(ASMR)? 한 비트만인가(강조)?
```

**반드시 `mix_pipeline.synthesize_line()`을 타야 한다** — 작업대와 렌더가 각자 파이프라인을 조립하면 인자가 갈린다(2026-07-15 seam 10건의 근본원인). 새 호출부를 만들지 말 것.

- [ ] **Step 5: 사장님 판정 수령**

들려드리고 확인받는다:
- 강조 도구(기본 `["반전"]`)가 일반 톤 영상에서 자연스러운가?
- ASMR 톤(전체 role)이 영상 하나로 쓸 만한가?
- `[감정][whispers]` 조합이 프로브 4번만큼 좋은가?

**미검증으로 남는 것(정직하게 남김)**: 태그 순서 민감도(§3.2) — 프로브에서 관측된 순서를 고정했을 뿐 바꿔서 비교한 적 없다.

- [ ] **Step 6: 핸드오프·로그 갱신**

```bash
# handoff/보이스.md 에 이 세션 결과 추가(완료/미완료 구체적으로)
# wiki/log.d/보이스.md 에 1~3줄
git -C .tracks/보이스 add handoff/보이스.md wiki/log.d/보이스.md
git -C .tracks/보이스 commit -m "docs(보이스): 속삭임 톤 구현·판정 기록"
```

---

## 안 만드는 것 (설계 §6 재확인)

| 항목 | 왜 |
|---|---|
| 울림·리버브·에코 | 사장님 판정 "에코는 빼자" |
| 영상별 비트 토글 | 새 저장층이 필요하다. role 규칙으로 먼저 내보고 "이 영상만 다르게"가 실제로 생기면 그때 (사장님 승인: "나중에 수정하자") |
| 속삭임 강도 슬라이더 | 태그는 켜지거나 꺼질 뿐 중간이 없다 |
| `recommended` + `sort_order` 두 필드 | `best` 하나가 둘 다 한다(§4.1) |
| 통과 6명 whisper 프리셋 | 사장님 확정 "그냥 5명만 써" |
| 샘플 재합성 | 판정본이 이미 있다. 재합성하면 사장님이 들은 그 take가 아니다 |
