# 회원관리 고도화 (이름·전화·결제이력·서비스기간) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 가입 시 이름·전화 수집, 결제이력 수동누적, 서비스 시작·마감일 관리를 admin에 붙인다.

**Architecture:** customers에 name·phone 컬럼 추가 + payments 테이블 신규. [승인] 시 금액·수단·이용일수를 함께 받아 결제기록+기간설정을 원자적으로 처리(자동 7일 체험 제거). 이미 승인된 고객은 [결제추가]로 연장. 서비스 시작일=approved_at, 마감일=full_access_until 재활용.

**Tech Stack:** FastAPI, sqlite3(Store), TestClient(pytest). 기존 유료게이트/회원승인 코드 위에 얹음.

**작업 폴더:** `.tracks/회원승인` (모든 경로는 이 폴더 기준). git은 `git -C .tracks/회원승인`.

**테스트 스타일(기존 관례 — 반드시 따를 것):** `shopping_shorts/tests/test_paywall_*.py`의 헬퍼를 쓴다:
```python
from datetime import datetime, timezone
from fastapi.testclient import TestClient
from shopping_shorts import app as appmod
from shopping_shorts.store import Store
def _cookie(cid):
    exp = int(datetime.now(timezone.utc).timestamp()) + 3600
    return appmod._sign_session(cid, exp)
def _setup(tmp_path, monkeypatch):
    monkeypatch.setattr(appmod, "DB_PATH", str(tmp_path / "t.db"))
    monkeypatch.setattr(appmod, "_AUTH_ON", True)
    monkeypatch.setattr(appmod, "DASH_SECRET", "test-secret-xyz")
    s = Store(str(tmp_path / "t.db"))
    s.ensure_paywall_schema()
    return s
```
사장님 세션 = `_cookie(0)`. 전체 회귀: `py -m pytest shopping_shorts/tests/test_paywall_*.py -q`.

---

### Task 1: 스키마 — customers.name·phone + payments 테이블

**Files:**
- Modify: `shopping_shorts/store.py` — `ensure_paywall_schema`(approved_at 백필 블록 근처, 약 678행)
- Test: `shopping_shorts/tests/test_member_mgmt.py` (신규)

**Interfaces:** ensure_paywall_schema가 name·phone 컬럼과 payments 테이블을 멱등 생성.

- [ ] **Step 1: 실패 테스트 작성** — `test_member_mgmt.py` 신규

```python
from datetime import datetime, timezone
from fastapi.testclient import TestClient
from shopping_shorts import app as appmod
from shopping_shorts.store import Store


def _cookie(cid):
    exp = int(datetime.now(timezone.utc).timestamp()) + 3600
    return appmod._sign_session(cid, exp)


def _setup(tmp_path, monkeypatch):
    monkeypatch.setattr(appmod, "DB_PATH", str(tmp_path / "t.db"))
    monkeypatch.setattr(appmod, "_AUTH_ON", True)
    monkeypatch.setattr(appmod, "DASH_SECRET", "test-secret-xyz")
    s = Store(str(tmp_path / "t.db"))
    s.ensure_paywall_schema()
    return s


def test_schema_has_name_phone_and_payments(tmp_path, monkeypatch):
    s = _setup(tmp_path, monkeypatch)
    with s._conn() as c:
        cols = {r[1] for r in c.execute("PRAGMA table_info(customers)").fetchall()}
        assert "name" in cols and "phone" in cols
        pcols = {r[1] for r in c.execute("PRAGMA table_info(payments)").fetchall()}
    assert {"id", "customer_id", "amount", "method", "period_days", "paid_at", "note", "created_at"} <= pcols
```

- [ ] **Step 2: 실패 확인**

Run: `py -m pytest shopping_shorts/tests/test_member_mgmt.py::test_schema_has_name_phone_and_payments -q`
Expected: FAIL (name 컬럼 없음)

- [ ] **Step 3: 구현** — `ensure_paywall_schema`의 approved_at 백필 블록 바로 뒤(약 685행 `UPDATE customers SET approved_at...` 다음)에 추가

```python
        # ── 회원관리(2026-07-22): 이름·전화 + 결제이력 ──
        with self._conn() as c:
            existing = {r[1] for r in c.execute("PRAGMA table_info(customers)").fetchall()}
            for col in ("name", "phone"):
                if col not in existing:
                    c.execute(f"ALTER TABLE customers ADD COLUMN {col} TEXT")
            c.execute(
                "CREATE TABLE IF NOT EXISTS payments ("
                "id INTEGER PRIMARY KEY AUTOINCREMENT, customer_id INTEGER NOT NULL, "
                "amount INTEGER NOT NULL DEFAULT 0, method TEXT, "
                "period_days INTEGER NOT NULL DEFAULT 0, paid_at INTEGER NOT NULL, "
                "note TEXT, created_at INTEGER NOT NULL)"
            )
            c.execute("CREATE INDEX IF NOT EXISTS idx_payments_customer ON payments(customer_id)")
```

> ⚠️ 이 블록이 `ensure_paywall_schema` 안(들여쓰기 8칸, `with self._conn()` 이미 열려있으면 새로 열어도 됨 — sqlite 재진입 OK)에 들어가는지 확인. approved_at 블록과 같은 패턴.

- [ ] **Step 4: 통과 확인**

Run: `py -m pytest shopping_shorts/tests/test_member_mgmt.py -q`
Expected: PASS

- [ ] **Step 5: 커밋**

```bash
git -C .tracks/회원승인 add shopping_shorts/store.py shopping_shorts/tests/test_member_mgmt.py
git -C .tracks/회원승인 commit -m "feat(회원관리): customers.name·phone 컬럼 + payments 테이블"
```

---

### Task 2: create_customer가 name·phone 저장

**Files:**
- Modify: `shopping_shorts/store.py` — `create_customer`(약 2459행) 시그니처·INSERT
- Test: `shopping_shorts/tests/test_member_mgmt.py`

**Interfaces:** `create_customer(username, password, email=None, google_sub=None, approved=True, name=None, phone=None)`. get_customer가 name·phone 노출.

- [ ] **Step 1: 실패 테스트**

```python
def test_create_customer_stores_name_phone(tmp_path, monkeypatch):
    s = _setup(tmp_path, monkeypatch)
    cid = s.create_customer("u1", "pw12", name="홍길동", phone="010-1234-5678")
    cust = s.get_customer(cid)
    assert cust["name"] == "홍길동" and cust["phone"] == "010-1234-5678"
```

- [ ] **Step 2: 실패 확인**

Run: `py -m pytest shopping_shorts/tests/test_member_mgmt.py::test_create_customer_stores_name_phone -q`
Expected: FAIL (create_customer가 name 인자 없음 → TypeError)

- [ ] **Step 3: 구현**

`create_customer` 시그니처에 `name=None, phone=None` 추가. INSERT 문에 `name, phone` 컬럼과 값 추가. 현재 INSERT(약 2480행):
```python
                    "INSERT INTO customers(username, password_hash, salt, created_at, "
                    "plan, full_access_until, email, google_sub, approved_at) "
```
을 다음으로:
```python
                    "INSERT INTO customers(username, password_hash, salt, created_at, "
                    "plan, full_access_until, email, google_sub, approved_at, name, phone) "
```
그리고 VALUES 튜플 끝에 `name, phone` 추가(기존 튜플 마지막이 `approved_at` 뒤 → `..., approved_at, name, phone)`). VALUES 플레이스홀더 개수(`?`)도 2개 늘린다.

> get_customer가 `SELECT *` 또는 명시 컬럼인지 확인. 명시 컬럼이면 name·phone 추가. `SELECT *`면 자동 노출(dict 키 확인).

- [ ] **Step 4: 통과 확인**

Run: `py -m pytest shopping_shorts/tests/test_member_mgmt.py -q`
Expected: PASS

- [ ] **Step 5: 커밋**

```bash
git -C .tracks/회원승인 add shopping_shorts/store.py shopping_shorts/tests/test_member_mgmt.py
git -C .tracks/회원승인 commit -m "feat(회원관리): create_customer가 name·phone 저장"
```

---

### Task 3: payments 스토어 — add_payment / list_payments

**Files:**
- Modify: `shopping_shorts/store.py` — approve_customer 근처에 결제 메서드 추가
- Test: `shopping_shorts/tests/test_member_mgmt.py`

**Interfaces:**
- `add_payment(customer_id, amount, method, period_days, paid_at=None, note=None)` → 삽입된 payment dict. paid_at 없으면 now.
- `list_payments(customer_id)` → 결제 dict 리스트, 최신(paid_at desc) 먼저.

- [ ] **Step 1: 실패 테스트**

```python
def test_add_and_list_payments(tmp_path, monkeypatch):
    s = _setup(tmp_path, monkeypatch)
    cid = s.create_customer("u2", "pw12")
    p1 = s.add_payment(cid, 30000, "계좌이체", 30, paid_at=1000, note="1월")
    s.add_payment(cid, 30000, "카드", 30, paid_at=2000)
    assert p1["amount"] == 30000 and p1["period_days"] == 30
    rows = s.list_payments(cid)
    assert len(rows) == 2
    assert rows[0]["paid_at"] == 2000   # 최신 먼저
    assert rows[1]["method"] == "계좌이체"
```

- [ ] **Step 2: 실패 확인**

Run: `py -m pytest shopping_shorts/tests/test_member_mgmt.py::test_add_and_list_payments -q`
Expected: FAIL (add_payment 없음)

- [ ] **Step 3: 구현** — `approve_customer` 정의 바로 위에 추가

```python
    def add_payment(self, customer_id, amount, method, period_days, paid_at=None, note=None):
        """결제 1건 기록(append-only, 연장은 새 건). 삽입된 결제 dict 반환."""
        now_ts = int(datetime.now(timezone.utc).timestamp())
        pa = int(paid_at) if paid_at is not None else now_ts
        with self._conn() as c:
            cur = c.execute(
                "INSERT INTO payments(customer_id, amount, method, period_days, paid_at, note, created_at) "
                "VALUES(?,?,?,?,?,?,?)",
                (customer_id, int(amount), method, int(period_days), pa, note, now_ts))
            pid = cur.lastrowid
        return {"id": pid, "customer_id": customer_id, "amount": int(amount), "method": method,
                "period_days": int(period_days), "paid_at": pa, "note": note, "created_at": now_ts}

    def list_payments(self, customer_id):
        """그 고객 결제 전체, 최신(paid_at desc, id desc) 먼저."""
        with self._conn() as c:
            rows = c.execute(
                "SELECT id, customer_id, amount, method, period_days, paid_at, note, created_at "
                "FROM payments WHERE customer_id=? ORDER BY paid_at DESC, id DESC",
                (customer_id,)).fetchall()
        return [{"id": r[0], "customer_id": r[1], "amount": r[2], "method": r[3],
                 "period_days": r[4], "paid_at": r[5], "note": r[6], "created_at": r[7]} for r in rows]
```

- [ ] **Step 4: 통과 확인**

Run: `py -m pytest shopping_shorts/tests/test_member_mgmt.py -q`
Expected: PASS

- [ ] **Step 5: 커밋**

```bash
git -C .tracks/회원승인 add shopping_shorts/store.py shopping_shorts/tests/test_member_mgmt.py
git -C .tracks/회원승인 commit -m "feat(회원관리): add_payment/list_payments 결제이력 스토어"
```

---

### Task 4: approve_customer 재설계 — 결제+기간, 재승인=연장

**Files:**
- Modify: `shopping_shorts/store.py` — `approve_customer`(약 2459행 직전에 있음) 교체
- Test: `shopping_shorts/tests/test_member_mgmt.py`

**Interfaces:** `approve_customer(customer_id, period_days, amount, method, note=None)`:
- 최초 승인: approved_at=now(시작일), full_access_until=now+period_days*86400.
- 이미 승인됨(approved_at 있음): approved_at 유지, full_access_until을 **연장**(현재 만료일이 미래면 그 뒤로 +period_days, 지났으면 now+period_days).
- 매 호출 add_payment 1건.
- 반환: 갱신된 customer dict.

- [ ] **Step 1: 실패 테스트**

```python
def test_approve_sets_start_end_and_payment(tmp_path, monkeypatch):
    s = _setup(tmp_path, monkeypatch)
    cid = s.create_customer("pend", "pw12", approved=False)
    assert appmod.access_level(cid) == "pending"
    cust = s.approve_customer(cid, period_days=30, amount=30000, method="계좌이체")
    assert cust["approved_at"] is not None                 # 시작일
    assert cust["full_access_until"] > cust["approved_at"] # 마감일 미래
    assert appmod.access_level(cid) == "full"
    assert len(s.list_payments(cid)) == 1


def test_reapprove_extends_and_keeps_start(tmp_path, monkeypatch):
    s = _setup(tmp_path, monkeypatch)
    cid = s.create_customer("pend2", "pw12", approved=False)
    c1 = s.approve_customer(cid, period_days=30, amount=30000, method="계좌")
    start1, end1 = c1["approved_at"], c1["full_access_until"]
    c2 = s.approve_customer(cid, period_days=30, amount=30000, method="계좌")
    assert c2["approved_at"] == start1          # 시작일 불변
    assert c2["full_access_until"] >= end1 + 30 * 86400 - 5   # 만료일 뒤로 연장(±오차)
    assert len(s.list_payments(cid)) == 2       # 결제 2건 누적
```

- [ ] **Step 2: 실패 확인**

Run: `py -m pytest shopping_shorts/tests/test_member_mgmt.py::test_approve_sets_start_end_and_payment shopping_shorts/tests/test_member_mgmt.py::test_reapprove_extends_and_keeps_start -q`
Expected: FAIL (approve_customer 시그니처 다름)

- [ ] **Step 3: 구현** — 기존 `approve_customer` 전체를 교체

```python
    def approve_customer(self, customer_id, period_days, amount, method, note=None):
        """승인(최초)=서비스 시작, 재호출=연장. 매번 결제 1건 기록.
        - 최초(approved_at NULL): approved_at=now, full_access_until=now+period.
        - 재호출: approved_at 유지, full_access_until을 현재 만료(미래면 그 뒤, 지났으면 now)에서 +period."""
        now_ts = int(datetime.now(timezone.utc).timestamp())
        try:
            days = max(0, int(period_days))
        except (TypeError, ValueError):
            days = 0
        add = days * 86400
        with self._conn() as c:
            row = c.execute("SELECT approved_at, full_access_until FROM customers WHERE id=?",
                            (customer_id,)).fetchone()
            if row is None:
                return None
            approved_at, fau = row[0], row[1] or 0
            base = fau if fau > now_ts else now_ts          # 연장 기준: 미래 만료면 그 뒤, 아니면 now
            new_until = base + add
            if approved_at is None:
                c.execute("UPDATE customers SET approved_at=?, full_access_until=? WHERE id=?",
                          (now_ts, now_ts + add, customer_id))
            else:
                c.execute("UPDATE customers SET full_access_until=? WHERE id=?",
                          (new_until, customer_id))
        self.add_payment(customer_id, amount, method, days, paid_at=now_ts, note=note)
        return self.get_customer(customer_id)
```

- [ ] **Step 4: 통과 확인**

Run: `py -m pytest shopping_shorts/tests/test_member_mgmt.py -q`
Expected: PASS

- [ ] **Step 5: 기존 테스트 갱신** — 기존 approve_customer 시그니처를 쓰던 테스트 수정

`shopping_shorts/tests/test_paywall_admin.py`의 `test_approve_customer_starts_trial_idempotent`(약 63행)는 옛 시그니처(`trial_days=7`)·멱등 가정이다. 새 의미로 교체:
```python
def test_approve_customer_sets_service_and_records_payment(tmp_path, monkeypatch):
    s = _setup(tmp_path, monkeypatch)
    cid = s.create_customer("pend", "pw12", approved=False)
    assert appmod.access_level(cid) == "pending"
    cust = s.approve_customer(cid, period_days=30, amount=30000, method="계좌이체")
    assert cust["approved_at"] is not None
    assert cust["full_access_until"] > 0
    assert appmod.access_level(cid) == "full"
    assert len(s.list_payments(cid)) == 1
```
(이름이 바뀌므로 기존 함수 def 줄부터 통째로 교체.)

- [ ] **Step 6: 통과 확인(회귀 포함)**

Run: `py -m pytest shopping_shorts/tests/test_member_mgmt.py shopping_shorts/tests/test_paywall_admin.py -q`
Expected: PASS (admin API 테스트는 Task5에서 손봄 — 지금 깨지면 Task5 대상. 여기선 store 레벨만 green이면 OK)

- [ ] **Step 7: 커밋**

```bash
git -C .tracks/회원승인 add shopping_shorts/store.py shopping_shorts/tests/test_member_mgmt.py shopping_shorts/tests/test_paywall_admin.py
git -C .tracks/회원승인 commit -m "feat(회원관리): approve_customer=결제+기간, 재승인=연장(자동체험 제거)"
```

---

### Task 5: API — approve 본문확장 + payment/payments 신규 + signup name·phone + list_customers

**Files:**
- Modify: `shopping_shorts/app.py` — `_admin_approve`(약 3860행), `_api_signup`(약 3533행), `list_customers` 호출부(`_admin_customers`)
- Modify: `shopping_shorts/store.py` — `list_customers`에 name·phone·최근결제 추가
- Test: `shopping_shorts/tests/test_member_mgmt.py`

**Interfaces:**
- `POST /api/admin/approve` 본문 `{customer_id, period_days, amount, method, note?}` → 승인+결제. period_days·amount 필수(없으면 422).
- `POST /api/admin/payment` 본문 동일 → 이미 승인된 고객 연장+결제(내부적으로 approve_customer 재호출).
- `GET /api/admin/payments?customer_id=` → `{payments:[...]}`.
- `POST /api/signup` 폼필드 name·phone 수용.
- `list_customers()` 각 dict에 `name, phone, approved_at, full_access_until, last_payment(None|{amount,paid_at}), payment_count`.

- [ ] **Step 1: 실패 테스트**

```python
def test_admin_approve_with_payment(tmp_path, monkeypatch):
    s = _setup(tmp_path, monkeypatch)
    cid = s.create_customer("pend", "pw12", approved=False)
    owner = TestClient(appmod.app, cookies={"dash_auth": _cookie(0)})
    r = owner.post("/api/admin/approve",
                   json={"customer_id": cid, "period_days": 30, "amount": 30000, "method": "계좌"})
    assert r.status_code == 200 and r.json()["ok"] is True
    assert appmod.access_level(cid) == "full"
    assert len(s.list_payments(cid)) == 1


def test_admin_payment_extends(tmp_path, monkeypatch):
    s = _setup(tmp_path, monkeypatch)
    cid = s.create_customer("u", "pw12", approved=False)
    owner = TestClient(appmod.app, cookies={"dash_auth": _cookie(0)})
    owner.post("/api/admin/approve", json={"customer_id": cid, "period_days": 30, "amount": 30000, "method": "계좌"})
    end1 = s.get_customer(cid)["full_access_until"]
    r = owner.post("/api/admin/payment", json={"customer_id": cid, "period_days": 30, "amount": 30000, "method": "카드"})
    assert r.status_code == 200
    assert s.get_customer(cid)["full_access_until"] > end1
    assert len(s.list_payments(cid)) == 2


def test_admin_payments_list(tmp_path, monkeypatch):
    s = _setup(tmp_path, monkeypatch)
    cid = s.create_customer("u", "pw12", approved=False)
    owner = TestClient(appmod.app, cookies={"dash_auth": _cookie(0)})
    owner.post("/api/admin/approve", json={"customer_id": cid, "period_days": 30, "amount": 30000, "method": "계좌"})
    r = owner.get(f"/api/admin/payments?customer_id={cid}")
    assert r.status_code == 200 and len(r.json()["payments"]) == 1


def test_admin_customers_has_name_phone_and_last_payment(tmp_path, monkeypatch):
    s = _setup(tmp_path, monkeypatch)
    cid = s.create_customer("u", "pw12", approved=False, name="김철수", phone="010-0000-0000")
    owner = TestClient(appmod.app, cookies={"dash_auth": _cookie(0)})
    owner.post("/api/admin/approve", json={"customer_id": cid, "period_days": 30, "amount": 30000, "method": "계좌"})
    row = [c for c in owner.get("/api/admin/customers").json()["customers"] if c["id"] == cid][0]
    assert row["name"] == "김철수" and row["phone"] == "010-0000-0000"
    assert row["last_payment"]["amount"] == 30000 and row["payment_count"] == 1


def test_signup_stores_name_phone(tmp_path, monkeypatch):
    s = _setup(tmp_path, monkeypatch)
    c = TestClient(appmod.app, follow_redirects=False)
    c.post("/api/signup", data={"user": "newby", "pass": "pw12345", "name": "이영희", "phone": "010-1111-2222"})
    with s._conn() as conn:
        row = conn.execute("SELECT name, phone FROM customers WHERE username='newby'").fetchone()
    assert row is not None and row[0] == "이영희" and row[1] == "010-1111-2222"
```

- [ ] **Step 2: 실패 확인**

Run: `py -m pytest shopping_shorts/tests/test_member_mgmt.py -q`
Expected: 위 5개 FAIL

- [ ] **Step 3a: store.list_customers 확장**

`shopping_shorts/store.py`의 `list_customers`(약 2457행) 교체:
```python
    def list_customers(self):
        """관리자용 전체 고객 목록(사장님 cid0 제외). 최근 가입 먼저. 최근 결제 요약 포함."""
        with self._conn() as c:
            rows = c.execute(
                "SELECT id, username, email, plan, full_access_until, created_at, approved_at, name, phone "
                "FROM customers WHERE id != 0 ORDER BY id DESC"
            ).fetchall()
            out = []
            for r in rows:
                cid = r[0]
                p = c.execute("SELECT amount, paid_at FROM payments WHERE customer_id=? "
                              "ORDER BY paid_at DESC, id DESC LIMIT 1", (cid,)).fetchone()
                cnt = c.execute("SELECT COUNT(*) FROM payments WHERE customer_id=?", (cid,)).fetchone()[0]
                out.append({"id": cid, "username": r[1], "email": r[2], "plan": r[3] or "free",
                            "full_access_until": r[4] or 0, "created_at": r[5], "approved_at": r[6],
                            "name": r[7], "phone": r[8],
                            "last_payment": ({"amount": p[0], "paid_at": p[1]} if p else None),
                            "payment_count": cnt})
        return out
```

- [ ] **Step 3b: _admin_approve 본문확장** — `app.py`의 `_admin_approve` 교체

```python
@app.post("/api/admin/approve")
async def _admin_approve(request: Request):
    denied = _require_admin(request)
    if denied:
        return denied
    body = await request.json()
    try:
        cid = int(body.get("customer_id"))
        period_days = int(body.get("period_days"))
        amount = int(body.get("amount"))
    except (TypeError, ValueError):
        return JSONResponse({"error": "customer_id·period_days·amount 필요"}, status_code=422)
    if period_days < 0 or amount < 0:
        return JSONResponse({"error": "음수 불가"}, status_code=422)
    method = (body.get("method") or "").strip()
    note = body.get("note")
    st = Store(DB_PATH)
    cust = st.approve_customer(cid, period_days, amount, method, note=note)
    if cust is None:
        return JSONResponse({"error": "없는 고객"}, status_code=404)
    import sys as _s
    print(f"[admin] approve cid={cid} +{period_days}d {amount}원", file=_s.stderr)
    return {"ok": True, "customer": cust}


@app.post("/api/admin/payment")
async def _admin_payment(request: Request):
    """이미 승인된 고객에 결제 추가+기간 연장(approve_customer 재호출과 동일 경로)."""
    denied = _require_admin(request)
    if denied:
        return denied
    body = await request.json()
    try:
        cid = int(body.get("customer_id"))
        period_days = int(body.get("period_days"))
        amount = int(body.get("amount"))
    except (TypeError, ValueError):
        return JSONResponse({"error": "customer_id·period_days·amount 필요"}, status_code=422)
    if period_days < 0 or amount < 0:
        return JSONResponse({"error": "음수 불가"}, status_code=422)
    method = (body.get("method") or "").strip()
    note = body.get("note")
    st = Store(DB_PATH)
    cust = st.approve_customer(cid, period_days, amount, method, note=note)
    if cust is None:
        return JSONResponse({"error": "없는 고객"}, status_code=404)
    return {"ok": True, "customer": cust}


@app.get("/api/admin/payments")
def _admin_payments(request: Request, customer_id: int = 0):
    denied = _require_admin(request)
    if denied:
        return denied
    return {"payments": Store(DB_PATH).list_payments(customer_id)}
```

- [ ] **Step 3c: _api_signup에 name·phone** — `app.py`의 `_api_signup`(약 3533행) 확인 후, 폼에서 name·phone을 읽어 create_customer로 전달

`_api_signup`을 읽어 `form.get("user")`·`form.get("pass")` 패턴을 파악하고, 같은 방식으로 `name = (form.get("name") or "").strip()`, `phone = (form.get("phone") or "").strip()` 추가 → `create_customer(..., name=name or None, phone=phone or None)`. (가입은 approved=False 유지 — 기존 로직 보존.)

- [ ] **Step 3d: _SIGNUP_HTML에 입력칸** — `app.py`의 `_SIGNUP_HTML`(약 3448행) 폼에 추가

`<input name=user ...>` 아래에:
```html
<input name=name placeholder=이름 autocomplete=name>
<input name=phone placeholder="전화번호(010-0000-0000)" autocomplete=tel>
```

- [ ] **Step 4: 통과 확인**

Run: `py -m pytest shopping_shorts/tests/test_member_mgmt.py shopping_shorts/tests/test_paywall_admin.py -q`
Expected: PASS. (기존 admin approve 테스트 `test_api_admin_approve_requires_admin`이 옛 본문으로 호출하면 422가 날 수 있음 → 그 테스트의 owner.post에 `json={"customer_id": cid, "period_days":30, "amount":0, "method":"무료"}` 로 본문 채워 갱신.)

- [ ] **Step 5: 커밋**

```bash
git -C .tracks/회원승인 add shopping_shorts/app.py shopping_shorts/store.py shopping_shorts/tests/test_member_mgmt.py shopping_shorts/tests/test_paywall_admin.py
git -C .tracks/회원승인 commit -m "feat(회원관리): approve 결제본문+/api/admin/payment·payments+signup name·phone+list_customers 확장"
```

---

### Task 6: admin.html — 이름·전화·기간 표시 + 승인/결제 입력 폼 + 결제이력 펼침

**Files:**
- Modify: `shopping_shorts/static/admin.html`
- Test: (정적파일 — pytest 미커버. 렌더 그라운딩으로 검증)

**Interfaces:** 대기명단에 이름·전화, [승인]→입력폼(금액·수단·이용일수)→`POST /api/admin/approve`. 고객목록에 이름·전화·시작일·마감일·최근결제(N건, 클릭시 펼침)·[결제추가]→`POST /api/admin/payment`.

- [ ] **Step 1: 대기명단 표 헤더·행에 이름·전화 추가**

현재 `<thead>` (약 admin.html의 대기명단 table): `<th>ID</th><th>이메일/아이디</th><th>가입일</th><th>승인</th>` →
`<th>ID</th><th>이름</th><th>전화</th><th>이메일/아이디</th><th>가입일</th><th>승인</th>`.
`prows` 렌더 map의 각 행에 `<td>${esc(c.name)}</td><td>${esc(c.phone)}</td>`를 이메일 셀 앞에 삽입. colspan(빈 상태)도 4→6으로.

- [ ] **Step 2: [승인] 버튼이 입력폼을 열도록**

`approveCustomer(cid)`를 교체 — prompt 3개(간단·확실)로 금액·수단·이용일수를 받아 approve 호출:
```javascript
async function approveCustomer(cid){
  const days = prompt('이용일수 (예: 30)', '');
  if(days===null) return;
  const amount = prompt('결제 금액(원)', '');
  if(amount===null) return;
  const method = prompt('결제 수단 (계좌/카드/현금/카톡 등)', '계좌이체') || '';
  const r = await fetch('/api/admin/approve',{method:'POST',headers:{'Content-Type':'application/json'},
    body:JSON.stringify({customer_id:cid, period_days:Number(days)||0, amount:Number(amount)||0, method})});
  const m=document.getElementById('pMsg');
  if(r.ok){m.textContent='✅ 승인됨';m.className='msg';load();}else{m.textContent='❌ 실패';m.className='msg err';}
  setTimeout(()=>m.textContent='',2000);
}
```

- [ ] **Step 3: 고객목록 헤더·행 확장**

고객 table `<thead>`에 이름·전화·시작일·마감일·결제 열 추가:
`<th>ID</th><th>이름</th><th>전화</th><th>이메일/아이디</th><th>등급</th><th>시작일</th><th>마감일</th><th>결제</th><th>오늘 사용(렌즈/렌더/대본)</th><th>조작</th>`.
`rows` 렌더 map 각 행:
- 이름·전화 셀 추가.
- 시작일 = `c.approved_at? new Date(c.approved_at*1000).toLocaleDateString('ko-KR'):'-'`
- 마감일 = `c.full_access_until? new Date(c.full_access_until*1000).toLocaleDateString('ko-KR'):'-'`
- 결제 셀 = `c.last_payment? \`<span class=paylink data-cid=${c.id} style="cursor:pointer;color:#8ab4f8">${c.last_payment.amount.toLocaleString()}원 · ${c.payment_count}건</span>\` : '-'`
- 조작 셀에 `<button class=bpay onclick="addPayment(${c.id})">결제추가</button>`를 pro승격/무료로 앞에 추가.

- [ ] **Step 4: 결제추가 핸들러 + 결제이력 펼침**

```javascript
async function addPayment(cid){
  const days = prompt('연장 이용일수 (예: 30)', '');
  if(days===null) return;
  const amount = prompt('결제 금액(원)', '');
  if(amount===null) return;
  const method = prompt('결제 수단', '계좌이체') || '';
  const r = await fetch('/api/admin/payment',{method:'POST',headers:{'Content-Type':'application/json'},
    body:JSON.stringify({customer_id:cid, period_days:Number(days)||0, amount:Number(amount)||0, method})});
  const m=document.getElementById('cMsg');
  if(r.ok){m.textContent='✅ 결제 추가됨';m.className='msg';load();}else{m.textContent='❌ 실패';m.className='msg err';}
  setTimeout(()=>m.textContent='',2000);
}
// 결제 금액 클릭 → 그 고객 결제이력 alert(간단). load() 뒤 이벤트 위임으로 붙인다.
document.addEventListener('click', async (e)=>{
  const el = e.target.closest('.paylink'); if(!el) return;
  const cid = el.dataset.cid;
  const d = await (await fetch('/api/admin/payments?customer_id='+cid)).json();
  const lines = (d.payments||[]).map(p=>{
    const dt = new Date(p.paid_at*1000).toLocaleDateString('ko-KR');
    return `${dt}  ${Number(p.amount).toLocaleString()}원  ${p.method||''}  ${p.period_days}일`;
  }).join('\n') || '결제 내역 없음';
  alert('결제 이력\n\n'+lines);
});
```
(alert는 dialog라 CLAUDE.md의 브라우저 자동화 주의사항 대상이지만, 이건 정적 admin 페이지의 사장님 수동조작이라 허용. 다만 렌더 그라운딩 시 alert를 트리거하지 말 것 — 존재만 확인.)

`.bpay` 스타일을 `<style>`에 추가: `.bpay{background:#3a5bd0;color:#fff}`.

- [ ] **Step 5: 렌더 그라운딩(수동)**

스크래치패드로 admin.html 복사 → fetch 목킹(customers에 name·phone·approved_at·full_access_until·last_payment·payment_count 포함, /api/admin/payments 응답) → `py -m http.server`로 서빙 → Playwright로 열어 스냅샷:
- 대기명단에 이름·전화 열 보이는지
- 고객목록에 이름·전화·시작일·마감일·결제(N건) 보이는지
- 결제 링크 클릭 시 payments fetch 되는지(alert는 트리거 회피 — 콘솔로 확인하거나 alert를 console.log로 잠깐 바꿔 확인)

> ⚠️ 렌더 확인용 목킹 파일은 스크래치패드에만. admin.html 원본엔 목킹 넣지 말 것.

- [ ] **Step 6: 커밋**

```bash
git -C .tracks/회원승인 add shopping_shorts/static/admin.html
git -C .tracks/회원승인 commit -m "feat(회원관리): admin 이름·전화·시작·마감·결제 표시 + 승인/결제추가 입력폼 + 결제이력 펼침"
```

---

## 최종 검증 (완료 정의)

- [ ] 전체 회원관리 테스트 green: `py -m pytest shopping_shorts/tests/test_member_mgmt.py -q`
- [ ] 페이월 회귀 없음: `py -m pytest shopping_shorts/tests/test_paywall_*.py -q` (실패가 새로 늘지 않음)
- [ ] admin 렌더 그라운딩 완료(Task6 Step5)
- [ ] `py tools/track.py finish 회원승인` → 게이트 통과 시 main 병합 → 3분 뒤 라이브
- [ ] (사장님) 실계정 최종 확인: 가입 시 이름·전화 입력 → admin 대기명단 표시 → [승인] 금액·일수 입력 → 계정 열림·시작·마감 표기 → [결제추가] 연장

## Self-Review (계획↔스펙 대조)

- 스펙 A(가입폼 이름·전화): Task2(store)·Task5(signup API·HTML). ✅
- 스펙 B(결제이력 수동·누적·승인시함께): Task3(payments)·Task4(approve=결제)·Task5(payment API). ✅
- 스펙 C(시작·마감일): Task4(approved_at/full_access_until)·Task5(list_customers)·Task6(표시). ✅
- 자동체험 제거: Task4에서 trial_days 삭제·period_days 필수. ✅
- 시그니처 일관성: approve_customer(cid, period_days, amount, method, note=None) — Task4 정의, Task5 API에서 동일 호출. add_payment(cid, amount, method, period_days, paid_at, note) — Task3 정의, Task4에서 동일. ✅
- 기존 테스트 파손 처리: Task4 Step5(store 테스트), Task5 Step4(admin API 테스트) 명시. ✅
- 플레이스홀더: Step3c(_api_signup)만 "읽어서 패턴 따라" — 폼 파싱 방식이 파일마다 달라 실코드 대신 지시. 나머지 실코드. 의도적.
