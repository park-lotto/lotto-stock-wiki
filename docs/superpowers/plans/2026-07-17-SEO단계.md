# 영상 제작소 6단계 SEO Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 제작소 6단계에서 확정 대본으로 업로드용 메타데이터(제목·설명·태그·해시태그·후킹·플랫폼별 CTA)를 생성하되, 뽑은 키워드를 유튜브에서 실제로 측정해 근거와 함께 보여준다.

**Architecture:** 생성(Gemini, `seo_generate.py`) → 측정(YouTube Data API, `seo_probe.py`) → 되먹임(부분 재생성). 산출물은 `mix_jobs.seo_json` 한 칸에, 키워드 측정치는 전역 캐시 `seo_keyword_stats`에 저장. `youtube_client.py`는 발굴이 쓰는 코드라 **읽기만 하고 수정하지 않는다.**

**Tech Stack:** FastAPI(`app.py` 단일 파일, 라우터 분리 없음) · SQLite(`store.py`) · google.genai(Gemini `gemini-3.1-flash-lite`) · `pipeline.atoms.key_vault` 공유 키풀 · 바닐라 JS(`produce.html`) · pytest

**설계 문서:** `docs/superpowers/specs/2026-07-17-SEO단계-design.md`

---

## Global Constraints

- **트랙 폴더에서만 작업한다**: `C:\Users\TheRose\Desktop\로또의 주식\.tracks\SEO`. main 폴더의 코드 파일은 단 하나도 건드리지 않는다.
- **`youtube_client.py`는 수정 금지** — 발굴 파이프라인(`service.py:14`)이 쓰는 코드. 순수 헬퍼(`_LANG_REGION`, `_title_lang_ok`, `_stats`)만 import해 재사용한다.
- **키풀은 `key_vault` 공유 캐스케이드**(`_GEN_GROUP = "general"`). `comment_gen` 전용키는 1개뿐이라 쉽게 소진되므로 쓰지 않는다(`script_generate.py:20-25` 주석의 이유 그대로).
- **모델은 `comment_gen._MODEL`** (= `gemini-3.1-flash-lite`). 하드코딩하지 말고 import한다.
- **Gemini 출력은 `response_schema`로 JSON 강제** (`script_generate.py:37` 관례).
- **무과금** — 포인트 차감·환불 없음(`fx/suggest` 방식).
- **쿼터 상한 `_MAX_PROBE = 6`** — `search.list`가 호출당 100유닛이고 발굴이 같은 `YOUTUBE_API_KEYS` 풀을 공유한다.
- **캐시 TTL 7일** — `seo_keyword_stats`.
- **판정 문턱값**(모듈 상수, 실측 후 튜닝 대상 — 이 숫자가 맞다고 주장하지 않는다):
  `_VIEWS_FLOOR = 100_000` · `_SMALL_RATIO_FLOOR = 0.3` · `_SMALL_SUBS = 10_000` · `_WINDOW_DAYS = 90` · `_SAMPLE_MAX = 20` · `_SAMPLE_MIN = 3`
- **플랫폼은 유튜브 쇼츠 / 틱톡 / 쓰레드 3개.** 인스타는 범위 밖.
- **⚠️ `produce.html` 앵커 문자열을 건드리지 않는다** — `tests/test_produce_preview_gate.py:26-34`가 실소스를 앵커로 잘라 Node로 실행한다. 보호 대상: `"function jump(i){"` / `"async function startProduceMix(){"` / `"async function loadMixReview(){"` / `"// ── 1단계 대본: 3모드"` / `"async function pollMix(){"` / `"// ── 3단계 자막제거"`, 그리고 `const STEPS = [...]` 배열(하네스 `:63`이 복제한다).
- **테스트 실행은 `-s` 필수**: `python -m pytest ... -s`. 안 주면 윈도우 pytest 캡처 아티팩트로 유령 실패가 뜬다(썸네일 트랙 실측: 44 failed → -s 주면 12 passed).
- **기준선**: 병합 전보다 실패가 늘지 않으면 통과. 선존재 실패 약 10~11건은 이미 깨져 있다.

### ⚠️ 착수 시 반드시 다시 확인할 것 (숫자가 이미 움직였다)

**`store.py`의 row 인덱스를 계획서 숫자로 믿지 마라.** 이 계획을 쓰는 시점(2026-07-17, `59f826dc`)에
SELECT 컬럼은 25개(`row[0]`=job_id … `row[24]`=preview_error)라 `seo_json`은 `row[25]`다. 그러나:

- **썸네일 트랙이 `thumbnail`을 똑같이 `row[25]`에 넣으려고 대기 중이다.** 먼저 병합하는 쪽이 25를 가져가고 나중 쪽은 26으로 밀린다.
- 전례: 모션효과가 `fx_plan`을 row19-21에 끼워 `preview_*`가 22-24로 밀렸다.
- `store.py`·`produce.html`을 **트랙 3개(썸네일·대본믹스통합·장면라이브러리)가 동시 편집 중**이다.

→ **Task 1 시작 전과, 이후 각 태스크 시작 전에 `git fetch origin && git merge origin/main`을 하고,
`get_mix_job`의 SELECT를 직접 세어 인덱스를 확정한다.** 새 컬럼은 **반드시 SELECT 끝에 추가**한다.

---

## File Structure

| 파일 | 책임 | 태스크 |
|---|---|---|
| `shopping_shorts/store.py` (수정) | `mix_jobs.seo_json` 왕복 + `seo_keyword_stats` 캐시 | T1, T2 |
| `shopping_shorts/seo_probe.py` (신설) | 유튜브 키워드 측정 + 판정. 읽기 전용, 캐시 경유 | T3, T4 |
| `shopping_shorts/seo_generate.py` (신설) | Gemini 프롬프트·스키마·캐스케이드 호출 | T5 |
| `shopping_shorts/app.py` (수정) | `/api/produce/seo/generate` + `mix/settings`에 `seo` | T6 |
| `shopping_shorts/static/produce.html` (수정) | 6단계 패널 UI + `initSeo()` | T7 |
| `tests/shopping_shorts/test_seo_*.py` (신설) | 위 각각의 테스트 | 각 태스크 |

**테스트 위치 주의**: 루트 `tests/`는 **위키·파이프라인(atoms)용 선존재 폴더**다. 쇼핑쇼츠 테스트는
`shopping_shorts/tests/`에 있다(`test_produce_preview_gate.py`가 거기 있다). 새 테스트는
**`shopping_shorts/tests/`에 넣는다** — 썸네일 트랙이 루트 `tests/`에 넣었다가 `no tests ran`으로 회수한 전례가 있다.

---

## Task 1: `mix_jobs.seo_json` 왕복

**Files:**
- Modify: `shopping_shorts/store.py` — 마이그레이션(`:396-403` 부근 튜플 리스트 끝) / `get_mix_job` SELECT(`:1227-1233`) + 반환 dict(`:1237-1253`) / `update_mix_job` 화이트리스트(`:1255-1290`)
- Test: `shopping_shorts/tests/test_seo_store.py`

**Interfaces:**
- Produces: `Store.get_mix_job(job_id)["seo"]` → `dict | None` (JSON 파싱됨) / `Store.update_mix_job(job_id, seo={...})` → `None`

**왜 이 태스크가 따로인가:** `update_mix_job`은 화이트리스트 방식이라 한 곳이라도 빠뜨리면
**에러도 없이 조용히 무시된다.** 코드가 직접 경고해둔 함정이다(`store.py:1260-1262`).
이 왕복이 되기 전엔 뒷 태스크가 전부 허공에 저장한다.

- [x] **Step 1: 최신 main을 얹고 row 인덱스를 직접 센다**

```bash
cd "C:/Users/TheRose/Desktop/로또의 주식/.tracks/SEO"
git fetch origin && git merge origin/main
grep -n "def get_mix_job" -A 12 shopping_shorts/store.py
```

`SELECT` 컬럼을 손으로 세어 마지막 인덱스를 확인한다. 이 계획서 작성 시점엔 `row[24]`=`preview_error`였다.
**다르면 아래 코드의 `row[25]`를 실제 값으로 바꾼다.**

- [x] **Step 2: 실패하는 테스트를 쓴다**

`shopping_shorts/tests/test_seo_store.py`:

```python
import json
import pytest
from shopping_shorts.store import Store


@pytest.fixture
def store(tmp_path):
    s = Store(str(tmp_path / "t.db"))
    s.init_db()
    return s


def _job(store):
    job_id = "seojob01"
    store.create_mix_job(job_id, ["https://x/1"], 20, "free")
    return job_id


def test_seo_roundtrip(store):
    """seo dict를 저장하면 get_mix_job이 파싱된 dict로 돌려준다."""
    job_id = _job(store)
    seo = {"title": "테스트 제목", "tags": ["a", "b"],
           "keyword_stats": [{"keyword": "a", "verdict": "blue"}]}
    store.update_mix_job(job_id, seo=seo)
    got = store.get_mix_job(job_id)["seo"]
    assert got == seo


def test_seo_defaults_to_none(store):
    """저장 전엔 None(빈 dict가 아니다 — 미생성과 빈 생성을 구분해야 한다)."""
    job_id = _job(store)
    assert store.get_mix_job(job_id)["seo"] is None


def test_seo_clearable(store):
    """None을 주면 지워진다."""
    job_id = _job(store)
    store.update_mix_job(job_id, seo={"title": "x"})
    store.update_mix_job(job_id, seo=None)
    assert store.get_mix_job(job_id)["seo"] is None


def test_seo_does_not_disturb_neighbors(store):
    """seo 저장이 옆 컬럼을 밟지 않는다 — row 인덱스 밀림 사고 방지."""
    job_id = _job(store)
    store.update_mix_job(job_id, preview_status="ready", fx_status="done",
                         given_script=None)
    store.update_mix_job(job_id, seo={"title": "x"})
    got = store.get_mix_job(job_id)
    assert got["preview_status"] == "ready"
    assert got["fx_status"] == "done"
    assert got["seo"] == {"title": "x"}


def test_seo_korean_not_escaped(store):
    """ensure_ascii=False 관례 — DB에 한글이 그대로 들어간다."""
    job_id = _job(store)
    store.update_mix_job(job_id, seo={"title": "한글제목"})
    with store._conn() as c:
        raw = c.execute("SELECT seo_json FROM mix_jobs WHERE job_id=?", (job_id,)).fetchone()[0]
    assert "한글제목" in raw
```

⚠️ `create_mix_job`의 실제 시그니처를 먼저 확인한다:
`grep -n "def create_mix_job" -A 6 shopping_shorts/store.py` — 다르면 `_job()`을 맞춘다.

- [x] **Step 3: 실패를 확인한다**

Run: `python -m pytest shopping_shorts/tests/test_seo_store.py -v -s`
Expected: 5건 FAIL — `KeyError: 'seo'` 또는 `sqlite3.OperationalError: no such column: seo_json`

- [x] **Step 4: 마이그레이션에 컬럼을 추가한다**

`store.py` 마이그레이션 튜플 리스트의 **맨 끝**(`("preview_error", "TEXT"),` 다음):

```python
                ("preview_error", "TEXT"),
                # 6단계 SEO(2026-07-17) — 제목·설명·태그·해시태그·CTA + 키워드 실측치 일습.
                # 산출물이 한 덩어리로만 의미가 있어(제목만 바꿔도 태그·CTA와 어울려야 한다)
                # 필드를 쪼개지 않고 JSON 한 칸에 둔다.
                ("seo_json", "TEXT"),
```

- [x] **Step 5: `get_mix_job`에 읽기를 추가한다**

SELECT 문자열 **끝**(`"preview_status, preview_path, preview_error "` 다음 줄):

```python
                "preview_status, preview_path, preview_error, "
                "seo_json "
                "FROM mix_jobs WHERE job_id=?", (job_id,),
```

반환 dict **끝**:

```python
            "preview_status": row[22], "preview_path": row[23], "preview_error": row[24],
            "seo": json.loads(row[25]) if row[25] else None,
        }
```

⚠️ Step 1에서 센 인덱스가 24가 아니었으면 `row[25]`를 그에 맞춰 바꾼다.

- [x] **Step 6: `update_mix_job` 화이트리스트에 쓰기를 추가한다**

`if "voice" in fields:` 블록 **다음**(다른 JSON 필드들과 같은 자리):

```python
        if "seo" in fields:
            cols.append("seo_json=?")
            vals.append(json.dumps(fields["seo"], ensure_ascii=False) if fields["seo"] else None)
```

- [x] **Step 7: 통과를 확인한다**

Run: `python -m pytest shopping_shorts/tests/test_seo_store.py -v -s`
Expected: 5 passed

- [x] **Step 8: 뮤테이션으로 자물쇠를 확인한다**

Step 6에서 추가한 3줄을 **주석 처리**하고:

Run: `python -m pytest shopping_shorts/tests/test_seo_store.py -v -s`
Expected: `test_seo_roundtrip` · `test_seo_does_not_disturb_neighbors` · `test_seo_korean_not_escaped` **FAIL**
(= 화이트리스트가 진짜 자물쇠임을 증명. **조용히 통과하면 테스트가 가짜다**)

원복하고 다시 5 passed 확인.

- [x] **Step 9: 회귀를 확인한다**

Run: `python -m pytest shopping_shorts/tests/test_store.py shopping_shorts/tests/test_app.py -q -s`
Expected: 병합 전과 실패 수가 같다 (row 인덱스를 안 밟았다는 증거)

- [x] **Step 10: 커밋**

```bash
git add shopping_shorts/store.py shopping_shorts/tests/test_seo_store.py
git commit -m "feat(seo): mix_jobs.seo_json 왕복 — 마이그레이션·읽기·화이트리스트 3곳"
```

---

## Task 2: `seo_keyword_stats` 캐시 테이블

**Files:**
- Modify: `shopping_shorts/store.py` — `init_db` 안 `CREATE TABLE` 구역 + 새 메서드 2개
- Test: `shopping_shorts/tests/test_seo_store.py` (Task 1 파일에 추가)

**Interfaces:**
- Consumes: 없음
- Produces:
  - `Store.get_keyword_stats(keyword, region="KR", ttl_days=7)` → `dict | None` (TTL 지났으면 `None`)
  - `Store.put_keyword_stats(stat)` → `None`. `stat` = `{"keyword": str, "region": str, "views_median": int, "small_ratio": float, "sample_n": int, "top_titles": [str], "verdict": str}`

**왜 job이 아니라 전역인가:** 키워드 측정치는 어느 영상이 쟀든 같은 값이다. job에 매면
다른 영상이 같은 키워드를 다시 100유닛 주고 산다. 발굴과 쿼터를 나눠 쓰는 상황에서 이건 낭비다.

- [x] **Step 1: 실패하는 테스트를 쓴다**

`shopping_shorts/tests/test_seo_store.py` 끝에 추가:

```python
from datetime import datetime, timedelta, timezone


_STAT = {"keyword": "빨대텀블러", "region": "KR", "views_median": 320000,
         "small_ratio": 0.4, "sample_n": 20,
         "top_titles": ["a", "b", "c"], "verdict": "blue"}


def test_keyword_stats_roundtrip(store):
    store.put_keyword_stats(_STAT)
    got = store.get_keyword_stats("빨대텀블러")
    assert got["views_median"] == 320000
    assert got["small_ratio"] == 0.4
    assert got["top_titles"] == ["a", "b", "c"]
    assert got["verdict"] == "blue"


def test_keyword_stats_miss_returns_none(store):
    assert store.get_keyword_stats("없는키워드") is None


def test_keyword_stats_upsert(store):
    """같은 키워드를 다시 재면 덮어쓴다(행이 늘지 않는다)."""
    store.put_keyword_stats(_STAT)
    store.put_keyword_stats({**_STAT, "views_median": 999})
    assert store.get_keyword_stats("빨대텀블러")["views_median"] == 999
    with store._conn() as c:
        n = c.execute("SELECT COUNT(*) FROM seo_keyword_stats").fetchone()[0]
    assert n == 1


def test_keyword_stats_ttl_expired(store):
    """TTL 지난 건 없는 것으로 친다 — 낡은 측정치로 근거를 만들면 안 된다."""
    store.put_keyword_stats(_STAT)
    old = (datetime.now(timezone.utc) - timedelta(days=8)).isoformat()
    with store._conn() as c:
        c.execute("UPDATE seo_keyword_stats SET checked_at=?", (old,))
    assert store.get_keyword_stats("빨대텀블러", ttl_days=7) is None


def test_keyword_stats_ttl_boundary_fresh(store):
    """6일된 건 아직 유효(경계 off-by-one 방지)."""
    store.put_keyword_stats(_STAT)
    recent = (datetime.now(timezone.utc) - timedelta(days=6)).isoformat()
    with store._conn() as c:
        c.execute("UPDATE seo_keyword_stats SET checked_at=?", (recent,))
    assert store.get_keyword_stats("빨대텀블러", ttl_days=7) is not None


def test_keyword_stats_region_separate(store):
    """같은 키워드라도 지역이 다르면 다른 행."""
    store.put_keyword_stats(_STAT)
    store.put_keyword_stats({**_STAT, "region": "US", "views_median": 111})
    assert store.get_keyword_stats("빨대텀블러", region="KR")["views_median"] == 320000
    assert store.get_keyword_stats("빨대텀블러", region="US")["views_median"] == 111
```

- [x] **Step 2: 실패를 확인한다**

Run: `python -m pytest shopping_shorts/tests/test_seo_store.py -v -s -k keyword_stats`
Expected: 6건 FAIL — `AttributeError: 'Store' object has no attribute 'put_keyword_stats'`

- [x] **Step 3: 테이블을 만든다**

`store.py`의 `init_db` 안, 다른 `CREATE TABLE` 옆:

```python
            c.execute("""CREATE TABLE IF NOT EXISTS seo_keyword_stats (
                keyword TEXT NOT NULL,
                region TEXT NOT NULL DEFAULT 'KR',
                views_median INTEGER,
                small_ratio REAL,
                sample_n INTEGER,
                top_titles_json TEXT,
                verdict TEXT,
                checked_at TEXT NOT NULL,
                PRIMARY KEY (keyword, region)
            )""")
```

- [x] **Step 4: 메서드 2개를 만든다**

`store.py`에 추가 (`# ── 6단계 SEO 키워드 측정 캐시(2026-07-17) ──` 구역):

```python
    # ── 6단계 SEO 키워드 측정 캐시(2026-07-17) ──
    # job이 아니라 전역이다 — 키워드 측정치는 어느 영상이 쟀든 같은 값이고,
    # search.list가 100유닛인데 발굴과 키풀을 나눠 쓰므로 재측정은 순손실이다.
    def put_keyword_stats(self, stat):
        """키워드 측정치 upsert(keyword+region 충돌 시 덮어씀)."""
        with self._conn() as c:
            c.execute(
                "INSERT OR REPLACE INTO seo_keyword_stats "
                "(keyword, region, views_median, small_ratio, sample_n, "
                " top_titles_json, verdict, checked_at) VALUES (?,?,?,?,?,?,?,?)",
                (stat["keyword"], stat.get("region") or "KR",
                 stat.get("views_median"), stat.get("small_ratio"), stat.get("sample_n"),
                 json.dumps(stat.get("top_titles") or [], ensure_ascii=False),
                 stat.get("verdict"),
                 datetime.now(timezone.utc).isoformat()),
            )

    def get_keyword_stats(self, keyword, region="KR", ttl_days=7):
        """TTL 안이면 측정치 dict, 없거나 낡았으면 None.
        낡은 측정치로 근거를 만들면 안 되므로 만료는 '없음'과 같이 다룬다."""
        with self._conn() as c:
            row = c.execute(
                "SELECT views_median, small_ratio, sample_n, top_titles_json, "
                "verdict, checked_at FROM seo_keyword_stats "
                "WHERE keyword=? AND region=?", (keyword, region),
            ).fetchone()
        if not row:
            return None
        try:
            checked = datetime.fromisoformat(row[5])
        except (TypeError, ValueError):
            return None
        if datetime.now(timezone.utc) - checked > timedelta(days=ttl_days):
            return None
        return {
            "keyword": keyword, "region": region,
            "views_median": row[0], "small_ratio": row[1], "sample_n": row[2],
            "top_titles": json.loads(row[3]) if row[3] else [],
            "verdict": row[4], "checked_at": row[5],
        }
```

⚠️ `store.py` 상단 import에 `timedelta`가 있는지 확인한다:
`grep -n "^from datetime import" shopping_shorts/store.py` → 없으면 추가한다.

- [x] **Step 5: 통과를 확인한다**

Run: `python -m pytest shopping_shorts/tests/test_seo_store.py -v -s`
Expected: 11 passed (Task 1의 5건 + 6건)

- [x] **Step 6: 뮤테이션으로 TTL 자물쇠를 확인한다**

`get_keyword_stats`의 TTL 검사 2줄을 주석 처리:

Run: `python -m pytest shopping_shorts/tests/test_seo_store.py -v -s -k ttl`
Expected: `test_keyword_stats_ttl_expired` **FAIL** / `test_keyword_stats_ttl_boundary_fresh` PASS

원복하고 11 passed 확인.

- [x] **Step 7: 커밋**

```bash
git add shopping_shorts/store.py shopping_shorts/tests/test_seo_store.py
git commit -m "feat(seo): seo_keyword_stats 전역 캐시(TTL 7일) — 재측정 쿼터 낭비 차단"
```

---

## Task 3: `seo_probe` 판정 로직 (API 없이)

**Files:**
- Create: `shopping_shorts/seo_probe.py`
- Test: `shopping_shorts/tests/test_seo_probe.py`

**Interfaces:**
- Consumes: 없음 (순수 함수)
- Produces:
  - 상수: `_VIEWS_FLOOR=100_000` · `_SMALL_RATIO_FLOOR=0.3` · `_SMALL_SUBS=10_000` · `_WINDOW_DAYS=90` · `_SAMPLE_MAX=20` · `_SAMPLE_MIN=3` · `_MAX_PROBE=6`
  - `judge(views_median, small_ratio, sample_n)` → `"blue" | "red" | "dead" | "unknown"`
  - `summarize(items)` → `{"views_median": int, "small_ratio": float, "sample_n": int, "top_titles": [str], "verdict": str}`
    - `items` = `[{"title": str, "views": int, "subs": int}, ...]`

**왜 API를 분리하나:** 판정 로직은 순수 함수라 네트워크 없이 전 분기를 정확히 테스트할 수 있다.
API 배선(Task 4)과 섞으면 둘 다 대충 테스트된다.

- [x] **Step 1: 실패하는 테스트를 쓴다**

`shopping_shorts/tests/test_seo_probe.py`:

```python
from shopping_shorts import seo_probe


def _items(n, views, subs):
    return [{"title": f"t{i}", "views": views, "subs": subs} for i in range(n)]


def test_judge_blue():
    """수요 있고(조회수 높음) 작은 채널도 상위권 → 뚫린다."""
    assert seo_probe.judge(320_000, 0.4, 20) == "blue"


def test_judge_red():
    """수요는 있으나 대형 채널이 독식 → 레드오션."""
    assert seo_probe.judge(1_800_000, 0.05, 20) == "red"


def test_judge_dead():
    """조회수가 낮으면 소형채널 비율과 무관하게 수요 없음."""
    assert seo_probe.judge(3_000, 0.9, 20) == "dead"


def test_judge_unknown_small_sample():
    """샘플이 모자라면 판정하지 않는다 — 거짓 근거를 만들지 않는다."""
    assert seo_probe.judge(320_000, 0.4, 2) == "unknown"


def test_judge_boundary_views_floor():
    """문턱 정확히 = 통과(>=). off-by-one 방지."""
    assert seo_probe.judge(100_000, 0.4, 20) == "blue"
    assert seo_probe.judge(99_999, 0.4, 20) == "dead"


def test_judge_boundary_small_ratio():
    assert seo_probe.judge(320_000, 0.3, 20) == "blue"
    assert seo_probe.judge(320_000, 0.29, 20) == "red"


def test_summarize_median_and_ratio():
    items = [{"title": "a", "views": 100, "subs": 500},
             {"title": "b", "views": 300, "subs": 50_000},
             {"title": "c", "views": 200, "subs": 900}]
    got = seo_probe.summarize(items)
    assert got["views_median"] == 200          # 중앙값(평균 아님 — 이상치에 안 흔들리게)
    assert got["small_ratio"] == 2 / 3         # subs < 10,000 인 게 2개
    assert got["sample_n"] == 3


def test_summarize_top_titles_by_views():
    """top_titles는 조회수 상위 3개 — 사장님이 눈으로 검증할 실물."""
    items = [{"title": "low", "views": 1, "subs": 1},
             {"title": "high", "views": 999, "subs": 1},
             {"title": "mid", "views": 50, "subs": 1},
             {"title": "x", "views": 2, "subs": 1}]
    got = seo_probe.summarize(items)
    assert got["top_titles"] == ["high", "mid", "x"]


def test_summarize_empty_is_unknown():
    got = seo_probe.summarize([])
    assert got["verdict"] == "unknown"
    assert got["sample_n"] == 0


def test_summarize_missing_subs_counts_as_large():
    """구독자를 못 받아온 채널을 '작다'고 치면 블루오션이 과대평가된다 → 크다고 친다."""
    items = _items(5, 500_000, 0)
    for it in items:
        it.pop("subs")
    got = seo_probe.summarize(items)
    assert got["small_ratio"] == 0.0
    assert got["verdict"] == "red"
```

- [x] **Step 2: 실패를 확인한다**

Run: `python -m pytest shopping_shorts/tests/test_seo_probe.py -v -s`
Expected: 10건 FAIL — `ModuleNotFoundError: No module named 'shopping_shorts.seo_probe'`

- [x] **Step 3: 모듈을 만든다**

`shopping_shorts/seo_probe.py`:

```python
"""6단계 SEO — 키워드를 유튜브에서 실제로 재서 근거를 만든다(2026-07-17).

경쟁사 SEO 생성기는 대본만 보고 문구를 뱉는다. 우리는 뽑은 키워드를 실제로
검색해 '수요가 있나'와 '작은 채널도 뚫리나'를 재서 화면에 근거로 띄운다.

⚠️ youtube_client.py는 발굴 파이프라인이 쓰는 코드라 수정하지 않는다.
   순수 헬퍼만 재사용하고, 측정은 이 모듈이 따로 한다.
   (search_shorts()는 pageInfo.totalResults를 버리고 결과가 키워드별로 안 갈려 재사용 불가)
"""

# 판정 문턱 — 실측 후 튜닝 대상. 이 숫자가 맞다는 근거는 아직 없다(설계 §10 리스크 1).
_VIEWS_FLOOR = 100_000       # 상위 쇼츠 조회수 중앙값이 이 밑이면 '수요 없음'
_SMALL_RATIO_FLOOR = 0.3     # 상위권 중 소형채널이 이 비율 이상이면 '뚫린다'
_SMALL_SUBS = 10_000         # 이 미만이면 소형채널
_WINDOW_DAYS = 90            # 최근 N일 안에 올라온 영상만
_SAMPLE_MAX = 20             # 키워드당 검색 결과 수
_SAMPLE_MIN = 3              # 이 밑이면 판정하지 않는다
_MAX_PROBE = 6               # 생성 1회당 측정할 키워드 상한 (search.list = 100유닛/회)


def judge(views_median, small_ratio, sample_n):
    """측정치 → blue/red/dead/unknown.

    조회수 중앙값만으로는 '대형채널이 독식한 키워드'와 '작은 채널도 뚫리는
    키워드'가 구분되지 않는다. 우리한테 필요한 건 후자라 둘을 같이 본다.
    """
    if not sample_n or sample_n < _SAMPLE_MIN:
        return "unknown"          # 표본 부족 — 거짓 근거를 만들지 않는다
    if (views_median or 0) < _VIEWS_FLOOR:
        return "dead"             # 검색해도 사람이 안 본다
    if (small_ratio or 0) >= _SMALL_RATIO_FLOOR:
        return "blue"             # 수요 있고 작은 채널도 상위권
    return "red"                  # 수요는 있으나 대형채널 독식


def summarize(items):
    """[{title, views, subs}] → 측정치 dict.

    subs가 없는 항목은 '큰 채널'로 친다 — 못 받아온 걸 작다고 치면
    블루오션이 과대평가돼서 없는 기회를 있다고 보고하게 된다.
    """
    n = len(items)
    if not n:
        return {"views_median": 0, "small_ratio": 0.0, "sample_n": 0,
                "top_titles": [], "verdict": "unknown"}
    views = sorted(int(it.get("views") or 0) for it in items)
    mid = n // 2
    views_median = views[mid] if n % 2 else (views[mid - 1] + views[mid]) // 2
    small = sum(1 for it in items if 0 < int(it.get("subs") or 0) < _SMALL_SUBS)
    small_ratio = small / n
    top = sorted(items, key=lambda it: int(it.get("views") or 0), reverse=True)[:3]
    return {
        "views_median": views_median,
        "small_ratio": small_ratio,
        "sample_n": n,
        "top_titles": [it.get("title") or "" for it in top],
        "verdict": judge(views_median, small_ratio, n),
    }
```

- [x] **Step 4: 통과를 확인한다**

Run: `python -m pytest shopping_shorts/tests/test_seo_probe.py -v -s`
Expected: 10 passed

- [x] **Step 5: 뮤테이션 2종으로 자물쇠를 확인한다**

(a) `judge`의 `sample_n < _SAMPLE_MIN` 검사를 지운다 → `test_judge_unknown_small_sample` FAIL
(b) `summarize`의 `0 < int(...)` 를 `int(...)` 로 바꾼다(subs 없음을 소형으로 계산) → `test_summarize_missing_subs_counts_as_large` FAIL

둘 다 확인 후 원복, 10 passed.

- [x] **Step 6: 커밋**

```bash
git add shopping_shorts/seo_probe.py shopping_shorts/tests/test_seo_probe.py
git commit -m "feat(seo): 키워드 판정 로직 — 조회수 중앙값 + 소형채널 침투율"
```

---

## Task 4: `seo_probe` 유튜브 배선 + 캐시

**Files:**
- Modify: `shopping_shorts/seo_probe.py`
- Test: `shopping_shorts/tests/test_seo_probe.py` (추가)

**Interfaces:**
- Consumes: `judge`/`summarize`/상수 (Task 3) · `Store.get_keyword_stats`/`put_keyword_stats` (Task 2) · `youtube_client._LANG_REGION`·`_title_lang_ok`·`_stats` (읽기만) · `config.YOUTUBE_API_KEYS`
- Produces: `probe_keywords(keywords, store, lang="ko")` → `[stat_dict, ...]` — `stat_dict`는 Task 2의 `put_keyword_stats` 입력과 같은 모양 + `checked_at`

**비용:** 키워드당 `search.list`(100u) + `videos.list`(1u) + `channels.list`(1u) = **102유닛**.
`_MAX_PROBE=6` → 최대 612유닛/회. 캐시 적중분은 0.

- [x] **Step 1: 실패하는 테스트를 쓴다**

`shopping_shorts/tests/test_seo_probe.py` 끝에 추가:

```python
import pytest
from shopping_shorts.store import Store


@pytest.fixture
def store(tmp_path):
    s = Store(str(tmp_path / "p.db"))
    s.init_db()
    return s


class _FakeResp:
    def __init__(self, payload, status=200):
        self._p, self.status_code = payload, status

    def json(self):
        return self._p


def _search_payload(titles):
    return {"items": [
        {"id": {"videoId": f"v{i}"},
         "snippet": {"channelId": f"c{i}", "channelTitle": f"ch{i}", "title": t,
                     "description": "", "thumbnails": {}, "publishedAt": "2026-07-01T00:00:00Z"}}
        for i, t in enumerate(titles)]}


def _videos_payload(n, views):
    return {"items": [{"id": f"v{i}", "statistics": {"viewCount": str(views)}} for i in range(n)]}


def _channels_payload(n, subs):
    return {"items": [{"id": f"c{i}", "statistics": {"subscriberCount": str(subs)}} for i in range(n)]}


def _fake_get(search, videos, channels, calls=None):
    """URL로 어느 API인지 갈라 가짜 응답을 준다."""
    def _get(url, params=None, timeout=None):
        if calls is not None:
            calls.append(url)
        if "search" in url:
            return _FakeResp(search)
        if "videos" in url:
            return _FakeResp(videos)
        return _FakeResp(channels)
    return _get


def test_probe_keywords_measures(monkeypatch, store):
    monkeypatch.setattr(seo_probe, "YOUTUBE_API_KEYS", ["k1"])
    titles = ["한글제목1", "한글제목2", "한글제목3", "한글제목4"]
    monkeypatch.setattr(seo_probe.requests, "get", _fake_get(
        _search_payload(titles), _videos_payload(4, 500_000), _channels_payload(4, 500)))
    got = seo_probe.probe_keywords(["빨대텀블러"], store)
    assert len(got) == 1
    assert got[0]["keyword"] == "빨대텀블러"
    assert got[0]["views_median"] == 500_000
    assert got[0]["small_ratio"] == 1.0
    assert got[0]["verdict"] == "blue"


def test_probe_keywords_caps_at_max_probe(monkeypatch, store):
    """쿼터 상한 — 6개를 넘겨도 6개만 잰다."""
    monkeypatch.setattr(seo_probe, "YOUTUBE_API_KEYS", ["k1"])
    calls = []
    monkeypatch.setattr(seo_probe.requests, "get", _fake_get(
        _search_payload(["한글1", "한글2", "한글3"]),
        _videos_payload(3, 500_000), _channels_payload(3, 500), calls))
    got = seo_probe.probe_keywords([f"키워드{i}" for i in range(20)], store)
    assert len(got) == seo_probe._MAX_PROBE
    assert sum(1 for u in calls if "search" in u) == seo_probe._MAX_PROBE


def test_probe_keywords_uses_cache(monkeypatch, store):
    """캐시가 있으면 API를 아예 안 부른다 — 100유닛짜리다."""
    store.put_keyword_stats({"keyword": "캐시된", "region": "KR", "views_median": 7,
                             "small_ratio": 0.5, "sample_n": 20,
                             "top_titles": ["x"], "verdict": "blue"})

    def _boom(*a, **k):
        raise AssertionError("캐시 적중인데 API를 불렀다")

    monkeypatch.setattr(seo_probe, "YOUTUBE_API_KEYS", ["k1"])
    monkeypatch.setattr(seo_probe.requests, "get", _boom)
    got = seo_probe.probe_keywords(["캐시된"], store)
    assert got[0]["views_median"] == 7


def test_probe_keywords_writes_cache(monkeypatch, store):
    monkeypatch.setattr(seo_probe, "YOUTUBE_API_KEYS", ["k1"])
    monkeypatch.setattr(seo_probe.requests, "get", _fake_get(
        _search_payload(["한글1", "한글2", "한글3"]),
        _videos_payload(3, 500_000), _channels_payload(3, 500)))
    seo_probe.probe_keywords(["새키워드"], store)
    assert store.get_keyword_stats("새키워드") is not None


def test_probe_keywords_no_keys_is_unknown(monkeypatch, store):
    """키가 없어도 예외를 던지지 않는다 — SEO 문구 생성이 측정보다 우선이다."""
    monkeypatch.setattr(seo_probe, "YOUTUBE_API_KEYS", [])
    got = seo_probe.probe_keywords(["아무거나"], store)
    assert got[0]["verdict"] == "unknown"


def test_probe_keywords_403_rotates_then_unknown(monkeypatch, store):
    """모든 키가 403이면 unknown — 우아하게 꺼진다."""
    monkeypatch.setattr(seo_probe, "YOUTUBE_API_KEYS", ["k1", "k2"])
    calls = []

    def _get(url, params=None, timeout=None):
        calls.append(params.get("key"))
        return _FakeResp({}, status=403)

    monkeypatch.setattr(seo_probe.requests, "get", _get)
    got = seo_probe.probe_keywords(["소진"], store)
    assert got[0]["verdict"] == "unknown"
    assert "k1" in calls and "k2" in calls      # 로테이션은 했다


def test_probe_keywords_403_not_cached(monkeypatch, store):
    """실패를 캐시하면 7일간 unknown에 갇힌다."""
    monkeypatch.setattr(seo_probe, "YOUTUBE_API_KEYS", ["k1"])
    monkeypatch.setattr(seo_probe.requests, "get",
                        lambda url, params=None, timeout=None: _FakeResp({}, status=403))
    seo_probe.probe_keywords(["소진"], store)
    assert store.get_keyword_stats("소진") is None


def test_probe_keywords_filters_foreign_titles(monkeypatch, store):
    """한국어 필터 — 외국 영상이 섞이면 측정이 틀어진다."""
    monkeypatch.setattr(seo_probe, "YOUTUBE_API_KEYS", ["k1"])
    monkeypatch.setattr(seo_probe.requests, "get", _fake_get(
        _search_payload(["한글제목", "English Only Title", "another english"]),
        _videos_payload(3, 500_000), _channels_payload(3, 500)))
    got = seo_probe.probe_keywords(["섞임"], store)
    assert got[0]["sample_n"] == 1          # 한글 1건만 남는다
    assert got[0]["verdict"] == "unknown"   # 표본 부족
```

- [x] **Step 2: 실패를 확인한다**

Run: `python -m pytest shopping_shorts/tests/test_seo_probe.py -v -s -k probe_keywords`
Expected: 8건 FAIL — `AttributeError: module 'shopping_shorts.seo_probe' has no attribute 'probe_keywords'`

- [x] **Step 3: 배선을 구현한다**

`shopping_shorts/seo_probe.py`에 추가 (파일 상단 import부터):

```python
from datetime import datetime, timedelta, timezone

import requests

from shopping_shorts.config import YOUTUBE_API_KEYS
from shopping_shorts.youtube_client import _LANG_REGION, _title_lang_ok

_SEARCH_URL = "https://www.googleapis.com/youtube/v3/search"
_VIDEOS_URL = "https://www.googleapis.com/youtube/v3/videos"
_CHANNELS_URL = "https://www.googleapis.com/youtube/v3/channels"
```

이어서 함수들:

```python
def _unknown(keyword, region):
    """측정 실패 — 판정하지 않는다. 캐시하지 않으므로 다음에 다시 시도된다."""
    return {"keyword": keyword, "region": region, "views_median": 0,
            "small_ratio": 0.0, "sample_n": 0, "top_titles": [],
            "verdict": "unknown", "checked_at": None}


def _search(kw, tok, region, lang):
    """(status, items|None). items = [{video_id, channel_id, title}]"""
    since = (datetime.now(timezone.utc) - timedelta(days=_WINDOW_DAYS)).isoformat()
    r = requests.get(_SEARCH_URL, params={
        "part": "snippet", "q": kw, "type": "video", "videoDuration": "short",
        "order": "viewCount", "publishedAfter": since,
        "regionCode": region, "relevanceLanguage": lang,
        "maxResults": _SAMPLE_MAX, "key": tok}, timeout=30)
    if r.status_code != 200:
        return r.status_code, None
    items = []
    for it in r.json().get("items", []):
        vid = (it.get("id") or {}).get("videoId")
        sn = it.get("snippet") or {}
        if not vid or not _title_lang_ok(sn.get("title"), lang):
            continue      # 외국 영상 제거 — 섞이면 측정이 틀어진다
        items.append({"video_id": vid, "channel_id": sn.get("channelId"),
                      "title": sn.get("title") or ""})
    return r.status_code, items


def _fetch_stats(url, ids, tok, field):
    """videos/channels statistics → {id: int}. 실패는 조용히 건너뛴다(빈 dict)."""
    out = {}
    for i in range(0, len(ids), 50):          # API 상한 50개/호출
        chunk = [x for x in ids[i:i + 50] if x]
        if not chunk:
            continue
        r = requests.get(url, params={
            "part": "statistics", "id": ",".join(chunk), "key": tok}, timeout=30)
        if r.status_code != 200:
            continue
        for it in r.json().get("items", []):
            out[it["id"]] = int((it.get("statistics") or {}).get(field) or 0)
    return out


def probe_keywords(keywords, store, lang="ko"):
    """키워드별 유튜브 실측. 캐시 우선, _MAX_PROBE개까지만.

    비용: 키워드당 search.list(100u) + videos.list(1u) + channels.list(1u) = 102u.
    발굴 파이프라인과 같은 키풀을 나눠 쓰므로 상한과 캐시가 필수다.
    키 소진·실패는 verdict='unknown'으로 우아하게 꺼진다 — SEO 문구 생성이 측정보다 우선이다.
    """
    region = _LANG_REGION.get(lang, "KR")
    out = []
    tokens = list(YOUTUBE_API_KEYS)
    tok_idx = 0
    for kw in list(keywords)[:_MAX_PROBE]:
        cached = store.get_keyword_stats(kw, region)
        if cached:
            out.append(cached)
            continue
        if not tokens:
            out.append(_unknown(kw, region))
            continue
        items = None
        while tok_idx < len(tokens):
            status, items = _search(kw, tokens[tok_idx], region, lang)
            if status == 403:      # 쿼터 소진 — 다음 키로 이 키워드부터 재시도
                tok_idx += 1
                items = None
                continue
            break
        if items is None:
            out.append(_unknown(kw, region))
            continue
        tok = tokens[min(tok_idx, len(tokens) - 1)]
        views = _fetch_stats(_VIDEOS_URL, [i["video_id"] for i in items], tok, "viewCount")
        subs = _fetch_stats(_CHANNELS_URL, [i["channel_id"] for i in items], tok, "subscriberCount")
        stat = summarize([{"title": i["title"],
                           "views": views.get(i["video_id"], 0),
                           "subs": subs.get(i["channel_id"], 0)} for i in items])
        stat.update({"keyword": kw, "region": region})
        store.put_keyword_stats(stat)          # 성공만 캐시 — 실패를 캐시하면 7일간 갇힌다
        stat["checked_at"] = datetime.now(timezone.utc).isoformat()
        out.append(stat)
    return out
```

- [x] **Step 4: 통과를 확인한다**

Run: `python -m pytest shopping_shorts/tests/test_seo_probe.py -v -s`
Expected: 18 passed (Task 3의 10건 + 8건)

- [x] **Step 5: 뮤테이션 3종으로 자물쇠를 확인한다**

(a) `[:_MAX_PROBE]` 슬라이스를 지운다 → `test_probe_keywords_caps_at_max_probe` FAIL
(b) 캐시 조회(`cached = ...` / `if cached:`)를 지운다 → `test_probe_keywords_uses_cache` FAIL
(c) `_unknown` 경로에서도 `put_keyword_stats`를 부르게 한다 → `test_probe_keywords_403_not_cached` FAIL

셋 다 확인 후 원복, 18 passed.

- [x] **Step 6: 커밋**

```bash
git add shopping_shorts/seo_probe.py shopping_shorts/tests/test_seo_probe.py
git commit -m "feat(seo): 유튜브 키워드 실측 배선 — 6개 상한·7일 캐시·소진 시 우아한 unknown"
```

---

## Task 5: `seo_generate` — Gemini 생성

**Files:**
- Create: `shopping_shorts/seo_generate.py`
- Test: `shopping_shorts/tests/test_seo_generate.py`

**Interfaces:**
- Consumes: `comment_gen._MODEL` · `pipeline.atoms.key_vault`
- Produces:
  - `generate(job, captions=None, only=None, locked=None, keyword_stats=None)` → `dict | None`
    - `job` = `Store.get_mix_job()` 반환 dict
    - `captions` = `[str]` — 소스 릴스 캡션 (프론트 `HANDOFF[].caption`)
    - `only` = `None | "title" | "description" | "tags" | "hashtags" | "hook" | "cta"`
    - `locked` = `{"title": bool, ...}` — 잠긴 카드는 프롬프트에 "확정, 건드리지 마라"로 들어간다
    - `keyword_stats` = `[stat_dict]` — 되먹임용(Pass 3). 있으면 프롬프트에 실측치가 들어간다
    - 반환: 아래 `_SCHEMA` 모양. 실패·무키면 `None`
  - `seed_keywords(seo)` → `[str]` — `title_candidates[].keywords`를 평탄화·중복제거(순서 보존)

- [x] **Step 1: 실패하는 테스트를 쓴다**

`shopping_shorts/tests/test_seo_generate.py`:

```python
import json
from shopping_shorts import seo_generate


_JOB = {
    "given_script": "이 텀블러는 뒤집어도 안 새요. 얼음이 하루 종일 유지됩니다.",
    "script_structure": {"product_category": "홈템", "hook_line": "가방에 내용물 샐까",
                         "appeal": "완벽 밀폐", "one_line_why": "새지 않는 텀블러",
                         "tone": "친근한 반말"},
    "headcopy": {"text": "샐 걱정 ZERO"},
    "edit_plan": {"beats": [{"narration": "뒤집어도 안 새요", "role": "본문"}]},
}

_OUT = {
    "title": "샐 걱정 ZERO 텀블러",
    "title_candidates": [{"text": "A", "why": "훅을 앞으로", "keywords": ["빨대텀블러", "보냉텀블러"]},
                         {"text": "B", "why": "카테고리", "keywords": ["빨대텀블러", "홈템"]}],
    "description": "설명",
    "tags": ["t1", "t2"],
    "hashtags": {"youtube": ["#a"], "tiktok": ["#b"], "threads": ["#c"]},
    "hook_line": "훅", "comment_bait": "힙해",
    "cta": {"youtube": "y", "tiktok": "t", "threads": "th"},
}


class _FakeResp:
    def __init__(self, payload):
        self.text = json.dumps(payload, ensure_ascii=False)


class _FakeModels:
    def __init__(self, payload, exc=None, log=None):
        self._p, self._exc, self._log = payload, exc, log

    def generate_content(self, model=None, contents=None, config=None):
        if self._log is not None:
            self._log.append(contents)
        if self._exc:
            raise self._exc
        return _FakeResp(self._p)


class _FakeClient:
    def __init__(self, payload, exc=None, log=None):
        self.models = _FakeModels(payload, exc, log)


def _patch(monkeypatch, keys, client):
    monkeypatch.setattr(seo_generate.key_vault, "get_live_keys_cascade", lambda g: keys)
    monkeypatch.setattr(seo_generate.key_vault, "get_client_for_key", lambda k: client)


def test_generate_returns_seo(monkeypatch):
    _patch(monkeypatch, ["k1"], _FakeClient(_OUT))
    got = seo_generate.generate(_JOB)
    assert got["title"] == "샐 걱정 ZERO 텀블러"
    assert got["cta"]["tiktok"] == "t"


def test_generate_no_keys_returns_none(monkeypatch):
    monkeypatch.setattr(seo_generate.key_vault, "get_live_keys_cascade", lambda g: [])
    assert seo_generate.generate(_JOB) is None


def test_generate_cascades_on_exhausted_key(monkeypatch):
    """소진키는 마킹하고 다음 키로 — 공유풀 캐스케이드."""
    marked = []
    bad, good = _FakeClient(None, exc=RuntimeError("quota")), _FakeClient(_OUT)
    clients = iter([bad, good])
    monkeypatch.setattr(seo_generate.key_vault, "get_live_keys_cascade", lambda g: ["k1", "k2"])
    monkeypatch.setattr(seo_generate.key_vault, "get_client_for_key", lambda k: next(clients))
    monkeypatch.setattr(seo_generate.key_vault, "is_daily_exhausted_error", lambda e: True)
    monkeypatch.setattr(seo_generate.key_vault, "is_account_disabled_error", lambda e: False)
    monkeypatch.setattr(seo_generate.key_vault, "_owner_group", lambda k: "general")
    monkeypatch.setattr(seo_generate.key_vault, "mark_exhausted", lambda g, k: marked.append(k))
    got = seo_generate.generate(_JOB)
    assert got["title"] == "샐 걱정 ZERO 텀블러"
    assert marked == ["k1"]


def test_generate_bad_json_returns_none(monkeypatch):
    class _Bad:
        text = "not json"

    class _M:
        def generate_content(self, **k):
            return _Bad()

    class _C:
        models = _M()

    _patch(monkeypatch, ["k1"], _C())
    assert seo_generate.generate(_JOB) is None


def test_prompt_includes_script_and_structure(monkeypatch):
    log = []
    _patch(monkeypatch, ["k1"], _FakeClient(_OUT, log=log))
    seo_generate.generate(_JOB)
    p = log[0]
    assert "뒤집어도 안 새요" in p        # 대본
    assert "가방에 내용물 샐까" in p       # hook_line
    assert "샐 걱정 ZERO" in p           # headcopy


def test_prompt_includes_captions(monkeypatch):
    log = []
    _patch(monkeypatch, ["k1"], _FakeClient(_OUT, log=log))
    seo_generate.generate(_JOB, captions=["#텀블러 이거 대박 #홈템"])
    assert "#텀블러 이거 대박" in log[0]


def test_prompt_includes_keyword_stats_when_given(monkeypatch):
    """되먹임 — 실측치가 프롬프트에 들어가야 Pass 3가 의미 있다."""
    log = []
    _patch(monkeypatch, ["k1"], _FakeClient(_OUT, log=log))
    seo_generate.generate(_JOB, only="title", keyword_stats=[
        {"keyword": "빨대텀블러", "verdict": "red", "views_median": 1_800_000, "small_ratio": 0.05}])
    p = log[0]
    assert "빨대텀블러" in p
    assert "red" in p or "레드" in p


def test_prompt_marks_locked_fields(monkeypatch):
    log = []
    _patch(monkeypatch, ["k1"], _FakeClient(_OUT, log=log))
    seo_generate.generate(_JOB, locked={"title": True}, only="tags")
    assert "잠" in log[0] or "확정" in log[0]


def test_seed_keywords_dedups_preserving_order():
    got = seo_generate.seed_keywords(_OUT)
    assert got == ["빨대텀블러", "보냉텀블러", "홈템"]


def test_seed_keywords_empty_on_missing():
    assert seo_generate.seed_keywords({}) == []
```

- [x] **Step 2: 실패를 확인한다**

Run: `python -m pytest shopping_shorts/tests/test_seo_generate.py -v -s`
Expected: 10건 FAIL — `ModuleNotFoundError: No module named 'shopping_shorts.seo_generate'`

- [x] **Step 3: 모듈을 만든다**

`shopping_shorts/seo_generate.py`:

```python
"""6단계 SEO — 확정 대본으로 업로드용 메타데이터 생성(2026-07-17).

제목(+추천5)·설명·태그·해시태그·후킹·댓글유도·플랫폼별 CTA를 한 번에 뽑는다.
키워드 실측치(seo_probe)를 되먹이면 그 근거 위에서 다시 쓴다.

키풀: comment_gen 전용키(1개, 쉽게 소진) 대신 key_vault 공유풀을 캐스케이드로 쓴다
(general→ingest→embed→briefing). SEO는 사장님이 버튼 누르고 기다리는 대화형이라
대본생성과 똑같이 소진에 취약하다 — script_generate.py:20-25와 같은 이유.
"""
import json

from google.genai import types

from shopping_shorts import comment_gen
from pipeline.atoms import key_vault

_MODEL = comment_gen._MODEL
_GEN_GROUP = "general"

_PLATFORMS = ("youtube", "tiktok", "threads")

_SCHEMA = {
    "type": "object",
    "properties": {
        "title": {"type": "string"},
        "title_candidates": {
            "type": "array",
            "items": {
                "type": "object",
                "properties": {
                    "text": {"type": "string"},
                    "why": {"type": "string"},
                    "keywords": {"type": "array", "items": {"type": "string"}},
                },
                "required": ["text", "why", "keywords"],
            },
        },
        "description": {"type": "string"},
        "tags": {"type": "array", "items": {"type": "string"}},
        "hashtags": {
            "type": "object",
            "properties": {p: {"type": "array", "items": {"type": "string"}} for p in _PLATFORMS},
            "required": list(_PLATFORMS),
        },
        "hook_line": {"type": "string"},
        "comment_bait": {"type": "string"},
        "cta": {
            "type": "object",
            "properties": {p: {"type": "string"} for p in _PLATFORMS},
            "required": list(_PLATFORMS),
        },
    },
    "required": ["title", "title_candidates", "description", "tags",
                 "hashtags", "hook_line", "comment_bait", "cta"],
}

_ONLY_LABELS = {
    "title": "제목과 추천 제목 5개",
    "description": "영상 설명",
    "tags": "유튜브 태그 20개",
    "hashtags": "플랫폼별 해시태그",
    "hook": "후킹 멘트와 댓글 유도",
    "cta": "플랫폼별 CTA",
}

_BASE_PROMPT = """너는 한국 쇼츠 채널의 SEO 담당이다. 아래 확정 대본으로 업로드용 메타데이터를 만든다.

[규격 — 어기면 못 쓴다]
- 제목: 100자 이내. 검색 키워드를 앞쪽에 둔다. 추천 제목 5개를 주고, 각각 why에 "왜 이 제목인지"를 한 줄로 쓴다(훅에서 왔는지·카테고리에서 왔는지·어필포인트에서 왔는지).
- title_candidates[].keywords: 그 제목이 노리는 검색 키워드 1~3개. 실제로 사람이 검색창에 칠 법한 말로.
- 설명: 200~300자. 첫 줄에 핵심, 이후 줄바꿈으로 나눈다.
- 태그: 정확히 20개. 유튜브 태그 전체가 500자를 넘지 않게.
- 해시태그: 플랫폼마다 다르게. youtube 3~5개 / tiktok 3~5개 / threads 1~2개(쓰레드는 해시태그를 거의 안 쓴다).
- CTA: 플랫폼마다 어투가 다르다. youtube=설명란 링크 유도 / tiktok=댓글 유도형 / threads=되묻는 대화체.
- 전부 한국어. 과장·허위 금지(대본에 없는 효능을 지어내지 마라).

[대본]
{script}

[구조 분석]
{structure}

[화면 헤드카피]
{headcopy}
"""


def _fmt_stats(keyword_stats):
    """실측치를 프롬프트에 넣을 문장으로. 없으면 빈 문자열."""
    if not keyword_stats:
        return ""
    lines = []
    for s in keyword_stats:
        v = s.get("verdict")
        if v == "blue":
            note = "뚫린다(수요 있고 소형채널도 상위권) — 밀어라"
        elif v == "red":
            note = "레드오션(대형채널 독식) — 이걸 메인으로 쓰지 마라"
        elif v == "dead":
            note = "수요 낮음 — 빼라"
        else:
            note = "미측정"
        lines.append(
            f"- {s.get('keyword')}: {v} / {note} "
            f"(상위 조회수 중앙값 {s.get('views_median')}, 소형채널 비율 {s.get('small_ratio')})")
    return ("\n[유튜브 실측 — 이 근거 위에서 다시 써라]\n" + "\n".join(lines) + "\n")


def _fmt_locked(locked):
    if not locked:
        return ""
    on = [k for k, v in locked.items() if v]
    if not on:
        return ""
    return ("\n[잠긴 항목 — 확정이다. 그대로 두고, 나머지를 여기에 어울리게 써라]\n"
            + ", ".join(on) + "\n")


def _build_prompt(job, captions, only, locked, keyword_stats):
    struct = job.get("script_structure") or {}
    head = (job.get("headcopy") or {}).get("text") or ""
    p = _BASE_PROMPT.format(
        script=job.get("given_script") or "",
        structure=json.dumps(struct, ensure_ascii=False, indent=1),
        headcopy=head,
    )
    if captions:
        # 소스 릴스 캡션 = 실제로 터진 영상의 문구. 단 인스타 문법이라 톤 참고용이다
        # (유튜브 검색 키워드는 seo_probe 실측으로 검증한다).
        p += ("\n[참고 — 이 영상이 참고한 인스타 릴스 캡션. 톤·소구점만 참고하고 "
              "인스타 해시태그 문법을 유튜브에 그대로 옮기지 마라]\n"
              + "\n".join(f"- {c}" for c in captions if c) + "\n")
    p += _fmt_stats(keyword_stats)
    p += _fmt_locked(locked)
    if only:
        p += (f"\n[이번엔 {_ONLY_LABELS.get(only, only)}만 새로 써라. "
              "나머지 항목도 스키마대로 채워서 반환하되 기존 것을 유지해라.]\n")
    return p


def generate(job, captions=None, only=None, locked=None, keyword_stats=None):
    """key_vault 캐스케이드로 SEO 일습 생성. 소진키는 마킹하고 다음 키로.
    무키·전부실패면 None."""
    keys = key_vault.get_live_keys_cascade(_GEN_GROUP)
    if not keys:
        return None
    prompt = _build_prompt(job, captions, only, locked, keyword_stats)
    for key in keys:
        try:
            resp = key_vault.get_client_for_key(key).models.generate_content(
                model=_MODEL, contents=prompt,
                config=types.GenerateContentConfig(
                    response_mime_type="application/json", response_schema=_SCHEMA),
            )
            return json.loads(resp.text)
        except Exception as e:  # noqa: BLE001 — 생성 실패는 치명적 아님
            if key_vault.is_daily_exhausted_error(e) or key_vault.is_account_disabled_error(e):
                key_vault.mark_exhausted(key_vault._owner_group(key) or _GEN_GROUP, key)
                continue
            if key_vault.is_quota_error(e):
                continue  # 순간 rate limit — 다음 키로
            return None
    return None


def seed_keywords(seo):
    """title_candidates[].keywords 평탄화·중복제거(순서 보존).
    seo_probe.probe_keywords()가 앞 _MAX_PROBE개만 재므로 순서가 곧 우선순위다."""
    out, seen = [], set()
    for c in (seo or {}).get("title_candidates") or []:
        for k in c.get("keywords") or []:
            if k and k not in seen:
                seen.add(k)
                out.append(k)
    return out
```

⚠️ `test_generate_bad_json_returns_none`이 통과하려면 `json.loads` 실패가 마지막 `return None`으로
가야 한다. `is_quota_error`가 monkeypatch되지 않은 실제 함수라 `JSONDecodeError`에 `False`를
돌려주는지 확인한다: `grep -n "def is_quota_error" -A 6 pipeline/atoms/key_vault.py`

- [x] **Step 4: 통과를 확인한다**

Run: `python -m pytest shopping_shorts/tests/test_seo_generate.py -v -s`
Expected: 10 passed

- [x] **Step 5: 커밋**

```bash
git add shopping_shorts/seo_generate.py shopping_shorts/tests/test_seo_generate.py
git commit -m "feat(seo): Gemini 생성 모듈 — key_vault 캐스케이드·실측 되먹임·부분 재생성"
```

---

## Task 6: 엔드포인트

**Files:**
- Modify: `shopping_shorts/app.py` — `mix/settings`(`:1964-1982`)에 `seo` 추가 / `fx/suggest`(`:2443`) 근처에 신규 라우트
- Test: `shopping_shorts/tests/test_seo_api.py`

**Interfaces:**
- Consumes: `seo_generate.generate`/`seed_keywords` (T5) · `seo_probe.probe_keywords` (T4) · `Store.get_mix_job`/`update_mix_job` (T1)
- Produces:
  - `POST /api/produce/seo/generate` body `{job_id, captions?: [str], only?: str, locked?: {}}` → `{"seo": {...}}` / 404 `{"ok": false, "error": "job 없음"}` / 502 `{"ok": false, "error": "생성 실패"}`
  - `POST /api/produce/mix/settings` body에 `seo` 필드 지원

**설계 결정:** 생성 결과를 **엔드포인트가 저장하지 않는다.** `fx/suggest`와 같이 무과금 미리보기다 —
사장님이 화면에서 고치고 `mix/settings`로 확정 저장한다. 생성할 때마다 덮어쓰면
손으로 다듬은 걸 날린다.

- [x] **Step 1: 실패하는 테스트를 쓴다**

`shopping_shorts/tests/test_seo_api.py`:

```python
import pytest
from fastapi.testclient import TestClient

from shopping_shorts import app as app_mod
from shopping_shorts.store import Store


@pytest.fixture
def client(tmp_path, monkeypatch):
    db = str(tmp_path / "api.db")
    monkeypatch.setattr(app_mod, "DB_PATH", db)
    s = Store(db)
    s.init_db()
    s.create_mix_job("job1", ["https://x/1"], 20, "free")
    s.update_mix_job("job1", given_script="대본입니다")
    return TestClient(app_mod.app)


_SEO = {"title": "제목", "title_candidates": [{"text": "A", "why": "w", "keywords": ["kw1"]}],
        "description": "d", "tags": ["t"], "hashtags": {"youtube": [], "tiktok": [], "threads": []},
        "hook_line": "h", "comment_bait": "c",
        "cta": {"youtube": "y", "tiktok": "t", "threads": "th"}}


def test_generate_404_on_missing_job(client):
    r = client.post("/api/produce/seo/generate", json={"job_id": "nope"})
    assert r.status_code == 404


def test_generate_returns_seo_with_stats(client, monkeypatch):
    monkeypatch.setattr(app_mod.seo_generate, "generate", lambda *a, **k: dict(_SEO))
    monkeypatch.setattr(app_mod.seo_probe, "probe_keywords",
                        lambda kws, store, lang="ko": [{"keyword": "kw1", "verdict": "blue",
                                                        "views_median": 300000, "small_ratio": 0.4,
                                                        "top_titles": [], "sample_n": 20}])
    r = client.post("/api/produce/seo/generate", json={"job_id": "job1"})
    assert r.status_code == 200
    seo = r.json()["seo"]
    assert seo["title"] == "제목"
    assert seo["keyword_stats"][0]["verdict"] == "blue"
    assert "generated_at" in seo


def test_generate_502_when_ai_fails(client, monkeypatch):
    monkeypatch.setattr(app_mod.seo_generate, "generate", lambda *a, **k: None)
    r = client.post("/api/produce/seo/generate", json={"job_id": "job1"})
    assert r.status_code == 502


def test_generate_does_not_save(client, monkeypatch):
    """무과금 미리보기 — 저장은 사장님이 확정할 때만(fx/suggest와 같은 규약)."""
    monkeypatch.setattr(app_mod.seo_generate, "generate", lambda *a, **k: dict(_SEO))
    monkeypatch.setattr(app_mod.seo_probe, "probe_keywords", lambda kws, store, lang="ko": [])
    client.post("/api/produce/seo/generate", json={"job_id": "job1"})
    assert Store(app_mod.DB_PATH).get_mix_job("job1")["seo"] is None


def test_generate_only_skips_probe(client, monkeypatch):
    """부분 재생성은 측정을 다시 하지 않는다 — 100유닛짜리를 공짜로 태우면 안 된다."""
    called = []
    monkeypatch.setattr(app_mod.seo_generate, "generate", lambda *a, **k: dict(_SEO))
    monkeypatch.setattr(app_mod.seo_probe, "probe_keywords",
                        lambda kws, store, lang="ko": called.append(kws) or [])
    client.post("/api/produce/seo/generate",
                json={"job_id": "job1", "only": "title",
                      "keyword_stats": [{"keyword": "kw1", "verdict": "red"}]})
    assert called == []


def test_settings_saves_seo(client):
    r = client.post("/api/produce/mix/settings", json={"job_id": "job1", "seo": _SEO})
    assert r.status_code == 200
    assert Store(app_mod.DB_PATH).get_mix_job("job1")["seo"]["title"] == "제목"


def test_settings_404_on_missing_job(client):
    r = client.post("/api/produce/mix/settings", json={"job_id": "nope", "seo": _SEO})
    assert r.status_code == 404
```

⚠️ 기존 `test_app.py`가 TestClient를 어떻게 만드는지 먼저 본다 —
`grep -n "TestClient\|DB_PATH" shopping_shorts/tests/test_app.py | head`. 관례가 다르면 그걸 따른다.

- [x] **Step 2: 실패를 확인한다**

Run: `python -m pytest shopping_shorts/tests/test_seo_api.py -v -s`
Expected: 7건 FAIL — 404(라우트 없음) 또는 `AttributeError: module 'shopping_shorts.app' has no attribute 'seo_generate'`

- [x] **Step 3: import를 추가한다**

`app.py` 상단 import 구역, `from shopping_shorts import ...` 줄에 `seo_generate`, `seo_probe` 추가.
(정확한 현재 형태를 먼저 본다: `grep -n "^from shopping_shorts import" -A 3 shopping_shorts/app.py`)

- [x] **Step 4: `mix/settings`에 `seo`를 추가한다**

`app.py:1978-1979`의 `deco` 블록 **다음**:

```python
    if "deco" in body:
        fields["deco"] = body.get("deco")  # 워터마크·추가텍스트·오버레이·BGM dict or None
    if "seo" in body:
        fields["seo"] = body.get("seo")  # 6단계 SEO 일습 dict or None
```

- [x] **Step 5: 생성 라우트를 만든다**

`app.py`의 `api_fx_suggest`(`:2443`) **앞**에 추가:

```python
@app.post("/api/produce/seo/generate")
def api_seo_generate(body: dict):
    """확정 대본으로 SEO 일습 생성 + 키워드 유튜브 실측.

    DB에 기록하지 않는다(무과금 미리보기 — fx/suggest와 같은 규약). 사장님이
    화면에서 다듬고 /mix/settings로 확정 저장한다. 생성할 때마다 덮어쓰면
    손으로 고친 걸 날린다.

    only가 있으면 부분 재생성 — 측정(100유닛/키워드)을 다시 하지 않고
    호출부가 준 keyword_stats를 프롬프트에 되먹인다.
    """
    job_id = (body.get("job_id") or "").strip()
    store = Store(DB_PATH)
    job = store.get_mix_job(job_id) if job_id else None
    if not job:
        return JSONResponse(status_code=404, content={"ok": False, "error": "job 없음"})

    only = body.get("only") or None
    locked = body.get("locked") or {}
    captions = body.get("captions") or []
    stats = body.get("keyword_stats") or []

    seo = seo_generate.generate(job, captions=captions, only=only,
                                locked=locked, keyword_stats=stats)
    if not seo:
        return JSONResponse(status_code=502, content={"ok": False, "error": "생성 실패"})

    if not only:
        # 전체 생성일 때만 측정한다 — search.list가 100유닛이고 발굴과 키풀을 나눠 쓴다.
        stats = seo_probe.probe_keywords(seo_generate.seed_keywords(seo), store)
    seo["keyword_stats"] = stats
    seo["generated_at"] = datetime.now(timezone.utc).isoformat()
    return {"seo": seo}
```

⚠️ `app.py`에 `datetime`/`timezone`이 import돼 있는지 확인한다:
`grep -n "^from datetime import\|^import datetime" shopping_shorts/app.py`

- [x] **Step 6: 통과를 확인한다**

Run: `python -m pytest shopping_shorts/tests/test_seo_api.py -v -s`
Expected: 7 passed

- [x] **Step 7: import와 회귀를 확인한다**

Run: `python -c "import shopping_shorts.app; print('OK')"`
Expected: `OK`

Run: `python -m pytest shopping_shorts/tests/test_app.py -q -s`
Expected: 병합 전과 실패 수가 같다

- [x] **Step 8: 커밋**

```bash
git add shopping_shorts/app.py shopping_shorts/tests/test_seo_api.py
git commit -m "feat(seo): /api/produce/seo/generate + mix/settings에 seo 저장"
```

---

## Task 7: 6단계 화면

**Files:**
- Modify: `shopping_shorts/static/produce.html` — 패널(`:401`) 교체 / `showPanel()`(`:460-469`)에 훅 1줄 / 새 스크립트 구역
- Test: 없음(프런트, pytest 없음) — **`node --check`로 문법만 잠근다. 사장님 육안이 진짜 게이트다.**

**Interfaces:**
- Consumes: `POST /api/produce/seo/generate` · `POST /api/produce/mix/settings` (T6) · 전역 `MIX_JOB`(`:1270`) · `HANDOFF[]`(`:1282`)
- Produces: `initSeo()` — `showPanel()`이 `cur===5`에 부른다

**⚠️ 앵커 보호:** `const STEPS`(`:449`)와 `function jump(i){` 등 앵커 문자열을 건드리지 않는다.
`showPanel()`에 **1줄만** 넣는다. `tests/test_produce_preview_gate.py`가 실소스를 잘라 Node로 돌린다.

- [x] **Step 1: 패널 스텁을 교체한다**

`produce.html:401` 한 줄을 아래로 교체:

```html
    <section class="panel" data-step="5">
      <h3>6 · SEO</h3>
      <div class="sub">제목·설명·태그를 만들고, 키워드를 유튜브에서 실제로 재서 근거를 붙입니다.</div>
      <div class="seoBar">
        <button class="btn primary" id="seoGenAll" onclick="seoGenerate()">AI로 전체 생성</button>
        <button class="btn" onclick="seoCopyYoutube()">유튜브 전체 복사</button>
        <span class="seoMsg" id="seoMsg"></span>
      </div>
      <div id="seoBody" class="seoBody"></div>
    </section>
```

- [x] **Step 2: `showPanel()`에 훅 1줄을 넣는다**

`produce.html:468`의 `if(cur===6) refreshFinal();` **앞**:

```javascript
  if(cur===3) initHeadcopy();
  if(cur===5) initSeo();
  if(cur===6) refreshFinal();
```

- [x] **Step 3: 스크립트 구역을 추가한다**

`// ── 5단계 꾸미기(헤드카피 디자인) ──` 구역 **앞**(`:471` 근처)에 새 구역:

```javascript
// ── 6단계 SEO(2026-07-17) ──────────────
// 산출물은 서버 mix_jobs.seo_json이 유일한 진실이다. STATE에만 두면
// saveWork()가 seo를 안 실어서 새로고침에 날아간다.
let SEO = null;

const SEO_VERDICT = {
  blue: {icon:'🟦', label:'뚫린다'},
  red:  {icon:'🟥', label:'레드오션'},
  dead: {icon:'⬜', label:'수요 낮음'},
  unknown: {icon:'–', label:'미측정'},
};

function seoEsc(s){
  return String(s==null?'':s).replace(/[&<>"']/g, c=>(
    {'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
}

async function initSeo(){
  if(!MIX_JOB){ document.getElementById('seoBody').innerHTML =
    '<div class="stub">1단계에서 대본을 먼저 확정하세요.</div>'; return; }
  if(SEO){ renderSeo(); return; }
  try{
    const r = await fetch('/api/produce/mix/state?job_id='+encodeURIComponent(MIX_JOB));
    const d = await r.json();
    SEO = (d && d.seo) || null;
  }catch(e){ SEO = null; }
  renderSeo();
}

async function seoGenerate(only){
  if(!MIX_JOB){ alert('1단계에서 대본을 먼저 확정하세요'); return; }
  const msg = document.getElementById('seoMsg');
  const btn = document.getElementById('seoGenAll');
  msg.textContent = only ? '다시 쓰는 중…' : '생성 중… (키워드 실측까지 20초쯤 걸립니다)';
  if(btn) btn.disabled = true;                 // 더블서밋 차단 — 제일 비싼 작업이다
  try{
    const body = {
      job_id: MIX_JOB,
      captions: (typeof HANDOFF!=='undefined' ? HANDOFF : []).map(h=>h && h.caption).filter(Boolean),
    };
    if(only){
      body.only = only;
      body.locked = (SEO && SEO.locked) || {};
      body.keyword_stats = (SEO && SEO.keyword_stats) || [];   // 실측치 되먹임
    }
    const r = await fetch('/api/produce/seo/generate', {
      method:'POST', headers:{'Content-Type':'application/json'}, body: JSON.stringify(body)});
    const d = await r.json();
    if(!r.ok || !d.seo){ msg.textContent = '실패: ' + ((d && d.error) || r.status); return; }
    const locked = (SEO && SEO.locked) || {};
    if(only){
      // 부분 재생성 — 요청한 카드만 갈아끼우고 나머지는 손으로 다듬은 걸 지킨다
      const keep = Object.assign({}, SEO);
      const map = {title:['title','title_candidates'], description:['description'],
                   tags:['tags'], hashtags:['hashtags'],
                   hook:['hook_line','comment_bait'], cta:['cta']};
      (map[only]||[]).forEach(k=>{ keep[k] = d.seo[k]; });
      SEO = keep;
    }else{
      SEO = d.seo;
    }
    SEO.locked = locked;
    renderSeo();
    await seoSave();
    msg.textContent = '완료';
  }catch(e){ msg.textContent = '오류: ' + e; }
  finally{ if(btn) btn.disabled = false; }
}

async function seoSave(){
  if(!MIX_JOB || !SEO) return;
  try{
    await fetch('/api/produce/mix/settings', {
      method:'POST', headers:{'Content-Type':'application/json'},
      body: JSON.stringify({job_id: MIX_JOB, seo: SEO})});
  }catch(e){ /* 저장 실패는 화면을 막지 않는다 */ }
}

function seoLock(card){
  if(!SEO) return;
  SEO.locked = SEO.locked || {};
  SEO.locked[card] = !SEO.locked[card];
  renderSeo(); seoSave();
}

function seoStatBadge(kw){
  const st = ((SEO && SEO.keyword_stats) || []).find(s=>s.keyword===kw);
  if(!st) return `<span class="kwBadge">${seoEsc(kw)}</span>`;
  const v = SEO_VERDICT[st.verdict] || SEO_VERDICT.unknown;
  const tip = (st.top_titles||[]).join(' / ');
  const detail = st.verdict==='unknown' ? '' :
    ` · 상위 조회수 ${Number(st.views_median||0).toLocaleString()}` +
    ` · 소형채널 ${Math.round((st.small_ratio||0)*100)}%`;
  return `<span class="kwBadge ${seoEsc(st.verdict)}" title="${seoEsc(tip)}">`
       + `${v.icon} ${seoEsc(kw)} — ${v.label}${detail}</span>`;
}

function seoCard(id, title, inner){
  const on = !!((SEO && SEO.locked) || {})[id];
  return `<div class="seoCard">
    <div class="seoCardHead">
      <b>${seoEsc(title)}</b>
      <span>
        <button class="mini" onclick="seoLock('${id}')">${on?'🔒':'🔓'}</button>
        <button class="mini" onclick="seoGenerate('${id}')" ${on?'disabled':''}>↻</button>
      </span>
    </div>${inner}</div>`;
}

function renderSeo(){
  const el = document.getElementById('seoBody');
  if(!SEO){ el.innerHTML = '<div class="stub">「AI로 전체 생성」을 누르세요.</div>'; return; }
  const cands = (SEO.title_candidates||[]).map((c,i)=>`
    <li onclick="seoPickTitle(${i})">
      <div class="ct">${seoEsc(c.text)}</div>
      <div class="cw">${seoEsc(c.why)}</div>
      <div class="ck">${(c.keywords||[]).map(seoStatBadge).join(' ')}</div>
    </li>`).join('');
  const tags = (SEO.tags||[]).map(t=>`<span class="chip">${seoEsc(t)}</span>`).join('');
  const hs = SEO.hashtags||{};
  const cta = SEO.cta||{};
  const plat = [['youtube','유튜브'],['tiktok','틱톡'],['threads','쓰레드']];
  el.innerHTML =
    seoCard('title','영상 제목',
      `<input id="seoTitle" maxlength="100" value="${seoEsc(SEO.title)}"
              oninput="SEO.title=this.value; seoSave()">
       <div class="cnt">${(SEO.title||'').length}/100</div>
       <div class="sub">추천 제목 (클릭하면 선택)</div><ol class="cands">${cands}</ol>`) +
    seoCard('description','영상 설명',
      `<textarea id="seoDesc" rows="6"
                 oninput="SEO.description=this.value; seoSave()">${seoEsc(SEO.description)}</textarea>`) +
    seoCard('tags',`태그 (${(SEO.tags||[]).length})`, `<div class="chips">${tags}</div>`) +
    seoCard('hashtags','해시태그',
      plat.map(([k,l])=>`<div class="sub">${l}</div><div class="chips">`
        + (hs[k]||[]).map(t=>`<span class="chip">${seoEsc(t)}</span>`).join('') + '</div>').join('')) +
    seoCard('hook','후킹 · 댓글 유도',
      `<input value="${seoEsc(SEO.hook_line)}" oninput="SEO.hook_line=this.value; seoSave()">
       <input value="${seoEsc(SEO.comment_bait)}" oninput="SEO.comment_bait=this.value; seoSave()">`) +
    seoCard('cta','플랫폼별 CTA',
      plat.map(([k,l])=>`<div class="sub">${l}</div>`
        + `<textarea rows="4" oninput="SEO.cta['${k}']=this.value; seoSave()">`
        + seoEsc(cta[k]||'') + '</textarea>').join(''));
}

function seoPickTitle(i){
  const c = (SEO.title_candidates||[])[i];
  if(!c) return;
  SEO.title = c.text; renderSeo(); seoSave();
}

function seoCopyYoutube(){
  if(!SEO){ alert('먼저 생성하세요'); return; }
  const txt = [SEO.title, '', SEO.description, '',
               (SEO.hashtags && SEO.hashtags.youtube || []).join(' '), '',
               '[태그]', (SEO.tags||[]).join(', ')].join('\n');
  navigator.clipboard.writeText(txt);
  document.getElementById('seoMsg').textContent = '유튜브용 복사 완료';
}
```

- [x] **Step 4: `/api/produce/mix/state`가 `seo`를 주는지 확인한다**

`initSeo()`가 기존 상태를 복원하려면 어떤 엔드포인트가 job을 돌려주는지 알아야 한다:

```bash
grep -n "produce/mix/state\|produce/mix/status" -A 8 shopping_shorts/app.py | head -20
```

- 그런 라우트가 **있고 `seo`를 포함하면** → 그대로 둔다.
- **없거나 `seo`를 안 주면** → `initSeo()`의 fetch를 실제 라우트에 맞춘다. 없으면
  `/api/produce/seo/generate`와 같은 자리에 읽기 전용 라우트를 추가한다:

```python
@app.get("/api/produce/seo/get")
def api_seo_get(job_id: str = ""):
    """저장된 SEO 복원용(6단계 재진입·새로고침). 없으면 seo: null."""
    job = Store(DB_PATH).get_mix_job((job_id or "").strip()) or {}
    return {"seo": job.get("seo")}
```
이 경우 `initSeo()`의 URL도 `/api/produce/seo/get?job_id=...`으로 바꾸고,
`test_seo_api.py`에 테스트를 추가한다:
```python
def test_seo_get_returns_saved(client):
    client.post("/api/produce/mix/settings", json={"job_id": "job1", "seo": _SEO})
    r = client.get("/api/produce/seo/get?job_id=job1")
    assert r.json()["seo"]["title"] == "제목"
```

- [x] **Step 5: 스타일을 추가한다**

`produce.html`의 `<style>` 구역 끝에 (선존재 클래스명과 겹치는지 먼저 확인:
`grep -n "\.chip\b\|\.mini\b\|\.cands" shopping_shorts/static/produce.html | head`):

```css
.seoBar{display:flex;gap:8px;align-items:center;margin-bottom:12px}
.seoMsg{opacity:.75;font-size:13px}
.seoBody{display:flex;flex-direction:column;gap:12px}
.seoCard{border:1px solid #8884;border-radius:8px;padding:12px}
.seoCardHead{display:flex;justify-content:space-between;align-items:center;margin-bottom:8px}
.seoCard input,.seoCard textarea{width:100%;box-sizing:border-box}
.seoCard .cnt{text-align:right;font-size:12px;opacity:.6}
.seoCard .cands{margin:4px 0 0;padding-left:20px}
.seoCard .cands li{cursor:pointer;padding:6px;border-radius:6px}
.seoCard .cands li:hover{background:#8882}
.seoCard .cands .cw{font-size:12px;opacity:.7}
.chips{display:flex;flex-wrap:wrap;gap:6px}
.chip{border:1px solid #8886;border-radius:12px;padding:2px 8px;font-size:12px}
.kwBadge{font-size:11px;border-radius:10px;padding:1px 6px;border:1px solid #8886}
.kwBadge.blue{border-color:#4a90d9}
.kwBadge.red{border-color:#d94a4a}
.mini{font-size:12px;padding:2px 6px}
```

- [x] **Step 6: 문법을 확인한다**

```bash
node --check <(sed -n '/^<script>/,/^<\/script>/p' shopping_shorts/static/produce.html | sed '1d;$d') 2>&1 | head
```
(스크립트 태그 구조가 다르면 새 JS 구역만 임시 파일로 떼어 `node --check`)

Expected: 에러 없음

- [x] **Step 7: 앵커가 살아있는지 확인한다 — 이게 죽으면 게이트가 죽는다**

Run: `python -m pytest shopping_shorts/tests/test_produce_preview_gate.py -v -s`
Expected: 병합 전과 같은 결과 (`assert s != -1, "START 못 찾음"`이 안 뜬다)

- [x] **Step 8: 커밋**

```bash
git add shopping_shorts/static/produce.html shopping_shorts/tests/test_seo_api.py
git commit -m "feat(seo): 6단계 화면 — 근거 배지·카드별 잠금·부분 재생성"
```

---

## Task 8: 실구동 관측 — 진짜 합격 기준

**Files:** 없음(관측만). 발견한 결함은 여기서 고치고 커밋한다.

**왜 별도 태스크인가:** 앞의 테스트는 **Gemini도 유튜브도 전부 가짜다.** 아무도 실제 응답을 본 적이 없다.
이 저장소가 반복해 데인 패턴이다 — 보이스 트랙의 *"카드에서 듣는 소리 ≠ 실제 합성될 소리"*.
**값이 맞는 것과 사장님이 보는 것이 같다는 건 별개다.**

- [x] **Step 1: `.env`에 키가 있는지 확인한다**

```bash
cd "C:/Users/TheRose/Desktop/로또의 주식/.tracks/SEO"
python -c "
from shopping_shorts.config import YOUTUBE_API_KEYS
from pipeline.atoms import key_vault
print('youtube keys:', len(YOUTUBE_API_KEYS))
print('gemini live  :', len(key_vault.get_live_keys_cascade('general')))
"
```
둘 중 하나라도 0이면 `copy .env .tracks\SEO\` 후 재시도. 그래도 0이면
**"미관측"으로 정직하게 보고**하고 사장님께 알린다 — 관측한 척하지 않는다.

- [x] **Step 2: 유튜브 실측을 1회 관측한다**

```bash
python -c "
from shopping_shorts import seo_probe
from shopping_shorts.store import Store
from shopping_shorts.config import DB_PATH
s = Store(DB_PATH); s.init_db()
for r in seo_probe.probe_keywords(['빨대텀블러','보냉텀블러'], s):
    print(r['keyword'], '|', r['verdict'], '| 중앙값', r['views_median'],
          '| 소형', round(r['small_ratio'],2), '| n', r['sample_n'])
    print('   ', r['top_titles'])
"
```

**눈으로 볼 것:**
- `views_median`이 말이 되나 (0이거나 10억이면 배선이 틀렸다)
- `top_titles`가 그 키워드로 유튜브에서 검색했을 때 나올 법한 실제 한국 쇼츠 제목인가
- `sample_n`이 0~3이면 `_WINDOW_DAYS=90`이 너무 좁거나 언어 필터가 과한 것 → 기록
- `verdict`가 top_titles를 봤을 때 납득되나

**납득이 안 되면 문턱값을 조정하고 그 근거를 기록한다.** 이 숫자들엔 원래 근거가 없었다.

- [x] **Step 3: Gemini 생성을 1회 관측한다**

```bash
python -c "
from shopping_shorts import seo_generate
job = {'given_script':'가방에 내용물 샐 걱정 이제 그만. 뒤집어도 안 새는 빨대 텀블러예요. 얼음도 하루 종일 유지돼요.',
       'script_structure':{'product_category':'홈템','hook_line':'가방에 내용물 샐까',
                           'appeal':'완벽 밀폐','one_line_why':'새지 않는 텀블러','tone':'친근한 반말'},
       'headcopy':{'text':'샐 걱정 ZERO'}}
seo = seo_generate.generate(job)
if not seo: print('생성 실패 — 키 확인'); raise SystemExit
print('제목:', seo['title'])
for c in seo['title_candidates']: print(' -', c['text'], '|', c['why'], '|', c['keywords'])
print('태그', len(seo['tags']), ':', seo['tags'])
print('CTA 틱톡:', seo['cta']['tiktok'])
print('CTA 쓰레드:', seo['cta']['threads'])
"
```

**눈으로 볼 것:**
- 제목이 **한국어로 쓸 만한가.** 스키마 통과 ≠ 쓸 만함
- `why`가 진짜 근거인가, 아니면 "매력적이라서" 같은 헛소리인가
- 태그가 **정확히 20개**인가
- **틱톡 CTA와 쓰레드 CTA가 실제로 다른가** — 같으면 플랫폼 분리가 말뿐이다
- 대본에 없는 효능을 지어내지 않았나

**품질이 나쁘면 `_BASE_PROMPT`를 고치고 재관측한다.** 여기가 프롬프트 튜닝 지점이다.

- [x] **Step 4: 라이브 화면을 관측한다**

```bash
python -m uvicorn shopping_shorts.app:app --port 8872
```
브라우저로 `http://localhost:8872/produce.html` (`/`가 인증 없이 200이다 — 정적 마운트).

**눈으로 볼 것:**
1. 6단계로 이동 → `initSeo()`가 돌아 「AI로 전체 생성」 버튼이 보이나
2. 생성 → 카드 6개가 뜨나, 배지에 실측치가 붙나
3. 추천 제목 클릭 → 상단 제목이 바뀌나
4. 🔒 잠금 → ↻ 버튼이 비활성되나
5. 제목만 ↻ → **다른 카드가 그대로 살아있나** (부분 재생성의 핵심)
6. **★새로고침 → SEO가 살아있나** (= 서버 저장이 진짜 됐나. 이게 안 되면 전부 헛일)
7. 콘솔 에러 0

끝나면 `netstat -ano | grep :8872`로 PID 찾아 `taskkill`.

- [x] **Step 5: 관측 결과를 기록한다**

`handoff/SEO.md`에 **본 것과 못 본 것을 정직하게** 쓴다. 키가 없어 못 봤으면 "미관측"이라고 쓴다.
문턱값을 조정했으면 그 근거를 남긴다.

- [x] **Step 6: 커밋**

```bash
git add -A
git commit -m "docs(seo): 실구동 관측 기록 + 관측으로 드러난 수정"
```

---

## Task 9: 전체 게이트 + 병합

- [ ] **Step 1: 최신 main을 얹는다**

```bash
git fetch origin && git merge origin/main
```
충돌 나면 `store.py`는 **양쪽 컬럼을 다 살리고** row 인덱스를 다시 센다(썸네일 트랙의 `thumbnail`과
`seo_json`이 자리를 다툴 수 있다). 해결 후:

```bash
python -m pytest shopping_shorts/tests/test_seo_store.py -v -s
python -c "import shopping_shorts.app; print('OK')"
```

- [ ] **Step 2: 전체 테스트를 돌린다**

Run: `python -m pytest shopping_shorts/tests/ -q -s`
Expected: **병합 전 기준선보다 실패가 늘지 않았다.** (전부 green이 기준이 아니다 — 선존재 실패 10~11건)

- [ ] **Step 3: 사장님 확인을 받는다**

Task 8 Step 4의 화면을 사장님이 직접 보신 뒤 병합한다. **프런트는 pytest가 없어 육안이 진짜 게이트다.**

- [ ] **Step 4: 병합**

```bash
cd "C:/Users/TheRose/Desktop/로또의 주식"
py tools/track.py finish SEO
```
게이트(문법 → `import shopping_shorts.app` → pytest)를 통과해야만 main으로 간다.
막히면 **라이브는 무사**하고 트랙 폴더도 그대로다. 고치고 다시 `finish`.

---

## Self-Review

**스펙 커버리지:**

| 스펙 | 태스크 |
|---|---|
| §3 Pass 1 생성 | T5 |
| §3 Pass 2 측정 | T3(판정) + T4(배선) |
| §3 Pass 3 되먹임 | T5(`keyword_stats` 프롬프트) + T6(`only`) + T7(`seoGenerate(only)`) |
| §4 `seo_probe`, 판정표, 쿼터 방어 | T3, T4 |
| §5 `mix_jobs.seo_json`, store 3곳 | T1 |
| §5 `seo_keyword_stats` | T2 |
| §5 STATE 금지 | T7 (서버 저장 + 새로고침 관측 T8) |
| §6 플랫폼 분리 | T5(`_SCHEMA`·프롬프트), T7(탭) |
| §7 엔드포인트·프론트 훅 | T6, T7 |
| §8 테스트·앵커 함정 | 각 태스크 + T7 Step 7 |
| §8 실호출 관측 | T8 |
| §9 범위 밖 | 계획에 없음 ✅ |
| §10 리스크 1(문턱값 근거 없음) | T8 Step 2에서 실측 후 조정 |
| §10 리스크 3(동시 편집) | Global Constraints + T1 Step 1 + T9 Step 1 |

**빠진 것 1건 — 의도적:** 스펙 §7의 "태그 칩 입력+추가/삭제"를 T7에서 뺐다. 첫 구현은
읽기+잠금+재생성까지만 하고, 칩 편집은 Task 8에서 사장님이 실제로 필요하다고 하시면 넣는다(YAGNI).
**T8 Step 4에서 이 판단을 확인받는다.**

**타입 일관성:** `stat_dict`의 키(`keyword`/`region`/`views_median`/`small_ratio`/`sample_n`/
`top_titles`/`verdict`)가 T2 `put_keyword_stats` ↔ T3 `summarize` ↔ T4 `probe_keywords` ↔
T6 응답 ↔ T7 `seoStatBadge`에서 동일하다. `seo` dict의 키(`title`/`title_candidates`/`description`/
`tags`/`hashtags`/`hook_line`/`comment_bait`/`cta`/`keyword_stats`/`locked`/`generated_at`)가
T1 테스트 ↔ T5 `_SCHEMA` ↔ T6 ↔ T7 `renderSeo`에서 동일하다. 카드 id(`title`/`description`/
`tags`/`hashtags`/`hook`/`cta`)가 T5 `_ONLY_LABELS` ↔ T6 `only` ↔ T7 `seoCard`/`map`에서 동일하다.
