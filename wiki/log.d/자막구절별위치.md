# 자막 구절별 위치

- 장면꾸미기의 `이 자막만` 드래그가 비트 전체 `cap_xy`를 공유하던 문제를 구절별 `cap_xy_segs` 저장으로 분리했다.
- API·미리보기·최종 렌더가 같은 구절 좌표를 사용하며 기존 비트 좌표는 호환 유지한다.
- 라이브에서 한 구절만 이동하고 같은 비트의 다른 구절이 제자리를 유지하는 것을 확인한 뒤 원위치 복구했다.
