# 억양 규칙 v2 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development. Steps use checkbox (`- [ ]`) syntax.

**Goal:** 나레이션이 대본을 덮어쓰지 않게 하고, 비트 역할별로 억양 규칙(감탄 추임새·앞글자 강조·물음표 끝음·결론어 강조)을 적용한다.

**Architecture:** `shopping_shorts/narration_naturalize.py`의 순수 스테이지 함수들을 확장한다. 새 스테이지는 최소로 추가하고(결론어 1개), 나머지는 기존 스테이지(`fillers`·`intonation`)에 노브를 단다. 모든 규칙은 `DEFAULT_PROFILE`로 노출돼 프리셋이 덮어쓸 수 있어야 한다.

**Tech Stack:** Python 3, pytest. UI 변경 없음(작업대 노브는 이 계획 범위 밖).

## 사장님이 정한 것 (전부 청취 판정 또는 직접 지시 — 임의로 바꾸지 마라)

1. **추임새는 감탄·놀람류로.** `"음"` 같은 머뭇거림은 "의미없다"고 명시적으로 기각됨.
2. **추임새는 훅에만.**
3. **앞글자 강조 = ③ 쉼표+느낌표** (7종 청취 비교에서 선택). 예: `…들려있는, 이거!`
4. **페인포인트의 물음표 구간은 끝음을 올린다.**
5. **실용의 `끝이에요`는 `끝!이에요`로 강조한다.**
6. **속삭임(반전)·[excited](CTA)는 현행 유지** — A/B 청취에서 "지금 라이브가 제일 좋다"로 판정됨.

## Global Constraints

- **파일은 `.tracks/보이스/` 안에서만 수정한다.** main 폴더 파일은 단 하나도 건드리지 마라.
- **`.py` 파일은 LF로 쓴다** (`newline='\n'`). CRLF로 쓰면 git status에 유령 M이 뜬다.
- **기본 프로파일 경로(`merge_profile({})`)로 검증하라.** 격리 프로파일(`_p`)만으로 테스트하면 기본값의 결함이 숨는다 — 이 트랙에서 이미 당했다.
- **`_STAGES`는 함수 객체를 캡처한다.** 모듈 속성 패치(`monkeypatch.setattr(mod, "_fillers", ...)`)는 **초록불이 뜨지만 아무것도 검증하지 않는다.** 뮤테이션은 `_STAGES` 튜플 자체를 갈아끼워라.
- **"테스트 통과" ≠ "테스트가 결함을 잡는다".** 각 태스크는 뮤턴트를 심어 테스트가 실제로 죽는지 보고하라.
- **회귀 기준선(2026-07-17 실측, 커밋 `7d5e24fe`): `34 failed, 858 passed, 2 skipped`.**
  **"전부 green"이 아니라 "실패가 늘지 않았나"로 판정한다.** 선존재 실패 34건의 소재:
  `test_produce_wiki_attribution.py`(11) · `test_produce_preview_gate.py`(8) · `test_apify_client.py`(6) ·
  `test_produce_subject.py`(3) · `test_mix_render_flow.py`(2) · `test_edit_plan_build.py`(2) ·
  `test_mix_pipeline.py`(1) · `test_ffmpeg_error_reporting.py`(1).
  **이 중 `narration_naturalize.py`를 건드리는 건 하나도 없다** — 남의 트랙 코드이거나 네트워크·stdin 의존이다.
  **고치려 들지 마라. 범위 밖이다.**
- **진짜 게이트는 이것이다:** `python -m pytest shopping_shorts/tests/ -q -k "naturalize"` → **실측 124 passed(전부 green)**.
  이 숫자가 줄거나 실패가 하나라도 생기면 **네가 깬 것이다.**
- 비트 role 정본 = `edit_plan._REQUIRED_ROLES` = 훅·페인포인트·반전·실용·CTA.

---

### Task 1: 추임새 — 감탄 뱅크 + 역할 노브 + 멱등 가드

**Files:**
- Modify: `shopping_shorts/narration_naturalize.py` (`DEFAULT_PROFILE`, `_fillers`)
- Test: `shopping_shorts/tests/test_naturalize_fillers_v2.py` (create)

**Interfaces:**
- Consumes: `_LEADING_TAGS_PAT`, `_beat_selected`, `_take_count`, `_sentence_starts`, `_bump`, `ctx["role_canon"]`
- Produces: `_INTERJECTIONS` (set) — Task 없음, 이 파일 안에서만 씀

**왜 세 개가 한 태스크인가:** 뱅크만 바꾸면 감탄사가 전 비트에 붙어 지금보다 나빠진다(반전 비트가 "헐, 근데 이건…"이 된다). 세 개가 같이 들어가야 비로소 사장님이 요청한 동작이 된다.

- [ ] **Step 1: 실패하는 테스트를 쓴다**

`shopping_shorts/tests/test_naturalize_fillers_v2.py`:

```python
"""추임새 규칙 v2 — 감탄·놀람만, 훅에만, 대본을 덮어쓰지 않는다.

★2026-07-17 실측(라이브 경로 synthesize_line, 미나 1.6)으로 드러난 것:
대본에 "와, 요새…"라고 써넣었는데 엔진이 그 앞에 "[curious] 음,"을 또 붙여
**"음, 와, 요새…"**가 됐다. 사장님이 "실제 대본 넣으니 억양이 부자연스럽다"고
한 것의 정체다. fillers가 대본을 덮어쓴 것.

사장님 판정: "음~ 이런건 빼고 앞에 들어가는 추임새는 감탄 놀람 이런 추임새로
해야해. 저런건 의미없어."
"""
import pytest

from shopping_shorts.narration_naturalize import (
    DEFAULT_PROFILE, merge_profile, naturalize_detail,
)


def _run(text, role, profile=None, bi=0, bt=5):
    """기본 프로파일 경로로 돌린다 — 격리 프로파일은 기본값의 결함을 숨긴다."""
    return naturalize_detail(text, merge_profile(profile or {}),
                             beat_role=role, beat_index=bi, beat_total=bt)


def test_bank_has_no_hesitation_fillers():
    """머뭇거림('음','아','그','뭐','자')은 뱅크에서 사라져야 한다 — 사장님이 기각."""
    bank = DEFAULT_PROFILE["fillers"]["bank"]
    for w in ("음", "아", "그", "뭐", "자"):
        assert w not in bank, f"머뭇거림 '{w}'가 아직 뱅크에 있다: {bank}"
    assert bank, "뱅크가 비면 추임새 기능 자체가 죽는다"


def test_bank_is_exclamation_type():
    """감탄·놀람류만 남는다."""
    assert set(DEFAULT_PROFILE["fillers"]["bank"]) <= {"와", "오", "우와", "헐", "이야"}


def test_filler_only_on_hook_role():
    """훅에만 붙는다 — 반전이 '헐, 근데 이건…'이 되면 안 된다."""
    for role in ("페인포인트", "반전", "실용", "CTA"):
        out = _run("근데 이건 진짜 다르더라고요.", role)
        for w in DEFAULT_PROFILE["fillers"]["bank"]:
            assert not out.lstrip().startswith(w + ","), \
                f"{role}에 추임새가 붙었다: {out!r}"


def test_filler_does_appear_on_hook():
    """반대편 봉인 — 역할 게이트가 과해서 훅에서도 안 나오면 기능이 죽은 것."""
    out = _run("요새 다들 쓰는 이거.", "훅",
               {"fillers": {"intensity": 1.0}})
    bank = DEFAULT_PROFILE["fillers"]["bank"]
    assert any(out.lstrip().startswith(w + ",") or f"] {w}," in out for w in bank), \
        f"훅에 추임새가 안 붙었다: {out!r}"


@pytest.mark.parametrize("already", ["와, 요새 다들 쓰는 이거.",
                                     "헐, 이거 보셨어요.",
                                     "음, 요새 다들 쓰는 이거."])
def test_does_not_stack_on_script_that_already_has_one(already):
    """★대본에 이미 추임새가 있으면 손대지 않는다 — '음, 와,' 사고 재발 방지.

    옛 뱅크의 '음'도 막는다: 사장님이 옛 대본을 그대로 넣을 수 있다.
    """
    out = _run(already, "훅", {"fillers": {"intensity": 1.0}})
    body = out.split("]")[-1].strip() if "]" in out else out.strip()
    first_word = body.split(",")[0].strip()
    assert body.startswith(first_word + ","), f"형태가 깨졌다: {out!r}"
    # 추임새가 두 개 연달아 나오면 안 된다
    assert not any(body.startswith(f"{w}, {v},")
                   for w in ("와", "오", "우와", "헐", "이야", "음", "아", "그", "뭐", "자")
                   for v in ("와", "오", "우와", "헐", "이야", "음", "아", "그", "뭐", "자")), \
        f"추임새가 겹쳐 붙었다: {out!r}"


def test_roles_absent_means_all_beats():
    """저장된 옛 프리셋엔 roles 키가 없다 — 없으면 옛 동작(전 비트)을 유지한다.

    없는 키를 '아무 비트도 아님'으로 읽으면 기존 프리셋의 추임새가 통째로 사라진다.
    실패 방향을 '보이는 쪽'으로 잡는다.
    """
    prof = merge_profile({"fillers": {"intensity": 1.0}})
    prof["fillers"].pop("roles", None)
    out = naturalize_detail("근데 이건 다르더라고요.", prof,
                            beat_role="반전", beat_index=0, beat_total=5)
    bank = prof["fillers"]["bank"]
    assert any(w + "," in out for w in bank), f"roles 없는 프리셋에서 추임새가 죽었다: {out!r}"
```

- [ ] **Step 2: 실패 확인**

Run: `python -m pytest shopping_shorts/tests/test_naturalize_fillers_v2.py -v`
Expected: FAIL — 뱅크에 "음"이 있고 `roles` 키가 없음.

- [ ] **Step 3: 구현**

`DEFAULT_PROFILE`의 `fillers`를 교체한다:

```python
    # 뱅크가 감탄·놀람인 이유(2026-07-17 사장님 지시): 옛 뱅크 ["음","아","그","뭐","자"]는
    # 전부 **머뭇거림**이었다 — "저런건 의미없어". 릴스 훅은 말을 더듬는 자리가 아니라
    # 놀라는 자리다. roles=["훅"]인 이유: 감탄사가 반전·실용에 붙으면
    # "헐, 근데 이건…"처럼 톤이 무너진다. 추임새는 문을 여는 도구지 문장을 여는 도구가 아니다.
    "fillers":       {"on": True, "intensity": 0.2,
                      "bank": ["와", "오", "우와", "헐", "이야"],
                      "roles": ["훅"]},
```

`_LEADING_TAGS_PAT` 정의부 근처(파일 상단 패턴 모음)에 추가:

```python
# 대본이 이미 데리고 온 추임새 — 여기에 걸리면 `_fillers`는 손을 뗀다.
# 옛 뱅크(음/아/그/뭐/자)도 포함한다: 사장님이 옛 대본을 그대로 붙여넣을 수 있고,
# 그때 "음, 와," 같은 겹침이 나면 안 된다(2026-07-17 실측 사고).
_INTERJECTIONS = {"와", "오", "우와", "헐", "이야", "음", "아", "그", "뭐", "자", "어", "어머"}
# 문두 추임새 = 한글 1~2자 + 쉼표. 태그 묶음은 미리 벗겨내고 본다.
_LEADING_INTERJECTION_PAT = re.compile(r"^\s*([가-힣]{1,2})\s*,")
```

`_fillers` 본문에서 `if not text or not text.strip():` 바로 **다음**에 두 게이트를 넣는다:

```python
    # 역할 게이트 — `_whisper`와 같은 구조(설계 통일). roles가 **없으면** 전 비트에
    # 적용한다(옛 프리셋 호환): 저장된 프리셋엔 이 키가 없고, 없는 걸 "아무 비트도
    # 아님"으로 읽으면 기존 프리셋의 추임새가 조용히 통째로 사라진다.
    roles = cfg.get("roles")
    if roles is not None:
        canon = ctx.get("role_canon")
        if canon is None or canon not in roles:
            return text

    # 멱등 가드 — 대본에 이미 추임새가 있으면 손대지 않는다.
    # ★2026-07-17 실사고: 대본 "와, 요새…"에 엔진이 "[curious] 음,"을 덧붙여
    # "음, 와, 요새…"가 나왔다. 사장님의 "억양이 부자연스럽다"가 이것이었다.
    # 태그 묶음은 emotion_arc가 앞에 붙이므로 벗겨내고 본문 첫 어절만 본다.
    _m = _LEADING_TAGS_PAT.match(text)
    _body = text[_m.end():] if _m else text
    _lead = _LEADING_INTERJECTION_PAT.match(_body)
    if _lead and _lead.group(1) in _INTERJECTIONS:
        return text
```

- [ ] **Step 4: 통과 확인 + 회귀**

Run: `python -m pytest shopping_shorts/tests/test_naturalize_fillers_v2.py -v`
Expected: PASS (7건)

Run: `python -m pytest shopping_shorts/tests/ -q 2>&1 | tail -3`
Expected: 실패가 기준선(2 failed)보다 늘지 않음.

- [ ] **Step 5: 뮤테이션 — 테스트가 진짜 잡는지 확인**

각각 심고 **어느 테스트가 죽는지** 보고하라. `_STAGES`가 함수 객체를 캡처하므로 모듈 속성 패치는 무효다 — **소스를 직접 고쳐서** 심고 되돌려라.

1. 역할 게이트 `if roles is not None:` → `if False:` ⇒ `test_filler_only_on_hook_role` 사망 기대
2. 멱등 가드의 `return text` → `pass` ⇒ `test_does_not_stack_on_script_that_already_has_one` 사망 기대
3. `roles = cfg.get("roles")` → `roles = cfg.get("roles") or []` ⇒ `test_roles_absent_means_all_beats` 사망 기대
4. 뱅크에 `"음"` 되돌리기 ⇒ `test_bank_has_no_hesitation_fillers` 사망 기대

**안 죽는 뮤턴트가 있으면 그 테스트는 가짜다 — 보고하라.**

- [ ] **Step 6: 커밋**

```bash
git add shopping_shorts/narration_naturalize.py shopping_shorts/tests/test_naturalize_fillers_v2.py
git commit -m "feat(보이스): 추임새를 감탄·놀람으로, 훅에만, 대본 덮어쓰기 중단"
```

---

### Task 2: 훅 앞글자 강조 — 쉼표 + 느낌표

**Files:**
- Modify: `shopping_shorts/narration_naturalize.py` (`DEFAULT_PROFILE`, `_intonation`)
- Test: `shopping_shorts/tests/test_naturalize_hook_emphasis.py` (create)

**Interfaces:**
- Consumes: `_EMPHASIS_PAT`, `_take_count`, `_bump`, `ctx["role_canon"]`
- Produces: `_HOOK_TAIL_PAT`

**근거(사장님 청취 판정):** 7종 비교에서 **③ 쉼표+느낌표**를 골랐다. 한글엔 음절 단위 강세 지정 수단이 없다(영어의 대문자 같은 게 없다). 대신 한국어는 **억양구 첫 음절에서 피치가 오르므로**, 강조할 단어 앞에 쉼표를 찍어 그 단어를 새 억양구의 시작으로 만들면 앞글자가 뜬다. 느낌표는 뒤를 받친다 — 둘은 경쟁이 아니라 짝이다.

**⚠️ 기존 `_EMPHASIS_WORDS`로는 안 된다:** `["진짜","완전","역대급","훨씬","절대","딱"]`은 전부 **부사**다. 사장님 예시 `이거!`는 **문장 끝 지시어**라 이 목록에 없다. 훅의 마지막 어절을 잡는 별도 규칙이 필요하다.

- [ ] **Step 1: 실패하는 테스트를 쓴다**

`shopping_shorts/tests/test_naturalize_hook_emphasis.py`:

```python
"""훅 마지막 어절 강조 — 쉼표(앞글자 띄우기) + 느낌표(뒤 받치기).

사장님 청취 판정(2026-07-17, 7종 비교): ③ `…들려있는, 이거!`
한글엔 음절 단위 강세 지정 수단이 없어(대문자가 없다) 억양구 구조를 쓴다 —
쉼표로 끊으면 '이거'가 새 억양구의 첫 음절이 돼 피치가 오른다.
"""
from shopping_shorts.narration_naturalize import merge_profile, naturalize_detail


def _run(text, role, profile=None):
    return naturalize_detail(text, merge_profile(profile or {}),
                             beat_role=role, beat_index=0, beat_total=5)


def test_hook_last_word_gets_comma_and_bang():
    out = _run("요새 해외 인플루언서들 손에 하나씩 들려있는 이거.", "훅")
    assert ", 이거!" in out, f"③ 규칙(쉼표+느낌표)이 적용 안 됐다: {out!r}"


def test_hook_emphasis_does_not_double_comma():
    """대본이 이미 쉼표를 찍어 왔으면 ',, 이거!'가 되면 안 된다."""
    out = _run("요새 다들 쓰는, 이거.", "훅")
    assert ",," not in out and ", , " not in out, f"쉼표가 겹쳤다: {out!r}"
    assert ", 이거!" in out


def test_hook_emphasis_is_idempotent():
    """이미 규칙이 적용된 문장을 다시 태워도 안 망가진다."""
    once = _run("요새 다들 쓰는 이거.", "훅")
    twice = naturalize_detail(once, merge_profile({}), beat_role="훅",
                              beat_index=0, beat_total=5)
    assert "!!" not in twice, f"느낌표가 겹쳤다: {twice!r}"
    assert ",," not in twice, f"쉼표가 겹쳤다: {twice!r}"


def test_non_hook_roles_untouched_by_tail_emphasis():
    """반전·실용 문장 끝을 느낌표로 바꾸면 톤이 무너진다(속삭임 반전이 '다르더라고요!')."""
    out = _run("근데 이건 완전 다르더라고요.", "반전")
    assert not out.rstrip().endswith("!"), f"훅 아닌 비트에 느낌표가 붙었다: {out!r}"


def test_hook_single_word_sentence_is_left_alone():
    """어절이 하나뿐이면 앞에 끊을 자리가 없다 — ', 이거!'로 시작하면 안 된다."""
    out = _run("이거.", "훅")
    assert not out.lstrip().startswith(","), f"문두에 쉼표가 붙었다: {out!r}"


def test_existing_adverb_emphasis_still_works():
    """기존 intonation(부사 앞 쉼표)을 깨뜨리면 안 된다 — 반대편 봉인."""
    out = _run("이건 진짜 좋아요. 완전 좋아요. 딱 좋아요.", "실용",
               {"intonation": {"intensity": 1.0}})
    assert ", 진짜" in out or ", 완전" in out or ", 딱" in out, \
        f"부사 강조가 죽었다: {out!r}"
```

- [ ] **Step 2: 실패 확인**

Run: `python -m pytest shopping_shorts/tests/test_naturalize_hook_emphasis.py -v`
Expected: FAIL

- [ ] **Step 3: 구현**

`_EMPHASIS_PAT` 정의 아래에 추가:

```python
# 훅 마지막 어절 = 강조 대상. `_EMPHASIS_WORDS`(부사)로는 못 잡는다 — 사장님 예시
# "이거!"는 문장 끝 지시어다. 앞 어절이 있어야만(=끊을 자리가 있어야만) 매치되게
# `(?<=[가-힣])\s+`를 요구한다: 한 어절짜리 문장("이거.")은 문두에 쉼표를 찍을 수 없다.
# 끝의 문장부호는 통째로 삼켜 "!" 하나로 교체한다 — "이거….!" 같은 겹침 방지.
_HOOK_TAIL_PAT = re.compile(r"(?<=[가-힣])(\s*,)?\s+([가-힣]{1,5})\s*([.…!?]*)\s*$")
```

`DEFAULT_PROFILE`의 `intonation`을 교체:

```python
    # emphasis_roles: 마지막 어절을 "쉼표+느낌표"로 강조할 비트(2026-07-17 사장님 청취 판정 ③).
    # 훅만인 이유: 반전은 속삭이고 CTA는 [excited]가 이미 띄운다 — 거기에 느낌표까지
    # 붙이면 과해진다. 이 노브를 비우면(=[]) 옛 동작(부사 앞 쉼표만)으로 돌아간다.
    "intonation":    {"on": True, "intensity": 0.2, "emphasis_roles": ["훅"]},
```

`_intonation` 본문 **맨 앞**(`intensity` 게이트 다음)에 꼬리 강조를 넣는다. 부사 쉼표 로직은 그대로 두고 그 **뒤에** 이어붙인다:

```python
    # ── 훅 꼬리 강조 (사장님 판정 ③: 쉼표로 앞글자 띄우고 느낌표로 뒤 받치기) ──
    # 부사 강조(_EMPHASIS_PAT)보다 **먼저** 돌린다: 꼬리 치환이 문자열 끝을 바꾸는데,
    # 부사 후보 좌표는 그 앞쪽이라 영향받지 않는다(역순 적용 원칙과 같은 이유).
    if (ctx.get("role_canon") in (cfg.get("emphasis_roles") or [])):
        m = _HOOK_TAIL_PAT.search(text)
        if m:
            before = text
            text = f"{text[:m.start()]}, {m.group(2)}!"
            if text != before:
                _bump(ctx, "intonation", 1)
```

- [ ] **Step 4: 통과 확인 + 회귀**

Run: `python -m pytest shopping_shorts/tests/test_naturalize_hook_emphasis.py -v` → PASS (6건)
Run: `python -m pytest shopping_shorts/tests/ -q 2>&1 | tail -3` → 실패가 기준선보다 늘지 않음

- [ ] **Step 5: 뮤테이션**

1. `emphasis_roles` 게이트 → `if True:` ⇒ `test_non_hook_roles_untouched_by_tail_emphasis` 사망 기대
2. `f"{text[:m.start()]}, {m.group(2)}!"` → `f"{text[:m.start()]} {m.group(2)}!"` (쉼표 제거) ⇒ `test_hook_last_word_gets_comma_and_bang` 사망 기대
3. `_HOOK_TAIL_PAT`의 `(?<=[가-힣])` 제거 ⇒ `test_hook_single_word_sentence_is_left_alone` 사망 기대
4. 꼬리 강조 블록을 부사 로직 **뒤로** 옮기기 ⇒ 뭐가 죽는지 보고(안 죽으면 순서 주석이 근거 없는 것이니 그렇게 보고하라)

- [ ] **Step 6: 커밋**

```bash
git add shopping_shorts/narration_naturalize.py shopping_shorts/tests/test_naturalize_hook_emphasis.py
git commit -m "feat(보이스): 훅 마지막 어절을 쉼표+느낌표로 강조(사장님 청취 판정 ③)"
```

---

### Task 3: 페인포인트 물음표 — 끝음 올리기

**Files:**
- Modify: `shopping_shorts/narration_naturalize.py` (`DEFAULT_PROFILE`, `_endings`)
- Test: `shopping_shorts/tests/test_naturalize_question_ending.py` (create)

**Interfaces:**
- Consumes: `_take_count`, `_bump`, `ctx["role_canon"]`
- Produces: `_QUESTION_TAIL_PAT`

**근거:** 사장님 지시 "페인포인트에서 물음표구간은 끝음을 올려서". ElevenLabs는 `?`에서 자연히 끝음을 올린다 — 문제는 **대본이 `?`를 안 달거나 `endings`가 `.`을 `…`로 바꿔 끝음이 처지는 것**이다(실측: `이거.` → `이거…`). 그래서 규칙은 "`?`를 새로 만드는 것"이 아니라 **의문형 어미인데 `?`가 아닌 것을 `?`로 되돌리는 것**이다.

**⚠️ 보수적으로 가라.** `세요`는 명령형("해보세요")도 된다. 의문형이 **확실한** 어미만 넣는다.

- [ ] **Step 1: 실패하는 테스트를 쓴다**

`shopping_shorts/tests/test_naturalize_question_ending.py`:

```python
"""페인포인트 의문형 어미는 물음표로 — 끝음을 올린다.

사장님 지시(2026-07-17): "페인포인트에서 물음표구간은 끝음을 올려서".
ElevenLabs는 `?`에서 자연히 끝음을 올린다. 문제는 endings가 마침표를 `…`로
바꿔 끝음이 처지는 것(실측: "이거." → "이거…"). 그래서 `?`를 **되돌린다**.
"""
import pytest

from shopping_shorts.narration_naturalize import merge_profile, naturalize_detail


def _run(text, role, profile=None):
    return naturalize_detail(text, merge_profile(profile or {}),
                             beat_role=role, beat_index=0, beat_total=5)


@pytest.mark.parametrize("text", [
    "매번 이렇게 다 흘리고 닦기도 귀찮지 않나요.",
    "이거 하나 여는 데 몇 번을 씨름하셨을까요.",
    "다들 이러고 사는 건가요.",
])
def test_painpoint_question_tail_becomes_question_mark(text):
    out = _run(text, "페인포인트")
    assert out.rstrip().endswith("?"), f"끝음이 안 올라간다: {out!r}"
    assert "…" not in out[-3:], f"말줄임이 남아 끝음이 처진다: {out!r}"


def test_painpoint_declarative_is_left_alone():
    """평서문에 물음표를 달면 안 된다 — 과적용 봉인."""
    out = _run("매번 이렇게 다 흘리고 닦기도 귀찮아요.", "페인포인트")
    assert not out.rstrip().endswith("?"), f"평서문에 물음표가 붙었다: {out!r}"


def test_imperative_seyo_is_not_a_question():
    """'해보세요'는 명령형이다 — 물음표를 달면 안 된다."""
    out = _run("한번 열어보세요.", "페인포인트")
    assert not out.rstrip().endswith("?"), f"명령형에 물음표가 붙었다: {out!r}"


def test_existing_question_mark_survives():
    """대본이 이미 '?'로 끝나면 endings가 '…'로 덮어쓰면 안 된다."""
    out = _run("닦기도 귀찮지 않으세요?", "페인포인트")
    assert out.rstrip().endswith("?"), f"물음표가 사라졌다: {out!r}"
    assert "??" not in out, f"물음표가 겹쳤다: {out!r}"


def test_other_roles_untouched():
    """훅·CTA의 의문형까지 건드리지 않는다 — 범위를 페인포인트로 묶는다."""
    out = _run("이거 보셨나요.", "CTA")
    assert not out.rstrip().endswith("?"), f"CTA에 적용됐다: {out!r}"
```

- [ ] **Step 2: 실패 확인**

Run: `python -m pytest shopping_shorts/tests/test_naturalize_question_ending.py -v`
Expected: FAIL

- [ ] **Step 3: 구현**

`_ENDING_TAIL` 정의 근처에 추가:

```python
# 의문형이 **확실한** 어미만 넣는다. "세요"는 뺐다 — "해보세요"는 명령형이라
# 물음표를 달면 문장이 뒤집힌다. 확신이 없으면 넣지 않는 쪽으로 실패시킨다
# (평서문에 물음표가 붙는 건 사장님 귀에 바로 걸리지만, 안 붙은 건 현행과 같다).
_QUESTION_TAIL_PAT = re.compile(
    r"(나요|까요|은가요|는가요|ㄴ가요|던가요|을까요|ㄹ까요|않으세요|없으세요)\s*([.…]*)\s*$"
)
```

`DEFAULT_PROFILE`의 `endings`를 교체:

```python
    # question_roles: 의문형 어미를 물음표로 되돌릴 비트(2026-07-17 사장님 지시).
    # 페인포인트만인 이유: 끝음을 올려 되묻는 건 아픈 곳을 찌르는 자리의 문법이다.
    "endings":       {"on": True, "intensity": 0.3, "question_roles": ["페인포인트"]},
```

`_endings` 본문 **맨 앞**(`intensity` 게이트 다음)에 넣는다 — 기존 dot/tail 로직보다 **먼저** 돌려야 `.`이 `…`로 바뀌기 전에 잡는다:

```python
    # ── 의문형 → 물음표 (사장님 지시: 페인포인트 물음표 구간은 끝음을 올린다) ──
    # 기존 dot/tail 치환보다 **먼저** 돌린다: 뒤로 미루면 마침표가 이미 "…"로 바뀐
    # 뒤라 의문형을 잡아도 끝음이 처진 채로 남는다(실측: "이거." → "이거…").
    # 여기서 "?"를 박고 나면 dot 패턴(`\.(?=\s|$)`)도 tail 패턴도 더는 안 걸린다 —
    # 둘 다 마침표나 한글 어미 뒤 공백/끝을 요구하는데 "?"가 그 자리를 막는다.
    if ctx.get("role_canon") in (cfg.get("question_roles") or []):
        _m = _QUESTION_TAIL_PAT.search(text)
        if _m:
            before = text
            text = f"{text[:_m.start()]}{_m.group(1)}?"
            if text != before:
                _bump(ctx, "endings", 1)
```

- [ ] **Step 4: 통과 확인 + 회귀**

Run: `python -m pytest shopping_shorts/tests/test_naturalize_question_ending.py -v` → PASS (7건)
Run: `python -m pytest shopping_shorts/tests/ -q 2>&1 | tail -3` → 실패가 기준선보다 늘지 않음

- [ ] **Step 5: 뮤테이션**

1. `question_roles` 게이트 → `if True:` ⇒ `test_other_roles_untouched` 사망 기대
2. 의문형 블록을 dot/tail 로직 **뒤로** 옮기기 ⇒ `test_painpoint_question_tail_becomes_question_mark`의 `"…" not in out[-3:]` 사망 기대 (안 죽으면 순서 주석이 근거 없는 것이니 보고)
3. `_QUESTION_TAIL_PAT`에 `|세요` 추가 ⇒ `test_imperative_seyo_is_not_a_question` 사망 기대

- [ ] **Step 6: 커밋**

```bash
git add shopping_shorts/narration_naturalize.py shopping_shorts/tests/test_naturalize_question_ending.py
git commit -m "feat(보이스): 페인포인트 의문형 어미를 물음표로 — 끝음 올리기"
```

---

### Task 4: 실용 결론어 강조 — `끝!이에요`

**Files:**
- Modify: `shopping_shorts/narration_naturalize.py` (`DEFAULT_PROFILE`, `_STAGES`, `_conclusion` 신설)
- Test: `shopping_shorts/tests/test_naturalize_conclusion.py` (create)

**Interfaces:**
- Consumes: `_bump`, `ctx["role_canon"]`
- Produces: `_conclusion` 스테이지 (`_STAGES`에 등록)

**근거:** 사장님 지시 "실용부분에서 끝이에요를 끝!이에요 이런식으로 강조".

**왜 새 스테이지인가:** 이건 **단어 중간에 느낌표를 꽂는** 유일한 규칙이라 기존 스테이지 어디에도 안 맞는다. `intonation`은 어절 **앞**에 쉼표를 찍고, `endings`는 문장 **끝**을 만진다. 결론어 강조는 둘 다 아니다.

- [ ] **Step 1: 실패하는 테스트를 쓴다**

`shopping_shorts/tests/test_naturalize_conclusion.py`:

```python
"""실용 결론어 강조 — "끝이에요" → "끝!이에요".

사장님 지시(2026-07-17): "실용부분에서 끝이에요를 끝!이에요 이런식으로 강조".
단어 중간에 느낌표를 꽂는 유일한 규칙이라 기존 스테이지(intonation=어절 앞 쉼표,
endings=문장 끝)에 안 맞아 새 스테이지로 뺐다.
"""
import pytest

from shopping_shorts.narration_naturalize import (
    DEFAULT_PROFILE, merge_profile, naturalize_detail,
)


def _run(text, role, profile=None):
    return naturalize_detail(text, merge_profile(profile or {}),
                             beat_role=role, beat_index=0, beat_total=5)


@pytest.mark.parametrize("src,want", [
    ("뚜껑을 이렇게 딱 돌리면 끝이에요.", "끝!이에요"),
    ("이렇게 눌러주면 끝입니다.", "끝!입니다"),
    ("한 번에 완성이에요.", "완성!이에요"),
])
def test_conclusion_word_gets_bang(src, want):
    out = _run(src, "실용")
    assert want in out, f"결론어 강조가 안 됐다: {out!r}"


def test_only_on_practical_role():
    """훅·반전의 '끝이에요'까지 건드리지 않는다."""
    out = _run("근데 이건 이걸로 끝이에요.", "반전")
    assert "끝!" not in out, f"실용 아닌 비트에 적용됐다: {out!r}"


def test_is_idempotent():
    """두 번 태워도 '끝!!이에요'가 되면 안 된다."""
    once = _run("돌리면 끝이에요.", "실용")
    twice = naturalize_detail(once, merge_profile({}), beat_role="실용",
                              beat_index=0, beat_total=5)
    assert "!!" not in twice, f"느낌표가 겹쳤다: {twice!r}"


def test_unrelated_word_untouched():
    """'끝'이 결론어가 아닌 자리(예: '끝나면')엔 안 붙는다 — 과적용 봉인."""
    out = _run("작업이 끝나면 알려드릴게요.", "실용")
    assert "끝!" not in out, f"엉뚱한 '끝'에 붙었다: {out!r}"


def test_knob_can_be_turned_off():
    """words를 비우면 규칙이 꺼진다."""
    out = _run("돌리면 끝이에요.", "실용", {"conclusion": {"words": []}})
    assert "끝!" not in out, f"노브가 안 먹는다: {out!r}"


def test_stage_is_registered():
    """_STAGES에 등록 안 하면 프로파일만 있고 아무 일도 안 일어난다."""
    from shopping_shorts.narration_naturalize import _STAGES
    assert "conclusion" in [name for name, _fn in _STAGES]
    assert "conclusion" in DEFAULT_PROFILE
```

- [ ] **Step 2: 실패 확인**

Run: `python -m pytest shopping_shorts/tests/test_naturalize_conclusion.py -v`
Expected: FAIL — `conclusion` 스테이지 없음

- [ ] **Step 3: 구현**

`_intonation` 정의 **아래**에 새 스테이지를 넣는다:

```python
# 결론어 = 그 문장의 착지점. 뒤에 오는 서술어(이에요/입니다)와 붙어 흘러가면 안 들린다.
# `(?<![!])`로 이미 붙은 느낌표를 배제해 멱등성을 지킨다(두 번 태워도 "끝!!이에요"가 안 된다).
_CONCLUSION_TAIL = r"(?:이에요|이예요|입니다|이야|이죠|예요)"


def _conclusion(text, cfg, ctx):
    """실용 비트의 결론어를 "끝!이에요" 꼴로 강조(2026-07-17 사장님 지시).

    단어 **중간**에 느낌표를 꽂는 유일한 규칙이라 별도 스테이지다 — `_intonation`은
    어절 앞 쉼표, `_endings`는 문장 끝이라 둘 다 자리가 안 맞는다.
    """
    canon = ctx.get("role_canon")
    if canon is None or canon not in (cfg.get("roles") or []):
        return text
    words = cfg.get("words") or []
    if not words:
        return text
    n = 0
    for w in words:
        # (?<![!]) — 이미 강조된 것 재적용 금지(멱등).
        # 결론어 바로 뒤에 서술어가 붙은 경우만 잡는다: "끝나면"의 '끝'은 안 걸린다.
        pat = re.compile(r"(" + re.escape(w) + r")(?<![!])(" + _CONCLUSION_TAIL + r")")
        text, k = pat.subn(r"\1!\2", text)
        n += k
    if n:
        _bump(ctx, "conclusion", n)
    return text
```

`DEFAULT_PROFILE`에 추가(`intonation` 바로 아래):

```python
    # 결론어 강조(2026-07-17 사장님 지시): "실용부분에서 끝이에요를 끝!이에요 이런식으로".
    # roles=["실용"]인 이유: 착지점을 때리는 건 방법을 보여주고 끝맺는 자리의 문법이다.
    "conclusion":    {"on": True, "roles": ["실용"], "words": ["끝", "완성"]},
```

`_STAGES`에 등록 — **`intonation` 앞**에 넣는다:

```python
    ("whisper", _whisper),
    ("conclusion", _conclusion),
    ("intonation", _intonation),
]
```

- [ ] **Step 4: 통과 확인 + 회귀**

Run: `python -m pytest shopping_shorts/tests/test_naturalize_conclusion.py -v` → PASS (8건)
Run: `python -m pytest shopping_shorts/tests/ -q 2>&1 | tail -3` → 실패가 기준선보다 늘지 않음

- [ ] **Step 5: 뮤테이션**

1. `_STAGES`에서 `("conclusion", _conclusion)` 제거 ⇒ `test_stage_is_registered` + `test_conclusion_word_gets_bang` 사망 기대
2. `roles` 게이트 제거 ⇒ `test_only_on_practical_role` 사망 기대
3. `(?<![!])` 제거 ⇒ `test_is_idempotent` 사망 기대
4. `_CONCLUSION_TAIL`을 `r".*"`로 넓히기 ⇒ `test_unrelated_word_untouched` 사망 기대

- [ ] **Step 6: 커밋**

```bash
git add shopping_shorts/narration_naturalize.py shopping_shorts/tests/test_naturalize_conclusion.py
git commit -m "feat(보이스): 실용 결론어 강조(끝!이에요) 스테이지 신설"
```

---

### Task 5: 실대본 실음성 검증 — 규칙 4개가 같이 돌 때

**Files:**
- Create: `scratchpad/rules_v2_probe.py` (커밋하지 않는다 — 산출물만 확인)
- Test: `shopping_shorts/tests/test_naturalize_rules_v2_e2e.py` (create)

**왜 필요한가:** 태스크 4개가 각자 초록불이어도 **같이 돌 때** 서로를 밟을 수 있다. 특히 `caps.max_tags_per_beat`(2)와 `max_tags_total`(3)이 새 규칙들과 경쟁한다 — 이 트랙에서 이미 한 번 당했다(손으로 박은 감정태그가 캡을 먹어 whisper 샘플이 안 속삭였다).

- [ ] **Step 1: 실패하는 테스트를 쓴다**

`shopping_shorts/tests/test_naturalize_rules_v2_e2e.py`:

```python
"""규칙 4개가 **같이** 돌 때 서로를 밟지 않는지 — 5비트 실대본 전수.

★이 트랙의 실사고 재발 방지: 스테이지가 각자 맞아도 캡(max_tags_per_beat)을
먼저 먹는 쪽이 이겨 뒤 스테이지가 조용히 밀려난다(2026-07-17: 손으로 박은
감정태그가 캡을 먹어 whisper 샘플이 안 속삭였다 — 탭 이름이 거짓말이 됐다).
"""
from shopping_shorts.narration_naturalize import merge_profile, naturalize_detail

SCRIPT = [
    ("훅", "요새 해외 인플루언서들 손에 하나씩 들려있는 이거."),
    ("페인포인트", "매번 뚜껑 열다가 다 흘리고 닦기도 귀찮지 않나요."),
    ("반전", "근데 이건 완전 다르더라고요."),
    ("실용", "뚜껑을 이렇게 딱 돌리면 끝이에요."),
    ("CTA", "지금 링크에서 확인해보세요."),
]


def _run_all():
    prof = merge_profile({})
    return {role: naturalize_detail(t, prof, beat_role=role,
                                    beat_index=i, beat_total=len(SCRIPT))
            for i, (role, t) in enumerate(SCRIPT)}


def test_hook_has_exclamation_filler_not_hesitation():
    out = _run_all()["훅"]
    for bad in ("음,", "그,", "뭐,", "자,"):
        assert bad not in out, f"머뭇거림이 살아있다: {out!r}"


def test_hook_tail_emphasised():
    assert ", 이거!" in _run_all()["훅"]


def test_painpoint_rises():
    assert _run_all()["페인포인트"].rstrip().endswith("?")


def test_reversal_still_whispers():
    """사장님 A/B 판정 '지금 라이브가 제일 좋다' — 반전 속삭임은 건드리지 않는다."""
    assert "[whispers]" in _run_all()["반전"]


def test_practical_conclusion_emphasised():
    assert "끝!이에요" in _run_all()["실용"]


def test_no_beat_exceeds_tag_cap():
    """새 규칙이 태그를 밀어내지 않는지 — 캡 경쟁 봉인."""
    import re
    for role, out in _run_all().items():
        assert len(re.findall(r"\[[^\]]+\]", out)) <= 2, f"{role}: 태그 초과 {out!r}"


def test_no_doubled_punctuation_anywhere():
    for role, out in _run_all().items():
        for bad in ("!!", ",,", "??", ", ,"):
            assert bad not in out, f"{role}에 {bad!r} 겹침: {out!r}"
```

- [ ] **Step 2: 실패 확인 → Step 3: 통과할 때까지 고친다**

Run: `python -m pytest shopping_shorts/tests/test_naturalize_rules_v2_e2e.py -v`

**여기서 실패가 나면 그게 이 태스크의 존재 이유다.** Task 1~4 중 어느 규칙이 다른 걸 밟는지 찾아 고쳐라. 고칠 때 **앞 태스크의 테스트가 계속 통과하는지** 확인하라.

- [ ] **Step 4: 실음성 뽑기 (실호출 — 크레딧 소모)**

`scratchpad/rules_v2_probe.py`를 만들어 **라이브 경로**(`synthesize_line`)로 5비트를 뽑는다.

⚠️ **`synthesize_tts`를 직접 부르지 마라** — 파이프라인을 따로 조립하는 꼴이라 샘플이 실제 출력과 갈린다(2026-07-15 whole-branch 리뷰가 잡은 seam 10건이 정확히 그 패턴).

⚠️ **키는 `os.environ`에서만 온다.** 맨몸 `python3`로 띄우면 키가 0개가 되고 `tts.py`가 **글자 수로 길이를 추정한 무음 mock**을 쓴다(2026-07-17 실측: -91dB 9개를 진짜 음성으로 착각할 뻔했다). 서버에서 돌린다면 반드시:
```bash
set -a && . /etc/shopping-shorts.env && set +a
```

**뽑은 뒤 무음이 아닌지 반드시 검증하라**(이게 이 태스크의 핵심 자물쇠):
```bash
ffmpeg -i <f>.mp3 -af volumedetect -f null /dev/null 2>&1 | grep mean_volume
```
`-91 dB`가 나오면 무음 mock이다 — **그 산출물은 버려라.** 정상은 -18~-24dB.

미나 프리셋(라이브 그대로): `voice_id="aiUUgjHa4mpHf6UenZuf"`, `settings={"stability":0.55,"similarity_boost":0.78,"style":0.15}`, `speed=1.60`, `silence_trim="mid"`, `model_id="eleven_v3"`.

- [ ] **Step 5: 보고**

보고서에 **엔진을 통과한 5비트 문장 전문**과 각 파일의 **길이·mean_volume**을 적어라. 사장님이 귀로 판정할 자료다.

- [ ] **Step 6: 커밋** (테스트만 — probe는 scratchpad라 커밋하지 않는다)

```bash
git add shopping_shorts/tests/test_naturalize_rules_v2_e2e.py
git commit -m "test(보이스): 억양규칙 v2 5비트 합동 검증"
```
