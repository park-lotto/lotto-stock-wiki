# 구절컷반영 — 자막을 쪼개면 장면은 그대로, 실험실도 즉시 반영 (2026-09-11)

> 소유 트랙: 구절컷반영. 고객 오류 접수(cid 357, job 6534d20ee935 '대형수납함바퀴')에서 출발.

## 증상 → 원인 (실측)
- 완성본 2번 비트가 하양(구절1)→남색(구절2, 0.57초)→하양(구절3)→남색(구절4). 남색 조각이 두 번.
- 서버 로그: 14:46 실험실 열림(자막 2덩어리→컷 2개) → 15:40 제작소 자막 줄나누기 9회(4줄) → 16:15 렌더.
- 규칙: 컷 수 = 자막 구절 수, 조각 < 구절이면 `k % 조각수` 순환(09-02 사장님, b412934aa).
  → 4구절 × 2조각 = 1,2,1,2. 실험실은 열 때 한 번만 자막을 받아 옛 2개로 그림(화면≠결과물).

## 사장님 결정 (2026-09-11)
- "줄을 4칸으로 바꾸면 장면은 2개인데 줄만 4개면 편하잖아" / "굳이 믹스로 가서 또 2장 넣어야 하잖아"
  → 순환(1,2,1,2) **폐기**, **이어붙임**(1,1,2,2). 장면 수 = 조각 수, 자막만 늘어난다.
  (처음 "1212 기존으로"는 A/B 설명이 어려워 나온 답 — 이어서 명확히 정정하셨다)

## 고친 것
| 파일 | 무엇 |
|---|---|
| `video_assemble.py _plan_phrase_clips` | `idx = k if 구절≤조각 else (k*조각수)//구절수` — 이어붙임 |
| `static/scene_play.js planClips` | 같은 식(`Math.floor(k*segments.length/nPhrase)`) — 화면=결과물 |
| `static/produce.html` | `_labRefreshCaptions()` — 자막 줄 저장·되돌리기 뒤 실험실 iframe에 갱신 신호 |
| `static/scene_lab.html` | `window.refreshCaptions(i)` — 서버 자막 다시 받아 컷 다시 그림 + 안내("장면은 m개 그대로") |
| tests | `test_caplines_refresh_lab.py` 4건 신규 · `test_phrase_sync_plan.py` 순환→이어붙임으로 갱신(+고객 사고 모양 1,1,2,2 테스트) |

## ⏭ 다음
- [ ] 배포 후 라이브 실측: 실험실(job 6534d20ee935) 컷이 2장면으로 보이는지 / 자막 줄 바꾸면 즉시 갱신되는지 / 실제 렌더 1회
- [ ] 이 규칙 변경으로 09-02 테스트 의미("조각 빼도 앞자리 유지")는 첫 자리만 보장 — 사장님이 09-02 증상을 다시 제보하면 여기부터 본다

## 2026-09-11 라이브 실측 (배포본 d7a847e2c, job 6534d20ee935 비트1)
- 서버 `_plan_phrase_clips`: tts 2.664 · 구절 4 · 조각 2 → `s3,s3,s1,s1` (장면 전환 2회)
- 브라우저 `planClips(lists[1], beatDur(1), STRETCH[1], 1)` 배포본 실행: `s3,s3,s1,s1` — 서버와 동일
- `window.refreshCaptions(1)` 라이브 호출 → true, 서버 자막 4줄(no-store) 다시 받음
- 배포된 `/produce`에 `_labRefreshCaptions` 정의 1 + 호출 2(저장·되돌리기) 확인
- ⚠️ 못 한 것: 고객 job에 실제 caplines 저장·재렌더는 안 함(고객 final.mp4·자막을 덮어쓰게 되므로). 저장→실험실 갱신 경로는 코드 존재 + refreshCaptions 단독 실측까지.

## 2026-09-11 17:30 실제 렌더 실측 (관리자 테스트 job b08ef18a6994 비트0, 조각 2·자막 4줄)
- 브라우저에서 caplines 저장 → `refreshCaptions(0)` → 실험실 자막 4줄로 즉시 갱신, planClips = seg-3(10.6→11.1 이어서), seg-1(4.7→5.79 이어서)
- `/api/mix/render` 실제 렌더(done 17:29:21) → 0~3초 프레임 15장 타일 육안 확인: 장면A(이어폰 손) "독일·천재를" → 1.0초 크로스페이드 → 장면B(케이스) "돈방석에 앉힌·제품의 정체" 끝까지. **장면 2개, 되돌아옴 없음 = 1,1,2,2 렌더 확인**
- 테스트 뒤 caplines reset으로 원복. 고객 job(6534d20ee935)은 손대지 않음

## 2026-09-11 핀터레스트 원클릭 담기 (고객 "담기 버튼이 없다")
- 뿌리: 담기 스크립트가 핀터레스트를 몰랐다(로더 @match·확장 매니페스트·서버 _GRAB_DOMAINS 셋 다 없음). 렌즈 📌 버튼은 pinterest.com 새 탭.
- 붙인 것: 서버 _GRAB_DOMAINS pinterest / grab.user.js @match + v2.5.0 / manifest 1.10.0 / grab_logic LOGIC_VER 20260911 — 핀 페이지 플로팅, 검색 그리드 [data-test-id="pin"] 래퍼마다 📥(a는 0x0, 영상 핀은 <video>만 있어 img|video·poster)
- 실측: kr.pinterest.com 검색 20카드에 버튼 부착·클릭 시 페이지 안 넘어감·href /pin/숫자/ / /api/grab 핀 URL → "담겼어요" 성공
- 고객 안내: Tampermonkey는 자동업데이트(권한 확인 1회) / 확장 zip은 /grab에서 다시 받아야
