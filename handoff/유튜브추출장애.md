# 유튜브 1단계 영상추출 장애 (2026-08-30)

## 제보
cid 110 이준연 — "1단계 영상추출이 잘 안되요"
`https://shoppingshorts.duckdns.org/produce?work=e550d2b07f0e` (작업명 "미니 돌돌이")
bug_reports id=11, 23:23 KST 접수.

## 실측 결론 — 코드 문제 아님. 유튜브 봇차단 + 릴레이 에이전트 정지 (2겹)

담은 영상 5개 중 **인스타 2개는 정상 추출, 유튜브 3개 전부 `failed_download`**.
(script_extracts: 인스타 2건 있음 / 유튜브 3건 rows=0)

### 서버에서 직접 재현 (추측 아님)
```
python3 -m yt_dlp https://www.youtube.com/shorts/oUNTVzbj4_Y
→ ERROR: Sign in to confirm you're not a bot.
```
- player_client 6종(default·tv·web_safari·ios·mweb·android_vr) **전부 동일 실패**
- `/home/ubuntu/cookies/youtube_cookies.txt`(2,091B) 붙여도 동일 실패 = 로그아웃/만료 쿠키
- 같은 영상 `/shorts/`·`/watch?v=` 둘 다 실패 → URL 형태 문제 아님
- yt-dlp 2026.07.04 (약 2개월 경과)

### 설계상 방어선과 그 상태
`media_download.py:527` — 유튜브는 서버 데이터센터 IP가 막히는 걸 **이미 알고** 있어
우선순위 `프록시(B) > 릴레이(A) > 직접`으로 짜여 있다. 그런데 둘 다 죽어 있었다:

| 방어선 | 설정/상태 | 결과 |
|---|---|---|
| `YTDLP_PROXY` (B안) | `/etc/shopping-shorts.env`에 **빈 값** | 미사용 |
| `YT_RELAY` (A안, 집PC 115.138.49.105) | `YT_RELAY_ENABLED=1` | **오늘 2번 깨짐** |

### yt_relay 성공률 — 어제까지 멀쩡, 오늘 붕괴
```
08-29  done=126 failed=  0 pending= 0   100%
08-30  done= 13 failed= 77 pending=47     9%   ← 오늘
```
**두 겹으로 깨졌다(둘 다 오늘):**
1. **04:00 UTC(13시 KST)** — 에이전트는 도는데 yt-dlp가 실패.
   집PC(주거용 IP)까지 같은 `Sign in to confirm you're not a bot` → 유튜브가 최근 차단을 넓힌 것.
2. **13:30 UTC(22:30 KST)** — 에이전트가 **작업을 아예 안 집어감**.
   마지막 `/api/yt_relay/next` 폴 = **22:18 KST**, 그 뒤 81분간 무응답 → pending 43건 적체.
   (제보 시각 23:23은 이 구간 안 → 고객은 영원히 "분석 중"을 봄)

### 코드 회귀 아님
`git log --since=08-29 -- media_download.py yt_relay.py app.py` → 해당 경로 변경 **0건**.

## 화면이 거짓말한 부분 (별도 버그)
서버는 3분 타임아웃마다 재시도만 하고 `gave_up`을 안 세워, 프론트
`renderSourceAnalysis`가 8초마다 **무한 폴링**했다.
실측: 23:19~23:29 사이 cid110 혼자 `source_brief` **293회**
(youtube 3종 각 81회 — 제보 후에도 계속). 고객 화면엔 "⏳ 분석 중" 문구만.

## 다음 할 일
- [ ] **집PC 릴레이 에이전트 재기동** (22:18 KST 이후 정지) ← 즉시
- [ ] 유튜브 쿠키 갱신: 로그인 상태 브라우저에서 재추출(현 파일은 로그아웃본)
- [ ] `YTDLP_PROXY`에 주거용 프록시 지정 = PC 의존 제거(설계상 B안이 상위)
- [ ] yt-dlp 상시 최신화(봇차단 우회는 버전에 민감)
- [ ] 실패 N회 후 `gave_up` 확정 → 무한폴링 차단 + 화면에 사유 표기
- [ ] `failed_download`만 찍지 말고 **yt-dlp stderr 원문**을 로그로 (이번 조사 대부분이 이것 때문)
