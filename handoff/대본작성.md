# 대본작성 — 이야기 작가 대본이 씨앗과 같게 나오던 것 (2026-09-26~27, 사장님 PC)

## 사장님 지적
- "대본이 씨앗이랑 거의 똑같다. 차별 포인트는 기능·특징·장점" (work 28499a00008a 티라미수)
- "「OO의 정체」를 골랐는데 결과가 안 나온다" (work 65358b12dd6e 이어폰)
- "다른 소스에 나온 고조·반전 장면을 정말 쓰는지, 쓸 수밖에 없는 구조인지"
- 벤치마킹 원문: docs/nova_writer_script_2026-09-22.md (노바: 씨앗 7줄 → 18줄, 늘어난 건 씨앗에 없던 셀링포인트)

## 원인(실측)
- extract_feats가 '제일 센 3~4개'만 → 대부분 씨앗이 이미 말한 특징. 반전이 씨앗 반전 문장을 거의 그대로.
- write에 씨앗 원문 통째 + "씨앗 특징 금지" 규칙 없음. 줄의 feat 번호와 글 내용이 달라도 검사 없음.
- AI 장면 매칭이 특징↔근거 컷을 모르고 코드 매칭을 덮어씀.
- 첫 줄 꼴 은행(story_hook)이 고른 스타일의 제목 틀 지시를 지움(write의 hook_angle="") — 0순위-B.

## 고침 (shopping_shorts/story_writer.py)
- 특징 후보 5~6 + seed_points(씨앗 셀링포인트 목록 먼저) + seed_quote/seed_point → pick_diff_feats(코드가 고름):
  새 특징(영상 수 순), 씨앗 특징 최대 1개(고조1), 반전 = 새 특징 1위(맨 뒤, 돌리지 않음). _in_seed_points 낱말 대조로 인용 누락 보완.
- _diff_rule / _diff_violations: 새 2개↑면 고조2도 새것, 1개면 반전 전용·고조 서로 다른 특징. 반전·고조2가 씨앗 낱말 2개↑면 1회 재작성.
  twist_feat 안 적으면 코드가 채움.
- 장면 고정: 특징 줄은 근거 컷(from_cuts) 안에서만 — 매칭 방식(AI/코드) 무관.
- 고른 스타일: 제목 틀 중 채울 수 있는 것을 코드가 채워 강제(_fill_nodup: 틀 낱말과 겹치는 빈칸 낱말 제거), 못 채우면 스타일 이름 각도.
  마무리는 스타일 land 문구 중 하나로 강제. 은행은 자동 1안에만.
- 반전 신호어 중복 제거(_TWIST_LEAD: "진짜 충격인 건"·"충격은" 등).
- 초안에 feats_meta·seed_points·writer_note(diff_retry·locked_lines·n_new) 저장(점검용).

## 검증
- 테스트 test_story_writer_link 등 33 통과(새 검사 10개 중 9개가 라이브 코드에서 실패 확인).
- tools/script_diff/dump_works.py(서버 읽기 전용) + compare.py(같은 입력·라이브 대본과 같은 잣대).
- 로컬 5작업: 반전 씨앗 반복 라이브 4안 → 0안, 근거 컷 61/61. (5작업은 로컬 키 소진 no_keys)
- 서버·사장님 Vertex(gemini-3.6-flash, 10호출 전부 Vertex OK·키풀 0): 반전 씨앗 반복 0/4, 스타일 첫줄 2/2, 근거 컷 28/28, 신호어 중복 없음.

## 한계·다음
- 영상들에 새 특징이 0~1개면 고조2는 씨앗 특징 허용(지어낼 수 없다) → 반전만 차별.
- 인스타(존댓말) 이야기 작가는 반전 칸이 없어 이번 규칙 미적용 — 별도 작업.
- compare.py의 '스타일 첫줄 준수' 자동 판정식은 부정확(눈으로 확인함).
