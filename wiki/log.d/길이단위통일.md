# 길이단위통일

## 2026-08-24 (회사PC)
- 대본 길이 "또 길어진다"의 뿌리를 잡았다 — 6번째 제보, 앞선 5번은 전부 배수만 만졌다.
- **뿌리: 예산은 raw(공백 포함), 판정은 norm(공백 제외).** 실측 norm/raw=0.7395(전사 693편).
  → [바꾸기] 상한이 26% 헐렁(실효 1.8배 = 조이기 전과 동일), 62% 긴 문장이 통과.
  → 더 큰 건 **스타일별 밀도가 통째로 죽어 있던 것**: raw 밀도가 norm 천장을 항상 넘어
    승인 스타일 11개가 100% 같은 창(155~222자)을 썼다. 밀도를 240으로 적든 327로 적든 동일.
- 고침: `norm_chars_per_30s()`(DB→판정 경계 하나) + `beat_len()`(길이 재는 함수 하나).
  ★DB 값은 안 고쳤다 — 환산해 넣으면 저장된 수의 뜻이 조용히 바뀌고 다음 재측정이 raw를 되돌린다.
- 검증: 재측정 교차검증 2% 내 일치 / 밀도 창 1→4종류 / E2E 62% 문장 거부 /
  게이트 통과율 43→45%(안 빡빡해짐) / 사보타주 2종 / 5289 passed·기준선 동일.
- ⏭ 별건: 실측 발화속도 6.64 vs 상수 7.41(천장 12% 낙관). 성우 speed가 역산돼 있어 별도 트랙.
