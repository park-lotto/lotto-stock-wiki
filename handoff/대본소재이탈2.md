# 대본소재이탈2 — "후보 2개인데 다른 내용이 두 개씩" 재발 (2026-09-11, 사무실 PC)

> 소유 트랙: 대본소재이탈2. 이 파일은 이 트랙 세션만 수정한다.
> 앞선 기록: `handoff/대본소재이탈.md`(09-07 스타일 예시 가드) · 커밋 `4a8a66613`(09-09 소재 일치를 치명으로)

## 사장님 제보
"대본생성이 2개로 나올 때 고질적으로 다른 내용이 두 개씩 나오는데 몇 번을 수정한 건데
아스트라(gpt-6-astra, Codex) 불러서 페이블(Claude)이랑 원인 규명하고 고쳐"

## 실측 (서버 reference.db, 2026-09-11)
- work `79e2c2b40481`(cid 57), s2.picked=[57, 53]. 재료 sources **6편 전부 '반려동물 털 제거 젤 패드'**.
- draft0 (style 57): "여러분 **다이소** 가면 이거 무조건 데려오세요… 제 지인이 **다이소 매니저**로 있거든요…" ← 재료에 다이소 없음
- draft1 (style 53): 정상. → **같은 요청에서 한쪽 스타일만 샜다 = 차이가 원인**(0순위-B 갈라보기)
- spine 57 = "다이소 내부인형", situation "인스타 릴스 · 다이소축", source_count 1, fit ["다이소형","홈템","뷰티"].
  templates_json의 **reveal 6틀 전부 "저희 언니가 다이소 점장인데 / 제 지인이 다이소 매니저로…"**, 본문에 다이소 22회.
- 최근 30일 57을 고른 작업 **109건 중 50건(46%)**: 재료에 다이소 없는데 초안에 다이소 등장(진짜 오류율).
- approved 스파인 중 판매처가 문장틀에 박힌 건 **57 하나뿐**. 진행 중(step<6) 작업 0건.
- 서버 로그 12:04 `assemble_off=1 → 조립은 건너뛰되…` — **조립이 전역으로 꺼져 있다**(08-21 사장님 결정).

## 원인 사슬 (아스트라 검토로 보강·정정)
0. 08-19 다이소 **전용 조립 템플릿**(57)을 일반 스타일 목록에 fit=홈템(+08-21 뷰티)으로 노출. "재료가 다이소일 때만"이란 조건은 코드 어디에도 없다.
1. 08-21 조립 OFF → 57은 조립을 한 번도 안 타고 **항상 일반 생성기(generate_one_style)로 폴백**.
2. `bank_assemble.style_block`이 문장틀을 "빈칸만 채워라, 틀 자체를 새로 짓지 마라"로 강제 → 모델은 **지시대로** 다이소 점장 스토리를 그대로 썼다. 이탈이 아니라 지시 준수.
3. 소재 격리 가드(`style_profiles._SUBJECT_GUARD`, `bank_assemble` 631)는 예시·은행 블록에만 있고 **문장틀 블록엔 없었다**. 게다가 반대 지시.
4. `script_gate` '소재 일치'는 우리 제품 토큰이 **하나라도** 있으면 OK(포함 검사) → "반려동물"이 있으니 통과. 판매처·인맥 허위는 검사 대상이 아니었다(아스트라가 순수함수로 재현: `소재 일치=True, fatal_fail=""`).
5. **공통 은폐 안내의 정답 예시 자체가 "여러분 다이소 가면…"** (bank_assemble 532) — 57만 고쳐도 다른 은폐 스타일이 다이소를 배운다. ★내가 놓쳤고 아스트라가 잡았다.
6. 09-07 정리는 "우승패턴: <제품명>" 이름만 걸러 26~45를 내렸고 57은 이름에 제품명이 없어 생존.
7. `scripts/seed_daiso_spine.py --apply`는 status가 approved가 아니면 **무조건 approved로 되돌리고** 템플릿·fit까지 덮어쓴다 → pending으로 내려도 재승인 통로가 열려 있었다(수동 실행이라 자동 재발은 아님).

아스트라가 기각한 내 가설: `_sanitize()`(중괄호 치환뿐)·`hook_conceal`(제품 빈칸 틀만 제외)은 판매처와 무관.
아스트라 정정: 53/109(→50/109)는 "조합 비율"이지 오류율이 아니라 별도로 쟀다 / `aa99d4e33`은 소재 차단이 아니라 소구점 확장 커밋 — "3번 고쳤는데 재발"이 아니라 **서로 다른 실패를 같은 증상명으로 부른 것**.

## 고친 것 (이 커밋)
| 파일 | 무엇 |
|---|---|
| `shopping_shorts/script_gate.py` | `RETAILERS` 사전 + 검사 **'재료 밖 판매처'**(대본에 판매처가 나왔는데 재료 원문에 없으면 실패) + `FATAL_CHECKS`에 추가. `check(materials_text="")` 인자 신설 — 안 주면 검사 없음(회귀 0) |
| `shopping_shorts/script_generate.py` | `_materials_text(sources)`(전사 원문+제품명, ★facts_block 아님) → 게이트에 배선. 치명 안내를 원인별로: 판매처 이탈이면 **"이 재료엔 다른 스타일을 고르세요"**(종전 "재료 부족"은 오처방) |
| `shopping_shorts/bank_assemble.py` | 문장틀 안내에 **사실성 우선** 문구(판매처·소속·인맥·가격·판매실적은 사실 주장 — 재료에 근거 없으면 그 틀을 쓰지 마라) / 은폐 예시·트렌드 예시에서 '다이소' 제거 |
| `scripts/seed_daiso_spine.py` | **pending은 되살리지 않는다**(사람이 내린 것) — 템플릿·fit 덮어쓰기도 멈춤 |
| `tests/test_script_retailer_leak.py` | 7건: 실제 오류 대본으로 게이트가 잡는지 / 치명 / 재료에 다이소 있으면 통과 / 인자 없으면 검사 없음 / generate_one_style이 버리고 reason=판매처이탈 / 공통 안내에 다이소 없음 / seed가 pending 존중 |

관련 테스트 50파일 **518 passed**(회귀 0).

## ✅ 서버 데이터 조치 (2026-09-11 사장님 승인 후 실행)
- **spine 57 → `pending`** — `/api/pattern/spine/status` {id:57,status:'pending'}(관리자 브라우저). 스타일 목록 16→15.
  부작용: [57,53]을 고른 작업은 53만 생성돼 **1안**이 나온다(허위보다 낫다). 진행 중 작업 0건.
  ★57을 되살리려면 문장틀에서 '다이소 점장/매니저/직원' 6틀을 중립으로 고친 뒤(또는 fit을 다이소형만으로) 승인.
  백업은 못 만들었다(DB 쓰기 SSH가 분류기에 막힘) — 되돌리기는 같은 API로 approved.

## ✅ 라이브 실측 (2026-09-11 13:24~13:26, DEPLOY_NOW + 재시작 후, 관리자 계정)
같은 재료(job e09fcc6d0258, 반려동물 털 패드 6편)로 57을 잠시 approved로 되살려 `style_ids:[57,53]` 생성 2회.
- 라이브 서버 코드로 게이트 직접 호출: 실제 오류 대본 → `재료 밖 판매처=False`, 재료에 다이소가 있으면 `True`.
- 생성 응답 tries(57): 1회차 fails에 **'재료 밖 판매처'** → 재작성 → 3회차에 또 → 4회차 최종 안은 **다이소 없음**
  ("여러분, 이거 사세요. 털 지옥에서 탈출하게 해 줄 꿀템 발견했거든요…"). 53도 다이소 없음.
  = 모델은 여전히 다이소를 넣으려 하지만 **게이트가 잡고 재작성이 걷어낸다.** 끝까지 남으면 FATAL로 버려진다.
- 첫 번째 생성(13:24)도 초안 2개 모두 다이소 없음(script_usage: spine 57/53).
- 실측 후 57은 다시 pending으로 되돌렸다(목록 15개 확인).
- ⚠️`script_drafts`엔 style_id가 안 남는다(save_draft가 안 넘김) — 스타일 매핑은 `script_usage.spine_id`로 봐야 한다.

## ⏭ 다음 (아스트라 권고 중 이번 범위 밖)
- [ ] 배포 후 **라이브 실측**: 같은 젤 패드 재료로 57 단독 / 53 단독 / 둘 동시 생성 → 57은 반려(판매처이탈)·53은 정상인지. 사장님 계정 작업 생성이라 승인 후.
- [ ] 부분 재생성(`script_generate.py` ~931)·조립 경로(`spine_fill.py` ~843)에도 같은 검사 배선 — 지금은 전체 생성만 막는다. 조립은 꺼져 있어 당장은 안전.
- [ ] 승인/템플릿 변경 게이트: `set_spine_style()`이 승인 여부 무관하게 템플릿을 덮어쓴다(store.py ~4541). 승격 시 문장틀에 RETAILERS가 박혀 있으면 막기.
- [ ] 조립 불가 스타일이 **생성기로 조용히 폴백**하는 구조(app.py ~3431) 자체 재검토 — 조립 전용 템플릿은 생성기로 넘기지 말고 "이 재료로는 못 만든다"로 알리기.
- [ ] `RETAILERS`는 고유명사만 본다. '지인이 점장'·'전국 품절' 같은 **고유명사 없는 허위**는 사실검증 몫 — 별건.

## 아스트라 호출법 (재현용)
```
codex exec -m gpt-6-astra -s read-only -C <repo> -c 'model_reasoning_effort="high"' "$(cat 프롬프트.md)" < /dev/null
```
read-only라 파일은 못 고치고, 게이트 순수함수를 직접 실행해 재현까지 해줬다. 출력은 UTF-8로 읽어야 한다(콘솔은 cp949로 깨진다).
