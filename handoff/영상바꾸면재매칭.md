# 영상바꾸면재매칭 — 핸드오프

**소유 트랙**: 영상바꾸면재매칭 (2026-09-21, PC TheRose)

## 제보 (회원 이윤정님, 2026-09-21 16:44)
"영상대본믹스에서 재봉틀 영상을 잘못 넣어서 빼고 2개 다른 영상 넣었는데, 수정을 해도 계속 미니 재봉틀 영상이 나옵니다."

## 뿌리 (코드로 확인)
- 서버 `/api/produce/mix/start`는 요청 body의 urls로만 job을 만든다 → 서버가 옛 영상을 끼워 넣는 게 아니다.
- 3단계 진입(`initMatch`)은 **대본 지문(script_hash)만** 대조해 다시 매칭했다. 영상만 바꾸고 대본이 같으면 옛 job(재봉틀 포함)이 그대로 남는다.
- ⚠️ 이윤정님의 실제 job은 **못 봤다** — 운영 DB 조회가 권한 분류기에 막힘. 위는 코드 경로 근거다.

## 고친 것 (트랙 브랜치에만 있음 — ★라이브 미배포, 사장님 승인 대기)
- `app.py` `_sources_hash(urls)` 신설 + `/api/mix/status` 응답에 `sources_hash`.
- `produce.html` `_sourcesHash` 신설, `_remixIfScriptChanged` → `_remixIfChanged`(대본 **또는** 담은 영상이 바뀌면 다시 매칭).
  - 옛 job(지문 없음)·영상풀 빈 화면·도는 중에는 재매칭 안 함(매칭=과금).
  - 순서·중복·공백은 같은 재료로 본다.
- 테스트 `tests/test_sources_hash_remix.py` 8건(서버↔JS 지문을 node로 실제 대조 + 제보 재현). 고치기 전 실패 확인.
- 회귀: `-k "produce or mix or remix or script_hash"` 683 passed / 10 skipped.

## ⏭ 남은 일
1. 사장님 승인 → `py tools/track.py finish 영상바꾸면재매칭` → 라이브에서 직접: 작업 열기 → 1단계에서 영상 빼고 새로 담기 → 3단계 진입 → "🎬 담은 영상이 바뀌었어요" 뜨고 새 job 팔레트에 뺀 영상이 없는지 눈으로 확인.
2. ⚠️ 결정 필요: 영상 교체 시 **자동** 재매칭은 render 1회가 나간다(대본 변경 자동 재매칭 = 사장님 결정 A와 같은 방식으로 맞췄다). 버튼식을 원하면 `_remixIfChanged`의 영상 분기만 배너로 바꾸면 된다.
3. 이윤정님 당장 우회: 2단계에서 대본을 한 글자 고쳐 확정 → 3단계가 지금 담긴 영상으로 다시 매칭한다.
