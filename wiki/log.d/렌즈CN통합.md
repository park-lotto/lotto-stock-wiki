# 렌즈CN통합 트랙 로그

## 2026-08-17 (회사PC)

- 두 세션(CN검색통합·렌즈지연) 합류 — 확인해보니 **둘 다 이미 main에 병합**돼 있어
  남은 건 검증·후속이었다. CN검색통합의 유일한 미검증 게이트인 **서버 실측 통과**
  (샤오홍슈 `pw_xiaohongshu` 무료 8건 / 도우인 `apify_douyin` 8건 = 기대치 일치).
- 렌즈 월가드를 **SerpApi 실잔량** 기준으로 교체(라이브). 우리 카운터 196/500인데
  실제는 369/500 소진 — 클릭당 1로 세는데 실제로는 로케일 3벌×재시도로 최대 3회가
  나가기 때문. 그대로 두면 키가 죽는 순간 안내 없이 조용히 빈손이 된다.
- ⚠️ 게이트가 한 번 막았지만 **내 변경과 무관**이었다 — 변경이 없는 main 기준선에서도
  2건이 깨졌고 실패 조합이 매번 달랐다(CPU 경쟁 타이밍 flaky).

## 2026-08-17 (2차)

- **인스타·틱톡·유튜브 키워드 검색을 렌즈 안 카드로**(샤오홍슈·도우인과 같은 모양).
  사슬 엔진을 `search_chain.py`로 뽑아 cn_search와 **공용화**(0순위-B) — 새로 짠 게
  아니라 이미 범용이던 구조를 나눠 쓴 것. 라이브 종단: 24건 21.9초, 비용 정확히 보고.
- **★"틱톡도 프록시 되잖아"(사장님) — 맞았다.** 실측 결과 프록시로 페이지·API 모두
  200인데 **본문이 비고 로그인 모달**이 뜬다 = 막는 건 IP가 아니라 **세션**.
  샤오홍슈(세션 O→무료) / 도우인(세션 X→Apify)과 같은 구도라, 도우인과 같은 모양으로
  `pw_tiktok`을 사슬 맨 앞에 깔아뒀다 — **세션 파일이 생기면 자동으로 $0**.
- 키 실태·소요량 실측 정리 2건을 바탕화면에 산출(전체키정리 / 키소요_시뮬레이션).
  ★SerpApi는 지금 사용량 기준 **키 5개**가 필요한데 2개(1개 소진)뿐 = 남은 41클릭.
- **SerpApi 키 3개 등록**(SERPAPI_KEY_3~5, parklotto900~902). 넣기 전 account API로
  유효성 확인 후 등록 → 실측 **키풀 5개·실잔량 869회(약 289클릭 = 25일치)**.
  ⚠️ 서버 env 변경이라 git에 안 남는다 — 상세는 `handoff/렌즈CN통합.md`.
- **Apify 토큰 17개 → 5개 정리**(401 죽음 9 + 중복 3 삭제, 1~5로 재정렬). env 백업 후
  작업하고 APIFY 외 변수는 diff로 동일함을 확인. 5개 전부 살아있음 실측.
- ★정리 직후 도우인 0건이 났지만 **정리 탓이 아니었다** — run 로그에 `used all 15 free
  runs`(월 $5와 별개인 **계정당 무료 15회** 제한). 토큰1 잔액은 $4.255로 멀쩡했다.
  진짜 함정은 **액터가 SUCCEEDED+0건으로 끝나 로테이션이 안 넘어가는 것**
  (apify_client.py:150-153) — 계정 2·4·5엔 무료 실행이 남아 있는데도 계정1에 고정됐다.
  임시조치=풀 포인터를 계정2로 이동(코드 무수정) → CN 검색 10건 복구. 근본수정은 미착수.
