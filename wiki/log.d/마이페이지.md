# 마이페이지 — 작업 로그

## 2026-08-17
- `/account`+`/settings`를 **마이페이지** 한 화면(탭 3개)으로 통합. 사장님이 "어디?"로 못 찾으신
  원인이 이름 갈림(내 계정 vs 내 설정)이라 진입점을 하나로. `/account`는 307로 넘김.
- 합치다 무료 등급 구멍 발견·수정: `/account`는 무료인데 `/settings`는 서버가 막고 있었다
  (사이드바는 free:true라 화면·서버가 어긋남) → GET만 개방, 키 등록 POST는 그대로 차단.
- **SerpApi BYOK 추가** — 저장만 되는 게 아니라 `_lens_api_keys(cid)`로 렌즈 호출부 2곳에
  실배선. 렌즈 과금·환불 기준을 gemini→serpapi로 맞춰 화면 문구와 일치시킴.
  문구 수치는 코드 실측(검색 1번=3회, 월 250회≈83번, 하루 10회 유지)이고 테스트로 고정.
- 검증: 신규 6건 통과, 전체 4022 passed(기존 실패 1건은 스태시 재현으로 무관 확인).
  main 병합 `a564d677d` → 서버 `535670198` 반영·active 확인. **사장님 육안 확인은 미완.**
- **종단 실측 완료**(실제 키·실제 SerpApi): 등록→확인 ok→검색 3회(ko/en/zh-cn)→전부 등록키로
  →25건→포인트 0P 그대로. ★그 과정에 함정 발견: 250회 다 쓴 키도 account.json이 200이라
  "정상"으로 떴다(검색은 0건, 폴백 없어 사장님 키로도 안 감). `_key_status` 신설로 ok/empty/bad
  3갈래, 화면에 "이번 달 무료분을 다 썼습니다" 추가. 서버 실측으로 소진키=empty 확인(a1888e398).
- 낡은 기록 정정: 회원 0명 아님(실측 15명), 실DB는 app.db가 아니라 reference.db.
