# 블로그 인제스트 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 블로그 포스트(.md)를 내용기반 라우팅으로 추출해 텔레그램 2층 fan-out으로 atoms DB에 누적한다.

**Architecture:** 리포트의 내용라우팅 + 텔레그램의 2층 fan-out(`questionnaire_to_atoms_tg`) + sector_classify를 합성. 신규는 얇은 3개(blog_questionnaire·blog_registry·blog_ingest)뿐이고, 기존 fan-out은 source_type 파라미터화만 확장.

**Tech Stack:** Python 3.14, google-genai(gemini-3.1-flash-lite), pytest. 재사용: telegram_questionnaire, sector_classify, stock_resolve, telegram_stance, db, vector_db.

## Global Constraints

- 설계 출처: `docs/superpowers/specs/2026-06-21-blog-인제스트-design.md`.
- **최대 재사용**: 새 코드 최소. fan-out `questionnaire_to_atoms_tg` 그대로 쓰되 `_base`의 source_type만 파라미터화.
- 라우터는 **텔레 호환 타입명** 출력: `target_kind ∈ {stock_tips, sector, market, insight}`. fan-out 분기 무변경.
- 블로그 종목 슬롯에 **sector_hint** 포함(sector_classify 재사용). 섹터는 sectors.json 목록에서.
- `source_type="blog"`, `source_name`=블로거(출처), `stance_key="{블로거}|{대상}"`(meta["channel"]=블로거).
- 신뢰등급: `blog_registry.json`(블로거→trust), 미등록=기본 "C".
- quote 검증 약함(블로그는 요약본) — 원문대조 대신 요약본 자체 대조(verify_telegram 재사용은 선택).
- Gemini 호출은 테스트 안 함(프롬프트 구조·결정론 로직만). 테스트는 운영 로그/DB 격리.
- 이 plan은 **블로그만**. 유튜브·뉴스는 후속.

---

### Task 1: fan-out source_type 파라미터화

**Files:**
- Modify: `pipeline/atoms/telegram_questionnaire.py` (`_base`)
- Test: `pipeline/atoms/test_fanout_source_type.py`

**Interfaces:**
- Produces: `questionnaire_to_atoms_tg(q, meta)`가 `meta["source_type"]`(있으면)을 원자 source_type에 반영. 없으면 "telegram"(기존 동작).

- [ ] **Step 1: Write the failing test**

```python
# pipeline/atoms/test_fanout_source_type.py
from pipeline.atoms.telegram_questionnaire import questionnaire_to_atoms_tg


def test_source_type_defaults_telegram():
    q = {"stocks": [{"name": "삼성전자", "signal": "bull", "reason": "x",
                     "ts": "10:00", "quote": "q", "sector": "반도체"}]}
    meta = {"date": "2026-06-19", "channel": "잠실개미고급수집", "type": "stock_tips", "trust": "C"}
    atoms = questionnaire_to_atoms_tg(q, meta)
    assert atoms and all(a["source_type"] == "telegram" for a in atoms)


def test_source_type_blog_override():
    q = {"stocks": [{"name": "삼성전자", "signal": "bull", "reason": "x",
                     "ts": "10:00", "quote": "q", "sector": "반도체"}]}
    meta = {"date": "2026-06-19", "channel": "pokara61", "type": "stock_tips",
            "trust": "B", "source_type": "blog"}
    atoms = questionnaire_to_atoms_tg(q, meta)
    assert atoms and all(a["source_type"] == "blog" for a in atoms)
```

- [ ] **Step 2: Run test to verify it fails**

Run: `python -m pytest pipeline/atoms/test_fanout_source_type.py -v`
Expected: FAIL — blog 케이스가 "telegram" 반환.

- [ ] **Step 3: _base source_type 파라미터화**

`telegram_questionnaire.py`의 `_base` 안 `"source_type": "telegram",`를 교체:
```python
        "source_type": meta.get("source_type", "telegram"),
```

- [ ] **Step 4: Run tests to verify they pass**

Run: `python -m pytest pipeline/atoms/test_fanout_source_type.py pipeline/atoms/test_telegram_fanout.py -v`
Expected: PASS (신규 2개 + 기존 텔레 회귀 없음).

- [ ] **Step 5: Commit**

```bash
git add pipeline/atoms/telegram_questionnaire.py pipeline/atoms/test_fanout_source_type.py
git commit -m "feat(blog): fan-out source_type 파라미터화 (재사용 준비)"
```

---

### Task 2: blog_registry + blog_questionnaire (라우팅+질문지)

**Files:**
- Create: `pipeline/atoms/blog_registry.json`
- Create: `pipeline/atoms/blog_questionnaire.py`
- Test: `pipeline/atoms/test_blog_questionnaire.py`

**Interfaces:**
- Consumes: `sector_classify.sectors_list`, `atomizer._load_gemini_key`.
- Produces:
  - `blog_trust(blogger: str) -> str` (등록되면 그 등급, 아니면 "C").
  - `BLOG_PROMPT: str` (STEP0 라우팅 + 4타입 슬롯 + 섹터목록 주입).
  - `extract_blog(md_path: Path) -> dict` (Gemini; `target_kind` + 슬롯. 실패 시 {}).

- [ ] **Step 1: blog_registry.json 작성**

```json
{
  "pokara61": "B"
}
```

- [ ] **Step 2: Write the failing test**

```python
# pipeline/atoms/test_blog_questionnaire.py
from pipeline.atoms.blog_questionnaire import blog_trust, BLOG_PROMPT


def test_registered_blogger_trust():
    assert blog_trust("pokara61") == "B"


def test_unregistered_blogger_default_c():
    assert blog_trust("듣보블로거") == "C"


def test_prompt_has_router_and_types():
    # 내용 라우팅 + 4타입 + sector + quote 포함
    for kw in ("target_kind", "stock_tips", "sector", "market", "insight", "quote"):
        assert kw in BLOG_PROMPT


def test_prompt_injects_sector_taxonomy():
    from pipeline.atoms.sector_classify import sectors_list
    assert sectors_list()[0] in BLOG_PROMPT  # "반도체"
```

- [ ] **Step 3: Run test to verify it fails**

Run: `python -m pytest pipeline/atoms/test_blog_questionnaire.py -v`
Expected: FAIL — 모듈 없음.

- [ ] **Step 4: blog_questionnaire.py 구현**

```python
# pipeline/atoms/blog_questionnaire.py
"""블로그 인제스트 — 내용기반 라우팅 + 타입별 질문지 (리포트 라우팅 차용, 텔레 슬롯 호환)."""
import json
from pathlib import Path

from google import genai
from google.genai import types

from .atomizer import _load_gemini_key
from .sector_classify import sectors_list

_MODEL = "gemini-3.1-flash-lite"
_DIR = Path(__file__).parent
_REGISTRY = json.loads((_DIR / "blog_registry.json").read_text(encoding="utf-8"))


def blog_trust(blogger: str) -> str:
    return _REGISTRY.get((blogger or "").strip(), "C")


BLOG_PROMPT = """너는 개인 투자 블로그 포스트를 '정해진 칸'에 옮겨 적는 사람이다.
판단하지 말고 칸을 채워라. 잡담·인사는 버려라.
철칙: 없으면 null(지어내지 마라) / 종목명·수치·날짜 원문 그대로 /
각 종목에 quote(근거 원문 문장)와 sector를 붙여라. sector는 아래 목록에서 정확히 하나(모르면 "기타"): """ + " / ".join(sectors_list()) + """

## STEP 0 — target_kind 판별 (본문 내용 기준)
- "stock_tips" : 특정 종목 집중 분석
- "sector"     : 단일 섹터/테마 전반
- "market"     : 시황/매크로/시장전략
- "insight"    : 에세이·전략·잡생각(매매원칙·시장철학)

## STEP 1 — target_kind에 맞는 칸만 채워라
[stock_tips] stocks:[{name, signal(bull/bear/neutral), reason, ts(null 허용), quote, sector}], news_items:[{fact, ts, quote}], quote
[sector] sector_name, sector_view(긍정/중립/부정), points(배열), stocks_mentioned:[{name, comment, ts, quote, sector}], events:[{fact, ts, quote}], quote
[market] market_direction, macro_events:[{fact, ts, quote}], sectors_mentioned:[{sector, stance, comment}], quote
[insight] leading_sectors(배열), stance:[{target, view(긍정/중립/부정), ts, quote}], methods:[{rule, quote}], stocks_mentioned:[{name, comment, ts, quote, sector}], quote

## 출력
target_kind + 해당 타입 칸만 채운 JSON 1개. 다른 텍스트 금지."""


def extract_blog(md_path: Path) -> dict:
    """블로그 .md를 Gemini로 읽어 채워진 질문지(target_kind+슬롯) 반환. 실패 시 {}."""
    text = md_path.read_text(encoding="utf-8")
    client = genai.Client(api_key=_load_gemini_key())
    try:
        resp = client.models.generate_content(
            model=_MODEL, contents=[text, BLOG_PROMPT],
            config=types.GenerateContentConfig(response_mime_type="application/json"),
        )
        return json.loads(resp.text)
    except Exception as e:
        print(f"  [WARN] 블로그 추출 실패: {e}")
        return {}
```

- [ ] **Step 5: Run tests to verify they pass**

Run: `python -m pytest pipeline/atoms/test_blog_questionnaire.py -v`
Expected: PASS (4 tests).

- [ ] **Step 6: Commit**

```bash
git add pipeline/atoms/blog_registry.json pipeline/atoms/blog_questionnaire.py pipeline/atoms/test_blog_questionnaire.py
git commit -m "feat(blog): 내용라우팅 질문지 + 블로거 레지스트리"
```

---

### Task 3: blog_ingest 오케스트레이터

**Files:**
- Create: `pipeline/atoms/blog_ingest.py`
- Test: `pipeline/atoms/test_blog_ingest_helpers.py`

**Interfaces:**
- Consumes: `blog_questionnaire.extract_blog/blog_trust`, `questionnaire_to_atoms_tg`, `telegram_stance.deactivate_prior_stance`, `db`, `vector_db`.
- Produces: `_parse_blog_header(md_path) -> dict` ({blogger, date, link, title}), `get_done_blog_files() -> set[str]`, `get_pending_blog(date_filter=None) -> list[Path]`, `ingest_blog(md_path) -> int`, `main()`.

- [ ] **Step 1: Write the failing test**

```python
# pipeline/atoms/test_blog_ingest_helpers.py
import pytest
import pipeline.atoms.db as dbmod
from pipeline.atoms.blog_ingest import _parse_blog_header, get_done_blog_files


@pytest.fixture(autouse=True)
def _temp_db(tmp_path, monkeypatch):
    monkeypatch.setattr(dbmod, "DB_PATH", tmp_path / "t.db")
    yield


def test_parse_blog_header(tmp_path):
    p = tmp_path / "2026-06-21_0900_바이오 주식.md"
    p.write_text(
        "# 바이오 주식과 코스닥 승강제에 대해서\n"
        "- **출처**: pokara61 블로그\n"
        "- **날짜**: 2026-06-21\n"
        "- **링크**: [원문](https://blog.naver.com/pokara61/123)\n"
        "## 본문\n내용",
        encoding="utf-8",
    )
    h = _parse_blog_header(p)
    assert h["blogger"] == "pokara61"        # "블로그" 접미사 제거
    assert h["date"] == "2026-06-21"
    assert "blog.naver.com/pokara61/123" in h["link"]


def test_get_done_blog_files_basename(tmp_path):
    dbmod.init_db(); dbmod.migrate_db()
    import uuid
    from datetime import datetime
    conn = dbmod.get_conn()
    conn.execute(
        "INSERT OR REPLACE INTO atoms (id,date,source_type,source_name,source_trust,"
        "raw_file,layer,sector,asset,asset_level,signal,event_type,magnitude,"
        "content_type,strength_score,content,relations,created_at) "
        "VALUES (?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?)",
        (str(uuid.uuid4()), "2026-06-21", "blog", "pokara61", 3,
         "raw/blog/2026-06-21_0900_바이오.md", "L5", "바이오", "바이오", "sector",
         "neutral", "report", "minor", "fact", 2, "x", "[]", datetime.now().isoformat()),
    )
    conn.commit(); conn.close()
    done = get_done_blog_files()
    assert "2026-06-21_0900_바이오.md" in done
```

- [ ] **Step 2: Run test to verify it fails**

Run: `python -m pytest pipeline/atoms/test_blog_ingest_helpers.py -v`
Expected: FAIL — 모듈 없음.

- [ ] **Step 3: blog_ingest.py 구현**

```python
# pipeline/atoms/blog_ingest.py
"""블로그 인제스트 오케스트레이터 (telegram_ingest 패턴 차용).

사용법:
    python -m pipeline.atoms.blog_ingest --all [--date YYYY-MM-DD] [--limit N] [--dry-run]
    python -m pipeline.atoms.blog_ingest raw/blog/2026-06-21_0900_바이오.md
"""
import re
import sys
import json
import argparse
from pathlib import Path

if sys.stdout.encoding and sys.stdout.encoding.lower() not in ("utf-8", "utf8"):
    try:
        sys.stdout.reconfigure(encoding="utf-8", errors="replace")
    except Exception:
        pass

from .db import init_db, migrate_db, insert_atom, get_conn
from .vector_db import embed_and_store
from .blog_questionnaire import extract_blog, blog_trust
from .telegram_questionnaire import questionnaire_to_atoms_tg
from .telegram_stance import deactivate_prior_stance

_ROOT = Path(__file__).parent.parent.parent
_BLOG_DIR = _ROOT / "raw" / "blog"
_Q_ROOT = _ROOT / "raw" / "blog_q"

_FNAME = re.compile(r"^(\d{4}-\d{2}-\d{2})_\d+_(.+)$")


def _parse_blog_header(md_path: Path) -> dict:
    text = md_path.read_text(encoding="utf-8", errors="replace")
    title = md_path.stem
    blogger = re.search(r"\*\*출처\*\*[:\s]+(.+)", text)
    date = re.search(r"\*\*날짜\*\*[:\s]+(\d{4}-\d{2}-\d{2})", text)
    link = re.search(r"\*\*링크\*\*.*?(https?://[^\)\s]+)", text)
    b = blogger.group(1).strip() if blogger else title
    b = re.sub(r"\s*블로그$", "", b).strip()  # "pokara61 블로그" → "pokara61"
    d = date.group(1) if date else ""
    if not d:
        m = re.search(r"(\d{4}-\d{2}-\d{2})", md_path.name)
        d = m.group(1) if m else ""
    return {"blogger": b, "date": d, "link": link.group(1) if link else "", "title": title}


def get_done_blog_files() -> set[str]:
    conn = get_conn()
    rows = conn.execute(
        "SELECT DISTINCT raw_file FROM atoms WHERE source_type='blog'"
    ).fetchall()
    conn.close()
    return {Path(r[0]).name for r in rows if r[0]}


def get_pending_blog(date_filter: str = None) -> list[Path]:
    done = get_done_blog_files()
    files = []
    for f in sorted(_BLOG_DIR.glob("*.md")):
        if f.suffix != ".md" or "_analysis" in f.name:
            continue
        m = _FNAME.match(f.stem)
        if date_filter and (not m or m.group(1) != date_filter):
            continue
        if f.name in done:
            continue
        files.append(f)
    return sorted(files, reverse=True)


def _save_artifact(q: dict, date: str, title: str) -> None:
    out = _Q_ROOT / (date or "unknown")
    out.mkdir(parents=True, exist_ok=True)
    safe = re.sub(r"[^\w가-힣]+", "_", title)[:60]
    (out / f"{safe}.json").write_text(
        json.dumps(q, ensure_ascii=False, indent=2), encoding="utf-8")


def ingest_blog(md_path: Path) -> int:
    h = _parse_blog_header(md_path)
    q = extract_blog(md_path)
    if not q or not q.get("target_kind"):
        print(f"  [WARN] 빈/미라우팅 질문지: {md_path.name}")
        return 0
    _save_artifact(q, h["date"], h["title"])
    meta = {
        "date": h["date"], "channel": h["blogger"], "type": q["target_kind"],
        "source_type": "blog", "trust": blog_trust(h["blogger"]),
        "raw_file": str(md_path),
    }
    atoms = questionnaire_to_atoms_tg(q, meta)
    for a in atoms:
        if a.get("stance_key"):
            deactivate_prior_stance(a["stance_key"], keep_id=a["id"])
        insert_atom(a)
        try:
            embed_and_store(a)
        except Exception:
            pass
    return len(atoms)


def main():
    ap = argparse.ArgumentParser()
    ap.add_argument("file", nargs="?")
    ap.add_argument("--all", action="store_true")
    ap.add_argument("--date", default=None)
    ap.add_argument("--limit", type=int, default=30)
    ap.add_argument("--dry-run", action="store_true")
    args = ap.parse_args()
    init_db(); migrate_db()

    if args.file:
        targets = [Path(args.file)]
    elif args.all:
        targets = get_pending_blog(args.date)[: args.limit]
        print(f"미처리 블로그: {len(targets)}개\n")
    else:
        print("file 또는 --all 필요")
        return

    if args.dry_run:
        for f in targets:
            print(f"  {f.name} → {_parse_blog_header(f)['blogger']}")
        return

    total = 0
    for i, f in enumerate(targets, 1):
        print(f"[{i}/{len(targets)}] {f.name}")
        n = ingest_blog(f)
        total += n
        print(f"  → {n}개 원자")
    print(f"\n완료: {len(targets)}개 포스트, {total}개 원자")


if __name__ == "__main__":
    main()
```

- [ ] **Step 4: Run tests to verify they pass**

Run: `python -m pytest pipeline/atoms/test_blog_ingest_helpers.py -v`
Expected: PASS (2 tests).

- [ ] **Step 5: Commit**

```bash
git add pipeline/atoms/blog_ingest.py pipeline/atoms/test_blog_ingest_helpers.py
git commit -m "feat(blog): 오케스트레이터 (헤더파싱·dedup·2층 fan-out 재사용)"
```

---

### Task 4: 파이프라인 연결 + 라이브 검증

**Files:**
- Modify: `scripts/atom_pipeline.py` (STEP3.6 추가)
- (라이브 검증)

- [ ] **Step 1: atom_pipeline.py에 STEP3.6 추가**

`atom_pipeline.py`의 STEP3.5(telegram) 블록 다음에 추가:
```python
    # 3.6단계: 블로그 인제스트 (내용라우팅 + 2층 fan-out)
    run([PYTHON, "-m", "pipeline.atoms.blog_ingest",
         "--all", "--limit", "30"],
        "STEP3.6 blog")
```
dry-run 블록(STEP3.5 dry-run 다음)에도 추가:
```python
        subprocess.run([PYTHON, "-m", "pipeline.atoms.blog_ingest", "--all", "--dry-run", "--limit", "5"], cwd=str(ROOT))
```

- [ ] **Step 2: 전체 테스트 회귀**

Run: `python -m pytest pipeline/atoms/ -q`
Expected: 전체 PASS.

- [ ] **Step 3: dry-run 스모크**

Run: `python -m pipeline.atoms.blog_ingest --all --dry-run --limit 5`
Expected: 미처리 블로그 목록 + 블로거명 출력, 에러 없음.

- [ ] **Step 4: 라이브 1포스트 인제스트**

먼저 raw/blog에 포스트가 동기화돼 있어야 함(없으면 crawling_bot_data에서 1개 복사). 그 다음:
Run: `python -m pipeline.atoms.blog_ingest "raw/blog/2026-06-21_0900_바이오 주식과 코스닥 승강제에 대해서.md"`
(실제 존재하는 파일명으로) — Expected: 원자 생성, 아티팩트 `raw/blog_q/2026-06-21/` 생성.

- [ ] **Step 5: DB 표면 확인**

```bash
python -c "import sys; sys.stdout.reconfigure(encoding='utf-8'); from pipeline.atoms.db import get_conn; c=get_conn(); [print(r) for r in c.execute(\"SELECT source_name, asset_level, sector, asset FROM atoms WHERE source_type='blog' ORDER BY asset_level\").fetchall()]; c.close()"
```
Expected: source_type=blog 원자, source_name=블로거(pokara61), 섹터 분류됨(기타 아님), target_kind에 맞는 asset_level.

- [ ] **Step 6: Commit**

```bash
git add scripts/atom_pipeline.py
git commit -m "feat(blog): atom_pipeline STEP3.6 + 라이브 검증"
```

---

## Self-Review

**1. Spec coverage:**
- §3 재사용(fan-out/sector_classify/stance) → Task 1(source_type)·Task 3(orchestrator 재사용 호출) ✅
- §4 내용라우팅 4타입 + sector_hint 슬롯 → Task 2 BLOG_PROMPT ✅
- §5 fan-out source_type 파라미터화 + 텔레 타입명 출력 → Task 1 + Task 2(라우터 출력 stock_tips 등) ✅
- §6 오케스트레이터(헤더파싱·dedup·아티팩트·STEP3.6) → Task 3·4 ✅
- §2 신뢰등급 레지스트리(기본 C) → Task 2 blog_trust ✅
- §7 quote 약함 → BLOG_PROMPT가 요약본 quote 사용(원문대조 생략) ✅

**갭:** verify(quote 대조)는 이 plan에서 연결 안 함 — 블로그는 요약본이라 원문대조 무의미(스펙 §7 정합). strength는 blog_trust(C=2) 기반으로 fan-out이 처리. 명시적 verify 훅은 후속.

**2. Placeholder scan:** TBD/TODO 없음. Task 4 Step 4는 "실제 존재 파일명으로"라 다소 열려있으나 구체 경로 예시 제공.

**3. Type consistency:** `extract_blog -> dict(target_kind+슬롯)`, `blog_trust(blogger)->str`, `questionnaire_to_atoms_tg(q, meta)` meta["type"]=target_kind·source_type="blog" 일치. `_parse_blog_header`→{blogger,date,link,title} Task3 정의·사용 일치. fan-out이 `meta.get("source_type")` 읽는 것(Task1)과 blog_ingest가 meta에 넣는 것(Task3) 일치.
