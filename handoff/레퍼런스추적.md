# handoff — 레퍼런스추적

## 2026-08-03 (회사PC, 집에서 이어서)

### 오늘 완료(전부 라이브 반영)
- 'X시간 전' 실시간화: posted_at(shortcode 복원)을 랭킹·발굴·소통 API에 부착, 프론트 실시간 계산
- 렌즈 oEmbed 실검증: 틱톡·유튜브 결과를 실제 제목·썸네일로 교체, 404 숨김
- 렌즈 인스타 키워드 0건 완화: _igKw로 동사꼬리·부사 제거('한국식 김밥 만들기'→'한국식 김밥')
- ★채널 아카이브 시스템(사장님 궁극목표): 
  - channel_archive 크롤러(스크롤 페이지네이션, 팔로워순, 렌더 양보, 로그인벽 백오프, 신규채널 자동편입)
  - 크롤 직후 비전태깅(cap 500/채널, CDN 만료 전 골든타임)
  - /archive 역대 히트작 화면(관리자): 댓글순 기본·조회수/최신, 카테고리 필터(영상 단위=비전태그 판정), 썸네일 인라인 재생, 채널 🚫 영구차단, 담기
  - 랭킹 카드 🏠 내채널 버튼: 비전태그 겹침으로 아카이브에서 같은 소재 옛 영상 → 담기→믹스 (/api/archive/similar)
- 레시피 매칭 수정: 채널 단위→영상 단위 카테고리(실측: 태그 941 중 레시피 76 구제)

### ⏭ 미결
- ★서버 크롤러 진행 관찰: nohup python3 -u -m shopping_shorts.channel_archive (로그 /tmp/channel_archive.log, 695채널 중 진행 중). 죽으면 같은 명령으로 재기동(이어서 감). 서버 재부팅 시에도 재기동 필요 — systemd 유닛화 검토
- 대형채널 태깅 잔여(상한 500 초과분, 예: self_interior 1793건)는 재크롤 또는 별도 배치 필요 — 아카이브 전용 태깅 백필 스크립트 검토
- 렌즈 '내 채널' 탭(모달 통합)은 🏠 버튼으로 대체됨 — 필요시 렌즈 모달에도 합류
- 확장(grab.user.js) 채널등록 팝업이 엑셀 추적 여부 미확인 → 발굴 트랙 전달거리
- 인스타 렌즈 결과 검증(oEmbed 없음) — 어긋남 계속 보이면 Apify 유료 검증 검토

### 핵심 파일
- shopping_shorts/channel_archive.py (크롤러), store.py(channel_archive/archive_state/heavy_job_running)
- shopping_shorts/static/archive.html, app.py(/api/archive/*, _archive_channel_cat_fn)
- index.html(fmtAgo, _igKw, archiveSimilar), lens_discover.py(verify_matches, _igKw는 프론트)

---

## 2026-08-03 (타 세션 — 레퍼런스 URL추적/썸네일)

## ⏭ 진행중/다음 (집에서)
1. **maxres 썸네일 우선** 커밋 `410f60514` — origin/track/레퍼런스추적 **푸시됨**, **finish(main 병합) 미완**.
   → 집에서: `git -C .tracks/레퍼런스추적 fetch origin && merge origin/main` 후 `py tools/track.py finish 레퍼런스추적`.
   (hqdefault 레터박스로 구글렌즈 0건 나던 것 → maxresdefault 풀프레임 우선, 없으면 hq 폴백)
2. **maxres로도 0건이면** = 리버스이미지 근본한계(구글이 원본 footage 미인덱싱). 다음 카드:
   - (a) URL 추적에 **유튜브 키워드 검색 증강**(`/api/lens/yt` 방식) 붙이기 — 프레임렌즈가 결과 많은 이유가 이것.
   - (b) 다른 장면 프레임으로 시도(썸네일 말고).
3. 실검증 미완: 엔드포인트가 auth 요구라 curl 직접확인 막힘 → 사장님 육안이 최종 증명.

## 큰 그림(별개, 이 트랙 밖)
- 스토리 해설 포맷 **조립** = `장면스파인` 트랙(다른 세션 P1~P5 진행중, 건드리지 말 것).
- 스토리 소스 **발굴**(장르×바이럴 자동수집) = 미착수, 별도 조각.

## 파일
- `shopping_shorts/app.py` — `_YT_ID_RE`, `_lens_image_for_url`, `api_lens_trace_url`
- `shopping_shorts/static/index.html` — `refTrace` 위젯, `traceByUrl`(TRACE-START/END)
- `shopping_shorts/tests/test_lens_trace_url.py`(8), `test_lens_trace_js.py`(1)

---

## 2026-08-03 (회사PC) — 뷰티탭 레시피 오염 수정
- **완료·라이브**: 전수조사(census) 경로가 비전 재분류를 빼먹어, 캡션 없는 릴스가 채널 최빈 카테고리(뎁덕=뷰티)로 고착되던 것 수정. `_run_census_job` 후속에 `recategorize_by_vision`+`apply_channel_category`+캐시 재저장 추가 (30b8d00fd, finish 병합 완료). 라이브 캐시도 즉시 교정 확인(tem.duck 뷰티→레시피·홈템).
- 같은 커밋에 이 트랙의 WIP였던 즐겨찾기(collection.html) 인앱재생 이식도 함께 라이브로 나감(/api/media·오버레이 헬퍼 존재 확인함).
- **⏭ 다음**: ①다음 전수조사 후 뷰티탭 분포 육안 재확인 ②reel_history의 뷰티 오염 이력(tem.duck 11건)은 그대로라, 비전태그 없는 릴스는 여전히 뷰티 폴백됨 — 오염 이력 정리(재분류 백필) 여부 사장님 결정 ③즐겨찾기 인앱재생 육안 확인.
