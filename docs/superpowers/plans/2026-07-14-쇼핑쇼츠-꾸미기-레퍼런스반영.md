# 쇼핑쇼츠 꾸미기 — 레퍼런스 프리셋 확장 + 단어별 강조 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 빅채널 인스타 레퍼런스 7개 분석에서 나온 3가지 자막 스타일(흰+노랑 2톤, 청록 네온글로우, 단어별 색상강조)을 쇼핑쇼츠 꾸미기(5단계)에 반영한다.

**Architecture:** 앞의 2가지(2톤/네온글로우)는 기존 헤드카피+추가텍스트 조합 및 outline_color 필드로 엔진 변경 없이 프리셋만 추가한다. 단어별 강조는 `video_assemble.py`에 신규 함수 `_segmented_drawtext`를 추가해, 텍스트를 줄/키워드 단위 세그먼트로 쪼개고 Pillow로 실측한 폭을 기준으로 여러 `drawtext` 필터를 절대 좌표로 이어붙인다. 기존 `_headcopy_drawtext`/`_caption_drawtexts`는 이 함수의 얇은 래퍼가 되며, 강조 규칙이 없으면 이전과 100% 동일한 출력을 낸다.

**Tech Stack:** Python(FastAPI 백엔드, ffmpeg drawtext, Pillow), 순수 JS(produce.html, 서버 렌더링 없는 SPA-ish 페이지)

## Global Constraints

- 기존 `_fixed_drawtext`/`_caption_drawtexts`/`_headcopy_drawtext`/`_burn_captions`의 **외부 호출 시그니처는 변경하지 않는다** (설계서 §4-2).
- `highlight_rules`가 비어있거나 매칭 0건이면 렌더 출력은 리팩터 전과 **완전히 동일**해야 한다(하위호환 최우선 — 라이브 서비스 회귀 금지).
- 서버 스키마(`store.py` deco_json) 변경 없음 — `highlight_rules`는 기존 `deco` dict 안의 새 키로만 추가.
- 신규 의존성 추가 금지 — Pillow(12.2.0)는 이미 설치돼 있음, `requirements.txt`에 명시 추가만 한다(간접 의존성이었으므로).
- 폰트 파일 경로는 항상 `shopping_shorts/static/fonts/` 기준(`_FONT_DIR`), 폴백은 `font.ttf`.

---

### Task 1: `_segmented_drawtext` 핵심 함수 (TDD)

**Files:**
- Modify: `shopping_shorts/video_assemble.py` (신규 함수 추가, 기존 `_fixed_drawtext`/`_caption_drawtexts` 근처인 330번째 줄 부근)
- Test: `shopping_shorts/tests/test_video_assemble.py`

**Interfaces:**
- Produces: `_segmented_drawtext(text: str, base_style: dict, work: Path, key_prefix: str, x_pct: float, y_pct: float, highlight_rules: list[dict] | None = None, default_color: str = "0xFFFFFF") -> list[str]`
  - `base_style`: 기존 hc/cap 스타일 dict와 동일 키(`font`, `size`, `outline`, `outline_color`, `outline_w`, `box`, `box_color`, `box_pad`, `box_opacity`, `color`)
  - `highlight_rules`: `[{"keyword": str, "color": str, "box": bool, "box_color": str}, ...]`
  - 반환: ffmpeg filter 문자열(`drawtext=...`) 리스트, 여러 개면 같은 줄에 나란히 배치되도록 x좌표가 이미 계산되어 있음.
- Consumes: `_hex_to_ff`(이미 있음, `video_assemble.py:328`), `_FONT_DIR`(이미 있음, `video_assemble.py:375` — **이 태스크에서 375번째 줄보다 앞으로 옮겨야 함**, 아래 Step 1 참고), `PIL.ImageFont`(신규 import).

- [ ] **Step 1: `_FONT_DIR` 정의를 파일 상단으로 이동**

`_FONT_DIR`이 현재 375번째 줄(파일 뒤쪽)에 정의돼 있어 이번 태스크의 새 함수(330번째 줄 부근에 추가 예정)에서 참조할 수 없다. 파일 상단 `_OUT_W, _OUT_H = 720, 1280`(21번째 줄) 바로 아래로 옮긴다.

`video_assemble.py` 21번째 줄 근처를 다음과 같이 수정:
```python
_OUT_W, _OUT_H = 720, 1280
_FONT_DIR = Path(__file__).parent / "static" / "fonts"
```
그리고 기존 375번째 줄의 `_FONT_DIR = Path(__file__).parent / "static" / "fonts"` 줄은 삭제한다(중복 정의 제거).

- [ ] **Step 2: 실패하는 테스트 작성**

`shopping_shorts/tests/test_video_assemble.py` 맨 끝에 추가:

```python
# ── 단어별 강조(highlight_rules) — 세그먼트 drawtext ──────────

def test_segmented_drawtext_no_rules_single_segment(tmp_path):
    """규칙 없으면 세그먼트 1개(기존 _fixed_drawtext와 동일 산출물 형태)."""
    style = {"font": "", "size": 64, "color": "#FFFFFF", "outline": True,
              "outline_color": "#000000", "outline_w": 6}
    parts = va._segmented_drawtext("안녕하세요", style, tmp_path, "hc", 50, 14)
    assert len(parts) == 1
    assert "drawtext=fontfile=" in parts[0]
    assert "fontcolor=0xFFFFFF" in parts[0]


def test_segmented_drawtext_matches_keyword_splits_segments(tmp_path):
    """키워드 매칭 시 세그먼트가 여러 개로 쪼개지고, x좌표가 증가 순서.
    v1은 정확 단어(공백 분리 토큰) 일치만 지원(설계서 §7 — 부분문자열 매칭은 범위 밖).
    레퍼런스 원본은 "쿠팡꿀템"처럼 붙어있지만, 그 형태는 부분매칭이 필요해 이번엔 다루지 않는다."""
    style = {"font": "", "size": 64, "color": "#FFFFFF"}
    rules = [{"keyword": "쿠팡", "color": "#FF2D2D", "box": True, "box_color": "#FF2D2D"}]
    parts = va._segmented_drawtext("나만 몰랐던 쿠팡 꿀템", style, tmp_path, "hc", 50, 14,
                                     highlight_rules=rules)
    assert len(parts) >= 2
    import re
    xs = [int(m.group(1)) for p in parts for m in [re.search(r"x=(-?\d+)", p)] if m]
    assert xs == sorted(xs)
    assert any("fontcolor=0xFF2D2D" in p and "box=1" in p for p in parts)


def test_segmented_drawtext_no_keyword_match_falls_back_to_one_segment(tmp_path):
    style = {"font": "", "size": 64, "color": "#FFFFFF"}
    rules = [{"keyword": "없는단어", "color": "#FF2D2D"}]
    parts = va._segmented_drawtext("안녕하세요 반갑습니다", style, tmp_path, "hc", 50, 14,
                                     highlight_rules=rules)
    assert len(parts) == 1


def test_segmented_drawtext_two_lines_resets_x_per_line(tmp_path):
    style = {"font": "", "size": 40, "color": "#FFFFFF"}
    rules = [{"keyword": "꿀템", "color": "#FFE100"}]
    parts = va._segmented_drawtext("나만 몰랐던\n쿠팡 꿀템", style, tmp_path, "hc", 50, 14,
                                     highlight_rules=rules)
    assert len(parts) >= 3  # 1줄(1세그먼트) + 2줄(쿠팡/꿀템 최소 2세그먼트)
    import re
    ys = sorted(set(int(m.group(1)) for p in parts for m in [re.search(r"y=(-?\d+)", p)] if m))
    assert len(ys) == 2  # 줄마다 다른 y
```

- [ ] **Step 3: 테스트 실행해 실패 확인**

Run: `cd shopping_shorts && python -m pytest tests/test_video_assemble.py -k segmented_drawtext -v`
Expected: FAIL — `AttributeError: module 'shopping_shorts.video_assemble' has no attribute '_segmented_drawtext'`

- [ ] **Step 4: `_segmented_drawtext` 구현**

`video_assemble.py`의 `_fixed_drawtext` 함수(337번째 줄) 바로 앞에 추가:

```python
import re  # video_assemble.py에 없음 — 파일 최상단 import 블록(13~17번째 줄)에 추가
from PIL import ImageFont  # 파일 최상단 import 블록에 추가(os/shutil/subprocess 근처)


def _resolve_seg_font(base_style, work, key_prefix):
    """세그먼트 폰트파일 경로(work에 복사된 실제 경로)와 ffmpeg fontfile 참조명을 함께 반환.
    _fixed_drawtext/_caption_drawtexts의 폰트 해석 로직과 동일 규칙(있으면 그 폰트, 없으면 font.ttf)."""
    fontref = "font.ttf"
    fname = os.path.basename((base_style or {}).get("font") or "")
    if fname:
        fpath = _FONT_DIR / fname
        if fpath.exists():
            shutil.copy(fpath, work / f"font_{key_prefix}.ttf")
            fontref = f"font_{key_prefix}.ttf"
    real_path = work / fontref if (work / fontref).exists() else _FONT_DIR.parent.parent / fontref
    return fontref, str(work / fontref)


def _match_highlight(word, highlight_rules):
    """word가 highlight_rules의 keyword와 정확히 일치하면 그 규칙(dict) 반환, 아니면 None."""
    for rule in (highlight_rules or []):
        if rule.get("keyword") and word == rule["keyword"]:
            return rule
    return None


def _build_segments(line, base_color, highlight_rules):
    """한 줄 텍스트를 공백 기준 토큰화하고, 연속된 동일 스타일(강조 or 기본) 토큰을 묶어
    [(text, color, box, box_color), ...] 세그먼트 리스트로 반환. 사이 공백은 각 세그먼트
    텍스트에 뒤따르는 공백으로 포함시켜(마지막 세그먼트 제외) 폭 계산 시 자연스럽게 처리한다."""
    words = line.split(" ")
    segs = []
    cur_text, cur_style = "", None
    for i, w in enumerate(words):
        rule = _match_highlight(w, highlight_rules)
        style = (rule["color"], bool(rule.get("box")), rule.get("box_color")) if rule else (base_color, False, None)
        piece = w + (" " if i < len(words) - 1 else "")
        if cur_style is None:
            cur_text, cur_style = piece, style
        elif style == cur_style:
            cur_text += piece
        else:
            segs.append((cur_text, *cur_style))
            cur_text, cur_style = piece, style
    if cur_text:
        segs.append((cur_text, *cur_style))
    return segs


def _segmented_drawtext(text, base_style, work, key_prefix, x_pct, y_pct,
                          highlight_rules=None, default_color="0xFFFFFF"):
    """헤드카피/자막 한 블록을 줄 단위로 나누고, highlight_rules에 매칭되는 단어만
    별도 색·배지로 세그먼트를 쪼개 나란히 이어붙인 drawtext 필터 리스트를 반환한다.
    규칙이 없거나 매칭 0건이면 줄마다 세그먼트 1개 = 기존 _fixed_drawtext/_caption_drawtexts와
    동일한 산출물(하위호환). 폭 측정은 Pillow로 실제 폰트파일 기준 수행."""
    base_style = base_style or {}
    lines = (text or "").split("\n")
    if not any(l.strip() for l in lines):
        return []
    fontref, font_disk_path = _resolve_seg_font(base_style, work, key_prefix)
    size = max(8, int(base_style.get("size") or 64))
    try:
        pil_font = ImageFont.truetype(font_disk_path, size)
    except OSError:
        pil_font = ImageFont.load_default()
    base_color_hex = _hex_to_ff(base_style.get("color"), default_color)
    x_center = x_pct / 100.0 * _OUT_W
    y_top = y_pct / 100.0 * _OUT_H
    line_h = size * 1.2
    total_h = line_h * len(lines)
    parts = []
    for li, line in enumerate(lines):
        segs = _build_segments(line, base_color_hex, highlight_rules or [])
        if not segs:
            continue
        widths = [pil_font.getlength(s[0]) for s in segs]
        line_w = sum(widths)
        start_x = x_center - line_w / 2
        line_y = y_top - total_h / 2 + li * line_h
        run_x = start_x
        for (seg_text, seg_color, seg_box, seg_box_color), w in zip(segs, widths):
            if not seg_text.strip():
                run_x += w
                continue
            key = f"{key_prefix}_{li}_{len(parts)}"
            (work / f"txt_{key}.txt").write_text(seg_text.rstrip(), encoding="utf-8")
            seg_parts = [
                f"drawtext=fontfile={fontref}:textfile=txt_{key}.txt",
                f"fontcolor={_hex_to_ff(seg_color, default_color)}",
                f"fontsize={size}",
                f"x={int(run_x)}", f"y={int(line_y)}",
            ]
            if base_style.get("outline"):
                seg_parts.append(f"borderw={max(1, int(base_style.get('outline_w') or 6))}")
                seg_parts.append(f"bordercolor={_hex_to_ff(base_style.get('outline_color'), '0x000000')}")
            if seg_box:
                bc = _hex_to_ff(seg_box_color, "0x000000")
                seg_parts += ["box=1", f"boxcolor={bc}@0.90", "boxborderw=8"]
            elif base_style.get("box") and not seg_box:
                bc = _hex_to_ff(base_style.get("box_color"), "0x000000")
                op = max(0.0, min(1.0, (base_style.get("box_opacity") or 80) / 100.0))
                pad = max(0, int(base_style.get("box_pad") if base_style.get("box_pad") is not None else 16))
                seg_parts += ["box=1", f"boxcolor={bc}@{op:.2f}", f"boxborderw={pad}"]
            parts.append(":".join(seg_parts))
            run_x += w
    return parts
```

**주의(구현 시 확인)**: `_resolve_seg_font`의 `real_path` 계산은 work 폴더에 아직 파일이 없는 최초 호출 시점엔 `work/fontref`가 존재하지 않을 수 있다(폰트 복사는 이 함수 안에서 수행하므로 `fpath.exists()` 분기를 타면 항상 `work/font_{key_prefix}.ttf`가 생성됨; 폴백 시 `font.ttf`는 `_burn_captions`가 이미 `work/font.ttf`로 복사해둔 뒤에만 존재 — **이 함수가 `_burn_captions` 안에서, `font.ttf` 복사 이후에 호출되는 순서를 유지**해야 한다. 순서가 어긋나면 Step 5 테스트가 실패하므로 즉시 드러남).

- [ ] **Step 5: 테스트 실행해 통과 확인**

Run: `cd shopping_shorts && python -m pytest tests/test_video_assemble.py -k segmented_drawtext -v`
Expected: PASS (4 tests)

- [ ] **Step 6: Commit**

```bash
git add shopping_shorts/video_assemble.py shopping_shorts/tests/test_video_assemble.py
git commit -m "feat(shopping_shorts): 헤드카피·자막 단어별 강조 렌더 함수 _segmented_drawtext 추가"
```

---

### Task 2: 기존 `_headcopy_drawtext`/`_caption_drawtexts`를 `_segmented_drawtext` 위에서 재구현 (하위호환 회귀 테스트)

**Files:**
- Modify: `shopping_shorts/video_assemble.py:238-261`(`_caption_drawtexts`), `:375-377`(`_headcopy_drawtext`)
- Test: `shopping_shorts/tests/test_video_assemble.py`

**Interfaces:**
- Consumes: `_segmented_drawtext`(Task 1에서 정의)
- Produces: `_headcopy_drawtext(hc, work)`, `_caption_drawtexts(narration, dur, work, idx, t0=0.0, style=None)` — **외부 시그니처 동일 유지**(Global Constraints)

- [ ] **Step 1: 실패하는 회귀 테스트 작성**

`test_video_assemble.py`에 추가 — 강조 규칙이 없을 때 리팩터 전후 산출물이 실질적으로 동일한 필터를 만드는지 확인:

```python
def test_headcopy_drawtext_no_highlight_matches_single_block(tmp_path):
    """highlight_rules 없는 hc는 세그먼트 1개(폭측정 x좌표 무관하게 fontcolor/폰트 등 필드는 기존과 동일)."""
    hc = {"text": "테스트 문구", "font": "", "color": "#FF8800", "size": 60,
          "x": 50, "y": 14, "outline": True, "outline_color": "#000000", "outline_w": 7}
    dt = va._headcopy_drawtext(hc, tmp_path)
    assert dt is not None
    assert "fontcolor=0xFF8800" in dt
    assert "borderw=7" in dt
    assert "bordercolor=0x000000" in dt


def test_caption_drawtexts_no_style_still_renders_bar_and_text(tmp_path):
    parts = va._caption_drawtexts("여러분 안녕하세요 반갑습니다", 2.0, tmp_path, 0)
    assert any("drawbox" in p for p in parts)  # 하단 바 유지
    assert any("drawtext=fontfile=" in p for p in parts)
```

- [ ] **Step 2: 테스트 실행해 실패 확인 (또는 기존 구현으로 이미 통과하는지 확인)**

Run: `cd shopping_shorts && python -m pytest tests/test_video_assemble.py -k "headcopy_drawtext_no_highlight or caption_drawtexts_no_style" -v`
Expected: 이 시점엔 기존 구현으로도 PASS해야 함(아직 리팩터 전) — 이 테스트는 **리팩터 후에도 계속 PASS**해야 하는 회귀 가드다.

- [ ] **Step 3: `_headcopy_drawtext` 리팩터**

`video_assemble.py` 375번째 줄 부근:

```python
def _headcopy_drawtext(hc, work):
    """헤드카피(고정 타이틀) drawtext — _segmented_drawtext 래퍼(기본색 오렌지).
    highlight_rules가 있으면(hc.get('highlight_rules')) 단어별 강조, 없으면 기존과 동일."""
    if not hc or not (hc.get("text") or "").strip():
        return None
    parts = _segmented_drawtext(
        hc["text"], hc, work, "hc", hc.get("x", 50), hc.get("y", 14),
        highlight_rules=hc.get("highlight_rules"), default_color="0xFF8800",
    )
    return ";".join(parts) if len(parts) > 1 else (parts[0] if parts else None)
```

**주의**: 헤드카피는 기존에 단일 `drawtext=...` 문자열 하나만 반환했고, `_burn_captions`가 `filters.append(hc_dt)`로 리스트에 단일 원소를 추가한다(`video_assemble.py:405-408`). 세그먼트가 2개 이상이면 **여러 개의 독립 필터를 모두 filters 리스트에 추가해야 하므로**, 위 임시 구현(`";".join`)은 잘못됨 — 실제로는 `_burn_captions` 쪽 호출부를 다음처럼 고쳐야 한다:

```python
# _burn_captions 안, 기존:
#   if headcopy:
#       hc_dt = _headcopy_drawtext(headcopy, work)
#       if hc_dt:
#           filters.append(hc_dt)
# 다음으로 교체:
    if headcopy and (headcopy.get("text") or "").strip():
        filters.extend(_headcopy_drawtext_parts(headcopy, work))
```

그리고 `_headcopy_drawtext`는 **리스트를 반환하는 `_headcopy_drawtext_parts`로 이름을 바꿔** 세그먼트 리스트 그대로 반환하게 한다(단일 문자열 반환하던 기존 함수는 제거). `test_burn_captions_signature`(기존 테스트, `video_assemble.py` 168번째 줄 부근)는 `_burn_captions`의 파라미터만 확인하므로 이 이름 변경엔 영향 없음 — 단, `_headcopy_drawtext`를 직접 참조하는 다른 코드가 없는지 아래 커맨드로 먼저 확인:

Run: `cd shopping_shorts && grep -rn "_headcopy_drawtext" . --include=*.py`
Expected: `video_assemble.py` 정의부 + `_burn_captions` 호출부 2곳만 나와야 함(다른 참조 있으면 그것도 함께 수정).

최종 함수:
```python
def _headcopy_drawtext_parts(hc, work):
    """헤드카피(고정 타이틀) drawtext 필터 리스트 — _segmented_drawtext 래퍼(기본색 오렌지).
    hc['highlight_rules']가 있으면 단어별 강조, 없으면 세그먼트 1개(기존과 동일)."""
    return _segmented_drawtext(
        hc.get("text", ""), hc, work, "hc", hc.get("x", 50), hc.get("y", 14),
        highlight_rules=hc.get("highlight_rules"), default_color="0xFF8800",
    )
```

위에 맞춰 Step 1의 `test_headcopy_drawtext_no_highlight_matches_single_block` 테스트도 `va._headcopy_drawtext(hc, tmp_path)` → `va._headcopy_drawtext_parts(hc, tmp_path)[0]`로 수정.

- [ ] **Step 4: `_caption_drawtexts` 리팩터**

**중요한 설계 갭(계획 작성 중 발견)**: 기존 자막은 `y=h-text_h-100`처럼 **ffmpeg가 렌더 시점에 직접 계산하는 `text_h`**(ffmpeg 자체 폰트 메트릭)를 쓴다. `_segmented_drawtext`는 반대로 **Pillow로 미리 계산한 절대 px** 좌표를 쓴다. 자막의 기본 위치(`y_pct` 미지정 시 "하단에서 100px 위")를 그대로 유지하려면, 호출 전에 `y_pct`를 퍼센트로 환산해서 넘겨야 한다 — 아래 코드에 그 변환을 포함했다.

`video_assemble.py` 238번째 줄 `_caption_drawtexts` 함수 전체를 다음으로 교체:

```python
def _caption_drawtexts(narration, dur, work, idx, t0=0.0, style=None):
    """나레이션 한 비트의 자막(하단 바 + 순차 drawtext)을 필터 문자열 리스트로 반환한다.
    _segmented_drawtext 기반: highlight_rules가 있으면 단어별 강조, 없으면 세그먼트 1개
    (기존과 동일 산출물). 각 구절 enable 구간은 t0(전체 타임라인 오프셋)만큼 밀린다."""
    segs = _caption_segments(narration)
    if not segs:
        return []
    style = style or {}
    durs = _caption_durations(segs, dur)
    size = max(10, int(style.get("size") or _CAP_FONTSIZE))
    ypct = style.get("y_pct")
    if ypct is None:
        # 기존 폴백 "h-text_h-100"의 근사치를 %로 환산(문자 높이는 size*1.2로 근사)
        ypct = max(0.0, min(100.0, (_OUT_H - 100 - size * 0.6) / _OUT_H * 100.0))
    use_box = bool(style.get("box"))
    show_bar = style.get("bar", True) and not use_box
    effect = style.get("effect") or "none"
    parts = []
    if show_bar:
        parts.append(f"drawbox=x=0:y=ih-{_BAR_H}:w=iw:h={_BAR_H}:color=black@0.82:t=fill")
    t = 0.0
    for i, (seg, d) in enumerate(zip(segs, durs)):
        start = t + t0
        t += d
        end = (dur + 0.5 if i == len(segs) - 1 else t) + t0
        seg_parts = _segmented_drawtext(
            seg, style, work, f"cap_{idx}_{i}", 50, ypct,
            highlight_rules=style.get("highlight_rules"), default_color="0xFFFFFF",
        )
        enable_clause = f"enable='between(t,{start:.2f},{end:.2f})'"
        for sp in seg_parts:
            if effect == "slide":
                # _segmented_drawtext가 만든 "y=<정수>"를 시간기반 슬라이드 표현식으로 치환.
                # 기존 로직과 동일하게 등장 0.25초 동안 +30px 아래에서 위로 미끄러짐.
                sp = re.sub(
                    r"y=(-?\d+)",
                    lambda m: f"y=({m.group(1)}+30*(1-min(1\\,(t-{start:.2f})/0.25)))",
                    sp, count=1,
                )
            elif effect in ("fade", "pop"):
                spd = 0.18 if effect == "fade" else 0.12
                sp = sp + f":alpha='min(1,max(0,(t-{start:.2f})/{spd}))'"
            parts.append(sp + ":" + enable_clause)
    return parts
```

`re` 모듈은 파일 상단에 이미 import돼 있는지 확인 후 없으면 추가(`import re`).

**검증 포인트**: 이 치환으로 slide 효과가 강조 규칙 유무와 무관하게 리팩터 전과 동일하게 동작해야 한다(Global Constraints "highlight_rules 없으면 완전히 동일" 조건에 slide 포함). Task 6 육안검증에서 slide를 쓰는 저장된 job으로 렌더해 리팩터 전 결과물과 동일하게 보이는지 반드시 확인한다.

- [ ] **Step 5: 테스트 재실행 — 전체 스위트 통과 확인**

Run: `cd shopping_shorts && python -m pytest tests/test_video_assemble.py -v`
Expected: 전체 PASS(기존 테스트 포함 회귀 없음)

- [ ] **Step 6: Commit**

```bash
git add shopping_shorts/video_assemble.py shopping_shorts/tests/test_video_assemble.py
git commit -m "refactor(shopping_shorts): 헤드카피·자막 drawtext를 _segmented_drawtext 기반으로 통합"
```

---

### Task 3: FULL_PRESETS에 흰+노랑 2톤 2종 + 청록 네온글로우 2종 추가

**Files:**
- Modify: `shopping_shorts/static/produce.html:400-443`(`FULL_PRESETS` 배열), `:467-470`(`applyFullPreset`)

**Interfaces:**
- Consumes: `STATE.deco.extra_texts`(기존, `produce.html:773`), `renderExtraPreview()`(기존, `:516`), `renderExtraTexts()`(기존, `:506`), `applyConfig(h, c)`(기존, `:449`)
- Produces: 없음(UI 프리셋 데이터 추가)

- [ ] **Step 1: `FULL_PRESETS`에 4개 항목 추가**

`produce.html` 442번째 줄(`{name:'정보전달 블루', ...}` 다음) 바로 뒤에 추가:

```js
  {name:'옐로 2톤 A', chip:'#FFE100',
    hc:{font:'GmarketSansBold.otf',color:'#FFFFFF',weight:900,size:38,x:50,y:8,outline:true,outline_color:'#000000',outline_w:6},
    extra:[{text:'',font:'GmarketSansBold.otf',color:'#FFE100',size:68,x:50,y:19,outline:true,outline_color:'#000000',outline_w:8}],
    cap:{font:'Pretendard-ExtraBold.otf',color:'#FFFFFF',size:52,y:84,outline:true,outline_color:'#000000',outline_w:6,box:false,effect:'fade'}},
  {name:'옐로 2톤 B(좌측)', chip:'#FFE100',
    hc:{font:'BMDOHYEON.ttf',color:'#FFFFFF',weight:900,size:36,x:32,y:9,outline:true,outline_color:'#000000',outline_w:6},
    extra:[{text:'',font:'BMDOHYEON.ttf',color:'#FFE100',size:64,x:32,y:19,outline:true,outline_color:'#000000',outline_w:7}],
    cap:{font:'SUIT-Bold.otf',color:'#FFFFFF',size:50,y:84,outline:true,outline_color:'#000000',outline_w:6,box:false,effect:'slide'}},
  {name:'네온 글로우 화이트', chip:'#00E5FF',
    hc:{font:'Pretendard-ExtraBold.otf',color:'#FFFFFF',weight:900,size:60,x:50,y:14,outline:true,outline_color:'#00E5FF',outline_w:9},
    cap:{font:'Pretendard-ExtraBold.otf',color:'#FFFFFF',size:50,y:84,outline:true,outline_color:'#00E5FF',outline_w:7,box:false,effect:'fade'}},
  {name:'네온 글로우 센터', chip:'#00E5FF',
    hc:{font:'SUIT-Bold.otf',color:'#FFFFFF',weight:900,size:58,x:50,y:16,outline:true,outline_color:'#00D9E8',outline_w:8},
    cap:{font:'SUIT-Bold.otf',color:'#FFFFFF',size:48,y:84,outline:true,outline_color:'#00D9E8',outline_w:6,box:false,effect:'pop'}},
```

- [ ] **Step 2: `applyFullPreset`이 `extra` 필드도 반영하도록 확장**

`produce.html` 467번째 줄:

```js
function applyFullPreset(i){
  const p = FULL_PRESETS[i];
  applyConfig(p.hc, p.cap);
  STATE.deco = STATE.deco || {extra_texts: []};
  if(p.extra){
    // 프리셋의 extra는 텍스트가 비어있음(스타일만 지정) — 기존 문구가 있으면 유지, 없으면 빈칸
    const prevText = (STATE.deco.extra_texts[0] && STATE.deco.extra_texts[0].text) || '';
    STATE.deco.extra_texts = p.extra.map((e,idx)=>({...e, text: idx===0 ? prevText : (e.text||'')}));
  } else {
    STATE.deco.extra_texts = [];
  }
  renderExtraTexts(); renderExtraPreview();
  document.querySelectorAll('.fullPreset').forEach((b,idx)=>b.style.outline = idx===i?'2px solid var(--accent2)':'none');
}
```

기존 `applyFullPreset`(467~470번째 줄) 전체를 위 코드로 교체.

- [ ] **Step 3: 수동 스모크 테스트(브라우저)**

Run: 로컬 서버 기동(`cd shopping_shorts && python -m uvicorn app:app --reload --port 8849`) → `/produce`(로그인) → 5단계 이동 → "🎯 완성 스타일"에서 신규 4개 버튼 클릭 → 콘솔 에러 없이 미리보기에 헤드카피(작게, 상단)+빈 텍스트 추가텍스트(스타일만 적용된 채로) 반영되는지 확인. 추가텍스트 입력창에 문구 입력 시 그 스타일로 렌더되는지 확인.
Expected: 콘솔 에러 0건, 미리보기에 색상/크기 반영.

- [ ] **Step 4: Commit**

```bash
git add shopping_shorts/static/produce.html
git commit -m "feat(shopping_shorts): 딸깍 완성스타일에 흰노랑 2톤·네온글로우 프리셋 4종 추가"
```

---

### Task 4: 강조 단어(highlight_rules) UI

**Files:**
- Modify: `shopping_shorts/static/produce.html` (헤드카피 섹션, `hcText` 입력 아래 — 172번째 줄 근처에 신규 블록 삽입 / JS는 `updateHC()`(666번째 줄), `saveHeadcopy()`(764번째 줄) 근처)

**Interfaces:**
- Consumes: `STATE.headcopy`(기존), `STATE.deco`(기존)
- Produces: `STATE.deco.highlight_rules = [{keyword, color, box, box_color}, ...]`, JS 함수 `addHighlightRule()`, `deleteHighlightRule(i)`, `updateHighlightRule(i, field, val)`, `renderHighlightRules()`

- [ ] **Step 1: HTML 블록 추가**

`produce.html` 173번째 줄(`<div id="hcPresets" ...></div>` 다음) 뒤에 삽입:

```html
<div style="background:#0d1420;border:1px solid var(--line);border-radius:10px;padding:10px 12px;margin-bottom:14px">
  <div style="font-size:13px;font-weight:700;margin-bottom:2px">🎯 강조 단어 <span style="font-size:11px;color:var(--sub);font-weight:400">— 헤드카피 + 자막 공통 적용(키워드 일치 시 자동 강조)</span></div>
  <div id="highlightRuleList" style="margin-top:6px"></div>
  <button class="tab" onclick="addHighlightRule()" style="flex:0 0 auto;min-width:auto;padding:6px 12px;font-size:12px;margin-top:4px">+ 강조 단어 추가</button>
</div>
```

- [ ] **Step 2: JS 함수 추가**

`produce.html` 505번째 줄(`updateExtraField` 함수) 근처에 추가:

```js
function addHighlightRule(){
  STATE.deco = STATE.deco || {extra_texts: []};
  STATE.deco.highlight_rules = STATE.deco.highlight_rules || [];
  STATE.deco.highlight_rules.push({keyword:'', color:'#FFE100', box:false, box_color:'#FF2D2D'});
  renderHighlightRules(); updateHC();
}
function deleteHighlightRule(i){
  STATE.deco.highlight_rules.splice(i,1); renderHighlightRules(); updateHC();
}
function updateHighlightRule(i, field, val){
  STATE.deco.highlight_rules[i][field] = (field==='box' ? val : val);
  renderHighlightRules(); updateHC();
}
function renderHighlightRules(){
  const box=document.getElementById('highlightRuleList'); if(!box) return;
  const list=(STATE.deco && STATE.deco.highlight_rules)||[];
  box.innerHTML=list.map((r,i)=>`<div style="display:flex;gap:6px;flex-wrap:wrap;align-items:center;margin-bottom:6px;background:#0b1018;border:1px solid var(--line);border-radius:8px;padding:6px 8px">
    <input type="text" placeholder="단어(예: 쿠팡)" value="${(r.keyword||'').replace(/"/g,'&quot;')}"
      oninput="updateHighlightRule(${i},'keyword',this.value)" style="flex:1;min-width:100px;background:#0d1420;border:1px solid var(--line);color:var(--txt);border-radius:6px;padding:5px 8px;font-size:13px">
    <label style="font-size:11px;color:var(--sub)">글자색 <input type="color" value="${r.color}" oninput="updateHighlightRule(${i},'color',this.value)"></label>
    <label style="font-size:11px;color:var(--sub)"><input type="checkbox" ${r.box?'checked':''} onchange="updateHighlightRule(${i},'box',this.checked)"> 배지</label>
    <input type="color" value="${r.box_color||'#FF2D2D'}" oninput="updateHighlightRule(${i},'box_color',this.value)" style="${r.box?'':'display:none'}">
    <button onclick="deleteHighlightRule(${i})" title="삭제" style="background:none;border:none;color:var(--sub);cursor:pointer;font-size:15px">✕</button>
  </div>`).join('');
}
```

- [ ] **Step 3: 헤드카피 미리보기에서 강조 단어를 색/배지로 렌더**

`updateHC()`(666번째 줄) 안, `el.textContent=hc.text;`(679번째 줄)를 다음으로 교체:

```js
  const rules = (STATE.deco && STATE.deco.highlight_rules) || [];
  if(rules.length && hc.text){
    const esc2 = s => s.replace(/&/g,'&amp;').replace(/</g,'&lt;');
    el.innerHTML = hc.text.split(' ').map(w=>{
      const r = rules.find(x=>x.keyword && x.keyword===w);
      if(!r) return esc2(w);
      const bg = r.box ? `background:${r.box_color};padding:0 4px;border-radius:4px;` : '';
      return `<span style="color:${r.color};${bg}">${esc2(w)}</span>`;
    }).join(' ');
  } else {
    el.textContent = hc.text;
  }
```

**주의**: 이후 줄들(680~683번째 줄, `el.style.color=hc.color` 등)은 `<span>`으로 감싸지 않은 나머지 텍스트에 상속되므로 그대로 둔다(강조 안 된 단어는 `esc2(w)` 그대로 상위 `el.style.color` 상속받아 기본색 적용됨 — 의도된 동작).

- [ ] **Step 4: `updateHC()` 호출 시점에 `renderHighlightRules()`가 이미 반영된 규칙을 읽도록 초기화 순서 확인**

`produce.html`에서 5단계 패널이 처음 열릴 때(`showPanel()` 또는 `jump`/`go` 함수가 step 4 패널을 그릴 때) `renderHighlightRules()`가 최초 1회 호출되는지 확인 — 기존 `renderPresets()`, `renderFullPresets()`가 호출되는 지점(파일에서 `DOMContentLoaded` 또는 초기화 함수)을 찾아 같은 곳에 `renderHighlightRules();` 한 줄 추가.

Run: `cd shopping_shorts && grep -n "renderFullPresets()\|renderPresets()" static/produce.html`
Expected: 초기화 호출부 라인 확인 후 그 옆에 `renderHighlightRules();` 추가.

- [ ] **Step 5: 수동 스모크 테스트(브라우저)**

로컬 서버 기동 → `/produce` → 5단계 → "강조 단어" + 클릭 → "쿠팡" 입력 + 배지 체크 + 빨간색 지정 → 헤드카피 문구에 "쿠팡" 포함 문장 입력 → 미리보기에서 "쿠팡"만 배지색으로 렌더되는지 확인.
Expected: 콘솔 에러 0건, "쿠팡" 단어만 시각적으로 강조됨.

- [ ] **Step 6: Commit**

```bash
git add shopping_shorts/static/produce.html
git commit -m "feat(shopping_shorts): 헤드카피·자막 공통 강조 단어(highlight_rules) UI 추가"
```

---

### Task 5: 서버 payload 스모크 테스트 + requirements.txt 명시

**Files:**
- Modify: `shopping_shorts/requirements.txt`
- Test: `shopping_shorts/tests/test_app.py`(기존 파일이 있으면 그 안에 추가, 없으면 파일명 확인 후 적절한 기존 테스트 파일에 추가)

**Interfaces:**
- Consumes: `/api/produce/mix/settings`(기존 엔드포인트, `app.py:1427`)

- [ ] **Step 1: requirements.txt에 Pillow 명시**

`shopping_shorts/requirements.txt`에 한 줄 추가(파일 끝):
```
Pillow  # 단어별 강조(highlight_rules) 폰트 폭 측정(video_assemble._segmented_drawtext)
```

- [ ] **Step 2: `deco.highlight_rules` 저장/조회 스모크 테스트 작성**

Run: `cd shopping_shorts && grep -rln "mix/settings\|api_produce_mix_settings" tests/`
Expected: 기존 관련 테스트 파일 경로 확인(예: `tests/test_app.py` 또는 `tests/test_produce_mix.py`). 그 파일에 추가:

```python
def test_mix_settings_saves_highlight_rules_inside_deco(client, ...):  # 기존 fixture 이름은 해당 파일의 다른 테스트 참고
    # 기존 job 생성 fixture 패턴을 따라 job_id 준비 후:
    resp = client.post("/api/produce/mix/settings", json={
        "job_id": job_id,
        "deco": {"extra_texts": [], "highlight_rules": [{"keyword": "쿠팡", "color": "#FF2D2D", "box": True}]},
    })
    assert resp.status_code == 200
    saved = store.get_mix_job(job_id)
    import json as _json
    assert _json.loads(saved["deco_json"])["highlight_rules"][0]["keyword"] == "쿠팡"
```

**주의**: 이 태스크는 기존 테스트 파일의 fixture(`client`, `store`, job 생성 헬퍼) 이름이 태스크 착수 시점에 확인되어야 정확한 코드가 나온다 — 착수 전 반드시 `grep -n "def test_.*mix.*settings\|@pytest.fixture" tests/test_app.py`(또는 찾은 실제 파일)로 기존 패턴을 그대로 따라 쓸 것. 이 테스트는 **서버 코드를 전혀 수정하지 않고** 통과해야 한다(설계서 §2: deco는 이미 통짜 dict 저장 — 회귀 확인용 테스트).

- [ ] **Step 3: 테스트 실행**

Run: `cd shopping_shorts && python -m pytest tests/ -k highlight_rules -v`
Expected: PASS (서버 코드 무변경으로 통과 확인 — 설계서 §2 주장의 실증)

- [ ] **Step 4: Commit**

```bash
git add shopping_shorts/requirements.txt shopping_shorts/tests/
git commit -m "test(shopping_shorts): highlight_rules가 deco payload로 그대로 저장됨을 확인 + Pillow 의존성 명시"
```

---

### Task 6: 실제 렌더 1회 육안 검증

**Files:** 없음(코드 변경 없음, 검증만)

- [ ] **Step 1: 로컬에서 실제 ffmpeg 렌더 1회 실행**

기존 믹스 job 하나를 로컬에서 완주(2단계 영상 매칭까지)한 뒤, 5단계에서 강조 단어 규칙 1개("쿠팡" 등)를 걸고 헤드카피에 그 단어를 포함해 최종 렌더(assemble) 실행.

- [ ] **Step 2: 렌더된 mp4를 직접 열어 확인**

확인 항목:
1. 강조 단어만 지정한 색/배지로 나오는지
2. 세그먼트 사이 간격이 부자연스럽게 겹치거나 벌어지지 않는지(PIL 폭측정 오차 범위 확인 — 설계서 §4-2 "1~3px 오차" 실측)
3. 기존 프리셋(강조 규칙 없는 것)이 리팩터 이전과 동일하게 나오는지(회귀 확인)

Expected: 위 3가지 모두 육안상 문제없음. 오차가 눈에 띄면 `_segmented_drawtext`의 폭 측정 로직에 여백 보정값(예: 세그먼트 사이 1~2px 여유) 추가.

- [ ] **Step 3: 문제 없으면 최종 커밋 없이 종료(이미 Task 1~5에서 커밋 완료), 문제 있으면 수정 후 해당 태스크에 fixup 커밋**

```bash
# 문제 발견 시:
git add shopping_shorts/video_assemble.py
git commit -m "fix(shopping_shorts): 세그먼트 drawtext 폭 보정(육안검증 반영)"
```
