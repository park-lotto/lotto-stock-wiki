# MIX 장면 편성 재진입 덮어쓰기 방지

## 확인한 사고

- 고객 박세현(cid 340) 최신 job `4530798b7084`에서 렌더 뒤 MIX 재진입 시각(2026-09-19 11:11 KST)에 `GET scene_lab` 직후 `apply/caplines/scenezoom` POST가 연속 발생했다.
- 이전 수정은 같은 프로세스의 읽기-수정-쓰기를 잠금으로 직렬화하고 옛 완성본을 무효화했지만, 오래 열린 탭의 정상적인 200 POST가 최신 편성을 덮는 것은 막지 못했다.
- `scene_lab.html`의 `render()`는 읽기용 부팅·배경 repaint에도 `saveWork() → autoApply()`를 실행했고, 30초 시각 휴리스틱은 렌더 직후 재진입에서 옛 localStorage를 서버보다 우선시킬 수 있었다.

## 수정

- 부팅·동일 상태 repaint는 로컬/서버 저장을 하지 않는다. 실제 편집 스냅샷이 달라질 때만 저장한다.
- 서버 편성이 존재하면 재진입 화면은 서버를 단일 기준으로 복원한다.
- 장면 편성 내용 SHA 판본을 GET/apply 왕복에 추가했다. 오래된 탭·판본 없는 구버전 탭의 후속 저장은 409로 거절한다.
- 충돌 후 자동 재시도를 중단하고, 미리보기/최종 렌더도 다른 편성으로 진행하지 않게 막는다.
- 서버가 AI 배치로 되돌아간 뒤 오래된 탭이 옛 편성을 되살리는 경계 조건도 막았다.

## 검증

- 관련 집중 테스트: `74 passed, 1 skipped`
- 렌더 진입 JS 동작 테스트: `21 passed`
- 새 회귀 테스트: 오래된 탭 덮어쓰기, 판본 없는 구버전 후속 저장, revert 뒤 옛 편성 부활, 부팅 무저장.

## 남은 일

- main 병합 뒤 낮 수동 배포는 사장님 확인 후 실행한다.
- 배포 후 실제 브라우저에서 박세현 job을 열기만 했을 때 `apply` POST가 발생하지 않는지 로그로 확인한다.
