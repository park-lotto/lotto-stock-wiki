# 인스타대본스파인 트랙 로그

## 2026-08-19
- 인스타 대본 패턴 실측 조사 완료. reel_history 6,366편 캡션 + script_extracts 571편 전사 대조.
- ★최대 발견: 인스타는 음성=스토리 / 화면=시연이 분리. 대사에 사람 나오는 컷 49개 중 화면에도 사람 나온 건 1개(2%). → 장면 슬롯 4종(문제/투입/작동/결과대비)만 있으면 어떤 스토리든 얹힌다.
- 스파인 후보 4축 확정: 다이소 시리즈(146편/68채널, 훅 고정률 47%, 밀도 226) · 방법배신형(절대~마세요) · 가게비법 훔쳐오기형(45편/28채널) · 발견감탄형(roomoftem.kr).
- 상세: handoff/인스타대본스파인.md

## 2026-08-19 (저녁)
- 다이소축 슬롯 조립 파이프라인 완성(실험실). 영상 5편 전사 → 재료 7종(insta_facts, 쿠팡 안 씀) → 슬롯 → 템플릿 조립 = **모델 호출 0회** 대본.
- 실측: 한국 다이소 5편 7/7·8칸·188자 / 중국 샤오홍슈 3편 5/7·7칸·186자(중국어→한국어 번역 성공).
- 함정 6개: 조용한 실패(pipeline import) · 다른제품 합치기 · 명사형 어미 · 접속사 붕뜸 · 외국어 혼입 · 상한 이중정의.
- ★미결: 장면 근거 게이트(사장님 "장면에도 없는 대본 나오는 거 아닌가" → 실측 5/7·5/6으로 확인됨). 설계 완료, 구현만 남음.

## 2026-08-19 (밤) — 장면 근거 게이트 구현 완료
- `insta_facts.gate_by_scene` 신설. 사물·동작 슬롯(how_to·targets·pain)만 scene_desc에 근거를 요구하고, 판단·추상(effects·numbers·edge·price)은 면제(원래 카메라로 못 찍는다).
- ★값이 아니라 **재료 목록**을 거르는 게 핵심 — 실측 "…누렇게 변한 흰 양말까지"(화면에 양말 없음) → "…오래된 욕실 곰팡이까지"로 대체되며 **8/8칸 유지**. `_first`로 집은 뒤 걸렀다면 칸이 비었다.
- 장면 인벤토리가 비면 그대로 통과(근거 없음 ≠ 모름 — 무자막 소스 전멸 방지).
- 함정 2개: ①낱말 경계 걸친 조각("보여주는 모습"이 `는모`·`여주`로 통과) → bigrams를 낱말 안에서만 자르게 수정 ②로그 try/except 4벌 → `_say` 하나로(침묵예산 테스트가 잡음).
- 0순위-B: 2글자 조각 판정을 `spine_fill.bigrams` 한 벌로 공용화(`_join_targets`와 같은 판정이었다).
- 실측: 전체 4861 passed / 0 failed. 조립 기준선 8/8·6/8·7/8 그대로. 커밋 4133053be.
- ⏭ 다음: **라이브 배선**(`insta_facts`는 아직 app.py에 안 붙었다 — 실험실에서만 돈다).

## 2026-08-19 (밤) — 라이브 배선 완료
- `insta_facts`가 app.py에서 부르는 곳이 0곳이었다 → `INSTA_CATEGORIES`·`_is_insta_context`·`_insta_slots`로 조립 경로에 배선. 다이소축이 실제로 돈다.
- `scripts/seed_daiso_spine.py` 신설 — 파이썬 상수로만 있던 스파인을 DB `spine` 테이블에 심는다(멱등, 정본은 daiso_spine.py에서 읽어옴).
- 0순위-B 공용화 2건: `_is_context()`(썰·인스타 갈래판정 한 벌) · `_facts_per_source()`(편 단위 캐시 추출).
- 실측(DB 스파인으로 라이브 경로 태움·반사실 대조): 게이트 OFF "누렇게 변한 흰 양말부터…" → ON "욕실 수전 물때부터…" 8/8칸 유지. 재료 부족 시 7/8 폴백 + 이유 표시.
- 전체 4867 passed / 0 failed. 커밋 e1e7dd1d8.
- ✅ **라이브 반영 완료(08-20 00:19)**: main 병합 96912f6cc(직접 대조 확인) · 서버 배포·3서비스 재시작 · 서버 스파인 심음(id=57, `_is_insta_context` True).
- ⏭ 남은 것: **사장님 육안 확인** — 같은 소재 5편 담아 대본 뽑기. 지금까지 실측은 재료를 손으로 넣은 것이라, 진짜 Gemini 추출로 도는 건 아직 안 봤다.

## 2026-08-20 — 소스 카드 지표(조회수·댓글·팔로워) + 2단계 이름 버그
- 사장님 지시: 썸네일에 지표 3종, 캡션·대본은 접어서. C안(카드 안 키움) 채택.
- ★"인스타는 조회수 없다"는 **내 오답**(주석만 보고 DB를 안 봄). 실측: 인스타 6,364/6,367 · 유튜브 1,394/1,410건이 이미 있었고, 서버가 `views:None`을 하드코딩해 안 실었을 뿐. → `store.latest_views()`가 reel_history(shortcode 키)+source_enrichment(url 키) **두 곳**을 읽는다. 한 곳만 보면 한 플랫폼이 통째로 빈다.
- ★조용한 0건 함정 예방: `sendToProduce`(랭킹→제작소)가 views/comments/followers를 안 싣고 있었다 — 서버만 고쳤으면 오류 없이 영영 안 떴다.
- 없는 값은 줄을 안 그린다(0을 쓰면 "댓글 0개"가 사실처럼 보인다).
- 2단계 버그: 서버는 `name`을 주는데 화면이 없는 필드 `title`을 읽어 캡션 전문이 이름 자리에 → 썸네일 눌림. 기존 CSS 2줄 클램프는 증상만 가린 반창고였고 원인을 고쳤다.
- 검증: renderPool을 실데이터로 **직접 실행**해 9항목 확인(문법검사는 TDZ를 못 잡는다). 전체 4880 passed/0 failed. 라이브 실측 조회수 5/5 반환.
- 라이브: eb705ff8f (01:15 배포·재시작 완료).

## 2026-08-20 — 다이소 틀이 목록에 안 뜨던 것(내 실수)
- 사장님 제보 "다이소내부인형이 없는데". 스파인은 멀쩡(beat_roles·templates 완비·조립경로 인식 True)했는데 **status를 'active'로 심어서** `list_style_spines(status="approved")`가 통째로 걸러냈다. **오류 없음** — 라이브 5개가 전부 approved인 걸 안 보고 상태 이름을 지어낸 탓.
- ★"심었다"와 "목록에 뜬다"는 다른 일이다. 상태 문자열은 짐작 말고 기존 행이 쓰는 값을 따라라.
- fit_categories에 `홈템` 추가 — 담은 영상은 대개 홈템이라 '다이소형'만 달면 항상 "⚠타소재"로 맨 아래 밀려 못 찾는다. 기계가 읽는 이름(다이소형=조립경로)과 사람이 고르는 이름(홈템=화면목록)이 달라 생긴 문제라 둘 다 단다.
- `set_spine_style(fit_categories=...)` 신설 — 없으면 잘못 심은 행을 영영 못 고친다(add_spine에서만 정해졌었다).
- 회귀 테스트 5건(active로 심으면 목록에서 사라지는 것 자체를 잡는다).
- 라이브: 1fa4fb2eb. 서버 실측 — 상태 approved / fit ['다이소형','홈템'] / 홈템 목록 6개 중 4번째 "검증" / 조립경로 True.
