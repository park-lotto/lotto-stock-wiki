# 영상길이표시 트랙

## 2026-08-04 (회사PC)
랭킹(모든 카테고리) 카드에 ⏱ 영상 길이 표시 — 라이브 병합(c61072bba).

- 인스타 목록 수집엔 길이가 없음(그리드 GraphQL 실측 0/24노드) → `duration_backfill.py` 신설:
  - 인스타 = 릴 상세 노드의 `video_dash_manifest`에서 `mediaPresentationDuration` 파싱
    (서버 실측 3/3 성공). yt-dlp 인스타 추출기는 404로 죽어 있음(같은 날 실측).
  - 유튜브·틱톡 = yt-dlp `--skip-download --print duration`.
  - shortcode당 평생 1회 캐시(`reel_durations`), 실패 3회면 포기, 회당 40건·2s 간격(429 예방).
- 트리거 = `/api/reference`가 1시간에 1번 워커 큐 `durfill` 예약(systemd 추가 없음).
- UI = index.html render() 카드 랭크줄에 `⏱ m:ss`(_fmtDur), 캐시 없는 항목은 표시 생략.
- 테스트 5건(test_duration_backfill.py) green.

⏭ 미완료
- 라이브 실측: /api/reference 호출로 durfill 예약→워커 소비→reel_durations 적재→카드 ⏱ 육안.
- 첫 백필은 508건이라 40건/시간 페이스 — 몇 시간에 걸쳐 채워짐. 빠르게 채우려면
  서버에서 `python3 -m` 루프 실행 또는 PER_RUN_LIMIT 상향 결정 필요(429 리스크와 트레이드오프).
