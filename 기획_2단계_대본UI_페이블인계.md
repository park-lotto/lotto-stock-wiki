# 기획 — 2단계 "대본" 화면 (페이블 인계용)

> 2026-08-16 · 트랙 `채널전략분석` 세션 작성 · **UI 디자인·구현은 페이블이 담당**
> 대상: `shopping_shorts/static/produce.html` 의 **`data-step="8"` 패널 안쪽**
> 짝문서: `.tracks/장면라벨/기획_제작소_단계개편_9단계.md` (골격 개편 — 반드시 먼저 읽을 것)

---

## 0. ⚠️ 착수 전 — 골격을 기다린다 (충돌 방지)

**지금 `produce.html`을 건드리면 병합 충돌이 난다.** 장면라벨 세션이 9단계 골격
(`STEP_LABELS`·`ORB_TO_PANEL`·`jump()` 5곳)을 먼저 main에 올린다.

```
[1] 장면라벨 세션 → 골격 + 빈 패널 <section class="panel" data-step="8"> 올림
[2] 골격이 main에 들어온 뒤 → 페이블이 그 <section> 안쪽만 채운다
```

- **`data-step` 번호를 다시 매기지 마라.** 화면 순서는 `ORB_TO_PANEL`이 정한다.
- 서로 다른 `<section>` 안만 고치면 충돌이 안 난다.
- 진행 중 전달할 말이 생기면 이 문서에 적고 상대 세션 문서를 읽는다(서로 보면서 간다).

---

## 1. 이 화면이 뭘 하는 곳인가

**대본을 고르고, 구조를 지정하고, 문장을 고치는 자리.**

경쟁 프로그램(사장님 제공 화면)은 대본을 **1~7번 나열**로만 보여준다. 어느 줄이 훅이고
어느 줄이 CTA인지 화면이 말해주지 않고, 고치려면 직접 타이핑해야 한다.

**우리가 넘는 지점 = 세 가지. 저쪽은 구조적으로 못 한다.**

| | 저쪽 | 우리 |
|---|---|---|
| 대본 표시 | 번호 나열(1~7) | **역할이 보인다**(훅·전개·CTA) |
| 고치기 | 직접 타이핑 | **은행에서 클릭 교체** |
| 품질 | 고른 대로 나왔는지 아무도 검사 안 함 | **게이트가 검사하고 틀리면 고칠 곳을 짚어준다** |

★사장님 지시: **저 프로그램을 따라하면 안 된다. 넘어야 한다.**

---

## 2. 화면 구성 — 3단

### 2-1. 스타일 고르기 (조합식)

지금 라이브는 **드롭다운 2개**뿐이고 이름 한 줄만 보여준다(`produce.html:1109~1114`).
스타일 하나가 대본 전체를 결정해 **뻣뻣하다.** 축을 갈라 조합할 수 있게 한다.

```
훅 스타일     [단정 명령형 ▾]      ← 사장님이 지정
스토리 전개   [AI에게 맡김 ▾]      ← ★기본값
CTA          [받는 것 명시형 ▾]    ← 헌장 고정
```

- **기본은 전부 "AI에게 맡김"**이고, 바꾸고 싶은 칸만 지정한다(사장님 지시:
  "기본적으로는 AI가 만들게 하고 훅만 이런 스타일로 변경").
- 각 칸은 **수정 가능**해야 한다.

**스타일 카드에 보여줄 것** — 이름만으론 못 고른다. 아래는 **API가 이미 전부 주고 있다**
(`GET /api/script/styles?category=…`, 실측 확인). 지금 화면이 `name`만 쓰고 버리는 중이다.

| 필드 | 값 예시 | 쓸 곳 |
|---|---|---|
| `name` | `단정 명령형` | 카드 제목 |
| `situation_type` | `아는 전문가가 알려준 걸 단정적으로 알려준다` | 한 줄 설명 |
| `beat_roles` | `[hook, before, reveal, after, cta]` | 구조 뱃지 |
| `chars_per_30s` | `240` | `240자/30초` |
| `source_count` | `12` | `근거 12편` |
| `fit` | `검증` / `타소재` | ✅ / ⚠️ 뱃지 |

★**썸네일보다 강한 패가 있다 — 실제 히트작 훅 문장.**
저쪽은 썸네일로 "어떤 영상인지"를 보여주지만, 대본 고르는 화면에서 진짜 궁금한 건
**"내 대본이 어떻게 시작될지"**다. 우리는 전사본 36편이 DB에 있어 이게 가능하다.

```
┌──────────────────────────────────────┐
│ ✅ 단정 명령형        근거 12편 · 검증됨 │
│                                      │
│ "여러분 애들 아침에 쌀 밥 절대 안 돼요"│
│                    ← 실제 557만회 훅  │
│                                      │
│ 단정으로 열고 → 아는 전문가가         │
│ 알려준 걸로 뒤집는다                  │
│                                      │
│ 훅·문제·반전·결과·CTA      240자/30초 │
└──────────────────────────────────────┘
```

⚠️ **대표 훅 문장·썸네일은 지금 API가 안 준다** — 아래 §4 참조(제가 만들 것).

### 2-2. 대본 표시 — 나열식이 아니라 **역할이 보이게**

```
🎣 훅       여러분 애들 아침에 쌀 밥 절대 안 돼요        [바꾸기]
😰 문제     저도 매일 쌀 밥에 김 싸서 먹였는데…          [바꾸기]
💡 반전     단골 소아과 선생님 말씀이…                   [바꾸기]
✨ 결과     아이 아침 컨디션이 훨씬 좋아졌어요            [바꾸기]
📢 CTA      댓글에 '한끼' 남겨주시면 레시피 드릴게요       [바꾸기]
            └ ✅ 받는 것 명시됨
```

- 역할 이름은 **재미있게** 보여준다(사장님 지시). 내부값은 `beat_roles`
  (`hook·before·reveal·after·cta`)이고 라벨은 화면에서 붙인다.
- **[바꾸기]를 누르면 그 칸에 맞는 은행 후보가 뜬다** → 클릭으로 교체(타이핑 없이).
- **문장 단위 수정**이 되어야 한다(사장님 지시: "훅 전개 과정에서 문장들을 다시 수정되게").

### 2-3. 재생성 — 두 층

사장님 지시: **"스타일 1번·메종(예시이름) 재생성 전체를 해도 다시 나오고"**

```
[🔄 전체 다시]   ← 고른 스타일 그대로 대본 전체를 새로
[🎣 훅만 다시]   ← 그 칸만 새로 (다른 칸은 그대로 둔다)
```

칸마다 부분 재생성이 되어야 한다. 전체를 다시 뽑으면 마음에 들던 문장까지 날아간다.

---

## 3. 게이트 결과를 어떻게 보여주나 (★사장님 질문에 대한 답)

**결론: 체크리스트(✓✓✓)로 보여주면 안 된다. 도움이 안 된다.**
"말 밀도 231자 ✓"를 봐도 사장님이 할 수 있는 게 없다 — 개발자 정보다.

**도움이 되는 건 실패했을 때, 어디가 실패했고 뭘 누르면 고쳐지는지다.**

```
통과했을 때 →  ✅ 단정 명령형   (작은 뱃지 하나. 조용히.)

실패했을 때 →  ⚠️ 훅이 이 스타일에서 벗어났어요
                 나온 것: "이 제품 정말 좋아요"
                 [🎣 은행에서 훅 바꾸기]  [🔄 훅만 다시]
```

**게이트를 "성적표"가 아니라 "고칠 곳 안내"로 쓴다.** 실패한 칸을 짚어주고 그 자리에서
고칠 버튼을 단다. 이게 저쪽이 못 하는 부분이다 — 저쪽은 나열식이라 어디가 문제인지
알 수 없고, 고치려면 직접 타이핑해야 한다.

**사용자에게 무슨 도움이 되나** (사장님 질문):
1. **어디를 고쳐야 하는지 즉시 안다** — 대본 전체를 다시 읽고 판단할 필요가 없다
2. **고치는 방법이 그 자리에 있다** — 은행 후보 클릭 = 타이핑 0
3. **품질이 우연이 아님을 안다** — "1회 재작성 후 통과"는 시스템이 관리 중이라는 증거

게이트는 이미 6항목을 검사한다(`shopping_shorts/script_gate.py`, 순수함수).
지금은 **서버 안에서만 돌고 결과가 화면에 안 나온다.** 그걸 드러내는 게 이 작업이다.

---

## 4. 데이터 — 무엇이 준비돼 있고 무엇이 없나 (전부 실측)

### 4-1. 지금 바로 쓸 수 있는 것

`GET /api/script/styles?category=<홈템|레시피|뷰티|기타>`
→ `{ok, styles:[{id,name,situation_type,beat_roles,chars_per_30s,fit_categories,
   source_count,perf_score,fit,evidence}]}`

**라이브 스타일 3종** (2026-08-16 기준, 이름은 **채널명 아닌 특징 기반**):

| id | 이름 | 훅이 여는 방식 | 밀도 | 카테고리 |
|---|---|---|---|---|
| 52 | 가족갈등 반전형 | 가족에게 혼났다 → 알고 보니 그 물건 덕분 | 300 | 홈템 |
| 53 | 단정 명령형 | "여러분 OO 무조건 이렇게 하세요" | 240 | 레시피·홈템 |
| 54 | 물건 발견형 | "모마가 선정한 지우개가 있다는데" | 254 | 홈템·기타 |

⚠️ **카테고리는 4개다** — `홈템·레시피·뷰티·기타`(`app.py:489`). '가전'은 2026-07-31
홈템으로 병합됐다. 없는 라벨을 쓰면 드롭다운이 통째로 안 뜬다(실사고 있음).

### 4-2. ★내가 만들어야 하는 것 (페이블은 기다리면 된다)

**① `[바꾸기]` 후보 API — 은행에서 걸러서 준다**

사장님 지시: **"은행 날것으로 쓰자는 게 아니고, 은행도 검증된 걸 다시 검사해야 한다."**

은행 현황 실측(`pattern_item`, status=approved **5,262건**):

| bucket | 건수 | 상태 |
|---|---|---|
| ending | 1,118 | 어미 조각(`~하더라고요`) — 문장 아님, 후보로 못 씀 |
| cta | 935 | **77%가 헌장 위반**(아래) |
| evidence | 895 | 대부분 빈칸 틀(`{인물}이 {도구}를…`) — 사람이 읽을 문장 아님 |
| emotion | 693 | |
| conflict | 587 | 대부분 빈칸 틀 |
| hook | 534 | 8자 이하 쓰레기 5건(`"대본"`, `"생선 굽기 꿀팁"`) |
| adverb | 379 | |
| price | 121 | |

**CTA가 특히 심각하다 (실측)**:
- 935건 중 **받는 것 명시형은 221건(23%)뿐** → **714건이 헌장 위반**
- **팔로우 유도 130건** — 헌장은 "저장·팔로우로 빠지지 마라, 무조건 댓글"로 못 박았다
  (`script_generate.py:163~168`, 2026-08-04 사장님 확정)

→ **그대로 노출하면 사장님이 쓰레기 중에서 고르게 된다.** 그래서 거르는 층이 필요하다:
```
□ 스타일 문장틀에 맞는 것만 (게이트의 template_matches 재사용 — 판정 로직 1벌 유지)
□ 카테고리 맞는 것만
□ CTA는 헌장 위반(받는 것 없음·팔로우 유도) 제외
□ 길이 하한(훅 8자 이하 등 조각 제거)
□ 정렬 = **빈도순(freq)** ← 사장님 확정
```
⚠️ **`perf_score`는 5,262건 전부 0이다** — "좋은 순" 정렬이 지금은 불가능하다.
   우리 영상 실적이 쌓이면 그때 perf_score로 갈아탄다.

**② 스타일 카드용 대표 훅 문장 / 썸네일**
지금 API는 근거 영상 shortcode를 안 준다(`spine` 테이블이 그 연결을 저장 안 함).
2-1의 "실제 557만회 훅" 카드를 만들려면 이게 필요하다 → **내가 붙인다.**

**③ 부분 재생성 API** (`[훅만 다시]`)
지금 생성은 대본 전체 단위다. 칸 단위 재생성 경로가 없다 → **내가 만든다.**

---

## 5. 페이블에게 — 정리

```
□ 착수 시점: 장면라벨 세션의 9단계 골격이 main에 들어온 뒤
□ 작업 범위: <section class="panel" data-step="8"> 안쪽만
□ data-step 번호 재배치 금지 (순서는 ORB_TO_PANEL이 정한다)
□ 지금 API로 바로 되는 것: 스타일 카드(이름·설명·구조·밀도·근거·검증뱃지)
□ 내가 준비해서 줄 것: [바꾸기] 후보 API · 대표 훅 문장 · 부분 재생성 API
□ 작업은 자기 트랙 폴더에서 (main 폴더 파일 금지 — 흡수 사고)
```

**설계 원칙 세 줄**
1. 저쪽을 따라하지 않는다 — 나열식이 아니라 **역할이 보이는** 대본
2. 고치는 건 타이핑이 아니라 **클릭**(은행에서 고른다)
3. 게이트는 성적표가 아니라 **고칠 곳 안내**

---

## 6. 아직 사장님이 정하실 것

- **`#aiPick`과 스타일 고르기의 관계**: 장면라벨 문서 §6은 새 2단계로 `#aiPick`(AI PICK
  대본) + 스마트믹스 토글을 옮기라고 한다. 그런데 이 문서가 설계한 건 `#pmStyleSec`
  (스타일 고르기) 쪽이다. **둘은 지금 다른 화면**이라 새 2단계에서 어떻게 합칠지는
  사장님 판단이 필요하다.
- 스타일 고르기를 **2개 고정**으로 둘지(대결 구도) 1개도 되게 할지.
