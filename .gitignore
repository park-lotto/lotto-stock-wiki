node_modules/
.env
out/*.mp4
out/*.pdf
out/*.png
raw/*.xlsm
raw/*.xlsx
raw/*.pdf
raw/*.png
raw/리모션*
.obsidian/
demos/
.claude/*
!.claude/settings.json
.agents/
!.agents/skills/**
data/

# 카카오 OAuth 비밀
scripts/kakao/.env
scripts/kakao/.tokens.json

# Playwright MCP cache
.playwright-mcp/

# 네이버 프리미엄 아카이브 (대용량 로컬 전용)
raw/naver_premium/

# 임시 테스트/스크린샷 파일
viewtrap_check.png
naver_premium_test.png
raw/actual_ingest_result.txt
raw/full_test_result.txt
raw/ingest_now.txt
raw/osc_*.txt
raw/tg_test.txt
raw/universe_check.txt
raw/wise_test.txt
raw/excel_*.txt
scripts/__pycache__/
__pycache__/
*.pyc
*.pyo
.gstack/

# MCP 설정 (API 키 포함 가능)
.mcp.json

# 캡처 이미지 폴더
raw/캡쳐본/

# videos 폴더 대용량 MP4 (로컬 전용)
videos/**/*.mp4
videos/**/input-video.mp4

# remotion public 영상 (로컬 전용)
remotion-stock/public/**/*.mp4
remotion-stock/public/**/*.mov

# productions 원본 녹화·음성 (대용량 로컬 전용)
productions/**/*.mp4
productions/**/*.mkv
productions/**/*.m4a
productions/**/*.mp3
productions/**/*.wav
.superpowers/

# 시그널 스냅샷 (매일 build_signal_snapshot로 재생성)
output/signal/

# 벡터DB 생성물 (재생성됨, 대용량)
pipeline/atoms/chroma_db/
pipeline/atoms/.gemini_key_state.json
pipeline/atoms/krx_industry.json
pipeline/atoms/stock_sector_review.md
sync_crawl.log
pipeline/atoms/staging/

# 딸깍 스튜디오 런타임 산출물
out/studio/

# 서버 배포 자격증명 (커밋 금지)
deploy/stockbrain.env
/etc/stockbrain.env
pipeline/push_token.txt
pipeline/news_feed.json
pipeline/callouts/
scratch_pw.txt
scripts/.kiwoom_token_cache.json
scripts/.kis_token_cache.json
scripts/.*_token_cache.json

# 사용자 런타임 편집/백업 (배포로 덮이면 안 됨)
pipeline/sector_custom.json
pipeline/watchlist.json
pipeline/sector_custom_snapshots/

# 골-루프 런타임 상태
out/goal_loop/

# 인용 스튜디오 저장 산출물(픽한 quotes.json)
out/quote_studio/

# 런타임 자동생성 데이터 (추적 제외 — 매 실행마다 바뀌어 dirty/충돌 유발)
pipeline/atoms/atoms.db
pipeline/atoms/daily_verify_history.json
output/etf_bar_last_good.json
output/flow_history.json
output/market_briefing.json
output/weather_state.json

# 장면 수집 산출물(프레임/클립)
out/scenes/

# Remotion 자산생성기 의존성 (로컬 전용 — 자산은 프리렌더 커밋)
shopping_shorts/motion/node_modules/

# 트랙 작업 폴더(worktree) — 프로젝트 안에 두되 git·옵시디언 양쪽에서 안 보이게.
# 점(.)으로 시작해 옵시디언이 자동으로 인덱싱에서 제외한다(11,120개 .md의 중복 방지).
/.tracks/

# ── 서버·PC가 각자 '직접 쓰는' 런타임 상태 (2026-07-16) ────────────────
# git으로 공유하면 안 되는 것들. 두 대가 같은 파일을 쓰면 서로 덮어써 무의미하고,
# auto_deploy.sh가 reset --hard로 도는 지금은 추적된 채 두면 서버 상태가 3분마다 초기화된다
# (실측: autopilot_state.json에 그날 detected/escalated 카운터가 들어있어 초기화되면
#  자동수정 오토파일럿이 이미 처리한 걸 재감지·재시도한다).
pipeline/atoms/autopilot_state.json
output/regular_close_prices.json
pipeline/watchlist.json.bak
test_final.json
