# 보이스 자연화 엔진 (계획 2: ASR 검증 + 튜닝 작업대) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.
> **선행:** 계획 1(`2026-07-15-보이스-자연화엔진-1-엔진과통합.md`) 완료 필수 — `narration_naturalize.naturalize`·`tts.synthesize_best`가 있어야 함.

**Goal:** 성우별로 자연화 프로파일을 귀로 튜닝하는 작업대(HTML+API 4개)를 만든다. 회귀 코퍼스 전체를 N-best로 합성하고, Whisper 재전사 diff로 오독을 자동경보하며, 완성 프로파일을 프리셋에 동결한다.

**Architecture:** 튜닝 전용 API 4개는 프로덕션 믹스 경로와 분리. `asr_check.py`가 Whisper(GROQ)로 TTS를 재전사해 원문과 diff. `voice_tune.html`이 코퍼스 카드 UI. 동결 프로파일은 `voice_presets` 테이블의 신규 컬럼 `naturalize_profile_json`에 저장.

**Tech Stack:** Python, FastAPI(app.py 라우트 추가), pytest+monkeypatch, GROQ Whisper API(requests), SQLite(ALTER 마이그레이션), 순수 HTML/JS.

**참고 스펙:** `docs/superpowers/specs/2026-07-15-쇼핑쇼츠-보이스-자연화엔진-AI티제거-design.md` §4

**공유 워킹트리 주의:** `git add`는 자기 파일만. 커밋 전 `git status`로 남의 hunk 확인. main 고정.

---

## Task 1: config GROQ 키 + asr_check.py (Whisper 재전사 diff)

**Files:**
- Modify: `shopping_shorts/config.py` (키 추가)
- Create: `shopping_shorts/asr_check.py`
- Test: `shopping_shorts/tests/test_asr_check.py`

- [ ] **Step 1: 실패 테스트 작성**

```python
# shopping_shorts/tests/test_asr_check.py
from shopping_shorts import asr_check


def test_diff_flags_mismatched_words():
    """원문 vs 재전사 → 다른 토큰을 mismatch로 표시(정규화 후 비교)."""
    d = asr_check.diff_words("이건 정말 좋아요", "이건 저말 좋아요")
    assert d["ok"] is False
    assert any(w["kind"] == "sub" and w["ref"] == "정말" for w in d["words"])

def test_diff_ignores_tags_and_punct():
    """감정태그·문장부호는 무시하고 발음 토큰만 비교."""
    d = asr_check.diff_words("[warm] 이건 좋아요.", "이건 좋아요")
    assert d["ok"] is True

def test_transcribe_without_key_returns_none(monkeypatch):
    monkeypatch.setattr(asr_check.config, "GROQ_API_KEY", "")
    assert asr_check.transcribe("/x.mp3") is None

def test_asr_score_higher_when_more_mismatch():
    good = asr_check.diff_words("좋아요", "좋아요")
    bad = asr_check.diff_words("좋아요 예뻐요", "조아요 이뻐요")
    assert asr_check.mismatch_score(bad) > asr_check.mismatch_score(good)
```

- [ ] **Step 2: 실패 확인**

Run: `cd shopping_shorts && python -m pytest tests/test_asr_check.py -v`
Expected: FAIL (모듈 없음)

- [ ] **Step 3: config에 키 추가**

`shopping_shorts/config.py`의 ELEVENLABS 키 근처에 추가:

```python
# ASR 라운드트립 검증(튜닝 작업대) — Whisper로 TTS를 재전사해 오독 탐지. GROQ 우선.
GROQ_API_KEY = os.environ.get("GROQ_API_KEY", "")
```

- [ ] **Step 4: asr_check.py 구현**

```python
# shopping_shorts/asr_check.py
"""Whisper(GROQ)로 TTS mp3를 재전사해 원문과 diff → 오독/탈락 자동경보(튜닝 작업대용).

키 없으면 transcribe가 None(작업대는 diff 없이 재생만). 순수 diff는 키와 무관하게 동작."""
import re
import difflib

import requests

from shopping_shorts import config

_GROQ_URL = "https://api.groq.com/openai/v1/audio/transcriptions"
_MODEL = "whisper-large-v3"


def _tokens(text):
    """비교용 토큰화: 감정태그([..])·문장부호 제거, 공백분리, 소문자."""
    text = re.sub(r"\[[^\]]+\]", " ", text)
    text = re.sub(r"[^\w가-힣\s]", " ", text)
    return [t for t in text.split() if t]


def diff_words(ref_text, hyp_text):
    """원문 ref vs 재전사 hyp → {ok, words:[{kind, ref, hyp}]}. kind: equal|sub|del|ins."""
    ref, hyp = _tokens(ref_text), _tokens(hyp_text)
    sm = difflib.SequenceMatcher(a=ref, b=hyp)
    words = []
    for tag, i1, i2, j1, j2 in sm.get_opcodes():
        if tag == "equal":
            for k in range(i1, i2):
                words.append({"kind": "equal", "ref": ref[k], "hyp": ref[k]})
        elif tag == "replace":
            for k in range(max(i2 - i1, j2 - j1)):
                words.append({"kind": "sub",
                              "ref": ref[i1 + k] if i1 + k < i2 else "",
                              "hyp": hyp[j1 + k] if j1 + k < j2 else ""})
        elif tag == "delete":
            for k in range(i1, i2):
                words.append({"kind": "del", "ref": ref[k], "hyp": ""})
        elif tag == "insert":
            for k in range(j1, j2):
                words.append({"kind": "ins", "ref": "", "hyp": hyp[k]})
    ok = all(w["kind"] == "equal" for w in words)
    return {"ok": ok, "words": words}


def mismatch_score(diff):
    """오독 점수(낮을수록 좋음) = 불일치 토큰 수. N-best ranker로 사용."""
    return sum(1 for w in diff["words"] if w["kind"] != "equal")


def transcribe(mp3_path):
    """GROQ Whisper로 재전사. 키 없으면 None. 실패 시 None(작업대는 graceful)."""
    if not config.GROQ_API_KEY:
        return None
    try:
        with open(mp3_path, "rb") as f:
            r = requests.post(_GROQ_URL,
                              headers={"Authorization": f"Bearer {config.GROQ_API_KEY}"},
                              files={"file": (mp3_path, f, "audio/mpeg")},
                              data={"model": _MODEL, "language": "ko"}, timeout=60)
        r.raise_for_status()
        return r.json().get("text", "")
    except Exception:
        return None
```

- [ ] **Step 5: 통과 확인 + 커밋**

Run: `cd shopping_shorts && python -m pytest tests/test_asr_check.py -v`
Expected: PASS

```bash
git add shopping_shorts/config.py shopping_shorts/asr_check.py shopping_shorts/tests/test_asr_check.py
git commit -m "feat(shopping_shorts): asr_check — Whisper 재전사 diff(오독 자동경보)"
```

---

## Task 2: voice_presets 프로파일 저장 (컬럼 마이그레이션 + JSON 필드)

**Files:**
- Modify: `shopping_shorts/store.py` (테이블 컬럼·upsert·get·row_to_preset)
- Modify: `shopping_shorts/assets/voice_presets.json` (프로파일 필드 + speaker_boost 정리)
- Test: `shopping_shorts/tests/test_voice_profile_store.py` (신규)

- [ ] **Step 1: 실패 테스트 작성**

```python
# shopping_shorts/tests/test_voice_profile_store.py
import tempfile, os
from shopping_shorts.store import Store


def _store():
    p = os.path.join(tempfile.mkdtemp(), "t.db")
    return Store(p)


def test_upsert_and_get_naturalize_profile():
    s = _store()
    s.upsert_voice_preset({
        "preset_id": "kr-x", "name": "X", "lang": "KR", "base_voice_id": "v",
        "voice_settings": {"stability": 0.5}, "model_id": "eleven_v3",
        "naturalize_profile": {"spoken_style": {"intensity": 0.7}},
    })
    p = s.get_voice_preset("kr-x")
    assert p["naturalize_profile"]["spoken_style"]["intensity"] == 0.7

def test_missing_profile_is_none():
    s = _store()
    s.upsert_voice_preset({"preset_id": "kr-y", "name": "Y", "lang": "KR",
                           "base_voice_id": "v", "voice_settings": {}})
    assert s.get_voice_preset("kr-y").get("naturalize_profile") in (None, {})
```

- [ ] **Step 2: 실패 확인**

Run: `cd shopping_shorts && python -m pytest tests/test_voice_profile_store.py -v`
Expected: FAIL

- [ ] **Step 3: store.py 수정**

(a) 테이블 백필 루프([store.py:238-241])에 컬럼 추가:

```python
            for col, ddl in (("group_id", "TEXT"), ("variant", "TEXT NOT NULL DEFAULT 'stable'"),
                             ("naturalize_profile_json", "TEXT")):
                try:
                    c.execute(f"ALTER TABLE voice_presets ADD COLUMN {col} {ddl}")
                except sqlite3.OperationalError:
                    pass
```

(b) `upsert_voice_preset`([store.py:1069])에서 INSERT 컬럼·값에 `naturalize_profile_json` 포함. 기존 INSERT문에 컬럼 추가:

```python
    def upsert_voice_preset(self, p):
        import json as _json
        prof = p.get("naturalize_profile")
        prof_json = _json.dumps(prof, ensure_ascii=False) if prof is not None else None
        with self._conn() as c:
            c.execute("""
                INSERT INTO voice_presets(preset_id, name, one_liner, lang, archetype,
                    base_voice_id, model_id, voice_settings_json, default_speed,
                    default_silence_trim, sample_file, source_ref, origin, created_at,
                    group_id, variant, naturalize_profile_json)
                VALUES(?,?,?,?,?,?,?,?,?,?,?,?,?,COALESCE((SELECT created_at FROM voice_presets WHERE preset_id=?), datetime('now')),?,?,?)
                ON CONFLICT(preset_id) DO UPDATE SET
                    name=excluded.name, one_liner=excluded.one_liner, lang=excluded.lang,
                    archetype=excluded.archetype, base_voice_id=excluded.base_voice_id,
                    model_id=excluded.model_id, voice_settings_json=excluded.voice_settings_json,
                    default_speed=excluded.default_speed, default_silence_trim=excluded.default_silence_trim,
                    sample_file=excluded.sample_file, source_ref=excluded.source_ref,
                    origin=excluded.origin, group_id=excluded.group_id, variant=excluded.variant,
                    naturalize_profile_json=excluded.naturalize_profile_json
            """, (p["preset_id"], p["name"], p.get("one_liner"), p.get("lang", "KR"),
                  p.get("archetype"), p["base_voice_id"], p.get("model_id", "eleven_multilingual_v2"),
                  _json.dumps(p.get("voice_settings", {}), ensure_ascii=False),
                  p.get("default_speed", 1.0), p.get("default_silence_trim", "off"),
                  p.get("sample_file"), p.get("source_ref"), p.get("origin", "curated"),
                  p["preset_id"], p.get("group_id"), p.get("variant", "stable"), prof_json))
```

> 주: 위는 기존 upsert의 구조를 유지하되 컬럼 하나 추가한 형태다. 실제 파일의 기존 INSERT문 정확한 형태를 먼저 읽고, `naturalize_profile_json` 컬럼·값·ON CONFLICT 절만 얹어라(파라미터 순서 어긋나지 않게).

(c) `get_voice_preset`·`list_voice_presets` SELECT에 `naturalize_profile_json` 추가하고, `_row_to_preset`에서 파싱:

```python
    # _row_to_preset 내부에 추가(voice_settings_json 파싱하는 근처):
    #   "naturalize_profile": _json.loads(r["naturalize_profile_json"]) if r["naturalize_profile_json"] else None,
```

SELECT 컬럼 목록 두 곳(get·list)에 `,naturalize_profile_json` 추가.

- [ ] **Step 4: voice_presets.json 정리**

`shopping_shorts/assets/voice_presets.json`의 모든 프리셋에서 `"use_speaker_boost": true` 제거(v3 비호환 — 코드도 방어하지만 소스도 정리). 스크립트로 일괄:

```bash
cd shopping_shorts && python -c "
import json,io
p='assets/voice_presets.json'
d=json.load(open(p,encoding='utf-8'))
for x in d: x.get('voice_settings',{}).pop('use_speaker_boost',None)
json.dump(d,open(p,'w',encoding='utf-8'),ensure_ascii=False,indent=2)
print('cleaned',len(d))
"
```

- [ ] **Step 5: 통과 확인 + 커밋**

Run: `cd shopping_shorts && python -m pytest tests/test_voice_profile_store.py tests/test_app_voice.py -v`
Expected: PASS (신규 + 기존 보이스 회귀)

```bash
git add shopping_shorts/store.py shopping_shorts/assets/voice_presets.json shopping_shorts/tests/test_voice_profile_store.py
git commit -m "feat(shopping_shorts): 프리셋에 naturalize_profile 저장 + speaker_boost 정리"
```

---

## Task 3: 회귀 코퍼스 + /corpus + /preview API

**Files:**
- Create: `shopping_shorts/assets/tune_corpus.json`
- Modify: `shopping_shorts/app.py` (라우트 2개)
- Test: `shopping_shorts/tests/test_voice_tune_api.py` (신규)

- [ ] **Step 1: 코퍼스 파일 작성**

```json
// shopping_shorts/assets/tune_corpus.json
[
  {"id": "hook1", "role": "hook", "text": "이거 진짜 대박이거든요 끝까지 보세요"},
  {"id": "hook2", "role": "hook", "text": "어머 이건 사야 돼 진짜"},
  {"id": "product1", "role": "body", "text": "3.5kg에 이천 원이면 완전 혜자죠"},
  {"id": "product2", "role": "body", "text": "SNS에서 2024년부터 난리 난 그 제품이에요"},
  {"id": "number1", "role": "body", "text": "50% 할인에 배송비까지 무료입니다"},
  {"id": "story1", "role": "build", "text": "저도 처음엔 반신반의했는데 써보고 생각이 완전 바뀌었어요"},
  {"id": "long1", "role": "body", "text": "이게 싸고 좋은데 품질도 최고고 심지어 디자인까지 예뻐서 안 살 이유가 없더라고요"},
  {"id": "cta1", "role": "cta", "text": "지금 링크 눌러서 확인해보세요"},
  {"id": "cta2", "role": "cta", "text": "품절되기 전에 서두르세요"},
  {"id": "short1", "role": "hook", "text": "이거 봤어요?"}
]
```

- [ ] **Step 2: 실패 테스트 작성**

```python
# shopping_shorts/tests/test_voice_tune_api.py
from fastapi.testclient import TestClient
from shopping_shorts.app import app

client = TestClient(app)


def test_corpus_returns_lines():
    r = client.get("/api/voice-tune/corpus")
    assert r.status_code == 200
    lines = r.json()["lines"]
    assert any(l["role"] == "hook" for l in lines)

def test_preview_transforms_text_no_synth():
    r = client.post("/api/voice-tune/preview", json={
        "text": "이건 정말 좋습니다. 50% 할인",
        "profile": {"spoken_style": {"on": True, "intensity": 1.0},
                    "normalize": {"on": True}},
        "beat_role": "hook",
    })
    assert r.status_code == 200
    out = r.json()["text"]
    assert "좋아요" in out and "오십 퍼센트" in out
```

- [ ] **Step 3: 실패 확인**

Run: `cd shopping_shorts && python -m pytest tests/test_voice_tune_api.py -v`
Expected: FAIL (404 라우트 없음)

- [ ] **Step 4: app.py 라우트 추가**

app.py 상단 import에 추가:

```python
from shopping_shorts.narration_naturalize import naturalize as _naturalize
```

voice-presets 라우트 근처([app.py:1049] 부근)에 추가:

```python
_TUNE_CORPUS = Path(__file__).parent / "assets" / "tune_corpus.json"


@app.get("/api/voice-tune/corpus")
def api_voice_tune_corpus():
    import json
    lines = json.loads(_TUNE_CORPUS.read_text(encoding="utf-8")) if _TUNE_CORPUS.exists() else []
    return {"lines": lines}


@app.post("/api/voice-tune/preview")
async def api_voice_tune_preview(req: Request):
    body = await req.json()
    text = body.get("text", "")
    profile = body.get("profile") or {}
    out = _naturalize(text, profile, beat_role=body.get("beat_role"),
                      beat_index=body.get("beat_index"), beat_total=body.get("beat_total"))
    return {"text": out}
```

> 주: `Request`가 import 안 돼있으면 `from fastapi import Request` 추가. auth 미들웨어([app.py:1370])가 `/api/voice-tune/*`를 막으면 `_AUTH_ALLOW` 또는 prefix 허용에 추가(작업대는 운영자 전용이지만 로컬 튜닝이라 접근 가능해야 함).

- [ ] **Step 5: 통과 확인 + 커밋**

Run: `cd shopping_shorts && python -m pytest tests/test_voice_tune_api.py -v`
Expected: PASS

```bash
git add shopping_shorts/assets/tune_corpus.json shopping_shorts/app.py shopping_shorts/tests/test_voice_tune_api.py
git commit -m "feat(shopping_shorts): 튜닝 코퍼스 + /voice-tune/corpus·preview API"
```

---

## Task 4: /synth (N-best + ASR diff + 캐싱) + /profile GET·POST(동결)

**Files:**
- Modify: `shopping_shorts/app.py` (라우트 3개)
- Test: `shopping_shorts/tests/test_voice_tune_api.py`

- [ ] **Step 1: 실패 테스트 추가**

```python
def test_synth_returns_audio_and_diff(monkeypatch, tmp_path):
    from shopping_shorts import app as appmod
    # TTS·ASR 스텁(실 API 안 부름)
    monkeypatch.setattr(appmod.tts, "synthesize_best",
                        lambda text, out_path, **kw: (open(out_path, "wb").write(b"x"), out_path)[1])
    monkeypatch.setattr(appmod.asr_check, "transcribe", lambda p: "이건 좋아요")
    r = client.post("/api/voice-tune/synth", json={
        "text": "이건 좋아요", "profile": {"n_best": 1, "seed": 3},
        "line_id": "hook1", "preset_id": "kr-test",
    })
    assert r.status_code == 200
    j = r.json()
    assert "audio_url" in j and "diff" in j and j["diff"]["ok"] is True

def test_profile_freeze_and_load():
    save = client.post("/api/voice-tune/profile/kr-test",
                       json={"profile": {"spoken_style": {"intensity": 0.6}}, "frozen": True})
    assert save.status_code == 200
    got = client.get("/api/voice-tune/profile/kr-test")
    assert got.json()["profile"]["spoken_style"]["intensity"] == 0.6
```

- [ ] **Step 2: 실패 확인**

Run: `cd shopping_shorts && python -m pytest tests/test_voice_tune_api.py -k "synth or freeze" -v`
Expected: FAIL

- [ ] **Step 3: app.py 라우트 추가**

app.py import에 추가:

```python
from shopping_shorts import tts, asr_check
```

라우트 추가:

```python
import hashlib
_TUNE_CACHE = Path(__file__).parent / "data" / "voice_tune_cache"


@app.post("/api/voice-tune/synth")
async def api_voice_tune_synth(req: Request):
    """변환텍스트를 N-best 합성 + ASR diff. (변환텍스트,seed,voice) 캐시."""
    body = await req.json()
    profile = body.get("profile") or {}
    text = _naturalize(body.get("text", ""), profile, beat_role=body.get("beat_role"))
    preset_id = body.get("preset_id", "adhoc")
    n = int(profile.get("n_best", 1))
    seed = profile.get("seed")
    voice_id = body.get("voice_id")
    settings = body.get("voice_settings")
    key = hashlib.sha1(f"{text}|{seed}|{voice_id}|{settings}|{n}".encode()).hexdigest()[:16]
    _TUNE_CACHE.mkdir(parents=True, exist_ok=True)
    out = _TUNE_CACHE / f"{preset_id}_{body.get('line_id','x')}_{key}.mp3"
    if not out.exists():
        def _ranker(path, t):
            hyp = asr_check.transcribe(path)
            return asr_check.mismatch_score(asr_check.diff_words(t, hyp)) if hyp else 0
        tts.synthesize_best(text, str(out), n=n, base_seed=seed, ranker=_ranker,
                            voice_id=voice_id, voice_settings=settings, model_id="eleven_v3")
    hyp = asr_check.transcribe(str(out))
    diff = asr_check.diff_words(text, hyp) if hyp else {"ok": True, "words": [], "no_asr": True}
    return {"audio_url": f"/api/voice-tune/audio/{out.name}", "text": text, "diff": diff}


@app.get("/api/voice-tune/audio/{fname}")
def api_voice_tune_audio(fname: str):
    f = _TUNE_CACHE / fname
    if not f.exists():
        return JSONResponse({"error": "not found"}, status_code=404)
    return FileResponse(str(f), media_type="audio/mpeg")


@app.get("/api/voice-tune/profile/{preset_id}")
def api_voice_tune_profile_get(preset_id: str):
    p = Store(DB_PATH).get_voice_preset(preset_id)
    return {"profile": (p or {}).get("naturalize_profile") if p else None}


@app.post("/api/voice-tune/profile/{preset_id}")
async def api_voice_tune_profile_save(preset_id: str, req: Request):
    body = await req.json()
    store = Store(DB_PATH)
    p = store.get_voice_preset(preset_id)
    if not p:
        # 작업대 임시 프리셋 허용(테스트/adhoc): 최소 필드로 생성
        p = {"preset_id": preset_id, "name": preset_id, "lang": "KR",
             "base_voice_id": body.get("voice_id", "v"), "voice_settings": {},
             "model_id": "eleven_v3"}
    prof = dict(body.get("profile") or {})
    prof["_frozen"] = bool(body.get("frozen"))
    p["naturalize_profile"] = prof
    store.upsert_voice_preset(p)
    return {"ok": True}
```

> 주: `JSONResponse`·`FileResponse`는 이미 import됨([app.py:13]). `Store`·`DB_PATH`도 app.py에 존재. `Request` 없으면 추가.

- [ ] **Step 4: 통과 확인 + 커밋**

Run: `cd shopping_shorts && python -m pytest tests/test_voice_tune_api.py -v`
Expected: PASS

```bash
git add shopping_shorts/app.py shopping_shorts/tests/test_voice_tune_api.py
git commit -m "feat(shopping_shorts): /voice-tune synth(N-best+ASR diff+캐싱)·profile 동결 API"
```

---

## Task 5: 튜닝 작업대 HTML

**Files:**
- Create: `shopping_shorts/static/voice_tune.html`
- Test: 수동(브라우저) — 자동테스트는 API로 이미 커버

- [ ] **Step 1: voice_tune.html 작성**

핵심 요소만(스펙 §4 레이아웃): 좌측 코퍼스 카드 리스트, 우측 프로파일 컨트롤(8스테이지 토글+슬라이더, seed, n_best), [전체 재합성], 각 카드에 [▶]재생·변환텍스트·ASR diff·[재롤], [💾 동결].

```html
<!doctype html><html lang="ko"><head><meta charset="utf-8">
<meta name="viewport" content="width=device-width,initial-scale=1">
<title>보이스 튜닝 작업대</title>
<style>
 body{font-family:system-ui,sans-serif;margin:0;display:flex;height:100vh}
 #left{flex:1;overflow:auto;padding:16px;border-right:1px solid #ddd}
 #right{width:320px;padding:16px;overflow:auto}
 .card{border:1px solid #ddd;border-radius:8px;padding:10px;margin-bottom:10px}
 .orig{color:#333;font-weight:600}
 .conv{color:#0a7;font-size:14px;margin:4px 0;white-space:pre-wrap}
 .diff .sub,.diff .del{color:#e33;font-weight:700}
 .row{display:flex;align-items:center;gap:8px;margin:6px 0}
 .stage{display:flex;align-items:center;gap:6px;margin:4px 0;font-size:13px}
 .stage input[type=range]{flex:1}
 button{cursor:pointer}
</style></head><body>
<div id="left">
  <div class="row"><select id="preset"></select>
    <button onclick="synthAll()">전체 재합성</button></div>
  <div id="cards"></div>
</div>
<div id="right">
  <h3>프로파일</h3><div id="stages"></div>
  <div class="row">seed <input id="seed" type="number" value="42" style="width:70px">
    N-best <input id="nbest" type="number" value="3" style="width:50px"></div>
  <button onclick="freeze()">💾 이 프로파일 동결</button>
  <p id="msg" style="color:#0a7"></p>
</div>
<script>
const STAGES=[["normalize","정규화"],["spoken_style","구어체"],["pronunciation","연음/발음"],
 ["phrasing","끊어읽기"],["endings","끝음"],["fillers","추임새"],
 ["emotion_arc","감정곡선"],["intonation","억양"]];
let corpus=[], profile={};

function defaultProfile(){
 const p={caps:{max_tags_total:3,max_tags_per_beat:1,max_fillers_per_text:1}};
 STAGES.forEach(([k])=>p[k]={on:true,intensity:0.3});
 p.normalize={on:true}; p.pronunciation={on:true,dict:{}};
 p.fillers={on:true,intensity:0.2,bank:["음","아","그","뭐","자"]};
 return p;
}
function renderStages(){
 document.getElementById('stages').innerHTML=STAGES.map(([k,label])=>{
  const s=profile[k]||{on:true,intensity:0.3};
  const slider=('intensity' in s)?`<input type=range min=0 max=1 step=0.05 value=${s.intensity}
     oninput="setInt('${k}',this.value)">`:'';
  return `<div class="stage"><input type=checkbox ${s.on?'checked':''}
     onchange="setOn('${k}',this.checked)">${label}${slider}</div>`;
 }).join('');
}
function setOn(k,v){profile[k]=profile[k]||{};profile[k].on=v;previewAll();}
function setInt(k,v){profile[k]=profile[k]||{};profile[k].intensity=parseFloat(v);previewAll();}

async function previewAll(){
 for(const c of corpus){
  const r=await fetch('/api/voice-tune/preview',{method:'POST',headers:{'Content-Type':'application/json'},
   body:JSON.stringify({text:c.text,profile:withMeta(),beat_role:c.role})});
  const j=await r.json();
  document.getElementById('conv-'+c.id).textContent=j.text;
 }
}
function withMeta(){profile.seed=parseInt(seed.value);profile.n_best=parseInt(nbest.value);return profile;}
function renderCards(){
 document.getElementById('cards').innerHTML=corpus.map(c=>`
  <div class="card">
   <div class="orig">${c.role} · ${c.text}</div>
   <div class="conv" id="conv-${c.id}"></div>
   <div class="row"><button onclick="synthOne('${c.id}')">▶ 합성/재롤</button>
     <audio id="au-${c.id}" controls style="height:32px"></audio></div>
   <div class="diff" id="diff-${c.id}"></div>
  </div>`).join('');
}
async function synthOne(id){
 const c=corpus.find(x=>x.id===id);
 const r=await fetch('/api/voice-tune/synth',{method:'POST',headers:{'Content-Type':'application/json'},
  body:JSON.stringify({text:c.text,profile:withMeta(),line_id:id,preset_id:preset.value,beat_role:c.role})});
 const j=await r.json();
 document.getElementById('au-'+id).src=j.audio_url+'?t='+Date.now();
 document.getElementById('conv-'+id).textContent=j.text;
 const d=document.getElementById('diff-'+id);
 if(j.diff.no_asr){d.innerHTML='<span style=color:#999>ASR 미설정</span>';}
 else d.innerHTML=j.diff.ok?'<span style=color:#0a7>✓ 오독없음</span>':
   j.diff.words.map(w=>`<span class="${w.kind}">${w.ref||w.hyp}</span>`).join(' ');
}
async function synthAll(){for(const c of corpus)await synthOne(c.id);}
async function freeze(){
 const r=await fetch('/api/voice-tune/profile/'+preset.value,{method:'POST',
  headers:{'Content-Type':'application/json'},body:JSON.stringify({profile:withMeta(),frozen:true})});
 document.getElementById('msg').textContent=(await r.json()).ok?'동결 완료 ✓':'실패';
}
async function loadPreset(){
 const r=await fetch('/api/voice-tune/profile/'+preset.value);
 const j=await r.json();
 profile=(j.profile&&Object.keys(j.profile).length)?j.profile:defaultProfile();
 if(profile.seed)seed.value=profile.seed; if(profile.n_best)nbest.value=profile.n_best;
 renderStages();previewAll();
}
async function init(){
 const pr=await (await fetch('/api/voice-presets?lang=KR')).json();
 preset.innerHTML=pr.presets.map(p=>`<option value="${p.preset_id}">${p.name}·${p.variant||''}</option>`).join('');
 preset.onchange=loadPreset;
 corpus=(await (await fetch('/api/voice-tune/corpus')).json()).lines;
 renderCards(); await loadPreset();
}
init();
</script></body></html>
```

> 주: `/api/voice-presets` 응답의 키(`presets` 배열, `preset_id`·`name`·`variant`)는 실제 응답형태를 [app.py:1049] 확인 후 맞춰라(다르면 매핑만 조정). auth가 `/voice_tune.html`을 막으면 `_AUTH_ALLOW`에 추가.

- [ ] **Step 2: 서버 기동 + 브라우저 grounding (수동 게이트)**

Run(repo 루트에서): `python -m uvicorn shopping_shorts.app:app --port 8850`
브라우저 `http://localhost:8850/voice_tune.html`:
- 프리셋 드롭다운·코퍼스 카드 렌더 확인
- 스테이지 토글/슬라이더 → 변환텍스트 실시간 갱신 확인(합성 없이)
- (ELEVENLABS 키 있으면) [▶] → 오디오 재생 + ASR diff 표시
- [💾 동결] → "동결 완료" 확인, 재로드 시 프로파일 유지

- [ ] **Step 3: 커밋**

```bash
git add shopping_shorts/static/voice_tune.html
git commit -m "feat(shopping_shorts): 보이스 튜닝 작업대 HTML"
```

---

## 자체검토 결과 (계획 2)

- **스펙 §4 커버리지**: 코퍼스(T3) ✅ / preview 실시간 미리보기(T3·HTML) ✅ / synth N-best+ASR diff+캐싱(T4) ✅ / profile 동결·로드(T4) ✅ / 작업대 HTML(T5) ✅ / ASR 자동경보(T1·T4·T5) ✅.
- **타입 일관성**: `synthesize_best(text, out_path, n, base_seed, ranker, **kw)` = 계획1 T10 시그니처와 일치. `diff_words`/`mismatch_score`/`transcribe` = asr_check T1 정의와 라우트 사용 일치. `naturalize` 시그니처 = 계획1과 일치.
- **플레이스홀더 스캔**: 없음. 단 3곳 "실제 파일 확인 후 맞춰라" 주석(upsert INSERT문·voice-presets 응답키·auth allow) — 이는 기존 코드 형태 의존이라 구현자가 읽고 정합, 플레이스홀더 아님.
- **크로스 계획 의존**: 계획 1의 `naturalize`·`synthesize_best` 필수 → 헤더에 명시. 계획 1 먼저 실행.
- **수동 게이트**: T5 Step2가 실합성 육안/청각 검증(스펙 §6의 "진짜 게이트"). 자동테스트는 API 로직까지만.
