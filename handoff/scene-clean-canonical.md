# 장면꾸미기 청소본 정본 선택

## 제보와 라이브 실측

- 2026-09-15 박진우 고객(341), `무타공_얼음정수기`, job `c9ae4dc6b8a6`.
- `clean_status=ready`, 현재 편성 청소본 `final_clean_d507194bec022d18.mp4` 정상 존재(16,663,290B).
- 현재 편성 서명도 `d507194bec022d18`로 파일과 일치했지만 `clean_video_path=None`이었다.
- 배포 전 `_clean_frame_src()` 실측 결과는 `({}, None, None, '', False)`라 장면꾸미기가 원본으로 폴백했다.

## 근본 원인과 수정

- 완성본 1편 청소의 정본은 `final_clean_{편성서명}.mp4`이고 `clean_video_path`는 구형 호환 칸이다.
- 꾸미기 공용 함수가 구형 칸을 먼저 검사해, 정본이 있어도 구형 칸이 비면 정본 탐색 전에 반환했다.
- `_clean_frame_src()`가 현재 편성 정본 → 최근 청소본 → 구형 호환 경로 순으로 선택하게 고쳤다.
- `clean_final_matches_plan()`도 구형 경로를 선행조건으로 삼지 않고 서명 정본 자체를 판정한다.

## 고객 우선 복구와 검증

- 라이브 DB에서 박진우 job의 `clean_video_path`를 현재 편성 정본으로 연결했다.
- 실제 꾸미기 프레임 `0_c0_s5@15.73_clean.jpg`(56,971B)를 생성해 청소본 출처와 자막 제거 화면을 눈으로 확인했다.
- 핵심 테스트 32개 통과.
- 자막제거·청소본·beatframe 관련 묶음 251개 통과, 2개 건너뜀.

