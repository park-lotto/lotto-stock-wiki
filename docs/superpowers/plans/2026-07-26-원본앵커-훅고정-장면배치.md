# 원본 앵커 차용 + 훅 고정 + 결 맞춤 장면배치 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 원본 크리에이터가 맞춰둔 대사↔장면 페어를 `is_key` 앵커로 차용해 우리 비법 구간에 고정하고, 첫·마지막 세그먼트(CTA·썸네일 박제)를 삭제하며, 목표 30초·훅 고정·레시피 결 맞춤 배치를 구현한다.

**Architecture:** 새 시스템이 아니라 기존 뼈대에 얹는다. `script_extract`(Gemini 영상분석 1회에 필드 2개 추가) → `edit_plan._build_inventory`(첫·끝 세그먼트 제외) → `edit_plan._chronological_respine`(레시피 결 맞춤·훅 고정 강화). 소재 분기는 이미 있는 `category` 태그(레시피/그외)를 재활용.

**Tech Stack:** Python, pytest, Gemini(google-genai) JSON schema, 기존 shopping_shorts 모듈.

**설계**: `docs/superpowers/specs/2026-07-26-원본앵커-훅고정-장면배치-design.md`

**작업 폴더**: `.tracks/장면스파인` (트랙 격리 — main 폴더 코드 절대 안 건드림). 커밋은 `git -C .tracks/장면스파인 ...` 또는 트랙 폴더 안에서.

**게이트 주의**: `video_assemble.py`·`edit_plan.py`·`produce.html`은 다트랙 hot. baseline 실패는 기존 것 — "병합 전보다 실패가 늘지 않았나"로 본다.

**태스크 순서**: 5→6→8은 `_chronological_respine`을 순차 확장한다(훅 앵커 → 레시피 결 → 앵커 dedup). 반드시 이 순서로. Task 7은 Task 6의 `is_recipe` 시그니처에 의존.

---

## 파일 구조

- **Modify** `shopping_shorts/script_extract.py`
  - `_RESPONSE_SCHEMA`: `is_key`(bool)·`shot_role`(str) 속성 추가 (required 아님)
  - `_PROMPT`: 두 필드 판정 지시 추가
  - `_assign_seg_ids`: 두 필드 출력에 포함(fail-open 기본값)
- **Modify** `shopping_shorts/edit_plan.py`
  - `_build_inventory`: 소스별 첫·마지막 세그먼트 제외(3개↑일 때만) + `is_key`/`shot_role` 보존
  - `_dedup_anchors`(신설 순수함수): 장점(scene_desc 요지)별 중복 제거
  - `_chronological_respine`: 훅(머리) 앵커 고정 + `is_recipe` 시 완성/조리 결 맞춤
  - `_validate_and_ground`: respine 호출에 `is_recipe` 전달
- **Modify** `shopping_shorts/script_generate.py`
  - `generate_mix` 기본값 20→30, `_GEN_PROMPT` "20초" 파라미터화
- **Modify** `shopping_shorts/mix_pipeline.py`
  - 소스 다수결 `is_recipe` 계산 → edit_plan 경로에 전달
- **Test**: 각 태스크별 신규 테스트 파일

---

## Task 1: 목표 길이 20초 → 30초 (기본값 교체)

**Files:**
- Modify: `shopping_shorts/script_generate.py:222` (`generate_mix` 기본값), `:164-165` (`_GEN_PROMPT` 하드코딩)
- Test: `shopping_shorts/tests/test_target_seconds_30.py`

- [ ] **Step 1: Write the failing test**

```python
"""목표 길이 기본값이 30초(스토리 여유)로 상향됐는지."""
import inspect
from shopping_shorts import script_generate


def test_generate_mix_default_is_30():
    sig = inspect.signature(script_generate.generate_mix)
    assert sig.parameters["target_seconds"].default == 30


def test_gen_prompt_not_hardcoded_20sec():
    # _GEN_PROMPT는 seconds/words 파라미터화돼야 한다(20초 하드코딩 제거).
    assert "20초 분량" not in script_generate._GEN_PROMPT
    assert "{seconds}" in script_generate._GEN_PROMPT
    assert "{words}" in script_generate._GEN_PROMPT
```

- [ ] **Step 2: Run test to verify it fails**

Run: `cd .tracks/장면스파인 && python -m pytest shopping_shorts/tests/test_target_seconds_30.py -v`
Expected: FAIL (default==20, "20초 분량" 존재)

- [ ] **Step 3: Implement**

`script_generate.py` `generate_mix` 시그니처와 fallback:

```python
def generate_mix(sources, target_seconds=30, n=3, max_key_tries=3, bank_context=""):
```
```python
    seconds = max(5, min(int(target_seconds or 30), 90))
```

`_GEN_PROMPT`의 하드코딩 줄(현재 `검증된 구조를 빌려, 20초 분량(약 45~70단어)의 새 대본 초안 {n}개를 만들어라.`)을 파라미터화:

```python
검증된 구조를 빌려, 약 {seconds}초 분량(대략 {words}단어)의 새 대본 초안 {n}개를 만들어라.
```

그리고 `_GEN_PROMPT`를 쓰는 `generate()` 호출부(`_GEN_PROMPT.format(...)`)에 `seconds=seconds, words=words` 인자를 추가한다. `generate()` 내부에 seconds/words가 없으면 `_verify_and_fix`와 동일하게 계산:
```python
    seconds = max(5, min(int(target_seconds or 30), 90))
    words = max(15, round(seconds * 2.3))
```

- [ ] **Step 4: Run test to verify it passes**

Run: `python -m pytest shopping_shorts/tests/test_target_seconds_30.py -v`
Expected: PASS

- [ ] **Step 5: 기존 대본 테스트 회귀 확인**

Run: `python -m pytest shopping_shorts/tests/test_script_generate.py -v`
Expected: PASS (또는 baseline 실패만 — 20초 문자열을 exact-match하던 테스트가 있으면 30초 유효 불변식으로 갱신)

- [ ] **Step 6: Commit**

```bash
git add shopping_shorts/script_generate.py shopping_shorts/tests/test_target_seconds_30.py
git commit -m "feat(대본): 목표 길이 기본 20초→30초 + _GEN_PROMPT 파라미터화"
```

---

## Task 2: script_extract에 is_key·shot_role 필드 추가

**Files:**
- Modify: `shopping_shorts/script_extract.py:31-53` (`_RESPONSE_SCHEMA`), `:82-89` (`_PROMPT` has_effect 뒤), `:99-107` (`_assign_seg_ids`)
- Test: `shopping_shorts/tests/test_iskey_field.py`

- [ ] **Step 1: Write the failing test**

```python
"""script_extract가 is_key·shot_role를 스키마·출력에 담되 required는 아님(기존 추출본 호환)."""
from shopping_shorts import script_extract


def test_schema_has_iskey_shotrole_optional():
    props = script_extract._RESPONSE_SCHEMA["properties"]["segments"]["items"]["properties"]
    assert props["is_key"]["type"] == "boolean"
    assert props["shot_role"]["type"] == "string"
    req = script_extract._RESPONSE_SCHEMA["properties"]["segments"]["items"]["required"]
    assert "is_key" not in req and "shot_role" not in req


def test_assign_seg_ids_defaults_faillopen():
    raw = [{"start": 0, "end": 2, "text": "a", "scene_desc": "s"}]  # is_key 없음
    out = script_extract._assign_seg_ids("vid", raw)
    assert out[0]["is_key"] is False        # fail-open 기본
    assert out[0]["shot_role"] == "기타"    # fail-open 기본


def test_assign_seg_ids_preserves_iskey():
    raw = [{"start": 0, "end": 2, "text": "a", "scene_desc": "s",
            "is_key": True, "shot_role": "조리"}]
    out = script_extract._assign_seg_ids("vid", raw)
    assert out[0]["is_key"] is True
    assert out[0]["shot_role"] == "조리"


def test_prompt_mentions_iskey():
    assert "is_key" in script_extract._PROMPT
    assert "shot_role" in script_extract._PROMPT
```

- [ ] **Step 2: Run test to verify it fails**

Run: `python -m pytest shopping_shorts/tests/test_iskey_field.py -v`
Expected: FAIL (KeyError is_key / "is_key" not in _PROMPT)

- [ ] **Step 3: Implement — 스키마에 필드 추가**

`_RESPONSE_SCHEMA` items properties에 추가(`has_effect` 뒤):

```python
                    "has_effect": {"type": "boolean"},
                    "is_key": {"type": "boolean"},
                    "shot_role": {"type": "string", "enum": ["조리", "완성", "기타"]},
```

- [ ] **Step 4: Implement — _PROMPT에 판정 지시 추가**

`_PROMPT`의 `has_effect: ...` 항목 뒤(라인 86 다음)에 추가:

```python
- is_key: 이 구간이 **제품/도구의 기능·성능·장점·효과를 화면으로 실증**하거나(넓다·크다·쏙
  들어간다·때가 빠진다를 실제 행동/결과로 보여줌), 요리·살림의 **핵심 방법을 손동작으로 보여주는**
  구간이면 true. 단순 도입 상황·인물 등장·감상·완성 인사·CTA·링크유도면 false. (원본 제작자가
  "이 대사에 이 장면"으로 맞춰둔 실증 페어를 골라내려는 것 — 대사가 기능을 설명하며 화면이 그걸
  보여주면 true. 애매하면 false.)
- shot_role: 화면의 성격. 손이 재료/도구를 다루는 과정이면 "조리", 완성된 결과물이 화면 주인공이면
  "완성", 그 외(인물·배경·인사 등)면 "기타". (레시피 소재의 화면 결 맞춤 배치에 쓴다.)
```

- [ ] **Step 5: Implement — _assign_seg_ids 출력에 포함**

`_assign_seg_ids`의 `out.append({...})` dict에 `has_effect` 뒤 추가:

```python
            "has_effect": bool(seg.get("has_effect")),  # 원본 효과 박힘 → B롤 제외용
            "is_key": bool(seg.get("is_key")),           # 기능·장점 실증 앵커 (fail-open False)
            "shot_role": seg.get("shot_role") or "기타", # 조리/완성/기타 (fail-open 기타)
```

- [ ] **Step 6: Run test to verify it passes**

Run: `python -m pytest shopping_shorts/tests/test_iskey_field.py -v`
Expected: PASS

- [ ] **Step 7: 기존 추출 테스트 회귀**

Run: `python -m pytest shopping_shorts/tests/test_script_extract.py -v`
Expected: PASS (또는 baseline만)

- [ ] **Step 8: Commit**

```bash
git add shopping_shorts/script_extract.py shopping_shorts/tests/test_iskey_field.py
git commit -m "feat(추출): script_extract에 is_key·shot_role 필드 추가(fail-open, required 아님)"
```

---

## Task 3: _build_inventory에서 첫·마지막 세그먼트 제외

**Files:**
- Modify: `shopping_shorts/edit_plan.py:50-73` (`_build_inventory`)
- Test: `shopping_shorts/tests/test_trim_first_last_seg.py`

- [ ] **Step 1: Write the failing test**

```python
"""소스별 첫·마지막 세그먼트를 인벤토리에서 제외(3개↑일 때만). 전사(full_text)는 무관."""
from shopping_shorts import edit_plan


def _seg(sid, txt, key=False):
    return {"seg_id": sid, "start": 0.0, "end": 2.0, "text": txt,
            "scene_desc": txt, "action": None, "is_key": key, "shot_role": "기타"}


def test_first_and_last_excluded_when_3plus():
    script = {"video_id": "v", "segments": [
        _seg("v-0", "썸네일"), _seg("v-1", "본문A"), _seg("v-2", "본문B"), _seg("v-3", "CTA")]}
    seg_map, _ = edit_plan._build_inventory([script])
    assert "v-0" not in seg_map      # 첫 제외
    assert "v-3" not in seg_map      # 마지막 제외
    assert "v-1" in seg_map and "v-2" in seg_map


def test_kept_when_2_or_fewer():
    script = {"video_id": "v", "segments": [_seg("v-0", "a"), _seg("v-1", "b")]}
    seg_map, _ = edit_plan._build_inventory([script])
    assert "v-0" in seg_map and "v-1" in seg_map   # 2개면 삭제 안 함


def test_iskey_preserved_in_seg_map():
    script = {"video_id": "v", "segments": [
        _seg("v-0", "썸네일"), _seg("v-1", "본문", key=True), _seg("v-2", "CTA")]}
    seg_map, _ = edit_plan._build_inventory([script])
    assert seg_map["v-1"]["is_key"] is True
```

- [ ] **Step 2: Run test to verify it fails**

Run: `python -m pytest shopping_shorts/tests/test_trim_first_last_seg.py -v`
Expected: FAIL (v-0/v-3 여전히 존재, is_key 키 없음)

- [ ] **Step 3: Implement**

`_build_inventory`의 순회 부분을 교체. 현재:
```python
    for script in source_scripts:
        vid = script.get("video_id", "")
        for seg in script.get("segments", []):
```
을:
```python
    for script in source_scripts:
        vid = script.get("video_id", "")
        segs = script.get("segments", [])
        # 첫·마지막 세그먼트 제외(CTA·썸네일 박제 차단) — 3개 이상일 때만(2개↓면 삭제 안 함).
        usable = segs[1:-1] if len(segs) >= 3 else segs
        for seg in usable:
```
그리고 `seg_map[sid] = {...}` dict에 `is_key`·`shot_role` 보존 추가:
```python
                "action": seg.get("action"),
                "is_key": bool(seg.get("is_key")),
                "shot_role": seg.get("shot_role") or "기타",
```

- [ ] **Step 4: Run test to verify it passes**

Run: `python -m pytest shopping_shorts/tests/test_trim_first_last_seg.py -v`
Expected: PASS

- [ ] **Step 5: 기존 edit_plan 테스트 회귀**

Run: `python -m pytest shopping_shorts/tests/test_chronological_respine.py shopping_shorts/tests/test_visual_verb_anchor.py -v`
Expected: PASS (또는 baseline만)

- [ ] **Step 6: Commit**

```bash
git add shopping_shorts/edit_plan.py shopping_shorts/tests/test_trim_first_last_seg.py
git commit -m "feat(장면): 소스별 첫·마지막 세그먼트 인벤토리 제외(CTA·썸네일 차단, 3개↑만)"
```

---

## Task 4: _dedup_anchors — 장점별 중복 앵커 제거 (순수함수 신설)

**Files:**
- Modify: `shopping_shorts/edit_plan.py` (`_is_face_seg` 근처에 신설)
- Test: `shopping_shorts/tests/test_dedup_anchors.py`

- [ ] **Step 1: Write the failing test**

```python
"""여러 소스가 같은 장점을 실증하면 하나만 남긴다(scene_desc 요지 기준). 강한 순 상위 N개."""
from shopping_shorts import edit_plan


def _seg(sid, desc, vid="v"):
    return {"seg_id": sid, "video_id": vid, "start": 0.0, "end": 2.0,
            "scene_desc": desc, "is_key": True, "shot_role": "완성"}


def test_dedup_same_claim_kept_once():
    anchors = [
        _seg("a-1", "내부가 넓어 그릇 가득", "a"),
        _seg("b-1", "안쪽이 넓어 접시 가득", "b"),   # 같은 장점(넓다)
        _seg("a-2", "과일 야채도 세척", "a"),
    ]
    out = edit_plan._dedup_anchors(anchors, top_n=4)
    descs = [s["scene_desc"] for s in out]
    # "넓다" 계열은 하나만
    wide = [d for d in descs if "넓" in d]
    assert len(wide) == 1
    assert any("세척" in d for d in descs)


def test_dedup_caps_top_n():
    anchors = [_seg(f"v-{i}", f"장점{i}") for i in range(8)]
    out = edit_plan._dedup_anchors(anchors, top_n=4)
    assert len(out) <= 4


def test_dedup_empty():
    assert edit_plan._dedup_anchors([], top_n=4) == []
```

- [ ] **Step 2: Run test to verify it fails**

Run: `python -m pytest shopping_shorts/tests/test_dedup_anchors.py -v`
Expected: FAIL (AttributeError: _dedup_anchors 없음)

- [ ] **Step 3: Implement**

`edit_plan.py`의 `_is_face_seg` 함수 뒤에 신설. 장점 요지 = scene_desc에서 핵심 명사/형용사 토큰 집합의 교집합으로 근사(결정적, Gemini 없음):

```python
def _claim_key(scene_desc):
    """장점 요지 근사 키 — scene_desc의 2글자 이상 토큰 정렬 집합.
    같은 장점을 다른 말로 쓴 컷(넓어 그릇 가득 / 넓어 접시 가득)을 근접시키기 위한 근사."""
    import re
    toks = [t for t in re.split(r"[\s,·]+", (scene_desc or "")) if len(t) >= 2]
    return tuple(sorted(set(toks)))


def _dedup_anchors(anchors, top_n=4):
    """is_key 앵커를 장점(scene_desc 요지)별로 묶어 중복 제거, 강한 순 상위 top_n개.
    같은 장점은 첫 등장(선명 가정)만 남긴다. 순수함수."""
    seen_tokens = []   # 이미 채택한 장점들의 토큰 집합
    out = []
    for a in anchors:
        key = set(_claim_key(a.get("scene_desc", "")))
        # 기존 채택 장점과 토큰이 과반 겹치면 같은 장점으로 보고 스킵.
        dup = any(key and len(key & prev) / max(1, len(key)) >= 0.5 for prev in seen_tokens)
        if dup:
            continue
        seen_tokens.append(key)
        out.append(a)
        if len(out) >= top_n:
            break
    return out
```

- [ ] **Step 4: Run test to verify it passes**

Run: `python -m pytest shopping_shorts/tests/test_dedup_anchors.py -v`
Expected: PASS

- [ ] **Step 5: Commit**

```bash
git add shopping_shorts/edit_plan.py shopping_shorts/tests/test_dedup_anchors.py
git commit -m "feat(장면): _dedup_anchors — 장점별 중복 앵커 제거 순수함수(상위 N)"
```

---

## Task 5: _chronological_respine — 훅(머리) 앵커 고정

**Files:**
- Modify: `shopping_shorts/edit_plan.py:144-179` (`_chronological_respine`)
- Test: `shopping_shorts/tests/test_hook_anchor.py`

현재는 꼬리(마지막) 비트만 앵커로 고정한다. 훅(첫 비트)도 앵커로 고정해 "리스트 최고 장면 = 훅"이 respine에 흔들리지 않게 한다.

- [ ] **Step 1: Write the failing test**

```python
"""첫 비트(훅)는 respine 대상에서 제외 — 모델이 훅에 고정한 화면이 시간순 재배치에 안 밀린다."""
from shopping_shorts import edit_plan


def _beat(sid, vid, start, txt="t"):
    seg = {"seg_id": sid, "video_id": vid, "start": start, "end": start + 2,
           "scene_desc": txt, "text": txt}
    return {"primary": seg, "alternates": [], "text": txt, "fit": 4}


def test_first_beat_not_respined():
    beats = [_beat("a-9", "a", 9.0, "훅"), _beat("b-1", "b", 1.0),
             _beat("a-1", "a", 1.0), _beat("b-9", "b", 9.0)]
    out = edit_plan._chronological_respine(beats)
    # 첫 비트 primary는 원본 그대로(seg_id a-9 유지), respined 플래그 없음
    assert out[0]["primary"]["seg_id"] == "a-9"
    assert not out[0].get("respined")
```

- [ ] **Step 2: Run test to verify it fails**

Run: `python -m pytest shopping_shorts/tests/test_hook_anchor.py -v`
Expected: FAIL (첫 비트가 재배치돼 seg_id 바뀜)

- [ ] **Step 3: Implement**

`_chronological_respine`의 body/tail 분리(라인 147)를 머리·꼬리 둘 다 앵커로:

```python
    if len(beats) < 3:
        # 머리·꼬리를 둘 다 앵커로 빼면 body가 비므로, 짧은 경우는 꼬리만 고정(기존 동작).
        body, tail = beats[:-1], beats[-1]
        head = None
    else:
        head, body, tail = beats[0], beats[1:-1], beats[-1]
```

`movable_idx`는 body 기준이라 그대로. 마지막 조립부(라인 176~178)를 head 포함해 재구성:

```python
    out = [moved[j] if j in moved else dict(b) for j, b in enumerate(body)]
    if head is not None:
        out.insert(0, dict(head))   # 훅 앵커 — 모델이 고른 화면 그대로(respined 아님)
    out.append(dict(tail))
    return out
```

- [ ] **Step 4: Run test to verify it passes**

Run: `python -m pytest shopping_shorts/tests/test_hook_anchor.py -v`
Expected: PASS

- [ ] **Step 5: 기존 respine 회귀 (중요)**

Run: `python -m pytest shopping_shorts/tests/test_chronological_respine.py -v`
Expected: PASS — 기존 테스트가 "첫 비트도 재배치됨"을 가정하면 그 테스트를 새 불변식(첫 비트 앵커)으로 갱신하되, 꼬리 앵커·visual_verb 앵커·비트당 세그먼트 개수 보존은 그대로 지킨다.

- [ ] **Step 6: Commit**

```bash
git add shopping_shorts/edit_plan.py shopping_shorts/tests/test_hook_anchor.py
git commit -m "feat(장면): 훅(첫 비트)도 앵커 고정 — 리스트 최고장면이 respine에 안 밀림"
```

---

## Task 6: _chronological_respine — 레시피 결 맞춤 (완성이 조리 뒤로 안 감)

**Files:**
- Modify: `shopping_shorts/edit_plan.py` (`_chronological_respine` 시그니처에 `is_recipe`, 정렬 키), `_validate_and_ground` 전달
- Test: `shopping_shorts/tests/test_recipe_grain.py`

- [ ] **Step 1: Write the failing test**

```python
"""레시피면 movable 재배치 시 shot_role=완성 세그먼트를 조리 뒤로 정렬(완성이 조리 앞에 안 낌).
제품(is_recipe=False)이면 기존 (video_id,start) 시간순 유지."""
from shopping_shorts import edit_plan


def _beat(sid, vid, start, role):
    seg = {"seg_id": sid, "video_id": vid, "start": start, "end": start + 2,
           "scene_desc": role, "text": "t", "shot_role": role}
    return {"primary": seg, "alternates": [], "text": "t", "fit": 4}


def test_recipe_finish_after_cook():
    # 머리·꼬리 앵커 제외를 피하려 비트 5개, 가운데 3개가 movable.
    beats = [_beat("h", "a", 0, "기타"),
             _beat("a-5", "a", 5, "완성"), _beat("a-1", "a", 1, "조리"),
             _beat("a-3", "a", 3, "조리"),
             _beat("t", "a", 9, "기타")]
    out = edit_plan._chronological_respine(beats, is_recipe=True)
    mid_roles = [b["primary"]["shot_role"] for b in out[1:-1]]
    # 조리들이 완성보다 앞
    assert mid_roles.index("완성") == max(range(len(mid_roles)))  # 완성이 가장 뒤


def test_product_keeps_time_order():
    beats = [_beat("h", "a", 0, "기타"),
             _beat("a-5", "a", 5, "완성"), _beat("a-1", "a", 1, "완성"),
             _beat("a-3", "a", 3, "완성"),
             _beat("t", "a", 9, "기타")]
    out = edit_plan._chronological_respine(beats, is_recipe=False)
    mids = [b["primary"]["start"] for b in out[1:-1]]
    assert mids == sorted(mids)   # 제품은 (start) 시간순 그대로
```

- [ ] **Step 2: Run test to verify it fails**

Run: `python -m pytest shopping_shorts/tests/test_recipe_grain.py -v`
Expected: FAIL (is_recipe 인자 없음 → TypeError)

- [ ] **Step 3: Implement**

`_chronological_respine` 시그니처에 기본값 추가:
```python
def _chronological_respine(beats, is_recipe=False):
```

정렬 키(라인 159~160)를 레시피면 shot_role 우선(완성을 뒤로) 추가:
```python
    def _sort_key(s):
        finish_last = 1 if (is_recipe and s.get("shot_role") == "완성") else 0
        return (finish_last, s.get("video_id", ""), s.get("start", 0.0),
                _is_face_seg(s.get("scene_desc", "")))
    ordered = sorted(flat, key=_sort_key)
```
(제품이면 finish_last가 항상 0 → 기존 (video_id,start) 정렬과 동일.)

- [ ] **Step 4: Run test to verify it passes**

Run: `python -m pytest shopping_shorts/tests/test_recipe_grain.py -v`
Expected: PASS

- [ ] **Step 5: _validate_and_ground에 is_recipe 전달**

`_validate_and_ground` 시그니처에 `is_recipe=False` 추가하고, 내부 `_chronological_respine(...)` 호출에 전달:
```python
def _validate_and_ground(raw_plan, seg_map, n_alternates, respine=True, is_recipe=False):
```
respine 호출부:
```python
        beats = _chronological_respine(beats, is_recipe=is_recipe)
```

- [ ] **Step 6: 회귀**

Run: `python -m pytest shopping_shorts/tests/test_chronological_respine.py shopping_shorts/tests/test_recipe_grain.py -v`
Expected: PASS (또는 baseline만)

- [ ] **Step 7: Commit**

```bash
git add shopping_shorts/edit_plan.py shopping_shorts/tests/test_recipe_grain.py
git commit -m "feat(장면): 레시피 결 맞춤 — 완성 세그먼트를 조리 뒤로(제품은 무변경)"
```

---

## Task 7: mix_pipeline에서 is_recipe 계산·전달

**Files:**
- Modify: `shopping_shorts/mix_pipeline.py` (`_validate_and_ground`/`build_*_plan` 호출 경로에 is_recipe 배선)
- Test: `shopping_shorts/tests/test_mix_is_recipe.py`

- [ ] **Step 1: 배선 지점 확인 (읽기)**

Run: `cd .tracks/장면스파인 && grep -n "_validate_and_ground\|category\|build_scene_first_plan\|build_edit_plan" shopping_shorts/mix_pipeline.py shopping_shorts/edit_plan.py`
소스 dict에 `category`가 실려 오는지, edit_plan 진입 함수가 무엇인지 확인해 정확한 전달 경로를 잡는다.

- [ ] **Step 2: Write the failing test**

```python
"""소스 다수결로 is_recipe 판정 — category=='레시피'가 과반이면 True."""
from shopping_shorts import mix_pipeline


def test_is_recipe_majority():
    srcs = [{"category": "레시피"}, {"category": "레시피"}, {"category": "홈템"}]
    assert mix_pipeline._sources_is_recipe(srcs) is True


def test_is_recipe_minority_false():
    srcs = [{"category": "레시피"}, {"category": "가전"}, {"category": "홈템"}]
    assert mix_pipeline._sources_is_recipe(srcs) is False


def test_is_recipe_empty_false():
    assert mix_pipeline._sources_is_recipe([]) is False
```

- [ ] **Step 3: Run test to verify it fails**

Run: `python -m pytest shopping_shorts/tests/test_mix_is_recipe.py -v`
Expected: FAIL (_sources_is_recipe 없음)

- [ ] **Step 4: Implement — 판정 헬퍼 + 전달**

`mix_pipeline.py`에 순수함수 추가:
```python
def _sources_is_recipe(sources):
    """소스 category 다수결이 '레시피'면 True(장면 결 맞춤 분기). 비면 False."""
    cats = [s.get("category") for s in (sources or []) if s.get("category")]
    if not cats:
        return False
    return cats.count("레시피") * 2 > len(cats)
```

그리고 Step 1에서 확인한 edit_plan 진입 호출부에 `is_recipe=_sources_is_recipe(sources)`를 넘긴다. (진입 함수가 `is_recipe`를 받도록 Task 6에서 이미 `_validate_and_ground` 시그니처를 확장했다. `build_scene_first_plan`·`build_edit_plan`이 중간에 있으면 그 함수들도 `is_recipe`를 받아 `_validate_and_ground`로 pass-through하도록 한 줄씩 추가.)

- [ ] **Step 5: Run test to verify it passes**

Run: `python -m pytest shopping_shorts/tests/test_mix_is_recipe.py -v`
Expected: PASS

- [ ] **Step 6: 믹스 파이프라인 회귀**

Run: `python -m pytest shopping_shorts/tests/test_mix_pipeline.py shopping_shorts/tests/test_app_mix.py -v`
Expected: PASS (또는 baseline만)

- [ ] **Step 7: Commit**

```bash
git add shopping_shorts/mix_pipeline.py shopping_shorts/edit_plan.py shopping_shorts/tests/test_mix_is_recipe.py
git commit -m "feat(믹스): 소스 다수결 is_recipe 판정 → 레시피 결 맞춤 배선"
```

---

## Task 8: 앵커 dedup를 respine 경로에 배선

**Files:**
- Modify: `shopping_shorts/edit_plan.py` (`_chronological_respine` 또는 `_validate_and_ground`에서 is_key 앵커에 `_dedup_anchors` 적용)
- Test: `shopping_shorts/tests/test_anchor_dedup_wiring.py`

Task 4의 `_dedup_anchors`를 실제 파이프라인에 연결한다. is_key 앵커가 상위 N개(30초=4개)로 제한되고, 같은 장점 반복이 제거되는지 통합 검증.

- [ ] **Step 1: Write the failing test**

```python
"""respine이 is_key 앵커를 _dedup_anchors로 상위 4개·장점중복 제거 후 배치하는지 통합 확인."""
from shopping_shorts import edit_plan


def _beat(sid, vid, start, desc, key):
    seg = {"seg_id": sid, "video_id": vid, "start": start, "end": start + 2,
           "scene_desc": desc, "text": "t", "is_key": key, "shot_role": "완성"}
    return {"primary": seg, "alternates": [], "text": "t", "fit": 4, "visual_verb": key}


def test_duplicate_key_anchors_collapsed():
    # 같은 장점(넓다)을 3소스가 실증 → 앵커로 하나만 남아야
    beats = [_beat("h", "a", 0, "훅", False),
             _beat("a-1", "a", 1, "내부 넓어 가득", True),
             _beat("b-1", "b", 1, "안쪽 넓어 가득", True),
             _beat("c-1", "c", 1, "넓어서 그릇 가득", True),
             _beat("a-2", "a", 2, "본문", False),
             _beat("t", "a", 9, "끝", False)]
    out = edit_plan._chronological_respine(beats, is_recipe=False)
    wide_anchors = [b for b in out if b["primary"].get("is_key")
                    and "넓" in b["primary"]["scene_desc"]]
    assert len(wide_anchors) == 1   # 넓다 장점은 하나만
```

- [ ] **Step 2: Run test to verify it fails**

Run: `python -m pytest shopping_shorts/tests/test_anchor_dedup_wiring.py -v`
Expected: FAIL (중복 앵커 3개 다 남음)

- [ ] **Step 3: Implement**

`_chronological_respine`에서 앵커(visual_verb 또는 is_key) 판정을 통합하고, is_key 앵커에 dedup 적용. movable 판정(라인 149)을 확장:

```python
    # 앵커 = visual_verb 또는 is_key(기능·장점 실증 페어). 나머지만 movable.
    def _is_anchor(b):
        return bool(b.get("visual_verb")) or bool(b.get("primary", {}).get("is_key"))
    anchor_idx = [j for j, b in enumerate(body) if _is_anchor(b)]
    movable_idx = [j for j, b in enumerate(body) if not _is_anchor(b)]
```

그리고 앵커 비트들을 dedup: `_dedup_anchors`는 세그먼트 리스트를 받으므로, 앵커 비트의 primary를 넘겨 살아남은 seg_id 집합을 구하고, 살아남지 못한 앵커 비트는 movable로 강등(또는 드롭). 30초 앵커 상한 4:

```python
    anchor_primaries = [dict(body[j]["primary"], _beat_idx=j) for j in anchor_idx]
    kept = _dedup_anchors(anchor_primaries, top_n=4)
    kept_idx = {s["_beat_idx"] for s in kept}
    # 살아남은 앵커만 고정, 나머지는 movable 풀로 강등(장점 중복·상한 초과분).
    movable_idx = sorted(movable_idx + [j for j in anchor_idx if j not in kept_idx])
    anchor_idx = [j for j in anchor_idx if j in kept_idx]
```

(주의: `_beat_idx`는 dedup 내부 통과용 임시 키 — 최종 primary엔 남기지 않도록 `_dedup_anchors` 결과 사용 후 무시하거나, primary 스냅샷에만 붙였으니 원본 비트 primary는 오염 안 됨. 실제 배치 시엔 `body[j]["primary"]` 원본을 쓴다.)

기존 out 조립 시 movable로 강등된 앵커도 시간순 재배치에 포함되게 자연히 흐른다.

- [ ] **Step 4: Run test to verify it passes**

Run: `python -m pytest shopping_shorts/tests/test_anchor_dedup_wiring.py -v`
Expected: PASS

- [ ] **Step 5: 전체 respine 회귀 (핵심)**

Run: `python -m pytest shopping_shorts/tests/test_chronological_respine.py shopping_shorts/tests/test_visual_verb_anchor.py shopping_shorts/tests/test_hook_anchor.py shopping_shorts/tests/test_recipe_grain.py -v`
Expected: PASS (또는 baseline만) — visual_verb 앵커 계약이 안 깨졌는지 특히 확인.

- [ ] **Step 6: Commit**

```bash
git add shopping_shorts/edit_plan.py shopping_shorts/tests/test_anchor_dedup_wiring.py
git commit -m "feat(장면): is_key 앵커 dedup 배선 — 장점 중복 제거+상위4 고정(30초)"
```

---

## Task 9: 전체 게이트 + 통합 확인

**Files:** 없음(검증만)

- [ ] **Step 1: 전체 테스트 실행**

Run: `cd .tracks/장면스파인 && python -m pytest shopping_shorts/tests/ -x -q 2>&1 | tail -30`
Expected: 새 실패 0 (baseline 실패는 스펙 §8 기준으로 허용 — 병합 전보다 늘지 않았는지 확인).

- [ ] **Step 2: import 스모크**

Run: `python -c "import shopping_shorts.app; import shopping_shorts.edit_plan; import shopping_shorts.script_extract; import shopping_shorts.mix_pipeline; print('OK')"`
Expected: `OK`

- [ ] **Step 3: baseline 대조**

Run: `git stash && python -m pytest shopping_shorts/tests/ -q 2>&1 | tail -3 && git stash pop`
(내 변경 없이 baseline 실패 수를 기록 → 위 Step 1과 대조해 새 실패 0 확인)

- [ ] **Step 4: 커밋 정리 확인**

Run: `git -C . log --oneline -9`
Expected: Task 1~8 커밋이 순서대로.

---

## 검증 (구현 후 육안 — 스펙 §7)

코드 게이트 통과 후 **라이브 배포(finish) → shoppingshorts.duckdns.org에서 사장님 육안**:
1. 소스 2~5개 믹스 렌더 → 첫/끝 세그먼트(CTA·썸네일) 안 나오는지.
2. 훅에 리스트 최고 장면 뜨는지.
3. 레시피: 조리→완성→조리 핑퐁 없는지.
4. 제품: is_key 장점 실증 컷이 본문 한 덩어리, 대사만 우리 톤인지.
5. 스토리 인과·회수 살아있는지(가장 중요).

⚠️ 이건 코드로 증명 불가 — 실렌더 육안이 최종 게이트(fablize).
