# 대본↔화면 싱크 페이즈1 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 제작소 믹스 영상에서 대사와 화면이 최소한 중요 장면에서 맞고, 같은 장면 반복이 사라지고, 어긋난 비트가 UI에 정직하게 빨간불로 뜨게 한다.

**Architecture:** 기존 2트랙 모델(`edit_plan._chronological_respine`) 위에 4겹을 얹는다 — ④respine이 fit을 조작하지 않고 `respined` 플래그로 대체 → 기존 스왑버튼 부활, ⓪분석을 4~8초 행위단위로 세분화해 카드 수를 늘림, ②비트 간 중복 세그먼트 제거+서브슬라이스+얼굴 후순위, ①대사가 시각행위를 지목하는 비트는 respine에서 제외(앵커)해 모델 매칭 유지, ③fit 낮은 비트만 Gemini 1콜로 문장을 화면에 맞게 미세수정.

**Tech Stack:** Python 3.14, pytest, google-genai(Gemini, `key_vault` 캐스케이드), FastAPI + 정적 HTML(produce.html JS).

## Global Constraints

- **작업 폴더**: 모든 편집은 `.tracks/장면스파인/...` 절대경로. main 폴더 코드 파일 금지(흡수 방지). git은 `git -C .tracks/장면스파인`.
- **핑퐁 루프 금지**: ③ 리컨사일은 **정확히 1왕복(1 Gemini 콜)**. 반복 수렴 루프를 만들지 않는다(딸깍 원클릭·시니어 북극성).
- **실패 시 fail-open**: ③ 리컨사일·⓪·① 스키마 필드가 없거나 Gemini 실패 시 원래 동작으로 폴백(성능 저하 없음).
- **환각 방지**: 세그먼트는 seg_id로만 지목, start/end는 코드가 인벤토리에서 되붙인다(`_ground_ref`). 서브슬라이스 시간구간도 코드가 계산.
- **fit 의미**: 1~5, 5=딱맞음. 스왑버튼·빨간불 임계 = `fit<=2`(produce.html 기존 계약, 변경 금지).
- **respined 비트 = b-roll by design**: 시간순 스파인으로 깔린 비트는 의미매칭을 포기한 것이므로 UI에서 빨간불 대상이 아니다(중립/초록). 빨간불은 **앵커 비트(respine 제외)의 정직한 fit≤2**에만.
- **로컬 TTS 무음 주의**: 길이·오디오 검증은 서버에서(로컬은 무음 mock, reference_local_tts_silent_mock_trap). 순수함수 로직은 로컬 pytest로 검증.
- **소스 원칙(운영)**: 같은 주제 3~4개 다다익선, 이종 혼합 금물 — 코드 제약 아님, 문서화만.

---

### Task 1: ④ fit 정직화 — respine이 fit을 조작하지 않고 respined 플래그로 대체

respine이 모든 body·tail 비트의 `fit`을 4로 덮어써서(edit_plan.py:116,120) produce.html의 스왑버튼(fit≤2)이 **영구히 죽어 있다**. fit 덮어쓰기를 제거하고, 시간순 스파인으로 재배치된 body 비트에는 `respined: True` 플래그를 단다. tail(앵커)은 respine 대상이 아니므로 플래그 없이 모델 fit 유지. 프런트는 `respined` 비트를 중립(초록)으로, 비-respined 비트의 fit≤2만 빨간불+스왑으로 취급.

**Files:**
- Modify: `.tracks/장면스파인/shopping_shorts/edit_plan.py:85-122` (`_chronological_respine`)
- Modify: `.tracks/장면스파인/shopping_shorts/tests/test_chronological_respine.py:55-59` (`test_fit_marked_intentional_not_red` → respined 플래그 계약으로 교체)
- Modify: `.tracks/장면스파인/shopping_shorts/static/produce.html:2092,2094,2722-2724,2736` (fit 게이트가 respined 비트를 중립 처리)

**Interfaces:**
- Produces: `_chronological_respine(beats)` — body 비트에 `"respined": True` 추가, `fit`은 입력값 보존(덮어쓰기 삭제). tail 비트는 `respined` 없음, `fit` 보존.

- [ ] **Step 1: 기존 테스트를 새 계약으로 교체(먼저 실패하게)**

`tests/test_chronological_respine.py`의 `test_fit_marked_intentional_not_red`(55-59줄)를 아래로 교체:

```python
def test_respined_body_flagged_fit_preserved():
    # ④ fit 정직화: respine은 fit을 덮어쓰지 않고 respined 플래그만 단다.
    beats = [_beat("a", 3), _beat("b", 1), _beat("cta", 5)]  # fit=1로 생성됨(_beat)
    out = _chronological_respine(beats)
    # body(a,b)는 시간순 재배치됨 → respined 표시, fit은 모델값(1) 보존
    assert out[0]["respined"] is True and out[1]["respined"] is True
    assert out[0]["fit"] == 1 and out[1]["fit"] == 1
    # 꼬리(cta)는 앵커 — respined 아님, fit 보존
    assert out[-1].get("respined") is not True
    assert out[-1]["fit"] == 1
```

- [ ] **Step 2: 실패 확인**

Run: `cd .tracks/장면스파인 && python -m pytest shopping_shorts/tests/test_chronological_respine.py::test_respined_body_flagged_fit_preserved -v`
Expected: FAIL — 현재는 `out[0]["fit"]==4`이고 `respined` 키가 없어 KeyError/assert 실패.

- [ ] **Step 3: `_chronological_respine` 수정**

edit_plan.py의 body 루프(109-117줄)와 tail 처리(118-121줄)를 아래로 교체:

```python
    out, i = [], 0
    for b, n in zip(body, counts):
        chunk = ordered[i:i + n]
        i += n
        nb = dict(b)
        nb["primary"] = chunk[0]
        nb["alternates"] = chunk[1:]
        nb["respined"] = True   # 시간순 스파인 배치 = b-roll by design(빨간불 대상 아님)
        out.append(nb)          # ④ fit 덮어쓰기 삭제 — 모델 fit 그대로 보존
    # 꼬리: 앵커 — 세그먼트·fit 모두 모델이 고른 그대로(respined 아님).
    out.append(dict(tail))
    return out
```

docstring의 "fit=4(정상)로 표시한다" 문단(96-98줄)을 "respined 플래그로 구분한다 — fit은 조작하지 않는다"로 갱신.

- [ ] **Step 4: 통과 확인 + 기존 respine 테스트 회귀**

Run: `cd .tracks/장면스파인 && python -m pytest shopping_shorts/tests/test_chronological_respine.py -v`
Expected: 전부 PASS(신규 포함). `test_body_primaries_reordered_tail_anchored`·`test_tail_beat_anchor_preserved` 등 순서 계약은 불변.

- [ ] **Step 5: produce.html fit 게이트가 respined 비트를 중립 처리**

`static/produce.html`에서 4곳 수정:

2092줄 per-beat 빨간 경고 — respined 비트는 제외:
```javascript
      ${(b.fit&&b.fit<=2&&!b.respined)?`<div style="color:var(--fire);font-size:12px;margin-top:3px">⚠️ 이 문장에 맞는 영상이 부족합니다 — 대본을 살짝 바꾸거나 소스 영상을 추가하세요.</div>`:''}
```

2722-2724줄 🟢🟡🔴 집계 — respined는 초록:
```javascript
    if(b.respined) green++;            // 시간순 스파인 = 사람이 신경 안 쓰는 b-roll → 중립
    else if(b.fit && b.fit<=2) red++;
    else if(b.fit===3) yellow++;
    else green++;
```

2736줄 스왑 `weak` 판정 — respined 제외:
```javascript
  const weak = !beat.respined && beat.fit && beat.fit <= 2;
```

2096줄 하단 요약의 `some(b=>b.fit&&b.fit<=2)`도 `some(b=>!b.respined&&b.fit&&b.fit<=2)`로 동일하게.

- [ ] **Step 6: HTML JS 문법 검증(제작소가 안 열리는 사고 방지)**

Run: `cd .tracks/장면스파인 && python -m pytest shopping_shorts/tests/test_mix_swap_js.py shopping_shorts/tests/test_voice_preview_js.py -v`
Expected: PASS(JS 파싱/구조 테스트가 있으면 통과). 없으면 `node --check`로 인라인 스크립트 추출 검증은 생략하고 육안 확인.

- [ ] **Step 7: 커밋**

```bash
git -C .tracks/장면스파인 add shopping_shorts/edit_plan.py shopping_shorts/tests/test_chronological_respine.py shopping_shorts/static/produce.html
git -C .tracks/장면스파인 commit -m "fix(edit_plan): ④ respine이 fit 덮어쓰기 대신 respined 플래그 — 스왑버튼 부활"
```

---

### Task 2: ⓪ 분석 세분화 — script_extract 세그먼트를 4~8초 행위단위로

세그먼트가 15~25초 덩어리(140초 영상이 5조각)라 모델이 고를 카드가 없어 반복·억지가 필연. `script_extract._PROMPT`의 분할 지시(60줄)를 "4~8초 **행위 단위**"로 강화해 같은 1회 Gemini 호출로 카드 수를 3~4배 늘린다. 순수 로직 변화 없음 — 프롬프트 문자열만.

**Files:**
- Modify: `.tracks/장면스파인/shopping_shorts/script_extract.py:60` (`_PROMPT` 분할 지시)
- Modify: `.tracks/장면스파인/shopping_shorts/tests/test_script_extract.py` (프롬프트에 세분화 지시 포함 검증 추가)

**Interfaces:**
- Produces: `script_extract._PROMPT` 문자열에 "4~8초" + "행위" 키워드 포함.

- [ ] **Step 1: 프롬프트 계약 테스트 추가(먼저 실패)**

`tests/test_script_extract.py` 끝에 추가:
```python
def test_prompt_asks_fine_grained_action_segments():
    from shopping_shorts import script_extract
    p = script_extract._PROMPT
    assert "4~8초" in p and "행위" in p
```

- [ ] **Step 2: 실패 확인**

Run: `cd .tracks/장면스파인 && python -m pytest shopping_shorts/tests/test_script_extract.py::test_prompt_asks_fine_grained_action_segments -v`
Expected: FAIL — 현재 프롬프트에 해당 문구 없음.

- [ ] **Step 3: `_PROMPT` 분할 지시 강화**

script_extract.py 60줄 "각 세그먼트는 화면이 크게 바뀌거나 말의 주제가 바뀌는 단위로 끊어라."를 아래로 교체:
```
각 세그먼트는 **하나의 행위/동작 단위**로 잘게 끊어라 — 목표는 세그먼트당 4~8초.
"반죽을 치댄다 / 동그랗게 굴린다 / 밥솥에 넣는다 / 뚜껑을 연다 / 완성품을 찢는다"처럼
화면 속 동작이 바뀌면 새 세그먼트로 나눠라. 한 동작이 길어도 8초를 크게 넘기지 마라.
말의 주제가 바뀌거나 화면이 크게 바뀌면 당연히 새 세그먼트다.
```

- [ ] **Step 4: 통과 확인**

Run: `cd .tracks/장면스파인 && python -m pytest shopping_shorts/tests/test_script_extract.py -v`
Expected: PASS(신규 포함, 기존 회귀 없음).

- [ ] **Step 5: 커밋**

```bash
git -C .tracks/장면스파인 add shopping_shorts/script_extract.py shopping_shorts/tests/test_script_extract.py
git -C .tracks/장면스파인 commit -m "feat(script_extract): ⓪ 세그먼트를 4~8초 행위단위로 세분화 — 카드 수 증대"
```

---

### Task 3: ② 비트 간 중복 제거 + 서브슬라이스 + 얼굴 후순위

모델이 같은 seg를 여러 비트에 줘도(실측: beat0·beat1 둘 다 s1-0) 걸러지지 않아 같은 장면이 2연속으로 뜬다(`_ground_ref` dedup은 한 비트 alternates 안에서만, edit_plan.py:139). respine의 flat 풀에서 **같은 (seg_id,start) 중복을 제거**하고, 그로 인해 카드가 모자라면 가장 긴 세그먼트를 **서브슬라이스**(시간구간 절반)해 새 카드를 만든다. 얼굴 정면 신호가 있는 scene_desc는 풀 정렬에서 후순위로 민다.

**Files:**
- Modify: `.tracks/장면스파인/shopping_shorts/edit_plan.py` (`_chronological_respine` 앞에 `_dedup_pool`·`_subslice`·`_face_rank` 순수함수 추가, respine에서 호출)
- Create: `.tracks/장면스파인/shopping_shorts/tests/test_respine_dedup.py`

**Interfaces:**
- Consumes: Task 1의 `_chronological_respine`(respined 플래그).
- Produces:
  - `_is_face_seg(scene_desc: str) -> bool` — scene_desc에 인물 정면 신호(얼굴/정면/셀카/脸/人物/正面/자기소개) 있으면 True.
  - `_dedup_and_fill(flat: list[dict], need: int) -> list[dict]` — 같은 (video_id,seg_id,start) 중복 제거 후, 길이가 `need` 미만이면 가장 긴 세그먼트를 서브슬라이스로 분할해 `need`개까지 채운다. 각 항목은 `{video_id,seg_id,start,end}` 유지(서브슬라이스는 seg_id에 `#2` 접미사, start/end 재계산).

- [ ] **Step 1: 얼굴 판별 실패 테스트**

`tests/test_respine_dedup.py` 생성:
```python
from shopping_shorts.edit_plan import _is_face_seg, _dedup_and_fill


def test_face_seg_detected():
    assert _is_face_seg("진행자가 정면으로 카메라를 보며 설명한다") is True
    assert _is_face_seg("主理人正面讲解") is True
    assert _is_face_seg("반죽을 실리콘매트에 올려 치댄다") is False
```

- [ ] **Step 2: 실패 확인**

Run: `cd .tracks/장면스파인 && python -m pytest shopping_shorts/tests/test_respine_dedup.py::test_face_seg_detected -v`
Expected: FAIL — `_is_face_seg` 미정의(ImportError).

- [ ] **Step 3: `_is_face_seg` 구현**

edit_plan.py의 `_chronological_respine` 정의 위(85줄 앞)에 추가:
```python
_FACE_TOKENS = ("얼굴", "정면", "셀카", "자기소개", "말하는 사람", "脸", "人物", "正面", "自拍")


def _is_face_seg(scene_desc):
    """scene_desc에 인물 정면/얼굴 신호가 있으면 True — 대체 카드가 있을 때 후순위로 민다."""
    s = (scene_desc or "").lower()
    return any(t.lower() in s for t in _FACE_TOKENS)
```

- [ ] **Step 4: 통과 확인**

Run: `cd .tracks/장면스파인 && python -m pytest shopping_shorts/tests/test_respine_dedup.py::test_face_seg_detected -v`
Expected: PASS.

- [ ] **Step 5: dedup+서브슬라이스 실패 테스트 추가**

`tests/test_respine_dedup.py`에 추가:
```python
def _seg(v, sid, start, end):
    return {"video_id": v, "seg_id": sid, "start": float(start), "end": float(end)}


def test_dedup_removes_identical_and_keeps_order():
    flat = [_seg("A", "A-0", 0, 5), _seg("A", "A-0", 0, 5), _seg("A", "A-2", 35, 60)]
    out = _dedup_and_fill(flat, need=3)
    # 같은 (A-0,0.0) 중복 1개 제거 → 서브슬라이스로 3개 채움, 동일 (seg_id,start) 재등장 없음
    keys = [(s["seg_id"], s["start"]) for s in out]
    assert len(keys) == 3 and len(set(keys)) == 3


def test_subslice_splits_longest_when_short():
    flat = [_seg("A", "A-0", 0, 5), _seg("A", "A-2", 10, 30)]  # 2개인데 3 필요
    out = _dedup_and_fill(flat, need=3)
    assert len(out) == 3
    # 가장 긴 A-2(20초)를 반으로: 앞[10~20], 뒤[20~30]
    a2 = sorted([s for s in out if s["seg_id"].startswith("A-2")], key=lambda s: s["start"])
    assert len(a2) == 2 and a2[0]["end"] == a2[1]["start"]
```

- [ ] **Step 6: 실패 확인**

Run: `cd .tracks/장면스파인 && python -m pytest shopping_shorts/tests/test_respine_dedup.py -k dedup_or_subslice -v` (또는 두 테스트명 지정)
Expected: FAIL — `_dedup_and_fill` 미정의.

- [ ] **Step 7: `_dedup_and_fill` 구현**

`_is_face_seg` 아래에 추가:
```python
def _dedup_and_fill(flat, need):
    """같은 (video_id,seg_id,start) 중복 제거 후, need 미만이면 가장 긴 세그먼트를
    시간 이등분 서브슬라이스로 분할해 need개까지 채운다. 환각 없음 — start/end는 코드 계산."""
    seen, uniq = set(), []
    for s in flat:
        k = (s.get("video_id", ""), s.get("seg_id", ""), s.get("start", 0.0))
        if k in seen:
            continue
        seen.add(k)
        uniq.append(s)
    # 부족분을 서브슬라이스로 채움 — 가장 긴 것부터 반으로 쪼갠다.
    while len(uniq) < need:
        longest = max(uniq, key=lambda s: s.get("end", 0.0) - s.get("start", 0.0), default=None)
        if longest is None or (longest["end"] - longest["start"]) < 1.0:
            break  # 더 쪼갤 게 없음 — 있는 만큼만
        mid = round((longest["start"] + longest["end"]) / 2, 2)
        half = dict(longest)
        half["seg_id"] = f"{longest['seg_id']}#2"
        half["start"] = mid
        longest["end"] = mid  # 원본은 앞 절반으로 줄임(제자리 수정)
        uniq.append(half)
    return uniq[:max(need, len(uniq))]
```

- [ ] **Step 8: respine에서 dedup+얼굴정렬 배선**

`_chronological_respine`의 flat 수집·정렬(103-108줄)을 아래로 교체:
```python
    flat, counts = [], []
    for b in body:
        segs = [b["primary"]] + list(b.get("alternates") or [])
        counts.append(len(segs))
        flat.extend(segs)
    flat = _dedup_and_fill(flat, need=sum(counts))
    # 정렬: 시간순 우선, 동시각이면 얼굴 세그먼트를 뒤로(대체 있을 때 후순위).
    ordered = sorted(flat, key=lambda s: (s.get("video_id", ""), s.get("start", 0.0),
                                          _is_face_seg(s.get("scene_desc", ""))))
```

주의: `flat`의 각 세그먼트에 `scene_desc`가 있어야 얼굴 정렬이 동작한다 — Task 5에서 `_ground_ref`가 scene_desc를 실어주도록 확장(현재는 video_id/seg_id/start/end만). 이 Task에서는 scene_desc 없으면 `_is_face_seg`가 False라 무해(정렬 안정성만 유지).

- [ ] **Step 9: 전체 respine 회귀 + 신규 통과**

Run: `cd .tracks/장면스파인 && python -m pytest shopping_shorts/tests/test_respine_dedup.py shopping_shorts/tests/test_chronological_respine.py -v`
Expected: 전부 PASS. 기존 순서 계약 테스트는 중복이 없던 케이스라 불변.

- [ ] **Step 10: 커밋**

```bash
git -C .tracks/장면스파인 add shopping_shorts/edit_plan.py shopping_shorts/tests/test_respine_dedup.py
git -C .tracks/장면스파인 commit -m "feat(edit_plan): ② 비트간 중복제거+서브슬라이스+얼굴 후순위"
```

---

### Task 4: ① visual_verb 앵커 — 대사가 시각행위를 지목하는 비트는 respine 제외

대사에 구체적 시각행위("결대로 찢어지는데")가 있으면 그 화면은 반드시 맞아야 한다. 모델에게 비트별 `visual_verb: bool`을 스키마로 받아, True인 비트는 respine에서 제외(tail처럼 앵커)해 모델 매칭을 유지한다. `_ground_ref`가 scene_desc를 실어 Task 3의 얼굴 정렬도 살린다.

**Files:**
- Modify: `.tracks/장면스파인/shopping_shorts/edit_plan.py:73-82` (`_ground_ref`가 scene_desc 포함), `:186-220` (`_RESPONSE_SCHEMA`에 visual_verb), `:143-152` (`_validate_and_ground` 출력에 visual_verb), `:85-122` (`_chronological_respine`가 visual_verb 비트 앵커), `:222-297` (프롬프트에 visual_verb 지시)
- Modify: `.tracks/장면스파인/shopping_shorts/tests/test_edit_plan_pure.py` 또는 신규 `tests/test_visual_verb_anchor.py`

**Interfaces:**
- Consumes: Task 3의 `_dedup_and_fill`, `_is_face_seg`.
- Produces:
  - `_ground_ref` 반환에 `"scene_desc"` 추가.
  - `_validate_and_ground` 각 beat에 `"visual_verb": bool`.
  - `_chronological_respine`: `visual_verb=True`인 body 비트는 재배치 대상에서 빼고 원위치·원세그먼트 고정(respined 플래그 없음), 나머지 body만 시간순.

- [ ] **Step 1: 앵커 동작 실패 테스트**

`tests/test_visual_verb_anchor.py` 생성:
```python
from shopping_shorts.edit_plan import _chronological_respine


def _beat(narr, start, verb=False, video="A"):
    return {"beat_idx": 0, "role": "x", "narration": narr, "target_seconds": 2.0,
            "primary": {"video_id": video, "seg_id": f"{video}-{start}",
                        "start": float(start), "end": float(start)+1, "scene_desc": ""},
            "alternates": [], "fit": 3, "visual_verb": verb}


def test_visual_verb_beat_anchored_not_respined():
    # "찢어진다"(visual_verb) 비트는 원래 화면 고정, 나머지 body만 시간순.
    beats = [_beat("훅", 10), _beat("결대로 찢어진다", 2, verb=True),
             _beat("실용", 8), _beat("cta", 6)]
    out = _chronological_respine(beats)
    verb_beat = out[1]
    assert verb_beat["narration"] == "결대로 찢어진다"
    assert verb_beat["primary"]["start"] == 2.0        # 모델이 고른 그대로
    assert verb_beat.get("respined") is not True        # 앵커 = respined 아님
    # 나머지 body(훅·실용)만 시간순 재배치, cta는 꼬리 앵커
    assert out[0]["primary"]["start"] == 8.0 and out[2]["primary"]["start"] == 10.0
```

- [ ] **Step 2: 실패 확인**

Run: `cd .tracks/장면스파인 && python -m pytest shopping_shorts/tests/test_visual_verb_anchor.py -v`
Expected: FAIL — 현재 respine은 visual_verb를 모르고 verb 비트도 재배치함.

- [ ] **Step 3: `_chronological_respine`가 visual_verb 앵커 처리**

body/tail 분리(102줄) 다음에 visual_verb 비트를 앵커 집합으로 분리. body 루프를 "앵커가 아닌 비트만 flat 풀에 넣고 재배치, 앵커 비트는 원위치·원세그먼트로 되꽂기"로 확장:
```python
    body, tail = beats[:-1], beats[-1]
    # visual_verb 비트는 앵커(원 화면 고정), 나머지 body만 시간순 재배치.
    movable_idx = [j for j, b in enumerate(body) if not b.get("visual_verb")]
    flat, counts = [], []
    for j in movable_idx:
        segs = [body[j]["primary"]] + list(body[j].get("alternates") or [])
        counts.append(len(segs))
        flat.extend(segs)
    flat = _dedup_and_fill(flat, need=sum(counts))
    ordered = sorted(flat, key=lambda s: (s.get("video_id", ""), s.get("start", 0.0),
                                          _is_face_seg(s.get("scene_desc", ""))))
    out, i = [], 0
    moved = {}
    for j, n in zip(movable_idx, counts):
        chunk = ordered[i:i + n]; i += n
        nb = dict(body[j]); nb["primary"] = chunk[0]; nb["alternates"] = chunk[1:]
        nb["respined"] = True
        moved[j] = nb
    for j, b in enumerate(body):
        out.append(moved[j] if j in moved else dict(b))   # 앵커 비트는 원본 그대로
    out.append(dict(tail))
    return out
```
(Task 1·3에서 넣은 단일 body 루프는 이 구조로 흡수 — flat/ordered/dedup 로직은 movable만 대상.)

- [ ] **Step 4: 통과 확인 + respine 회귀**

Run: `cd .tracks/장면스파인 && python -m pytest shopping_shorts/tests/test_visual_verb_anchor.py shopping_shorts/tests/test_chronological_respine.py shopping_shorts/tests/test_respine_dedup.py -v`
Expected: 전부 PASS. 기존 테스트 비트엔 `visual_verb` 키가 없어 `.get()`이 False → movable로 취급되어 기존 계약 유지.

- [ ] **Step 5: `_ground_ref`가 scene_desc 포함**

edit_plan.py:82 반환을 교체:
```python
    return {"video_id": seg["video_id"], "seg_id": sid, "start": seg["start"],
            "end": seg["end"], "scene_desc": seg.get("scene_desc", "")}
```

- [ ] **Step 6: 스키마·검증 출력에 visual_verb 추가**

`_RESPONSE_SCHEMA` beats.items.properties(194-213줄)에 추가:
```python
                    "visual_verb": {"type": "boolean"},
```
`_validate_and_ground`의 beats_out.append(143-152줄) dict에 추가:
```python
            "visual_verb": bool(beat.get("visual_verb", False)),
```

- [ ] **Step 7: 프롬프트에 visual_verb 지시**

`_PROMPT`(236줄 규칙 블록)와 `_SCRIPTED_PROMPT`(281줄 규칙 블록) 각각에 규칙 한 줄 추가:
```
- **visual_verb: 이 비트의 나레이션이 화면에서 눈으로 보이는 구체적 동작·상태(찢다·붓다·완성·꺼내다·바르다 등)를
  지목하면 true, 감정·설명·도입부처럼 특정 화면을 요구하지 않으면 false로 표시해라.**
```
스키마 required에는 넣지 않는다(없으면 False 폴백 — fail-open).

- [ ] **Step 8: build_edit_plan 빌드 테스트 회귀**

Run: `cd .tracks/장면스파인 && python -m pytest shopping_shorts/tests/test_edit_plan_build.py shopping_shorts/tests/test_edit_plan_pure.py -v`
Expected: PASS. 신규 필드는 옵셔널이라 기존 mock 응답에도 무해.

- [ ] **Step 9: 커밋**

```bash
git -C .tracks/장면스파인 add shopping_shorts/edit_plan.py shopping_shorts/tests/test_visual_verb_anchor.py
git -C .tracks/장면스파인 commit -m "feat(edit_plan): ① visual_verb 비트 앵커 — 시각행위 대사는 respine 제외"
```

---

### Task 5: ③ 리컨사일 — fit 낮은 비트만 Gemini 1콜로 문장을 화면에 맞춤

매칭 후 앵커 비트 중 fit≤3인 것만 모아 **1회** Gemini 호출로 나레이션을 화면(scene_desc)에 맞게 뜻 유지·표현만 미세수정. 루프 없음, 실패 시 원문 유지(fail-open). respined 비트는 대상 아님(b-roll이라 대사 안 맞아도 됨).

**Files:**
- Modify: `.tracks/장면스파인/shopping_shorts/edit_plan.py` (`_reconcile_weak_beats` 추가, `build_edit_plan`에서 호출)
- Create: `.tracks/장면스파인/shopping_shorts/tests/test_reconcile.py`

**Interfaces:**
- Consumes: `_vault_call(prompt, schema)`(edit_plan.py:300, 이미 존재), Task 4의 beat `visual_verb`/`respined`/`fit`/`scene_desc`.
- Produces: `_reconcile_weak_beats(beats, call=_vault_call) -> beats` — fit≤3이고 respined가 아닌(=앵커) 비트의 narration만 교체된 새 리스트. 대상 0개면 입력 그대로. call 주입으로 테스트에서 Gemini mock.

- [ ] **Step 1: 리컨사일 실패 테스트**

`tests/test_reconcile.py` 생성:
```python
from shopping_shorts.edit_plan import _reconcile_weak_beats


def _beat(idx, narr, fit, scene, respined=False):
    return {"beat_idx": idx, "narration": narr, "fit": fit, "respined": respined,
            "primary": {"scene_desc": scene}}


def test_only_weak_anchor_beats_reworded():
    beats = [_beat(0, "결대로 찢어진다", 2, "도우를 치댄다"),      # 약함+앵커 → 대상
             _beat(1, "완성입니다", 5, "완성품 클로즈업"),          # fit 높음 → 제외
             _beat(2, "촉촉해요", 1, "도우 반죽", respined=True)]   # respined → 제외
    def fake_call(prompt, schema, **kw):
        return {"rewrites": [{"beat_idx": 0, "narration": "반죽을 정성껏 치대요"}]}
    out = _reconcile_weak_beats(beats, call=fake_call)
    assert out[0]["narration"] == "반죽을 정성껏 치대요"
    assert out[1]["narration"] == "완성입니다" and out[2]["narration"] == "촉촉해요"


def test_reconcile_fail_open_keeps_original():
    beats = [_beat(0, "결대로 찢어진다", 2, "도우를 치댄다")]
    out = _reconcile_weak_beats(beats, call=lambda *a, **k: None)  # Gemini 실패
    assert out[0]["narration"] == "결대로 찢어진다"


def test_no_weak_beats_noop():
    beats = [_beat(0, "완성", 5, "완성품")]
    calls = []
    _reconcile_weak_beats(beats, call=lambda *a, **k: calls.append(1))
    assert calls == []   # 대상 0개면 Gemini 호출 자체를 안 함
```

- [ ] **Step 2: 실패 확인**

Run: `cd .tracks/장면스파인 && python -m pytest shopping_shorts/tests/test_reconcile.py -v`
Expected: FAIL — `_reconcile_weak_beats` 미정의.

- [ ] **Step 3: `_reconcile_weak_beats` 구현**

edit_plan.py의 `build_edit_plan` 위에 추가:
```python
_RECONCILE_SCHEMA = {
    "type": "object",
    "properties": {"rewrites": {"type": "array", "items": {
        "type": "object",
        "properties": {"beat_idx": {"type": "integer"}, "narration": {"type": "string"}},
        "required": ["beat_idx", "narration"]}}},
    "required": ["rewrites"],
}


def _reconcile_weak_beats(beats, call=_vault_call):
    """앵커(respined 아님)이면서 fit<=3인 비트의 나레이션만 화면(scene_desc)에 맞게
    1회 Gemini 호출로 미세수정. 대상 0개면 호출 없이 그대로. 실패 시 원문 유지(fail-open)."""
    weak = [b for b in beats if not b.get("respined") and 0 < int(b.get("fit") or 0) <= 3]
    if not weak:
        return beats
    lines = "\n".join(
        f"[{b['beat_idx']}] 화면:{(b.get('primary') or {}).get('scene_desc','')} | 현재대사:{b.get('narration','')}"
        for b in weak)
    prompt = (
        "아래 비트들은 대사와 화면이 어긋난다. 각 비트의 대사를 **뜻과 정보는 유지**하되 "
        "화면(scene_desc)에 어울리도록 표현만 자연스럽게 고쳐라. 화면에 없는 사실을 지어내지 마라.\n"
        f"{lines}\n출력은 rewrites 배열의 JSON만.")
    raw = call(prompt, _RECONCILE_SCHEMA)
    if not raw or not isinstance(raw, dict):
        return beats
    fixes = {int(r["beat_idx"]): r["narration"]
             for r in raw.get("rewrites", []) if r.get("narration")}
    out = []
    for b in beats:
        nb = dict(b)
        if b["beat_idx"] in fixes:
            nb["narration"] = fixes[b["beat_idx"]]
        out.append(nb)
    return out
```

- [ ] **Step 4: 통과 확인**

Run: `cd .tracks/장면스파인 && python -m pytest shopping_shorts/tests/test_reconcile.py -v`
Expected: 3개 PASS.

- [ ] **Step 5: build_edit_plan에 배선**

`build_edit_plan`에서 grounding 후 target_seconds 재계산(473-475줄) **앞**에 삽입:
```python
    grounded["beats"] = _reconcile_weak_beats(grounded["beats"])
```
(리컨사일이 narration을 바꾸므로 그 다음 글자수 기반 target_seconds 재계산이 새 문장 길이를 반영한다 — 순서 중요.)

- [ ] **Step 6: build 회귀**

Run: `cd .tracks/장면스파인 && python -m pytest shopping_shorts/tests/test_edit_plan_build.py -v`
Expected: PASS. `_vault_call`이 실키를 안 쓰는 mock 경로(기존 빌드 테스트가 캐스케이드 빈리스트로 None 반환)면 리컨사일도 fail-open으로 그대로 통과.

- [ ] **Step 7: 커밋**

```bash
git -C .tracks/장면스파인 add shopping_shorts/edit_plan.py shopping_shorts/tests/test_reconcile.py
git -C .tracks/장면스파인 commit -m "feat(edit_plan): ③ fit<=3 앵커 비트 1회 리컨사일 — 대사를 화면에 맞춤"
```

---

### Task 6: 실 job 렌더 검증 (합성 fixture 금지)

순수 로직은 pytest로 검증했으나, 실제 대사↔화면 개선은 서버에서 실 소스로 확인해야 한다(reference_verify_with_real_data). finish 전 검증 게이트.

**Files:** 없음(검증 태스크).

- [ ] **Step 1: finish 게이트로 병합**

```bash
cd .tracks/장면스파인 && git -C . status --porcelain   # 내 트랙 파일만인지 눈으로 확인
py tools/track.py finish 장면스파인
```
Expected: 문법→import→pytest 게이트 통과(신규 실패 0, baseline 8건은 기존). 통과 시 main 병합 → 3분 뒤 라이브.

- [ ] **Step 2: 서버 실 job 재생성 육안 검증**

같은 주제 소스 3~4개(예: 밥솥식빵 계열)로 shoppingshorts.duckdns.org에서 새 믹스 생성 후 확인:
- (a) 같은 시간구간이 2연속 비트에 오지 않음
- (b) visual_verb 비트(찢기·완성 등)의 화면이 대사와 일치
- (c) 어긋난 앵커 비트에 빨간불+스왑버튼 노출(respined 비트는 중립)
- (d) 렌더 mp4에서 초반 반복 없음

- [ ] **Step 3: 결과를 handoff/장면스파인.md에 기록**

검증 결과(개선 확인/잔여 이슈)를 핸드오프 완료 항목에 추가하고 커밋.

---

## Self-Review

**1. Spec coverage:**
- ④ fit 정직화 → Task 1 ✅
- ⓪ 분석 세분화 → Task 2 ✅
- ② dedup+서브슬라이스+얼굴필터 → Task 3 ✅
- ① visual_verb 앵커 → Task 4 ✅
- ③ 1회 리컨사일 → Task 5 ✅
- 실 데이터 검증 → Task 6 ✅
- 소스 원칙(같은 주제 다다익선) → Global Constraints에 문서화(코드 제약 아님) ✅

**2. Placeholder scan:** 모든 코드 스텝에 실제 코드/명령/기대출력 포함, TBD 없음.

**3. Type consistency:** `_dedup_and_fill(flat, need)`·`_is_face_seg(scene_desc)`·`visual_verb`(bool)·`respined`(bool)·`_reconcile_weak_beats(beats, call)` 시그니처가 Task 1→5 전체에서 일관. `_ground_ref`가 Task 4에서 scene_desc를 실어야 Task 3의 얼굴 정렬이 실효(Task 3 Step 8 주석에 의존성 명시). fit 임계(≤2 빨간불/≤3 리컨사일)는 produce.html 기존 계약과 일치.

**주의 — Task 간 코드 흐름 충돌:** Task 1·3·4가 모두 `_chronological_respine`의 body 루프를 수정한다. Task 4 Step 3이 Task 1·3의 단일 루프를 movable-only 구조로 **흡수·교체**함을 명시했으므로, 순서대로(1→3→4) 실행하면 최종 형태로 수렴한다. 태스크를 건너뛰거나 순서를 바꾸면 루프가 어긋나니 1→2→3→4→5 순서를 지킬 것.
