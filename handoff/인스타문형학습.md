> **소유 트랙**: 인스타문형학습 — 이 파일은 이 트랙 세션만 수정한다. 다른 트랙은 읽기만.
> 착수 지시서: `handoff/인스타대본스파인_전면학습.md`
> 산출 보고서: `out/인스타_훅문형_본문패턴_실측보고서.md` (바탕화면에도 복사됨)
> 도구: `tools/script_lab/insta_body_pattern.py` (재실행 가능, 서버 reference.db 대상)

# 핸드오프 — 인스타 훅문형·본문패턴 학습 (2026-08-20)

## 사장님 확정 사항

```
2026-08-20 ① "훅 문형 방향이 맞고"                    → 판정 단위 = 훅 문형(축). 채널 아님
           ② "훅은 거의 잘되는게 정해져있으니"          → 훅은 틀로 고정
           ③ "본문 패턴 스타일도 패턴화가 가능하다"      → 본문 5구간 역할 프로필로 실측
           ④ "데이터 많으니 시간 걸려도 좋으니 계속"     → 규모 제한 없음
           ⑤ "순서대로"                               → 보고서 §7 순서대로 진행 승인
```

## 왜 채널 판정을 기각했나 (핸드오프 원안 변경)

원안은 "채널당 3편 → 훅 틀이 2/3 같으면 패턴 있음"이었다. **실측으로 기각됐다.**

```
채널 안 훅 일치율    7~42%뿐 (chae2home 42편에서 첫2어절 최다 23%)
축을 넘는 고정 어구  "여러분 다이소 가면 이거" 25편/12채널
```
채널로 가르면 20채널이 거의 다 "패턴 없음"으로 떨어진다. 문형으로 모으면 잡힌다.

**승격 기준(제안·적용): 8편 이상 + 3채널 이상.** 부품은행 승격게이트 `source≥3`과 결이 같다.

## 한 일 (전부 실측·검증 완료)

```
□ 522~527편(한국어·컷5+) 훅 문형 분류 — 축 커버리지 73%, 승격통과 12축
□ 축별 본문 5구간 역할 프로필 (12개 역할 태거)
□ 축 무관 상수 3개: 길이 22~25초 · 밀도 297~327자/30초 · 마무리 CTA 76~92%
□ 장면 규격 7,047컷 (scene_desc 전량) — 축별 구간 특성
□ spine 58 금지경고형 · 59 사회증거형 심음(라이브 DB, approved, 조립경로 ON)
□ spine 53 밀도 240 → 327자 교정
□ 조립 경로 배선: INSTA_CATEGORIES = ("다이소형","금지경고형","사회증거형")
□ 전사 백필 진행 중 (599 → 607편, 계속 도는 중)
```

## ★함정 4개 — 다음 세션이 또 안 밟게

### 1. 심어도 안 돈다 — `INSTA_CATEGORIES`에 없으면 조립 경로를 못 탄다

`_assembled_drafts`는 스파인의 `fit_categories`에 `app.INSTA_CATEGORIES` 이름이 있어야
조립 대상으로 본다. 없으면 **오류 없이 생성기로 폴백한다.** 새 인스타 축을 만들면
반드시 이 상수에 기계용 이름을 같이 추가하라.

### 2. 가짜 축 — 접두어 검정을 반드시 해라

"여러분"(32편)·"와"(32편)·"저 이거"(16편)가 축으로 잡혔는데, **접두어를 지우고
다시 판정하니 80편 중 75편이 여전히 미분류**였다. 내용 축이 아니라 수식어다.
축으로 두면 진짜 축을 가린다. 검정법: 접두어 제거 → 재판정 → 다른 축으로 안 가면 가짜.

### 3. 슬롯 뒤에 '이라/이더라'를 붙이지 마라

`{차별점}이라` → `fix_josa`가 받침을 보고 **"구조가라 되는 거였어요"**로 망가뜨린다.
슬롯 뒤는 쉼표로 끊어라. **조립을 실제로 돌려보기 전엔 안 보인다**(정적 검토로 못 잡았다).

### 4. 같은 슬롯을 두 칸에 쓰면 조립 티가 난다

`{불편함}`을 mistake·reason 두 칸에 뒀더니 같은 문구가 한 대본에 2번 나왔다.

## ⏭ 다음 할 일

```
□ 1. ★라이브 육안 확인 — 제작소에서 대본 스타일 목록에 '금지경고형'·'사회증거형'이
      뜨는지, 실제 job으로 조립본이 나오는지. (DB·조립 함수 단위로는 검증했지만
      화면에서 본 적은 없다 — CLAUDE.md 렌더 그라운딩 규칙)
□ 2. 전사 백필 마저 확인 — shopping_flavorr·homedukddak·j_ujjjj 3채널이 남아 있었다
      py: backfill_channel_scripts --username <채널> --limit 3 (서버, /etc/shopping-shorts.env 실어야 함)
□ 3. spine 52·53·54를 조립 경로로 올린다 ← ★가장 값이 큰 일
      실측: 템플릿 문장 18/18 · 30/31 · 21/22개가 슬롯 이름 불일치로 건너뛰어진다
      ({가족}{전문가}{장소}{단어}{받는것}{대상}{동사}{출처}{물건} — insta_facts 7종에 없음)
      → 지금은 생성기가 템플릿을 '참고'로만 쓰는 경로로 가고, **거기서 훅 사족 15%가 난다**
      → 슬롯 이름을 7종에 맞추면 세 축이 조립으로 올라오고 사족 자리가 물리적으로 사라진다
      ⚠️ insta_facts.py에 슬롯을 **추가해야만** 하면 인스타대본스파인 트랙과 조율하라(공용 파일)
□ 4. 미분류 144편 5차 발굴 (커버리지 73% → 80%)
□ 5. 장면 '기타' 32% 줄이기 → 축별 장면 슬롯표 만들기
□ 6. 전세공포축(10편/6채널) 재검토 — 2편 오분류라 보류했다. 전사가 늘면 다시 봐라
```

## 다른 트랙에 인계할 것

```
→ 인스타대본스파인 트랙: spine 57(다이소 내부인형) 밀도 226 → 297자
   실측 근거: 다이소지목축 62편 밀도 중앙값 297자/30초 (현재 226자, 71자 낮음)
   ★이쪽에서 안 고쳤다 — daiso_spine.py가 그쪽 소유라 두 군데서 정하면 어긋난다(0순위-B)
```

## 착수 전 다시 확인할 것

```
□ script_extracts 편수 — 백필이 계속 돌아 늘어난다(599 → 607 → ?)
□ spine 행 — 58·59를 심었다. 그 뒤 다른 세션이 더 심었을 수 있다
□ 축 분포 — 전사가 늘면 비율이 바뀐다. 도구를 다시 돌려라(1분)
```


---

# 【2차 세션】 스타일 자동선택 배선 (2026-08-20 이어서)

## 사장님 지시

> "몇 개 영상들 중 메인 영상이 스타일을 선택하잖아 대본 전에. 그럼 그걸로 하든지,
>  영상 분석 시 점수를 매길 때 지금 만든 스타일이 있으면 그렇게 하면 되나"
> → 확인 결과 **그렇다.** 종전 자동선택은 점수가 아니라 **목록 앞 2개**였다.
> 결정: **둘 다** (분석에서 축 저장 → 대본이 가져다 쓴다) / 미분류는 종전 폴백.

> "말이 안 되는 대본을 뽑는거야" (52 조립 결과를 보고)
> → 맞는 지적. 틀의 문장이 **특정 소재를 전제**하고 있었다. 5축 전부 같은 병이었다.

## 한 일

```
□ shopping_shorts/hook_axis.py 신설 — 축 정규식·축↔스파인 매핑의 **정본**
   분석 도구가 이 모듈을 import (대조 실측 535편/535건 일치 후 합침)
□ app.py: auto_style일 때 메인 영상 축으로 스타일 정렬(거르지 않고 정렬만)
□ script_extracts.hook_axis 컬럼 + save_script/get_script 단일 출구에서 저장·제공
□ scripts/backfill_hook_axis.py — 서버 실행 완료(617편 중 394편 채움)
□ produce.html: "🎯 훅 문형 X → 대본 틀 Y" 표시(판정 실패도 말한다)
□ 소재 전제 문장 전수 제거(52는 갈아엎음, 53은 레시피 전용으로 좁힘)
□ 고조 칸(escalation) 5축 추가 — 없으면 script_gate '고조 심화'에서 전부 반려된다
```

## 라이브 상태 (실측)

```
인스타 스파인 6개 전부 조립 ON · 게이트 통과(목표 24초, 재료 풍부 기준)
  52 가족갈등 반전형 10칸 307자 / 53 단정 명령형 9칸 327자 / 54 물건 발견형 9칸 327자
  57 다이소 내부인형 8칸 226자 / 58 금지경고형 8칸 311자 / 59 사회증거형 8칸 323자

축 판정: 전사 617편 중 394편(64%)에 축이 붙음
스파인이 정해지는 편수: 317/617 (51%) — 나머지는 종전 폴백
```

## ★함정 (2차에서 새로 밟은 것)

### 5. 배선을 재료가 생기기 전 자리에 넣으면 조용히 죽는다
`_src`(영상 재료)는 `_materials_for_generate` 뒤에야 생긴다. 그 앞에서 쓰면 `NameError`가
`except`에 삼켜져 **아무 일도 안 하는 폴백**이 된다. 실제로 한 번 그렇게 넣었다가 고쳤다.

### 6. 서버에 검증용 파일을 복사해 두고 지우지 마라
`scp`로 서버에 올려 검증하고 안 지웠더니 워킹트리가 더러워졌다(`M`·`??`).
그 상태면 `auto_deploy`가 pull 실패로 **모든 배포가 조용히 멈춘다**(CLAUDE.md 9번).
정리했고 배포 정상 확인. ⚠️`git checkout -- a b`는 b가 untracked면 **통째로 실패**한다.

### 7. HTML 속 JS는 병합 게이트가 못 잡는다
`node --check`로 문법 + 분기 3가지를 직접 돌려 확인했다.

## ⏭ 다음 할 일

```
□ 1. ★제작소 화면 육안 확인 — 유일하게 안 한 검증
      HTTP 307(로그인 게이트)이라 익명 curl로는 못 본다. 사장님 로그인 브라우저 필요.
      볼 것: 대본 추출 후 "🎯 훅 문형 … → 대본 틀 …"이 뜨는가 / 실제 job의 조립본
□ 2. 틀이 목표 초에 맞춰 길이를 못 바꾼다 — 지금은 24~30초 전용
      변형 선택 기준이 '슬롯이 차는가'뿐이라 초를 안 본다(spine_fill.pick_template)
      20초로 걸면 초과, 재료가 적으면 미달(54·57)
□ 3. 자막 컷 수 미검증 — 대본 칸 8~10개 vs 실측 화면 컷 중앙 13개.
      대본 칸과 자막 컷은 다른 단위라 렌더 단계를 봐야 답할 수 있다
□ 4. 커버리지 72%가 한계 — 5차 발굴 수익 없음을 실측으로 확인했다(아래)
```

### 5차 발굴을 접은 근거 (같은 조사 반복 금지)

```
후보 7개 중 8편 넘는 건 '생활불편개시' 11편 하나뿐인데, 그건 훅 문형이 아니라
단어 매칭(청소·스트레스·귀찮)이라 가짜 축이 된다 → 넣지 않았다.
미분류 150편의 본문 프로필이 어느 축과도 다르다(도입 반응19%·실증17%·문제15%
vs 지인증언 상황48% / 금지경고 문제27%) = 여러 축이 섞여 있다 → 폴백이 맞다.
```

## 다른 트랙에 인계 (그대로 남아 있음)

```
→ 인스타대본스파인 트랙: spine 57 밀도 226 → 297자/30초
   근거: 다이소지목축 62편 밀도 중앙값 297자 (현재 226자, 71자 낮음)
   ★이쪽에서 안 고쳤다 — daiso_spine.py가 그쪽 소유다(0순위-B)
```


## 2026-08-21 — 나열형(9번째 틀) 라이브 배선

**한 일**
- `spine.is_list` 컬럼 신설(store 마이그레이션 + list_spines SELECT + set_spine_style).
  no_cta가 겪은 함정과 같다 — SELECT에 안 실으면 플래그가 조용히 죽는다.
- 조립 갈래를 `insta` / `insta_list`로 가름(`_assembled_drafts`).
  나열형만 재료를 **안 합친다** — `_insta_slot_sets`가 편별 슬롯을 만들고
  장면 근거도 **그 편의 화면**으로만 검사한다.
- `hook_axis`: 리스트형(8편/6채널) → `나열 추천형`.
- 항목 beat에 `src_index`(몇 번째 담긴 영상인지) 표식.
- 버그 2건 수정: 건너뛴 항목이 순번에 구멍("첫 번째 → 세 번째") / 변형 돌려쓰기 번호.
- 서버 spine id=63 `나열 추천형` approved, is_list=1 확인.

**실측(실조립 경로)**
5편 담기 → 20초 2항목 / 30초 3항목 / 40초 4항목, 항목마다 다른 제품·다른 꼬리.
1편 재료 부족 시 순번 1·2·3·4 연속 · src_index 0·2·3·4.

**⏭ 다음**
1. ~~화면(컷) 배치 실측~~ → **2026-08-21 완료. 어긋남을 실측으로 잡고 고쳤다.**
   실측: `_assign_timeline`에 나열형 대본을 넣으니 **3항목 중 2항목이 남의 제품 화면**
   (신발건조기 말에 우산 화면). 원인은 이 함수가 비트를 그룹에 **위치순**으로 나눠 주는
   것 — 담긴 영상이 곧 순서인 8축엔 정답이지만 나열형은 말↔화면이 짝지어져야 하고,
   intro·cta가 그룹 자리를 한 칸씩 민다.
   고침: `_pin_by_src_video`를 화면 확정 단일 관문(`_pin_screens`) 맨 앞에 뒀다.
   `src_video`를 선언한 비트만 자기 영상 세그로 못박고 `screen_pinned` 표시(CTA 핀과
   같은 장치). 영상 id는 `backbone._vid_of` 한 곳에서만 뽑는다.
   실측 후: 3/3 일치. **src_video 없는 8축은 배정값이 변경 전과 완전히 동일**(회귀 0).
   회귀 테스트 `tests/test_list_screen_binding.py` 2건.
2. 라이브 제작소에서 나열형 배지·선택 육안 확인.
3. 미분류 160편 · 전세공포 축(한 채널 쏠림)은 보류 그대로.

**기준선 메모**: pytest 실패 15건은 origin/main과 동일. 그중 `test_script_gate` ·
`test_style_spine_store` 2건은 **말속도 천장(246자) 작업**이 남긴 것으로, 테스트가
옛 기대치(300자)를 들고 있다. 내 변경과 무관함을 워크트리 대조로 확인했다.


## 2026-08-21 — 나열형 화면 배치 실측·수정

말과 화면이 어긋나던 것(3항목 중 2항목)을 실측으로 잡아 고쳤다. 위 ⏭ 1번 참조.
`spine_fill.fill_list`가 항목에 `src_video`를 달고, `_insta_slot_sets`가 그 편의
세그먼트에서 영상 id를 뽑는다(순번이 아니라 **id**로 잇는다 — 담긴 순서와 화면 쪽
목록 순서가 같다고 믿으면 언젠가 어긋난다).

**⏭ 다음**
1. ~~실제 job의 세그먼트 모양 확인~~ → **완료(서버 실데이터)**.
   ★찾은 것: 저장된 전사의 세그먼트엔 **video_id가 없다**(seg.video_id=None).
     seg_id는 `<URL>-<번호>` 꼴이라 `backbone._vid_of`의 접두사 규칙으로 뽑힌다.
     화면 쪽(`_build_inventory`)은 `script["video_id"]`를 싣는다 — 두 쪽이 같은 값이
     되는 근거는 `script_extract.py:453`이 seg_id를 `f"{video_id}-{n}"`으로 만든다는
     것 **하나뿐**이다. 그 규약이 깨지면 못박기가 통째로 헛돈다 → 테스트로 못박았다.
   실측: 서버 실전사 4편 → 두 쪽 영상 id 집합 일치, 실제 `_build_source_sentence_sets`
   그룹으로 못박기 **3/3 일치**.
2. 나열형 배지·훅 문형 자동선택 육안 확인.
