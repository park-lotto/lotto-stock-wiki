# 보이스 프리셋 라이브러리 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** produce 4단계에서 페르소나 프리셋 카드로 성우·속도·감도를 한 번에 고르고, ElevenLabs로 그 설정대로 나레이션을 생성한다.

**Architecture:** 큐레이션 프리셋을 git추적 `assets/voice_presets.json`에 두고 기동 시 DB `voice_presets`로 seed. 유저가 produce에서 프리셋을 고르면 `mix_jobs.voice_json`에 스냅샷 저장 → 기존 edit_plan에 대해 TTS만 재생성. ElevenLabs `voice_settings`(stability/similarity/style/speaker_boost/speed 0.7~1.2)를 payload에 실고, 1.2 초과 속도·무음삭제는 ffmpeg 후처리(atempo+silenceremove) 한 패스로 처리.

**Tech Stack:** FastAPI, SQLite(store.py), requests(ElevenLabs), ffmpeg, pytest.

**주의(워킹트리 상태):** `store.py` `app.py` `service.py` `static/produce.html` 등은 다른 세션(꾸미기/caption_style)의 **미커밋 변경**이 섞여 있다. 현재 워킹트리 상태 위에서 작업하고, 커밋은 이 기능 파일만 골라 담는다(`git add -A` 금지 — CLAUDE.md 배포규칙).

**speed 사실(ElevenLabs 문서 확인):** `voice_settings.speed` 유효범위 **0.7~1.2**(기본 1.0). stability/similarity_boost/style는 0~1. UI의 1.3~1.5는 API 밖이라 `atempo`로 보정.

---

## File Structure

- Create: `shopping_shorts/voice_presets.py` — 프리셋 JSON 로더 + DB seed + 조회 헬퍼
- Create: `shopping_shorts/audio_post.py` — ffmpeg 후처리(atempo 속도보정 + silenceremove 무음삭제) 한 패스
- Create: `shopping_shorts/assets/voice_presets.json` — 큐레이션 프리셋(KR) 시드 데이터
- Create: `shopping_shorts/scripts/build_voice_samples.py` — 프리셋별 미리듣기 mp3 생성(큐레이션 시 1회 실행)
- Modify: `shopping_shorts/tts.py` — `synthesize_tts`에 voice_settings/speed 인자
- Modify: `shopping_shorts/store.py` — `voice_presets` 테이블 + `mix_jobs.voice_json` 마이그레이션 + 조회/저장 메서드
- Modify: `shopping_shorts/mix_pipeline.py` — 비트별 TTS에 프리셋 적용 + 후처리 + `resynth_tts_job`
- Modify: `shopping_shorts/app.py` — 프리셋 목록/샘플/선택저장/프리뷰 엔드포인트 + startup seed
- Modify: `shopping_shorts/static/produce.html` — 4단계 프리셋 선택 UI
- Test: `shopping_shorts/tests/test_tts.py`(확장), `test_audio_post.py`, `test_voice_presets.py`, `test_store_voice.py`

---

## Task 1: tts.py — voice_settings/speed payload

**Files:**
- Modify: `shopping_shorts/tts.py:37-58`
- Test: `shopping_shorts/tests/test_tts.py`

- [ ] **Step 1: Write the failing test** (append to `tests/test_tts.py`)

```python
def test_synthesize_sends_voice_settings_and_speed(monkeypatch):
    calls = {}

    class FakeResp:
        content = b"ID3fake"
        def raise_for_status(self): pass

    def fake_post(url, **kw):
        calls["json"] = kw.get("json")
        return FakeResp()

    monkeypatch.setattr(tts.config, "ELEVENLABS_API_KEY", "sk-test")
    monkeypatch.setattr(tts.requests, "post", fake_post)

    import tempfile
    from pathlib import Path
    out = Path(tempfile.mkdtemp()) / "b.mp3"
    tts.synthesize_tts(
        "테스트", str(out), voice_id="vX",
        voice_settings={"stability": 0.65, "similarity_boost": 0.75,
                        "style": 0.0, "use_speaker_boost": True},
        speed=1.1, model_id="eleven_multilingual_v2",
    )
    vs = calls["json"]["voice_settings"]
    assert vs["stability"] == 0.65
    assert vs["style"] == 0.0
    assert vs["use_speaker_boost"] is True
    assert vs["speed"] == 1.1                      # speed는 voice_settings 안에 병합
    assert calls["json"]["model_id"] == "eleven_multilingual_v2"


def test_synthesize_speed_clamped_to_api_range(monkeypatch):
    calls = {}

    class FakeResp:
        content = b"ID3"
        def raise_for_status(self): pass

    monkeypatch.setattr(tts.config, "ELEVENLABS_API_KEY", "sk-test")
    monkeypatch.setattr(tts.requests, "post", lambda url, **kw: calls.setdefault("json", kw["json"]) or FakeResp())

    import tempfile
    from pathlib import Path
    out = Path(tempfile.mkdtemp()) / "b.mp3"
    tts.synthesize_tts("x", str(out), voice_id="v", speed=1.5)  # 1.5 요청
    assert calls["json"]["voice_settings"]["speed"] == 1.2       # API 상한 1.2로 clamp
```

- [ ] **Step 2: Run to verify fail**

Run: `python -m pytest shopping_shorts/tests/test_tts.py::test_synthesize_sends_voice_settings_and_speed -v`
Expected: FAIL (`synthesize_tts() got an unexpected keyword argument 'voice_settings'`)

- [ ] **Step 3: Implement** — replace `synthesize_tts` in `tts.py` (lines 37-58)

```python
# API가 허용하는 speed 범위(문서: 0.7~1.2). 이 밖은 호출부에서 atempo로 보정.
_SPEED_API_MIN, _SPEED_API_MAX = 0.7, 1.2


def synthesize_tts(text, out_path, voice_id=None, voice_settings=None,
                   speed=None, model_id=None, max_retries=3):
    """text → mp3(out_path). ElevenLabs 호출, 키 없으면 무음 mock. out_path 반환.

    voice_settings: {stability, similarity_boost, style, use_speaker_boost} (0~1).
    speed: 재생속도. API는 0.7~1.2만 허용하므로 그 범위로 clamp해 voice_settings.speed로 보냄
           (1.2 초과분은 audio_post에서 atempo로 별도 보정)."""
    if not config.ELEVENLABS_API_KEY:
        _write_silent_mp3(out_path, _estimate_seconds(text))
        return out_path
    vid = voice_id or config.ELEVENLABS_VOICE_ID
    url = _ENDPOINT.format(voice_id=vid)
    headers = {"xi-api-key": config.ELEVENLABS_API_KEY, "Content-Type": "application/json"}
    payload = {"text": text, "model_id": model_id or "eleven_multilingual_v2"}
    settings = dict(voice_settings) if voice_settings else {}
    if speed is not None:
        settings["speed"] = max(_SPEED_API_MIN, min(_SPEED_API_MAX, speed))
    if settings:
        payload["voice_settings"] = settings
    for attempt in range(max_retries):
        try:
            r = requests.post(url, headers=headers, json=payload, timeout=60)
            r.raise_for_status()
            with open(out_path, "wb") as f:
                f.write(r.content)
            return out_path
        except requests.RequestException:
            if attempt < max_retries - 1:
                time.sleep((attempt + 1) * 2)
                continue
            raise
    return out_path
```

- [ ] **Step 4: Run to verify pass**

Run: `python -m pytest shopping_shorts/tests/test_tts.py -v`
Expected: PASS (기존 test_synthesize_with_key_calls_api 포함 전부)

- [ ] **Step 5: Commit**

```bash
git add shopping_shorts/tts.py shopping_shorts/tests/test_tts.py
git commit -m "feat(shopping_shorts): tts synthesize_tts에 voice_settings/speed 지원"
```

---

## Task 2: audio_post.py — 속도보정 + 무음삭제 후처리

**Files:**
- Create: `shopping_shorts/audio_post.py`
- Test: `shopping_shorts/tests/test_audio_post.py`

- [ ] **Step 1: Write the failing test** (`tests/test_audio_post.py`)

```python
import tempfile
from pathlib import Path
from shopping_shorts import audio_post


def test_no_op_returns_input_untouched(monkeypatch):
    """tempo=1.0 + silence off면 ffmpeg 호출 없이 입력경로 그대로 반환."""
    called = {"n": 0}
    monkeypatch.setattr(audio_post.subprocess, "run", lambda *a, **k: called.__setitem__("n", called["n"] + 1))
    p = Path(tempfile.mkdtemp()) / "in.mp3"
    p.write_bytes(b"ID3")
    out = audio_post.post_process(str(p), str(p), tempo=1.0, silence_trim="off")
    assert out == str(p)
    assert called["n"] == 0


def test_builds_filter_chain(monkeypatch):
    cmd = {}
    def fake_run(c, **k):
        cmd["c"] = c
        Path(c[-1]).write_bytes(b"ID3out")
        class R: returncode = 0
        return R()
    monkeypatch.setattr(audio_post.subprocess, "run", fake_run)

    src = Path(tempfile.mkdtemp()) / "in.mp3"; src.write_bytes(b"ID3")
    dst = Path(tempfile.mkdtemp()) / "out.mp3"
    audio_post.post_process(str(src), str(dst), tempo=1.25, silence_trim="mid")
    af = cmd["c"][cmd["c"].index("-af") + 1]
    assert "atempo=1.25" in af
    assert "silenceremove" in af


def test_silence_level_params():
    assert audio_post._silence_filter("off") is None
    assert "stop_threshold" in audio_post._silence_filter("weak")
    # 강할수록 짧은 무음도 자름(stop_duration 작아짐)
    import re
    def dur(level):
        return float(re.search(r"stop_duration=([\d.]+)", audio_post._silence_filter(level)).group(1))
    assert dur("strong") < dur("mid") < dur("weak")
```

- [ ] **Step 2: Run to verify fail**

Run: `python -m pytest shopping_shorts/tests/test_audio_post.py -v`
Expected: FAIL (`No module named 'shopping_shorts.audio_post'`)

- [ ] **Step 3: Implement** (`audio_post.py`)

```python
"""TTS mp3 후처리: 속도 미세보정(atempo)과 무음삭제(silenceremove)를 ffmpeg 한 패스로.

ElevenLabs speed는 0.7~1.2만 지원해, 그 이상(1.3~1.5) 속도는 여기서 atempo로 얹는다.
무음삭제는 나레이션 사이 쉬는 구간을 잘라 빠르게 이어붙인다(레벨: off/weak/mid/strong)."""
import subprocess

# 레벨별 silenceremove 파라미터. stop_duration=자를 최소 무음길이(초), stop_threshold=무음 판정 dB.
# 강할수록 짧은 무음까지 자르고(작은 duration), 판정 임계도 관대(높은 dB).
_SILENCE = {
    "weak":   "silenceremove=stop_periods=-1:stop_duration=0.6:stop_threshold=-40dB",
    "mid":    "silenceremove=stop_periods=-1:stop_duration=0.4:stop_threshold=-38dB",
    "strong": "silenceremove=stop_periods=-1:stop_duration=0.25:stop_threshold=-35dB",
}


def _silence_filter(level):
    """레벨 문자열 → silenceremove 필터 문자열. off/미지정이면 None."""
    return _SILENCE.get(level or "off")


def post_process(in_path, out_path, tempo=1.0, silence_trim="off"):
    """in_path mp3에 속도(tempo)·무음삭제를 적용해 out_path로. 둘 다 no-op이면 in_path 그대로 반환.

    tempo: atempo 배율(1.0=변화없음). silence_trim: off/weak/mid/strong."""
    filters = []
    if tempo and abs(tempo - 1.0) > 1e-3:
        filters.append(f"atempo={tempo:.3f}".rstrip("0").rstrip("."))
    sf = _silence_filter(silence_trim)
    if sf:
        filters.append(sf)
    if not filters:
        return in_path
    subprocess.run(
        ["ffmpeg", "-y", "-i", str(in_path), "-af", ",".join(filters),
         "-q:a", "4", str(out_path)],
        stdin=subprocess.DEVNULL, capture_output=True, check=True,
    )
    return str(out_path)
```

- [ ] **Step 4: Run to verify pass**

Run: `python -m pytest shopping_shorts/tests/test_audio_post.py -v`
Expected: PASS

- [ ] **Step 5: Commit**

```bash
git add shopping_shorts/audio_post.py shopping_shorts/tests/test_audio_post.py
git commit -m "feat(shopping_shorts): audio_post — atempo 속도보정+silenceremove 무음삭제"
```

---

## Task 3: store.py — voice_presets 테이블 + voice_json 마이그레이션 + 메서드

**Files:**
- Modify: `shopping_shorts/store.py` (CREATE 블록 ~200, 마이그레이션 루프 268-277, get/update_mix_job 921-966, 새 메서드 추가)
- Test: `shopping_shorts/tests/test_store_voice.py`

- [ ] **Step 1: Write the failing test** (`tests/test_store_voice.py`)

```python
import tempfile
from pathlib import Path
from shopping_shorts.store import Store


def _store():
    return Store(Path(tempfile.mkdtemp()) / "t.db")


def test_upsert_and_list_and_get_preset():
    s = _store()
    p = {
        "preset_id": "kr-calm-seulgi", "name": "슬기", "one_liner": "차분한 여성",
        "lang": "KR", "archetype": "차분정보", "base_voice_id": "v1",
        "model_id": "eleven_multilingual_v2",
        "voice_settings": {"stability": 0.65, "similarity_boost": 0.75, "style": 0.0, "use_speaker_boost": True},
        "default_speed": 1.1, "default_silence_trim": "mid",
        "sample_file": "seulgi.mp3", "source_ref": "채널X 참고", "origin": "curated",
    }
    s.upsert_voice_preset(p)
    got = s.get_voice_preset("kr-calm-seulgi")
    assert got["name"] == "슬기"
    assert got["voice_settings"]["stability"] == 0.65
    assert got["default_speed"] == 1.1

    # upsert는 갱신(중복 PK여도 예외 없이 덮어씀)
    p2 = dict(p, name="슬기2")
    s.upsert_voice_preset(p2)
    assert s.get_voice_preset("kr-calm-seulgi")["name"] == "슬기2"

    rows = s.list_voice_presets(lang="KR")
    assert len(rows) == 1 and rows[0]["preset_id"] == "kr-calm-seulgi"
    assert s.list_voice_presets(lang="JP") == []


def test_mix_job_voice_json_roundtrip():
    s = _store()
    s.create_mix_job("j1", ["u"], 30, "구조")
    s.update_mix_job("j1", voice={"preset_id": "kr-calm-seulgi", "speed": 1.3, "silence_trim": "mid"})
    job = s.get_mix_job("j1")
    assert job["voice"]["preset_id"] == "kr-calm-seulgi"
    assert job["voice"]["speed"] == 1.3
```

- [ ] **Step 2: Run to verify fail**

Run: `python -m pytest shopping_shorts/tests/test_store_voice.py -v`
Expected: FAIL (`'Store' object has no attribute 'upsert_voice_preset'`)

- [ ] **Step 3a: Implement — CREATE TABLE** (store.py, `settings` 테이블 CREATE 바로 앞 ~line 200에 추가)

```python
            c.execute("""
                CREATE TABLE IF NOT EXISTS voice_presets (
                    preset_id TEXT PRIMARY KEY,
                    name TEXT NOT NULL,
                    one_liner TEXT,
                    lang TEXT NOT NULL DEFAULT 'KR',
                    archetype TEXT,
                    base_voice_id TEXT NOT NULL,
                    model_id TEXT NOT NULL DEFAULT 'eleven_multilingual_v2',
                    voice_settings_json TEXT NOT NULL,
                    default_speed REAL NOT NULL DEFAULT 1.0,
                    default_silence_trim TEXT NOT NULL DEFAULT 'off',
                    sample_file TEXT,
                    source_ref TEXT,
                    origin TEXT NOT NULL DEFAULT 'curated',
                    created_at TEXT NOT NULL
                )
            """)
```

- [ ] **Step 3b: Implement — voice_json 마이그레이션** (store.py 마이그레이션 루프 268-273 튜플에 한 줄 추가)

```python
                ("headcopy_json", "TEXT"),  # 영상제작 5단계 꾸미기 헤드카피(2026-07-13)
                ("voice_json", "TEXT"),     # 영상제작 4단계 보이스 프리셋 선택 스냅샷(2026-07-14)
```

- [ ] **Step 3c: Implement — get_mix_job에 voice 포함** (SELECT 컬럼 목록 끝 `caption_style_json ` 뒤에 `, voice_json` 추가하고, 반환 dict에 `voice` 키 추가)

get_mix_job SELECT 문자열을 다음으로:
```python
                "subtitle_removal, clean_video_path, given_script, headcopy_json, "
                "caption_style_json, voice_json "
```
반환 dict의 `caption_style` 줄 다음에 추가(row 인덱스 16):
```python
            "caption_style": json.loads(row[15]) if row[15] else None,
            "voice": json.loads(row[16]) if row[16] else None,
```

- [ ] **Step 3d: Implement — update_mix_job에 voice 핸들러** (caption_style 핸들러 다음에 추가)

```python
        if "voice" in fields:
            cols.append("voice_json=?")
            vals.append(json.dumps(fields["voice"], ensure_ascii=False) if fields["voice"] else None)
```

- [ ] **Step 3e: Implement — 프리셋 메서드** (store.py 클래스 내 임의 위치, 예: get_mix_job 근처에 추가)

```python
    def upsert_voice_preset(self, p):
        """보이스 프리셋 1건 upsert(preset_id 충돌 시 덮어씀). p는 dict(4-1 스키마)."""
        now = datetime.now(timezone.utc).isoformat()
        with self._conn() as c:
            c.execute("""
                INSERT INTO voice_presets(preset_id, name, one_liner, lang, archetype,
                    base_voice_id, model_id, voice_settings_json, default_speed,
                    default_silence_trim, sample_file, source_ref, origin, created_at)
                VALUES(?,?,?,?,?,?,?,?,?,?,?,?,?,?)
                ON CONFLICT(preset_id) DO UPDATE SET
                    name=excluded.name, one_liner=excluded.one_liner, lang=excluded.lang,
                    archetype=excluded.archetype, base_voice_id=excluded.base_voice_id,
                    model_id=excluded.model_id, voice_settings_json=excluded.voice_settings_json,
                    default_speed=excluded.default_speed,
                    default_silence_trim=excluded.default_silence_trim,
                    sample_file=excluded.sample_file, source_ref=excluded.source_ref,
                    origin=excluded.origin
            """, (
                p["preset_id"], p["name"], p.get("one_liner"), p.get("lang", "KR"),
                p.get("archetype"), p["base_voice_id"],
                p.get("model_id", "eleven_multilingual_v2"),
                json.dumps(p.get("voice_settings", {}), ensure_ascii=False),
                p.get("default_speed", 1.0), p.get("default_silence_trim", "off"),
                p.get("sample_file"), p.get("source_ref"), p.get("origin", "curated"), now,
            ))

    def _row_to_preset(self, r):
        return {
            "preset_id": r[0], "name": r[1], "one_liner": r[2], "lang": r[3],
            "archetype": r[4], "base_voice_id": r[5], "model_id": r[6],
            "voice_settings": json.loads(r[7]) if r[7] else {},
            "default_speed": r[8], "default_silence_trim": r[9],
            "sample_file": r[10], "source_ref": r[11], "origin": r[12], "created_at": r[13],
        }

    def get_voice_preset(self, preset_id):
        with self._conn() as c:
            r = c.execute("SELECT preset_id,name,one_liner,lang,archetype,base_voice_id,"
                          "model_id,voice_settings_json,default_speed,default_silence_trim,"
                          "sample_file,source_ref,origin,created_at FROM voice_presets "
                          "WHERE preset_id=?", (preset_id,)).fetchone()
        return self._row_to_preset(r) if r else None

    def list_voice_presets(self, lang=None):
        q = ("SELECT preset_id,name,one_liner,lang,archetype,base_voice_id,model_id,"
             "voice_settings_json,default_speed,default_silence_trim,sample_file,"
             "source_ref,origin,created_at FROM voice_presets")
        args = ()
        if lang:
            q += " WHERE lang=?"; args = (lang,)
        q += " ORDER BY created_at"
        with self._conn() as c:
            return [self._row_to_preset(r) for r in c.execute(q, args).fetchall()]
```

- [ ] **Step 4: Run to verify pass**

Run: `python -m pytest shopping_shorts/tests/test_store_voice.py -v`
Expected: PASS

- [ ] **Step 5: Commit**

```bash
git add shopping_shorts/store.py shopping_shorts/tests/test_store_voice.py
git commit -m "feat(shopping_shorts): store — voice_presets 테이블+voice_json+조회/저장 메서드"
```

---

## Task 4: voice_presets.py — JSON 로더 + DB seed

**Files:**
- Create: `shopping_shorts/voice_presets.py`
- Create: `shopping_shorts/assets/voice_presets.json` (Step 3에서)
- Test: `shopping_shorts/tests/test_voice_presets.py`

- [ ] **Step 1: Write the failing test** (`tests/test_voice_presets.py`)

```python
import json, tempfile
from pathlib import Path
from shopping_shorts import voice_presets
from shopping_shorts.store import Store


def test_load_presets_file_has_kr_presets():
    rows = voice_presets.load_presets_file()
    assert len(rows) >= 6                     # 큐레이션 최소 6개
    assert all(r.get("lang") for r in rows)
    assert all(r.get("base_voice_id") for r in rows)
    assert all("stability" in r.get("voice_settings", {}) for r in rows)


def test_seed_upserts_into_db(monkeypatch):
    fake = [{
        "preset_id": "kr-x", "name": "X", "lang": "KR", "base_voice_id": "v",
        "voice_settings": {"stability": 0.5, "similarity_boost": 0.75, "style": 0.0, "use_speaker_boost": True},
        "default_speed": 1.0, "default_silence_trim": "off", "origin": "curated",
    }]
    monkeypatch.setattr(voice_presets, "load_presets_file", lambda: fake)
    s = Store(Path(tempfile.mkdtemp()) / "t.db")
    n = voice_presets.seed_presets(s)
    assert n == 1
    assert s.get_voice_preset("kr-x")["name"] == "X"
    # 재실행 idempotent
    assert voice_presets.seed_presets(s) == 1
    assert len(s.list_voice_presets()) == 1
```

- [ ] **Step 2: Run to verify fail**

Run: `python -m pytest shopping_shorts/tests/test_voice_presets.py -v`
Expected: FAIL (`No module named 'shopping_shorts.voice_presets'`)

- [ ] **Step 3a: Create seed data** (`shopping_shorts/assets/voice_presets.json`)

> voice_id는 ElevenLabs 공개 라이브러리의 실제 voiceID로 채운다. 아래는 구조 예시(6개). 큐레이션 튜닝 시 값 조정.
> **음색 A/B는 사장님 귀 확인 필수** — 이 JSON은 Task 5 build_voice_samples로 샘플 생성 후 함께 검수.

```json
[
  {"preset_id": "kr-calm-info", "name": "슬기", "one_liner": "차분한 여성 나레이션. 정보 전달에 적합.", "lang": "KR", "archetype": "차분정보", "base_voice_id": "21m00Tcm4TlvDq8ikWAM", "model_id": "eleven_multilingual_v2", "voice_settings": {"stability": 0.68, "similarity_boost": 0.75, "style": 0.0, "use_speaker_boost": true}, "default_speed": 1.1, "default_silence_trim": "mid", "sample_file": "kr-calm-info.mp3", "source_ref": "정보전달형 벤치마크 참고", "origin": "curated"},
  {"preset_id": "kr-friendly", "name": "소라", "one_liner": "선명하고 친근한 젊은 여성. 일상 콘텐츠에 적합.", "lang": "KR", "archetype": "친근수다", "base_voice_id": "EXAVITQu4vr4xnSDxMaL", "model_id": "eleven_multilingual_v2", "voice_settings": {"stability": 0.45, "similarity_boost": 0.8, "style": 0.25, "use_speaker_boost": true}, "default_speed": 1.15, "default_silence_trim": "mid", "sample_file": "kr-friendly.mp3", "source_ref": "친근수다형 벤치마크 참고", "origin": "curated"},
  {"preset_id": "kr-hightension", "name": "애니", "one_liner": "밝고 귀여운 젊은 여성. 트렌디 콘텐츠에 적합.", "lang": "KR", "archetype": "하이텐션", "base_voice_id": "ThT5KcBeYPX3keUQqHPh", "model_id": "eleven_multilingual_v2", "voice_settings": {"stability": 0.35, "similarity_boost": 0.75, "style": 0.45, "use_speaker_boost": true}, "default_speed": 1.25, "default_silence_trim": "strong", "sample_file": "kr-hightension.mp3", "source_ref": "하이텐션형 벤치마크 참고", "origin": "curated"},
  {"preset_id": "kr-trust-male", "name": "지훈", "one_liner": "신뢰감 있는 남성. 전문·리뷰 콘텐츠에 적합.", "lang": "KR", "archetype": "신뢰전문", "base_voice_id": "onwK4e9ZLuTAKqWW03F9", "model_id": "eleven_multilingual_v2", "voice_settings": {"stability": 0.7, "similarity_boost": 0.75, "style": 0.0, "use_speaker_boost": true}, "default_speed": 1.05, "default_silence_trim": "mid", "sample_file": "kr-trust-male.mp3", "source_ref": "신뢰전문형(남) 벤치마크 참고", "origin": "curated"},
  {"preset_id": "kr-soft", "name": "혜인", "one_liner": "자연스럽고 부드러운 여성 음성.", "lang": "KR", "archetype": "차분정보", "base_voice_id": "XrExE9yKIg1WjnnlVkGX", "model_id": "eleven_multilingual_v2", "voice_settings": {"stability": 0.6, "similarity_boost": 0.78, "style": 0.1, "use_speaker_boost": true}, "default_speed": 1.1, "default_silence_trim": "mid", "sample_file": "kr-soft.mp3", "source_ref": "부드러운형 벤치마크 참고", "origin": "curated"},
  {"preset_id": "kr-energetic-male", "name": "도현", "one_liner": "활기찬 남성. 텐션 높은 소개 콘텐츠에 적합.", "lang": "KR", "archetype": "하이텐션", "base_voice_id": "TxGEqnHWrfWFTfGW9XjX", "model_id": "eleven_multilingual_v2", "voice_settings": {"stability": 0.4, "similarity_boost": 0.75, "style": 0.4, "use_speaker_boost": true}, "default_speed": 1.2, "default_silence_trim": "strong", "sample_file": "kr-energetic-male.mp3", "source_ref": "활기찬형(남) 벤치마크 참고", "origin": "curated"}
]
```

- [ ] **Step 3b: Implement** (`voice_presets.py`)

```python
"""큐레이션 보이스 프리셋(assets/voice_presets.json)을 읽어 DB로 seed한다.

파일이 소스오브트루스 — 기동 시 seed_presets로 upsert(origin=curated). 유저 생성분은
DB에만 존재(origin=generated)하며 여기서 건드리지 않는다."""
import json
from pathlib import Path

PRESETS_JSON = Path(__file__).parent / "assets" / "voice_presets.json"
SAMPLES_DIR = Path(__file__).parent / "assets" / "voice_samples"


def load_presets_file():
    """assets/voice_presets.json → list[dict]. 파일 없으면 빈 리스트."""
    if not PRESETS_JSON.exists():
        return []
    return json.loads(PRESETS_JSON.read_text(encoding="utf-8"))


def seed_presets(store):
    """큐레이션 프리셋을 DB에 upsert. 등록 건수 반환(idempotent)."""
    rows = load_presets_file()
    for p in rows:
        store.upsert_voice_preset(p)
    return len(rows)
```

- [ ] **Step 4: Run to verify pass**

Run: `python -m pytest shopping_shorts/tests/test_voice_presets.py -v`
Expected: PASS

- [ ] **Step 5: Commit**

```bash
git add shopping_shorts/voice_presets.py shopping_shorts/assets/voice_presets.json shopping_shorts/tests/test_voice_presets.py
git commit -m "feat(shopping_shorts): voice_presets — 큐레이션 JSON 로더+DB seed (KR 6종)"
```

---

## Task 5: build_voice_samples.py — 프리셋별 미리듣기 mp3 생성

**Files:**
- Create: `shopping_shorts/scripts/build_voice_samples.py`

> 테스트 없음(1회성 큐레이션 스크립트, ElevenLabs 실호출·크레딧 소모). 실행은 사장님과 함께 검수.

- [ ] **Step 1: Implement** (`scripts/build_voice_samples.py`)

```python
"""각 큐레이션 프리셋으로 공통 데모 문장을 읽어 assets/voice_samples/<sample_file>를 생성.

ElevenLabs 실호출(크레딧 소모)이라 큐레이션/튜닝 시 수동 1회 실행.
  python -m shopping_shorts.scripts.build_voice_samples
--only <preset_id> 로 특정 프리셋만 재생성 가능."""
import argparse
from shopping_shorts import voice_presets, audio_post
from shopping_shorts.tts import synthesize_tts

DEMO_TEXT = "요즘 이거 하나씩 다 들고 다니시더라고요. 왜 인기인지 제가 직접 써봤습니다."


def main():
    ap = argparse.ArgumentParser()
    ap.add_argument("--only", default=None, help="특정 preset_id만")
    args = ap.parse_args()

    voice_presets.SAMPLES_DIR.mkdir(parents=True, exist_ok=True)
    presets = voice_presets.load_presets_file()
    for p in presets:
        if args.only and p["preset_id"] != args.only:
            continue
        out = voice_presets.SAMPLES_DIR / p["sample_file"]
        speed = p.get("default_speed", 1.0)
        synthesize_tts(DEMO_TEXT, str(out), voice_id=p["base_voice_id"],
                       voice_settings=p.get("voice_settings"), speed=speed,
                       model_id=p.get("model_id"))
        # 1.2 초과 속도·무음삭제 후처리(미리듣기도 실제 출력과 동일 조건)
        extra = speed / 1.2 if speed > 1.2 else 1.0
        audio_post.post_process(str(out), str(out), tempo=extra,
                                silence_trim=p.get("default_silence_trim", "off"))
        print(f"✓ {p['preset_id']} → {out}")


if __name__ == "__main__":
    main()
```

- [ ] **Step 2: (수동) 샘플 생성 실행 — 사장님과 함께**

Run: `python -m shopping_shorts.scripts.build_voice_samples`
Expected: `assets/voice_samples/*.mp3` 6개 생성. 각 파일 재생 → 🎧 사장님이 프리셋별 느낌 확인 → JSON 튜닝 반복.

- [ ] **Step 3: Commit** (샘플 확정 후)

```bash
git add shopping_shorts/scripts/build_voice_samples.py shopping_shorts/assets/voice_samples
git commit -m "feat(shopping_shorts): 프리셋 샘플 생성 스크립트 + KR 6종 미리듣기 mp3"
```

---

## Task 6: mix_pipeline.py — 프리셋 적용 TTS + resynth_tts_job

**Files:**
- Modify: `shopping_shorts/mix_pipeline.py` (imports, `_plan_and_tts` 81-105, 새 함수)
- Test: `shopping_shorts/tests/test_mix_pipeline.py` (확장)

- [ ] **Step 1: Write the failing test** (append to `tests/test_mix_pipeline.py`)

```python
def test_resynth_tts_job_applies_voice(monkeypatch, tmp_path):
    from shopping_shorts import mix_pipeline
    from shopping_shorts.store import Store

    captured = {}
    def fake_synth(text, out, **kw):
        captured["kw"] = kw
        from pathlib import Path
        Path(out).write_bytes(b"ID3"); return out
    monkeypatch.setattr(mix_pipeline, "synthesize_tts", fake_synth)
    monkeypatch.setattr(mix_pipeline.audio_post, "post_process", lambda *a, **k: a[1])

    db = tmp_path / "t.db"; s = Store(db)
    s.create_mix_job("j1", ["u"], 30, "구조")
    s.update_mix_job("j1", edit_plan={"structure": "구조", "beats": [
        {"beat_idx": 0, "narration": "안녕", "primary": {}}]})
    s.update_mix_job("j1", voice={"voice_id": "vZ",
        "settings": {"stability": 0.4, "similarity_boost": 0.75, "style": 0.3, "use_speaker_boost": True},
        "speed": 1.3, "silence_trim": "strong"})

    mix_pipeline.resynth_tts_job("j1", str(db), str(tmp_path / "work"))

    assert captured["kw"]["voice_id"] == "vZ"
    assert captured["kw"]["voice_settings"]["style"] == 0.3
    assert captured["kw"]["speed"] == 1.3
    assert s.get_mix_job("j1")["status"] == "ready_for_review"
```

- [ ] **Step 2: Run to verify fail**

Run: `python -m pytest shopping_shorts/tests/test_mix_pipeline.py::test_resynth_tts_job_applies_voice -v`
Expected: FAIL (`module 'shopping_shorts.mix_pipeline' has no attribute 'audio_post'` 또는 `resynth_tts_job` 없음)

- [ ] **Step 3a: Implement — import + 헬퍼** (mix_pipeline.py import 블록에 추가)

```python
from shopping_shorts import audio_post
```

그리고 `_source_video_id` 근처에 헬퍼 추가:
```python
def _voice_params(voice):
    """job의 voice 스냅샷(dict|None) → (voice_id, voice_settings, speed, extra_tempo, silence_trim).
    voice 없으면 전부 기본값(config 기본 성우, 속도 1.0, 무음삭제 off)."""
    v = voice or {}
    speed = v.get("speed", 1.0)
    extra_tempo = speed / 1.2 if speed > 1.2 else 1.0  # 1.2 초과분만 atempo로
    return (v.get("voice_id"), v.get("settings"), speed, extra_tempo,
            v.get("silence_trim", "off"))
```

- [ ] **Step 3b: Implement — _plan_and_tts TTS 루프에 적용** (mix_pipeline.py 96-103의 TTS 루프 교체)

`_plan_and_tts` 시그니처에 `voice=None` 추가하고 루프를:
```python
    # 4) 비트별 TTS (프리셋 적용 + 후처리)
    store.update_mix_job(job_id, status="tts")
    tts_dir = work / "tts"
    tts_dir.mkdir(parents=True, exist_ok=True)
    voice_id, vs, speed, extra_tempo, trim = _voice_params(voice)
    for beat in plan["beats"]:
        out = tts_dir / f"beat_{beat['beat_idx']}.mp3"
        synthesize_tts(beat["narration"], str(out), voice_id=voice_id,
                       voice_settings=vs, speed=speed)
        audio_post.post_process(str(out), str(out), tempo=extra_tempo, silence_trim=trim)
        beat["tts_path"] = str(out)
```

그리고 호출부 두 곳에서 job의 voice를 넘긴다:
- `run_mix_job`의 `_plan_and_tts(...)` 호출에 `voice=job.get("voice")` 추가
- `retype_mix_job`의 `_plan_and_tts(...)` 호출에 `voice=job.get("voice")` 추가

- [ ] **Step 3c: Implement — resynth_tts_job** (mix_pipeline.py 끝에 추가)

```python
def resynth_tts_job(job_id, db_path, work_root):
    """기존 edit_plan은 그대로 두고, job의 voice 설정으로 비트별 TTS만 다시 생성한다
    (영상제작 4단계 '이 대본으로 다시 듣기'·프리셋 변경). 재다운로드·재매칭 없음."""
    store = Store(db_path)
    job = store.get_mix_job(job_id)
    if not job or not job.get("edit_plan"):
        return
    plan = job["edit_plan"]
    work = Path(work_root) / job_id
    tts_dir = work / "tts"
    tts_dir.mkdir(parents=True, exist_ok=True)
    store.update_mix_job(job_id, status="tts")
    voice_id, vs, speed, extra_tempo, trim = _voice_params(job.get("voice"))
    try:
        for beat in plan["beats"]:
            out = tts_dir / f"beat_{beat['beat_idx']}.mp3"
            synthesize_tts(beat["narration"], str(out), voice_id=voice_id,
                           voice_settings=vs, speed=speed)
            audio_post.post_process(str(out), str(out), tempo=extra_tempo, silence_trim=trim)
            beat["tts_path"] = str(out)
        store.update_mix_job(job_id, edit_plan=plan, status="ready_for_review")
    except Exception as e:
        traceback.print_exc(file=sys.stderr)
        store.update_mix_job(job_id, status="failed", error=str(e))
```

- [ ] **Step 4: Run to verify pass**

Run: `python -m pytest shopping_shorts/tests/test_mix_pipeline.py -v`
Expected: PASS (신규 + 기존 통과. 단 사전 실패였던 test_run_render_happy_path의 headcopy는 본 작업 범위 밖 — 그 케이스가 여전히 실패하면 별도 이슈로 남긴다)

- [ ] **Step 5: Commit**

```bash
git add shopping_shorts/mix_pipeline.py shopping_shorts/tests/test_mix_pipeline.py
git commit -m "feat(shopping_shorts): mix_pipeline — 비트TTS 프리셋 적용 + resynth_tts_job"
```

---

## Task 7: app.py — 프리셋 API + startup seed

**Files:**
- Modify: `shopping_shorts/app.py` (import, startup, 엔드포인트 4개)
- Test: 수동 curl(아래) — FastAPI 통합은 앱 기동 검증으로 대체

- [ ] **Step 1: Implement — startup seed** (app.py, `app = FastAPI(...)` 다음에 추가)

```python
from shopping_shorts import voice_presets

@app.on_event("startup")
def _seed_voice_presets():
    """기동 시 큐레이션 프리셋을 DB로 upsert(idempotent)."""
    try:
        voice_presets.seed_presets(Store(DB_PATH))
    except Exception:
        pass  # seed 실패가 앱 기동을 막지 않게
```

- [ ] **Step 2: Implement — 엔드포인트** (app.py, `/api/mix/tts` 근처에 추가)

```python
@app.get("/api/voice-presets")
def api_voice_presets(lang: str = "KR"):
    """프리셋 목록(유저 노출용 — source_ref는 내부 전용이라 제외)."""
    rows = Store(DB_PATH).list_voice_presets(lang=lang)
    out = []
    for p in rows:
        out.append({
            "preset_id": p["preset_id"], "name": p["name"], "one_liner": p["one_liner"],
            "lang": p["lang"], "archetype": p["archetype"],
            "voice_settings": p["voice_settings"], "default_speed": p["default_speed"],
            "default_silence_trim": p["default_silence_trim"],
            "sample_url": f"/api/voice-presets/{p['preset_id']}/sample" if p["sample_file"] else None,
        })
    return {"ok": True, "presets": out}


@app.get("/api/voice-presets/{preset_id}/sample")
def api_voice_preset_sample(preset_id: str):
    p = Store(DB_PATH).get_voice_preset(preset_id)
    if not p or not p.get("sample_file"):
        return JSONResponse(status_code=404, content={"ok": False})
    f = voice_presets.SAMPLES_DIR / p["sample_file"]
    if not f.exists():
        return JSONResponse(status_code=404, content={"ok": False})
    return FileResponse(str(f), media_type="audio/mpeg")


@app.post("/api/mix/voice")
def api_mix_voice(background_tasks: BackgroundTasks, body: dict):
    """프리셋 선택을 job에 스냅샷 저장 후 기존 plan에 대해 TTS 재생성(백그라운드).
    body: {job_id, preset_id?, voice_id, settings, speed, silence_trim}."""
    job_id = body.get("job_id")
    store = Store(DB_PATH)
    job = store.get_mix_job(job_id)
    if not job or not job.get("edit_plan"):
        return JSONResponse(status_code=404, content={"ok": False, "error": "job/plan 없음"})
    voice = {
        "preset_id": body.get("preset_id"),
        "voice_id": body.get("voice_id"),
        "settings": body.get("settings"),
        "speed": body.get("speed", 1.0),
        "silence_trim": body.get("silence_trim", "off"),
    }
    store.update_mix_job(job_id, voice=voice)
    background_tasks.add_task(resynth_tts_job, job_id, DB_PATH, _MIX_WORK_DIR)
    return {"ok": True}


@app.post("/api/mix/voice/preview")
def api_mix_voice_preview(body: dict):
    """첫 비트만 주어진 설정으로 즉시 합성해 미리듣기 mp3 반환('이 대본으로 다시 듣기').
    전체 재생성보다 크레딧 절약."""
    job_id = body.get("job_id")
    job = Store(DB_PATH).get_mix_job(job_id)
    if not job or not job.get("edit_plan") or not job["edit_plan"].get("beats"):
        return JSONResponse(status_code=404, content={"ok": False, "error": "plan 없음"})
    beat0 = job["edit_plan"]["beats"][0]
    speed = body.get("speed", 1.0)
    extra = speed / 1.2 if speed > 1.2 else 1.0
    d = _MIX_WORK_DIR / job_id / "tts"
    d.mkdir(parents=True, exist_ok=True)
    out = d / "preview.mp3"
    synthesize_tts(beat0["narration"], str(out), voice_id=body.get("voice_id"),
                   voice_settings=body.get("settings"), speed=speed)
    audio_post.post_process(str(out), str(out), tempo=extra, silence_trim=body.get("silence_trim", "off"))
    return FileResponse(str(out), media_type="audio/mpeg")
```

import 라인 정리: 파일 상단 `from shopping_shorts.mix_pipeline import run_mix_job, run_render, retype_mix_job, _source_video_id` 에 `resynth_tts_job` 추가하고, `from shopping_shorts import audio_post` 추가.

- [ ] **Step 3: Verify — 앱 기동 + curl**

```bash
python -c "import shopping_shorts.app"   # import 에러 없어야 함
python -m uvicorn shopping_shorts.app:app --port 8848 &
sleep 3
curl -s "http://127.0.0.1:8848/api/voice-presets?lang=KR" | python -m json.tool | head -30
```
Expected: `{"ok": true, "presets": [ ... 6개, source_ref 없음, sample_url 채워짐 ...]}`

- [ ] **Step 4: Commit**

```bash
git add shopping_shorts/app.py
git commit -m "feat(shopping_shorts): app — 보이스 프리셋 API(목록/샘플/선택/프리뷰)+startup seed"
```

---

## Task 8: produce.html — 4단계 프리셋 선택 UI

**Files:**
- Modify: `shopping_shorts/static/produce.html` (step 3 패널 118-122, JS loadTtsBeats 601-613 교체/보강)
- Test: 수동 브라우저 확인(verify 스킬)

- [ ] **Step 1: Implement — 패널 마크업** (produce.html `data-step="3"` 섹션 교체)

```html
    <section class="panel" data-step="3">
      <h3>4 · TTS 음성</h3>
      <div class="sub">성우 프리셋을 고르면 톤·속도·감도가 한 번에 세팅됩니다. 들어보고 다음으로.</div>
      <label style="display:flex;align-items:center;gap:8px;margin:8px 0">
        <input type="checkbox" id="noVoice" onchange="onNoVoice()"> <b>자막/음성 없이 진행</b>
        <span class="hint">켜면 TTS·자막 없이 영상만 내보냅니다</span>
      </label>
      <div id="voiceUI">
        <div id="presetCards">불러오는 중…</div>
        <div id="voiceControls" style="display:none;margin-top:12px">
          <div style="font-size:12px;color:var(--sub)">속도</div>
          <div id="speedBtns" style="display:flex;gap:6px;flex-wrap:wrap;margin:4px 0 10px"></div>
          <div style="font-size:12px;color:var(--sub)">무음 삭제</div>
          <div id="silenceBtns" style="display:flex;gap:6px;margin:4px 0 10px"></div>
          <details style="margin:8px 0">
            <summary style="cursor:pointer;font-size:13px;color:var(--sub)">고급 ▾</summary>
            <div style="padding:8px 0;display:flex;flex-direction:column;gap:8px">
              <label>Stability <input type="range" id="advStability" min="0" max="1" step="0.05" oninput="onAdv()"><span id="advStabilityV"></span></label>
              <label>Similarity <input type="range" id="advSimilarity" min="0" max="1" step="0.05" oninput="onAdv()"><span id="advSimilarityV"></span></label>
              <label>Style <input type="range" id="advStyle" min="0" max="1" step="0.05" oninput="onAdv()"><span id="advStyleV"></span></label>
              <label><input type="checkbox" id="advBoost" onchange="onAdv()"> Speaker boost</label>
            </div>
          </details>
          <button onclick="previewVoice()" class="btn" style="margin-right:8px">🎧 이 대본으로 다시 듣기</button>
          <button onclick="applyVoice()" class="btn primary">이 음성으로 전체 생성</button>
          <div id="voicePreview" style="margin-top:8px"></div>
        </div>
      </div>
      <div id="ttsBeats" style="margin-top:14px"></div>
    </section>
```

- [ ] **Step 2: Implement — JS** (produce.html, 기존 `loadTtsBeats` 교체 + 상태/핸들러 추가)

```javascript
// ── 4단계 보이스 프리셋 ──────────────
let VOICE = { preset_id:null, voice_id:null, settings:null, speed:1.0, silence_trim:'off' };
const SPEEDS=[0.8,0.9,1.0,1.1,1.2,1.3,1.4,1.5];
const SIL=[['off','OFF'],['weak','약'],['mid','중'],['strong','강']];

async function loadTtsBeats(){
  if(!MIX_JOB){ document.getElementById('presetCards').innerHTML='<div class="hint">⚠️ 2단계에서 영상 매칭을 먼저 실행하세요.</div>'; return; }
  const d=await (await fetch('/api/voice-presets?lang=KR')).json();
  const cards=(d.presets||[]).map(p=>`
    <div class="preset ${VOICE.preset_id===p.preset_id?'sel':''}" onclick='pickPreset(${JSON.stringify(p)})'
      style="border:1px solid ${VOICE.preset_id===p.preset_id?'var(--accent2)':'var(--line)'};border-radius:9px;padding:9px;margin-bottom:6px;cursor:pointer">
      <b>${esc(p.name)}</b> — ${esc(p.one_liner||'')}
      ${p.sample_url?`<audio controls src="${esc(p.sample_url)}" style="height:26px;vertical-align:middle;margin-left:8px"></audio>`:''}
    </div>`).join('');
  document.getElementById('presetCards').innerHTML=cards||'<div class="hint">프리셋이 없습니다.</div>';
  renderTtsBeats();
}

function pickPreset(p){
  VOICE={preset_id:p.preset_id, voice_id:p.base_voice_id||p.voice_id,
         settings:Object.assign({}, p.voice_settings), speed:p.default_speed,
         silence_trim:p.default_silence_trim};
  document.getElementById('voiceControls').style.display='block';
  document.getElementById('speedBtns').innerHTML=SPEEDS.map(s=>`<button onclick="setSpeed(${s})" class="btn ${VOICE.speed===s?'primary':''}">${s}x</button>`).join('');
  document.getElementById('silenceBtns').innerHTML=SIL.map(([v,l])=>`<button onclick="setSil('${v}')" class="btn ${VOICE.silence_trim===v?'primary':''}">${l}</button>`).join('');
  const vs=VOICE.settings||{};
  advStability.value=vs.stability??0.5; advSimilarity.value=vs.similarity_boost??0.75;
  advStyle.value=vs.style??0; advBoost.checked=vs.use_speaker_boost!==false;
  syncAdvLabels();
  loadTtsBeats();  // 카드 선택 하이라이트 갱신
}
function setSpeed(s){ VOICE.speed=s; pickHighlight(); }
function setSil(v){ VOICE.silence_trim=v; pickHighlight(); }
function pickHighlight(){
  document.getElementById('speedBtns').innerHTML=SPEEDS.map(s=>`<button onclick="setSpeed(${s})" class="btn ${VOICE.speed===s?'primary':''}">${s}x</button>`).join('');
  document.getElementById('silenceBtns').innerHTML=SIL.map(([v,l])=>`<button onclick="setSil('${v}')" class="btn ${VOICE.silence_trim===v?'primary':''}">${l}</button>`).join('');
}
function syncAdvLabels(){
  advStabilityV.textContent=(+advStability.value).toFixed(2);
  advSimilarityV.textContent=(+advSimilarity.value).toFixed(2);
  advStyleV.textContent=(+advStyle.value).toFixed(2);
}
function onAdv(){
  VOICE.settings={stability:+advStability.value, similarity_boost:+advSimilarity.value,
                  style:+advStyle.value, use_speaker_boost:advBoost.checked};
  syncAdvLabels();
}
function onNoVoice(){
  const off=document.getElementById('noVoice').checked;
  document.getElementById('voiceUI').style.opacity=off?0.4:1;
  STATE.noVoice=off;
}
async function previewVoice(){
  const el=document.getElementById('voicePreview'); el.textContent='⏳ 생성 중…';
  const r=await fetch('/api/mix/voice/preview',{method:'POST',headers:{'Content-Type':'application/json'},
    body:JSON.stringify({job_id:MIX_JOB, voice_id:VOICE.voice_id, settings:VOICE.settings, speed:VOICE.speed, silence_trim:VOICE.silence_trim})});
  if(!r.ok){ el.textContent='❌ 실패'; return; }
  const b=await r.blob();
  el.innerHTML=`<audio controls autoplay src="${URL.createObjectURL(b)}" style="height:30px"></audio>`;
}
async function applyVoice(){
  const el=document.getElementById('voicePreview'); el.textContent='⏳ 전체 음성 생성 중…';
  await fetch('/api/mix/voice',{method:'POST',headers:{'Content-Type':'application/json'},
    body:JSON.stringify({job_id:MIX_JOB, preset_id:VOICE.preset_id, voice_id:VOICE.voice_id, settings:VOICE.settings, speed:VOICE.speed, silence_trim:VOICE.silence_trim})});
  const poll=setInterval(async()=>{
    const d=await (await fetch('/api/mix/status/'+MIX_JOB)).json();
    if(d.status==='ready_for_review'){ clearInterval(poll); el.textContent='✅ 완료'; renderTtsBeats(); }
    else if(d.status==='failed'){ clearInterval(poll); el.textContent='❌ 실패: '+(d.error||''); }
  },2500);
}
async function renderTtsBeats(){
  const el=document.getElementById('ttsBeats');
  const d=await (await fetch('/api/mix/result/'+MIX_JOB)).json();
  if(!d.ok || !(d.beats||[]).length){ el.innerHTML=''; return; }
  el.innerHTML='<div style="font-size:12px;color:var(--sub);margin-bottom:4px">비트별 음성</div>'+d.beats.map((b,i)=>`
    <div style="border:1px solid var(--line);border-radius:9px;padding:9px;margin-bottom:6px;background:#0d1420">
      <div style="font-size:13px;margin-bottom:4px"><b>${i+1}.</b> ${esc(b.narration||'')}</div>
      ${b.tts_preview_url?`<audio controls src="${esc(b.tts_preview_url)}?t=${Date.now()}" style="height:26px"></audio>`:'<span class="hint">음성 없음</span>'}
    </div>`).join('');
}
```

> `?t=${Date.now()}` 캐시버스터 — 같은 URL로 재생성된 mp3를 브라우저가 캐시된 옛 음성으로 재생하지 않도록.

- [ ] **Step 3: Verify — 브라우저 확인** (verify 스킬)

Run: 앱 기동 후 produce에서 job 하나 4단계까지 진행 → 프리셋 카드 6개·샘플 재생 → 프리셋 선택 시 속도/무음/고급 노출 → "다시 듣기" 미리듣기 재생 → "전체 생성" 후 비트별 음성 갱신 확인.

- [ ] **Step 4: Commit**

```bash
git add shopping_shorts/static/produce.html
git commit -m "feat(shopping_shorts): produce 4단계 보이스 프리셋 선택 UI(카드·속도·무음·고급)"
```

---

## Self-Review 결과 (작성자 점검)

- **스펙 커버리지**: §4 데이터모델→T3/T4, §5 저장배포→T4/T7(seed), §6 튜닝절차→T5(수동)+Phase2 이월, §7 코드변경→T1/T2/T6/T7, §8 UI→T8, §10 테스트→각 T. 측정유틸(§10)은 스펙 §9에서 Phase2로 명시 → 이번 계획 제외(정합).
- **speed**: 문서 실측(0.7~1.2) 반영 — T1 clamp + T2 atempo 보정.
- **타입 일관**: `voice` 스냅샷 dict 키(preset_id/voice_id/settings/speed/silence_trim)를 store·mix_pipeline·app·produce 전 구간 동일 사용. `_voice_params` 반환 5튜플 일관.
- **워킹트리 미커밋**: 커밋 시 기능 파일만 add(§배포규칙). test_run_render_happy_path(headcopy) 사전실패는 범위 밖으로 명시.

## 미해결(구현 중 조정 — 사용자 "진행 보고 수정")
- 큐레이션 voice_id 실제값·개수(6→8) 및 감도값은 T5 샘플 검수에서 사장님 귀로 튜닝
- 서버 배포 시 `/etc/shopping-shorts.env`에 `ELEVENLABS_API_KEY` 추가 필요(별도)
