# 장면꾸미기타임아웃 — 렌더 시간 상한을 프레임 수 비례로

## 2026-09-17 (TheRose PC)
- 제보: 김성현님(cid352) 최종렌더가 안 됨. 자막제거는 2분(basic)에 정상. 진짜는 렌더 3연속 시간초과.
- 원인: scene_style.render_layers가 node render_scene_style.js를 timeout=240 고정으로 부른다. 워터마크 motion=float(a051564b3, 09-13)이면 JS가 **매 프레임** PNG를 찍는다. 김성현 job eeb35a6e9878 = 33장면·892프레임 → 약 0.25초/프레임이라 240초 안에 못 끝남. 14:25·14:37·14:49 3번 다 TimeoutExpired.
- 조치: timeout = max(240, 120 + 장면수×10 + 프레임×0.8). 테스트 2건(test_scene_style_timeout.py).
- ⏭ 배포 뒤 김성현 job 재렌더 성공 실측. 근본 개선은 JS 쪽 속도(프레임당 스크린샷 대신 CSS 애니메이션을 ffmpeg 오버레이로) — 별 트랙.
