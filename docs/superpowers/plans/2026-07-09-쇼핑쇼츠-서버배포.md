# 쇼핑쇼츠 서버 배포 + 도메인 + 로그인 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** `shopping_shorts/`를 Lightsail 서버(3.39.179.148)에 stockbrain과 완전 분리된
새 systemd 서비스 + nginx + DuckDNS 서브도메인 + 공유 로그인으로 상시 배포한다.

**Architecture:** stockbrain(dashboard/server.py, :8090)이 이미 쓰고 있는 HMAC 쿠키
공유-비밀번호 로그인 패턴을 shopping_shorts/app.py로 포팅하고, 같은 서버에 포트만
다른(:8849) 별도 systemd 유닛 + nginx server 블록 + DuckDNS 서브도메인을 새로 만든다.
자동배포는 기존 `deploy/auto_deploy.sh` 크론을 확장해 재사용한다.

**Tech Stack:** FastAPI, uvicorn, systemd, nginx, certbot, DuckDNS, pytest

## Global Constraints

- 서버: `ubuntu@3.39.179.148`, repo 경로 `/home/ubuntu/lotto-stock-wiki`, SSH키
  `C:\Users\TheRose\crawling_bot_client\LightsailDefaultKey-ap-northeast-2.pem` (user=ubuntu)
- shopping-shorts 포트: **8849** (stockbrain 8090과 분리, 로컬 개발용 8848과도 분리)
- 도메인: **shoppingshorts.duckdns.org** (신규, stockbrain.duckdns.org와 별개)
- 로그인: 공유 계정 1개 (직원별 계정 없음), 수집 버튼 추가 제한 없음 — 스코프 밖
- 비밀 값(`DASH_PASS`, `DASH_SECRET`, `APIFY_TOKEN`~`APIFY_TOKEN_4`, `SHORTS_EXCEL`)은
  git에 절대 커밋하지 않는다 — `/etc/shopping-shorts.env`에만 저장
- `.gitattributes`(eol=lf) 위반 방지: 새 텍스트 파일은 LF로 저장

---

### Task 1: `/healthz` 엔드포인트 추가

**Files:**
- Modify: `shopping_shorts/app.py`
- Test: `shopping_shorts/tests/test_app.py` (신규)

**Interfaces:**
- Produces: `GET /healthz` → `{"ok": true}` (nginx/systemd 헬스체크, Task 2 인증 예외 목록에서 사용)

- [ ] **Step 1: Write the failing test**

`shopping_shorts/tests/test_app.py` 신규 생성:

```python
from fastapi.testclient import TestClient
from shopping_shorts.app import app


def test_healthz():
    client = TestClient(app)
    r = client.get("/healthz")
    assert r.status_code == 200
    assert r.json() == {"ok": True}
```

- [ ] **Step 2: Run test to verify it fails**

Run: `python -m pytest shopping_shorts/tests/test_app.py -q`
Expected: FAIL (404, `/healthz` 없음)

- [ ] **Step 3: Write minimal implementation**

`shopping_shorts/app.py`에 `api_thumb` 함수 아래, 정적 마운트(`app.mount(...)`) 코드 **위에** 추가:

```python
@app.get("/healthz")
def api_healthz():
    return {"ok": True}
```

- [ ] **Step 4: Run test to verify it passes**

Run: `python -m pytest shopping_shorts/tests/test_app.py -q`
Expected: `1 passed`

- [ ] **Step 5: Commit**

```bash
git add shopping_shorts/app.py shopping_shorts/tests/test_app.py
git commit -m "feat(shopping_shorts): /healthz 엔드포인트 추가"
```

---

### Task 2: 공유 비밀번호 로그인 게이트 포팅

**Files:**
- Modify: `shopping_shorts/app.py`
- Test: `shopping_shorts/tests/test_app.py`

**Interfaces:**
- Consumes: 없음 (Task 1과 독립적으로 동작, 같은 파일만 공유)
- Produces:
  - 환경변수 `DASH_USER`(기본 `"admin"`), `DASH_PASS`(기본 `""` → 인증 OFF), `DASH_SECRET`
  - `GET /login` → 로그인 폼 HTML
  - `POST /api/login` (form body `user`, `pass`) → 성공 시 303 리다이렉트 + `dash_auth` 쿠키, 실패 시 `/login?e=1`
  - HTTP 미들웨어: 쿠키 없으면 API는 401 JSON, 페이지는 `/login` 리다이렉트. `DASH_PASS` 비어있으면 전체 우회(로컬 개발)

- [ ] **Step 1: Write the failing tests**

`shopping_shorts/tests/test_app.py`에 추가 (기존 `test_healthz` 아래):

```python
import shopping_shorts.app as app_module
from fastapi.testclient import TestClient


def _client_with_auth(monkeypatch, password="secret123"):
    monkeypatch.setattr(app_module, "DASH_PASS", password)
    monkeypatch.setattr(app_module, "DASH_SECRET", "test-secret")
    monkeypatch.setattr(app_module, "_AUTH_ON", True)
    return TestClient(app_module.app)


def test_auth_off_by_default(monkeypatch):
    """DASH_PASS 비어있으면(로컬 개발) 인증 없이 통과."""
    monkeypatch.setattr(app_module, "_AUTH_ON", False)
    client = TestClient(app_module.app)
    r = client.get("/api/reference")
    assert r.status_code == 200


def test_unauthenticated_api_returns_401(monkeypatch):
    client = _client_with_auth(monkeypatch)
    r = client.get("/api/reference")
    assert r.status_code == 401


def test_unauthenticated_page_redirects_to_login(monkeypatch):
    client = _client_with_auth(monkeypatch)
    r = client.get("/", follow_redirects=False)
    assert r.status_code in (302, 303, 307)
    assert r.headers["location"] == "/login"


def test_login_wrong_password_redirects_with_error(monkeypatch):
    client = _client_with_auth(monkeypatch)
    r = client.post("/api/login", data={"user": "admin", "pass": "wrong"},
                     follow_redirects=False)
    assert r.status_code == 303
    assert r.headers["location"] == "/login?e=1"


def test_login_correct_password_sets_cookie_and_grants_access(monkeypatch):
    client = _client_with_auth(monkeypatch)
    r = client.post("/api/login", data={"user": "admin", "pass": "secret123"},
                     follow_redirects=False)
    assert r.status_code == 303
    assert "dash_auth" in r.cookies
    r2 = client.get("/api/reference")
    assert r2.status_code == 200
```

- [ ] **Step 2: Run tests to verify they fail**

Run: `python -m pytest shopping_shorts/tests/test_app.py -q`
Expected: `test_healthz` PASS (Task 1에서 이미 구현), 새 5개 테스트는 FAIL (로그인 게이트 없음 —
`_AUTH_ON`/`DASH_PASS` 등 속성 자체가 없어서 `AttributeError`)

- [ ] **Step 3: Write minimal implementation**

`shopping_shorts/app.py` 상단 import 블록을 교체:

```python
"""FastAPI: 수집 API + 정적 프론트 서빙."""
import hmac
import hashlib
import os
import urllib.parse
from pathlib import Path
from fastapi import FastAPI, Request
from fastapi.responses import JSONResponse, HTMLResponse, RedirectResponse
from fastapi.staticfiles import StaticFiles
from shopping_shorts.service import collect
from shopping_shorts.outreach import build_queue
from shopping_shorts.store import Store
from shopping_shorts.config import DB_PATH

app = FastAPI(title="쇼핑쇼츠 레퍼런스 랭킹")
```

`app.mount("/", StaticFiles(...))` **바로 위**(파일 맨 끝)에 로그인 게이트를 통째로 추가
(dashboard/server.py의 검증된 패턴 그대로 포팅 — `DASH_USER` 부재 시 기본값만 다름 없음):

```python
# ── 로그인 게이트 (공유 계정, dashboard/server.py 패턴 포팅) ──
DASH_USER = os.environ.get("DASH_USER", "admin")
DASH_PASS = os.environ.get("DASH_PASS", "")  # 비어있으면 인증 OFF(로컬 개발)
DASH_SECRET = os.environ.get("DASH_SECRET", "shopping-shorts-local-secret")
_AUTH_ON = bool(DASH_PASS)
_AUTH_ALLOW = ("/login", "/api/login", "/favicon.ico", "/healthz")


def _auth_token() -> str:
    return hmac.new(DASH_SECRET.encode(), f"{DASH_USER}:{DASH_PASS}".encode(),
                     hashlib.sha256).hexdigest()


_LOGIN_HTML = """<!doctype html><html lang=ko><head><meta charset=utf-8>
<meta name=viewport content="width=device-width,initial-scale=1"><title>쇼핑쇼츠 로그인</title>
<style>body{margin:0;height:100vh;display:flex;align-items:center;justify-content:center;
background:#0b0b0e;font-family:system-ui,'Noto Sans KR',sans-serif}
.box{background:#16161c;border:1px solid #2a2a30;border-radius:14px;padding:32px 28px;width:280px}
h1{color:#4f9dfa;font-size:18px;margin:0 0 18px;text-align:center;letter-spacing:1px}
input{width:100%;box-sizing:border-box;margin:6px 0;padding:11px 12px;background:#0e0e12;
border:1px solid #333;border-radius:8px;color:#eee;font-size:14px}
button{width:100%;margin-top:12px;padding:11px;background:#4f9dfa;color:#111;border:0;
border-radius:8px;font-weight:700;font-size:14px;cursor:pointer}
.err{color:#e74c3c;font-size:12px;text-align:center;margin-top:10px;min-height:14px}</style></head>
<body><form class=box method=post action=/api/login>
<h1>🛍️ 쇼핑쇼츠</h1>
<input name=user placeholder=아이디 autocomplete=username autofocus>
<input name=pass type=password placeholder=비밀번호 autocomplete=current-password>
<button>로그인</button>
<div class=err>__ERR__</div></form></body></html>"""


@app.get("/login", response_class=HTMLResponse)
def _login_page(e: str = ""):
    return _LOGIN_HTML.replace("__ERR__", "아이디 또는 비밀번호가 틀렸습니다" if e else "")


@app.post("/api/login")
async def _api_login(req: Request):
    body = (await req.body()).decode("utf-8", "ignore")
    form = urllib.parse.parse_qs(body)
    u = (form.get("user") or [""])[0]
    p = (form.get("pass") or [""])[0]
    if _AUTH_ON and u == DASH_USER and p == DASH_PASS:
        r = RedirectResponse("/", status_code=303)
        r.set_cookie("dash_auth", _auth_token(), max_age=60 * 60 * 24 * 30,
                     httponly=True, samesite="lax")
        return r
    return RedirectResponse("/login?e=1", status_code=303)


@app.middleware("http")
async def _auth_guard(request: Request, call_next):
    if not _AUTH_ON:
        return await call_next(request)
    path = request.url.path
    if path in _AUTH_ALLOW or path.startswith("/static"):
        return await call_next(request)
    if request.cookies.get("dash_auth") == _auth_token():
        return await call_next(request)
    if path.startswith("/api/"):
        return JSONResponse({"error": "unauthorized"}, status_code=401)
    return RedirectResponse("/login")
```

**주의**: `_AUTH_ON`은 모듈 로드 시 한 번 계산되는 전역 변수라서, 테스트에서
`monkeypatch.setattr(app_module, "_AUTH_ON", True)`로 직접 덮어써야 미들웨어 안의
`if not _AUTH_ON:` 분기가 테스트 값을 본다(closure가 아니라 매 요청마다 모듈 전역을
다시 읽으므로 monkeypatch가 정확히 적용됨).

- [ ] **Step 4: Run tests to verify they pass**

Run: `python -m pytest shopping_shorts/tests/test_app.py -q`
Expected: `6 passed`

- [ ] **Step 5: Commit**

```bash
git add shopping_shorts/app.py shopping_shorts/tests/test_app.py
git commit -m "feat(shopping_shorts): 공유 비밀번호 로그인 게이트 포팅 (dashboard/server.py 패턴)"
```

---

### Task 3: 배포 설정 파일 3종 작성 (nginx / systemd / env 템플릿)

**Files:**
- Create: `deploy/nginx-shoppingshorts.conf`
- Create: `deploy/shopping-shorts.service`
- Create: `deploy/shopping-shorts.env.example`

**Interfaces:**
- Consumes: Task 2에서 만든 `DASH_USER`/`DASH_PASS`/`DASH_SECRET` 환경변수 이름,
  기존 `shopping_shorts/config.py`의 `SHORTS_EXCEL`/`APIFY_TOKEN`~`APIFY_TOKEN_4`
- Produces: Task 5(서버 반영)에서 그대로 서버에 복사해 쓸 3개 파일

- [ ] **Step 1: `deploy/nginx-shoppingshorts.conf` 작성** (`deploy/nginx-stockbrain.conf` 복제, 포트/도메인만 교체)

```nginx
# /etc/nginx/sites-available/shopping-shorts → sites-enabled 에 심볼릭
# 80 포트만 먼저. certbot --nginx 실행하면 443(SSL) 자동 추가됨.
server {
    listen 80;
    server_name shoppingshorts.duckdns.org;

    client_max_body_size 20m;

    location / {
        proxy_pass http://127.0.0.1:8849;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_read_timeout 900s;
    }
}
```

(`proxy_read_timeout`을 900s로: Task2 배경의 443채널 수집 API가 최대 15분까지 걸릴
수 있어 nginx가 먼저 끊지 않도록 — Apify 폴링 타임아웃(`fetch_reels`의 `timeout=900`)과
맞춤. stockbrain의 SSE용 `proxy_buffering off`는 여기선 불필요라 제외.)

- [ ] **Step 2: `deploy/shopping-shorts.service` 작성** (`deploy/stockbrain.service` 복제)

```ini
[Unit]
Description=Shopping Shorts Reference Ranking (FastAPI :8849)
After=network.target

[Service]
Type=simple
User=ubuntu
WorkingDirectory=/home/ubuntu/lotto-stock-wiki
EnvironmentFile=/etc/shopping-shorts.env
ExecStart=/usr/bin/python3 -m uvicorn shopping_shorts.app:app --host 127.0.0.1 --port 8849
Restart=always
RestartSec=5

[Install]
WantedBy=multi-user.target
```

- [ ] **Step 3: `deploy/shopping-shorts.env.example` 작성**

```bash
# sudo cp deploy/shopping-shorts.env.example /etc/shopping-shorts.env 후 실제 값으로 채울 것
DASH_USER=admin
DASH_PASS=여기에_원하는_비번
DASH_SECRET=openssl_rand_hex_32_로_생성한_값

# raw/report_q 파이프라인과 무관 — 쇼핑쇼츠 전용 채널 리스트 엑셀 경로.
# 서버에는 scp로 직접 업로드 후 그 경로를 적는다 (Task 5 참고).
SHORTS_EXCEL=/home/ubuntu/lotto-stock-wiki/shopping_shorts/data/벤치마킹시트.xlsx

# Apify 4계정 로테이션 (2026-07-09 구현)
APIFY_TOKEN=
APIFY_TOKEN_2=
APIFY_TOKEN_3=
APIFY_TOKEN_4=
```

- [ ] **Step 4: 파일 3개 생성 확인 및 문법 셀프체크**

Run: `python -c "import configparser" ` (systemd unit은 ini 유사 포맷 — 로컬에 nginx/systemd가
없으므로 실제 `nginx -t`/`systemd-analyze verify`는 Task 5에서 서버 SSH로 검증한다)

로컬에서는 세 파일 모두 다음으로 육안 검증:
```bash
cat deploy/nginx-shoppingshorts.conf deploy/shopping-shorts.service deploy/shopping-shorts.env.example
```
Expected: `server_name shoppingshorts.duckdns.org`, `proxy_pass http://127.0.0.1:8849`,
`ExecStart=...--port 8849`, `EnvironmentFile=/etc/shopping-shorts.env` 가 정확히 일치하는지 확인

- [ ] **Step 5: Commit**

```bash
git add deploy/nginx-shoppingshorts.conf deploy/shopping-shorts.service deploy/shopping-shorts.env.example
git commit -m "chore(deploy): 쇼핑쇼츠 nginx/systemd/env 템플릿 추가"
```

---

### Task 4: `auto_deploy.sh`가 shopping-shorts도 재시작하도록 확장

**Files:**
- Modify: `deploy/auto_deploy.sh`

**Interfaces:**
- Consumes: Task 3의 systemd 유닛 이름 `shopping-shorts`
- Produces: `shopping_shorts/` 경로 변경 감지 시 자동 재시작 (stockbrain 재시작 로직과 병렬)

- [ ] **Step 1: 현재 로직 확인**

`deploy/auto_deploy.sh`의 재시작 분기(17~21행)는 `.py|.html|.js|.css` 확장자 또는
`^dashboard/`/`^scripts/` 경로 변경 시에만 `stockbrain`을 재시작한다. `shopping_shorts/`
경로는 감지 대상이 아니므로, shopping_shorts 코드를 push해도 서버가 재시작 안 함
(stockbrain도 불필요하게 재시작되지 않는 것도 확인 — 지금은 shopping_shorts 변경이
`.py` 확장자라 stockbrain 재시작 조건에 걸려버리는 게 실제 버그: 무관한 서비스가 재시작됨).

- [ ] **Step 2: 스크립트 수정**

`deploy/auto_deploy.sh`의 15~21행을 교체:

```bash
if git pull --ff-only origin main >>"$LOG" 2>&1; then
  CHANGED=$(git diff --name-only "$LOCAL" "$REMOTE")
  if echo "$CHANGED" | grep -qE '^dashboard/|^scripts/'; then
    sudo systemctl restart stockbrain >>"$LOG" 2>&1 && echo "$(date '+%F %T') stockbrain 재시작완료 $(git rev-parse --short HEAD)" >>"$LOG"
  fi
  if echo "$CHANGED" | grep -qE '^shopping_shorts/'; then
    sudo systemctl restart shopping-shorts >>"$LOG" 2>&1 && echo "$(date '+%F %T') shopping-shorts 재시작완료 $(git rev-parse --short HEAD)" >>"$LOG"
  fi
  if ! echo "$CHANGED" | grep -qE '^dashboard/|^scripts/|^shopping_shorts/'; then
    echo "$(date '+%F %T') 코드변경없음(데이터/문서만) 재시작생략 $(git rev-parse --short HEAD)" >>"$LOG"
  fi
else
  echo "$(date '+%F %T') pull실패(작업트리충돌?) 스킵-수동확인필요" >>"$LOG"
fi
```

(기존에 `.py|.html|.js|.css` 전체를 stockbrain 재시작 조건에 넣었던 것 자체가
shopping_shorts 코드 변경 때도 stockbrain을 불필요하게 재시작시키는 버그였음 —
`^dashboard/`/`^scripts/` 경로 조건으로 좁혀서 같이 고침.)

- [ ] **Step 3: 스크립트 문법 검증**

Run: `bash -n deploy/auto_deploy.sh`
Expected: 출력 없음 (문법 오류 없음)

- [ ] **Step 4: 로컬 dry-run으로 grep 분기 확인**

```bash
echo -e "shopping_shorts/app.py\ndashboard/server.py\nwiki/log.md" | grep -qE '^shopping_shorts/' && echo "SHOPPING_MATCH_OK"
echo -e "shopping_shorts/app.py\ndashboard/server.py\nwiki/log.md" | grep -qE '^dashboard/|^scripts/' && echo "DASHBOARD_MATCH_OK"
echo -e "wiki/log.md" | grep -qE '^dashboard/|^scripts/|^shopping_shorts/' || echo "DATA_ONLY_NO_RESTART_OK"
```

Expected: 세 줄 모두 출력됨 (`SHOPPING_MATCH_OK`, `DASHBOARD_MATCH_OK`, `DATA_ONLY_NO_RESTART_OK`)

- [ ] **Step 5: Commit**

```bash
git add deploy/auto_deploy.sh
git commit -m "fix(deploy): shopping_shorts 변경 시 stockbrain 대신 shopping-shorts만 재시작"
```

---

### Task 5: 서버 반영 (SSH 실행) + DuckDNS + SSL

**Files:** 없음 (서버 측 명령 실행 — SSH로 직접 수행)

**Interfaces:**
- Consumes: Task 1~4에서 push된 커밋 전부, `C:\Users\TheRose\crawling_bot_client\LightsailDefaultKey-ap-northeast-2.pem`
- Produces: `https://shoppingshorts.duckdns.org` 실서비스

- [ ] **Step 1: DuckDNS 서브도메인 등록 (사용자 수동 — 브라우저 로그인 필요, 대행 불가)**

사용자가 직접: https://www.duckdns.org 로그인(구글) → `shoppingshorts` 도메인 add →
current ip에 `3.39.179.148` 입력 → update.

확인 (에이전트가 실행):
```bash
nslookup shoppingshorts.duckdns.org
```
Expected: `3.39.179.148` 반환. 실패 시 Step 1을 사용자가 아직 안 했다는 뜻 — 여기서 멈추고 확인 요청.

- [ ] **Step 2: 채널 리스트 엑셀 서버 업로드**

```bash
scp -i "C:\Users\TheRose\crawling_bot_client\LightsailDefaultKey-ap-northeast-2.pem" \
  "C:\Users\TheRose\Documents\카카오톡 받은 파일\벤치마킹시트 신규.xlsx" \
  ubuntu@3.39.179.148:/home/ubuntu/lotto-stock-wiki/shopping_shorts/data/벤치마킹시트.xlsx
```
Expected: 전송 완료(진행률 100%), 에러 없음. (`shopping_shorts/data/`는 `.db`/`.xlsx` 등
런타임 데이터 폴더라 이미 `.gitignore` 대상 — git 추적 안 됨을 사전 확인)

- [ ] **Step 3: 서버 최신 커밋 pull + `/etc/shopping-shorts.env` 배치**

```bash
ssh -i "C:\Users\TheRose\crawling_bot_client\LightsailDefaultKey-ap-northeast-2.pem" ubuntu@3.39.179.148 \
  "cd /home/ubuntu/lotto-stock-wiki && git pull --ff-only origin main"
```
Expected: Task 1~4 커밋이 전부 반영됨 (`git log -1 --oneline`으로 커밋 해시 확인)

```bash
ssh -i "C:\Users\TheRose\crawling_bot_client\LightsailDefaultKey-ap-northeast-2.pem" ubuntu@3.39.179.148 \
  "sudo cp /home/ubuntu/lotto-stock-wiki/deploy/shopping-shorts.env.example /etc/shopping-shorts.env && openssl rand -hex 32"
```
나온 hex 값을 `DASH_SECRET`에, 원하는 비번을 `DASH_PASS`에, 4개 Apify 토큰을 채워
`/etc/shopping-shorts.env`를 서버에서 직접 편집(SSH `nano` 또는 `heredoc`으로 덮어쓰기).
Expected: `sudo cat /etc/shopping-shorts.env`로 4개 값(DASH_PASS/DASH_SECRET/4개 APIFY_TOKEN) +
`SHORTS_EXCEL=/home/ubuntu/lotto-stock-wiki/shopping_shorts/data/벤치마킹시트.xlsx` 모두 채워짐 확인

- [ ] **Step 4: systemd 서비스 등록**

```bash
ssh -i "C:\Users\TheRose\crawling_bot_client\LightsailDefaultKey-ap-northeast-2.pem" ubuntu@3.39.179.148 "
  sudo cp /home/ubuntu/lotto-stock-wiki/deploy/shopping-shorts.service /etc/systemd/system/ &&
  sudo systemctl daemon-reload &&
  sudo systemctl enable --now shopping-shorts &&
  sudo systemctl status shopping-shorts --no-pager &&
  curl -s localhost:8849/healthz
"
```
Expected: `active (running)`, `curl` 응답 `{"ok":true}`

- [ ] **Step 5: nginx 등록**

```bash
ssh -i "C:\Users\TheRose\crawling_bot_client\LightsailDefaultKey-ap-northeast-2.pem" ubuntu@3.39.179.148 "
  sudo cp /home/ubuntu/lotto-stock-wiki/deploy/nginx-shoppingshorts.conf /etc/nginx/sites-available/shopping-shorts &&
  sudo ln -sf /etc/nginx/sites-available/shopping-shorts /etc/nginx/sites-enabled/ &&
  sudo nginx -t &&
  sudo systemctl reload nginx
"
```
Expected: `nginx -t` → `syntax is ok` / `test is successful`

확인: `curl -s -o /dev/null -w "%{http_code}\n" http://shoppingshorts.duckdns.org/login`
Expected: `200`

- [ ] **Step 6: 무료 SSL**

```bash
ssh -i "C:\Users\TheRose\crawling_bot_client\LightsailDefaultKey-ap-northeast-2.pem" ubuntu@3.39.179.148 \
  "sudo certbot --nginx -d shoppingshorts.duckdns.org --non-interactive --agree-tos -m parklotto12@gmail.com --redirect"
```
Expected: `Congratulations! ... https://shoppingshorts.duckdns.org` 인증서 발급 완료 메시지

확인: `curl -s -o /dev/null -w "%{http_code}\n" https://shoppingshorts.duckdns.org/login`
Expected: `200`

- [ ] **Step 7: 로그인 → 실제 기능 e2e 확인**

브라우저로 `https://shoppingshorts.duckdns.org` 접속 → `/login`으로 리다이렉트되는지,
설정한 비번으로 로그인 → 랭킹 페이지 뜨는지, 「지금 수집」(limit 없이 전체는 비용 발생하니
`curl`로 `limit=3`만) 눌러 정상 동작 확인:

```bash
curl -s -b "dash_auth=<브라우저에서 로그인 후 얻은 쿠키값>" \
  -X POST "https://shoppingshorts.duckdns.org/api/collect?limit=3"
```
Expected: `{"ok":true,"count":...}` — 401이면 쿠키 재확인, 500이면 `/etc/shopping-shorts.env`의
Apify 토큰 오타 확인

- [ ] **Step 8: `deploy/README.md`에 shopping-shorts 섹션 추가 + 커밋**

`deploy/README.md` 하단에 stockbrain 섹션과 같은 형식으로 아래 내용 추가:
- 접속 URL, 로그인 계정 안내(비번은 git에 안 올림 명시)
- 위 Step 1~6 요약 절차
- 갱신: `git push`만 하면 3분 내 자동배포(Task 4 확장 덕분), 수동 즉시반영 명령,
  로그 `journalctl -u shopping-shorts -f`

```bash
git add deploy/README.md
git commit -m "docs(deploy): 쇼핑쇼츠 배포 절차 README 추가"
git push origin main
```

---

## Self-Review Notes

- **Spec coverage**: 설계서의 서버/도메인/로그인/nginx/systemd/env/자동배포/엑셀 업로드
  7개 항목 모두 Task 1~5에 매핑됨. "수집 버튼 제한 없음", "직원별 계정 없음"은 스코프
  밖으로 명시했으므로 별도 태스크 불필요.
- **Type/name consistency**: `DASH_USER`/`DASH_PASS`/`DASH_SECRET`/`_AUTH_ON`/`_auth_token()`
  네이밍이 Task 2 코드, Task 3 env 템플릿, Task 5 SSH 명령 전체에서 동일하게 사용됨.
  포트 `8849`가 Task 3 nginx/systemd와 Task 5 healthz 확인 전부 일치.
- **auto_deploy.sh 기존 버그**: 원래 스크립트가 `.py` 확장자만으로 stockbrain을 재시작하던
  게 shopping_shorts 코드 변경 시에도 stockbrain을 불필요하게 재시작시키는 버그였음 —
  Task 4에서 경로 기반 조건으로 좁혀 같이 수정함(설계서에 없던 부수 발견, 범위 안 벗어남).
