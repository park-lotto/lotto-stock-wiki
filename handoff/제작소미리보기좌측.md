# 트랙: 제작소미리보기좌측

## 2026-07-20 (3) — ✅ 실전검증 통과 + 라이브 배포 (회사PC 인계 → 집PC 완료)

- **실 TTS로 3체크 전부 확인**(서버 ElevenLabs 키를 트랙 shopping_shorts/.env로 내려받아):
  ① 멈춤 사라짐 = conform이 sync_gap **5.03s→0.0s**로 완전히 닫음(재TTS 3.04s ≤ 예산 3.45s → freeze 불필요)
  ② 배지 = `conformed=True` 정상 ③ 리라이트 뜻 보존(실 Gemini).
- **핵심 교훈**: 로컬 `.env`엔 ElevenLabs 키가 없어 TTS가 무음 mock(`_write_silent_mp3`, `_CHARS_PER_SEC=5.0`)으로
  나온다. 무음 기준으로 재면 "conform이 gap 못 닫음·상수 2배 어긋남"이라는 **거짓 결론**이 나온다.
  실 TTS = **5.92자/초**로 코드 상수 `_SYLLABLES_PER_SEC=5.7`과 거의 일치(정확히 튜닝돼 있음).
  → 앞으로 TTS 관련 검증은 반드시 `config.ELEVENLABS_API_KEY` 로드 확인 + mean_volume(-91dB=무음) 체크부터.
- **finish 병합**: 27커밋 뒤처져 produce.html 3충돌 → HEAD의 추출함수(_renderMixReviewBody·
  _renderPreviewVideo)가 origin의 커버리지신호등·스왑버튼·검정라인수정을 이미 포함한 **상위집합**이라
  충돌2·3은 HEAD 채택, 충돌1(jump/go)만 origin `stepLocked` 게이트 + 내 `saveWork()` 병합.
  병합 후 신규실패 3건(cutaway 길이계약 2 = 여운 미반영, nav 하네스 = stepLocked 스텁 누락) 갱신 →
  게이트 통과(기준선 8 그대로) → **origin/main 93a583bb5 라이브**.
- ⏭ **남은 것 = 사장님 라이브 육안**(3분 뒤): 실제 영상 매칭→미리보기서 ①긴 대사 비트에서 화면 멈춤
  사라졌나 ②편집안에 "⏱ 길이 맞춤" 배지 뜨나. 코드/데이터는 검증 끝, 실사용 관측만 남음.

## 2026-07-20 (2) — ✅ 콘폼루프 구현 완료 (Fable, 커밋 914420991)

- T1~T5 전부 구현: conform_narration(게이트 0.8~1.2배) / _conform_beats(gap>0.8s만,
  재TTS 성공 후에만 narration 교체) / 마지막 비트 여운 1s(유출 0 유지) / 편집안 ⏱배지 /
  테스트 12건 신설 + 길이계약 2건 갱신. 신규 실패 0(잔여 3건 전부 기준선 stash 실측).
- ⏭ 남은 것: **실전 검증** — 실제 영상 2~3개로 매칭→미리보기 1회 돌려 ①멈춤 사라짐
  ②콘폼 배지 표시 ③리라이트 품질(뜻 유지) 눈확인. Gemini 키 필요(트랙 .env 확인).
- ⏭ finish(라이브)는 "다른 창 정리 후" — 사장님 결정 유지. 새로고침 수정(4ecbbd382)과
  이번 콘폼루프가 같이 나간다.

## 2026-07-20 (1) — 미리보기 "화면 멈춤" 설계 리뷰 완료 → 구현됨(위 참조)

- **트리거**: "믹스 싱크 콘폼루프 구현 이어서" → `docs/superpowers/specs/2026-07-20-대본영상싱크-콘폼루프-design.md` 먼저 읽기
- 진단(실측): `828588e2e38f.mp4` 파일 자체는 정상(PTS 깨끗). 멈춤 = `_MAX_SLOWMO=1.15` 상한 초과분을
  마지막 프레임 freeze로 채운 것(9.4~12.2s·15.7~16.5s·25~27.6s). freeze-to-fill이 원인.
- 리뷰 판정: "대본 먼저 vs 영상 먼저"는 가짜 이분법. 영상 레버는 이미 소진돼 있음
  (`_plan_beat_clips`가 primary+alternates 전부 사용 후에만 얼림 — 코드 검증).
  전면 영상선행(A안) **기각**, **콘폼 루프**(초과 문장만 제미니 리라이트→그 비트만 재TTS) 채택.
- 구현 태스크 T1~T5는 설계문서 §2. 기반 검증됨: 비트별 개별 mp3(`beat_N.mp3`),
  말속도 상수 `_SYLLABLES_PER_SEC=5.7`, shortfall 계산 기존재.


## 2026-07-19 (집PC) — 제작소 위저드 새로고침 상태유지 버그 수정

### ✅ 완료 (커밋됨, 라이브 미반영)
- **버그**(사장님 제보): ① 1단계에서 미리보기까지 만들고 새로고침하면 프리뷰 사라짐 ② 2·3단계에서 새로고침하면 1단계로 되돌아옴
- **근본원인**: `?work=<id>`로 새로고침이 서버복원(`_restoreWork`)을 타게 해뒀지만, 대본 확정 **이후**의 상태변화(MIX_JOB 획득·단계 이동)가 `saveWork()`를 안 불러 서버 작업파일이 낡음 → 복원이 step=0·job_id=null을 읽어 미리보기·단계 소실. 게다가 `_restoreWork`가 preview_status만 읽고 `<video>`를 다시 안 그림.
- **수정** (`shopping_shorts/static/produce.html`):
  - `jump`/`go`/`startProduceMix` → `saveWork()` 추가 (step·job_id 서버 저장)
  - `_renderPreviewVideo`·`_renderMixReviewBody` 추출(pollPreview·loadMixReview와 단일 출처), `_restoreMixContext` 신설 → 복원 시 편집안+미리보기 `<video>` 다시 그림
  - 게이트 안전: 복원 후 `preview_status!='ready'`면 1단계로 되돌림(유료 자막제거 우회 차단)
  - 테스트: `tests/test_produce_refresh_persistence.py` 신설(저장·복원·게이트 3케이스, 실제 소스 node 구동). 기존 `test_produce_preview_gate.py`는 추출 헬퍼 슬라이스+saveWork 스텁 반영.
- **검증**: 신규 15 green, produce계열 320 green(실패 2건은 기준선: test_mix_pipeline render, test_sidebar_works no_fetch — 내 변경 무관), /produce HTTP 200, JS 문법 무오류
- **커밋**: `4ecbbd382` (fix) / `8be34a4ae`(좌측 패널 폭 1120 CSS)

### ⏭ 다음 (미완료)
- **`py tools/track.py finish 제작소미리보기좌측`** = main 병합 → 3분 뒤 라이브. **아직 안 돌림** — 사장님이 "다른 창 정리 후 배포"로 결정.
- 여력되면 실제 브라우저에서 end-to-end 재현(작업생성→매칭→미리보기→새로고침) 1회 눈확인.

### ⚠️ 동시편집 흡수 주의 (규칙5 위반 정황)
- 이 세션 작업 중 **다른 창이 같은 트랙 폴더를 동시 편집**했음. 커밋 `0d3b20808`는 **메시지("전체 가로폭 1120 통일")와 내용(내 saveWork 수정)이 불일치** — 다른 창의 `git add -A`가 내 미커밋 편집을 쓸어담음.
- **유실은 없음**: 내 수정 전체가 HEAD에 온전, CSS도 살아있음. 라벨만 꼬임.
- finish 전에 이 폴더에서 도는 다른 창을 먼저 정리할 것. 안 그러면 재발.
