# 대본 주제 무결성 근본 수정

## 원인

- 박선정 고객(cid 174), 작업 `4205361b7ff5`에는 차량용 어린이 화장실 영상 5개와 차량용 액세서리 4종 모음 영상 1개가 함께 있었다.
- 기존 생성기는 소스를 정렬한 뒤 5개로 잘라 선택 영상과 다수 제품을 보존하지 못했고, `차량용` 같은 일반 단어만 겹쳐도 같은 제품으로 통과시켰다.
- 3단계 작업 연결값이 비어 있으면 브라우저에 남은 같은 고객의 이전 job을 재사용할 수 있었다.
- 전체 생성과 한 문장 재생성이 서로 다른 시점의 주제·재료를 볼 수 있었다.

## 수정

- `topic_contract.py`를 제품 주제 판정의 단일 기준으로 추가했다.
- 담긴 영상 전체에서 제품 합의를 먼저 정하고, 합의 제품 자료만 추린 뒤 개수 제한을 적용한다.
- 선택 영상은 합의 제품 안에서만 우선권을 갖고 다른 제품으로 주제를 바꾸지 못한다.
- work가 있으면 서버의 `work.job_id`만 사용하며 고객·작업 소유권과 연결 관계를 검증한다.
- 생성 결과와 부분 재생성 결과를 같은 고정 주제로 검사하고, 판정기를 쓸 수 없으면 제품 주제 검사는 실패로 닫는다.
- 화면은 확정 주제와 주제 출처를 저장한 뒤 부분 재생성을 시작한다.

## 검증

- 실제 6개 소스 재현, 순서 반전, 잘못된 선택 영상, 같은 고객의 이전 job, 혼합 나열형, 명시 주제 이식, 전체·부분 재생성 경쟁을 회귀 테스트에 포함했다.
- 아스트라: 관련 53개 통과, 추가 차단 결함 없음.
- 페이블: Python 161개 + 화면 19개 통과, `diff --check`·`py_compile` 통과, 추가 차단 결함 없음.
- 전체 묶음: 8,086 passed, 31 skipped, 45 failed. 정적 검사 1개는 함수 범위 검사로 보완 후 통과. 나머지 44개를 main에서 재실행한 결과 38 failed / 6 passed였다(전체 green 아님).
- finish에서 성공경로 테스트가 2회 실패한 원인은 새 필수 `_speaker_judge`를 mock하지 않아 실제 API에 의존한 것이었다. judge `{}`로 502, 정상 판정으로 200을 재현했다. 성공 테스트의 외부 AI·wow 호출을 격리하고 관련 42개 통과.
- 20시대 실측: 고객 원본은 handoff 5개 / job `48cc32996354` / step 5로 진행했으나 s2에는 옛 컵홀더 대본이 남아 있다. 원본 대신 관리자 QA 작업 `e84bd727991a`에 당시 혼합 소스 조건을 복제했다. 실제 2단계 화면 열림 / JS 오류 0.

## 배포 후 확인

- 박선정 작업을 새로고침하고 전체 대본을 다시 생성한다.
- A안과 B안 모두 차량용 어린이 화장실 주제인지 확인한다.
- 한 문장 `바꾸기` 후에도 같은 주제가 유지되는지 확인한다.

## 2026-09-14 추가 게이트 검증
- 최신 main 기준선 13건 실패. 대본 성공경로 격리 수정 후 해당 실패는 사라졌으나 후보 병렬성 elapsed<0.75초 테스트가 신규 실패로 게이트 차단. main과 트랙의 후보 병렬 실행부는 동일. 단독 6건은 통과.
- 병렬성 테스트를 3개 서로 다른 스레드의 첫 호출 Barrier 동시 진입으로 변경해 실제 겹침을 검사한다. 시간 임계치를 느슨하게 하는 대신 순차 실행을 거부하는 구조다. 관련 39개 통과.
- 아스트라 실 AI 중간검증: 실제 6개 자료에서 변기 5개 유지, 모음 제외. 전송 프롬프트에서 사고 타제품 내용 제외. 로컬 API HTTP429(asia-east1 quota_limit_value=0)로 생성문이 없어 실제 생성/의미판정 성공으로 세지 않음.
- 라이브 QA는 재생성 응답이 서버의 해당 문장으로 실제 저장될 때까지 확인한 후 새로고침 결과와 비교한다.

## 2026-09-14 21시 배포본 실측 및 추가 결함
- main `3a455c61a` 배포. 전체게이트 통과(기준선 14건 실패 잔존). 웹21:02:01, 워커21:01:06 재시작 확인.
- QA work `e84bd727991a`: 사고 당시 6소스 유지 조건에서 실제 브라우저 생성 HTTP200, 2안, 고정주제 휴대용 차량 간이 변기, 변기자료5개/모음제외, 양쪽 주제단일성 통과. 실제 한문장재생성 HTTP200, 응답문장서버저장 확인, 새로고침시 2안·주제 동일복원, JS오류0.
- 그러나 실제출력 읽기검수에서 B 품절/방광건강 단정, A 가격/냄새분자 차단확대 등 별도 사실근거 결함 발견. 주제혼합은 차단됐지만 대본 전체품질 해결로 보고하면 안 됨.
- 추가원인: 스타일54 scale틀이 품절을 요구, 스타일68 가격/강한성능 유도. 최종judge는 원본근거없이 제품명·대본만 판정. 수치검사는 아라비아숫자만, 장면검사는 ID존재/비율만. WOW는 모델why문장을근거로 취급하고 groundingmetadata검증/출처보존이 없다.
- 추가수정 진행: Fable WOW실제웹지원근거 검증, Astra 공용사실판정+생성/부분재생성, Root app assembled/pickup연결·캐시버전·통합검증. 기존배포는 유지. 완료아님.
- 실측산출물: `%TEMP%/script-topic-qa-20260914/phase1_topic_verified/` (인증정보 제외).

## 사실근거 보완 구현 (배포 전)
- WOW v2: 실제 Google grounding support가 훅 전체 UTF-8 범위를 덮고 유효 URL에 연결된 경우만채택. why별도검증, 지원범위·원문·출처캐시재검증, 구형캐시차단. HTTP성공이나근거없는응답은키20회반복하지않고빈보강으로종료.
- 공용 claim_evidence: 원본전사·장면관측, 경계닫힌크롤제품facts, 검증WOW만근거. AI일반지식·활용추론·스타일예시와타제품이식원본을근거로승격하지않음.
- 공용 semantic_content_checks: 기존실제judge에근거를보내가격·품절·성능확대·의학인과까지판정, 사실근거실패/판정불능은제품locked경로 FATAL. 전체·재단·부분재생성·픽업·조립모두같은출구.
- 관련통합93개통과. 추가기존83개중81통과/2실패는main에서같이재현(beat_polish가틀문구를AI교정해정확문자열기대와충돌), 이번제품수정결함으로단정하지않음.
- 다음: 전체게이트→서버반영→실제A/B 생성+부분재생성+저장/새로고침 재검증, phase1의근거없는실제대본2개를실제AI가거부하는지추가검증.
