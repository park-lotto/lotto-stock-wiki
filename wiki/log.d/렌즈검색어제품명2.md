# 렌즈검색어제품명2 트랙 로그

## 2026-09-16 — 「대본 분석해서 정확히 찾기」 해당항목없음 404 수리

- 원인: `_find_collected_item`이 **수집 스냅샷 2종만** 뒤지는데, 화면은 네 갈래
  (`last_run`·`last_run::<plat>`·`reel_history`·`channel_archive`)에서 카드를 꺼낸다.
  게다가 렌즈·담기 카드(`lens_*`·`grab_*`)는 `mix_basket`에만 있어 **원리적으로** 못 찾았다.
  실측: 담긴 7,374건 + 스냅샷 밖 유튜브 45,143건이 전부 404 대상.
- 조치: 판정 한 곳(`_find_collected_item`)에 서랍 3개 추가 → `/api/extract_script` ·
  `/api/wiki/save` · `/api/find/analyze` 세 곳이 같이 살아남(0순위-B).
  신설: `store.basket_item_any_customer` · `history_item` · `archive_item`.
- ★라이브가 내 설계를 두 번 반박: ①`reel_history`엔 platform 컬럼이 있고 유튜브 행이
  실재(못박으면 안 됨) ②접두사 없는 담김 1,336건 중 213건이 비인스타 → **url 1순위,
  접두사 폴백**(도메인 표는 `lens_discover._platform_of` 재사용).
- 검증: 신규 20 + 관련 828 통과, 사보타주 5회 전부 빨간불, 라이브 DB 실측
  (사장님 카드 5/5 복구 · 회귀 400/400 · 새 서랍 300/300).
- ⏭ 배포 후 사장님 육안 확인 필요(확인된 것은 "항목을 찾는다"까지).
