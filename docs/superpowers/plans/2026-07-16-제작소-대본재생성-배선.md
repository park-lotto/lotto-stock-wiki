# 제작소 대본 재생성 배선 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 영상제작소 mix 탭에서 대본 1개를 고르면 원본을 그대로 복사하는 대신 이미 이식돼 있는 즐겨찾기 생성패널(`pmModal`)로 **재생성**하게 배선한다.

**Architecture:** 신규 개발 없음. `produce.html`의 `pmModal`(요소 7토글·리메이크/이식·초안 재작성/이력)은 이미 존재하나 왼쪽 "넘어온 영상 → 📝뽑기" 경로에만 배선돼 있다. mix 목록 선택을 같은 모달에 물리고(추출 단계는 건너뜀 — 위키 대본이라 텍스트가 이미 있고 백엔드가 shortcode로 구조를 읽는다), 이식 과정에서 빠진 소재칸을 메우고, 죽은 제미니 자동을 걷어내고, 배선하다 드러난 위키담기 오귀속을 막는다. **백엔드 신규 작업 0** — `/api/wiki/generate`는 위키 조회 + `base_script` 폴백을 이미 지원한다.

**Tech Stack:** FastAPI(`app.py`) · 순수 JS(`static/produce.html`, 프레임워크 없음) · pytest · Node v24(HTML 안의 JS를 앵커로 잘라내 실행하는 기존 테스트 하네스)

**설계문서:** `docs/superpowers/specs/2026-07-16-제작소-대본재생성-배선-design.md`

## Global Constraints

- **작업 폴더는 `.tracks/대본믹스통합`뿐.** main 폴더(`C:\Users\TheRose\Desktop\로또의 주식`)의 코드 파일은 단 하나도 건드리지 않는다.
- **범위 밖 — 손대지 말 것:** 2개+ 조합 생성(`genMix`) 엔진 · 왼쪽 뽑기 경로의 `pmUseRaw` · `static/library.html` · 요소 토글/모드의 UX 재설계.
- **테스트 하네스 관례:** `produce.html`의 JS는 재구현하지 않는다. `tests/test_produce_category_race.py`가 하듯 **파일에서 앵커로 실제 소스를 잘라내 Node로 실행**한다. Node 없으면 `pytest.skip`(다른 baseline 실패로 집계되지 않게).
- **앵커는 이번 수정으로 바뀌지 않는 문자열**로 고른다(주석·다음 함수 시그니처). 수정 전후 모두 매치돼야 한다.
- **인코딩:** 파이썬 CLI 실행 시 `PYTHONIOENCODING=utf-8`. 이 저장소는 cp949로 죽은 전례가 4건 있다.
- **게이트 기준:** "전부 green"이 아니라 **"병합 전보다 실패가 늘지 않았나"**.
  **착수 시점 기준선 실측(2026-07-16, 이 트랙 폴더에서 측정): `10 failed, 678 passed` (167초).**
  실패 10건은 착수 전부터 깨져 있던 것이다(`test_mix_pipeline.py::test_run_render_happy_path`,
  `test_mix_render_flow.py::test_render_off_skips_vmake`, `test_render_on_calls_vmake` 등).
  **이 10건을 고치는 것은 이 계획의 범위가 아니다** — 늘지만 않으면 된다.
- 커밋은 `track/대본믹스통합` 브랜치로만. 트랙 폴더 안이므로 `git add -A` 안전.

---

### Task 1: 제미니 자동 제거 (뿌리째)

셋은 서로만 호출하고 다른 데서 안 쓴다. `tests/`에 참조 0건(확인됨). mix 목록이 재생성 경로가 되면 "주제만 넣고 처음부터"는 즐겨찾기 🔀이식 모드와 겹친다.

**Files:**
- Modify: `shopping_shorts/static/produce.html` (탭 `:154` · `SCRIPT_MODE_HTML.gemini` `:1041-1048` · `genGemini()` `:1104-1109`)
- Modify: `shopping_shorts/app.py` (`:1769-1781` 라우트)
- Modify: `shopping_shorts/script_generate.py` (`_TOPIC_PROMPT` `:103-117` · `generate_from_topic()` `:118-131`)
- Test: `shopping_shorts/tests/test_gemini_auto_removed.py` (create)

**Interfaces:**
- Consumes: 없음
- Produces: 없음 (순수 제거). Task 3이 `SCRIPT_MODE_HTML`의 `mix` 항목을 수정하므로 이 태스크가 먼저 끝나야 충돌이 없다.

- [ ] **Step 1: Write the failing test**

Create `shopping_shorts/tests/test_gemini_auto_removed.py`:

```python
"""제미니 자동(주제만으로 대본 생성) 제거 봉인 — 2026-07-16.

mix 목록이 재생성 경로가 되면서 이 기능은 즐겨찾기 🔀이식 모드와 겹쳐 폐기됐다.
탭·라우트·생성함수 셋은 서로만 쓰던 폐쇄 묶음이라 뿌리째 지웠다.
이 테스트는 되살아나는 것을 막는다(부활시키려면 이 파일을 먼저 지워라).
"""
import pathlib

ROOT = pathlib.Path(__file__).resolve().parents[1]
PRODUCE_HTML = ROOT / "static" / "produce.html"
APP_PY = ROOT / "app.py"
SCRIPT_GEN_PY = ROOT / "script_generate.py"


def test_produce_html_has_no_gemini_auto():
    text = PRODUCE_HTML.read_text(encoding="utf-8")
    assert 'data-mode="gemini"' not in text, "제미니 자동 탭이 남아 있다"
    assert "genGemini" not in text, "genGemini() 가 남아 있다"
    assert "gemDrafts" not in text, "gemDrafts 컨테이너가 남아 있다"
    assert "gemTopic" not in text, "gemTopic 입력칸이 남아 있다"


def test_app_has_no_gemini_route():
    text = APP_PY.read_text(encoding="utf-8")
    assert "/api/produce/script/gemini" not in text, "제미니 자동 라우트가 남아 있다"
    assert "api_produce_script_gemini" not in text, "라우트 핸들러가 남아 있다"


def test_script_generate_has_no_topic_mode():
    text = SCRIPT_GEN_PY.read_text(encoding="utf-8")
    assert "generate_from_topic" not in text, "generate_from_topic() 이 남아 있다"
    assert "_TOPIC_PROMPT" not in text, "_TOPIC_PROMPT 가 남아 있다"


def test_other_generators_survive():
    """과잉 삭제 방지 — 살아 있어야 할 형제 함수들."""
    text = SCRIPT_GEN_PY.read_text(encoding="utf-8")
    for name in ("generate_variations", "generate_mix", "refine_draft_rewrite", "_GEN_PROMPT", "_MIX_PROMPT"):
        assert name in text, f"{name} 이 실수로 지워졌다"
```

- [ ] **Step 2: Run test to verify it fails**

```
cd .tracks/대본믹스통합
PYTHONIOENCODING=utf-8 py -m pytest shopping_shorts/tests/test_gemini_auto_removed.py -q
```
Expected: FAIL — 처음 3개 테스트가 "제미니 자동 탭이 남아 있다" 등으로 실패, `test_other_generators_survive`만 PASS.

- [ ] **Step 3: 탭 제거 — `produce.html:154`**

이 줄을 **삭제**한다:
```html
              <div class="tab" data-mode="gemini">✨ 제미니 자동</div>
```
남는 탭은 `manual`(직접작성) · `mix`(우리 시스템으로 믹스) 둘.

- [ ] **Step 4: `SCRIPT_MODE_HTML.gemini` 제거 — `produce.html:1041-1048`**

`SCRIPT_MODE_HTML` 객체에서 `gemini:` 항목 전체를 삭제한다. 삭제 대상:
```js
  gemini: () => `
    <div style="display:flex;gap:8px;margin-bottom:6px">
      <input id="gemTopic" type="text" placeholder="주제/제품 (예: 실리콘 다용도 집게)"
        style="flex:1;background:#0d1420;border:1px solid var(--line);color:var(--txt);border-radius:9px;padding:11px">
      <button class="btn-next" style="padding:11px 16px" onclick="genGemini()">✨ 생성</button>
    </div>
    <div class="hint">주제만 넣으면 제미니가 여러 초안을 만듭니다. 마음에 드는 초안을 고르면 편집창으로 넘어갑니다.</div>
    <div id="gemDrafts" style="margin-top:12px"></div>`,
```
`manual:`과 `mix:` 항목은 그대로 둔다.

- [ ] **Step 5: `genGemini()` 제거 — `produce.html:1104-1109`**

주석 포함 삭제:
```js
// 제미니 자동
function genGemini(){
  const topic=(document.getElementById('gemTopic').value||'').trim();
  if(!topic){ document.getElementById('gemDrafts').innerHTML='<div class="hint" style="color:var(--fire)">주제를 입력하세요</div>'; return; }
  postDrafts('/api/produce/script/gemini', {topic, target_seconds:20, n:3}, 'gemDrafts');
}
```
⚠️ 바로 아래 `postDrafts`는 `genMix`도 쓴다 — **남긴다.**

- [ ] **Step 6: 라우트 제거 — `app.py:1769-1781`**

`# ── 영상제작 위저드(produce) ───...` 주석 바로 아래 블록을 삭제한다:
```python
@app.post("/api/produce/script/gemini")
def api_produce_script_gemini(body: dict):
    """1단계 대본 · 제미니 자동 — 주제/제품으로 처음부터 대본 초안 N개 생성."""
    topic = (body.get("topic") or "").strip()
    if not topic:
        return JSONResponse(status_code=422, content={"ok": False, "error": "주제/제품을 입력하세요"})
    drafts = script_generate.generate_from_topic(
        topic, target_seconds=body.get("target_seconds") or 20, n=body.get("n") or 3)
    if not drafts:
        return JSONResponse(status_code=502,
                            content={"ok": False, "error": "생성 실패(키 소진 또는 응답 오류)"})
    return {"ok": True, "drafts": drafts}
```
`# ── 영상제작 위저드(produce) ───` 구분 주석은 **남긴다**(뒤따르는 다른 produce 라우트들의 머리다).

- [ ] **Step 7: `_TOPIC_PROMPT` + `generate_from_topic()` 제거 — `script_generate.py:103-131`**

`_TOPIC_PROMPT = """너는 한국 쇼핑 숏폼(살림·요리·인테리어·생활용품) 대본 작가다.` 로 시작하는 상수 전체(`:103-117`)와 그 아래 함수 전체(`:118-131`)를 삭제한다:
```python
def generate_from_topic(topic, target_seconds=20, n=3, max_key_tries=3):
    """주제/제품 하나로 처음부터 대본 초안 리스트 생성(제미니 자동 모드). 실패/무키면 [].

    generate_variations(원본 필요=Feature A)와 달리 소스 대본 없이 주제만으로 만든다.
    한국어 대략 6.5자/초 기준으로 목표 길이에 맞춰 분량을 지시한다."""
    if not comment_gen.SHORTS_GEMINI_KEYS or not (topic or "").strip():
        return []
    n = max(1, min(int(n or 3), 5))
    seconds = max(5, min(int(target_seconds or 20), 90))
    words = max(15, round(seconds * 2.3))  # 대략치(초당 ~2.3단어)
    prompt = _TOPIC_PROMPT.format(topic=topic.strip()[:1000], seconds=seconds, words=words, n=n)
    return _generate_drafts(prompt)
```
⚠️ `_GEN_PROMPT`(`:80`) · `_MIX_PROMPT`(`:132`) · `_generate_drafts`(`:26`)는 **남긴다** — 형제 기능이 쓴다.

- [ ] **Step 8: Run tests to verify they pass**

```
PYTHONIOENCODING=utf-8 py -m pytest shopping_shorts/tests/test_gemini_auto_removed.py -q
```
Expected: 4 passed

임포트 게이트도 확인(라우트 삭제가 문법을 깨지 않았나):
```
PYTHONIOENCODING=utf-8 py -c "import shopping_shorts.app; print('import OK')"
```
Expected: `import OK`

- [ ] **Step 9: Commit**

```bash
git add -A
git commit -m "refactor(shopping_shorts): 제미니 자동 제거 — mix 재생성 배선으로 이식모드와 중복

주제만으로 대본을 뽑는 경로는 즐겨찾기 🔀이식 모드와 기능이 겹친다.
탭·라우트·generate_from_topic은 서로만 쓰던 폐쇄 묶음이라 뿌리째 제거.
테스트 참조 0건 확인. 부활 방지 봉인 테스트 추가."
```

---

### Task 2: 소재칸 이식 — `pmModal`의 빠진 조각

즐겨찾기엔 소재 자동감지 칸이 있는데 `pmModal`엔 없다. `pmRunGen`이 `subject`를 아예 안 보낸다(`produce.html:1326`) — 백엔드는 `body.get("subject")`로 받을 준비가 돼 있는데도. **지금 제작소 리메이크는 소재가 고정되지 않는다.** 소재고정 트랙 작업이 `library.html`에만 반영된 것으로 보인다.

같은 모달이라 이 태스크만으로 **왼쪽 뽑기 경로에도 소재 고정이 생긴다**(Task 3 없이도 이득).

**Files:**
- Modify: `shopping_shorts/static/produce.html` (CSS 추가 · `pmModal` 마크업 `:410-415` · `pmToggleTopic()` `:1295-1298` · `pmRunGen()` `:1314-1333` · `openScriptModal()` `:1290` 부근)
- Test: `shopping_shorts/tests/test_produce_subject.py` (create)

**Interfaces:**
- Consumes: 없음
- Produces: Task 3이 쓰는 것들 —
  - `async function pmPrefillSubject(shortcode: string): Promise<void>` — `#pmSubject`를 `/api/wiki/subject`로 채운다. shortcode가 falsy면 칸만 비우고 fetch 안 함.
  - DOM id `pmSubject`(input) · `pmSubjectRow`(래퍼 div)
  - `pmRunGen()`이 보내는 body에 `subject: string` 포함

- [ ] **Step 1: Write the failing test**

Create `shopping_shorts/tests/test_produce_subject.py`:

```python
"""제작소 pmModal 소재칸(2026-07-16).

즐겨찾기(library.html)엔 소재 자동감지 칸이 있는데 제작소 pmModal엔 없어서
pmRunGen이 subject를 안 보냈다 → 리메이크 모드인데 소재가 고정되지 않았다.
(백엔드 /api/wiki/generate는 subject를 이미 받는다.)

test_produce_category_race.py와 같은 방식 — produce.html의 **실제 소스**를
앵커로 잘라내 Node로 실행한다. 재구현이 아니라 실물 코드를 검증한다.
"""
import pathlib
import shutil
import subprocess

import pytest

PRODUCE_HTML = pathlib.Path(__file__).resolve().parents[1] / "static" / "produce.html"
NODE = shutil.which("node")

# 앵커: 이번 수정으로 안 바뀌는 문자열
START_ANCHOR = "function pmToggleTopic(){"
END_ANCHOR = "function pmRenderDrafts(drafts){"

_HARNESS_PREFIX = r"""
'use strict';
function makeGenericEl(){
  return { style:{}, classList:{ contains(){return false;}, add(){}, remove(){} },
           textContent:'', innerHTML:'', disabled:false, value:'', placeholder:'', appendChild(){},
           querySelector(){ return null; }, querySelectorAll(){ return []; } };
}
const _elements = { pmSubject: makeGenericEl(), pmSubjectRow: makeGenericEl(),
                    pmTopic: makeGenericEl(), pmRun: makeGenericEl(),
                    pmResults: makeGenericEl(), pmModal: makeGenericEl() };
let _mode = 'A';                       // 리메이크(A) 기본
let _postBody = null;
const document = {
  getElementById(id){ return _elements[id] || null; },
  querySelector(sel){
    if (sel.indexOf('pmmode') !== -1) return { value: _mode };
    return null;
  },
  querySelectorAll(sel){ return []; },   // pmToggles 요소행 없음 → elemModes {}
  addEventListener(){},
};
function fetch(url, opts){
  if (String(url).indexOf('/api/wiki/subject') !== -1) {
    return Promise.resolve({ json: async () => ({ ok:true, subject:'감자 스낵 만들기' }) });
  }
  if (String(url).indexOf('/api/wiki/generate') !== -1) {
    _postBody = JSON.parse(opts.body);
    return Promise.resolve({ json: async () => ({ ok:true, drafts:[] }) });
  }
  return Promise.reject(new Error('unmocked fetch: ' + url));
}
function esc(s){ return s; }
// pmRenderDrafts는 END_ANCHOR 밖(잘라낸 범위에 없음)인데 pmRunGen이 부른다 → 스텁 필수.
function pmRenderDrafts(){}
let PM_IDX=null, PM_BASE_STRUCT=null, PM_CATEGORY='레시피', PM_URL='', PM_SHORTCODE='ABC123', PM_BASE_SCRIPT='원본 대본';
// ---- 여기부터 produce.html에서 그대로 잘라낸 실제 소스 ----
"""

_SCENARIO_SUBJECT_SENT = r"""
(async () => {
  await pmPrefillSubject('ABC123');
  const el = document.getElementById('pmSubject');
  if (el.value !== '감자 스낵 만들기') {
    console.error('FAIL: 소재 프리필 안 됨 — value=' + JSON.stringify(el.value)); process.exit(1);
  }
  await pmRunGen();
  if (_postBody === null) { console.error('FAIL: generate 호출 안 됨'); process.exit(1); }
  if (_postBody.subject !== '감자 스낵 만들기') {
    console.error('FAIL: subject가 payload에 안 실림 — ' + JSON.stringify(_postBody.subject)); process.exit(1);
  }
  console.log('PASS');
})();
"""

_SCENARIO_TRANSPLANT_NO_SUBJECT = r"""
(async () => {
  document.getElementById('pmSubject').value = '감자 스낵 만들기';
  document.getElementById('pmTopic').value = '무선 가습기';
  _mode = 'B';                       // 이식 모드
  await pmRunGen();
  if (_postBody === null) { console.error('FAIL: generate 호출 안 됨'); process.exit(1); }
  if (_postBody.subject !== '') {
    console.error('FAIL: 이식 모드인데 subject가 실림 — ' + JSON.stringify(_postBody.subject)); process.exit(1);
  }
  if (_postBody.my_topic !== '무선 가습기') {
    console.error('FAIL: my_topic 누락 — ' + JSON.stringify(_postBody.my_topic)); process.exit(1);
  }
  console.log('PASS');
})();
"""


def _extract() -> str:
    text = PRODUCE_HTML.read_text(encoding="utf-8")
    start = text.find(START_ANCHOR)
    end = text.find(END_ANCHOR)
    assert start != -1, f"START_ANCHOR 못 찾음(produce.html이 바뀌었나): {START_ANCHOR!r}"
    assert end != -1 and end > start, f"END_ANCHOR 못 찾음: {END_ANCHOR!r}"
    return text[start:end]


def _run(scenario: str, tmp_path) -> subprocess.CompletedProcess:
    src = _HARNESS_PREFIX + _extract() + scenario
    f = tmp_path / "probe.js"
    f.write_text(src, encoding="utf-8")
    return subprocess.run([NODE, str(f)], capture_output=True, text=True, timeout=30)


@pytest.mark.skipif(NODE is None, reason="node 없음 — JS 하네스 스킵")
def test_subject_prefilled_and_sent(tmp_path):
    r = _run(_SCENARIO_SUBJECT_SENT, tmp_path)
    assert r.returncode == 0, f"stdout={r.stdout} stderr={r.stderr}"
    assert "PASS" in r.stdout


@pytest.mark.skipif(NODE is None, reason="node 없음 — JS 하네스 스킵")
def test_transplant_mode_sends_empty_subject(tmp_path):
    r = _run(_SCENARIO_TRANSPLANT_NO_SUBJECT, tmp_path)
    assert r.returncode == 0, f"stdout={r.stdout} stderr={r.stderr}"
    assert "PASS" in r.stdout


def test_subject_markup_exists():
    text = PRODUCE_HTML.read_text(encoding="utf-8")
    assert 'id="pmSubject"' in text, "소재 입력칸이 없다"
    assert 'id="pmSubjectRow"' in text, "소재 래퍼가 없다"
    assert ".subject-row" in text, "subject-row CSS가 없다(library.html에만 있으면 스타일이 깨진다)"
```

- [ ] **Step 2: Run test to verify it fails**

```
PYTHONIOENCODING=utf-8 py -m pytest shopping_shorts/tests/test_produce_subject.py -q
```
Expected: FAIL — `pmPrefillSubject is not defined` (Node 시나리오) 및 `소재 입력칸이 없다`.

- [ ] **Step 3: CSS 추가 — `produce.html` `<style>` 블록 끝**

`produce.html`에는 이 클래스들이 **없다**(`library.html:83-87`에만 있다). 셀렉터는 `#genSubject` → `#pmSubject`로 바꿔 옮긴다. `<style>` 안 아무 끝에 추가:

```css
  .subject-row{margin:6px 0 10px 20px;font-size:13px}
  .subject-row .subj-label{color:var(--sub);margin-right:6px}
  .subject-row #pmSubject{width:70%;background:#0d131c;border:1px solid var(--line);
    color:var(--txt);border-radius:6px;padding:7px 9px;font-size:13px}
  .subject-row .subj-hint{color:var(--sub);font-size:11.5px;margin-top:5px}
```

- [ ] **Step 4: 마크업 추가 — `produce.html:411-415` `.modes` 블록**

현재:
```html
        <div class="modes">
          <label><input type="radio" name="pmmode" value="A" checked onchange="pmToggleTopic()"> ♻️ 원본 소재 그대로 · 표현만 새로 (중복 회피)</label>
          <label><input type="radio" name="pmmode" value="B" onchange="pmToggleTopic()"> 🔀 내 제품으로 이식 (구조만 빌림)</label>
          <input id="pmTopic" placeholder="예: 무선 가습기 / 여름 결로 방지 커튼" style="display:none">
        </div>
```
로 교체 — 리메이크 라벨 **바로 아래**에 소재칸을 넣는다(library의 배치와 동일):
```html
        <div class="modes">
          <label><input type="radio" name="pmmode" value="A" checked onchange="pmToggleTopic()"> ♻️ 원본 소재 그대로 · 표현만 새로 (중복 회피)</label>
          <div class="subject-row" id="pmSubjectRow">
            <span class="subj-label">감지된 소재:</span><input id="pmSubject" placeholder="소재 감지 중…">
            <div class="subj-hint">원본 영상 재사용·2영상 믹싱용. 제품·사실·장면은 그대로, 훅·문장·순서만 새로.</div>
          </div>
          <label><input type="radio" name="pmmode" value="B" onchange="pmToggleTopic()"> 🔀 내 제품으로 이식 (구조만 빌림)</label>
          <input id="pmTopic" placeholder="예: 무선 가습기 / 여름 결로 방지 커튼" style="display:none">
        </div>
```

- [ ] **Step 5: `pmToggleTopic()` 확장 + `pmPrefillSubject()` 추가 — `produce.html:1295-1298`**

현재:
```js
function pmToggleTopic(){
  const mode=document.querySelector('input[name=pmmode]:checked').value;
  const t=document.getElementById('pmTopic'); if(t) t.style.display = mode==='B' ? 'block' : 'none';
}
```
로 교체:
```js
function pmToggleTopic(){
  const mode=document.querySelector('input[name=pmmode]:checked').value;
  const t=document.getElementById('pmTopic'); if(t) t.style.display = mode==='B' ? 'block' : 'none';
  const s=document.getElementById('pmSubjectRow'); if(s) s.style.display = mode==='A' ? 'block' : 'none';
}
// 소재 자동감지 프리필(도서관 library.html openGen의 소재 블록 이식, 2026-07-16).
// 경합 가드 필수: 모달을 빨리 두 번 열면 먼저 띄운 요청이 늦게 도착해 남의 소재를 덮어쓴다.
// library는 window._genSC로 봤고 여기선 PM_SHORTCODE가 같은 역할을 한다.
async function pmPrefillSubject(sc){
  const el=document.getElementById('pmSubject'); if(!el) return;
  el.value=''; el.placeholder='소재 감지 중…';
  if(!sc){ el.placeholder='예: 무선 가습기 물때 청소'; return; }   // 직행 URL 등 shortcode 없음 — 손입력은 열어둔다
  const mine=sc;
  try{
    const s=await (await fetch('/api/wiki/subject?shortcode='+encodeURIComponent(sc))).json();
    if(s.ok && PM_SHORTCODE===mine) el.value=s.subject||'';
  }catch(e){}
  el.placeholder='예: 무선 가습기 물때 청소';
}
```

- [ ] **Step 6: `pmRunGen()`이 `subject` 전송 — `produce.html:1321-1326`**

현재:
```js
  const mode=document.querySelector('input[name=pmmode]:checked').value;
  const topic=document.getElementById('pmTopic').value.trim();
  if(mode==='B'&&!topic){ res.innerHTML='<div class="hint" style="color:var(--fire)">내 주제/제품을 입력하세요.</div>'; return; }
  btn.disabled=true; res.innerHTML='<div class="hint">✨ 초안 만드는 중… (10~20초)</div>';
  try{
    const body={mode, my_topic:topic, n:3, elem_modes:elemModes, structure:PM_BASE_STRUCT, base_script:PM_BASE_SCRIPT, category:PM_CATEGORY};
```
로 교체(두 줄만 바뀐다 — `subject` 선언 추가, `body`에 실기):
```js
  const mode=document.querySelector('input[name=pmmode]:checked').value;
  const topic=document.getElementById('pmTopic').value.trim();
  const subjEl=document.getElementById('pmSubject');
  const subject = mode==='A' ? ((subjEl&&subjEl.value)||'').trim() : '';   // 이식(B)엔 소재 안 보냄
  if(mode==='B'&&!topic){ res.innerHTML='<div class="hint" style="color:var(--fire)">내 주제/제품을 입력하세요.</div>'; return; }
  btn.disabled=true; res.innerHTML='<div class="hint">✨ 초안 만드는 중… (10~20초)</div>';
  try{
    const body={mode, my_topic:topic, subject, n:3, elem_modes:elemModes, structure:PM_BASE_STRUCT, base_script:PM_BASE_SCRIPT, category:PM_CATEGORY};
```

- [ ] **Step 7: 뽑기 경로에서 프리필 호출 — `produce.html:1290`**

`openScriptModal()` 안, `await pmLoadElementOptions(PM_CATEGORY);` 바로 아래에 한 줄 추가:
```js
    await pmLoadElementOptions(PM_CATEGORY);
    pmToggleTopic();                    // 소재칸 표시상태를 현재 모드에 맞춤
    pmPrefillSubject(PM_SHORTCODE);     // shortcode 없으면(직행 URL) 손입력만 열어둔다
```

- [ ] **Step 8: Run tests to verify they pass**

```
PYTHONIOENCODING=utf-8 py -m pytest shopping_shorts/tests/test_produce_subject.py -q
```
Expected: 3 passed

- [ ] **Step 9: Commit**

```bash
git add -A
git commit -m "fix(shopping_shorts): 제작소 pmModal에 소재칸 이식 — 리메이크가 소재를 고정하게

pmRunGen이 subject를 안 보내 제작소 리메이크는 소재가 흘렀다(백엔드는
이미 받는 중). 소재고정 트랙 작업이 library.html에만 반영된 것으로 보임.
library의 소재 블록 + 경합가드(_genSC===mySC → PM_SHORTCODE)를 이식.
같은 모달이라 왼쪽 뽑기 경로에도 소재 고정이 생긴다."
```

---

### Task 3: mix 탭 1개 선택 → `pmModal` 배선 (핵심)

**Files:**
- Modify: `shopping_shorts/static/produce.html` (`SCRIPT_MODE_HTML.mix` 버튼 `:1056-1060` · `useSingleScript()` `:1161-1171` 삭제 · `openScriptModal()` `:1272-1275`에 `PM_FROM_WIKI` 초기화 · `PM_*` 선언 `:1271`)
- Test: `shopping_shorts/tests/test_produce_mix_regen.py` (create)

**Interfaces:**
- Consumes: Task 2의 `pmPrefillSubject(shortcode)` · DOM id `pmSubject`/`pmSubjectRow`
- Produces:
  - `function openGenFromWiki(): void` — mix 체크박스에서 1개를 읽어 `pmModal`을 연다
  - 전역 `let PM_FROM_WIKI = null` — 위키 유래면 shortcode 문자열, 뽑기 유래면 `null`. **Task 4가 읽는다.**

- [ ] **Step 1: Write the failing test**

Create `shopping_shorts/tests/test_produce_mix_regen.py`:

```python
"""mix 탭 1개 선택 → 재생성 배선(2026-07-16).

기존 useSingleScript()는 고른 대본을 그대로 복사해 확정했다 = 원본 영상에
원본 대본을 얹는 재업로드라 무의미. 이제 이미 이식돼 있던 pmModal(즐겨찾기
생성패널)을 열어 재생성한다. 위키 대본이라 추출 단계는 건너뛴다.

produce.html의 실제 소스를 앵커로 잘라내 Node로 실행한다.
"""
import pathlib
import shutil
import subprocess

import pytest

PRODUCE_HTML = pathlib.Path(__file__).resolve().parents[1] / "static" / "produce.html"
NODE = shutil.which("node")

START_ANCHOR = "function openGenFromWiki(){"
END_ANCHOR = "function genMix(){"

_HARNESS_PREFIX = r"""
'use strict';
function makeGenericEl(){
  return { style:{}, classList:{ _on:false, contains(){return this._on;},
           add(){this._on=true;}, remove(){this._on=false;} },
           textContent:'', innerHTML:'', disabled:false, value:'', placeholder:'', appendChild(){} };
}
const _elements = { pmModal: makeGenericEl(), pmResults: makeGenericEl(),
                    mixDrafts: makeGenericEl(), pmSubject: makeGenericEl(),
                    pmSubjectRow: makeGenericEl(), pmTopic: makeGenericEl() };
let _checked = [];                    // 체크된 shortcode 목록
const document = {
  getElementById(id){ return _elements[id] || null; },
  querySelectorAll(sel){
    if (sel === '.mixPick:checked') return _checked.map(v => ({ value: v }));
    return [];
  },
  querySelector(){ return null; },
};
let _prefilled = null, _loadedCat = null;
function pmPrefillSubject(sc){ _prefilled = sc; return Promise.resolve(); }
function pmLoadElementOptions(cat){ _loadedCat = cat; return Promise.resolve(); }
function pmToggleTopic(){}
function esc(s){ return s; }
let PM_IDX='기존값', PM_BASE_STRUCT='기존값', PM_CATEGORY='', PM_URL='기존값',
    PM_SHORTCODE='', PM_BASE_SCRIPT='', PM_FROM_WIKI=null;
// Node엔 window가 없다. 'use strict'라 선언 없이 대입하면 ReferenceError → globalThis에 붙인다.
globalThis.window = { _mixItems: [
  { shortcode:'ABC123', name:'감자스낵', category:'레시피', full_text:'원본 대본 본문' },
  { shortcode:'DEF456', name:'가습기',   category:'가전',   full_text:'' },
] };
// ---- 여기부터 produce.html에서 그대로 잘라낸 실제 소스 ----
"""

_SCENARIO_OPENS_MODAL = r"""
_checked = ['ABC123'];
openGenFromWiki();
const fails = [];
if (!document.getElementById('pmModal').classList.contains()) fails.push('모달이 안 열림');
if (PM_SHORTCODE !== 'ABC123') fails.push('PM_SHORTCODE=' + JSON.stringify(PM_SHORTCODE));
if (PM_CATEGORY !== '레시피') fails.push('PM_CATEGORY=' + JSON.stringify(PM_CATEGORY));
if (PM_BASE_SCRIPT !== '원본 대본 본문') fails.push('PM_BASE_SCRIPT=' + JSON.stringify(PM_BASE_SCRIPT));
if (PM_BASE_STRUCT !== null) fails.push('PM_BASE_STRUCT는 null이어야(백엔드가 위키에서 읽음)=' + JSON.stringify(PM_BASE_STRUCT));
if (PM_IDX !== null) fails.push('PM_IDX는 null이어야(HANDOFF 유래 아님)=' + JSON.stringify(PM_IDX));
if (PM_FROM_WIKI !== 'ABC123') fails.push('PM_FROM_WIKI=' + JSON.stringify(PM_FROM_WIKI));
if (_prefilled !== 'ABC123') fails.push('소재 프리필 안 부름=' + JSON.stringify(_prefilled));
if (_loadedCat !== '레시피') fails.push('요소옵션 로드 카테고리=' + JSON.stringify(_loadedCat));
if (fails.length) { console.error('FAIL: ' + fails.join(' / ')); process.exit(1); }
console.log('PASS');
"""

_SCENARIO_REJECTS_TWO = r"""
_checked = ['ABC123', 'DEF456'];
openGenFromWiki();
if (document.getElementById('pmModal').classList.contains()) {
  console.error('FAIL: 2개 선택인데 모달이 열림'); process.exit(1);
}
if (document.getElementById('mixDrafts').innerHTML.indexOf('1개만') === -1) {
  console.error('FAIL: 안내문 없음 — ' + document.getElementById('mixDrafts').innerHTML); process.exit(1);
}
console.log('PASS');
"""

_SCENARIO_REJECTS_EMPTY_SCRIPT = r"""
_checked = ['DEF456'];                 // full_text 빈 항목
openGenFromWiki();
if (document.getElementById('pmModal').classList.contains()) {
  console.error('FAIL: 대본 없는 항목인데 모달이 열림'); process.exit(1);
}
console.log('PASS');
"""


def _extract() -> str:
    text = PRODUCE_HTML.read_text(encoding="utf-8")
    start = text.find(START_ANCHOR)
    end = text.find(END_ANCHOR)
    assert start != -1, f"START_ANCHOR 못 찾음: {START_ANCHOR!r}"
    assert end != -1 and end > start, f"END_ANCHOR 못 찾음: {END_ANCHOR!r}"
    return text[start:end]


def _run(scenario: str, tmp_path) -> subprocess.CompletedProcess:
    src = _HARNESS_PREFIX + _extract() + scenario
    f = tmp_path / "probe.js"
    f.write_text(src, encoding="utf-8")
    return subprocess.run([NODE, str(f)], capture_output=True, text=True, timeout=30)


@pytest.mark.skipif(NODE is None, reason="node 없음 — JS 하네스 스킵")
def test_one_pick_opens_modal_with_wiki_item(tmp_path):
    r = _run(_SCENARIO_OPENS_MODAL, tmp_path)
    assert r.returncode == 0, f"stdout={r.stdout} stderr={r.stderr}"


@pytest.mark.skipif(NODE is None, reason="node 없음 — JS 하네스 스킵")
def test_two_picks_rejected(tmp_path):
    r = _run(_SCENARIO_REJECTS_TWO, tmp_path)
    assert r.returncode == 0, f"stdout={r.stdout} stderr={r.stderr}"


@pytest.mark.skipif(NODE is None, reason="node 없음 — JS 하네스 스킵")
def test_empty_script_rejected(tmp_path):
    r = _run(_SCENARIO_REJECTS_EMPTY_SCRIPT, tmp_path)
    assert r.returncode == 0, f"stdout={r.stdout} stderr={r.stderr}"


def test_use_single_script_is_gone():
    text = PRODUCE_HTML.read_text(encoding="utf-8")
    assert "useSingleScript" not in text, "그대로 쓰기(useSingleScript)가 남아 있다"
    assert "그대로 쓰기" not in text, "'그대로 쓰기' 버튼 문구가 남아 있다"


def test_mix_combine_survives():
    """범위 밖 — 2개+ 조합은 그대로여야 한다."""
    text = PRODUCE_HTML.read_text(encoding="utf-8")
    assert "function genMix(){" in text
    assert "/api/produce/script/mix" in text
```

- [ ] **Step 2: Run test to verify it fails**

```
PYTHONIOENCODING=utf-8 py -m pytest shopping_shorts/tests/test_produce_mix_regen.py -q
```
Expected: FAIL — `START_ANCHOR 못 찾음: 'function openGenFromWiki(){'` 및 `useSingleScript가 남아 있다`.

- [ ] **Step 3: `PM_FROM_WIKI` 전역 추가 — `produce.html:1271`**

현재:
```js
let PM_IDX=null, PM_BASE_STRUCT=null, PM_CATEGORY='', PM_URL='', PM_SHORTCODE='', PM_BASE_SCRIPT='';
```
로 교체:
```js
let PM_IDX=null, PM_BASE_STRUCT=null, PM_CATEGORY='', PM_URL='', PM_SHORTCODE='', PM_BASE_SCRIPT='';
// 이 대본이 위키(mix 목록)에서 왔나 — 왔으면 shortcode, 왼쪽 뽑기(HANDOFF) 유래면 null.
// Task 4의 위키담기 오귀속 차단이 이걸 읽는다.
let PM_FROM_WIKI=null;
```

- [ ] **Step 4: 뽑기 경로에서 `PM_FROM_WIKI` 초기화 — `produce.html:1274-1275`**

`openScriptModal()` 안, 현재:
```js
  PM_IDX=i; PM_URL=it.url; PM_SHORTCODE=it.shortcode||''; PM_CATEGORY=it.category||'';
  PM_BASE_STRUCT=null; PM_BASE_SCRIPT='';
```
로 교체(마지막에 한 항목 추가):
```js
  PM_IDX=i; PM_URL=it.url; PM_SHORTCODE=it.shortcode||''; PM_CATEGORY=it.category||'';
  PM_BASE_STRUCT=null; PM_BASE_SCRIPT=''; PM_FROM_WIKI=null;   // HANDOFF 유래 — 위키담기 대상이 있다
```

- [ ] **Step 5: mix 버튼 교체 — `produce.html:1056-1060`**

`SCRIPT_MODE_HTML.mix` 안, 현재:
```js
    <div style="display:flex;gap:8px;flex-wrap:wrap">
      <button class="btn-next" style="padding:10px 16px;background:linear-gradient(90deg,#1c5a2e,#0d3a1c)" onclick="useSingleScript()">✅ 선택 1개 그대로 쓰기</button>
      <button class="btn-next" style="padding:10px 16px" onclick="genMix()">🧬 선택 2개+ 조합 생성</button>
    </div>
    <div class="hint" style="margin-top:6px">같은 주제면 1개 대본 그대로 쓰고 영상만 여러 개 붙이는 게 자연스럽습니다. 텍스트 조합이 필요할 때만 2개+.</div>
```
로 교체:
```js
    <div style="display:flex;gap:8px;flex-wrap:wrap">
      <button class="btn-next" style="padding:10px 16px;background:linear-gradient(90deg,#1c5a2e,#0d3a1c)" onclick="openGenFromWiki()">♻️ 선택 1개로 새 대본 생성</button>
      <button class="btn-next" style="padding:10px 16px" onclick="genMix()">🧬 선택 2개+ 조합 생성</button>
    </div>
    <div class="hint" style="margin-top:6px">1개 = 그 대본의 구조를 빌려 새로 씁니다(요소별로 유지·교체 선택). 여러 대본의 강점을 섞으려면 2개+.</div>
```

- [ ] **Step 6: `useSingleScript()` → `openGenFromWiki()` 교체 — `produce.html:1161-1171`**

현재 함수 전체:
```js
function useSingleScript(){
  const picks=[...document.querySelectorAll('.mixPick:checked')].map(c=>c.value);
  const box=document.getElementById('mixDrafts');
  if(picks.length!==1){ box.innerHTML='<div class="hint" style="color:var(--fire)">1개만 선택하세요(그대로 쓰기)</div>'; return; }
  const it=(window._mixItems||[]).find(w=>w.shortcode===picks[0]);
  if(!it || !(it.full_text||'').trim()){ box.innerHTML='<div class="hint" style="color:var(--fire)">대본 내용이 없습니다</div>'; return; }
  STATE.script=it.full_text;
  setScriptMode('manual');   // 그 대본 그대로 편집창에 채움
  refreshFinalPeek();
  if(HANDOFF.length) saveWork();
}
```
로 교체:
```js
// mix 목록에서 고른 위키 대본 1개를 재료로 pmModal(즐겨찾기 생성패널)을 연다.
// 원본을 그대로 복사하던 useSingleScript()를 대체 — 원본 대본을 원본 영상에 얹는 건
// 리메이크가 아니라 재업로드다(2026-07-16).
// 뽑기 경로와 달리 **추출 단계가 없다**: 위키 대본이라 본문이 이미 있고, 구조는
// 백엔드 /api/wiki/generate가 shortcode로 읽는다. PM_BASE_SCRIPT는 위키에 항목이
// 없을 때를 위한 폴백으로만 실린다(pmRunGen이 body에 이미 담고 있다).
function openGenFromWiki(){
  const picks=[...document.querySelectorAll('.mixPick:checked')].map(c=>c.value);
  const box=document.getElementById('mixDrafts');
  if(picks.length!==1){ box.innerHTML='<div class="hint" style="color:var(--fire)">1개만 선택하세요 — 2개 이상은 아래 조합 생성입니다</div>'; return; }
  const it=(window._mixItems||[]).find(w=>w.shortcode===picks[0]);
  if(!it || !(it.full_text||'').trim()){ box.innerHTML='<div class="hint" style="color:var(--fire)">대본 내용이 없습니다</div>'; return; }
  box.innerHTML='';
  PM_IDX=null; PM_URL='';                       // HANDOFF 유래가 아니다
  PM_SHORTCODE=it.shortcode; PM_CATEGORY=it.category||'';
  PM_BASE_STRUCT=null;                          // 백엔드가 위키에서 읽는다
  PM_BASE_SCRIPT=it.full_text; PM_FROM_WIKI=it.shortcode;
  document.getElementById('pmModal').classList.add('open');
  document.getElementById('pmResults').innerHTML='<div class="hint">✅ 이 위키 대본을 재료로 새 대본을 만듭니다. 아래에서 요소를 조절하고 초안을 생성하세요.</div>';
  pmToggleTopic();
  pmLoadElementOptions(PM_CATEGORY);
  pmPrefillSubject(it.shortcode);
}
```
⚠️ `pmResults`에 **`pmUseRaw` 버튼을 그리지 않는다** — 그게 사장님이 지적한 무의미 동작이다. 왼쪽 뽑기 경로(`openScriptModal:1289`)의 `pmUseRaw`는 성격이 다르므로 **그대로 둔다**.

- [ ] **Step 7: Run tests to verify they pass**

```
PYTHONIOENCODING=utf-8 py -m pytest shopping_shorts/tests/test_produce_mix_regen.py -q
```
Expected: 5 passed

- [ ] **Step 8: Commit**

```bash
git add -A
git commit -m "feat(shopping_shorts): mix 탭 1개 선택 → 대본 재생성 배선

'선택 1개 그대로 쓰기'는 원본 영상에 원본 대본을 얹는 재업로드라 무의미.
이미 이식돼 있던 pmModal(즐겨찾기 생성패널)에 물려 재생성하게 한다.
위키 대본이라 추출 단계를 건너뛰고 백엔드가 shortcode로 구조를 읽는다 —
백엔드 신규 작업 0. 2개+ 조합은 그대로."
```

---

### Task 4: 위키담기 오귀속 차단 (배선하다 드러난 기존 버그)

`useSingleScript()`는 `STATE.script_src_idx`를 안 남겼다. 그래서 위키 대본을 확정하면 `_currentSrcItem()`(`:1447`)이 **엉뚱한 HANDOFF 영상으로 폴백**하고, `⭐이 원본 영상 위키에 담기` 버튼이 그 영상을 학습 재료로 담는다. 코드 주석의 I-1이 잡으려던 바로 그 오귀속인데 mix 경로만 안 막혀 있었다.

`genMix`(2개+ 조합) 결과도 위키 대본들에서 나오므로 같은 오귀속이 난다 — 원본을 하나로 특정할 수 없으니 역시 버튼을 숨긴다.

**Files:**
- Modify: `shopping_shorts/static/produce.html` (`STATE` `:1032` · `pmUseDraft()` `:1350-1355` · `pmUseRaw()` `:1293` · `useDraft()` `:1086-1092` · `refreshFinalPeek()` `:1472-1487`)
- Test: `shopping_shorts/tests/test_produce_wiki_attribution.py` (create)

**Interfaces:**
- Consumes: Task 3의 `PM_FROM_WIKI`
- Produces: `STATE.script_from_wiki: string|null` — 위키 유래면 shortcode(조합은 `'mix'`), HANDOFF 유래면 `null`

- [ ] **Step 1: Write the failing test**

Create `shopping_shorts/tests/test_produce_wiki_attribution.py`:

```python
"""위키담기 오귀속 차단(2026-07-16).

기존 버그: mix 목록에서 온 대본을 확정하면 script_src_idx가 안 남아
_currentSrcItem()이 HANDOFF[0] 등으로 폴백 → '⭐이 원본 영상 위키에 담기'가
대본과 무관한 영상을 학습 재료로 담았다. 주석의 I-1이 잡으려던 오귀속인데
mix 경로만 안 막혀 있었다.

위키 유래 대본은 이미 위키에 있으니 담을 원본이 없다 → 버튼을 숨긴다.
"""
import pathlib
import shutil
import subprocess

import pytest

PRODUCE_HTML = pathlib.Path(__file__).resolve().parents[1] / "static" / "produce.html"
NODE = shutil.which("node")

START_ANCHOR = "function refreshFinalPeek(){"
END_ANCHOR = "// 이 \"원본 영상\"(내 리메이크 아님)을 위키(도서관)에 담아"

_HARNESS_PREFIX = r"""
'use strict';
function makeGenericEl(){
  return { style:{}, classList:{ contains(){return false;}, add(){}, remove(){} },
           textContent:'', innerHTML:'', disabled:false, value:'', appendChild(){} };
}
const _elements = { finalScriptPeek: makeGenericEl(), saveWikiBtn: makeGenericEl(),
                    finalCategory: makeGenericEl() };
const document = { getElementById(id){ return _elements[id] || null; } };
function esc(s){ return s; }
function lenText(){ return '10자 · 약 2초'; }
function ensureCategoryOptions(){}
const STATE = { script: '', script_src_idx: null, script_from_wiki: null };
// ---- 여기부터 produce.html에서 그대로 잘라낸 실제 소스 ----
"""

_SCENARIO_WIKI_HIDES = r"""
STATE.script = '확정된 대본';
STATE.script_from_wiki = 'ABC123';     // mix 목록(위키)에서 온 대본
refreshFinalPeek();
if (document.getElementById('saveWikiBtn').style.display !== 'none') {
  console.error('FAIL: 위키 유래인데 담기 버튼이 보임 — display=' +
    JSON.stringify(document.getElementById('saveWikiBtn').style.display));
  process.exit(1);
}
console.log('PASS');
"""

_SCENARIO_HANDOFF_SHOWS = r"""
STATE.script = '확정된 대본';
STATE.script_from_wiki = null;         // 왼쪽 뽑기(HANDOFF) 유래 — 담을 원본이 있다
refreshFinalPeek();
if (document.getElementById('saveWikiBtn').style.display !== 'inline-block') {
  console.error('FAIL: 뽑기 유래인데 담기 버튼이 안 보임(회귀) — display=' +
    JSON.stringify(document.getElementById('saveWikiBtn').style.display));
  process.exit(1);
}
console.log('PASS');
"""

_SCENARIO_NO_SCRIPT_HIDES = r"""
STATE.script = '';
STATE.script_from_wiki = null;
refreshFinalPeek();
if (document.getElementById('saveWikiBtn').style.display !== 'none') {
  console.error('FAIL: 대본이 없는데 담기 버튼이 보임'); process.exit(1);
}
console.log('PASS');
"""


def _extract() -> str:
    text = PRODUCE_HTML.read_text(encoding="utf-8")
    start = text.find(START_ANCHOR)
    end = text.find(END_ANCHOR)
    assert start != -1, f"START_ANCHOR 못 찾음: {START_ANCHOR!r}"
    assert end != -1 and end > start, f"END_ANCHOR 못 찾음: {END_ANCHOR!r}"
    return text[start:end]


def _run(scenario: str, tmp_path) -> subprocess.CompletedProcess:
    src = _HARNESS_PREFIX + _extract() + scenario
    f = tmp_path / "probe.js"
    f.write_text(src, encoding="utf-8")
    return subprocess.run([NODE, str(f)], capture_output=True, text=True, timeout=30)


@pytest.mark.skipif(NODE is None, reason="node 없음 — JS 하네스 스킵")
def test_wiki_sourced_script_hides_save_button(tmp_path):
    r = _run(_SCENARIO_WIKI_HIDES, tmp_path)
    assert r.returncode == 0, f"stdout={r.stdout} stderr={r.stderr}"


@pytest.mark.skipif(NODE is None, reason="node 없음 — JS 하네스 스킵")
def test_handoff_sourced_script_still_shows_button(tmp_path):
    r = _run(_SCENARIO_HANDOFF_SHOWS, tmp_path)
    assert r.returncode == 0, f"stdout={r.stdout} stderr={r.stderr}"


@pytest.mark.skipif(NODE is None, reason="node 없음 — JS 하네스 스킵")
def test_no_script_hides_button(tmp_path):
    r = _run(_SCENARIO_NO_SCRIPT_HIDES, tmp_path)
    assert r.returncode == 0, f"stdout={r.stdout} stderr={r.stderr}"


def test_confirm_points_set_the_flag():
    """확정 경로 넷이 전부 플래그를 명시적으로 세팅하는가(누락 = stale 플래그 버그)."""
    text = PRODUCE_HTML.read_text(encoding="utf-8")
    assert "script_from_wiki: null" in text or "script_from_wiki:null" in text, "STATE 초기값이 없다"
    assert "STATE.script_from_wiki=PM_FROM_WIKI" in text.replace(" ", ""), "pmUseDraft가 플래그를 안 세운다"
    assert text.count("STATE.script_from_wiki") >= 4, "확정 경로 일부가 플래그를 안 세운다"
```

- [ ] **Step 2: Run test to verify it fails**

```
PYTHONIOENCODING=utf-8 py -m pytest shopping_shorts/tests/test_produce_wiki_attribution.py -q
```
Expected: FAIL — 위키 유래 시나리오가 `display='inline-block'`으로 나와 "담기 버튼이 보임".

- [ ] **Step 3: `STATE`에 플래그 추가 — `produce.html:1032`**

현재:
```js
const STATE = { script: "", script_src_idx: null, subtitleRemoval: false, headcopy: null, captionStyle: null, deco: { extra_texts: [], motion: null } };   // 확정 대본 + 렌더 설정
```
로 교체:
```js
const STATE = { script: "", script_src_idx: null, script_from_wiki: null, subtitleRemoval: false, headcopy: null, captionStyle: null, deco: { extra_texts: [], motion: null } };   // 확정 대본 + 렌더 설정
// script_from_wiki: 확정한 대본이 위키(mix 목록)에서 왔나 — 왔으면 shortcode(조합은 'mix'),
// 왼쪽 뽑기(HANDOFF)에서 왔으면 null. 위키 유래면 '⭐이 원본 영상 위키에 담기'를 숨긴다:
// 이미 위키에 있는 원본이라 담을 게 없고, 그냥 두면 _currentSrcItem()이 엉뚱한 HANDOFF
// 영상으로 폴백해 무관한 영상을 학습 재료로 담는다(I-1이 잡으려던 오귀속의 mix 경로판).
```

- [ ] **Step 4: 확정 경로 넷이 플래그를 세우게**

**(a) `pmUseDraft()` — `:1350-1355`**, 현재:
```js
function pmUseDraft(btn){
  const t=btn.closest('.draft').querySelector('.dtext'); STATE.script=(t?t.value:'')||'';
  STATE.script_src_idx=PM_IDX;
```
로 교체:
```js
function pmUseDraft(btn){
  const t=btn.closest('.draft').querySelector('.dtext'); STATE.script=(t?t.value:'')||'';
  STATE.script_src_idx=PM_IDX;
  STATE.script_from_wiki=PM_FROM_WIKI;   // mix 유래면 shortcode, 뽑기 유래면 null
```

**(b) `pmUseRaw()` — `:1293`**, 현재:
```js
function pmUseRaw(){ STATE.script=PM_BASE_SCRIPT; STATE.script_src_idx=PM_IDX; pmClose(); setScriptMode('manual'); refreshFinalPeek(); setFinalCategoryValue(PM_CATEGORY); if(HANDOFF.length) saveWork(); }
```
로 교체:
```js
function pmUseRaw(){ STATE.script=PM_BASE_SCRIPT; STATE.script_src_idx=PM_IDX; STATE.script_from_wiki=PM_FROM_WIKI; pmClose(); setScriptMode('manual'); refreshFinalPeek(); setFinalCategoryValue(PM_CATEGORY); if(HANDOFF.length) saveWork(); }
```

**(c) `useDraft()` — `:1086-1092`** (Task 1 이후 `genMix` 전용), 현재:
```js
function useDraft(i){
  const dr=(window._drafts||[])[i]; if(!dr) return;
  STATE.script=dr.script||'';
```
로 교체:
```js
function useDraft(i){
  const dr=(window._drafts||[])[i]; if(!dr) return;
  STATE.script=dr.script||'';
  STATE.script_from_wiki='mix';   // 조합 초안 = 위키 대본 여러 개에서 나옴. 원본을 하나로 특정할 수 없다.
```

- [ ] **Step 5: `refreshFinalPeek()`가 플래그를 반영 — `produce.html:1475-1481`**

현재:
```js
  const b=document.getElementById('saveWikiBtn');
  if(b){
    b.style.display=STATE.script?'inline-block':'none';
```
로 교체:
```js
  const b=document.getElementById('saveWikiBtn');
  if(b){
    // 위키 유래 대본(mix 목록/조합)은 담을 원본이 없다 — 버튼을 숨기지 않으면
    // _currentSrcItem()이 무관한 HANDOFF 영상으로 폴백해 그걸 담아버린다.
    b.style.display=(STATE.script && !STATE.script_from_wiki)?'inline-block':'none';
```

- [ ] **Step 6: Run tests to verify they pass**

```
PYTHONIOENCODING=utf-8 py -m pytest shopping_shorts/tests/test_produce_wiki_attribution.py -q
```
Expected: 4 passed

- [ ] **Step 7: 전체 테스트 — 회귀 확인**

```
PYTHONIOENCODING=utf-8 py -m pytest shopping_shorts/tests/ -q 2>&1 | tail -5
```
Expected: `10 failed, 69X passed` — **실패는 기준선 10건 그대로**(Global Constraints의 실측치),
통과 수만 새 테스트(4파일 14건)만큼 늘어난다. **실패가 11건 이상이면 내가 깬 것이다** — 늘어난
그 건을 고치기 전엔 다음으로 넘어가지 않는다. 2~3분 걸린다(기준선 측정 167초).

- [ ] **Step 8: Commit**

```bash
git add -A
git commit -m "fix(shopping_shorts): 위키 유래 대본의 위키담기 오귀속 차단

useSingleScript가 script_src_idx를 안 남겨 위키 대본 확정 시
_currentSrcItem()이 엉뚱한 HANDOFF 영상으로 폴백 → '이 원본 영상 위키에
담기'가 대본과 무관한 영상을 학습 재료로 담았다(I-1이 잡으려던 오귀속의
mix 경로판). STATE.script_from_wiki로 위키 유래를 표시해 버튼을 숨긴다.
조합(genMix) 초안도 원본 특정 불가라 동일 처리."
```

---

## 마무리 — 배포

모든 태스크 완료 후 **main 폴더에서**(트랙 폴더 안에서 부르면 BASE가 깨진다):

```
cd C:\Users\TheRose\Desktop\로또의 주식
py tools/track.py finish 대본믹스통합
```

게이트: 문법 → `import shopping_shorts.app` → pytest(기준선 대비). 통과하면 main 병합 → 3분 뒤 shoppingshorts.duckdns.org 반영.

⚠️ **`finish` 직전에 반드시 `git -C .tracks/대본믹스통합 log --oneline -1`로 내 마지막 커밋을 확인하고, 병합 후 `git log --oneline origin/main | head`에 그게 들어갔는지 확인한다.** 2026-07-16에 다른 세션의 `finish`가 트랙 브랜치를 **1커밋 뒤처진 시점으로 병합**하고 브랜치를 지워 설계문서 커밋이 dangling으로 떨어진 사고가 있었다(cherry-pick으로 복구). `finish`에 경합이 있다.

## 라이브 육안검증 (게이트 통과 후)

게이트는 코드가 도는 것만 보증한다. 실제 화면은 직접 봐야 한다:

1. shoppingshorts.duckdns.org → `/produce` → 1단계
2. 🧬 우리 시스템으로 믹스 탭 → **제미니 자동 탭이 사라졌는지** 확인
3. 대본 1개 체크 → `♻️ 선택 1개로 새 대본 생성` → **pmModal이 뜨고 추출 대기 없이 즉시** 요소 토글이 채워지는지
4. **소재칸에 감지된 소재가 프리필**되는지 (리메이크 모드일 때만 보이는지)
5. `✨ 초안 생성` → 초안 3개 → `이 대본 확정 →`
6. **`⭐이 원본 영상 위키에 담기` 버튼이 안 보이는지** (위키 유래니까)
7. 왼쪽 `📝뽑기` 경로로 확정하면 **그 버튼이 여전히 보이는지**(회귀)
