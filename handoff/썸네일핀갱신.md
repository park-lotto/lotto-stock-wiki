# 썸네일핀갱신 — 6단계 [이 장면을 썸네일 후보로] → 7단계에 꾸민 그림이 안 들어가던 것 (2026-09-25, 사장님 PC)

사장님: "6단계 설정완료 후 썸네일로 넘어오면 프레임적용이 어제는 되더니 오늘 또 안 된다"

## 원인 (라이브 서버 로그 실측, 고객 job 92976b481a86, 09-25 21:05~21:13)
1. 핀을 6번 눌렀는데 브라우저가 핀 그림을 받은 건 2번(21:06:15·21:13:04). 핀 파일명이 장면마다 고정
   (`pin_0_s1_7.75_cb_…jpg`, 덮어쓰기)이라 7단계 `_loadThumbFrames`가 "목록 URL 그대로 → 다시 안 그림"으로 빠지고 브라우저도 옛 그림을 씀.
   → 첫 핀은 되고 다시 보내면 안 바뀜 = "어제는 됐는데 오늘 안 됨"으로 보인다.
2. 핀 요청(21:07:13)이 저장(21:07:16, [썸네일 단계로 이동]의 saveAndClose)보다 먼저 나감 → 서버는 **직전 저장본**으로 찍음.
   편집기에서 방금 바꾼 꾸미기가 썸네일 후보에 안 들어감.
(서버 합성 자체는 정상 — thumb_style 폴더가 오늘도 생성됨. 핀 API 전부 200.)

## 고침 (track/썸네일핀갱신, 미병합·라이브 승인 대기)
- `app.py _with_pins`: 핀 URL에 `?v=<mtime_ns>` → 다시 보내면 주소가 바뀌어 7단계가 새로 그리고 맨 앞(새 핀)을 고른다.
- `app.py api_thumb_pin`: body에 `scene_style`이 오면(null=템플릿 없음) validate 후 그걸로 찍는다. **저장은 안 함**. 형식 오류면 저장본.
- `out/precision20-ui.js`: 핀 요청에 `scene_style: window.sceneStyle.snapshot()` 추가. showcase `?v=222`.
- 렌더·캡컷 경로는 안 건드림(저장본 그대로). 썸네일 저장 PNG는 캔버스(=선택한 핀 그림)에서 나온다.

## 검증
- `check_thumb_pin_styled.py` ④저장 전 화면설정으로 찍힘 ⑤화면 템플릿없음이면 원본 ⑥재핀 시 주소 바뀜 — 새 코드 전부 통과 / 옛 app.py로 ④⑤⑥ 실패.
- `check_thumb_pin.py`(진짜 앱+Playwright) ③핀 요청에 presetId 실림 ④재핀 시 7단계 맨 앞 img src 바뀌고 선택됨·그려짐 — 새 코드 통과 / 옛 코드 ③④ 실패.
- ⏭ 라이브 배포 후 실제 job에서 핀 2번 눌러 7단계 그림이 바뀌는지 직접 확인(Chrome 확장 미연결이라 이번 세션은 로컬 실브라우저까지).

## 신고 건 대조 (조율가님 cid 207, bug_reports id 28, work e581c3a2fc3c → job 7cfa8bd7a23e)
- 11:30~11:48 핀 17회, 핀 그림 수신 3회. **deco.scene_style=None**(서버에 장면꾸미기 저장 없음) → thumb_style 폴더 없음 = 전부 원본 프레임.
- 같은 고객 09-24 작업(64915505abed·b124f8ec8f41)과 09-25 12:23 작업(d569f4c74b92)은 t11 저장됨 → 꾸민 후보 정상.
  = "어제는 됐다"의 차이는 [이 영상에 적용] 전에 핀을 보냈느냐. 수정 A(화면 설정 동봉)가 이 경우를 고친다.
- ⚠️ 남은 것(안 고침, 사장님 판단): [썸네일 단계로 이동]/닫기는 적용한 적 없는 job이면 저장 안 함(09-16 규칙).
  그래서 적용 안 누른 작업은 썸네일은 이제 꾸며져도 **최종 영상엔 템플릿이 안 들어간다**.

## 2026-09-26 — 6단계를 적용 없이 떠나면 "이 템플릿으로 진행할까요?" (사장님 결정)
- 무조건 자동 저장은 09-16 사고(열어만 봐도 t11 저장) 재발이라 **묻는다**. `scene-style-produce.js sceneStyleLeaveGate` 한 곳에서 판단,
  `produce.html jump()`가 6단계를 **앞으로** 떠날 때 부른다(다음 버튼·단계 칩·[썸네일 단계로 이동] 전부 jump 경유). 뒤로는 안 묻는다.
- 조건: 이번에 편집기로 연 job · applied() 거짓(서버 저장본 없음) · 화면(또는 임시저장)에 템플릿 있음(null=템플릿 없음은 안 물음).
- 선택: [✔ 이 템플릿으로 진행]=saveSnapshot 후 이동 / [템플릿 없이 진행]=종전 / [계속 꾸미기]=머묾. 브라우저 confirm 안 씀.
- 함정: 처음엔 STATE.deco.scene_style도 봤는데 job을 바꾸면 앞 job 값이 남아 안 물었다(⑥ 실측) → applied() 하나로.
- 검사 `check_leave_gate.py [--inline]`(라이브는 인라인=전 고객 '1') ①~⑦ 팝업·인라인 둘 다 통과 / 옛 코드 ①③④⑥ 실패.
- 남은 경우: 이번 페이지에서 편집기를 안 열고 옛 임시저장만 있는 job은 안 묻는다(종전 동작).

## 2026-09-26 00:56 라이브 반영
- main `5a59ad619`(내 7d9249294 포함) → 서버 pull 00:55:03, 웹 재시작 00:55:58. 서빙 파일 5개 md5 = main 일치.
- 병합본 그대로 check_leave_gate(--inline)·check_thumb_pin_styled·check_thumb_pin 전부 통과.
- ⚠️ 라이브 화면을 직접 누르지는 못함(Chrome 확장 미연결). 사장님 실사용 확인 필요.
- 조율가님(bug_reports 28) 답장: 관리자 세션이 없어 서버에서 Store.reply_bug_report(API가 부르는 함수)로 저장 → my_bug_replies(207)에 표시 확인.
- 게이트 함정: jump 슬라이스 하네스엔 PANEL_BY_KEY가 없다 → typeof를 조건 맨 앞에(첫 finish가 2건으로 막힘).

## 2026-09-26 — 핀 그림 구도가 완성본과 다름("썸네일로 보냈는데 비율이 안 맞는다", 사장님)
- 원인(실측 job a012b53091f7): `api_thumb_pin`이 원본 9:16 프레임 **전체** 위에 레이어만 얹었다 — 영상 칸(media top 39%·height 61%)에
  맞춰 줄이지 않아 제목 띠가 원본 윗부분(얼굴)을 덮고 아랫부분만 보였다. 렌더(compose)는 원본을 영상 칸에 cover로 앉힌다.
- 고침: `scene_style.media_geometry`(compose와 공용, 0순위-B) + `compose_still`(PIL로 같은 기하) → app.py 핀이 이걸 부른다.
- 실측: 같은 job 실데이터로 사진칸 평균차(완성본 첫 프레임 대비) 옛 핀 76.0 → 새 핀 2.0.
- 검사 `check_thumb_pin_styled.py` ⑦(세로 그라데이션 원본, 핀 vs compose 첫 프레임): 새 코드 0.8 통과 / 옛 app.py 49.3 실패.
  관련 pytest 137 통과, check_plain_downstream 전부 통과(렌더 경로 무회귀).
- ⏭ 라이브 반영은 사장님 승인 대기. 반영 후 실제 job에서 핀 → 7단계 그림이 6단계 미리보기 구도와 같은지 확인.
- 안 한 것: 핀에 장면별 돋보기(highlight)·흐림 마스크·훅 카메라 모션은 안 넣음(0프레임 정지 그림이라 카메라는 1배).

## 2026-09-26 오후 — 썸네일 핀 구도 라이브 반영(15:23 KST 재시작, c3661ba1c)
- 서버 새 코드로 실job 재합성: 사진칸 차 2.0(옛 76.0). ⏭ 라이브 UI에서 핀 눌러 7단계 확인은 아직(브라우저 연결 끊김).

## 2026-09-26 — 6단계 들어가면 구버전이 보이고 회색 버튼을 눌러야 새 편집기가 열림(사장님 job 65358b12dd6e)
- 실측: 서버 로그에 15:03 flags 200 뒤 context 요청이 **한 번도 없음** → 편집기 시작 자체가 안 됨.
  구조: 인라인은 context·제목후보를 다 불러온 **뒤에야** 구버전을 숨겼고, 자동 열기는 패널 class 변화에만 걸려 작업번호(MIX_JOB)가 늦으면 영영 안 열림. 실패 시엔 구버전으로 떨어짐.
- 고침(scene-style-produce.js, produce.html ?v=19): 인라인 켜지면 즉시 구버전 숨김 / 6단계가 보이는 동안 1초마다 작업번호 확인해 스스로 열기 / 실패는 새 편집기 자리에 이유+[다시 시도] / 안내문구는 인라인 칸으로.
- 검사 check_inline_mode.py ④느린 제목후보 ⑤불러오기 실패 ⑥작업번호 늦음 — 새 코드 전부 통과 / 옛 JS ⑤⑥ 실패. check_leave_gate --inline 통과, pytest 151 통과.
- ⏭ 라이브는 사장님 승인 대기(고객 화면 변경).
