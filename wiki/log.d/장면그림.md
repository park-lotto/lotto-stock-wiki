# 장면그림
- 2026-09-09: 최종 저장 출구에서 primary JPEG 1장 검증, 텍스트 폴백·두 판정 JSONL·중복 호출 방지 구현. screen_verify_enabled 기본 OFF, 화면 교체 없음.
- 계약/회귀 테스트 78개 통과 및 앱 import 확인. 실제 모델 키·소스·ffmpeg가 없어 라이브 판정/비용/시간 미측정. finish 미실행.
- git add는 트랙 밖 Git index.lock 쓰기 권한 거부로 실패하여 커밋 미완료. 변경 파일은 트랙에 보존.

## 2026-09-10
- 아스트라(코덱스) 구현분 서버 실측 완료: 비트 17개 중 텍스트 vs 그림 판정 **2건 갈림**, 둘 다 그림이 맞았다(프레임 육안 확인).
- ★이미지 판정은 comment_gen 키풀을 쓴다 — 비면 로그 없이 조용히 죽는다(로컬 17/17 null로 재현).
- finish 보류: 표본 확대 + 텍스트 호출 유지 여부 결정 뒤.
- (2) 표본 확대: job 60건·240칸. 갈림 24건(10.9%) — 사진이 더 잡음 14 / 풀어줌 10. 눈으로 본 4건 모두 사진이 맞았다. ★CTA 자막('안전' vs '나도') 불일치는 글자로는 원리상 못 잡는다.
