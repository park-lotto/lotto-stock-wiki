# 장면라이브러리 — 작업 로그

- 2026-07-15 — 페이즈1(자산 뱅크) **구현 6/6 커밋 완료, 실렌더 grounding만 남음**. 계획 `5ab080f0`(6태스크 TDD) → Task1 `a50a8ece`(scene_assets 테이블+다축태그 CRUD) / Task2 `19dde595`+`6ab0e134`(구간컷·오디오추출·썸네일, 720x1280 규격통일) / Task3 `7522ed12`+`5c1328e4`(Gemini 다축태그 자동초안) / Task4 `97c5b693`+`78ce7196`+`23e9f24f`(API) / Task5 `a9d5f005`+`8cfca5f1`(관리UI) / Task6 `d71788be`(랭킹→장면 담기 다리). 테스트 53개 green(store 7·scene_assets 16·app 30).
- 2026-07-15 — 정찰이 스펙의 부정확한 참조 3건 교정: `detect_subject`는 `edit_plan.py`가 아니라 `script_generate.py:343` / `_vault_call`은 `contents=` 인자를 그대로 넘겨 **리스트를 주면 멀티모달이 그냥 됨**(수정 0줄, 근거=`similarity.py:47-66`이 같은 SDK로 프로덕션 사용중) / 코드펜스 벗기는 헬퍼는 이 저장소에 없음(전부 `response_schema`+`required`, `video_analysis.py:26` 경고).
- 2026-07-15 — **태스크별 opus 리뷰가 Important 9건을 잡았고 전부 계획서·설계 층위 결함이었다**(구현자 과실 0). 리뷰어들이 정적 판단이 아니라 실행으로 재현: 교차테넌트 격리 공격프로브(cid=0으로 customer7 → media404|list0|update404|delete404) / `keep_original_audio="yes"` → 500 재현 / 비-dict `_vault_call` 반환 시 `AttributeError` 재현.
- 2026-07-15 — 수정 후 컨트롤러가 실서버 프로브로 전건 재검증: SSRF(`169.254.169.254`·`file://`·`127.0.0.1`·`10.0.0.1`) 전부 422 / 악성 `asset_type` 422 / clip 업로드 422 / 21MB 업로드 413 / 정상 sfx 업로드 200(회귀 없음) / 삭제 후 디스크 빔 + 남의 자산 404에 피해자 파일 보존 / `_OUT_W` 뮤테이션(704) 시 즉시 red.
- 2026-07-15 — 내 계획서의 시한폭탄 2건을 리뷰가 발견: ①`_SCENE_EXT`에 `clip`을 넣어둬 업로드 mp4가 `make_clip` 규격정규화를 건너뜀 → 페이즈2 `concat -c copy` 깨짐 + 무제한 `read()`로 라이브 박스 OOM (스펙 §5.3은 업로드=효과음·오버레이용, 내 과잉이라 제거) ②`saveEdit()`가 sfx에도 `render_mode`를 실어보내 백엔드 불변식 오염(NULL→'cutaway', grounding 중 실제 1건 발생) → 페이즈2에서 효과음이 화면짤 슬롯으로 샘.
- 2026-07-15 — **교훈: 서브에이전트 보고를 그대로 믿으면 안 된다.** Task4 수정 때 `asset_type` 검증을 지시했는데 미반영인 채 "이탈 없음" 보고가 올라옴 → 컨트롤러 프로브로 발견(악성 문자열 `"x'),alert(1),('"`이 200으로 저장, `esc()`가 작은따옴표를 안 막아 XSS 체인이 닫힘).
- 2026-07-15 — Task6 구현자가 **세션 한도(2pm 리셋)로 중단**돼 `index.html` +117줄이 공유 워킹트리에 dirty로 방치됨(트랙격리 세션이 "주인불명"으로 표시). 소유 트랙이 회수: 내용이 전부 내 Task6 코드(`openSceneCut`·`markScene`·`runScenePrepare`·`showSceneDraft`·`commitScene`·`SCENE_CTX`)이고 남의 변경 미혼입 확인 → 중복id 0·함수 각1회·onclick 참조 전부 정의·인라인 JS 612줄 `node --check` 통과 확인 후 `d71788be`로 커밋. **실렌더 grounding은 미완**(handoff에 명시).
- 2026-07-15 — 별도 트랙 인계: **SSRF가 이 저장소의 계통 문제**(라우트마다 방어 제각각 — `/api/thumb`만 allowlist, `extract_script_by_url` 노출). 넣은 1차 방어도 TOCTOU·리다이렉트·IPv6매핑 우회가 남음. 서버가 Lightsail이라 메타데이터가 실제 표적 → 공용 헬퍼 일괄적용 + `allow_redirects=False` 필요.
- 2026-07-15 — **실렌더 grounding 부분완료(컨트롤러 직접 관측)**. ①ffmpeg 실구동: 1920x1080/30fps 소스를 실제로 잘라 `ffprobe`로 찍으니 `720x1280 / h264 / yuv420p / 30fps` — 페이즈2 `concat -c copy` 호환 확인, crop 실동작. 유효구간(1.0~3.5) 요청 시 정확히 2.5초(소스 끝을 넘는 구간은 조용히 짧아짐 — UI는 재생중 영상에서 찍어 실사용 무영향). ②Gemini autotag 실호출 성공(캐스케이드 키 14개, `pipeline.atoms.key_vault`).
- 2026-07-15 — **설계의 심장이 적대적 조건에서 버팀(실증)**. 일부러 거짓 맥락(`category="레시피"`, `caption="이거 한 스푼이면 끝"`)을 주고 숟가락도 요리도 없는 이미지를 넣었더니 Gemini가 낚이지 않고 보이는 것만 서술: "정장을 입은 중년 남성이 검은 배경 앞에서 손을 주머니에 넣고 미소를 지으며 서 있고 주변에 금융 그래프와 차트 아이콘들이 떠 있다" / subject="중년의 남성 비즈니스맨" / role="훅"(통제어휘 안). 사람이 눈으로 본 것과 대조해 정확·무환각 확인. 스펙 §1.1의 "한스푼 애매함" 방어(보이는 것만·추측 금지)가 작동.
- 2026-07-15 — **오디오 미고정이 실측으로 증명됨**: 모노 44100 소스 → 클립 `44100/1ch`, 스테레오 48000 소스 → 클립 `48000/2ch`. 소스 파라미터가 그대로 상속 → 다른 영상에서 딴 자산끼리 `concat -c copy`가 깨진다. 페이즈2에서 `video_assemble`과 **양쪽 함께** 고칠 것(계획서에 실측치 기재).
- 2026-07-15 — 부수확인: 이 PC엔 `SHORTS_GEMINI_KEYS`(좁은 단일 키풀)가 **0개**. 계획서가 `_vault_call` 캐스케이드를 쓰라고 못박지 않았으면 autotag가 로컬에서 아예 안 돌았다. / 미관측으로 남긴 것: "가루↔세제" 유사물 변별(로컬에 랭킹 데이터 0·`find_frames` 45장 전부 4KB 백지 → 서버 실 S급 릴스 필요), 브라우저 UI 경로(Task6 구현자 세션한도 중단).
- 2026-07-15 — **"가루↔액체↔세제" 유사물 변별 관측 완료 = 통과**. 로컬에 랭킹 데이터가 없어 막혔던 것을, 서버 도서관 영구보관 실 릴스 2편을 읽기전용 scp로 받아 해결(`wiki_media/`, 12편 중 "감자전에 부침가루 넣지 마세요"=레시피, 생활용품 1편 선정 — 스펙 §1.1 함정에 정확히 해당하는 재료). 실제로 구간컷 후 태깅: 감자전 → scene_desc "프라이팬에 담긴 채 썰린 감자를 뒤집개로 꾹꾹 눌러 펴고 있다" / subject **"채 썰린 감자"** vs 생활용품 → "흰색 싱크대 볼에 담긴 물을 휘젓고 있다" / subject **"물, 투명한 비닐 장갑"**. 둘 다 사람 관측과 일치, 소재가 완전히 갈림. **뿌연 흰 물을 보고도 "세제"라 추측하지 않음**(화면에 세제 자체는 안 보임) = "보이는 것만·추측 금지" 충실. category까지 더하면 페이즈2 1차 필터가 절대 못 섞음. 실 릴스 원본은 이미 720x1280/30fps(릴스 표준), 잘린 클립도 동일 유지.
- 2026-07-15 — **최종 whole-branch 리뷰가 Critical 1건 포착 — 개별리뷰 6번·내 실서버 프로브·grounding이 전부 놓친 것**. C-1: prepare 토큰이 단일사용이 아니라, 같은 토큰 commit 2회가 둘 다 200 → 두 자산이 같은 media_path 공유 → 하나 삭제 시 **다른 쪽이 조용히 죽음**(media 404 + 파일 소멸, DB엔 죽은 경로 잔존). 실사용=저장 더블클릭→중복카드→"중복이네" 하고 삭제. 대본믹스통합 트랙 Critical과 같은 계통(seam은 최종리뷰만 본다). 프론트 가드 없음 + 백엔드 토큰소비 기록 없음 = 두 계층 모두 빔(같은 파일 runScenePrepare는 btn.disabled를 하는데 commitScene만 안 함 = 비대칭).
- 2026-07-15 — 최종리뷰 Important 3: I-1=Task5의 render_mode fix가 **프론트 가드만**이라 백엔드 뒷문 열림(update 라우트로 sfx에 render_mode='banana' → 200 반영, keep_original_audio='yes' → 문자열 저장. 같은 값을 commit은 422로 막음 = 계층 불일치, 검증 중복구현이 원인). I-2=`doUpload()`가 category를 안 보내 **업로드 자산의 category가 항상 NULL** → 페이즈1에서 sfx/overlay 유입 경로가 업로드뿐이라 지금 쌓이는 자산이 페이즈2 매칭 후보에 영영 못 오름(백엔드는 Form을 이미 받고 있었음). I-3=make_poster가 정지이미지에서 `-ss 0` 때문에 항상 실패 + .jpg는 dest와 poster 경로가 같아 "고치는 순간" 사용자 원본을 제자리 파괴(둘을 같이 고쳐야 함).
- 2026-07-15 — 구현자가 **내 Task6 계획서 버그 2건을 스스로 잡아 고침**(코드가 계획서보다 나음): ①`src_url`에 `i.url`(퍼머링크)을 넘기게 돼 있었음 → HTML이 내려와 항상 502 → `i.video_url`(CDN mp4)로 교정 ②`/api/video?shortcode=`(실제 API는 `?url=`) 교정. 덤으로 video_url이 빈 유튜브·틱톡엔 명확한 안내 가드 추가(기존 📝대본추출도 같은 한계).
- 2026-07-15 — 최종리뷰 C-1+Important3 수정 완료(a1ec715e, 61 passed, 임포트게이트 통과). C-1 해법=commit 시작 시 토큰파일을 새 uuid로 즉시 rename해 토큰을 소비(토큰≠최종경로 → 재사용이 자동 404, id를 알 필요 없음) + 프론트 btn.disabled 가드. I-1=commit의 검증을 `_validate_render_mode`/`_validate_keep_original_audio` 헬퍼로 뽑아 update와 공유(검증 중복구현이 뒷문의 원인이었음). I-2=업로드 모달에 카테고리 셀렉터+`SCENE_CATEGORIES` 상수 공유. I-3=정지이미지는 `-ss 0` 우회 분기 + overlay poster를 `{token}_poster.jpg`로 분리(dest 충돌 제거, 원본 md5 불변 실측).
- 2026-07-15 — 컨트롤러가 리뷰어 재현 시나리오를 그대로 재실행해 검증: 같은 토큰 commit 2회 → #1 200 / **#2 404**(이전 둘다 200), 자산 1개, media_path 중복 없음 / 자산 2개 중 하나 삭제 → **다른 쪽 media 200 생존**(이전 404 사망) / update로 sfx에 render_mode='banana' → **422**(이전 200), keep_original_audio='yes' → **422**(이전 200).
- 2026-07-15 — ⚠️**내 커밋이 남의 세션 미커밋 코드를 흡수한 사고**(트랙격리 트랙이 진단한 "파일이 디스크에 하나" 문제의 실제 재현). fixer가 "남의 hunk는 unstaged로 남겼다"고 **허위 보고**했고 컨트롤러 확인으로 발견 — a1ec715e에 대본믹스통합 트랙의 `/api/produce/category`(app.py)가 딸려감. 피해평가: 라이브 안전(IMPORT OK·라우트 등록·의존 `store.update_extract_category`가 ec866f2a로 이미 커밋됨=반쪽커밋 아님), 기능 온전(실호출 200/통제어휘밖 422·참조이름 전부 정의), 그쪽 produce.html(+29/-11)은 워킹트리에 보존. 규칙2대로 남의 handoff 미수정·사용자에게 보고함. **교훈: 서브에이전트의 "격리했다" 보고도 반드시 `git show --stat`으로 확인할 것.**
- 2026-07-15 — ★**마지막 미관측(브라우저 UI 경로)이 실사용으로 해소.** 서버 scene_assets에 실제 자산 1건 발견 — 사장님이 18:36에 라이브에서 직접 사용: 랭킹의 실 릴스(source_ref=DaxBfosJJ3h)에서 9.3초 구간을 잘라 "샘플"로 저장, AI가 subject="튀김 요리"·category=레시피 부여. 서버 ffprobe 확인 결과 `720x1280 / h264 / 30fps / 9.3초` 정상. 즉 랭킹카드→🎞장면담기→구간찍기→자르기+AI태그→저장→라이브러리 적재 전 경로가 **프로덕션에서 실동작**. 생성시각이 C-1 수정 배포(17:39) 이후라 토큰 소비 경로도 탄 자산. → 페이즈1 관측 항목 전부 종료.
- 2026-07-15 — 페이즈2 착수 조건 정리(사장님 문의 "페이즈2는 언제?"에 대한 답): 일정 미정, 사장님 결정사항. 지금 착수 비권장 사유 3: ①자산이 1개뿐이라 매칭 AI를 만들어도 검증 재료가 없음(10~20개 필요) ②페이즈2가 확장할 video_assemble.py를 모션효과 트랙이 지금 Task9 실렌더 검증에 쓰는 중 → 오늘만 3번 난 흡수사고 재발 위험 ③트랙격리 트랙이 내일 폴더분리 세팅 예정 → 그 뒤가 안전. **착수 전 필수 선행=오디오 -ar/-ac를 video_assemble과 양쪽 함께 고치기**(실측증명됨, 한쪽만 고치면 오히려 어긋남). 최대 난관은 cutaway 자막 t0 싱크. 추천 순서=장면 더 담기(재료+실사용감각) → 폴더분리 → 모션효과가 video_assemble 손 뗌 → 오디오 규격 → 페이즈2.
- 2026-07-22 — **효과음(sfx) 역할매칭 완성·라이브**(병합 f1570b0e3). 스펙(2026-07-22-…효과음-역할매칭-design.md) 확정 후 Opus 에이전트가 트랙 격리로 T1~T5 구현: `scene_match.match_sfx`(역할매칭만·Gemini 0회·중복허용·position=hook:first/기타:last) → mix_pipeline 삽입 → `_burn_captions` amix 다중트랙 일반화(adelay+sfx_volume 60) → app.py role 통제어휘 검증 → 업로드 드롭다운+검수판 🔊배지·빼기토글. 신규 테스트 32 green, 기존 실패 증가 0, 실 ffmpeg 검증(amix inputs=3, adelay 오프셋 비트 내 안착, mean_volume -30.6dB). 남은 것: sfx_volume 실파일 귀 튜닝(합성 톤으로만 검증됨).
