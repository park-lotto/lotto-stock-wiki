# 폰트일원화 — 작업 로그

## 2026-08-26 (집PC) — 폰트 22→37종 + 즐겨찾기. 커밋 완료·배포 전 확인 대기

**①일원화**: 폰트 목록이 세 벌(produce.html의 @font-face·HC_FONTS, produce_intro.html의
@font-face)이라 하나만 빠뜨려도 조용히 기본체로 나왔다 → 정본 `static/fonts.json` 하나 +
`tools/sync_fonts.py`가 세 곳을 다시 써 넣는다. ★백엔드는 이미 일원화돼 있었다(마운트·
파일명 조회라 무수정) — 처음에 "4벌"이라 한 건 내 오판이었다. 런타임 JS 주입 대신 빌드
스크립트를 고른 이유는 FOUT(첫 화면 글꼴 깜빡임)를 고객에게 안 보이려고(사장님 선택).

**②폰트 15종**(전부 OFL, google/fonts raw에서): 손글씨 8·귀여움 2·레트로 3·임팩트 2.
⚠️Dosis는 한글이 xxxx로 깨져 제외(자동검사 폭 22 이상 → **육안 렌더로 확정**).
공백 글리프(⊠ 사고) 15종 전부 통과, 테스트가 앞으로도 검사한다.

**③즐겨찾기**: 계정마다 ★로 자주 쓰는 폰트를 맨 위로. 서버 저장(customer_prefs),
기본값은 fonts.json의 star. 폰트 고르는 곳 6군데 전부 적용하되 **정렬은 fontList() 한 곳**
(테스트가 HC_FONTS.map 잔존을 실패시켜 강제 — 실제로 내가 놓친 ⚡빠른 메뉴를 잡았다).
'안 고침'과 '별 다 끔'을 구분(후자는 기본값으로 안 돌아간다).

★**내가 낸 회귀 1건**: 썸네일 테스트가 6단계 구간만 잘라 Node로 돌리는데 그 안에서
바깥 함수 fontList()를 불러 ReferenceError 5건. 슬라이스 하네스 함정 **네 번째**.
→ thumbFontOptions()를 슬라이스 안에 두고 typeof 가드. **브라우저에선 안 걸린다.**

검증: 브라우저(playwright) 실측 — 순서·별렌더·서버저장·선택폰트 불변·되돌리기·빈목록 유지,
콘솔 오류 0. 전체 **5560 passed / 3 failed = 기준선 동일(회귀 0)**.
⚠️기존 실패 2건은 고정날짜 시한폭탄(2026-07-26 + reel_history 30일 롤링 = 오늘 만료), 별건.

⏭ 사장님 확인 2건: 기본 ⭐8종 확정 · 배포 승인.
