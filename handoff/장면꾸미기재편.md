# 장면꾸미기재편 — 6단계 화면 재편 (2026-09-03, 사장님 PC)

## 왜
사장님: "장면꾸미기에서 너무 정신없고 사람들이 어떤기능이 어디에있는지 잘모르고 산만하다는 말을 많이 한다"
실측(`produce.html` 2013~2841): 버튼 65·입력 76·드롭다운 13·탭 2벌·접힘 5단(중첩).
같은 값을 두 곳에서 만짐(⚡빠른조절 vs 직접다듬기 / 헤드카피 글씨 vs 템플릿 글자꾸미기).
감춰만 둔 죽은 섹션 6개(모션팩·강조단어·프리셋줄·추가텍스트·BGM·고급효과).

## 설계 (사장님 확인 중)
탭 3개 **자막 / 제목 / 장면**. 층은 탭→세부설정 2단. 각 탭 맨 위는 카드 고르기.
자리·확대중심·띠높이·[광고]위치는 미리보기에서 끌기만(슬라이더 0).
- 자막: 스타일카드 / 자리 / 글씨 → 세부(정렬·효과·이 장면 줄·프리셋)
- 제목: **틀 고르기(내용물 틀 29종 + 색띠 12 + 내그림 + 내틀)** / 문구·채널명 / 글씨·띠색 → 세부(띠높이·띠끝·하단띠·테두리·채널명·조회수)
- 장면: 확대 1×~3× / 강조 / 가림막·스티커 / [광고] → 세부(로고·오버레이·고급렌더)
정리표·목업: `out/장면꾸미기_재편_디자인.html`

## 작업대 (★사장님이 여기서 색감·질감·위치를 직접 다듬는다)
`out/장면꾸미기_작업대/` — `시작.bat`(바탕화면 `장면꾸미기_작업대.bat`) → http://127.0.0.1:8766
- `server.py`: 로컬 전용. **라이브 렌더 코드 `deco_frame.render`를 그대로 호출**해 PNG를 돌려준다.
  /presets(29종) /thumb/<id>(실제 프레임 위 합성) /render?spec=json(540x960) /tpl/tpl_NN.png /fonts
- `index.html`: 3탭 화면. 제목 탭은 값이 바뀔 때마다 실제 렌더를 다시 받는다(120ms 디바운스).
  미리보기: 제목 끌기=title_x/y, 띠 아래 선 끌기=bar_h. "📋 지금 값 복사"로 spec JSON을 뽑을 수 있다.
- `bg.jpg`: 서버 `data/find_frames/…mp4`에서 뽑은 실제 세로 프레임(자막 박힌 원본).
- 브라우저 실측: 탭·카드·틀 29종 전환·띠높이 끌기 동작 확인. 색띠 12는 PNG만 라이브, 글자는 HTML 근사치.
- 자막 탭·장면 탭은 아직 HTML 근사치(자막 스타일은 video_assemble 쪽이라 렌더 미연결).

## ⏭ 다음
1. 사장님이 작업대에서 눌러보고 확정 → 확정된 배치대로 `produce.html` 6단계를 옮긴다
   (저장 배선 유지, `test_deco_handlers_save.py` 감시. 죽은 섹션 6개 제거. 중복 조작 2건 통합)
2. 작업대 자막 탭을 실제 자막 렌더(video_assemble 규칙)로 연결할지 결정
3. 옛 시제품 `out/장면꾸미기_시제품.html`(바탕화면 사본 포함)은 작업대로 대체됨 — 지워도 된다

## 2026-09-03 오후 — 사장님 피드백 반영
- "스크롤 내리는 게 제일 문제" → 페이지 스크롤 없음(body overflow hidden), 오른쪽 패널만 내부 스크롤, 미리보기는 화면 높이에 맞춰 고정(엘리베이터). 틀 고르기는 5줄 세로 나열 → 칩(썰채널형/커뮤니티/빈틀/회색띠/색띠/내것) + 한 줄.
- "후킹 템플릿과 본문 템플릿을 썰쇼핑은 나눠야 한다"(참고 영상 youtube 9mtUoCs0tGM) → 제목 탭에 **후킹 틀 / 본문 틀** 토글 + 후킹 장면 수(기본 1). 장면 ◀▶로 넘기면 자동 전환. SPECS={hook,body} 두 벌. "후킹→본문 같게" 복사 버튼.
- 참고 영상 실측(첫 프레임만 확보): 반투명 검정 상단띠에 ☰·🔍, 굵은 2줄 헤드라인(흰+민트, 검은 외곽선), 그 아래 흰 자막바에 검은 글씨 한 줄. 본문 구간 프레임은 못 봤다 — yt-dlp 봇확인·자동화 브라우저 버퍼링·쿠키복사 권한거부. 사장님이 본문 구간 캡처를 주면 본문 틀 기본값을 거기에 맞춘다.

## 2026-09-03 저녁 — 사장님 4건 처리 (전제: ★숨겨놓아도 60대가 쓰기 편하게 — memory feedback_숨겨도_60대가_쓰기편하게)
| # | 요청 | 처리 | 어디 |
|---|---|---|---|
| 1 | 문구 비면 미리보기 칸이 안 생김 | 비면 회색 안내글 "여기에 제목을 적어주세요"로 자리를 그린다(최종본엔 안 나감) | 작업대 index.html hcPayload() |
| 2 | 문구 엔터로 줄 내려가게 | 헤드카피 렌더는 원래 \n을 존중. 화면이 헤드카피 레이어를 안 그리고 있던 게 문제 → /hc 엔드포인트로 라이브 함수 `video_assemble.headcopy_layer_png` 호출 | 작업대 server.py |
| 3 | 자간·행간은 접어서 고급옵션 | 렌더에 값이 없었음 → `_segmented_drawtext`에 leading(줄 사이, 기본 1.34)·tracking(글자 사이, 기본 0) 추가. 빈값이면 종전 그림과 동일. 화면은 세부 설정 「글자 사이 −＋ / 줄 사이 −＋ / ↩ 기본」 | 트랙 커밋 f3009992d |
| 4 | 채널별 폰트·크기 똑같이 | 이미 20채널 실측값(PRESETS[].headcopy: font·size·색·y·테두리)이 있었는데 틀 그림엔 안 쓰이고 있었다 → 틀 고르면 그 채널 헤드카피 세트가 자동으로 들어간다. 8채널 "확신"·12채널 "추정"(docs/reference/썰쇼핑_헤드라인글꼴.json) | 작업대 pickTpl |
- 작업대 구조: 미리보기 = bg.jpg + /render(틀, deco_frame) + /hc(헤드카피, video_assemble) 두 레이어. 흰 줄 문구(틀 제목)는 비우면 헤드카피 문구를 한 줄로 합쳐 쓴다.
- ⚠️ 관찰: 살림킹왕짱 프리셋 headcopy.box=true(흰 박스)인데 렌더에 박스가 안 보인다 — 라이브도 같은 함수라 같은 현상일 것. 미확인, 후속.
- ⚠️ 자간(tracking)은 이관 때 produce.html 미리보기(#hcPreviewText)에도 같은 값을 걸어야 미리보기≠렌더가 안 난다(0순위-B).

## ⏭ 다음
1. 사장님 작업대 확인 → "추정" 12채널 폰트를 원본과 대조해 확정(폰트 드롭다운으로 골라주면 그 값으로 PRESETS 갱신)
2. 본문 구간 캡처 받아 본문 틀 기본값
3. 확정되면 produce.html 6단계 이관(3탭·후킹/본문·헤드카피 자동세트·자간행간 세부) + finish

## 2026-09-03 — 자막 탭 재편 (사장님: "줄 나누기 기능 + 자리·줄이 장면마다·탭 갔다 와도 기억되게, 글씨체 진짜 목록")
- 서버 `/cap`(자막 한 줄, 라이브 `_segmented_drawtext` single_line 그대로) · `/segs`(라이브 줄 나누기 규칙 `_caption_segments`)
- 화면: CAPS={style(전체 공통), all(모든 장면에/이 장면만), beats[12]{text,lines,pos,y_pct,x_pct,cur}} — **localStorage 'deco_caps'** 에 매번 저장(새로고침·탭 이동 유지)
- 줄 나누기 칸은 라이브 `_capLineInput` 로직 그대로(Enter 끊기/Backspace 붙이기) + 이 장면 줄 저장 / ↩ 자동으로 / 다음 줄 보기
- 폰트 목록 = /fonts 실제 파일. 스타일 카드 5종 + ⭐내 스타일(저장). 세부: 테두리 두께·박스·투명도·효과·그림자·초기화
- 실측: 이 장면만+위 → 1번 위·2번 아래 유지, 제목 탭 갔다 와도 유지. 본문 장면엔 헤드카피 안내글 안 뜸(후킹만)
- ⏭ 이관 때: beats[].lines/pos는 라이브의 /caplines·/cappos API와 1:1. CAPS.all은 라이브 CAP_DRAG_SCOPE와 같음

## 2026-09-03 — "글자 치면 비율로 줄지 말고 썸네일처럼" (사장님)
- 뿌리: 헤드카피는 `fit_lines`(줄 수 유지, 폰트 축소). 썸네일은 처음 한 번 폭에 맞춰 크기를 잡고(THUMB_FIT_HOLD) 그 뒤엔 크기 유지·줄 늘림.
- 렌더: `style.wrap=True`면 축소를 건너뛰고 기존 `_wrap_to_width` 줄바꿈 경로(트랙 커밋 c2742ead7, 빈값=종전).
- 작업대: 틀 고르면 `/fit`로 한 번 폭에 맞춤(86→79 실측) → 이후 타이핑은 줄만 늘어남. 「↔ 폭에 맞추기」 버튼, 세부 설정에 「글이 길면: 줄을 늘린다/글자를 줄인다」.
- ⏭ 이관 때 produce.html 헤드카피 미리보기(#hcPreviewText)도 wrap 규칙을 같이 타야 한다(0순위-B).

## 2026-09-03 저녁 — "베스트 채널 템플릿 모양·배치까지 그대로" (사장님)
- ★실측 자료: 서버 `media_download.download_any`(주거용 프록시)로 10채널×3편 다운 → 편당 4프레임(1초·30·50·70%) 120장.
  바탕화면 `썰채널_실측프레임/` 에 채널별 모음 사진 10장. 서버 /tmp/capref/ 에 원본 프레임.
- 실측 결론: 후킹 = 띠 + (어둡거나 흰) 띠 위 2줄 헤드라인 + 흰 한 줄 박스 / 본문 = 띠 + 흰 블록(제목·조회수·자막이 그 안에).
  기존 PRESETS의 살림킹왕짱 bar=#FFFFFF는 틀림(실제 분홍 #EE7481, 흰 후킹 띠에 검은고딕 헤드라인).
- 렌더(트랙 ee0895bb1): `hook_band_h/color/alpha`, `sub_line/_h/_bg/_color`, `head_h` 추가. 빈값=종전.
- 데이터: `out/장면꾸미기_작업대/channel_layouts.json` 10채널 {hook:{spec,hc}, body:{spec,hc}, cap}. 카드에 ★ 표시. 고르면 후킹·본문·자막 세 벌 자동.
- 작업대 실측: 살림킹왕짱 후킹(분홍띠+흰띠+검은 헤드라인)·본문(분홍띠+흰블록+제목·조회수+검은 자막) 확인.
- 흰 줄 문구는 따로 적을 때만(복사 안 함). 본문 제목 = 후킹 문구 한 줄.
- ⚠️ 추정치: 폰트·자막 크기는 눈대중(Pretendard-SemiBold 30 등). 공가미 본문(어두운 블록+흰 자막띠)·살림장착(연파랑 큰 띠 안 자막·하단 회색띠 @채널명)·쇼핑천재는 근사.
- ⏭ 1) 사장님 10채널 확인·수정 2) 나머지 10채널도 같은 방법으로 3) 이관 때 channel_layouts → PRESETS 통합, 후킹/본문 두 벌 저장 구조
- (저녁 2차) 사장님 "완성품을 올리고 제목만 간단히 수정" → ★카드 고르면 그 채널 샘플 헤드라인 2줄·흰 박스·띠까지 통째로(channel_layouts[].sample). 어두운 띠 alpha 60→85~100(실측은 거의 불투명). 흰 박스는 띠 아래 경계에 걸침(트랙 커밋). 어느 화면에서 골라도 후킹 문구에 폭 맞춤(hcFit(SPECS.hook)).
- 실측 확인: 살림킹왕짱·럭키박스 후킹이 참고 프레임과 같은 구조로 뜸. 주의: 미리보기에서 끌면 hc.x가 바뀐다(가운데 되돌리기 없음 → 후속).
- (저녁 3차) 사장님 "훅은 큰제목·작은제목 구간, 작은제목 박스는 그라데이션 네모, 디테일" → 렌더에 sub_line_w(폭)·sub_line_soft(번짐)·sub_line_grad(아래로 옅어짐) 추가(트랙 커밋). 실측 세트: 박스 940px·번짐 6·그라데이션 25, 큰제목 테두리 10+그림자(shadow_d 4). 썰칩12·쇼핑치트키·럭키박스 합성으로 눈 확인.

## 2026-09-03 밤 — 나머지 9채널 실측 + 제목 탭 대제목/소제목 구조
- 서버에서 11~20번 채널 27편 프레임(집돌이 3편은 봇확인 실패). 모음 사진 바탕화면 `썰채널_실측프레임/11_~19_*.jpg`.
- channel_layouts.json 19채널. 새 유형: `body_keep_hc`(달래샵·꿀팁꿀템·요새난리 — 검은 상단 띠 헤드라인이 본문까지 유지, 자막은 검은 박스 흰 글씨) / 큰제목 없이 틀 제목만(나만또모르고있었지·코어장바구니 = hook.hc.text '').
- 후킹 띠 높이 자동(fitBand: 줄수×크기×1.34+100 → 띠가 작으면 키우고 헤드라인은 띠 가운데). /fit 안전여유 4%.
- 사장님 "대제목/소제목 구간으로 같은 기능, 복잡하지 않게, 본문틀도" → 제목 탭 = [대제목: 글·폰트·크기·폭맞춤·1줄색·2줄색·AI추천] + [소제목: 글·폰트·크기·글자색·박스색·채널명]. 후킹=hc/sub_line, 본문=title/조회수줄로 자동 매핑(라벨도 바뀜). 띠색·띠 위 글자는 세부 설정으로.
- 렌더(트랙 커밋): sub_line_font/sub_line_size — 소제목 글자 키우면 박스가 따라 자람. 브라우저 실측: 공가미 소제목 ＋4번 → 박스 커짐, 본문 라벨 전환 확인.
- ⏭ 사장님 19채널 대조 → 폰트/색 미세조정 → produce.html 이관(3탭·후킹/본문·대제목/소제목·자막 장면기억)

## 2026-09-03 밤 2차 — 사장님 "기본세팅·내 프리셋·박스로 묶기·채널명 동일 배치·마지막 상태 기억"
- 제목 탭 = 박스 5개: [어느 장면의 틀] [틀 고르기] [채널명: 글·폰트·크기·글자색·띠색] [대제목] [소제목] [저장] + 세부 설정. .sec 박스 스타일(파란 막대 제목).
- 저장 박스: ⭐내 프리셋으로 저장(localStorage deco_mypresets → 「내 것」칩에 카드) / 📌이 채널 기본값으로(deco_defaults → 그 틀 고르면 그 값으로 열림, 폭맞춤 생략) / ↩채널 원래 값.
- 마지막 상태 전부 기억: deco_title(hook·body·part·beatN·hookBeats) 렌더마다 저장, 열 때 복원. 자막은 deco_caps(기존).
- 큰 글씨 크기·글 바꾸면 서버 /fit?keep=1 이 실제 접힌 줄 수를 돌려주고 fitOne이 띠 높이를 다시 맞춤.
- 브라우저 실측: 새로고침 후 크기 89·틀 유지, "내 설정 2" 저장 메시지, 채널명 박스 맨 위.
- (밤 3차) 사장님 "칸 사이즈 레퍼런스와 동일·대제목 박스색 투명·채널/대제목 높이 간단·이름 통일" → 채널명 박스: 글자색·박스색(=옛 띠색)·높이 −＋ / 대제목 2줄째: 박스색·투명도 −＋·높이 −＋·↔글자에 맞춤. fitOne이 줄 수에 딱 맞게 늘고 줄음(+56px), 손으로 높이 만지면 _bandLock(부족할 때만 자동으로 키움). 세부 설정의 띠 높이 줄 삭제(중복 금지).
- (밤 4차) 사장님 "조절한 값을 기본값으로, 틀 이동했다 오면 리셋된다" → persistTitle이 렌더마다 DEF[preset]에 hook·body 자동 저장. 실측 없는 틀도 DEF 있으면 그 값으로. 📌 버튼 삭제(자동), ↩채널 원래 값 유지. 실측: 활용정점 투명도 55→75, 살림킹왕짱 갔다 오니 75 유지.
- (밤 5차) 폰트 드롭다운 = 라이브 produce.html 이름표(⭐티몬 몬소리·배민 주아·배민 도현·빙그레·… 40종) 순서 그대로, 목록에 없는 파일은 뒤에 파일명으로. 채널명·대제목·소제목·자막 4곳 공통. 줄 압축(select 150px·lbl 폭 자동). 본문 틀 소제목 = 조회수·댓글 2칸. 대제목 둘째 줄 = 박스색·투명도·높이(↔글자에 맞춤 제거).
