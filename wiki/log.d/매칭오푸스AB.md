# 매칭오푸스AB 로그

## 2026-09-02 (회사 PC, 새벽)
- 사장님 질문 "태깅 잘하는 거랑 매칭 잘되는 건 다른 문제 같다 / 오푸스가 판단하면 어떻게 되나"에서 출발.
- **오푸스는 구독으로 된다** — `claude -p --model opus` RC=0 실측(잡담 없이 JSON만). 선례:
  `pipeline/atoms/synth_engine.py:57`(Max 구독, API 과금 0원).
  ⚠️단 그 경로는 "신뢰불가로 폐기"(2026-06-22 골드페이지 파괴) — **매칭에선 재현 안 됨**
  (출력이 짧은 JSON이고 `_validate_and_ground`가 환각 제거. 호출 60여 회 파싱실패 0건).
  ⚠️**서버 배선은 불가** — 고객 트래픽을 구독으로 태우는 건 약관 문제(기술 문제 아님).
- 매칭이 지금 쓰는 모델 실측: **`gemini-3.1-flash-lite`**(comment_gen.py:22 → edit_plan.py:2303,3594).
- SSH 포트22 타임아웃(443은 200 — 사이트 정상, 회사PC 방화벽) → **서버 라이브 잡 못 가져옴**.
  로컬 `mix_jobs` **1건**뿐(409f894230c6). 출발점만 바꿔 케이스 25개로 합성.
- 하네스는 **프로덕션 함수 `_repick_weak_beats(call=...)`를 그대로 호출**하고 `call`만 교체(0순위-B).
  채점축도 프로덕션 `backbone.beat_role_mismatch` 그대로 — 새 기준을 지어내지 않았다.
- 중간 실측(20건): **opus 교정률 50%(20→10) / gemini 45%(20→11), 양쪽 나빠짐 0건.**
  속도는 opus 9.2초 vs gemini 3.0초(중앙).
- ★**가장 큰 단서 — `live_asis`는 두 모델 다 "미호출"이었다.** 5비트 전부 `fit=5`라
  재픽(대상 fit≤3)이 **아무도 안 부른다**. 그런데 문을 열면(fit=1) **제미니도 3→0**.
  → 라이브 훅에 '사용중'이 붙는 원인은 **모델 판단력이 아니라 fit 문턱**일 수 있다
  (08-18 "fit=5 자기신고 37건 중 26건이 실제 어긋남"과 일치). 처방 비용이 100배 다르므로
  **실험2(`run_gate.py`: 문턱 5→1을 훑는다)**를 추가해 밤새 이어 돌림.
- 실측 결함(잡 409f894230c6): 훅="반죽 정리하는 손"(사용중), CTA="설탕 붓고 핸드믹서"(사용중).
  **완성/after 재고가 14개 있었는데도** 전부 fit=5라 교정 0건.
- ⚠️한계: **표본이 잡 1건(레시피)**. "어느 모델이 잘 고르나"는 답하지만 "라이브 전체에서
  몇 % 좋아지나"는 못 답한다. SSH 되는 환경에서 잡 20건 넣고 재실행 필요.
- 라이브 영향 **없음** — `shopping_shorts/` 코드 한 줄도 안 고침(읽기만).

### 실측 완료 (같은 날 새벽, 실험1 150건 + 실험2 50건 rc=0)
- ★**결론: 처방은 모델 교체가 아니라 fit 판정 수정(원가 0).**
  실험2가 문턱만 바꿔 재픽시킨 결과 —
  `fit=5·4 → 모델 호출 0/5회, 어긋남 3.0→3.0` / `fit=3·2·1 → 호출 5/5회, 어긋남 3.0→0.0`.
  라이브는 전 비트가 fit=5라 **어느 모델도 호출되지 않는다**(오푸스를 붙여도 소용없음).
  문턱만 넘기면 **제미니도 어긋남 0**.
- 모델 성능(문 열어준 상태): 교정률 **opus 52%(135→65) / gemini 47%(135→72)**.
  역할별 잔존 어긋남 — **CTA opus 26% vs gemini 41%**, **훅 opus 17% vs gemini 22%**
  (훅·CTA는 오푸스 우세), problem만 opus 48% vs gemini 42%로 역전. 속도 11.4초 vs 2.5초.
- ⚠️**내가 중간에 "나빠짐 0건"이라 보고한 건 틀렸다** — 표본이 커지자 오푸스 13건.
  원인은 채점 방식: `blank`·`pad`처럼 출발이 '화면 없음'인 비트는 채점축이 판정을 보류해
  어긋남 0(만점처럼 보임)이 되고, **채우는 순간 무엇을 골라도 "나빠짐"**이 된다.
  집계에서 분리 + 별도 표로 노출하게 고쳤다.
- ★**조사 중 실제 코드 결함 발견(아직 안 고침)**: `_want_shots_for_role(role, available=)`는
  available을 주면 재고 없는 결에서 차선으로 내려가는데(problem: before·문제 → 사용중·조리),
  **호출 2곳 중 `edit_plan.py:3861`만 넘기고 `:3978`(재픽)은 안 넘긴다** → 재고 0건인
  `before·문제`를 그대로 지시. 3861엔 이 함정을 막는 주석까지 있다. **0순위-B 네 번째 재발.**
  ⚠️단 **고쳐 재실측해도 오푸스 선택은 안 바뀌었다**(짝비교 3쌍 동일, 프롬프트 변경은 로그로 확인)
  → 힌트 누락은 진짜 버그지만 **problem 증상의 원인은 아니다**. 그 대사가 "맛보더니 가져가도
  되냐고 매달려서"라 문제 상황이 아니라 자랑 → **채점축이 problem에 요구하는 결과 대사가
  어긋난 경우**로 보인다. 다음 세션은 채점축부터 점검할 것.
- 한계: 표본이 **라이브 잡 1건(레시피)**. SSH 포트22 막혀 서버 잡 못 가져옴 → 집 PC에서 20건 재실행 필요.
- 라이브 영향 없음(`shopping_shorts/` 무변경).
